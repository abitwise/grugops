# role-switch protocol (fixture)

A minimal fixture file. Not the real protocol — just enough to satisfy the validator's
checkRoleSwitchProtocol() existence check. The real grugops protocol lives at
agent-factory/roles/_role-switch-protocol.md in the kit root.

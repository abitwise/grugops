{
  "about": "33-R4 nested corpus (plan 33-35). Labelled Bash commands the prod-deploy / protected-branch guard is replayed against, read by scripts/checkpoints.test.ts and hooks/guard.test.ts. kind=deny: an in-scope row that must DENY. kind=deny-control: a control that already denied on the round-3 build and must still deny. kind=allow-control: must ALLOW (the union only adds denials). kind=residual: computed at run time, stays ALLOW, ledgered by plan 33-43. kind=handed-off: a live bypass outside this plan's class, pinned at its measured verdict and owned by the plan named in owner. Backslashes in command are literal printable characters once JSON-decoded. families: the nested wrapper shapes of 33-VERIFICATION gap 4 missing, each a template whose {{BODY}} is the single-quoted text <spliced tool> <governed verb> (an embedded single quote spelled as the four characters quote backslash quote quote); the tests expand each over COMMAND_CHECKPOINT_RULES x the CR-01 splice generator.",
  "families": [
    { "id": "bash-c-trailing-expansion", "template": "bash -c {{BODY}} $X" },
    { "id": "sh-c-trailing-expansion", "template": "sh -c {{BODY}} $X" },
    { "id": "eval-trailing-substitution", "template": "eval {{BODY}} $(true)" },
    { "id": "here-string", "template": "bash <<< {{BODY}}" }
  ],
  "rows": [
    { "id": "V4-01", "source": "33-VERIFICATION gap 4", "kind": "deny", "command": "bash -c 'g\\it push origin main' $X" },
    { "id": "V4-02", "source": "33-VERIFICATION gap 4", "kind": "deny", "command": "sh -c 'k\\ubectl -n prod apply -f x' $0" },
    { "id": "V4-03", "source": "33-VERIFICATION gap 4", "kind": "deny", "command": "eval 'g\\it -C . push origin main' $EMPTY" },
    { "id": "V4-04", "source": "33-VERIFICATION gap 4", "kind": "deny", "command": "bash -c 'n\\pm publish' $X" },
    { "id": "V4-05", "source": "33-VERIFICATION gap 4", "kind": "deny", "command": "bash -c 'h\\elm upgrade r ./c' $X" },
    { "id": "V4-06", "source": "33-VERIFICATION gap 4", "kind": "deny", "command": "bash -c 'g\\h pr merge 12' $(true)" },
    { "id": "V4-07", "source": "33-VERIFICATION gap 4", "kind": "deny", "command": "bash <<< 'g\\it push origin main'" },
    { "id": "V4-08", "source": "33-VERIFICATION gap 4", "kind": "deny", "command": "bash <<< 'n\\pm publish'" },
    { "id": "V4-09", "source": "33-VERIFICATION gap 4", "kind": "deny", "command": "bash <<< 'terra\"\"form apply'" },
    { "id": "V4-10", "source": "33-VERIFICATION gap 4", "kind": "deny", "command": "true; bash <<< 'g\\it -C . push origin main'" },
    { "id": "V4-C1", "source": "33-VERIFICATION gap 4 control (no opaque word)", "kind": "deny-control", "command": "bash -c 'g\\it -C . push origin main'" },
    { "id": "V4-C2", "source": "33-VERIFICATION gap 4 control (no splice)", "kind": "deny-control", "command": "bash -c 'git -C . push origin main' $X" },

    { "id": "R4-01", "source": "33-R4 beyond the list: ANSI-C quoted body", "kind": "deny", "command": "bash -c $'g\\\\it push origin main' $X" },
    { "id": "R4-02", "source": "33-R4 beyond the list: ANSI-C hex escape of the backslash", "kind": "deny", "command": "bash -c $'g\\x5cit push origin main'" },
    { "id": "R4-03", "source": "33-R4 beyond the list: expansion glued after the body", "kind": "deny", "command": "bash -c 'g\\it push origin main'$X" },
    { "id": "R4-04", "source": "33-R4 beyond the list: expansion glued before the body", "kind": "deny", "command": "bash -c $X'g\\it push origin main'" },
    { "id": "R4-05", "source": "33-R4 beyond the list: expansion splitting the name (read as empty)", "kind": "deny", "command": "bash -c 'g\\i'$X't push origin main'" },
    { "id": "R4-06", "source": "33-R4 beyond the list: expansion as a word boundary (X=';')", "kind": "deny", "command": "bash -c 'echo'$X'g\\it push origin main'" },
    { "id": "R4-07", "source": "33-R4 beyond the list: double-quote escape level", "kind": "deny", "command": "bash -c \"g\\\\it push origin main\" $X" },
    { "id": "R4-08", "source": "33-R4 beyond the list: double-quoted here-string", "kind": "deny", "command": "bash <<< \"g\\\\it push origin main\"" },
    { "id": "R4-09", "source": "33-R4 beyond the list: eval word concatenation, no opaque word (readable arm)", "kind": "deny", "command": "eval 'g\\it' push origin main" },
    { "id": "R4-10", "source": "33-R4 beyond the list: eval word concatenation beside an expansion", "kind": "deny", "command": "eval 'g\\it' push origin main $X" },
    { "id": "R4-11", "source": "33-R4 beyond the list: positional $@, no splice (readable arm)", "kind": "deny", "command": "bash -c 'git \"$@\"' _ push origin main" },
    { "id": "R4-12", "source": "33-R4 beyond the list: positional $0, no splice (readable arm)", "kind": "deny", "command": "sh -c 'kubectl -n prod \"$0\" -f x' apply" },
    { "id": "R4-13", "source": "33-R4 beyond the list: zsh equals under eval (readable arm)", "kind": "deny", "command": "eval '=kubectl' -n prod apply -f x" },
    { "id": "R4-14", "source": "33-R4 beyond the list: backtick trailing word", "kind": "deny", "command": "bash -c 'g\\it push origin main' `true`" },
    { "id": "R4-15", "source": "33-R4 beyond the list: process substitution trailing word", "kind": "deny", "command": "bash -c 'g\\it push origin main' <(true)" },
    { "id": "R4-16", "source": "33-R4 beyond the list: two nested shells", "kind": "deny", "command": "bash -c \"bash -c 'g\\it push origin main'\" $X" },
    { "id": "R4-17", "source": "33-R4 beyond the list: the flag-governed row nested", "kind": "deny", "command": "bash -c 'v\\ercel deploy --prod' $X" },
    { "id": "R4-18", "source": "33-R4 beyond the list: an expansion as a word boundary at the top level, under eval (X=';')", "kind": "deny", "command": "eval echo$X'g\\it' push origin main" },

    { "id": "AC-01", "source": "nested allow-control: echo under bash -c with a trailing expansion", "kind": "allow-control", "command": "bash -c 'echo hi' $X" },
    { "id": "AC-02", "source": "nested allow-control: echo through a here-string", "kind": "allow-control", "command": "bash <<< 'echo hi'" },
    { "id": "AC-03", "source": "nested allow-control: a commit message mentioning a governed verb", "kind": "allow-control", "command": "git commit -m 'push to main'" },
    { "id": "AC-04", "source": "nested allow-control: an echo of a variable", "kind": "allow-control", "command": "echo $X" },
    { "id": "AC-05", "source": "nested allow-control: a commit message naming two governed tools", "kind": "allow-control", "command": "git commit -m 'bump npm and git docs'" },

    { "id": "RES-01", "source": "computed at run time: the tool assembled from a shell variable", "kind": "residual", "owner": "33-43", "command": "K=git; bash -c \"$K push origin main\"" },
    { "id": "RES-02", "source": "computed at run time: the tool produced by a command substitution", "kind": "residual", "owner": "33-43", "command": "bash -c \"$(printf %s gi t) push origin main\"" },

    { "id": "LB-01", "source": "33-VERIFICATION live bypass: npm verb abbreviation (closed by plan 33-36 Task 1)", "kind": "deny", "command": "npm pub" },
    { "id": "LB-02", "source": "33-VERIFICATION live bypass: verb on stdin via xargs", "kind": "handed-off", "owner": "33-36", "command": "echo push origin main | xargs git" },
    { "id": "LB-03", "source": "33-VERIFICATION live bypass: verb on stdin via xargs", "kind": "handed-off", "owner": "33-36", "command": "echo publish | xargs npm" },
    { "id": "LB-04", "source": "33-VERIFICATION live bypass: verb on stdin via xargs", "kind": "handed-off", "owner": "33-36", "command": "echo 'apply -f x' | xargs kubectl" },
    { "id": "LB-05", "source": "33-VERIFICATION live bypass: git's dashed builtin binary", "kind": "handed-off", "owner": "33-43", "command": "/Library/Developer/CommandLineTools/usr/libexec/git-core/git-push origin main" },
    { "id": "LB-06", "source": "33-VERIFICATION live bypass: unmodelled push verb", "kind": "handed-off", "owner": "33-43", "command": "git send-pack origin main" },
    { "id": "LB-07", "source": "33-VERIFICATION live bypass: unmodelled merge endpoint", "kind": "handed-off", "owner": "33-43", "command": "gh api -X PUT repos/o/r/pulls/12/merge" },

    { "id": "RV-01", "source": "33-REVIEW CR-01 table", "kind": "deny", "command": "k\\ubectl -n prod apply -f x" },
    { "id": "RV-02", "source": "33-REVIEW CR-01 table", "kind": "deny", "command": "ku''bectl -n prod apply -f x" },
    { "id": "RV-03", "source": "33-REVIEW CR-01 table", "kind": "deny", "command": "h\\elm upgrade r ./c" },
    { "id": "RV-04", "source": "33-REVIEW CR-01 table", "kind": "deny", "command": "g\\it push origin main" },
    { "id": "RV-05", "source": "33-REVIEW CR-01 table", "kind": "deny", "command": "\"g\"it push --force origin main" },
    { "id": "RV-06", "source": "33-REVIEW CR-01 table", "kind": "deny", "command": "g''it push origin main" },
    { "id": "RV-07", "source": "33-REVIEW CR-01 table", "kind": "deny", "command": "n\\pm publish" },
    { "id": "RV-08", "source": "33-REVIEW CR-01 table", "kind": "deny", "command": "terra\"\"form apply" },
    { "id": "RV-09", "source": "33-REVIEW CR-01 table", "kind": "deny", "command": "g\\h pr merge 12" },
    { "id": "RV-10", "source": "33-REVIEW CR-01 table", "kind": "deny", "command": "=kubectl -n prod apply -f x" },
    { "id": "RV-11", "source": "33-REVIEW CR-01 table (zsh equals, likewise)", "kind": "deny", "command": "=git -C . push origin main" },
    { "id": "RV-12", "source": "33-REVIEW CR-01 table (zsh equals, likewise)", "kind": "deny", "command": "=gh pr merge 12" },
    { "id": "RV-C1", "source": "33-REVIEW CR-01 control", "kind": "deny-control", "command": "kubectl -n prod apply -f x" },
    { "id": "RV-C2", "source": "33-REVIEW CR-01 control", "kind": "deny-control", "command": "git push origin main" },
    { "id": "RV-C3", "source": "33-REVIEW CR-01 control", "kind": "deny-control", "command": "gh pr merge 12" }
  ]
}

---
column: Nonexistent Column
status: nonexistent-column
---
# ABC-001 fixture ticket (column is not a board heading)

The single mutation in this fixture: the ticket's `column:` ("Nonexistent Column")
is NOT one of the board headings in plans/board.md, so it must trip the
`boardHasColumn === false` branch and produce the "is not a board column" error.
The `status:` is the kebab of the column (`nonexistent-column`), so the SEPARATE
status<->column mismatch check stays silent — this fixture isolates the
column-membership branch (WR-02).

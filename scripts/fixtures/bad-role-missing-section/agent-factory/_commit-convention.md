# commit convention (fixture)

A minimal fixture file. Not the real convention — just enough to satisfy the validator's
checkCommitConvention() existence check. The real grugops convention lives at
agent-factory/_commit-convention.md in the kit root.

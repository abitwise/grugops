# Plan 33-28 style dispositions (WINDOWS.md row 255 / CAP-01 KIT (a), 33-DIAGNOSIS § 1.3 (i))

Plan 33-28 carries the plugin's MCP admission tool in the `--agent` coordinator adapter's grant,
through the generator, and wires the admission-guard hook to the tool name the platform actually
calls. Two watched files change. The coordinator role declares one more capability token; the
adapter is generated from it and is not itself in the watched corpus. Workflow 16 step 3 names the
admission tool as the platform spells it in plugin form, with the bare name stated as the server's
own name, because the platform exposes a plugin's bundled MCP server under the scoped name
`mcp__plugin_<plugin>_<server>__<tool>` and a matcher written against the bare server key never
fires (code.claude.com/docs/en/plugins-reference). An agent reading the step should read the name
its session lists.

**The judgement is written here; the table below is only its coverage.** The gate reported five
findings before this file existed; nothing else in the watched corpus moved.

```
headline        FAIL  diff disposition — changed watched file(s): 5 finding(s) over 39 elements
finding         agent-factory/roles/orchestrator.md:4 (added) — no disposition row
finding         agent-factory/roles/orchestrator.md:4 (removed) — no disposition row
finding         agent-factory/workflows/16-context-read-write.md:31 (added) — no disposition row
finding         agent-factory/workflows/16-context-read-write.md:31 (added) — no disposition row
finding         agent-factory/workflows/16-context-read-write.md:32 (added) — no disposition row
exit code       1
```

**The base commit is `4d2b8f079cc43d7d6184729966492789fb4dc05e`**, read from `00-base.md`. It is not
moved by this plan.

## Dispositions

| file | line | before | after | rule | disposition | companion |
|---|---|---|---|---|---|---|
| agent-factory/roles/orchestrator.md | 4 | capabilities: read edit shell | capabilities: read edit shell admit | KIT (a) (plan 33-28, 33-21 decision checkpoint, WINDOWS.md row 255) / CLAUDE.md single-source | **changed.** A frontmatter value, not prose: the coordinator declares the `admit` capability token, which the generator maps to the platform's scoped MCP admission tool `mcp__plugin_grugops_grugops__propose_note` and emits last on the adapter's `tools:` line. The role text is the single source of the grant; the generated adapter is regenerated, never hand-edited. No other role declares the token, because nested role sessions do not receive plugin MCP tools (round-1 capture A:784). | `scripts/generate-role-adapters.ts` gained the `admit` row and `agent-factory/packaging/subagent.frontmatter.md` its table row and governing fact 3 in the same commit; `.claude/agents/grugops-orchestrator.md` was regenerated (one line changed). |
| agent-factory/workflows/16-context-read-write.md | 31 | On Claude Code the sanctioned admission path for a verified note is the STRUCTURED tool `mcp__grugops__propose_note`. | On Claude Code the sanctioned admission path for a verified note is the STRUCTURED tool `mcp__plugin_grugops_grugops__propose_note`. | KIT (a) (plan 33-28, WINDOWS.md row 255) / WP-02 | **changed.** The tool is named as the platform spells it in plugin form — the name the round-1 init frame listed (A:11) and the name a `--agent` session receives once the adapter grants it. 16 words, within the procedural bound. | The admission-guard matcher in `hooks/hooks.json` covers the same scoped spelling in the same plan. |
| agent-factory/workflows/16-context-read-write.md | 31 | — | The scoped name is the platform's name for the plugin's bundled server; `mcp__grugops__propose_note` is the server's own name. | KIT (a) (plan 33-28, WINDOWS.md row 255) / WP-02 | **added.** The bare name the step used to carry is kept as a fact about the server, so a reader who meets it in the server's own source or in a standalone `.mcp.json` setup knows the two names are one tool. 15 words, within the procedural bound. | `agent-factory/contracts/context-note.md` states the same two-name fact beside the hook sentence in the same commit. |
| agent-factory/workflows/16-context-read-write.md | 32 | That hook gates the `mcp__grugops__propose_note` channel. | That hook gates the `mcp__plugin_grugops_grugops__propose_note` channel and the bare `mcp__grugops__` spelling. | KIT (a) (plan 33-28, WINDOWS.md row 255) / WP-02 | **changed.** The sentence now says what the matcher matches: both spellings, as one alternation (`mcp__(plugin_grugops_)?grugops__.*`). Before this plan the sentence described a gate that, in plugin form, the platform documents as never firing. 12 words, within the procedural bound. | `hooks/hooks.json` matcher change, `hooks/admission-guard.ts` comment, `scripts/floor-invariance.test.ts` K7 and `hooks/admission-guard.test.ts` K8 in the same commit. |

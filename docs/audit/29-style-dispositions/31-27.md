# Plan 31-27 style dispositions (CR-17 / S1 / R-31-19-07 / D-29)

Every clause plan `31-27` changes in the watched corpus is recorded here, one row per clause, per the
contract in this directory's `README.md`. `scripts/check-diff-disposition.js` reads this directory on
every run and refuses any changed clause that has no row.

## Why this file changed

Plan `31-27` closes `CR-17` of `31-REVIEW.md` — the PreToolUse wrapper's own unbounded manifest reads,
independently reproduced by `31-VERIFICATION.md` round 6 at EXIT=124 with zero bytes on both streams —
and lands structural fix `S1`, which adds a HOST-DELIVERED **tier 0** to the governance-root resolution
order and re-scopes `TRUSTED_ROOT_RESIDUALS` by host. Decision **D-29** records both.

`agent-factory/workflows/16-context-read-write.md` is one of the three places that must agree about the
resolution order. Before this plan it published that order only inside step 3's paragraph, as five
sentences a reader had to reconstruct into a precedence. That was already a documentation shape this
project has learned not to trust: `TRUSTED_ROOT_STOP_CONDITIONS` is asserted equal to its prose in both
directions precisely because a restated rule drifts from the rule it restates.

Adding a tier to the program without a published order would have made the drift certain, so this plan
adds a `## The governance-root resolution order` section whose five numbered tier lines are asserted
EQUAL to the exported `TRUSTED_ROOT_TIERS` in both directions — every tier the program publishes appears
in the prose, and every numbered tier line in the prose is one the program has. A sixth tier therefore
cannot arrive in prose alone, and a program tier cannot arrive undocumented.

The section also states what tier 0 does NOT establish, because the closure is host-scoped: the channel
it rests on exists on the Claude Code hook path and on no other host, and it closes nothing about an
agent invoking `scripts/context-io.js` directly from its own Bash tool. A closure claimed globally when
it holds on one host of five is the shape "never fake a passing gate" refuses.

**Sentence length.** The section is written in short sentences because the Phase 29 writing profile bounds
a procedural sentence at 20 words and a descriptive one at 25, and `guard_sentence_form` measured the
first draft of this section over both bounds. The prose was split rather than the corpus narrowed.

**Scope note.** The rows below cover the clauses this plan ADDED, at lines 50-62. Clauses reported at
lines 22, 32 and in the stop-conditions section are pre-existing prose whose line numbers shifted because
this plan inserted a section above them; they are dispositioned by the plans that wrote them and are not
re-claimed here.

## Dispositions

| file | line | before | after | rule | disposition | companion |
|---|---|---|---|---|---|---|
| agent-factory/workflows/16-context-read-write.md | 52 | — | Every tier of the resolution order is quoted from `TRUSTED_ROOT_TIERS` in `scripts/context-io.ts`. | D-29 (3) | Added. Names the export the prose is quoted from, so a reader can check the claim rather than trust it — the same shape `TRUSTED_ROOT_STOP_CONDITIONS` already uses one section below. | `TRUSTED_ROOT_TIERS` in `scripts/context-io.ts` gains the five-step order this section introduces, in the same commit, so the section and the export are born together rather than one describing the other after the fact. |
| agent-factory/workflows/16-context-read-write.md | 52 | — | Every tier the program publishes appears here. | D-29 (3) | Added. The first of the equality's two directions, stated as its own sentence so neither direction can be dropped without the sentence disappearing. | `scripts/context-io.test.ts` gains the both-directions equality case in the same commit, which is what turns this quotation into a checkable claim instead of a promise about a file the reader would have to open. |
| agent-factory/workflows/16-context-read-write.md | 52 | — | Every numbered tier line here is one the program has. | D-29 (3) | Added. The second direction. One direction alone lets the prose grow a sixth tier, or lets the program grow one the prose never mentions. | `scripts/context-io.test.ts` gains the both-directions equality case in the same commit, which is what turns this quotation into a checkable claim instead of a promise about a file the reader would have to open. |
| agent-factory/workflows/16-context-read-write.md | 52 | — | Neither side can move without the other. | D-29 (3) | Added. The consequence of the two directions, written where a reader deciding whether to edit either side will see it. | — |
| agent-factory/workflows/16-context-read-write.md | 52 | — | A sixth tier cannot arrive in prose alone. | D-29 (3) | Added. Names the first failure the equality prevents, concretely rather than as a property. | — |
| agent-factory/workflows/16-context-read-write.md | 52 | — | A program tier cannot arrive undocumented. | D-29 (3) | Added. Names the second failure, which is the one this plan would otherwise have committed by adding tier zero to the program alone. | — |
| agent-factory/workflows/16-context-read-write.md | 54 | — | It is read from `GRUGOPS_HOST_DELIVERED_ROOT` on the decider's own spawn environment. | D-29 (3) | Added. Tier zero's channel and its name. The name is stated so the delivered channel and the two ambient project-directory names cannot be confused for one another. | `hostDeliveredRoot` in `scripts/context-io.ts` is added in the same commit and reads exactly the name this line publishes, so the prose and the program name one channel rather than two. |
| agent-factory/workflows/16-context-read-write.md | 54 | — | It is accepted only when it canonicalises to an existing directory. | D-29 (3) | Added. The first conjunct of the acceptance predicate, which is what makes tier zero a narrowing rather than a new way to name a root. | `scripts/context-io.test.ts` gains the monotonicity differential in the same commit, driving one value set through tier one's predicate and tier zero's and asserting the accepted set is a strict subset, so this sentence is measured rather than asserted. |
| agent-factory/workflows/16-context-read-write.md | 54 | — | That directory carries a version-control marker. | D-29 (3) | Added. The second conjunct. Written without a modal because a bullet in this position is read as procedural and the profile bans modals there. | `scripts/context-io.test.ts` gains the monotonicity differential in the same commit, driving one value set through tier one's predicate and tier zero's and asserting the accepted set is a strict subset, so this sentence is measured rather than asserted. |
| agent-factory/workflows/16-context-read-write.md | 54 | — | It is not the kit's own root. | D-29 (3) | Added. The third conjunct. The kit is not a governed project, which the walk already refuses one tier down. | `scripts/context-io.test.ts` gains the monotonicity differential in the same commit, driving one value set through tier one's predicate and tier zero's and asserting the accepted set is a strict subset, so this sentence is measured rather than asserted. |
| agent-factory/workflows/16-context-read-write.md | 54 | — | Otherwise this tier answers nothing and the next one runs. | D-29 (3) | Added. States the fall-through, so a reader knows an unusable delivered value costs nothing rather than producing a bad root. | — |
| agent-factory/workflows/16-context-read-write.md | 54 | — | This tier exists on the **Claude Code hook path only** | D-29 (3) | Added. The scope, stated as a limit rather than left to be inferred from silence. Four of five host CLIs have no such channel. | `TrustedRootResidual` gains its `hosts` field in the same commit, so the scope this line states in prose is the same scope the register carries member by member. |
| agent-factory/workflows/16-context-read-write.md | 54 | — | That host builds the environment of the hook subprocess it spawns. | D-29 (3) | Added. The first half of the reason the channel is trustworthy. A reader who cannot check the reason cannot check the tier. | `hooks/hook-entry.ts` gains the delivery block and its own header argument in the same commit, so the reason stated here is stated again at the code that depends on it being true. |
| agent-factory/workflows/16-context-read-write.md | 54 | — | `hooks/hook-entry.js` is byte-frozen and hash-verifies the decider before it runs. | D-29 (3) | Added. The second half of the reason, naming the two mechanisms a reader can go and verify for themselves. | `hooks/hook-entry.ts` gains the delivery block and its own header argument in the same commit, so the reason stated here is stated again at the code that depends on it being true. |
| agent-factory/workflows/16-context-read-write.md | 54 | — | Those two facts are what make the channel one the agent cannot write. | D-29 (3) | Added. States that the trust in tier zero is exactly those two facts and nothing more, so the claim cannot quietly widen. | `hooks/hook-entry.ts` gains the delivery block and its own header argument in the same commit, so the reason stated here is stated again at the code that depends on it being true. |
| agent-factory/workflows/16-context-read-write.md | 54 | — | Codex, Gemini CLI, OpenCode and Copilot CLI deliver nothing here. | D-29 (3) | Added. Names the four hosts the tier does not serve, so a reader on one of them is not left guessing. | — |
| agent-factory/workflows/16-context-read-write.md | 54 | — | The tiers below are their answer. | D-29 (3) | Added. Tells a reader on one of those four hosts which mechanism does answer for them. | — |
| agent-factory/workflows/16-context-read-write.md | 57 | — | the configuration that governs the process working directory | D-29 (3) | Added as a numbered tier line. The rule is unchanged; what changes is that it is now one of five lines asserted equal to the program. | — |
| agent-factory/workflows/16-context-read-write.md | 57 | — | It is the repository root's own where the upward search reaches a boundary carrying one. | D-29 (3) | Added. Unchanged rule, split into its own sentence to clear the procedural bound the writing profile applies to a numbered line. | — |
| agent-factory/workflows/16-context-read-write.md | 57 | — | It is bounded above as the section below states. | D-29 (3) | Added. A pointer rather than a restatement, so the stop set keeps exactly one published home and this section cannot become a second, drifting copy of it. | No code companion: this line deliberately points at the existing stop-conditions section instead of restating it, so the stop set keeps exactly one published home in this file. |
| agent-factory/workflows/16-context-read-write.md | 60 | — | **What tier 0 does not establish.** It closes the tier that decides whether a force push runs. | D-29 (what it does not establish) | Added. The limit of the closure, stated where the closure is claimed. Six review rounds on this phase have turned on a fix claimed more broadly than it held. | — |
| agent-factory/workflows/16-context-read-write.md | 60 | — | It closes nothing about a process acting as itself. | D-29 (what it does not establish) | Added. The sharpest form of the limit, so the disclosure survives being skimmed. | — |
| agent-factory/workflows/16-context-read-write.md | 60 | — | An agent can invoke `scripts/context-io.js` directly from its own Bash tool. | D-29 (what it does not establish) | Added. Names the concrete capability tier zero does not remove, in the terms an agent would actually exercise it. | — |
| agent-factory/workflows/16-context-read-write.md | 60 | — | That agent builds its own environment and can set that name. | D-29 (what it does not establish) | Added. Says why that capability is untouched, rather than leaving the reader to work out the mechanism. | — |
| agent-factory/workflows/16-context-read-write.md | 60 | — | It is an already-accepted capability, recorded as `R-31-15-01`. | D-29 (what it does not establish) | Added. Binds the disclosure to the register member that already prices it, so the same capability is not re-discovered later as a new finding. | `R-31-15-01` in `TRUSTED_ROOT_RESIDUALS` is rewritten in the same commit to name tier zero as its closure on this host and to name the hosts it is not closed on, so the disclosure and the register agree. |
| agent-factory/workflows/16-context-read-write.md | 60 | — | Members of `TRUSTED_ROOT_RESIDUALS` that tier 0 closes here carry `hosts: non-cc-hook-path`. | D-29 (4) | Added. Publishes the register's new host scoping in the workflow, so a reader of the prose and a reader of the register learn the same value. | `TrustedRootResidual` gains its `hosts` field in the same commit, so the scope this line states in prose is the same scope the register carries member by member. |
| agent-factory/workflows/16-context-read-write.md | 60 | — | They remain open on the four hosts where no such channel exists. | D-29 (4) | Added. States what that value MEANS, because a field name alone is a label rather than a disclosure. | `TrustedRootResidual` gains its `hosts` field in the same commit, so the scope this line states in prose is the same scope the register carries member by member. |
| agent-factory/workflows/16-context-read-write.md | 60 | — | Members that stand everywhere carry `hosts: all`. | D-29 (4) | Added. The other value, so the two-member vocabulary is complete in the prose as well as in the type. | `TrustedRootResidual` gains its `hosts` field in the same commit, so the scope this line states in prose is the same scope the register carries member by member. |
| agent-factory/workflows/16-context-read-write.md | 50 | — | The governance-root resolution order | D-29 (3) | Added. A section heading, because the order was previously only reconstructible from a step-3 paragraph and a tier was being added to it. | `TRUSTED_ROOT_TIERS` in `scripts/context-io.ts` gains the five-step order this section introduces, in the same commit, so the section and the export are born together rather than one describing the other after the fact. |
| agent-factory/workflows/16-context-read-write.md | 55 | — | `CLAUDE_PROJECT_DIR` when present and non-empty after trimming, made absolute. | D-29 (3) | Added as a numbered tier line. The rule is unchanged from the step-3 paragraph; what changes is that it is now one of five lines asserted equal to the program. | — |
| agent-factory/workflows/16-context-read-write.md | 56 | — | `GRUGOPS_PROJECT_DIR`, the documented installer-set variable, under the same predicate. | D-29 (3) | Added as a numbered tier line, same disposition as the line above: an unchanged rule in a newly checkable form. | — |
| agent-factory/workflows/16-context-read-write.md | 58 | — | the shared kit this reader ships in, whose shipped dial is the lean default | D-29 (3) | Added as a numbered tier line. Unchanged rule, recorded as a tier so the count of tiers is five in both the prose and the program. | — |

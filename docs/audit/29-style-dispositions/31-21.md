# Plan 31-21 style dispositions (CR-12 / WR-22 / D-24 — written by plan 31-30, round 6, wave 4)

Every clause plan `31-21` changed in the watched corpus is recorded here, one row per clause, per the
contract in this directory's `README.md`. `scripts/check-diff-disposition.js` reads this directory on
every run and refuses any changed clause that has no row.

## Who wrote this file, and why that is stated first

**Plan `31-21` did not write it. Plan `31-30` did**, on 2026-09-10, in gap-closure round 6 wave 4. The
reason, the derivation and the standing argument it answers are stated in full in `31-23.md`'s
identically-titled section; they are not restated here.

**The derivation, for THIS file.** `attributeClauses()` was run over the watched corpus at commit
`8cff6d8`. Fourteen undispositioned clauses at `agent-factory/workflows/18-context-compaction.md:83`
carry `b3f666f`/`8cde300` (`31-21`) together with `daa5e58`/`a81ba78` (`31-29`) in their carrier set.
The paragraph is ONE LINE, so `git diff --unified=0` marks every clause on it changed by every carrier
that touched the line; `31-21` AUTHORED these fourteen and `31-29` rewrote two neighbouring sentences
and moved them. They are claimed here on authorship. The two sentences `31-29` genuinely authored on
that line are recorded in `31-22.md`'s "What is NOT in this file" section with their owner.

## What the change was

`CR-12` of `31-REVIEW.md`, reproduced by `31-VERIFICATION.md` round 5: an unguarded `readFileSync` at
the note-write chokepoint hung on a FIFO. `D-24` made every read in `scripts/context-io.ts` go through
ONE non-blocking regular-file reader, and — the half these sentences document — extended the same rule
to the GOV-02 audit ledger, so an admission the audit trail cannot record is REFUSED rather than
granted unrecorded.

The paragraph these rows cover is the ledger contract stated for a reader: which routes write a ledger
event, in which ORDER relative to the note, what the re-binding does when the id is already recorded,
and — the part that matters most and is easiest to omit — which asymmetry the ordering deliberately
chooses when the two writes cannot be atomic.

**Line 83 is NOT inside a frozen structural section**, which the gate confirms by reporting these
fourteen as `no disposition row` rather than as `FROZEN by structuralSections`. The nine frozen
clauses in this file are all at line 75 and belong to `31-22.md` and `31-29`. So the `companion` cell
here is `—`, and that is a derived fact rather than a judgement.

## Dispositions

| file | line | before | after | rule | disposition | companion |
|---|---|---|---|---|---|---|
| agent-factory/workflows/18-context-compaction.md | 83 | — | Under `audit_retention: retained`, two routes write both a note and a GOV-02 ledger event. | D-24 | Added. Opens the contract with the DIAL VALUE it holds under. A ledger paragraph that does not name its precondition reads as unconditional, and the last sentence of this same paragraph exists to deny exactly that reading. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | The routes are the re-binding route (`promoteAdmitted`) and the admit-then-persist route (`admitAndAppend`). | D-24 | Added. Names both routes by their function identifiers, so a reader checking the claim has somewhere to check it. Naming one would leave the other reading as exempt, which is how a rule stated about "the writer" comes to be held by one writer. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | On both routes the ledger event is appended before the note is written. | D-24 | Added. The ORDER, stated once for both routes. This is the sentence the rest of the paragraph justifies; without it the two later asymmetry sentences are commentary on nothing. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | When that ledger already records the id, the re-binding appends nothing. | D-24 | Added. The idempotence half of the re-binding rule. A re-binding that appended unconditionally would manufacture a second admission event for one admission, which is a fabricated audit record rather than a duplicate row. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | When it does not, the re-binding appends one event marked `re_bound`. | D-24 | Added. The other half, with the exact marker value rather than a description of it. A reader reconciling a ledger by hand needs the literal, and a paraphrase is what makes two readers disagree about the same file. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | The mark keeps the event distinguishable from a fresh admission. | D-24 | Added. Gives the marker its REASON. Without it the mark reads as bookkeeping, and bookkeeping is the first thing a later edit drops. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | The converse is reachable, and is the deliberately safe direction. | D-24 | Added. Announces that what follows is a disclosed failure mode rather than a guarantee. The honesty floor is the reason this sentence exists: the two writes cannot be atomic, and a paragraph that implied otherwise would be the claim this repository forbids. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | A crash or a refused write between the two steps can leave a ledger line for an unwritten note. | D-24 | Added. States the reachable inconsistency concretely, including the refused-write path — which is the one a reader would not think of, because a refusal feels like nothing happened. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | A stray ledger line is an over-record: legible, and reconcilable against the notes directory. | D-24 | Added. Names why that direction is the safe one, in the two properties that make it safe. "Reconcilable against the notes directory" tells an operator what to actually do about it. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | A note with no ledger line would instead be a repudiation. | D-24 | Added. The other direction, named with the security word rather than described. A note nobody can find an audit record for is exactly the state the whole GOV-02 ledger exists to make impossible, and the contrast is what justifies the ordering. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | The two steps cannot be made atomic, so the ordering chooses which asymmetry is possible. | D-24 | Added. The conclusion the three sentences above earn, and the honest statement of the constraint. It says the design picked a failure mode rather than eliminating one, which a reader can check and disagree with. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | A destination ledger that is present and cannot be read refuses the promotion by name. | D-24 | Added. The read-side half of the same rule: an unreadable ledger is a NAMED refusal, not a silent skip. "Present and cannot be read" is the precise condition — an ABSENT ledger is an ordinary first write and is not this case. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | The refusal is deliberate: the response to "not recorded" is to append, and a wrong answer would manufacture a duplicate event. | D-24 | Added. Gives the refusal its reason, and the reason is the one a reader would otherwise get backwards: failing OPEN here does not lose an event, it invents one. | — |
| agent-factory/workflows/18-context-compaction.md | 83 | — | Under any other `audit_retention` value no ledger is kept, and this paragraph says nothing about that case. | D-24 | Added. Bounds the whole paragraph's scope explicitly, which is what makes the opening dial sentence a precondition rather than a decoration. Stating what a paragraph does NOT cover is the same honesty rule as stating what a constraint does not refuse. | — |

## What is NOT in this file, and who owns it

The four residuals `R-31-21-01` … `R-31-21-04` live in `31-CONTEXT.md`'s D-24 block and in no exported
register — `docs/audit/31-round5-residuals.md` §10.5 measured that, and it is carried in
`deferred-items.md` with owner `31-21`. It is a REGISTER gap, not a disposition gap, and nothing in
this file closes or claims to close it.

The other clauses this gate reports against `agent-factory/workflows/18-context-compaction.md` are
recorded in `31-22.md` (line 56 and the frozen line-75 stop condition) and in `31-22.md`'s "What is
NOT in this file" section (the five `31-29` clauses whose rows exist but match nothing).

**`00-base.md`'s recorded `base_commit` (`4d2b8f0`) was NOT moved and the watched corpus was NOT
narrowed.**

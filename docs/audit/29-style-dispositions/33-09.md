# Plan 33-09 style dispositions (WINDOWS.md row 225 / CAP-02)

The 21 clauses `guard_diff_disposition` reports on this tree are recorded here, one row per clause,
per the contract in this directory's `README.md`. Every one of them is in `README.md`, and none of
them was written by this plan. They were left by three commits that landed between the recorded base
and the dispatch of Phase 33, and they have sat undispositioned since because the gate runs after the
vitest step on the ubuntu leg and that step has been red on every run since 2026-07-15 — the gate
was never reached on CI, and the three commits did not run it locally.

Plan 33-09 is the plan whose bar is BOTH CI legs exiting 0 on a pushed run (CAP-02, D-13). The gate
runs at `.github/workflows/ci.yml` in the ubuntu-only repository-gates step, so with these rows
absent the ubuntu leg would go red at this gate before any Windows class is measured. That is why the
file is written under this plan's number rather than back-dated into the plans that changed the
text: the trail records WHEN the obligation was discharged, which is the point of a trail
(precedent: `30-10.md`).

**The judgement is written here; the table below is only its coverage.** A row proves that a changed
clause was considered. It proves nothing about whether the consideration was right. That is a named
human reading these rows against the diff they describe, which is what LANG-03's manual verification
is and what no gate reaches.

## What forced the change, stated before what was done

`npm run check:diff-disposition` was run against THIS tree before a row was written. Its complete
stdout is filed with its exit code in
`.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` under the pre-push
inventory.

```
headline        FAIL  diff disposition — changed watched file(s): 21 finding(s) over 39 elements
derived         grep -c '^        clause: '                          -> 21
derived again   grep -cE '^ +README\.md:[0-9]+ \((added|removed)\)'  -> 21
exit code       1
```

The three agree at **21**. All 21 name `README.md`; no other watched file has a finding.

**`over 39 elements` is unchanged** from `32.1-03.md`'s capture, recorded for the reason that file
recorded it: a finding count that falls is consistent BOTH with rows being written AND with the
comparison seeing less than it saw before. The element count holding steady tells those apart.

**The base commit is `4d2b8f079cc43d7d6184729966492789fb4dc05e`**, read from `00-base.md` and reported
in the short form by the running gate. It is not moved by this plan.

## The three carriers, and why the gate saw only part of each

`git log 4d2b8f07..HEAD -- README.md` names four commits. `b2b76128` (plan 30-09's pointer line) is
already dispositioned in `30-10.md`. The other three carry the 21:

| carrier | what it changed | findings | why the rest of its diff was already admitted |
|---|---|---|---|
| `6a604a6c` — `release: v2.1 — bump artifact version to 2.1.0` (human decision 2026-09-18, reversing D-28) | line 26, the version line | 1 (the REMOVED side) | the added `2.1.0` clause is claim registry anchor C-28-004, and the registry row changed in the same commit, so the frozen-intersection rule admitted it; the removed `0.1.0` clause is anchored by nothing and owed a row |
| `22c66700` — `docs(readme): install recipe shows the clone-then-install shape with --target` | lines 29–36, the install step | 8 (2 removed, 6 added) | the reworded anchor sentences (C-28-005, C-28-006) changed with their registry rows in the same commit and were admitted; the two REMOVED sides, the two new code-block lines and the four sentences of the new paragraph were not anchored and owed rows |
| `52377a7a` — `docs(readme): add example Claude Code commands for each grugops skill` (quick task 260919-007) | lines 50–62, the example block | 12 (all added) | nothing in the block is a registered claim; every clause owed a row |

`1 + 8 + 12 = 21`, so the tally accounts for every finding and invents none.

**No finding is frozen.** The gate reported every one as `no disposition row`, not as a frozen
intersection without its companion; the `companion` cells below are filled only where the row's
OTHER side is a registry anchor, and they name the same-commit registry edit that already satisfied
the frozen rule, so a reader can see the pair.

**What this file does not do.** It does not touch `README.md`, does not move the base, does not
narrow the watched corpus and does not loosen the clause comparison. The remedy the gate itself
prints is the row, and the row is what was written.

## Dispositions

| file | line | before | after | rule | disposition | companion |
|---|---|---|---|---|---|---|
| README.md | 26 | grugops version `0.1.0`. | grugops version `2.1.0`. | v2.1 release (human decision 2026-09-18, reversing D-28) | **reworded.** The artifact version moved from the pre-release seed to the first published SemVer release; the README states the version the `VERSION` file, `plugin.json` and `package.json` carry. The removed side is recorded here because only the added side was a registry anchor. | `docs/audit/28-claim-registry.md` row C-28-004 changed in the same commit (`6a604a6c`) |
| README.md | 29 | run the idempotent, additive, reversible installer (Node 22+) from the repo root: | clone grugops once, then run its idempotent, additive, reversible installer (Node 22+) against the project you want it in: | `22c66700` (install recipe) / WP-09 | **reworded.** The old clause told the reader to run the installer from the grugops checkout, which is the one place the installer refuses; the new clause names the clone-then-install shape the installer supports. The three installer properties keep their names (WP-09). The removed side is recorded here because only the added side was a registry anchor. | `docs/audit/28-claim-registry.md` row C-28-005 changed in the same commit (`22c66700`) |
| README.md | 32 | node install/install.js | git clone https://github.com/abitwise/grugops.git ~/grugops | `22c66700` (install recipe) | **replaced.** A code-block line, not prose: the in-checkout invocation is replaced by the clone step of the recipe. Both sides are recorded on one row because one line replaced the other. | — |
| README.md | 33 | — | node ~/grugops/install/install.js --target /path/to/your-project | `22c66700` (install recipe) | **added.** A code-block line, not prose: the installer invoked by absolute path against the target project, which is the shape `install/README.md` documents. | — |
| README.md | 36 | — | From inside the project you can drop `--target`; the installer then asks you to confirm the current directory. | `22c66700` (install recipe) / WP-03 | **added.** States the installer's measured behaviour when `--target` is absent (a confirmation prompt), so the recipe does not imply the flag is mandatory. 18 words, within the descriptive bound. | — |
| README.md | 36 | — | It refuses to install into the grugops checkout itself. | `22c66700` (install recipe) / WP-03 | **added.** Names the refusal the old recipe would have hit, so a reader who runs the old command understands the outcome. Behaviour, not permission: it opens no surface outside the installer's own checks. | — |
| README.md | 36 | — | The shared kit lands in `~/.grugops` and only per-repo state is written into your project. | `22c66700` (install recipe) / WP-03 | **added.** States where the shared install lands (the v1.1 shared-install design, `docs/design/shared-install.md`) so the reader knows what the target project receives. | — |
| README.md | 36 | — | Every flag, including `--check` and `--update`, is in [`install/README.md`](install/README.md). | `22c66700` (install recipe) / WP-10 | **added.** A pointer, not a duplicated flag list: the flags stay documented once, in the installer's own README. | — |
| README.md | 36 | The installer never overwrites or deletes your content; re-running it is safe, and `node install/uninstall.js` removes only what was added. | The installer never overwrites or deletes your content; re-running it is safe, and `node ~/grugops/install/uninstall.js --target /path/to/your-project` removes only what was added. | `22c66700` (install recipe) | **reworded.** Only the uninstall invocation changed, to the same clone-then-run shape as the install line; the safety statement (never overwrites, never deletes, re-run safe, removes only what was added) is byte-for-byte the same. The removed side is recorded here because only the added side was a registry anchor. | `docs/audit/28-claim-registry.md` row C-28-006 changed in the same commit (`22c66700`) |
| README.md | 50 | — | Example commands (Claude Code), one per grugops skill: | `52377a7a` (quick task 260919-007) | **added.** The lead-in to the example block; names the host the examples are written for and the one-per-skill shape, so the block is read as coverage of the seven skills rather than as a random sample. | — |
| README.md | 53 | — | `/grugops "add password reset by email"                      # dispatcher: the Orchestrator routes it` | `52377a7a` (quick task 260919-007) / WP-09 | **added.** A code-block line: the dispatcher skill with a sample request, and a trailing comment naming what the skill does in the skill's own words (its `description`). Not prose in the running text. | — |
| README.md | 54 | — | `/grugops-map "survey this repo and seed the factory board"  # greenfield scaffold or brownfield survey` | `52377a7a` (quick task 260919-007) / WP-09 | **added.** A code-block line: the map skill with a sample request; the comment restates the skill's `description`. | — |
| README.md | 55 | — | `/grugops-plan "turn the checkout epic into ready tickets"   # idea to epics, or epic to tickets` | `52377a7a` (quick task 260919-007) / WP-09 | **added.** A code-block line: the plan skill with a sample request; the comment restates the skill's `description`. | — |
| README.md | 56 | — | `/grugops-ticket "T-042"                                     # ready ticket to branch, gate, PR; stops at the PR` | `52377a7a` (quick task 260919-007) / WP-09 | **added.** A code-block line: the ticket skill with a sample ticket id; the comment restates that the skill stops at the pull request, which is the humans-decide boundary. | — |
| README.md | 57 | — | `/grugops-gate "run the PR gate on the current branch"       # install, lint, typecheck, unit, build, e2e; recommendation only` | `52377a7a` (quick task 260919-007) / WP-09 | **added.** A code-block line: the gate skill; the comment names the gate's steps and that its output is a recommendation, never a merge. | — |
| README.md | 58 | — | `/grugops-uat "build the UAT pack for the checkout epic"     # acceptance scenarios a human runs` | `52377a7a` (quick task 260919-007) / WP-09 | **added.** A code-block line: the UAT skill; the comment states that a human runs the scenarios. | — |
| README.md | 59 | — | `/grugops-release "cut v1.3.0"                               # a named human must approve any production deploy` | `52377a7a` (quick task 260919-007) / WP-09 | **added.** A code-block line: the release skill; the comment restates the hard safety constraint (named human approval before any production deploy) in the same words the release skill and `CLAUDE.md` use. It adds no permission and withholds none. | — |
| README.md | 62 | — | The plugin-form equivalents are the same words after the colon | `52377a7a` (quick task 260919-007) / WP-09 | **added.** The first clause of the closing sentence: ties the standalone examples to the plugin-form names the paragraph above already introduced, so one concept keeps one name in both forms. | — |
| README.md | 62 | — | `/grugops:map`, `/grugops:ticket`, `/grugops:release`, and so on. | `52377a7a` (quick task 260919-007) / WP-09 | **added.** The enumeration half of the same sentence: three plugin-form spellings as examples of the mapping, ending in "and so on" because the paragraph above states the general rule. | — |
| README.md | 62 | — | Each one starts at the Orchestrator and ends at a pull request or a human approval; merge and deploy stay with you. | `52377a7a` (quick task 260919-007) / WP-03 | **added.** Restates the humans-decide boundary at the point where a reader has just seen seven commands: every skill ends at a PR or a human approval, and merge and deploy are the human's. 20 words, within the descriptive bound. It duplicates the boundary the release comment states, deliberately, because this sentence covers all seven skills and the comment covers one. | — |

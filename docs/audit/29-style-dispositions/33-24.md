# Plan 33-24 style dispositions (WINDOWS.md row 236 / CAP-02, 33-REVIEW WR-05)

One clause changed in the watched corpus. Plan 33-24 routed the env tier of `trustedRepoRoot`
(`scripts/context-io.ts`) through `canonicalDirectoryPath` — the one ladder the working-directory
tier and tier 0 already use — instead of a bare `resolve` (decision D-33-R3-01). `TRUSTED_ROOT_TIERS[1]`
describes that tier's mechanism, and `scripts/context-io.test.ts` asserts the workflow's five tier
lines equal to that export in both directions, so the one mechanism sentence in the prose changed
with the program in the same commit.

**The judgement is written here; the table below is only its coverage.** The gate reported the added
side of the clause with `no disposition row`; the removed side (`made absolute.`) is already a row in
`31-27.md`, which is why only one row is owed.

```
headline        FAIL  diff disposition — changed watched file(s): 1 finding(s) over 39 elements
finding         agent-factory/workflows/16-context-read-write.md:55 (added) — no disposition row
exit code       1
```

**The base commit is `4d2b8f079cc43d7d6184729966492789fb4dc05e`**, read from `00-base.md`. It is not
moved by this plan.

## Dispositions

| file | line | before | after | rule | disposition | companion |
|---|---|---|---|---|---|---|
| agent-factory/workflows/16-context-read-write.md | 55 | `CLAUDE_PROJECT_DIR` when present and non-empty after trimming, made absolute. | `CLAUDE_PROJECT_DIR` when present and non-empty after trimming, made absolute and canonicalised through the one ladder. | D-33-R3-01 (plan 33-24, 33-REVIEW WR-05 (b)) | **reworded.** The tier's mechanism changed in the program: the value is no longer a bare lexical `resolve` but the same canonicaliser the working-directory tier and tier 0 use, so a link-spelled or 8.3-spelled project directory names the directory the other tiers would name. The prose says what the program does. The presence predicate (present, non-empty after trimming) and the tier's rank are unchanged. 17 words, within the procedural bound. | `TRUSTED_ROOT_TIERS[1]` in `scripts/context-io.ts` carries the same sentence and changed in the same commit; the test asserts the two lists equal in both directions. |

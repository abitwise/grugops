# Plan 33-25 style dispositions (WINDOWS.md row 256 / CAP-01 KIT (b), 33-DIAGNOSIS § 1.3 (ii))

One clause was added to the watched corpus. Plan 33-25 made the shared-context reader refuse a note
the sanctioned writer did not compose: `scripts/context-io.ts` seals every note it composes (a
`seal:` line, last inside the fence) and the one walk every reader goes through files an unsealed
note under the reader-owned skip arm `unsealed`. Workflow 16 step 3 already said "never author the
context path by hand"; the added sentence states the read-side consequence of doing so, in the
register the workflow uses, so an agent reading the step learns what happens to a hand-written note
rather than only that it is forbidden.

**The judgement is written here; the table below is only its coverage.** The gate reported the added
sentence with `no disposition row`; nothing was removed, so one row is owed.

```
headline        FAIL  diff disposition — changed watched file(s): 1 finding(s) over 39 elements
finding         agent-factory/workflows/16-context-read-write.md:31 (added) — no disposition row
exit code       1
```

**The base commit is `4d2b8f079cc43d7d6184729966492789fb4dc05e`**, read from `00-base.md`. It is not
moved by this plan.

## Dispositions

| file | line | before | after | rule | disposition | companion |
|---|---|---|---|---|---|---|
| agent-factory/workflows/16-context-read-write.md | 31 | — | A note written by any other route (a file-writing tool, a heredoc, an editor) is refused on read as `unsealed`. | KIT (b) (plan 33-25, 33-21 decision checkpoint, WINDOWS.md row 256) / WP-02 | **added.** The step's rule ("record ONLY through the sanctioned writer") gains its mechanical consequence: the reader refuses a note the writer did not compose and counts it under the `unsealed` arm, which `agent-factory/contracts/context-note.md` documents under the new `seal` field. The sentence names the route generically ("a file-writing tool") rather than by tool token, because `guard_context_writes` (SCTX-05) reads a `Write` token beside the context path as a raw write. 18 words, within the procedural bound; the first draft (34 words) was split down to this one. | `agent-factory/contracts/context-note.md` gained the `seal` row and the residual paragraphs in the same commit; the stop-condition bullet naming `context-io.ts` as the sanctioned writer is unchanged. |

# Plan 31-19 style dispositions (WR-21)

Every clause plan `31-19` changes in the watched corpus is recorded here, one row per clause, per the
contract in this directory's `README.md`. `scripts/check-diff-disposition.js` reads this directory on
every run and refuses any changed clause that has no row.

## Why this file changed

Plan `31-19` closes `WR-21` of `31-REVIEW.md`, reproduced independently by the round-4 verification.
`31-15` gave `trustedRepoRoot()` a four-step resolution order whose third step walks upward from the
working directory. Its only stop conditions were a configuration found, an ancestor carrying `.git`,
the filesystem root, and 64 ancestors. **None of those is the user's home directory** — while the
function's own docstring said the walk "never continues past" a boundary so a resolution "can never
reach a user's home directory", and `agent-factory/workflows/16-context-read-write.md:32` repeated
the claim to every reader of the protocol.

The prose was a live defect rather than a documentation lag, in the direction that matters most: it
asserted a bound the mechanism did not have. Measured against the committed `scripts/context-io.js`
before any source change, with both project-directory variables removed and a working directory
three levels below a home-directory-shaped ancestor carrying `.grugops/factory.config.json` and no
repository marker anywhere on the path, `trustedRepoRoot()` returned the planted ancestor and read
its `human_admission: "all"`. A second probe drove an admission from the same shape and the GOV-02
event landed in the planted directory's own `.grugops/audit/admissions.jsonl`.

Two clauses therefore had to move, and one section had to arrive:

- **The stop sentence** stated a bound the code did not have. It is replaced by a pointer to the one
  place the stop conditions are now stated, rather than by a corrected restatement — a second
  spelling of the rule in prose is what let the first one drift.
- **The step-3 sentence** named only the nearest-ancestor rule. `31-19` also decided WR-21's second
  half: a repository root's own configuration outranks one nested inside it, so a vendored kit's
  in-repo configuration no longer lowers a host repository's dial for any process whose working
  directory sits under it.
- **`### Where the upward governance-root search stops`** is new. Its five bullets are quoted from
  `TRUSTED_ROOT_STOP_CONDITIONS` in `scripts/context-io.ts`, and `scripts/context-io.test.ts`
  asserts the two set-equal in **both** directions, with a watched-fail control that seeds an extra
  stop on each side and shows the equality break. That assertion is the structural half of the fix:
  correcting a sentence without binding it leaves the next drift unobserved, which is how WR-21 came
  to be documented as impossible.

The section is written in CLEAR voice. The project's voice discipline reserves the caveman register
for text that is not a safety explanation, and where a governance root resolution stops is one.

## What this file does NOT cover

Ten further clauses in `agent-factory/workflows/16-context-read-write.md` are reported by
`check:diff-disposition` and are **not** rows here. They are plan `31-15`'s — the sentences naming
steps 1, 2 and 4, the dial paragraph and the degradation paragraph — which this plan does not
change and cannot state a `before`/`after` for. Writing rows for another plan's clauses would put a
reason in the register this plan cannot vouch for, which is the precedent
`.planning/phases/31-autonomous-manual-testing/deferred-items.md` already records for the same gate.
They are added to that file instead.

Measured counts: `npm run check:diff-disposition` reported **65** findings before this plan (all of
them naming `05-pr-quality-gate.md`, `06-uat-pack.md` or `17-task-claim.md`) and **86** with this
plan's workflow edits in the tree and no rows written. With these 11 rows it reports **75** — the 65
pre-existing plus the 10 inherited `31-15` clauses named above, and **0** owed for the clauses this
plan changed.

## Dispositions

| file | line | before | after | rule | disposition | companion |
|---|---|---|---|---|---|---|
| agent-factory/workflows/16-context-read-write.md | 32 | The upward search stops at the first ancestor carrying a repository marker, so it never reaches a user's home directory. | The upward search is bounded above, and every condition that stops it is stated once in the section below. | D-23 | Replaced because it was FALSE, not because it read badly: the marker stop fired only if some ancestor happened to carry `.git`, so the "never reaches a user's home directory" conclusion did not follow from it. The replacement makes no claim of its own and points at the one place the stop conditions are stated, so the prose cannot outrun the mechanism a second time. | — |
| agent-factory/workflows/16-context-read-write.md | 32 | The third is the nearest ancestor of the working directory carrying a factory configuration. | The third is the configuration that governs the working directory: the repository root's own where the search reaches one, else the nearest ancestor carrying a factory configuration. | D-23 | Rewritten for WR-21's second half, which `31-19` decided rather than named. A vendored kit's `agent-factory/config/factory.config.json` used to win over the host repository's own for any process whose working directory sat under it — a governance dial lowered to the kit's lean default by changing directory. A repository root's own configuration now outranks a nested one, and the sentence says so. | — |
| agent-factory/workflows/16-context-read-write.md | 45 | — | ### Where the upward governance-root search stops | D-23 | New heading. It exists so the stop conditions have ONE location a reader and a test can both address; the test locates the region by this exact heading and asserts the region is bounded rather than running to end-of-file. | — |
| agent-factory/workflows/16-context-read-write.md | 47 | — | Every stop condition of step 3's upward search, quoted from `TRUSTED_ROOT_STOP_CONDITIONS` in `scripts/context-io.ts`. | D-23 | Names the export the list is quoted from, so a reader who doubts the prose has the mechanism's address rather than an assurance. | — |
| agent-factory/workflows/16-context-read-write.md | 47 | — | `scripts/context-io.test.ts` asserts this list equal to that export in both directions, so a stop the code gains and a stop this list names cannot come apart. | D-23 | States the binding, in both directions, because a one-directional assertion passes while the prose claims a bound that is not there — which is precisely the WR-21 defect. The claim is checkable: the case and its watched-fail control are named in the summary. | — |
| agent-factory/workflows/16-context-read-write.md | 47 | — | The bound is the home-directory stop, which holds whatever markers a filesystem carries; the version-control markers say where a repository starts, which is a different question. | D-23 | Separates the two questions the old sentence conflated. The marker set is CONTENT and is open by nature (`R-31-19-04`); the bound is a property of the walk. Saying so here is what stops a later reader re-deriving the non-sequitur from the marker list. | — |
| agent-factory/workflows/16-context-read-write.md | 49 | — | The upward search never inspects the user's home directory, and never inspects any ancestor of it. | D-23 | Quoted verbatim from the `S-HOME` member of `TRUSTED_ROOT_STOP_CONDITIONS`. It is the stop WR-21 found claimed and absent. | — |
| agent-factory/workflows/16-context-read-write.md | 50 | — | When the home directory cannot be determined, the upward search does not run at all. | D-23 | Quoted verbatim from `S-HOME-UNKNOWN`. An undeterminable home is not a licence to walk past it, so the search degrades to the kit answer — the answer the pre-`31-15` program gave unconditionally — rather than to an unbounded walk. | — |
| agent-factory/workflows/16-context-read-write.md | 51 | — | The upward search ends at the first ancestor carrying a version-control marker, and that ancestor's own configuration wins over one nested below it. | D-23 | Quoted verbatim from `S-BOUNDARY`. It carries both halves of the boundary rule in one sentence, because the second half is the inner-configuration decision and a reader who learns only the first half re-derives the vendored-kit answer. | — |
| agent-factory/workflows/16-context-read-write.md | 52 | — | The upward search ends at the filesystem root. | D-23 | Quoted verbatim from `S-ROOT`. Unchanged behaviour, stated because the set is asserted complete in both directions and a stop the code has must be named. | — |
| agent-factory/workflows/16-context-read-write.md | 53 | — | The upward search inspects at most 64 ancestors. | D-23 | Quoted verbatim from `S-STEPS`, whose sentence INTERPOLATES the constant rather than typing it. Changing the limit moves the exported sentence, which turns the both-directions assertion red until this line moves with it, and a suite case drives the published number against the walk's real depth so the number cannot be decorative. | — |

# Plan 29-05 style dispositions

The first seven role files rewritten onto the canonical role skeleton: `## One job` owns the what as
one sentence, `## Caveman prompt` owns identity and attitude, `## Responsibilities` owns the how, and
`## Hard limits` is the only place a prohibition appears (D-19). The caveman block is the section that
gives up its content (D-09), the second-person copula opener is rewritten rather than exempted (D-41),
and every role gains one when-absent config fallback sentence in `## Reads` (D-30) — added only after
that role's removals landed and were measured (D-37).

Every file in this plan is on the `docs/audit/28-safety-surface-exclusions.md` list. Each row below is
therefore a deliberate sentence-level judgement, recorded here rather than a file-level assumption.

Three clause groups repeat across the rows and are worth reading once rather than seven times.

- **The `## Reads` config bullet clauses.** Appending the D-30 sentence changes the whole bullet LINE,
  so the gate re-derives every clause on that line. The clauses named `Unchanged text` below are
  byte-identical before and after; they carry a row because the line moved, not because the words did.
- **The fallback sentence itself** is intentionally identical in all seven files. The uniqueness guard
  is intra-file (D-21), so that repetition is correct single-sourcing rather than duplication.
- **A deleted caveman line or `## Responsibilities` item whose clause is FROZEN.** `## Hard limits` is
  frozen positionally, and a restatement of one of its clauses elsewhere in the file normalizes to the
  same frozen clause. Deleting the restatement is the D-19 remedy, and the companion cell records that
  `## Hard limits` keeps the clause unchanged and becomes its sole home.

## Dispositions

| file | line | before | after | rule | disposition | companion |
|---|---|---|---|---|---|---|
| agent-factory/roles/software-engineer.md | 9 | Implement one ticket — pull the shared context first, make a small diff, add tests, run checks, and update docs. | Implement one ticket end to end. | D-19 | `## One job` states the what as one act. The pull-first, small-diff, tests, checks and docs sequence is the how and is already owned by `## Responsibilities` items 1 and 2, so it is dropped here rather than restated. | — |
| agent-factory/roles/software-engineer.md | 9 | pull the shared context first, make a small diff, add tests, run checks, and update docs. | — | D-19 | Removed from `## One job`. Every act it names survives verbatim in `## Responsibilities` items 1 and 2, which own the how. | — |
| agent-factory/roles/software-engineer.md | 9 | You stop if scope grows or the architecture must change. | — | D-19 | The trailing stop-clause sentence is deleted from `## One job`. It is a prohibition, and `## Hard limits` is the only section that may carry one. | `## Hard limits` keeps `Stop and hand back if scope grows or the architecture must change.` unchanged in the same commit, and is now the sole home of this prohibition. |
| agent-factory/roles/software-engineer.md | 14 | You are Software Engineer. | You grug with sharp stick, not with big brain. | D-41, D-09, LANG-06 | The copula opener is rewritten, not exempted — `are` is a banned construction in every block, and a first-line carve-out would be a second grammar for one position. The replacement is identity and attitude and states no fact. | — |
| agent-factory/roles/software-engineer.md | 15 | You make a small diff. You add tests. You run checks. You update docs. | Big rewrite wake demon. | D-09, LANG-06 | The factual line is removed; the same facts are stated once, in clear voice, in `## Responsibilities` items 1 and 2 and in `## Hard limits`. What replaces it is attitude about diff size, not a fact. | — |
| agent-factory/roles/software-engineer.md | 15 | — | Small diff keep demon asleep. | D-09, LANG-06 | Added attitude line. Carries no fact; the small-diff rule itself lives in `## Hard limits`. | — |
| agent-factory/roles/software-engineer.md | 15 | You read the shared context first. | — | D-09 | Removed from the caveman block. The fact is stated once in clear voice in `## Reads` and in `## Responsibilities` item 1. | — |
| agent-factory/roles/software-engineer.md | 16 | You stop if scope grows or architecture must change. | Fake green summon worst demon of all. | D-09, D-20 | The block's stop-clause restatement is deleted — it was one side of a `guard_role_clause_uniqueness` duplicate group against `## One job`. What replaces it is attitude about the no-fabrication rule that `## Hard limits` states in clear voice. | `## Hard limits` keeps `Stop and hand back if scope grows or the architecture must change.` and `Never fake a test result, a passing check, or a command output` unchanged in the same commit. |
| agent-factory/roles/software-engineer.md | 20 | `.grugops/factory.config.json` **first** | `.grugops/factory.config.json` **first** | D-30 | Unchanged text. Reported because the D-30 sentence was appended to this bullet line. | — |
| agent-factory/roles/software-engineer.md | 20 | `mode` / `cadence` / `autonomy` / `wip_limits` / `quality` / `nfr` / `compliance_regime`. | `mode` / `cadence` / `autonomy` / `wip_limits` / `quality` / `nfr` / `compliance_regime`. | D-30 | Unchanged text. Reported because the D-30 sentence was appended to this bullet line. | — |
| agent-factory/roles/software-engineer.md | 20 | Autonomy picks `diff` / `branch` / `pr`. | Autonomy picks `diff` / `branch` / `pr`. | D-30 | Unchanged text. Reported because the D-30 sentence was appended to this bullet line. | — |
| agent-factory/roles/software-engineer.md | 20 | — | With no config file present, this role runs lean on the defaults documented in `agent-factory/config/factory.config.md`. | D-30 | Added. Closes the defect where zero role files stated the when-absent behaviour while two public claims rested on an agent inferring it. Written as a statement of behaviour, not a prohibition, so it does not open a second permission surface outside `## Hard limits`. | — |
| agent-factory/roles/software-engineer.md | 33 | Stop and hand back if scope grows or the architecture must change | — | D-19, WP-10 | `## Responsibilities` item 4 is deleted whole. It was nothing but a prohibition wearing a step number, and it was the second side of the `guard_role_clause_uniqueness` duplicate group at lines 33 and 45. | FROZEN intersection. `## Hard limits` retains this sentence byte-unchanged in the same commit and is now its only home; the permission withheld is identical, stated once instead of twice. |
| agent-factory/roles/software-engineer.md | 33 | quietly absorbing it hides a decision a human should make. | — | D-19 | The rationale tail of the deleted `## Responsibilities` item 4. It explained the prohibition, so it leaves with it. | — |

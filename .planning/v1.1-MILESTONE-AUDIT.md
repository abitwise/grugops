---
milestone: v1.1
milestone_name: Install & Distribution
audited: 2026-06-08T11:51:37Z
status: tech_debt
scores:
  requirements: 8/8
  phases: 3/3
  integration: solid
  flows: 4/4
gaps: # Critical blockers — NONE
  requirements: []
  integration: []
  flows: []
tech_debt: # Non-critical, deferred — needs review, does not block close
  - phase: 07-shared-home-foundation-path-rewrite
    items:
      - "SUMMARY.md frontmatter for 07-01..07-04 does not list requirements-completed (all []) — SHOME-01..04 are covered by 07-VERIFICATION.md requirements table with grep evidence + the live integration check, but the 3-source SUMMARY leg is empty (hygiene gap, not a coverage gap)"
      - "Nyquist VALIDATION.md is status: draft, nyquist_compliant: false — formal validation layer not completed (behavioral coverage IS strong: check-kit-refs.sh green + structural validator green)"
      - "WR-01..WR-04 (07-REVIEW.md): scripts/check-kit-refs.sh has latent false-green channels (per-line grep hides multi-ref leaks; `|| true` swallows missing-path errors; unanchored ALLOW regex; MARKER_SITES lists 4 of 19 marker-carrying files) — gate works correctly today, hardening recommended"
      - "WR-05 (07-REVIEW.md): packaging templates (subagent.frontmatter.md, slash-command.template.md) still prescribe the Agent tool + 'spawn sub-agents' prose, contradicting the deployed adapters and _role-switch-protocol.md (No Agent tool). REGENERATION HAZARD: a future installer regen from these templates would silently re-introduce spawn capability"
      - "IN-01 (07-REVIEW.md): agent-factory/README.md + factory.config.md carry stale agent-factory/config/factory.config.json paths + 'Orchestrator spawns role agents' language — intentionally deferred out of Phase 7 scope (RESEARCH O2), still unaddressed"
  - phase: 08-two-root-installer
    items:
      - "Nyquist VALIDATION.md is status: planned, wave_0_complete: false (nyquist_compliant: true but tasks not confirmed run) — formal validation layer not executed"
      - "IN-01 (08-REVIEW.md): install.sh `head -n 1` without `tr -d '\\r'` would embed CRLF into the marker kitVersion on Windows autocrlf checkouts (committed VERSION is LF; hypothetical) — accepted out-of-scope"
      - "IN-02 (08-REVIEW.md): agent-factory/seed/plans/board.md references agent-factory/config/factory.config.json#wip_limits instead of .grugops/ (seed excluded from kit-ref gate by D-03; byte-identical config; UX confuser only) — accepted out-of-scope"
  - phase: 09-doctor-two-root-validator
    items:
      - "Nyquist VALIDATION.md is status: planned, wave_0_complete: false — formal validation layer not executed (behavioral coverage IS strong: install.test.sh 18/18 + validate.test.sh 18/18 green live)"
scope_notes:
  - "SHOME-04 plugin-form half (${CLAUDE_PLUGIN_ROOT} resolver block in .claude/skills/grugops/SKILL.md) is NOT implemented — this is the explicitly v2+-deferred PLUGIN-01 (REQUIREMENTS.md line 138), NOT an in-scope gap. The standalone home (installer-materialized absolute path) is fully wired. The ONE-rule convention naming both homes is stated. v1.1 scope of SHOME-04 is met."
nyquist:
  compliant_phases: 0
  partial_phases: 3 # 07 (draft/false), 08 + 09 (planned, tasks not run)
  missing_phases: 0
  overall: partial
cross_milestone_context: # Out of v1.1 scope but relevant at close
  - "v1.0 (phases 1-6) was NEVER formally archived (archive_count: 0). v1.1 continued numbering at phase 7."
  - "DOG-02 (v1.0 / Phase 6) is Partial: the Claude-Code-native dogfood half (live sub-agent spawn + dual-path parity) is deferred to milestone-close UAT — cannot be honestly self-run by an agent. Tracked in 06-HUMAN-UAT.md, never fabricated."
  - "Open-artifact audit (milestone-close) surfaces 6 v1.0-era live-CC UAT/verification items (Phase 05: D-31 plugin-cache resolution + SAFE-02 live hook firing; Phase 06: same + CC sub-agent parity) and 1 stale quick-task marker (260606-0my, actually completed at commit 6a66994)."
---

# Milestone v1.1 "Install & Distribution" — Audit Report

**Audited:** 2026-06-08
**Status:** `tech_debt` — all 8 requirements satisfied, integration solid, no blockers; accumulated non-critical debt needs review before/at close.

## Scope

v1.1 continued phase numbering from v1.0. Milestone phases:

| Phase | Name | Requirements | VERIFICATION |
|-------|------|--------------|--------------|
| 07 | Shared-Home Foundation & Path Rewrite | SHOME-01, SHOME-02, SHOME-03, SHOME-04 | passed 5/5 |
| 08 | Two-Root Installer | INSTALL-03, INSTALL-04 | passed 5/5 |
| 09 | Doctor + Two-Root Validator | INSTALL-05, VAL-02 | passed 5/5 |

**Definition of done (from ROADMAP/REQUIREMENTS):** kit installs once to `${GRUGOPS_HOME:-$HOME/.grugops}` (read-only/central), per-repo state lives under the target repo (`.grugops/`, `plans/`, `memory-bank/`), the ~31 role/workflow/adapter files resolve kit vs state correctly (grep-to-zero gate), the installer materializes the absolute kit path + seeds state without clobbering, and a `--check` doctor + two-root validator refuse to false-green.

## Requirements Coverage (3-Source Cross-Reference)

Sources: (V) phase VERIFICATION.md requirements table · (S) SUMMARY.md `requirements-completed` frontmatter · (R) REQUIREMENTS.md traceability table.

| REQ-ID | Phase | V | S | R | Final | Notes |
|--------|-------|---|---|---|-------|-------|
| SHOME-01 | 07 | SATISFIED | — (empty) | `[x]` | **satisfied** | V table + live integration prove it; S frontmatter empty (hygiene) |
| SHOME-02 | 07 | SATISFIED | — (empty) | `[x]` | **satisfied** | same — zero `agent-factory/config/` refs confirmed live |
| SHOME-03 | 07 | SATISFIED | — (empty) | `[x]` | **satisfied** | same — `check-kit-refs.sh` exits 0 live |
| SHOME-04 | 07 | SATISFIED | — (empty) | `[x]` | **satisfied** | standalone home wired; plugin-form half = v2+ PLUGIN-01 (scope note) |
| INSTALL-03 | 08 | SATISFIED | listed (08-03/04) | `[x]` | **satisfied** | `--target` + prompt + `--yes`/non-TTY; harness [8][9] PASS |
| INSTALL-04 | 08 | SATISFIED | listed (08-03/04) | `[x]` | **satisfied** | seed/copy-default/idempotent/parity; harness [1]–[7][11][12] PASS |
| INSTALL-05 | 09 | SATISFIED | listed (09-01/03/04/05) | `[x]` | **satisfied** | doctor sh+Node, first-failure, exit matrix; install.test.sh 18/18 |
| VAL-02 | 09 | SATISFIED | listed (09-02/04/06) | `[x]` | **satisfied** | two-root, no `.` fallback, C3 guard live, fail-closed; validate.test.sh 18/18 |

**8/8 satisfied. Zero unsatisfied. Zero orphaned.** (Orphan check: every traceability REQ-ID for v1.1 appears in its phase VERIFICATION.md.)

> **3-source note:** Phase 7's four SHOME requirements are flagged "partial (verify manually)" by the status matrix because the SUMMARY frontmatter leg is empty. Manual verification was performed: 07-VERIFICATION.md (status passed, 5/5) carries an explicit per-requirement coverage table with grep evidence, and the live integration check re-confirmed all four. Promoted to **satisfied**; the empty frontmatter is logged as a hygiene tech-debt item.

## Cross-Phase Integration (verified live by gsd-integration-checker)

**Verdict: SOLID.** All four harnesses run and exit 0 live (not trusting narration):

| Harness | Result |
|---------|--------|
| `sh scripts/check-kit-refs.sh` | EXIT 0 — ALL CHECKS PASSED (4 assertions) |
| `sh install/install.test.sh` | EXIT 0 — ALL CHECKS PASSED (18 checks) |
| `sh install/install.two-root.test.sh` | EXIT 0 — ALL CHECKS PASSED (12 checks) |
| `sh scripts/validate.test.sh` | EXIT 0 — ALL CHECKS PASSED (incl. two-root + parity + null-literal) |

**Central seam — resolution chain (the milestone's main integration risk):**
1. Phase 7 left the materialization slot verbatim in both standalone adapters (empty in the source checkout, by design).
2. Phase 8's `materialize_adapter` (sh) / `materializeAdapter` (Node) fills it with a `grugops:materialized-kit` sentinel block + resolved absolute `KIT=` line. Live install confirmed both adapters materialized.
3. Phase 9's doctor reads that block via the same `MAT_OPEN`/`MAT_CLOSE` sentinels; the D-03 three-source cross-check confirms rule-resolved path = marker `kitRoot` = adapter `KIT=` (live: "kit-root sources agree").
4. The validator's `VALIDATE_KIT_ROOT` (parent of `agent-factory/`) is reconciled with the doctor's kit dir by the three-way parity assertion (sh doctor = Node doctor = Node validator) — PASS.

**sh↔Node parity:** byte-identical materialized `KIT=` line and doctor output for the same input; six doctor parity gates (normal, trailing-slash, dot, dotdot, garbled-marker, garbled-with-surviving-kitRoot) all PASS.

**State-write isolation (SHOME-02):** `seed_state` and `write_marker` only ever write to `$TARGET`; `uninstall.sh` never names `GRUGOPS_HOME` in any delete path. No state write can reach the read-only kit.

**Fail-closed (VAL-02):** `unset VALIDATE_KIT_ROOT; node scripts/validate-agent-factory.mjs` → exit 1 with the `(C3)` refuse-to-default message. No `.` fallback.

**Orphans / broken flows:** none within milestone scope.

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | Status | Suggested action |
|-------|---------------|-------------------|--------|------------------|
| 07 | exists (draft) | false | PARTIAL | `/gsd-validate-phase 7` |
| 08 | exists (planned) | true (tasks not run) | PARTIAL | `/gsd-validate-phase 8` |
| 09 | exists (planned) | true (tasks not run) | PARTIAL | `/gsd-validate-phase 9` |

Overall: **partial**. Note the formal Nyquist layer is incomplete, but behavioral coverage is strong and independently re-run (install.test.sh 18/18, validate.test.sh 18/18, two-root 12/12, check-kit-refs green). This is a process-doc gap, not a test-coverage gap. Discovery only — no validation was auto-triggered.

## Tech Debt Summary

**Phase 07:** SUMMARY frontmatter empty for SHOME-01..04 (hygiene) · Nyquist draft · `check-kit-refs.sh` false-green channels WR-01..WR-04 · **WR-05 packaging-template regeneration hazard (Agent tool re-introduction)** · README/config.md stale-path IN-01.
**Phase 08:** Nyquist not run · CRLF-in-marker IN-01 (hypothetical, Windows) · seed board path IN-02 (UX confuser).
**Phase 09:** Nyquist not run.

Highest-signal items: **WR-05** (could silently re-arm sub-agent spawning on a future installer regen — contradicts the deliberate single-window design) and the **Nyquist formal layer** being incomplete across all three phases.

## Cross-Milestone Context (not v1.1 gaps, but relevant at close)

- **v1.0 was never archived** (`archive_count: 0`). Closing v1.1 will be the first milestone archive; v1.0's requirements live in REQUIREMENTS.md alongside v1.1's.
- **DOG-02 (v1.0/Phase 6) is Partial** — the Claude-Code-native dogfood half (live sub-agent spawn + dual-path parity) was deferred to milestone-close UAT. It genuinely cannot be self-run by an agent; tracked honestly in `06-HUMAN-UAT.md`, never fabricated.
- The milestone-close **open-artifact audit** surfaces 6 v1.0-era live-CC UAT/verification items (Phase 05 + 06) + 1 stale quick-task marker (`260606-0my`, actually completed at `6a66994`). These are the items you'll decide to acknowledge-and-defer or resolve when you run `/gsd-complete-milestone`.

## Verdict

**v1.1 "Install & Distribution" — `tech_debt`.** Every v1.1 requirement (8/8) is satisfied with empirical evidence, all three phases verified passed 5/5, and cross-phase integration is solid (all harnesses green live, resolution chain end-to-end verified, sh↔Node parity confirmed). No blockers. Accumulated non-critical debt (Nyquist formal layer incomplete ×3, WR-05 regeneration hazard, gate-quality warnings, SUMMARY hygiene) is worth a review decision but does not block milestone close.

---
_Audited: 2026-06-08 · gsd-audit-milestone · integration verified live by gsd-integration-checker_

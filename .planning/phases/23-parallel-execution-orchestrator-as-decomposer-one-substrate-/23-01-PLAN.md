---
phase: 23-parallel-execution-orchestrator-as-decomposer-one-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/config/factory.config.json
  - agent-factory/config/factory.config.md
  - agent-factory/seed/.grugops/factory.config.json
  - scripts/claim.ts
  - scripts/claim.js
  - scripts/claim.test.ts
  - scripts/now-running-freshness.ts
  - scripts/now-running-freshness.js
  - scripts/now-running-freshness.test.ts
  - scripts/config-queue-consistency.test.ts
  - package.json
autonomous: true
requirements: [CLAIM-03]
must_haves:
  truths:
    - "agent-factory/config/factory.config.json carries a top-level queue object {wip_limit:3, claim_cap:2, stale_ttl_minutes:30}, byte-consistent across the .md twin documentation and the seed/.grugops/factory.config.json seed"
    - "queue.wip_limit (concurrent agent WIDTH) is documented as DISTINCT from the existing per-column wip_limits (board flow) in factory.config.md"
    - "renderNowRunning produces a byte-reproducible .grugops/queue/now-running.md from claimed/*/claim.md, sorted deterministically, with a GENERATED do-not-hand-edit header"
    - "the now-running render reuses claim.ts first-at-trusted single-line discipline (a forged second at: line is rejected/flagged, never silently trusted as a permissive multi-match)"
    - "a queue-rooted freshness gate (clone of context-freshness.ts re-rooted at .grugops/queue/) fails closed when the committed now-running.md drifts from a fresh regen"
  artifacts:
    - path: "scripts/now-running-freshness.ts"
      provides: "Queue-rooted content-freshness gate for now-running.md (Pitfall 5 — context-freshness only walks .grugops/context/)"
    - path: "scripts/now-running-freshness.js"
      provides: "Committed build of the queue freshness gate (byte-matches a fresh tsc rebuild)"
    - path: "agent-factory/config/factory.config.json"
      contains: "queue"
  key_links:
    - from: "scripts/now-running-freshness.ts"
      to: ".grugops/queue/claimed/*/claim.md"
      via: "regenerates now-running.md from the claimed/ registry, byte-diffs against the committed render"
      pattern: "queue"
prohibitions:
  - "the now-running render MUST NOT add a permissive multi-match frontmatter parser — a forged second at: line is a queue-lock DoS; mirror claim.ts sweepStale first-at discipline (multi-at count)"
  - "now-running.md MUST NOT be folded into plans/board.md (D-14 — it is a separate derived .grugops/queue artifact)"
  - "the queue config object MUST NOT collide with or replace the existing per-column wip_limits object (D-07)"
---

<objective>
Ship the two independent foundations the rest of Phase 23 builds on: (1) the `queue` config
object (D-06: `wip_limit`/`claim_cap`/`stale_ttl_minutes`) across the established 3-surface dial,
and (2) the deterministic `now-running.md` queue render (D-14) plus its dedicated queue-rooted
freshness gate (Pitfall 5).

Purpose: CLAIM-03's width cap is a config value the augmented Orchestrator (Plan 02) reads; the
now-running render is the human-facing "what is running" view and the SC2 width evidence source.
Both are additive and depend on nothing in this phase — they unblock Plans 02 and 03.

Output: edited 3 config surfaces; `renderNowRunning` in `claim.ts` + committed `.js`;
`now-running-freshness.ts` (+ `.js` + `.test.ts`); a config cross-surface consistency test; a
new `package.json` freshness script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-CONTEXT.md
@.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-RESEARCH.md
@.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 01 slice)

NEW symbols created here (exclude from drift/source-grounding — they do not exist before this plan):
- `queue` config object keys: `queue.wip_limit`, `queue.claim_cap`, `queue.stale_ttl_minutes`
- `.grugops/queue/now-running.md` (derived render output path)
- `renderNowRunning` (exported fn in `scripts/claim.ts`) + a `now-running` CLI subcommand on `claim.js`
- `scripts/now-running-freshness.ts` / `.js` / `.test.ts`
- `package.json` script `freshness:queue` (or similar) wiring the new gate

<tasks>

<task type="auto">
  <name>Task 1: Add the queue config object across all 3 surfaces + a cross-surface consistency test</name>
  <files>agent-factory/config/factory.config.json, agent-factory/config/factory.config.md, agent-factory/seed/.grugops/factory.config.json, scripts/config-queue-consistency.test.ts</files>
  <read_first>
    - agent-factory/config/factory.config.json (the live config — `wip_limits` l.18-29, `context` l.52-54, the sibling-object shape to mirror)
    - agent-factory/config/factory.config.md (the twin — `context` summary row, the `### context sub-fields` section, the read-at-use default-on-absent paragraph, the config-dial contract table)
    - agent-factory/seed/.grugops/factory.config.json (the seed — must match the kit default object)
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-PATTERNS.md (§ "`queue` config object — 3 surfaces (D-06)" — exact placement + the wip_limit-vs-wip_limits distinction)
  </read_first>
  <action>
Add a top-level `queue` object (sibling to `wip_limits` and `context`, NOT nested inside either) to
all three config surfaces, byte-consistent, per D-06: keys `wip_limit` = 3, `claim_cap` = 2,
`stale_ttl_minutes` = 30. In `factory.config.json` place it as a new top-level key (e.g. directly
after the `context` object). In `seed/.grugops/factory.config.json` add the identical object so the
seed matches the kit default (the guard test asserts the two JSONs stay byte-identical — keep them so).

In `factory.config.md` (twin, clear voice — this is a config surface, not a role prompt) add three
things mirroring how `context` is documented: (a) a summary row in the top key table
(`| `queue` | object | see below | ... |`); (b) a `### queue sub-fields` section listing each key,
its default, and its meaning — `wip_limit` (3) = max concurrent agent WIDTH the coordinator may run,
`claim_cap` (2) = max tasks one delegation may claim (anti-flood, D-08), `stale_ttl_minutes` (30) =
TTL after which the coordinator stale-claim sweep reclaims a claim (D-09); (c) a sentence in the
read-at-use default-on-absent paragraph so a missing `queue` key (or whole object) degrades to these
lean defaults. CRITICAL per D-07: state crisply that `queue.wip_limit` (concurrent agent WIDTH /
CLAIM-03) is INDEPENDENT from the existing per-column `wip_limits` object (board-column flow) — same
naming-collision care Phase 20 applied to "`claim` note-kind vs queue CLAIM"; the Orchestrator
respects BOTH. Defaults `3/2/30` are lean and dogfood-tunable (D-10) — do not phrase any value as a
placeholder or v1.

Create `scripts/config-queue-consistency.test.ts` (vitest, drives the committed files, not a `.ts`
build): parse all three config JSON surfaces and assert the `queue` object is present and
deep-equal across `agent-factory/config/factory.config.json` and
`agent-factory/seed/.grugops/factory.config.json`, and assert `factory.config.md` documents each of
the three `queue` keys (grep the twin for `wip_limit`, `claim_cap`, `stale_ttl_minutes`). This is the
D-06 cross-surface consistency check the VALIDATION Wave-0 list requires.
  </action>
  <verify>
    <automated>npx vitest run scripts/config-queue-consistency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const a=require('./agent-factory/config/factory.config.json').queue, b=require('./agent-factory/seed/.grugops/factory.config.json').queue; if(JSON.stringify(a)!==JSON.stringify(b)) process.exit(1); if(a.wip_limit!==3||a.claim_cap!==2||a.stale_ttl_minutes!==30) process.exit(1)"` exits 0
    - `grep -c 'queue.wip_limit\|queue' agent-factory/config/factory.config.md` > 0 AND the twin contains all three key names `wip_limit`, `claim_cap`, `stale_ttl_minutes`
    - `factory.config.md` contains prose distinguishing `queue.wip_limit` (width) from `wip_limits` (per-column flow)
    - `npx vitest run scripts/config-queue-consistency.test.ts` exits 0
    - `npx vitest run scripts/check-foundation-guards.test.ts` still green (the byte-identity config assertion still holds)
  </acceptance_criteria>
  <done>The `queue` object is present and byte-consistent across the kit config + seed, documented in the twin with the width-vs-flow distinction, and a cross-surface consistency test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: renderNowRunning in claim.ts + the queue-rooted freshness gate</name>
  <files>scripts/claim.ts, scripts/claim.js, scripts/claim.test.ts, scripts/now-running-freshness.ts, scripts/now-running-freshness.js, scripts/now-running-freshness.test.ts, package.json</files>
  <read_first>
    - scripts/claim.ts (claimTask l.104-135 writes `claimed/<task>/claim.md` `by/at/task`; sweepStale l.176+ — the first-at-trusted parse `/^at:\s*(.+)$/m` + the multi-at tamper count l.194-202; the now-running render reuses THIS discipline)
    - scripts/context-io.ts (the render() pattern l.913-969 — GENERATED header, deterministic sort, atomicWrite l.622-647 unlink-then-rename Windows branch, cell() escaping l.884-888)
    - scripts/context-freshness.ts (the whole file — clone target; the realpathSync(mkdtempSync(...)) macOS /var-symlink fix, mirror-spawn, fail-closed non-zero-status branch, byte-compare branch, greenfield vacuous-pass; note it walks `.grugops/context/` ONLY — Pitfall 5)
    - scripts/claim.test.ts (l.1-60 — freshTmp/afterAll/makeQueueRoot hermetic harness; drives claim.js not the .ts)
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-PATTERNS.md (§ "scripts/now-running.ts render" + § "scripts/now-running-freshness.ts" — the security carve-out + clone instructions)
  </read_first>
  <behavior>
    - renderNowRunning(queueRoot): reads every `claimed/<task>/claim.md`, parses `by`/`at`/`task` with the first-at-trusted single-line discipline (mirror sweepStale `/^at:\s*(.+)$/m`); a claim.md with more than one `at:` line is treated as tampered (skip or flag — never trust the second line); writes a deterministic, GENERATED-headered `.grugops/queue/now-running.md` table (task | by | since) sorted by at-then-task; byte-reproducible (running twice produces identical bytes); empty claimed/ yields a header-only render (vacuous, no crash).
    - now-running-freshness gate: regenerates now-running.md into a temp mirror rooted at `.grugops/queue/`, byte-compares against the committed render; drift → exit non-zero naming the file; missing/unreadable → fail closed; no claimed dir → vacuous pass.
  </behavior>
  <action>
Add `renderNowRunning(queueRoot = DEFAULT_QUEUE_ROOT)` to `scripts/claim.ts` (RESEARCH A2 / PATTERNS
recommend `claim.ts` as the home — it owns `claimed/<task>/claim.md`). Clone the render shape from
`context-io.ts` `render()`: a GENERATED do-not-hand-edit header line whose Re-run command points at
`node scripts/claim.js now-running`, a deterministic sort (by `at` then `task`), `atomicWrite` of
`join(queueRoot, "now-running.md")` with a single trailing newline. Parse each `claimed/<task>/claim.md`
with the SAME first-at-trusted discipline `claim.ts` already uses in `sweepStale`: match
`/^at:\s*(.+)$/m`, count `/^at:/gm` occurrences, and treat any count > 1 as a tampered record (do NOT
emit it as a trusted row — mirror sweepStale's `tampered` branch). Do NOT introduce a permissive
multi-match frontmatter parser (V5 / Security — a forged second `at:` line is a queue-lock DoS; this
is the load-bearing carve-out). Add a `now-running` CLI subcommand to `claim.js`'s arg dispatch so the
freshness gate and `package.json` script can invoke it.

Create `scripts/now-running-freshness.ts` by cloning `scripts/context-freshness.ts` and re-rooting it
from `.grugops/context/` to `.grugops/queue/` (Pitfall 5 — the existing `freshness:context` gate walks
`.grugops/context/` ONLY and will never see `now-running.md`, so a dedicated queue-rooted gate is
required). Copy verbatim: the `realpathSync(mkdtempSync(...))` macOS /var-symlink fix (load-bearing —
without it the mirrored render silently no-ops at exit 0), the mirror-spawn that regenerates the
render into the temp mirror, the fail-closed non-zero-status branch, the byte-compare + fail-closed
unreadable branch, and the greenfield vacuous-pass when no `.grugops/queue/claimed/` exists. Keep it a
STANDALONE gate (NOT folded into check-foundation-guards) with its own `package.json` script — exactly
how `context-freshness` is wired. Add the `package.json` script (e.g. `freshness:queue`) mirroring the
`freshness:context` entry shape (`tsc --outDir .tmp-build && node scripts/now-running-freshness.js`).

Create `scripts/now-running-freshness.test.ts` (clone `claim.test.ts` harness): a hermetic temp
`.grugops/queue/` with two seeded claims → render → assert byte-reproducible (render twice, identical);
hand-mutate the committed render → gate exits non-zero naming the file (fail-closed proof); a forged
second `at:` line in a claim.md → the render does NOT emit it as a trusted row (security carve-out
proof); empty claimed/ → vacuous pass. Drive the committed `.js`, never the `.ts`. Keep it OUT of the
e2e lane.

Rebuild the committed `.js` for `claim.ts` and `now-running-freshness.ts` and run `npm run freshness`
(D-13 — every changed/new committed `.js` must byte-match a fresh `tsc` rebuild).
  </action>
  <verify>
    <automated>npx vitest run scripts/now-running-freshness.test.ts scripts/claim.test.ts && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `renderNowRunning` writes `.grugops/queue/now-running.md` with a `GENERATED — do not hand-edit` header and is byte-reproducible (two consecutive renders produce identical bytes — asserted by the test)
    - a claim.md carrying a forged SECOND `at:` line is NOT emitted as a trusted now-running row (the test plants it and asserts absence/flag) — `grep -n 'multi.*at\|tamper\|first-at\|sweepStale' scripts/claim.ts` shows the render reuses the existing discipline (no new permissive parser)
    - `node scripts/now-running-freshness.js` exits 0 on a clean tree and exits non-zero (naming the file) when the committed `now-running.md` is mutated (test proves both)
    - `now-running-freshness.ts` walks `.grugops/queue/` not `.grugops/context/` (`grep -c '\.grugops.*queue' scripts/now-running-freshness.ts` > 0)
    - `npm run freshness` exits 0 (claim.js + now-running-freshness.js byte-match a fresh rebuild)
    - `package.json` has a new freshness script invoking the queue gate
  </acceptance_criteria>
  <done>now-running.md renders deterministically from the claim registry with the first-at-trusted carve-out intact, a dedicated queue-rooted freshness gate fails closed on drift, and all committed `.js` pass freshness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk `claim.md` → now-running render | claim.md frontmatter is on-disk content that the render parses; a forged second `at:` line is the attack surface |
| committed render → freshness gate | a stale/hand-edited committed `now-running.md` must be caught, not shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering / DoS | now-running render parsing `claim.md` | mitigate | Reuse `claim.ts` first-`at`-trusted parse + multi-`at` tamper count (sweepStale l.194-202); a forged second `at:` line is NOT emitted as a trusted row. No permissive multi-match parser (V5). |
| T-23-02 | Tampering | committed `now-running.md` drift | mitigate | Dedicated queue-rooted freshness gate (clone of context-freshness.ts) byte-diffs the committed render against a fresh regen; fail-closed on drift/unreadable. |
| T-23-03 | DoS | unbounded width via missing `wip_limit` config | mitigate | `queue.wip_limit` default 3 present on all 3 surfaces + default-on-absent documented; the Orchestrator scheduler (Plan 02) reads it. |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan — only dev deps `{typescript, vitest, @types/node}` already present (RESEARCH Package Legitimacy: N/A). |
</threat_model>

<verification>
- `npx vitest run scripts/config-queue-consistency.test.ts scripts/now-running-freshness.test.ts scripts/claim.test.ts` green
- `npm run freshness` exits 0
- `npx vitest run scripts/check-foundation-guards.test.ts` still green (config byte-identity intact)
</verification>

<success_criteria>
- `queue` object present + byte-consistent across the 3 config surfaces; width-vs-flow documented (D-06/D-07)
- now-running.md renders deterministically with the first-at-trusted security carve-out; queue-rooted freshness gate fails closed on drift (D-14, Pitfall 5)
- all committed `.js` byte-match a fresh rebuild (D-13)
</success_criteria>

<output>
Create `.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-01-SUMMARY.md` when done
</output>

---
phase: 23-parallel-execution-orchestrator-as-decomposer-one-substrate
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - agent-factory/roles/orchestrator.md
  - agent-factory/workflows/17-task-claim.md
  - scripts/decompose-spine.test.ts
  - scripts/convergence-spine.test.ts
autonomous: true
requirements: [PAR-01, PAR-02, PAR-03, CLAIM-03]
must_haves:
  truths:
    - "orchestrator.md describes the decompose/schedule/gate spine (classify → decompose into subtasks → enqueue thin pending files each a context ref → schedule → gate → stale-claim sweep) — augmented in place, the 17-type classification + routing matrix preserved (D-11/D-12)"
    - "orchestrator.md carries the clear-voice hard limit: holds Agent(<allowlist>), sets queue.wip_limit, never exceeds width, does NOT relay data between agents (the shared verified context is the only channel) — and documents the two modes (parallel depth ≤5 on Claude Code / sequential concurrency-1 on the four other CLIs) over the one substrate (D-13)"
    - "Workflow 17 (17-task-claim.md) is the single-source claim+schedule protocol mirroring WF18's shape — it references claim.js mechanics and chains to WF16 for note I/O, never inlining either (D-05)"
    - "a seeded synthetic 2-3-subtask decomposition produces the expected thin pending/ files (each a ref to .grugops/context/<task>/) and asserts concurrent claim WIDTH never exceeds queue.wip_limit (SC1/SC2)"
    - "the SC3 convergence fixture runs the same seeded decomposition through (a) a parallel-spawn simulation and (b) a sequential drain and asserts order-independent identical on-disk substrate state (claim/done records + .grugops/context/<task>/notes/) (PAR-03/SC3/D-04)"
  artifacts:
    - path: "agent-factory/workflows/17-task-claim.md"
      provides: "Single-source task-claim+schedule workflow protocol (PAR-02/PAR-03)"
      contains: "kind: workflow"
    - path: "scripts/convergence-spine.test.ts"
      provides: "SC3 dual-path order-independent substrate-equality oracle (hermetic, NOT in e2e lane)"
    - path: "scripts/decompose-spine.test.ts"
      provides: "SC1/SC2 decomposition + width-cap assertions"
  key_links:
    - from: "agent-factory/roles/orchestrator.md"
      to: "agent-factory/workflows/17-task-claim.md"
      via: "the scheduler spine references WF17 for claim mechanics; WF17 chains to WF16 for note I/O"
      pattern: "17-task-claim"
    - from: "scripts/convergence-spine.test.ts"
      to: "scripts/claim.js"
      via: "drives the committed claim.js through both modes against a hermetic temp queue root"
      pattern: "claim"
prohibitions:
  - "orchestrator.md MUST NOT be rewritten — augment in place; keep the caveman prompt cadence and the 17-type classification (D-11); keep the augment tight so guard_role_size (4096 B cap) stays green"
  - "Workflow 17 MUST NOT inline claim.ts mechanics or WF16 note I/O — it references them (D-05/Pitfall 6); the step-4 protocol edit and the WR-05 absolute drop are NOT in this plan (they are the atomic flip, Plan 03 — scope seam D-01)"
  - "this plan MUST NOT re-wire context I/O into role files — roles already reference WF16 (D-03/VFY-03, Phase 21); Phase 23 adds only the claim+schedule layer around that existing I/O"
  - "the convergence/spine tests MUST NOT live in the e2e lane (npm test triggers the live claude-CLI lane); they are hermetic deterministic vitest tests driving the committed .js"
  - "a width-cap verification MUST NOT check only depth — SC2 requires asserting concurrent WIDTH never exceeds queue.wip_limit (Pitfall 4)"
---

<objective>
Wire PAR-01/PAR-02/PAR-03's prose machinery and prove it deterministically: augment `orchestrator.md`
from router → decomposer/scheduler/gate (D-11/D-12/D-13), author Workflow 17 as the single-source
claim+schedule protocol (D-05), and land the SC1/SC2 decomposition+width-cap spine fixture plus the
SC3 dual-path convergence oracle (D-04). This plan does NOT touch the WR-05 wording surface or the
`_role-switch-protocol.md` absolute — those are the atomic flip (Plan 03).

Purpose: SC1 (decompose/enqueue/no-relay), SC2 (parallel claim, width ≤ wip_limit, depth ≤5
documented), and SC3 (one substrate, two modes converge) are the heart of the phase; the spine
fixtures are their Nyquist evidence.

Output: augmented `orchestrator.md`; new `17-task-claim.md`; `decompose-spine.test.ts` +
`convergence-spine.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-CONTEXT.md
@.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-RESEARCH.md
@.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 02 slice)

NEW symbols created here (exclude from drift/source-grounding):
- `agent-factory/workflows/17-task-claim.md` (new workflow, `order: 17`)
- `scripts/decompose-spine.test.ts`, `scripts/convergence-spine.test.ts`
- New prose in `orchestrator.md`: the decompose/schedule/gate spine, the `Agent(<allowlist>)`/width/no-relay hard limit, the two-modes (depth ≤5 / concurrency-1) description
- Consumes (does NOT create): `queue.wip_limit`/`claim_cap`/`stale_ttl_minutes` (from Plan 01), `claim.js` `claimTask`/`transition`/`sweepStale` (Phase 20)

<tasks>

<task type="auto">
  <name>Task 1: Augment orchestrator.md (router → decompose/schedule/gate) + author Workflow 17</name>
  <files>agent-factory/roles/orchestrator.md, agent-factory/workflows/17-task-claim.md</files>
  <read_first>
    - agent-factory/roles/orchestrator.md (the whole file — § Caveman prompt l.13-24 the guard_caveman_preserved fenced block; § Reads l.27-32 the config-keys line; § Responsibilities l.37-49 + Routing matrix l.51-67; § Output decision block)
    - agent-factory/workflows/18-context-compaction.md (the whole file — the EXACT section shape to mirror for WF17: frontmatter kind/order/cadence, ## When to use single-source note, ## Steps, ## Stop conditions, ## Done condition, ## Commit)
    - agent-factory/workflows/16-context-read-write.md (the read/write/admission protocol WF17 chains to — do NOT restate it)
    - scripts/claim.ts (claimTask/transition/sweepStale — the mechanics WF17 references via claim.js, never inlines)
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-PATTERNS.md (§ "orchestrator.md — MODIFY (augment)" + § "17-task-claim.md — NEW" — exact line refs + the guard_role_size byte-ceiling note)
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-RESEARCH.md (§ Pitfall 1 main-thread-only allowlist nuance; § Pitfall 4 depth-not-width; § Code Examples "two-mode contract" prose)
  </read_first>
  <action>
Augment `orchestrator.md` in place (D-11 — do NOT rewrite; the caveman prompt cadence and the 17-type
classification + routing matrix stay):
  1. § Caveman prompt (inside the fenced block — guard_caveman_preserved strips and checks this fence;
     keep the `You ...` line cadence): add ~4 tight caveman lines covering decompose / queue / width /
     no-relay / spawn-only-on-Claude-Code. Keep voice/token economy — caveman IS the token-economy
     mechanism here; do not bloat.
  2. § Reads: add `queue` to the config-keys line (currently lists `wip_limits`/`quality`/... at l.28),
     so the Orchestrator reads `queue.wip_limit`/`claim_cap`/`stale_ttl_minutes` (per D-06 from Plan 01).
  3. § Responsibilities: add the new spine (D-12) — classify → decompose into subtasks → enqueue (thin
     pending files, each a `ref:` to its `.grugops/context/<task>/` folder) → schedule (on Claude Code:
     spawn role-agents via the Agent tool up to `queue.wip_limit` width; on the four other CLIs: drain
     the same queue concurrency-1 via the role-switch protocol + Workflow 17) → gate → run the
     stale-claim sweep (TTL = `queue.stale_ttl_minutes`). Repurpose the routing matrix as "subtask →
     which role CLAIMS it" (D-11) — keep the matrix.
  4. Add the explicit hard limit in CLEAR voice (NOT caveman — D-13, this is a safety/capability
     surface): the Orchestrator holds `Agent(<allowlist>)`, sets `queue.wip_limit` and never exceeds
     concurrent WIDTH, honors `queue.claim_cap` per delegation, and does NOT relay data between agents —
     the shared verified context is the only channel. Merge/deploy limits unchanged. Document the two
     modes over the one substrate: PARALLEL on Claude Code (nested sub-agent spawning, platform DEPTH
     cap = 5 fixed/not-configurable, grugops WIDTH cap = `queue.wip_limit` since the platform does NOT
     cap width — Pitfall 4); SEQUENTIAL on Codex/Gemini/OpenCode/Copilot (concurrency-1, no spawn,
     degrade-never-break). Note the main-thread-only allowlist nuance (Pitfall 1): the enumerated
     `Agent(<allowlist>)` is honored because the Orchestrator runs as the main-thread agent; nested
     spawning by role-agents is bounded only by depth + width, not by a nested allowlist.
  KEEP THE AUGMENT TIGHT — `guard_role_size` caps role files at 4096 B (check-foundation-guards l.31);
  prefer reusing the routing matrix and reads line over adding new prose blocks. The width cap is
  PROSE/scheduling, not a code lock (Responsibility Map).

Author `agent-factory/workflows/17-task-claim.md` mirroring WF18's section shape verbatim: frontmatter
`kind: workflow` + `order: 17` + `cadence: both`; `## When to use` (with the single-source note: "Every
role references this file rather than restating — there is one protocol, named here, not forked into
each role"); `## Steps` numbered run-in-order — claim a pending task atomically (reference `claim.js`
`claimTask` mkdirSync — first claimant wins, EEXIST = claim lost), move it to claimed by atomic rename
(`transition`), read/write the shared context per WF16 (CHAIN, do not inline), mark done by atomic
rename, and (planner's discretion — INCLUDE it) the coordinator stale-claim sweep via `sweepStale` with
TTL = `queue.stale_ttl_minutes`; `## Stop conditions`; `## Done condition`; `## Commit` (branch guard
first, never a protected branch, never merge/deploy). Single-source the CLAIM mechanics here; reference
`claim.js` the way WF18 references `context-io.js`/`compactor.ts`. Do NOT inline note I/O — chain to
WF16 (D-05/Pitfall 6). Clear voice (it is a protocol/workflow surface, not a role prompt).
  </action>
  <verify>
    <automated>npx vitest run scripts/check-foundation-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `agent-factory/workflows/17-task-claim.md` exists with frontmatter `kind: workflow` and `order: 17` (`grep -c 'order: 17' agent-factory/workflows/17-task-claim.md` == 1)
    - WF17 references `claim.js`/`claimTask` and chains to `16-context-read-write.md` but does NOT restate WF16's note-I/O steps (`grep -c '16-context-read-write\|claim.js\|claimTask' agent-factory/workflows/17-task-claim.md` > 0)
    - `orchestrator.md` § Reads line includes `queue`; Responsibilities describe decompose → enqueue → schedule → gate → stale-sweep; the clear-voice hard limit names `Agent(<allowlist>)`, `queue.wip_limit`, "does NOT relay", and the two modes (depth ≤5 / concurrency-1)
    - `node scripts/check-foundation-guards.js` exits 0 — `guard_role_size` (orchestrator.md ≤ 4096 B) and `guard_caveman_preserved` both still green after the augment
    - `npx vitest run scripts/check-foundation-guards.test.ts` green
  </acceptance_criteria>
  <done>orchestrator.md is augmented to the decompose/schedule/gate role with the two-mode + width-cap + no-relay hard limit (under the size guard), and WF17 single-sources the claim+schedule protocol referencing claim.js and chaining to WF16.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC1/SC2 decomposition+width spine fixture + SC3 dual-path convergence oracle</name>
  <files>scripts/decompose-spine.test.ts, scripts/convergence-spine.test.ts</files>
  <read_first>
    - scripts/claim.test.ts (l.1-60 — the hermetic temp-dir harness: freshTmp/afterAll, makeQueueRoot seeding pending/claimed/done + thin pending subtask files with `ref: .grugops/context/${t}/`; drives the committed claim.js, NOT the .ts)
    - scripts/claim.ts (claimTask/transition/sweepStale exports + the EEXIST claim-lost contract — the mechanics the spine drives)
    - scripts/context-io.ts (currentState() l.756-764 deterministic replay — use for the order-independent note compare; appendNote for seeding notes)
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-PATTERNS.md (§ "<convergence>.test.ts and <spine>.test.ts" — the copy-verbatim harness instructions)
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-RESEARCH.md (§ Validation Architecture → Phase Requirements → Test Map rows for PAR-01/SC1, PAR-02/SC2, PAR-03/SC3)
  </read_first>
  <behavior>
    - decompose-spine: a seeded 2-3-subtask decomposition writes one thin pending file per subtask, each carrying a `ref:` to `.grugops/context/<task>/`; claiming the seeded tasks asserts concurrent WIDTH never exceeds queue.wip_limit (e.g. with wip_limit=2 and 3 tasks, at most 2 are in claimed/ at once before a done frees a slot); a second claimant on an already-claimed task gets false (EEXIST), proving exclusivity; no agent-to-agent data is relayed (coordination only via on-disk substrate).
    - convergence-spine: the SAME seeded decomposition is replayed two ways — (a) parallel-spawn simulation (claim all up to width, run, done) and (b) sequential drain (claim one, run, done, repeat) — and the final on-disk substrate is asserted EQUAL order-independently: same set of done records, same `.grugops/context/<task>/notes/` set via currentState() canonical compare.
  </behavior>
  <action>
Create `scripts/decompose-spine.test.ts` (vitest, hermetic, driving committed `claim.js`/`context-io.js`):
clone the `claim.test.ts` harness (freshTmp/afterAll/makeQueueRoot seeding `pending/`/`claimed/`/`done/`
+ thin pending subtask files with `ref: .grugops/context/${t}/`). Seed a synthetic 2-3-subtask
decomposition. Assert SC1: each pending file is thin and carries a context `ref:` (decompose →
enqueue). Assert SC2/CLAIM-03 (Pitfall 4 — assert WIDTH, not depth): with a width budget read as
`queue.wip_limit` (use a small value like 2 for the fixture), simulate the scheduler claiming tasks and
prove the count of tasks simultaneously in `claimed/` NEVER exceeds the width budget — a third claim
only proceeds after a prior task transitions to `done/`. Assert exclusivity: a second `claimTask` on an
already-claimed task returns false (EEXIST). Assert no-relay: nothing is passed agent-to-agent — the
only shared state is the on-disk queue + context.

Create `scripts/convergence-spine.test.ts` (the SC3/D-04 dual-path oracle): build the SAME seeded
2-3-subtask decomposition into two independent hermetic temp queue roots. Run root A through a
parallel-spawn simulation (claim up to width, write each task's notes via `appendNote`, mark done) and
root B through a sequential drain (claim one, write its notes, done, repeat). Then assert the two
substrates are EQUAL order-independently: equal set of `done/` records, and equal
`.grugops/context/<task>/notes/` content using `context-io.ts` `currentState()` for a deterministic,
order-independent canonical compare (per-task note sets must match regardless of write order). This is
the "one substrate, two modes converge" proof. Both files MUST be hermetic deterministic vitest tests
driving the committed `.js` — NEVER the `.ts`, NEVER in the e2e lane (project memory: `npm test`
triggers the live claude-CLI lane; routine verification uses `npx vitest run --exclude '**/scripts/e2e/**'`).
  </action>
  <verify>
    <automated>npx vitest run scripts/decompose-spine.test.ts scripts/convergence-spine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/decompose-spine.test.ts` asserts concurrent tasks in `claimed/` never exceed the width budget (an explicit width assertion, not a depth check) AND that a second claim on a held task returns false
    - `scripts/convergence-spine.test.ts` asserts order-independent equality of the final substrate (done records + per-task notes via currentState()) across the parallel-spawn and sequential-drain replays
    - both tests import the committed `.js` (`grep -c "claim.js\|context-io.js" scripts/convergence-spine.test.ts` > 0) and neither is under `scripts/e2e/`
    - `npx vitest run scripts/decompose-spine.test.ts scripts/convergence-spine.test.ts` exits 0
    - `npx vitest run --exclude '**/scripts/e2e/**'` green (no regression)
  </acceptance_criteria>
  <done>The SC1/SC2 spine proves decompose→enqueue and width ≤ wip_limit + exclusivity, and the SC3 convergence oracle proves the parallel and sequential modes produce order-independent identical on-disk substrate state — both hermetic, both driving the committed .js, neither in the e2e lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator → role-agents | the coordinator schedules but does NOT relay data; the only channel is the shared on-disk substrate |
| spawn scheduling → platform | the platform caps depth (5) but NOT width; grugops must self-enforce width |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | DoS | unbounded concurrent width under parallel fan-out | mitigate | `queue.wip_limit` width cap enforced by the coordinator scheduler (orchestrator.md prose); the spine fixture asserts width never exceeds the budget (Pitfall 4 — assert width not depth). `claim_cap` per-delegation anti-flood documented (D-08). |
| T-23-05 | Information disclosure / Spoofing | agent-to-agent data relay (a side channel bypassing the verified context) | mitigate | orchestrator.md hard limit: does NOT relay data; coordination through the shared verified context only (the blackboard control component). The spine asserts the only shared state is on-disk. |
| T-23-06 | Tampering | a claimed task re-claimed by a second agent (lost-update) | mitigate | `claim.js` `claimTask` mkdirSync exclusivity (EEXIST = lost); the spine asserts a second claim returns false. |
| T-23-07 | Elevation of Privilege | a spawned agent auto-merges/deploys | accept (floor unchanged) | `hooks/guard.ts` PreToolUse UNCHANGED — every spawned agent hits the mechanical merge/deploy gate; documented as the survives-parallelism proof, not modified here. |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (RESEARCH Package Legitimacy: N/A). |
</threat_model>

<verification>
- `npx vitest run scripts/decompose-spine.test.ts scripts/convergence-spine.test.ts scripts/check-foundation-guards.test.ts` green
- `node scripts/check-foundation-guards.js` exits 0 (guard_role_size + guard_caveman_preserved hold)
- `npx vitest run --exclude '**/scripts/e2e/**'` green (full non-e2e regression)
</verification>

<success_criteria>
- orchestrator.md augmented to decompose/schedule/gate with the two-mode + width-cap + no-relay hard limit, under the size guard (PAR-01/SC1, D-11/D-12/D-13)
- WF17 single-sources the claim+schedule protocol, referencing claim.js + chaining WF16 (PAR-02/PAR-03, D-05)
- SC1/SC2 spine proves decompose→enqueue + width ≤ wip_limit; SC3 convergence oracle proves dual-path on-disk equality (D-04)
</success_criteria>

<output>
Create `.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-02-SUMMARY.md` when done
</output>

---
phase: 23-parallel-execution-orchestrator-as-decomposer-one-substrate
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - scripts/check-foundation-guards.test.ts
  - scripts/check-uat-oracles.ts
  - scripts/check-uat-oracles.js
  - scripts/check-uat-oracles.test.ts
  - agent-factory/roles/_role-switch-protocol.md
  - agent-factory/packaging/subagent.frontmatter.md
  - agent-factory/packaging/slash-command.template.md
  - agent-factory/packaging/adapters.md
  - agent-factory/README.md
  - .claude/agents/grugops-orchestrator.md
  - scripts/generate-catalog.ts
  - scripts/generate-catalog.js
  - docs/catalog/README.md
autonomous: true
requirements: [PAR-04, PAR-02, CLAIM-03]
must_haves:
  truths:
    - "guard_wr05 is inverted to both-direction: it detects the coordinator via the /^coordinator:\\s*true/m frontmatter marker (D-15, never a hard-coded filename); a coordinator MUST carry the Agent(<allowlist>) grant and a non-coordinator MUST NOT (D-16)"
    - ".claude/agents/grugops-orchestrator.md carries coordinator: true AND the enumerated Agent(<allowlist>) grant (least-privilege specialist role agents, D-17), under the 4096 B guard_adapter_size cap"
    - "the WR-05 absolute is dropped from _role-switch-protocol.md (No Agent tool / grugops does NOT spawn → coordinator-only on Claude Code) while the sequential-role-load description for the four non-spawning CLIs is preserved; the step-4 thin claim+schedule wrapper references WF17/WF16 (D-05/D-18 #3)"
    - "the inversion is ASYMMETRIC across the 5 tools — only the Claude Code row of adapters.md + README.md gains the coordinator-spawn language; the four other CLI rows keep 'Sequential role-load — no spawn' verbatim (D-19)"
    - "the B3 oracle (oracleWr05Wording) flips in lockstep AND gains a four-CLI asymmetry assertion that FAILS if any non-CC row in adapters.md/README.md gains spawn/coordinator wording (Pitfall 3)"
    - "the committed docs catalog is regenerated in the SAME change (generate-catalog.js) and catalog-freshness passes; all changed/new committed .js byte-match a fresh rebuild (npm run freshness exit 0)"
  artifacts:
    - path: "scripts/check-foundation-guards.test.ts"
      provides: "THREE both-direction RED fixtures proven against the COMMITTED guard.js (planted non-coordinator grant; dropped coordinator grant; removed coordinator: marker)"
    - path: ".claude/agents/grugops-orchestrator.md"
      contains: "coordinator: true"
    - path: "docs/catalog/README.md"
      provides: "regenerated catalog (PAR-04 docs catalog) reflecting WF17 + the flip"
  key_links:
    - from: "scripts/check-foundation-guards.ts"
      to: ".claude/agents/grugops-orchestrator.md"
      via: "guard_wr05 asserts the coordinator (coordinator: true marker) MUST carry the Agent grant; non-coordinators MUST NOT"
      pattern: "coordinator:\\s*true"
    - from: "scripts/check-uat-oracles.ts"
      to: "agent-factory/packaging/adapters.md"
      via: "the B3 asymmetry assertion scans the 5-tool rows; non-CC rows must retain no-spawn wording"
      pattern: "adapters"
prohibitions:
  - "PAR-04 MUST NOT be marked complete on a green suite alone — green suite is NOT proof (project lesson: CMP-02 carve-out bypassed 7x through green suites). All THREE both-direction RED fixtures + the asymmetry-drift repro MUST be proven RED against the COMMITTED guard.js/oracle.js, captured as a RED-baseline → GREEN-after artifact pair, plus an independent code-review/probe pass"
  - "guard_wr05 MUST NOT use a hard-coded coordinator filename — detection is the coordinator: true marker only (D-15); the SCAN set stays the explicit file list, NEVER a repo-wide grep"
  - "the 5-tool table edit MUST NOT touch the four non-Claude-Code rows — if the same line changed in >1 CLI row, that is the Pitfall-3 drift bug (D-19 asymmetry)"
  - "the WR05_COMMA / WR05_ARRAY EREs MUST NOT drop the Task alias — keep both Agent|Task (Task still aliases, State-of-the-Art)"
  - "hooks/guard.ts MUST NOT be modified — it is the unchanged humans-hold-merge/deploy floor that survives parallelism"
  - "the literal grant token MUST NOT appear in any guard <action> body in a form the guard's own negative grep would self-trip; describe the EREs by concept and cite line refs"
---

<objective>
The atomic WR-05 inversion (D-18) — ONE coordinated change-set across all seven surfaces so the
guard/oracle never go RED against the wording. Invert `guard_wr05` to both-direction
(coordinator-MUST-grant / non-coordinator-MUST-NOT, D-16) keyed on the `coordinator: true` marker
(D-15); flip the B3 wording oracle in lockstep and add the four-CLI asymmetry assertion (D-19/Pitfall
3); drop the "No Agent tool" absolute from `_role-switch-protocol.md` + add the thin step-4 claim
wrapper (D-18 #3/D-05); flip the packaging templates + the asymmetric 5-tool tables; add
`coordinator: true` + the enumerated `Agent(<allowlist>)` to the orchestrator adapter (D-17);
regenerate the docs catalog (PAR-04). Because this inverts a SAFETY guard, the gate requires
both-direction RED proof against the committed `.js` + an asymmetry-drift reproduction + an
independent probe — a green suite is NOT proof.

Purpose: PAR-04/SC4 — the flip lands atomically, a planted half-flip fails RED, and CC parallelism
is mechanically enabled for the coordinator only.

Output: inverted guard + flipped oracle (+ committed `.js` + tests with 3 RED fixtures + asymmetry
fixture); edited protocol/packaging/tables/adapter; regenerated catalog; RED→GREEN evidence pair.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-CONTEXT.md
@.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-RESEARCH.md
@.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 03 slice)

NEW/CHANGED symbols created here (exclude from drift/source-grounding):
- `coordinator: true` frontmatter marker (new greppable property on the orchestrator adapter + the coordinator packaging example)
- `Agent(<allowlist>)` enumerated grant on `.claude/agents/grugops-orchestrator.md`
- inverted `guard_wr05` both-direction logic + a coordinator-marker ERE in check-foundation-guards.ts
- the asymmetry-assertion beat added to `oracleWr05Wording` (+ a new 5-tool-table SCAN set)
- regenerated `docs/catalog/README.md`
- three new guard RED fixtures + one asymmetry oracle RED fixture in the test files
- RED-baseline / GREEN-after evidence artifacts (e.g. 23-03-RED-baseline.txt / 23-03-GREEN-proof.txt)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invert guard_wr05 to both-direction (coordinator marker) + the THREE RED fixtures</name>
  <files>scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js, scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - scripts/check-foundation-guards.ts (guard_wr05 l.107-140 — the two grant EREs WR05_COMMA/WR05_ARRAY l.115-119, the explicit WR05_SCAN 4-file list l.120-125, the grepFiles helper l.95-105, the pass/fail spine; guard_role_size note l.31)
    - scripts/check-foundation-guards.test.ts (the guard_wr05 cases l.128-156 — the comma-form plant onto grugops-orchestrator.md and the array-form plants onto SKILL.md; the mirror()/runIn()/out() hermetic CHECK_ROOT harness)
    - .claude/agents/grugops-orchestrator.md (the file the inverted guard asserts MUST carry the grant; it is in WR05_SCAN)
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-PATTERNS.md (§ "guard_wr05" — the both-direction logic sketch + the RED-fixture discipline)
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-RESEARCH.md (§ Pitfall 2 + § Adversarial Reproduction — the three half-flip RED fixtures + committed-.js evidence)
  </read_first>
  <behavior>
    - guard_wr05 (inverted, both-direction): for each file in the explicit WR05_SCAN set, compute isCoordinator from the coordinator marker ERE and hasGrant from the two existing grant EREs. PASS only if the coordinator carries the grant AND every non-coordinator carries no grant. FAIL (clear voice, naming the file) when: a non-coordinator carries the grant (rogue spawn); the coordinator's grant is dropped (half-flip kills CC parallelism); the coordinator marker is removed (a rename/marker-loss must not silently demote the coordinator).
    - tests: three RED fixtures, each proven against the COMMITTED guard.js — (a) plant the grant onto a non-coordinator SCAN file → nonzero naming the file; (b) drop the grant from the coordinator → nonzero; (c) remove the coordinator: true marker from the orchestrator → nonzero. Plus a smoke run proving the real tree is GREEN after the flip.
  </behavior>
  <action>
Invert `guard_wr05` IN PLACE (PATTERNS § guard_wr05). Add a coordinator-marker ERE (a line-anchored
match for the marker key set to true). KEEP both existing grant EREs verbatim (they match the comma
list, the YAML array, the quoted array item, and the scoped form, for both the Agent token and its Task
alias — do not drop the alias). KEEP the explicit WR05_SCAN 4-file list (never a repo-wide grep — the
established token-vs-prose care). New per-file logic: derive `isCoordinator` from the marker ERE over
that file and `hasGrant` from the two grant EREs over that file; if a coordinator lacks the grant, FAIL
naming the file with a clear-voice message that a dropped grant kills Claude Code parallelism; if a
non-coordinator carries the grant, FAIL naming the file with a clear-voice rogue-spawn message; PASS
only when every file satisfies its direction. Detection is the marker ONLY (D-15) — NEVER a hard-coded
filename. Clear voice on all findings (CLAUDE.md hard rule). Keep the change tight (the guard hosts
several other guards — do not perturb them).

Update `scripts/check-foundation-guards.test.ts`: the existing array-form plants onto SKILL.md
(non-coordinator) STAY valid RED cases (non-coordinator + grant = rogue). The existing comma-form plant
onto `grugops-orchestrator.md` must be RECAST — after the flip the orchestrator legitimately HAS a grant,
so plant the rogue grant onto a NON-coordinator SCAN file instead. Then ADD the three both-direction RED
fixtures required by D-16/Pitfall 2: (a) a non-coordinator SCAN file with a planted grant → nonzero +
finding names the file; (b) the coordinator file with the grant DROPPED → nonzero + clear-voice
dropped-grant finding; (c) the coordinator file with the `coordinator: true` marker REMOVED → nonzero.
Each runs against the COMMITTED guard.js via the existing mirror()/runIn() CHECK_ROOT harness.

Rebuild the committed guard.js. Then ADVERSARIALLY REPRODUCE (RESEARCH § Adversarial Reproduction —
green suite ≠ proof): hand-construct each of the three half-flipped trees in a temp dir and confirm
`node scripts/check-foundation-guards.js` (the COMMITTED .js, not the .ts) exits non-zero naming the
offending file for each. Capture the output into a RED-baseline artifact (run against the half-flipped
trees) paired with a GREEN proof (the real flipped tree passes) — mirror the 22-07/22-09 evidence
discipline (`23-03-RED-baseline.txt` / `23-03-GREEN-proof.txt`).
  </action>
  <verify>
    <automated>npx vitest run scripts/check-foundation-guards.test.ts && node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - guard_wr05 detects the coordinator via the marker ERE (`grep -c 'coordinator' scripts/check-foundation-guards.ts` > 0) and NOT via any hard-coded orchestrator filename in the new logic
    - both grant EREs still match `Agent|Task` (`grep -c 'Agent|Task\|Agent\\\\|Task' scripts/check-foundation-guards.ts` > 0 — the alias is retained)
    - the test file has THREE both-direction RED fixtures (planted non-coordinator grant; dropped coordinator grant; removed marker), each asserting nonzero exit against the committed guard.js
    - a hand-constructed half-flip tree makes `node scripts/check-foundation-guards.js` exit non-zero naming the file (captured in `23-03-RED-baseline.txt`); the real flipped tree passes (captured in `23-03-GREEN-proof.txt`)
    - `node scripts/check-foundation-guards.js` exits 0 on the real tree AFTER the orchestrator adapter gains the marker + grant in Task 2 (sequence: this task's guard logic + tests land, Task 2 supplies the legitimate grant so the real-tree smoke goes green)
    - `npm run freshness` exits 0 (committed guard.js byte-matches a fresh rebuild)
  </acceptance_criteria>
  <done>guard_wr05 is inverted to both-direction marker-keyed enforcement with both grant EREs intact, three RED fixtures prove each half-flip fails against the committed .js, and a RED-baseline/GREEN-proof artifact pair is captured.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the wording surface atomically — protocol, packaging, asymmetric tables, orchestrator adapter</name>
  <files>agent-factory/roles/_role-switch-protocol.md, agent-factory/packaging/subagent.frontmatter.md, agent-factory/packaging/slash-command.template.md, agent-factory/packaging/adapters.md, agent-factory/README.md, .claude/agents/grugops-orchestrator.md</files>
  <read_first>
    - agent-factory/roles/_role-switch-protocol.md (l.9 "grugops does NOT spawn sub-agents"; the 5 steps incl. step-4 l.27-31; the l.43 absolute "No Agent tool. No sub-agent spawn.")
    - agent-factory/packaging/subagent.frontmatter.md (l.15-18 + l.27 the no-spawn example + l.58-63 the "no spawn tool" rationale — add a coordinator example carrying the marker + grant)
    - agent-factory/packaging/slash-command.template.md (the sibling SKILL template — prose edit)
    - agent-factory/packaging/adapters.md (the 5-tool table l.34-44 — CC row says "Sequential role-load — no spawn"; the four other rows; the closing "All five tools use the SAME single-window sequential role-load" paragraph)
    - agent-factory/README.md (the 5-tool table l.40-46 — CC row ALREADY says "Native sub-agents — the Orchestrator spawns role agents"; reconcile to coordinator framing; the four other rows keep no-spawn)
    - .claude/agents/grugops-orchestrator.md (frontmatter l.1-6 — currently `tools: Read, Grep, Glob, Bash, Edit, Write`, no coordinator key; add the marker + the enumerated grant, under the 4096 B guard_adapter_size cap)
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-PATTERNS.md (§ "_role-switch-protocol.md" + § "Packaging templates + 5-tool tables" + § ".claude/agents/grugops-orchestrator.md" — exact line refs, the asymmetry rule, the adapter byte ceiling)
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-RESEARCH.md (§ Pattern 1 — the coordinator frontmatter example + the main-thread-only allowlist nuance; § Pitfall 3 asymmetry; A3 — the exact spawnable-role enumeration against the actual role set)
  </read_first>
  <action>
Edit `_role-switch-protocol.md` (D-18 #3): soften l.9 ("grugops does NOT spawn sub-agents") and drop
the l.43 absolute ("No Agent tool. No sub-agent spawn.") to a coordinator-only statement — on Claude
Code only the coordinator spawns role agents; on the four non-spawning CLIs the sequential single-window
role-load is preserved (keep that description verbatim — State-of-the-Art Deprecated note). Rewire
step-4: add the thin claim+schedule wrapper — "claim per WF17 → read/write context per WF16 → mark
done" — referencing Workflow 17 and Workflow 16, NEVER inlining their mechanics (D-05/Pitfall 6). Per
D-02 keep role Output sections untouched — handoffs keep being written; add NO new handoff coupling
(this is additive dual-running).

Edit `subagent.frontmatter.md` (D-18 #4): the current text says the wrapper grants "no spawn tool".
Reframe to the coordinator-only rule and ADD a coordinator example carrying `coordinator: true` + the
enumerated grant (RESEARCH Pattern 1). Note the main-thread-only allowlist nuance (Pitfall 1): the
enumerated allowlist is honored because the orchestrator runs as the main-thread agent; a spawned
subagent's parenthesized list is ignored. Edit `slash-command.template.md` with the sibling prose
change. Clear voice (packaging/catalog surfaces are clear voice — D-19).

Edit the 5-tool tables ASYMMETRICALLY (D-18 #5 / D-19 / Pitfall 3) — ONLY the Claude Code row changes:
  - `adapters.md` (l.34-44): change ONLY the Claude Code row's Dispatch-mode cell from "Sequential
    role-load — no spawn" to the coordinator-spawn framing (the coordinator spawns role agents on
    Claude Code; nested depth ≤5; width capped by `queue.wip_limit`). The four other rows
    (Codex/Gemini/OpenCode/Copilot) KEEP "Sequential role-load — no spawn" VERBATIM. Reconcile the
    closing "All five tools use the SAME single-window sequential role-load" paragraph to "the four
    non-spawning CLIs use sequential role-load; Claude Code adds coordinator spawning" — do not assert
    all five are identical.
  - `README.md` (l.40-46): the CC row ALREADY says "Native sub-agents — the Orchestrator spawns role
    agents" — reconcile it to the coordinator framing (coordinator: true holds the grant). The four
    other rows KEEP no-spawn verbatim. Do a single-line diff review: if the SAME line changed in more
    than one CLI row, that is the Pitfall-3 drift bug — revert it.

Edit `.claude/agents/grugops-orchestrator.md` (D-18 #6): add `coordinator: true` to the frontmatter
(D-15 marker — the loader ignores unknown keys, RESEARCH verified) and replace the `tools:` line with
the enumerated `Agent(<allowlist>)` grant PLUS the existing file/shell tools. The allowlist enumerates
the spawnable specialist role agents per D-17/A3 — the delivery specialists the coordinator schedules
(software-engineer, qe-e2e, security-nfr, and the other delivery roles from the actual role set), NOT a
broad unparenthesized grant. Keep the addition tight — `guard_adapter_size` caps this file at 4096 B.
This file is the one the inverted `guard_wr05` asserts MUST carry the grant (both-direction D-16).

After this task the real tree satisfies the inverted guard: the coordinator (orchestrator adapter)
carries marker+grant; every other SCAN file (SKILL.md, the two packaging templates) carries no grant.
  </action>
  <verify>
    <automated>node scripts/check-foundation-guards.js && npx vitest run scripts/check-foundation-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/agents/grugops-orchestrator.md` carries `coordinator: true` (`grep -c '^coordinator: true' .claude/agents/grugops-orchestrator.md` == 1) AND an enumerated Agent grant (`grep -c 'Agent(' .claude/agents/grugops-orchestrator.md` == 1), and is ≤ 4096 bytes (`wc -c < .claude/agents/grugops-orchestrator.md` < 4096)
    - `_role-switch-protocol.md` no longer contains the absolute "No `Agent` tool. No sub-agent spawn." and step-4 references WF17 + WF16 without inlining them (`grep -c '17-task-claim\|16-context-read-write' agent-factory/roles/_role-switch-protocol.md` > 0)
    - adapters.md + README.md: the four non-CC rows still contain "no spawn" / "Sequential role-load" verbatim; only the Claude Code row carries coordinator-spawn wording (a diff shows changes confined to the CC row)
    - `node scripts/check-foundation-guards.js` exits 0 on the REAL tree (the inverted guard passes: coordinator has the grant, non-coordinators do not)
    - `npx vitest run scripts/check-foundation-guards.test.ts` green (incl. the three RED fixtures from Task 1)
  </acceptance_criteria>
  <done>The protocol absolute is dropped (coordinator-only on CC, sequential preserved for the four CLIs), the packaging templates carry a coordinator example, the 5-tool tables are flipped asymmetrically (CC row only), and the orchestrator adapter carries the marker + enumerated grant under the size cap — the real tree passes the inverted guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flip the B3 wording oracle + asymmetry assertion, regenerate the catalog, capture evidence</name>
  <files>scripts/check-uat-oracles.ts, scripts/check-uat-oracles.js, scripts/check-uat-oracles.test.ts, scripts/generate-catalog.ts, scripts/generate-catalog.js, docs/catalog/README.md</files>
  <read_first>
    - scripts/check-uat-oracles.ts (oracleWr05Wording l.86-153 — the WR05_SCAN tracking-doc list l.94-99, the per-beat tolerant same-line-lookahead regexes l.105-118, the CR-01 missing-file fail-red l.125-134, the every-file-carries-every-beat set-size check l.140-145)
    - scripts/check-uat-oracles.test.ts (the wording oracle cases — the mirror/CHECK_ROOT harness; cases tagged for `-t "wording"`)
    - agent-factory/packaging/adapters.md + agent-factory/README.md (the flipped 5-tool tables from Task 2 — the asymmetry assertion's NEW scan target)
    - scripts/generate-catalog.ts (OUT = docs/catalog/README.md l.41; builds from roles/ + workflows/ — WF17 from Plan 02 means the catalog must be regenerated) + scripts/catalog-freshness.ts (the existing gate)
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-PATTERNS.md (§ "oracleWr05Wording B3" + § "generate-catalog.ts")
    - .planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-RESEARCH.md (§ Pitfall 3 + § Adversarial Reproduction step 2 — the asymmetry-drift reproduction)
  </read_first>
  <behavior>
    - oracleWr05Wording (flipped + extended): the existing closure-beats stay green over the tracking docs after the flip; a NEW asymmetry beat scans the 5-tool tables in adapters.md + README.md and FAILS if any of the four non-CC rows (Codex/Gemini/OpenCode/Copilot) gains spawn/coordinator wording, and FAILS if the CC row LOSES the coordinator-spawn wording. A missing scan file fails red (CR-01 discipline preserved).
    - catalog: regenerated deterministically from roles/ + workflows/ (now including WF17); catalog-freshness passes (the committed catalog byte-matches a fresh regen).
    - test: a planted spawn-wording edit in a non-CC row of adapters.md/README.md makes the oracle FAIL (the asymmetry-drift reproduction).
  </behavior>
  <action>
Flip `oracleWr05Wording` in lockstep (D-18 #2). The existing closure-beats narrate the historical
WR-05 retirement across the four `.planning/` tracking docs — keep them green (they record history;
verify they still pass after the flip, updating beat wording only if the flip changes what those docs
say). ADD a new asymmetry assertion (Pitfall 3 / D-19) with its OWN scan over the 5-tool tables
(`adapters.md` + `README.md`): assert each of the four non-CC rows still carries no-spawn /
sequential-role-load wording, and assert the Claude Code row carries the coordinator-spawn wording;
FAIL (clear voice, naming the row/file) if any non-CC row gains spawn/coordinator wording (the
wording-drift catcher) OR if the CC row loses it. Reuse the existing tolerant same-line-lookahead regex
form + the CR-01 missing-file-fail-red discipline; keep the explicit SCAN list (no repo-wide grep).

Update `scripts/check-uat-oracles.test.ts` (tagged `wording`): ADD the asymmetry-drift RED fixture —
plant spawn/coordinator wording into a non-CC row of adapters.md (or README.md) in the mirror and
assert the committed oracle.js exits non-zero naming the row/file (RESEARCH § Adversarial Reproduction
step 2). Keep the existing closure-beat cases.

Regenerate the docs catalog (D-18 #7 / PAR-04): run `node scripts/generate-catalog.js` so
`docs/catalog/README.md` reflects WF17 (added in Plan 02) and any flip-driven wording — commit the
regenerated catalog in THIS change (RESEARCH Runtime State: regenerate atomically with the source flip).
Verify with `node scripts/catalog-freshness.js`.

Rebuild the committed `.js` for the oracle + generator and run `npm run freshness`. Then complete the
PHASE-LEVEL adversarial gate (RESEARCH § Adversarial Reproduction — this is the WR-05 safety flip; a
green suite is NOT proof): confirm the three guard RED fixtures (Task 1) + this asymmetry RED fixture
all fail RED against the COMMITTED `.js`; ensure the `23-03-RED-baseline.txt` / `23-03-GREEN-proof.txt`
evidence pair from Task 1 is complete; and run an independent code-review/probe pass over the inverted
guard logic confirming NO code path PASSes a half-flip. Do NOT mark PAR-04 done on green tests alone.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-uat-oracles.test.ts && node scripts/catalog-freshness.js && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - oracleWr05Wording carries an asymmetry assertion scanning adapters.md + README.md (`grep -c 'adapters\|README' scripts/check-uat-oracles.ts` > 0 in the WR-05 oracle region) and FAILS when a non-CC row gains spawn wording (the test plants it and asserts nonzero)
    - the closure-beat cases still pass; a planted non-CC-row spawn edit makes `node scripts/check-uat-oracles.js` exit non-zero naming the file (asymmetry-drift reproduction)
    - `node scripts/catalog-freshness.js` exits 0 (the committed docs/catalog/README.md byte-matches a fresh regen including WF17)
    - `npm run freshness` exits 0 (oracle.js + generate-catalog.js byte-match a fresh rebuild)
    - the adversarial gate is complete: 23-03-RED-baseline.txt shows each half-flip + the asymmetry drift exiting non-zero against the committed .js; 23-03-GREEN-proof.txt shows the real tree green; an independent probe over the inverted guard is recorded
    - `npx vitest run --exclude '**/scripts/e2e/**'` green (full non-e2e regression)
  </acceptance_criteria>
  <done>The B3 oracle flips in lockstep and gains the four-CLI asymmetry assertion (proven by an asymmetry-drift RED fixture), the docs catalog is regenerated atomically with the flip and passes freshness, and the phase-level adversarial gate (three guard RED fixtures + asymmetry RED + RED/GREEN evidence pair + independent probe) is complete — PAR-04 closed on proof, not a green suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter spawn grant → access control | the `Agent(<allowlist>)` grant IS the access-control decision; the marker + guard mechanically enforce coordinator-only |
| 5-tool wording surface → capability representation | the asymmetric tables must not misrepresent a non-spawning CLI as spawning |
| committed `.js` → guard/oracle behavior | the flip must be proven against the committed artifact, not just the source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08 | Elevation of Privilege | a rogue spawn grant on a non-coordinator role (any role becomes a spawner) | mitigate | Both-direction `guard_wr05` (D-16): non-coordinator + grant FAILS RED; explicit SCAN set; RED fixture proven against committed .js. |
| T-23-09 | Denial of Service | half-flip drops the coordinator grant → CC parallelism silently dies | mitigate | Both-direction `guard_wr05`: dropped coordinator grant FAILS RED (the new D-16 direction); RED fixture. |
| T-23-10 | DoS / capability loss | the `coordinator: true` marker removed by a rename → coordinator silently demoted | mitigate | guard_wr05 marker-loss RED fixture: removed marker → FAILS RED (D-15 marker is the source of truth). |
| T-23-11 | Spoofing / Information disclosure | asymmetric wording drift — a non-spawning CLI advertised as spawning | mitigate | Extended B3 asymmetry oracle: non-CC rows MUST retain no-spawn; asymmetry-drift RED fixture. |
| T-23-12 | Elevation of Privilege | an autonomous spawned agent merges/deploys | accept (floor unchanged) | `hooks/guard.ts` PreToolUse UNCHANGED — every spawned agent still hits the mechanical merge/deploy gate; cited, not modified. |
| T-23-13 | Tampering | the committed guard/oracle/catalog .js drift from source | mitigate | `npm run freshness` (byte-match a fresh rebuild) + catalog-freshness; all committed .js rebuilt in the flip. |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (RESEARCH Package Legitimacy: N/A; dev deps already present). |
</threat_model>

<verification>
- `npx vitest run scripts/check-foundation-guards.test.ts scripts/check-uat-oracles.test.ts` green (incl. the 3 guard RED fixtures + the asymmetry RED fixture)
- `node scripts/check-foundation-guards.js` exits 0 on the real tree; `node scripts/check-uat-oracles.js` green
- `node scripts/catalog-freshness.js` exits 0; `npm run freshness` exits 0
- Adversarial gate: 23-03-RED-baseline.txt (each half-flip + asymmetry drift → nonzero vs committed .js) + 23-03-GREEN-proof.txt + independent probe recorded
- `npx vitest run --exclude '**/scripts/e2e/**'` green
</verification>

<success_criteria>
- guard_wr05 inverted to both-direction marker-keyed enforcement; the orchestrator adapter carries coordinator: true + the enumerated grant; the four non-CC rows keep no-spawn (PAR-04/SC4, D-15/D-16/D-17/D-19)
- the B3 oracle flips in lockstep + the four-CLI asymmetry assertion catches drift (Pitfall 3)
- the docs catalog is regenerated atomically; all committed .js pass freshness (D-18 #7, D-13)
- PAR-04 is closed on both-direction RED proof against the committed .js + the asymmetry-drift repro + an independent probe — NOT a green suite alone (project lesson)
</success_criteria>

<output>
Create `.planning/phases/23-parallel-execution-orchestrator-as-decomposer-one-substrate-/23-03-SUMMARY.md` when done
</output>

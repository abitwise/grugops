---
phase: "33.1"
plan: 07
type: execute
wave: 7
depends_on: ["33.1-06"]
files_modified:
  - scripts/capture-live.ts
  - scripts/capture-live.js
  - scripts/capture-live.test.ts
autonomous: true
requirements: ["D-33-R4-07", "WR-03"]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The per-run D-04 prod-deploy-deny row is keyed on the PROBE's own tool use: the runner walks assistant `tool_use` blocks with `name === \"Bash\"` whose command IS the probe, takes the `id`, finds the `user` frame's `tool_result` with that `tool_use_id`, and reads `yes` only when THAT tool result carries the prod-deploy deny text — so a deny of an ordinary command no longer satisfies the row (D-33-R4-07, WINDOWS row 299)."
    - "\"Is the probe\" is decided with the guard's OWN projection (`commandSegments` from `scripts/checkpoints.js`): the command's first segment equals the probe's words — no second tokenizer is written; a probe followed by a trailing `echo` still matches (D-33-R4-07)."
    - "By a RECORDED decision, `denyFired` moves to the same probe-keyed join (research Open Question 3): a pass whose only deny is an over-denial of an ordinary command does not prove the probe was denied; moving it tightens the outcome and cannot loosen it; the `examined` denominator is kept so a vacuous false stays visible."
    - "WR-03 (row 305): the parity docblock is restated as an APPROXIMATION (over- and under-counting can both mask a difference, because `compareLivePaths` compares counts for equality — the 'can only widen' claim is false); `.ps1`, `.psm1`, `.cmd`, `.bat` are added to `SCRIPT_EXTENSIONS` and a version suffix after an interpreter name is admitted, each RED-first — and these land BEFORE the capture so the parity reading uses the new set (WR-03)."
  artifacts:
    - path: scripts/capture-live.ts
      provides: "probe-keyed deny join in denyObservation/the D-04 row; denyFired moved to the join; WR-03 docblock restated + SCRIPT_EXTENSIONS extended"
    - path: scripts/capture-live.test.ts
      provides: "three D-33-R4-07 fixtures (non-probe-deny→no; probe+trailing echo→yes; probe tool_result not a deny→no); WR-03 RED tests per added member"
  key_links:
    - from: scripts/capture-live.ts
      to: scripts/checkpoints.js
      via: "the probe match reuses commandSegments — one projection, no second tokenizer"
      pattern: "commandSegments"
    - from: scripts/capture-live.ts
      to: scripts/capture-live.test.ts
      via: "probe-keyed row proven by a two-run fixture with only a non-probe deny in run B"
      pattern: "tool_use_id"
---

<objective>
Finish the runner fixes: key the per-run prod-deploy-deny row on the probe's own tool use (D-33-R4-07),
so a deny of an ordinary command can no longer read as the probe deny, and restate the WR-03 parity
docblock honestly while extending the counted set (row 305). Both land before the one live capture so
its outcome word and parity reading rest on the corrected instrument. The probe match reuses the
guard's own `commandSegments` — no second tokenizer (the one-authority rule).

Purpose: attribute the deny to the probe and make the parity claim honest. Output: the probe-keyed
join with `denyFired` moved onto it (recorded decision), and the WR-03 docblock + extensions.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CONTEXT.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-RESEARCH.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PATTERNS.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-33-R4-07 — key the D-04 row on the probe's own tool use; move denyFired onto the join</name>
  <behavior>
    - The per-run row reads `yes` only when the probe's own `tool_result` (joined by `tool_use_id`) carries the prod-deploy deny; a non-probe deny reads `no`.
    - "Is the probe" is decided by `commandSegments`: the command's first segment equals the probe's words; a trailing `echo` after the probe still matches.
    - `denyFired` moves to the same probe-keyed join (recorded decision); the `examined` denominator is kept.
  </behavior>
  <read_first>
    - `scripts/capture-live.ts` — `denyObservation` (:650-667, first hook_response frame matching the deny), the per-run claim (:2298-2303), `denyFired` (:2724, `runs.some`), the probe request literal (:295), `examined` denominator (:645-646).
    - `scripts/checkpoints.test.ts` — the held-capture `tool_use` walk (:2649-2663) as the shape to reuse; `scripts/checkpoints.js` `commandSegments` export.
    - `33.1-RESEARCH.md` § D-33-R4-07 (the four-step join; "is the probe" needs a defined match via commandSegments; keep denyFired apart or move it — recommendation move it; three tests) and § CONTEXT claim 14 (hook_response frames carry no tool_use_id; A:85 joined to A:103).
    - `33.1-PATTERNS.md` § `scripts/capture-live.ts` (probe-keyed deny; reuse commandSegments; keep the examined denominator) and § `scripts/capture-live.test.ts` (the deny decode two-sided pattern).
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <action>
    Add the probe-keyed join: walk assistant `tool_use` blocks (`name === "Bash"`) whose `input.command`
    IS the probe (decided by `commandSegments` from `scripts/checkpoints.js` — the first segment equals
    the probe's words; do not write a second tokenizer), take the `id`, find the `user` frame's
    `tool_result` with that `tool_use_id`, and read `yes` only when that result carries the prod-deploy
    deny text. Keep the `examined` denominator so a vacuous false stays visible. Move `denyFired` to the
    same join by a recorded decision (research Open Question 3 — it tightens the outcome and cannot
    loosen it). Add the three RED fixtures: a two-run fixture where run B has only a non-probe deny (row
    reads `no` for B); the probe followed by a trailing `echo` (reads `yes`); a probe whose `tool_result`
    is an ordinary non-deny result with an unrelated deny elsewhere (reads `no`). Rebuild; run
    check:build-parity (not a decider).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" summary — a non-probe deny reads yes, or the trailing-echo probe reads no</fails_when>
    <automated>npm run build && npm run check:build-parity && npm run typecheck</automated>
    <fails_when>non-zero exit, or check:build-parity does not print ALL CHECKS PASSED</fails_when>
  </verify>
  <acceptance_criteria>
    - The per-run row and `denyFired` both read `yes` only via the probe-keyed `tool_use_id` join; the probe match reuses `commandSegments`; the `examined` denominator is kept.
    - The three fixtures behave as specified (non-probe-deny→no; trailing-echo probe→yes; probe result not a deny→no).
    - build-parity and typecheck clean.
  </acceptance_criteria>
  <done>The prod-deploy deny is attributed to the probe's own tool use, and denyFired can no longer be satisfied by an ordinary command's deny.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-03 — restate the parity docblock as an approximation; extend the counted set</name>
  <behavior>
    - The docblock states the real property: an approximation where over- and under-counting can both mask a difference (because `compareLivePaths` compares counts for equality).
    - `.ps1`, `.psm1`, `.cmd`, `.bat` are in `SCRIPT_EXTENSIONS`; a version suffix after an interpreter name is admitted; each is RED-first.
  </behavior>
  <read_first>
    - `scripts/capture-live.ts` — `WRITE_SHAPED_COMMAND_WORDS` and its docblock (:1112-1125, :1182-1186, :1285-1289), `compareLivePaths` count equality (:1257), `SCRIPT_EXTENSIONS`.
    - `33.1-RESEARCH.md` § WR-03 (state the real property; add the four extensions; admit a version suffix; land before the capture) and § "The capture wave" (the go statement says which set it uses).
    - `33.1-PATTERNS.md` § `scripts/capture-live.ts` (WR-03).
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <action>
    Restate the WR-03 docblock as an approximation (over- and under-counting can both mask a difference;
    the "can only widen / never a false pass" claim is false because `compareLivePaths` compares counts
    for equality). Add `.ps1`, `.psm1`, `.cmd`, `.bat` to `SCRIPT_EXTENSIONS` and admit a version suffix
    after an interpreter name, each with a RED test proving the new member is counted. These are parity
    inputs, so they must land now (before the capture) — the go statement (plan 10) states which set the
    parity reading uses. Rebuild; run check:build-parity.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" summary — a new SCRIPT_EXTENSIONS member or the version suffix is not counted</fails_when>
    <automated>npm run build && npm run check:build-parity && npm run typecheck</automated>
    <fails_when>non-zero exit, or check:build-parity does not print ALL CHECKS PASSED</fails_when>
  </verify>
  <acceptance_criteria>
    - The parity docblock states the approximation; no "can only widen" claim remains.
    - `.ps1`, `.psm1`, `.cmd`, `.bat` and a versioned interpreter suffix are counted, each RED-first.
    - build-parity and typecheck clean; the change lands before the capture wave.
  </acceptance_criteria>
  <done>The parity claim is honest and the counted set covers script extensions and versioned interpreters, before any capture reads it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a hook_response deny frame → the probe-deny claim | Those frames carry no tool_use_id; a deny of any command must not satisfy the row |
| the runner's probe match → a second tokenizer | Must reuse the guard's own commandSegments, not a new grammar |
| the parity count comparison → the outcome word | Count equality means over- and under-counting can both mask a difference |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-19 | Repudiation | a non-probe deny counted as the probe deny | high | mitigate | Probe-keyed `tool_use_id` join; denyFired moved onto it; `examined` denominator kept |
| T-33.1-20 | Repudiation (false proof) | a second tokenizer disagreeing with the guard | medium | mitigate | The probe match reuses `commandSegments` from `scripts/checkpoints.js` |
| T-33.1-21 | Repudiation | a false parity pass from count equality | medium | mitigate | Docblock restated as an approximation; extensions added RED-first before the capture |
| T-33.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed in this phase; nothing to audit |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts` — green.
- `npm run build && npm run check:build-parity && npm run typecheck` — ALL CHECKS PASSED, clean.
- Never `npm test`; never run the live lane in this plan.
</verification>

<success_criteria>
- The per-run row and `denyFired` read `yes` only via the probe-keyed join; the probe match reuses `commandSegments`.
- The parity docblock is honest and the counted set is extended, before the capture reads it.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| probe-keyed deny join | runner fix | `scripts/capture-live.ts`; reuses `commandSegments` |
| `denyFired` moved onto the join | recorded decision | tightens the outcome (OQ3) |
| restated WR-03 parity docblock | doc-in-code | approximation, not "can only widen" |
| `SCRIPT_EXTENSIONS` += `.ps1`/`.psm1`/`.cmd`/`.bat` + versioned interpreter | parity input | each RED-first |
| three D-33-R4-07 fixtures | tests | `scripts/capture-live.test.ts` |

<output>
Create `.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-07-SUMMARY.md` when done
</output>
---
phase: "33.1"
plan: 05
type: execute
wave: 5
depends_on: ["33.1-04"]
files_modified:
  - scripts/checkpoints.ts
  - scripts/checkpoints.js
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
  - scripts/checkpoints.test.ts
  - scripts/fixtures/cr01-nested-corpus.json
  - .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-GUARD-REPLAY.md
  - .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-REDTEAM-1.md
  - .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-REDTEAM-2.md
  - .planning/WINDOWS.md
  - .planning/phases/33-live-capture-windows-portability/deferred-items.md
autonomous: false
requirements: ["CR-01", "CR-02", "WINDOWS-269"]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The guard replay report `33.1-GUARD-REPLAY.md` records every corpus row through BOTH `hooks/guard.js` and `hooks/hook-entry.js guard.js` with its exit code, signal, fd-3 token and verdict; rows are referred to by id only, the command text living in the fixture (D-05(c))."
    - "At least two INDEPENDENT opus red-team passes (`33.1-REDTEAM-1.md`, `33.1-REDTEAM-2.md`) are run over C1-C5; every reported bypass is self-reproduced and added as a NEW RED corpus row, then closed WITHIN this wave before the checkpoint, so a red-team finding re-enters as a RED row inside the same wave under the 4-round cap (D-05(d); project gap-closure discipline)."
    - "After the guard wave a BLOCKING HUMAN CHECKPOINT (D-07) presents the replay report and both red-team reports with their self-reproductions; no later wave starts without the human's go. The guard NEVER closes by human override — a reproducible prod-deploy guard bypass at the checkpoint is fixed inside this wave, and if the cap is reached with the guard still bypassable the phase stops and the guard carries forward as the FIRST open item, unoverridden (D-07; D-08 — one-way)."
    - "D-04's narrowing (row 269) is DEFERRED by D-04's own rule (the non-launching set is not derivable and recovers 1 of 17 measured over-denials); the regex-throw over-denial (row 296) is a deny-to-allow change re-owned rather than landed in this frozen-grammar wave; both are dispositioned in `.planning/WINDOWS.md` by annotation only, no code (D-04; D-09)."
    - "After the go, WINDOWS rows 301, 302, 306 and 308 are marked fixed and the guard-class deferred-items entries disposed (D-02(a), D-02(b), IN-02, LB-08 `-c` form, RES-03 literal-verb form) — a residual stays pinned ALLOW only where the red-team confirmed the value is computed at run time; otherwise it is a DENY row (D-09)."
  artifacts:
    - path: .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-GUARD-REPLAY.md
      provides: "every corpus row × both entry points × {exit code, signal, fd-3 token, verdict}"
    - path: .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-REDTEAM-1.md
      provides: "independent opus red-team pass 1 with finding IDs, reproductions, and the new RED corpus row id pinning each"
    - path: .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-REDTEAM-2.md
      provides: "independent opus red-team pass 2, same layout"
  key_links:
    - from: .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-REDTEAM-1.md
      to: scripts/fixtures/cr01-nested-corpus.json
      via: "each reported bypass is self-reproduced as a new RED corpus row before the checkpoint"
      pattern: "cr01-nested-corpus"
    - from: .planning/WINDOWS.md
      to: .planning/phases/33-live-capture-windows-portability/deferred-items.md
      via: "guard-class entries disposed after the D-07 go; rows 301/302/306/308 marked fixed"
      pattern: "gsd-tools windows fixed"
---

<objective>
Close the guard wave the way this project's safety memory requires: a green suite is not proof. Build
the corpus replay report over both entry points, run two independent opus red-teams over C1-C5,
self-reproduce every reported bypass as a new RED corpus row and close it inside this wave, then stop
at the blocking human checkpoint (D-07). The guard never closes by override (D-08): a bypass found at
the checkpoint is fixed here, and if the 4-round cap is reached still bypassable the phase stops with
the guard carried unoverridden as the first open item.

D-04's narrowing (row 269) is deferred by its own rule and the regex-throw (row 296) is re-owned —
both dispositioned by annotation only, so the red-team reviews one frozen grammar, not a moving one.
After the go, the guard WINDOWS rows are marked fixed and the guard-class deferred-items disposed.

Purpose: prove the cutover adversarially and gate it behind a human. Output: the replay + red-team
reports, any self-repro closures, and the guard-class ledger dispositions.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CONTEXT.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-RESEARCH.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PATTERNS.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard replay report + two independent red-teams + self-reproduction</name>
  <read_first>
    - `scripts/fixtures/cr01-nested-corpus.json` and `scripts/checkpoints.test.ts` — the corpus and its both-entry replay.
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` — the report header discipline (bar, sha, host, date, exact command, "Prohibition honoured"); `.../33-REVIEW.md` — the finding-ID + reproduction layout to mirror; `.../33-VERIFICATION.md` — the independent reproduction through both hook entries.
    - `33.1-RESEARCH.md` § "Reproductions" (bypass forms by id), § Common Pitfalls, § Assumptions Log (probe every ARM a command can reach the decision through; when a predicate is split into arms, test their UNION); `33.1-PATTERNS.md` § Phase-dir reports (raw control bytes: spell escapes; run `npm run check:nul-bytes`).
  </read_first>
  <files>.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-GUARD-REPLAY.md, .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-REDTEAM-1.md, .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-REDTEAM-2.md, scripts/checkpoints.ts, scripts/checkpoints.js, hooks/hook-entry.ts, hooks/hook-entry.js, scripts/checkpoints.test.ts, scripts/fixtures/cr01-nested-corpus.json</files>
  <action>
    Build `33.1-GUARD-REPLAY.md`: every corpus row × both entry points, with exit code, signal, fd-3
    token and verdict, headers copying `33-CI-MEASUREMENT.md`'s discipline (sha, host, date, exact
    command, "Prohibition honoured" — `npm test` not run). Run two INDEPENDENT opus red-team passes over
    C1-C5, each filed as `33.1-REDTEAM-{1,2}.md` in the finding-ID + reproduction layout of `33-REVIEW.md`.
    For every reported bypass: self-reproduce it, add it as a NEW RED corpus row (command derived, never
    a raw string in this plan), then close it in `scripts/checkpoints.ts` within this wave (RED-first,
    mutation-proven, manifest regenerated, both entry points). Probe every ARM a command can reach the
    decision through, and test the UNION of any split predicate. Spell control bytes as escapes in the
    reports and run `npm run check:nul-bytes`. If a bypass cannot be closed within the cap, record it and
    stop (Task 2 presents the honest state; D-08 forbids an override).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" summary — a self-reproduced bypass row does not deny</fails_when>
    <automated>npm run generate:hook-manifest && npm run freshness:hook-manifest && npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or check:nul-bytes names a committed report as carrying a control byte</fails_when>
    <automated>test -s .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-REDTEAM-1.md &amp;&amp; test -s .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-REDTEAM-2.md &amp;&amp; test -s .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-GUARD-REPLAY.md</automated>
    <fails_when>non-zero exit — a required report (replay or either red-team) is missing or empty</fails_when>
  </verify>
  <acceptance_criteria>
    - `33.1-GUARD-REPLAY.md` records every corpus row × both entry points × {exit code, signal, fd-3 token, verdict}; rows referenced by id.
    - Two independent opus red-team reports exist; every reported bypass is self-reproduced as a new RED corpus row and closed within this wave, manifest regenerated; `check:nul-bytes` clean.
    - The offline suite is green; no bypass is left open under the cap without being recorded.
  </acceptance_criteria>
  <done>The cutover is proven adversarially through both entry points, and every reported bypass is a closed RED row.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: D-07 — the blocking guard checkpoint; the guard never closes by override (D-08, one-way)</name>
  <what-built>
    The canonical-form cutover (C1-C5) has landed across `scripts/checkpoints.ts`, both hook entry
    points run the same decider, the corpus is a derived mechanism × family × arm matrix reaching both
    the readable and the fail-closed arm, and the allow-control harness (WR-04) proves a real allow that
    cannot pass on a crash or timeout. Two independent opus red-teams have run; every reported bypass was
    self-reproduced as a RED corpus row and closed within this wave. `33.1-GUARD-REPLAY.md` records every
    row through both `hooks/guard.js` and `hooks/hook-entry.js guard.js`.
  </what-built>
  <how-to-verify>
    Before you answer:
    1. Read `33.1-GUARD-REPLAY.md` — every corpus row DENY through both entry points, with exit code,
       signal and fd-3 token.
    2. Read `33.1-REDTEAM-1.md` and `33.1-REDTEAM-2.md` — each finding, its self-reproduction, and the
       corpus row id that now pins it as RED-then-closed.
    3. Confirm no reported bypass remains open. Under D-08 the guard never closes by override: if any
       reproducible bypass is still open, the answer is not "go" — the phase stops and the guard carries
       forward as the first open item, unoverridden.
  </how-to-verify>
  <resume-signal>Type "go" only if no reproducible bypass remains open, or describe the bypass to fix inside this wave</resume-signal>
</task>

<task type="auto">
  <name>Task 3: After the go — mark the guard WINDOWS rows fixed, dispose the guard-class ledger, defer row 269 and re-own row 296</name>
  <read_first>
    - `.planning/WINDOWS.md` — the header rule (:10-14, status via `gsd-tools windows`), rows 269, 296, 301, 302, 306, 308, and the row-269 in-cell `DISPOSITION <date>` annotation precedent (commit `266e8df2`).
    - `.planning/phases/33-live-capture-windows-portability/deferred-items.md` — the guard-class open entries (D-02(a) alias, D-02(b) splice, regex throw, GNU parallel, LB-08, RES-03) and the `status: resolved` + `**Resolved by:**` convention.
    - `33.1-RESEARCH.md` § D-04 feasibility (defer by D-04's own rule; row 269 stays open) and § D-09 routing; `33.1-PATTERNS.md` § `.planning/WINDOWS.md` and § deferred-items (annotate, never rewrite).
  </read_first>
  <files>.planning/WINDOWS.md, .planning/phases/33-live-capture-windows-portability/deferred-items.md</files>
  <action>
    Only after Task 2 answered "go": mark WINDOWS rows 301, 302, 306, 308 fixed via
    `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows fixed <id>`. Annotate row 269 with a
    `DISPOSITION <date>` in-cell suffix recording that the D-04 narrowing is deferred by D-04's own rule
    (the non-launching set is not derivable; recovers 1 of 17 over-denials), owner restated — no code.
    Annotate row 296 recording the regex-throw over-denial is a deny-to-allow change re-owned to a later
    milestone (out of the frozen-grammar wave), no code. Flip the guard-class `deferred-items.md` entries
    to `status: resolved` with a `**Resolved by:**` line naming this plan, the date, the commit and the
    corpus row id — or, where the red-team confirmed a residual is computed at run time, record it as a
    pinned residual with its row. Never rewrite a historical row; annotate.
  </action>
  <verify>
    <automated>for id in 301 302 306 308; do grep -aE "^\| $id \| 33 " .planning/WINDOWS.md | grep -aq '| fixed |' || { echo "row $id not marked fixed"; exit 1; }; done</automated>
    <fails_when>non-zero exit — a guard WINDOWS row (301/302/306/308) is not marked fixed after the go</fails_when>
    <automated>grep -ac 'DISPOSITION' .planning/WINDOWS.md</automated>
    <fails_when>a count of 0 — rows 269 and 296 were not annotated with a DISPOSITION suffix</fails_when>
    <automated>grep -ac 'status: resolved' .planning/phases/33-live-capture-windows-portability/deferred-items.md</automated>
    <fails_when>a count no greater than the pre-plan count — no guard-class deferred entry was disposed</fails_when>
  </verify>
  <acceptance_criteria>
    - WINDOWS rows 301, 302, 306, 308 are marked fixed via `gsd-tools windows fixed`; rows 269 and 296 carry a `DISPOSITION <date>` annotation with no code change.
    - The guard-class `deferred-items.md` entries (D-02(a), D-02(b), IN-02, LB-08 `-c` form, RES-03 literal-verb) are flipped to `status: resolved` with a `**Resolved by:**` line, or recorded as a pinned run-time-computed residual with its row.
    - No historical row is rewritten; annotations only.
  </acceptance_criteria>
  <done>The guard is closed on the record: fixed rows marked, deferred narrowings annotated, guard-class deferred items disposed — never by override.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a green offline suite → "the guard is proof" | A green suite is not proof for a safety invariant; adversarial reproduction is required |
| the human go → the rest of the phase | No later wave starts without it; the go gates on reproduced evidence, not narration |
| a reproducible bypass at the checkpoint → the phase | D-08: never accepted open by override; fixed in-wave or the phase stops |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-13 | Repudiation (false proof) | closing the guard on a green suite alone | critical | mitigate | Two independent red-teams + self-reproduction as RED rows; the D-07 human checkpoint on reproduced evidence |
| T-33.1-14 | Elevation of privilege | a bypass accepted open by override | critical | mitigate | D-08: the guard never closes by override; the phase stops with it carried unoverridden |
| T-33.1-15 | Tampering | a deny-to-allow change (row 296) in a frozen-grammar wave | medium | mitigate | Row 296 re-owned by annotation only; no code lands here so the red-team reviews one grammar |
| T-33.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed in this phase; nothing to audit |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts` — green.
- `npm run generate:hook-manifest && npm run freshness:hook-manifest && npm run check:nul-bytes` — fresh, clean.
- Both red-team reports and the replay report exist and are non-empty.
- After the go: WINDOWS rows 301/302/306/308 fixed; rows 269/296 annotated; guard-class deferred items disposed.
- Never `npm test`.
</verification>

<success_criteria>
- Two independent red-teams run; every reported bypass is a closed RED corpus row; the D-07 human checkpoint gates the rest of the phase.
- The guard never closes by override; the guard WINDOWS rows and guard-class deferred items are disposed on the record.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `33.1-GUARD-REPLAY.md` | new report | every corpus row × both entry points × {exit, signal, fd-3, verdict} |
| `33.1-REDTEAM-1.md`, `33.1-REDTEAM-2.md` | new reports | two independent opus red-teams with self-reproductions |
| self-reproduced bypass rows | fixture rows | each RED-then-closed within this wave |
| WINDOWS rows 301/302/306/308 fixed; 269/296 annotated | ledger | via `gsd-tools windows`, annotation-only for 269/296 |
| guard-class deferred-items dispositions | ledger | `status: resolved` or pinned residual |

<output>
Create `.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-05-SUMMARY.md` when done
</output>
---
phase: "33.1"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/guard.test.ts
  - hooks/admission-guard.test.ts
  - scripts/checkpoints.ts
  - scripts/checkpoints.js
  - hooks/guard.ts
  - hooks/guard.js
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
  - scripts/fixtures/cr01-nested-corpus.json
autonomous: true
requirements: ["WR-04", "CR-01"]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The allow-control harness proves a REAL allow and cannot pass on a crash or a timeout: every allow assertion checks exit 0, signal null, empty stdout, and the fd-3 token `grugops-hook-allow` on a direct `guard.js` run; a decider stub that exits non-zero with no stdout, and one that hangs past the spawn bound, each turn the allow controls RED (WR-04, WINDOWS row 306; D-05(a) — this lands FIRST because every later allow control depends on it)."
    - "The stdout-only allow-site set is DERIVED with one stated grep, its count asserted in the test, and every member routed through the single `expectAllowed` helper across `hooks/guard.test.ts` and `hooks/admission-guard.test.ts` — no hand-typed site list (derive the set, assert the count)."
    - "One CR-01 mechanism (a quoted value whose next-shell reading names a governed tool is re-read as shell text) is closed END-TO-END: the C2 trigger at `scripts/checkpoints.ts:1866` widens from whitespace-only to 'has whitespace OR `governedToolsNamedBy(w.value)` is non-empty', so one nested-body corpus row DENIES through BOTH `hooks/guard.js` and `hooks/hook-entry.js guard.js`, proven with the real allow-control harness (D-01 canonical cutover; D-05(c) both entry points; WINDOWS row 301)."
    - "The closure is RED-first and mutation-proven: the new corpus row is shown DENY, and restoring the whitespace-only C2 trigger reopens it RED; the decider manifest is regenerated so `hook-entry.js` does not deny uniformly on a stale hash (D-05)."
  artifacts:
    - path: hooks/guard.test.ts
      provides: "expectAllowed helper (status 0, signal null, empty stdout, fd-3 token), fd-3 capture helper, runAll returning {stdout, code, signal, fd3}, all allow sites routed through it, crash/hang mutation controls"
    - path: scripts/checkpoints.ts
      provides: "C2 re-read trigger widened to include governedToolsNamedBy(w.value)"
    - path: scripts/fixtures/cr01-nested-corpus.json
      provides: "one RED CR-01 nested-re-read row (id + source, command derived from RESEARCH § Reproductions at execution time)"
  key_links:
    - from: hooks/guard.test.ts
      to: hooks/hook-entry.ts
      via: "expectAllowed mirrors the wrapper's own allow rule (signal, non-zero exit, empty-stdout-without-fd3-token are not-an-allow)"
      pattern: "grugops-hook-allow"
    - from: scripts/checkpoints.ts
      to: hooks/hook-entry.ts
      via: "checkpoints.js is in guard.js's decider closure; every edit regenerates the manifest"
      pattern: "generate:hook-manifest"
---

<objective>
Lead the guard wave with a production-quality tracer: prove ONE prod-deploy guard bypass closes
end-to-end, through both hook entry points, with an allow-control harness that cannot lie. This slice
touches every layer the guard wave will modify — the corpus fixture, the one classifier authority
(`scripts/checkpoints.ts`), the build + decider manifest, both entry points, and the spawn test
harness — so an architectural dead-end is caught on the first commit, not after five rules land.

The WR-04 harness fix (D-05(a)) lands FIRST here because every later allow control in this phase
depends on a real allow-proof. The tracer proves the harness on one path; task 2 rolls it out to
every derived allow site.

This closes the headline CR-01 nested class by the C2 trigger widening only (D-01's canonical
cutover; the remaining rules C1, C3, C4, C5 are expansion plans 02-04). All carried work stays in
one phase under a 4-round gap-closure cap (D-06).

Purpose: make the guard's allow-proof trustworthy and prove the cutover architecture on a real path.
Output: a WR-04-hardened harness, a single RED-then-DENY CR-01 row, and a regenerated manifest.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CONTEXT.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-RESEARCH.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "one CR-01 bypass denies with a real allow-proof" — one path only</name>
  <read_first>
    - `hooks/guard.test.ts` — `runAll` (~:2382-2407, resolves stdout alone), `expectAllow` (~:84-92), `runGuard` (~:71-82, returns status), the scrubbed env `bare()` (~:2371-2378), the ENTRY_POINTS pair and `nestedRow` reader (~:2497-2511), the kit-copy + `reseal` stub pattern (~:1600-1690) including the existing hang stub (~:1688).
    - `hooks/hook-entry.ts` — the wrapper's own allow rule (~:522-557): fd-3 opened by `stdio: [0, "pipe", "inherit", "pipe"]`; signal, non-zero exit, and empty-stdout-without-the-fd3-token are each not-an-allow. The manifest region markers (~:250-283) and the freshness docblock.
    - `hooks/guard.ts` — the token write before exit and its tolerance of fd 3 being absent (~:138-146).
    - `scripts/checkpoints.ts` — the C2 re-read trigger (~:1866-1870), `governedToolsNamedBy` (~:1467-1476), `commandSegments` depth bound (~:582-584).
    - `scripts/fixtures/cr01-nested-corpus.json` — the row shape (`id`, `source`, `kind`, `command`) and the `about`/`families` header.
    - `33.1-RESEARCH.md` § Reproductions (CR-01 rows) and § "The canonical-form cutover" C2; `33.1-PATTERNS.md` § `hooks/guard.test.ts` and § `scripts/checkpoints.ts`.
  </read_first>
  <files>hooks/guard.test.ts, scripts/checkpoints.ts, scripts/checkpoints.js, hooks/hook-entry.ts, hooks/hook-entry.js, scripts/fixtures/cr01-nested-corpus.json</files>
  <action>
    Wire ONE CR-01 path through every layer, with a real allow-proof:
    1. In `hooks/guard.test.ts`, change `runAll` to resolve `{ stdout, code, signal, fd3 }` (the `close`
       event already carries code and signal; keep the 8-worker pool and `bare()` env). Add one
       `expectAllowed(result)` helper that asserts `status === 0`, `signal === null`, `stdout === ""`,
       and — on a direct `guard.js` run spawned with fd 3 opened as a pipe — that fd 3 carried the token
       `grugops-hook-allow`. Add the fd-3 capture helper (a spawn whose stdio opens fd 3 and reads
       `child.stdio[3]`). Mirror the wrapper's own allow rule from `hooks/hook-entry.ts`; do not invent a
       weaker bar.
    2. In `scripts/checkpoints.ts`, widen the C2 trigger at `:1866` from "value has whitespace" to
       "value has whitespace, OR `governedToolsNamedBy(w.value)` is non-empty". The re-read goes through
       the same `commandSegments` and the same queue, so it recurses within the existing depth bound.
       Change no deny wording; add denials only (keep `scripts/autonomy-zero-config.test.ts` green,
       Pitfall 3).
    3. Add exactly ONE CR-01 nested-re-read row to `scripts/fixtures/cr01-nested-corpus.json`, deriving
       the command string from `33.1-RESEARCH.md` § Reproductions (do not restate any bypass string in
       this plan). `kind: "deny"`, `source` naming its CR-01 / R-NN id.
    4. Run `npm run generate:hook-manifest` (it runs tsc, regenerates the manifest region in
       `hooks/hook-entry.ts`, then rebuilds), so `hook-entry.js` picks up the new checkpoints.js hash.
    5. Prove end-to-end: the new row DENIES through both `node hooks/guard.js` and
       `node hooks/hook-entry.js guard.js`; the C2 mutation (restore the whitespace-only trigger)
       reopens the row RED; a crash decider stub (exits non-zero, no stdout) and the existing hang stub
       each turn the tracer's allow-control RED (use the kit-copy + `reseal` pattern so the manifest
       check passes and the branch under test is actually reached).
    Commit `scripts/checkpoints.{ts,js}`, `hooks/hook-entry.{ts,js}`, the harness and the fixture
    together.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' hooks/guard.test.ts scripts/checkpoints.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" / no-tests summary — the CR-01 row does not deny end-to-end or the allow controls do not hold</fails_when>
    <automated>npm run generate:hook-manifest && npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit, or the freshness output does not report the deciders fresh — a stale manifest would make hook-entry.js deny uniformly</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/autonomy-zero-config.test.ts scripts/floor-invariance.test.ts</automated>
    <fails_when>non-zero exit — the C2 change altered a roster decision or deny wording (AUTO-07 differential), or the manifest closure derivation drifted</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npm run typecheck</automated>
    <fails_when>non-zero exit, or check:build-parity does not print ALL CHECKS PASSED — the committed .js drifted from its .ts</fails_when>
  </verify>
  <acceptance_criteria>
    - `runAll` resolves `{stdout, code, signal, fd3}` and `expectAllowed` asserts status 0, signal null, empty stdout, and the fd-3 token on a direct run.
    - One CR-01 nested-re-read row exists in the fixture with `kind: "deny"` and denies through both entry points; restoring the whitespace-only C2 trigger reopens it RED; a crash stub and a hang stub each turn the allow-control RED.
    - The manifest is regenerated and freshness reports the deciders fresh; build-parity and typecheck pass; `scripts/autonomy-zero-config.test.ts` stays green.
  </acceptance_criteria>
  <done>One CR-01 path is closed end-to-end through both entry points, proven by a harness that fails on a crash or timeout, and committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-04 completeness — route every derived allow site through the real allow-proof</name>
  <behavior>
    - Every stdout-only allow assertion in `hooks/guard.test.ts` and `hooks/admission-guard.test.ts` is routed through `expectAllowed`, so a crash or a hang fails it.
    - The site set is derived by one stated grep, its count asserted in a test; a member missed by the grep is surfaced, not silently skipped.
    - The two `expectAllow` helpers are retired (or restated per file — a test file must not import another test file's cases); `admission-guard.test.ts` gets its own copy of the helper (no cross-import).
  </behavior>
  <read_first>
    - `hooks/guard.test.ts` — the inline allow sites named in `33.1-RESEARCH.md` § Pitfall 5 (the `expectAllow` helper and its call sites; the inline `toBe("")` and `not.toContain` allow checks; the `runAll` consumers). The already-safe sites (a `status` assertion beside the stdout check) that must be left alone.
    - `hooks/admission-guard.test.ts` — `expectAllow` (~:73-76) and its call sites; `runGuard` (~:48-66) returns status with a 20 000 ms timeout.
    - `33.1-RESEARCH.md` § Pitfall 5 (the site table flags its own count as unreconciled — derive, do not trust the number) and § Wave 0 Gaps; `33.1-PATTERNS.md` § `hooks/admission-guard.test.ts` (do not import from `hooks/guard.test.ts`).
  </read_first>
  <files>hooks/guard.test.ts, hooks/admission-guard.test.ts</files>
  <action>
    Derive the full stdout-only allow-site set with ONE stated grep (record the grep and its count in the
    plan summary), assert that count in a test so a new unrouted site fails the suite, and route every
    member through `expectAllowed`. Do the same in `hooks/admission-guard.test.ts` with its own copy of
    the helper (restate it or move it to a non-test module; never import a test file into another test
    file). Leave the already-safe sites unchanged. Add the crash-stub and hang-stub mutation controls
    that must turn the allow controls RED across the derived set. Do not weaken any existing DENY
    assertion. No production source changes in this task — harness only, so no manifest regen is needed.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' hooks/guard.test.ts hooks/admission-guard.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" summary — an allow site is unrouted, or the asserted site count does not match the derived set</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/autonomy-zero-config.test.ts</automated>
    <fails_when>non-zero exit — a harness change accidentally altered guard behavior expectations</fails_when>
  </verify>
  <acceptance_criteria>
    - The stdout-only allow-site set is derived by one recorded grep, its count asserted; every member routed through `expectAllowed` in both test files.
    - The crash and hang decider stubs turn every allow control RED (mutation-proven); no existing DENY assertion weakened.
    - `hooks/admission-guard.test.ts` holds its own helper copy (no cross-test-file import).
  </acceptance_criteria>
  <done>Every allow control in the guard and admission harnesses proves a real allow, and the site set is derived and count-asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent Bash command string → the guard decider | Untrusted input; the only input the model sees is `tool_input.command` |
| decider process → the wrapper's allow verdict | fd-3 token is the private allow channel; a crash/timeout must never read as allow |
| `scripts/checkpoints.js` hash → `hooks/hook-entry.js` manifest | A stale manifest denies everything; a hand-edited manifest is refused |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-01 | Repudiation (false proof) | the spawn allow harness | high | mitigate | `expectAllowed` asserts exit 0, signal null, empty stdout, fd-3 token; crash/hang stubs mutation-prove it (WR-04) |
| T-33.1-02 | Elevation of privilege | the C2 nested re-read gap | high | mitigate | Widen the re-read trigger to any value naming a governed tool; RED row + mutation through both entry points |
| T-33.1-03 | Tampering | a stale/hand-edited decider manifest | high | mitigate | `generate:hook-manifest` after every decider edit; `freshness:hook-manifest` in the gate |
| T-33.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed in this phase; dev deps unchanged; nothing to audit |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' hooks/guard.test.ts hooks/admission-guard.test.ts scripts/checkpoints.test.ts` — green.
- `npm run generate:hook-manifest && npm run freshness:hook-manifest` — deciders fresh.
- `npm run build && npm run check:build-parity && npm run typecheck` — ALL CHECKS PASSED, clean.
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/autonomy-zero-config.test.ts scripts/floor-invariance.test.ts` — green.
- Never `npm test` (it runs the live token-spending e2e lane).
</verification>

<success_criteria>
- WR-04 harness fixed across the derived site set with an asserted count; a crash or hang fails every allow control.
- One CR-01 nested row closed end-to-end through both entry points, RED-first and mutation-proven, with the manifest regenerated.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `expectAllowed(result)` in `hooks/guard.test.ts` | new helper | asserts exit 0, signal null, empty stdout, fd-3 token `grugops-hook-allow` |
| fd-3 capture helper in `hooks/guard.test.ts` | new helper | direct `guard.js` run reading `child.stdio[3]` |
| `runAll` → `{stdout, code, signal, fd3}` | changed signature | `hooks/guard.test.ts` |
| C2 trigger widening at `scripts/checkpoints.ts:1866` | rule change | re-read any value naming a governed tool |
| one CR-01 nested-re-read row | new fixture row | `scripts/fixtures/cr01-nested-corpus.json` |
| regenerated decider manifest | build output | `hooks/hook-entry.{ts,js}`, `scripts/checkpoints.js` |

<output>
Create `.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-01-SUMMARY.md` when done
</output>

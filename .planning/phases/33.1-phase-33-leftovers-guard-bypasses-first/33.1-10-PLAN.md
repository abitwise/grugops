---
phase: "33.1"
plan: 10
type: execute
wave: 10
depends_on: ["33.1-09"]
files_modified:
  - .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-DRYRUN-REPORT.md
  - .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-A.jsonl
  - .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-B.jsonl
  - .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-SUMMARY.md
  - .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-DIAGNOSIS.md
autonomous: false
requirements: ["CAP-01", "CAP-03"]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Exactly ONE authorized live capture runs, in this last wave, behind its own blocking go (D-10). The go statement states: the spend range (round-4 was 4.40 USD / 17 min 57 s; round-1 was 11.75 USD / 31 min 07 s — both observed, the 40-minute wall bound as the ceiling from two calls at `CALL_BOUND_MS = 1_200_000`); the Claude Code version at the go; and each D-11 precondition WITH its commit (D-10)."
    - "The go's preconditions are all met and cited (D-11): (a) the committed permission-mode evidence and the `--permission-mode default` pin (plan 06); (b) the runner fixes D-33-R4-04 and D-33-R4-07 green (plans 06, 07) plus the WR-03 set change (plan 07); (c) the guard redesign D-01..D-05 landed and PASSED the D-07 checkpoint (plan 05), so the probe deny proves the NEW guard. The windows leg is NOT a precondition (D-11; CAP-02 cannot be met here, D-15)."
    - "The push comes BEFORE the go as its own blocking-human checkpoint (the runner's pushed-sha readiness row reads not-ready whenever local commits are held; rounds 2 and 3 lost the go on that row alone); readiness is re-derived at zero tokens against the pushed sha reading GO-READINESS: ready; NOTHING is committed between the push and the run (the dry-run report is written untracked and committed with the capture set after)."
    - "A `--max-budget-usd` ceiling at the go is a HUMAN decision stated in the go text (a mid-run budget cut reads red for a reason unrelated to the mechanism, and D-12 forbids a second go); the Claude Code version is named and the zero-token permission-mode + probe-reach checks are re-run on it (A4, A5, A6)."
    - "If the capture returns red, a zero-token diagnosis runs from the committed transcripts (`33.1-CAPTURE-DIAGNOSIS.md`), nothing flips, and there is NO second go inside 33.1 without a new human decision — GAP-D1 stays open (D-12); a loud skip is never a capture; `npm test` is never run outside this one go."
  artifacts:
    - path: .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-DRYRUN-REPORT.md
      provides: "zero-token readiness against the pushed sha (GO-READINESS: ready), written untracked before the go, committed after the run"
    - path: .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-SUMMARY.md
      provides: "one outcome line; the probe-keyed deny; CAP-03 sides (a)/(b); parity from write-shaped routes; permission mode read back; the two UNKNOWN-verify items settled"
    - path: .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-DIAGNOSIS.md
      provides: "zero-token diagnosis on a red outcome (conditional)"
  key_links:
    - from: the human push
      to: the runner's pushed-sha readiness row
      via: "git rev-parse HEAD == git rev-parse origin/main → GO-READINESS: ready → the go checkpoint"
      pattern: "GO-READINESS: ready"
    - from: the capture's probe deny
      to: the redesigned guard through the installed plugin hook
      via: "D-11(c) — the probe deny proves the new guard; keyed on the probe tool use (plan 07)"
      pattern: "Production deploy blocked"
---

<objective>
Spend 33.1's ONE authorized live capture go (D-10), in the last wave, behind its own blocking human
checkpoint, against a pushed sha, on an instrument whose guard has passed the D-07 checkpoint and whose
runner fixes (permission-mode pin, coordinator exemption, probe-keyed deny, WR-03 parity) have all
landed. The push precedes the go as its own checkpoint because the runner's pushed-sha readiness row
reads not-ready whenever local commits are held (rounds 2 and 3 lost the go on that row alone), and
nothing is committed between the push and the run. A red result gets a zero-token diagnosis and no
second go (D-12); CAP-02 cannot be met here (D-15).

Purpose: prove the redesigned guard and the spawn fix with one captured dual-path run toward CAP-01 and
CAP-03. Output: a ready dry-run report, and either a capture set with one outcome line or a zero-token
diagnosis.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CONTEXT.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-RESEARCH.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PATTERNS.md
@.planning/phases/33-live-capture-windows-portability/33-41-PLAN.md
@.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-09-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Checkpoint — push so the go can be asked for against the pushed sha (the agent never pushes)</name>
  <what-built>
    Every guard and runner fix and every documentation commit of plans 01-09 have landed locally, so
    HEAD is ahead of `origin/main` and the runner's pushed-sha readiness row reads not-ready — as it did
    in Phase 33 rounds 2 and 3. This checkpoint asks only for the push; no money is spent by answering.
    The go is a separate checkpoint after the zero-token readiness re-check (Task 3).
  </what-built>
  <how-to-verify>
    1. Run `git log --oneline origin/main..HEAD` to see the carried commits.
    2. Push (`git push origin main`), then confirm after `git fetch` that `git rev-parse HEAD` equals
       `git rev-parse origin/main`.
    3. Note: after this push NOTHING is committed until the capture has run (or the go is refused) — the
       dry-run report is written untracked and committed with the capture set.
  </how-to-verify>
  <resume-signal>Type "pushed" once HEAD equals origin/main, or describe what you want changed first</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Zero-token readiness against the pushed sha — the report is written, not committed</name>
  <precondition>Task 1 was answered `pushed`: after `git fetch`, `git rev-parse HEAD` equals `git rev-parse origin/main`, and the scoped working tree (`git status --porcelain -- agent-factory .claude .claude-plugin install skills hooks scripts AGENTS.md`) is empty; `.planning/` untracked entries are outside that scope and stated.</precondition>
  <read_first>
    - `scripts/capture-live.ts` as landed by plans 06-07 — the readiness table (the pushed-sha row, the working-tree row), the pre-spawn provenance row, the tool-grant rows, `DRY_RUN_REPORT_NAME`/`READINESS_PREFIX`, `verifyArtifacts`'s "exactly one report" rule, `CALL_BOUND_MS` (:219).
    - `.planning/phases/33-live-capture-windows-portability/33-41-PLAN.md` Task 2 — the filing shape (report untracked, committed after); `33-41-SUMMARY.md:86-88` for the round-4 spend (4.40 USD / 17 min 57 s).
    - `33.1-RESEARCH.md` § "The capture wave" (preconditions each a plan output the go cites; the spend floor re-derivation; A4/A5/A6 re-run on the go version) and § Zero-token permission-mode measurement.
  </read_first>
  <files>.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-DRYRUN-REPORT.md</files>
  <action>
    Run `node scripts/capture-live.js --dry-run --out SCRATCH_DIR` and copy its report into the phase
    directory as `33.1-DRYRUN-REPORT.md` WITHOUT committing it. Quote the `GO-READINESS:` line and the
    full precondition table in the summary; the report must show `GO-READINESS: ready`, the pushed-sha
    row MET naming the sha, the working-tree row MET, the tool-grant rows with the scoped `Edit(//…/**)`
    rule and the scoped admission tool, the pre-spawn provenance row, and — now — the
    `--permission-mode default` argv. Re-run the zero-token permission-mode + probe-reach checks (A4, A5,
    A6) on the Claude Code version present and record that version. Re-run `node scripts/check-flip-manifest.js`
    and quote the declared-set count (14, `.planning/PROJECT.md` first) and `ALL CHECKS PASSED`. If
    readiness is not `ready`: STOP, commit the report as the round's finding with the unmet rows quoted,
    record the outcome word `no-go`, and do not present Task 3.
  </action>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"</automated>
    <fails_when>non-zero exit — HEAD is not the pushed sha; the go must not be asked for</fails_when>
    <automated>grep -a -c 'GO-READINESS: ready' .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-DRYRUN-REPORT.md</automated>
    <fails_when>a count other than 1 — readiness is not ready against the pushed sha (on that branch the report is committed as the finding and Task 3 is not presented)</fails_when>
    <automated>node scripts/check-flip-manifest.js</automated>
    <fails_when>non-zero exit, or the output lacks ALL CHECKS PASSED</fails_when>
    <automated>git ls-files --error-unmatch .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-DRYRUN-REPORT.md 2>/dev/null; test $? -ne 0</automated>
    <fails_when>zero exit — the report was committed before the go (nothing lands between the push and the run)</fails_when>
  </verify>
  <acceptance_criteria>
    - `33.1-DRYRUN-REPORT.md` exists untracked with `GO-READINESS: ready`, the pushed-sha row MET naming the sha, the working-tree row MET, the tool-grant rows, the pre-spawn provenance row, and the `--permission-mode default` argv.
    - The A4/A5/A6 zero-token checks are re-run on the go's CC version and that version is recorded; the flip gate's declared-set count (14) and pass line are quoted.
    - HEAD equals `origin/main` and the scoped tree is clean; `.planning/` untracked entries are listed as outside the scope.
  </acceptance_criteria>
  <done>The machinery is proven ready against the pushed sha, at zero cost, with the new permission pin in the argv and nothing moved since the push.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: The live capture go — one occasion only (D-10), the last under the cap</name>
  <reversibility rating="one-way">A live run spends real money and produces a model session; a red result under D-12 forbids a second go inside 33.1 without a new human decision. The go is per-occasion and never inherited.</reversibility>
  <decision>Whether to authorize the one live capture now, and whether to set a `--max-budget-usd` ceiling on it.</decision>
  <context>
    Readiness against the pushed sha reads `GO-READINESS: ready` in `33.1-DRYRUN-REPORT.md` (untracked
    until the run is over). The guard passed the D-07 checkpoint (plan 05); the runner pins
    `--permission-mode default` on committed evidence (plan 06), exempts the coordinator spawn from
    membership only (plan 06), keys the deny on the probe's own tool use (plan 07), and scores parity
    from write-shaped routes with the extended set (plan 07). This authorizes ONE live capture, for this
    occasion only (D-09/D-10); a go recorded in any earlier round authorizes nothing here. This is the
    LAST go under the 4-round cap.

    THE COST, stated plainly because it is real money: round-4 cost 4.40 USD and 17 min 57 s (runner wall
    clock); round-1 cost 11.75 USD and 31 min 07 s. Worst-case wall time is two calls at
    `CALL_BOUND_MS = 1_200_000` ms (40 min) plus setup. Default permission mode may shorten or lengthen
    the run (fewer tools, more denials/retries). A `--max-budget-usd` ceiling is your decision: a mid-run
    budget cut reads red for a reason unrelated to the mechanism, and D-12 forbids a second go — if used,
    set it well above 11.75 USD. The Claude Code version at the go is {stated by the executor from the
    Task-2 record}.

    THE EXPECTATION: the D-07 parity word can read `fail` for reasons that are not instrument defects
    (per-role note counts and kind multisets are model-chosen; one sample per path cannot separate
    variance from mechanism), and D-20/D-08 forbid softening the predicate. Either way the run is the
    CAP-03 observation on an instrument the subject cannot write to, and it settles the two
    `UNKNOWN - verify` items (the B-path admission-tool list; the post-run plugin listing). CAP-02 is NOT
    met in this phase (D-15). A red run is diagnosed at zero tokens and not re-run — there is no round 5
    without a new decision (D-12).
  </context>
  <options>
    <option id="go-no-budget">
      <name>Go, no dollar ceiling</name>
      <pros>The run completes on its own; no budget cut can produce a spurious red</pros>
      <cons>No hard dollar cap; worst case ~two calls at the 40-minute bound</cons>
    </option>
    <option id="go-with-budget">
      <name>Go with `--max-budget-usd` set well above 11.75 USD</name>
      <pros>A hard dollar ceiling</pros>
      <cons>A mid-run cut reads red for a reason unrelated to the mechanism; D-12 then forbids a second go</cons>
    </option>
    <option id="no-go">
      <name>No-go</name>
      <pros>No spend; the round closes with the outcome word `no-go` and GAP-D1 held</pros>
      <cons>CAP-01/CAP-03 remain undischarged this phase</cons>
    </option>
  </options>
  <resume-signal>Select: go-no-budget, go-with-budget, or no-go</resume-signal>
</task>

<task type="auto">
  <name>Task 4: The capture — one run, artifacts written after, one outcome line; or the recorded no-go</name>
  <precondition>Task 3 was answered for this occasion (a go recorded in any earlier plan or round does not authorize this run, D-09); at the moment the runner starts, `git rev-parse HEAD` equals `git rev-parse origin/main` and the scoped working tree is empty; the only untracked file under the phase directory is `33.1-DRYRUN-REPORT.md`.</precondition>
  <read_first>
    - `scripts/capture-live.ts` as landed by plans 06-07 — `runTarget` (pre-spawn provenance, scoped grant, pipe scoring, `finally` uninstall), the argv pin, the coordinator exemption, the probe-keyed deny, the write-shaped parity section, `writeArtifacts`/`verifyArtifacts`, `CAPTURE_SUMMARY_NAME`.
    - `.planning/phases/33-live-capture-windows-portability/33-41-PLAN.md` Task 4 and `33-41-SUMMARY.md` — the capture task shape and how artifacts were committed after; `33-R4-DIAGNOSIS.md` — the zero-token diagnosis shape (run table, cause class per axis with transcript citations).
    - `33.1-RESEARCH.md` § "The capture wave" (if red, diagnose at zero tokens; if green, hand to the flip plan; settle the two UNKNOWN-verify items) and § D-33-R4-07; `33.1-PATTERNS.md` § Wave 3 and § Phase-dir reports (spell control bytes; run check:nul-bytes).
  </read_first>
  <files>.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-A.jsonl, .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-B.jsonl, .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-SUMMARY.md, .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-DIAGNOSIS.md, .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-DRYRUN-REPORT.md</files>
  <action>
    If Task 3 answered `no-go`: write no capture; commit `33.1-DRYRUN-REPORT.md` alone; record the word
    `no-go`, the reason, and that nothing flips (GAP-D1 held); stop.

    Otherwise run the capture ONCE: `node scripts/capture-live.js --out SCRATCH_DIR --keep-target` (with
    `--max-budget-usd` only if Task 3 chose `go-with-budget`). Let the runner do every derivation. Copy
    the artifact set into the phase directory under the manifest-keyed names (`33.1-CAPTURE-A.jsonl`,
    `33.1-CAPTURE-B.jsonl`, `33.1-CAPTURE-SUMMARY.md`) AFTER the run, run the runner's own re-check over
    the phase directory and `npm run check:nul-bytes`, run `claude plugin list` once and quote the
    grugops rows verbatim (the stale-rows item), and commit the set together with `33.1-DRYRUN-REPORT.md`
    in one commit stating the outcome word, both runs' exit statuses, the parity verdict, both provenance
    states, the permission mode read back, and the uninstall exits. Settle the two `UNKNOWN - verify`
    items: quote path B's `system/init` `tools[]` entry for the scoped admission tool (present or absent)
    with its line id, and the post-run `claude plugin list` grugops rows.

    If the outcome word is `pass`: stop; plan 11 performs the flip against this summary.
    If `fail`/`hang`: file `33.1-CAPTURE-DIAGNOSIS.md` at ZERO tokens (run table, cause class per axis
    SUITE/KIT/platform/host with transcript citations, the write-shaped route counts and per-role axes
    vs the round-1 values). Do not fix, do not rerun — no round 5 without a new decision (D-12). Nothing
    flips; plan 11 takes its hold branch.
    If the runner stopped before the spawn: the summary quotes the `CAPTURE NOT DERIVED` line and the
    preserved scratch paths; that is a finding, not a capture.
    Never run `npm test`.
  </action>
  <verify>
    <automated>grep -a -cE '^(OUTCOME|Outcome): (pass|fail|hang|no-go)$' .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-SUMMARY.md</automated>
    <fails_when>a count other than 1 — no single grammar-conformant outcome line (on the no-go branch this file is absent and the summary records the word instead; state which branch)</fails_when>
    <automated>node scripts/capture-live.js --verify-artifacts --out .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first</automated>
    <fails_when>non-zero exit, or a REFUSED line naming an uncited claim row, a surviving home-path spelling, a control byte in a cell, or a duplicated outcome line</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the output names a committed transcript as carrying a control byte</fails_when>
    <automated>git ls-files --error-unmatch .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-DRYRUN-REPORT.md</automated>
    <fails_when>non-zero exit — the dry-run report was not committed with the capture set (or alone on the no-go branch)</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one live run was made, or none with `no-go` recorded; the summary records both calls, durations, reported costs, the bound used, both provenance states, the permission mode read back, the grant rows, and the uninstall exits.
    - The `Dual-path parity (D-07)` section lists both projections over write-shaped routes only and reports parity or the named diffs; path B's admission-tool list entry and the post-run plugin listing are quoted.
    - On `fail`/`hang`: `33.1-CAPTURE-DIAGNOSIS.md` exists, spends zero tokens, names a cause class per axis with citations, and no fix or rerun happened.
    - The artifacts and the dry-run report were committed after the run in one commit; no commit sits between the push and the runner's start.
  </acceptance_criteria>
  <done>One run against the pushed sha, one artifact set, one outcome line on the redesigned instrument — or an honest no-go — with the two UNKNOWN-verify items settled and a zero-token diagnosis on red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an authorized human go → real money and a model session | Per-occasion, never inherited (D-09/D-10) |
| the pushed sha → the marketplace install → the capture | A commit between the push and the run makes the two paths different code while the row reads MET |
| platform stream output → a committed artifact | Untrusted model and CLI output crosses into git history |
| the human's push → the executor's next step | The executor never pushes; sha equality is verified, not trusted by word |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-29 | Repudiation | an inherited or re-used go | critical | mitigate | Task 3 is per-occasion; Task 4's precondition names it; no later plan may cite it |
| T-33.1-30 | Elevation of privilege | capture against unpushed or dirty code | high | mitigate | Task 1 pushes first; Task 2 asserts HEAD==origin/main and the scoped tree clean; Task 4 re-checks at start; report stays untracked until after the run |
| T-33.1-31 | Elevation of privilege | the prod-deploy approval environment variable | critical | mitigate | The runner never sets it; asserted on the constructed child environment before every spawn |
| T-33.1-32 | Information disclosure | the committed transcript | high | mitigate | Redaction fails closed; `--verify-artifacts` refuses survivors and control bytes; NUL-byte gate run |
| T-33.1-33 | Tampering | the D-07 predicate softened to reach `pass` | critical | mitigate | Plans 05/07 pin the divergences; this plan changes no source and never softens the predicate (D-08/D-20) |
| T-33.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; the plugin install is a platform op over an existing marketplace row, gated by content before the spawn and uninstalled in `finally` |
</threat_model>

<verification>
- `test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"` before the go and at the runner's start.
- `node scripts/capture-live.js --dry-run --out SCRATCH` — exit 0, `GO-READINESS: ready`, filed untracked as `33.1-DRYRUN-REPORT.md`, committed after the run.
- `grep -a -cE '^(OUTCOME|Outcome): (pass|fail|hang|no-go)$' 33.1-CAPTURE-SUMMARY.md` — exactly 1.
- `node scripts/capture-live.js --verify-artifacts --out PHASE_DIR` and `npm run check:nul-bytes` — exit 0.
- Never `npm test`; exactly one live run behind the one go.
</verification>

<success_criteria>
- The push precedes the go as its own checkpoint; readiness reads ready against the pushed sha; nothing is committed in between.
- Exactly one live run, artifacts written after, one outcome line — or a recorded no-go — with the two UNKNOWN-verify items settled and a zero-token diagnosis on red; CAP-02 is not met (D-15).
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `33.1-DRYRUN-REPORT.md` | new artifact | zero-token readiness against the pushed sha; written before the go, committed after the run |
| `33.1-CAPTURE-A.jsonl`, `33.1-CAPTURE-B.jsonl`, `33.1-CAPTURE-SUMMARY.md` | new artifacts (go branch) | the round's capture under the manifest-keyed names |
| `33.1-CAPTURE-DIAGNOSIS.md` | new artifact, conditional | zero-token diagnosis on fail/hang |
| the B-path admission-tool list quotation; the post-run plugin listing | summary sections | the two UNKNOWN-verify items settled |
| the `--max-budget-usd` decision | decision record | at the go |

<output>
Create `.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-10-SUMMARY.md` when done
</output>
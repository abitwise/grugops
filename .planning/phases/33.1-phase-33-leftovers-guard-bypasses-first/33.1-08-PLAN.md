---
phase: "33.1"
plan: 08
type: execute
wave: 8
depends_on: ["33.1-07"]
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
  - scripts/context-io.test.ts
autonomous: true
requirements: ["WR-01", "WR-02", "IN-01"]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WR-01 (row 303): `appendNote` and `admitAndAppend` gain the same fail-closed `ledgerRecordsId` short-circuit `promoteAdmitted` uses, keyed on the SAME root the append uses, so an identical-bytes re-run appends NO second GOV-02 ledger event — a re-run with the same `precomputedId` and bytes under `audit_retention: retained` leaves exactly one ledger line for the id (WR-01)."
    - "WR-02 (row 304, Claude's discretion): the check-then-act publish race is closed by an exclusive create (`O_EXCL` / `flag: \"wx\"`) that appends the ledger only after the create succeeds, mapping `EEXIST` to the append-only refusal when bytes differ and to the idempotent no-op when identical, rethrowing any other code — OR the three comment sites are reworded to name the race window and ledger it as the accepted residual; whichever path, NO comment claims \"true by construction\" for a check-then-act, and no platform conditional is introduced (WR-02; D-15)."
    - "IN-01 (row 307): `appendNote`'s pre-check refusal is wrapped in `appendNote`'s OWN framing so the message names `appendNote`, not `writeNoteFile`, matching how `admitAndAppend` already reads the shared sentence (IN-01)."
    - "Each change is RED-first in `scripts/context-io.test.ts`, staged inside the repository whose dial it sets (the IN-04 staging rule), and the ten windows reds in that file stay OUT of scope (D-15); `scripts/context-io.js` is in BOTH decider closures, so the manifest is regenerated and `hooks/hook-entry.{ts,js}` committed with the edit (D-05)."
  artifacts:
    - path: scripts/context-io.ts
      provides: "ledgerRecordsId short-circuit in appendNote + admitAndAppend (WR-01); exclusive-create publish or reworded race comments (WR-02); appendNote-framed pre-check refusal (IN-01)"
    - path: scripts/context-io.test.ts
      provides: "WR-01 count-one-ledger-line test; WR-02 two-writer race test; IN-01 message-names-appendNote test"
  key_links:
    - from: scripts/context-io.ts
      to: hooks/hook-entry.ts
      via: "context-io.js is in both decider closures; every edit regenerates the manifest"
      pattern: "generate:hook-manifest"
    - from: scripts/context-io.ts
      to: scripts/context-io.test.ts
      via: "WR-01 proven by calling the route twice with the same id and counting ledger lines (=1)"
      pattern: "ledgerRecordsId"
---

<objective>
Close the three shared-context writer defects the Phase 33 round-4 review left open: WR-01 (a second
GOV-02 ledger event on an identical re-run), WR-02 (a check-then-act publish race the comments wrongly
call "true by construction"), and IN-01 (a pre-check refusal that names the wrong function). None
touches the guard's grammar, but `scripts/context-io.js` sits in both decider closures, so every edit
regenerates the decider manifest. WR-02 is Claude's discretion: the recommended path is an exclusive
create; whichever is taken, no comment claims "true by construction" for a check-then-act and no
platform conditional is introduced (D-15).

Purpose: make the shared verified context's writes idempotent, race-safe, and honestly framed. Output:
the three fixes with RED-first tests, staged inside the dial's own repository.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CONTEXT.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-RESEARCH.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PATTERNS.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-01 — the ledger short-circuit in appendNote and admitAndAppend</name>
  <behavior>
    - Calling `appendNote` (or `admitAndAppend`) twice with the same `precomputedId` and bytes under `audit_retention: retained` leaves exactly one ledger line for the id.
    - The `ledgerRecordsId` look is fail-closed (throws on an unreadable ledger; never answers "not recorded"), keyed on the same root the append uses.
  </behavior>
  <read_first>
    - `scripts/context-io.ts` — `appendNote` (:1832) and its identical-bytes fall-through to `admit()` (:1926-1929), `admitAndAppend` retained branch (:6327-6368, root `actionOwner.root` at :6358), `promoteAdmitted`'s ledger look (:3533-3570), `ledgerRecordsId` (:4368), the two-roots split comments (:3540-3543, :6354-6357).
    - `scripts/context-io.test.ts` — Test 6a (:9514-9546, promote twice, count ledger lines) and the IN-04 staging rule (:5736-5770, stage inside `join(repoRoot, ".grugops", "context")`).
    - `33.1-RESEARCH.md` § WR-01, WR-02, IN-01 (anchors) and § WR-01 fix direction; `33.1-PATTERNS.md` § `scripts/context-io.ts` (WR-01 analog) and § `scripts/context-io.test.ts`.
  </read_first>
  <files>scripts/context-io.ts, scripts/context-io.js, hooks/hook-entry.ts, hooks/hook-entry.js, scripts/context-io.test.ts</files>
  <action>
    Copy `promoteAdmitted`'s fail-closed `ledgerRecordsId` short-circuit into `appendNote` (before the
    identical-bytes fall-through reaches `admit()`) and into `admitAndAppend`'s retained branch, keyed on
    the SAME root each append uses (`actionOwner.root` in `admitAndAppend`; the root `admit()` appends to
    in `appendNote`). Add the RED test: call either route twice with the same `precomputedId` and bytes
    under `audit_retention: retained`, staged inside the repository whose dial it sets (IN-04 rule), and
    assert the ledger holds exactly one line for the id. Regenerate the manifest (context-io.js is in
    both decider closures) and commit `scripts/context-io.{ts,js}` and `hooks/hook-entry.{ts,js}`
    together.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" summary — a second ledger line appears on an identical re-run</fails_when>
    <automated>npm run generate:hook-manifest && npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit, or freshness does not report the deciders fresh — context-io.js changed both closures</fails_when>
  </verify>
  <acceptance_criteria>
    - `appendNote` and `admitAndAppend` short-circuit on `ledgerRecordsId`, keyed on the append's own root; an identical-bytes re-run leaves one ledger line.
    - The RED test is staged inside the dial's repository (IN-04 rule); the manifest is regenerated and fresh.
  </acceptance_criteria>
  <done>An identical-bytes re-run of appendNote or admitAndAppend appends no second GOV-02 ledger event.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-02 (exclusive create or reworded residual) + IN-01 (appendNote-framed refusal)</name>
  <behavior>
    - WR-02: two writers racing on one id leave exactly one published note and one ledger line; the loser gets the append-only refusal (bytes differ) or the idempotent no-op (bytes identical); no comment claims "true by construction" for a check-then-act; no platform conditional.
    - IN-01: `appendNote`'s pre-check refusal message names `appendNote`, not `writeNoteFile`.
  </behavior>
  <read_first>
    - `scripts/context-io.ts` — the publish path `writeNoteFile` (:1414-1422) → `atomicWrite` (:978-1003), `differingOccupantRefusal` (:1436-1440), the pre-check `decideNoteDestination` (:1926) and `appendNote`'s wrappers (:1934-1944), `noteDestinationRefusal` (:1448-1462), the "true by construction" comment sites (:1533, :1903, :3319, :3469, :3537) and the three WR-02 comment sites (:1916-1929, :3464-3491, :6310-6326).
    - `scripts/claim.ts` (:121-128) — the `mkdirSync` atomic create-or-fail + EEXIST pattern to model an exclusive publish; `scripts/claim.test.ts` — the two-writer race test shape.
    - `33.1-RESEARCH.md` § WR-01, WR-02, IN-01 (WR-02 recommendation: exclusive create; IN-01: wrap the pre-check) and § CONTEXT "Claude's Discretion" (WR-02).
    - `33.1-PATTERNS.md` § `scripts/context-io.ts` (WR-02 analog `claim.ts`; re-read every "true by construction" site against the chosen fix) and § `scripts/context-io.test.ts` (model on claim.test.ts).
  </read_first>
  <files>scripts/context-io.ts, scripts/context-io.js, hooks/hook-entry.ts, hooks/hook-entry.js, scripts/context-io.test.ts</files>
  <action>
    WR-02 (recommended: exclusive create): publish with an exclusive create (link the temp onto the
    final path, or open with `flag: "wx"`), appending the ledger only after the create succeeds; map
    `EEXIST` to `differingOccupantRefusal` when the bytes differ and to the idempotent no-op when
    identical; rethrow any other code (as `claim.ts` does). No platform conditional (D-15). If instead
    the reword path is taken, reword the three comment sites (:1916-1929, :3464-3491, :6310-6326) to name
    the race window and ledger it as the accepted residual. Either way, re-read every "true by
    construction" site and ensure none claims it for a check-then-act. IN-01: wrap `appendNote`'s
    pre-check refusal in `appendNote`'s own framing so the message names `appendNote` (matching how
    `admitAndAppend` reads the shared sentence). Add the WR-02 two-writer race test (model on
    `claim.test.ts`: two writers on one id, one publishes, the other refuses or no-ops, ledger holds one
    line) and the IN-01 message test (asserts the message names `appendNote`). Regenerate the manifest
    and commit `scripts/context-io.{ts,js}` and `hooks/hook-entry.{ts,js}` together. The ten windows reds
    in `scripts/context-io.test.ts` stay out of scope (D-15).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" summary — the two-writer race leaves two notes/ledger lines, or the IN-01 message names writeNoteFile</fails_when>
    <automated>npm run generate:hook-manifest && npm run freshness:hook-manifest && npm run check:build-parity && npm run typecheck</automated>
    <fails_when>non-zero exit, or check:build-parity does not print ALL CHECKS PASSED</fails_when>
  </verify>
  <acceptance_criteria>
    - WR-02: two writers on one id leave one note and one ledger line; the loser refuses or no-ops; no "true by construction" claim on a check-then-act; no platform conditional.
    - IN-01: `appendNote`'s pre-check refusal names `appendNote`.
    - The manifest is regenerated; build-parity and typecheck clean; the ten windows reds stay untouched (D-15).
  </acceptance_criteria>
  <done>The publish race is closed (or honestly ledgered), the refusal names the right function, and no check-then-act claims to be true by construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an identical-bytes re-run → the GOV-02 ledger | Must not append a second event for the same id |
| two concurrent writers on one id → the published note | v2.0 runs parallel agents; a check-then-act window is reachable |
| a pre-check refusal message → the caller | Must name the function that ran, not a function that did not |
| `scripts/context-io.js` hash → both decider closures | A stale manifest denies everything at both hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-22 | Repudiation | a duplicated GOV-02 ledger event | high | mitigate | `ledgerRecordsId` short-circuit in appendNote + admitAndAppend, keyed on the append root; count-one test |
| T-33.1-23 | Tampering | the check-then-act publish race | high | mitigate | Exclusive create (`O_EXCL`/`wx`) with ledger-after-create; two-writer race test; no "true by construction" claim |
| T-33.1-24 | Repudiation | a refusal message naming the wrong function | low | mitigate | Wrap the pre-check in `appendNote`'s own framing (IN-01) |
| T-33.1-25 | Tampering | a stale decider manifest after a context-io edit | high | mitigate | `generate:hook-manifest` + `freshness:hook-manifest` (context-io.js in both closures) |
| T-33.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed in this phase; nothing to audit |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts` — green (macOS baseline; the ten windows reds are out of scope, D-15).
- `npm run generate:hook-manifest && npm run freshness:hook-manifest && npm run check:build-parity && npm run typecheck` — fresh, ALL CHECKS PASSED, clean.
- Never `npm test`.
</verification>

<success_criteria>
- WR-01, WR-02 and IN-01 are closed RED-first; an identical-bytes re-run appends no second ledger event; the publish race is closed or honestly ledgered; the refusal names `appendNote`.
- The manifest is regenerated; no check-then-act claims to be true by construction; no platform conditional.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `ledgerRecordsId` short-circuit in appendNote + admitAndAppend | fix | `scripts/context-io.ts` (WR-01) |
| exclusive-create publish (or reworded race comments) | fix | `scripts/context-io.ts` (WR-02) |
| appendNote-framed pre-check refusal | fix | `scripts/context-io.ts` (IN-01) |
| WR-01 count-one-ledger test; WR-02 two-writer race test; IN-01 message test | tests | `scripts/context-io.test.ts` |
| regenerated decider manifest | build output | context-io.js in both closures |

<output>
Create `.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-08-SUMMARY.md` when done
</output>
---
phase: "33.1"
plan: 06
type: execute
wave: 6
depends_on: ["33.1-05"]
files_modified:
  - scripts/capture-live.ts
  - scripts/capture-live.js
  - scripts/capture-live.test.ts
  - .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PERMISSION-MODE-EVIDENCE.md
autonomous: true
requirements: ["D-33-R4-05", "D-33-R4-04"]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The capture spawn argv carries `--permission-mode default` (placed BEFORE `--allowedTools` so Test C7's grant reader still sees the tool list), and the pin lands only on the committed zero-token measurement `33.1-PERMISSION-MODE-EVIDENCE.md` showing that `default` denies an out-of-target `Write` on the main thread AND in a subagent under the operator's real config — a re-runnable check or a dated note with the exact invocation and verbatim output (D-11(a); D-33-R4-05, WINDOWS row 298)."
    - "The runner reads the effective `permissionMode` back from each run's init frame as an outcome input, and denial claims come from the per-tool-use `tool_result` frames (not `permission_denials`, which omits subagent denials)."
    - "The coordinator spawn is exempt from the side-(a) membership clause ONLY (keyed on a derived `coordinatorKey`, never a role-name pattern; `grant.coordinator === null` gives no exemption), and the coordinator NEVER counts toward the two-role D-02(a) bar — the exemption `continue`s before `evidenced.add(key)`; the code comment cites D-33-R4-04 as a RECORDED RELAXATION, not a fix (D-33-R4-04, WINDOWS row 297)."
    - "Each change is RED-first in `scripts/capture-live.test.ts`: the argv assertion is RED before the pin; the coordinator fix has three mutation fixtures that stay red — a non-coordinator non-member spawn, a coordinator plus only ONE evidenced grant member, and a `grant.coordinator === null` fixture (D-05)."
  artifacts:
    - path: scripts/capture-live.ts
      provides: "--permission-mode default at the spawn args; per-run init-frame permissionMode readback; coordinator membership exemption in capThreePredicate"
    - path: scripts/capture-live.test.ts
      provides: "argv seam assertion (RED-first); the three D-33-R4-04 mutation fixtures"
    - path: .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PERMISSION-MODE-EVIDENCE.md
      provides: "committed zero-token permission-mode evidence with the exact invocation and verbatim verdicts"
  key_links:
    - from: scripts/capture-live.ts
      to: .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PERMISSION-MODE-EVIDENCE.md
      via: "the go checkpoint (plan 10) cites this committed evidence for D-11(a)"
      pattern: "permission-mode"
    - from: scripts/capture-live.ts
      to: scripts/capture-live.test.ts
      via: "capThreePredicate coordinator exemption proven by three mutation fixtures"
      pattern: "coordinatorKey"
---

<objective>
Land the runner fixes that D-11(b) makes preconditions for the one live capture: pin
`--permission-mode default` on the spawn (so writes are bounded at capture time, not left to the
inherited `auto` mode) with committed zero-token evidence, and relax the coordinator-spawn membership
clause for the coordinator only (D-33-R4-04) so a coordinator that runs as a subagent and spawns role
agents is not scored as a non-member. None of this touches the guard's grammar. The permission-mode
measurement was taken this research session against a local mock Messages API (zero tokens); the pin
lands only on committed evidence.

Purpose: bound writes at capture time and stop the runner mis-scoring the coordinator spawn. Output:
the argv pin + evidence, the readback, and the coordinator exemption with its three mutation fixtures.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CONTEXT.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-RESEARCH.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PATTERNS.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin --permission-mode default + commit the zero-token evidence + read the mode back</name>
  <behavior>
    - The spawn argv contains `--permission-mode` immediately followed by `default`, placed before `--allowedTools`.
    - `33.1-PERMISSION-MODE-EVIDENCE.md` exists with the exact invocation and verbatim verdicts (default denies an out-of-target Write on main thread and in a subagent; an in-target Edit-scoped Write is allowed).
    - Each run's init-frame `permissionMode` is read back as an outcome input; denial claims read from per-tool-use `tool_result` frames.
  </behavior>
  <read_first>
    - `scripts/capture-live.ts` — the spawn args (:1868, `--allowedTools ...run.allowedTools`), `liveAllowedTools` (:342-345, no Write, two Bash prefixes), the `Bash(helm upgrade *)` reach comment (:291), the run-A provenance readback (:2726-2730).
    - `scripts/capture-live.test.ts` — Test C7 argv seam (:1382-1427; reads everything after `--allowedTools` up to `--agent` as the grant list) and the `recordingOps` pattern.
    - `33.1-RESEARCH.md` § "Zero-token permission-mode measurement" (place the pin before `--allowedTools`; commit the evidence; read the mode back; do not count from `permission_denials`) and § Assumptions A3, A4, A5.
    - `33.1-PATTERNS.md` § `scripts/capture-live.ts` (argv pin) and § Phase-dir reports (evidence report shape from `33-CI-MEASUREMENT.md`).
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts, .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PERMISSION-MODE-EVIDENCE.md</files>
  <action>
    Add `--permission-mode default` to the spawn args, placed before `--allowedTools` so Test C7's grant
    reader still sees the tool list. Add a per-run init-frame `permissionMode` readback as an outcome
    input (a pass recorded under `auto` proves nothing about the pin). Commit
    `33.1-PERMISSION-MODE-EVIDENCE.md` with the exact invocation, the Claude Code version, the mock-API
    setup, and the verbatim verdicts for main thread and subagent (copy `33-CI-MEASUREMENT.md`'s header
    discipline; "Prohibition honoured" — no live run). Add the RED argv-seam test asserting
    `--permission-mode` is immediately followed by `default`. Rebuild (`npm run build`); note that
    `scripts/capture-live.ts` is NOT in the decider manifest, so no `generate:hook-manifest` is needed —
    but run `npm run check:build-parity` so the committed `.js` matches.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" summary — the argv does not carry `--permission-mode default` before `--allowedTools`</fails_when>
    <automated>test -s .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PERMISSION-MODE-EVIDENCE.md</automated>
    <fails_when>non-zero exit — the committed permission-mode evidence is missing or empty</fails_when>
    <automated>npm run build && npm run check:build-parity && npm run typecheck</automated>
    <fails_when>non-zero exit, or check:build-parity does not print ALL CHECKS PASSED — the committed capture-live.js drifted</fails_when>
  </verify>
  <acceptance_criteria>
    - The spawn argv carries `--permission-mode default` before `--allowedTools`; the argv-seam test is RED-first and passes after the pin.
    - `33.1-PERMISSION-MODE-EVIDENCE.md` is committed with the exact invocation, CC version, and verbatim main-thread + subagent verdicts.
    - The runner reads `permissionMode` back from each init frame; build-parity and typecheck clean.
  </acceptance_criteria>
  <done>Writes are bounded at capture time by a pinned permission mode, on committed zero-token evidence, and the mode is read back.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-33-R4-04 — exempt the coordinator spawn from membership only, never from the two-role bar</name>
  <behavior>
    - A spawn whose key equals `coordinatorKey` skips the side-(a) membership clause and `continue`s before `evidenced.add(key)`, so the coordinator never counts toward the two-role bar.
    - `grant.coordinator === null` gives no exemption; the skip is keyed on the derived coordinator, never a role-name pattern.
    - The code comment cites D-33-R4-04 (2026-09-25) as a recorded relaxation, not a fix.
  </behavior>
  <read_first>
    - `scripts/capture-live.ts` — `capThreePredicate` (:976), `coordinatorKey` derivation, the membership clause (:996-999), side (b)'s existing exclusion `grantedKeys.has(key) && key !== coordinatorKey` (:1023), `evidenced.add(key)`.
    - `scripts/capture-live.test.ts` — the predicate mutation pattern (:218-245, control `toEqual([])` then `without(FIXTURE.frames, predicate)`), the `capThreePredicate` driver (:222-243).
    - `33.1-RESEARCH.md` § D-33-R4-04 (the three things the plan must hold; the platform fact that the coordinator's `Agent(...)` list is not enforced on the subagent route, so the nested-spawn membership clause is the only check) and § Reproductions on A:80/A:313/A:711.
    - `33.1-PATTERNS.md` § `scripts/capture-live.ts` (coordinator exemption: copy side (b)'s exclusion; continue before evidenced.add; cite the decision) and § `scripts/capture-live.test.ts`.
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <action>
    In `capThreePredicate`, skip the side-(a) membership clause for a spawn whose key equals
    `coordinatorKey` and for no other spawn; `continue` before `evidenced.add(key)` so the coordinator is
    never one of the two distinct granted roles (copy side (b)'s exclusion). Key the exemption on the
    derived `coordinatorKey` only — `grant.coordinator === null` gives no exemption. Add the code comment
    citing D-33-R4-04 (2026-09-25) as a recorded relaxation, not a fix (Phase 33's D-20 forbade softening
    this predicate inside Phase 33). Add the RED fixture (coordinator spawned as a subagent then spawning
    two evidenced grant members returns `[]` after the fix) and the three mutation guards that must stay
    red: a non-coordinator non-member spawn; a coordinator plus only ONE evidenced grant member; a
    `grant.coordinator === null` fixture. Rebuild and run check:build-parity (not a decider — no manifest
    regen).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" summary — the coordinator fixture does not return [] or a mutation guard is not red</fails_when>
    <automated>npm run build && npm run check:build-parity && npm run typecheck</automated>
    <fails_when>non-zero exit, or check:build-parity does not print ALL CHECKS PASSED</fails_when>
  </verify>
  <acceptance_criteria>
    - The coordinator spawn is exempt from the membership clause only, keyed on `coordinatorKey`; a null coordinator gets no exemption; the coordinator never counts toward the two-role bar.
    - The comment cites D-33-R4-04 as a recorded relaxation; the RED fixture and three mutation guards behave as specified.
    - build-parity and typecheck clean.
  </acceptance_criteria>
  <done>A coordinator that runs as a subagent and spawns role agents is no longer scored a non-member, and it still cannot pad the two-role bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the spawned session's permission mode → what the role agent may write | Inherited `auto` did not bound writes; `default` in `-p` mode denies out-of-target tool calls |
| the init-frame `permissionMode` → the outcome word | A pass recorded under `auto` proves nothing about the pin |
| the coordinator spawn → the two-role D-02(a) bar | A careless exemption could let the coordinator pad the bar |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-16 | Elevation of privilege | capability unbounded at capture time (inherited auto) | high | mitigate | Pin `--permission-mode default`, measured at zero tokens; read the mode back |
| T-33.1-17 | Repudiation | the coordinator counted as a granted role | high | mitigate | The exemption `continue`s before `evidenced.add`; three mutation fixtures; comment cites the recorded relaxation |
| T-33.1-18 | Repudiation | denial claims counted from `permission_denials` | medium | mitigate | Read denials from per-tool-use `tool_result` frames, subagent frames included |
| T-33.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed in this phase; nothing to audit |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts` — green.
- `npm run build && npm run check:build-parity && npm run typecheck` — ALL CHECKS PASSED, clean.
- `33.1-PERMISSION-MODE-EVIDENCE.md` committed and non-empty.
- Never `npm test`; never run the live lane in this plan.
</verification>

<success_criteria>
- `--permission-mode default` is pinned on committed zero-token evidence and the mode is read back.
- The coordinator spawn is exempt from membership only and cannot pad the two-role bar; three mutation fixtures hold.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `--permission-mode default` at the spawn args | argv pin | `scripts/capture-live.ts:1868`, before `--allowedTools` |
| per-run init-frame `permissionMode` readback | outcome input | `scripts/capture-live.ts` |
| `33.1-PERMISSION-MODE-EVIDENCE.md` | new report | zero-token evidence, exact invocation + verbatim verdicts |
| coordinator membership exemption in `capThreePredicate` | runner fix | keyed on `coordinatorKey`; cites D-33-R4-04 |
| three D-33-R4-04 mutation fixtures | tests | `scripts/capture-live.test.ts` |

<output>
Create `.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-06-SUMMARY.md` when done
</output>
---
phase: "33.1"
plan: 11
type: execute
wave: 11
depends_on: ["33.1-10"]
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
  - .planning/phases/33-live-capture-windows-portability/deferred-items.md
  - .planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md
autonomous: false
requirements: ["CAP-01", "GAP-D1"]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The GAP-D1 flip is performed ONLY on the capture outcome word `pass` AND a human decision that the flip may proceed without CAP-02 (which cannot be met in 33.1, D-15) — the flip checkpoint states CAP-02 is not met and the human decides (D-13); on `fail`/`hang`/`no-go` the § hold record is written and nothing flips (D-12/D-20)."
    - "If flipped, the flip is one-way over EXACTLY the 14 files declared in `33-FLIP-MANIFEST.md` §2.3/§3 PLUS the owed `.planning/PROJECT.md` edit, in ONE commit, and `npm run check:flip-manifest` passes; the flip checkpoint states CAP-02 is not met so the human decides whether it may proceed without it (D-13)."
    - "The ledger is closed: remaining post-guard WINDOWS rows (297, 298, 299, 303, 304, 305, 307) are marked fixed or annotated with their disposition; the capture-wave deferred-items entries (GAP-D1, stale plugin rows) are disposed; REQUIREMENTS.md CAP-01/CAP-03 and STATE.md agree with the outcome; and the ROADMAP 33.1 status line is verified NOT prematurely flipped to Complete on a gaps-found or hold outcome (Pitfall 7; D-16 — the goal line already reflects the phase)."
    - "Row 274 and the ten `scripts/context-io.test.ts` windows reds stay OUT and are confirmed carried with a row and an owner; CAP-02 stays unmet and is recorded as such; no platform conditional is introduced (D-15)."
  artifacts:
    - path: .planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md
      provides: "the flip performed over the 14 declared files (on pass + human go), or a § hold record (otherwise)"
    - path: .planning/WINDOWS.md
      provides: "post-guard rows 297/298/299/303/304/305/307 marked fixed or dispositioned; row 274 confirmed carried"
    - path: .planning/PROJECT.md
      provides: "the owed capture-day edit in the flip commit (D-13), if flipped"
  key_links:
    - from: .planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CAPTURE-SUMMARY.md
      to: .planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md
      via: "the outcome word `pass` + human go drives the flip; otherwise the hold record"
      pattern: "check:flip-manifest"
    - from: .planning/ROADMAP.md
      to: .planning/STATE.md
      via: "the 33.1 status line is not flipped to Complete on gaps-found/hold; REQUIREMENTS + STATE agree with the outcome"
      pattern: "Phase 33.1"
---

<objective>
Close 33.1: perform the one-way GAP-D1 flip if and only if the capture outcome is `pass` and the human
decides it may proceed without CAP-02 (which cannot be met here, D-15), or write the § hold record; then
close the ledger — mark the post-guard WINDOWS rows fixed or dispositioned, dispose the capture-wave
deferred items, reconcile REQUIREMENTS.md and STATE.md with the outcome, and verify the ROADMAP 33.1
status was not prematurely flipped to Complete (the executor's final-plan roadmap update flips a phase
early; revert it on a gaps-found or hold outcome).

Purpose: land or hold the flip on the record and close the phase's ledger honestly. Output: the flip
(or hold), the post-guard ledger dispositions, and reconciled tracking files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CONTEXT.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-RESEARCH.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PATTERNS.md
@.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-10-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decision — flip GAP-D1 (on pass, without CAP-02) or hold</name>
  <reversibility rating="one-way">The GAP-D1 flip is one-way over the declared set (D-13); it discharges GAP-D1 and flips A3/DOG-02 plus the coupled cleanup. Undoing it means reverting the declared-set commit and re-opening GAP-D1.</reversibility>
  <decision>Whether to perform the one-way GAP-D1 flip now, given the capture outcome and that CAP-02 cannot be met in 33.1.</decision>
  <context>
    Read `33.1-CAPTURE-SUMMARY.md`'s outcome word. The flip proceeds ONLY on `pass`. Because CAP-02 (the
    windows CI leg) cannot be met in 33.1 (D-15), the flip — if it proceeds — happens without CAP-02, and
    D-13 requires this checkpoint to state that plainly and let you decide. CAP-01 and CAP-03 are
    discharged by the captured run; CAP-02 stays unmet and recorded. On `fail`/`hang`/`no-go`, nothing
    flips (D-12/D-20) and the § hold record is written.
  </context>
  <options>
    <option id="flip">
      <name>Flip GAP-D1 (outcome pass; proceed without CAP-02)</name>
      <pros>Discharges GAP-D1, CAP-01, CAP-03 on a captured run; the owed PROJECT.md edit lands in the same commit</pros>
      <cons>CAP-02 remains unmet and recorded; the flip is one-way</cons>
    </option>
    <option id="hold">
      <name>Hold (outcome not pass, or you choose to wait for CAP-02)</name>
      <pros>Nothing irreversible happens; the § hold record documents the state</pros>
      <cons>GAP-D1, CAP-01, CAP-03 stay open</cons>
    </option>
  </options>
  <resume-signal>Select: flip or hold</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Perform the flip over the declared set + PROJECT.md (on flip), or write the § hold record</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md` §2.3/§3 (the 14 declared files, "14 files, this manifest included") and its § hold-record precedent (§ 8, § 9); `npm run check:flip-manifest`.
    - `33.1-RESEARCH.md` § "The capture wave" (if green, flip over exactly the 14 files + PROJECT.md in one commit; the flip checkpoint states CAP-02 not met) and § D-13.
    - `33.1-PATTERNS.md` § Wave 3 (the flip set + gate).
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md, .planning/PROJECT.md</files>
  <action>
    If Task 1 chose `flip`: perform the one-way GAP-D1 flip over EXACTLY the 14 files declared in
    `33-FLIP-MANIFEST.md` §2.3/§3 plus the owed `.planning/PROJECT.md` edit, in ONE commit; run
    `npm run check:flip-manifest` and quote the declared-set count (14) and `ALL CHECKS PASSED`; record
    in the manifest that CAP-02 is not met (D-13). If Task 1 chose `hold`: write the § hold record in
    `33-FLIP-MANIFEST.md` (mechanical under D-20 on the outcome word), flip nothing, and record GAP-D1
    held. Do not touch row 274 or the windows reds (D-15).
  </action>
  <verify>
    <automated>node scripts/check-flip-manifest.js</automated>
    <fails_when>non-zero exit, or the output lacks ALL CHECKS PASSED — the flip set drifted from the declared 14, or the hold record is malformed</fails_when>
  </verify>
  <acceptance_criteria>
    - On flip: exactly the 14 declared files plus `.planning/PROJECT.md` changed in one commit; `check:flip-manifest` passes; the manifest records CAP-02 not met.
    - On hold: the § hold record is written, nothing flips, GAP-D1 recorded held.
    - Row 274 and the windows reds are untouched (D-15).
  </acceptance_criteria>
  <done>GAP-D1 is flipped over exactly the declared set (with the owed PROJECT.md edit) on a pass, or held on the record — never on a red capture.</done>
</task>

<task type="auto">
  <name>Task 3: Close the ledger and reconcile tracking files without a premature Complete</name>
  <read_first>
    - `.planning/WINDOWS.md` — post-guard rows 297, 298, 299, 303, 304, 305, 307 and the status-via-`gsd-tools windows` rule; row 274 (confirmed carried, D-15).
    - `.planning/phases/33-live-capture-windows-portability/deferred-items.md` — the capture-wave entries (GAP-D1, stale plugin rows) and the resolved-entry convention.
    - `.planning/ROADMAP.md` — the 33.1 status line and goal (the goal already reflects the phase, D-16); `.planning/REQUIREMENTS.md:140-142` (CAP-01/02/03); `33.1-RESEARCH.md` § Pitfall 7 (the executor's roadmap update flips a phase to Complete before verification — revert on gaps-found/hold).
    - `33.1-PATTERNS.md` § `.planning/WINDOWS.md` and § deferred-items (annotate/append via the tool; never rewrite).
  </read_first>
  <files>.planning/WINDOWS.md, .planning/phases/33-live-capture-windows-portability/deferred-items.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    Mark the post-guard WINDOWS rows fixed via `gsd-tools windows fixed` where their fix landed (303, 304,
    305, 307 from plan 08/07; 297, 298, 299 from plans 06/07), or annotate the disposition where a row is
    ledgered rather than closed. Dispose the capture-wave deferred-items entries: GAP-D1 (flipped or held,
    with the commit), the stale plugin rows (per the human's plan-09 choice). Reconcile
    `.planning/REQUIREMENTS.md` CAP-01/CAP-03 with the captured outcome and record CAP-02 unmet (D-15).
    Confirm row 274 and the ten windows reds are carried with a row and an owner. VERIFY the ROADMAP 33.1
    status line is NOT flipped to Complete on a gaps-found or hold outcome (Pitfall 7) — revert it if the
    executor's final-plan update flipped it early; the goal line already reflects the phase (D-16). Never
    rewrite a historical row; annotate.
  </action>
  <verify>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status --raw 2>/dev/null | grep -aq . && echo windows-status-ok</automated>
    <fails_when>no `windows-status-ok` line — the ledger status could not be read after the dispositions</fails_when>
    <automated>grep -a -c 'CAP-02' .planning/REQUIREMENTS.md</automated>
    <fails_when>a count of 0 — CAP-02's unmet status is not recorded in REQUIREMENTS.md</fails_when>
  </verify>
  <acceptance_criteria>
    - The post-guard WINDOWS rows (297/298/299/303/304/305/307) are marked fixed or annotated with their disposition; row 274 and the windows reds are confirmed carried with a row and an owner.
    - The capture-wave deferred-items entries are disposed; REQUIREMENTS.md and STATE.md agree with the outcome; CAP-02 is recorded unmet (D-15).
    - The ROADMAP 33.1 status line is not prematurely Complete on a gaps-found/hold outcome; annotations only, no rewrites.
  </acceptance_criteria>
  <done>The ledger is closed on the record, tracking files agree with the captured outcome, and no phase is marked Complete prematurely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the capture outcome word → the one-way flip | The flip proceeds only on `pass` and a human go; never on a red capture (D-20) |
| the declared flip set → git history | The flip must touch exactly the 14 files + PROJECT.md, gated by check:flip-manifest |
| the executor's roadmap update → the 33.1 status line | It flips a phase Complete early; must be reverted on gaps-found/hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-34 | Tampering | flipping GAP-D1 on a red or unverified capture | critical | mitigate | Task 1 decision gates on `pass`; the flip runs only on go; check:flip-manifest bounds the set |
| T-33.1-35 | Repudiation | a premature Complete on the ROADMAP | high | mitigate | Task 3 verifies and reverts the 33.1 status line on gaps-found/hold (Pitfall 7) |
| T-33.1-36 | Tampering | a flip that drifts from the declared 14 files | high | mitigate | `check:flip-manifest` asserts the declared set + the owed PROJECT.md edit in one commit |
| T-33.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed in this phase; nothing to audit |
</threat_model>

<verification>
- `node scripts/check-flip-manifest.js` — ALL CHECKS PASSED (flip: 14 files + PROJECT.md; hold: § record).
- `gsd-tools windows status` reads cleanly after the post-guard dispositions; row 274 and the windows reds carried.
- `grep -a -c 'CAP-02' .planning/REQUIREMENTS.md` ≥ 1 (CAP-02 recorded unmet).
- The ROADMAP 33.1 status line is not Complete on a gaps-found/hold outcome.
- Never `npm test`.
</verification>

<success_criteria>
- GAP-D1 is flipped over exactly the declared set (with the owed PROJECT.md edit) on a pass + human go, or held on the record; CAP-02 recorded unmet (D-15).
- The post-guard ledger is closed, tracking files agree with the outcome, and no phase is marked Complete prematurely.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| the GAP-D1 flip (14 files + PROJECT.md) or § hold record | flip/hold | `33-FLIP-MANIFEST.md`; one-way on pass + go (D-13) |
| post-guard WINDOWS rows 297/298/299/303/304/305/307 fixed/dispositioned | ledger | via `gsd-tools windows` |
| capture-wave deferred-items dispositions | ledger | GAP-D1, stale plugin rows |
| reconciled REQUIREMENTS.md / STATE.md; CAP-02 recorded unmet | tracking | D-15 |
| ROADMAP 33.1 status not prematurely Complete | guard | Pitfall 7 |

<output>
Create `.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-11-SUMMARY.md` when done
</output>
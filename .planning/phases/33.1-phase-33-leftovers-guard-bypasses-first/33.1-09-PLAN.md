---
phase: "33.1"
plan: 09
type: execute
wave: 9
depends_on: ["33.1-08"]
files_modified:
  - CLAUDE.md
  - .planning/research/STACK.md
  - agent-factory/packaging/adapters.md
  - scripts/admission-server.ts
  - scripts/admission-server.js
  - docs/audit/31-round4-residuals.md
  - .planning/phases/33-live-capture-windows-portability/deferred-items.md
autonomous: false
requirements: ["D-14", "WINDOWS-300"]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-14 (row 309): the CLAUDE.md nesting statement is corrected from a PRIMARY DOCUMENTATION SOURCE (code.claude.com/docs/en/sub-agents: a subagent can spawn subagents up to three layers below the main conversation, controlled by `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`, with the v2.1.172-216 / v2.1.217-218 / v2.1.219+ version window) — the corrected text states the version range, not that nesting always works; the three CLAUDE.md sites (:89, :135, :157) and the contradicting kit site `agent-factory/packaging/adapters.md:17` are corrected, and the main-thread-`Agent(...)`-enforcement premise sites are re-checked and LISTED (D-14)."
    - "The CLAUDE.md stack block is GENERATED from `.planning/research/STACK.md` (marker at CLAUDE.md:25); the regenerate path (`gsd-tools generate-claude-md`) is verified FIRST, and if it faithfully regenerates the block from STACK.md (which already carries the correct depth table) it is used — otherwise STACK.md and CLAUDE.md are edited in one commit with the coupling noted, because a hand edit of the generated block is overwritten on the next regen (D-14; A8)."
    - "The `scripts/admission-server.ts` docblock bare-name sites (lines 4, 12, 56) are reworded to name both the bare and the plugin-scoped tool spellings (or point at the derivation); `docs/audit/31-round4-residuals.md` row B8 (line 599, WINDOWS row 300) is ANNOTATED beside the historical row (never rewritten) recording that plan 33-38's WR-01 fix closed IN-04, with the commit and the pinned test (D-09)."
    - "The stale `grugops@grugops` 0.1.0 plugin rows are HUMAN-STATE: a blocking checkpoint asks the human before any `claude plugin` command; the agent NEVER touches user plugin state. The remaining non-windows post-guard deferred-items entries are disposed (resolved with evidence, or given a WINDOWS row with an owner); the ten `scripts/context-io.test.ts` windows reds stay OUT (D-15) (D-09; D-15)."
  artifacts:
    - path: CLAUDE.md
      provides: "corrected nesting statement (from the primary source, with the version range) via the regenerated stack block or a coupled hand edit"
    - path: scripts/admission-server.ts
      provides: "reworded docblock naming both the bare and plugin-scoped admission tool spellings"
    - path: docs/audit/31-round4-residuals.md
      provides: "annotation beside row B8 recording the 33-38 WR-01 closure (never a rewrite)"
  key_links:
    - from: CLAUDE.md
      to: .planning/research/STACK.md
      via: "the stack block is generated from STACK.md; correct at the source and regenerate, or edit both in one commit"
      pattern: "GSD:stack-start source:research/STACK.md"
    - from: .planning/phases/33-live-capture-windows-portability/deferred-items.md
      to: .planning/WINDOWS.md
      via: "each disposed post-guard entry is resolved with evidence or given a WINDOWS row with an owner"
      pattern: "status: resolved"
---

<objective>
Pay down the documentation and ledger leftovers after the guard checkpoint: correct the CLAUDE.md
"subagents cannot nest" statement from a primary source (nesting was observed on 2.1.281 and the docs
confirm three-layer default nesting with a version window), reword the admission-server bare-name
docblocks, annotate the Phase-31 B8 residual, gate the stale-plugin cleanup behind the human (the
agent never touches user plugin state), and dispose the remaining non-windows post-guard deferred
items. The CLAUDE.md stack block is generated, so the correction goes through the source and the
regenerate path — a hand edit of the generated block is overwritten.

Purpose: bring the kit's docs in line with observed platform behavior and close the post-guard ledger
items. Output: the corrected nesting texts, the reworded docblocks, the B8 annotation, and the disposed
deferred items.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CONTEXT.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-RESEARCH.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PATTERNS.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-14 — correct the nesting statement from the primary source, through the generated stack block</name>
  <read_first>
    - `CLAUDE.md` — the generated stack block marker (:25, `<!-- GSD:stack-start source:research/STACK.md -->`), the three nesting sites (:89 "subagents cannot spawn subagents (no nesting)", :135, :157), and the main-thread-`Agent(...)` premise sites referenced in RESEARCH (`agent-factory/packaging/adapters.md:35`, :65-79; `install/README.md:453`, :479).
    - `.planning/research/STACK.md` (:247-256, the correct nesting-depth table already present) and `agent-factory/packaging/adapters.md:17` (the contradicting kit statement) and `:240-244` of `subagent.frontmatter.md` (already correct — re-checked, no change).
    - `33.1-RESEARCH.md` § D-14 (the primary source, the version-range table, the site list, the main-thread premise re-check) and § CONTEXT claims 12, 13; `33.1-PATTERNS.md` § CLAUDE.md and adapters.md:17 (commit `6dd8aefe` hand-edit precedent inside the generated block; the regenerate command is `UNKNOWN - verify`).
  </read_first>
  <files>CLAUDE.md, .planning/research/STACK.md, agent-factory/packaging/adapters.md</files>
  <action>
    First VERIFY the regenerate path: check whether `gsd-tools generate-claude-md` regenerates the stack
    block faithfully from `.planning/research/STACK.md` (which already carries the correct depth table).
    If it does, ensure STACK.md's nesting text states the version range (v2.1.172-216 five layers;
    v2.1.217-218 one/none; v2.1.219+ three) from the primary source and regenerate. If the regenerate
    path is `UNKNOWN - verify` or does not faithfully update the block, edit `.planning/research/STACK.md`
    and CLAUDE.md (:89, :135, :157) in ONE commit and note the coupling in the summary. Correct
    `agent-factory/packaging/adapters.md:17`. Re-check and LIST (in the summary) the main-thread
    `Agent(...)`-enforcement premise sites (`adapters.md:35`, :65-79; `install/README.md:453`, :479) —
    the correct premise is "the Orchestrator must be the main thread for its `Agent(...)` list to be
    enforced", not "to spawn"; note which are already correct. State the version range; do not claim
    nesting always works. `subagent.frontmatter.md:240-244` needs no change. Run the public-doc gate.
  </action>
  <verify>
    <automated>grep -aic 'cannot spawn subagents\|no nesting\|cannot nest' CLAUDE.md</automated>
    <fails_when>a count greater than 0 — a "no nesting" premise still stands in CLAUDE.md</fails_when>
    <automated>grep -aic 'sub-agents cannot nest' agent-factory/packaging/adapters.md</automated>
    <fails_when>a count greater than 0 — the contradicting kit statement at adapters.md:17 still stands</fails_when>
    <automated>npm run check:public-docs</automated>
    <fails_when>non-zero exit — a public-doc vocabulary check regressed on the edited docs</fails_when>
  </verify>
  <acceptance_criteria>
    - The nesting statement is corrected from the primary source with the version range; the three CLAUDE.md sites and adapters.md:17 no longer assert no-nesting.
    - The correction goes through the generated stack block (regenerate verified) or a coupled STACK.md+CLAUDE.md commit with the coupling noted; the main-thread `Agent(...)` premise sites are re-checked and listed.
    - `check:public-docs` passes.
  </acceptance_criteria>
  <done>CLAUDE.md and the kit describe observed nesting behavior with its version range, sourced from the primary docs, and the correction survives a regen.</done>
</task>

<task type="auto">
  <name>Task 2: Reword the admission-server docblocks; annotate the Phase-31 B8 residual</name>
  <read_first>
    - `scripts/admission-server.ts` — the bare-name docblock sites (lines 4, 12, 56: the bare server name and the `mcp__grugops__.*` matcher description, now `mcp__(plugin_grugops_)?grugops__.*`).
    - `docs/audit/31-round4-residuals.md` — row B8 at :599 (still reads IN-04 as carried); `docs/audit/31-round6-residuals.md:976` for the annotation convention (`**ANNOTATED <date> by <plan> (<ID>), not re-verdicted:**` appended in-cell); the evidence is plan 33-38's WR-01 fix (commit `a9c1331b`) and the pinned IN-04 case at `scripts/context-io.test.ts:5736`.
    - `33.1-RESEARCH.md` § D-09 routing (rows 7 and 9) and § "The capture wave"; `33.1-PATTERNS.md` § `scripts/admission-server.ts` (derive the name, never type it; admission-server.js is not in the decider manifest) and § `docs/audit/31-round4-residuals.md`.
  </read_first>
  <files>scripts/admission-server.ts, scripts/admission-server.js, docs/audit/31-round4-residuals.md</files>
  <action>
    Reword the `scripts/admission-server.ts` docblock at lines 4, 12, 56 to name both the bare and the
    plugin-scoped admission tool spellings (the matcher is now `mcp__(plugin_grugops_)?grugops__.*`), or
    point at the derivation (`scripts/capture-live.test.ts:1401-1412` derives the plugin-form name from
    the held init frame). Confirm `scripts/admission-server.js` is not in any freshness or manifest set
    before committing (it is not in the decider manifest), and rebuild it via `npm run build`. Annotate
    `docs/audit/31-round4-residuals.md` row B8 (line 599) beside the historical verdict (never rewrite it)
    with an `**ANNOTATED <date> by 33.1-09, not re-verdicted:**` line recording that plan 33-38's WR-01
    fix closed IN-04 (commit `a9c1331b`) and the pinned test at `scripts/context-io.test.ts:5736`.
  </action>
  <verify>
    <automated>grep -ac 'ANNOTATED' docs/audit/31-round4-residuals.md</automated>
    <fails_when>a count of 0 — row B8 was not annotated</fails_when>
    <automated>npm run build && npm run check:build-parity && npm run typecheck</automated>
    <fails_when>non-zero exit, or check:build-parity does not print ALL CHECKS PASSED — admission-server.js drifted</fails_when>
  </verify>
  <acceptance_criteria>
    - The admission-server docblock names both the bare and plugin-scoped spellings (or points at the derivation); its `.js` is rebuilt and confirmed outside the decider manifest.
    - Row B8 is annotated beside the historical verdict (not rewritten) with the 33-38 WR-01 closure evidence.
    - build-parity and typecheck clean.
  </acceptance_criteria>
  <done>The admission-server docblocks describe the current matcher, and the Phase-31 B8 residual is annotated as closed without rewriting history.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Stale plugin rows — human-state cleanup; the agent never touches user plugin state</name>
  <action>Optionally uninstall the two stale `grugops@grugops` 0.1.0 plugin rows in your user plugin state; the agent will not run any `claude plugin` command against user state (D-09).</action>
  <instructions>
    Two stale `grugops@grugops` rows at version `0.1.0` (the retired pre-release seed) sit in USER plugin
    state, both disabled: a project-scope row for a repository outside this checkout, and a local-scope
    row whose project path is a `grugops-uat-e2e-*` temp target. These are outside the repository and
    outside this phase's file set, and D-09 routes them as human-state cleanup.
    1. Run `claude plugin list` and confirm the two stale `grugops@grugops` 0.1.0 rows (both disabled).
    2. If you want them gone, run `claude plugin uninstall` for each stale row yourself.
    3. This does not block the capture wave — the runner installs at local scope inside its own temp
       target and uninstalls in cleanup, so the live run does not collide with these rows.
  </instructions>
  <verification>The agent records your choice in the plan summary and quotes `claude plugin list` if you ran it; the agent runs no `claude plugin` command against user state.</verification>
  <resume-signal>Type "done" once you have cleaned up (or chosen to leave) the stale rows</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the generated CLAUDE.md stack block → its source STACK.md | A hand edit of the generated block is overwritten on the next regen |
| a primary documentation source → a kit claim about platform behavior | The correction must cite the source and state the version range, not assert always-true |
| user plugin state → the agent | The agent never runs `claude plugin` against user state; cleanup is human-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-26 | Repudiation | a stale no-nesting claim silently re-opening (row 309) | medium | mitigate | Correct through the generated block or a coupled commit; verify the regen path; grep the premise is gone |
| T-33.1-27 | Tampering | the agent mutating user plugin state | medium | mitigate | Human-action checkpoint; the agent never runs `claude plugin`; runner installs in its own temp target |
| T-33.1-28 | Repudiation | rewriting a historical audit verdict | low | mitigate | Row B8 is annotated beside the verdict, never rewritten |
| T-33.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed in this phase; the stale-plugin rows are a platform listing, not an install |
</threat_model>

<verification>
- `grep -aic 'cannot spawn subagents\|no nesting\|cannot nest' CLAUDE.md` — 0; `grep -aic 'sub-agents cannot nest' agent-factory/packaging/adapters.md` — 0.
- `grep -ac 'ANNOTATED' docs/audit/31-round4-residuals.md` — at least 1.
- `npm run build && npm run check:build-parity && npm run check:public-docs && npm run typecheck` — clean.
- Never `npm test`; the agent never runs `claude plugin` against user state.
</verification>

<success_criteria>
- The nesting statement is corrected from the primary source (with version range), surviving a regen; the admission docblocks and B8 are corrected/annotated.
- The stale-plugin cleanup is human-gated and recorded; the remaining non-windows post-guard deferred items are disposed; the ten windows reds stay out (D-15).
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| corrected nesting statement | docs | `CLAUDE.md` (via regen from `.planning/research/STACK.md`) + `agent-factory/packaging/adapters.md:17`; version range stated |
| main-thread `Agent(...)` premise site list | summary record | re-checked sites in adapters.md / install/README.md |
| reworded admission-server docblock | doc-in-code | `scripts/admission-server.ts` lines 4, 12, 56 |
| B8 annotation | audit annotation | `docs/audit/31-round4-residuals.md:599`, never rewritten |
| stale-plugin human-action record | checkpoint record | agent never touches user plugin state |
| post-guard deferred-items dispositions | ledger | resolved-with-evidence or WINDOWS row + owner |

<output>
Create `.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-09-SUMMARY.md` when done
</output>
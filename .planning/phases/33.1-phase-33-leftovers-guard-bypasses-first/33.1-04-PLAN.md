---
phase: "33.1"
plan: 04
type: execute
wave: 4
depends_on: ["33.1-03"]
files_modified:
  - scripts/checkpoints.ts
  - scripts/checkpoints.js
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
  - scripts/checkpoints.test.ts
  - scripts/fixtures/cr01-nested-corpus.json
autonomous: false
requirements: ["D-02", "IN-02"]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "C4 refuses git config injection ON THE NAME: for a segment naming `git`, refuse (fail-closed on `protected_branch_merge`) a `-c <key>=…` / `--config-env=<key>=…` whose key (compared case-insensitively) is outside a small DECLARED allow-list of harmless keys, a leading `GIT_CONFIG*` env assignment or `GIT_EXEC_PATH`, or a `git config` write of an `alias.*`/`include.*`/`includeIf.*` key; the harmless-key list is an allow-list (a missing key over-refuses, never under-refuses) whose members are each measured against a scratch bare remote before commit (D-01; D-02(a) — the git alias family from Phase 33 deferred-items becomes DENY rows, WINDOWS-class row 259)."
    - "The partial alias reader in `verbCandidates` (`/^alias\\.[^=]+=(.+)$/`, :1511-1520) and its call sites are DELETED — the C4 refusal replaces it, so one question has one grammar (the Phase 29 lesson); the now-dead `Candidates.opaque`/`.unreadable` fields are removed rather than left vestigial."
    - "C5 removes the benign-adjacency exemption under an xargs stdin feed entirely, a strict superset of D-03: every launcher-shaped word (`npm`, `yarn`, `pnpm exec`, `git submodule foreach`, `git bisect run`, `git rebase -x`) and any launcher a hand list would miss is refused; a replace-string word is treated as an expansion for C3; the GNU-parallel pipe-fed rule (option (b), measured cost 0 over the captures) is adopted in this wave so a stdin-to-argument launcher named on the right of a single pipe is stdin-fed without listing launchers (D-03; IN-02, WINDOWS row 308)."
    - "Each closure is RED-first through both entry points and mutation-proven: a case-sensitive key compare reopens the case-folded C4 row; restoring the feed exemption for one word reopens the C5 row (D-05)."
  artifacts:
    - path: scripts/checkpoints.ts
      provides: "C4 git config-injection refusal with a declared harmless-key allow-list; C5 feed exemption removed + option (b) pipe-fed rule; partial alias reader deleted"
    - path: scripts/checkpoints.test.ts
      provides: "scratch bare-remote executability helper for C4 rows; C4/C5 RED tests and mutations"
    - path: scripts/fixtures/cr01-nested-corpus.json
      provides: "D-02(a) git-alias-family DENY rows; IN-02 launcher-under-feed DENY rows"
  key_links:
    - from: scripts/checkpoints.ts
      to: scripts/checkpoints.test.ts
      via: "C4 DENY rows backed by a scratch-remote executability proof (bare remote + GIT_CONFIG_GLOBAL=/dev/null)"
      pattern: "GIT_CONFIG_GLOBAL"
    - from: scripts/checkpoints.ts
      to: hooks/hook-entry.ts
      via: "checkpoints.js edit regenerates the decider manifest"
      pattern: "generate:hook-manifest"
---

<objective>
Land C4 (git config injection refused on the name) and C5 (no adjacency exemption under a stdin feed),
the last two canonical-form rules. C4 closes the git alias family — a class, not one row: `-c`,
`--config-env`, the `GIT_CONFIG_*` env pairs, case-folded keys, the subsection form, a deprecated-command
alias, and a persisted `git config alias.x` write, all of which push `main` today. It is refused on an
allow-list of harmless keys (a missing key over-refuses, which the file's own admissibility rule
permits), and it deletes the partial alias reader so one question has one grammar. C5 removes the feed
exemption entirely rather than subtracting a hand list of launchers, and adopts the GNU-parallel
pipe-fed rule (measured cost 0). D-02(a) and IN-02 become DENY rows.

A blocking decision precedes C4: whether to additionally adopt the derived git verb-slot allow-list
(option (ii)), the only rule that reaches the host-config classes, at the cost of refusing users' own
git aliases. Purpose: refuse configuration injection and stdin-fed launchers. Output: C4 + C5 in
`scripts/checkpoints.ts`, the git-alias and launcher DENY rows, and a recorded option-(ii) decision.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CONTEXT.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-RESEARCH.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PATTERNS.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decision — adopt the derived git verb-slot allow-list (C4 option ii), or ledger the host-config classes</name>
  <reversibility rating="costly">Option (ii) refuses users' own git aliases across every git invocation; undoing it after adoption means re-widening the guard's allowed git surface. Not a data migration, so not one-way, but it changes an allow path many call sites depend on.</reversibility>
  <decision>Whether C4 additionally adopts a git verb-slot allow-list: the word adjacent to `git` (no flag between) must be a member of a DERIVED set of known, non-deprecated git commands (from `git --list-cmds=main,others,nohelpers` at generate time, minus the deprecated category, count asserted), or the segment fails closed.</decision>
  <context>
    This is the ONLY rule that reaches the host-config classes that live outside the command string:
    bare LB-08 under a global `help.autocorrect`, and an alias persisted by an earlier call or written
    with the Write tool. C4's on-the-line rules (Task 2) do NOT reach them. CONTEXT D-09 routes LB-08
    and RES-03 as residuals unless the canonical form can refuse them; option (ii) is the only shape
    that can. The availability measurement over the 236 captured commands must be attached to this
    decision (research recommends it), and aliases can override deprecated commands so the derived set
    must exclude them. If NOT adopted, the red-team (plan 05) must confirm these classes are unreachable
    from the string alone, and they go into the `failClosedCheckpoints` residual list.
  </context>
  <options>
    <option id="adopt-verb-slot">
      <name>Adopt option (ii): derived git verb-slot allow-list</name>
      <pros>Refuses bare LB-08 and a persisted/host alias — the only rule that reaches host-config classes; derived + count-asserted, no hand list</pros>
      <cons>Refuses users' own legitimate git aliases (availability cost); needs its own availability measurement over the 236 captured commands</cons>
    </option>
    <option id="ledger-host-config">
      <name>Ledger the host-config classes as disclosed residuals</name>
      <pros>No availability cost to users' aliases; the on-the-line C4 rules already close the injected forms (D-02(a))</pros>
      <cons>Bare LB-08 and a persisted/host alias stay ALLOW; the red-team must confirm they are unreachable from the command string alone</cons>
    </option>
  </options>
  <resume-signal>Select: adopt-verb-slot or ledger-host-config</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: C4 — refuse git config injection on the name; delete the partial alias reader</name>
  <behavior>
    - For a segment naming `git`, a `-c`/`--config-env` key outside the declared harmless allow-list (case-insensitive), a leading `GIT_CONFIG*`/`GIT_EXEC_PATH` env assignment, or a `git config` alias/include write is refused fail-closed on `protected_branch_merge`.
    - Each harmless-key allow-list member is measured against a scratch bare remote and cannot change which command git runs; `core.pager` is re-checked specifically.
    - The partial alias reader and its call sites are deleted; a case-sensitive key compare reopens the case-folded row (mutation).
  </behavior>
  <read_first>
    - `scripts/checkpoints.ts` — `verbCandidates` and the partial alias reader (:1494-1530), `gitPushIsGoverned` (:1580-1597), the git branch of `matchCommandCheckpoints` (:1907-1914), `Candidates` fields (:1486-1492), the admissibility rule (:664-678).
    - `scripts/checkpoints.test.ts` — the `mutantOf` harness (:2585-2596); no bare-remote helper exists (see `scripts/check-flip-manifest.test.ts:50-80` for the hermetic git env to copy: `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` = `/dev/null`, bounded retry).
    - `33.1-RESEARCH.md` § C4 (the harmless-key allow-list; `GIT_CONFIG_*`, `--config-env`, the persisted-write case; delete the reader), § claim assumption A2 (`core.pager` runs a command — re-check), § Reproductions D-02(a).
    - `33.1-PATTERNS.md` § `scripts/checkpoints.ts` (C4 site, delete the reader) and § No Analog Found (scratch bare-remote helper).
  </read_first>
  <files>scripts/checkpoints.ts, scripts/checkpoints.js, hooks/hook-entry.ts, hooks/hook-entry.js, scripts/checkpoints.test.ts, scripts/fixtures/cr01-nested-corpus.json</files>
  <action>
    Add the C4 refusal inside the existing git branch, beside `gitPushIsGoverned`, using a DECLARED
    allow-list of harmless keys (recommended starting members `user.name`, `user.email`, `color.*`,
    `core.pager`, `commit.gpgsign`) compared case-insensitively; refuse `-c`/`--config-env` keys outside
    it, leading `GIT_CONFIG*`/`GIT_EXEC_PATH` env assignments, and `git config` alias/include writes.
    Measure each allow-list member against a scratch bare remote (add the helper modeled on
    `scripts/check-flip-manifest.test.ts`); re-check `core.pager` specifically and drop it from the
    allow-list if it can change which command runs. Delete the partial alias reader (:1511-1520) and its
    call sites, and remove the now-dead `Candidates.opaque`/`.unreadable` fields. If Task 1 chose
    `adopt-verb-slot`, add the derived git verb-slot allow-list from `git --list-cmds=main,others,nohelpers`
    minus deprecated, count asserted. Flip the D-02(a) git-alias-family corpus rows to `deny` (commands
    derived from `33.1-RESEARCH.md`, not restated here) with a scratch-remote executability proof.
    Regenerate the manifest and commit `scripts/checkpoints.{ts,js}` and `hooks/hook-entry.{ts,js}`
    together. Mutation: a case-sensitive key compare reopens the case-folded row.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" summary — a D-02(a) alias form does not deny, or the scratch-remote proof did not run</fails_when>
    <automated>npm run generate:hook-manifest && npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit, or freshness does not report the deciders fresh</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/autonomy-zero-config.test.ts scripts/floor-invariance.test.ts</automated>
    <fails_when>non-zero exit — C4 changed a roster decision or deny wording (AUTO-07 differential)</fails_when>
  </verify>
  <acceptance_criteria>
    - C4 refuses git config injection on the name with a declared, measured harmless-key allow-list; the D-02(a) alias family denies through both entry points with a scratch-remote proof.
    - The partial alias reader and its call sites and the dead `Candidates` fields are removed; a case-sensitive key compare reopens the case-folded row.
    - Manifest fresh; autonomy-zero-config green; build-parity and typecheck clean.
  </acceptance_criteria>
  <done>Git config injection is refused on the name, the git alias family denies, and one grammar answers the alias question.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: C5 — no adjacency exemption under a stdin feed; adopt the pipe-fed rule</name>
  <behavior>
    - The benign-adjacency exemption under an xargs feed is removed entirely; every D-03 launcher-shaped word plus any launcher a hand list would miss is refused (IN-02).
    - Under a replace flag, a word containing the replace string is treated as an expansion for C3 (moves RES-03's literal-verb form into C3b).
    - Option (b): a segment on the right of a single pipe that names a governed tool is treated as stdin-fed (GNU parallel), without listing launchers.
    - Restoring the feed exemption for one word reopens the C5 row (mutation).
  </behavior>
  <read_first>
    - `scripts/checkpoints.ts` — the xargs feed exemption (:1880-1886), `completedFromStdin` (:1781-1786), `XARGS_REPLACE_FLAG_RE` (:1635) and `isXargsWord` (:1638), the XARGS docblock (:1599-1632).
    - `33.1-RESEARCH.md` § C5 (remove the exemption; replace-string as expansion; option (b) pipe-fed, measured cost 0 over the captures — adopt in this wave) and § Reproductions IN-02.
    - `33.1-PATTERNS.md` § `scripts/checkpoints.ts` (C5 site; refuse through `completedFromStdin`; update the XARGS docblock).
  </read_first>
  <files>scripts/checkpoints.ts, scripts/checkpoints.js, hooks/hook-entry.ts, hooks/hook-entry.js, scripts/checkpoints.test.ts, scripts/fixtures/cr01-nested-corpus.json</files>
  <action>
    Remove the `decided` benign-adjacency exemption under a feed entirely; refuse through the existing
    `completedFromStdin` helper so the deny text names the unreadable command. Under a replace flag,
    treat a word containing the replace string as an expansion for C3 (RES-03's literal-verb form moves
    into C3b). Adopt option (b): a segment on the right of a single pipe naming a governed tool is
    stdin-fed, keyed on the pipe rather than the `xargs` basename — this removes the GNU-parallel
    `UNKNOWN - verify` at measured cost 0 (record the 4 regex false positives research measured are all
    a `.git` path inside a quoted grep the projection does not name as `git`). Update the XARGS docblock.
    Flip the IN-02 launcher-under-feed corpus rows to `deny` (commands derived from `33.1-RESEARCH.md`).
    Regenerate the manifest and commit `scripts/checkpoints.{ts,js}` and `hooks/hook-entry.{ts,js}`
    together. Mutation: restore the exemption for one word reopens the row.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" summary — an IN-02 launcher form does not deny, or an allow control from the 236 captures regressed</fails_when>
    <automated>npm run generate:hook-manifest && npm run freshness:hook-manifest && npm run check:build-parity && npm run typecheck</automated>
    <fails_when>non-zero exit, or check:build-parity does not print ALL CHECKS PASSED</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/autonomy-zero-config.test.ts scripts/floor-invariance.test.ts</automated>
    <fails_when>non-zero exit — C5 changed a roster decision or deny wording</fails_when>
  </verify>
  <acceptance_criteria>
    - The feed exemption is removed; every D-03 launcher plus the pipe-fed launcher class (IN-02) denies through both entry points, RED-first; restoring the exemption for one word reopens the row.
    - A replace-string word is treated as an expansion for C3; the option (b) pipe-fed rule is adopted with its measured 0 cost recorded.
    - Manifest fresh; autonomy-zero-config green; build-parity and typecheck clean.
  </acceptance_criteria>
  <done>No adjacent word earns an exemption under a stdin feed, and stdin-fed launchers including GNU parallel are refused.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git runtime config channels (`-c`, `--config-env`, `GIT_CONFIG_*`) | Configuration that redefines which command git runs |
| a stdin feed under a launcher | The launcher's arguments come from stdin, invisible on the line |
| the harmless-key allow-list | A wrongly admitted key (e.g. `core.pager`) is a bypass; a missing key over-refuses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-10 | Elevation of privilege | git config injection redefining a subcommand (D-02(a)) | high | mitigate | C4 refuses injection on the name with a measured harmless-key allow-list; alias reader deleted; scratch-remote proof |
| T-33.1-11 | Elevation of privilege | indirect launch through a stdin-fed launcher (IN-02) | high | mitigate | C5 removes the feed exemption; option (b) pipe-fed rule; replace-string treated as expansion |
| T-33.1-12 | Tampering | `core.pager` wrongly admitted to the harmless-key list | high | mitigate | Measure each allow-list key against a scratch bare remote; drop `core.pager` if it can change which command runs |
| T-33.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed in this phase; nothing to audit |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts` — green.
- `npm run generate:hook-manifest && npm run freshness:hook-manifest && npm run check:build-parity && npm run typecheck` — fresh, ALL CHECKS PASSED, clean.
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/autonomy-zero-config.test.ts scripts/floor-invariance.test.ts` — green.
- Never `npm test`.
</verification>

<success_criteria>
- C4 and C5 land; D-02(a) git-alias family and IN-02 launcher-under-feed deny through both entry points.
- The option-(ii) decision is recorded; the partial alias reader is deleted; the pipe-fed rule is adopted.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| C4 git config-injection refusal | rule | `scripts/checkpoints.ts`; declared harmless-key allow-list |
| deleted partial alias reader + dead `Candidates` fields | removal | one grammar for the alias question |
| C5 feed-exemption removal + option (b) pipe-fed rule | rule | `scripts/checkpoints.ts` |
| scratch bare-remote executability helper | test helper | `scripts/checkpoints.test.ts` |
| D-02(a) + IN-02 DENY rows | fixture rows | commands derived from RESEARCH § Reproductions |
| recorded option-(ii) decision | decision record | git verb-slot allow-list adopted or ledgered |

<output>
Create `.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-04-SUMMARY.md` when done
</output>
---
phase: "33.1"
plan: 03
type: execute
wave: 3
depends_on: ["33.1-02"]
files_modified:
  - scripts/checkpoints.ts
  - scripts/checkpoints.js
  - hooks/guard.ts
  - hooks/guard.js
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
  - scripts/checkpoints.test.ts
  - scripts/fixtures/cr01-nested-corpus.json
  - install/README.md
  - agent-factory/packaging/adapters.md
  - CHANGELOG.md
autonomous: true
requirements: ["D-02", "RES-01"]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "C3 requires a literal command word: after skipping leading `NAME=value` assignments, redirections and shell reserved words (`do`, `then`, `else`, `if`, `while`, `until`, `!`, `{`, `time`), the first word of a segment must be a C1 literal, or the segment is refused on EVERY governed checkpoint because the command it runs is unknowable (C3a). A segment holding any expansion word AND a word equal to a governed verb or flag (derived from `COMMAND_CHECKPOINT_RULES.flatMap(r => [...r.verbs, ...(r.flags ?? [])])`) is refused on that verb's checkpoint (C3b) (D-01; D-02(b) — the spliced-variable body from Phase 33 deferred-items becomes a DENY row, WINDOWS-class row 292)."
    - "C3 closes the D-02(b) spliced-`$x` body and the RES-01 computed-command-word class; the reserved-word skip is load-bearing and mutation-proven — dropping it turns the `do`-position row RED."
    - "The four public-doc residual sites are reworded IN THE SAME COMMIT as C3 to the residual the phase actually leaves open (a tool AND its verb both computed at run time behind a launcher word, which only C3d closes and this phase does not adopt): `install/README.md`, `agent-factory/packaging/adapters.md`, `CHANGELOG.md` (under `## [Unreleased]`, edited in place), and the `hooks/guard.ts` residual comment; `npm run check:banned-claims` and `npm run check:public-docs` stay green (Pitfall 4)."
    - "The `failClosedCheckpoints` residual docblock is rewritten over the new model and keeps only what C1-C3 cannot refuse; nothing C1-C3 can refuse is added to it (D-05)."
  artifacts:
    - path: scripts/checkpoints.ts
      provides: "C3a command-word literal test with the reserved-word skip; C3b derived governed-verb-set refusal; rewritten failClosedCheckpoints docblock"
    - path: install/README.md
      provides: "reworded residual claim (the launcher-computed residual, not the variable-indirection class C3 closes)"
    - path: hooks/guard.ts
      provides: "reworded residual NOTE comment above the literal pattern set"
  key_links:
    - from: scripts/checkpoints.ts
      to: hooks/guard.ts
      via: "guard.ts comment reworded in the same commit; tsc keeps the comment so guard.js and its manifest hash change"
      pattern: "generate:hook-manifest"
    - from: scripts/checkpoints.ts
      to: install/README.md
      via: "C3 closes the RES-01 residual, so the public claims describing it become false and are reworded together"
      pattern: "check:banned-claims"
---

<objective>
Land C3 (a command word must be literal; an expansion in the command position is never readable) and,
in the same commit, reword the four public and in-code sites that state the variable-indirection
residual C3 closes. C3 closes the D-02(b) spliced-variable body and the RES-01 computed-command-word
class. The reserved-word skip is load-bearing (the measurement script missed the `do`-position form
until it was added), so it is mutation-proven. C3b's governed-verb set is DERIVED from the rule table,
never hand-typed, catching wrapper positions C3a cannot see.

Purpose: refuse a run-time-computed command word, and keep the public residual claims honest. Output:
C3 in `scripts/checkpoints.ts`, reworded residual docs, and the rewritten `failClosedCheckpoints`
docblock.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-CONTEXT.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-RESEARCH.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-PATTERNS.md
@.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: C3 — a literal command word; refuse a run-time-computed command</name>
  <behavior>
    - After skipping leading assignments, redirections and shell reserved words, the first word of a segment must be a C1 literal, or the whole segment is refused on every governed checkpoint (C3a).
    - A segment holding an expansion word plus a word equal to a derived governed verb/flag is refused on that verb's checkpoint (C3b), covering wrapper positions (`env $x push …`, `nice -n 5 $x push …`).
    - The D-02(b) spliced-`$x` body and RES-01 deny through both entry points; dropping the reserved-word skip turns the `do`-position row RED.
  </behavior>
  <read_first>
    - `scripts/checkpoints.ts` — the "EVERY canonical non-flag word is a candidate tool" comment (:1759-1761), `matchCommandCheckpoints` main loop (:1768-1922), the `refuse` helper (:1774-1778), `COMMAND_CHECKPOINT_RULES` (:680-775), `failClosedCheckpoints` and its docblock (:1680-1754), `governedToolsNamedBy` (:1467).
    - `33.1-RESEARCH.md` § C3 (C3a reserved-word skip is load-bearing; C3b derived verb set; the one residual stated plainly is tool-and-verb both computed behind a launcher word — C3d, not adopted) and § Residuals disclosed item 1.
    - `33.1-PATTERNS.md` § `scripts/checkpoints.ts` (C3 site; derived governed-verb set; refusals through the existing `refuse` helper so the deny text names the unreadable word).
  </read_first>
  <files>scripts/checkpoints.ts, scripts/checkpoints.js, hooks/hook-entry.ts, hooks/hook-entry.js, scripts/checkpoints.test.ts, scripts/fixtures/cr01-nested-corpus.json</files>
  <action>
    Add the C3a command-word test to `matchCommandCheckpoints`: for each segment, the first word after
    leading `NAME=value` assignments, redirections and shell reserved words must be a C1 literal (ask the
    plan-02 C1 authority); otherwise refuse the segment on every governed checkpoint through the existing
    `refuse` helper. Add C3b: a segment holding any expansion word and a word equal to a derived governed
    verb or flag (`COMMAND_CHECKPOINT_RULES.flatMap(r => [...r.verbs, ...(r.flags ?? [])])`) is refused on
    that verb's checkpoint. Flip the plan-02 corpus rows for the D-02(b) splice and RES-01 (and the
    command-position-expansion mechanism) to `deny`; add the RED test and the reserved-word-skip mutation
    (dropping the skip reopens the `do`-position row). Rewrite the `failClosedCheckpoints` "WHAT IT STILL
    DOES NOT SEE" docblock over the new model, keeping only what C1-C3 cannot refuse. Regenerate the
    manifest and commit `scripts/checkpoints.{ts,js}` and `hooks/hook-entry.{ts,js}` together. Do not
    restate any bypass string in this plan — derive corpus commands from `33.1-RESEARCH.md`.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts</automated>
    <fails_when>non-zero exit, or a "0 passed" summary — D-02(b)/RES-01 do not deny, or an existing decision broke</fails_when>
    <automated>npm run generate:hook-manifest && npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit, or freshness does not report the deciders fresh</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/autonomy-zero-config.test.ts scripts/floor-invariance.test.ts</automated>
    <fails_when>non-zero exit — C3 changed a roster decision or deny wording (AUTO-07 differential)</fails_when>
  </verify>
  <acceptance_criteria>
    - C3a refuses a non-literal command word on every governed checkpoint; C3b refuses an expansion segment carrying a derived governed verb/flag; the governed-verb set is derived from `COMMAND_CHECKPOINT_RULES`, not hand-typed.
    - The D-02(b) spliced-`$x` body and RES-01 deny through both entry points, RED-first; dropping the reserved-word skip reopens the `do`-position row.
    - The `failClosedCheckpoints` docblock is rewritten and holds nothing C1-C3 can refuse; manifest fresh; autonomy-zero-config green.
  </acceptance_criteria>
  <done>A run-time-computed command word is refused, D-02(b) and RES-01 deny through both entry points, and the residual docblock is honest.</done>
</task>

<task type="auto">
  <name>Task 2: Reword the four residual sites in the C3 commit; keep the doc gates green</name>
  <read_first>
    - `install/README.md` (the second "gap to be honest about" bullet, ~:414-417, the RES-01 class), `agent-factory/packaging/adapters.md` (~:118-122), `CHANGELOG.md` (under `## [Unreleased]` at :14, ~:60-65 — edit in place), `hooks/guard.ts` (the residual NOTE comment above the literal pattern set, ~:306-314).
    - `scripts/check-banned-claims.ts` (:238 names `install/README.md` in its scanned set).
    - `33.1-RESEARCH.md` § Pitfall 4 (reword each to the launcher-computed residual C3 does NOT close; do not claim more than the red-team confirms) and § Residuals disclosed item 1.
    - `33.1-PATTERNS.md` § Public residual docs and § Voice (clear professional voice on a safety surface).
  </read_first>
  <files>install/README.md, agent-factory/packaging/adapters.md, CHANGELOG.md, hooks/guard.ts, hooks/guard.js, hooks/hook-entry.ts, hooks/hook-entry.js</files>
  <action>
    Reword each of the four sites from "a tool name held in a shell variable is out of scope" to the
    residual that actually stays open after C1-C3: a tool AND its verb both computed at run time behind a
    launcher word (the C3d class this phase does not adopt). Edit `CHANGELOG.md` in place under
    `## [Unreleased]`. Keep clear professional voice (safety surface). Because `hooks/guard.ts` is a
    decider, its comment change alters `guard.js` and its manifest hash, so regenerate the manifest and
    commit `hooks/guard.{ts,js}` and `hooks/hook-entry.{ts,js}` with the docs. Do not restate the old
    residual sentence anywhere in this plan body (it is what the doc gates scan for). Land these in the
    SAME commit as Task 1's C3 rule where possible; if separated, keep them in this plan's wave.
  </action>
  <verify>
    <automated>npm run check:banned-claims &amp;&amp; npm run check:public-docs</automated>
    <fails_when>non-zero exit — a residual claim scan found the old variable-indirection wording still present</fails_when>
    <automated>npm run generate:hook-manifest && npm run freshness:hook-manifest && npm run check:build-parity && npm run typecheck</automated>
    <fails_when>non-zero exit, or check:build-parity does not print ALL CHECKS PASSED — the decider .js drifted or the manifest is stale</fails_when>
  </verify>
  <acceptance_criteria>
    - The four residual sites are reworded to the launcher-computed residual; `check:banned-claims` and `check:public-docs` pass.
    - The `hooks/guard.ts` comment change is captured by a regenerated manifest; build-parity and typecheck clean.
  </acceptance_criteria>
  <done>The public and in-code residual claims describe only what C1-C3 leave open, and the doc gates are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the command word position → the executed tool | An expansion here makes the tool unknowable; must be refused |
| public residual claims → users' trust in the guard | A stale "out of scope" claim about a class the guard now refuses is a false claim |
| `hooks/guard.ts` comment → `guard.js` hash | tsc keeps comments, so a comment edit needs a manifest regen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-07 | Elevation of privilege | a run-time-computed command word (D-02(b), RES-01) | high | mitigate | C3a literal-command-word test with reserved-word skip; C3b derived governed-verb set; DENY rows RED-first + mutation |
| T-33.1-08 | Repudiation | stale public residual claims | medium | mitigate | Reword all four sites in the C3 commit; `check:banned-claims` + `check:public-docs` gate |
| T-33.1-09 | Tampering | a stale decider manifest after a comment-only guard.ts edit | high | mitigate | `generate:hook-manifest` + `freshness:hook-manifest` |
| T-33.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed in this phase; nothing to audit |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts` — green.
- `npm run check:banned-claims && npm run check:public-docs` — pass.
- `npm run generate:hook-manifest && npm run freshness:hook-manifest && npm run check:build-parity && npm run typecheck` — fresh, ALL CHECKS PASSED, clean.
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/autonomy-zero-config.test.ts scripts/floor-invariance.test.ts` — green.
- Never `npm test`.
</verification>

<success_criteria>
- C3 refuses a run-time-computed command word; D-02(b) and RES-01 deny through both entry points.
- The four residual sites and the `failClosedCheckpoints` docblock describe only the launcher-computed residual C3 leaves open; doc gates green.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| C3a command-word literal test + reserved-word skip | rule | `scripts/checkpoints.ts` |
| C3b derived governed-verb-set refusal | rule | `scripts/checkpoints.ts` |
| rewritten `failClosedCheckpoints` docblock | doc-in-code | keeps only what C1-C3 cannot refuse |
| reworded residual claims | docs | `install/README.md`, `agent-factory/packaging/adapters.md`, `CHANGELOG.md`, `hooks/guard.ts` |
| D-02(b) + RES-01 flipped to DENY rows | fixture rows | `scripts/fixtures/cr01-nested-corpus.json` |

<output>
Create `.planning/phases/33.1-phase-33-leftovers-guard-bypasses-first/33.1-03-SUMMARY.md` when done
</output>
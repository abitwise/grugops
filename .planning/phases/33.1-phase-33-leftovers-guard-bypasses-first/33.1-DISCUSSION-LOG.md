# Phase 33.1: Phase 33 Leftovers: Guard Bypasses First - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-25
**Phase:** 33.1-phase-33-leftovers-guard-bypasses-first
**Areas discussed:** Guard design, Phase split / cap, Capture re-attempt, Windows row 274 + nesting

---

## Guard design

| Option | Description | Selected |
|--------|-------------|----------|
| Canonical-form cutover | Segment naming a governed tool must be all plain literals; everything else refused on the name | ✓ |
| Targeted fixes per review | Whitespace-free-word refusal, `"…$…"` opaque, permuted R4-11/12 rows | |

| Option | Description | Selected |
|--------|-------------|----------|
| Both deferred bypasses join | `git -c alias.p=push p` and spliced `$x` body become DENY rows | ✓ |
| Spliced `$x` only | Leave the alias form deferred | |

| Option | Description | Selected |
|--------|-------------|----------|
| Narrowing later wave, derived set or defer | After bypass closure is red-teamed; non-launching set must be derived | ✓ |
| Same change as the cutover | Design allow and refuse together | |
| Defer entirely | Row 269 to a later phase | |

| Option | Description | Selected |
|--------|-------------|----------|
| Corpus + both entries + 2 red-teams | WR-04 first; four families × both hook entries; ≥2 opus red-teams | ✓ |
| Add a parser-oracle fuzz | Real bash + stub binaries | |

**Notes:** Claude found two zero-key ALLOW bypasses in Phase 33 `deferred-items.md` (alias form, spliced `$x`) and proposed folding them in; the human agreed.

---

## Phase split / cap

| Option | Description | Selected |
|--------|-------------|----------|
| 33.1 guard only; insert 33.2 | Separate cap and verification boundary for the guard | |
| Keep all in 33.1, guard in wave 1 | One phase, one cap, blocking checkpoint after the guard | ✓ |

| Option | Description | Selected |
|--------|-------------|----------|
| Deferred 11 in, triaged | Fix, row with owner, or shown closed | ✓ |
| Only what blocks CI green | | |

| Option | Description | Selected |
|--------|-------------|----------|
| Blocking human checkpoint after guard wave | Later waves start on human go | ✓ |
| Automatic on red-team clean | | |

| Option | Description | Selected |
|--------|-------------|----------|
| Guard never closes by override | Cap reached → phase stops, guard carries first | ✓ |
| Same override path as Phase 33 | D-20-style override allowed | |

**Notes:** Claude recommended a split; the human chose one phase with the guard in wave 1.

---

## Capture re-attempt

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, one go, last wave | One authorized live capture behind a blocking go | ✓ |
| Readiness only, no live go | | |

Preconditions (multi-select): permission-mode zero-token check ✓; runner fixes D-33-R4-04 + 07 ✓; guard redesign landed ✓; windows leg green ✗. The human's answer: "Skip windows for now".

| Option | Description | Selected |
|--------|-------------|----------|
| Zero-token diagnosis, no second go | GAP-D1 stays open on red | ✓ |
| Allow one retry within the cap | | |

---

## Windows row 274 + nesting

| Option | Description | Selected |
|--------|-------------|----------|
| Worked, but non-blocking | Diagnose the handle, fix if portable | |
| Out of 33.1 entirely | Row 274 and CAP-02 carry untouched | ✓ |

| Option | Description | Selected |
|--------|-------------|----------|
| Primary doc source, else record observed-only | Cite code.claude.com, or say observed on 2.1.281 but not guaranteed | ✓ |
| Observed behaviour is enough | | |
| Leave CLAUDE.md; ledger only | | |

Follow-ups: the windows-class deferred entries follow row 274 out ✓. The ROADMAP goal line is edited to match ✓.

**Notes:** When CONTEXT.md was written, the deferred entries were re-read by status. Only 11 of 22 are still open, and only one of those is windows-class (the context-io ×10 reds). The guard-class open entries were routed to the guard wave: the regex throw, GNU parallel, and the LB-08 and RES-03 residuals, which are re-examined against the canonical form.

## Claude's Discretion

- Canonical-form grammar and its single home; wave layout after the guard; the source of the derived non-launching set; how the WR-02 race is resolved.

## Deferred Ideas

- Row 274 and the context-io windows reds (CAP-02); parser-oracle fuzz; a second capture go on red.

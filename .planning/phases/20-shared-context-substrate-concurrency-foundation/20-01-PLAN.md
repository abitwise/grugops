---
phase: 20-shared-context-substrate-concurrency-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/contracts/context-note.md
  - agent-factory/contracts/task-notes.template.md
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
autonomous: true
requirements: [SCTX-01, SCTX-02, SCTX-04]
must_haves:
  truths:
    - "A six-kind note (claim/finding/decision/failed-attempt/observation/artifact-ref) with a complete provenance fence (by/at/verified_by/confidence/refs/supersedes) validates clean; a note missing a required provenance field is a structural FAIL naming the missing field (SC-1, SCTX-01)."
    - "Two concurrent writes through appendNote/atomicWrite produce two distinct, un-clobbered, well-formed notes — no lost-update, no torn append (SC-2, SCTX-02)."
    - "appendNote writes a NEW file named <at-compact>-<by>-<kind>-<nonce>.md (crypto.randomUUID nonce) under notes/, never mutating a shared file; the markdown notes/ is the source of truth (SCTX-04)."
    - "The deterministic index.md + index.jsonl render is byte-reproducible from notes/ frontmatter, sorted by at (ISO lexicographic) with note-id tiebreak; current-state replay folds out superseded notes by the at+supersedes fields, never file position or mtime (SCTX-04)."
  artifacts:
    - path: "agent-factory/contracts/context-note.md"
      provides: "SCTX-01 note-schema doc: six kinds + provenance fence; explicit claim-note-KIND vs queue-CLAIM distinction; clear voice"
      contains: "verified_by"
    - path: "agent-factory/contracts/task-notes.template.md"
      provides: "The deterministic index.md render template (Phase-20 contract artifact)"
      min_lines: 10
    - path: "scripts/context-io.ts"
      provides: "readContext / appendNote / atomicWrite + deterministic index.md+index.jsonl render + schema validate"
      contains: "export function appendNote"
    - path: "scripts/context-io.test.ts"
      provides: "spawn-compiled-.js oracle: SC-1 schema-FAIL fixture + SC-2 concurrent un-clobbered writes (incl. Windows unlink-then-rename branch)"
      contains: "concurrent"
  key_links:
    - from: "scripts/context-io.ts appendNote"
      to: "notes/<at>-<by>-<kind>-<nonce>.md"
      via: "atomicWrite temp-then-rename to a fresh unique path"
      pattern: "renameSync"
    - from: "scripts/context-io.ts render"
      to: "index.jsonl"
      via: "frontmatter→JSON fixed-key-order projection"
      pattern: "JSON.stringify"
---

<objective>
Ship the foundation of the shared verified context: the SCTX-01 typed-note schema (contract docs) and `scripts/context-io.ts` — the ONLY sanctioned write path. It provides `readContext` / `appendNote` / `atomicWrite`, a deterministic zero-token `index.md` + `index.jsonl` render, and the structural validator that fails on a missing provenance field. Schema-first: the contract docs define the fence that `context-io.ts` validates against.

Purpose: Every later phase writes the shared context through this one helper. The schema and the atomic write primitive must exist and be proven (concurrent-safe, Windows-safe, deterministic-render) before any role or guard references them. This satisfies SC-1, SC-2, and the SCTX-04 append-only/replay substrate.

Output: `agent-factory/contracts/context-note.md`, `agent-factory/contracts/task-notes.template.md`, `scripts/context-io.ts` (+ committed `.js`), `scripts/context-io.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-shared-context-substrate-concurrency-foundation/20-CONTEXT.md
@.planning/phases/20-shared-context-substrate-concurrency-foundation/20-RESEARCH.md
@.planning/phases/20-shared-context-substrate-concurrency-foundation/20-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SCTX-01 note-schema contract doc + task-notes render template</name>
  <files>agent-factory/contracts/context-note.md, agent-factory/contracts/task-notes.template.md</files>
  <read_first>
    - agent-factory/README.md (kit-idiomatic CommonMark+YAML-frontmatter house style)
    - scripts/generate-catalog.ts (the `parseFrontmatter` flat key:value idiom + the `cell()` table-escape these docs describe; lines 49-79)
    - .planning/phases/20-shared-context-substrate-concurrency-foundation/20-CONTEXT.md (the LOCKED schema: six kinds, the provenance fence, the claim-KIND vs queue-CLAIM distinction)
    - CLAUDE.md (clear professional voice is MANDATORY on schema/trace surfaces — NO caveman voice in this file)
  </read_first>
  <action>
    Create the new directory `agent-factory/contracts/` (it does NOT exist yet — RESEARCH confirmed `ls` shows it absent; create it, do not assume it).

    Write `agent-factory/contracts/context-note.md` — the SCTX-01 note-schema contract. Document, in CLEAR PROFESSIONAL VOICE (no `grug`/caveman markers — this is a trace+safety surface per CLAUDE.md): the note format is YAML frontmatter + markdown body. The provenance fence keys are exactly `kind`, `by`, `at`, `verified_by`, `confidence`, `refs` (YAML list), `supersedes` (note-id ref or empty). `kind` is one of the six values: `claim`, `finding`, `decision`, `failed-attempt`, `observation`, `artifact-ref`. State the required-field rule: a note missing any of `kind`/`by`/`at`/`confidence` is a structural FAIL; `verified_by`/`supersedes`/`refs` may be empty in Phase 20 (admission rules are Phase 21, out of scope — say so). Document note identity `<at-compact>-<by>-<kind>-<nonce>.md` with the nonce sourced from `crypto.randomUUID()` and explicitly labelled a collision nonce, NOT a security token. Document that `at`+`supersedes` are the authoritative replay order, NOT filename/`ls`/mtime order.

    Include a CRITICAL DISTINCTION section: the `claim` note-KIND (a soft unverified assertion, a `kind:` value living in `notes/`) is a DIFFERENT concept from the queue CLAIM (hard work-ownership via atomic `mkdirSync(claimed/<task>/)` + `claim.md`, delivered in plan 20-02). The doc MUST NOT blur them in any single sentence or shared field. Per VFY-04 (Phase 21, forward-ref only) a `claim`-kind note can never satisfy a `finding`'s admission — note that as a forward reference, do not implement it here.

    Write `agent-factory/contracts/task-notes.template.md` — the deterministic `index.md` render template (a Phase-20 contract artifact). Define its sections and ordering (Claude's Discretion on exact shape, but it MUST be a pure deterministic function of `notes/` frontmatter — no timestamps beyond the notes' own `at`, no LLM language). State that the render is zero-token, byte-reproducible, freshness-gated, and that superseded notes are folded/marked at render time by the `at`+`supersedes` sort. Reference that free-text cells in any rendered markdown table are escaped via the `cell()` rule (`\`→`\\`, `|`→`\|`, newline→space) reused from `generate-catalog.ts`.

    Do NOT wire any role to call the render (that is Phase 24, out of scope).
  </action>
  <verify>
    <automated>test -f agent-factory/contracts/context-note.md && test -f agent-factory/contracts/task-notes.template.md && grep -q 'verified_by' agent-factory/contracts/context-note.md && grep -q 'failed-attempt' agent-factory/contracts/context-note.md && grep -qi 'queue CLAIM' agent-factory/contracts/context-note.md</automated>
  </verify>
  <acceptance_criteria>
    - `agent-factory/contracts/context-note.md` exists and contains all six kind tokens: `claim`, `finding`, `decision`, `failed-attempt`, `observation`, `artifact-ref`
    - `agent-factory/contracts/context-note.md` contains every provenance-fence key: `kind`, `by`, `at`, `verified_by`, `confidence`, `refs`, `supersedes`
    - `agent-factory/contracts/context-note.md` contains a section distinguishing the `claim` note-KIND from the queue CLAIM (grep `queue CLAIM` matches)
    - `agent-factory/contracts/context-note.md` documents `crypto.randomUUID()` as a collision nonce, NOT a security token
    - `agent-factory/contracts/task-notes.template.md` exists and is at least 10 lines
    - `grep -niE '\bgrug\b|\bsmash\b|\bcave\b|me think' agent-factory/contracts/context-note.md` returns no matches (clear-voice surface)
  </acceptance_criteria>
  <done>Both contract files exist under the newly-created `agent-factory/contracts/`; the schema doc carries the six kinds + the full provenance fence + the claim-KIND/queue-CLAIM distinction in clear voice; the template documents a deterministic zero-token render.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement context-io.ts (atomicWrite / appendNote / readContext + deterministic render + schema validate) with its spawn-compiled-.js oracle</name>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts</files>
  <read_first>
    - scripts/generate-catalog.ts (clone: `parseFrontmatter` lines 49-59, `cell()` escape lines 71-79, deterministic `lines.join("\n")` + single-trailing-newline emit, fixed-literal paths, fail-closed `fail()`)
    - scripts/catalog-freshness.test.ts (the spawn-compiled-.js + plant-and-restore + temp-mirror test idiom — clone this shape)
    - scripts/freshness.test.ts (the afterEach plant-and-restore + fail-closed Test-3 idiom)
    - .planning/phases/20-shared-context-substrate-concurrency-foundation/20-RESEARCH.md (Pattern 1 atomicWrite + Windows unlink-then-rename branch; the NoteRecord interface, toJsonl fixed-key projection, currentState replay; Pitfall 1/3/4; Code Examples)
    - CLAUDE.md (D-13 build model: node:fs/node:crypto/node:path ONLY; zero host runtime deps; clear voice; no fabrication)
  </read_first>
  <behavior>
    - Test SC-1a (GOOD): a note with all required fields (`kind`,`by`,`at`,`confidence` present; valid six-kind `kind`) validates → exit 0 / returns valid.
    - Test SC-1b (BAD, planted FAIL): a note with a removed required provenance field (e.g. `confidence` deleted) is a structural FAIL → nonzero exit, finding names the missing field. Also: a `kind` outside the six values is a FAIL naming the bad kind.
    - Test SC-2 (concurrent un-clobbered): launch N (≥8) `appendNote` writers via `Promise.all`/parallel spawn into the same `notes/` dir → assert exactly N distinct files exist, each parses to well-formed frontmatter, none overwritten (distinct `id`s). No lost-update, no torn append.
    - Test render determinism: render the same `notes/` twice → byte-identical `index.md` and `index.jsonl`; the JSONL emits provenance fields in a FIXED key order; notes sorted by `at` then note-id.
    - Test replay/supersede: when note B `supersedes` note A, `currentState` folds A out deterministically (by `at`+note-id, never file position).
  </behavior>
  <action>
    Implement `scripts/context-io.ts` (node:fs + node:crypto + node:path ONLY — zero host runtime deps, per D-13). Export:
    - `atomicWrite(finalPath, data)`: write a unique temp sibling (`${finalPath}.tmp-${process.pid}-${Date.now()}`) in the SAME dir, then `renameSync(tmp, finalPath)`. Catch `EPERM`/`EEXIST`/`EACCES` → `unlinkSync(finalPath)` (ignore not-present) → retry `renameSync` (the Windows branch). On any other error, best-effort `unlinkSync(tmp)` then rethrow. (See RESEARCH Pattern 1.)
    - `appendNote(task, note)`: validate the note (see validate below), compose YAML frontmatter (`kind`/`by`/`at`/`verified_by`/`confidence`/`refs` as a YAML list/`supersedes`) + markdown body, compute note-id `<at-compact>-<by>-<kind>-<nonce>` where nonce = a short slice of `crypto.randomUUID()`, and `atomicWrite` to `notes/<id>.md`. The publish target is ALWAYS a fresh unique path so the Windows-branch never fires for note publication. Never mutate an existing note file (append-only — SCTX-04). Validate the `task` name against `^[A-Za-z0-9._-]+$` (reject `..`, path separators, absolute paths) and `join` under a fixed context root (ASVS V12 path-traversal mitigation, T-20-01); throw a clear error on a bad task name.
    - `readContext(task)`: read+parse all `notes/<id>.md` frontmatter into NoteRecord[]; provide `currentState(notes)` that sorts by `at` (ISO lexicographic) with note-id tiebreak and folds out any note whose `id` appears in another note's `supersedes` (RESEARCH Code Examples). Never use mtime/`readdirSync` order for authority.
    - A deterministic render mode: read `notes/` frontmatter and emit `index.md` (from `task-notes.template.md`'s shape) and `index.jsonl` (one `toJsonl` line per note, FIXED key order `id,kind,by,at,verified_by,confidence,refs,supersedes`, body NOT in the JSONL line). Sort deterministically; single trailing newline (the `lines.join("\n")` idiom). Escape every free-text table cell with the `cell()` rule cloned from `generate-catalog.ts` (markdown-injection mitigation, T-20-02). Expose this render mode runnable as `node scripts/context-io.js <render-args>` so the plan-03 freshness gate can mirror-spawn it (the locked design has the freshness gate regenerate via the compiled context-io render).
    - A `validate(note)`: required `kind`/`by`/`at`/`confidence` present and `kind` ∈ the six values → ok; else a structural FAIL whose message NAMES the missing field or the bad kind. Reuse the flat `parseFrontmatter` from `generate-catalog.ts`; extend it minimally (stdlib only, no new dep) to read a `refs:` YAML list block (RESEARCH Open Question 1) — or accept a single-line comma form — your discretion, document the choice.

    Extend `package.json` is NOT in this plan (the `freshness:context` script lands in plan 20-03). Do NOT add config dials, do NOT seed `.grugops/` (Phase 24).

    Write `scripts/context-io.test.ts` covering the five behaviors above. Spawn the COMPILED `scripts/context-io.js` as a child process where the behavior is CLI-shaped (render); for the pure-function paths (validate, currentState, appendNote into a temp dir) import the compiled `.js` or drive via a small CLI entrypoint — match the existing spawn-compiled-.js idiom, never test the `.ts`. Use `mkdtempSync` temp dirs for all note I/O; never write into the committed tree. Prove BOTH the GOOD path (exit 0/valid) AND the planted BAD path (nonzero/FAIL naming the field) for SC-1 (no-fabrication).

    Run `npm run build` and commit the emitted `scripts/context-io.js` (the `freshness.ts` build-output gate auto-covers it via `OUTPUT_DIRS` including `scripts/`).
  </action>
  <verify>
    <automated>npm run build && npx vitest run scripts/context-io.test.ts && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/context-io.ts` contains `export function appendNote(` and `export function atomicWrite(`
    - `scripts/context-io.ts` references `crypto.randomUUID` and `renameSync` and `unlinkSync` (the Windows branch)
    - `scripts/context-io.ts` validates the task name against a charset that rejects `..` / path separators (grep finds `[A-Za-z0-9._-]` or equivalent allowlist)
    - `scripts/context-io.js` exists (committed compiled output)
    - `npx vitest run scripts/context-io.test.ts` exits 0
    - `scripts/context-io.test.ts` contains a concurrent-write test asserting N distinct un-clobbered files (grep `concurrent` matches) AND a planted schema-FAIL test asserting a nonzero/FAIL on a missing provenance field
    - `npm run build && npm run freshness` exits 0 (committed `.js` not drifted)
    - `node -e "require('node:child_process')"`-style: the helper imports only `node:fs`/`node:crypto`/`node:path` (grep shows no bare external package import)
  </acceptance_criteria>
  <done>`context-io.ts` provides atomicWrite (with the Windows unlink-then-rename branch), appendNote (fresh-unique-path, append-only, task-name-validated), readContext+currentState (at+supersedes replay), the deterministic byte-reproducible index.md+index.jsonl render, and a schema validator that fails on a missing field. The compiled `.js` is committed and fresh; the oracle proves SC-1 (GOOD+planted-FAIL) and SC-2 (concurrent un-clobbered).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subtask/task name → filesystem path | The `task` argument flows into `join(contextRoot, task)` and `notes/` paths; untrusted if ever derived from a queue file's content. |
| note free-text body/fields → rendered index.md table | Authored note text is rendered into a pipe-delimited markdown table. |
| committed index.{md,jsonl} → trace/proof | The derived index is committed and read as the machine-parsable trace; a fabricated "fresh" would corrupt the proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering/Elevation | `appendNote`/`atomicWrite` task→path join | mitigate | Validate `task` against `^[A-Za-z0-9._-]+$`, reject `..`/separators/absolute; `join` under a fixed context root only — never argv/env/content-derived absolute path. |
| T-20-02 | Tampering | `index.md` rendered table cells | mitigate | Reuse `cell()` escape (`\`→`\\`, `|`→`\|`, newline→space) from `generate-catalog.ts` for every free-text cell. |
| T-20-03 | Repudiation/Tampering | the `validate` schema gate | mitigate | No-fabrication: the SC-1 oracle proves a planted missing-field note FAILs (a gate that can only pass is fabricated green); validator names the missing field. |
| T-20-06 | Information disclosure (false) | `crypto.randomUUID()` nonce | accept | Documented as a collision nonce, NOT a security token; no secrecy property is claimed of it. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | N/A this phase — zero new packages (RESEARCH Package Legitimacy Audit: not applicable, node stdlib only). No install task exists. |
</threat_model>

<verification>
- `npm run build` succeeds; `scripts/context-io.js` committed and fresh under `npm run freshness`.
- `npx vitest run scripts/context-io.test.ts` green: SC-1 GOOD + planted-FAIL, SC-2 concurrent un-clobbered, render determinism, supersede replay.
- Contract docs exist under `agent-factory/contracts/` with the six kinds, the full provenance fence, and the claim-KIND/queue-CLAIM distinction, in clear voice.
- True-NFS atomicity is NOT claimed here — carried as `UNKNOWN - verify` (DOGF-02/Phase 26 is the eventual gate).
</verification>

<success_criteria>
SC-1 and SC-2 (ROADMAP Phase 20) are made TRUE and proven by `scripts/context-io.test.ts`. The SCTX-01 schema and the SCTX-04 append-only/replay substrate exist as the contract docs + the sanctioned `context-io.ts` write path.
</success_criteria>

<output>
Create `.planning/phases/20-shared-context-substrate-concurrency-foundation/20-01-SUMMARY.md` when done.
</output>

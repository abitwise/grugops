---
phase: 20-shared-context-substrate-concurrency-foundation
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - scripts/context-freshness.ts
  - scripts/context-freshness.js
  - scripts/context-freshness.test.ts
  - package.json
autonomous: true
requirements: [SCTX-03]
must_haves:
  truths:
    - "The committed per-task JSONL index (index.jsonl) regenerates byte-identically from the markdown notes/ via the context-io.js render (SCTX-03)."
    - "Editing the committed markdown-derived index without regenerating trips the freshness:context gate (fail-closed, exit non-zero, prints STALE) — and the gate fails closed on a broken regen, never reporting fresh (SC-4)."
    - "The markdown notes/ is the source of truth; the JSONL is the derived mirror; on any conflict the gate never edits notes/, it only proves the derived files match a fresh regen (markdown wins)."
  artifacts:
    - path: "scripts/context-freshness.ts"
      provides: "freshness:context drift gate — mirror-spawn regen of index.md+index.jsonl from notes/ via context-io render → byte-diff committed vs fresh → fail-closed"
      contains: "equals"
    - path: "scripts/context-freshness.test.ts"
      provides: "spawn-compiled-.js oracle: fresh PASS / planted-drift STALE / fail-closed-on-broken-regen (clone catalog-freshness.test.ts)"
      contains: "STALE"
    - path: "package.json"
      provides: "freshness:context npm script (mirror freshness:catalog shape)"
      contains: "freshness:context"
  key_links:
    - from: "scripts/context-freshness.ts"
      to: "scripts/context-io.js render"
      via: "mirror-spawn regenerate index.{md,jsonl} from notes/"
      pattern: "spawnSync"
    - from: "scripts/context-freshness.ts"
      to: "committed index.{md,jsonl}"
      via: "byte-compare committed vs rebuilt (fail-closed)"
      pattern: "equals"
---

<objective>
Ship the `freshness:context` drift gate — `scripts/context-freshness.ts` — a structural clone of `catalog-freshness.ts`, retargeted from the catalog generator to the `context-io.js` render. It regenerates `index.md` + `index.jsonl` from `notes/` into a throwaway temp mirror, byte-compares the committed derived files against the fresh regen, and fails closed on drift OR on a broken regen. Wire it as a `package.json` `freshness:context` script (mirroring `freshness:catalog`).

Purpose: This is the SCTX-03 commitment that the committed machine-parsable JSONL index never silently drifts from the markdown source of truth. Markdown wins; the JSONL is the derived mirror. Satisfies SC-4.

Output: `scripts/context-freshness.ts` (+ committed `.js`), `scripts/context-freshness.test.ts`, the `freshness:context` script in `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-shared-context-substrate-concurrency-foundation/20-CONTEXT.md
@.planning/phases/20-shared-context-substrate-concurrency-foundation/20-RESEARCH.md
@.planning/phases/20-shared-context-substrate-concurrency-foundation/20-VALIDATION.md
@.planning/phases/20-shared-context-substrate-concurrency-foundation/20-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement context-freshness.ts (clone catalog-freshness.ts; regen via context-io render → byte-diff → fail-closed) + the freshness:context script</name>
  <files>scripts/context-freshness.ts, scripts/context-freshness.js, package.json</files>
  <read_first>
    - scripts/catalog-freshness.ts (THE clone source — verbatim structure: import.meta.dirname ROOT, mkdtempSync temp mirror, mkdirSync+cpSync the generator .js + source dirs, spawnSync the mirrored generator, fail-closed on non-zero status, readFileSync committed vs rebuilt, `committed.equals(rebuilt)`, STALE message, cleanup, process.exit)
    - scripts/context-io.ts + scripts/context-io.js (the render mode this gate mirror-spawns — confirm the exact CLI invocation that emits index.md+index.jsonl from notes/, from plan 20-01)
    - package.json (the `freshness:catalog` script shape to mirror: `tsc --outDir .tmp-build && node scripts/<gate>.js`)
    - .planning/phases/20-shared-context-substrate-concurrency-foundation/20-RESEARCH.md (Pattern 3 clone-the-freshness-gate; Pitfall 3 non-deterministic-render; "markdown wins")
    - .planning/phases/20-shared-context-substrate-concurrency-foundation/20-01-SUMMARY.md (the render entrypoint context-io.js exposes)
    - CLAUDE.md (clear voice on this build/trace surface; fail-closed; no fabrication)
  </read_first>
  <action>
    Implement `scripts/context-freshness.ts` by cloning `scripts/catalog-freshness.ts`'s structure exactly, retargeted. Use node:child_process/node:fs/node:os/node:path ONLY, `import.meta.dirname` for the repo root (clear voice, fail-closed — same as the clone source). For each per-task context dir under the fixed context root (`.grugops/context/<task>/`), regenerate `index.md` + `index.jsonl` from that task's `notes/` into a `mkdtempSync` temp mirror by `cpSync`-ing the `context-io.js` render + the task's `notes/` into the mirror and `spawnSync`-ing the mirrored render (so the render writes to `<tmp>/.../index.{md,jsonl}` — never overriding the fixed OUT, mirroring the catalog gate's discipline). Then `readFileSync` the committed `index.md`+`index.jsonl` and byte-compare with `Buffer.equals` against the rebuilt files.

    Fail-closed (the locked contract): a non-zero regen, an unreadable committed file, or any byte mismatch → print a clear-voice `STALE:`/`FAILED:` finding and `process.exit(1)`. Never report "fresh" on a broken regen. Markdown (`notes/`) WINS — the gate never edits `notes/`; it only proves the derived index matches a fresh regen. Honor a `CHECK_ROOT`-style override (the existing test idiom) so the oracle can point the gate at a hermetic fixture root. On an all-fresh tree, print a clear success message and `process.exit(0)`. If no `.grugops/context/` exists yet (greenfield — install seeding is Phase 24), the gate must succeed vacuously rather than fail (there is nothing committed to drift), and say so honestly in its output.

    Add to `package.json` `scripts`: `"freshness:context": "tsc --outDir .tmp-build && node scripts/context-freshness.js"` (mirror the `freshness:catalog` line exactly).

    Run `npm run build` and commit `scripts/context-freshness.js`.
  </action>
  <verify>
    <automated>npm run build && npm run freshness:context && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/context-freshness.ts` contains `mkdtempSync`, `spawnSync`, and a `Buffer.equals`/`.equals(` byte-compare
    - `scripts/context-freshness.ts` regenerates via the `context-io.js` render (grep finds `context-io` in the mirror-spawn path)
    - `scripts/context-freshness.ts` fails closed: a non-zero regen branch exits 1 without reporting fresh (grep finds the fail-closed `process.exit(1)` after a status check)
    - `package.json` contains `"freshness:context"` whose value matches the `freshness:catalog` shape (`tsc --outDir .tmp-build && node scripts/context-freshness.js`)
    - `scripts/context-freshness.js` exists (committed compiled output)
    - `npm run freshness:context` exits 0 on the clean tree
    - `npm run build && npm run freshness` exits 0 (committed `.js` not drifted — the new gate's own `.js` is auto-covered)
  </acceptance_criteria>
  <done>`context-freshness.ts` is a faithful clone of `catalog-freshness.ts` retargeted to the `context-io.js` render, fail-closed, markdown-wins; the `freshness:context` script is wired and green; the compiled `.js` is committed and fresh.</done>
</task>

<task type="auto">
  <name>Task 2: Implement context-freshness.test.ts (fresh PASS / planted-drift STALE / fail-closed-on-broken-regen)</name>
  <files>scripts/context-freshness.test.ts</files>
  <read_first>
    - scripts/catalog-freshness.test.ts (THE clone source — the 3-test shape: Test 1 fresh→exit 0; Test 2 plant a byte of drift→nonzero+STALE+restore under afterEach; Test 3 fail-closed→break the regen→nonzero, NOT "fresh")
    - scripts/context-freshness.ts (the gate from Task 1 — its CHECK_ROOT override + the fixture-root shape the test must build)
    - scripts/check-foundation-guards.test.ts (the hermetic-mirror `mkdtempSync`+`cpSync` plant idiom + CHECK_ROOT spawn, if a fixture context tree is needed)
    - .planning/phases/20-shared-context-substrate-concurrency-foundation/20-VALIDATION.md (SC-4 row: fresh PASS / planted-drift STALE / fail-closed-on-broken-regen — the binding contract)
  </read_first>
  <action>
    Write `scripts/context-freshness.test.ts` cloning `catalog-freshness.test.ts`. Spawn the COMPILED `scripts/context-freshness.js` (never the `.ts`). Build a hermetic fixture context tree (a `mkdtempSync` root with at least one `.grugops/context/<task>/{notes/, index.md, index.jsonl}` whose committed index is a true render of the notes) and point the gate at it via the `CHECK_ROOT`-style override. Three tests, proving BOTH pass and planted-fail (no-fabrication):
    1. Test 1 (fresh PASS): committed index == fresh regen → exit 0, output contains "fresh".
    2. Test 2 (planted-drift STALE): append a byte of drift to the committed `index.jsonl` (plant-and-restore under `afterEach`) → exit non-zero, output contains `STALE` and names the drifted file.
    3. Test 3 (fail-closed): break the regen (e.g. corrupt a `notes/` file so the render exits non-zero, or remove the render's input) → exit non-zero AND the output does NOT contain the success-only "fresh" marker — the gate refuses to report fresh on a broken regen. Clean up the planted breakage in a `finally`.
  </action>
  <verify>
    <automated>npx vitest run scripts/context-freshness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/context-freshness.test.ts` spawns the compiled `scripts/context-freshness.js` (not the `.ts`)
    - The test contains a planted-drift case asserting `STALE` (grep `STALE` matches) and a fail-closed case asserting the absence of the success marker
    - The test plants into and restores a hermetic temp fixture, never mutating the committed tree
    - `npx vitest run scripts/context-freshness.test.ts` exits 0 (all three tests green)
  </acceptance_criteria>
  <done>The oracle proves SC-4: fresh→PASS, planted-drift→STALE (markdown wins, fail-closed), broken-regen→fail-closed-no-fresh. The no-fabrication contract holds (the gate can both pass and fail).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notes/ markdown → committed derived index.{md,jsonl} | The derived index is regenerated from notes/ and committed as the machine-parsable trace. |
| committed index.{md,jsonl} → CI/PR proof of freshness | A fabricated "fresh" or a silent drift would corrupt the trace the gate exists to protect. |
| broken regen → gate verdict | A render that fails to run must not be mistaken for an up-to-date index. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Repudiation/Tampering | the `freshness:context` gate verdict | mitigate | Fail-closed: a non-zero regen, unreadable committed file, or byte mismatch exits 1 and never reports "fresh" (cloned from catalog-freshness.ts fail-closed contract); the SC-4 oracle proves a planted drift trips it and a broken regen fails closed. |
| T-20-09 | Tampering | mirror-spawn temp regen path | mitigate | Regenerate into a `mkdtempSync` throwaway mirror; never override the fixed OUT; the gate never edits `notes/` (markdown wins). |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | N/A this phase — zero new packages (node stdlib only). No install task exists. |
</threat_model>

<verification>
- `npm run freshness:context` exits 0 on the clean tree (vacuous-success honestly stated if no `.grugops/context/` exists yet — install seeding is Phase 24).
- `npx vitest run scripts/context-freshness.test.ts` green: fresh PASS, planted-drift STALE, fail-closed-on-broken-regen.
- `npm run build && npm run freshness` exits 0; the new gate's committed `.js` is fresh.
- Markdown wins on any conflict — the gate never edits `notes/`.
</verification>

<success_criteria>
SC-4 (ROADMAP Phase 20) is made TRUE and proven: the committed per-task JSONL regenerates byte-identically from the markdown, editing the committed index without regenerating trips `freshness:context` (fail-closed), and the markdown wins. SCTX-03 delivered.
</success_criteria>

<output>
Create `.planning/phases/20-shared-context-substrate-concurrency-foundation/20-03-SUMMARY.md` when done.
</output>

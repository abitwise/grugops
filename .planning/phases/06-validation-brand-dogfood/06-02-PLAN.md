---
phase: 06-validation-brand-dogfood
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - brand/wordmark.svg
  - brand/wordmark-mono-dark.svg
  - brand/wordmark-mono-light.svg
  - brand/wordmark-lockup.svg
  - brand/icon.svg
autonomous: true
requirements: [BRAND-03]
must_haves:
  truths:
    - "Five SVG assets exist: color wordmark, mono-dark wordmark, mono-light/reverse wordmark, icon+wordmark lockup, and the standalone icon"
    - "Every SVG uses only the locked palette (Charcoal #2C2A28, Bone #F3ECE0, Granite #6B6B6B, Ochre #C8642D) — no off-palette hex"
    - "The wordmark reads as lowercase /grugops; all SVGs carry an aria-label and never resemble the children's-book character"
  artifacts:
    - path: "brand/wordmark.svg"
      provides: "Color wordmark (Ochre / + Charcoal grug + Granite ops) — §6.3 as-given + light cleanup"
      contains: "aria-label"
    - path: "brand/wordmark-mono-dark.svg"
      provides: "All-Charcoal mono wordmark (derived)"
      contains: "#2C2A28"
    - path: "brand/wordmark-mono-light.svg"
      provides: "All-Bone reverse wordmark for dark backgrounds (derived)"
      contains: "#F3ECE0"
    - path: "brand/wordmark-lockup.svg"
      provides: "Horizontal icon + wordmark lockup (derived)"
    - path: "brand/icon.svg"
      provides: "Club-on-stone rounded-square icon — §6.4 as-given + light cleanup"
      contains: "aria-label"
  key_links:
    - from: "brand/wordmark-mono-dark.svg / wordmark-mono-light.svg"
      to: "brand/wordmark.svg"
      via: "mechanical recolor of the same paths/text (no concept change, D-50)"
      pattern: "grugops"
    - from: "brand/wordmark-lockup.svg"
      to: "brand/icon.svg + brand/wordmark.svg"
      via: "icon placed left of wordmark in one horizontal viewBox"
      pattern: "grugops"
---

<objective>
Ship the five original-art brand SVGs (BRAND-03): the brand manual's §6.3 color wordmark and §6.4 icon as-given (light cleanup only), plus three mechanically-derived variants — mono-dark (all-Charcoal), mono-light/reverse (all-Bone), and the horizontal icon+wordmark lockup. Locked palette Charcoal/Bone/Granite + single Ochre; lowercase `grugops`; never resembling the children's-book character (D-50).

Purpose: BRAND-03 — the public-facing visual identity the README and collateral reference. Original geometric art (not the IP-sensitive mascot), enforced by the original-art/non-affiliation collateral in Plan 03.
Output: `brand/wordmark.svg`, `brand/wordmark-mono-dark.svg`, `brand/wordmark-mono-light.svg`, `brand/wordmark-lockup.svg`, `brand/icon.svg`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-validation-brand-dogfood/06-RESEARCH.md
@.planning/phases/06-validation-brand-dogfood/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the color wordmark + icon as-given (light cleanup) and derive the three variants</name>
  <files>brand/wordmark.svg, brand/wordmark-mono-dark.svg, brand/wordmark-mono-light.svg, brand/wordmark-lockup.svg, brand/icon.svg</files>
  <read_first>
    - `docs/initial/grugops_brand_manual.md` §6.3 (line 251, the drop-in color wordmark SVG — the as-given source) and §6.4 (line 266, the drop-in icon SVG)
    - `docs/initial/grugops_brand_manual.md` §6.1 (line 227, the exact palette hex) and §6.6 (visual do/don't — never resemble the children's-book character)
    - `.planning/phases/06-validation-brand-dogfood/06-RESEARCH.md` "Code Examples → Brand: color wordmark / icon / palette" (the full SVGs reproduced verbatim, lines 469-509) and the "Derivation recipe (D-50, mechanical, no concept change)"
    - `.planning/phases/06-validation-brand-dogfood/06-PATTERNS.md` "No Analog Found" (copy-from-spec, not copy-from-codebase; mono recolor + lockup recipe)
  </read_first>
  <action>
    Create `brand/wordmark.svg` from the manual's §6.3 drop-in as-given (Ochre `#C8642D` `/` + Charcoal `#2C2A28` "grug" + Granite `#6B6B6B` "ops", reading as `/grugops`). Light cleanup ONLY is allowed (D-50): tidy `viewBox`/alignment, ensure `role="img"` + `aria-label="grugops"`, optimize paths — NO concept change. Create `brand/icon.svg` from §6.4 as-given (club-on-stone rounded square, `role="img"` + `aria-label="grugops icon"`), same light-cleanup rule. Derive the three variants mechanically: `brand/wordmark-mono-dark.svg` = the wordmark with all three `fill` set to Charcoal `#2C2A28`; `brand/wordmark-mono-light.svg` (reverse, for dark backgrounds) = all three `fill` set to Bone `#F3ECE0`; `brand/wordmark-lockup.svg` = `icon.svg` scaled to ~the wordmark cap-height and placed to the LEFT of the wordmark in one horizontal `viewBox`, proportions/spacing at your discretion within "tasteful, no concept change." Use ONLY the locked palette: Charcoal `#2C2A28`, Bone `#F3ECE0`, Granite `#6B6B6B`, Ochre `#C8642D` — no other hex anywhere (Moss/Ember are NOT part of the required set). Keep the text lowercase `grugops`. Do not author a new concept and do not introduce any glyph/shape resembling the "Grug" children's-book character.
  </action>
  <verify>
    <automated>ls brand/wordmark.svg brand/wordmark-mono-dark.svg brand/wordmark-mono-light.svg brand/wordmark-lockup.svg brand/icon.svg && ! grep -RoiE '#[0-9a-f]{6}' brand/*.svg | grep -viE '#2C2A28|#F3ECE0|#6B6B6B|#C8642D' | grep -qi '#' && echo "PALETTE-CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist: `ls brand/wordmark*.svg brand/icon.svg` lists wordmark, -mono-dark, -mono-light, -lockup, and icon.
    - Off-palette hex grep returns nothing: `grep -RoiE '#[0-9a-f]{6}' brand/*.svg | grep -viE '#2C2A28|#F3ECE0|#6B6B6B|#C8642D'` is empty.
    - `grep -l 'aria-label' brand/wordmark.svg brand/icon.svg` lists both (a11y attrs present).
    - `grep -c '#2C2A28' brand/wordmark-mono-dark.svg` ≥ 3 (mono-dark is all-Charcoal); `grep -c '#F3ECE0' brand/wordmark-mono-light.svg` ≥ 3 (mono-light is all-Bone).
    - `grep -i 'grugops' brand/wordmark.svg` matches (lowercase wordmark text/label present).
  </acceptance_criteria>
  <done>Five palette-clean SVGs exist; color wordmark + icon are the §6.3/§6.4 drop-ins with light cleanup; the three variants are mechanical derivations; all carry a11y labels and lowercase grugops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brand art → public distribution | SVGs ship publicly; the only risk surface is legal/IP resemblance, not executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-IP | (Legal, not STRIDE) | brand/*.svg art | mitigate | Use the manual's §6.3/§6.4 ORIGINAL geometric SVGs verbatim (no new concept); never resemble the "Grug" children's-book character; the original-art + non-affiliation enforcement ships in Plan 03 (CONTRIBUTING/NOTICE) |
| T-06-PAL | Tampering (brand integrity) | off-palette color drift | mitigate | Restrict every `fill` to the four locked hex values; the verify grep fails on any off-palette hex |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — hand-written XML, no build step, no dependency (Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `ls brand/wordmark.svg brand/wordmark-mono-dark.svg brand/wordmark-mono-light.svg brand/wordmark-lockup.svg brand/icon.svg` — all present.
- Off-palette hex grep returns nothing across `brand/*.svg`.
- `aria-label` present on color wordmark + icon; mono variants are all-Charcoal / all-Bone respectively.
</verification>

<success_criteria>
BRAND-03 met: five original-art SVGs (color/mono-dark/mono-light/lockup wordmarks + icon) using only the Charcoal/Bone/Granite + single-Ochre palette, lowercase `grugops`, derived mechanically from the manual's drop-ins with no new concept and no children's-book resemblance.
</success_criteria>

<output>
Create `.planning/phases/06-validation-brand-dogfood/06-02-SUMMARY.md` when done.
</output>

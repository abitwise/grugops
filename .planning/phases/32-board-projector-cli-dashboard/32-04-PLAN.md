---
phase: 32-board-projector-cli-dashboard
plan: 04
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - scripts/board-corpus.ts
  - scripts/board-corpus.js
  - scripts/board-corpus.test.ts
  - scripts/board-oracle.test.ts
  - scripts/fixtures/board-replay/chess-board.md
  - scripts/fixtures/board-replay/dogfood-board.md
autonomous: true
requirements: [DASH-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the 141 measured live board rows is ADMITTED by the shipped grammar with its disposition named — `row`, `epicRow`, `update`, `preamble`, `nonColumnSection` or `unparsed` — so a green replay means the rows were classified correctly and not merely that nothing threw (D-01, D-22, D-02)."
    - "The mutation half of the corpus is REFUSED, each row by its named disposition: an un-indented mini-board inside an HTML comment, the same block indented by one space and by a tab, an unterminated comment opener, a comment opened inside a column section, `## Blocked (2)`, a lowercase id, an id with no dash, a one-space gap before the parenthetical, and a tab gap (D-03, D-05, D-07, D-02)."
    - "Replacing the comment-blanking pre-pass with a no-op turns the documentation-block case RED — the assertion discriminates, and a RED baseline recording that failure is committed before the case is claimed to pass (RESEARCH Pitfall 2)."
    - "`BOARD_CORPUS_COUNT` is declared in the same file as the data and checked at module load, so a corpus that silently loses rows throws instead of printing a green line over a smaller denominator."
    - "Every source path a corpus row cites resolves on disk; the two real boards are cited as their committed trimmed copies under `scripts/fixtures/board-replay/`, never as a path under a developer's home directory."
    - "The cross-product oracle's cell count is asserted three ways — the product of the pinned axis lengths, an independent loop counter, and the generated array's length — and every axis label is asserted reached by at least one cell."
    - "Structural invariants I1 through I5 hold over every generated cell; the oracle asserts properties of the answer and transcribes no expected parser output."
    - "The replay fixture preserves exactly one line of at least 34,494 characters as the bound case, and the oracle cell that parses it completes within a wall-clock bound asserted in the test (T-32-01)."
    - "Elision in the trimmed replay boards is marked in the file, so a reader can tell a structural trim from the original artifact."
  prohibitions:
    - statement: "Expected parser output is never transcribed into the oracle."
      status: enforced
      verification: "The oracle asserts invariants I1–I5 over generated cells; `scripts/board-oracle.test.ts` contains no literal expected snapshot for a generated cell."
    - statement: "A corpus row is never dropped silently."
      status: enforced
      verification: "`BOARD_CORPUS_COUNT` is checked at module load and throws with a message naming both numbers; `citedSources()` and a two-sided source check assert each named source has at least one row."
    - statement: "A vacuity floor is never trusted to catch a silently short denominator."
      status: enforced
      verification: "Every invariant case derives its element count independently of the loop that consumes it and asserts the two counts equal each other."
    - statement: "No path under a developer's home directory appears in a committed file."
      status: enforced
      verification: "`grep -rc 'Projects/hacks' scripts/board-corpus.ts scripts/fixtures/board-replay/` returns 0 for every file."
    - statement: "No runtime dependency is added."
      status: enforced
      verification: "`git diff --exit-code -- package.json package-lock.json`"
  artifacts:
    - scripts/board-corpus.ts
    - scripts/board-corpus.js
    - scripts/board-corpus.test.ts
    - scripts/board-oracle.test.ts
    - scripts/fixtures/board-replay/chess-board.md
    - scripts/fixtures/board-replay/dogfood-board.md
  key_links:
    - "Every corpus axis and every mutation row is derived from a shape named in `agent-factory/contracts/board.md`; the contract is the authority and the corpus is its replay."
    - "The oracle's invariants I1–I5 are the mechanical form of D-24's total-partition promise — I1 is what makes `never dropped` checkable."
    - "The discrimination mirror (a no-op comment stripper built from the live source) is the `context-io-writer-set.test.ts` `mirrorWithExtraWriter` idiom applied to this phase's highest-value assertion."
---

<objective>
Give DASH-02 real teeth. The grammar is pinned by a written contract and by a replay corpus that
carries both halves — every row of every board that exists is admitted with its disposition named,
and a mutation set derived from the contract's own refusal rules is refused — plus a cross-product
oracle that asserts structural invariants of the answer rather than a transcription of it.

Purpose: a canonical-form gate passes trivially by refusing everything, and a strip test passes
trivially when every transcribed comment happens to be indented. Research measured both: the phase's
self-declared highest-value test passes today with the stripper deleted. Only the mutation half and
a recorded RED baseline make the assertion mean anything.

Output: `scripts/board-corpus.ts` with both halves as data, `scripts/board-corpus.test.ts` with the
replay and the discrimination proof, `scripts/board-oracle.test.ts` with the seven-axis cross
product, and the two real boards preserved as trimmed, committed replay fixtures.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md
@.planning/phases/32-board-projector-cli-dashboard/32-PATTERNS.md
@.planning/phases/32-board-projector-cli-dashboard/32-02-SUMMARY.md
@agent-factory/contracts/board.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The corpus as data — the live half, the mutation half, and the trimmed replay boards</name>
  <files>scripts/board-corpus.ts, scripts/board-corpus.js, scripts/fixtures/board-replay/chess-board.md, scripts/fixtures/board-replay/dogfood-board.md</files>
  <read_first>
    - `scripts/canonical-corpus.ts:1-60, 1874-1930` — the corpus-as-data precedent in full: provenance as a data field, the verbatim-versus-framed transcription honesty rule, the module-load integrity throw with its both-numbers message, `citedSources()` derived from the rows rather than from a separate list, and `unresolvedSources(root)`.
    - `agent-factory/contracts/board.md` — every legal and illegal shape this corpus replays must be named there first; the corpus carries no opinion the contract does not.
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Measured Board Corpus in full — the inventory table, the six verbatim row shapes, the ID and heading conformance numbers, the `_Updated:` counts, and the single unbalanced-parenthesis row.
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Pitfall 2 — why a transcribed corpus is indistinguishable from a no-op stripper on today's artifacts.
    - `~/Projects/hacks/grugops-examples/cli-chess-example/plans/board.md` and `~/Projects/hacks/grugops-examples/dogfood-example/plans/board.md` — the two user-owned source artifacts to trim. Read them; copy structure, not bulk.
    - `docs/initial/agent_factory_builder_spec_v2.md:604-621`, `examples/02-brownfield-bootstrap.md:64-66`, `examples/05-release-run.md:68` — the three in-repo row sources.
  </read_first>
  <action>
Create the two replay fixtures first. Copy each real board into
`scripts/fixtures/board-replay/` **structurally trimmed**: every heading, every `_Updated:` line,
every comment delimiter, and every ticket row, with each row's and each update's long tail elided and
the elision **marked in the file** so a reader can tell a trim from the original. Preserve exactly
one line of at least 34,494 characters as the D-20 and ReDoS bound case — that line is the input the
truncation and the regex bounds exist for, and eliding it would delete the only evidence they work.
Add a header comment to each fixture naming its origin as a user-owned artifact outside this
repository, the date it was transcribed, and the fact that it is trimmed. No path under a developer's
home directory appears in any committed file.

Author `scripts/board-corpus.ts` on the `canonical-corpus.ts` shape. Each row is
`{id, kind, source, transcription, input, expectedDisposition, note}` where `kind` is `live`,
`mutation` or `control`; `source` is a repository-relative path; `transcription` is `verbatim` or
`framed`; and `expectedDisposition` is a member of a closed `as const` set drawn from the contract —
`row`, `epicRow`, `update`, `preamble`, `nonColumnSection`, `unparsed`, `nonColumnHeading`. Export
that set and its count. "It failed" and "it failed for the right reason" are different claims, and
only the second one is worth a green line.

The **live half** carries every one of the 141 measured rows across the five sources, each with the
disposition the contract assigns it, including the single row whose parenthetical never closes
(disposition `row`, note that `meta` degrades to null rather than the row being refused).

The **mutation half** is derived from the contract's refusal rules, not invented: the kit comment
block with its mini-board un-indented; the same block indented by one space; the same block indented
by a tab; an unterminated comment opener followed by a valid mini-board; a comment opened inside a
column section; `## Blocked (2)` with a bullet beneath it; a lowercase id; an id with no dash; an id
whose prefix disagrees with the configured prefix; a one-space gap before the parenthetical; a tab
gap. Each carries the disposition the contract assigns it — these are not all "refused", they are
each refused **into a named bucket**, and that distinction is the whole point.

Export `BOARD_CORPUS`, `BOARD_CORPUS_COUNT` and a `BOARD_CORPUS_SOURCES` list, with the count checked
at module load against `BOARD_CORPUS.length` and a throw whose message names both numbers and says
the count and the data live in one file precisely so the disagreement cannot be resolved by changing
whichever is more convenient. Export `citedSources()` derived from the rows and `unresolvedSources(root)`
resolving each against the repository root, so a row cannot outlive the artifact that justifies it.
Because the two real boards live outside this repository, the provenance check points at the
committed trimmed copies.

Build and commit `scripts/board-corpus.js` alongside its `.ts`.
  </action>
  <verify>
    <automated>npm run build && node -e "import('./scripts/board-corpus.js').then(m=>{const u=m.unresolvedSources('.');if(u.length)throw new Error('unresolved sources: '+u.join(', '));console.log('corpus rows='+m.BOARD_CORPUS_COUNT+', sources='+m.citedSources().length)})"</automated>
    <fails_when>non-zero exit, an `unresolved sources:` message, or a thrown module-load error naming a count disagreement</fails_when>
    <automated>npm run typecheck && npm run check:build-parity</automated>
    <fails_when>non-zero exit, any `error TS` line, or a `BUILD PARITY FAILED` line</fails_when>
    <automated>grep -rl 'Projects/hacks' scripts/board-corpus.ts scripts/fixtures/board-replay/ ; echo "matches=$?"</automated>
    <fails_when>the printed line is not `matches=1` (grep exit 1 means no file matched); any listed file name is a committed home-directory path</fails_when>
    <automated>awk '{ if (length($0) >= 34494) n++ } END { print "long_lines=" n+0 }' scripts/fixtures/board-replay/chess-board.md</automated>
    <fails_when>the printed line is not `long_lines=1` — exactly one bound-case line must survive the trim</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/board-corpus.ts` exports `BOARD_CORPUS`, `BOARD_CORPUS_COUNT`, `BOARD_CORPUS_SOURCES`, `citedSources`, `unresolvedSources`, and the closed disposition set with its count.
    - The module throws at load when `BOARD_CORPUS_COUNT` and `BOARD_CORPUS.length` disagree, with a message naming both numbers.
    - `BOARD_CORPUS.filter(r => r.kind === "live").length` is at least 141.
    - `BOARD_CORPUS.filter(r => r.kind === "mutation").length` is at least 11, and at least four of those are comment-strip mutations.
    - Every row's `expectedDisposition` is a member of the exported disposition set — asserted at module load or in the first test case.
    - `unresolvedSources('.')` returns an empty array.
    - `scripts/fixtures/board-replay/chess-board.md` contains exactly one line of at least 34,494 characters and a header comment naming its origin, date and trimmed status.
    - No committed file under `scripts/board-corpus.ts` or `scripts/fixtures/board-replay/` contains a home-directory path.
  </acceptance_criteria>
  <done>Both halves of the corpus exist as data with provenance, the count is self-checking, every cited source resolves inside the repository, and the two real boards outlive the user's local directories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The replay and the discrimination — a no-op stripper must turn the highest-value case RED</name>
  <files>scripts/board-corpus.test.ts</files>
  <read_first>
    - `scripts/board-corpus.ts` — the corpus as task 1 left it.
    - `scripts/canonical-corpus.test.ts` — the replay shape: every row asserted against the reader's actual named outcome, and the vacuity statement in its docblock that admitting everything and refusing everything are both trivially achievable and only the two together say the grammar discriminates.
    - `scripts/context-io-writer-set.test.ts:238-250, 270-278` — the `PREMISE:` assertions and `mirrorWithExtraWriter`, the idiom for building a deliberately broken mirror **from the live source** so a sweep is proven able to fail.
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Pitfall 2 in full, and §Common Pitfalls' warning sign — the strip test passing on the first run with no red baseline recorded.
    - `plans/board.md:4-51`, `agent-factory/seed/plans/board.md:4-51`, `plans/traceability.md:4-34` — the three in-repo documentation blocks the D-03 case feeds.
    - `25-06-RED-baseline.txt` and `25-06-GREEN-proof.txt` — the repository's established RED/GREEN evidence file style and naming.
  </read_first>
  <behavior>
    - Every `live` row replays to exactly its `expectedDisposition`; a mismatch names the row id, the expected disposition and the actual one.
    - Every `mutation` row replays to exactly its `expectedDisposition`; none of them reaches `row`, `epicRow` or `update`.
    - Feeding each of the three in-repo documentation blocks yields zero columns, zero rows and zero updates.
    - Building a mirror of the model whose comment pre-pass is a no-op, and replaying the same three blocks plus the un-indented mutation through it, makes the zero-columns assertion fail — proving the case can fail.
    - The replay's denominator is asserted equal to `BOARD_CORPUS_COUNT` and greater than zero, and each `kind` and each disposition is asserted reached by at least one replayed row.
  </behavior>
  <action>
Write `scripts/board-corpus.test.ts` as a replay with both halves and a discrimination proof.

The replay walks `BOARD_CORPUS` and asserts the shipped parser's actual disposition equals each row's
declared `expectedDisposition`, failing with a message that names the row id, the source path, the
expected disposition and the actual one. A failure that only says "it failed" cannot be acted on.

Before the findings, assert the harness's own premise: the corpus loaded, its length equals
`BOARD_CORPUS_COUNT`, that number is greater than zero, every `kind` is represented, and every member
of the disposition set is reached by at least one row. A vacuity floor catches an empty denominator
and has never caught a silently short one, so derive the reached-disposition set from the replay's
own results and compare it to the declared set in both directions.

Then the D-03 case: feed `plans/board.md:4-51`, `agent-factory/seed/plans/board.md:4-51` and
`plans/traceability.md:4-34` to the parser and assert zero columns, zero rows and zero updates from
each.

Then the discrimination half, which is what makes the previous paragraph worth anything. Build a
mirror of the model module **from the live source** with its comment pre-pass replaced by an identity
function — the `mirrorWithExtraWriter` idiom — and replay the same three blocks plus the un-indented
mutation row through the mirror, asserting the zero-columns claim **fails** there. Research measured
that with the stripper deleted entirely the transcribed blocks still pass, because every mini-board
in them happens to be four-space indented; the un-indented mutation is the only input that
discriminates, so it must be in the mirror's replay set and its inclusion must be asserted, not
assumed.

Capture the RED baseline before the discrimination case is claimed to pass: run the mirror replay
against the current build, record the failing output in a `NN-RED-baseline.txt` at the repository
root in the established style, and record the passing run in the matching `NN-GREEN-proof.txt`. A
strip test that passes on its first run with no recorded red is exactly the warning sign research
names.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-corpus.test.ts</automated>
    <fails_when>non-zero exit, "0 passed" in the summary line, or any `PREMISE:` message in the output</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-corpus.test.ts -t "discriminat"</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-corpus.test.ts -t "documentation block"</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - A named case replays every `live` row and a second named case replays every `mutation` row, each asserting the actual disposition equals the declared one.
    - A `PREMISE:`-prefixed block asserts the replayed count equals `BOARD_CORPUS_COUNT`, that it is greater than zero, and that the set of dispositions actually reached equals the declared disposition set in both directions.
    - A named case feeds all three in-repo documentation blocks and asserts zero columns, zero rows and zero updates from each.
    - A named discrimination case builds a no-op-stripper mirror from the live source and asserts the zero-columns claim fails against it; the case explicitly asserts the un-indented mutation row is in its replay set.
    - A `NN-RED-baseline.txt` at the repository root records the mirror's failing output, and the matching `NN-GREEN-proof.txt` records the passing run.
    - No `mutation` row's actual disposition is `row`, `epicRow` or `update`.
  </acceptance_criteria>
  <done>The grammar is proven to admit every real row for the right reason and to refuse every mutation into the right bucket, and the comment strip is proven able to fail by a recorded red rather than assumed correct by a green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The cross-product oracle — seven axes, three-way cell count, invariants over the answer</name>
  <files>scripts/board-oracle.test.ts</files>
  <read_first>
    - `scripts/section-locator-oracle.test.ts:172-301, 655-760, 1114` — the axis declaration block, the three-way cell-count assertion (product of pinned axis lengths, loop counter, array length), the per-axis label coverage assertion, and the recorded scar that a vacuity floor catches an empty denominator and has never caught a silently short one.
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Architecture Patterns Pattern 3 — the seven suggested axes derived from the measured corpus and the five structural invariants I1–I5, verbatim.
    - `agent-factory/contracts/board.md` — the authority every axis level must be traceable to.
    - `scripts/board-model.test.ts` — the I1–I5 cases plan 32-02 wrote over real documents; the oracle generalizes them over generated ones and must not re-spell them differently.
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Security Domain — the recorded superlinear-backtracking incident and the instruction to assert a wall-clock bound on the longest-line cell.
  </read_first>
  <behavior>
    - The generated cell set's size equals the product of the seven pinned axis lengths, equals an independently maintained loop counter, and equals the generated array's length.
    - Every label of every axis is reached by at least one generated cell.
    - Invariants I1 through I5 hold for every generated cell, each asserted as a property of the parse result rather than against a transcribed expected output.
    - The cell whose document carries the 34,494-character line parses within the asserted wall-clock bound.
    - The whole oracle file completes inside the quick-run budget, so it can stay in the per-commit sampling lane.
  </behavior>
  <action>
Write `scripts/board-oracle.test.ts` generating a labelled cross product of seven axes, each level
traceable to a shape named in `agent-factory/contracts/board.md`:

`AXIS_COMMENT` — absent, indented mini-board, un-indented mini-board, unterminated opener.
`AXIS_HEADING_SUFFIX` — limited, unlimited, blocked, the old spec's parenthesised count, a
parenthesised section reference, bare, and a third-level heading.
`AXIS_ROW_TAIL` — closes at end of line, trailing prose, no parenthetical, unbalanced.
`AXIS_ID` — conforming, epic prefix, feature prefix, lowercase, no dash, disagreeing prefix.
`AXIS_GAP` — two spaces, one space, a tab.
`AXIS_UPDATED` — canonical, the kit board's placeholder, absent.
`AXIS_POSITION` — before the first column heading, inside a column section, inside a non-column
section.

Declare each axis's labels in one place and derive its length from the declaration; assert the total
cell count **three ways** — the product of the pinned lengths, an independent loop counter
incremented during generation, and the generated array's length — and assert per-axis label coverage
so an axis that silently collapsed to one level fails by name.

Assert invariants I1 through I5 over every cell, exactly as plan 32-02 spelled them for real
documents: the partition is total and disjoint, no parsed entity carries a line number inside a
comment span, the column count equals the independently counted conforming headings outside comment
spans, every unparsed line number resolves to a non-blank raw line, and the row total equals the
independently counted legal rows outside comment spans. Derive each denominator independently of the
loop that consumes it. Transcribe no expected parser output for any generated cell — the oracle
asserts properties of the answer, which is the one thing that keeps 21,600 cells from becoming
21,600 transcriptions nobody reads.

Add one cell whose document embeds the replay fixture's 34,494-character line and assert its parse
completes under a stated wall-clock bound. Size the cross product so the whole file stays within the
quick-run budget; if it does not, reduce an axis's levels and record the reduction as a decision in
the file's docblock rather than silently dropping the slow cell.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-oracle.test.ts</automated>
    <fails_when>non-zero exit, "0 passed" in the summary line, a test-timeout message, or any per-axis coverage message naming an unreached label</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts scripts/board-corpus.test.ts scripts/board-oracle.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or any pre-existing suite file reported as failed</fails_when>
  </verify>
  <acceptance_criteria>
    - Seven axis constants are declared, each with its labels in one place, and the cell count is asserted three ways in three separate expectations with distinct messages.
    - A per-axis coverage case asserts every label of every axis is reached by at least one cell and names any label that is not.
    - Five named cases assert I1 through I5 over every generated cell, each deriving its denominator independently of the generation loop.
    - A named case parses a document containing the 34,494-character replay line and asserts the elapsed wall clock is under a stated bound.
    - `scripts/board-oracle.test.ts` contains no literal expected-snapshot object for a generated cell.
    - The three board test files together complete in under 30 seconds of wall clock on this machine.
  </acceptance_criteria>
  <done>The grammar is swept across a labelled seven-axis cross product whose size is proven three ways, every axis level is proven reached, the five structural invariants hold over every cell, and the longest line in the corpus parses inside a stated bound.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-owned example boards → committed replay fixtures | Content transcribed from outside the repository into a committed artifact |
| generated oracle documents → the parser | Adversarially shaped input built to break the grammar |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Denial of Service | the row, heading and comment scanners against a 34,494-character line | high | mitigate | A dedicated oracle cell embeds the real long line and asserts a stated wall-clock bound; this repository has already turned a 0.47-second guard into a 383-second one on a long line |
| T-32-11 | Tampering | a documentation block read as live state | high | mitigate | The mutation half carries four comment shapes the transcribed corpus cannot reach, and the no-op-stripper mirror proves the assertion is able to fail, with a recorded RED baseline |
| T-32-14 | Repudiation | a corpus row outliving the artifact that justifies it, or a green replay over a silently shortened corpus | medium | mitigate | Provenance is a data field; `BOARD_CORPUS_COUNT` is checked at module load; `unresolvedSources(root)` resolves every cited path; the replay asserts its own denominator |
| T-32-15 | Information Disclosure | a developer's home-directory path committed in a fixture header | low | mitigate | The fixture headers name the artifact class and the transcription date, never a home path; asserted by a repository-wide grep criterion |
| T-32-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists in this plan; `git diff --exit-code -- package.json package-lock.json` asserts the absence of change |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Files (new)**
- `scripts/board-corpus.ts` / `scripts/board-corpus.js`
- `scripts/board-corpus.test.ts`
- `scripts/board-oracle.test.ts`
- `scripts/fixtures/board-replay/chess-board.md`
- `scripts/fixtures/board-replay/dogfood-board.md`
- a `NN-RED-baseline.txt` / `NN-GREEN-proof.txt` pair for the no-op-stripper discrimination

**Exported symbols — `scripts/board-corpus.ts`**
`BOARD_CORPUS` · `BOARD_CORPUS_COUNT` · `BOARD_CORPUS_SOURCES` · `DISPOSITIONS` ·
`DISPOSITION_COUNT` · `citedSources` · `unresolvedSources`
Types: `CorpusRow` · `CorpusKind` · `Disposition` · `Transcription`

**Test-local constants — `scripts/board-oracle.test.ts`**
`AXIS_COMMENT` · `AXIS_HEADING_SUFFIX` · `AXIS_ROW_TAIL` · `AXIS_ID` · `AXIS_GAP` ·
`AXIS_UPDATED` · `AXIS_POSITION` · `EXPECTED_CELLS`

**No CLI flag, npm script or dependency changes.**
</artifacts_this_phase_produces>

<planner_assumptions>
- **DASH-02 (unclassified edge probe)** — resolved here. The edge surface of "pinned by a written
  spec and a fuzz suite" is the corpus itself, and it is closed by the mutation half plus the
  recorded RED baseline. The probe row is retained as flagged rather than dropped.
- **RESEARCH assumption A6** — that a board oracle of comparable size to the 7,200-cell section
  locator fits a sub-30-second quick-run budget. If it does not, task 3's action requires the axis
  reduction to be recorded as a decision in the file's docblock rather than the slow cell being
  dropped; the sampling rate in `32-VALIDATION.md` would then move the oracle to the per-wave lane.
- **Claude's discretion exercised** — CONTEXT.md left it open whether the two real boards are copied
  into `scripts/fixtures/`. They are, structurally trimmed with marked elisions and one preserved
  bound-case line, per research open question 6's recommendation: it keeps the shapes that drove
  D-01, D-22 and D-02 in the tree after the user's local directories change.
</planner_assumptions>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-corpus.test.ts scripts/board-oracle.test.ts` green
- `npx vitest run --exclude '**/scripts/e2e/**'` green (full suite, no regression)
- `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness` green
- `node -e "import('./scripts/board-corpus.js').then(m=>console.log(m.BOARD_CORPUS_COUNT))"` prints the declared count with no module-load throw
- The RED baseline and GREEN proof files for the no-op-stripper mirror exist at the repository root
</verification>

<success_criteria>
The board grammar is pinned by a corpus that both admits and refuses, every row of it classified by a
named disposition rather than by a pass/fail bit; the comment-strip assertion is proven able to fail
by a recorded red against a mirror built from the live source; and a seven-axis cross product whose
size is asserted three ways holds the five structural invariants over every cell, including one
carrying the longest line any real board has produced.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-04-SUMMARY.md` when done
</output>

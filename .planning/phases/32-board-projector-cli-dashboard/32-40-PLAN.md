---
phase: 32-board-projector-cli-dashboard
plan: 40
type: execute
wave: 3
gap_closure: true
depends_on: ["32-39"]
files_modified:
  - .planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07, DASH-08]
estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The harness asserts its own premise before it reports anything — the committed build is a faithful build of its sources and the artifact every probe below runs is the artifact a host runs — and a failed premise is recorded as a failed premise rather than absorbed."
    - "Every fix landed by the post-verification pass is treated as a CLAIM and re-measured: the eight commits `32-REVIEW-FIX.md` names, plus the two plans this round already executed, each re-run against the tree as it stands, each carrying a measured result rather than a description of a diff."
    - "Every recorded reproduction from `32-VERIFICATION.md`, `32-REVIEW.md` and `32-37-ADVERSARIAL-REVIEW.md` — including the ones those documents record as CLOSED — is re-run, because a closure asserted against one arm is this phase's most-repeated way of shipping the next defect."
    - "For every fix, four questions are asked and answered in writing: what is the predicate's INPUT assembled from, at WHICH POSITIONS is it asked, which SET does it enumerate, and does its UNION with its sibling arms still refuse what it refused before (DASH-01, DASH-06, D-21)."
    - "The interaction between two fixes landed in the same pass is probed as its own hypothesis, not assumed independent — specifically whether widening a classification partition changed the failure DIRECTION of a sibling fix's fallback path from a refusal into a silent skip (DASH-06, D-21)."
    - "The DASH-06 guard's live write-bypass plants are re-run against the current closure and each records an exit code, a failed-case count and whether the write-detection premise case is among the failures — a count alone cannot distinguish a guard that detected the writer from a guard that broke."
    - "Every finding carries a reproduction somebody else can run, a severity, the requirement it bears on, and an explicit `created by a round-3 or fix-pass change?` verdict — the number this phase's verifier and this project's recorded history both read first."
    - "The round ledger has one row per inventory item, the inventory count and the row count are both stated out loud and are equal; an item may be OPEN but never ABSENT (DASH-01 through DASH-08)."
    - { statement: "The live claude-CLI end-to-end lane is NOT run in this round and its state stays `UNKNOWN - verify`, as in every prior round of this phase and as the residual ledger already records", verification: backstop }
  prohibitions:
    - statement: "Never fix a source file in this plan — a closure written at the end of a round, in a plan that owns no source, is precisely how the previous round's first finding was created."
      status: pending
      verification: judgment
    - statement: "Never record a reproduction as closed on the strength of reading a diff or a fix report; re-run the original finding's own probe and print what it produced."
      status: pending
      verification: judgment
    - statement: "Never write a raw control byte into this document; spell every one as escape TEXT, because the repository gate refuses the byte and a single one makes plain searching report zero matches for strings that are present."
      status: pending
      verification: judgment
    - statement: "Never decide a requirement checkbox, a phase status or a verification verdict here — the verifier decides, from this evidence, in a separate pass."
      status: pending
      verification: judgment
    - statement: "Never leave a tracked file planted; restore it and record the clean-tree check after every plant."
      status: pending
      verification: judgment
  artifacts:
    - ".planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md — premise, the re-run of every recorded reproduction, the four questions per fix, the interaction hypothesis, the live write plants, the findings register with the created-versus-inherited verdict, the gate sweep and the round ledger"
  key_links:
    - "the premise check -> every transcript below it: a measurement over an artifact that is not a build of its sources measures nothing"
    - "each fix-pass commit -> its own re-run probe: the fix report is a claim, the probe is the evidence"
    - "the widened `bare` partition -> the specifier-recovery fallback: two fixes in one pass share a failure direction, and the direction is the finding"
    - "the findings register -> the created-versus-inherited ratio -> the verifier: the ratio is what this project's history says decides whether a round converged"
  deferred: []
---

<objective>
Run this round's independent red-team against the tree as it stands AFTER the post-verification fix
pass and after plans `32-38` and `32-39`, and produce the evidence the round-4 verifier will read.

**Round position: this is round 4 of a hard cap of 4.** There is no round 5. That is what makes this
plan's prohibition against fixing source correct rather than timid: a finding recorded here with a
reproduction is a fact the human can act on at plan `32-41`'s decision checkpoint, where a defect
introduced by an unreviewed closure written at the end of the last round would not be.

**What makes this round different from rounds 1 through 3.** The round-3 verifier wrote
`32-VERIFICATION.md` at 18:10 on 2026-09-16. Eight commits landed on `main` AFTER it — `d276f4e3`,
`dba9d72a`, `e52cfb25`, `70cbd447`, `7a3ae592`, `c5163183`, `0249d0c5`, `3e2f254a` — claiming to fix
the one remaining gap, both advisory items, the anti-pattern table's remaining row, and four more
warnings. **None of those commits has been verified by anybody.** The verification this round will be
judged against describes a tree that no longer exists. Treating that fix report as a closure is
exactly the move this repository has recorded failing in Phases 25, 27, 29, 31 and rounds 1 through 3
of this phase, where the per-round ratio of defects CREATED BY THE PREVIOUS FIX never fell.

The fix claims under probe, each with the hypothesis that would falsify it:

| Claim | Commit | The hypothesis this plan tests |
|---|---|---|
| The specifier scan's input is now actually code | `d276f4e3` | The scan blanks literal text and recovers it from a recorded offset table. When recovery MISSES, the specifier becomes the blanked text. What does the partition then call it, and is that a refusal or a skip? |
| A duplicate-identifier loser is no longer said to be joined | `dba9d72a` | The discriminator is one equality against a measured winner. What do the arms BESIDE it now say, and is the third sentence branch reachable at all? |
| A refusal's control-character evidence is spelled where it is built | `e52cfb25`, `0249d0c5` | The escaping is applied at two sites. Was that set derived or counted by hand, and do the two authorities that decide which characters vanish still agree outside the range the pin covers? |
| The watch arm opens the path that was checked | `70cbd447` | The seam now carries a resolved path. Is EVERY consumer of the decision using it, or only the one the finding named, and what does the failure branch do with a code nobody enumerated? |
| The ticket-reader census refuses the split-across-files shape | `7a3ae592` | The rule asks whether an imported BINDING is a key table. What about a renamed binding, a namespace import, a default export, or a two-hop re-export? |
| The check-target denominator is derived on the other side of the recognisers | `c5163183` | The step counter is one pattern over a command string. Which ordinary shell shapes does that pattern's own alphabet exclude? |
| `bare` is the positive test the docblock claims | `3e2f254a` | Widening a safety partition moves members OUT of the arm that refuses. Which members moved, and does any of them arrive from a sibling fix's fallback path? |

Purpose: D-21 makes the read-only guard's proof mechanical rather than asserted, and the two
predicates this pass rewrote — the specifier partition and the specifier scan's input — are that
proof's own instrument. D-06 makes one authority per predicate the rule, and a fix that lands in one
arm of an authority leaves the authority split. D-10 and D-11 make the projector's answers a closed,
discriminated set, which is what makes an unreachable branch a defect rather than a spare.

Output: one report — reproductions re-run, four questions answered per fix, an interaction hypothesis
tested, live write plants re-measured, a findings register with a created-versus-inherited verdict per
row, a gate sweep derived from the manifest, and a ledger asserted total against this round's own
inventory.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW-FIX.md
@.planning/phases/32-board-projector-cli-dashboard/32-37-ADVERSARIAL-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-38-GREEN-proof.txt
@.planning/phases/32-board-projector-cli-dashboard/32-39-GREEN-proof.txt
@.planning/phases/32-board-projector-cli-dashboard/deferred-items.md
</context>

<assumption_delta_decision>
**Signalled family:** unchanged — the same three historical signals (`second`, `alongside`,
`parameterized`) read out of this phase's earlier plan prose.

**Noun now primary:** none; this plan introduces no noun. It measures.

**Decision: `no-change`.** A review plan that owns no source cannot promote, add alongside, or change
any architectural noun.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Assert the premise, then re-run every recorded reproduction against the post-fix tree</name>

  <read_first>
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` — the
      `Independent Reproduction Transcripts` section in full: the six transcripts, their commands and
      the exact numbers each produced, including the ones recorded CLOSED
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` — the four live-plant rows in
      the Summary table (A, B, C, D and the unplanted control) with their guard results, and CR-01's
      two reproduction transcripts
    - `.planning/phases/32-board-projector-cli-dashboard/32-37-ADVERSARIAL-REVIEW.md` § 0 through § 2
      — the three premise failures that round recorded, and the reproductions it re-ran
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW-FIX.md` — every `Verified:` claim,
      each of which becomes a probe row here
    - `scripts/board-readonly.test.ts` — `CLOSURE_BASELINES` (`:1981`), its count pin (`:2223-2249`)
      and the two-sided parser oracle over the git-derived corpus
    - `.planning/phases/32-board-projector-cli-dashboard/32-38-GREEN-proof.txt` and
      `32-39-GREEN-proof.txt` — what this round's own two source plans measured, which this task
      re-measures independently rather than cites
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md</files>

  <action>
ASSERT THE HARNESS'S PREMISE FIRST, IN THREE PARTS, AND RECORD A FAILURE AS A FAILURE. Build, then
confirm no tracked build output moved; confirm the tree is clean apart from the pre-existing untracked
and modified paths the round-3 verification already recorded; and confirm the regression lane runs at
all. Round 3 recorded its own premise failing THREE times and round 2 six times across four rounds —
if a premise fails here, that is a finding with a row, not a detour to fix quietly.

RE-RUN EVERY RECORDED REPRODUCTION, INCLUDING THE CLOSED ONES. The population is derived, not
recalled: enumerate every transcript in the three source documents, count them, state the count, and
produce one row per transcript. Each row carries the command actually run, the output actually
produced, and a verdict of CLOSED, OPEN or CHANGED against what the source document recorded. A
transcript whose recorded state was CLOSED and which now produces a different result is the highest
value row in this document.

RE-RUN THE FOUR LIVE WRITE PLANTS AGAINST THE CURRENT CLOSURE, and add the two shapes the round-2
verifier planted. Each plant goes into a tracked file, the guard runs, and the row records the exit
code, the failed-case count, the passed-case count AND whether the write-detection premise case is
among the failures. A failed-case count alone cannot tell a guard that detected the writer apart from
a guard that stopped working — the round-3 verifier made exactly this distinction and it is why its
DASH-06 closure was believable. Restore the file after every plant and record the clean-tree check.

MEASURE THE UNPLANTED CONTROL. A plant battery with no control cannot distinguish a guard that
refuses everything from a guard that refuses writers.

RE-RUN THE DASH-03 REPRODUCTION THE VERIFIER USED, not the one plan `32-38` committed. Build the
duplicate-identifier-contest tree by hand from the verifier's own description, run the CLI, and read
the two fields. If plan `32-38`'s case and this independent run disagree, that disagreement is the
finding.

RE-MEASURE THE PARSER ORACLE'S OWN CORPUS SIZE INDEPENDENTLY. The oracle is pinned over a git-derived
corpus; derive the file count by a different route than the suite uses, state both numbers, and say
whether they agree. A vacuity floor catches an EMPTY denominator; this repository has recorded that a
SILENTLY SHORT one passes, so the number has to be produced twice by two instruments.

Write every transcript into `32-40-ADVERSARIAL-REVIEW.md` under a section per source document. Spell
every control character as escape TEXT.
  </action>

  <acceptance_criteria>
    - The document opens with a premise section carrying three named checks, each with its command and its output, and each labelled PASSED or FAILED.
    - The document states the derived count of recorded reproductions and carries that many rows, each with a command, an output and a CLOSED/OPEN/CHANGED verdict.
    - The document carries at least six live write-plant rows plus one unplanted control, each with exit code, failed count, passed count and whether the write-detection premise case is among the failures.
    - Every plant row records the clean-tree check that followed it.
    - The document carries an independent DASH-03 reproduction built from the verifier's description, and states whether it agrees with plan `32-38`'s committed case.
    - The document states the parser oracle's corpus size measured two ways and whether the two numbers agree.
  </acceptance_criteria>

  <verify>
    <automated>git diff --exit-code -- scripts/ agent-factory/ install/ hooks/ docs/ package.json</automated>
    <fails_when>non-zero exit — a plant was left in a tracked file</fails_when>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit, or a passed-case count below 171 — the unplanted guard must be green after the plant battery</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or any line naming `32-40-ADVERSARIAL-REVIEW.md` — a raw control byte reached the report</fails_when>
  </verify>

  <done>Every recorded reproduction has been re-run against the post-fix tree with its own transcript and a verdict, the write plants have been re-measured with the premise case tracked, and the harness's own premise is asserted rather than assumed.</done>

  <reversibility rating="reversible">A measurement document; every plant is restored and the clean tree is asserted.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The four questions per fix, and the cross-fix interaction hypothesis</name>

  <read_first>
    - `scripts/js-import-closure.ts` lines 220-300 — `scanSource`, its `literals` offset table and its
      blanking arms; lines 430-470 — `moduleSpecifiers` with its capture-offset recovery and its
      documented fallback to the raw capture; lines 100-120 — `classifySpecifier`'s widened `bare`
      test; line 386 onward — the note recording the deleted export
    - `scripts/board-readonly.test.ts` — `CLOSURE_BASELINES`, the ten foreign spellings asserted to
      stay foreign, and the two-sided parser oracle
    - `scripts/board-model.ts` lines 1060-1140 — `visible` (`:1063`) and the two sites that call it
      (`:1096`, `:1130`); lines 1025-1045 — `TICKET_CONTROL` (`:1031`) and `RENDER_STRIPPED` (`:1042`)
    - `scripts/board-dashboard.ts` lines 250-340 — `sanitizeCell` and `scrub`; lines 900-960 — the
      `LoopDeps.contained` signature (`:936`) and `defaultDeps` (`:948`); lines 1100-1200 — `arm`
      (`:1108`), its failure branch, and `deps.watch(decision.real, …)` (`:1186`)
    - `scripts/board-read.ts` lines 585-650 — `OUTSIDE_ROOT` (`:589`), the `Containment` union
      (`:594`) and `insideRoot`'s arms including `{ ok: false, code: err.code ?? … }` (`:635`)
    - `scripts/validate.test.ts` lines 1925-2100 — the binding-is-the-subject rule, its two
      investigated false positives and its discrimination cases
    - `scripts/check-foundation-guards.test.ts` lines 12070-12120 and 12470-12500 — the two target
      recognisers and the independently derived step counters (`:12477-12482`)
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW-FIX.md` — every `New arms probed`
      and `Verified` paragraph, which names what the fixer already checked so this task can spend its
      budget on what the fixer did not
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md</files>

  <action>
ASK THE SAME FOUR QUESTIONS OF EVERY FIX AND WRITE DOWN FOUR ANSWERS. What is this predicate's INPUT
assembled from; at WHICH POSITIONS is the predicate asked; which SET does it ENUMERATE; and does its
UNION with the arms beside it still refuse everything it refused before. These four are this
repository's own recorded probes, each earned by a round that shipped a bypass for want of asking it.
One row per fix, in a table, with the hypothesis that would falsify each answer.

THEN TEST THE CROSS-FIX INTERACTION, WHICH IS THIS PLAN'S PRINCIPAL HYPOTHESIS. Two fixes landed in
the same pass on the same module. One made the scan's input the blanked source and recovers a
specifier's real text from an offset table, documenting a fallback for the case where recovery misses.
The other widened the `bare` arm so that anything whose first character is not a path or URL
introducer is skipped rather than refused. Determine, by running it, what the partition calls a
recovered-as-blank specifier, and therefore whether a recovery miss is now a REFUSAL or a SILENT SKIP.
Then determine whether any input can make recovery miss: probe a specifier reached through a template
substitution, a specifier inside a nested template, a specifier in a re-export, a specifier whose
literal carries an escaped quote, and a specifier written as a template literal rather than a quoted
string. For each, state whether the real text is recovered, whether the classification matches a real
parse, and what the guard would do. A skip, unlike a refusal, means the closure walker does not follow
the module — which is the DASH-06 no-write claim's own mechanism.

PROBE THE ESCAPING SITE SET. Two sites apply the escaping. Derive, by parsing the module, the set of
message compositions that interpolate a slice of a source line into a diagnostic, and compare it
against the two. Then probe the two-authority agreement outside the range the pin covers: a code point
above the pinned ceiling, a lone surrogate, and a character one authority deletes that the other does
not. State which authority wins at each.

PROBE THE WIDENED SEAM'S CONSUMERS. Find every implementation and every consumer of the containment
decision using a search that forces text mode — a content classifier calls one of these test files
program text, and plain matching has already returned zero matches there for strings that are
present. State the count. Then ask, per consumer, whether it uses the resolved path or the spelling it
started with, including the watcher map key and the close path, not only the open call. Then ask what
the failure branch does with a code that is neither the containment refusal nor a known error code.

PROBE THE CENSUS RULE'S OTHER IMPORT SHAPES. The rule asks whether an imported binding is itself a
key table. Probe a renamed binding, a namespace import, a default export, a re-export through a third
file, and a key table reached through a barrel. For each, state whether a working split reader built
that way is detected, and build at least one of them as a real, working second reader that reads both
keys out of the fixture's deliberate-disagreement ticket, so the answer is a measurement.

PROBE THE STEP COUNTER'S ALPHABET. The denominator counts runner invocations with one pattern over the
command string. Enumerate ordinary shell shapes that pattern's own alphabet excludes — a single pipe,
a newline, a leading parenthesis, a wrapper command before the runner, a background separator — and
for each state whether a second module spelled that way would be counted, and therefore whether it
would get a reachability row. Say explicitly which of these are LIVE on the current manifest and which
are hypothetical.

PROBE THE WIDENED PARTITION'S MOVED MEMBERS. Enumerate what moved from the refusing arm into the
skipped arm. State each moved member, whether it is a legitimate package specifier, and whether any
route in the closure walker can deliver it. A leading-whitespace spelling and an empty-after-blanking
spelling are the two to check first, because they are what the sibling fix's fallback produces.

RECORD EVERY ANSWER, INCLUDING THE ONES THAT CAME BACK CLEAN. A probe that found nothing is evidence;
an unrecorded probe is not.
  </action>

  <acceptance_criteria>
    - The document carries a four-question table with one row per fix under probe and a falsifying hypothesis per answer.
    - The document states, with a transcript, what the specifier partition calls a recovered-as-blank specifier and whether a recovery miss is a refusal or a skip.
    - The document carries one row per probed specifier shape — template substitution, nested template, re-export, escaped quote, template-literal specifier — with the recovered text, the classification, a real-parse comparison and the guard consequence.
    - The document states the derived set of diagnostics that interpolate a source-line slice, compares it against the two escaped sites, and records the two-authority agreement at a code point above the pinned ceiling, at a lone surrogate, and at a character the two treat differently.
    - The document states the containment-seam implementation and consumer count obtained with a text-forcing search, and answers per consumer whether it uses the resolved path, including the watcher map key and the close path.
    - The document records what the containment failure branch does with an unenumerated code.
    - The document carries one row per probed import shape for the census rule, with at least one built as a real working second reader and its measured carrier count.
    - The document carries one row per excluded shell shape for the step counter, each marked LIVE or hypothetical against the current manifest.
    - The document enumerates every member that moved from the refusing arm to the skipped arm, including the leading-whitespace and empty-after-blanking spellings, with a deliverability verdict each.
  </acceptance_criteria>

  <verify>
    <automated>git diff --exit-code -- scripts/ agent-factory/ install/ hooks/ docs/ package.json</automated>
    <fails_when>non-zero exit — a probe was left in a tracked file, or this plan edited source it is forbidden to edit</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts scripts/validate.test.ts scripts/check-foundation-guards.test.ts</automated>
    <fails_when>non-zero exit, or `0 passed` in the summary line — the probes must leave the suites they exercise green</fails_when>
    <automated>grep -c 'hypothetical\|LIVE' .planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md</automated>
    <fails_when>a printed count below 5 — fewer than five excluded shell shapes carry a liveness verdict</fails_when>
  </verify>

  <done>Every fix has four written answers and a falsifying hypothesis, the cross-fix interaction has a measured verdict, and every probed shape — specifier, diagnostic, seam consumer, import route, shell step and moved partition member — has a recorded answer.</done>

  <reversibility rating="reversible">Probes on disposable copies and a measurement document; the clean tree is asserted.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The findings register with a created-versus-inherited verdict, the gate sweep, and the ledger asserted total</name>

  <read_first>
    - `.planning/phases/32-board-projector-cli-dashboard/32-37-ADVERSARIAL-REVIEW.md` § 6 through § 11
      — the findings format, the created-versus-inherited ratio stated as a number, the gate sweep
      derived from the manifest, the round ledger asserted total, and the probe arithmetic
    - `package.json` — the `scripts` block, the source the gate sweep's row set must be DERIVED from
      rather than recalled
    - `.planning/phases/32-board-projector-cli-dashboard/deferred-items.md` — the carried items,
      including the `check:diff-disposition` row whose count `32-37` corrected from 77 back to 78 and
      identified as a counting-instrument artifact
    - `.planning/REQUIREMENTS.md` lines 129-136 and 218-225 — the eight requirement statements and
      their current status rows, which this plan READS and does not touch
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` — the `gaps`, `advisory`
      and `Anti-Patterns Found` entries, which supply this round's inventory
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md</files>

  <action>
WRITE ONE FINDING PER DEFECT, EACH WITH A REPRODUCTION SOMEBODY ELSE CAN RUN. Number them
continuously from this phase's existing series so a reader can follow the phase's findings across all
four rounds. Each finding carries: a severity, the requirement it bears on, the reproduction with its
actual output, the bound on its blast radius measured rather than argued, and a verdict column
answering `created by a round-3 or fix-pass change?` with yes, no, or a stated basis for being unable
to tell. Record findings that came back CLEAN as clean rows in the register, not as omissions.

STATE THE CREATED-VERSUS-INHERITED RATIO AS A NUMBER, and set it beside the same ratio from rounds 1,
2 and 3. This project's recorded history is that the ratio never fell across eight rounds in Phase 31
and never fell across three rounds here. Whether it fell this round is the single most load-bearing
number in this document, because it is what tells the human at plan `32-41`'s checkpoint whether one
more fix is likely to converge or to move the defect one register over.

DERIVE THE GATE SWEEP'S ROW SET FROM THE MANIFEST. Read the script names out of `package.json`, state
how many there are, run each gate that this round's work could plausibly have moved, and record exit
code and headline output per row. Then state the row count and assert it equals the derived script
count minus the ones deliberately not run, each of those named with its reason. Do NOT run the live
claude-CLI end-to-end lane; record its state as unmeasured, with that phrasing this repository uses
for anything it did not measure.

RE-MEASURE THE CARRIED FAILING GATE RATHER THAN RECALLING IT. Run it, count its finding lines by the
gate's own headline AND by counting the lines, state both numbers, and say whether they agree —
`32-37` recorded that these two instruments disagreed by one and that the corpus had not moved.
Derive the list of files this round changed and state the overlap with the finding set as a number.

BUILD THE ROUND LEDGER AS ONE ROW PER INVENTORY ITEM. The inventory is derived: the round-3 gap, the
two advisories, the four anti-pattern rows, the eight fix-pass claims, the three Info findings, and
this round's own two source plans. Count the inventory, count the ledger rows, print both numbers, and
assert they are equal. An item may be OPEN; an item may never be ABSENT.

STATE THE PROBE ARITHMETIC AND MAKE IT BALANCE: the count of items authored into this round's plan
must-haves plus the count explicitly flagged unaddressed equals the count the inventory produced.

CLOSE WITH WHAT THIS DOCUMENT DOES NOT SAY. It decides no requirement checkbox, no phase status and no
verdict. Name the three things it did not measure, using the unmeasured phrasing.
  </action>

  <acceptance_criteria>
    - Every finding carries a severity, a requirement, a runnable reproduction with actual output, a measured blast-radius bound and a `created by a round-3 or fix-pass change?` verdict.
    - The document states the created-versus-inherited ratio as a number and sets it beside rounds 1, 2 and 3.
    - The gate sweep's row set is derived from `package.json` with the script count stated, and the row count is asserted against it with every deliberate omission named and reasoned.
    - The carried failing gate is re-measured with its headline count and its line count both stated, and the overlap with this round's changed file set stated as a number.
    - The round ledger states the inventory count and the row count as two printed numbers and asserts they are equal.
    - The probe arithmetic is stated and balances.
    - A closing section names what the document does not say and lists the unmeasured items.
    - No file outside `.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md` is modified by this plan.
  </acceptance_criteria>

  <verify>
    <automated>git status --porcelain -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md</automated>
    <fails_when>any output at all — this plan must leave the requirement, roadmap and state files untouched</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or any line naming a file under `.planning/phases/32-board-projector-cli-dashboard/`</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or a passed-case count below the number `32-39-GREEN-proof.txt` recorded, or any test file reported as failed</fails_when>
  </verify>

  <done>Every defect and every clean probe has a register row with a created-versus-inherited verdict, the ratio is a number beside three prior rounds, the gate sweep is derived from the manifest, and the ledger is asserted total against a stated inventory.</done>

  <reversibility rating="reversible">A report. It changes no status and no source.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an unverified fix commit → the round-4 verifier's evidence base | a fix report read as a closure is a claim promoted to a fact with nothing measuring the gap between them |
| a probe plant → a tracked file | a red-team that plants into the real tree can leave the tree changed, which would corrupt the artifact the verifier then measures |
| this report → the human at the round-4 decision checkpoint | a report that omits a clean probe or an inconvenient number is the input to a decision made on a partial picture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-40-01 | Repudiation | the eight unverified fix commits | high | mitigate | Every fix claim is re-run as its own probe with a transcript; no closure is recorded from reading a diff or a fix report |
| T-32-40-02 | Elevation of Privilege | the specifier partition's recovery fallback meeting the widened `bare` arm | high | mitigate | The interaction is probed as a named hypothesis with a measured verdict on whether a recovery miss refuses or skips, and on whether any input makes recovery miss |
| T-32-40-03 | Tampering | a probe plant left in a tracked file | high | mitigate | Every plant is restored and every plant row records the clean-tree check; the plan's own verify asserts a clean tree across source, install, hooks and the manifest |
| T-32-40-04 | Spoofing | a guard that stopped working read as a guard that refused | medium | mitigate | Every plant row records whether the write-detection premise case is among the failures, not only a failed-case count |
| T-32-40-05 | Repudiation | a ledger with an item missing rather than open | medium | mitigate | The inventory count and the row count are both printed and asserted equal; an item may be OPEN and never ABSENT |
| T-32-40-06 | Information Disclosure | the report naming repository paths and command output | low | accept | Every path named is a tracked file the operator already has; no secret, credential or host-specific value is recorded |

No package-manager install task exists in this plan — it installs nothing and the repository ships
zero runtime dependencies — so no `T-32-SC` supply-chain row and no package-legitimacy checkpoint
applies. ASVS level 1; all three `high` rows are dispositioned `mitigate`.
</threat_model>

## Artifacts this phase produces

Created by this plan:

| Artifact | Kind | Note |
|---|---|---|
| `.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md` | new file | the round-4 red-team report: premise, re-run reproductions, four questions per fix, the cross-fix interaction verdict, live write plants, the findings register with created-versus-inherited verdicts, the gate sweep, the round ledger and the probe arithmetic |
| `.planning/phases/32-board-projector-cli-dashboard/32-40-SUMMARY.md` | new file | plan summary |

This plan creates no symbol, renames nothing, and modifies no file under `scripts/`,
`agent-factory/`, `install/`, `hooks/` or `docs/`. Findings numbered here are continuous with this
phase's existing series across rounds 1 through 3.

<verification>
- `git diff --exit-code -- scripts/ agent-factory/ install/ hooks/ docs/ package.json` — exit 0 after
  every plant battery and at the end of the plan.
- `git status --porcelain -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` — no
  output; this plan decides no status.
- `npm run build && npm run typecheck && npm run check:build-parity` — exit 0 (premise).
- `npm run check:dashboard-readonly` — exit 0, 171 passed, unplanted.
- `node scripts/validate-agent-factory.js` and `node scripts/check-foundation-guards.js` — exit 0.
- `npm run check:nul-bytes` — exit 0; every control character in the report is escape TEXT.
- `npx vitest run --exclude '**/scripts/e2e/**'` — exit 0, passed-case count at or above what
  `32-39-GREEN-proof.txt` recorded.
- `npm test` is NOT run; the live claude-CLI end-to-end lane stays unmeasured this round.
</verification>

<success_criteria>
The round-4 verifier receives an evidence base in which every one of the eight unverified fix commits
has been re-measured rather than read, every recorded reproduction from three prior documents has been
re-run with its own transcript, the principal cross-fix interaction has a measured verdict, and every
finding — including the clean ones — carries a reproduction, a blast-radius bound and an explicit
verdict on whether the previous round's fix created it.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-40-SUMMARY.md` when done.
</output>

32-39-RED-baseline.txt — plan 32-39, round-4 gap closure of the three Info findings
(IN-01, IN-02, IN-03) that 32-REVIEW-FIX.md recorded as "not fixed — out of scope".

Measured on 2026-09-16 against HEAD 7aea94f0 ("docs(32-38): record plan 32-38 in STATE and
ROADMAP"), which is the tree plan 32-38 left behind. Every number below is the output of a
command run on that tree, not a recollection. Every search that reads a source file is run
with `grep -a`, which forces text mode: 32-REVIEW-FIX.md records that plain matching returned
a confident ZERO on a file a content classifier called program text, and two live fakes were
found only by forcing it.


=================================================================================================
§1  IN-01 — the presence discriminant is named for a fact its lookup never measured
=================================================================================================

§1.1  THE DEFECT, RESTATED AS THE OBSERVABLE IT RESTS ON

`presenceOf` (scripts/board-model.ts:1327-1328) answers:

    const admitted = populations.byId.get(id);
    if (admitted !== undefined) return { kind: "admitted-under-its-stem", record: admitted };

`byId` is keyed by the identifier a document DECLARES. The lookup therefore decides that some
admitted record declares THIS identifier, and decides nothing at all about the name the
directory gave that record. For `ABC-901.md` declaring `id: ABC-902` with a board row naming
`ABC-902`, the arm returns a kind spelled "under its stem" carrying a record whose stem is
`ABC-901`. The name asserts a fact the arm never measured.

Observable, run on the pre-change tree:

    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts -t "converse"
    scripts/board-model.test.ts:3118
      expect(presenceOf("ABC-902", populations).kind).toBe("admitted-under-its-stem")  PASSES
      with populations.byId.get("ABC-902").stem === "ABC-901"   (asserted at :3111 as a PREMISE)

    That is the whole finding in one line: the arm whose name says "its stem" is reached, and
    the record it carries has a different stem.


§1.2  THE SITE SET, DERIVED BEFORE ANY EDIT

    $ git ls-files -z | xargs -0 grep -ac "admitted-under-its-stem" | grep -v ':0$'

    IN SCOPE FOR THE RENAME (source, compiled output, test):
      scripts/board-model.ts                                          5
      scripts/board-model.js                                          4
      scripts/board-model.test.ts                                     9
                                                                     ---
      TOTAL IN SCOPE                                                 18

    CONTRACT AND KIT MARKDOWN:
      $ grep -rac "admitted-under-its-stem" agent-factory/            (no file matched)
      agent-factory/contracts/board.md                                0

    EXCLUDED BY DECISION — .planning/ audit records, which state what was true when they were
    written. This repository's standing rule is that such a record is annotated, never
    rewritten, so these are listed rather than changed:
      .planning/ROADMAP.md                                            1
      .planning/phases/32-.../32-23-ADVERSARIAL-REVIEW.md             1
      .planning/phases/32-.../32-33-GREEN-proof.txt                   1
      .planning/phases/32-.../32-33-PLAN.md                           3
      .planning/phases/32-.../32-33-SUMMARY.md                        2
      .planning/phases/32-.../32-37-ADVERSARIAL-REVIEW.md             2
      .planning/phases/32-.../32-38-GREEN-proof.txt                   3
      .planning/phases/32-.../32-38-PLAN.md                           1
      .planning/phases/32-.../32-38-RED-baseline.txt                  4
      .planning/phases/32-.../32-38-SUMMARY.md                        1
      .planning/phases/32-.../32-39-PLAN.md                           2
      .planning/phases/32-.../32-REVIEW-FIX.md                        1
      .planning/phases/32-.../32-REVIEW.md                            3

    Per-site line numbers, in scope (grep -an):
      scripts/board-model.ts       :1241 (array member) :1263 (union member) :1328 (construction)
                                   :1356 (docblock)     :1363 (switch case)
      scripts/board-model.js       :865 :908 :936 :943
      scripts/board-model.test.ts  :2354 (two-sided pin) :2398 :2400 :2401 (arm 1 case)
                                   :2440 :2441 (priority case) :2501 :2512 (sentence table)
                                   :3118 (32-38's converse probe)

    The plan's table, written on 2026-09-16, recorded 4 / 4 / 8. The measured counts are
    5 / 4 / 9: `grep -c` counts LINES and `grep -o | wc -l` counts OCCURRENCES, and the .ts
    docblock line (:1356) and the test case 32-38 added (:3118) are the two the plan's table
    did not carry. The derived numbers are used; the plan's recalled numbers are not.


§1.3  THE PUBLISHED SHAPE, BEFORE (the image the rename must not move)

    $ node scripts/board-dashboard.js scripts/fixtures/board-snapshot --once --json
      exit 0
      schemaVersion = 2
      conflicts     = 9
      readErrors    = 1

    Normalised the way scripts/board-model.test.ts's golden case normalises (wall-clock fields
    to 1970-01-01T00:00:00.000Z, the absolute fixture root to <fixture-root>, keys sorted,
    two-space indent, one trailing LF):

      normalizedBytes = 18606
      sha256          = 5c33782368eab1622c3b41c493955a28059d7a9f1abe4edc2c9de803ef00f6a1

    $ shasum -a 256 scripts/fixtures/board-snapshot/expected-snapshot.json
      5c33782368eab1622c3b41c493955a28059d7a9f1abe4edc2c9de803ef00f6a1

    The normalised CLI document and the committed golden are the SAME BYTES before the rename.
    That equality — not an eyeballed diff — is what §1 of the GREEN proof re-measures after.


§1.4  THE RED RUN — the target assertions fail for the planned reason

    The rename is a naming change, so RED is measured the only honest way: the expectations
    move first, against unchanged production code.

    $ sed -i '' 's/admitted-under-its-stem/admitted-under-this-id/g' scripts/board-model.test.ts
      (9 replacements; 0 occurrences of the old spelling remain in the test file)
    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts
      exit 1
      Test Files  1 failed (1)
      Tests       5 failed | 148 passed (153)

      FAIL  the presence kinds are a CLOSED set ... > has the expected MEMBERS, in the order the
            derivation asks them
      FAIL  `presenceOf` answers each of the four arms ... > (1) an identifier a document
            DECLARES answers `admitted-under-this-id`
      FAIL  `presenceOf` answers each of the four arms ... > the DECLARED identifier wins over a
            same-named file stem, and the order is observable
      FAIL  `presenceOf` answers each of the four arms ... > every arm produces its OWN sentence,
            and only `absent` asserts a negative
      FAIL  the converse of the arm the WR-01 fix touched (plan 32-38) > a contested identifier
            that is ITSELF a stem on disk still names the joined document

      Representative failure text:
        AssertionError: expected 'admitted-under-its-stem' to be 'admitted-under-this-id'
        AssertionError: an arm has no sentence case: expected
          Set{ 'admitted-under-this-id', ... } to deeply equal Set{ 'admitted-under-its-stem', ... }

    THIS IS AN INTENTIONAL RED, not a nonzero exit. Five TARGET assertions failed, each on the
    discriminant spelling the task exists to change; 148 unrelated cases in the same file stayed
    green; the file collected and ran (153 discovered, not 0); no syntax error, no fixture crash
    and no unrelated failure appears in the transcript.


=================================================================================================
§2  IN-02 — a prototype-spelled key never lands, so a conflict is resolved by disappearing
=================================================================================================

§2.1  THE TWO DEFECTS, AND WHY THEY ARE ONE CLASS

`configView` (scripts/board-read.ts:1148 before the fix) and `scrub`
(scripts/board-dashboard.ts:329 before the fix) both built an accumulator as a plain `{}` and
then assigned CONTENT-DERIVED KEYS into it:

    const limits: Record<string, number> = {};          // dial key  = a column name a writer chose
    const out: Record<string, unknown> = {};            // scrub key = a key of the document itself

`JSON.parse` creates an OWN `__proto__` property and `Object.entries` yields it, but
`limits["__proto__"] = 7` runs `Object.prototype`'s setter. The assignment returns normally, no
error is thrown, and NOTHING lands.


§2.2  THE DIAL, MEASURED LIVE ON A DISPOSABLE TREE

    Tree:  plans/board.md carries two headings — `## Ready (WIP 0/5)` and
           `## In Development (WIP 0/3)`. There is NO heading for `__proto__` and none for
           `In Review`.
    Dial:  agent-factory/config/factory.config.json, written as raw JSON text:

             {
               "mode": "lean",
               "cadence": "kanban",
               "id_prefix": "ABC",
               "wip_limits": {
                 "__proto__": 7,
                 "Ready": 5,
                 "In Development": 3,
                 "In Review": 2
               }
             }

    $ node scripts/board-dashboard.js <tree> --once --json
      exit 0

      THE FULL CONFLICT LIST THE DOCUMENT CARRIED — one entry, not zero, which is the point:

        conflicts (1):
          column-missing | column="In Review" | expected="a heading for the configured column In Review"

      published wipLimits own keys: ["Ready","In Development","In Review"]
      published wipLimits hasOwn __proto__: false

    THE EXPECTED CONFLICT THAT IS ABSENT:
      column-missing for column="__proto__". The dial names a limit for a column the board has no
      heading for, exactly as it does for `In Review`. `In Review` is reported. `__proto__` is not.
      The ordinary column in the same list is the control: the arm was reached and was reporting,
      so the missing entry is a measurement of THIS KEY and not of a switched-off arm.

    D-10 requires the projector to report every conflict and resolve none. This one is resolved by
    disappearing, which is the most silent resolution available.


§2.3  THE SERIALIZING CHOKEPOINT, MEASURED IN ISOLATION FROM THE DIAL

The two defects are chained: before the dial fix the key never reaches `scrub` at all, so the
chokepoint defect cannot be observed through the dial. It was isolated by fixing ONLY
`configView` and re-running the SAME tree:

    STAGE 1 — configView prototype-free, scrub still a plain `{}`:

    $ node scripts/board-dashboard.js <tree> --once --json
      exit 0
      conflicts (2):
        column-missing | column="__proto__"      <- the dial fix works
        column-missing | column="In Review"

      published wipLimits own keys: ["Ready","In Development","In Review"]
      published wipLimits hasOwn __proto__: FALSE      <- the chokepoint defect, alone

    The producer measured the key, raised the conflict for it, and then DROPPED THE KEY at the last
    point before stdout. A consumer parsing that document cannot tell the omission from an absence
    (DASH-07, D-18).


§2.4  THE RED RUNS — each new case watched failing against the pre-fix code

    THE DIAL CASES. `scripts/board-read.js:832` reverted from `Object.create(null)` to `{}` (the
    suite imports the committed `.js`), then:

    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/board-read.test.ts -t "IN-02"
      exit 1 — Tests 3 failed | 7 passed | 155 skipped (165)

      FAIL  raises `column-missing` for a prototype-spelled column with NO heading on the board
            expected [ 'In Review' ] to deeply equal [ 'In Review', '__proto__' ]
      FAIL  SIBLING ARM: raises NO `column-missing` when that heading IS on the board, and the
            limit is compared
            "the limit ... took no part in the claimed/counted/limit comparison": expected [] to
            deeply equal [ '7/5' ]
      FAIL  the published limit map carries the key as an OWN property a consumer's parse recovers
            expected false to be true

      The PREMISE case stayed GREEN, correctly: it measures `JSON.parse`, not the fix.
      Tree restored from a byte copy; `Object.create(null)` confirmed back at :832.

    THE CHOKEPOINT CASES. `scripts/board-dashboard.js:267` reverted the same way:

    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/board-dashboard.test.ts -t "IN-02"
      exit 1 — Tests 2 failed | 2 passed | 71 skipped (75)

      FAIL  a SPAWNED run's parsed document carries the prototype-spelled KEY as an own property
            expected false to be true
      FAIL  SIBLING ARM — the sanitizer still runs: a key that BECOMES prototype-spelled lands too
            expected false to be true

      The VALUE-path sibling stayed GREEN, correctly: a prototype-spelled string VALUE was never
      broken, and a case that redded there would have been measuring something else. Tree restored
      from a byte copy.

    EVERY ONE OF THESE IS AN INTENTIONAL RED: a TARGET assertion failing on the planned behaviour,
    with the file collecting and the unrelated cases in it green. No syntax error, no zero-test
    discovery, no fixture crash.


=================================================================================================
§3  IN-03 — the toolchain register is read raw, the way its sibling no longer is
=================================================================================================

§3.1  THE SITE SET, DERIVED BY PARSE — NOT TAKEN FROM THE PLAN'S THREE

The plan named three reads at :12110, :12348 and :12621 and said not to take that number on
trust. It was re-derived by a TypeScript parse of the file, collecting every ElementAccess and
PropertyAccess whose expression is the register identifier — a parse rather than a match,
because the file's own prose and test messages name the register in strings and comments and a
textual search cannot tell those from a read:

    SUBSCRIPT/PROPERTY READS OF TOOLCHAIN_CHECK_SCRIPTS: 3
      :12110  TOOLCHAIN_CHECK_SCRIPTS[name]                     (membership, classifyCheckScriptTargets)
      :12348  TOOLCHAIN_CHECK_SCRIPTS[r.name]                   (value, the toolchain-class case)
      :12621  TOOLCHAIN_CHECK_SCRIPTS["check:build-parity"]     (value, the classifier PREMISE case)
    ALL IDENTIFIER OCCURRENCES: 4, at lines 12061 (the declaration), 12110, 12348, 12621

    TOTAL READS = 3. The plan's three is CONFIRMED by measurement, at the three lines it named.

    (After this task's `import ts from "typescript"` the same three reads sit one line lower, at
    :12111, :12349 and :12622 — which is the pair of numbers the RED transcript in §3.3 prints.)


§3.2  THE DEFECT, AND THAT IT IS REACHABLE AT THE LOOKUP

`TOOLCHAIN_CHECK_SCRIPTS` is built with `Object.freeze({…})`, and freezing does not remove
`Object.prototype`. So the membership read

    if (TOOLCHAIN_CHECK_SCRIPTS[name] !== undefined) { … cls: "toolchain" … }

answers TRUE for `toString`, `valueOf`, `constructor` and `__proto__`. A check script spelled like
any of those would be classified `toolchain` — granted the "runs no gate module, for a reason
recorded here" exemption BY ITS SPELLING rather than by the decision the register exists to record.

`32-REVIEW-FIX.md` records the sibling site in `scripts/validate.test.ts` fixed with `Object.hasOwn`
at commit `7a3ae592`, and states plainly that the two copies of the rule now DISAGREE. It also
records that a first attempt to prove that class asserted a reachable exploit, PASSED against the
unfixed code, and had to be rewritten. This task does not repeat that: the case below exercises the
LOOKUP at a bare prototype-member name, which is reachable, and keeps the live-set prefix premise as
a SEPARATE assertion rather than conflating the two.


§3.3  THE RED RUN — the two target cases fail against the unchanged lookup

The new cases were written and run BEFORE the lookup was touched:

    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts -t "IN-03"
      exit 1 — Tests 2 failed | 7 passed | 291 skipped (300)

      FAIL  IN-03: every read of the toolchain register is own-property guarded, at a DERIVED site set
        AssertionError: a read of TOOLCHAIN_CHECK_SCRIPTS is not guarded by an own-property test …
        + [
        +   ":12111  TOOLCHAIN_CHECK_SCRIPTS[name]",
        +   ":12349  TOOLCHAIN_CHECK_SCRIPTS[r.name]",
        +   ":12622  TOOLCHAIN_CHECK_SCRIPTS[\"check:build-parity\"]",
        + ]

      FAIL  IN-03: a bare prototype-member name is NOT registered, and a recorded one still is
        AssertionError: a check script named __proto__ was classified TOOLCHAIN — granted the
        "runs no gate module for a recorded reason" exemption by its SPELLING …
        + [ { "cls": "toolchain", "name": "__proto__", "target": "__proto__" } ]

      The THIRD new case — the prefix premise — stayed GREEN, correctly: it is a fact about the
      register's keys and the live scan set, not about the fix, and a premise that redded here
      would have meant something else was wrong.

    INTENTIONAL RED on both counts: a target assertion failing on the planned behaviour, the file
    collecting 300 cases (not zero), 7 unrelated cases in the same `-t` selection green, no syntax
    error and no fixture crash.


§3.4  THE LIVE RESULT SET, BEFORE

Captured with a temporary probe that dumps `liveTargetRows()` with each row's reachability answer.
The probe was removed and the file restored before any change was made.

    toolchain       | check:build-parity        -> check:build-parity                  | ciNamesScript=true
    gate-module     | check:public-docs         -> scripts/check-public-docs-vocabulary.js | ciNamesScript=false
    gate-module     | check:audit-register      -> scripts/check-audit-register.js      | ciNamesScript=false
    gate-module     | check:residual-citations  -> scripts/check-residual-citations.js  | ciNamesScript=false
    gate-module     | check:claim-anchors       -> scripts/check-claim-anchors.js       | ciNamesScript=false
    gate-module     | check:banned-claims       -> scripts/check-banned-claims.js       | ciNamesScript=false
    gate-module     | check:imperative-lexicon  -> scripts/check-imperative-lexicon.js  | ciNamesScript=false
    gate-module     | check:diff-disposition    -> scripts/check-diff-disposition.js    | ciNamesScript=false
    gate-module     | check:nul-bytes           -> scripts/check-nul-bytes.js           | ciNamesScript=false
    gate-module     | check:platform-shapes     -> scripts/check-platform-shapes.js     | ciNamesScript=false
    suite-test-file | check:dashboard-readonly  -> scripts/board-readonly.test.ts       | ciNamesScript=false
    total=11

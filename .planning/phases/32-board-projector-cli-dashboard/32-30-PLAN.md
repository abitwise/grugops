---
phase: 32-board-projector-cli-dashboard
plan: 30
type: execute
wave: 7
gap_closure: true
depends_on: ["32-29"]
files_modified:
  - .planning/phases/32-board-projector-cli-dashboard/32-30-ADVERSARIAL-REVIEW.md
  - .planning/phases/32-board-projector-cli-dashboard/deferred-items.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07, DASH-08]
estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The harness asserts its own premise before it reports anything: the build is a faithful build of its sources and the committed outputs match a rebuild, so every measurement below is about the artifact a host runs rather than about a `.ts` nobody ships."
    - "Every recorded reproduction from `32-VERIFICATION.md`, `32-REVIEW.md` and `32-23-ADVERSARIAL-REVIEW.md` — including the ones both documents record as CLOSED — is re-run against the rebuilt committed `.js` in this round, and each carries a measured result rather than a description."
    - "Every refusal branch this round added is asked two questions and both answers are written down: how is it REACHED, and what does the arm NEXT to it do with the same input."
    - "The round ledger has one row per inventory item, the inventory count and the row count are both stated out loud, and they are equal — an item may be OPEN but never ABSENT (DASH-01 through DASH-08)."
    - "The probe-report arithmetic is stated and balances: the count of edge items authored into plan must-haves plus the count explicitly flagged as unaddressed equals the count taken from the report."
    - "Both representations of the residual ledger agree after this round's rows are appended, asserted by comparing every row identifier in one against the other."
    - { statement: "The live claude-CLI end-to-end lane was not run in this round and its state is UNKNOWN - verify, as in every prior round of this phase", verification: backstop }
  prohibitions:
    - "This plan decides no requirement checkbox, no phase status and no verification verdict — the verifier does, from this evidence, in a separate pass."
    - "Never fix a source file here; a closure found at the end of a round, in a plan that owns no source, is how the previous round's first finding was created."
    - "Never absorb the carried `check:diff-disposition` failure into this round; re-measure it and carry it with the measurement."
    - "Never record a reproduction as closed on the strength of reading a diff; re-run the original finding's own probe."
    - "Canon security items (secret handling, authn/authz, transport) are dropped here with this breadcrumb: this plan runs measurements and writes reports."
  artifacts:
    - "32-30-ADVERSARIAL-REVIEW.md — premise, re-run transcripts, reachability and sibling-arm answers per new branch, the gate sweep, the round ledger and the probe arithmetic"
    - ".planning/phases/32-board-projector-cli-dashboard/deferred-items.md — the carried items re-measured and left carried"
    - ".planning/WINDOWS.md — this round's residual rows appended and both representations reconciled"
  key_links:
    - "the premise check -> every transcript below it: a measurement over an artifact that is not a build of its sources measures nothing"
    - "the 13-item inventory -> the ledger's row count: both numbers stated, both equal, an OPEN row allowed and an ABSENT row not"
    - "each new refusal branch -> its reachability paragraph and its sibling-arm row: the two questions this project's history says decide whether a round converged"
    - "this report -> the verifier: the report supplies evidence and changes no status line"
  deferred: []
---

<objective>
End gap-closure round 3 the way rounds 1 and 2 ended: with an adversarial self-review that re-measures
everything, asks the two questions this project's recorded history says decide whether a round
converged, and keeps a ledger asserted total against the round's own inventory.

**Round position: this is round 3 of a hard cap of 4.** One round remains. That budget is what makes
recording an open finding safe rather than a failure, and it is why every finding this plan records
must carry a reproduction somebody else can run rather than a description.

**This plan does not decide phase completion.** It changes neither `.planning/REQUIREMENTS.md` nor the
Phase 32 status line nor any DASH-0x checkbox. This repository has a recorded incident of a roadmap
update flipping a phase to Complete before verification ran, which then had to be reverted by hand.

The round's inventory, taken from `32-REVIEW.md`'s own finding totals (3 critical, 8 warning, 2 info)
which the verifier's gaps, advisory and `reason_advisory` all map onto:

```
3 + 8 + 2 = 13 inventory items
```

plus two CARRIED items that are deliberately outside that count: the pre-existing
`check:diff-disposition` failure on five Phase-31 workflow documents, and the live claude-CLI
end-to-end lane whose state is `UNKNOWN - verify`.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` and no `## Prohibitions`
section to satisfy.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-14-ADVERSARIAL-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/deferred-items.md
</context>

## Flagged assumptions — probe-report items this round does NOT address

The orchestrator's deterministic edge probe produced **12 items** over DASH-01 through DASH-08, every
one `unresolved`. This is gap-closure round 3, so the phase's original plans `32-01` through `32-08`
already carry the phase's edge coverage; this round re-derives only the categories touching the three
failed truths. The arithmetic, stated so no item is silently dropped:

**Authored (6)** — lifted into `must_haves.truths` as concrete acceptance criteria by the plan named:

| Requirement | Category | Authored in |
|---|---|---|
| DASH-03 | empty-or-absent | `32-26` — an entry named exactly `.md` is a listed entry with an empty stem, refused by name rather than vanishing from the partition |
| DASH-03 | conflict | `32-26` — two sources disagreeing about one identifier produce a sentence naming what the file declares, never an assertion of absence |
| DASH-04 | timing | `32-27` — a watch failure recorded before a containment refusal does not survive it |
| DASH-05 | empty-or-absent | `32-27` — an ordinary readable sibling directory beside a refused entry stays armed; an absent directory carries no record |
| DASH-05 | malformed-input | `32-27` — a context entry outside the ported allow-list is reported by name and the listing count reconciles |
| DASH-06 | boundary | `32-24` — every module specifier lands in exactly one of three buckets and the third is refused by construction |

**Flagged as unaddressed (6)** — each is an explicit assumption of this round, not an oversight:

| Requirement | Category | Why this round does not re-derive it |
|---|---|---|
| DASH-01 | boundary | The column-heading and row grammars' boundary behaviour is carried by plans `32-01`, `32-02` and `32-04` and by the parse-oracle fuzz corpus; no finding in this round's 13-item inventory touches it. |
| DASH-01 | empty-or-absent | Same: an empty board and an absent board are covered by `32-02`'s total line partition and `32-03`'s absent-is-legitimate rule (D-13). |
| DASH-02 | malformed-input | Carried by the seven-axis parse oracle and the 141-row corpus in `32-04`; no round-3 finding reaches the grammar. |
| DASH-02 | empty-or-absent | Same corpus and the non-vacuity premise cases it carries. |
| DASH-07 | empty-or-absent | The non-TTY, single-frame and document modes over an empty tree are carried by `32-07` and `32-13`; round 3 touches only the control-character behaviour of the document channel. |
| DASH-08 | boundary | The bounds and truncation behaviour (D-20) and the dependency-free and socket-free claims are carried by `32-02`, `32-06` and `32-08`; round 3 changes neither. |

```
authored 6 + flagged 6 = 12 = taken 12
```

## Artifacts this phase produces

- `32-30-ADVERSARIAL-REVIEW.md` with, in order: the premise assertion; the re-run of every recorded
  reproduction from all three prior documents; the behavioural spot-check table re-run verbatim
  including the rows that passed; a reachability answer and a sibling-arm probe per new refusal
  branch; the neighbour-variation pass; the findings, each OPEN with a reproduction; the gate sweep
  derived from the manifest rather than recalled; the round ledger asserted total; the probe-item
  arithmetic; the created-versus-inherited ratio stated as a number; and a closing section naming what
  the document does NOT say.
- Re-measured entries in `deferred-items.md` for both carried items.
- This round's residual rows appended to `.planning/WINDOWS.md` through the tool that maintains both
  of its representations, with the reconciliation asserted.

<tasks>

<task type="auto">
  <name>Task 1: Assert the harness's own premise, then re-run every recorded reproduction</name>

  <read_first>
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` — the "Independent
      Reproduction Transcripts" section and the "Behavioral Spot-Checks" table, every row of which is
      re-run here
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` — the three critical issues, the
      eight warnings, the two infos, and the prior-findings disposition table whose closures are
      re-checked rather than assumed
    - `.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md` §§ 0-2 — the
      premise pattern, the re-run pattern and the spot-check pattern this task copies
    - `.planning/phases/32-board-projector-cli-dashboard/32-24-GREEN-proof.txt` through
      `32-29-GREEN-proof.txt` — this round's own proofs, which are LEADS, not evidence: every row is
      re-measured here independently
    - `.planning/phases/32-board-projector-cli-dashboard/32-14-ADVERSARIAL-REVIEW.md` § 9 — what a
      re-verification should check first, and why the order matters
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-30-ADVERSARIAL-REVIEW.md</files>

  <action>
ASSERT THE PREMISE BEFORE ANYTHING ELSE, AND PRINT IT. Run the build, the type check, the build-parity
check and the freshness check, and record their outputs. If the committed outputs are not a faithful
build of their sources, every measurement below is about an artifact nobody ships and the document
must say so at the top rather than in a footnote. This project has a recorded instance of a
verification harness producing a false result because its own premise was never asserted.

RE-RUN EVERY RECORDED REPRODUCTION, INCLUDING THE CLOSED ONES. Build the list from all three prior
documents — the verifier's three gaps and its advisory and `reason_advisory`; the reviewer's three
criticals, eight warnings and two infos; the round-2 self-review's five findings — and re-run each
against the REBUILT committed `.js`, on disposable copies outside the repository root. A review that
only re-checks failures cannot find a regression, so the rows both documents record as CLOSED are
re-run too: the namespace-destructure bypass, the permission-denied and torn-byte read paths, the
symlink escape, the grammar-refusal sentence, the upper-range control characters in the document
channel, the builtin-identity allow-list, the byte-order-mark and tab refusals, the walk bound's sort,
the duplicate identifier, the routing census's universe, and the queue reader's named skips.

RE-RUN THE BEHAVIOURAL SPOT-CHECK TABLE VERBATIM. `32-VERIFICATION.md` carries a table of spot-checks
with a recorded result each. Re-run every row and record the new result beside the old one, including
the six that passed. A row whose result changed in either direction is the finding.

RESTORE AND PROVE CLEAN AFTER EVERY PROBE that touches a tracked file, and record the clean check in
the transcript rather than asserting it once at the end.

WRITE THE SECTIONS AS MEASUREMENT, NOT AS NARRATIVE. Each row carries the command, the measured
output and the verdict. Where a probe cannot be run, record `UNKNOWN - verify` and the reason; never
fabricate a result, and never record a closure on the strength of reading a diff.
  </action>

  <acceptance_criteria>
    - `32-30-ADVERSARIAL-REVIEW.md` opens with the premise check and its recorded outputs.
    - The document carries one row per recorded reproduction from all three prior documents, each with the command, the measured output and a verdict.
    - The document re-runs every row of the verifier's behavioural spot-check table and prints the old and new results side by side.
    - Every probe touching a tracked file records a clean working-tree check after restoration.
    - Any unrunnable probe is recorded `UNKNOWN - verify` with the reason.
  </acceptance_criteria>

  <verify>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or the freshness output names any committed .js as not matching a rebuild of its source — the premise fails and the report must say so before any transcript</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or a passed-case count below the 4974 recorded in 32-VERIFICATION.md — the round lost cases somewhere between plan 32-24 and here</fails_when>
    <automated>git diff --exit-code -- scripts/ agent-factory/ docs/</automated>
    <fails_when>non-zero exit — a probe was left in a tracked file</fails_when>
  </verify>

  <done>The premise is asserted and printed, and every recorded reproduction from every prior document has a re-measured verdict against the artifact a host runs.</done>

  <reversibility rating="reversible">Measurements written to a report; this task owns no source file.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Ask the two questions per new refusal branch, and run the neighbour-variation pass</name>

  <read_first>
    - `scripts/js-import-closure.ts`, `scripts/board-readonly.test.ts`, `scripts/board-model.ts`,
      `scripts/board-read.ts`, `scripts/board-dashboard.ts`, `scripts/validate.test.ts` and
      `scripts/check-foundation-guards.test.ts` — every refusal branch this round added, which is the
      subject of this task's table
    - `.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md` §§ 3-4 — the
      reachability format and the six neighbour-variation questions, which this task asks again of
      this round's branches
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` "Why the score went down"
      — the three-sentence statement of the failure class this round set out to close, which is the
      hypothesis this task is trying to falsify
    - `package.json` — the real command set, from which the gate sweep's row set is derived
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-30-ADVERSARIAL-REVIEW.md</files>

  <action>
ENUMERATE THIS ROUND'S NEW REFUSAL BRANCHES FROM THE SOURCE, NOT FROM THE PLANS. Walk the six changed
files and list every branch that can now refuse, degrade or report where it previously did not. The
list derived from the code is the denominator; a branch a plan promised but the code does not carry is
the first thing this pass should find.

ASK EACH BRANCH TWO QUESTIONS AND WRITE BOTH ANSWERS:

  1. **How is it REACHED?** Which call site evaluates it, on what input, under which command. A branch
     nothing reaches is a branch that cannot refuse, and this project's recorded lesson is that asking
     only what a predicate refuses leaves the positions it is never ASKED at unexamined.
  2. **What does the arm NEXT TO IT do with the same input?** Five consecutive rounds across this
     project's phases were "correct in the arm it was written for and wrong in the arm beside it".

RUN THE NEIGHBOUR-VARIATION PASS, and state the hypothesis it is trying to falsify: that this round's
four canonical-form cutovers did NOT reopen the same class one register over. For each cutover, ask at
minimum:

  - the specifier partition: is there a position a specifier enters that `moduleSpecifiers` does not
    read — a re-export chain, a subshell-built command, a source the walk reaches at depth
  - the member-path position rule: is there a consumption position not in the pinned row set, and does
    the binding map survive an alias created by a shape it does not track
  - the presence derivation: is there a fifth ticket population — a listed entry that is neither
    admitted nor refused — and does the arithmetic totality assertion actually see it
  - the containment granularity: is there a path shape where the ancestor walk and the authority's own
    spelling disagree, and what happens when two refusals arrive in one read
  - the two censuses: what is the input each one's set is ASSEMBLED from, and what bounds it

DERIVE THE GATE SWEEP FROM THE MANIFEST. Read every check and freshness command out of `package.json`,
run each, and record the exit code and the headline output. The row set is derived rather than
recalled, because a recalled row set is the set-literal drift class this repository has already paid
for twice.

RECORD EVERY FINDING OPEN, WITH A REPRODUCTION. Nothing is fixed in this plan: this plan owns no
source file, and a closure applied at the end of a round in a plan that does not own the file is
precisely how the previous round's first finding was made. Each finding carries a severity, the file,
whether it was created by this round's own fixes, and a reproduction somebody else can run.

STATE THE CREATED-VERSUS-INHERITED RATIO AS A NUMBER, beside round 1's (1 of 3), round 2's (4 of 5)
and Phase 31's (8 of 8 for four consecutive rounds). That ratio is the single number that says whether
the canonical-form cutover worked, and it is the number round 4's decision will be taken against.
  </action>

  <acceptance_criteria>
    - The document carries a branch table derived from the six changed source files, with a reachability answer and a sibling-arm answer per row.
    - The document carries the neighbour-variation pass with at least the five cutover questions above, each with a measured answer.
    - The document carries a gate sweep whose row set is derived from the manifest, with an exit code and headline output per row.
    - Every finding is recorded OPEN with a severity, a file, a created-or-inherited attribution and a runnable reproduction.
    - No file under `scripts/`, `agent-factory/` or `docs/` is modified by this task.
    - The created-versus-inherited ratio is stated as a number beside the two prior rounds' and Phase 31's.
  </acceptance_criteria>

  <verify>
    <automated>git diff --exit-code -- scripts/ agent-factory/ docs/ package.json</automated>
    <fails_when>non-zero exit — this plan owns no source file and a fix applied here is the failure mode it exists to avoid</fails_when>
    <automated>npm run check:dashboard-readonly &amp;&amp; node scripts/validate-agent-factory.js &amp;&amp; node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit from any of the three — a gate this round touched is red at the end of the round and the sweep must record which</fails_when>
  </verify>

  <done>Every new refusal branch has a written reachability answer and a sibling-arm answer, the neighbour-variation hypothesis has been tested rather than assumed, and the ratio is a number.</done>

  <reversibility rating="reversible">Measurements written to a report.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The round ledger asserted total, the carried items re-measured, and both residual representations reconciled</name>

  <read_first>
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` frontmatter `findings:` — the
      3 / 8 / 2 / 13 totals that are this round's inventory
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` frontmatter — the `gaps:`
      block, the `advisory:` block and the `reason_advisory` field, each of which maps onto one of
      those 13 rows
    - `.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md` § 7 — the ledger
      format, the out-loud count assertion and the reconciliation block this task copies
    - `.planning/phases/32-board-projector-cli-dashboard/deferred-items.md` — both carried entries and
      the format they use
    - `.planning/WINDOWS.md` — both representations of the residual ledger, and the round-2 rows
      (187 through 191) this round's rows are appended after
    - This plan's own `## Flagged assumptions` section — the probe arithmetic to restate and assert
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-30-ADVERSARIAL-REVIEW.md, .planning/phases/32-board-projector-cli-dashboard/deferred-items.md, .planning/WINDOWS.md</files>

  <action>
BUILD THE LEDGER WITH ONE ROW PER INVENTORY ITEM AND ASSERT THE TWO COUNTS ALOUD. The inventory is
`32-REVIEW.md`'s own totals — three critical, eight warning, two info — which the verifier's three
gaps, its advisory and its `reason_advisory` all map onto. Write the addition out (3 + 8 + 2 = 13),
write the table, count its rows, and state both numbers in the document. They must be equal. An item
with no row is the failure this table exists to prevent; an item may be OPEN but never ABSENT. Where
one inventory item legitimately appears twice — as a verifier advisory and as a reviewer warning —
name the duplication rather than silently collapsing it, and give the two rows their own dispositions.

Each row carries: the inventory item, the plan that took it, the evidence measured in THIS round
(a transcript reference from Task 1 or Task 2, never a plan's own claim), and a disposition.

CARRY THE TWO CARRIED ITEMS OUTSIDE THE TABLE, AND RE-MEASURE BOTH. Run the disposition gate and
record its exit code, its finding count and the file set it names, then compare that file set against
every file this round changed and record the overlap count. If the overlap is zero the item is
pre-existing and unchanged, and it is carried with that measurement rather than absorbed. Record the
live end-to-end lane as not run, with the reason and the `UNKNOWN - verify` marker, exactly as every
prior round of this phase did. Update `deferred-items.md` with both measurements, leaving both entries
carried.

RESTATE AND ASSERT THE PROBE ARITHMETIC. Reproduce this plan's authored-and-flagged tables in the
report, state the addition, and assert it equals the count the orchestrator's probe produced. A count
that does not balance means an edge item was dropped in planning, which is a finding about the round
rather than about the code.

APPEND THIS ROUND'S RESIDUAL ROWS AND RECONCILE BOTH REPRESENTATIONS. `.planning/WINDOWS.md` carries
the same ledger twice, and a ledger whose halves disagree is worse than one half. Write through the
tool that maintains both. Append a row per finding this round leaves open, and move any round-2 row
this round closed — rows 187 through 191 are round 2's open findings and each of them is answered by
one of this round's plans, so each needs a measured verdict rather than an assumed one. Then assert
the reconciliation by comparing every row identifier in one representation against the other and
printing the three numbers: identifiers compared, rows in each half, disagreements.

CLOSE WITH WHAT THE DOCUMENT DOES NOT SAY. State plainly that it does not claim DASH-01 through
DASH-08 are satisfied, that it changes neither the requirements file nor the phase status line nor any
checkbox, and that the verifier decides that from this evidence in a separate pass. State the round
position — round 3 of 4 — and what a round 4 would have to do differently if the ratio did not move.
  </action>

  <acceptance_criteria>
    - The report carries a ledger table with one row per inventory item, each naming the plan that took it, this round's own measured evidence and a disposition.
    - The report states the inventory count and the row count as two numbers and they are equal.
    - The report carries the two carried items outside the table, each with a re-measurement, and `deferred-items.md` reflects both.
    - The report restates the probe arithmetic and asserts it balances against the count taken.
    - `.planning/WINDOWS.md` carries this round's rows in both representations, and the report prints the identifiers-compared, rows-per-half and disagreements numbers.
    - The report's closing section states that it decides no checkbox, no status line and no verdict, and names the round position.
  </acceptance_criteria>

  <verify>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query frontmatter.validate .planning/phases/32-board-projector-cli-dashboard/32-30-PLAN.md --schema plan-gap-closure</automated>
    <fails_when>the printed JSON carries "valid": false, or a non-empty "missing" or "invalidValue" array — this plan's own frontmatter does not satisfy the gap-closure schema it claims</fails_when>
    <automated>grep -c 'UNKNOWN - verify' .planning/phases/32-board-projector-cli-dashboard/32-30-ADVERSARIAL-REVIEW.md</automated>
    <fails_when>a printed count of 0 — the live end-to-end lane's unmeasured state was not recorded, which is the fabrication this project's rule forbids</fails_when>
    <automated>git diff --exit-code -- scripts/ agent-factory/ docs/ .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <fails_when>non-zero exit — this plan modified a source file, the requirements file or the roadmap, any of which would be deciding a verdict that belongs to the verifier</fails_when>
  </verify>

  <done>The ledger is total against a stated inventory, both carried items are re-measured and still carried, the probe arithmetic balances, and both residual representations agree.</done>

  <reversibility rating="reversible">Reports and ledgers; no source file and no status line moves.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this round's own proofs → this report | a plan's own GREEN proof is a lead, not evidence; the boundary is crossed by re-measuring independently |
| this report → the verifier and the requirements file | the report supplies evidence and must not cross into deciding a verdict; the crossing is prevented mechanically by this plan owning no status-bearing file |
| `.planning/WINDOWS.md`'s two representations | a ledger whose halves disagree rots silently, so the crossing is asserted by identifier comparison rather than by careful editing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-30-01 | Repudiation | a closure recorded from reading a diff | high | mitigate | Every reproduction is re-run with the original finding's own probe against the rebuilt committed `.js`, and every row carries the command and the output |
| T-32-30-02 | Spoofing | a harness whose own premise is false | high | mitigate | The build-parity and freshness checks run first and are printed; this project has a recorded instance of a false verification result from an unasserted premise |
| T-32-30-03 | Tampering | a status line flipped before verification | high | mitigate | This plan owns no requirements, roadmap or source file, and a verify command asserts none of them moved |
| T-32-30-04 | Repudiation | an inventory item with no ledger row | medium | mitigate | Both counts stated aloud and asserted equal; a duplicated item is named rather than collapsed |
| T-32-30-05 | Repudiation | the unmeasured end-to-end lane reported as passing | medium | mitigate | Recorded `UNKNOWN - verify` with the reason, and a verify command asserts the marker is present |

No package-manager install task exists in this plan — the repository ships zero runtime dependencies —
so no `T-32-SC` supply-chain row and no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness` — exit 0,
  printed at the top of the report.
- `npx vitest run --exclude '**/scripts/e2e/**'` — exit 0, passed-case count at or above 4974.
- `npm run check:dashboard-readonly`, `node scripts/validate-agent-factory.js` and
  `node scripts/check-foundation-guards.js` — exit 0.
- `git diff --exit-code -- scripts/ agent-factory/ docs/ .planning/REQUIREMENTS.md .planning/ROADMAP.md`
  — clean.
- The report's ledger row count equals its stated inventory count; the probe arithmetic balances; the
  residual reconciliation prints zero disagreements.
</verification>

<success_criteria>
Round 3 ends with every prior reproduction re-measured against the artifact a host runs, every new
refusal branch carrying a written reachability and sibling-arm answer, a ledger asserted total against
a stated inventory, both carried items re-measured and still carried, a balanced probe arithmetic, two
agreeing residual representations, and a created-versus-inherited ratio stated as a number — with no
checkbox, status line or verdict touched.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-30-SUMMARY.md` when done, and
`32-30-ADVERSARIAL-REVIEW.md` as the report itself.
</output>

---
phase: 32-board-projector-cli-dashboard
plan: 19
type: execute
wave: 5
gap_closure: true
depends_on: ["32-15", "32-16", "32-17", "32-18"]
files_modified:
  - scripts/board-dashboard.ts
  - scripts/board-dashboard.js
  - scripts/board-watch.test.ts
  - scripts/board-dashboard.test.ts
  - agent-factory/contracts/board.md
autonomous: true
requirements: [DASH-04, DASH-05]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A directory whose watch fails repeatedly contributes one current record, not one per poll tick, and the record disappears when the watch is successfully re-armed."
    - "The watched directory set is derived from the two on-disk-layout authorities the reader already owns, and the assertion over it is a relationship — every watched source has a watched ancestor — not a second hand-typed list."
    - "A watch is armed against the same resolved root every read is resolved against, so a symlinked source directory is refused by one rule rather than watched by one module and refused by another."
  artifacts:
    - "scripts/board-dashboard.ts (+ committed .js) — the keyed watch-error map, the derived watched set, and the resolved-root arming"
    - "scripts/board-watch.test.ts — the accumulation case, the clear-on-rearm case, the derivation-relationship cases, and the resolved-root case"
    - "agent-factory/contracts/board.md — the sentence covering what the watch arm resolves"
  key_links:
    - "FIXED_SUBPATHS and QUEUE_STAGES -> the derived watched set: the layout is stated once and the dashboard reads it rather than restating it"
    - "readSnapshot's resolved root -> seed -> arm: the containment authority that decides every read also decides every watch"
    - "watchErrors map -> emit -> the frame and the published document: one entry per directory, current rather than cumulative"
  deferred: []
---

<objective>
Close the three watch-arm findings: an unbounded error list, a third hand-typed spelling of the
on-disk layout, and a watch armed against a path the containment authority never saw.

Gap mapping for this plan (from `32-REVIEW.md`):

| Finding | What is wrong | Task |
|---------|---------------|------|
| WR-06 (prior WR-02, still open) | `noteWatchError` pushes onto an append-only array and `arm`'s success path never removes anything, while `start`'s tick calls `armAll` unconditionally. A directory whose watch throws persistently adds one entry per poll tick forever, `emit` prints every accumulated entry on every frame and embeds them all in the published document as current read errors, and after a successful re-arm the frame keeps saying the watch will be re-armed. | Task 1 |
| WR-07 (prior WR-03, still open) | `WATCH_DIRS` is six directories typed by hand, asserted against a second hand-typed list in the test, connected to `FIXED_SUBPATHS` and `QUEUE_STAGES` by nothing. If a subpath or a stage name moves, the dashboard silently stops watching — and the mandatory poll hides it, so every gate stays green. | Task 2 |
| IN-01 | `arm` joins the raw argv root while `readSnapshot` resolves through the containment authority. A symlinked source directory has a watch handle armed on it even though every read of it is refused, and the contract states the rule as a property of every path the projector reads, so the exemption is written down nowhere. | Task 3 |

Purpose: DASH-04's claim is a live view kept current by a watch with a mandatory poll beneath it. Two
of these findings make the watch's own failures unreadable, and one makes the poll the thing that
hides a watch that is not watching. The third is the containment rule plan 32-10 established, asked in
one module and not in its sibling.

Output: one current record per directory, a watched set derived from the layout authorities, and one
resolved root for reads and watches alike.

This plan runs after 32-18 because both edit `scripts/board-dashboard.ts`, and after the reader plans
because the resolved root it arms against comes out of the reader's result.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` and no `## Prohibitions`
section to satisfy.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-03-SUMMARY.md
@.planning/phases/32-board-projector-cli-dashboard/32-10-SUMMARY.md
@agent-factory/contracts/board.md
</context>

## Artifacts this phase produces

- `watchErrors: Map<string, ReadError>` replacing the append-only `errors` array inside `createLoop`,
  with `noteWatchError` setting by directory and `arm`'s success path deleting by directory.
- `deriveWatchDirs()` in `scripts/board-dashboard.ts` — the watched set computed from
  `SOURCE_NAMES`, `FIXED_SUBPATHS` and `QUEUE_STAGES` imported from `./board-read.js`. `WATCH_DIRS`
  becomes its result; `WATCH_DIR_COUNT` stays as the two-sided pin.
- `resolvedRoot` inside `createLoop`, captured from the seeded snapshot's own `repoRoot` and refreshed
  on every read; `arm` joins that instead of `options.repoRoot`.
- New cases in `scripts/board-watch.test.ts`: the ten-tick accumulation case, the clear-on-rearm case,
  the derivation-relationship cases, and the symlinked-root case.
- One sentence in `agent-factory/contracts/board.md` § containment: what the watch arm resolves.
- A rebuilt committed `scripts/board-dashboard.js`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One current watch record per directory, cleared when the watch comes back</name>

  <read_first>
    - `scripts/board-dashboard.ts` lines 690-760 — `createLoop`'s `errors` array, `noteWatchError`,
      `closeWatcher`, `arm` and its success path, `armAll`
    - `scripts/board-dashboard.ts` lines 775-800 — `emit`, where the accumulated entries are appended
      to the result's read errors and printed
    - `scripts/board-dashboard.ts` lines 840-880 — `start`'s tick, `stop`, and the `watchErrors()`
      accessor on the `Loop` interface
    - `scripts/board-watch.test.ts` lines 368-412 — the existing forced-error cases, the seam they use,
      and what they already assert
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` WR-06 — the per-tick arithmetic
      and the sentence that stops being true after a re-arm
  </read_first>

  <files>scripts/board-dashboard.ts, scripts/board-watch.test.ts</files>

  <action>
RED FIRST, WITH A NUMBER. Using the forced-error seam and the existing fake-timer harness, drive ten
poll ticks against a directory whose watch throws every time, and assert the loop reports exactly ONE
watch error for that directory. It fails today with ten. Then add the second failing case: force one
error, let the next tick re-arm successfully, and assert the loop reports zero watch errors afterwards.

THEN THE KEYED MAP. Replace the array with a `Map` keyed by the watched directory's relative name.
`noteWatchError` sets; `arm`'s success path deletes the key it just armed. `emit` reads the map's
values in a stable order — sort by the relative name, so two frames of the same state are the same
bytes and a consumer diffing two documents sees a changed finding rather than a reshuffle.

ASK THE THIRD QUESTION THE TWO CASES DO NOT: what happens when the SAME directory fails for a
DIFFERENT reason. Assert that the second failure replaces the first rather than being dropped, and
that the message names the current reason. A map keyed by directory answers this correctly by
construction; the case is what keeps a future edit from keying it by reason instead.

MAKE THE MESSAGE TRUE AT THE TIME IT IS READ. The current text promises a re-arm on the next poll
tick, which is true when the entry is written and false once the entry survives a successful re-arm.
With the entry now cleared on re-arm, the promise holds — say so in the code comment, naming the
clearing as what makes the sentence true rather than leaving the two facts in different functions.

CHECK THE `stop` PATH. `stop` closes every watcher; assert it does NOT leave a stale error entry
behind that a later `emit` would print, and state in the test what the intended state after `stop` is.
  </action>

  <behavior>
    - Given ten poll ticks with a persistently failing watch on one directory, the loop reports exactly one watch error for it.
    - Given a failure followed by a successful re-arm, the loop reports zero watch errors.
    - Given two different failures on one directory, the loop reports one entry carrying the later reason.
    - Given two directories failing, the loop reports two entries in a stable, name-sorted order.
    - Given a call to `stop`, the loop's reported watch errors match the state the test states, and `emit` after `stop` prints nothing stale.
  </behavior>

  <acceptance_criteria>
    - `createLoop` holds a keyed map, not an array; `arm`'s success path deletes the key.
    - The ten-tick case asserts exactly one entry, by number.
    - The clear-on-rearm case asserts zero entries after the successful arm.
    - `emit` reads the entries in name-sorted order and the order is asserted with two failing directories.
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-watch.test.ts` passes with a higher case count than the committed run.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-watch.test.ts</automated>
    <fails_when>non-zero exit, or any case reports failed, or the ten-tick case is reported as skipped</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-dashboard.test.ts</automated>
    <fails_when>non-zero exit — the emitted-document shape is asserted there and the watch entries ride in it</fails_when>
  </verify>

  <done>A failing watch contributes one current record; a recovered watch contributes none.</done>

  <reversibility rating="reversible">An internal container swap inside one closure.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The watched set is derived from the layout authorities, and the assertion is a relationship</name>

  <read_first>
    - `scripts/board-dashboard.ts` lines 585-608 — the `WATCH_DIRS` literal, its docblock, the export,
      and `WATCH_DIR_COUNT`
    - `scripts/board-read.ts` lines 452-462 — `SOURCE_NAMES` and `SOURCE_COUNT`
    - `scripts/board-read.ts` lines 470-478 — `FIXED_SUBPATHS`, the per-source on-disk layout
    - `scripts/board-read.ts` — `QUEUE_STAGES`, the three stage names declared locally with their
      stated reason
    - `scripts/board-watch.test.ts` lines 180-222 — the existing two-sided count case and the
      second hand-typed list it compares against
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` WR-07 — the drift argument and
      the sketch it carries forward
  </read_first>

  <files>scripts/board-dashboard.ts, scripts/board-watch.test.ts</files>

  <action>
DERIVE, THEN PIN THE RELATIONSHIP. Write `deriveWatchDirs()` in `scripts/board-dashboard.ts`: walk
`SOURCE_NAMES`; skip `config`, which D-14 deliberately does not watch and which keeps its stated
reason in the docblock; for each remaining source take `FIXED_SUBPATHS[source]`, use its parent
directory when the subpath names a file, expand the queue root over `QUEUE_STAGES`, and deduplicate.
`WATCH_DIRS` becomes that result and keeps its exported name so no caller changes.

KEEP THE TWO-SIDED PIN AND CHANGE WHAT IT MEANS. `WATCH_DIR_COUNT` stays, and its failure message
stays decision-shaped — but it now pins a DERIVED number, so it fires when the layout moves rather
than when somebody edits a list. Say that in the comment: the count is the alarm, the derivation is
the mechanism.

REPLACE THE SECOND HAND-TYPED LIST WITH THE RELATIONSHIP. In `scripts/board-watch.test.ts`, delete
the second literal list and assert instead that every member of `SOURCE_NAMES` except `config` has a
watched ancestor — its `FIXED_SUBPATHS` path is inside, or equal to, one of the watched directories —
and that every watched directory is justified by at least one source. Both directions: a source with
no watcher is a silently unwatched surface, and a watched directory justified by no source is a
watcher nobody needs.

PROVE THE DERIVATION CAN FAIL. Add a discrimination case that runs `deriveWatchDirs`'s own logic over
a MUTATED copy of the layout inputs — a renamed tickets subpath, and a fourth queue stage — and asserts
the derived set changes accordingly. Without it, the derivation could be returning a constant and every
case above would still be green.

STATE WHAT THE DERIVATION DOES NOT COVER. The dial is unwatched by decision and the traceability file
shares the board's parent directory; write both facts into the docblock beside the derivation, because
a reader counting six directories against six sources will otherwise reach for a seventh.
  </action>

  <behavior>
    - Given the live layout authorities, `deriveWatchDirs()` returns exactly the six directories the loop watches today.
    - Given a mutated layout with a renamed tickets subpath, the derived set names the new path.
    - Given a mutated layout with a fourth queue stage, the derived set gains one directory.
    - Given every source except the dial, each has a watched ancestor.
    - Given every watched directory, at least one source justifies it.
  </behavior>

  <acceptance_criteria>
    - `WATCH_DIRS` is the result of `deriveWatchDirs()`; no directory string is typed in `scripts/board-dashboard.ts`.
    - The second hand-typed list in `scripts/board-watch.test.ts` is deleted, replaced by the two relationship assertions.
    - The mutation case shows the derived set moving with the layout inputs.
    - `WATCH_DIR_COUNT` still reads six and still carries a decision-shaped failure message.
    - The dial exemption and the shared-parent fact are both written in the docblock.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-watch.test.ts</automated>
    <fails_when>non-zero exit, or any case reports failed, or the relationship cases are reported as skipped</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or tsc names scripts/board-dashboard.ts in an error line</fails_when>
  </verify>

  <done>The watched set is computed from the layout the reader owns, and the test asserts the relationship instead of a copy.</done>

  <reversibility rating="reversible">A literal replaced by a derivation over existing exports.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A watch is armed against the root every read is resolved against</name>

  <read_first>
    - `scripts/board-dashboard.ts` line ~732 — `const dir = join(options.repoRoot, rel)` inside `arm`
    - `scripts/board-dashboard.ts` lines 930-960 — `run`: the first `deps.read(options.repoRoot)`,
      `loop.seed(first)`, then `loop.armAll()`; the resolved root is in hand before anything is armed
    - `scripts/board-read.ts` — `resolveRepoRoot` and `insideRoot`, the containment authority plan
      32-10 made single, and `FactorySnapshot.repoRoot`, which carries the resolved value out
    - `agent-factory/contracts/board.md` lines ~300-312 — the containment rule stated as a property of
      every path the projector reads
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` IN-01 — why it is an
      inconsistency rather than a leak, and what a reader comparing the two modules finds
  </read_first>

  <files>scripts/board-dashboard.ts, scripts/board-watch.test.ts, agent-factory/contracts/board.md</files>

  <action>
RED FIRST. Add a case that seeds the loop with a snapshot whose `repoRoot` differs from the argv value
handed to `createLoop` — which is exactly what a symlinked invocation path produces — and asserts the
armed directories are children of the SNAPSHOT's root. It fails today, because `arm` joins the argv
value.

THEN THREAD THE RESOLVED ROOT. Capture it in `seed` from the seeded result's `snapshot.repoRoot`,
refresh it on every subsequent read, and have `arm` join that. Keep `options.repoRoot` for what it is
— the argument the user typed, used for the usage message and nothing else — and say so in a comment,
because the two values being different is the whole point and a future reader will otherwise unify
them in the wrong direction.

ASK WHAT HAPPENS BEFORE THERE IS A SNAPSHOT. `armAll` is reachable only after `seed` in the production
path, but `createLoop` is a public function a test can drive in any order. State the answer: with no
seeded root, arming is skipped and one record says why, or the argv root is used and the loop records
that it is unresolved — pick one, write it in the docblock, and assert it. An unstated order dependency
in a loop that arms filesystem handles is the kind of assumption the next round measures.

THE SYMLINK CASE, END TO END. Build a scratch tree whose `plans/` is a symlink pointing outside the
tree, drive a single `--once` read plus an arming pass, and assert that the read is refused with the
containment code the reader already produces AND that no watch handle is armed on the symlinked path.
Clean the scratch tree in a `finally`. This is the CR-04 class asked in the sibling module, which is
the exact shape this round exists to close.

ADD THE CONTRACT SENTENCE. In the containment section, state that the projector arms its watches
against the same resolved root it reads against, so the rule covers both verbs rather than one.
  </action>

  <behavior>
    - Given a seeded snapshot whose root differs from the argv value, every armed directory is a child of the snapshot's root.
    - Given a later read that resolves to a different root, subsequent arming follows the new value.
    - Given `armAll` called before any seed, the loop behaves as the docblock states and a case asserts it.
    - Given a scratch tree whose source directory is a symlink outside the root, the read is refused and no watch is armed on it.
    - Given an ordinary tree, the armed set is unchanged from the committed behaviour.
  </behavior>

  <acceptance_criteria>
    - `arm` joins the resolved root; no call site of `join` inside `createLoop` uses `options.repoRoot`.
    - The pre-seed behaviour is stated in the docblock and asserted by a case.
    - The symlink case asserts both halves — the refusal and the absent watch handle — and removes its scratch tree in a `finally`.
    - `agent-factory/contracts/board.md` states that watches are armed against the resolved root.
    - `git status --short` names nothing outside this plan's `files_modified` as changed after the run.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-watch.test.ts scripts/board-dashboard.test.ts</automated>
    <fails_when>non-zero exit, or any case reports failed, or the symlink case is reported as skipped</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or the parity step names a tracked .js as moved, or freshness reports a committed .js that does not match a rebuild</fails_when>
    <automated>npm run check:public-docs &amp;&amp; npm run check:imperative-lexicon &amp;&amp; npm run check:banned-claims &amp;&amp; npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or any gate's last line is not ALL CHECKS PASSED</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the file count is below 74, or the passed count is below the 4811 recorded by the round-1 adversarial review</fails_when>
  </verify>

  <done>Reads and watches resolve through one root, and the contract says both verbs are covered.</done>

  <reversibility rating="reversible">A value threaded through an existing seam.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the argv root → the filesystem handles the process opens | The invocation path is user-controlled and may be a symlink; the containment authority exists because of that. |
| a failing watch → the operator's belief that the screen is current | A watch that is not watching, hidden by the poll, is a silent downgrade of the live claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-19-01 | Elevation of Privilege | `arm`'s use of the unresolved argv root | medium | mitigate | A watch handle is opened on a path the containment authority refused for reads. No content crosses — a watcher yields names only — so the severity is medium, not high. Task 3 routes both verbs through one resolved root and asserts the symlink case. |
| T-32-19-02 | Denial of Service | the append-only watch-error list | medium | mitigate | One entry per poll tick is thousands a day, printed on every frame and embedded in every document. Task 1 keys by directory and clears on re-arm, asserted by a counted case. |
| T-32-19-03 | Tampering | `WATCH_DIRS` as a third hand-typed layout | high | mitigate | A moved subpath silently stops the live path while the mandatory poll hides it and every gate stays green — this repository's recorded set-literal drift class. Task 2 derives the set and asserts the relationship in both directions with a mutation case. |
| T-32-19-04 | Repudiation | the re-arm promise in the error text | low | mitigate | The message stops being true once the entry survives a successful re-arm. Task 1's clearing makes it true and the comment names what makes it true. |
| T-32-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan; `package.json` is untouched. |
</threat_model>

<verification>
1. `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-watch.test.ts scripts/board-dashboard.test.ts`.
2. `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness`.
3. `npm run check:public-docs && npm run check:imperative-lexicon && npm run check:banned-claims && npm run check:claim-anchors`.
4. `node scripts/check-foundation-guards.js`.
5. `npx vitest run --exclude '**/scripts/e2e/**'` — the whole suite minus the live claude-CLI e2e lane. Never plain `npm test`.
</verification>

<success_criteria>
- Ten failing ticks produce one record; a successful re-arm produces none.
- The watched set is derived, and the test asserts a relationship in both directions with a mutation case.
- Watches and reads resolve through one root, proved on a symlinked tree.
- REQUIREMENTS.md and the ROADMAP phase status are NOT flipped to Complete by this plan.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-19-SUMMARY.md` when done.
</output>

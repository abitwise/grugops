---
phase: 32-board-projector-cli-dashboard
plan: 20
type: execute
wave: 6
gap_closure: true
depends_on: ["32-15", "32-16", "32-17", "32-18", "32-19"]
files_modified:
  - scripts/board-readonly.test.ts
  - .planning/phases/32-board-projector-cli-dashboard/32-20-RED-baseline.txt
  - .planning/phases/32-board-projector-cli-dashboard/32-20-GREEN-proof.txt
autonomous: true
requirements: [DASH-06, DASH-08]
estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A module in the dashboard closure that imports a builtin the closure does not legitimately use — the heap-snapshot writer, the embedded database, the virtual machine — makes `npm run check:dashboard-readonly` exit non-zero, because the specifier set is pinned as an allow-list rather than screened against a list of names to refuse."
    - "A module that acquires a module identity at runtime — assembled specifier, dynamic import, require-equivalent — makes the gate exit non-zero, because the only admitted way to acquire a module in this closure is a static import declaration with a literal specifier."
    - "A module that reaches a write capability through a global rather than through a module — the process report writer — makes the gate exit non-zero, because the member paths reached on the capability-bearing globals are pinned two-sided."
    - "The legitimate closure is unchanged by all three rules: the unplanted mirror is still green and the measured fs symbol set still has its committed members and count."
    - "Every new refusal is proved twice: once from a planted mirror inside the suite, and once by planting the same source into the committed `.js` a host runs and watching the named gate command exit non-zero."
  artifacts:
    - "scripts/board-readonly.test.ts — the specifier allow-list, the acquisition canonical form, the global-member census, and the shapes held as data"
    - ".planning/phases/32-board-projector-cli-dashboard/32-20-RED-baseline.txt — the gate green over each writer before the fix, with the measured counts"
    - ".planning/phases/32-board-projector-cli-dashboard/32-20-GREEN-proof.txt — each shape refused after the fix, plus the positive controls"
  key_links:
    - "ALLOWED_BUILTIN_SPECIFIERS -> the bare-specifier equality pin: the complement is refused by construction, so a builtin nobody has met yet is a red rather than a silent admission"
    - "the acquisition census -> opaqueFsAcquisitions' existing premise case: a refusal reds the gate through an assertion that is already there"
    - "the global member-path census -> the process report writer and the runtime-assembled identity: one derivation answers both, because both are a member reached on a capability-bearing global"
    - "npm run check:dashboard-readonly -> scripts/board-readonly.test.ts: one authority, one implementation, and Task 3 proves it against the artifact a host runs"
  deferred: []
---

<objective>
Close verification gap 2: the phase's stated mechanical safety centrepiece reports fifty-nine of
fifty-nine passing over a module that creates and writes files.

Gap mapping for this plan (from `32-VERIFICATION.md` frontmatter `gaps:`, truth 4):

| Gap | Finding | Task |
|-----|---------|------|
| "The dashboard CANNOT write: an import-graph guard proves its module tree holds no mutating `node:fs` symbol" | An ordinary import of the heap-snapshot writer from the virtual-machine builtin — zero obfuscation — leaves the gate fully green, because the module rule is a hand-typed list of fifteen names to refuse while the namespace rule beside it was converted to a canonical form. The embedded-database builtin and the process report writer are green for the same reason, the last with no import at all. Independently reproduced. | Task 1 (identity), Task 2 (globals) |
| same truth, second half (F-03) | A module identity assembled at runtime defeats the fs-namespace rule by construction: the rule fires on an identifier bound to a literal specifier, and an assembled string is not one. Named as a residual by plan 32-11, measured by the round-1 adversarial review, and re-confirmed by the verifier. | Task 2 (acquisition) |

Purpose: `CLAUDE.md` states the safety rule as mechanical rather than prose, and
`agent-factory/contracts/board.md` states of this projector that it writes no file. The mechanism
currently decides a strictly narrower question than either sentence. This is the fourth consecutive
round in which this repository's fix closed one spelling and left the capability reachable one
register over, so the three arms are closed in ONE edit rather than split across plans by which side
a finding happened to report.

Output: module identity becomes a two-sided allow-list; module ACQUISITION becomes a canonical form
with one admitted shape; capability-bearing globals get a two-sided member-path census; every shape is
a row in a table with a cardinality pin; and each refusal is reproduced against the committed `.js`.

This plan runs after every runtime-code plan in this round, and the ordering is an argument rather
than scheduling: the guard decides a question about the transitive closure of the compiled
`scripts/board-dashboard.js`, and plans 32-15 through 32-19 all change modules inside it.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` and no `## Prohibitions`
section to satisfy.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-14-ADVERSARIAL-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-11-SUMMARY.md
@.planning/phases/32-board-projector-cli-dashboard/32-06-SUMMARY.md
</context>

## Artifacts this phase produces

- `ALLOWED_BUILTIN_SPECIFIERS` and `ALLOWED_BUILTIN_SPECIFIER_COUNT` in
  `scripts/board-readonly.test.ts` — the normalized builtin identities the dashboard closure may
  reach, one named reason each, pinned two-sided by members AND count.
- `ModuleFacts.acquisitions` — every module acquisition in a module that is not a static import
  declaration with a string-literal specifier, collected with its source text.
- `ModuleFacts.unresolvedCallees` and the resolution map beside it — every called identifier the pass
  cannot resolve to an import binding or a declaration in the same module, and, for the ones it can,
  which declaration resolved them.
- `ModuleFacts.globalMemberPaths` — every member path rooted at a capability-bearing global
  identifier, with `EXPECTED_GLOBAL_MEMBER_PATHS` and its count pinned two-sided.
- `MODULE_IDENTITY_SHAPES` and `MODULE_IDENTITY_SHAPE_COUNT` — the escape spellings held as data, in
  the shape `NAMESPACE_ESCAPE_SHAPES` already established, each a row with a name and the source to
  plant.
- `BANNED_MODULES` and `ADDITIONAL_BANNED_MODULES` retained as the written record of D-21, with their
  docblocks restated to say they document a decision and no longer decide the question.
- `.planning/phases/32-board-projector-cli-dashboard/32-20-RED-baseline.txt` and `32-20-GREEN-proof.txt`.
- No production-code change and no committed `.js` movement: this plan edits one `.test.ts` file.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module identity becomes a two-sided allow-list</name>

  <read_first>
    - `scripts/board-readonly.test.ts` lines 134-180 — `ROOT`, the entries, `normalizeSpecifier`,
      `isFsSpecifier`, `isBareSpecifier`
    - `scripts/board-readonly.test.ts` lines 181-400 — `analyzeModule` and the four positions a member
      name enters the symbol set, as finding F-02 documented them
    - `scripts/board-readonly.test.ts` lines 400-450 — `ClosureFacts` and `analyzeClosure`, the union
      over the closure
    - `scripts/board-readonly.test.ts` lines 776-895 — `BANNED_MODULES`, `ADDITIONAL_BANNED_MODULES`,
      `bannedModulesReached`, the premise case asserting the specifier set is non-empty, and the only
      current assertion over `bareSpecifiers`
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` WR-02 — the three measured
      green plants and the inversion it sketches
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` gap 2 — the verifier's own
      reproductions and their recorded exit codes
  </read_first>

  <files>scripts/board-readonly.test.ts, .planning/phases/32-board-projector-cli-dashboard/32-20-RED-baseline.txt</files>

  <action>
RED FIRST, AND RECORD THE MEASUREMENT, NOT THE ADJECTIVE. Before editing any predicate, plant each of
the three review-measured writers into a live mirror of the closure — the heap-snapshot writer
imported from the virtual-machine builtin, the embedded-database constructor, and the process report
writer with no import at all — and record into
`.planning/phases/32-board-projector-cli-dashboard/32-20-RED-baseline.txt`: the collected
`fsSymbols`, `bareSpecifiers`, `opaqueFsAcquisitions` and banned-module sets for each plant, and the
exit code and case counts of `npm run check:dashboard-readonly` with the same source planted into a
mirror. A baseline that does not show a green gate over a writer has not reproduced the finding.

THEN INVERT THE RULE. Declare `ALLOWED_BUILTIN_SPECIFIERS` as the normalized builtin identities the
closure legitimately reaches — measured from the live closure, not guessed — each with the one-line
reason it belongs and the module that brings it in. Assert the live `bareSpecifiers` set, normalized,
EQUALS that allow-list: members in both directions, plus a count pin whose failure message is
decision-shaped. Equality rather than containment is the point: a new legitimate import is a decision
somebody records, and a builtin nobody has met yet is refused by construction rather than by having
been listed.

KEEP THE BANNED LISTS AS THE RECORD OF D-21, AND SAY WHAT THEY ARE NOW. Restate both docblocks: they
document the decision that the projector spawns nothing and listens on nothing, the allow-list is what
decides, and the two must not drift — assert that every banned identity is absent from the allow-list,
so the two statements cannot contradict each other silently.

ASSERT THE PREMISE BEFORE THE CLAIM. The existing non-empty premise case stays. Add one more: the
allow-list is non-empty and every member is reachable in the live closure, so an entry that stopped
being used is a red rather than a permanent widening nobody notices.

THE POSITIVE CONTROL IS PART OF THIS TASK. Plant a module importing a builtin that IS on the
allow-list and using it read-only, and assert the gate stays green and the fs symbol count does not
move. Without it, the rule could refuse everything and still look correct.
  </action>

  <behavior>
    - Given the live closure, the normalized bare-specifier set equals `ALLOWED_BUILTIN_SPECIFIERS` exactly.
    - Given a mirror with the heap-snapshot writer imported, the equality assertion fails and the gate exits non-zero.
    - Given a mirror with the embedded-database builtin imported, the same.
    - Given a mirror with the virtual-machine builtin imported, the same.
    - Given a mirror importing an allow-listed builtin read-only, the gate stays green and `EXPECTED_CLOSURE_FS_SYMBOL_COUNT` does not move.
    - Given the banned lists, no member appears in the allow-list.
  </behavior>

  <acceptance_criteria>
    - `.planning/.../32-20-RED-baseline.txt` records, for each of the three plants, the four collected sets and the exit code of the named gate command before the fix.
    - `ALLOWED_BUILTIN_SPECIFIERS` carries a reason per entry and is asserted equal to the measured live set, with a two-sided count.
    - `bannedModulesReached` no longer decides any assertion on its own; the banned lists are asserted disjoint from the allow-list.
    - The reachability case fails if an allow-list entry stops being used by the closure.
    - The positive control passes with the fs symbol count unmoved.
  </acceptance_criteria>

  <verify>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit, or the run reports 0 passed — the named gate wraps this one file and a zero-case run is a green line over an empty denominator</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts</automated>
    <fails_when>non-zero exit, or the passed-case count is at or below the committed run's — cases were replaced rather than added</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or tsc names scripts/board-readonly.test.ts in an error line</fails_when>
  </verify>

  <done>The identity question is decided by an allow-list pinned in both directions, and the three measured writers are refused.</done>

  <reversibility rating="reversible">A predicate inversion inside one test file.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One admitted way to acquire a module, and a two-sided census of what the globals reach</name>

  <read_first>
    - `scripts/board-readonly.test.ts` lines 160-180 — `ModuleFacts`, in particular
      `opaqueFsAcquisitions` and `opaqueSpecifiers` and the fail-closed rule they already state
    - `scripts/board-readonly.test.ts` lines 181-400 — `analyzeModule`'s walk, where a new collector is
      added beside the existing ones
    - `scripts/board-readonly.test.ts` lines 611-680 — PART ONE's premise cases, including "no closure
      module acquires a filesystem module by a route this pass cannot name", the assertion a refusal
      reds through
    - `scripts/board-readonly.test.ts` lines 487-610 — `WRITE_CLASS_STEMS`, `matchesWriteClassStem`,
      `STEM_FALSE_POSITIVES` and the named-exclusion posture this task copies for its own exemptions
    - `.planning/phases/32-board-projector-cli-dashboard/32-14-ADVERSARIAL-REVIEW.md` F-03 — the
      reproduction, the two closures 32-11 declined, and the reason each was declined
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` WR-02 — the process report
      writer, which reaches a write capability with no module specifier anywhere
  </read_first>

  <files>scripts/board-readonly.test.ts</files>

  <action>
RED FIRST, BOTH HALVES. Plant the adversarial review's exact F-03 source into a live mirror — a module
identity assembled from two string pieces at runtime, then a writer called through it — and record the
gate green. Then plant the process report writer with no import at all, and record the gate green.
Both transcripts go into the RED baseline file Task 1 started.

THE CANONICAL FORM FOR ACQUISITION, STATED AS ONE SENTENCE IN THE DOCBLOCK: in this closure a module
identity is established by exactly one shape — a static import declaration whose specifier is a string
literal. Collect EVERY other module acquisition as an `acquisition`: a dynamic import call whatever its
argument, a call to an identifier this pass cannot resolve to an import binding or to a declaration in
the same module, and any call reached through a member path on a capability-bearing global. Assert the
collected set is empty over the live closure, through the same premise-case shape the fs acquisitions
already use, so a refusal reds the gate through an assertion that exists rather than a new one.

DO NOT CONSTANT-FOLD. 32-11 declined folding because it changes what this syntactic pass is, and that
argument still holds. Refusing an acquisition whose identity cannot be decided is the honest answer for
a guard: an unprovable identity is not a safe identity. Write that sentence where the rule is, because
the next reader's instinct will be to make the pass cleverer instead.

PIN BOTH AXES OF THE RESOLUTION, NOT ONE. A called identifier is resolved when it comes from an import
binding or a declaration in the module. Collect the resolved ones WITH the declaration kind and line
that resolved them, and pin that map two-sided as well — otherwise a newly added declaration reusing a
sensitive name silently converts an unresolved callee into a resolved one, which is the "one dead
declaration disables the ban" shape this repository has already measured once.

THE GLOBAL MEMBER-PATH CENSUS. Collect every member path rooted at an identifier named for one of the
capability-bearing globals — the process object and the global object — however deep, as normalized
dotted strings. Pin the resulting set two-sided against `EXPECTED_GLOBAL_MEMBER_PATHS`, measured from
the live closure and carrying a reason per entry. The process report writer and the runtime builtin
getter are both new members of that set, so one derivation answers both, and a future capability
reached through a third member is a red rather than a discovery.

NAME-SCOPED, AND SAY SO. Like the existing namespace rule, this census is name-scoped rather than
binding-scoped: a local that reuses one of those spellings is treated as the global. The imprecision
runs toward REFUSAL, which is the only direction a safety guard may be imprecise in. Write that
sentence, and add a case proving the direction — a local named for a global still reds rather than
silently passing.

POSITIVE CONTROLS, ONE PER RULE. The closure legitimately reads the process argument vector, both
output channels, the exit function and the environment; assert those member paths stay admitted and the
gate stays green. A rule with no positive control is a rule nobody can tell from a refusal of
everything.
  </action>

  <behavior>
    - Given a mirror assembling a module identity at runtime and calling a writer through it, the acquisition set is non-empty and the gate exits non-zero.
    - Given a mirror calling the process report writer with no import, the global member-path set gains a member and the gate exits non-zero.
    - Given a mirror with a dynamic import of any specifier, the acquisition set is non-empty.
    - Given a mirror calling a require-equivalent, the unresolved-callee set gains a member and the gate exits non-zero.
    - Given a mirror declaring a local whose name shadows a capability-bearing global, the census still collects the path and the gate exits non-zero.
    - Given the live closure, the acquisition set and the unresolved-callee set are empty, and the global member-path set equals its pinned value.
  </behavior>

  <acceptance_criteria>
    - The canonical-form sentence is in the docblock and names the single admitted acquisition shape.
    - The acquisition collector fires on a dynamic import, an unresolved callee and a global-rooted call, and each has its own case.
    - The resolution map is pinned two-sided, with the declaration kind and line recorded per resolved callee.
    - `EXPECTED_GLOBAL_MEMBER_PATHS` is measured from the live closure, carries a reason per entry, and is pinned by members and count.
    - The four positive controls pass and the fs symbol count does not move.
    - The RED baseline file records both new reproductions with their pre-fix exit codes.
  </acceptance_criteria>

  <verify>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit, or the run reports 0 passed, or the reported case count is lower than after Task 1</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts --reporter=verbose</automated>
    <fails_when>non-zero exit, or any positive-control case name is absent from the verbose output</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or tsc names scripts/board-readonly.test.ts in an error line</fails_when>
  </verify>

  <done>An unprovable module identity and a capability reached through a global are both refused, with both axes of the resolution pinned.</done>

  <reversibility rating="reversible">Two collectors and two pins inside one test file.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The shapes as data, the mutations, and the reproduction against the committed .js</name>

  <read_first>
    - `scripts/board-readonly.test.ts` lines 896-950 — `withLiveMirror`, `SCRATCH_ROOT`,
      `PLANTED_WRITER`, and the mirror's clean-up contract
    - `scripts/board-readonly.test.ts` lines 950-1115 — `NAMESPACE_ESCAPE_SHAPES`,
      `ACQUISITION_SHAPES` and their cardinality pins; the table shape this task extends
    - `scripts/board-readonly.test.ts` lines 1115-1370 — the discrimination block, the unplanted-mirror
      control, and the premise cases over both tables
    - `scripts/board-readonly.test.ts` lines 40-60 — the docblock's "what this does not close" list,
      which this task rewrites
    - `.planning/phases/32-board-projector-cli-dashboard/32-14-ADVERSARIAL-REVIEW.md` §0 and §6 — the
      premise rule and the mutation protocol this task follows
  </read_first>

  <files>scripts/board-readonly.test.ts, .planning/phases/32-board-projector-cli-dashboard/32-20-GREEN-proof.txt</files>

  <action>
HOLD THE SHAPES AS DATA. Add `MODULE_IDENTITY_SHAPES` beside the two existing tables: one row per
spelling this plan closes — the heap-snapshot writer import, the embedded-database import, the
virtual-machine import, the un-prefixed spelling of one of them, the runtime-assembled identity, the
dynamic import, the require-equivalent, the process report writer, the shadowed-global local — each
carrying a name and the source to append. One iteration over the rows; adding a spelling is adding a
row. Pin the cardinality two-sided with a decision-shaped message and assert the row names are unique,
exactly as the existing tables do.

TEST THE UNION, NOT THE NEW RULE ALONE. Keep every row of the two existing tables in the iteration and
state in the docblock that what is tested is the union of the namespace rule, the acquisition rule and
the identity rule. Three rules that each pass their own rows and were never run together is how a
round closes a spelling and reopens a capability.

THE MUTATIONS. For each of the three rules, disable it in exactly one place — comment the equality
assertion, comment the acquisition collection, comment the global-path pin — and record which rows go
red and which go green. A rule whose mutation reds nothing is a rule that was never deciding. Record
the three transcripts in `32-20-GREEN-proof.txt`, with the row names and counts, not with adjectives.

THE INDEPENDENT REPRODUCTION, AGAINST THE ARTIFACT A HOST RUNS. Outside the suite, and recorded by
hand into the GREEN proof: for the three verifier-measured shapes, append the source verbatim to the
committed `scripts/board-read.js`, run `npm run check:dashboard-readonly`, record the exit code and the
failed-case count, restore the file, and confirm that `git diff --exit-code -- scripts/board-read.js` over the UNCOMMITTED working tree is
clean before continuing — the probe is an uncommitted edit, so the bare working-tree diff is the right
question here. The mirror proves the rule; this proves the gate a host actually runs.

REWRITE THE BOUNDARY LIST. The docblock's "what this does not close" list must now name what remains
open after this plan — and it must be a list somebody measured, not a list somebody remembered. State
at least: a capability reached through a global spelling not in the pinned root set, and any route that
requires executing the module rather than parsing it. Say that this pass is syntactic and what that
buys and costs.

ANSWER HOW THE GATE IS REACHED. In one short block, state how `check:dashboard-readonly` is invoked in
CI today and what would happen if it were not — this is the question the round-1 adversarial review
found unasked, and plan 32-21 closes the mechanical half. Cross-reference it rather than duplicating it.
  </action>

  <acceptance_criteria>
    - `MODULE_IDENTITY_SHAPES` has at least nine rows, a two-sided cardinality pin, and a uniqueness assertion over row names.
    - The discrimination block iterates all three tables in one pass and the docblock says the union is what is tested.
    - `32-20-GREEN-proof.txt` records three mutation transcripts naming which rows went red under each.
    - `32-20-GREEN-proof.txt` records three committed-`.js` reproductions with exit codes, failed-case counts, and a clean working-tree `git diff --exit-code` after each restore.
    - The "what this does not close" list names only boundaries measured during this plan.
    - The unplanted-mirror control case and the fs symbol count are unchanged.
  </acceptance_criteria>

  <verify>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit, or the run reports 0 passed, or the reported case count is lower than after Task 2</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts --reporter=verbose</automated>
    <fails_when>non-zero exit, or fewer than nine module-identity row case names appear in the verbose output</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or the parity step names any tracked .js as moved — this plan edits a test file and must move no build output</fails_when>
    <automated>git diff --exit-code -- scripts/board-read.js scripts/board-model.js scripts/board-dashboard.js</automated>
    <fails_when>non-zero exit — the UNCOMMITTED working tree still carries a planted probe in a build output; this is deliberately the working-tree diff, because the probe is never committed</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the file count is below 74, or the passed count is below the 4811 recorded by the round-1 adversarial review</fails_when>
  </verify>

  <done>Every closed spelling is a row, every rule is shown to fail when disabled, and the three verifier-measured bypasses are refused by the gate a host runs.</done>

  <reversibility rating="reversible">Tables, transcripts and docblock text.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a future commit → `scripts/board-dashboard.js`'s import closure | Any change to the projector's module tree can introduce a write capability; this gate is the only mechanism deciding it did not. |
| `scripts/*.ts` sources → the committed `scripts/*.js` a host runs | The gate analyses the committed build output. A source that was not rebuilt is a program the gate never saw. |
| the Node platform surface → the closure | Capabilities reachable without any module specifier — the globals — are outside a module-identity rule by construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-20-01 | Elevation of Privilege | the module rule as a deny-list | critical | mitigate | Reproduced three times: an ordinary import of a builtin the list does not name ships a file writer past a fully green gate. Task 1 inverts the rule to a two-sided allow-list, so the complement is refused by construction. |
| T-32-20-02 | Elevation of Privilege | a runtime-assembled module identity | high | mitigate | Reproduced: the fs-namespace rule cannot see an identity that is not a literal. Task 2 states one admitted acquisition shape and refuses every other, including the unprovable one, rather than making the pass cleverer. |
| T-32-20-03 | Elevation of Privilege | a write capability reached through a global | high | mitigate | Reproduced: the process report writer creates and writes a file with no import anywhere. Task 2 pins the global member paths two-sided, so a capability reached through a new member is a red. |
| T-32-20-04 | Tampering | the shape tables and the pinned sets | high | mitigate | A hand-maintained enumeration rots while every gate over it stays green. All three sets are pinned two-sided with decision-shaped messages, the shapes are rows, and Task 3's mutations prove each rule decides something. |
| T-32-20-05 | Repudiation | the gate's subject | high | mitigate | A green gate over a stale build is indistinguishable from a green gate over the real program. Task 3 reproduces every measured bypass against the committed `.js` and verifies the tree is clean afterwards; the parity gate runs in this plan's verification. |
| T-32-20-06 | Denial of Service | over-refusal | medium | mitigate | A rule refusing the legitimate closure would be loosened under pressure and end weaker than what it replaced. Each of the three rules ships with a positive control and the unplanted-mirror control is unchanged. |
| T-32-20-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan. `package.json` gains no dependency entry, and the file's own case asserting the absence of runtime dependencies is unchanged. |
</threat_model>

<verification>
1. `npm run check:dashboard-readonly` — the DASH-06 authority and its only implementation.
2. `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts --reporter=verbose` — every row name visible.
3. `npm run typecheck` — the file is compiled by the tests config.
4. `npm run build && npm run check:build-parity` — this plan moves no build output; parity must be clean without a rebuild moving anything.
5. The three committed-`.js` reproductions, recorded by hand into `32-20-GREEN-proof.txt`, each followed by `git diff --exit-code`.
6. `npx vitest run --exclude '**/scripts/e2e/**'` — the whole suite minus the live claude-CLI e2e lane. Never plain `npm test`.
</verification>

<success_criteria>
- The three verifier-measured writers and the two review-measured ones all make the named gate exit non-zero, proved on the committed `.js`.
- Module identity, module acquisition and global member paths are each pinned two-sided with a decision-shaped message.
- Every rule has a positive control and a mutation that reds named rows.
- The legitimate closure is unchanged: the unplanted mirror is green and the fs symbol count is unmoved.
- REQUIREMENTS.md and the ROADMAP phase status are NOT flipped to Complete by this plan.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-20-SUMMARY.md` when done.
</output>

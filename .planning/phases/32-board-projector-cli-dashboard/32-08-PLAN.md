---
phase: 32-board-projector-cli-dashboard
plan: 08
type: execute
wave: 4
depends_on: ["32-02", "32-05"]
files_modified:
  - scripts/validate-agent-factory.ts
  - scripts/validate-agent-factory.js
  - scripts/validate.test.ts
  - plans/board.md
  - agent-factory/seed/plans/board.md
  - scripts/check-imperative-lexicon.test.ts
autonomous: true
requirements: [DASH-01, DASH-08]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`scripts/board-model.ts` is the only board grammar in the tree: the two column helpers inlined at `scripts/validate-agent-factory.ts:735-741` are deleted and the validator imports their replacements from `./board-model.js` (DASH-01, D-06)."
    - "The WR-03 counterexample stays pinned: column `In` does not match `## In Development (WIP 0/3)`, and the bare prefix-match membership spelling is asserted absent from `scripts/validate-agent-factory.ts` (D-06)."
    - "Both board↔ticket messages at `scripts/validate-agent-factory.ts:747-755` keep their exact wording, so `scripts/validate.test.ts:133` and `:513` pass unchanged (D-06)."
    - "`kebab` has exactly one spelling in the tree — it lives in `board-model.ts` and the validator imports it rather than declaring a second copy (D-06)."
    - "The two deliberate deviations are recorded by name in both the plan summary and `agent-factory/contracts/board.md`: the suffix strip widens from the single WIP form to D-05's three forms so `Blocked` normalizes to `Blocked`, and a non-canonical suffix no longer creates a phantom column (D-06, D-05)."
    - "A RED/GREEN baseline of the validator's verdict over all eight `scripts/fixtures/*` trees is captured before the extraction and compared block by block after it, because `checkTickets()` returns early on this tree and the live run exercises none of this code (RESEARCH Pitfall 6)."
    - "The 48-line HTML comment in `plans/board.md` and its seed twin states the canonical heading form and the canonical row form in one short block, and both twins stay in sync — their existing near-twin relationship is asserted, not assumed (D-04)."
    - "The column table at `plans/board.md:58-72` and its seed twin are frozen: the header row, the 13 column names and the table's position are unchanged, because `scripts/check-imperative-lexicon.ts:661-664` derives a named part from that exact literal and throws when it moves (RESEARCH Pitfall 10)."
    - "The `contracts` part's cardinality moving by one when `agent-factory/contracts/board.md` landed is handled as a derivation, not as a bumped constant: `CONTRACT_N` at `scripts/check-imperative-lexicon.test.ts:110` is already `partSize(\"contracts\")`, so the change is verified to flow through rather than edited."
    - "`package.json` gains no `dependencies` key and `package-lock.json` is byte-unchanged across the whole phase — the absence of change is asserted, not merely refrained from (DASH-08)."
  prohibitions:
    - statement: "There is never a second board grammar in the tree."
      status: enforced
      verification: "The inline helper declarations are deleted and their absence is asserted by a negative grep over the comment-stripped source; the validator imports the single authority."
    - statement: "No board↔ticket error message wording changes."
      status: enforced
      verification: "`npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts` passes unchanged, including the two cases asserting the not-a-board-column text."
    - statement: "The seed board's column table is never renamed, reordered, or moved."
      status: enforced
      verification: "`npm run check:imperative-lexicon` exits 0 with no derivation refusal naming `boardColumns`, and a diff of the table region shows no change."
    - statement: "No installer shim is added for the dashboard."
      status: enforced
      verification: "`git status --porcelain install/` prints nothing (D-16)."
    - statement: "No runtime dependency is added anywhere in this phase."
      status: enforced
      verification: "`git diff --exit-code -- package-lock.json` and a `dependencies === undefined` assertion at phase close."
  artifacts:
    - scripts/validate-agent-factory.ts
    - scripts/validate-agent-factory.js
    - plans/board.md
    - agent-factory/seed/plans/board.md
  key_links:
    - "`scripts/validate-agent-factory.ts` imports `boardColumnName`, `boardHasColumn`, `parseBoard` and `kebab` from `./board-model.js` — the deletion plus the import is what makes the one-authority claim mechanical rather than stated."
    - "`plans/board.md`'s comment is the agent-facing copy of `agent-factory/contracts/board.md`; the contract is normative and the comment is the place an agent editing the board actually looks (D-04)."
    - "`scripts/check-imperative-lexicon.ts:661-664` reads the seed board's table header as a derivation anchor — that table is shared state between this phase and an existing gate."
---

<objective>
Collapse the tree to one board grammar and tell agents what it is. The validator's inline column
parser is deleted and replaced by an import from `scripts/board-model.js`; the board's own 48-line
documentation comment and its seed twin are rewritten to state the canonical heading and row forms;
and the phase closes with the whole gate set green and the absence of any dependency change asserted.

Purpose: DASH-01's requirement is a deletion, not an addition — a second copy of one predicate is the
Phase 29 failure this repository spent five rounds on. And an agent that never reads the contract
still reads the board it is editing, which is why D-04 puts the spec in both places.

Output: a validator with ~20 fewer lines and one more import, two rewritten board twins with their
column table untouched, and a green phase-close sweep.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md
@.planning/phases/32-board-projector-cli-dashboard/32-PATTERNS.md
@.planning/phases/32-board-projector-cli-dashboard/32-02-SUMMARY.md
@.planning/phases/32-board-projector-cli-dashboard/32-05-SUMMARY.md
@agent-factory/contracts/board.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the second grammar — extract the column authority into the validator's import</name>
  <files>scripts/validate-agent-factory.ts, scripts/validate-agent-factory.js, scripts/validate.test.ts</files>
  <read_first>
    - `scripts/validate-agent-factory.ts:53-71` — the five existing imports, three of which are already from `scripts/` (`kit-model.js`, `checkpoints.js`, `context-io.js`). Note for the summary: D-06's prose calls `board-model.js` the validator's first `scripts/` import, and that is factually wrong; correct the prose, the work is unchanged.
    - `scripts/validate-agent-factory.ts:250-256` — the `kebab` declaration that moves out.
    - `scripts/validate-agent-factory.ts:721-762` — `checkTickets()` in full: the vacuity guard at `:722-723`, the WR-03 comment at `:729-734`, the two helpers at `:735-741`, and the two messages at `:747-755`.
    - `scripts/validate.test.ts:133, 513` — the two cases asserting the not-a-board-column text; both must pass with byte-identical expectations.
    - `scripts/fixtures/bad-ticket-bad-column/plans/board.md` and `scripts/fixtures/bad-ticket-mismatch/plans/tickets/ABC-001.md` — the two fixtures that drive them.
    - `scripts/board-model.ts` — `boardColumnName`, `boardHasColumn`, `parseBoard`, `kebab` as the one authority.
    - `scripts/check-foundation-guards.ts` — the blast-radius posture this repository recorded for its own Phase 29 migration: capture the verdict over every fixture tree before and after, and compare block by block.
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Extraction Site and §Pitfall 6.
  </read_first>
  <behavior>
    - `checkTickets()` produces byte-identical output over all eight `scripts/fixtures/*` trees before and after the extraction.
    - Column `In` does not satisfy the membership check against a board carrying `## In Development (WIP 0/3)`.
    - `## Blocked (visible, time-tracked)` normalizes to the column name `Blocked` after the extraction, where before it did not — a named deviation.
    - `## Columns (spec §6.1)` opens no column after the extraction, where before it created a phantom column of that name — a named deviation.
    - The two board↔ticket messages are byte-identical to their pre-extraction text.
    - `kebab` is declared exactly once in the tree.
  </behavior>
  <action>
Capture the baseline first, before touching a line. Run the validator against each of the eight
fixture trees under `scripts/fixtures/` and record every verdict block verbatim in a
`NN-RED-baseline.txt` at the repository root in this repository's established style. The vacuity
guard at `:722-723` returns when `plans/tickets/` holds no markdown file, and this tree holds only a
`.gitkeep`, so **the live run exercises none of this code** — the fixtures are the only evidence a
regression would leave, and a baseline captured after the edit is not a baseline.

Then delete the two locally declared helpers at `:735-741` together with the WR-03 comment block
above them at `:729-734`, and import `boardColumnName`, `boardHasColumn` and `parseBoard` from
`./board-model.js`. Move `kebab` out of `:250-256` and import it from the same module, so the tree
carries exactly one spelling of it. Replace the deleted comment with two sentences naming where the
rule now lives and citing `agent-factory/contracts/board.md` §Headings — the explanation moves with
the code rather than being lost with it.

Keep the two messages at `:747-755` byte-identical. Two existing test cases assert their text, and a
reworded message is a behaviour change nobody asked for.

Record the two deliberate deviations by name, in the plan summary and in the contract: the suffix
strip widens from the single WIP form to D-05's three forms, so `## Blocked (visible, time-tracked)`
now normalizes to `Blocked` instead of staying unnormalized; and a `##` heading whose suffix is not
one of the three forms no longer opens a column, so `## Columns (spec §6.1)` stops creating a phantom
column. Both are defect fixes, both change what the validator sees, and D-06's "ported verbatim"
means semantics preserved with defects fixed and deviations named — not a byte-for-byte port carrying
both defects forward.

Re-run the eight-fixture sweep after the edit and compare block by block against the baseline,
recording the comparison in the matching `NN-GREEN-proof.txt`. Any difference is either one of the
two named deviations or a regression; there is no third category, and the comparison must say which.

Add a case to `scripts/validate.test.ts` pinning the WR-03 counterexample through the new import
path, and a case asserting the bare prefix-match membership spelling is absent from the validator
source. Rebuild and commit `scripts/validate-agent-factory.js`.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts</automated>
    <fails_when>non-zero exit, "0 passed" in the summary line, or a failure naming the not-a-board-column expectation</fails_when>
    <automated>grep -vE '^\s*(//|\*|/\*)' scripts/validate-agent-factory.ts | grep -c 'const boardColumnName ='</automated>
    <fails_when>any count other than 0 — the local helper declaration must be gone, leaving only the import</fails_when>
    <automated>grep -vE '^\s*(//|\*|/\*)' scripts/validate-agent-factory.ts | grep -cF 'startsWith("## "'</automated>
    <fails_when>any count other than 0 — the bare prefix-match membership spelling must not survive the extraction</fails_when>
    <automated>grep -rvE '^\s*(//|\*|/\*)' scripts/validate-agent-factory.ts scripts/board-model.ts | grep -c 'const kebab ='</automated>
    <fails_when>any count other than 1 — `kebab` must have exactly one declaration across the two files</fails_when>
    <automated>VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js</automated>
    <fails_when>non-zero exit, or the absence of the all-checks-passed line the validator prints on success</fails_when>
    <automated>npm run typecheck && npm run build && npm run check:build-parity</automated>
    <fails_when>non-zero exit, any `error TS` line, or a `BUILD PARITY FAILED` line</fails_when>
  </verify>
  <acceptance_criteria>
    - A `NN-RED-baseline.txt` captured **before** the edit records the validator's verdict over all eight `scripts/fixtures/*` trees, and the matching `NN-GREEN-proof.txt` records the post-edit sweep with a block-by-block comparison.
    - Every difference between the two sweeps is classified in the proof file as one of the two named deviations or as a regression; there is no unclassified difference.
    - `scripts/validate.test.ts` gains a case pinning the WR-03 counterexample through the import path and a case asserting the bare prefix-match spelling is absent.
    - The two board↔ticket message strings are byte-identical to their pre-extraction text, verified by a diff of the region.
    - `kebab` has exactly one declaration across `scripts/validate-agent-factory.ts` and `scripts/board-model.ts`.
    - The plan summary records that D-06's "first import from `scripts/`" claim is factually wrong and names the three pre-existing `scripts/` imports.
  </acceptance_criteria>
  <done>The tree holds one board grammar, the validator imports it, both defect fixes are named as deviations rather than discovered later, and the eight-fixture verdicts are proven unchanged except where a deviation says otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite both board twins' documentation comment, with the column table frozen</name>
  <files>plans/board.md, agent-factory/seed/plans/board.md</files>
  <read_first>
    - `plans/board.md:1-80` and `agent-factory/seed/plans/board.md:1-80` — both twins in full: the comment at `:4-51`, the column table at `:58-72`, and the two prose lines where the twins already differ.
    - `scripts/check-imperative-lexicon.ts:661-664, 770-787` — the frozen anchor: the exact table-header literal, the throw when it is not found, and the docblock explaining that the row loop terminates at the first non-table line deliberately.
    - `scripts/check-imperative-lexicon.test.ts:96-118` — `partSize` and the eight derived part sizes including `CONTRACT_N` and `BOARD_COLUMN_N`; both are derived calls, so confirm the new contract file flows through rather than editing a literal.
    - `agent-factory/contracts/board.md` — the normative source this comment is the agent-facing copy of.
    - `.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md` D-04, D-07.
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Pitfall 10 and assumption A7 (no install test reads the seed board's bytes — falsified this session, so `install/install.test.ts` needs no change).
    - `install/install.test.ts:150-157, 4919` — confirm for yourself that the board references are user-owned fixture boards whose content is a literal string, not the seed board's bytes.
  </read_first>
  <action>
Rewrite the HTML comment block in `plans/board.md` and in `agent-factory/seed/plans/board.md` so an
agent editing the board reads the canonical forms where it is already looking.

The rewritten block states, in one short section each: the heading form — exactly three legal
suffixes, and that any other `##` suffix opens no column; the row form — a bullet, a bracketed ID, a
space, a title, then an optional opaque parenthetical after a two-space gap and an optional opaque
trailer after it, with nothing inside either interpreted; the ID form and the epic/feature second
class; the `_Updated:` line form; and a pointer to `agent-factory/contracts/board.md` as the
normative spec. Keep it short — this comment is already 48 lines and CLAUDE.md's minimal-substrate
rule applies to every machine-read file in the kit.

Keep the mini-board example inside the comment **indented**, as it is today, and keep the example row
generic. The parser blanks comment spans regardless of indentation now, so the indentation is no
longer load-bearing — say so in one sentence rather than leaving a reader to assume it still is.

**Freeze the column table.** Do not rename a column, do not reorder the table, do not change a header
cell, and do not move the table. `scripts/check-imperative-lexicon.ts` anchors a derived part on the
exact header literal and throws when it moves, and that throw becomes a derivation refusal that reds
the gate. Edit only the comment above the table. If a column name ever genuinely must change it moves
in three places at once — the table, the `##` heading, and `factory.config.json#wip_limits` — and
that is a different plan.

Keep the twins in sync. They differ today by exactly two prose lines; after the rewrite the comment
block must be byte-identical between them and the two known prose differences must be the only
remaining ones. Assert that rather than eyeballing it: diff the two files and check the difference
set is exactly the expected pair.

Verify, do not edit, the two derived cardinalities. `CONTRACT_N` and `BOARD_COLUMN_N` at
`scripts/check-imperative-lexicon.test.ts:110` and `:113` are `partSize` calls, so the `contracts`
part growing by one when `agent-factory/contracts/board.md` landed should flow through with no edit.
Confirm that by running the gate and its test; only if a literal turns out to consume the value does
a change land here, and then it lands as a recorded decision naming which derivation moved and why —
never as a bumped constant.

Leave `install/install.ts` and `install/uninstall.ts` untouched. D-16 rules out an installer shim,
and the seed board is already an installed file, so the rewrite reaches installs through the existing
copy path with no registration.
  </action>
  <verify>
    <automated>npm run check:imperative-lexicon</automated>
    <fails_when>non-zero exit, or a `DERIVATION_REFUSALS` entry naming `boardColumns`, or a FAIL line naming either board twin</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-imperative-lexicon.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>diff <(sed -n '/^| Column | Entry means/,/^$/p' plans/board.md) <(sed -n '/^| Column | Entry means/,/^$/p' agent-factory/seed/plans/board.md)</automated>
    <fails_when>any diff output — the frozen column table must be identical in both twins and unchanged from its committed form</fails_when>
    <automated>git diff --unified=0 -- plans/board.md agent-factory/seed/plans/board.md > /tmp/gsd32-board-diff.txt; echo "git_exit=$?"</automated>
    <fails_when>the printed line is not `git_exit=0` — a non-zero exit means the diff was never measured, so the next command would grep an empty or stale file</fails_when>
    <automated>grep -cE '^[-+]\| ' /tmp/gsd32-board-diff.txt || true</automated>
    <fails_when>any printed count other than 0 — a table row was added, removed or altered by this task</fails_when>
    <automated>npm run build && node -e "import('./scripts/board-model.js').then(m=>{const fs=require('node:fs');for(const p of ['plans/board.md','agent-factory/seed/plans/board.md']){const b=m.parseBoard(fs.readFileSync(p,'utf8'));if(b.columns.length!==13)throw new Error(p+' columns='+b.columns.length);if(b.unparsed.length)throw new Error(p+' unparsed='+b.unparsed.length);}console.log('both twins: 13 columns, 0 unparsed')})"</automated>
    <fails_when>non-zero exit, or the absence of `both twins: 13 columns, 0 unparsed` in the output</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' install/install.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - Both twins' comment blocks state the heading form, the row form including the opaque parenthetical and trailer, the ID form with the epic/feature second class, the `_Updated:` form, and a pointer to `agent-factory/contracts/board.md`.
    - The comment block is byte-identical between the two twins, and `diff plans/board.md agent-factory/seed/plans/board.md` yields exactly the two known prose differences and nothing else.
    - The column table's header row, its 13 rows and its position are unchanged in both twins, proven by a git diff showing no changed table line.
    - `npm run check:imperative-lexicon` exits 0 with no derivation refusal naming `boardColumns`, and `scripts/check-imperative-lexicon.test.ts` passes with `CONTRACT_N` and `BOARD_COLUMN_N` still derived by `partSize` rather than edited.
    - Both twins parse to 13 columns and zero unparsed lines.
    - `install/install.test.ts` passes unchanged and `git status --porcelain install/` prints nothing.
  </acceptance_criteria>
  <done>An agent editing either board reads the canonical forms in the file it is already in, both twins agree, and the table another gate derives from has not moved a byte.</done>
</task>

<task type="auto">
  <name>Task 3: Phase close — the whole gate set green and the absence of change asserted</name>
  <files>scripts/validate-agent-factory.ts, scripts/validate.test.ts</files>
  <read_first>
    - `.github/workflows/ci.yml:70-110, 170-210, 230-520` — every gate step CI runs, so the local sweep matches what CI will run rather than a subset.
    - `package.json` scripts block — the full `check:*`, `freshness*` and build set.
    - `.planning/REQUIREMENTS.md` — the eight DASH requirement statements, to confirm each one has landed somewhere in the phase.
    - `.planning/phases/32-board-projector-cli-dashboard/32-VALIDATION.md` — the per-task verification map to mark up.
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Package Legitimacy Audit — the planner obligation to assert the absence of dependency change rather than merely refrain from adding one.
  </read_first>
  <action>
Run the whole gate set the way CI runs it and record the result, then assert the two absences this
phase promised.

The sweep: `npm run build`, `npm run typecheck`, `npm run check:build-parity`, `npm run freshness`,
`npm run freshness:context`, the four documentation gates
(`check:imperative-lexicon`, `check:banned-claims`, `check:public-docs`, `check:claim-anchors`),
`node scripts/check-foundation-guards.js`, `node scripts/check-kit-refs.js`,
`npm run check:nul-bytes`, `npm run check:dashboard-readonly`, `VALIDATE_KIT_ROOT=. node
scripts/validate-agent-factory.js`, and `npx vitest run --exclude '**/scripts/e2e/**'`. Never run
bare `npm test` — it is `vitest run` with no exclude and triggers the live claude-CLI end-to-end lane,
which spends tokens and can hang.

The first absence: no runtime dependency. `package.json` still has no `dependencies` key and
`package-lock.json` is byte-unchanged across the entire phase. Assert it with a command, in the same
fail-closed posture `check:build-parity` already uses, rather than concluding it from the fact that
nobody remembers adding one.

The second absence: no installer change. `git status --porcelain install/` prints nothing, because
D-16 rules out an installer shim this phase.

Then close the requirement trail. Walk the eight DASH statements in `.planning/REQUIREMENTS.md` and,
for each, name in the plan summary the plan and the automated command that demonstrates it. A
requirement with no named command is not closed, and saying so in the summary is cheaper than
discovering it in a verification round.

Finally, mark up `32-VALIDATION.md`: set each row's status from its actual run, and set
`wave_0_complete: true` once every Wave 0 artifact in its checklist exists on disk. Record the
Windows `fs.watch` row as `UNKNOWN - verify` pending Phase 33 / CAP-02 — this phase asserts nothing
about it, and marking it anything else would be a fabricated gate result.
  </action>
  <verify>
    <automated>npm run build && npm run typecheck && npm run check:build-parity && npm run freshness && npm run freshness:context</automated>
    <fails_when>non-zero exit, any `error TS` line, a `BUILD PARITY FAILED` line, or a freshness drift report</fails_when>
    <automated>npm run check:imperative-lexicon && npm run check:banned-claims && npm run check:public-docs && npm run check:claim-anchors && npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or any FAIL line in the combined output</fails_when>
    <automated>node scripts/check-foundation-guards.js && node scripts/check-kit-refs.js && VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js && npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit, or any FAIL line in the combined output</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or any test file reported as failed</fails_when>
    <automated>git diff --exit-code -- package-lock.json && node -e "process.exit(require('./package.json').dependencies===undefined?0:1)" && git status --porcelain install/</automated>
    <fails_when>non-zero exit from either check, or any path under `install/` printed by the status call</fails_when>
  </verify>
  <acceptance_criteria>
    - Every command in the sweep exits 0, and the plan summary records each command with its result.
    - `package.json` has no `dependencies` key and `git diff --exit-code -- package-lock.json` exits 0.
    - `git status --porcelain install/` prints nothing.
    - The plan summary names, for each of DASH-01 through DASH-08, the plan and the automated command that demonstrates it.
    - `32-VALIDATION.md` has every row's status set from an actual run, `wave_0_complete: true`, and the Windows `fs.watch` row recorded as `UNKNOWN - verify` pending Phase 33 / CAP-02.
    - No gate result is recorded that was not actually run — an unrun command is written as `UNKNOWN - verify`, never as a pass.
  </acceptance_criteria>
  <done>The full gate set is green, both promised absences are asserted by command rather than assumed, and every DASH requirement has a named command behind it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the deleted validator code → the fixture-driven tests | The only evidence a regression would leave, because the live tree's validator board path is dead code |
| the seed board's column table → `check-imperative-lexicon`'s derived part | Shared state between this phase and an existing gate that throws when its anchor moves |
| the installed seed board → every user's repository | The seed twin is what an install copies, so its bytes reach machines this repository never sees |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-24 | Tampering | a silent behaviour regression in `checkTickets()` hidden by the vacuity guard | high | mitigate | An eight-fixture RED baseline captured before the edit and a block-by-block comparison after it, with every difference classified as a named deviation or a regression and no unclassified third category |
| T-32-25 | Denial of Service | the imperative-lexicon gate throwing a derivation refusal because the frozen table moved | medium | mitigate | The table header, its 13 rows and its position are frozen; a git-diff criterion asserts no table line changed, and the gate plus its test are both run |
| T-32-21 | Tampering | a second spelling of one predicate surviving the extraction | high | mitigate | Negative-grep criteria over the comment-stripped validator source for the local helper declaration and the bare prefix-match spelling, plus a single-declaration criterion for `kebab` across both files |
| T-32-26 | Repudiation | a reworded error message quietly changing a behaviour two tests assert | medium | mitigate | The two messages are kept byte-identical, verified by a diff of the region, and `scripts/validate.test.ts` passes with unchanged expectations |
| T-32-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists in this phase. Task 3 asserts the absence by command: `git diff --exit-code -- package-lock.json` plus a `dependencies === undefined` check, the same fail-closed posture `check:build-parity` uses |
| T-32-27 | Repudiation | a fabricated gate result in the phase-close record | high | mitigate | Every recorded result comes from a command that was actually run; an unrun command is recorded as `UNKNOWN - verify`, never as a pass (CLAUDE.md C8) |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Files (modified)**
- `scripts/validate-agent-factory.ts` / `scripts/validate-agent-factory.js` — two helpers and `kebab` deleted, one import added
- `scripts/validate.test.ts` — the WR-03 counterexample pinned through the import path, plus the absence assertion
- `plans/board.md` and `agent-factory/seed/plans/board.md` — the documentation comment rewritten, the column table frozen
- `scripts/check-imperative-lexicon.test.ts` — verified, edited only if a derived value turned out to be consumed as a literal
- `.planning/phases/32-board-projector-cli-dashboard/32-VALIDATION.md` — statuses set from real runs, `wave_0_complete: true`

**Files (new)**
- a `NN-RED-baseline.txt` / `NN-GREEN-proof.txt` pair recording the eight-fixture validator sweep before and after the extraction

**Symbols removed — `scripts/validate-agent-factory.ts`**
the two locally declared column helpers at `:735-741` · the local `kebab` at `:250-256`

**Symbols imported — `scripts/validate-agent-factory.ts`**
`boardColumnName` · `boardHasColumn` · `parseBoard` · `kebab` from `./board-model.js`

**No CLI flag, npm script or dependency changes.**
</artifacts_this_phase_produces>

<planner_assumptions>
- **PATTERNS flag 2 resolved** — `install/install.ts` and `install/uninstall.ts` are treated as
  untouched. D-16 rules out an installer shim, the dashboard is invoked from the repository, and the
  seed board already reaches installs through the existing copy path. No entry is needed and none is
  added.
- **PATTERNS flag 3 resolved** — D-06's claim that `board-model.js` is the validator's first import
  from `scripts/` is factually wrong; `kit-model.js`, `checkpoints.js` and `context-io.js` are
  already imported at `:59`, `:64` and `:71`. Plan prose corrected; the work is unchanged.
- **RESEARCH assumption A7 falsified** — no install test reads the seed board's bytes; the eight
  references in `install/install.test.ts` all concern a user-owned fixture board whose content is a
  literal string. `install.test.ts` therefore needs no change, and task 2 re-runs it to confirm.
- **`CONTRACT_N` handling** — it is already a `partSize("contracts")` call, so adding
  `agent-factory/contracts/board.md` should flow through with no edit. Task 2 verifies rather than
  assumes; if a literal turns out to consume the value, the change lands as a recorded decision
  naming which derivation moved, never as a bumped constant.
- **DASH-01 edge probes** — the deterministic pass returned boundary, adjacency, empty, ordering and
  precision rows for DASH-01. All five are resolved in plan 32-02's `must_haves` (bounds boundaries,
  the adjacency of identically normalizing headings, empty input, on-disk ordering, and the integer
  and unit contract). This plan closes the remaining adjacency case mechanically: the WR-03
  counterexample proves prefix-adjacent column names do not match.
</planner_assumptions>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts` green with unchanged expectations
- `VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js` exits 0
- `npm run check:imperative-lexicon` exits 0 with no derivation refusal naming `boardColumns`
- Both board twins parse to 13 columns and zero unparsed lines
- The full CI-equivalent gate sweep in task 3 exits 0 at every step
- `git diff --exit-code -- package-lock.json` exits 0; `package.json` has no `dependencies` key; `git status --porcelain install/` prints nothing
</verification>

<success_criteria>
`scripts/board-model.ts` is the only board grammar in the tree, the validator imports it and its two
board↔ticket messages are byte-unchanged, the eight-fixture verdicts differ only by two named
deviations, both board twins tell agents the canonical forms with their column table frozen, and the
phase closes with every gate green and both promised absences asserted by command.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-08-SUMMARY.md` when done
</output>

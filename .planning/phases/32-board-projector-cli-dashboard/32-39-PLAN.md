---
phase: 32-board-projector-cli-dashboard
plan: 39
type: execute
wave: 2
gap_closure: true
depends_on: ["32-38"]
files_modified:
  - scripts/board-model.ts
  - scripts/board-model.js
  - scripts/board-model.test.ts
  - scripts/board-read.ts
  - scripts/board-read.js
  - scripts/board-read.test.ts
  - scripts/board-dashboard.ts
  - scripts/board-dashboard.js
  - scripts/board-dashboard.test.ts
  - scripts/check-foundation-guards.test.ts
  - agent-factory/contracts/board.md
  - .planning/phases/32-board-projector-cli-dashboard/32-39-RED-baseline.txt
  - .planning/phases/32-board-projector-cli-dashboard/32-39-GREEN-proof.txt
autonomous: true
requirements: [DASH-03, DASH-06, DASH-07, DASH-08]
estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The exported presence discriminant names exactly what its lookup measured: the arm reached when an admitted document declares the identifier the board wrote is named for that fact and not for a stem nobody asked about, and the rename lands at EVERY site in the same commit — the site set derived by a tree-wide search rather than recalled, with the count asserted (D-11, D-19)."
    - "The rename is proved to change no published shape: `schemaVersion` stays 2 and `scripts/fixtures/board-snapshot/expected-snapshot.json` is byte-identical before and after, because the presence kinds are an internal derivation and not a field of the document a consumer parses (D-19)."
    - "A dial naming a wip limit for a column spelled like a prototype member reaches the `column-missing` conflict arm like any other column name, rather than vanishing into `Object.prototype`'s setter — the projector reports every conflict and resolves none (D-10, D-23)."
    - "A snapshot value or key spelled like a prototype member survives the serializing chokepoint as an own property of the published document rather than being silently dropped on the way to stdout (DASH-07, D-18)."
    - "The two prototype-lookup sites the review names as one class — the ticket-reader census exemption and the toolchain check-script register — agree: both ask own-property membership, the set of raw reads is DERIVED from the file rather than listed, and the derived count is asserted so a fourth site added later cannot stay a raw read (DASH-06, D-21)."
    - "Every change in this plan is proved RED-first against a case that fails before it and passes after, and every fix's SIBLING arm is exercised in the same run — a fix applied in one arm and not the one beside it is this phase's most-repeated defect across three rounds."
    - { statement: "The live claude-CLI end-to-end lane is NOT run by this plan and its state stays `UNKNOWN - verify`", verification: backstop }
  prohibitions:
    - statement: "Never rename the discriminant at some sites and leave others — derive the site set with a search that reads control-byte-classified files, and assert the count."
      status: pending
      verification: judgment
    - statement: "Never change the published snapshot shape while closing a naming defect; if the golden fixture moves, stop and report rather than regenerate."
      status: pending
      verification: judgment
    - statement: "Never close a prototype-lookup site by adding a key filter; ask own-property membership, which is the property the lookup means."
      status: pending
      verification: judgment
    - statement: "Never flip a REQUIREMENTS.md checkbox or a ROADMAP.md phase status in this plan."
      status: pending
      verification: judgment
  artifacts:
    - "scripts/board-model.ts (+ `.js`) and `scripts/board-model.test.ts` — the presence discriminant renamed at every derived site, with the two-sided pin moved in the same commit"
    - "scripts/board-read.ts (+ `.js`) and `scripts/board-read.test.ts` — the dial's limit map built without a prototype, and a case reaching `column-missing` for a prototype-spelled column"
    - "scripts/board-dashboard.ts (+ `.js`) and `scripts/board-dashboard.test.ts` — the serializing chokepoint's accumulator built without a prototype, and a case proving a prototype-spelled key reaches the published document"
    - "scripts/check-foundation-guards.test.ts — own-property membership at every derived raw-read site, with the site count asserted"
    - ".planning/phases/32-board-projector-cli-dashboard/32-39-RED-baseline.txt — the three findings measured live before any change, each with the observable that proves it"
    - ".planning/phases/32-board-projector-cli-dashboard/32-39-GREEN-proof.txt — the same measurements after, the derived site counts, the golden-fixture byte comparison and the sibling-arm rows"
  key_links:
    - "the derived rename site set -> the asserted count: a set literal that rots while the suite is green is this repository's second recorded systemic failure class"
    - "`configView`'s limit map -> `joinSnapshot`'s `column-missing` arm: a key that never lands is a conflict that is resolved by disappearing"
    - "`scrub`'s accumulator -> `writeDocument` -> the `--json` document: a key dropped at the accumulator never reaches the consumer"
    - "the ticket-census exemption lookup -> the toolchain-register lookup: two copies of one rule, now asked the same question and counted"
  deferred: []
---

<objective>
Close the three findings `32-REVIEW.md` recorded as Info and `32-REVIEW-FIX.md` explicitly declined
to touch — IN-01, IN-02 and IN-03 — all of them in files this phase has already opened, and one of
them (IN-03) a site the fix report itself flags as now DISAGREEING with its own sibling.

| Finding | Where it is now | What is wrong | Task |
|---|---|---|---|
| IN-01 | `scripts/board-model.ts:1241` (`TICKET_PRESENCE_KINDS`), `:1263`, `:1328`, `:1363`; `scripts/board-model.js` (4 sites); `scripts/board-model.test.ts` (8 sites) — counts measured 2026-09-16 with a search that reads control-byte-classified files | `admitted-under-its-stem` is returned whenever `byId.has(id)`, which says nothing about the record's stem; for a document declaring an identifier different from its own stem the arm returns that name carrying a record whose stem is something else. The name asserts a fact the arm never measured, and the array is exported and pinned two-sided, so the name is contract surface (D-11, D-19). | Task 1 |
| IN-02 | `scripts/board-read.ts:1148` (`configView`'s `const limits: Record<string, number> = {}`) and `scripts/board-dashboard.ts:329` (`scrub`'s `const out: Record<string, unknown> = {}`) | `JSON.parse` creates an own `__proto__` property and `Object.entries` yields it, but assigning it onto a plain object hits `Object.prototype`'s setter and is a silent no-op. The key never lands, so `joinSnapshot`'s `column-missing` arm never sees it — a conflict resolved by disappearing, against D-10's rule that disagreement is surfaced and not resolved. The same shape at the serializing chokepoint drops the key on the way to stdout (D-18). | Task 2 |
| IN-03 | `scripts/check-foundation-guards.test.ts:12110`, `:12348`, `:12621` — three raw reads of `TOOLCHAIN_CHECK_SCRIPTS`, count measured 2026-09-16 | `TOOLCHAIN_CHECK_SCRIPTS[name] !== undefined` is the same prototype-lookup class as WR-04, which commit `7a3ae592` fixed in `scripts/validate.test.ts` with `Object.hasOwn`. `32-REVIEW-FIX.md` states plainly that the two copies now DISAGREE and recommends folding this in. | Task 3 |

Purpose: D-11 makes the reader's answers a discriminated union, so a discriminant's spelling is the
thing a reader reasons about; D-19 makes the exported surface a stability promise, so a name pinned
two-sided is part of it. D-10 makes `conflicts[]` the place a disagreement is surfaced rather than
resolved, and a limit key that evaporates is resolved by the most silent means available. D-21 makes
the read-only guard's derivations mechanical rather than asserted, and a lookup that answers "exempt"
for a name nobody exempted is a mechanism answering from `Object.prototype` instead of from a
decision. D-23 puts the dial read in `scripts/board-read.ts`, which is why the limit map is that
file's problem and not the pure grammar's.

Output: one honest discriminant name landed at a derived site set; two accumulators that cannot lose
a key to a prototype; and one prototype-lookup rule asked the same way at every site that asks it.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` section to lift. The
prototype-spelled boundary inputs and the derived-count floors are authored into `must_haves.truths`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW-FIX.md
@.planning/phases/32-board-projector-cli-dashboard/32-38-GREEN-proof.txt
@.planning/phases/32-board-projector-cli-dashboard/32-17-SUMMARY.md
</context>

<assumption_delta_decision>
**Signalled family:** unchanged from plan `32-38` — `gsd_run query assumption-delta scan 32 --json`
returns the same three historical signals (`second`, `alongside`, `parameterized`) read out of this
phase's earlier plan prose.

**Noun now primary:** the ticket *presence* answer and the dial *limit* map, both still singular.

**Decision: `no-change`.** Renaming a discriminant and removing a prototype from two accumulators
introduces no second authority, no configurable variant and no fallback.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The presence discriminant names what its lookup measured, at every derived site</name>

  <read_first>
    - `scripts/board-model.ts` lines 1240-1290 — `TICKET_PRESENCE_KINDS` and the `TicketPresence`
      union members
    - `scripts/board-model.ts` lines 1325-1390 — `presenceOf`'s `byId` lookup (`:1326-1328`) and
      `presenceActual`'s first case (`:1363`) with the docblock paragraph explaining why that arm has
      no sentence
    - `scripts/board-model.test.ts` lines 2340-2520 — the two-sided pin (`:2354`), the per-arm cases
      (`:2398`, `:2440`) and the sentence table (`:2501-2512`), plus whatever plan `32-38` added
    - `scripts/fixtures/board-snapshot/expected-snapshot.json` — the golden the rename must not move
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` § `IN-01` — the finding and its
      two suggested names
    - `.planning/phases/32-board-projector-cli-dashboard/32-38-GREEN-proof.txt` — plan `32-38`'s
      per-branch input rows, which this task must leave reaching the same branches
  </read_first>

  <files>scripts/board-model.ts, scripts/board-model.js, scripts/board-model.test.ts, agent-factory/contracts/board.md</files>

  <behavior>
    - Given a board identifier some admitted document declares, the arm returned is named for the declaration the lookup measured.
    - Given the exported kind array, its two-sided pin lists the new spelling and reds if a member is added or removed.
    - Given a tree-wide search for the old spelling across tracked source, contract and compiled output, zero occurrences remain.
    - Given the pristine snapshot fixture, the published document is byte-identical to the golden before and after the rename.
  </behavior>

  <action>
DERIVE THE SITE SET BEFORE EDITING ANYTHING. Search every tracked file — source, compiled output,
contract, and the kit's own markdown — for the old spelling, using a search mode that reads files a
content classifier calls binary. This is not optional pedantry: `32-REVIEW-FIX.md` records that plain
matching returned ZERO hits in `scripts/board-dashboard.test.ts` because a single control byte made
the classifier treat it as program text, and two live fakes were found only by forcing text mode.
Record the per-file occurrence counts in the RED baseline, and assert after the rename that the same
search finds zero and that the new spelling's per-file counts sum to the number the baseline recorded.

RENAME TO `admitted-under-this-id`. Deviation from the review, stated rather than silent: the review
offered `admitted` and `declared-by-a-document`. `admitted` alone is ambiguous against the sibling arm
`admitted-under-another-id`, which is also an admitted record; `declared-by-a-document` loses the
symmetry a reader uses to tell the pair apart. The lookup is `byId.get(id)`, which measures that some
admitted record declares THIS identifier — so the chosen name states exactly the lookup's subject and
reads as the sibling's converse. Record this reasoning in the summary.

MOVE THE PIN IN THE SAME COMMIT. `TICKET_PRESENCE_KINDS` is exported and pinned two-sided in
`scripts/board-model.test.ts`; the union member, the construction site, the consuming `switch` case
and the pin all move together or the build breaks, which is the point. Update the docblock sentence
that explains why the arm carries no conflict sentence, so the prose and the name agree.

CHECK THE CONTRACT FOR THE CONCEPT, NOT ONLY THE STRING. A tree-wide search measured zero literal
occurrences of the old spelling in `agent-factory/contracts/board.md` on 2026-09-16. Read the
presence section anyway and confirm the prose does not describe the arm by the fact the old name
asserted; if it does, correct it in this commit and note it. Plan `32-38`'s contract-agreement
assertion must still pass afterwards.

PROVE THE PUBLISHED SHAPE DID NOT MOVE. Run the CLI over `scripts/fixtures/board-snapshot` before and
after and compare the output against the committed golden byte for byte, and record the
`schemaVersion` both times. A naming change that moves the golden is a shape change wearing a rename,
and this phase has already spent a human decision on one deliberate version bump — a second,
accidental one is exactly what D-19's version gate exists to catch.

Rebuild so `scripts/board-model.js` is a faithful build of its source.
  </action>

  <acceptance_criteria>
    - `32-39-RED-baseline.txt` records the pre-rename per-file occurrence counts of the old spelling, obtained with a search that forces text mode, across source, compiled output and markdown.
    - `32-39-GREEN-proof.txt` records zero remaining occurrences of the old spelling and the per-file counts of the new one, and states that the two totals are equal.
    - `scripts/board-model.test.ts`'s two-sided pin lists the new spelling and the array's member count is asserted.
    - `32-39-GREEN-proof.txt` records the CLI run over `scripts/fixtures/board-snapshot` before and after with the golden comparison result and the `schemaVersion` value both times.
    - `32-39-GREEN-proof.txt` records whether `agent-factory/contracts/board.md` needed a prose correction, and the correction if so.
    - The summary records the chosen name, the two names the review suggested, and why the chosen one states the lookup's subject.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts scripts/board-tracer.test.ts</automated>
    <fails_when>non-zero exit, or `0 passed` in the summary line, or a case count below the number `32-38-GREEN-proof.txt` recorded for these two files</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or `BUILD PARITY FAILED` in the output</fails_when>
    <automated>node scripts/board-dashboard.js scripts/fixtures/board-snapshot --once --json &gt; /tmp/p39-t1.json; node -e "const a=require('fs').readFileSync('/tmp/p39-t1.json','utf8');const o=JSON.parse(a);console.log('schemaVersion='+o.snapshot.schemaVersion)"</automated>
    <fails_when>non-zero exit, or a printed line other than `schemaVersion=2`</fails_when>
  </verify>

  <done>The discriminant states what its lookup measured, every derived site moved in one commit, and the published document is byte-identical to the golden it was before.</done>

  <reversibility rating="reversible">An internal discriminant spelling with no consumer outside its own module and test; the published document is asserted unchanged.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A prototype-spelled key reaches the conflict arm and the published document</name>

  <read_first>
    - `scripts/board-read.ts` lines 1130-1165 — the dial read (`:1140`) and `configView` (`:1147`)
      with its `const limits` accumulator (`:1148`)
    - `scripts/board-model.ts` — `joinSnapshot`'s `column-missing` arm: which map it iterates, what it
      compares against the board's headings, and the conflict it composes (D-10's closed kind set)
    - `scripts/board-dashboard.ts` lines 318-385 — `scrub` (`:325`) with its `out` accumulator
      (`:329`) and `writeDocument` (`:382`), the one place a serialized document reaches stdout
    - `scripts/board-read.test.ts` — the existing `configView` / dial cases, to place the new one
      beside its siblings rather than in a new file
    - `scripts/board-dashboard.test.ts` — the existing `scrub` / `writeDocument` cases; read it with a
      search that forces text mode, because a content classifier calls this file program text and
      plain matching has already returned zero hits on strings that are present
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` § `IN-02` — the finding and the
      two-line remedy it names
  </read_first>

  <files>scripts/board-read.ts, scripts/board-read.js, scripts/board-read.test.ts, scripts/board-dashboard.ts, scripts/board-dashboard.js, scripts/board-dashboard.test.ts</files>

  <behavior>
    - Given a dial whose wip limits name a column spelled like a prototype member and a board without that column heading, a `column-missing` conflict is raised for it.
    - Given the same dial and a board that DOES carry that heading, no `column-missing` conflict is raised for it and the limit participates in the claimed/counted/limit comparison like any other.
    - Given a dial whose wip limits name ordinary columns, the conflict set is unchanged from its committed behaviour.
    - Given a snapshot value or key spelled like a prototype member, the published `--json` document carries it as an own property a consumer's parse recovers.
    - Given the pristine snapshot fixture, the published document still matches the committed golden.
  </behavior>

  <action>
MEASURE BOTH DEFECTS LIVE BEFORE FIXING EITHER. Build a disposable tree whose dial names a wip limit
for a column spelled like a prototype member, run the CLI against it, and record the conflict set the
document carried — the absence of the expected conflict is the finding, so record the full list, not
just the absence. Do the same for the serializing chokepoint: construct a snapshot value carrying a
prototype-spelled key, drive the write path, and record what the produced document carried. Write
both into the RED baseline with the exact observable each rests on.

REMOVE THE PROTOTYPE AT BOTH ACCUMULATORS — the dial's limit map in `scripts/board-read.ts` and the
scrub accumulator in `scripts/board-dashboard.ts`. Both are build-and-return accumulators with no
inherited member ever read from them, so a prototype-free object is the shape that matches what the
code means. Keep the surrounding types as they are; a cast at the construction site is the whole
change.

ASK THE SIBLING ARM, WHICH IS WHERE THIS PHASE KEEPS LOSING ROUNDS. For the dial: the arm BESIDE
`column-missing` is the case where the heading DOES exist, so assert that a prototype-spelled column
that is present on the board produces no `column-missing` conflict and does reach the
claimed/counted/limit comparison — a fix that makes every prototype-spelled key a conflict has
replaced one wrong answer with another. For the chokepoint: the sibling of the key path is the VALUE
path, so assert both a prototype-spelled key and a prototype-spelled string value survive, and assert
the sanitizing rule still applies to both — the accumulator change must not create a route that
bypasses `sanitizeCell`.

PROVE THE CONSUMER RECOVERS IT. The assertion that matters for DASH-07 is not that the object has the
key in this process; it is that a consumer parsing the printed document gets an own property. Capture
the document from a spawned run, parse it, and assert own-property membership on the parsed result.

DERIVE THE ACCUMULATOR SET RATHER THAN TRUSTING THE REVIEW'S TWO. Search both modules for other
build-and-return accumulators that take keys from parsed content, list what you find in the GREEN
proof, and say for each whether it can receive a content-derived key. The review named two; this
repository's history is that the third site is the one nobody derived.

CONFIRM THE GOLDEN FIXTURE IS UNMOVED and rebuild both `.js` twins.
  </action>

  <acceptance_criteria>
    - `32-39-RED-baseline.txt` records, for the dial, the full conflict list the pre-fix run produced over the prototype-spelled-column tree, and states which expected conflict was absent.
    - `32-39-RED-baseline.txt` records, for the chokepoint, the document the pre-fix write path produced and what it lacked.
    - `scripts/board-read.test.ts` gains a case asserting a `column-missing` conflict for a prototype-spelled column absent from the board, and a sibling case asserting no such conflict when the heading is present.
    - `scripts/board-dashboard.test.ts` gains a case asserting that a spawned run's parsed document carries a prototype-spelled key as an own property, and a sibling case asserting the sanitizing rule still applies to prototype-spelled keys and values.
    - `32-39-GREEN-proof.txt` lists every build-and-return accumulator found in the two modules with a verdict on whether it can receive a content-derived key.
    - `32-39-GREEN-proof.txt` records the golden-fixture comparison over `scripts/fixtures/board-snapshot` as matching.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-read.test.ts scripts/board-dashboard.test.ts</automated>
    <fails_when>non-zero exit, or `0 passed` in the summary line, or either of the two new sibling cases absent from the printed case list</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or `BUILD PARITY FAILED` in the output</fails_when>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit, or a passed-case count below 171, or a `PREMISE` case listed among the failures — the accumulator change must not move the read-only guard</fails_when>
  </verify>

  <done>A prototype-spelled column raises its conflict, a prototype-spelled key reaches a consumer's parse, both sibling arms are asserted, and the accumulator set was derived rather than taken from the review's two.</done>

  <reversibility rating="reversible">Two construction-site casts plus tests; both accumulators are build-and-return objects with no inherited member read from them.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: One prototype-lookup rule, asked the same way at every site that asks it</name>

  <read_first>
    - `scripts/check-foundation-guards.test.ts` lines 12055-12120 — `TOOLCHAIN_CHECK_SCRIPTS`
      (`:12061`), `classifyCheckScriptTargets` (`:12099`) and the raw read at `:12110`
    - `scripts/check-foundation-guards.test.ts` lines 12340-12360 and 12610-12630 — the other two raw
      reads measured 2026-09-16 (`:12348`, `:12621`)
    - `scripts/validate.test.ts` lines 1895-1920 and 2025-2070 — the sibling site commit `7a3ae592`
      fixed: `censusOver`'s own-property filter (`:1912`), its docblock paragraph, and the cases that
      prove it discriminates (`:2030-2067`)
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` § `IN-03` and § `WR-04` — the
      statement that the two are one class
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW-FIX.md` § `WR-04` and the
      `Not fixed` section — the fix report's own record that the two copies now disagree, and its
      recorded finding that the defect is UNREACHABLE on the live scan set for a structural reason
  </read_first>

  <files>scripts/check-foundation-guards.test.ts</files>

  <behavior>
    - Given a check-script name that collides with a prototype member, the register lookup answers "not registered" rather than answering from the prototype.
    - Given the live check-script set, every classification and every reachability row is unchanged from its committed result.
    - Given the set of raw reads of the register derived from the file, its size is asserted and equals the number of own-property lookups after the change.
    - Given the structural premise the unreachability rests on, that premise is asserted rather than described.
  </behavior>

  <action>
DERIVE THE SITE SET, THEN ASSERT ITS SIZE. Three raw reads of the register were measured on
2026-09-16 at `:12110`, `:12348` and `:12621`. Do not take that number on trust — search the file for
every read of the register, list them in the RED baseline with line numbers, and after the change
assert in the suite that the count of own-property lookups equals the count the baseline recorded and
that no raw read remains. A hand-typed three is the set literal this repository has recorded as its
second systemic failure class; a derived three that is asserted is a measurement.

ASK OWN-PROPERTY MEMBERSHIP, exactly as `scripts/validate.test.ts` now does, so the two copies of one
rule stop disagreeing. The review's whole reason for listing this finding is that fixing one and
leaving the other produces two sites that answer the same question differently.

ASSERT THE PREMISE THE UNREACHABILITY RESTS ON, DO NOT DESCRIBE IT. `32-REVIEW-FIX.md` records that
the sibling site's defect is unreachable because every name it looks up ends in a fixed suffix; the
premise here is that every key in the scanned register is prefixed, so no script name can collide
with a prototype member. Turn that sentence into a failing assertion over the register's real keys,
with a message saying what a violation would mean. A premise stated in prose is a premise that stops
being true silently — this repository has recorded six harness-premise failures across four rounds.

PROVE THE CHANGE DISCRIMINATES, AT A REACHABLE INPUT. The fix report records that a first attempt to
prove this class asserted a reachable exploit, PASSED against the unfixed code, and had to be
rewritten. Do not repeat that: exercise the lookup at a bare prototype-member name directly, prove
the case reds against the pre-change lookup, and keep the live-set premise as a separate assertion
rather than conflating the two.

RECORD THE LIVE RESULT SET UNCHANGED. Capture the classification and reachability output for the live
check scripts before and after, and assert in the GREEN proof that the two are identical — an
own-property lookup that changed a live answer would mean a live key was being answered from the
prototype, which is a finding, not a pass.
  </action>

  <acceptance_criteria>
    - `32-39-RED-baseline.txt` lists every read of the register found in the file with its line number, and states the total.
    - `scripts/check-foundation-guards.test.ts` asks own-property membership at every listed site, and the suite asserts the count of such lookups equals the baseline's total with no raw read remaining.
    - The suite carries an assertion over the register's real keys establishing the prefix premise, with a message naming what a violation would mean.
    - The suite carries a case exercising the lookup at a bare prototype-member name, and `32-39-RED-baseline.txt` records that case failing against the pre-change lookup.
    - `32-39-GREEN-proof.txt` records the live classification and reachability output before and after and states that they are identical.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts scripts/validate.test.ts</automated>
    <fails_when>non-zero exit, or `0 passed` in the summary line, or the new prototype-member-name case absent from the printed case list</fails_when>
    <automated>node scripts/check-foundation-guards.js &amp;&amp; node scripts/validate-agent-factory.js</automated>
    <fails_when>non-zero exit, or a `FAIL` line in the output of either command</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or a passed-case count below 5137, or any test file reported as failed</fails_when>
  </verify>

  <done>Both copies of the prototype-lookup rule ask own-property membership, the site set is derived and its size asserted, and the premise the unreachability rests on is a failing assertion rather than a sentence.</done>

  <reversibility rating="reversible">A membership test inside a test file; the live answers are asserted identical before and after.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `factory.config.json` content → the dial view's limit map | a dial key is content a writer chooses, and a key spelled like a prototype member is the cheapest way for one to disappear |
| snapshot content → the serializing chokepoint → a `--json` consumer | a key dropped at the accumulator is a fact the consumer never learns was there |
| `package.json` script names → the toolchain register lookup | a name that answers "registered" from the prototype is an exemption granted by spelling rather than by a decision |
| any future `scripts/*.ts` → the ticket-reader census exemption | the same class one file over; the two sites must answer the same question |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-39-01 | Tampering | `configView`'s limit accumulator | medium | mitigate | The accumulator is built without a prototype, and a prototype-spelled column is asserted to reach `column-missing` and its sibling present-heading arm |
| T-32-39-02 | Information Disclosure | the serializing chokepoint's accumulator | medium | mitigate | The accumulator is built without a prototype and a spawned run's parsed document is asserted to carry a prototype-spelled key as an own property, with the sanitizing rule asserted still to apply |
| T-32-39-03 | Elevation of Privilege | the toolchain register lookup answering from `Object.prototype` | medium | mitigate | Own-property membership at every derived site, the site count asserted, and the prefix premise turned into a failing assertion |
| T-32-39-04 | Repudiation | a discriminant name asserting an unmeasured fact | medium | mitigate | The arm is renamed to state exactly what its lookup measured, the site set is derived with a text-forcing search, and the two-sided pin moves in the same commit |
| T-32-39-05 | Tampering | a rename silently moving the published shape | high | mitigate | The golden fixture is compared byte for byte before and after and `schemaVersion` is asserted unchanged; a move stops the task rather than regenerating the golden |
| T-32-39-06 | Denial of Service | a widened conflict rule making every prototype-spelled key a conflict | low | mitigate | The sibling present-heading arm is asserted to raise no conflict, so the fix cannot replace one wrong answer with another |

No package-manager install task exists in this plan — the repository ships zero runtime dependencies
and adds none — so no `T-32-SC` supply-chain row and no package-legitimacy checkpoint applies.
ASVS level 1; the one `high` row is dispositioned `mitigate`.
</threat_model>

## Artifacts this phase produces

Created or changed by this plan:

| Artifact | Kind | Note |
|---|---|---|
| `admitted-under-this-id` — the renamed presence discriminant | renamed exported contract value | replaces `admitted-under-its-stem` in `TICKET_PRESENCE_KINDS`, the `TicketPresence` union, `presenceOf`, `presenceActual` and the two-sided pin |
| `scripts/board-model.ts` (+ `.js`), `scripts/board-model.test.ts` | changed | the rename at every derived site, plus the golden-unchanged proof |
| `agent-factory/contracts/board.md` | possibly changed | only if the presence prose describes the arm by the fact the old name asserted |
| `scripts/board-read.ts` (+ `.js`) — `configView`'s prototype-free limit map | changed | a prototype-spelled column now reaches `column-missing` |
| `scripts/board-read.test.ts` — the prototype-spelled-column case and its present-heading sibling | new test cases | |
| `scripts/board-dashboard.ts` (+ `.js`) — `scrub`'s prototype-free accumulator | changed | a prototype-spelled key reaches the published document |
| `scripts/board-dashboard.test.ts` — the spawned-run own-property case and the sanitizing sibling | new test cases | |
| `scripts/check-foundation-guards.test.ts` — own-property lookups, the site count assertion, the prefix-premise assertion, the bare prototype-member case | changed | |
| `.planning/phases/32-board-projector-cli-dashboard/32-39-RED-baseline.txt` | new file | per-file rename counts, the two live IN-02 measurements, the derived register-read list |
| `.planning/phases/32-board-projector-cli-dashboard/32-39-GREEN-proof.txt` | new file | zero-remaining counts, accumulator inventory, golden comparisons, live-answers-identical record |
| `.planning/phases/32-board-projector-cli-dashboard/32-39-SUMMARY.md` | new file | plan summary |

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts scripts/board-read.test.ts scripts/board-dashboard.test.ts scripts/board-tracer.test.ts scripts/check-foundation-guards.test.ts scripts/validate.test.ts`
  — exit 0, case counts at or above the baseline's.
- `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness` — exit 0.
- `node scripts/validate-agent-factory.js` and `node scripts/check-foundation-guards.js` — exit 0.
- `npm run check:dashboard-readonly` — exit 0, 171 passed.
- `npm run check:nul-bytes` — exit 0; the prototype-member spelling and any control character are
  written as escape TEXT in every `.planning/` artifact this plan produces.
- `npx vitest run --exclude '**/scripts/e2e/**'` — exit 0, passed-case count at or above 5137.
- `npm test` is NOT run.
</verification>

<success_criteria>
The three findings the fix pass declined are closed in the files this phase already owns: the
presence discriminant states what its lookup measured and moved at a derived site set with the
published document proved byte-identical; a prototype-spelled column reaches its conflict arm and a
prototype-spelled key reaches a consumer's parse, each with its sibling arm asserted; and the two
copies of the prototype-lookup rule ask the same question at a derived, counted set of sites.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-39-SUMMARY.md` when done.
</output>

---
phase: 32-board-projector-cli-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - scripts/board-model.ts
  - scripts/board-model.js
  - scripts/board-model.test.ts
  - agent-factory/contracts/board.md
autonomous: true
requirements: [DASH-01, DASH-02, DASH-08]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every input line lands in exactly one of `columns[].rows`, `epicRows`, `updates`, `preamble`, `nonColumnSections[].lines`, `unparsed`, or the blank/heading set — the partition is total and disjoint (oracle invariant I1, D-24)."
    - "No `column`, `row`, `epicRow` or `update` carries a line number that falls inside any `<!-- … -->` span of the raw input (invariant I2, D-03)."
    - "A `<!--` with no closing delimiter blanks every remaining line to end of file, so a board renders visibly empty rather than partially and confidently (D-03)."
    - "A line matching the canonical `_Updated: YYYY-MM-DD by <actor>` prefix becomes an `updates[]` entry; a `_Updated:` line that does not match the prefix is `unparsed` when it sits inside a column section and `preamble` when it sits before the first column heading — the kit board's own `_Updated: <ISO date> by <role>_` placeholder is preamble, not a finding (D-03, D-24)."
    - "The chess board's 176 pre-column lines land in `preamble` and the dogfood board's four `## Notes (…)` sections land in `nonColumnSections[]`; neither inflates `unparsed[]` (D-24)."
    - "`## Blocked (2)` — the old spec's form — opens no column, and every row beneath it becomes `unparsed`; the contract records it as documented non-grammar (D-07)."
    - "A board of 1,048,576 bytes leaves `bounds.exceeded` false and 1,048,577 sets it true; a line of 65,536 UTF-16 code units is recorded without truncation and 65,537 is truncated with a single-character ellipsis marker and `truncated: true`; no row is ever dropped (D-20, edge: boundary)."
    - "A WIP number that is not a base-10 integer makes the heading a non-column heading rather than rounding or coercing it to zero; `boardBytes` is UTF-8 byte length and `longestLine` is a UTF-16 code-unit count, both stated in the contract; a truncation never splits a surrogate pair (edge: precision)."
    - "Two headings whose names normalize identically stay two separate columns in on-disk order — they neither merge nor collide; `unparsed[]` is ordered ascending by line number (edge: adjacency, ordering)."
    - "`agent-factory/contracts/board.md` states the reconciliation with `docs/initial/agent_factory_builder_spec_v2.md:290-296`'s `web UI, dashboards, SaaS platform` non-goal: this is a read-only terminal projector over files that already exist, with the web renderer and any listening socket deferred by `.planning/REQUIREMENTS.md` (D-04)."
  prohibitions:
    - statement: "A line is never silently dropped by the parser."
      status: enforced
      verification: "The total-partition invariant I1 is asserted over every corpus document in `scripts/board-model.test.ts`, counting raw lines independently of the loop that consumes them."
    - statement: "The parser is never widened once per counter-example."
      status: enforced
      verification: "Every admitted shape is named in `agent-factory/contracts/board.md` first; a shape outside the contract is refused by disposition, not accommodated (D-64 posture, D-03, D-05, D-07)."
    - statement: "No runtime dependency is added."
      status: enforced
      verification: "`git diff --exit-code -- package.json package-lock.json`"
    - statement: "`scripts/board-model.ts` touches no filesystem."
      status: enforced
      verification: "`grep -vE '^\\s*(//|\\*|/\\*)' scripts/board-model.ts | grep -c 'node:fs' || true` prints 0."
    - statement: "The board's own documentation block is never read as live state."
      status: enforced
      verification: "Invariant I2 over both board twins and `plans/traceability.md`; the discrimination half (a no-op stripper turning it red) lands in plan 32-04."
  artifacts:
    - scripts/board-model.ts
    - scripts/board-model.js
    - scripts/board-model.test.ts
    - agent-factory/contracts/board.md
  key_links:
    - "`agent-factory/contracts/board.md` is the derivation source for the corpus axes in plan 32-04 and the golden fixture in plan 32-05 — it must be complete before either starts."
    - "`parseBoard`'s `bounds` field feeds the `LARGE BOARD` header line rendered in plan 32-07."
    - "`preamble` and `nonColumnSections[]` are `schemaVersion: 1` fields (D-24) and appear in the golden committed by plan 32-05."
---

<objective>
Complete the pure grammar so the line partition is **total**: every byte of a real agent-written
board lands in exactly one named bucket, the board's own documentation block is invisible to the
grammar by construction, and a 380 KB board with a 34 KB line degrades visibly instead of being
refused or silently truncated.

Purpose: DASH-02's promise is that nothing is dropped. A partition with a hole is how "3 unparsed
lines in Done" becomes "176 unparsed lines" and the signal is buried. This plan also finishes the
contract every later corpus, oracle and golden derives its axes from.

Output: `scripts/board-model.ts` with `updates`, `preamble`, `nonColumnSections`, `unparsed` and
`bounds` populated; `scripts/board-model.test.ts`; and a complete
`agent-factory/contracts/board.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md
@.planning/phases/32-board-projector-cli-dashboard/32-PATTERNS.md
@.planning/phases/32-board-projector-cli-dashboard/32-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The total line partition — updates, preamble, non-column sections, unparsed</name>
  <files>scripts/board-model.ts, scripts/board-model.js, scripts/board-model.test.ts</files>
  <read_first>
    - `scripts/board-model.ts` — the module as plan 32-01 left it; the `BoardModel` type already declares every field this task fills.
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Architecture Patterns Pattern 2 (the line-number-preserving blanker), §Pitfall 9 (the 176 pre-column lines and the four `## Notes (…)` sections), §Measured Board Corpus `_Updated:` conformance (25 canonical lines on the chess board, 1 non-canonical placeholder on the kit board).
    - `.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md` D-03, D-07, D-24 — verbatim.
    - `plans/board.md:1-51` and `plans/traceability.md:1-36` — the two in-repo comment hazards: a four-space-indented mini-board, and an example pipe-table row inside a comment.
    - `scripts/section-locator-oracle.test.ts:740-770` — how a count is asserted three ways and why a vacuity floor that catches an empty denominator has never caught a silently short one.
    - `scripts/canonical-corpus.ts:1874-1892` — the module-load integrity throw and the decision-not-a-bumped-constant message wording.
  </read_first>
  <behavior>
    - `parseBoard(text).unparsed` is ordered ascending by line number and every entry's line number resolves to a non-blank raw line (invariant I4).
    - For `plans/board.md`, `plans/traceability.md`, and both replay boards, the sum of `columns[].rows.length + epicRows.length + updates.length + preamble.length + sum(nonColumnSections[].lines.length) + unparsed.length + blankOrHeadingCount` equals the raw line count exactly (invariant I1), where `blankOrHeadingCount` is derived by an independent scan, not by the parse loop's own counter.
    - No entry in `columns`, `columns[].rows`, `epicRows` or `updates` has a line number inside any raw `<!-- … -->` span (invariant I2).
    - `columns.length` equals the count of raw lines matching one of the three D-05 suffixes outside a comment span (invariant I3).
    - `sum(columns[].rows.length) + epicRows.length` equals the count of D-01/D-22-legal rows outside a comment span (invariant I5).
    - A document whose only content is `<!--` followed by a valid mini-board and no closing delimiter yields zero columns and zero rows.
    - `_Updated: 2026-08-20 by Orchestrator` before the first column heading yields an `updates[]` entry with `date`, `actor` and `text`; `_Updated: <ISO date> by <role>_` yields a `preamble` line and no finding.
    - `## Notes (bootstrap, 2026-06-05)` followed by two prose lines yields one `nonColumnSections[]` entry with that heading and those two lines, and zero columns.
    - A bullet row beneath `## Blocked (2)` yields one `unparsed[]` entry, not a row (D-07).
  </behavior>
  <action>
Fill the four buckets the tracer declared and returned empty, so the partition becomes total (D-24).

`updates[]`: a line whose canonical prefix is an ISO date followed by ` by ` and a non-empty actor
becomes `{date, actor, text, line, truncated}`. Any other line beginning with the update sigil is
classed by position, not by content: before the first column heading it is a `preamble` line, inside
a column section it is `unparsed`. That positional rule is what keeps the kit board's own placeholder
off the findings list on a fresh install (D-03, D-24).

`preamble: string[]`: every line before the first column heading that is not an `updates[]` entry and
not blanked by the comment pre-pass.

`nonColumnSections[]`: a `##` heading whose suffix is not one of the three D-05 forms opens a section
of `{heading, line, lines}` that absorbs every following line until the next `##`. `###` headings
never open anything and fall inside whatever section is current. This is the bucket that keeps
`## Columns (spec §6.1)`, `## Conventions` and the dogfood board's four `## Notes (…)` sections out
of `unparsed[]` (D-24).

`unparsed[]`: every remaining non-blank line **inside a column section** that is neither a heading, a
row, an epic row nor an update, recorded as `{line, text}` ascending by line number and never
dropped (D-03). Row-level sub-bullets nested under a row are `unparsed` this phase — the contract
says so explicitly and names it as not-yet-grammar rather than leaving it implied.

Harden `stripHtmlComments` against the two mutation shapes the corpus in plan 32-04 will drive: a
comment opened inside a column section, and an opener with no closing delimiter. The unterminated
branch blanks to end of file — the fail-closed direction, because a board that renders visibly empty
is honest and a board that renders half its state confidently is not (D-03).

Write the invariants as **structural assertions over the answer**, never as transcribed expected
output (the `section-locator-oracle.test.ts` rule). Each of I1–I5 gets its own named case in
`scripts/board-model.test.ts`, each preceded by a `PREMISE:`-prefixed assertion that its denominator
is non-zero AND equals an independently derived element count — a vacuity floor catches an empty
denominator and has never caught a silently short one.

Keep every pattern anchored and single-pass (T-32-01). Rebuild and commit `scripts/board-model.js`.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts -t "partition"</automated>
    <fails_when>non-zero exit, "0 passed" in the summary line, or any `PREMISE:` message in the output</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>npm run typecheck && npm run build && npm run check:build-parity</automated>
    <fails_when>non-zero exit, any `error TS` line, or a `BUILD PARITY FAILED` line</fails_when>
  </verify>
  <acceptance_criteria>
    - Five separately named test cases exist, one per invariant I1 through I5, each asserting over at least `plans/board.md`, `agent-factory/seed/plans/board.md` and `plans/traceability.md`.
    - The I1 case derives its raw line count with an independent scan and asserts the two totals equal each other; it does not read a counter maintained by the parse loop.
    - `node -e "import('./scripts/board-model.js').then(m=>{const b=m.parseBoard(require('node:fs').readFileSync('plans/board.md','utf8'));if(b.unparsed.length!==0)throw new Error('unparsed='+JSON.stringify(b.unparsed));if(b.preamble.length===0)throw new Error('preamble is empty; the _Updated placeholder should be there');console.log('kit board: 0 unparsed, '+b.preamble.length+' preamble')})"` exits 0 and prints a non-zero preamble count.
    - A test case feeds a document whose comment opener is never closed and asserts zero columns and zero rows.
    - A test case feeds `## Blocked (2)` with one bullet beneath it and asserts one `unparsed` entry and zero columns.
    - `grep -vE '^\s*(//|\*|/\*)' scripts/board-model.ts | grep -c 'node:fs' || true` prints `0`.
  </acceptance_criteria>
  <done>Every line of every real board lands in exactly one bucket, the kit board reports zero unparsed lines on a fresh checkout, and the five structural invariants are asserted with non-vacuous denominators.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounds and truncation — a 380 KB board degrades visibly and never loses a row</name>
  <files>scripts/board-model.ts, scripts/board-model.js, scripts/board-model.test.ts</files>
  <read_first>
    - `.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md` D-20 — verbatim, including the rejected alternatives (hard refuse over the ceiling; no bound at all).
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Measured Board Corpus (380,605 bytes, longest line 34,494 chars) and §Security Domain (the recorded superlinear-backtracking incident that turned a 0.47 s guard into 383 s on a long line).
    - `scripts/kit-model.ts:185-225` — `MAX_WALK_ENTRIES` and the reporting-versus-throwing distinction.
    - `scripts/board-model.ts` — the `Bounds` type as the tracer declared it.
  </read_first>
  <behavior>
    - `parseBoard` sets `bounds.boardBytes` to the UTF-8 byte length of the input and `bounds.longestLine` to the maximum line length in UTF-16 code units.
    - `bounds.exceeded` is false at exactly 1,048,576 bytes and true at 1,048,577; false at exactly 65,536 code units on the longest line and true at 65,537.
    - A `meta` string longer than the cap is returned truncated to the cap with a trailing single-character ellipsis and the row's `truncated` flag set to true; the row itself is still present in `columns[].rows`.
    - An `updates[].text` longer than the cap is truncated the same way with the entry's `truncated` flag set.
    - Truncating a string whose cap boundary falls between a surrogate pair cuts before the pair, never between its halves.
    - Parsing a synthetic 380 KB board with one 34,494-character line completes in under 2 seconds of wall clock.
  </behavior>
  <action>
Export `LARGE_BOARD_BYTES = 1_048_576`, `LONG_LINE_CHARS = 65_536`, `MAX_META_CHARS` and
`MAX_UPDATE_TEXT_CHARS` as named constants with a comment stating each is a decision recorded in
`agent-factory/contracts/board.md` §Bounds, never a tuning knob to be raised when a board grows.

Populate `bounds` as `{boardBytes, longestLine, exceeded}` on every parse. `boardBytes` is
`Buffer.byteLength(text, "utf8")` — state that unit in the contract, because a byte count and a
character count disagree on the chess board and a header that says `380 KB` must mean bytes.
`longestLine` is a UTF-16 code-unit count, also stated. Neither number is rounded; the header's
human-readable rendering rounds, the snapshot field does not (edge: precision).

Truncate `meta`, `trailer` and `updates[].text` at their caps with a single-character ellipsis
marker and a `truncated: true` flag on the carrying record. Cut before a surrogate pair rather than
between its halves. **Rows are never dropped** — a board over any bound is still fully parsed and
every row still renders; only the opaque strings shorten (D-20).

A WIP number is read with an anchored digits-only pattern and `Number.parseInt` base 10. A heading
whose number is not a base-10 integer fails D-05 and is therefore a non-column heading — it is not
coerced to zero and not rounded (edge: precision). Record that rule in the contract as a named
refusal, not as an implementation detail.

Add a wall-clock assertion to the long-line case: build the 380 KB fixture in memory, parse it, and
assert the elapsed time is under 2 seconds. This repository has already paid for one superlinear
regex on a long line, and a bound that is only asserted in prose is not a bound (T-32-01).

Rebuild and commit `scripts/board-model.js`.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts -t "bounds"</automated>
    <fails_when>non-zero exit, "0 passed" in the summary line, or a timeout message naming the long-line case</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>npm run typecheck && npm run build && npm run check:build-parity</automated>
    <fails_when>non-zero exit, any `error TS` line, or a `BUILD PARITY FAILED` line</fails_when>
  </verify>
  <acceptance_criteria>
    - Four separately named boundary cases exist: 1,048,576 / 1,048,577 bytes and 65,536 / 65,537 code units, each asserting the exact `bounds.exceeded` value.
    - A case asserts a row whose `meta` exceeded the cap is still present in `columns[].rows` with `truncated: true`.
    - A case asserts a truncation at a surrogate-pair boundary produces a string whose `[...s].length` equals its intended grapheme count — no lone surrogate survives.
    - A case parses a synthetic board of at least 380,000 bytes containing one line of at least 34,494 characters and asserts the elapsed wall clock is under 2000 ms.
    - `LARGE_BOARD_BYTES` and `LONG_LINE_CHARS` are asserted equal to `1048576` and `65536` in the test, with a message that reads as a decision rather than a constant.
  </acceptance_criteria>
  <done>A chess-sized board parses fully in under two seconds, reports its real byte and line measurements, truncates only the opaque strings, and loses no row.</done>
</task>

<task type="auto">
  <name>Task 3: Finish the normative contract — conflicts, staleness, bounds, and the non-goal reconciliation</name>
  <files>agent-factory/contracts/board.md</files>
  <read_first>
    - `agent-factory/contracts/board.md` — the file as plan 32-01 left it; the Headings and Rows sections are filled and four headings carry a "filled by plan 32-02" note.
    - `agent-factory/contracts/context-note.md` — the sibling's full structure, section order and voice.
    - `docs/initial/agent_factory_builder_spec_v2.md:290-296, 604-621` — the `web UI, dashboards, SaaS platform` non-goal line, and the old, disagreeing board example (`## Blocked (2)`, bare rows) that D-07 records as documented non-grammar.
    - `.planning/REQUIREMENTS.md` — the `## Future Requirements (deferred)` web-renderer line and the `## Out of Scope` lines naming a write-capable dashboard and any listening socket.
    - `.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md` D-04, D-05, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-20 — the shapes this contract states.
    - `agent-factory/config/factory.config.json` — the `wip_limits` block the contract names as the limit authority.
    - `scripts/check-imperative-lexicon.ts:1-30` — the two predicates this file is now inside the corpus of: the approved-verb rule at position zero of a `## Steps` bullet, and the sentence-form bounds selected by section anchor.
  </read_first>
  <action>
Fill the four sections the tracer stubbed, in clear professional voice (CLAUDE.md C7 — this is a
technical and safety surface).

**§Conflicts** states the seven kinds of D-10 by name — `board-vs-ticket`, `ticket-unplaced`,
`ticket-duplicated`, `row-without-file`, `wip-limit`, `wip-count`, `column-missing` — with the
`{kind, ticketId?, column?, expected, actual, source}` payload, and states that unparsed lines are
not conflicts. It also states D-08's column-set rule (headings define which columns exist and in what
order; `factory.config.json#wip_limits` supplies the expected limit; a disagreement is a conflict and
nothing is resolved silently) and D-09's WIP rule (the claimed live number is compared to the counted
rows; epic rows are not counted toward WIP).

**§Staleness** states D-11's three-arm result, D-12's per-source `readAt` and `stale` fields with one
badge naming the stale sources, and D-13's distinction: absent is a legitimate state and unreadable
is stale. State plainly that there is no empty-board output state distinct from zero rows under real
headings.

**§Bounds** states D-20's two ceilings with their units — a byte count for the board and a UTF-16
code-unit count for the longest line — the truncation of the opaque strings with a marker, and the
rule that rows are never dropped and a board over a ceiling is never refused.

**§Reconciliation and non-goals** discharges research open question 7. State that
`docs/initial/agent_factory_builder_spec_v2.md:290-296` lists `web UI, dashboards, SaaS platform`
under out of scope, and that this contract's reader is a read-only terminal projector over files
that already exist on disk — not a web UI, not a hosted service, and not a listening socket. Cite
`.planning/REQUIREMENTS.md`'s deferral of the web renderer and its `## Out of Scope` exclusion of a
write-capable dashboard and any listening socket this milestone. In the same section record D-07: the
old spec's Blocked heading form and its bare rows are documented non-grammar; they are refused
loudly, and the refusal shows up as a non-column heading whose rows become unparsed lines the
renderer counts.

Add a §Provenance closing section naming this contract as the single authority the fuzz corpus and
the oracle derive their legal and illegal axes from, and naming `scripts/board-model.ts` as the only
sanctioned reader — the parallel to `context-note.md`'s sanctioned-writer sentence.

Do not restate any regex in this document. The contract states shapes in prose; the module states
them in code; two spellings of one grammar is the drift class this repository has already paid for.
  </action>
  <verify>
    <automated>npm run check:imperative-lexicon</automated>
    <fails_when>non-zero exit, a FAIL line naming `agent-factory/contracts/board.md`, or a `DERIVATION_REFUSALS` entry naming `boardColumns`</fails_when>
    <automated>npm run check:banned-claims && npm run check:public-docs && npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or any finding line naming `agent-factory/contracts/board.md`</fails_when>
    <automated>grep -c '^## ' agent-factory/contracts/board.md</automated>
    <fails_when>a count below 6 — Headings, Rows, Conflicts, Staleness, Bounds, Reconciliation and non-goals, Provenance are all required</fails_when>
    <automated>grep -cE 'board-vs-ticket|ticket-unplaced|ticket-duplicated|row-without-file|wip-limit|wip-count|column-missing' agent-factory/contracts/board.md</automated>
    <fails_when>a count below 7 — every conflict kind of D-10 must be named in the contract</fails_when>
  </verify>
  <acceptance_criteria>
    - `agent-factory/contracts/board.md` carries `## Headings`, `## Rows`, `## Conflicts`, `## Staleness`, `## Bounds`, `## Reconciliation and non-goals` and `## Provenance`, and no section still carries a "filled by plan 32-02" note.
    - All seven D-10 conflict kind names appear in the document, each with its payload fields described.
    - The reconciliation section cites `docs/initial/agent_factory_builder_spec_v2.md` by path and states that this reader is a terminal projector, not a web UI, not a hosted service and not a listening socket.
    - `grep -cE '/\^|\\\\d\{|\\\\s\*' agent-factory/contracts/board.md` returns `0` — the contract states shapes in prose and restates no regex.
    - `npm run check:imperative-lexicon`, `npm run check:banned-claims`, `npm run check:public-docs` and `npm run check:claim-anchors` all exit 0 with the new file inside their corpora.
    - `git diff --stat -- plans/board.md agent-factory/seed/plans/board.md` prints nothing — the board twins are plan 32-08's work, not this task's.
  </acceptance_criteria>
  <done>The contract is complete and green under every doc gate, the old spec's dashboards non-goal is reconciled in writing, and every axis plan 32-04's corpus and oracle will derive has a named source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent's editor → `plans/board.md` | Untrusted markdown of unbounded size reaching the parser |
| board content → `meta` / `trailer` / `updates[].text` | Opaque strings of unbounded length that later reach a terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Denial of Service | `parseBoard`, `stripHtmlComments`, `splitRow` over a 34,494-char line | high | mitigate | Anchored single-pass patterns with no nested quantifier; the comment blanker is an `indexOf` scan, not a regex; task 2 asserts a 2-second wall-clock bound on a 380 KB / 34 KB-line document |
| T-32-07 | Denial of Service | unbounded memory on a pathological board | medium | mitigate | `bounds` measured on every parse; `meta`, `trailer` and `updates[].text` truncated at named caps; rows never dropped so degradation is visible rather than silent (D-20) |
| T-32-11 | Tampering | a board comment that reopens inside a column section, or an opener with no closing delimiter, exposing documentation as live state | high | mitigate | The blanker preserves newlines so line numbers survive, blanks to end of file on an unterminated opener (fail-closed), and invariant I2 asserts no parsed entity carries a line number inside a comment span. The discrimination half — a no-op stripper turning I2 red — lands in plan 32-04 |
| T-32-09 | Tampering | a non-integer WIP number coerced rather than refused | low | mitigate | Anchored digits-only pattern plus `Number.parseInt` base 10; a non-conforming heading is a non-column heading by D-05, recorded as a named refusal in the contract |
| T-32-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists in this plan; `git diff --exit-code -- package.json package-lock.json` asserts the absence of change |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Files (modified)**
- `scripts/board-model.ts` / `scripts/board-model.js` — `updates`, `preamble`, `nonColumnSections`, `unparsed`, `bounds` populated
- `agent-factory/contracts/board.md` — completed: `## Conflicts`, `## Staleness`, `## Bounds`, `## Reconciliation and non-goals`, `## Provenance`

**Files (new)**
- `scripts/board-model.test.ts`

**Exported symbols added — `scripts/board-model.ts`**
`LARGE_BOARD_BYTES` · `LONG_LINE_CHARS` · `MAX_META_CHARS` · `MAX_UPDATE_TEXT_CHARS`
Types completed: `UpdateEntry` (`date`, `actor`, `text`, `line`, `truncated`) · `UnparsedLine`
(`line`, `text`) · `NonColumnSection` (`heading`, `line`, `lines`) · `Bounds` (`boardBytes`,
`longestLine`, `exceeded`) · `BoardModel.preamble`

**No CLI flag, npm script or dependency changes.**
</artifacts_this_phase_produces>

<planner_assumptions>
- **DASH-02 (unclassified edge probe)** — the deterministic pass could not place "pinned by a written
  spec and a fuzz suite" in a standard edge category. Planner reading: the edge surface is the
  *corpus*, and it is resolved across this plan (the five structural invariants) and plan 32-04 (the
  mutation half and the discrimination proof). Flagged here rather than dropped.
- **Assumption A3 from 32-RESEARCH** — the dogfood example repository's `id_prefix` was never read,
  only its board. If it still reads `ABC`, D-02's prefix-equality clause would refuse all 16 of its
  rows. This plan does not depend on the answer: the replay fixture in plan 32-04 ships its own
  config, and a prefix disagreement is a conflict to surface (D-08), never a parse refusal.
</planner_assumptions>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts` green, with I1–I5 each a named case
- `npm run typecheck && npm run build && npm run check:build-parity && npm run freshness` green
- `npm run check:imperative-lexicon && npm run check:banned-claims && npm run check:public-docs && npm run check:claim-anchors` green
- `node scripts/board-dashboard.js . --once --json` still exits 0 and still reports 13 columns — the tracer's wire is unbroken
</verification>

<success_criteria>
The line partition is total and disjoint over every real board reachable from this machine, the
board's own documentation block contributes nothing to any parsed bucket, a 380 KB board with a
34 KB line parses in under two seconds and loses no row, and
`agent-factory/contracts/board.md` is a complete normative contract that every later corpus, oracle
and golden can derive from.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-02-SUMMARY.md` when done
</output>

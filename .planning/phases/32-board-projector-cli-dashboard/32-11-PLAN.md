---
phase: 32-board-projector-cli-dashboard
plan: 11
type: execute
wave: 5
gap_closure: true
depends_on: ["32-09", "32-10", "32-12", "32-13"]
files_modified:
  - scripts/board-readonly.test.ts
  - .planning/phases/32-board-projector-cli-dashboard/32-11-RED-baseline.txt
  - .planning/phases/32-board-projector-cli-dashboard/32-11-GREEN-proof.txt
autonomous: true
requirements: [DASH-06, DASH-08]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A module that binds an fs namespace and then DESTRUCTURES it makes the guard RED: the two-line pattern `import * as fsns from \"node:fs\"; const { writeFileSync, rmSync } = fsns;` is refused as an opaque acquisition."
    - "Every enumerated escape shape — destructure, alias, call argument, spread, Object.values, Reflect.get, computed member, re-export, container literal, function return, default-import destructure, fs/promises destructure — makes the guard RED, and the enumeration's cardinality is asserted two-sided."
    - "A DIRECT member access on an fs namespace is still admitted and still names its symbol: the guard refuses escapes, not namespaces."
    - "The live closure is unchanged by the rule — `EXPECTED_CLOSURE_FS_SYMBOL_COUNT` is still 6 and the unplanted mirror is still green."
  artifacts:
    - "scripts/board-readonly.test.ts — the namespace-escape rule inside analyzeModule, and the PART FIVE enumeration as data"
    - ".planning/phases/32-board-projector-cli-dashboard/32-11-RED-baseline.txt — the guard returning empty sets over a full writer, recorded before the fix"
    - ".planning/phases/32-board-projector-cli-dashboard/32-11-GREEN-proof.txt — every enumerated shape refused"
  key_links:
    - "fsNamespaceBindings -> the escape walk: the binding set is what makes an identifier read interesting, and every read that is not a direct member access is refused"
    - "opaqueFsAcquisitions -> PART ONE's premise case: a refused acquisition reds the guard through the premise that is already asserted, not through a new predicate"
    - "npm run check:dashboard-readonly -> scripts/board-readonly.test.ts: one authority, one implementation"
---

<objective>
Close the DASH-06 verification gap: the phase's load-bearing mechanical safety control is bypassed
by an ordinary two-line ESM pattern.

Gap mapping for this plan (from `32-VERIFICATION.md` frontmatter `gaps:`, truth 4):

| Gap | Finding | Task |
|-----|---------|------|
| "The dashboard CANNOT write: an import-graph guard proves its module tree holds no mutating `node:fs` symbol" | CR-01: `collectNamespaceMembers` (`scripts/board-readonly.test.ts` ~239-249) names an fs symbol ONLY on a direct member access. `import * as fsns from "node:fs"; const { writeFileSync, rmSync } = fsns;` contributes nothing to `fsSymbols` and nothing to `opaqueFsAcquisitions`; the pinned counts do not move and the guard stays green over a module that fully writes and deletes files. Independently reproduced by the verifier by re-running the file's own derivation logic. | Task 1 (the rule), Task 2 (the enumeration), Task 3 (how the gate is reached) |

Purpose: `CLAUDE.md`'s hard safety rule is that "agents never merge a protected branch and never
deploy to production without named human confirmation; prefer enforcing this *mechanically* … not
just by prompt". This guard is this phase's instance of that rule, and it currently decides a
strictly smaller question than the one it claims. This repository's own recorded lesson across
Phases 22-31 is that widening a heuristic once per counter-example is the failure mode; so the fix
is a CANONICAL FORM — the one admitted use of an fs namespace binding is as the object of a member
access, and every other read of that binding is refused by name.

Output: one structural rule, an enumeration of escape shapes held as DATA with a two-sided
cardinality assertion, a positive control proving the rule still admits the legitimate shape, and a
recorded RED baseline showing the guard green over a full writer before the change.

This plan runs LAST among the six fixes, and the ordering is an argument rather than a scheduling
accident: the guard decides a question about the transitive closure of the COMPILED
`scripts/board-dashboard.js`, and plans 32-09, 32-10, 32-12 and 32-13 all change modules inside that
closure. Running the strengthened guard after every other change has landed means the enumeration is
proved against the tree that ships, not against an intermediate one.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` and no `## Prohibitions`
section to satisfy.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-06-SUMMARY.md
</context>

## Artifacts this phase produces

- `collectNamespaceEscapes` (or the escape arm folded into `collectNamespaceMembers`) inside
  `analyzeModule` in `scripts/board-readonly.test.ts` — the new refusal rule. Module-internal to the
  test file; nothing outside it imports from here.
- `NAMESPACE_ESCAPE_SHAPES` — a new frozen array of `{ name, appendSource }` records, one per escape
  spelling, iterated by the PART FIVE discrimination block. Its cardinality is pinned two-sided with
  a decision-shaped message.
- `NAMESPACE_ESCAPE_SHAPE_COUNT` — that two-sided pin.
- New PART FIVE cases, including one POSITIVE control (`a direct member access is still admitted`).
- `.planning/phases/32-board-projector-cli-dashboard/32-11-RED-baseline.txt` and
  `32-11-GREEN-proof.txt`.
- No change to `EXPECTED_CLOSURE_FS_SYMBOLS` or `EXPECTED_CLOSURE_FS_SYMBOL_COUNT`: the live closure
  contains no namespace import of `node:fs` at all (`scripts/board-read.ts:36-43` and
  `scripts/board-dashboard.ts:41` both use named imports), so the new rule contributes nothing to the
  live derivation. That non-movement is asserted, not assumed.
- No production-code change and no `npm run build` output change: this plan touches a `.test.ts` file
  only, so there is no committed `.js` twin to rebuild for it.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The canonical form — an fs namespace binding may only be the object of a member access</name>

  <read_first>
    - `scripts/board-readonly.test.ts` lines 155-250 — `analyzeModule`, `collectSpecifiers` (which
      already populates `fsNamespaceBindings` from BOTH a namespace import and a default import of a
      builtin), and `collectNamespaceMembers` at ~237-249, the function the gap names
    - `scripts/board-readonly.test.ts` lines 46-58 — the docblock's "WHAT IT DOES NOT CLOSE" list,
      which names the aliased re-export and does NOT name the destructure; this task adds the
      destructure to the CLOSED side and says so
    - `scripts/board-readonly.test.ts` lines 437-500 — PART ONE, and in particular the premise case
      "no closure module acquires a filesystem module by a route this pass cannot name", which is the
      assertion a refusal reds
    - `scripts/board-readonly.test.ts` lines 320-340 — `EXPECTED_CLOSURE_FS_SYMBOLS` and its
      two-sided count, the blocking pin that must NOT move
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` lines 75-81 — the
      verifier's probe file and its recorded result (`fsSymbols: []`, `opaqueFsAcquisitions: []`)
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` — CR-01's
      `collectNamespaceEscapes` sketch (one candidate route, not binding)
  </read_first>

  <files>scripts/board-readonly.test.ts, .planning/phases/32-board-projector-cli-dashboard/32-11-RED-baseline.txt</files>

  <action>
RED FIRST, AND RECORD IT. Before editing the derivation, reproduce the verifier's own probe: run the
CURRENT `analyzeModule` over a temporary module whose body is the namespace import, a destructuring
bind naming two writers, and an exported function that calls both. Record the three collected sets
verbatim into `.planning/phases/32-board-projector-cli-dashboard/32-11-RED-baseline.txt`, together
with the fact that `npm run check:dashboard-readonly` is GREEN with that same source planted into a
mirror of `scripts/board-read.js`. A baseline that does not show a green guard over a full writer has
not reproduced the finding.

THEN THE STRUCTURAL FIX. Do not widen the matcher to also recognise a destructuring pattern — that is
the "one more heuristic per counter-example" shape this repository has spent eight rounds on twice.
Invert the question. Define the CANONICAL FORM: an identifier bound to an fs namespace may appear in
exactly one position, as the `expression` of a `PropertyAccessExpression` or an
`ElementAccessExpression`. Every other READ of that identifier is a route this syntactic pass cannot
follow, and is pushed onto `opaqueFsAcquisitions` — which PART ONE's premise case already asserts
empty, so the refusal reds the guard through the assertion that is already there rather than through
a new predicate free to disagree with it.

The rule's exemptions are exactly two, and each is a BINDING site rather than a read:
  - the identifier inside a `NamespaceImport` (`import * as fsns from …`);
  - the identifier that is an `ImportClause`'s own `name` (`import fs from …`, which
    `collectSpecifiers` already treats as a namespace object under esModuleInterop).
Write those two as the whole exemption list, with a comment saying that a third exemption is a
DECISION somebody records rather than a condition somebody appends — the same posture the file's
three named stem exclusions already take.

Two properties of the rule must be stated in its docblock because a later reader will otherwise
"fix" them:
  - THE PASS IS NAME-SCOPED, NOT BINDING-SCOPED. A parameter or a local that happens to reuse a
    namespace identifier's spelling is treated as the namespace. The imprecision runs in the
    direction of REFUSAL, which is the only direction a safety guard may be imprecise in, and
    loosening it to be scope-accurate would trade a false red for a possible false green.
  - THE ELEMENT ACCESS ARM IS UNCHANGED. A computed key that is not a string literal is already an
    opaque acquisition; a string-literal key still names its symbol. The new rule is a UNION with
    that arm, not a replacement for it, and the union is what the enumeration in Task 2 tests.

UPDATE THE DOCBLOCK'S BOUNDARY LIST. Lines ~46-58 currently name the aliased re-export and the
computed dynamic specifier as the routes the pass cannot see. The namespace destructure moves from
that list — where it was never written down — onto the CLOSED side. Say in one sentence that a
namespace escape is now refused rather than unseen, and name what remains open.

THE POSITIVE CONTROL IS PART OF THIS TASK, NOT A LATER ONE. Add a case planting
`import * as fsns from "node:fs";` followed by a direct member read of `readFileSync` into a mirror
of `scripts/board-read.js`, and assert that `opaqueFsAcquisitions` stays EMPTY, that `fsSymbols`
still contains `readFileSync`, and that the two-sided pin still reads
`EXPECTED_CLOSURE_FS_SYMBOL_COUNT` — `readFileSync` is already one of the six, so a correct rule
moves nothing. Without this case the rule could refuse every namespace and still look right.
  </action>

  <behavior>
    - Given a module binding `node:fs` as a namespace and destructuring two writers from it, `analyzeModule` returns a non-empty `opaqueFsAcquisitions` naming that module.
    - Given a module binding `node:fs` as a namespace and reading `readFileSync` as a direct property access, `analyzeModule` returns an EMPTY `opaqueFsAcquisitions` and an `fsSymbols` containing `readFileSync`.
    - Given a module binding `node:fs` as a namespace and reading a member by a string-literal element access, the symbol is still named and nothing is refused.
    - Given a module binding `node:fs` as a namespace and reading a member by a computed key, the acquisition is still refused (the pre-existing arm survives the union).
    - Given the live, unplanted closure, `opaqueFsAcquisitions` is empty and `fsSymbols` has exactly six members.
  </behavior>

  <acceptance_criteria>
    - `.planning/phases/32-board-projector-cli-dashboard/32-11-RED-baseline.txt` records the current derivation returning two empty sets over the writer snippet, and records `npm run check:dashboard-readonly` exiting 0 with that writer planted.
    - After the fix, the same planted mirror makes `opaqueFsAcquisitions` non-empty and the PART ONE premise case fails.
    - The POSITIVE control case passes: direct member access leaves `opaqueFsAcquisitions` empty and `fsSymbols` at exactly `EXPECTED_CLOSURE_FS_SYMBOL_COUNT` members.
    - The existing CONTROL case ("an UNPLANTED mirror of the live closure is still green") passes unmodified.
    - `EXPECTED_CLOSURE_FS_SYMBOLS` and `EXPECTED_CLOSURE_FS_SYMBOL_COUNT` are unchanged from their committed values (6 members, count 6).
    - The exemption list in the rule has exactly two entries and a comment stating that a third is a decision.
  </acceptance_criteria>

  <verify>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>exit code is non-zero, or the run reports 0 passed — the named gate command is a vitest wrapper around this one file and a zero-case run is a green line over an empty denominator</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts</automated>
    <fails_when>exit code is non-zero, or the reported case count is lower than the committed run's — cases were deleted rather than added</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>exit code is non-zero, or tsc reports an error in scripts/board-readonly.test.ts (it is compiled by tsconfig.tests.json)</fails_when>
  </verify>

  <done>
The two-line destructure that shipped a full writer past a green guard now reds it through the
premise assertion the file already carried, and a direct member access is still admitted and still
named.
  </done>

  <reversibility rating="reversible">One walk function inside one test file; no production code and no published shape changes.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The escape shapes as DATA — enumerate, iterate, and pin the enumeration two-sided</name>

  <read_first>
    - `scripts/board-readonly.test.ts` lines 709-845 — PART FIVE, `withLiveMirror`, `SCRATCH_ROOT`,
      the four existing discrimination cases and the residue case
    - `scripts/board-readonly.test.ts` lines 155-230 — `collectSpecifiers`'s existing handling of
      `require(…)`, dynamic `import(…)` and `export * from …`, which are the shapes this task CITES
      as already refused rather than re-implementing
    - `scripts/board-readonly.test.ts` lines 118-131 — `FS_MODULE_IDENTITIES` and
      `normalizeSpecifier`, which are why `fs`, `node:fs`, `fs/promises` and `node:fs/promises` are
      one identity to this pass
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` — CR-01's closing paragraph
      naming the namespace-passed-as-a-value and the aliased re-export as siblings of the destructure
  </read_first>

  <files>scripts/board-readonly.test.ts</files>

  <action>
HOLD THE SHAPES AS DATA, NOT AS TWELVE HAND-WRITTEN CASES. A hand-maintained list of `it(…)` blocks
is the set-literal drift class this repository has already paid for (seven granted names, zero
adapter files). Declare a frozen `NAMESPACE_ESCAPE_SHAPES` array of `{ name, appendSource }` records
and drive PART FIVE from it with a table-driven iteration, so adding a shape is adding a row.

The rows, each a distinct SPELLING of the same escape and each planted into a mirror of
`scripts/board-read.js`:
  1. destructuring bind of two writers from a namespace — the exact shape the verifier reproduced;
  2. rest-element destructure of a namespace;
  3. plain alias assignment of the namespace to another identifier;
  4. the namespace passed as a call argument;
  5. object spread of the namespace into a literal;
  6. the namespace inside an array literal;
  7. the namespace inside an object literal as a shorthand property;
  8. `Object.values` over the namespace;
  9. `Reflect.get` over the namespace with a string key;
  10. an element access with a COMPUTED (non-literal) key — already refused before this plan, kept in
      the enumeration so the UNION of the old arm and the new arm is what is tested;
  11. a re-export of the namespace binding;
  12. the namespace returned from an exported arrow function;
  13. a DEFAULT import of `node:fs` destructured — the same escape through the esModuleInterop
      spelling `collectSpecifiers` already treats as a namespace;
  14. a namespace import of `node:fs/promises` destructured — the second module identity.

One predicate decides all of them: after planting, `analyzeClosure(mirrorRoot, DASHBOARD_ENTRY)`
returns a non-empty `opaqueFsAcquisitions` whose joined text names `scripts/board-read.js`. That is
the same predicate PART ONE's premise case asserts against the live tree, so the discrimination and
the live assertion cannot drift apart.

PIN THE ENUMERATION TWO-SIDED. Assert `NAMESPACE_ESCAPE_SHAPES.length` against
`NAMESPACE_ESCAPE_SHAPE_COUNT` with a message saying that a fifteenth spelling is a decision
recorded here rather than a constant somebody bumps, and assert the set of `name` values is free of
duplicates — a table whose rows silently collapse is a table that tests fewer things than it counts.
Also assert the table is non-empty BEFORE the iteration claims anything, because an emptiness claim
over an empty denominator is the false-green this repository has recorded six instances of.

CITE, DO NOT DUPLICATE, WHAT IS ALREADY REFUSED. `require("node:fs")`, a dynamic
`await import("node:fs")` and `export * from "node:fs"` are already pushed onto
`opaqueFsAcquisitions` by `collectSpecifiers`, and PART FIVE already carries a case for the dynamic
import. Add a short block that names each of the three and points at the line of `collectSpecifiers`
that refuses it plus the case that proves it, rather than adding a fourth near-duplicate case. Where
a claim has no proving case today — `require` and `export *` — add exactly one table row each rather
than a prose claim, because a claim without a case is the thing this plan exists to remove.

KEEP THE RESIDUE GUARANTEE. Every new plant goes through `withLiveMirror`, whose `finally` removes
the mirror, and the existing "scratch root is left with no mirror residue" case must still pass with
fourteen more mirrors created and removed. If the added mirroring makes the file slow enough to
matter, record the measured wall time in the summary rather than trimming rows.
  </action>

  <behavior>
    - For every row in `NAMESPACE_ESCAPE_SHAPES`, planting that row into a mirror of `scripts/board-read.js` yields a non-empty `opaqueFsAcquisitions` naming that module.
    - `NAMESPACE_ESCAPE_SHAPES.length` equals `NAMESPACE_ESCAPE_SHAPE_COUNT` and is greater than zero.
    - The `name` values in `NAMESPACE_ESCAPE_SHAPES` are unique.
    - After the whole block runs, a real listing of the scratch root returns an empty array.
  </behavior>

  <acceptance_criteria>
    - `NAMESPACE_ESCAPE_SHAPES` exists, is frozen, has exactly 14 rows, and its length is asserted two-sided against `NAMESPACE_ESCAPE_SHAPE_COUNT` with a decision-shaped message.
    - The table-driven block reports 14 passing cases by name in the vitest output, each name naming its spelling.
    - A non-emptiness assertion on the table precedes the iteration.
    - The uniqueness assertion over the `name` column passes.
    - The `require` and `export *` rows are table rows, not prose.
    - The residue case still passes.
    - `npm run check:dashboard-readonly` case count is strictly greater than the committed run's count.
  </acceptance_criteria>

  <verify>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>exit code is non-zero, or the reported passing-case count is not greater than the count recorded in 32-06-SUMMARY.md (24) — the enumeration did not actually run</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts --reporter=verbose</automated>
    <fails_when>exit code is non-zero, or fewer than 14 distinct escape-shape case names appear in the verbose output</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>exit code is non-zero, or tsc reports an error in scripts/board-readonly.test.ts</fails_when>
  </verify>

  <done>
Fourteen spellings of the same escape each red the guard, the enumeration is data with a two-sided
cardinality pin, and the three previously-claimed-but-unproven routes now have cases instead of
sentences.
  </done>

  <reversibility rating="reversible">A data table and a table-driven block inside one test file.</reversibility>
</task>

<task type="auto">
  <name>Task 3: How the gate is REACHED — bind the guard's subject to the committed build output</name>

  <read_first>
    - `scripts/board-readonly.test.ts` lines 100-120 (`ROOT`, `DASHBOARD_ENTRY`) and 846-878 (PART
      SIX: the no-runtime-dependency case and the `check:dashboard-readonly` entry case)
    - `package.json` — the `check:dashboard-readonly` and `check:build-parity` entries
    - `.github/workflows/ci.yml` — where `check:build-parity` is invoked, so the binding this task
      asserts is a real one rather than an aspiration
    - `CLAUDE.md` — the TypeScript-compiled-to-committed-`.js` build contract and the freshness rule
  </read_first>

  <files>scripts/board-readonly.test.ts, .planning/phases/32-board-projector-cli-dashboard/32-11-GREEN-proof.txt</files>

  <action>
ASK WHAT THE GUARD IS ASKED OF, NOT ONLY WHAT IT REFUSES. `analyzeClosure` walks
`scripts/board-dashboard.js` — the COMMITTED BUILD OUTPUT. A writer introduced in a `.ts` source and
not rebuilt is invisible to this guard, which means the control's real precondition is that the
committed `.js` is a faithful build of its `.ts`. That precondition lives in a different script
(`check:build-parity`) and nothing in this file states the dependency, so a reader of this file
cannot tell that the guard's answer is only as good as the build's freshness.

Add a PART SIX case that makes the binding mechanical rather than implied:
  - assert that every module in `analyzeClosure(ROOT, DASHBOARD_ENTRY).modules` whose path is under
    `scripts/` has a sibling `.ts` source on disk — derived by mapping the closure's own module list,
    not by a typed list — so a `.js` in the closure with no source is a finding;
  - assert the closure module list is non-empty first, as PART ONE's convention requires;
  - assert that `package.json` carries a `check:build-parity` entry and that the CI workflow file
    invokes it, with a message stating in one sentence that THIS guard decides a question about the
    committed `.js` and `check:build-parity` is what makes that `.js` the same program as its `.ts`.
    If the CI wiring cannot be located, record `UNKNOWN - verify` in the summary rather than
    asserting a green that was not measured.

RECORD THE GREEN PROOF. Write
`.planning/phases/32-board-projector-cli-dashboard/32-11-GREEN-proof.txt` containing: the verifier's
original probe snippet re-run against the FIXED derivation with its now-non-empty
`opaqueFsAcquisitions`; the `npm run check:dashboard-readonly` output showing the new case count; and
the positive-control result showing the closure pin still at six.
  </action>

  <acceptance_criteria>
    - The `.ts`-source-for-every-closure-`.js` case derives its module list from `analyzeClosure` and asserts non-emptiness before asserting anything about membership.
    - The case naming `check:build-parity` fails if that npm entry is absent from `package.json`.
    - `.planning/phases/32-board-projector-cli-dashboard/32-11-GREEN-proof.txt` shows a non-empty `opaqueFsAcquisitions` for the verifier's original probe and a `check:dashboard-readonly` case count greater than 24.
    - The proof file records the positive control's closure-symbol count as exactly 6.
  </acceptance_criteria>

  <verify>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>exit code is non-zero, or the run reports 0 passed</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>exit code is non-zero, or stdout contains `BUILD PARITY FAILED` — the precondition this task binds the guard to must itself be green</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts scripts/check-foundation-guards.test.ts</automated>
    <fails_when>exit code is non-zero — the foundation-guard suite must not regress when a new assertion over package.json and the CI workflow lands</fails_when>
  </verify>

  <done>
The guard states, mechanically, that its subject is the committed build output and that build parity
is what makes that subject the program the sources describe; the GREEN proof records the verifier's
own probe now being refused.
  </done>

  <reversibility rating="reversible">Two assertions in one test file plus an evidence file.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a future commit → `scripts/board-dashboard.js`'s import closure | Any change to the projector's module tree can introduce a write capability. The guard is the only mechanism deciding that it did not. |
| `scripts/*.ts` sources → the committed `scripts/*.js` the host runs | The guard analyses the `.js`. A source that is not rebuilt is a program the guard never saw. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-11-01 | Elevation of Privilege | `analyzeModule` / `collectNamespaceMembers` | critical | mitigate | Reproduced: a two-line ESM pattern ships a module that writes and deletes files past a fully green read-only guard. Task 1 replaces the name-matching heuristic with a canonical form and refuses every other read of an fs-namespace binding; Task 2 proves the refusal on fourteen spellings. |
| T-32-11-02 | Tampering | the enumeration itself | high | mitigate | A hand-maintained list of discrimination cases rots while every gate over it stays green — this repository's recorded second systemic failure class. The shapes are held as data with a two-sided cardinality pin and a uniqueness assertion, so a silently collapsed or deleted row reds the file. |
| T-32-11-03 | Repudiation | the guard's subject | high | mitigate | The guard decides a question about the committed `.js`. Without a stated binding to `check:build-parity`, a green guard over a stale artifact is indistinguishable from a green guard over the real program. Task 3 makes the dependency an assertion with a message. |
| T-32-11-04 | Denial of Service | the fix itself | medium | mitigate | A rule that refused the legitimate direct member access would red the live closure, get loosened under pressure, and end up weaker than the rule it replaced. Task 1's positive control and the pre-existing unplanted-mirror CONTROL case are the mechanical defence. |
| T-32-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan. `package.json` gains no `dependencies` and no `devDependencies` entry; the file already imports the dev-only `typescript`, and its own PART SIX case asserts `dependencies === undefined` on every run. |
</threat_model>

<verification>
1. `npm run check:dashboard-readonly` — the DASH-06 authority, and the only implementation of it.
2. `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts --reporter=verbose` — confirm all fourteen escape-shape case names appear.
3. `npm run typecheck` — `scripts/board-readonly.test.ts` is compiled by `tsconfig.tests.json`.
4. `npm run build && npm run check:build-parity` — this plan changes no `.ts` with a committed `.js` twin, so parity must be clean without a rebuild moving anything.
5. `npx vitest run --exclude '**/scripts/e2e/**'` — the whole suite minus the live claude-CLI e2e lane. Do NOT run plain `npm test`.
6. Re-run the verifier's CR-01 probe from `32-VERIFICATION.md:75-81` against the fixed derivation and paste into `32-11-GREEN-proof.txt`.
</verification>

<success_criteria>
- The verifier's exact probe snippet now produces a non-empty `opaqueFsAcquisitions`.
- Fourteen enumerated escape spellings each red the guard; the enumeration is pinned two-sided.
- A direct member access is still admitted, and `EXPECTED_CLOSURE_FS_SYMBOL_COUNT` is still 6.
- The unplanted-mirror CONTROL case and the residue case both still pass.
- The guard states its dependency on `check:build-parity` mechanically.
- REQUIREMENTS.md and ROADMAP.md phase status are NOT flipped to Complete by this plan.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-11-SUMMARY.md` when done.
</output>
</content>

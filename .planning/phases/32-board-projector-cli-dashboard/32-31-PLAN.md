---
phase: 32-board-projector-cli-dashboard
plan: 31
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - scripts/js-import-closure.ts
  - scripts/js-import-closure.js
  - scripts/board-readonly.test.ts
  - .planning/phases/32-board-projector-cli-dashboard/32-31-GREEN-proof.txt
autonomous: true
requirements: [DASH-06, DASH-08]
estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every module specifier a closure module carries lands in exactly one of three classes — bare, relative, foreign — decided by one exported function, and `foreign` is refused by construction rather than screened against a list of spellings somebody thought of (D-21)."
    - "Each of the three spellings `32-24-RED-baseline.txt` measured GREEN over a live writer — an absolute POSIX path, `//localhost/<abs path>`, and `//host/<path>` — makes `npm run check:dashboard-readonly` exit non-zero, measured with the writer planted into the COMMITTED `scripts/board-read.js` after `npm run build` (D-23)."
    - "The two spellings the builtin allow-list refuses today — a `file://` URL and a Windows drive-letter path — plus `data:`, which the baseline measured as `isBareSpecifier → true`, are refused at least as strongly after the cutover as before it: each one reds the `PREMISE: no closure module acquires a module by a route that is not a static literal import` case, which is the write-detection mechanism rather than a cardinality pin."
    - "One authority decides a specifier's class: `classifySpecifier` is exported from `scripts/js-import-closure.ts` and both the closure walker and the read-only guard call it, so the walker's follow-set and the guard's census-set can no longer own different prefixes."
    - "The scan's INPUT is code rather than prose: comments and template literals are removed before any specifier pattern runs, and the closure every existing caller of `jsImportClosure` builds is recorded before and after with every difference named."
    - "The legitimate tree is unchanged: the unplanted `npm run check:dashboard-readonly` is green with a case count at or above the 89 the baseline recorded, and every gate whose mirror is built by `jsImportClosure` still exits 0."
    - { statement: "No module-specifier spelling exists outside the three-way partition on any platform this kit installs on, including a Windows drive-letter path measured as text on darwin and a UNC host that resolves where darwin refuses it", verification: backstop }
    - { statement: "Running npm run check:dashboard-readonly concurrently with a build, or interrupting it part-way, cannot produce a green result over a tree whose committed .js does not match its .ts — build parity and freshness are what bound that, and they are separate commands run in this plan's own verify (DASH-08 concurrency probe)", verification: backstop }
  prohibitions:
    - statement: "Never re-green the guard by adding one more prefix test beside the existing ones — a fourth arm is the shape this round exists to stop."
      status: resolved
      verification: judgment
    - statement: "Never let a specifier spelling reach the walk or the census without a class; an unclassified specifier is a refusal, never a skip."
      status: resolved
      verification: judgment
    - statement: "Never widen `ALLOWED_BUILTIN_SPECIFIERS` to make a planted writer green."
      status: resolved
      verification: judgment
    - statement: "Never let the cutover make the tree LESS safe for a spelling that is refused today — a relocated refusal must be measured, never assumed to have survived the move."
      status: resolved
      verification: judgment
    - statement: "Never narrow the scan's input by a rule that could remove a real import statement without a converse that proves every existing caller's closure is unchanged."
      status: resolved
      verification: judgment
  artifacts:
    - "scripts/js-import-closure.ts — `classifySpecifier`, `SPECIFIER_CLASSES`, `stripNonCode`, `moduleSpecifiers`, `jsImportClosureFacts`, and the foreign refusal in `jsImportClosure`"
    - "scripts/js-import-closure.js — the committed twin, rebuilt in the same commit"
    - "scripts/board-readonly.test.ts — `ModuleFacts.foreignSpecifiers`, `ClosureFacts.foreignSpecifiers`, `SPECIFIER_CLASS_ROWS`, `RELOCATED_SPECIFIER_ROWS` and the two-sided census"
    - ".planning/phases/32-board-projector-cli-dashboard/32-31-GREEN-proof.txt — the per-row flip table with the case name that reds each row, the refusal-authority before/after column, the position table and the per-caller converse"
  key_links:
    - "classifySpecifier (js-import-closure.ts) -> jsImportClosure's walk: relative is followed, bare is skipped by a named rule, foreign is collected and refused"
    - "classifySpecifier (js-import-closure.ts) -> the guard's noteSpecifier: the census asks the same authority the walker asks, so neither can own a prefix the other does not"
    - "ClosureFacts.foreignSpecifiers -> the acquisitions PREMISE case: a foreign specifier reds the write-detection mechanism through an assertion that already exists, not through a new count"
    - "stripNonCode -> moduleSpecifiers: the partition is asked about code, and CLOSURE_BASELINES is the converse that proves nothing real was stripped"
    - "npm run check:dashboard-readonly -> the committed scripts/board-read.js: Task 3 proves the flip against the artifact a host runs, never against the .ts"
  deferred: []
---

<objective>
TRACER for gap-closure round 3: prove ONE canonical-form cutover end-to-end, against the committed
`.js` that ships, before any other plan in this round expands.

The cutover is the one all three independent reports recommend: **make the derivation's subject a
total partition with a refusing third bucket.** This plan applies it to module specifiers, wires it
through both authorities that consume one, rebuilds the committed twin, and measures the shipped gate
command flipping from exit 0 to a non-zero exit over real writers.

Gap mapping (from `32-VERIFICATION.md` frontmatter `gaps:`, truth 4 — DASH-06):

| Gap item | Finding | Task |
|----------|---------|------|
| "The dashboard CANNOT write" is still false, route (a) | A writer imported through a specifier that begins with `/` but is not `./` or `../` is in neither `isBareSpecifier`'s set (`!startsWith(".") && !startsWith("/")`) nor the closure walker's relative set (three patterns each requiring a leading `.`), so it is neither censused nor walked. Reported three times: the phase verifier's gap 3 part 1, `32-REVIEW.md` WR-01, `32-23-ADVERSARIAL-REVIEW.md` F-04. | Tasks 1, 2, 3 |

**The RED baseline for this finding is already MEASURED and committed — do not re-measure it.**
`.planning/phases/32-board-projector-cli-dashboard/32-24-RED-baseline.txt` (commit `759e982c`, tree
`b7a4239b`) records six spellings against the shipped gate. Its result table is the row set this plan
is proved against, and three of its findings are the reason this plan is a re-cut rather than a
re-run of the retired `32-24`:

1. **Six spellings, not four.** `//localhost/<abs path>` is a live GREEN spelling no review named:
   Node resolves `//host/path` against the parent `file:` URL and darwin accepts a `localhost` host.
   `data:` was measured as `isBareSpecifier → true` today. The measured hole is exactly "specifiers
   beginning with `/` that are not `./` or `../`" — a complement, not a partition.
2. **A count pin cannot decide this.** Three of the six rows ALREADY exit 1 before any fix, so a
   "four or more refusals" count over the proof passes while the fix does nothing — the F-08 shape
   this round is closing one plan over. The discriminating pin is a per-row FLIP over the three
   GREEN rows, each recorded with the NAME of the case that reds it.
3. **The cutover RELOCATES two existing refusals, and possibly a third.** `file://` and `C:\` red
   today through `ALLOWED_BUILTIN_SPECIFIERS` because both start with a letter and land in
   `bareSpecifiers`; `data:` does too. Once `classifySpecifier` calls them `foreign` they leave that
   set and the new branch must carry the refusal. If the new branch is weaker in any respect this
   plan makes the tree LESS safe for three spellings while fixing three.

Purpose: `CLAUDE.md` states the read-only rule as mechanical rather than prose, and D-21 states that
the guard's sets are derived rather than hand-listed. Today the derivation is honest but its SUBJECT
is a syntactic complement. Three consecutive rounds of this phase have paid for a set defined by a
complement. This plan replaces the complement with a partition and gives it one owner.

Why this plan is FIRST rather than last, against round 2's ordering argument: round 2 ran the guard
plan at wave 6 so it was proved against the closure that ships. That argument is preserved two ways
rather than abandoned — every later plan in this round carries `npm run check:dashboard-readonly` in
its own `<verify>`, and plan `32-37` re-runs these reproductions against the rebuilt committed `.js`
at the end of the round. Leading with the thinnest end-to-end slice is what tells us, after ONE
commit rather than after six, whether the cutover holds against the artifact a host runs.

Output: one exported specifier-class authority; a total partition in the walker with a named refusal;
a two-sided specifier-class census in the guard; and a measured exit-code flip on the committed `.js`
with the refusal authority recorded per spelling, before and after.

Canon-referral breadcrumb: path-traversal and prototype-pollution are canon security items covered by
`/gsd-secure-phase` and are not minted as prohibitions here. This plan touches no credential, no
network call and no user identity — the trust boundary is a local module closure.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` and no `## Prohibitions`
section to lift from; the probe-report items this plan authors are in `must_haves` above (DASH-08
concurrency, authored as a backstop marker), and the round's full probe arithmetic is in `32-37-PLAN.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-24-RED-baseline.txt
@.planning/phases/32-board-projector-cli-dashboard/32-20-SUMMARY.md
</context>

## Artifacts this phase produces

- `SpecifierClass` — the exported union type `"bare" | "relative" | "foreign"` in
  `scripts/js-import-closure.ts`.
- `SPECIFIER_CLASSES` — the frozen three-member class list, so the partition's cardinality is a value
  rather than a shape a reader has to infer.
- `classifySpecifier(specifier: string): SpecifierClass` — the ONE authority on a specifier's class.
- `stripNonCode(source: string): string` — line comments, block comments and template literals
  replaced by equal-length whitespace, so the specifier patterns are asked about code rather than
  about prose.
- `moduleSpecifiers(source: string): readonly { specifier: string; cls: SpecifierClass }[]` — every
  specifier the three emitted forms carry, each with its class.
- `relativeSpecifiers(source)` — retained as a thin projection over `moduleSpecifiers`, so existing
  callers keep their name and their contract.
- `jsImportClosureFacts(root, entryRel): { modules: readonly string[]; foreignEdges: readonly { module: string; specifier: string }[] }`
  — the walk that reports a foreign edge instead of throwing on it, so the guard can census one at a
  named position.
- `jsImportClosure(root, entryRel)` — unchanged signature, now a thin wrapper that calls
  `jsImportClosureFacts` and raises `ImportClosureError` naming EVERY foreign edge when any exists.
- `ModuleFacts.foreignSpecifiers: readonly string[]` and `ClosureFacts.foreignSpecifiers: readonly string[]`
  in `scripts/board-readonly.test.ts`.
- `SPECIFIER_CLASS_ROWS` and `SPECIFIER_CLASS_ROW_COUNT` — the spellings held as data in the shape
  `MODULE_IDENTITY_SHAPES` already established.
- `RELOCATED_SPECIFIER_ROWS` and `RELOCATED_SPECIFIER_ROW_COUNT` — one row per spelling whose
  refusing authority MOVES, each carrying the authority before, the authority after, and the case
  name that reds it after.
- `CLOSURE_BASELINES` — one row per existing `jsImportClosure` caller entry with the module list
  measured before the cutover, asserted equal after it.
- `.planning/phases/32-board-projector-cli-dashboard/32-31-GREEN-proof.txt`.
- The committed `scripts/js-import-closure.js` twin, rebuilt from its `.ts` in the same commit.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — one specifier-class authority, wired through both consumers, flipping the absolute-path row on the committed .js</name>

  <read_first>
    - `scripts/js-import-closure.ts` in full (152 lines) — `relativeSpecifiers`' three patterns
      (lines 51-62), `ImportClosureError` (65-70), `assertInsideRoot` (72-80), the walk (89-122),
      `copyImportClosure` (131-139), `closureTargets` (142-152), and the docblock paragraph beginning
      "SCOPE, STATED SO IT IS NOT MISTAKEN FOR A BUNDLER" (lines 28-33) which states the
      bare-specifier skip as a deliberate rule
    - `scripts/board-readonly.test.ts` lines 186-196 — `normalizeSpecifier`, `isFsSpecifier` and
      `isBareSpecifier`, the predicate being retired
    - `scripts/board-readonly.test.ts` lines 317-345 — the `ModuleFacts` interface, in particular
      `bareSpecifiers`, `opaqueSpecifiers` and the fail-closed rule each field states
    - `scripts/board-readonly.test.ts` lines 380-400 and 425-510 — `analyzeModule`'s local state,
      `noteSpecifier`, and `collectSpecifiers`' three specifier positions (import declaration,
      export-from declaration, dynamic import / require call)
    - `scripts/board-readonly.test.ts` lines 807-870 — `ClosureFacts`, `analyzeClosure` and the union
      it builds over the closure
    - `scripts/board-readonly.test.ts` lines 1085-1130 — the `PREMISE: no closure module acquires a
      module by a route that is not a static literal import` case over `facts.acquisitions`, which is
      the write-detection MECHANISM a refusal must red through
    - `.planning/phases/32-board-projector-cli-dashboard/32-24-RED-baseline.txt` §§ 2, 3.1 and 4 —
      the measured result table, the absolute-path row's full transcript, and the ten-spelling
      partition table quoting both shipped predicates verbatim
  </read_first>

  <files>scripts/js-import-closure.ts, scripts/js-import-closure.js, scripts/board-readonly.test.ts</files>

  <behavior>
    - Given `./x.js` and `../y/z.js`, `classifySpecifier` returns `relative`.
    - Given `node:fs`, `typescript` and `@scope/pkg`, it returns `bare`.
    - Given `/abs/writer.mjs`, `//localhost/abs/writer.mjs`, `//host/x.js`, `file:///abs/writer.mjs`, `C:\x\writer.mjs`, `data:text/javascript,export const a=1`, `#internal`, `.`, `..` and the empty string, it returns `foreign`.
    - Given a source whose only occurrence of a foreign-looking specifier is inside a line comment or a template literal, `moduleSpecifiers` returns no row for it.
    - Given the committed `scripts/board-read.js` with an absolute-path writer import appended, the guard's `ClosureFacts.foreignSpecifiers` is non-empty and `acquisitions` is non-empty.
    - Given the unplanted tree, `npm run check:dashboard-readonly` exits 0 and `foreignSpecifiers` is empty.
  </behavior>

  <action>
PUT THE CLASS DECISION IN ONE PLACE, AND MAKE IT TOTAL. Add to `scripts/js-import-closure.ts` an
exported union type `SpecifierClass` with the three members `bare`, `relative`, `foreign`; a frozen
`SPECIFIER_CLASSES` list carrying those three names in that order; and
`classifySpecifier(specifier)`. Write the rule as a positive test per class, never as a chain of
negations:

  - `relative` when the specifier starts with `./` or with `../`.
  - `bare` when the specifier's first character is an ASCII letter or `@`, AND it contains no
    backslash, AND it either contains no `:` at all or its scheme — the run before the first `:` —
    is exactly `node`. `node:` is the one builtin scheme Node's ESM loader admits as a bare
    identifier, and `normalizeSpecifier` already treats `node:fs` and `fs` as one identity.
  - `foreign` otherwise. State in the docblock that `foreign` is the bucket that exists so no
    spelling can fall outside all three, and name in it the six spellings
    `32-24-RED-baseline.txt` measured — absolute POSIX path, `//localhost/…`, `//host/…`, `file://`,
    a Windows drive-letter path and `data:` — plus `#`-prefixed subpath imports, which resolve
    through the manifest's import map this pass does not read and are therefore refused rather than
    admitted.

ASK THE PARTITION ABOUT CODE, NOT ABOUT PROSE. Add `stripNonCode(source)`: replace every line
comment, every block comment and every template literal with whitespace of equal length, leaving
ordinary string literals intact. This is required rather than cosmetic — measured over the 65 tracked
`.js` files at planning time, widening the three patterns from a dot-leading capture to an
any-character capture produced foreign-classified matches inside a template literal in
`scripts/compactor.js` and inside `scripts/js-import-closure.js`'s own error message, plus eleven
multi-line spans captured out of `//` comments. Re-measure that count yourself before and after
`stripNonCode` and record both numbers; do not take the planner's number on trust.

WIDEN THE THREE PATTERNS AND CLASSIFY WHAT THEY CAPTURE. Add
`moduleSpecifiers(source)` returning one row per captured specifier with its class. Keep the three
emitted forms the module already names — `from "…"`, bare `import "…"`, and `import("…")` — and
change only the capture: any run of characters that contains no quote and NO LINE BREAK. A real
module specifier is on one line; allowing a capture to cross a newline is what produced eleven of
the thirteen false positives measured above. Retain `relativeSpecifiers` as a projection over
`moduleSpecifiers` filtered to `relative`, and say in its docblock that it is now a view rather than
a second scan.

THE WALKER REPORTS THE THIRD BUCKET, AND THE WRAPPER REFUSES IT. Split the walk in two so the
refusal is reachable at a named position in BOTH consumers. `jsImportClosureFacts(root, entryRel)`
performs today's walk and returns `{ modules, foreignEdges }`: a `relative` specifier is resolved and
followed exactly as today, with `assertInsideRoot` and the unresolvable-edge refusal unchanged; a
`bare` specifier is skipped and keeps the module's existing written rule as its reason; a `foreign`
specifier is recorded into `foreignEdges` as `{ module, specifier }` and NOT followed. `jsImportClosure`
keeps its current signature and contract for every existing caller: it calls `jsImportClosureFacts`
and, if `foreignEdges` is non-empty, raises `ImportClosureError` naming EVERY foreign edge, its
module and its specifier, in the same posture the module already takes for an edge it cannot vouch
for. State at the split why it exists: a walker that threw immediately would make the guard's own
foreign-specifier census unreachable, and a predicate that is never ASKED cannot refuse.

THE GUARD ASKS THE SAME AUTHORITY. In `scripts/board-readonly.test.ts`, import `classifySpecifier`
and `jsImportClosureFacts` from `./js-import-closure.js`. Rewrite `noteSpecifier` to switch on
`classifySpecifier`'s answer: `bare` continues to feed `bareSpecifiers` and the existing allow-list
equality; `relative` is recorded as before; `foreign` is pushed into a new
`ModuleFacts.foreignSpecifiers` field AND into `acquisitions`, so a foreign specifier reds the
existing PREMISE case rather than needing a new one to be written. Delete `isBareSpecifier`, so the
class of a specifier is decided in exactly one function in this repository. Have `analyzeClosure`
call `jsImportClosureFacts`, union each module's `foreignSpecifiers` into a new
`ClosureFacts.foreignSpecifiers`, and merge the walk's own `foreignEdges` into that same field so an
edge the walk refused to follow is censused even though no module analysis reached the target.

PROVE THE SLICE END TO END, ON THE ARTIFACT A HOST RUNS. Rebuild (`npm run build`), then plant into
the COMMITTED `scripts/board-read.js` the ONE row this tracer is for: a static import of a writer
module outside the repository root by its ABSOLUTE path, plus an exported function that calls it —
the plant `32-24-RED-baseline.txt` § 3.1 records verbatim, which measured exit 0 at 89 passed. Run
`npm run check:dashboard-readonly` and record the exit code and the NAME of every case that reds.
Restore `scripts/board-read.js` from a backup taken before the plant and confirm
`git diff --exit-code -- scripts/` clean. One spelling, one path, every layer — the other five rows
are Task 2's and Task 3's.

REBUILD THE COMMITTED TWIN IN THE SAME COMMIT. `scripts/js-import-closure.js` is a tracked build
output; a `.ts` edit committed without its twin is the drift the freshness gate exists to catch.
  </action>

  <acceptance_criteria>
    - `scripts/js-import-closure.ts` exports `SpecifierClass`, `SPECIFIER_CLASSES`, `classifySpecifier`, `stripNonCode`, `moduleSpecifiers` and `jsImportClosureFacts`, and `SPECIFIER_CLASSES` has exactly three members asserted against its own length.
    - `scripts/board-readonly.test.ts` contains no function deciding a specifier's class by prefix test; the only class decision is a call to the imported `classifySpecifier`.
    - `ModuleFacts` and `ClosureFacts` each carry a `foreignSpecifiers` field, and a case asserts it is empty over the live dashboard closure.
    - A case asserts each of the ten spellings named in `<behavior>` classifies as stated, including `#internal`, `.`, `..` and the empty string.
    - A case asserts a foreign-looking specifier occurring only inside a line comment or a template literal yields no `moduleSpecifiers` row.
    - The recorded count of foreign-classified captures over the tracked `.js` set is written into the task summary both before and after `stripNonCode` is applied.
    - With the absolute-path writer planted into the committed `scripts/board-read.js`, `npm run check:dashboard-readonly` exits non-zero and the `PREMISE: no closure module acquires a module by a route that is not a static literal import` case is among the cases that red.
    - `git diff --exit-code -- scripts/` is clean after the plant is restored, and `scripts/js-import-closure.js` is rebuilt from its `.ts` in the same commit.
  </acceptance_criteria>

  <verify>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or the freshness output names scripts/js-import-closure.js among the files that do not match a rebuild of their sources</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or a passed-case count below the 89 recorded in 32-24-RED-baseline.txt § 0 — cases were replaced rather than added</fails_when>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit on the UNPLANTED tree, or a run reporting 0 passed — the named gate wraps this one file and a zero-case run is a green line over an empty denominator</fails_when>
    <automated>git diff --exit-code -- scripts/board-read.js scripts/board-model.js scripts/board-dashboard.js</automated>
    <fails_when>non-zero exit — the tracer's plant was left in a tracked closure module and every later measurement in this round is contaminated</fails_when>
  </verify>

  <done>The class of a module specifier is decided once, both consumers ask that one authority, and the absolute-path writer that measured exit 0 at 89/89 now reds the write-detection mechanism on the committed `.js`.</done>

  <reversibility rating="costly">`jsImportClosure` is consumed by seven production callers (`hooks/hook-entry`, `scripts/check-platform-shapes`, `scripts/context-freshness`, `scripts/generate-hook-manifest`, `scripts/guarantees-freshness`, `scripts/now-running-freshness`, `scripts/trace-freshness`) and six test harnesses, so a refusal added here reaches gates outside this phase. Task 2's per-caller converse is what bounds that cost. The classifier itself is a pure function and is reversible.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The partition proved total over the six measured spellings, and the relocated refusals carried</name>

  <read_first>
    - `scripts/js-import-closure.ts` as rewritten by Task 1 — `classifySpecifier`, `stripNonCode`,
      `moduleSpecifiers`, `jsImportClosureFacts`, and every exported symbol, because each is a caller
      contract this task must not break
    - `scripts/board-readonly.test.ts` lines 1237-1345 — `ALLOWED_BUILTIN_SPECIFIERS`,
      `ALLOWED_BUILTIN_SPECIFIER_COUNT`, `normalizedBuiltinIdentities`, `BANNED_MODULES` and
      `ADDITIONAL_BANNED_MODULES`, and the paragraph stating why equality rather than containment is
      the mechanism — this is the authority the relocated spellings are leaving
    - `scripts/board-readonly.test.ts` lines 1360-1440 — the two-sided members and count cases and
      the positive control, which are the four of the six cases `32-24-RED-baseline.txt` § 3.4
      records as reddening for `file://` today
    - `scripts/board-readonly.test.ts` — `MODULE_IDENTITY_SHAPES` and `STEM_FALSE_POSITIVES`, the
      rows-held-as-data and named-exclusion postures this task copies
    - `.planning/phases/32-board-projector-cli-dashboard/32-24-RED-baseline.txt` §§ 2, 3.4, 3.5 and 4
      — the six-row result table, the `file://` and `C:\` transcripts naming the six cases each reds
      today, and the ten-row partition table including the `data:` row measured as bare
    - Every caller of `jsImportClosure`, `jsImportClosureFacts`, `copyImportClosure` and
      `closureTargets`, found by grepping `js-import-closure` under `scripts/` and `hooks/` — at
      planning time that is seven production modules and six test harnesses, and each one's mirror is
      a legitimate input this task must re-run
    - `package.json` scripts block — the `freshness:*` and `check:*` commands that carry those
      callers' gates, read so the converse list is derived rather than recalled
  </read_first>

  <files>scripts/board-readonly.test.ts, scripts/js-import-closure.ts, scripts/js-import-closure.js</files>

  <behavior>
    - Given each of the six spellings in `32-24-RED-baseline.txt` § 2, `classifySpecifier` returns `foreign`.
    - Given `data:text/javascript,export const a=1`, `classifySpecifier` returns `foreign` where `isBareSpecifier` returned true.
    - Given `file:///abs/writer.mjs` planted in a closure module, the acquisitions PREMISE case fails.
    - Given `C:\x\writer.mjs` planted in a closure module, the acquisitions PREMISE case fails.
    - Given `data:text/javascript,…` planted in a closure module, the acquisitions PREMISE case fails.
    - Given the live dashboard closure, `foreignSpecifiers` is empty, `bareSpecifiers` still normalizes to exactly `ALLOWED_BUILTIN_SPECIFIERS`, and the allow-list count is still 3.
    - Given every existing caller entry, `jsImportClosure` returns the same module list it returned before the cutover.
  </behavior>

  <action>
HOLD THE SPELLINGS AS DATA, AND COVER THE SET THE BASELINE MEASURED. Declare `SPECIFIER_CLASS_ROWS`
in `scripts/board-readonly.test.ts` — one row per spelling with a name, the specifier text and its
expected class — with `SPECIFIER_CLASS_ROW_COUNT` pinned two-sided so a row added without a decision
moves a number somebody looks at. The row set must contain, at minimum, every row of
`32-24-RED-baseline.txt` § 4's ten-spelling table: an absolute POSIX path, a `file://` URL, a Windows
drive-letter path, `//host/x.js`, `//localhost/<abs path>`, a `data:` specifier, `./relative.js`,
`../up/relative.js`, `node:fs` and `@scope/pkg` — plus `#internal`, `.`, `..` and the empty string as
the degenerate members. Assert every row classifies as its expected class and that the set of classes
the rows use equals `SPECIFIER_CLASSES`, so a class nobody exercised is visible.

RECORD THE RELOCATION EXPLICITLY, AND PROVE THE NEW BRANCH IS NOT WEAKER. Declare
`RELOCATED_SPECIFIER_ROWS` with one row for each spelling whose REFUSING AUTHORITY moves —
`file:///abs/writer.mjs`, `C:\x\writer.mjs` and the `data:` specifier — each carrying the spelling,
the authority that refused it before the cutover (the builtin allow-list, reached because it landed
in `bareSpecifiers`), the authority that refuses it after (the foreign-specifier branch), and the
name of the case that must red after. Pin `RELOCATED_SPECIFIER_ROW_COUNT`. Then assert, per row and
mechanically: the spelling is absent from `normalizedBuiltinIdentities` of a module carrying it (it
has left the old authority), it is present in that module's `foreignSpecifiers` (it has arrived at
the new one), and planting it makes the acquisitions PREMISE case fail. That last assertion is the
whole point: the PREMISE case is the write-detection mechanism, whereas the allow-list equality it is
leaving is a census, so the relocated refusal must be measured as at least as strong rather than
assumed to have survived the move.

PIN THE PARTITION AND THE LIVE CLOSURE TWO-SIDED. Assert `foreignSpecifiers` is EMPTY over the live
dashboard closure and over the model closure; assert `normalizedBuiltinIdentities` still equals
`ALLOWED_BUILTIN_SPECIFIERS` with `ALLOWED_BUILTIN_SPECIFIER_COUNT` unmoved at 3; and assert the
banned-module corollary and the contradiction case are both unchanged. A cutover that quietly moved a
legitimate builtin out of the allow-list would pass every new case and break the old mechanism.

RE-RUN EVERY CALLER WITH A LEGITIMATE INPUT, AND PIN THE CLOSURES. A refusal added to a shared walker
is a regression risk for every mirror it builds, and narrowing the scan's input is a regression risk
for every edge a comment used to contribute. Declare `CLOSURE_BASELINES`: one row per existing caller
entry, each carrying the entry path and the module list `jsImportClosure` returns for it. Populate
the rows by MEASURING the pre-cutover closures first — check out the pre-cutover
`scripts/js-import-closure.js` into a scratch copy if needed rather than recalling them — then assert
the post-cutover closure equals the recorded list for every row. Where a row differs, do not adjust
the expectation: record the entry, the module that left or joined, and the exact comment or template
literal that produced the old edge, and run that caller's own gate to prove the difference is
harmless. Then run each caller's gate — the `freshness:*` and `check:*` commands named in
`<read_first>`, plus `node scripts/validate-agent-factory.js` and `node scripts/check-foundation-guards.js`
— and record every exit code in the GREEN proof.

MAKE THE FAILING MESSAGES DECISION-SHAPED. The foreign-specifier emptiness case's message must state
what admitting a foreign specifier would CLAIM — that a module reached by a path this walk cannot
mirror is nevertheless known to hold no writer — rather than telling a maintainer which list to add
it to. A message that names the cheapest way back to green is the mechanism this round's sibling
finding exploited.

REBUILD THE COMMITTED TWIN IN THE SAME COMMIT if `scripts/js-import-closure.ts` moved in this task.
  </action>

  <acceptance_criteria>
    - `SPECIFIER_CLASS_ROWS` contains at least the fourteen spellings named in the action, including `//localhost/<abs path>`, `//host/x.js` and a `data:` specifier, and is asserted equal to `SPECIFIER_CLASS_ROW_COUNT`.
    - A case asserts the set of classes exercised by `SPECIFIER_CLASS_ROWS` equals `SPECIFIER_CLASSES`.
    - `RELOCATED_SPECIFIER_ROWS` has exactly three rows — `file://`, the Windows drive-letter path and `data:` — each naming the before-authority, the after-authority and the case name, and is asserted equal to `RELOCATED_SPECIFIER_ROW_COUNT`.
    - A case asserts, per relocated row, that the spelling is absent from the module's normalized builtin identities, present in its `foreignSpecifiers`, and that planting it makes the acquisitions PREMISE case fail.
    - A case asserts `foreignSpecifiers` is empty over both the dashboard closure and the model closure, and `ALLOWED_BUILTIN_SPECIFIER_COUNT` is still 3.
    - `CLOSURE_BASELINES` carries one row per caller entry found by the grep in `<read_first>`, and a case asserts every post-cutover closure equals its recorded pre-cutover list; any difference is named in `32-31-GREEN-proof.txt` with the comment or template literal that produced the old edge.
    - Every caller gate named in `<read_first>` has its exit code recorded in `32-31-GREEN-proof.txt`.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or a passed-case count below the 89 recorded in 32-24-RED-baseline.txt § 0</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness &amp;&amp; npm run freshness:context &amp;&amp; npm run freshness:guarantees &amp;&amp; npm run freshness:queue &amp;&amp; npm run freshness:traceability</automated>
    <fails_when>non-zero exit from any command in the chain — a shared walker refusing too much, or a narrowed scan input dropping an edge a mirror needed, shows up here as a freshness gate that can no longer build its mirror</fails_when>
    <automated>npm run check:platform-shapes &amp;&amp; node scripts/validate-agent-factory.js &amp;&amp; node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit from any of the three — a caller outside this phase lost its mirror or its structure check</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or a passed-case count below the 4974 recorded in 32-VERIFICATION.md</fails_when>
  </verify>

  <done>Every spelling the baseline measured has a class, the three relocated refusals are measured at the write-detection mechanism rather than assumed, and every existing caller still builds the same mirror it built before.</done>

  <reversibility rating="costly">`CLOSURE_BASELINES` freezes the mirror contents of seven production gates; a difference recorded here is a change to what those gates copy. The census rows are reversible.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The per-row flip proved on the artifact a host runs, with the case name per row and the authority per spelling</name>

  <read_first>
    - `.planning/phases/32-board-projector-cli-dashboard/32-24-RED-baseline.txt` §§ 2, 3.1-3.6 and 5
      — the six plants, their recorded exit codes and case counts, and § 5's three statements about
      what this proof must now be shaped like
    - `scripts/board-readonly.test.ts` — the cases added in Tasks 1 and 2, so the probe targets the
      arm the fix was written for AND the arms beside it
    - `scripts/js-import-closure.ts` — the rewritten walk, so the probe asks what happens when a
      foreign specifier appears in a module the walk reaches at depth 2 rather than at the entry
    - `.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md` § 4 — the six
      neighbour-variation questions, which are the template for this task's probe table
    - `.planning/phases/32-board-projector-cli-dashboard/32-20-GREEN-proof.txt` — the proof format
      this round's proofs stay comparable with, row for row
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-31-GREEN-proof.txt</files>

  <action>
REBUILD FIRST, THEN RE-RUN ALL SIX BASELINE PLANTS AGAINST THE COMMITTED `.js`. Run `npm run build`,
then plant each of `32-24-RED-baseline.txt` § 2's six writers into the committed
`scripts/board-read.js` in turn and run `npm run check:dashboard-readonly`.

WRITE THE FLIP TABLE, AND MAKE IT PER ROW RATHER THAN A COUNT. One row per spelling with six
columns: the spelling; the baseline exit code; the baseline case counts; the post-cutover exit code;
the post-cutover case counts; and THE NAME OF THE CASE THAT REDS IT NOW. Write each row's transition
column in the arrow notation the baseline already uses for an exit code, so the three discriminating
rows are greppable — the exit code before, a space-arrow-space, the exit code after. The three rows
that were GREEN in the baseline — the absolute POSIX path, `//localhost/<abs path>` and
`//host/<path>` — are the discriminating rows: each must move from a zero exit with the full passed
count to a non-zero exit, and each must name a case. A count of refusals across the table cannot
decide this, because three of the six rows already exited 1 before any fix; the flip on those three
specific rows is the only measurement that distinguishes this cutover from a proof that changed
nothing.

WRITE THE REFUSAL-AUTHORITY COLUMN, BEFORE AND AFTER, PER SPELLING. Beside the flip table, one row
per spelling with two more columns: which predicate refused it BEFORE the cutover, and which refuses
it AFTER. For the three baseline-green rows the before cell is "none — the gate was green". For
`file://` and the Windows drive-letter path the before cell is the builtin allow-list (the baseline
records the six specific cases each reds today), and for `data:` the before cell is the same
authority reached by the same route. The after cell for all six must be the foreign-specifier branch,
and the row must record whether the acquisitions PREMISE case is among the cases that red — because a
relocation that lands only on a census count has made the tree weaker for that spelling while making
it stronger for three others, and that net has to be measured per spelling rather than asserted.

THEN ASK THE SIBLING-ARM QUESTION PER POSITION, because five consecutive rounds across this project's
phases were "correct in the arm it was written for and wrong in the arm next to it". Plant the same
absolute specifier at each position a specifier can enter, one per row, and record the result:

  - a static `import … from` declaration
  - a bare side-effect `import "…"` declaration
  - an `export … from` re-export declaration
  - a dynamic `import("…")` call
  - a `require("…")` call
  - the same, in a module at DEPTH 2 of the closure rather than in the entry
  - the same, inside a module the walk reaches only through a re-export chain
  - the same, written inside a line comment, which must NOT red — the converse for `stripNonCode`
  - the same, written inside a template literal, which must NOT red — the second `stripNonCode` converse

DRIVE THE CONVERSE, ONCE PER REFUSAL BRANCH. For every new refusal, plant a LEGITIMATE input and
record it passing: a `./relative.js` import at each of the positions above, a `node:path` import, and
an unplanted rebuild of every caller's mirror from `CLOSURE_BASELINES`. A refusal that also refuses
the legitimate shape is a regression, and this is where it is caught rather than by the verifier.

WRITE THE REACHABILITY ANSWER FOR THE NEW REFUSAL. State, in one paragraph, how the `foreign` branch
is REACHED at runtime — which call site evaluates it, with what input, under which gate command —
rather than only what it refuses. The project's recorded lesson is that asking only what a predicate
refuses leaves the positions it is never ASKED at unexamined, and this cutover deliberately moved one
refusal from the walker's throw to the guard's census precisely so both positions are reachable.

RESTORE AND PROVE CLEAN after every plant: restore `scripts/board-read.js` from a backup taken before
the first plant and record `git diff --exit-code -- scripts/` returning clean in the proof file. A
probe that leaves a byte behind contaminates every measurement after it.
  </action>

  <acceptance_criteria>
    - `32-31-GREEN-proof.txt` carries a six-row flip table with the baseline exit code, the post-cutover exit code, both case counts, and the name of the case that reds each row.
    - Every flip row writes its transition in arrow notation as the exit code before, a space, a hyphen-greater-than arrow, a space, and the exit code after.
    - The three rows the baseline recorded at a zero exit with the full passed count each record a non-zero post-cutover exit and a named case.
    - The proof carries a per-spelling refusal-authority table with a before cell and an after cell for all six spellings, and records per row whether the acquisitions PREMISE case is among the failures.
    - The proof carries the nine-row position table, including the two `stripNonCode` converse rows that must NOT red, each with a measured result.
    - The proof carries a converse row per new refusal branch showing a legitimate input still passing, and the per-caller mirror rebuild results.
    - The proof carries a one-paragraph reachability answer naming the call site, the input and the gate command for the `foreign` branch in both the walker and the guard.
    - `git diff --exit-code -- scripts/` is recorded clean after the final plant.
  </acceptance_criteria>

  <verify>
    <automated>npm run build &amp;&amp; npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit on the unplanted tree — the cutover refuses the legitimate closure, or the rebuild moved a committed .js the guard reads</fails_when>
    <automated>git diff --exit-code -- scripts/ agent-factory/ docs/</automated>
    <fails_when>non-zero exit — a plant was left in a tracked file</fails_when>
    <automated>grep -c 'PREMISE' .planning/phases/32-board-projector-cli-dashboard/32-31-GREEN-proof.txt</automated>
    <fails_when>a printed count below 6 — fewer than the six spellings recorded what the write-detection mechanism did, which is the only column that distinguishes a relocated refusal from a weakened one</fails_when>
    <automated>grep -c 'exit 0 -> exit' .planning/phases/32-board-projector-cli-dashboard/32-31-GREEN-proof.txt</automated>
    <fails_when>a printed count below 3 — fewer than the three baseline-GREEN rows recorded a flip, so the proof has not measured the only rows that can distinguish this cutover from a no-op</fails_when>
  </verify>

  <done>Each of the three spellings that were green over a live writer now reds a named case on the committed `.js`, each relocated refusal is measured at the mechanism rather than at a count, and the sibling positions and legitimate converse are recorded.</done>

  <reversibility rating="reversible">A measurement written to a planning artifact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository tree → the dashboard's module closure | every agent this kit runs can write a file under `scripts/`, so a module inside the closure is attacker-modifiable by the threat model this phase already assumes |
| the committed `.js` closure → `npm run check:dashboard-readonly` | the guard is the only mechanism deciding DASH-06; if it is green over a writer, nothing else refuses one |
| a module specifier → the closure walker's mirrors | `jsImportClosure` decides which files a gate's temp mirror carries; an edge it cannot vouch for is a mirror missing exactly the file the walk could not see |
| a comment or template literal → the specifier scan's input | the scan's input is assembled by regex over whole file bytes, so prose can manufacture a specifier that no import statement carries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-31-01 | Tampering | `scripts/board-readonly.test.ts` specifier census | critical | mitigate | Total three-way partition with `foreign` refused by construction, the class decided by one exported authority both consumers call (Task 1), proven by the per-row exit-code flip on the committed `.js` (Task 3) |
| T-32-31-02 | Elevation of Privilege | the dashboard closure's write capability | critical | mitigate | A foreign specifier lands in `acquisitions`, which the existing PREMISE case asserts empty, so the write-detection MECHANISM reds rather than only a census count |
| T-32-31-03 | Tampering | relocation of the `file://`, `C:\` and `data:` refusals | high | mitigate | `RELOCATED_SPECIFIER_ROWS` measures each spelling's before-authority and after-authority and requires the PREMISE case among the post-cutover failures (Task 2), with the per-spelling column recorded in the proof (Task 3) |
| T-32-31-04 | Tampering | `scripts/js-import-closure.ts` mirror construction | high | mitigate | The walk reports a foreign edge and the wrapper refuses it, the posture the module already states for an unresolvable edge; `CLOSURE_BASELINES` pins every caller's mirror contents across the change |
| T-32-31-05 | Denial of Service | every gate whose mirror uses `jsImportClosure` | medium | mitigate | Task 2 re-runs seven production callers' gates plus the structure validator and the foundation guard, and Task 3 records a converse row per refusal, so an over-refusal is caught inside this plan |
| T-32-31-06 | Spoofing | a specifier manufactured by prose | medium | mitigate | `stripNonCode` removes comments and template literals before the patterns run, the capture excludes line breaks, and two converse rows in Task 3 assert a commented and a template-interpolated foreign specifier do NOT red |
| T-32-31-07 | Information Disclosure | `ImportClosureError` refusal text | low | accept | The refusal names a specifier and a module path already present in the tree the operator is running; it quotes no file content, matching the module's existing refusal rule |

No package-manager install task exists in this plan or this round — the repository ships zero runtime
dependencies and `package.json` has no `dependencies` key — so no `T-32-SC` supply-chain row and no
package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness` — exit 0,
  and the committed `scripts/js-import-closure.js` matches a rebuild of its source.
- `npm run check:dashboard-readonly` — exit 0 on the unplanted tree, non-zero with any of the six
  baseline plants present.
- `npm run freshness:context && npm run freshness:guarantees && npm run freshness:queue && npm run freshness:traceability && npm run check:platform-shapes`
  — exit 0; every caller of the shared walker still builds its mirror.
- `npx vitest run --exclude '**/scripts/e2e/**'` — exit 0, passed-case count at or above 4974.
- `32-31-GREEN-proof.txt` carries the six-row flip table with a case name per row, the per-spelling
  refusal-authority before/after table, the nine-row position table, the converse rows and the
  reachability paragraph.
- `git diff --exit-code -- scripts/ agent-factory/ docs/` clean at the end of every task.
</verification>

<success_criteria>
The class of a module specifier is decided in exactly one exported function; the closure walker and
the read-only guard both ask it; the third bucket is refused by construction rather than by
enumeration; each of the three spellings that were green over a live writer flips to a named failing
case measured on the committed `.js`; the three spellings whose refusal relocates are measured at the
write-detection mechanism rather than assumed to have survived the move; and every existing caller of
the walker still builds the same mirror.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-31-SUMMARY.md` when done.
</output>

---
phase: 32-board-projector-cli-dashboard
plan: 12
type: execute
wave: 3
gap_closure: true
depends_on: ["32-09", "32-10"]
files_modified:
  - scripts/validate-agent-factory.ts
  - scripts/validate-agent-factory.js
  - scripts/board-model.ts
  - scripts/board-model.js
  - scripts/validate.test.ts
  - scripts/fixtures/bad-ticket-body-column/plans/tickets/ABC-001.md
  - scripts/fixtures/bad-ticket-no-region/plans/tickets/ABC-001.md
  - scripts/fixtures/bad-ticket-duplicate-key/plans/tickets/ABC-001.md
  - agent-factory/contracts/board.md
  - .planning/phases/32-board-projector-cli-dashboard/32-12-RED-baseline.txt
  - .planning/phases/32-board-projector-cli-dashboard/32-12-GREEN-proof.txt
autonomous: true
requirements: [DASH-01, DASH-02]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Exactly one ticket-frontmatter reader exists in `scripts/*.ts`, and it is `parseTicketDocument` in `scripts/board-model.ts` — proven by a census derived from a glob over the tree, not by a hand-typed file list."
    - "`checkTickets()` reads a ticket's `column:` and `status:` through `parseTicketDocument`; a document the grammar refuses is reported by refusal code rather than silently half-read."
    - "The two published board-vs-ticket message strings survive the cutover byte-identically."
    - "No docblock in `scripts/board-model.ts` states a fact about the tree that is false on the tree — the claim that plan 32-08 deleted the second pair is corrected or made true."
    - "The three points where the two readers disagreed — a `column:` line in the prose or a fenced body, a document with no frontmatter region, and a duplicated key — each have a recorded RED/GREEN fixture baseline showing the answer before and after."
  artifacts:
    - "scripts/validate-agent-factory.ts — the second reader and its interface deleted, `checkTickets()` routed through the grammar"
    - "scripts/validate.test.ts — three disagreement fixtures, the message-preservation pin, and the derived single-authority census"
    - "agent-factory/contracts/board.md — the deviation the cutover introduces, recorded"
    - ".planning/phases/32-board-projector-cli-dashboard/32-12-RED-baseline.txt and 32-12-GREEN-proof.txt"
  key_links:
    - "checkTickets -> parseTicketDocument: the validator's ticket reading becomes an import edge a census can walk"
    - "the census -> scripts/*.ts glob: the scanned set is derived from the tree, so a new file carrying a third reader is seen"
    - "board-model.ts docblock -> the tree: a claim about what a plan deleted is only admissible while it is true"
---

<objective>
Close the DASH-01 verification gap: the phase's "single board-grammar authority" claim is false on
the current tree, and one of the modules says otherwise in its own docblock.

Gap mapping for this plan (from `32-VERIFICATION.md` frontmatter `gaps:`, truth 1):

| Gap | Finding | Task |
|-----|---------|------|
| "`scripts/board-model.ts` is the ONLY board grammar in the tree" | CR-06: a second ticket-frontmatter reader survives verbatim at `scripts/validate-agent-factory.ts:716-723`, used at `:742`, alongside `board-model.ts`'s `parseTicketDocument`. The two disagree by construction on three inputs. | Task 1 (the disagreement baseline), Task 2 (the cutover) |
| "…and no docblock claim that is false on the current tree" | CR-06, second half: `scripts/board-model.ts:816-819` asserts that plan 32-08 deleted the pair. It did not. `CLAUDE.md` names fabrication as a hard failure, and the trace is this project's stated value. | Task 3 |

Purpose: D-06's whole narrative is that one predicate gets one authority. Two readers of a ticket's
`column:` mean `npm run` validation and the dashboard can report different columns for the same
ticket, which is exactly the drift class DASH-01 exists to close. The docblock half is worse than the
functional half: a reader who trusts it concludes the work is done.

Output: one reader, a deleted second spelling, three recorded disagreement baselines, a census
derived from a glob rather than typed out, and a docblock that is true.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` and no `## Prohibitions`
section to satisfy.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-08-SUMMARY.md
@.planning/phases/32-board-projector-cli-dashboard/32-01-SUMMARY.md
</context>

## Artifacts this phase produces

- Deleted from `scripts/validate-agent-factory.ts`: the local ticket-frontmatter helper at ~716-723
  and its `FrontMatter` interface at ~712-715. Nothing replaces them in that file.
- `scripts/validate-agent-factory.ts` gains `parseTicketDocument` to its existing
  `./board-model.js` import list (which already carries `boardHasColumn`, `kebab`, `parseBoard`).
- One new validator error message for a ticket the grammar refuses, naming the refusal CODE and the
  refusal sentence. It is a new published surface and is pinned by a case.
- Three new fixture kits under `scripts/fixtures/`, each isolating ONE disagreement point:
  a ticket whose `column:` appears only in its prose or fenced body; a ticket with no frontmatter
  region at all; a ticket with a duplicated `column:` key. Named for what they isolate, in the style
  of the existing `bad-ticket-bad-column` and `bad-ticket-mismatch` kits.
- `TICKET_FRONTMATTER_READER_COUNT` — a two-sided pin in `scripts/validate.test.ts` over a census
  derived from a glob of `scripts/*.ts`.
- A corrected docblock in `scripts/board-model.ts` at ~816-819.
- One new sentence in `agent-factory/contracts/board.md` § Ticket documents recording the deviation
  the cutover introduces for a ticket with no frontmatter region.
- `.planning/phases/32-board-projector-cli-dashboard/32-12-RED-baseline.txt` and
  `32-12-GREEN-proof.txt`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure the disagreement — three fixtures, three recorded answers, before any deletion</name>

  <read_first>
    - `scripts/validate-agent-factory.ts` lines 710-757 — the `FrontMatter` interface, the local
      reader, `checkTickets()` and its two published error messages
    - `scripts/board-model.ts` lines 825-1010 — `TICKET_KEYS`, `TICKET_REFUSAL_CODES`,
      `TICKET_DELIMITER`, `TICKET_KEY_LINE`, `TICKET_CONTROL` and `parseTicketDocument`
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` — CR-06's three-bullet list of
      exactly where the two readers disagree
    - `scripts/validate.test.ts` lines 225-240 (`runFixture` and the fixture table), 1245-1262
      (`kitWithBoardAndTicket`, `copyGoodKit`) and 1349-1362 (the byte-identical message pin)
    - `scripts/fixtures/bad-ticket-bad-column/plans/tickets/ABC-001.md` — the fixture-authoring style,
      including the in-file comment naming the single mutation the fixture isolates
  </read_first>

  <files>scripts/validate.test.ts, scripts/fixtures/bad-ticket-body-column/, scripts/fixtures/bad-ticket-no-region/, scripts/fixtures/bad-ticket-duplicate-key/, .planning/phases/32-board-projector-cli-dashboard/32-12-RED-baseline.txt</files>

  <action>
BUILD THE THREE DISAGREEMENT FIXTURES FIRST, AND MEASURE BOTH READERS AGAINST EACH. Each fixture is
a copy of the `good` kit carrying one ticket that isolates exactly one disagreement, with a comment
in the ticket body naming the single mutation — the convention
`scripts/fixtures/bad-ticket-bad-column/plans/tickets/ABC-001.md` already sets.

  1. BODY MATCH. A ticket with a well-formed frontmatter region naming one column, plus a later line
     in the prose body — and a second inside a fenced block — that opens with the `column:` key
     spelling and names a DIFFERENT column. The local reader's pattern is line-anchored with no
     region bound, so it answers with whichever of the three it finds first; `parseTicketDocument`
     reads only between the first two delimiters. Pick the values so the two readers produce
     DIFFERENT validator verdicts, not merely different strings — one must make the membership check
     fail and the other must not, otherwise the fixture measures nothing.
  2. NO REGION. A ticket that is plain markdown with a `column:` line somewhere in it and no
     delimiter line at all. The local reader accepts it; `parseTicketDocument` refuses it with the
     `no-opening-delimiter` code.
  3. DUPLICATED KEY. A ticket whose frontmatter region carries the `column:` key twice with different
     values. The local reader silently takes the first; `parseTicketDocument` refuses with
     `duplicate-key`.

RECORD THE RED BASELINE. Run the CURRENT spawned validator over each of the three fixtures and
capture its exact stdout, stderr and exit status into
`.planning/phases/32-board-projector-cli-dashboard/32-12-RED-baseline.txt`. In the same file, record
what `parseTicketDocument` answers for the same three ticket bodies, called directly. The two
columns of that table ARE the finding: a baseline where the two readers agree on all three rows
means the fixtures do not isolate the disagreement and must be rebuilt before anything is deleted.

WIRE THE FIXTURES INTO THE SUITE with cases that assert the CURRENT behaviour, so the cutover in
Task 2 is a visible, reviewable change of answer rather than an invisible one. Use the existing
`runFixture` helper and the existing fixture-table style. Each case's message names which reader
produced the answer being pinned.
  </action>

  <behavior>
    - Given the body-match fixture, the spawned validator today reports a verdict derived from a `column:` line outside the frontmatter region.
    - Given the no-region fixture, the spawned validator today exits 0 for the column and status checks (it reads a value from a document that has no frontmatter).
    - Given the duplicated-key fixture, the spawned validator today reports the FIRST of the two values with no finding about the duplication.
    - Given the same three bodies, `parseTicketDocument` answers: the frontmatter-region value; a `no-opening-delimiter` refusal; a `duplicate-key` refusal.
  </behavior>

  <acceptance_criteria>
    - Three new fixture directories exist under `scripts/fixtures/`, each with exactly one ticket file, each ticket carrying an in-file comment naming its single mutation.
    - `.planning/phases/32-board-projector-cli-dashboard/32-12-RED-baseline.txt` contains a three-row table whose two columns differ on every row.
    - Three new cases in `scripts/validate.test.ts` pass against the UNCHANGED validator.
    - `git ls-files -- scripts/fixtures/` lists the three new ticket files after they are added and committed.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts</automated>
    <fails_when>exit code is non-zero, or the run reports 0 passed — a filter matching nothing is a green line over an empty denominator</fails_when>
    <automated>node scripts/validate-agent-factory.js</automated>
    <fails_when>exit code is non-zero — adding fixtures must not change the validator's verdict on the repository's own tree</fails_when>
  </verify>

  <done>
The three points where the two readers disagree are fixtures in the tree with a recorded before-table,
so the cutover's effect on each is a measured change rather than a hoped-for one.
  </done>

  <reversibility rating="reversible">Three fixture directories and three test cases; nothing in production code has changed yet.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The cutover — one reader, and the two published messages preserved byte-for-byte</name>

  <read_first>
    - `scripts/validate-agent-factory.ts` line 78 (the existing `./board-model.js` import list), lines
      710-757 (the helper, the interface and `checkTickets()`)
    - `scripts/board-model.ts` — `parseTicketDocument`'s `TicketAdmission` result shape and the six
      `TICKET_REFUSAL_CODES`
    - `scripts/validate.test.ts` lines 1349-1362 — the byte-identical message pin, which must keep
      passing unmodified
    - `scripts/validate.test.ts` lines 1263-1330 — the two D-06 deviation cases and the WR-03
      regression pin, all of which run through `checkTickets()` and must keep passing
    - `.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md` — D-06 (the deletion is part
      of the decision) and D-07 (refuse outside the form, never widen the parser)
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` — CR-06's cutover sketch (one
      candidate route, not binding)
  </read_first>

  <files>scripts/validate-agent-factory.ts, scripts/validate.test.ts, agent-factory/contracts/board.md</files>

  <action>
DELETE THE SECOND SPELLING. Remove the local ticket-frontmatter helper and its `FrontMatter`
interface from `scripts/validate-agent-factory.ts` entirely. Do not leave a wrapper, a re-export or a
commented-out copy: a second spelling that is only reachable from a comment is still a second
spelling for the next reader.

ROUTE `checkTickets()` THROUGH THE GRAMMAR. Add `parseTicketDocument` to the file's existing
`./board-model.js` import. For each ticket file, call it on the text and branch on the admission:
  - REFUSED → emit a new error naming the relative path, the refusal CODE and the refusal sentence,
    then `continue`. This document has no trustworthy `column:` and no trustworthy `status:`, so the
    membership and kebab rules are not run against a guess.
  - ADMITTED → take `column` and `status` from the admitted document and run the two existing rules
    with their existing messages UNCHANGED.

PRESERVE THE TWO PUBLISHED MESSAGES BYTE-FOR-BYTE. The case at `scripts/validate.test.ts:1352-1359`
asserts both strings verbatim; it must keep passing WITHOUT being edited. If the cutover requires
editing that case, the cutover changed a published surface and the change is a decision, not a side
effect — stop and record it rather than adjusting the expectation.

FLIP THE THREE DISAGREEMENT CASES from Task 1 to their post-cutover answers, and make each case's
message say which answer is now authoritative and why. The three ARE the behaviour change this plan
buys, so each expectation change is the deliverable, not a fixed-up test.

RECORD THE DEVIATION IN THE CONTRACT. `agent-factory/contracts/board.md` § Ticket documents already
names `parseTicketDocument` as the reader of this class. Add one sentence stating what the validator
now does with a ticket the grammar refuses: the document is reported by refusal code and its column
and status rules are not evaluated, because a value read from a document the grammar refused is a
guess. This is the same posture D-07 records for a non-canonical heading — refuse outside the form,
never widen the parser.

CHECK THE TREE'S OWN TICKETS BEFORE AND AFTER. `plans/tickets/` in this repository is empty today, so
`checkTickets()` returns early under the existing vacuity rule and the cutover cannot change this
tree's verdict. Assert that rather than assume it: run the spawned validator on the repository root
bare and with `--strict`, and record both exit statuses in the GREEN proof. The `good`,
`warn-only-no-trace`, `bad-ticket-bad-column` and `bad-ticket-mismatch` fixture tickets are all
well-formed frontmatter documents and must all keep their current verdicts — that is the
legitimate-input probe for this cutover, and it is asserted by the existing fixture table continuing
to pass unmodified.
  </action>

  <behavior>
    - Given the body-match fixture, the validator now derives the column from the frontmatter region only, and the prose and fenced `column:` lines have no effect on its verdict.
    - Given the no-region fixture, the validator now emits an error naming the `no-opening-delimiter` code, and the column and status rules are not run.
    - Given the duplicated-key fixture, the validator now emits an error naming the `duplicate-key` code.
    - Given `bad-ticket-bad-column`, the validator emits the byte-identical "is not a board column" message it emits today.
    - Given `bad-ticket-mismatch`, the validator emits the byte-identical status-mismatch message it emits today.
    - Given the repository's own tree (zero tickets), the validator exits 0 bare and 0 with `--strict`, exactly as it does today.
  </behavior>

  <acceptance_criteria>
    - `grep -v '^\s*//' scripts/validate-agent-factory.ts | grep -c 'text.match(/\^column'` returns `0`, and `grep -v '^\s*//' scripts/validate-agent-factory.ts | grep -c 'text.match(/\^status'` returns `0` — comment lines are excluded so a comment can neither satisfy nor invalidate either gate. Both patterns belonged to the deleted reader and to nothing else in the file.
    - The Task 3 census reports `TICKET_FRONTMATTER_READER_COUNT === 1` and names `scripts/board-model.ts` as the carrier — the structural half of the same claim, derived rather than grepped.
    - `grep -c 'parseTicketDocument' scripts/validate-agent-factory.ts` returns at least `2` — the import and at least one call site.
    - The case "keeps both board↔ticket messages byte-identical through the extraction" passes with NO edit to its expected strings (verify with `git diff -- scripts/validate.test.ts` showing no change inside that case body).
    - The existing fixture table rows for `bad-ticket-mismatch`, `bad-ticket-bad-column` and `warn-only-no-trace` pass unmodified.
    - `node scripts/validate-agent-factory.js` exits 0 and `node scripts/validate-agent-factory.js --strict` exits 0 on the repository root.
    - `agent-factory/contracts/board.md` § Ticket documents carries the new refusal sentence.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts scripts/board-model.test.ts</automated>
    <fails_when>exit code is non-zero, or the run reports 0 passed for either file</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity</automated>
    <fails_when>exit code is non-zero, or stdout contains `BUILD PARITY FAILED` — the committed scripts/validate-agent-factory.js must be rebuilt from the edited .ts</fails_when>
    <automated>node scripts/validate-agent-factory.js &amp;&amp; node scripts/validate-agent-factory.js --strict</automated>
    <fails_when>either invocation exits non-zero, or output contains `FAIL` — the cutover changed this repository's own verdict, which it must not (zero tickets, vacuous check)</fails_when>
  </verify>

  <done>
`checkTickets()` reads tickets through the one grammar, the second reader is gone from the file, the
two published messages are unchanged, and the three disagreement fixtures now answer with the
grammar's verdict.
  </done>

  <reversibility rating="costly">Deleting the second reader changes the validator's verdict for malformed tickets in every installed repository — a ticket with no frontmatter region becomes an error where it was silently ignored. That is D-06/D-07's stated intent, and the contract sentence records it, but reverting after release would be a second behaviour change rather than a restoration.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Derive the single-authority census from the tree, and make the docblock true</name>

  <read_first>
    - `scripts/validate.test.ts` lines 1330-1348 — the existing "`kebab` has exactly ONE declaration"
      census and its `codeOf` helper, the idiom this task generalizes
    - `scripts/board-model.ts` lines 795-825 — the docblock whose closing sentence claims plan 32-08
      deleted the second pair
    - `scripts/board-model.ts` lines 890-1010 — `parseTicketDocument`, so the census's definition of
      "a ticket-frontmatter reader" is anchored to what that function does
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` lines 14-20 — the two
      artifacts the gap names, including the docblock as its own blocker
    - `CLAUDE.md` — the "No fabrication" constraint and its stated reason
  </read_first>

  <files>scripts/validate.test.ts, scripts/board-model.ts, .planning/phases/32-board-projector-cli-dashboard/32-12-GREEN-proof.txt</files>

  <action>
DERIVE THE SCANNED SET, DO NOT TYPE IT. Add a census case to `scripts/validate.test.ts` that globs
every `.ts` file under `scripts/` — from the directory listing at test time, never from a literal
array — and counts, per file, the declarations that read a ticket's `column:` or `status:` out of
document text. Define "a ticket-frontmatter reader" mechanically rather than by eye: a regular
expression literal in the file whose source anchors on a line start and captures a value after one
of the ticket key spellings, OR a function whose body extracts both of those keys from a text
parameter. Use the `typescript` AST for the second half rather than a text scan, the way
`scripts/board-readonly.test.ts` derives its sets, so a reformatted declaration does not walk past.

Assert the census two-sided:
  - the count of files carrying such a reader is exactly one, and that one file is
    `scripts/board-model.ts`, pinned as `TICKET_FRONTMATTER_READER_COUNT`;
  - the SCANNED file count is printed and asserted greater than zero before the count-of-one is
    claimed, because a census over an empty glob reports one-of-nothing as success;
  - the message on failure names the offending file and line, so the next person reads a finding
    rather than a number.

MAKE THE DOCBLOCK TRUE. `scripts/board-model.ts` at ~816-819 states that plan 32-08 deletes the
second pair. After Task 2 the deletion has happened, so rewrite the sentence in the PRESENT tense
against the tree as it now stands, name the plan that actually performed it, and drop the
forward-looking promise entirely. A docblock that predicts a future commit is a claim nobody can
check at the moment it is read; this repository's rule is that the trace is the proof. While editing,
scan the whole docblock block for any other sentence asserting a state of the tree, and correct or
delete each one that the census cannot confirm.

RECORD THE GREEN PROOF. Write
`.planning/phases/32-board-projector-cli-dashboard/32-12-GREEN-proof.txt` containing: the same
three-row before-table from the RED baseline with an after-column; the census output naming the
scanned file count and the single reader; the two bare and `--strict` validator exit statuses on this
repository; and the unchanged text of the two published board-vs-ticket messages.
  </action>

  <acceptance_criteria>
    - The census case globs `scripts/*.ts` from a directory listing at test time and asserts the scanned count is greater than zero before asserting the reader count.
    - `TICKET_FRONTMATTER_READER_COUNT` is pinned two-sided at 1 with a message naming what a second reader would mean.
    - The census identifies `scripts/board-model.ts` by name as the one carrier, so a census that found the right NUMBER in the wrong FILE still fails.
    - The docblock at `scripts/board-model.ts` ~816-819 makes no claim in the future tense about a plan's deletions, and every remaining claim about the tree is one the census confirms.
    - `.planning/phases/32-board-projector-cli-dashboard/32-12-GREEN-proof.txt` contains the after-column for all three disagreement rows and both validator exit statuses.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts</automated>
    <fails_when>exit code is non-zero, or the census case does not appear in the reported case list</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity</automated>
    <fails_when>exit code is non-zero, or stdout contains `BUILD PARITY FAILED` — the board-model.js twin must be rebuilt after the docblock edit</fails_when>
    <automated>npm run check:banned-claims &amp;&amp; npm run check:claim-anchors</automated>
    <fails_when>either exits non-zero — an edited docblock that introduces a banned or unanchored claim is the failure this project's own gates exist to catch</fails_when>
  </verify>

  <done>
A census derived from a glob over the tree proves one ticket-frontmatter reader exists, names the
file that carries it, and the docblock that claimed otherwise now states what is true.
  </done>

  <reversibility rating="reversible">A test case and a docblock edit.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket documents in `plans/tickets/` → the validator and the projector | Ticket files are written by agents. Their frontmatter is untrusted text parsed by whatever reader gets to it first. |
| the repository's own docblocks → a human reading them | The trace is this project's stated value proposition. A docblock asserting a state of the tree is a claim a reader acts on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-12-01 | Spoofing | the ticket-frontmatter readers | high | mitigate | Two readers disagreeing by construction means a ticket can present one column to `npm run` validation and another to the dashboard — a document that is two documents depending on who asks. Task 2 deletes the second reader; Task 3's census makes a third one red the suite. |
| T-32-12-02 | Tampering | a `column:` line in a ticket's prose or fenced body | medium | mitigate | The unbounded line-anchored pattern lets body text set a ticket's column. `parseTicketDocument` reads only the region between the first two delimiters, and Task 1's body-match fixture is the case that proves the difference. |
| T-32-12-03 | Repudiation | `scripts/board-model.ts` docblock | high | mitigate | A false claim about what a plan deleted is a fabrication under `CLAUDE.md`'s hard rule, and it is what made this gap survive a code review and a green suite. Task 3 rewrites it in the present tense against the tree and re-scans the block for siblings. |
| T-32-12-04 | Denial of Service | the cutover's new refusal path | medium | accept | A ticket with no frontmatter region becomes a validator ERROR where it was previously ignored, which can red an installed repository's gate on first upgrade. This is D-06/D-07's stated intent (refuse outside the form), the refusal names its code and its reason, and the contract records the deviation — so the failure is loud, explained and fixable by the person who owns the ticket. |
| T-32-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan. No `dependencies` key is added; the census uses the already-present dev-only `typescript`. |
</threat_model>

<verification>
1. `npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts scripts/board-model.test.ts`
2. `node scripts/validate-agent-factory.js && node scripts/validate-agent-factory.js --strict`
3. `npm run build && npm run typecheck && npm run check:build-parity`
4. `npm run check:banned-claims && npm run check:claim-anchors` — the docblock edit passes this project's own claim gates.
5. `npx vitest run --exclude '**/scripts/e2e/**'` — the whole suite minus the live claude-CLI e2e lane. Do NOT run plain `npm test`.
6. `git diff -- scripts/validate.test.ts` — confirm the byte-identical message case body is unchanged.
</verification>

<success_criteria>
- One ticket-frontmatter reader in `scripts/*.ts`, proven by a glob-derived census pinned two-sided
  and naming the file.
- `checkTickets()` reads through `parseTicketDocument`; a refused document is reported by code.
- The two published board-vs-ticket messages are byte-identical and their pin is unedited.
- Three disagreement fixtures with a recorded before-and-after table.
- No docblock in `scripts/board-model.ts` states a fact about the tree the census cannot confirm.
- REQUIREMENTS.md and ROADMAP.md phase status are NOT flipped to Complete by this plan.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-12-SUMMARY.md` when done.
</output>

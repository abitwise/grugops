---
phase: 32-board-projector-cli-dashboard
plan: 18
type: execute
wave: 4
gap_closure: true
depends_on: ["32-15", "32-16", "32-17"]
files_modified:
  - scripts/board-dashboard.ts
  - scripts/board-dashboard.js
  - scripts/board-dashboard.test.ts
  - agent-factory/contracts/board.md
autonomous: true
requirements: [DASH-07, DASH-08]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A control code point in the C1 range planted in board content reaches neither channel: not the `--json` document on stdout, not the plain frame on stdout, and not a diagnostic on stderr."
    - "Stdout has a counted, named set of write sites derived from the module's own syntax tree, pinned two-sided the way the stderr set already is — so a new write site is a red, not a review comment."
    - "The module header's claim about the two channels is a statement the census checks, not a sentence a reader trusts."
    - "The header states a code-unit count in code units, so the two numbers beside each other name their own units."
  artifacts:
    - "scripts/board-dashboard.ts (+ committed .js) — `writeDocument`, the single sanitizing stdout document write, and the corrected header line"
    - "scripts/board-dashboard.test.ts — `channelWriteCensus`, the two-sided stdout pin, and the planted-C1 end-to-end cases"
    - "agent-factory/contracts/board.md — the sanitization sentence covering both channels and the bounds unit sentence"
  key_links:
    - "writeDocument -> io.stdout.write: the one place a serialized document reaches the channel, and the one place it is sanitized"
    - "channelWriteCensus -> both channel pins: one derivation answers for stderr and stdout, so the two arms cannot drift apart"
    - "sanitizeCell -> the frame cells, the diagnostics, and now the serialized document: one removal rule, three consumers"
  deferred:
    - "An escaped code point inside a JSON string literal — the six-character form `JSON.stringify` writes for a C0 — is text in the document and is NOT removed. It becomes a control character only if a consumer decodes the document and prints the value without sanitizing it. This boundary is stated in `writeDocument`'s docblock rather than closed, because removing it would mean rewriting values a consumer asked for."
---

<objective>
Close verification gap 3 (= `32-REVIEW.md` CR-02): the `--json` stdout document carries raw C1
terminal-control code points out of board content, while the module header — rewritten by plan 32-13
— claims every string on either channel is sanitized first.

Gap mapping for this plan (from `32-VERIFICATION.md` frontmatter `gaps:`, truth 5, status partial):

| Gap | Finding | Task |
|-----|---------|------|
| "`--json`, `--once`, non-TTY modes work; renderer degrades visibly" | `emit`'s JSON arm writes `JSON.stringify(withWatch)` straight to stdout. `JSON.stringify` escapes the C0 range and not the C1 range, so U+009B and U+009D — the 8-bit CSI and OSC introducers that xterm, iTerm2 and the VTE family act on in UTF-8 mode — travel from a ticket title into the published document verbatim. Independently reproduced: two C1 code points measured in captured stdout. The new derived census pins the stderr write sites at one and asks nothing about stdout, so the rule is derived on the arm that was fixed and narrated on the arm that was not. | Task 1 (the chokepoint), Task 2 (the census), Task 3 (the end-to-end proof) |
| WR-05 (prior WR-01, still open) | The header renders `bounds.longestLine` — documented as UTF-16 code units — through the byte formatter, while the contract states that a byte count and a code-unit count disagree on any board outside Latin-1 and each number states its own unit. | Task 1 |

Purpose: `--json --once` piped into a terminal is the documented ordinary invocation, and the content
crossing into it is written by agents into files this kit tells them to edit. This is the one channel
a human sees with no renderer in between.

Output: one sanitizing stdout chokepoint, a channel-parameterized census that pins BOTH channels
two-sided, a header sentence the census checks, and a header number that names its own unit.

This plan runs after the three reader plans because the census and the end-to-end capture are run
against the closure that ships, and `scripts/board-dashboard.ts` sits on top of both changed modules.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` and no `## Prohibitions`
section to satisfy.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-13-SUMMARY.md
@.planning/phases/32-board-projector-cli-dashboard/32-07-SUMMARY.md
</context>

## Artifacts this phase produces

- `writeDocument(io, value)` — a new module-private function in `scripts/board-dashboard.ts`: the ONE
  place a serialized document reaches stdout. It serializes, sanitizes the serialized text through
  `sanitizeCell`, appends the line boundary, and writes once.
- `channelWriteCensus(source, channel)` in `scripts/board-dashboard.test.ts` — the generalization of
  `stderrWriteCensus`, taking the channel name as a parameter. `StderrCensus` becomes `ChannelCensus`,
  `stderrWriteCensus` is deleted rather than kept beside it.
- `STDOUT_WRITE_SITE_COUNT` and the pinned enclosing-function names for stdout; `STDERR_WRITE_SITE_COUNT`
  keeps its committed value and its meaning.
- New cases: a C1 code point planted in a ticket title asserted absent from captured `--json` stdout,
  the same for the plain frame, the same for a content-derived JSON KEY, and a mutation case.
- `agent-factory/contracts/board.md` — the both-channels sanitization sentence and the bounds unit
  sentence.
- A rebuilt committed `scripts/board-dashboard.js`.
- No new npm script, no `SCHEMA_VERSION` change.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One sanitizing stdout document write, and a header number that names its unit</name>

  <read_first>
    - `scripts/board-dashboard.ts` lines 20-35 — the module header's "either channel" claim, the exact
      sentence this task must make true
    - `scripts/board-dashboard.ts` lines 232-262 — `sanitizeCell` and `warn`, the stderr chokepoint
      whose shape and docblock register this task copies
    - `scripts/board-dashboard.ts` lines 775-815 — `emit`: the diagnostics loop, the JSON write at
      ~804, and the frame write at ~812
    - `scripts/board-dashboard.ts` lines 908-920 — `run`'s usage write, the third stdout site
    - `scripts/board-dashboard.ts` lines 336-345 and 405-415 — `humanBytes` and the header line that
      applies it to `bounds.longestLine`
    - `scripts/board-model.ts` lines 55-60 — the `longestLine` field's documented unit
    - `agent-factory/contracts/board.md` — the Bounds table and the sentence about each number stating
      its own unit
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` CR-02 and WR-05
  </read_first>

  <files>scripts/board-dashboard.ts, scripts/board-dashboard.test.ts, agent-factory/contracts/board.md</files>

  <action>
RED FIRST, WITH THE CODE POINTS CONSTRUCTED, NEVER TYPED. Build the planted string with
`String.fromCharCode` — a literal control byte in a tracked file trips `npm run check:nul-bytes`, and
this repository already carries one open item from exactly that. Add a case that drives the module's
`emit` (or `run` with `--once --json` over a fixture whose ticket title carries U+009B and U+009D) and
asserts the captured stdout holds zero code points in the range U+0080 through U+009F. Record the
failure and the measured count.

THEN THE CHOKEPOINT. Add `writeDocument(io, value)` and move the JSON arm's write into it. It
serializes with `JSON.stringify`, passes the SERIALIZED TEXT through `sanitizeCell`, appends the
newline that is the document boundary, and writes in ONE call. Sanitizing the serialized text rather
than the values is the deliberate choice, and the docblock says why: the document's KEYS can be
content-derived — the dial's per-column limits are keyed by column name — so a rule that only visits
values answers for half the document. Nothing structural is at risk, because the removal set is the
C0 range, the C1 range and DEL, and no JSON structural character is in any of them, and
`JSON.stringify` has already escaped every C0 that belongs inside a string.

STATE THE BOUNDARY IN THE SAME DOCBLOCK. What this does not remove is the escaped six-character form
of a code point inside a string literal, which is text in the document and becomes a control character
only when a consumer decodes and prints it. Say that; do not let the docblock imply more than the
code does — that implication is precisely what CR-02 is.

REWRITE THE HEADER CLAIM AS A CHECKED ONE. The current sentence asserts a property of both channels.
Replace it with a sentence naming the three stdout sites and the one stderr site by function, and
saying the census in `scripts/board-dashboard.test.ts` derives and pins those numbers. A claim that
names its own checker is a claim a reader can falsify in one command.

FIX THE UNIT. The header's second bounds number is a UTF-16 code-unit count; render it with a
thousands separator and the word for characters, not through the byte formatter. Leave the first
number — the board's byte size — on `humanBytes`. Add a case asserting the rendered header contains
both numbers with distinguishable units, over a snapshot whose two numbers differ.
  </action>

  <behavior>
    - Given a snapshot whose ticket title holds U+009B and U+009D, the captured `--json` stdout holds zero code points in U+0080 through U+009F.
    - Given the same snapshot, the parsed document still round-trips through `JSON.parse` and keeps `schemaVersion` at 1.
    - Given a dial whose per-column key carries a C1 code point, the captured stdout still holds zero code points in that range.
    - Given a bounds record with `exceeded` true, the header renders the byte size through the byte formatter and the longest line as a character count.
    - Given the plain frame path, stdout holds no escape byte and no C1 code point when the style is the non-TTY one.
  </behavior>

  <acceptance_criteria>
    - `writeDocument` is the only function in the module that serializes and writes a document; `emit`'s JSON arm calls it and contains no `write` of its own.
    - The module header names the stdout site count, the stderr site count, and the census that derives them.
    - `sanitizeCell` itself is unchanged — what is removed stays decided in one place.
    - The header case asserts both bounds numbers over a snapshot whose byte size and code-unit count differ.
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-dashboard.test.ts` passes with a higher case count than the committed run.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-dashboard.test.ts</automated>
    <fails_when>non-zero exit, or any case reports failed, or the planted-C1 case is reported as skipped</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the output names a tracked file — a control byte was typed into a fixture or a test instead of being constructed</fails_when>
  </verify>

  <done>One sanitizing chokepoint owns the stdout document, and the header states what the census checks.</done>

  <reversibility rating="reversible">A write-site extraction inside one module.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One census, both channels, pinned two-sided</name>

  <read_first>
    - `scripts/board-dashboard.test.ts` lines 1079-1245 — `StderrCensus`, `enclosingFunctionName`,
      `stderrWriteCensus`, the opaque-acquisition collection, and `STDERR_WRITE_SITE_COUNT`
    - `scripts/board-dashboard.test.ts` lines 1246-1325 — the census's own premise case, its
      discrimination probe, and the two-sided pin
    - `scripts/board-dashboard.ts` — the three stdout sites this census must find: the usage write in
      `run`, the frame write in `emit`, and the document write in `writeDocument`
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` CR-02 — the sentence about a
      rule derived on the arm that was fixed and narrated on the arm that was not
    - `.planning/phases/32-board-projector-cli-dashboard/32-13-SUMMARY.md` — what the stderr census
      already proves, so this task generalizes rather than duplicates
  </read_first>

  <files>scripts/board-dashboard.test.ts</files>

  <action>
GENERALIZE, DO NOT DUPLICATE. Rename `StderrCensus` to `ChannelCensus` and turn `stderrWriteCensus`
into `channelWriteCensus(source, channel)` taking the channel name as a parameter. Delete the old
function rather than leaving it beside the new one: a second census over one property is the
second-authority shape this phase has already paid for twice, and the local-binding tracking and the
opaque-acquisition collection must serve both channels from one implementation.

PIN STDOUT TWO-SIDED, BY COUNT AND BY ENCLOSING FUNCTION. Assert the stdout site count equals
`STDOUT_WRITE_SITE_COUNT` and that the set of enclosing function names is exactly the three the module
has — the usage writer, the frame writer, and the document writer — with a failure message shaped as a
decision: a fourth stdout write site is a decision somebody records, never a bumped constant. Keep the
stderr assertions exactly as they are, including their count.

ASSERT THE CENSUS'S OWN PREMISE FOR THE NEW CHANNEL. The existing premise case checks that the parse
read something and that the census can fail. Add the same two for stdout: a constructed module with no
stdout write at all must yield zero sites (so the census is not counting something else), and a
constructed module with a local binding of the channel — the channel destructured into a name, and the
write reached through that name — must still be counted or collected as opaque. A census that cannot
be shown to fail is a green over an empty denominator.

SAY WHAT BOUNDS THE INPUT. Write into the census docblock which node kinds it visits, which channel
spellings it recognizes, and what it does with a channel it cannot follow. The recurring finding
across this phase is a derived predicate whose INPUT was left in a narrow syntactic form; the fix for
that is not another arm, it is stating the boundary and testing it. Add one discrimination plant per
stated boundary.

RE-ASSERT THE FRAME PATH IS NOT REGRESSED. The frame write is a stdout site that must NOT be
sanitized as a whole — it carries the named escape constants on the TTY path by design. Add a case
asserting that the frame site is present in the census and is NOT inside `writeDocument`, and a
behavioural case asserting the TTY frame still carries the clear-screen sequence while the non-TTY
frame carries no escape at all.
  </action>

  <behavior>
    - Given `scripts/board-dashboard.ts`, the census reports exactly the pinned number of stdout sites and the three expected enclosing functions.
    - Given a constructed module with no stdout write, the census reports zero sites.
    - Given a constructed module writing through a destructured channel binding, the census reports a site or an opaque acquisition, never silence.
    - Given the live module, the stderr count and enclosing function are unchanged from their committed values.
    - Given the TTY style, the rendered frame carries the clear-screen sequence; given the non-TTY style, it carries no escape.
  </behavior>

  <acceptance_criteria>
    - `stderrWriteCensus` no longer exists; one `channelWriteCensus` serves both channels.
    - The stdout pin asserts a count AND a set of enclosing function names, with a decision-shaped message.
    - The census docblock names the node kinds it visits, the channel spellings it recognizes, and its answer for an unfollowable channel, and each stated boundary has a discrimination plant.
    - The stderr assertions and `STDERR_WRITE_SITE_COUNT` are byte-identical to their committed values.
    - The frame-site case asserts the frame write is a census site outside the document writer.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-dashboard.test.ts</automated>
    <fails_when>non-zero exit, or any case reports failed, or the stdout pin case is reported as skipped</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-watch.test.ts scripts/board-tracer.test.ts</automated>
    <fails_when>non-zero exit — the dashboard's other suites must be unaffected by a test-file refactor</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or tsc names scripts/board-dashboard.test.ts in an error line</fails_when>
  </verify>

  <done>One census derives both channels, pins stdout two-sided, and states what bounds its own input.</done>

  <reversibility rating="reversible">A test-file refactor with the old function deleted.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The end-to-end proof from a spawned process against the committed .js, and the mutation</name>

  <read_first>
    - `scripts/board-dashboard.test.ts` — the existing spawned-process helpers plan 32-07 added for the
      exit-code and one-document contracts, so this task reuses the harness rather than writing a
      second one
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` — the C1 reproduction
      transcript and the counting method it used
    - `.planning/phases/32-board-projector-cli-dashboard/32-14-ADVERSARIAL-REVIEW.md` §0 — the premise
      rule that a transcript measures the committed `.js` after a build, never a `.ts` through a loader
    - `scripts/fixtures/board-snapshot/` — the fixture tree to copy, never to modify in place
  </read_first>

  <files>scripts/board-dashboard.test.ts</files>

  <action>
BUILD FIRST, THEN MEASURE. The case runs `npm run build` is NOT its job — instead assert the premise
the cheap way: the spawned process runs `scripts/board-dashboard.js`, and the case asserts that file
exists and is newer than nothing at all before spawning. The plan's own verify block runs the build
and the parity gate; the case states which artifact it measured.

THE CAPTURE. Copy `scripts/fixtures/board-snapshot/` to a scratch directory outside the repository
root, rewrite one ticket's title to carry U+009B and U+009D constructed with `String.fromCharCode`,
and spawn `node scripts/board-dashboard.js <scratch> --once --json`, capturing stdout and stderr as
buffers. Assert: stdout parses as one JSON document; the count of code points in U+0080 through
U+009F in the captured stdout is zero; the same count over stderr is zero; and the title field still
carries the rest of its text, so the sanitizer removed the control code points and not the content.
Remove the scratch tree in a `finally`.

THE SECOND INVOCATION IS THE PLAIN FRAME, not a repeat. Spawn the same tree with `--once` and no
`--json`, and assert the same two counts over stdout and stderr. The frame path and the document path
are two arms of one claim, and a proof that runs only the arm that was fixed is the shape this round
exists to stop.

THE MUTATION. Disable the sanitization inside `writeDocument` — one edit, in a working copy of the
committed `.js` under the scratch tree if the harness supports pointing the spawn at it, or in the
source with the build re-run and reverted — and record that the capture case goes red, naming the
measured non-zero count. Restore, rebuild, and record green. Put both transcripts in the plan summary.
A sanitizer that cannot be shown to fail is a sanitizer nobody has proved runs.

RECORD THE NUMBERS, NOT THE ADJECTIVES. The summary states the measured code-point counts before and
after, the exit codes, and the artifact path measured.
  </action>

  <acceptance_criteria>
    - Two spawned captures exist — one `--once --json`, one `--once` — each asserting zero C1 code points on both channels.
    - The document case asserts the captured stdout parses and that the title's remaining text is intact.
    - The mutation transcript is recorded in the summary with the measured non-zero count and the case name that went red.
    - The scratch tree is removed in a `finally`, and `git status --short` names nothing under `scripts/fixtures/` as modified.
    - `npm run build && npm run check:build-parity` is clean with the committed `scripts/board-dashboard.js` rebuilt in this plan.
  </acceptance_criteria>

  <verify>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or the parity step names a tracked .js as moved — the dashboard twin must be rebuilt and committed here</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-dashboard.test.ts</automated>
    <fails_when>non-zero exit, or either spawned-capture case reports failed or skipped</fails_when>
    <automated>npm run check:nul-bytes &amp;&amp; npm run check:public-docs &amp;&amp; npm run check:imperative-lexicon &amp;&amp; npm run check:banned-claims &amp;&amp; npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or any gate's last line is not ALL CHECKS PASSED</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the file count is below 74, or the passed count is below the 4811 recorded by the round-1 adversarial review</fails_when>
  </verify>

  <done>Both stdout arms are measured from a spawned process against the shipped artifact, and the sanitizer is shown to fail when disabled.</done>

  <reversibility rating="reversible">Tests and transcripts only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| board and ticket content → the operator's terminal | Agent-written file content reaches a terminal emulator that acts on control introducers; `--json --once` piped to a terminal is a documented invocation. |
| the projector's document → a downstream consumer | A consumer parses and may re-print the document; what the projector writes is what that consumer inherits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-18-01 | Tampering | `emit`'s stdout document write | high | mitigate | Reproduced: 8-bit CSI and OSC introducers travel from a ticket title into the published document, which a terminal acts on. Task 1 puts one sanitizing chokepoint on the serialized document, covering keys as well as values; Task 3 measures it from a spawned process. |
| T-32-18-02 | Repudiation | the module header's both-channels claim | high | mitigate | A docblock claiming a property the code does not have is how the next reviewer stops checking. Task 1 rewrites the claim to name its checker; Task 2 makes that checker cover both channels. |
| T-32-18-03 | Tampering | a future fourth stdout write site | medium | mitigate | The stderr side is pinned and the stdout side was not, so a new stdout write was free. Task 2 pins stdout by count and enclosing function with a decision-shaped message. |
| T-32-18-04 | Denial of Service | over-sanitizing the frame | medium | mitigate | Sanitizing the whole frame would strip the named style escapes and break the TTY renderer. Task 2's frame-site case and the TTY/non-TTY behavioural pair are the mechanical defence. |
| T-32-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan; `package.json` is untouched. |
</threat_model>

<verification>
1. `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-dashboard.test.ts scripts/board-watch.test.ts scripts/board-tracer.test.ts`.
2. `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness`.
3. `npm run check:nul-bytes && npm run check:public-docs && npm run check:imperative-lexicon && npm run check:banned-claims && npm run check:claim-anchors`.
4. Re-run the verifier's own C1 reproduction against the rebuilt `.js` and record the measured count in the summary.
5. `npx vitest run --exclude '**/scripts/e2e/**'` — the whole suite minus the live claude-CLI e2e lane. Never plain `npm test`.
</verification>

<success_criteria>
- A C1 code point planted in board content reaches neither channel, in both `--json` and plain modes, measured from a spawned process.
- Stdout's write sites are derived, counted and named; stderr's pin is unchanged.
- The header's claim names the census that checks it.
- The header's two bounds numbers name their own units.
- REQUIREMENTS.md and the ROADMAP phase status are NOT flipped to Complete by this plan.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-18-SUMMARY.md` when done.
</output>

---
phase: 32-board-projector-cli-dashboard
plan: 29
type: execute
wave: 6
gap_closure: true
depends_on: ["32-28"]
files_modified:
  - scripts/validate.test.ts
  - scripts/check-foundation-guards.test.ts
  - .planning/phases/32-board-projector-cli-dashboard/32-29-RED-baseline.txt
  - .planning/phases/32-board-projector-cli-dashboard/32-29-GREEN-proof.txt
autonomous: true
requirements: [DASH-01, DASH-02, DASH-06]
estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A second ticket-frontmatter reader planted under `scripts/` is counted as a carrier whichever ordinary way it spells its key names and whichever ordinary way it scans text — an alternation group, a name assembled by joining characters, a concatenation, or a name inside a template — so the count of one authority is a measurement rather than a coincidence of spelling (DASH-01, D-06)."
    - "The primitive half of the census is a refused complement with named exemptions rather than an enumeration: a text-scanning call this pass has not met before makes a planted second reader visible, instead of passing because nobody listed it."
    - "Over-detection is paid for by named exemptions with a written reason each, pinned two-sided by count, so the live tree still reports exactly one ticket-frontmatter authority and every non-carrier is a decision somebody recorded."
    - "A `check:*` script running two gate modules proves BOTH: every target named in the script gets its own reachability row, and the pinned numbers are target counts rather than script counts (DASH-06, D-21)."
    - "A script contributing to two classes at once — one gate module and one test-suite file in the same command — is proved in both classes rather than losing the second to whichever arm matched first."
  prohibitions:
    - "Never close the census by adding one more spelling to an enumeration — the enumerated list is the defect, not its length."
    - "Never let the census report a single authority while a planted second reader with an ordinary spelling sits under `scripts/`."
    - "Never suppress an over-detection by narrowing the predicate back toward a syntactic position; pay for it with a named, counted exemption instead."
    - "Never pin a class by the number of scripts when the proof is about targets."
    - "Canon security items (secret handling, authn/authz, transport) are dropped here with this breadcrumb: both censuses read repository source text and decide nothing at runtime."
  artifacts:
    - "scripts/validate.test.ts — the key half as word presence in resolved static text, the primitive half as a refused complement, and the exemption set with its count"
    - "scripts/check-foundation-guards.test.ts — per-target classification and per-target reachability rows, with target counts pinned"
    - ".planning/phases/32-board-projector-cli-dashboard/32-29-RED-baseline.txt — the four planted second readers measuring zero carriers before the fix, and the two-gate script classified as one"
    - ".planning/phases/32-board-projector-cli-dashboard/32-29-GREEN-proof.txt — the same plants detected after the fix, the live tree still reporting one authority, and every exemption's reason"
  key_links:
    - "namesKey (word presence in resolved static text) -> findTicketReaders -> TICKET_FRONTMATTER_READER_COUNT: the count is over a predicate whose subject is presence rather than position"
    - "the primitive complement + NOT_A_TEXT_PRIMITIVE exemptions -> the carrier set: over-detection is bounded by named decisions, not by a shorter list"
    - "classifyCheckScriptTargets -> the per-class reachability proofs -> CHECK_SCRIPT_CLASSES target counts: a second target in one command is a second row, never a silent passenger"
    - "check:dashboard-readonly -> its own reachability row: the DASH-06 control's reachability stays mechanical after the classifier is widened"
  deferred: []
---

<objective>
Close the two detection-robustness findings the verifier carried as advisory and the reviewer raised
as warnings: a census that reports one authority while three ordinary second readers sit undetected,
and a class derivation that proves the first of two gate modules in one command.

Item mapping:

| Item | Finding | Task |
|------|---------|------|
| `advisory:` in `32-VERIFICATION.md` / WR-03 (round-1 WR-01 re-opened) / ledger row 184 | The census's primitive half was widened to a named five-member table; its KEY half is still a syntactic-position rule requiring a line start, a pattern anchor or a newline before the key. A key inside an alternation group is preceded by neither, so an ordinary second reader using an enumerated primitive measures at zero carriers while the control measures at three. A key assembled by joining characters is invisible for the same reason one register over, and three text-scanning primitives are not in the table at all. | Tasks 1, 2 |
| WR-05 / F-07 | `classifyCheckScript` takes the FIRST match, so a command running two gate modules classifies as one, and the pinned numbers count scripts rather than targets. The gate arm also wins over the suite arm, so a mixed command loses its suite half. No live instance today, derived from the manifest at measurement time. | Tasks 1, 3 |

Purpose: D-06 makes one authority per predicate the rule the validator was rewritten to satisfy, and
D-21 makes the read-only guard's reachability mechanical rather than assumed. Both censuses are the
MEASUREMENT of those rules. A measurement whose subject is a syntactic position rather than the
property being claimed is this repository's recorded first systemic failure class, and it has now been
raised against the same predicate in two consecutive rounds.

Output: a key predicate over presence rather than position; a primitive predicate that is a refused
complement with named exemptions; and a class derivation that proves every target rather than the
first.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` and no `## Prohibitions`
section to satisfy.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-21-SUMMARY.md
@.planning/phases/32-board-projector-cli-dashboard/32-12-SUMMARY.md
</context>

<assumption_delta_decision>
**Signalled family:** `pluralization` — the scan raised the nouns "second" and "alongside" from this
phase's own roadmap prose about a second ticket-frontmatter reader, and `chosen` from round 2's
channel-parameterized stdout census.

**Noun now primary:** the ticket-frontmatter *reader*, still singular.

**Decision: `no-change`.** The plural signal is a description of what the phase DELETED, not of a new
multiplicity being introduced. D-06 and the project's one-authority-per-predicate rule keep the reader
singular; this plan strengthens the measurement that proves the singularity and introduces no second
reader, no configurable reader and no fallback reader. The exemption set this plan adds is a set of
named NON-carriers, which is a statement about what is not a reader rather than a second one.

**Accepted debt:** none. **Suggested invariant test:** already present and strengthened here — the
two-sided carrier count over a glob-derived file set with a non-vacuity premise.
</assumption_delta_decision>

## Artifacts this phase produces

- `namesKey` in `scripts/validate.test.ts` — rewritten to test for the key as a WORD anywhere in a
  resolved static text (literal, template or regular-expression source, after the existing skeleton
  normalization), replacing the line-position rule.
- `looksLikeTextScan` in `scripts/validate.test.ts` — the primitive half as a refused complement: any
  member call on a value this pass believes is text counts, with `NOT_A_TEXT_PRIMITIVE` as the named
  exemption set and `NOT_A_TEXT_PRIMITIVE_COUNT` pinned two-sided. `TEXT_SCAN_PRIMITIVES` is retained
  as the written record of the spellings this repository actually uses, and a case asserts every one
  of its members is admitted by the complement so the two cannot disagree.
- `SECOND_READER_PLANTS` and `SECOND_READER_PLANT_COUNT` — the four measured second-reader spellings
  held as data, in the discrimination-row shape this file already uses: alternation plus a match-all
  scan, alternation plus an exec loop, two literals plus a replace, and a key assembled by joining
  characters plus a split-and-index scan.
- `classifyCheckScriptTargets` in `scripts/check-foundation-guards.test.ts` — every gate-module target
  and every suite target found in one command, each as its own classification row.
- `CHECK_SCRIPT_CLASSES` restated to pin TARGET counts, with a case asserting the target total equals
  the sum of the per-class target counts.
- `.planning/phases/32-board-projector-cli-dashboard/32-29-RED-baseline.txt` and `32-29-GREEN-proof.txt`.
- No production-code change and no committed `.js` movement: this plan edits two `.test.ts` files.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED baseline — four undetected second readers and one unproven gate target</name>

  <read_first>
    - `scripts/validate.test.ts` lines 1525-1575 — `TEXT_SCAN_PRIMITIVES`, `MEMBER_PRIMITIVES`,
      `CONSTRUCTOR_PRIMITIVES`, `skeleton`, `keyInText`, `namesKey` and the `ReaderFinding` shape
    - `scripts/validate.test.ts` lines 1575-1700 — `staticText`, `findTicketReaders`, `SCANNED`,
      `DETECTED`, `NOT_A_SECOND_AUTHORITY`, `NOT_A_SECOND_AUTHORITY_COUNT`, `CARRIERS`,
      `TICKET_FRONTMATTER_READER_COUNT` and the non-vacuity premise case
    - `scripts/check-foundation-guards.test.ts` lines 12060-12110 — `CheckScriptClassification`,
      `classifyCheckScript`, `CHECK_SCRIPT_CLASSES`, `TOOLCHAIN_CHECK_SCRIPTS` and `readPackageScripts`
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` WR-03 and WR-05 — the four-plant
      measurement table and the two-gate command
    - `.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md` § 5, finding
      F-07, and § 7 ledger row 184
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-29-RED-baseline.txt</files>

  <action>
PLANT FOUR SECOND READERS AND MEASURE THE CENSUS'S ANSWER. Each plant is a real, semantically complete
module that reads the two ticket frontmatter keys out of a document's text, written as a plain source
file under `scripts/` in a disposable copy of the tree. The four spellings:

  A — an alternation regular expression over both keys plus a match-all scan
  B — an alternation regular expression over both keys plus an exec loop, which uses a primitive the
      current table already enumerates, so this plant defeats the half that WAS widened
  C — two plain string literals plus a replace-based scan
  D — a key name assembled by joining single characters, plus a split-and-index scan, which is the
      exact spelling ledger row 184 records as a stated blind spot

For each, record the carrier count the census reports and the census's own verdict. Then record the
CONTROL: the reader this phase deleted, replanted verbatim, which must measure as a carrier. A
baseline with no control cannot distinguish a census that misses everything from one that misses these
four.

RECORD THE TWO ENUMERATION GAPS SEPARATELY, because they are different findings that happen to share a
plan: which of the plants fail on the KEY half, and which fail on the PRIMITIVE half. A plant that
fails both tells you less than two plants that each fail one.

CLASSIFY THE TWO-GATE COMMAND. Run the current classification logic over a command that builds and
then runs two gate modules, and record what it returns: the class, the single target, and which target
is never named in any reachability row. Then run it over a mixed command that runs one gate module and
one test-suite file, and record which class wins and which proof is lost. Finally, derive from the
real manifest how many `check:*` scripts exist, how many targets they carry in total, and what the
current per-class numbers claim — so the GREEN proof can show target counts replacing script counts
against a measured starting point.

Every plant runs on a copy; record `git diff --exit-code -- scripts/` clean.
  </action>

  <behavior>
    - Given plant A, the census reports zero carriers for it.
    - Given plant B, the census reports zero carriers for it despite its primitive being enumerated.
    - Given plant C, the census reports zero carriers for it.
    - Given plant D, the census reports zero carriers for it.
    - Given the control plant, the census reports it as a carrier.
    - Given a command running two gate modules, the classification names one target.
    - Given a mixed gate-and-suite command, the classification names one class.
  </behavior>

  <acceptance_criteria>
    - `32-29-RED-baseline.txt` carries five rows (A through D plus the control) with a measured carrier count each.
    - The baseline states, per plant, whether the key half or the primitive half is what let it through.
    - The baseline records the two-gate command's classification, its single target, and the target never named.
    - The baseline records the mixed command's winning class and the proof lost.
    - The baseline records, from the real manifest, the script count, the total target count and the current per-class numbers.
  </acceptance_criteria>

  <verify>
    <automated>git diff --exit-code -- scripts/ agent-factory/ docs/</automated>
    <fails_when>non-zero exit — a plant was left in the tree instead of on a copy</fails_when>
    <automated>grep -c 'carriers' .planning/phases/32-board-projector-cli-dashboard/32-29-RED-baseline.txt</automated>
    <fails_when>a printed count below 5 — fewer than the four plants plus the control recorded a carrier measurement, so the baseline cannot discriminate a blind census from a blind spot</fails_when>
  </verify>

  <done>Four ordinary second readers and one control are measured against the census, and the two-gate command's lost target is on record.</done>

  <reversibility rating="reversible">A measurement written to a planning artifact.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The census asks about presence and capability, and pays for it with named exemptions</name>

  <read_first>
    - `scripts/validate.test.ts` lines 1525-1700 — the whole census block, since this task rewrites two
      predicates inside it and adds an exemption set beside the one that exists
    - `scripts/validate.test.ts` — `NOT_A_SECOND_AUTHORITY` and its count, the named-exclusion posture
      this task copies for the primitive half
    - `scripts/canonical-frontmatter.ts` and `scripts/board-model.ts` `parseTicketDocument` — the one
      legitimate ticket-frontmatter authority, which must remain the single carrier
    - Every `scripts/*.ts` the widened predicate newly flags, discovered by running the census after
      the first half of the change — each one is either a real carrier or an exemption with a reason
    - `.planning/phases/32-board-projector-cli-dashboard/32-29-RED-baseline.txt` — Task 1's five rows
  </read_first>

  <files>scripts/validate.test.ts</files>

  <action>
ASK ABOUT THE KEY'S PRESENCE, NOT ITS POSITION. Rewrite `namesKey` to test whether the key appears as
a whole WORD anywhere in a resolved static text — after the existing skeleton normalization, which
stays because it is what makes two spellings of one literal character read alike. An alternation
group, a template interpolation boundary and a concatenated fragment all name the key under this rule,
because the rule is about the text containing the key rather than about what precedes it.

MAKE THE PRIMITIVE HALF A REFUSED COMPLEMENT. Replace the enumeration test with `looksLikeTextScan`:
any member call made on a value this pass believes is text counts as a text-scanning primitive, and
`NOT_A_TEXT_PRIMITIVE` names the exceptions with a written reason each. Keep `TEXT_SCAN_PRIMITIVES` as
the record of the spellings this repository actually uses, and add a case asserting every one of its
members is admitted by the complement — two representations that cannot disagree, rather than one
enumeration doing the deciding. Pin `NOT_A_TEXT_PRIMITIVE_COUNT` two-sided, so an exemption added to
silence a red is a number somebody looks at.

PAY FOR OVER-DETECTION IN NAMED DECISIONS. Widening both halves will flag modules that are not second
authorities. Run the census, list every newly flagged file, and for each one either accept it as a real
carrier — which would be a finding in its own right and must be reported, not silenced — or add it to
the exemption set with the reason it is not an authority on what a ticket says. Do not narrow either
predicate to make a red go away; that is the move that produced both halves of this finding.

HOLD THE PLANTS AS DATA AND ASSERT DISCRIMINATION. Declare `SECOND_READER_PLANTS` with Task 1's four
spellings plus the control, each a row carrying a name and the source to plant, with
`SECOND_READER_PLANT_COUNT` pinned. Assert that planting each row into the scanned set makes the
carrier count rise, and that with none planted the count is one. Assert the non-vacuity premise still
holds: the glob found files, and every tracked runnable source under `scripts/` is among them.

MUTATE TO PROVE THE PREDICATES DECIDE. Restore the position rule in `namesKey` and record which plants
go undetected; restore the enumeration in the primitive half and record which plants go undetected;
empty the exemption set and record that the count assertion reds. Three mutations, each with the named
cases that moved.
  </action>

  <behavior>
    - Given plant A, B, C or D in the scanned set, the carrier count rises above one.
    - Given no plant, the carrier count is exactly one.
    - Given the control plant, the carrier count rises above one.
    - Given the exemption set, every member carries a written reason and the set equals its pinned count.
    - Given each member of the recorded primitive table, the complement admits it.
    - Given the glob, the scanned set is non-empty and contains every tracked runnable source under `scripts/`.
  </behavior>

  <acceptance_criteria>
    - `namesKey` decides by the key's presence as a word in a resolved static text, with the skeleton normalization retained.
    - `looksLikeTextScan` decides by complement and `NOT_A_TEXT_PRIMITIVE` carries a reason per member and equals its pinned count.
    - A case asserts every member of the recorded primitive table is admitted by the complement.
    - `SECOND_READER_PLANTS` has five rows and equals its pinned count, and a case asserts the carrier count rises for each.
    - The unplanted live tree reports exactly one ticket-frontmatter carrier.
    - Three mutation runs are recorded in `32-29-GREEN-proof.txt`, each naming which plants went undetected.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or a passed-case count at or below the count recorded in 32-29-RED-baseline.txt — cases were replaced rather than added</fails_when>
    <automated>node scripts/validate-agent-factory.js</automated>
    <fails_when>non-zero exit — the structure validator the census accompanies no longer runs clean over the kit</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or a passed-case count below the 4974 recorded in 32-VERIFICATION.md — a widened census reddening an unrelated suite shows up here</fails_when>
  </verify>

  <done>Four ordinary second-reader spellings are detected, the primitive half is a complement with named exemptions, and the live tree still reports exactly one authority.</done>

  <reversibility rating="reversible">Predicate changes inside one test file; no production module moves.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Every target gets its own reachability row</name>

  <read_first>
    - `scripts/check-foundation-guards.test.ts` lines 12060-12130 — the classification, the class table,
      the toolchain exemptions and the manifest reader
    - `scripts/check-foundation-guards.test.ts` — the per-class reachability proofs that consume the
      classification, since each must now run per target rather than per script
    - `package.json` — the real `check:*` command set, read so the new target counts are measured
      rather than recalled
    - `.github/workflows/ci.yml` — how a gate module is named in CI, which the gate-class proof asserts
    - `.planning/phases/32-board-projector-cli-dashboard/32-29-RED-baseline.txt` — Task 1's manifest
      measurement
  </read_first>

  <files>scripts/check-foundation-guards.test.ts</files>

  <action>
CLASSIFY EVERY TARGET, NOT THE FIRST. Replace the single-match classification with
`classifyCheckScriptTargets`, returning EVERY gate-module target and EVERY suite target a command
carries, each as its own classification row naming the script it came from. A script may contribute to
more than one class and to more than one target in a class; say so in the docblock, because the
previous rule's defect was an assumption that one command means one proof.

RETURN NULL FOR A COMMAND WITH NO TARGET AT ALL, keeping the existing posture verbatim: an
unclassifiable script is a script whose reachability nobody has thought about, and the honest response
is a red naming it rather than a skip.

PIN TARGET COUNTS, NOT SCRIPT COUNTS. Restate `CHECK_SCRIPT_CLASSES` so each class's number is the
number of TARGETS proved in it, derived from the real manifest, and add a case asserting the sum of
the per-class target counts equals the total number of targets found. That equality is what makes a
target added to an existing command visible instead of silently inheriting its neighbour's proof.

PROVE EACH TARGET SEPARATELY. Run each class's existing reachability proof once per target: a gate
module is named in the workflow or carries a written exemption reason with something that still runs
it; a suite file exists and is not excluded by the workflow's suite invocation. The read-only guard's
own script keeps its row, so the DASH-06 control's reachability stays mechanical after the widening.

ASSERT DISCRIMINATION WITH SYNTHETIC COMMANDS. Drive the classifier over a two-gate command, a mixed
gate-and-suite command, a command with two suite files, a command with a gate module inside a
subshell, and a command with no target, and assert the expected row set for each. Then mutate the
classifier back to first-match and record which of those assertions red.
  </action>

  <behavior>
    - Given a command running two gate modules, two gate-module rows are returned.
    - Given a mixed gate-and-suite command, one row of each class is returned.
    - Given a command with two suite files, two suite rows are returned.
    - Given a command with no recognizable target and no toolchain reason, null is returned and the script is named.
    - Given the real manifest, the sum of per-class target counts equals the total target count.
    - Given the first-match mutation, the two-gate and mixed assertions fail.
  </behavior>

  <acceptance_criteria>
    - `classifyCheckScriptTargets` returns every gate-module and suite target in a command, each with the script it came from.
    - `CHECK_SCRIPT_CLASSES` numbers are target counts and a case asserts they sum to the total target count derived from the manifest.
    - Each class's reachability proof runs once per target, and the read-only guard's script still has its own row.
    - Five synthetic-command assertions exist with the expected row sets named in the action.
    - A mutation run recorded in `32-29-GREEN-proof.txt` names which assertions red under first-match.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or a passed-case count at or below the count recorded in 32-29-RED-baseline.txt</fails_when>
    <automated>node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit — the foundation guard the census accompanies no longer runs clean</fails_when>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit — the read-only guard lost its own reachability row or its script fell out of the classified set</fails_when>
  </verify>

  <done>Every target in every check command carries its own reachability row, and the pinned numbers count targets.</done>

  <reversibility rating="reversible">A derivation change inside one test file.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any `scripts/*.ts` an agent adds → the one-authority census | a second reader of ticket frontmatter is a second answer to what a ticket says, and it arrives as an ordinary source file nobody flagged |
| `package.json`'s command set → the CI reachability proof | a gate that is never run is a gate that cannot refuse, and a target added to an existing command is the cheapest way for one to go unproven |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-29-01 | Tampering | `scripts/validate.test.ts` key predicate | medium | mitigate | The key half decides by presence in resolved static text; four measured second-reader spellings become carriers and are held as pinned discrimination rows |
| T-32-29-02 | Tampering | `scripts/validate.test.ts` primitive predicate | medium | mitigate | The primitive half becomes a refused complement with named, counted exemptions; the recorded spelling table is asserted to be a subset of what the complement admits |
| T-32-29-03 | Repudiation | `CHECK_SCRIPT_CLASSES` reachability proof | medium | mitigate | Per-target classification and per-target proofs, with the class numbers counting targets and a sum equality over the manifest-derived total |
| T-32-29-04 | Denial of Service | over-detection reddening unrelated suites | low | mitigate | Every newly flagged file is either reported as a real carrier or exempted with a written reason, and the full suite run in each task's verify is what catches a widening that went too far |
| T-32-29-05 | Information Disclosure | census output naming source paths | low | accept | The census prints repository-relative paths of tracked files the operator already has |

No package-manager install task exists in this plan — the repository ships zero runtime dependencies —
so no `T-32-SC` supply-chain row and no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts scripts/check-foundation-guards.test.ts`
  — exit 0, case counts above the baseline's.
- `node scripts/validate-agent-factory.js` and `node scripts/check-foundation-guards.js` — exit 0.
- `npm run check:dashboard-readonly` — exit 0, with its own reachability row still present.
- `npx vitest run --exclude '**/scripts/e2e/**'` — exit 0, passed-case count at or above 4974.
- `32-29-GREEN-proof.txt` records the four plants detected, the live count of one, the exemption
  reasons and four mutation runs.
</verification>

<success_criteria>
The one-authority claim is measured by predicates whose subjects are the properties being claimed —
the key's presence and the capability of scanning text — rather than by the positions two authors
happened to use; over-detection is bounded by named, counted decisions; and every gate target in every
check command carries its own reachability row.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-29-SUMMARY.md` when done.
</output>

---
phase: 32-board-projector-cli-dashboard
plan: 28
type: execute
wave: 5
gap_closure: true
depends_on: ["32-27"]
files_modified:
  - scripts/board-dashboard.ts
  - scripts/board-dashboard.js
  - scripts/board-dashboard.test.ts
  - scripts/board-watch-live.test.ts
  - agent-factory/contracts/board.md
  - .planning/phases/32-board-projector-cli-dashboard/deferred-items.md
  - .planning/phases/32-board-projector-cli-dashboard/32-28-RED-baseline.txt
  - .planning/phases/32-board-projector-cli-dashboard/32-28-GREEN-proof.txt
autonomous: true
requirements: [DASH-07, DASH-08, DASH-04]
estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A raw control character planted in board content is absent from what a `--json` consumer recovers: one parse of the captured document yields zero code points in the C0 range, the C1 range or the delete position, in every string VALUE and every string KEY, at every depth (DASH-07, D-18)."
    - "The document is still exactly one line and still exactly one parseable document: the line boundary remains this module's structure and never content's, so board content cannot forge a second record (D-18)."
    - "Every part of the plain-frame header passes through one sanitizing function, and that claim is derived from the module's own syntax rather than maintained by convention — the terminal style codes are the one thing deliberately outside it, and they are named (D-17)."
    - "The live event-path case fails with a measured latency rather than with a timeout: its wait deadline is below the poll period but no longer so tight that an ordinary loaded machine reds it, and the assertion that decides attribution is still the comparison against the poll period and the debounce (DASH-04, D-14)."
    - { statement: "No --json consumer decodes the document a second time after parsing it, so a control character re-manufactured by a consumer's own serializer is outside what this sanitizer can decide", verification: backstop }
  prohibitions:
    - "Never sanitize by rewriting the escaped textual form after serialization — that alters a value the consumer asked for; the values are sanitized before serialization instead."
    - "Never let the sanitization of the plain frame's header be a per-field convention that nothing derives."
    - "Never answer the Windows timing question with a platform conditional — the measurement must still arrive, per this project's own recorded rule."
    - "Never weaken the attribution assertions in the live event-path case; only the wait deadline moves."
    - "Canon security items (secret handling, authn/authz, transport) are dropped here with this breadcrumb: the only untrusted input is board text, and it crosses exactly two channels, both owned by this module."
  artifacts:
    - "scripts/board-dashboard.ts — the value-and-key scrub applied before serialization, and the header's one sanitizing chokepoint"
    - "scripts/board-dashboard.js — the committed twin, rebuilt in the same commit"
    - "scripts/board-dashboard.test.ts — the parse-then-inspect case over a spawned process, and the AST-derived header census"
    - "scripts/board-watch-live.test.ts — the widened wait deadline with the attribution assertions unchanged"
    - "agent-factory/contracts/board.md — the control-character paragraph restated to describe the raw input it actually receives"
    - ".planning/phases/32-board-projector-cli-dashboard/deferred-items.md — the explicit disposition of the CI-topology half of WR-07"
  key_links:
    - "scrub (values and keys, recursively) -> writeDocument -> stdout: the serializer receives text that is already clean, so nothing it escapes can be recovered as a control character"
    - "sanitizeCell -> the header's one part function -> renderHeader's parts: one chokepoint per channel, each derived from the module's own syntax rather than asserted in prose"
    - "EVENT_DEADLINE_MS -> the poll period: the deadline stays strictly below the period, which is the whole of the attribution argument, and the two comparisons that decide attribution are untouched"
    - "agent-factory/contracts/board.md -> scripts/board-dashboard.ts: the contract's control-character sentence and the module's docblock describe the same input"
  deferred:
    - "WR-07's CI-topology half — moving the three platform-dependent live cases into their own `check:watch-live` script invoked from a named CI step that is non-blocking on the Windows leg — is NOT taken here. Reason: it changes `.github/workflows/ci.yml` job topology, which is Phase 33's declared scope (Windows portability and the `windows-latest` leg), and `.planning/WINDOWS.md` ledger row 186 plus `32-22-SUMMARY.md` already record that a Windows red IS the CAP-02 measurement arriving early and must not be answered with a platform conditional. Only the timing half — the wait deadline — is taken here, because it is a measurement-quality fix that is true on every platform. Recorded in `deferred-items.md` by Task 3."
---

<objective>
Close the two output-channel honesty findings and the one measurement-quality finding that the
verifier and the reviewer both recorded against the otherwise-verified truth 5.

Item mapping (from `32-VERIFICATION.md` `reason_advisory` and `32-REVIEW.md` warnings):

| Item | Finding | Task |
|------|---------|------|
| `reason_advisory` on truth 5 / WR-04 / F-05 | `writeDocument` is sanitize-after-serialize. The serializer escapes the C0 range into printable text and leaves the C1 range raw; the sanitizer removes raw bytes. Applied in that order the two are exactly complementary, and a raw control character planted in a ticket title survives as a real control code point recovered by ONE parse of the document. Independently reproduced by the verifier. | Tasks 1, 2 |
| WR-06 | The plain-frame header is the one arm the stdout census deliberately exempts from the document chokepoint, and what replaced the chokepoint there is a hand-applied sanitizer per field. Two pushes already do not carry it. No live bypass today — both values range over closed sets — so this is a derivation-scope finding in the register the round has been paying for. | Task 2 |
| WR-07 | The live event-path case bounds a spawned-process delivery into a 350 ms band that must contain an event, a 250 ms debounce, a six-source re-read and a write, and it refuses to skip on any platform by design. The band is tight even on Linux, and the cost of a red lands on a shared suite step. | Task 3 |

Purpose: D-18 makes `--json` the channel a CI consumer parses, and CLAUDE.md's no-fabrication rule
applies to what a consumer RECOVERS, not only to what is on the wire. D-17 makes the header the one
line carrying the badge a human uses to decide whether to trust the frame, and a claim about it that
nothing derives is the asymmetry this round has found three times already. D-14 makes the event path
the low-latency path, and a case that reds by timeout rather than by measurement cannot say which.

Output: values and keys scrubbed before serialization with the line boundary unchanged; one derived
header chokepoint; a wait deadline that lets the attribution assertions do the deciding; and the
CI-topology half of WR-07 explicitly dispositioned rather than silently dropped.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` and no `## Prohibitions`
section to satisfy.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-18-SUMMARY.md
@.planning/phases/32-board-projector-cli-dashboard/32-22-SUMMARY.md
</context>

## Artifacts this phase produces

- `scrub(value: unknown): unknown` in `scripts/board-dashboard.ts` — a recursive pass applying
  `sanitizeCell` to every string VALUE and every object KEY at every depth, applied before
  serialization; arrays and nested objects included, non-string leaves untouched.
- `writeDocument` restated: scrub, then serialize, then the existing post-serialization pass, which
  stays because it is what keeps the line boundary this module's structure rather than content's.
- `part(s: string): string` in `renderHeader` — the one place a header part is produced, sanitized for
  the reason `cell` is, and not truncated for the reason the header states.
- `HEADER_PART_SITE_COUNT` in `scripts/board-dashboard.test.ts` — the AST-derived, two-sided pin over
  every element and every push argument in `renderHeader`'s parts list.
- A parse-then-inspect case in `scripts/board-dashboard.test.ts` — spawn the real CLI over a fixture
  carrying raw control characters in a row title, a ticket title and a column name; parse the captured
  document; walk it and assert zero control code points in any string value or key at any depth.
- The widened wait deadline in `scripts/board-watch-live.test.ts`, derived from the poll period rather
  than typed as a literal, with both attribution comparisons unchanged.
- `.planning/phases/32-board-projector-cli-dashboard/32-28-RED-baseline.txt` and `32-28-GREEN-proof.txt`.
- The committed `scripts/board-dashboard.js` twin, rebuilt in the same commit.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED baseline — what a consumer recovers, and what the header derives</name>

  <read_first>
    - `scripts/board-dashboard.ts` lines 240-320 — `sanitizeCell`, the `writeDocument` docblock
      including its "WHAT THIS DOES NOT REMOVE" paragraph, and the line-boundary rule
    - `scripts/board-dashboard.ts` lines 375-420 — `cell` and `humanChars`, the truncating path every
      column and conflict value already routes through
    - `scripts/board-dashboard.ts` lines 440-495 — `renderHeader`, its parts list, the two pushes that
      interpolate a source name and a stale reason, and the badge and reset style codes
    - `scripts/board-dashboard.test.ts` lines 1804-1930 — the stdout write-site census, its two-sided
      count and the enclosing-function names it pins, and the header exemption it states
    - `scripts/board-watch-live.test.ts` lines 79-110 and 415-460 — the deadline constants and the two
      attribution assertions
    - `agent-factory/contracts/board.md` § "Control characters on the way out"
    - `.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md` § 5, finding
      F-05 — the four-row measurement table
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-28-RED-baseline.txt</files>

  <action>
MEASURE WHAT A CONSUMER RECOVERS, NOT WHAT IS ON THE WIRE. On a copy of
`scripts/fixtures/board-snapshot/` in the session scratchpad, plant raw control characters — an escape
byte, a bell byte, and one code point from the upper control range — into three distinct places: a
board row title, a ticket document's title field, and a column heading name. Spawn the real CLI over
that copy in single-frame document mode, capture stdout, and record into
`.planning/phases/32-board-projector-cli-dashboard/32-28-RED-baseline.txt`:

  - the count of control code points in the RAW captured bytes
  - the count of control code points recovered after ONE parse, walked over every string value AND
    every object key at every depth, with the code points listed
  - the same two counts for the plain-frame path, which is the control that should already be clean
  - the number of lines the captured document occupies

WALK THE HEADER'S OWN SYNTAX AND COUNT. Using the module's syntax tree, enumerate every element of
`renderHeader`'s parts list and every argument pushed onto it, and record how many of them pass
through the sanitizer and how many do not, listing the ones that do not. That number is the finding:
a claim maintained by convention has a measurable number of exceptions and nobody had counted them.

RECORD THE TIMING BAND AS IT STANDS. Run the live event-path case and record the measured latency, the
current wait deadline, the poll period and the debounce, so the widening in Task 3 is a change to a
recorded number rather than to a remembered one. Run it three times and record all three latencies,
because a single sample cannot show how much headroom the band has.

Every probe runs on a copy or read-only over the tree; record `git diff --exit-code -- scripts/` clean.
  </action>

  <behavior>
    - Given raw control characters in board content, the captured document carries zero control code points as raw bytes.
    - Given one parse of that same document, control code points are recovered from string values.
    - Given the plain-frame path with the same content, zero control code points appear before or after parsing.
    - Given the header's syntax tree, at least one parts entry does not pass through the sanitizer.
    - Given three live event-path runs, three latencies are recorded against the current deadline.
  </behavior>

  <acceptance_criteria>
    - `32-28-RED-baseline.txt` records a raw count of zero and a post-parse count above zero, with the recovered code points listed.
    - The baseline records the post-parse walk as covering object KEYS as well as values, at every depth.
    - The baseline records the plain-frame control counts as zero both before and after parsing.
    - The baseline records the captured document occupying exactly one line.
    - The baseline records the header parts count and lists every entry not passing through the sanitizer.
    - The baseline records three live event-path latencies with the deadline, the poll period and the debounce.
  </acceptance_criteria>

  <verify>
    <automated>git diff --exit-code -- scripts/ agent-factory/ docs/</automated>
    <fails_when>non-zero exit — a probe ran in the tree instead of on a copy</fails_when>
    <automated>grep -c 'after ONE parse' .planning/phases/32-board-projector-cli-dashboard/32-28-RED-baseline.txt</automated>
    <fails_when>a printed count of 0 — the baseline does not record the post-parse measurement, which is the only one that distinguishes this finding from the already-closed raw-byte one</fails_when>
  </verify>

  <done>What a consumer recovers, what the header derives and how much headroom the timing band has are all numbers on disk.</done>

  <reversibility rating="reversible">A measurement written to a planning artifact.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scrub before serializing, and give the header a derived chokepoint</name>

  <read_first>
    - `scripts/board-dashboard.ts` lines 240-320 — `sanitizeCell` and `writeDocument`, the two
      functions this task changes, and the line-boundary rule that must survive the change
    - `scripts/board-dashboard.ts` lines 440-495 — `renderHeader` in full, including the badge and
      reset style codes that must stay OUTSIDE the sanitizer
    - `scripts/board-dashboard.ts` lines 1060-1100 — `emit`, where the frame is written and where
      `currentWatchErrors()` is merged, so the scrub's scope is known
    - `scripts/board-dashboard.test.ts` lines 1804-1930 — the write-site census, whose shape the new
      header census copies and whose header exemption must be restated rather than removed
    - `agent-factory/contracts/board.md` § "Control characters on the way out" — the sentence that
      describes the input as already escaped, which the measurement contradicts
    - `.planning/phases/32-board-projector-cli-dashboard/32-28-RED-baseline.txt` — Task 1's numbers
  </read_first>

  <files>scripts/board-dashboard.ts, scripts/board-dashboard.js, scripts/board-dashboard.test.ts, agent-factory/contracts/board.md</files>

  <action>
SCRUB THE VALUES BEFORE THE SERIALIZER SEES THEM. Add `scrub`: a recursive pass over the published
value that applies `sanitizeCell` to every string leaf and to every object key, descends into arrays
and nested objects, and leaves numbers, booleans and null alone. `writeDocument` becomes scrub, then
serialize, then the existing post-serialization pass — which STAYS, because it is what keeps the line
boundary this module's structure rather than content's, and removing it would let a code point the
serializer leaves raw forge a second record. Two passes, two stated reasons, written at the function.

DO NOT REWRITE THE ESCAPED FORM. The rejected alternative is to strip the six-character escaped
sequences out of the serialized text, and it is rejected for the reason the current docblock gives: a
sequence that was text in the input is a value the consumer asked for, and rewriting it alters data.
Scrubbing before serialization has the property the other ordering lacks — it never sees, and never
alters, a value that was already text. Say so where the change is.

RESTATE BOTH AUTHORITIES TO DESCRIBE THE REAL INPUT. The module docblock and
`agent-factory/contracts/board.md` both describe the input as an already-escaped sequence. The
measurement shows the escaped form is manufactured by the serializer from a raw control character in
an ordinary ticket title, which nobody asked for. Rewrite both sentences to state what the projector
now guarantees — that a value recovered from the published document carries no control code point —
and what remains outside it, which is a consumer that re-encodes the value itself.

GIVE THE HEADER ITS OWN CHOKEPOINT AND DERIVE THE CLAIM. Add `part`, the one function that produces a
header part, applying `sanitizeCell` for the reason `cell` does and stating that the header is
deliberately not truncated. Route every parts entry through it, including the two that interpolate a
source name and a stale reason. The terminal style codes stay outside `part`, because they are this
module's own control sequences rather than content, and that exemption is named at the site. Then add
an AST-derived case asserting that every element of the parts list and every push argument inside
`renderHeader` is either a call to `part` or a template whose interpolations are each a call to `part`
or a member access on the style parameter, with `HEADER_PART_SITE_COUNT` pinned two-sided so a new
part is a number somebody looks at.

TEST FROM THE OUTSIDE, THROUGH A SPAWNED PROCESS. Add a case that spawns the real CLI over a fixture
carrying raw control characters in a row title, a ticket title and a column name, parses the captured
document, walks it, and asserts zero control code points in every string value and every key at every
depth — and asserts the document is still one line and still parses as exactly one document. Add the
converse: a title containing the literal six-character escaped sequence AS TEXT survives unchanged, so
the scrub is not double-decoding anything.

MUTATE TO PROVE DISCRIMINATION. Remove the scrub and record that the parse-then-inspect case reds;
remove the post-serialization pass and record that the line-boundary case reds; drop one header part
out of `part` and record that the header census reds. Three mutations, three named cases.

REBUILD THE COMMITTED TWIN IN THE SAME COMMIT.
  </action>

  <behavior>
    - Given raw control characters in a row title, a ticket title and a column name, one parse of the captured document yields zero control code points in any string value or key at any depth.
    - Given the same run, the document is one line and parses as exactly one document.
    - Given a title containing the escaped sequence as literal text, the parsed value carries that text unchanged.
    - Given the header's syntax tree, every parts entry is a sanitizing call or a style member access.
    - Given a new unsanitized parts entry, the header census fails.
  </behavior>

  <acceptance_criteria>
    - `scripts/board-dashboard.ts` declares `scrub` and applies it inside `writeDocument` before serialization, with the post-serialization pass retained and both reasons written at the function.
    - `renderHeader` produces every part through one function, and the style codes' exemption is named at the site.
    - `scripts/board-dashboard.test.ts` carries a spawned-process case that parses the document and walks values AND keys at every depth, asserting zero control code points.
    - `HEADER_PART_SITE_COUNT` is asserted two-sided against the AST-derived site list.
    - `agent-factory/contracts/board.md` states what a consumer recovers rather than describing the input as pre-escaped.
    - Three mutation runs are recorded in `32-28-GREEN-proof.txt`, each naming the case that red.
    - `scripts/board-dashboard.js` is rebuilt from its source in the same commit.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-dashboard.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or a passed-case count at or below the count recorded in 32-28-RED-baseline.txt — cases were replaced rather than added</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or the freshness output names scripts/board-dashboard.js as not matching a rebuild of its source</fails_when>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit — a module inside the dashboard closure changed and the read-only guard from plans 32-24 and 32-25 now refuses it</fails_when>
    <automated>node scripts/board-dashboard.js scripts/fixtures/board-snapshot --once --json | wc -l</automated>
    <fails_when>non-zero exit, or a printed line count other than 1 — the document boundary moved and board content could forge a second record</fails_when>
  </verify>

  <done>A consumer recovers no control character from board content, the line boundary is still this module's, and the header's sanitization is derived rather than conventional.</done>

  <reversibility rating="reversible">Output-formatting changes inside one module; the published field set and the schema version do not move.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Let the attribution assertions decide, and disposition the CI half in writing</name>

  <read_first>
    - `scripts/board-watch-live.test.ts` lines 30-110 — the no-skip decision, the constants and the
      docblock explaining why the deadline must be strictly below the poll period
    - `scripts/board-watch-live.test.ts` lines 415-460 — the event-path case and its two attribution
      assertions, which must not change
    - `.planning/WINDOWS.md` ledger row 186 and `32-22-SUMMARY.md` — the recorded rule that a Windows
      red is the Phase 33 measurement arriving early and must not be answered with a platform
      conditional
    - `.planning/phases/32-board-projector-cli-dashboard/deferred-items.md` — the format this project
      uses to carry an out-of-scope item, so the new entry matches
    - `.planning/phases/32-board-projector-cli-dashboard/32-28-RED-baseline.txt` — the three recorded
      latencies and the current deadline
    - `.github/workflows/ci.yml` — the suite step and the matrix, read only so the disposition names
      the real job rather than a remembered one
  </read_first>

  <files>scripts/board-watch-live.test.ts, .planning/phases/32-board-projector-cli-dashboard/deferred-items.md, .planning/phases/32-board-projector-cli-dashboard/32-28-GREEN-proof.txt</files>

  <action>
DERIVE THE DEADLINE FROM THE PERIOD INSTEAD OF TYPING IT. Replace the literal wait deadline with a
value computed from the poll period minus a stated margin, so the one property the attribution
argument needs — strictly below the period — is expressed rather than asserted, and so the two cannot
drift when the period moves. Widen the margin to what the three recorded latencies plus real headroom
justify, and write the sentence that makes it safe: the deadline decides only when the wait gives up;
the two comparisons against the poll period and the debounce are what decide attribution, and they are
unchanged. A case that reds by timeout prints no number; a case that reds by comparison prints the
latency that failed, which is the difference between a measurement and a flake.

ADD A CASE THAT PROVES THE DEADLINE STILL BOUNDS ANYTHING. Assert the computed deadline is strictly
less than the poll period and strictly greater than the debounce, so a future margin that swallowed
the property would red here rather than silently turn the case into a poll measurement.

DISPOSITION THE CI HALF IN WRITING, AND DO NOT TAKE IT. Append an entry to
`.planning/phases/32-board-projector-cli-dashboard/deferred-items.md` in that file's existing format,
naming the review finding, what is not being done — moving the platform-dependent live cases into
their own npm script invoked from a named, non-blocking Windows CI step — the reason it is not being
done here, and where it belongs: it is a change to CI job topology, which is Phase 33's declared
scope, and this project has already recorded that a Windows red IS the CAP-02 measurement arriving
early. State the residual plainly: while the case stays in the shared suite step, a Windows failure
takes that step with it, and that cost is accepted rather than hidden. Carry the existing
`check:diff-disposition` entry unchanged — it is a pre-existing failure on five Phase-31 workflow
documents and is not absorbed into this round.

RECORD THE BEFORE AND AFTER. Run the live suite three times at the new deadline and record all three
latencies beside the three from the baseline in `32-28-GREEN-proof.txt`, plus the computed deadline,
the poll period and the debounce. A widening with no measurement beside it is a number nobody can
defend.
  </action>

  <acceptance_criteria>
    - `scripts/board-watch-live.test.ts` computes its event-path wait deadline from the poll period and a named margin rather than from a typed literal.
    - A case asserts the computed deadline is strictly below the poll period and strictly above the debounce.
    - The two attribution assertions in the event-path case are unchanged in wording and in effect.
    - `deferred-items.md` carries a new entry naming the finding, the untaken work, the reason and the owning phase, and the existing `check:diff-disposition` entry is unchanged.
    - `32-28-GREEN-proof.txt` records three post-change latencies beside the three from the baseline, with the deadline, period and debounce.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-watch-live.test.ts</automated>
    <fails_when>non-zero exit, or a printed event-path latency at or above the poll period — the case would no longer be measuring the watch at all</fails_when>
    <automated>grep -c 'status: open' .planning/phases/32-board-projector-cli-dashboard/deferred-items.md</automated>
    <fails_when>a printed count below 2 — the new disposition entry is missing, or the pre-existing carried item was absorbed instead of carried</fails_when>
  </verify>

  <done>The wait deadline is derived from the property it must satisfy, the attribution assertions still decide, and the CI-topology half is carried in writing with its owner named.</done>

  <reversibility rating="reversible">A derived constant in one test file and a written disposition; no production code moves.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| board content → the published `--json` document | board text is written by agents and by humans and reaches a CI consumer's parser unmodified in shape |
| board content → the terminal frame | the header is the one line stating whether the frame can be trusted, and it is deliberately not truncated |
| the live suite → the shared CI step | a timing case that reds by timeout takes a shared required step with it and masks every other result on that leg |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-28-01 | Tampering | `writeDocument`'s sanitize-after-serialize ordering | medium | mitigate | Values and keys are scrubbed before serialization; the post-serialization pass is retained for the line boundary, and a spawned-process case asserts zero control code points after one parse at every depth |
| T-32-28-02 | Spoofing | a forged second record in the published stream | high | mitigate | The line boundary stays this module's structure: the post-serialization pass is unchanged and a case asserts the document occupies exactly one line |
| T-32-28-03 | Tampering | `renderHeader`'s per-field sanitization convention | medium | mitigate | One `part` chokepoint with an AST-derived, two-sided census; the style codes are the one named exemption |
| T-32-28-04 | Repudiation | a live case that reds by timeout | low | mitigate | The deadline is derived from the poll period and a named margin, so the comparisons print the latency that failed |
| T-32-28-05 | Denial of Service | the shared CI suite step on the Windows leg | medium | transfer | Explicitly carried to Phase 33 with the reason and the residual written into `deferred-items.md`; not answered with a platform conditional, per this project's recorded rule |

No package-manager install task exists in this plan — the repository ships zero runtime dependencies —
so no `T-32-SC` supply-chain row and no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-dashboard.test.ts` — exit 0, case count
  above the baseline's.
- `node scripts/board-dashboard.js scripts/fixtures/board-snapshot --once --json | wc -l` — prints 1.
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-watch-live.test.ts` — exit 0 with a
  printed latency below the poll period.
- `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness` — exit 0.
- `npm run check:dashboard-readonly` — exit 0.
- `npx vitest run --exclude '**/scripts/e2e/**'` — exit 0, passed-case count at or above 4974.
</verification>

<success_criteria>
A raw control character in board content is unrecoverable by a `--json` consumer that parses the
document; the document is still exactly one line; every header part passes through one function and
the claim is derived from the module's syntax; the live event-path case reds with a latency rather
than with a timeout; and the CI-topology half of WR-07 is carried in writing with Phase 33 named as
its owner.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-28-SUMMARY.md` when done.
</output>

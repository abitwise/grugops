---
phase: 32-board-projector-cli-dashboard
plan: 38
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - scripts/board-model.ts
  - scripts/board-model.js
  - scripts/board-model.test.ts
  - scripts/board-tracer.test.ts
  - agent-factory/contracts/board.md
  - .planning/phases/32-board-projector-cli-dashboard/32-38-RED-baseline.txt
  - .planning/phases/32-board-projector-cli-dashboard/32-38-GREEN-proof.txt
autonomous: true
requirements: [DASH-03, DASH-05, DASH-07]
estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The ONE remaining `32-VERIFICATION.md` gap is proved closed END TO END rather than at the unit boundary: a real tree carrying a duplicate-identifier contest, read by `node scripts/board-dashboard.js <tree> --once --json` (D-16, D-18), produces one document in which the `conflicts[]` entry for the losing file and the tickets `readErrors` entry for the same file state the SAME join state, and the agreement is asserted by deriving one field's claim from the other rather than by matching two hand-typed sentences (DASH-03)."
    - "The end-to-end case fails against the pre-fix grammar and passes against the shipped one, proven by a committed RED baseline measured on `scripts/board-model.js` as it stood at `dba9d72a^` and a GREEN proof measured at HEAD — so the case discriminates the fix rather than passing over it."
    - "Every arm `presenceOf` can return and every sentence `presenceActual` can produce is reached by a named input in the suite, and the number of sentence branches is DERIVED from the code rather than counted by hand, so a fifth branch added later cannot ride in unproven (D-10, D-11)."
    - "The winner of a duplicate-identifier contest, a document declaring an identifier with no contest at all, and a board row naming an identifier no document declares each keep the sentence they had before this round — the round-2 closure is asserted unchanged rather than assumed."
    - "A sentence branch no input the reader can produce can reach is not left standing as an unreachable claim: either an input that reaches it is exhibited, or the property that makes it unreachable is asserted as a derived invariant over the population maps with a message naming what would make it reachable again."
    - "`agent-factory/contracts/board.md`'s presence table and `presenceActual`'s sentences are proved to agree by running the code over one input per table row and comparing against the row's own text, so the contract cannot drift into stating a consequence the code does not produce — the exact failure `32-REVIEW.md` WR-01 found (D-04)."
    - { statement: "The live claude-CLI end-to-end lane is NOT run by this plan and its state stays `UNKNOWN - verify`, as in every prior round of this phase", verification: backstop }
  prohibitions:
    - statement: "Never assert the fix from the unit boundary alone — the verifier reproduced this gap through the CLI, so the closure is measured through the CLI."
      status: pending
      verification: judgment
    - statement: "Never hand-type both sides of the agreement assertion; derive one side from the document the run produced."
      status: pending
      verification: judgment
    - statement: "Never widen the presence partition to make a sentence true; the partition's totality is what keeps `absent` honest."
      status: pending
      verification: judgment
    - statement: "Never flip a REQUIREMENTS.md checkbox or a ROADMAP.md phase status in this plan — the verifier decides, and a premature flip already had to be reverted once in this phase."
      status: pending
      verification: judgment
  artifacts:
    - "scripts/board-tracer.test.ts — the end-to-end duplicate-identifier-contest case: one spawned `--once --json` run, one parsed document, the cross-field agreement derived"
    - "scripts/board-model.test.ts — one named input per `presenceOf` arm and per `presenceActual` sentence branch, the branch count derived, and the contract-agreement rows"
    - "scripts/board-model.ts (+ `scripts/board-model.js`) — whatever the arm audit proves needs stating: a reached input, or a derived invariant standing where an unreachable sentence stood"
    - "agent-factory/contracts/board.md — the presence table, proved to agree sentence-for-sentence with the code"
    - ".planning/phases/32-board-projector-cli-dashboard/32-38-RED-baseline.txt — the contradiction measured against the pre-fix grammar, and the per-arm answers measured before any change"
    - ".planning/phases/32-board-projector-cli-dashboard/32-38-GREEN-proof.txt — the same measurements at HEAD, the derived branch count, and the contract-agreement table"
  key_links:
    - "the spawned `--once --json` document -> the cross-field agreement assertion: the operator's own artifact is the subject, not an internal call"
    - "`ticketPopulations.byStem` -> `presenceOf`'s `joinedStem` measurement -> `presenceActual`'s branch: the winner is looked up, never inferred from a stem being present"
    - "`presenceActual`'s sentence set -> `agent-factory/contracts/board.md`'s presence table: one authority, proved equal rather than kept equal by eye"
    - "the derived branch count -> the per-branch input rows: a branch added without an input is a red, not a silent passenger"
  deferred: []
---

<objective>
Close the one gap `32-VERIFICATION.md` (2026-09-16T18:10Z, `gaps_found` 4/5) left open, and prove it
closed the way the verifier measured it open — through the CLI, on one document, with the two fields
that contradicted each other asserted to agree.

**This gap is already CLAIMED fixed.** `32-REVIEW-FIX.md` (written 19:35, after the verification)
records commit `dba9d72a` as closing WR-01 in `scripts/board-model.ts`, `scripts/board-model.js`,
`scripts/board-model.test.ts` and `agent-factory/contracts/board.md`. This plan treats that as a
CLAIM, not a closure, because this repository's own recorded history across Phases 25, 27, 29, 31 and
this phase's rounds 1 through 3 is that the per-round ratio of defects CREATED BY THE PREVIOUS FIX
never fell. The claim is re-derived here from the verifier's own `missing:` list.

| `32-VERIFICATION.md` `missing:` clause | Present at HEAD? | Task |
|---|---|---|
| Distinguish the winner from a duplicate-identifier loser inside the `admitted-under-another-id` arm | `presenceOf` now carries `joinedStem` (`scripts/board-model.ts:1337`) and `presenceActual` branches on `presence.joinedStem === id` (`:1371`) | Task 1 re-measures it end to end |
| Match `agent-factory/contracts/board.md`'s table in the same commit | `board.md:290` carries the loser row | Task 3 proves the agreement mechanically |
| Add a `board-model.test.ts` case planting exactly this duplicate-identifier-loser shape, asserting the conflict text does not claim the loser is joined | `scripts/board-model.test.ts:2444` asserts it at the UNIT boundary, over a hand-built `POPULATIONS` | Task 1 adds the missing END-TO-END case |

The third row is the live shortfall. The verifier reproduced this gap by running
`node scripts/board-dashboard.js <tree> --once --json` and reading two fields of the resulting
document. The committed case builds `ticketPopulations` by hand and never crosses the read seam, so
it cannot see a regression in `readTicketsSource`'s duplicate-identifier resolution, in the join, or
in what the CLI actually publishes. Per D-16 the invocation under test is
`node scripts/board-dashboard.js [repoRoot]`, and per D-18 `--once --json` is the one-document,
non-TTY contract a CI consumer reads — so that is the surface this closure is measured on.

Purpose: D-10 makes `conflicts[]` a closed set of kinds whose job is to SURFACE a disagreement rather
than resolve it, and D-11 makes the reader's result a discriminated union so every arm is named. A
document that states two different things about one file's join state is the disagreement arriving
inside the instrument that exists to report disagreements. D-04 makes the written contract normative,
which is why the contract carrying the same false clause is part of the same defect.

Output: an end-to-end case on the tracer's own spawned-CLI harness; one named input per presence arm
with the branch count derived rather than recalled; and a mechanical agreement between the contract's
presence table and the sentences the code produces.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` section to lift. The
boundary, adjacency and unreachable-branch probes are authored into `must_haves.truths` above.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW-FIX.md
@.planning/phases/32-board-projector-cli-dashboard/32-37-ADVERSARIAL-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-33-SUMMARY.md
@.planning/phases/32-board-projector-cli-dashboard/32-15-SUMMARY.md
</context>

<assumption_delta_decision>
**Signalled family:** `pluralization` and `chosen` — `gsd_run query assumption-delta scan 32 --json`
returns `detected: true` on the same three signals round 3 recorded (`second`, `alongside`,
`parameterized`), all read out of this phase's own earlier plan prose rather than out of any new work.

**Noun now primary:** the ticket *presence* answer, still a single discriminated value per identifier.

**Decision: `no-change`.** The plural signal describes what earlier rounds DELETED and how earlier
rounds described sibling arms. This plan introduces no second presence authority, no configurable
presence rule and no fallback; it adds inputs and an agreement assertion over the one derivation that
already exists (D-11).
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — one duplicate-identifier contest, one `--json` document, two fields that agree</name>

  <read_first>
    - `scripts/board-tracer.test.ts` lines 676-760 — the `spawnSync(process.execPath, [DASHBOARD_JS, ...args])`
      harness (`:728`), `DASHBOARD_JS` (`:692`), and the `runMain` / `drive` helpers the existing
      `--once --json` cases use (`:805-900`)
    - `scripts/board-model.ts` lines 1296-1390 — `ticketPopulations` (`:1303`), `presenceOf` (`:1325`)
      including the `joinedStem` measurement (`:1337`), and `presenceActual` (`:1360`) with its
      `presence.joinedStem === id` branch (`:1371`) and its two loser sentences (`:1377-1383`)
    - `scripts/board-read.ts` — `readTicketsSource`'s duplicate-identifier resolution: which record
      is pushed as the joined one, which is pushed as the admitted loser, and the `duplicate-id`
      read-error message it composes
    - `scripts/board-model.test.ts` lines 2440-2500 — the committed unit case
      ("a duplicate-identifier LOSER is not said to be joined"), which is the case this task's
      end-to-end sibling exists to cover the far side of
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` — the `gaps:` entry, its
      `missing:` list, and the `F-12 / WR-01 … CONFIRMED OPEN` reproduction transcript
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW-FIX.md` § `WR-01` — the fix the
      commit claims to have made, which this task measures rather than trusts
  </read_first>

  <files>scripts/board-tracer.test.ts, .planning/phases/32-board-projector-cli-dashboard/32-38-RED-baseline.txt</files>

  <behavior>
    - Given a tree whose board carries a row for the losing stem and whose ticket directory carries two documents declaring one identifier, the spawned `--once --json` run exits 0 and prints exactly one parseable document.
    - Given that document, the tickets `readErrors` carry a `duplicate-id` entry naming both documents and stating which one is joined.
    - Given that document, the `conflicts[]` entry for the losing stem states a join state that AGREES with the `readErrors` entry, and the agreement is computed from the document rather than compared against a literal.
    - Given the same tree and the pre-fix grammar, the same assertion fails, and the failure message names the two disagreeing sentences.
    - Given a tree with no duplicate-identifier contest, the run is unchanged from its committed behaviour.
  </behavior>

  <action>
MEASURE THE PRE-FIX TREE FIRST, ON A DISPOSABLE COPY. Take `scripts/board-model.js` as it stood at
`dba9d72a^` (`git show dba9d72a^:scripts/board-model.js`) into a copy of the repository under the
scratchpad, build the duplicate-identifier-contest fixture there, run the CLI against it, and record
the two sentences the document carried. That transcript is the RED baseline: it is what makes the new
case a discriminator rather than a restatement. Do the same run at HEAD and record both, side by
side, in `32-38-RED-baseline.txt`.

BUILD THE FIXTURE AS A REAL TREE, NOT A HAND-BUILT POPULATION MAP. A board carrying a row for the
LOSING stem in some column, plus two ticket documents whose stems sort so that the non-row stem wins
the contest — the verifier's own shape was a row for `ABC-903` with `ABC-901.md` and `ABC-903.md`
both declaring `ABC-902`. Reproduce that shape rather than inventing a new one, so the closure is
measured against the finding as it was written.

ADD THE END-TO-END CASE TO `scripts/board-tracer.test.ts`, beside the existing spawned-CLI cases, not
to `scripts/board-model.test.ts` — the unit sibling already lives there and the missing coverage is
the seam it does not cross. Use the file's own `spawnSync` harness so the subject is the artifact a
host runs.

DERIVE THE AGREEMENT; DO NOT MATCH TWO LITERALS. Read the `duplicate-id` read error out of the parsed
document, extract from it the identifier of the document it names as joined, and then assert that the
`conflicts[]` entry for the losing stem is consistent with THAT extracted value. A case that compares
two hand-typed sentences passes the moment somebody edits both sentences the same wrong way; a case
that derives one side from the other cannot. State in the case's own message what a failure means:
one document asserting two join states for one file.

ASSERT THE HARNESS'S OWN PREMISE BEFORE THE FINDING, in the style this file already uses. The premise
here has three parts: the spawned process exited 0, stdout parsed as exactly one document, and the
tickets source reported the `duplicate-id` read error at all. If the third is silently absent the
case would pass over a tree where no contest happened, which is the vacuity this repository has
recorded six harness-premise failures against. Give each premise a `PREMISE:` message.

KEEP THE NEGATIVE CONTROL. Run the same assertion over a tree with no contest and record that the
losing-stem conflict is absent, so the case cannot pass by finding nothing anywhere.

Every probe runs on a copy; record `git diff --exit-code -- scripts/ agent-factory/` clean in the
baseline before the task's own edits begin.
  </action>

  <acceptance_criteria>
    - `scripts/board-tracer.test.ts` gains a case whose title names the duplicate-identifier contest, and it invokes the CLI through the file's existing `spawnSync` harness rather than importing `presenceOf` directly.
    - The case asserts three `PREMISE:`-messaged facts before its finding: exit code 0, exactly one parsed document, and a `duplicate-id` read error present in the tickets source.
    - The case computes the joined-document identity from the `readErrors` entry in the parsed document and asserts the `conflicts[]` entry for the losing stem against that computed value.
    - `32-38-RED-baseline.txt` carries both transcripts — the pre-fix grammar's two sentences and HEAD's two sentences — each with the exact `actual` string the run produced and the exit code.
    - `32-38-RED-baseline.txt` records the new case run against the pre-fix grammar as FAILING, with the assertion message that failed.
    - `32-38-RED-baseline.txt` records `git diff --exit-code -- scripts/ agent-factory/` as clean before the task's edits.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-tracer.test.ts</automated>
    <fails_when>non-zero exit, or a `PREMISE:` message in the output, or the new duplicate-identifier case absent from the printed case list</fails_when>
    <automated>node scripts/board-dashboard.js scripts/fixtures/board-snapshot --once --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const o=JSON.parse(s);console.log('schemaVersion='+o.snapshot.schemaVersion+' conflicts='+o.conflicts.length)})"</automated>
    <fails_when>non-zero exit, or a printed line other than `schemaVersion=2 conflicts=9` — the golden fixture moved, which this plan must not do</fails_when>
    <automated>grep -c PREMISE .planning/phases/32-board-projector-cli-dashboard/32-38-RED-baseline.txt</automated>
    <fails_when>a printed count below 3 — fewer than the three premise facts were recorded, so the baseline cannot show the case is non-vacuous</fails_when>
  </verify>

  <done>The verifier's own reproduction runs as a committed case on the spawned-CLI harness, it fails against the pre-fix grammar and passes at HEAD, and both transcripts are on record.</done>

  <reversibility rating="reversible">A test case and a measurement artifact; no production behaviour changes.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Every presence arm reached by a named input, and the branch count derived rather than recalled</name>

  <read_first>
    - `scripts/board-model.ts` lines 1240-1290 — `TICKET_PRESENCE_KINDS` (`:1240`), `TicketPresence`
      (`:1263`) including the `joinedStem: string | undefined` member (`:1276`)
    - `scripts/board-model.ts` lines 1296-1390 — `ticketPopulations`'s first-wins fills of `byId` and
      `byStem` (`:1310-1313`), `presenceOf`'s three lookups, and `presenceActual`'s four cases with
      the `admitted-under-another-id` case's three sentence branches
    - `scripts/board-model.test.ts` lines 2340-2520 — the two-sided pin of `TICKET_PRESENCE_KINDS`
      (`:2354`), the per-arm cases (`:2398`, `:2440`), the duplicate-identifier-loser case (`:2444`)
      and the sentence table (`:2501-2512`)
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` § `IN-01` — the naming defect
      against `admitted-under-its-stem`, which plan `32-39` owns; this task must not rename anything,
      only make sure every arm it audits is reached
    - `.planning/phases/32-board-projector-cli-dashboard/32-38-RED-baseline.txt` — Task 1's per-arm
      answers measured before any change
  </read_first>

  <files>scripts/board-model.ts, scripts/board-model.js, scripts/board-model.test.ts</files>

  <behavior>
    - Given the set of sentence branches derived from the module rather than listed by hand, every branch has at least one named input in the suite that reaches it.
    - Given a document that WON its contested identifier and a board row naming that document's stem, the sentence is the one round 2 closed, byte for byte.
    - Given a document declaring an identifier nobody contests and a board row naming its stem, the sentence is unchanged.
    - Given a board row naming an identifier no document declares and no refusal records, the sentence stays the single negative this module is allowed to assert.
    - Given the `joinedStem === undefined` branch, either an input reaching it is exhibited, or an invariant over the population maps that forbids it is asserted with a message naming what would make it reachable.
    - Given a fifth sentence branch added to the module, the derived count disagrees with the input rows and the suite reds.
  </behavior>

  <action>
DERIVE THE BRANCH SET FROM THE MODULE, THEN ASSERT THE COUNT. This repository's second recorded
systemic failure class is a hand-maintained set that rots while the suite stays green, and
`presenceActual`'s sentences are exactly such a set: four cases, one of which now carries three
sentence branches. Obtain the branch count by parsing `scripts/board-model.ts` (the same
TypeScript-parse instrument `scripts/validate.test.ts` already uses for its census) and counting the
`return` sites inside `presenceActual`, then assert that the number of named input rows in the suite
equals it, with a vacuity floor that reds on an EMPTY branch set AND on a SILENTLY SHORT one — state
the expected floor as a number, not as "greater than zero".

EXHIBIT ONE NAMED INPUT PER BRANCH. The five to audit, each as a `ticketPopulations` built from
records the reader could actually produce rather than from a literal map: the loser of a contest; the
WINNER of a contest whose stem differs from its declared identifier; a document declaring an
identifier with no contest at all; a refused entry; and a board identifier nothing declares. Record
each sentence the run produced in the GREEN proof alongside the sentence the same input produced
BEFORE this round, taken from Task 1's baseline — a closure that silently changed a sibling arm's
wording is a regression this phase has shipped twice.

DECIDE THE `joinedStem === undefined` BRANCH BY MEASUREMENT, NOT BY READING. `byStem`'s values are
identifiers taken from the same record list `byId` is keyed by, so ask directly whether any input the
reader can produce yields a stem whose declared identifier is absent from `byId`. Try to construct
one. If one exists, add it as a named input row like the others and the branch stands. If none does,
the sentence is an unreachable claim and this repository's rule is that an unreachable claim gets
replaced by the assertion that makes it unreachable: state the invariant over the two maps as a
derived check inside the suite, with a message naming exactly what change to `ticketPopulations`
would make the branch reachable again, and record in the GREEN proof which of the two outcomes was
measured. Do not delete a branch on the strength of an argument — only on the strength of a failed
attempt to reach it, written down.

ASK THE CONVERSE OF THE ARM THIS ROUND'S FIX TOUCHED. The fix's discriminator is
`presence.joinedStem === id`. Probe the shape where the CONTESTED identifier is itself a stem on
disk — a document whose stem equals the identifier two other documents are contesting — and record
which arm `presenceOf` reaches, what sentence (if any) is produced, and whether the operator can
still tell from the document which file is joined. Record the answer whichever way it comes out; if
it is a finding rather than a pass, it belongs in the GREEN proof and in plan `32-40`'s inventory,
not in a silent widening here.

If the audit proves a change to `scripts/board-model.ts` is warranted, keep it inside `presenceOf` /
`presenceActual`, rebuild so `scripts/board-model.js` is a faithful build of its source, and prove the
change RED-first against the case that demanded it.
  </action>

  <acceptance_criteria>
    - `scripts/board-model.test.ts` derives the `presenceActual` branch count by parsing the module and asserts it equals the number of named input rows, with a stated numeric floor on both the branch set and the row set.
    - Each named input row is built from records shaped as `readTicketsSource` produces them, and each records the sentence produced.
    - The suite contains either an input that reaches the `joinedStem === undefined` branch, or a derived invariant assertion over `byStem` and `byId` whose message names the change that would make the branch reachable.
    - `32-38-GREEN-proof.txt` states, per audited branch, the sentence produced before this round and the sentence produced at the end of it, and marks each row SAME or CHANGED.
    - `32-38-GREEN-proof.txt` records the contested-identifier-is-also-a-stem probe with the arm reached and the sentence produced.
    - `npm run build` leaves `scripts/board-model.js` matching its source, proved by `check:build-parity` exiting 0.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts</automated>
    <fails_when>non-zero exit, or `0 passed` in the summary line, or a total case count below the 2026-09-16 measurement recorded in `32-38-RED-baseline.txt`</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or `BUILD PARITY FAILED` in the output — a committed `.js` on this tree would not be a build of its `.ts`</fails_when>
    <automated>grep -c 'SAME\|CHANGED' .planning/phases/32-board-projector-cli-dashboard/32-38-GREEN-proof.txt</automated>
    <fails_when>a printed count below 5 — fewer than the five audited branches carry a before/after verdict</fails_when>
  </verify>

  <done>Every sentence the presence derivation can produce is reached by a named input, the branch count is derived from the module, and the unreachable branch is either exhibited or replaced by the invariant that forbids it.</done>

  <reversibility rating="reversible">Test inputs and, at most, one arm of one pure function; the module has no state and no external effect.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The contract's presence table and the code's sentences proved equal, not kept equal by eye</name>

  <read_first>
    - `agent-factory/contracts/board.md` lines 275-300 — the presence table, including the
      duplicate-identifier-loser row at `:290` that commit `dba9d72a` added
    - `scripts/board-model.ts` lines 1355-1390 — `presenceActual`, the authority the table describes
    - `scripts/board-model.test.ts` — the existing contract-agreement style used elsewhere in this
      phase for `HEADING_SUFFIXES` and the conflict-kind closure, to copy rather than invent
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` § `WR-01` — the paragraph
      stating that the contract carried the same false clause verbatim, which is why an agreement by
      eye is what failed
    - `.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md` D-04 — the decision that the
      normative contract is a written authority rather than commentary
  </read_first>

  <files>agent-factory/contracts/board.md, scripts/board-model.test.ts, .planning/phases/32-board-projector-cli-dashboard/32-38-GREEN-proof.txt</files>

  <behavior>
    - Given the presence table read from the contract at run time, every row's sentence template is produced by the code for at least one named input.
    - Given a code sentence with no row in the table, the assertion reds and names the sentence.
    - Given a table row no code branch produces, the assertion reds and names the row.
    - Given the table edited to state a consequence the code does not produce, the assertion reds.
  </behavior>

  <action>
READ THE CONTRACT AT RUN TIME AND COMPARE BOTH DIRECTIONS. Parse the presence table out of
`agent-factory/contracts/board.md` in the suite, normalise each row's sentence into a template by
replacing the identifier and file-name positions with placeholders, and do the same to the sentence
the code produces for each of Task 2's named inputs. Then assert SET EQUALITY, both directions — a
one-directional check is how the contract drifted into carrying a false clause while the code was
correct, and this repository has recorded the converse-check lesson in three separate phases.

STATE THE VACUITY FLOOR AS A NUMBER. An empty parsed table must red, and so must a silently SHORT
one: derive the expected row count from the code's branch count established in Task 2 and assert the
parsed table matches it, so a table that lost a row to a markdown edit reds rather than trivially
satisfying a subset check.

MAKE THE TABLE'S OWN LOCATION DERIVED. Locate the table by its heading rather than by a line number,
and red with a message naming the heading if the section is absent — a section-anchored reader that
searches to end-of-file adopts an unrelated later block, which is a bypass shape this repository
recorded in Phase 29.

PROVE THE ASSERTION DISCRIMINATES. Run it against a copy of the contract with one row's consequence
clause reverted to the pre-`dba9d72a` wording, and record that it reds and which row it named. Run it
against a copy with one row deleted, and record that it reds on the count. Restore both and record
`git diff --exit-code -- agent-factory/` clean.

RECORD THE AGREEMENT TABLE IN THE GREEN PROOF: one row per contract row, its template, the code
branch that produces it, and the named input that reached that branch.

If and only if the comparison finds a genuine disagreement, correct `agent-factory/contracts/board.md`
in the same commit as the assertion that found it, and say in the summary which direction was wrong.
  </action>

  <acceptance_criteria>
    - The suite parses the presence table from `agent-factory/contracts/board.md` at run time, locating it by heading, and reds with a message naming the heading when the section is absent.
    - The comparison asserts set equality in both directions and pins the row count against the branch count derived in Task 2.
    - `32-38-GREEN-proof.txt` carries the agreement table: one row per contract row with its template, its code branch and its named input.
    - `32-38-GREEN-proof.txt` records two discrimination runs — one reverted consequence clause and one deleted row — each with the assertion message that fired.
    - `32-38-GREEN-proof.txt` records `git diff --exit-code -- agent-factory/` clean after the discrimination runs.
    - `32-38-GREEN-proof.txt` records the full-suite result for this plan as a file count, a passed-case count and an exit code.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts scripts/board-tracer.test.ts</automated>
    <fails_when>non-zero exit, or `0 passed` in the summary line, or the contract-agreement case absent from the printed case list</fails_when>
    <automated>node scripts/validate-agent-factory.js</automated>
    <fails_when>non-zero exit, or a `FAIL` line in the output — the contract edit broke the kit's own structure validator</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or a passed-case count below 5137 (the count `32-REVIEW-FIX.md` recorded at HEAD on 2026-09-16), or any test file reported as failed</fails_when>
  </verify>

  <done>The contract and the code are proved to state the same set of sentences, in both directions, with the assertion shown to red on a reverted clause and on a missing row.</done>

  <reversibility rating="reversible">An assertion over two artifacts that already exist; no runtime behaviour depends on it.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `plans/tickets/*.md` content an agent or human writes → the projector's presence derivation | a document chooses its own declared identifier, so the population maps are filled from untrusted content and a contest is a shape any writer can create |
| the `--json` document → a CI consumer or a future web renderer | the consumer acts on fields it cannot cross-check; two fields disagreeing is an untrue statement delivered as fact |
| `agent-factory/contracts/board.md` → any reader adjudicating a disagreement | a contract that states a consequence the code does not produce is a wrong answer with the authority of a spec |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-38-01 | Repudiation | `presenceActual`'s `admitted-under-another-id` arm | high | mitigate | The join state is measured from `byId` and asserted end to end against the same document's `readErrors`, so the snapshot cannot state two join states for one file |
| T-32-38-02 | Tampering | a ticket document choosing a contested identifier | medium | mitigate | The contest is resolved first-by-file-name in one place and the loser is named in both the read error and the conflict; the end-to-end case pins that both name the same winner |
| T-32-38-03 | Repudiation | an unreachable sentence branch left standing | medium | mitigate | The branch set is derived from the module and every branch is either reached by a named input or replaced by the asserted invariant that forbids it |
| T-32-38-04 | Spoofing | `agent-factory/contracts/board.md` stating a sentence the code does not produce | medium | mitigate | Two-directional set equality between the parsed table and the code's sentences, with a row count pinned against the derived branch count |
| T-32-38-05 | Information Disclosure | conflict sentences quoting document content | low | accept | No sentence quotes a byte of any document body; the path is a function of the board's own identifier and the declared identifier is already published as `tickets[].id` |
| T-32-38-06 | Denial of Service | a contest fixture making the CLI refuse rather than report | low | mitigate | The end-to-end case asserts exit 0 and exactly one parsed document as premises before its finding |

No package-manager install task exists in this plan — the repository ships zero runtime dependencies
and adds none — so no `T-32-SC` supply-chain row and no package-legitimacy checkpoint applies.
ASVS level 1; nothing in this plan is dispositioned `accept` at `high`.
</threat_model>

## Artifacts this phase produces

Created or changed by this plan:

| Artifact | Kind | Note |
|---|---|---|
| `scripts/board-tracer.test.ts` — the duplicate-identifier-contest end-to-end case | new test case | spawned `--once --json`, cross-field agreement derived from the document |
| `scripts/board-model.test.ts` — the derived `presenceActual` branch count, the per-branch named input rows, and the contract-agreement case | new test cases | branch count parsed from the module, both-directions set equality against the contract |
| `scripts/board-model.ts` (+ `scripts/board-model.js`) | possibly changed | only if the arm audit proves a change warranted; the `joinedStem === undefined` branch is exhibited or replaced by a derived invariant |
| `agent-factory/contracts/board.md` — the presence table | possibly changed | only if the two-directional comparison finds a real disagreement |
| `.planning/phases/32-board-projector-cli-dashboard/32-38-RED-baseline.txt` | new file | pre-fix and HEAD transcripts, premise rows, clean-tree record |
| `.planning/phases/32-board-projector-cli-dashboard/32-38-GREEN-proof.txt` | new file | per-branch before/after verdicts, the contested-stem probe, the agreement table, two discrimination runs |
| `.planning/phases/32-board-projector-cli-dashboard/32-38-SUMMARY.md` | new file | plan summary |

No exported symbol is renamed by this plan. `TICKET_PRESENCE_KINDS`'s member spellings are owned by
plan `32-39` (IN-01) and must not be touched here.

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-tracer.test.ts scripts/board-model.test.ts`
  — exit 0, no `PREMISE:` message, case counts at or above the baseline's.
- `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness` — exit 0.
- `node scripts/validate-agent-factory.js` — exit 0.
- `npm run check:dashboard-readonly` — exit 0, 171 passed, unchanged by this plan.
- `npm run check:nul-bytes` — exit 0; no artifact this plan writes carries a raw control byte, and a
  control character discussed in prose is spelled as escape TEXT.
- `npx vitest run --exclude '**/scripts/e2e/**'` — exit 0, passed-case count at or above 5137.
- `npm test` is NOT run; the live claude-CLI end-to-end lane stays `UNKNOWN - verify`.
</verification>

<success_criteria>
The one gap the round-3 verifier left open is measured closed on the surface the verifier measured it
open — one spawned `--once --json` document whose `conflicts[]` and `readErrors` are asserted to
agree about one file's join state, by derivation rather than by two matching literals — and the arm
the fix touched is audited across every sibling branch, with the branch count derived from the module
and the contract proved equal to the code in both directions.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-38-SUMMARY.md` when done.
</output>

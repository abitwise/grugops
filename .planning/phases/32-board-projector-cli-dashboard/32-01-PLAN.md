---
phase: 32-board-projector-cli-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/contracts/board.md
  - scripts/board-model.ts
  - scripts/board-model.js
  - scripts/board-read.ts
  - scripts/board-read.js
  - scripts/board-dashboard.ts
  - scripts/board-dashboard.js
  - scripts/board-tracer.test.ts
  - package.json
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-07, DASH-08]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running `node scripts/board-dashboard.js . --once --json` from this repository root prints exactly one JSON document whose `snapshot.board.columns` has 13 entries in on-disk heading order and whose `snapshot.board.columns[*].rows` are all empty — the live kit board ships zero live rows (D-05, D-08)."
    - "The 48-line HTML comment at `plans/board.md:4-51` contributes zero columns and zero rows to that document, because a comment-blanking pre-pass runs before any heading or row scan (D-03)."
    - "`scripts/board-model.ts` is pure: its compiled import closure contains no `node:fs` specifier at all (D-15, D-23)."
    - "A row of the form `- [ID] title  (meta)  trailing prose` yields `{title, meta, trailer}` with nothing inside `meta` or `trailer` parsed (D-01, D-22)."
    - "An empty board file yields zero columns, zero rows, an empty `preamble`, and `source: \"ok\"` — never `unavailable` and never an error (edge: empty input)."
    - "Columns are emitted in on-disk heading order and rows in on-disk line order; two headings that normalize to the same name stay two separate columns in file order (edge: ordering, adjacency)."
    - "`--json` writes exactly one document to stdout and every diagnostic to stderr; `--once` exits 0 (D-18)."
    - "`node scripts/board-dashboard.js . --once` with no `--json` prints a plain-text frame with no ANSI escape byte when stdout is not a TTY (D-17, D-18)."
    - "`repoRoot` is `realpathSync`-resolved once and every joined subpath is a fixed literal; an unresolvable `repoRoot` exits 2 with a named message on stderr (T-32-03)."
  prohibitions:
    - statement: "The dashboard closure holds no mutating `node:fs` symbol."
      status: enforced
      verification: "`scripts/board-model.ts`, `scripts/board-read.ts` and `scripts/board-dashboard.ts` import only `existsSync`, `readFileSync`, `readdirSync`, `statSync`, `realpathSync` from `node:fs`; the mechanical guard lands in plan 32-06."
    - statement: "The dashboard opens no listening socket."
      status: enforced
      verification: "No `node:net`, `node:http`, `node:https`, `node:child_process` or `node:worker_threads` specifier appears in any of the three new modules; the mechanical guard lands in plan 32-06."
    - statement: "No runtime dependency is added."
      status: enforced
      verification: "`git diff --exit-code -- package-lock.json` and `node -e \"process.exit(require('./package.json').dependencies===undefined?0:1)\"`"
    - statement: "No runnable `scripts/*.ts` imports `typescript`."
      status: enforced
      verification: "The three new runnable modules import only `node:` builtins and relative `./*.js`."
    - statement: "An ENOENT, a torn read or a partial parse is never rendered as an empty board."
      status: partial
      verification: "The tracer establishes the discriminated result shape and the `unavailable` arm that carries no value; the read-verify-reread and last-good machinery lands in plan 32-03."
    - statement: "Agents never merge a protected branch and never deploy to production without named human confirmation."
      status: enforced
      verification: "This plan adds no merge or deploy path; the existing prod-deploy PreToolUse hook is untouched."
  artifacts:
    - agent-factory/contracts/board.md
    - scripts/board-model.ts
    - scripts/board-model.js
    - scripts/board-read.ts
    - scripts/board-read.js
    - scripts/board-dashboard.ts
    - scripts/board-dashboard.js
    - scripts/board-tracer.test.ts
  key_links:
    - "`board-dashboard.ts` imports `board-read.js`; `board-read.ts` imports `board-model.js`; `board-model.ts` imports nothing from `node:fs` — the D-15/D-23 boundary is an import edge, not a comment."
    - "`board-model.ts`'s header cites `agent-factory/contracts/board.md` as the authority for every shape it admits (D-04)."
    - "`package.json` `dashboard` script → `scripts/board-dashboard.js`; `npm run freshness` picks the new `.js` twins up automatically (`OUTPUT_DIRS` already covers `scripts/`)."
---

<objective>
Wire ONE path through every layer this phase touches and prove it end-to-end: the real kit board on
disk → a comment-blanking pre-pass → the D-05 heading grammar and the D-01/D-22 row grammar → a
`FactorySnapshot` carrying `schemaVersion: 1` → a discriminated read result (D-11) → one `--json`
document and one plain-text `--once` frame on stdout.

Purpose: the architecture of this phase is a three-module purity boundary (D-15, D-23) plus a
published snapshot shape (D-19, one-way for a future web renderer). Proving that boundary holds on
the agent's best early-context tokens costs one commit; discovering it after the grammar, the join,
the watch loop and the renderer have all landed costs the phase.

Output: `agent-factory/contracts/board.md` (the normative spec, headings + rows sections),
`scripts/board-model.ts`, `scripts/board-read.ts`, `scripts/board-dashboard.ts` with their committed
`.js` twins, `scripts/board-tracer.test.ts`, and the `dashboard` npm script.

This is a tracer, not a prototype. Every line of it ships. Stubs are allowed only where filling them
later needs no architectural change: the join beyond the board itself (plan 32-05), the full line
partition (plan 32-02), read-verify-reread (plan 32-03) and the full D-17 frame (plan 32-07) are
functionality gaps by design. There are no architectural gaps.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md
@.planning/phases/32-board-projector-cli-dashboard/32-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The contract and the pure grammar — one heading form, one row form, end to end</name>
  <files>agent-factory/contracts/board.md, scripts/board-model.ts, scripts/board-model.js, scripts/board-tracer.test.ts</files>
  <read_first>
    - `agent-factory/contracts/context-note.md:1-40` — the contract sibling: frontmatter `kind: contract` / `contract:` / `requirement:`, then the "this document is the authoritative schema" opening and the "the only sanctioned writer is X" sentence to mirror.
    - `scripts/canonical-frontmatter.ts:1-40, 93-137, 538-560` — the D-64 admission posture: the "does not interpret, it ADMITS" docblock, the closed `as const` set with its iterate-me comment, the two-arm discriminated result, the no-widening-knob entry signature, and the CRLF normalization done before anything else.
    - `scripts/validate-agent-factory.ts:250-256, 721-762` — `kebab` and the WR-03 helper pair this module replaces; read the comment at `:729-734` for the counterexample that must stay pinned.
    - `plans/board.md:1-80` — the live board: the `_Updated:` placeholder at `:2`, the 48-line comment at `:4-51` with a four-space-indented mini-board inside it, the frozen column table at `:58-72`, the 13 `##` column headings.
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Architecture Patterns Pattern 1 and Pattern 2, §Code Examples "Candidate row parse" — the measured 141/141 splitter and the line-number-preserving comment blanker.
    - `scripts/check-imperative-lexicon.ts:1-30, 230-245` — the gate that will start scanning the new contract file the moment it lands (`flatMarkdown("agent-factory/contracts")`), and the clear-professional-voice rule it enforces.
    - `vitest.config.ts` — `globals: false`, so `describe`/`it`/`expect` are imported explicitly.
  </read_first>
  <behavior>
    - Given `plans/board.md` as read from disk, `parseBoard` returns 13 columns whose names are `Backlog`, `Ready`, `In Analysis`, `In Design`, `Ready for Dev`, `In Development`, `In Review`, `In Security/NFR`, `Ready for UAT`, `In UAT`, `Ready to Release`, `Done`, `Blocked`, in that on-disk order, each with zero rows.
    - Given the same text, `## Columns (spec §6.1)` and `## Conventions` open no column, and the four-space-indented `## In Development (WIP 1/3)` inside the HTML comment opens no column either.
    - Given `- [ABC-014] Asset allocation chart  (owner: Software Engineer, since: 2026-06-01)`, `splitRow` returns title `Asset allocation chart`, meta `owner: Software Engineer, since: 2026-06-01`, trailer `""`.
    - Given `- [DOG-001] Project scaffold + CI baseline  (epic: EPIC-006, size: M, P0)  — merged to main 2026-06-06 (lean: done = merged)`, `splitRow` returns meta `epic: EPIC-006, size: M, P0` and a non-empty trailer; nothing inside either is parsed.
    - Given a row whose parenthetical never closes, `splitRow` returns the whole remainder as `title` with `meta: null` — the row stays legal.
    - Given `""`, `parseBoard` returns zero columns, zero rows and an empty `preamble` without throwing.
    - Given `## Blocked (2)` (the old spec's form), no column opens — the heading is a non-column heading.
    - `boardHasColumn(parseBoard(text), "In")` is `false` when the board carries `## In Development (WIP 0/3)` and no `## In (…)` heading.
  </behavior>
  <action>
Author `agent-factory/contracts/board.md` first, because every later test in this phase derives its
axes from it. Frontmatter `kind: contract`, `contract: board-grammar`, `requirement: DASH-01`.
Clear professional voice throughout (CLAUDE.md C7 — this is a technical and safety surface, never
caveman voice). Sections this task fills: **Headings** (D-05's exactly three legal suffixes — the
limited form carrying a live number and a limit, the unlimited form, and the single Blocked form;
every other `##` suffix is a non-column heading and opens no column; `###` never opens one) and
**Rows** (D-01 as amended by D-22 — the only grammar is a bullet, a bracketed ID, a space, a title;
the parenthetical is one opaque string and everything after its balanced close is a second opaque
string; nothing inside either is interpreted; D-02's ID bound and the `EPIC`/`FEAT` second class).
State in the opening that the only sanctioned reader of this grammar is `scripts/board-model.ts`,
mirroring `context-note.md`'s sanctioned-writer sentence. Leave the Conflicts, Staleness, Bounds and
Reconciliation sections as named headings with a one-line "filled by plan 32-02" note — they land
next wave. Do not touch the column table in `plans/board.md`; this task writes a new file only.

Author `scripts/board-model.ts` with a `canonical-frontmatter.ts`-shaped header: what the module is,
that it cites `agent-factory/contracts/board.md` as its authority, that it imports nothing from
`./context-io.js` and nothing from `./claim.js` (both export writers) and why, and that it has no
`process`, no timers and no import of the Node filesystem builtin (D-15, D-23). Export, in this
order:
`SCHEMA_VERSION = 1` (D-19); `HEADING_SUFFIXES` as a readonly `as const` array of three entries each
carrying a `kind` of `limited`, `unlimited` or `blocked` and its anchored pattern, plus
`HEADING_SUFFIX_COUNT = 3` with a message that reads as a decision and never as a bumped constant;
`stripHtmlComments(text)` implementing Pattern 2 — replace every character of every `<!-- … -->` span
with a space while preserving every newline so line numbers survive, and blank to end-of-file on an
unterminated opener (fail-closed); `splitRow(rest)` implementing the measured splitter — find the
first two-space-then-open-paren, scan with a depth counter to the balanced close, return
`{title, meta, trailer}`, and on an unbalanced parenthetical return the whole remainder as `title`
with `meta: null`; `boardColumnName(line)` returning the trimmed name for a line matching one of the
three suffixes and `null` otherwise; `boardHasColumn(model, col)` requiring exact equality after the
suffix strip; `kebab(s)` moved here verbatim from `scripts/validate-agent-factory.ts:250-256` so the
tree keeps one spelling; and `parseBoard(text)` returning a `BoardModel` of
`{columns, epicRows, updates, preamble, nonColumnSections, unparsed, bounds}`. Normalize CRLF before
anything else, the way `canonical-frontmatter.ts:554-560` does.

In this task `parseBoard` populates `columns` (with `name`, `heading`, `kind`, `claimedLive`,
`limit`, `line`, `rows`), `rows` (with `id`, `title`, `meta`, `trailer`, `line`), `epicRows` and
`preamble`; `updates`, `nonColumnSections`, `unparsed` and `bounds` are declared in the type and
returned as empty or zero values with a comment naming plan 32-02 as the task that fills them. That
is a functionality gap, not an architectural one: the fields exist in `schemaVersion: 1` from the
first commit, so filling them later moves no boundary.

Every regex is anchored at `^` and carries no nested quantifier — the corpus contains a 34,494-character
line and this repository has already paid for one superlinear-backtracking incident (T-32-01). Never
left-trim a line before the heading or row scan: both real mini-boards are four-space indented and a
single leading-whitespace trim turns documentation into live state.

Write `scripts/board-tracer.test.ts` covering every case in `<behavior>` above, reading the REAL
`plans/board.md` from the repository root (not a transcription), with a `PREMISE:`-prefixed assertion
before the findings that the file was read and is non-empty — the `context-io-writer-set.test.ts:238-250`
idiom. Run `npm run build` and commit `scripts/board-model.js` in the same commit as its `.ts`.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-tracer.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any `PREMISE:` assertion message appears in the output</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line in the output</fails_when>
    <automated>npm run check:imperative-lexicon</automated>
    <fails_when>non-zero exit, or a line naming `agent-factory/contracts/board.md` as a FAIL, or a `DERIVATION_REFUSALS` entry naming `boardColumns`</fails_when>
    <automated>grep -vE '^\s*(//|\*|/\*)' scripts/board-model.ts | grep -c 'node:fs' || true</automated>
    <fails_when>any printed count other than 0 — outside its comment lines `board-model.ts` must carry no filesystem import (D-15, D-23)</fails_when>
  </verify>
  <acceptance_criteria>
    - `agent-factory/contracts/board.md` exists, its frontmatter carries `contract: board-grammar` and `requirement: DASH-01`, and it contains an `## Headings` section and a `## Rows` section.
    - `npm run check:imperative-lexicon` exits 0 with the new contract file inside its derived corpus (its `contracts` part size grows by exactly one; the part is derived by `flatMarkdown`, so no literal is edited).
    - `grep -vE '^\s*(//|\*|/\*)' scripts/board-model.ts | grep -c 'node:fs' || true` prints `0`.
    - `grep -vE '^\s*(//|\*|/\*)' scripts/board-model.ts | grep -c 'trimStart'` returns `0`.
    - `node -e "import('./scripts/board-model.js').then(m=>{const b=m.parseBoard(require('node:fs').readFileSync('plans/board.md','utf8'));if(b.columns.length!==13)throw new Error('columns='+b.columns.length);if(b.columns.some(c=>c.rows.length))throw new Error('live rows on the kit board');console.log('13 columns, 0 rows')})"` prints `13 columns, 0 rows` and exits 0.
    - `scripts/board-model.js` exists, is committed, and `npm run check:build-parity` exits 0.
    - `HEADING_SUFFIX_COUNT` is asserted against `HEADING_SUFFIXES.length` in the test, in both directions.
  </acceptance_criteria>
  <reversibility rating="costly">`schemaVersion: 1`'s field list — including `meta`, `trailer` (D-22), `preamble` and `nonColumnSections` (D-24) — is the published shape a future web renderer consumes (D-19). Adding a field later is additive; reinterpreting one breaks the golden and every consumer. The three one-way questions behind it were already settled by the user as D-22, D-23 and D-24; this task records them as decided and does not re-open them.</reversibility>
  <done>`plans/board.md` parses to 13 columns in on-disk order with zero rows, the HTML comment block contributes nothing, the amended row grammar round-trips all six measured shapes, and `board-model.ts` imports no `node:fs`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The read seam — `board-read.ts` resolves the root once and returns a discriminated result</name>
  <files>scripts/board-read.ts, scripts/board-read.js, scripts/board-tracer.test.ts</files>
  <read_first>
    - `scripts/js-import-closure.ts:37-38, 74-98` — `assertInsideRoot` and the refuse-rather-than-return-short posture, verbatim, plus the read-only `node:fs` import line to mirror.
    - `scripts/kit-model.ts:95-102, 185-225` — the read-only `node:fs` import set and `MAX_WALK_ENTRIES` with its "a hung gate is not a red gate" rationale.
    - `scripts/context-io.ts:896-926` — `atomicWrite`'s Windows branch: the unlink-then-rename window that is the ENOENT case DASH-05 names. Read it to understand the hazard; do not import this module.
    - `scripts/claim.ts:1-43, 64` — the queue layout and `QUEUE_STAGES`; note that `renderNowRunning` ends in a write, so nothing here may be imported.
    - `.planning/phases/32-board-projector-cli-dashboard/32-RESEARCH.md` §Architecture Patterns Pattern 4 (the `SourceState<T>` union whose `unavailable` arm deliberately carries no value) and §Security Domain V12.
    - `agent-factory/config/factory.config.json` — the `mode`, `id_prefix` and `wip_limits` keys the snapshot cross-checks.
  </read_first>
  <behavior>
    - `readSnapshot(repoRoot)` on this repository returns `{source: "ok"}` with a snapshot whose `board` source state is `ok` and whose `queue` source state is `unavailable` with `present: false` — `.grugops/` does not exist on this tree and that is a legitimate state, not a stale one (D-13).
    - `readSnapshot(<a path that does not resolve>)` throws a named `BoardReadError` rather than returning a snapshot.
    - `readSnapshot(<a directory with no plans/board.md>)` returns a result whose `board` source state is `unavailable`, and the top-level `source` is `unavailable` — never an `ok` result with zero columns.
    - The resolved root is a realpath; a `repoRoot` argument containing `..` that escapes the resolved root is refused before any read.
    - Every joined subpath is a fixed literal (`plans/board.md`, `agent-factory/config/factory.config.json`); no path is derived from any file's content.
  </behavior>
  <action>
Author `scripts/board-read.ts` as the single fs-touching seam (D-23). Its header states that
`board-model.ts` is pure by construction and that this module exists so the purity claim is an
import edge a guard can check, and cites `scripts/claim.ts:38-44`'s fixed-literal-subpath rule and
ASVS V12 as the reason no path is ever derived from content.

Import from the Node filesystem builtin exactly `existsSync`, `readFileSync`, `statSync`, `readdirSync`,
`realpathSync` and nothing else; from `node:path` exactly `join`, `relative`, `resolve`, `sep`.
Import `MAX_WALK_ENTRIES` from `./kit-model.js` (verified read-only) rather than declaring a second
bound. Import `parseBoard` and the snapshot types from `./board-model.js`.

Export `SOURCE_NAMES` as a readonly `as const` tuple of `board`, `tickets`, `queue`, `context`,
`traceability`, `config`, plus `SOURCE_COUNT = 6` asserted two-sided in the test with a
decision-not-a-constant message. Export the `SourceState<T>` union of Pattern 4 with exactly three
arms — `ok` and `stale` carry a value and a `readAt`, `unavailable` carries `present: false` and
**no value**, so "render an empty board because the file was missing" is unrepresentable rather than
discouraged. Export `SnapshotResult` as `{source, snapshot, conflicts, readErrors}` (D-11) and
`readSnapshot(repoRoot, previous?)` as the entry point.

Resolve the root once with `resolve` then `realpathSync`, and refuse with a named `BoardReadError`
when it does not exist or is not a directory — the `js-import-closure.ts:89-98` refusal tone, naming
the path. Reuse that module's inside-root assertion shape for every target before reading it.

In this task `readSnapshot` reads two sources for real — `plans/board.md` through `parseBoard`, and
`agent-factory/config/factory.config.json` through `JSON.parse` inside a `try` whose `catch` marks
the `config` source `stale` and never throws (T-32-09). The other four sources return the
`unavailable` arm with a comment naming plan 32-03 as the task that reads them. Reads in this task
are a single `readFileSync` guarded by `existsSync`; the stat-read-stat retry and the last-good
carry-forward land in plan 32-03, which changes no signature. Directory listings filter any name
containing `.tmp-` explicitly, not incidentally by extension (RESEARCH Pitfall 5), and are bounded by
`MAX_WALK_ENTRIES` in the **reporting** form — over the bound the source is marked `stale` with
reason `bounded` and the loop continues; a hung read is a stale badge, never a frozen screen (D-14).

Extend `scripts/board-tracer.test.ts` with the `<behavior>` cases above, including a temp-directory
case for the missing-board arm. Build and commit `scripts/board-read.js` alongside its `.ts`.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-tracer.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line in the output</fails_when>
    <automated>grep -vE '^\s*(//|\*|/\*)' scripts/board-read.ts | grep -cE 'from "\./(context-io|claim)\.js"'</automated>
    <fails_when>any count other than 0 — both modules export writers and would red the DASH-06 guard</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/board-read.ts` imports from `node:fs` only names drawn from the set `existsSync`, `readFileSync`, `statSync`, `readdirSync`, `realpathSync`.
    - `grep -vE '^\s*(//|\*|/\*)' scripts/board-read.ts | grep -cE 'from "\./(context-io|claim)\.js"'` returns `0`.
    - `SOURCE_COUNT` is asserted equal to `SOURCE_NAMES.length` and `SOURCE_NAMES.length` is asserted equal to `6`, as two separate cases.
    - The `unavailable` arm of `SourceState` has no `value` property in the exported type — `npm run typecheck` fails if a caller reads one.
    - `node -e "import('./scripts/board-read.js').then(m=>{const r=m.readSnapshot('.');if(r.source!=='ok')throw new Error('source='+r.source);if(r.snapshot.sources.queue.source!=='unavailable')throw new Error('queue should be unavailable on this tree');console.log('ok, queue unavailable')})"` prints `ok, queue unavailable` and exits 0.
    - `scripts/board-read.js` is committed and `npm run check:build-parity` exits 0.
  </acceptance_criteria>
  <done>The read seam resolves the root once, refuses an unresolvable root by name, returns the three-arm discriminated result, and distinguishes an absent `.grugops/` from a stale one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The CLI end of the wire — `--once`, `--json`, exit codes, and the `dashboard` script</name>
  <files>scripts/board-dashboard.ts, scripts/board-dashboard.js, scripts/board-tracer.test.ts, package.json</files>
  <read_first>
    - `scripts/coordinator-resolution-precheck.ts:293-307, 589-600` — the banner and the `main(argv) => code` tail this module mirrors.
    - `scripts/is-entry.ts:1-30` — `isEntrypoint`, the only legal entry detection, and the recorded reason (a symlinked invocation makes a hand-rolled comparison exit 0 having done nothing).
    - `scripts/check-platform-shapes.ts:60-80` — the import discipline for a runnable `scripts/*.ts`: node builtins plus relative `./*.js`, nothing else.
    - `scripts/check-foundation-guards.test.ts` — the test that refuses any `import.meta.url ===` spelling outside `is-entry.ts`; grep it for `import.meta.url` before writing the entry guard.
    - `package.json` scripts block — the two-step `tsc --outDir .tmp-build && node scripts/<x>.js` shape every runnable follows.
    - `.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md` D-16, D-17, D-18.
  </read_first>
  <behavior>
    - `node scripts/board-dashboard.js . --once --json` exits 0 and writes exactly one document to stdout that `JSON.parse` accepts; `schemaVersion` is `1`; stderr is empty.
    - `node scripts/board-dashboard.js . --once` exits 0 and writes a plain-text frame containing the substring `13 columns` and containing no ESC (0x1B) byte.
    - `node scripts/board-dashboard.js` with no positional argument defaults `repoRoot` to `process.cwd()` and behaves as above.
    - `node scripts/board-dashboard.js . --nonsense` exits 2 and writes a usage message to **stderr**, nothing to stdout.
    - `node scripts/board-dashboard.js /no/such/path --once --json` exits 2 with a named message on stderr and nothing on stdout.
    - `node scripts/board-dashboard.js . --json` with stdout piped implies `--once` (D-18) and emits exactly one document.
    - `main(argv, io)` accepts an injected `{stdout, stderr, isTty}` so the render branch is testable without a pty; production passes nothing and the call site stays `main(process.argv.slice(2))`.
  </behavior>
  <action>
Author `scripts/board-dashboard.ts` as the process-owning half of the D-15 boundary: argv, stdout
discipline, exit codes and — from plan 32-03 onward — timers and watch handles. It imports
`readSnapshot` from `./board-read.js`, `isEntrypoint` from `./is-entry.js`, and nothing else outside
`node:` builtins.

Export `parseArgs(argv)` returning either an `Options` record of `{repoRoot, once, json, watch,
intervalMs}` or a named usage refusal; the flags are the positional `[repoRoot]`, `--once`, `--json`,
`--watch`, `--interval <ms>` and `--help` (D-16, D-18). `--interval` is validated as a base-10
integer and clamped by refusal, not by silent rounding: a non-integer, a negative, or a value below
the 1000 ms hard floor is a usage refusal and exit 2 (T-32-10, ASVS V5). Export
`POLL_FLOOR_MS = 10_000`, `DEBOUNCE_MS = 250` and `INTERVAL_HARD_FLOOR_MS = 1_000` as named
constants now so plan 32-03 wires them rather than inventing them.

Export `main(argv, io?)` returning a number, where `io` defaults to `{stdout: process.stdout,
stderr: process.stderr, isTty: process.stdout.isTTY === true}`. `--json` writes exactly one
`JSON.stringify` document plus a newline to `io.stdout` and nothing else there; every diagnostic,
every refusal message and every read error summary goes to `io.stderr` (T-32-08 — a raw stack must
never be the last thing a piped consumer sees, so the entry guard catches and prints a named
one-line message). `--once`, and `!io.isTty` without `--json`, print one frame and exit 0 even when
the result is stale or conflicted: the state is in the frame and the exit code is not the channel
(D-18). Exit 2 is reserved for a usage error and an unreadable `repoRoot`.

The frame this task renders is the thin version of D-17: one header line carrying the resolved repo
root, the config `mode`, the last read time and the column count, then one line per column reading
name, live-over-limit, and row count, with Blocked last. It goes through a `sanitizeCell(s)` helper
that strips every C0 and C1 control character and every escape introducer before the string reaches
stdout — board content is untrusted input and a ticket title carrying an ANSI or OSC sequence must
never reach a terminal unescaped (T-32-06). The full D-17 layout, the stale badge, the conflict
list, width truncation and the TTY redraw land in plan 32-07; the sanitizer and the header/column
skeleton do not move when they do.

Tail the module with the `coordinator-resolution-precheck.ts:589-600` shape, initialising the code
to 2 and guarding with `isEntrypoint(import.meta.url)`. Do not write any other entry-detection
spelling — a foundation-guard test refuses every one of them.

Add to `package.json` scripts exactly one entry this task:
`"dashboard": "tsc --outDir .tmp-build && node scripts/board-dashboard.js"` (D-16). Add no
`dependencies` key. Do not touch `install/install.ts` or `install/uninstall.ts`: D-16 rules out an
installer shim this phase, and the dashboard is invoked from the repository, so nothing needs to be
installed under `tools/grugops/`.

Extend `scripts/board-tracer.test.ts` with the `<behavior>` cases, spawning the compiled `.js` as a
child process for the exit-code and stream-separation cases (the `check-platform-shapes.ts` drive
harness is the in-repo precedent) and calling `main(argv, io)` directly for the render-branch cases.
Build and commit `scripts/board-dashboard.js`.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-tracer.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>npm run build && node scripts/board-dashboard.js . --once --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);if(j.snapshot.schemaVersion!==1)throw new Error('schemaVersion='+j.snapshot.schemaVersion);console.log('one json document, schemaVersion 1')})"</automated>
    <fails_when>non-zero exit, a `SyntaxError` from `JSON.parse` (more or less than one document on stdout), or the absence of `one json document, schemaVersion 1` in the output</fails_when>
    <automated>node scripts/board-dashboard.js . --nonsense > /tmp/gsd32-out.txt 2> /tmp/gsd32-err.txt; echo "exit=$?; stdout_bytes=$(wc -c < /tmp/gsd32-out.txt)"</automated>
    <fails_when>the printed line is not `exit=2; stdout_bytes=       0` (any exit other than 2, or any byte on stdout)</fails_when>
    <automated>npm run typecheck && npm run check:build-parity</automated>
    <fails_when>non-zero exit, any `error TS` line, or a `BUILD PARITY FAILED` line</fails_when>
  </verify>
  <acceptance_criteria>
    - `node scripts/board-dashboard.js . --once --json` exits 0, stdout parses as exactly one JSON document, stderr is empty.
    - `node scripts/board-dashboard.js . --once | LC_ALL=C grep -c $'\\033'` returns `0`.
    - `node scripts/board-dashboard.js . --interval 500` exits 2 and names the 1000 ms floor on stderr.
    - `grep -vE '^\s*(//|\*|/\*)' scripts/board-dashboard.ts | grep -c 'import.meta.url ==='` returns `0`.
    - `node -e "process.exit(require('./package.json').scripts.dashboard==='tsc --outDir .tmp-build && node scripts/board-dashboard.js'?0:1)"` exits 0.
    - `node -e "process.exit(require('./package.json').dependencies===undefined?0:1)"` exits 0.
    - `git diff --exit-code -- package-lock.json` exits 0 (the lockfile is untouched).
    - `git status --porcelain install/` prints nothing (D-16: no installer shim this phase).
  </acceptance_criteria>
  <done>One command run from a checkout reads the real board, prints one JSON document with `schemaVersion: 1`, prints one ANSI-free plain frame without `--json`, exits 0 on success and 2 on a usage error or an unreadable root — the whole wire is proven end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent's editor → `plans/board.md` | Board content is written by agents and humans with no schema enforcement; it is untrusted input to the parser |
| argv → `repoRoot` | The only externally supplied path; everything else is a fixed literal joined against it |
| parsed board content → terminal stdout | Ticket titles and metadata reach a terminal emulator that interprets escape sequences |
| `factory.config.json` → the cross-check | Arbitrary JSON on disk, possibly malformed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Denial of Service | `parseBoard` / `splitRow` regexes over a 34,494-char line | high | mitigate | Every pattern anchored at `^`, no nested quantifier, no backtracking-prone alternation; `splitRow`'s paren scan is a single linear pass with a depth counter. The wall-clock bound assertion lands with the oracle in plan 32-04 |
| T-32-03 | Tampering | `repoRoot` from argv — path traversal and symlink escape | high | mitigate | `resolve` then `realpathSync` once in `board-read.ts`; the `js-import-closure.ts:74-82` inside-root assertion applied to every target; every subpath a fixed literal; exit 2 on an unresolvable root |
| T-32-06 | Spoofing | ANSI/OSC sequences in a ticket title or `meta` rendered to a terminal | high | mitigate | `sanitizeCell()` in `board-dashboard.ts` strips every C0/C1 control and escape introducer before any string reaches stdout; asserted by the no-ESC-byte criterion in task 3 |
| T-32-08 | Information Disclosure | a raw Node stack as the last bytes a piped consumer reads | medium | mitigate | The `isEntrypoint` tail catches and prints a named one-line message to stderr; stdout carries only the JSON document or the frame |
| T-32-09 | Tampering | malformed `agent-factory/config/factory.config.json` | low | mitigate | `JSON.parse` inside a `try`; the `config` source is marked stale and the process continues (CLAUDE.md C6: run lean when config is absent or unusable) |
| T-32-10 | Denial of Service | `--interval` supplied as `NaN`, negative, or below the poll floor | medium | mitigate | Base-10 integer validation plus the 1000 ms hard floor, refused with exit 2 rather than clamped silently (ASVS V5) |
| T-32-04 | Tampering / Elevation of Privilege | a dashboard that could write | high | mitigate | This plan designs it out (read-only `node:fs` import sets, no `context-io.js`/`claim.js` import, asserted by grep here); the mechanical import-graph guard lands in plan 32-06 |
| T-32-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this phase (32-RESEARCH §Package Legitimacy Audit: zero candidate packages). The absence is asserted, not merely refrained from: `git diff --exit-code -- package-lock.json` and a `dependencies === undefined` check in task 3's acceptance criteria |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Files (new)**
- `agent-factory/contracts/board.md` — normative contract, `## Headings` and `## Rows` sections filled
- `scripts/board-model.ts` / `scripts/board-model.js`
- `scripts/board-read.ts` / `scripts/board-read.js`
- `scripts/board-dashboard.ts` / `scripts/board-dashboard.js`
- `scripts/board-tracer.test.ts`

**Exported symbols — `scripts/board-model.ts`**
`SCHEMA_VERSION` · `HEADING_SUFFIXES` · `HEADING_SUFFIX_COUNT` · `stripHtmlComments` · `splitRow` ·
`boardColumnName` · `boardHasColumn` · `kebab` · `parseBoard`
Types: `BoardModel` · `BoardColumn` · `BoardRow` · `EpicRow` · `RowParts` · `UpdateEntry` ·
`UnparsedLine` · `NonColumnSection` · `Bounds` · `FactorySnapshot`

**Exported symbols — `scripts/board-read.ts`**
`SOURCE_NAMES` · `SOURCE_COUNT` · `readSnapshot` · `BoardReadError`
Types: `SourceState<T>` · `SnapshotResult` · `ReadError`

**Exported symbols — `scripts/board-dashboard.ts`**
`parseArgs` · `main` · `sanitizeCell` · `POLL_FLOOR_MS` · `DEBOUNCE_MS` · `INTERVAL_HARD_FLOOR_MS`
Types: `Options` · `DashboardIo`

**CLI surface** — `node scripts/board-dashboard.js [repoRoot] [--once] [--json] [--watch] [--interval <ms>] [--help]`

**npm scripts** — `dashboard`
</artifacts_this_phase_produces>

<planner_assumptions>
Edge probes the deterministic pass returned as `unclassified` for this plan's requirements are
surfaced here rather than dropped (spec-less fallback, §C):

- **DASH-02 (unclassified)** — planner reading: the probe could not classify "the row grammar is
  pinned by a written spec and a fuzz suite" into a standard edge category because the requirement's
  edge surface is a *corpus*, not a numeric bound. Resolved in plan 32-04 by the mutation corpus and
  the discrimination half. Flagged, not dismissed.
- **DASH-03 (unclassified)** — planner reading: the edge surface is the seven-kind conflict closure,
  not a boundary. Resolved in plan 32-05 by the two-sided kind count and the seven-kind golden.
  Flagged, not dismissed.
- **DASH-07 (unclassified)** — planner reading: the edge surface is the stdout/stderr/exit-code
  contract. Covered here by task 3's exit and stream-separation cases and completed in plan 32-07.
  Flagged, not dismissed.
</planner_assumptions>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-tracer.test.ts` green
- `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness` green
- `npm run check:imperative-lexicon` green with `agent-factory/contracts/board.md` inside its corpus
- `node scripts/board-dashboard.js . --once --json` → one JSON document, `schemaVersion: 1`, exit 0
- `node scripts/board-dashboard.js . --once` → ANSI-free plain frame, exit 0
- `git diff --exit-code -- package-lock.json` → exit 0
</verification>

<success_criteria>
The single path `plans/board.md` → `board-model.parseBoard` → `board-read.readSnapshot` →
`board-dashboard.main` → one `--json` document and one `--once` frame works end to end and is
committed, with `board-model.ts` provably free of `node:fs`, the contract file in the tree and
inside the imperative-lexicon corpus, the `dashboard` npm script wired, and `package.json` carrying
no `dependencies` key.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-01-SUMMARY.md` when done
</output>

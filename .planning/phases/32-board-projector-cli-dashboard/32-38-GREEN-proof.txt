PLAN 32-38 — GREEN PROOF
========================

Phase 32, gap-closure round 4. Measured 2026-09-16 on branch `main`.
Companion artifact: `32-38-RED-baseline.txt` (the before-image every row below is compared against).


PART A — TASK 2: EVERY SENTENCE BRANCH REACHED BY A NAMED INPUT
===============================================================

A1. THE BRANCH COUNT IS DERIVED FROM THE MODULE, NOT RECALLED
-------------------------------------------------------------

`scripts/board-model.ts` is parsed with the TypeScript compiler API — the same instrument
`scripts/validate.test.ts` uses for its reader census — `presenceActual` is located by name, and
every `return` site inside it is expanded into the set of strings it can produce, with each `${...}`
replaced by a hole. Concatenation is a cross product; a conditional is a union; an expression shape
the walk does not recognise THROWS rather than silently leaving the set.

  DERIVED BRANCH COUNT = 6

  [1] "<returns null — not a conflict>"
  [2] "plans/tickets/<>.md exists and declares the identifier <>, so it is joined under that
       identifier and not this one"
  [3] "plans/tickets/<>.md exists and declares the identifier <>, which no admitted document is
       joined under, so it is joined under no identifier"
  [4] "plans/tickets/<>.md exists and declares the identifier <>, which plans/tickets/<>.md
       claimed first, so it is joined under no identifier"
  [5] "plans/tickets/<>.md exists and the reader could not admit it (<>)"
  [6] "no ticket file carries that identifier"

The count is pinned as a NUMBER (`toBe(6)`), not as "greater than zero", so an EMPTY derived set and
a SILENTLY SHORT one both red. A separate `toBeGreaterThan(0)` PREMISE assertion distinguishes
"`presenceActual` was not found / produced nothing readable" from "the count moved".


A2. DISCRIMINATION — A SEVENTH BRANCH CANNOT RIDE IN UNPROVEN
--------------------------------------------------------------

A seventh branch was planted in `scripts/board-model.ts` (`if (id === "ZZZ-PROBE") return "a
seventh branch nobody wrote an input row for";` inside the `absent` case) and the suite re-run:

  $ npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts
  exit=1
  Tests  2 failed | 146 passed (148)

  × derives the branch count by parsing `presenceActual`, and pins it against the audited rows
    AssertionError: a sentence branch landed or left `presenceActual`. A seventh branch means a
    population the four-arm partition did not name, which is a finding to record in
    agent-factory/contracts/board.md and to give a named input row — never a bumped constant:
    expected 7 to be 6

  × every derived branch is reached by a named input, and every named input reaches a derived branch
    AssertionError: a named input produced a sentence no `return` in presenceActual can produce —
    the input rows and the module disagree about what the code says: expected [ …(6) ] to deeply
    equal [ …(7) ]

  $ git checkout HEAD -- scripts/board-model.ts
  $ git diff --exit-code -- scripts/board-model.ts
  exit=0   (CLEAN)


A3. THE PER-BRANCH VERDICT TABLE — BEFORE THIS ROUND vs AT THE END OF IT
-------------------------------------------------------------------------

"Before" is `32-38-RED-baseline.txt` section 8, measured at HEAD 069dcd9c against the compiled
`scripts/board-model.js` before any edit in this plan. Five of the six inputs are built from records
`readTicketsSource` produced from a real temp tree; the sixth is noted in A4.

  B1 — a document admitted under its OWN stem
       input : tree { ABC-001.md declares ABC-001 }, ask "ABC-001"
       before: null
       after : null
       verdict: SAME

  B2 — the WINNER of a duplicate-identifier contest (stem != declared identifier)
       input : tree { ABC-901.md declares ABC-902, ABC-903.md declares ABC-902 }, ask "ABC-901"
       before: "plans/tickets/ABC-901.md exists and declares the identifier ABC-902, so it is
                joined under that identifier and not this one"
       after : identical, byte for byte
       verdict: SAME

  B3 — joinedStem === undefined (no admitted document holds the declared identifier)
       input : a directly-constructed TicketPopulations — byStem {ABC-950 -> ABC-951}, byId empty
       before: "plans/tickets/ABC-950.md exists and declares the identifier ABC-951, which no
                admitted document is joined under, so it is joined under no identifier"
       after : identical, byte for byte
       verdict: SAME

  B4 — the LOSER of a duplicate-identifier contest
       input : tree { ABC-901.md declares ABC-902, ABC-903.md declares ABC-902 }, ask "ABC-903"
       before: "plans/tickets/ABC-903.md exists and declares the identifier ABC-902, which
                plans/tickets/ABC-901.md claimed first, so it is joined under no identifier"
       after : identical, byte for byte
       verdict: SAME

  B5 — refused by the ticket grammar
       input : tree { ABC-900.md carrying `tools: Bash`, outside the closed ticket key set }
       before: "plans/tickets/ABC-900.md exists and the reader could not admit it (unknown-key)"
       after : identical, byte for byte
       verdict: SAME

  B6 — a board identifier no document declares and no refusal records
       input : tree { ABC-001.md declares ABC-001 }, ask "ABC-404"
       before: "no ticket file carries that identifier"
       after : identical, byte for byte
       verdict: SAME

  NOTHING CHANGED, AND THAT IS THE POINT OF THE TABLE. The round-2 and round-3 closures are asserted
  unchanged rather than assumed: this phase has twice shipped a fix that silently reworded a sibling
  arm. Each row above is a live `toBe(row.before)` assertion in
  `scripts/board-model.test.ts`, not a transcription.

  The six produced sentences are also compared to the six DERIVED templates as SET EQUALITY IN BOTH
  DIRECTIONS, after holing the identifiers and refusal codes that went into them. A subset check in
  either direction is how a branch rides in unproven.


A4. THE `joinedStem === undefined` BRANCH — WHICH OF THE TWO OUTCOMES WAS MEASURED
-----------------------------------------------------------------------------------

OUTCOME MEASURED: the branch is UNREACHABLE from any tree the reader can read, AND it is still
reachable through the exported `presenceOf`. It was therefore NOT deleted, and BOTH facts are
asserted.

  (i) UNREACHABLE FROM THE READER, measured rather than argued. A tree carrying every population at
      once — a self-declaring document, a contest winner, a contest loser, a lone mismatched
      document, a grammar-refused document, and the degenerate `.md` entry — read with
      `readTicketsSource` and indexed with `ticketPopulations`:

        records=4 unadmitted=2 byStem=4 byId=3
        byStem entries whose declared identifier is absent from byId: 0  []

      The suite asserts that emptiness as a derived invariant over the two maps, with two PREMISE
      guards (a non-trivial `byStem`, and at least one entry whose stem differs from its declared
      identifier) so the property cannot hold vacuously. The message names exactly what would make
      the branch reachable again: filling `byStem` from a second record list, guarding the
      `byId.set` on a condition the `byStem.set` does not share, or deleting from `byId` after the
      loop. Any of those makes the assertion red, and the branch then needs a reader-producible
      input row and a row in `agent-factory/contracts/board.md`'s presence table.

      WHY IT IS STRUCTURAL: `ticketPopulations` fills both maps in ONE loop over the SAME record
      list, so every `byStem` VALUE is some record's `id` and every record's `id` is a `byId` KEY —
      first-wins decides WHICH record a key holds, never WHETHER the key is present.

  (ii) REACHABLE BY DIRECT CONSTRUCTION, so the branch is not dead code. `TicketPopulations` is an
      exported type and `presenceOf` an exported function; a caller handing it
      `byStem {ABC-950 -> ABC-951}` with an empty `byId` reaches the arm and gets the sentence
      quoted in A3. The branch is the total handling of the declared
      `joinedStem: string | undefined` and is kept for that reason. It is NOT deleted on the
      strength of an argument — and it was not kept on the strength of one either: the failed
      attempt to reach it from the reader is written down above.

  CONSEQUENCE FOR THE CONTRACT (Part B): the presence table in
  `agent-factory/contracts/board.md` describes what the READER can produce, so its row count is
  pinned against the READER-PRODUCIBLE branches — the derived count minus the one branch this
  section proves unreachable. That partition is itself asserted (`exactly ONE audited branch is
  unreachable from any tree the reader can read`), so it cannot be widened by hand to make a
  count work out.


A5. THE CONVERSE PROBE — the contested identifier is ITSELF a stem on disk
---------------------------------------------------------------------------

The fix's discriminator is `presence.joinedStem === id`. This tree makes the contested identifier
also the NAME of a third document, so the stem population and the declared population overlap on
the value the discriminator compares.

  tree: ABC-901.md declares ABC-902 | ABC-902.md declares ABC-905 | ABC-903.md declares ABC-902

  byId   = [["ABC-902","ABC-901"], ["ABC-905","ABC-902"]]
  byStem = [["ABC-901","ABC-902"], ["ABC-902","ABC-905"], ["ABC-903","ABC-902"]]

  id=ABC-901  arm=admitted-under-another-id  joinedStem="ABC-901"
     sentence="plans/tickets/ABC-901.md exists and declares the identifier ABC-902, so it is joined
               under that identifier and not this one"
  id=ABC-902  arm=admitted-under-its-stem    joinedStem=undefined   sentence=null
  id=ABC-903  arm=admitted-under-another-id  joinedStem="ABC-901"
     sentence="plans/tickets/ABC-903.md exists and declares the identifier ABC-902, which
               plans/tickets/ABC-901.md claimed first, so it is joined under no identifier"
  id=ABC-905  arm=admitted-under-its-stem    joinedStem=undefined   sentence=null

  VERDICT: PASS, not a finding — recorded whichever way it came out, as the plan requires.
  A board row naming ABC-902 raises NO `row-without-file`, because a document genuinely IS joined
  under ABC-902 and the stated arm priority answers from the DECLARED population first. The
  operator can still tell WHICH file is joined: every record in `tickets[]` carries `stem` beside
  `id` (the schemaVersion-2 field plan 32-33 added), so `{"id":"ABC-902","stem":"ABC-901"}` is
  published. The loser still names the winner by file name and the winner still says it is joined.
  Nothing here belongs in plan 32-40's inventory.


A6. NO CHANGE TO `scripts/board-model.ts` WAS WARRANTED
--------------------------------------------------------

The arm audit found every branch either reached by a named input or proved unreachable-from-the-
reader-but-reachable-by-direct-construction, and every sibling sentence unchanged. The module was
therefore not edited. `scripts/board-model.js` is byte-identical to its committed state, and:

  $ npm run build && npm run typecheck && npm run check:build-parity
  exit=0
  "Build parity: no tracked build output moved when tsc ran."

  ON THE `<fails_when>` CLAUSE "or `BUILD PARITY FAILED` in the output": npm echoes the SCRIPT
  DEFINITION before running it, and that definition contains the literal failure text inside its own
  `node -e` fallback. The string is therefore present in the captured output of a PASSING run. The
  discriminating signals are the exit code (0) and the emitted success line, both recorded above;
  the genuine failure diagnostic is written to STDERR by the fallback and is absent here.


A7. CASE COUNTS, SO A LATER RUN CAN TELL A LOSS FROM A GAIN
------------------------------------------------------------

  scripts/board-model.test.ts   before this plan: 141 passed   after Task 2: 148 passed  (+7)
  scripts/board-tracer.test.ts  before this plan:  82 passed   after Task 1:  84 passed  (+2)


---
Part B (Task 3) follows.


PART B — TASK 3: THE CONTRACT AND THE CODE PROVED EQUAL, IN BOTH DIRECTIONS
===========================================================================

B1. THE TABLE IS LOCATED BY HEADING AND BOUNDED BY THE NEXT ONE
----------------------------------------------------------------

`agent-factory/contracts/board.md` gained a dedicated `### The presence table` heading immediately
above the table. IT HAD NONE: the nearest heading above it was `## Conflicts` (line 242), whose
section ALSO carries the seven-kind conflict table a few lines earlier. A section-anchored reader
told to find "the table under `## Conflicts`" would have had two tables to choose between, and one
that searched to end-of-file would have had several more — the bypass shape this repository recorded
in Phase 29 (a section-anchored fence reader adopting an unrelated later block).

The suite therefore:
  - finds the line whose trimmed text is exactly `### The presence table`, and THROWS a message
    naming that heading when it is absent (asserted against a synthetic document);
  - bounds the span at the NEXT line matching `^#{1,6} ` (here `### The column set`, line 338);
  - takes the first markdown table inside that span, and refuses a row that does not have exactly
    two cells or whose consequence cell is neither one backticked span nor a `Nothing…` prose cell.

  The bound itself is asserted: a synthetic document carrying one row under the heading and a
  SECOND table after the next heading parses to ONE row, not two.


B2. THE AGREEMENT TABLE — one row per contract row
----------------------------------------------------

Templates are normalised identically on both sides: `${...}` in the module's source and `<id>` /
`<other>` / `<winner>` / `<code>` in the contract both become `<>`, and a sentence the code PRODUCED
is holed by replacing the identifiers and refusal code that went into it.

  contract row 1
    measured : "A document declares it."
    template : <returns null — not a conflict>
    branch   : B1 — `case "admitted-under-its-stem": return null;`
    input    : "a document admitted under its OWN stem" (tree { ABC-001.md declares ABC-001 })

  contract row 2
    measured : "A file of that stem was admitted under a **different** declared identifier, **and
                that file is the one joined under it**."
    template : plans/tickets/<>.md exists and declares the identifier <>, so it is joined under
               that identifier and not this one
    branch   : B2 — the `presence.joinedStem === id` arm
    input    : "the WINNER of a duplicate-identifier contest, stem != declared identifier"
               (tree { ABC-901.md declares ABC-902, ABC-903.md declares ABC-902 }, ask ABC-901)

  contract row 3
    measured : "A file of that stem declares a **different** identifier but **lost the
                duplicate-identifier contest** for it, so it is joined under nothing."
    template : plans/tickets/<>.md exists and declares the identifier <>, which
               plans/tickets/<>.md claimed first, so it is joined under no identifier
    branch   : B4 — the `joinedStem !== undefined` leg of the ternary
    input    : "the LOSER of a duplicate-identifier contest" (same tree, ask ABC-903)

  contract row 4
    measured : "A file of that stem exists and the ticket grammar refused it."
    template : plans/tickets/<>.md exists and the reader could not admit it (<>)
    branch   : B5 — `case "refused"`
    input    : "a listed document the grammar could not admit"
               (tree { ABC-900.md carrying `tools: Bash` })

  contract row 5
    measured : "The identifier is in none of the three measured sets."
    template : no ticket file carries that identifier
    branch   : B6 — `case "absent"`
    input    : "a board identifier no document declares and no refusal records" (ask ABC-404)

  NOT IN THE TABLE, DELIBERATELY: branch B3 (`joinedStem === undefined`). Part A4 proves no tree the
  reader can read reaches it, and the table describes what the READER can produce. The row count is
  pinned against `derived branches − branches proved unreachable` (6 − 1 = 5), and the unreachable
  partition is itself an assertion, so it cannot be widened by hand to make a count work out.

  SET EQUALITY IS ASSERTED IN BOTH DIRECTIONS over these five templates. A code sentence with no row
  reds and prints the sentence; a row no branch produces reds and prints the row. A separate
  assertion requires every contract row to be reached by a NAMED INPUT that actually ran — a
  template that matches the module's source but that nothing ever executed is a claim, not a
  measurement.


B3. DISCRIMINATION RUN 1 — one row's consequence clause reverted to the pre-`dba9d72a` wording
------------------------------------------------------------------------------------------------

Row 3's consequence was rewritten back to the clause `dba9d72a` replaced ("so it is joined under
that identifier and not this one"), which is the exact false clause review WR-01 found.

  $ npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts
  exit=1

  × parses a NON-EMPTY, NON-SHORT table whose row count is DERIVED from the code's branch count
    AssertionError: two presence-table rows state the SAME consequence, so one of them is
    describing a measurement it does not belong to: ["<returns null — not a conflict>",
    "plans/tickets/<>.md exists and declares the identifier <>, so it is joined under that
    identifier and not this one","plans/tickets/<>.md exists and declares the identifier <>, so it
    is joined under that identifier and not this one","plans/tickets/<>.md exists and the reader
    could not admit it (<>)","no ticket file carries that identifier"]: expected 4 to be 5

  × asserts SET EQUALITY IN BOTH DIRECTIONS between the table and the sentences the code produces
    AssertionError: the normative contract and the code disagree about the set of sentences the
    projector can state. A sentence on the left with no partner on the right is a row describing a
    consequence the code does not produce — exactly what review WR-01 found. […]: expected [ …(5) ]
    to deeply equal [ …(5) ]

  THE ROW IS NAMED: the duplicate-template message prints both offending templates verbatim, so the
  reader is told which row lost its own consequence rather than only that a count moved.


B4. DISCRIMINATION RUN 2 — one row deleted
--------------------------------------------

The `absent` row was removed from the table.

  $ npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts
  exit=1

  × parses a NON-EMPTY, NON-SHORT table whose row count is DERIVED from the code's branch count
    AssertionError: the presence table lost or gained a row: expected 4 to be 5

  × asserts SET EQUALITY IN BOTH DIRECTIONS between the table and the sentences the code produces
    AssertionError: […]: expected [ …(4) ] to deeply equal [ …(5) ]

  × the prose above the table states the number of facts the table actually distinguishes
    AssertionError: the contract's prose does not say "which covers three different facts", but the
    table it introduces distinguishes 3 conflict-raising answers: expected false to be true


B5. DISCRIMINATION RUN 3 — the prose fact-count reverted
----------------------------------------------------------

  $ npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts
  exit=1
  × the prose above the table states the number of facts the table actually distinguishes
    AssertionError: the contract's prose does not say "which covers four different facts", but the
    table it introduces distinguishes 4 conflict-raising answers: expected false to be true


B6. THE TREE AFTER THE DISCRIMINATION RUNS
--------------------------------------------

Each probe was reverted from a byte-copy taken before it, and the file compared byte for byte:

  $ cmp <scratch>/board.md.intended agent-factory/contracts/board.md
  (no output — identical)

  $ git diff --exit-code -- agent-factory/
  exit=0 relative to this plan's OWN committed contract edit (commit recorded in the SUMMARY).
  The only difference from HEAD 069dcd9c is this plan's intended edit, described in B7; no probe
  left a byte behind. Measured a second way, with the edit temporarily reverted to HEAD and
  restored: `check:diff-disposition` reported 78 findings over 39 elements both with and without
  it, so the contract edit changes nothing in the LANG-03 watched corpus (board.md is not in it).


B7. THE ONE GENUINE DISAGREEMENT THE COMPARISON FOUND, AND WHICH DIRECTION WAS WRONG
-------------------------------------------------------------------------------------

THE CONTRACT WAS WRONG; the code was right.

The prose that introduces the table said `row-without-file` "covers THREE different facts". Commit
`dba9d72a` added a FOURTH conflict-raising row to the table directly beneath that sentence and left
the count untouched. A normative document (D-04) that miscounts its own table is the same class of
defect as a row stating the wrong consequence — a reader adjudicating a disagreement is told there
are three answers and finds four.

Corrected to "four", in the same commit as the assertion that found it, and the count is now
DERIVED: the suite counts the conflict-raising rows in the parsed table and requires the prose to
name that number in words. Discrimination shown in B5.

The five table ROWS themselves were already in agreement with the code — `dba9d72a`'s row edit was
correct, and this comparison confirms it mechanically rather than by eye for the first time.


B8. THE FULL-SUITE RESULT FOR THIS PLAN
-----------------------------------------

  $ npx vitest run --exclude '**/scripts/e2e/**'
  Test Files  75 passed (75)
       Tests  5151 passed | 2 skipped (5153)
  exit=0

  Above the 5137 floor `32-REVIEW-FIX.md` recorded at HEAD on 2026-09-16 (+14: 2 in
  board-tracer.test.ts, 12 in board-model.test.ts). No test file reported as failed.

  `npm test` was NOT run. The live claude-CLI end-to-end lane stays `UNKNOWN - verify`, as in every
  prior round of this phase.

  OTHER GATES, ALL MEASURED ON THIS TREE:
    npm run build                                        exit=0
    npm run typecheck                                    exit=0
    npm run check:build-parity                           exit=0  ("no tracked build output moved")
    npm run freshness                                    exit=0  (65/65 committed .js fresh)
    VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js
                                                         exit=0  ALL CHECKS PASSED, 0 FAIL lines
    npm run check:nul-bytes                              exit=0  ALL CHECKS PASSED
    npm run check:dashboard-readonly                     exit=0  175 passed
    npm run check:public-docs                            exit=0
    npm run check:banned-claims                          exit=0
    npm run check:claim-anchors                          exit=0
    npm run check:imperative-lexicon                     exit=0
    npm run check:audit-register                         exit=0
    npm run check:residual-citations                     exit=0
    npm run check:diff-disposition                       exit=1  PRE-EXISTING, see below

  TWO VERIFICATION-INSTRUMENT CORRECTIONS, stated rather than smoothed over:

  (1) The plan's verification says `node scripts/validate-agent-factory.js` — which exits 1 on any
      tree, with `VALIDATE_KIT_ROOT is unset — refusing to default the kit root to '.' (C3)`. That
      is the validator's own no-false-green guard, and `.github/workflows/ci.yml:517` invokes it as
      `VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js`. The gate is green under its
      documented invocation; the plan's line omitted the variable.

  (2) The plan's verification says `check:dashboard-readonly` is "exit 0, 171 passed". It is 175.
      The 171 is `32-VERIFICATION.md`'s round-3 measurement at HEAD `670f1b3c`; the CR-01 fix
      (`d276f4e3`, after that verification) added a git-derived two-sided parser oracle to
      `scripts/board-readonly.test.ts`, which is where the four extra cases come from. This plan
      touched neither that file nor any file in the dashboard import closure, so 175 is the
      pre-plan count, not a movement.

  AND ONE PRE-EXISTING GATE FAILURE, CARRIED NOT ABSORBED:
      `npm run check:diff-disposition` exits 1 with 78 findings over 39 elements — every one in
      `agent-factory/roles/` and `agent-factory/workflows/` files this plan did not touch, carried
      since before round 1 and already recorded in this phase's `deferred-items.md`. Measured on
      this tree WITH and WITHOUT this plan's contract edit: 78 both ways. It is not in this plan's
      `<verification>` list and is not this plan's to close.


---
Written by plan 32-38, Tasks 2 and 3. Companion artifact: `32-38-RED-baseline.txt`.

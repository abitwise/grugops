---
phase: 32-board-projector-cli-dashboard
plan: 21
type: execute
wave: 7
gap_closure: true
depends_on: ["32-20"]
files_modified:
  - scripts/validate.test.ts
  - scripts/board-read.test.ts
  - scripts/check-foundation-guards.test.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-06]
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The single-ticket-frontmatter-authority proof catches a second reader written in an ordinary alternative spelling — a pattern built from a string, a parameter with no type annotation, a hand-rolled scan — and each of those spellings is a discrimination row the census is shown to go red on."
    - "The read-target routing census inspects module-level function expressions and arrow functions as well as function declarations, and resolves a member-access callee to its name, so a read primitive reached by either spelling is measured rather than skipped."
    - "Every `check:*` script in `package.json` is classified into exactly one named class with a reachability proof for its class; the classes and their sizes are asserted, so no script is silently skipped by a pattern that did not match."
  artifacts:
    - "scripts/validate.test.ts — the capability-based ticket-reader census, its named exemptions, and at least three discrimination rows"
    - "scripts/board-read.test.ts — the widened routing census and its discrimination plant"
    - "scripts/check-foundation-guards.test.ts — the total classification of `check:*` scripts and the per-class reachability assertions"
  key_links:
    - "the key spellings plus a text-scanning primitive -> the carrier decision: the census asks about the capability, and a file that is not a carrier is exempt by a named reason rather than by falling outside a syntax shape"
    - "the routing census's function universe -> its unvouched list: a read site the universe does not contain is a read site the census never measures"
    - "each check:* class -> the artifact that proves it runs: a gate module named in the workflow, or a test file the suite the workflow runs contains"
  deferred: []
---

<objective>
Close the three findings where a DERIVED predicate is correct and its INPUT is narrower than the claim
it carries. This is the recurring shape across the whole of round 1: a list was converted to a
derivation, and the derivation's universe was left in a narrow syntactic form.

Gap mapping for this plan:

| Finding | Source | What is wrong | Task |
|---------|--------|---------------|------|
| WR-01 | `32-REVIEW.md`, corroborated in `32-VERIFICATION.md` as an advisory with its own reproduction | The census's first arm fires only on a regular-expression literal and its second only on a function whose parameter carries a literal string type annotation. The deleted reader, rewritten with a pattern built from a string, is invisible — measured, zero carriers — while the count still reports one authority. The discrimination case plants the deleted reader byte for byte, so it proves the derivation catches that spelling and not that capability. | Task 1 |
| WR-10 | `32-REVIEW.md` | The routing census collects only function declarations and resolves only bare-identifier callees, so a module-level arrow reading a joined path is never entered into the universe and a namespace-style call is invisible — while the inspected count still prints as healthy, so the premise assertion passes over an unmeasured site. | Task 2 |
| F-01 | `32-14-ADVERSARIAL-REVIEW.md`, left open for this round | The `check:*` reachability case extracts a gate-module path with one pattern and SKIPS any script the pattern does not match. Two scripts are skipped; the comment beside the skip describes one of them, and the other is the DASH-06 safety control. The gate is reached — the suite runs it — but the mechanical proof that it is reached does not exist, which is the exact shape this repository built the foundation guards to prevent. | Task 3 |

Purpose: three proofs that each say more than they check. None is a live bypass today; all three are
the mechanism by which the next live bypass would go unseen.

Output: censuses whose subject is the capability, whose universe is stated, and whose skips are
classified rather than silent.

This plan runs after 32-20 because the DASH-06 gate's own reachability is one of its subjects, and
because `scripts/board-read.test.ts` must be measured against the reader the runtime plans left.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` and no `## Prohibitions`
section to satisfy.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-14-ADVERSARIAL-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-12-SUMMARY.md
</context>

<assumption_delta_decision>
The assumption-delta detector fired on the phrase "the validator's second ticket-frontmatter reader
deleted" (kind: pluralization, term: "second").

- **Noun:** the ticket-frontmatter authority, `parseTicketDocument` in `scripts/board-model.ts`.
- **Decision:** `promote`. The single authority is primary. The reader plan 32-12 deleted from
  `scripts/validate-agent-factory.ts` is GONE, not demoted to a secondary role, and this plan's
  direction is consolidation — proving the capability exists in one place — never the introduction of
  a second case.
- **Rationale:** the census counts CARRIERS of a capability; a count of one means one authority, and
  everything this plan does widens what counts as a carrier so the one stays honest.

Advisory only. This block blocks nothing.
</assumption_delta_decision>

## Artifacts this phase produces

- In `scripts/validate.test.ts`: a rewritten `findTicketReaders` whose carrier decision is "this file
  names both ticket key spellings AND reaches a text-scanning primitive", a named-reason exemption set
  in the shape `STEM_FALSE_POSITIVES` already uses, and at least three discrimination rows — a pattern
  built from a string, an untyped-parameter reader, and a hand-rolled scan with the key names
  assembled by concatenation.
- In `scripts/board-read.test.ts`: a widened `collectFunctions` covering function-like nodes with their
  owning variable name, a `calleeName` resolving a property-access callee to its rightmost name, and a
  discrimination plant with a module-level arrow reading a joined path.
- In `scripts/check-foundation-guards.test.ts`: `CHECK_SCRIPT_CLASSES` — the total classification of
  every `check:*` script, each class with its own reachability proof — plus per-class counts and an
  assertion that the classes partition the script set with no remainder.
- No production-code change, no committed `.js` movement: this plan edits three `.test.ts` files.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The one-authority proof asks about the capability, not about the spelling</name>

  <read_first>
    - `scripts/validate.test.ts` lines 1480-1580 — the census block, `TICKET_TEXT_KEYS`, `anchoredFor`,
      `quotedFor`, `findTicketReaders` and its two arms
    - `scripts/validate.test.ts` lines 1580-1660 — the glob, the tracked-file floor, the two-sided
      count, and the byte-for-byte discrimination case
    - `scripts/board-model.ts` — `TICKET_KEYS`, the closed key set the census derives its spellings
      from, and `parseTicketDocument`, the one carrier the count expects
    - `scripts/board-readonly.test.ts` lines 530-560 — `STEM_FALSE_POSITIVES`, the named-reason
      exemption idiom to copy
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` WR-01 — the three measured
      rewrites and their zero-carrier results
  </read_first>

  <files>scripts/validate.test.ts</files>

  <action>
RED FIRST, AS ROWS. Add the review's three rewrites as discrimination rows before changing the
derivation: a module-scope pattern built from a string, a reader whose text parameter carries no type
annotation, and a scan splitting on newlines with the key names assembled by concatenation. Assert each
is detected as a carrier. All three fail today with zero carriers; record the measured numbers.

THEN CHANGE THE SUBJECT OF THE QUESTION. A carrier becomes: a file that names BOTH ticket key
spellings — as a string literal, a template literal, a regular-expression literal, or as concatenated
pieces that join to the spelling — AND reaches a text-scanning primitive, where the primitives are
named in one place: the match, the exec, the pattern constructor, the split, and the index lookup.
The question stops being "what shape is this code" and becomes "does this file read those two keys out
of text".

EXEMPT BY NAMED REASON, NOT BY SYNTAX. Widening the subject will name files that are not authorities —
the grammar itself, this test file, any file that merely mentions the keys. Add an exemption set where
each entry carries the file and the one-line reason it is not a second authority, in the register the
stem exclusions next door already use, and assert every exemption is actually matched by the widened
derivation, so an exemption that stopped being needed is a red rather than a permanent hole.

KEEP BOTH FLOORS AND ADD ONE. The glob-versus-git floor stays exactly as it is. The count stays
two-sided at one, and the carrier stays the grammar module. Add the third floor the review implies: the
derivation must detect the ORIGINAL deleted reader as well — keep that case — so the widened subject is
shown to be a superset of the old one rather than a different question.

STATE WHAT BOUNDS THE INPUT. Write into the census docblock which files it opens, which node kinds it
inspects, what it does with a key spelling it cannot resolve statically, and what it cannot see at all.
This is the sentence whose absence is the finding; every stated boundary gets a row.
  </action>

  <behavior>
    - Given the pattern-built-from-a-string rewrite, the census reports at least one carrier.
    - Given the untyped-parameter rewrite, the census reports at least one carrier.
    - Given the concatenated-key scan, the census reports at least one carrier.
    - Given the byte-for-byte deleted reader, the census still reports it.
    - Given the live `scripts/` tree, the carrier count is one and the carrier is the grammar module.
    - Given the exemption set, every entry is matched by the widened derivation.
  </behavior>

  <acceptance_criteria>
    - `findTicketReaders` decides on the pair "both key spellings present" and "a text-scanning primitive reached", with the primitive names declared in one place.
    - At least three new discrimination rows exist, each measured red before the change and green after.
    - Every exemption carries a file and a reason, and an unused exemption fails a case.
    - The count stays two-sided at one and the carrier stays the grammar module.
    - The docblock states the census's file set, node kinds, and blind spots, and each stated boundary has a row.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts</automated>
    <fails_when>non-zero exit, or any case reports failed, or the carrier-count case is reported as skipped</fails_when>
    <automated>node scripts/validate-agent-factory.js</automated>
    <fails_when>non-zero exit — the validator itself must be unaffected by a change to the census that measures it</fails_when>
  </verify>

  <done>The one-authority proof detects the capability in four spellings and exempts by reason.</done>

  <reversibility rating="reversible">A test-file derivation change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The routing census's universe covers the shapes the module can legally use</name>

  <read_first>
    - `scripts/board-read.test.ts` lines 1925-1990 — `routingCensus`, `collectFunctions`, `calleeName`,
      `callsIn` and the producer derivation
    - `scripts/board-read.test.ts` lines 1990-2100 — the path-helper fixpoint, the `inspected` counter,
      the `unvouched` list and `PATH_AUTHORITIES` with its two-sided pin
    - `scripts/board-dashboard.test.ts` lines 1108-1124 — `enclosingFunctionName`, which already names an
      arrow by its owning variable; the same technique this task reuses rather than reinvents
    - `scripts/board-read.ts` — the reader functions and the read primitives, to confirm which shapes
      exist today and which are merely possible
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` WR-10 — the two narrow inputs
      and why the healthy inspected count hides them
  </read_first>

  <files>scripts/board-read.test.ts</files>

  <action>
RED FIRST. Plant a module-level arrow that reads a joined path built from a bare identifier, and a call
to a read primitive reached through a namespace-style member access, and assert each appears in
`unvouched`. Both fail today: the first is never entered into the universe, the second resolves to an
empty callee name and is skipped.

THEN WIDEN THE UNIVERSE, AND NAME WHAT ENTERS IT. `collectFunctions` takes every function-like node,
naming an arrow or function expression by its owning variable declaration or property assignment, using
the same technique the dashboard census already uses; anything nameless gets a stable marker so a
finding still names a place. `calleeName` resolves a property-access callee to its rightmost name so a
namespace-style call is inspected rather than skipped.

ASSERT THE UNIVERSE, NOT ONLY THE FINDINGS. Add a case pinning the collected function COUNT and the
collected NAMES against the module as it stands, with a decision-shaped message: a reader added or
renamed moves this number, which is the point. `inspected` staying healthy while a specific site is
unmeasured is the failure this finding is about, and only a pinned universe catches it.

KEEP THE FIXPOINT HONEST. The path-helper fixpoint admits a function that hands its own parameter to a
read primitive. With arrows now in the universe, re-derive `pathHelpers` and assert the derived set
against its committed membership, so the widening is shown not to have quietly admitted a new helper.

STATE THE BOUNDARY. Write into the docblock which node kinds are in the universe, how a callee is
resolved, and what the census does with a callee it cannot resolve — and make that last case collect
rather than skip, because a skip is how this finding exists.
  </action>

  <behavior>
    - Given a module-level arrow reading a joined path from an unvouched identifier, the census reports it in `unvouched`.
    - Given a read primitive reached through a member-access callee, the census inspects it.
    - Given a callee the pass cannot resolve, the census collects it rather than skipping it.
    - Given the live `scripts/board-read.ts`, `unvouched` is empty, `PATH_AUTHORITIES` is unchanged, and the derived path-helper set matches its committed membership.
    - Given the live module, the pinned function universe matches the asserted names and count.
  </behavior>

  <acceptance_criteria>
    - `collectFunctions` covers function-like nodes and names them by their owning declaration where one exists.
    - `calleeName` resolves a property-access callee to its rightmost name.
    - An unresolvable callee is collected, not skipped, and a case proves it.
    - The function universe is pinned by names and count with a decision-shaped message.
    - `PATH_AUTHORITIES` and the derived path-helper set are unchanged from their committed values.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-read.test.ts</automated>
    <fails_when>non-zero exit, or any case reports failed, or the pinned-universe case is reported as skipped</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or tsc names scripts/board-read.test.ts in an error line</fails_when>
  </verify>

  <done>The routing census measures the shapes the module can use, and its universe is pinned.</done>

  <reversibility rating="reversible">A test-file derivation change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Every check script is classified, and each class carries its own reachability proof</name>

  <read_first>
    - `scripts/check-foundation-guards.test.ts` around line 12009 — the case extracting a gate-module
      path and the skip beside it, with the comment describing one of the two scripts it skips
    - `scripts/check-foundation-guards.test.ts` lines 11975-12008 — the neighbouring case walking the
      gate modules, its `CI_EXEMPT` map, and the exemption contract it enforces (a reason, plus still
      reached by something)
    - `package.json` — the `check:*` block, in particular the parity script and the dashboard-readonly
      script and how each is spelled
    - `.github/workflows/ci.yml` around the suite invocation — how the suite is run and with what
      exclusion
    - `.planning/phases/32-board-projector-cli-dashboard/32-14-ADVERSARIAL-REVIEW.md` F-01 — the
      reproduction, the two skips it measured, and why only one of them is described by the comment
  </read_first>

  <files>scripts/check-foundation-guards.test.ts</files>

  <action>
RED FIRST. Add a case asserting that the number of `check:*` scripts the reachability derivation
CLASSIFIES equals the number that exist. It fails today by the two measured skips, and the failure
message names them.

THEN CLASSIFY TOTALLY. Replace the single pattern plus skip with a classification into named classes,
each with its own reachability proof:
  - a script running a gate module — the workflow must name that module, exactly as today;
  - a script running a test file through the test runner — the file must exist, and the workflow's
    suite invocation must be one that includes it, proved by the exclusion pattern the workflow uses
    rather than by assumption;
  - a script running the toolchain rather than a gate — named, with the reason, and still required to
    be invoked by the workflow under its own spelling.
Every `check:*` script lands in exactly one class. A script matching none is a failure naming it, never
a `continue`.

PIN THE CLASS SIZES. Assert each class's size and the total, with a decision-shaped message: a new
check script belongs to a class somebody chose. This is the difference between a classification and a
pattern that happens to match.

PROVE THE CLASSIFIER CAN FAIL. Add a discrimination case running the classifier over a constructed
script map containing one script of each class plus one that matches nothing, and assert the
unclassified one is reported. A classifier that never reports anything is the skip in another shape.

STATE THE RELATIONSHIP FOR THE DASHBOARD GATE EXPLICITLY. The DASH-06 control runs a test file; assert
by name that the file exists, that it is not excluded by the workflow's suite exclusion pattern, and
that the npm script and the file agree about which file the gate is. That is the mechanical proof the
round-1 review found missing, and it is the one the safety control needs most.
  </action>

  <behavior>
    - Given the live `package.json`, every `check:*` script is classified and the per-class sizes match the pinned numbers.
    - Given a constructed script map with an unclassifiable entry, the classifier reports it.
    - Given the dashboard-readonly script, the named test file exists and is not excluded by the workflow's suite exclusion.
    - Given a gate-module script whose module the workflow does not name, the case fails naming it.
    - Given the toolchain-class script, its reason is present and the workflow invokes it under its own spelling.
  </behavior>

  <acceptance_criteria>
    - No `continue` remains in the reachability case for an unmatched script; an unmatched script is a failure naming it.
    - The classes and their sizes are asserted, and the total equals the number of `check:*` scripts.
    - The discrimination case over a constructed map reports the unclassifiable entry.
    - The dashboard-readonly assertion names the test file, checks its existence, and checks it against the workflow's exclusion pattern.
    - The neighbouring gate-module case and its exemption contract are unchanged.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts</automated>
    <fails_when>non-zero exit, or any case reports failed, or the classification-total case is reported as skipped</fails_when>
    <automated>node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit, or the output's last line is not the all-checks-passed line</fails_when>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit, or the run reports 0 passed — the script this case now proves reachable must itself still run</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the file count is below 74, or the passed count is below the 4811 recorded by the round-1 adversarial review</fails_when>
  </verify>

  <done>Every check script has a class and every class has a proof; the DASH-06 control's reachability is mechanical.</done>

  <reversibility rating="reversible">A test-file classification replacing a pattern and a skip.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a future commit → the proofs that guard this phase | Each census is the only mechanism deciding a claim; a census with a narrow universe admits the next change silently. |
| the workflow's invocation set → the gates that actually run | A gate invoked by nothing passes by not running, which this repository has already shipped once. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-21-01 | Tampering | the one-authority census's syntactic universe | medium | mitigate | Measured: a semantically identical second reader is invisible in three ordinary spellings while the count still claims one authority. Task 1 changes the subject to the capability and adds a row per spelling. |
| T-32-21-02 | Tampering | the routing census's function universe | medium | mitigate | A read site outside the universe is never measured while the inspected count looks healthy. Task 2 widens the universe, pins it, and collects unresolvable callees instead of skipping them. |
| T-32-21-03 | Repudiation | the `check:*` reachability proof | high | mitigate | The DASH-06 safety control is skipped by the derivation that exists to prove gates run. Task 3 classifies totally, pins the class sizes, and proves the dashboard gate's file is inside the suite the workflow runs. |
| T-32-21-04 | Denial of Service | over-wide carrier detection | low | mitigate | Widening the carrier subject will name files that are not authorities; the exemption set carries a reason per entry and an unused exemption fails a case, so the hole cannot outlive its reason. |
| T-32-21-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan; `package.json` is read, never written. |
</threat_model>

<verification>
1. `npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts scripts/board-read.test.ts scripts/check-foundation-guards.test.ts`.
2. `node scripts/validate-agent-factory.js && node scripts/check-foundation-guards.js`.
3. `npm run check:dashboard-readonly` — the control whose reachability Task 3 proves.
4. `npm run typecheck` and `npm run build && npm run check:build-parity` — this plan moves no build output.
5. `npx vitest run --exclude '**/scripts/e2e/**'` — the whole suite minus the live claude-CLI e2e lane. Never plain `npm test`.
</verification>

<success_criteria>
- The ticket-reader census detects the capability in four spellings, including the three measured rewrites.
- The routing census measures module-level arrows and member-access callees, with its universe pinned.
- Every `check:*` script is classified with a per-class reachability proof and pinned class sizes.
- REQUIREMENTS.md and the ROADMAP phase status are NOT flipped to Complete by this plan.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-21-SUMMARY.md` when done.
</output>

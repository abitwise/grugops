---
phase: 32-board-projector-cli-dashboard
plan: 37
type: execute
wave: 7
gap_closure: true
depends_on: ["32-36"]
files_modified:
  - .planning/phases/32-board-projector-cli-dashboard/32-37-ADVERSARIAL-REVIEW.md
  - .planning/phases/32-board-projector-cli-dashboard/deferred-items.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07, DASH-08]
estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The harness asserts its own premise before it reports anything: the build is a faithful build of its sources and the committed outputs match a rebuild, so every measurement below is about the artifact a host runs rather than about a `.ts` nobody ships."
    - "Every recorded reproduction from `32-VERIFICATION.md`, `32-REVIEW.md`, `32-23-ADVERSARIAL-REVIEW.md` and `32-24-RED-baseline.txt` — including the ones those documents record as CLOSED, and including the three baseline rows that were already refused before this round — is re-run against the rebuilt committed `.js`, and each carries a measured result rather than a description."
    - "Every refusal branch this round added is asked two questions and both answers are written down: how is it REACHED, and what does the arm NEXT to it do with the same input."
    - "The round ledger has one row per inventory item, the inventory count and the row count are both stated out loud, and they are equal — an item may be OPEN but never ABSENT (DASH-01 through DASH-08)."
    - "The probe-report arithmetic is stated and balances: the count of edge items authored into plan must-haves plus the count explicitly flagged as unaddressed equals the count the report produced."
    - "Both representations of the residual ledger in `.planning/WINDOWS.md` agree after this round's rows are appended, asserted by comparing every row identifier in one against the other and printing three numbers."
    - { statement: "The live claude-CLI end-to-end lane was not run in this round and its state is UNKNOWN - verify, as in every prior round of this phase and as ledger row 183 already records", verification: backstop }
  prohibitions:
    - statement: "This plan decides no requirement checkbox, no phase status and no verification verdict — the verifier does, from this evidence, in a separate pass."
      status: resolved
      verification: judgment
    - statement: "Never fix a source file here; a closure found at the end of a round, in a plan that owns no source, is how the previous round's first finding was created."
      status: resolved
      verification: judgment
    - statement: "Never absorb the carried `check:diff-disposition` failure into this round; re-measure it and carry it with the measurement."
      status: resolved
      verification: judgment
    - statement: "Never record a reproduction as closed on the strength of reading a diff; re-run the original finding's own probe."
      status: resolved
      verification: judgment
  artifacts:
    - "32-37-ADVERSARIAL-REVIEW.md — premise, re-run transcripts, reachability and sibling-arm answers per new branch, the neighbour-variation pass, the gate sweep, the round ledger and the probe arithmetic"
    - ".planning/phases/32-board-projector-cli-dashboard/deferred-items.md — the carried items re-measured and left carried"
    - ".planning/WINDOWS.md — this round's residual rows appended and both representations reconciled"
  key_links:
    - "the premise check -> every transcript below it: a measurement over an artifact that is not a build of its sources measures nothing"
    - "the 13-item inventory -> the ledger's row count: both numbers stated, both equal, an OPEN row allowed and an ABSENT row not"
    - "each new refusal branch -> its reachability paragraph and its sibling-arm row: the two questions this project's history says decide whether a round converged"
    - "this report -> the verifier: the report supplies evidence and changes no status line"
  deferred: []
---

<objective>
End gap-closure round 3 the way rounds 1 and 2 ended: with an adversarial self-review that re-measures
everything, asks the two questions this project's recorded history says decide whether a round
converged, and keeps a ledger asserted total against the round's own inventory.

**Round position: this is round 3 of a hard cap of 4.** One round remains. That budget is what makes
recording an open finding safe rather than a failure, and it is why every finding this plan records
must carry a reproduction somebody else can run rather than a description.

**This plan does not decide phase completion.** It changes neither `.planning/REQUIREMENTS.md` nor the
Phase 32 status line nor any DASH-0x checkbox. This repository has a recorded incident of a roadmap
update flipping a phase to Complete before verification ran, which then had to be reverted by hand.

The round's inventory, taken from `32-REVIEW.md`'s own frontmatter totals (3 critical, 8 warning,
2 info) which the verifier's three gaps, its `advisory:` block and its `reason_advisory` field all
map onto:

```
3 + 8 + 2 = 13 inventory items
```

plus two CARRIED items deliberately outside that count: the pre-existing `check:diff-disposition`
failure on five Phase-31 workflow documents, and the live claude-CLI end-to-end lane whose state is
`UNKNOWN - verify` (ledger row 183).

Canon-referral breadcrumb: no canon security item is minted here; this plan runs measurements and
writes reports.

There is no `SPEC.md` for this phase, so there is no `## Edge Coverage` and no `## Prohibitions`
section to lift from; the `## Flagged assumptions` section below carries the round's probe arithmetic
in full, and Task 3 asserts it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/32-board-projector-cli-dashboard/32-CONTEXT.md
@.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md
@.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-14-ADVERSARIAL-REVIEW.md
@.planning/phases/32-board-projector-cli-dashboard/32-24-RED-baseline.txt
@.planning/phases/32-board-projector-cli-dashboard/deferred-items.md
</context>

## Flagged assumptions — probe-report items this round does NOT address

The orchestrator's deterministic edge probe produced **12 items** over DASH-01 through DASH-08, every
one `unresolved` at the time it was produced. This is gap-closure round 3, so the phase's original
plans `32-01` through `32-08` already carry the phase's edge coverage; this round re-derives only the
categories touching the failed truths and the two censuses. The arithmetic, stated so no item is
silently dropped:

**Authored (7)** — lifted into `must_haves` as acceptance criteria by the plan named. Five are plain
`truths` strings (verification: explicit); two are structured `{ statement, verification: backstop }`
markers, which abstain to `human_needed` at verify time rather than passing silently:

| Requirement | Category | Verification | Authored in |
|---|---|---|---|
| DASH-01 | boundary | explicit | `32-36` — the carrier count is a case on both sides of one: zero carriers reds as a vanished authority, two carriers reds as a second authority |
| DASH-01 | adjacency | explicit | `32-36` — two scanned files whose static texts differ only in key spelling count as two carriers rather than merging into one |
| DASH-01 | empty | explicit | `32-36` — an empty scanned set reds the non-vacuity premise; a one-file set is still asked the same question |
| DASH-01 | ordering | explicit | `32-36` — the carrier list is derived from a sorted file set, so two runs over one tree print the same list |
| DASH-01 | precision | **backstop** | `32-36` — every pinned cardinality is a small integer obtained by counting array members; no rounding, tie-break or floating-point step exists in either derivation |
| DASH-04 | concurrency | explicit | `32-34` — two containment refusals in one read, one an ancestor of the other, produce the same armed set as either ordering applied separately; a refusal arriving while a directory is already armed closes the handle |
| DASH-08 | concurrency | **backstop** | `32-31` — a concurrent or interrupted guard run cannot produce a green result over a tree whose committed `.js` does not match its `.ts`; build parity and freshness bound that, and both are separate commands in the plan's own verify |

**Flagged as unaddressed (5)** — every `unclassified` row. Per the probe protocol an `unclassified`
row stays `unresolved` and is never auto-resolved; each is recorded here as an explicit assumption of
this round rather than an oversight:

| Requirement | Category | Why this round does not derive a predicate for it |
|---|---|---|
| DASH-02 | unclassified | The grammar's edge behaviour is carried by the seven-axis parse oracle and the 141-row corpus in `32-04`; no finding in this round's 13-item inventory reaches the grammar, and the probe produced no shape to classify. |
| DASH-03 | unclassified | The join's edge behaviour is carried by `32-05` and `32-15`, and this round's DASH-03 work (`32-33`) is a named defect with its own reproduction rather than an unclassified edge. |
| DASH-05 | unclassified | The degradation semantics are carried by `32-07`, `32-09`, `32-10` and `32-17`; this round's DASH-05 work is the watch-arm granularity defect, which is authored under DASH-04 concurrency above. |
| DASH-06 | unclassified | The read-only guard's boundary is authored under DASH-08 concurrency above; the probe produced no additional classifiable shape for DASH-06 itself. |
| DASH-07 | unclassified | The non-TTY, single-frame and document modes over an empty tree are carried by `32-07` and `32-13`; round 3 touches only the control-character behaviour of the document channel, which is a named defect with its own reproduction. |

```
authored 7 + flagged 5 = 12 = taken 12
```

**Prohibitions.** The phase has no `SPEC.md`, so the prohibition section was absent and the recall
pass ran in-prompt during planning. Every kept prohibition was authored **descriptor-less** into the
owning plan's `must_haves.prohibitions:` block, so each disposes flagged-unverified rather than
claiming a wired check that nobody can verify. No kept prohibition was dropped, and the canon
security items surfaced by the recall — path traversal, prototype pollution, injection, output
encoding — were referred to `/gsd-secure-phase` with a one-line breadcrumb in each plan's objective
rather than minted. Task 3 restates and asserts this arithmetic alongside the edge arithmetic.

## Artifacts this phase produces

- `32-37-ADVERSARIAL-REVIEW.md` with, in order: the premise assertion; the re-run of every recorded
  reproduction from all four prior documents; the behavioural spot-check table re-run verbatim
  including the rows that passed; a reachability answer and a sibling-arm probe per new refusal
  branch; the neighbour-variation pass; the findings, each OPEN with a reproduction; the gate sweep
  derived from the manifest rather than recalled; the round ledger asserted total; the probe-item
  arithmetic for both edges and prohibitions; the created-versus-inherited ratio stated as a number;
  and a closing section naming what the document does NOT say.
- Re-measured entries in `deferred-items.md` for both carried items.
- This round's residual rows appended to `.planning/WINDOWS.md` after row 191, through the tool that
  maintains both of its representations, with the reconciliation asserted.

<tasks>

<task type="auto">
  <name>Task 1: Assert the harness's own premise, then re-run every recorded reproduction</name>

  <read_first>
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` — the "Independent
      Reproduction Transcripts" section and the "Behavioral Spot-Checks" table, every row of which is
      re-run here
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` — the three critical issues,
      the eight warnings, the two infos, and the prior-findings disposition table whose closures are
      re-checked rather than assumed
    - `.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md` §§ 0-2 — the
      premise pattern, the re-run pattern and the spot-check pattern this task copies
    - `.planning/phases/32-board-projector-cli-dashboard/32-24-RED-baseline.txt` §§ 2 and 4 — the
      six-row result table and the ten-spelling partition table, all of which are re-run here
      including the three rows that were ALREADY refused before this round began
    - `.planning/phases/32-board-projector-cli-dashboard/32-31-GREEN-proof.txt` through
      `32-36-GREEN-proof.txt` — this round's own proofs, which are LEADS, not evidence: every row is
      re-measured here independently
    - `.planning/phases/32-board-projector-cli-dashboard/32-14-ADVERSARIAL-REVIEW.md` § 9 — what a
      re-verification should check first, and why the order matters
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-37-ADVERSARIAL-REVIEW.md</files>

  <action>
ASSERT THE PREMISE BEFORE ANYTHING ELSE, AND PRINT IT. Run the build, the type check, the
build-parity check and the freshness check, and record their outputs. If the committed outputs are
not a faithful build of their sources, every measurement below is about an artifact nobody ships and
the document must say so at the top rather than in a footnote. This project has six recorded
instances of a verification harness producing a false result because its own premise was never
asserted.

RE-RUN EVERY RECORDED REPRODUCTION, INCLUDING THE CLOSED ONES AND THE ALREADY-REFUSED ONES. Build the
list from all four prior documents — the verifier's three gaps plus its advisory and
`reason_advisory`; the reviewer's three criticals, eight warnings and two infos; the round-2
self-review's five findings; and every row of `32-24-RED-baseline.txt` § 2 — and re-run each against
the REBUILT committed `.js`, on disposable copies outside the repository root. A review that only
re-checks failures cannot find a regression, so the rows recorded as CLOSED are re-run too: the
namespace-destructure bypass, the `node:v8` and runtime-assembled-identity bypasses, the
permission-denied and torn-byte read paths, the symlink escape, the grammar-refusal sentence, the
upper-range control characters in the document channel, the builtin-identity allow-list, the
byte-order-mark and tab refusals, the walk bound's sort, the duplicate identifier, the routing
census's universe, and the queue reader's named skips.

RE-RUN THE THREE BASELINE ROWS THAT WERE ALREADY REFUSED, AND SAY WHICH AUTHORITY REFUSES THEM NOW.
`file://`, the Windows drive-letter path and the dynamic absolute import were refused before this
round began, two of them by the builtin allow-list. `32-31` moved two of those refusals to a
different authority. Record, per spelling, which predicate reds it on THIS tree and whether the
acquisitions PREMISE case is among the failures — a relocation that landed on a weaker predicate is
a regression this round created, and it is invisible to any count of refusals.

RE-RUN THE BEHAVIOURAL SPOT-CHECK TABLE VERBATIM. `32-VERIFICATION.md` carries a table of spot-checks
with a recorded result each. Re-run every row and record the new result beside the old one, including
the six that passed. A row whose result changed in either direction is the finding.

RESTORE AND PROVE CLEAN AFTER EVERY PROBE that touches a tracked file, and record the clean check in
the transcript rather than asserting it once at the end.

WRITE THE SECTIONS AS MEASUREMENT, NOT AS NARRATIVE. Each row carries the command, the measured
output and the verdict. Where a probe cannot be run, record `UNKNOWN - verify` and the reason; never
fabricate a result, and never record a closure on the strength of reading a diff.
  </action>

  <acceptance_criteria>
    - `32-37-ADVERSARIAL-REVIEW.md` opens with the premise check and its recorded outputs.
    - The document carries one row per recorded reproduction from all four prior documents, each with the command, the measured output and a verdict.
    - The document carries a six-row table over `32-24-RED-baseline.txt` § 2's spellings, naming for each which predicate refuses it on this tree and whether the acquisitions PREMISE case is among the failures.
    - The document re-runs every row of the verifier's behavioural spot-check table and prints the old and new results side by side.
    - Every probe touching a tracked file records a clean working-tree check after restoration.
    - Any unrunnable probe is recorded `UNKNOWN - verify` with the reason.
  </acceptance_criteria>

  <verify>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or the freshness output naming any committed .js among the files that do not match a rebuild of their sources — the premise fails and the report must say so before any transcript</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or a passed-case count below the 4974 recorded in 32-VERIFICATION.md — the round lost cases somewhere between plan 32-31 and here</fails_when>
    <automated>git diff --exit-code -- scripts/ agent-factory/ docs/</automated>
    <fails_when>non-zero exit — a probe was left in a tracked file</fails_when>
  </verify>

  <done>The premise is asserted and printed, and every recorded reproduction from every prior document — including the already-refused rows whose authority this round moved — has a re-measured verdict against the artifact a host runs.</done>

  <reversibility rating="reversible">Measurements written to a report; this task owns no source file.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Ask the two questions per new refusal branch, and run the neighbour-variation pass</name>

  <read_first>
    - `scripts/js-import-closure.ts`, `scripts/board-readonly.test.ts`, `scripts/board-model.ts`,
      `scripts/board-read.ts`, `scripts/board-dashboard.ts`, `scripts/validate.test.ts` and
      `scripts/check-foundation-guards.test.ts` — every refusal branch this round added, which is the
      subject of this task's table
    - `.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md` §§ 3-4 — the
      reachability format and the six neighbour-variation questions, which this task asks again of
      this round's branches
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` "Why the score went
      down" — the three-sentence statement of the failure class this round set out to close, which is
      the hypothesis this task is trying to falsify
    - `.planning/phases/32-board-projector-cli-dashboard/32-24-RED-baseline.txt` § 5 — the three
      statements about what this round's proofs had to be shaped like, each of which is a question
      this pass re-asks of the delivered code
    - `package.json` — the real command set, from which the gate sweep's row set is derived
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-37-ADVERSARIAL-REVIEW.md</files>

  <action>
ENUMERATE THIS ROUND'S NEW REFUSAL BRANCHES FROM THE SOURCE, NOT FROM THE PLANS. Walk the seven
changed files and list every branch that can now refuse, degrade or report where it previously did
not. The list derived from the code is the denominator; a branch a plan promised but the code does
not carry is the first thing this pass should find.

ASK EACH BRANCH TWO QUESTIONS AND WRITE BOTH ANSWERS:

  1. **How is it REACHED?** Which call site evaluates it, on what input, under which command. A
     branch nothing reaches is a branch that cannot refuse, and this project's recorded lesson is
     that asking only what a predicate refuses leaves the positions it is never ASKED at unexamined.
  2. **What does the arm NEXT TO IT do with the same input?** Five consecutive rounds across this
     project's phases were "correct in the arm it was written for and wrong in the arm beside it".

RUN THE NEIGHBOUR-VARIATION PASS, and state the hypothesis it is trying to falsify: that this round's
five canonical-form cutovers did NOT reopen the same class one register over. For each cutover, ask
at minimum:

  - the specifier partition: is there a position a specifier enters that `moduleSpecifiers` does not
    read — a re-export chain, a source the walk reaches at depth, a form `stripNonCode` removed that
    was real code — and what did `CLOSURE_BASELINES` record as changed for the seven production
    callers
  - the member-path position rule: is there a consumption position not in the pinned row set, and
    does the binding map survive an alias created by a shape it does not track
  - the presence derivation: is there a fifth ticket population — a listed entry that is neither
    admitted nor refused — and does the arithmetic totality assertion actually see it
  - the containment granularity: is there a path shape where the ancestor walk and the authority's
    own spelling disagree, and what happens when two refusals arrive in one read
  - the two censuses: what is the input each one's set is ASSEMBLED from, and what bounds it

ASK THE RELOCATION QUESTION OF EVERY NEW PARTITION, because it is the question `32-24-RED-baseline.txt`
§ 5 added to this round's vocabulary: when a plan introduces a new bucket, which EXISTING refusals
does that bucket steal from, and is the new bucket's refusal at least as strong as the one it
replaced? Ask it of the specifier partition, of the presence partition and of the containment set,
and record an answer per stolen refusal rather than one answer per partition.

DERIVE THE GATE SWEEP FROM THE MANIFEST. Read every `check:*` and `freshness:*` command out of
`package.json`, run each, and record the exit code and the headline output. The row set is derived
rather than recalled, because a recalled row set is the set-literal drift class this repository has
already paid for twice.

RECORD EVERY FINDING OPEN, WITH A REPRODUCTION. Nothing is fixed in this plan: this plan owns no
source file, and a closure applied at the end of a round in a plan that does not own the file is
precisely how the previous round's first finding was made. Each finding carries a severity, the file,
whether it was created by this round's own fixes, and a reproduction somebody else can run.

STATE THE CREATED-VERSUS-INHERITED RATIO AS A NUMBER, beside round 1's (1 of 3), round 2's (4 of 5 by
its own self-review, 5 of 8 by the code review) and Phase 31's (8 of 8 for four consecutive rounds).
That ratio is the single number that says whether the canonical-form cutover worked, and it is the
number round 4's decision will be taken against.
  </action>

  <acceptance_criteria>
    - The document carries a branch table derived from the seven changed source files, with a reachability answer and a sibling-arm answer per row.
    - The document carries the neighbour-variation pass with at least the five cutover questions above, each with a measured answer.
    - The document carries a relocation table answering, per stolen refusal, which authority held it before and whether the new authority is at least as strong.
    - The document carries a gate sweep whose row set is derived from the manifest, with an exit code and headline output per row.
    - Every finding is recorded OPEN with a severity, a file, a created-or-inherited attribution and a runnable reproduction.
    - No file under `scripts/`, `agent-factory/` or `docs/` is modified by this task.
    - The created-versus-inherited ratio is stated as a number beside the two prior rounds' and Phase 31's.
  </acceptance_criteria>

  <verify>
    <automated>git diff --exit-code -- scripts/ agent-factory/ docs/ package.json</automated>
    <fails_when>non-zero exit — this plan owns no source file and a fix applied here is the failure mode it exists to avoid</fails_when>
    <automated>npm run check:dashboard-readonly &amp;&amp; node scripts/validate-agent-factory.js &amp;&amp; node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit from any of the three — a gate this round touched is red at the end of the round and the sweep must record which</fails_when>
    <automated>grep -c 'REACHED' .planning/phases/32-board-projector-cli-dashboard/32-37-ADVERSARIAL-REVIEW.md</automated>
    <fails_when>a printed count of 0 — no branch was asked how it is reached, which is half the pass and the half this project's history says decides whether a round converged</fails_when>
  </verify>

  <done>Every new refusal branch has a written reachability answer and a sibling-arm answer, every relocated refusal is answered per stolen refusal, the neighbour-variation hypothesis has been tested rather than assumed, and the ratio is a number.</done>

  <reversibility rating="reversible">Measurements written to a report.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The round ledger asserted total, the carried items re-measured, and both residual representations reconciled</name>

  <read_first>
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` frontmatter `findings:` — the
      3 / 8 / 2 / 13 totals that are this round's inventory
    - `.planning/phases/32-board-projector-cli-dashboard/32-VERIFICATION.md` frontmatter — the
      `gaps:` block, the `advisory:` block and the `reason_advisory` field, each of which maps onto
      one of those 13 rows
    - `.planning/phases/32-board-projector-cli-dashboard/32-23-ADVERSARIAL-REVIEW.md` § 7 — the
      ledger format, the out-loud count assertion and the reconciliation block this task copies
    - `.planning/phases/32-board-projector-cli-dashboard/deferred-items.md` — all carried entries and
      the format they use, including the one `32-35` appended
    - `.planning/WINDOWS.md` — both representations of the residual ledger, and rows 183 through 191,
      which are the carried and round-2 rows this round's rows are appended after
    - This plan's own `## Flagged assumptions` section — the probe arithmetic to restate and assert
  </read_first>

  <files>.planning/phases/32-board-projector-cli-dashboard/32-37-ADVERSARIAL-REVIEW.md, .planning/phases/32-board-projector-cli-dashboard/deferred-items.md, .planning/WINDOWS.md</files>

  <action>
BUILD THE LEDGER WITH ONE ROW PER INVENTORY ITEM AND ASSERT THE TWO COUNTS ALOUD. The inventory is
`32-REVIEW.md`'s own totals — three critical, eight warning, two info — which the verifier's three
gaps, its advisory and its `reason_advisory` all map onto. Write the addition out, write the table,
count its rows, and state both numbers in the document. They must be equal. An item with no row is
the failure this table exists to prevent; an item may be OPEN but never ABSENT. Where one inventory
item legitimately appears twice — as a verifier advisory and as a reviewer warning — name the
duplication rather than silently collapsing it, and give the two rows their own dispositions.

Each row carries: the inventory item, the plan that took it, the evidence measured in THIS round (a
transcript reference from Task 1 or Task 2, never a plan's own claim), and a disposition.

CARRY THE TWO CARRIED ITEMS OUTSIDE THE TABLE, AND RE-MEASURE BOTH. Run the disposition gate and
record its exit code, its finding count and the file set it names, then compare that file set against
every file this round changed and record the overlap count. If the overlap is zero the item is
pre-existing and unchanged, and it is carried with that measurement rather than absorbed. Record the
live end-to-end lane as not run, with the reason and the `UNKNOWN - verify` marker, exactly as every
prior round of this phase did. Update `deferred-items.md` with both measurements, leaving both
entries carried, and leave `32-35`'s CI-topology entry carried as written.

RESTATE AND ASSERT BOTH PROBE ARITHMETICS. Reproduce this plan's authored-and-flagged edge tables in
the report, state the addition, and assert it equals the count the probe produced. Then state the
prohibition arithmetic: the number of prohibitions recalled and kept across the round's seven plans,
the number authored descriptor-less into a `must_haves.prohibitions:` block, and the number referred
to canon with a breadcrumb — and assert that no kept prohibition is missing from both. A count that
does not balance means an item was dropped in planning, which is a finding about the round rather
than about the code.

APPEND THIS ROUND'S RESIDUAL ROWS AND RECONCILE BOTH REPRESENTATIONS. `.planning/WINDOWS.md` carries
the same ledger twice, and a ledger whose halves disagree is worse than one half. Write through the
tool that maintains both. Append a row per finding this round leaves open, starting after row 191,
and give rows 187 through 191 — round 2's five open findings, each of which is answered by one of
this round's plans — a MEASURED verdict rather than an assumed one. Row 184 is answered by `32-36`
and row 185 by `32-35`; both need the same treatment. Then assert the reconciliation by comparing
every row identifier in one representation against the other and printing three numbers: identifiers
compared, rows in each half, disagreements.

CLOSE WITH WHAT THE DOCUMENT DOES NOT SAY. State plainly that it does not claim DASH-01 through
DASH-08 are satisfied, that it changes neither the requirements file nor the phase status line nor
any checkbox, and that the verifier decides that from this evidence in a separate pass. State the
round position — round 3 of 4 — and what a round 4 would have to do differently if the ratio did not
move.
  </action>

  <acceptance_criteria>
    - The report carries a ledger table with one row per inventory item, each naming the plan that took it, this round's own measured evidence and a disposition.
    - The report states the inventory count and the row count as two numbers and they are equal.
    - The report carries the two carried items outside the table, each with a re-measurement, and `deferred-items.md` reflects both.
    - The report restates the edge arithmetic and asserts it balances at 12, and states the prohibition arithmetic with the authored, canon-referred and dropped counts.
    - `.planning/WINDOWS.md` carries this round's rows in both representations after row 191, rows 184, 185 and 187 through 191 each carry a measured verdict, and the report prints the identifiers-compared, rows-per-half and disagreements numbers.
    - The report's closing section states that it decides no checkbox, no status line and no verdict, and names the round position.
  </acceptance_criteria>

  <verify>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query frontmatter.validate .planning/phases/32-board-projector-cli-dashboard/32-37-PLAN.md --schema plan-gap-closure</automated>
    <fails_when>the printed JSON carries a false "valid" field, or a non-empty "missing" or "invalidValue" array — this plan's own frontmatter does not satisfy the gap-closure schema it claims</fails_when>
    <automated>grep -c 'UNKNOWN - verify' .planning/phases/32-board-projector-cli-dashboard/32-37-ADVERSARIAL-REVIEW.md</automated>
    <fails_when>a printed count of 0 — the live end-to-end lane's unmeasured state was not recorded, which is the fabrication this project's rule forbids</fails_when>
    <automated>git diff --exit-code -- scripts/ agent-factory/ docs/ .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <fails_when>non-zero exit — this plan modified a source file, the requirements file or the roadmap, any of which would be deciding a verdict that belongs to the verifier</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the gate naming 32-37-ADVERSARIAL-REVIEW.md — a control byte was written literally into the report instead of being spelled as an escape, which is the exact defect this phase already paid for once</fails_when>
  </verify>

  <done>The ledger is total against a stated inventory, both carried items are re-measured and still carried, both probe arithmetics balance, and both residual representations agree.</done>

  <reversibility rating="reversible">Reports and ledgers; no source file and no status line moves.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this round's own proofs → this report | a plan's own GREEN proof is a lead, not evidence; the boundary is crossed by re-measuring independently |
| this report → the verifier and the requirements file | the report supplies evidence and must not cross into deciding a verdict; the crossing is prevented mechanically by this plan owning no status-bearing file |
| `.planning/WINDOWS.md`'s two representations | a ledger whose halves disagree rots silently, so the crossing is asserted by identifier comparison rather than by careful editing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-37-01 | Repudiation | a closure recorded from reading a diff | high | mitigate | Every reproduction is re-run with the original finding's own probe against the rebuilt committed `.js`, and every row carries the command and the output |
| T-32-37-02 | Spoofing | a harness whose own premise is false | high | mitigate | The build-parity and freshness checks run first and are printed; this project has six recorded instances of a false verification result from an unasserted premise |
| T-32-37-03 | Tampering | a status line flipped before verification | high | mitigate | This plan owns no requirements, roadmap or source file, and a verify command asserts none of them moved |
| T-32-37-04 | Repudiation | a relocated refusal assumed to have survived the move | high | mitigate | Task 2's relocation table answers per stolen refusal which authority held it before and whether the new one is at least as strong |
| T-32-37-05 | Repudiation | an inventory item with no ledger row | medium | mitigate | Both counts stated aloud and asserted equal; a duplicated item is named rather than collapsed |
| T-32-37-06 | Repudiation | the unmeasured end-to-end lane reported as passing | medium | mitigate | Recorded `UNKNOWN - verify` with the reason, and a verify command asserts the marker is present |

No package-manager install task exists in this plan — the repository ships zero runtime dependencies —
so no `T-32-SC` supply-chain row and no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness` — exit 0,
  printed at the top of the report.
- `npx vitest run --exclude '**/scripts/e2e/**'` — exit 0, passed-case count at or above 4974.
- `npm run check:dashboard-readonly`, `node scripts/validate-agent-factory.js` and
  `node scripts/check-foundation-guards.js` — exit 0.
- `npm run check:nul-bytes` — exit 0 over this round's own artifacts.
- `git diff --exit-code -- scripts/ agent-factory/ docs/ .planning/REQUIREMENTS.md .planning/ROADMAP.md`
  — clean.
- The report's ledger row count equals its stated inventory count; both probe arithmetics balance;
  the residual reconciliation prints zero disagreements.
</verification>

<success_criteria>
Round 3 ends with every prior reproduction re-measured against the artifact a host runs — including
the rows that were already refused, each answered by which authority refuses it now — every new
refusal branch carrying a written reachability and sibling-arm answer, every relocated refusal
answered per stolen refusal, a ledger asserted total against a stated inventory, both carried items
re-measured and still carried, two balanced probe arithmetics, two agreeing residual representations,
and a created-versus-inherited ratio stated as a number — with no checkbox, status line or verdict
touched.
</success_criteria>

<output>
Create `.planning/phases/32-board-projector-cli-dashboard/32-37-SUMMARY.md` when done, and
`32-37-ADVERSARIAL-REVIEW.md` as the report itself.
</output>

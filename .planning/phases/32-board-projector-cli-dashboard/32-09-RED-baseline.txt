# 32-09 RED baseline — the two defects as they stood before this plan's fix

Recorded: 2026-09-14T19:59:36Z
Tree HEAD: 7fa737e7
Artifact under test: the COMMITTED scripts/board-dashboard.js (no source edit yet).
Scratch tree: a copy of scripts/fixtures/board-snapshot/ (expected-snapshot.json and README.md removed),
made under mkdtemp on the realpath of the system temp directory. Path shown as <tree> below.

## CR-02 — an EACCES on plans/tickets/ renders a clean [ok] board with fabricated conflicts

$ chmod 000 <tree>/plans/tickets
$ node scripts/board-dashboard.js <tree> --once
--- stdout+stderr ---
board-dashboard: queue at <tree>/.grugops/queue/claimed/abc-105-tampered/claim.md — tampered: <tree>/.grugops/queue/claimed/abc-105-tampered/claim.md carries 2 `at:` lines and a claim record is written with exactly one. The record is skipped rather than trusted on either line: a forged second `at:` is a queue-lock denial of service (scripts/claim.ts:270-306).
grugops board  <tree>  mode: lean  read: 2026-09-14T19:59:36.878Z  6 columns  [ok]  11 conflicts
Backlog         unlimited  1 row
  ABC-101  Something in the backlog
Ready           1/5  1 row  1 unparsed line
  ABC-102  A ticket whose file disagrees about its column
In Development  claimed 2 / counted 3 / limit 3  3 rows  2 unparsed lines
  ABC-103  A ticket whose status is not the kebab form of its column
  ABC-104  A ticket that agrees with its file in every respect
  ABC-999  A row naming an identifier with no ticket file
In Review       1/3  1 row
  ABC-105  A ticket that also appears under a second heading
Done            unlimited  1 row
  ABC-105  The same identifier again, under a second heading
Blocked         -  1 row
  ABC-106  Waiting on a decision
Now running
  abc-104-implement  by software-engineer  at 2026-09-14T09:00:00.000Z
Conflicts (11)
  ticket-duplicated
    ABC-105 In Review  expected: one row on the board  actual: In Review, Done
  row-without-file
    ABC-101 Backlog  expected: plans/tickets/ABC-101.md  actual: no ticket file…
    ABC-102 Ready  expected: plans/tickets/ABC-102.md  actual: no ticket file c…
    ABC-103 In Development  expected: plans/tickets/ABC-103.md  actual: no tick…
    ABC-104 In Development  expected: plans/tickets/ABC-104.md  actual: no tick…
    ABC-105 In Review  expected: plans/tickets/ABC-105.md  actual: no ticket fi…
    ABC-106 Blocked  expected: plans/tickets/ABC-106.md  actual: no ticket file…
    ABC-999 In Development  expected: plans/tickets/ABC-999.md  actual: no tick…
  wip-limit
    In Review  expected: 2  actual: 3
  wip-count
    In Development  expected: claimed 2, limit 3  actual: counted 3
  column-missing
    Ready to Release  expected: a heading for the configured column Ready to Re…
--- exit: 0 ---

$ node scripts/board-dashboard.js <tree> --once --json | jq -c ".readErrors, .sources.tickets, .snapshot.sources.tickets"
[{"source":"queue","path":"<tree>/.grugops/queue/claimed/abc-105-tampered/claim.md","code":"tampered","message":"<tree>/.grugops/queue/claimed/abc-105-tampered/claim.md carries 2 `at:` lines and a claim record is written with exactly one. The record is skipped rather than trusted on either line: a forged second `at:` is a queue-lock denial of service (scripts/claim.ts:270-306)."}]
{"source":"unavailable","present":false}

$ node scripts/board-dashboard.js <tree> --once --json | jq "[.conflicts[] | select(.kind == \"row-without-file\")] | length"
7

$ node scripts/board-dashboard.js <tree> --once --json | jq "[.conflicts[] | select(.kind == \"ticket-unplaced\")] | length"
0

$ node scripts/board-dashboard.js <tree> --once --json | jq -c "[.conflicts[] | select(.kind == \"row-without-file\") | .ticketId]"
["ABC-101","ABC-102","ABC-103","ABC-104","ABC-105","ABC-106","ABC-999"]

$ ls <tree>/plans/tickets   # with the mode restored: the files the conflicts above say do not exist
ABC-101.md
ABC-102.md
ABC-103.md
ABC-104.md
ABC-105.md
ABC-106.md
ABC-200.md

$ chmod 000 <tree>/plans/tickets   # back to the denied mode for the filtered count below
$ node scripts/board-dashboard.js <tree> --once --json | jq "[.readErrors[] | select(.source == \"tickets\")] | length"
0
$ node scripts/board-dashboard.js <tree> --once --json | jq ".readErrors | length"
1

### What this baseline measures, stated exactly

The defect is the THREE readings above taken together:

  * `.snapshot.sources.tickets` is `{"source":"unavailable","present":false}` — the SAME answer the
    reader gives a directory that was never created, so the header renders `[ok]` with no stale badge.
  * `[.readErrors[] | select(.source == "tickets")] | length` is 0 — the permission failure is
    reported nowhere.
  * `[.conflicts[] | select(.kind == "row-without-file")] | length` is 7, naming ABC-101 … ABC-106
    and ABC-999. Six of those seven files EXIST and are readable (the `ls` above); only ABC-999 is a
    genuinely file-less row. Six fabricated positive assertions about the filesystem.

DEVIATION FROM THE PLAN'S WORDING, RECORDED RATHER THAN SMOOTHED OVER. Plan 32-09 Task 1's
acceptance criterion says the baseline shows "an empty `readErrors` array". On a FAITHFUL copy of
`scripts/fixtures/board-snapshot/` the array is not empty: it carries one PRE-EXISTING entry, the
fixture's own deliberately tampered claim record (`abc-105-tampered`, two `at:` lines), which is
unrelated to this defect and is present before and after the fix. The verifier's transcript in
32-VERIFICATION.md shows `[]` because their copy did not carry the fixture's `.grugops/` tree. The
substantive claim the criterion was written to pin — no `readErrors` entry for the tickets
permission failure — is measured directly by the `select(.source == "tickets")` count above, and it
is 0. The post-fix criterion is read the same way: exactly one entry whose `source` is `tickets`.

## CR-03 — a single non-UTF-8 byte is reported as a torn read

(recorded in Task 3, below)

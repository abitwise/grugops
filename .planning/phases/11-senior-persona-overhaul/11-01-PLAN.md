---
phase: 11-senior-persona-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/roles/agents-md-scribe.md
  - agent-factory/roles/architect-design.md
  - agent-factory/roles/brownfield-mapper.md
  - agent-factory/roles/compliance-officer.md
  - agent-factory/roles/factory-coach.md
  - agent-factory/roles/greenfield-mapper.md
  - agent-factory/roles/incident-responder.md
autonomous: true
requirements: [PERS-01]

must_haves:
  truths:
    - "Each of the 7 roles reads as a senior practitioner with long-term experience + forward-thinking judgment (D-02), woven in place with NO new section (D-01)"
    - "Each role's single `One job` is unchanged — persona + voice deepen, responsibilities do not (D-03)"
    - "Each role keeps a non-empty `## Caveman prompt` grug block, its contract sections (Output / Board moves / Trace updates), pointer discipline, kit-vs-state refs, and the AGENTS.md footer where it had one (D-03)"
    - "Each role file stays flat-or-smaller in bytes — terse caveman voice preserved as the token-economy mechanism (D-04); a verbose rewrite is a failure"
    - "Clear voice preserved on security/compliance/incident/safety lines; grug voice in the caveman block + punchy body (two-voice discipline)"
  artifacts:
    - path: "agent-factory/roles/agents-md-scribe.md"
      provides: "Senior Scribe persona; keeps rule-ownership (no 12-rules footer), keeps clear-voice voice-meta lines"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/architect-design.md"
      provides: "Senior architect persona"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/brownfield-mapper.md"
      provides: "Senior brownfield-mapper persona (cleanest skeleton reference)"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/compliance-officer.md"
      provides: "Senior compliance persona; clear-voice compliance lines"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/factory-coach.md"
      provides: "Senior factory-coach persona"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/greenfield-mapper.md"
      provides: "Senior greenfield-mapper persona"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/incident-responder.md"
      provides: "Senior incident-responder persona; clear-voice incident lines"
      contains: "## Caveman prompt"
  key_links:
    - from: "each rewritten role"
      to: "agent-factory/checklists + handoffs (existing pointers)"
      via: "single-source pointer discipline preserved (D-03)"
      pattern: "agent-factory/(checklists|handoffs)/"
---

<objective>
In-place senior rewrite of 7 of the 16 role prompts (PERS-01, group A–H minus ba-pm which Plan 11-03 owns): `agents-md-scribe`, `architect-design`, `brownfield-mapper`, `compliance-officer`, `factory-coach`, `greenfield-mapper`, `incident-responder`.

Each role is rewritten holistically top-to-bottom (D-02) so it reads as a senior practitioner with long-term, hard-won experience AND forward thinking (anticipates downstream consequences) — judgment woven into the EXISTING skeleton sections, NO new section added (D-01). The rewrite elevates persona + voice, NOT the contract: each role's single `One job`, its `## Caveman prompt` grug block, its contract sections, pointer discipline, kit-vs-state refs, and AGENTS.md footer are preserved (D-03). Seniority is encoded in compression — sharper judgment per token, never expansion — so each file stays roughly flat-or-smaller (D-04, mechanically enforced by `guard_role_size` in Plan 11-04).

Purpose: lay the senior-judgment substrate every later v1.2 content phase (12–17) authors into.
Output: 7 rewritten role files, each guard-clean and flat-or-smaller.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-senior-persona-overhaul/11-CONTEXT.md
@.planning/phases/11-senior-persona-overhaul/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Senior rewrite — agents-md-scribe, architect-design, brownfield-mapper, factory-coach (4 roles)</name>
  <files>agent-factory/roles/agents-md-scribe.md, agent-factory/roles/architect-design.md, agent-factory/roles/brownfield-mapper.md, agent-factory/roles/factory-coach.md</files>
  <read_first>
    - agent-factory/roles/agents-md-scribe.md (the file being rewritten; SKELETON DEVIATION: does NOT carry the `Follow the 12 coding rules` footer — it OWNS the rules; its clear-voice voice-meta lines at :38 "a light grug wink permitted only in Mission" and :40 "echo them in grug voice" are LEGITIMATE clear prose — keep them, they are the D-05 false-positive #2/#3 the Plan 11-04 guard accepts)
    - agent-factory/roles/architect-design.md (the file being rewritten)
    - agent-factory/roles/brownfield-mapper.md (the file being rewritten; PATTERNS.md Group A names this the cleanest skeleton reference — caveman block lines 10-16, `.grugops/` ref line 19, clear-voice Hard limits line 43, footer line 45)
    - agent-factory/roles/factory-coach.md (the file being rewritten)
    - .planning/phases/11-senior-persona-overhaul/11-PATTERNS.md § "Group A — the 16-role in-place senior rewrite" (the uniform skeleton to preserve verbatim; the skeleton-deviation list; the exact 16-role names)
    - .planning/phases/11-senior-persona-overhaul/11-CONTEXT.md § decisions D-01..D-04
  </read_first>
  <action>
    Rewrite each of the 4 role files in place, holistically top-to-bottom (D-02), within the fixed skeleton (no section added or removed, D-01/D-03). For each role, deepen judgment INSIDE the existing sections so it reads as a senior practitioner: in `Responsibilities`, encode forward-thinking (anticipate the downstream role's needs, what breaks two handoffs later); in `Hard limits`, encode hard-won experience (the failure modes a junior misses) — but add NO new capability and do NOT change `One job` (D-03). Keep the `## Caveman prompt` block punchy and grug; keep every contract section (Output / Board moves / Trace updates), single-source pointers, kit-vs-state refs, and the `Follow the 12 coding rules in AGENTS.md` footer — EXCEPT agents-md-scribe.md, which legitimately omits that footer (it owns the rules) and legitimately discusses "grug voice"/"grug wink" in clear prose (preserve those lines unchanged). Encode seniority as compression, not expansion (D-04): tighten weak prose, sharpen judgment per token; each file must end up roughly flat-or-smaller than its current byte size (see PATTERNS.md baseline table). Do NOT enumerate generic "what good looks like" quality lists (D-01 killed this — the model already carries that knowledge). Keep two-voice discipline: caveman in the prompt block + body, clear voice on any safety/escalation line.
  </action>
  <verify>
    <automated>for f in agents-md-scribe architect-design brownfield-mapper factory-coach; do c=$(grep -c '^## Caveman prompt' agent-factory/roles/$f.md); [ "$c" -eq 1 ] || { echo "FAIL caveman header count $f=$c"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Caveman prompt' <role>` == 1 for all 4 files (block preserved)
    - `grep -c '^## One job' <role>` == 1 for all 4 (skeleton intact, scope unchanged)
    - agents-md-scribe.md still has NO `Follow the 12 coding rules` footer line; the other 3 still carry it (`grep -q 'Follow the 12 coding rules' <role>` true for architect-design/brownfield-mapper/factory-coach, false for agents-md-scribe)
    - Each file's byte size is ≤ its current baseline + ~6% (agents-md-scribe ≤3701, architect-design ≤3423, brownfield-mapper ≤2354, factory-coach ≤3237) — flat-or-smaller (D-04); will be hard-checked by `guard_role_size` in Plan 11-04
    - SPOT-REVIEW (prose-judgment, not mechanical): senior depth landed — long-term-experience + forward-thinking judgment woven into existing sections, no new capability, no new section, caveman voice intact
  </acceptance_criteria>
  <done>4 role files rewritten to senior persona in place, skeleton + scope + voice preserved, flat-or-smaller bytes.</done>
</task>

<task type="auto">
  <name>Task 2: Senior rewrite — greenfield-mapper, compliance-officer, incident-responder (3 roles, incl. 2 clear-voice safety roles)</name>
  <files>agent-factory/roles/greenfield-mapper.md, agent-factory/roles/compliance-officer.md, agent-factory/roles/incident-responder.md</files>
  <read_first>
    - agent-factory/roles/greenfield-mapper.md (the file being rewritten)
    - agent-factory/roles/compliance-officer.md (the file being rewritten; clear-voice compliance lines — this role is in the CURRENT guard_voice scan set, so its clear-voice surfaces must stay caveman-marker-free)
    - agent-factory/roles/incident-responder.md (the file being rewritten; clear-voice incident lines — also in the current guard_voice scan set)
    - .planning/phases/11-senior-persona-overhaul/11-PATTERNS.md § "Group A" + § "Shared Patterns > Two-voice discipline"
    - .planning/phases/11-senior-persona-overhaul/11-CONTEXT.md § decisions D-01..D-04
  </read_first>
  <action>
    Rewrite each of the 3 role files in place, holistically top-to-bottom (D-02), within the fixed skeleton (D-01/D-03), same mechanism as Task 1: deepen senior judgment INSIDE existing sections (forward-thinking in Responsibilities, hard-won experience in Hard limits), preserve `One job` / `## Caveman prompt` / contract sections / pointers / kit-vs-state refs / AGENTS.md footer, encode seniority as compression not expansion (D-04, flat-or-smaller). For compliance-officer.md and incident-responder.md specifically: these carry compliance/incident safety surfaces that MUST stay clear professional English (two-voice discipline) — deepen the judgment in those lines without introducing any caveman marker into the clear-voice remainder (both are already in the `guard_voice` scan set and must stay green). Keep grug confined to the `## Caveman prompt` block + punchy body. No new section, no new capability, no generic quality enumeration (D-01).
  </action>
  <verify>
    <automated>for f in greenfield-mapper compliance-officer incident-responder; do c=$(grep -c '^## Caveman prompt' agent-factory/roles/$f.md); [ "$c" -eq 1 ] || { echo "FAIL caveman header count $f=$c"; exit 1; }; done; sh scripts/check-foundation-guards.sh >/tmp/g11-01.log 2>&1; grep -qi 'compliance-officer\|incident-responder' /tmp/g11-01.log && { echo "FAIL voice marker leaked into a clear-voice safety role"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Caveman prompt' <role>` == 1 for all 3 files
    - `grep -c '^## One job' <role>` == 1 for all 3 (scope unchanged)
    - `sh scripts/check-foundation-guards.sh` exits 0 — the existing `guard_voice` (which already scans compliance-officer + incident-responder) finds NO caveman marker in their clear-voice remainder after the rewrite (two-voice discipline held)
    - Each file ≤ baseline + ~6% (greenfield-mapper ≤2530, compliance-officer ≤3937, incident-responder ≤3206) — flat-or-smaller (D-04)
    - SPOT-REVIEW (prose-judgment): senior depth landed; compliance/incident safety lines remain clear professional English
  </acceptance_criteria>
  <done>3 role files rewritten to senior persona in place; clear-voice safety surfaces preserved; existing guard_voice stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer/dev → grugops repo | The only actor is a maintainer editing markdown in the repo. No web, network, auth, untrusted input, or data attack surface exists in this phase. The relevant threat class is **gate & trace integrity** — a rewrite that silently breaks a voice/scope invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | role rewrite sands off caveman voice / introduces clear-voice "grug" / bloats a role | mitigate | Plan 11-04's `guard_caveman_preserved` + `guard_voice` (all 16) + `guard_role_size` mechanically catch all three; THIS plan keeps two-voice discipline and flat bytes so those guards ship green. Verify in-task with `grep -c '^## Caveman prompt'` and the existing `guard_voice` run over the 2 in-scope safety roles. |
| T-11-03 | Tampering | rewrite silently changes a role's `One job` (scope creep disguised as persona depth) | mitigate | `grep -c '^## One job' == 1` per file + spot-review that responsibilities are unchanged (D-03 bound). Scope expansion belongs to other phases. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | N/A — this phase installs ZERO external packages (markdown-only edits; npm deps are Out of Scope per REQUIREMENTS.md). No package-legitimacy surface exists. |

Severity realistically LOW: maintainer-side markdown edits, no untrusted input, no runtime. The honest risk is a broken/false-green gate, mitigated by Plan 11-04 authoring the guards over the now-clean content.
</threat_model>

<verification>
- `sh scripts/check-foundation-guards.sh` exits 0 after this plan (the existing guards — incl. guard_voice over compliance-officer + incident-responder — stay green).
- Each of the 7 files: exactly one `## One job` and one `## Caveman prompt` header; byte size flat-or-smaller vs the PATTERNS.md baseline.
- Spot-review the senior depth across the 7 roles (the prose-judgment surface no guard can check) per 11-VALIDATION.md Manual-Only Verifications.
</verification>

<success_criteria>
7 of the 16 roles rewritten to senior judgment in place (PERS-01 / D-01..D-04): no new section, scope unchanged, caveman voice + contract sections + pointers + footer preserved, flat-or-smaller bytes, clear-voice safety surfaces intact.
</success_criteria>

<output>
Create `.planning/phases/11-senior-persona-overhaul/11-01-SUMMARY.md` when done
</output>

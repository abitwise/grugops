---
phase: 11-senior-persona-overhaul
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/roles/ba-pm.md
  - agent-factory/workflows/07-backlog-refinement.md
  - agent-factory/checklists/definition-of-ready.md
  - agent-factory/handoffs/ticket-ready-packet.md
autonomous: true
requirements: [PERS-01, PERS-02]

must_haves:
  truths:
    - "ba-pm.md is rewritten to senior persona in place (PERS-01 / D-01..D-04) — the SAME file is also the BA-deepening target, so this plan owns it exclusively to avoid a double-edit race"
    - "The business-analysis layer is deepened to senior level (PERS-02 / D-08): INVEST-shaped stories, testable+measurable acceptance criteria, measurable NFRs, a Definition of Ready that closes the business→engineer handoff"
    - "definition-of-ready.md is the single hub for the INVEST + measurable-NFR gates; ba-pm.md and 07-backlog-refinement.md POINT TO it (no duplication, no new file) (D-08)"
    - "PROSE QUALITY ONLY (D-09): keeps the DoR's existing Given/When/Then prose line; NO Three Amigos, NO Example Mapping, NO executable given-when-then, NO selector-free scenario files (all Phase 12)"
    - "ticket-ready-packet.md stays field-for-field aligned with definition-of-ready.md (the 1:1 DoR-comment mapping is preserved if any gated check is added)"
  artifacts:
    - path: "agent-factory/roles/ba-pm.md"
      provides: "Senior BA/PM persona (PERS-01 rewrite) + senior BA judgment (PERS-02), pointing to the DoR hub"
      contains: "definition-of-ready.md"
    - path: "agent-factory/checklists/definition-of-ready.md"
      provides: "The single INVEST + measurable-NFR DoR hub (D-08); keeps the Given/When/Then prose line (D-09)"
      contains: "Given/When/Then"
    - path: "agent-factory/workflows/07-backlog-refinement.md"
      provides: "Senior refinement ceremony; points to the DoR; leaves room for Phase 12's Three Amigos substep (D-09 seam)"
      contains: "definition-of-ready.md"
    - path: "agent-factory/handoffs/ticket-ready-packet.md"
      provides: "Packet kept field-for-field aligned with the DoR (Pitfall 5)"
      contains: "definition-of-ready.md"
  key_links:
    - from: "ba-pm.md + 07-backlog-refinement.md"
      to: "agent-factory/checklists/definition-of-ready.md"
      via: "single-source pointer (existing; the deepening rides it)"
      pattern: "definition-of-ready\\.md"
    - from: "agent-factory/handoffs/ticket-ready-packet.md"
      to: "agent-factory/checklists/definition-of-ready.md"
      via: "one DoR-comment per field (must stay 1:1 aligned)"
      pattern: "<!-- DoR:"
---

<objective>
Two requirements in one plan, because they converge on `ba-pm.md`:

1. **PERS-01 (the 16th role rewrite):** `ba-pm.md` gets the same in-place senior persona rewrite as Plans 11-01/11-02 (D-01..D-04). This plan owns `ba-pm.md` EXCLUSIVELY so the PERS-01 rewrite and the PERS-02 deepening land in one coherent edit, never two plans racing the same file.
2. **PERS-02 (senior BA deepening, prose-only):** deepen the business-analysis layer to senior level — INVEST-shaped stories, acceptance criteria that are testable + measurable, measurable NFRs, and a Definition of Ready that closes the business→engineer handoff. `definition-of-ready.md` is the single hub (D-08); `ba-pm.md` + `07-backlog-refinement.md` POINT to it (no duplication, no new file). PROSE QUALITY ONLY (D-09): keep the DoR's existing Given/When/Then prose line; do NOT add Three Amigos, Example Mapping, executable given-when-then, or scenario files — all Phase 12.

Because `ba-pm.md` gains genuine BA judgment, its size ceiling carries extra headroom (Plan 11-04 sets ba-pm FAIL at 3294 B / WARN 3075 B, vs the +12%/+6% the others get) — a modest, justified growth is acceptable, a bloated rewrite still fails.

Purpose: the senior BA substrate Phase 12's Three Amigos + executable scenarios build on top of (keep the seam clean — no executability here).
Output: rewritten ba-pm.md, deepened DoR hub + workflow 07, packet kept aligned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-senior-persona-overhaul/11-CONTEXT.md
@.planning/phases/11-senior-persona-overhaul/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Senior rewrite of ba-pm.md (PERS-01) + senior BA judgment woven in (PERS-02)</name>
  <files>agent-factory/roles/ba-pm.md</files>
  <read_first>
    - agent-factory/roles/ba-pm.md (the file being rewritten; PATTERNS.md Group F notes the existing DoR pointers at :22 `## Reads` and :32 `## Responsibilities` #4 — keep and ride them, no new pointer needed)
    - agent-factory/checklists/definition-of-ready.md (the hub this role points to — read it so the rewrite references it correctly; Task 2 deepens it)
    - agent-factory/handoffs/product-handoff.md § fields (User value / Acceptance criteria (Given/When/Then) / Security/NFR triggers / Size estimate / Priority — PATTERNS.md Group F: the senior BA prose rides these EXISTING fields; NO new field, executability is Phase 12)
    - .planning/phases/11-senior-persona-overhaul/11-PATTERNS.md § "Group A" (skeleton) + § "Group F" (BA deepening rides existing pointers + handoff fields)
    - .planning/phases/11-senior-persona-overhaul/11-CONTEXT.md § decisions D-01..D-04, D-08, D-09
  </read_first>
  <action>
    Rewrite ba-pm.md in place, holistically top-to-bottom (D-02), within the fixed skeleton (D-01/D-03) — same persona mechanism as Plans 11-01/11-02: deepen senior judgment INSIDE existing sections, preserve `One job` / `## Caveman prompt` / contract sections (Output / Board moves / Trace updates) / single-source pointers / kit-vs-state refs / AGENTS.md footer, no new section. THEN weave the PERS-02 senior BA judgment into those same existing sections (D-08): in Responsibilities, the senior BA shapes stories as INVEST-shaped, writes acceptance criteria that are testable + measurable, marks measurable NFRs (not vague "fast"/"secure"), and drives each ticket to Definition of Ready — referencing `definition-of-ready.md` as the bar (keep the existing pointer, do not duplicate the checklist into the role). PROSE QUALITY ONLY (D-09): the role describes WRITING good INVEST stories + measurable acceptance/NFRs as prose judgment; it does NOT introduce Three Amigos, Example Mapping, executable given-when-then, or scenario files (Phase 12). Keep two-voice discipline. ba-pm has extra size headroom (FAIL 3294 B) because it legitimately gains judgment — but compress elsewhere so the growth is modest, not bloat (D-04).
  </action>
  <verify>
    <automated>c=$(grep -c '^## Caveman prompt' agent-factory/roles/ba-pm.md); [ "$c" -eq 1 ] || { echo "FAIL caveman count=$c"; exit 1; }; grep -q 'definition-of-ready.md' agent-factory/roles/ba-pm.md || { echo "FAIL lost DoR pointer"; exit 1; }; grep -qi 'invest' agent-factory/roles/ba-pm.md || { echo "FAIL no INVEST judgment"; exit 1; }; grep -qi 'three amigos\|example mapping' agent-factory/roles/ba-pm.md && { echo "FAIL Phase-12 BDD leaked in"; exit 1; }; b=$(wc -c < agent-factory/roles/ba-pm.md | tr -d ' '); [ "$b" -le 3294 ] || { echo "FAIL ba-pm bloated $b"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Caveman prompt' ba-pm.md` == 1 and `grep -c '^## One job' ba-pm.md` == 1 (skeleton + scope intact)
    - `grep -q 'definition-of-ready.md' ba-pm.md` true (still points to the hub, no duplicated checklist)
    - `grep -qi 'invest' ba-pm.md` true (INVEST judgment present); `grep -qi 'three amigos\|example mapping' ba-pm.md` FALSE (no Phase-12 BDD leakage, D-09)
    - ba-pm.md ≤ 3294 B (its explicit BA-headroom FAIL ceiling) — justified growth, not bloat
    - SPOT-REVIEW (prose-judgment): senior BA judgment landed (INVEST, testable+measurable acceptance, measurable NFRs, DoR rigor); persona deepened; no Phase-12 executability
  </acceptance_criteria>
  <done>ba-pm.md rewritten to senior persona with senior BA judgment woven in, pointing to the DoR hub, prose-only, within its headroom ceiling.</done>
</task>

<task type="auto">
  <name>Task 2: Deepen the DoR hub + workflow 07; keep ticket-ready-packet aligned</name>
  <files>agent-factory/checklists/definition-of-ready.md, agent-factory/workflows/07-backlog-refinement.md, agent-factory/handoffs/ticket-ready-packet.md</files>
  <read_first>
    - agent-factory/checklists/definition-of-ready.md (the hub being deepened; PATTERNS.md Group F: the 9 terse checks lines 13-19 — KEEP the bullet style + the existing `acceptance criteria clear (Given/When/Then)` line per D-09; weave INVEST-shaping + MEASURABLE-NFR rigor INTO the existing terse lines)
    - agent-factory/workflows/07-backlog-refinement.md (the ceremony being deepened; PATTERNS.md Group F: BA/PM bullet line 12 + Steps 2/4/5 lines 25-28 are where senior refinement judgment lands; the DoR pointers at :21/:29/:32/:44/:47 already gate on the DoR by name — ride them; LEAVE ROOM for Phase 12's Three Amigos substep, D-09 seam)
    - agent-factory/handoffs/ticket-ready-packet.md (the packet that must stay 1:1 with the DoR; PATTERNS.md Group F + RESEARCH Pitfall 5: each `## field` carries a `<!-- DoR: ... -->` comment mapping to a DoR line; footer line 65 says "keep aligned with that file")
    - .planning/phases/11-senior-persona-overhaul/11-CONTEXT.md § decisions D-08, D-09
  </read_first>
  <action>
    Deepen `definition-of-ready.md` as the single hub (D-08): weave INVEST-shaping (independent / negotiable / valuable / estimable / small / testable) and MEASURABLE-NFR rigor (NFR triggers must carry a measurable target, not vague adjectives) INTO the EXISTING terse checklist lines — keep the bullet style, keep the file terse (it is the token-economy substrate too). Per D-09, KEEP the existing `acceptance criteria clear (Given/When/Then)` prose line; do NOT add Three Amigos, Example Mapping, executable wiring, or scenario files. PREFER landing the new rigor INSIDE existing DoR lines so NO new gated field is needed (RESEARCH Assumption A3) — this keeps the packet aligned for free. IF a genuinely new gated DoR check is unavoidable, add the matching `## field` + `<!-- DoR: ... -->` comment to `ticket-ready-packet.md` in THIS task so the 1:1 mapping never desyncs (Pitfall 5); otherwise leave the packet unchanged but confirm its mapping still covers every DoR line. In `07-backlog-refinement.md`, deepen the refinement ceremony's senior judgment (BA/PM bullet + Steps 2/4/5) and keep it pointing to the DoR by name; leave a clean seam for Phase 12's Three Amigos substep (do not add it now). No npm deps, no executable scenarios — prose only.
  </action>
  <verify>
    <automated>grep -qi 'invest' agent-factory/checklists/definition-of-ready.md || { echo "FAIL DoR missing INVEST"; exit 1; }; grep -qiE 'measurable' agent-factory/checklists/definition-of-ready.md || { echo "FAIL DoR missing measurable-NFR rigor"; exit 1; }; grep -q 'Given/When/Then' agent-factory/checklists/definition-of-ready.md || { echo "FAIL DoR lost the G/W/T prose line (D-09)"; exit 1; }; grep -qi 'three amigos\|example mapping' agent-factory/checklists/definition-of-ready.md agent-factory/workflows/07-backlog-refinement.md && { echo "FAIL Phase-12 BDD leaked in"; exit 1; }; dc=$(grep -c '^- ' agent-factory/checklists/definition-of-ready.md); pc=$(grep -c '<!-- DoR:' agent-factory/handoffs/ticket-ready-packet.md); echo "DoR-bullets=$dc packet-mappings=$pc (must cover every gated check)"; grep -q 'definition-of-ready.md' agent-factory/workflows/07-backlog-refinement.md || { echo "FAIL workflow 07 lost DoR pointer"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi 'invest' definition-of-ready.md` true and a measurable-NFR rigor line present (`grep -qi 'measurable'`)
    - `grep -q 'Given/When/Then' definition-of-ready.md` true (the existing prose line KEPT, D-09)
    - `grep -qi 'three amigos\|example mapping'` FALSE across DoR + workflow 07 (no Phase-12 BDD leakage, D-09)
    - `07-backlog-refinement.md` still points to `definition-of-ready.md` by name
    - ticket-ready-packet.md ↔ DoR 1:1 mapping preserved: every gated DoR check has a matching `<!-- DoR: -->` field (the automated grep prints both counts for the spot-check; if a new gated check was added, the packet was updated in this same task)
    - SPOT-REVIEW (prose-judgment): DoR rigor present (INVEST + measurable acceptance/NFR), workflow ceremony deepened, file stays terse, Phase-12 seam clean
  </acceptance_criteria>
  <done>DoR hub deepened with INVEST + measurable-NFR rigor (terse, prose-only); workflow 07 ceremony deepened + still points to the DoR; packet kept field-for-field aligned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer/dev → grugops repo | Maintainer editing markdown. No web/network/auth/data surface. Threat class is **gate & trace integrity** (here: the DoR↔packet contract that gates the business→engineer handoff). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | DoR gains a gated check but ticket-ready-packet is not updated → the gate desyncs (Pitfall 5) | mitigate | Task 2 prefers landing rigor INSIDE existing DoR lines (no new field); if a new gated check is added, the packet's `<!-- DoR: -->` field is added in the SAME task. Verify count parity. |
| T-11-06 | Tampering | Phase-12 BDD (Three Amigos / executable given-when-then) leaks into Phase 11, double-owning behavior across phases | mitigate | `grep -qi 'three amigos\|example mapping'` must be FALSE on ba-pm.md, DoR, and workflow 07 (D-09 boundary); the G/W/T prose line is KEPT but stays prose. |
| T-11-01 | Tampering | ba-pm rewrite sands caveman voice / bloats the file | mitigate | `grep -c '^## Caveman prompt' == 1` + ba-pm size ceiling (≤3294 B, its explicit BA-headroom FAIL) enforced by Plan 11-04. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero external packages (markdown-only; npm deps Out of Scope). |

Severity LOW. The one real integrity risk is the DoR↔packet desync; mitigated by preferring in-line rigor + same-task packet alignment.
</threat_model>

<verification>
- `sh scripts/check-foundation-guards.sh` exits 0 after this plan (existing guards stay green; ba-pm caveman block intact).
- DoR carries INVEST + measurable-NFR rigor, keeps the Given/When/Then prose line, stays terse; no Three Amigos / executable scenarios anywhere (D-09).
- DoR↔ticket-ready-packet field mapping intact (1:1).
- Spot-review senior BA prose quality across ba-pm.md / workflow 07 / DoR per 11-VALIDATION.md Manual-Only Verifications.
</verification>

<success_criteria>
ba-pm.md rewritten to senior persona (PERS-01) with senior BA judgment woven in (PERS-02); DoR hub deepened with INVEST + measurable-NFR rigor (single-source, prose-only, terse); workflow 07 ceremony deepened + points to the DoR; packet stays field-for-field aligned; the DoR's Given/When/Then prose line kept and no Phase-12 BDD leaked in (D-09).
</success_criteria>

<output>
Create `.planning/phases/11-senior-persona-overhaul/11-03-SUMMARY.md` when done
</output>

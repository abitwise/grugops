---
phase: 13-frontend-ui-persona-design-build-workflow
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - agent-factory/roles/orchestrator.md
  - scripts/check-foundation-guards.sh
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "The Orchestrator classification list carries the new ui-build token (15→16 request types)"
    - "The Orchestrator routing matrix routes 'Need UI/frontend → Frontend/UI'"
    - "The Orchestrator workflow-map table maps ui-build → 14-ui-design-to-build.md (registered without renumbering 00-13)"
    - "The request-count literal reads 'all 16 request types' (hand-maintained counter updated)"
    - "orchestrator.md's role_ceiling() case is raised to fit the UI-03 edits and guard_role_size stays GREEN (trap 2)"
  artifacts:
    - path: "agent-factory/roles/orchestrator.md"
      provides: "ui-build classification + routing-matrix row + workflow-map row + updated count (UI-03)"
      contains: "ui-build"
    - path: "scripts/check-foundation-guards.sh"
      provides: "Raised orchestrator.md role_ceiling() case off the post-wiring byte count (trap 2)"
      contains: "orchestrator.md)"
  key_links:
    - from: "agent-factory/roles/orchestrator.md"
      to: "agent-factory/workflows/14-ui-design-to-build.md"
      via: "workflow-map row maps the ui-build classification to the new workflow file"
      pattern: "ui-build.*14-ui-design-to-build.md"
    - from: "scripts/check-foundation-guards.sh"
      to: "agent-factory/roles/orchestrator.md"
      via: "role_ceiling() case raised to cover the post-wiring size"
      pattern: "orchestrator.md\\)"
---

<objective>
Wire the Orchestrator to route UI work to the new persona (**UI-03**): add the `ui-build` classification token, a `Need UI/frontend → Frontend/UI` routing-matrix row, a `ui-build → 14-ui-design-to-build.md` workflow-map row, and update the "all 15 request types" literal to "16". Then **raise orchestrator.md's `role_ceiling()` case** to fit those edits and keep `guard_role_size` GREEN.

This plan owns **guard trap 2**: orchestrator.md is 6661B against a 6664 WARN / 7041 FAIL ceiling — ~3B to WARN, ~380B to FAIL. The UI-03 edits will exceed 380B and turn the build RED unless the ceiling is raised in the SAME plan, measured off the post-wiring size (measure-then-set). Author the routing edits terse to need the smallest bump.

Depends on Plan 01 (which already edited `check-foundation-guards.sh` to register the role — sequential to avoid a file conflict) and Plan 02 (the workflow-map row points to the workflow 14 file it authored).

Purpose: completes SC3 — "the Orchestrator routing matrix AND classification list route UI work to the frontend/UI persona, and the new role + workflow register in the workflow map without renumbering 00-13."
Output: edited `orchestrator.md` + the raised orchestrator ceiling in `check-foundation-guards.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-CONTEXT.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-RESEARCH.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-PATTERNS.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-VALIDATION.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the four Orchestrator UI-03 edit sites (classification + matrix + workflow-map + count)</name>
  <files>agent-factory/roles/orchestrator.md</files>
  <read_first>
    - agent-factory/roles/orchestrator.md — the four edit sites: the "all 15 request types" literal (line 35); the responsibility-3 classification list (lines 40-43, the 15-token list ending `| install`); the routing matrix fenced block (lines 51-66, ending `Need adapters installed -> Installer`); the workflow-map table (lines 92-109, 13 numbered rows ending `| incident | \`13-incident.md\` |` + the install-has-no-workflow note).
    - .planning/phases/13-frontend-ui-persona-design-build-workflow/13-PATTERNS.md — § "orchestrator.md (EDIT)" carries the VERBATIM current text of all four sites + the locked token spelling.
    - .planning/phases/13-frontend-ui-persona-design-build-workflow/13-RESEARCH.md — Runtime State Inventory (the hand-maintained counters that adding a file does NOT auto-update) + the token-spelling lock (A4: `ui-build` is the classification, `ui-design-to-build` is the workflow name).
  </read_first>
  <action>
    Make four terse edits to `agent-factory/roles/orchestrator.md`, keeping every change minimal (trap 2 — the smaller the byte add, the smaller the ceiling bump):
    (1) Line 35: change `all 15 request types` → `all 16 request types`.
    (2) Responsibility-3 classification list: append the 16th token `ui-build` after `install` (e.g. `… | install | ui-build`). Token spelling is locked `ui-build` (D-05/A4 — NOT `ui-design-to-build`, that is the workflow filename).
    (3) Routing matrix (inside the fenced block): add one row keeping the `->` alignment: `Need UI/frontend          -> Frontend/UI`.
    (4) Workflow-map table: add one row `| ui-build | \`14-ui-design-to-build.md\` |` (this registers the workflow without renumbering 00-13 — Pitfall 6). Keep `agent-factory/README.md` consistent per D-05; RESEARCH A1 confirmed the README enumerates no roles/workflows and carries no routing matrix (grep returned 0), so this is a no-op — do NOT invent a README list.
    Do NOT raise the ceiling in this task — that is Task 2 (it must measure orchestrator.md AFTER these edits land).
  </action>
  <acceptance_criteria>
    - `grep -q 'all 16 request types' agent-factory/roles/orchestrator.md` (count literal updated)
    - `grep -q 'ui-build' agent-factory/roles/orchestrator.md` (classification token present)
    - `grep -qi 'Need UI/frontend' agent-factory/roles/orchestrator.md` AND `grep -qi 'Frontend/UI' agent-factory/roles/orchestrator.md` (routing-matrix row)
    - `grep -qE '\| ui-build \| .14-ui-design-to-build.md' agent-factory/roles/orchestrator.md` (workflow-map row mapping ui-build → workflow 14)
    - `grep -q 'all 15 request types' agent-factory/roles/orchestrator.md` returns nothing (old literal fully replaced)
    - frozen ordinals untouched: `grep -qE '\| incident \| .13-incident.md' agent-factory/roles/orchestrator.md` still present and no 00-13 row renumbered
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'all 16 request types' agent-factory/roles/orchestrator.md && grep -q 'ui-build' agent-factory/roles/orchestrator.md && grep -qi 'Need UI/frontend' agent-factory/roles/orchestrator.md && grep -qE '\| ui-build \| .14-ui-design-to-build.md' agent-factory/roles/orchestrator.md && ! grep -q 'all 15 request types' agent-factory/roles/orchestrator.md</automated>
  </verify>
  <done>orchestrator.md carries the ui-build classification token, the Need UI/frontend → Frontend/UI matrix row, the ui-build → 14-ui-design-to-build.md workflow-map row, and the "all 16 request types" count; the 00-13 workflow-map ordinals are unchanged. Ceiling not yet raised (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Raise orchestrator.md's role_ceiling() case off the post-wiring size and ship both guards GREEN</name>
  <files>scripts/check-foundation-guards.sh</files>
  <read_first>
    - scripts/check-foundation-guards.sh — the `role_ceiling()` `orchestrator.md)` case (currently `echo "7041 6664"`, line 367) and the +12%/+6% formula comment block; the per-role headroom-comment precedent (ba-pm's `# PERS-02 BA headroom` note).
    - .planning/phases/13-frontend-ui-persona-design-build-workflow/13-PATTERNS.md — § guard edit (c) (RAISE the orchestrator ceiling, measure-then-set off the NEW baseline) + the documented headroom-comment convention.
    - .planning/phases/13-frontend-ui-persona-design-build-workflow/13-RESEARCH.md — Pitfall 2 + Open Question 1 (the precise post-wiring byte count depends on row wording — re-measure, do not guess).
  </read_first>
  <action>
    After Task 1's edits land, re-measure: `wc -c agent-factory/roles/orchestrator.md`. Compute NEW_FAIL = round(new_bytes × 1.12), NEW_WARN = round(new_bytes × 1.06). Update the `orchestrator.md)` case in `role_ceiling()` from `echo "7041 6664"` to `echo "<NEW_FAIL> <NEW_WARN>"`, appending a headroom comment in the ba-pm style — e.g. `# +Phase-13 routing (ui-build classification + matrix + map row)`. This is the ONLY ceiling change in this plan (Plan 01 already added the frontend-ui.md case — do not duplicate it). Then run `sh scripts/check-foundation-guards.sh && sh scripts/check-foundation-guards.test.sh` — both MUST print ALL CHECKS PASSED. If `guard_role_size` still WARNs/FAILs on orchestrator.md, the ceiling was computed off a stale byte count — re-run `wc -c` and recompute.
  </action>
  <acceptance_criteria>
    - `grep -qE 'orchestrator.md\)[[:space:]]*echo "[0-9]+ [0-9]+"' scripts/check-foundation-guards.sh` (case present with two numbers)
    - the orchestrator ceiling is no longer the stale `7041 6664`: `grep -qE 'orchestrator.md\)[[:space:]]*echo "7041 6664"' scripts/check-foundation-guards.sh` returns nothing
    - `sh scripts/check-foundation-guards.sh` exits 0 (guard_role_size PASS for orchestrator.md after the UI-03 edits — trap 2 closed)
    - `sh scripts/check-foundation-guards.test.sh` exits 0 (fail-proof harness GREEN)
  </acceptance_criteria>
  <verify>
    <automated>grep -qE 'orchestrator.md\)[[:space:]]*echo "[0-9]+ [0-9]+"' scripts/check-foundation-guards.sh && ! grep -qE 'orchestrator.md\)[[:space:]]*echo "7041 6664"' scripts/check-foundation-guards.sh && sh scripts/check-foundation-guards.sh && sh scripts/check-foundation-guards.test.sh</automated>
  </verify>
  <done>The orchestrator.md role_ceiling() case is raised off the post-wiring byte count (measure-then-set, +12%/+6%) with a Phase-13 headroom comment; both guard scripts exit 0 GREEN; trap 2 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-maintained counter → registry truth | The request-count literal, classification list, and workflow-map are hand-maintained; a missed counter leaves the registry lying about its own size |
| raised ceiling → guard credibility | Raising a size ceiling must be off a real measured byte count, not a number picked to make the guard pass — measure-then-set keeps the guard honest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering (registry drift) | orchestrator.md counters | mitigate | All four hand-maintained sites updated together (count + classification + matrix + map); structural greps confirm each, and `! grep 'all 15 request types'` confirms the old literal is gone. |
| T-13-08 | Tampering (fabricated guard pass) | the raised orchestrator ceiling | mitigate | Measure-then-set off `wc -c` of the post-wiring file at the documented +12%/+6%; the fail-proof harness `check-foundation-guards.test.sh` still plants a real oversize violation and asserts the size guard fails red, so the raise cannot license a bloated rewrite. |
| T-13-09 | Tampering (frozen-ordinal break) | the workflow-map table | mitigate | The new row appends as `ui-build → 14-…`; the `| incident | 13-incident.md |` row and all 00-13 ordinals stay unchanged (Pitfall 6) — verified by grep. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — markdown + read-only POSIX-sh guard edits only (CLAUDE.md hard constraint; RESEARCH "no Package Legitimacy Audit required"). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages exist to gate. |
</threat_model>

<verification>
- `grep -q 'ui-build' agent-factory/roles/orchestrator.md` (classification list — UI-03)
- `grep -qi 'Frontend/UI' agent-factory/roles/orchestrator.md` (routing matrix — UI-03)
- `grep -qE '\| ui-build \| .14-ui-design-to-build.md' agent-factory/roles/orchestrator.md` (workflow-map — UI-03, SC3)
- `grep -q 'all 16 request types' agent-factory/roles/orchestrator.md` (count updated)
- `sh scripts/check-foundation-guards.sh && sh scripts/check-foundation-guards.test.sh` both exit 0 (trap 2 closed; guard_role_size PASS on orchestrator.md after the edits)
</verification>

<success_criteria>
- Orchestrator routing matrix AND classification list AND workflow-map route UI work to Frontend/UI via the ui-build classification → 14-ui-design-to-build.md (UI-03, SC3)
- The "15"→"16" request-count literal is updated (hand-maintained counter)
- 00-13 ordinals unchanged; workflow 14 registered (Pitfall 6)
- orchestrator.md ceiling raised off the post-wiring size; both guard scripts GREEN (trap 2 closed)
</success_criteria>

<output>
Create `.planning/phases/13-frontend-ui-persona-design-build-workflow/13-03-SUMMARY.md` when done
</output>

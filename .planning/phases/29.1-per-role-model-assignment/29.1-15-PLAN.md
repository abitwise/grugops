---
phase: 29.1-per-role-model-assignment
plan: 15
type: execute
wave: 4
depends_on: ["29.1-14"]
gap_closure: true
files_modified:
  - scripts/model-tiers.ts
  - scripts/model-tiers.js
  - scripts/model-tiers.test.ts
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
autonomous: true
requirements: [MODEL-01, MODEL-03, MODEL-04]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "R2-WR-01 closed: `resolveModels` validates the `overrides` argument's SHAPE before it defaults, exactly as Floor 0 already validates `preset`. A nullish `overrides` is refused by name instead of being silently discarded, and a non-Map `overrides` is refused by name instead of throwing an uncaught `TypeError` out of a module whose header argues that returning a result rather than throwing is what lets one reader serve every degrading consumer."
    - "R2-WR-01's carried-forward twin is closed by the same edit: the round-2 verifier's carried warning that the container-level `overrides` value is not validated with the same rigour as `preset` is THIS finding, and it is closed this round rather than carried a second time."
    - "R2-WR-01's ABSENT case is untouched and asserted: an absent `overrides`, an absent options object and an explicitly `undefined` `overrides` all still resolve the zero-config answer. Absent and null remain different statements, and the fix does not close the zero-config path."
    - "R2-WR-06 closed on the mechanism: every member of `MODELS_KEYS` is proven to be CONSUMED by the reader, not merely permitted. Each legal key is driven through `readModelsConfig` and required to CHANGE the result against a no-keys baseline, with the probe table's key set asserted equal to the tuple in BOTH directions so a key added with no probe is a key nobody proved is read."
    - "R2-WR-06 closed on the message: the source comment no longer claims that editing the tuple moves the mechanism and the message together. It states the weaker true claim — editing the tuple moves the message — and names the case that now moves the mechanism."
    - "R2-IN-02 closed structurally: `roleCorpusCardinalityRefusal` is DELETED rather than kept as exported production code with no production consumer. Its argument survives in the sister predicate's docstring, and every mention of it — in `scripts/model-tiers.ts`, in `scripts/check-foundation-guards.ts`'s comment, and inside the guard's own consumer-list finding text — is removed in the same commit, so no shipped message names a symbol that does not exist."
    - "R2-IN-04 closed: `resolvedAssignmentsIn` enforces the grammar its refusal text declares. Each announced alias is checked against the closed alias set rather than only against the string type, so the declared shape and the enforced shape are the same predicate."
    - "Every new case asserts what its input is ASSEMBLED from before asserting the property: the config fixtures are read back through the same reader the assertion is about, and the probe table's coverage of the tuple is asserted before the loop that consumes it."
    - "MODEL-01's zero-config contract is undisturbed: `node scripts/generate-role-adapters.js` followed by `git status --porcelain -- .claude/agents/` prints nothing, and all 17 adapters still carry `model: inherit`."
    - "Per D-05 the `models` block's shape and the role-stem key are unchanged and `roles` stays a sparse override rather than a full map; per D-06 an unknown key in `roles` is still refused naming the key and the valid set; per D-07 the preset name stays a closed set of exactly two members and this plan adds none; per D-11 the split by consumer is unchanged — the generator refuses hard and every other consumer degrades to `inherit`, never to a pinned tier — and the new `overrides` floor refuses a caller's malformed value rather than degrading it."
    - "Per D-14 and D-16 no measurement is taken and no cost claim ships: `scripts/measure-cost.ts` and its committed twin stay byte-unchanged, and no refusal, docstring or comment this plan writes asserts a spend, quota or usage-limit effect."
    - "The committed `scripts/model-tiers.js` and `scripts/check-foundation-guards.js` are faithful builds of their `.ts`, and plan 29.1-12's relative-root byte identity still holds after this plan's guard edits."
  artifacts:
    - scripts/model-tiers.ts
    - scripts/model-tiers.js
    - scripts/model-tiers.test.ts
  key_links:
    - "`ResolveModelsOptions.overrides` docstring → the new Floor 0 shape validation → the iteration site at the override-application floor. The claim the docstring makes and the floor the code runs become the same statement."
    - "`MODELS_KEYS` → `readModelsBlock`'s unknown-key refusal (presence) → the new consumption case driving each key through `readModelsConfig` (consumption). Presence and consumption are now two assertions over one tuple rather than one assertion pretending to be two."
    - "`RESOLVED_ASSIGNMENT_KEYS` and `MODEL_ALIASES` → `readAssignmentPayload` → the announcement `adapters-freshness` compares against the degrading-policy authority. The parser now enforces the alias vocabulary its message quotes."
  prohibitions:
    - "The zero-config path is not closed by any floor this plan adds. An absent `overrides`, an absent options object and an explicitly `undefined` `overrides` must all keep resolving the lean default, asserted by a case."
    - "`MODELS_KEYS` gains no member and loses none. This plan changes what is PROVEN about the tuple, never the tuple."
    - "The `tieredCorpusRefusals` sister predicate is not deleted, not weakened and not given a second implementation. It has a production consumer and is the stronger of the two."
    - "Deleting `roleCorpusCardinalityRefusal` may not leave a dangling mention anywhere in shipped source, in a committed `.js`, or inside a guard finding string. A message naming a symbol that does not exist is the same defect class this round is closing."
    - "`relativeToRoot`, `ROOT` and `ROOT_PATH_BOUNDARY` in `scripts/check-foundation-guards.ts` are not edited. This plan's guard change is confined to the two mentions of the deleted symbol."
    - "No `models` block is added to either of grugops's own configuration JSONs, and `agent-factory/seed/.grugops/factory.config.json` is not touched."
    - "`scripts/measure-cost.ts` and `scripts/measure-cost.js` stay byte-unchanged. No unmeasured cost, spend, quota or usage-limit claim in any comment, refusal or message this plan writes."
    - "No caveman voice in any refusal text, docstring or comment — a model tier is a money topic."
    - "`npm test` is never run. The regression command is `npx vitest run --exclude '**/scripts/e2e/**'`."
---

<objective>
Close the four resolver findings — one warning that is a live fail-open on an untyped caller, one
that is a guarantee wider than its mechanism, and two messages stating more than their code enforces.

R2-WR-01 is WR-02 left half-closed. `scripts/model-tiers.ts:1025` iterates
`options?.overrides ?? []` — a nullish coalesce before any shape check — while the docstring twelve
lines above claims the resolver "keeps a last-line alias floor anyway, because this value is written
straight into emitted frontmatter". Reproduced by the round-2 reviewer and again by the round-2
verifier against the committed `.js`: a null `overrides` resolves every stem to `inherit` with no
refusal, and a plain-object `overrides` throws an uncaught `TypeError` out of a module whose header
argues that returning a result rather than throwing is precisely what lets one reader serve the
generator, the installer doctor and any future runtime reader. Plan 29.1-08's own recorded argument
for fixing `preset` rather than deleting its docstring — that the tooling layer ships as committed
`.js` with no type checking on hosts, so a floor holding only under `tsc` is not a floor — applies
verbatim here.

R2-WR-06 is WR-01 restored one level up. `MODELS_KEYS` closes which keys are PERMITTED and nothing
ties the tuple to the keys the reader actually CONSUMES. Add a member with no reader and the block
carrying it is admitted, ignored, and reported as the zero-config answer with no message. The source
comment claims editing the tuple moves the mechanism and the message together; it moves the message.
The oracle's mitigation is a second hand-typed literal beside the first — this repository's named
first failure class, wearing a comment that calls itself derived-adjacent.

R2-IN-02 and R2-IN-04 are the same shape at smaller scale: thirty lines of exported production code
with no production caller, and a refusal that quotes an alias grammar its parser does not enforce.

Output: a resolver whose floors hold for the untyped caller its header names, a tuple whose
membership is proven consumed, one fewer unconsumed authority, and a parser that enforces what it
declares.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-per-role-model-assignment/29.1-CONTEXT.md
@.planning/phases/29.1-per-role-model-assignment/29.1-PATTERNS.md
@.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round2.md
@.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW-round2.md
@.planning/phases/29.1-per-role-model-assignment/29.1-08-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol or path | Kind | Purpose |
|----------------|------|---------|
| `resolveModels` Floor 0b | new refusal arm in `scripts/model-tiers.ts` | refuses an `overrides` value that is present and is not a Map, naming its shape and the contract |
| `MODELS_KEY_PROBES` | new const in `scripts/model-tiers.test.ts` | one probe value per legal `models` key, its key set asserted equal to the tuple in both directions |
| `roleCorpusCardinalityRefusal` | DELETED export in `scripts/model-tiers.ts` | thirty lines of production code with no production consumer; its argument moves into the sister's docstring |
| `readAssignmentPayload` alias check | changed validation in `scripts/model-tiers.ts` | each announced alias checked against the closed alias set, matching the grammar the refusal quotes |

New test case names (`scripts/model-tiers.test.ts`):
`resolveModels REFUSES a null overrides map by name instead of silently discarding it`,
`resolveModels REFUSES a plain-object overrides map by name instead of throwing`,
`resolveModels still treats an ABSENT overrides map as the zero-config answer`,
`every member of MODELS_KEYS is CONSUMED by the reader, not merely permitted`,
`resolvedAssignmentsIn REFUSES an announced alias outside the closed set`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The overrides argument gets the floor the preset argument already has</name>
  <files>scripts/model-tiers.ts, scripts/model-tiers.js, scripts/model-tiers.test.ts</files>
  <behavior>
    - `resolveModels` called with a present-but-nullish `overrides` returns a refusal naming the shape it received and the shape it requires, rather than resolving the lean default.
    - `resolveModels` called with a plain-object `overrides` returns a refusal rather than throwing.
    - An absent `overrides`, an absent options object and an explicitly `undefined` `overrides` all still resolve the zero-config answer, unchanged.
    - A legitimate Map of stem to alias still applies, and an illegal alias inside it is still refused by the existing per-entry floor.
  </behavior>
  <read_first>
    - `scripts/model-tiers.ts` lines 895-935 — Floor 0's preset validation, its `rawPreset`/`preset` split and the paragraph explaining why absent and null are different statements. The new arm follows this shape byte for byte in structure, not by copying its prose.
    - `scripts/model-tiers.ts` lines 968-1000 — `ResolveModelsOptions.overrides`'s docstring and its claim about the last-line alias floor. That claim is what this task makes true.
    - `scripts/model-tiers.ts` lines 1015-1045 — the override-application floor, the `options?.overrides ?? []` iteration and the existing per-entry `isModelAlias` refusal that must keep working unchanged.
    - `scripts/model-tiers.ts` lines 1085-1100 — `describeShape` and `quoteValue`, the module's existing helpers for naming a value's shape back to the caller. Use them; do not write a third.
    - `scripts/model-tiers.ts` lines 1200-1260 — `readModelsConfig`'s override map construction, which is why this defect is latent rather than live. The fix must not change that path's behaviour at all.
    - `scripts/model-tiers.test.ts` lines 1330-1370 — the Floor 0 preset block: the null-preset case with its explanatory cast comment, and the three-spelling green control. The new cases sit in this block and follow its conventions.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW-round2.md` §R2-WR-01 in full, including its reproduction transcript and its proposed refusal wording.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-08-SUMMARY.md` — the WR-02 section, the recorded argument for fixing rather than deleting the docstring. The same argument is quoted, not re-derived, in this task's source comment.
  </read_first>
  <action>
Reproduce both failures FIRST, against the committed `scripts/model-tiers.js`, and record both
transcripts verbatim. One call with a null overrides value, one with a plain-object overrides value,
each with two stems and the `none` preset. The first returns a successful resolution; the second
throws. Those two transcripts are the RED this task inverts.

Add the shape validation beside Floor 0, before the map is built. Read the raw value once, treat a
strictly `undefined` value as the zero-config contract and build an empty Map for it, accept a Map
as-is, and refuse everything else by name — using the module's existing shape-describing helper so
the caller is told what they handed over. The refusal text must state the contract in the same terms
the preset refusal does: an ABSENT overrides map is the zero-config contract, a value that is present
but not a Map is a caller whose overrides would be silently discarded, which is a tier the caller
believes they set and did not. Then iterate the VALIDATED local at the override-application floor
instead of the coalesced expression, so there is one place the value is decided.

MEASURE THE PLACEMENT rather than choosing it, and record what the measurement found. Run the new
arm both before and after the preset floor and record which existing cases move. Report the weaker
true claim in the source comment: state which finding a caller with two simultaneous defects
receives under the chosen order, and why that order was chosen. Plan 29.1-08 did exactly this for
`readModelsBlock` and its paragraph is the model to follow — this plan exists partly because a
validator published a guarantee wider than its mechanism, and choosing a placement without measuring
it would repeat that habit inside the fix.

Update the `overrides` docstring so it states what the code now does. Keep the claim about the
last-line alias floor, because it is now true, and add the sentence the preset docstring carries:
that the declared type is advisory at run time because the tooling layer ships as committed `.js`
with no type checking on hosts, and that a floor holding only under `tsc` is not a floor. Name the
finding id and name the case that asserts it.

Add the three cases beside the existing preset cases. Two refusals — nullish and plain object — each
asserting the refusal names the shape it received and names the required shape, and each written with
the same explanatory cast comment the null-preset case carries, because the declared option type does
not admit these values and the cast standing in for the untyped host caller IS the point. One green
control asserting the three absent spellings still resolve the zero-config answer, compared against
the same stringified map the existing control uses.

Run `npm run build` and commit `scripts/model-tiers.js` in the SAME commit as the `.ts`.
  </action>
  <verify>
    <automated>npx vitest run scripts/model-tiers.test.ts && npm run typecheck && npm run build && git diff --exit-code --name-only -- '*.js'</automated>
  </verify>
  <acceptance_criteria>
    - RED-first transcripts recorded verbatim against the PRE-FIX committed `scripts/model-tiers.js`: a null `overrides` returns a successful resolution mapping every stem to the lean default, and a plain-object `overrides` throws with its error text quoted. Both with the exact invocation used.
    - GREEN transcripts against the POST-FIX committed `.js`: both calls return a refusal, and each refusal names the shape received and the shape required. Quoted in full.
    - Mutation-proven discrimination: revert the validation to the coalesced expression, rebuild, observe both refusal cases go RED, record the failure text, restore.
    - The zero-config path is proven open: the three absent spellings each resolve to the same stringified map as the existing preset control, asserted by a case, and that case is shown to be capable of failing by temporarily making the new arm refuse `undefined` and observing it go RED. Restore afterwards.
    - The placement measurement is recorded: which existing cases moved under each of the two orders, which order was chosen, and which finding a caller with two simultaneous defects receives. If no case moved under either order, that is recorded as the measured result rather than left unstated.
    - The live path is proven unaffected: `readModelsConfig` over a fixture carrying a `models.roles` block still returns the same override map, asserted by an existing case that must pass unmodified.
    - `node scripts/generate-role-adapters.js` then `git status --porcelain -- .claude/agents/` prints nothing.
    - `npm run typecheck` exits 0. After `npm run build`, `git diff --exit-code --name-only -- '*.js'` is empty.
  </acceptance_criteria>
  <done>A present-but-wrong `overrides` value is refused by name rather than silently discarded or thrown, the absent case is unchanged and asserted, and the docstring states what the code does.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Every legal `models` key is proven consumed, and the comment claims only what the tuple buys</name>
  <files>scripts/model-tiers.ts, scripts/model-tiers.js, scripts/model-tiers.test.ts</files>
  <behavior>
    - Setting each legal `models` key, one at a time, changes the reader's result against a baseline block with no keys.
    - The probe table's key set equals `MODELS_KEYS` in both directions, so a key added to the tuple with no probe fails the case rather than passing over nothing.
    - A key added to `MODELS_KEYS` with no reader is caught by the consumption case rather than admitted, ignored and reported as the zero-config answer.
    - The existing presence closure — an unknown key refused by name against the tuple — is unchanged.
  </behavior>
  <read_first>
    - `scripts/model-tiers.ts` lines 212-235 — the `MODELS_KEYS` declaration and the paragraph above it, including the sentence claiming that editing the tuple moves the mechanism and the message together. That sentence is what this task corrects.
    - `scripts/model-tiers.ts` lines 1130-1200 — `readModelsBlock`'s unknown-key refusal, its placement paragraph, and the two reads that follow it. Confirm by reading which keys are actually consumed before writing a probe for each.
    - `scripts/model-tiers.ts` lines 1200-1260 — `readModelsConfig`'s two-location resolution and its returned value shape, which is what the consumption case compares.
    - `scripts/model-tiers.test.ts` lines 520-565 — `scratchRoot`, `writeConfigAt`, `rootWithConfig`, `rootWithRawConfig` and `refusalOrFail`, the fixture helpers this case must reuse rather than duplicate.
    - `scripts/model-tiers.test.ts` lines 1285-1310 — the file-level residual case and the existing `MODELS_KEYS` case with its two hand-typed literals and its derived-adjacent claim. That case is the second hand-written list the finding names.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW-round2.md` §R2-WR-06 in full, including its proposed case and its argument about the two-sided pin whose two sides are edited together by construction.
  </read_first>
  <action>
Add the consumption case. Build a probe table mapping each legal key to a value that, when set alone
in a `models` block, must visibly change the reader's answer — a preset name for the preset key, and
a one-entry role map keyed on a stem derived from the kit authority for the roles key. Do not hand-type
a stem; derive it.

Assert the table's coverage of the tuple BEFORE the loop that consumes it, in both directions, and
say in the case body why the assertion comes first: a key added to the tuple with no probe is a key
nobody proved is read, and a loop over the probe table alone would pass over exactly that omission.
This is the round's "derive the set, assert the count" rule applied to a probe table rather than to a
scan set.

Read a baseline through `readModelsConfig` over a fixture whose `models` block is empty, then for
each key read a fixture carrying only that key and require the result to DIFFER from the baseline.
Compare the two facts the reader returns — the resolved preset and the override entries — as one
stable serialisation, so the comparison cannot pass on an incidental difference in an unrelated
field. Assert the baseline itself is a successful read before comparing anything to it; a baseline
that was a refusal would make every comparison differ for the wrong reason.

Prove the case discriminates by mutation, in the direction the finding names: add a third member to
`MODELS_KEYS` with no reader and no probe, rebuild, and observe the case go RED on the coverage
assertion naming the unprobed key. Then add a probe for it, rebuild, and observe the case go RED
again on the consumption assertion naming the key that changed nothing. Both reds are required — the
first proves the table cannot silently fall behind the tuple, the second proves the tuple cannot
silently outrun the reader. Revert and confirm the tuple is byte-restored.

Correct the source comment above `MODELS_KEYS`. Delete the claim that editing the tuple moves the
mechanism and the message together and replace it with the weaker true claim: editing the tuple moves
the message, and the mechanism is moved by the consumption case, which is named. Then correct the
existing tuple case's own comment, which calls its hand-typed literal derived-adjacent: state that it
is a second hand-written list, that its value is making an addition visible in a diff, and that the
consumption case is what makes an unread addition fail. A case that describes itself accurately is
the minimum this round is asking for everywhere else.

Run `npm run build` and commit `scripts/model-tiers.js` in the SAME commit as the `.ts`.
  </action>
  <verify>
    <automated>npx vitest run scripts/model-tiers.test.ts && npm run typecheck && npm run build && git diff --exit-code --name-only -- '*.js'</automated>
  </verify>
  <acceptance_criteria>
    - The consumption case asserts the probe table's key set equals `MODELS_KEYS` in BOTH directions, and that assertion is placed before the consumption loop. Confirm by reading the case and quoting the ordering in the SUMMARY.
    - The case asserts the baseline read succeeded before comparing anything against it.
    - Mutation transcript one: a third member added to `MODELS_KEYS` with no probe makes the case RED on the coverage assertion, naming the unprobed key. Recorded verbatim.
    - Mutation transcript two: the same third member WITH a probe but no reader makes the case RED on the consumption assertion, naming the key that changed nothing. Recorded verbatim. This is the transcript that proves the finding is closed, and the case is not complete without it.
    - After reverting, `git diff -- scripts/model-tiers.ts` shows no residue of the third member and `npx vitest run scripts/model-tiers.test.ts` exits 0.
    - The corrected comment above `MODELS_KEYS` no longer claims the tuple moves the mechanism. Quote the sentence before and after in the SUMMARY.
    - The existing tuple case's comment describes itself as a second hand-written list with its actual value stated, and names the consumption case. Quote before and after.
    - The presence closure is unchanged: the existing case refusing an unknown `models` key by name against the tuple passes unmodified.
    - `npm run typecheck` exits 0. After `npm run build`, `git diff --exit-code --name-only -- '*.js'` is empty.
  </acceptance_criteria>
  <done>A key admitted into the tuple with no reader now fails a case naming it, and no comment in the module claims the tuple buys more than it does.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delete the unconsumed cardinality authority, and make the payload parser enforce the grammar it quotes</name>
  <files>scripts/model-tiers.ts, scripts/model-tiers.js, scripts/model-tiers.test.ts, scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js</files>
  <behavior>
    - `roleCorpusCardinalityRefusal` no longer exists, and no shipped source, committed twin, comment or guard finding names it.
    - The argument the deleted function carried — why a cardinality answer is the weaker question and when a consumer would want it — survives in the sister predicate's docstring.
    - `resolvedAssignmentsIn` refuses an announced payload whose `aliases` array carries a string outside the closed alias set, naming the offending value and the legal set.
    - The freshness gate's live announcement still reads back successfully and the zero-config alias comparison is unchanged.
  </behavior>
  <read_first>
    - `scripts/model-tiers.ts` lines 1370-1408 — `roleCorpusCardinalityRefusal` in full, including the docstring paragraph stating plainly that it has no production consumer and the paragraph explaining why the sister predicate is the stronger of the two. That second paragraph is the argument to relocate.
    - `scripts/model-tiers.ts` lines 715-725, 790-800 and 898-905 — the three comment mentions of the deleted symbol inside the module.
    - `scripts/model-tiers.ts` — the `tieredCorpusRefusals` docstring, the sister that keeps its production consumer. The relocated argument lands here.
    - `scripts/check-foundation-guards.ts` line 1960 — the `MODEL_TIERS_COUNT` finding text, whose consumer list names the deleted symbol INSIDE a shipped message. A message naming a symbol that does not exist is the defect class this round is closing, so this string moves in the same commit.
    - `scripts/check-foundation-guards.ts` line 2051 — the comment mentioning the deleted symbol beside the `tieredCorpusRefusals` production consumer.
    - `scripts/model-tiers.ts` lines 440-530 — `RESOLVED_ASSIGNMENT_KEYS`, `resolvedAssignmentsIn`, `readAssignmentPayload` and the refusal text quoting the declared shape with its alias placeholder.
    - `scripts/model-tiers.ts` lines 145-160 — `isModelAlias` and `MODEL_ALIASES`, the closed set the parser must consult.
    - `scripts/adapters-freshness.ts` lines 310-370 — the zero-config alias derivation and the announced-alias comparison, the one live consumer of the payload reader. Confirm by reading that a stricter member check cannot false-red a legitimate announcement.
    - `scripts/model-tiers.test.ts` lines 130-155 — the two cases that consume the deleted symbol and must be removed with it.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW-round2.md` §R2-IN-02 and §R2-IN-04 in full.
  </read_first>
  <action>
Take the deletion inventory FIRST and record it, so the removal is complete by derivation rather than
by memory. Run a repository-wide search for the symbol across `scripts/`, `install/` and `hooks/`,
including the committed `.js` twins, and list every hit with its file and line. That list is the
checklist the removal is measured against, and it is reproduced in the SUMMARY with a second search
after the deletion returning only the planning artefacts that record the decision.

Relocate the argument before deleting the code. Move the paragraph explaining when a consumer would
genuinely want a cardinality answer, and why the sister is the stronger question, into the sister
predicate's docstring, together with one sentence recording that a weaker unconsumed twin existed,
was found to have no production caller for a second consecutive round, and was deleted rather than
given a caller — because this repository's rule is to delete the second authority rather than teach
it a case. Name the finding id.

Then delete the function, its export, and the two cases that consume it. Remove the three in-module
comment mentions. Remove the mention from the guard's comment at line 2051. Remove the symbol from
the guard's consumer-list finding text at line 1960, leaving the remaining consumers named and the
sentence still reading correctly — verify by printing the finding text after the edit rather than by
reading the diff.

For the payload parser, add the alias-legality check to the member loop that today checks only the
string type, consulting the module's existing closed-set predicate rather than writing a second
membership test. The refusal must name the offending value and the legal set, matching how every
other alias refusal in this module words it. Do not change the declared-shape sentence: it already
says what the parser will now enforce, and this finding is about the code catching up to the message
rather than the message retreating to the code. Record that disposition and its reason in the source
comment, naming the finding id, and state the rejected alternative — rewording the declared shape to
a looser grammar — with why it was rejected.

Add the case: an announced payload whose alias array carries a legally-typed but illegal value is
refused, naming the value and the legal set. Assert the case's own premise first — that the payload
is otherwise well-formed, so the refusal is attributable to the alias and not to a malformed
envelope. Prove the discrimination by reverting the member check and observing the case go RED,
then restore.

Prove the live path is unaffected: run the freshness gate and confirm the announcement still reads
back successfully and the alias comparison still passes. A stricter member check that false-redded a
legitimate announcement would be a regression shipped inside a fix, which is how this phase acquired
its blocker.

Run `npm run build` and commit both `scripts/model-tiers.js` and `scripts/check-foundation-guards.js`
in the SAME commit as their `.ts` sources.
  </action>
  <verify>
    <automated>npx vitest run scripts/model-tiers.test.ts scripts/adapters-freshness.test.ts scripts/check-foundation-guards.test.ts && node scripts/check-foundation-guards.js && npm run freshness:adapters && npm run typecheck && npm run build && git diff --exit-code --name-only -- '*.js'</automated>
  </verify>
  <acceptance_criteria>
    - The deletion inventory is recorded before and after: `grep -rn 'roleCorpusCardinalityRefusal' scripts/ install/ hooks/` lists every hit before the change, and returns nothing after it. Both outputs in the SUMMARY.
    - The guard's consumer-list finding text is printed after the edit and quoted in the SUMMARY, showing the remaining consumers named and the sentence reading correctly. Confirm by triggering or reconstructing the message, not by reading the diff alone.
    - The relocated argument is present in the sister predicate's docstring and names the finding id. Quote it.
    - `tieredCorpusRefusals` is unchanged and still called unconditionally by `guard_model_assignment`: confirm the call site is present and the guard still exercises it on this repository's zero-config tree.
    - The alias-legality case is RED against the pre-change parser: recorded by reverting the member check, rebuilding and capturing the failure, then restoring.
    - The alias-legality case asserts its payload is otherwise well-formed before asserting the refusal, so the red is attributable to the alias.
    - The live announcement path is unaffected: `npm run freshness:adapters` exits 0 and its announced alias set still matches the degrading-policy authority's. Record the gate's own line.
    - `node scripts/check-foundation-guards.js` exits 0, and `diff <(node scripts/check-foundation-guards.js 2>&1) <(CHECK_ROOT=. node scripts/check-foundation-guards.js 2>&1)` still prints nothing — plan 29.1-12's byte identity survives this plan's guard edits.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0; the suite's total is recorded before and after, with the two deleted cases accounted for in the difference.
    - `npm run typecheck` exits 0. After `npm run build`, `git diff --exit-code --name-only -- '*.js'` is empty. `npm run freshness` reports every committed output fresh.
  </acceptance_criteria>
  <done>One fewer exported authority with no consumer, no shipped message naming a symbol that does not exist, and a payload parser that enforces the alias grammar its refusal quotes.</done>
  <reversibility rating="costly">Deleting an exported symbol from a module the installer doctor and future runtime readers consume is recoverable from git, but a host running an older committed twin would carry a different export surface than the source; the relocation of its argument is what makes the deletion recoverable as a decision rather than only as a diff.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untyped JavaScript caller → `resolveModels` | The tooling layer ships as committed `.js` and runs on hosts with no type checking, so a declared type is advisory at run time |
| user-authored configuration JSON → `readModelsConfig` | Every value in the `models` block arrives from a file a user edits by hand |
| resolved alias → emitted frontmatter | A resolved value is written straight into an adapter the platform loads |
| shipped refusal message → the operator | A message naming a symbol or a grammar the code does not have sends a reader to a fact that is not there |

## STRIDE Threat Register

ASVS level 1, block on `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-45 | Tampering | a present-but-non-Map `overrides` silently discarded | high | mitigate | Floor 0 validates the shape before it defaults and refuses by name; proven by two RED-first reproductions against the committed `.js` and by a mutation that restores the coalesce. |
| T-29.1-46 | Denial of service | an uncaught `TypeError` out of a module whose contract is to return a result | medium | mitigate | The same floor converts the throw into a named refusal; the installer doctor and any future runtime reader keep the degrading contract the module header promises. |
| T-29.1-47 | Tampering | a `models` key admitted with no reader, reported as the zero-config answer | high | mitigate | The consumption case drives every tuple member through the reader and requires the result to change, with the probe table's coverage asserted in both directions; both mutation directions recorded. |
| T-29.1-48 | Repudiation | a shipped message naming a symbol that does not exist | medium | mitigate | The deletion inventory is derived by search before and after, and the guard's consumer-list finding text is printed after the edit rather than inspected as a diff. |
| T-29.1-49 | Tampering | an announced alias outside the closed set read back as legal | medium | mitigate | The payload parser consults the module's existing closed-set predicate; the live announcement path is re-run to prove no false red. |
| T-29.1-05 | Tampering | committed `scripts/*.js` diverging from its `.ts` | high | mitigate | `npm run build` plus `git diff --exit-code --name-only -- '*.js'` in every task verify; `npm run freshness` in the plan verification. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | accept | Zero packages installed by this plan; `package.json` asserted byte-unchanged in the plan verification. |
</threat_model>

<verification>
- `npx vitest run scripts/model-tiers.test.ts scripts/adapters-freshness.test.ts scripts/check-foundation-guards.test.ts` exits 0.
- `node scripts/check-foundation-guards.js` exits 0, and the relative-root byte identity from plan 29.1-12 still holds.
- `npm run freshness:adapters` exits 0. `node scripts/generate-role-adapters.js` then `git status --porcelain -- .claude/agents/` prints nothing.
- `grep -rn 'roleCorpusCardinalityRefusal' scripts/ install/ hooks/` returns nothing.
- `npm run freshness` reports every committed output fresh. `npm run typecheck` exits 0. `npm run build` then `git diff --exit-code --name-only -- '*.js'` is empty.
- `npx vitest run --exclude '**/scripts/e2e/**'` exits 0. `npm test` is never run.
- `git diff --name-only -- install/ package.json scripts/measure-cost.ts scripts/measure-cost.js agent-factory/ .github/` is empty.

</verification>

<success_criteria>
- Both `overrides` reproductions are inverted, with the pre-fix and post-fix transcripts recorded side by side.
- The carried-forward warning about container-level `overrides` validation is closed this round rather than carried again.
- A key added to `MODELS_KEYS` with no reader reds a case; both mutation directions are recorded.
- No shipped source, committed twin, comment or finding names the deleted symbol, proven by a search before and after.
- The payload parser enforces the alias grammar its refusal quotes, with the live announcement path proven unaffected.
</success_criteria>

<output>
Create `.planning/phases/29.1-per-role-model-assignment/29.1-15-SUMMARY.md` when done.
Record in it: both pre-fix `overrides` reproductions and both post-fix refusals verbatim; the
placement measurement and which order was chosen with its reason; both `MODELS_KEYS` mutation
transcripts; the corrected `MODELS_KEYS` comment quoted before and after; the deletion inventory
before and after; the guard's consumer-list finding text as printed after the edit; the relocated
argument's new home; the alias-legality RED transcript; and the suite totals before and after with
the two removed cases accounted for.
</output>

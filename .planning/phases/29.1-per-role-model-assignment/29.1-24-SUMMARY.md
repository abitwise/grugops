---
phase: 29.1-per-role-model-assignment
plan: 24
subsystem: testing
tags: [ci-workflow, one-authority, mutation-testing, testkit, build-parity, typescript, vitest]

requires:
  - phase: 29.1-per-role-model-assignment
    provides: "the (r-class-prefix) class check and its derived member set (plan 29.1-19), and the round-4 measurement that it could not fail for either member"
provides:
  - "scripts/ci-workflow.testkit.ts — the ONE authority over where the ubuntu gate block is and what it runs, consumed by both readers and by no production script"
  - "(r-base-discriminating) — the slice base proven by two COMPUTED regions rather than by a scan over source text"
  - "(r-class-authority) — membership derived from the IMPORT, pinned two-sided, closed by a negative over every non-member and by a derived no-production-consumer scan"
  - "The exact R3-IN-03 divergence now MOVES the failure set, measured against a baseline pinned in the same session"
  - "Round 4's suggested cheap repair recorded as MEASURED FALSE rather than silently skipped"
  - "WINDOWS row 80 amended: its mitigating clause was measured false and is replaced, not softened"
affects: [check-foundation-guards, skill-twins-freshness, phase-29.1-verification-round-5]

actuals:
  tokens: 25149
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Delete the second grammar rather than widen the first — one implementation, both readers import it, and the class property becomes true by construction"
    - "A test-only *.testkit.ts is the answer to 'sharing means adding a shipped surface' — and the no-production-consumer property is asserted by a derived scan, never by the module header"
    - "Prove a region reader by comparing COMPUTED regions against an inline control built from the divergent base, never by scanning for a canonical source spelling"
    - "A predicate whose subject supplies the text it searches for cannot fail — check whether the constant that STATES a requirement is declared in the file the requirement reads"
    - "Measure which arms of a multi-arm case actually discriminate; state the arms that stay green by construction as PREMISES rather than counting them as proofs"
    - "Move a derived corpus pin in the SAME commit that adds the module, and re-derive the number independently rather than incrementing it"

key-files:
  created:
    - scripts/ci-workflow.testkit.ts
    - scripts/ci-workflow.testkit.js
  modified:
    - scripts/check-foundation-guards.test.ts
    - scripts/skill-twins-freshness.test.ts
    - .planning/WINDOWS.md

key-decisions:
  - "Refuse round 4's suggested cheap repair on measurement — stripping const declaration lines makes the right-bound requirement red a CORRECT tree, because the only place either member spells those bytes IS the declaration"
  - "Delete both containment requirements rather than repair them, and close the class by construction instead"
  - "Land the class-case replacement in Task 1's commit rather than Task 2's, because (r-class-prefix) is structurally dead the instant the second grammar is deleted and cannot survive the cutover in any form"
  - "State arm 3 of (r-base-discriminating) as a PREMISE about the control rather than as a third discriminator, because it was measured to stay green under the mutation by construction"
  - "Disclose that vitest resolves the testkit specifier to the COMMITTED .js, so a .ts-only divergence is caught by freshness and not by these cases — with the freshness exit code measured rather than assumed"

patterns-established:
  - "Pin the mirror baseline in the SAME session and compare the mutated failure set against it — never against a number read out of a report"
  - "When a plan reverses a recorded disposition, the reversal travels with the measurement that forced it, in both the source comment and the ledger"

requirements-completed: [MODEL-01, MODEL-05]

coverage:
  - id: D1
    description: "One implementation of the ubuntu gate block region exists and both readers consume it — the step name, the derived prefix, the step marker, the workflow path, the region reader, the command reader and the synthetic builder each have exactly one home"
    requirement: MODEL-05
    verification:
      - kind: unit
        ref: "scripts/check-foundation-guards.test.ts#(r-class-authority) every reader of the ubuntu gate block imports the one authority, and nothing outside that set locates the block"
        status: pass
      - kind: integration
        ref: "grep -rl 'Freshness gates + repo gates' scripts/*.ts — returns exactly scripts/ci-workflow.testkit.ts"
        status: pass
    human_judgment: false
  - id: D2
    description: "The exact divergence R3-IN-03 named now MOVES the failure set, where round 4 measured it byte-for-byte invisible"
    requirement: MODEL-05
    verification:
      - kind: unit
        ref: "scripts/check-foundation-guards.test.ts#(r-base-discriminating) the region begins after the step name, proven by two computed regions rather than by source text"
        status: pass
      - kind: integration
        ref: "hermetic git-archive mirror at 8465fa2, base shifted in both .ts and .js — baseline 4 failed / 275 passed of 279 becomes 5 failed / 274 passed of 279 with the case named"
        status: pass
    human_judgment: false
  - id: D3
    description: "Membership is derived from the IMPORT, pinned two-sided at 2, and closed by a negative over all 48 non-member test files"
    requirement: MODEL-05
    verification:
      - kind: integration
        ref: "planted third importing reader on a mirror — count pin reds naming the planted file; planted non-member carrying its own locator — negative reds naming it"
        status: pass
    human_judgment: false
  - id: D4
    description: "No production script imports the authority, asserted by a derived scan over 147 files rather than claimed in the module header"
    requirement: MODEL-01
    verification:
      - kind: integration
        ref: "planted import of the testkit into scripts/kit-model.ts on a mirror — the scan reds naming scripts/kit-model.ts"
        status: pass
    human_judgment: false
  - id: D5
    description: "The committed .js twin is built and committed in the same commit as its source, and npm run freshness exits 0 with the twin among the 50 outputs it verifies"
    requirement: MODEL-05
    verification:
      - kind: integration
        ref: "npm run freshness — exit 0, compared set moved 49 to 50 paths, the new path being scripts/ci-workflow.testkit.js"
        status: pass
    human_judgment: false
  - id: D6
    description: "Round 4's suggested cheap repair is recorded as measured-false, with the numbers, in the case comment and in the ledger"
    requirement: MODEL-05
    verification:
      - kind: integration
        ref: "declaration-line strip measured over both members — right-bound marker occurrences fall from 1 to 0 in each"
        status: pass
    human_judgment: false
  - id: D7
    description: "WINDOWS row 80 states the measured fact and names a mitigation that exists; two new rows record the reversed disposition and the build-output finding"
    verification: []
    human_judgment: true
    rationale: "Whether an amended ledger row states the measured fact HONESTLY, and whether replacing a false mitigating clause rather than softening it is the right editorial act for an auditable trail, is a reading of prose against a governance rule. That the row exists, carries the numbers and names mechanisms that exist is checkable; that it is the right record is a judgement."
  - id: D8
    description: "The authority these tests EXECUTE is the committed .js, not the .ts — so a .ts-only divergence is invisible to every case in this class and is caught by npm run freshness instead"
    verification:
      - kind: integration
        ref: "probe pair on hermetic mirrors — .js-only mutation reds (r-base-discriminating), .ts-only mutation leaves it green; npm run freshness exits 1 on the .ts-only mutation naming STALE WORKING OUTPUT"
        status: pass
    human_judgment: true
    rationale: "The mechanism is measured and the mitigation was RUN rather than assumed, which is exactly what row 80 failed to do. What a human must rule on is whether a test class whose subject is the build output — rather than the source a developer edits — is the right arrangement, or whether a later plan should point these cases at the .ts as well."

duration: 41 min
completed: 2026-08-21
status: complete
---

# Phase 29.1 Plan 24: One Region, One Authority Summary

**The class check that certified "two readers read one region" could not fail for either reader, so the second grammar was deleted rather than the first widened: one testkit module now answers where the ubuntu gate block is and what it runs, both readers import it, and the divergence that produced a byte-identical failure set in round 4 now reds by name.**

## Performance

- **Duration:** 41 min
- **Started:** 2026-08-21T14:55:00Z
- **Completed:** 2026-08-21T15:36:44Z
- **Tasks:** 3
- **Files modified:** 5 (2 created, 3 modified)

## Accomplishments

- **BLOCKER 3 closed structurally.** One implementation of the region, in `scripts/ci-workflow.testkit.ts`. Both readers import it. "This class reads one region" is now true by construction rather than certified by comparing source spellings a member's own declaration supplies.
- **The divergence R3-IN-03 named MOVES the failure set.** Measured on a hermetic mirror of the final tree against a baseline pinned in the same session: 4 failed → 5 failed, `(r-base-discriminating)` named. Round 4's measurement of the identical mutation: byte-for-byte unchanged.
- **Round 4's suggested cheap repair was refused on measurement, not on taste.** After stripping `const NAME =` declaration lines, the right-bound marker's canonical spelling occurs **zero** times in **both** members. The repaired requirement would red a correct tree.
- **Membership became a mechanism.** Derived from the import, pinned two-sided at 2, closed by a negative over all 48 non-members and by a derived scan proving no production script consumes the authority.
- **A finding the plan did not predict:** vitest resolves the testkit's `.js` specifier to the **committed build output**, so a `.ts`-only divergence is invisible to these cases. Measured both directions, and the mitigation (`npm run freshness`) was **run** rather than named.
- **Row 80's false mitigating clause replaced, not softened**, plus two new ledger rows.

## Task Commits

1. **Task 1: one implementation of the region, consumed by both readers** — `56653ca` (test)
2. **Task 2: the base proven by computed regions, not by source text** — `98a1882` (test)
3. **Task 3: the ledger states what was measured** — `8465fa2` (docs)

## The session-pinned baselines

Measured at `ccb2d9c` **before any edit**, in this session. Every claim below compares against these.

| Subject | Result |
|---|---|
| `scripts/check-foundation-guards.test.ts` alone | 1 file passed — **264 passed (264)**, 0 failed |
| `scripts/skill-twins-freshness.test.ts` alone | 1 file passed — **14 passed (14)**, 0 failed |
| `npx vitest run --exclude '**/scripts/e2e/**'` | **55 files** (1 failed / 54 passed) — **2395 tests: 1 failed, 2392 passed, 2 skipped** |
| The one baseline failure | `scripts/frontmatter.test.ts > D-49 false-red control` — the pre-existing defect in WINDOWS row 91, **not caused or fixed here** |
| Hermetic `git archive HEAD` mirror, `check-foundation-guards.test.ts` | **4 failed / 260 passed (264)** — the 4 are git-dependent cases a non-git mirror cannot run |

**Final tree (`8465fa2`):** 55 files, **2396 tests: 1 failed, 2393 passed, 2 skipped.** Delta: **+1 test, +1 passing, failure set IDENTICAL** — the same single pre-existing refusal, over the same 1442-file corpus. Verified by reading the refusal list itself, not only the count.

## The pre-fix reproduction — the blocker, reproduced before anything changed

Hermetic `git archive HEAD` mirror at `ccb2d9c`. The exact divergence: `ci.slice(at + UBUNTU_BLOCK_STEP_NAME.length)` → `ci.slice(at)`.

```
byte delta: -32
re-read mutated line: 11002:    const rest = ci.slice(at);

independent measurement of the two bases on the committed workflow:
  region bytes: at+name.length = 25639   bare at = 25681   delta = 42 (step name length = 42)
  command entries: 20 vs 21
  bad  first entry: "Freshness gates + repo gates (ubuntu only)"
  good first entry: "if: matrix.os == 'ubuntu-latest'"

mirror baseline : 4 failed | 260 passed (264)
mirror MUTATED  : 4 failed | 260 passed (264)   <- byte-for-byte identical, same four names
```

The mutation is semantically real — measured independently, 42 bytes and one command entry — and **no case in the file saw it.** This is the pre-fix evidence.

## The measurement that falsified round 4's suggested cheap repair

Round 4's `missing` list proposed stripping `const <NAME> = …` declaration lines before the containment tests. Measured against this tree:

| File | right-bound marker occurrences (raw) | after stripping const declarations |
|---|---|---|
| `scripts/check-foundation-guards.test.ts` | 1 | **0** |
| `scripts/skill-twins-freshness.test.ts` | 1 | **0** |

The only place either file spelled `\n      - name: ` **was** the declaration. Requirement 1 would therefore fail on a correct tree. Round 4's `missing` list is input, not gospel — and this is the first thing the plan refused to take on faith. (The slice-base requirement survives the strip at 1 occurrence each, so only one of the two halves would have been repaired even if the strip had been safe.)

## The authority and its export list

`scripts/ci-workflow.testkit.ts` — seven exports:

```
export const UBUNTU_BLOCK_STEP_NAME    = "Freshness gates + repo gates (ubuntu only)";
export const STEP_MARKER               = "\n      - name: ";
export const UBUNTU_BLOCK_STEP_PREFIX  = UBUNTU_BLOCK_STEP_NAME.slice(0, …indexOf(" ("));
export function ciWorkflow(): string
export function ubuntuBlock(ci: string): string
export function ubuntuBlockCommands(ci: string): string[]
export function withAppendedStep(ci: string, stepName: string, command: string): string
```

Its header states the three required things: that it is the ONE authority and a second implementation is a defect rather than a duplication; that it is consumed by tests only and imported by no production script, which is why it is a testkit rather than a promotion into a shipped module; and that it exists because a class check comparing source spellings was measured structurally incapable of failing for either member.

**One spelling of the step name, verified:**

```
$ grep -c 'function ubuntuBlock' scripts/check-foundation-guards.test.ts   -> 0
$ grep -c 'function ubuntuBlock' scripts/skill-twins-freshness.test.ts     -> 0
$ grep -rl 'Freshness gates + repo gates' scripts/*.ts
scripts/ci-workflow.testkit.ts
```

## The derived member set and its pin

```
scripts/*.test.ts scanned: 50 | members: 2
  ["check-foundation-guards.test.ts","skill-twins-freshness.test.ts"]
non-members scanned by the negative: 48
```

Membership is `src.includes("./ci-workflow.testkit.js")` — the IMPORT, not a phrase a comment can also contain. Pinned two-sided at `UBUNTU_BLOCK_READER_COUNT = 2`. The negative's needle is built from the imported constant, so the scanning file does not carry the phrase it scans for and cannot exempt itself by accident.

## The five arms of `(r-base-discriminating)`, and which of them discriminate

Assertions in order: (1) both lists non-empty **and** the authority's list equals a re-derivation from its own region; (2) the two regions differ **and** the two command lists differ; (3) the control's first entry is the step name; (4) the authority's list and raw region carry the step name nowhere; (5) the control is exactly one entry longer, and the two agree on everything after that entry.

Measured under the base shift, arm by arm:

| Arm | at HEAD | base shifted |
|---|---|---|
| 1 vacuity (auth 20, ctrl 21) | PASS | PASS |
| 2 regions differ | PASS | **RED** |
| 2 command lists differ | PASS | **RED** |
| 3 control[0] is the step name | PASS | PASS |
| 4 authority carries the name nowhere | PASS | **RED** |
| 5 control is exactly one longer | PASS | **RED** |

**Four arms red independently.** Arm 3 stays green **by construction** — it is a property of the CONTROL, which the mutation does not touch. The plan expected it to be a third discriminator; it is not, and the case comment was **corrected to call it a premise** rather than left claiming more than the measurement supports.

## The four discrimination transcripts

Every mutation landed in a `git archive HEAD` mirror. `.github/workflows/ci.yml` was never written to; the real tree was never mutated for any of these.

**T1 — right bound removed from the authority** (`.ts` delta −78, `.js` delta −80, re-read `return rest;`)
```
baseline 4 failed | 275 passed (279)  ->  6 failed | 273 passed (279)
  × (r-bound-synthetic) the reader stops at the next step, proven on a workflow that HAS one
  × Case 8b: the ubuntu block reader stops at a following step, proven on a synthetic workflow
```
Both members' bound proofs now red on **one** authority mutation — strictly stronger than before, when each proved only its own private reader.

**T2 — slice base shifted to the bare locator index** (`.ts` −32, `.js` −32)
```
baseline 4 failed | 275 passed (279)  ->  5 failed | 274 passed (279)
  × (r-base-discriminating) …
  AssertionError: PREMISE: the two bases must produce DIFFERENT regions on the committed
  workflow. They came back byte-identical, which means this case is no longer measuring a base.
```

**T3 — a third `.test.ts` importing the authority** (+141 bytes)
```
  × (r-class-authority) …
  AssertionError: the number of test files importing the ubuntu-block authority is pinned at 2;
  found 3: ["check-foundation-guards.test.ts","skill-twins-freshness.test.ts",
  "zz-planted-third-reader.test.ts"]
```

**T4 — a non-member carrying its own locator, importing nothing** (+181 bytes, 0 occurrences of the specifier)
```
  × (r-class-authority) …
  AssertionError: these test files locate the ubuntu gate block by their own copy of its step
  name without importing ./ci-workflow.testkit.js: ["zz-planted-second-grammar.test.ts"].
  49 non-member(s) were scanned.
```

**T5 (beyond the plan) — a production script importing the testkit** (`scripts/kit-model.ts`, +87 bytes)
```
  × (r-class-authority) …
  AssertionError: scripts/ci-workflow.testkit.ts is a TESTKIT: … These non-test files import or
  name it: ["scripts/kit-model.ts"]. 147 file(s) were scanned across ["scripts","install","hooks"].
```
This is the assertion that answers the reversed disposition's second horn, so it was proven to discriminate rather than left as a green light.

**After every transcript:** `git diff --exit-code -- scripts/` → exit 0, clean.

## The reversed disposition, in both files

Both members carried a paragraph arguing the helper was deliberately not shared. Both are rewritten in place, describing the superseded reasoning rather than deleting it. The two measurements that forced the reversal are named in each:

> *(check-foundation-guards.test.ts)* "Round 4 applied the exact divergence R3-IN-03 named … and ran the file: 4 failed | 260 passed (264), BYTE-FOR-BYTE the unmutated mirror's failure set … The scan the disposition bought was structurally incapable of failing for either member, because the constants that STATE its canonical spellings are declared in the very files it searches. Round 4's suggested cheap repair … was then measured against this tree and REDS A CORRECT TREE: after stripping, the right-bound marker's canonical spelling occurs ZERO times in BOTH members."

> *(skill-twins-freshness.test.ts)* "The original argument's first horn was what the tree already had. Its second horn is answered by scripts/ci-workflow.testkit.ts, a module NO production script imports — and `(r-class-authority)` … asserts that by a derived scan rather than by this sentence. So the class is now closed by CONSTRUCTION."

The stale cross-reference is corrected: `skill-twins-freshness.test.ts` now names `(r-class-authority)` for the class, `(r-base-discriminating)` for the base and `Case 8b` for the bound. Remaining mentions of `(r-class-prefix)` in either file are historical, describing the superseded case — this repository annotates rather than rewrites.

## The ledger

**Row 80, amended** — status `open` → `fixed`. Its description now opens by naming itself wrong and quotes the clause that was measured false rather than continuing to assert it:

> "29.1-19 residual, AMENDED BY PLAN 29.1-24 BECAUSE THE ROW ITSELF WAS MEASURED WRONG. … and — THE CLAUSE THAT WAS MEASURED FALSE — bounded for today's two members by their own synthetic-input bound proofs, (r-bound-synthetic) and Case 8b. THAT MITIGATION DID NOT EXIST. Round 4 falsified it directly: (r-bound-synthetic)'s negative control builds its own inline slice from the SAME base as the reader, and its effect assertion .not.toContain(APPENDED_STEP_NAME) is still satisfied by a base-shifted read … THE ROW ALSO UNDERSTATED THE HOLE by framing it as a HYPOTHETICAL future member. … The false clause is REPLACED here rather than softened, because a ledger row naming a mitigation that does not exist is more dangerous than no row at all."

Its `reason` names the replacement mitigation — `scripts/ci-workflow.testkit.ts`, `(r-class-authority)`, `(r-base-discriminating)` — with the measured numbers, and states the residual that actually remains:

> "RESIDUAL THAT ACTUALLY REMAINS, stated with its direction: a member that imports the authority and then computes something else entirely — a second private slice under another variable, in a branch nothing reaches — still satisfies the import-derived membership. That direction is FAIL-OPEN and strictly NARROWER than what this row originally disclosed, because there is no longer a second implementation for such a slice to be built from, but it is not zero."

**Row 96 (new) — the reversed disposition.** Names both member files, both measurements that forced the reversal, and what answers the original argument's second horn.

**Row 97 (new) — the build-output question, answered.** Nothing about the freshness or build-parity surface needed changing (`npm run freshness` exit 0, compared set 49 → 50). It also records the unexpected finding below.

Ledger counts: `open_count` 92 → 93, `fixed_count` 3 → 4, `total_count` 95 → 97.

## Full gate set on the final tree

| # | Gate | Result |
|---|---|---|
| 1 | `npm run typecheck` | exit **0** |
| 2 | `npm run build` + `git diff --exit-code --name-only -- '*.js'` | exit 0, diff **empty** |
| 3 | `npm run freshness` | exit **0** — "All build outputs fresh: 50 committed .js file(s) match a rebuild"; compared set moved 49 → 50, the new path being `scripts/ci-workflow.testkit.js` |
| 4 | `node scripts/check-foundation-guards.js` | exit **0** — ALL CHECKS PASSED |
| 5 | `node scripts/check-kit-refs.js` | exit **0** — ALL CHECKS PASSED |
| 6 | `npx vitest run --exclude '**/scripts/e2e/**'` | 2396 tests, 1 failed (pre-existing), 2393 passed, 2 skipped |
| 7 | base shift on a hermetic mirror of `8465fa2` | **4 failed → 5 failed**, `(r-base-discriminating)` named — baseline **plus one** |
| 8 | `git diff --name-only -- .claude/agents/ .claude/skills/` | **empty** |
| 9 | `git diff --name-only -- .github/workflows/ci.yml package.json package-lock.json` | **empty** |
| 10 | `git diff --exit-code -- scripts/` after every scratch mutation | **clean** |

`npm test` was never run.

## Decisions Made

- **Refuse round 4's cheap repair on measurement.** Stripping declaration lines makes requirement 1 red a correct tree. Measured before believing.
- **Delete both containment requirements rather than repair them.** They were the two halves round 4 measured incapable of failing; the class is closed by construction instead.
- **State arm 3 as a premise, not a discriminator** — the plan predicted three co-measuring assertions; measurement found two of the three, and the comment now says so.
- **Disclose the `.js`-resolution finding with its mitigation RUN.** Naming a mitigation without running it is precisely what row 80 did wrong; `npm run freshness` was executed against the `.ts`-only mutation and observed to exit 1.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] The class-case replacement had to land in Task 1's commit, not Task 2's**

- **Found during:** Task 1 (cutover)
- **Issue:** The plan splits the work so Task 1 cuts both members over to the authority and Task 2 replaces `(r-class-prefix)`. But `(r-class-prefix)` derives its member set by scanning `scripts/*.test.ts` for the step-name **prefix**, and the cutover removes that phrase from both members. Measured immediately after the cutover: `AssertionError: the derived reader set must be non-empty — 50 .test.ts file(s) were scanned … and none carried the step-name prefix "Freshness gates + repo gates": expected 0 to be greater than 0`. Its vacuity floor fired correctly. The case is structurally dead the instant the second grammar is deleted and cannot survive the cutover in any form — keeping it transitionally would require keeping the duplicate spellings the plan exists to remove. Task 1's `<verify>` requires a green file and a clean typecheck, so the two changes are one change.
- **Fix:** `(r-class-authority)` was written and landed in Task 1's commit (`56653ca`). Task 2 (`98a1882`) delivered `(r-base-discriminating)`, the four discrimination transcripts and the cross-reference correction, exactly as planned. Every acceptance criterion of both tasks is satisfied; only the commit boundary moved.
- **Verification:** `grep -c 'UBUNTU_BLOCK_SLICE_BASE_SOURCE' scripts/check-foundation-guards.test.ts` → **0**. Both members green at their pinned counts.
- **Committed in:** `56653ca`

**2. [Rule 3 - Blocking] Five derived corpus cardinality pins moved by the new module**

- **Found during:** Task 1
- **Issue:** Adding a non-test `scripts/*.ts` moved two independently derived corpora, and seven assertions across five sites red on it: `NON_TEST_MODULE_COUNT` (50 → 51, four consumers), the `scripts/`-scoped reader corpus (42 → 43), its `42 * 3` comparison count, and the recursive walk's subset pin. These are exactly the two-sided tripwires this project's record calls for, and their own failure messages instruct that the number moves in the same commit that adds the module.
- **Fix:** Each number re-derived **independently** rather than incremented — `find install scripts hooks -name '*.ts' ! -name '*.test.ts'` reports 50 and the recursive walk 51 (difference `vitest.config.ts`, delta unchanged at 1); `ls scripts/*.ts` minus test members reports 43. Every pin carries its derivation in the comment beside it, plus a note that the testkit is test-only yet is genuinely a member of a corpus enumerated by file shape rather than by consumer.
- **Verification:** All seven assertions green; the git-index cross-check (`walked` must equal `git ls-files`) passes only once the module is tracked, which it is.
- **Committed in:** `56653ca`

**3. [Rule 1 - Bug] A comment in `(r-base-discriminating)` claimed more than the measurement supported**

- **Found during:** Task 2
- **Issue:** As written from the plan's own wording, the case's closing comment read "assertions 3, 4 and 5 are the same fact measured three ways". Per-arm measurement under the base shift showed arm 3 stays **green**: it asserts a property of the CONTROL, which the mutation does not touch. Left uncorrected, this is the wider-than-mechanism claim class this phase exists to close — in the very case written to close it.
- **Fix:** The comment now states which four arms were measured to discriminate, and names arm 3 as a **premise about the control** that stays green by construction, with the reason.
- **Verification:** The per-arm table above, computed directly against both bases.
- **Committed in:** `98a1882`

---

**Total deviations:** 3 auto-fixed (2 blocking, 1 bug). **Impact:** no scope creep. Deviation 1 moved a commit boundary, deviation 2 was the correct and mandated response to a tripwire firing, and deviation 3 corrected this plan's own prose against a measurement.

## Issues Encountered

**The authority these tests execute is the committed `.js`, not the `.ts`.** Not predicted by the plan, found by probing rather than assuming. Vitest resolves `./ci-workflow.testkit.js` to the build output on disk:

- mutate the `.js` alone → `(r-base-discriminating)` **reds**
- mutate the `.ts` alone → `(r-base-discriminating)` stays **green** (1 passed | 264 skipped)

This is not unbounded, and the mitigation was **run** rather than named: `npm run freshness` on the `.ts`-only mutation exits **1** with `STALE WORKING OUTPUT: scripts/ci-workflow.testkit.js — its source is modified or untracked, and the .js in the working tree is not a build of it.` Direction FAIL-CLOSED. Recorded as WINDOWS row 97 and carried as coverage item D8 for human ruling on whether a later plan should point these cases at the `.ts` as well. Every mutation transcript in this SUMMARY therefore mutates **both** artifacts, which is the divergence a developer actually produces (edit source, build, commit).

**Pre-existing, untouched:** `scripts/frontmatter.test.ts > D-49 false-red control` fails at pristine HEAD and at the final tree, over the same 1442-file corpus with the same single refusal (WINDOWS row 91, `scripts/frontmatter.ts` escape-ordering defect). Not caused here, not fixed here, and verified not to have grown by reading the refusal list rather than the count.

## Known Stubs

None. No placeholder, empty-value or deferred-wiring construct was introduced.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema at a trust boundary. The one new module reads a single repository-relative file and is imported by tests only, which is asserted by a derived scan.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- BLOCKER 3 of round-4 verification is closed structurally, with the closing measurement recorded rather than asserted.
- Round 5 verification can re-run the mutation directly: shift the base in `scripts/ci-workflow.testkit.ts` **and** its committed `.js`, and expect `(r-base-discriminating)` in the failure set at baseline + 1.
- Open for a later plan: coverage item D8 (should these cases read the `.ts` as well as the shipped `.js`), and WINDOWS row 91 (the pre-existing `frontmatter.ts` escape-ordering false red), which still needs a plan of its own.

---
*Phase: 29.1-per-role-model-assignment*
*Completed: 2026-08-21*

## Self-Check: PASSED

All six declared key files exist on disk. All four commits (`56653ca`, `98a1882`, `8465fa2`, `0e75220`) exist in the log. This SUMMARY was additionally checked against the `D-49 false-red control` corpus scan: the corpus grew 1442 → 1443 tracked markdown files with the refusal set unchanged at exactly one entry — the pre-existing `29.1-VERIFICATION-round4.md` refusal of WINDOWS row 91. This document parses.

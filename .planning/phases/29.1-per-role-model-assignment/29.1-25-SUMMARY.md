---
phase: 29.1-per-role-model-assignment
plan: 25
subsystem: governance
tags: [decision-record, reconciliation, ledger, adversarial-verification, kit-refs, model-tiers]

requires:
  - phase: 29.1-22
    provides: the kit-ref one-extraction fix whose three spellings this plan re-reproduces
  - phase: 29.1-23
    provides: the one-authority key-axis fix whose two regressed shapes this plan re-reproduces
  - phase: 29.1-24
    provides: the shared ci-workflow testkit whose slice base this plan re-reproduces
provides:
  - "A human ruling on round-4 WR-03, applied at the surface it names"
  - "Decision D-29.1-18, superseding D-06's resolver-side corollary only"
  - "A derived, count-asserted disposition table over all 64 round-4 items"
  - "Four silently-dropped round-4 Info findings, found and filed"
  - "Three blocker reproductions watched red against the final tree"
affects: [phase-verification, gsd-ship, 29.1-round-5]

actuals:
  tokens: 3700
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Derive the item list, then assert the JOIN with dispositions is total in BOTH directions — not just that the denominator is non-empty"
    - "A ruling is applied at the surface it names and nowhere else; the half that was ruled out is recorded as OWED, never as closed"

key-files:
  created:
    - .planning/phases/29.1-per-role-model-assignment/29.1-25-SUMMARY.md
  modified:
    - .planning/phases/29.1-per-role-model-assignment/29.1-CONTEXT.md
    - .planning/WINDOWS.md

key-decisions:
  - "WR-03 ruled record-decision-only by Olger Oeselg on 2026-08-21: a numbered decision is required, a CHANGELOG entry is not"
  - "The new decision is spelled D-29.1-18, phase-qualified, because D-18 already names install/kit-source.ts's own decision three times in this phase directory"
  - "The CHANGELOG half is recorded as OWED ON FIRST REACHABLE CHANGE, never as closed"
  - "Round-4 IN-01..IN-04 were undispositioned by every plan in this round; filed rather than absorbed"

patterns-established:
  - "Blocker re-reproduction asserts the mutation landed by byte delta AND an independent re-measurement of the property the mutation was supposed to move, before the run is believed"
  - "When a gate's scan set excludes the surface a plan wrote, that fact is measured and stated rather than left implied by a green verdict"

requirements-completed: [MODEL-04, MODEL-06, MODEL-07]

coverage:
  - id: D1
    description: "Round-4 WR-03 ruled by a named human and the ruling applied at the surface it names"
    requirement: "MODEL-06"
    verification:
      - kind: manual_procedural
        ref: "checkpoint:decision resolved by Olger Oeselg 2026-08-21; ruling quoted verbatim in this SUMMARY and beside WINDOWS row 86"
        status: pass
    human_judgment: true
    rationale: "The ruling IS the deliverable and it was made by the named human. Whether the applied record satisfies the governance intent is that human's reading, not a predicate."
  - id: D2
    description: "Decision D-29.1-18 written into 29.1-CONTEXT.md, additions only, D-06 left standing"
    verification:
      - kind: other
        ref: "git diff -U0 -- 29.1-CONTEXT.md: single hunk @@ -231,0 +232,57 @@, 57 additions, 0 deletions"
        status: pass
    human_judgment: false
  - id: D3
    description: "WINDOWS row 86 amended to point at the decision and carry the ruling"
    verification:
      - kind: other
        ref: "awk field count over row 86: 12 cells, status open, counts unchanged"
        status: pass
    human_judgment: false
  - id: D4
    description: "All 64 round-4 items dispositioned in one derived, count-asserted table"
    verification:
      - kind: other
        ref: "/tmp/derive.py two independent methods agree at 64; /tmp/disp.py asserts join total both directions, table rows 64 == asserted 64"
        status: pass
    human_judgment: false
  - id: D5
    description: "Three round-4 blockers re-reproduced against the final tree, each watched failing"
    verification:
      - kind: integration
        ref: "hermetic git archive HEAD mirrors: Blocker 1 four plants exit 1; Blocker 2 both shapes throw under reverting mutation; Blocker 3 4->5 failed with (r-base-discriminating) named"
        status: pass
    human_judgment: false
  - id: D6
    description: "Four undispositioned round-4 Info findings found and filed as WINDOWS rows 101-104"
    verification:
      - kind: other
        ref: "grep by id AND by subject across all round summaries, WINDOWS.md and deferred-items.md: zero hits before filing"
        status: pass
    human_judgment: false
  - id: D7
    description: "The three carried human items remain unclosed and no mechanism added here decides any of them"
    verification: []
    human_judgment: true
    rationale: "This is a negative claim about the absence of a mechanism. It is verified by reading what this plan changed — two prose surfaces and six ledger rows — and no test can assert that a future mechanism was not added."

duration: 79 min
completed: 2026-08-21
status: complete
---

# Phase 29.1 Plan 25: Rule, Record, Reconcile Summary

**A named human ruled round-4 WR-03 `record-decision-only`; the reversal is now decision D-29.1-18 with the changelog half recorded as owed rather than closed, and a derived 64-item reconciliation caught four round-4 findings that every plan in this round had silently dropped.**

## Performance

- **Duration:** 79 min
- **Started:** 2026-08-21T17:55:00Z (approx — first context read)
- **Completed:** 2026-08-21T19:14:00Z
- **Tasks:** 3 (1 blocking checkpoint, 2 auto)
- **Files modified:** 2 (plus this SUMMARY)

## Accomplishments

- **The governance question was put to a human and not answered by the plan.** Task 1 stopped with the measured facts and the three options, and did not recommend among them. The ruling came back `record-decision-only`.
- **The ruling was applied at the surface it names and nowhere else.** A numbered decision exists; `CHANGELOG.md` is byte-unchanged; the ledger row points at the decision.
- **The changelog half is recorded as OWED, not closed.** The distinction is the whole value of the ruling and it is stated in the decision, in the ledger row, and in the disposition table.
- **The 64-item reconciliation is derived, and the derivation caught a real drop.** Round-4 review findings IN-01, IN-02, IN-03 and IN-04 — and their four matching anti-pattern rows — had no disposition in any plan and no ledger row. Eight of the 64 items were carried by nobody. They are now filed as WINDOWS rows 101-104.
- **All three round-4 blockers were watched failing on the final tree**, each with its mutation asserted landed by byte delta plus an independent re-measurement of the property it moved.

## Task Commits

1. **Task 1: Rule on the D-06 reversal's record** — no commit (blocking checkpoint; the ruling is an input, not an artifact)
2. **Task 2: Apply the ruling** — `2de446e` (docs)
3. **Task 3: Reconcile, carry, prove red** — `77d7f1e` (docs)

**Plan metadata:** see final commit (docs: complete plan)

## The ruling, verbatim

Ruled by **Olger Oeselg** on **2026-08-21**. Selected option: **`record-decision-only`**.

> WR-03 ruled `record-decision-only` by Olger Oeselg on 2026-08-21. The reversal of D-06's unknown-key disposition inside `resolveModels` requires a numbered decision in `29.1-CONTEXT.md`'s amendments section. `CHANGELOG.md` is NOT to be touched for this reversal: the refusal branch is unreachable from production (both call sites hand `readModelsConfig` and `resolveModels` the same `stems` array), and the changelog's stated audience is humans reading about observable changes. A `### Changed` entry is owed if and when a reachable behaviour change ships.

### The facts the ruling was made against

Every one measured this session, at HEAD `1f69800`, not inherited from the round-4 report:

| Fact | Measurement |
|---|---|
| `CHANGELOG.md` untouched across the phase | `git diff 6f8411e..HEAD --stat -- CHANGELOG.md` → empty, over **130** commits (round 4 measured 111 at `ad033f0`; 19 more since) |
| Last CHANGELOG edit | `1834f4d`, 2026-08-17, `fix(29-43)` — phase 29, not 29.1 |
| Both call sites share one `stems` array | `generate-role-adapters.ts:299-308` and `check-foundation-guards.ts:2107-2110`, each read; `grep resolveModels( scripts/**.ts \| grep -v test` returns exactly these two |
| Pre-1.0 | `agent-factory/VERSION` → `0.1.0`; CHANGELOG's own note says the milestone tags are not published SemVer releases |
| Docstring half already closed | 29.1-23 added a fourth paragraph; the opening `ALREADY-VALIDATED` sentence deliberately still stands above it (annotate-never-rewrite) |

## Every surface the ruling caused to be written

### 1. `29.1-CONTEXT.md` — decision D-29.1-18

Added as a single hunk, `@@ -231,0 +232,57 @@`, **57 additions and 0 deletions**, entirely inside the "Amendments after research — USER-RULED, BINDING" section. D-06's text at lines 80-82 is byte-unchanged and was re-read after the edit to confirm it.

The decision carries all five elements the D-15/D-16/D-17 shape requires — the number, what it amends, the measurements, the honest consequence, and the reversibility line — plus a sixth this one needed: why its id is spelled the way it is.

**On the numbering.** `29.1-CONTEXT.md`'s own sequence runs D-01 … D-17, so `D-18` was next. But `D-18` already names `install/kit-source.ts`'s own module-local decision, cited three times inside this same phase directory (`29.1-CONTEXT.md:213`, `29.1-RESEARCH.md:87`, `29.1-VALIDATION.md:115`). Because this project annotates and never rewrites, the earlier spelling cannot be moved aside. The id is therefore phase-qualified as **`D-29.1-18`**, in the shape `deferred-items.md` already uses for its own ids (`D-29.1-22-01`, `D-29.1-23-01`). The reasoning is recorded inside the decision itself, not only here.

**What survives of D-06, stated explicitly in the decision:** `readModelsConfig`'s refusal of an unknown role key, by name, against the stems it is handed, is unchanged. Only the resolver-side corollary — the argument that the resolver should defer the question entirely — is superseded.

**The honest consequence, as written:** the new refusal branch is unreachable from production today; its only exerciser is the unit suite. What changed is the documented contract of a public export, not an observable behaviour.

### 2. `CHANGELOG.md` — deliberately NOT written

`git diff --name-only -- CHANGELOG.md` → **empty**, asserted before the Task 2 commit and again at plan close. This is the ruling applied, not an omission.

### 3. `.planning/WINDOWS.md` row 86 — amended

Row 86 now opens with `AMENDED BY PLAN 29.1-25 — THIS ROW IS NO LONGER THE ONLY RECORD OF THE REVERSAL OUTSIDE THE SOURCE FILE`, points at D-29.1-18 by name and path, carries the ruling verbatim, and states that the changelog half is owed rather than closed. It **stays `open`**, because the ruling settled where the reversal is recorded and not the FAIL-CLOSED residual the row discloses. Row integrity re-checked after the edit: 12 cells, `status: open`, ledger counts unchanged (no row was added by the amendment).

## The reconciliation

### The derivation, and its count

The item list is **derived from the two round-4 documents by two structurally independent methods**, and the two are asserted equal before anything is built on them.

- **Method A** — an indentation-aware structural walk that tracks the current top-level key and its sub-key, collecting line indices.
- **Method B** — a different mechanism entirely: the frontmatter text is sliced by top-level key, each slice re-split on its sub-keys, and bullets counted inside each region with a separate regex family.

| Category | Method A | Method B | Agree |
|---|---|---|---|
| `gaps` entries | 5 | 5 | yes |
| `artifacts` bullets | 15 | 15 | yes |
| `missing` bullets | 15 | 15 | yes |
| `regressions` entries | 1 | 1 | yes |
| `coincidental_reliance_items` | 1 | 1 | yes |
| anti-pattern table rows | 12 | 12 | yes |
| review findings | 12 | 12 | yes |
| `human_verification` entries | 3 | 3 | yes |
| **TOTAL** | **64** | **64** | **yes** |

**ASSERTED ITEM COUNT = 64.**

**Per document:** `29.1-VERIFICATION-round4.md` → **52** (5 + 15 + 15 + 1 + 1 + 12 + 3). `29.1-REVIEW-round4.md` → **12**. 52 + 12 = 64.

### Guarding against a silently SHORT denominator

Two agreeing regex methods over the same indentation could share a blind spot, so a third check was run on a **different structural feature** — each gap's sibling keys rather than its leading bullet — and then a line-anchored exhaustion pass that buckets *every* bulleted line in the `gaps` block:

```
gap entries by `- truth:` bullet    : 5
gap entries by sibling `status:`    : 5
gap entries by sibling `reason:`    : 5
gap entries by sibling `artifacts:` : 5
gap entries by sibling `missing:`   : 5

2sp -truth  : 5
6sp -path   : 15
6sp -other  : 15
OTHER-DEPTH : (0, [])
bulleted lines accounted for: 35 | unaccounted: 0
```

**Zero unaccounted bullets.** The denominator is exhaustive, not merely non-empty — which is the distinction the plan's third truth turns on. Review ids were separately asserted duplicate-free and complete across the CR/WR/IN space: `['CR-01','CR-02','CR-03','CR-04','WR-01','WR-02','WR-03','WR-04','IN-01','IN-02','IN-03','IN-04']`, duplicates 0.

A first attempt at the exhaustion check reported **1** bullet at an uncounted depth. That was a false positive in my own probe — a `\s+` in the negative-lookahead regex matched across a newline. Re-run line-anchored, it reports 0. Recorded because an unexamined "1" there would have been read as a real uncounted item.

### The disposition table

The table is generated by joining the derived id list against a disposition map, and **the join is asserted total in both directions** — zero items without a disposition, zero dispositions without an item. **Table row count = 64 = asserted item count.** That equality is machine-asserted, not eyeballed.

| # | Id | Item, as the source document spells it | Verdict | Plan | Evidence or reason |
|---|---|---|---|---|---|
| 1 | `G1` | gaps[1].truth — D-08.1 zero-tolerance kit-ref gate governs the shipped configuration reference | CLOSED | 29.1-22 | Watched red THIS PLAN, Blocker 1: all four plants exit 1 on a final-tree mirror; clean mirror exits 0. 29.1-22 §"The three post-fix RED transcripts". |
| 2 | `G2` | gaps[2].truth — `resolveModels` returns a result rather than throwing | DISPOSITIONED | 29.1-23 | Closed on the two REPRODUCED shapes (watched red this plan, Blocker 2). The TRUTH AS STATED is NOT achieved: four input classes still throw. 29.1-23 §Deviations 3; WINDOWS row 95. |
| 3 | `G3` | gaps[3].truth — `(r-class-prefix)` certifies every derived reader reads ONE region | CLOSED | 29.1-24 | Watched red THIS PLAN, Blocker 3: 4 failed -> 5 failed, `(r-base-discriminating)` named. 29.1-24 §"The four discrimination transcripts". |
| 4 | `G4` | gaps[4].truth — Assertion 3 derived and actual sets built from one path-spelling authority | CARRIED | 29.1-22 (posix half) | Posix half CLOSED by `relKey` (29.1-22 §"Every path literal entering a compared set, DERIVED not assumed"). WINDOWS half REASONED under `path.win32`, never run on `windows-latest`. Owner: human ruling; WINDOWS row 89, CAP-02 Phase 33. |
| 5 | `G5` | gaps[5].truth — no record makes a claim wider than its mechanism; no ratified decision reversed in a comment | DISPOSITIONED | 29.1-23 + 29.1-25 | WR-02 and WR-04 CLOSED by 29.1-23. WR-03 RULED `record-decision-only` and recorded as D-29.1-18 this plan. Changelog half OWED, not closed — WINDOWS row 99. |
| 6 | `G1-A1` | artifacts: `scripts/check-kit-refs.ts` — CR-02 REPRODUCED (bare-directory spelling) | CLOSED | 29.1-22 | Watched red this plan: plant P1, +55 bytes, needle occurrences 3->4 (independent indexOf loop), TRUE exit 1, stray finding names `factory.config.md:138`. |
| 7 | `G1-A2` | artifacts: `scripts/check-kit-refs.ts` — CR-03 REPRODUCED (suffix-traversal spelling) | CLOSED | 29.1-22 | Watched red this plan: plant P2, +23 bytes, needle occurrences 3->3 — moves NO cardinality and reds anyway, which is the whole point. TRUE exit 1. |
| 8 | `G1-A3` | artifacts: `scripts/check-kit-refs.ts` — NEW, no-capture prefix, strictly worse than CR-02 | CLOSED | 29.1-22 | Watched red this plan: plant P3, +88 bytes, needle occurrences 3->5, TRUE exit 1. (Round 4 recorded +85 for its wording; my sentence is 3 bytes longer. The occurrence delta matches exactly.) |
| 9 | `G1-A4` | artifacts: `scripts/check-kit-refs.test.ts:368-405` — the oracle cannot see any of the three | CLOSED | 29.1-22 | Six new oracle cases, five watched failing against the pre-fix build first. 29.1-22 §"The corpus: RED-FIRST, watched". |
| 10 | `G2-A1` | artifacts: `scripts/model-tiers.js` (committed build) — Symbol key THREW TypeError | CLOSED | 29.1-23 | Watched red this plan, Blocker 2: at HEAD returns `ok:false` naming the stem; under a -20-byte mutation reverting `quoteValue(stem)` (occurrences 2->0) it THROWS again. |
| 11 | `G2-A2` | artifacts: `scripts/model-tiers.ts:1196-1233` — both refusal templates interpolate `${stem}` raw | CLOSED | 29.1-23 | Same mutation as G2-A1 proves discrimination at BOTH sites. 29.1-23 §"The two templates after the change, and the four call positions". |
| 12 | `G2-A3` | artifacts: `scripts/model-tiers.test.ts:1639-1738` — corpus driven only at the VALUE position | CLOSED | 29.1-23 | 29.1-23 §"The KEY-axis and cross-product transcripts" — KEY, VALUE and the 169-cell cross product; a thirteenth entry was required. |
| 13 | `G2-A4` | artifacts: `scripts/model-tiers.ts:950-953` — `overrides` docstring still reads ALREADY-VALIDATED | CLOSED | 29.1-23 | 29.1-23 §"The overrides docstring". Annotate-never-rewrite: a fourth paragraph states the by-name refusal; the opening ALREADY-VALIDATED sentence deliberately still stands above it. |
| 14 | `G3-A1` | artifacts: `scripts/check-foundation-guards.test.ts:11002` — the R3-IN-03 divergence changed nothing | CLOSED | 29.1-24 | Watched red this plan: `ci.slice(at + UBUNTU_BLOCK_STEP_NAME.length)` -> `ci.slice(at)` in BOTH testkit artifacts, -32 bytes each, 0 sites remaining, region head visibly moved; 4 failed -> 5 failed. |
| 15 | `G3-A2` | artifacts: `scripts/check-foundation-guards.test.ts:11078-11145` — `(r-bound-synthetic)` cannot catch it either | CLOSED | 29.1-24 | Closed STRUCTURALLY: one implementation in `scripts/ci-workflow.testkit.ts` consumed by both readers. 29.1-24 §"The authority and its export list". |
| 16 | `G3-A3` | artifacts: `.planning/WINDOWS.md row 80` — residual framed as hypothetical when it was live | CLOSED | 29.1-24 | Row 80 false mitigating clause REPLACED, not softened. 29.1-24 §"The reversed disposition, in both files". |
| 17 | `G4-A1` | artifacts: `scripts/check-kit-refs.ts:137, 602` — a raw `/` literal entering a `walk()`-compared set | CLOSED | 29.1-22 | `relKey()` applied at both sites, entry points derived rather than taken from the report. 29.1-22 §"Every path literal entering a compared set, DERIVED not assumed". |
| 18 | `G5-A1` | artifacts: `scripts/model-tiers.ts:950-953, 1176-1196` — contract changed; docstring and CHANGELOG did not | DISPOSITIONED | 29.1-23 + 29.1-25 | Docstring CLOSED by 29.1-23. Decision record CLOSED by this plan (D-29.1-18). CHANGELOG deliberately NOT written, by ruling — WINDOWS row 99. |
| 19 | `G5-A2` | artifacts: `scripts/model-tiers.test.ts:1657-1700` — totality argument on a hand-typed denominator | CLOSED | 29.1-23 | Both denominators pinned two-sided and proven to discriminate against a co-edit the set-equality assertion passes over. 29.1-23 §"Both denominator pins, and their discrimination". |
| 20 | `G5-A3` | artifacts: `scripts/model-tiers.test.ts:1782-1806` — one-authority scan over a file that is not the shipped one | CLOSED | 29.1-23 | Scan reads the committed `.js`. 29.1-23 §"The two serialiser-call counts, and the compiled-side discrimination". |
| 21 | `G1-M1` | missing: count the PUBLISHED unit from the needle (`exemptMentions += line.split(NEEDLE).length - 1`) | SUPERSEDED | 29.1-22 | Not applied as written. 29.1-22 DELETED the second grammar instead, so the published count IS the occurrence count by construction rather than two units reconciled. Structurally stronger than the suggested repair. |
| 22 | `G1-M2` | missing: add the missing arm — a hit whose needle count EXCEEDS `named.length` | SUPERSEDED | 29.1-22 | Same structural fix removes the two-unit gap the arm would have detected; a run-time premise floor compares the two on every hit and prints both on disagreement. |
| 23 | `G1-M3` | missing: capture the WHOLE path a line names rather than its first segment | APPLIED | 29.1-22 | Membership decided over the whole path: `factory.config.json` is a member, `factory.config.json/../../../../etc/passwd` is not — as a consequence of the unchanged rule. |
| 24 | `G1-M4` | missing: add RED cases at the SUFFIX and NO-CAPTURE positions | APPLIED | 29.1-22 | Six new oracle cases, five watched failing pre-fix. Re-proven this plan by plants P2 and P3. |
| 25 | `G2-M1` | missing: route the stem through the same authority at both sites | APPLIED | 29.1-23 | Both positions in both sentences obtain both values from `quoteValue`; 127 refusal renderings diffed pre/post: empty. |
| 26 | `G2-M2` | missing: widen SHAPE_CORPUS to the KEY axis | APPLIED | 29.1-23 | Widened to KEY, VALUE and the cross product; a thirteenth entry was required because the two axes fail under different operators. |
| 27 | `G2-M3` | missing: update the `overrides` docstring AND file the D-06 reversal as a numbered decision with a CHANGELOG entry | SPLIT | 29.1-23 + 29.1-25 | Docstring APPLIED by 29.1-23. Decision APPLIED by this plan as D-29.1-18. CHANGELOG entry NOT written — ruled out by the human, OWED on first reachable change (WINDOWS row 99). |
| 28 | `G3-M1` | missing: strip `const <NAME> = ...` declaration lines before the `includes` test | REFUSED ON MEASUREMENT | 29.1-24 | Measured: after stripping declarations the marker occurs ZERO times in BOTH members, so the repaired requirement would red a correct tree. 29.1-24 §"The measurement that falsified round 4's suggested cheap repair". |
| 29 | `G3-M2` | missing: compare BEHAVIOUR via a shared `*.testkit.ts` consumed only by tests | APPLIED | 29.1-24 | This is the fix 29.1-24 shipped. Membership derived from the import, pinned two-sided at 2, negative over all 48 non-members. |
| 30 | `G3-M3` | missing: amend WINDOWS.md row 80 to state the measured fact | APPLIED | 29.1-24 | Row 80 amended; false mitigating clause replaced rather than softened. |
| 31 | `G4-M1` | missing: `ghLegal.add(join(PACKAGING_TEMPLATE))` and normalise the direct-file SCAN/GH_SCAN entries | APPLIED | 29.1-22 | `relKey()` is the one path-spelling authority, applied at both sites, entry set derived. |
| 32 | `G4-M2` | missing: add a `path.win32`-based unit case, or log the row in `.planning/WINDOWS.md` | APPLIED (both) — PLATFORM HALF CARRIED | 29.1-22 | Both done: two-directional `path.win32` case AND WINDOWS row 89. The row itself discloses that nothing has RUN on Windows. Carried as a human item. |
| 33 | `G5-M1` | missing: file the D-06 reversal as a numbered decision, add the CHANGELOG entry, correct the docstring | SPLIT | 29.1-23 + 29.1-25 | Identical to G2-M3: docstring 29.1-23, decision D-29.1-18 this plan, CHANGELOG owed by ruling. |
| 34 | `G5-M2` | missing: drop the "not this file's to choose" claim, or make the denominator observable | APPLIED | 29.1-23 | Denominator made observable and pinned two-sided. |
| 35 | `G5-M3` | missing: run the `JSON.stringify(` scan over BOTH `model-tiers.ts` and `model-tiers.js` | APPLIED | 29.1-23 | Scan reads both; the narrow case passes the exact plant the widened one reds on. |
| 36 | `REG-1` | regressions[1] — `resolveModels` THROWS on two shapes that returned `{ok:true}` at `e9907f5` | CLOSED | 29.1-23 | Watched red this plan, Blocker 2: both shapes return a named refusal at HEAD; both throw under the reverting mutation. |
| 37 | `COIN-1` | coincidental_reliance_items[1] — `(r-class-prefix)` requirement 1, reason: fixture-only | CLOSED | 29.1-24 | Closed structurally, not by hardening the predicate: the shared testkit makes "one region" true by construction. The suggested harden was measured and refused (see G3-M1). |
| 38 | `AP-01` | Anti-Patterns Found row: `scripts/check-kit-refs.ts` 185-216, 351-366 — published number in a unit the code does not count | CLOSED | 29.1-22 | Same subject as CR-02; watched red this plan (plant P1). |
| 39 | `AP-02` | Anti-Patterns Found row: `scripts/check-kit-refs.ts` 216 — extraction judges the path's first segment, not the path | CLOSED | 29.1-22 | Same subject as CR-03; watched red this plan (plant P2). |
| 40 | `AP-03` | Anti-Patterns Found row: `scripts/model-tiers.ts` 1196-1233 — one guarded and one raw position in the same sentence | CLOSED | 29.1-23 | Same subject as CR-01 declared axis; watched red this plan (Blocker 2). |
| 41 | `AP-04` | Anti-Patterns Found row: `scripts/check-foundation-guards.test.ts` 10936, 11225-11290 — predicate satisfied by its own subject | CLOSED | 29.1-24 | Same subject as CR-04; watched red this plan (Blocker 3). |
| 42 | `AP-05` | Anti-Patterns Found row: `scripts/check-kit-refs.ts` 137, 602 — two path spellings entering one compared set | CARRIED | 29.1-22 (posix half) | Same subject as WR-01. Windows half never executed — WINDOWS row 89, CAP-02 Phase 33. |
| 43 | `AP-06` | Anti-Patterns Found row: `scripts/model-tiers.test.ts` 1657-1700 — "external denominator" hand-typed below the claim | CLOSED | 29.1-23 | Same subject as WR-02. |
| 44 | `AP-07` | Anti-Patterns Found row: `scripts/model-tiers.test.ts` 1782-1806 — one-authority scan reads the `.ts`, not the shipped `.js` | CLOSED | 29.1-23 | Same subject as WR-04. |
| 45 | `AP-08` | Anti-Patterns Found row: `scripts/model-tiers.ts` 950-953, 1176-1196 — ratified decision reversed in a comment; docstring stale | DISPOSITIONED | 29.1-23 + 29.1-25 | Same subject as WR-03. Docstring closed; decision recorded; changelog owed by ruling (row 99). |
| 46 | `AP-09` | Anti-Patterns Found row: `scripts/model-tiers.ts` 1080-1088 — `describeShape(null)` -> "an object rather than a Map" | CARRIED — NO OWNER | none | Same subject as IN-01. Undispositioned by every plan in this round; filed by THIS reconciliation as WINDOWS row 101. |
| 47 | `AP-10` | Anti-Patterns Found row: `scripts/check-kit-refs.ts` 259, 342 — `join(dir)+sep` computed twice | CARRIED — NO OWNER | none | Same subject as IN-02. Filed by THIS reconciliation as WINDOWS row 102. |
| 48 | `AP-11` | Anti-Patterns Found row: `scripts/check-kit-refs.ts` 232-244 — `walk()` follows symlinks, no cycle guard | CARRIED — NO OWNER | none | Same subject as IN-03. Filed by THIS reconciliation as WINDOWS row 103. |
| 49 | `AP-12` | Anti-Patterns Found row: `scripts/model-dial-consistency.test.ts` 490-513 — `sectionCitationsIn` throws over the whole document | CARRIED — NO OWNER | none | Same subject as IN-04. Filed by THIS reconciliation as WINDOWS row 104. |
| 50 | `CR-01` | CR-01: `resolveModels`'s refusal path still THROWS — and plan 21 added a throw where there was none | DISPOSITIONED | 29.1-23 | The two REPRODUCED shapes are closed and watched red this plan. The module is still NOT total: four input classes throw. WINDOWS row 95, deferred-items D-29.1-23-01. |
| 51 | `CR-02` | CR-02: R3-CR-01 is NOT closed — the gate still publishes a number in a unit it does not count | CLOSED | 29.1-22 | Watched red this plan (plant P1). Units unified by construction. |
| 52 | `CR-03` | CR-03: R3-CR-02 is NOT closed — a path that leaves the directory is still certified as a reference inside it | CLOSED | 29.1-22 | Watched red this plan (plant P2), the spelling that moves no cardinality. |
| 53 | `CR-04` | CR-04: `(r-class-prefix)`'s two requirements CANNOT FAIL for either existing member | CLOSED | 29.1-24 | Watched red this plan (Blocker 3), 4 -> 5 failed with the case named. |
| 54 | `WR-01` | WR-01: Assertion 3's positive half compares two path spellings — a guaranteed false RED on Windows | CARRIED | 29.1-22 (posix half) | Posix half closed by `relKey`; Windows half REASONED under `path.win32`, never run on a `windows-latest` leg. Ruling owed on whether that closes the round-4 partial. WINDOWS row 89. |
| 55 | `WR-02` | WR-02: `TYPEOF_RESULTS` claims to be a denominator "not this file's to choose" — it is hand-typed | CLOSED | 29.1-23 | Both denominators pinned two-sided, discrimination proven against a co-edit. |
| 56 | `WR-03` | WR-03: D-06 is reversed in a code comment rather than in a decision record | RULED AND RECORDED | 29.1-25 | Ruled `record-decision-only` by Olger Oeselg, 2026-08-21. Decision D-29.1-18 written; WINDOWS row 86 amended to point at it. CHANGELOG half OWED on first reachable change, NOT closed — WINDOWS row 99. |
| 57 | `WR-04` | WR-04: the `String(JSON.stringify(` / two-sites scan runs over the `.ts` only | CLOSED | 29.1-23 | Scan reads the shipped `.js`; narrow case passes the plant the widened one reds on. |
| 58 | `IN-01` | IN-01: `describeShape(null)` renders the null-overrides refusal as "an object rather than a Map" | CARRIED — NO OWNER | none | SILENT DROP CAUGHT BY THIS RECONCILIATION. No plan in this round claimed it; no ledger row carried it. Filed as WINDOWS row 101. Owner: unassigned. |
| 59 | `IN-02` | IN-02: `join(CONFIG_SELF_REF_DIR) + sep` is computed in two places | CARRIED — NO OWNER | none | SILENT DROP CAUGHT BY THIS RECONCILIATION. Filed as WINDOWS row 102. Note 29.1-22 rewrote this module, so the cited lines need re-locating. Owner: unassigned. |
| 60 | `IN-03` | IN-03: `walk()` follows symlinks with no cycle guard, and admits them to the exemption's sibling set | CARRIED — NO OWNER | none | SILENT DROP CAUGHT BY THIS RECONCILIATION. Filed as WINDOWS row 103. Distinct from round-3 R3-IN-03 (closed by 29.1-24) — the id collision across rounds is why it was easy to miss. Owner: unassigned. |
| 61 | `IN-04` | IN-04: `sectionCitationsIn` throws on a lone backtick-`###` sequence anywhere in the authority | CARRIED — NO OWNER | none | SILENT DROP CAUGHT BY THIS RECONCILIATION. Filed as WINDOWS row 104. Distinct from WINDOWS row 83 (same function, different finding). Owner: unassigned. |
| 62 | `HUM-1` | human_verification[1] — MODEL-06 / D5 vendor citation reading | CARRIED VERBATIM | none | Carried unchanged from rounds 1-4. Quoted verbatim in §"The carried human items". No mechanism added by this plan decides it. |
| 63 | `HUM-2` | human_verification[2] — MODEL-07/boundary and MODEL-07/precision | CARRIED VERBATIM | none | Carried unchanged from rounds 1-4. Mechanical half re-confirmed this plan: `measure-cost.{ts,js}` byte-unchanged. Prose half is a human reading. |
| 64 | `HUM-3` | human_verification[3] — rule on the D-06 reversal's record (WR-03) | RESOLVED THIS PLAN | 29.1-25 | The only one of the three that closes: ruled `record-decision-only` by Olger Oeselg on 2026-08-21 and applied. Its residue (the owed changelog entry) is carried, not closed. |

**Verdict distribution:** CLOSED 29 · APPLIED 9 · CARRIED — NO OWNER 8 · DISPOSITIONED 5 · CARRIED 3 · SUPERSEDED 2 · SPLIT 2 · CARRIED VERBATIM 2 · REFUSED ON MEASUREMENT 1 · RULED AND RECORDED 1 · RESOLVED THIS PLAN 1 · APPLIED (both) — PLATFORM HALF CARRIED 1.

### The drop this reconciliation caught

**Round-4 review findings IN-01, IN-02, IN-03 and IN-04 had no disposition anywhere.** Searched twice — first by id, then by subject (`describeShape`, `cycle guard`, `symlink`, `sectionCitationsIn`, `CONFIG_SELF_REF_DIR`) across all four round summaries, `WINDOWS.md` and `deferred-items.md`. Zero hits for all four.

Two near-misses made this easy to miss, and both are worth recording:

- The hits for "IN-03" are **round three's** R3-IN-03, a different finding, which *was* closed by plan 29.1-24. The id space collides across rounds.
- The hits for "sectionCitationsIn" are **row 83**, plan 29.1-20's finding about the same function — a different defect.

Counting their four matching anti-pattern rows, **8 of the 64 items were carried by nobody**. Filed as WINDOWS rows **101-104** so they are visible to the ship gate rather than resident only in a summary. Recorded as a deviation below.

## The carried human items, verbatim

Copied unchanged from `29.1-VERIFICATION-round4.md:112-121`. `test`, `expected` and `why_human` are byte-identical to the source. **No mechanism added by this plan decides any of them.**

```yaml
human_verification:
  - test: "MODEL-06 / D5 (carried UNCHANGED from rounds 1, 2 and 3): read the four vendor citations at agent-factory/packaging/subagent.frontmatter.md:251-255 (Codex CLI, Gemini CLI, OpenCode, GitHub Copilot CLI, all retrieved 2026-08-20) against their live vendor pages, and confirm the authority sentence at :249, the amended MODEL-06 text and the amended ROADMAP SC-4 genuinely agree in substance."
    expected: "Each citation's quoted claim (a per-agent `model` field in that CLI's own agent-definition format) matches what the vendor page currently states; the three prose surfaces assert nothing beyond (a) grugops emits per-agent adapters for Claude Code alone and (b) every claim about the other four carries a named source."
    why_human: "Citation fidelity to external vendor documentation is not a predicate this verifier's tooling can decide. I confirmed the citations are present, dated, specific, internally consistent, and that the document holds 0 occurrences of `agent-factory/config/` — not that the vendor pages still say what is quoted. This is what this repository's 'never fake a citation' rule exists to route to a human."
  - test: "MODEL-07/boundary and MODEL-07/precision (carried unchanged from rounds 1-3; plan 29.1-06 and plan 29.1-21 `verification: backstop`) — confirm no phase-touched prose asserts a cost, spend or usage-limit saving, and that no numeric cost figure, percentage, multiplier or price ships."
    expected: "Zero cost or limit-savings claims. Mechanical half CONFIRMED this round: `git diff 6f8411e..HEAD -- scripts/measure-cost.ts scripts/measure-cost.js` is EMPTY across the whole phase, so the standing `UNKNOWN - verify` is untouched."
    why_human: "D-59/D-16: an open-set totality is not a decidable predicate here, and docs/audit/28-claim-registry.md row C-28-047 discloses the direction as FAIL-OPEN — there is no cost literal group in check-banned-claims, so a cost sentence would pass every gate in this repository green. This is the phase's own declared human-adjudication item."
  - test: "Rule on the D-06 reversal's record (round-4 WR-03). WINDOWS.md row 86 discloses that plan 29.1-21 reversed a ratified decision as a contract change rather than a bug fix, and the reversal is otherwise recorded only as a source comment: CHANGELOG.md is byte-unchanged across the entire phase and the `overrides` docstring still reads ALREADY-VALIDATED."
    expected: "A ruling on whether a numbered decision record plus a CHANGELOG `Changed` entry are required before this phase closes, or whether WINDOWS.md row 86 is a sufficient record for a pre-1.0 internal contract."
    why_human: "Whether a disclosure ledger substitutes for a decision record in a repository whose stated value proposition is an auditable decision trail is a governance judgement, not a predicate. The new refusal branch is also unreachable from production (both call sites hand `readModelsConfig` and `resolveModels` the same stems), so nothing measurable turns on it today."
```

### Additional open human items carried from waves 1-3

Named by the coordinator and **not dispositioned by me** — each points at its recorded location and awaits a ruling:

| Item | Recorded at | Waiting on |
|---|---|---|
| Pre-existing `frontmatter.ts` escape-ordering false refusal | WINDOWS row 91, `deferred-items.md` D-29.1-22-01 | Its own plan. Not fixed here; it is the one live suite failure and was deliberately left alone. |
| WR-01's Windows half — reasoned under `path.win32`, never run on a `windows-latest` leg | WINDOWS row 89, CAP-02 Phase 33 | A ruling on whether reasoning under `path.win32` closes the round-4 partial. |
| Plan 23's four still-throwing input classes — totality disclosed open | WINDOWS row 95, `deferred-items.md` D-29.1-23-01, STATE blocker | A ruling; deliberately not half-fixed. |
| Plan 24's D8 — vitest resolves the testkit to the committed `.js`, so tests execute build output | WINDOWS row 97 | A ruling. `npm run freshness` was verified to exit 1 on a `.ts`-only mutation, which bounds but does not close it. |

## The three final blocker reproductions

Each on a **fresh hermetic `git archive HEAD` mirror** of the final tree, run against the **committed build** hosts execute. The mirror's `scripts/model-tiers.js` was `cmp`-verified byte-identical to the committed one before any run.

### Blocker 1 — the kit-ref spellings exit 1 by name

Mirror of `2de446e`, 1707 files. Fix present in the committed build the mirror runs: `configReferencesIn` 3 occurrences, `relKey` 5.

Occurrence counts were re-measured by an **`indexOf` loop that is not the gate's counter and not a regex**, before any run was believed.

| Plant | Byte delta | Needle occurrences (independent) | Mutation asserted | TRUE exit |
|---|---|---|---|---|
| P1 bare-directory | **+55** | 3 → 4 | yes | **1** |
| P2 suffix-traversal | **+23** | 3 → 3 | yes | **1** |
| P3 no-capture prefix | **+88** | 3 → 5 | yes | **1** |
| P4 union of all three | **+166** | 3 → 6 | yes | **1** |

P1 and P2 reproduce round 4's recorded deltas exactly. P3/P4 are **3 bytes larger** than plan 22's recorded +85/+163 because my P3 sentence wording differs; the load-bearing measurement — the occurrence delta 3 → 5 and 3 → 6 — matches round 4's recorded true occurrence counts exactly. Stated rather than smoothed over.

**Clean control**, unmutated mirror — the gate is not inert:

```
[Assertion 1] no agent-factory/config/ refs remain (config now .grugops/factory.config.json)
  PASS  no agent-factory/config/ refs remain (3 counted self-reference mention(s) on 2 line(s) exempt, in agent-factory/config/factory.config.md)
  PASS  the config self-reference exemption is exactly 3 mention(s) on 2 line(s) in 1 file(s), as declared
TRUE EXIT = 0
```

**The stray half NAMES the planted line.** From P2 — the spelling that moves no cardinality at all, and the one round 4 recorded shipping at exit 0:

```
  FAIL  stray agent-factory/config/ ref(s) — config must be .grugops/factory.config.json:
agent-factory/config/factory.config.md:138:**Where the block is read from, and which file wins.** ...
  FAIL  the agent-factory/config/ self-reference exemption has drifted — it is 3 mention(s) on 2 line(s) in 1 file(s) BY DECLARATION, and a new mention must be argued and counted, never absorbed:
```

### Blocker 2 — the two regressed key shapes return a named refusal

**On the final tree**, against the committed `.js`:

```
[Symbol key, legal alias]
  ok:false — model-tiers: the override names the role stem undefined, which is not one of the 2 stem(s) this resolution covers: "orchestrator", "software-engineer"

[throwing-toString key, legal alias]
  ok:false — model-tiers: the override names the role stem {}, which is not one of the 2 stem(s) this resolution covers: ...
```

**Discrimination — the watched red.** Mutation: revert both stem positions from the one authority back to raw interpolation, in the committed `.js` the mirror runs.

- **Mutation landed:** 92315 → 92295 bytes (**−20**).
- **Independent re-measurement of the property moved:** `quoteValue(stem)` occurrences **2 → 0**.
- **Result:**

```
[Symbol key, legal alias]              *** THREW *** TypeError: Cannot convert a Symbol value to a string
[throwing-toString key, legal alias]   *** THREW *** Error: boom
```

Both shapes throw under the mutation and refuse without it. The property is real and the case discriminates.

**A residual noticed while running this** and not silently passed over: the Symbol key renders as the literal `undefined` in the refusal (`JSON.stringify(Symbol)` returns `undefined`). It **refuses**, which is the blocker's declared axis, but it does not usefully *name* the offending key. This is adjacent to round-4 IN-01's complaint about `describeShape(null)` and is covered by the filing of IN-01 as WINDOWS row 101.

### Blocker 3 — the slice-base divergence reds the named case

Mirror of the final tree, `node_modules` symlinked, run against `scripts/check-foundation-guards.test.ts`.

**Baseline (unmutated):** `4 failed | 261 passed (265)`. The four are the git-dependent census/tripwire cases a non-git mirror cannot run — named and confirmed, and `(r-base-discriminating)` is **not** among them.

**Mutation:** the exact divergence R3-IN-03 named — `ci.slice(at + UBUNTU_BLOCK_STEP_NAME.length)` → `ci.slice(at)` — applied to **both** `ci-workflow.testkit.ts` and `ci-workflow.testkit.js`, because plan 24's D8 measured that vitest resolves the testkit specifier to the committed `.js` and a `.ts`-only mutation would be invisible.

- **Mutation landed:** `.ts` 9512 → 9480 (**−32**), `.js` 9447 → 9415 (**−32**); remaining `slice(at + UBUNTU` sites in both: **0**.
- **Independent re-measurement of the property moved** — the computed region head, read by importing the testkit directly rather than through the test:
  - before: `"if: matrix.os == 'ubuntu-latest',run: |,npm run freshness,np"`
  - after: `"Freshness gates + repo gates (ubuntu only),if: matrix.os == "`
  - The region now begins **at** the step name instead of after it. The property genuinely moved.
- **Result:** `5 failed | 260 passed (265)` — baseline **+1**, and the added failure is named:

```
× (r-base-discriminating) the region begins after the step name, proven by two computed regions rather than by source text
```

Round 4's measurement of this identical mutation was byte-for-byte unchanged. It now moves the failure set.

*(My probe printed a `CONTAINS the step name? no` line that is misleading — the heuristic looked for the literal `- name: Ubuntu` while the step is actually named `Freshness gates + repo gates (ubuntu only)`. The raw region head above is the dispositive evidence, and the heuristic's answer was ignored rather than relied on.)*

## Full gate set on the final tree

| Gate | Result | Baseline comparison |
|---|---|---|
| `npm run typecheck` | **exit 0** | — |
| `npm run build` | **exit 0** | — |
| `git diff --exit-code --name-only -- '*.js'` | **exit 0, empty** | build reproduces the committed `.js` |
| `npm run freshness` | **exit 0** | 50 committed `.js` match a rebuild; 0 committed-and-absent, 0 on-disk-and-absent |
| `node scripts/check-foundation-guards.js` | **exit 0** | ALL CHECKS PASSED |
| `node scripts/check-kit-refs.js` | **exit 0** | invariant marker at all 26 marker sites |
| `VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js` | **exit 0** | ALL CHECKS PASSED |
| `npm run check:public-docs` | **exit 0** | 10 public docs, zero retired vocabulary |
| `npm run check:banned-claims` | **exit 0** | 117 derived documents, 22 pinned literals |
| `npm run check:claim-anchors` | **exit 0** | 47 registry rows, 47 verbatim comparisons byte-identical |
| `npm run check:audit-register` | **exit 0** | all four equalities hold |
| `npx vitest run --exclude '**/scripts/e2e/**'` | **1 failed / 2393 passed / 2 skipped (2396), 55 files** | **IDENTICAL to plan 24's pinned final-tree baseline.** Delta 0. |

**The one failure is the known pre-existing defect** — `scripts/frontmatter.test.ts`, the escape-ordering false refusal in WINDOWS row 91 / `deferred-items.md` D-29.1-22-01. Not fixed here, per instruction. Note what it refuses: `.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round4.md`, for a `\[` sequence inside a double-quoted scalar — the round-4 document itself.

**My delta is zero and that was checked by reading the failure list, not only the count.** After both of my commits the refusal list still contains exactly one entry, and neither `29.1-CONTEXT.md` nor `WINDOWS.md` appears in it (`scripts/frontmatter.test.ts` alone: 1 failed / 293 passed).

**Prohibitions, all asserted empty:**

```
git diff --name-only -- .planning/REQUIREMENTS.md scripts/ install/ hooks/          → empty
git diff --name-only -- CHANGELOG.md                                               → empty
git diff --name-only -- .claude/agents/ .claude/skills/ scripts/measure-cost.ts \
    scripts/measure-cost.js package.json package-lock.json .planning/REQUIREMENTS.md → empty
git diff --exit-code -- scripts/ agent-factory/   (after every scratch mutation)   → exit 0, clean
```

Every mutation stayed inside a hermetic mirror. The real tree was never mutated.

## What this round did NOT do — residuals, with directions and ledger rows

| Residual | Direction | Row |
|---|---|---|
| The CHANGELOG entry for the D-06 reversal | **FAIL-OPEN for changelog consumers.** No gate reads reachability, so nothing mechanical will prompt the entry when the branch first becomes reachable. | **99** (new) |
| Every prose gate excludes `.planning/` at the walk | **FAIL-OPEN.** A banned claim, a cost sentence or caveman voice written into any `.planning/` surface passes every gate green. | **100** (new) |
| Round-4 IN-01 — `describeShape(null)` renders as "an object rather than a Map" | Worst rendering of the exact case Floor 0b exists to reject | **101** (new) |
| Round-4 IN-02 — `join(dir)+sep` computed twice | A second spelling for the first to drift from | **102** (new) |
| Round-4 IN-03 — `walk()` follows symlinks, no cycle guard | A symlink can join the set the D-08.1 exemption is counted over | **103** (new) |
| Round-4 IN-04 — `sectionCitationsIn` throws on a lone backtick-`###` | Unbounded scope on an otherwise right default | **104** (new) |
| `resolveModels` still throws on four input classes | Totality claim false; disclosed open, deliberately not half-fixed | 95 |
| WR-01's Windows half never executed | Reasoned under `path.win32` only; no `windows-latest` leg exists | 89 |
| Vitest resolves the testkit to the committed `.js` | A `.ts`-only divergence is invisible to those cases; bounded by `freshness` | 97 |
| `frontmatter.ts` escape-ordering false refusal | Pre-existing; FALSE RED, never a bypass | 91 |
| Plan 22's clean-tree premise case has no pre-fix RED | A premise pin, not a discriminator — labelled as such | 22-SUMMARY §Deviations 3 |

**Ledger movement this plan:** `open_count` 94 → 100, `total_count` 98 → 104. Six new rows, one row amended in place (86, no count change).

## Decisions Made

- **`record-decision-only` applied exactly.** No changelog entry, no second surface. The option not taken is recorded as owed, so the next reader meets a decision rather than an absence.
- **`D-29.1-18`, phase-qualified.** The reasoning lives inside the decision, because a reader meeting `D-29.1-18` will wonder why it is not `D-18` and the answer must be where they are looking.
- **Row 86 stays `open`.** The ruling settled where the reversal is recorded; it did not close the FAIL-CLOSED residual the row exists to disclose. Closing it would have been the ruling doing work it was not asked to do.
- **The four Info findings were filed, not merely noted.** A summary scrolls out of context; the ledger blocks `/gsd-ship`. Noting them here alone would have reproduced the drop this reconciliation exists to catch.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] The plan's `<verify>` invokes `validate-agent-factory.js` bare, which cannot pass on any tree**

- **Found during:** Task 2 (prose gates)
- **Issue:** `node scripts/validate-agent-factory.js` exits 1 with `VALIDATE_KIT_ROOT is unset — refusing to default the kit root to '.' (C3)`. This is the validator's deliberate no-false-green guard, not a defect and not caused by my change. The plan's verify block omits the variable.
- **Fix:** Used the sanctioned invocation recorded in `.github/workflows/ci.yml:451`: `VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js` → exit 0.
- **Files modified:** none
- **Verification:** ALL CHECKS PASSED
- **Committed in:** n/a (no file change)

**2. [Rule 2 - Missing critical] Four round-4 items had no owner, and the plan had no slot for that outcome**

- **Found during:** Task 3 (reconciliation)
- **Issue:** The plan assumed every round-4 item would map to a plan that closed it or a stated reason it was carried. IN-01 … IN-04 mapped to neither: no plan claimed them, no ledger row carried them. A disposition of "carried" with no owner and no record is indistinguishable from a drop.
- **Fix:** Filed all four as WINDOWS rows 101-104 with their directions and `Owner: unassigned`, and marked them `CARRIED — NO OWNER` in the table so the absence is visible rather than absorbed.
- **Files modified:** `.planning/WINDOWS.md`
- **Verification:** Searched by id and independently by subject across all round summaries, `WINDOWS.md` and `deferred-items.md` — zero hits before filing; rows 101-104 present after, counts consistent.
- **Committed in:** the plan-metadata commit, not `77d7f1e`. Rows 99-100 landed in `77d7f1e`; rows 101-104 were filed later in the session, after the reconciliation had been built, and are carried by the final commit. Recorded precisely because an attribution to the wrong hash is exactly the kind of unchecked claim this round exists to catch.

**3. [Rule 2 - Missing critical] The prose gates do not reach either surface this plan wrote**

- **Found during:** Task 2
- **Issue:** Task 2's acceptance criteria require stating when a gate does not reach a surface the task wrote. Measured rather than assumed: `BANNED_CLAIM_EXCLUDED_LOCATIONS` carries the segment class `**/.planning/` enforced at the walk, and `check-kit-refs.ts` states `.planning/` is intentionally absent from its scan. All six green verdicts say nothing about my two files.
- **Fix:** Ran a compensating measurement — extracted the gate's own 22 pinned claim literals and ran them directly over this plan's added text (0 hits), plus a cost-vocabulary sweep. Disclosed the gap as WINDOWS row 100 rather than letting the green imply coverage.
- **Files modified:** `.planning/WINDOWS.md`
- **Verification:** 22 literals recovered — matching the gate's own reported count of 22, which validates the extraction. 0 banned-literal hits in added text.
- **Committed in:** `77d7f1e`

**4. [Measurement contradicts the plan] The phase commit count is 130, not the 111 the plan states**

- **Found during:** Task 1
- **Issue:** The plan's context fact 4 says `git diff 6f8411e..HEAD -- CHANGELOG.md` is empty across "111 commits". Measured now: **130**. The plan quoted round 4's figure at `ad033f0`; 19 commits have landed since.
- **Fix:** Reported the current figure in the checkpoint and in this SUMMARY, with both numbers and the reason they differ. The finding is not weakened — it is 19 commits stronger.
- **Files modified:** none
- **Verification:** `git rev-list --count 6f8411e..HEAD` → 130; `6f8411e..ad033f0` → 111; `ad033f0..HEAD` → 19.

**5. [Self-caught measurement error] The first exhaustion probe reported a false uncounted item**

- **Found during:** Task 3
- **Issue:** My initial exhaustion regex used `\s+` in a negative lookahead, which matched across a newline and reported 1 bullet at an uncounted indent depth. Left unexamined, that "1" would have read as a real dropped item.
- **Fix:** Re-ran the check line-anchored, bucketing every bulleted line explicitly: 5 + 15 + 15 = 35, unaccounted 0.
- **Verification:** Both the corrected exhaustion pass and the sibling-key cross-check agree with Methods A and B.

**6. [Cost-vocabulary hit, measured and dispositioned] "cheap" appears in the added text**

- **Found during:** Task 2 compensating sweep
- **Issue:** The cost-vocabulary sweep flagged `cheap`, from `— **Reversibility:** cheap — a record and prose.`
- **Disposition:** Not a MODEL-07 violation. It rates the effort to reverse a decision, not model spend, and it is this section's established convention — three prior instances at lines 190, 208 and 229 (D-15, D-16, D-17). Mine is the fourth. Recorded rather than passed over silently, because a flagged literal that goes unmentioned is indistinguishable from one that was never checked.

---

**Total deviations:** 6 (2 missing-critical auto-fixed, 1 blocking auto-fixed, 1 plan-fact correction, 1 self-caught measurement error, 1 measured-and-dispositioned literal)
**Impact on plan:** No scope creep. Two of the six are the plan's own no-silent-drops truth doing its job — the reconciliation found items the plan did not anticipate existing, and filing them is what the truth requires.

## Issues Encountered

- The hermetic mirror initially failed with `ERR_MODULE_NOT_FOUND` because `git archive` carries no `node_modules`. Resolved by symlinking the repository's `node_modules` into the mirror for the vitest run. The mirror's *source and build artifacts* remain hermetic — only the test runner's dependencies are shared.
- The first Blocker 2 probe script resolved its relative import against `/tmp` rather than the mirror. Moved inside the mirror and re-run.

## Known Stubs

None. This plan wrote prose and ledger rows; it added no code and no placeholder.

## Threat Flags

None. No new network, auth, file-access or schema surface — `git diff --name-only -- scripts/ install/ hooks/` is empty for this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Round 5's ruling load is now explicit.** Four human items await a ruling (the three carried verbatim plus WR-01's Windows half), and four newly-filed Info findings await an owner.
- **`open_count` is 100.** With `workflow.windows_enforce` on, `/gsd-ship` blocks until these are waived or fixed. Six of those rows were opened by this plan deliberately — they are disclosures, not regressions.
- **The phase's mechanical work is green.** Full gate set exits 0 and the suite matches its pinned baseline exactly, with one known pre-existing failure that is a false red and never a bypass.
- **`.planning/REQUIREMENTS.md` was not touched.** The verdict on MODEL-04, MODEL-06 and MODEL-07 belongs to phase verification, per the plan's explicit prohibition.

---
*Phase: 29.1-per-role-model-assignment*
*Completed: 2026-08-21*

## Self-Check: PASSED

- `.planning/phases/29.1-per-role-model-assignment/29.1-25-SUMMARY.md` exists on disk.
- Commits `2de446e`, `77d7f1e`, `7ce9f21` all present in `git log --oneline --all`.
- Disposition table row count 64 equals the asserted item count 64.
- The three carried human items are byte-identical to `29.1-VERIFICATION-round4.md:112-121` (verified by `diff`, no output).
- `CHANGELOG.md`, `.planning/REQUIREMENTS.md`, `scripts/`, `install/` and `hooks/` all byte-unchanged by this plan.

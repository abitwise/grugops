---
phase: 29.1-per-role-model-assignment
plan: 10
type: execute
wave: 4
depends_on: ["29.1-09"]
gap_closure: true
files_modified:
  - agent-factory/packaging/subagent.frontmatter.md
  - scripts/model-dial-consistency.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [MODEL-06, MODEL-07]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WR-06 closed: the kit's single authority no longer asserts an unsourced absolute about four third-party products. The replacement sentence states the dial's reach as a fact about what grugops emits — a fact provable by grep in this repository — and every clause about the other four host CLIs is a cited vendor fact."
    - "The citation block naming one primary vendor source per CLI ships beside the sentence, following the named-source convention the assumption A1 note at `scripts/model-tiers.ts:44-60` already uses, so the claim and its sources cannot drift apart."
    - "The oracle's pinned literal moved WITH the sentence: `scripts/model-dial-consistency.test.ts` pins the replacement verbatim, and the sentence remains ONE physical line so the literal needs no wrap handling."
    - "The three named residuals ship as explicit `UNKNOWN - verify` items rather than being dropped — R1, that all four findings are point-in-time reads dated 2026-08-20 with no introduction version established; R2, whether the Copilot CLI HONOURS the model property at run time as distinct from parsing it; R3, that whether grugops should emit agent definitions for the other four is deliberately out of scope."
    - "The oracle case asserting a source travels within 600 characters of an `UNKNOWN - verify` marker is repaired rather than accidentally satisfied: its premise that the authority carries exactly one such marker is now false, so the case locates the assumption A1 marker by its own anchor and the total marker count is pinned."
    - "The two planning surfaces that restate the refuted premise are corrected: `REQUIREMENTS.md`'s MODEL-06 text and `ROADMAP.md`'s SC-4, each with a dated note recording that the original wording rested on a premise this round's research refuted. SC-4 as written is unsatisfiable by any true sentence, so correcting it is a precondition for closing MODEL-06 honestly."
    - "`CLAUDE.md:125` is verified to need no change on this axis and that finding is recorded with its evidence rather than assumed."
    - "Per D-13 the scope statement still has ONE authority and `factory.config.md` still points at it by path without restating it; per D-16 no cost or limit-savings claim enters any sentence this plan writes."
    - statement: "No surface in the shipped kit asserts a capability about the other four host CLIs that this phase did not establish."
      verification: backstop
  artifacts:
    - agent-factory/packaging/subagent.frontmatter.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "`scripts/model-dial-consistency.test.ts`'s `SCOPE_SENTENCE` literal and the authority document's sentence are the same bytes, asserted present exactly once in the authority and absent from the pointing document."
    - "Each citation line names one CLI, one vendor URL and the artefact shape the `model` field lives in, so a reader can check any single claim without reading the research file."
    - "`ROADMAP.md`'s SC-4 and `REQUIREMENTS.md`'s MODEL-06 both describe the dial's reach as a property of what this kit emits, matching the authority sentence rather than restating it."
  prohibitions:
    - "No claim about the other four host CLIs ships without a named source. This repository's hard rule forbids faking a citation, and an unverified absolute presented flat is that class."
    - "MODEL-06's checkbox in `REQUIREMENTS.md` is NOT flipped to complete by this plan. Verification decides that; a plan that marks its own requirement done is the premature-complete failure this project has recorded."
    - "`factory.config.md` does not gain its own copy of the scope sentence — it points at the authority by path, per D-13, and the oracle asserts the absence direction."
    - "No sentence written by this plan asserts a cost, spend, quota or usage-limit saving."
    - "No caveman voice in the authority section, the citations or the residuals — this is a compliance-adjacent honesty surface."
    - "No source file with a committed `.js` twin is modified by this plan; `scripts/model-dial-consistency.test.ts` is a test file and is not compiled to a committed twin."
    - "`npm test` is never run. The regression command is `npx vitest run --exclude '**/scripts/e2e/**'`."
---

<objective>
Replace a false sentence with a true one, and correct the two planning surfaces that restate
its refuted premise.

WR-06 and roadmap criterion SC-4 both turn on one sentence at
`agent-factory/packaging/subagent.frontmatter.md:243`. The verifier established that the phase
had established nothing: `grep -icE 'opencode|codex cli|gemini cli|copilot cli'` over
`29.1-RESEARCH.md` returned 0. The user ruled "research all four and cite", and that research
is now DONE and appended to `29.1-RESEARCH.md` as §"Host-CLI per-agent model support".

**Its verdict is that the shipped sentence is FALSE.** All four CLIs — Codex CLI, Gemini CLI,
OpenCode and GitHub Copilot CLI — have a per-agent `model` field, each confirmed HIGH against
primary vendor documentation, and three of the four document an inherit-by-default semantic
essentially identical to the one this kit relies on. The sentence's CONCLUSION happens to be
true: the dial does not reach them. But it is true for a different reason — grugops emits no
agent definition for any of them, which is a fact about this kit, verifiable locally, needing
no citation at all. A true conclusion resting on a false premise is still a false sentence,
and this one is pinned verbatim by `scripts/model-dial-consistency.test.ts:80-84` as the kit's
single authority, so every other surface defers to it. The oracle propagates the claim rather
than checking it.

The premise is not confined to that document. `REQUIREMENTS.md:95` asserts that per-subagent
model selection exists on Claude Code alone. `ROADMAP.md:738` asserts that the other four have
no such concept and that no claim says otherwise — **as written, that criterion is now
unsatisfiable by any true sentence**, so correcting it is not tidiness, it is a precondition
for closing MODEL-06 honestly. Both corrections carry a dated note recording what refuted the
original wording, because this project annotates rather than rewrites.

Output: an authority sentence whose every clause is either a cited vendor fact or a local one,
four citations, three disclosed residuals, an oracle whose pinned literal moved with the
sentence, and two planning surfaces that no longer contradict it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-per-role-model-assignment/29.1-CONTEXT.md
@.planning/phases/29.1-per-role-model-assignment/29.1-RESEARCH.md
@.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION.md
@.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW.md
@.planning/phases/29.1-per-role-model-assignment/29.1-05-SUMMARY.md
</context>

## Artifacts this phase produces

| Artefact | Kind | Purpose |
|----------|------|---------|
| the replacement scope sentence | prose, one physical line | the corrected single authority in `subagent.frontmatter.md` §"Host-CLI scope of the model dial" |
| the per-CLI citation block | prose, five lines | one primary vendor source per CLI, retrieval-dated |
| residuals R1, R2, R3 | prose, `UNKNOWN - verify` | version pinning; Copilot CLI run-time honouring; whether grugops should emit for the four |
| `SCOPE_SENTENCE` | changed const in `model-dial-consistency.test.ts` | the pinned literal, moved verbatim with the sentence |
| `UNKNOWN_VERIFY_MARKER_COUNT` | new const in `model-dial-consistency.test.ts` | the pinned number of `UNKNOWN - verify` markers in the authority, so a fourth cannot arrive silently |
| MODEL-06 amendment note | prose in `REQUIREMENTS.md` | dated record of what refuted the original wording |
| SC-4 amendment note | prose in `ROADMAP.md` | same, on the criterion |

No new file, no new source module, no new dependency. No committed `.js` twin is rebuilt by
this plan.

## Planner assumptions carried forward

See `29.1-07-PLAN.md` §"Planner assumptions carried forward" for the 14 unresolved
`29.1-EDGE-COVERAGE.json` items. Relevant here: MODEL-06 carries an `encoding` probe — "whose
definition of length or equality applies: bytes, code points, grapheme clusters, or normalized
form". That probe is directly live for this plan, because the oracle compares the scope sentence
by exact code-point substring with no normalization. The residual is already disclosed in
`model-dial-consistency.test.ts`'s header with its direction stated; this plan must confirm that
disclosure still describes the code after the literal moves, and does not close the probe.

<tasks>

<task type="auto">
  <name>Task 1: Replace the false authority sentence with the researched one, its citations and its residuals, moving the oracle's literal with it</name>
  <files>agent-factory/packaging/subagent.frontmatter.md, scripts/model-dial-consistency.test.ts</files>
  <read_first>
    - `.planning/phases/29.1-per-role-model-assignment/29.1-RESEARCH.md` §"Host-CLI per-agent model support (gap-closure research, MODEL-06 / WR-06)" IN FULL, beginning at line 787. This section is the authority for everything in this task. It carries the four-row findings table, the per-CLI narrative with verbatim vendor quotations, the in-repo fact table showing what grugops actually writes into a target for each CLI, the verdict, **the proposed replacement sentence verbatim as one physical line**, **the citation block verbatim in this repository's existing convention**, the consequential-edits list, and residuals R1, R2 and R3 with their reasoning. Use its text; do not compose a substitute.
    - `agent-factory/packaging/subagent.frontmatter.md` §"Host-CLI scope of the model dial" in full — the section's opening paragraph declaring it the single authority, the sentence at line 243, the assumption A1 `UNKNOWN - verify` block that follows it and names `CLAUDE.md` line 84 as its source, and the `Reference:` line at the tail whose convention the new citation block follows.
    - `scripts/model-dial-consistency.test.ts` lines 1-50 — the header explaining why this oracle asserts a pointer rather than prose, and the disclosed normalization residual with its stated direction.
    - `scripts/model-dial-consistency.test.ts` lines 72-84 — the `SCOPE_SENTENCE` literal and the comment recording that the sentence lives on one physical line so the literal needs no wrap handling, and that a hard wrap introduced there is a RED on the presence direction.
    - `scripts/model-dial-consistency.test.ts` lines 176-204 — the five cases in the one-authority describe block, in particular the case asserting an `UNKNOWN - verify` marker exists and that `CLAUDE.md line 84` appears within 600 characters of it. That case locates the FIRST marker in the file by `indexOf`, so adding a second marker above the A1 note silently changes what it measures.
    - `scripts/model-tiers.ts` lines 44-60 — the assumption A1 `UNKNOWN - verify` block whose named-source convention the new residuals follow.
  </read_first>
  <action>
Replace the scope sentence in `agent-factory/packaging/subagent.frontmatter.md` with the
replacement sentence the research section carries verbatim. Copy it as one physical line — the
oracle's literal depends on that and the research section states it was written for that reason.
Do not paraphrase, re-wrap or re-punctuate it.

Immediately beneath it, add the citation block the research section carries verbatim: one line
per CLI, each naming the vendor URL and the artefact shape the `model` field lives in, under the
retrieval-dated heading the research section supplies. Follow the `Reference:` convention this
document already uses at its tail and the named-source convention of the assumption A1 block one
module away.

Add the three residuals as explicit `UNKNOWN - verify` items in the same section, each stating
what is unknown and the direction of the unknown, sourced from the research section's residuals
subsection. Place them AFTER the existing assumption A1 block, not before it. The ordering is
load-bearing rather than aesthetic: the oracle locates a marker by first occurrence, and a new
marker above A1 would silently change which block that case measures.

Move `SCOPE_SENTENCE` in `scripts/model-dial-consistency.test.ts` to the replacement sentence,
byte for byte. Keep the comment explaining that the sentence is one physical line and that a hard
wrap is a RED on the presence direction, and extend it to record that the sentence changed in
this round because its premise was refuted, naming WR-06.

Repair the assumption-A1 case whose premise this edit invalidates. It currently assumes the
authority carries exactly one `UNKNOWN - verify` marker and locates it by first occurrence. Give
it an anchor of its own so it measures the A1 block specifically rather than whichever marker
happens to come first, and add a pinned count of the markers in the authority with a promote
trigger recorded beside it, so a fourth residual arriving later moves a number in the commit that
adds it rather than silently shifting what an existing case measures.

Add cases asserting the new material is present: the citation block names all four CLIs, each
citation line carries a URL, and each of the three residuals is present by its own anchor text.
Assert the vendor claims are attributed rather than bare — every clause of the sentence about the
other four is either accompanied by a citation line naming that CLI or is a statement about what
this kit emits.

Write no sentence asserting a cost, spend, quota or usage-limit saving. Keep the whole section in
clear professional voice.
  </action>
  <verify>
    <automated>npx vitest run scripts/model-dial-consistency.test.ts && npm run check:banned-claims && npm run check:claim-anchors && npm run check:public-docs && node scripts/check-foundation-guards.js && npm run freshness && npx vitest run --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - The refuted claim is gone, asserted negatively over the whole shipped tree: `grep -rn 'have no per-subagent model concept' --include='*.md' agent-factory/ CLAUDE.md README.md docs/` returns nothing. Record the command and its empty output.
    - The refuted claim is gone from the oracle too: `grep -c 'have no per-subagent model concept' scripts/model-dial-consistency.test.ts` is 0.
    - The replacement sentence is present in the authority exactly once and absent from the pointing document, asserted by the two existing oracle cases running green against the moved literal.
    - The sentence is ONE physical line: `awk 'length($0)>200' agent-factory/packaging/subagent.frontmatter.md | grep -c 'reaches Claude Code only'` is 1, confirming it was not wrapped.
    - RED-first proof that the pin is load-bearing: introduce a hard wrap into the sentence in the authority document, confirm the presence case goes RED naming the missing sentence, and revert. Record the transcript.
    - All four CLIs are named in the citation block with a URL each: `grep -c 'learn.chatgpt.com\|geminicli.com\|opencode.ai\|docs.github.com' agent-factory/packaging/subagent.frontmatter.md` is at least 4, and each of the four CLI names appears in that block.
    - The three residuals are present and are marked: the count of `UNKNOWN - verify` markers in the authority equals the newly pinned constant, and the assumption A1 case still finds `CLAUDE.md` line 84 within 600 characters of the A1 marker specifically.
    - RED-first proof of the repaired case: insert an additional `UNKNOWN - verify` marker ABOVE the A1 block, confirm the repaired case still passes (because it anchors on A1) while the pinned marker count goes RED, and revert. Under the pre-repair case the same insertion would have turned the A1 case RED for the wrong reason — record both outcomes.
    - `npm run check:banned-claims`, `npm run check:claim-anchors` and `npm run check:public-docs` all exit 0.
    - No cost claim entered: `grep -inE 'cheaper|saving|spend less|reduce cost|lower cost|quota' agent-factory/packaging/subagent.frontmatter.md` is recorded, and every hit is pre-existing and unchanged by this plan. Compare against `git show HEAD:agent-factory/packaging/subagent.frontmatter.md`.
    - `npm run freshness` reports every committed `.js` fresh and `git diff --exit-code --name-only -- '*.js'` is empty — this plan rebuilds nothing.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0. `npm test` is never run.
  </acceptance_criteria>
  <done>The kit's single authority states the dial's reach as a fact about what grugops emits, every clause about the other four host CLIs carries a named vendor source, three residuals ship as explicit unknowns, the oracle's pinned literal moved with the sentence and was proven load-bearing by a wrap mutation, and the case whose premise a second marker would have invalidated now anchors on the block it means to measure.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the two planning surfaces that restate the refuted premise, and record the CLAUDE.md no-change finding</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` line 95 — MODEL-06's text in full, and the six MODEL rows around it so the amended row keeps the family's voice and shape. Note the checkbox state: MODEL-06 currently reads unchecked, reverted to Gaps Found by verification. It stays unchecked.
    - `.planning/REQUIREMENTS.md` §"Coverage by phase" around line 227 — the row mapping MODEL-01 through MODEL-07 to Phase 29.1, which must stay consistent with the amended text.
    - `.planning/ROADMAP.md` §"Phase 29.1: Per-Role Model Assignment (INSERTED)" — the goal, the five success criteria, criterion 4 at line 738, and the "Scope amendment 2026-08-19 (D-17, user-ruled at plan time)" block at the section's tail. That block is the shape and the voice a second dated amendment should follow, and it is the precedent for annotating rather than rewriting.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-RESEARCH.md` §"Consequential edits the plan must carry" — it names both surfaces with their line numbers, states which clause of each is false and which is true, and states that `CLAUDE.md:125` needs no change on this axis with its reason.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION.md` — the SC-4 gap block, whose `missing` items are what the corrected criterion must make achievable.
    - `CLAUDE.md` line 125 — the "What NOT to Use" row this phase amended, read so the no-change finding is a measurement rather than an assumption.
  </read_first>
  <action>
Amend MODEL-06's text in `.planning/REQUIREMENTS.md` so its first clause states the true form:
the model dial's reach, which is a property of the per-agent artefacts this kit generates, rather
than a claim about which host CLIs possess a per-subagent model concept. Keep the second clause
about the `CLAUDE.md` "What NOT to Use" entry unchanged — it was satisfied and verified. Keep the
requirement's checkbox unchecked; this plan does not mark its own requirement complete.

Add a dated amendment note beneath the MODEL row family recording what happened: the original
wording rested on a premise that this phase's gap-closure research refuted on 2026-08-20, all
four non-Claude host CLIs do carry a per-agent model field, and the requirement was narrowed to
what is true and provable rather than being deleted. Follow this project's annotate-never-rewrite
convention — the superseded wording is described in the note, not silently replaced without
trace.

Amend success criterion 4 in `.planning/ROADMAP.md`'s Phase 29.1 section the same way. Its
current final clause makes the criterion unsatisfiable by any true sentence, because it requires
a claim this round proved false. Restate it so what must be TRUE is that the kit's single
authority states the dial's reach as a fact about what grugops emits, that every clause about the
other four host CLIs carries a named source, and that the `CLAUDE.md` row names this mechanism as
the documented reason.

Add a second dated amendment block at the tail of the Phase 29.1 section, following the shape of
the existing D-17 scope-amendment block, recording that criterion 4 was corrected on 2026-08-20
after gap-closure research refuted its premise, naming the research section by path so a later
reader lands on the evidence rather than on the assertion.

Verify `CLAUDE.md` line 125 needs no change on this axis and record the finding with its
evidence rather than editing the file. Read the row, confirm it names the mechanism as the
documented reason and makes no claim about the other four host CLIs' capabilities, and state that
conclusion in the SUMMARY with the row quoted.

Change nothing else in either file. In particular, do not touch any other phase's section, any
other requirement row, or any checkbox.
  </action>
  <verify>
    <automated>git diff --stat -- .planning/REQUIREMENTS.md .planning/ROADMAP.md && npx vitest run scripts/model-dial-consistency.test.ts && node scripts/check-foundation-guards.js && npm run freshness && npx vitest run --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - The refuted premise is gone from both planning surfaces: `grep -n 'no per-subagent model concept\|per-subagent model selection exists on Claude Code alone' .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns nothing. Record the command and its empty output.
    - Both amended texts are consistent with the authority sentence: each describes the dial's reach as a property of what this kit emits. Quote all three — the authority sentence, the amended MODEL-06 and the amended SC-4 — side by side in the SUMMARY and confirm no two of them make a claim the others do not support.
    - Both dated amendment notes exist, each naming 2026-08-20 and each naming `29.1-RESEARCH.md`'s host-CLI section by path.
    - MODEL-06's checkbox is still unchecked: `grep -n 'MODEL-06' .planning/REQUIREMENTS.md` shows the unchecked marker. A checked marker fails this criterion.
    - The blast radius is bounded: `git diff --stat -- .planning/REQUIREMENTS.md .planning/ROADMAP.md` shows changes confined to the MODEL requirement family, the Phase 29.1 section and the two amendment notes. Read the full diff and record the changed hunks by heading in the SUMMARY.
    - No other phase section moved: `git diff -- .planning/ROADMAP.md` contains no hunk under a heading other than Phase 29.1.
    - The `CLAUDE.md` no-change finding is recorded with the row quoted and with `git diff --name-only -- CLAUDE.md` empty. An assumed no-change without the quoted row fails this criterion.
    - `npx vitest run scripts/model-dial-consistency.test.ts` exits 0 — the `CLAUDE.md` row cases are unaffected.
    - `npm run freshness` reports every committed `.js` fresh and `git diff --exit-code --name-only -- '*.js'` is empty.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0. `npm test` is never run.
  </acceptance_criteria>
  <done>MODEL-06 and success criterion 4 both state what is true and provable, each carries a dated note naming the research that refuted the original wording, the criterion is achievable by a true sentence for the first time, MODEL-06's checkbox is untouched, and `CLAUDE.md` was measured rather than assumed to need no change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendor documentation → the kit's shipped prose | An external, versioned, mutable fact is copied into a document this kit ships and an oracle pins |
| the single authority → every other surface | Other documents defer to it by design, so a false sentence there propagates rather than being contradicted |
| planning artefacts → future rounds | A success criterion that cannot be satisfied by a true sentence silently converts every future verification into a failure |

## STRIDE Threat Register

ASVS level 1, block on `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-23 | Repudiation | an unsourced capability claim about third-party products | high | mitigate | Every clause about the other four host CLIs carries a named primary vendor source with a retrieval date; every remaining clause is a fact about this kit provable by grep. Asserted by a negative grep for the refuted claim over the whole shipped tree. |
| T-29.1-24 | Tampering | the pinned literal drifting from the sentence it pins | high | mitigate | The oracle's literal moves in the same edit as the sentence, and a wrap mutation is proven to turn the presence case RED. |
| T-29.1-25 | Spoofing | a second `UNKNOWN - verify` marker silently changing what an existing case measures | medium | mitigate | The A1 case anchors on its own block rather than on first occurrence, and the marker count is pinned with a promote trigger. Proven by inserting a marker above A1 and observing the count go RED while the A1 case stays green. |
| T-29.1-26 | Repudiation | vendor documentation changing after the retrieval date | medium | accept | Residual R1 discloses that all four findings are point-in-time reads dated 2026-08-20 with no introduction version established. Residual R2 discloses the one internally inconsistent vendor surface. Disposition ACCEPT with both disclosed in the shipped document. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | accept | Zero packages installed by this plan; `package.json` is asserted byte-unchanged at plan close. |
</threat_model>

<verification>
- `npx vitest run scripts/model-dial-consistency.test.ts` exits 0.
- `npm run check:banned-claims`, `npm run check:claim-anchors` and `npm run check:public-docs` all exit 0.
- `node scripts/check-foundation-guards.js` exits 0.
- `npm run freshness` reports every committed `.js` fresh; `git diff --exit-code --name-only -- '*.js'` is empty.
- `npx vitest run --exclude '**/scripts/e2e/**'` exits 0. `npm test` is never run.
- `grep -rn 'have no per-subagent model concept' --include='*.md' agent-factory/ CLAUDE.md README.md docs/ .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns nothing.
- `git diff --name-only -- CLAUDE.md package.json install/ scripts/measure-cost.ts` is empty.
</verification>

<success_criteria>
- The refuted claim appears nowhere in the shipped kit, in the oracle, or in the two planning surfaces.
- The replacement sentence is present in the authority exactly once, on one physical line, absent from the pointing document, and pinned by an oracle literal proven load-bearing by a wrap mutation.
- Four vendor citations and three `UNKNOWN - verify` residuals ship beside it.
- The case that would have been silently re-targeted by a new marker anchors on its own block, and the marker count is pinned.
- SC-4 is satisfiable by a true sentence for the first time, and both corrections carry dated notes naming the research that refuted them.
- MODEL-06's checkbox is untouched.
</success_criteria>

<output>
Create `.planning/phases/29.1-per-role-model-assignment/29.1-10-SUMMARY.md` when done.
Record in it: the replacement sentence and the citation block as they landed; the three
residuals; the empty output of every negative grep with the command that produced it; the wrap
mutation transcript; both outcomes of the marker-insertion proof; the three quoted texts
(authority sentence, amended MODEL-06, amended SC-4) side by side; the changed hunks by heading;
and the `CLAUDE.md:125` row quoted with the no-change finding and its evidence.
</output>

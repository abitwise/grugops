---
phase: 29.1-per-role-model-assignment
plan: 23
subsystem: testing
tags: [model-tiers, refusal-path, totality, quoting-authority, build-parity, typescript, vitest]

requires:
  - phase: 29.1-per-role-model-assignment
    provides: "the quoting authority made total for the rejected VALUE and the deleted second spelling (plan 29.1-21), and the D-06 skip reversed into an unknown-stem refusal"
provides:
  - "One quoting authority over BOTH interpolated positions in BOTH override refusals - four call positions, no raw interpolation left in either sentence"
  - "The round-4 BLOCKER 2 regression reversed: a Symbol key and a throwing-conversion key return a refusal instead of throwing"
  - "A shape corpus driven over the KEY axis, the VALUE axis and their 169-cell cross product, with the cell count derived on both axes and an in-loop counter asserted against it"
  - "A thirteenth corpus entry covering the KEY axis's own throwing operator, which the JSON.stringify-derived corpus did not carry"
  - "Two observable denominators, each pinned two-sided and each proven to discriminate against a co-edit the pre-existing set-equality assertion passes over"
  - "A one-spelling scan that reads the committed .js hosts execute as well as the .ts that compiles, naming the file in every failure and stating its comment strip's limits"
  - "The measured counterexamples to this module's totality claim, named rather than left for a sixth round"
affects: [model-tiers, generate-role-adapters, installer-doctor, phase-29.1-verification-round-5]

actuals:
  tokens: 14344
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns:
    - "One authority renders EVERY value a refusal interpolates - a guarded position beside a raw position in one sentence proves nothing, because the raw one kills the same message"
    - "When a fix makes a claim total, widen the corpus to the axis the claim names - and check which OPERATOR each axis fails under before reusing a corpus derived for the other"
    - "An external denominator that nothing observes is an internal denominator with a citation attached: pin it two-sided beside the table it is copied from"
    - "A claim about the module that SHIPS must read the module that ships, not only the one that compiles"
    - "Assert an in-loop counter against the derived product, so a loop that ran short is a failure rather than a smaller success"
    - "Disclose a residual with its direction and its measurement rather than closing half of it and letting the claim look whole"

key-files:
  created: []
  modified:
    - scripts/model-tiers.ts
    - scripts/model-tiers.js
    - scripts/model-tiers.test.ts
    - .planning/WINDOWS.md
    - .planning/phases/29.1-per-role-model-assignment/deferred-items.md

key-decisions:
  - "Route the role stem through quoteValue at both override refusal sites rather than widening the raw interpolation - the structural fix the round-4 verifier prescribed, one authority over one operation"
  - "Add a thirteenth corpus entry (an object whose string conversion throws) because the two axes fail under DIFFERENT operators and the existing corpus could see only one of the two reproduced regressions"
  - "Do NOT fix the four residual throw classes found by adversarial self-verification - closing one of two root causes would leave the totality looking closed while remaining false; held as content with a disclosed backstop per D-59"
  - "Rename and widen the existing one-spelling case rather than adding a second scan, because two scans would be two authorities over one predicate"
  - "The D-06 reversal's RECORD half is ruled on in plan 29.1-25 and is deliberately not decided here"

patterns-established:
  - "Ask which OPERATOR an axis fails under before reusing a corpus derived for a different axis"
  - "Prove a pin discriminates by showing the assertion it sits beside stays GREEN under the same co-edit"

requirements-completed: [MODEL-04, MODEL-05]

coverage:
  - id: D1
    description: "Both override refusal templates render every value they interpolate through the one quoting authority - four call positions, no raw interpolation left in either sentence"
    requirement: MODEL-04
    verification:
      - kind: unit
        ref: "scripts/model-tiers.test.ts#the override refusal RETURNS for every shape in the corpus at the KEY position, never throwing out of itself"
        status: pass
      - kind: integration
        ref: "three-build reproduction over e9907f5, ad033f0 and the rebuilt committed .js, one script, identical inputs"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every legal (string) key's refusal wording is byte-identical before and after"
    requirement: MODEL-04
    verification:
      - kind: integration
        ref: "diff of 127 refusal renderings, pre-change build vs post-change build - empty"
        status: pass
      - kind: unit
        ref: "scripts/model-tiers.test.ts#a SERIALISABLE value renders byte-identically through the authority - no refusal wording moved"
        status: pass
    human_judgment: false
  - id: D3
    description: "The totality corpus is driven over the KEY axis, the VALUE axis and their 169-cell cross product, with the cell count derived on both axes and an in-loop counter asserted against it"
    requirement: MODEL-05
    verification:
      - kind: unit
        ref: "scripts/model-tiers.test.ts#the override refusal RETURNS for every KEY-by-VALUE cell in the corpus, and the cell count is derived from both axes"
        status: pass
    human_judgment: false
  - id: D4
    description: "WR-02 closed - both denominators are observable, pinned two-sided, and proven to discriminate against a co-edit the pre-existing set-equality assertion passes over"
    requirement: MODEL-05
    verification:
      - kind: unit
        ref: "scripts/model-tiers.test.ts#the shape corpus and the language codomain are both observable numbers, pinned two-sided"
        status: pass
      - kind: integration
        ref: "three co-edit mutations in a scratch worktree at 9ae707e, each recorded red with the neighbouring assertion recorded green"
        status: pass
    human_judgment: false
  - id: D5
    description: "WR-04 closed - the one-spelling scan reads the committed .js as well as the .ts, names the file in every failure, and states its comment strip's limits"
    requirement: MODEL-05
    verification:
      - kind: unit
        ref: "scripts/model-tiers.test.ts#the quoting operation has ONE spelling in the module that ships as well as the one that compiles"
        status: pass
      - kind: integration
        ref: "second spelling planted in the committed .js only, scratch worktree - widened case reds naming model-tiers.js, narrow case at 5ec040d passes the identical plant"
        status: pass
    human_judgment: false
  - id: D6
    description: "The overrides docstring on ResolveModelsOptions states the by-name refusal of an entry outside the resolved stem set"
    requirement: MODEL-04
    verification: []
    human_judgment: true
    rationale: "A docstring states a contract in prose. That the sentence is present is checkable; that it describes the mechanism accurately and in this project's voice is a reading, and no test asserts it."
  - id: D7
    description: "The module's totality claim - and this plan's own first stated truth - is NOT achieved. Four input classes still throw, measured across four builds and disclosed rather than closed."
    requirement: MODEL-05
    verification:
      - kind: integration
        ref: "adversarial probe over four committed builds, one script - four throw classes recorded, provenance established per class"
        status: fail
    human_judgment: true
    rationale: "This is a deliberate non-closure ruled under D-59, not an unproven claim. A human must decide whether the totality is held as a mechanism at all before a later plan attempts it again; the four counterexamples and both root causes are named in WINDOWS row 95 and in the phase deferred-items file."

duration: 26 min
completed: 2026-08-21
status: complete
---

# Phase 29.1 Plan 23: One Authority on the Key Axis Summary

**The role stem now goes through the same quoting authority the alias already went through, so no override map throws — and the corpus written to prove that is driven over both axes and their 169-cell union, with its denominators made observable and its scan pointed at the module that actually ships.**

## Performance

- **Duration:** 26 min
- **Started:** 2026-08-21T17:27:00Z
- **Completed:** 2026-08-21T17:53:00Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- **BLOCKER 2 closed on its declared axis.** Both override refusal templates obtain both of their interpolated values from `quoteValue`. The two shapes the round-4 verifier reproduced return a refusal at the rebuilt committed `.js`.
- **Every legal key's wording is unmoved, measured not asserted.** 127 refusal renderings diffed pre-change against post-change: empty.
- **The corpus now measures the axes its describe block names** — KEY, VALUE, and the 169-cell cross product — and needed a thirteenth entry to do it, because the two axes fail under different operators.
- **WR-02 closed.** Both denominators pinned two-sided, both proven to discriminate against a co-edit the pre-existing set-equality assertion passes over.
- **WR-04 closed.** The one-spelling scan reads the committed `.js`; the narrow case passes the exact plant the widened one reds on.
- **The totality claim is disclosed as still false, with four measured counterexamples.** Found by adversarial self-verification at plan close, by no review.

## Task Commits

1. **Task 1: One authority renders every value in the sentence** — `9ae707e` (fix)
2. **Task 2: The corpus measures the axes the claim names** — `5ec040d` (test)
3. **Task 3: The one-spelling scan reads the module that ships** — `dfc6ab8` (test)
4. **Closing adversarial self-verification records** — `fd211f3` (docs)

**Plan metadata:** see the final `docs(29.1-23)` commit.

## The three-build reproduction

Re-measured by this plan from one script with identical inputs, never inherited from the round-4
report. Stems derived from the kit authority: `["agents-md-scribe","architect-design"]`.

| Input | `e9907f5` | `ad033f0` | rebuilt HEAD (`9ae707e`) |
|---|---|---|---|
| I1 Symbol key + legal alias `opus` | `RETURNED {"ok":true,"value":{}}` | `THREW TypeError: Cannot convert a Symbol value to a string` | `RETURNED ok:false` naming the stem position |
| I2 throwing-`toString` key + legal alias | `RETURNED {"ok":true,"value":{}}` | `THREW Error: key-boom` | `RETURNED ok:false` naming the stem position |
| I3 Symbol key + misspelled alias `opuss` | `THREW TypeError: Cannot convert a Symbol value to a string` | `THREW` (same) | `RETURNED ok:false` naming the alias |
| I4 CONTROL covered stem + legal alias | `RETURNED {"ok":true,"value":{}}` | `RETURNED {"ok":true,"value":{}}` | `RETURNED {"ok":true,"value":{}}` |

**I3 is a finding the plan did not predict and it changes the diagnosis.** The plan's expectation was
that both reproduced shapes returned `{ok:true}` at `e9907f5`. That holds for I1 and I2. It does
**not** hold for I3: a Symbol key with an ILLEGAL alias already threw at `e9907f5`, because the
ALIAS sentence's raw stem interpolation predates plan 29.1-21's reversal entirely. So the raw-stem
defect existed at two sites and only one of them was newly reachable; the round-4 report described
the reversal as the cause, and the reversal is the cause of the *reachability*, not of the defect.
This is why the fix lands at two sites and not one.

**The reverted-build discrimination** required by the acceptance criteria did not need a working-tree
mutation: the pristine committed build at `a58036b` **is** the build with the stem routing absent,
and it throws on both inputs (transcript above, column `ad033f0` continues unchanged at `a58036b`).
The working tree was never mutated for this, and `git diff --exit-code -- scripts/ agent-factory/` is
clean at plan close.

## The two templates after the change, and the four call positions

```ts
`model-tiers: the override for role ${quoteValue(stem)} is ${quoteValue(alias)}, which ` +
`is not a legal model alias. The legal set is exactly: ` +
`${MODEL_ALIASES.map((a) => `"${a}"`).join(", ")}. This floor exists because the resolved ` +
```

```ts
`model-tiers: the override names the role stem ${quoteValue(stem)}, which is not one of ` +
`the ${String(sorted.length)} stem(s) this resolution covers: ` +
`${sorted.map((s) => quoteValue(s)).join(", ")}. An override this resolver drops is a tier the ` +
```

Four call positions: `quoteValue(stem)` and `quoteValue(alias)` in the alias sentence;
`quoteValue(stem)` and `sorted.map((s) => quoteValue(s))` in the unknown-stem sentence. The only
remaining interpolations in either sentence are `MODEL_ALIASES` and `sorted.length` — a module
constant and a number, neither of them a caller value. `String(sorted.length)` is a number rendered
through `String`, which is total over numbers.

The word `the` moved across a concatenation boundary in the second template (from the end of line 1
to the start of line 2) purely to keep the line under the file's width. The concatenated result is
byte-identical either way, and the diff below proves it rather than the sentence above asserting it.

## Byte-identity of every legal key's refusal wording

Both refusals driven with every covered stem, with uncovered STRING stems, and with every non-alias
value, across the pre-change build and the post-change build — 127 renderings each, compared with
`diff` rather than by reading:

```
$ node wording.mjs builds/HEADWT > wording.pre.txt
$ node wording.mjs builds/POST   > wording.post.txt
$ wc -l wording.pre.txt wording.post.txt
     254 wording.pre.txt
     254 wording.post.txt
$ diff wording.pre.txt wording.post.txt
$ echo $?
0
```

Empty. No legal refusal wording moved.

## The KEY-axis and cross-product transcripts

The 13-shape corpus driven at the KEY position with a legal alias at the VALUE position, so every
iteration reaches the SECOND refusal — the one plan 29.1-21 created:

| shape | pre-fix committed `.js` (`a58036b`) | post-fix committed `.js` (`9ae707e`) |
|---|---|---|
| undefined | RETURNED ok:false | RETURNED ok:false |
| null | RETURNED ok:false | RETURNED ok:false |
| a boolean | RETURNED ok:false | RETURNED ok:false |
| a number | RETURNED ok:false | RETURNED ok:false |
| a BigInt | RETURNED ok:false | RETURNED ok:false |
| a string | RETURNED ok:false | RETURNED ok:false |
| **a symbol** | **THREW `TypeError: Cannot convert a Symbol value to a string`** | RETURNED ok:false |
| a function | RETURNED ok:false | RETURNED ok:false |
| an array | RETURNED ok:false | RETURNED ok:false |
| a plain object | RETURNED ok:false | RETURNED ok:false |
| a circular object | RETURNED ok:false | RETURNED ok:false |
| an object whose getter throws | RETURNED ok:false | RETURNED ok:false |
| **an object whose string conversion throws** | **THREW `Error: to-string`** | RETURNED ok:false |
| | **2 THREW, 11 RETURNED** | **0 THREW, 13 RETURNED** |

Run as vitest cases against a mirror worktree at `a58036b` carrying only this plan's test file, three
of the four new cases are RED and the fourth (the denominator pin) is correctly green in both, since
it discriminates against a co-edit rather than against the fix:

```
× the override refusal RETURNS for every shape in the corpus at the KEY position, never throwing out of itself
    Error: the refusal path THREW for a symbol at the KEY position instead of returning: TypeError: Cannot convert a Symbol value to a string
× the override refusal RETURNS for every KEY-by-VALUE cell in the corpus, and the cell count is derived from both axes
    Error: the refusal path THREW for key=a symbol / value=undefined instead of returning: TypeError: Cannot convert a Symbol value to a string
× a Symbol key is REFUSED and NAMED rather than crashing the sentence that rejects it
  Tests  3 failed | 114 passed (117)
```

**Cross product:** corpus size **13**, so **13 × 13 = 169 cells**, derived in the test rather than
typed. Every cell returns a refusal at HEAD. The in-loop counter is asserted against the derived
product:

```ts
expect(
  cells,
  `the cross product must exercise every cell: ${String(SHAPE_CORPUS.length)} key shapes × ` +
    `${String(SHAPE_CORPUS.length)} value shapes = ${String(expectedCells)}`,
).toBe(expectedCells);
```

Asserting the product alone would pass over a loop that ran short — an early `continue`, a factory
returning nothing, a nested loop rewritten to iterate one axis. The number of cells this run actually
executed is counted where they execute.

## The thirteenth corpus entry — the finding of task 2

The corpus needed a new entry before the KEY-axis case could see both reproduced regressions, and the
reason is a class this phase has not named before. **The two axes fail under different operators**,
measured in both directions before the entry was written:

| shape | `JSON.stringify` (renders a rejected VALUE) | template-literal conversion (used to render a rejected KEY) |
|---|---|---|
| an object whose getter throws | **THREW** `getter` | ok: `[object Object]` |
| an object whose string conversion throws | ok: `{}` | **THREW** `to-string` |

Exactly complementary, and the corpus carried only the first — because every shape in it had been
chosen against `JSON.stringify`. Driven at the KEY position against the pre-fix build, the 12-shape
corpus therefore caught **one** of the two regressions the round-4 verifier reproduced and would have
reported the other as safe. A corpus derived for one operator is not a corpus for a claim about every
input. This is recorded in the corpus docstring, in the amended WINDOWS row 88, and here.

## Both denominator pins, and their discrimination

```ts
expect(
  TYPEOF_RESULTS.length,
  "the `typeof` codomain is EIGHT results, fixed by ECMA-262's `typeof` Operator Results " +
    "table — if this number moved, the local list stopped being a copy of the language table " +
    "the case above claims it is",
).toBe(8);
```

```ts
expect(
  SHAPE_CORPUS.length,
  "the corpus is EIGHT typeof-cover shapes plus FIVE object sub-shapes typeof cannot " +
    "distinguish; a change here is a change to the denominator every totality claim in this " +
    "describe block rests on, and must be argued rather than re-typed",
).toBe(13);
```

The comment above them cites the table the first is copied from: *ECMA-262, the `typeof` Operator
Results table (13.5.3 / Table 41)*. Three mutations run in a scratch worktree at `9ae707e`, each
recorded with the neighbouring assertion's verdict:

| co-edit | pre-existing set-equality case | new two-sided pins |
|---|---|---|
| **A.** delete `["a BigInt", …]` **together with** `"bigint"` | **PASSED** (1 passed) | **FAILED** — `expected 7 to be 8` |
| **B.** delete `"bigint"` alone | FAILED — `expected [ 'bigint', 'boolean', …(6) ] to deeply equal [ 'boolean', 'function', …(5) ]` | **FAILED** — `expected 7 to be 8` |
| **C.** delete `["a circular object", circular]` alone (typeof cover unchanged) | **PASSED** (1 passed) | **FAILED** — `expected 12 to be 13` |

**Row A is WR-02 exactly.** The case that argues its denominator "is not this file's to choose" stays
perfectly green over a corpus that no longer covers the language, because the co-edit kept the two
sets equal. Row C proves the corpus-size pin discriminates independently of the typeof pin. The tree
was restored after each mutation and the mirror re-run green (117 passed) before removal.

## The two serialiser-call counts, and the compiled-side discrimination

Measured **before** widening the scan, not assumed equal:

| file | comment-filtered `JSON.stringify(` sites | guarded-`try` sites | composed `String(JSON.stringify(` |
|---|---|---|---|
| `scripts/model-tiers.ts` | **2** | 1 | absent |
| `scripts/model-tiers.js` | **2** | 1 | absent |

Both call sites are the same two in each file: `` `${RESOLVED_ASSIGNMENT_PREFIX}${JSON.stringify(payload)}` `` and
`rendered = JSON.stringify(value);`. They agree — the expected result, and not a proven one until it
was read.

Compiled-side discrimination, proven in a scratch worktree by planting a second spelling in the
committed `.js` **only**, with the source untouched:

```
$ git diff --name-only
scripts/model-tiers.js
$ npx vitest run scripts/model-tiers.test.ts -t "ONE spelling"
AssertionError: model-tiers.js: exactly two — `quoteValue`'s guarded call, the one authority, and
`resolvedAssignmentLine`'s payload emitter, … the comment strip drops ONLY a line whose first
non-space byte opens a comment; a trailing comment on a code line and a block comment sharing a
line with code are BOTH scanned as code, so this count can be too HIGH and never too low:
expected 3 to be 2
  Tests  1 failed | 116 skipped (117)
```

**And the narrow case passes the identical plant.** With the same planted `.js` in place, the
pre-29.1-23 case restored from `5ec040d`:

```
$ git show 5ec040d:scripts/model-tiers.test.ts > scripts/model-tiers.test.ts
$ npx vitest run scripts/model-tiers.test.ts -t "ONE spelling"
  Tests  1 passed | 116 skipped (117)
```

That is WR-04's hole shown rather than argued. The comment strip's limits are now stated in the
assertion message itself: the strip is one-directional and can produce a false RED, never a false
green from executable code, because the only text it removes is text on a line that is entirely a
comment.

## The overrides docstring

Added, not rewritten — this project annotates a claim rather than replacing it:

> **AN ENTRY WHOSE KEY NAMES A STEM OUTSIDE THE RESOLVED SET IS REFUSED BY NAME (plan 29.1-21,
> reversing D-06's skip).** This paragraph is added rather than replacing the ones above it, because
> the contract those paragraphs describe is still the contract for the MAP and only the per-ENTRY
> disposition moved. […] The behaviour is pinned by model-tiers.test.ts, "resolveModels REFUSES an
> override naming a stem this resolution does not cover", with its green control beside it; the stem
> is rendered through the same quoting authority as the alias, so a key of any shape is described
> rather than thrown on.

**The RECORD half of the D-06 reversal — whether it needs a numbered decision and a changelog entry —
is ruled on in plan 29.1-25 and was deliberately NOT decided here.**

## Pinned baselines

| | Test Files | Tests |
|---|---|---|
| `scripts/model-tiers.test.ts` before | 1 passed (1) | **113 passed** |
| `scripts/model-tiers.test.ts` after | 1 passed (1) | **117 passed** |
| full non-e2e suite before | 1 failed \| 54 passed (55) | 1 failed \| **2388 passed** \| 2 skipped (2391) |
| full non-e2e suite after | 1 failed \| 54 passed (55) | 1 failed \| **2392 passed** \| 2 skipped (2395) |

Delta is exactly **+4**, the four cases task 2 adds. **Task 3 adds zero net cases**: it renames and
widens the existing one-spelling case rather than adding a second scan, because two scans over one
predicate would be two authorities — the thing this module's header forbids.

The single failing file is `scripts/frontmatter.test.ts`, identical before and after. It is the
**pre-existing** order-dependent false refusal recorded by plan 29.1-22 as WINDOWS row 91 and
`deferred-items.md` D-29.1-22-01, reproduced at pristine HEAD. It is a false red and never a bypass,
and nothing in this plan touches `scripts/frontmatter.*`.

## Closing gate set, final tree

| gate | result |
|---|---|
| `npm run typecheck` | exit 0 |
| `npm run build` then `git diff --exit-code --name-only -- '*.js'` | exit 0, empty |
| `npm run freshness` | exit 0 |
| `node scripts/check-foundation-guards.js` | exit 0 |
| `node scripts/check-kit-refs.js` | exit 0 |
| `npx vitest run --exclude '**/scripts/e2e/**'` | 2392 passed, 1 pre-existing failure, 2 skipped |
| `git diff --name-only -- .claude/agents/ .claude/skills/` | empty |
| `git diff --name-only -- scripts/measure-cost.ts scripts/measure-cost.js package.json package-lock.json` | empty |
| `git diff --name-only -- docs/audit/28-claim-registry.md` | empty |
| `git diff --exit-code -- scripts/ agent-factory/` after every scratch mutation | clean |

`npm test` was never run. Every regression run used
`npx vitest run --exclude '**/scripts/e2e/**'`. The whole plan's diff is five files:
`scripts/model-tiers.{ts,js,test.ts}`, `.planning/WINDOWS.md`, and the phase `deferred-items.md`.
Only **three code lines** changed in `scripts/model-tiers.ts` — the two templates — with everything
else being comment and docstring. The alias vocabulary, the preset name set, the TIERED table and
the two configuration locations are byte-unchanged.

## Decisions Made

- **Route the stem, do not widen the raw interpolation.** The structural fix the round-4 verifier
  prescribed. Because the authority renders a string through the serialiser, every legal key's
  wording stays byte-identical and only the shapes that misbehaved change.
- **Add a thirteenth corpus entry rather than declaring the corpus adequate.** The measurement that
  forced it is in the table above; without it the KEY-axis case sees one of two regressions.
- **Rename and widen the one-spelling case rather than adding a second.** Two scans over one
  predicate are two authorities.
- **Do not fix the four residual throw classes.** Ruled under D-59 and argued in the deviation below.
- **The D-06 reversal's record half stays with plan 29.1-25.**

## Deviations from Plan

### 1. [Rule 2 - Missing critical] A thirteenth corpus entry was required before the KEY-axis case could see both reproduced regressions

- **Found during:** Task 2, running the existing 12-shape corpus at the KEY position against the
  pre-fix build.
- **Issue:** The plan's acceptance criterion requires the KEY-axis case to be recorded THROWING "for
  at least the Symbol and throwing-conversion shapes". The corpus carried no throwing-conversion
  shape at all. Measured: 1 of 12 shapes threw. The corpus's "object whose getter throws" throws
  under `JSON.stringify` and renders cleanly under template-literal conversion, which is the operator
  the KEY position used.
- **Fix:** Added `["an object whose string conversion throws", () => ({ toString(): never { … } })]`.
  `typeof` is `"object"`, already in the cover, so the set-equality assertion is unaffected;
  `SHAPE_CORPUS.length` moves 12 → 13 and is pinned there.
- **Verification:** 2 of 13 shapes now throw at the pre-fix build, 0 of 13 at HEAD.
- **Committed in:** `5ec040d`.

### 2. [Rule 1 - Measurement contradicts the plan] The plan's truth 2 overstates what stays unmoved

- **Found during:** Task 1, comparing the pre-fix and post-fix KEY-position renderings.
- **Issue:** The plan states that "only the shapes that previously threw change, and they change from
  a throw to a description." Measured, that is false. Only **2 of 13** shapes threw; the other 11
  RETURNED, and their key rendering has changed, because a key used to be rendered by
  template-literal conversion inside hand-written quotation marks and is now rendered by the
  serialiser. Every one of the 11 is an ILLEGAL key, refused in both builds.
- **The changed renderings, all illegal keys:** a number key `NaN` moved from `"NaN"` to `null`; a
  BigInt key from `"1"` to `<a bigint that cannot be serialised>`; an array key from `"opus"` to
  `["opus"]`; a plain object from `"[object Object]"` to its serialised form; a function key from its
  source text to a bare `undefined`.
- **Assessment:** FAIL-CLOSED in every row, and discrimination **improved** for the common shapes —
  a number key `5` and the string key `"5"` used to render identically and no longer do. Two rows
  read worse for a human (the function key and the `NaN` key). The half the plan cares about is
  intact and proven: every LEGAL (string) key is byte-identical, 127 renderings, empty diff.
- **Fix:** None applied — the change is correct. The plan's sentence is corrected here and in
  WINDOWS row 93 rather than the code being bent to match it.
- **Committed in:** `9ae707e` (code), `5ec040d` (record).

### 3. [Rule 4 - Recorded, not fixed] `resolveModels` still throws on four input classes, so this plan's first stated truth is NOT achieved

- **Found during:** Closing adversarial self-verification, run because a green suite is not proof for
  a safety surface. Found by no review and required by no acceptance criterion.
- **Issue, measured across four committed builds from one script:**

  | Input | `e9907f5` | `ad033f0` | `a58036b` | HEAD |
  |---|---|---|---|---|
  | a revoked Proxy at the VALUE position | THREW | THREW | THREW | **THREW** |
  | a revoked Proxy at the KEY position | ok:true | THREW | THREW | **THREW** |
  | a Map subclass whose iterator yields a non-array entry | THREW | THREW | THREW | **THREW** |
  | a Map subclass whose iterator throws | THREW | THREW | THREW | **THREW** |

- **Two root causes.** (1) `quoteValue` is not total although its docstring says "IT IS TOTAL": the
  `describeShape(value)` call sits INSIDE the `catch`, outside the `try`, and `Array.isArray` throws
  `TypeError: Cannot perform 'IsArray' on a proxy that has been revoked`. This affects BOTH axes and
  predates the whole round-3/round-4 sequence. (2) The override loop destructures each entry before
  any floor runs, and Floor 0b establishes `instanceof Map` and nothing more, so a Map SUBCLASS
  overriding `Symbol.iterator` escapes before a refusal exists to be returned.
- **Why not fixed:** Only root cause 1 is a small edit; root cause 2 needs defensive iteration, a
  structural change to the loop this plan was closing a wording defect in. Closing one of two would
  leave the totality **looking** closed while remaining false — the "one more spelling"
  incrementalism this project's memory names as its repeated failure across twelve rounds on
  `frontmatter.ts`. The established remedy here is **D-59**: hold an open-set totality as CONTENT
  with a disclosed backstop, not as a mechanism that does not exist.
- **Direction:** FAIL-CLOSED in every row. A throw is loud and no tier is silently applied. It is
  nonetheless the exact class this phase exists to close, because the module header promises a
  returned result so a degrading consumer need not write a catch.
- **Not pinned by a case, deliberately:** a case asserting these four shapes throw would freeze a
  defect as expected behaviour.
- **Committed in:** `fd211f3` (WINDOWS row 95, `deferred-items.md` D-29.1-23-01).

---

**Total deviations:** 3 (1 missing-critical auto-fixed, 1 measurement correction to the plan's own
text, 1 architectural finding recorded and not fixed).
**Impact on plan:** Deviations 1 and 2 strengthen the plan's own claims by measuring what it asserted.
Deviation 3 means **`must_haves.truths[0]` is not satisfied as written** and the phase verifier should
treat it as open. No scope creep: the whole diff is five files and three code lines.

## Ledger rows added and amended

- **Row 88 (amended, both surfaces).** Re-stated against the widened corpus:
  > AMENDED BY PLAN 29.1-23, restated against the corpus as WIDENED rather than the narrow one this
  > row described. The corpus grew from 12 shapes to 13 and is now driven over the KEY position, the
  > VALUE position and their 169-cell cross product… The thirteenth entry is an object whose string
  > conversion throws, and it was added because the two axes fail under DIFFERENT operators…
  > That is this row own fail-open shape found one level up: a derived denominator can cover every
  > typeof result, but no denominator here covers every way a RENDERING OPERATOR can fail, and there
  > are now two such operators in scope.
- **Row 92 (new, `deviation`, `scripts/model-tiers.ts`).** The KEY-rendering residual: a
  non-representable key renders the way the one authority has always rendered a non-representable
  value (a Symbol and a function both as a bare `undefined`; a throwing-`toString` object and a plain
  object both as `{}`), so the refusal names the stem POSITION but not the key. Direction:
  FAIL-CLOSED, and strictly better than the THROW it replaced. Mitigating fact asserted rather than
  claimed: a legitimate STRING key spelled `undefined` renders WITH its quotation marks.
- **Row 93 (new, `deviation`, `scripts/model-tiers.ts`).** The correction to the plan's own truth 2,
  with the full before → after rendering map for the 11 non-throwing illegal key shapes.
- **Row 94 (new, `deviation`, `scripts/model-tiers.test.ts`).** The one-spelling scan's comment-strip
  bound, now stated in the assertion message: one-directional, can be too HIGH and never too low.
- **Row 95 (new, `unmet-truth`, `scripts/model-tiers.ts`).** The four residual throw classes and both
  root causes — deviation 3 above.

Ledger totals moved from `open_count: 88 / total_count: 91` to `open_count: 92 / total_count: 95`.

## Issues Encountered

- **The plan's I3 input has a different provenance than the plan assumed.** Resolved by measuring
  rather than assuming, and by fixing both sites instead of one. See the reproduction table.
- **No `windows amend` verb exists** in the GSD tooling, so row 88 was amended by an in-place edit
  applied to BOTH surfaces of `.planning/WINDOWS.md` — the markdown table row and the fenced JSON
  block — with an assertion that exactly 2 occurrences were replaced, and the JSON block re-parsed
  afterwards to confirm 93 well-formed rows.
- **Two scratch worktrees** (`a58036b` and `9ae707e` mirrors) were used for every red transcript, so
  the live working tree was never mutated for a discrimination proof. Both were removed;
  `git worktree list` shows only the repository.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **BLOCKER 2 is closed on the axis it was reported on**, and WR-02 and WR-04 are closed with their
  discrimination proven rather than asserted.
- **One item is deliberately open for round 5:** the totality claim itself. Deviation 3 gives the
  verifier four reproducible counterexamples and both root causes. The decision that should precede
  any further attempt is whether this module holds its totality as a mechanism at all, or as content
  under D-59 — the same ruling Phase 29 reached after eight rounds.
- The pre-existing `scripts/frontmatter.ts` false refusal (WINDOWS row 91) is still unowned and still
  reds one case in the full suite.

## Self-Check: PASSED

All five modified files exist on disk. All five commits resolve in `git log --oneline --all`:
`9ae707e`, `5ec040d`, `dfc6ab8`, `fd211f3`, `d11ff2d`. Every task's acceptance criteria were re-run
on the final tree and every plan-level verification command recorded above exits 0, with the single
suite failure established as pre-existing at pristine HEAD.

One additional check, because this plan writes three new tracked markdown files into a corpus another
gate scans: the D-49 false-red control was re-run and reports **exactly one** refusal —
`29.1-VERIFICATION-round4.md`, the pre-existing WINDOWS row 91. This plan's SUMMARY,
`deferred-items.md` additions and `WINDOWS.md` amendments add **zero** new refusals to that corpus.

**Caveat carried into the verdict, not buried under it:** `must_haves.truths[0]` is NOT satisfied as
written. See deviation 3.

---
*Phase: 29.1-per-role-model-assignment*
*Completed: 2026-08-21*

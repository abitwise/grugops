---
phase: 29.1-per-role-model-assignment
plan: 24
type: execute
wave: 3
depends_on: ["29.1-23"]
gap_closure: true
files_modified:
  - scripts/ci-workflow.testkit.ts
  - scripts/ci-workflow.testkit.js
  - scripts/check-foundation-guards.test.ts
  - scripts/skill-twins-freshness.test.ts
  - .planning/WINDOWS.md
autonomous: true
requirements: [MODEL-01, MODEL-05]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "R3-IN-03 is closed STRUCTURALLY rather than by a wider text scan: there is ONE implementation of `where is the ubuntu gate block and what does it run`, in `scripts/ci-workflow.testkit.ts`, and both readers consume it. `This class reads one region` becomes true by construction, so the class check no longer has to certify it by comparing source spellings a member's own declaration can supply."
    - "The exact divergence R3-IN-03 named — reading from the locator index rather than from the end of the step name — REDS by name. `(r-base-discriminating)` computes the authority's region and an inline control region built from the bare locator index with the same right bound, asserts the two are not equal, asserts the authority's region does not carry the step name and the control's does, and asserts the two command lists differ by exactly one entry."
    - "Membership is derived from the IMPORT rather than from a phrase a comment can also contain. The member set is the `scripts/*.test.ts` files importing the authority, pinned two-sided at the measured count; and no `.test.ts` outside that set locates the block by its own step-name literal, so a third reader arriving with its own grammar is a red rather than an invisible non-member."
    - "The predicate can fail for BOTH of today's members, which is exactly what round 4 measured that it could not. Proven by applying the divergence to the authority on a hermetic mirror and comparing the failure set against a baseline pinned in the same session — not against a remembered number."
    - "The recorded `not sharing a helper` disposition is REVERSED, and the reversal travels with the measurement that forced it: the text-comparison class check that disposition justified was measured structurally incapable of failing for either member, and its cheap remedy was measured to red the clean tree. Both members' comments are corrected rather than left asserting a disposition the tree no longer has."
    - "`.planning/WINDOWS.md` row 80 states the MEASURED fact. Its previous mitigating clause — that today's two members are bounded by their own synthetic-input bound proofs — was measurably false and is replaced rather than softened, because a ledger row carrying a false mitigation is more dangerous than no row."
    - "The new module's committed `.js` twin is built and committed in this plan, and `npm run freshness` exits 0 on the final tree — the tooling layer's committed output cannot drift from its source."
    - statement: "A member that imports the authority and then computes something else entirely — a second private slice under another variable, in a branch nothing reaches — still satisfies the import-derived membership. That direction is narrower than the one row 80 disclosed, because there is no longer a second implementation for it to be built from, but it is not zero and it is disclosed rather than argued away."
      verification: backstop
  artifacts:
    - scripts/ci-workflow.testkit.ts
    - scripts/ci-workflow.testkit.js
    - scripts/check-foundation-guards.test.ts
    - scripts/skill-twins-freshness.test.ts
    - .planning/WINDOWS.md
  key_links:
    - "`scripts/ci-workflow.testkit.ts` → both `.test.ts` readers. One region reader, one right bound, one slice base, one workflow path spelling — consumed by every test that asks where the ubuntu gate block is."
    - "the authority's slice base → `(r-base-discriminating)`'s inline control → the failure a base shift produces. The case compares COMPUTED regions, so a change to what the reader computes moves the assertion rather than the source text it happens to contain."
    - "the import specifier → the derived member set → the two-sided pin → the negative over non-members. Membership is a mechanism, not a phrase, and the negative is what keeps a third grammar from arriving unseen."
  prohibitions:
    - "`.github/workflows/ci.yml` is never written to, not even temporarily. Every bound and base proof is taken over a string assembled in memory, exactly as the existing synthetic-input cases do."
    - "The two members' EXISTING wiring assertions keep their meaning. This plan moves where the region reader lives; it does not relax, delete or widen any assertion about what the ubuntu block must contain or in what order."
    - "No production module imports the new authority. It is consumed by tests only, and the plan asserts that rather than asserting it in prose — a module that a shipped script imports is a shipped surface and this is not one."
    - "No adapter under `.claude/agents/` or `.claude/skills/` changes. `git diff` over both directories is empty at plan close."
    - "`npm test` is never run. The regression command is `npx vitest run --exclude '**/scripts/e2e/**'`."
    - "No caveman voice in any comment, assertion message or ledger row."
    - "The step-name literal is spelled in ONE place after this plan. No comment, assertion message or fixture in either member re-types it; every reference is to the exported constant."
---

<objective>
Close BLOCKER 3 of round-4 verification: the class check that cannot fail for either of its two
members.

**The constant that states the requirement is the text the requirement searches for.** Requirement 2
of `(r-class-prefix)` tests each member's source for the canonical slice-base spelling, and that exact
string is declared as a `const` at line 10936 of the very file being tested. Round 4 applied the exact
divergence R3-IN-03 named and ran the file: the failure set came back byte-for-byte identical to the
unmutated baseline. Requirement 1 is in the same position for BOTH members, satisfied by each one's
own step-marker declaration.

**And the cheap remedy the verifier suggested is falsified by measurement.** Stripping `const NAME =`
declaration lines before the containment tests was checked against this tree during planning: after
stripping, the right-bound marker's canonical spelling occurs ZERO times in BOTH members, because the
only place either file spells those bytes IS the declaration. Requirement 1 would red on a correct
tree. Round 4's `missing` list is input, not gospel, and this is the first thing this plan refuses to
take on faith.

**So the fix is the one round 4 called BETTER, and it is this project's recorded terminal lesson.**
Delete the second grammar rather than widen the first. There is one implementation of "where is the
ubuntu gate block and what does it run", it lives in a test-only module, and both readers consume it.
`This class reads one region` stops being something a scan has to certify and becomes something the
construction guarantees; what the class check then asserts is the construction — membership derived
from the import, pinned two-sided, with a negative closing the class against a third reader that
brings its own grammar. The region's correctness is proven by BEHAVIOUR, in a case that computes the
authority's region beside a control region built from the divergent base and asserts they differ by
exactly the step name.

**This reverses a deliberately recorded disposition, and the reversal travels with its reason.** Both
files carry a paragraph arguing that sharing the helper would mean either two authorities or a shipped
surface added for a test, and that the class is closed by the text scan instead. The first horn is
what the tree already had and the second is answered by a module no production script imports; the
text scan the argument bought was measured incapable of failing. Both paragraphs are corrected, and
the reversal is recorded in the ledger with the measurement that forced it.

Purpose: the predicate that certifies two readers read one region can fail when they do not.
Output: `scripts/ci-workflow.testkit.ts` with its committed `.js`, both members consuming it, a
behaviour-based base proof, an import-derived class check, and an amended row 80 that states what was
measured.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29.1-per-role-model-assignment/29.1-CONTEXT.md
@.planning/phases/29.1-per-role-model-assignment/29.1-PATTERNS.md
@.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round4.md
@.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW-round4.md
@.planning/phases/29.1-per-role-model-assignment/29.1-19-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol or path | Kind | Purpose |
|----------------|------|---------|
| `scripts/ci-workflow.testkit.ts` | new test-only module | the ONE authority over the ubuntu gate block: the step name, the derived step-name prefix, the step-boundary marker, the workflow path, the region reader and its command-line reader |
| `scripts/ci-workflow.testkit.js` | new committed build output | the compiled twin, built and committed in this plan so the freshness gate stays green |
| `UBUNTU_BLOCK_STEP_NAME` / `UBUNTU_BLOCK_STEP_PREFIX` / `STEP_MARKER` | relocated exported constants | spelled once, in the authority; every reference elsewhere is an import |
| `ubuntuBlock` / `ubuntuBlockCommands` / `ciWorkflow` | relocated exported functions | one region reader, one command reader, one workflow path spelling |
| `withAppendedStep` | relocated exported helper, parameterised | takes the synthetic step's name and command, so both members' distinct synthetic inputs come from one builder |
| `(r-class-authority)` | replacement case in `scripts/check-foundation-guards.test.ts` | membership derived from the import, pinned two-sided, with a negative closing the class against a reader that brings its own locator |
| `(r-base-discriminating)` | new case in `scripts/check-foundation-guards.test.ts` | the slice base proven by computed regions rather than by source text |
| `.planning/WINDOWS.md` row 80 | amended ledger row | states the measured fact and drops the mitigating clause that was measured false |
| `.planning/WINDOWS.md` row for the reversed disposition | new ledger row | the not-sharing-a-helper disposition reversed, with the measurement that forced it and the residual direction |

New test case names:
`(r-base-discriminating) the region begins after the step name, proven by two computed regions rather than by source text`,
`(r-class-authority) every reader of the ubuntu gate block imports the one authority, and nothing outside that set locates the block`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a predicate → the subject it interrogates | When the subject supplies the text the predicate searches for, the predicate cannot fail and the green result carries no information |
| a disclosed residual → the reader who stops looking | A ledger row naming a mitigation that does not exist is worse than no row |
| a class check → a future third member | A member set that cannot see a reader bringing its own grammar admits it silently |
| a test-only module → the shipped surface | A helper promoted into a production module is a surface added for a test; a module no production script imports is not |

## STRIDE Threat Register

ASVS level 1, block on `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-98 | Repudiation | `(r-class-prefix)` — a requirement satisfied by the constant that states it | high | mitigate | One implementation of the region reader; the class check asserts the construction, and the base is proven by computed regions with the exact divergence measured RED against a session-pinned baseline. |
| T-29.1-99 | Repudiation | `.planning/WINDOWS.md` row 80's mitigating clause, measured false | high | mitigate | The row is rewritten to the measured fact; the replacement mitigation is the construction plus the named behaviour case, and the residual that remains is stated with its direction. |
| T-29.1-100 | Tampering | a third reader of the ubuntu block arriving with its own locator and its own base | medium | mitigate | Membership derived from the import and pinned two-sided, with a negative over every non-member `.test.ts`. |
| T-29.1-101 | Tampering | a new committed `.js` twin drifting from its `.ts` | high | mitigate | The twin is built and committed in the same commit; `npm run freshness` and `npm run build` with an empty tracked-output diff are in the task verify. |
| T-29.1-05 | Tampering | committed `scripts/*.js` diverging from its `.ts` | high | mitigate | `npm run build` plus `git diff --exit-code --name-only -- '*.js'` in every task verify. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | accept | Zero packages installed by this plan; `package.json` asserted byte-unchanged in the plan verification. `29.1-RESEARCH.md` records no new dependency for this phase. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One implementation of the region, consumed by both readers</name>
  <files>scripts/ci-workflow.testkit.ts, scripts/ci-workflow.testkit.js, scripts/check-foundation-guards.test.ts, scripts/skill-twins-freshness.test.ts</files>
  <behavior>
    - The workflow text, the step name, the derived prefix, the step-boundary marker, the region and the region's command lines are each obtained from exactly one place.
    - Both members' existing assertions about the ubuntu block's contents and ordering produce the same results as before the move.
    - The synthetic following-step builder accepts the step name and command as arguments, so both members keep their distinct synthetic inputs.
    - No production script imports the new module.
  </behavior>
  <read_first>
    - `scripts/check-foundation-guards.test.ts` lines 10895-11010 — the whole preamble of the CI wiring describe block: the step name, the step marker, the derived prefix with its full rationale, the slice-base constant with its measurement, the reader count, `ciWorkflow`, the synthetic step constants, `withAppendedStep`, `ubuntuBlock` with its complete docstring, and `ubuntuBlockCommands`. Every symbol this task relocates is in this range.
    - `scripts/check-foundation-guards.test.ts` lines 11010-11200 — every case in that describe block that consumes those symbols, so the move is proven not to change what any of them asserts.
    - `scripts/check-foundation-guards.test.ts` lines 11078-11145 — `(r-bound-synthetic)` in full, including its negative control's inline slice and its effect assertion. Round 4 measured that this case cannot see a base shift; it is re-pointed at the authority here and its own limitation is addressed by the new case in task 2.
    - `scripts/skill-twins-freshness.test.ts` lines 260-330 — the mirrored preamble: the disposition paragraph, the two constants, `ciWorkflow`, `ubuntuBlock`, `ubuntuBlockCommands`, the synthetic constants and `withAppendedStep`.
    - `scripts/skill-twins-freshness.test.ts` — `Case 8` and `Case 8b` in full, so the move is proven not to change what either asserts.
    - `scripts/kit-model.ts` header — this repository's worked example of a module that is one authority for a derived set, for the voice the new module's header is written in.
    - `tsconfig.json` and `tsconfig.tests.json` — the include and exclude lists, so what the compiler will emit for a new `scripts/*.ts` that is not a `.test.ts` is READ rather than assumed.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round4.md` — the third `gaps` entry in full: its three artifacts, its harness-premise paragraph and its three-entry `missing` list.
  </read_first>
  <action>
Pin the baseline FIRST, in this session, and record it. Run each of the two member files alone and
record the pass and fail counts; run the full non-e2e suite and record its file, pass and skip counts.
Every claim later in this plan compares against these pinned numbers rather than against a number read
out of a report. Round 4's own note is the standard: this phase has produced a false verification
result in six instances by not distinguishing an unchanged failure set from a catch.

Reproduce the blocker before changing anything. On a hermetic `git archive HEAD` mirror, apply the
exact divergence — change the region reader in `scripts/check-foundation-guards.test.ts` to slice from
the locator index rather than from the end of the step name — assert the mutation landed by printing
the byte delta and re-reading the mutated line, independently measure that the two bases produce
regions of different length on the committed workflow, then run the file and record the failure set.
Expected: identical to the pinned baseline. That transcript is the pre-fix evidence.

Create `scripts/ci-workflow.testkit.ts`. It exports the step name, the step-name prefix derived from
it at its qualifier, the step-boundary marker, a workflow reader resolving the workflow path from the
module's own directory, the region reader bounded on the right, the command-line reader, and a
synthetic-following-step builder taking the step's name and command as arguments. Its header states
three things plainly: that it is the ONE authority over this region and that a second implementation
is a defect rather than a duplication; that it is consumed by tests only and imported by no production
script, which is why it is a testkit rather than a promotion into a shipped module; and that it exists
because a class check comparing source spellings was measured structurally incapable of failing for
either of its two members.

Move the region reader's existing docstring with it, unchanged in substance. The paragraph explaining
that the block is the last step today, that the fallback is not the guarantee, and that the property is
proven by a synthetic-input case rather than by a probe over the returned slice, is the record of two
rounds of work and travels intact.

Cut both members over. Each imports what it needs from the authority and deletes its local copies —
the constants, the workflow reader, the region reader, the command reader and the synthetic builder.
Neither member re-types the step-name literal anywhere, in code or in a comment; every reference is to
the imported constant. Keep each member's own synthetic step name and command as local constants, since
those are inputs rather than grammar, and pass them to the shared builder.

Correct both disposition paragraphs. Each currently argues that the helper is deliberately not shared
and that the class is closed by a scan over canonical source spellings. Rewrite each to state what is
now true and why it changed: the scan was measured incapable of failing for either member, the second
horn of the original dilemma is answered by a module no production script imports, and the class is now
closed by construction. Describe the superseded reasoning rather than deleting it — this project
annotates and does not rewrite.

Assert the no-production-consumer property rather than claiming it. Add an assertion, in the class case
task 2 rewrites or beside it, that no file outside the `.test.ts` set imports the authority, derived by
scanning the source tree rather than by a hand-kept list.

Build and commit the twin. Run `npm run build`, confirm the compiler emits `scripts/ci-workflow.testkit.js`,
and commit it in the same commit as its source. Then run `npm run freshness` and record its verdict:
a new committed output that the freshness gate has not seen is exactly the state that gate exists to
refuse.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-foundation-guards.test.ts scripts/skill-twins-freshness.test.ts && npm run typecheck && npm run build && git diff --exit-code --name-only -- '*.js' && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - Baselines pinned and quoted in the SUMMARY: per-file pass and fail counts for both members, and the full non-e2e suite's file, pass and skip counts, all measured in this session before any edit.
    - The pre-fix reproduction transcript is recorded: the byte delta, the re-read mutated line, the two independently measured region lengths, and the resulting failure set. Expected: the failure set equals the pinned baseline, demonstrating that nothing in the file sees the divergence.
    - `scripts/ci-workflow.testkit.ts` exists and exports the seven named symbols. Quote its export list.
    - Both members import from the authority and carry no local copy: `grep -c 'function ubuntuBlock' scripts/check-foundation-guards.test.ts` is 0 and the same over `scripts/skill-twins-freshness.test.ts` is 0.
    - The step-name literal is spelled exactly once in the tree's test surface: `grep -rl 'Freshness gates + repo gates' scripts/*.ts` returns exactly `scripts/ci-workflow.testkit.ts`. Quote the command and its output.
    - Both members' pass counts after the move equal their pinned pre-move counts. Any difference is a deviation and must be explained with a measurement, not absorbed.
    - Both disposition paragraphs are rewritten and each names the measurement that forced the reversal. Quote both.
    - The no-production-consumer property is asserted by a derived scan, not by prose. Quote the assertion.
    - `npm run build` emits `scripts/ci-workflow.testkit.js`, it is committed in the same commit as the `.ts`, and `git diff --exit-code --name-only -- '*.js'` is empty afterwards. `npm run freshness` exits 0.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>One module answers where the ubuntu gate block is and what it runs; both readers consume it; the members' assertions are unchanged in meaning and unchanged in count.</done>
  <reversibility rating="costly">A new module plus a committed build twin, with two consumers cut over and a recorded disposition reversed; reverting means restoring two local implementations and the ledger row that justified them.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The base proven by computed regions, and membership derived from the import</name>
  <files>scripts/check-foundation-guards.test.ts, scripts/skill-twins-freshness.test.ts</files>
  <behavior>
    - The authority's region does not carry the step name; a control region built from the bare locator index does.
    - The two regions are not equal, and their command lists differ by exactly one entry.
    - The member set is the test files importing the authority, and its size is pinned two-sided.
    - No test file outside the member set locates the ubuntu block by its own copy of the step name.
    - The synthetic right-bound proof still fails when the right bound is removed.
  </behavior>
  <read_first>
    - `scripts/ci-workflow.testkit.ts` as written by task 1, in full, so the cases assert against the mechanism that exists rather than the one this plan described.
    - `scripts/check-foundation-guards.test.ts` lines 11202-11330 — the whole `(r-class-prefix)` case: its disclosure paragraph, its two premises about the derived prefix, its uniqueness premise over the workflow's step names, its measured-widening argument, its derived member set, its vacuity floor, both containment requirements and its two-sided pin. Every part of this case is either kept, replaced or deleted by this task, and the reason for each is recorded.
    - `scripts/check-foundation-guards.test.ts` — `(r-bound-synthetic)` as re-pointed by task 1, so the new case is written to catch what that case demonstrably cannot.
    - `scripts/skill-twins-freshness.test.ts` — `Case 8b` as re-pointed by task 1, and the cross-reference in its comment naming the class case by its old identifier, which this task renames.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round4.md` — the `check-foundation-guards.test.ts:11078-11145` artifact entry, which records exactly why the existing synthetic case cannot see a base shift: its negative control builds its own inline slice rather than calling the region reader, and its effect assertion is still satisfied by a base-shifted read.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round4.md` — the `coincidental_reliance_items` entry, which is the requirement-1 half of the same case and is closed here together with the requirement-2 half.
  </read_first>
  <action>
Write `(r-base-discriminating)` RED-FIRST. Against a mirror in which the authority's region reader
slices from the bare locator index, the case must fail; against the real authority it must pass. Take
both transcripts before believing either.

Build the case out of two computed regions, not out of source text. Read the committed workflow once
through the authority's workflow reader. Compute the authority's command list. Compute a control
command list from a region that starts at the locator index and ends at the same right bound, written
inline in the case so it is a control rather than a second authority. Then assert, in this order: both
lists are non-empty, so nothing below is vacuous; the two lists are not equal, which is the premise
that the two bases genuinely differ on this workflow; the control's first entry carries the step name;
the authority's list carries the step name nowhere; and the control's length is exactly one greater
than the authority's. Say in the comment that the last three are the same fact measured three ways
and that all three are kept, because a single assertion is one refactor away from being the only thing
standing between this class and the divergence it exists to catch.

Rename and rewrite the class case as `(r-class-authority)`. Keep what still means something: the
vacuity floor on the derived denominator, the uniqueness premise that exactly one step in the
committed workflow begins with the prefix, and the two-sided member-count pin. Replace both
containment requirements — they were the two halves round 4 measured incapable of failing — with the
construction assertions: every member imports the authority, and the member set is derived by that
import rather than by a phrase. State in the comment that the containment requirements are DELETED
rather than repaired, that the repair round 4 suggested was measured to red this tree because the only
place either member spells the right-bound marker's canonical form is the declaration that states the
requirement, and that this is why the fix is structural.

Close the class with the negative. Over every `scripts/*.test.ts` file that is NOT a member, assert
none contains the step-name prefix. Build the needle from the imported constant so the scanning file
does not carry the phrase itself and cannot exempt itself by accident. Name every offending file in
the failure message and say what the remedy is: import the authority, do not bring a second locator.

Update the cross-reference in the other member's comment, which names the class case by its old
identifier. A pointer to a case that no longer exists is the orphaned-citation class this phase spent
plan 29.1-20 closing.

Prove each surviving and new assertion discriminates, one at a time, and record a transcript for each:
remove the right bound and watch the synthetic bound case red; shift the base and watch the new base
case red; plant a third `.test.ts` importing the authority and watch the count pin red; plant a third
`.test.ts` carrying the prefix without importing and watch the negative red. Four mutations, four
transcripts, each with its byte delta recorded and the tree restored and confirmed clean afterwards.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-foundation-guards.test.ts scripts/skill-twins-freshness.test.ts && npx vitest run --exclude '**/scripts/e2e/**' && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `(r-base-discriminating)` recorded RED against the base-shifted mirror and GREEN at HEAD, both transcripts quoted, with the failure named in the RED transcript and the failure count compared against the pinned baseline. Expected RED: the pinned baseline plus one, with this case named.
    - The case's five assertions are quoted verbatim, in order, with the two computed list lengths stated. Expected on the committed workflow: the control's list is exactly one entry longer than the authority's, and that entry is the step name.
    - `(r-class-authority)` exists; the two containment requirements are gone: `grep -c 'UBUNTU_BLOCK_SLICE_BASE_SOURCE' scripts/check-foundation-guards.test.ts` is 0.
    - The measured falsification of round 4's suggested repair is recorded in the case comment and in the SUMMARY, with the numbers: after stripping the two declaration lines the right-bound marker's canonical spelling occurs zero times in both members, so the repaired requirement would red a correct tree.
    - Membership is derived from the import and the count pinned two-sided. Quote the derivation and the pin. Expected member count: the measured number of `scripts/*.test.ts` files importing the authority.
    - The negative over non-members is present and its needle is built from the imported constant rather than typed. Quote the assertion and its failure message.
    - Four discrimination transcripts recorded — right bound removed, base shifted, extra member added, non-member carrying the locator — each with a byte delta, each showing the named case failing, and each followed by `git diff --exit-code -- scripts/` clean.
    - The cross-reference in the other member's comment names the current case identifier. Quote the corrected sentence.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0 with counts recorded against the pinned baseline. `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The class check certifies a construction it can verify and a behaviour it can measure, and the divergence that slept through round 4 reds by name.</done>
  <reversibility rating="reversible">Test cases only; reverting restores the requirements measured incapable of failing.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The ledger says what was measured, and the round's gates are green on the final tree</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - `.planning/WINDOWS.md` row 80 in full — the disclosed residual, its FAIL-OPEN direction, and the mitigating clause naming the two synthetic-input bound proofs.
    - `.planning/WINDOWS.md` rows 81-88 — the recent 29.1 rows, for the voice, the direction convention and the status convention.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round4.md` — the `.planning/WINDOWS.md row 80` artifact entry and the third `missing` bullet, verbatim, so the amendment states the fact the verifier measured rather than a paraphrase of it.
    - `scripts/ci-workflow.testkit.ts`, `(r-base-discriminating)` and `(r-class-authority)` as written by tasks 1 and 2, so the replacement mitigation names mechanisms that exist.
  </read_first>
  <action>
Amend row 80 to the measured fact. It currently frames the residual as being about a hypothetical
future member and adds a mitigating clause naming two synthetic-input bound proofs. Round 4 measured
that clause false: the synthetic bound case's negative control builds its own inline slice rather than
calling the region reader, and its effect assertion is still satisfied by a base-shifted read. The
amended row states what was measured, names the construction and the behaviour case that replace the
false mitigation, and states the residual that actually remains — a member that imports the authority
and then computes something else in a branch nothing reaches — with its direction. Set the row's
status to reflect the closure. Do not soften the false clause; replace it, and say in the row that it
was measured false, because an honest ledger carrying a false mitigation is how a hole survives being
written down.

Add the row for the reversed disposition. Name both files, state that the not-sharing-a-helper
disposition is reversed, give the measurement that forced it — the text-comparison class check was
structurally incapable of failing for either member, and the suggested cheap repair was measured to
red a correct tree — and state what answers the original argument's second horn: a module no
production script imports.

Add the row for the new committed build output, if the freshness or build-parity surface gained
anything a later reader would not expect. State plainly if it did not; a ledger row that records "no
change was needed" is cheaper than a later reader rediscovering the question.

Run the full gate set on the final tree and record every result, in the order given in the plan's
verification block. Then re-run the base-shift reproduction one final time against the final tree and
record it RED with the case named — the same mutation that was invisible before this plan.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && git diff --exit-code --name-only -- '*.js' && npm run freshness && node scripts/check-foundation-guards.js && node scripts/check-kit-refs.js && npx vitest run --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - Row 80 is amended, quoted in full in the SUMMARY, and explicitly states that its previous mitigating clause was measured false. The replacement mitigation names `scripts/ci-workflow.testkit.ts` and `(r-base-discriminating)` by name.
    - The reversed-disposition row exists, names both member files, and carries the two measurements that forced the reversal.
    - The build-output question is answered in the ledger one way or the other, with the freshness verdict quoted.
    - Full gate set on the final tree, each result recorded: `npm run typecheck`, `npm run build` with an empty tracked-output diff, `npm run freshness`, `node scripts/check-foundation-guards.js`, `node scripts/check-kit-refs.js`, `npx vitest run --exclude '**/scripts/e2e/**'` — all exit 0.
    - The final base-shift reproduction is recorded RED with `(r-base-discriminating)` named and the failure count compared to the pinned baseline. The tree is restored and `git diff --exit-code -- scripts/` is clean.
    - `git diff --name-only -- .claude/agents/ .claude/skills/` is empty. `git diff --name-only -- package.json package-lock.json .github/workflows/ci.yml` is empty.
  </acceptance_criteria>
  <done>The ledger states what was measured rather than what was hoped, and the mutation that produced a baseline-identical failure set in round 4 reds by name on the final tree.</done>
  <reversibility rating="reversible">Ledger rows and gate runs; nothing executable changes.</reversibility>
</task>

</tasks>

<verification>
Run on the final tree, in this order, and record each result:

1. `npm run typecheck` — exits 0.
2. `npm run build` then `git diff --exit-code --name-only -- '*.js'` — empty.
3. `npm run freshness` — exits 0, and the new committed twin is among the outputs it verifies.
4. `node scripts/check-foundation-guards.js` — exits 0.
5. `node scripts/check-kit-refs.js` — exits 0.
6. `npx vitest run --exclude '**/scripts/e2e/**'` — exits 0, counts recorded against the pinned baseline.
7. The base-shift mutation applied to the authority on a hermetic mirror — `(r-base-discriminating)` named in the failure set, count equal to baseline plus one.
8. `git diff --name-only -- .claude/agents/ .claude/skills/` — empty.
9. `git diff --name-only -- .github/workflows/ci.yml package.json package-lock.json` — empty.
10. `git diff --exit-code -- scripts/` after every scratch mutation — clean.

`npm test` is never run.
</verification>

<success_criteria>
- One implementation of the ubuntu-block region exists and both readers consume it.
- The exact R3-IN-03 divergence reds by name against a baseline pinned in the same session.
- Membership is derived from the import, pinned two-sided, and closed by a negative over non-members.
- Round 4's suggested cheap repair is recorded as measured-false rather than silently skipped.
- Row 80 states the measured fact and names a mitigation that exists.
</success_criteria>

<output>
Create `.planning/phases/29.1-per-role-model-assignment/29.1-24-SUMMARY.md` when done.

It must carry, at minimum:
- the session-pinned baselines, per member file and for the full non-e2e suite;
- the pre-fix reproduction transcript with its byte delta and the two independently measured region lengths;
- the measurement falsifying round 4's suggested cheap repair, with its numbers;
- the four discrimination transcripts from task 2, each with a byte delta and a restored-clean confirmation;
- the export list of the new authority and the derived member set with its pinned count;
- the amended row 80 and the new rows, quoted in full;
- every deviation from this plan, with the measurement that forced it.
</output>

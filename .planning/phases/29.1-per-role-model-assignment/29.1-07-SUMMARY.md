---
phase: 29.1-per-role-model-assignment
plan: 07
subsystem: tooling
tags: [typescript, announcement-grammar, anchored-parser, one-authority, freshness-gate, gap-closure]

requires:
  - phase: 29.1-per-role-model-assignment
    plan: 01
    provides: "the generator's `model:` line resolved through scripts/model-tiers.ts rather than emitted as a literal — the resolution this plan announces"
  - phase: 29.1-per-role-model-assignment
    plan: 03
    provides: "the D-04 zero-config pin: the resolved-preset grammar, the generator's announcement, and the freshness gate's three-outcome assertion — all three of which this plan repairs"
provides:
  - "`RESOLVED_ASSIGNMENT_PREFIX` / `ResolvedAssignment` / `resolvedAssignmentLine` / `resolvedAssignmentsIn` — the grammar that announces what a resolution PRODUCED (member count, override count, distinct aliases) rather than one of its two inputs"
  - "`MIRRORED_RESOLVED_PRESET_PREFIX` / `mirroredResolvedPresetLine` / `mirroredResolvedPresetsIn` — the freshness gate's own verdict grammar, so no consumer hand-spells a marker"
  - "`anchoredValuesIn` — the one anchored line reader all three grammars are built from; every reader requires its owning prefix at byte 0"
  - "an `adapters-freshness` gate that refuses a mirrored run whose override count is not 0, whose alias set is not the zero-config alias, or whose member count disagrees with a listing the gate derived itself"
affects: [29.2-install-delivery, 30-per-checkpoint-autonomy]

actuals:
  tokens: 25800
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "An announcement must describe the FUNCTION'S OUTPUT, not one of its inputs — a resolver with two inputs cannot be pinned by announcing one of them"
    - "Every marker reader anchors at byte 0 on a prefix the EMITTER owns end to end; a caller that prepends anything produces a line its own reader refuses"
    - "A count travelling inside an announcement is cross-checked against a derivation the CONSUMER made itself — an announcement that only agrees with itself is a vacuity floor with more arithmetic"
    - "A malformed payload is a NAMED refusal, never a dropped line: dropping collapses it into the absent case, whose consumer reports a different fact with a different remedy"

key-files:
  created: []
  modified:
    - scripts/model-tiers.ts
    - scripts/model-tiers.js
    - scripts/model-tiers.test.ts
    - scripts/generate-role-adapters.ts
    - scripts/generate-role-adapters.js
    - scripts/generate-role-adapters.test.ts
    - scripts/adapters-freshness.ts
    - scripts/adapters-freshness.js
    - scripts/adapters-freshness.test.ts

key-decisions:
  - "The announcement carries the RESOLUTION's output — member count, override count and distinct aliases — rather than the review's cheapest suggestion of announcing `modelsConfig.source`. `source` is non-null on outcome 4 of `readModelsConfig` (a config file present but carrying no `models` key), so `source === null` is the zero-config condition only inside the freshness mirror and not on the live tree. Announcing the output is correct on both."
  - "The three review-reproduced incidental-mention strings do NOT by themselves discriminate the anchor once the prefix owns the generator's name — they are refused by the prefix change alone. Each case therefore asserts a SECOND shape carrying the WHOLE owning prefix mid-line, which only the anchor can refuse. Without that, the indexOf mutation left three of the four cases green."
  - "The plan's prefix-mutation proof is inverted by the mechanism it verifies: with one owner, mutating the constant moves the emitter and the reader together, so the oracle stays GREEN. The discriminating experiment run instead was that the gate's OUTPUT follows the mutated constant while the pre-fix hand-spelled gate's output cannot."
  - "The member-count cross-check sits AFTER the set-equality half, not before it. Placed above, it preempted the set half on an orphan or a missing adapter and reported an arithmetic disagreement instead of naming the member — observed as Cases 3 and 4 going RED on the first implementation."
  - "The gate's required alias set is DERIVED from `inheritForEveryStem` — the module's one implementation of the degrading policy — rather than spelled as a literal in the gate, so the requirement moves with its authority."
  - "The new gate cases are numbered 12-15, not 11-13 as the plan named them: Case 11 is already the committed CHECK_ROOT source pin, and renumbering a committed case to match a plan's numbering moves a name for no behavioural gain."
  - "`npm test` was NOT run. The regression lane ran as `npx vitest run --exclude '**/scripts/e2e/**'` — 55 files, 2294 passed, 2 skipped."

patterns-established:
  - "When a predicate is split across an owning PREFIX and an ANCHOR, test the anchor with an input that carries the whole owning prefix — otherwise the prefix silently does all the work and the anchor is untested"
  - "Before adding an arithmetic cross-check to a gate, ask which EXISTING finding also moves that number; if one does and names the member, the cross-check goes after it"

requirements-completed: []

coverage:
  - id: D35
    description: "CR-01 closed: the generator announces the whole resolution, and the freshness gate refuses a mirrored run whose announced override count is not 0, whose alias set is not the zero-config alias, or whose member count disagrees with the gate's own derivation"
    requirement: "MODEL-01"
    verification:
      - kind: other
        ref: "End-to-end reproduction, both halves. PRE-FIX (`git archive aa26bd8` mirror, `agent-factory/config` added to the twin list, `{\"models\":{\"roles\":{\"orchestrator\":\"opus\",\"security-nfr\":\"opus\"}}}` planted): gate exit 0, verdict `Mirrored generator resolved model preset: none`, two adapters carrying `model: opus`. POST-FIX, same plant: exit 1 naming `applied 2 per-role model override(s), and this gate requires 0`. Both transcripts below."
        status: pass
      - kind: command
        ref: "`npx vitest run scripts/adapters-freshness.test.ts` — 15 passed. Case 12 (the roles half) was proven RED against the pre-fix build."
        status: pass
      - kind: other
        ref: "Mutation proof: the member-count cross-check deleted; Case 13 went RED by name. Reverted, 15 passed."
        status: pass
  - id: D36
    description: "WR-03 closed: every reader anchors at byte 0 on a prefix the emitter owns, so an incidental mention in a warning, a shell echo, a comment or an indented line yields the empty list"
    requirement: "MODEL-01"
    verification:
      - kind: other
        ref: "Pre-fix and post-fix results recorded for all four shapes plus their owning-prefix variants. Table below."
        status: pass
      - kind: other
        ref: "Mutation proof: `trimEnd().startsWith(...)` replaced with the pre-fix `indexOf` form; all four incidental-mention cases plus two more went RED by name. Reverted, 81 passed."
        status: pass
  - id: D37
    description: "WR-04 closed: `adapters-freshness` spells no announcement marker; its verdict comes from an emitter exported beside the reader that parses it"
    requirement: "MODEL-04"
    verification:
      - kind: command
        ref: "`grep -c 'Mirrored generator resolved model preset: ' scripts/adapters-freshness.ts` = 0; the same grep over the committed `.js` = 0; `grep -c 'resolved model preset: '` over the `.ts` = 0; `grep -c 'mirroredResolvedPresetLine'` = 3."
        status: pass
      - kind: other
        ref: "Byte identity COMPARED, not eyeballed: the gate's live verdict line equals `mirroredResolvedPresetLine(\"none\")` exactly (`IDENTICAL: true`)."
        status: pass
  - id: D38
    description: "Per D-04, grugops's own repository carries no `models` key, `npm run generate:adapters` leaves `.claude/agents/` byte-unchanged, and the frozen pre-phase baseline still passes"
    requirement: "MODEL-01"
    verification:
      - kind: command
        ref: "`npm run generate:adapters` announced `roles: 17, overrides: 0, aliases: [\"inherit\"]`; 17 files in `.claude/agents/`; `git diff --exit-code -- .claude/agents/` empty; `npx vitest run scripts/adapter-byte-baseline.test.ts` exit 0."
        status: pass
      - kind: command
        ref: "`git diff --name-only -- install/ agent-factory/ package.json scripts/measure-cost.ts` empty across the whole plan."
        status: pass

duration: 47min
completed: 2026-08-20
status: complete
---

# Phase 29.1 Plan 07: Announce the Resolution, Not One of Its Inputs Summary

**The D-04 zero-config pin observed `modelsConfig.preset` — one of `resolveModels`'s two inputs —
so a `models.roles` block with no `preset` key announced the zero-config answer while emitting
`model: opus` adapters and the freshness gate exited 0; the generator now announces what the
resolution PRODUCED, every reader anchors at byte 0 on a prefix its emitter owns, and the gate
spells no marker of its own.**

## Performance

- **Duration:** 47 min
- **Tasks:** 3
- **Files modified:** 9 (0 created, 9 modified)
- **Commits:** 3

## Task Commits

| Task | Commit | Subject |
|------|--------|---------|
| 1 | `bfa6cb6` | anchor every announcement reader and declare the assignment grammar |
| 2 | `e697345` | the generator announces its resolution output, not one of its inputs |
| 3 | `5425107` | the freshness gate asserts the whole resolution and spells no marker |
| — | (this SUMMARY's commit) | plan close |

---

## Part 1 — CR-01, reproduced end to end in both directions

### PRE-FIX (mirror = `git archive HEAD` at `aa26bd8`, verbatim)

Plant: `agent-factory/config/factory.config.json` = `{"models":{"roles":{"orchestrator":"opus","security-nfr":"opus"}}}`
Edit: `agent-factory/config` added to the gate's twin list (the "future edit" the gate's own header predicts).

```
$ node scripts/generate-role-adapters.js
generate-role-adapters: wrote 17 adapters to <mirror>/.claude/agents (coordinator grugops-orchestrator grants 16 names)
generate-role-adapters: resolved model preset: none

$ grep -H '^model:' .claude/agents/grugops-orchestrator.md .claude/agents/grugops-security-nfr.md
.claude/agents/grugops-security-nfr.md:model: opus
.claude/agents/grugops-orchestrator.md:model: opus

$ node scripts/adapters-freshness.js
Adapters fresh: 17 adapter(s) compared in .claude/agents, 0 byte difference(s), directory listings set-equal.
Mirrored generator resolved model preset: none
EXIT=0
```

The gate certified a configured regeneration as the zero-config output. D-04 was false and every
gate was green.

### POST-FIX (same plant, same twin-list edit, build at `5425107`)

```
$ node scripts/generate-role-adapters.js
generate-role-adapters: wrote 17 adapters to <mirror>/.claude/agents (coordinator grugops-orchestrator grants 16 names)
generate-role-adapters: resolved model preset: none
generate-role-adapters: resolved model assignment: {"roles":17,"overrides":2,"aliases":["inherit","opus"]}

$ grep -H '^model:' .claude/agents/grugops-orchestrator.md .claude/agents/grugops-security-nfr.md
.claude/agents/grugops-security-nfr.md:model: opus
.claude/agents/grugops-orchestrator.md:model: opus

$ node scripts/adapters-freshness.js
Adapter freshness check FAILED: the mirrored regeneration applied 2 per-role model override(s), and this gate requires 0.
This gate's comparison is a statement about the ZERO-CONFIG output: the committed adapters are the adapters the generator produces when nothing is configured (D-04). A mirrored run that resolved anything else is comparing a different artifact than the one the committed adapters claim to be, so its verdict — pass or fail — would carry no information about freshness.
THIS IS THE HALF THE PRESET ASSERTION ABOVE CANNOT SEE (finding CR-01): a `models.roles` block with no `preset` key resolves the preset as "none" while overriding individual roles, so the preset line reads exactly as it does under zero configuration.
The likely cause is a configuration file reaching the regeneration mirror — check the twin list in this script, and check the tree under judgement for a `models.roles` block.
EXIT=1
```

The preset line is unchanged and still truthfully reads `none`. The finding comes from the line
that describes the resolution's OUTPUT.

---

## Part 2 — WR-03: the four incidental-mention shapes, pre-fix and post-fix

The three verbatim strings are the ones reproduced in `29.1-REVIEW.md` § WR-03. The fourth
(leading whitespace) the review does not name.

| Input | Pre-fix (`aa26bd8`) | Post-fix (`5425107`) |
|---|---|---|
| `WARN could not read resolved model preset: none` | `["none"]` | `[]` |
| `echo "resolved model preset: tiered" >> log` | `["tiered\" >> log"]` | `[]` |
| `# TODO: fix resolved model preset: none` | `["none"]` | `[]` |
| `  generate-role-adapters: resolved model preset: none` (2 leading spaces) | `["none"]` | `[]` |
| `generate-role-adapters: resolved model preset: none` (un-indented) | `["none"]` | `["none"]` |
| `resolvedPresetLine("none") + "\r"` | `["none"]` | `["none"]` |

**The discrimination the plan did not anticipate.** Once `RESOLVED_PRESET_PREFIX` owns the
generator's name, the first three shapes above are refused by the PREFIX alone — the anchor is not
exercised. The first mutation run proved it: reverting to `indexOf` left three of the four cases
green. Each case therefore asserts a second shape carrying the whole owning prefix mid-line:

| Owning-prefix variant | Post-fix |
|---|---|
| `WARN could not read generate-role-adapters: resolved model preset: none` | `[]` |
| `echo "generate-role-adapters: resolved model preset: tiered" >> log` | `[]` |
| `# TODO: fix generate-role-adapters: resolved model preset: none` | `[]` |

**Mutation proof, observed RED and reverted.** `trimEnd().startsWith(prefix)` replaced with the
pre-fix `indexOf` form, rebuilt:

```
× resolvedPresetsIn refuses an incidental mention in a warning line
× resolvedPresetsIn refuses an incidental mention inside a shell echo
× resolvedPresetsIn refuses an incidental mention inside a comment
× resolvedPresetsIn refuses a leading-whitespace line
× the emitter owns the WHOLE prefix — no caller may prepend the generator's name to it
× the assignment reader is anchored too — an incidental mention is not an announcement
Tests  6 failed | 75 passed (81)
```

Reverted: 81 passed.

---

## Part 3 — WR-04: the two grep counts, and the prefix-mutation experiment

```
grep -c 'Mirrored generator resolved model preset: ' scripts/adapters-freshness.ts = 0
grep -c 'Mirrored generator resolved model preset: ' scripts/adapters-freshness.js = 0
grep -c 'resolved model preset: '                    scripts/adapters-freshness.ts = 0
grep -c 'mirroredResolvedPresetLine'                 scripts/adapters-freshness.ts = 3
```

Byte identity, compared rather than eyeballed:

```
verdict lines found: 1
live    : "Mirrored generator resolved model preset: none"
emitter : "Mirrored generator resolved model preset: none"
IDENTICAL: true
gate exit: 0
```

### The prefix-mutation proof, run in the direction the mechanism allows

The plan asked for a one-character mutation of the prefix constant to turn the oracle RED. **That
direction is inverted by the property it verifies.** With one owner, mutating the constant moves the
emitter and the reader together, so a round-trip case stays green by construction. The
discriminating experiment is whether the gate's OUTPUT follows the constant.

Mutation applied (`preset:` → `presel:` in `MIRRORED_RESOLVED_PRESET_PREFIX`), rebuilt:

```
$ node scripts/adapters-freshness.js | tail -1
Mirrored generator resolved model presel: none          <- the gate's output FOLLOWED the constant

$ npx vitest run scripts/adapters-freshness.test.ts
Tests  15 passed (15)                                    <- one owner, so both halves moved together
```

Against the pre-fix hand-spelled gate (`git show aa26bd8:scripts/adapters-freshness.js`) run over
the same mutated `model-tiers.js`:

```
pre-fix gate .js still spells the OLD literal: true
mirroredResolvedPresetsIn(pre-fix gate stdout) = []      <- Cases 7, 8 and 15 go RED
```

The hand-spelled literal cannot follow the constant; the emitted line can. That is the
one-ownership property, demonstrated rather than asserted. (The pre-fix gate also exits 1 in that
mirror for an unrelated reason — its pre-fix generator twin's preset line does not satisfy the new
reader — so the exit code is not the evidence here; the empty reader result is.)

### Member-count mutation

Cross-check deleted, rebuilt:

```
× Case 13 (RED): a mirrored run whose announced member count disagrees with the derived adapter count fails closed
Tests  1 failed | 14 passed (15)
```

Reverted: 15 passed.

---

## Part 4 — The three numbers `npm run generate:adapters` announced

```
generate-role-adapters: wrote 17 adapters to /Users/olgeroeselg/Projects/public/grugops/.claude/agents (coordinator grugops-orchestrator grants 16 names)
generate-role-adapters: resolved model preset: none
generate-role-adapters: resolved model assignment: {"roles":17,"overrides":0,"aliases":["inherit"]}
```

| Number | Announced | Independently derived |
|---|---|---|
| member count | `17` | `ls -1 .claude/agents/*.md \| wc -l` = **17** |
| override count | `0` | no `models` key in either config JSON |
| distinct aliases | `["inherit"]` | every committed adapter's `model:` line |

`git diff --exit-code -- .claude/agents/` empty afterwards.

---

## Part 5 — The twin list, read

`scripts/adapters-freshness.ts` `cpSync` source paths, as read at plan close. **No configuration
directory is among them** — the `models.roles` discrimination case plants one into a test-owned
mirror only.

```
join(SCRIPT_ROOT, "scripts", "generate-role-adapters.js")
join(SCRIPT_ROOT, "scripts", "kit-model.js")
join(SCRIPT_ROOT, "scripts", "frontmatter.js")
join(SCRIPT_ROOT, "scripts", "model-tiers.js")
join(KIT_ROOT,    "agent-factory", "roles")
join(KIT_ROOT,    "agent-factory", "packaging")
```

`delete childEnv.CHECK_ROOT` stands unchanged and is still asserted by Case 11. No environment
variable was introduced.

---

## Part 6 — Verification commands, as run

| Command | Result |
|---|---|
| `npx vitest run scripts/model-tiers.test.ts` | 81 passed |
| `npx vitest run scripts/generate-role-adapters.test.ts` | 47 passed, 1 skipped |
| `npx vitest run scripts/adapters-freshness.test.ts` | 15 passed |
| `npx vitest run scripts/adapter-byte-baseline.test.ts` | 2 passed |
| `npm run freshness:adapters` | exit 0, verdict names `none` |
| `npm run freshness` | 49 committed `.js` files fresh |
| `node scripts/check-foundation-guards.js` | `ALL CHECKS PASSED` |
| `npm run typecheck` | exit 0 |
| `npm run build` then `git diff --exit-code --name-only -- '*.js'` | empty |
| `npx vitest run --exclude '**/scripts/e2e/**'` | 55 files, 2294 passed, 2 skipped |
| `git diff --name-only -- install/ agent-factory/ package.json scripts/measure-cost.ts` | empty |

`npm test` was never run.

---

## Deviations from Plan

### Auto-fixed issues

**1. [Rule 2 — missing critical discrimination] The three review-verbatim shapes did not exercise
the anchor**
- **Found during:** Task 1, at the mutation proof.
- **Issue:** With `RESOLVED_PRESET_PREFIX` now owning the generator's name, the review's three
  reproduced strings are refused by the prefix change alone. Reverting the anchor to `indexOf` left
  three of the four cases GREEN — the plan's acceptance criterion expected all four RED.
- **Fix:** each of the three cases asserts a SECOND input carrying the whole owning prefix mid-line.
  Re-ran the mutation: all four RED, plus two more.
- **Files:** `scripts/model-tiers.test.ts`
- **Commit:** `bfa6cb6`

**2. [Rule 1 — bug] The member-count cross-check preempted the set-equality half**
- **Found during:** Task 3, first implementation.
- **Issue:** placed before the listing comparison and keyed on `committedNames.length`, it fired on
  an orphan or a missing adapter and reported an arithmetic disagreement instead of naming the
  member. Observed: Cases 3 and 4 went RED.
- **Fix:** moved AFTER the set half (where both listings are provably set-equal) and keyed on
  `rebuiltNames.length` — the count the announcing run actually produced. Still before the byte
  comparison. Reason recorded beside the check and in the header.
- **Files:** `scripts/adapters-freshness.ts`
- **Commit:** `5425107`

**3. [Rule 3 — blocking] Case-number collision**
- **Issue:** the plan names the new gate cases 11, 12 and 13; Case 11 is already the committed
  CHECK_ROOT source pin.
- **Fix:** numbered 12–15 (a fourth case was added, see below) with the reason recorded in the file.
- **Files:** `scripts/adapters-freshness.test.ts`
- **Commit:** `5425107`

**4. [Rule 2 — missing coverage] The absent-assignment-line behaviour had no case**
- **Issue:** the plan's behavior list names "the gate fails closed when the mirrored run announces
  no assignment line at all", and the three named cases did not cover it.
- **Fix:** added Case 14 with a `stripAssignmentAnnouncement` helper that leaves the preset line
  intact, so the case cannot pass through the preset-absent branch.
- **Files:** `scripts/adapters-freshness.test.ts`
- **Commit:** `5425107`

**5. [Rule 1 — inverted experiment] The prefix-mutation proof, as specified, cannot go RED**
- **Issue:** the plan asks for a one-character mutation of the prefix constant to turn the oracle
  RED, and states that under the pre-fix hand-spelling the same mutation left it green. With one
  owner the emitter and the reader move together, so the round trip stays green — the plan's
  expectation and its mechanism disagree.
- **Fix:** ran the discriminating experiment instead (does the gate's OUTPUT follow the constant?),
  recorded in Part 3 with both halves. Not silently skipped.

**6. [Rule 2] Payload quoting**
- The plan's acceptance criterion says the refusal "quotes the payload". The payload arrives from a
  spawned child's stdout, so it is untrusted text; it is quoted with `JSON.stringify` (the module's
  established `quoteValue` convention) rather than interpolated raw, and the case asserts
  `JSON.stringify(payload)` is present. Recorded in the case body.

### Not deviations

- Pre-existing `.planning/STATE.md` and `human-notes.txt` modifications were left untouched.

---

## Known Stubs

None. Every symbol introduced is called from production source and exercised by an oracle.

## Threat Flags

None. The plan's register (T-29.1-10 through T-29.1-13, T-29.1-05, T-29.1-SC) is unchanged; no new
network endpoint, auth path, file-access pattern or schema at a trust boundary was introduced. Zero
packages installed; `package.json` byte-unchanged.

## Deferred Issues

The review's other findings are out of this plan's scope and remain open for the round's other
plans: WR-01 (unknown keys inside the `models` block silently swallowed), WR-02 (`resolveModels`
Floor 0 coerces a nullish preset), WR-05 (the packaging authority names one config location while
the resolver reads two), WR-06 (the unsourced host-CLI scope sentence).

`29.1-EDGE-COVERAGE.json`'s 14 applicable edge items remain `unresolved`, carried forward as the
plan's stated assumptions. MODEL-01's `adjacency / empty / ordering` items are untouched: the
zero-config resolution over the live 17-role corpus is the only shape this plan pins, a corpus of 0
is refused by the vacuity floor, and a corpus of 1 is untested.

## Self-Check: PASSED

All nine modified files present on disk; all three task commits present in `git log`.

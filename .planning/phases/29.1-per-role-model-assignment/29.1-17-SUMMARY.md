---
phase: 29.1-per-role-model-assignment
plan: 17
subsystem: tooling-gates
tags: [check-kit-refs, D-08.1, R2-WR-05, scan-widening, derived-exemption, set-literal-drift, round-3-close]
status: complete

requires:
  - "29.1-12 (relative-root byte identity in scripts/check-foundation-guards.ts) — re-asserted after this plan's guard edit"
  - "29.1-16 (the packaging authority's shape + the WR-05 oracle's current wording) — the comment block this plan corrects"
  - "`MODELS_CONFIG_CANDIDATE_RELS` in scripts/model-tiers.ts as the single declaration of the two configuration locations"
provides:
  - "`agent-factory/config` inside check-kit-refs's SCAN — D-08.1 now reaches the document install.ts:1065 ships"
  - "`exemptConfigSelfRefs` — Assertion 1's hits partitioned by a PREDICATE (self-reference to an existing sibling), never by a list"
  - "`CONFIG_SELF_REF_FILES` / `CONFIG_SELF_REF_HITS` — the exemption's two cardinalities, asserted two-sided and named in the failure text"
  - "An Assertion 1 vacuity floor: a tree with no configuration directory is a NAMED finding"
  - "An Assertion 1 pass line that PUBLISHES the exempt count and the exempting file"
  - "`gateScanSet()` — the mirror's scan set derived from the gate's own declaration, bounded at both ends, cardinality asserted"
  - "Three corrected records: the oracle's WR-05 comment block, foundation-guards inventory row 7, and 29.1-11-SUMMARY.md (annotated)"
affects:
  - scripts/check-kit-refs.ts
  - scripts/check-kit-refs.js
  - scripts/check-kit-refs.test.ts
  - scripts/model-dial-consistency.test.ts
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - .planning/phases/29.1-per-role-model-assignment/29.1-11-SUMMARY.md

actuals:
  tokens: 13614
  basis: "chars/4 over the realized diff of the three task commits against the plan's base 668600d (54,457 chars). Same basis 29.1-14 and 29.1-15 used. The estimate was 95,000 at `confidence: low`; the gap is the same basis difference those two plans recorded plus a genuinely contained edit — two gate files, one test file, one oracle comment block, one inventory row and one annotated planning document."
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Widen a zero-tolerance scan by one entry and pay for it with a DERIVED, COUNTED exemption rather than leaving the directory unscanned"
    - "An exemption's cardinality is asserted TWO-SIDED in the gate itself, in the commit that creates the exemption"
    - "Publish an exemption in the PASS line — a green run must disclose what it excused, not imply it by silence"
    - "A vacuity floor guards an exemption's DENOMINATOR: an absent directory is a named finding, never an unattributable cardinality disagreement"
    - "Require a self-reference to name something that EXISTS — a line naming no path at all satisfies 'every named path exists' vacuously"
    - "Delete the second copy of a set and derive it from the first, bounded at both ends, with the member count asserted"
    - "Correct a falsified record in the SAME commit as the change that falsifies it; ANNOTATE a superseded summary, never rewrite it"

key-files:
  created:
    - .planning/phases/29.1-per-role-model-assignment/29.1-17-SUMMARY.md
  modified:
    - scripts/check-kit-refs.ts
    - scripts/check-kit-refs.js
    - scripts/check-kit-refs.test.ts
    - scripts/model-dial-consistency.test.ts
    - scripts/check-foundation-guards.ts
    - scripts/check-foundation-guards.js
    - .planning/phases/29.1-per-role-model-assignment/29.1-11-SUMMARY.md

key-decisions:
  - "R2-WR-05 / D6 closed as the USER RULED, not as the executor would have chosen: `agent-factory/config` joined check-kit-refs's SCAN, with an explicit argued exemption. The alternative (deleting the two mentions) was never available — `scripts/model-dial-consistency.test.ts` asserts the field reference names every declared configuration location, which IS the WR-05 closure, so deleting them would reopen WR-05 to close R2-WR-05."
  - "The exemption is a PREDICATE over hits, with two parts, and the second part's 'at least one named path' clause is load-bearing: a line mentioning the directory prefix and naming no file would otherwise satisfy 'every named path exists' vacuously and be exempted for having said nothing."
  - "Both cardinalities are asserted in the GATE, not only in a test. A test can be deleted by the same editor who adds the third mention; a gate assertion fails the build."
  - "MEASURED CORRECTION: the field reference carries THREE substring occurrences of the kit-internal prefix but TWO gate HITS. Line 138 names the path twice on one line and `grepSubstring` emits one hit per LINE. The plan's 'two mentions' is true at line granularity and false at occurrence granularity; the premise helper in the test counts LINES, the unit the gate counts."
  - "The acceptance criterion `grep -c 'const SCAN' scripts/check-kit-refs.test.ts` → 0 CANNOT be satisfied honestly and is recorded NOT satisfied as written. The surviving hit is the extractor's LOCATOR STRING, which must spell the gate's declaration verbatim to find it. Splitting the literal to make a grep return zero is the move this repository rejected in plan 29.1-11 deviation 3. The honest predicates are recorded in the file and below."
  - "The disposition table's asserted count of fifteen is reported with its ambiguity rather than as a clean number: 12 findings + 3 `human_verification` ENTRIES = 15, but the third entry names TWO items and D6 is one of the three. At item granularity the round has SIXTEEN items, of which D6 is the one this plan closes."

patterns-established:
  - "When a plan asserts a count over a set assembled from two documents, state which GRANULARITY the count is taken at before asserting it — entries and items are different denominators"
  - "A locator string that must quote what it locates will always answer a grep for the thing it locates; report the honest anchored predicate instead of splitting the literal"

requirements-completed: [MODEL-01, MODEL-02, MODEL-05, MODEL-07]

coverage:
  - id: D1
    description: "check-kit-refs's SCAN reaches agent-factory/config, the directory install.ts:1065 ships to every installed user"
    requirement: MODEL-02
    verification:
      - kind: command
        ref: "node scripts/check-kit-refs.js → exit 0, Assertion 1 pass line names 2 exempt hits in agent-factory/config/factory.config.md"
        status: pass
      - kind: unit
        ref: "scripts/check-kit-refs.test.ts#the mirror's scan set is DERIVED from the gate's own declaration, with its cardinality asserted"
        status: pass
    human_judgment: false
  - id: D2
    description: "The exemption is a predicate over hits with both cardinalities asserted two-sided and named in the failure text"
    requirement: MODEL-05
    verification:
      - kind: unit
        ref: "scripts/check-kit-refs.test.ts#Assertion 1 RED: a THIRD kit-internal mention in the config field reference fails, naming both counts"
        status: pass
      - kind: unit
        ref: "scripts/check-kit-refs.test.ts#Assertion 1 RED: a kit-internal mention in a SECOND file under the config directory fails"
        status: pass
      - kind: other
        ref: "mutation transcripts A and B below — captured against the committed .js on a hermetic mirror"
        status: pass
    human_judgment: false
  - id: D3
    description: "A self-reference must name an EXISTING sibling; a mention outside the configuration directory still fails with the pre-existing wording"
    requirement: MODEL-05
    verification:
      - kind: unit
        ref: "scripts/check-kit-refs.test.ts#Assertion 1 RED: a kit-internal mention naming a path that does not exist is a stray, not a self-reference"
        status: pass
      - kind: unit
        ref: "scripts/check-kit-refs.test.ts#Assertion 1 RED: a kit-internal mention outside the config directory still fails as it always did"
        status: pass
    human_judgment: false
  - id: D4
    description: "The exemption is visible in a GREEN verdict, and an absent configuration directory is a named finding"
    requirement: MODEL-05
    verification:
      - kind: unit
        ref: "scripts/check-kit-refs.test.ts#Assertion 1 GREEN: the config field reference's self-references are exempt, and the verdict says so"
        status: pass
      - kind: unit
        ref: "scripts/check-kit-refs.test.ts#Assertion 1 vacuity floor: a tree with no configuration directory is a named finding"
        status: pass
    human_judgment: false
  - id: D5
    description: "The second hand-maintained copy of the scan set is deleted and derived, proven RED on a removed member"
    requirement: MODEL-01
    verification:
      - kind: command
        ref: "grep -cE '^const SCAN = ' scripts/check-kit-refs.test.ts → 0; grep -c '\"agent-factory/checklists\"' scripts/check-kit-refs.test.ts → 0"
        status: pass
      - kind: other
        ref: "derivation RED transcript below — one member removed from the gate's declaration, cardinality reds naming 10 and 9, gate source byte-restored"
        status: pass
    human_judgment: false
  - id: D6
    description: "Three records falsified by the scan widening state what is true, in the same commit"
    requirement: MODEL-02
    verification:
      - kind: command
        ref: "grep -c 'lives outside the scan set' scripts/model-dial-consistency.test.ts → 0; grep -c 'admits no exemption list' scripts/model-dial-consistency.test.ts → 0"
        status: pass
      - kind: other
        ref: "before/after quotes of all three records below; 29.1-11-SUMMARY.md original shown still standing above its annotation"
        status: pass
    human_judgment: false
  - id: D7
    description: "The round closes green: the fourteen-command phase gate block in CI's order, freshness first"
    requirement: MODEL-07
    verification:
      - kind: command
        ref: "the fourteen-command gate table below — every command EXIT=0"
        status: pass
      - kind: command
        ref: "npx vitest run --exclude '**/scripts/e2e/**' → 55 files, 2355 passed, 2 skipped, exit 0"
        status: pass
    human_judgment: false
  - id: D8
    description: "All twelve round-2 findings and every carried human item are reconciled with nothing silently dropped"
    requirement: MODEL-07
    verification:
      - kind: other
        ref: "the disposition table below — item list DERIVED from the two round-2 documents by grep, count stated at both granularities"
        status: pass
    human_judgment: true
    rationale: "Whether the three CARRIED items (D5 vendor-citation fidelity, MODEL-07/boundary, MODEL-07/precision) should remain carried rather than closed is the phase's own declared human-adjudication question. This plan reconciles them; it does not adjudicate them."

duration: 22 min
completed: 2026-08-20
---

# Phase 29.1 Plan 17: Scan Reach, an Argued Exemption, and the Round's Close — Summary

`agent-factory/config` joined `check-kit-refs`'s zero-tolerance D-08.1 scan, and the two kit-internal
mentions the shipped field reference owes its reader are exempt by a derived predicate whose
cardinality the gate asserts two-sided and publishes in its own pass line — closing R2-WR-05 as the
user ruled on D6, deleting the second copy of the scan set, and correcting three records the change
falsifies.

**Duration:** 22 min · **Tasks:** 3 · **Commits:** 3 · **Files changed:** 7

---

## Task 1 — the three measurements, recorded verbatim before any edit

### Measurement one: what the new SCAN member moves, per assertion

Run with `agent-factory/config` temporarily inserted into a scratch copy of the committed `.js`
(`scripts/.tmp-measure.mjs`, deleted immediately after; the committed gate was not touched).

| Assertion | BEFORE (baseline `node scripts/check-kit-refs.js`) | AFTER (config in SCAN, no exemption) |
|---|---|---|
| `[derivation]` | `PASS 24 adapter file(s) derived` | `PASS 24 adapter file(s) derived` |
| Assertion 1 | `PASS no agent-factory/config/ refs remain` | **`FAIL stray agent-factory/config/ ref(s)`** — 2 hits named |
| Assertion 2 | `PASS no agent-factory/handoffs/ refs remain` | `PASS no agent-factory/handoffs/ refs remain` |
| Assertion 3, negative half | `PASS no kit prose / AGENTS.md names $GRUGOPS_HOME (5 scan entries compared)` | *identical* |
| Assertion 3, positive half | `PASS $GRUGOPS_HOME appears in exactly the 19 derived legal site(s)` | *identical* |
| SC2 marker check | `PASS invariant marker present at all 26 marker sites (2 named + 24 derived adapters)` | *identical* |
| Result | `ALL CHECKS PASSED` (exit 0) | `1 CHECK(S) FAILED` (exit 1) |

**Assertion 1 is the only assertion the new member moves.** This was confirmed by measurement, not
assumed. Assertions 2 and 3 and SC2 report byte-identical verdicts and identical numbers — 5 scan
entries, 19 legal sites, 26 marker sites. The reason Assertion 3 does not move is itself measured:
`grep -rn 'GRUGOPS_HOME' agent-factory/config/` returns nothing.

### Measurement two: the kit-internal mentions the directory carries

Enumerated with the gate's own needle, `agent-factory/config/`:

```
agent-factory/config/factory.config.md:3:  … The copy beside this reference at
   `agent-factory/config/factory.config.json` is a byte-identical companion to this document.
agent-factory/config/factory.config.md:138: … The resolver looks in exactly two places, in this
   order: `.grugops/factory.config.json` first, then `agent-factory/config/factory.config.json`.
   … a `models` block written into `agent-factory/config/factory.config.json` …
```

Directory contents: `agent-factory/config/.gitkeep`, `agent-factory/config/factory.config.json`,
`agent-factory/config/factory.config.md`. Neither the `.gitkeep` nor the JSON carries the needle.

**A precision the plan's wording did not carry, recorded rather than smoothed over.** Line 138 names
the kit-internal path **twice on one line**. `grepSubstring` emits one `path:lineno:line` hit per
LINE, so the file carries **three substring occurrences and two gate hits**. `CONFIG_SELF_REF_HITS`
is therefore 2 — the LINE count, the unit the gate counts. A test premise written in substring
occurrences reds for a reason nobody could attribute; the first draft of the green case did exactly
that and was corrected by measurement (see Deviations §2).

### Measurement three: the `copyKit` line, with its surrounding call

`install/install.ts`, lines 1063-1067:

```ts
1063:   const old = `${KIT_ROOT}.old.${process.pid}`;
1064:   rmSync(tmp, { recursive: true, force: true });
1065:   cpSync(join(GRUGOPS_SRC, "agent-factory"), tmp, { recursive: true });
1066:   // Move the existing kit aside (if any), put the new kit in place via a single atomic rename,
1067:   // then handle the old copy. …
```

`copyKit` is reached unconditionally on the install path (`install/install.ts:1410`,
`copyKit(false);`) and on the `--update` path (`:1341`, `copyKit(true);`). The whole
`agent-factory/` tree — `config/` included — lands at `$GRUGOPS_HOME/agent-factory`. **That is the
measured fact that makes the field reference shipped kit prose, and therefore in scope for D-08.1.**

### The `SCAN` diff — exactly one added member

```diff
 const SCAN = [
   "agent-factory/roles",
   "agent-factory/workflows",
   "agent-factory/checklists",
   "agent-factory/packaging",
+  "agent-factory/config",
   "agent-factory/_commit-convention.md",
   ".claude/skills",
   ".claude/agents",
   "skills",
   "AGENTS.md",
 ];
```

One added member, no other membership change. `seed/`, `examples/`, `install/`, `docs/`,
`.planning/` and the root documents stay omitted, and the header's warning that derived membership is
not a licence to widen the scan is restated in the new rationale comment rather than quietly dropped.

### The Assertion 1 pass line, as printed

```
[Assertion 1] no agent-factory/config/ refs remain (config now .grugops/factory.config.json)
  PASS  no agent-factory/config/ refs remain (2 counted self-reference(s) exempt, in agent-factory/config/factory.config.md)
  PASS  the config self-reference exemption is exactly 2 hit(s) in 1 file(s), as declared
```

A reader of a green run learns the exemption exists, how large it is and which file carries it.

### The exemption is a PREDICATE, quoted

```ts
function exemptConfigSelfRefs(hits: string[]): { exempt: string[]; strays: string[]; files: string[] } {
  const dirPrefix = join(CONFIG_SELF_REF_DIR) + sep;
  …
  for (const hit of hits) {
    const m = HIT_SHAPE.exec(hit);
    const file = m ? m[1] : "";
    const line = m ? m[3] : hit;
    const named = [...line.matchAll(CONFIG_NAMED_PATH)].map((n) => n[1]);
    const inConfigDir = file.startsWith(dirPrefix);
    const allResolve =
      named.length > 0 &&
      named.every((base) => existsSync(abs(join(CONFIG_SELF_REF_DIR, base))));
    if (inConfigDir && allResolve) { exempt.push(hit); files.add(file); }
    else { strays.push(hit); }
  }
  …
}
```

No line-number list, no hit-string allow-list, no file allow-list. The `named.length > 0` clause is
load-bearing: without it, a line that mentioned the directory prefix and named no file at all would
satisfy "every named path exists" vacuously and be exempted for having said nothing.

---

## The five mutation transcripts — five different messages, not five non-zero exits

Each ran the **committed `.js`** against a hermetic mirror built from the SCAN set, with
`CHECK_ROOT` pointed at the mirror. Only the Assertion 1 region is quoted.

### A — a THIRD kit-internal mention in the field reference

```
  PASS  no agent-factory/config/ refs remain (3 counted self-reference(s) exempt, in agent-factory/config/factory.config.md)
  FAIL  the agent-factory/config/ self-reference exemption has drifted — it is 2 hit(s) in 1 file(s)
        BY DECLARATION, and a new mention must be argued and counted, never absorbed:
  exempt HITS: found 3, required exactly 2:
agent-factory/config/factory.config.md:3:…
agent-factory/config/factory.config.md:138:…
agent-factory/config/factory.config.md:176:See `agent-factory/config/factory.config.json` for the bundled copy.
```

Exit 1. **Distinguishing text:** `exempt HITS: found 3, required exactly 2` — the found count, the
required count and the new hit, quoted in full. No `exempting FILES:` clause: the third mention is in
the same file.

### B — a kit-internal mention in a SECOND file under the configuration directory

```
  FAIL  the agent-factory/config/ self-reference exemption has drifted — …
  exempting FILES: found 2, required exactly 1 (agent-factory/config/NOTES.md, agent-factory/config/factory.config.md)
  exempt HITS: found 3, required exactly 2:
agent-factory/config/NOTES.md:1:The bundled copy lives at `agent-factory/config/factory.config.json`.
…
```

Exit 1. **Distinguishing text:** `exempting FILES: found 2, required exactly 1` — the clause A never
emits. The exemption is scoped to one FILE, not to the directory.

### C — a mention naming a path that does NOT exist

```
  FAIL  stray agent-factory/config/ ref(s) — config must be .grugops/factory.config.json:
agent-factory/config/factory.config.md:176:See `agent-factory/config/nowhere.json` for the bundled copy.
  PASS  the config self-reference exemption is exactly 2 hit(s) in 1 file(s), as declared
```

Exit 1. **Distinguishing text:** the STRAY arm's pre-existing wording, naming a hit **inside** the
configuration directory — and the cardinality clause still PASSES, because a stray was never
exempted. A self-reference means a reference to something that is there.

### D — a kit-internal mention OUTSIDE the configuration directory

```
  FAIL  stray agent-factory/config/ ref(s) — config must be .grugops/factory.config.json:
agent-factory/roles/orchestrator.md:83:Edit agent-factory/config/factory.config.json to configure.
  PASS  the config self-reference exemption is exactly 2 hit(s) in 1 file(s), as declared
```

Exit 1. **Distinguishing text:** the same pre-existing wording naming a hit in `agent-factory/roles/`
— the exemption did not widen the gate for the rest of the scan set. C and D share an arm and are
distinguished by the file the arm names: C's is inside the configuration directory, D's is not.

### E — the vacuity floor: a tree with no configuration directory

```
  FAIL  no agent-factory/config/ directory found — refusing to adjudicate its exemption against a
        tree that does not carry it (the counted self-references below would be absent for an
        unattributable reason)
  PASS  no agent-factory/config/ refs remain (0 counted self-reference(s) exempt, in no file)
```

Exit 1. **Distinguishing text:** a NAMED finding. Without the floor this tree would have produced
`exempting FILES: found 0, required exactly 1` — a cardinality disagreement nobody could attribute.

### Control — the unmutated mirror

```
  PASS  no agent-factory/config/ refs remain (2 counted self-reference(s) exempt, in agent-factory/config/factory.config.md)
  PASS  the config self-reference exemption is exactly 2 hit(s) in 1 file(s), as declared
```

Exit 0.

---

## Task 2 — the mirror's scan set derived, and the derivation proven RED

### The derivation's RED transcript on a removed member

One member (`"skills"`) removed from the gate's **`.ts`** declaration; the committed `.js` untouched;
the derivation case run against the mutated source:

```
FAIL scripts/check-kit-refs.test.ts > check-kit-refs mirror — the scan set is derived from the gate,
     not copied (plan 29.1-17) > the mirror's scan set is DERIVED from the gate's own declaration,
     with its cardinality asserted

AssertionError: expected [ 'agent-factory/roles', …(8) ] to have a length of 10 but got 9
  - Expected  10
  + Received   9
```

Both numbers named. Restored immediately; `git diff --exit-code -- scripts/check-kit-refs.ts`
returned **exit 0 — byte-identical to HEAD**.

### The derivation's own bounds, proven

`gateScanSet()` locates `const SCAN = [` and ends at its closing bracket. Both bounds throw by name:

- closing bracket removed → `/has no closing\s+bracket/` — *"refusing to read to end of file, which
  would adopt every later array in the module"*
- declaration absent → `/carries no ".*" declaration/`

Both asserted by the case `gateScanSet throws by NAME when the closing bracket is missing, rather
than reading to end of file`, against in-memory scratch copies. The committed gate source is never
written to.

### One premise assertion quoted per new case

| Case | Premise asserted before the gate runs |
|---|---|
| GREEN | `expect(linesContaining(readFileSync(ref,"utf8"), CONFIG_REF_NEEDLE), "PREMISE: the reference must carry exactly the two mentions the exemption is declared for").toBe(2)` |
| RED, third mention | `"PREMISE: the plant must take the mention count from two to three"` → `.toBe(2)` before the plant |
| RED, second file | `"PREMISE: the sibling the plant names must exist, so the plant is a SELF-reference and not a stray"` → `.toBe(true)` |
| RED, ghost path | `"PREMISE: the named path must NOT exist, or this would be a legal self-reference"` → `.toBe(false)` |
| RED, outside dir | `"PREMISE: the target must be OUTSIDE the configuration directory"` → `.toBe(false)` |
| Vacuity floor | `"PREMISE: the mirror must carry the directory before it is removed"` → `.toBe(true)` |
| Derivation | `"PREMISE: the derivation must be non-empty — an empty mirror is green for every absence assertion here"` |

The GREEN case asserts the exemption is **named in the pass line**, not merely that the gate exited 0:

```ts
expect(r.stdout).toContain("2 counted self-reference(s) exempt, in agent-factory/config/factory.config.md");
expect(r.stdout).toContain("the config self-reference exemption is exactly 2 hit(s) in 1 file(s), as declared");
```

### Case count and the pre-existing cases

`npx vitest run scripts/check-kit-refs.test.ts` — **13 cases before, 21 after** (8 added: the
derivation's cardinality, the derivation's two named throws in one case, one green, four red, one
vacuity floor). **Every pre-existing case passes unmodified.** None was deleted, retargeted or
reworded.

**One transient state, recorded because it is the drift this plan closes, observed.** Between the
Task 1 commit (`ba9f3c4`) and the Task 2 commit (`22749a1`) the suite was RED: four pre-existing
GREEN-expecting cases failed, because the test file's hand-copied scan set did not carry
`agent-factory/config`, so every mirror it built was missing the directory the widened gate then
judged — and the new vacuity floor named it. That is exactly the failure the plan predicted the copy
would produce. It is recorded rather than hidden by squashing the two commits.

---

## Task 3 — the three corrected records, quoted before and after

### Record 1 — the oracle's WR-05 comment block (`scripts/model-dial-consistency.test.ts`)

**BEFORE** (the three falsified sentences, in place):

> `// … That gate is twenty-two phases`
> `// older than this dial and admits no exemption list.`
> `//`
> `// SO THE OBLIGATION IS SPLIT BY AUDIENCE, WHICH IS WHAT THE TWO DOCUMENTS ALREADY ARE. The config`
> `// field reference is the developer-facing authority and lives outside the scan set; it names BOTH`
> `// locations by path and holds the precedence rule. …`

**AFTER** (the correction, dated and finding-tagged; the surviving argument kept):

> `// older than this dial.`
> `//`
> `// CORRECTED 2026-08-20 (finding R2-WR-05, plan 29.1-17). Three sentences that stood here are now`
> `// false and are replaced rather than left to be believed. …`
> `//   - THE CONFIGURATION DIRECTORY IS INSIDE THE SCAN SET. … its earlier absence was an omission,`
> `//     not an audience judgment: install/install.ts:1065 recursively copies the WHOLE`
> `//     agent-factory/ tree, config/ included, to every installed user …`
> `//   - THE GATE CARRIES ONE ARGUED, DERIVED, COUNT-ASSERTED EXEMPTION. …`
> `//   - THE OBLIGATION IS NO LONGER SPLIT BY AUDIENCE. Both documents are under one scan, and the`
> `//     obligation is discharged by their two ROLES: … That second half is unchanged and is still`
> `//     the shape D-13 asks for.`

Acceptance greps, run:

```
$ grep -c 'lives outside the scan set'  scripts/model-dial-consistency.test.ts   →  0
$ grep -c 'admits no exemption list'    scripts/model-dial-consistency.test.ts   →  0
```

The block's kept half — that the packaging authority names the location a user configures and points
at the rule's home — survives, with an added sentence stating that the D-08.1 absence assertion below
is unaffected because the exemption is scoped to the configuration directory and the packaging
authority is not in it.

### Record 2 — foundation-guards set-literal inventory, row 7

**BEFORE:**

```
//    7  SCAN                     check-kit-refs.ts               PARTIALLY derived — the file entry
//                                                                becomes a directory entry; the
//                                                                deliberately-omitted directories
//                                                                stay omitted. Plan 27-04.
```

**AFTER (excerpt):**

```
//    7  SCAN                     check-kit-refs.ts               PARTIALLY derived — the file entry
//                                                                becomes a directory entry.
//                                                                Plan 27-04. AMENDED in plan
//                                                                29.1-17 (finding R2-WR-05): this
//                                                                row used to end "the
//                                                                deliberately-omitted directories
//                                                                stay omitted", which is now one
//                                                                member less true.
//                                                                agent-factory/config JOINED the
//                                                                set … It carries ONE exemption …
//                                                                counted two-sided (2 hits, 1 file)
//                                                                and published in the verdict.
//                                                                Every REMAINING omission (seed/,
//                                                                examples/, install/, docs/,
//                                                                .planning/, the root documents) is
//                                                                unchanged.
```

The superseded claim is quoted inside the correction rather than deleted, per this repository's
"record the false text you are correcting inside the correction" pattern (29.1-13). Committed twin
rebuilt in the same commit; `relativeToRoot`, `ROOT` and `ROOT_PATH_BOUNDARY` were not touched.

### Record 3 — `29.1-11-SUMMARY.md`, ANNOTATED not rewritten

The original key decision 1 is **still present in full**, unchanged, byte for byte:

> `- "THE PLAN'S CENTRAL WR-05 MUST-HAVE IS NOT MET AS WRITTEN, AND THE REASON IS ITSELF AN`
> `  INVARIANT. … That gate is twenty-two phases older than this dial and carries no exemption list.`
> `  The obligation was split by AUDIENCE instead — the config field reference (outside the scan set)`
> `  names both paths and holds the precedence rule; …"`

Immediately beneath it, a new dated entry recording the four required facts:

> `- "CORRECTION 2026-08-20 (finding R2-WR-05, implemented by plan 29.1-17) — the key decision ABOVE`
> `  is left standing in full and is superseded on its premise, not on its facts about the two`
> `  documents. Four things are now recorded. (1) install/install.ts:1065 is cpSync(join(GRUGOPS_SRC,`
> `  "agent-factory"), tmp, { recursive: true }) inside copyKit … (2) 'outside the check-kit-refs`
> `  SCAN set' and 'not shipped kit prose' are DIFFERENT FACTS, and the rationale above used the`
> `  first to establish the second … (3) D6 was put to the user in round 3 and the USER RULED: add`
> `  the directory to the scan, with an explicit, argued exemption … (4) Plan 29.1-17 implemented`
> `  that ruling — SCAN gained exactly one entry … The patterns-established entry below naming the`
> `  audience split is superseded by the same ruling and is likewise left standing rather than`
> `  rewritten."`

A second annotation, a blockquote, sits beneath the SUMMARY's **Deviations §1** — the prose statement
of the same decision, where options 2 and 3 were weighed — recording that option 2 was declined
partly on the refuted premise and that the user's D6 ruling supersedes option 3. The original
deviation, including its three weighed options, stands in full above it. **Nothing was rewritten.**

---

## The round's disposition table — derived, counted, nothing dropped

**How the item list was derived, not typed.** `grep -oE '^### (R2-[A-Z]+-[0-9]+)'
29.1-REVIEW-round2.md | sort -u` → **12** findings. `human_verification` entries in
`29.1-VERIFICATION-round2.md` → **3**. Each round-3 plan's declared findings were derived the same
way (`grep -ohE 'R2-(CR|WR|IN)-[0-9]+' 29.1-NN-PLAN.md | sort -u`), and their union is exactly the
twelve, with each finding declared by exactly one plan.

**The count, stated with its ambiguity rather than as a clean number.** The plan asserts fifteen.
That number is right at ENTRY granularity — 12 findings + 3 `human_verification` entries — but the
third `human_verification` entry names TWO items (MODEL-07/boundary and MODEL-07/precision), and D6
is one of the three entries. At ITEM granularity the round carries **sixteen** items. Both numbers
are reported; the table below has sixteen rows. Reporting only "fifteen" would have been the
double-counting trap this project has already paid for once (memory: backstop-marker conflation).

| # | Item | Closed by | Evidence, or reason carried |
|---|------|-----------|------------------------------|
| 1 | R2-CR-01 (`relativeToRoot` corrupts rendered paths under a relative `CHECK_ROOT`) | **29.1-12** | `ROOT` resolved at its declaration and matched on a `ROOT_PATH_BOUNDARY` lookahead; `diff <(guards) <(CHECK_ROOT=. guards)` prints nothing — re-verified on THIS tree at HEAD |
| 2 | R2-WR-01 (`overrides` nullish coercion / plain-object `TypeError`) | **29.1-15** | Floor 0b validates the `overrides` argument's SHAPE before it defaults, refusing a present-but-non-Map value by name; both failure modes reproduced against the committed `.js` first |
| 3 | R2-WR-02 (CI-wiring pin slices to EOF and matches inside comments) | **29.1-14** | `ubuntuBlock` right-bounded at the next step marker; `(r-bound)` asserts no second marker in the slice; `(r-commands)` decides membership by exact line equality over commands, not substring over prose; `(r-class)` pins the class two-sided at 2 |
| 4 | R2-WR-03 (recorded stray-pin measurement false — the packaging templates ARE admitted) | **29.1-13** | Case `(s)` pins the MEASURED mechanism (both templates admit on `kind`/`tier` and are skipped by the no-model-key arm); the false text is recorded inside its own correction |
| 5 | R2-WR-04 (authority restates the rule in the sentence saying it does not) | **29.1-16** | Both clauses and the self-assessing sentence deleted; `RETIRED_PRECEDENCE_PHRASINGS` pins them absent; `grep -c` → 0 on all three |
| 6 | **R2-WR-05** (split-by-audience rests on a premise the installer contradicts) | **29.1-17 (this plan)** | `agent-factory/config` joined SCAN; exemption derived, counted two-sided, published in the verdict; five mutation transcripts above; three records corrected |
| 7 | R2-WR-06 (`MODELS_KEYS` closes presence, nothing closes consumption) | **29.1-15** | Every `MODELS_KEYS` member driven through the reader by a probe table and required to MOVE the answer; probe-table coverage asserted in both directions before the loop |
| 8 | R2-IN-01 (case `(m)`'s advisory probe forbids an identifier, not the channel) | **29.1-13** | Case `(m-channel)` derives the guard body's advisory-CHANNEL write sites from BOTH the `.ts` and the committed `.js`, asserts exactly one and that it is the section header |
| 9 | R2-IN-02 (`roleCorpusCardinalityRefusal` has no production consumer) | **29.1-15** | The symbol is DELETED, not given a consumer; its argument relocated into `tieredCorpusRefusals`'s docstring. Residual (the name is absent from source) logged in `WINDOWS.md` #75 |
| 10 | R2-IN-03 (two freshness cases assert bare numbers as substrings) | **29.1-16** | The cases assert the gate's own refusal SENTENCE with its numbers still derived at run time |
| 11 | R2-IN-04 (`resolvedAssignmentsIn` validates alias type, not legality) | **29.1-15** | `readAssignmentPayload` enforces the `[<alias>,…]` grammar its refusal quotes — the CODE moved to the MESSAGE, with the rejected alternative recorded beside the check |
| 12 | R2-IN-05 (`git diff --exit-code` cannot see an untracked adapter) | **29.1-14** | A two-line report-and-assert pair in the ubuntu gate block that sees an untracked adapter and prints the offending entries; `(r-commands)` asserts its position |
| 13 | **D6** — the WR-05 disposition (human item) | **29.1-17 (this plan)** | The USER RULED in round 3: add the directory to the scan with an argued exemption. Implemented as ruled; not relitigated |
| 14 | **D5** — MODEL-06 vendor-citation fidelity | **CARRIED** | Not closable by tooling: whether the four vendor pages still say what `29.1-RESEARCH.md` quotes is not a predicate any grep here can decide. Carried verbatim in plan 29.1-16's frontmatter. **Adjudicator: the human, at phase verification.** |
| 15 | **MODEL-07/boundary** | **CARRIED** | `verification: backstop`. The boundary between a cost claim and a neutral mechanism description is not a decidable predicate. Mechanical half re-run this session: `git diff --name-only -- scripts/measure-cost.ts scripts/measure-cost.js` empty across the whole round. **Adjudicator: the human.** |
| 16 | **MODEL-07/precision** | **CARRIED** | Same `verification: backstop` premise, same adjudicator. Ruled as CONTENT with a disclosed backstop under D-59/D-16, not as a decidable mechanism. |

**Twelve findings: all twelve closed, each by exactly one plan, with no item appearing in neither
column.** **Four human items: one closed (D6), three carried with their adjudicator named.**

---

## The fourteen-command phase gate block, in CI's order

`npm run freshness` ran **first**, before any build, as `29.1-VALIDATION.md` requires.

| # | Command | Exit |
|---|---------|------|
| 1 | `npm run freshness` | **0** — `All build outputs fresh: 49 committed .js file(s) match a rebuild of their sources.` |
| 2 | `npm run freshness:catalog` | **0** — `Catalog fresh: docs/catalog/README.md matches a fresh regeneration.` |
| 3 | `npm run freshness:context` | **0** — `no .grugops/context/ tree exists yet — nothing committed to drift (vacuous pass)` |
| 4 | `npm run freshness:adapters` | **0** — `17 adapter(s) compared in .claude/agents, 0 byte difference(s), directory listings set-equal.` |
| 5 | `npm run freshness:skill-twins` | **0** |
| 6 | `node scripts/check-foundation-guards.js` | **0** |
| 7 | `node scripts/check-kit-refs.js` | **0** |
| 8 | `node scripts/check-public-docs-vocabulary.js` | **0** |
| 9 | `node scripts/check-audit-register.js` | **0** |
| 10 | `node scripts/check-claim-anchors.js` | **0** |
| 11 | `node scripts/check-banned-claims.js` | **0** |
| 12 | `node scripts/check-imperative-lexicon.js` | **0** |
| 13 | `node scripts/check-diff-disposition.js` | **0** |
| 14 | `node scripts/check-nul-bytes.js` | **0** |

Nothing was marked `UNKNOWN - verify`; every command ran and every command exited 0.

### The two round-specific end-to-end checks

```
$ diff <(node scripts/check-foundation-guards.js 2>&1) <(CHECK_ROOT=. node scripts/check-foundation-guards.js 2>&1)
(prints nothing)
```

**The 29.1-12 relative-root byte identity survives every later plan, including this one's edit to the
same file.**

```
$ npx vitest run --exclude '**/scripts/e2e/**'
 Test Files  55 passed (55)
      Tests  2355 passed | 2 skipped (2357)
   Duration  158.16s   → exit 0
```

`npm test` was never run.

### Suite totals reconciled across plans 29.1-12 … 29.1-17

| Point | Files | Passed | Skipped |
|---|---|---|---|
| Pre-round-3 baseline (`29.1-VALIDATION.md`, measured) | 52 | 2140 | 2 |
| After wave 5 (plans 12-16 landed on `main`) | 55 | 2347 | 2 |
| After this plan | 55 | **2355** | 2 |

**+8 in this plan, and the eight are named:** `scripts/check-kit-refs.test.ts` went from 13 cases to
21 — the derivation's cardinality case, the derivation's named-throw case, one green, four red, one
vacuity floor. No file was added or removed by this plan; no case was deleted anywhere in it. The
two skips are the pre-existing environment skips, unchanged all round.

### Adapters and the prohibited set

```
$ node scripts/generate-role-adapters.js && git status --porcelain -- .claude/agents/
(prints nothing)
$ grep -h '^model:' .claude/agents/*.md | sort | uniq -c
  17 model: inherit
$ git diff --name-only -- install/ package.json scripts/measure-cost.ts scripts/measure-cost.js agent-factory/config/
(prints nothing)
$ npm run build && git diff --exit-code --name-only -- '*.js'
(prints nothing; exit 0)
$ npm run typecheck
exit 0
```

`agent-factory/config/factory.config.md` and `agent-factory/config/factory.config.json` are
**byte-unchanged** by this plan, as the prohibitions require — which is the point: the gate was moved
to the document, not the document to the gate.

---

## Deviations from Plan

### 1. [Rule 1 — acceptance criterion cannot be satisfied honestly] `grep -c 'const SCAN'` returns 1, not 0

- **Found during:** Task 2, running the acceptance greps.
- **Criterion:** *"`grep -c 'const SCAN' scripts/check-kit-refs.test.ts` returns 0 — the second copy
  is deleted, not commented out."*
- **Measured:** **1**, not 0. The surviving hit is
  `const GATE_SCAN_DECL_OPEN = "const SCAN = [";` — the extractor's LOCATOR STRING, which must spell
  the gate's declaration verbatim in order to find it.
- **What was done.** Two of the three original hits were removed honestly: the bound constants were
  renamed `SCAN_DECL_OPEN`/`SCAN_DECL_CLOSE` → `GATE_SCAN_DECL_OPEN`/`GATE_SCAN_DECL_CLOSE`, which is
  also the more accurate name (they describe the GATE's declaration). The third hit is irreducible.
- **What was NOT done, and why.** Splitting the locator literal (`"const SCAN" + " = ["`) would make
  the grep return 0 while the file still carries the string. That is hiding a string from a check —
  the move plan 29.1-11 rejected in its deviation 3, for the same reason, in this same round.
- **The honest predicates, recorded in the file and asserted here:**
  ```
  $ grep -cE '^const SCAN = ' scripts/check-kit-refs.test.ts        →  0   (declares no scan set of its own)
  $ grep -c '"agent-factory/checklists"' scripts/check-kit-refs.test.ts →  0   (no member spelling survives)
  ```
  The intent of the criterion — no second copy of the scan set — **is met**, and is provable. The
  criterion **as written is NOT satisfied** and this SUMMARY does not claim it is.
- **Commit:** `22749a1`

### 2. [Rule 1 — measured premise more precise than the plan's] "two mentions" is a LINE count, and the file carries three OCCURRENCES

- **Found during:** Task 2, the green case's first run, which failed on its own premise:
  `AssertionError: PREMISE: the reference must carry exactly the two mentions the exemption is
  declared for: expected 3 to be 2`.
- **Issue:** the plan's must-haves and objective say "the two mentions". Line 138 of the field
  reference names `agent-factory/config/factory.config.json` **twice on one line**, so the file
  carries **three substring occurrences**. `grepSubstring` emits one hit per LINE, so the gate sees
  **two hits** — the plan's number is right, at line granularity, and its unit was unstated.
- **Fix:** the premise helper counts LINES (`linesContaining`), the unit the gate counts, with the
  measurement and its reasoning recorded in the helper's docstring. A premise written in occurrences
  reds for a reason a reader of the failure could not attribute to the plant.
- **Commit:** `22749a1`

### 3. [Record, not fix] The disposition table's asserted count of fifteen is granularity-dependent

- The plan requires *"twelve findings plus three carried items, with the number stated"*. Derived
  from the two round-2 documents, the number **fifteen is right at ENTRY granularity** (12 findings +
  3 `human_verification` entries) and **wrong at ITEM granularity**: the third `human_verification`
  entry names two items, and D6 is one of the three entries — so the round carries **sixteen items**,
  of which this plan closes one (D6). Both numbers are reported above and the table has sixteen rows.
  Recorded rather than trimmed to make the plan's number come out.

### 4. [Record, not fix] A FOURTH record adjacent to this change makes a claim worth a later look

- `scripts/check-public-docs-vocabulary.ts:32` reads *"What this gate does NOT do is widen
  check-kit-refs's SCAN — that set is byte-unchanged"*. Read in context it is scoped to what THAT
  gate's own Phase-27 change did, and it remains true of that gate. Read out of context a reader
  could take it as a standing claim about `SCAN`, which is now one member wider.
- **Not edited.** The file is outside this plan's declared `<files>`, and the plan pins its scope to
  three named records. Raised here for verification rather than fixed silently.

### 5. [Rule 2 — a record would have asserted a verdict nobody reached] The MODEL-05 row was flipped to `Complete` by the requirements verb, and the flip was REVERTED

- **Found during:** the close-out `requirements.mark-complete` step. The shared-ID gate released all
  four declared IDs (`{"ready":["MODEL-01","MODEL-02","MODEL-05","MODEL-07"],"blocked":[],"total":4}`)
  because this is the last plan declaring them. Applying it produced
  `"marked_complete": ["MODEL-05"]` — MODEL-01/02/07 were already `Complete`.
- **Issue:** **MODEL-05 is the requirement round-2 verification REGRESSED** from `Complete` back to
  `Gaps Found`, on R2-CR-01. That finding was closed by plan **29.1-12**, and no verification round
  has run since. Flipping the row to `Complete` here would publish a passing verdict that no
  verification produced — this repository's `no fabrication / never fake a passing gate` rule, and
  the standing round rule three prior SUMMARYs in this phase record ("no plan flips a requirement
  row; the verdict belongs to the verifier").
- **Fix:** the MODEL-05 checkbox and its traceability row were restored to `[ ]` / `Gaps Found`;
  `git diff -- .planning/REQUIREMENTS.md` is now **empty**. MODEL-01, MODEL-02 and MODEL-07 were
  already `Complete` before this plan and were not touched by it either way.
- **Consequence, stated plainly:** the success criterion *"requirement IDs now fully covered … are
  marked via the requirements verb"* is satisfied for the shared-ID GATE (it ran, it released all
  four) and deliberately **not** applied to the one ID whose status is a verification verdict.
  Re-verification is the one step that should move MODEL-05, and it is a one-line change when it
  does. This SUMMARY's `requirements-completed` frontmatter carries the plan's declared array
  verbatim, as the workflow requires — that is the plan's declaration, not a status claim.

### 6. [Record, not fix] The suite was RED between the Task 1 and Task 2 commits

- `ba9f3c4` widened the gate; `22749a1` derived the mirror. In between, four pre-existing cases in
  `check-kit-refs.test.ts` failed, because the file's hand-copied scan set was one member behind the
  set the gate walked and every mirror it built was missing the directory the gate then judged.
- **That is precisely the drift this plan exists to delete, observed rather than argued.** It is
  recorded rather than hidden by squashing the two commits into one. Every commit *at HEAD* is green.

**Total deviations:** 3 corrections applied (2 measured, 1 reverted write), 3 recorded-not-fixed.
**Impact:** one stated acceptance criterion is NOT satisfied as written (deviation 1) and is reported
as such with the honest substitute predicate; one plan number is refined by measurement (deviation
2); one plan count is reported at both granularities (deviation 3); one requirement-status write was
reverted because it would have published an unverified verdict (deviation 5). No deviation weakens
the exemption, the cardinality assertions or the vacuity floor.

---

## Known Stubs

None. No hardcoded empty value, placeholder string, `TODO` or `FIXME` was introduced by this plan.

## Threat Flags

None. This plan widened a read-only gate's scan set by one directory, added a read-only predicate
over its own hits, edited three comment/record surfaces and one test file. It introduced no endpoint,
auth path, file-access pattern or schema change, and installed no package.

**Register dispositions:** **T-29.1-56** mitigated (the configuration directory is inside SCAN; every
mention in it is adjudicated by a predicate). **T-29.1-57** mitigated (both cardinalities asserted
two-sided in the gate and named in the failure text; four red transcripts prove the exemption is
scoped to one file, one directory and existing paths only). **T-29.1-58** mitigated (the pass line
names the exempt count and the exempting file, asserted by the green case, not by the exit code).
**T-29.1-59** mitigated (the test's copy deleted and derived, bounded at both ends, cardinality
asserted and proven RED on a removed member). **T-29.1-60** mitigated (three records corrected in the
same commit as the change that falsifies them, with the measured `copyKit` line cited and
`29.1-11-SUMMARY.md` annotated rather than rewritten). **T-29.1-61** mitigated (the disposition
table's item list derived by grep from both round-2 documents; the count reported at both
granularities so the plan's own number could not quietly absorb an item). **T-29.1-05** mitigated
(`npm run build` + empty `*.js` diff after every task; `npm run freshness` first in the gate block,
49/49 fresh). **T-29.1-SC** accepted and asserted by absence — `git diff --name-only -- package.json`
is empty.

## What Verification Should Re-check

- **The exemption is a genuine weakening of a twenty-two-phase-old absolute, and it is now the thing
  most worth adversarially probing.** This project's own terminal lesson is that a green suite is not
  proof for a safety invariant. Six cases and five transcripts is what this plan could produce; an
  independent red-team of the predicate is what the lesson asks for. Specific surfaces to attack: the
  `CONFIG_NAMED_PATH` character class (`[A-Za-z0-9._-]+`) — a filename outside it truncates the
  extracted basename, and a truncated basename that happens to exist would be exempted; and
  `HIT_SHAPE`'s non-greedy path capture against a repo-relative path containing `:` followed by
  digits.
- **Acceptance criterion `grep -c 'const SCAN' → 0` is NOT satisfied** (deviation 1). The substitute
  predicates are anchored and were run; the criterion as written is not claimed.
- **The fourth stale record** at `scripts/check-public-docs-vocabulary.ts:32` (deviation 4) was
  deliberately not edited. Someone should rule on whether it needs the same treatment.
- **D5, MODEL-07/boundary and MODEL-07/precision remain CARRIED, not closed.** This plan reconciles
  them and names their adjudicator; it does not adjudicate them.
- **No requirement checkbox was flipped by hand and Phase 29.1 was NOT marked complete.** The verdict
  belongs to phase verification. Requirement completion below is the tool's shared-ID gate releasing
  IDs whose every declaring plan now has a SUMMARY — not a claim that the phase passed.
- **The round is not clean, and this SUMMARY does not report it as clean.** Three of the five prior
  round-3 plans recorded an acceptance criterion that did not hold when measured (29.1-14 §1,
  29.1-15 §1, 29.1-16 §1), each with a deviation rather than a claimed pass, and this plan adds a
  fourth (deviation 1). Four of six round-3 plans hit a false plan premise. That pattern is the
  round's most durable finding.

## Self-Check: PASSED

All 7 claimed files exist on disk (1 created + 6 modified) — verified with `[ -f ]`. All 3 claimed
commits resolve in `git log`: `ba9f3c4`, `22749a1`, `4a68b19`. Every transcript, table cell and exit
code above was captured from a command run in this session; nothing claimed here is unbacked, and
nothing that did not hold is reported as holding.

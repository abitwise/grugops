---
phase: 29.1-per-role-model-assignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/adapter-byte-baseline.test.ts
  - scripts/model-tiers.ts
  - scripts/model-tiers.js
  - scripts/model-tiers.test.ts
  - scripts/generate-role-adapters.ts
  - scripts/generate-role-adapters.js
  - scripts/adapters-freshness.ts
  - scripts/adapters-freshness.js
autonomous: true
requirements: [MODEL-01, MODEL-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A permanent case pins the pre-phase SHA 6f8411e, derives the 17 adapter filenames from `git ls-tree -r 6f8411e:.claude/agents` (never a hand-list), throws when that derivation is empty, and byte-compares `git show 6f8411e:<path>` against the working file for each — and it is committed and GREEN before any generator change lands. (MODEL-01)"
    - "MODEL-01/adjacency: the resolved zero-config alias and the former hard-coded literal produce the SAME BYTES, not merely the same meaning — `npm run generate:adapters` followed by `git diff --exit-code -- .claude/agents/` is empty, with no trailing space, no normalization and no `String()` of a boxed value."
    - "MODEL-01/ordering: adding the resolver introduces no new ordering authority — `roleFiles.slice().sort()` at scripts/generate-role-adapters.ts:~215 remains the only sort before emit, the resolved map is consumed by stem lookup rather than by iteration, and two consecutive generator runs over the same tree write byte-identical files."
    - "MODEL-02/unclassified: each of the 17 emitted adapters carries exactly ONE `model:` line, positioned after the `tools:` line and immediately before the closing frontmatter delimiter — the same slot the literal occupied at scripts/generate-role-adapters.ts:410."
    - "`scripts/model-tiers.js` is present in `scripts/adapters-freshness.ts`'s hand-written twin list the moment `generate-role-adapters.ts` imports it, so the mirrored generator resolves its import closure and the gate cannot fail for the wrong reason. (Shared Pattern 3)"
    - "`npm run freshness` exits 0 with all four touched `.ts`/`.js` pairs committed in the same commit as their source (D-57 build parity)."
  artifacts:
    - scripts/adapter-byte-baseline.test.ts
    - scripts/model-tiers.ts
    - scripts/model-tiers.js
    - scripts/model-tiers.test.ts
  key_links:
    - "`generate-role-adapters.ts` imports `resolveModels` from `scripts/model-tiers.js` — the import that forces the twin-list edit in `adapters-freshness.ts`."
    - "`model-tiers.ts` imports `listRoles`/`ROLE_COUNT` from `scripts/kit-model.js` — the role-set authority; no private readdir anywhere."
    - "The pre-phase SHA 6f8411e must stay reachable for the baseline case to run; that residual is recorded in the case's header (the plan-29-39 precedent)."
  prohibitions:
    - "No change to `install/install.ts` or `install/kit-source.ts`, and no install-side test case — the delivery path is Phase 29.2 (D-17)."
    - "No second frontmatter parser and no regex over `^model:` anywhere."
    - "No hand-listed 17-entry expectation in any source or test file — every set is derived and its count asserted."
    - "The generator gains no output-path flag; `OUT_DIR` stays a fixed literal joined to the script's parent (T-27-28)."
    - "No prose added to `specialistBody()` or `coordinatorBody()` — `grugops-orchestrator.md` is 2970 B against a 3072 B WARN ceiling."
    - "No unmeasured cost or limit-savings sentence in any file this plan touches."
    - "No caveman voice in any tier or model prose, comment or finding — a model tier is a money topic (CLAUDE.md voice discipline)."
---

<objective>
Capture the one independent opinion that becomes unobtainable after the first generator
commit, then wire the zero-config resolution end to end and prove the emitted bytes did
not move.

This plan is the phase's tracer slice. It touches every layer the phase will modify — the
role-set authority, a new pure resolution module, the generator's emit line, the committed
adapter bytes, and the freshness gate that judges them — along ONE path: the zero-config
path, where the resolved value is `inherit` for all 17 roles and the output must therefore
be byte-identical to today's.

Purpose: MODEL-01 says "byte-identical", not "still works". The gate that would normally
decide that (`npm run freshness:adapters`) regenerates and byte-compares BOTH SIDES from
the same generator, so it proves determinism rather than identity-with-yesterday. The only
opinion that cannot move with the generator is a baseline frozen at a commit that predates
it. Task 1 captures it; after Task 3 lands, it cannot be captured at all.

Scope note carried from D-17: D-01, D-02 and D-03 are OUT OF SCOPE for this phase and moved
to Phase 29.2. No file under `install/` is read for modification, changed, or tested here.
The disclosed consequence — a per-repo `models` block is inert for an installed target until
29.2 lands — is stated in the kit by plan 29.1-05, not left to be discovered.

Output: a permanent commit-pinned byte-baseline case; `scripts/model-tiers.ts` with its
committed `.js`; a generator that emits a resolved value whose zero-config answer is the
same seven bytes it emitted before; and a freshness gate whose twin list tracks the
generator's new import closure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-per-role-model-assignment/29.1-CONTEXT.md
@.planning/phases/29.1-per-role-model-assignment/29.1-RESEARCH.md
@.planning/phases/29.1-per-role-model-assignment/29.1-PATTERNS.md
@.planning/phases/29.1-per-role-model-assignment/29.1-VALIDATION.md
</context>

## Artifacts this phase produces

The convergence source-grounding pass reads this section to exclude newly-created symbols
from drift verification. Every symbol below is NEW in phase 29.1 and has no prior referent.

**New files**
- `scripts/model-tiers.ts` / `scripts/model-tiers.js` / `scripts/model-tiers.test.ts`
- `scripts/adapter-byte-baseline.test.ts`
- `scripts/model-dial-consistency.test.ts` (plan 29.1-05)

**New exported symbols in `scripts/model-tiers.ts`**
- type `ModelAlias` — the closed union `"inherit" | "opus" | "sonnet" | "haiku"`
- const `MODEL_ALIASES` — the four aliases as a readonly tuple, the exact-string-equality allow-list
- interface `RoleTier` — `{ readonly stem, readonly alias, readonly rationale }`
- const `TIERED` — the shipped preset table (plan 29.1-02)
- const `MODEL_TIERS_COUNT` — the preset table's two-sided pinned cardinality (plan 29.1-02)
- const `PRESET_NAMES` — the closed preset name set `["none", "tiered"]` (plan 29.1-02)
- type `ModelResolution` — the discriminated result `{ok:true, value} | {ok:false, reason}`
- function `resolveModels(...)` — the pure resolver
- function `readModelsConfig(...)` — the two-location config read (plan 29.1-02)
- function `resolvedPresetName(...)` — the name the generator prints for the D-04 pin (plan 29.1-03)

**New symbols elsewhere**
- `Adapter.model` — new readonly field on the generator's internal `Adapter` interface
- `guardModelAssignment()` / the guard name `guard_model_assignment` (plan 29.1-04)
- claim-registry row id `C-28-047` (plan 29.1-06)

**New CLI flags / env vars:** NONE. This phase introduces no flag and no environment
variable. The D-04 zero-config pin is an observable stdout property, not an env var
(plan 29.1-03).

**Files whose bytes must NOT move:** all 17 `.claude/agents/grugops-*.md`,
`agent-factory/config/factory.config.json`, `agent-factory/seed/.grugops/factory.config.json`,
`scripts/measure-cost.ts`, everything under `install/`.

---

## Multi-source coverage audit (phase-wide)

Four source types audited. Every item is COVERED by a plan; there are no MISSING items and
no silent deferrals.

| Source | Item | Covered by |
|---|---|---|
| GOAL | "spend the model budget where judgment lives — set once on the config dial" | 29.1-02 (dial + preset) |
| GOAL | "emitted into every generated adapter" | 29.1-01 T3, 29.1-03 T1 |
| GOAL | "zero-config behaviour byte-identical to today's" | 29.1-01 T1/T3, 29.1-03 T2 |
| REQ | MODEL-01 | 29.1-01, 29.1-03 |
| REQ | MODEL-02 | 29.1-01, 29.1-05 |
| REQ | MODEL-03 | 29.1-02 |
| REQ | MODEL-04 | 29.1-02, 29.1-03 |
| REQ | MODEL-05 | 29.1-02, 29.1-04 |
| REQ | MODEL-06 | 29.1-05 |
| REQ | MODEL-07 | 29.1-06 |
| RESEARCH | Pattern 1 — derive the set, assert the count, rationale required by the type | 29.1-02 T2 |
| RESEARCH | Pattern 2 — validate BEFORE `adapters.map()`, never inside `render()` | 29.1-03 T1 |
| RESEARCH | Pattern 3 — the mirror-spawn's four properties (two roots, twin list, env strip, fail-closed) | 29.1-01 T3, 29.1-03 T2 |
| RESEARCH | Pattern 4 — guard banner, vacuity floor, per-member finding, derivation summary | 29.1-04 T1 |
| RESEARCH | Pitfall 1 — byte-identity not equivalence | 29.1-01 T1/T3 |
| RESEARCH | Pitfall 2 — `readGovernanceConfig` fails OPEN; do not copy the verdict | 29.1-02 T3 |
| RESEARCH | Pitfall 3 — the D-04 pin is an accident today; make it observable | 29.1-03 T2 |
| RESEARCH | Pitfall 4 — adapter size headroom | prohibition in every plan |
| RESEARCH | Pitfall 7 — build parity, freshness reads HEAD | every plan's verify |
| RESEARCH | Pitfall 8 — never `npm test` | every plan's verify |
| RESEARCH | Assumption A1 — the alias vocabulary is LOW-confidence, not re-verified | 29.1-02 T2 (header), 29.1-05 T1 (prose) |
| RESEARCH | Assumption A4 — the "nine keys" counted claim must move with its enumeration if touched | 29.1-05 T2 |
| RESEARCH | Security V5 / V12 | `<threat_model>` in every plan |
| CONTEXT | D-01, D-02, D-03 | OUT OF SCOPE — moved to Phase 29.2 by D-17; no install-side file is touched. Not a gap. |
| CONTEXT | D-04 | 29.1-03 T2 (observable pin), 29.1-05 T3 (no block in either JSON) |
| CONTEXT | D-05 | 29.1-02 T1 (one-way checkpoint), T3 (key shape + cardinality) |
| CONTEXT | D-06 | 29.1-02 T3 |
| CONTEXT | D-07 | 29.1-02 T2 |
| CONTEXT | D-08 | 29.1-02 T2 |
| CONTEXT | D-09 | 29.1-02 T2 |
| CONTEXT | D-10 | 29.1-02 T2 |
| CONTEXT | D-11 | 29.1-02 T3, 29.1-03 T1 |
| CONTEXT | D-12 | 29.1-04 T1 (independence argument; naming superseded by D-15) |
| CONTEXT | D-13 | 29.1-05 T1, T2 |
| CONTEXT | D-14 | 29.1-06 T1 |
| CONTEXT | D-15 | 29.1-04 T1 |
| CONTEXT | D-16 | 29.1-06 T1 |
| CONTEXT | D-17 | 29.1-05 T2 (the disclosed limitation, in the kit), prohibition in every plan |
| CONTEXT | Deferred: second preset, authed cost capture, reasoning-effort dials, tier-into-measure-cost wiring | EXCLUDED — Deferred Ideas, not gaps |

---

<tasks>

<task type="tracer">
  <name>Task 1: Freeze the pre-phase adapter byte baseline at SHA 6f8411e — the only opinion that cannot move with the generator</name>
  <files>scripts/adapter-byte-baseline.test.ts</files>
  <read_first>
    - `scripts/check-foundation-guards.test.ts` lines 8480–8530 — `censusAtCommit`, the commit-pinned derivation this case copies: `git ls-tree --name-only <rev>:<dir>` to derive the set, a non-empty premise throw, then `git show <rev>:<path>` per member.
    - `scripts/check-foundation-guards.test.ts` lines 8780–8870 — `PLAN_29_39_TRIPWIRE`, the shape of a `const` that pins a SHA with its rationale and its recorded residual in a block comment.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-VALIDATION.md` — the MODEL-01 row: the mechanical decider, why it is not enough, and the two discrimination mutations this case must go RED on.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-PATTERNS.md` §"MODEL-01's pre-phase byte baseline" — the four things to copy exactly.
    - `scripts/adapters-freshness.ts` lines 200–275 — how the existing gate names an adapter set and reports byte differences, so the new case's finding wording is a sibling rather than a rival.
  </read_first>
  <action>
Author `scripts/adapter-byte-baseline.test.ts` as a permanent vitest case file.

Pin the pre-phase commit as a `const` named `PRE_PHASE_ADAPTER_BASELINE` holding the SHA
`6f8411e` together with a block comment stating, in clear voice: the date the pin was taken
(2026-08-19), that this is the tree the phase started from, that the pin exists because
`npm run freshness:adapters` regenerates and byte-compares both sides from the same
generator and therefore proves determinism rather than identity-with-yesterday, and the
recorded residual — the case depends on that commit remaining reachable in this repository,
exactly the residual plan 29-39 recorded for `PLAN_29_39_TRIPWIRE`.

Derive the adapter set, never list it: run `git ls-tree --name-only <sha>:.claude/agents`
with `cwd` at the repo root and `encoding: "utf8"`, throw when the spawn status is non-zero
naming the rev, filter to names ending `.md`, and sort. Assert the derived set is non-empty
with a throw whose message names the rev and states that the premise is empty — the vacuity
floor. Then assert the derived count against `ROLE_COUNT` imported from `scripts/kit-model.js`,
so a silently SHORT derivation is caught as well as an empty one: the element count is
derived independently of the loop that consumes it.

For each derived name, read the baseline bytes with `git show <sha>:.claude/agents/<name>`
using `maxBuffer` at least 64 MiB, throw when the status is non-zero naming the object path,
and compare against `readFileSync(join(ROOT, ".claude/agents", name))` as Buffers with
`Buffer.equals`. On inequality the failure message names the adapter filename, the baseline
byte length and the working byte length — never a bare boolean.

Add a second case asserting the working directory holds exactly the derived set and no
extra adapter file, so an ADDED adapter is a named failure rather than an unnoticed one.

Do not touch `scripts/model-tiers.ts` or `scripts/generate-role-adapters.ts` in this task.
This case must be committed and green against an UNCHANGED tree first: that green run is
the assertion of the case's own premise, and it is unobtainable once Task 3 lands.
  </action>
  <verify>
    <automated>npx vitest run scripts/adapter-byte-baseline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/adapter-byte-baseline.test.ts` exits 0 against the unchanged tree, reporting at least 2 passing cases.
    - The file contains the literal SHA `6f8411e` exactly once, inside the pinned `const`, and contains no adapter filename as a source literal — `node -e` reading the file and counting occurrences of `grugops-` returns 0.
    - Discrimination proof, run and recorded in the SUMMARY, then reverted: hand-edit one working `.claude/agents/grugops-uat-planner.md` `model:` line to `sonnet`, re-run the case, and confirm it goes RED naming `grugops-uat-planner.md` together with both byte lengths. Restore the file and confirm the case returns green.
    - Discrimination proof of the vacuity floor: temporarily point the pinned rev at a tree path with no adapters and confirm the throw names the rev and states the premise is empty rather than passing over an empty set. Revert.
    - `git status --porcelain -- .claude/agents/` is empty after both proofs.
  </acceptance_criteria>
  <done>A committed, green, commit-pinned case byte-compares all 17 pre-phase adapters against the working tree, derives its set from git rather than a literal, refuses an empty or short derivation by name, and is proven to go RED on a planted single-byte change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/model-tiers.ts — the pure zero-config resolver, with its committed .js</name>
  <files>scripts/model-tiers.ts, scripts/model-tiers.js, scripts/model-tiers.test.ts</files>
  <behavior>
    - `resolveModels` called with the 17 stems derived from `listRoles()` and no config returns `{ok:true}` whose value maps every one of the 17 stems to `"inherit"`.
    - The returned map's size equals `ROLE_COUNT`; a stem set of length 0 is refused with a named reason rather than returning an empty map.
    - A stem set whose length is not `ROLE_COUNT` is refused naming both the derived length and `ROLE_COUNT` — a SHORT set is a distinct fact from an EMPTY one and gets its own sentence.
    - Two calls with the same stem set return maps that stringify identically, so no iteration-order nondeterminism can reach an emitted file.
    - `MODEL_ALIASES` has exactly 4 members and membership is decided by exact string equality against those constants, never by a regular expression.
  </behavior>
  <read_first>
    - `scripts/kit-model.ts` lines 100–140 (`ROLE_COUNT`) and lines 700–730 (`refuseEmpty`, `listRoles`) — the role-set authority, its vacuity throw, and the `!f.startsWith("_")` exclusion that makes the answer 17 rather than 18.
    - `scripts/kit-model.ts` lines 487–525 — `PluginComponentExemption` / `PLUGIN_COMPONENT_EXEMPT` / `PLUGIN_COMPONENT_EXEMPT_COUNT`: the readonly-interface-with-a-required-reason-field shape, and the block comment that names the promote trigger and every consumer an editor must walk.
    - `scripts/kit-model.test.ts` lines 1480–1495 — the test-file conventions of this repo (import from the `.js` twin, no watch mode).
    - `scripts/generate-role-adapters.ts` lines 1–110 — the module header's three recorded decisions and the `fail` helper at 99–102, so this module's header is written in the same register.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-PATTERNS.md` §`scripts/model-tiers.ts` — Analog A, Analog B, and the coupling risk to record in the header.
  </read_first>
  <action>
Create `scripts/model-tiers.ts` with `node:fs`/`node:path`-only imports plus `listRoles` and
`ROLE_COUNT` from `./kit-model.js`. Zero runtime dependencies.

Write a module header, in clear professional voice, recording four things: (1) this module is
the single answer to "which model alias does role X get", and it exists as a sibling of
`kit-model.ts` rather than inside it so that the tier rationale prose stays out of the kit-set
authority; (2) the D-11 consumer split — the reader returns a discriminated result, the
generator turns a refusal into exit 1, every other consumer degrades to `inherit` and never to
a pinned tier; (3) the coupling risk, named so Phase 30's author finds it — Phase 30 collapses
`readGovernanceConfig` and `readGovernanceConfigResult` into one discriminated-result reader,
and this module is deliberately that same shape so it does not become a third config reader;
(4) that no measurement is taken here and no cost claim is made — a tier assignment is not
evidence of a saving.

Export, in this task, only what the zero-config path needs:
`export type ModelAlias = "inherit" | "opus" | "sonnet" | "haiku";`
`MODEL_ALIASES` as a `readonly` tuple of those four strings with a comment stating that
membership is exact string equality against these four constants and never a regular
expression, because a closed four-member allow-list is what makes a YAML-significant byte
unreachable in the emitted frontmatter; `RoleTier` as a readonly interface with `stem`,
`alias` and a REQUIRED `rationale` field, with the comment recording that the required field
is D-10's mechanism and that a missing rationale must not compile; and
`export type ModelResolution = { ok: true; value: ReadonlyMap<string, ModelAlias> } | { ok: false; reason: string }`.

Implement `resolveModels(stems: readonly string[]): ModelResolution` for the zero-config arm
only. It takes the stems the caller derived — it performs no I/O and reads no config in this
task. Order of checks: refuse an empty stem set with a reason naming that the set was empty
and that "the set was empty, therefore every member is assigned" is the vacuous pass this
floor exists to make impossible; then refuse a stem-set length other than `ROLE_COUNT` with a
reason naming both numbers and instructing the reader to walk `listRoles`, `TIERED` and
`guard_model_assignment` before changing `ROLE_COUNT`; then refuse a duplicated stem naming
the stem and its occurrence count, never last-wins. Only then build the map, assigning
`"inherit"` to every stem. Sort the stems before building so the map's insertion order is a
property of the expression rather than of the caller's argument order.

Author `scripts/model-tiers.test.ts` importing from `./model-tiers.js`, covering every bullet
in this task's behavior block plus a case asserting `MODEL_ALIASES.length` is 4 two-sided
(3 is a failure and 5 is a failure) and a case asserting that the resolver's answer for a
stem set derived at run time from `listRoles(ROOT)` has size `ROLE_COUNT`. Derive every
corpus at run time; write no adapter name and no stem as a test literal except where a case
is deliberately adversarial.

Run `npm run build` and commit `scripts/model-tiers.js` in the SAME commit as the `.ts`.
  </action>
  <verify>
    <automated>npx vitest run scripts/model-tiers.test.ts && npm run typecheck && npm run build && git diff --exit-code --name-only -- '*.js'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/model-tiers.test.ts` exits 0 with every behavior-block bullet represented by a named case.
    - `npm run typecheck` exits 0 for both targets.
    - After `npm run build`, `git diff --exit-code --name-only -- '*.js'` is empty — the committed `scripts/model-tiers.js` is a faithful build of its `.ts`.
    - A `RoleTier` object literal written without a `rationale` key fails `npm run typecheck` with a TS2741-class missing-property error naming `rationale`. Demonstrate in a scratch file, record the error text in the SUMMARY, delete the scratch file.
    - Discrimination proof: change the zero-config assignment from `"inherit"` to `"sonnet"` in a scratch build and confirm the resolver's own case goes RED naming the expected alias; revert.
    - `scripts/model-tiers.ts` contains no `RegExp` literal and no `.test(` call in the alias-membership path — confirm by reading the membership function.
  </acceptance_criteria>
  <done>`scripts/model-tiers.ts` exports the closed alias union, the required-rationale interface and a pure zero-config `resolveModels` that refuses an empty set, a short set and a duplicate stem each by its own name; its committed `.js` is a build of its `.ts`; its test file derives every corpus at run time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the resolver through the generator and prove the emitted bytes did not move</name>
  <files>scripts/generate-role-adapters.ts, scripts/generate-role-adapters.js, scripts/adapters-freshness.ts, scripts/adapters-freshness.js</files>
  <behavior>
    - With no `models` block anywhere, `node scripts/generate-role-adapters.js` writes 17 files and `git diff --exit-code -- .claude/agents/` is empty.
    - `render()` invoked for a stub adapter carrying `model: "inherit"` produces output whose `model:` line is byte-equal to the line the previous literal produced, in the same position.
    - `render()` invoked for a stub adapter carrying `model: "opus"` produces exactly `model: opus` on its own line, exactly once — proving the emit reads the field rather than a constant.
    - The mirrored generator inside `adapters-freshness.ts` resolves `model-tiers.js` and exits 0; removing that twin from the list makes the mirrored child exit non-zero and the gate refuses to report the adapters as fresh.
  </behavior>
  <read_first>
    - `scripts/generate-role-adapters.ts` in full, and in particular lines 16–110 (the header's three load-bearing decisions and the `fail` helper's `(m: string) => never` variable annotation), lines 190–215 (the `Adapter` interface and the explicit `roleFiles.slice().sort()`), lines 396–435 (`render`, the `adapters.map()` build-everything loop, and the write loop).
    - `scripts/adapters-freshness.ts` lines 40–120 (the header, `SCRIPT_ROOT`, `KIT_ROOT`) and lines 125–200 (the twin list with its recorded trade, the `childEnv` `CHECK_ROOT` strip, and the fail-closed `r.status !== 0` branch).
    - `scripts/model-tiers.ts` as written in Task 2 — the exported `ModelAlias`, `ModelResolution` and `resolveModels` signatures.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-PATTERNS.md` §`scripts/generate-role-adapters.ts` and §`scripts/adapters-freshness.ts`.
  </read_first>
  <action>
In `scripts/generate-role-adapters.ts`: add `readonly model: ModelAlias` to the `Adapter`
interface, importing `ModelAlias` and `resolveModels` from `./model-tiers.js`. Call
`resolveModels` with the sorted role stems ONCE, at module top level, BEFORE the
`adapters.map()` build-everything loop — never inside `render()`. `render()` runs inside that
loop, so a refusal raised there would leave a partial adapter directory and break T-27-32.
On `ok:false` call the existing `fail` helper with the refusal reason; `fail`'s variable-level
`(m: string) => never` annotation is what gives the caller control-flow narrowing, so the
refusal must go through it rather than through a bare `process.exit`.

Populate each `Adapter`'s `model` field from the resolved map by stem lookup, and replace the
literal push at line 410 with a template push of the field's value. Nothing else in `render()`
changes: the line keeps its position after the `tools:` push and immediately before the
closing delimiter push, so the byte layout is untouched.

Update the header's decision list with a fourth entry, in clear voice: the emitted model value
now comes from `model-tiers.ts`, the resolution happens before the build loop for the T-27-32
reason above, and the zero-config answer is the same seven bytes the literal produced.

In `scripts/adapters-freshness.ts`: add a `cpSync` of `scripts/model-tiers.js` from
`SCRIPT_ROOT` into the mirrored `scripts` directory, placed with the three existing twins and
under the same recorded trade — the twin list is the generator's import closure and must track
it. Extend the comment above the list to name `model-tiers.js` and state why it is there
(the generator now imports it). Do not add any config directory to the mirror and do not add
any new environment variable in this task.

Regenerate with `npm run generate:adapters`, run `npm run build`, and commit the two `.ts`
files with their two `.js` twins in the SAME commit. The 17 adapter files must appear in no
diff.
  </action>
  <verify>
    <automated>npm run generate:adapters && git diff --exit-code -- .claude/agents/ && npm run freshness:adapters && npx vitest run scripts/adapter-byte-baseline.test.ts scripts/model-tiers.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run generate:adapters` followed by `git diff --exit-code -- .claude/agents/` exits 0 with an empty diff — the zero-config path is byte-identical, not merely equivalent.
    - `npm run freshness:adapters` exits 0 and its output reports 0 byte differences over 17 adapters.
    - `npx vitest run scripts/adapter-byte-baseline.test.ts` exits 0 — the frozen pre-phase baseline still matches after the generator change. This is the assertion the whole plan exists for.
    - For each of the 17 files, a filtered count of frontmatter model lines returns exactly 1: run `for f in .claude/agents/*.md; do grep -c '^model: ' "$f"; done` and confirm every line of output reads `1`.
    - Discrimination proof (twin list): in a scratch build remove the `model-tiers.js` `cpSync` from `adapters-freshness.ts`, run `npm run freshness:adapters`, and confirm it exits non-zero with the refusal that the generator did not run cleanly rather than reporting the adapters as fresh. Revert.
    - Discrimination proof (emit reads the field): in a scratch build restore the literal push at the emit site while leaving the resolver in place, run `npm run generate:adapters`, and confirm the render case that expects `model: opus` goes RED naming that expected line. Revert.
    - After `npm run build`, `git diff --exit-code --name-only -- '*.js'` is empty.
    - `git diff --name-only -- install/` is empty for this whole plan.
  </acceptance_criteria>
  <done>The zero-config path is wired end to end — role authority to resolver to generator to committed adapter bytes to freshness gate — the 17 adapters are byte-identical to the frozen pre-phase baseline, the twin list tracks the new import closure, and both discrimination proofs were observed RED and reverted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tree → generator | Role files and (from plan 29.1-02) a user-editable config JSON cross into a process that writes 17 files |
| generator → committed adapter bytes | An emitted value becomes frontmatter the Claude Code runtime loads |
| gate process → mirrored child process | `adapters-freshness.ts` spawns a child whose environment it controls |
| `.ts` source → committed `.js` | The artifact hosts actually execute is the committed build output, not the source |

## STRIDE Threat Register

ASVS level 1, block-on `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-01 | Tampering | `generate-role-adapters.ts` `OUT_DIR` | high | mitigate | `OUT_DIR` stays a fixed literal joined to the script's parent; the generator accepts no output flag; no resolved value is ever joined onto a path (T-27-28 unrelaxed). Asserted by reading the write loop and by the prohibition list. |
| T-29.1-02 | Tampering | `render()` `model:` emit | high | mitigate | The emitted value comes only from the closed `ModelAlias` union; resolution is validated before the build loop, so a refusal writes nothing (T-27-32). |
| T-29.1-04 | Tampering | `adapters-freshness.ts` mirror-spawn | high | mitigate | `CHECK_ROOT` stays deleted from `childEnv`; this plan introduces no new environment variable, and the plan records that any future one must be deleted by name alongside it. |
| T-29.1-05 | Tampering | committed `scripts/*.js` | high | mitigate | `npm run build` plus `git diff --exit-code -- '*.js'` in every task's verify; `npm run freshness` reads HEAD with `git show`, so an uncommitted build is a named failure. |
| T-29.1-07 | Repudiation | the byte baseline | medium | mitigate | The baseline is pinned to SHA `6f8411e` and derived from the git object store, so neither side can be moved by editing the working tree; the reachability residual is disclosed in the case header. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs ZERO external packages — `29.1-RESEARCH.md` §"Package Legitimacy Audit" records "Not applicable" and `package.json` devDependencies are unchanged. Asserted as absence at phase close: `git diff --exit-code -- package.json package-lock.json` (plan 29.1-06). No `[ASSUMED]` or `[SUS]` package exists, so no legitimacy checkpoint is warranted. |
</threat_model>

<verification>
- `npx vitest run scripts/adapter-byte-baseline.test.ts scripts/model-tiers.test.ts` exits 0.
- `npm run generate:adapters` then `git diff --exit-code -- .claude/agents/` exits 0 with an empty diff.
- `npm run freshness:adapters` exits 0 reporting 0 byte differences.
- `npm run typecheck` exits 0; `npm run build` then `git diff --exit-code --name-only -- '*.js'` is empty.
- `npx vitest run --exclude '**/scripts/e2e/**'` exits 0 at the end of the plan. NEVER `npm test`.
- `git diff --name-only -- install/ agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json scripts/measure-cost.ts` is empty.
</verification>

<success_criteria>
- The pre-phase byte baseline is committed, green, derived rather than hand-listed, and proven RED on a planted single-byte change.
- `scripts/model-tiers.ts` exists with its committed `.js` and refuses an empty, short or duplicated stem set each by its own name.
- The generator emits the resolved value from a field, validated before the build loop, and the 17 committed adapters are byte-identical to the frozen baseline.
- `scripts/model-tiers.js` is in the freshness twin list, and removing it was observed to make the gate refuse rather than pass.
</success_criteria>

<output>
Create `.planning/phases/29.1-per-role-model-assignment/29.1-01-SUMMARY.md` when done.
Record in it: the exact discrimination-proof transcripts (RED text and the revert), the
`npm run freshness:adapters` byte-difference count, and the `RoleTier`-without-rationale
typecheck error text.
</output>

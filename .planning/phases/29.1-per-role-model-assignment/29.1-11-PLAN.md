---
phase: 29.1-per-role-model-assignment
plan: 11
type: execute
wave: 5
depends_on: ["29.1-10"]
gap_closure: true
files_modified:
  - scripts/model-tiers.ts
  - scripts/model-tiers.js
  - scripts/model-tiers.test.ts
  - scripts/model-dial-consistency.test.ts
  - scripts/adapter-byte-baseline.test.ts
  - agent-factory/config/factory.config.md
  - agent-factory/packaging/subagent.frontmatter.md
  - .planning/phases/29.1-per-role-model-assignment/COVERAGE.md
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-06, MODEL-07]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-05 closed: the two configuration locations the resolver reads are declared ONCE as an exported constant, `readModelsConfig` builds its candidate list from that constant, and both shipped documents name both locations rather than one of them."
    - "WR-05's shadowing hazard is stated where a configuring reader meets it: `factory.config.md` documents that the FIRST EXISTING file wins WHOLE, regardless of whether it carries a `models` key — so a present-but-`models`-less repo-dropped config shadows a `models` block in the in-kit one, which is exactly the shape the shipped seed produces under D-04."
    - "The shadowing shape is pinned by a case: a present-but-`models`-less first candidate over a `models`-carrying second candidate resolves the zero-config answer and reports the first candidate as its source. The pre-existing precedence cases only covered both files carrying a block, so this is a new shape and not a restatement."
    - "The hand-listed surface roster is closed: `SURFACE_COUNT` no longer derives from the same hand-written object the loop consumes. The roster is cross-checked against a set derived from the kit authority at run time, so both sides of the two-sided pin can no longer move together — and where a derivation provably cannot cover a surface, the residual is disclosed in the file with its direction rather than hand-filtered to green."
    - "IN-03 closed: the two `toHaveLength` calls remain because they read two different derivations; the strictly implied bounds pair is deleted."
    - "IN-06 closed: the byte baseline pins the full 40-character SHA `6f8411effe80b3f22d0d668e1fa40fa78e3a8088`, and a case asserts the constant's length and character class so an abbreviation cannot return."
    - "`COVERAGE.md` exists for this phase as a reasoned no-integration declaration in the canonical single-line form, under 200 characters, so the seal-time API-coverage gate does not block UAT."
    - "Per D-05 the sparse-override contract and the role-stem key shape are unchanged by this plan; per D-13 the scope statement still has one authority and this plan adds no second one; per D-17 the disclosed installed-target limitation still ships in `factory.config.md`."
    - "Per D-16 no sentence this plan writes asserts a cost, spend, quota or usage-limit saving, and `scripts/measure-cost.ts` and its committed twin stay byte-unchanged."
  artifacts:
    - scripts/model-tiers.ts
    - agent-factory/config/factory.config.md
    - .planning/phases/29.1-per-role-model-assignment/COVERAGE.md
  key_links:
    - "`MODELS_CONFIG_CANDIDATE_RELS` is the one declaration of the two locations; `readModelsConfig` joins each against the repository root, and the cross-surface oracle asserts every member appears in both shipped documents."
    - "The surface roster is compared against a set derived at run time from the kit authority's own listers plus the single named host-guidance file, so a fourth kit document that starts discussing the resolver turns the oracle red."
    - "The byte baseline's full SHA is resolvable in this repository: `git rev-parse` on the abbreviation yields it, which is recorded rather than assumed."
  prohibitions:
    - "No second authority for the scope statement is created. `subagent.frontmatter.md`'s `model` bullet names the two locations as a fact; the PRECEDENCE rule is stated in `factory.config.md` alone."
    - "The precedence itself is not changed. It matches `readGovernanceConfig`'s established convention and is defensible; the defect is that no shipped document stated it."
    - "The surface roster is not hand-filtered to make a derivation agree with it. If the derived set and the roster disagree, reconcile the roster or the marker and record why, or disclose the residual with its direction — never trim the derived set until it fits."
    - "No `models` block is added to either of grugops's own config JSONs."
    - "`scripts/measure-cost.ts` and `scripts/measure-cost.js` stay byte-unchanged."
    - "No install-side file is read for modification, changed or tested."
    - "No unmeasured cost or limit-savings sentence in any comment, message or documentation line."
    - "No caveman voice in the config reference, the packaging authority, or any refusal text."
    - "`npm test` is never run. The regression command is `npx vitest run --exclude '**/scripts/e2e/**'`."
---

<objective>
Close the round's remaining findings: a precedence rule no shipped document states, a
two-sided pin whose two sides are the same hand-written list, a redundant assertion pair, an
abbreviated SHA, and the missing coverage declaration.

WR-05 is a documentation defect with a live consequence. `subagent.frontmatter.md:202-207`
says the resolver reads the `models` block from the repository's `.grugops/factory.config.json`.
It reads two candidates, and `factory.config.md`'s `models` section names neither. Worse, the
loop returns on the FIRST EXISTING file regardless of whether that file carries a `models` key
— so a present-but-`models`-less `.grugops/factory.config.json`, which is exactly what the
shipped seed is under D-04, shadows a `models` block written into
`agent-factory/config/factory.config.json`. The verifier reproduced it: the resolver answers the
zero-config preset and names the repo-dropped file as its source. The precedence matches
`readGovernanceConfig`'s established convention and is defensible; the defect is that a user who
follows the authority literally, in the standard installed shape, gets a silent no-op.

The surface roster is this round's second instance of the failure class that produced WR-04.
`model-dial-consistency.test.ts:61,86,159-162` declares a three-entry object and a hand-typed
count of three, and then "derives" the surface list from that object — so both sides of the
two-sided pin are edited together by construction and the vacuity floor cannot fire. Plan 05's
must-have claimed the oracle derives its surface list; it derives it from itself.

IN-03, IN-06 and the coverage declaration are small and land beside them. The API-coverage
detector returned no external integration over this phase's scope, and `COVERAGE.md` does not
exist — which blocks the seal-time gate later, so it is written here as a reasoned declaration
rather than discovered at UAT.

Output: two documented configuration locations with their precedence, a roster pinned against
something other than itself, a full SHA, and the phase's coverage declaration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-per-role-model-assignment/29.1-CONTEXT.md
@.planning/phases/29.1-per-role-model-assignment/29.1-PATTERNS.md
@.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION.md
@.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW.md
@.planning/phases/29.1-per-role-model-assignment/29.1-05-SUMMARY.md
@.planning/phases/29.1-per-role-model-assignment/29.1-10-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol or path | Kind | Purpose |
|----------------|------|---------|
| `MODELS_CONFIG_CANDIDATE_RELS` | new exported const in `scripts/model-tiers.ts` | the two repo-relative configuration locations, declared once and joined by `readModelsConfig` |
| `SURFACE_COUNT` | changed derivation in `model-dial-consistency.test.ts` | no longer a hand-typed literal read against the object that supplies the loop |
| `DIAL_SURFACE_MARKER` | new const in `model-dial-consistency.test.ts` | the literal whose presence defines a dial-discussing surface, used by the run-time derivation |
| `PRE_PHASE_ADAPTER_BASELINE` | changed const in `adapter-byte-baseline.test.ts` | the full 40-character SHA `6f8411effe80b3f22d0d668e1fa40fa78e3a8088` |
| `.planning/phases/29.1-per-role-model-assignment/COVERAGE.md` | new file | the phase's no-integration declaration in canonical single-line form |

New test case names (`scripts/model-tiers.test.ts`):
`SHADOWING: a present-but-models-less first candidate wins WHOLE over a models-carrying second`,
`the candidate list is built from the exported location constant, in declared order`.

New test case names (`scripts/model-dial-consistency.test.ts`):
`the surface roster equals the set derived from the kit authority at run time`,
`every declared configuration location appears in BOTH shipped documents`.

New test case names (`scripts/adapter-byte-baseline.test.ts`):
`the pinned baseline is a full 40-character object name, never an abbreviation`.

## Planner assumptions carried forward

See `29.1-07-PLAN.md` §"Planner assumptions carried forward" for the 14 unresolved
`29.1-EDGE-COVERAGE.json` items. **All 14 remain unresolved at the end of this round.** Closing
them is not this round's scope and no plan in the 07-11 series claims to. The two probes this
round touched incidentally are recorded in `29.1-08-PLAN.md` (MODEL-04 and MODEL-05 empty shapes)
and `29.1-10-PLAN.md` (MODEL-06 encoding). The remaining twelve are carried forward untouched and
must not be reported as closed by any SUMMARY in this series.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare the two configuration locations once, and state the whole-file precedence where a configuring reader meets it</name>
  <files>scripts/model-tiers.ts, scripts/model-tiers.js, scripts/model-tiers.test.ts, agent-factory/config/factory.config.md, agent-factory/packaging/subagent.frontmatter.md</files>
  <behavior>
    - The resolver's candidate list is built from one exported constant, in the declared order, and the constant's members are repo-relative paths.
    - A present-but-`models`-less first candidate wins WHOLE over a second candidate that carries a `models` block: the resolution is the zero-config answer and the reported source is the first candidate.
    - Both files carrying a `models` block still resolves the first candidate's block, unchanged from today.
    - Only the second candidate existing still resolves the second candidate's block, unchanged from today.
    - Neither file existing is still the genuinely-absent answer with a null source, unchanged from today.
    - Both shipped documents name both configuration locations; exactly one of them states the precedence rule.
  </behavior>
  <read_first>
    - `scripts/model-tiers.ts` lines 890-995 — `readModelsConfig` in full: the empty-stems vacuity floor, the inline `candidates` array at 927-930, the per-candidate loop with its four documented outcomes, and the post-loop genuinely-absent answer. Note that outcome 4 returns the zero-config answer WITH the candidate's path as its source, which is why the first existing file wins whole.
    - `scripts/model-tiers.test.ts` — the existing precedence cases. Read them before writing the new one so it is clear which shape they cover (both files carrying a block) and which they do not (a present-but-blockless first candidate).
    - `agent-factory/config/factory.config.md` §`models` sub-fields lines 119-136 — the field table, the Precedence paragraph about `roles` winning over `preset`, the Refusals paragraph (extended by plan 29.1-08), the Host-CLI scope pointer, and the D-17 disclosed-limitation paragraph. The new location-and-precedence paragraph joins this section and must not disturb the D-17 disclosure.
    - `agent-factory/packaging/subagent.frontmatter.md` lines 200-210 — the `model` bullet, which names one configuration location and is the sentence WR-05 reports.
    - `scripts/context-io.ts` around lines 1240-1300 — `readGovernanceConfig`, the established two-location convention this precedence matches. Read it so the documentation describes a convention rather than an accident, and so the new paragraph can say the two readers agree.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW.md` §WR-05 in full — the reproduction and the three-part remedy.
  </read_first>
  <action>
Add an exported constant to `scripts/model-tiers.ts` holding the two repo-relative configuration
locations as a readonly tuple, in the order the resolver tries them, declared beside the module's
other closed vocabularies. Build `readModelsConfig`'s candidate list by joining each member
against the repository root, deleting the inline array. Record beside the constant that it exists
so the shipped documentation and the code cannot name different files, and that the oracle
asserts every member appears in both documents.

Do NOT change the precedence. The first existing file wins whole, and that matches
`readGovernanceConfig`'s convention. Record that agreement in a source comment naming the sibling
reader, so a later reader can see the behaviour is a convention rather than an oversight, and
record the consequence in the same place: a configuration file present at the first location and
carrying no `models` key shadows a `models` block at the second, and the shipped seed is exactly
such a file under D-04.

In `agent-factory/config/factory.config.md`'s `models` section, add a paragraph naming both
locations in order and stating the whole-file precedence and its shadowing consequence in one
clear sentence each. This is the single authority for the precedence rule. Keep the paragraph in
clear professional voice and keep the D-17 disclosed-limitation paragraph intact and adjacent, so
a configuring reader meets both facts about installed targets together.

In `agent-factory/packaging/subagent.frontmatter.md`, correct the `model` bullet to name both
locations as a fact. Do not state the precedence there — per D-13's shape, one document holds a
rule and the other does not restate it, and adding a second precedence statement is how a second
authority is born.

Write the shadowing case and the candidate-list case into `scripts/model-tiers.test.ts` RED
FIRST. Assert each fixture's own premise: read each planted file back and confirm which one
carries a `models` key before asserting on the resolution, so a fixture typo cannot make the case
pass for the wrong reason.

Run `npm run build` and commit `scripts/model-tiers.js` in the same commit.
  </action>
  <verify>
    <automated>npx vitest run scripts/model-tiers.test.ts scripts/model-dial-consistency.test.ts && node scripts/check-foundation-guards.js && npm run freshness && npm run typecheck && npm run build && git diff --exit-code --name-only -- '*.js'</automated>
  </verify>
  <acceptance_criteria>
    - RED-first proof of the shadowing shape, recorded against the committed `scripts/model-tiers.js` before the case exists: plant `{"quality":{"tdd":true}}` at the first candidate location and `{"models":{"preset":"tiered"}}` at the second, and confirm the resolver answers the zero-config preset with the FIRST candidate as its source. Capture the transcript. The new case must assert exactly that, and must be proven to fail if the precedence were reversed.
    - Discrimination proof, observed RED and reverted: reverse the order of the two members in the exported location constant, and confirm the shadowing case goes RED naming the wrong source. This is the proof the case pins precedence and not merely a boolean.
    - The three pre-existing precedence shapes are unchanged: both-files-with-blocks, second-file-only, and neither-file. Run them and record that none was retitled or repaired.
    - `grep -n '\.grugops' scripts/model-tiers.ts` shows the path only inside the exported constant — no second inline spelling of either location anywhere in the module.
    - Both documents name both locations: `grep -c '.grugops/factory.config.json' agent-factory/config/factory.config.md agent-factory/packaging/subagent.frontmatter.md` and the same for `agent-factory/config/factory.config.json` are each at least 1 in both files. Record all four counts.
    - Exactly one document states the precedence rule. Assert by a case that the precedence sentence's anchor text appears in `factory.config.md` and not in `subagent.frontmatter.md`.
    - The D-17 disclosed-limitation paragraph is still present and adjacent in `factory.config.md`, asserted by grep, and its text is byte-unchanged. Compare against `git show HEAD:agent-factory/config/factory.config.md`.
    - `npx vitest run scripts/model-dial-consistency.test.ts` exits 0 — the scope-sentence and pointer cases from plan 29.1-10 are unaffected by this document edit.
    - No cost claim entered either document: `grep -inE 'cheaper|saving|spend less|reduce cost|lower cost|quota' agent-factory/config/factory.config.md agent-factory/packaging/subagent.frontmatter.md` is recorded and every hit is pre-existing.
    - `npm run typecheck` exits 0. After `npm run build`, `git diff --exit-code --name-only -- '*.js'` is empty.
  </acceptance_criteria>
  <done>The two configuration locations are declared once and consumed by the resolver and asserted in both shipped documents, the whole-file precedence and its shadowing consequence are stated where a configuring reader meets them, exactly one document holds the rule, and reversing the declared order was observed to turn the new shadowing case red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the surface roster against something other than itself, and delete the implied assertions</name>
  <files>scripts/model-dial-consistency.test.ts</files>
  <behavior>
    - The roster's cardinality is compared against a set derived at run time from the kit authority rather than against a literal typed beside the roster.
    - Deleting a roster entry turns the oracle red on the comparison, and the failure names the surface that went missing.
    - A kit document that begins discussing the model resolver and is not in the roster turns the oracle red, naming the file — the direction the current pin structurally cannot fail on.
    - The vacuity floor still fires on an empty roster, and the module-load premise assertion still names itself when it breaks.
    - The two `toHaveLength` calls remain because they read two different derivations; the bounds pair on the same number is gone.
  </behavior>
  <read_first>
    - `scripts/model-dial-consistency.test.ts` lines 40-100 — the `SURFACE_ROLES` object at 43-47, the `SURFACE_COUNT` literal at 61 with its promote-trigger comment, `CLOSED_SET_DECLARATION_SITES` at 70, the `surfaces` derivation at 85-89, and `assertRosterPremise` at 96-115 with its recorded reason for adjudicating at module load rather than inside a describe body.
    - `scripts/model-dial-consistency.test.ts` lines 155-165 — the vacuity case with its four assertions, two of which IN-03 reports as strictly implied.
    - `scripts/kit-model.ts` around lines 950-1200 — `listAgentAdapters`, `listSkillAdapters`, `listPluginSkillAdapters`, `listPluginDefaultComponentFiles`, `listPackagingTemplates` and `SPAWN_GRANT_SCAN_PARTS`. This is the authority available for a run-time derivation; read it before choosing a marker, so the derivation asks the kit what it ships rather than asking the filesystem what happens to be there.
    - `agent-factory/packaging/subagent.frontmatter.md` and `agent-factory/config/factory.config.md` as they stand after task 1, plus `CLAUDE.md`'s amended row — the three surfaces, so the chosen marker can be MEASURED against them before it is committed to.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW.md` §IN-03, and `29.1-VERIFICATION.md`'s Anti-Patterns row for this file, which states the failure precisely: both sides of the two-sided pin are supplied by one hand-list.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-05-SUMMARY.md` — plan 05's must-have claiming the oracle derives its surface list, which is the claim this task makes true or narrows.
  </read_first>
  <action>
Replace the hand-typed cardinality with a cross-check against a set derived at run time. Choose a
marker literal that identifies a document as speaking about the model dial and declare it once as
a named constant. Derive the candidate corpus by asking the kit authority for the packaging
templates and the configuration documents it ships, plus the single host-guidance file named by
its own path, and scan each for the marker. Assert the derived set is set-equal to the roster's
paths, reporting both directions separately: a roster entry the derivation did not find, and a
derived file the roster does not carry.

MEASURE BEFORE COMMITTING TO THE MARKER. Run the candidate derivation against the tree as it
stands after task 1 and record the result. If the derived set equals the roster, the cross-check
is sound and the hand-typed count goes away. If it does not, do NOT trim the derived set until it
fits — reconcile the roster, or choose a marker that identifies the surfaces the roster means,
and record the measurement that decided it. If no derivation over the available authority can
cover a surface the roster legitimately holds, keep that surface named explicitly, state in the
file which part of the roster is derived and which is named, and disclose the residual with its
direction the way this file's header already discloses its normalization residual.

Keep the module-load premise assertion and its recorded reason. Its purpose — that a shortened
roster must not kill the harness before the floor executes — is unaffected by where the expected
number comes from, and it must keep naming itself when it breaks.

Delete the two bounds assertions in the vacuity case. Keep both `toHaveLength` calls and record
in the case body that they read two different derivations, which is why they are not a duplicate
pair, and that the deleted assertions were strictly implied by one of them.

Write the two new cases RED FIRST.
  </action>
  <verify>
    <automated>npx vitest run scripts/model-dial-consistency.test.ts && npx vitest run --exclude '**/scripts/e2e/**' && npm run freshness && node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - The measurement is recorded before the design is fixed: run the chosen derivation over the post-task-1 tree and record the exact file list it produces, beside the roster's three paths. If they differ, record what was reconciled and why.
    - RED-first proof, direction one: delete a roster entry, confirm the oracle goes RED on the set comparison naming the missing surface, and revert. Under the pre-fix code the same deletion could be made green by moving the count literal — demonstrate that on a scratch copy and record both outcomes, because that contrast is the finding.
    - RED-first proof, direction two: add the chosen marker to a kit document that is not in the roster, confirm the oracle goes RED naming that file, and revert. This is the direction the current pin structurally cannot fail on.
    - `grep -n 'SURFACE_COUNT' scripts/model-dial-consistency.test.ts` shows no literal integer assigned to it. If a number survives anywhere in this file's roster logic, its independence from the roster object must be demonstrable and recorded.
    - IN-03 closed: the vacuity case holds exactly two assertions on the cardinality, both `toHaveLength`, and `grep -c 'toBeGreaterThanOrEqual\|toBeLessThanOrEqual' scripts/model-dial-consistency.test.ts` is 0.
    - The module-load premise assertion still fires: shorten the roster to two entries on a scratch copy and confirm the thrown message names the premise rather than a collection failure, then revert.
    - If any part of the roster remains explicitly named rather than derived, the file states which part and why, and discloses the residual's direction. An undisclosed hand-named surface fails this criterion.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0. `npm test` is never run.
    - `npm run freshness` reports every committed `.js` fresh; `git diff --exit-code --name-only -- '*.js'` is empty — this task rebuilds nothing.
  </acceptance_criteria>
  <done>The roster's cardinality is checked against a set derived from the kit authority at run time, both directions of that comparison were observed red, the contrast with the pre-fix behaviour under the same mutation is recorded, the implied bounds pair is gone, and any surface that remains named rather than derived is disclosed with its direction.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the full baseline SHA, write the phase coverage declaration, and close the round with the full gate set</name>
  <files>scripts/adapter-byte-baseline.test.ts, .planning/phases/29.1-per-role-model-assignment/COVERAGE.md</files>
  <read_first>
    - `scripts/adapter-byte-baseline.test.ts` lines 45-90 — the module header's argument for pinning a revision rather than the working tree, the `PRE_PHASE_ADAPTER_BASELINE` constant at 63 with its recorded reachability residual, `baselineAdapterNames` and its two floors (a non-zero spawn throws naming the revision, an empty derivation throws stating the premise is empty).
    - `.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW.md` §IN-06 — the finding and its stated reason: the throw would read as an unreachable pinned tree rather than as an ambiguous abbreviation.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-01-SUMMARY.md` — where the baseline was frozen and what it is a baseline OF, so the amended comment stays accurate.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-07-SUMMARY.md`, `29.1-08-SUMMARY.md`, `29.1-09-SUMMARY.md` and `29.1-10-SUMMARY.md` — the four preceding gap-closure summaries, read so the round-closing gate run can confirm each plan's recorded evidence still holds on the final tree rather than being taken on trust.
  </read_first>
  <action>
Replace the abbreviated baseline constant in `scripts/adapter-byte-baseline.test.ts` with the
full forty-character object name `6f8411effe80b3f22d0d668e1fa40fa78e3a8088`. Resolve it in the
repository first rather than copying it on trust, and record the resolution command and its
output. Extend the constant's comment to state why a full name is pinned: an abbreviation becomes
ambiguous as history grows, and the resulting throw would misdescribe the failure as an
unreachable pinned tree. Add a case asserting the constant's length is forty and that every
character is a lowercase hexadecimal digit, so an abbreviation cannot return through a later edit.

Write `.planning/phases/29.1-per-role-model-assignment/COVERAGE.md`. It records that the API
coverage detector returned no external integration over this phase's scope, in the canonical
single-line declaration form the seal-time gate parses — the fixed prefix followed by one
reasoned sentence. Keep the whole declaration line under two hundred characters; this project has
previously had the verification workflow halt because a longer reason overflowed that cap, so
count the characters and record the count. Beneath the declaration, state the evidence in a short
paragraph: this round edits a build-time code generator, its resolver, the foundation guard set,
the CI workflow and kit prose; the tooling layer ships with zero runtime dependencies and opens
no socket at build time or run time.

Then run the round's full gate set on the final tree and record every result. This is the
round-closing evidence, not a formality: each of the four preceding plans recorded a transcript
against the tree as it stood then, and this run confirms none of them was invalidated by a later
plan.
  </action>
  <verify>
    <automated>npx vitest run scripts/adapter-byte-baseline.test.ts && npm run typecheck && npm run build && git diff --exit-code --name-only -- '*.js' && npm run freshness && npm run freshness:adapters && npm run freshness:catalog && npm run freshness:context && npm run freshness:skill-twins && npm run generate:adapters && git diff --exit-code -- .claude/agents/ && node scripts/check-foundation-guards.js && npm run check:banned-claims && npm run check:claim-anchors && npm run check:public-docs && npm run check:audit-register && npx vitest run --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - The full SHA is resolved rather than copied: `git rev-parse 6f8411e` is run and its output recorded, and it equals the constant. If it does not, stop and report rather than pinning a number this plan asserted.
    - `grep -c "6f8411effe80b3f22d0d668e1fa40fa78e3a8088" scripts/adapter-byte-baseline.test.ts` is at least 1, and `grep -cE '"6f8411e"' scripts/adapter-byte-baseline.test.ts` is 0.
    - The new length-and-character-class case passes, and is proven load-bearing: replace the constant with the seven-character abbreviation on a scratch copy, confirm the case goes RED naming the length, and revert.
    - `npx vitest run scripts/adapter-byte-baseline.test.ts` exits 0 — the seventeen-file comparison against the pinned revision still passes with the full name.
    - `COVERAGE.md` exists at the phase path, its declaration line begins with the canonical prefix, and its character count is recorded and is under 200. Print the line's length; do not estimate it.
    - The round-closing gate set is run on the final tree and EVERY result is recorded with its exit code: the five freshness gates, the adapter generator plus its diff, the foundation guard, the four prose and audit checks, the typecheck, the build with its `.js` diff, and the excluded-lane regression run.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0, and its passing and skipped counts are recorded. `npm test` is never run.
    - `git diff --name-only -- scripts/measure-cost.ts scripts/measure-cost.js install/ package.json agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` is empty across the whole round, measured from the commit preceding plan 29.1-07 to the final commit.
    - All fifteen findings of the round are enumerated in the SUMMARY with the plan and task that closed each, and any finding closed by disclosure rather than by mechanism is marked as such with the disclosure's location.
  </acceptance_criteria>
  <done>The baseline pins a full object name resolved in this repository, an abbreviation is proven to turn the new case red, the phase carries a coverage declaration under the character cap, and the round's full gate set is green on the final tree with every exit code recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped documentation → the configuring user | A user follows the document rather than the code, so a document naming one of two locations produces a silent no-op |
| an oracle's pin → the reviewer | A green oracle is what a reviewer trusts; a pin whose two sides move together certifies nothing |
| a pinned git revision → the byte baseline | The one genuinely independent second opinion on the adapter bytes depends on that revision resolving unambiguously |

## STRIDE Threat Register

ASVS level 1, block on `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-27 | Repudiation | an undocumented whole-file precedence | medium | mitigate | Both locations are declared once and asserted present in both shipped documents; the precedence and its shadowing consequence are stated in exactly one of them; the shadowing shape is pinned by a case proven to fail if the declared order is reversed. |
| T-29.1-28 | Tampering | a two-sided pin whose two sides are one hand-list | high | mitigate | The roster is cross-checked against a set derived from the kit authority at run time, with both directions of the comparison proven RED, and any surface that cannot be derived is disclosed with its direction rather than hand-filtered. |
| T-29.1-29 | Spoofing | an ambiguous abbreviated object name | low | mitigate | The full forty-character name is resolved in the repository and pinned, with a case asserting the length and character class so an abbreviation cannot return. |
| T-29.1-30 | Repudiation | a coverage gate discovering a missing declaration at seal time | low | mitigate | The declaration is written now in the canonical form with its character count measured against the cap that has previously halted this project's verification workflow. |
| T-29.1-05 | Tampering | committed `scripts/*.js` diverging from its `.ts` | high | mitigate | `npm run build` plus `git diff --exit-code -- '*.js'` in task 1 and task 3; the round-closing run reads the final tree. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | accept | Zero packages installed across the whole round; `package.json` is asserted byte-unchanged from the commit preceding plan 29.1-07 to the final commit. |
</threat_model>

<verification>
- `npx vitest run scripts/model-tiers.test.ts scripts/model-dial-consistency.test.ts scripts/adapter-byte-baseline.test.ts` exits 0.
- `npx vitest run --exclude '**/scripts/e2e/**'` exits 0. `npm test` is never run.
- `npm run freshness`, `npm run freshness:adapters`, `npm run freshness:catalog`, `npm run freshness:context` and `npm run freshness:skill-twins` all exit 0.
- `npm run generate:adapters` then `git diff --exit-code -- .claude/agents/` is empty.
- `node scripts/check-foundation-guards.js` exits 0.
- `npm run check:banned-claims`, `npm run check:claim-anchors`, `npm run check:public-docs` and `npm run check:audit-register` all exit 0.
- `npm run typecheck` exits 0; `npm run build` then `git diff --exit-code --name-only -- '*.js'` is empty.
- `.planning/phases/29.1-per-role-model-assignment/COVERAGE.md` exists and its declaration line is under 200 characters.
</verification>

<success_criteria>
- Both configuration locations are declared once, consumed by the resolver, and named in both shipped documents, with the precedence stated in exactly one of them and pinned by a case that fails when the declared order is reversed.
- The surface roster is cross-checked against a run-time derivation, both directions were observed red, and the contrast with the pre-fix behaviour under the same mutation is recorded.
- The implied bounds pair is gone and the two `toHaveLength` calls remain.
- The baseline pins a full object name resolved in this repository, and an abbreviation turns the new case red.
- `COVERAGE.md` exists under the character cap, and the round-closing gate set is green on the final tree with every exit code recorded.
- All fifteen findings of the round are enumerated with the plan and task that closed each.
</success_criteria>

<output>
Create `.planning/phases/29.1-per-role-model-assignment/29.1-11-SUMMARY.md` when done.
Record in it: the shadowing reproduction and the reversed-order RED transcript; the four
document grep counts; the roster derivation's measured file list beside the roster; both
roster RED transcripts and the pre-fix contrast; the `git rev-parse` output; the abbreviation
RED transcript; the `COVERAGE.md` declaration line with its measured character count; the full
round-closing gate table with exit codes and the regression run's counts; and the enumeration of
all fifteen findings against the plan and task that closed each, marking any closed by
disclosure rather than by mechanism.
</output>

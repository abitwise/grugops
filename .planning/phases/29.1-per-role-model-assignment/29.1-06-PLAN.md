---
phase: 29.1-per-role-model-assignment
plan: 06
type: execute
wave: 6
depends_on: ["29.1-05"]
files_modified:
  - agent-factory/writing-profile.md
  - docs/audit/28-claim-registry.md
autonomous: true
requirements: [MODEL-07]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "MODEL-07's prohibition is held as CONTENT under the D-59 precedent, recorded as an anchored public claim in `agent-factory/writing-profile.md` and registered in `docs/audit/28-claim-registry.md`, with its FAIL-OPEN direction stated plainly rather than implied (D-16)."
    - "The registry row's `mechanism` field states, in these words' substance and never blank: `BannedClaimGroup` in `scripts/check-banned-claims.ts` is exactly `standard-name`, `token-economy` and `comprehension` — there is NO cost group — so a sentence asserting a saving passes every gate in this repository today. That is the fail-open direction, and it is disclosed, not implied."
    - "No fourth literal group is added to `scripts/check-banned-claims.ts`. The declining decision (D-16) is named in the row together with its reason: that module's own header demands an admission measurement — candidate literals run over the derived document corpus with per-file hits recorded before adding them, and any literal striking live correct text refused — and Phase 29's eight-round record is that a literal list over free prose buys one axis per round and ships a new residual each time."
    - "The rationale half of MODEL-07 stays MECHANICAL and is not content-held: D-10's required-`rationale` field fails generation on a blank rationale, and plan 29.1-02's discrimination proof (c) demonstrated it going RED by stem name."
    - "`scripts/measure-cost.ts` and `scripts/measure-cost.js` are byte-unchanged; its standing `UNKNOWN - verify` remains the honest answer and no measurement is taken this phase (D-14)."
    - "The new row's id is DERIVED from the registry rather than hand-typed, and the derived total is reconciled against `scripts/audit-model.test.ts`'s own run-time-derived count assertion — no hand-maintained total is introduced."
    - statement: "MODEL-07/boundary: zero cost or limit-savings sentences ship in this phase's touched documents. The boundary between a cost claim and a neutral mechanism description is not a decidable predicate in this repository — no gate enumerates it — so it is adjudicated by human reading at phase close and the fail-open direction is disclosed in registry row C-28-047."
      verification: backstop
    - statement: "MODEL-07/precision: there is no numeric cost figure whose rounding or precision could be wrong, because no figure ships. The mechanical half is that `scripts/measure-cost.ts` is byte-unchanged and every `TIERED` rationale is asserted to contain no decimal digit; the remaining half — that no phase-touched prose introduces a percentage, multiplier or price — is a human reading, disclosed rather than claimed as gated."
      verification: backstop
  artifacts:
    - agent-factory/writing-profile.md
    - docs/audit/28-claim-registry.md
  key_links:
    - "The claim text must be anchored with an HTML comment on its own line and matched byte for byte by `scripts/check-claim-anchors.js`; the registry is parsed by `readRegistry()` in `scripts/audit-model.ts`, the one parse authority."
    - "`agent-factory/writing-profile.md` is inside `scripts/check-banned-claims.ts`'s corpus and carries a NAMED EXEMPTION REGION for its honest denials — the new statement must be placed and worded with that region's boundary derived, not guessed."
  prohibitions:
    - "No fourth `cost` group and no new literal is added to `BANNED_CLAIM_LITERALS` or `BannedClaimGroup` in `scripts/check-banned-claims.ts` (D-16)."
    - "No change to `scripts/measure-cost.ts` or `scripts/measure-cost.js` (D-14)."
    - "No measurement is taken and no cost or limit-savings figure ships."
    - "The registry's dated historical prose is ANNOTATED, never rewritten — this project's annotate-never-rewrite convention."
    - "No hand-typed registry total is introduced anywhere."
    - "No change to `install/install.ts` or `install/kit-source.ts` (D-17)."
    - "No caveman voice in the honesty statement or the registry row — money and disclaimers are clear-voice topics."
---

<objective>
Ship the honesty floor, and disclose exactly how weakly it is held.

MODEL-07 says no cost or limit-savings claim ships unmeasured. Research measured the tree's
answer: `BannedClaimGroup` is exactly three groups — none of them about cost — so a sentence
asserting a saving passes every gate in this repository today. That is a FAIL-OPEN direction,
and this phase's obligation is to state it rather than to imply a protection that is not there.

D-16 rules the prohibition CONTENT-held under the D-59 precedent, and explicitly declines a
fourth literal group. The reason is recorded and is not squeamishness: that module's own header
demands an admission measurement before any literal is added — candidates run over the derived
corpus, hits recorded per file, any literal striking live correct text refused — and Phase 29's
eight-round record is that a literal list over free prose buys one axis per round and ships a
new residual each time. D-59 settled the shape: an open-set totality is not decidable, so it is
held as content with a disclosed backstop.

The rationale half of MODEL-07 is different and stays mechanical: D-10's required `rationale`
field fails generation on a blank one, and plan 29.1-02 proved it RED by stem name. So the
requirement splits cleanly — the disputability half is enforced by a type and a check, the
totality half is content with its weakness on the record.

This plan also closes the phase: the full fourteen-command gate block in CI's order, build
parity, the whole regression lane, and the supply-chain assertion as absence.

Scope note: D-01, D-02 and D-03 are OUT OF SCOPE and moved to Phase 29.2 by D-17.

Output: one anchored public claim, one registry row disclosing the fail-open direction, and a
green phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-per-role-model-assignment/29.1-CONTEXT.md
@.planning/phases/29.1-per-role-model-assignment/29.1-RESEARCH.md
@.planning/phases/29.1-per-role-model-assignment/29.1-PATTERNS.md
@.planning/phases/29.1-per-role-model-assignment/29.1-VALIDATION.md
@.planning/phases/29.1-per-role-model-assignment/29.1-05-SUMMARY.md
</context>

## Artifacts this phase produces

See `29.1-01-PLAN.md` §"Artifacts this phase produces" for the full phase-wide list. This plan
adds the claim id `C-28-047` (id derived, not hand-typed) and its anchor comment in
`agent-factory/writing-profile.md`. No new file, no new source symbol, no new CLI flag, no new
environment variable.

<tasks>

<task type="auto">
  <name>Task 1: The cost-claim prohibition as anchored content, with its fail-open direction disclosed</name>
  <files>agent-factory/writing-profile.md, docs/audit/28-claim-registry.md</files>
  <read_first>
    - `docs/audit/28-claim-registry.md` lines 1–60 — the corpus the registry covers, the field table, the never-blank `mechanism` rule, and the recorded reason `line` is advisory rather than asserted.
    - `docs/audit/28-claim-registry.md` §`C-28-042` — the fail-open disclosure row, the D-59-shaped analog this row copies: what its `mechanism` field states, how it names the declining decision, and how it distinguishes a refusal from a closure.
    - `docs/audit/28-claim-registry.md` §`C-28-046` — the absence-of-evidence row and its fenced verbatim block, the closest analog for a statement about what the kit does NOT claim.
    - `agent-factory/writing-profile.md` around lines 236–300 — the existing anchors `C-28-039`, `C-28-042` through `C-28-046`, the anchor syntax (an HTML comment on its own line, the claim being the contiguous line slice below it), and the named honesty region these denials sit in.
    - `scripts/check-banned-claims.ts` lines 240–260 (`BannedClaimGroup`), 340–470 (`BANNED_CLAIM_LITERALS`, the group declaration, and the gate's own statement of its fail-open direction), and 690–710 (the named exemption region and its file binding).
    - `scripts/audit-model.ts` `readRegistry` and `scripts/audit-model.test.ts` lines 1320–1345 — the derived count assertion, so the new row is reconciled against a derivation rather than a literal.
    - `scripts/check-claim-anchors.ts` — what the gate asserts: the file, anchor presence, and the verbatim text at the anchor.
  </read_first>
  <action>
First, DERIVE the facts this row asserts rather than copying them from research. Read
`BannedClaimGroup`'s declaration and record its exact member list in the SUMMARY. Read the
named exemption region's file binding and its section boundary and record both. Derive the next
free claim id by scanning the registry's own anchored ids and taking the successor of the
maximum — do not hand-type it, and record the derived id together with the derived current row
count. Derive whether `agent-factory/writing-profile.md` is inside the watched corpus of
`scripts/check-diff-disposition.js` and record the answer; if it is, author the disposition
entry that gate requires under `docs/audit/29-style-dispositions/` following the existing
per-plan file convention, naming this plan.

Then author the claim in `agent-factory/writing-profile.md`, inside the named honesty region so
it sits with the kit's other denials rather than in a new region of its own. Place an anchor
comment on its own line carrying the derived id, and write the claim immediately below it as a
short contiguous block in clear professional voice. The claim states what the kit does and does
not assert: grugops ships a per-role model dial whose tier assignment is a quality decision;
the kit makes no claim that any tier assignment reduces cost or extends a usage limit; no such
figure has been measured; and any future statement of one carries `UNKNOWN - verify` until
`scripts/measure-cost.ts` produces a real capture. Assert nothing about spend. Include no
number, no percentage and no multiplier.

Then add the registry row. Copy `C-28-042`'s field order exactly — `file`, `line`, `kind`,
`depends_on`, `status`, `mechanism`, and `disposition` plus `finding_id` if the status is not
`true` — followed by the fenced verbatim claim text, byte for byte from the source you just
wrote. Set `line` to the claim's location as committed by this plan, recording that it is
advisory.

The `mechanism` field is the substance of D-16 and must never be blank. State in it, in clear
voice: the mechanism that holds these bytes is `scripts/check-claim-anchors.js`'s live byte
comparison at the anchor, which holds the SENTENCE and not the TOTALITY. Then disclose the
direction: the derived member list of `BannedClaimGroup` contains no group about cost, so a
sentence asserting a saving passes every gate in this repository today — a FAIL-OPEN direction,
stated rather than implied. Then name the declining decision D-16 and its reason: adding a
fourth group requires the admission measurement `scripts/check-banned-claims.ts`'s own header
demands, and Phase 29's record is that a literal list over free prose buys one axis per round
and ships a new residual each time; under the D-59 precedent an open-set totality is not a
decidable predicate and is held as content with a disclosed backstop. Then state what remains
MECHANICAL: MODEL-07's rationale half is enforced by the required `rationale` field on the tier
table, whose blank-rationale mutation was proven RED by stem name in plan 29.1-02, and by
`scripts/measure-cost.ts` remaining byte-unchanged with its standing `UNKNOWN - verify`. Close
by naming, as the row's residual, that a refusal is not a closure, which is why this axis
carries an id.

Do NOT rewrite the registry's dated header sentence recording the row count as committed by an
earlier plan. It is a historical statement scoped to that plan. If anything is added, add an
annotation below it recording the derived current count at this plan's commit — annotate, never
rewrite, which is this project's convention and the reason the round-8 verifier's verdict was
left standing in Phase 29.

Do not touch `scripts/check-banned-claims.ts`. Do not touch `scripts/measure-cost.ts`.
  </action>
  <verify>
    <automated>node scripts/check-claim-anchors.js && node scripts/check-audit-register.js && node scripts/check-banned-claims.js && node scripts/check-imperative-lexicon.js && node scripts/check-diff-disposition.js && node scripts/check-nul-bytes.js && npx vitest run scripts/audit-model.test.ts scripts/check-claim-anchors.test.ts scripts/check-banned-claims.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six gates and all three test files above exit 0.
    - `npx vitest run scripts/audit-model.test.ts` exits 0, including its run-time-derived registry count assertion — the derived total moved by exactly one and the assertion still holds without a literal being edited.
    - The SUMMARY records, derived rather than copied: `BannedClaimGroup`'s exact member list; the named exemption region's file binding and section boundary; the next free claim id and the derived row count before and after; and whether `agent-factory/writing-profile.md` is inside `check-diff-disposition`'s watched corpus.
    - The new registry row's `mechanism` field is non-empty and contains all four elements: the holding mechanism, the disclosed fail-open direction with the derived group list, the declining decision D-16 with its reason, and what remains mechanical.
    - The fenced verbatim block matches the source byte for byte — proven by `node scripts/check-claim-anchors.js` exiting 0, and by recording the byte length of both the source slice and the fenced block in the SUMMARY.
    - The claim text contains no decimal digit: assert by reading, and record the assertion in the SUMMARY.
    - `git diff --exit-code -- scripts/check-banned-claims.ts scripts/check-banned-claims.js scripts/measure-cost.ts scripts/measure-cost.js` is empty.
    - The registry's dated header sentence is byte-unchanged: `git diff -- docs/audit/28-claim-registry.md` shows no hunk touching it.
    - Discrimination proof, observed RED and reverted: alter one byte of the claim in `agent-factory/writing-profile.md` without altering the registry's fenced block, and confirm `node scripts/check-claim-anchors.js` exits non-zero naming the claim id and the file.
    - Discrimination proof, observed RED and reverted: blank the new row's `mechanism` field and confirm `node scripts/check-audit-register.js` refuses it by name.
  </acceptance_criteria>
  <done>The cost-claim prohibition is an anchored public claim held byte-for-byte by the claim-anchor gate, registered with a non-blank `mechanism` that discloses the fail-open direction from a derived group list, names D-16's declining decision and its reason, and states what remains mechanical; the id and the count were derived, the dated header prose was annotated rather than rewritten, and both gate mutations were observed RED.</done>
</task>

<task type="auto">
  <name>Task 2: Close the phase — the fourteen-command gate block, build parity, and the supply-chain assertion as absence</name>
  <files>docs/audit/28-claim-registry.md</files>
  <read_first>
    - `.planning/phases/29.1-per-role-model-assignment/29.1-VALIDATION.md` §"Test Infrastructure" — the fourteen-command phase gate block verbatim, and the ordering rule that `npm run freshness` runs BEFORE any build.
    - `.github/workflows/ci.yml` — CI's actual order, so the manual run reproduces it rather than approximating it.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-RESEARCH.md` §"Pitfall 7" and §"Pitfall 8" — the build/freshness ordering and the reason `npm test` must never be run.
    - Every prior SUMMARY in this phase — `29.1-01` through `29.1-05` — to confirm every discrimination proof was observed RED and reverted, and that no working-tree residue from a proof survives.
  </read_first>
  <action>
Run the phase gate block manually, in CI's order, and record each command's exit code and its
salient output line in the SUMMARY. Run `npm run freshness` FIRST, before any build, because it
reads the committed side with a git object read and an uncommitted build output is a named
failure rather than a silent pass.

The block, in order: `npm run freshness`, `npm run freshness:catalog`,
`npm run freshness:context`, `npm run freshness:adapters`, `npm run freshness:skill-twins`,
`node scripts/check-foundation-guards.js`, `node scripts/check-kit-refs.js`,
`node scripts/check-public-docs-vocabulary.js`, `node scripts/check-audit-register.js`,
`node scripts/check-claim-anchors.js`, `node scripts/check-banned-claims.js`,
`node scripts/check-imperative-lexicon.js`, `node scripts/check-diff-disposition.js`,
`node scripts/check-nul-bytes.js`.

Then run `npm run check:build-parity`, `npm run typecheck`, and the regression lane
`npx vitest run --exclude '**/scripts/e2e/**'`. Never `npm test` — it is the bare runner and
pulls the live authenticated end-to-end lane, which spends tokens and can hang.

Assert the supply-chain mitigation as ABSENCE at phase scope: confirm `package.json` and the
lockfile are byte-unchanged across the whole phase with a diff from the phase's base commit to
the current head, and record the empty result. This phase installs zero external packages, so
the mitigation is a demonstrated absence rather than an audit table.

Assert the out-of-scope boundary the same way: confirm the diff from the phase base to head
touches no path under `install/`, and touches neither configuration JSON nor
`scripts/measure-cost.ts` or its build output. Record each empty result.

Re-run the phase's own anchor cases together — the frozen pre-phase byte baseline, the
resolver, the generator, the freshness gate, the foundation guards and the cross-surface
oracle — and confirm the 17 committed adapters are still byte-identical to the pre-phase
baseline. That is the phase's headline claim and it is asserted last, against everything that
landed.

Append to the SUMMARY a short reconciliation of every recorded assumption and residual this
phase carries forward, each with an owner: assumption A1 and where it is recorded; the
byte-baseline case's dependence on the pinned commit remaining reachable; the disclosed
installed-target limitation and the phase that closes it; and the MODEL-07 fail-open direction
and the registry row that discloses it. Do not resolve any of them here — record them so the
verification round finds them stated rather than implied.
  </action>
  <verify>
    <automated>npm run freshness && npm run freshness:catalog && npm run freshness:context && npm run freshness:adapters && npm run freshness:skill-twins && node scripts/check-foundation-guards.js && node scripts/check-kit-refs.js && node scripts/check-public-docs-vocabulary.js && node scripts/check-audit-register.js && node scripts/check-claim-anchors.js && node scripts/check-banned-claims.js && node scripts/check-imperative-lexicon.js && node scripts/check-diff-disposition.js && node scripts/check-nul-bytes.js && npm run check:build-parity && npm run typecheck && npx vitest run --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - All fourteen gate commands exit 0, in CI's order, with `npm run freshness` run before any build. Each exit code and salient output line is recorded in the SUMMARY.
    - `npm run check:build-parity` exits 0 and `npm run typecheck` exits 0.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0. Record the file count, passed count and skipped count. `npm test` is never run.
    - `git diff --exit-code <phase-base>..HEAD -- package.json package-lock.json` is empty — the supply-chain mitigation asserted as absence at phase scope.
    - `git diff --name-only <phase-base>..HEAD -- install/` is empty — D-17's boundary held.
    - `git diff --name-only <phase-base>..HEAD -- agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json scripts/measure-cost.ts scripts/measure-cost.js` is empty — D-04 and D-14 held.
    - `npx vitest run scripts/adapter-byte-baseline.test.ts` exits 0 as the LAST assertion of the phase — the 17 committed adapters are byte-identical to the pre-phase baseline after every change landed.
    - `git status --porcelain` is clean — no discrimination-proof residue survives from any plan.
    - The SUMMARY carries the four-item forward-carried residual reconciliation, each with an owner.
  </acceptance_criteria>
  <done>Every gate in the phase block is green in CI's order, build parity and typecheck pass, the full regression lane passes without the live lane, the supply-chain and out-of-scope boundaries are asserted as demonstrated absences, the pre-phase byte baseline still matches, and every carried assumption and residual is reconciled with an owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public prose → a reader's belief about cost | An unmeasured saving stated as fact is the harm MODEL-07 exists to prevent |
| registry row → auditor | The row is the record an auditor reads to know how strongly a claim is held |
| phase base → head | Everything the phase changed, judged as one diff at close |

## STRIDE Threat Register

ASVS level 1, block-on `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-20 | Repudiation | an unmeasured cost claim shipping green | high | mitigate | Held as content: an anchored public claim byte-held by `check-claim-anchors`, registered with the fail-open direction disclosed from a DERIVED group list. Disclosure is the mitigation the increment ships; a fourth literal group is declined by D-16 with its reason recorded. |
| T-29.1-21 | Repudiation | a registry row implying stronger enforcement than exists | high | mitigate | The `mechanism` field states that the gate holds the SENTENCE and not the TOTALITY, and names the fail-open direction explicitly. A blank `mechanism` is refused by `check-audit-register`, proven RED. |
| T-29.1-22 | Tampering | a hand-typed registry total drifting from the real row count | medium | mitigate | The id and the count are derived at task start; the count assertion in `scripts/audit-model.test.ts` is itself run-time-derived; the dated header prose is annotated rather than rewritten. |
| T-29.1-05 | Tampering | a committed `.js` that is not a build of its `.ts` | high | mitigate | `npm run freshness` before any build plus `npm run check:build-parity` after — two independent mechanisms, both required, both in the close block. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Asserted as DEMONSTRATED ABSENCE at phase scope: `git diff --exit-code <phase-base>..HEAD -- package.json package-lock.json` is empty. `29.1-RESEARCH.md` §"Package Legitimacy Audit" records that this phase installs zero external packages, so there is no `[ASSUMED]` or `[SUS]` package and no legitimacy checkpoint is warranted. |
</threat_model>

<verification>
- The fourteen-command phase gate block exits 0 in CI's order, `npm run freshness` first.
- `npm run check:build-parity` and `npm run typecheck` exit 0.
- `npx vitest run --exclude '**/scripts/e2e/**'` exits 0. NEVER `npm test`.
- `npx vitest run scripts/adapter-byte-baseline.test.ts` exits 0 as the phase's last assertion.
- Phase-scope diffs are empty for `package.json`, the lockfile, `install/`, both config JSONs, and `scripts/measure-cost.*`.

**Human-verify (end-of-phase, no blocking checkpoint):**
<human-check>
Two readings, both manual by construction — VALIDATION.md's "Manual-Only Verifications" table
names them, and neither is a fact a gate can decide.

1. **The tier assignment on merit.** Read all 17 `rationale` strings in `scripts/model-tiers.ts`.
   The mechanism guarantees each reason is present, non-empty and co-located with the assignment
   it explains — not that you agree with it. `incident-responder` is recorded as the deliberately
   arguable one: an incident is where time pressure and blast radius coincide, and it was left on
   the cheaper tier because the role is procedural and hands to a named human. Dispute any
   assignment on quality grounds; that is what D-10 exists for.

2. **No unmeasured cost claim ships.** Read every document this phase touched —
   `agent-factory/packaging/subagent.frontmatter.md`, `agent-factory/config/factory.config.md`,
   `CLAUDE.md`, `agent-factory/writing-profile.md`, `docs/audit/28-claim-registry.md` — for cost,
   spend, saving or limit language. This is a human reading because no gate in this repository
   enumerates it, which is precisely what registry row `C-28-047` discloses. Record the
   disposition in that row rather than anywhere new.
</human-check>
</verification>

<success_criteria>
- The cost-claim prohibition is anchored content, byte-held at its anchor, with its fail-open direction disclosed from a derived group list and D-16's declining decision named with its reason.
- No fourth literal group exists and `scripts/measure-cost.ts` is byte-unchanged.
- The rationale half of MODEL-07 remains mechanical, with plan 29.1-02's blank-rationale RED as its evidence.
- The full phase gate block, build parity, typecheck and the regression lane are all green.
- The 17 committed adapters are byte-identical to the pre-phase baseline as the phase's last assertion.
- Every carried assumption and residual is reconciled in the SUMMARY with an owner.
</success_criteria>

<output>
Create `.planning/phases/29.1-per-role-model-assignment/29.1-06-SUMMARY.md` when done.
Record in it: the derived `BannedClaimGroup` member list; the derived claim id and the row count
before and after; the registry row's `mechanism` field verbatim; both gate-mutation RED
transcripts; every gate command's exit code and salient output; the regression lane's file,
passed and skipped counts; the five empty phase-scope diffs; and the four-item residual
reconciliation with owners.
</output>

---
phase: 29.1-per-role-model-assignment
round: 2
verified: 2026-08-20T13:10:00Z
status: gaps_found
score: 4/5 roadmap success criteria verified (up from 3/5 in round 1)
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "3/5 roadmap success criteria (plus 1 plan-declared must-have FAILED)"
  gaps_closed:
    - "CR-01 — the D-04 zero-config pin was half-blind to `models.roles`; now announces the whole resolution (member count, override count, distinct aliases) and refuses a mirrored run whose numbers disagree — reproduced end to end, both directions"
    - "WR-01 — unknown keys inside the `models` block were silently swallowed; now closed by `MODELS_KEYS`, refused before any legal key is read, with the file-level residual disclosed rather than implied"
    - "WR-02 — `resolveModels` accepted `preset: null` as `none`; Floor 0 now validates before it defaults"
    - "WR-03 — `resolvedPresetsIn` matched an incidental mention anywhere in a line; readers now anchor at byte 0 on a prefix the emitter owns"
    - "WR-04 — `adapters-freshness.ts` hand-spelled the marker it forbade hand-spelling; now consumes `mirroredResolvedPresetLine` from the one authority"
    - "WR-06 — the host-CLI scope sentence was an unsourced absolute about four third-party CLIs; replaced with a researched, HIGH-confidence, four-citation sentence plus three disclosed `UNKNOWN - verify` residuals; REQUIREMENTS.md and ROADMAP.md amended in lockstep"
    - "WR-07 — a malformed live `models` block had no fail-closed reader in CI; both guard degraded branches now `fail()`, and `generate:adapters` + an adapter diff were added to the ubuntu gate block, pinned at both ends"
    - "WR-08 — the guard's degraded-branch source label stated the opposite of what happened; corrected to state no source was established"
    - "IN-01, IN-02, IN-04, IN-05, IN-06 — production consumer given, absolute-path leak removed, stray-pin roster derived (14→16), dead initialisers deleted (definite-assignment now enforces the four-path fan-out), full 40-char baseline SHA pinned"
    - "IN-03 — the redundant bounds pair deleted from the vacuity case"
  gaps_remaining:
    - "R2-CR-01 (NEW, critical) — `relativeToRoot` in `scripts/check-foundation-guards.ts` builds an unanchored regex from an unvalidated, unresolved `CHECK_ROOT`. Under a relative root (`.`, the documented and realistic case) it corrupts the model-assignment guard's own verdict AND refusal-reason text — reproduced independently on this tree: `CHECK_ROOT=. node scripts/check-foundation-guards.js` prints a green PASS naming `agent-factory/config/factoryconfig.json`, a file that does not exist. This is the exact WR-08 defect class (a verdict that misdescribes the run it performed), reintroduced by the very fix (29.1-09, IN-02) that closed WR-08. Not exercised by any existing case — every planted mirror in the suite uses an absolute root."
  regressions:
    - "MODEL-05 regresses from Complete to Gaps Found: the guard this requirement names as the second opinion on the emitted model value can now misdescribe its own verdict and its own refusal reasons under a realistic, documented invocation shape (a relative `CHECK_ROOT`). The comparison's pass/fail boolean is unaffected — only its self-description is corrupted — but a green verdict that names a file that does not exist is precisely the failure class this project has paid for repeatedly, and the round's own WR-08 fix was meant to retire it, not relocate it."
overrides: []
gaps:
  - truth: "SC-3 — anything other than the four legal aliases is refused fail-closed, and the guard's own verdict is trustworthy. (MODEL-04, MODEL-05)"
    status: partial
    reason: "The ALIAS vocabulary and the `models`-block key set are now genuinely closed (round-1's WR-01/WR-02 both fixed and independently re-verified). What is NOT closed is the guard's own verdict integrity: `relativeToRoot` corrupts every rendered path — including PASS-line source labels and FAIL-line refusal reasons — whenever `CHECK_ROOT` is relative, because `ROOT` is taken from `process.env.CHECK_ROOT` with no `resolve()` and compiled into an unanchored, un-separator-bounded regex. Reproduced independently: `CHECK_ROOT=. node scripts/check-foundation-guards.js` exits 0 and prints `preset \"none\" from agent-factory/config/factoryconfig.json` — the period-eating substring match silently deleted a byte from a real, correctly-resolved path. The comparison logic (pass/fail) is unaffected by this bug; only the verdict's self-description is. That distinction does not save it: this project's own history (MEMORY, and this same round's WR-08 finding) treats a green verdict that misdescribes its own run as the worst-case failure mode, not a cosmetic one."
    artifacts:
      - path: "scripts/check-foundation-guards.ts"
        issue: ":374-376 accepts `CHECK_ROOT` verbatim, relative or absolute, with no `resolve()` and no refusal of a non-absolute value. :1917-1925 `relativeToRoot`'s `ROOT_PATH_RE` is `${escaped(ROOT)}([^\\s'\"\\`]*)` — a global, unanchored match with no requirement that a path separator (or end-of-token) follow the root, so `ROOT=\".\"` becomes `\\.([^\\s'\"\\`]*)`, matching any period followed by non-whitespace anywhere in ANY rendered line, not just a path. Applied to `config.reason` and `resolution.reason` too (:2012, :2037), so a FAILING verdict's refusal text can be corrupted in the same way, potentially sending an operator to a fabricated file name."
    missing:
      - "Resolve CHECK_ROOT before use: `const ROOT = process.env.CHECK_ROOT ? resolve(process.env.CHECK_ROOT) : join(import.meta.dirname, \"..\");` — or refuse a non-absolute override outright, naming the value."
      - "Require a path-boundary after the match (`(?=[\\\\/]|[\\s'\"\\`]|$)`) so the regex is a path-prefix match rather than an arbitrary substring match, per the round-2 review's proposed fix."
      - "Add a case that runs the guard (or the pure helper) with a RELATIVE `CHECK_ROOT` and asserts the verdict still names `agent-factory/config/factory.config.json` verbatim — today's cases (o) and (o-reason) only exercise absolute mirror roots, so neither can see this."
human_verification:
  - test: "MODEL-06 D5 (carried from plan 29.1-10): read the four vendor citations (Codex CLI, Gemini CLI, OpenCode, GitHub Copilot CLI in `29.1-RESEARCH.md` §'Host-CLI per-agent model support') against their live vendor pages, and confirm the authority sentence, the amended MODEL-06 text and the amended ROADMAP SC-4 genuinely agree in substance."
    expected: "Each citation's quoted claim (a per-agent `model` field in that CLI's own agent-definition format) matches what the vendor page currently states; the three prose surfaces assert nothing beyond (a) grugops emits per-agent adapters for Claude Code alone and (b) every claim about the other four carries a named source."
    why_human: "Citation fidelity to external vendor documentation is not a predicate this verifier's tooling can check — I can confirm the citations are present, dated, specific, and internally consistent (which I did), but not that the vendor pages still say what is quoted. This is the same class of check this repository's hard rule ('never fake a citation') exists to route to a human rather than to a grep."
  - test: "D6 (carried from plan 29.1-11): decide whether narrowing the WR-05 packaging-authority documentation obligation by AUDIENCE (developer-facing config reference vs. shipped kit prose) is the right disposition, given round-2 review's R2-WR-05 finding that `install/install.ts:1065` (`cpSync` of the whole `agent-factory/` tree, `config/` included) ships `agent-factory/config/factory.config.md` to every installed user — contradicting the premise that the config field reference is 'outside' the shipped-kit-prose concern."
    expected: "Either accept the split as-is (the actual precedence documentation IS present, correct and complete in a shipped file, even if the stated justification for exempting it from `check-kit-refs`'s scan was imprecise), or add `agent-factory/config` to `check-kit-refs`'s SCAN with an explicit, argued exemption for the two kit-internal path mentions it would then need to carry."
    why_human: "Two ratified contracts collided (D-08.1's zero-tolerance scan vs. WR-05's documentation completeness) and the executor's chosen resolution is a scope call, not a mechanical predicate. This verifier confirmed R2-WR-05's underlying fact (`install.ts:1065` does ship `agent-factory/config/`) but the disposition itself is the same open item plan 29.1-11 already routed to human judgment as D6 — this round sharpens the evidence, it does not close the question."
  - test: "MODEL-07/boundary and MODEL-07/precision (carried unchanged from round 1, plan 29.1-06 `verification: backstop`) — confirm no phase-touched prose asserts a cost, spend or usage-limit saving."
    expected: "Zero cost or limit-savings claims. Round 2's mechanical re-scan (this verifier and the round-2 review) found only honest denials and unchanged pre-existing wording; `measure-cost.ts`/`.js` remain byte-unchanged across the whole round (`git diff aa26bd8..HEAD -- scripts/measure-cost.ts scripts/measure-cost.js` empty)."
    why_human: "Unchanged from round 1: the boundary between a cost claim and a neutral mechanism description is not a decidable predicate here, and this is the phase's own declared human-adjudication item."
---

# Phase 29.1: Per-Role Model Assignment — Gap-Closure Round 2 Verification Report

**Phase Goal:** A project can spend its model budget where judgment actually lives — a stronger model for orchestration, architecture and security review, a cheaper one for implementation, QE and UAT — set once on the config dial and emitted into every generated adapter, with zero-config behaviour byte-identical to today's.

**Verified:** 2026-08-20T13:10:00Z
**Status:** gaps_found
**Re-verification:** Yes — round 2, after gap-closure plans 29.1-07 .. 29.1-11 (all 5 executed, sequential waves 1-5)
**Method:** Every load-bearing claim from round 1's gaps and round 2's code review re-checked against the live tree. Independent reproduction, not SUMMARY acceptance, for every finding that could bear on phase status. `29.1-VERIFICATION.md` (round 1) is left byte-unchanged — not re-read as a template, only as the baseline this round closes against.

## Summary

Round 1 returned `gaps_found` at 3/5 roadmap success criteria plus one plan-declared FAILED must-have, and a code review returned 1 critical / 8 warnings / 6 info (15 findings total). The user ruled all fifteen in scope. Five sequential gap-closure plans executed. A round-2 code review then found 1 new critical, 6 warnings, 5 info — and confirmed 14 of round 1's 15 findings genuinely closed by mechanism (WR-02 partial).

I independently re-verified the load-bearing claims rather than accepting either round's SUMMARY or REVIEW text as evidence:

- **Zero-config byte identity still holds.** `node scripts/generate-role-adapters.js && git diff --exit-code -- .claude/agents/` → empty, exit 0. `grep -h '^model:' .claude/agents/*.md | sort | uniq -c` → `17 model: inherit`. Neither shipped config JSON carries a `models` key.
- **Build parity holds.** `npm run freshness` → 49/49 fresh. `npm run typecheck` → exit 0.
- **The full phase-scoped test suite is green.** `npx vitest run` over the six files under review (`model-tiers`, `generate-role-adapters`, `adapters-freshness`, `check-foundation-guards`, `model-dial-consistency`, `adapter-byte-baseline`) → 442 passed, 1 skipped — matching the round-2 review's own count.
- **All non-`CHECK_ROOT` gates are green on this tree**: `check-foundation-guards.js`, `check-kit-refs.js`, `check:banned-claims`, `check:claim-anchors`, `check:public-docs`, `check:audit-register` all report ALL CHECKS PASSED.
- **R2-CR-01 (the round-2 review's critical finding) is real and I reproduced it independently**, not merely re-read the review's transcript: `CHECK_ROOT=. node scripts/check-foundation-guards.js` prints a PASS line naming `agent-factory/config/factoryconfig.json` — a file that does not exist on this or any tree (the real file is `factory.config.json`; the leading dot of the relative root ate a character out of the real path via an unanchored, un-bounded regex substitution). Confirmed by reading `scripts/check-foundation-guards.ts:374-376` (no `resolve()`, no refusal of a relative override) and `:1917-1925` (`relativeToRoot`'s regex has no separator-boundary requirement after the matched root).
- **MODEL-06's underlying research is real and specific**, not merely asserted: `29.1-RESEARCH.md` §"Host-CLI per-agent model support" carries four dated, URL-cited, HIGH-confidence, verbatim-quoted findings (Codex CLI TOML `model` key, Gemini CLI frontmatter `model` defaulting to `inherit`, OpenCode frontmatter/`opencode.json` `model`, GitHub Copilot CLI `.agent.md` `model` property). The authority sentence in `subagent.frontmatter.md:251` matches this research, is guarded by a 30-case oracle (`model-dial-consistency.test.ts`, all green), and the refuted premise (`grep -rn 'have no per-subagent model concept'` across all planning and kit surfaces) returns nothing.
- **R2-WR-04 and R2-WR-05 are also real** (independently confirmed: the packaging-authority bullet does state the precedence clause it claims not to restate; `install/install.ts:1065` does `cpSync` the whole `agent-factory/` tree including `config/` to every installed user) but are documentation-honesty/audit-trail issues, not functional breaks — the actual precedence behaviour is correctly and completely documented in a shipped file (`factory.config.md`), and both are routed to human judgment rather than treated as blockers.

**One regression:** MODEL-05 moves from Complete back to Gaps Found. The critical finding is a fresh instance of the exact defect class (a green verdict misdescribing its own run) that this same round's WR-08 fix was built to retire — reintroduced by that very fix (plan 29.1-09, the IN-02 closure) — and it is unexercised by any case in the suite because every existing case plants an absolute mirror root.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Zero-config: all 17 adapters byte-identical to today's, `model: inherit`. (MODEL-01, MODEL-02) | ✓ VERIFIED | Independently re-run this session: `git diff --exit-code -- .claude/agents/` empty; `17 model: inherit`; both config JSONs carry no `models` key. CR-01's fix (plan 29.1-07) additionally makes the pin observe the WHOLE resolution (member/override count + alias set), not just the preset half — reproduced end-to-end in `29.1-07-SUMMARY.md` and independently plausible from reading the committed code (`MIRRORED_RESOLVED_PRESET_PREFIX`, the assignment-grammar readers, all present and anchored at byte 0). |
| 2 | One opt-in key switches to the preset; role→tier derived against `kit-model.listRoles()` with an asserted count. (MODEL-03) | ✓ VERIFIED | Unchanged from round 1's independent reproduction (tiered mirror → 4 opus/13 sonnet, 18th-role mirror → guard exit 1 naming both cardinalities). `tieredCorpusRefusals` now has a production consumer (`check-foundation-guards.ts:2070`, confirmed present). Caveat: this requirement shares `guard_model_assignment` with MODEL-05, whose verdict-rendering is the subject of the new regression below — the DERIVATION logic this requirement is about is unaffected by that bug (confirmed: `relativeToRoot` only rewrites rendered text, never the comparison inputs). |
| 3 | Only `inherit`/`opus`/`sonnet`/`haiku` legal; anything else refused fail-closed; emitted model asserted equal to the resolved config. (MODEL-04, MODEL-05) | ✗ FAILED | **The alias and `models`-block closures now hold** (round 1's WR-01/WR-02 both independently re-verified fixed: `{"models":{"presets":"tiered"}}` now refused by name; `resolveModels(stems,{preset:null})` now refused by name). **The guard's own verdict-and-refusal-text integrity does not hold.** R2-CR-01, reproduced independently on this tree (see Summary). See split table below. |
| 4 | Claude-Code-only scope stated in the kit and in `CLAUDE.md`; every clause about the other four host CLIs carries a named source. (MODEL-06) | ✓ VERIFIED | Narrowed and researched this round (plan 29.1-10). Authority sentence (`subagent.frontmatter.md:251`) matches HIGH-confidence, dated, four-CLI vendor research; refuted premise absent everywhere (`grep` returns nothing); 30-case oracle green; `CLAUDE.md:125` confirmed unchanged and independently free of the refuted premise (it names only the mechanism, not a capability claim about the other four). Citation-fidelity-to-vendor-pages remains a human-verification item (unchanged in kind from round 1, now narrower and evidence-backed). |
| 5 | Any cost or limit-savings claim is measured or carries `UNKNOWN - verify`. (MODEL-07) | ✓ VERIFIED (mechanical) + backstop | Unchanged from round 1. `git diff aa26bd8..HEAD -- scripts/measure-cost.ts scripts/measure-cost.js` empty (checked across the WHOLE round, not just round 1). No new cost-adjacent language introduced by plans 07-11 (independently greped: `grep -inE 'cheaper|saving|spend less|reduce cost|lower cost|quota'` over every phase-touched prose file this round returns nothing). Totality half remains `verification: backstop` → human item. |

**Score: 4/5 roadmap success criteria verified** (up from 3/5 in round 1). SC-3 remains FAILED, on a different axis than round 1: round 1's "unknown-`models`-key" and "preset:null" holes are closed; a NEW hole (the guard's verdict-rendering integrity) opened in the same round that closed WR-08, the finding that named the sibling defect.

### Criterion 3, split by clause (re-assessed)

| Clause | Status | Evidence |
|--------|--------|----------|
| Only the four aliases are legal | ✓ VERIFIED | Unchanged from round 1 — 18 value shapes, exact equality, no coercion. |
| The `models` block's own key set is closed | ✓ VERIFIED (NEW this round) | `MODELS_KEYS = ["preset","roles"]`, adjudicated first in `readModelsBlock`. Reproduced: `{"models":{"presets":"tiered"}}` now refused naming `` `models.presets` ``. File-level residual (singular `model` key) disclosed in source, by a titled case, and in `factory.config.md`. |
| Preset vocabulary closed including nullish | ✓ VERIFIED (NEW this round) | `resolveModels(stems,{preset:null})` now refused by name, reproduced independently: `node -e '...'` returns `{ok:false, reason:"model-tiers: null is not a legal preset name..."}`. |
| The container-level `overrides` value is validated with the same rigour | ⚠️ PARTIAL (unresolved, WARNING not blocking) | Reproduced independently: `resolveModels(["a","b"],{preset:"none",overrides:null})` silently resolves to `inherit` for every stem (no refusal); `resolveModels(["a","b"],{preset:"none",overrides:{a:"opus"}})` throws an uncaught `TypeError`. **Not reachable from the live config-reading path** — `readModelsConfig` always constructs a proper `Map` (confirmed by reading `scripts/model-tiers.ts:1203-1254`) — so this is latent in the same sense round 1's WR-02 was before this round fixed it. Recorded as a carried-forward warning, not a phase blocker. |
| Anything else refused fail-closed AND the refusal/verdict text is trustworthy | ✗ FAILED | Unknown keys and nullish presets are now refused (closed). **The guard's own rendering of BOTH its passing verdict and its refusal reasons is corrupted under a realistic, previously-undocumented-as-safe invocation** (`CHECK_ROOT` set to a relative value) — see the gap above. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/model-tiers.ts` / `.js` | Pure resolver, tier table, alias contract, closed `models`-block key set, two-location reader | ✓ VERIFIED | `MODELS_KEYS`, `MODELS_CONFIG_CANDIDATE_RELS`, validated Floor 0, all present and read; committed `.js` confirmed fresh (`npm run freshness`: 49/49). |
| `scripts/generate-role-adapters.ts` / `.js` | Announces the whole resolution, not one input | ✓ VERIFIED | `resolved model assignment: {"roles":17,"overrides":0,"aliases":["inherit"]}` — reproduced independently this session on the live tree. |
| `scripts/adapters-freshness.ts` / `.js` | D-04 pin observes the whole resolution; no hand-spelled marker | ✓ VERIFIED | `grep -c 'Mirrored generator resolved model preset: '` = 0 in both `.ts` and `.js` (confirmed via the round-2 review's reproduction, consistent with reading the source); `mirroredResolvedPresetLine` consumed from the one authority. |
| `scripts/check-foundation-guards.ts` / `.js` | `guard_model_assignment`, both degraded branches fail the build, no absolute-path leak, no false source label | ⚠️ MECHANISM REGRESSED | Both degradations now `fail()` (confirmed: no `warn(` call anywhere in the function body, per the round-2 review's stripped-comment probe and this verifier's reading). The absolute-path fix (`relativeToRoot`) that closed IN-02/WR-08 introduced a NEW correctness bug — reproduced independently — that corrupts rendered text under a relative `CHECK_ROOT`. |
| `.github/workflows/ci.yml` | `generate:adapters` + adapter diff in the ubuntu gate block | ✓ VERIFIED | Read directly: both commands present in the block, in the right order, ahead of `check-foundation-guards.js`. Not executed on an actual GitHub runner this session (same `UNKNOWN - verify` disclosed by plan 29.1-09, unresolved and correctly labeled). |
| `agent-factory/packaging/subagent.frontmatter.md` | Single scope authority, researched and cited | ⚠️ VERIFIED WITH A DISCLOSED FLAW | Sentence matches research, cited, oracle-guarded. R2-WR-04 confirmed: the `model` bullet's precedence clause ("only when that file is absent entirely...") does restate the whole-file-precedence rule it claims, one sentence later, not to restate — a genuine but narrow documentation-honesty defect, not a functional one (the ONE-authority document, `factory.config.md`, is itself correct and complete). |
| `agent-factory/config/factory.config.md` | `models` row + sub-fields + two-location precedence + shadowing consequence stated | ✓ VERIFIED | Read directly: `:138` states "The first of those two files that EXISTS wins WHOLE" and the shadowing consequence in full, correct, plain prose. |
| `CLAUDE.md` | "What NOT to Use" row amended | ✓ VERIFIED | Unchanged since round 1; `git diff --name-only -- CLAUDE.md` empty across the whole round-2 series; row independently re-read and confirmed free of the refuted premise. |
| `scripts/model-dial-consistency.test.ts` | Cross-surface oracle, derived roster, derived citation coverage | ✓ VERIFIED | 30 cases, all green (`npx vitest run scripts/model-dial-consistency.test.ts` — 30 passed). Surface roster now derived from a run-time scan (`SURFACE_COUNT = derivedDialSurfaces().prose.length`), independently confirmed by reading the file — no more hand-listed two-sided pin (round 1's IN-flagged defect). |
| `scripts/adapter-byte-baseline.test.ts` | Full 40-char SHA | ✓ VERIFIED | `grep -c "6f8411effe80b3f22d0d668e1fa40fa78e3a8088"` = 2; `grep -cE '"6f8411e"'` = 0 (per SUMMARY, consistent with the file's stated intent). |
| `.planning/phases/29.1-per-role-model-assignment/COVERAGE.md` | No-integration declaration, measured length | ✓ VERIFIED | Read directly: 194-character declaration line, under the 200-char cap; agrees with the `api-coverage.verify-pre` detector. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `generate-role-adapters.ts` | `model-tiers.js` | resolved assignment announcement | ✓ WIRED | Reproduced live this session. |
| `adapters-freshness.ts` | `model-tiers.js` | `mirroredResolvedPresetLine`, anchored readers | ✓ WIRED | Consistent with round-2 review's byte-identity comparison. |
| `check-foundation-guards.ts` | `model-tiers.js` + `kit-model.js` | `resolveModels`/`readModelsConfig`/`tieredCorpusRefusals` | ⚠️ WIRED, VERDICT RENDERING CORRUPTED UNDER A RELATIVE ROOT | Comparison logic unaffected; rendered text (PASS source label, FAIL refusal reasons) is corrupted — reproduced independently. |
| `.github/workflows/ci.yml` | `generate:adapters` + guard | ubuntu gate block ordering | ✓ WIRED (by reading; not runner-executed) | Read directly, order confirmed; `UNKNOWN - verify` on live-runner behaviour stands, correctly disclosed. |
| `factory.config.md` | `subagent.frontmatter.md` | one-way pointer, no restatement of the LITERAL string | ⚠️ WIRED, WITH A PARAPHRASE RESIDUAL | The oracle's literal-string absence check passes; the pointer's OWN clause paraphrases the rule it says it does not restate (R2-WR-04, confirmed). |

### Data-Flow Trace (Level 4)

| Artifact | Data variable | Source | Produces real data | Status |
|----------|---------------|--------|--------------------|--------|
| generated adapter `model:` field | `resolution.value.get(stem)` | `resolveModels(stems, readModelsConfig(ROOT, stems))` | Yes — reproduced live | ✓ FLOWING |
| `guard_model_assignment` expectation (pass/fail boolean) | `resolved` map | recomputed independently | Yes — unaffected by the rendering bug | ✓ FLOWING |
| `guard_model_assignment` VERDICT TEXT (source label, refusal reason) | `relativeToRoot(...)` output | `ROOT` (unvalidated, unresolved) | **No, under a relative `CHECK_ROOT`** — the rendered path is a corrupted substring rewrite, not a faithful relative path | ✗ CORRUPTED (R2-CR-01) |
| freshness pin input | announced assignment grammar | the whole resolution (member/override count, aliases) | Yes — this round's fix | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Zero-config regeneration is byte-stable | `node scripts/generate-role-adapters.js && git diff --exit-code -- .claude/agents/` | empty, exit 0 | ✓ PASS |
| Build parity | `npm run freshness` | 49/49 fresh | ✓ PASS |
| Typecheck clean | `npm run typecheck` | exit 0 | ✓ PASS |
| Phase-scoped suite green | `npx vitest run` over the 6 phase files | 442 passed, 1 skipped | ✓ PASS |
| Default-root foundation guard | `node scripts/check-foundation-guards.js` | ALL CHECKS PASSED | ✓ PASS |
| Kit-refs / claim / public-doc / audit-register gates | 4 commands | ALL CHECKS PASSED (×4) | ✓ PASS |
| **Guard verdict integrity under a relative CHECK_ROOT** | `CHECK_ROOT=. node scripts/check-foundation-guards.js` | exit 0, PASS line names `agent-factory/config/factoryconfig.json` (missing a `/`, no such file) | ✗ FAIL (R2-CR-01, reproduced independently) |
| `overrides: null` refused like `preset: null` | `node -e 'resolveModels(["a","b"],{preset:"none",overrides:null})'` | `{"ok":true,"value":{}}` — silently resolves, no refusal | ✗ FAIL (R2-WR-01, latent/non-blocking — reachable only through a manual non-Map caller, not through `readModelsConfig`) |
| `overrides` as a plain object | `node -e 'resolveModels(["a","b"],{preset:"none",overrides:{a:"opus"}})'` | uncaught `TypeError: object is not iterable` | ✗ FAIL (same finding, same non-blocking disposition) |
| MODEL-06 refuted premise absent everywhere | `grep -rn 'have no per-subagent model concept' agent-factory/ CLAUDE.md README.md docs/ .planning/REQUIREMENTS.md .planning/ROADMAP.md` | no output, exit 1 | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` files exist and none is declared by any plan or SUMMARY in this round. **Step 7c: SKIPPED (no probe files; this project's equivalent — the gate scripts — was executed above).**

### Requirements Coverage

| Requirement | Round-1 status | Round-2 status | Evidence |
|-------------|-----------------|-----------------|----------|
| MODEL-01 | ✓ SATISFIED (caveat: CR-01 pin incomplete) | ✓ SATISFIED — caveat CLOSED | The CR-01 pin now observes the whole resolution; zero-config byte-identity re-verified. |
| MODEL-02 | ✓ SATISFIED | ✓ SATISFIED (documentation-honesty warning, non-blocking) | Resolved value emitted, confirmed. R2-WR-04's paraphrase residual is a warning against the "single authority" documentation ideal, not against MODEL-02's functional claim. |
| MODEL-03 | ✓ SATISFIED | ✓ SATISFIED | Tiered derivation, asserted count, role-#18 detection all re-confirmed; `tieredCorpusRefusals` now has a production consumer. Shares `guard_model_assignment` with MODEL-05's regression, but the derivation/count logic this requirement names is unaffected by the verdict-text bug. |
| MODEL-04 | ✓ SATISFIED | ✓ SATISFIED | Alias vocabulary closed (unchanged); `models`-block key set now also closed (round-1's WR-01 fixed). The unresolved `overrides` container-validation gap (R2-WR-01) is latent, not reachable from the live config path, and is recorded as a carried-forward warning rather than a blocker. |
| MODEL-05 | ✓ SATISFIED (warning: WR-07 fail-open) | ✗ **REGRESSED to BLOCKED** | WR-07/WR-08 (the round-1 warnings) are closed by mechanism, confirmed independently (both degraded branches now `fail()`; no absolute-path leak on the default root). But the very fix that closed them (`relativeToRoot`) introduces R2-CR-01: the guard's verdict and refusal text can be corrupted under a realistic invocation shape this project's own tooling documents as legitimate (a `CHECK_ROOT` override "point[ing] the gate at a hermetic mirror"). A requirement whose text is "a guard asserts the emitted model... " cannot be marked satisfied while the guard's assertions can misdescribe themselves. |
| MODEL-06 | ✗ BLOCKED | ✓ **SATISFIED — narrowed and closed this round** | Research is HIGH-confidence, dated, cited, four CLIs; authority sentence matches it; refuted premise absent everywhere; REQUIREMENTS.md and ROADMAP.md amended in lockstep, describing (not reproducing) the superseded wording. Citation-fidelity-to-vendor-pages remains routed to human verification, as it must (this verifier cannot browse the cited vendor pages), but that residual does not block the requirement's own text, which only requires a named source per clause — satisfied. |
| MODEL-07 | ✓ SATISFIED (mechanical) / ? human (totality) | ✓ SATISFIED (mechanical) / ? human (totality), unchanged | `measure-cost.ts`/`.js` byte-unchanged across the whole round; no new cost-adjacent language in any phase-touched file this round. |

**Recommended traceability update:** MODEL-06 → Complete. MODEL-05 → Gaps Found (regressed from the current `Complete` marking; a gap-closure round 3 plan is needed, scoped narrowly to `relativeToRoot`). MODEL-01/02/03/04/07 stand as currently marked.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/check-foundation-guards.ts` | 374-376, 1917-1925 | Unvalidated, unresolved `ROOT` compiled into an unanchored, un-bounded regex used to rewrite verdict AND refusal text | 🛑 Blocker | A green verdict can name a file that does not exist; a failing verdict's refusal reason can be corrupted, sending an operator to a fabricated path. Reproduced independently; unexercised by any existing case (all mirror roots in the suite are absolute). |
| `scripts/model-tiers.ts` | 1025 | `options?.overrides ?? []` — nullish coalesce before any shape check, same class as the already-fixed preset Floor 0 | ⚠️ Warning | `overrides: null` silently discarded; a plain-object `overrides` throws uncaught. Not reachable via `readModelsConfig` today (latent), same disposition round 1 gave the now-fixed preset case. |
| `agent-factory/packaging/subagent.frontmatter.md` | 202-215 | The `model` bullet states the whole-file precedence rule ("only when that file is absent entirely...") in the same paragraph that claims it "deliberately does not restate the rule" | ⚠️ Warning | A genuine paraphrase-vs-literal gap in the "one authority" design goal; the oracle only pins a literal string's absence, so any rewording of the rule clears it. Not a functional defect — `factory.config.md` is itself correct and complete. |
| `scripts/check-foundation-guards.test.ts` | ~10170-10185 | The CI-wiring pin (`ci.indexOf(...); ci.slice(at)`) has no right bound and does not strip comments before probing | ⚠️ Warning (fragility, not a live defect) | Today's block happens to be the last step in the file, so the slice is accidentally correct; a future step appended after it, or a comment mentioning the command, would clear the case with the real command gone. Confirmed by reading the case; not independently mutated this session (round-2 review already ran and recorded this mutation). |
| `scripts/check-foundation-guards.ts` | 2172-2178 | Comment claims the packaging templates are skipped by the `!parsed.ok` (unadmittable) branch; measured, they are admitted and skipped by the `!parsed.value.has("model")` branch instead | ℹ️ Info | The soundness argument for the silent `continue` names the wrong mechanism; the `!parsed.ok` branch is unexercised and unproven on this tree. Confirmed consistent with the round-2 review's own measurement (not independently re-run against `canonical-frontmatter.js` this session). |
| `scripts/model-tiers.ts` | 231 (`MODELS_KEYS`) | Tuple closes key PRESENCE, not key CONSUMPTION — a key added to the tuple with no reader is admitted, ignored, and reported as the zero-config answer | ℹ️ Info | Comment overstates what the tuple buys ("moves the mechanism and the message together" — it moves only the message). No live defect on today's two-key tuple. |

No `TBD`/`FIXME`/`XXX` markers found in any file touched by plans 07-11 (checked directly this session).

### Deferred Items

None newly identified this round. Phase 29.2's stated scope (install-delivery path) still does not cover R2-CR-01, R2-WR-01, R2-WR-04, or R2-WR-05 — none of these concerns the install path; all are in-kit resolver/guard/documentation defects. No gap is deferred to 29.2.

### Coincidental-Reliance Check

No `✓ VERIFIED` truth in this report rests on an undeclared precondition, incidental ordering, or a fixture-only setup that the production path lacks. The zero-config byte-identity truth, the tiered-derivation truth, and the MODEL-06 truth were each re-derived from a live run on this tree (not a fixture), so no `coincidental_reliance_items` entry applies.

### Gaps Summary

Round 2 closed fourteen of round 1's fifteen findings by genuine mechanism — I independently re-verified the load-bearing ones (the CR-01 whole-resolution announcement, the `models`-block key closure, the preset-null refusal, the CI-wiring commands, the MODEL-06 research and its citations) rather than trusting the SUMMARYs' transcripts. That is real, substantial progress, and it moves the roadmap score from 3/5 to 4/5.

But the round did not close cleanly, and the reason matters more than the count. **Plan 29.1-09's fix for WR-08 (the guard's degraded-branch source label stating the opposite of what happened) introduced a new defect in the identical class**: `relativeToRoot`, the helper built specifically to stop the guard's verdict from leaking information it shouldn't, instead makes the verdict able to say something that is not true — a file name that does not exist, printed on a PASS line, silently. I reproduced this independently, not from the round-2 review's transcript alone: `CHECK_ROOT=. node scripts/check-foundation-guards.js` really does exit 0 and really does print `agent-factory/config/factoryconfig.json`. The bug is a straightforward regex-anchoring mistake — the fix is small (resolve the root, require a path boundary after the match) — but its SEVERITY is not small in this project's own terms: this exact repository's memory records "a passing test suite is NOT evidence a guard works" and "assert the verification harness's own premise," and this defect is precisely a harness (the guard) whose own premise (repo-relative, honest paths) does not hold under an input shape the harness's own header documents as legitimate. It went unexercised by 442 passing tests because every existing case constructs its mirror with an absolute path.

MODEL-05's requirement text is "a guard asserts the emitted model of all 17 adapters equals the resolved config" — the assertion's boolean result is unaffected by this bug, but a guard whose own assertions can misdescribe themselves is not the guard MODEL-05 asked for. I am regressing it to Gaps Found rather than accepting a caveat, because this project's own precedent (WR-08, the finding this bug reintroduces) treated the identical class of defect as blocking in round 1.

Two further findings (R2-WR-04, R2-WR-05) are real — I confirmed both independently rather than taking the review's word — but they are documentation-honesty and audit-trail issues, not functional breaks: the actual precedence rule and the actual scope statement are both correctly and completely stated somewhere a user or developer will find them. Both are correctly routed to human judgment (D5, D6) rather than treated as blockers, consistent with how the executing plans themselves flagged them.

MODEL-06 is the one clean win this round: the research is specific, dated, cited, and directly checkable in its structure (which I did); only its fidelity to the live vendor pages needs a human, and that is a narrower and better-evidenced ask than round 1's "settle whether this claim is even researched at all."

---

_Verified: 2026-08-20T13:10:00Z_
_Verifier: Claude (gsd-verifier)_
_Round 1's `29.1-VERIFICATION.md` is unmodified by this report. Every finding above marked "reproduced independently" or "confirmed by reading" was executed or read in this session, not taken from either round's SUMMARY or REVIEW text._

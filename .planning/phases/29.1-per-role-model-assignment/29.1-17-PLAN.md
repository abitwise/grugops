---
phase: 29.1-per-role-model-assignment
plan: 17
type: execute
wave: 6
depends_on: ["29.1-16"]
gap_closure: true
files_modified:
  - scripts/check-kit-refs.ts
  - scripts/check-kit-refs.js
  - scripts/check-kit-refs.test.ts
  - scripts/model-dial-consistency.test.ts
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - .planning/phases/29.1-per-role-model-assignment/29.1-11-SUMMARY.md
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-05, MODEL-07]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "R2-WR-05 / D6 closed as the USER RULED: `agent-factory/config` is added to `check-kit-refs`'s SCAN, and Assertion 1 carries an explicit, argued exemption for the kit-internal path mentions the config field reference must keep. The former exclusion was by omission rather than by an audience judgment, and the premise that made the omission look principled is refuted by the installer, measured rather than asserted."
    - "The exemption is DERIVED, not hand-listed: a hit is exempt only when the file carrying it is itself inside the configuration directory AND the kit-internal path it names resolves to a file that exists in that directory on the tree under judgement. Anything else is a stray and fails exactly as before."
    - "The exemption's CARDINALITY is asserted two-sided in the gate itself — the number of exempt files and the number of exempt hits — with both numbers named in the failure text, so a third mention or a second exempting file fails loudly rather than passing silently. That is this repository's own remedy for the set-literal-drift class, applied to the exemption in the commit that creates it."
    - "The exemption is VISIBLE in the verdict: the Assertion 1 pass line publishes how many hits were exempt and in which file, so a reader of a green run learns the exemption exists rather than inferring it from silence."
    - "A vacuity floor guards the exemption's denominator: a tree under judgement that carries no configuration directory at all is a NAMED finding, not a silent zero. An absent directory would otherwise make the cardinality assertion fail for a reason nobody could attribute, or pass over nothing."
    - "The mentions could not simply be deleted, and the reason is recorded rather than assumed: `scripts/model-dial-consistency.test.ts` asserts the config field reference names BOTH configuration locations — the WR-05 closure — and the second location IS the kit-internal path. Deleting the mentions would reopen WR-05 to close R2-WR-05."
    - "The second hand-maintained copy of the scan set is DELETED: `scripts/check-kit-refs.test.ts` no longer carries its own `SCAN` literal. It derives the set from the gate's own source, bounded at both ends, with the member count asserted, so the mirror the cases plant into can no longer fall behind the set the gate walks."
    - "Three stale recorded premises are corrected in the same commit as the change that falsifies them: the oracle's WR-05 comment block (which states the config field reference lives outside the scan set and that the gate admits no exemption list), the foundation guards' set-literal inventory row for this scan set, and `29.1-11-SUMMARY.md`'s split-by-audience key decision — the last ANNOTATED rather than rewritten, per this project's convention."
    - "No silent drops: all twelve round-2 findings (R2-CR-01, R2-WR-01..06, R2-IN-01..05) and all three carried human items (D5, MODEL-07/boundary, MODEL-07/precision) are reconciled in a disposition table naming, for each, the plan that closed it or the reason it is carried."
    - "The round closes green: the full phase gate block from `29.1-VALIDATION.md` runs in CI's order with `npm run freshness` first, and every command exits 0."
  artifacts:
    - scripts/check-kit-refs.ts
    - scripts/check-kit-refs.js
    - scripts/check-kit-refs.test.ts
    - .planning/phases/29.1-per-role-model-assignment/29.1-11-SUMMARY.md
  key_links:
    - "`install/install.ts`'s `copyKit` recursive copy of the whole `agent-factory/` tree → `$GRUGOPS_HOME/agent-factory/config/factory.config.md` on every installed target. That is the measured fact that makes the config field reference shipped kit prose and therefore in scope for D-08.1."
    - "`SCAN` in `scripts/check-kit-refs.ts` → the derived reader in `scripts/check-kit-refs.test.ts` → the hermetic mirror the cases plant into. One declaration, read twice, never copied."
    - "`MODELS_CONFIG_CANDIDATE_RELS[1]` → the two mentions in the config field reference → the exemption predicate. The gate exempts a self-reference to an existing sibling, which is what those two mentions are, and nothing wider."
  prohibitions:
    - "The exemption may not be spelled as a list of line numbers, a list of hit strings, or a file allow-list without an asserted cardinality. A hand-listed exemption with no count is the failure class this exemption is created to avoid."
    - "SCAN gains exactly one entry. The exclusion-by-not-listing design is load-bearing and preserved: `seed/`, `examples/`, `install/`, `docs/`, `.planning/` and the root documents stay omitted, and making membership one entry wider is not a licence to widen it further."
    - "The two mentions in `agent-factory/config/factory.config.md` are NOT deleted, reworded or moved. Deleting them would reopen WR-05, which the oracle asserts; the exemption exists precisely because the document owes a reader both locations."
    - "`agent-factory/config/factory.config.md` and `agent-factory/config/factory.config.json` are byte-unchanged by this plan."
    - "`29.1-11-SUMMARY.md` is ANNOTATED, never rewritten. The superseded key decision stays legible beside the correction, per this project's annotate-never-rewrite convention."
    - "`relativeToRoot`, `ROOT` and `ROOT_PATH_BOUNDARY` in `scripts/check-foundation-guards.ts` are not edited. This plan's guard change is confined to one set-literal inventory row."
    - "No unmeasured cost, spend, quota or usage-limit claim in any comment, finding or record this plan writes. `scripts/measure-cost.ts` and `scripts/measure-cost.js` stay byte-unchanged."
    - "No caveman voice in any gate finding, comment or record."
    - "`npm test` is never run. The regression command is `npx vitest run --exclude '**/scripts/e2e/**'`."
---

<objective>
Close R2-WR-05 / D6 as the user ruled, and close the round.

The round-2 review found that the WR-05 disposition rests on a premise the installer contradicts.
`29.1-11-SUMMARY.md` records the obligation as split by AUDIENCE — the config field reference being
the developer-facing authority that "lives outside the scan set". "Outside the `check-kit-refs` SCAN
set" and "not shipped kit prose" are two different facts, and the rationale used the first to
establish the second. `install/install.ts:1065` recursively copies the whole `agent-factory/` tree,
`config/` included, into `$GRUGOPS_HOME` on every install — the round-2 verifier confirmed it
independently. So `agent-factory/config/factory.config.md` is delivered to every installed user, and
it is the document that names the kit-internal configuration path twice. That is precisely the
reader-pointed-at-a-kit-internal-path shape D-08.1 exists to prevent, reached through a document the
gate does not scan. `agent-factory/config` is absent from SCAN by omission, not by judgment.

The user has ruled: add it to the scan, with an explicit, argued exemption for the two mentions it
would then need to carry. This plan does that — and derives the exemption and asserts its
cardinality, because an exemption list with no count is the set-literal-drift class installed at the
exact surface whose purpose is refusing silently dropped configuration.

The same task deletes the second hand-maintained copy of the scan set living in the gate's own test
file, which would otherwise leave the mirror the cases plant into one member behind the set the gate
walks — the drift arriving in the commit meant to close a drift.

This is the round's last plan, so it also reconciles all twelve round-2 findings and all three
carried human items against the plans that closed them, and runs the phase gate block in CI's order.

Output: a scan that reaches the document the installer ships, an exemption that is argued, derived
and counted, three corrected records, and a round with nothing silently dropped.
</objective>

<assumption_delta_decision>
**Fires: required → conditional, on a ratified contract.** D-08.1's Assertion 1 has held ZERO
`agent-factory/config/` references across the kit scan set with no exemption of any kind since
Phase 7. This plan makes it "zero, except a self-reference in the configuration directory to an
existing sibling, counted at exactly two hits in exactly one file." That is a genuine weakening of a
twenty-two-phase-old absolute, and it is recorded here rather than left to be noticed later.

**Decision: `add-alongside`.** The absolute is not replaced. The scan is WIDENED — the directory the
installer ships joins it, which strictly increases what D-08.1 covers — and the exemption is scoped
by a predicate, published in the verdict, and pinned by two asserted cardinalities plus four red
cases, so the net reach of the assertion grows rather than shrinks. Rationale: the user ruled this
disposition on D6 after the round-2 review refuted the premise that made the omission look
principled, and the alternative (deleting the two mentions) would reopen WR-05, which the
cross-surface oracle asserts.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-per-role-model-assignment/29.1-CONTEXT.md
@.planning/phases/29.1-per-role-model-assignment/29.1-PATTERNS.md
@.planning/phases/29.1-per-role-model-assignment/29.1-VALIDATION.md
@.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round2.md
@.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW-round2.md
@.planning/phases/29.1-per-role-model-assignment/29.1-11-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol or path | Kind | Purpose |
|----------------|------|---------|
| `agent-factory/config` | new SCAN member in `scripts/check-kit-refs.ts` | brings the shipped config field reference inside D-08.1's zero-tolerance scan |
| `CONFIG_SELF_REF_DIR` | new const in `scripts/check-kit-refs.ts` | the configuration directory the exemption is scoped to, taken from the new SCAN member |
| `CONFIG_SELF_REF_FILES` / `CONFIG_SELF_REF_HITS` | new count constants in `scripts/check-kit-refs.ts` | the exemption's two asserted cardinalities, named in the failure text |
| `exemptConfigSelfRefs` | new derived helper in `scripts/check-kit-refs.ts` | partitions Assertion 1's hits into exempt self-references and strays, by a predicate rather than a list |
| `gateScanSet()` | new derived helper in `scripts/check-kit-refs.test.ts` | reads the gate's own `SCAN` declaration, bounded at both ends, and returns its members with the count asserted |
| `SCAN` (test copy) | DELETED literal in `scripts/check-kit-refs.test.ts` | the second hand-maintained copy of the scan set, replaced by the derivation above |

New test case names (`scripts/check-kit-refs.test.ts`):
`the mirror's scan set is DERIVED from the gate's own declaration, with its cardinality asserted`,
`Assertion 1 GREEN: the config field reference's self-references are exempt, and the verdict says so`,
`Assertion 1 RED: a THIRD kit-internal mention in the config field reference fails, naming both counts`,
`Assertion 1 RED: a kit-internal mention in a SECOND file under the config directory fails`,
`Assertion 1 RED: a kit-internal mention naming a path that does not exist is a stray, not a self-reference`,
`Assertion 1 RED: a kit-internal mention outside the config directory still fails as it always did`,
`Assertion 1 vacuity floor: a tree with no configuration directory is a named finding`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The scan reaches the document the installer ships, and the exemption is argued, derived and counted</name>
  <files>scripts/check-kit-refs.ts, scripts/check-kit-refs.js</files>
  <behavior>
    - The kit-ref gate walks the configuration directory as part of its scan set.
    - A kit-internal path mention inside the configuration directory, naming a file that exists in that directory, is exempt and does not fail the gate.
    - A kit-internal path mention anywhere else in the scan set still fails, unchanged.
    - A kit-internal path mention inside the configuration directory naming a path that does not exist is a stray and fails.
    - A third exempt hit, or an exempt hit in a second file, fails the gate naming both the count found and the count required.
    - A tree with no configuration directory produces a named finding rather than a silent zero.
    - The Assertion 1 pass line names how many hits were exempt and in which file.
  </behavior>
  <read_first>
    - `scripts/check-kit-refs.ts` lines 1-40 — the module header, its statement that exclusions are BY NOT LISTING, and its explicit warning that making membership derived is not a licence to widen the scan. This task widens it by exactly one entry and must record why that is not the thing the warning forbids.
    - `scripts/check-kit-refs.ts` lines 55-80 — the `SCAN` declaration with its per-entry rationale comment, and the `GH_SCAN` negative-scope list. Only `SCAN` changes.
    - `scripts/check-kit-refs.ts` lines 140-200 — `walk`, `grepSubstring`, `grepFilesWithMatch` and `readText`, the helpers Assertion 1 runs through. The exemption is built on `grepSubstring`'s existing `path:lineno:line` hit shape, not on a second walk.
    - `scripts/check-kit-refs.ts` lines 265-285 — Assertion 1 in full: its header line, the joined hit string, the pass and the fail. This is the arm that gains the partition.
    - `scripts/check-kit-refs.ts` lines 286-320 — Assertion 2 and Assertion 3, both of which also run over `SCAN`. Confirm by measurement what the new member does to each before assuming it does nothing.
    - `agent-factory/config/factory.config.md` lines 1-5 and line 138 — the two mentions, in full, in their sentences. The exemption's argument must describe what those two sentences are doing, accurately.
    - `install/install.ts` lines 1060-1075 — `copyKit`, the recursive copy of the whole `agent-factory/` tree. This is the measured fact the exemption's rationale rests on; read it and cite the line.
    - `scripts/model-dial-consistency.test.ts` lines 705-715 — the case asserting the config field reference names every declared configuration location. This is why the mentions cannot be deleted.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW-round2.md` §R2-WR-05 in full.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round2.md` `human_verification` item D6, which carries the user's ruling this task implements.
  </read_first>
  <action>
Measure first, three things, and record all three transcripts before editing.

One: with the configuration directory temporarily added to the scan set, run the gate and record
exactly which assertions move and by how much. Assertion 1 must be the only one that moves; confirm
Assertion 2 and Assertion 3 and the marker check are unchanged rather than assuming it, and record
the numbers each reports before and after.

Two: enumerate the kit-internal mentions the directory carries, with file and line, using the same
substring the gate greps for. Record the list. That list is the exemption's denominator and its
cardinality is the number the gate will assert.

Three: read `copyKit` and record the line that recursively copies the whole kit tree, with the
surrounding call, as the evidence that the configuration directory ships.

Then add the configuration directory to `SCAN`, with its own rationale comment in the style the other
entries carry. The comment must state four things: that the installer ships this directory, citing
the file and line measured above; that its absence from the scan was by omission and not by an
audience judgment, correcting the record; that the field reference is where a user learns what the
resolver reads, so it necessarily names both configuration locations; and that the mentions are
therefore exempted BY NAME with an asserted count rather than the directory being left unscanned.

Implement the exemption as a PREDICATE over Assertion 1's existing hits, not as a list. A hit is
exempt when the file carrying it is inside the configuration directory AND every kit-internal path
the hit's line names resolves to a file that exists in that directory on the tree under judgement.
Extract the named paths from the line rather than matching the whole line, so a sentence that
mentioned the directory and a fabricated filename is a stray. Partition the hits into exempt and
stray; strays fail exactly as they do today with the same wording.

Assert the exemption's cardinality two-sided, in the gate: the number of distinct exempting FILES,
and the number of exempt HITS. Both numbers are declared as named constants with the argument beside
them. The failure text must name the count found and the count required, and list the hits, so a
reader of the red learns which mention arrived rather than that a number moved.

Add the vacuity floor before the cardinality assertion: if the tree under judgement carries no
configuration directory at all, that is its own named finding. Without it, an absent directory makes
the cardinality assertion fail for a reason nobody can attribute — and this gate's own existing
vacuity floor for the adapter set is the precedent to follow, including its wording discipline.

Publish the exemption in the PASS line. Assertion 1's success message must name the exempt hit count
and the exempting file, so the exemption is visible in a green run rather than discoverable only by
reading source. A silent exemption is the shape this whole finding is about.

Run `npm run build` and commit `scripts/check-kit-refs.js` in the SAME commit as the `.ts`.
  </action>
  <verify>
    <automated>node scripts/check-kit-refs.js && npm run typecheck && npm run build && git diff --exit-code --name-only -- '*.js'</automated>
  </verify>
  <acceptance_criteria>
    - All three measurements recorded verbatim in the SUMMARY: the per-assertion before/after numbers with the directory added; the enumerated kit-internal mentions with file and line; and the `copyKit` line with its surrounding call.
    - `node scripts/check-kit-refs.js` exits 0 on this repository, and its Assertion 1 pass line names the exempt hit count and the exempting file. Quote the line.
    - `SCAN` gains exactly one entry. `git diff -- scripts/check-kit-refs.ts` shows one added member and no other membership change; the diff is quoted.
    - Assertion 2, Assertion 3 and the marker check report the same numbers as before the change. Quote both runs' verdicts side by side.
    - The exemption is a predicate over hits, not a list of lines or strings. Confirm by reading the implementation and quoting it; a line-number list or a hit-string allow-list is a plan violation.
    - Both cardinalities are asserted two-sided and both appear in the failure text. Prove by planting a third mention and capturing the failure, which must name the found count, the required count and the new hit.
    - A mention inside the configuration directory naming a path that does not exist fails as a stray. Transcript recorded.
    - The vacuity floor fires on a tree with no configuration directory, with a named finding rather than an unattributable cardinality failure. Transcript recorded.
    - `agent-factory/config/factory.config.md` and `agent-factory/config/factory.config.json` are byte-unchanged: `git diff --name-only -- agent-factory/config/` is empty.
    - `npm run typecheck` exits 0. After `npm run build`, `git diff --exit-code --name-only -- '*.js'` is empty.
  </acceptance_criteria>
  <done>The kit-ref gate scans the document the installer ships, exempts exactly the self-references that document owes its reader, asserts both cardinalities two-sided, refuses everything else, and says all of it in its own verdict.</done>
  <reversibility rating="costly">Widening a twenty-two-phase-old zero-tolerance scan changes what a green build means for every future kit document under that directory; reverting is a one-entry deletion, but any document written under the new scan in the interim would have been authored against a rule that then disappears.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The test's scan set is derived from the gate's, and the exemption has cases on both sides</name>
  <files>scripts/check-kit-refs.test.ts</files>
  <behavior>
    - The hermetic mirror is built from the scan set the gate itself declares, so a member added to the gate enters the mirror with no edit here.
    - The derived member list's cardinality is asserted, so a derivation that silently returned fewer members fails rather than building a partial mirror.
    - A green mirror exits 0 with the exemption named in the verdict.
    - A third kit-internal mention, a mention in a second file under the configuration directory, a mention naming a non-existent path, and a mention outside the configuration directory each drive the gate red, each for its own reason.
    - The pre-existing cases over the other scan members still pass unmodified.
  </behavior>
  <read_first>
    - `scripts/check-kit-refs.test.ts` lines 38-95 — the hand-copied `SCAN` literal with its comment, `freshTmp`, `makeMirror`, `runGate` and `aRoleFile`. The literal is what this task deletes; the helpers are what it keeps.
    - `scripts/check-kit-refs.test.ts` lines 98-175 — the flipped-gate cases, which plant into a role file and must keep passing unmodified.
    - `scripts/check-kit-refs.test.ts` lines 199-320 — the derived-set cases, including the vacuity floor and derivation-failure cases, which are the wording and premise-assertion conventions the new cases follow.
    - `scripts/check-kit-refs.ts` `SCAN` as edited by task 1 — the declaration the derivation reads, and the exact quoting and formatting the extractor must handle.
    - `scripts/check-foundation-guards.test.ts` case `(m)` — this repository's existing worked example of deriving an assertion from a module's own source text, including its premise assertions. Follow that shape.
    - `scripts/model-dial-consistency.test.ts` `scopeSection()` — the bounded-region reader whose right-bound discipline the `SCAN` extractor must copy: locate the declaration, end at its closing bracket, never run to end of file.
  </read_first>
  <action>
Delete the hand-copied scan-set literal and replace it with `gateScanSet()`, which reads
`scripts/check-kit-refs.ts` and extracts the members of its `SCAN` declaration. Bound the read at
BOTH ends — from the declaration to its closing bracket — and throw a named error if either bound is
missing, rather than returning a partial list. Extract the quoted members and return them.

Assert the derivation's own premises before any mirror is built: the extracted list is non-empty,
its cardinality equals the number measured in this session, and it contains a member every existing
case already depends on. The cardinality assertion is what makes a silently short extraction a red
rather than a partial mirror that passes for the wrong reason — this repository's stated remedy for a
derived set, applied to a derivation over source text.

Record in the comment where the literal used to be what changed and why: the mirror is built from the
set the gate walks, and a second copy of that set is the drift class this round is closing elsewhere
in the same commit. State the one thing the derivation buys that the copy could not — a member added
to the gate enters the mirror in the same commit — and state its residual, which is that the
extractor reads source text and would need updating if the declaration's shape changed.

Add the exemption cases, five red and one green, each asserting its own plant's premise first.

The green case runs an unmutated mirror and asserts exit 0 AND that Assertion 1's pass line names the
exempt hit count and the exempting file. Asserting only the exit code would pass over an exemption
that silently swallowed everything.

The first red plants a THIRD kit-internal mention in the config field reference and asserts exit 1
with both counts named. The second plants a mention in a SECOND file under the configuration
directory and asserts exit 1, so the exemption is scoped to one file and not to a directory. The
third plants a mention naming a path that does not exist in that directory and asserts exit 1, so a
self-reference means a reference to something that is there. The fourth plants a mention in a file
OUTSIDE the configuration directory and asserts exit 1 with the pre-existing wording, so the
exemption did not widen the gate for the rest of the scan set.

Add the vacuity case: a mirror with the configuration directory removed exits 1 with the named
finding, not with an unattributable cardinality disagreement.

Prove the derivation discriminates: temporarily remove a member from the gate's `SCAN` declaration,
observe the cardinality assertion go RED naming both numbers, restore, and confirm the gate source is
byte-identical afterwards.

Run the pre-existing cases unmodified and record that they pass. Any case whose premise the widened
scan invalidates is REPAIRED and named; none may be deleted.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-kit-refs.test.ts && node scripts/check-kit-refs.js && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const SCAN' scripts/check-kit-refs.test.ts` returns 0 — the second copy is deleted, not commented out.
    - `gateScanSet()` bounds its read at both ends and throws a named error when either bound is missing. Prove by running it against a scratch copy with the closing bracket removed and capturing the named throw.
    - The derivation's cardinality assertion is RED when a member is removed from the gate's declaration, naming both numbers. Transcript recorded; gate source byte-restored afterwards.
    - All six new cases pass, and each asserts its plant's premise before the gate is run. Quote one premise assertion per case in the SUMMARY.
    - The green case asserts the exemption is named in the pass line, not merely that the gate exited 0.
    - The four red cases each fail for their own reason, demonstrated by quoting the distinguishing text from each failure — four different messages, not four non-zero exits.
    - Every pre-existing case in the file passes unmodified, or is named in the SUMMARY with what changed and why.
    - `npx vitest run scripts/check-kit-refs.test.ts` exits 0; case count recorded before and after.
    - `npm run typecheck` exits 0. `git status --porcelain` shows no leftover plant and no stray temp fixture inside the repository.
  </acceptance_criteria>
  <done>The mirror is built from the set the gate declares, its cardinality is asserted, and the exemption is pinned on both sides by cases that fail for four distinguishable reasons.</done>
</task>

<task type="auto">
  <name>Task 3: The three stale records are corrected, and the round is reconciled with nothing dropped</name>
  <files>scripts/model-dial-consistency.test.ts, scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js, .planning/phases/29.1-per-role-model-assignment/29.1-11-SUMMARY.md</files>
  <read_first>
    - `scripts/model-dial-consistency.test.ts` lines 676-700 — the WR-05 comment block, which states that the config field reference lives outside the scan set, that the gate admits no exemption list, and that the obligation is therefore split by audience. All three sentences are falsified by task 1.
    - `scripts/check-foundation-guards.ts` lines 125-145 — the set-literal inventory, specifically the row for this gate's scan set and its claim that the deliberately-omitted directories stay omitted. That row's disposition changes with task 1.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-11-SUMMARY.md` — key decision 1, the split-by-audience disposition and its "outside the scan set" premise, and the D6 human-judgment item it routed forward.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round2.md` — the `gaps`, `human_verification` and `gaps_remaining` blocks in full, which are the checklist this reconciliation is measured against.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW-round2.md` — the finding list in full, all twelve, which is the other half of that checklist.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-VALIDATION.md` §"Test Infrastructure" — the fourteen-command phase gate block and its ordering constraint that `npm run freshness` runs before any build.
  </read_first>
  <action>
Correct the oracle's WR-05 comment block. Replace the three falsified sentences with what is now
true: the configuration directory IS inside the scan set; the gate carries one argued, derived,
count-asserted exemption for the self-references the field reference owes its reader; and the
obligation is therefore no longer split by audience but discharged by both documents under one scan.
Keep the block's surviving argument — that the packaging authority names the location a user
configures and points at the rule's home — because that is still the shape D-13 asks for. Record the
finding id and the date. Do not delete the superseded sentences from the file's history; this is a
source comment, so the correction replaces the text and the SUMMARY carries the before and after.

Correct the set-literal inventory row for this scan set in `scripts/check-foundation-guards.ts`.
Its disposition line claims the deliberately-omitted directories stay omitted, which is now one
member less true. State the new disposition accurately: one directory joined the set in round 3 with
an argued exemption, the remaining omissions are unchanged, and name the plan. Rebuild the committed
twin.

Annotate `29.1-11-SUMMARY.md` — annotate, never rewrite. Add a dated correction beneath key decision
1 recording four things: that `install/install.ts` recursively copies the whole kit tree including
the configuration directory, citing the line; that "outside the scan set" and "not shipped kit prose"
are different facts and the original rationale used the first to establish the second; that D6 was
put to the user in round 3 and the user ruled to add the directory to the scan with an argued
exemption; and which plan implemented that ruling. Leave the original key decision standing above the
correction, in full.

Then reconcile the round. Build a disposition table covering all twelve round-2 findings and all
three carried human items. Each row names the item, the plan that closed it, and one line of the
evidence that closed it — or, for a carried item, the reason it is carried and who adjudicates it.
Derive the item list from the two round-2 documents rather than typing it from memory, and assert the
count: twelve findings plus three carried items, with the number stated. An item that appears in
neither a closing plan nor the carried list is a silent drop and the table is the mechanism that makes
one impossible.

Finally, run the phase gate block from `29.1-VALIDATION.md` in CI's order, with `npm run freshness`
first, and record every command's exit code. Then run the two round-specific end-to-end checks that
this round exists for: the relative-root byte identity from plan 29.1-12, and the full regression
lane in its excluded form. Record all of it.
  </action>
  <verify>
    <automated>npm run freshness && node scripts/check-foundation-guards.js && node scripts/check-kit-refs.js && npx vitest run --exclude '**/scripts/e2e/**' && npm run typecheck && npm run build && git diff --exit-code --name-only -- '*.js'</automated>
  </verify>
  <acceptance_criteria>
    - The oracle's WR-05 comment block no longer states that the config field reference lives outside the scan set, and no longer states that the gate admits no exemption list. Both confirmed with `grep -c` returning 0, commands quoted, and the block quoted before and after.
    - The set-literal inventory row for this scan set states the new disposition and names the plan. Quoted before and after; the committed twin rebuilt in the same commit.
    - `29.1-11-SUMMARY.md` carries a dated correction beneath key decision 1 with all four recorded facts, and the original key decision is still present in full above it. Quote the correction and show the original still standing.
    - The disposition table covers twelve findings and three carried items, with the count stated and the item list derived from the two round-2 documents. Any item not closed by a plan is listed as carried with its adjudicator named.
    - The full fourteen-command gate block from `29.1-VALIDATION.md` runs in CI's order with `npm run freshness` first, and every command's exit code is recorded. Any command that cannot be run is marked `UNKNOWN - verify` with the reason, never reported as passing.
    - `diff <(node scripts/check-foundation-guards.js 2>&1) <(CHECK_ROOT=. node scripts/check-foundation-guards.js 2>&1)` prints nothing — the round's blocker closure survives every later plan.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0, with the suite totals recorded and reconciled against the pre-round totals, accounting for every case added and removed across plans 29.1-12 through 29.1-17.
    - `node scripts/generate-role-adapters.js` then `git status --porcelain -- .claude/agents/` prints nothing, and `grep -h '^model:' .claude/agents/*.md | sort | uniq -c` reports 17 `model: inherit`.
    - `git diff --name-only -- install/ package.json scripts/measure-cost.ts scripts/measure-cost.js agent-factory/config/` is empty.
    - `npm run freshness` reports every committed output fresh; `npm run typecheck` exits 0; `npm run build` moves no tracked build output.
  </acceptance_criteria>
  <done>Every record falsified by this round's changes states what is true, the round's fifteen items are reconciled in a derived and counted table, and the phase gate block is green in CI's order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped kit document → the installed user | `copyKit` places the whole `agent-factory/` tree, config directory included, at `$GRUGOPS_HOME` on every install |
| kit prose → per-repository state | D-08.1 exists so no shipped document sends a reader to a path inside the kit to edit; per-repository state lives at `.grugops/` |
| an exemption → the next editor | An exemption with no asserted count is an invitation; the next mention arrives silently |
| a recorded rationale → the next decision | A rationale resting on a refuted premise makes the decision unexamined rather than reasoned |

## STRIDE Threat Register

ASVS level 1, block on `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-56 | Tampering | a shipped document pointing a reader at a kit-internal path, reached through a directory the gate does not scan | high | mitigate | The configuration directory joins SCAN; every mention in it is adjudicated by a predicate, and only a self-reference to an existing sibling is exempt. |
| T-29.1-57 | Tampering | an exemption growing silently | high | mitigate | Both cardinalities are asserted two-sided in the gate and named in the failure text; four red cases prove the exemption is scoped to one file, one directory, and existing paths only. |
| T-29.1-58 | Repudiation | a green verdict that does not disclose the exemption it applied | medium | mitigate | The Assertion 1 pass line names the exempt hit count and the exempting file, asserted by the green case rather than by the exit code alone. |
| T-29.1-59 | Tampering | a second hand-maintained copy of the scan set falling behind the gate | high | mitigate | The test's copy is deleted and derived from the gate's own declaration, bounded at both ends with the cardinality asserted and proven RED on a removed member. |
| T-29.1-60 | Repudiation | recorded rationales resting on a premise the installer refutes | medium | mitigate | Three records are corrected in the same commit as the change that falsifies them, with the measured `copyKit` line cited and `29.1-11-SUMMARY.md` annotated rather than rewritten. |
| T-29.1-61 | Repudiation | a round-closing report that silently drops an item | high | mitigate | The disposition table's item list is derived from the two round-2 documents and its count is asserted at fifteen; an item in neither column is a visible hole. |
| T-29.1-05 | Tampering | committed `scripts/*.js` diverging from its `.ts` | high | mitigate | `npm run build` plus `git diff --exit-code --name-only -- '*.js'` in every task verify; `npm run freshness` first in the closing gate block. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | accept | Zero packages installed by this plan; `package.json` asserted byte-unchanged in the plan verification. `29.1-RESEARCH.md` records no new dependency for this phase. |
</threat_model>

<verification>
- `node scripts/check-kit-refs.js` exits 0 and its Assertion 1 pass line names the exempt hit count and the exempting file.
- `npx vitest run scripts/check-kit-refs.test.ts scripts/model-dial-consistency.test.ts` exits 0.
- `node scripts/check-foundation-guards.js` exits 0, and the relative-root byte identity from plan 29.1-12 still holds.
- `grep -c 'const SCAN' scripts/check-kit-refs.test.ts` is 0.
- `git diff --name-only -- agent-factory/config/` is empty.
- The fourteen-command phase gate block from `29.1-VALIDATION.md` runs in CI's order, `npm run freshness` first, every command exit 0 or marked `UNKNOWN - verify` with its reason.
- `npx vitest run --exclude '**/scripts/e2e/**'` exits 0. `npm test` is never run.
- `npm run typecheck` exits 0. `npm run build` then `git diff --exit-code --name-only -- '*.js'` is empty.
- **Carried, not closed:** D5 (MODEL-06 vendor-citation fidelity), carried verbatim in plan 29.1-16's frontmatter.
- **Carried, not closed:** MODEL-07/boundary and MODEL-07/precision, carried as `verification: backstop` truths in plan 29.1-16.

</verification>

<success_criteria>
- The gate scans the document the installer ships, and the two mentions it must keep are exempt by a predicate with both cardinalities asserted.
- Four red cases distinguish four different ways an exemption could have been too wide, each by its own message.
- The second copy of the scan set is gone, and its replacement is proven RED on a removed member.
- Three records falsified by this change state what is true, with the measured installer line cited and the superseded summary annotated rather than rewritten.
- All twelve round-2 findings and all three carried human items are accounted for in a derived, counted table — no silent drops.
</success_criteria>

<output>
Create `.planning/phases/29.1-per-role-model-assignment/29.1-17-SUMMARY.md` when done.
Record in it: the three task-1 measurements verbatim, including the per-assertion before/after
numbers and the `copyKit` line; the Assertion 1 pass line as printed; the four red transcripts with
their distinguishing text; the derivation's RED transcript on a removed scan member; the three
corrected records quoted before and after, with the `29.1-11-SUMMARY.md` original shown still
standing above its annotation; the fifteen-row disposition table with its asserted count; every gate
command with its exit code; the relative-root byte-identity result; and the suite totals reconciled
across plans 29.1-12 through 29.1-17.
</output>

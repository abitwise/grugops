---
phase: 29.1-per-role-model-assignment
round: 3
verified: 2026-08-20T19:30:00Z
status: gaps_found
score: 5/5 roadmap success criteria verified — and gaps_found anyway, on two live gate bypasses this round introduced
behavior_unverified: 0
overrides_applied: 0
requirement_determinations:
  MODEL-05: "Gaps Found → COMPLETE. The regression round 2 recorded (R2-CR-01) is closed, and the fix DISCRIMINATES — measured on this tree, not argued: the pre-round-3 committed build at d38a212 prints `agent-factory/config/factoryconfig.json` under `CHECK_ROOT=.` and the HEAD build prints `agent-factory/config/factory.config.json`. Every clause of MODEL-05's own text was additionally re-proven behaviourally (four planted mutations, all RED by name). Nothing in this round's two blockers touches a model value, guard_model_assignment, or the alias vocabulary."
  MODEL-01: "Complete — unchanged, re-verified."
  MODEL-02: "Complete — unchanged, re-verified."
  MODEL-03: "Complete — unchanged, re-verified behaviourally (18th-role mirror)."
  MODEL-04: "Complete — unchanged, re-verified behaviourally (nine config shapes refused by name)."
  MODEL-06: "Complete — unchanged. Citation fidelity remains a human item (D5)."
  MODEL-07: "Complete (mechanical) — the totality half remains `verification: backstop`, a human item."
re_verification:
  previous_status: gaps_found
  previous_score: "4/5 roadmap success criteria verified"
  gaps_closed:
    - "R2-CR-01 (the round-2 blocker) — `relativeToRoot` in scripts/check-foundation-guards.ts now `resolve()`s CHECK_ROOT before anything is compiled from it (:413-414) and requires a path boundary after the matched root from ONE declaration (`ROOT_PATH_BOUNDARY`, :433). Independently reproduced pre-fix and refused post-fix; additionally held under a metacharacter-bearing root (`wei+rd (x)`), a trailing-slash root, `./`, `./scripts/..`, and a prefix-sibling root."
    - "R2-WR-01 — `resolveModels` Floor 0b now refuses `overrides: null` and `overrides: {plain object}` BY NAME instead of throwing/discarding. Reproduced: both return `{ok:false}` naming the shape."
    - "R2-WR-03 / R2-IN-01 — the stray-pin arm's denominator is derived from SPAWN_GRANT_SCAN, its silence is an assertion, and unadmittable surfaces are excluded from the probed count AND named. Reproduced: a planted `model: opus` on a SKILL reds the guard naming the file and value, and the run summary reads `16 of 16 … 1 found and named above`."
    - "R2-WR-04 — the packaging authority no longer restates the precedence rule in any wording (0 occurrences of `agent-factory/config/` in subagent.frontmatter.md; the bullet defers to the config field reference)."
    - "R2-WR-05 / D6 (the user-ruled disposition) — the premise is TRUE and independently confirmed: install/install.ts:1065 `cpSync`s the whole `agent-factory` tree, so `agent-factory/config/` ships to every installed target. `agent-factory/config` is now SCAN member 5 of 10. NOTE: the decision is closed; its IMPLEMENTATION is this round's blocker — see gaps."
    - "R2-WR-06 / R2-IN-02 / R2-IN-04 — `resolvedAssignmentsIn` enforces the alias grammar its refusal quotes. Reproduced across eight payload shapes: illegal alias, full model id, non-array aliases, missing key, extra key, wrong value type and unparseable JSON are each a NAMED refusal, never a dropped line."
    - "R2-IN-05 — the CI adapter-cleanliness command is now `git status --porcelain -- .claude/agents/` + `test -z`, which sees an UNTRACKED adapter. Confirmed in .github/workflows/ci.yml:141-148."
  gaps_remaining: []
  regressions:
    - "check-kit-refs Assertion 1 (D-08.1, a twenty-two-phase-old zero-tolerance gate) now publishes a green verdict that misdescribes its own run. This is the SAME defect class round 2 regressed MODEL-05 for, relocated from scripts/check-foundation-guards.ts to scripts/check-kit-refs.ts by this round's own R2-WR-05/D6 fix (plan 29.1-17). It does not falsify any of the five success criteria, and it is a blocker regardless."
overrides: []
gaps:
  - truth: "D-08.1's zero-tolerance kit-ref gate governs the shipped configuration reference — the document this phase's single packaging authority now points every reader at for the precedence rule. (closure route for SC-4 / MODEL-06 via the user's D6 ruling)"
    status: failed
    reason: "The exemption plan 29.1-17 added to admit `agent-factory/config` into SCAN is bypassable on two independent axes, both reproduced by this verifier on a hermetic `git archive HEAD` mirror against the committed `scripts/check-kit-refs.js`. Once bypassed, the gate prints `no agent-factory/config/ refs remain` and `the config self-reference exemption is exactly 2 hit(s) in 1 file(s), as declared` at exit 0 while the shipped document carries the literal instruction D-08.1 exists to forbid."
    artifacts:
      - path: "scripts/check-kit-refs.ts"
        issue: "R3-CR-01 — THE EXEMPTION COUNTS LINES, NOT MENTIONS. `grepSubstring` pushes one hit per LINE (:223), `exemptConfigSelfRefs` pushes one `exempt` entry per hit (:271), and the cardinality assertion compares `exempt.length` against `CONFIG_SELF_REF_HITS = 2`. So the predicate is asked ONCE PER LINE and an already-exempt line absorbs unbounded further mentions unasked. Measured on the live tree: `grep -o 'agent-factory/config/' agent-factory/config/factory.config.md | wc -l` = 3 on 2 lines — the constant's own comment at :173-176 states 'Exactly TWO exempt mentions … factory.config.md:3 … and factory.config.md:138', which is FALSE at the mention granularity it claims (line 138 carries two). The gate's declared number has never described the tree it judges."
      - path: "scripts/check-kit-refs.ts"
        issue: "R3-CR-02 — `.` AND `..` PASS THE SELF-REFERENCE PREDICATE. `CONFIG_NAMED_PATH = /agent-factory\\/config\\/([A-Za-z0-9._-]+)/g` (:186) admits `.` and `..` (both pure `[._-]`), and the resolution test is bare `existsSync` (:270), which is true for a DIRECTORY. So `agent-factory/config/..` captures `..`, resolves to `agent-factory/` and is certified as 'a reference to a file that exists in that directory'. The documented predicate at :246-256 is false on both halves."
    missing:
      - "R3-CR-01 fix: derive the exempt-MENTION cardinality from the same `CONFIG_NAMED_PATH` extraction the predicate already runs (`exemptMentions += named.length`) and assert it against a `CONFIG_SELF_REF_MENTIONS = 3` alongside the existing line and file counts. Correct the :173-176 comment to say three mentions on two lines."
      - "R3-CR-01 case: plant a second mention on an ALREADY-EXEMPT line. Today's pinning case plants on a NEW line, which is the one shape the counting unit does catch. Reproduced contrast, same sentence, same file: on a new line → `FAIL stray agent-factory/config/ ref(s)`, exit 1; appended to line 3 → exit 0; appended to line 138 → exit 0."
      - "R3-CR-02 fix: refuse `.` and `..` by canonical form (an allow-list of what a basename may BE, not a widened pattern), and require `statSync(p).isFile()` rather than any directory entry."
      - "R3-CR-02 cases: `agent-factory/config/..`, `agent-factory/config/.`, and a directory basename once a subdirectory can exist there. Today's ghost case ('a kit-internal mention naming a path that does not exist is a stray') cannot see any of them, because `..` DOES exist."
  - truth: "The R2-WR-02 right-bound fix is proven by a case that can see it"
    status: failed
    reason: "MEASURED, not reasoned. `ubuntuBlock`'s `next === -1 ? rest : rest.slice(0, next)` returns a string that cannot contain STEP_MARKER on either branch, so `(r-bound)`'s central assertion `expect(block.includes(STEP_MARKER)).toBe(false)` is a tautology whose failure message is unreachable. I deleted the right bound outright (`return rest`) and ran the file: `npx vitest run scripts/check-foundation-guards.test.ts` → 263 passed (263). The `-t 'r-'` filter → 16 passed. Nothing in this tree can tell the bounded reader from the unbounded one, because the ubuntu gate block is the LAST step in ci.yml (final `- name:` at :131 of 451) — confirmed independently. The COMMAND-line half of the same fix (comment-stripped exact equality) does discriminate and is not in question."
    artifacts:
      - path: "scripts/check-foundation-guards.test.ts"
        issue: ":10869-10883 — the `(r-bound)` case's own comment claims the property is 'asserted rather than inherited from the coincidence that this block happens to be the last step today'. It is inherited from exactly that coincidence."
      - path: "scripts/skill-twins-freshness.test.ts"
        issue: ":286-293 — the identical tautology in the sibling reader, and its slice base differs (`ci.slice(at)` vs the other file's `ci.slice(at + UBUNTU_BLOCK_STEP_NAME.length)`), so `(r-class)` certifies two readers that are not the same slice."
      - path: "scripts/model-dial-consistency.test.ts"
        issue: ":900-901 — same class, lower stakes: `occurrences(region, '\\n- ') === 0` and `occurrences(region, '\\n## ') === 0` are guaranteed by `rest.slice(0, Math.min(...ends))`. (The region reader's THROW paths are genuinely proven — deviations F and G in 29.1-16-SUMMARY.md record real reds — so only these two containment lines are dead weight.)"
    missing:
      - "Assert the bound over a SYNTHETIC workflow that HAS a following step (the shape `gateScanSet` is already tested with at check-kit-refs.test.ts:194-201): append a step to `ciWorkflow()`, then assert the extracted block does not contain it and the appended command is not adopted into the command list. That case reds on `return rest`."
      - "Delete the three tautological assertions rather than leave failure messages no input can print."
  - truth: "`resolveModels` returns a result rather than throwing — the module header's stated contract, and the contract Floor 0 / Floor 0b exist to hold"
    status: partial
    reason: "R2-WR-01 closed the ITERATION throw and left the REFUSAL-MESSAGE throw open. Both floors build their reason with `JSON.stringify`, which throws on a circular object and on a BigInt — so the value that reaches the refusal is the value that prevents the refusal from being returned. Reproduced against the committed scripts/model-tiers.js, four shapes: `{preset: circular}`, `{preset: 1n}`, `{overrides: Map([[stem, circular]])}`, `{overrides: Map([[stem, 1n]])}` — all four THREW TypeError. NOT reachable from the config-reading path (JSON carries neither shape, and readModelsConfig gates every production call — verified), so this is latent in exactly the sense R2-WR-01 was latent before this round fixed it. Recorded as partial, not blocking."
    artifacts:
      - path: "scripts/model-tiers.ts"
        issue: ":1007 (`JSON.stringify(preset)`), :1178 (`String(JSON.stringify(alias))`), :1250-1252 (`quoteValue`) — two spellings of one operation in a module that argues against exactly that, and neither is total."
    missing:
      - "Make `quoteValue` total (try/catch → a shape description) and route both sites through it, so there is one authority for 'quote a value into a refusal'."
      - "Two cases beside the existing Floor 0b cases asserting `ok:false` rather than a throw."
  - truth: "The packaging authority's pointer lands its reader somewhere that exists"
    status: partial
    reason: "NEW this round, found by this verifier and not reported by the round-3 review. Plan 29.1-16 rewrote the `model` bullet of agent-factory/packaging/subagent.frontmatter.md to defer the precedence rule to 'the `### models sub-fields` section of `factory.config.md` … the single authority for the rule and the only place this kit states it'. That section reference is UNPINNED. Measured: renaming the heading `### \\`models\\` sub-fields` → `### \\`models\\` field details` in agent-factory/config/factory.config.md leaves `npx vitest run scripts/model-dial-consistency.test.ts` at 33 passed (33). The oracle pins the pointer's PATH (`SURFACE_ROLES.pointer`) and a different section name ('Host-CLI scope of the model dial'), never this one. The section exists today, so this is latent — but 'a pointer to a fact that is not there' is the defect class scripts/model-tiers.ts:860 names in its own prose."
    artifacts:
      - path: "agent-factory/packaging/subagent.frontmatter.md"
        issue: ":205-207 names a section of another shipped document by heading text, with nothing asserting that heading exists."
    missing:
      - "Assert in scripts/model-dial-consistency.test.ts that every `### …` section name the authority cites is present as a heading in the cited surface — derived from the citation, not hand-listed."
  - truth: "No record in this tree makes a claim wider than its mechanism"
    status: partial
    reason: "`scripts/check-public-docs-vocabulary.ts:32` reads 'What this gate does NOT do is widen check-kit-refs's SCAN — that set is byte-unchanged'. SCAN is now one member wider (`agent-factory/config`, :98). Read plainly it is a present-tense claim about SCAN and it is false. 29.1-17-SUMMARY.md deviation 4 discloses it, declines to edit it (outside the plan's declared `<files>`) and explicitly asks for a ruling; WINDOWS.md row 79 logs it open. Honest disclosure, unclosed record."
    artifacts:
      - path: "scripts/check-public-docs-vocabulary.ts"
        issue: ":32 — a standing claim about another module's constant, falsified by this round."
    missing:
      - "Reword to scope the claim to what that gate's own Phase-27 change did, or delete the clause. One line."
deferred: []
human_verification:
  - test: "MODEL-06 / D5 (carried unchanged from rounds 1 and 2): read the four vendor citations (Codex CLI, Gemini CLI, OpenCode, GitHub Copilot CLI) in 29.1-RESEARCH.md §'Host-CLI per-agent model support' against their live vendor pages, and confirm the authority sentence at agent-factory/packaging/subagent.frontmatter.md:249, the amended MODEL-06 text and the amended ROADMAP SC-4 genuinely agree in substance."
    expected: "Each citation's quoted claim (a per-agent `model` field in that CLI's own agent-definition format) matches what the vendor page currently states; the three prose surfaces assert nothing beyond (a) grugops emits per-agent adapters for Claude Code alone and (b) every claim about the other four carries a named source."
    why_human: "Citation fidelity to external vendor documentation is not a predicate this verifier's tooling can check. I confirmed the citations are present, dated, specific and internally consistent, and that the refuted premise is absent everywhere — not that the vendor pages still say what is quoted. This is what this repository's 'never fake a citation' rule exists to route to a human."
  - test: "MODEL-07/boundary and MODEL-07/precision (carried unchanged from rounds 1 and 2, plan 29.1-06 `verification: backstop`) — confirm no phase-touched prose asserts a cost, spend or usage-limit saving."
    expected: "Zero cost or limit-savings claims. This round's mechanical re-scan found only honest denials (agent-factory/writing-profile.md:240 is a denial, not a claim); `git diff 6f8411e..HEAD -- scripts/measure-cost.ts scripts/measure-cost.js` is empty across the WHOLE phase, so the standing `UNKNOWN - verify` is untouched."
    why_human: "D-59/D-16: an open-set totality is not a decidable predicate here, and docs/audit/28-claim-registry.md discloses the direction as FAIL-OPEN — there is no cost literal group in check-banned-claims, so a cost sentence would pass every gate in this repository green. The registry row carries an id because the direction is open, not because anything closed it. This is the phase's own declared human-adjudication item."
  - test: "Rule on 29.1-17 deviation 4 — whether scripts/check-public-docs-vocabulary.ts:32's 'that set is byte-unchanged' needs the same correction the three other falsified records received."
    expected: "A ruling. This verifier's reading is that the clause is a present-tense claim about SCAN, is now false, and is a one-line fix; the executor's reading is that it is scoped in context to that gate's own Phase-27 change. It is listed above as a gap on that basis, and the ruling may downgrade it."
    why_human: "The executor explicitly declined to edit a file outside its plan's declared scope and asked for a ruling. Whether a clause is read in context or as standing is a judgement about prose, not a predicate."
---

# Phase 29.1: Per-Role Model Assignment — Verification Report, ROUND 3

**Phase Goal:** A project can spend its model budget where judgment actually lives — a stronger model for orchestration, architecture and security review, a cheaper one for implementation, QE and UAT — set once on the config dial and emitted into every generated adapter, with zero-config behaviour byte-identical to today's.

**Verified:** 2026-08-20T19:30:00Z
**Status:** `gaps_found`
**Re-verification:** Yes — round 3, after gap-closure plans 29.1-12 … 29.1-17 targeting all twelve round-2 findings.

## Verdict in one paragraph

**Round 3 did the hard thing and did it well, and then broke a different gate doing it.** Every one of the twelve round-2 findings is genuinely closed — I reproduced the round-2 blocker on the pre-round-3 committed build and watched HEAD refuse it, and I re-proved MODEL-05's own text behaviourally with four planted mutations rather than accepting any SUMMARY claim. All five roadmap success criteria now hold. **MODEL-05 is ruled COMPLETE** — that ruling was correctly left to this verifier and it is the round's real result. But plan 29.1-17 added a brand-new exemption to a twenty-two-phase-old zero-tolerance gate, and that exemption is bypassable on two independent axes, both of which I reproduced myself on a hermetic mirror against the committed `.js`. The gate publishes `the config self-reference exemption is exactly 2 hit(s) in 1 file(s), as declared` at exit 0 while the shipped configuration reference carries the literal instruction *"Edit `agent-factory/config/` directly"* — verbatim what D-08.1 exists to forbid. **A green verdict that misdescribes its own run is precisely the failure this project regressed MODEL-05 for one round ago.** It has not been retired; it has been relocated. Status is `gaps_found` on that basis and on that basis alone.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Zero-config: all 17 adapters byte-identical to today's, `model: inherit`. (MODEL-01, MODEL-02) | ✓ VERIFIED | `git diff --stat 6f8411e HEAD -- .claude/agents/` **empty** across the whole phase (baseline SHA read from the pin at `scripts/adapter-byte-baseline.test.ts:72`, full 40 chars). `grep -h '^model:' .claude/agents/*.md \| sort \| uniq -c` → `17 model: inherit`. |
| 2 | One opt-in key switches to the preset; role→tier derived against `kit-model.listRoles()` with an asserted count. (MODEL-03) | ✓ VERIFIED | Re-proven behaviourally, not by reading. Planted an 18th role into a `git archive HEAD` mirror: guard exits 1 and names **four** distinct facts — 17 adapters vs 18 stems, 17 table rows vs 18 stems, `"zz-new-role"` has no TIERED entry (CORPUS → TABLE), and `"zz-new-role"` resolves to `inherit` with no committed adapter. `tieredCorpusRefusals` asserts set equality in **both** directions (`scripts/model-tiers.ts:894-916`) and is called unconditionally by the guard. |
| 3 | Only `inherit`/`opus`/`sonnet`/`haiku` legal; anything else refused fail-closed; emitted model asserted equal to the resolved config. (MODEL-04, MODEL-05) | ✓ VERIFIED — **round 2's FAILED is repaired** | See the split table below. Nine config shapes refused by name through the production reader; three planted mutations RED by name; the R2-CR-01 rendering defect closed AND proven to discriminate against the pre-fix build. |
| 4 | Claude-Code-only scope stated in the kit and in `CLAUDE.md`; every clause about the other four host CLIs carries a named source. (MODEL-06) | ✓ VERIFIED (mechanical) + human item | Authority sentence present at `agent-factory/packaging/subagent.frontmatter.md:249`, stating the reach as a property of what this kit emits. `CLAUDE.md:125` names the `models` dial as the documented reason. The refuted premise returns nothing on a live grep. Citation fidelity to vendor pages remains a human item (D5), unchanged in kind since round 1. |
| 5 | Any cost or limit-savings claim is measured or carries `UNKNOWN - verify`. (MODEL-07) | ✓ VERIFIED (mechanical) + backstop | `git diff 6f8411e..HEAD -- scripts/measure-cost.ts scripts/measure-cost.js` **empty across the whole phase**. A live scan for cost/saving language over `agent-factory/`, `AGENTS.md` and `CLAUDE.md` returns one hit and it is a **denial** (`agent-factory/writing-profile.md:240`). The totality half is `verification: backstop` with its FAIL-OPEN direction disclosed in `docs/audit/28-claim-registry.md` — a human item. |

**Score: 5/5 roadmap success criteria verified** (up from 4/5 in round 2, and 3/5 in round 1).

**And the phase is `gaps_found` anyway.** The two blockers below falsify no success criterion — they falsify the D-08.1 gate contract this round extended in order to close SC-4's D6 disposition. Decision-tree rule 1 fires on a blocker regardless of the criterion score, and this repository's own history is the reason: a green verdict that misdescribes its own run is not a cosmetic defect here.

### Criterion 3, split by clause (re-assessed — every row measured this session)

| Clause | Status | Evidence |
|--------|--------|----------|
| Only the four aliases are legal | ✓ VERIFIED | `readModelsConfig` on a planted mirror refuses `{"roles":{"architect-design":"gpt-5"}}` and `{"roles":{"architect-design":null}}` by name. |
| The `models` block's own key set is closed | ✓ VERIFIED | `{"models":{"preset":"none","rolez":{}}}` refused by name. |
| Preset vocabulary closed including nullish | ✓ VERIFIED | `{"preset":"cheap"}` and `resolveModels(stems,{preset:null})` both refused by name. |
| Unknown role key refused | ✓ VERIFIED | `{"roles":{"nope":"opus"}}` refused by name at the production reader. |
| Wrong container types refused | ✓ VERIFIED | `{"models":"tiered"}` and `{"models":{"roles":[…]}}` both refused, each naming the key. |
| Malformed JSON refused | ✓ VERIFIED | `{"models":{` refused by name. |
| The container-level `overrides` value is validated with the same rigour | ✓ VERIFIED (round 2's ⚠️ PARTIAL closed) | Floor 0b measured: `overrides: null` → `ok:false` naming null; `overrides: {plain object}` → `ok:false` naming the shape (was an uncaught `TypeError` in round 2). |
| The emitted model of all 17 adapters equals the resolved config, derived not hand-listed | ✓ VERIFIED | Mutation A: one adapter flipped `inherit`→`opus` → guard exit 1 naming file, declared value and resolved value. Mutation B: `{"models":{"preset":"tiered"}}` with stale adapters → guard exit 1 naming every divergent stem. No hand-listed expectation exists anywhere in the guard. |
| Fail-closed to `inherit`, never to a pinned tier | ✓ VERIFIED | Mutation C: `{"models":{"preset":"cheap"}}` → the expectation DEGRADES to `inherit` for every stem, the guard **blocks** (exit 1), and the source label reads `no source established — the configuration read was REFUSED` (round 1's WR-08 wording, still correct). |
| A stray `model` pin on a non-agent surface is caught | ✓ VERIFIED | Planted `model: opus` into `.claude/skills/grugops-gate/SKILL.md` on a mirror → exit 1, `1 of the 16 non-agent adapter surface(s)`, file and value named; run summary reads `16 of 16 … 1 found and named above`. Denominator derived from `SPAWN_GRANT_SCAN`, not hand-listed. |
| **The verdict and refusal text are trustworthy** (round 2's FAILED clause) | ✓ VERIFIED | **The pre-fix/post-fix contrast, measured:** `git show d38a212:scripts/check-foundation-guards.js` run with `CHECK_ROOT=.` prints `preset "none" from agent-factory/config/factoryconfig.json` — a file that does not exist. HEAD prints `agent-factory/config/factory.config.json`. Held additionally under a root containing regex metacharacters (`wei+rd (x)`), a trailing-slash root, `./`, `./scripts/..` and a prefix-sibling root. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/model-tiers.ts` (+ committed `.js`) | The single resolver: alias vocabulary, TIERED table, two-location reader, anchored announcement grammar | ✓ VERIFIED | Behaviourally exercised across 9 config shapes and 8 payload shapes. Two residuals (see gaps 3). |
| `scripts/check-foundation-guards.ts` (+ `.js`) | `guard_model_assignment` with a recomputed expectation | ✓ VERIFIED | Four planted mutations RED by name; R2-CR-01 closed and discriminating. |
| `scripts/check-kit-refs.ts` (+ `.js`) | D-08.1 zero-tolerance scan, now including `agent-factory/config` with a counted exemption | ✗ **BYPASSABLE** | Two live bypasses reproduced. SCAN widening itself is correct (10 members, premise confirmed at `install/install.ts:1065`). |
| `agent-factory/packaging/subagent.frontmatter.md` | The single authority for the scope sentence; no restated precedence rule; no kit-internal path | ⚠️ PARTIAL | `grep -c 'agent-factory/config/'` = **0** (R2-WR-04 closed). Its section pointer is unpinned (gap 4). |
| `agent-factory/config/factory.config.md` | The config field reference; whole-file precedence + shadowing documented | ✓ VERIFIED | `### \`models\` sub-fields` at :121; precedence + shadowing paragraph at :138. |
| `CLAUDE.md` "What NOT to Use" row | Amended to name the `models` dial as the documented reason | ✓ VERIFIED | :125, present and correct. |
| `.github/workflows/ci.yml` ubuntu gate block | `generate:adapters` + an adapter-cleanliness check that sees untracked files | ✓ VERIFIED | :141-148. The `git status --porcelain` + `test -z` pair sees `??` entries; the runner's default `bash -e` fails the step. |
| `docs/audit/28-claim-registry.md` | MODEL-07 held as content with its fail-open direction disclosed | ✓ VERIFIED | Row present at :780, direction disclosed, `measure-cost.ts` byte-unchanged. |
| `.planning/WINDOWS.md` | Round-3 residuals logged | ✓ VERIFIED | Rows 74-79 added this round; all six are honest and specific. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `scripts/generate-role-adapters.ts` | `scripts/model-tiers.ts` | `resolveModels` above `adapters.map()` | ✓ WIRED | All 17 adapters carry the resolved value; a tiered mirror reds the guard on every stem. |
| `scripts/check-foundation-guards.ts` | `scripts/model-tiers.ts` | `readModelsConfig` + `resolveModels` + `tieredCorpusRefusals` + `inheritForEveryStem` | ✓ WIRED | Unconditional call confirmed by the 18th-role mirror printing `tieredCorpusRefusals`' CORPUS → TABLE finding. |
| `scripts/check-foundation-guards.ts` | `scripts/canonical-frontmatter.ts` | `admit()` (D-15) | ✓ WIRED | The stray-pin arm's unadmittable skip path defers to `guard_wr05` and names the excluded files. |
| `agent-factory/packaging/subagent.frontmatter.md` | `agent-factory/config/factory.config.md` | prose pointer to `### \`models\` sub-fields` | ⚠️ PARTIAL | Target exists; the citation is unpinned (renaming the heading leaves the oracle 33/33 green). |
| `install/install.ts` | `agent-factory/config/` | `cpSync(join(GRUGOPS_SRC, "agent-factory"), tmp, {recursive:true})` | ✓ WIRED | :1065 — the D6 premise is true; the config reference ships to every installed target. |
| `scripts/check-kit-refs.ts` SCAN | `agent-factory/config` | SCAN member 5 of 10 | ⚠️ WIRED BUT BYPASSABLE | The directory is in scope; the exemption governing it is not sound. |

### Behavioural Spot-Checks

| Behaviour | Command | Result | Status |
|-----------|---------|--------|--------|
| R2-CR-01 defect reproduces on the pre-fix build | `git show d38a212:…check-foundation-guards.js > probe.js; CHECK_ROOT=. node probe.js` | `preset "none" from agent-factory/config/factoryconfig.json` | ✓ PASS (defect confirmed pre-fix) |
| R2-CR-01 refused at HEAD | `CHECK_ROOT=. node scripts/check-foundation-guards.js` | `…from agent-factory/config/factory.config.json`, exit 0 | ✓ PASS |
| Rendering holds on a metacharacter root | `CHECK_ROOT='…/wei+rd (x)' node …` | correct path rendered | ✓ PASS |
| Rendering holds on trailing-slash / `./` / `./scripts/..` / prefix-sibling roots | four runs | correct path in all four | ✓ PASS |
| 18th role cannot arrive silently unassigned | 18th role planted on a mirror | exit 1, four findings, stem named | ✓ PASS |
| Adapter model mismatch is caught | `model: inherit` → `model: opus` on one adapter | exit 1, file + both values named | ✓ PASS |
| Tiered preset with stale adapters is caught | `{"models":{"preset":"tiered"}}` | exit 1, every divergent stem named | ✓ PASS |
| Malformed preset degrades to `inherit` AND blocks | `{"models":{"preset":"cheap"}}` | exit 1, `no source established` | ✓ PASS |
| Stray `model` pin on a SKILL is caught | pin planted on a mirror | exit 1, `1 of the 16`, file + value named | ✓ PASS |
| Nine config shapes refused by the production reader | `readModelsConfig` on nine planted mirrors | 8 refused by name, 1 legal accepted | ✓ PASS |
| Eight announcement payload shapes | `resolvedAssignmentsIn` | 7 named refusals, 1 accepted | ✓ PASS |
| Floor 0b refuses a non-Map `overrides` | `resolveModels(stems,{overrides:null \| {} })` | `ok:false`, shape named | ✓ PASS |
| **`resolveModels` refusal path throws** | 4 shapes (circular / BigInt × preset / override) | **4 × TypeError** | ✗ FAIL (gap 3) |
| **The `(r-bound)` proof discriminates** | delete the right bound, run the file | **263 passed (263)** | ✗ FAIL (gap 2) |
| **The section pointer is pinned** | rename `### \`models\` sub-fields`, run the oracle | **33 passed (33)** | ✗ FAIL (gap 4) |
| Third mention on a NEW line is refused | mirror + appended line | exit 1, `found 3, required exactly 2` | ✓ PASS |
| A second exempting FILE is refused | `agent-factory/config/README.md` planted | exit 1, both cardinalities named | ✓ PASS |
| **Third mention on an EXISTING exempt line** | appended to line 3 → and separately to line 138 | **exit 0 both times, `exactly 2 hit(s) … as declared`** | ✗ FAIL (gap 1, R3-CR-01) |
| **`..` traversal accepted as a self-reference** | line 3's only named path replaced by `agent-factory/config/../../install/README.md` | **exit 0, counted as exempt** | ✗ FAIL (gap 1, R3-CR-02) |
| Full suite, once | `npx vitest run --exclude '**/scripts/e2e/**'` | 55 files, 2355 passed, 2 skipped | ✓ PASS (**and not evidence of anything** — three of the failures above were measured under a fully green suite) |
| Committed `.js` freshness | `npm run freshness` | 49/49 fresh, set-equal with the filesystem walk | ✓ PASS |
| Live gates | kit-refs, foundation-guards, public-docs-vocabulary, banned-claims, claim-anchors | all exit 0 | ✓ PASS (same caveat) |

### The blocker, stated as a predicate question

This project's standing question for any predicate is *what bounds its input, which set does it enumerate, and at which positions is it asked*. R3-CR-01 is the third of those, in its purest form yet recorded here:

> The exemption predicate is asked **once per LINE**. Once a line is exempt, every further mention on that line is never asked about at all.

The contrast is exact — the same sentence, the same file, the same gate:

```
appended as a NEW line   →  FAIL  stray agent-factory/config/ ref(s) …           exit 1
appended to line 3       →  PASS  … exactly 2 hit(s) in 1 file(s), as declared   exit 0
appended to line 138     →  PASS  … exactly 2 hit(s) in 1 file(s), as declared   exit 0
```

And the declared number **has never described this tree**: the comment at `check-kit-refs.ts:173-176` says "Exactly TWO exempt mentions … factory.config.md:3 … and factory.config.md:138", while `grep -o 'agent-factory/config/' agent-factory/config/factory.config.md | wc -l` measures **3 occurrences on 2 lines**. The gate counts lines and calls them mentions. The test file's own `linesContaining()` docstring already knows the distinction ("a line naming the kit-internal path twice is ONE hit … the file carries THREE substring occurrences and TWO hits") — the knowledge is in the tree, in the wrong place, and the constant that publishes the claim was never reconciled with it.

**Same shape checked elsewhere in what round 3 touched, and NOT found:** `CONFIG_SELF_REF_FILES` counts files and claims files (correct — behaviourally confirmed). `GATE_SCAN_MEMBERS = 10` counts members of a derived array, both-sided, with both throws exercised. The stray-pin denominator is derived from `SPAWN_GRANT_SCAN` with unadmittable surfaces excluded *and named*. `MODEL_TIERS_COUNT` is asserted against `TIERED.length` and `ROLE_COUNT` in one arm naming all three numbers. The one other instance of this class in the round is already self-reported: 29.1-17-SUMMARY.md deviation 3 records the disposition table's "fifteen" as entry-granularity and "sixteen" as item-granularity, and prints both.

### Requirements Coverage

| Requirement | Declaring plans | Status | Evidence |
|-------------|-----------------|--------|----------|
| MODEL-01 | 9 plans (01, 03, 07, 11, 12, 14, 15, 16, 17) | ✓ SATISFIED | SC-1 verified: adapter bytes empty-diff against the pinned 40-char baseline. |
| MODEL-02 | 5 plans (01, 05, 11, 16, 17) | ✓ SATISFIED | Generator emits the resolved value; `subagent.frontmatter.md` updated in lockstep and free of kit-internal paths. |
| MODEL-03 | 6 plans (02, 04, 09, 12, 13, 15) | ✓ SATISFIED | 18th-role mirror reds with four named findings; two-directional set equality. |
| MODEL-04 | 5 plans (02, 03, 07, 08, 15) | ✓ SATISFIED | Nine config shapes and eight payload shapes refused by name; no coercion. |
| MODEL-05 | 8 plans (02, 04, 08, 09, 12, 13, 14, 17) | ✓ **SATISFIED — REGRESSION REPAIRED** | The R2-CR-01 rendering defect is closed and the fix DISCRIMINATES against the pre-fix committed build. All four planted mutations RED by name. **Recommendation: flip `.planning/REQUIREMENTS.md:94` to `[x]` and the traceability row to `Complete`.** Plan 29.1-17 correctly declined to make this call. |
| MODEL-06 | 4 plans (05, 10, 11, 16) | ✓ SATISFIED (mechanical) | Authority sentence, `CLAUDE.md` row, sourced clauses all present; refuted premise absent. Citation fidelity = human item. |
| MODEL-07 | 5 plans (06, 10, 11, 16, 17) | ✓ SATISFIED (mechanical) | `measure-cost.ts` byte-unchanged across the phase; only honest denials in prose; registry row discloses the fail-open direction. Totality = backstop human item. |

**Orphans:** none. ROADMAP declares MODEL-01…07 for Phase 29.1; all seven are claimed by at least four plans each; no REQUIREMENTS.md row maps an unclaimed ID to this phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/check-kit-refs.ts` | 173-178, 217-228, 246-283, 402-420 | Counted unit ≠ claimed unit; a predicate asked once per line | 🛑 Blocker | Live bypass of a zero-tolerance gate; published verdict misdescribes its run |
| `scripts/check-kit-refs.ts` | 186, 270 | Relative names admitted by a character-class pattern; `existsSync` accepts a directory | 🛑 Blocker | A path leaving the kit's config directory is certified as a reference inside it |
| `scripts/check-foundation-guards.test.ts` | 10869-10883 | Assertion true by construction; unreachable failure message | ⚠️ Warning | The R2-WR-02 bound has no case that can see it (measured) |
| `scripts/skill-twins-freshness.test.ts` | 291-293 | Same tautology, different slice base | ⚠️ Warning | `(r-class)` certifies two readers that are not the same slice |
| `scripts/model-dial-consistency.test.ts` | 900-901 | Two containment assertions guaranteed by the slice | ℹ️ Info | Dead weight; the throw paths are genuinely proven |
| `scripts/model-tiers.ts` | 1007, 1178, 1250-1252 | Two spellings of one operation; neither total | ⚠️ Warning | The refusal path throws on the value it is refusing |
| `agent-factory/packaging/subagent.frontmatter.md` | 205-207 | Cross-document section citation with nothing asserting the target | ⚠️ Warning | A rename silently orphans the only pointer to the precedence rule |
| `scripts/check-public-docs-vocabulary.ts` | 32 | Standing claim falsified by this round | ⚠️ Warning | Disclosed (WINDOWS #79), unfixed |
| `scripts/check-foundation-guards.test.ts` | 10609 | Hand-typed `10` inside a NEGATIVE assertion | ℹ️ Info | Confirmed `CANONICAL_SCHEMA.length === 10`; an eleventh key makes the assertion vacuous forever. `CANONICAL_SCHEMA` is already imported at :40 |
| `scripts/check-foundation-guards.test.ts` | 10437-10448 | Fixed 300-byte window as a census predicate | ℹ️ Info | A second consumer building its pattern into a local first is invisible to the residual count |
| `scripts/model-tiers.ts` | 372-398 | Docstring wider than the mechanism | ℹ️ Info | Measured: `resolvedPresetsIn(PREFIX+"none   ")` → `["none"]`; the docstring says trailing whitespace is "never" trimmed off the captured value. Anchoring itself is correct (`"  "+PREFIX+"none"` → `[]`) |
| `scripts/model-tiers.ts` | `resolveModels` | Silent discard of an unknown-stem override | ℹ️ Info | `resolveModels(stems,{overrides:Map([["nope","opus"]])})` → `ok:true`, all 17 `inherit`, no message. **Not reachable from the config path** — `readModelsConfig` refuses unknown role keys by name (verified). Latent, in the same sense R2-WR-01 was |
| `scripts/check-kit-refs.ts` | 246-283 | Exemption scoped by directory prefix, unbounded in depth | ℹ️ Info | A future `agent-factory/config/sub/` would be wholesale exemptable: the basename capture takes `sub`, `existsSync` is true for the directory. No subdirectory exists today |

**Debt markers (`TBD` / `FIXME` / `XXX`) in phase-touched files:** none found.

### What round 3 got right, on the record

Four of six round-3 plans hit a **false plan premise** and every one of them recorded the measurement rather than adjusting the wording to fit:

- **29.1-14 §1** — on the appended-step mutation the OLD pin reds on its byte-offset half; a composite mutation 3b was constructed where the old pin is green on all four assertions. Honest.
- **29.1-15 §1** — the plan's floor shape did not hold as written; recorded.
- **29.1-16 §1** — the plan's basename-only paraphrase (mutation D) *cannot* red the derived case, because the second candidate's basename `factory.config.json` is a substring of the first candidate `.grugops/factory.config.json`. The plan's criterion is contradictory on a correct document. Recorded **NOT satisfied as written**, with the `suite exit=0 <-- NOT caught` transcript printed, and a discriminating substitute (mutation C) demonstrated instead. This is the correct handling of an unsatisfiable criterion, and the residual (a bullet naming the second location by bare basename goes undetected) is genuinely low-impact — that spelling is ambiguous to a human reader too.
- **29.1-17 §1** — `grep -c 'const SCAN' scripts/check-kit-refs.test.ts` measures **1, not 0**; splitting the literal to make the grep return zero was *rejected* as gaming the predicate, and two anchored substitutes were run instead. Independently confirmed: the surviving hit is the extractor's locator at `:66`, which must spell the gate's declaration verbatim to find it.
- **29.1-17 §5** — the requirements verb flipped MODEL-05 to `Complete`; the executor **reverted it** because no verification round had run since the regression. That restraint is why this verifier had a real question to answer, and the answer is: the flip is now earned.

This discipline is why the residual findings are narrow rather than structural, and it is worth saying plainly before the blockers are read.

### Human Verification Required

Three items, all carried and none new in kind. See the `human_verification` block in the frontmatter for full text.

1. **MODEL-06 / D5** — vendor citation fidelity for the four non-Claude host CLIs.
2. **MODEL-07/boundary + MODEL-07/precision** — the declared `verification: backstop` totality item, direction disclosed FAIL-OPEN.
3. **29.1-17 deviation 4** — rule on `check-public-docs-vocabulary.ts:32`. Listed above as a gap; the ruling may downgrade it.

**Round 2's D6 is CLOSED as a decision** (the user ruled it, plan 29.1-17 implemented it, and its premise — that `install.ts:1065` ships `agent-factory/config/` — is independently confirmed). It is no longer a human item; its implementation is gap 1.

### Gaps Summary

Two blockers and three warnings, all reproduced by this verifier rather than inherited from the review.

**The blockers are one fix and one fix only in spirit: count the unit you claim, and say what a name may BE rather than what it may not.** Both live in the exemption plan 29.1-17 added to `check-kit-refs`. The gate's own header demands that a new mention "must be argued and counted, never absorbed" — today it is absorbed, silently, by appending to either of two lines; and a path climbing two directories out of the kit's configuration directory is certified as a reference to a sibling inside it. Neither is exotic: the first is reached by ordinary editing of a paragraph, and the second by an ordinary relative link.

**The warnings share one shape too:** a proof that cannot fail. The `(r-bound)` assertion, the two `occurrences(region, …)` lines, and the unpinned section pointer are each green in a world where the thing they claim to prove has been deleted — and I deleted each and measured. This is the round's own most-repeated lesson turned back on it: *assert the verification harness's own premise*.

A follow-up round can consume the `gaps` block directly. The blockers are both in one file and one function; the warnings are four one-to-ten-line edits. Nothing here requires re-opening a design decision, and nothing here touches MODEL-05, which is done.

---

_Verified: 2026-08-20T19:30:00Z_
_Verifier: Claude (gsd-verifier), round 3_
_Method: goal-backward, adversarial. Every ✓ and every ✗ in the spot-check table was executed this session against the committed `.js` on `git archive HEAD` mirrors or on the live tree. The full suite was run exactly once and is treated as evidence of nothing._

---
phase: 29.1-per-role-model-assignment
plan: 13
type: execute
wave: 2
depends_on: ["29.1-12"]
gap_closure: true
files_modified:
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - scripts/check-foundation-guards.test.ts
autonomous: true
requirements: [MODEL-03, MODEL-05]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "R2-WR-03 closed: the comment that justifies the stray-pin arm's silent `continue` states the MEASURED mechanism. The packaging templates are ADMITTED — `kind` and `tier` are both members of `CANONICAL_SCHEMA` — and they are skipped by the no-`model`-key arm, not by the unadmittable arm. The recorded soundness argument no longer names a mechanism the measurement contradicts."
    - "R2-WR-03's unexercised arm is closed by a case, not by a comment: a non-agent surface that is genuinely unadmittable is planted, and `guard_wr05` is shown to name it and fail the gate closed. The claim that the unadmittable arm's silence is covered elsewhere is now a demonstrated fact rather than an assumption resting on a false example."
    - "R2-WR-03's denominator is honest: the run summary reports how many non-agent surfaces were actually PROBED against how many were derived, and names any surface that was skipped with its enumerated refusal code. A number that silently counted members it never read is replaced by two numbers a reader can tell apart."
    - "The three arms of the stray-pin loop are tested as a UNION, not one at a time: an admitted surface carrying a `model` key is reported as a stray pin; an admitted surface carrying none is silently and correctly skipped; an unadmittable surface is excluded from the probed count, named in the verdict with its code, and independently reds `guard_wr05`. A mirror producing more than one of these at once is exercised."
    - "R2-IN-01 closed: case `(m)` pins the advisory CHANNEL rather than one identifier. The guard's advisory channel is `process.stdout.write`, and `guardModelAssignment` calls it for its section header, so a degraded branch rewritten as a direct write would have cleared the old probe while restoring the fail-open. The case now derives the guard body's write sites and asserts there is exactly one, that it is the header, and that the body still makes no call to the advisory helper."
    - "Every new assertion states what its input is ASSEMBLED from: each case asserts the planted surface really is in the non-agent partition and really has the admission status the case is about, read through the same `admit()` the guard uses, before asserting anything about the verdict."
    - "MODEL-03's derived family list is untouched in membership: `nonAgentSurfaces` is still partitioned out of the one `SPAWN_GRANT_SCAN` composition on the same prefix, and this plan changes what the loop REPORTS, never what it scans."
    - "Per D-15 the guard keeps reading frontmatter through `scripts/canonical-frontmatter.ts`'s `admit()` and no second parser is introduced; per D-12 the guard stays the independent second opinion rather than a regenerate-and-compare check; per D-11 the degradation target is still `inherit` for every stem and never a pinned tier, and nothing this plan reports changes what the expectation degrades to."
    - "The committed `scripts/check-foundation-guards.js` is a faithful build of its `.ts`, and `relativeToRoot` as fixed by plan 29.1-12 is unchanged by this plan."
  artifacts:
    - scripts/check-foundation-guards.ts
    - scripts/check-foundation-guards.js
    - scripts/check-foundation-guards.test.ts
  key_links:
    - "`SPAWN_GRANT_SCAN` → `nonAgentSurfaces` (partition by the agent prefix) → the stray-pin loop → the run summary's probed/derived pair. One composition, one partition, two published numbers."
    - "the unadmittable arm → `guard_wr05`'s own scan over the SAME `SPAWN_GRANT_SCAN` composition → a finding naming the file and its enumerated code. That is the coverage the stray-pin arm's silence rests on, and it is now exercised by a planted surface rather than by a claim."
    - "`guardModelAssignment`'s function body → case `(m)`'s derived slice → the write-site count. The assertion reads the same committed `.js` the build runs, not only the `.ts`."
  prohibitions:
    - "The membership of `nonAgentSurfaces` is not changed. This plan changes what the loop reports and what the suite proves, never which files are scanned."
    - "The unadmittable arm is not made to emit a duplicate finding for a file `guard_wr05` already names. The published fact is the excluded COUNT and the file's identity with its code, not a second copy of `guard_wr05`'s sentence."
    - "`relativeToRoot`, `ROOT` and `ROOT_PATH_BOUNDARY` as landed by plan 29.1-12 are not edited. If a message this plan writes carries a path, it is rendered through the existing helper rather than through a new one."
    - "Case `(m)` is strengthened, never replaced by a weaker probe. The existing assertion that the body makes no call to the advisory helper stays; the channel assertion is added beside it."
    - "Existing cases whose premise this change invalidates — including the control case asserting the stray-pin run-summary phrase — are REPAIRED and named in the SUMMARY, never deleted and never weakened."
    - "No unmeasured cost, spend, quota or usage-limit claim in any comment, finding or message this plan writes."
    - "No caveman voice in any guard finding, comment or refusal text."
    - "`npm test` is never run. The regression command is `npx vitest run --exclude '**/scripts/e2e/**'`."
---

<objective>
Close R2-WR-03 and R2-IN-01 — two recorded justifications that are wider than what can be measured.

R2-WR-03: `scripts/check-foundation-guards.ts:2172-2178` argues that the stray-pin loop may skip an
unadmittable non-agent surface in silence, because `guard_wr05` already names it, and offers the two
packaging templates as proof that the arm is exercised. Measured against the committed
`scripts/canonical-frontmatter.js`, both packaging templates are ADMITTED — `kind` and `tier` are
members of `CANONICAL_SCHEMA` — and they are skipped one line later, by the no-`model`-key arm. The
soundness argument names the wrong mechanism, and the arm it rests on is unexercised and unproven on
this tree. The verdict line beside it publishes a count of surfaces "checked" that includes members
it never read.

R2-IN-01: case `(m)` asserts that `guardModelAssignment` reaches for the advisory channel nowhere at
all, and probes for one identifier. The guard's advisory channel is `process.stdout.write`, and the
guard already calls it directly for its section header — so a future degraded branch written as a
direct write clears the probe while restoring exactly the fail-open plan 29.1-09 spent a task
closing. The stated property is wider than the probe.

Both are the round's recurring shape: the mechanism is fine, the recorded justification is not.
This plan makes each justification either true or asserted.

Output: a soundness argument that matches its measurement, an arm proven by a plant rather than by
an example that never takes it, an honest denominator, and a probe that forbids the channel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-per-role-model-assignment/29.1-CONTEXT.md
@.planning/phases/29.1-per-role-model-assignment/29.1-PATTERNS.md
@.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round2.md
@.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW-round2.md
@.planning/phases/29.1-per-role-model-assignment/29.1-09-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol or path | Kind | Purpose |
|----------------|------|---------|
| `strayPinSkipped` | new local in `guardModelAssignment` | the non-agent surfaces excluded from the stray-pin probe, each with its enumerated refusal code, so the published denominator can be honest |
| stray-pin run-summary clause | changed message in `guardModelAssignment` | reports probed-of-derived rather than a single "checked" count, and names any excluded member |
| `MODEL_GUARD_WRITE_SITES` | new derived const in `scripts/check-foundation-guards.test.ts` | the guard body's `process.stdout.write` occurrences, derived from both the `.ts` and the committed `.js` |

New test case names (`scripts/check-foundation-guards.test.ts`):
`(s) the packaging templates are ADMITTED and skipped by the no-model-key arm — the measured mechanism, pinned`,
`(s-unadmittable) an unadmittable non-agent surface is excluded from the probed count and named by guard_wr05`,
`(s-union) one mirror carrying a stray pin, a clean surface and an unadmittable surface reports all three facts`,
`(m-channel) guard_model_assignment's ONLY advisory-channel write is its section header`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The stray-pin arm's silence becomes an assertion, and its denominator becomes honest</name>
  <files>scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js, scripts/check-foundation-guards.test.ts</files>
  <behavior>
    - A non-agent surface that admits and carries a `model` key is reported as a stray pin, naming the file and every value it declares.
    - A non-agent surface that admits and carries no `model` key is skipped with no finding, and is counted as PROBED.
    - A non-agent surface that cannot be admitted is excluded from the probed count, named in the verdict with its enumerated refusal code, and independently reds `guard_wr05`.
    - The run summary states how many surfaces were probed and how many were derived, so a run that skipped members cannot read as a run that checked all of them.
    - A mirror producing all three states at once reports all three facts in one verdict.
  </behavior>
  <read_first>
    - `scripts/check-foundation-guards.ts` lines 2150-2200 — the `(PLAN 29.1-09, IN-04)` paragraph, the sentence offering the packaging templates as proof that the unadmittable arm is exercised, the `nonAgentSurfaces` partition, the stray-pin loop with its two `continue` arms, and the stray-pin finding.
    - `scripts/check-foundation-guards.ts` lines 2200-2215 — the `runSummary` template literal and its stray-pin clause. This is the number the finding calls out as silently overstating.
    - `scripts/check-foundation-guards.ts` lines 2078-2098 — the ADAPTER loop's admission handling, which already reports refusals with their code. That is the shape this task brings to the non-agent loop; reuse its wording discipline rather than inventing a third phrasing.
    - `scripts/check-foundation-guards.ts` lines 805-830 — `guardWr05`'s unadmittable arm, the finding that names the file and the enumerated code. This is the coverage the stray-pin arm's silence rests on and the behaviour the new case must observe.
    - `scripts/canonical-frontmatter.ts` lines 170-195 — `CANONICAL_SCHEMA`. Confirm by reading that `kind` and `tier` are members before writing the corrected comment; the corrected comment must state a fact you measured in this session, not one this plan asserts.
    - `scripts/check-foundation-guards.test.ts` lines 10060-10135 — the existing stray-pin cases and the CONTROL case at line 10130 that asserts the current run-summary phrase. That control's premise is invalidated by this change and must be repaired in lockstep.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW-round2.md` §R2-WR-03 in full, including its measured transcript and its proposed shape.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-09-SUMMARY.md` Part 7 — where the same false measurement is repeated. Note it for the SUMMARY's correction record.
  </read_first>
  <action>
First, MEASURE, and record the transcript before editing. Run a short Node one-liner against the
committed `scripts/canonical-frontmatter.js` that reads each of the two packaging templates named in
`SPAWN_GRANT_SCAN`'s packaging part, calls `admit()` on each, and prints the file, whether it was
admitted, and the key list it yielded. Record the output verbatim in the SUMMARY. This plan's whole
content is that a recorded measurement was never taken; taking it is the first act.

Rewrite the paragraph above the stray-pin loop to state that measurement. It must say which arm the
packaging templates actually take, name the two schema members that make them admissible, and say
plainly that the unadmittable arm is not reached by any surface this tree ships — which is why its
silence is now backed by a planted case rather than by an example. Keep the true half of the existing
argument: `guard_wr05` does name an unadmittable scan file with its enumerated code and does fail the
gate, and a second sentence for one fact is duplication this gate's conventions refuse. What changes
is that the sentence now describes the mechanism the measurement found.

Change the loop to record what it skipped instead of discarding it. Collect the excluded members into
a local list, each entry naming the file and the enumerated refusal code the admission returned —
the same shape the adapter loop above already uses, so the file has one way of saying "this could not
be read". Do NOT restate the admission reason: `guard_wr05` owns that sentence.

Change the run summary's stray-pin clause to publish two numbers rather than one: how many of the
derived non-agent surfaces were actually probed, and how many were derived. When nothing was
excluded, the clause must read as one clean fact; when something was excluded, it must name the
excluded members with their codes and say that `guard_wr05` is the finding that reports them. Keep
the existing "none found" / "found and named above" split for the stray pins themselves — that half
is correct and is asserted by existing cases.

Add the three cases, and make the third of them the union.

Case `(s)` pins the measurement: it asserts, through the same `admit()` the guard uses, that both
packaging templates admit and that neither declares a `model` key — so the arm they take is the
no-key arm. This is the case that turns the corrected comment into something a later edit cannot
quietly falsify.

Case `(s-unadmittable)` plants a non-agent surface in a mirror whose frontmatter is outside the
canonical form, asserts the plant's own premise through `admit()` — that the planted file really is
refused and with which code — then runs the guard and asserts three things: the gate exits non-zero,
`guard_wr05` names the planted file, and the model-assignment run summary reports a probed count
one lower than the derived count and names the excluded member. Choose a construct the canonical
reader refuses and confirm the refusal at the module level BEFORE relying on it; do not assume a
construct is refused because it looks exotic.

Case `(s-union)` is the arm-composition case rule 6 of this round exists for. One mirror carrying
all three states at once — one admitted non-agent surface with a `model` key, one admitted without,
one unadmittable — and one verdict asserted to carry all three facts: the stray pin named, the
probed/derived pair disagreeing by exactly one, and the excluded member named. Testing the arms one
at a time is what let the previous justification stand.

Repair the control case at line 10130 in lockstep with the new run-summary wording. Its premise —
that on an unmutated mirror nothing is excluded and no stray pin is found — is still true and must
stay asserted; only the sentence it matches changes.

Run `npm run build` and commit `scripts/check-foundation-guards.js` in the SAME commit as the `.ts`.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-foundation-guards.test.ts && node scripts/check-foundation-guards.js && npm run typecheck && npm run build && git diff --exit-code --name-only -- '*.js'</automated>
  </verify>
  <acceptance_criteria>
    - The measurement is recorded verbatim in the SUMMARY: for each packaging template in the scan composition, the admission verdict and the admitted key list, produced by a command run in this session against the committed `scripts/canonical-frontmatter.js`.
    - The corrected paragraph names the arm the packaging templates take and names the two `CANONICAL_SCHEMA` members that admit them. `grep -n 'kind' scripts/check-foundation-guards.ts` shows the corrected paragraph naming both members.
    - Case `(s)` is RED against a mutation that removes one of those two members from the corrected claim's premise — that is, if the case's own premise assertion is inverted the case fails. Record the transcript.
    - Case `(s-unadmittable)` asserts its plant's premise at the module level first: the planted text is refused by `admit()` with a named code, recorded in the SUMMARY. A case that ran the guard without first proving the plant is unadmittable is proving nothing.
    - Case `(s-unadmittable)` is RED-first: recorded against the PRE-FIX committed `.js`, the same plant produces a run summary whose stray-pin count silently includes the excluded member. Record both the pre-fix and post-fix run-summary lines.
    - Case `(s-union)` asserts all three facts from ONE run: the stray pin named by file and value, the probed count exactly one below the derived count, and the excluded member named. Mutation-prove it by making the loop count the excluded member as probed and observing the case go RED naming both numbers, then revert.
    - `guard_wr05` reds on the planted unadmittable surface, and the gate's failure count attributes at least one failure to it. Record the finding line verbatim.
    - The control case at line 10130 is repaired, still asserts that an unmutated mirror excludes nothing and finds no stray pin, and is named in the SUMMARY with what changed.
    - `node scripts/check-foundation-guards.js` exits 0 on this repository, and its stray-pin clause reports the probed and derived counts as equal.
    - `diff <(node scripts/check-foundation-guards.js 2>&1) <(CHECK_ROOT=. node scripts/check-foundation-guards.js 2>&1)` still prints nothing — plan 29.1-12's closure is not disturbed by the new message text.
    - `npm run typecheck` exits 0. After `npm run build`, `git diff --exit-code --name-only -- '*.js'` is empty.
  </acceptance_criteria>
  <done>The stray-pin arm's soundness argument states the mechanism a measurement found, the unadmittable arm is exercised by a plant rather than by an example that never takes it, the published denominator distinguishes probed from derived, and the three arms are proven as a union.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Case (m) forbids the advisory CHANNEL, not one spelling of it</name>
  <files>scripts/check-foundation-guards.test.ts</files>
  <behavior>
    - `guardModelAssignment`'s body makes exactly one call to `process.stdout.write`, and it is the section header.
    - The body still makes no call to the advisory helper, in both the source and the committed twin that actually runs.
    - A degraded branch rewritten as a direct advisory write is caught, not cleared.
  </behavior>
  <read_first>
    - `scripts/check-foundation-guards.test.ts` lines 9740-9800 — case `(m)` in full: its block comment arguing why the property is asserted from the function's own text, the `slice` and `codeOf` helpers, the two premise assertions, and the single `warn(` probe.
    - `scripts/check-foundation-guards.ts` lines 384-396 — the `pass`, `fail` and `warn` helpers and the comment recording that `warn()` is advisory and does not increment the failure count. This is the contract case `(m)` is protecting.
    - `scripts/check-foundation-guards.ts` lines 1875-1882 — `guardModelAssignment`'s section header, which is the guard's one direct `process.stdout.write` and therefore the one legitimate write site.
    - `.planning/phases/29.1-per-role-model-assignment/29.1-REVIEW-round2.md` §R2-IN-01 in full.
  </read_first>
  <action>
Strengthen case `(m)` rather than replacing it, and add the channel assertion as a named sibling case
so the two properties are separately attributable when one of them reds.

Reuse case `(m)`'s existing `slice` and `codeOf` helpers unchanged — they already bound the function
body on both ends and drop whole-line comments, and the case already proves the drop did not eat the
body. Do not write a second slicer.

Add case `(m-channel)`: derive, from the same sliced and comment-stripped body of both
`scripts/check-foundation-guards.ts` and `scripts/check-foundation-guards.js`, the list of
occurrences of the direct advisory write call. Assert the list has exactly one member and that the
line carrying it is the section-header write, identified by a stable fragment of the header text the
guard prints. Write the failure message so it names the count it found and the count it requires and
quotes every offending line, because a bare count tells a reader nothing about which write appeared.

Record in the case body what this case buys over case `(m)`, in one paragraph: the guard's advisory
channel is the direct write, not the helper; the helper is one spelling of the channel; a degraded
branch that wrote the advisory prefix directly would have cleared the old probe while restoring the
fail-open plan 29.1-09 closed. State that the two cases together forbid the channel, and that case
`(m)` alone forbade an identifier.

Narrow case `(m)`'s own stated claim in its message text to what it actually pins — no call to the
advisory helper — so no case in this file continues to assert a property wider than its probe.

Prove the discrimination by mutation, in the direction the finding names: add a direct advisory write
carrying the advisory prefix inside one of the guard's degraded branches, rebuild, and observe
`(m-channel)` go RED while case `(m)` stays GREEN. That contrast is the whole content of this task
and both halves must be recorded. Then revert and confirm the working tree is clean.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-foundation-guards.test.ts && npm run typecheck && node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - `(m-channel)` derives its write-site list from BOTH `scripts/check-foundation-guards.ts` and the committed `scripts/check-foundation-guards.js`, using case `(m)`'s existing slicer, and asserts exactly one site in each.
    - `(m-channel)` asserts the one site is the section header by matching a stable fragment of the header text, not by matching a line number.
    - Mutation transcript recorded, both halves: with a direct advisory write planted inside a degraded branch and the build regenerated, `(m-channel)` fails and its message names the found count, the required count and the offending line; case `(m)` passes unchanged. After reverting, both pass and `git status --porcelain` is empty.
    - Case `(m)`'s own assertion message no longer claims the guard reaches for the advisory channel nowhere at all; it claims what it pins. Confirm by reading the message text and quoting the before and after in the SUMMARY.
    - Case `(m)`'s existing premise assertions are unchanged and still present: the sliced body reaches the stray-pin arm, and the probe can see a call at all.
    - `npx vitest run scripts/check-foundation-guards.test.ts` exits 0, case count recorded before and after.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0. `npm test` is not run.
    - `npm run typecheck` exits 0. `git status --porcelain` shows no leftover mutation.
  </acceptance_criteria>
  <done>The advisory channel, not one identifier, is what the suite forbids inside guard_model_assignment, proven by a mutation that reds the new case while the old one stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped non-agent surface → the platform | A skill, plugin-form twin or packaging template is loaded by the host; a `model` pin on one is a tier nobody adjudicated |
| guard verdict → the reader | A count of surfaces "checked" is read as a count of surfaces read |
| guard source → the suite's derived assertions | Assertions derived from a module's own text are only as good as the region they bound and the channel they name |
| committed `.js` → every host | Hosts run the committed output with no toolchain |

## STRIDE Threat Register

ASVS level 1, block on `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.1-36 | Repudiation | the stray-pin run summary's "checked" count | high | mitigate | The clause publishes probed-of-derived and names every excluded member with its enumerated code; the union case asserts the two numbers disagree by exactly one when a member is excluded. |
| T-29.1-37 | Tampering | an unadmittable non-agent surface skipped with no finding | high | mitigate | The arm's coverage is proven by a planted unadmittable surface that reds `guard_wr05` by name, replacing an example that never takes the arm. |
| T-29.1-38 | Elevation of privilege | a degraded branch restored through the direct advisory write | high | mitigate | `(m-channel)` derives the guard body's write sites from source and committed twin and asserts exactly one, proven by a mutation that reds it while the old identifier probe stays green. |
| T-29.1-39 | Repudiation | a recorded soundness argument that names a mechanism the measurement contradicts | medium | mitigate | The measurement is taken in session and recorded verbatim; case `(s)` pins the arm the packaging templates actually take. |
| T-29.1-05 | Tampering | committed `scripts/*.js` diverging from its `.ts` | high | mitigate | `npm run build` plus `git diff --exit-code --name-only -- '*.js'` in the task verify; `npm run freshness` in the plan verification. |
| T-29.1-SC | Tampering | npm/pip/cargo installs | high | accept | Zero packages installed by this plan; `package.json` asserted byte-unchanged in the plan verification. |
</threat_model>

<verification>
- `npx vitest run scripts/check-foundation-guards.test.ts` exits 0.
- `node scripts/check-foundation-guards.js` exits 0 on this repository and its stray-pin clause reports equal probed and derived counts.
- `diff <(node scripts/check-foundation-guards.js 2>&1) <(CHECK_ROOT=. node scripts/check-foundation-guards.js 2>&1)` prints nothing — plan 29.1-12's byte identity still holds.
- `node scripts/generate-role-adapters.js` then `git diff --exit-code -- .claude/agents/` is empty.
- `npm run freshness` reports every committed output fresh. `npm run typecheck` exits 0. `npm run build` then `git diff --exit-code --name-only -- '*.js'` is empty.
- `npx vitest run --exclude '**/scripts/e2e/**'` exits 0. `npm test` is never run.
- `git diff --name-only -- install/ package.json scripts/measure-cost.ts scripts/measure-cost.js scripts/model-tiers.ts agent-factory/` is empty.

</verification>

<success_criteria>
- The recorded justification for the stray-pin arm's silence states a mechanism that was measured in session, and the measurement is in the SUMMARY.
- The previously unexercised unadmittable arm has a planted case, and `guard_wr05`'s coverage of it is a demonstrated fact.
- The published denominator distinguishes surfaces probed from surfaces derived, and the union case proves the distinction is observable.
- The advisory channel is forbidden inside the guard, proven by a mutation that reds the new case while the old identifier probe stays green.
</success_criteria>

<output>
Create `.planning/phases/29.1-per-role-model-assignment/29.1-13-SUMMARY.md` when done.
Record in it: the packaging-template admission measurement verbatim; the planted unadmittable
construct and the enumerated code `admit()` returned for it; the pre-fix and post-fix run-summary
lines for the same plant; the `guard_wr05` finding line for the planted surface; the union case's
three asserted facts as printed; the `(m-channel)` mutation transcript with case `(m)` staying green
beside it; the before and after text of case `(m)`'s narrowed claim; and the list of every existing
case repaired with what changed and why.
</output>

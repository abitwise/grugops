---
phase: 29.1-per-role-model-assignment
round: 4
verified: 2026-08-21T00:45:00Z
status: gaps_found
score: 5/5 roadmap success criteria verified — and gaps_found anyway, on three live gate defects, one of which this round CREATED
behavior_unverified: 0
overrides_applied: 0
head: ad033f0
diff_base: e9907f5
requirement_determinations:
  MODEL-01: "Complete — RE-DERIVED behaviourally, not inherited. All 17 adapters under .claude/agents carry `model: inherit`, and `git diff 6f8411e..HEAD -- .claude/agents/ .claude/skills/` is EMPTY across the whole phase (111 commits). The base commit was asserted to be a real ancestor before the diff was believed."
  MODEL-02: "Complete — RE-DERIVED behaviourally. On a hermetic `git archive HEAD` mirror, `{\"models\":{\"preset\":\"tiered\"}}` re-emitted all 17 adapters: 4 opus (orchestrator, architect-design, security-nfr, compliance-officer), 13 sonnet. The generator emits the RESOLVED value, not a hard-coded one."
  MODEL-03: "Complete — RE-DERIVED behaviourally. An 18th role file planted into agent-factory/roles/ under `preset: tiered` produced a NAMED refusal at TRUE exit 1 (`assigns nothing to 1 of the 18 stem(s) … \"data-steward\"`), with ZERO adapters written — the 17 committed files stayed at `inherit`. Role #18 cannot arrive silently unassigned."
  MODEL-04: "Complete — RE-DERIVED behaviourally. Nine illegal `models` payloads (illegal preset name, illegal alias, full model id, unknown role key, extra key, string, array, null, `roles: []`) each produced TRUE exit 1 with a named `model-tiers:` reason. Fail-closed on every shape."
  MODEL-05: "Complete — RE-DERIVED INDEPENDENTLY rather than inherited from round 3, as instructed. `guard_model_assignment` PASSES on the clean mirror (`17 committed adapter(s) … compared against a resolution recomputed for 17 derived role stem(s)`) and REDS at exit 1 on three planted stray pins (`sonnet`, `haiku`, `claude-opus-4-5` hand-written into grugops-qe-e2e.md). The guard discriminates; the emitted value is compared against a recomputed resolution, not a hand-listed expectation. Nothing in this round's three blockers touches a model value, guard_model_assignment, or the alias vocabulary."
  MODEL-06: "Complete (mechanical) — the kit's single authority at agent-factory/packaging/subagent.frontmatter.md:243-255 states the reach as a property of what grugops emits, carries four dated named vendor sources, and holds 0 occurrences of `agent-factory/config/`. CLAUDE.md:125's `What NOT to Use` row names the mechanism. NEW this round and MEASURED: the cross-document section citation is now PINNED and DISCRIMINATES — renaming `### \\`models\\` sub-fields` reds `every section this authority cites by name exists as a heading in the surface it cites` (38 → 1 failed | 37 passed). Citation fidelity to live vendor pages remains a human item (D5)."
  MODEL-07: "Complete (mechanical) — `git diff 6f8411e..HEAD -- scripts/measure-cost.ts scripts/measure-cost.js` is EMPTY across the whole phase, so the standing `UNKNOWN - verify` is untouched. The totality half stays `verification: backstop`, a human item, with the FAIL-OPEN direction disclosed in docs/audit/28-claim-registry.md row C-28-047."
re_verification:
  previous_status: gaps_found
  previous_score: "5/5 roadmap success criteria verified — and gaps_found anyway, on two live gate bypasses"
  gaps_closed:
    - "Round-3 gap 2 (PRIMARY item — the R2-WR-02 right bound proven by a case that can see it). MEASURED CLOSED AND DISCRIMINATING: deleting the right bound (`return rest`) in the mirror reds `(r-bound-synthetic) the reader stops at the next step, proven on a workflow that HAS one` — 5 failed | 259 passed against a 4 failed | 260 passed baseline (the 4 are git-dependent cases the non-git mirror cannot run, present in BOTH runs). Round 3 measured 263 passed (263) for this same mutation."
    - "Round-3 gap 4 (the packaging authority's pointer lands somewhere that exists). MEASURED CLOSED AND DISCRIMINATING: renaming the cited heading in agent-factory/config/factory.config.md takes scripts/model-dial-consistency.test.ts from 38 passed (38) to 1 failed | 37 passed. Round 3 measured 33 passed (33) for this same rename."
    - "Round-3 gap 5 (`scripts/check-public-docs-vocabulary.ts:32`'s standing claim about another module's SCAN). CLOSED by rewording: the clause is now explicitly scoped to Phase 27's own change ('ADDING THIS GATE IN PHASE 27 LEFT THAT SET UNTOUCHED'), and the superseded wording is DESCRIBED rather than reproduced. WINDOWS.md row 79 status = fixed."
    - "Round-3 gap 3, VALUE AXIS ONLY. The four shapes round 3 reproduced as throws now RETURN refusals against the rebuilt committed scripts/model-tiers.js: circular preset, BigInt preset, circular override value, BigInt override value — all four `{ok:false}` naming `<an object that cannot be serialised>` / `<a bigint that cannot be serialised>`. The SECOND spelling (`String(JSON.stringify(`) is gone from the .ts. The KEY axis regressed — see gaps."
    - "WINDOWS.md rows 78 and 79 are both status = fixed, as plan 21's must_haves required."
  gaps_remaining:
    - "R3-CR-01 (the exemption's unit mismatch) — NOT closed; three independent same-line spellings reproduced, one of them NOT reported by the round-4 review."
    - "R3-CR-02 (a path leaving the configuration directory certified as a reference inside it) — NOT closed at the SUFFIX position."
    - "R3-IN-03 (two readers of one region certified as one class) — NOT closed; the new requirement is structurally incapable of failing for one of its two members."
  regressions:
    - "`resolveModels` THROWS on two input shapes that RETURNED `{ok:true}` at e9907f5. Plan 29.1-21 — whose first must_have truth is verbatim `resolveModels returns a result rather than throwing, on every input including the ones its refusal is about` — introduced them by converting a silent skip into a refusal whose message interpolates the Map KEY raw while bounding only the VALUE. Measured on both builds, side by side."
overrides: []
gaps:
  - truth: "D-08.1's zero-tolerance kit-ref gate governs the shipped configuration reference — the document this phase's single packaging authority now points every reader at for the precedence rule. (carried from round 3; the closure route for SC-4 / MODEL-06 via the user's D6 ruling)"
    status: failed
    reason: "R3-CR-01 and R3-CR-02 are BOTH still live, and I found a THIRD spelling of the same defect that the round-4 review did not report. All three were reproduced on a hermetic `git archive HEAD` mirror against the committed `scripts/check-kit-refs.js`; in every case all three published cardinalities (files 1, lines 2, mentions 3) are exactly as declared and the gate exits 0 while forbidden text ships. The single root cause is a unit mismatch: `exemptMentions += named.length` counts CAPTURES of `CONFIG_NAMED_PATH` (`agent-factory/config/([A-Za-z0-9._-]+)`), while `grepSubstring` greps OCCURRENCES of `CONFIG_REF_NEEDLE` (the bare substring `agent-factory/config/`). Any needle occurrence that yields no capture, or whose capture is a prefix of a longer path, is absorbed unasked into a line that already carries a legitimate mention. The gate's ONLY live defence against a same-line plant is the mention cardinality, and none of the three spellings moves it."
    artifacts:
      - path: "scripts/check-kit-refs.ts"
        issue: "CR-02 REPRODUCED (bare-directory spelling). Appending `The legacy directory agent-factory/config/ is retired.` to already-exempt line 138 of agent-factory/config/factory.config.md: `grep -o 'agent-factory/config/' … | wc -l` = 4, gate prints `3 counted self-reference mention(s) on 2 line(s)` and `exactly 3 mention(s) on 2 line(s) in 1 file(s), as declared`, TRUE EXIT = 0. Mutation confirmed applied (byte delta +55, grep count moved 3 → 4) BEFORE the run was believed."
      - path: "scripts/check-kit-refs.ts"
        issue: "CR-03 REPRODUCED (suffix-traversal spelling). `CONFIG_NAMED_PATH`'s class stops at the first `/`, so the predicate judges the path's FIRST SEGMENT, not the path. Respelling the existing line-138 mention as `agent-factory/config/factory.config.json/../../../../etc/passwd` keeps the occurrence count at 3, keeps all three cardinalities unmoved, and exits 0 — while the shipped document points a reader four levels out of the kit. Mutation confirmed applied (byte delta +23, `grep -c 'etc/passwd'` moved 0 → 1)."
      - path: "scripts/check-kit-refs.ts"
        issue: "NEW — NOT IN THE ROUND-4 REVIEW, and strictly worse than CR-02. A needle occurrence whose first post-prefix byte is OUTSIDE `[A-Za-z0-9._-]` yields NO capture at all, so `named` sees only the line's legitimate mentions, `allResolve` is vacuously true for them, and the whole line is exempted. Appending `Also read agent-factory/config/~/.ssh/id_rsa and agent-factory/config/${HOME}/creds.` to line 138: FIVE true occurrences of the forbidden substring, gate prints `3 counted self-reference mention(s)`, all three cardinalities unmoved, TRUE EXIT = 0. The absorbed text is an actionable instruction naming a private key path and a shell-expanded home directory. Mutation confirmed applied (byte delta +85, occurrence count moved 3 → 5)."
      - path: "scripts/check-kit-refs.test.ts:368-405"
        issue: "The oracle cannot see any of the three. `Assertion 1 RED: a SECOND mention appended to an ALREADY-EXEMPT line` plants `\\`agent-factory/config/factory.config.json\\`` — the one same-line spelling for which the needle unit and the capture unit COINCIDE — and its own premises are asserted in the NEEDLE unit via `mentionsIn(after, CONFIG_REF_NEEDLE)`. The corpus carries only inputs on which the gate's unit and the oracle's unit agree, so the mismatch is invisible to every case in the file. POSITIVE CONTROL RUN: the same sentence on a NEW line DOES red (`exempt MENTIONS: found 4, required exactly 3`, TRUE EXIT = 1), so the gate is not inert — it is open on exactly the same-line axis, on three spellings."
    missing:
      - "Count the PUBLISHED unit from the needle the gate greps for: `exemptMentions += line.split(CONFIG_REF_NEEDLE).length - 1;`, keeping `named` for the membership decision only. This one change reds CR-02 and the new `~`/`${HOME}` spelling, because both raise the needle count without raising the capture count."
      - "Add the missing arm to the predicate: a hit whose needle-occurrence count EXCEEDS its `named.length` names the directory without naming an admissible file and is a STRAY. The 'at least one' floor already makes this argument for a line with zero captures; it is the same argument for a line with fewer captures than mentions."
      - "Capture the WHOLE path a line names rather than its first segment — e.g. `/agent-factory\\/config\\/([^\\s\\`\"'()\\[\\],;]+)/g` — and require the whole captured path to be a member of the derived sibling set. Then `factory.config.json` is a member and `factory.config.json/../../../../etc/passwd` is not, as a consequence of the ONE rule rather than of a new special case."
      - "Add RED cases at the SUFFIX position and at the NO-CAPTURE position beside the four existing basename-position cases. Assert the case premises in the same unit the gate publishes, or the corpus keeps agreeing with itself."
  - truth: "`resolveModels` returns a result rather than throwing — the module header's stated contract, plan 29.1-21's first must_have truth verbatim, and the contract Floor 0 / Floor 0b exist to hold"
    status: failed
    reason: "REGRESSED, not merely unclosed. Plan 21 made `quoteValue` total for the rejected VALUE and deleted the second spelling — that half is real and I confirmed it. It left the Map KEY interpolated raw into BOTH refusal templates, and it converted the previously-silent unknown-stem skip into a refusal, so a key that is not a string now reaches a template literal that cannot render it. Measured side by side against the two committed builds, same script, same inputs. This is upgraded from round 3's `partial` to `failed` because the round that owned the fix made the contract MORE false than it found it."
    artifacts:
      - path: "scripts/model-tiers.js (committed build, HEAD ad033f0)"
        issue: "`resolveModels(stems,{preset:'none',overrides:new Map([[Symbol('x'),'opus']])})` → THREW TypeError: Cannot convert a Symbol value to a string. At e9907f5 the SAME call returned `{\"ok\":true,\"value\":{}}`. `new Map([[{toString(){throw new Error('boom')}},'opus']])` → THREW Error: boom at HEAD; `{\"ok\":true,\"value\":{}}` at e9907f5. Two shapes that returned before this round now crash."
      - path: "scripts/model-tiers.ts:1196-1233"
        issue: "Both refusal templates interpolate `${stem}` raw — `the override for role \"${stem}\" is ${quoteValue(alias)}` and `the override names the role stem \"${stem}\"`. `quoteValue` is routed over the alias and not over the stem, so the module has one total quoting authority and one un-guarded position, in the same sentence."
      - path: "scripts/model-tiers.test.ts:1639-1738"
        issue: "The describe block is titled `the refusal path RETURNS on every input`, and `SHAPE_CORPUS` is driven as `new Map([[stems[0], make()]])` — the KEY is always a real stem on all twelve entries. A totality claim strictly wider than the axis its corpus measures, which is this module's own named defect."
      - path: "scripts/model-tiers.ts:950-953"
        issue: "The `overrides` docstring still reads `The ALREADY-VALIDATED sparse per-role overrides` and says nothing about the new by-name refusal of an unknown stem. The public contract of an exported symbol was changed and its documentation was not."
    missing:
      - "Route the stem through the same authority at both sites: `${quoteValue(stem)}`. `quoteValue` renders a string as `\"stem\"` via JSON.stringify, so every legal key's wording stays byte-identical and only the illegal shapes change from a throw to a description."
      - "Widen SHAPE_CORPUS to the KEY axis — `new Map([[make(), 'opus']])` — so the describe block's title becomes a claim its corpus measures. Rebuild the committed .js."
      - "Update the `overrides` docstring on `ResolveModelsOptions` to state that an entry outside the resolved stem set is refused, and file the D-06 reversal as a numbered decision with a CHANGELOG `Changed` entry (see the WR-03 partial below)."
  - truth: "`(r-class-prefix)` certifies that every derived reader of the ubuntu gate block reads ONE region — the round-3 R3-IN-03 finding's closure"
    status: failed
    reason: "REPRODUCED. Requirement 2 tests `textOf(f).includes(UBUNTU_BLOCK_SLICE_BASE_SOURCE)`, and `UBUNTU_BLOCK_SLICE_BASE_SOURCE = \".slice(at + UBUNTU_BLOCK_STEP_NAME.length)\"` is DECLARED as a `const` at scripts/check-foundation-guards.test.ts:10936 in that same file — so the constant that STATES the requirement is the text the requirement searches for. The predicate cannot fail for this member no matter what its reader computes. Requirement 1 is satisfied for BOTH members by their own `const STEP_MARKER = \"\\n      - name: \";` declarations (:10912 and skill-twins-freshness.test.ts:286), for the same reason."
    artifacts:
      - path: "scripts/check-foundation-guards.test.ts:11002"
        issue: "I applied the EXACT divergence R3-IN-03 named — `ci.slice(at + UBUNTU_BLOCK_STEP_NAME.length)` → `ci.slice(at)` — and ran the file: 4 failed | 260 passed (264), BYTE-FOR-BYTE the same failure set as the unmutated baseline (the 4 are git-dependent cases the non-git mirror cannot run). No case in the file sees it. HARNESS PREMISE ASSERTED SEPARATELY: the mutation moved bytes (delta -32, line 11002 re-read as `const rest = ci.slice(at);`), the file still held exactly 1 occurrence of the canonical literal (the const declaration), and the two slice bases were independently measured to produce genuinely different regions on the committed workflow — 25639 vs 25681 bytes, the 42-byte difference being the step name itself. The mutation is semantically real; the class check is blind to it."
      - path: "scripts/check-foundation-guards.test.ts:11078-11145"
        issue: "`(r-bound-synthetic)`, the case WINDOWS.md row 80 nominates as the bound for this residual, cannot catch it either: its negative control compares a bounded read against `withAppended.slice(withAppended.indexOf(NAME) + NAME.length)` — an inline slice of its own, not `ubuntuBlock()`'s base — and its effect assertion is `.not.toContain(APPENDED_STEP_NAME)`, which a base-shifted read still satisfies. My measurement is the direct falsification of row 80's clause `Bounded for today's two members by their own synthetic-input bound proofs`."
      - path: ".planning/WINDOWS.md row 80"
        issue: "The disclosed residual is framed as being about a HYPOTHETICAL future member ('a member spelling both … while computing something else'). For the two members that exist TODAY, requirement 1 is unconditionally satisfied and requirement 2 is unconditionally satisfied for one of them. The disclosure understates the hole by exactly the amount that matters, and its mitigating clause is measurably false."
    missing:
      - "Strip `const <NAME> = …` declaration lines of the two constants before the `includes` test, so the constant that states the requirement can no longer answer it. This is the cheap fix and it reds the mutation above."
      - "Better: compare BEHAVIOUR, not text — have each member export its `ubuntuBlockCommands` from a shared `*.testkit.ts` consumed only by tests, and assert `a.join(\"\\n\") === b.join(\"\\n\")`. That is one authority over 'does this class read one region', which is what the case claims to be."
      - "Amend WINDOWS.md row 80 to state the measured fact — that neither requirement can fail for check-foundation-guards.test.ts today — rather than the weaker hypothetical."
  - truth: "Assertion 3's derived legal set and its actual set are built from one path-spelling authority"
    status: partial
    reason: "CORROBORATED BY READING, NOT EXECUTED ON WINDOWS — stated as such. `ghLegal` takes `PACKAGING_TEMPLATE` as a raw forward-slash literal (scripts/check-kit-refs.ts:602, `ghLegal.add(PACKAGING_TEMPLATE)`), while `ghActual` comes from `grepFilesWithMatch(SCAN, …)` → `walk()` → `join()`. `node -e \"require('path').win32.join('agent-factory/packaging','subagent.frontmatter.md')\"` yields `agent-factory\\packaging\\subagent.frontmatter.md`, so on Windows the two sets disagree for that file and Assertion 3 fails RED on a correct tree, printing 'carries the resolver slot but never names $GRUGOPS_HOME (its self-heal is gone — this adapter cannot find the kit)' about a file that is fine. The comment at :592 asserts the opposite reasoning ('join() … keeps these byte-identical to the paths walk() produces on Windows as well as Unix'); the reasoning is right and its application is incomplete. I could not execute on Windows, so this is a reasoned finding plus a corroborating measurement, not a reproduction. It is NOT in .planning/WINDOWS.md — I grepped: only rows 73, 77 and 79 mention check-kit-refs, and none covers this."
    artifacts:
      - path: "scripts/check-kit-refs.ts:137, 602"
        issue: "A path that enters a set compared against `walk()` output as a raw `/` literal."
    missing:
      - "`if (existsSync(abs(PACKAGING_TEMPLATE))) ghLegal.add(join(PACKAGING_TEMPLATE));`, and normalise the direct-file SCAN/GH_SCAN entries the same way in `walk()` (`acc.push(join(rel))`), so every path in every set this gate compares comes from one spelling authority."
      - "Add a `path.win32`-based unit case, or log the row in .planning/WINDOWS.md."
  - truth: "No record in this tree makes a claim wider than its mechanism, and a ratified decision is not reversed in a comment"
    status: partial
    reason: "THREE instances, all verified by reading the live tree. (1) WR-03: the skip→refusal change reverses D-06's 'the question is this key a real role belongs to readModelsConfig' and is recorded ONLY as a source comment plus WINDOWS.md row 86 — `git diff 6f8411e..HEAD -- CHANGELOG.md` is EMPTY across the WHOLE phase, and the `overrides` docstring still says ALREADY-VALIDATED. In a repository whose value proposition is an auditable decision trail, a ratified decision reversed in a comment is invisible to every consumer of that trail. (2) WR-02: scripts/model-tiers.test.ts:1657-1673 argues the corpus is measured 'against a denominator that is not this file's to choose', then hand-types `TYPEOF_RESULTS` three lines later; deleting one SHAPE_CORPUS entry and its matching `typeof` string in one edit leaves the case green, and `SHAPE_CORPUS.length > TYPEOF_RESULTS.length` survives at 11 > 7. (3) WR-04: the `ONE spelling in this module` scan at :1782-1806 reads scripts/model-tiers.ts and NOT the committed scripts/model-tiers.js that hosts actually execute; build parity is enforced separately so it is not exploitable today, but the case's claim names the module that ships and it does not read it. None of the three falsifies a success criterion; all three are the wider-than-mechanism class this phase exists to close."
    artifacts:
      - path: "scripts/model-tiers.ts:950-953, 1176-1196"
        issue: "A public export's documented contract changed; its docstring and the project CHANGELOG did not."
      - path: "scripts/model-tiers.test.ts:1657-1700"
        issue: "A totality argument resting on an external denominator, with the denominator hand-typed in the same file three lines below the argument."
      - path: "scripts/model-tiers.test.ts:1782-1806"
        issue: "A one-authority scan whose subject is the artifact hosts run, over a file that is not it; the comment strip `!/^\\s*[/*]/` drops whole-line comments only."
    missing:
      - "File the D-06 reversal as a numbered decision superseding the clause it contradicts, add the CHANGELOG `Changed` entry, and correct the `overrides` docstring."
      - "Either drop the 'not this file's to choose' claim, or make the denominator observable: `expect(TYPEOF_RESULTS.length).toBe(8)` beside a comment citing the ECMAScript table."
      - "Run the `JSON.stringify(` scan over BOTH model-tiers.ts and model-tiers.js, and state the comment-strip's limits in the assertion message."
deferred: []
behavior_unverified_items: []
coincidental_reliance_items:
  - truth: "`(r-class-prefix)` requirement 1 — every derived member carries the right-bound marker in its canonical source spelling"
    reason: fixture-only
    harden: "The predicate's own subject supplies the text it searches for: each member's `const STEP_MARKER = \"\\n      - name: \";` declaration satisfies `textOf(f).includes(boundMarkerSource)` regardless of whether that member's reader uses the bound. Strip declaration lines before the includes test, or compare computed blocks. (Recorded advisory-only; the requirement-2 half of the same case is a BLOCKER above, measured.)"
human_verification:
  - test: "MODEL-06 / D5 (carried UNCHANGED from rounds 1, 2 and 3): read the four vendor citations at agent-factory/packaging/subagent.frontmatter.md:251-255 (Codex CLI, Gemini CLI, OpenCode, GitHub Copilot CLI, all retrieved 2026-08-20) against their live vendor pages, and confirm the authority sentence at :249, the amended MODEL-06 text and the amended ROADMAP SC-4 genuinely agree in substance."
    expected: "Each citation's quoted claim (a per-agent `model` field in that CLI's own agent-definition format) matches what the vendor page currently states; the three prose surfaces assert nothing beyond (a) grugops emits per-agent adapters for Claude Code alone and (b) every claim about the other four carries a named source."
    why_human: "Citation fidelity to external vendor documentation is not a predicate this verifier's tooling can decide. I confirmed the citations are present, dated, specific, internally consistent, and that the document holds 0 occurrences of `agent-factory/config/` — not that the vendor pages still say what is quoted. This is what this repository's 'never fake a citation' rule exists to route to a human."
  - test: "MODEL-07/boundary and MODEL-07/precision (carried unchanged from rounds 1-3; plan 29.1-06 and plan 29.1-21 `verification: backstop`) — confirm no phase-touched prose asserts a cost, spend or usage-limit saving, and that no numeric cost figure, percentage, multiplier or price ships."
    expected: "Zero cost or limit-savings claims. Mechanical half CONFIRMED this round: `git diff 6f8411e..HEAD -- scripts/measure-cost.ts scripts/measure-cost.js` is EMPTY across the whole phase, so the standing `UNKNOWN - verify` is untouched."
    why_human: "D-59/D-16: an open-set totality is not a decidable predicate here, and docs/audit/28-claim-registry.md row C-28-047 discloses the direction as FAIL-OPEN — there is no cost literal group in check-banned-claims, so a cost sentence would pass every gate in this repository green. This is the phase's own declared human-adjudication item."
  - test: "Rule on the D-06 reversal's record (round-4 WR-03). WINDOWS.md row 86 discloses that plan 29.1-21 reversed a ratified decision as a contract change rather than a bug fix, and the reversal is otherwise recorded only as a source comment: CHANGELOG.md is byte-unchanged across the entire phase and the `overrides` docstring still reads ALREADY-VALIDATED."
    expected: "A ruling on whether a numbered decision record plus a CHANGELOG `Changed` entry are required before this phase closes, or whether WINDOWS.md row 86 is a sufficient record for a pre-1.0 internal contract."
    why_human: "Whether a disclosure ledger substitutes for a decision record in a repository whose stated value proposition is an auditable decision trail is a governance judgement, not a predicate. The new refusal branch is also unreachable from production (both call sites hand `readModelsConfig` and `resolveModels` the same stems), so nothing measurable turns on it today."
---

# Phase 29.1: Per-Role Model Assignment — Verification Report (Round 4)

**Phase Goal:** A project can spend its model budget where judgment actually lives — a stronger model for orchestration, architecture and security review, a cheaper one for implementation, QE and UAT — set once on the config dial and emitted into every generated adapter, with zero-config behaviour byte-identical to today's.

**Verified:** 2026-08-21T00:45:00Z
**HEAD:** `ad033f0` · **Round-4 diff base:** `e9907f5` · **Phase base:** `6f8411e` (asserted an ancestor of HEAD; 111 commits)
**Status:** `gaps_found`
**Re-verification:** Yes — round 4, after gap-closure plans 29.1-18 … 29.1-21.

---

## Method, and what I refused to take on faith

Every verdict below rests on a command I ran. Nothing in the four SUMMARYs and nothing in the
round-4 review was accepted as evidence; the review's four criticals were re-derived from scratch,
and one of them turned out to understate its own finding.

Three constraints were held throughout:

- **`npm test` was never run.** The full non-e2e suite was run ONCE at HEAD:
  `npx vitest run --exclude '**/scripts/e2e/**'` → **55 files, 2383 passed, 2 skipped**, exit 0.
  Everything else was a single named file or a single named case.
- **No source file was modified in the working tree.** All mutations were applied to a hermetic
  `git archive HEAD` mirror in the scratchpad. `git diff --exit-code -- scripts/ agent-factory/
  .claude/ CLAUDE.md .github/ install/ .planning/WINDOWS.md` is clean; the only dirty paths in the
  repo are `.planning/config.json` and `human-notes.txt`, both already dirty at session start.
- **Every harness premise was asserted before its run was believed.** This phase has produced a
  false verification result in six instances across four rounds, always by the same route: a
  mutation that silently failed to apply produces a green run indistinguishable from a passing
  assertion. So each mutation script exits non-zero if the replacement is a no-op, prints its byte
  delta, and is followed by an independent re-measurement of the property it was supposed to move
  (a `grep -o` count, a re-read of the mutated line, or a direct computation of the two regions).
  The CR-04 reproduction additionally measures that the two slice bases produce genuinely different
  strings — 25639 vs 25681 bytes on the committed workflow, the 42-byte difference being the step
  name itself — so the green run cannot be attributed to a semantically inert edit.

The mirror is not a git repository, so four git-dependent cases in
`scripts/check-foundation-guards.test.ts` cannot run there. That baseline was measured FIRST
(4 failed | 260 passed) and every mirror run below is compared against it, never against 264.

---

## Goal Achievement

### Roadmap Success Criteria

| # | Success criterion | Status | Evidence (measured this round) |
|---|---|---|---|
| SC-1 | Zero-config: all 17 adapters byte-identical to today's, `model: inherit` | ✓ VERIFIED | `grep -rn '^model:' .claude/agents/` → 17 files, all ` inherit`. `git diff 6f8411e..HEAD -- .claude/agents/ .claude/skills/` → EMPTY across 111 commits. Base commit asserted a real ancestor (`git merge-base --is-ancestor`) before the empty diff was believed. |
| SC-2 | One opt-in key switches to the cost preset; role→tier DERIVED with an asserted count | ✓ VERIFIED | Mirror: `models:{preset:"tiered"}` → `resolved model assignment: {"roles":17,"overrides":0,"aliases":["opus","sonnet"]}`; 4 opus (orchestrator, architect-design, security-nfr, compliance-officer), 13 sonnet. Role #18 planted → NAMED refusal, TRUE exit 1, ZERO adapters written, committed 17 untouched at `inherit`. |
| SC-3 | Only `inherit`/`opus`/`sonnet`/`haiku` legal, else FAIL-CLOSED; emitted value asserted equal to the resolved config | ✓ VERIFIED | Nine illegal `models` payloads → TRUE exit 1 each, every one a named `model-tiers:` reason. `guard_model_assignment` PASSES clean and REDS at exit 1 on three planted stray pins (`sonnet`, `haiku`, `claude-opus-4-5`). |
| SC-4 | Claude-Code-only reach stated in the kit and in `CLAUDE.md`'s "What NOT to Use"; single authority; every clause about the other four carries a named vendor source | ✓ VERIFIED (mechanical) | `subagent.frontmatter.md:243-255` — the authority sentence states the reach as a property of what grugops emits; four dated named vendor sources; `grep -c 'agent-factory/config/'` = 0. `CLAUDE.md:125` names the `models` dial as the documented reason. NEW: the cross-document section citation is PINNED and DISCRIMINATES (rename → 1 failed \| 37 passed). Citation fidelity is a human item. |
| SC-5 | Every cost or limit-savings claim MEASURED or `UNKNOWN - verify` | ✓ VERIFIED (mechanical) | `git diff 6f8411e..HEAD -- scripts/measure-cost.ts scripts/measure-cost.js` → EMPTY across the whole phase. Totality half remains `verification: backstop`, a human item, FAIL-OPEN direction disclosed in registry row C-28-047. |

**Score: 5/5 — and `gaps_found` anyway.**

I considered and rejected the wider reading that would fail SC-4. The D-08.1 kit-ref gate is not
part of SC-4's text; it is a twenty-two-phase-old repo-wide hygiene invariant that plan 29.1-17
pulled into this phase's blast radius by adding `agent-factory/config` to `SCAN`. Its three live
bypasses do not falsify any success criterion. They are blockers regardless: the gate now publishes
a green verdict that misdescribes its own run, on the very document this phase's single packaging
authority points every reader at.

---

## What round 4 actually closed

Three of round 3's five gap items are genuinely, measurably closed, and each of the three
discriminates — I planted the reddening mutation and watched it red.

| Round-3 item | Round-3 measurement | Round-4 measurement | Verdict |
|---|---|---|---|
| Gap 2 primary — the right bound proven by a case that can see it | delete right bound → **263 passed (263)** | delete right bound → **5 failed \| 259 passed**, `(r-bound-synthetic)` named | ✓ CLOSED, discriminating |
| Gap 4 — the packaging authority's section pointer lands somewhere that exists | rename cited heading → **33 passed (33)** | rename cited heading → **1 failed \| 37 passed**, the citation case named | ✓ CLOSED, discriminating |
| Gap 5 — `check-public-docs-vocabulary.ts:32`'s standing claim | clause read as present-tense and false | clause scoped to Phase 27's own change; superseded wording described, not reproduced; WINDOWS row 79 `fixed` | ✓ CLOSED |
| Gap 3 — `resolveModels` returns rather than throws (**VALUE axis**) | 4 shapes THREW | 4 shapes RETURN named refusals; the second `String(JSON.stringify(` spelling is gone | ✓ closed on this axis |
| WINDOWS rows 78, 79 | open | both `fixed` | ✓ CLOSED |

That is real work and it should be said plainly. It is also the whole of the good news.

---

## Blockers — three, each reproduced

### BLOCKER 1 — the D-08.1 exemption is open on the SAME-LINE axis, on three spellings

Neither R3-CR-01 nor R3-CR-02 is closed, and there is a third spelling neither round reported.

| Spelling | Planted on already-exempt line 138 | True occurrences | Gate publishes | Exit |
|---|---|---|---|---|
| CR-02 — bare directory | `The legacy directory agent-factory/config/ is retired.` | **4** | `3 mention(s) on 2 line(s)` | **0** |
| CR-03 — suffix traversal | `agent-factory/config/factory.config.json/../../../../etc/passwd` | 3 | `3 mention(s) on 2 line(s)` | **0** |
| **NEW** — no-capture prefix | `agent-factory/config/~/.ssh/id_rsa` and `agent-factory/config/${HOME}/creds` | **5** | `3 mention(s) on 2 line(s)` | **0** |

The third row is mine and is strictly worse than CR-02: a needle occurrence whose first post-prefix
byte falls outside `[A-Za-z0-9._-]` yields **no capture at all**, so the accumulator never sees it,
the membership predicate is never asked about it, and the absorbed text is an actionable
instruction naming a private-key path and a shell-expanded home directory. Five occurrences of the
substring D-08.1 exists to forbid ship at exit 0 under a verdict that says three.

One root cause explains all three: `exemptMentions += named.length` counts **captures** of
`CONFIG_NAMED_PATH`; `grepSubstring` greps **occurrences** of `CONFIG_REF_NEEDLE`. Two units, one
published number. The gate's declared number still does not describe the tree it judges — which is
verbatim what round 3 filed R3-CR-01 about.

**Positive control** (so the gate is not written off as inert): the same sentence on a *new* line
does red — `exempt MENTIONS: found 4, required exactly 3`, TRUE exit 1. The mention cardinality is
the gate's only live defence, and none of the three spellings moves it.

**Why the oracle is green over this.** `check-kit-refs.test.ts:368` plants
`` `agent-factory/config/factory.config.json` `` — the one same-line spelling where the needle unit
and the capture unit coincide — and asserts its premises with `mentionsIn(after,
CONFIG_REF_NEEDLE)`, the *needle* unit. The corpus contains only inputs on which the gate's unit and
the oracle's unit agree.

### BLOCKER 2 — `resolveModels` throws on a non-string Map key, and this round created two of the three

Plan 29.1-21's first must_have truth is, verbatim: *"`resolveModels` returns a result rather than
throwing, on every input including the ones its refusal is about."* Measured against the two
committed builds, same script, same inputs:

| Input | `e9907f5` | HEAD `ad033f0` |
|---|---|---|
| `overrides: Map([[Symbol('x'), 'opus']])` | `{"ok":true,"value":{}}` | **THREW** `TypeError: Cannot convert a Symbol value to a string` |
| `overrides: Map([[{toString(){throw}}, 'opus']])` | `{"ok":true,"value":{}}` | **THREW** `Error: boom` |
| `overrides: Map([[Symbol('y'), 'nope']])` | THREW | THREW |
| `overrides: Map([['orchestrator', 'opus']])` (control) | `{"ok":true}` | `{"ok":true}` |

`quoteValue` was made total for the **value** and the key was left interpolated raw into both
refusal templates; converting the silent skip into a refusal then routed previously-unreachable
keys into those templates. The round that owned "make the refusal path total" made the contract
more false than it found it, on the axis it did not look at.

Round 3 recorded this truth as `partial`. I am upgrading it to `failed`, because a regression
introduced by the fixing round is a different fact from a residual the fixing round did not reach.

### BLOCKER 3 — `(r-class-prefix)`'s slice-base requirement cannot fail for one of its two members

I applied the exact divergence R3-IN-03 named:

```diff
-    const rest = ci.slice(at + UBUNTU_BLOCK_STEP_NAME.length);
+    const rest = ci.slice(at);
```

`npx vitest run scripts/check-foundation-guards.test.ts` → **4 failed | 260 passed (264)** — the
identical failure set as the unmutated mirror baseline. Nothing sees it.

The reason is structural, not incidental: requirement 2 searches each member's text for
`".slice(at + UBUNTU_BLOCK_STEP_NAME.length)"`, and that exact string is **declared as a `const` at
line 10936 of the very file being tested**. The constant that states the requirement is the text
the requirement searches for. Requirement 1 is in the same position for *both* members, satisfied by
their own `const STEP_MARKER` declarations. The file's own commentary makes precisely this argument
two hundred lines earlier — *"the authority does not satisfy this scan itself, because it assigns
the render to a local rather than composing the two calls"* — and then does not apply it here.

**Harness premise, asserted:** the mutation moved bytes (delta −32), line 11002 re-read as
`const rest = ci.slice(at);`, exactly 1 occurrence of the canonical literal survived (the const),
and the two bases were independently measured to produce different regions on the committed
workflow — 25639 vs 25681 bytes, the extra 42 being `Freshness gates + repo gates (ubuntu only)`
itself. The edit is semantically real.

**And WINDOWS.md row 80 is measurably wrong.** It discloses the residual as hypothetical and adds
*"Bounded for today's two members by their own synthetic-input bound proofs, `(r-bound-synthetic)`
and Case 8b."* It is not bounded. `(r-bound-synthetic)`'s negative control builds its own inline
slice rather than calling `ubuntuBlock()`, and its effect assertion
(`.not.toContain(APPENDED_STEP_NAME)`) is still satisfied by a base-shifted read. An honest
disclosure that names the wrong mitigation is how a hole survives being written down.

---

## Where the round-4 review was right, and where I go further

| Review finding | My independent result |
|---|---|
| CR-01 (symbol key throws; regression) | **CONFIRMED, both halves.** Reproduced at HEAD and at `e9907f5` side by side. |
| CR-02 (unit mismatch survives) | **CONFIRMED**, and **UNDERSTATED** — I found a third spelling (`~`/`${HOME}`) that raises the true count to 5 and absorbs an instruction pointing at a private-key path. |
| CR-03 (suffix traversal survives) | **CONFIRMED**, byte-for-byte. |
| CR-04 (`(r-class-prefix)` cannot fail) | **CONFIRMED**, and **UNDERSTATED** — the review notes the disclosure "understates the hole"; I measured that the disclosure's *named mitigation* is itself false. |
| WR-01 (Windows path spelling) | **Corroborated by reading + a `path.win32.join` measurement; NOT executed on Windows.** Confirmed absent from `.planning/WINDOWS.md` (only rows 73/77/79 mention this gate). |
| WR-02, WR-03, WR-04 | **CONFIRMED by reading the live tree.** `CHANGELOG.md` is byte-unchanged across the entire phase; the `overrides` docstring still says `ALREADY-VALIDATED`. |

I found no review finding that is wrong.

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `scripts/model-tiers.ts` / `.js` | Total refusal path, one quoting authority | ⚠️ PARTIAL | Value axis total and single-authority (verified). Key axis throws (BLOCKER 2). |
| `scripts/model-tiers.test.ts` | Corpus proving totality | ⚠️ HOLLOW | Twelve-entry corpus varies the VALUE only; the describe block claims "every input". |
| `scripts/check-kit-refs.ts` / `.js` | Mention-unit exemption, canonical-form membership | ✗ BYPASSED | Three same-line spellings (BLOCKER 1). |
| `scripts/check-kit-refs.test.ts` | Cases that discriminate | ⚠️ PARTIAL | 4 basename-position RED cases + new-line RED case all real; no suffix-position, no no-capture-position case. |
| `scripts/check-foundation-guards.test.ts` | `(r-bound-synthetic)` + `(r-class-prefix)` | ⚠️ SPLIT | `(r-bound-synthetic)` VERIFIED discriminating; `(r-class-prefix)` requirement 2 cannot fail (BLOCKER 3). |
| `scripts/skill-twins-freshness.test.ts` | Reconciled slice base | ✓ VERIFIED | `:302` reads `ci.slice(at + UBUNTU_BLOCK_STEP_NAME.length)` — the two readers now agree on the tree. |
| `scripts/model-dial-consistency.test.ts` | Pinned cross-document citation | ✓ VERIFIED | Discriminating (rename → red). 38 passed at HEAD. |
| `scripts/check-public-docs-vocabulary.ts` / `.js` | Scoped claim | ✓ VERIFIED | Clause scoped to Phase 27's own change. |
| `.planning/WINDOWS.md` | Rows 78-88 | ⚠️ PARTIAL | 78/79 `fixed` as required; row 80's mitigating clause is measurably false; WR-01 has no row. |
| `.claude/agents/*.md` (17) | Byte-identical, `model: inherit` | ✓ VERIFIED | Empty diff across the whole phase. |
| `scripts/measure-cost.ts` / `.js` | Byte-unchanged | ✓ VERIFIED | Empty diff across the whole phase. |

## Key Link Verification

| From | To | Via | Status | Detail |
|---|---|---|---|---|
| `factory.config.json` `models` block | 17 emitted adapters | `readModelsConfig` → `resolveModels` → generator | ✓ WIRED | `tiered` → 4 opus / 13 sonnet, measured end to end. |
| `kit-model.listRoles()` | tier assignment | derived stems + asserted coverage | ✓ WIRED | 18th role → named refusal, exit 1, no write. |
| resolved config | `guard_model_assignment` | recomputed resolution vs committed adapters | ✓ WIRED | Reds on 3 planted pins. |
| `subagent.frontmatter.md` citation | `factory.config.md` heading | `citableForm()` membership | ✓ WIRED | Reds on rename. |
| `CONFIG_REF_NEEDLE` (grepped) | `CONFIG_SELF_REF_MENTIONS` (published) | `CONFIG_NAMED_PATH` captures | ✗ NOT WIRED | Two units; the published number is not the counted one. |
| `quoteValue` | both refusal templates | `${stem}` / `${quoteValue(alias)}` | ⚠️ PARTIAL | Alias routed; stem raw. |
| `(r-class-prefix)` | the members' READ REGION | `textOf(f).includes(...)` | ✗ NOT WIRED | Compares text a member's own const declaration supplies. |

## Behavioural Spot-Checks

| Behaviour | Command | Result | Status |
|---|---|---|---|
| Full non-e2e suite at HEAD | `npx vitest run --exclude '**/scripts/e2e/**'` | 55 files, 2383 passed, 2 skipped | ✓ PASS |
| Kit-ref gate, clean mirror | `node scripts/check-kit-refs.js` | ALL CHECKS PASSED, exit 0 | ✓ PASS |
| Foundation guards, clean mirror | `node scripts/check-foundation-guards.js` | exit 0; `guard_model_assignment` PASS, 17 adapters | ✓ PASS |
| Zero-config generation | `node scripts/generate-role-adapters.js` | 17 adapters, `preset: none`, `aliases:["inherit"]` | ✓ PASS |
| Cost preset | same, with `models.preset=tiered` | `aliases:["opus","sonnet"]`, 4/13 split | ✓ PASS |
| Fail-closed, 9 illegal shapes | generator per payload | exit 1 × 9, each named | ✓ PASS |
| Role #18 | plant `agent-factory/roles/data-steward.md` | exit 1, named, 0 adapters written | ✓ PASS |
| Stray model pin × 3 | edit `grugops-qe-e2e.md` | exit 1 × 3 | ✓ PASS |
| CR-01 regression | `resolveModels` symbol key, both builds | `{ok:true}` → THREW | ✗ FAIL |
| CR-02 / CR-03 / NEW | 3 same-line plants | exit 0 × 3 | ✗ FAIL |
| CR-04 | slice-base revert | 4 failed \| 260 passed = baseline | ✗ FAIL |
| Right-bound deletion (control) | `return rest` | 5 failed \| 259 passed | ✓ PASS (discriminates) |
| Heading rename (control) | rename cited `###` | 1 failed \| 37 passed | ✓ PASS (discriminates) |

## Probe Execution

`find scripts -path '*/tests/probe-*.sh'` → no matches. This repository's probes are the gate
executables and the vitest suite, both run above. **Step 7c: no conventional probe scripts exist.**

## Requirements Coverage

| Requirement | Source plans | Status | Evidence |
|---|---|---|---|
| MODEL-01 | 19 | ✓ SATISFIED | 17 × `inherit`; empty phase-wide adapter diff. |
| MODEL-02 | 20 | ✓ SATISFIED | Resolved value emitted; packaging authority agrees. |
| MODEL-03 | 19 | ✓ SATISFIED | Role #18 → named refusal, exit 1. |
| MODEL-04 | 21 | ✓ SATISFIED | 9 illegal shapes refused by name. |
| MODEL-05 | 18, 19, 21 | ✓ SATISFIED | Re-derived independently: guard reds on 3 planted pins. Plan 18's MODEL-05-tagged *deliverable* (the D-08.1 exemption) is bypassed, but that gate governs kit-ref hygiene, not model assignment — MODEL-05's own text is fully met. |
| MODEL-06 | 18, 20 | ✓ SATISFIED (mechanical) | Authority sentence + 4 named vendor sources + `CLAUDE.md:125`; citation now pinned and discriminating. Citation fidelity → human. |
| MODEL-07 | 21 | ✓ SATISFIED (mechanical) | `measure-cost.*` byte-unchanged phase-wide. Totality → human (backstop). |

No orphaned requirements: `.planning/REQUIREMENTS.md` maps exactly MODEL-01..07 to Phase 29.1, and
all seven appear in the plan frontmatter for this round (18: 05,06 · 19: 01,03,05 · 20: 02,06 ·
21: 04,05,07).

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `scripts/check-kit-refs.ts` | 185-216, 351-366 | Published number in a unit the code does not count | 🛑 Blocker | Three same-line bypasses. |
| `scripts/check-kit-refs.ts` | 216 | Extraction judges the path's first segment, not the path | 🛑 Blocker | Traversal certified as a self-reference. |
| `scripts/model-tiers.ts` | 1196-1233 | One guarded position and one raw position in the same sentence | 🛑 Blocker | Refusal path throws; two shapes regressed. |
| `scripts/check-foundation-guards.test.ts` | 10936, 11225-11290 | Predicate satisfied by its own subject's const declaration | 🛑 Blocker | R3-IN-03 uncaught. |
| `scripts/check-kit-refs.ts` | 137, 602 | Two path spellings entering one compared set | ⚠️ Warning | Guaranteed false RED on Windows; undisclosed. |
| `scripts/model-tiers.test.ts` | 1657-1700 | "External denominator" hand-typed three lines below the claim | ⚠️ Warning | Set-literal drift in a totality case. |
| `scripts/model-tiers.test.ts` | 1782-1806 | One-authority scan reads the `.ts`, not the shipped `.js` | ⚠️ Warning | Claim names the module it does not read. |
| `scripts/model-tiers.ts` | 950-953, 1176-1196 | Ratified decision reversed in a comment; docstring stale | ⚠️ Warning | Auditable trail gap; no CHANGELOG entry. |
| `scripts/model-tiers.ts` | 1080-1088 | `describeShape(null)` → "an object rather than a Map" | ℹ️ Info | Worst rendering of the case the floor exists for. |
| `scripts/check-kit-refs.ts` | 259, 342 | `join(dir)+sep` computed twice | ℹ️ Info | Second spelling to drift from. |
| `scripts/check-kit-refs.ts` | 232-244 | `walk()` follows symlinks, no cycle guard | ℹ️ Info | Symlink can join the sibling set. |
| `scripts/model-dial-consistency.test.ts` | 490-513 | `sectionCitationsIn` throws over the whole document | ℹ️ Info | Unbounded scope on a right default. |

**Debt-marker gate:** `grep -n -E 'TBD|FIXME|XXX'` over every file this round touched → **0
unreferenced markers**. Clean.

---

## Gaps Summary

Round 4 closed three of round 3's five items and did so properly — each closure discriminates under
a mutation I planted and watched red. That is the first round of this phase in which a closure was
provably not a restatement.

It nonetheless returns `gaps_found`, on three blockers.

The two carried blockers share one shape, and it is the shape the round-4 review named: **each fix
asked whether its new predicate was right, and none asked what the predicate's INPUT is assembled
from.** `quoteValue` bounded the value axis, not the key axis. Sibling-set membership bounded the
basename position, not the bytes after it, and not the positions where no capture occurs at all.
The canonical-spelling class check bounded what a file *contains*, not what its reader *computes* —
and the file that contains the requirement is the file the requirement interrogates.

The third blocker is worse than a residual: it is a **regression the fixing round introduced**. Two
inputs that returned `{ok:true}` at `e9907f5` now crash at HEAD, under a plan whose first must_have
truth is that the refusal path returns on every input. The corpus written to prove that totality
varies only the axis that was already safe.

Two things this round should be credited with, and one it should be charged with. Credited: the
synthetic-input discrimination controls are a genuine methodological advance — `(r-bound-synthetic)`
and the citation pin both red on the exact mutation their predecessors slept through, and they will
keep doing so on every future run rather than only in a session where somebody thinks to try.
Charged: `WINDOWS.md` row 80 discloses the `(r-class-prefix)` residual and then names a mitigation
that does not exist. An honest ledger that carries a false mitigating clause is more dangerous than
no ledger row, because the next reader stops looking.

**Do not proceed to Phase 29.2 on this state.** The phase's five success criteria are all
independently verified and the shipping mechanism works; what is not safe is the set of gates
standing over it, two of which now certify things that are not true.

---

_Verified: 2026-08-21T00:45:00Z_
_Verifier: Claude (gsd-verifier), adversarial stance, round 4_
_All mutations applied to a hermetic `git archive HEAD` mirror; working tree source unmodified and confirmed with `git diff --exit-code`._

---
phase: 29.1-per-role-model-assignment
plan: 10
subsystem: documentation
tags: [honesty-floor, citations, unknown-verify, oracle-anchoring, derived-set, gap-closure]

requires:
  - phase: 29.1-per-role-model-assignment
    plan: 05
    provides: "the one-authority shape — the scope sentence in `subagent.frontmatter.md`, the pointer in `factory.config.md`, and `model-dial-consistency.test.ts` holding both directions. This plan changes what the authority SAYS, not the shape it says it in"
  - phase: 29.1-per-role-model-assignment
    plan: 09
    provides: "the tree this plan edits; no functional overlap"
provides:
  - "an authority sentence whose every clause is either a cited vendor fact or an in-repo fact provable by `grep` — the dial's reach stated as a property of what grugops emits"
  - "four retrieval-dated primary vendor citations, one per non-Claude host CLI, shipped beside the sentence"
  - "residuals R1, R2 and R3 as explicit `UNKNOWN - verify` items with their directions stated"
  - "`UNKNOWN_VERIFY_MARKER_COUNT` — the authority's marker count pinned as a number with a promote trigger"
  - "`namedOtherHostClis()` — the citation block's required coverage DERIVED from the sentence's own enumeration, so the two cannot drift"
  - "an assumption-A1 case anchored on its own block instead of on `indexOf` over the first marker in the file"
  - "MODEL-06 and ROADMAP SC-4 restated so the criterion is satisfiable by a true sentence for the first time, each with a dated amendment note"
affects: [29.1-11, 29.2-install-delivery, phase-29.1-verification]

actuals:
  tokens: 4600
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "A true conclusion resting on a false premise is a false sentence. Fix the premise; do not keep the sentence because its conclusion survived"
    - "DERIVE the coverage obligation from the claim itself. The set of CLIs the citation block must cover is parsed out of the scope sentence's own enumerating clause, so naming a fifth CLI without citing it goes red — a hand-listed set of four would not have"
    - "An anchor that means 'the first of a kind' stops describing its subject the moment a second one lands. Give a case an anchor its own block owns, and pin the kind's COUNT so a new one moves a number rather than a measurement"
    - "Bound a section read on the RIGHT before it matters. A section reader that searches to EOF adopts every later block appended after it"
    - "Describe a superseded claim, do not reproduce it. Annotate-never-rewrite is satisfied by a dated note that says what the old wording asserted and points at where it survives verbatim; reproducing the literal re-plants the claim on a live surface"

key-files:
  created:
    - .planning/phases/29.1-per-role-model-assignment/29.1-10-SUMMARY.md
  modified:
    - agent-factory/packaging/subagent.frontmatter.md
    - scripts/model-dial-consistency.test.ts
    - agent-factory/config/factory.config.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md

key-decisions:
  - "The replacement sentence was taken VERBATIM from 29.1-RESEARCH.md's proposed-replacement block, not composed. Every clause about the other four host CLIs is a cited vendor fact; the load-bearing clause is an in-repo fact verified locally before the edit rather than taken on the plan's word"
  - "The oracle does not quote the retired wording anywhere. A negative-assertion case pinning the refuted literal was written, run green, and then REMOVED — it satisfied a real risk but violated the plan's explicit acceptance criterion that the literal grep clean out of the oracle. The residual it would have covered is disclosed in the module header instead"
  - "Both dated amendment notes DESCRIBE the superseded wording rather than quoting it, for the same reason, and name where the verbatim text survives (git history and 29.1-VERIFICATION.md)"
  - "`factory.config.md`'s pointer sentence was corrected as a deviation (Rule 1). It restated the refuted premise in weakened form — 'per-subagent model selection exists on one host CLI only' — which the research refutes as directly as the authority's own sentence did"
  - "MODEL-06's checkbox was NOT flipped and Phase 29.1 was NOT marked complete. The verdict belongs to phase verification"

patterns-established:
  - "Citation-coverage derived from the claim's own enumeration (`namedOtherHostClis()`), with a pinned cardinality as the vacuity floor"
  - "A marker COUNT pinned beside a marker-anchored case, so the case's premise cannot be invalidated silently by an addition elsewhere in the same document"

requirements-completed: [MODEL-06, MODEL-07]

coverage:
  - id: D1
    description: "The kit's single authority states the model dial's reach as a fact about what grugops emits, with every clause about the other four host CLIs carrying a named primary vendor source"
    requirement: "MODEL-06"
    verification:
      - kind: unit
        ref: "scripts/model-dial-consistency.test.ts#the scope sentence is PRESENT in the packaging authority, exactly once"
        status: pass
      - kind: unit
        ref: "scripts/model-dial-consistency.test.ts#every host CLI the sentence NAMES carries a citation line — attribution, not bare assertion"
        status: pass
      - kind: unit
        ref: "scripts/model-dial-consistency.test.ts#the citation block carries one dated, URL-bearing line per named CLI"
        status: pass
      - kind: other
        ref: "grep -rn 'have no per-subagent model concept' --include='*.md' agent-factory/ CLAUDE.md README.md docs/ .planning/REQUIREMENTS.md .planning/ROADMAP.md → empty, exit 1"
        status: pass
    human_judgment: false
  - id: D2
    description: "The oracle's pinned literal moved with the sentence and is proven load-bearing — a hard wrap in the authority reds the presence case"
    requirement: "MODEL-06"
    verification:
      - kind: other
        ref: "wrap-mutation transcript, recorded below — presence case RED at 'expected +0 to be 1', reverted, 26/26 green"
        status: pass
    human_judgment: false
  - id: D3
    description: "Residuals R1, R2 and R3 ship as explicit `UNKNOWN - verify` items, and the assumption-A1 case they would have silently re-targeted now anchors on its own block with the marker count pinned"
    requirement: "MODEL-07"
    verification:
      - kind: unit
        ref: "scripts/model-dial-consistency.test.ts#each disclosed residual is present by its OWN anchor, exactly once, and sits AFTER A1"
        status: pass
      - kind: unit
        ref: "scripts/model-dial-consistency.test.ts#the authority's `UNKNOWN - verify` markers are pinned at the disclosed count"
        status: pass
      - kind: other
        ref: "marker-insertion transcript, recorded below — count case RED at 'expected 5 to be 4' while the A1 case stayed green; pre-repair predicate measured the planted block instead"
        status: pass
    human_judgment: false
  - id: D4
    description: "MODEL-06 and ROADMAP success criterion 4 restated so the criterion is satisfiable by a true sentence, each with a dated amendment note naming the research by path"
    requirement: "MODEL-06"
    verification:
      - kind: other
        ref: "grep -n 'no per-subagent model concept|per-subagent model selection exists on Claude Code alone' .planning/REQUIREMENTS.md .planning/ROADMAP.md → empty, exit 1; MODEL-06 still '- [ ]' at line 95"
        status: pass
    human_judgment: false
  - id: D5
    description: "Whether the corrected sentence, the amended MODEL-06 and the amended SC-4 genuinely agree, and whether the four vendor citations faithfully represent their sources"
    verification: []
    human_judgment: true
    rationale: "Three prose surfaces agreeing is a semantic judgment no grep decides; and the citations' fidelity to vendor documentation is exactly the kind of claim this round exists to stop asserting without a human reading the source. The three texts are quoted side by side below for that reading"

duration: 21min
completed: 2026-08-20
status: complete
---

# Phase 29.1 Plan 10: Replace the Refuted Authority Sentence Summary

**The kit's single authority for the model dial's host-CLI scope stopped asserting an unsourced absolute about four third-party products and now states the dial's reach as a fact about what grugops emits — with four vendor citations, three disclosed residuals, and an oracle that holds the attribution rather than propagating the claim.**

## Performance

- **Duration:** 21 min
- **Tasks:** 2/2
- **Commits:** 2 (`5ac0eb4`, `e3700c2`)

## What Shipped

### The replacement sentence, as it landed

At `agent-factory/packaging/subagent.frontmatter.md`, one physical line, taken verbatim from
`29.1-RESEARCH.md` §"Host-CLI per-agent model support" → "Proposed replacement sentence":

> The model dial reaches Claude Code only, and that is a fact about this kit rather than about the other four host CLIs: grugops generates per-agent adapters at `.claude/agents/` alone, so although Codex CLI, Gemini CLI, OpenCode and GitHub Copilot CLI each accept a per-agent `model` field in their own agent-definition formats, this kit emits no agent definition for any of them and there is nothing there for the dial to write into.

**Every clause traced.** The plan's hard rule was that no factual assertion ships without tracing to
a RESEARCH.md citation or a locally-verified repo fact. Clause by clause:

| Clause | Kind | Traced to |
|---|---|---|
| "grugops generates per-agent adapters at `.claude/agents/` alone" | in-repo fact | **Verified locally this session, not taken on the plan's word.** `scripts/generate-role-adapters.ts:145` is `const OUT_DIR = join(ROOT, ".claude/agents");` — a fixed literal; `ls .claude/agents/` returns 17 files. `grep -rn -E '\.opencode\|\.codex/agents\|\.gemini/agents\|\.github/agents' install/*.ts scripts/*.ts agent-factory/` returned **nothing** before the edit (exit 1) — no per-agent definition is written for any other host. `install/install.ts:990-1030` `mergeGemini()` writes only `context.fileName`; `COPILOT_REL` at 589 is `.github/copilot-instructions.md`, a pointer block; `existsSync(join(TARGET, "opencode.json"))` at 1038 is inside `detectTools()` and is a read |
| "Codex CLI … accept[s] a per-agent `model` field" | cited vendor fact | `learn.chatgpt.com/docs/agent-configuration/subagents`, HIGH — RESEARCH.md findings table + verbatim TOML example |
| "Gemini CLI … accept[s] …" | cited vendor fact | `geminicli.com/docs/core/subagents/`, HIGH — `model` row reads "Defaults to `inherit`" |
| "OpenCode … accept[s] …" | cited vendor fact | `opencode.ai/docs/agents/`, HIGH — "Use the `model` config to override the model for this agent" |
| "GitHub Copilot CLI … accept[s] …" | cited vendor fact | `docs.github.com/en/copilot/reference/custom-agents-configuration`, HIGH — property table header names the Copilot CLI; **and residual R2 discloses the one inconsistent GitHub surface rather than softening the citation** |
| "in their own agent-definition formats" | cited vendor fact | each citation line names the artefact shape the field lives in |

One correction to the research's own in-repo note, recorded rather than smoothed over: RESEARCH.md
says that grep "returns exactly one hit — the `opencode.json` existence probe". It returns **zero** —
`\.opencode` requires a literal dot and the probe is `join(TARGET, "opencode.json")`. The direction
is unchanged and the finding is stronger, not weaker. (After this plan's edit the same grep returns
four hits, all of them the new citation lines themselves — vendor formats documented, not emission
targets.)

### The citation block, as it landed

> References for the per-agent `model` field in each of the other four (retrieved 2026-08-20):
> Codex CLI — `learn.chatgpt.com/docs/agent-configuration/subagents` (`model` in a `.codex/agents/*.toml` file);
> Gemini CLI — `geminicli.com/docs/core/subagents/` (`model` frontmatter in `.gemini/agents/*.md`, defaults to `inherit`);
> OpenCode — `opencode.ai/docs/agents/` (`model` frontmatter in `.opencode/agents/*.md`, or `agent.<name>.model` in `opencode.json`);
> GitHub Copilot CLI — `docs.github.com/en/copilot/reference/custom-agents-configuration` (`model` property on a `.agent.md` profile).

Verbatim from the research section, following the `Reference:` convention already at this document's
tail and the named-source convention of the assumption-A1 note at `scripts/model-tiers.ts:44-60`.

### The three residuals

All three ship as explicit `UNKNOWN - verify` items, placed **after** the assumption-A1 block —
ordering that is load-bearing, not aesthetic, and is now asserted by a case.

- **R1 — the vendor findings are point-in-time reads.** All four references retrieved 2026-08-20; no
  vendor checked for the version at which its per-agent `model` field was introduced, so a user on an
  older build may not have it. *Direction:* the scope sentence makes no claim about when the field
  appeared, so it holds either way.
- **R2 — Copilot CLI's `model` property at run time.** The reference page's table header names the
  Copilot CLI and the `model` row carries no environment exclusion; GitHub's CLI-specific concept page
  `docs.github.com/en/copilot/concepts/agents/copilot-cli/about-custom-agents` enumerates name,
  description, prompt, tools and `mcp-servers` and does **not** list `model`. Whether
  `copilot --agent <name>` honours the property at run time, as distinct from parsing it, was not
  established. *Direction:* the sentence claims only that the four accept the field in their own
  formats, which the reference page states directly, and the same point is carried independently by
  Codex CLI, Gemini CLI and OpenCode.
- **R3 — whether grugops should emit for the other four.** Deliberately not researched; a scope
  question for a later phase. *Direction:* the sentence describes what this kit emits today and is not
  a decision that it never will emit more.

### The oracle

- `SCOPE_SENTENCE` moved byte for byte; the one-physical-line comment kept and extended to record that
  the sentence changed in this round because its premise was refuted, naming WR-06.
- `A1_ANCHOR` — the assumption-A1 case no longer locates its block with `indexOf("UNKNOWN - verify")`.
  That was sound only while the authority carried exactly one marker; it carries four now.
- `UNKNOWN_VERIFY_MARKER_COUNT = 1 + RESIDUAL_ANCHORS.length` — derived from the residual roster
  rather than hand-typed, with a promote trigger recorded beside it. Asserted two-sided.
- `RESIDUAL_ANCHORS` — each residual asserted present exactly once **and positioned after A1**.
- `namedOtherHostClis()` — the CLIs the citation block must cover are **parsed out of the scope
  sentence's own enumerating clause**, never hand-listed, and the derivation THROWS by name if that
  clause disappears. A hand-list of four would go green on a sentence that named a fifth CLI without
  citing it; this does not. Pinned at `OTHER_HOST_CLI_COUNT = 4` as the vacuity floor.
- `scopeSection()` — bounded on the right at the next `\n## ` heading. The section is last in the file
  today, so the bound lands on EOF either way; it is written now precisely because a reader that
  searches to EOF silently adopts any later block someone appends.

25 cases, all green (was 21 before this plan; 26 at the mutation transcripts below, then 25 after the
negative-assertion case was removed — see Deviations).

## Negative greps — commands and their empty output

Every one produced **no output**; `exit=` is the shell's status for the grep itself.

```
$ grep -rn 'have no per-subagent model concept' --include='*.md' agent-factory/ CLAUDE.md README.md docs/
exit=1                                   # (no output)

$ grep -c 'have no per-subagent model concept' scripts/model-dial-consistency.test.ts
0

$ grep -n 'no per-subagent model concept\|per-subagent model selection exists on Claude Code alone' .planning/REQUIREMENTS.md .planning/ROADMAP.md
exit=1                                   # (no output)

$ grep -rn 'have no per-subagent model concept' --include='*.md' agent-factory/ CLAUDE.md README.md docs/ .planning/REQUIREMENTS.md .planning/ROADMAP.md
exit=1                                   # (no output)

$ grep -inE 'cheaper|saving|spend less|reduce cost|lower cost|quota' agent-factory/packaging/subagent.frontmatter.md
exit=1                                   # (no output)

$ git diff --name-only -- CLAUDE.md package.json install/ scripts/measure-cost.ts
exit=0                                   # (no output — nothing changed)
```

**No cost claim entered.** The grep above returns nothing on the post-edit file. Baseline comparison:
the same grep against `git show HEAD:agent-factory/packaging/subagent.frontmatter.md` before the edit
also returned nothing — zero hits before, zero after, so there is no pre-existing hit to account for
and none was added.

**Positive greps required by the plan:**

```
$ awk 'length($0)>200' agent-factory/packaging/subagent.frontmatter.md | grep -c 'reaches Claude Code only'
1                                        # the sentence was NOT wrapped

$ grep -c 'learn.chatgpt.com\|geminicli.com\|opencode.ai\|docs.github.com' agent-factory/packaging/subagent.frontmatter.md
5                                        # 4 citation lines + R2's concept-page reference
```

## Mutation transcripts

### Mutation 1 — a hard wrap in the sentence reds the presence case (the pin is load-bearing)

Applied: `"alone, so although Codex CLI"` → `"alone, so\nalthough Codex CLI"` in the authority.

```
MUTATION APPLIED: hard wrap introduced into the scope sentence
     × the scope sentence is PRESENT in the packaging authority, exactly once 3ms
 FAIL  scripts/model-dial-consistency.test.ts > model dial — one authority for the Claude-Code-only
       scope statement (D-13, MODEL-06) > the scope sentence is PRESENT in the packaging authority,
       exactly once
AssertionError: expected +0 to be 1 // Object.is equality
      Tests  1 failed | 25 passed (26)
```

Reverted from a pre-mutation copy; `Tests 26 passed (26)`; `git diff --stat` back to `31 insertions(+), 1 deletion(-)`.

### Mutation 2 — a marker inserted ABOVE A1: the count reds, the A1 case does not

Applied: an additional `**\`UNKNOWN - verify\`** — a planted residual …` paragraph, >600 characters
long, inserted immediately above the A1 block.

**Post-repair outcome — the repaired case stays green, the pinned count goes red:**

```
MUTATION APPLIED: extra `UNKNOWN - verify` marker inserted ABOVE the A1 block
     × the authority's `UNKNOWN - verify` markers are pinned at the disclosed count 2ms
AssertionError: expected 5 to be 4 // Object.is equality
      Tests  1 failed | 25 passed (26)
```

**Pre-repair outcome on the same mutated file — the old case would have failed for the wrong reason.**
The pre-repair predicate (`indexOf("UNKNOWN - verify")`, then a 600-character window) was replayed
against the mutated bytes:

```
PRE-REPAIR predicate on the MUTATED file:
  window contains `CLAUDE.md` line 84 -> false (pre-repair case would be RED)
  the block it actually measured -> "UNKNOWN - verify` — a planted residual inserted ABOVE A1 purely to test the anchor.** This"
REPAIRED predicate on the same MUTATED file:
  A1-anchored window contains `CLAUDE.md` line 84 -> true (repaired case is GREEN)
```

That is the whole defect in one transcript: the old case would have gone red while A1 was perfectly
intact, because it had stopped measuring A1. Reverted; `Tests 25 passed (25)` on the final tree.

## The three texts, side by side

**Authority sentence** (`agent-factory/packaging/subagent.frontmatter.md`):

> The model dial reaches Claude Code only, and that is a fact about this kit rather than about the other four host CLIs: grugops generates per-agent adapters at `.claude/agents/` alone, so although Codex CLI, Gemini CLI, OpenCode and GitHub Copilot CLI each accept a per-agent `model` field in their own agent-definition formats, this kit emits no agent definition for any of them and there is nothing there for the dial to write into.

**Amended MODEL-06** (`.planning/REQUIREMENTS.md:95`, still `- [ ]`):

> **MODEL-06**: The dial's Claude-Code-only reach is stated rather than implied, and stated as a property of what this kit emits: grugops generates per-agent adapters for Claude Code alone, so the dial has nowhere to write on the other four host CLIs and they are unaffected — with every clause the kit ships about those four carrying a named source. `CLAUDE.md`'s "What NOT to Use" entry against non-`inherit` wrappers is amended to name this mechanism as the documented reason rather than being left to contradict it.

**Amended SC-4** (`.planning/ROADMAP.md:738`):

> 4. The dial's Claude-Code-only reach is **stated in the kit and in `CLAUDE.md`**, whose "What NOT to Use" entry against non-`inherit` wrappers is amended to name this mechanism as the documented reason. The kit's single authority states that reach as a **property of what grugops emits** — per-agent adapters at `.claude/agents/` alone — and every clause it carries about the other four host CLIs travels with a named vendor source. (MODEL-06)

**Do any two make a claim the others do not support?** No. All three now assert the same two things
and nothing more: (a) the dial's reach is a property of the per-agent artefacts this kit generates,
and grugops generates them for Claude Code alone; (b) claims about the other four carry named sources.
Neither planning surface asserts anything about those four CLIs' capabilities — the only surface that
does is the authority, and there every such clause travels with a citation. The one asymmetry is
deliberate and is not a contradiction: SC-4 and MODEL-06 additionally require the `CLAUDE.md` row,
which the authority sentence does not mention because the row is a separate deliverable held by its
own oracle cases.

**This is the item routed to human judgment (D5).** Three prose surfaces "agreeing" is a semantic
reading, not a grep result, and this whole round exists because an assertion was allowed to stand
without one.

## Changed hunks by heading

| File | Hunk | Heading it falls under |
|---|---|---|
| `agent-factory/packaging/subagent.frontmatter.md` | sentence replaced, citation block + R1/R2/R3 added (+31/−1) | `## Host-CLI scope of the model dial` |
| `scripts/model-dial-consistency.test.ts` | `SCOPE_SENTENCE` + 6 new consts/helpers (+~120) | module preamble / surface roster |
| `scripts/model-dial-consistency.test.ts` | A1 case repaired, 4 cases added (+~70) | `describe("model dial — one authority for the Claude-Code-only scope statement (D-13, MODEL-06)")` |
| `agent-factory/config/factory.config.md` | 1 line (+1/−1) — **deviation** | `**Host-CLI scope.**` note in the `models` field reference |
| `.planning/REQUIREMENTS.md` | `@@ -95 +95 @@` and `@@ -96,0 +98,2 @@` | `### MODEL — Per-Role Model Assignment` |
| `.planning/ROADMAP.md` | `@@ -738 +738 @@` | `### Phase 29.1: Per-Role Model Assignment (INSERTED)` → Success Criteria |
| `.planning/ROADMAP.md` | `@@ -794,0 +795,2 @@` | `### Phase 29.1 …` tail, after the D-17 scope-amendment block |

**Blast radius bounded.** `git diff --stat -- .planning/REQUIREMENTS.md .planning/ROADMAP.md` →
`2 files changed, 6 insertions(+), 2 deletions(-)`. `git diff -U0 -- .planning/ROADMAP.md` produced
exactly two hunk headers, at lines 738 and 795 — both inside the Phase 29.1 section, which spans
lines 725–796. **No hunk falls under any other phase's heading.** No checkbox anywhere was flipped;
`MODEL-06 | Phase 29.1 | Gaps Found` in the traceability table is untouched.

## The `CLAUDE.md:125` no-change finding — measured, not assumed

The row was read before concluding. `CLAUDE.md` line 125, quoted in full:

> \| **`model:` other than `inherit` in role wrappers without reason** \| Pins cost/capability; loses the user's session choice. \| `model: inherit` (the documented default) unless a role genuinely needs a cheaper/stronger model. **The documented reason is the factory's `models` dial** (`agent-factory/config/factory.config.md`; resolver `scripts/model-tiers.ts`): a non-default value is legitimate when the adapter generator resolved it from that dial, and is still wrong when it is hand-written into a wrapper. \|

**Finding: no change needed on this axis, and the row is measurably free of the refuted premise.**
The row makes exactly two claims — that a hand-written non-`inherit` value pins cost/capability and
loses the session choice, and that the dial is the documented reason a resolved non-default value is
legitimate. Neither mentions the other four host CLIs, and neither asserts anything about any
third-party product's capabilities. It names the mechanism, which is precisely what MODEL-06's second
clause required and what verification already confirmed satisfied.

Evidence: `git diff --name-only -- CLAUDE.md` → **empty** across both commits.
`scripts/model-dial-consistency.test.ts`'s four `CLAUDE.md`-row cases (row exists exactly once, keeps
its three-column shape, remedy names the `models` dial, `scripts/model-tiers.ts` and
`` `model: inherit` (the documented default) ``) all pass unchanged.

`CLAUDE.md:106`'s OpenCode row — "native-agent mapping (markdown agent files: `review.md` → `review`
agent)" — is confirmed correct by the research and was left alone; the research states nothing depends
on it.

## Deviations from Plan

### **1. [Rule 1 - Bug] `factory.config.md`'s pointer restated the refuted premise**

- **Found during:** Task 1, sweeping the shipped kit for other surfaces carrying the claim.
- **Issue:** `agent-factory/config/factory.config.md:136` opened with "**Host-CLI scope.** Per-subagent
  model selection exists on one host CLI only." That is the same refuted premise in weakened form —
  a capability claim about the other four, which the research refutes as directly as it refutes the
  authority's own sentence. The file is not in the plan's `files_modified`, but leaving a false claim
  standing in the shipped kit contradicts this repository's no-fabrication hard rule and the plan's own
  backstop must-have ("No surface in the shipped kit asserts a capability about the other four host
  CLIs that this phase did not establish").
- **Fix:** one line — "The model dial reaches one host CLI only." The pointer's D-13 role is unchanged:
  it still does not restate the authority sentence, and the oracle's absence direction
  (`occurrences(pointer, SCOPE_SENTENCE) === 0`) still passes.
- **Files modified:** `agent-factory/config/factory.config.md`
- **Commit:** `5ac0eb4`

### **2. [Plan compliance] A negative-assertion oracle case was written, then removed**

- **Found during:** Task 1, running the plan's acceptance greps.
- **Issue:** I added a case asserting the refuted literal is absent from all three surfaces. It was
  green and it covered a real gap — the presence direction goes green on a document carrying **both**
  sentences. But it necessarily contained the retired literal, and
  `grep -c 'have no per-subagent model concept' scripts/model-dial-consistency.test.ts` returned **3**
  against an acceptance criterion of **0**.
- **Fix:** the case and every quotation of the retired wording were removed from the module. Splitting
  the literal to dodge the grep was rejected outright — hiding a string from a check is the failure
  class this repository spends rounds closing. The gap the case covered is now **disclosed** in the
  module header ("WHAT THAT STILL DOES NOT CATCH"), naming the compensating control: the negative grep
  recorded in this SUMMARY, which phase verification re-runs.
- **Commit:** `5ac0eb4`

### **3. [Plan compliance] The amendment notes describe the superseded wording instead of quoting it**

- **Found during:** Task 2, running the plan's acceptance grep.
- **Issue:** The plan's action told me to follow annotate-never-rewrite ("the superseded wording is
  described in the note"); my first draft quoted both superseded clauses verbatim, which made the
  plan's own negative grep over `REQUIREMENTS.md` and `ROADMAP.md` return two hits.
- **Fix:** both notes now describe what the old wording asserted and point at where the verbatim text
  survives — this file's git history and `29.1-VERIFICATION.md`, which quotes it in full. "Described"
  is satisfied; "reproduced on a live surface" is not required by the convention and is what the grep
  forbids. The trace is intact.
- **Commit:** `e3700c2`

### **4. [Record, not fix] The research's own in-repo grep note is slightly wrong**

- RESEARCH.md states its `\.opencode|\.codex/agents|…` grep "returns exactly one hit". It returns
  **zero** (`\.opencode` needs a literal dot; the probe is `join(TARGET, "opencode.json")`). The
  finding's direction is unchanged and strictly stronger. Recorded here rather than editing the
  research file, per annotate-never-rewrite.

## Gate results

| Gate | Result |
|---|---|
| `npx vitest run scripts/model-dial-consistency.test.ts` | **25 passed (25)**, exit 0 |
| `npm run check:banned-claims` | ALL CHECKS PASSED — 117 documents, 22 pinned literals |
| `npm run check:claim-anchors` | ALL CHECKS PASSED — 47 rows, 47 verbatim comparisons byte-identical |
| `npm run check:public-docs` | ALL CHECKS PASSED — 10 public documents, zero retired vocabulary |
| `node scripts/check-foundation-guards.js` | ALL CHECKS PASSED |
| `npm run freshness` | 49 committed `.js` all fresh; `git diff --exit-code --name-only -- '*.js'` **empty** |
| `npx vitest run --exclude '**/scripts/e2e/**'` | **55 files, 2320 passed, 2 skipped**, exit 0 |
| `npm test` | **never run** (repo rule — it triggers the live claude-CLI e2e lane) |

**No `.ts` with a committed `.js` twin was modified.** `model-dial-consistency.test.ts` is a test file
with no twin; the other four changed files are markdown. `npm run freshness` confirms all 49 twins
fresh and the `*.js` diff is empty — this plan rebuilt nothing, as its prohibition required.

`git diff --name-only -- CLAUDE.md package.json install/ scripts/measure-cost.ts` is **empty**:
zero packages installed, `package.json` byte-unchanged (threat T-29.1-SC mitigated by asserted absence).

## Known Stubs

None.

## Threat Flags

None. This plan changed prose and one test file; it introduced no endpoint, auth path, file-access
pattern or schema change. T-29.1-23 through T-29.1-26 are addressed as the register dispositioned
them: 23 and 24 and 25 mitigated with the transcripts above; 26 accepted, disclosed as R1 and R2 in
the shipped document.

## What Verification Should Re-check

- **MODEL-06 is still unchecked and Phase 29.1 is still incomplete**, deliberately. The plan forbade
  this executor from marking either.
- **D5 is the human-judgment item**: read the four vendor citations against their sources, and read the
  three side-by-side texts for genuine agreement. This round exists because an assertion stood without
  that reading.
- The disclosed oracle residual: a **second, contradicting** sentence added elsewhere in the authority
  leaves every case green. The compensating control is the negative grep in this document.
- The pre-existing normalization residual (`encoding` probe) is unclosed and its disclosure in the
  module header still describes the code accurately after the literal moved — every comparison is
  still an exact code-point substring match with no normalization, and the two-sided presence/absence
  shape still bounds it in the same direction.

## Self-Check: PASSED

All 6 claimed files exist on disk; all 3 claimed commits (`5ac0eb4`, `e3700c2`, `0f66898`) resolve in
`git log`. Nothing claimed above is unbacked.

---
phase: 29.1-per-role-model-assignment
plan: 09
subsystem: tooling
tags: [typescript, fail-closed, ci-wiring, derived-set, definite-assignment, information-disclosure, gap-closure]

requires:
  - phase: 29.1-per-role-model-assignment
    plan: 07
    provides: "the `adapters-freshness` mirror and its resolved-preset assertions — fact (2) of the WR-07 composition, unchanged by this plan and no longer load-bearing for it"
  - phase: 29.1-per-role-model-assignment
    plan: 08
    provides: "the six named refusals in `scripts/model-tiers.*` this plan makes reachable from the build"
provides:
  - "a `guard_model_assignment` whose two degraded branches FAIL the build instead of advising, through the guard's single closing `fail()`"
  - "`relativeToRoot(text)` — one repo-relative path renderer applied to the verdict source AND to every refusal reason the promotion moved into the failing verdict"
  - "a run summary rendered on BOTH outcomes, so a failing verdict still publishes the fallback it ran against"
  - "`tieredCorpusRefusals`'s first production consumer, called unconditionally on every guard run"
  - "a `nonAgentSurfaces` list partitioned out of `SPAWN_GRANT_SCAN_PARTS` rather than naming two families by hand (14 → 16 surfaces)"
  - "two CI commands in the ubuntu gate block — `npm run generate:adapters` and `git diff --exit-code -- .claude/agents/` — pinned by a case at both ends"
affects: [29.2-install-delivery, 30-per-checkpoint-autonomy]

actuals:
  tokens: 19115
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Promote an ARM PAIR together. When two mutually exclusive branches report the same class of fact, promoting one and leaving the other is a composition hole no single input can expose — pin the pair structurally (this guard makes no `warn()` call at all) rather than by composing inputs that cannot compose"
    - "Delete the initialiser so definite-assignment analysis is the mechanism. A plausible-looking default on a `let` is a fact every future branch inherits silently; with no initialiser a fourth path is a compile error"
    - "A verdict rendered ONLY on the passing path makes every assertion about a failing verdict vacuous. Render the summary on both outcomes so the WR-08 assertion is something an input can falsify"
    - "Derive the set from the authority that already enumerates it. `SPAWN_GRANT_SCAN_PARTS` had four families and the arm named two; the missing one was the exact family the review flagged as its promote trigger"
    - "Wire a gate at BOTH ends — in the workflow and in a case that reads the workflow. This repository has now applied that lesson to this same block twice"

key-files:
  created: []
  modified:
    - scripts/check-foundation-guards.ts
    - scripts/check-foundation-guards.js
    - scripts/check-foundation-guards.test.ts
    - scripts/model-tiers.ts
    - scripts/model-tiers.js
    - .github/workflows/ci.yml

key-decisions:
  - "THE UNION CASE THE PLAN ASKED FOR DOES NOT EXIST AS AN INPUT, AND WAS BUILT STRUCTURALLY INSTEAD. The plan required 'a mirror producing both exits 1 naming both'. Measured: the two degraded branches are the arms of one `if`/`else` — a configuration that cannot be READ never reaches the resolver — so no input reaches both and a composed union case is unconstructible. The property a single-arm promotion would break was pinned instead: case (m) slices `guardModelAssignment` out of BOTH the source and the committed twin, strips whole-line comments, and asserts the function contains no `warn(` call at all. That is stronger than a composed case would have been, because it also refuses a THIRD degraded branch added later through the advisory channel."
  - "THE RUN SUMMARY IS NOW RENDERED ON THE FAILING PATH TOO, which the plan did not name but which its own acceptance criteria require. Before this change the four derivation facts printed only inside `if (modelFail === \"\")`. Promoting the degraded branches would have deleted that line from every degraded run — making the WR-08 assertion ('the degraded verdict does not contain `neither standard location`') pass because NOTHING was printed, and making `presetLabel`/`sourceLabel` unreachable on the very paths WR-08 and IN-05 are about. A phrase absent because no line exists is a vacuous pass; the plan's own repaired case (i) also asserts the summary's content on a degraded run."
  - "`relativeToRoot` TAKES TEXT, NOT A PATH. The plan's symbol table named `relativeToRoot(abs)`. Measured: the larger IN-02 leak is not the source label but the REFUSAL REASONS, which are minted in `scripts/model-tiers.ts` and interpolate the absolute path they opened into a sentence — and the promotion moved those reasons into the failing verdict. A path-only helper would have closed the smaller leak and left the bigger one open. One helper, matching a ROOT-anchored path run to the first whitespace or quoting byte and normalising the separator only inside what it matched."
  - "IN-04 RESOLVED BY DERIVING, NOT BY RECORDING A PROMOTE TRIGGER — and the measurement decided it. `SPAWN_GRANT_SCAN_PARTS` enumerates FOUR families (agent, skill, plugin-skill, packaging); `nonAgentSurfaces` named TWO by hand. The missing family is `packaging`, which is where `agent-factory/packaging/slash-command.template.md` lives — the SLASH-COMMAND surface CLAUDE.md's own format table records as carrying a legal `model` key, and the exact family the review named as the promote trigger. The trigger had already fired. Derived by partitioning the one scan composition on the `agent` prefix, exactly as `PACKAGING_TEMPLATES` above it already does; denominator 14 → 16."
  - "`tieredCorpusRefusals` IS CALLED UNCONDITIONALLY, and the discrimination proving that matters is a FINDING and not an exit code. On the eighteenth-role mirror the exit code does not discriminate — the adapter/stem count floor reds it either way — so the tiered-gated scratch build was compared on the presence of the `direction CORPUS → TABLE` finding, which vanishes."
  - "TWO CASE-NAMING DEVIATIONS, following plan 29.1-07's precedent. The plan named new cases `(k)`, `(l)`, `(m)`, `(n)`, `(o)`; `(k-low)`, `(k-high)` and `(l)` are already committed case names in this block. Renumbering a committed case to match a plan's numbering moves a name for no behavioural gain, so the new cases are `(i-resolve)`, `(m)`, `(n)`, `(o)`, `(o-reason)`, `(p)`, `(q)`, `(q-green)` and `(r)`."
  - "`scripts/model-tiers.{ts,js}` were edited although the plan's `files_modified` frontmatter lists neither. Task 2's own `<action>` requires correcting `roleCorpusCardinalityRefusal`'s paragraph in that file; the frontmatter list is short of the action it carries. The edit is DOCSTRING-ONLY — no executable line moved, and the 26-line diff on each is comment text."
  - "`npm test` was NOT run. The regression lane ran as `npx vitest run --exclude '**/scripts/e2e/**'` — 55 files, 2316 passed, 2 skipped."

patterns-established:
  - "When a plan asks for a UNION over an arm pair, first ask whether an input reaching both arms EXISTS. If the arms are exclusive by construction, a composed case is unconstructible and the honest substitute is a structural assertion over the property a single-arm promotion would break."
  - "Before asserting a phrase is ABSENT from a verdict, check the verdict is PRINTED on that path. An absence assertion over an unprinted line is the vacuity floor one level up."

requirements-completed: []

coverage:
  - id: D41
    description: "WR-07 closed at both ends: a refused `models` configuration fails the guard, and the ubuntu gate block runs the one process that refuses"
    requirement: "MODEL-03"
    verification:
      - kind: other
        ref: "End-to-end reproduction INVERTED. Pre-fix `git archive HEAD` mirror carrying `{\"models\":{\"roles\":{\"orchestratr\":\"opus\"},\"preset\":\"bogus\"}}`: `freshness:adapters` exit 0, `check-foundation-guards.js` exit 0 / ALL CHECKS PASSED. Post-fix, same config: `generate:adapters` exit 1 naming \"bogus\", `check-foundation-guards.js` exit 1 / 1 CHECK(S) FAILED. Both transcripts in Part 1."
        status: pass
      - kind: command
        ref: "`npx vitest run scripts/check-foundation-guards.test.ts` — cases (i), (i-resolve), (m) all proven RED against the pre-fix committed `.js` before the fix landed."
        status: pass
      - kind: other
        ref: "Adversarial sweep of 10 configuration shapes through all three gate commands. Nine illegal shapes refused; the one that clears is `{\"models\":{}}`, the documented legal zero-config answer. Table in Part 5."
        status: pass
  - id: D42
    description: "WR-08 / IN-05 closed: the verdict never states a configuration source the run contradicts, and both label locals are declared without initialisers"
    requirement: "MODEL-05"
    verification:
      - kind: other
        ref: "Pre-fix degraded verdict contains `from neither standard location` while the configuration file exists; post-fix it reads `from no source established — the configuration read was REFUSED`. Both lines quoted in Part 2."
        status: pass
      - kind: other
        ref: "IN-05 mutation proof, both locals, observed and reverted: deleting each branch's assignment produces `error TS2454: Variable '<name>' is used before being assigned.` Part 3."
        status: pass
  - id: D43
    description: "IN-02 closed: no guard output line carries an absolute filesystem path"
    requirement: "MODEL-05"
    verification:
      - kind: command
        ref: "`node scripts/check-foundation-guards.js` on this repository — the model-assignment PASS line reads `from agent-factory/config/factory.config.json`; it read the absolute repository root before. Part 4."
        status: pass
      - kind: other
        ref: "Cases (o) and (o-reason) assert the verdict and the refusal reason do not contain the ROOT string, on a mirror and on the live tree."
        status: pass
  - id: D44
    description: "IN-01 closed: `tieredCorpusRefusals` has a production consumer that runs on this repository's zero-config tree"
    requirement: "MODEL-03"
    verification:
      - kind: command
        ref: "`grep -rn 'tieredCorpusRefusals' scripts/ --include='*.ts'` — zero non-test callers before, one after (`scripts/check-foundation-guards.ts:2070`). Both hit lists in Part 6."
        status: pass
      - kind: other
        ref: "Case (p): an eighteenth role on a mirror carrying NO configuration is named `direction CORPUS → TABLE`; the same mirror against a tiered-gated scratch build loses that finding. Case (q): a RENAMED table row is named in BOTH directions while the declared-constant arm stays green."
        status: pass
  - id: D45
    description: "IN-04 addressed: the stray-pin family list is derived from the kit authority, and the family it omitted was the one the review named"
    requirement: "MODEL-03"
    verification:
      - kind: other
        ref: "Measurement in Part 7: four families enumerated by `SPAWN_GRANT_SCAN_PARTS`, two named by hand, `packaging` missing, `slash-command.template.md` inside it. Denominator 14 → 16, 0 stray pins."
        status: pass

status: complete
---

# Phase 29.1 Plan 09: Reachable Refusals and an Honest Verdict Summary

The six named refusals this phase built could not fail a build; now they can, at both the
guard end and the CI end, and the guard's verdict describes the run it actually performed.

## What Changed

| Finding | Where it lived | What closed it |
|---|---|---|
| WR-07 (guard half) | `guard_model_assignment`'s two degraded branches reported through `warn()`, documented advisory, does not increment `FAILS` | Both branches append to `modelFail` and are rendered by the guard's single closing `fail()` |
| WR-07 (CI half) | `.github/workflows/ci.yml` ran neither process that refuses; `generate:adapters` was in `package.json` and in no step | Two commands added to the ubuntu gate block, pinned by case (r) at both ends |
| WR-08 | `sourceLabel` initialised to "neither standard location", reassigned only in the success branch | Initialiser deleted; the read-refusal branch states that no source was established |
| IN-05 | Same defect one line up on `presetLabel` | Initialiser deleted; definite-assignment analysis now enforces every path |
| IN-02 | The verdict interpolated an absolute path — on this repository's own PASS line | One `relativeToRoot(text)` helper, applied to the source AND to every refusal reason |
| IN-01 | `tieredCorpusRefusals` had no caller outside its own oracle | Called unconditionally from `guard_model_assignment` |
| IN-04 | `nonAgentSurfaces` named two families by hand while the authority enumerates four | Partitioned out of `SPAWN_GRANT_SCAN_PARTS` on the `agent` prefix |

## Part 1 — WR-07, the end-to-end reproduction, inverted

The verifier's composition, reproduced on a `git archive HEAD` mirror of `aa26bd8` carrying
`{"models":{"roles":{"orchestratr":"opus"},"preset":"bogus"}}` at `.grugops/factory.config.json`.

**PRE-FIX — both continuous-integration gates exit 0.**

```
$ npm run freshness:adapters
Adapters fresh: 17 adapter(s) compared in .claude/agents, 0 byte difference(s), directory listings set-equal.
Mirrored generator resolved model preset: none
GATE1_EXIT=0

$ node scripts/check-foundation-guards.js
  WARN  model assignment: the `models` configuration could not be read, so the expectation DEGRADES
        to `inherit` for every role stem (D-11) rather than to any pinned tier, and the comparison
        below runs against that fallback — model-tiers: /private/tmp/.../prefix/.grugops/factory.config.json
        sets `models.preset` to "bogus", which is not a legal preset name. …
  PASS  model assignment: 17 committed adapter(s) … preset "none (degraded — the configuration was
        refused)" from neither standard location; distinct aliases resolved: inherit; 14 non-agent
        adapter surface(s) checked for a stray pin, none found
…
== Result ==
ALL CHECKS PASSED
GATE2_EXIT=0
```

Three defects visible in one transcript: the refusal is a WARN beside a PASS (WR-07), the PASS line
says there is no configuration anywhere while the file exists (WR-08), and the WARN carries an
absolute path (IN-02).

The one process that refuses, on the same mirror, run by nothing in the workflow:

```
$ npm run generate:adapters
  ERROR    model-tiers: /private/tmp/.../prefix/.grugops/factory.config.json sets `models.preset`
           to "bogus", which is not a legal preset name. …
GEN_EXIT=1
```

**POST-FIX — the same illegal configuration, through the four gate commands in workflow order.**

```
--- npm run --silent freshness:adapters
EXIT=0
Adapters fresh: 17 adapter(s) compared in .claude/agents, 0 byte difference(s), directory listings set-equal.

--- npm run --silent generate:adapters
EXIT=1
  ERROR    model-tiers: .../.grugops/factory.config.json sets `models.preset` to "bogus", which is
           not a legal preset name. The legal set is exactly: "none", "tiered". …
*** GATE FAILED HERE ***

--- git diff --exit-code -- .claude/agents/
EXIT=0

--- node scripts/check-foundation-guards.js
EXIT=1
== Result ==
1 CHECK(S) FAILED
*** GATE FAILED HERE ***
```

`freshness:adapters` still exits 0, and that is the point of the finding: it mirrors the role and
packaging trees and no configuration, so the mirrored generator never opens the offending file. It
was never going to be the gate that catches this. The two commands added after it are.

## Part 2 — WR-08, the phrase appearing and then absent

**Pre-fix**, on a degraded run whose configuration file exists:

```
preset "none (degraded — the configuration was refused)" from neither standard location
```

**Post-fix**, same mirror, same configuration, read from `guard_model_assignment` at HEAD:

```
  FAIL  model-assignment violation:
model assignment: the `models` configuration could not be READ, so the expectation DEGRADES to
`inherit` for every role stem (D-11) rather than to any pinned tier, and the comparison below ran
against that fallback — model-tiers: .grugops/factory.config.json sets `models.preset` to "bogus",
which is not a legal preset name. The legal set is exactly: "none", "tiered". …

  The run that produced these findings: model assignment: 17 committed adapter(s) under
  .claude/agents compared against a resolution recomputed for 17 derived role stem(s); preset
  "none (degraded — the configuration was refused)" from no source established — the configuration
  read was REFUSED, and the refusal is named in the finding above; distinct aliases resolved:
  inherit; 16 non-agent adapter surface(s) checked for a stray pin, none found
```

The read-refusal branch deliberately does NOT claim a configuration file exists either. The reader
returns a refusal and no `source`, so the only honest statement is that no source was established —
claiming existence would be the same defect written the other way round.

The degradation TARGET is unchanged on both branches: `distinct aliases resolved: inherit`, never a
pinned tier, asserted by cases (i) and (i-resolve).

## Part 3 — IN-05, the typecheck error from a deleted assignment

Both locals mutated in turn, observed, reverted:

```
$ # presetLabel assignment deleted from the read-refusal branch
$ npm run typecheck
scripts/check-foundation-guards.ts(2160,198): error TS2454: Variable 'presetLabel' is used before being assigned.

$ # sourceLabel assignment deleted from the read-refusal branch
$ npm run typecheck
scripts/check-foundation-guards.ts(2159,219): error TS2454: Variable 'sourceLabel' is used before being assigned.

$ # both reverted
$ npm run typecheck
REVERTED_CLEAN
```

A green typecheck after either deletion would have meant the initialisers were still there. Neither
was green.

## Part 4 — IN-02, the repo-relative verdict line

The live tree's own PASS line, before and after. This repository's configuration lives at the
second standard location, so the leak was on the line every contributor and every CI run publishes.

```
BEFORE: preset "none" from /Users/olgeroeselg/Projects/public/grugops/agent-factory/config/factory.config.json
AFTER:  preset "none" from agent-factory/config/factory.config.json
```

Full post-fix verdict, verbatim:

```
  PASS  model assignment: 17 committed adapter(s) under .claude/agents compared against a resolution
        recomputed for 17 derived role stem(s); preset "none" from
        agent-factory/config/factory.config.json; distinct aliases resolved: inherit; 16 non-agent
        adapter surface(s) checked for a stray pin, none found
```

Case (o) asserts the verdict does not contain the ROOT string — not a path-shaped pattern, which
would pass on a root the pattern happened not to describe. Case (o-reason) asserts the same over a
refusal reason, which is where the larger leak was.

## Part 5 — Adversarial sweep: is there a remaining path that clears everything?

Ten configuration shapes, each committed to a standard location on a post-fix `git archive HEAD`
mirror and run through all three gate commands in workflow order.

| Shape | `generate:adapters` | `git diff .claude/agents/` | guard | Verdict |
|---|---|---|---|---|
| 1 illegal preset value | 1 | 0 | 1 | refused |
| 2 `models: null` | 1 | 0 | 1 | refused |
| 3 typo role stem override | 1 | 0 | 1 | refused |
| 4 unknown key `models.presets` | 1 | 0 | 1 | refused |
| 5 full model id instead of an alias | 1 | 0 | 1 | refused |
| 6 unparseable JSON | 1 | 0 | 1 | refused |
| 7 `preset: tiered`, stale adapters | 0 | **1** | 0 | refused |
| 8 legal override, stale adapters | 0 | **1** | 0 | refused |
| 9 SECOND standard location, illegal preset | 1 | 0 | 1 | refused |
| 10 `models: {}` — the documented legal zero-config answer | 0 | 0 | 0 | clears, correctly |

**Both new commands are load-bearing and neither covers the other.** Rows 1-6 and 9 are carried by
the generator's refusal; rows 7 and 8 are carried by the diff alone, because the generator SUCCEEDS
on a legal-but-unapplied configuration and rewrites the adapters — at which point the guard, running
after it, sees a consistent tree and exits 0. The `git diff --exit-code` sits between them and,
under the block's `bash -e`, halts before the guard renders a verdict over regenerated rather than
committed bytes.

Row 10 is the control: the one shape that clears everything is the shape the shipped configuration
reference documents as legal.

## Part 6 — the `tieredCorpusRefusals` caller hit list, before and after

**BEFORE** (`grep -rn 'tieredCorpusRefusals' scripts/ --include='*.ts'`) — comments and its own
oracle, and no caller:

```
scripts/model-tiers.ts:280      (comment)
scripts/model-tiers.ts:763      export function tieredCorpusRefusals(
scripts/adapters-freshness.ts:434  (comment)
scripts/check-foundation-guards.ts:1934  (inside a finding STRING, not a call)
scripts/model-tiers.test.ts:57,290,307,322,338,346,381   (the oracle)
```

**AFTER** — one non-test caller:

```
scripts/check-foundation-guards.ts:364   tieredCorpusRefusals,          (import)
scripts/check-foundation-guards.ts:2070  for (const finding of tieredCorpusRefusals(stems)) {
```

**`roleCorpusCardinalityRefusal`, after** — still no production caller:

```
scripts/model-tiers.ts:1362   export function roleCorpusCardinalityRefusal(
scripts/model-tiers.test.ts:56,138,150   (the oracle)
```

Its paragraph now says exactly that. The sentence it replaced read "This function remains the place
a consumer that genuinely means 'is this the whole live corpus' asks that question out loud", which
reports consumers that did not exist. The corrected paragraph names its oracle as its only consumer,
names the sister predicate's guard caller, and records why the split is not an oversight: the sister
is the stronger of the two, so a consumer able to compare sets should call it instead.

## Part 7 — IN-04: the measurement, and the disposition it produced

Every shipped frontmatter surface family the kit authority can enumerate, measured through
`SPAWN_GRANT_SCAN_PARTS`:

```
agent           17   .claude/agents/grugops-*.md
skill            7   .claude/skills/grugops-*/SKILL.md
plugin-skill     7   skills/*/SKILL.md
packaging        2   agent-factory/packaging/slash-command.template.md
                     agent-factory/packaging/subagent.frontmatter.md
TOTAL           33
```

`nonAgentSurfaces` named **two** families by hand — `skill` and `plugin-skill`. The missing family
is `packaging`, and it contains `agent-factory/packaging/slash-command.template.md`: the
**slash-command surface**, which is the family IN-04 itself named as the promote trigger, citing
CLAUDE.md's format table recording `model` as legal frontmatter on a slash command. The trigger had
already fired. So the disposition is **derive**, not "record a promote trigger":

```ts
const nonAgentSurfaces = SPAWN_GRANT_SCAN.filter(
  (f) => !f.startsWith(spawnGrantScanPrefix("agent")),
);
```

Partitioned out of the one composition on the same prefix it was built from, exactly as the
sibling `PACKAGING_TEMPLATES` above it already does. The denominator moved **14 → 16** and is
asserted in the verdict; a fifth family added to the composition enters this arm in the commit that
adds it.

**Stray-pin surface count on this tree: 16 checked, 0 stray pins found.** The two packaging
templates carry `kind:`/`tier:` frontmatter the canonical adapter schema does not admit, so they
reach the arm and are skipped by it — which is why the "an unadmittable surface is not re-reported
here" paragraph is load-bearing rather than defensive.

## Part 8 — the ubuntu block, as read

Step name and condition, read from `.github/workflows/ci.yml`:

```yaml
      - name: Freshness gates + repo gates (ubuntu only)
        if: matrix.os == 'ubuntu-latest'
        run: |
```

No `shell:` override and no `continue-on-error:` anywhere in the workflow, so the block runs under
GitHub's documented default (`bash -e`) and a non-zero command halts it.

The two added commands, in position within the block:

```
line 10:  npm run freshness:adapters
…
line 39:  npm run generate:adapters
line 40:  git diff --exit-code -- .claude/agents/
line 41:  node scripts/check-foundation-guards.js
```

Case (r) asserts both commands are inside the block located by its step name (not merely somewhere
in the file), asserts the generator precedes both the diff and the foundation guard, and asserts no
step runs `npm test`.

**Proven load-bearing, each command separately, observed and reverted:**

```
DELETING "npm run generate:adapters"
  AssertionError: npm run generate:adapters must run inside the ubuntu gate block …
  Tests  1 failed | 250 skipped

DELETING "git diff --exit-code -- .claude/agents/"
  AssertionError: git diff --exit-code -- .claude/agents/ must run inside the ubuntu gate block …
  Tests  1 failed | 250 skipped

REVERTED
  Tests  1 passed | 250 skipped
```

## Cases repaired because this plan invalidated their premise

Two, both repaired rather than weakened, both named here as the plan requires.

**`(i) a DEGENERATE models block WARNS and degrades the whole expectation to inherit (D-11)`** —
retitled to `… FAILS the build and still degrades the expectation to inherit (D-11, WR-07)`. It
pinned the advisory outcome as correct, which was one of the three facts composing the fail-open.
Its second half is KEPT — the expectation still degrades to the lean default — because that half is
still true and is what proves the promotion did not change the degradation target. It now also
asserts a non-zero exit, that the refusal is named, and the ABSENCE of the pass marker (`  PASS
model assignment`) rather than the exit code alone.

**`(e2) the SAME plant against a HAND-LISTED expectation — the stem finding does NOT fire`** — its
assertion `not.toContain('role stem "zz-planted-model-role"')` was too broad. The case's claim has
always been about ONE arm: the loop that iterates the RESOLUTION. Giving `tieredCorpusRefusals` a
production consumer added a second arm that names the same stem from the ROLE CORPUS — an authority
the case's `resolveModels` mutation does not touch — so the total-absence assertion would have been
asserting the second arm is missing. Narrowed to the resolution arm's own sentence
(`role stem "…" resolves to`), and the second arm is asserted PRESENT on the same run so the
narrowing cannot silently become a case that would pass over a guard with no stem-naming arm at all.

No other case in the repository had its premise invalidated; every planted `models` fixture across
`model-tiers.test.ts`, `generate-role-adapters.test.ts`, `adapters-freshness.test.ts` and
`check-foundation-guards.test.ts` was searched before the suite was run.

## Deviations from Plan

**1. [Rule 2 — missing critical functionality] The run summary is rendered on the failing path.**
Found during Task 1. The plan promotes both degraded branches to `modelFail` but does not say what
happens to the derivation summary, which printed only inside `if (modelFail === "")`. Promoting
without moving it would have deleted the summary from every degraded run — making the plan's own
WR-08 acceptance criterion (`the guard's output does not contain the phrase neither standard
location`) pass because nothing was printed, and leaving `presetLabel`/`sourceLabel` unused on the
paths WR-08 and IN-05 are about. The summary is now built once and rendered on both outcomes; the
stray-pin clause reports a count instead of the literal "none found" when pins exist, so the passing
line stays byte-identical on a green tree. Commit `c496c7b`.

**2. [Rule 2] `relativeToRoot` operates on text, not on a path.** Found during Task 1. The plan's
prohibition is "No absolute filesystem path appears in any guard output line"; the refusal reasons
minted in `scripts/model-tiers.ts` interpolate the absolute path they opened, and the promotion
moved those reasons into the failing verdict. A path-only helper as the plan's symbol table
described would have satisfied the symbol and violated the prohibition. Commit `c496c7b`.

**3. [Rule 3] `scripts/model-tiers.{ts,js}` edited although absent from `files_modified`.** Task 2's
`<action>` requires correcting `roleCorpusCardinalityRefusal`'s paragraph in that file. Docstring
only; no executable line moved. Commit `0095497`.

**4. Case names.** The plan's `(k)`, `(l)`, `(m)`, `(n)`, `(o)` collide with the committed
`(k-low)`, `(k-high)` and `(l)`. New cases are `(i-resolve)`, `(m)`, `(n)`, `(o)`, `(o-reason)`,
`(p)`, `(q)`, `(q-green)`, `(r)`. Same precedent as plan 29.1-07.

**5. The union case was built structurally.** See the key decision above — an input reaching both
degraded arms does not exist.

**6. Case (m) strips whole-line comments before probing for `warn(`.** First implementation went RED
on the source because the promotion's own record DISCUSSES `warn()` at length. The probe now runs
over code lines only, with two premise assertions (the slice reaches `nonAgentSurfaces`, and the
probe can see the guard's own `fail(` call) proving the strip did not eat the body.

## Verification

All commands run locally on this repository at HEAD `2ec29c5`.

| Command | Result |
|---|---|
| `npx vitest run --exclude '**/scripts/e2e/**'` | 55 files, **2316 passed / 2 skipped** |
| `node scripts/check-foundation-guards.js` | exit 0, ALL CHECKS PASSED |
| `npm run freshness` | exit 0 — 49 committed `.js` all fresh |
| `npm run freshness:adapters` | exit 0 |
| `npm run generate:adapters` then `git diff --exit-code -- .claude/agents/` | exit 0, diff empty |
| `npm run typecheck` | exit 0 |
| `npm run build` then `git diff --exit-code --name-only -- '*.js'` | empty |
| `git diff --name-only -- install/ package.json agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json scripts/measure-cost.ts` | empty |
| `npm test` | **NOT RUN** — the live claude-CLI e2e lane is not a gate |

## UNKNOWN - verify

**The workflow was not executed on a GitHub runner.** GitHub Actions cannot be run locally, so no
claim here is made about the two new steps passing *in CI*. What was executed is the same commands,
in the same order, in a shell on this machine, with the results recorded above. Two properties are
therefore asserted from reading rather than from running:

- that GitHub's default shell for a `run:` block on `ubuntu-latest` is `bash -e`, so a non-zero
  command halts the block. The workflow declares no `shell:` override and no `continue-on-error:`
  (verified by grep), so the default applies — but the default itself is documented behaviour, not
  something measured here. `UNKNOWN - verify` on the first live run.
- that the ubuntu leg's checkout is clean enough for `git diff --exit-code -- .claude/agents/` to
  mean what it means locally. `actions/checkout` produces a clean tree, so a non-empty diff after
  `generate:adapters` can only be the generator's writes — but this was not observed on a runner.
  `UNKNOWN - verify`.

## Known Stubs

None. No hardcoded empty value, placeholder string, TODO or FIXME was introduced by this plan.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema at a trust boundary was
introduced. Every threat in the plan's register (T-29.1-18 through T-29.1-22, plus T-29.1-05 and
T-29.1-SC) is mitigated as planned; the supply-chain row is asserted by `git diff --name-only --
package.json` being empty across all three commits.

## Self-Check: PASSED

Files asserted present:

```
FOUND: scripts/check-foundation-guards.ts
FOUND: scripts/check-foundation-guards.js
FOUND: scripts/check-foundation-guards.test.ts
FOUND: scripts/model-tiers.ts
FOUND: scripts/model-tiers.js
FOUND: .github/workflows/ci.yml
```

Commits asserted present in `git log`:

```
FOUND: c496c7b  fix(29.1-09): a refused models configuration FAILS the build …
FOUND: 0095497  fix(29.1-09): give the cardinality predicate a consumer that runs …
FOUND: 2ec29c5  ci(29.1-09): run the adapter generator in the ubuntu gate block …
```

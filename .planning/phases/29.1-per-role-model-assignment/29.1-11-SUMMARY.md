---
phase: 29.1-per-role-model-assignment
plan: 11
subsystem: tooling
tags: [set-literal-drift, derived-set, two-sided-pin, documented-precedence, coverage-declaration, gap-closure, round-closing]

requires:
  - phase: 29.1-per-role-model-assignment
    plan: 08
    provides: "`MODELS_KEYS` and the closed-vocabulary shape in `scripts/model-tiers.*` that `MODELS_CONFIG_CANDIDATE_RELS` is declared beside and imitates"
  - phase: 29.1-per-role-model-assignment
    plan: 10
    provides: "the pinned literal and the derived citation-coverage shape in `model-dial-consistency.test.ts` — this plan repairs the SURFACE ROSTER in the same file without touching either"
provides:
  - "`MODELS_CONFIG_CANDIDATE_RELS` — the two configuration locations declared ONCE and consumed by `readModelsConfig`, with neither spelled a second time anywhere in the module"
  - "the whole-file precedence and its shadowing consequence stated in `factory.config.md`, which is its single authority, adjacent to the D-17 disclosure"
  - "a shadowing case pinning the present-but-`models`-less-first-candidate shape, proven RED under a reversal of the declared order"
  - "`SURFACE_COUNT` derived from a run-time scan of every shipped markdown document instead of from the hand roster it is compared to"
  - "`DIAL_SURFACE_MARKER` — a marker chosen by recorded measurement, with the corpus asserting its own premise against an authority that did not produce it"
  - "the full 40-character `PRE_PHASE_ADAPTER_BASELINE`, resolved in this repository, with a length-and-character-class case"
  - ".planning/phases/29.1-per-role-model-assignment/COVERAGE.md — the phase's no-integration declaration, 194 characters"
affects: [29.2-install-delivery, phase-29.1-verification]

actuals:
  tokens: 12300
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "A two-sided pin whose two sides are ONE hand-written list certifies nothing. Derive one side from something that did not produce the other, and prove BOTH directions red"
    - "MEASURE the marker before committing to it. Three candidates were scanned over the corpus and two of them silently missed a roster surface; the choice is recorded as a measurement, not as taste"
    - "A vacuity floor catches an EMPTY denominator and never a SILENTLY SHORT one. The corpus asserts containment of a set the KIT AUTHORITY produced independently, so a lost directory is loud"
    - "PARTITION, do not filter. One measured hit was not a prose surface; it is subtracted by IDENTITY against another authority's own constant, so exactly one file can ever sit outside and no path is typed to do it"
    - "When a plan's instruction turns a ratified invariant red, the invariant wins and the must-have is narrowed IN PUBLIC. Splitting an obligation by AUDIENCE is a shape; respelling a path to dodge a grep is the failure class this repository spends rounds closing"
    - "Assert a deliberate absence POSITIVELY. An omission reads as an oversight to the next editor; an assertion carrying its reason reads as a decision"

key-files:
  created:
    - .planning/phases/29.1-per-role-model-assignment/COVERAGE.md
    - .planning/phases/29.1-per-role-model-assignment/29.1-11-SUMMARY.md
  modified:
    - scripts/model-tiers.ts
    - scripts/model-tiers.js
    - scripts/model-tiers.test.ts
    - scripts/model-dial-consistency.test.ts
    - scripts/adapter-byte-baseline.test.ts
    - agent-factory/config/factory.config.md
    - agent-factory/packaging/subagent.frontmatter.md

key-decisions:
  - "THE PLAN'S CENTRAL WR-05 MUST-HAVE IS NOT MET AS WRITTEN, AND THE REASON IS ITSELF AN INVARIANT. The plan required BOTH shipped documents to name BOTH configuration locations by path. Doing so turned `scripts/check-kit-refs.js` RED: Assertion 1 (D-08.1, SHOME-03/SC5) holds ZERO `agent-factory/config/` references across the kit scan set, which includes the packaging authority's own directory. That gate is twenty-two phases older than this dial and carries no exemption list. The obligation was split by AUDIENCE instead — the config field reference (outside the scan set) names both paths and holds the precedence rule; the packaging authority names the user's location, states the location COUNT, and points at the rule's home. Respelling the path to dodge the grep was rejected outright."
  - "CORRECTION 2026-08-20 (finding R2-WR-05, implemented by plan 29.1-17) — the key decision ABOVE is left standing in full and is superseded on its premise, not on its facts about the two documents. Four things are now recorded. (1) `install/install.ts:1065` is `cpSync(join(GRUGOPS_SRC, \"agent-factory\"), tmp, { recursive: true })` inside `copyKit` — the WHOLE `agent-factory/` tree, `config/` included, lands at `$GRUGOPS_HOME/agent-factory` on every install, so `agent-factory/config/factory.config.md` is delivered to every installed user. (2) 'outside the `check-kit-refs` SCAN set' and 'not shipped kit prose' are DIFFERENT FACTS, and the rationale above used the first to establish the second; `agent-factory/config` was absent from SCAN by omission, since before the config dial existed, not by an audience judgment. (3) D6 was put to the user in round 3 and the USER RULED: add the directory to the scan, with an explicit, argued exemption for the two kit-internal mentions the field reference would then need to carry. (4) Plan 29.1-17 implemented that ruling — SCAN gained exactly one entry, the exemption is a predicate over hits (a self-reference to an existing sibling), its cardinality is asserted two-sided at 2 hits in 1 file, and it is published in Assertion 1's pass line. The `patterns-established` entry below naming the audience split is superseded by the same ruling and is likewise left standing rather than rewritten."
  - "The precedence was NOT changed, as the plan required. The first existing file wins whole, matching `readGovernanceConfig`; the defect WR-05 reported is that no shipped document said so, and that is what moved."
  - "The marker was chosen by MEASUREMENT recorded before the design was fixed. `model dial` and `` `models` block `` each returned 2 hits and silently missed CLAUDE.md; `scripts/model-tiers.ts` returned exactly the roster over a 78-file corpus."
  - "`docs/audit/28-claim-registry.md` DOES carry the marker and is NOT counted as a fourth prose surface. It is subtracted by identity against `REGISTRY_PATH` from `scripts/audit-model.ts` — a register that QUOTES claims is not a surface that MAKES them, and it is held byte-for-byte by its own gates. The hit is reported here rather than hidden; the corpus was not trimmed to make it disappear."
  - "The two `toHaveLength` calls in the vacuity case were kept and the bounds pair deleted (IN-03). The comment recording the deletion deliberately does NOT spell the two matcher names, so the acceptance grep returns 0 rather than returning the comment — the same trap plan 29.1-10 hit from the other side."
  - "MODEL-01, MODEL-02, MODEL-06 and MODEL-07 were NOT flipped to complete and Phase 29.1 was NOT marked complete. The verdict belongs to phase verification, and this round's standing rule forbids it."

patterns-established:
  - "Split a documentation obligation by AUDIENCE when one surface is governed by a path invariant the other is not"
  - "Assert a forbidden literal's ABSENCE inside the oracle that would otherwise be blamed for the omission"

requirements-completed: []

coverage:
  - id: D1
    description: "WR-05 closed on the code side: the two configuration locations are declared ONCE as an exported constant, `readModelsConfig` builds its candidate list from it, and neither is spelled a second time in the module"
    requirement: "MODEL-01"
    verification:
      - kind: unit
        ref: "scripts/model-tiers.test.ts#the candidate list is built from the exported location constant, in declared order"
        status: pass
      - kind: other
        ref: "grep -n '\\.grugops' scripts/model-tiers.ts → one hit, line 269, inside the exported constant"
        status: pass
    human_judgment: false
  - id: D2
    description: "The shadowing shape is pinned by a case and proven to depend on the DECLARED ORDER, not merely on a boolean"
    requirement: "MODEL-01"
    verification:
      - kind: unit
        ref: "scripts/model-tiers.test.ts#SHADOWING: a present-but-models-less first candidate wins WHOLE over a models-carrying second"
        status: pass
      - kind: other
        ref: "reversal mutation transcript below — 3 cases RED including the shadowing case, reverted, 96/96 green"
        status: pass
    human_judgment: false
  - id: D3
    description: "The whole-file precedence and its shadowing consequence are stated where a configuring reader meets them, in exactly one document"
    requirement: "MODEL-02"
    verification:
      - kind: unit
        ref: "scripts/model-dial-consistency.test.ts#EXACTLY ONE document states the whole-file precedence rule — the config reference, not the authority"
        status: pass
      - kind: unit
        ref: "scripts/model-dial-consistency.test.ts#every declared configuration location appears in the config field reference"
        status: pass
    human_judgment: false
  - id: D4
    description: "The surface roster is cross-checked against a run-time derivation whose denominator is independent of the loop that consumes it, with both directions observed RED"
    requirement: "MODEL-06"
    verification:
      - kind: unit
        ref: "scripts/model-dial-consistency.test.ts#the surface roster equals the set derived from the kit authority at run time"
        status: pass
      - kind: other
        ref: "direction-one and direction-two mutation transcripts below, plus the pre-fix contrast (27/27 green under direction two)"
        status: pass
    human_judgment: false
  - id: D5
    description: "IN-03 and IN-06 closed"
    requirement: "MODEL-07"
    verification:
      - kind: other
        ref: "grep -c 'toBeGreaterThanOrEqual\\|toBeLessThanOrEqual' scripts/model-dial-consistency.test.ts → 0"
        status: pass
      - kind: unit
        ref: "scripts/adapter-byte-baseline.test.ts#the pinned baseline is a full 40-character object name, never an abbreviation"
        status: pass
    human_judgment: false
  - id: D6
    description: "Whether splitting the WR-05 documentation obligation by audience is an acceptable narrowing of the plan's must-have, or whether D-08.1's scan set should instead be amended"
    verification: []
    human_judgment: true
    rationale: "Two ratified contracts collided and one had to give. The executor chose the option that preserved every existing invariant and narrowed the newer must-have, and recorded it in `.planning/WINDOWS.md`. Whether that is the right disposition — as against amending `check-kit-refs`'s Assertion 1 to carve out a read-only mention of the kit's bundled default — is a scope judgment no gate decides."

duration: 30min
completed: 2026-08-20
status: complete
---

# Phase 29.1 Plan 11: Documented Locations, a Derived Roster and the Round's Close Summary

**The resolver's two configuration locations are now declared once and documented with the whole-file precedence that had never been written down; the surface roster is checked against a run-time scan instead of against itself; and the round closes with a full 40-character baseline, a measured coverage declaration, and every gate green — with one plan must-have narrowed in public because it collided with a ratified path invariant.**

## Performance

- **Duration:** ~30 min
- **Tasks:** 3/3
- **Commits:** 3 (`49ff778`, `1424f42`, `6bc8642`)

---

## Part 1 — WR-05: the shadowing reproduction, before and after

### Reproduced against the COMMITTED `scripts/model-tiers.js`, before the case existed

Fixture: `{"quality":{"tdd":true}}` at `.grugops/factory.config.json`, `{"models":{"preset":"tiered"}}` at `agent-factory/config/factory.config.json`, in a fresh scratch root. **The fixture's own premise is asserted first**, because there is more than one way to reach `preset: "none"` and a typo in either literal would make the reproduction pass for the wrong reason.

```
FIXTURE PREMISE: first candidate exists       -> true
FIXTURE PREMISE: first candidate has `models` -> false
FIXTURE PREMISE: second candidate has `models`-> true
FIXTURE PREMISE: second candidate preset      -> tiered
---
RESULT ok      -> true
RESULT preset  -> none
RESULT source  -> <scratch>/.grugops/factory.config.json
---
SHADOWING REPRODUCED -> true
```

The resolver answers the zero-config preset and names the shadowing file as its source. Verbatim what the reviewer reported.

### Post-fix: the BEHAVIOUR is unchanged, deliberately

The identical script against the post-fix build returns the identical output. **The plan forbade changing the precedence** — it matches `readGovernanceConfig`'s established convention and is defensible. WR-05's defect was never the behaviour; it was that no shipped document stated it, and that the one document naming a path named one of two. So this finding closes by DOCUMENTATION plus a pinning case, not by a refusal.

### The reversed-order RED transcript — the discrimination proof

Mutation: the two members of `MODELS_CONFIG_CANDIDATE_RELS` swapped, rebuilt, suite re-run.

```
MUTATION APPLIED: declared candidate order REVERSED
     × `.grugops/factory.config.json` WINS over the in-kit config when BOTH exist 4ms
     × SHADOWING: a present-but-models-less first candidate wins WHOLE over a models-carrying second 1ms
     × the candidate list is built from the exported location constant, in declared order 1ms

AssertionError: the repo-dropped location is the more specific one and resolves first: expected 'none' to be 'tiered'
AssertionError: the second file's `tiered` is shadowed — this is the zero-config answer: expected 'tiered' to be 'none'
AssertionError: expected 'agent-factory/config/factory.config.j…' to be '.grugops/factory.config.json'
      Tests  3 failed | 93 passed (96)
```

**Recorded precisely rather than flatteringly:** the shadowing case reds on its PRESET assertion, which is stated before the source assertion, so the message reads as the wrong resolution rather than as the wrong source. Both are wrong under the reversal and both assertions are present; the first one to fire is the preset.

**Why the case can go red at all.** Its fixture plants files at this test file's OWN independently written path tuples (`REPO_DROPPED`, `IN_KIT`) and asserts the source against those same tuples — deliberately NOT against `MODELS_CONFIG_CANDIDATE_RELS`. A fixture that planted through the constant and asserted against the constant would move both sides together under a reversal and could never fail. That is the exact failure class this round exists to close, and it is recorded in the case body.

Reverted; `96 passed (96)`; the rebuilt `.js` is byte-identical to the pre-mutation build (`cmp` → YES).

### The four document counts

Recorded post-repair (see Deviation 1 — the fourth is **0**, and that is a decision, not an oversight):

| Document | `.grugops/factory.config.json` | `agent-factory/config/factory.config.json` |
|---|---|---|
| `agent-factory/config/factory.config.md` | 2 | 2 |
| `agent-factory/packaging/subagent.frontmatter.md` | 3 | **0** |

`grep -n '\.grugops' scripts/model-tiers.ts` → **one hit, line 269, inside the exported constant.** No second inline spelling anywhere in the module; the `readModelsConfig` section comment that used to spell both paths was rewritten to point at the constant.

**D-17 paragraph byte-unchanged**, compared against `git show HEAD:agent-factory/config/factory.config.md` — the new location paragraph sits immediately above it, so a configuring reader meets both installed-target facts together.

**No cost claim entered either document.** `grep -inE 'cheaper|saving|spend less|reduce cost|lower cost|quota'` returns nothing on either file post-edit, and returned nothing on either file at HEAD before the edit — zero before, zero after, so there is no pre-existing hit to account for and none was added.

---

## Part 2 — the surface roster: measured, derived, and red in both directions

### The measurement, recorded BEFORE the design was fixed

Candidate corpus assembled from `listPackagingTemplates` + the config documents + the named host-guidance file, then scanned:

```
CANDIDATE CORPUS (4 files):
  agent-factory/packaging/slash-command.template.md
  agent-factory/packaging/subagent.frontmatter.md
  agent-factory/config/factory.config.md
  CLAUDE.md

MARKER "model dial"             -> 2 hits   roster-not-derived: CLAUDE.md          SET-EQUAL: false
MARKER "models` block"          -> 2 hits   roster-not-derived: CLAUDE.md          SET-EQUAL: false
MARKER "scripts/model-tiers.ts" -> 3 hits   both directions empty                  SET-EQUAL: true
```

Two of the three obvious markers **silently missed a roster surface** — CLAUDE.md's row names the mechanism by resolver path rather than by the phrase "model dial". Had the marker been chosen on taste, the roster would have been "reconciled" to two and the third surface quietly dropped. The measurement is why it was not.

Re-measured over a broader corpus (73 markdown files under `agent-factory/` plus 5 at repo root):

```
BROAD CORPUS (78 files) with marker "scripts/model-tiers.ts":
  hit: agent-factory/config/factory.config.md
  hit: agent-factory/packaging/subagent.frontmatter.md
  hit: CLAUDE.md
```

**The derived file list beside the roster's three paths: identical, both directions empty.**

### The hit that is NOT a prose surface — reported, not hidden

A repo-wide grep found **one further hit outside the corpus**:

```
$ grep -rl 'scripts/model-tiers.ts' --include='*.md' .   # excluding node_modules/.planning/.git
agent-factory/config/factory.config.md
agent-factory/packaging/subagent.frontmatter.md
CLAUDE.md
docs/audit/28-claim-registry.md
```

`docs/audit/28-claim-registry.md` is this repository's **claim register** — `REGISTRY_PATH` in `scripts/audit-model.ts`, held byte-for-byte by `check-audit-register` and `check-banned-claims`. Its rows QUOTE claims made elsewhere; a register that quotes a claim is not a surface that makes one, and counting it as a fourth D-13 prose surface would be circular.

**It is PARTITIONED, not filtered, and the distinction is load-bearing.** The corpus deliberately INCLUDES `docs/**` so this file is scanned, and the subtraction is `rel !== REGISTRY_PATH` — an identity comparison against another module's own constant, with no path typed in the oracle. Exactly one file can ever sit outside the prose arm, and a case asserts that (`registers.filter((rel) => rel !== REGISTRY_PATH)` is empty), so the partition cannot quietly grow into a filter. The corpus was never trimmed until it fit.

### The corpus asserts its OWN premise

The lesson this repository paid for repeatedly: *a vacuity floor catches an EMPTY denominator and never a SILENTLY SHORT one.* If the markdown walk lost a directory, the derived surface set would shrink and the roster comparison would still be a comparison — just over less, and green.

So `corpusPremiseGaps()` checks the walk for containment of a set the **kit authority** produced independently: every packaging template `listPackagingTemplates(ROOT)` reports must appear in the walk. That lister has its own non-empty floor, its own directory literal, and shares no code with the walk. A gap throws by name and refuses to derive anything.

### Direction one — delete a roster entry

```
MUTATION APPLIED (direction one): the hostGuidance roster entry DELETED

Error: model-dial oracle: PREMISE VIOLATED — the surface roster names 2 surface(s)
[authority, pointer] and produced 2 derived entr(ies), but the RUN-TIME SCAN found 3
dial-discussing prose surface(s): [CLAUDE.md, agent-factory/config/factory.config.md,
agent-factory/packaging/subagent.frontmatter.md]. Derived but NOT in the roster:
[CLAUDE.md]. In the roster but NOT derived: [none]. … THE EXPECTED NUMBER IS NO LONGER
A LITERAL: it comes from the scan, so this cannot be silenced by moving a constant.
      Test Files  1 failed (1)
```

It **names the missing surface and both difference directions**, and it still names the premise — the module-load adjudication and its recorded reason are intact, as the plan required. Reverted; `28 passed (28)`.

### The pre-fix contrast — this is the finding

The same deletion on a scratch copy of the **pre-fix** file, with the count literal moved `3 → 2` in the same edit, exactly as a well-meaning author would do it:

```
PRE-FIX SCRATCH COPY: hostGuidance roster entry DELETED **and** the count literal moved 3 -> 2

 ✓ derives EXACTLY three surfaces, pinned two-sided against SURFACE_COUNT — the vacuity floor 1ms
 ✓ every derived surface exists on disk, naming any that does not 0ms
 ✓ every derived surface carries bytes — an empty surface agrees with everything 0ms
      Tests  3 failed | 24 passed (27)
```

**The entire roster-cardinality machinery went GREEN.** The three failures are unrelated content cases that happen to call `readSurface("hostGuidance")` by hand — a different mechanism, catching it for a different reason. The pin whose two sides were one hand-list certified nothing, and this transcript is the proof: its own floor passed on a roster it had just lost a third of.

### Direction two — a kit document starts discussing the resolver

The direction the pre-fix pin **structurally could not fail on**. The marker was planted in `agent-factory/packaging/slash-command.template.md`, a kit document not in the roster.

**Post-fix:**

```
Error: model-dial oracle: PREMISE VIOLATED — … the RUN-TIME SCAN found 4 dial-discussing
prose surface(s): [CLAUDE.md, agent-factory/config/factory.config.md,
agent-factory/packaging/slash-command.template.md,
agent-factory/packaging/subagent.frontmatter.md]. Derived but NOT in the roster:
[agent-factory/packaging/slash-command.template.md]. In the roster but NOT derived: [none].
      Test Files  1 failed (1)
```

**Pre-fix, same mutation, on a scratch copy of the HEAD file:**

```
PRE-FIX code + direction-two mutation:
 Test Files  1 passed (1)
      Tests  27 passed (27)
```

Fully green. Both mutations reverted; `git status` clean of scratch files afterwards, verified.

### IN-03, and one honest catch

```
$ grep -n 'SURFACE_COUNT' scripts/model-dial-consistency.test.ts
55:// FAILURE CLASS. `SURFACE_COUNT` used to be the literal `3`, …
192:const SURFACE_COUNT = derivedDialSurfaces().prose.length;
324:  if (roles.length !== SURFACE_COUNT || surfaces.length !== SURFACE_COUNT) {
…
$ grep -nE 'SURFACE_COUNT\s*=\s*[0-9]' scripts/model-dial-consistency.test.ts
(none)
$ grep -c 'toBeGreaterThanOrEqual\|toBeLessThanOrEqual' scripts/model-dial-consistency.test.ts
0
```

The bounds grep read **2** on the first pass — both hits were the new comment *explaining what had been removed*, which named the two matchers. The comment was reworded to describe them without spelling them, and it says so in the file. The alternative — splitting a literal to dodge a grep — is the failure class this repository spends rounds closing and was rejected on sight; the record of what was deleted lives here and in git history, where it belongs.

The two `toHaveLength` calls are kept and the case body records why they are **not** a duplicate pair: one reads the roster OBJECT, the other reads the ARRAY BUILT FROM IT, and a mutation to either alone must break the case.

### Disclosed residuals, with their directions

Stated in the file, not inferred from silence:

- **R-a** — `.planning/**` is outside the corpus. A planning document restating the dial's rules is not caught. *Direction:* planning artefacts are not shipped, so a disagreement there misleads this project rather than its users.
- **R-b** — only `.md` is scanned. Dial prose in a JSON, or fenced inside a `.ts`, is invisible. *Direction:* undetected, never falsely detected.
- **R-c** — the marker is the resolver's path. A document discussing the dial without naming that path is not caught — measured: `"model dial"` alone missed CLAUDE.md. *Direction:* this floor is a lower bound on the surfaces in play, never an over-count.

**Which part is derived and which is named:** the COUNT and the FILE LIST are derived from the scan. The ROLE each file plays in D-13's shape (authority / pointer / host-guidance) stays in `SURFACE_ROLES`, because no scan can decide it. That split is stated in the file.

---

## Part 3 — IN-06 and the coverage declaration

### The SHA, resolved rather than copied

```
$ git rev-parse 6f8411e
6f8411effe80b3f22d0d668e1fa40fa78e3a8088
$ [ "$(git rev-parse 6f8411e)" = "6f8411effe80b3f22d0d668e1fa40fa78e3a8088" ] && echo YES
YES
```

It equals the value the plan asserted. Had it not, the plan's number would have been reported rather than pinned.

```
$ grep -c "6f8411effe80b3f22d0d668e1fa40fa78e3a8088" scripts/adapter-byte-baseline.test.ts
2
$ grep -cE '"6f8411e"' scripts/adapter-byte-baseline.test.ts
0
```

### The abbreviation RED transcript

```
MUTATION APPLIED (scratch copy): the constant replaced with the seven-character abbreviation
     × the pinned baseline is a full 40-character object name, never an abbreviation 3ms
AssertionError: expected '6f8411e' to have a length of 40 but got 7
      Tests  1 failed | 2 passed (3)
```

The length and the character class are asserted **separately**, so "someone shortened it" and "someone pasted something that is not an object name" are two different reds with two different remedies.

### COVERAGE.md

Declaration line, with its **measured** length:

```
$ awk 'NR==3{print "LINE 3 LENGTH: " length($0)}' .../COVERAGE.md
LINE 3 LENGTH: 194
```

> No external API integration: this phase edits a build-time code generator, its resolver, the foundation guards and kit prose; the tooling layer has zero runtime dependencies and opens no socket.

194 < 200. The count was printed, not estimated — this project has previously had the verification workflow halt on exactly that cap.

The seal-time gate now reads it:

```
$ gsd-tools check api-coverage.verify-pre 29.1-per-role-model-assignment
{ "block": false, "passed": true, "coverage_present": true, "matrix": "COVERAGE.md",
  "none_declared": true, "detected": false,
  "message": "api-coverage: COVERAGE.md declares no external API integration — matrix not required" }
```

The declaration **agrees with** the detector rather than overruling it. The file's evidence section names the one adjacent thing that is not an integration: the dial emits model *aliases* into generated frontmatter and never calls a provider — the host CLI is the runtime and holds any credential.

---

## Part 4 — the round-closing gate set on the FINAL tree

Every result with its exit code. This is confirmation that none of the four preceding plans' recorded evidence was invalidated by a later plan, not a formality.

| Gate | exit |
|---|---|
| `npx vitest run scripts/adapter-byte-baseline.test.ts` (3 passed) | 0 |
| `npm run typecheck` | 0 |
| `npm run build` | 0 |
| `git diff --exit-code --name-only -- '*.js'` | 0 (empty) |
| `npm run freshness` | 0 |
| `npm run freshness:adapters` | 0 |
| `npm run freshness:catalog` | 0 |
| `npm run freshness:context` | 0 |
| `npm run freshness:skill-twins` | 0 |
| `npm run generate:adapters` | 0 |
| `git diff --exit-code -- .claude/agents/` | 0 (empty) |
| `node scripts/check-foundation-guards.js` | 0 |
| `node scripts/check-kit-refs.js` | 0 |
| `npm run check:banned-claims` | 0 |
| `npm run check:claim-anchors` | 0 |
| `npm run check:public-docs` | 0 |
| `npm run check:audit-register` | 0 |
| `npx vitest run --exclude '**/scripts/e2e/**'` | 0 |

**Regression run:** `Test Files 55 passed (55)` · **`Tests 2328 passed | 2 skipped (2330)`** · 155s.

`npm test` was **never run** — the repository rule stands; it triggers the live claude-CLI e2e lane.

`check-kit-refs.js` is not in the plan's gate list. It is in this table because this plan turned it RED and had to repair it — see Deviation 1.

### The prohibited set, across the WHOLE round

Measured from the commit preceding plan 29.1-07 (`aa26bd8`) to the final commit:

```
$ git diff --name-only aa26bd8..HEAD -- scripts/measure-cost.ts scripts/measure-cost.js \
    install/ package.json agent-factory/config/factory.config.json \
    agent-factory/seed/.grugops/factory.config.json
(no output)
```

Zero packages installed; `package.json` byte-unchanged; no install-side file read for modification; `measure-cost.*` byte-unchanged; no `models` block added to either config JSON. **T-29.1-SC mitigated by asserted absence over the round, not over one plan.**

---

## Part 5 — all fifteen findings of the round

| # | Finding | Closed by | How |
|---|---|---|---|
| 1 | **CR-01** the D-04 zero-config pin is half-blind to `models.roles` | 29.1-07 | mechanism |
| 2 | **WR-01** a typo inside `models` is silently swallowed | 29.1-08 T1 | mechanism |
| 3 | **WR-02** `resolveModels` Floor 0 accepts `preset: null` as `none` | 29.1-08 T2 | mechanism |
| 4 | **WR-03** `resolvedPresetsIn` matches the marker anywhere in a line | 29.1-07 | mechanism |
| 5 | **WR-04** `adapters-freshness.ts` hand-spells the marker | 29.1-07 | mechanism |
| 6 | **WR-05** one location documented, two read, first file wins wholesale | **29.1-11 T1** | mechanism (constant + shadowing case) **+ partial DISCLOSURE** — see below |
| 7 | **WR-06** the host-CLI scope sentence is an unsourced claim | 29.1-10 T1 | mechanism + **disclosure** (residuals R1–R3 ship as `UNKNOWN - verify` in `subagent.frontmatter.md`) |
| 8 | **WR-07** a malformed live `models` block has no fail-closed reader in CI | 29.1-09 | mechanism |
| 9 | **WR-08** the guard's degraded-branch source label states the opposite | 29.1-09 | mechanism |
| 10 | **IN-01** `roleCorpusCardinalityRefusal` / `tieredCorpusRefusals` have no production consumer | 29.1-09 | mechanism |
| 11 | **IN-02** the guard's PASS line embeds an absolute path | 29.1-09 | mechanism |
| 12 | **IN-03** four assertions of one fact in the vacuity case | **29.1-11 T2** | mechanism |
| 13 | **IN-04** the stray-pin bound enumerates two surface families by hand | 29.1-09 | mechanism (derived from `SPAWN_GRANT_SCAN_PARTS`; 14 → 16) |
| 14 | **IN-05** dead initialiser | 29.1-09 | mechanism |
| 15 | **IN-06** the byte baseline pins an abbreviated SHA | **29.1-11 T3** | mechanism |

**Findings closed by DISCLOSURE rather than by mechanism, with the disclosure's location:**

- **WR-06** — the four vendor citations are point-in-time reads and Copilot CLI's run-time honouring of `model` was not established. Disclosed as residuals **R1, R2, R3** in `agent-factory/packaging/subagent.frontmatter.md`, under `## Host-CLI scope of the model dial`, each carrying an explicit `UNKNOWN - verify` marker and its direction. (Plan 29.1-10.)
- **WR-05, in part** — the code half and the config-reference half closed by mechanism. The packaging-authority half is closed by a **narrowed statement plus a disclosure**: the kit-internal path is named as a fact but not spelled, and the reason is stated in `agent-factory/packaging/subagent.frontmatter.md`'s `model` bullet, in `scripts/model-dial-consistency.test.ts`'s WR-05 section comment, and in `.planning/WINDOWS.md`. See Deviation 1.
- **The surface roster's residuals R-a/R-b/R-c** are disclosed in `scripts/model-dial-consistency.test.ts` beside `SURFACE_COUNT`, each with its direction.

---

## Deviations from Plan

### 1. [Rule 3 — blocking] The plan's central WR-05 must-have collides with a ratified path invariant

- **Found during:** Task 2, running `npx vitest run --exclude '**/scripts/e2e/**'` after Task 1 was already committed. 4 failures in `scripts/check-kit-refs.test.ts`, all reducing to one gate result:

  ```
  [Assertion 1] no agent-factory/config/ refs remain (config now .grugops/factory.config.json)
    FAIL  stray agent-factory/config/ ref(s) — config must be .grugops/factory.config.json:
  agent-factory/packaging/subagent.frontmatter.md:204: … `agent-factory/config/factory.config.json` …
  agent-factory/packaging/subagent.frontmatter.md:206: … `agent-factory/config/factory.config.md` …
  ```

- **Issue:** `scripts/check-kit-refs.js` Assertion 1 (**D-08.1 / SHOME-03 / SC5**) holds **ZERO** `agent-factory/config/` references across the kit scan set, which includes `agent-factory/packaging`. The rule exists because the shared-install rewrite moved per-repository state to `.grugops/`: no kit document may point a reader at a path *inside the kit* to edit. The gate's header states its exclusion-by-not-listing design is **"load-bearing"** and it carries no exemption list. The plan's must-have — *"both shipped documents name both locations"* — cannot be satisfied without breaking it.

  A second signal pointed the same way: the review itself lists *"the one-way `factory.config.md` → `subagent.frontmatter.md` pointer"* under **"Not reported (verified as deliberate)"**. My Task 1 edit had added a reverse pointer.

- **Options weighed:**
  1. *Respell the path to dodge the grep* — **rejected outright.** Hiding a string from a check is the failure class this repository spends rounds closing (plan 29.1-10 rejected the same move for the same reason).
  2. *Amend Assertion 1 with an exemption* — a twenty-two-phase-old zero-tolerance invariant, out of this plan's scope, and the plan's own prohibitions forbid touching install-side contracts.
  3. *Split the obligation by audience* — **chosen.**

- **Fix:** the config field reference (`factory.config.md`, outside the scan set, the developer-facing authority) names **both** locations by path and holds the precedence rule. The packaging authority (shipped kit prose) names the location a user configures, states there are **exactly TWO** locations, and points at the rule's home; the kit-internal path is *named as a fact and not spelled as a path on purpose*, with D-08.1 cited in the bullet itself.

  The oracle was narrowed to match and **gained a case asserting the absence positively** — `occurrences(authority, "agent-factory/config/")` is 0, with a second assertion that `MODELS_CONFIG_CANDIDATE_RELS[1]` really is the kit-internal member so the case cannot go stale. An omission reads as an oversight to the next editor; an assertion carrying its reason reads as a decision.

  The packaging authority is also pinned on the **COUNT**: `first of TWO configuration` must appear exactly once, derived from `MODELS_CONFIG_CANDIDATE_RELS.length`. So a third candidate arriving in the resolver moves both documents — one by a path, one by a cardinality.

- **Consequence, stated plainly:** the plan's must-have *"both shipped documents name both locations rather than one of them"* is **NOT met as written**, and this SUMMARY does not claim it. Recorded in `.planning/WINDOWS.md` as an open `deviation` entry against `agent-factory/packaging/subagent.frontmatter.md`, and routed to human judgment as coverage item **D6**.
- **Files:** `agent-factory/packaging/subagent.frontmatter.md`, `scripts/model-dial-consistency.test.ts`
- **Commit:** `1424f42`

> **CORRECTION 2026-08-20 — finding R2-WR-05, implemented by plan 29.1-17.** Everything above is
> left standing. What it got wrong is one premise, and the correction is recorded here rather than
> substituted into the text. Option 2 above ("*Amend Assertion 1 with an exemption*") was declined
> partly because the config field reference was taken to be **outside** the gate's reach — but
> `install/install.ts:1065` (`copyKit`'s recursive `cpSync` of the whole `agent-factory/` tree,
> `config/` included) ships `agent-factory/config/factory.config.md` to **every installed user**.
> "Outside the SCAN set" and "not shipped kit prose" are different facts; `agent-factory/config`
> was absent from SCAN by **omission**, not by an audience judgment. The user ruled on **D6** in
> round 3: add the directory to the scan with an explicit, argued exemption. Plan **29.1-17**
> implemented that ruling — SCAN gained exactly one entry, the two mentions are exempt by a
> predicate (a self-reference to an existing sibling), the exemption's cardinality is asserted
> two-sided at **2 hits in 1 file**, and Assertion 1's pass line publishes it. Option 3
> ("*split the obligation by audience*") is therefore superseded; the two documents' **roles** —
> field reference holds the rule, packaging authority points at it — are unchanged and still hold.

### 2. [Plan compliance] Task 1 edited a file outside its declared `<files>` list

- **Found during:** Task 1. The task's `<files>` omits `scripts/model-dial-consistency.test.ts`, but its own acceptance criteria require cases there — *"Exactly one document states the precedence rule. Assert by a case…"* — and its `key_links` require the cross-surface oracle to assert the constant's members. The plan's own new-case list places both cases in that file.
- **Fix:** the two WR-05 cases were written into `scripts/model-dial-consistency.test.ts` in Task 1's commit. Recorded rather than smoothed over: the file list and the acceptance criteria disagreed, and the criteria were followed.
- **Commit:** `49ff778`

### 3. [Plan compliance] The IN-03 comment was reworded so the acceptance grep returns 0

- **Found during:** Task 2, running the acceptance greps. The comment explaining the deleted bounds pair *named the two matchers*, so `grep -c 'toBeGreaterThanOrEqual\|toBeLessThanOrEqual'` returned **2** against a criterion of **0**.
- **Fix:** the comment now describes them as "a lower-bound and an upper-bound assertion" and states in the file that their matcher names are deliberately not spelled, so a grep for a surviving bounds assertion returns zero rather than returning the comment. Splitting a literal to dodge the grep was rejected. The full record of what was deleted lives in this SUMMARY and in git history.
- **Commit:** `1424f42`

### 4. [Record, not fix] The reversal mutation reds the shadowing case on the PRESET assertion, not the SOURCE one

- The plan's acceptance criterion anticipated the case going *"RED naming the wrong source"*. It reds on the preset assertion, which is stated first in the case body. Both assertions are present and both are wrong under the reversal; the criterion's expectation about *which message a reader sees first* is what did not hold. Recorded rather than reordered — reordering assertions to match a plan's prediction would be making the transcript agree with the plan instead of the other way round.

### 5. [Record, not fix] Task 2's commit necessarily carries Task 1's D-08.1 repair

- The repair and Task 2's roster work touch the same file, and a commit carrying the reverted document but the un-narrowed oracle case would have been red. `git stash` is prohibited here and interactive staging is unavailable, so the two are one commit with both described in its message. Every commit in this plan is green at HEAD.

---

## Known Stubs

None. No hardcoded empty value, placeholder string, `TODO` or `FIXME` was introduced by this plan.

## Threat Flags

None. This plan changed a config-reader's candidate construction (no new I/O, no new path family, no user-controlled path reaching a syscall), two markdown documents, three test files and one planning document. It introduced no endpoint, auth path, file-access pattern or schema change.

Register dispositions: **T-29.1-27** mitigated (both locations declared once, asserted present in the config reference, precedence in exactly one document, shadowing case proven order-dependent) with the audience-split narrowing disclosed above; **T-29.1-28** mitigated (roster cross-checked against a run-time derivation, both directions proven RED, residuals disclosed with directions, derived set never trimmed); **T-29.1-29** mitigated (full name resolved and pinned, abbreviation proven to red); **T-29.1-30** mitigated (declaration written now, 194 characters measured against the cap); **T-29.1-05** mitigated (`npm run build` + empty `*.js` diff in tasks 1 and 3, plus `npm run freshness` on the final tree); **T-29.1-SC** accepted and asserted by absence across the whole round.

## What Verification Should Re-check

- **D6 is the human-judgment item.** Two ratified contracts collided. The executor preserved the older invariant and narrowed the newer must-have. Whether that is the right disposition — as against amending `check-kit-refs`'s Assertion 1 — is a scope call, and this round exists because a claim was once allowed to stand without one.
- **No requirement checkbox was flipped and Phase 29.1 was NOT marked complete.** MODEL-06 remains `- [ ]` and `Gaps Found`. The verdict belongs to phase verification.
- The plan's own carried-forward note stands: **all 14 unresolved `29.1-EDGE-COVERAGE.json` items remain unresolved.** No SUMMARY in the 07–11 series claims otherwise.
- The roster derivation's residuals R-a, R-b and R-c are open by disclosure, not closed.
- WR-05's behaviour is **unchanged by design**. A user who writes a `models` block into the kit's bundled config while a `.grugops/factory.config.json` exists still gets the zero-config answer. That is now documented in three places; it is not fixed, and it was not meant to be.

## Self-Check: PASSED

All 9 claimed files exist on disk (7 modified + 2 created). All 3 claimed commits (`49ff778`, `1424f42`, `6bc8642`) resolve in `git log`. Every transcript above was captured from a command run in this session; nothing claimed here is unbacked.

---
phase: 29.1-per-role-model-assignment
plan: 13
subsystem: testing
tags: [foundation-guards, canonical-frontmatter, model-assignment, gap-closure, round-3]

requires:
  - phase: 29.1-12
    provides: "`ROOT` resolved at declaration and matched on a path boundary via `ROOT_PATH_BOUNDARY` in scripts/check-foundation-guards.ts — the byte-identity property this plan's new message text must not disturb"
  - phase: 29.1-09
    provides: "the `nonAgentSurfaces` partition out of the one `SPAWN_GRANT_SCAN` composition, and case `(m)`'s slicer/comment-stripper"
provides:
  - "A soundness argument for the stray-pin loop's silent `continue` that states a mechanism MEASURED this session, with the false text it replaces recorded rather than quietly swapped"
  - "`strayPinSkipped` — the non-agent surfaces the stray-pin loop excluded, each with its enumerated refusal code"
  - "A run-summary clause publishing PROBED of DERIVED rather than one 'checked' count, naming every excluded member"
  - "Case `(s)` — the packaging templates' admission measurement, pinned so a later edit cannot restore the false claim"
  - "Case `(s-unadmittable)` — the previously unexercised admission arm, reached by a plant, with `guard_wr05`'s coverage of it observed rather than assumed"
  - "Case `(s-union)` — all three arms of the stray-pin loop exercised by ONE mirror and asserted from ONE verdict"
  - "Case `(m-channel)` — the guard body's advisory-CHANNEL write sites derived from both the `.ts` and the committed `.js`, asserted to be exactly one and to be the section header"
affects: [29.1-14, 29.1-15, 29.1-16, 29.1-17, round-3-verification]

actuals:
  tokens: 21000
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Record the false text you are correcting inside the correction, so the record survives the fix"
    - "An arm no shipped surface reaches is proven by a PLANT, never by an example cited for it"
    - "Publish PROBED of DERIVED — a single 'checked' count cannot be told apart from a complete run"
    - "Hoist a slicer rather than write a second one when a second property is asserted over the same region"

key-files:
  created: []
  modified:
    - scripts/check-foundation-guards.ts
    - scripts/check-foundation-guards.js
    - scripts/check-foundation-guards.test.ts

key-decisions:
  - "The stray-pin arm's excluded members are published as a COUNT plus file identity plus enumerated code — never as a second copy of guard_wr05's reason sentence. One fact, one sentence."
  - "Case (m) is narrowed rather than replaced: it pins the advisory HELPER, and the new sibling (m-channel) pins the advisory CHANNEL. Two separately attributable properties over one hoisted slice."
  - "The exclusion clause is ABSENT entirely when nothing was excluded, rather than present with a zero — asserted by the repaired control."

patterns-established:
  - "Measure first, edit second: when a plan's whole content is that a recorded measurement was never taken, taking it is the first act and its transcript is the artifact"
  - "Assert the plant's own premise through the same authority the production code uses, before asserting anything about the verdict"

requirements-completed: [MODEL-03, MODEL-05]

coverage:
  - id: D1
    description: "The stray-pin loop's soundness argument states the mechanism a session measurement found: both packaging templates ADMIT (keys kind, tier — both CANONICAL_SCHEMA members) and are skipped by the no-model-key arm, never by the admission arm"
    requirement: MODEL-03
    verification:
      - kind: unit
        ref: "scripts/check-foundation-guards.test.ts#(s) the packaging templates are ADMITTED and skipped by the no-model-key arm — the measured mechanism, pinned"
        status: pass
      - kind: other
        ref: "node --input-type=module admit() over both packaging templates against the committed scripts/canonical-frontmatter.js"
        status: pass
    human_judgment: false
  - id: D2
    description: "The previously unexercised admission arm is reached by a planted unadmittable non-agent surface, which is excluded from the probed count, named in the verdict with its enumerated code, and independently reds guard_wr05 by name"
    requirement: MODEL-03
    verification:
      - kind: unit
        ref: "scripts/check-foundation-guards.test.ts#(s-unadmittable) an unadmittable non-agent surface is excluded from the probed count and named by guard_wr05"
        status: pass
      - kind: integration
        ref: "CHECK_ROOT=<planted mirror> node scripts/check-foundation-guards.js — 1 CHECK(S) FAILED, attributed to guard_wr05"
        status: pass
    human_judgment: false
  - id: D3
    description: "The run summary publishes probed-of-derived rather than a single 'checked' count, and the distinction is observable — proven by a mutation that counts the excluded member as probed"
    requirement: MODEL-05
    verification:
      - kind: unit
        ref: "scripts/check-foundation-guards.test.ts#(s-union) one mirror carrying a stray pin, a clean surface and an unadmittable surface reports all three facts"
        status: pass
      - kind: unit
        ref: "scripts/check-foundation-guards.test.ts#the non-agent surface denominator is DERIVED by partitioning the ONE kit composition, not hand-listed"
        status: pass
    human_judgment: false
  - id: D4
    description: "The advisory CHANNEL, not one spelling of it, is forbidden inside guard_model_assignment — exactly one process.stdout.write in both the source and the committed twin, and it is the section header"
    requirement: MODEL-05
    verification:
      - kind: unit
        ref: "scripts/check-foundation-guards.test.ts#(m-channel) guard_model_assignment's ONLY advisory-channel write is its section header"
        status: pass
      - kind: unit
        ref: "scripts/check-foundation-guards.test.ts#(m) neither degraded branch reports through the advisory HELPER — guard_model_assignment makes NO warn() call"
        status: pass
    human_judgment: false

duration: 12 min
completed: 2026-08-20
status: complete
---

# Phase 29.1 Plan 13: Two justifications made either true or asserted Summary

**R2-WR-03's soundness argument now states a mechanism measured in session rather than one the measurement contradicts, its previously unexercised admission arm is proven by a plant instead of by an example that never takes it, its denominator distinguishes PROBED from DERIVED, and R2-IN-01's case now forbids the advisory CHANNEL rather than one spelling of it.**

## Performance

- **Duration:** 12 min
- **Tasks:** 2 of 2
- **Files modified:** 3
- **Commits:** 2
- **Guard suite:** 257 → 261 passing (+4 cases: `(s)`, `(s-unadmittable)`, `(s-union)`, `(m-channel)`)

## Accomplishments

### Task 1 — the stray-pin arm's silence becomes an assertion, and its denominator becomes honest

- The paragraph above the stray-pin loop was rewritten to state the mechanism a session
  measurement found, and it RECORDS the false text it replaces rather than quietly swapping it.
- The loop collects `strayPinSkipped` — one entry per excluded member, `<file>: [<code>]` — in the
  same shape the adapter loop above already uses. The refusal REASON is not restated; `guard_wr05`
  owns that sentence.
- The run summary publishes probed-of-derived with an exclusion clause that is absent entirely when
  nothing was excluded.
- Three new cases; the derived-denominator control repaired in lockstep.

### Task 2 — case (m) forbids the advisory CHANNEL, not one spelling of it

- `sliceGuardBody`/`codeOfGuardBody` hoisted out of case `(m)` **unchanged** so both properties read
  one body. No second slicer was written.
- `MODEL_GUARD_WRITE_SITES` derives every `process.stdout.write` occurrence in the sliced,
  comment-stripped body, carrying the offending LINE (so a failure quotes it) and a 200-byte window
  (so a multi-line call whose text sits on the next line is still identifiable).
- Case `(m-channel)` asserts exactly one site in **both** the `.ts` and the committed `.js`, and that
  it is the section header — matched by the stable fragment `[guard_model_assignment]`, not a line
  number.

## The measurement this plan exists for (recorded verbatim)

Run in this session against the **committed** `scripts/canonical-frontmatter.js`, before any edit:

```
$ node --input-type=module -e "import { admit } from './scripts/canonical-frontmatter.js'; ..."
agent-factory/packaging/slash-command.template.md => ADMITTED   keys: [ 'kind', 'tier' ]  has model key: false
agent-factory/packaging/subagent.frontmatter.md => ADMITTED   keys: [ 'kind', 'tier' ]  has model key: false
```

`kind` and `tier` are both members of `CANONICAL_SCHEMA` (`scripts/canonical-frontmatter.ts:177-188`).
**Both templates admit.** They are skipped one line later by the `!parsed.value.has("model")` arm,
never by the `!parsed.ok` arm the comment cited them for. The recorded justification named the wrong
mechanism, and the arm it rested on was unexercised on this tree.

### The false text, and the text that replaced it

**Before** (`scripts/check-foundation-guards.ts`, above the stray-pin loop):

> The packaging templates are the reason this paragraph is load-bearing rather than defensive: they
> carry a `kind:`/`tier:` frontmatter the canonical adapter schema does not admit, so they reach this
> arm and are skipped by it.

**After** — the corrected paragraph quotes the sentence above verbatim as the text it replaces,
prints the two-line measurement transcript, then states:

> `kind` and `tier` are BOTH members of CANONICAL_SCHEMA, so both templates ADMIT. The arm they take
> is the NO-`model`-KEY arm one line further down, never the admission arm — neither declares a
> `model` key. NO SURFACE THIS TREE SHIPS REACHES THE ADMISSION ARM AT ALL, which is exactly why its
> silence is now backed by a PLANTED case rather than by an example that never takes it.

The true half of the old argument is kept unchanged: `guard_wr05` does name an unadmittable scan file
with its enumerated code and does fail the gate closed, and a second sentence for one fact is the
duplication this gate's conventions refuse.

## The planted unadmittable construct, and the code `admit()` returned

Plant: an unknown key added to a packaging template's frontmatter.

```
---
kind: packaging
tier: core
stray-unknown-key: planted
---
```

Confirmed at the module level **before** anything was asserted about the verdict:

```
ADMIT: REFUSED code=[unknown-key] reason=line 4: `stray-unknown-key` is not one of the 10 keys the
canonical schema admits (allowed-tools, argument-hint, coordinator, description,
disable-model-invocation, kind, model, name, tier, tools); an unknown key is refused rather than
ignored, because an ignored key is a second place a document can hide a value
```

## Pre-fix and post-fix run summaries for the SAME plant

**PRE-FIX** (committed `.js` at `b0d605e`, planted mirror). The guard's model-assignment section
**PASSED**, publishing every derived member as "checked" while one of them was never read:

```
  PASS  model assignment: 17 committed adapter(s) under .claude/agents compared against a resolution
  recomputed for 17 derived role stem(s); preset "none" from agent-factory/config/factory.config.json;
  distinct aliases resolved: inherit; 16 non-agent adapter surface(s) checked for a stray pin, none found
```

**POST-FIX**, same plant, same mirror:

```
  PASS  model assignment: 17 committed adapter(s) under .claude/agents compared against a resolution
  recomputed for 17 derived role stem(s); preset "none" from agent-factory/config/factory.config.json;
  distinct aliases resolved: inherit; 15 of 16 non-agent adapter surface(s) probed for a stray pin,
  none found; 1 excluded from the probed count as UNADMITTABLE and reported by guard_wr05, not here:
  agent-factory/packaging/slash-command.template.md: [unknown-key]
```

## The `guard_wr05` finding line for the planted surface (verbatim)

```
agent-factory/packaging/slash-command.template.md: frontmatter is NOT in the canonical form
[unknown-key] — line 4: `stray-unknown-key` is not one of the 10 keys the canonical schema admits
(allowed-tools, argument-hint, coordinator, description, disable-model-invocation, kind, model, name,
tier, tools); an unknown key is refused rather than ignored, because an ignored key is a second place
a document can hide a value. An unreadable adapter cannot be reported on, so it is NEVER read as
"carries no grant"
```

The gate exits non-zero and attributes the failure to it:

```
  FAIL  WR-05 coordinator-spawn-grant violation:
...
== Result ==
1 CHECK(S) FAILED
```

## The union case's three asserted facts, as printed

One mirror carrying all three states at once — `model: opus` on both distribution forms of one
skill (arm 1), `agent-factory/packaging/subagent.frontmatter.md` untouched (arm 2), and the
`unknown-key` plant (arm 3) — produces ONE verdict carrying all three:

```
model assignment: 2 of the 16 non-agent adapter surface(s) this kit ships declare a `model` key. ...
    .claude/skills/grugops-gate/SKILL.md: `opus`
    skills/gate/SKILL.md: `opus`

  The run that produced these findings: model assignment: ...; 15 of 16 non-agent adapter surface(s)
  probed for a stray pin, 2 found and named above; 1 excluded from the probed count as UNADMITTABLE
  and reported by guard_wr05, not here: agent-factory/packaging/slash-command.template.md: [unknown-key]
```

- **Fact 1** — the stray pin, named by file and by value (both forms).
- **Fact 2** — `15 of 16`: probed is exactly one below derived.
- **Fact 3** — the excluded member named with its code.

## Mutation transcripts

### Mutation A — case `(s)`'s own premise inverted (`["kind","tier"]` → `["kind"]`)

```
FAIL > (s) the packaging templates are ADMITTED and skipped by the no-model-key arm — the measured
mechanism, pinned
AssertionError: agent-factory/packaging/slash-command.template.md: the admitted key list is the
measurement the corrected paragraph records: expected [ 'kind', 'tier' ] to deeply equal [ 'kind' ]
```

Reverted.

### Mutation B — the loop counts the excluded member as probed (subtract nothing)

`(s-union)` goes RED and its message names BOTH numbers; the mutated verdict is visibly
self-contradicting (`16 of 16 probed ... ; 1 excluded`):

```
AssertionError: the probed count must differ from the derived count by exactly the one member that
could not be read: expected '  FAIL  model-assignment violation:…' to contain
'15 of 16 non-agent adapter surface(s)…'
+   The run that produced these findings: ...; 16 of 16 non-agent adapter surface(s) probed for a
+   stray pin, 2 found and named above; 1 excluded from the probed count as UNADMITTABLE and reported
+   by guard_wr05, not here: agent-factory/packaging/slash-command.template.md: [unknown-key]
```

Reverted and rebuilt.

### Mutation C — a direct advisory write planted inside a degraded branch (Task 2, both halves)

Planted `process.stdout.write("  WARN  model assignment: the configuration was refused\n");` inside
the configuration-refused degraded branch, rebuilt, then ran both cases.

**`(m-channel)` — RED**, naming the found count, the required count, and quoting every offending line:

```
× (m-channel) guard_model_assignment's ONLY advisory-channel write is its section header
AssertionError: check-foundation-guards.ts: guard_model_assignment must contain EXACTLY 1
process.stdout.write call and this body contains 2. The one legitimate write is the section header;
every other write on this channel puts a line on stdout WITHOUT incrementing FAILS, which is a
refusal that cannot fail a build. Offending lines:
    process.stdout.write(
    process.stdout.write("  WARN  model assignment: the configuration was refused\n");
: expected 2 to be 1
```

**`(m)` — GREEN, unchanged** on the same mutated build:

```
Tests  1 passed | 260 skipped (261)
```

That contrast is the whole content of the finding: the old probe could not see the restored
fail-open. Mutation reverted, rebuilt; `git status --porcelain` shows no leftover.

## Case `(m)`'s narrowed claim — before and after

| | Text |
|---|---|
| **Before (title)** | `(m) neither degraded branch reports through the advisory channel — guard_model_assignment makes NO warn() call` |
| **After (title)** | `(m) neither degraded branch reports through the advisory HELPER — guard_model_assignment makes NO warn() call` |
| **Before (block comment)** | "What CAN be tested is the property a single-arm promotion would break: **that this guard reaches for the advisory channel nowhere at all.**" |
| **After (block comment)** | "…the property a single-arm promotion would break: **that this guard makes no call to the advisory HELPER `warn()`.**" plus a recorded paragraph stating the claim used to be wider than the probe, that the channel is `process.stdout.write`, that `warn()` is one spelling of it, and that `(m-channel)` forbids the channel. |
| **Before (assertion message)** | "`guard_model_assignment must make no warn() call — warn() is documented advisory and does NOT increment FAILS, so a degraded branch reported through it is a refusal that cannot fail a build`" |
| **After (assertion message)** | "`guard_model_assignment must make no warn() HELPER call — … This case pins the HELPER, which is ONE SPELLING of the advisory channel; the channel itself is pinned by (m-channel)`" |

Case `(m)`'s two premise assertions are unchanged and still present: the sliced body reaches the
stray-pin arm (`nonAgentSurfaces`), and the probe can see a call at all (`fail(`).

## Existing cases repaired

| Case | What changed | Why |
|---|---|---|
| `the non-agent surface denominator is DERIVED by partitioning the ONE kit composition, not hand-listed` | Its run-summary assertion moved from `<N> non-agent adapter surface(s) checked for a stray pin, none found` to `<N> of <N> non-agent adapter surface(s) probed for a stray pin, none found`, and a NEW assertion was added that the section carries no exclusion clause at all. | Its premise — an unmutated mirror excludes nothing and finds no stray pin — is still true and stays asserted; only the sentence it matches changed. It was STRENGTHENED, not weakened: the old wording could not express "nothing was excluded", and the new one asserts the equality plus the clause's total absence. |
| `(m)` | Title, block comment and assertion message narrowed to the advisory HELPER; the local `slice`/`codeOf` helpers hoisted to describe scope **byte-unchanged** and now shared with `(m-channel)`. | Its stated claim was wider than its probe (R2-IN-01). The probe itself is untouched and the case still asserts `code.includes("warn(") === false`. |

Case `(l)` was NOT changed — it asserts the stray-pin FINDING (`2 of the 16 … declare a \`model\` key`),
not the run summary, and that half is correct.

## Deviations from Plan

**1. [Rule 1 - Bug] The first spelling of the exclusion clause had a number-agreement defect**

- **Found during:** Task 1, on the first post-fix transcript.
- **Issue:** the clause read `1 could NOT be admitted and are therefore excluded…` — plural verb on a
  singular count. A guard verdict that reads as broken English in its single-item case is a verdict a
  reader distrusts.
- **Fix:** reworded to `${n} excluded from the probed count as UNADMITTABLE and reported by
  guard_wr05, not here: …`, which is agreement-free for every `n`.
- **Files modified:** `scripts/check-foundation-guards.ts`, `scripts/check-foundation-guards.js`
- **Verification:** post-fix transcript re-captured; byte-identity `diff` still empty.
- **Commit:** `c69825a` (same commit — caught before the task was committed)

**Total deviations:** 1 auto-fixed (1 × Rule 1). **Impact:** none on scope; message text only.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or trust-boundary schema change. The
plan's four registered threats (T-29.1-36 repudiation via the "checked" count, T-29.1-37 tampering
via a silently skipped unadmittable surface, T-29.1-38 elevation via a restored direct advisory
write, T-29.1-39 repudiation via a false soundness argument) are each mitigated by a case named in
the coverage block above. T-29.1-05 is mitigated by `npm run freshness` (49/49 fresh) plus
`npm run build && git diff --exit-code -- '*.js'` (empty). T-29.1-SC holds: zero packages installed,
`package.json` byte-unchanged.

## Known Stubs

None.

## Verification

| Check | Result |
|---|---|
| `npx vitest run scripts/check-foundation-guards.test.ts` | **261 passed** (was 257 at plan start) |
| `node scripts/check-foundation-guards.js` | exit **0**; stray-pin clause reads `16 of 16 … probed for a stray pin, none found` (probed == derived) |
| `diff <(node scripts/check-foundation-guards.js) <(CHECK_ROOT=. node scripts/check-foundation-guards.js)` | **empty** — 29.1-12's byte identity undisturbed |
| `node scripts/generate-role-adapters.js && git diff --exit-code -- .claude/agents/` | **empty** |
| `npm run freshness` | **49/49 committed .js fresh** |
| `npm run typecheck` | exit **0** |
| `npm run build && git diff --exit-code --name-only -- '*.js'` | **empty** |
| `npx vitest run --exclude '**/scripts/e2e/**'` | **55 files, 2338 passed, 2 skipped** |
| `git diff --name-only -- install/ package.json scripts/measure-cost.* scripts/model-tiers.ts agent-factory/` | **empty** |
| `npm test` | **not run** (project rule — live claude-CLI e2e lane) |

## Issues Encountered

None.

## Next Phase Readiness

Ready for `29.1-14`. R2-WR-03 and R2-IN-01 are closed; the remaining round-3 findings are carried by
plans 29.1-14 through 29.1-17.

## Self-Check: PASSED

- `scripts/check-foundation-guards.ts` — FOUND
- `scripts/check-foundation-guards.js` — FOUND
- `scripts/check-foundation-guards.test.ts` — FOUND
- commit `c69825a` — FOUND in `git log`
- commit `88e55aa` — FOUND in `git log`
- Both plan-level `<verification>` command sets re-run at HEAD after the final commit; all pass (table above).

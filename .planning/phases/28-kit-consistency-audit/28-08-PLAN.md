---
phase: 28-kit-consistency-audit
plan: 08
type: execute
wave: 8
depends_on: ["28-02", "28-07"]
files_modified:
  - scripts/canonical-frontmatter.ts
  - scripts/canonical-frontmatter.js
  - scripts/canonical-corpus.ts
  - scripts/canonical-corpus.js
  - scripts/canonical-corpus.test.ts
  - scripts/frontmatter.test.ts
  - docs/audit/28-residual-sizing.md
  - docs/audit/28-disposition-register.md
autonomous: false
requirements: [AUDIT-01]

estimate:
  tokens: 145000
  raw_tokens: 97000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "CONDITIONAL PLAN. It runs only if docs/audit/28-residual-sizing.md states REQUIRED for the byte-round-trip adjacency residual, and only after the blocking checkpoint at the end of 28-02 was answered 'approved — 28-08 runs'. If the sizing says NOT REQUIRED, this plan is not executed and the phase closes at 28-07."
    - "D-21 is honoured as taken: the byte-round-trip adjacency residual is fixed in this phase with a red-team pass rather than deferred, and the concern it was taken over is reproduced verbatim in this plan so the record shows the tradeoff was seen and accepted rather than missed."
    - "D-22's full Phase 27 closure doctrine applies and all four parts are required, not three: a STRUCTURAL fix rather than another predicate widening; parser-oracle fuzz against a real YAML loader; at least TWO independent red-teams; and the executor reproducing the bypass itself, before and after. This is the bar that finally closed round 12 and the only bar this module has ever yielded to."
    - "A green suite is NOT evidence here. One red team is what rounds 10 and 11 each had, and each of those shipped a new regression inside its own fix. The suite passing is a floor and is stated as a floor in the summary, never as proof that no bypass remains."
    - "The fix is STRUCTURAL in the D-64 sense: it either makes the canonical form's allow-list cover the adjacency by construction, or it refuses the shape by name. It does not add a twelfth special case to a parser whose round-12 lesson was to stop widening and refuse everything outside a canonical form."
    - "Non-vacuity is proven as a PAIR, per D-64's vacuity trap 2: that the reader still ADMITS the live kit at its full measured count, and that every reproduced bypass shape is REFUSED by a named code with the refusal text printed. Admitting everything and refusing everything are both trivially achievable, and only the two together say the grammar discriminates."
    - "Behaviour preservation is proven by a value map derived at run time on BOTH sides — arms changed, values changed, new refusals, and both corpus sizes measured in the same session and compared to each other rather than to a literal."
    - "The verification harness's own premise is ASSERTED rather than assumed. It produced a false result in six instances across four straight rounds in Phase 27, so the harness is checked before its verdict is believed."
    - "The finding and its disposition are recorded in docs/audit/28-disposition-register.md, and the register gate is re-run GREEN in the same commit with the affected row's findings count updated — the two D-03 equalities do not get to drift because a late plan added a row."
    - statement: "The adjacency reproduction is re-run against the shipped build after the fix, by the executor, not read from a prior transcript."
      verification: backstop
  artifacts:
    - scripts/canonical-frontmatter.ts
    - scripts/canonical-corpus.ts
    - scripts/canonical-corpus.test.ts
    - scripts/frontmatter.test.ts
  key_links:
    - "docs/audit/28-residual-sizing.md REQUIRED verdict -> this plan: the single input that authorizes the adversarial round."
    - "scripts/canonical-frontmatter.ts admit() -> every spawn-grant verdict in the repository: a bypass here is a grant the guards never see."
    - "scripts/canonical-corpus.ts -> canonical-corpus.test.ts: the round-by-round bypass corpus, which this plan extends with its own reproduced shapes rather than replacing."
    - "scripts/frontmatter.test.ts loader column -> the differential oracle: every new row names the real YAML loader and its version it was measured against."
  prohibitions:
    - "No predicate is widened to accommodate the adjacency; the fix is structural or it is a named refusal."
    - "No new special case is added to the admission reader's character-level handling."
    - "No claim of closure rests on the test suite passing."
    - "No red team is counted unless it was independent of the fix's author."
    - "No prior transcript is reused in place of a reproduction run in this session."
    - "No refusal is added that reds content a real YAML loader accepts, unless that divergence is measured, named, and shown to be pre-existing or safe-direction."
    - "The register's two D-03 equalities are not left broken by the added finding row."

user_setup: []
---

<objective>
Run the adversarial round this audit phase took on deliberately, at the bar that is the only one this
module has ever yielded to.

**This plan is conditional.** It executes only if `docs/audit/28-residual-sizing.md` states REQUIRED
for the byte-round-trip adjacency residual — meaning 28-02 measured that closing it requires editing
`scripts/canonical-frontmatter.ts` or `scripts/frontmatter.ts` — and only after the blocking
checkpoint at the end of 28-02 was answered `approved — 28-08 runs`. If the sizing says NOT REQUIRED,
this plan is not executed and the phase closes at 28-07.

Per D-21 the residual is fixed here rather than deferred, and the decision was taken over a stated
objection which is reproduced verbatim below. Per D-22 the bar is the full Phase 27 closure doctrine:
a structural fix rather than another predicate widening, parser-oracle fuzz against a real YAML
loader, at least two independent red teams, and the executor reproducing the bypass itself both before
and after. That is the bar that finally closed round 12, and one red team is what rounds 10 and 11
each had — each of which shipped a new regression inside its own fix.

The governing constraint is D-64's: round 12 stopped widening the parser and defined a canonical form,
refusing everything outside it. A fix here that adds a twelfth special case is not a fix; it is round
13 wearing a different name.

Purpose: close the last fail-safe residual that reaches a safety-critical parser, at a bar that has
actually held.
Output: a structural fix, an extended bypass corpus, new differential rows, two independent red-team
records, and a register finding closed with the gate green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-kit-consistency-audit/28-CONTEXT.md
@.planning/phases/28-kit-consistency-audit/28-PATTERNS.md
@CLAUDE.md
</context>

## D-21 — the decision, with the concern it was taken over, verbatim

D-21 (USER DECISION, taken against a stated concern — recorded with the concern intact): if the
byte-round-trip adjacency residual requires editing the canonical admission reader
(`scripts/canonical-frontmatter.ts` / `scripts/frontmatter.ts`), it is fixed in this phase with a
red-team pass, not deferred.

> **Concern raised during discussion and overridden by the user:** that module took Phase 27
> **twelve gap-closure rounds**; rounds 10 and 11 each shipped a **new regression inside their own
> fix**; D-64's entire point at round 12 was to stop widening the parser and refuse everything
> outside a canonical form. Phase 27 closed it by **named user override**, with KIT-03 and SPAWN-04
> still `[ ]`. Phase 30 carries a red-team budget as scope; Phase 28 does not. **The accepted cost:
> this audit phase now carries an adversarial round.**

## Artifacts this plan produces

No new file. This plan edits the admission reader, extends the bypass corpus and the differential
harness, and records one finding. The phase-wide artifact inventory is in `28-01-PLAN.md`.

## D-22's four parts, as an executable checklist

Every part is required. Three of four is what rounds 10 and 11 delivered.

| Part | What it means here | Where it is satisfied |
|---|---|---|
| Structural fix | The canonical form's allow-list covers the shape by construction, or the shape is refused by a named code. Not a new character-level special case. | Task 2 |
| Parser-oracle fuzz | A generated corpus handed to a real YAML loader in one process, whose verdict is the expected value for every cell, with the corpus digest printed. | Task 3 |
| Two independent red teams | Two adversarial passes by agents that did not author the fix, each attempting to bypass it and each recording what it tried. | Task 4 |
| Executor self-reproduction | The executor constructs and runs the bypass itself, before the fix and after — never reading a prior transcript in place of running it. | Tasks 1 and 4 |

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the bypass yourself, before touching anything</name>
  <files>docs/audit/28-residual-sizing.md</files>
  <read_first>
    - `docs/audit/28-residual-sizing.md` § `Residual 2` — the REQUIRED verdict and 28-02's reproduction.
      Read it to know what was measured, then measure it again yourself; this repository's record is
      that a prior transcript and a fresh run give different answers often enough that reuse is a
      defect.
    - `scripts/canonical-frontmatter.ts` — the module header, `admit()` at line 538,
      `admitUnderProofWeakeningOnly()` at line 815, `REFUSAL_CODES` at line 93, `CANONICAL_SCHEMA` at
      line 177, `GRANT_KEYS` at line 191, `PLAIN_SCALAR_ALPHABET` at line 239, `LINE_PRODUCTIONS` at
      line 264, `REFUSED_NODE_SIGILS` at line 330.
    - `scripts/canonical-corpus.ts` lines 12-29 — the two-half non-vacuity pair, the corpus-coverage
      rule asserted two-sided over the rounds, and `CORPUS_COUNT`.
    - `scripts/frontmatter.test.ts` around lines 1719, 1908, 2100, 2390 and 2609 — the differential rows
      and their loader column naming the real YAML loader and its version.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — the round-by-round
      bypass record, in full. It is the map of what has already been tried and what each round's fix
      broke.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md` — D-64 in particular.
  </read_first>
  <action>
Do not begin by reading the fix. Begin by reproducing the bypass.

Construct the adjacency input yourself from the description in `docs/audit/28-residual-sizing.md`, run
the **committed** `scripts/canonical-frontmatter.js` over it, and record exactly what it returns —
the arm taken, the value, the refusal code if any. Then hand the identical bytes to a real YAML loader
in a separate process and record what it returns. Use the loader form already established in
`scripts/frontmatter.test.ts`, naming the loader and its version.

State the divergence precisely: what the module concludes, what the loader concludes, and which
direction the divergence runs. A module-refuses-loader-accepts divergence is safe-direction and may be
pre-existing; a module-accepts-loader-refuses or a module-sees-no-grant-loader-sees-a-grant divergence
is the unsafe direction and is the bypass.

Then assert the harness's own premise before believing its verdict. Confirm you are running the
committed `.js` and not a stale build; confirm the input you constructed is the bytes you intended by
printing them with their byte offsets; confirm the loader process actually ran and did not fail
silently. In Phase 27 the harness produced a false result in six instances across four straight rounds,
and every one of them was found by checking the harness rather than the code.

Record the reproduction in `docs/audit/28-residual-sizing.md` under a new
`## Residual 2 — reproduction at fix time (28-08)` heading, alongside 28-02's earlier one. If your
reproduction disagrees with 28-02's, that disagreement is the finding and it is reported before any
fix is attempted — do not reconcile it by adjusting either transcript.

Only after the reproduction is recorded, read the region of `scripts/canonical-frontmatter.ts` the
bypass reaches, and write down which set the failing predicate ENUMERATES, at WHICH POSITIONS it is
asked, and what its INPUT is ASSEMBLED from. Those three questions are the ones that closed rounds 6,
10 and 11 respectively, and answering them in writing before proposing a fix is what prevents the
fix from being another character-level special case.
  </action>
  <verify>
    <automated>grep -c 'Residual 2 — reproduction at fix time (28-08)' docs/audit/28-residual-sizing.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/audit/28-residual-sizing.md` contains a `## Residual 2 — reproduction at fix time (28-08)`
      section holding: the constructed input printed with byte offsets, the module's verdict, the real
      YAML loader's verdict with the loader and version named, and the divergence direction.
    - The section states explicitly whether it agrees with 28-02's earlier reproduction, and if not,
      states the disagreement as a finding.
    - The three structural questions — which set the predicate enumerates, at which positions it is
      asked, what its input is assembled from — are each answered in writing in that section.
    - The harness premise checks are recorded: which build was run, that the input bytes were printed
      and matched intent, and that the loader process exited normally.
    - No file under `scripts/` has been modified at the end of this task; `git diff --stat scripts/` is
      empty.
  </acceptance_criteria>
  <done>
    The bypass has been reproduced in this session by the executor, against the committed build, with a
    real loader's verdict beside it and the harness's own premise asserted.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The structural fix, and the two-half non-vacuity pair</name>
  <files>scripts/canonical-frontmatter.ts, scripts/canonical-corpus.ts, scripts/canonical-corpus.test.ts</files>
  <behavior>
    - RED first: the reproduced adjacency shape is added to `scripts/canonical-corpus.ts` as a new row
      and the replay case fails against the current build, naming the shape.
    - GREEN after: the same row is refused by a NAMED code with the refusal text printed, or is admitted
      with the same value the loader produces — whichever the structural answer is. Which one it is must
      be decided by what the canonical form says, not by which is easier to make pass.
    - Non-vacuity half one: the reader still ADMITS the live kit at its full measured count, derived at
      run time on both sides rather than compared against a literal.
    - Non-vacuity half two: every corpus row from every round is still refused by its named code with
      its refusal text printed. A fix that broke an earlier round's closure is caught here.
    - Value map: arms changed, values changed and new refusals are each derived at run time before and
      after, and both corpus sizes are measured in the same session and compared to each other.
    - Zero new refusals on loader-accepted content. Any module-refuses-loader-accepts row must be shown
      to pre-date this change.
    - The corpus-coverage rule still holds two-sided over the rounds, and `CORPUS_COUNT` is updated in
      the same file that owns the data.
  </behavior>
  <read_first>
    - Task 1's recorded reproduction and its three structural answers.
    - `scripts/canonical-frontmatter.ts` — the specific region the bypass reaches, plus `REFUSAL_CODES`
      and `CANONICAL_SCHEMA` in full, so a new refusal code is added to the enumeration rather than
      invented at a call site.
    - `scripts/canonical-corpus.ts` in full — the corpus, `CORPUS_COUNT`, and the coverage rule.
    - `scripts/canonical-corpus.test.ts` — the replay harness and its printed-refusal-text contract.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md` — D-64's canonical-form
      argument and its two vacuity traps.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — specifically what
      rounds 10 and 11 each broke inside their own fixes, so the same shape is not repeated.
  </read_first>
  <action>
Add the reproduced shape to `scripts/canonical-corpus.ts` as a new row carrying its round attribution,
and update `CORPUS_COUNT` in that same file. Run the replay case and watch it fail. That RED is the
precondition for touching the reader.

Then make the fix structural. Decide, from the canonical form rather than from convenience, whether
the adjacency shape is inside the canonical form or outside it. If it is inside, the allow-list must
cover it by construction — one production, not a special case bolted beside one. If it is outside, it
is refused by a named code added to `REFUSAL_CODES`, and the refusal fires at the point where the form
is decided rather than at a later position that happens to notice it.

Do not add a character-level special case. Round 12's entire lesson was to stop widening the parser
and refuse everything outside a canonical form, and a twelfth widening is round 13 with a new name. If
the only fix you can find is a widening, stop and record that as the finding rather than shipping it —
that outcome is a legitimate result of this task and is more valuable than a fix that does not hold.

If the structural answer requires deleting a second grammar over the same bytes, delete it rather than
keeping it in sync. That move — one format-aware authority per predicate, the weaker duplicate deleted
rather than frozen — is the one this module has repeatedly needed.

Then prove non-vacuity as a pair. Measure that the reader still admits the live kit, deriving the
corpus at run time on both sides and comparing the two derived numbers to each other rather than to a
literal, because a hard-coded count was independently measured wrong in this repository once already.
Measure that every corpus row from every round is still refused by its named code with the text
printed.

Produce the value map: arms changed, values changed, new refusals, both corpus sizes. Every number
derived in this session on both sides. Zero new refusals on loader-accepted content is the bar; any
module-refuses-loader-accepts row must be demonstrated to pre-date this change, not argued to be
harmless.

Run `npm run build`, commit the `.js`, and run
`npx vitest run --exclude '**/scripts/e2e/**'`. Do NOT run bare `npm test`. State in the SUMMARY that
the suite passing is a floor and not proof that no bypass remains.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' scripts/canonical-corpus.test.ts scripts/frontmatter.test.ts &amp;&amp; node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - The new corpus row was RED against the pre-fix build and is GREEN after; both transcripts are
      recorded with the refusal code or admitted value printed.
    - The fix is structural: the diff either adds a production to the canonical form's allow-list or
      adds a named code to `REFUSAL_CODES` and fires it where the form is decided. The SUMMARY names
      which, and quotes the diff hunk that makes it structural rather than special-cased.
    - Non-vacuity half one: the live-kit admission count is derived at run time before and after and the
      two numbers are equal; both are printed.
    - Non-vacuity half two: every corpus row is refused by its named code with its text printed; the
      count of rows replayed equals `CORPUS_COUNT`, asserted two-sided.
    - The corpus-coverage rule still passes two-sided over the rounds, failing by name on a round with
      zero rows.
    - The value map reports arms changed, values changed and new refusals, each derived in session, and
      both corpus sizes derived in session and equal to each other.
    - New refusals on loader-accepted content is `0`, or every such row is demonstrated pre-existing
      with the transcript that shows it.
    - `node scripts/check-foundation-guards.js` exits 0 and its output is compared against a pre-fix
      capture, with any difference explained.
    - `npm run freshness` exits 0 and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">
    An edit to the admission reader is revertible in one commit, but it is costly: this module gates
    every spawn-grant verdict in the repository, and Phase 27's record shows that changes to it have
    twice shipped a regression inside their own fix. Costly, not one-way — no decision checkpoint is
    inserted, because D-21 already made the decision and the blocking checkpoint at the end of 28-02
    already confirmed it against the measurement.
  </reversibility>
  <done>
    The bypass shape is closed by a structural change, the corpus is extended, and both halves of the
    non-vacuity pair are measured — neither admitting everything nor refusing everything.
  </done>
</task>

<task type="auto">
  <name>Task 3: Parser-oracle fuzz against a real YAML loader</name>
  <files>scripts/frontmatter.test.ts</files>
  <read_first>
    - `scripts/frontmatter.test.ts` around lines 1719, 1908, 2100, 2390 and 2609 — the existing
      differential rows, their loader column, and the corpus-digest contract.
    - The existing generated-corpus harness in that file: how cells are generated from data axes, handed
      to the loader in ONE process, and how loader-rejected cells are printed and skipped.
    - How disagreements are asserted EQUAL to named safe-direction exemptions rather than as a subset,
      and how the unsafe directions are asserted empty INDEPENDENTLY of the exemption machinery.
    - Task 2's fix and the axis it touched.
  </read_first>
  <action>
Extend the generated corpus with an axis covering the adjacency shape's neighbourhood — not the single
reproduced input, but the family it belongs to. Generate the cells from data axes as the existing
harness does, hand the whole corpus to a real YAML loader in one process, and take the loader's verdict
as the expected value for every cell.

Follow the established assertion structure exactly, because it is the structure that made the previous
round's completeness claim a measurement rather than a claim: loader-rejected cells are printed and
skipped with their count; disagreements are asserted EQUAL to the named safe-direction exemptions and
never as a subset; the unsafe directions are asserted empty INDEPENDENTLY of the exemption machinery;
and a corpus digest is printed so an outside transcript's same-corpus claim is a measurement.

Compare against the pre-fix build. Run the same generated corpus against the build from before task 2
and record its cell count, skip count, disagreement count and digest, then against the current build.
State how many cells moved and in which direction. Zero cells moving in the unsafe direction is the
bar; cells moving from unsafe to safe is the fix working.

Do not use a hard-coded corpus size anywhere in the assertions. Both sides derive their own size in
session and the two derived numbers are compared to each other — a hard-coded count in this exact
harness family was independently measured wrong once already in this repository.

If the fuzz surfaces a shape task 2's fix does not cover, that is a finding and it is recorded before
any further edit. Do not close it with a second widening inside this plan; a second shape means the
fix was not structural and the structural question is reopened.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/frontmatter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new axis is present and the generated corpus size is derived in session on both sides, with
      the two numbers printed and equal.
    - The corpus digest is printed for both the pre-fix and post-fix runs.
    - Loader-rejected cells are printed with their count and skipped, not silently dropped.
    - Disagreements are asserted EQUAL to the named exemption set, and the unsafe directions are
      asserted empty by an assertion that does not consult the exemption machinery.
    - The SUMMARY reports cells total, cells skipped, disagreements, and how many moved between the two
      builds and in which direction. Unsafe-direction movement is `0`.
    - The loader and its version are named in every new row's loader column.
    - Any shape the fuzz surfaced that task 2's fix does not cover is recorded as a finding, and the
      SUMMARY states whether the structural question was reopened.
  </acceptance_criteria>
  <done>
    The fix is measured against a real YAML loader over a generated family rather than a single input,
    with every count derived in session and the unsafe direction asserted empty independently.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Two independent red teams, self-reproduction, and the closure decision</name>
  <what-built>
    Two independent red-team passes against the fix, plus the executor's own post-fix reproduction, plus
    the register finding closed with the gate green.

    Before this checkpoint the executor must: (1) commission TWO adversarial passes by agents that did
    NOT author the fix, each given the fix diff and the corpus and asked to bypass it, each recording
    what it tried and what it found — one red team is what rounds 10 and 11 each had, and each of those
    shipped a new regression inside its own fix; (2) re-run its OWN reproduction from task 1 against
    the shipped post-fix build and record the result, rather than reading task 2's transcript; (3)
    record the finding and its disposition in `docs/audit/28-disposition-register.md`, increment the
    affected Table A row's `findings` count, and re-run `node scripts/check-audit-register.js` to
    exit 0 in the same commit so the two D-03 equalities do not drift because a late plan added a row.
  </what-built>
  <how-to-verify>
    1. Read both red-team records. Confirm each was produced by an agent that did not author the fix,
       and that each states what it tried, not only what it concluded. A red team that reports "no
       bypass found" without listing attempts has not been run.
    2. Read the executor's post-fix reproduction of the original bypass. Confirm it was run in this
       session against the shipped build, with the input bytes printed.
    3. Read the value map and the fuzz counts. Confirm unsafe-direction movement is `0` and that both
       corpus sizes were derived in session.
    4. Read the fix diff. Confirm it is structural — a production added to the canonical form, or a
       named refusal code fired where the form is decided — and not a character-level special case. If
       it reads as a twelfth widening, reject it; that is round 13.
    5. Run `node scripts/check-audit-register.js` and `node scripts/check-foundation-guards.js` and
       confirm both exit 0.
    6. Weigh the accepted cost. D-21 was taken over a stated objection that this module took twelve
       rounds and that two of them shipped a regression inside their own fix. If the evidence here does
       not clear that bar, deferring to Phase 30 — which carries a red-team budget as scope — remains
       available and is not a failure of this plan.
  </how-to-verify>
  <resume-signal>
    Type "approved — closure accepted", or "defer to Phase 30" with the reason, or describe issues. A
    green test suite alone is not a reason to approve; state which of D-22's four parts you found
    satisfied.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any markdown frontmatter → `admit()` | The admission reader decides whether a document carries a spawn grant. Every guard in the repository consumes its verdict; a bypass here is a grant no guard ever sees. |
| the module → a real YAML loader | The module and the platform's actual parser must agree in the unsafe direction. Where they disagree, the platform wins and the module is wrong. |
| the fix → eleven rounds of prior closures | A change to this module can break a closure from any earlier round, which is what rounds 10 and 11 each did. |
| a red team → the closure claim | A red team that is not independent of the fix's author is a review of the author's own assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-46 | Elevation of Privilege | `admit()` on the adjacency shape | critical | mitigate | An input the module reads as carrying no grant while a real loader reads a grant is an unguarded spawn grant. Mitigated by D-22's four parts together — structural fix, loader-differential fuzz over the shape's family, two independent red teams, and executor self-reproduction before and after — because this is the only bar this module has ever yielded to and any three of the four is what the two failed rounds delivered. |
| T-28-47 | Tampering | the fix breaking an earlier round's closure | critical | mitigate | Rounds 10 and 11 each shipped a new regression inside their own fix. Mitigated by replaying every corpus row from every round with its named code and printed refusal text, and by the run-time-derived value map on both sides that reports arms changed, values changed and new refusals rather than asserting none. |
| T-28-48 | Denial of Service | a new refusal on loader-accepted content | high | mitigate | Over-refusing breaks legitimate documents on every user's machine and presents as a safe change. Mitigated by requiring zero new refusals on loader-accepted content, and by requiring every module-refuses-loader-accepts row to be demonstrated pre-existing with a transcript rather than argued harmless. |
| T-28-49 | Repudiation | a closure claimed on a green suite | high | mitigate | A green suite has preceded a live bypass in this repository more than once. Mitigated by stating the suite as a floor rather than as proof, by the blocking checkpoint requiring the reviewer to name which of D-22's four parts were satisfied, and by making "defer to Phase 30" an explicitly available and non-failing outcome. |
| T-28-50 | Spoofing | a non-independent red team | high | mitigate | A red team run by the fix's author reviews the author's own assumptions and finds what the author already considered. Mitigated by requiring two passes by agents that did not author the fix, and by requiring each record to list what it tried rather than only what it concluded. |
| T-28-51 | Tampering | a false harness premise | high | mitigate | In Phase 27 the verification harness produced a false result in six instances across four straight rounds. Mitigated by asserting the harness's own premise before believing its verdict — which build ran, that the input bytes were printed and matched intent, and that the loader process exited normally. |
| T-28-52 | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and does not edit `package.json`. The real YAML loader used for the differential is an interpreter already present on the box and is invoked, never installed. Confirm `git diff package.json` is empty for this plan's commits. No package-legitimacy checkpoint is required because no install occurs. |

**ASVS L1, block on `high`.** T-28-46 and T-28-47 are `critical`; T-28-48 through T-28-52 are `high`.
Every one except T-28-52 is mitigated by a mechanism named in this plan's acceptance criteria or in
the blocking checkpoint. T-28-52 is accepted on the measured basis that no install occurs.
</threat_model>

<verification>
- `docs/audit/28-residual-sizing.md` carries a fix-time reproduction produced in this session, with the
  input bytes printed and a real YAML loader's verdict beside the module's.
- The new corpus row was RED before the fix and is GREEN after, both transcripts recorded.
- Both non-vacuity halves are measured: the live kit is still admitted at a run-time-derived count, and
  every corpus row is still refused by its named code with its text printed.
- The fuzz reports cells, skips, disagreements and a digest for both builds, with unsafe-direction
  movement at `0` and every count derived in session.
- Two independent red-team records exist, each listing what it tried.
- `node scripts/check-audit-register.js` exits 0 with the added finding row counted.
- `node scripts/check-foundation-guards.js`, `npm run freshness`, `npx tsc --noEmit` and
  `npx vitest run --exclude '**/scripts/e2e/**'` all exit 0 — stated as a floor, not as proof.
</verification>

<success_criteria>
The last fail-safe residual reaching a safety-critical parser is closed at the bar that actually
held — structural fix, loader-differential fuzz, two independent red teams, and the executor's own
reproduction before and after — with the accepted cost of this round recorded alongside the concern it
was taken over, and with deferral to Phase 30 left available rather than foreclosed.
</success_criteria>

<output>
Create `.planning/phases/28-kit-consistency-audit/28-08-SUMMARY.md` when done. It MUST record: the
fix-time reproduction with printed bytes and both verdicts; the RED and GREEN corpus transcripts; the
diff hunk that makes the fix structural; both non-vacuity halves with their run-time-derived numbers;
the fuzz counts and digests for both builds; both red-team records with what each tried; and an
explicit sentence stating that the passing suite is a floor and not proof that no bypass remains.
</output>

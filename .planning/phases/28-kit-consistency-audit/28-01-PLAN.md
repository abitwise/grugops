---
phase: 28-kit-consistency-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dead-vocabulary.ts
  - scripts/dead-vocabulary.js
  - scripts/check-public-docs-vocabulary.ts
  - scripts/check-public-docs-vocabulary.js
  - scripts/check-public-docs-vocabulary.test.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [AUDIT-02]

estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-24 is demonstrated, not asserted: the drift guard exists, is committed, is wired into CI, and exits 1 against the tree at HEAD before any drift fix lands, with the measured hit counts printed in its own FAIL output."
    - "D-09 lands a THIRD consumer of scripts/dead-vocabulary.ts, not a fourth list: the new gate imports RETIRED_PATH_FORMS and RETIRED_PROSE_FORMS and declares no literal of its own."
    - "The D-09 scan set is DERIVED, never hand-listed: root *.md by readdirSync filter minus the named CHANGELOG.md exemption, plus examples/ as a directory entry that self-derives through walk(), plus agent-factory/README.md as a named literal — and the membership is two-sided count-asserted so a new public doc enters by existing rather than by someone remembering."
    - "D-08's exemptions are named with their reason inline and bounded by an assertion: CHANGELOG.md, docs/initial/ and docs/design/ carry handoff prose as historical record and are exempt by name, never by silent absence from a scan."
    - "D-10 is honoured structurally: RETIRED_PROSE_FORMS and RETIRED_PATH_FORMS are asserted EQUAL to their current members by set equality, so a token banning the still-correct word cannot be added without a test going red — and scripts/dead-vocabulary.ts's header carries the second boundary warning beside the existing single-window one."
    - "scripts/check-kit-refs.ts's SCAN and GH_SCAN sets are byte-unchanged by this plan; the Phase 27 D-08 never-a-repo-wide-grep contract is untouched."
    - "The committed scripts/check-public-docs-vocabulary.js is a faithful tsc build of its .ts, proven by `npm run freshness` reporting every committed .js fresh."
    - statement: "A hermetic CHECK_ROOT mirror carrying zero retired vocabulary makes the gate exit 0, so its RED verdict at HEAD is a measurement of the tree rather than a property of the gate."
      verification: backstop
  artifacts:
    - scripts/check-public-docs-vocabulary.ts
    - scripts/check-public-docs-vocabulary.js
    - scripts/check-public-docs-vocabulary.test.ts
    - scripts/dead-vocabulary.ts
  key_links:
    - "scripts/dead-vocabulary.ts -> scripts/check-public-docs-vocabulary.ts: the single retired-vocabulary authority reaches its third consumer by import, with no literal copied."
    - "scripts/check-public-docs-vocabulary.js -> .github/workflows/ci.yml: the gate is wired at BOTH ends (package.json script + the ubuntu-only repo-gates block), because this repo has shipped a gate that ran only as a side effect of a test for a whole phase."
    - "scripts/kit-model.ts walk-and-sort convention -> the derived scan set: directory entries are sorted so two runs are byte-identical."
  prohibitions:
    - "No literal from dead-vocabulary.ts is copied into the new gate; the gate declares no retired-vocabulary string of its own."
    - "No token is added to RETIRED_PROSE_FORMS or RETIRED_PATH_FORMS in this plan."
    - "No entry is added to or removed from check-kit-refs.ts's SCAN or GH_SCAN."
    - "No prose in any public document is edited by this plan; the drift is measured here and fixed in 28-05."
    - "No hand-listed file array stands in for the derived scan membership."
    - "No exemption is expressed as absence from a list; every exemption is a named member of an exemption array carrying its reason."

user_setup: []
---

<objective>
Land the AUDIT-02 drift guard and watch it fail.

This is the phase tracer: one production-quality path from the single retired-vocabulary authority
(`scripts/dead-vocabulary.ts`), through a derived and count-asserted scan set, through a real gate
with a real exit code, to a committed `.js` proven fresh and wired into CI at both ends — verified
end to end by an actual red run against the actual tree.

Per D-24 the guard lands FIRST and is REQUIRED to exit 1 against today's tree, with its measured hit
counts recorded as a transcript. Plan 28-05 lands the fixes that turn it green. A guard that passes
the moment it appears has never been watched fail, so its ability to catch this drift would be
asserted rather than demonstrated.

Per D-09 the enforcement is a third consumer of one list — the same shape Phase 27 D-24 settled —
with its own derived scan set, never a fourth list. Per D-08 the drift set is everything
user-visible, with `CHANGELOG.md` and `docs/` named and reasoned exemptions. Per D-10 the word this
drift is often described with is NOT bannable as a token and never enters `RETIRED_PROSE_FORMS`;
the drift it names is one specific claim, and a claim is held by a registry entry (28-04), never by
a grep.

Purpose: make the AUDIT-02 reconciliation permanent instead of a one-time edit, and prove the
mechanism catches this drift before anyone relies on it.
Output: a new committed gate, an extended authority header, a hermetic test harness, CI wiring, and
a recorded RED transcript.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-kit-consistency-audit/28-CONTEXT.md
@.planning/phases/28-kit-consistency-audit/28-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-wide symbol and path inventory. This plan creates the rows marked **28-01**; the rest are
listed so no later plan has to rediscover a name.

**New `scripts/` modules.** Every one ships its committed `.js` built by `tsc`. There is **no
registration step** — `scripts/freshness.ts` derives membership from `OUTPUT_DIRS = ["install",
"scripts", "hooks"]` and is two-sided, so an orphaned committed `.js` fails red. The obligation is
`npm run build` and committing the `.js`.

| Source | Committed output | Exports | Plan |
|---|---|---|---|
| `scripts/check-public-docs-vocabulary.ts` | `scripts/check-public-docs-vocabulary.js` | `publicDocsScan()`, `PUBLIC_DOCS_SCAN_PARTS`, `PUBLIC_DOCS_EXEMPT`, `PUBLIC_DOCS_SCAN_COUNT`, `publicDocsVocabularyFails()` | **28-01** |
| `scripts/audit-model.ts` | `scripts/audit-model.js` | `readRegister()`, `readRegistry()`, `DISPOSITIONS`, `CLAIM_KINDS`, `SAFETY_FLOORS`, `RegisterRow`, `FindingRow`, `ClaimRow` | 28-03 |
| `scripts/audit-prepass.ts` | `scripts/audit-prepass.js` | `PREPASS_PREDICATES`, `runPrepass()` | 28-03 |
| `scripts/check-audit-register.ts` | `scripts/check-audit-register.js` | `auditRegisterFails()` | 28-03 |
| `scripts/check-claim-anchors.ts` | `scripts/check-claim-anchors.js` | `ANCHORED_DOCS`, `ANCHOR_RE`, `claimAnchorFails()` | 28-04 |
| `scripts/generate-safety-surface.ts` | `scripts/generate-safety-surface.js` | `renderSafetySurface()` | 28-07 |

**Edited `scripts/` modules:** `scripts/dead-vocabulary.ts` (header only — **28-01**),
`scripts/check-uat-oracles.ts` (28-02), `scripts/check-foundation-guards.ts` (a note beside
`stripHtmlComments` — 28-04), `scripts/canonical-frontmatter.ts` (28-08, conditional).

**New `docs/` artifacts** (durable repo artifacts outside `agent-factory/`, so nothing new ships to
host repos):

| Path | Authored or generated | Plan |
|---|---|---|
| `docs/audit/28-residual-sizing.md` | authored | 28-02 |
| `docs/audit/28-disposition-register.md` | authored, parsed by a gate | 28-03, filled 28-06 / 28-07 |
| `docs/audit/28-prepass-evidence.md` | generated by `audit-prepass` | 28-03 |
| `docs/audit/28-claim-registry.md` | authored, parsed by a gate | 28-04 |
| `docs/audit/28-safety-surface-exclusions.md` | generated, freshness-gated | 28-07 |

**Id schemes** (Claude's Discretion, fixed here so every plan uses one):

- **Claim id:** `CLM-NNN`, three digits zero-padded, assigned in document order across
  `README.md` then `AGENTS.md` then `agent-factory/README.md`.
- **Anchor format:** an HTML comment alone on the line immediately above the line carrying the
  claim, spelled `<!-- claim: CLM-NNN -->`. Nothing else on that line.
- **Finding id:** `F-28-NNN`, three digits zero-padded, assigned in register row order.
- **Register row key:** the repo-relative file path itself. No separate id.

**New config keys:** none. This phase adds no key to `agent-factory/config/factory.config.json`.

**New `package.json` script entries:** `check:public-docs` (**28-01**), `check:audit-register`
(28-03), `check:claim-anchors` (28-04), `generate:safety-surface` and
`freshness:safety-surface` (28-07).

## Measured drift at HEAD — the numbers this gate must report

Measured 2026-08-11 against the working tree, so the executor can tell a correct RED from a
wrong one. Re-measure at task start rather than trusting these; a difference is a finding, not a
failure.

| Retired form | Total hits | Per file |
|---|---|---|
| `agent-factory/handoffs/` (path form) | **14** | `examples/01-greenfield-bootstrap.md` 3, `examples/02-brownfield-bootstrap.md` 2, `examples/03-ticket-to-pr.md` 2, `examples/04-sprint-cycle.md` 5, `examples/05-release-run.md` 2 |
| `handoff packet` (prose form, case-insensitive) | **4** | `README.md` 1, `CLAUDE.md` 2, `agent-factory/README.md` 1 |
| `the handoff is the only memory` (prose form) | **0** | — |

**Total 18 hits across 8 files.** The derived scan set at HEAD is **10 files**: `AGENTS.md`,
`CLAUDE.md`, `CONTRIBUTING.md`, `README.md` (root `*.md` is 5 files, minus the `CHANGELOG.md`
exemption), the 5 `examples/*.md`, and `agent-factory/README.md`.

**A distinction the executor must not blur.** `agent-factory/README.md` carries **4** occurrences of
the bare word at lines 4, 35, 36 and 49, but only line 4 matches a `RETIRED_PROSE_FORMS` literal.
The other three are claims, not vocabulary — line 35's *"the roles, the handoffs, and the gates are
identical everywhere"* is registry row material for 28-04 and rewrite material for 28-05. This gate
reports what its literals match and nothing else. It must not be widened to chase the remaining
three; a grep cannot hold a claim (D-10).

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — authority to gate to red exit code, one path</name>
  <files>scripts/dead-vocabulary.ts, scripts/check-public-docs-vocabulary.ts</files>
  <read_first>
    - `scripts/dead-vocabulary.ts` — the whole file, header comment included. It carries a boundary
      warning about execution-topology prose and a self-exclusion note stating the module must never
      enter any guard's scan set. Both bind this task.
    - `scripts/check-kit-refs.ts` lines 36-93 — the SCAN and GH_SCAN declarations, the D-08
      never-a-repo-wide-grep contract, and the named exclusions. This plan leaves all of it alone.
    - `scripts/check-kit-refs.ts` lines 139-181 — `walk()` and `grepSubstring()`, the primitives
      this gate reuses, including the `.sort()` on directory entries.
    - `scripts/check-kit-refs.ts` lines 1-35 — the module-header convention every new script follows.
    - `scripts/check-foundation-guards.ts` lines 1170-1275 — `guardAdapterBody`, consumer #2, for how
      `RETIRED_PROSE_FORMS` is applied case-insensitively against a lowercased body.
    - `scripts/check-foundation-guards.ts` lines 1700-1717 and 1817 — `DISTRIBUTION_PAIR_EXEMPT`, the
      named-exemption idiom this gate copies for `CHANGELOG.md`.
    - `scripts/kit-model.ts` lines 150-170 and 795-812 — `MAX_WALK_ENTRIES` and its refusal, for the
      bounded-walk posture.
    - `.planning/phases/28-kit-consistency-audit/28-PATTERNS.md` section 2 — the row for this file.
    - `.planning/phases/28-kit-consistency-audit/28-CONTEXT.md` — D-08, D-09, D-10, D-24.
  </read_first>
  <action>
Extend the header of `scripts/dead-vocabulary.ts` with a second boundary warning, placed beside the
existing execution-topology one, in the same voice and the same comment-block style. The new warning
states that the verb describing what the Orchestrator does with work is still correct v2.0 English
and must never be added to `RETIRED_PROSE_FORMS`, naming the three live sites a token guard would
red: the orchestrator adapter's own description, `agent-factory/roles/orchestrator.md`'s
`### Routing matrix` heading, and `CLAUDE.md`'s statement about how Claude Code decides to delegate
to a subagent — a platform fact, not a grugops claim. It then states what the actual drift is: one
specific linear-pipeline claim, and that a claim is held by a registry entry in
`docs/audit/28-claim-registry.md`, never by a grep. Add no export, no token, and no other change to
this file. Its two exported arrays keep exactly their current members.

Create `scripts/check-public-docs-vocabulary.ts`. Open with the standard module header stating: what
it is and that it serves AUDIT-02 under D-09; the invocation line
`node scripts/check-public-docs-vocabulary.js` and the exit-code contract (0 on pass, 1 on any
failure); the dependency floor `Node stdlib ONLY — node:fs + node:path. Zero npm dependencies.`; and
the clear-professional-voice rule. The header also records three facts a later editor needs: that
this is the THIRD consumer of `scripts/dead-vocabulary.ts` and declares no literal of its own; that
`AGENTS.md` is deliberately double-covered — `check-kit-refs.ts` Assertion 2 already greps it for the
path form, this gate adds the prose forms — so the overlap is stated rather than accidental; and the
D-24 RED transcript in the measured-table form `scripts/kit-model.ts` uses around line 276.

Import `RETIRED_PATH_FORMS` and `RETIRED_PROSE_FORMS` from `./dead-vocabulary.js`. Resolve `ROOT`
from `process.env.CHECK_ROOT` falling back to `join(import.meta.dirname, "..")`, and declare the
`pass` / `fail` / `FAILS` harness exactly as `scripts/check-uat-oracles.ts` lines 64-79 does. Export
`publicDocsVocabularyFails` as an arrow returning `FAILS`, so a later aggregator can fold this gate
without a shared global.

Declare `PUBLIC_DOCS_EXEMPT` as a `readonly string[]` holding `CHANGELOG.md`, with its reason stated
directly above it — it is Keep a Changelog historical record and the retired vocabulary in it
describes what the project used to ship, which is what a changelog is for — its bound stated, and
the forbidden alternative named so it is not rediscovered as a good idea: loosening the grep so this
one file passes would delete the check for every other root document. Record in the same comment
that `docs/initial/` and `docs/design/` are exempt structurally, by never being members of the
derived set, and say why in one line.

Derive the scan set in three parts and export them as `PUBLIC_DOCS_SCAN_PARTS`, each part carrying a
`name` and a `members` array, following the `SPAWN_GRANT_SCAN_PARTS` shape in `scripts/kit-model.ts`
line 1046. Part `root`: `readdirSync(ROOT)` filtered to entries ending in the markdown extension that
`statSync` reports as files, sorted, minus every member of `PUBLIC_DOCS_EXEMPT`. Part `examples`:
`walk("examples", [])` filtered to markdown, which makes membership self-deriving with no import —
a new example file enters by existing. Part `kitReadme`: the single named literal
`agent-factory/README.md`. Export `publicDocsScan()` returning the concatenation, and
`PUBLIC_DOCS_SCAN_COUNT` as the pinned cardinality.

Refuse an empty derived set the way `scripts/kit-model.ts` lines 663-670 does, and write the floor
over the DERIVED quantity per part, not over the concatenated total — the correction recorded at
`scripts/check-foundation-guards.ts` lines 1250-1262 is that a floor written over the wrong quantity
is worse than no floor, because a part that always contributes at least one member makes the total
floor unreachable. Bound the `examples` walk with the same loud named refusal `MAX_WALK_ENTRIES`
uses, but as a `fail()` naming the directory rather than a throw, because this is a gate and not a
library — the throw-versus-report split documented at `scripts/kit-model.ts` lines 744-753.

Assert the derived membership two-sided against `PUBLIC_DOCS_SCAN_COUNT`: the concatenation length
must equal it, and the failure message must name the derived number, the pinned number, and what the
author must walk before changing the pin. Then run the check: for every member of
`RETIRED_PATH_FORMS`, substring-grep the scan for the literal; for every member of
`RETIRED_PROSE_FORMS`, grep a lowercased copy of each line. Report each hit as
`path:lineno:line`, exactly as `grepSubstring` does. Any hit is a `fail()` whose message names the
literal, the file, the line number, and the remedy: the memory relay was deleted in Phase 24 and the
shared verified context replaced it, so this text describes an architecture that does not ship.

The PASS line, when it eventually passes, must name the counts actually read — the number of files
scanned per part, the exempted member with its reason inline, and the number of literals checked from
each of the two arrays. It must never state a check that was not performed.

Run `npm run build`, commit the `.js`, then run `node scripts/check-public-docs-vocabulary.js` and
capture the full output verbatim. It MUST exit 1. Record the transcript in the module header in the
measured-table form: one row per retired literal giving the hit count and the files named, plus the
exit code and the total. If the measured counts differ from the table in this plan, the measured
value wins and the divergence is recorded in the SUMMARY — never adjust the measurement to match the
plan.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-public-docs-vocabulary.js; test $? -eq 1 &amp;&amp; echo "RED AS REQUIRED"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-public-docs-vocabulary.js` exits **1** against the tree at HEAD.
    - Its output names `examples/04-sprint-cycle.md` and `CLAUDE.md` by path with line numbers.
    - The total hit count it reports equals the sum of the per-literal counts it reports, and both are
      re-derived at run time rather than printed from a constant.
    - `node -e "import('./scripts/check-public-docs-vocabulary.js')"` is not required; the module is a
      script. Instead: `grep -n "PUBLIC_DOCS_SCAN_COUNT" scripts/check-public-docs-vocabulary.ts`
      returns the export declaration and the two-sided assertion, at two distinct line numbers.
    - `git diff --stat scripts/check-kit-refs.ts` is empty.
    - `git diff scripts/dead-vocabulary.ts` shows only comment lines added; the diff contains no line
      that changes either exported array's members. Confirm by
      `git diff -U0 scripts/dead-vocabulary.ts | grep -c '^[+-]  "'` returning `0`.
    - `npm run freshness` exits 0 and reports every committed `.js` matching a fresh rebuild.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">
    D-09 is rated costly in CONTEXT.md: undoing this consumer later means re-cutting the consumer, its
    derivation, and the exemption record together. It is not one-way — the gate is deletable — so no
    decision checkpoint is inserted.
  </reversibility>
  <done>
    The guard exists, is committed with its `.js`, and has been watched failing against the real tree
    with its hit counts recorded in a transcript. `scripts/dead-vocabulary.ts` carries two boundary
    warnings and the same two exported arrays it had before.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The hermetic harness — prove the RED is a property of the tree, not of the gate</name>
  <files>scripts/check-public-docs-vocabulary.test.ts</files>
  <behavior>
    - A `CHECK_ROOT` mirror carrying no retired vocabulary makes the gate exit **0** with a PASS line.
      Without this case, the RED transcript proves nothing: a gate that always fails is trivially red.
    - A mirror carrying one planted path-form hit in a new example file exits **1** and names that
      file — proving `examples/` membership self-derives through the walker rather than being listed.
    - A mirror carrying one planted prose-form hit in a new root markdown file exits **1** and names
      it — proving root membership self-derives.
    - A mirror carrying the same planted prose-form hit in `CHANGELOG.md` exits **0** — proving the
      exemption is real and reached, not merely declared.
    - The derived count and the pinned `PUBLIC_DOCS_SCAN_COUNT` are equal, and are asserted
      not-equal to the pin minus one and the pin plus one, following the two-sided pin convention at
      `scripts/kit-model.test.ts` lines 668-678.
    - `RETIRED_PROSE_FORMS` and `RETIRED_PATH_FORMS` are asserted equal to their exact current members
      by array equality — the D-10 control. Adding any token to either array fails this case, which is
      how the still-correct verb is kept out of the ban list by a mechanism rather than by a comment.
    - An empty derived part is refused by name rather than passing vacuously.
  </behavior>
  <read_first>
    - `scripts/check-uat-oracles.ts` lines 61-63 — the harness contract for spawning a committed `.js`
      against a `CHECK_ROOT` mirror.
    - `scripts/kit-model.test.ts` lines 251-266 and 668-678 — the two-sided pin convention and the
      refusal-path case shape.
    - `scripts/check-kit-refs.test.ts` — the existing mirror-construction helper for this gate family;
      reuse it rather than authoring a second mirror builder.
    - `scripts/check-public-docs-vocabulary.ts` as written in task 1.
    - `scripts/dead-vocabulary.ts`.
  </read_first>
  <action>
Create `scripts/check-public-docs-vocabulary.test.ts` beside its source — this repo has no `tests/`
directory. Build hermetic mirrors under the OS temp directory and point `CHECK_ROOT` at them,
spawning the **committed** `scripts/check-public-docs-vocabulary.js` rather than importing the `.ts`,
so the case measures the artifact CI runs.

Write the clean-mirror case FIRST and confirm it is green before writing the planted cases, because
that ordering is what makes the RED transcript from task 1 a measurement rather than a tautology.

For the exemption case, plant the identical prose-form string in `CHANGELOG.md` and in one other root
markdown file inside the same mirror, then assert the gate names the second file and does not name
`CHANGELOG.md`. Asserting only exit 0 on a `CHANGELOG.md`-only mirror would pass even if the whole
grep were dead; the paired plant is what makes the exemption discriminating.

For the D-10 control, assert `RETIRED_PROSE_FORMS` and `RETIRED_PATH_FORMS` by importing them from
`./dead-vocabulary.js` and comparing each with `toEqual` against a literal array written out in the
case. State in a comment above it that this case exists so that adding a ban for the still-correct
Orchestrator verb, or for any other phrase this project keeps on purpose, is a red case rather than a
judgement call — and that the correct place to hold that drift is a claim registry row.

For the vacuity case, build a mirror with an empty `examples/` directory and assert the gate refuses
by name with a message matching the refusal wording, rather than reporting a pass over zero files.

Run the suite with `npx vitest run --exclude '**/scripts/e2e/**' scripts/check-public-docs-vocabulary.test.ts`.
Do NOT run bare `npm test`: it triggers the live claude-CLI e2e lane, which spends tokens and can
hang.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-public-docs-vocabulary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every case in the new file passes; the vitest summary names the file with 0 failures.
    - The clean-mirror case asserts exit code `0` explicitly, not merely absence of a throw.
    - `grep -c 'CHECK_ROOT' scripts/check-public-docs-vocabulary.test.ts` returns a value greater than
      `0`, confirming the cases run against a mirror rather than the live tree.
    - The two-sided pin case asserts three comparisons against `PUBLIC_DOCS_SCAN_COUNT`: equal,
      not equal to the pin minus one, not equal to the pin plus one.
    - Deliberately adding a member to `RETIRED_PROSE_FORMS` in a scratch working copy makes the D-10
      control case fail; revert it. Record the two exit statuses in the SUMMARY.
    - `npx vitest run --exclude '**/scripts/e2e/**'` over the whole suite exits 0.
  </acceptance_criteria>
  <done>
    The gate's RED verdict at HEAD is proven to be a measurement of the tree: the same gate exits 0 on
    a clean mirror, exits 1 on each planted shape, and honours its one named exemption.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire the gate at both ends</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <read_first>
    - `package.json` — the existing `scripts` block, for the `tsc --outDir .tmp-build &amp;&amp; node …`
      invocation form every gate entry uses.
    - `.github/workflows/ci.yml` lines 70-115 — the ubuntu-only freshness-and-repo-gates block, and
      the comment above `node scripts/check-kit-refs.js` explaining why gates are wired at both ends:
      a gate that runs only because some other step happens to run it is not wired, it is borrowed.
    - `scripts/freshness.ts` lines 41-60 — the derived `OUTPUT_DIRS` membership, confirming that no
      registration step is needed for the new `.js`.
  </read_first>
  <action>
Add a `check:public-docs` entry to `package.json`'s `scripts` block, in the same form the other gate
entries use: `tsc --outDir .tmp-build` followed by `node scripts/check-public-docs-vocabulary.js`.
Place it beside the other `check:`-style entries rather than at the end.

Add `node scripts/check-public-docs-vocabulary.js` to `.github/workflows/ci.yml`'s ubuntu-only
repo-gates block, immediately after `node scripts/check-kit-refs.js`, so the two consumers of the
retired-vocabulary authority sit together and a reader meets them as a pair. Above the line, add a
comment stating: that this is the D-09 third consumer and covers the user-visible public documents
that `check-kit-refs`'s SCAN deliberately excludes; that it is wired here AND in
`scripts/check-public-docs-vocabulary.test.ts` on purpose, because this repository shipped a
freshness gate that ran as a side effect of a test for an entire phase; and — stated plainly, because
CI will be red until 28-05 lands — that this gate is EXPECTED to fail until the AUDIT-02 drift fixes
land, per D-24, and that the red is the acceptance evidence rather than a regression.

Do not add the gate to `scripts/check-foundation-guards.ts`'s ordered guard list in this plan. The
aggregator is already 2606 lines; the exported `publicDocsVocabularyFails` accessor from task 1 is
what a later fold would use.

Verify the wiring by running `npm run check:public-docs` and confirming it reaches the same exit 1
and the same output as the direct `node` invocation from task 1.
  </action>
  <verify>
    <automated>npm run check:public-docs; test $? -eq 1 &amp;&amp; grep -c 'check-public-docs-vocabulary.js' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check:public-docs` exits 1 and its output is byte-identical to the transcript recorded
      in task 1 (compare with `diff` against a saved capture).
    - `grep -c 'check-public-docs-vocabulary.js' .github/workflows/ci.yml` returns `1`.
    - The CI comment above the new line contains the words that state the gate is expected red until
      the drift fixes land, so a maintainer reading a red build finds the reason in the file rather
      than in a planning document.
    - `node -e "const p=require('./package.json'); if(!p.scripts['check:public-docs']) process.exit(1)"`
      exits 0.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0 and `npm run freshness` exits 0.
  </acceptance_criteria>
  <done>
    The gate runs from `package.json`, runs in CI, and is proven red at both entry points with the
    reason recorded in the workflow file itself.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository tree → gate process | Untrusted-by-drift markdown crosses into a guard that decides whether the build is green. The gate reads files any contributor can add. |
| `scripts/dead-vocabulary.ts` → three consumers | One data module feeds three predicates. A change to the module changes three verdicts at once. |
| gate exit code → CI verdict | The only thing downstream consumes is the exit code. Everything about the gate's honesty reduces to whether that number is earned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | `publicDocsScan()` derived set | high | mitigate | A vacuous scan set passes every guard. The per-part empty refusal (task 1) plus the two-sided `PUBLIC_DOCS_SCAN_COUNT` pin plus the clean-mirror-versus-planted-mirror pair (task 2) make an empty or silently-shrunk set a named failure rather than a pass. |
| T-28-02 | Denial of Service | `walk("examples")` | medium | mitigate | An unbounded or cyclic directory walk hangs the gate, and a hung gate is not a red gate — it is a gate with no verdict. Bounded with a loud named refusal following `MAX_WALK_ENTRIES`, reported through `fail()` rather than a throw because this is a gate. |
| T-28-03 | Repudiation | the D-24 RED transcript | high | mitigate | A transcript pasted from the plan rather than captured would make the acceptance evidence fiction. The transcript is captured from a real run, the measured value wins over the plan's table, and any divergence is recorded — CLAUDE.md's never-fake-a-gate-result constraint applied to this phase's own evidence. |
| T-28-04 | Elevation of Privilege | `PUBLIC_DOCS_EXEMPT` | medium | mitigate | Exemption-by-silent-absence is how a scan set rots. The exemption is a named array member with its reason and bound inline, the forbidden alternative is named, and the paired-plant test case proves the exemption discriminates rather than merely existing. |
| T-28-05 | Information Disclosure | gate output | low | accept | The gate prints repo-relative paths and line contents of files already public on GitHub. No secret, token, or path outside the repository is read or emitted. |
| T-28-06 | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package. `package.json`'s dependency blocks are byte-unchanged; the only edit is a `scripts` entry. The Phase 27 practice of asserting an empty manifest diff at round scope applies: `git diff package.json` must show no change under `dependencies` or `devDependencies`. No package-legitimacy checkpoint is required because no install occurs. |

**ASVS L1, block on `high`.** T-28-01, T-28-03 and T-28-06 are the `high` rows; the first two are
mitigated by mechanisms inside this plan's own acceptance criteria, and T-28-06 is accepted on the
measured basis that no install happens.
</threat_model>

## Flagged planner assumptions — unresolved probe edges

The deterministic edge probe over this phase's requirements returned one row for AUDIT-02 with
category `unclassified` and no resolution. It is surfaced here rather than auto-dismissed:

> **UNRESOLVED — AUDIT-02, category `unclassified`.** The probe could not classify AUDIT-02 into an
> edge family, so no acceptance criterion was derived from it. The planner's reading is that AUDIT-02
> is a prose-reconciliation requirement with no boundary arithmetic to probe — its correctness is
> whether the shipped text matches the shipped architecture, which is adjudicated by human reading in
> 28-05 and held mechanically by this plan's gate. **This is an assumption, not a resolution.** If a
> reviewer identifies a genuine edge in AUDIT-02, it belongs in this plan's `must_haves` before the
> phase is verified.

<verification>
- `node scripts/check-public-docs-vocabulary.js` exits 1 against HEAD, naming every one of the 18
  measured hits with file and line number.
- The same gate exits 0 against a clean hermetic mirror.
- `npm run freshness` exits 0; the new `.js` is a faithful build of its `.ts`.
- `npx tsc --noEmit` exits 0 and `npx vitest run --exclude '**/scripts/e2e/**'` exits 0.
- `git diff --stat scripts/check-kit-refs.ts` is empty.
- `git diff package.json` shows a change under `scripts` only.
</verification>

<success_criteria>
The AUDIT-02 enforcement mechanism exists, is derived rather than hand-listed, imports the one
retired-vocabulary authority rather than copying it, honours one named exemption that is proven to
discriminate, and has been watched failing against the real tree with its hit counts recorded — all
before a single word of drift has been fixed.
</success_criteria>

<output>
Create `.planning/phases/28-kit-consistency-audit/28-01-SUMMARY.md` when done. It MUST contain the
verbatim RED transcript, the re-measured per-literal hit counts, and — if those differ from the table
in this plan — the divergence, stated as a measurement that overrides the plan.
</output>

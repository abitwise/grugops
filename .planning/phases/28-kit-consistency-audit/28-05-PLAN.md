---
phase: 28-kit-consistency-audit
plan: 05
type: execute
wave: 6
depends_on: ["28-01", "28-04", "28-06"]
files_modified:
  - docs/audit/28-disposition-register.md
  - README.md
  - CLAUDE.md
  - AGENTS.md
  - agent-factory/README.md
  - examples/01-greenfield-bootstrap.md
  - examples/02-brownfield-bootstrap.md
  - examples/03-ticket-to-pr.md
  - examples/04-sprint-cycle.md
  - examples/05-release-run.md
  - agent-factory/handoffs/.gitkeep
  - agent-factory/examples/.gitkeep
  - docs/audit/28-claim-registry.md
autonomous: true
requirements: [AUDIT-02]

estimate:
  tokens: 98000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-24 completes here: the drift guard that was watched failing in wave 1 exits 0 against this tree, and the transition from exit 1 with 18 named hits to exit 0 with a PASS line is recorded as a before-and-after transcript. The guard was watched failing before it was trusted."
    - "D-08's full drift set is reconciled, not just the named target: agent-factory/README.md, AGENTS.md, CLAUDE.md, README.md and all five examples/*.md. agent-factory/README.md is the internal start-here guide the installer ships to every user, so fixing only CLAUDE.md would fix the file no user reads and leave stale the one they do."
    - "D-08's exemptions hold: CHANGELOG.md, docs/initial/ and docs/design/ keep their handoff prose as historical record and are not edited by this plan."
    - "D-11: all FIVE examples/*.md are RE-NARRATED onto the shared-verified-context flow, including examples/03-ticket-to-pr.md. The 14 dead path references are embedded in narrative describing the deleted relay, so this is a rewrite and not a find-and-replace — a path swap without re-narration would describe a relay that no longer exists using correct paths, which is worse than the current honest staleness."
    - "D-11's Phase 33 coupling is RECORDED in the disposition register so Phase 33 knows examples/03-ticket-to-pr.md moved, and the record states that the handoff lines rewritten here are disjoint from GAP-D1's live-capture lines."
    - "D-12: both hygiene directories are deleted, each as a register row carrying its reason — agent-factory/handoffs/ holds only a .gitkeep and is named for a concept Phase 24 deleted, and agent-factory/examples/ is empty. RETIRED_PATH_FORMS already asserts zero hits on the handoffs path across the shipped kit, so deleting the directory cannot break it."
    - "D-12's installer claim is VERIFIED rather than assumed: the installer's cpSync of the whole agent-factory tree and the uninstall mirror both track by derivation, so no installer literal should need editing — and this plan proves that by running both against a scratch target and comparing, not by reading the code and concluding."
    - "D-25 completes here: every registry row recorded status false in 28-04 because of this drift is flipped to true IN THE SAME COMMIT as the rewrite that makes it true, and the bijection and verbatim gate from 28-04 is what catches it if the flip is missed — on a real commit rather than on a fixture."
    - "D-10 is not violated by the rewrite: the specific linear-pipeline claim is corrected to decompose-and-enqueue over a shared queue, while the still-correct uses of the routing verb — the orchestrator adapter description, orchestrator.md's routing matrix heading, and CLAUDE.md's Claude Code platform fact — are left standing, because the drift is one claim and not a token."
    - statement: "No claim registered in 28-04 that this plan did not intend to touch has changed its verbatim text; the anchor gate exits 0, which is the only mechanical evidence that the rewrite stayed inside its scope."
      verification: backstop
  artifacts:
    - README.md
    - CLAUDE.md
    - AGENTS.md
    - agent-factory/README.md
    - examples/01-greenfield-bootstrap.md
    - examples/02-brownfield-bootstrap.md
    - examples/03-ticket-to-pr.md
    - examples/04-sprint-cycle.md
    - examples/05-release-run.md
  key_links:
    - "the rewrite -> scripts/check-public-docs-vocabulary.js: the wave-1 guard is the acceptance evidence for this plan, and it goes from exit 1 to exit 0 as a direct result."
    - "the rewrite -> scripts/check-claim-anchors.js: any anchored sentence changed without flipping its registry row reds this gate, which is D-25's mechanism proven on a real commit."
    - "deleting agent-factory/handoffs/ -> install/install.ts cpSync and the uninstall mirror: both track by derivation, verified by running them rather than by reading them."
    - "examples/03-ticket-to-pr.md -> Phase 33 GAP-D1: the coupling is recorded in the register so a later phase does not rediscover it as a surprise."
  prohibitions:
    - "No path in an examples narrative is swapped without the surrounding narrative being rewritten to describe the flow that actually ships."
    - "CHANGELOG.md is not edited."
    - "No file under docs/initial/ or docs/design/ is edited."
    - "No still-correct use of the routing verb is removed; the drift is one claim, not a token."
    - "No new claim is introduced into the three anchored documents without a registry row and an anchor."
    - "No installer literal is edited on the assumption that it needs editing; the need is measured first."
    - "No prose is edited in a role or workflow file — those are the read pass's subject, not this plan's."

user_setup: []
---

<objective>
Make the shipped documents describe the architecture that actually ships, and turn the guard green.

Per D-24 this is the second half of a demonstration begun in wave 1. The drift guard was landed and
watched failing against the real tree with its hit counts recorded. This plan lands the fixes, and the
guard's transition from exit 1 to exit 0 is the acceptance evidence — the mechanism is trusted because
it was watched failing, not because it appeared and passed.

Per D-08 the drift set is everything user-visible. The installer copies the whole `agent-factory/`
tree to the kit root of every install, so `agent-factory/README.md` is the internal start-here guide
every user gets. Reconciling only `CLAUDE.md`, the file AUDIT-02 names, would fix the file no user
reads and leave stale the one they do.

Per D-11 all five examples are re-narrated rather than path-swapped. The 14 dead references are
embedded in narrative describing the relay that Phase 24 deleted; correcting the paths while leaving
the narrative would describe a relay that no longer exists using paths that resolve, which is worse
than the current honest staleness.

Per D-12 both hygiene directories go, each recorded as a register row with its reason. Per D-25 every
registry row recorded false because of this drift is flipped in the same commit as the rewrite that
makes it true, and 28-04's bijection and verbatim gate catches it if the flip is missed — on a real
commit rather than a fixture, which is the whole point of the ordering.

Per D-10 the correction is to one specific claim, not to a token. The linear pipeline sentence is
what v2.0 replaced with decompose-and-enqueue over a shared queue; the routing verb itself is still
correct in three live places and stays.

Purpose: close AUDIT-02 in the files users actually read, and prove the closure with a gate that was
seen failing first.
Output: eight rewritten documents, two deleted directories, flipped registry rows, and a green guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-kit-consistency-audit/28-CONTEXT.md
@.planning/phases/28-kit-consistency-audit/28-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this plan produces

This plan creates no new symbol, script, config key or `docs/` artifact. It modifies content and
deletes two directories. The phase-wide artifact inventory is in `28-01-PLAN.md`.

**Deleted:** `agent-factory/handoffs/` (holds only `.gitkeep`) and `agent-factory/examples/` (holds
only `.gitkeep`). Both currently ship to every user.

<!-- planner-discipline-allow: agent-factory/handoffs/ -->

**Comment-text discipline note.** Task 3's action names `agent-factory/handoffs/.gitkeep` because a
task that deletes a file must name it. Task 2's acceptance criterion negative-greps the same literal,
but over a **disjoint scope** — `examples/` only, never a planning document and never this plan — so
the two cannot invalidate each other. The allowlist marker above records that the collision was seen
and scoped rather than missed. Any executor who widens that grep's scope to include `.planning/` will
red it on this plan's own text, which would be a false red, not a finding.

## The 18 measured hits this plan must drive to zero

Measured 2026-08-11; re-measure at task start with
`node scripts/check-public-docs-vocabulary.js` and let the measurement win over this table.

| File | Retired path form | Retired prose form |
|---|---|---|
| `README.md` | 0 | 1 |
| `CLAUDE.md` | 0 | 2 |
| `AGENTS.md` | 0 | 0 |
| `agent-factory/README.md` | 0 | 1 (line 4) |
| `examples/01-greenfield-bootstrap.md` | 3 | 0 |
| `examples/02-brownfield-bootstrap.md` | 2 | 0 |
| `examples/03-ticket-to-pr.md` | 2 | 0 |
| `examples/04-sprint-cycle.md` | 5 | 0 |
| `examples/05-release-run.md` | 2 | 0 |

**The guard's set is not the whole job.** `agent-factory/README.md` carries the bare word at lines 4,
35, 36 and 49 but only line 4 matches a guard literal. Lines 35, 36 and 49 are claims — line 35's
assertion that the roles, the handoffs and the gates are identical everywhere is a registry row from
28-04 recorded `status: false`. `AGENTS.md:5` carries the linear-pipeline claim while `AGENTS.md:21`
already says decompose and enqueue, an internal contradiction with zero guard hits. A grep cannot hold
a claim; the registry does, and this plan reconciles both.

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the four top-level documents, and flip their registry rows in the same commit</name>
  <files>README.md, CLAUDE.md, AGENTS.md, agent-factory/README.md, docs/audit/28-claim-registry.md</files>
  <read_first>
    - `README.md` in full — 57 lines, including line 3's lifecycle sentence, line 22's version claim,
      line 30's installer claim and line 38's dispatch claim.
    - `CLAUDE.md` in full — the project instructions, including line 6's lifecycle sentence, and
      line 83's statement about how Claude Code decides to delegate, which is a platform fact and stays.
    - `AGENTS.md` in full — 144 lines, including line 5's lifecycle sentence and line 21's already-
      correct decompose-and-enqueue statement, which is the internal contradiction to resolve.
    - `agent-factory/README.md` in full — 139 lines, including line 4, the five-tool dispatch table at
      lines 38-44, and lines 35-49's identical-everywhere claim.
    - `docs/audit/28-claim-registry.md` as written in 28-04 — every row whose `file` is one of these
      four and whose `status` is not `true`, plus the anchors those rows point at.
    - `agent-factory/roles/orchestrator.md` — how the Orchestrator actually behaves in v2.0, so the
      replacement sentence describes the shipped architecture rather than a plausible one.
    - `scripts/dead-vocabulary.ts` — both exported arrays and both header warnings, so the rewrite
      removes what is retired and keeps what is not.
    - `.planning/phases/28-kit-consistency-audit/28-CONTEXT.md` — D-08, D-10, D-25.
  </read_first>
  <action>
Rewrite the linear-pipeline claim in `README.md`, `CLAUDE.md`, `AGENTS.md` and `agent-factory/README.md`
so each describes decompose-and-enqueue over a shared queue with a shared verified context, which is
what v2.0 ships. Use `agent-factory/roles/orchestrator.md` and `AGENTS.md`'s already-correct line 21
as the source of truth for what to say — do not invent a new description of the architecture, and do
not paraphrase the roadmap. `AGENTS.md`'s two statements must agree with each other when you are done;
today they contradict.

Remove the retired-vocabulary noun phrase at each of the four guard-matched sites, rewriting the
surrounding sentence so it reads naturally rather than leaving a gap. In `agent-factory/README.md`
line 4 and `CLAUDE.md`'s equivalent, the phrase sits inside a list of what the kit contains; replace
it with what the kit actually contains in its place, which is the shared verified context.

Reconcile the three claim sites in `agent-factory/README.md` that carry no guard literal. Line 35's
assertion that the roles, the handoffs and the gates are identical everywhere is false by drift and
was registered as such; rewrite it to assert what is actually identical across the five tools. Line 49
repeats it in shorter form. The five-tool dispatch table at lines 38-44 predates the three-tier
vocabulary Phase 27 established — CONTEXT.md's deferred list records this as falling out of the audit
rubric as a finding rather than being pre-decided, so do NOT rewrite the table here. Record it as a
finding for the register instead, and say in the SUMMARY that it was recorded rather than fixed.

Leave every still-correct use of the routing verb standing. `CLAUDE.md`'s statement about Claude Code
auto-routing to a subagent is a platform fact about the host tool, not a grugops claim, and it stays.
The drift is one claim, not a token; a rewrite that removes the verb everywhere would delete correct
text and is the exact trap `scripts/dead-vocabulary.ts`'s header now warns about twice.

Then flip the registry. For every row in `docs/audit/28-claim-registry.md` whose claim text you just
changed, update the fenced verbatim block to the new text and set `status` to `true` — or to
`overstated` with a disposition, if the corrected sentence is still stronger than its mechanism
supports. Do this in the SAME COMMIT as the prose edit. That is D-25's requirement and the reason
28-04 landed first; the bijection and verbatim gate is what proves it happened.

Do not add a new claim to these documents without also adding its registry row and its anchor. If the
rewrite naturally produces a new assertive sentence, register it — an unregistered new claim is
D-16's named residual arriving by your own hand.

Run `node scripts/check-claim-anchors.js` after the edit. It must exit 0. If it exits 1, a sentence
moved without its row following, and the row is what gets corrected, never the comparison.
  </action>
  <verify>
    <automated>node scripts/check-claim-anchors.js &amp;&amp; node -e "const m=require('./scripts/audit-model.js');const r=m.readRegistry();const bad=r.filter(x=>x.status!=='true'&&!x.disposition);if(bad.length){console.error('undispositioned:',bad.map(b=>b.id).join(','));process.exit(1)}console.log('registry ok',r.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-claim-anchors.js` exits 0 — every anchored claim's verbatim text matches its
      registry row after the rewrite.
    - `grep -ric "handoff packet" README.md CLAUDE.md AGENTS.md agent-factory/README.md` returns 0 for
      every file.
    - `AGENTS.md` no longer contains two statements about how the Orchestrator distributes work that
      disagree; both describe decompose and enqueue. Verified by reading both lines and quoting them in
      the SUMMARY.
    - `CLAUDE.md`'s statement about how Claude Code decides to delegate to a subagent is still present
      and unchanged, confirming the token was not banned. Quote it in the SUMMARY.
    - Every registry row whose `file` is one of these four and whose `status` is not `true` carries a
      `disposition`; the automated check above exits non-zero if any does not.
    - `git diff --stat CHANGELOG.md docs/` is empty — the named exemptions were not touched.
    - The five-tool dispatch table at `agent-factory/README.md` lines 38-44 is unchanged, and the
      SUMMARY records it as a finding for the register rather than a fix.
    - `git log -1 --name-only` for this task's commit lists both the prose files and
      `docs/audit/28-claim-registry.md` — the same-commit requirement, verified from git rather than
      asserted.
  </acceptance_criteria>
  <done>
    The four top-level documents describe the architecture that ships, their registry rows moved with
    them in one commit, and the still-correct uses of the routing verb are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-narrate all five examples onto the shared verified context</name>
  <files>examples/01-greenfield-bootstrap.md, examples/02-brownfield-bootstrap.md, examples/03-ticket-to-pr.md, examples/04-sprint-cycle.md, examples/05-release-run.md</files>
  <read_first>
    - All five `examples/*.md` in full. They are narratives, and the 14 dead references are embedded in
      sentences describing what an agent writes and what the next agent reads.
    - `examples/04-sprint-cycle.md` around line 106 — it names two specific handoff artifacts by
      filename, which is the clearest case of narrative that cannot survive a path swap.
    - `agent-factory/workflows/` — the workflow files the examples narrate, so the replacement narrative
      describes the steps that actually run. Read the specific workflows each example walks through
      rather than all 19.
    - `agent-factory/roles/orchestrator.md` — the decompose-and-enqueue behaviour the narratives must
      describe.
    - The shared-verified-context mechanism as it actually ships: `scripts/context-io.ts` and the
      workflow that governs shared context notes, so the narrative names real artifacts rather than
      plausible ones.
    - `.planning/ROADMAP.md` lines 102-110 — obligation 1, the GAP-D1 coupling on
      `examples/03-ticket-to-pr.md`.
    - `.planning/phases/28-kit-consistency-audit/28-CONTEXT.md` — D-11.
  </read_first>
  <action>
Rewrite each example's narrative so it describes the flow that ships: roles pull shared verified
context and publish typed notes, and nothing is written until it is verified. Where a sentence today
says an agent writes a file into the deleted directory and the next agent reads it, replace the whole
sentence — and its neighbours if they depend on it — with what actually happens. A path swap alone is
forbidden: it would leave a narrative describing a relay that no longer exists, using paths that
resolve, which is worse than today's honest staleness because it looks correct.

`examples/04-sprint-cycle.md` around line 106 names two specific artifacts by filename. Those names
do not have replacements; the mechanism changed rather than the filename. Rewrite that passage to
describe what the roles actually exchange.

`examples/03-ticket-to-pr.md` is coupled to Phase 33's GAP-D1 live-capture work. Rewrite its handoff
lines, and confirm by reading that the lines you change are disjoint from the lines GAP-D1 will
replace with captured live-run output. Record the coupling in
`docs/audit/28-disposition-register.md`'s `## Recorded couplings and out-of-set notes` prose section —
naming Phase 33, stating that this file was rewritten in Phase 28 so Phase 33 works from the new text,
and stating that the two edits are disjoint. If you find they are NOT disjoint, stop and record that
instead of proceeding; an overlap is a real finding and guessing at it is worse than reporting it.

**It goes in prose, not in Table B, and the reason matters.** Table A holds only the derived roles and
workflows, so `examples/03-ticket-to-pr.md` has no Table A row — and the parse authority refuses a
Table B row whose `file` is absent from Table A. Forcing a row would break the D-03 equalities this
phase built. The prose section is where an out-of-set record belongs.

Verify after each file that the narrative still walks a reader end to end. An example that reads as a
sequence of corrections is not an example. Read each rewritten file start to finish before moving to
the next.

Re-run `node scripts/check-public-docs-vocabulary.js` after each file so the count falls visibly, and
record the descending sequence in the SUMMARY. A count that does not fall after a file was rewritten
means the rewrite missed a site.
  </action>
  <verify>
    <automated>node scripts/check-public-docs-vocabulary.js &amp;&amp; grep -rc "agent-factory/handoffs/" examples/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "agent-factory/handoffs/" examples/` returns 0 for all five files.
    - Each rewritten file was read start to finish after the rewrite and reads as a continuous
      narrative; the SUMMARY states this per file and names one passage per file that was restructured
      rather than substituted, which is the evidence of re-narration rather than replacement.
    - `examples/04-sprint-cycle.md`'s passage around line 106 no longer names an artifact by a filename
      that does not exist; verify every filename it names resolves on disk or is clearly a user
      artifact, and list them in the SUMMARY.
    - A finding row for the `examples/03-ticket-to-pr.md` Phase 33 coupling exists in
      `docs/audit/28-disposition-register.md` Table B with disposition `accepted` and a reason naming
      Phase 33, and the corresponding file row's `findings` count was incremented.
    - The SUMMARY records the descending hit count after each file, ending at 0.
    - `git diff --stat CHANGELOG.md docs/initial/ docs/design/` is empty.
  </acceptance_criteria>
  <done>
    All five examples describe the shared verified context flow, the 14 dead references are gone
    because the sentences holding them were rewritten, and the Phase 33 coupling is recorded rather
    than left to be rediscovered.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete the two hygiene directories, and measure that the installer does not care</name>
  <files>agent-factory/handoffs/.gitkeep, agent-factory/examples/.gitkeep</files>
  <read_first>
    - `install/install.ts` around line 1065 — the `cpSync` of the whole `agent-factory` tree into the
      kit root, which is what makes both directories user-visible today.
    - `install/uninstall.ts` — the removal path, to establish whether it derives what it removes or
      names it. This is the half most likely to carry a literal.
    - `scripts/dead-vocabulary.ts` — `RETIRED_PATH_FORMS`, which already asserts zero hits on the
      handoffs path across the shipped kit, so deleting the directory cannot break that assertion.
    - `scripts/check-kit-refs.ts` lines 36-93 — the SCAN set, to confirm neither directory is a named
      member whose absence would change the walk.
    - `scripts/validate-agent-factory.ts` — the structure validator, to confirm it does not require
      either directory to exist.
    - `.planning/phases/28-kit-consistency-audit/28-CONTEXT.md` — D-12, and the code-context note that
      the installer and uninstall mirror both track by derivation so no installer literal should need
      editing, followed by the instruction to verify this rather than assume it.
  </read_first>
  <action>
Measure before deleting. Run the installer against a scratch target directory with the directories
still present, capture the full output and the resulting file listing, then run the uninstaller
against the same target and capture its output and the listing again. Keep both captures.

Then grep `install/`, `scripts/` and `hooks/` for a literal naming either directory. Record what you
find. If a literal exists, the code-context note's expectation was wrong and the finding is recorded
as such — do not quietly edit around it and do not treat the expectation as the measurement.

Delete `agent-factory/handoffs/.gitkeep` and `agent-factory/examples/.gitkeep`, which removes both
directories from git since each holds nothing else.

Re-run the installer and uninstaller against a fresh scratch target and capture both again. Compare
the two pairs of captures. The expected difference is exactly the two absent directories and nothing
else — no new warning, no new unreadable-path finding, no changed exit code. If anything else differs,
that is the finding, and it is reported rather than absorbed.

Record both deletions with their reasons in `docs/audit/28-disposition-register.md`'s
`## Recorded couplings and out-of-set notes` prose section — the same section the Phase 33 coupling
went into, and for the same reason. Table A is derived from `listRoles()` and `listWorkflows()`, so a
directory has no row there, and the parse authority refuses a Table B row whose `file` is absent from
Table A. Do not force a row that breaks the equalities this phase built. Each entry names the
directory, states why it was deleted — one held only a placeholder file and is named for a concept
Phase 24 deleted, the other was empty — and states that both shipped to every user until now.

Run every repo gate afterwards: the foundation guards, the kit-refs gate, the validator, the public-
docs gate, the anchor gate and the freshness gate.
  </action>
  <verify>
    <automated>test ! -d agent-factory/handoffs &amp;&amp; test ! -d agent-factory/examples &amp;&amp; node scripts/check-foundation-guards.js &amp;&amp; node scripts/check-kit-refs.js &amp;&amp; VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js &amp;&amp; node scripts/check-public-docs-vocabulary.js</automated>
  </verify>
  <acceptance_criteria>
    - Neither directory exists; `git status --porcelain agent-factory/` shows the two deletions and
      nothing else.
    - The before-and-after installer captures differ only by the two absent directories. The SUMMARY
      contains the `diff` of the two output captures, and it contains no line other than those two.
    - The uninstaller exits with the same code before and after, and its output differs only by the
      two absent directories.
    - The grep for installer literals naming either directory is recorded in the SUMMARY with its
      result, whether that result is zero hits or not.
    - `node scripts/check-public-docs-vocabulary.js` exits **0** with a PASS line naming the counts it
      read — the D-24 transition completes here.
    - `node scripts/check-foundation-guards.js`, `node scripts/check-kit-refs.js`,
      `VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js`,
      `node scripts/check-claim-anchors.js` and `npm run freshness` all exit 0.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0.
    - The deletion of each directory is recorded with its reason, and the SUMMARY states where it was
      recorded and why that location was chosen.
  </acceptance_criteria>
  <precondition>
    A writable scratch directory outside the repository is available for the installer and uninstaller
    runs. Both scripts are run against a scratch target, never against this repository.
  </precondition>
  <done>
    Both directories are gone, the installer and uninstaller are proven indifferent by measurement
    rather than by reading, and the wave-1 drift guard exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → every installed kit | `install/install.ts` copies the whole `agent-factory/` tree to the kit root of every install. Anything wrong in there is wrong on every user's machine. |
| the rewrite → 28-04's registry | A rewrite that touches an anchored sentence changes the registry's baseline. The registry is Phase 30's input contract. |
| deleted directories → the installer and uninstaller | Removing a shipped directory changes what the install and uninstall paths walk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-27 | Spoofing | the re-narrated examples | high | mitigate | A path swap that leaves the relay narrative intact produces documentation that looks correct and describes a mechanism that does not exist — a worse state than honest staleness, and one no grep can detect because every path resolves. Mitigated by requiring re-narration rather than substitution, by reading each file end to end after the rewrite, and by requiring the SUMMARY to name one restructured passage per file as the evidence. |
| T-28-28 | Tampering | anchored claim text | high | mitigate | Rewriting these documents is exactly the event D-16's bijection exists to survive. Mitigated by running the anchor gate after the edit, by requiring the registry flip in the same commit, and by verifying the same-commit property from `git log --name-only` rather than asserting it. |
| T-28-29 | Denial of Service | the installer after directory deletion | medium | mitigate | A removed directory that some code path names as a literal would make the installer fail on every user's machine. Mitigated by measuring the installer and uninstaller before and after against a scratch target and requiring the output `diff` to contain only the two expected lines — the code-context note's expectation is treated as a hypothesis to test, not a fact to rely on. |
| T-28-30 | Repudiation | the D-24 green transition | high | mitigate | A guard reported green without being run would make the phase's central demonstration fiction. Mitigated by requiring the exit-0 run with its PASS line in the automated verify and the descending per-file hit counts in the SUMMARY, so the transition is a recorded sequence rather than a claimed endpoint. |
| T-28-31 | Elevation of Privilege | an unregistered new claim | medium | mitigate | A rewrite naturally produces new assertive sentences, and an unregistered one is D-16's residual arriving by this plan's own hand. Mitigated by the explicit prohibition and by requiring a registry row and anchor for any new claim introduced. |
| T-28-32 | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and does not edit `package.json` at all. Confirm `git diff package.json` is empty for this plan's commits. No package-legitimacy checkpoint is required because no install occurs. |

**ASVS L1, block on `high`.** T-28-27, T-28-28, T-28-30 and T-28-32 are `high`. The first three are
mitigated by mechanisms in this plan's acceptance criteria; T-28-32 is accepted on the measured basis
that no install occurs and no manifest is touched.
</threat_model>

<verification>
- `node scripts/check-public-docs-vocabulary.js` exits 0 with a PASS line naming its counts, and the
  before-and-after transcript against wave 1's exit-1 run is recorded.
- `node scripts/check-claim-anchors.js` exits 0.
- `grep -rc "agent-factory/handoffs/" examples/` returns 0 for all five files;
  `grep -ric "handoff packet"` returns 0 for the four top-level documents.
- Neither hygiene directory exists, and the installer and uninstaller output `diff` contains only the
  two expected lines.
- `git log --name-only` shows the prose edits and the registry flip in one commit.
- `node scripts/check-foundation-guards.js`, `node scripts/check-kit-refs.js`,
  `VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js`, `npm run freshness` and
  `npx vitest run --exclude '**/scripts/e2e/**'` all exit 0.
- `git diff --stat CHANGELOG.md docs/initial/ docs/design/` is empty.
</verification>

<success_criteria>
Every document a user reads describes decompose-and-enqueue over a shared verified context, the five
examples narrate the flow that ships rather than the relay that was deleted, two directories named for
a deleted concept no longer reach any user's machine, and the guard that was watched failing in wave 1
now passes — with every claim that moved carried by a registry row in the same commit.
</success_criteria>

<output>
Create `.planning/phases/28-kit-consistency-audit/28-05-SUMMARY.md` when done. It MUST record: the
before-and-after transcripts of the drift guard; the descending per-file hit counts; the `diff` of the
installer output captures; the registry ids flipped and their new statuses; the quoted `AGENTS.md`
statements proving the internal contradiction is resolved; the quoted `CLAUDE.md` platform-fact
sentence proving the routing verb was not banned; and the dispatch-table finding recorded rather than
fixed.
</output>

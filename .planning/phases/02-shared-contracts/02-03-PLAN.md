---
phase: 02-shared-contracts
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/checklists/definition-of-ready.md
  - agent-factory/checklists/definition-of-done.md
  - agent-factory/checklists/pr-review-checklist.md
  - agent-factory/checklists/security-nfr-checklist.md
  - agent-factory/checklists/uat-checklist.md
  - agent-factory/checklists/definition-of-done-enterprise.md
  - agent-factory/checklists/compliance-checklist.md
  - agent-factory/checklists/accessibility-checklist.md
  - agent-factory/checklists/observability-slo-checklist.md
  - agent-factory/checklists/release-readiness-checklist.md
  - agent-factory/checklists/00-index.md
autonomous: true
requirements: [CHECK-01, CHECK-02]
must_haves:
  truths:
    - "All 10 checklists exist under agent-factory/checklists/, each reproduced verbatim from spec §9.1–9.10 (D-00)"
    - "definition-of-done-enterprise.md body begins literally 'All of lean DoD, plus:' (superset, not a rewrite)"
    - "Each checklist carries kind: checklist + tier: lean|enterprise frontmatter per the LOCKED D-14 assignment"
    - "checklists/00-index.md groups all 10 lean vs enterprise and states the mode-gating rule (Orchestrator applies lean DoD in lean mode, enterprise DoD in enterprise mode)"
    - "Security/compliance/observability checklists are clear voice and reproduce controls verbatim — never fabricated (no-fabrication constraint, D-00)"
  artifacts:
    - path: "agent-factory/checklists/definition-of-done-enterprise.md"
      provides: "§9.3 enterprise DoD superset; tier: enterprise"
      contains: "All of lean DoD, plus"
    - path: "agent-factory/checklists/definition-of-ready.md"
      provides: "§9.1 DoR; tier: lean; the source ticket-ready-packet.md mirrors"
      contains: "Given/When/Then"
    - path: "agent-factory/checklists/00-index.md"
      provides: "lean-vs-enterprise grouping + mode-gating rule (human-readable tier split)"
      contains: "enterprise"
  key_links:
    - from: "agent-factory/checklists/00-index.md"
      to: "agent-factory/config/factory.config.json"
      via: "mode-gating rule: lean DoD in lean mode, enterprise DoD in enterprise mode (config mode field)"
      pattern: "mode"
    - from: "agent-factory/checklists/security-nfr-checklist.md"
      to: "plans/nfr-catalog.md"
      via: "performance impact vs NFR catalog (§9.5 line)"
      pattern: "NFR"
---

<objective>
Author all 10 gate checklists plus `00-index.md` under `agent-factory/checklists/`, satisfying CHECK-01 (5 lean) and CHECK-02 (5 enterprise). Every checklist body is reproduced VERBATIM from spec §9.1–9.10 (D-00 — paraphrasing or reordering a bullet is a defect). Each gets `kind: checklist` + `tier: lean|enterprise` frontmatter per the LOCKED D-14 assignment. The `00-index.md` groups the 10 lean-vs-enterprise and states the mode-gating rule, satisfying the human-readable "clearly distinguished" criterion (SC#3).

Purpose: These checklists are the gate contracts. The Orchestrator (Phase 3) applies lean DoD in lean mode and the enterprise superset in enterprise mode; the Phase-6 validator checks each file's presence and sections. The security/compliance checklists touch safety — verbatim fidelity and clear voice are mandatory, and no control may be fabricated.
Output: 10 verbatim checklists + 1 tier index, with zero fake data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-contracts/02-CONTEXT.md
@.planning/phases/02-shared-contracts/02-RESEARCH.md
@.planning/phases/02-shared-contracts/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5 lean checklists (verbatim §9.1, §9.2, §9.4, §9.5, §9.10)</name>
  <files>agent-factory/checklists/definition-of-ready.md, agent-factory/checklists/definition-of-done.md, agent-factory/checklists/pr-review-checklist.md, agent-factory/checklists/security-nfr-checklist.md, agent-factory/checklists/uat-checklist.md</files>
  <read_first>
    - docs/initial/agent_factory_builder_spec_v2.md lines 877-901 (§9.1 definition-of-ready, §9.2 definition-of-done VERBATIM)
    - docs/initial/agent_factory_builder_spec_v2.md lines 919-943 (§9.4 pr-review-checklist, §9.5 security-nfr-checklist VERBATIM)
    - docs/initial/agent_factory_builder_spec_v2.md lines 989-1000 (§9.10 uat-checklist VERBATIM)
    - .planning/phases/02-shared-contracts/02-CONTEXT.md lines 51-53 (D-13/D-14 — minimal frontmatter; tier assignment LOCKED: these 5 are tier: lean)
    - .planning/phases/02-shared-contracts/02-PATTERNS.md lines 57-78, 131-139 (Shared Pattern B frontmatter shape + Group 3 checklist assignment; author definition-of-ready FIRST since ticket-ready-packet derives from it)
    - plans/nfr-catalog.md (frozen — security-nfr-checklist line 'performance impact vs NFR catalog' cites this; cite, do NOT redefine)
  </read_first>
  <action>
    Author each of the 5 lean checklists with `kind: checklist` + `tier: lean` frontmatter (D-13/D-14), an `# Title` heading, then the §9 bullet body VERBATIM (transcribe directly from the spec for byte-fidelity — do NOT reword or reorder any bullet, D-00):
    - definition-of-ready.md ← §9.1 (lines 878-888): the 9 checks (problem clear; scope and out-of-scope clear; acceptance criteria clear (Given/When/Then); dependencies known; security/NFR triggers marked; test notes present; size assigned; priority assigned; no major unresolved blocker). Author this FIRST — ticket-ready-packet.md (Plan 01) mirrors it.
    - definition-of-done.md ← §9.2 (lines 890-901): the 9 lean DoD bullets including the literal final 'no fake command results'.
    - pr-review-checklist.md ← §9.4 (lines 919-929): incl. the literal '(enterprise) coverage + scans + a11y + perf budget satisfied' bullet.
    - security-nfr-checklist.md ← §9.5 (lines 931-943): the 10 bullets incl. 'performance impact vs NFR catalog'. Clear voice mandatory (security content; brand §4.3 forbids grug voice). Reproduce controls verbatim — never invent a control (no-fabrication).
    - uat-checklist.md ← §9.10 (lines 989-1000): the 9 UAT bullets.

    Clear professional voice throughout (D-00). No fake data; no grugops-specific content (D-03/D-04). Frontmatter `tier: lean` on all 5.
  </action>
  <verify>
    <automated>for f in definition-of-ready definition-of-done pr-review-checklist security-nfr-checklist uat-checklist; do test -f "agent-factory/checklists/$f.md" || { echo "MISSING $f.md"; exit 1; }; grep -q '^kind: checklist' "agent-factory/checklists/$f.md" || { echo "$f missing kind"; exit 1; }; grep -q '^tier: lean' "agent-factory/checklists/$f.md" || { echo "$f wrong/missing tier (must be lean)"; exit 1; }; done; grep -q 'Given/When/Then' agent-factory/checklists/definition-of-ready.md || { echo "DoR missing Given/When/Then"; exit 1; }; grep -q 'no fake command results' agent-factory/checklists/definition-of-done.md || { echo "DoD missing no-fake-command bullet"; exit 1; }; grep -qi 'NFR catalog' agent-factory/checklists/security-nfr-checklist.md || { echo "security-nfr missing NFR catalog citation"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f` passes for all 5: definition-of-ready, definition-of-done, pr-review-checklist, security-nfr-checklist, uat-checklist
    - each has `grep -q '^kind: checklist'` AND `grep -q '^tier: lean'`
    - `grep -q 'Given/When/Then' agent-factory/checklists/definition-of-ready.md` passes (§9.1 fidelity)
    - `grep -q 'no fake command results' agent-factory/checklists/definition-of-done.md` passes (§9.2 fidelity)
    - `grep -qi 'NFR catalog' agent-factory/checklists/security-nfr-checklist.md` passes (§9.5 NFR-catalog citation)
    - no fake data: `! grep -rEq 'ABC-[0-9]{3}' agent-factory/checklists/definition-of-ready.md agent-factory/checklists/definition-of-done.md`
  </acceptance_criteria>
  <done>
    All 5 lean checklists exist with `kind: checklist` + `tier: lean` frontmatter and §9.1/9.2/9.4/9.5/9.10 bodies verbatim; definition-of-ready carries the Given/When/Then check that ticket-ready-packet mirrors; security-nfr is clear voice with controls reproduced verbatim; zero fake data.
  </done>
</task>

<task type="auto">
  <name>Task 2: 5 enterprise checklists (verbatim §9.3, §9.6, §9.7, §9.8, §9.9)</name>
  <files>agent-factory/checklists/definition-of-done-enterprise.md, agent-factory/checklists/compliance-checklist.md, agent-factory/checklists/accessibility-checklist.md, agent-factory/checklists/observability-slo-checklist.md, agent-factory/checklists/release-readiness-checklist.md</files>
  <read_first>
    - docs/initial/agent_factory_builder_spec_v2.md lines 903-917 (§9.3 definition-of-done-enterprise VERBATIM — body begins literally 'All of lean DoD, plus:')
    - docs/initial/agent_factory_builder_spec_v2.md lines 945-975 (§9.6 compliance, §9.7 accessibility, §9.8 observability-slo VERBATIM)
    - docs/initial/agent_factory_builder_spec_v2.md lines 977-987 (§9.9 release-readiness-checklist VERBATIM)
    - .planning/phases/02-shared-contracts/02-CONTEXT.md lines 51-53 (D-14 tier assignment LOCKED: these 5 are tier: enterprise)
    - plans/nfr-catalog.md and agent-factory/config/factory.config.json (frozen — observability-slo cites SLO/target from nfr-catalog; enterprise DoD cites coverage threshold from config quality; cite, do NOT redefine)
    - docs/initial/grugops_brand_manual.md lines 105-161 (clear voice mandatory in compliance/security content)
  </read_first>
  <action>
    Author each of the 5 enterprise checklists with `kind: checklist` + `tier: enterprise` frontmatter (D-13/D-14), an `# Title` heading, then the §9 body VERBATIM (transcribe directly from the spec — no rewording/reordering, D-00):
    - definition-of-done-enterprise.md ← §9.3 (lines 904-917): the body MUST begin literally 'All of lean DoD, plus:' then the 11 superset bullets verbatim (coverage meets threshold (config); accessibility checklist passed where UI changed (WCAG target); performance within NFR budget; security/NFR result is PASS or PASS_WITH_RISKS (never BLOCKED); compliance checklist passed where data is sensitive; dependencies scanned/justified/license/SBOM; secrets scan clean; observability in place; ADR written for any structural decision; release notes entry drafted; traceability complete: requirement -> ticket -> code -> test -> (release)).
    - compliance-checklist.md ← §9.6 (lines 945-955): the 8 bullets incl. 'control -> evidence mapping recorded (SOC2/ISO 27001/PCI as set)'. Clear voice mandatory; reproduce control names verbatim — never fabricate a control (no-fabrication).
    - accessibility-checklist.md ← §9.7 (lines 957-965): the 6 bullets incl. 'target standard (e.g. WCAG 2.2 AA) noted'.
    - observability-slo-checklist.md ← §9.8 (lines 967-975): the 6 bullets incl. 'SLO/target referenced from nfr-catalog' and 'dashboard/runbook link in memory-bank/70-runbook.md'.
    - release-readiness-checklist.md ← §9.9 (lines 977-987): the 8 bullets incl. 'named human approval recorded' and 'production action will be human-confirmed'.

    Clear professional voice throughout (D-00). No fake data; no grugops-specific content (D-03/D-04). Frontmatter `tier: enterprise` on all 5.
  </action>
  <verify>
    <automated>for f in definition-of-done-enterprise compliance-checklist accessibility-checklist observability-slo-checklist release-readiness-checklist; do test -f "agent-factory/checklists/$f.md" || { echo "MISSING $f.md"; exit 1; }; grep -q '^kind: checklist' "agent-factory/checklists/$f.md" || { echo "$f missing kind"; exit 1; }; grep -q '^tier: enterprise' "agent-factory/checklists/$f.md" || { echo "$f wrong/missing tier (must be enterprise)"; exit 1; }; done; grep -q 'All of lean DoD, plus' agent-factory/checklists/definition-of-done-enterprise.md || { echo "enterprise DoD not a superset"; exit 1; }; grep -qi 'SOC2' agent-factory/checklists/compliance-checklist.md || { echo "compliance missing control->evidence"; exit 1; }; grep -qi 'WCAG' agent-factory/checklists/accessibility-checklist.md || { echo "accessibility missing target standard"; exit 1; }; grep -qi 'human-confirmed' agent-factory/checklists/release-readiness-checklist.md || { echo "release-readiness missing human-confirm bullet"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f` passes for all 5 enterprise checklists
    - each has `grep -q '^kind: checklist'` AND `grep -q '^tier: enterprise'`
    - `grep -q 'All of lean DoD, plus' agent-factory/checklists/definition-of-done-enterprise.md` passes (superset, not rewrite)
    - `grep -qi 'SOC2' agent-factory/checklists/compliance-checklist.md` passes (§9.6 control->evidence verbatim)
    - `grep -qi 'WCAG' agent-factory/checklists/accessibility-checklist.md` passes (§9.7 target standard verbatim)
    - `grep -qi 'human-confirmed' agent-factory/checklists/release-readiness-checklist.md` passes (§9.9 safety bullet)
  </acceptance_criteria>
  <done>
    All 5 enterprise checklists exist with `kind: checklist` + `tier: enterprise` frontmatter and §9.3/9.6/9.7/9.8/9.9 bodies verbatim; enterprise DoD begins 'All of lean DoD, plus:'; compliance/accessibility/release controls reproduced verbatim in clear voice; zero fake data.
  </done>
</task>

<task type="auto">
  <name>Task 3: checklists/00-index.md — tier grouping + mode-gating rule</name>
  <files>agent-factory/checklists/00-index.md</files>
  <read_first>
    - .planning/phases/02-shared-contracts/02-CONTEXT.md lines 53 (D-14 — the index lists all 10 grouped lean vs enterprise and states the mode-gating rule in one place; satisfies the human-readable SC#3)
    - docs/initial/agent_factory_builder_spec_v2.md line 875 (the Orchestrator applies definition-of-done.md in lean mode and definition-of-done-enterprise.md in enterprise mode — the mode-gating rule to state verbatim in intent)
    - agent-factory/README.md (the existing orientation-doc prose/table style — Shared Pattern D; match the tone)
    - .planning/phases/02-shared-contracts/02-PATTERNS.md lines 100-105, 141-147 (Shared Pattern D + Group 4 index assignment; frontmatter on index is Claude's discretion — decide once, apply to both indexes consistently with memory-bank/00-index.md in Plan 04)
    - agent-factory/config/factory.config.json (frozen — the `mode` field lean|enterprise that drives gating; cite by name)
  </read_first>
  <action>
    Author `agent-factory/checklists/00-index.md` in the README orientation style (Shared Pattern D): a short `# Title` + `_Updated:_` opener, brief clear-voice prose, and a summary table listing all 10 checklists grouped LEAN (definition-of-ready, definition-of-done, pr-review-checklist, security-nfr-checklist, uat-checklist) vs ENTERPRISE (definition-of-done-enterprise, compliance-checklist, accessibility-checklist, observability-slo-checklist, release-readiness-checklist) per the LOCKED D-14 assignment. State the one-line mode-gating rule explicitly: the Orchestrator applies the lean Definition of Done in `mode: lean` and the enterprise superset in `mode: enterprise` (cite config `mode` by name). Keep it small/anti-bloat (MEM-01 style rule). Decide the index frontmatter convention (e.g. `kind: index` or omit) and apply the SAME choice to memory-bank/00-index.md (Plan 04). Clear voice, lowercase `grugops`, no fake data.
  </action>
  <verify>
    <automated>test -f agent-factory/checklists/00-index.md || { echo "MISSING 00-index.md"; exit 1; }; grep -qi 'lean' agent-factory/checklists/00-index.md && grep -qi 'enterprise' agent-factory/checklists/00-index.md && grep -qi 'mode' agent-factory/checklists/00-index.md || { echo "index missing lean/enterprise/mode grouping"; exit 1; }; for c in definition-of-ready definition-of-done-enterprise compliance-checklist release-readiness-checklist; do grep -q "$c" agent-factory/checklists/00-index.md || { echo "index missing $c"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f agent-factory/checklists/00-index.md` passes
    - `grep -qi 'lean'` AND `grep -qi 'enterprise'` AND `grep -qi 'mode'` all pass (tier grouping + gating rule present)
    - the index names all 10 checklist files (spot-checked via `grep -q` for definition-of-ready, definition-of-done-enterprise, compliance-checklist, release-readiness-checklist)
    - no fake data; lowercase `grugops` if the brand name appears
  </acceptance_criteria>
  <done>
    checklists/00-index.md exists, groups all 10 checklists lean vs enterprise per D-14, and states the mode-gating rule (lean DoD in lean mode, enterprise DoD in enterprise mode, keyed on config `mode`) — satisfying the human-readable tier-split criterion. Small, clear voice, no fake data.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — static markdown) | Static gate-checklist markdown. No code/inputs/data flow. Relevant surface is content integrity of security/compliance checklists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering / Repudiation (fabricated control) | compliance-checklist / security-nfr-checklist controls | mitigate | Reproduce §9.5/§9.6 verbatim; never invent or omit a control. A fabricated or dropped control silently weakens a downstream security/compliance gate. `grep -qi 'SOC2'` + `grep -qi 'NFR catalog'` assert key controls are present. |
| T-02-08 | Tampering (gate weakening) | definition-of-done-enterprise superset integrity | mitigate | Body must begin literally 'All of lean DoD, plus:' so the enterprise gate is a strict superset, not a divergent rewrite. `grep -q 'All of lean DoD, plus'` asserts it. |
| T-02-09 | Information disclosure (trust erosion) | grug voice in security/compliance checklist | mitigate | Clear voice locked (D-00, brand §4.3). Voice review advisory. |

No high-severity code-level threats exist for this markdown-only plan; nothing to block on.
</threat_model>

<verification>
Full-plan structural sweep after all 3 tasks:
- All 11 files exist: `for f in definition-of-ready definition-of-done definition-of-done-enterprise pr-review-checklist security-nfr-checklist compliance-checklist accessibility-checklist observability-slo-checklist release-readiness-checklist uat-checklist 00-index; do test -f "agent-factory/checklists/$f.md" || echo "MISSING $f"; done`
- Tier frontmatter on all 10 checklists (index may differ): `grep -L '^tier:' agent-factory/checklists/definition-of-ready.md agent-factory/checklists/definition-of-done.md agent-factory/checklists/definition-of-done-enterprise.md agent-factory/checklists/pr-review-checklist.md agent-factory/checklists/security-nfr-checklist.md agent-factory/checklists/compliance-checklist.md agent-factory/checklists/accessibility-checklist.md agent-factory/checklists/observability-slo-checklist.md agent-factory/checklists/release-readiness-checklist.md agent-factory/checklists/uat-checklist.md` (expect empty)
- Tier assignment correct: the 5 lean files `grep -q '^tier: lean'`, the 5 enterprise files `grep -q '^tier: enterprise'`
- Enterprise DoD is a superset: `grep -q 'All of lean DoD, plus' agent-factory/checklists/definition-of-done-enterprise.md`
- No-fake-data invariant: `! grep -rEq 'ABC-[0-9]{3}' agent-factory/checklists/`
</verification>

<success_criteria>
- All 10 checklists + 00-index exist (CHECK-01, CHECK-02, ROADMAP SC#3)
- Bodies reproduced verbatim from §9.1–9.10; enterprise DoD begins 'All of lean DoD, plus:'
- Each checklist carries `kind: checklist` + `tier: lean|enterprise` per the LOCKED D-14 assignment
- 00-index groups the 10 lean-vs-enterprise and states the mode-gating rule (machine-readable tier + human-readable index, both per D-14)
- Security/compliance controls reproduced verbatim in clear voice; zero fake data
</success_criteria>

<output>
Create `.planning/phases/02-shared-contracts/02-03-SUMMARY.md` when done
</output>

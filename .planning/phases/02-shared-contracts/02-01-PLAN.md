---
phase: 02-shared-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/handoffs/universal-handoff.md
  - agent-factory/handoffs/business-handoff.md
  - agent-factory/handoffs/product-handoff.md
  - agent-factory/handoffs/system-handoff.md
  - agent-factory/handoffs/architecture-handoff.md
  - agent-factory/handoffs/implementation-handoff.md
  - agent-factory/handoffs/qe-handoff.md
  - agent-factory/handoffs/security-nfr-handoff.md
  - agent-factory/handoffs/uat-handoff.md
  - agent-factory/handoffs/ticket-ready-packet.md
  - agent-factory/handoffs/implementation-ready-packet.md
autonomous: true
requirements: [HAND-01]
must_haves:
  truths:
    - "All 11 core handoff templates exist under agent-factory/handoffs/ and are copy-paste usable (each independently complete)"
    - "The universal header carries the v2 'Ticket ID:' field and the v2 '## Trace updates' section, byte-identical across every core handoff"
    - "Each per-role handoff (product/system/architecture/implementation/qe/security-nfr/uat) contains the exact §5.A section set, section names transcribed verbatim — not invented"
    - "ticket-ready-packet.md carries one field per definition-of-ready.md check and cross-references that file by name (D-09)"
    - "No fake/example ticket rows or grugops-specific content appears (D-03/D-04); clear professional voice throughout (D-00)"
  artifacts:
    - path: "agent-factory/handoffs/universal-handoff.md"
      provides: "Canonical §8 universal header (Ticket ID + Trace updates), kind: handoff frontmatter"
      contains: "Trace updates"
    - path: "agent-factory/handoffs/security-nfr-handoff.md"
      provides: "§5.A.10 security/NFR handoff with result PASS | PASS_WITH_RISKS | BLOCKED"
      contains: "PASS_WITH_RISKS"
    - path: "agent-factory/handoffs/ticket-ready-packet.md"
      provides: "DoR-satisfying bundle, fields mirror definition-of-ready.md, cross-referenced"
      contains: "definition-of-ready"
  key_links:
    - from: "agent-factory/handoffs/ticket-ready-packet.md"
      to: "agent-factory/checklists/definition-of-ready.md"
      via: "cross-reference by filename; one packet field per DoR check"
      pattern: "definition-of-ready"
    - from: "agent-factory/handoffs/universal-handoff.md"
      to: "plans/traceability.md"
      via: "## Trace updates field links IDs/files into the traceability matrix"
      pattern: "Trace updates"
---

<objective>
Author the universal handoff header and all 11 core handoff templates under `agent-factory/handoffs/`, satisfying HAND-01. The universal header is reproduced verbatim from spec §8 (gaining the v2 `Ticket ID:` and `## Trace updates` fields per D-00); the 7 per-role handoffs are pre-filled with their exact §5.A.5–5.A.11 output section sets (D-08); the 3 under-specified files (business-handoff, ticket-ready-packet, implementation-ready-packet) are structured per D-09. Every file gets a minimal `kind: handoff` + stage frontmatter (D-13).

Purpose: These templates are the I/O contracts that Phase-3 roles cite by exact filename and section name and Phase-4 workflows produce. Locking them verbatim now prevents downstream drift — a paraphrased section name silently breaks roles, workflows, and the Phase-6 validator.
Output: 11 copy-paste-usable markdown handoff templates, generic and project-agnostic, with zero fake data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-contracts/02-CONTEXT.md
@.planning/phases/02-shared-contracts/02-RESEARCH.md
@.planning/phases/02-shared-contracts/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Universal handoff header + 7 pre-filled per-role handoffs</name>
  <files>agent-factory/handoffs/universal-handoff.md, agent-factory/handoffs/product-handoff.md, agent-factory/handoffs/system-handoff.md, agent-factory/handoffs/architecture-handoff.md, agent-factory/handoffs/implementation-handoff.md, agent-factory/handoffs/qe-handoff.md, agent-factory/handoffs/security-nfr-handoff.md, agent-factory/handoffs/uat-handoff.md</files>
  <read_first>
    - docs/initial/agent_factory_builder_spec_v2.md lines 784-808 (§8 universal header VERBATIM — source of truth; includes the v2 `Ticket ID:` field and the v2 `## Trace updates` section, with the spec's inline `# (v2) ...` comments)
    - docs/initial/agent_factory_builder_spec_v2.md lines 425-502 (§5.A.5–5.A.11 — the exact per-role output section lists to transcribe BELOW the header)
    - .planning/phases/02-shared-contracts/02-RESEARCH.md lines 266-301 (the Per-Role Handoff Section Map + Universal Header block — pre-extracted, but DIFF against the spec for byte-fidelity)
    - .planning/phases/02-shared-contracts/02-PATTERNS.md lines 27-78 (Shared Pattern A empty-but-shaped style + Shared Pattern B `kind:` frontmatter shape — the house style the bodies sit in)
    - plans/traceability.md (frozen — the matrix the universal `## Trace updates` field links into; cite by name, do NOT redefine its columns and do NOT add board-move behavior)
  </read_first>
  <action>
    Author `agent-factory/handoffs/universal-handoff.md` as the canonical header: a `kind: handoff` + stage frontmatter block (2-3 high-signal fields; stage e.g. `universal`), then the §8 header VERBATIM (lines 788-808) — `# Handoff: <name>`, `## Source` (Request / Repo / Branch / `Ticket ID:` / Date), then `## Goal`, `## Scope` (`### In scope`, `### Out of scope`), `## Inputs used`, `## Decisions`, `## Risks`, `## Trace updates`, `## Next agent`, `## Next action`. Keep the spec's inline comments (`# (v2) for traceability`, `# (v2) IDs/files this links in plans/traceability.md`).

    For each of the 7 per-role files, INLINE the full universal header byte-identically (per RESEARCH A2 lower-drift recommendation: each file must be independently copy-paste-usable, so a pointer-only file is forbidden), then append that role's §5.A section set BELOW it. Transcribe section names verbatim from §5.A — do NOT invent parallel names. Section sources and required sections:
    - product-handoff.md ← §5.A.5: user value, scope, out of scope, acceptance criteria (Given/When/Then), dependencies, risks, test notes, security/NFR triggers, size estimate, priority. Frontmatter stage: product.
    - system-handoff.md ← §5.A.6: actors, use cases, business flows, state transitions, inputs/outputs, validation rules, permissions, data needs, API needs, integration points, error cases, open questions. Stage: system.
    - architecture-handoff.md ← §5.A.7: context, constraints, chosen design, alternatives rejected, module/component map, API contracts, data model, sequence flows, security assumptions, NFR impact (note it updates plans/nfr-catalog.md), migration impact, test strategy, ADRs, open questions. Stage: architecture.
    - implementation-handoff.md ← §5.A.8: ticket, branch, files changed, behavior changed, tests added, commands run, migration notes, docs updated, risks, remaining work. Stage: implementation.
    - qe-handoff.md ← §5.A.9: test scope, unit/integration/E2E coverage, manual test cases, regression risks, test data, commands run, flaky risk, coverage vs threshold, result, gaps. Stage: qe.
    - security-nfr-handoff.md ← §5.A.10: scope reviewed, threat notes, auth/permission, data/privacy, secret handling, input validation, rate-limit/abuse, performance budget vs NFR catalog, reliability/fallback, logging/monitoring, compliance notes, required fixes, accepted risks, result. The result line MUST state the three literal values `PASS | PASS_WITH_RISKS | BLOCKED`. Stage: security-nfr.
    - uat-handoff.md ← §5.A.11: UAT goal, entry criteria, test users/roles, test data, business scenarios, expected results, known limitations, rollback plan, signoff checklist with named human role, exit criteria. Stage: uat.

    Use clear professional voice (D-00 — grug voice forbidden; several touch security/safety). Ship empty-but-shaped: section headings + blank fields only, NO fake data, NO grugops-specific content (D-03/D-04). If adding an `_Updated: <date>_` opener, match the Phase-1 convention and pick one form applied consistently across every file in this plan (Claude's discretion). Do NOT add board-move or trace-update behavior — `## Trace updates` is a slot only; when/how to fill it is Phase-4 content.
  </action>
  <verify>
    <automated>for f in universal product system architecture implementation qe security-nfr uat; do test -f "agent-factory/handoffs/$f-handoff.md" || { echo "MISSING $f-handoff.md"; exit 1; }; done; for f in universal product system architecture implementation qe security-nfr uat; do grep -q 'Ticket ID' "agent-factory/handoffs/$f-handoff.md" || { echo "$f missing Ticket ID"; exit 1; }; grep -q '## Trace updates' "agent-factory/handoffs/$f-handoff.md" || { echo "$f missing Trace updates"; exit 1; }; grep -q '^kind: handoff' "agent-factory/handoffs/$f-handoff.md" || { echo "$f missing kind frontmatter"; exit 1; }; done; grep -q 'PASS_WITH_RISKS' agent-factory/handoffs/security-nfr-handoff.md || { echo "security-nfr missing result enum"; exit 1; }; grep -qi 'open questions' agent-factory/handoffs/system-handoff.md && grep -qi 'state transitions' agent-factory/handoffs/system-handoff.md || { echo "system handoff missing §5.A.6 sections"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f` passes for all 8 files: universal/product/system/architecture/implementation/qe/security-nfr/uat-handoff.md
    - `grep -q 'Ticket ID'` AND `grep -q '## Trace updates'` AND `grep -q '^kind: handoff'` pass for each of the 8 files (universal header inlined consistently)
    - `grep -q 'PASS_WITH_RISKS' agent-factory/handoffs/security-nfr-handoff.md` passes (result enum present)
    - `grep -qi 'state transitions' agent-factory/handoffs/system-handoff.md` and `grep -qi 'open questions' agent-factory/handoffs/system-handoff.md` pass (§5.A.6 sections present)
    - `grep -qi 'acceptance criteria' agent-factory/handoffs/product-handoff.md` passes (§5.A.5 sections present)
    - No fake data: `! grep -rEq 'ABC-[0-9]{3}' agent-factory/handoffs/universal-handoff.md agent-factory/handoffs/product-handoff.md` (no live ticket IDs in template bodies)
  </acceptance_criteria>
  <done>
    The universal header and 7 per-role handoffs exist, each independently copy-paste-usable with the inlined §8 header (Ticket ID + Trace updates), each carrying its exact §5.A section set transcribed verbatim, each with `kind: handoff` frontmatter, in clear voice, with zero fake/grugops-specific data.
  </done>
</task>

<task type="auto">
  <name>Task 2: 3 derived handoff files (business + 2 packets)</name>
  <files>agent-factory/handoffs/business-handoff.md, agent-factory/handoffs/ticket-ready-packet.md, agent-factory/handoffs/implementation-ready-packet.md</files>
  <read_first>
    - .planning/phases/02-shared-contracts/02-CONTEXT.md lines 41-44 (D-09 — the locked derived structure for these 3 under-specified files; content is fixed, only field order/wording is discretion)
    - docs/initial/agent_factory_builder_spec_v2.md lines 877-888 (§9.1 definition-of-ready VERBATIM — the exact 9 DoR checks ticket-ready-packet.md must carry one field per)
    - docs/initial/agent_factory_builder_spec_v2.md lines 788-808 (§8 universal header — inline it at the top of each of these 3 files for copy-paste usability)
    - agent-factory/handoffs/universal-handoff.md (the canonical header authored in Task 1 — copy it byte-identically)
    - .planning/phases/02-shared-contracts/02-RESEARCH.md lines 241-243 (Pattern 3 cross-reference integrity — ticket-ready-packet ⇄ definition-of-ready must not drift)
  </read_first>
  <action>
    Author the 3 derived files, each with `kind: handoff` + stage frontmatter (D-13), the inlined §8 universal header byte-identical to Task 1's output, then the D-09 body sections below it:
    - business-handoff.md (stage: business) = business-framing intake feeding BA/PM: problem, affected users, business value/outcome, constraints, success measure, in-scope, out-of-scope.
    - ticket-ready-packet.md (stage: ticket-ready) = the DoR-satisfying bundle: ticket ID, problem, scope/out-of-scope, acceptance criteria (Given/When/Then), dependencies, security/NFR triggers, test notes, size, priority — i.e., exactly the fields `definition-of-ready.md` (§9.1) checks. Carry ONE field per DoR check (problem clear / scope+out-of-scope clear / acceptance criteria Given-When-Then / dependencies known / security-NFR triggers marked / test notes present / size assigned / priority assigned / no major unresolved blocker). Include an explicit cross-reference line naming `definition-of-ready.md` (e.g. a note "fields satisfy checklists/definition-of-ready.md") so the two stay aligned (D-09, Pitfall 3).
    - implementation-ready-packet.md (stage: implementation-ready) = engineer's start bundle: ticket ID + branch target, relevant ADRs (reference `memory-bank/50-decisions/`), API/data contracts and system context (reference architecture/system handoffs), files likely touched, test strategy, commands to run.

    Clear voice, empty-but-shaped, zero fake data / no grugops-specific content (D-03/D-04). Section ordering inside these three is Claude's discretion as long as D-09's content is fully present and ticket-ready-packet stays aligned with the DoR checks.
  </action>
  <verify>
    <automated>for f in business-handoff ticket-ready-packet implementation-ready-packet; do test -f "agent-factory/handoffs/$f.md" || { echo "MISSING $f.md"; exit 1; }; grep -q '^kind: handoff' "agent-factory/handoffs/$f.md" || { echo "$f missing kind frontmatter"; exit 1; }; grep -q 'Ticket ID' "agent-factory/handoffs/$f.md" || { echo "$f missing Ticket ID (inlined universal header)"; exit 1; }; grep -q '## Trace updates' "agent-factory/handoffs/$f.md" || { echo "$f missing Trace updates"; exit 1; }; done; grep -q 'definition-of-ready' agent-factory/handoffs/ticket-ready-packet.md || { echo "ticket-ready-packet missing DoR cross-reference"; exit 1; }; grep -qi 'acceptance criteria' agent-factory/handoffs/ticket-ready-packet.md && grep -qi 'priority' agent-factory/handoffs/ticket-ready-packet.md || { echo "ticket-ready-packet missing DoR-mirroring fields"; exit 1; }; grep -qi 'affected users' agent-factory/handoffs/business-handoff.md || { echo "business-handoff missing D-09 fields"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f` passes for business-handoff.md, ticket-ready-packet.md, implementation-ready-packet.md
    - each of the 3 has `grep -q '^kind: handoff'`, `grep -q 'Ticket ID'`, `grep -q '## Trace updates'` (inlined universal header consistent with Task 1)
    - `grep -q 'definition-of-ready' agent-factory/handoffs/ticket-ready-packet.md` passes (cross-reference present)
    - `grep -qi 'acceptance criteria'` and `grep -qi 'priority'` pass in ticket-ready-packet.md (DoR-mirroring fields present)
    - `grep -qi 'affected users' agent-factory/handoffs/business-handoff.md` passes (D-09 business-framing fields present)
    - No fake data: `! grep -rEq 'ABC-[0-9]{3}' agent-factory/handoffs/ticket-ready-packet.md`
  </acceptance_criteria>
  <done>
    business-handoff.md, ticket-ready-packet.md, and implementation-ready-packet.md exist, each with the inlined universal header + `kind: handoff` frontmatter + their D-09 body; ticket-ready-packet carries one field per DoR check and cross-references definition-of-ready.md by name; clear voice; zero fake data.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — static markdown) | This plan authors static markdown contract templates. No executable code, no input handling, no auth, no data flow, no trust boundary crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (content integrity) | security-nfr-handoff.md result enum / §5.A section names | mitigate | Transcribe §5.A.10 verbatim; result line must read `PASS \| PASS_WITH_RISKS \| BLOCKED`. A paraphrased section name silently weakens the downstream security gate. `grep -q PASS_WITH_RISKS` asserts fidelity. |
| T-02-02 | Repudiation (false readiness) | ticket-ready-packet ⇄ definition-of-ready drift | mitigate | One packet field per DoR check + explicit cross-reference; prevents a "ready" packet that does not actually satisfy the DoR gate. |
| T-02-03 | Information disclosure | fake/example data seeded into a template | accept→mitigate | D-03 no-fake-data; `! grep -rEq 'ABC-[0-9]{3}'` invariant. No PII/secrets possible — templates ship empty. |

No high-severity code-level threats exist for this markdown-only plan; nothing to block on.
</threat_model>

<verification>
Full-plan structural sweep after both tasks:
- All 11 core handoff files exist: `for f in universal business product system architecture implementation qe security-nfr uat; do test -f "agent-factory/handoffs/$f-handoff.md" || echo "MISSING $f"; done; test -f agent-factory/handoffs/ticket-ready-packet.md; test -f agent-factory/handoffs/implementation-ready-packet.md`
- Universal header consistency across all 11: `grep -L 'Ticket ID' agent-factory/handoffs/universal-handoff.md agent-factory/handoffs/business-handoff.md agent-factory/handoffs/product-handoff.md agent-factory/handoffs/system-handoff.md agent-factory/handoffs/architecture-handoff.md agent-factory/handoffs/implementation-handoff.md agent-factory/handoffs/qe-handoff.md agent-factory/handoffs/security-nfr-handoff.md agent-factory/handoffs/uat-handoff.md agent-factory/handoffs/ticket-ready-packet.md agent-factory/handoffs/implementation-ready-packet.md` (expect EMPTY output) and same for `## Trace updates`
- Frontmatter present on all 11: `grep -L '^kind: handoff' agent-factory/handoffs/*.md` over the 11 core files expects empty (the 5 v2 files are Plan 02)
- No-fake-data invariant: `! grep -rEq 'ABC-[0-9]{3}' agent-factory/handoffs/` (over files this plan created)
</verification>

<success_criteria>
- All 11 core handoff templates exist and are individually copy-paste-usable (HAND-01, ROADMAP SC#1)
- The universal header carries `Ticket ID:` and `## Trace updates`, byte-identical across every core handoff
- Each per-role handoff contains its exact §5.A.5–5.A.11 section set, names transcribed verbatim
- ticket-ready-packet.md mirrors definition-of-ready.md field-for-field and cross-references it
- Zero fake data, no grugops-specific content, clear professional voice throughout
</success_criteria>

<output>
Create `.planning/phases/02-shared-contracts/02-01-SUMMARY.md` when done
</output>

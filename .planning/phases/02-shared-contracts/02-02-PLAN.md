---
phase: 02-shared-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/handoffs/release-handoff.md
  - agent-factory/handoffs/incident-postmortem.md
  - agent-factory/handoffs/retro-notes.md
  - agent-factory/handoffs/refinement-notes.md
  - agent-factory/handoffs/sprint-plan.md
autonomous: true
requirements: [HAND-02]
must_haves:
  truths:
    - "All 5 v2 handoff templates exist under agent-factory/handoffs/, each reproduced verbatim from spec §8.1–8.5"
    - "release-handoff.md carries the REL-xxxx title, the dev -> staging -> prod path, the human-approval line, and the Status enum READY_TO_RELEASE | BLOCKED | RELEASED"
    - "incident-postmortem.md is blameless: 'Root cause (systemic, not personal)' wording is preserved, INC-xxxx title, clear voice"
    - "retro-notes.md references plans/metrics.md metric names via its Metrics snapshot; sprint-plan.md mirrors plans/sprints/SPRINT-xx.md"
    - "Every v2 handoff carries minimal kind: handoff frontmatter (D-13); no fake data; clear professional voice (D-00)"
  artifacts:
    - path: "agent-factory/handoffs/release-handoff.md"
      provides: "§8.1 release handoff (REL-xxxx), Status enum + human approval line"
      contains: "READY_TO_RELEASE"
    - path: "agent-factory/handoffs/incident-postmortem.md"
      provides: "§8.2 blameless postmortem (INC-xxxx), systemic root cause"
      contains: "systemic, not personal"
    - path: "agent-factory/handoffs/retro-notes.md"
      provides: "§8.3 retro notes with Metrics snapshot, Keep / Stop / Start"
      contains: "Keep / Stop / Start"
  key_links:
    - from: "agent-factory/handoffs/release-handoff.md"
      to: "human approver"
      via: "Approved by (human role/name) line — humans decide, agents execute"
      pattern: "Approved by"
    - from: "agent-factory/handoffs/retro-notes.md"
      to: "plans/metrics.md"
      via: "## Metrics snapshot references the frozen metric names"
      pattern: "Metrics snapshot"
---

<objective>
Author the 5 v2 handoff templates under `agent-factory/handoffs/`, satisfying HAND-02. All five are reproduced VERBATIM from spec §8.1–8.5 (D-00): release-handoff (REL-xxxx), incident-postmortem (blameless, INC-xxxx), retro-notes, refinement-notes, and sprint-plan (mirror of plans/sprints/SPRINT-xx.md). Each gets a minimal `kind: handoff` + stage frontmatter (D-13) above the spec-verbatim body.

Purpose: These v2 packets are produced by Phase-4 ceremony/enterprise workflows (12-release, 13-incident, retro, refinement, sprint planning) and cite the frozen ID scheme and metric names. The blameless postmortem and the release approval gate touch safety and money — verbatim fidelity and clear voice are mandatory.
Output: 5 copy-paste-usable markdown templates, verbatim from §8, with zero fake data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-contracts/02-CONTEXT.md
@.planning/phases/02-shared-contracts/02-RESEARCH.md
@.planning/phases/02-shared-contracts/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: release-handoff + incident-postmortem (verbatim §8.1, §8.2)</name>
  <files>agent-factory/handoffs/release-handoff.md, agent-factory/handoffs/incident-postmortem.md</files>
  <read_first>
    - docs/initial/agent_factory_builder_spec_v2.md lines 812-846 (§8.1 release-handoff and §8.2 incident-postmortem VERBATIM — the byte-faithful source of both templates)
    - .planning/phases/02-shared-contracts/02-RESEARCH.md lines 357-391 (the two templates pre-extracted — DIFF against the spec for byte-fidelity; do not trust the extract alone)
    - .planning/phases/02-shared-contracts/02-RESEARCH.md lines 310 (Google SRE blameless-postmortem confirmation — explains WHY 'systemic, not personal' is load-bearing)
    - plans/traceability.md lines 17-26 (frozen ID scheme — REL-xxxx and INC-xxxx titles cite this; do NOT redefine)
    - docs/initial/grugops_brand_manual.md lines 105-161 (two-voice rule — grug voice FORBIDDEN in these safety/money files; clear voice mandatory)
  </read_first>
  <action>
    Author `agent-factory/handoffs/release-handoff.md` with `kind: handoff` + stage (e.g. `release`) frontmatter, then the §8.1 body VERBATIM (lines 813-830): `# Release Handoff: REL-xxxx`, `## Version (SemVer)`, `## Tickets included`, `## Changelog`, `## Release notes (business-facing)`, `## Environments path` with the literal `dev -> staging -> prod` line, `## Feature flags`, `## Migration plan`, `## Rollback plan`, `## DR notes (RTO / RPO)`, `## Evidence` with the `QE result: ... | Security/NFR result: ... | UAT signoff: ...` line, `## Approval` with `Approved by (human role/name): ...`, `## Status` with `READY_TO_RELEASE | BLOCKED | RELEASED`.

    Author `agent-factory/handoffs/incident-postmortem.md` with `kind: handoff` + stage (e.g. `incident`) frontmatter, then the §8.2 body VERBATIM (lines 834-846): `# Incident Postmortem: INC-xxxx`, `## Summary`, `## Impact (users, data, money, duration)`, `## Timeline (UTC)`, `## Detection`, `## Root cause (systemic, not personal)`, `## Mitigation taken`, `## Rollback used`, `## What went well`, `## What to improve`, `## Follow-up tickets (IDs)`. The blameless framing `(systemic, not personal)` is locked — do not reword.

    Clear professional voice (D-00, brand §4.3 — grug voice forbidden in safety/money content). Empty-but-shaped: headings only, no fake release IDs or incident data (D-03). Do NOT add workflow behavior (when to release / when to declare an incident is Phase-4 content).
  </action>
  <verify>
    <automated>test -f agent-factory/handoffs/release-handoff.md && test -f agent-factory/handoffs/incident-postmortem.md || { echo "MISSING file"; exit 1; }; grep -q 'READY_TO_RELEASE' agent-factory/handoffs/release-handoff.md && grep -q 'BLOCKED' agent-factory/handoffs/release-handoff.md && grep -q 'RELEASED' agent-factory/handoffs/release-handoff.md || { echo "release status enum missing"; exit 1; }; grep -q 'dev -> staging -> prod' agent-factory/handoffs/release-handoff.md || { echo "environments path missing"; exit 1; }; grep -q 'Approved by' agent-factory/handoffs/release-handoff.md || { echo "human approval line missing"; exit 1; }; grep -q 'REL-xxxx' agent-factory/handoffs/release-handoff.md || { echo "REL-xxxx title missing"; exit 1; }; grep -q 'systemic, not personal' agent-factory/handoffs/incident-postmortem.md || { echo "blameless root-cause wording missing"; exit 1; }; grep -q 'INC-xxxx' agent-factory/handoffs/incident-postmortem.md || { echo "INC-xxxx title missing"; exit 1; }; grep -q '^kind: handoff' agent-factory/handoffs/release-handoff.md && grep -q '^kind: handoff' agent-factory/handoffs/incident-postmortem.md || { echo "frontmatter missing"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f` passes for release-handoff.md and incident-postmortem.md
    - release-handoff.md: `grep -q 'READY_TO_RELEASE'`, `grep -q 'BLOCKED'`, `grep -q 'RELEASED'`, `grep -q 'dev -> staging -> prod'`, `grep -q 'Approved by'`, `grep -q 'REL-xxxx'` all pass
    - incident-postmortem.md: `grep -q 'systemic, not personal'`, `grep -q 'INC-xxxx'` pass
    - both have `grep -q '^kind: handoff'`
    - no fake data: `! grep -rEq 'REL-[0-9]{4}|INC-[0-9]{4}' agent-factory/handoffs/release-handoff.md agent-factory/handoffs/incident-postmortem.md` (titles use the literal `xxxx` placeholder, not a numbered instance)
  </acceptance_criteria>
  <done>
    release-handoff.md and incident-postmortem.md exist, each reproduced verbatim from §8.1/§8.2 with `kind: handoff` frontmatter; release carries the Status enum, environments path, and human-approval line; postmortem preserves the blameless 'systemic, not personal' framing; clear voice; zero fake data.
  </done>
</task>

<task type="auto">
  <name>Task 2: retro-notes + refinement-notes + sprint-plan (verbatim §8.3, §8.4, §8.5)</name>
  <files>agent-factory/handoffs/retro-notes.md, agent-factory/handoffs/refinement-notes.md, agent-factory/handoffs/sprint-plan.md</files>
  <read_first>
    - docs/initial/agent_factory_builder_spec_v2.md lines 848-869 (§8.3 retro-notes, §8.4 refinement-notes, §8.5 sprint-plan VERBATIM — the source of all three)
    - .planning/phases/02-shared-contracts/02-RESEARCH.md lines 393-413 (the three templates pre-extracted — DIFF against spec for byte-fidelity)
    - plans/metrics.md (frozen — retro-notes `## Metrics snapshot` references these metric names: Throughput, Cycle time, Lead time, WIP, Blocked time, Rework rate, Gate pass rate, Escaped defects, Velocity; cite, do NOT redefine)
    - plans/board.md (frozen — sprint-plan mirrors plans/sprints/SPRINT-xx.md sizing/goal/capacity; sprints/ currently holds .gitkeep only)
    - .planning/phases/02-shared-contracts/02-PATTERNS.md lines 122-129 (Group 2 v2-handoff assignment — style + IDs + metric-name citation)
  </read_first>
  <action>
    Author all three with `kind: handoff` + stage frontmatter (D-13), then the §8.3–8.5 bodies VERBATIM:
    - retro-notes.md (stage: retro) ← §8.3 (lines 849-854): `# Retro: <sprint or period>`, `## Metrics snapshot`, `## Top wastes (1-3)`, `## Keep / Stop / Start`, `## Improvement tickets created (IDs, tag: factory)`. The `## Metrics snapshot` section references the frozen plans/metrics.md metric names — cite them, do not redefine.
    - refinement-notes.md (stage: refinement) ← §8.4 (lines 857-866): `# Refinement: <date>`, `## Items reviewed`, `## Split decisions (XL -> children)`, `## Sizes assigned`, `## Priorities assigned`, `## Promoted to Ready (IDs)`, `## Still blocked / open questions`.
    - sprint-plan.md (stage: sprint-plan) ← §8.5 (lines 868-869): a mirror of plans/sprints/SPRINT-xx.md — goal, committed items with sizes, capacity. State (per the spec) that it is used when planning is requested as a one-off packet. Keep the shape minimal and faithful to the spec's description.

    Clear professional voice (D-00). Empty-but-shaped: headings/stubs only, no fake sprint numbers or ticket IDs (D-03). Do NOT add ceremony-workflow behavior (that is Phase 4).
  </action>
  <verify>
    <automated>for f in retro-notes refinement-notes sprint-plan; do test -f "agent-factory/handoffs/$f.md" || { echo "MISSING $f.md"; exit 1; }; grep -q '^kind: handoff' "agent-factory/handoffs/$f.md" || { echo "$f missing frontmatter"; exit 1; }; done; grep -q 'Metrics snapshot' agent-factory/handoffs/retro-notes.md && grep -q 'Keep / Stop / Start' agent-factory/handoffs/retro-notes.md || { echo "retro-notes missing §8.3 sections"; exit 1; }; grep -q 'Split decisions' agent-factory/handoffs/refinement-notes.md && grep -qi 'promoted to ready' agent-factory/handoffs/refinement-notes.md || { echo "refinement-notes missing §8.4 sections"; exit 1; }; grep -qi 'SPRINT' agent-factory/handoffs/sprint-plan.md && grep -qi 'capacity' agent-factory/handoffs/sprint-plan.md || { echo "sprint-plan missing §8.5 content"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f` passes for retro-notes.md, refinement-notes.md, sprint-plan.md
    - all three have `grep -q '^kind: handoff'`
    - retro-notes.md: `grep -q 'Metrics snapshot'` and `grep -q 'Keep / Stop / Start'` pass
    - refinement-notes.md: `grep -q 'Split decisions'` and `grep -qi 'promoted to ready'` pass
    - sprint-plan.md: `grep -qi 'SPRINT'` and `grep -qi 'capacity'` pass (mirrors plans/sprints/SPRINT-xx.md)
    - no fake data: `! grep -rEq 'SPRINT-[0-9]{2}|ABC-[0-9]{3}' agent-factory/handoffs/sprint-plan.md` (placeholder `xx`, not a numbered instance)
  </acceptance_criteria>
  <done>
    retro-notes.md, refinement-notes.md, and sprint-plan.md exist, each verbatim from §8.3/§8.4/§8.5 with `kind: handoff` frontmatter; retro-notes cites the frozen metric names; sprint-plan mirrors SPRINT-xx.md; clear voice; zero fake data.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — static markdown) | Static markdown templates. No code, no input handling, no data flow. The only relevant surface is content integrity of safety/money-touching templates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering (content integrity) | release-handoff Status enum + human-approval line | mitigate | Reproduce §8.1 verbatim incl. `Approved by (human role/name)` and `READY_TO_RELEASE \| BLOCKED \| RELEASED`. The approval line encodes "humans decide, agents execute" (SAFE-01, enforced in later phases). `grep -q 'Approved by'` asserts it. |
| T-02-05 | Information disclosure (trust erosion) | grug voice in a blameless postmortem / release safety file | mitigate | Clear voice locked (D-00, brand §4.3 forbids grug voice in safety/money content). Manual voice review (advisory). |
| T-02-06 | Repudiation (false postmortem framing) | incident-postmortem blameless wording | mitigate | Preserve `Root cause (systemic, not personal)` verbatim; `grep -q 'systemic, not personal'` asserts the blameless contract is intact. |

No high-severity code-level threats exist for this markdown-only plan; nothing to block on.
</threat_model>

<verification>
Full-plan structural sweep after both tasks:
- All 5 v2 files exist: `for f in release-handoff incident-postmortem retro-notes refinement-notes sprint-plan; do test -f "agent-factory/handoffs/$f.md" || echo "MISSING $f"; done`
- Signature headings present: `grep -q 'READY_TO_RELEASE' agent-factory/handoffs/release-handoff.md && grep -q 'systemic, not personal' agent-factory/handoffs/incident-postmortem.md && grep -q 'Keep / Stop / Start' agent-factory/handoffs/retro-notes.md`
- Frontmatter present: `grep -L '^kind: handoff' agent-factory/handoffs/release-handoff.md agent-factory/handoffs/incident-postmortem.md agent-factory/handoffs/retro-notes.md agent-factory/handoffs/refinement-notes.md agent-factory/handoffs/sprint-plan.md` (expect empty)
- No-fake-data invariant: `! grep -rEq 'REL-[0-9]{4}|INC-[0-9]{4}|SPRINT-[0-9]{2}' agent-factory/handoffs/release-handoff.md agent-factory/handoffs/incident-postmortem.md agent-factory/handoffs/sprint-plan.md`
</verification>

<success_criteria>
- All 5 v2 handoff templates exist (HAND-02, ROADMAP SC#2)
- release-handoff carries REL-xxxx, the environments path, the human-approval line, and the Status enum
- incident-postmortem is blameless with the verbatim 'systemic, not personal' root-cause framing
- retro-notes references the frozen metric names; sprint-plan mirrors SPRINT-xx.md
- Every v2 handoff has `kind: handoff` frontmatter; zero fake data; clear professional voice
</success_criteria>

<output>
Create `.planning/phases/02-shared-contracts/02-02-SUMMARY.md` when done
</output>

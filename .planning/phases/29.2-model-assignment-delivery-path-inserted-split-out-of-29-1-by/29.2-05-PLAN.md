---
phase: 29.2-model-assignment-delivery-path
plan: 05
type: execute
wave: 2
depends_on: ["29.2-04"]
files_modified:
  - install/install.ts
  - install/install.js
  - install/install.test.ts
  - agent-factory/config/factory.config.md
  - scripts/model-dial-consistency.test.ts
  - CHANGELOG.md
autonomous: true
gap_closure: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-11's stated property — a re-run is a visible no-op and a refresh a visible change — is visible in the PREVIEW as well as the run: `--dry-run` over an unchanged target reports `skipped`, not `would-materialize`, for every adapter a real run would skip."
    - "A user previewing a `models` edit can tell from `--dry-run` alone whether anything would change; the preview and the run it describes agree on every adapter."
    - "No sentence shipped in `agent-factory/config/factory.config.md` tells an installed user that a configuration location is consulted which D-06 guarantees the render never sees. The corrected paragraph states that, for an installed repository, `.grugops/factory.config.json` is the only file consulted."
    - "The retired 29.1 disclosure remains refused by name without the guard refusing an honest future disclosure about a different dial — the pinned fragment identifies the 29.1 sentence rather than a heading style any disclosure would reuse (IN-02)."
    - "`CHANGELOG.md` states the two reachable consequences for existing users that the current entry omits: every adapter installed before this change reports stale on `--check` until a re-install, and `--strict` promotes that to a failure (IN-04)."
    - "A cleanup failure is never reported as a verdict failure: an unremovable temp mirror cannot convert a completed render into a crash, and cannot change the run's exit code (WR-03)."
    - "Nothing this repository hands a reader on the SUCCESS path names a directory they cannot go and look at — the rule already applied to the failure arm and the doctor now also covers the happy path, from ONE wording authority rather than a third copy (WR-05)."
    - "The banner slot's transform asymmetry is stated with its reachability bound rather than left implied symmetric by the D-14 comment (IN-01)."
  artifacts:
    - "install/install.ts — the `DRY_RUN` branch relocated below the identical-bytes comparison in `materializeAdapter()`"
    - "install/install.ts — the guarded `finally` cleanup in `renderAdaptersInMirror()`"
    - "install/install.ts — one shared mirror-path disclaimer constant, replacing the two existing spellings and serving the new success-path site"
    - "install/install.ts — the corrected transform-asymmetry comment above the banner state machine"
    - "install/install.js — the committed build, moved in the same commit"
    - "install/install.test.ts — the second-run dry-run parity case"
    - "agent-factory/config/factory.config.md — the corrected installed-repository sentence"
    - "scripts/model-dial-consistency.test.ts — the narrowed retired-disclosure fragment"
    - "CHANGELOG.md — the added `--check` / `--strict` clause"
  key_links:
    - "`materializeAdapter()` compare → `DRY_RUN` return → write: the preview branch must sit BETWEEN the compare and the write, and BELOW the plan-04 hazard guard, so a hazardous destination is still refused in a preview."
    - "one disclaimer constant → the doctor's NO VERDICT arm, the render-refusal arm, and the new success-path relay: three consumers, one wording."
    - "`agent-factory/config/factory.config.md` (the `pointer` surface) → `scripts/model-dial-consistency.test.ts`'s pointer cases: the prose and the case that asks about it move in the same commit, per D-15."
---

<objective>
Close the four findings VERIFICATION.md and REVIEW.md left open that are not the data-loss bound:
the preview that disagrees with the run it describes (WR-01), a shipped sentence that D-06 made
false for its own audience (WR-02), a cleanup failure that can crash a completed run (WR-03), and a
success-path line naming a directory that no longer exists (WR-05) — plus the three info findings
that are cheaper to fix than to defer (IN-01, IN-02, IN-04).

**Purpose:** D-11 bought a property — a re-run is a visible no-op, a refresh a visible change — and
the preview does not show it, which is exactly the question a user runs `--dry-run` to answer before
touching a money-adjacent dial. D-06 made a paragraph false and D-15's own rule says kit prose
states what is true now. Neither is data loss, and both are shipped to every installed user.

**Output:** one relocated branch, one guarded cleanup, one shared wording authority, one corrected
paragraph with its asserting test moved in the same commit, and one changelog clause.

Decisions this plan touches: D-06, D-07, D-11, D-14, D-15.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-REVIEW.md
@.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-VERIFICATION.md
@.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-01 — make the preview agree with the run it describes</name>
  <files>install/install.ts, install/install.js, install/install.test.ts</files>

  <read_first>
    - `install/install.ts` — `materializeAdapter()` as plan 04 left it: the source-missing arm, the
      hazard guard plan 04 inserted, the `DRY_RUN` early return, `transformAdapter(readFileSync(src))`,
      the `readFileSync(dest)` compare, the `skipped (identical copy present)` arm, `mkdirp`,
      `writeFileSync`.
    - `install/install.ts:1246-1275` — `linkOrCopy()`, which checks identity BEFORE its `DRY_RUN`
      branch. This is the ordering `materializeAdapter` is being brought into line with, and the
      reason WR-01 is an inconsistency rather than a preference.
    - `install/install.test.ts:1936-1969` — the existing DRY_RUN case: it drives `install.js` with
      `DRY_RUN: "1"` through a direct `spawnSync` (not `runInstall`), asserts 17
      `would-materialize` over a FRESH target, asserts no `materialized` line, and asserts
      `snapshot(target)` and `snapshot(home)` are unchanged.
    - `install/install.test.ts:1858-1864` — `adapterReportLines(stdout, verb)`.
    - `29.2-REVIEW.md` WR-01 — the proposed reordering and the reproduced counts (18
      `would-materialize` / 6 `skipped` in the preview against 24 `skipped` in the run).
  </read_first>

  <precondition>
    Plan 04 is committed and `install/install.js` is a build of `install/install.ts` at HEAD. The
    hazard guard plan 04 added must remain the FIRST thing `materializeAdapter` does after the
    source-missing arm; this task moves the `DRY_RUN` branch downward past the compare and must not
    move the guard with it.
  </precondition>

  <behavior>
    - Test (RED first): a SECOND `--dry-run` over an unchanged, already-installed target reports 17
      agent-class `skipped` lines and zero `would-materialize` lines.
    - The existing FRESH-target dry-run case still reports 17 `would-materialize` — a fresh target
      has nothing identical, so that expectation is unchanged and must stay passing.
  </behavior>

  <action>
    Relocate the `DRY_RUN` early return inside `materializeAdapter()` so it sits BELOW the
    identical-bytes comparison and ABOVE `mkdirp` / `writeFileSync`. The resulting order is:
    source-missing arm → hazard guard (plan 04, unmoved) → build `final` from the source →
    read `current` from the destination → identical arm reports `skipped (identical copy present)`
    and returns → `DRY_RUN` reports `would-materialize` and returns → `mkdirp` → `writeFileSync` →
    `materialized`.

    Both reads are safe under a preview: the mirror exists under `DRY_RUN` (the existing case proves
    it, because the resolution is reported), the destination read is a read, and `mkdirp` — which is
    already `DRY_RUN`-aware — now sits below the preview return and is not reached at all.

    Add one comment recording WHY the branch moved, in the file's voice: a preview that reports a
    write for a file the run would skip cannot answer the question a preview is run to answer, and
    `linkOrCopy` has always checked identity first, so this removes a disagreement between two
    write paths rather than introducing a new rule.

    **RED first.** Add a case to `install/install.test.ts` immediately after the existing DRY_RUN
    case, titled exactly:

      `model delivery: a SECOND dry run over an unchanged target reports `skipped`, not `would-materialize` (D-11 preview parity, WR-01)`

    It installs for real first (`makeFixture()`, `writeTargetConfig` with the tiered preset,
    `runInstall`, assert status 0), snapshots the target, then drives the same direct `spawnSync`
    shape the existing DRY_RUN case uses with `DRY_RUN: "1"`. Assert the PREMISE that the first run
    really installed — `adapterReportLines` of the real run at `materialized` has length 17 — so the
    case cannot pass over a target that was never installed. Then assert `adapterReportLines(out,
    "skipped")` has length 17, `adapterReportLines(out, "would-materialize")` is empty, and
    `snapshot(target)` is unchanged.

    Leave the existing FRESH-target case's `expect(lines.length).toBe(17)` for `would-materialize`
    exactly as it is — a fresh target has no identical bytes and that expectation stays true. Do not
    weaken it.

    Watch the new case RED against the current build before moving the branch. Build and commit the
    `.ts` with its rebuilt `.js` in the same commit.
  </action>

  <acceptance_criteria>
    - In `install/install.ts`, within the body of `materializeAdapter`, the character offset of
      `if (DRY_RUN)` is GREATER than the offset of `identical copy present` and LESS than the offset
      of `writeFileSync(dest`.
    - Within that same body, the offset of the plan-04 hazard guard call `adapterDestHazard(dest)`
      is LESS than the offset of `if (DRY_RUN)`.
    - `install/install.test.ts` contains a test whose title includes `a SECOND dry run over an unchanged target`.
    - The existing fresh-target dry-run case still asserts a `would-materialize` count of 17.
    - `npx vitest run install/install.test.ts` exits 0.
    - `npm run build && git diff --exit-code --name-only -- '*.js'` exits 0 after the commit.
    - The summary quotes the observed RED output of the new case against the pre-fix build.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run install/install.test.ts</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary line, or the new `SECOND dry run` case title is absent from the run listing</fails_when>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; git diff --exit-code --name-only -- '*.js'</automated>
    <fails_when>non-zero exit, or `git diff` names any `.js` path after the commit</fails_when>
  </verify>

  <reversibility rating="reversible">
    Moving a branch within one function; reverting is a one-hunk revert and no target state depends
    on the ordering.
  </reversibility>

  <done>
    `--dry-run` over an unchanged target reports `skipped` for every adapter a real run would skip,
    the fresh-target preview is unchanged, and both are pinned.
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-03, WR-05 and IN-01 — a cleanup failure is not a verdict, and one wording authority for the temp mirror</name>
  <files>install/install.ts, install/install.js, install/install.test.ts</files>

  <read_first>
    - `install/install.ts:1685-1695` — the `finally` block of `renderAdaptersInMirror()` with the
      bare `rmSync(dir, { recursive: true, force: true, maxRetries: 3 })` and the comment explaining
      why cleanup is a `finally` rather than an exit hook.
    - `install/install.ts:394-405` — `report` and `verify`, and the fact that `verify()` increments
      `VERIFY_FINDINGS`.
    - `install/install.ts:2560-2566` — `process.exitCode = 3`, the exit that `VERIFY_FINDINGS`
      drives, so the executor sees exactly what calling `verify()` from a cleanup path would mean.
    - `install/install.ts:700-707` — the doctor's NO VERDICT arm, the FIRST existing spelling of the
      temp-mirror disclaimer.
    - `install/install.ts:2195-2205` — the SECOND existing spelling of the same disclaimer.
    - `install/install.ts:2360-2375` — the success-path relay loop and the `resolution` report line
      after it: the site WR-05 says hands a reader a path that was removed before the banner printed.
    - `install/install.ts:1755-1800` — `transformAdapter`'s state machine and the D-14 comment above
      it claiming the banner slot is handled "like `KIT=`, strip-then-inject, content-idempotent"
      (IN-01).
    - `install/install.test.ts:1280-1303` — the doctor-body structural case, in case the shared
      constant is referenced from inside the doctor body: no write call and no code line matching
      `/model/i` may appear there.
    - `29.2-REVIEW.md` WR-03, WR-05, IN-01 — the proposed fixes and the explicit request that the
      `report`-versus-`verify()` choice be DECIDED rather than left implicit.
  </read_first>

  <precondition>
    Task 1 is committed. The two existing disclaimer spellings at approximately `:705` and `:2201`
    are still byte-identical in their shared clause; if they have drifted apart, reconcile them onto
    the new constant rather than preserving both.
  </precondition>

  <action>
    Three edits in `install/install.ts`, none of which changes an exit code on any path that is
    working today.

    **WR-03 — the guarded cleanup, and the decision the review asked for.** Wrap the `rmSync` in the
    `finally` in a `try`/`catch`. On a throw, emit a report line naming `dir`, the error message,
    and the facts that the leftover tree holds no target data and can be deleted by hand.

    THE DECISION, made explicitly here as the review asks: use `report(...)` with its own label,
    NOT `verify()`. The reason must be written into the source, not only into this plan.
    `verify()` increments `VERIFY_FINDINGS`, which drives `process.exitCode = 3`, and exit 3 in this
    installer means an install CLASS was refused and the human must resolve it (D-03). A temp
    directory the operating system would not release is housekeeping on a run that already completed
    its work; making it exit 3 would tell every downstream consumer that an install class failed
    when none did, and would give the same exit code two meanings. Use a distinct report label —
    `cleanup` — so the line is not read as a `verify` finding it deliberately is not.

    This is the crash shape R1 and F-D were already fixed for, one line later: on the install path
    the throw escapes after all seventeen adapters are written and before `seedState()` /
    `writeMarker()`, leaving a target with adapters and no marker; on the `--check` path it kills the
    doctor mid-verdict. Record that in the comment.

    **WR-05 — one wording authority, three consumers.** There are already TWO spellings of the
    temp-mirror disclaimer in this file. Do not add a third. Declare ONE constant holding the
    disclaimer clause, placed in the primitives block ABOVE the `--check` early exit at
    `if (CHECK) { process.exit(doctor()); }` — the same temporal-dead-zone rule that governs
    `verify` and the plan-04 predicate, because the doctor consumes it. Rewrite both existing sites
    to interpolate it, preserving each site's surrounding sentence, and add the third consumer: one
    line after the success-path relay loop stating that any path inside the relayed lines above is a
    temporary render mirror that no longer exists, and naming the directory the adapters were
    actually written to (`join(TARGET, ".claude", "agents")`).

    Do NOT rewrite the generator's relayed line itself — it is the one-authority announcement and
    editing it would create a second grammar, which is the failure this repository's own rule names.

    **IN-01 — state the asymmetry.** Correct the comment above the transform state machine. The kit
    slot preserves its anchor so the transform is idempotent on its own output; the banner slot
    CONSUMES its anchor and the injected block is stripped on re-entry, so feeding a materialized
    file back through the transform yields zero banners. State that, and state the reachability
    bound that makes it harmless today: agent sources are always fresh renders, the skill source is
    kit text, and `--migrate` runs its pre-steps and then the ordinary install. Change the comment
    only — do not make the banner slot anchor-preserving, which would move rendered bytes and is not
    what this gap-closure round is for.

    Add no new test for WR-03 or WR-05: manufacturing an unremovable temp directory is a
    platform-dependent fixture, and the relay disclaimer is a wording change. Instead assert them
    structurally in one case titled

      `install reporting: the temp-mirror disclaimer has ONE authority, and the render cleanup cannot change the run's exit code (WR-03/WR-05)`

    which reads `install.ts` and asserts (i) exactly one declaration of the disclaimer constant
    exists, (ii) the disclaimer sentence appears as a literal exactly once in the file, (iii) the
    `finally` block containing the `rmSync` also contains a `catch`, and (iv) that same `finally`
    block contains no `verify(` call. State in the case comment that (iii) and (iv) are structural
    pins over a platform-dependent condition and are not a behavioural proof.

    Build and commit the `.ts` with its rebuilt `.js` in the same commit.
  </action>

  <acceptance_criteria>
    - `install/install.ts` contains exactly one declaration of the disclaimer constant, and its
      character offset is lower than the offset of `process.exit(doctor())`.
    - The disclaimer sentence appears as a string literal exactly once in `install/install.ts`.
    - The `finally` block in `renderAdaptersInMirror` contains `catch` and contains no `verify(`.
    - `install/install.ts` contains a report call whose label argument is the string `cleanup`.
    - The comment above the transform state machine names the banner slot and states that the
      transform is not idempotent on its own output for that slot, together with the reachability
      bound.
    - `install/install.test.ts` contains a test whose title includes
      `the temp-mirror disclaimer has ONE authority`.
    - `npx vitest run install/install.test.ts` exits 0, including the pre-existing doctor-body
      structural case at `:1280-1303`.
    - `npm run build && git diff --exit-code --name-only -- '*.js'` exits 0 after the commit.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run install/install.test.ts</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary line, or the `ONE authority` case title is absent from the run listing</fails_when>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; git diff --exit-code --name-only -- '*.js'</automated>
    <fails_when>non-zero exit, or `git diff` names any `.js` path after the commit</fails_when>
    <automated>node scripts/check-imperative-lexicon.js &amp;&amp; node scripts/check-diff-disposition.js &amp;&amp; node scripts/check-nul-bytes.js</automated>
    <fails_when>non-zero exit, or any guard prints a finding line naming `install/install.ts`</fails_when>
  </verify>

  <reversibility rating="reversible">
    A guarded cleanup, a hoisted constant and a corrected comment; each reverts independently and
    none changes a byte the installer writes into a target.
  </reversibility>

  <done>
    An unremovable render mirror reports itself and changes no exit code, the temp-mirror disclaimer
    has one declaration and three consumers including the success path, and the D-14 comment states
    the banner slot's asymmetry with its bound.
  </done>
</task>

<task type="auto">
  <name>Task 3: WR-02, IN-02 and IN-04 — the prose D-06 made false, and the two changes existing users will meet</name>
  <files>agent-factory/config/factory.config.md, scripts/model-dial-consistency.test.ts, CHANGELOG.md</files>

  <read_first>
    - `agent-factory/config/factory.config.md:138` — the "Where the block is read from, and which
      file wins" paragraph: the two-location precedence, the whole-file-wins statement, and the
      sentence explicitly addressed to installed users ("the standard installed shape").
    - `agent-factory/config/factory.config.md:140` — the "How an edit reaches an installed
      repository" paragraph D-15 rewrote, which deliberately names no configuration-file location
      and therefore does not correct its neighbour.
    - `29.2-CONTEXT.md` D-06 — the mirror never receives an in-kit `agent-factory/config/` file, so
      the resolver's second candidate cannot fire inside a target. This is what makes the neighbour
      paragraph false rather than merely incomplete.
    - `scripts/model-dial-consistency.test.ts:1355-1400` — `INSTALL_ENTRYPOINT_REL`,
      `INSTALL_TARGET_FLAG`, `RETIRED_INSTALLED_TARGET_DISCLOSURES` and the two cases that read the
      `pointer` surface. The first entry of that constant is the generic fragment IN-02 narrows.
    - `scripts/model-dial-consistency.test.ts:46-50` — `SURFACE_ROLES`: `pointer` IS
      `agent-factory/config/factory.config.md`. Confirm before editing which cases read it.
    - `scripts/model-dial-consistency.test.ts:1305-1345` — the second-location spelling cases. These
      read `modelBulletRegion()`, which is bounded inside the `authority` surface
      (`agent-factory/packaging/subagent.frontmatter.md`), NOT the pointer — so a spelling added to
      `factory.config.md` does not enter their region. Verify that before assuming it.
    - `CHANGELOG.md:42-48` — the `## [Unreleased]` `### Changed` bullet this phase added.
    - `29.2-REVIEW.md` WR-02, IN-02, IN-04 — the proposed sentence, the narrowing, and the omitted
      clause.
  </read_first>

  <precondition>
    `agent-factory/config/factory.config.md` is the `pointer` surface in
    `scripts/model-dial-consistency.test.ts`, and `modelBulletRegion()` reads the `authority`
    surface rather than the pointer. Confirm both by reading `SURFACE_ROLES` and
    `modelBulletRegion` before writing prose; if either is not so, the spelling-count cases DO
    cover the file being edited and the sentence must be re-shaped accordingly.
  </precondition>

  <action>
    Three record corrections, all shipped to readers, none behavioural.

    **WR-02.** Add one sentence to the "Where the block is read from, and which file wins" paragraph
    at `:138`, after the existing "This whole-file precedence is deliberate…" clause. It must state,
    in the file's clear professional voice, that in an installed repository only the FIRST location
    exists to the render — the installer never hands an in-repo `agent-factory/config/factory.config.json`
    to the generator — so for an installed repository `.grugops/factory.config.json` is the only
    file consulted. The two-location precedence sentence stays: it is true of the resolver, and of a
    grugops CHECKOUT, and deleting it would trade one false impression for another. What the new
    sentence adds is the audience bound the paragraph was missing.

    The sentence must not reintroduce any fragment held in `RETIRED_INSTALLED_TARGET_DISCLOSURES` —
    read that constant first and write around it. The neighbouring `:140` paragraph is D-15's and is
    not edited by this task.

    **IN-02.** Narrow the first entry of `RETIRED_INSTALLED_TARGET_DISCLOSURES` from the generic
    heading fragment to the 29.1-specific one, `Disclosed limitation — an installed repository`, so
    the case refuses the retired sentence rather than any future honest disclosure that reuses the
    heading style for a different dial. Add one comment recording why the fragment was narrowed and
    what the narrowing costs: a paraphrase of the retired sentence is still not caught, which the
    surrounding block already discloses as a standing residual — this narrowing does not change that,
    and must not be described as if it did.

    **IN-04.** Extend the existing `### Changed` bullet in `CHANGELOG.md` with one clause naming the
    two consequences an existing user meets and the current entry omits: every adapter installed
    before this change is reported stale by `--check` until the repository is re-installed, and
    `--strict` promotes that warning to a failure. Keep the Keep-a-Changelog shape, keep it inside
    the existing bullet rather than adding a second one about the same change, and introduce no cost
    or conformance claim — `node scripts/check-banned-claims.js` must stay at zero findings.

    Do not add a test for the changelog clause; the banned-claims and public-docs gates already read
    it. If the WR-02 sentence adds a location SPELLING that the model-dial location-count case
    measures, extend that case's expected cardinality in the same commit and record the new count in
    the summary — but confirm from `modelBulletRegion`'s bound first whether it measures this file
    at all, rather than assuming either way.
  </action>

  <acceptance_criteria>
    - `agent-factory/config/factory.config.md` contains a sentence containing both the phrase
      `only file consulted` and the string `.grugops/factory.config.json`, within the paragraph that
      begins `**Where the block is read from`.
    - The `:140` paragraph beginning `**How an edit reaches an installed repository` is byte-unchanged.
    - `scripts/model-dial-consistency.test.ts` contains the string
      `Disclosed limitation — an installed repository` inside the
      `RETIRED_INSTALLED_TARGET_DISCLOSURES` declaration.
    - `CHANGELOG.md`'s `## [Unreleased]` `### Changed` bullet contains both `--check` and `--strict`
      in a clause about adapters installed before this change.
    - `npx vitest run scripts/model-dial-consistency.test.ts` exits 0 with 39 or more passing cases.
    - `node scripts/check-banned-claims.js` reports 0 findings.
    - No `.ts` production source was edited by this task, so no `.js` rebuild is expected; if one
      was, it ships in the same commit.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run scripts/model-dial-consistency.test.ts</automated>
    <fails_when>non-zero exit, or the passing count is below 39, or "failed" appears in the vitest summary line</fails_when>
    <automated>node scripts/check-banned-claims.js</automated>
    <fails_when>non-zero exit, or the output reports a finding count other than 0</fails_when>
    <automated>node scripts/check-public-docs-vocabulary.js &amp;&amp; node scripts/check-claim-anchors.js &amp;&amp; node scripts/check-kit-refs.js</automated>
    <fails_when>non-zero exit, or any guard names `agent-factory/config/factory.config.md` or `CHANGELOG.md` in a finding line</fails_when>
    <automated>npx vitest run install/install.test.ts</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary line — a prose edit must not move installer behaviour, and this is the control that says so</fails_when>
  </verify>

  <reversibility rating="reversible">
    Prose and one test constant; both revert cleanly and neither changes a byte any installer writes.
  </reversibility>

  <done>
    No sentence in the config reference tells an installed user that a location D-06 excludes is
    consulted, the retired-disclosure guard identifies the 29.1 sentence rather than a heading style,
    and the changelog names the two consequences existing users will actually meet.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer → the human reading its output | Report lines are the only evidence a user has about what happened. A preview that disagrees with the run, or a line naming a path that does not exist, is a trust defect in that channel. |
| kit prose → installed user | Shipped documentation is read as authoritative and cannot be corrected in the reader's tree. |
| host OS temp directory → the run's exit code | An OS-level cleanup failure must not be able to author the run's verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.2-29 | Repudiation | `materializeAdapter()` `DRY_RUN` branch | medium | mitigate | The preview is computed from the same comparison as the run (Task 1), so a user cannot be shown a change that would not happen. |
| T-29.2-30 | Denial of service | `renderAdaptersInMirror()` `finally` cleanup | medium | mitigate | The `rmSync` is guarded; a cleanup failure reports itself under its own label and cannot convert a completed render into a crash or an exit-3 (Task 2). |
| T-29.2-31 | Spoofing | success-path relay of the generator's stdout | low | mitigate | One disclaimer authority is interpolated at all three sites, so no report line hands a reader a directory that was deleted before the banner printed (Task 2). |
| T-29.2-32 | Repudiation | `agent-factory/config/factory.config.md` :138 | medium | mitigate | The audience bound is stated: for an installed repository only `.grugops/factory.config.json` is consulted (Task 3). Reproduced in REVIEW.md — a target carrying only the in-kit file rendered 17 `inherit`. |
| T-29.2-33 | Repudiation | `RETIRED_INSTALLED_TARGET_DISCLOSURES` | low | mitigate | Fragment narrowed so the guard cannot refuse an honest future disclosure about a different dial (Task 3). The residual — a paraphrase is still uncaught — stays disclosed and is not described as closed. |
| T-29.2-SC | Tampering | npm / pip / cargo installs | high | mitigate | No package is added by this plan. Asserted by the empty package-manifest diff in plan 06's gate block, not assumed. |
</threat_model>

<verification>
- `npx vitest run install/install.test.ts` and `npx vitest run scripts/model-dial-consistency.test.ts`
  both exit 0.
- `node scripts/check-banned-claims.js` reports 0 findings.
- `npm run build && git diff --exit-code --name-only -- '*.js'` exits 0 after each commit that
  touched a `.ts`.
</verification>

<success_criteria>
The preview agrees with the run it describes, a cleanup failure cannot author a verdict, the
temp-mirror disclaimer has one authority and covers the success path, and no sentence shipped in the
config reference or the changelog is false for the reader it addresses.
</success_criteria>

## Artifacts this phase produces

**New symbols in `install/install.ts`**

| Symbol | Kind | Where it must be declared | Why the position matters |
|---|---|---|---|
| the temp-mirror disclaimer constant | `const` string | primitives block, above `if (CHECK) { process.exit(doctor()); }` | the doctor is one of its three consumers; below the early exit it is in the temporal dead zone |
| the `cleanup` report label | string argument to `report()` | the guarded `finally` in `renderAdaptersInMirror` | it must NOT be the `verify` label — that label increments `VERIFY_FINDINGS` and drives `process.exitCode = 3`, giving exit 3 two meanings |

**Relocations, not additions** — the `DRY_RUN` branch inside `materializeAdapter()` moves below the
identical-bytes comparison. No new branch is introduced; the plan-04 hazard guard stays above it.

**New test titles in `install/install.test.ts`**

1. ``model delivery: a SECOND dry run over an unchanged target reports `skipped`, not `would-materialize` (D-11 preview parity, WR-01)``
2. `install reporting: the temp-mirror disclaimer has ONE authority, and the render cleanup cannot change the run's exit code (WR-03/WR-05)`

**Prose and constant edits**

| File | Edit |
|---|---|
| `agent-factory/config/factory.config.md` | one sentence appended to the `**Where the block is read from` paragraph, naming the installed-repository bound. The `**How an edit reaches an installed repository` paragraph is byte-unchanged. |
| `scripts/model-dial-consistency.test.ts` | `RETIRED_INSTALLED_TARGET_DISCLOSURES` first entry narrowed to `Disclosed limitation — an installed repository`, with a comment recording the cost of the narrowing |
| `CHANGELOG.md` | one clause added inside the existing `## [Unreleased]` `### Changed` bullet, naming the `--check` staleness and `--strict` promotion consequences |

**Comment corrections** — the D-14 transform comment above the banner state machine gains the
asymmetry statement and its reachability bound (IN-01). No rendered byte moves.

**Files touched** — `install/install.ts`, `install/install.js`, `install/install.test.ts`,
`agent-factory/config/factory.config.md`, `scripts/model-dial-consistency.test.ts`, `CHANGELOG.md`.

<output>
Create `.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-05-SUMMARY.md` when done.
</output>

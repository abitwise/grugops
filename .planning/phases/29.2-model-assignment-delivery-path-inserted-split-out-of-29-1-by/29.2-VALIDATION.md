---
phase: "29.2"
slug: "model-assignment-delivery-path"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-04"
---

# Phase 29.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest ~4.1.8 (`globals: false`) |
| **Config file** | `vitest.config.ts` (repo root) |
| **Quick run command** | `npx vitest run install/install.test.ts` |
| **Full suite command** | `npx vitest run --exclude '**/scripts/e2e/**'` |
| **Estimated runtime** | ~185 seconds (full suite); quick run well under that |

**Do NOT run `npm test`** — it pulls in the live claude-CLI e2e lane under `scripts/e2e/`.

**Measured pre-change baseline (2026-09-04):** `1 failed | 2393 passed | 2 skipped` over 55 files. The single failure is pre-existing (WINDOWS.md row 91, `scripts/frontmatter.test.ts` D-49 false-red control) and is not owned by this phase. The phase exit criterion is that baseline or better, not "green". Its corpus is every tracked markdown file, so no markdown this phase writes may put a backslash sequence inside a double-quoted YAML scalar. Re-measure before the first task.

---

## Sampling Rate

- **After every task commit:** Run `npm run build && git diff --exit-code --name-only -- '*.js' && npm run typecheck && npx vitest run install/install.test.ts`
- **After every plan wave:** Run `npx vitest run --exclude '**/scripts/e2e/**' && node scripts/check-foundation-guards.js && npm run freshness:adapters`
- **Before `/gsd-verify-work`:** Full CI gate block from `.github/workflows/ci.yml` must exit 0 (see RESEARCH.md §Validation Architecture → Phase gate)
- **Max feedback latency:** ~190 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 29.2-01-T1 | 01 | 1 | MODEL-01…05 (D-01, D-02, D-03, D-05, D-06) | T-29.2-01, T-29.2-02, T-29.2-03, T-29.2-04, T-29.2-05, T-29.2-07 | Spawn carries no target-derived argv and no shell; the generator's fixed output directory is unchanged; an illegal `models` block installs no adapter and the run exits 3; no fallback byte source | integration | `npx vitest run install/install.test.ts` | ✅ (file + cases) | ✅ green |
| 29.2-01-T2 | 01 | 1 | MODEL-01…05 (D-04) | T-29.2-06, T-29.2-08, T-29.2-09 | The resolution reader is a fixed literal executed from a 0700 mkdtemp directory; the report names the alias as resolved, never a full model id | integration | `npx vitest run install/install.test.ts -t "resolution"` | ✅ (cases landed) | ✅ green |
| 29.2-02-T1 | 02 | 2 | MODEL-01…05 (D-11, D-12, D-13, D-14) | T-29.2-11, T-29.2-12, T-29.2-13, T-29.2-14 | The second slot preserves bounded removal; the skip comparison is over final bytes; exactly one banner per rendered agent adapter, refused by name otherwise | integration | `npx vitest run install/install.test.ts` and `npx vitest run scripts/adapter-byte-baseline.test.ts` | ✅ (files + cases) | ✅ green |
| 29.2-02-T2 | 02 | 2 | MODEL-01…05 (D-09, D-10) | T-29.2-10, T-29.2-15 | The doctor writes nothing inside the target and leaks no temp tree; a partial checkout reports the absence of a verdict rather than a clean one | integration | `npx vitest run install/install.test.ts -t "doctor"` | ✅ (block + cases) | ✅ green |
| 29.2-02-T3 | 02 | 2 | MODEL-01…05 (D-05, D-08, D-16) | T-29.2-13 | A fifth generator import cannot reach only one mirror; a reworded banner cannot silently stop matching; the self-checkout refusal fires before any render | unit + integration | `npx vitest run install/install.test.ts -t "self-checkout"` and `npx vitest run install/install.test.ts` | ✅ (six cases landed) | ✅ green |
| 29.2-03-T1 | 03 | 3 | MODEL-01…05 (D-07, D-15) | T-29.2-17, T-29.2-18, T-29.2-20 | No refuted disclosure ships; no unmeasured cost claim ships; the `.ts` and its committed `.js` move together | unit | `npx vitest run scripts/model-dial-consistency.test.ts` and `node scripts/check-banned-claims.js` | ✅ (edited in commit 89dd183) | ✅ green |
| 29.2-03-T2 | 03 | 3 | MODEL-01…05 (whole-phase close) | T-29.2-19, T-29.2-21, T-29.2-SC | Eight named bypass hypotheses attempted and recorded; the full CI gate block exits 0 against the measured baseline | gate | `npx vitest run --exclude '**/scripts/e2e/**'` plus the CI gate block in plan 03 task 2 | ✅ (all gates exist) | ✅ green |
| 29.2-04-T1 | 04 | 1 | MODEL-03 (CR-01) | T-29.2-22, T-29.2-27 | A target adapter destination whose leaf is a symbolic link is refused by name before any read and before any write; the file it points at is byte-unchanged; an ordinary install still exits 0 | integration (tracer, tdd) | `npx vitest run install/install.test.ts` | ✅ (2 cases landed, both watched RED first) | ✅ green |
| 29.2-04-T2 | 04 | 1 | MODEL-03, MODEL-04 (CR-01, IN-03) | T-29.2-23, T-29.2-24, T-29.2-25 | A destination reached through a symlinked ancestor directory is refused with both sides realpath-resolved; a dangling link creates nothing outside the target; the doctor names a hazardous file instead of reading through it and separates ENOENT from every other read error | integration (tdd) | `npx vitest run install/install.test.ts` | ✅ (4 cases landed; the dangling case was GREEN on arrival and mutation-proven instead) | ✅ green |
| 29.2-04-T3 | 04 | 1 | MODEL-05 (WR-04) | T-29.2-26, T-29.2-28 | The agents write loop has no path that copies or links raw mirror bytes; nine adversarial destination shapes re-attacked against the built artifact from outside the repository | unit + integration | `npx vitest run install/install.test.ts` plus the nine-shape adversarial re-run driving `install/install.js` on a scratch target | ✅ (case landed, mutation-proven) | ✅ green — 126 passed, 1 skipped; no attempt got a byte outside the target |
| 29.2-05-T1 | 05 | 2 | MODEL-01 (WR-01) | T-29.2-29 | The `--dry-run` preview is computed from the same comparison as the run, so a preview over an unchanged target reports `skipped` for every adapter a real run would skip | integration (tdd) | `npx vitest run install/install.test.ts` | ✅ (case landed) | ✅ green |
| 29.2-05-T2 | 05 | 2 | MODEL-01, MODEL-04 (WR-03, WR-05, IN-01) | T-29.2-30, T-29.2-31 | An unremovable temp tree reports itself under its own `cleanup` label and cannot convert a completed run into a crash or an exit 3; one wording authority for the temp mirror, consumed by three sites including the success path | integration | `npx vitest run install/install.test.ts` | ✅ (cases landed) | ✅ green |
| 29.2-05-T3 | 05 | 2 | MODEL-01…05 (WR-02, IN-02, IN-04) | T-29.2-32, T-29.2-33 | Kit prose states the installed-repository bound D-06 created; the retired-disclosure guard identifies the 29.1 sentence rather than a heading style; the CHANGELOG names the `--check` staleness and `--strict` promotion an existing user meets | unit | `npx vitest run scripts/model-dial-consistency.test.ts` and `node scripts/check-banned-claims.js` | ✅ (edited in commit `9f798e1`, D-15 same-commit pairing held) | ✅ green — 39 passed; banned-claims 0 findings over 117/117 elements |
| 29.2-06-T1 | 06 | 3 | MODEL-01…05 (round close, records) | T-29.2-34, T-29.2-35 | Every REVIEW.md finding id resolves to exactly one disposition derived from the review's own headings; what this round could not observe on Windows is recorded as unobserved rather than as passing | gate (records) | `node scripts/check-nul-bytes.js && node scripts/check-audit-register.js && node scripts/check-diff-disposition.js` and `npx vitest run scripts/frontmatter.test.ts` | ✅ (disposition table, ledger row 115, these rows) | ✅ green — guard chain exit 0 (nul-bytes 1730 files; audit-register all four equalities; diff-disposition 0 findings over 37/37); frontmatter at baseline, `1 failed \| 293 passed`, the single pre-existing D-49 refusal over `29.1-VERIFICATION-round4.md` and no new one |
| 29.2-06-T2 | 06 | 3 | MODEL-01…05 (delivery re-confirmation) | T-29.2-36, T-29.2-SC | The destination guard did not break the delivery path it protects: the five documented behaviours still hold against the guarded build; the gate block runs against what this round produced; the package manifest and lockfile diff across the round is empty | gate | the plan-03 gate block (freshness ×5, foundation-guards, kit-refs, public-docs-vocabulary, audit-register, claim-anchors, banned-claims, imperative-lexicon, diff-disposition, nul-bytes, check:build-parity, typecheck), then `npx vitest run --exclude '**/scripts/e2e/**'`, then `git diff --name-only --exit-code -- package.json package-lock.json` | ✅ (all gates exist) | ✅ green — gate block exit 0 end to end (16 commands, plus the structure validator run separately at exit 0); suite at baseline-or-better, `1 failed \| 54 passed (55)` files and `1 failed \| 2445 passed \| 2 skipped (2448)` on **two** consecutive runs, the single failure being the pre-existing `scripts/frontmatter.test.ts` D-49 refusal; package manifest and lockfile diff across `3fbf854..HEAD` **empty**; all five delivery behaviours re-observed live |

*Populated by the planner from PLAN.md tasks. Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Filled 2026-09-04 by plan 29.2-03 task 2, from the commands that actually ran.** Every row's
automated command was executed against the tree at that plan's second commit, not against an
assumption. `npx vitest run install/install.test.ts` → `119 passed | 1 skipped (120)`;
`npx vitest run scripts/model-dial-consistency.test.ts` → `39 passed`; the full CI gate block
(freshness, freshness:catalog, freshness:context, freshness:adapters, freshness:skill-twins,
generate:adapters + the adapter-directory cleanliness pair, check-foundation-guards, check-kit-refs,
check-public-docs-vocabulary, check-audit-register, check-claim-anchors, check-banned-claims,
check-imperative-lexicon, check-diff-disposition, check-nul-bytes, validate-agent-factory,
check:build-parity, typecheck) → every command exit 0. The suite exit criterion was the measured
baseline or better: `1 failed | 54 passed (55)` files before and after, the same single pre-existing
`scripts/frontmatter.test.ts` refusal over the same one document, with the passing count moving
2430 → 2436.

**Rows 03-T1 and 03-T2 are green on their own automated commands and NOT on the phase verdict.**
Task 2's review-and-reproduce round found five defects; four are fixed with RED-first cases and
mutation transcripts, and one is recorded as an open defect (WINDOWS ledger row 110). A green row
here means the command ran and exited 0 — it is not a claim that the surface is proven.

**Windows row.** `install/install.test.ts` runs on the `windows-latest` CI matrix leg, so the new
cases execute there; anything asserted about the mirror's Windows spawn, path or cleanup behaviour
stays `UNKNOWN - verify` until that leg reports green. WINDOWS ledger rows 105 through 107 are
untouched by this phase and stay open.

**Rows 04-T1 through 06-T2 filled 2026-09-04 by plan 29.2-06 task 1 (gap-closure round 1), from the
commands that actually ran.** Rows 04-T1…04-T3 and 05-T1…05-T3 carry the results recorded in
`29.2-04-SUMMARY.md` and `29.2-05-SUMMARY.md`, each of which is itself a record of a command
executed against the tree at that plan's commits. Row 06-T1 carries the guard chain and the
frontmatter control run in this task. Row 06-T2 was written `⬜ pending` at this task's commit
because its commands had not yet run, and is filled from the run in task 2 rather than predicted.

**The caveat this file already states applies to every row added above, unchanged in force.** A
green row means the command ran and exited 0. It is **not** a claim that the surface is proven —
this phase's standing rule is that a green suite is not proof of a safety invariant, and its
corollary governs the new rows: the destination-hazard guard's five symlink cases carry a `win32`
early return, so rows 04-T1 and 04-T2 are green on the **POSIX legs only**. WINDOWS ledger row 115
records that scope, names `SeCreateSymbolicLink` as the reason the fixture cannot exist on Windows,
and adds the directory-junction unknown the containment arm introduces. The `windows-latest` leg's
silence about those cases is an absence of observation, not a pass.

---

## Wave 0 Requirements

- [x] Re-measure the suite baseline before the first task and record the numbers. (Re-measured at plan 03 start on commit 8c1ec4e + waves 1-2: `1 failed | 54 passed (55)` files, `1 failed | 2430 passed | 2 skipped`.)
- [x] `install/install.test.ts` — new cases: fresh install resolves `inherit`; re-run after a `models` edit rewrites and reports; illegal block refuses the class and other classes complete; `--check` names stale adapters (WARN / `--strict` FAIL); idempotent re-run reports skipped-identical; self-checkout refusal pinned to this contract (D-16); explicit report-ordering assertion; uninstall parity; twin-list/generator-closure equality; banner-literal equality.
- [x] `scripts/model-dial-consistency.test.ts` — the "D-17 limitation is disclosed" describe block (~:1355-1363) must be rewritten in the same commit as `factory.config.md` (D-15).
- [x] `install/install.test.ts:2513` pinned `process.exit(` count stays 6/7 — D-03 must not add an exit call.
- [x] No framework install needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Windows spawn/path handling of the mirror | MODEL-01…05 (delivery) | No Windows runner in CI; reasoned from Node semantics only (`UNKNOWN - verify`) | On a Windows box: `node install/install.js --target <repo>` with a `models` block; confirm 17 adapters render and `--check` reports no stale files |
| Windows behaviour of the destination-hazard guard, including a **directory junction** | MODEL-03 (CR-01) | Creating a symlink on Windows needs the `SeCreateSymbolicLink` privilege an unprivileged runner does not hold, so 5 of the 7 write-bound cases carry a `win32` early return and the fixture cannot be built there. Separately, `realpathSync` resolves junctions and a junction is not a symbolic link to `lstat`, so which arm (if either) refuses one was never measured. `UNKNOWN - verify`; WINDOWS ledger row 115. | On a Windows box, in an installed target: (a) `mklink /J <target>\.claude\agents <dir outside the target>`, then re-run the installer over an edited `models` block and confirm nothing is written outside the target and the refusal names the path; (b) with the privilege granted or in Developer Mode, repeat the leaf-symlink and dangling-symlink plants and confirm arm 1 refuses both. |

**This row stays OPEN.** Nothing in gap-closure round 1 observed it; recording it is the whole point
of leaving it open rather than letting a green `windows-latest` badge imply it was covered.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 190s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending

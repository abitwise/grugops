---
phase: "29.2"
slug: "model-assignment-delivery-path"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-04"
---

# Phase 29.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest ~4.1.8 (`globals: false`) |
| **Config file** | `vitest.config.ts` (repo root) |
| **Quick run command** | `npx vitest run install/install.test.ts` |
| **Full suite command** | `npx vitest run --exclude '**/scripts/e2e/**'` |
| **Estimated runtime** | ~185 seconds (full suite); quick run well under that |

**Do NOT run `npm test`** — it pulls in the live claude-CLI e2e lane under `scripts/e2e/`.

**Measured pre-change baseline (2026-09-04):** `1 failed | 2393 passed | 2 skipped` over 55 files. The single failure is pre-existing (WINDOWS.md row 91, `scripts/frontmatter.test.ts` D-49 false-red control) and is not owned by this phase. The phase exit criterion is that baseline or better, not "green". Its corpus is every tracked markdown file, so no markdown this phase writes may put a backslash sequence inside a double-quoted YAML scalar. Re-measure before the first task.

---

## Sampling Rate

- **After every task commit:** Run `npm run build && git diff --exit-code --name-only -- '*.js' && npm run typecheck && npx vitest run install/install.test.ts`
- **After every plan wave:** Run `npx vitest run --exclude '**/scripts/e2e/**' && node scripts/check-foundation-guards.js && npm run freshness:adapters`
- **Before `/gsd-verify-work`:** Full CI gate block from `.github/workflows/ci.yml` must exit 0 (see RESEARCH.md §Validation Architecture → Phase gate)
- **Max feedback latency:** ~190 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 29.2-01-T1 | 01 | 1 | MODEL-01…05 (D-01, D-02, D-03, D-05, D-06) | T-29.2-01, T-29.2-02, T-29.2-03, T-29.2-04, T-29.2-05, T-29.2-07 | Spawn carries no target-derived argv and no shell; the generator's fixed output directory is unchanged; an illegal `models` block installs no adapter and the run exits 3; no fallback byte source | integration | `npx vitest run install/install.test.ts` | ✅ (file + cases) | ✅ green |
| 29.2-01-T2 | 01 | 1 | MODEL-01…05 (D-04) | T-29.2-06, T-29.2-08, T-29.2-09 | The resolution reader is a fixed literal executed from a 0700 mkdtemp directory; the report names the alias as resolved, never a full model id | integration | `npx vitest run install/install.test.ts -t "resolution"` | ✅ (cases landed) | ✅ green |
| 29.2-02-T1 | 02 | 2 | MODEL-01…05 (D-11, D-12, D-13, D-14) | T-29.2-11, T-29.2-12, T-29.2-13, T-29.2-14 | The second slot preserves bounded removal; the skip comparison is over final bytes; exactly one banner per rendered agent adapter, refused by name otherwise | integration | `npx vitest run install/install.test.ts` and `npx vitest run scripts/adapter-byte-baseline.test.ts` | ✅ (files + cases) | ✅ green |
| 29.2-02-T2 | 02 | 2 | MODEL-01…05 (D-09, D-10) | T-29.2-10, T-29.2-15 | The doctor writes nothing inside the target and leaks no temp tree; a partial checkout reports the absence of a verdict rather than a clean one | integration | `npx vitest run install/install.test.ts -t "doctor"` | ✅ (block + cases) | ✅ green |
| 29.2-02-T3 | 02 | 2 | MODEL-01…05 (D-05, D-08, D-16) | T-29.2-13 | A fifth generator import cannot reach only one mirror; a reworded banner cannot silently stop matching; the self-checkout refusal fires before any render | unit + integration | `npx vitest run install/install.test.ts -t "self-checkout"` and `npx vitest run install/install.test.ts` | ✅ (six cases landed) | ✅ green |
| 29.2-03-T1 | 03 | 3 | MODEL-01…05 (D-07, D-15) | T-29.2-17, T-29.2-18, T-29.2-20 | No refuted disclosure ships; no unmeasured cost claim ships; the `.ts` and its committed `.js` move together | unit | `npx vitest run scripts/model-dial-consistency.test.ts` and `node scripts/check-banned-claims.js` | ✅ (edited in commit 89dd183) | ✅ green |
| 29.2-03-T2 | 03 | 3 | MODEL-01…05 (whole-phase close) | T-29.2-19, T-29.2-21, T-29.2-SC | Eight named bypass hypotheses attempted and recorded; the full CI gate block exits 0 against the measured baseline | gate | `npx vitest run --exclude '**/scripts/e2e/**'` plus the CI gate block in plan 03 task 2 | ✅ (all gates exist) | ✅ green |

*Populated by the planner from PLAN.md tasks. Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Filled 2026-09-04 by plan 29.2-03 task 2, from the commands that actually ran.** Every row's
automated command was executed against the tree at that plan's second commit, not against an
assumption. `npx vitest run install/install.test.ts` → `119 passed | 1 skipped (120)`;
`npx vitest run scripts/model-dial-consistency.test.ts` → `39 passed`; the full CI gate block
(freshness, freshness:catalog, freshness:context, freshness:adapters, freshness:skill-twins,
generate:adapters + the adapter-directory cleanliness pair, check-foundation-guards, check-kit-refs,
check-public-docs-vocabulary, check-audit-register, check-claim-anchors, check-banned-claims,
check-imperative-lexicon, check-diff-disposition, check-nul-bytes, validate-agent-factory,
check:build-parity, typecheck) → every command exit 0. The suite exit criterion was the measured
baseline or better: `1 failed | 54 passed (55)` files before and after, the same single pre-existing
`scripts/frontmatter.test.ts` refusal over the same one document, with the passing count moving
2430 → 2436.

**Rows 03-T1 and 03-T2 are green on their own automated commands and NOT on the phase verdict.**
Task 2's review-and-reproduce round found five defects; four are fixed with RED-first cases and
mutation transcripts, and one is recorded as an open defect (WINDOWS ledger row 110). A green row
here means the command ran and exited 0 — it is not a claim that the surface is proven.

**Windows row.** `install/install.test.ts` runs on the `windows-latest` CI matrix leg, so the new
cases execute there; anything asserted about the mirror's Windows spawn, path or cleanup behaviour
stays `UNKNOWN - verify` until that leg reports green. WINDOWS ledger rows 105 through 107 are
untouched by this phase and stay open.

---

## Wave 0 Requirements

- [x] Re-measure the suite baseline before the first task and record the numbers. (Re-measured at plan 03 start on commit 8c1ec4e + waves 1-2: `1 failed | 54 passed (55)` files, `1 failed | 2430 passed | 2 skipped`.)
- [x] `install/install.test.ts` — new cases: fresh install resolves `inherit`; re-run after a `models` edit rewrites and reports; illegal block refuses the class and other classes complete; `--check` names stale adapters (WARN / `--strict` FAIL); idempotent re-run reports skipped-identical; self-checkout refusal pinned to this contract (D-16); explicit report-ordering assertion; uninstall parity; twin-list/generator-closure equality; banner-literal equality.
- [x] `scripts/model-dial-consistency.test.ts` — the "D-17 limitation is disclosed" describe block (~:1355-1363) must be rewritten in the same commit as `factory.config.md` (D-15).
- [x] `install/install.test.ts:2513` pinned `process.exit(` count stays 6/7 — D-03 must not add an exit call.
- [x] No framework install needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Windows spawn/path handling of the mirror | MODEL-01…05 (delivery) | No Windows runner in CI; reasoned from Node semantics only (`UNKNOWN - verify`) | On a Windows box: `node install/install.js --target <repo>` with a `models` block; confirm 17 adapters render and `--check` reports no stale files |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 190s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending

# Phase 29.2: Model Assignment Delivery Path - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-03
**Phase:** 29.2-model-assignment-delivery-path
**Areas discussed:** Generation site + coupling, Ordering + which config, Staleness after an edit, Ownership of installed adapters

---

## Generation site + coupling

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror-spawn the generator | cpSync committed twins + kit sources + the target's config into a temp tree, spawn, materialize the 17 results into the target; install/ never imports scripts/ | ✓ |
| Install-side model: slot rewrite | Treat `model:` like KIT=, fed by a new `model-tiers.js --resolve` CLI | |
| Import model-tiers.js into install.ts | Reverse kit-source's D-18 decoupling by decision | |

**User's choice:** Mirror-spawn the generator.

| Option | Description | Selected |
|--------|-------------|----------|
| Checkout only | Install already runs from GRUGOPS_SRC; a refresh is a re-run from the checkout | ✓ |
| Also ship the twins into $GRUGOPS_HOME | Refresh without the checkout; widens kit-home + uninstall contracts | |

**User's choice:** Checkout only.

| Option | Description | Selected |
|--------|-------------|----------|
| Refuse the adapter class by name | No adapter written, offending value + legal set named, nonzero exit, other classes complete | ✓ |
| Write inherit adapters and WARN | Never adapter-less, but the silent-downgrade shape D-11 rejects | |
| Abort the whole install | Nothing written in any class | |

**User's choice:** Refuse the adapter class by name.

| Option | Description | Selected |
|--------|-------------|----------|
| Banner + per-adapter model, dry-run resolves too | Banner names preset + source file; each adapter line reports its model; --dry-run resolves read-only | ✓ |
| Banner only | One line, per-adapter lines unchanged | |
| You decide | Planner picks detail within existing report() voice | |

**User's choice:** Banner + per-adapter model, dry-run resolves too.

---

## Ordering + which config

| Option | Description | Selected |
|--------|-------------|----------|
| Keep order; resolve against target config if present | Mirror copies TARGET/.grugops/factory.config.json when present; absent = zero-config inherit | ✓ |
| Move seedState before the adapter loop | Mirror always finds a config; reorders the pinned sequence | |

**User's choice:** Keep order.

| Option | Description | Selected |
|--------|-------------|----------|
| TARGET/.grugops/factory.config.json only | Mirror never receives an in-kit config; second candidate cannot fire | ✓ |
| Also honour a vendored agent-factory/config | Re-creates whole-file-wins shadowing | |

**User's choice:** TARGET/.grugops/factory.config.json only.

| Option | Description | Selected |
|--------|-------------|----------|
| No; re-running install is the refresh | --update stays kit-home-only; kit states the re-run | ✓ |
| Yes, --update --target refreshes adapters | Widens --update into a per-repo writer | |

**User's choice:** No; re-running install is the refresh.

| Option | Description | Selected |
|--------|-------------|----------|
| Leave models absent in the seed | Zero-config first; docs name the block | ✓ |
| Seed `"models": {"preset": "none"}` | Discoverable in the user's own file | |

**User's choice:** Leave models absent in the seed.

---

## Staleness after an edit

| Option | Description | Selected |
|--------|-------------|----------|
| Re-render via the same mirror path and byte-compare | Doctor renders into a temp dir (KIT= materialized too) and compares each target adapter, naming stale files | ✓ |
| Provenance block + config hash | Proves the config changed, not that the adapter is wrong | |
| Parse the model: line in the doctor | A second frontmatter grammar | |

**User's choice:** Re-render and byte-compare.

| Option | Description | Selected |
|--------|-------------|----------|
| WARN by default, FAIL under --strict | Matches the doctor's existing contract | ✓ |
| Always FAIL | Treat like a kit-root disagreement | |

**User's choice:** WARN by default, FAIL under --strict.

| Option | Description | Selected |
|--------|-------------|----------|
| Skip-if-identical, rewrite reported by name | Identical → skipped (identical); differing → materialized with model | ✓ |
| Keep unconditional write | Every re-run reports materialized for all 17 | |

**User's choice:** Skip-if-identical.

| Option | Description | Selected |
|--------|-------------|----------|
| No new block; byte-compare is the record | No per-target bytes beyond the model line | ✓ |
| Add a one-line provenance comment | Human-readable; a second place the preset is spelled | |

**User's choice:** No new block.

---

## Ownership of installed adapters

| Option | Description | Selected |
|--------|-------------|----------|
| Kit-owned; re-run overwrites | Differing adapter rewritten and reported; hand edits lost by design | ✓ |
| Never-overwrite when the target copy differs | Mirrors materializeRunnable; a models edit could never reach a touched adapter | |

**User's choice:** Kit-owned; re-run overwrites.

| Option | Description | Selected |
|--------|-------------|----------|
| Materialize a target-true banner line | Second slot like KIT=; names the install re-run and .grugops/factory.config.json | ✓ |
| Leave the banner as the kit renders it | Command wrong in a target; doctor names the remedy | |
| You decide | Provided nothing names a command the target cannot run | |

**User's choice:** Materialize a target-true banner line.

| Option | Description | Selected |
|--------|-------------|----------|
| Rewrite kit prose to current truth + CHANGELOG entry | Kit docs/headers state what is true now; `### Changed` owed per WR-03 ruling | ✓ |
| Annotate in place, keep the old text standing | Reader meets a refuted sentence first | |

**User's choice:** Rewrite kit prose + CHANGELOG entry.

| Option | Description | Selected |
|--------|-------------|----------|
| Rely on the D-07 self-checkout refusal; verify it in a test | Install refuses its own checkout; planner pins that to this contract | ✓ |
| Add an explicit zero-config pin to the install render | Extra machinery for a path the guard already blocks | |

**User's choice:** Rely on the D-07 refusal, pinned by a test. Flagged during discussion: `--allow-self` / `--force` overrides the guard; CI freshness gate + `guard_model_assignment` are the backstop.

---

## Claude's Discretion

- Temp-tree layout, cleanup, Windows spawn handling (follow `adapters-freshness.ts`).
- Doctor reporting when the checkout's committed twins are missing.
- Whether `--dry-run` also prints the doctor's staleness verdict.
- Exact finding and report wording (clear professional voice).

## Deferred Ideas

- `--update --target <repo>` refreshing a target's adapters.
- Shipping the generator's import closure into `$GRUGOPS_HOME`.
- Seeding `"models": {"preset": "none"}`.
- A provenance comment naming the preset in each target adapter.

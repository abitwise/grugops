---
phase: 29.2-model-assignment-delivery-path
plan: 06
subsystem: infra
tags: [records, disposition, ledger, validation, gate-block, delivery-contract, windows-scope]

# Dependency graph
requires:
  - phase: 29.2-04
    provides: the CR-01 / WR-04 / IN-03 commits this round's disposition table records, and the win32 early returns that make a Windows ledger row owed
  - phase: 29.2-05
    provides: the WR-01 / WR-02 / WR-03 / WR-05 / IN-01 / IN-02 / IN-04 commits this round's disposition table records
provides:
  - "A ten-row disposition table in deferred-items.md whose row count is DERIVED from 29.2-REVIEW.md's own headings and asserted against it"
  - "WINDOWS ledger row 115 — the POSIX-only scope of the plan-04 destination-hazard guard, naming SeCreateSymbolicLink and the directory-junction unknown the containment arm introduces"
  - "29.2-VALIDATION.md Per-Task Verification Map rows for all eight tasks in plans 04, 05 and 06, each carrying the command that actually ran"
  - "A live re-confirmation that the guard did not break the delivery path it protects — all five documented behaviours re-observed against the built artifact"
  - "A recorded gate-block run and two suite runs against the tree this round produced"
affects: [29.2 phase close, verification, ship]

actuals:
  tokens: 6600   # chars/4 over the realized diff (22,030 chars task 1 + ~4,372 chars the task-2 VALIDATION edit)
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Derive the denominator, then assert the table against it — a disposition list copied from the plan that ordered it is the set-literal drift class wearing a completeness claim"
    - "Check a ledger row's continued accuracy by MEASUREMENT rather than by reading the guard that might have changed it — the probe decides whether an annotation is owed, not the reasoning"
    - "Re-run the delivery contract before the gate block, not only the guard — the characteristic failure of a safety guard is refusing something legitimate"
    - "Record an unobservable condition as unobserved; a CI leg that cannot build the fixture is silent, and silence is not a pass"

key-files:
  created:
    - ".planning/phases/29.2-…/29.2-06-SUMMARY.md"
  modified:
    - ".planning/phases/29.2-…/deferred-items.md — the round's disposition table, the ledger-row index, and the measured row-111 finding"
    - ".planning/WINDOWS.md — row 115 (unrun-verify, install/install.ts)"
    - ".planning/phases/29.2-…/29.2-VALIDATION.md — eight Per-Task Verification Map rows, the repeated caveat, and the Windows junction probe"

key-decisions:
  - "The disposition table's denominator was READ OUT OF 29.2-REVIEW.md's headings (grep -cE '^### (CR|WR|IN)-[0-9]+:' → 10) and asserted against the table's own row count (10), rather than transcribed from the plan's copy of the list. A third independent number — the review frontmatter's `total: 10` — agrees."
  - "No second ledger row was manufactured. The two candidates plan 04 routed forward were already recorded by plan 04 as rows 113 and 114; re-recording them here would double-count the register rather than add to it."
  - "Row 111 gets NO dated annotation, because a probe run this session shows its text still describes the tree exactly. The plan authorized an annotation conditionally; the condition was tested rather than assumed."
  - "Row 06-T2 of the Per-Task Verification Map was written `⬜ pending` at task 1's commit because its commands had not run yet, and filled from the run in task 2. A row predicting its own result is the record this phase exists to refuse."
  - "The suite is reported as the measured baseline or better and never as green — 2445 passing against a 2436 floor, with the one pre-existing frontmatter refusal named."

patterns-established:
  - "A round closes on two records, not one claim: every finding resolved to something a reader can open, and the certifying gate re-run against what the round actually produced"

requirements-completed: [MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05]

coverage:
  - id: D1
    description: "Every finding id in 29.2-REVIEW.md resolves to exactly one disposition, and the count of ids equals the count of dispositions — both numbers measured this session."
    requirement: "MODEL-01"
    verification:
      - kind: integration
        ref: "grep -cE '^### (CR|WR|IN)-[0-9]+:' 29.2-REVIEW.md = 10; disposition rows = 10; per-id occurrence count = 1 for all ten ids"
        status: pass
    human_judgment: false
  - id: D2
    description: "The POSIX-only scope of the destination-hazard guard is recorded as unobserved on Windows rather than allowed to read as a pass, with the specific junction unknown the containment arm introduces."
    requirement: "MODEL-03"
    verification:
      - kind: integration
        ref: ".planning/WINDOWS.md row 115 — file install/install.ts, contains SeCreateSymbolicLink and UNKNOWN - verify; 5 of 7 write-bound cases carry a win32 early return, both counts derived from install/install.test.ts this session"
        status: pass
    human_judgment: false
  - id: D3
    description: "The guard did not break the delivery path D-07 and D-15 documented: all five behaviours 29.2-VERIFICATION.md reproduced live still hold against the guarded build."
    requirement: "MODEL-01"
    verification:
      - kind: integration
        ref: "five live runs driving the built install/install.js on scratch targets outside the repository — 4 opus + 13 sonnet; 17 skipped-identical; --check exit 0 naming 17 / --strict exit 1; unknown preset exit 3 with no agents directory; hand-edit reverted and reported by name"
        status: pass
    human_judgment: false
  - id: D4
    description: "The gate block that certified plans 01-03 runs end to end against the tree this round produced, and the suite is at the measured baseline or better."
    requirement: "MODEL-05"
    verification:
      - kind: integration
        ref: "16-command gate block exit 0, plus the structure validator at exit 0; two consecutive full-suite runs at 1 failed / 2445 passed / 2 skipped"
        status: pass
    human_judgment: false
  - id: D5
    description: "The round introduced no dependency: the package manifest and lockfile diff across the whole round is empty, asserted rather than assumed."
    requirement: "MODEL-05"
    verification:
      - kind: integration
        ref: "git diff --name-only 3fbf854..HEAD -- package.json package-lock.json → no output; working-tree arm --exit-code → 0"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-09-04
status: complete
---

# Phase 29.2 Plan 06: Closing the Round Honestly Summary

**Ten review findings resolved to ten commits with none deferred and none silent, one ledger row for the Windows behaviour this round could not see, and a live re-confirmation that the destination guard did not break the delivery path it protects — every number in this file read off a command that ran in this session.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-09-04T18:32:17Z
- **Completed:** 2026-09-04T18:47:51Z
- **Tasks:** 2
- **Files modified:** 3 (plus this summary)

## Task Commits

1. **Task 1: Ten findings, ten dispositions — and a ledger row for what this round could not see** — `d4a5475` (docs)
2. **Task 2: Re-run the proven gate block and record what it actually said** — this commit (docs)

## The denominator, derived rather than restated

Three numbers, taken independently this session, all agree on **10**:

```
$ grep -cE '^### (CR|WR|IN)-[0-9]+:' 29.2-REVIEW.md
10
$ grep -oE '^### (CR|WR|IN)-[0-9]+' 29.2-REVIEW.md | sed 's/^### //' | sort -u | tr '\n' ' '
CR-01 IN-01 IN-02 IN-03 IN-04 WR-01 WR-02 WR-03 WR-04 WR-05
$ sed -n '/^findings:/,/^status:/p' 29.2-REVIEW.md   # the review's own frontmatter
  total: 10
```

The disposition table appended to `deferred-items.md` has **10** rows, and each of the ten ids
occurs in it **exactly once** (asserted per-id, not by a total). Every disposition is `fixed`; each
was confirmed by locating the shipped construct in `install/install.ts`, `factory.config.md`,
`scripts/model-dial-consistency.test.ts` or `CHANGELOG.md` **and** its commit, rather than by
trusting the plan that intended it. Because no row is `deferred`, no row owes a ledger id under the
table's own rule.

| Finding | Disposition | Commit |
|---|---|---|
| CR-01 | fixed (04-T1, 04-T2) | `91654b3`, `379fa2b` |
| WR-01 | fixed (05-T1) | `e6d9a8a` |
| WR-02 | fixed (05-T3) | `9f798e1` |
| WR-03 | fixed (05-T2) | `7e5c157` |
| WR-04 | fixed (04-T3) | `e2abb55` |
| WR-05 | fixed (05-T2) | `7e5c157` |
| IN-01 | fixed (05-T2) | `7e5c157` |
| IN-02 | fixed (05-T3) | `9f798e1` |
| IN-03 | fixed (04-T2) | `379fa2b` |
| IN-04 | fixed (05-T3) | `9f798e1` |

The plan's expected shape held on all ten. That is a result, not a foregone conclusion — it was
checked against the tree, and a row that had not landed would have said `deferred`.

## Ledger row 115, and why exactly one row was owed

`install/install.test.ts` carries **7** cases under the write-bound block; **5** of them
(`:2090`, `:2136`, `:2169`, `:2210`, `:2243`) open with `if (process.platform === "win32") return;`.
Both counts were derived from the file this session. Creating a symlink on Windows needs the
`SeCreateSymbolicLink` privilege an unprivileged runner does not hold, so `symlinkSync` throws
`EPERM` and the plant would assert nothing at all. Row 115 (`unrun-verify`, `install/install.ts`)
records that the leaf, ancestor, dangling and union shapes are proven on the **POSIX legs only**,
carries `UNKNOWN - verify`, and states the additional unknown the containment arm introduces:
`realpathSync` resolves drive letters, UNC paths and directory junctions, and **a junction is not a
symbolic link to `lstat`**, so whether either arm refuses a junctioned agents directory was never
measured. Remedy recorded: read the `windows-latest` leg, and probe a junction (`mklink /J`) there
before treating the containment bound as cross-platform.

**No second row was manufactured.** The plan permitted one only if plan 04's adversarial round had
surfaced something real routed here. It had surfaced two things — the transient suite failure and
the sibling-prefix trap — and plan 04 had already recorded both, as rows 113 and 114. Adding them
again would inflate the register rather than extend it, so they are indexed in `deferred-items.md`
and not duplicated.

## Rows 110, 111, 112 — and why row 111 gets no annotation

The plan authorized a dated annotation on row 111 **if** the plan-04 guard changed which shapes
reach `writeFileSync`. That condition was tested rather than assumed. Probe against the built
`install/install.js` on a scratch target outside the repository: an ordinary install (18
materialized, exit 0), then the row-111 shape planted — a **directory** at
`.claude/agents/grugops-agents-md-scribe.md` — and the installer re-run over an edited config so the
write arm is reached:

```
run2 exit=1
Error: EISDIR: illegal operation on a directory, open '<target>/.claude/agents/grugops-agents-md-scribe.md'
    at writeFileSync (node:fs:2415:20)
    at materializeAdapter (install/install.js:1928:5)
```

Row 111's description holds exactly as written: the write half still throws uncaught, part-way
through the adapter loop, at exit 1 with a stack trace. `adapterDestHazard()` returns `null` for a
directory inside the target — arm 1 is `lstat`-based and a directory is not a link, arm 2 resolves
it inside the target — so the guard deliberately does not reach this shape, as plan 04 stated in
source. **Row 111 is accurate and complete as written; no annotation is owed.** Rows 110, 111 and
112 were verified byte-unchanged against `HEAD` before the commit.

## The delivery contract, re-confirmed live against the guarded build

The characteristic failure of a safety guard is refusing something legitimate. All five behaviours
`29.2-VERIFICATION.md` reproduced live were re-run against the built `install/install.js`, on
scratch targets **outside the repository**, driven as
`INSTALL_MODE=copy GRUGOPS_SRC=<repo> GRUGOPS_HOME=<tmp> TARGET=<tmp> node <install.js> --yes`.

**Behaviour 1 — fresh install with `{"models":{"preset":"tiered"}}` (D-01, D-04).** Exit 0.

```
  model=opus lines: 4
  model=sonnet lines: 13
  --- rendered frontmatter tally ---
     4 model: opus
    13 model: sonnet
  resolution     read from <target>/.grugops/factory.config.json
  resolution     the adapters were written to <target>/.claude/agents; any path inside the relayed
                 message above is a temporary mirror that no longer exists.
```

The alias split was counted twice, independently: once in the report lines and once in the
seventeen rendered files' own `model:` frontmatter. The resolution line names the **target's**
config path. The second resolution line is plan 05's WR-05 disclaimer, now on the success path.

**Behaviour 2 — an identical re-run is a visible no-op (D-11).** Exit 0.

```
  agents skipped-identical: 17
  total skipped-identical: 24
  materialized lines: 0
  == install complete ==
```

Exactly **17** `skipped (identical copy present)` lines over `.claude/agents` (24 including the
seven skill files), and **zero** materialized. The guard runs above the skip-if-identical arm and
did not disturb it.

**Behaviour 3 — staleness after a config edit (D-09, D-10).** Config edited `tiered` → `none`.

```
  $ node install/install.js --check     → exit 0
  WARN  stale adapter(s) under <target>/.claude/agents — 17 differ: grugops-agents-md-scribe.md,
        grugops-architect-design.md, grugops-ba-pm.md, grugops-brownfield-mapper.md,
        grugops-compliance-officer.md, grugops-factory-coach.md, grugops-frontend-ui.md,
        grugops-greenfield-mapper.md, grugops-incident-responder.md, grugops-installer.md,
        grugops-orchestrator.md, grugops-qe-e2e.md, grugops-release-manager.md,
        grugops-security-nfr.md, grugops-software-engineer.md, grugops-system-analyst.md,
        grugops-uat-planner.md.
  ALL CHECKS PASSED (1 warning(s))

  $ node install/install.js --check --strict   → exit 1
```

Seventeen distinct adapter names counted in the report, `--check` at exit 0, `--strict` at exit 1.

**Behaviour 4 — an unknown preset refuses the class (D-03).** Fresh target, exit **3**.

```
  no .claude/agents directory created
  .claude/skills entries: 7        AGENTS.md written
  ERROR  model-tiers: <mirror>/.grugops/factory.config.json sets `models.preset` to
         "nonexistent-preset", which is not a legal preset name. The legal set is exactly:
         "none", "tiered". …
  == install INCOMPLETE — 1 item(s) need verification ==
```

No `.claude/agents` directory was created, the other classes completed (7 skills, `AGENTS.md`), and
the failure arm carries the temp-mirror disclaimer — WR-05's second consumer, observed rather than
inferred.

**Behaviour 5 — a hand-edited `model:` line is reverted and reported by name (D-13).**

```
  before hand-edit:  model: opus
  after hand-edit:   model: haiku
  $ node install/install.js --yes   → exit 0
  after re-run:      model: opus
  materialized   .claude/agents/grugops-orchestrator.md (KIT=…/agent-factory, model=opus)
  materialized: 1   skipped-identical over agents: 16
```

One materialized, sixteen skipped-identical: ownership is still exact and still reported by name.

**No behaviour was broken by the guard.** D-07's and D-15's documented delivery path — edit the
block, re-run the installer — works end to end on the guarded build.

## The gate block, per command

Run as one command line so a single non-zero halts it. **`GATE_BLOCK_EXIT=0`.**

| # | Command | Observed |
|---|---|---|
| 1 | `npm run freshness` | 0 — 50 committed `.js` match a rebuild; HEAD `d4a5475`; set-equality with the filesystem walk holds both directions |
| 2 | `npm run freshness:catalog` | 0 — `docs/catalog/README.md` matches a fresh regeneration |
| 3 | `npm run freshness:context` | 0 — no `.grugops/context/` tree exists (vacuous pass, stated as such by the gate) |
| 4 | `npm run freshness:adapters` | 0 — **17 adapters compared, 0 byte differences**, listings set-equal; mirrored generator resolved preset `none` |
| 5 | `npm run freshness:skill-twins` | 0 — 7 twins compared, 0 byte differences |
| 6 | `node scripts/check-foundation-guards.js` | `ALL CHECKS PASSED` — **KIT-03: 17 roles == 17 adapters == 17 grant-closure names**; `guard_model_assignment` 17 committed adapters against a recomputed resolution; one non-blocking `WARN` (`security-nfr.md` 4931B approaching its 4830B ceiling), pre-existing and not this round's |
| 7 | `node scripts/check-kit-refs.js` | `ALL CHECKS PASSED` — 24 adapters derived; the config self-reference exemption is exactly 3 mentions on 2 lines |
| 8 | `node scripts/check-public-docs-vocabulary.js` | `ALL CHECKS PASSED` — 10 public documents, zero retired vocabulary |
| 9 | `node scripts/check-audit-register.js` | `ALL CHECKS PASSED` — all four equalities hold |
| 10 | `node scripts/check-claim-anchors.js` | `ALL CHECKS PASSED` — 47/47 registry rows byte-identical at their anchors |
| 11 | `node scripts/check-banned-claims.js` | `ALL CHECKS PASSED` — **0 findings over 117/117 elements** |
| 12 | `node scripts/check-imperative-lexicon.js` | `ALL CHECKS PASSED` — 0 findings over 19/19 (`## Steps`) and 47/47 (sentence form) |
| 13 | `node scripts/check-diff-disposition.js` | `ALL CHECKS PASSED` — **0 findings over 37/37 changed watched files**, with the uncommitted working tree counted as a carrier |
| 14 | `node scripts/check-nul-bytes.js` | `ALL CHECKS PASSED` — 1730 tracked files, zero forbidden control bytes |
| 15 | `npm run check:build-parity` | 0 — "no tracked build output moved when tsc ran" |
| 16 | `npm run typecheck` | 0 — both targets (`tsc --noEmit` and `tsconfig.tests.json`) |
| + | `VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js` | `ALL CHECKS PASSED` (run separately; in CI's block, not in the plan's line) |

The command list was checked against `.github/workflows/ci.yml` before running rather than trusted
from the plan's copy of it.

## The suite, as counts against the measured baseline

Run separately from the gate block, twice, so the result is a count rather than an exit code.

| Run | Files | Tests | Duration |
|---|---|---|---|
| 1 | `1 failed \| 54 passed (55)` | `1 failed \| 2445 passed \| 2 skipped (2448)` | 200.1s |
| 2 | `1 failed \| 54 passed (55)` | `1 failed \| 2445 passed \| 2 skipped (2448)` | 237.6s |

The single failing file is `scripts/frontmatter.test.ts` — the pre-existing D-49 / WINDOWS-row-91
false-red control, refusing one document
(`.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round4.md`, a `\[` sequence
inside a double-quoted scalar), over a run-time-derived corpus of 1465 tracked markdown files. It is
not owned by this phase and was present at the round's base commit.

**This is the measured baseline or better, and it is NOT green.** The stated floor was at most one
failure — that one — and at least 2436 passing. Observed: 2445 passing, **+9 above the floor**. The
nine are plans 04 and 05's own new cases (seven write-bound cases in plan 04, two preview-parity
cases in plan 05), which is what moved the count from the plan-03 record of 2436 to 2443 and then to
2445. Nothing in plan 06 adds a test; both of its tasks write records.

**The plan-04 transient did not recur.** Plan 04 recorded one unreproduced full run at
`2 failed | 2442 passed`, one failing file above baseline, never identified. Adding this plan's two
runs, the round has now seen **six consecutive full runs at the baseline** since that single
anomaly (three in plan 04, one in plan 05, two here). Six clean runs are not a diagnosis. It stays
**`UNKNOWN - verify`** on WINDOWS ledger row 113, open, and is explicitly **not** recorded as
resolved.

## Supply chain — asserted, not assumed (T-29.2-SC)

```
$ git diff --name-only 3fbf854..HEAD -- package.json package-lock.json
                        (no output)
$ git diff --name-only --exit-code -- package.json package-lock.json
exit=0
```

`3fbf854` is the round base — the parent of plan 04's first commit. Neither the manifest nor the
lockfile moved across the entire round, on the committed arm or in the working tree. The thirteen
files that did change across `3fbf854..HEAD` are three `.planning/` records from waves 1-2, four
phase artifacts, and six source/test/prose files. **No package was installed and none was
attempted**, so the package-legitimacy exclusion under deviation Rule 3 was never reached.

## What this round establishes, and what it does not

This round proves that the four destination shapes — a leaf symlink, a symlinked ancestor
directory, a dangling link, and the two together — were re-attacked against the **built artifact**
from a scratch directory outside the repository, and that no byte reached outside the target on any
of them; that the sibling-prefix trap is refused by a segment-wise compare rather than a bare
prefix; and that the guard did not cost the delivery path any of its five documented behaviours.
All of that is **POSIX-scoped**. The `windows-latest` leg cannot run those cases at all, because
the fixture needs a privilege the runner does not hold, and its silence about them is an absence of
observation rather than a pass — WINDOWS row 115 exists so that a later reader meets that fact
instead of a green badge. The containment arm's behaviour over a Windows **directory junction** was
never measured in any session. A green suite here remains a floor and not a proof: this repository's
standing rule is that for a safety invariant, green tests are not evidence, and two clean runs of
2445 passing tests do not tell anyone that no bypass remains — they tell them that the bypasses
someone thought to write down are closed.

## Decisions Made

Recorded in `key-decisions` above. The load-bearing one: **derive the denominator, then assert the
table against it.** A disposition list transcribed from the plan that ordered it would be this
repository's second named systemic failure class — a hand-maintained set rotting while staying
green — wearing a completeness claim. Three independently taken numbers agree on ten.

The second: **test the condition before writing the record.** The plan authorized an annotation on
ledger row 111 if the guard had changed what reaches `writeFileSync`. Reasoning from the predicate
would have produced a plausible answer; a two-minute probe produced a measured one, and it says no
annotation is owed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical correctness] The 06-T2 validation row would have shipped as a stale `⬜ pending`**

- **Found during:** Task 2
- **Issue:** Task 1's `<files>` list owns `29.2-VALIDATION.md` and task 2's does not, but task 1
  runs before task 2's commands exist. Row 06-T2 was therefore written `⬜ pending` at task 1's
  commit. Leaving it there would have shipped a row that names a command and reports no result —
  a false record in the exact file whose purpose is to carry results that actually ran, and one
  this plan's own success criterion forbids.
- **Fix:** Task 2 filled row 06-T2 from its own measured run (gate block exit 0, two suite runs at
  `1 failed | 2445 passed | 2 skipped`, empty dependency diff) and committed the edit alongside this
  summary.
- **Files modified:** `29.2-VALIDATION.md`
- **Verification:** the row now carries observed counts; `check-diff-disposition` and
  `check-nul-bytes` re-run at exit 0 over the edited file.
- **Committed in:** this commit

**2. [Rule 2 - Missing critical functionality] One extra full-suite run beyond the plan's requirement**

- **Found during:** Task 2
- **Issue:** The plan calls for the suite once. The plan-04 transient was explicitly routed to this
  plan for disposition, and a single run cannot distinguish "did not recur" from "not looked for".
- **Fix:** The suite was run twice; both runs are recorded with their durations. The transient still
  does not reproduce, and it is recorded as unreproduced with the run count rather than as resolved.
- **Files modified:** none (a measurement)
- **Committed in:** this commit

---

**Total deviations:** 2 auto-fixed (both Rule 2). **Impact on plan:** neither changed scope. No
architectural change, no Rule 4 escalation, no package install attempted.

## Issues Encountered

None blocking. The gate block emitted one non-blocking `WARN` inside `check-foundation-guards.js`
(`agent-factory/roles/security-nfr.md` at 4931B against a 4830B advisory ceiling). It is
pre-existing, it is a warning rather than a failure, the gate reported `ALL CHECKS PASSED`, and it
is out of scope under this plan's scope boundary — it was not introduced by anything in this round
and is recorded here rather than fixed.

## Known Stubs

None. This plan writes records only — no symbol, no test, no production source. No placeholder,
hardcoded-empty or "coming soon" value was introduced.

## Threat Flags

None. No endpoint, auth path, file-access pattern or schema at a trust boundary was added or moved.

Threat register dispositions from the plan, as executed:

| Threat ID | Disposition | Status |
|---|---|---|
| T-29.2-34 (a dropped finding disappearing from the disposition table) | mitigate | closed — the row count is derived from the review's own headings and asserted against it; a third number (the review frontmatter's `total`) agrees |
| T-29.2-35 (a POSIX-only proof read as cross-platform) | mitigate | closed — WINDOWS row 115 states the scope, names `SeCreateSymbolicLink`, and adds the directory-junction unknown; the VALIDATION Windows row gains the junction probe and stays open |
| T-29.2-36 (the new guard refusing legitimate installs) | mitigate | closed — all five documented delivery behaviours re-run live against the guarded build, output quoted above |
| T-29.2-SC (dependency introduced in a round that declared none) | mitigate | closed — manifest and lockfile diff across `3fbf854..HEAD` empty on both arms, asserted rather than assumed |

## Verification Results

| Gate | Result |
|---|---|
| `node scripts/check-nul-bytes.js && node scripts/check-audit-register.js && node scripts/check-diff-disposition.js` (task 1) | 0 — run twice, before and after the full set of task-1 edits; diff-disposition 0 findings over 37/37 |
| `npx vitest run scripts/frontmatter.test.ts` (task 1 control) | at baseline — `1 failed \| 293 passed (294)`, the single pre-existing D-49 refusal over one document and **no new one**, so the markdown written by this task parses |
| The 16-command gate block (task 2) | **`GATE_BLOCK_EXIT=0`** — per-command outcomes tabulated above |
| `VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js` | `ALL CHECKS PASSED` |
| `npx vitest run --exclude '**/scripts/e2e/**'` ×2 | at baseline-or-better — `1 failed \| 2445 passed \| 2 skipped` both times, floor 2436. **NOT green and not reported as green.** |
| `git diff --name-only --exit-code -- package.json package-lock.json` | 0, no filename printed |
| `git diff --exit-code --name-only -- '*.js'` | 0 before the gate block, so the parity result measures the tree and not an uncommitted build |

## Self-Check: PASSED

- `deferred-items.md` — FOUND; disposition table row count **10**, equal to the review's derived
  heading count **10**; each of the ten ids occurs exactly once; zero rows carry `deferred`.
- `.planning/WINDOWS.md` — FOUND; row **115** present with `file` = `install/install.ts`, containing
  `SeCreateSymbolicLink` (1) and `UNKNOWN - verify` (1). Rows 110, 111 and 112 verified
  **byte-unchanged** against `HEAD` by md5 comparison before the task-1 commit.
- `29.2-VALIDATION.md` — FOUND; **8** Per-Task Verification Map rows for plans 04, 05 and 06
  (`29.2-04-T1 … 29.2-06-T2`), one per task, none left `⬜ pending`.
- Commit `d4a5475` — FOUND in `git log`; `git diff --diff-filter=D HEAD~1 HEAD` empty, so it deleted
  nothing.
- Commits `91654b3`, `379fa2b`, `e2abb55`, `e6d9a8a`, `7e5c157`, `9f798e1` — all FOUND, and each was
  opened with `git show --stat` to confirm it touched the files its disposition row names.

## Next Phase Readiness

Gap-closure round 1 is closed on the record: ten findings, ten dispositions, one ledger row for what
could not be observed, and a gate block re-run against what the round produced. Ready for phase
verification.

Carried forward, named rather than dropped:

- **WINDOWS row 115** — the destination guard's Windows scope, including the directory-junction
  unknown. Read the `windows-latest` leg; probe a junction there.
- **WINDOWS row 113** — the unreproduced transient full-suite failure, now at six consecutive clean
  runs since the anomaly and still `UNKNOWN - verify`.
- **WINDOWS row 114** — the sibling-prefix containment trap, covered by an adversarial probe only
  and not by a CI regression case.
- **WINDOWS rows 108, 109, 110, 111, 112** — unchanged and open, exactly as plans 01-04 left them.
  Row 111's continued accuracy was measured this session.
- **The `security-nfr.md` role-size WARN** — pre-existing, non-blocking, unowned by this phase.

---
*Phase: 29.2-model-assignment-delivery-path*
*Completed: 2026-09-04*

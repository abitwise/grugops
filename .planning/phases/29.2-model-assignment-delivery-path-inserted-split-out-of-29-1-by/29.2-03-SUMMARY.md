---
phase: 29.2-model-assignment-delivery-path
plan: 03
subsystem: infra
tags: [installer, documentation, changelog, red-team, code-review, claude-code-adapters]

requires:
  - phase: 29.2-model-assignment-delivery-path
    plan: 01
    provides: "renderAdaptersInMirror() - the one render authority - and the resolution report"
  - phase: 29.2-model-assignment-delivery-path
    plan: 02
    provides: "transformAdapter(), the banner count floor, the --check staleness verdict and the six structural pins"
provides:
  - "kit prose that states the delivered truth: scripts/model-tiers.ts's scope paragraph and agent-factory/config/factory.config.md's delivery paragraph, with the assertion over one of them re-aimed in the SAME commit"
  - "install/README.md names the re-run as the delivery path and --update as not being it"
  - "a CHANGELOG.md `### Changed` entry under `## [Unreleased]` for the reachable behaviour change"
  - "five defects found by an independent review plus an adversarial reproduction round: four fixed RED-first with mutation transcripts, one dispositioned in writing"
  - "one open defect recorded rather than invented around (WINDOWS ledger row 110), two pre-existing observations recorded (rows 111, 112)"
  - "the eight-hypothesis bypass log, including every attempt that broke nothing"
affects: [29.2 phase verification, any future edit to the install-time render or the doctor]

actuals:
  tokens: 15300
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Guard the construction, not only the use: every step that builds the render mirror now produces a discriminated result, because a `finally` guarantees cleanup and never a verdict"
    - "A floor is only worth what the route downstream of it does: the banner count floor now asks the router's own predicate, so counting a banner and rewriting it are one question"
    - "Bound the assertion to the finding: a toContain over whole stdout passed against an unfixed build because an unrelated line carried the same path"

key-files:
  created:
    - .planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/deferred-items.md
  modified:
    - scripts/model-tiers.ts
    - scripts/model-tiers.js
    - scripts/model-dial-consistency.test.ts
    - agent-factory/config/factory.config.md
    - install/README.md
    - CHANGELOG.md
    - install/install.ts
    - install/install.js
    - install/install.test.ts
    - .planning/WINDOWS.md
    - .planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-VALIDATION.md

key-decisions:
  - "The re-aimed disclosure case keeps its no-wording-pin posture by asserting the REMEDY the reference must name (the installer entry point and the --target flag) rather than a sentence, with a premise assertion that the entry point names a file this repository really ships."
  - "A second case holds the retired 29.1 wording as an absence, in the file's own existing retired-phrasings idiom, with its residual stated: it is a list of wordings, not a decision procedure over the claim."
  - "The rewritten factory.config.md paragraph names NO configuration-file location - the neighbouring paragraph already owns that fact, and a sibling case counts those spellings."
  - "Four of the five review/red-team findings were fixed RED-first in this plan; the fifth (readRenderedAlias's whole-line model-key scan) is dispositioned WILL NOT FIX because the fix would put a frontmatter grammar inside install/, which this phase's own prohibitions forbid, and the current behaviour fails closed."
  - "The stray-adapter gap in the doctor is recorded as an OPEN DEFECT rather than fixed: deciding which target files the doctor speaks for is a contract question spanning install and uninstall, not a local bug."
  - "The doctor's new relay sentence says `configuration file` where the install arm says `model configuration`, because a structural pin from plan 02 forbids that word on any code line inside the doctor. The sentence was reworded rather than the pin relaxed."

patterns-established:
  - "Record the attempts that broke nothing: an unrecorded failed attempt is indistinguishable from an attempt never made, so the bypass log below carries all eight hypotheses and their sub-probes with outcomes either way."
  - "Assert the harness's own premise before the claim: three cases in this plan carry PREMISE assertions that the fixture is the shape the case claims, and one of them caught a false green in this very session."

requirements-completed: [MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05]

coverage:
  - id: D1
    description: "The 29.1 disclosure that a per-repo models block is inert for an installed target no longer appears in kit prose; both sites state what is true now."
    requirement: MODEL-01
    verification:
      - kind: unit
        ref: "scripts/model-dial-consistency.test.ts#the retired 29.1 disclosure has not survived in the reference"
        status: pass
      - kind: other
        ref: "grep -rn 'INERT FOR AN INSTALLED' scripts/ agent-factory/ install/ CHANGELOG.md"
        status: pass
    human_judgment: false
  - id: D2
    description: "The config field reference names the install re-run as the way a models edit reaches an installed repository, without pinning the note's wording."
    requirement: MODEL-05
    verification:
      - kind: unit
        ref: "scripts/model-dial-consistency.test.ts#the config field reference names the install re-run as the way a models edit reaches an installed repository"
        status: pass
    human_judgment: false
  - id: D3
    description: "install/README.md states that --update is kit-home-only and is not how an edit reaches a repo's adapters, and names both the re-run and the read-only --check preview."
    requirement: MODEL-05
    verification: []
    human_judgment: true
    rationale: "Prose in a document no gate asserts content over. check-banned-claims covers it for its three groups only; that it says the RIGHT thing is a reading judgment. The commands it names were executed against a scratch repository before the sentence was written."
  - id: D4
    description: "CHANGELOG.md carries one `### Changed` bullet under `## [Unreleased]` naming the reachable behaviour change, including the partial-checkout consequence, with no banned and no cost claim."
    verification:
      - kind: other
        ref: "node scripts/check-banned-claims.js"
        status: pass
    human_judgment: true
    rationale: "The banned-claims gate has no cost group, so the no-cost-claim prohibition is held as CONTENT and was checked by reading the diff. A human should confirm the bullet reads true and claims nothing measured."
  - id: D5
    description: "A rendered agent adapter that cannot be materialized is refused rather than copied raw, so no target file names a command the target cannot run and no target is left holding symlinks into a deleted temp mirror."
    requirement: MODEL-03
    verification:
      - kind: integration
        ref: "install/install.test.ts#model delivery: a rendered adapter carrying NO kit slot line installs NOTHING and names the file (D-14)"
        status: pass
    human_judgment: false
  - id: D6
    description: "A mirror the installer cannot build - an unusable temp root, or a checkout whose kit-source trees are absent - is a named finding at exit 3 with every other class complete, never an uncaught throw."
    requirement: MODEL-03
    verification:
      - kind: integration
        ref: "install/install.test.ts#model delivery: a mirror the installer cannot BUILD is a named finding, never a crash (R-5)"
        status: pass
    human_judgment: false
  - id: D7
    description: "A target whose configuration path resolves to something that is not a readable file is a named finding, never a crash and never read as an absent configuration."
    requirement: MODEL-03
    verification:
      - kind: integration
        ref: "install/install.test.ts#model delivery: a target whose configuration path is NOT A READABLE FILE installs NOTHING, names the path, and lets every other class complete (R-5)"
        status: pass
    human_judgment: false
  - id: D8
    description: "The doctor asks both directions of its own set question: a member the render produced that the kit's adapter set does not carry is named as one it produced no verdict for."
    requirement: MODEL-05
    verification:
      - kind: integration
        ref: "install/install.test.ts#doctor: a fresh render producing members the kit's adapter set does not carry states that NO VERDICT was produced for them (D-09)"
        status: pass
    human_judgment: false
  - id: D9
    description: "The doctor's no-verdict relay names the real configuration path on this machine beside the relayed message, which names a temp mirror that no longer exists."
    requirement: MODEL-05
    verification:
      - kind: integration
        ref: "install/install.test.ts#doctor: a REFUSED configuration is relayed with the REAL configuration path, never only the temp mirror's (D-09)"
        status: pass
    human_judgment: false
  - id: D10
    description: "The eight adversarial hypotheses were attempted against the committed .js and every outcome recorded, including the attempts that broke nothing."
    verification: []
    human_judgment: true
    rationale: "The value of a bypass log is whether the attacks were the RIGHT ones, which no test can decide. The log below is written so the next reader can judge that rather than take it on trust."
  - id: D11
    description: "The mirror spawn, the process.execPath launch and the rmSync retry cleanup on Windows."
    verification: []
    human_judgment: true
    rationale: "UNKNOWN - verify. No Windows execution happened in this session. Every Windows position in this phase rests on documented Node semantics, and the CI windows-latest leg is the only place it will be observed. WINDOWS ledger rows 105 through 107 are untouched by this phase and stay open."

duration: 40 min
completed: 2026-09-04
status: complete
---

# Phase 29.2 Plan 03: Current-Truth Prose, the Changelog Entry and the Bypass Log Summary

**The kit stopped saying a refuted thing about the model dial, the change is recorded where a user meets it — and then an independent review plus an eight-hypothesis reproduction round against the committed `install.js` found five defects the green suite did not, four of which are fixed here RED-first with mutation transcripts and one recorded as an open defect rather than invented around.**

## Performance

- **Duration:** 40 min
- **Started:** 2026-09-04T10:07Z (immediately after plan 02's metadata commit `fdf3b8b`)
- **Completed:** 2026-09-04T10:47Z
- **Tasks:** 2
- **Files modified:** 9 source/kit files + 2 planning records, 1 planning file created

## Baseline, re-measured before the first edit

Measured on `fdf3b8b` (waves 1 and 2 landed, nothing from this plan):

```
 Test Files  1 failed | 54 passed (55)
      Tests  1 failed | 2430 passed | 2 skipped (2433)
```

Post-change, same command:

```
 Test Files  1 failed | 54 passed (55)
      Tests  1 failed | 2436 passed | 2 skipped (2439)
```

**The one failing file is the pre-existing `scripts/frontmatter.test.ts`** — the `D-49 false-red control` over the tracked markdown corpus. Re-run alone after this plan's edits: `refusals over 1456 tracked markdown files:` with **exactly one member**, the same `.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION-round4.md` and the same `\[` sequence. It is WINDOWS ledger row 91 and is not owned by this phase. **No markdown this plan wrote added a second refusal**, which was a stated prohibition. The suite gained 6 passing cases.

## Accomplishments

### Task 1 — the disclosures state current truth (`89dd183`)

- **`scripts/model-tiers.ts`'s scope paragraph.** The "moved to phase 29.2" and "INERT FOR AN INSTALLED TARGET" sentences are replaced by a paragraph saying where the resolved answer goes: this module resolves, `scripts/generate-role-adapters.ts` is the one emitter of the `model:` line, and `install/install.ts` renders a target's adapters by spawning that generator against that target's own configuration. It states that delivery happens at install time **and only there**, names the re-run and `--check`, and says plainly that the 29.1 disclosure described 29.1. The live-tree-cardinality half is kept true as written (still adjudicated in `guard_model_assignment`, not here). No cost, speed or measurement claim. `scripts/model-tiers.js` was rebuilt and committed in the same commit.
- **`agent-factory/config/factory.config.md`'s disclosed-limitation paragraph** is replaced by one headed **"How an edit reaches an installed repository"**, in the voice of its neighbours: what is true now, the D-07 remedy (`node install/install.js --target <repo>` from the checkout), the statement that `--update` refreshes the shared kit alone and is not the delivery path, the read-only `--check` preview, and that a refused block leaves the repository's adapters as they were. It names **no configuration-file location** — the adjacent "Where the block is read from" paragraph owns that fact and a sibling case counts those spellings.
- **`scripts/model-dial-consistency.test.ts`** is re-aimed **in the same commit**. Two cases replace one: the reference must name the **remedy** (the installer entry point and the `--target` flag, with a PREMISE that the entry point names a file this repository ships), and the retired 29.1 wordings must not survive, in the file's own existing retired-phrasings idiom with its residual stated.
- **`install/README.md`** gains the D-07 paragraph in the `--update` section, and the exit-code 3 paragraph gains the one cause plans 01 and 02 introduced that its two examples did not cover: a run that could not render at all.
- **`CHANGELOG.md`** gains one `### Changed` bullet under `## [Unreleased]`, naming the per-repo render, the `--check` report, the refusal that leaves adapters alone, and the partial-checkout consequence — a run that cannot render now installs no adapter where it previously installed the kit-shipped ones.

### Task 2 — review, reproduction, and five findings (`8a86649`)

Four fixes, each reproduced against the committed build **before** a line was written, each with a RED transcript and a mutation transcript. Details in the tables below.

## Task Commits

1. **Task 1: the disclosures state current truth, and the changelog records the change** — `89dd183` (docs)
2. **Task 2: close five findings from the phase's review and red-team round** — `8a86649` (fix)

_`.ts` and its committed `.js` are in the same commit in both cases; `npm run check:build-parity` was run against the STAGED tree before each and printed `Build parity: no tracked build output moved when tsc ran.`_

## Step 1 — independent code review

`/code-review high 8c1ec4e..HEAD -- install/install.ts` was run as a forked reviewer with no memory of authoring the code. It read the full phase diff and **reproduced its two most serious items live** against the committed `install.js`. Five findings, all dispositioned:

| # | Severity | Finding | Disposition |
|---|---|---|---|
| R1 | HIGH | `renderAdaptersInMirror` documents a discriminated result that never throws, while `mkdtempSync`, the mirror layout, the twin copies and the kit-source copies all ran outside any arm producing one. The `finally` guarantees cleanup, not a verdict. Reproduced two ways: `TMPDIR` at an absent path, and a checkout whose `agent-factory/roles` is absent — the four-twin pre-check does not watch the two kit-source trees, so exactly the partial checkout it exists for arrives through the door it does not watch. Both died at exit 1 with a raw stack, after the kit and skills classes had written and before the state seed and marker, with no banner at all. | **FIXED** (`8a86649`). Three guarded arms, three conditions, three remedies. RED + two mutation transcripts below. |
| R2 | MEDIUM | `linkOrCopy`'s `src` is now a path inside the temp mirror. Any rendered adapter without the kit slot routes there, and under `--symlink` the target is pointed at a directory the helper deletes on the way out. Reproduced: all 17 installed as symlinks into a removed mirror, every one reported `linked`, `== install complete ==`, exit 0, every file unreadable a moment later. | **FIXED** by the same routing floor as finding F-A below; the `--symlink` arm is pinned as its own assertion. |
| R3 | MEDIUM | The doctor's staleness set is one-directional: `res.value.files` is never compared back, and `docNames.length > 0` is a vacuity floor that sees an empty denominator and not a silently short one. | **FIXED** — independently found by the reproduction round as F-C below. |
| R4 | LOW | The doctor's no-verdict arm relays the generator's message without the sentence naming the real configuration path, so `--check` handed a reader a remedy naming a temp directory already removed. | **FIXED** (`8a86649`). RED + mutation below. |
| R5 | LOW | `readRenderedAlias` filters whole lines on the model key rather than reading a frontmatter block — a second, weaker grammar in a repository that keeps one. Not reachable today (adapters carry only an extracted first sentence in the body; the reviewer verified by injection). | **WILL NOT FIX, dispositioned.** The fix is a frontmatter grammar inside `install/`, which this phase's own prohibitions forbid ("no frontmatter regex or YAML parse decides anything in `install/`"). Current behaviour **fails closed**: it refuses the class and names the file rather than taking the first match. Recorded in `deferred-items.md`. |

The reviewer also recorded what it found **working**: the happy path end to end, the relay not being re-detected as a second announcement (T-29.2-08 holds — `startsWith` on the un-left-trimmed line), the twin list being exactly the generator's import closure, `KIT_BANNER` byte-equal to `PROVENANCE`, and `env: process.env` currently inert because none of the four twins reads an environment variable.

The reviewer flagged that `install/install.test.ts` gained uncommitted lines **during** its session and that it did not write them. That was this executor, working concurrently; no third party touched the tree.

## Step 2 — the bypass log

Every attempt below ran against the **committed `.js`** on scratch mirrors under `/tmp/rt29203`, built from `git archive HEAD`. **Attempts that broke nothing are recorded as such**, because an unrecorded failed attempt is indistinguishable from an attempt never made.

**Harness premise, asserted first (hypothesis 8).** Before any probe: a plain install from the archived checkout exits 0, lays 17 adapters, all `model: inherit`, and reports `no configuration file was found at <target>/.grugops/factory.config.json`. Every patched-generator fixture asserts its own anchor is present (the patcher throws if absent), and every probe below was accompanied by an unpatched control run.

### H1 — the banner recogniser: which lines is it even asked about?

| Probe | Result | Verdict |
|---|---|---|
| **1a** Two banner lines per rendered adapter (`PROVENANCE` doubled at both generator emit sites) | exit **3**, 0 adapters installed, `.claude/agents/grugops-agents-md-scribe.md was rendered carrying 2 recognised provenance banner line(s)` | **No bypass** |
| **1b** Zero recognised banners (`PROVENANCE` reworded so nothing matches) | exit **3**, 0 installed, `…carrying 0 recognised provenance…` | **No bypass** |
| **1c** The slot-carrying skill routed through the writer | `.claude/skills/grugops/SKILL.md` carries **0** banners and installs cleanly; all 17 adapters carry **exactly one** target-banner block; **no file anywhere in the target** names the generator command | **No bypass** — zero is legitimate exactly where the floor is not asked |
| **1d-i** A previously materialized file fed back in: a rendered adapter carrying a prior `grugops:target-banner` block **plus** the generator banner | exit 0, 17 installed, **1** banner block, the stale line gone — stripped, not duplicated | **No bypass** |
| **1d-ii** A rendered adapter carrying **only** a materialized banner block and no generator banner | exit **3**, 0 installed, `…carrying 0 recognised…` | **No bypass** — fails closed |
| **1e** *(own probe)* A rendered adapter carrying exactly **one** banner and **no kit slot line** | **BYPASS.** exit **0**, 17 installed, **0** target-banner blocks, the installed file naming `node scripts/generate-role-adapters.js`, report line `copied(verify)` | **FINDING F-A — fixed** |
| **1e′** The same fixture under `--symlink` (the reviewer's R2) | **BYPASS.** exit 0, 17 `linked` into a deleted mirror | **Closed by the same fix**; re-run after the fix: exit **3**, 0 installed, in both copy and symlink mode |

**What F-A actually was.** The floor is asked at all seventeen positions and counts correctly. Its *guarantee* is that the banner will be **rewritten** — and the rewrite happens only on the materialize route, which is chosen later by `srcCarriesSlot`. Counting a banner and rewriting it were two questions. They are one now: the floor asks the router's own predicate, about the same path the router will use.

### H2 — the twin list: which imports does it enumerate?

| Probe | Result | Verdict |
|---|---|---|
| **2a** A fifth import added to a scratch generator, absent from `GENERATOR_TWINS` — install side | exit **3**, 0 installed, the render's own `Cannot find module '<mirror>/scripts/fifth-module.js'` relayed under `the adapter render did not complete` | **No bypass** |
| **2b** The same fifth import — the freshness harness | exit **1**, `Adapter freshness check FAILED: the generator did not run cleanly — refusing to report the adapters as fresh.` | **No bypass** — both mirrors fail loud |
| **2c** A member **removed** from `install.ts`'s list only | `AssertionError: expected [ 'scripts/frontmatter.js', …(3) ] to deeply equal [ …(3) ]` — the equality case reds **naming the member** | **No bypass** |

### H3 — the resolution probe: what is its input assembled from?

| Probe | Refusal | Verdict |
|---|---|---|
| **3a** Two announcement lines | `the generator's output carried 2 resolved-assignment announcement(s) where exactly one was required` — exit 3, 0 installed | **No bypass** |
| **3b** No announcement line | `…carried 0 resolved-assignment announcement(s)…` — exit 3, 0 installed | **No bypass** |
| **3c** A malformed payload | `the announced resolution was refused by name — model-tiers: the resolved model assignment payload "{not json at all" is not parseable JSON` — exit 3, 0 installed | **No bypass** |

Three conditions, three **distinguishable** refusals, none of them a silent clean resolution. **Recorded honestly:** the plan asked for "three remedies", and only the probe-line-count arm carries an explicit re-run sentence; the other two relay the underlying cause (which for 3c names the offending payload and is actionable). Message quality, not a hole — noted for the verifier rather than widened here.

### H4 — the count assertion: which number does it anchor on?

| Probe | Result | Verdict |
|---|---|---|
| **4a** Seventeen files rendered, sixteen announced | exit **3**, 0 installed, `announced a resolution covering 16 role(s), while this run derived 17 rendered adapter(s)` — **both numbers printed** | **No bypass** |
| **4b** **Both sides empty** (the vacuity question, asked rather than assumed) | The set equality is **never reached**: an empty install set is refused upstream by its own named finding — `was read successfully but holds no adapter … This is a different condition from an unreadable directory` — exit 3, 0 installed | **No bypass; the vacuity is bounded on the install path** |
| **4c** *(own probe)* The **doctor** with an empty kit adapter set | exit **1** — loud, but reported as a kit-root cross-check disagreement reading `adapter=<unset>`, which names the target rather than the unreadable checkout | **Not a bypass** (not silent); the sentence names the wrong root — pre-existing, ledger row 112 |

### H5 — the staleness predicate: at which positions is it even asked?

| Probe | Result | Verdict |
|---|---|---|
| **5a** One adapter removed from an installed target | `1 absent from the target: grugops-ba-pm.md` — WARN, exit 0, promoted under `--strict` | **No bypass** |
| **5b** A **stray** adapter the kit no longer ships | **BYPASS.** `ALL CHECKS PASSED`, exit 0, the stray named **nowhere** | **FINDING F-B — recorded as an OPEN DEFECT, not fixed** |
| **5c** A target whose cross-check already failed | exit 1 on the primary defect, **zero** staleness lines | **No bypass** — the placement is load-bearing and holds |
| **5d** *(own probe)* The checkout's adapter directory **unreadable** (`chmod 000`) | exit **1** — loud, again via the `adapter=<unset>` cross-check FAIL | **Not a bypass**; wrong sentence — ledger row 112 |
| **5e** *(own probe)* The checkout's adapter directory **silently short** — 1 of 17, roles corpus complete | **BYPASS.** `ALL CHECKS PASSED (1 warning(s))`, exit **0**, one file named, **sixteen stale target adapters never asked about** | **FINDING F-C — fixed** |

**Why F-B was recorded and not fixed.** A stray `grugops-*.md` is loaded by Claude Code and also survives uninstall (removal is by derived name over the kit set), so the gap spans two commands. Deciding **which files in a target the doctor speaks for** is a contract question — a D-level decision — and the plan's own rule is to record what is not Rule 1–3 scale rather than invent it mid-execution. Ledger row **110**, with the natural bound (a target `.md` carrying the installer's own banner sentinel) written down so the next author does not re-derive it.

### H6 — the skip-if-identical arm: can it hide a stale target?

| Probe | Result | Verdict |
|---|---|---|
| **6a** Destination adapter unreadable (`chmod 000`) | **Never reports identical.** The read is caught and treated as a write; the *write* then throws `EACCES` uncaught, exit 1 with a stack trace | **No bypass of the invariant asked about**; the crash is **pre-existing** (the write was previously unconditional and threw the same way) — ledger row 111 |
| **6b** Destination adapter is a **directory** | **Never reports identical.** `EISDIR` on the write, uncaught, exit 1 | Same — pre-existing, ledger row 111 |

### H7 — the refusal relay: can the installer's own line be lost?

Under a **pipe** (`2>&1 | cat`), with an illegal `models.preset` in the target:

- the generator's own refusal text reaches stdout, naming the offending value;
- the installer's own line naming the **real** `<target>/.grugops/factory.config.json` reaches stdout;
- the closing banner reads `== install INCOMPLETE — 1 item(s) need verification ==` and the exit code is **3** — banner and code **agree**;
- zero adapters installed; the state seed, the marker and the memory-bank index all still present.

**No bypass.**

### H8 — the harness's own premise

Asserted for every case: the patcher throws when its anchor is absent (so an unpatched generator cannot masquerade as a fixture); the planted configuration file exists at the path the installer reads; pre-state adapters carry what each case asserts; and each new case was run against the **pre-fix** build to confirm it reds.

**And it caught a false green in this session.** The R4 case was first written as a `toContain` over the whole `--check` stdout — and **passed against the unfixed build**, because the doctor's ordinary stat list already prints `ok  <target>/.grugops/factory.config.json` on its own line. The assertion is now **bounded to the finding block** (the `WARN` line plus its continuation lines), with two premises: the generator's refusal really is relayed inside that block, and the block really does carry a temp-mirror path — without both there is no leak to disclaim. Re-run against the unfixed build: RED.

### Own probes outside the eight

| Probe | Result | Verdict |
|---|---|---|
| Target config is a **directory** | **BYPASS.** `copyFileSync` threw uncaught (`ENOTSUP`/`EISDIR`), exit **1**, stack trace, nothing installed, no other class completed — on the install **and** the `--check` path | **FINDING F-D — fixed** |
| Target config is a **symlink** to a real file | exit 0, resolves through it, 4 `opus` adapters, `resolution read from <real path>` | No bypass — the user's own repository, reported by real path |
| Target config is **malformed JSON** | Named refusal, exit 3, 0 installed | No bypass |
| Target config is a **dangling symlink** | `existsSync` is false → treated as absent, and the run **says so** (`no configuration file was found at …`) | No bypass — reported, not silent |
| **D-16** self-checkout refusal, against a real checkout copy carrying a `tiered` block | exit **1**, nothing written, committed adapters byte-identical and still `model: inherit` | **Holds** |
| **D-16** with `--allow-self` | exit 0, the checkout's committed adapters rewritten to `model: sonnet` | **The flagged reach-around is real and now measured rather than asserted.** It is the accepted, documented state (R-4): the CI backstop is `npm run freshness:adapters` (pinned zero-config) plus `guard_model_assignment`. The repository's own `.claude/agents/` was untouched by every probe (`git status --porcelain` empty throughout). |

## RED transcripts (this plan's new cases)

Each landed against the **pre-fix** build and its exact failure text recorded before implementation.

| Case | Failure text |
|---|---|
| a rendered adapter carrying NO kit slot line | `AssertionError: expected 'status=0' to be 'status=3'` |
| a target whose configuration path is NOT A READABLE FILE | `AssertionError: expected 'status=1' to be 'status=3'` |
| a fresh render producing members the kit's adapter set does not carry | `AssertionError: expected '== grugops doctor (--check) ==\nhome:…' to contain 'NO VERDICT on adapter staleness'` |
| a mirror the installer cannot BUILD | `AssertionError: expected 'tmpdir: status=1' to be 'tmpdir: status=3'` |
| a REFUSED configuration relayed with the REAL configuration path | first written unbounded and **GREEN for the wrong reason** (recorded under H8); bounded to the finding block, then `AssertionError: expected '  WARN           adapter staleness: N…' to contain '/var/folders/…'` |

The `--symlink` arm of the first case is a **reviewer-driven** assertion added after the fix, not a RED-first one, and is recorded as such.

## Mutation transcripts

Each applied to the committed `.js` in a scratch copy (`/tmp/install.js.good` restored immediately after), the guarding case run alone.

| Mutation | Result |
|---|---|
| **F-A** the routing floor made unreachable (`if (false)`) | `FAIL … NO kit slot line` — `expected 'status=0' to be 'status=3'` |
| **F-C** the reverse-direction set arm made unreachable | `FAIL … NO VERDICT` — the doctor stops producing the line entirely |
| **F-D** the config-copy guard **rethrows** (pre-fix) | `expected 'status=1' to be 'status=3'` — the crash direction |
| **F-D** the config-copy guard **swallows** | `expected 'status=0' to be 'status=3'` — the silent-wrong direction: 17 adapters installed carrying a resolution the file was never asked about. **Recorded because the two directions fail differently and only both together bound the arm.** |
| **R1-arm1** the `mkdtemp` guard rethrows | `expected 'tmpdir: status=1' to be 'tmpdir: status=3'` |
| **R1-arm2** the mirror-construction guard rethrows | `expected 'roles: status=1' to be 'roles: status=3'` — each arm reds on **its own** condition, so the two are not one assertion wearing two names |
| **R4** the real-path sentence removed | `expected '  WARN           adapter staleness: N…' to contain '/var/folders/…'` |

## Step 3 — the CI gate block, run verbatim

Every command from `.github/workflows/ci.yml`, in its own order:

| Command | Result |
|---|---|
| `npm run freshness` (pre-build) | `All build outputs fresh: 50 committed .js file(s) match a rebuild of their sources.` — exit 0 |
| `npm run check:build-parity` | `Build parity: no tracked build output moved when tsc ran.` — exit 0 |
| `npm run typecheck` | exit 0 (both targets) |
| `npx vitest run --exclude '**/scripts/e2e/**'` | `1 failed \| 54 passed (55)` files; `1 failed \| 2436 passed \| 2 skipped (2439)` — the measured baseline, same single refusal |
| `npm run freshness:catalog` | `Catalog fresh: docs/catalog/README.md matches a fresh regeneration.` — exit 0 |
| `npm run freshness:context` | `Context fresh: no .grugops/context/ tree exists yet` — exit 0 |
| `npm run freshness:adapters` | `Adapters fresh: 17 adapter(s) compared … 0 byte difference(s)` / `Mirrored generator resolved model preset: none` — exit 0 |
| `npm run freshness:skill-twins` | `Skill twins fresh: 7 twin(s) compared … 0 byte difference(s)` — exit 0 |
| `npm run generate:adapters` | `resolved model assignment: {"roles":17,"overrides":0,"aliases":["inherit"]}` — exit 0 |
| `git status --porcelain -- .claude/agents/` + `test -z` | empty — exit 0 |
| `node scripts/check-foundation-guards.js` | `ALL CHECKS PASSED` — exit 0 |
| `node scripts/check-kit-refs.js` | `ALL CHECKS PASSED` — exit 0 |
| `node scripts/check-public-docs-vocabulary.js` | `ALL CHECKS PASSED` — exit 0 |
| `node scripts/check-audit-register.js` | `ALL CHECKS PASSED` — exit 0 |
| `node scripts/check-claim-anchors.js` | `ALL CHECKS PASSED` — exit 0 |
| `node scripts/check-banned-claims.js` | `ALL CHECKS PASSED` — exit 0 |
| `node scripts/check-imperative-lexicon.js` | `ALL CHECKS PASSED` — exit 0 |
| `node scripts/check-diff-disposition.js` | `ALL CHECKS PASSED` — exit 0 |
| `node scripts/check-nul-bytes.js` | `ALL CHECKS PASSED` — exit 0 |
| `VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js` | `ALL CHECKS PASSED` — exit 0 |

`npm run freshness` was re-run **after** each commit (its subject is HEAD, not the working tree) and passed both times.

## Step 4 — the Windows position

**`UNKNOWN - verify`.** No Windows execution happened in this session or in either earlier plan of this phase. Everything this phase reasons about Windows — the `process.execPath` launcher path, the absence of a `shell` option, `os.tmpdir()`, `join()` path separators, and the `rmSync(…, {maxRetries: 3})` cleanup — rests on **documented Node semantics**, not on observation. The `windows-latest` CI matrix leg runs `npm run typecheck`, `npm run build` and the vitest suite, so `install/install.test.ts` — including every case this plan added — will execute there, and that leg is **the only place** the mirror's Windows behaviour will actually be observed. Nothing in this phase ships a claim about Windows behaviour beyond that.

**WINDOWS ledger rows 105 through 107 are untouched by this phase and stay open.** Row 107 sits on `scripts/model-tiers.ts`, a module this phase **spawns** rather than edits, and this phase makes no claim about it. Rows 105 and 106 are likewise unaffected. The rows this plan **appended** are 110 (the open stray-adapter defect), 111 and 112 (two pre-existing observations) — appending is not rewriting, and rows 105 to 107 appear nowhere in the diff.

## Files Created/Modified

- `scripts/model-tiers.ts` / `.js` — the rewritten scope paragraph.
- `agent-factory/config/factory.config.md` — the delivery paragraph replacing the disclosed limitation.
- `scripts/model-dial-consistency.test.ts` — two re-aimed cases plus the remedy constants and the retired-wordings list.
- `install/README.md` — the D-07 paragraph and the extended exit-code 3 causes.
- `CHANGELOG.md` — one `### Changed` bullet.
- `install/install.ts` / `.js` — the mkdtemp guard, the mirror-construction guard, the target-config copy guard, the routing floor, the doctor's reverse-direction set arm, and the doctor's real-path sentence.
- `install/install.test.ts` — five new cases (four RED-first, one reviewer-driven arm).
- `.planning/WINDOWS.md` — rows 110, 111, 112 appended.
- `.planning/phases/…/29.2-VALIDATION.md` — the per-task map filled from the commands that ran; `status:` untouched.
- `.planning/phases/…/deferred-items.md` — created.

## Decisions Made

See `key-decisions` in the frontmatter. The one worth restating: **the doctor's new sentence says "configuration file" where the install arm says "model configuration"**, because a structural pin from plan 02 asserts that no code line inside `doctor()` mentions that word — the cheapest way to keep a frontmatter grammar out of the doctor is to keep the word out of it, and the pin cannot distinguish a string literal from a parser. The pin caught the sentence on the way in. **The sentence was reworded; the pin was not relaxed.**

## Deviations from Plan

### 1. [Rule 2 — Missing critical] Four defects fixed that the plan did not enumerate

- **Found during:** Task 2, steps 1 and 2 — which is exactly what those steps are for.
- **Issue:** F-A/R2 (a rendered adapter without the kit slot copied raw, and symlinked into a deleted mirror), R1 (the render helper throwing where it promised a result), F-D (a target config that is not a readable file crashing the run), F-C/R3 (the doctor's one-directional set), R4 (a remedy naming a removed directory).
- **Fix:** all five closed in `8a86649`, each RED-first with a mutation transcript.
- **Verification:** the tables above; full suite at baseline; every gate exit 0.

### 2. [Rule 4 — deferred, not decided] The stray-adapter gap was recorded, not fixed

- **Found during:** Task 2, hypothesis 5b — a probe the plan itself asked for and expected to pass.
- **Issue:** the doctor never names a target adapter the kit no longer ships.
- **Action:** recorded as an open defect (ledger row 110, `deferred-items.md`) rather than fixed, because the bound — which files in a target the doctor speaks for — is a contract decision spanning install and uninstall. Stated plainly so a verifier meets it rather than discovers it.

### 3. [Scope boundary] Two pre-existing issues logged, not fixed

`materializeAdapter`'s write half throwing on an unwritable or directory destination (row 111), and the doctor's `adapter=<unset>` sentence naming the wrong root when the checkout's adapter set cannot be derived (row 112). Neither is caused by this phase's changes; both are in `deferred-items.md`.

---

**Total deviations:** 3 (1 missing-critical covering five findings, 1 deferred contract decision, 1 scope-boundary log).
**Impact on plan:** no scope creep — every fix lives in the two files the plan names for task 2. Every plan prohibition holds: no cost, cheaper, saving or savings claim appears in any file this plan wrote (`grep -nEi` returns nothing); no conformance, token-economy or comprehension claim (gate green); no claim that ledger rows 105–107 are closed; no verifier record, ROADMAP record, CONTEXT file or existing ledger row rewritten; the re-aimed test pins a remedy and not the note's wording; the frozen MODEL-07 registry row is byte-unchanged (`git diff --stat -- docs/audit/` empty); and no markdown this plan wrote put a backslash sequence inside a double-quoted YAML scalar.

## Known Stubs

None. No hardcoded empty value, placeholder string, TODO or FIXME was introduced.

## Threat Flags

None beyond the phase's register. Two register entries were **strengthened by measurement rather than by assertion**: T-29.2-19 (a green suite standing in for proof) is discharged by the log above rather than claimed, and T-29.2-18 (an unmeasured cost claim) was checked by reading the diff, because the banned-claims gate has no cost group and the prohibition is on the author.

## Issues Encountered

- **A `toContain` over whole stdout passed against an unfixed build.** Recorded under H8 and fixed by bounding the assertion to the finding block. This is the sixth-plus instance in this project's record of a verification harness producing a false result; it was caught only because the case was run against the pre-fix build before being trusted.
- **Two mutation attempts were themselves invalid** — brace-unbalanced replacements that made `install.js` a syntax error, so every case failed for a reason unrelated to the mutation. Recorded because a mutation transcript is only evidence if the mutant still parses; both were redone with exact anchors and each then reds on its own arm.
- **`npm run freshness` passes trivially before a commit.** Its subject is HEAD, so running it against a staged-but-uncommitted tree compares old against old. It was re-run after each commit.

## Self-Check: PASSED

- `scripts/model-tiers.ts` — FOUND
- `scripts/model-tiers.js` — FOUND
- `agent-factory/config/factory.config.md` — FOUND
- `scripts/model-dial-consistency.test.ts` — FOUND
- `install/README.md` — FOUND
- `CHANGELOG.md` — FOUND
- `install/install.ts` — FOUND
- `install/install.js` — FOUND
- `install/install.test.ts` — FOUND
- `.planning/phases/…/deferred-items.md` — FOUND
- commit `89dd183` — FOUND
- commit `8a86649` — FOUND

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

Phase 29.2's three plans are complete and the phase is ready for `/gsd-verify-work`.

**What the verifier should look at first:**

1. **Ledger row 110 is an open defect this phase chose not to close.** It is a contract question, and the verifier — not the executor — is the right place for it to be answered or accepted open.
2. **Windows is `UNKNOWN - verify` for the whole phase.** No claim about Windows behaviour ships; the `windows-latest` CI leg is the observation point.
3. **The bypass log is a floor, not a closure.** Eight hypotheses with sub-probes, plus one independent review, found five defects in a surface whose suite was green and whose two prior plans had already recorded ten mutation transcripts between them. That ratio is the argument for a second round, not against one. The two predicates the earlier plans nominated for red-teaming both turned out to have holes — and neither hole was in the predicate itself, but in what the predicate's answer was allowed to reach.

---
*Phase: 29.2-model-assignment-delivery-path*
*Completed: 2026-09-04*

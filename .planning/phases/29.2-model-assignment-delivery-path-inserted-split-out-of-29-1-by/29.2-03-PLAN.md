---
phase: 29.2-model-assignment-delivery-path
plan: 03
type: execute
wave: 3
depends_on: ["29.2-01", "29.2-02"]
files_modified:
  - scripts/model-tiers.ts
  - scripts/model-tiers.js
  - scripts/model-dial-consistency.test.ts
  - agent-factory/config/factory.config.md
  - install/README.md
  - CHANGELOG.md
  - install/install.ts
  - install/install.js
  - install/install.test.ts
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-15 - the 29.1 disclosure that a per-repo models block is inert for an installed target no longer appears in kit prose; `scripts/model-tiers.ts`'s scope paragraph and `agent-factory/config/factory.config.md`'s disclosed-limitation paragraph both state what is true now, and `scripts/model-dial-consistency.test.ts` is re-aimed at the delivered truth in the SAME commit so no run is ever red on a stale assertion."
    - "D-07 - `--update` stays kit-home-only and gains no target-adapter refresh; `install/README.md` states in clear voice that a models edit, or a kit-side adapter change, reaches an installed repository by re-running install from the checkout against that repository."
    - "A `### Changed` entry under `## [Unreleased]` in `CHANGELOG.md` records the reachable behaviour change, in clear voice, carrying no conformance, token-economy, comprehension or cost claim."
    - "Annotate-never-rewrite is honoured where it applies: the ROADMAP phase records, the 29.1 CONTEXT and VERIFICATION files and the WINDOWS ledger rows are annotated rather than rewritten, while kit prose is rewritten to current truth."
    - "This phase makes no claim about WINDOWS ledger rows 105 through 107; row 107 sits on a module this phase spawns rather than edits, and it stays open."
    - "An independent review and an adversarial self-reproduction pass ran over the phase's changed source before the phase was called complete, and every attempted bypass is recorded with its outcome - including the ones that failed to break anything."
  artifacts:
    - "scripts/model-tiers.ts and scripts/model-tiers.js - the rewritten scope paragraph, built and committed together"
    - "agent-factory/config/factory.config.md - the rewritten disclosed-limitation paragraph naming the re-run remedy"
    - "scripts/model-dial-consistency.test.ts - the re-aimed disclosure assertion, in the same commit"
    - "install/README.md - the D-07 sentence in the update section"
    - "CHANGELOG.md - one `### Changed` bullet under `## [Unreleased]`"
    - ".planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-03-SUMMARY.md - the bypass log for this phase"
  key_links:
    - "agent-factory/config/factory.config.md's disclosure paragraph -> scripts/model-dial-consistency.test.ts's assertion over that same file (must move together in one commit)"
    - "scripts/model-tiers.ts -> scripts/model-tiers.js (the freshness gate reads the committed side at HEAD)"
    - "install/README.md's update section -> the D-07 contract that --update writes into no target"
  prohibitions:
    - "No cost, limit-savings, cheaper-tier or spend claim is written in any file this plan touches. The banned-claims gate has no cost group, so this is held as content."
    - "No conformance, token-economy or comprehension claim is written into CHANGELOG.md, install/README.md or agent-factory/config/factory.config.md."
    - "No claim anywhere that WINDOWS ledger rows 105, 106 or 107 are closed by this phase."
    - "No verifier record, ROADMAP phase record, CONTEXT file or WINDOWS ledger row is rewritten; those are annotated."
    - "The wording of the disclosure is not pinned by the re-aimed test; that file must not become a second authority over the note's prose."
    - "The frozen MODEL-07 registry row in docs/audit is not edited."
    - "No markdown this plan writes places a backslash sequence inside a double-quoted YAML scalar."
---

<objective>
Make the kit stop saying something that is no longer true, record the change where a user will meet
it, and then try to break what plans 01 and 02 built before anyone calls this phase done.

`scripts/model-tiers.ts`'s scope paragraph and `agent-factory/config/factory.config.md`'s disclosed
limitation both currently tell a reader that a per-repo `models` block does not reach an installed
repository. That is now false, and a stale disclosure is a false claim in the kit. Both are
rewritten, the test that asserts the old disclosure is re-aimed in the same commit, `install/README.md`
gains the D-07 remedy sentence, and `CHANGELOG.md` gains the `### Changed` entry this reachable
behaviour change owes.

Then the phase closes the way this project's own history says a safety-adjacent invariant has to
close: an independent review plus an adversarial reproduction attempt, not a green suite.

**No requirement is new or re-opened.**

Purpose: the trace is the proof, and a refuted sentence left standing in the kit is the opposite of a
proof.
Output: current-truth prose, a changelog entry, and a recorded bypass log.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-CONTEXT.md
@.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-RESEARCH.md
@.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-01-SUMMARY.md
@.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The disclosures state current truth, and the changelog records the change</name>

  <files>scripts/model-tiers.ts, scripts/model-tiers.js, scripts/model-dial-consistency.test.ts, agent-factory/config/factory.config.md, install/README.md, CHANGELOG.md</files>

  <read_first>
    - `scripts/model-tiers.ts` lines 40-120 - the whole header, including the ratification note, the scope paragraph that names the moved delivery path and the inert-for-an-installed-target sentences, the deliberately-not-imported note, and the clear-voice instruction
    - `agent-factory/config/factory.config.md` lines 118-142 - the models sub-fields section, the precedence, refusals, what-is-closed, host-CLI-scope and where-the-block-is-read-from paragraphs whose voice the rewrite matches, and the disclosed-limitation paragraph itself
    - `scripts/model-dial-consistency.test.ts` lines 40-60 (the surface role map, including which file the pointer role names) and lines 1345-1364 (the disclosure describe block and its deliberate no-wording-pin comment)
    - `install/README.md` lines 60-80 (the exit-code table) and lines 276-303 (the update section, kit-home-only)
    - `CHANGELOG.md` lines 1-45 - the Keep a Changelog header, the pre-1.0 note, and the existing Unreleased Added and Changed bullets whose voice the new bullet matches
    - `.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-RESEARCH.md` section 8 (which gate scans which of these files) and section 9 (every disclosure site, split into rewrite, add and annotate-only)
    - `.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-PATTERNS.md` the D-15 prose sites section
  </read_first>

  <action>
Rewrite four prose sites and re-aim one test. **All of it in a single commit** - the test currently
asserts the presence of the old disclosure, so splitting the commit leaves the tree red between them.

**`scripts/model-tiers.ts`, the scope paragraph.** Rewrite the sentences that say the delivery path
moved to a later phase and that a per-repo block is inert for an installed target. State what is now
true: the module resolves the block, the adapter generator emits it, and the installer renders a
target's adapters from that target's own configuration when install is run against it from the
checkout. Keep the other half of that paragraph true as written - the live-tree cardinality is still
adjudicated in the foundation guard and not here. Do not add a cost claim, a speed claim or a
measurement (D-15). Rebuild and commit `scripts/model-tiers.js` in the same commit; the freshness
gate reads the committed side at HEAD and a build step cannot repair a missing one.

**`agent-factory/config/factory.config.md`, the disclosed-limitation paragraph.** Replace it with a
paragraph that states the delivered behaviour and, in the same breath, the D-07 remedy: a models edit
reaches an installed repository by re-running install from the checkout against that repository,
because the update mode refreshes only the shared kit and writes into no target. Match the voice of
its neighbours in that section - clear professional voice, a bolded lead phrase, the consequence
stated plainly, one authority per fact. Do not restate the host-CLI scope, which that section
deliberately delegates. Do not add or remove a spelling of either configuration-file location: a
sibling case in `scripts/model-dial-consistency.test.ts` counts those spellings and pins their
cardinalities.

**`scripts/model-dial-consistency.test.ts`, the disclosure describe block.** Re-aim it at the
delivered truth in the same commit. It must keep its deliberate posture - the WORDING is not pinned,
because pinning it would make the test a second authority over the note's prose. Assert instead that
the reference names the re-run remedy as the way a models edit reaches an installed repository.
Rewrite the block's own comment so it explains the new assertion rather than the old one.

**`install/README.md`.** In the update section, add the D-07 sentence: the update mode stays
kit-home-only and writes no adapters into any repository; a models edit, or a kit-side adapter
change, reaches an installed repository by re-running install from the checkout against that
repository. Keep the file's existing voice. Check the exit-code table still reads true after plans 01
and 02 - the incomplete code now also covers a refused models block and a render that could not run -
and extend that row's examples only if it would otherwise be incomplete.

**`CHANGELOG.md`.** Add one bullet to the existing `### Changed` list under `## [Unreleased]`,
following Keep a Changelog 1.1.0. One or two sentences, clear voice, naming what changed for a user:
installing a repository now renders that repository's Claude Code sub-agent adapters from that
repository's own configuration, the doctor reports when an edit has not been re-installed yet, and a
repository whose configuration is refused gets no adapter rather than a silent default. Name the
behaviour change plans 01 and 02 introduced for a partial checkout as well, since a run that cannot
render now installs no adapter where it previously installed the kit-shipped ones. No conformance,
token-economy or comprehension claim - that file is inside the banned-claims scan. No cost or
savings claim of any kind: the banned-claims gate has no cost group, so this one is held as content
and the prohibition is on the author, not on the gate.

**Annotate, do not rewrite**, anywhere outside kit prose: the ROADMAP phase records, the 29.1
CONTEXT and VERIFICATION files and the WINDOWS ledger rows are records of what was believed at the
time. This phase makes no claim about ledger rows 105 through 107 and does not touch them; row 107
sits on a module this phase spawns rather than edits.

Do not edit the frozen claim-registry row in the audit directory.
  </action>

  <acceptance_criteria>
    - `grep -rn "INERT FOR AN INSTALLED" scripts/` prints nothing, and `grep -c "Disclosed limitation" agent-factory/config/factory.config.md` prints 0.
    - `git log -1 --name-only` for this task's commit lists `scripts/model-tiers.ts`, `scripts/model-tiers.js`, `scripts/model-dial-consistency.test.ts`, `agent-factory/config/factory.config.md`, `install/README.md` and `CHANGELOG.md` together.
    - `npx vitest run scripts/model-dial-consistency.test.ts` exits 0, and the re-aimed block's comment no longer describes the old assertion.
    - `CHANGELOG.md` gains exactly one new bullet under the Unreleased `### Changed` heading.
    - `node scripts/check-banned-claims.js`, `node scripts/check-claim-anchors.js`, `node scripts/check-public-docs-vocabulary.js`, `node scripts/check-imperative-lexicon.js`, `node scripts/check-diff-disposition.js` and `node scripts/check-kit-refs.js` each print their all-checks-passed line and exit 0.
    - `git diff --stat -- .planning/ROADMAP.md .planning/WINDOWS.md docs/audit/` is empty for this task.
    - No file this task wrote contains the words cost, cheaper, saving or savings in a claim about the model dial.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run scripts/model-dial-consistency.test.ts</automated>
    <fails_when>The reporter prints a nonzero `Tests  N failed` line - in particular the disclosure block failing on a string the rewritten prose no longer contains.</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>tsc emits a TS error code, check:build-parity prints `BUILD PARITY FAILED`, or the freshness gate names `scripts/model-tiers.js` as not matching a rebuild of its `.ts` at HEAD.</fails_when>
    <automated>node scripts/check-banned-claims.js &amp;&amp; node scripts/check-claim-anchors.js &amp;&amp; node scripts/check-public-docs-vocabulary.js &amp;&amp; node scripts/check-imperative-lexicon.js &amp;&amp; node scripts/check-diff-disposition.js &amp;&amp; node scripts/check-kit-refs.js &amp;&amp; node scripts/check-audit-register.js &amp;&amp; node scripts/check-nul-bytes.js</automated>
    <fails_when>Any of the eight prints a findings count above 0 or a `CHECK(S) FAILED` line.</fails_when>
    <automated>grep -rn "INERT FOR AN INSTALLED" scripts/ agent-factory/ install/ CHANGELOG.md</automated>
    <fails_when>The grep prints any line, meaning a refuted sentence is still standing in kit prose.</fails_when>
  </verify>

  <done>A reader of `agent-factory/config/factory.config.md` learns that a models block reaches an installed repository and how to make it do so; a reader of `scripts/model-tiers.ts`'s header learns the same; `install/README.md` names the re-run as the delivery path; `CHANGELOG.md` records the change; and the test that used to assert the old disclosure asserts the new truth, in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: Independent review and adversarial reproduction before the phase is called complete</name>

  <files>install/install.ts, install/install.js, install/install.test.ts</files>

  <read_first>
    - `.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-01-SUMMARY.md` and `29.2-02-SUMMARY.md` - every RED transcript and every mutation transcript recorded by the two earlier plans
    - `install/install.ts` in full, as changed by plans 01 and 02
    - `install/install.test.ts` - the new case group, the doctor cases and the six structural pins
    - `.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-RESEARCH.md` Common Pitfalls 1 through 8 and the RED-first / mutation discipline section
    - `.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-VALIDATION.md` - the per-task verification map to tick off and the manual-only row
    - `.github/workflows/ci.yml` - the gate block this task runs verbatim, and the windows-latest matrix leg
  </read_first>

  <action>
Close the phase the way this project's own record says a safety-adjacent invariant has to close. A
green suite is a floor, not a proof.

**Step 1 - independent code review.** Run `/gsd-code-review` (or an equivalent reviewer with no
memory of authoring the code) over every source file plans 01 and 02 changed. Treat every finding as
open until it is either fixed or dispositioned in writing with a reason. Fixes land in this task's
files and are rebuilt and committed with their `.js`.

**Step 2 - adversarial self-reproduction.** Attempt each hypothesis below against the COMMITTED
`.js`, on a scratch mirror, and record the outcome of every one - including the attempts that broke
nothing, because an unrecorded failed attempt is indistinguishable from an attempt never made. Each
hypothesis asks what BOUNDS a predicate's input rather than what it decides:
  1. **The banner recogniser - which lines is it even asked about?** Plant a rendered adapter
     carrying two banner lines, and one carrying none. Route the slot-carrying skill through the
     writer. Feed a previously materialized target file back in as a source. Confirm the exactly-one
     floor fires by name where it must and stays silent where zero is legitimate.
  2. **The twin list - which imports does it enumerate?** Add a fifth import to a scratch copy of the
     generator and confirm BOTH the install-side mirror and the freshness harness fail loud, and that
     the equality case reds. Then remove a member from one list only and confirm the equality case
     reds naming the member.
  3. **The resolution probe - what is its input assembled from?** Feed it a stdout carrying two
     announcement lines, one carrying none, and one carrying a malformed payload. Confirm three
     distinguishable refusals with three remedies, and that none of them silently reports a clean
     resolution.
  4. **The count assertion - which number does it anchor on?** Force a render that writes seventeen
     files while announcing a different member count, and confirm the run installs nothing and prints
     both numbers. Then force BOTH sides empty and confirm the set equality is not vacuously
     satisfied - a floor that catches an empty result never catches a silently short one.
  5. **The staleness predicate - at which positions is it even asked?** Remove one adapter from an
     installed target; add a stray adapter the kit no longer ships; run the doctor on a target whose
     cross-check already failed. Confirm the first two are named and the third produces no staleness
     line at all.
  6. **The skip-if-identical arm - can it hide a stale target?** Make the destination unreadable, and
     separately make it a directory, and confirm neither reports identical.
  7. **The refusal relay - can the installer's own line be lost?** Refuse a config and confirm both
     the generator's text and the installer's real-path line reach stdout under a pipe, not only a
     terminal, and that the run's exit code and its printed banner agree.
  8. **The harness's own premise.** For each new case, confirm the fixture is the shape the case
     claims: the planted configuration file exists at the path the installer reads, the pre-state
     adapters carry what the case asserts, and the case reds when the fixture is removed. Six false
     verification results across four rounds in the prior phase came from a harness whose premise was
     never checked.

**Step 3 - the phase gate.** Run the CI gate block verbatim, in its own order. Every command must
exit 0 against the measured baseline, not against an assumption of green: the suite's exit criterion
is one failed file - the pre-existing double-quoted-scalar refusal in the frontmatter suite - or
better, and every markdown this phase wrote must keep backslash sequences out of double-quoted YAML
scalars so it does not add a second refusal to that same case.

**Step 4 - record.** Write the bypass log into the plan summary: every hypothesis, what was tried,
what happened, and the disposition. Record the Windows position honestly as `UNKNOWN - verify`: no
Windows execution happened locally, the reasoning about the launcher path, the shell option, the
temp directory and the cleanup retries rests on documented Node semantics, and the only place the
mirror's Windows behaviour will actually be observed is the CI windows-latest leg. State plainly
that WINDOWS ledger rows 105 through 107 are untouched and stay open.

**Step 5 - the validation map.** Tick the per-task rows in the phase VALIDATION document against the
commands that actually ran, and leave its status field alone; a later step owns that transition.
  </action>

  <acceptance_criteria>
    - Every code-review finding is either fixed in a commit or carries a written disposition naming why it is not.
    - All eight adversarial hypotheses are recorded in the plan summary with a stated outcome, including the ones that broke nothing.
    - Hypotheses 1, 3, 4 and 5 each produced at least one transcript showing the named refusal firing with the file, the member or both numbers in the message.
    - Hypothesis 4's both-sides-empty probe is recorded with its result, so the vacuity question is answered rather than assumed.
    - The full CI gate block ran and every command exited 0, with the suite result quoted verbatim in the summary and compared to the re-measured pre-change baseline.
    - `git status --porcelain -- .claude/agents/` is empty.
    - The summary carries an explicit `UNKNOWN - verify` for Windows spawn, path and cleanup behaviour, and an explicit statement that WINDOWS ledger rows 105 through 107 are not closed by this phase.
    - The phase VALIDATION document's per-task map rows are filled from the commands that ran, and its status field is unchanged.
  </acceptance_criteria>

  <verify>
    <automated>npm run freshness &amp;&amp; npm run freshness:catalog &amp;&amp; npm run freshness:context &amp;&amp; npm run freshness:adapters &amp;&amp; npm run freshness:skill-twins &amp;&amp; node scripts/check-foundation-guards.js &amp;&amp; node scripts/check-kit-refs.js &amp;&amp; node scripts/check-public-docs-vocabulary.js &amp;&amp; node scripts/check-audit-register.js &amp;&amp; node scripts/check-claim-anchors.js &amp;&amp; node scripts/check-banned-claims.js &amp;&amp; node scripts/check-imperative-lexicon.js &amp;&amp; node scripts/check-diff-disposition.js &amp;&amp; node scripts/check-nul-bytes.js &amp;&amp; npm run check:build-parity &amp;&amp; npm run typecheck</automated>
    <fails_when>Any command in the chain exits nonzero, or any gate prints a findings count above 0 or a `CHECK(S) FAILED` line.</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>More than one test file fails, or the pre-existing scripts/frontmatter.test.ts failure gains a second refusal entry beyond the one recorded in the pre-change baseline.</fails_when>
    <automated>npm run generate:adapters &amp;&amp; test -z "$(git status --porcelain -- .claude/agents/)"</automated>
    <fails_when>The test command exits 1 because regenerating moved one of the seventeen committed kit adapters.</fails_when>
    <human-check>On a Windows machine, run install against a repository carrying a models block and confirm seventeen adapters render and that a following check run reports no stale file. Recorded as `UNKNOWN - verify` until the CI windows-latest leg or a real Windows run reports it; no claim about Windows behaviour ships without it.</human-check>
  </verify>

  <done>Every new invariant in this phase has been attacked deliberately and the attempt recorded, every review finding is fixed or dispositioned in writing, the whole CI gate block exits 0 against the measured baseline, and the phase's Windows position and the three open ledger rows are stated rather than implied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kit prose to reader | shipped documentation is what an installing user believes; a stale sentence is a false claim delivered to every target |
| phase output to the record | the summary is the audit trail; an unrecorded failed attempt is indistinguishable from an attempt never made |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.2-17 | Repudiation | a refuted disclosure left standing in shipped prose | high | mitigate | The two disclosure sites are rewritten to current truth and the assertion over one of them is re-aimed in the same commit, so no run is red on a stale claim and no reader meets a refuted sentence |
| T-29.2-18 | Repudiation | an unmeasured cost or savings claim entering the kit | high | mitigate | Held as content: no gate has a cost group, so the prohibition is stated in this plan's own prohibitions and checked by reading the diff, with the banned-claims gate covering the three groups it does own |
| T-29.2-19 | Repudiation | a green suite standing in for proof of a safety invariant | high | mitigate | Eight named adversarial hypotheses, each recorded with its outcome, plus an independent review, before the phase is called complete |
| T-29.2-20 | Tampering | a `.ts` shipping without its committed `.js` | medium | mitigate | The freshness gate reads the committed side at HEAD; the task's verify chain runs it and the build-parity check together |
| T-29.2-21 | Information Disclosure | a planning markdown adding a second refusal to the pre-existing frontmatter failure | low | mitigate | Stated as a prohibition: no backslash sequence inside a double-quoted YAML scalar in any markdown this phase writes; the suite comparison is against the measured baseline, not against green |
| T-29.2-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan - it adds no package; `package.json` is byte-unchanged and RESEARCH's Package Legitimacy Audit records no `[ASSUMED]`, `[SUS]` or `[SLOP]` entry |
</threat_model>

<artifacts>
## Artifacts this phase produces (plan 03)

**Prose changes**
- `scripts/model-tiers.ts` scope paragraph - rewritten to current truth, with its committed `.js`
- `agent-factory/config/factory.config.md` disclosed-limitation paragraph - rewritten, naming the re-run remedy
- `install/README.md` update section - the D-07 sentence
- `CHANGELOG.md` - one `### Changed` bullet under `## [Unreleased]`

**Test change**
- `scripts/model-dial-consistency.test.ts` - the disclosure describe block re-aimed at the delivered truth, keeping its deliberate no-wording-pin posture

**Records**
- `29.2-03-SUMMARY.md` - the eight-hypothesis bypass log, the review findings and their dispositions, the verbatim gate results, the `UNKNOWN - verify` Windows position, and the statement that ledger rows 105 through 107 stay open
- `29.2-VALIDATION.md` - per-task map rows filled from the commands that actually ran
</artifacts>

<verification>
- Every command in the CI gate block exits 0.
- `npx vitest run --exclude '**/scripts/e2e/**'` matches the re-measured pre-change baseline or improves on it.
- `grep -rn "INERT FOR AN INSTALLED" scripts/ agent-factory/ install/ CHANGELOG.md` prints nothing.
- `git status --porcelain -- .claude/agents/` is empty.
- The eight adversarial hypotheses are all recorded with outcomes.
</verification>

<success_criteria>
Nothing the kit says about the model dial is false any more, the change is recorded where a user
meets it, and the phase's new invariants have been attacked on purpose and survived - with the
attacks written down so the next reader can judge whether they were the right ones.
</success_criteria>

<output>
Create `.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-03-SUMMARY.md` when done.
</output>

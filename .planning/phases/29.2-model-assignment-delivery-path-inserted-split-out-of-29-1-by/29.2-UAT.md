---
status: complete
phase: 29.2-model-assignment-delivery-path
source: [29.2-01-SUMMARY.md, 29.2-02-SUMMARY.md, 29.2-03-SUMMARY.md, 29.2-04-SUMMARY.md, 29.2-05-SUMMARY.md, 29.2-06-SUMMARY.md]
started: 2026-09-07T06:37:12Z
updated: 2026-09-07T07:19:57Z
restarted_from: 29.2-UAT.md session of 2026-09-04 (2 Windows tests deferred; carried forward as tests 9-11)
---

## Current Test

[testing complete]

## Tests

### 1. Self-checkout guard override (--allow-self) against the real grugops checkout
expected: From the grugops checkout, run `DRY_RUN=1 node install/install.js --target .` first: it refuses with a stderr line naming `--allow-self`, exit 1, nothing written. Then run `DRY_RUN=1 node install/install.js --target . --allow-self`: the guard is bypassed, the run proceeds through the model-delivery path (17 agent-class lines, each `would-materialize` or `skipped`, with an alias from the closed vocabulary), and the checkout is byte-unchanged afterwards (`git status --porcelain` empty). No test in the suite exercises this arm; the CI backstop is `npm run freshness:adapters` plus `guard_model_assignment`.
coverage_id: 29.2-02 D14
result: pass

### 2. install/README.md says --update is kit-home-only and names the real delivery path
expected: Read `install/README.md` §"Updating the shared kit (--update)" (around line 278-300). It states that `--update` refreshes only the shared kit at `~/.grugops` and writes no adapter into any repo, that a `models` edit reaches an installed repo by re-running `node install/install.js --target /path/to/repo`, and it names the read-only preview `node install/install.js --check --target <repo>`. The prose reads as true and complete; no sentence still implies `--update` delivers adapters.
coverage_id: 29.2-03 D3
result: pass

### 3. CHANGELOG.md Unreleased bullet reads true and claims nothing measured
expected: Under `## [Unreleased]` → `### Changed` in `CHANGELOG.md` (line 42 onward) there is exactly ONE bullet about this change. It names: adapters rendered from the repo's own `.grugops/factory.config.json`; `--check` naming un-reinstalled adapters; a refused `models` block keeping prior adapters; and the two existing-user consequences (pre-change adapters report stale on `--check` until re-install; `--strict` promotes that to a failure). It makes no cost, token-economy, speed or percentage claim.
coverage_id: 29.2-03 D4
result: pass

### 4. factory.config.md "Where the block is read from" paragraph leaves an installed reader with a true impression
expected: Read `agent-factory/config/factory.config.md` line 138 (the `**Where the block is read from, and which file wins.**` paragraph). Although the two-location resolver description is kept, the paragraph now states plainly that for an installed repository `.grugops/factory.config.json` is the ONLY file consulted and a `models` block in that repo's in-kit file is not read. Reading it as an installed user, you would write the block in exactly one place and would not believe a second location is consulted.
coverage_id: 29.2-05 D6
result: pass
note: "user passed all remaining tests (will re-test on a real project later); Claude re-read line 138: states only-file-consulted and one-location-not-two"

### 5. Banner-slot comment states the asymmetry and its reachability bound (D-14 / IN-01)
expected: Read the comment block in `install/install.ts` around lines 1965-1985 (`banner slot CONSUMES its anchor`). It says the banner slot REPLACES the `KIT_BANNER` anchor with the `BAN_OPEN…BAN_CLOSE` block (not "strip-then-inject like KIT=", and not content-idempotent), and it states the reachability bound: agent sources are fresh renders, the skill source is kit text, `--migrate` runs pre-steps then the ordinary install. Read against the code below it, the comment describes what the code does.
coverage_id: 29.2-05 D9
result: pass
note: "user pass-all; Claude re-read install.ts 1967-1981: states the asymmetry (banner slot CONSUMES anchor) and the reachability bound (fresh renders / kit text / --migrate)"

### 6. The adversarial bypass log attacked the right things
expected: Read `29.2-03-SUMMARY.md` §"Step 2 — the bypass log" (line 222 onward). Eight hypotheses (H1-H8) were run against the committed `.js` on scratch mirrors, with an asserted harness premise first. Attempts that broke nothing are recorded as "No bypass" alongside the one that did (F-A, probe 1e: a render with one banner and no kit slot line installed raw and named the generator command; fixed and re-run to exit 3 in both copy and symlink mode). Your judgment: the hypotheses cover the surfaces that matter (banner recogniser, slot routing, symlink mode, count cross-check, refusal paths) and none is padding.
coverage_id: 29.2-03 D10
result: pass
note: "user pass-all: will re-test on a real project later"

### 7. Structural pin on the unreachable slot-less write arm is acceptable assurance (WR-04)
expected: In `install/install.ts` around line 2664 the agents write loop's slot-less arm is a hard refusal (throws / names the file), not a copy-or-link fallthrough. The arm is unreachable by construction because the routing floor refuses every slot-less render before the first write, so the test pin is structural (mutation-proven: restoring the fallthrough turns it red) rather than behavioural. You accept that a structural backstop over dead code is sufficient here.
coverage_id: 29.2-04 D5
result: pass
note: "user pass-all: will re-test on a real project later"

### 8. Structural pin on render-mirror cleanup failure is acceptable assurance (WR-03)
expected: In `install/install.ts` around lines 1872-1895 the temp-mirror `rmSync(dir, {recursive, force, maxRetries: 3})` sits inside a try/catch in a `finally`, the failure is reported under a `cleanup` label, and no code path there can change the run's exit code. A real EBUSY/EPERM after three retries (the Windows antivirus/indexer shape) is NOT reproduced on this host; what is proven is the code shape. You accept a structural pin for this platform-dependent condition.
coverage_id: 29.2-05 D5
result: pass
note: "user pass-all: will re-test on a real project later"

### 9. Mirror spawn and retry-on-locked-handle cleanup on Windows (WINDOWS.md rows 105-107)
expected: On a Windows box, `node install/install.js --target <scratch-repo>` renders through a `mkdtemp` mirror via `process.execPath`, installs 17 adapters, and the mirror is removed (rmSync maxRetries) with no leftover temp tree and exit 0. Not observed on any Windows runner this phase.
coverage_id: 29.2-01 D8
result: pass
note: "USER OVERRIDE — not observed on Windows this session; user passed all remaining tests and will test later on Windows. WINDOWS.md rows 105-107/115 stay UNKNOWN - verify."

### 10. Doctor's second temp-mirror lifecycle on Windows (--check)
expected: On a Windows box, `node install/install.js --check --target <scratch-repo>` performs its fresh-render comparison through a second temp mirror, reports a clean verdict on an up-to-date target (and names stale adapters after a config edit), cleans the mirror up, and exits 0. Not observed on any Windows runner this phase.
coverage_id: 29.2-02 D15
result: pass
note: "USER OVERRIDE — not observed on Windows this session; user passed all remaining tests and will test later on Windows. WINDOWS.md rows 105-107/115 stay UNKNOWN - verify."

### 11. Destination-hazard guard and spawn/path handling on Windows (WINDOWS.md row 115)
expected: On a Windows box: symlink and junction shapes at a target adapter path (leaf symlink, ancestor-directory symlink, dangling symlink, `mklink /J` junction) are refused by name at exit 3 with the pointed-at file byte-unchanged, or the junction is explicitly shown to be a distinct unhandled shape. 5 of the 7 write-bound cases in `install/install.test.ts` skip on win32 for want of SeCreateSymbolicLink. Not observed on any Windows runner this phase.
coverage_id: 29.2-03 D11 (+ 29.2-06 D2 scope)
result: pass
note: "USER OVERRIDE — not observed on Windows this session; user passed all remaining tests and will test later on Windows. WINDOWS.md rows 105-107/115 stay UNKNOWN - verify."

### 12. [auto] A target carrying {\"models\":{\"preset\":\"tiered\"}} at .grugops/factory.config.json is installed with four …
expected: A target carrying {\"models\":{\"preset\":\"tiered\"}} at .grugops/factory.config.json is installed with four `model: opus` and thirteen `model: sonnet` adapters.
result: pass
source: automated
coverage_id: 29.2-01 D1
verified_by: install/install.test.ts#model delivery: a `tiered` target's adapters carry the aliases that preset resolves to (D-01, D-06)

### 13. [auto] A target with no configuration file is installed with seventeen adapters all carrying `model: inherit`, and th…
expected: A target with no configuration file is installed with seventeen adapters all carrying `model: inherit`, and the run states that none was found.
result: pass
source: automated
coverage_id: 29.2-01 D2
verified_by: install/install.test.ts#model delivery: a target with NO configuration file installs adapters that ALL resolve `inherit` (D-05) | install/install.test.ts#model delivery: a ZERO-CONFIG run relays the generator's own announcement and states that no configuration file was found (D-04)

### 14. [auto] An illegal models value installs no adapter, relays the generator's refusal naming the role, the offending val…
expected: An illegal models value installs no adapter, relays the generator's refusal naming the role, the offending value and the four legal aliases, adds one installer line naming the target's own configuration path, still seeds the marker and the memory-bank index, leaves pre-existing adapters byte-unchanged, and exits 3.
result: pass
source: automated
coverage_id: 29.2-01 D3
verified_by: install/install.test.ts#model delivery: an ILLEGAL `models` value refuses the adapter class by name; every other class completes (D-03, R-5)

### 15. [auto] R-5 - no fallback byte source. A missing generator twin, a render that exited nonzero, an unreadable output di…
expected: R-5 - no fallback byte source. A missing generator twin, a render that exited nonzero, an unreadable output directory, or any cross-check disagreement installs no adapter at all.
result: pass
source: automated
coverage_id: 29.2-01 D4
verified_by: install/install.test.ts#model delivery: a kit checkout MISSING a generator twin installs NOTHING, names every absent file, and states that no verdict was produced (R-5) | install/install.test.ts#model delivery: an install set that DIVERGES from the render installs NOTHING and names every differing member, in BOTH directions (D-01)

### 16. [auto] The run reports the resolution: the generator's own lines relayed verbatim, one installer-authored line naming…
expected: The run reports the resolution: the generator's own lines relayed verbatim, one installer-authored line naming the configuration file read, and every per-adapter report line carrying its resolved alias - never a full model id, never a cost claim.
result: pass
source: automated
coverage_id: 29.2-01 D5
verified_by: install/install.test.ts#model delivery: a TIERED run names the configuration file it read and carries the generator's assignment payload verbatim (D-04) | install/install.test.ts#model delivery: the installer holds NO copy of either announcement prefix - it consults the module that owns them (R-1, D-04)

### 17. [auto] The announced member count and the announced alias set are each cross-checked against something this side deri…
expected: The announced member count and the announced alias set are each cross-checked against something this side derived independently; a disagreement installs nothing and prints both sides.
result: pass
source: automated
coverage_id: 29.2-01 D6
verified_by: install/install.test.ts#model delivery: an announced member count that disagrees with the rendered listing installs NOTHING and prints both numbers | install/install.test.ts#model delivery: an announced alias SET that disagrees with the rendered bytes installs NOTHING and prints both sets | install/install.test.ts#model delivery: a rendered adapter carrying two `model:` lines installs NOTHING and names the file

### 18. [auto] DRY_RUN reports the same resolution, every per-adapter line reads would-materialize with its alias, and neithe…
expected: DRY_RUN reports the same resolution, every per-adapter line reads would-materialize with its alias, and neither root is mutated.
result: pass
source: automated
coverage_id: 29.2-01 D7
verified_by: install/install.test.ts#model delivery: DRY_RUN reports the same resolution, reads `would-materialize` with an alias, and mutates neither root

### 19. [auto] A second install over an unchanged target writes no adapter file - bytes AND mtimes unmoved - and reports the …
expected: A second install over an unchanged target writes no adapter file - bytes AND mtimes unmoved - and reports the identical-copy wording linkOrCopy already uses, for every one of the seventeen.
result: pass
source: automated
coverage_id: 29.2-02 D1
verified_by: install/install.test.ts#model delivery: a SECOND install over an unchanged target WRITES NOTHING and reports the identical-copy wording for every adapter (D-11)

### 20. [auto] An install after the target's models block changes from absent to tiered rewrites every changed adapter and na…
expected: An install after the target's models block changes from absent to tiered rewrites every changed adapter and names each with its new alias, read off the bytes rather than assumed from the preset.
result: pass
source: automated
coverage_id: 29.2-02 D2
verified_by: install/install.test.ts#model delivery: a models edit between two installs REWRITES every changed adapter and names each with its new alias (D-11, D-13)

### 21. [auto] Every target adapter carries a target-true banner exactly once, that banner is not the line the committed kit …
expected: Every target adapter carries a target-true banner exactly once, that banner is not the line the committed kit adapter carries, it names .grugops/factory.config.json, and no target file names the generator command.
result: pass
source: automated
coverage_id: 29.2-02 D3
verified_by: install/install.test.ts#model delivery: every target adapter carries a TARGET-TRUE banner exactly once and names no command the target cannot run (D-14) | install/install.test.ts#model delivery: three installs produce the same bytes as one, with exactly one banner block per adapter (D-14)

### 22. [auto] The banner slot carries the same bounded-removal contract as the kit slot: a terminated prior block is dropped…
expected: The banner slot carries the same bounded-removal contract as the kit slot: a terminated prior block is dropped and an unterminated one restores every line it buffered, in order.
result: pass
source: automated
coverage_id: 29.2-02 D4
verified_by: install/install.test.ts#model delivery: a prior TARGET-BANNER block in a materialized source is stripped, and an UNTERMINATED one is restored (CR-01, D-14)

### 23. [auto] A rendered adapter carrying zero or two recognised banner lines installs nothing and names the file - the sile…
expected: A rendered adapter carrying zero or two recognised banner lines installs nothing and names the file - the silent-drift direction and the ambiguous one, each refused.
result: pass
source: automated
coverage_id: 29.2-02 D5
verified_by: install/install.test.ts#model delivery: a rendered adapter carrying ZERO or TWO recognised banner lines installs NOTHING and names the file (D-14)

### 24. [auto] A hand-edited target adapter is rewritten and named on the next run, and its untouched siblings are not - D-13…
expected: A hand-edited target adapter is rewritten and named on the next run, and its untouched siblings are not - D-13 ownership, per file.
result: pass
source: automated
coverage_id: 29.2-02 D6
verified_by: install/install.test.ts#model delivery: a HAND-EDITED target adapter is rewritten and named, and its untouched siblings are not (D-13)

### 25. [auto] `--check` on a target whose configuration was edited after install names every stale adapter and the re-run re…
expected: `--check` on a target whose configuration was edited after install names every stale adapter and the re-run remedy, exits 0 with a warning count, and exits 1 under --strict.
result: pass
source: automated
coverage_id: 29.2-02 D7
verified_by: install/install.test.ts#doctor: a configuration edited AFTER install names every stale adapter and the re-run remedy — WARN bare, FAIL under --strict (D-09, D-10) | install/install.test.ts#doctor: a --check run immediately after a clean install reports NO staleness line at all (D-09)

### 26. [auto] A --check run over a stale target leaves the target byte-identical, and the doctor body carries no write call …
expected: A --check run over a stale target leaves the target byte-identical, and the doctor body carries no write call and no mention of the frontmatter key.
result: pass
source: automated
coverage_id: 29.2-02 D8
verified_by: install/install.test.ts#doctor: a --check run over a STALE target leaves the target byte-identical (T-29.2-10) | install/install.test.ts#doctor: the staleness verdict landed in the WARN tier only, and no frontmatter grammar came with it (D-09, D-10)

### 27. [auto] A checkout missing a generator module states that NO VERDICT on adapter staleness was produced, names the abse…
expected: A checkout missing a generator module states that NO VERDICT on adapter staleness was produced, names the absent module, never reports a clean compare, and escalates under --strict. A target with a failing cross-check reports that primary defect and produces no staleness lines at all.
result: pass
source: automated
coverage_id: 29.2-02 D9
verified_by: install/install.test.ts#doctor: a checkout MISSING a generator module states that NO VERDICT on adapter staleness was produced, and never a clean one (D-09) | install/install.test.ts#doctor: a target with a FAILING cross-check reports that primary defect and produces no staleness lines at all (D-09)

### 28. [auto] The install-side twin list and the freshness harness's twin list are the same four modules, both integers pinn…
expected: The install-side twin list and the freshness harness's twin list are the same four modules, both integers pinned, with a declared-versus-parsed floor whose refusal is exercised in memory.
result: pass
source: automated
coverage_id: 29.2-02 D10
verified_by: install/install.test.ts#model delivery: the install-side twin list and the freshness harness's twin list are the SAME four modules (29.2 plan 02) | install/install.test.ts#model delivery: the twin-copy parser FAILS LOUDLY on a shape it cannot read, it does not shrink the set (WR-04 idiom)

### 29. [auto] The banner literal install.ts recognises is byte-equal to the generator's own declaration, and appears exactly…
expected: The banner literal install.ts recognises is byte-equal to the generator's own declaration, and appears exactly once in each of the seventeen committed adapters derived from the directory listing.
result: pass
source: automated
coverage_id: 29.2-02 D11
verified_by: install/install.test.ts#model delivery: the banner literal install.ts recognises is BYTE-EQUAL to the generator's own declaration (D-14)

### 30. [auto] The report ordering kit -> adapters -> state seed is asserted explicitly over a real run's stdout for the firs…
expected: The report ordering kit -> adapters -> state seed is asserted explicitly over a real run's stdout for the first time; the shipped seed parses to a non-empty object carrying no models key; a non-default-preset target is still fully removed by the uninstaller.
result: pass
source: automated
coverage_id: 29.2-02 D12
verified_by: install/install.test.ts#model delivery: the run reports its classes in the pinned order — kit, then adapters, then state seed (D-05) | install/install.test.ts#model delivery: the shipped seed carries NO `models` key, so a freshly seeded target resolves `inherit` everywhere (D-08) | install/install.test.ts#model delivery: a target installed with a NON-DEFAULT preset is still fully removed by the uninstaller

### 31. [auto] D-16: a source-shaped target carrying a models block is refused before any render, so the kit's own committed …
expected: D-16: a source-shaped target carrying a models block is refused before any render, so the kit's own committed adapters cannot be rendered from a target configuration through this path.
result: pass
source: automated
coverage_id: 29.2-02 D13
verified_by: install/install.test.ts#D-07 self-checkout guard: refuses a source-shaped target by default; --allow-self overrides

### 32. [auto] The 29.1 disclosure that a per-repo models block is inert for an installed target no longer appears in kit pro…
expected: The 29.1 disclosure that a per-repo models block is inert for an installed target no longer appears in kit prose; both sites state what is true now.
result: pass
source: automated
coverage_id: 29.2-03 D1
verified_by: scripts/model-dial-consistency.test.ts#the retired 29.1 disclosure has not survived in the reference | grep -rn 'INERT FOR AN INSTALLED' scripts/ agent-factory/ install/ CHANGELOG.md

### 33. [auto] The config field reference names the install re-run as the way a models edit reaches an installed repository, …
expected: The config field reference names the install re-run as the way a models edit reaches an installed repository, without pinning the note's wording.
result: pass
source: automated
coverage_id: 29.2-03 D2
verified_by: scripts/model-dial-consistency.test.ts#the config field reference names the install re-run as the way a models edit reaches an installed repository

### 34. [auto] A rendered agent adapter that cannot be materialized is refused rather than copied raw, so no target file name…
expected: A rendered agent adapter that cannot be materialized is refused rather than copied raw, so no target file names a command the target cannot run and no target is left holding symlinks into a deleted temp mirror.
result: pass
source: automated
coverage_id: 29.2-03 D5
verified_by: install/install.test.ts#model delivery: a rendered adapter carrying NO kit slot line installs NOTHING and names the file (D-14)

### 35. [auto] A mirror the installer cannot build - an unusable temp root, or a checkout whose kit-source trees are absent -…
expected: A mirror the installer cannot build - an unusable temp root, or a checkout whose kit-source trees are absent - is a named finding at exit 3 with every other class complete, never an uncaught throw.
result: pass
source: automated
coverage_id: 29.2-03 D6
verified_by: install/install.test.ts#model delivery: a mirror the installer cannot BUILD is a named finding, never a crash (R-5)

### 36. [auto] A target whose configuration path resolves to something that is not a readable file is a named finding, never …
expected: A target whose configuration path resolves to something that is not a readable file is a named finding, never a crash and never read as an absent configuration.
result: pass
source: automated
coverage_id: 29.2-03 D7
verified_by: install/install.test.ts#model delivery: a target whose configuration path is NOT A READABLE FILE installs NOTHING, names the path, and lets every other class complete (R-5)

### 37. [auto] The doctor asks both directions of its own set question: a member the render produced that the kit's adapter s…
expected: The doctor asks both directions of its own set question: a member the render produced that the kit's adapter set does not carry is named as one it produced no verdict for.
result: pass
source: automated
coverage_id: 29.2-03 D8
verified_by: install/install.test.ts#doctor: a fresh render producing members the kit's adapter set does not carry states that NO VERDICT was produced for them (D-09)

### 38. [auto] The doctor's no-verdict relay names the real configuration path on this machine beside the relayed message, wh…
expected: The doctor's no-verdict relay names the real configuration path on this machine beside the relayed message, which names a temp mirror that no longer exists.
result: pass
source: automated
coverage_id: 29.2-03 D9
verified_by: install/install.test.ts#doctor: a REFUSED configuration is relayed with the REAL configuration path, never only the temp mirror's (D-09)

### 39. [auto] A target adapter destination whose leaf is a symbolic link is refused by name before any read and before any w…
expected: A target adapter destination whose leaf is a symbolic link is refused by name before any read and before any write; the file it points at is byte-unchanged; the run exits 3.
result: pass
source: automated
coverage_id: 29.2-04 D1
verified_by: install/install.test.ts#write bound: a target adapter that is a SYMLINK is refused by name, and the file it points at is byte-unchanged (CR-01) | adversarial shape 1 — INSTALL_MODE=copy … node install/install.js --yes over a leaf-symlinked target, run from a scratch dir outside the repo

### 40. [auto] A destination reached through a symlinked ANCESTOR directory is refused by name and no adapter byte lands outs…
expected: A destination reached through a symlinked ANCESTOR directory is refused by name and no adapter byte lands outside the resolved target, even though the leaf is a regular file.
result: pass
source: automated
coverage_id: 29.2-04 D2
verified_by: install/install.test.ts#write bound: an ANCESTOR directory symlink cannot carry a write outside the target (the leaf is a regular file) | adversarial shapes 2 and 9 (sibling-prefix trap)

### 41. [auto] A DANGLING symlink at a target adapter path creates nothing outside the target.
expected: A DANGLING symlink at a target adapter path creates nothing outside the target.
result: pass
source: automated
coverage_id: 29.2-04 D3
verified_by: install/install.test.ts#write bound: a DANGLING symlink at a target adapter path creates nothing outside the target | adversarial shape 3 (pre-fix CREATED a file outside the target; fixed exits 3)

### 42. [auto] The write path and the --check doctor decide a hazardous destination with ONE predicate; the doctor names such…
expected: The write path and the --check doctor decide a hazardous destination with ONE predicate; the doctor names such a file instead of reading through it and never prints a clean verdict over a target whose adapters are links.
result: pass
source: automated
coverage_id: 29.2-04 D4
verified_by: install/install.test.ts#write bound: --check names a symlinked target adapter instead of reading through it (D-09) | install/install.test.ts#write bound: install and --check ask the SAME question about a destination, and a target whose realpath differs from its literal path still installs | adversarial shapes 5a/5b + `--check --strict` promotion (pre-fix exit 0 over 17 links → fixed exit 1)

### 43. [auto] The happy path and D-11's visible no-op are unchanged: an ordinary install materializes 17 adapters and an ord…
expected: The happy path and D-11's visible no-op are unchanged: an ordinary install materializes 17 adapters and an ordinary re-run reports 17 `skipped (identical copy present)`.
result: pass
source: automated
coverage_id: 29.2-04 D6
verified_by: adversarial shape 7 — two consecutive real installs from a scratch dir outside the repo | install/install.test.ts — 119 pre-existing cases still passing, incl. the D-11 idempotence cases

### 44. [auto] A SECOND `--dry-run` over an unchanged, already-installed target reports 17 agent-class `skipped` lines and ze…
expected: A SECOND `--dry-run` over an unchanged, already-installed target reports 17 agent-class `skipped` lines and zero `would-materialize` lines, and the target is byte-unchanged (WR-01, D-11 preview parity).
result: pass
source: automated
coverage_id: 29.2-05 D1
verified_by: install/install.test.ts#model delivery: a SECOND dry run over an unchanged target reports `skipped`, not `would-materialize` (D-11 preview parity, WR-01) | live pre-fix/fixed comparison over the SAME installed target — pre-fix (cd107b2) 17 would-materialize / 0 skipped; fixed 0 / 17

### 45. [auto] The FRESH-target preview is unchanged — 17 `would-materialize` with aliases from the closed vocabulary — so th…
expected: The FRESH-target preview is unchanged — 17 `would-materialize` with aliases from the closed vocabulary — so the relocation did not weaken the direction that was already correct.
result: pass
source: automated
coverage_id: 29.2-05 D2
verified_by: install/install.test.ts#model delivery: DRY_RUN reports the same resolution, reads `would-materialize` with an alias, and mutates neither root

### 46. [auto] The relocated preview branch sits BELOW the identical-bytes compare and BELOW the plan-04 hazard guard, and AB…
expected: The relocated preview branch sits BELOW the identical-bytes compare and BELOW the plan-04 hazard guard, and ABOVE the write.
result: pass
source: automated
coverage_id: 29.2-05 D3
verified_by: offset check over non-comment lines of the materializeAdapter body — adapterDestHazard(dest) 311 < identical copy present 717 < if (DRY_RUN) 762 < writeFileSync(dest 875 | install/install.test.ts — the seven plan-04 write-bound cases, all still passing

### 47. [auto] The temp-mirror disclaimer has ONE declaration, the sentence appears as a literal exactly once, and three site…
expected: The temp-mirror disclaimer has ONE declaration, the sentence appears as a literal exactly once, and three sites interpolate it — the doctor's NO VERDICT arm, the install-side render refusal, and the success-path relay (WR-05).
result: pass
source: automated
coverage_id: 29.2-05 D4
verified_by: install/install.test.ts#install reporting: the temp-mirror disclaimer has ONE authority, and the render cleanup cannot change the run's exit code (WR-03/WR-05) | live install — the `resolution` line now prints directly beneath the relayed mirror path, naming the real agents directory; the relayed mirror was confirmed ABSENT on disk afterwards

### 48. [auto] The retired 29.1 disclosure is still refused by name, and an honest future disclosure about a different dial r…
expected: The retired 29.1 disclosure is still refused by name, and an honest future disclosure about a different dial reusing the same heading style is no longer refused (IN-02).
result: pass
source: automated
coverage_id: 29.2-05 D7
verified_by: scripts/model-dial-consistency.test.ts#the retired 29.1 disclosure has not survived in the reference | two-direction mutation proof — retired sentence restored verbatim RED (2 fragments returned); `Disclosed limitation.` about the `queue` dial PASSES at HEAD and RED under the pre-narrowing fragment

### 49. [auto] `CHANGELOG.md` states the two reachable consequences for existing users the entry omitted — pre-change adapter…
expected: `CHANGELOG.md` states the two reachable consequences for existing users the entry omitted — pre-change adapters report stale on `--check` until a re-install, and `--strict` promotes that to a failure (IN-04).
result: pass
source: automated
coverage_id: 29.2-05 D8
verified_by: the `## [Unreleased]` `### Changed` bullet contains both `--check` and `--strict`; the bullet count is unchanged (one bullet about this change, not two) | node scripts/check-banned-claims.js — 0 findings over 117/117 elements

### 50. [auto] Every finding id in 29.2-REVIEW.md resolves to exactly one disposition, and the count of ids equals the count …
expected: Every finding id in 29.2-REVIEW.md resolves to exactly one disposition, and the count of ids equals the count of dispositions — both numbers measured this session.
result: pass
source: automated
coverage_id: 29.2-06 D1
verified_by: grep -cE '^### (CR|WR|IN)-[0-9]+:' 29.2-REVIEW.md = 10; disposition rows = 10; per-id occurrence count = 1 for all ten ids

### 51. [auto] The POSIX-only scope of the destination-hazard guard is recorded as unobserved on Windows rather than allowed …
expected: The POSIX-only scope of the destination-hazard guard is recorded as unobserved on Windows rather than allowed to read as a pass, with the specific junction unknown the containment arm introduces.
result: pass
source: automated
coverage_id: 29.2-06 D2
verified_by: .planning/WINDOWS.md row 115 — file install/install.ts, contains SeCreateSymbolicLink and UNKNOWN - verify; 5 of 7 write-bound cases carry a win32 early return, both counts derived from install/install.test.ts this session

### 52. [auto] The guard did not break the delivery path D-07 and D-15 documented: all five behaviours 29.2-VERIFICATION.md r…
expected: The guard did not break the delivery path D-07 and D-15 documented: all five behaviours 29.2-VERIFICATION.md reproduced live still hold against the guarded build.
result: pass
source: automated
coverage_id: 29.2-06 D3
verified_by: five live runs driving the built install/install.js on scratch targets outside the repository — 4 opus + 13 sonnet; 17 skipped-identical; --check exit 0 naming 17 / --strict exit 1; unknown preset exit 3 with no agents directory; hand-edit reverted and reported by name

### 53. [auto] The gate block that certified plans 01-03 runs end to end against the tree this round produced, and the suite …
expected: The gate block that certified plans 01-03 runs end to end against the tree this round produced, and the suite is at the measured baseline or better.
result: pass
source: automated
coverage_id: 29.2-06 D4
verified_by: 16-command gate block exit 0, plus the structure validator at exit 0; two consecutive full-suite runs at 1 failed / 2445 passed / 2 skipped

### 54. [auto] The round introduced no dependency: the package manifest and lockfile diff across the whole round is empty, as…
expected: The round introduced no dependency: the package manifest and lockfile diff across the whole round is empty, asserted rather than assumed.
result: pass
source: automated
coverage_id: 29.2-06 D5
verified_by: git diff --name-only 3fbf854..HEAD -- package.json package-lock.json → no output; working-tree arm --exit-code → 0

## Summary

total: 54
passed: 54
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]

## Deferred Follow-Ups

- test: 1
  idea: "let's skip windows UAT for now, I will later test on windows."
  deferred_at: 2026-09-04
  note: "from the 2026-09-04 session (its test 1 = Windows destination-hazard guard); carried into this session as test 11"
- test: 2
  idea: "let's skip windows UAT for now, I will later test on windows."
  deferred_at: 2026-09-04
  note: "from the 2026-09-04 session (its test 2 = Windows render-mirror spawn/path handling); carried into this session as tests 9-10"

---
phase: 29.2-model-assignment-delivery-path
verified: 2026-09-04T22:10:00Z
status: human_needed
score: 29/29 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/10
  gaps_closed:
    - "The installer never overwrites content outside the target it does not own (truth 10 / CR-01) — reproduced fixed: a leaf symlink at a target adapter path is refused by name, exit 3, sentinel file byte-unchanged."
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Run the destination-hazard guard's leaf-symlink, ancestor-directory-symlink, dangling-symlink and directory-junction shapes on a Windows CI runner or Windows box against the built `install/install.js`."
    expected: "The same refusals observed on POSIX (exit 3, sentinel/outside-directory byte-unchanged, finding names the path) hold on Windows; a directory junction (`mklink /J`) is either refused by the ancestor arm or explicitly shown to be a distinct unhandled shape."
    why_human: "Creating a symlink on Windows needs the `SeCreateSymbolicLink` privilege an unprivileged CI runner does not hold, so 5 of the 7 new `install/install.test.ts` write-bound cases open with a `process.platform === \"win32\"` early return and assert nothing on that platform. `realpathSync`'s treatment of Windows directory junctions (which are not symbolic links to `lstat`) is a second, distinct unknown the containment arm introduces. This project's own `.planning/WINDOWS.md` records this at row 115 as `UNKNOWN - verify` rather than passing — this verification independently confirms row 115's claims about the test file (7 cases, 5 with a win32 early return) are accurate, but cannot itself supply a Windows runner."
  - test: "General render-mirror spawn/path handling of the install path on a Windows box (carried forward from the initial verification round, `29.2-VALIDATION.md` WINDOWS rows 105-107, unaffected by this round's fixes)."
    expected: "Mirror spawn, temp-tree cleanup and path handling behave the same as on macOS/Linux; the `--check` doctor's fresh-render comparison likewise holds."
    why_human: "No Windows runner was exercised in this or the initial verification session."
---

# Phase 29.2: Model Assignment Delivery Path Verification Report

**Phase Goal:** A `models` block written into a target repo actually changes the adapters that repo's Claude Code session loads — closing the disclosed gap 29.1 ships with.
**Verified:** 2026-09-04T22:10:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap-closure round 1 (plans 04, 05, 06)

## Summary of This Round

The prior verification (2026-09-04T11:04:06Z) returned `gaps_found` (9/10): the delivery mechanism
worked as designed, but truth 10 — the installer never overwrites content it does not own — failed
on an independently reproduced data-loss defect (REVIEW.md CR-01): a symlinked target-adapter
destination was written through at exit 0.

This round re-verifies **all** truths from all six plans (01-03 original, 04-06 gap-closure), not
just the failed one. Every check below was independently reproduced against the built
`install/install.js` on scratch targets outside the repository — not inferred from SUMMARY.md or
REVIEW.md claims. Per this project's standing rule ("a green suite is not proof of a safety
invariant"), the adversarial reproduction of the containment guard (leaf symlink, ancestor-directory
symlink, dangling symlink) was run fresh in this session with its own scratch fixtures, independent
of the 119 (now 126+) tests already in `install/install.test.ts`.

**Result: the CR-01 gap is closed and no regression was found.** All 10 original truths plus 19
additional truths introduced by the three gap-closure plans hold. The phase is not marked `passed`,
however, because the gap-closure round's own honesty mechanism (`.planning/WINDOWS.md` row 115)
surfaces a genuine, disclosed, currently-unverifiable question: whether the new destination-hazard
guard behaves identically on Windows. That is a human-verification item, not a failure — the guard's
own test suite explicitly skips the symlink-creating cases on `win32` because an unprivileged CI
runner lacks `SeCreateSymbolicLink`, and this verification session has no Windows environment to
supply the missing evidence. Per the decision tree, any unresolved human-verification item routes
the overall status to `human_needed` regardless of how many truths pass.

## Goal Achievement

### Observable Truths — original 10 (carried from initial verification)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A target carrying `{"models":{"preset":"tiered"}}` installs with `model:` lines matching the resolved aliases (4 `opus` / 13 `sonnet`) | ✓ VERIFIED | Reproduced live this session on a fresh scratch target: `node install/install.js --target <scratch> --yes` produced 4 `opus` + 13 `sonnet` lines. |
| 2 | A second install over an unchanged target writes nothing and reports `skipped (identical copy present)` for every adapter (D-11) | ✓ VERIFIED | Reproduced live: re-run reported 24 `skipped (identical copy present)` lines (17 agents + 7 skills), exit 0. |
| 3 | After a config edit, `--check` names every stale adapter, WARN by default, FAIL under `--strict` (D-09/D-10) | ✓ VERIFIED | Reproduced live: `--check --strict` over an edited-config target exited 1, `1 WARNING(S) (--strict: promoted to failure)`. |
| 4 | An illegal `models` block (unknown preset) installs no adapter, names the refusal, leaves other install classes complete, exits 3 (D-03) | ✓ VERIFIED | Reproduced live: `{"models":{"preset":"nonexistent"}}` → `== install INCOMPLETE — 1 item(s) need verification ==`, exit 3. |
| 5 | A hand-edited `model:` line on an installed adapter is lost (overwritten to the config-resolved value) on the next re-run, reported by name (D-13) | ✓ VERIFIED | Reproduced live: hand-edited `grugops-ba-pm.md` to `model: haiku`; re-run reported `materialized .claude/agents/grugops-ba-pm.md (... model=sonnet)`, file reverted. |
| 6 | The target banner names the install re-run and `.grugops/factory.config.json`, never the generator command that does not exist in a target (D-14) | ✓ VERIFIED | Confirmed on this session's installed adapter: `<!-- GENERATED — do not hand-edit ... re-run \`node install/install.js --target <this repository>\` ... The model line comes from .grugops/factory.config.json ... -->`. |
| 7 | The 29.1 "inert for an installed target" disclosure no longer appears in kit prose; `scripts/model-tiers.ts` states current truth, test moved in the same commit (D-15) | ✓ VERIFIED | `scripts/model-tiers.ts:62-75` unchanged since initial verification, still reads "SCOPE, AS OF PHASE 29.2 ... installing a repository from the checkout delivers that repository's models block into the adapters". |
| 8 | `--update` stays kit-home-only; `install/README.md` states the re-run-from-checkout remedy (D-07) | ✓ VERIFIED | `install/README.md:289-298` unchanged, states `--update` "does not touch any repo's per-repo state" and names the `--target` re-run remedy. |
| 9 | `CHANGELOG.md` records the reachable behaviour change under `## [Unreleased]` `### Changed`, no cost/conformance claim, and (new this round) names the two consequences existing users meet | ✓ VERIFIED | `CHANGELOG.md:41-49`: original bullet plus the added IN-04 clause naming the `--check` staleness and `--strict` promotion consequences. `node scripts/check-banned-claims.js` confirms 0 findings. |
| 10 | The installer never overwrites content outside the target it does not own (CLAUDE.md hard never-overwrite constraint / D-13's own rationale) | ✓ VERIFIED (was ✗ FAILED) | **Gap closed.** Reproduced independently this session: a leaf symlink at a target adapter path pointing at an outside sentinel file (`MY PRECIOUS USER NOTES`) is refused by name before any read/write — `verify .claude/agents/grugops-ba-pm.md — ... is a symbolic link ...`, exit 3 — and the sentinel file's bytes are unchanged and the symlink survives untouched. |

### Observable Truths — additional, introduced by gap-closure plans 04-06

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 11 | STRICT SHARPENING: the installer never CREATES content outside the target either — a dangling symlink must not cause a new file to be written at the link's target path | ✓ VERIFIED | Reproduced live: a dangling symlink (`grugops-installer.md → does-not-exist-at-all.md`, target does not exist) at a target adapter path is refused (`verify ... is a symbolic link ...`), exit 3; confirmed no file was created at the dangling target path afterward. |
| 12 | A destination reached through a symlinked ANCESTOR directory is refused, even though the leaf is a regular file and a leaf-only `lstat` would answer false | ✓ VERIFIED | Reproduced live: `.claude/agents` replaced with a symlink to an external `outside-agents/` directory, config edited to force a re-render; all 17 adapters refused by name (`... resolves to .../outside-agents/<name>.md, which is OUTSIDE the target ...`), exit 3, and `outside-agents/` was confirmed empty afterward (`ls -la` → 0 entries). |
| 13 | The write path and the `--check` doctor decide a hazardous destination with ONE shared predicate; the doctor names such files instead of reading through them, and does not print a clean `ALL CHECKS PASSED` verdict over a target whose adapters are links | ✓ VERIFIED | Code: `adapterDestHazard()` declared once at `install/install.ts:470`, consumed by `materializeAdapter()` at `:2069` and the doctor's byte-compare loop at `:897`. Live: `--check` over the leaf-symlinked target1 (2 symlinked adapters) printed `WARN adapter staleness: 2 member(s) ... sit at destinations this run will not read through — grugops-ba-pm.md, grugops-installer.md ...`, `ALL CHECKS PASSED (1 warning(s))` — named, not silent, and `--strict` on the same target exited 1. |
| 14 | The D-11 skip-if-identical arm is unreachable for a hazardous destination — `skipped (identical copy present)` can never be printed about bytes living outside the target | ✓ VERIFIED | Code inspection: `materializeAdapter()` calls `adapterDestHazard(dest)` and returns on a hazard (`:2069-2073`) strictly before the `readFileSync(dest)` / identical-bytes compare (`:2074-2086`). |
| 15 | D-13's ownership claim is bounded, not withdrawn: a regular file inside the target is still kit-owned and still rewritten on re-run | ✓ VERIFIED | Reproduced live (same session): the ordinary hand-edit-reversion case (truth 5 above) still rewrites a regular in-target file on re-run; the guard only intercepted the symlink/ancestor/dangling shapes, never a regular file. |
| 16 | The agents write loop cannot copy or link raw mirror bytes under any routing outcome (WR-04) — the slot-less arm is a hard refusal, not a fallthrough | ✓ VERIFIED | Code: `install/install.ts:2675-2683` replaces the prior `linkOrCopy` fallthrough with a `verify()` refusal naming "raw mirror bytes"; `install/install.test.ts:2364-2398` structurally pins that no `linkOrCopy(` call exists in the bounded write loop and that the slot-less arm calls `verify()`. Targeted run: `npx vitest run install/install.test.ts -t "write bound"` → 7 passed. |
| 17 | D-11's visible-no-op property is visible in the PREVIEW as well as the run: `--dry-run` over an unchanged target reports `skipped`, not `would-materialize` (WR-01) | ✓ VERIFIED | Reproduced live: `DRY_RUN=1 node install/install.js --target <installed target> --yes` reported 49 `skipped` lines and 0 `would-materialize` lines. |
| 18 | No sentence in `factory.config.md` tells an installed user that a configuration location is consulted which D-06 guarantees the render never sees (WR-02) | ✓ VERIFIED | `agent-factory/config/factory.config.md:138` (unchanged since prior verification's fix) states "for an installed repository `.grugops/factory.config.json` is the only file consulted", the two-location description kept and audience-bounded. |
| 19 | A cleanup failure is never reported as a verdict failure; an unremovable temp mirror cannot convert a completed render into a crash or change the exit code (WR-03) | ✓ VERIFIED (code inspection; not independently reproduced) | `install/install.ts:1888-1897`: `rmSync` wrapped in `try/catch`, reports under its own `cleanup` label rather than calling `verify()`. Not independently reproduced (requires simulating a locked/`EBUSY` temp directory), consistent with the initial verification's treatment. |
| 20 | Nothing on the SUCCESS path names a directory the reader cannot go look at — one shared wording authority, not a third spelling (WR-05) | ✓ VERIFIED | `TEMP_MIRROR_DISCLAIMER` declared once at `install/install.ts:438`, consumed at `:835` (doctor NO VERDICT), `:2465` (render-refusal), and `:2646` (the new success-path relay). |
| 21 | The banner slot's transform asymmetry is stated with its reachability bound rather than left implied symmetric (IN-01) | ✓ VERIFIED | Confirmed comment present near the `transformAdapter` state machine documenting the kit-slot-vs-banner-slot asymmetry and its reachability bound (per plan 05 task 2 commit `7e5c157`). |
| 22 | The retired 29.1 disclosure guard identifies the 29.1 SENTENCE, not a heading style a future honest disclosure would reuse (IN-02) | ✓ VERIFIED | `scripts/model-dial-consistency.test.ts:1382-1385`: `RETIRED_INSTALLED_TARGET_DISCLOSURES[0]` is now `"Disclosed limitation — an installed repository"`. |
| 23 | The doctor distinguishes ENOENT (`absent`) from every other read error (`unreadable`) (IN-03) | ✓ VERIFIED | `install/install.ts:906-914`: the catch checks `(e as NodeJS.ErrnoException)?.code !== "ENOENT"` and routes non-ENOENT errors to a separate `unreadable` list. |
| 24 | `CHANGELOG.md` states the two reachable consequences for existing users the entry previously omitted (IN-04) | ✓ VERIFIED | Same as truth 9 above — confirmed present. |
| 25 | Every finding in `29.2-REVIEW.md` (CR-01, WR-01…05, IN-01…04 — 10 total) has a recorded disposition: a commit that closed it, with no silent omission | ✓ VERIFIED | `deferred-items.md`'s gap-closure section: 10-row disposition table, denominator independently cross-checked this session — `grep -cE '^### (CR|WR|IN)-[0-9]+:' 29.2-REVIEW.md` → 10; all 6 cited commits (`91654b3`, `379fa2b`, `e6d9a8a`, `9f798e1`, `7e5c157`, `e2abb55`) confirmed to exist on the tree with matching subject lines. |
| 26 | An unobservable condition (Windows behaviour of the new guard) is recorded as unobserved rather than as passing | ✓ VERIFIED (as a disclosure) | `.planning/WINDOWS.md:132` row 115 present, `unrun-verify`, correctly describes 7 write-bound cases / 5 with a `win32` early return (independently counted this session, matches) and the directory-junction unknown. See Human Verification below — this truth is about the *disclosure*, and the disclosure is accurate; the underlying Windows behaviour itself remains unverified by anyone, which is the human-verification item. |
| 27 | The phase's exit criterion is the measured baseline or better, never asserted as a green suite | ✓ VERIFIED | Reproduced live this session: `npx vitest run --exclude '**/scripts/e2e/**'` → `1 failed \| 54 passed (55)` files, `1 failed \| 2445 passed \| 2 skipped (2448)` tests — exactly the measured baseline stated in the task (1 failed / 2445 passed), and the single failure is the same pre-existing `scripts/frontmatter.test.ts` D-49 control refusal on `29.1-VERIFICATION-round4.md`, not a new failure. |
| 28 | The gate block that closed plans 01-03 is re-run in full against the tree this round produced | ✓ VERIFIED | Independently re-run this session (not merely trusted from SUMMARY): `npm run freshness` (50/50 fresh), `npm run check:build-parity` (clean), `node scripts/check-foundation-guards.js` (ALL CHECKS PASSED, `KIT-03: 17 roles == 17 adapters`), `node scripts/check-banned-claims.js` (0 findings/117 elements), `node scripts/check-diff-disposition.js` (0 findings/37 elements), `npm run check:audit-register` (ALL CHECKS PASSED), `node scripts/check-nul-bytes.js` (0/1731 files). |
| 29 | The delivery contract (D-07/D-11/D-09/D-13/D-15) is re-confirmed end to end after the guard landed | ✓ VERIFIED | Reproduced live this session, post-guard, on a fresh scratch target: 4 opus/13 sonnet split, idempotent re-run (24 skipped), `--check`/`--strict` staleness detection (exit 1 under `--strict`), illegal preset refusal (exit 3, no `.claude/agents` created), hand-edit reversion (reported by name), dry-run preview parity (0 would-materialize / 49 skipped over an unchanged target). |

**Score:** 29/29 truths verified. 0 present-but-behavior-unverified. 1 disclosed-but-unobservable condition (Windows) routed to human verification, not counted as a failure.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `install/install.ts` — `adapterDestHazard()` | Bounds what a target adapter destination may be, before any read/write | ✓ VERIFIED | Declared once at `:470`, above the `--check` early exit (temporal-dead-zone rule respected); both arms (leaf-symlink, resolved-ancestor-containment) present and exercised live. |
| `install/install.ts` — `materializeAdapter()` consumer call | Guard precedes source/dest reads and the byte compare | ✓ VERIFIED | `:2069-2073`, before `readFileSync(src)`/`readFileSync(dest)`. |
| `install/install.ts` — doctor consumer call | Guard precedes the doctor's `readFileSync(destPath)` | ✓ VERIFIED | `:897-901`, hazard checked before `actual = readFileSync(destPath, ...)`. |
| `install/install.ts` — WR-04 hard refusal | No `linkOrCopy` fallthrough in the agents write loop | ✓ VERIFIED | `:2675-2683`; structurally pinned by `install.test.ts:2364-2398`. |
| `install/install.js` | Committed build of `install.ts` | ✓ VERIFIED | `npm run freshness` (50/50) and `npm run check:build-parity` both green at HEAD `7f1e32b`. |
| `install/install.test.ts` | 7 new write-bound cases, all watched RED-first per plan | ✓ VERIFIED | `npx vitest run install/install.test.ts -t "write bound"` → 7 passed. |
| `agent-factory/config/factory.config.md` | WR-02 audience-bound sentence | ✓ VERIFIED | `:138`, confirmed present and internally consistent with the surrounding paragraph. |
| `scripts/model-dial-consistency.test.ts` | IN-02 narrowed fragment | ✓ VERIFIED | `:1382-1385`. |
| `CHANGELOG.md` | IN-04 two-consequence clause | ✓ VERIFIED | `:41-49`. |
| `.planning/phases/29.2-.../deferred-items.md` | 10-row disposition table | ✓ VERIFIED | Present, denominator independently re-derived and matched (10). |
| `.planning/WINDOWS.md` | Row 115 (Windows-unobserved guard) | ✓ VERIFIED | Present at line 132, content matches independently-derived test-file counts (7 cases / 5 win32-skipped). |
| `29.2-VALIDATION.md` | Per-task rows for plans 04-06 | ✓ VERIFIED | Rows 04-T1 through 06-T2 present, each naming the command that ran and its observed result. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `adapterDestHazard()` | `materializeAdapter()` (write path) | direct call before read/write | ✓ WIRED | Confirmed by code read and live symlink/ancestor/dangling reproductions, all refused. |
| `adapterDestHazard()` | doctor byte-compare loop (read path) | direct call before read | ✓ WIRED | Confirmed by code read and live `--check`/`--strict` reproduction over a hazardous target. |
| `TARGET/.grugops/factory.config.json` | rendered adapters | mirror copy + spawned generator | ✓ WIRED | Confirmed live across multiple fresh installs this session (4 opus/13 sonnet). |
| `REVIEW.md` finding ids | commits / ledger rows | `deferred-items.md` disposition table | ✓ WIRED | All 10 ids resolve to exactly one disposition; all 6 distinct cited commit hashes exist on the tree with matching subjects. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| Installed adapter `model:` line | resolved alias | `TARGET/.grugops/factory.config.json` → mirrored generator → `materializeAdapter()` | Yes | ✓ FLOWING |
| `--check` stale/hazardous verdict | byte-diff result or hazard reason | fresh mirror render vs. `readFileSync(dest)`, gated by `adapterDestHazard(dest)` | Yes — and now correctly refuses to read through a link rather than misreporting | ✓ FLOWING (previously ⚠️ HOLLOW at the CR-01 blind spot; now closed) |

### Behavioral Spot-Checks (this session, independent of SUMMARY/REVIEW claims)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Fresh install, tiered preset | `node install/install.js --target <scratch> --yes` | 4 opus + 13 sonnet | ✓ PASS |
| Idempotent re-run | same command twice | 24 `skipped (identical copy present)` | ✓ PASS |
| `--check`/`--strict` staleness | edit config, `--check --strict` | exit 1, `1 WARNING(S) (--strict: promoted to failure)` | ✓ PASS |
| Illegal preset refusal | `{"models":{"preset":"nonexistent"}}` | exit 3, no `.claude/agents` created | ✓ PASS |
| Hand-edit reversion | edit `model:` line by hand, re-run | reverted, reported `materialized ... model=sonnet` | ✓ PASS |
| **Leaf symlink at target adapter path (CR-01)** | plant symlink → outside sentinel file, re-run | refused by name, exit 3, sentinel byte-unchanged, symlink survives | ✓ PASS (was FAIL at initial verification) |
| **Ancestor-directory symlink (`.claude/agents` → outside dir)** | plant symlink, force differing render, re-run | all 17 adapters refused by name, exit 3, outside directory confirmed empty afterward | ✓ PASS |
| **Dangling symlink** | plant symlink to a nonexistent path, re-run | refused by name, exit 3, no file created at the dangling target | ✓ PASS |
| Doctor over a target holding hazardous (symlinked) adapters | `--check` | `WARN adapter staleness: 2 member(s) ... sit at destinations this run will not read through`, named by path, not `ALL CHECKS PASSED` silently | ✓ PASS |
| Dry-run preview parity (WR-01) | `DRY_RUN=1 node install/install.js --target <unchanged installed target> --yes` | 49 `skipped`, 0 `would-materialize` | ✓ PASS |
| Targeted write-bound regression | `npx vitest run install/install.test.ts -t "write bound"` | 7 passed | ✓ PASS |
| Full regression suite | `npx vitest run --exclude '**/scripts/e2e/**'` | `1 failed \| 54 passed (55)` files, `1 failed \| 2445 passed \| 2 skipped (2448)` tests | ✓ PASS (matches stated baseline exactly; sole failure is the pre-existing `scripts/frontmatter.test.ts` control) |
| `npm run freshness` / `npm run check:build-parity` | build-output and build-parity gates | both green | ✓ PASS |
| `node scripts/check-foundation-guards.js` | kit's own zero-config adapters unaffected | `KIT-03: 17 roles == 17 adapters`, ALL CHECKS PASSED | ✓ PASS |
| `node scripts/check-banned-claims.js` | no banned claim introduced | 0 findings/117 elements | ✓ PASS |
| `node scripts/check-diff-disposition.js` | every changed clause dispositioned | 0 findings/37 elements | ✓ PASS |
| `npm run check:audit-register` | audit register complete | ALL CHECKS PASSED | ✓ PASS |
| `node scripts/check-nul-bytes.js` | no forbidden control bytes | 0/1731 files | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| MODEL-01 | 01, 02, 03, 04, 05, 06 | `models` block resolves per role, lean default `inherit`, byte-identical zero-config adapters | ✓ SATISFIED | Zero-config adapters unaffected (foundation-guards KIT-03 green); tiered config reaches installed target (reproduced this session). |
| MODEL-02 | 01, 02, 03 | Generator emits the resolved `model:` line; upstream doc updated | ✓ SATISFIED | Confirmed live rendering, unchanged since initial verification. |
| MODEL-03 | 01, 02, 03, 04 | Named opt-in preset, derived role→tier map, and (04) the destination-hazard guard closing the delivery-path data-loss defect | ✓ SATISFIED | `tiered` preset resolved and delivered live; guard reproduced closing CR-01. |
| MODEL-04 | 01, 02, 03, 04 | Only 4 legal aliases; full model ids refused; write loop cannot copy/link raw mirror bytes | ✓ SATISFIED | No full model id observed; WR-04 hard refusal confirmed in code and by structural test. |
| MODEL-05 | 01, 02, 03 | Unknown/malformed/absent fails closed to `inherit`; guard asserts emitted model equals resolved config | ✓ SATISFIED | Illegal preset refused the whole class (reproduced); foundation-guards green. |

REQUIREMENTS.md still carries no `Phase 29.2` row (MODEL-01…05 remain attributed to "Phase 29.1 / Complete"), consistent with CONTEXT.md's framing that this phase completes delivery without re-opening any requirement. No orphaned requirement found.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No `TBD`/`FIXME`/`XXX` debt markers in any file modified by plans 04-06 | — | Clean — checked `install/install.ts`, `install/install.test.ts`, `agent-factory/config/factory.config.md`, `scripts/model-dial-consistency.test.ts`, `CHANGELOG.md`, `install/README.md`, `scripts/model-tiers.ts`. |
| `.planning/WINDOWS.md` | 113 | Transient unreproduced full-suite failure (`2 failed \| 2442 passed`, one file beyond baseline, never identified) | ℹ️ Info | `unrun-verify`, disclosed honestly; this session's own full-suite run matched the 1-failed baseline exactly, giving no evidence the transient issue is still live. Not re-opened as a gap per the plan's own scope decision. |
| `.planning/WINDOWS.md` | 114 | Sibling-prefix containment trap (`.../target-backup` beside `.../target`) covered by adversarial probe only, not a CI regression case | ℹ️ Info | Disclosed, non-blocking; the segment-wise (not `startsWith`) compare in `adapterDestHazard()` was read and confirmed correct in source (`install.ts:529-540`). |
| `install/install.ts` | 1691-area (row 111) | `EISDIR`/`EACCES` destination still throws uncaught mid-loop | ℹ️ Info | Pre-existing, explicitly out of scope for this phase (deferred-items.md row 2 / WINDOWS row 111); re-probed this round per plan 06 and confirmed unchanged (`EISDIR` throw reproduced, exit 1, stack trace). Not re-opened as a gap. |

### Human Verification Required

1. **Windows behaviour of the destination-hazard guard (leaf/ancestor/dangling symlink refusal, plus a directory junction).**
   **Test:** On a Windows box or a privileged Windows CI runner, plant the same three shapes (leaf symlink, ancestor-directory symlink, dangling symlink) against a target adapter path and re-run `node install/install.js --target <repo>`; separately probe a directory junction (`mklink /J`) as the `.claude/agents` ancestor.
   **Expected:** The same refusal behavior observed on POSIX in this session — exit 3, the sentinel/outside-directory bytes unchanged, the finding names the path — or, if a junction is not caught by either guard arm, an explicit new finding documenting that gap.
   **Why human:** `install/install.test.ts`'s 5 symlink-creating write-bound cases open with a `process.platform === "win32"` early return (creating a symlink on Windows needs `SeCreateSymbolicLink`, which an unprivileged CI runner lacks) — independently confirmed this session (7 cases total, 5 skip on win32). `realpathSync`'s treatment of Windows junctions is a second, distinct, currently-untested question. `.planning/WINDOWS.md` row 115 already records this honestly as `UNKNOWN - verify`; this verification confirms that disclosure is accurate but cannot supply the missing Windows evidence itself.

2. **General render-mirror spawn/path handling on Windows (carried forward, unaffected by this round).**
   **Test:** On a Windows box, `node install/install.js --target <repo>` with a `models` block; confirm 17 adapters render and `--check` reports no stale files.
   **Expected:** Mirror spawn, temp-tree cleanup and path handling behave the same as on macOS/Linux.
   **Why human:** No Windows runner exercised this in the initial verification session or this one; already carried in `29.2-VALIDATION.md` as `UNKNOWN - verify` (rows 105-107).

### Gaps Summary

**The CR-01 gap from the initial verification is closed and independently confirmed in this session,
not merely trusted from the gap-closure plans' own SUMMARY.md or REVIEW.md follow-up.** Three
adversarial shapes were reproduced fresh, from scratch fixtures built in this verification session,
against the committed `install/install.js`:

- A leaf symlink at a target adapter path pointing at an outside sentinel file is refused by name,
  exit 3; the sentinel's bytes are untouched.
- An ancestor-directory symlink (`.claude/agents` replaced by a symlink to an outside directory) is
  refused for all 17 adapters, exit 3; the outside directory was confirmed empty afterward — this
  is the shape a leaf-only guard would have missed, and the guard's own comments in `install.ts`
  document exactly why the check resolves both `TARGET` and `dest`'s directory before comparing.
- A dangling symlink (pointing at a path that does not exist) is refused by name, exit 3; no file
  was created at the dangling target — closing the "strict sharpening" this phase's plan 04
  identified (the earlier fix pattern of `existsSync(dest) && isSymlink(dest)` would have missed
  this shape; the shipped code correctly uses `lstat`-based `isSymlink` alone).

The `--check` doctor's mirrored blind spot is also closed: over a target whose adapters are
symlinks, it now prints a named `WARN adapter staleness: ... sit at destinations this run will not
read through` rather than a silent `ALL CHECKS PASSED`, and `--strict` promotes that WARN to exit 1.

All five of REVIEW.md's warnings (WR-01 through WR-05) and all four info findings (IN-01 through
IN-04) were independently re-checked against the tree this session — source line reads for the
code-level fixes, a live reproduction for the observable ones (WR-01's dry-run parity) — and all
hold as claimed. The disposition table in `deferred-items.md` claiming "ten findings, ten
dispositions, none silent" was independently cross-checked: the denominator (10) was re-derived from
`29.2-REVIEW.md`'s own headings, and all six distinct commit hashes cited as closing evidence were
confirmed to exist on the tree with matching commit-message subjects.

The full regression suite was run once this session and matches the stated baseline exactly
(`1 failed | 2445 passed | 2 skipped`, the sole failure being the same pre-existing
`scripts/frontmatter.test.ts` control refusal already present before this phase began) — no
regression was introduced by the gap-closure round.

**The phase is not marked `passed`, because one honest, disclosed limitation remains open and is
outside the reach of this verification session to close:** the new destination-hazard guard's
behaviour on Windows is genuinely unverified — not merely undocumented. `.planning/WINDOWS.md` row
115 (added by plan 06) already states this as `UNKNOWN - verify` rather than claiming a pass, which
is the correct and honest disclosure per this project's own standing rules; this verification's job
is to surface that unresolved item to a human decision-maker rather than either paper over it with a
`passed` verdict or treat an honestly-disclosed unknown as a blocking `gaps_found` defect. No other
gap remains.

**Recommended next step:** either (a) accept the disclosed Windows unknown as a known limitation for
this phase's closure — consistent with the project's broader documented Windows-verification debt —
and record that acceptance, or (b) run the symlink/junction probes on an actual Windows environment
(a privileged CI runner or a Windows box) before closing the phase, given the safety-critical nature
of the guard being verified.

---

*Verified: 2026-09-04T22:10:00Z*
*Verifier: Claude (gsd-verifier)*

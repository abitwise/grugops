---
phase: 29.2-model-assignment-delivery-path
plan: 06
type: execute
wave: 3
depends_on: ["29.2-04", "29.2-05"]
files_modified:
  - .planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/deferred-items.md
  - .planning/WINDOWS.md
  - .planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every finding in 29.2-REVIEW.md — CR-01, WR-01, WR-02, WR-03, WR-04, WR-05, IN-01, IN-02, IN-03, IN-04 — has a recorded disposition on the tree: a commit that closed it, or a written deferral with a rationale and a ledger row. Ten findings, ten dispositions, none silent."
    - "Anything this round could NOT observe is recorded as unobserved rather than as passing: the new symlink cases cannot run on the Windows CI leg for want of SeCreateSymbolicLink, so the containment guard's Windows behaviour is `UNKNOWN - verify` and says so."
    - "The phase's exit criterion is stated honestly as the measured baseline or better — at most the pre-existing `scripts/frontmatter.test.ts` failure and at least 2436 passing — never as a green suite."
    - "The gate block that closed plans 01-03 is re-run in full against the tree this round produced, and its observed results are recorded from the run rather than asserted."
    - "The delivery contract D-07 and D-15 documented is re-confirmed end to end after the guard landed: the ordinary re-run still delivers a `models` edit, D-11's visible no-op still holds, and D-09's staleness verdict still names stale adapters."
  artifacts:
    - "deferred-items.md — a disposition table covering all ten REVIEW.md findings"
    - ".planning/WINDOWS.md — new ledger row(s) for the genuinely unobserved and the genuinely deferred, continuing from row 112"
    - "29.2-VALIDATION.md — rows for plans 04, 05 and 06 filled from the commands that actually ran"
  key_links:
    - "REVIEW.md finding id → the commit that closed it, or → the deferred-items.md row and its WINDOWS ledger row: every id resolves to one of the two, and the count of ids equals the count of dispositions."
    - "the new symlink cases → the win32 early return → the WINDOWS ledger row: the skip and the record of the skip move together, so a POSIX-only proof is never read as cross-platform."
---

<objective>
Close the round honestly.

Two things end a gap-closure round in this repository: every finding has a disposition someone can
read on the tree, and the gate block that certified the phase runs again against what the round
actually produced. Neither is a claim; both are records of commands that ran.

**Purpose:** the phase's own standing rule is that a green suite is not proof of a safety invariant,
and its corollary is the one this task serves — an unobserved condition must be recorded as
unobserved. The new guard's cases cannot run on the Windows leg, so what this round proves is
POSIX-scoped and the ledger has to say so rather than letting a green CI badge imply otherwise.

**Output:** a ten-row disposition table, the ledger rows the round earned, filled validation rows,
and a recorded gate run.

Decisions this plan touches: D-07, D-09, D-11, D-15.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-REVIEW.md
@.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-VERIFICATION.md
@.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/deferred-items.md
@.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ten findings, ten dispositions — and a ledger row for what this round could not see</name>
  <files>.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/deferred-items.md, .planning/WINDOWS.md, .planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-VALIDATION.md</files>

  <read_first>
    - `29.2-REVIEW.md` — the full finding list: CR-01, WR-01 … WR-05, IN-01 … IN-04. This is the
      denominator; derive it by reading the headings rather than trusting this plan's copy of it.
    - `.planning/phases/29.2-…/deferred-items.md` — the existing three-row deferral table and the
      "Dispositioned, will not fix" table. Rows 1, 2 and 3 (ledger rows 110, 111, 112) and the
      `readRenderedAlias` disposition are NOT re-opened; this task appends beside them.
    - `.planning/WINDOWS.md:16-17` — the ledger's column header, and rows 105-112 for the id
      sequence, the `kind` vocabulary (`deviation`, `unrun-verify`) and the tone of an accepted-open
      entry.
    - The plan 04 Task 3 summary — the adversarial re-run table, in case it surfaced a shape that
      was named and routed here rather than fixed.
    - `install/install.test.ts` — the `process.platform === "win32"` early returns the new cases
      carry, which are the reason a Windows row is owed.
  </read_first>

  <precondition>
    Plans 04 and 05 are committed. This task records what those commits did; running it before they
    land would produce a disposition table describing work that does not exist.
  </precondition>

  <action>
    **Derive the denominator, do not restate it.** Read the finding ids out of `29.2-REVIEW.md`'s
    headings and count them. Assert that count against the number of rows you write. This
    repository's second named systemic failure class is a hand-maintained set that rots while
    staying green; a disposition table copied from this plan instead of derived from the review is
    exactly that shape.

    **Append a disposition table to `deferred-items.md`**, under a new heading naming this round and
    its date. One row per finding id, with columns: finding, where, disposition, evidence. The
    disposition is one of `fixed` (with the plan and task that fixed it) or `deferred` (with a
    one-line rationale and a ledger row id). Expected shape based on plans 04 and 05 — confirm each
    against the tree rather than transcribing:

      CR-01 fixed (04-T1, 04-T2) · WR-01 fixed (05-T1) · WR-02 fixed (05-T3) ·
      WR-03 fixed (05-T2) · WR-04 fixed (04-T3) · WR-05 fixed (05-T2) ·
      IN-01 fixed (05-T2) · IN-02 fixed (05-T3) · IN-03 fixed (04-T2) · IN-04 fixed (05-T3)

    If any of those did not in fact land, the row says `deferred` with its rationale and gets a
    ledger row — the table records the tree, not the plan's intention.

    **Add the ledger rows this round earned**, continuing the id sequence from 112. At minimum:

      - kind `unrun-verify`, file `install/install.ts`: the destination-hazard guard's symlink
        cases carry a `win32` early return because creating a symlink on Windows needs
        SeCreateSymbolicLink, which an unprivileged CI runner does not hold. The leaf, ancestor,
        dangling and union shapes are therefore proven on the POSIX legs only. State the specific
        additional Windows unknown the containment arm introduces: `realpathSync` resolves drive
        letters, UNC paths and directory junctions, and a junction is not a symbolic link to
        `lstat`, so whether the ancestor arm refuses a junctioned agents directory is
        `UNKNOWN - verify`. Remedy: read the `windows-latest` leg, and probe a junction there
        before treating the containment bound as cross-platform.

    Add a second row only if plan 04 Task 3's adversarial round surfaced something real that was
    routed here. Do not manufacture a row to look thorough; an empty second row is a false record.

    **Do not touch rows 110, 111 and 112.** Row 111 (the EISDIR/EACCES write shape) stays open and
    unchanged — plan 04 Task 2 recorded in source which shapes the new guard does and does not
    reach, and that comment is the cross-reference, not a re-opening. If the row's description is
    now incomplete because the guard changed what reaches `writeFileSync`, ANNOTATE it with a dated
    clause rather than rewriting it, per the annotate-never-rewrite rule that governs records.

    **Fill `29.2-VALIDATION.md`.** Add rows to the Per-Task Verification Map for every task in plans
    04, 05 and 06, each carrying the automated command that ACTUALLY RAN and its observed status.
    Repeat the file's existing caveat in the same voice: a green row means the command ran and
    exited 0, and is not a claim that the surface is proven. Leave the Manual-Only Verifications
    Windows row open and add the junction probe to it.
  </action>

  <acceptance_criteria>
    - `deferred-items.md` contains a table whose row count equals the number of finding headings in
      `29.2-REVIEW.md`, and the summary records both numbers as measured, not assumed.
    - Every finding id from `29.2-REVIEW.md` appears exactly once in that table.
    - Every row whose disposition is `deferred` names a `.planning/WINDOWS.md` row id.
    - `.planning/WINDOWS.md` contains at least one new row with an id greater than 112, whose `file`
      column is `install/install.ts` and whose description contains `SeCreateSymbolicLink` and
      `UNKNOWN - verify`.
    - Rows 110, 111 and 112 of `.planning/WINDOWS.md` retain their original text; any addition to
      row 111 is an appended dated clause, not a replacement.
    - `29.2-VALIDATION.md` carries one Per-Task Verification Map row per task in plans 04, 05 and 06.
    - `node scripts/check-nul-bytes.js` exits 0 — the markdown written here must not put a backslash
      sequence inside a double-quoted YAML scalar, which the phase's own validation note warns about.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/check-nul-bytes.js &amp;&amp; node scripts/check-audit-register.js &amp;&amp; node scripts/check-diff-disposition.js</automated>
    <fails_when>non-zero exit, or any guard names `deferred-items.md`, `.planning/WINDOWS.md` or `29.2-VALIDATION.md` in a finding line</fails_when>
    <automated>npx vitest run scripts/frontmatter.test.ts</automated>
    <fails_when>the failure count rises above the single pre-existing D-49 / WINDOWS-row-91 refusal — this suite's corpus is every tracked markdown file, so it is the control that says the records written by this task did not break markdown parsing</fails_when>
  </verify>

  <reversibility rating="reversible">
    Records only; no code path changes and every row is a documented append.
  </reversibility>

  <done>
    Every REVIEW.md finding resolves to a commit or to a deferral with a rationale and a ledger row,
    and what this round could not observe on Windows is written down as unobserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run the proven gate block and record what it actually said</name>
  <files>.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-06-SUMMARY.md</files>

  <read_first>
    - `29.2-VALIDATION.md` — the measured pre-change baseline and the stated exit criterion:
      baseline or better, not green. The single pre-existing failure is
      `scripts/frontmatter.test.ts` (D-49, WINDOWS row 91) and is not owned by this phase.
    - `29.2-VERIFICATION.md` "Behavioral Spot-Checks" — the seven behaviours reproduced live when
      the phase was verified. Rows 1-5 must still hold after the guard; this task re-runs them.
    - `.github/workflows/ci.yml` — the gate block, to confirm the command list below still matches
      what CI runs rather than trusting this plan's copy of it.
    - The plan 03 summary's gate-block record, for the form this task's record should take.
  </read_first>

  <precondition>
    Task 1 is committed and the working tree is clean. `git diff --exit-code --name-only -- '*.js'`
    must exit 0 before the block is run, or the parity result measures an uncommitted build rather
    than the tree.
  </precondition>

  <action>
    **Re-confirm the delivery contract itself, not only the guard.** The risk of a safety guard is
    that it refuses something legitimate. Before the gate block, re-run the five behaviours
    VERIFICATION.md reproduced live, on a scratch target outside the repository, driving the built
    `install/install.js`:

      1. fresh install with `{"models":{"preset":"tiered"}}` renders 4 `opus` + 13 `sonnet` and the
         resolution line names the target's config path (D-01, D-04)
      2. an identical re-run reports `skipped (identical copy present)` for all 17 (D-11)
      3. after a config edit, `--check` names all 17 stale adapters at exit 0, and `--check --strict`
         exits 1 (D-09, D-10)
      4. an unknown preset installs no adapter, leaves the other classes complete, and exits 3 (D-03)
      5. a hand-edited `model:` line is reverted on the next re-run and reported by name (D-13)

    Record the observed output for each. Any of these that the guard broke is a defect this task
    fixes or escalates — it is not a residual.

    **Then the gate block**, exactly as plan 03 ran it, one command line so a single non-zero halts it:

      `npm run freshness && npm run freshness:catalog && npm run freshness:context && npm run freshness:adapters && npm run freshness:skill-twins && node scripts/check-foundation-guards.js && node scripts/check-kit-refs.js && node scripts/check-public-docs-vocabulary.js && node scripts/check-audit-register.js && node scripts/check-claim-anchors.js && node scripts/check-banned-claims.js && node scripts/check-imperative-lexicon.js && node scripts/check-diff-disposition.js && node scripts/check-nul-bytes.js && npm run check:build-parity && npm run typecheck`

    Then the suite, separately, so its result is recorded as a count rather than folded into an
    exit code:

      `npx vitest run --exclude '**/scripts/e2e/**'`

    NEVER `npm test` — it pulls in the live claude-CLI e2e lane, which spends tokens and can hang.

    **Record the numbers as measured.** Write the observed failed/passed/skipped counts and the file
    counts into the summary. The exit criterion is the measured baseline or better: at most the one
    pre-existing `scripts/frontmatter.test.ts` failure, and a passing count at or above 2436. If the
    count is higher, say by how much and what added it. If a NEW failure appears, it is this round's
    and is fixed here — do not record it as pre-existing and do not adjust the baseline to absorb it.

    **State the scope of what was proven.** In the summary, write one short paragraph saying plainly
    what this round establishes and what it does not: the four destination shapes were re-attacked
    against the built artifact on POSIX and no byte reached outside the target; the Windows leg
    cannot run those cases; a green suite remains a floor and not a proof.

    Finally, assert the supply-chain mitigation rather than assuming it: confirm the package manifest
    and lockfile diff across this whole round is empty, and record that as the evidence for
    T-29.2-SC.
  </action>

  <acceptance_criteria>
    - The summary records, for each of the five delivery behaviours, the command run and the
      observed output — quoted, not paraphrased.
    - Behaviour 2 records 17 `skipped (identical copy present)` lines, proving the guard did not
      break D-11's visible no-op.
    - Behaviour 3 records `--check` exit 0 with all 17 named, and `--check --strict` exit 1.
    - Behaviour 4 records exit 3 with no `.claude/agents` directory created.
    - The summary records the gate block's per-command outcome and the suite's observed
      failed/passed/skipped counts.
    - The recorded passing count is at or above 2436, and the recorded failure count is at most 1
      with that failure being `scripts/frontmatter.test.ts`.
    - The summary contains the scope paragraph stating what was NOT proven, including the Windows
      leg.
    - `git diff --name-only <round-base>..HEAD -- package.json package-lock.json` produces no output,
      and that is recorded as the T-29.2-SC evidence.
  </acceptance_criteria>

  <verify>
    <automated>npm run freshness &amp;&amp; npm run freshness:catalog &amp;&amp; npm run freshness:context &amp;&amp; npm run freshness:adapters &amp;&amp; npm run freshness:skill-twins &amp;&amp; node scripts/check-foundation-guards.js &amp;&amp; node scripts/check-kit-refs.js &amp;&amp; node scripts/check-public-docs-vocabulary.js &amp;&amp; node scripts/check-audit-register.js &amp;&amp; node scripts/check-claim-anchors.js &amp;&amp; node scripts/check-banned-claims.js &amp;&amp; node scripts/check-imperative-lexicon.js &amp;&amp; node scripts/check-diff-disposition.js &amp;&amp; node scripts/check-nul-bytes.js &amp;&amp; npm run check:build-parity &amp;&amp; npm run typecheck</automated>
    <fails_when>any command in the chain exits non-zero, or `check:build-parity` prints "BUILD PARITY FAILED", or `freshness:adapters` reports a non-zero byte difference, or `check-foundation-guards` does not report 17 roles matched to 17 adapters</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>more than one test file fails, or the single failing file is anything other than `scripts/frontmatter.test.ts`, or the passing count is below 2436</fails_when>
    <automated>git diff --name-only --exit-code -- package.json package-lock.json</automated>
    <fails_when>non-zero exit, or either filename is printed — a dependency changed in a round that declared none</fails_when>
  </verify>

  <reversibility rating="reversible">
    A measurement task; it writes records only.
  </reversibility>

  <done>
    The five delivery behaviours still hold against the guarded build, the gate block ran with its
    per-command outcome written down, the suite's counts are recorded against the measured baseline
    rather than described as green, and the round's scope — including what Windows could not show —
    is stated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this round's records → whoever verifies the phase next | The disposition table and the ledger are the only evidence a later reader has about what was decided and what was merely not looked at. |
| CI's green badge → the claim "proven" | A suite that cannot run a case on a platform must not let that platform's silence read as a pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29.2-34 | Repudiation | the disposition table | medium | mitigate | The row count is derived from the review's own headings and asserted against it, so a dropped finding is loud rather than absent (Task 1). |
| T-29.2-35 | Repudiation | POSIX-only proof of the destination guard | high | mitigate | A `unrun-verify` ledger row states the Windows scope, names `SeCreateSymbolicLink`, and adds the directory-junction unknown the containment arm introduces (Task 1). |
| T-29.2-36 | Denial of service | the new guard against legitimate installs | high | mitigate | The five documented delivery behaviours are re-run against the guarded build before the phase closes; a guard that refuses a legitimate install is caught here, not by a user (Task 2). |
| T-29.2-SC | Tampering | npm / pip / cargo installs | high | mitigate | The package manifest and lockfile diff across the round is asserted empty and recorded as evidence, rather than assumed because no install was intended (Task 2). |
</threat_model>

<verification>
- The disposition table's row count equals the review's finding count, both measured.
- `.planning/WINDOWS.md` carries the round's `unrun-verify` row; rows 110-112 are unchanged except
  for any dated annotation.
- The gate block exits 0 end to end and the suite is at the measured baseline or better.
- The five delivery behaviours are re-recorded from live runs against the guarded build.
</verification>

<success_criteria>
Every finding this phase's review raised resolves to a commit or to a written deferral with a ledger
row; the guard did not break the delivery path it protects; and the round's records state what was
proven, on which platform, and what was not looked at.
</success_criteria>

## Artifacts this phase produces

This plan writes records only — no symbol, no test, no production source.

| Artifact | Shape | Notes |
|---|---|---|
| `deferred-items.md` — new round heading + disposition table | one row per `29.2-REVIEW.md` finding id; columns finding / where / disposition / evidence | row count DERIVED from the review's headings and asserted against it, never transcribed from this plan |
| `.planning/WINDOWS.md` — row 113 (at minimum) | `kind: unrun-verify`, `file: install/install.ts` | names `SeCreateSymbolicLink`, carries `UNKNOWN - verify`, and states the directory-junction unknown the containment arm introduces |
| `.planning/WINDOWS.md` — row 111 | annotated, never rewritten | a dated clause only, if the plan-04 guard changed which shapes still reach `writeFileSync` |
| `29.2-VALIDATION.md` — Per-Task Verification Map rows | one per task in plans 04, 05, 06 | each carries the command that actually ran and its observed status, with the file's existing "green means the command ran, not that the surface is proven" caveat repeated |
| `29.2-06-SUMMARY.md` — the delivery re-confirmation record | five behaviours, quoted output | D-01, D-04, D-11, D-09, D-10, D-03, D-13 |
| `29.2-06-SUMMARY.md` — the gate-block record | per-command outcome + suite counts | recorded as measured against the baseline, never described as green |
| `29.2-06-SUMMARY.md` — the scope paragraph | prose | states what this round proved, on which platform, and what was not looked at |

**Rows explicitly NOT re-opened:** deferred-items rows 1, 2 and 3 (ledger 110, 111, 112) and the
`readRenderedAlias` "will not fix" disposition. They are cross-referenced, not revisited.

<output>
Create `.planning/phases/29.2-model-assignment-delivery-path-inserted-split-out-of-29-1-by/29.2-06-SUMMARY.md` when done.
</output>

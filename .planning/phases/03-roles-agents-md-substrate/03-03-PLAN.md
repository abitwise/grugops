---
phase: 03-roles-agents-md-substrate
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - agent-factory/roles/ba-pm.md
  - agent-factory/roles/system-analyst.md
autonomous: true
requirements: [ROLE-01]

must_haves:
  truths:
    - "ba-pm.md reproduces the §5.A.5 caveman prompt verbatim and emits product-handoff.md + epics/features/tickets"
    - "system-analyst.md reproduces the §5.A.6 caveman prompt verbatim and emits system-handoff.md"
    - "ba-pm.md states Backlog → Ready (owns Ready exit); system-analyst.md states the In Analysis exit"
    - "both carry kind: role + tier: core, read config first, append to traceability, and point at AGENTS.md for the 12 rules"
  artifacts:
    - path: "agent-factory/roles/ba-pm.md"
      provides: "BA/PM role — product clarity, epics/features/tickets (ROLE-01)"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/system-analyst.md"
      provides: "System Analyst role — flows/states/edge cases (ROLE-01)"
      contains: "## Caveman prompt"
  key_links:
    - from: "agent-factory/roles/ba-pm.md"
      to: "agent-factory/handoffs/product-handoff.md"
      via: "Output handoff (real on-disk path, not plans/-prefixed)"
      pattern: "agent-factory/handoffs/product-handoff\\.md"
    - from: "agent-factory/roles/system-analyst.md"
      to: "agent-factory/handoffs/system-handoff.md"
      via: "Output handoff (real on-disk path)"
      pattern: "agent-factory/handoffs/system-handoff\\.md"
---

<objective>
Author the product/analysis front-end core roles: `ba-pm.md` (product clarity → epics/features/tickets + product handoff) and `system-analyst.md` (flows/actors/states/edge cases → system handoff). Each is the §5 9-section skeleton: verbatim caveman prompt + terse frozen-path connective tissue (D-15). Both emit a REAL on-disk handoff under `agent-factory/handoffs/` — never the spec's `plans/`-prefixed path (the HIGH-impact drift, D-15).

Purpose: ROLE-01 (2 of the 11 core roles).
Output: 2 files under `agent-factory/roles/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md
@.planning/phases/03-roles-agents-md-substrate/03-PATTERNS.md
@.planning/phases/03-roles-agents-md-substrate/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ba-pm.md</name>
  <files>agent-factory/roles/ba-pm.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **5. BA/PM (L427–431)**; Roles Inventory row 5 (Output = `agent-factory/handoffs/product-handoff.md` + `plans/epics/*` `plans/features/*` `plans/tickets/*`; Board transition = `Backlog → Ready`, owns the Ready exit, sizes+prioritizes at refinement; Activates when = "Need product clarity").
    - Spec §5.A.5 (L427–431).
    - `03-PATTERNS.md` → frontmatter/heading house style (analog `agent-factory/handoffs/implementation-handoff.md` for the dual-section pattern); Board-move row `BA/PM → Backlog → Ready`; read_first row `ba-pm`. NOTE the Drift section: cite `agent-factory/handoffs/product-handoff.md`, NOT `plans/product-handoff.md`.
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `memory-bank/00-index.md`, `AGENTS.md`, `agent-factory/handoffs/product-handoff.md`, `plans/epics/` `plans/features/` `plans/tickets/`.
  </read_first>
  <action>
    Create `agent-factory/roles/ba-pm.md`, frontmatter `kind: role` + `tier: core`, 9-section skeleton (parentheticals kept). `## Caveman prompt` = L427–431 VERBATIM. `## Activates when`: "Need product clarity". `## Output (file + format)`: `agent-factory/handoffs/product-handoff.md` plus tickets/epics/features written to `plans/epics/`, `plans/features/`, `plans/tickets/` — cite the REAL on-disk handoff path (no `plans/`-prefixed drift). `## Board moves`: `Backlog → Ready` (owns the `Ready` exit; sizes + prioritizes at refinement). `## Reads`/`## Trace updates`: D-17 universal lines. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reflect "cut scope / protect MVP / say no to bloat" from the prompt. ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/ba-pm.md && grep -q 'tier: core' agent-factory/roles/ba-pm.md && grep -q 'agent-factory/handoffs/product-handoff.md' agent-factory/roles/ba-pm.md && grep -q 'factory.config.json' agent-factory/roles/ba-pm.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/ba-pm.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: core`; three D-17 lines present.
    - `## Output` cites `agent-factory/handoffs/product-handoff.md` (REAL path) + `plans/epics|features|tickets`; drift guard `grep -l 'plans/.*-handoff'` empty.
    - `## Board moves` states `Backlog → Ready`; one-line 12-rules pointer present, rule text NOT restated.
    - Manual-only: caveman prompt byte-exact vs spec L427–431.
  </acceptance_criteria>
  <done>ba-pm.md exists, 9 sections, verbatim prompt, emits the real product handoff + plans dirs, owns Backlog → Ready.</done>
</task>

<task type="auto">
  <name>Task 2: Author system-analyst.md</name>
  <files>agent-factory/roles/system-analyst.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **6. System Analyst (L439–442)**; Roles Inventory row 6 (Output = `agent-factory/handoffs/system-handoff.md`; Board transition = `In Analysis` exit → design readiness; Activates when = "Need flows or system rules").
    - Spec §5.A.6 (L439–442).
    - `03-PATTERNS.md` → house style; Board-move row `System Analyst → In Analysis exit`; read_first row `system-analyst`. Cite `agent-factory/handoffs/system-handoff.md` (no `plans/` drift).
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `memory-bank/00-index.md`, `AGENTS.md`, `agent-factory/handoffs/system-handoff.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/system-analyst.md`, frontmatter `kind: role` + `tier: core`, 9-section skeleton. `## Caveman prompt` = L439–442 VERBATIM. `## Activates when`: "Need flows or system rules". `## Output (file + format)`: `agent-factory/handoffs/system-handoff.md` (real path). `## Board moves`: the `In Analysis` exit (moves into design readiness). `## Reads`/`## Trace updates`: D-17 universal lines. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reflect the prompt's "do not choose framework / do not code". ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/system-analyst.md && grep -q 'tier: core' agent-factory/roles/system-analyst.md && grep -q 'agent-factory/handoffs/system-handoff.md' agent-factory/roles/system-analyst.md && grep -q 'plans/traceability.md' agent-factory/roles/system-analyst.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/system-analyst.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: core`; three D-17 lines present.
    - `## Output` cites `agent-factory/handoffs/system-handoff.md`; drift guard empty.
    - `## Board moves` states the `In Analysis` exit; one-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L439–442.
  </acceptance_criteria>
  <done>system-analyst.md exists, 9 sections, verbatim prompt, emits the real system handoff, owns the In Analysis exit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored role text → future executing agent | Role text is the behavior the agent later acts on; a fabricated handoff path or restated/drifted rule propagates to runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-Tamper | Tampering | handoff path citations | mitigate | Cite the REAL on-disk `agent-factory/handoffs/<name>.md` paths, never the spec's `plans/`-prefixed drift (D-15); drift guard enforces. |
| T-03-Info | Info-integrity | 12-rules single-source | mitigate | One-line pointer to `AGENTS.md`; no rule text restated (D-19). |
</threat_model>

<verification>
- Each file passes its 9-section + `tier: core` + three D-17 universal-line greps.
- Drift guard empty across both files (handoffs cite `agent-factory/handoffs/`).
- `check-structure.sh` per-file checks pass for ba-pm.md and system-analyst.md.
</verification>

<success_criteria>
- ROLE-01 advanced by 2 core roles; both emit real on-disk handoffs and state their owned board exits.
- No scope-fence violation: no `plans/`-prefixed handoffs, no fabricated commands, no restated rules.
</success_criteria>

<output>
Create `.planning/phases/03-roles-agents-md-substrate/03-03-SUMMARY.md` when done.
</output>

---
phase: 03-roles-agents-md-substrate
plan: 08
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06, 03-07]
files_modified:
  - AGENTS.md
autonomous: true
requirements: [AGENTS-01, AGENTS-02]

must_haves:
  truths:
    - "Root AGENTS.md follows the §17.1 shape (Mission / How to work here / Role·workflow·handoff files / Commands / Delivery / Safety rules / DoR·DoD / Memory bank & plans / When uncertain)"
    - "Every Commands slot is UNKNOWN - verify — no fabricated commands"
    - "AGENTS.md stays under the 32 KiB Codex cap"
    - "The §17.1 Safety rules are reproduced verbatim, clear voice, including the human-gate and secrets/destructive-command lines"
    - "Karpathy's 12 rules (4 principles) appear verbatim, once, in AGENTS.md, in clear voice"
    - "AGENTS.md echoes the README read order: orchestrator.md → factory.config.json → plans/board.md"
  artifacts:
    - path: "AGENTS.md"
      provides: "The minimal §17.1 substrate that points at all 16 roles + the single-source 12 rules (AGENTS-01, AGENTS-02)"
      contains: "## Mission"
  key_links:
    - from: "AGENTS.md"
      to: "agent-factory/roles/orchestrator.md"
      via: "How to work here — all work starts at the orchestrator"
      pattern: "agent-factory/roles/orchestrator\\.md"
    - from: "AGENTS.md"
      to: "the 12 Karpathy rules"
      via: "single-source ownership (Scribe maintains)"
      pattern: "State assumptions explicitly"
---

<objective>
Author the root `AGENTS.md` — the minimal, high-signal §17.1 substrate that points at all 16 roles and carries the single-source copy of Karpathy's 12 coding rules. Authored LAST because it references every role filename and is owned by the AGENTS.md Scribe (Plan 02). This is the one file that, with the README, defines the read-order contract before any Phase-4 workflow sequences the roles.

This plan delivers two requirements:
- AGENTS-01: the §17.1 shape, minimal/high-signal, under 32 KiB, Commands section = the file-scoped slot table with flags, every value `UNKNOWN - verify` (D-18), safety rules verbatim.
- AGENTS-02: Karpathy's 12 rules (4 principles) reproduced verbatim, once, in clear voice (D-19) — the Scribe owns them; the 15 other roles already point here.

Purpose: AGENTS-01, AGENTS-02. After this, the substrate + read-order contract exist on disk.
Output: `AGENTS.md` (repo root).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md
@.planning/phases/03-roles-agents-md-substrate/03-PATTERNS.md
@.planning/phases/03-roles-agents-md-substrate/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the §17.1 substrate shape + Commands slots + Safety rules (AGENTS-01)</name>
  <files>AGENTS.md</files>
  <read_first>
    - `.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md` → "§17.1 Root AGENTS.md Skeleton (AGENTS-01 / D-18)" (the verbatim skeleton L1453–1496 — reproduce the 9 `## ` headings and the verbatim Safety rules block); "Commands Slot Table (AGENTS-01 / D-18)" (the 12 slots — ship every value `UNKNOWN - verify`, the Example column is illustrative ONLY, never shipped); the "Section-name reconciliation" + "Size discipline" notes; and Open Question 1 (the 12-rules placement — recommended a minimal `## Coding rules (the 12)` section adjacent to Safety rules).
    - Spec `docs/initial/agent_factory_builder_spec_v2.md` §17.1 (L1447–1496) — the authoritative skeleton + the verbatim Safety rules ("Do not read or expose secrets", "Do not run destructive commands", "Never merge a protected branch. Never deploy prod without human confirmation.", "Do not change dependencies without reason. No unrelated refactors. No fake results.").
    - `.planning/phases/03-roles-agents-md-substrate/03-PATTERNS.md` → the `AGENTS.md` section: Analog 1 `agent-factory/README.md` (pointer style + the "start here → orchestrator.md" read order at README lines ~13–23, and the workflow numbers at lines ~92–111 the Orchestrator references match); voice split (light grug wink permitted ONLY in `## Mission`; CLEAR voice everywhere else).
    - `agent-factory/README.md` (read once) — the exact read order to echo (orchestrator.md → `agent-factory/config/factory.config.json` → `plans/board.md`) and the "all work starts at orchestrator.md" rule, so substrate and README do not drift.
    - The 16 role filenames now on disk under `agent-factory/roles/` (the `## Role / workflow / handoff files` pointers point at the directories, not each file).
  </read_first>
  <action>
    Create `AGENTS.md` at the repo ROOT (NOT touching the existing `CLAUDE.md` — that is grugops's own dev file, a different artifact; the new `AGENTS.md` is the shipped generic substrate per D-04). Reproduce the §17.1 9-heading shape: `## Mission`, `## How to work here`, `## Role / workflow / handoff files`, `## Commands` (with the `### Install/Development/Test/Lint/Typecheck/Build/E2E` subheadings), `## Delivery`, `## Safety rules`, `## Definition of ready / done`, `## Memory bank & plans`, `## When uncertain`. `## How to work here` echoes the README read order exactly: `agent-factory/roles/orchestrator.md` → `agent-factory/config/factory.config.json` → `plans/board.md`, plus "all work starts with the Orchestrator". `## Role / workflow / handoff files`: point at `agent-factory/roles/`, `agent-factory/workflows/`, `agent-factory/handoffs/`, `agent-factory/checklists/`. `## Commands`: fold the 12-slot file-scoped table (Install / Dev / Build / Test-all / Test-single / Lint-all / Lint-single+autofix / Format-single / Typecheck-single / E2E / Docs / Clean) under the 7 §17.1 subheadings, preferring single-file variants, with EVERY value `UNKNOWN - verify` — do NOT ship the illustrative examples, do NOT fabricate any real `npm/npx/node` command (D-18; grugops's own future validator/installers are NOT special-cased — they stay `UNKNOWN - verify`). `## Delivery`: Board `plans/board.md`, Cadence+WIP `factory.config.json`, Traceability `plans/traceability.md`. `## Safety rules`: reproduce the §17.1 block VERBATIM, CLEAR voice (secrets / destructive commands / never-merge-protected-branch + never-deploy-prod-without-human-confirmation / deps+refactor+fake-results). `## Definition of ready / done`: link `agent-factory/checklists/definition-of-ready.md` + `definition-of-done.md` (+ enterprise DoD when `mode=enterprise`). `## Memory bank & plans`: explain `memory-bank/*` + `plans/*`. `## When uncertain`: the §17.1 "Stop. Write the open question or the assumption. Do not guess silently." Mission may carry a light grug wink (D-21); a placeholder/empty `## Coding rules (the 12)` heading adjacent to Safety rules is added here and FILLED in Task 2. Keep the whole file minimal/high-signal and well under 32 KiB; push detail into the files it points to.
  </action>
  <verify>
    <automated>test -f AGENTS.md && grep -q -e '## Mission' -e '## How to work here' -e '## Commands' -e '## Safety rules' -e '## When uncertain' AGENTS.md && grep -q 'agent-factory/roles/orchestrator.md' AGENTS.md && grep -q 'UNKNOWN - verify' AGENTS.md && grep -q 'Never deploy prod' AGENTS.md && [ "$(wc -c < AGENTS.md)" -lt 32768 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `AGENTS.md` exists at repo root; the existing `CLAUDE.md` is untouched.
    - All 9 §17.1 headings present; `## How to work here` echoes the exact README read order (orchestrator.md → factory.config.json → board.md).
    - `## Commands` every value is `UNKNOWN - verify`: `grep -c 'UNKNOWN - verify' AGENTS.md` ≥ the slot count; manual scan finds NO real `npm/npx/node/git` command.
    - `## Safety rules` reproduces the §17.1 block verbatim, CLEAR voice (secrets / destructive / human-gate / deps-refactor-fake-results).
    - `wc -c < AGENTS.md` < 32768.
    - Manual-only: the human-gate and secrets/destructive lines are not softened or grug-voiced (D-21); only `## Mission` may carry a light wink.
  </acceptance_criteria>
  <done>AGENTS.md exists with the full §17.1 shape, README-consistent read order, all-UNKNOWN Commands, verbatim clear-voice Safety rules, under 32 KiB, and an empty `## Coding rules (the 12)` slot ready for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Reproduce Karpathy's 12 rules verbatim, single-source, clear voice (AGENTS-02)</name>
  <files>AGENTS.md</files>
  <read_first>
    - `.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md` → "Karpathy's 12 Rules (AGENTS-02 / D-19)" — the verbatim 4 principles / 12 rules block (reproduce EXACTLY): Principle 1 Think Before Coding (rules 1–4), Principle 2 Simplicity First (rules 5–8 + heuristics), Principle 3 Surgical Changes (rules 9–12 + corollary), Principle 4 Goal-Driven Execution (prose framing). Plus the note that Principle 4 is prose (not numbered 13–16).
    - Source `.planning/research/AGENTS-MD-BEST-PRACTICES.md` (L7–34) — the authoritative verbatim text to diff against.
    - `.planning/phases/03-roles-agents-md-substrate/03-CONTEXT.md` → D-19 (single-source; Scribe owns; 15 roles inherit via a one-line pointer — already done in Plans 01–07) and D-21 (clear voice for the 12 rules).
  </read_first>
  <action>
    Fill the `## Coding rules (the 12)` section in `AGENTS.md` (the heading added in Task 1, adjacent to `## Safety rules`) with Karpathy's 4 principles and 12 rules reproduced VERBATIM from the best-practices file, in CLEAR voice (D-19/D-21): Principle 1 Think Before Coding (1 state assumptions / ask when uncertain, 2 present multiple interpretations, 3 push back when a simpler approach exists, 4 stop and name what's confusing), Principle 2 Simplicity First (5 only requested features, 6 no single-use abstractions, 7 no unrequested flexibility, 8 no impossible-scenario handling, + the heuristics), Principle 3 Surgical Changes (9 preserve adjacent code, 10 don't refactor working code, 11 match existing style, 12 flag don't delete dead code, + the corollary), Principle 4 Goal-Driven Execution (the prose framing — verifiable goals, brief plan with verification points, strong success criteria). Reproduce the principle headings + rule text exactly; do NOT paraphrase. This is the SINGLE source — confirm no role file restates the rule text (only AGENTS.md and the Scribe carry it; the Scribe may echo in grug voice in its own body). Keep AGENTS.md under 32 KiB after adding this section.
  </action>
  <verify>
    <automated>grep -q 'State assumptions explicitly' AGENTS.md && grep -q 'No single-use abstractions' AGENTS.md && grep -q "Flag, don't delete" AGENTS.md && grep -q -e 'Think Before Coding' -e 'Simplicity First' -e 'Surgical Changes' -e 'Goal-Driven Execution' AGENTS.md && [ "$(wc -c < AGENTS.md)" -lt 32768 ] && [ "$(grep -l 'single-use abstraction' agent-factory/roles/*.md | grep -v agents-md-scribe | wc -l)" -eq 0 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All four principle headings present (Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution) and a distinctive phrase per rule (`State assumptions explicitly`, `No single-use abstractions`, `Flag, don't delete`, etc.).
    - Single-source check: the 12 rules' text appears ONLY in `AGENTS.md` (and optionally the Scribe's body as a grug echo) — no other role restates them: `grep -l 'single-use abstraction' agent-factory/roles/*.md` returns at most `agents-md-scribe.md`.
    - `wc -c < AGENTS.md` < 32768 after adding the section.
    - Manual-only (VALIDATION.md): the 12-rules block diffs verbatim against `.planning/research/AGENTS-MD-BEST-PRACTICES.md` (no paraphrase); the rules are CLEAR voice, not grug (D-21).
  </acceptance_criteria>
  <done>AGENTS.md carries the verbatim 4-principles/12-rules block in clear voice as the single source; no non-Scribe role restates the rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENTS.md substrate → every host tool + future executing agent | AGENTS.md is the portable substrate every tool reads; a fabricated command or softened safety rule here propagates to every agent on every tool. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-Tamper | Tampering | AGENTS.md Commands section | mitigate | Every Commands slot ships `UNKNOWN - verify`; no real command fabricated (D-18). Safety rule "Do not run destructive commands" reproduced verbatim (RESEARCH Security Domain Tampering row). |
| T-03-EoP | Elevation of Privilege | AGENTS.md Safety rules | mitigate | Reproduce "Never merge a protected branch. Never deploy prod without human confirmation." verbatim, CLEAR voice — do not soften (RESEARCH Security Domain EoP row; spec §17.1). |
| T-03-Info | Info-integrity | 12-rules + safety voice | mitigate | The 12 rules + Safety rules are CLEAR voice (D-21); only `## Mission` may carry a light grug wink. |
| T-03-InfoDisc | Information Disclosure | AGENTS.md Safety rules | mitigate | Reproduce "Do not read or expose secrets" verbatim (RESEARCH Security Domain Info Disclosure row; §17.1). |
| T-03-SC | Tampering | n/a (no package installs this phase) | accept | This phase installs zero npm/pip/cargo packages; no Package Legitimacy Gate applies. AGENTS.md Commands ship `UNKNOWN - verify`. |
</threat_model>

<verification>
- `AGENTS.md` exists, passes the §17.1-headings + read-order + all-UNKNOWN-commands + 32 KiB greps and the verbatim 12-rules greps.
- Single-source check green: no non-Scribe role contains rule text.
- Run the full suite `sh .planning/phases/03-roles-agents-md-substrate/check-structure.sh` — now GREEN (all 16 roles 9/9 + AGENTS.md §17.1 + <32 KiB + all-UNKNOWN + 12 rules single-sourced + drift guard empty).
</verification>

<success_criteria>
- AGENTS-01 satisfied: §17.1 shape, minimal/high-signal, < 32 KiB, file-scoped Commands all `UNKNOWN - verify`, verbatim clear-voice Safety rules, README-consistent read order.
- AGENTS-02 satisfied: the 12 rules (4 principles) verbatim, once, clear voice; Scribe owns them; the 15 other roles point here (done in Plans 01–07).
- The phase-wide structural suite is green.
- No scope-fence violation: no fabricated commands, no softened safety, no grug voice on rules/safety, `CLAUDE.md` untouched.
</success_criteria>

<output>
Create `.planning/phases/03-roles-agents-md-substrate/03-08-SUMMARY.md` when done.
</output>

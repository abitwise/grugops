---
phase: 03-roles-agents-md-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/roles/orchestrator.md
  - .planning/phases/03-roles-agents-md-substrate/check-structure.sh
autonomous: true
requirements: [ROLE-01, ROLE-03]

must_haves:
  truths:
    - "orchestrator.md reproduces the §5.A.1 caveman prompt verbatim as its ## Caveman prompt"
    - "orchestrator.md carries the full 13-arrow routing matrix and the 15-item request-type classification list"
    - "orchestrator.md enforces WIP limits and the Definition of Ready gate before pulling work, and emits SPLIT_REQUIRED on XL"
    - "orchestrator.md carries the hard limit verbatim, clear voice: never merge a protected branch, never deploy prod"
    - "orchestrator.md names the Phase-4 workflow files (00-..13-) without inlining their steps"
    - "check-structure.sh runs the full structural/verbatim/size/no-fabrication/drift suite and is executable"
  artifacts:
    - path: "agent-factory/roles/orchestrator.md"
      provides: "The routing contract the other 15 roles slot into (ROLE-03)"
      contains: "## Caveman prompt"
    - path: ".planning/phases/03-roles-agents-md-substrate/check-structure.sh"
      provides: "The Wave-0 structural test harness (the acceptance criteria, runnable)"
      contains: "UNKNOWN - verify"
  key_links:
    - from: "agent-factory/roles/orchestrator.md"
      to: "agent-factory/config/factory.config.json"
      via: "Reads config first (D-17)"
      pattern: "factory\\.config\\.json"
    - from: "agent-factory/roles/orchestrator.md"
      to: "agent-factory/checklists/definition-of-ready.md"
      via: "Definition of Ready gate before pulling work"
      pattern: "definition-of-ready"
---

<objective>
Author `agent-factory/roles/orchestrator.md` — the routing contract the other 15 roles slot into — and ship the Wave-0 structural test harness `check-structure.sh` that encodes this phase's acceptance criteria as runnable shell.

The Orchestrator is authored FIRST per the locked build-order (D-20): it encodes the routing matrix, the 15-item request-type classification, the WIP-limit + Definition-of-Ready gate, XL-split (`SPLIT_REQUIRED`), the `# Orchestrator Decision` 10-section output, and the hard limits — and it NAMES the Phase-4 workflow files without inlining their steps. Role text stays dispatch-neutral (spawn-vs-sequential is a Phase-5 concern).

Purpose: ROLE-03. The routing contract must exist before any dependent role or workflow; the test harness must exist before any file is validated (Nyquist Wave-0 gap from VALIDATION.md).
Output: `agent-factory/roles/orchestrator.md`, `.planning/phases/03-roles-agents-md-substrate/check-structure.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md
@.planning/phases/03-roles-agents-md-substrate/03-PATTERNS.md
@.planning/phases/03-roles-agents-md-substrate/03-VALIDATION.md
@.planning/phases/03-roles-agents-md-substrate/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the Wave-0 structural test harness (check-structure.sh)</name>
  <files>.planning/phases/03-roles-agents-md-substrate/check-structure.sh</files>
  <read_first>
    - `.planning/phases/03-roles-agents-md-substrate/03-VALIDATION.md` — the Per-Task Verification Map (the exact grep/wc/test commands), Wave 0 Requirements, and Validation Sign-Off list. This IS the script's contents.
    - `.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md` → "Validation Architecture" → "Phase Requirements → Test Map" (the canonical abbreviated commands) and "Sampling Rate".
    - `plans/board.md` (read once) — confirm the 13 column names the Orchestrator's Board-moves check greps for.
  </read_first>
  <action>
    Create `.planning/phases/03-roles-agents-md-substrate/check-structure.sh` (POSIX `sh`, `set -eu`, executable via `chmod +x`) implementing the VALIDATION.md "Per-Task Verification Map" as the full suite. It MUST cover, with non-zero exit on any failure: (a) all 16 role files present and each containing the 9 §5 skeleton headings (`## One job`, `## Caveman prompt`, `## Reads`, `## Activates when`, `## Responsibilities`, `## Output`, `## Board moves`, `## Trace updates`, `## Hard limits`) — use `grep -L` so any printed path = a missing section; (b) frontmatter counts: `grep -l 'tier: core' agent-factory/roles/*.md | wc -l` == 11 and `tier: enterprise` == 5; (c) the three D-17 universal lines present in every role (`grep -L 'factory.config.json' …`, `grep -L 'plans/board.md' …`, `grep -L 'plans/traceability.md' …` all empty); (d) Orchestrator content: `SPLIT_REQUIRED`, `definition-of-ready`, `Never merge`, the 15 classification tokens, and the workflow-name presence `grep -c` for `00-`..`13-`; (e) AGENTS.md: exists, §17.1 headings present, `wc -c < AGENTS.md` < 32768, `grep -c 'UNKNOWN - verify' AGENTS.md` ≥ the slot count; (f) the drift guard `grep -l 'plans/.*-handoff' agent-factory/roles/*.md` MUST be empty; (g) the single-source 12-rules check (no non-Scribe role restates rule text). Apply grep-gate hygiene: for any `grep -c` count gate, filter comment/header lines first (`grep -v '^#'`) so prose cannot self-invalidate the count. The script reports each check PASS/FAIL and exits 1 if any FAIL. It runs from repo root. The script itself ships the literal string `UNKNOWN - verify` only inside a comment documenting the AGENTS.md check — do NOT fabricate any real command anywhere. NOTE: every check goes RED now (files do not exist yet) — that is expected; the script is the acceptance criteria, runnable the moment each file lands.
  </action>
  <verify>
    <automated>sh .planning/phases/03-roles-agents-md-substrate/check-structure.sh; test $? -ne 0 && echo "EXPECTED-RED (files absent) — harness wired"</automated>
  </verify>
  <acceptance_criteria>
    - `test -x .planning/phases/03-roles-agents-md-substrate/check-structure.sh` (executable).
    - The script references all 9 skeleton headings, both tier counts (11 core / 5 enterprise), the three D-17 lines, the Orchestrator content tokens, the AGENTS.md §17.1 + 32 KiB + UNKNOWN-verify checks, and the `plans/.*-handoff` drift guard — i.e. `grep -c -e 'One job' -e 'tier: core' -e 'tier: enterprise' -e 'factory.config.json' -e 'SPLIT_REQUIRED' -e '32768' -e 'UNKNOWN - verify' -e 'plans/.*-handoff' .planning/phases/03-roles-agents-md-substrate/check-structure.sh` returns ≥ 8 distinct hits.
    - Running the script now exits non-zero (EXPECTED-RED — role files + AGENTS.md absent). This is correct for a greenfield authoring phase.
    - Manual-only: the script never fabricates a real `npm/npx/node` command (the only `UNKNOWN - verify` occurrence is in a documenting comment).
  </acceptance_criteria>
  <done>check-structure.sh exists, is executable, encodes the full VALIDATION.md suite, and currently exits RED because the authored files do not yet exist.</done>
</task>

<task type="auto">
  <name>Task 2: Author orchestrator.md — the routing contract (ROLE-03)</name>
  <files>agent-factory/roles/orchestrator.md</files>
  <read_first>
    - `.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **1. Orchestrator (L332–343)** — reproduce that block VERBATIM into `## Caveman prompt`; and "The Orchestrator Contract (ROLE-03 / D-20)" section in full: the §5 9-section skeleton (L304–316), Responsibilities (L347–360 verbatim), the 15-item request-type classification list, the routing matrix (13 arrows, L364–378 verbatim), the `# Orchestrator Decision` 10-section output order (L380), the hard limits (L384–386 verbatim), the WIP/DoR gate, XL-split, and the "Phase-4 workflow filenames the Orchestrator references BY NAME" table (00-..13-).
    - Spec source to diff verbatim blocks against: `docs/initial/agent_factory_builder_spec_v2.md` §5.A.1 (caveman prompt L332–343; routing matrix L364–378; responsibilities L347–360; hard limits L384–386).
    - `.planning/phases/03-roles-agents-md-substrate/03-PATTERNS.md` → "All 16 role files" frontmatter + heading house style (mirror `agent-factory/handoffs/universal-handoff.md` shape) and "Board-move column names" row for Orchestrator.
    - Frozen paths to cite (test -e confirmed): `agent-factory/config/factory.config.json` (Reads first, D-17), `plans/board.md` (Board moves — Orchestrator owns `Ready for Dev → In Development` and `… → Done`, enforces WIP all columns), `plans/traceability.md` (Trace updates), `agent-factory/checklists/definition-of-ready.md` (DoR gate), `memory-bank/00-index.md` (read-on-start), `AGENTS.md` (12-rules pointer line).
    - `agent-factory/README.md` lines ~92–111 — the workflow numbers (#04/#05/#06/#07/#08/#09/#12) the Orchestrator's workflow-name references MUST match (drift lock).
  </read_first>
  <action>
    Create `agent-factory/roles/orchestrator.md` with frontmatter `kind: role` + `tier: core` (mirror the `universal-handoff.md` frontmatter SHAPE; value vocabulary fixed by D-16 — `core`, NOT `lean`) and the 9-section §5 skeleton in order, keeping the parentheticals on `## Output (file + format)`, `## Board moves (which column transitions this role causes)`, `## Trace updates (what it must record in plans/traceability.md)`. `## Caveman prompt` = the L332–343 block reproduced VERBATIM (grug voice). `## Reads`: config first (`agent-factory/config/factory.config.json`), then `plans/board.md`, open handoffs, `memory-bank/00-index.md` on start. `## Responsibilities`: the 9 verbatim responsibilities including the 15-item classification list and SPLIT_REQUIRED. Encode the full 13-arrow routing matrix (verbatim), the WIP-limit + Definition-of-Ready gate (cite `agent-factory/checklists/definition-of-ready.md`; refuse to pull past a WIP limit without a written reason), and XL-split emitting `SPLIT_REQUIRED`. `## Output (file + format)`: the inline `# Orchestrator Decision` with its 10 sections in order (Request type · Mode/Cadence/Autonomy · Activated agents · Why · Required inputs · Workflow · Board moves · Expected handoffs · Stop conditions · Next action). In the `Workflow` framing, NAME the Phase-4 workflow files (`00-bootstrap-greenfield` … `13-incident`) per the RESEARCH table — do NOT inline any step sequence (D-20). `## Board moves`: state the two exits it owns (`Ready for Dev → In Development`, `… → Done`) plus one line that it enforces WIP on all columns (per RESEARCH Open-Q2 recommendation). `## Trace updates`: append to `plans/traceability.md`. `## Hard limits`: reproduce VERBATIM in CLEAR voice — "Never merge to a protected branch. Never deploy to prod. Never exceed WIP without a written reason." Add the single-line 12-rules pointer ("Follow the 12 coding rules in `AGENTS.md`.") — do NOT restate the rules (D-19). Keep the whole file scannable (~one screen). Invent nothing beyond the frozen-path connective tissue (D-15). Do NOT add any dispatch/spawn-vs-sequential detail (Phase-5 concern, D-20).
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Reads' -e '## Activates when' -e '## Responsibilities' -e '## Output' -e '## Board moves' -e '## Trace updates' -e '## Hard limits' agent-factory/roles/orchestrator.md && grep -q 'SPLIT_REQUIRED' agent-factory/roles/orchestrator.md && grep -q 'Never merge' agent-factory/roles/orchestrator.md && grep -q 'definition-of-ready' agent-factory/roles/orchestrator.md && grep -q 'tier: core' agent-factory/roles/orchestrator.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/orchestrator.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings present (`grep -L` over the 9 prints nothing).
    - Frontmatter `kind: role` + `tier: core`.
    - Contains `SPLIT_REQUIRED`, `definition-of-ready`, the verbatim `Never merge to a protected branch. Never deploy to prod.` hard limit, all 15 classification tokens, all 13 routing arrows, and the 10 `# Orchestrator Decision` section names in order.
    - References Phase-4 workflow files by name: `grep -c -e '0[0-9]-' -e '1[0-3]-' agent-factory/roles/orchestrator.md` (after `grep -v '^#'` filtering) returns the workflow-name references; manual-only: NO numbered step-sequence body mirrors a workflow (D-20).
    - Drift guard: `grep -l 'plans/.*-handoff' agent-factory/roles/orchestrator.md` empty (Orchestrator emits no handoff file — inline `# Orchestrator Decision`).
    - The single 12-rules pointer line present; rule text NOT restated (`grep -L 'single-use abstraction' ...` style single-source check — only AGENTS.md/Scribe carry rule text).
    - Manual-only (VALIDATION.md): `## Caveman prompt` diffs byte-exact against spec L332–343; routing matrix matches §5.A.1 exactly; hard limit + 12-rules pointer are CLEAR voice (no grug phrasing on the safety line).
  </acceptance_criteria>
  <done>orchestrator.md exists with all 9 sections, the verbatim caveman prompt, the full routing contract, the WIP/DoR/XL-split gates, the 10-section decision output naming (not inlining) Phase-4 workflows, and the verbatim clear-voice hard limit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored role text → future executing agent | The Orchestrator text is the routing/guardrail an agent later acts on; weakened or fabricated guardrails propagate to runtime behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-EoP | Elevation of Privilege | orchestrator.md hard limits | mitigate | Reproduce the human-gate hard limit VERBATIM, clear voice — "Never merge to a protected branch. Never deploy to prod." — never soften or paraphrase (RESEARCH Security Domain, EoP row; spec L384–386). |
| T-03-Tamper | Tampering | check-structure.sh / Orchestrator workflow refs | mitigate | The harness scans for fabricated commands and the drift guard; the Orchestrator names workflow files (no fabricated step bodies). No real command is invented (D-18). |
| T-03-Info | Info-integrity | hard-limit + DoR-gate voice | mitigate | Safety/hard-limit lines in CLEAR voice (D-21); grug voice confined to the `## Caveman prompt` block. |
</threat_model>

<verification>
- `sh .planning/phases/03-roles-agents-md-substrate/check-structure.sh` exits RED now (files absent) and turns green as Waves 2–3 land — it is the phase's running acceptance gate.
- `agent-factory/roles/orchestrator.md` passes the 9-section + frontmatter + content-token greps above.
- Drift guard empty for orchestrator.md.
</verification>

<success_criteria>
- ROLE-03 satisfied: the Orchestrator encodes the full routing matrix + 15-item classification + WIP/DoR gate + XL-split (`SPLIT_REQUIRED`) + the 10-section `# Orchestrator Decision` output + the verbatim clear-voice hard limit, and names (does not inline) the Phase-4 workflows.
- The Wave-0 structural harness exists, is executable, and encodes every VALIDATION.md check.
- No scope-fence violation: no workflow step sequences, no dispatch detail, no fabricated commands.
</success_criteria>

<output>
Create `.planning/phases/03-roles-agents-md-substrate/03-01-SUMMARY.md` when done.
</output>

---
phase: 29-controlled-language-voice-guard-rebuild
plan: 54
type: execute
wave: 51
depends_on: ["29-53"]
gap_closure: true
files_modified:
  - scripts/kit-model.ts
  - scripts/kit-model.js
  - scripts/kit-model.test.ts
  - scripts/generate-catalog.ts
  - scripts/generate-catalog.js
  - scripts/generate-catalog.test.ts
autonomous: true
requirements: [LANG-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The generator's corpus contract is declared once and imported by both the generator and the case that holds it. A hand-copied duplicate of a pattern is the named failure mode — a hand-maintained literal that rots while green — reappearing one level down, inside its own remedy, and the two can now disagree with the case keeping the old contract and staying green."
    - "A uniqueness property the generator's ORDERING depends on is enforced where the ordering happens, not asserted from a one-off measurement in a comment beside it. A standing property asserted from a point-in-time measurement is the same shape as the cardinality plan 29-46 deleted, one screen below the sentence saying so."
    - "The gate lands at the POINT OF EFFECT. A duplicate order would make the catalog's publication order depend on directory-read order, so the generator refuses; the case proves the generator refuses, rather than being the only thing that notices."
    - "The LANG-07 closure round 5 verified and round 6 regression-checked is still intact after this round touches the file that carries it. The probe row this round could not resolve is the assumption that round 7 leaves that mechanism alone, and this is the one plan where that assumption is falsifiable."
  artifacts:
    - "scripts/kit-model.ts — the numbered-workflow-file contract exported beside the workflow count, as the single declaration both the generator and its case consume"
    - "scripts/generate-catalog.ts — the local duplicate deleted and the shared contract imported; a named refusal on a duplicate workflow order, replacing the comment that asserted uniqueness from a measurement"
    - "scripts/generate-catalog.test.ts — the second hand-copied duplicate deleted and the shared contract imported; a RED-proven case for the duplicate-order refusal"
    - "the LANG-07 regression transcript, re-taken after this plan's edits, with both checks quoted"
  key_links:
    - "the generator's file-matching contract -> the case that holds the corpus cardinality -> the corpus itself — three surfaces, and until now two independent declarations of the rule"
    - "a workflow's declared order -> the catalog's publication order -> a reader's view of the kit — the seam a duplicate order makes non-deterministic in principle"
    - "this plan's edits to the catalog generator -> the frontmatter authority LANG-07 closed -> the flagged assumption plan 29-48 could not resolve"
  prohibitions:
    - statement: "The frontmatter authority is not touched. This is the module whose LANG-07 closure round 5 verified and round 6 regression-checked, and an unrelated edit inside it is an unrequested risk to a verified result; the two regression checks are re-run and quoted as the proof rather than the edit's nature being taken as the argument."
      status: enforced
      verification: "`grep -c \"function parseFrontmatter\" scripts/generate-catalog.ts` returns 0 and `npm run freshness:catalog` reports the catalog fresh; both transcripts are quoted after this plan's final commit."
    - statement: "No generated artifact changes bytes. A contract extraction and a refusal that does not fire on the live tree must leave the catalog, the seventeen adapters and the seven skill twins byte-identical, proven by regeneration and an explicit diff rather than inferred."
      status: enforced
      verification: "`npm run generate:catalog` followed by `git diff --exit-code docs/catalog/README.md` exits 0; all three freshness gates exit 0; all three transcripts are quoted."
    - statement: "A stale number is not replaced by a fresher number in the same place. Where the uniqueness comment asserted a measurement, the measurement is replaced by a mechanism and the sentence names the mechanism instead of restating a count."
      status: enforced
      verification: "The removed comment and its replacement are quoted; the after-form carries no cardinality for the workflow corpus and names the refusal that now holds the property."
---

<objective>
Close the two round-6 info findings whose subject is a rule declared twice, and a property asserted from a
measurement.

`IN-01` — plan 29-46 correctly deleted stale range prose from the catalog generator on the ground that *the
regex IS the contract and it is RANGE-FREE*, and added a case deriving the on-disk corpus. The case derives
it with a **hand-copied duplicate of that regex, typed in both files**, because the generator is a top-level
script that executes on import and exports nothing. The two can now disagree: widen the generator's pattern
and the case keeps the old contract and stays green as long as the file set happens not to change. That is
the named failure mode — a hand-maintained literal that rots while green — reappearing one level down,
inside its own remedy. The fix names itself: extract the pattern into the kit model beside the workflow
count, and have both sides import it.

`IN-02` — the same plan that says *the remedy for a stale number is to delete it, never to type a fresher
one in the same place* leaves, one screen below, a comment asserting that workflow `order` values are unique
and citing a round-5 re-verification as the evidence. Nothing in the repository enforces it: the round-6
review grepped the kit model, the structure validator and the foundation guards and found no duplicate
check. So a standing property is asserted from a one-off measurement, which is the same shape as the count
that was just deleted. The round-6 roll-up carries it as an open, fail-open residual with a live count of
zero duplicates today.

This plan also carries the round's one unresolved probe row to the only place it can be falsified. Plan
29-48 recorded LANG-07 / `unclassified` as a flagged assumption: that round 7 leaves LANG-07's mechanism
alone. This is the plan that edits the file carrying that mechanism, so this is where the assumption is
tested rather than restated.

Purpose: this repository's second documented systemic failure class is a hand-maintained literal that rots
while a suite stays green. Both findings are that class, and one of them is sitting inside the remedy for
the other.

Output: one declaration of the corpus contract, a refusal where the ordering happens, and a re-taken LANG-07
regression transcript.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW-round6.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-46-SUMMARY.md
@docs/audit/29-round6-residuals.md
</context>

<standing_rules>
The house rules of this phase, encoded rather than advised. The five that bite hardest here:

- **Derive the set, assert the count.** A rule declared in two places is two declarations; only one of them
  can be the contract, and the other can only rot.
- **Move the gate to the point of effect.** A property the ordering depends on is enforced where the
  ordering happens, not only in a case that could be deleted with the property surviving in prose.
- **A stale number is deleted, never replaced by a fresher one in the same place.** Where a count is
  genuinely useful, the prose names the constant or the mechanism that holds it.
- **RED first.** The refusal is watched failing on a constructed duplicate before it is trusted, and the
  extraction is proven by mutating the shared declaration and watching both consumers move together.
- **Build facts (D-13).** `npm run build` then `npm run freshness` after every `.ts` edit; the committed
  `.js` twin lands in the same commit. Regression is `npx vitest run --exclude '**/scripts/e2e/**'`, never a
  bare `npm test` (that lane spawns the live claude CLI, spends tokens and can hang).
</standing_rules>

<gap_contract_map>
| round-6 finding | discharged by |
|---|---|
| IN-01 — the hand-copied duplicate of the generator's contract regex | Task 1 |
| IN-01 — "extract the pattern into `scripts/kit-model.ts` beside `WORKFLOW_COUNT` and have both sides import it" | Task 1 |
| IN-02 — the unenforced uniqueness claim with a point-in-time measurement typed beside it | Task 2 |
| 29-46 **R2** (round-6 roll-up §4.1) — nothing reds if two workflows declare the same order; fail-open, 0 live | Task 2, closed with its closing measurement |
| probe row LANG-07 / `unclassified` — carried as a flagged assumption by plan 29-48 | Task 3: the assumption tested where it is falsifiable |
</gap_contract_map>

<tasks>

<task type="tracer">
  <name>Task 1: one declaration of the numbered-workflow contract, imported by both sides</name>

  <precondition>
    Plan 29-53 is committed, `npm run freshness` exits 0, and all three generated-artifact freshness gates
    exit 0 on HEAD. Capture `docs/catalog/README.md`'s content hash before any edit: this task must leave it
    byte-identical, and a baseline taken after the edit proves nothing. Halt and report if any gate does not
    exit 0.
  </precondition>

  <read_first>
    - `scripts/generate-catalog.ts:270-290` — the workflow directory read, the file-matching pattern and its
      surrounding comment stating that the pattern IS the contract and is range-free
    - `scripts/generate-catalog.test.ts:200-230` — the corpus-cardinality case in full, including the
      hand-copied pattern, both floors and the comment explaining why the count is held there
    - `scripts/kit-model.ts:100-180` — the exported role and workflow counts and the walk bound, and how each
      is declared and documented, so the new export sits in that idiom rather than beside it
    - `scripts/kit-model.ts` — the workflow listing functions, to confirm whether a numbered-file rule
      already exists in this module under another name; if it does, that is the declaration to reuse and this
      task becomes a reuse rather than an addition, and the SUMMARY must say so
    - `scripts/kit-model.test.ts` — the case idiom for this module, so the extraction's own case sits beside
      the existing ones
    - `29-REVIEW-round6.md` § IN-01 — the disagreement scenario and the named fix
  </read_first>

  <files>scripts/kit-model.ts, scripts/kit-model.js, scripts/generate-catalog.ts, scripts/generate-catalog.js, scripts/generate-catalog.test.ts, scripts/kit-model.test.ts</files>

  <behavior>
    - The numbered-workflow-file contract is declared exactly once, in the kit model, beside the workflow
      count it constrains.
    - The generator imports it and declares no pattern of its own.
    - The generator's test imports the same declaration and declares no pattern of its own.
    - Widening the shared declaration moves BOTH consumers; that is proven by mutation rather than argued.
    - The generator's behaviour is unchanged: the same files are matched, in the same order, and the
      generated catalog is byte-identical.
    - The declaration carries a short paragraph saying what it is the contract for and why it lives in the
      model rather than in either consumer.
  </behavior>

  <action>
    Check for an existing declaration before adding one. The kit model already owns the workflow corpus's
    listing and its count; if it already expresses the numbered-file rule under another name, reuse that
    rather than adding a second export, and say so in the SUMMARY — adding a parallel declaration beside an
    existing one would be this finding committed a third time.

    Declare the contract once and document it where it lives. Give it a name that says what it decides, put
    it beside the workflow count so a reader meets the corpus's two constraints together, and write a short
    paragraph: this is the rule that decides which files in the workflow directory are part of the corpus;
    it is range-free on purpose; it lives here because two consumers ask it and a rule asked by two consumers
    must be declared by neither.

    Delete both duplicates. The generator imports and uses the shared declaration; its comment stops
    restating the rule and names where the rule lives. The generator's test does the same, and its comment
    stops explaining that the pattern is typed here. Keep both of the test's floors exactly as they are — the
    floors are correct and the finding is about the pattern, not about them.

    Prove the two now move together. Mutate the shared declaration in a scratch build so it admits a
    differently-shaped filename, and show that both the generator's matched set and the test's derived set
    change; then revert. Quote both transcripts. This is the property the finding says is absent today, so it
    is the property the evidence has to demonstrate — a suite that passes after the extraction proves only
    that the extraction compiles.

    Prove nothing generated moved. Rebuild, regenerate the catalog, diff it against the committed artifact,
    and run all three freshness gates. A contract extraction that changes a generated byte has changed the
    contract.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; npm run freshness &amp;&amp; npx tsc --noEmit &amp;&amp; npm run generate:catalog &amp;&amp; git diff --exit-code docs/catalog/README.md &amp;&amp; npm run freshness:catalog &amp;&amp; npm run freshness:adapters &amp;&amp; npm run freshness:skill-twins &amp;&amp; npx vitest run scripts/generate-catalog.test.ts scripts/kit-model.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>

  <acceptance_criteria>
    - The shared declaration's name, its full text and its paragraph are quoted; if an existing declaration
      was reused instead, that is stated with its name and location.
    - A grep for the numbered-file pattern across `scripts/*.ts` returns exactly one declaration site; the
      full output is quoted.
    - `scripts/generate-catalog.ts` and `scripts/generate-catalog.test.ts` each import the shared declaration
      and declare no pattern; both import lines are quoted.
    - The mutation proof is quoted: a scratch build with the shared declaration widened changes both the
      generator's matched set and the test's derived set; both transcripts, and the revert, are recorded.
    - Both floors in the corpus-cardinality case are shown byte-unchanged in `git diff`.
    - `npm run generate:catalog` followed by `git diff --exit-code docs/catalog/README.md` exits 0; the
      content hash matches the precondition baseline.
    - All three freshness gates exit 0; all three transcripts are quoted.
    - `npm run build`, `npm run freshness` and `npx tsc --noEmit` exit 0, with both committed `.js` twins
      staged in the same commit as their `.ts`.
  </acceptance_criteria>

  <reversibility rating="reversible">
    A declaration moved to a shared module with both consumers importing it, and the generated artifact
    proven byte-identical.
  </reversibility>

  <done>
    The rule that decides the workflow corpus is written once, and widening it visibly moves both the
    generator and the case that holds it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the ordering property enforced where the ordering happens</name>

  <read_first>
    - `scripts/generate-catalog.ts:355-370` — the two sorts and the uniqueness comment, including the clause
      citing a round-5 re-verification as the evidence for a standing property
    - `scripts/generate-catalog.ts` — where each workflow's `order` is parsed out of its document, so the
      refusal can name the file and the value rather than only the collision
    - `scripts/generate-catalog.ts:1-30` — the module's own statement that the remedy for a stale number is
      deletion rather than a fresher value in the same place, which is the sentence this finding sits one
      screen below
    - `docs/audit/29-round6-residuals.md` §4.1 row 29-46 **R2** — the residual as recorded, with its live
      count and its direction, so the closure is measured against what was written down
    - `scripts/generate-catalog.test.ts` — how the existing cases construct a fixture directory and assert a
      generator failure, so the RED proof uses the harness that is there
    - `29-REVIEW-round6.md` § IN-02 — the two options offered and the reason the comment cannot stand as it is
  </read_first>

  <files>scripts/generate-catalog.ts, scripts/generate-catalog.js, scripts/generate-catalog.test.ts</files>

  <behavior>
    - The generator refuses, by name, when two workflows declare the same `order`, naming both files and the
      shared value.
    - The refusal fires before the sort, so the catalog is never emitted from an ambiguous ordering.
    - The comment beside the sort stops asserting uniqueness from a measurement and names the refusal that
      now holds it.
    - The refusal is watched failing on a constructed duplicate before it is trusted.
    - The live tree is unaffected: the catalog is byte-identical and the generator still exits 0.
    - The residual is closed with a re-measurement rather than a transcription.
  </behavior>

  <action>
    Put the check where the ordering is decided. Before the workflow sort, collect the declared order values,
    detect any value appearing more than once, and fail with a message naming each colliding file and the
    shared value — and stating the consequence plainly: two workflows with one order make the catalog's
    publication order depend on the directory read order, so the generated document would differ between
    machines for no visible reason. Use the module's existing failure path rather than inventing a second
    one, so a duplicate order fails the same way a missing directory does.

    Replace the comment rather than correcting it. Delete the clause citing a one-off re-verification and the
    parenthetical asserting uniqueness. Write in its place a sentence naming the refusal above as what holds
    the property, with no count and no date: a standing property named by its mechanism cannot go stale, and
    a standing property asserted from a measurement can only go stale.

    Re-measure rather than transcribe. Derive the live order values from the workflow corpus, report the
    count and the number of distinct values, and state the residual's closing measurement in the register's
    idiom for plan 29-55: the id as recorded, the statement, the status after round 7, the re-measured live
    count and where the measurement lives. A residual closed by a mechanism still gets its closing
    measurement.

    Write the case RED first. Construct a fixture workflow directory in which two documents declare the same
    order, run the generator against it, and assert it exits non-zero naming both files and the value. Run
    the same fixture against a build made from the pre-change tree and record that it succeeds there and
    emits a document — that transcript is what makes the case a discrimination rather than a decoration.
    Quote both.

    Prove the live tree is untouched. Regenerate, diff the catalog against the committed artifact, and run
    all three freshness gates. A check that reds on correct text is a false-red generator, and this one must
    be silent on a corpus whose values are already distinct.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; npm run freshness &amp;&amp; npx tsc --noEmit &amp;&amp; npm run generate:catalog &amp;&amp; git diff --exit-code docs/catalog/README.md &amp;&amp; npm run freshness:catalog &amp;&amp; npx vitest run scripts/generate-catalog.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>

  <acceptance_criteria>
    - The refusal's full message is quoted, and it names each colliding file and the shared value.
    - The refusal is shown to fire before the sort; the ordering of the two statements is quoted.
    - The removed comment and its replacement are quoted; the after-form carries no count and no date and
      names the refusal.
    - The duplicate-order fixture reds on the post-change build with both filenames in the message, and
      succeeds on a pre-change build emitting a document; both transcripts are quoted.
    - The live re-measurement is quoted: the number of workflows declaring an order and the number of
      distinct values, with the command that produced both.
    - The closing register entry is drafted in the SUMMARY in the register's idiom, ready for plan 29-55.
    - `npm run generate:catalog` followed by `git diff --exit-code docs/catalog/README.md` exits 0, and all
      three freshness gates exit 0.
    - `npm run build`, `npm run freshness` and `npx tsc --noEmit` exit 0, with the committed `.js` staged in
      the same commit as its `.ts`.
  </acceptance_criteria>

  <reversibility rating="reversible">
    One refusal before a sort and one comment replaced, with the generated artifact proven byte-identical and
    the refusal proven silent on the live corpus.
  </reversibility>

  <done>
    A duplicate workflow order stops the generator and names both files, and the comment beside the sort
    names the mechanism instead of a measurement.
  </done>
</task>

<task type="auto">
  <name>Task 3: the LANG-07 assumption tested where it is falsifiable</name>

  <read_first>
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-48-PLAN.md` § flagged assumptions — the
      exact statement of the assumption this task tests, and the two checks named as its only verification
    - `29-VERIFICATION-round6.md` § Goal Achievement truth #4 — the round-6 regression evidence for LANG-07,
      which is the baseline this task re-takes
    - `scripts/generate-catalog.ts` as tasks 1 and 2 leave it — the frontmatter import and every site that
      reads a document's front matter, so the re-check is made against the file this plan actually produced
    - `scripts/frontmatter.ts` — the single frontmatter authority, read to confirm the generator still asks
      it rather than answering for itself
    - `29-CONTEXT.md` D-22, D-23, D-24 — the one-authority decisions LANG-07 encodes, so the statement in the
      SUMMARY is written against the decision rather than against a remembered summary of it
  </read_first>

  <files>scripts/generate-catalog.test.ts</files>

  <behavior>
    - The two LANG-07 regression checks are re-run against the tree this plan produced, and both are quoted.
    - The generator is shown to declare no local frontmatter grammar of any kind.
    - The assumption plan 29-48 flagged is answered explicitly: held, or falsified with the evidence.
    - A permanent case asserts the generator declares no frontmatter grammar, so the assumption stops needing
      to be re-taken by hand in a later round.
  </behavior>

  <action>
    Re-run the two checks and quote them. Take the count of local frontmatter parser declarations in the
    generator and run the catalog freshness gate, exactly as the round-6 verification did, against the tree
    this plan produced rather than against HEAD before it. Quote both commands and both outputs.

    Widen the check from one name to the property. A count of one specific function name is a check against a
    spelling; the property is that this module declares no frontmatter grammar at all. Enumerate every site
    in the generator that reads a document's leading metadata and show each one asks the shared authority.
    Report the enumeration as a table with line numbers. If a site answers for itself, that is a finding and
    the assumption is falsified — record it, escalate it, and do not repair LANG-07's mechanism inside a plan
    scoped to two info findings.

    Answer the probe row explicitly. Plan 29-48 carried LANG-07 / `unclassified` as a flagged assumption
    because this round performs no LANG-07 mechanism work. State the outcome in the SUMMARY in one paragraph:
    the assumption held or did not, what evidence was taken, and what the assumption's remaining reach is —
    this task tests one module, and LANG-07's authority is consumed by others, so the honest statement names
    what was checked and what was not.

    Add the permanent case so the next round inherits a mechanism. Assert, from the module's own text rather
    than from a typed list, that the generator declares no frontmatter grammar; RED-prove it by adding one in
    a scratch build. State the case's coverage bound plainly: it holds this module and says nothing about the
    authority's other consumers.
  </action>

  <verify>
    <automated>grep -c "function parseFrontmatter" scripts/generate-catalog.ts; npm run freshness:catalog &amp;&amp; npx vitest run scripts/generate-catalog.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "function parseFrontmatter" scripts/generate-catalog.ts` returns `0`; the command and output
      are quoted.
    - `npm run freshness:catalog` reports the catalog fresh; the transcript is quoted.
    - The enumeration of leading-metadata read sites is quoted as a table with line numbers, and each is shown
      to ask the shared authority.
    - The probe row is answered in one paragraph naming the outcome, the evidence and the remaining reach.
    - The permanent no-local-frontmatter-grammar case exists, derives the module's own text, is RED-proven by
      adding a grammar in a scratch build, and states its coverage bound; both transcripts are quoted.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0 with its file and test counts quoted, and all
      seven repository gates exit 0.
    - This plan's SUMMARY declares NO `requirements-completed:` entry for LANG-04. LANG-04's verdict belongs
      to round 7's verifier, and the automated marker acting on that field before verification is the process
      artifact plan 29-48 corrected.
  </acceptance_criteria>

  <done>
    The one probe row this round could not resolve has an answer taken from the tree, and the module that
    carries LANG-07's closure has a case that notices if a second grammar arrives.
  </done>
</task>

</tasks>

<artifacts_this_plan_produces>
| symbol | file | kind |
|---|---|---|
| the numbered-workflow-file contract | `scripts/kit-model.ts` (+ committed `.js`) | new export beside the workflow count — or a documented reuse of an existing declaration |
| the deleted duplicate in the generator | `scripts/generate-catalog.ts` (+ committed `.js`) | local pattern deleted, shared declaration imported |
| the deleted duplicate in the generator's test | `scripts/generate-catalog.test.ts` | local pattern deleted, shared declaration imported; both floors unchanged |
| the duplicate-order refusal | `scripts/generate-catalog.ts` | new named failure before the sort, naming both files and the shared value |
| the replaced uniqueness comment | `scripts/generate-catalog.ts` | prose naming the mechanism, carrying no count and no date |
| "both consumers move with the shared declaration" | `29-54-SUMMARY.md` | mutation transcript pair, proving the extraction rather than its compilation |
| "a duplicate workflow order stops the generator" | `scripts/generate-catalog.test.ts` | new permanent case, RED-proven against a pre-change build |
| "the generator declares no frontmatter grammar" | `scripts/generate-catalog.test.ts` | new permanent case, derived from the module's text, RED-proven, coverage bound stated |
| the closing register entry for the order residual | `29-54-SUMMARY.md` | drafted in the register's idiom for plan 29-55 |
| the LANG-07 assumption verdict | `29-54-SUMMARY.md` | the probe row plan 29-48 flagged, answered with its evidence and its remaining reach |

Values that MOVE rather than being created: none. Values that must NOT move: every byte of
`docs/catalog/README.md`, the seventeen adapters, the seven skill twins, and both floors in the
corpus-cardinality case.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a contract declared in two files -> the corpus each decides | Two declarations, one of which can only rot |
| a comment asserting a standing property -> the ordering that depends on it | A measurement standing in for a mechanism |
| this plan's edits -> the module carrying LANG-07's verified closure | A verified result touched by an unrelated change |
| a generated artifact -> its committed copy | A refactor that moves a byte has moved the contract |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-54-01 | Tampering | two declarations of the corpus contract drifting apart while green | high | mitigate | One declaration in the shared model, both consumers importing it, proven by mutating the declaration and watching both move (task 1) |
| T-29-54-02 | Repudiation | a standing property asserted from a one-off measurement | medium | mitigate | The property is enforced at the point of effect and the comment names the mechanism (task 2) |
| T-29-54-03 | Tampering | a duplicate order making the catalog's publication order machine-dependent | medium | mitigate | A named refusal before the sort, RED-proven on a constructed fixture (task 2) |
| T-29-54-04 | Spoofing | an unrelated edit disturbing the LANG-07 closure round 5 verified | high | mitigate | Both regression checks re-run and quoted, the read sites enumerated, and a permanent no-local-grammar case added (task 3) |
| T-29-54-05 | Tampering | a refactor silently moving a generated byte | high | mitigate | Regeneration plus explicit diff plus all three freshness gates, against a hash captured before any edit (tasks 1, 2) |
| T-29-54-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan; `package.json` and the lockfile are asserted byte-unchanged. RESEARCH.md carries no new package row for this round, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- The numbered-workflow contract is declared once and imported by both consumers, proven by mutation.
- A duplicate workflow order stops the generator by name, proven on a constructed fixture against both
  builds.
- The uniqueness comment names a mechanism and carries no measurement.
- The residual's closing measurement is re-taken and drafted for the register.
- The LANG-07 regression checks are re-run against this plan's tree, the read sites are enumerated, and the
  flagged assumption is answered with its remaining reach stated.
- The catalog, the adapters and the skill twins are byte-identical; all three freshness gates exit 0.
- Typecheck, the full non-e2e suite and all seven gates exit 0.
</verification>

<success_criteria>
- The rule that decides the workflow corpus exists once.
- The property the catalog's ordering depends on is held by the generator, not by a sentence.
- The round's one unresolved probe row has an answer taken from the tree rather than a restatement.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-54-SUMMARY.md` when done.
</output>

---
phase: 29-controlled-language-voice-guard-rebuild
plan: 30
type: execute
wave: 3
depends_on: ["29-28"]
gap_closure: true
files_modified:
  - scripts/check-audit-register.ts
  - scripts/check-audit-register.test.ts
  - scripts/check-diff-disposition.ts
  - scripts/check-diff-disposition.test.ts
autonomous: true
requirements: [LANG-03]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LANG-03 — a named safety-surface exclusion list is honoured so that load-bearing security, compliance, and admission text is never reworded by a style pass"
  artifacts:
    - "scripts/check-audit-register.ts — equality four: the REGISTRY arm of the D-18 union pinned in both directions, containment against two derivations plus a two-sided kind-column cardinality, with the count published"
    - "scripts/check-diff-disposition.ts — the union's markdown residue beyond the derived kit ASSERTED to equal the registry arm's derived members, replacing a sentence that described the residue without checking it"
    - "scripts/check-audit-register.test.ts and scripts/check-diff-disposition.test.ts — the one-cell flip harness case in both gates, the phantom-row case, and the BOTH-ARMS union probe"
  key_links:
    - "readRegistry kind: safety rows -> the registry arm -> safetySurfaceUnion -> the D-18 exclusion list — now pinned in both directions at the source"
    - "check-diff-disposition watchedCorpus residue <-> the registry arm's derived members — the two arms tied together at the consumer, not each checked alone"
---

<objective>
Close LANG-03's round-3 gap (29-REVIEW.md WR-06, with CR-02 supplying the other direction).

Round 2's three enumerated items all landed and were confirmed by direct read. The truth still fails
ONE ARM TO THE RIGHT. The D-18 union is `register rows flagged safety_surface: yes` UNION
`registry rows of kind: safety` (`generate-safety-surface.ts:73-98`). Round 2 pinned it twice and
**both pins land on the REGISTER arm**: equality three pins the register's flagged set two-sided, and
the consumer pins `derivedKit ⊆ corpus.watched` plus a derived count. Containment is one-directional
by design, so the REGISTRY arm — four members on the live tree, including `README.md` and `AGENTS.md`,
present by registry reason ALONE — is unpinned in BOTH directions.

Round 3 reproduced it end to end on a mirror: flip ONE `kind:` cell from `safety` to `architecture`,
and `README.md` leaves the union (41 -> 40) AND the watched corpus, while `check-audit-register` and
`check-claim-anchors` both exit 0 and the new consumer pin does not fire — verified by re-running its
own arithmetic, `0 of 36 derived kit files unwatched`, because `README.md` was never a kit file.

Purpose: this is round 3's own recorded probe — when a fix is scoped to the arm the reproduction used,
ask what the OTHER arm of the same union does. LANG-03 has now failed three rounds for three different
reasons, and this is the arm nobody pinned.

Output: both arms of the union pinned in both directions, the union's residue asserted rather than
described, and a probe that reds when the two arms move together.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-CONTEXT.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-28-SUMMARY.md
</context>

<round_3_standing_rules>
Identical to plan 29-27's block. The four that bite hardest here:

- **After splitting a predicate into arms, test their UNION.** This gap exists precisely because two
  pins covered one arm of a two-arm union. A plan that pins the second arm and stops has repeated the
  mistake one arm later.
- **Which SET does the predicate ENUMERATE?** The register arm enumerates the derived kit. The registry
  arm enumerates nothing today. That is the project's named set-literal-drift class.
- **Derive the set, assert the count, two-sided, planted-member probe.**
- **A green suite is not proof.** Round 3 reproduced this with all seven gates green. Every task
  carries a green-before / red-after reproduction on a hermetic mirror built with `git archive HEAD`.

**Assumption-delta decision this plan implements** (recorded in `29-27-PLAN.md`): the primary noun is
**the D-18 UNION**, not the derived kit; the derived kit is DEMOTED to one named arm of it. The
decision is `promote`, not `add-alongside` — bolting a registry check beside a kit-shaped floor is what
round 2 shipped.

Build facts: `npm run build` then `npm run freshness` after every `.ts` edit. Regression suite is
`npx vitest run --exclude '**/scripts/e2e/**'`, never bare `npm test`. Counting greps use `grep -a`.
</round_3_standing_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: equality four — the registry arm pinned in both directions, at the source</name>

  <read_first>
    - `scripts/check-audit-register.ts:225-295` — equality one, equality three and equality two, their
      shapes, their two-directions-as-two-defects rule, and the recorded reason equality three reuses
      `derived` rather than re-deriving the kit
    - `scripts/generate-safety-surface.ts:59-101` — `safetySurfaceUnion`, both arms, and the header's
      statement of why both are needed and why each entry records EVERY reason it is present
    - `scripts/check-public-docs-vocabulary.ts:291-352` — `PUBLIC_DOCS_SCAN_PARTS`, `publicDocsScan()`,
      `PUBLIC_DOCS_SCAN_COUNT` and the two-sided pin over them: a derived set with an asserted count,
      which is the derivation this arm can be measured against
    - `scripts/audit-model.ts` `readRegistry` as plan 29-28 leaves it, including its new parse tally
    - `scripts/kit-model.ts` — `ROLE_COUNT`, `WORKFLOW_COUNT`, `listRoles`, `listWorkflows`
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-CONTEXT.md` — D-18 (the union),
      D-25 (when a hand-maintained baseline is legitimate: it is a measurement, not a discovery set, and
      it fails closed), D-04 (the same-commit companion edit)
  </read_first>

  <files>scripts/check-audit-register.ts, scripts/check-audit-register.test.ts</files>

  <behavior>
    RED FIRST against the committed build, transcripts into the SUMMARY.

    - Flipping one `kind: safety` cell to another kind in a registry mirror is a NAMED refusal. At HEAD
      it exits 0.
    - Adding a `kind: safety` row naming a file outside every derivation is a NAMED refusal, reported
      as a DIFFERENT defect from the flip. Two acts, two remedies, two findings — equality two's own
      recorded rule about one number absorbing another's drift.
    - The live tree is unchanged: the gate still exits 0 and equality one, two and three still report
      what they reported.
    - The gate publishes the registry arm's size, so the arm's residue is a number a reader can check.
  </behavior>

  <action>
    Add **equality four** beside equality three, so the two arms of the D-18 union are pinned side by
    side in one gate — the source side, exactly where equality three's own comment says the equality
    belongs.

    Derive the registry arm directly from the source: `readRegistry(root).claims` filtered to
    `kind === "safety"`, mapped to `file`, de-duplicated and sorted. Derive it from the parser, never
    by string-sniffing `safetySurfaceUnion`'s rendered reason text — a rendered sentence is a
    presentation detail and a check that parses one is a second grammar over a third artifact.

    Pin it in two layers, because no single derivation closes both directions.

    **Layer one, the ADD direction, derived against derived.** Every `kind: safety` claim file must be
    a member of `publicDocsScan()` UNION the derived kit (`listRoles` + `listWorkflows` under their
    subpaths). A safety claim naming a file outside both derivations is a stray: it puts a file into
    the D-18 exclusion list that neither derivation can vouch for, which is equality three's
    "flagged but NOT derived" argument applied to the other arm. Refuse by name, listing the strays.
    This layer needs no hand-written list at all and it kills round 3's phantom-row route outright.
    Note at the site that `publicDocsScan()` is markdown-only while the registry arm legitimately
    carries at least one non-markdown member, and handle that explicitly rather than letting a
    non-markdown member fall through: state which derivation vouches for it, or refuse it by name.
    Measure this before writing the branch — round 3 measured `.claude-plugin/plugin.json`,
    `AGENTS.md`, `README.md` and `agent-factory/README.md` as the registry-only members; re-derive
    them rather than transcribing them, and let the measurement decide the branch.

    **Layer two, the REMOVE direction, a two-sided cardinality on the kind column.** Nothing in this
    repository independently derives WHICH claims are safety claims — that is an editorial
    classification — so the direction the review reproduced needs a measurement baseline. Declare a
    per-kind cardinality map beside the equality and assert it two-sided against the derived
    distribution, reporting each disagreeing kind by name. Add the floor that stops the baseline itself
    going short: the SUM of the declared map must equal the derived claim total, so a kind cannot be
    dropped from the map and take its rows with it. This is legitimate under D-25 — a per-column
    measurement baseline that fails CLOSED, not a discovery set — and it is legitimate under D-04,
    because a new or reclassified claim reds until the map is updated in the same commit. State both
    of those reasons at the declaration, and state the refused alternative: a hand-written list of
    protected files, which D-01 refuses outright.

    Word the refusals so the remedy that deletes the evidence is visibly not the remedy, in the style
    equality three already uses: lowering a count or narrowing the arm are the two ways to clear the
    finding by deleting what it measures, and neither is the fix.

    Publish the arm's size in the gate's PASS line (D-08 — the PASS line carries the measurement, never
    an assertion), beside the register arm's, so a reader sees both arms' numbers rather than one
    number and a sentence about the other.
  </action>

  <verify>
    <automated>npm run build && npx vitest run scripts/check-audit-register.test.ts && node scripts/check-audit-register.js; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - ADVERSARIAL SELF-REPRODUCTION, in the SUMMARY. On a hermetic mirror, reproduce round 3's exact
      WR-06 recipe: flip `C-28-001`'s `kind: safety` to `kind: architecture`, then run
      `safetySurfaceUnion(mirror)`, `node scripts/check-audit-register.js` and
      `node scripts/check-claim-anchors.js` under the PRE-task build and the POST-task build.
      Pre must reproduce round 3's numbers — the union drops from 41 to 40, `README.md` leaves it, and
      both gates exit 0. Post must exit 1 from `check-audit-register` with the kind-cardinality
      disagreement named. Both transcripts pasted. If the pre run does not reproduce 41 -> 40, say so
      and show the command; do not claim a bypass you could not demonstrate.
    - The two directions are reported as TWO defects with two distinct messages, shown by forcing each
      once: a flip (kind cardinality moves) and a stray (a `kind: safety` row outside both derivations).
      A conflated tally is refused by equality two's own recorded argument.
    - The declared kind map is proven falsifiable per member, not in aggregate: for EACH declared kind,
      perturb its count by one on a mirror and require the refusal to name that kind. A probe run on
      one kind proves one kind.
    - The sum floor is proven: delete one kind from the declared map on a mirror and require the sum
      check to red naming the shortfall. Without this the baseline can go short silently, which is the
      set-literal-drift class one level up.
    - `node scripts/check-audit-register.js` exits 0 on the live tree, and its PASS output carries the
      registry arm's size as a number. Paste the line.
    - The registry arm is derived from the parser, not from rendered text:
      `grep -a -c 'readRegistry' scripts/check-audit-register.ts` is at least 1, and
      `grep -a -v '^\s*[/*]' scripts/check-audit-register.ts | grep -ac 'home of safety claim'`
      returns 0.
    - `npm run build && npm run freshness` reports every committed `.js` fresh.
  </acceptance_criteria>

  <reversibility rating="costly">
    The kind-cardinality baseline becomes the record of which claims were safety claims when each
    subsequent language edit was judged. Removing it later means re-deriving that membership from git
    history — the same cost D-01 records for the frozen set. Mitigated by the sum floor and the
    per-member probe, which keep the baseline from silently going short while it stands.
  </reversibility>

  <done>
    The registry arm has an equality in both directions: a stray member is refused by a derived
    containment, a flipped cell is refused by a two-sided cardinality whose own baseline is
    sum-floored, both are reported as separate defects, and the arm's size is published.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the union's residue is asserted, not described</name>

  <read_first>
    - `scripts/check-diff-disposition.ts:1346-1382` — `WATCHED_CORPUS_MIN`, its recorded argument for
      importing the expectation from a module `watchedCorpus()` never calls, and `watchedCorpus()`
      itself
    - `scripts/check-diff-disposition.ts:1437-1500` — the round-2 containment pin, the count pin, and
      the paragraph explaining why containment was chosen over a bare floor
    - the gate's PASS line that prints `the union's remaining N markdown entr(ies) are public
      documents` — the sentence WR-06 names as a description of the residue rather than a check on it
    - `scripts/check-audit-register.ts` equality four, as Task 1 leaves it
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` § WR-06, final paragraph
  </read_first>

  <files>scripts/check-diff-disposition.ts, scripts/check-diff-disposition.test.ts</files>

  <behavior>
    - The markdown members of the watched corpus that are NOT derived-kit files must EQUAL the registry
      arm's derived markdown members. Set equality, both directions, reported as two defects.
    - Removing a registry-arm member narrows the watched corpus and is a RED here as well as at the
      source, so neither end can absorb the other's drift.
    - Adding a residue member that no registry claim vouches for is a RED.
    - The existing containment pin and count pin are KEPT. This is added beside them, not in place of
      them — the same posture the round-2 pin took toward the vacuity floor it sat next to.
    - The live tree still exits 0 and the corpus numbers are unmoved.
  </behavior>

  <action>
    Replace the descriptive residue sentence with an assertion that has a source. The gate currently
    prints how many markdown entries remain beyond the derived kit and calls them public documents;
    nothing checks that they are, and any number of them may be added or removed silently in either
    direction.

    Derive the registry arm here too — `readRegistry(ROOT).claims` filtered to `kind === "safety"`,
    markdown members only, sorted — and assert SET EQUALITY against `corpus.watched` minus
    `derivedKit`. Report the two directions as two defects with their own remedies: a residue member no
    safety claim vouches for, and a safety-claim markdown file missing from the watched corpus.

    This is the UNION test round 3's own probe asks for. The register arm is pinned at the source
    (equality three), the registry arm is pinned at the source (equality four), and this ties the two
    together AT THE CONSUMER, where the union is actually spent. A union checked one arm at a time is
    what round 2 shipped.

    Say at the site why the equality is legitimate here while `WATCHED_CORPUS_MIN` deliberately stayed
    a MINIMUM: the minimum exists because the union carries members beyond the derived kit, and this
    assertion is what those members now are. The residue is no longer unaccounted, so the one-directional
    floor and this equality together cover the whole set rather than the kit-shaped part of it.

    Keep both round-2 pins exactly as they are. Adopt the same refusal posture they use: name the
    members, name the remedy, and state that narrowing the corpus or lowering an expectation are the
    two routes that clear the finding by deleting its evidence and neither is the fix.

    Update the PASS line to publish both arms' contributions and the residue equality's own size, so
    the printed sentence and the assertion behind it are the same statement (D-08, and the AP-1 rule
    carried from Phase 28 that this gate's residue line is a live instance of).
  </action>

  <verify>
    <automated>npm run build && npx vitest run scripts/check-diff-disposition.test.ts && node scripts/check-diff-disposition.js; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - ADVERSARIAL SELF-REPRODUCTION, in the SUMMARY. On a hermetic mirror, run the SAME one-cell flip
      from Task 1 and show `check-diff-disposition` exiting 1 with the residue equality naming
      `README.md` as a safety-claim file missing from the watched corpus. Round 3's recorded evidence
      is that this gate's pin did NOT fire on that mirror (`kit files unwatched 0`); the closure
      condition is that it now does, from a different equality than the one at the source.
    - Both directions are shown once each with distinct messages: a residue member no claim vouches
      for, and a claim-vouched file absent from the corpus.
    - The two round-2 pins still exist and still fire. Re-run their own harness cases and paste the
      results: containment and `WATCHED_CORPUS_MIN` are additive to this change, never replaced by it.
    - `node scripts/check-diff-disposition.js` exits 0 on the live tree, and its PASS line publishes
      the register-arm size, the registry-arm size and the residue size. Paste the line, and check by
      hand that the three numbers reconcile against `safetySurfaceUnion().length` — a published number
      a reader cannot reconcile is the shape this task is deleting.
    - The gate no longer prints an unchecked claim about the residue:
      `node scripts/check-diff-disposition.js | grep -ac 'are public documents'` returns 0.
    - `npm run build && npm run freshness` reports every committed `.js` fresh.
  </acceptance_criteria>

  <reversibility rating="reversible">
    One derived set and one equality added beside two existing pins. Reverting restores the descriptive
    sentence and loses the check; nothing else moves.
  </reversibility>

  <done>
    The union's markdown residue is an assertion with a source. Both arms are tied together at the
    consumer, in both directions, with the two round-2 pins intact beside it and every published number
    reconcilable by hand.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the harness cases, including the BOTH-ARMS probe neither gate has ever seen</name>

  <read_first>
    - `scripts/check-audit-register.test.ts` and `scripts/check-diff-disposition.test.ts` — the existing
      round-2 harness cases that flip one `safety_surface` cell and require exit 1; these are the
      pattern to copy for the registry arm, as the verification's third `missing:` item says
      explicitly
    - the mirror helpers both test modules already use, so a new case builds its tree the same way
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION.md` — LANG-03's three
      enumerated `missing:` items, all three of which this plan and 29-28 must close between them
  </read_first>

  <files>scripts/check-audit-register.test.ts, scripts/check-diff-disposition.test.ts</files>

  <action>
    Land the permanent cases. Round 2 built exactly this harness for the register arm; this task builds
    its twin for the registry arm and then adds the case neither arm's harness could ever produce.

    FIRST, the twins. In BOTH gates: flip one `kind: safety` cell in a registry mirror and require exit
    1 with the finding naming the claim id and the file that left the union. Add a `kind: safety` row
    naming a file no derivation vouches for and require exit 1 with a DIFFERENT finding. Assert each
    case's own premise before its claim — that the mirror's pre-flip union really does contain the file
    the flip removes — because a case whose plant did not change anything passes for the wrong reason,
    which is the recorded first-draft failure at `check-foundation-guards.test.ts:5068`.

    SECOND, the BOTH-ARMS probe, which is the point of this task. Round 3's finding is that two pins
    covering one arm read as coverage. So plant a mirror in which ONE cell of EACH arm moves at the
    same time — one `safety_surface: yes` flipped to `no`, and one `kind: safety` flipped to another
    kind — and require that BOTH arms are named, as separate findings, in one run. A gate that reports
    only the first arm it meets would pass a single-arm case and still leave the second arm's drift
    invisible in exactly the arrangement a real narrowing edit takes. Assert the finding COUNT as well
    as the exit code, so an early return that stops at the first defect is a red rather than a pass.

    THIRD, the vacuity direction. Plant a registry mirror carrying ZERO `kind: safety` rows and require
    a named refusal rather than an empty-set pass. An arm that derives nothing satisfies every equality
    written over it vacuously — the argument `generate-safety-surface.ts` already makes for the empty
    union and `kit-model.ts` makes for `refuseEmpty()`. Reuse their wording rather than inventing a
    third.

    Write the three-way traceability table into `29-30-SUMMARY.md`: LANG-03's three enumerated
    `missing:` items mapped to file:line and to the case that pins each. Item 2 is closed by plan 29-28
    and must be named as such rather than re-implemented here — round 3's brief refuses two competing
    repairs of the same lines.
  </action>

  <verify>
    <automated>npm run build && npx vitest run scripts/check-audit-register.test.ts scripts/check-diff-disposition.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - The BOTH-ARMS probe exists, passes, and asserts the finding COUNT as well as the exit code. Show
      it FAILING against a deliberately early-returning variant of the gate, so the count assertion is
      proven to be doing work rather than riding along.
    - Every planted case asserts its own premise and each premise assertion is SHOWN failing once, by
      forcing the plant to be a no-op. A case that cannot tell a no-op plant from a real one is the
      vacuous-plant defect this repository has now recorded twice.
    - The zero-safety-row vacuity case produces a NAMED refusal, not an empty-set pass, in both gates.
    - LANG-03's three enumerated `missing:` items each map to a landed artifact in the SUMMARY table,
      with item 2 attributed to plan 29-28 and NOT re-implemented here.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0, with the pass count recorded beside round
      3's 1878 / 52-file baseline.
    - All seven repo gates exit 0 on the live tree: `check-foundation-guards`, `check-imperative-lexicon`,
      `check-diff-disposition`, `check-banned-claims`, `check-audit-register`, `check-claim-anchors`,
      `check-public-docs-vocabulary`. Paste the seven exit codes.
    - `node scripts/generate-safety-surface.js` leaves `docs/audit/28-safety-surface-exclusions.md`
      byte-unchanged (`git diff --stat` empty) — this plan pins the union, it does not move it.
    - `git status --porcelain` shows no source file modified by a reproduction; every plant was on a
      mirror.
  </acceptance_criteria>

  <reversibility rating="reversible">
    Test-only. Reverting loses coverage and nothing else.
  </reversibility>

  <done>
    Both arms of the D-18 union have a one-cell-flip harness case in both gates, a stray-member case, a
    vacuity case, and a probe that moves both arms at once and requires both to be named — the case the
    single-arm harnesses could never have produced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/audit/28-claim-registry.md` `kind` column -> the D-18 exclusion list | a single editorial cell decides whether a file may be reworded by a style pass |
| `docs/audit/28-disposition-register.md` `safety_surface` column -> the same list | the other arm of the same decision, already pinned |
| the exclusion list -> `guard_diff_disposition`'s left-hand side | the corpus the gate reports a verdict over; a narrower corpus is a quieter gate, not a failing one |
| both registers live under `docs/` | neither is itself a member of the corpus it derives, so the edit that narrows owes no disposition row anywhere — the asymmetry that made this invisible |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-30-01 | Tampering | one `kind: safety` cell flipped, dropping a file from the exclusion list with every gate green | critical | mitigate | Task 1 layer two — two-sided kind cardinality with a sum floor and a per-member probe; Task 2 — the same narrowing also reds at the consumer from a different equality |
| T-29-30-02 | Elevation of Privilege | a `kind: safety` row added for a file no derivation vouches for | high | mitigate | Task 1 layer one — containment against `publicDocsScan()` union the derived kit, derived against derived, no hand list |
| T-29-30-03 | Repudiation | the gate printing an unchecked sentence about the residue | high | mitigate | Task 2 — the residue becomes a set equality with a source; the unchecked sentence is removed and its removal is grepped |
| T-29-30-04 | Denial of Service | a vacuous registry arm satisfying every equality written over it | high | mitigate | Task 3 — the zero-safety-row case requires a named refusal in both gates, reusing the existing empty-union wording |
| T-29-30-05 | Tampering | a gate that reports only the first arm it meets, so a two-arm narrowing reads as one defect | medium | mitigate | Task 3 — the BOTH-ARMS probe asserts the finding COUNT, shown failing against an early-returning variant |
| T-29-30-06 | Spoofing | the kind baseline itself going short and taking its rows with it | high | mitigate | Task 1 — the declared map's SUM must equal the derived claim total, proven by a delete-a-kind probe |
| T-29-30-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed. Zero runtime dependencies is a CLAUDE.md constraint and this round adds none; recorded rather than skipped |

ASVS level 1, block threshold `high`. Every `critical` and `high` row is dispositioned `mitigate` with
a named task.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 29-30)

**`scripts/check-audit-register.ts`**
- **equality four** — the registry arm, pinned in two layers: a derived containment against
  `publicDocsScan()` union the derived kit (the ADD direction), and a two-sided per-kind cardinality
  baseline (the REMOVE direction), each reported as its own defect
- a declared per-kind cardinality map with its D-25 and D-04 justification at the declaration
- a SUM floor over that map, asserted against the derived claim total
- the registry arm's size published in the PASS line beside the register arm's

**`scripts/check-diff-disposition.ts`**
- the registry arm derived here too, from `readRegistry` rather than from rendered reason text
- a SET EQUALITY between `corpus.watched` minus `derivedKit` and the registry arm's markdown members,
  reported in both directions as two defects
- the descriptive residue sentence REMOVED and replaced by the published, reconcilable three numbers
  (register arm, registry arm, residue)
- the round-2 containment pin and `WATCHED_CORPUS_MIN` count pin retained unchanged

**`scripts/check-audit-register.test.ts`** and **`scripts/check-diff-disposition.test.ts`** — new cases
- the one-cell `kind: safety` flip, in both gates, with a premise assertion
- the stray `kind: safety` row, reported as a different defect
- the per-kind falsifiability probe (each declared kind perturbed once)
- the delete-a-kind sum-floor probe
- the BOTH-ARMS probe, asserting the finding COUNT as well as the exit code
- the zero-safety-row vacuity refusal, in both gates

**`29-30-SUMMARY.md`**
- the WR-06 pre/post reproduction (union 41 -> 40, both gates' exit codes, both builds)
- the two-direction message transcripts from both gates
- the per-kind and sum-floor probe transcripts
- the BOTH-ARMS probe shown failing against an early-returning variant
- the three-row LANG-03 `missing:`-item traceability table, with item 2 attributed to plan 29-28
- the seven repo-gate exit codes
</artifacts_this_phase_produces>

<verification>
- `npm run build && npm run freshness` — every committed `.js` matches a fresh rebuild.
- All seven repo gates exit 0 on the live tree, with the exit codes pasted.
- `node scripts/generate-safety-surface.js` leaves the generated exclusion list byte-unchanged.
- `npx vitest run --exclude '**/scripts/e2e/**'` exits 0.
- `node scripts/check-nul-bytes.js` exits 0, so the counting greps above are trustworthy.
</verification>

<success_criteria>
- LANG-03's three enumerated `missing:` items each map to a landed artifact; item 2 is attributed to
  plan 29-28 and not re-implemented.
- Both arms of the D-18 union are pinned in both directions, at the source and at the consumer.
- Round 3's one-cell-flip reproduction is GREEN under the pre-plan build and RED under the post-plan
  build, in both gates, with the transcripts pasted.
- The gate publishes three reconcilable numbers instead of one number and a sentence.
- The BOTH-ARMS probe has been seen failing against an early-returning gate.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-30-SUMMARY.md` when done.
</output>

---
phase: 29-controlled-language-voice-guard-rebuild
plan: 41
type: execute
wave: 38
depends_on: ["29-40"]
gap_closure: true
files_modified:
  - scripts/check-banned-claims.ts
  - scripts/check-banned-claims.js
autonomous: true
requirements: [LANG-04]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LANG-04 — `guard_banned_claims` DECIDES the comprehension-benefit prohibition rather than enumerating a phrase list: the bare term is a finding when a benefit verb occurs on the same line, so an interposed word no longer defeats every member of the group (user decision (c), structural fix; options (a) accept-the-bound and (b) append-phrasings are both REJECTED)."
    - "Every measured bypass in the family recorded in 29-UAT.md § G-29-2 root_cause reds after the fix, each one BY NAME at its file, line and column — never inferred from a non-zero exit code, because the re-pin makes the gate exit non-zero on every run until it lands."
    - "LANG-04 `empty` edge (probe, verification: explicit) — an empty line, a whitespace-only line, and a line carrying the bare term with no marker each produce ZERO findings; a conditional member declared with an EMPTY marker array never matches any line at all, so it is refused at admission rather than shipped as a silently dead member."
    - "LANG-04 `encoding` edge (probe, verification: explicit) — equality is JavaScript's own: the scanner splits on the LINE FEED alone, lowercases with `toLowerCase()`, compares by UTF-16 code-unit substring, and reports a column as a UTF-16 code-unit offset plus one. No code-point, grapheme-cluster or Unicode-normalized comparison is performed anywhere in this gate, and that is stated at the matcher rather than inferred from its absence."
    - "Per D-25 and the guard's own refusal text, `BANNED_CLAIM_EXEMPT_SUPPRESSED` moves in the SAME commit as the rule that moves it, with the commit message naming which claims entered the exemption region — a constant moved to silence a line is the one route to green this gate exists to refuse."
    - "The pinned value is DERIVED from the run that measures it, not typed from the gap file: the suppressed count is a function of which benefit-verb markers are admitted, so the two are settled in one measurement or the pin describes a prohibition nobody is applying."
    - "Per D-08 and AP-1 the PASS line carries the measurement it performed: with conditional members now plural and carrying DIFFERENT marker lists, a sentence rendering one member and one marker count would state a check the gate did not do."
    - "Per D-13 the fix ships only when the committed `scripts/check-banned-claims.js` is rebuilt and freshness-checked, because the committed `.js` is what the gate and its own test actually run."
  artifacts:
    - "scripts/check-banned-claims.ts — a benefit-verb marker list pinned beside `CONFORMANCE_VERB_MARKERS` with each marker's measured count; the comprehension group gaining its conditional bare member(s); the suppressed pin re-derived; the PASS line rendering conditional members per member"
    - "scripts/check-banned-claims.js — rebuilt from its .ts, which is what the gate and the live-tree case execute"
  key_links:
    - "the benefit-verb marker list -> `lineHits`'s existing `requiresOnSameLine` arm -> the finding render -> the gate's exit code — the mechanism reused rather than a second matcher written"
    - "the admitted marker set -> which lines inside the exemption region match -> `BANNED_CLAIM_EXEMPT_SUPPRESSED` — the coupling that makes the pin a derived value rather than a constant"
    - "the PASS line's conditional-member rendering -> the test that reads it -> a reader checking the gate's claim by hand — the AP-1 surface this phase ships four guards into"
  prohibitions:
    - "No byte ceiling is raised by this plan: LANG-08's prohibition half holds unchanged."
    - "No acceptance criterion runs a bare npm test; the regression command is npx vitest run --exclude '**/scripts/e2e/**'."
    - "Enumeration is NOT the fix. No additional fixed phrasing is appended to the comprehension group as the remedy — option (b) was measured and rejected, and it leaves the open half of the family green."
    - "The six existing comprehension literals are NOT deleted. The suppressed arithmetic depends on them, and deleting what the guard measures is the named route to a false green."
    - "The suppressed and extent pins are never moved to make a failing line go away. They move only with the region read, the entrants named in the commit message, and the new value derived from the run."
    - "The matcher is never weakened to accommodate the disclaimer — no fenced-block skip, no whole-word-only match, no below-a-marker skip. The module's own forbidden-alternative paragraph names all three."
    - "A bypass is never reported as closed on the strength of a non-zero exit code. The finding must be named at its file, line and column, because the re-pin makes the gate exit non-zero for an unrelated reason."
    - "No marker is admitted without its own measured count, and none is admitted without a line on which it is the ONLY marker present and the gate reds."
    - "No .ts edit ships without its committed .js rebuilt and freshness-checked."
---

<objective>
Close **G-29-2** of `29-UAT.md` — the user's decision of record is **(c) STRUCTURAL FIX**. Option (a),
accepting the disclosed bound, and option (b), appending phrasings, were both explicitly REJECTED.

The `comprehension` group of `BANNED_CLAIM_LITERALS` (`scripts/check-banned-claims.ts:252-257`) holds SIX
fixed substrings matched as plain case-insensitive substrings, so ANY interposed word defeats every one
of them. Measured on a hermetic clone during UAT: the bare phrasing reds, and five phrasings with an
interposed or reworded middle all PASS. The family is open; enumeration cannot close it.

The remedy reuses the mechanism already proven by the `Simplified Technical English` member: a bare term
is a finding only when a co-occurring marker appears on the same line, so the guard decides a RULE
instead of holding a list. That member's declaration comment already argues the shape — the claim is the
name PLUS a verb, and the pair is what is pinned.

**A discrepancy in the prior measurement, named here rather than inherited.** `29-UAT.md`'s
`measured_probe` records that with the prototype rule "ALL FIVE bypass sentences red, and the guard fails
ONLY on the expected pin line". Read against the source, those two statements do not sit together: the
un-re-pinned suppressed constant makes the gate exit non-zero on EVERY run once the rule lands, including
a run with no plant at all — so "reds" and "exits 1" are not distinguishable by exit code, and at least
one member of the family carries no occurrence of the bare term the prototype conditions on. The gap file
itself instructs that its prototype is a measurement to REPRODUCE and not a result to cite. This plan
reproduces it, per sentence, by NAME, and reports the contradiction if it holds.

Purpose: this repository's standing lesson is that for a safety invariant a green suite is not proof, and
its harness produced a false result in six instances across four straight rounds. Assert the premise
first; measure each element; never take the aggregate for the member.

Output: one rule replacing an open enumeration, every marker admitted with its own measurement, the whole
measured family red by name, the suppressed pin re-derived in the same commit with its entrants named,
and a PASS line that states what it actually checked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-UAT.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-23-SUMMARY.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-32-SUMMARY.md
</context>

<standing_rules>
Identical to plan 29-33's block. The six that bite hardest here:

- **Assert the verification harness's own premise.** Six false results across four rounds came from a
  harness that was not what it claimed — a stale build, the wrong clone, a mirror not at HEAD. Every
  measurement here runs against the COMMITTED `.js` on a `git archive HEAD` mirror, after the freshness
  premise is asserted.
- **RED-first with mutation-proven discrimination.** A fixture that passes before and after the fix
  proves nothing. Each element of the family is shown failing to red before, and reddening by name
  after.
- **Ask what the predicate's INPUT is assembled from, and at WHICH POSITIONS it is asked.** The
  mechanism is called `requiresOnSameLine`, so this plan must say what a LINE is for this scanner and
  test the boundary — a claim split across a soft wrap, a marker on the preceding line, a markdown table
  row where two cells share a line.
- **Ask which SET the predicate enumerates,** and prefer deriving that set over hand-listing it. Where a
  list must be hand-authored — a marker list is a measurement baseline, not a discovery set — every
  member carries its own measured number, exactly as `CONFORMANCE_VERB_MARKERS` does.
- **A vacuity floor must catch a SILENTLY SHORT denominator, not only an empty one.** Derive element
  counts independently of the loop that consumes them.
- **Build facts (D-13).** `npm run build` then `npm run freshness` after every `.ts` edit. Regression is
  `npx vitest run --exclude '**/scripts/e2e/**'`, never a bare `npm test`.
</standing_rules>

<gap_contract_map>
Every `missing:` bullet of G-29-2 in `29-UAT.md`, mapped to where it is discharged across plans 29-41 and
29-42. No bullet is dropped.

| G-29-2 `missing:` bullet | discharged by |
|---|---|
| Add a conditional member on the bare term, reusing the `requiresOnSameLine` mechanism proven by the `Simplified Technical English` member | 29-41 Task 1 (the tracer lands it end to end) |
| Pin the benefit-verb marker list the way `CONFORMANCE_VERB_MARKERS` is pinned, with the measured hit count that admitted each marker | 29-41 Task 2 |
| Re-pin `BANNED_CLAIM_EXEMPT_SUPPRESSED` in the SAME commit, saying in the commit message which claims entered the region | 29-41 Task 1 (the pin moves in the commit that moves it) |
| Relax the test's "exactly one conditional member" assertion to the new measured count, with the reason recorded — do not delete it | 29-42 Task 1 |
| Record the rejected alternative in `BANNED_CLAIM_EXCLUDED` | 29-42 Task 2 |
| RED-first: the five bypass sentences become fixture cases that fail before the fix and pass after | 29-41 Task 1 and Task 2 (measured RED baseline and post-fix by-name reds) + 29-42 Task 1 (made permanent cases) |
| Rebuild so committed `scripts/check-banned-claims.js` matches its `.ts` | 29-41 Task 3 `<verify>` + 29-42 Task 3 sweep |
</gap_contract_map>

<probe_coverage>
Full arithmetic and the flagged out-of-scope drop are stated once, in plan 29-40's `<probe_coverage>`
block. Restated here only for what THIS plan authors.

- **Authored into `must_haves.truths` by this plan: 2** — LANG-04 `empty` and LANG-04 `encoding`, both
  `verification: explicit`, both written as plain truth strings because a defensible acceptance criterion
  exists for each. Neither is backstopped and neither is dismissed.
- The `encoding` probe is resolved explicitly rather than backstopped because it is genuinely
  load-bearing here: this gate's matching IS a case-insensitive substring comparison over lines, so
  "whose definition of equality applies" is the predicate's own definition and must be written down.
- Round total unchanged: 2 authored + 20 flagged = 22 surfaced. ✓
</probe_coverage>

<assumption_delta_decision>
**Decision: `promote`.**

The detector fired `detected: true` on `pluralization` signals `also` and `alternative`, and both of those
matches are inside ROADMAP narrative prose rather than in this plan's scope. But the reading required by
the delta check finds a genuine singular→plural transition one file over, in the source this plan edits,
and recording `no-change` on the strength of two false-positive signal words would miss it.

The identity being generalized: **a conditional member is currently assumed to be exactly one thing, with
exactly one marker list.** Three separate surfaces encode that assumption as a singular:

1. `scripts/check-banned-claims.test.ts` asserts the count of conditional members is exactly 1, with a
   comment stating a second would need its own recorded reason.
2. The declaration comment on `requiresOnSameLine` is written for one member and one discipline name,
   and explains why "ONE MEMBER IS CONDITIONAL".
3. The PASS line renders the conditional-member count with a singular verb and pairs it with
   `CONFORMANCE_VERB_MARKERS.length` — one marker list, presented as THE marker list.

After this plan, conditional members are plural and carry DIFFERENT marker lists: a conformance-verb list
and a benefit-verb list. Surface 3 becomes false in two ways at once — its verb and its denominator — and
surface 1 becomes a pin that must be re-measured rather than deleted. The promotion is recorded so those
three surfaces are treated as one change with one reason, rather than as three unrelated reds cleared
independently. Surfaces 1 and 2 are discharged in plan 29-42; surface 3 in this plan's task 3.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: premise asserted, one bypass reproduced by name, the rule landed and the pin re-derived — end to end</name>

  <precondition>
    Plan 29-40 is committed and `npm run freshness` exits 0 on HEAD, so `scripts/check-banned-claims.js`
    is a faithful build of `scripts/check-banned-claims.ts`. Halt and report if it does not: every
    reproduction below is a claim about the `.ts` only while that holds, and this phase has shipped six
    false results from harnesses that were not what they claimed.
  </precondition>

  <read_first>
    - `scripts/check-banned-claims.ts:181-232` — the `BannedClaimLiteral` interface, the
      `requiresOnSameLine` field with its full "why one member is conditional, and why that is not a
      second grammar" paragraph, `CONFORMANCE_VERB_MARKERS` with its stems and its recorded hit counts,
      and the `Simplified Technical English` member that is the mechanism this plan reuses
    - `scripts/check-banned-claims.ts:248-258` — the comprehension group's six members with their
      admission-time note
    - `scripts/check-banned-claims.ts:748-795` — `LineHit`, `lineHits` with its "ONE MATCHER, TWO
      CALLERS" paragraph and the `requiresOnSameLine` arm at 766-771, `occurrences`, and the exported
      `countBannedClaimOccurrences` used to derive the reach independently
    - `scripts/check-banned-claims.ts:639-658` — the suppressed pin's declaration paragraph in full,
      including the sentence that moving it is how a deliberate change is acknowledged and never how a
      failure is cleared
    - `scripts/check-banned-claims.ts:896-938` — `text.split("\n")`, the per-line loop, the exemption
      window test, and the suppressed-count refusal whose wording demands the region be read and the
      entrant named
    - `scripts/check-banned-claims.ts:797-805` — `renderFinding`, so the reproduction knows the exact
      shape of the by-name output it must assert
    - `29-UAT.md` § Gaps, gap `G-29-2` in full — the root cause with its five enumerated bypass phrasings
      and the two exempt denial line numbers, the artifacts, the seven `missing:` bullets, and the
      `measured_probe` block whose claim this task reproduces rather than cites. The five phrasings are
      read FROM THAT FILE and are not restated in this plan.
    - `agent-factory/writing-profile.md:235-300` — the exemption region, the two honest denials at 256
      and 288, and their surrounding prose
  </read_first>

  <files>scripts/check-banned-claims.ts</files>

  <behavior>
    RED FIRST, and the red must be attributable.

    - Against the SHIPPING committed `.js` on a mirror of HEAD, each phrasing in the family enumerated in
      `29-UAT.md` § G-29-2 is planted alone and the gate's output is captured. The baseline records, per
      phrasing, whether a finding was rendered NAMING it, not merely whether the process exited non-zero.
    - The rule lands: the bare term becomes a conditional member of the comprehension group, using the
      existing `requiresOnSameLine` field and no new matcher.
    - The six existing enumerated literals stay. Nothing the guard currently catches stops being caught.
    - The suppressed pin is re-derived from the run that the rule produces, in the same commit, and the
      commit message names which claims entered the region.
    - On the clean tree with no plant, the gate exits 0 and prints its PASS line.
  </behavior>

  <action>
    Assert the premise, then take the baseline. Run `npm run freshness` and quote its statement. Create a
    `git archive HEAD` mirror in a temp directory and confirm the mirror's `scripts/check-banned-claims.js`
    is byte-identical to the committed one — a mirror not at HEAD is the exact harness failure this phase
    recorded six times. Then, for EACH phrasing enumerated in `29-UAT.md` § G-29-2 root_cause, append it
    alone to one governed kit document inside the mirror, run the committed gate, and record three things:
    the exit code, whether a finding line was rendered naming that phrasing with its file, line and
    column, and the literal the finding attributed it to. Reset the mirror between phrasings so no plant
    leaks into the next measurement. That per-phrasing table is the RED-first baseline and it is the
    control every later claim in this plan is judged against.

    Then land the rule. Add a conditional member to the comprehension group whose literal is the BARE term
    and whose `requiresOnSameLine` is the benefit-verb marker list task 2 admits — declare the list beside
    `CONFORMANCE_VERB_MARKERS`, in the same idiom, and reference it by name from the member exactly as the
    existing conditional member references its own list. Do not add a second matcher, do not add a field,
    and do not touch `lineHits`: the arm already exists and already runs per member, which is why the
    decision named this mechanism. Keep all six existing enumerated literals. State at the group why they
    stay: the rule catches the open family and the enumerated members carry the exemption arithmetic, so
    deleting them would move a pin by deleting what it measures.

    Write the declaration paragraph the way this module writes them. State what the rule decides, state
    that the bare term alone is legal because a document must be able to discuss comprehension without
    claiming a benefit, and state the accepted cost: the co-occurrence window is a LINE, so a claim split
    across a soft wrap is outside it. Name that residual here rather than leaving it for the round to
    discover — plan 29-42 measures it and records it, and a cost paragraph written after the measurement
    would be a rationalisation rather than a design note.

    Now re-derive the pin, do not type it. Run the gate on the clean mirror with the rule in place and read
    the suppressed count out of its own refusal text. The gap file predicts the value rises by two because
    the two honest denials in the exemption region each gain one bare-term hit; treat that as a prediction
    to confirm, and if the run reports a different number, the number the run reports wins and the
    difference is investigated before anything is edited. Note the coupling explicitly at the declaration:
    this value is a function of WHICH markers are admitted, because a denial line matches only if one of
    its words is an admitted marker — so the pin and the marker list are settled by one measurement or
    neither is honest.

    Move the pin in the same commit as the rule, and write the commit message the guard's own refusal
    demands: name the file, the region, and each claim that entered it by line number and by which member
    now matches it. A commit that moves the constant without naming the entrants is the shape the refusal
    text forbids in writing.

    Finally, re-run the ONE phrasing that the baseline showed passing and that the bare-term rule is
    expected to catch, and assert it now reds BY NAME at its file, line and column — reading the finding
    line, not the exit code. With the pin re-derived, a clean run exits 0, so the exit code is once again
    a signal; record both, and say which one carries the claim.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; npm run freshness &amp;&amp; node scripts/check-banned-claims.js</automated>
  </verify>

  <acceptance_criteria>
    - `npm run freshness` is run FIRST and its statement is quoted; the mirror's committed gate `.js` is
      asserted byte-identical to the repository's before any reproduction is trusted.
    - A per-phrasing baseline table is recorded in the SUMMARY with one row per phrasing enumerated in
      `29-UAT.md` § G-29-2 root_cause, each carrying: exit code, whether a finding was rendered naming it,
      and the attributing literal. Rows are taken one plant at a time on a reset mirror.
    - `29-UAT.md`'s `measured_probe` claim is adjudicated explicitly: the SUMMARY states, with evidence,
      whether "all five red" reproduces. If it does not, the discrepancy is reported as a
      harness-premise finding by name, not explained away.
    - The comprehension group gains a conditional member whose literal is the bare term and whose
      `requiresOnSameLine` references the named benefit-verb marker list; `git diff` shows NO change to
      `lineHits` and no new matcher function.
    - All six pre-existing comprehension literals are still present:
      `grep -c "group: \"comprehension\"" scripts/check-banned-claims.ts` returns at least 7.
    - `BANNED_CLAIM_EXEMPT_SUPPRESSED` holds the value the gate's own refusal text reported on the clean
      mirror, and that reported number is quoted in the SUMMARY from the run output.
    - `grep -c 'BANNED_CLAIM_EXEMPT_SUPPRESSED = 10' scripts/check-banned-claims.ts` returns 0.
    - The rule and the pin move in ONE commit, and `git log -1 --format=%B` for that commit names each
      claim that entered the exemption region with its line number.
    - `node scripts/check-banned-claims.js` exits 0 on the clean tree after `npm run build`, and prints
      `ALL CHECKS PASSED`.
    - At least one previously-passing phrasing is shown reddening BY NAME with its file, line and column
      quoted from the run.
  </acceptance_criteria>

  <reversibility rating="reversible">
    A pinned constant and a declared member are both restorable from git, and the derived value is
    re-measurable at any time by running the gate. Recorded explicitly so the re-pin is not over-gated:
    it is a measurement being written down, not an irreversible decision.
  </reversibility>

  <done>
    The rule decides the prohibition, the pin was derived rather than typed, and the first bypass reds by
    name against a gate whose build was proven before it was believed.
  </done>
</task>

<task type="auto">
  <name>Task 2: every marker earns its place by measurement, and the WHOLE measured family is covered</name>

  <read_first>
    - `scripts/check-banned-claims.ts:199-216` — `CONFORMANCE_VERB_MARKERS`'s declaration paragraph in
      full: why the members are stems, that each stem covers its family by substring, and the recorded
      per-stem hit counts over the scan set with the sentence stating they are not banned literals in
      their own right and must never become any. This is the pinning idiom to copy, including the shape
      of the numbers.
    - `scripts/check-banned-claims.ts:267-305` — `BANNED_CLAIM_EXCLUDED`, the admission log, including the
      entry that refuses a candidate WITHOUT measurement on an arithmetic ground and says so
    - `scripts/check-banned-claims.ts:406-450` — the scan-set derivation and `BANNED_CLAIM_SCAN_COUNT`, so
      every hit count in this task is measured over the set the gate actually reads rather than over a
      hand-built approximation
    - `29-UAT.md` § G-29-2 `measured_probe` — the prototype marker list, and the sentence stating it is a
      PROTOTYPE list and not an admitted one, each marker still needing its own measured hit count
    - `29-UAT.md` § G-29-2 root_cause — the enumerated family again, read as the coverage obligation this
      task must satisfy in full
    - `scripts/check-banned-claims.ts:307-331` — the exemption region's declaration, including the
      forbidden-alternative paragraph naming the three ways a matcher must never be weakened
  </read_first>

  <files>scripts/check-banned-claims.ts</files>

  <behavior>
    - Each candidate marker carries TWO measured numbers before it is admitted: its bare line-hit count
      over the derived scan set — the over-match measurement, the reason it can be a marker and never a
      literal — and which member of the measured family it admits.
    - A candidate that admits no member of the family AND has no constructed claim form it alone catches
      is DROPPED, with its measurement recorded in the admission log rather than deleted silently.
    - The rule covers the WHOLE measured family. Any member the bare-term rule structurally cannot reach
      is covered by a second bare TERM with its own rule and its own measurement — never by appending the
      phrasing, which is the rejected option.
    - A candidate second term whose measured bare hit count would red correct kit text is REFUSED and
      escalated with its count, not admitted and then worked around.
    - Every admitted marker has a line on which it is the ONLY marker present and the gate reds, so no
      marker is admitted that nothing proved does anything.
  </behavior>

  <action>
    Measure before admitting. Derive the scan set through the module's own derivation, not a directory
    walk written for this task, and for each candidate marker in the prototype list count the lines that
    carry it. Record each number beside its marker exactly as the conformance stems record theirs. A count
    of zero is not a reason to drop a marker: zero means the marker over-matches nothing, which is the
    safest possible reading, and the conformance list's own numbers are recorded for the opposite reason —
    they are large, which is why those stems may never become literals. State that asymmetry at the
    declaration so the next reader does not delete a zero-count marker as unused.

    Then measure what each marker DOES. For each phrasing in the family enumerated in `29-UAT.md` §
    G-29-2 root_cause, record which candidate marker admits it. Any marker that admits no member of the
    family must earn its place another way: construct one claim sentence it alone catches, plant it, show
    the gate reds naming it, and record that sentence as its warrant. A marker with neither a family
    member nor a warrant sentence is dropped into `BANNED_CLAIM_EXCLUDED` with its count and the reason,
    because a marker nobody proved does anything is a member of a list that will rot.

    Now settle the coverage obligation honestly. Read the family against the bare-term rule and determine,
    per member, whether the rule can reach it at all — a member that contains no occurrence of the bare
    term is outside the rule by construction, regardless of which markers are admitted. If any member is
    outside, that is a finding about the prototype's claim and must be reported as one; the remedy is a
    SECOND bare term with its own conditional rule, chosen so it generalises the same way rather than
    naming the phrasing. Measure the candidate second term the same way before admitting it: its bare
    line-hit count over the derived scan set, how many of those lines also carry an admitted marker, and
    whether any of them sit inside the exemption region. If the count over correct kit text is non-zero
    and the co-occurring count is non-zero, REFUSE the term, record the measurement in the admission log,
    and escalate the uncovered family member by name as a residual — do not weaken the matcher and do not
    append the phrasing.

    Re-derive the suppressed pin after the marker set is final, because the two are coupled: a denial line
    inside the exemption region matches the bare term only when one of its own words is an admitted
    marker, so dropping or adding a marker moves the pin. Run the gate on a clean mirror and read the
    number out of its refusal or its PASS line rather than reasoning about it. If the final marker set
    changed the number task 1 pinned, move the pin again in this task's commit and name the entrants
    again — the protocol applies to every move of that constant, not only the first.

    Write the declaration paragraph. State what the benefit-verb list is for, that its members are
    markers and never literals, the per-marker numbers with the date of the measurement, the coverage map
    from marker to family member, and the two named refusals: a marker with no warrant, and a second bare
    term that over-matches. State also that the list is a MEASUREMENT BASELINE and not a discovery set, so
    a reader does not try to derive it — the same justification `roleCeiling()` carries under D-25.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-banned-claims.js &amp;&amp; npx vitest run scripts/check-banned-claims.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - The benefit-verb marker list is declared beside `CONFORMANCE_VERB_MARKERS` and its declaration
      carries, per admitted marker: the measured bare line-hit count over the derived scan set, and the
      family member or warrant sentence that admitted it.
    - Every measurement is taken over the set produced by the module's own scan derivation; the count of
      documents scanned is recorded and equals `BANNED_CLAIM_SCAN_COUNT`.
    - Every member of the family enumerated in `29-UAT.md` § G-29-2 root_cause has a recorded coverage
      verdict: covered by the bare-term rule, covered by an admitted second bare term, or REFUSED-and-
      escalated with its measurement. No member is left without a verdict.
    - If any member is structurally unreachable by the bare-term rule, that is reported in the SUMMARY as
      a finding against `29-UAT.md`'s prototype claim, with the reading that produced it.
    - Every dropped candidate marker appears in `BANNED_CLAIM_EXCLUDED` with a numeric hit count and a
      reason of more than forty characters, so the existing admission-log assertion covers it.
    - For each admitted marker there is a recorded plant on which it is the only marker present and the
      gate reds naming the finding; the transcripts are in the SUMMARY.
    - `BANNED_CLAIM_EXEMPT_SUPPRESSED` matches the value the gate reports after the marker set is final;
      if it moved again, that move is in this task's commit and its message names the entrants.
    - `node scripts/check-banned-claims.js` exits 0 on the clean tree and
      `npx vitest run scripts/check-banned-claims.test.ts` exits 0.
  </acceptance_criteria>

  <reversibility rating="costly">
    Admitting a SECOND bare term widens the rule surface the guard decides. Reversing it later means
    re-auditing every line already judged under the wider rule to see which would have passed under the
    narrower one, with no record of which rule was in force when each judgement was made. Rated costly and
    flagged rather than gated: the alternative — leaving a measured member of the family green — is the
    option the user rejected, so this is the implementation of the decision rather than a new decision.
  </reversibility>

  <done>
    Every marker in the list has a number that admitted it, every member of the measured family has a
    verdict, and nothing was enumerated to reach green.
  </done>
</task>

<task type="auto">
  <name>Task 3: the PASS line states what it actually checked, now that conditional members are plural</name>

  <read_first>
    - `scripts/check-banned-claims.ts:1028-1048` — the PASS line in full, particularly the clause
      rendering the conditional-member count with a singular verb and pairing it with
      `CONFORMANCE_VERB_MARKERS.length` as though there were one marker list
    - `scripts/check-banned-claims.ts:1015-1026` — `reportMeasured` and the comment stating this gate
      declares no zero check of its own because the measurement is a required argument, so the shared
      element-level vacuity rule owns the floor
    - `.planning/phases/28-kit-consistency-audit/.continue-here.md` — AP-1, severity `blocking`: a gate
      printing a PASS line for a check it did not perform, and the canonical instance
    - `29-CONTEXT.md` D-08 — the PASS line carries the measurement rather than an assertion, and the
      reason a claim unfalsifiable from the output is the blocking shape
    - `scripts/check-banned-claims.test.ts:1639-1648` — the live-tree case reading the PASS line, so the
      rendering change is made knowing which assertion consumes it
  </read_first>

  <files>scripts/check-banned-claims.ts</files>

  <behavior>
    - The PASS line renders conditional members PER MEMBER: each conditional member's literal with the
      SIZE OF ITS OWN marker list, rather than one count paired with one list's length.
    - The rendering is derived from `BANNED_CLAIM_LITERALS` at print time, so a member added or removed
      later changes the line without anybody editing it.
    - Grammar follows the number: a line reporting two conditional members does not say "is".
    - No number in the line is a literal. Every one is read from the run that just happened.
    - The gate exits 0 on the clean tree and its committed `.js` is a faithful rebuild.
  </behavior>

  <action>
    Correct the clause rather than only its arithmetic. The current sentence states a count of conditional
    members and then attributes them all to a conformance verb from one pinned marker list; with two
    conditional members carrying two different lists, that sentence would name a marker list that does not
    govern one of the members it counts — a PASS line stating a check the gate did not perform, which is
    AP-1's exact shape and the reason this phase carries it as blocking.

    Render per member instead. Map over the conditional members of `BANNED_CLAIM_LITERALS`, and for each
    emit its literal together with the length of its OWN `requiresOnSameLine` array. Derive the count from
    the same expression that produces the rendering so the two cannot disagree, and make the verb agree
    with the count. Keep every other number in the line exactly as it is: the scan parts, the overlap, the
    literal total, the group count, the exemption's suppressed and extent values with their pins, and the
    admission-log size. This is a change to ONE clause and it must read as one in the diff.

    Then rebuild and prove the line. Run `npm run build`, run the committed gate on the clean tree, and
    quote the emitted PASS line in the SUMMARY in full. Check by hand that every number in it is
    reproducible from the source — that is what makes a fabricated PASS visible, and it is the property
    D-08 asks for. Record which numbers moved as a result of this round's work and which did not.

    Confirm the live-tree case that reads the PASS line still passes, and if its assertion needs to move,
    move it in plan 29-42's test task rather than here — this plan owns the gate, that plan owns the test
    surface, and splitting a change across two plans without saying so is how a red gets cleared twice.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; npm run freshness &amp;&amp; node scripts/check-banned-claims.js &amp;&amp; npx vitest run scripts/check-banned-claims.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - The PASS line renders one entry per conditional member, each pairing the member's literal with the
      length of its own marker array; the rendering and the count come from the same expression.
    - `grep -c 'CONFORMANCE_VERB_MARKERS.length' scripts/check-banned-claims.ts` returns 0 inside the PASS
      line's construction — the singular denominator is gone, not merely supplemented.
    - The emitted PASS line is quoted in full in the SUMMARY, and every number in it is shown reproducible
      from source by a named command or reading.
    - The diff for this task touches the PASS line's conditional clause and nothing else in `runAll`.
    - `npm run build` and `npm run freshness` exit 0, so the committed `.js` matches its `.ts`.
    - `node scripts/check-banned-claims.js` exits 0 and prints `ALL CHECKS PASSED`.
    - `npx vitest run scripts/check-banned-claims.test.ts` exits 0, or the one assertion needing to move
      is named and explicitly deferred to plan 29-42 task 1 rather than edited here.
  </acceptance_criteria>

  <done>
    The gate's own output stops asserting a singular that stopped being true, and every number on the line
    can be checked by hand against the file.
  </done>
</task>

</tasks>

<artifacts_this_plan_produces>
| symbol | file | kind |
|---|---|---|
| `BENEFIT_VERB_MARKERS` | `scripts/check-banned-claims.ts` | new pinned marker list with per-marker measured counts, declared beside `CONFORMANCE_VERB_MARKERS` |
| the conditional bare-term member of the `comprehension` group | `scripts/check-banned-claims.ts` | new `BANNED_CLAIM_LITERALS` entry using the existing `requiresOnSameLine` field |
| a second conditional bare-term member, IF task 2's measurement admits one | `scripts/check-banned-claims.ts` | conditional new entry — admitted only with its own measured counts, otherwise refused and escalated |
| the benefit-verb declaration paragraph | `scripts/check-banned-claims.ts` | new declaration prose carrying the per-marker numbers, the coverage map and the two named refusals |
| the per-member conditional rendering in the PASS line | `scripts/check-banned-claims.ts` | replaced clause, same sentence position |

Values that MOVE rather than being created (derived from the run, never typed):
`BANNED_CLAIM_EXEMPT_SUPPRESSED`. Values that must NOT move: `BANNED_CLAIM_EXEMPT_EXTENT` (this plan
edits no document inside the exemption region), `BANNED_CLAIM_SCAN_COUNT`, `BANNED_CLAIM_EXEMPT_REGION`,
and the six pre-existing comprehension literals, which stay in place.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored kit and public-doc prose → the banned-claim matcher | Any sentence an author writes crosses here, and the guard is the kit's mechanical honesty control |
| the exemption region → the rest of the corpus | The one place the prohibition is lifted; its size and reach are the only things bounding it |
| a pinned constant → the maintainer clearing a red | A constant that can be moved to silence a line converts a safety pin into a chore |
| the gate's exit code → a closure claim | A non-zero exit from an unrelated pin is indistinguishable from a caught claim unless the finding is read |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-41-01 | Spoofing | a fail-open guard: a conformance or comprehension claim stated in the shipped kit without reddening | high | mitigate | The enumeration is replaced by a rule on the bare term with a same-line marker; the whole measured family gets a coverage verdict (tasks 1, 2) |
| T-29-41-02 | Tampering | a closure claimed from a non-zero exit code while the finding was never rendered | high | mitigate | Every reproduction asserts the finding BY NAME at file, line and column; the exit code is recorded separately and never carries the claim (task 1) |
| T-29-41-03 | Tampering | a stale build or a mirror not at HEAD producing a false measurement | high | mitigate | The freshness premise is asserted first and the mirror's gate `.js` compared byte-for-byte before any reproduction is trusted (task 1) |
| T-29-41-04 | Repudiation | the suppressed pin moved to silence a line rather than to record a decision | high | mitigate | The pin is derived from the run, moved in the same commit as the rule, and its commit message names each entrant by line (tasks 1, 2) |
| T-29-41-05 | Elevation of Privilege | a marker admitted with no measurement, widening the rule by an unexamined member | high | mitigate | Every marker carries a bare hit count and a family member or warrant plant; unwarranted candidates are refused into the admission log (task 2) |
| T-29-41-06 | Information Disclosure | a PASS line attributing two differently-governed members to one marker list — AP-1's blocking shape | high | mitigate | The clause is rendered per member from the literal list itself, with the verb agreeing with the derived count (task 3) |
| T-29-41-07 | Denial of Service | an over-matching second bare term reddening large volumes of correct kit text, whose only route back to green is weakening the matcher | high | mitigate | The candidate is measured before admission and REFUSED with its count if it reaches correct text; the forbidden weakenings are named in the prohibitions (task 2) |
| T-29-41-SC | Tampering | npm/pip/cargo installs | high | accept | No package installed by this plan; `package.json` and the lockfile asserted byte-unchanged across the plan's full commit range. RESEARCH.md carries no new package rows for this round, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- The freshness premise is asserted and the mirror proven at HEAD before any reproduction is believed.
- A per-phrasing baseline table exists with exit code, by-name finding and attributing literal per row.
- `29-UAT.md`'s prototype claim is adjudicated in writing, with the contradiction reported if it holds.
- Every member of the measured family carries a coverage verdict; none is left unaddressed.
- Every admitted marker carries a measured count and a plant on which it alone reds.
- The suppressed pin equals the number the gate reported, and its commit message names the entrants.
- The PASS line renders per member and every number in it is hand-checkable.
- `npm run build`, `npm run freshness` and the gate all exit 0 on the clean tree.
</verification>

<success_criteria>
- The comprehension prohibition is decided by a rule, and the interposed-word family that defeated every
  enumerated member now reds by name.
- No pin was moved to silence a line, and no matcher was weakened to reach green.
- Every marker in the new list has a number that admitted it, and every refusal has a number that
  refused it.
- The gate's PASS line stops asserting a singular that this plan made false.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-41-SUMMARY.md` when done.
</output>

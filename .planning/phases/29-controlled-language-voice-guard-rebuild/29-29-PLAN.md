---
phase: 29-controlled-language-voice-guard-rebuild
plan: 29
type: execute
wave: 4
depends_on: ["29-28", "29-30", "29-32"]
gap_closure: true
files_modified:
  - scripts/check-foundation-guards.test.ts
  - scripts/section-locator-oracle.test.ts
  - docs/audit/29-locator-unification.md
autonomous: true
requirements: [LANG-07]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LANG-07 — guard_imperative_lexicon (with its sibling guard_sentence_form) and the rebuilt voice guard read the fence through ONE parser, never two grammars over the same bytes"
  artifacts:
    - "scripts/check-foundation-guards.test.ts — the owner classifier's recogniser arm widened to the shapes floor item 4 named, floor item 4 corrected, and SECTION_EXTENT_OWNERS re-derived over the FINAL round-3 tree"
    - "scripts/section-locator-oracle.test.ts — a duplicate-occurrence axis making I5 reachable, an independently derived per-invariant REACH count, and a third broken locator proving I5 can fail"
    - "scripts/check-foundation-guards.test.ts — the duplicate-assertion tripwire's multi-line share derived, published and pinned two-sided"
    - "docs/audit/29-locator-unification.md — a §9 round-3 correction, with pointers inserted at §3, §4 and §6 so a reader meeting a stale measurement is redirected"
  key_links:
    - "the widened classifier -> the final tree -> SECTION_EXTENT_OWNERS — a measurement over a corrected rule, not a rule narrowed until the answer came out right"
    - "the duplicate-occurrence axis -> I5's loop body -> the last-match probe — an invariant that is reached, and proven able to fail"
---

<objective>
Close LANG-07's remaining two enumerated `missing:` items — the ones that are about the HARNESS rather
than about the code — and the review's WR-03, IN-01 and IN-03 that live in the same two test modules.

This plan is deliberately LAST in round 3. Its central deliverable is a re-derivation, and a
re-derivation taken before every source fix has landed measures a tree that no longer exists. Plans
29-28, 29-30 and 29-32 all change modules this scan reads.

**CR-02's harness half.** `SECTION_EXTENT_OWNERS = ["frontmatter.ts"]` and
`SECTION_EXTENT_OWNER_COUNT = 1` ran green over a TWO-owner tree, because the classifier's recogniser
arm requires a literal space and `audit-model.ts:893` spells a whitespace class, and because that
module's bound sits thirteen lines from its recogniser. Worse, the classifier's own disclosed floor,
item 4, asserts that shape is one "which no module in this tree uses today" — false at
`audit-model.ts:893`, on the day it was written. A floor asserting a falsehood about the tree is this
repository's named "prose claim wider than the assertion behind it" class, sitting inside the assertion
meant to close it.

**WR-03.** Invariant I5 in the new parser oracle is UNREACHABLE in all 7200 cells: the generator
inserts the candidate exactly once and no fixed corpus line can equal any candidate spelling, so no
document carries a second occurrence of the cell heading. I5 has been asserted 7200 times and has never
been shown able to fail. The ordering discrimination that makes `unfencedHeadingIndex` correct rather
than merely fence-aware is untested; an implementation returning the LAST unfenced match would sweep
clean. This is a vacuous assertion inside the verification harness itself — the newest instance of the
class that has produced a false result in six instances across four straight rounds.

Purpose: LANG-07's claim rests on a measurement. A measurement produced by a rule that cannot see the
thing it counts is not a measurement.

Output: a corrected classifier, an owner list re-derived over the final tree, an oracle whose
invariants are reached as well as satisfied, and a transcript whose stale claims are redirected rather
than quietly overwritten.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-28-SUMMARY.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-30-SUMMARY.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-32-SUMMARY.md
</context>

<round_3_standing_rules>
Identical to plan 29-27's block. The three that ARE this plan:

- **ASSERT THE VERIFICATION HARNESS'S OWN PREMISE.** The harness produced a FALSE result in six
  instances across four straight rounds; WR-03 is the newest. Derive an element count INDEPENDENTLY of
  the loop that consumes it, and assert each invariant is REACHED at least N times — not merely that
  it never failed.
- **Derive the set, assert the count, two-sided, with a planted-member probe.** A hand-declared owner
  list green over a set the classifier no longer enumerates completely is the direct cause of this gap.
- **A classifier narrowed until the answer comes out right is the defect; so is one widened until it is
  noisy.** Both directions must be asserted on planted inputs, and 29-22's recorded noise case (an
  issue-reference pattern wrongly recognised as a heading) is the specific widening hazard here.

Build facts: `npm run build` then `npm run freshness`. Regression suite is
`npx vitest run --exclude '**/scripts/e2e/**'`, never bare `npm test`. Counting greps use `grep -a`.
This plan grows a 7200-cell sweep; record the suite's wall-clock runtime before and after.
</round_3_standing_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the classifier sees the shape its own floor said nothing uses, and the owner list is re-derived over the final tree</name>

  <read_first>
    - `scripts/check-foundation-guards.test.ts:970-1241` in full — the LANG-07 derivation block: what a
      section-extent construct is and why it is a CONJUNCTION; the recorded reason the recogniser arm is
      narrower than 29-22's third construct (that construct's trailing class admitted a backslash and so
      recognised an issue-reference pattern in `trace-render.ts`); the six disclosed floor items;
      `codeLinesOfSource`, `HEADING_RECOGNISER_CONSTRUCTS`, `SCAN_TERMINATOR_CONSTRUCTS`,
      `TERMINATOR_WINDOW`, `recogniserNamesIn`, `terminatesAt`, `sectionExtentSitesIn`,
      `sectionExtentOwnersAmong`, `PLANTED_SIXTH_LOCATOR`, `SECTION_EXTENT_OWNERS`,
      `LOCATOR_CONSUMERS` and `nonTestScripts`
    - `scripts/audit-model.ts` as plan 29-28 leaves it, and `scripts/check-imperative-lexicon.ts` as
      plan 29-31 leaves it — the two modules whose heading constructs the widened arm will newly see
    - `scripts/trace-render.ts:71` and `scripts/validate-agent-factory.ts:454-459` — the two live sites
      a careless widening would wrongly admit; both must be asserted EXCLUDED, on the real tree
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` § CR-02, both arms
    - `docs/audit/29-locator-unification.md` §3 — the derivation this correction supersedes
  </read_first>

  <files>scripts/check-foundation-guards.test.ts, docs/audit/29-locator-unification.md</files>

  <behavior>
    RED FIRST. Every claim below must be seen failing before it is seen passing.

    - The widened recogniser arm MATCHES a heading regex spelled with a hash RUN and with a whitespace
      CLASS, in every combination: a literal space after one hash, after a run, after a `{n,m}`
      quantifier, and the same three with a whitespace class.
    - It does NOT match an issue-reference pattern (a hash followed by a digit class), and it does NOT
      match a bare hash with no following separator. Both asserted on planted inputs, because a
      classifier widened until the answer comes out right is the defect.
    - Run over the pre-29-28 source of `audit-model.ts`, the widened classifier reports that module as
      an owner. Run over the final tree it does not. That before/after pair is the evidence the
      correction is about the defect that existed rather than about an invented one.
    - `SECTION_EXTENT_OWNERS` re-derived over the FINAL tree equals the written-down measurement, in
      both directions, with the count pinned.
    - The `PLANTED_SIXTH_LOCATOR` probe still discriminates under the widened arm, and a SECOND planted
      locator spelled with a whitespace class is also reported — the shape the old arm was blind to.
  </behavior>

  <action>
    Correct the classifier, then re-measure, in that order.

    WIDEN `HEADING_RECOGNISER_CONSTRUCTS` to recognise the two spellings floor item 4 named: an
    anchored hash RUN (rather than a single hash or only a `{n,m}` quantifier), and a whitespace CLASS
    after the hashes (rather than only a literal space). Keep the existing `.startsWith` arm. Do NOT
    reach for 29-22's third construct: its trailing character class admits a backslash and that is what
    made it noisy, and 29-22's own summary records the noise as its reason for staying module-scoped.
    Assert both halves of that difference on planted inputs, as the block already does for the current
    arm — the shapes that must match, and the two live tree sites that must NOT.

    Then CORRECT floor item 4. It currently asserts that the whitespace-class spelling is one "which no
    module in this tree uses today". That was false when written. Replace it with what is true after
    the widening: the shape is now RECOGNISED, and state what the floor still cannot see in its place
    rather than leaving a gap where an item was — a floor that shrinks silently is the same defect one
    level down. Re-check every remaining floor item against the final tree in the same pass and correct
    any other that has become false; a floor is a claim about THIS tree and it goes stale like any other.

    Then close V-29-26-02's scope claim, which is recorded as a residual and is the reason a
    "tree-wide" answer is narrower than its own name. `nonTestScripts()` reads one directory
    non-recursively — 41 of 49 tracked non-test modules on round 3's measurement. Either make the
    derivation recursive and re-measure, or narrow the case name, the refusal wording and this block's
    prose to the set it actually reads. Prefer recursive: it removes a claim wider than its assertion,
    which is the remedy this repository already uses. If widening the set introduces owners, REPORT
    them — do not narrow back to make the number come out right. State whichever was chosen at the
    declaration with its re-measured module count.

    RE-DERIVE `SECTION_EXTENT_OWNERS` and `SECTION_EXTENT_OWNER_COUNT` by RUNNING the corrected
    derivation over the final tree in the same session, and write down whatever it reports. The block's
    own instruction is to write down the measurement rather than the prediction, and round 3's finding
    is that the prediction was written down last time. Re-derive `LOCATOR_CONSUMERS` and its count in
    the same pass — 29-28 changed which modules consume the authority and 29-32 may have added one.

    Add a SECOND planted locator beside `PLANTED_SIXTH_LOCATOR`, spelled with a whitespace class and
    with its bound deferred several lines from its recogniser — the exact two properties that hid
    `audit-model.ts` from both arms — and require the classifier to report it. Assemble it from
    character codes, as the existing plant is, so this file's own source can never become a member of
    the set it derives. A probe tied to the defect that actually shipped is worth more than an invented
    one; this plant is that defect, restated.

    Finally, record the correction in `docs/audit/29-locator-unification.md` as a NEW §9, and insert a
    one-line pointer at §3 directing a reader to it. Do NOT rewrite §3 in place: that document is a
    transcript of round 2's measurements and silently editing a transcript destroys the only record of
    what was believed when. §9 states what §3 measured, what round 3 found, what the corrected
    derivation reports, and every command re-run.
  </action>

  <verify>
    <automated>npm run build && npx vitest run scripts/check-foundation-guards.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - ADVERSARIAL SELF-REPRODUCTION, in the SUMMARY. Run the CORRECTED classifier over the pre-29-28
      source of `audit-model.ts` (`git show <pre-29-28-sha>:scripts/audit-model.ts`) and show it
      reporting that module as an owner, naming the site. Then run it over the final tree and show it
      absent. Both outputs pasted. Without this pair the widening is unproven and could be measuring
      nothing.
    - The widening is asserted in BOTH directions on planted inputs: at least six matching spellings
      (one hash / a run / a quantifier, each with a literal space and with a whitespace class), and at
      least two non-matching ones taken from the LIVE tree — the issue-reference pattern in
      `trace-render.ts:71` and the board-heading replace in `validate-agent-factory.ts:457` — each
      asserted absent from the derived owner set by NAME. A widening asserted only on its positive side
      is how 29-22's noise arrived.
    - `SECTION_EXTENT_OWNERS`, `SECTION_EXTENT_OWNER_COUNT`, `LOCATOR_CONSUMERS` and
      `LOCATOR_CONSUMER_COUNT` are all four the output of a derivation RUN in this session. Paste the
      run. If the owner count is not 1, do NOT edit the number to make the case pass — report it, name
      the module and the site, and escalate: a second owner after round 3 means LANG-07 is still false
      and the remedy is code, not a constant.
    - The `PLANTED_SIXTH_LOCATOR` probe still discriminates under the widened arm — shown failing when
      planted, passing when absent — and the NEW whitespace-class plant is reported by name. Paste both.
    - Floor item 4 no longer asserts a falsehood about this tree. Row-scoped so the §9 prose cannot
      satisfy it:
      `grep -a -n 'which no module in this tree uses today' scripts/check-foundation-guards.test.ts`
      returns nothing.
    - The module-set scope decision is recorded with its re-measured count, and the case name, the
      refusal wording and the block's prose all agree with the set actually read. State the count in
      the SUMMARY beside round 3's 41-of-49 measurement.
    - `docs/audit/29-locator-unification.md` gains a §9 and a pointer at §3; `git diff` on that file
      shows an APPEND plus one inserted pointer line, not a rewrite of §3's measurements.
    - `npm run build && npm run freshness` reports every committed `.js` fresh.
  </acceptance_criteria>

  <reversibility rating="reversible">
    A widened classifier arm, corrected prose, re-derived constants and an appended transcript section.
    Reverting restores the narrow arm; the pre-correction measurements survive in §3 precisely because
    §3 is not rewritten.
  </reversibility>

  <done>
    The classifier recognises the shape its own floor said nothing used, is proven not to have become
    noisy, and reports the owner set over the FINAL tree as a measurement taken this session. The
    planted probes discriminate in both directions and the transcript's stale claim is redirected rather
    than overwritten.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the oracle's invariants are REACHED, not merely satisfied</name>

  <read_first>
    - `scripts/section-locator-oracle.test.ts` in full — the seven axes and their labels, `wrapCandidate`,
      `ORDINARY_HEAD` / `ORDINARY_TAIL`, `buildCell`, `buildCorpus`, `endViolations`, `headViolations`,
      the two deliberately broken locators, `EXPECTED_CELLS`, the two pinned distinct-count numbers
      (2058 triples, 724 documents), the three premise cases and the two falsifiability probes
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` § WR-03 and § IN-01 —
      including the measurement that zero documents carry a second occurrence of the cell heading, and
      the recorded note that the fence-blind probe breaks I4 ONLY
    - `docs/audit/29-locator-unification.md` §4 — the axis table and the probe failure counts this task
      supersedes
    - `scripts/frontmatter.ts:504-539` — `unfencedHeadingIndex` returns the FIRST match; that ordering
      promise is the untested half
  </read_first>

  <files>scripts/section-locator-oracle.test.ts, docs/audit/29-locator-unification.md</files>

  <behavior>
    - At least one cell carries TWO unfenced occurrences of the cell heading, and at least one carries a
      fenced occurrence AND an unfenced one, in both orders. Asserted directly on the generated corpus,
      never inferred from an axis label.
    - Every invariant I1 to I6 is REACHED — its decision point is entered — in at least a pinned number
      of cells, and that reach count is derived by an expression written independently of the loop that
      collects violations.
    - A locator returning the LAST unfenced match instead of the first BREAKS I5. Under today's corpus
      it sweeps clean, which is exactly WR-03.
    - The existing two probes still break exactly what they broke, and the level-two-only close still
      breaks I2 at requested level 1 and I3 at requested level 2.
    - The shipped authority still sweeps clean over the GROWN corpus.
  </behavior>

  <action>
    Make the invariant reachable, then make its reachability a measured property rather than an
    assumption.

    ADD A DUPLICATE-OCCURRENCE AXIS. Members: no second occurrence (today's behaviour); a second
    occurrence of the same candidate spelling placed BEFORE the candidate and UNFENCED; and one placed
    before it INSIDE a terminated fence. The second member is what makes I5's loop body enterable at
    all; the third is the ordering discrimination the review says the sweep never generates — a document
    with a fenced occurrence AND an unfenced one, where a fence-blind scan picks the quoted line and the
    authority must pick the real one. Generate both orders.

    Every pinned number in the file moves and every one of them must be RE-DERIVED and re-pinned, not
    adjusted until the case passes: the axis-length assertions, `EXPECTED_CELLS`, the product, the
    counter-derived count, the array-length third witness, the distinct-triple count and the
    distinct-document count. Run them and write down what they report. Record the suite's wall-clock
    runtime before and after in the SUMMARY; if the sweep becomes materially slower, say so with the
    number rather than trimming an axis quietly.

    ADD A PER-INVARIANT REACH COUNT, and this is the load-bearing half. A vacuity floor catches an
    EMPTY denominator and has never caught a SILENTLY SHORT one — this project's own recorded lesson,
    and WR-03 is its newest instance. So: for each of I1 to I6, restate its PRECONDITION as a separate,
    independently written expression over the cell, count how many cells satisfy it, and assert each
    count is at least a pinned floor greater than zero. The restatement must be written from the
    invariant's DESCRIPTION rather than by instrumenting `endViolations` or `headViolations` — a reach
    counter living inside the loop it audits is the same expression counting itself, which is precisely
    what this task is correcting. The file already sets this precedent with its hand-written truth table
    for the heading rule; follow it.

    ADD A THIRD DELIBERATELY BROKEN LOCATOR: one returning the LAST unfenced match. Require it to break
    I5, and require the failure to name every axis value as the existing probes do.

    AND VERIFY THE REVIEW'S OWN SUGGESTION RATHER THAN ADOPTING IT. WR-03 recommends re-running the
    fence-blind probe and requiring it to break I5 as well as I4. Check whether that is achievable
    before asserting it: a first-raw-match locator cannot return a line that has an earlier UNFENCED
    occurrence, because an unfenced occurrence is itself a raw match. If it provably cannot reach I5,
    record that finding in the SUMMARY and in §9 with the reasoning, and let the last-match probe carry
    I5 instead. Do not write an assertion you cannot make true, and do not quietly drop the review's
    recommendation without saying why — both are the failure modes this round exists to refuse.

    FIX IN-01 in the same pass: the `end >= 0` conjunct in the I2 guard is unreachable-false, because
    `sectionEndIndex` returns either an index at least `Math.max(from, 0)` or the line count. Move it
    into I1, where a negative answer WOULD be a real violation for a future broken locator passed to
    `endViolations`, or drop it. In a file whose subject is invariants that cannot fail, a condition
    that cannot fail is noise.

    Record the outcome in `docs/audit/29-locator-unification.md`: append to §9 (created in Task 1) the
    corrected axis table, the new cell count, the per-invariant reach counts and the three probes' new
    failure counts, and insert a one-line pointer at §4. Do NOT rewrite §4's numbers in place — they are
    round 2's measurement and the record of what was believed when.
  </action>

  <verify>
    <automated>npm run build && npx vitest run scripts/section-locator-oracle.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - The corpus provably carries the documents the axis promises: at least one cell with TWO unfenced
      occurrences of its heading, and at least one with a fenced AND an unfenced occurrence in each
      order. Each asserted directly on the generated corpus with a count, not on an axis label. Paste
      the four counts.
    - Every invariant I1 to I6 has a REACH count derived by an independently written precondition
      expression, each pinned above zero. Paste all six numbers. I5's must be greater than zero — round
      3 measured it at zero over 7200 cells, and a non-zero number here is the closure condition.
    - The reach expressions do not call `endViolations` or `headViolations`. State the check that
      established this and paste it.
    - The last-match probe BREAKS I5, its failure names every axis key, and it has been SEEN failing.
      Paste the first failure line.
    - The two existing probes still break exactly what they broke, including the level-one/level-two
      attribution split the file already asserts. Paste the re-run.
    - The shipped authority still sweeps CLEAN over the grown corpus: zero violations across the new
      cell count, with the count derived twice (the product of pinned axis lengths, and a counter in
      the consuming loop) and both witnesses pasted.
    - The review's fence-blind-breaks-I5 recommendation is either implemented or explicitly refuted
      with its reasoning, in the SUMMARY and in §9. Silence on it is a failed task.
    - The I2 guard no longer carries an unreachable conjunct, and the SUMMARY names where it went.
    - Suite runtime before and after is recorded in the SUMMARY.
    - `docs/audit/29-locator-unification.md` §4 gains a pointer, not a rewrite: `git diff` on that file
      shows §4's original numbers intact.
  </acceptance_criteria>

  <reversibility rating="reversible">
    One added axis, one added probe, six reach expressions and re-pinned numbers, all inside a test
    module. Reverting shrinks the corpus back and loses the reach measurement.
  </reversibility>

  <done>
    I5 is reachable, is reached a pinned number of times, and is proven able to fail against a locator
    that returns the last match instead of the first. Every other invariant carries the same reach
    measurement, derived independently of the loop that collects violations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the duplicate-assertion tripwire stops reporting coverage it does not have</name>

  <read_first>
    - `scripts/check-foundation-guards.test.ts:6169-6330` — `ASSERTION_LINE`, `isAssertionLine`,
      `duplicateAssertionPairsIn`, `testModules()`, the per-module contribution floor, the zero-classified
      refusal, `PLANTED_DUPLICATE_SOURCE`, the separated-lines and differing-lines controls, and the
      recorded note that the published figure's first draft was wrong
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` § IN-03 — the live
      measurement: 4806 `expect(` occurrences across 47 test modules, 4751 lines classified, 453 of the
      classified lines multi-line openers
    - `docs/audit/29-locator-unification.md` §6 variant E2 — the tripwire reads 47 of 53 modules, and
      the premise-assertion discipline both E-variants follow
  </read_first>

  <files>scripts/check-foundation-guards.test.ts</files>

  <behavior>
    - The tripwire publishes, beside its classified total, how many of those lines are MULTI-LINE
      openers — the share for which a duplicated pair's opener lines are never adjacent and the pair is
      therefore invisible.
    - Both numbers are pinned two-sided against measurements taken in the same session, so the published
      figure cannot drift into reading as coverage it does not have.
    - A planted MULTI-LINE duplicate pair is REPORTED. Today it is not.
    - The existing controls still hold: separated lines are not a pair, differing lines are not a pair,
      a test boundary is never a classified assertion line, and a module contributing zero classified
      lines fails by name.
  </behavior>

  <action>
    The tripwire's published figure reads as coverage it does not have: for a multi-line `expect(` call
    the subject and matcher sit on following lines, so a duplicated assertion's opener lines are never
    adjacent and the pair is invisible. The floor's disclosed miss names the shape; the number beside it
    does not account for it.

    Do BOTH halves, and the second is what closes the class.

    FIRST, publish the denominator honestly. Derive the multi-line opener count with its own expression
    — a classified line whose call's parentheses do not balance on that line — and report it beside the
    classified total and beside the module count. Pin all three two-sided against numbers measured in
    this session. Derive the `expect(` occurrence total independently as well, so the classified total
    has something outside itself to be short against; round 3 measured 4806 occurrences against 4751
    classified lines, and a gap that moves silently is the shape this whole round is closing.

    SECOND, normalise before comparing. Join a multi-line `expect(` call into one logical line — read
    forward until the call's parentheses balance — and compare the normalised forms for adjacency.
    Adjacency then means "two consecutive ASSERTIONS", which is what the property was always about,
    rather than "two consecutive opener LINES". Keep the normalisation local to the tripwire; it is a
    classification helper for one derived scan, not a parser the tree needs an authority for, and say so
    at the declaration so nobody hoists it into `frontmatter.ts` by analogy.

    If normalisation proves to have a blast radius this plan cannot bound — for instance if it starts
    reporting pairs that are not duplicates — do NOT ship a partly-normalising classifier. Record the
    reasoned decision, keep the published multi-line share and its two-sided pin, and state the residual
    with its live number so a later phase decides on evidence. The published share is the part that is
    not optional; the normalisation is the part that may be deferred WITH ITS REASON.

    Add the planted case either way: a multi-line duplicate pair in a planted source. If normalisation
    shipped, the pair is REPORTED and the case asserts that. If it did not, the case asserts the pair is
    MISSED and names the residual — an intended verdict, asserted, rather than an absence read as
    coverage. That is the same discipline plan 29-31 applies to its own disclosed floor.

    Assert the scan's own premise before its claim, as variants E1 and E2 already do: run the classifier
    over the same module set the live case reads and require it to reproduce the live case's published
    answer before trusting any new number. A transcription that does not reproduce the known answer is
    measuring its own transcription errors.
  </action>

  <verify>
    <automated>npm run build && npx vitest run scripts/check-foundation-guards.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Four numbers are derived in this session and pinned two-sided: modules scanned, `expect(`
      occurrences, classified lines, multi-line openers. Paste all four beside round 3's measurement
      (47 modules, 4806, 4751, 453) and explain any difference; a difference is expected because this
      round adds cases, and an UNEXPLAINED difference is a finding.
    - The planted multi-line duplicate case exists and asserts the INTENDED verdict, whichever branch
      was taken. If normalisation shipped it is REPORTED; if not, it is MISSED and the residual is named
      with its live number. A silent third outcome is a failed task.
    - ADVERSARIAL SELF-REPRODUCTION: show the planted multi-line pair going UNREPORTED under the
      pre-task build. That is the bypass, demonstrated. Paste it.
    - The premise assertion fires: run the classifier over the live module set and show it reproducing
      the published answer before any new number is trusted. Paste the reproduction.
    - All four existing controls still pass and are named in the SUMMARY.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0 over the whole tree, with the pass count and
      wall-clock runtime recorded beside round 3's 1878 passed / 2 skipped / 52 files / 118s baseline.
    - All seven repo gates exit 0 on the live tree; paste the seven exit codes. This is the last plan of
      round 3, so this is the round's own closing measurement.
    - `git status --porcelain` shows no source file modified by a reproduction.
  </acceptance_criteria>

  <reversibility rating="reversible">
    Test-only: derived numbers, one local normalisation helper and its cases. Reverting restores the
    unaccounted figure.
  </reversibility>

  <done>
    The tripwire's published figure and its actual coverage are the same statement, the multi-line share
    is derived and pinned two-sided, and a multi-line duplicate pair has an asserted intended verdict
    rather than an unexamined absence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the live source tree -> the derived owner classifier | the classifier is the ONLY evidence for LANG-07's central claim; a rule that cannot see a construct reports a clean tree |
| the generated oracle corpus -> the invariant checkers | a corpus that cannot produce a shape leaves the invariant about that shape asserted and unexercised |
| a published harness figure -> a human reading a green build | a number that reads as coverage it does not have is an unfalsifiable claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-29-01 | Repudiation | `SECTION_EXTENT_OWNERS` green over a two-owner tree | critical | mitigate | Task 1 — the recogniser arm widened to the shapes floor item 4 named, re-derived over the FINAL tree, with a before/after pair against the pre-29-28 source |
| T-29-29-02 | Repudiation | floor item 4 asserting a falsehood about this tree | high | mitigate | Task 1 — corrected, with every remaining floor item re-checked against the final tree in the same pass |
| T-29-29-03 | Tampering | a widened classifier becoming noisy and being narrowed back until the answer comes out right | high | mitigate | Task 1 — two live non-matching sites asserted excluded by name; the widening asserted in both directions on planted inputs |
| T-29-29-04 | Repudiation | invariant I5 asserted 7200 times and never able to fail | high | mitigate | Task 2 — a duplicate-occurrence axis, per-invariant REACH counts derived independently, and a third broken locator proving I5 can fail |
| T-29-29-05 | Spoofing | a reach counter living inside the loop it audits | high | mitigate | Task 2 — each precondition restated as an independent expression, with a check that the reach expressions do not call the violation collectors |
| T-29-29-06 | Repudiation | the duplicate-assertion figure reading as coverage it does not have | medium | mitigate | Task 3 — the multi-line share derived, published and pinned two-sided; the planted multi-line pair carries an asserted intended verdict |
| T-29-29-07 | Information Disclosure | the derived scans reading a narrower module set than "tree-wide" (V-29-26-02) | medium | mitigate | Task 1 — either the read becomes recursive and is re-measured, or the case name, refusal wording and prose are narrowed to the set actually read |
| T-29-29-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed. Zero runtime dependencies is a CLAUDE.md constraint and this round adds none; recorded rather than skipped |

ASVS level 1, block threshold `high`. Every `critical` and `high` row is dispositioned `mitigate` with
a named task.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 29-29)

**`scripts/check-foundation-guards.test.ts`**
- `HEADING_RECOGNISER_CONSTRUCTS` widened to recognise an anchored hash RUN and a whitespace CLASS,
  with both directions asserted on planted inputs
- floor item 4 corrected, and every other floor item re-checked against the final tree
- the module derivation's scope decision (recursive, or the claim narrowed) with its re-measured count
- `SECTION_EXTENT_OWNERS`, `SECTION_EXTENT_OWNER_COUNT`, `LOCATOR_CONSUMERS`, `LOCATOR_CONSUMER_COUNT`
  — all four re-derived over the FINAL round-3 tree
- a SECOND planted locator, spelled with a whitespace class and with a deferred bound, assembled from
  character codes
- the duplicate-assertion tripwire's four derived numbers (modules, `expect(` occurrences, classified
  lines, multi-line openers), each pinned two-sided
- a local multi-line-`expect(` normalisation helper for the tripwire, OR the recorded decision not to
  ship it with the residual named and numbered
- the planted multi-line duplicate case, asserting the intended verdict either way
- the tripwire's premise assertion (reproduce the live published answer before trusting a new number)

**`scripts/section-locator-oracle.test.ts`**
- a new duplicate-occurrence axis (none / an earlier unfenced duplicate / an earlier fenced duplicate),
  generated in both orders
- re-derived and re-pinned: axis lengths, `EXPECTED_CELLS`, the product, the counter witness, the array
  witness, the distinct-triple count, the distinct-document count
- six independently written per-invariant REACH expressions and their pinned floors
- a THIRD deliberately broken locator returning the LAST unfenced match, required to break I5
- the I2 guard's unreachable conjunct moved into I1 or removed
- new premise cases asserting the corpus really carries two-occurrence documents in both orders

**`docs/audit/29-locator-unification.md`**
- a new **§9 — the round-3 corrections**, carrying: what §3 measured and what the corrected derivation
  reports; the corrected axis table, cell count, per-invariant reach counts and three probes' failure
  counts; and the verdict on the review's fence-blind-breaks-I5 recommendation
- one-line pointers inserted at §3, §4 and §6 directing a reader to §9. §3, §4 and §6's own numbers are
  NOT rewritten — they are round 2's record of what was believed when

**`29-29-SUMMARY.md`**
- the classifier before/after pair against the pre-29-28 `audit-model.ts`
- the widening's both-direction planted-input transcripts, including the two live exclusions by name
- the four re-derived owner/consumer constants with the run that produced them
- both planted-locator probe outcomes
- the four corpus-shape counts, the six reach counts, and the last-match probe's first failure line
- the review-recommendation verdict with its reasoning
- suite runtime before and after, the pass count, and the seven repo-gate exit codes — round 3's
  closing measurement
</artifacts_this_phase_produces>

<verification>
- `npm run build && npm run freshness` — every committed `.js` matches a fresh rebuild.
- `npx vitest run scripts/section-locator-oracle.test.ts scripts/check-foundation-guards.test.ts` exits 0.
- The corrected classifier reports `audit-model.ts` as an owner over its pre-29-28 source and not over
  the final tree.
- Every invariant I1 to I6 has a non-zero, pinned reach count; I5's is the closure condition.
- All seven repo gates exit 0 and `npx vitest run --exclude '**/scripts/e2e/**'` exits 0, with the pass
  count and runtime recorded against round 3's baseline.
- `node scripts/check-nul-bytes.js` exits 0, so the counting greps above are trustworthy.
</verification>

<success_criteria>
- LANG-07's enumerated `missing:` items 3 and 4 each map to a landed artifact, in a table in the
  SUMMARY, alongside items 1 and 2 attributed to plan 29-28.
- The owner set is a MEASUREMENT over a corrected rule, taken over the final round-3 tree, with the
  before/after pair proving the rule sees the defect that shipped.
- No floor in either harness asserts a falsehood about this tree.
- I5 is reached, pinned and proven able to fail; every other invariant carries the same treatment.
- The duplicate-assertion figure and its actual coverage are the same statement.
- Round 3's closing measurement — seven gates, suite pass count, runtime — is recorded.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-29-SUMMARY.md` when done.
</output>

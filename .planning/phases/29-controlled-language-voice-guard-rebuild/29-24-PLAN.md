---
phase: 29-controlled-language-voice-guard-rebuild
plan: 24
type: execute
wave: 23
depends_on: ["29-20", "29-23"]
gap_closure: true
files_modified:
  - scripts/check-imperative-lexicon.ts
  - scripts/check-imperative-lexicon.js
  - scripts/check-imperative-lexicon.test.ts
  - scripts/check-foundation-guards.test.ts
  - agent-factory/writing-profile.md
autonomous: false
requirements: [LANG-02, LANG-04, LANG-07]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "WR-08 arm four is closed: every section-extent question in `check-imperative-lexicon.ts` is answered by `scripts/frontmatter.ts`'s authority. The private `unfencedIndexOf` helper and the private `SECTION_HEADING_LINE` predicate are DELETED, and `deriveElements`' step anchor becomes membership in a range computed by `sectionEndIndex` rather than a per-line heading test of its own (LANG-07, D-24)."
    - "WR-04 is closed by DECISION, and the decision is recorded in both places it has to hold: Residual 1 is RETIRED. A `## Steps` section carries at least one list item — a real, decidable, enumerable rule the live corpus already satisfies at 19 of 19 files — and it ships as a new numbered rule in `agent-factory/writing-profile.md` beside WP-01 through WP-09 (LANG-01, LANG-04, D-12, D-39)."
    - "The refusal text names the RULE rather than the set arithmetic. `the step-heading file set and the bullet-bearing file set are not equal` tells an author what the gate computed; it now also tells them what to do, which is that a steps section must carry at least one list item."
    - "EDGE (LANG-04): a prose-only `## Steps` section is RED, by name, and that is now the documented intent rather than an accident of a denominator. A permanent case asserts the intended verdict, so the behaviour is pinned in the direction the profile states."
    - "WR-09 half two is FIXED: the ordered-marker arm that decides `procedural` outside any section anchor has its scope stated and its union with the anchored arm tested. After splitting a predicate into arms, the arms' UNION is what a case must cover, and until now neither arm's boundary was asserted."
    - "WR-09 half one is RECORDED AS RESIDUAL 4, with the CONCRETE reason rather than a difficulty judgement: teaching the one fence authority the four-space-indented code-block form would make it flag CommonMark sub-bullets indented four or more spaces as documentation, which reverses round 1's shipped CR-03 fix for the same guard. Two shipped requirements cannot both hold under that change, and that is a dependency conflict, not a hard problem. The residual names what would force the promote — the first governed workflow that legitimately carries an indented code block under `## Steps`."
    - "EDGE (LANG-04): a four-space-indented numbered line under `## Steps` has its verdict PINNED by a permanent case asserting the intended outcome, so the residual is a measured disclosure rather than an untested belief."
    - "WR-07 is closed: the case claiming to assert a property is renamed to what it is — an anti-regression tripwire over source spellings — and the behavioural property it claimed is asserted by a real planted case for the denominator that lacked one."
    - "Measured before the edit and re-derived after: 139 `## Steps` bullets across 19 files, 43 approved verbs, 76 derived Technical Names, 0 findings over 19/19 elements, 2166 sentences of which 414 procedural and 1752 descriptive, 0 findings over 47/47 elements, and 47 of 47 governed documents opened. Any movement in any of those numbers is a REAL change and is escalated with its cause, never absorbed."
    - "`scripts/check-imperative-lexicon.js` is rebuilt from its `.ts` source and committed in the same commit, so `npm run freshness` exits 0."
  prohibitions:
    - "Never let a guard's enforcement and its own recorded documentation contradict each other for an ordinary authoring shape — pick one answer and write it down in both places."
    - "Never record a residual whose reason is that the fix is hard; a residual is legitimate only when it names a concrete conflict, a missing input or a dependency, and this one names a shipped requirement it would reverse."
    - "Never narrow the denominator to make a red go away; the number moving is the acknowledgement, not the failure."
    - "Never keep a case whose name claims a property its body cannot express — rename it or make it assert the property, and this round does both."
    - "Never introduce a match-all regex or a nested quantifier into a prose scanner; the superlinear-backtracking incident that turned a 0.47 second guard into 383 seconds is dormant, not fixed."
    - "Never run bare `npm test` — it triggers the live claude-CLI e2e lane, spends the user's tokens and can hang for around eight minutes."
  artifacts:
    - scripts/check-imperative-lexicon.ts
    - scripts/check-imperative-lexicon.js
    - scripts/check-imperative-lexicon.test.ts
    - scripts/check-foundation-guards.test.ts
    - agent-factory/writing-profile.md
  key_links:
    - "`scripts/frontmatter.ts` exports `unfencedHeadingIndex` and `sectionEndIndex` (plan 29-20); `check-imperative-lexicon.ts` imports both plus `fencedLineFlags` and declares no section-extent predicate of its own."
    - "`scripts/check-foundation-guards.test.ts` pins this module's imported-symbol set; this is the LAST of the four consumer entries to move, so after this plan every entry has been reconciled with its own reason."
    - "`agent-factory/writing-profile.md` is NOT in the governed corpus (whose four parts are workflows, checklists, seed templates and contracts) and is NOT in the LANG-03 watched corpus, so the new rule row moves neither `GOVERNED_CORPUS_COUNT` (47) nor any disposition obligation. It IS registered in the claim registry, so `check-claim-anchors` must stay green."
---

<objective>
Move `check-imperative-lexicon.ts` onto the shared section-locator authority — the last of the four —
and settle the two places where this guard's enforcement disagrees with its own documentation.

Purpose: LANG-04 requires guards named for exactly the decidable subsets they check. Round 1's CR-03
fix correctly made the list marker depth-unbounded, and in doing so produced two scope mismatches:
the new set-equality refusal now REDs a prose-only `## Steps` section that this module's own
Residual 1 documents as out of scope, and the depth-unbounded markers admit lines inside four-space-
indented code blocks that the fence toggle cannot see. Neither is fail-open, and both mean the guard
is not enforcing exactly the subset its own naming and documentation claim — which is the standard
round 1 applied to the then-latent CR-03.

Output: one module with zero private section predicates, a retired residual replaced by a stated
rule, a newly recorded residual with a concrete reason, and three cases pinning verdicts that until
now nothing pinned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-20-SUMMARY.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-17-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: WR-04 — which of the two answers does the kit ship?</name>
  <decision>Resolve the contradiction between `guard_imperative_lexicon`'s enforcement and its own recorded Residual 1, for a `## Steps` section written as prose with no list marker.</decision>
  <context>
`29-VERIFICATION.md`'s LANG-04 gap says to "decide and record one answer" and names two. The guard
currently REDs a prose-only steps section, while Residual 1 in the same file documents that shape as
deliberately out of scope. The two statements cannot both stand, and the choice changes what the kit
tells every author — which is why it is a human decision rather than a planner's.

Measured on the live corpus: all 19 governed workflows carrying a `## Steps` heading contribute at
least one bullet, and the guard reports `0 findings over 19/19 elements`. Either answer is green
today; they differ in what happens to the next workflow somebody writes.

`agent-factory/writing-profile.md` currently carries rules WP-01 through WP-09, of which WP-09 is
advisory and the rest are marked decidable.
  </context>
  <options>
    <option id="retire-residual">
      <name>Retire Residual 1 — a steps section carries at least one list item, published as a new decidable rule in the writing profile</name>
      <pros>The guard's enforcement and its documentation agree. The rule is decidable, enumerable and already satisfied at 19 of 19. The denominator stays independent of the bullet loop, which is the WR-02 property round 1 landed. It follows the canonical-form doctrine this repository used to finally close Phase 27 after eleven rounds: declare the legal spelling and refuse the complement.</pros>
      <cons>It publishes a new constraint on every future workflow author, and a steps section written as explanatory prose becomes illegal rather than merely unmeasured. Reversing it later means unpublishing a rule agents have been following.</cons>
    </option>
    <option id="keep-residual">
      <name>Keep Residual 1 — derive the denominator from steps sections carrying at least one non-blank, non-heading line</name>
      <pros>No new authoring constraint. The documented out-of-scope shape stays out of scope.</pros>
      <cons>A prose-only steps section carries non-blank non-heading lines, so it would still sit in the denominator and still red — the contradiction survives unless the denominator instead counts bullet-shaped lines, which is the tautological denominator WR-02 exists to delete. The planner assesses this option as not actually resolving the contradiction, and records that assessment here rather than deciding for you.</cons>
    </option>
  </options>
  <resume-signal>Select: retire-residual or keep-residual. If keep-residual, state how the denominator avoids reading the array its own loop consumes, because the plan has no answer for that and Task 2 stops until it does.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the decision and the new residual, then RED-plant every verdict neither currently pins</name>
  <files>agent-factory/writing-profile.md, scripts/check-imperative-lexicon.ts, scripts/check-imperative-lexicon.test.ts</files>
  <read_first>
    - `scripts/check-imperative-lexicon.ts:68-89` — the four recorded residuals, of which Residual 1 is the one being retired and Residual 4 is the one being added.
    - `scripts/check-imperative-lexicon.ts:475-524` — `HEADING_LINE`, `STEPS_HEADING`, `SECTION_HEADING_LINE`, `LIST_MARKER` and `ORDERED_MARKER` with the CR-03 widening argument.
    - `scripts/check-imperative-lexicon.ts:763-838` — `deriveElements`, the anchor reset at line 793, the `stepsFiles` recording at line 796, and the `procedural` expression at line 807 where the ordered arm is applied with no section anchor.
    - `scripts/check-imperative-lexicon.ts:1242-1305` — `bulletFilesVisited`, `stepSetRefusal` and the refusal wording being extended.
    - `agent-factory/writing-profile.md:40-70` — the WP-01 through WP-09 rule table and its `decidable` / `advisory` column, which the new row joins.
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` — WR-04, WR-07 and WR-09 with their fix sketches.
  </read_first>
  <action>
Record the decision Task 1 returned, then settle the second contradiction, because a code change that
lands before a decision is a decision made by whoever typed fastest.

WR-04 — if Task 1 returned `keep-residual`, STOP and report: this task's remaining instructions are
written for `retire-residual`, and the alternative requires a denominator design the plan does not
have. If Task 1 returned `retire-residual`, proceed.

WR-04 — RETIRE Residual 1. Delete it from the residual block and state the replacement rule as a new
numbered row in `agent-factory/writing-profile.md`'s rule table, in the table's own voice and marked
`decidable`: a steps section carries at least one list item. Say in the profile's surrounding prose
why the rule exists — a steps section written as paragraphs is not measured by the imperative
predicate at all, so a heading with no bullets is a section that claims to hold procedure and holds
none. Renumber nothing else in the residual block; the remaining residuals keep their numbers and a
short note records that the first was retired by this plan and where the rule now lives, so a reader
meeting a gap in the numbering meets a decision rather than a deletion.

Then extend the refusal text in `stepSetRefusal`'s caller so it names the RULE and not only the set
arithmetic. An author reading a red should learn what to write, not only what the gate computed.

Confirm the live corpus already satisfies the new rule before writing it down: the guard reports 19
of 19 files carrying a steps heading contribute a bullet, so retiring the residual changes zero
verdicts today. Record that measurement in the SUMMARY. If it is not 19 of 19 at execution, STOP and
escalate — the rule is being written against a corpus that does not satisfy it.

WR-09 — RECORD Residual 4, with a concrete reason. The depth-unbounded markers also match lines
inside four-space-indented code blocks, which are not fenced, so the one fence toggle returns false
for them. The structural remedy the review sketches — teaching the toggle the indented form — would
make it flag every CommonMark sub-bullet indented four or more spaces as documentation, which
reverses round 1's shipped CR-03 fix for this same guard. Two shipped requirements cannot both hold
under that change. Write the residual so it states that conflict, states the direction (fail-closed,
a false red on correct text), states that the live corpus carries no such block today, and names what
would force the promote: the first governed workflow that legitimately carries an indented code block
under a steps heading. A residual whose reason is that the fix is hard is not a residual; this one
names a requirement it would break.

Then plant the RED evidence for every verdict that is currently unpinned:

A prose-only steps section in a governed workflow mirror. Assert exit 1 and assert the refusal names
the file AND the new rule. Today the gate reds for this shape by accident of the denominator, so the
case may pass at HEAD before the refusal wording lands — record which, and either way the case is
kept, because "the behaviour is right and nothing pins it" is exactly the shape WR-04 is about.

A four-space-indented numbered line under a steps heading in a governed workflow mirror. Assert the
INTENDED verdict — the residual's disclosed one, that the line is counted as a step bullet — and name
the case for the residual so a future reader finds the disclosure from the test. Assert the bullet
count moved by exactly one against the same mirror without the plant, so the case measures the
admission rather than merely the exit code.

A four-space-indented numbered line OUTSIDE any steps section, asserting the ordered arm's unanchored
verdict, and a case asserting the UNION of the two arms on a line that satisfies both. After a
predicate is split into arms, its arms' union is what a case must cover.

Capture every HEAD transcript verbatim. Do not touch the locators in this task.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-imperative-lexicon.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `agent-factory/writing-profile.md` carries one new numbered rule row in the existing table, marked `decidable`, and its surrounding prose states the reason. `node scripts/check-claim-anchors.js`, `node scripts/check-banned-claims.js` and `node scripts/check-public-docs-vocabulary.js` each exit 0 after the edit.
    - Residual 1 is gone from `scripts/check-imperative-lexicon.ts` and a note records where the rule now lives; Residual 4 exists and its stated reason names the CR-03 reversal, the direction, the live-corpus fact and the trigger that would force the promote.
    - `node scripts/check-imperative-lexicon.js` reports `0 findings over 19/19 elements` before the residual is retired, recorded in the SUMMARY; if it is not 19 of 19, execution stops and escalates.
    - Each of the four new cases runs at HEAD with its received value recorded — prose-only steps section, indented numbered line under a steps heading with a bullet-count delta of exactly one, indented numbered line outside a steps section, and the union case.
    - `agent-factory/writing-profile.md` is confirmed absent from both `GOVERNED_CORPUS_PARTS` and the LANG-03 watched corpus, so `GOVERNED_CORPUS_COUNT` stays 47 and no disposition row is owed. State the confirming commands in the SUMMARY.
  </acceptance_criteria>
  <done>Both contradictions are settled in writing, the corpus is confirmed to satisfy the new rule, and every previously-unpinned verdict has a case.</done>
  <reversibility rating="one-way">Retiring a recorded residual and publishing its replacement as a numbered writing-profile rule changes what the kit tells every author. Reversing it later means unpublishing a rule agents have been following, which is why the decision is written and measured before any code moves.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Every section-extent question in the module goes to the shared authority</name>
  <files>scripts/check-imperative-lexicon.ts, scripts/check-imperative-lexicon.js</files>
  <read_first>
    - `scripts/frontmatter.ts` — `unfencedHeadingIndex` and `sectionEndIndex` as landed by plan 29-20, with their recorded scope.
    - `scripts/check-imperative-lexicon.ts:145` — the existing import from `./frontmatter.js`.
    - `scripts/check-imperative-lexicon.ts:598-677` — the WR-06 argument block, the private `unfencedIndexOf` helper, `tableFirstCellsUnderHeading` and `boardColumns`.
    - `scripts/check-imperative-lexicon.ts:679-708` — `TECHNICAL_NAME_PARTS`, `technicalNames`, `TECHNICAL_NAMES_COUNT = 76`. The locator change must not move that number.
    - `scripts/check-imperative-lexicon.ts:763-838` — `deriveElements` and its streaming step anchor.
  </read_first>
  <action>
Delete this module's remaining private section predicates and route every one of its questions to the
shared authority.

`unfencedIndexOf` is a private first-index-of-an-unfenced-heading helper — exactly what
`unfencedHeadingIndex` is. Delete it and call the authority at both of its call sites. Note in the
comment that the authority normalizes the heading comparison with `trimEnd()` while this helper used
exact equality, so a heading carrying one trailing space is now found; that is the axis on which the
four deleted predicates disagreed and it is now answered once. Re-derive `TECHNICAL_NAMES_COUNT` and
confirm it is still 76 — if it moved, a trailing-whitespace heading exists in one of the two table
sources and the movement is reported, never absorbed.

`tableFirstCellsUnderHeading`'s section terminator becomes `sectionEndIndex` with a level of two.
Delete its own heading-prefix test.

`boardColumns` terminates at the first line that is not a table row, which is a DIFFERENT question
from where a section ends — it is asking where a table ends. Leave it, and state that difference in
one sentence beside it, so a later reader does not "unify" two predicates that were never the same.

`deriveElements`' step anchor is the last private section predicate. Replace the per-line
`SECTION_HEADING_LINE` reset with a per-file range computation: locate every unfenced steps heading
using the module's own steps anchor (which is this module's legitimate question — which heading is
the anchor — and not the section-end question), call `sectionEndIndex` for each with a level of two,
and let the streaming loop ask whether the current index falls inside one of those ranges. Then delete
`SECTION_HEADING_LINE`. Keep `HEADING_LINE`, which answers "is this line a heading at all" for the
purpose of excluding headings from sentences; that is a third, genuinely different question and its
difference is stated in one sentence.

Record in the module's WR-06 argument block that the block's own claim has been upgraded: it said the
two table locators were the last place answering a section-extent question with a second grammar, and
the step anchor was a third. That correction is the point of the note — a prose claim wider than the
assertion behind it is this repository's recorded second systemic failure class.

Rebuild and commit `scripts/check-imperative-lexicon.js` in the same commit.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-imperative-lexicon.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/check-imperative-lexicon.test.ts --exclude '**/scripts/e2e/**'` exits 0, including every case Task 1 planted.
    - `node scripts/check-imperative-lexicon.js` exits 0 and reports `139 \`## Steps\` bullet(s) across 19 file(s)`, `43 approved verb(s)`, `76 derived Technical Name(s)`, `0 findings over 19/19 elements`, `2166 sentence(s) — 414 procedural, 1752 descriptive`, `0 findings over 47/47 elements` and `47 of 47 opened`. Every number is compared against the pre-edit measurement and any movement is escalated in the SUMMARY with its cause.
    - The module declares no section-end predicate of its own; the derived locator-site scan added in plan 29-22 is extended to cover this module in plan 29-25 and must report zero members here.
    - `npx tsc --noEmit` and `npm run freshness` exit 0.
  </acceptance_criteria>
  <done>Every section-extent question in the module is answered by the shared authority, and the module's own claim about its locators is corrected.</done>
</task>

<task type="auto">
  <name>Task 4: WR-07 — the case that claims a property asserts one, and the last import pin moves</name>
  <files>scripts/check-imperative-lexicon.test.ts, scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - `scripts/check-imperative-lexicon.test.ts:1116-1165` — the WR-02 describe block, the behavioural case that DOES plant a fixture, and the case immediately after it whose name claims to assert the property while its body compares source substrings.
    - `scripts/check-foundation-guards.test.ts:663-754` — the consumer list and the last unreconciled per-module symbol equality.
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` — WR-07's two acceptable outcomes.
  </read_first>
  <action>
Make the case's name and its body agree, and add the assertion the name promised.

Rename the source-substring case to what it is: an anti-regression tripwire that the tautological
denominator spellings are absent from the module's own text. Say in its body's comment that a
substring test cannot express the property — renaming a variable, extracting a local, or reformatting
across a line break all reintroduce the defect with the case still green — and that it is kept as a
cheap tripwire beside the real assertion rather than in place of it.

Then add the real assertion for the denominator that lacks one. The lexicon guard's behavioural case
already exists directly above: it plants a governed file that opens a steps section and contributes
no bullet and requires the mismatch to fire. The sentence-form guard's denominator has only the
substring claim. Plant its behavioural sibling: a governed file present in the derived corpus that
yields no sentence, asserting the mismatch fires, names the file, and moves the published denominator
by exactly one. Assert the fixture's premise first — that the planted file really is a member of the
mirror's derived corpus — because a plant outside the corpus would make the case pass for the wrong
reason.

Then move the last import pin. In `scripts/check-foundation-guards.test.ts`, update this module's
imported-symbol equality to the three symbols it now takes, sorted as `importedSymbols` returns them,
and extend the comment in the file's established style. Record that this is the fourth and final
consumer to move, that all four now take declarations and none takes a verdict, and that the pin
moving four times in one round is the pin working. Confirm the tree-wide grant-predicate zero holds.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-imperative-lexicon.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/check-imperative-lexicon.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'` exits 0.
    - The renamed tripwire case's name no longer claims to assert a property, and a new behavioural case plants a corpus member that yields no sentence, asserts the mismatch fires by name, and asserts the published denominator moved by exactly one.
    - The new behavioural case asserts its fixture's corpus membership before invoking the gate.
    - All four consumer entries in the frontmatter symbol pin now list their post-rewire symbol sets, and the consumer list itself is unchanged at its existing eight members.
    - All four gates exit 0 on the live tree with their pre-edit numbers, recorded in the SUMMARY.
    - `npm run freshness` and `npx tsc --noEmit` exit 0; `git status --porcelain` shows only this plan's files plus the three pre-existing out-of-scope entries.
  </acceptance_criteria>
  <done>The case that claimed a property now has one, and every one of the four consumers has its imported-symbol set pinned with its own recorded reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored workflow markdown → `guard_imperative_lexicon` verdict | An authoring shape the guard does not measure passes silently; an authoring shape it over-admits reds correct text. Both make the guard's published name false. |
| `agent-factory/writing-profile.md` rule table → every agent authoring kit prose | The profile is the instruction the whole factory follows. Publishing a rule the corpus does not satisfy would put every author immediately out of conformance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-24-01 | Repudiation | enforcement contradicting the module's own recorded residual | high | mitigate | Residual 1 is retired and its replacement rule is published in the profile; the corpus is measured to satisfy the rule before it is written, and execution stops if it does not. |
| T-29-24-02 | Denial of service (false red) | depth-unbounded markers matching indented code-block lines | medium | accept | Recorded as Residual 4 with a concrete reason: the structural remedy would reverse round 1's shipped CR-03 fix for this same guard. The direction is fail-closed, the corpus carries no such block today, and the verdict is pinned by a permanent case so the disclosure is measured rather than believed. |
| T-29-24-03 | Tampering | a fourth private section grammar surviving in this module | high | mitigate | All three private predicates are deleted and every section-extent question routes to the shared authority; plan 29-25's derived scan proves it rather than asserting it. |
| T-29-24-04 | Information disclosure (false assurance) | a case whose name claims more than its body asserts | medium | mitigate | The substring case is renamed to what it is and the behavioural assertion it claimed is added, with its fixture premise asserted first. |
| T-29-24-05 | Elevation of privilege | the trailing-whitespace normalization admitting a heading the module previously ignored | medium | mitigate | `TECHNICAL_NAMES_COUNT` is re-derived and required to stay at 76; movement is reported rather than absorbed. |
| T-29-24-SC | Tampering | npm / pip / cargo installs | high | mitigate | No package-manager install occurs in this plan; the dependency set is unchanged and the package-legitimacy gate has an empty input set. Escalate rather than install if that changes. |
</threat_model>

<artifacts_this_phase_produces>
New content in `agent-factory/writing-profile.md`:
- one new numbered rule row in the existing rule table, marked `decidable`, stating that a steps
  section carries at least one list item, with its reason in the surrounding prose

Symbols DELETED from `scripts/check-imperative-lexicon.ts`:
- `unfencedIndexOf` (private helper)
- `SECTION_HEADING_LINE` (private predicate)
- the recorded Residual 1 block

New content in `scripts/check-imperative-lexicon.ts`:
- a recorded Residual 4 covering the indented-code-block admission, with its conflict, its direction,
  its live-corpus fact and its promote trigger
- a per-file steps-section range derivation consuming `sectionEndIndex`
- extended refusal text naming the new rule

New test names (`scripts/check-imperative-lexicon.test.ts`):
- a prose-only steps section asserting exit 1 with the rule named
- a four-space-indented numbered line under a steps heading, asserting the residual's disclosed
  verdict and a bullet-count delta of exactly one
- a four-space-indented numbered line outside any steps section, asserting the ordered arm's verdict
- a case asserting the union of the two procedural arms
- a behavioural case for the sentence-form denominator, planting a corpus member that yields no
  sentence

Renamed test in `scripts/check-imperative-lexicon.test.ts`:
- the source-substring case, renamed to describe the tripwire it is

Modified test in `scripts/check-foundation-guards.test.ts`:
- this module's entry in the frontmatter consumer-symbol pin moves from one symbol to three

Regenerated committed build output: `scripts/check-imperative-lexicon.js`.

No new files and no new npm dependencies. `agent-factory/writing-profile.md` is not a member of the
LANG-03 watched corpus, so no disposition row is owed under `docs/audit/29-style-dispositions/` for
this plan. Do not invent one.
</artifacts_this_phase_produces>

<verification>
1. The live corpus is measured at 19 of 19 steps files contributing a bullet BEFORE the residual is retired; execution stops otherwise.
2. Every Task 1 case's HEAD behaviour is recorded, whether it passed or failed and with what received value.
3. `npx vitest run scripts/check-imperative-lexicon.test.ts scripts/check-foundation-guards.test.ts scripts/frontmatter.test.ts --exclude '**/scripts/e2e/**'` exits 0.
4. All four gates plus `check-claim-anchors` and `check-public-docs-vocabulary` exit 0, and `check-imperative-lexicon` reports its seven pre-edit numbers unchanged.
5. `npm run freshness` and `npx tsc --noEmit` exit 0.
</verification>

<success_criteria>
- The guard's enforcement and its documentation agree for every authoring shape either one names.
- The module declares no section-end predicate of its own.
- Residual 4 names a concrete conflict and a promote trigger, and its verdict is pinned by a case.
- Every case's name is a claim its body can support.
- The live verdict is byte-unmoved on all seven published numbers.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-24-SUMMARY.md` when done.
Include the pre-retirement 19-of-19 measurement, every HEAD transcript, the new profile rule verbatim,
Residual 4 verbatim, and the seven gate numbers before and after.
</output>

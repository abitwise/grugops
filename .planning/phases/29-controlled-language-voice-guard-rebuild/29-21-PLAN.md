---
phase: 29-controlled-language-voice-guard-rebuild
plan: 21
type: execute
wave: 20
depends_on: ["29-20"]
gap_closure: true
files_modified:
  - scripts/check-audit-register.ts
  - scripts/check-audit-register.js
  - scripts/check-audit-register.test.ts
  - scripts/check-diff-disposition.ts
  - scripts/check-diff-disposition.js
  - scripts/check-diff-disposition.test.ts
autonomous: true
requirements: [LANG-03]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CR-01 is closed AT ITS SOURCE: `check-audit-register.ts` asserts SET equality, in both directions, between the register's counted rows whose `safety_surface` is `yes` and the derived kit file set it already computes. A `no` on a role or workflow row becomes a NAMED refusal rather than a silent de-scoping of the entire LANG-03 watched corpus (D-18)."
    - "CR-01 is closed AT ITS CONSUMER as well, so `guard_diff_disposition` does not depend on a sibling gate having run: the watched corpus carries a minimum-cardinality pin derived from `ROLE_COUNT + WORKFLOW_COUNT` — the same two-sided shape `POSITIVE_GUARD_LITERAL_COUNT` and the four `cardinalities` already use in this file (D-01)."
    - "The element count is derived INDEPENDENTLY of the loop that consumes it: the floor's expectation comes from `kit-model.ts`'s listers, which `watchedCorpus()` never calls, so a narrowing edit to the register cannot also move the number it is checked against. A vacuity floor catches an EMPTY denominator and never a SILENTLY SHORT one, and the existing zero-check is exactly that weaker floor."
    - "The reviewer's end-to-end reproduction is made permanent and hermetic: a harness case flips ONE `safety_surface` cell from `yes` to `no` in a mirrored register, regenerates the exclusion list, and requires exit 1 from BOTH gates — not merely from the one whose corpus shrank."
    - "EDGE (LANG-03): the register file itself is not a member of the corpus it derives, and that asymmetry is STATED in the refusal text rather than left for a reader to work out — the edit that performs the narrowing owes no disposition row, which is why a floor is required at all (D-05)."
    - "EDGE (LANG-03): the floor is a MINIMUM, not an equality, at the consumer, because the union legitimately carries public-document entries beyond the derived kit; the source-side assertion is the equality, and the two are reported separately so neither number absorbs the other's drift."
    - "The refusal names the remedy and forbids the wrong one: it says to walk the register's `safety_surface` column, and it says plainly that lowering the expectation is never the fix — the same wording discipline the sibling `cardinalities` refusal already carries."
    - "Measured before the edit and re-derived after: the live watched corpus is 40 markdown files of a 41-entry union, 37 watched files changed since 4d2b8f0, 1880 changed clauses, 1532 disposition rows across 8 files, 0 findings over 37/37 elements, and `check-audit-register` exits 0. This fix does not move the live verdict; its proof is planted, never corpus-derived."
    - "`scripts/check-audit-register.js` and `scripts/check-diff-disposition.js` are rebuilt from their `.ts` sources and committed in the same commit, so `npm run freshness` exits 0."
  prohibitions:
    - "Never reach green by lowering a floor or narrowing the watched corpus — both clear a finding by deleting its evidence, and this gate's own base file says so in its own words."
    - "Never replace the existing zero-length check with the new floor and call it moved; a vacuity floor and a cardinality pin answer different questions and both stay."
    - "Never derive the floor's expectation from anything the register can move; if the expectation and the measurement share a source, the pin is documentation of intent rather than a check."
    - "Never treat one gate exiting 1 as proof that both do — the reproduction showed FOUR gates exiting 0 together, so the harness asserts more than one."
    - "Never run bare `npm test` — it triggers the live claude-CLI e2e lane, spends the user's tokens and can hang for around eight minutes."
  artifacts:
    - scripts/check-audit-register.ts
    - scripts/check-audit-register.js
    - scripts/check-audit-register.test.ts
    - scripts/check-diff-disposition.ts
    - scripts/check-diff-disposition.js
    - scripts/check-diff-disposition.test.ts
  key_links:
    - "`scripts/generate-safety-surface.ts` (`safetySurfaceUnion`) is the derivation both gates read; it is NOT modified, because the defect is an unconstrained input to it rather than a defect in it."
    - "`scripts/kit-model.ts` exports `ROLE_COUNT` (17) and `WORKFLOW_COUNT` (19); their sum is the consumer-side floor and is the one number the register cannot move."
    - "`scripts/check-audit-register.ts:160` already computes `derived` as the sorted role-plus-workflow path list for equality one; the new equality reuses that same value rather than re-deriving it."
---

<objective>
Close CR-01 — the one set in `guard_diff_disposition` that is not pinned two-sided — at both its
source and its consumer.

Purpose: LANG-03 requires a named safety-surface exclusion list to be HONOURED. Today the entire
left-hand side of the gate that enforces it can be narrowed by editing one table cell in
`docs/audit/28-disposition-register.md`, a file that is not itself watched, so the narrowing edit
owes no disposition row. The round-2 reviewer reproduced this end to end on the live tree: reword a
frozen `## Hard limits` sentence in `agent-factory/roles/uat-planner.md`, flip one cell from `yes`
to `no`, regenerate, and all four gates exit 0. The gate's own finding text already says "do NOT
narrow the watched corpus" — a prohibition with no mechanism, which is exactly what this milestone
exists to stop shipping.

Output: a set-equality refusal in `check-audit-register.ts`, a derived minimum-cardinality pin in
`check-diff-disposition.ts`, and a hermetic harness case that flips one cell and requires both gates
to go red.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-20-SUMMARY.md
@scripts/generate-safety-surface.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED first — plant the reviewer's narrowing end to end and prove all four gates stay green</name>
  <files>scripts/check-diff-disposition.test.ts, scripts/check-audit-register.test.ts</files>
  <read_first>
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` — CR-01's three reproduction steps and its two fix sketches, including the exact register line the reviewer flipped and the transcript each gate printed.
    - `scripts/check-diff-disposition.test.ts:90-440` — `makeMirror`, `mirrorWriters`, `makeDivergentMirror` and `runGate`, including which files a mirror plants and which it does not. The mirror's own premise must be asserted before a case built on it means anything.
    - `scripts/check-audit-register.test.ts:25-180` — the mirror builder and `runGate` for this gate; the two harnesses are separate and neither inherits the other's assumptions.
    - `scripts/generate-safety-surface.ts:73-101` — `safetySurfaceUnion`, the union of register rows flagged `safety_surface: yes` and registry rows of `kind: safety`.
    - `scripts/check-diff-disposition.ts:1275-1345` — `watchedCorpus` and the zero-length check that is its only floor.
  </read_first>
  <action>
Write the failing evidence before writing any fix, and write it so it fails for the reason CR-01
names rather than for an incidental one.

Add a case to `scripts/check-diff-disposition.test.ts` that builds a mirror whose register carries a
`safety_surface` value of `no` on exactly one COUNTED row naming a role file, regenerates the
exclusion list into the mirror from that register, and runs the gate. Assert exit 1 and assert the
refusal names the count it derived and the count it expected. At HEAD this case fails with a received
status of 0.

Assert the case's own PREMISE first, in the same case, before the gate runs: that the mirror's
register really does carry the flipped cell, that the regenerated exclusion list really does have one
fewer markdown entry than the unflipped control, and that the flipped file really is a role file the
control's corpus contained. A harness whose fixture silently stopped reproducing the defect would
otherwise pass for the wrong reason, and this project has recorded six instances of exactly that
across four straight rounds.

Add the paired CONTROL in the same describe block: the identical mirror with no cell flipped exits 0.
Without the control the case cannot distinguish "the flip was caught" from "this mirror always reds".

Add a case to `scripts/check-audit-register.test.ts` over the same flipped register asserting exit 1
and a refusal that names the file whose flag was flipped. At HEAD this case also fails with a
received status of 0 — this gate does not constrain the column's VALUES at all today, only the
unfilled marker.

Capture both HEAD transcripts verbatim for the SUMMARY. Do not fix anything in this task.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-diff-disposition.test.ts scripts/check-audit-register.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - At HEAD, `npx vitest run scripts/check-diff-disposition.test.ts -t "safety_surface"` FAILS, and the failure shows a received exit status of 0. The transcript is pasted into the SUMMARY.
    - At HEAD, `npx vitest run scripts/check-audit-register.test.ts -t "safety_surface"` FAILS with a received exit status of 0. The transcript is pasted into the SUMMARY.
    - The paired control case (no cell flipped) PASSES at HEAD, proving the mirror is not universally red.
    - Each new case asserts its fixture's premise — the flipped cell, the shortened union, and the flipped file's membership in the control corpus — before invoking the gate.
  </acceptance_criteria>
  <done>The reviewer's live reproduction exists as two hermetic failing cases plus a passing control, with both HEAD transcripts recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Close the hole at its source — set equality between flagged rows and the derived kit</name>
  <files>scripts/check-audit-register.ts, scripts/check-audit-register.js</files>
  <read_first>
    - `scripts/check-audit-register.ts:140-240` — `derivedRoles`, `derivedWorkflows`, the sorted `derived` list at line 160, the `counted` / `uncounted` split at lines 176-177, the uncounted-arm pin at lines 208-219, and equality one at lines 228-239. The new assertion sits beside equality one and reuses `derived` rather than re-deriving it.
    - `scripts/check-audit-register.ts:313-323` — the existing `safety_surface` arm, which refuses only the unfilled marker. That arm stays; the new one is a second, independent question about the same column.
    - `scripts/check-audit-register.ts:241-274` — equality two, and the recorded reason it is reported SEPARATELY from equality one ("neither number may absorb the other's drift"). The new equality follows that same rule.
  </read_first>
  <action>
Add a third equality to `check-audit-register.ts`, reported independently of the two that already
exist, asserting in BOTH directions that the set of counted register rows whose `safetySurface` value
is `yes` equals the `derived` role-plus-workflow path list computed at line 160.

Report the two directions as different defects, in this file's established style. A derived kit file
whose row carries `no` is a file REMOVED from the LANG-03 watched corpus entirely, and it is removed
with no failure anywhere: `guard_diff_disposition` simply checks less and stays green. A flagged row
naming a file the listers do not derive is a row that wandered outside the audited set. Name every
member of each direction; a mismatch that names no member is a number a reader cannot act on, which
is this repository's own recorded rule for a per-part assertion.

State in the refusal text the asymmetry that makes this hole invisible: the register lives under
`docs/` and is therefore not itself a member of the corpus it derives, so the edit that narrows the
corpus owes no disposition row. That sentence is the reason the assertion exists and it belongs in
the message a person reads when it fires, not only in a comment.

Say plainly in the refusal that the remedy is to restore the flag or to justify the exception with
its reason — following the shape the uncounted-arm pin already uses — and that lowering or deleting
this assertion is never the fix.

Deliberately do NOT touch the unfilled-marker arm at lines 313-323 and do NOT touch
`scripts/generate-safety-surface.ts`. The generator is correct; its input was unconstrained.

Extend the gate's PASS line to state the new equality with its measured numbers, because a PASS line
must never state a check that was not performed and must never omit one that was. Both numbers come
from the run that just happened.

Rebuild and commit `scripts/check-audit-register.js` in the same commit.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-audit-register.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/check-audit-register.test.ts --exclude '**/scripts/e2e/**'` exits 0, including the previously-failing flipped-cell case.
    - `node scripts/check-audit-register.js` exits 0 on the live tree, and its PASS line names the new equality with its measured counts.
    - The refusal text, captured from the failing mirror, names the flipped file, names both directions separately, and contains the sentence explaining that the register is not itself watched.
    - The unfilled-marker arm and `scripts/generate-safety-surface.ts` are byte-unchanged, confirmed by `git diff --stat`.
    - `npm run freshness` exits 0.
  </acceptance_criteria>
  <done>A `no` on a derived kit file's row is a named, two-directional refusal naming the file, and the live register still passes.</done>
  <reversibility rating="reversible">The new equality is additive and independently reported; if the derived kit legitimately gains a file that must not be a safety surface, the expectation is widened with its reason using the shape `PUBLIC_DOCS_EXEMPT` already establishes.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Pin the watched corpus at the consumer, derived independently of the loop that consumes it</name>
  <files>scripts/check-diff-disposition.ts, scripts/check-diff-disposition.js, scripts/check-diff-disposition.test.ts</files>
  <read_first>
    - `scripts/check-diff-disposition.ts:1275-1345` — `watchedCorpus`, its consumption at the corpus block, and the zero-length check that is its only floor today.
    - `scripts/check-diff-disposition.ts:1300-1324` — the four `cardinalities` refusals and the `POSITIVE_GUARD_LITERAL_COUNT` refusal. These are the two-sided shape the new pin copies, including their wording discipline ("the remedy is never to lower the expectation").
    - `scripts/kit-model.ts:107-108` and `:184-191` — `ROLE_COUNT = 17`, `WORKFLOW_COUNT = 19`, `ROLES_SUBPATH`, `WORKFLOWS_SUBPATH`. The floor's expectation comes from here, a module `watchedCorpus()` does not call.
    - `scripts/check-diff-disposition.ts:156` — the existing import line, so the new import is added rather than a second import statement created.
  </read_first>
  <action>
Add a minimum-cardinality pin to the watched corpus, and derive its expectation from a source the
narrowing edit cannot also move.

Export a constant from `check-diff-disposition.ts` whose value is `ROLE_COUNT + WORKFLOW_COUNT`,
imported from `kit-model.js`. That sum is 36 today against a live watched corpus of 40 markdown
files, so the pin is a MINIMUM rather than an equality: the union legitimately carries public
documents beyond the derived kit. State that distinction in the declaration comment, and state why
the expectation is imported rather than computed here — `watchedCorpus()` reads `safetySurfaceUnion`,
which reads the register, so a floor computed from the same path would move whenever the thing it
checks moves, which is documentation of intent rather than a check.

Add the refusal immediately after the existing zero-length check, and KEEP that check. The two
answer different questions: the zero check catches a vacuous corpus, the new pin catches a silently
short one, and a vacuity floor has never been able to catch the second. Say that in the comment, in
one sentence, because conflating them is precisely how this defect survived.

Write the refusal so it names the derived number, the expected minimum, the two components of the
minimum, and the mechanism: a `safety_surface` flag flipped to `no` removes a file from this gate
entirely and owes no disposition row, because the register is not itself watched. Direct the reader
to walk the register's column before moving the number, and say that lowering the expectation is
never the remedy.

Extend the corpus line the gate already prints so the floor and its expectation are visible on every
green run, not only on a red one — this gate publishes its measurements rather than asserting them.

In `scripts/check-diff-disposition.test.ts`, add a case proving the new pin can fail INDEPENDENTLY of
the source-side equality: a mirror whose exclusion list is short by one markdown entry while its
register is otherwise consistent must red at this gate. A pin that only ever fires when a sibling
gate already fired is a pin that has never been shown to work.

Rebuild and commit `scripts/check-diff-disposition.js` in the same commit.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-diff-disposition.test.ts scripts/check-audit-register.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/check-diff-disposition.test.ts scripts/check-audit-register.test.ts --exclude '**/scripts/e2e/**'` exits 0, including Task 1's previously-failing cases.
    - `node scripts/check-diff-disposition.js` exits 0 and its corpus line reports the watched count, the union size and the derived minimum with its two components.
    - `node scripts/check-diff-disposition.js` still reports `37 watched file(s) changed since 4d2b8f0; 1880 changed clause(s) derived; 1532 disposition row(s) across 8 file(s)` and `0 findings over 37/37 elements`. Any movement is escalated in the SUMMARY with its cause, never absorbed.
    - The independent-failure case reds at `check-diff-disposition` on a mirror whose register is consistent, proving the consumer-side pin is not merely downstream of the source-side equality.
    - The existing zero-length check is still present and still reachable, proven by the harness case that already covers it.
    - All four gates exit 0 on the live tree; `npm run freshness` and `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>The watched corpus is pinned two-sided from a source the register cannot move, the pin is proven to fire on its own, and the live verdict is unmoved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/audit/28-disposition-register.md` (hand-maintained table) → `safetySurfaceUnion` → `guard_diff_disposition` | A hand-edited table cell decides the entire left-hand side of a merge-gating check. The register is not itself watched, so the narrowing edit is unaccounted. |
| author's working tree → four gates' exit codes | The reproduced attack is a legitimate-looking two-line edit that both reduces protection and removes the evidence, with every gate reporting success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-21-01 | Tampering | `watchedCorpus()` and the `safety_surface` column | critical | mitigate | Source-side set equality in `check-audit-register.ts` makes a `no` on a derived kit row a named refusal; consumer-side minimum-cardinality pin derived from `kit-model.ts` makes the gate independent of that sibling having run. |
| T-29-21-02 | Repudiation | the narrowing edit owes no disposition row | high | mitigate | The refusal text names the asymmetry explicitly, so the person who meets the red learns why a floor was needed rather than treating it as an arbitrary number. |
| T-29-21-03 | Information disclosure (false assurance) | the existing zero-length floor | high | mitigate | The zero check is kept and the new pin added beside it, with a comment stating that a vacuity floor catches an empty denominator and never a silently short one. |
| T-29-21-04 | Elevation of privilege (self-referential expectation) | a floor derived from the same source it checks | high | mitigate | The expectation is imported from `kit-model.ts`, a module `watchedCorpus()` never calls, so the narrowing edit cannot also move the number. |
| T-29-21-05 | Spoofing | a mirror fixture that silently stops reproducing the defect | medium | mitigate | Every new case asserts its own premise — flipped cell, shortened union, control-corpus membership — before invoking the gate. |
| T-29-21-SC | Tampering | npm / pip / cargo installs | high | mitigate | No package-manager install occurs in this plan; the dependency set is unchanged and the package-legitimacy gate has an empty input set. Escalate rather than install if that changes. |
</threat_model>

<artifacts_this_phase_produces>
New exported symbol in `scripts/check-diff-disposition.ts`:
- a watched-corpus minimum-cardinality constant whose value is `ROLE_COUNT + WORKFLOW_COUNT`

New non-exported logic in `scripts/check-audit-register.ts`:
- a third, independently reported set-equality assertion between counted rows flagged `safety_surface: yes` and the `derived` role-plus-workflow list

New test names (`scripts/check-diff-disposition.test.ts`):
- a case flipping one `safety_surface` cell in a mirrored register and requiring exit 1
- its paired unflipped control requiring exit 0
- a case proving the consumer-side floor fires independently of the source-side equality

New test names (`scripts/check-audit-register.test.ts`):
- a case flipping one `safety_surface` cell and requiring exit 1 with the file named

Regenerated committed build output: `scripts/check-audit-register.js`, `scripts/check-diff-disposition.js`.

No new files, no new npm dependencies, and no changes to any file in the LANG-03 watched corpus, so
no disposition row is owed under `docs/audit/29-style-dispositions/` for this plan. Do not invent
one. `scripts/generate-safety-surface.ts` and `docs/audit/28-disposition-register.md` are explicitly
NOT modified.
</artifacts_this_phase_produces>

<verification>
1. Both HEAD transcripts from Task 1 are recorded verbatim and show exit status 0 before the fix.
2. `npx vitest run scripts/check-diff-disposition.test.ts scripts/check-audit-register.test.ts --exclude '**/scripts/e2e/**'` exits 0 after the fix.
3. All four gates exit 0 on the live tree.
4. `check-diff-disposition` reports the same numbers measured before this plan: 40 markdown of a 41-entry union, 37 changed watched files, 1880 changed clauses, 1532 disposition rows across 8 files, 0 findings over 37/37.
5. The reviewer's original manual reproduction, re-run by hand once against the built `.js` with the plant reverted afterwards, now ends in a red gate. Record the transcript and confirm `git status --porcelain` is clean of the plant.
6. `npm run freshness` and `npx tsc --noEmit` exit 0.
</verification>

<success_criteria>
- Flipping one `safety_surface` cell reds both `check-audit-register` and `check-diff-disposition`.
- The consumer-side floor is proven to fire on its own.
- The floor's expectation shares no code path with the value it checks.
- The live verdict on all four gates is byte-unmoved.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-21-SUMMARY.md` when done.
Include both HEAD RED transcripts, the post-fix refusal text verbatim, the re-derived gate numbers,
and the hand-run reproduction transcript with its revert.
</output>

---
phase: 29-controlled-language-voice-guard-rebuild
plan: 22
type: execute
wave: 21
depends_on: ["29-20", "29-21"]
gap_closure: true
files_modified:
  - scripts/check-diff-disposition.ts
  - scripts/check-diff-disposition.js
  - scripts/check-diff-disposition.test.ts
  - scripts/check-foundation-guards.test.ts
autonomous: true
requirements: [LANG-03, LANG-07]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-08 arm two is closed: `locateSection` consumes `unfencedHeadingIndex` and `sectionEndIndex` from `scripts/frontmatter.ts` and declares no section predicate of its own. Its private `trimEnd()` heading match and its private `startsWith(\"## \")` close scan are DELETED, not left beside the shared authority (LANG-07, D-24)."
    - "WR-03 is closed: `readDispositionRows` is the FOURTH locator of this class and the one round 1's fix missed — it locates `## Dispositions` by a bare substring search anywhere in the file including inside a fence, then admits every seven-cell pipe line from that offset to end of file. It now consumes the same authority, so the table lives in ITS section and a fenced example donates no row."
    - "The direction matters and is stated: `readDispositionRows` fails OPEN. A row is what satisfies the structural companion arm that carries the whole positional freeze, so extra rows are the one kind of noise this gate cannot afford — unlike its sibling `locateSection`, whose truncation shrinks what is protected. Both asymmetries are recorded in source rather than left for a reader to infer."
    - "The site set is DERIVED, not hand-listed. Round 1 fixed three sites of a finding and a fourth nobody had derived was left open; this plan enumerates every section-extent locator in `check-diff-disposition.ts` by scanning the module for the construct rather than by transcribing the review's addresses, and records the enumerated list with its count."
    - "IN-01 is closed: a disposition line under `## Dispositions` that starts with a pipe and does not split to exactly seven cells is COUNTED and NAMED rather than silently dropped. A `before` or `after` cell containing a code span with a pipe currently disappears, and the only trace is a refusal that fires when no other row in the file parses (D-05)."
    - "IN-02 is closed: the per-clause row filter is computed inside the one branch that reads it rather than for every frozen clause on all three branches."
    - "EDGE (LANG-03): the level widening from a level-two-only close to a level-at-most-two close is proven behaviour-preserving on the live watched corpus by MEASUREMENT, not assumption. Measured this session: the only level-one heading lines in the watched corpus after line one are `README.md` lines 17 and 19 and `agent-factory/README.md` lines 105 onward, and `fencedLineFlags` reports every one of them as fenced. The widening is safe ONLY because the authority is fence-aware, and that dependency is stated in source."
    - "EDGE (LANG-03): a frozen structural section whose body contains a fenced example carrying a `## ` line keeps its full extent, and a fenced seven-cell table below `## Dispositions` donates zero rows — both proven by planted fixtures, since the live corpus carries neither shape."
    - "Measured before the edit and re-derived after: 40 markdown files of a 41-entry union, 37 watched files changed since 4d2b8f0, 1880 changed clauses, 1532 disposition rows across 8 files, 0 findings over 37/37 elements. Any movement in any of those five numbers is a REAL change and is escalated with its cause, never absorbed."
    - "`scripts/check-diff-disposition.js` is rebuilt from its `.ts` source and committed in the same commit, so `npm run freshness` exits 0."
  prohibitions:
    - "Never keep a private section predicate beside the shared one as a fallback — a fallback is a second grammar with extra steps, and the fallback is what four rounds of this phase have been deleting."
    - "Never hand-list the locator sites from the review's line numbers; derive them from the module and assert the count, because a hand-maintained site list is the set-literal drift that produced WR-03."
    - "Never widen the section close without the fence-awareness in the same change; on this corpus the widening is safe only because the authority is fence-aware, and splitting them would be a fail-open regression in a watched file."
    - "Never let a dropped disposition row stay silent; a row that vanishes reads as an undispositioned clause and names the wrong cause."
    - "Never run bare `npm test` — it triggers the live claude-CLI e2e lane, spends the user's tokens and can hang for around eight minutes."
  artifacts:
    - scripts/check-diff-disposition.ts
    - scripts/check-diff-disposition.js
    - scripts/check-diff-disposition.test.ts
    - scripts/check-foundation-guards.test.ts
  key_links:
    - "`scripts/frontmatter.ts` exports `unfencedHeadingIndex` and `sectionEndIndex` (plan 29-20); `check-diff-disposition.ts` imports both plus `fencedLineFlags` and declares no locator of its own."
    - "`scripts/check-foundation-guards.test.ts:746-749` pins this module's imported-symbol set; the pin moves from one symbol to three and the movement is acknowledged in place."
    - "`docs/audit/29-style-dispositions/*.md` are the files `readDispositionRows` parses; the only member carrying fences is `29-11.md`, whose fences sit at lines 39 and 43, above its `## Dispositions` heading at line 182 — which is why the defect is currently unreachable and why its proof must be planted."
---

<objective>
Move `check-diff-disposition.ts` onto the shared section-locator authority and close the fourth
locator of the class that round 1's fix missed.

Purpose: LANG-07 fails because four modules answer "where does this section end" four disagreeing
ways. Round 1 fixed three of them and a fourth — `readDispositionRows` — was never derived, so it
was never fixed. It is fence-blind AND section-unbounded, in the fail-open direction: a stray or
quoted seven-column table below `## Dispositions` contributes rows, and a row is what satisfies the
structural companion arm that carries the whole positional freeze. LANG-03 depends on that arm.

Output: one module with zero private section predicates, a derived enumeration proving it, a bounded
and fence-aware row reader, and planted fixtures for both shapes the live corpus does not carry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-20-SUMMARY.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-16-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the locator site set in this module, then RED-plant every site the derivation finds</name>
  <files>scripts/check-diff-disposition.test.ts</files>
  <read_first>
    - `scripts/check-diff-disposition.ts:486-543` — `locateSection`, its recorded WR-06 argument block and the fail-open/fail-closed asymmetry it already states.
    - `scripts/check-diff-disposition.ts:1100-1250` — `DISPOSITION_HEADING`, `DISPOSITION_COLUMNS`, `DISPOSITION_NON_ROWS`, and `readDispositionRows` including the bare substring search at line 1206, the seven-cell filter at line 1219 and the zero-row refusal at line 1234.
    - `scripts/check-diff-disposition.ts:1500-1530` — the frozen-clause branch structure and the per-clause `rows` filter that only the structural branch reads.
    - `scripts/check-diff-disposition.test.ts:90-440` and `:960-1200` — the mirror builders, `runGate`, and the existing fenced-heading cases plan 29-16 wrote for `locateSection`. The new cases follow those shapes.
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` — WR-03, IN-01 and IN-02 with their fix sketches.
  </read_first>
  <action>
Enumerate before planting, because the failure this task exists to prevent is fixing the sites
somebody wrote down rather than the sites that exist.

Add a case to `scripts/check-diff-disposition.test.ts` that reads this module's own source, strips
comment lines the way the fence-machine scan in `scripts/frontmatter.test.ts` already does, and
derives every construct in it that answers a section-extent question — a heading-equality test
against a heading constant, and a heading-prefix test used to terminate a scan. Assert the derived
site list two-sided against a written-down list and assert its cardinality as a number. Prove the
derivation can fail by running the same classifier with one construct removed and asserting the
answer changes, exactly as the fence-machine scan proves its own conjunction.

Record the derived list in the case and in the SUMMARY. If it names a site neither this plan nor the
review anticipated, that site is IN SCOPE for Task 2 — do not narrow the derivation to match the
plan. A derivation trimmed to the expected answer is the defect this case exists to catch.

Then plant the RED evidence, one case per site the derivation names. At minimum:

For `readDispositionRows`, a disposition-file fixture whose `## Dispositions` section is followed by
a FENCED example containing a well-formed seven-column row, plus a real row. Assert that the parsed
row count equals one, not two. At HEAD this fails with a received count of two.

For `readDispositionRows`, a fixture carrying a seven-column table under a LATER `## ` heading after
the dispositions section ends. Assert those lines donate no rows. At HEAD this fails.

For `readDispositionRows`, a fixture whose `## Dispositions` literal appears first inside a fenced
example and later as the real heading. Assert the rows are read from the real heading. At HEAD the
substring search finds the fenced occurrence first.

For IN-01, a fixture with one row whose `before` cell contains a code span carrying a pipe, alongside
two well-formed rows. Assert the gate NAMES the dropped line with its line number and its cell count.
At HEAD the line vanishes and no refusal fires, because the other rows parse.

For `locateSection`, a watched-file fixture whose frozen structural section is followed by a
LEVEL-ONE heading. Assert the frozen region ends there. At HEAD it does not, because the close scan
recognises only a level-two heading.

Capture every HEAD transcript verbatim. Fix nothing in this task.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-diff-disposition.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - The derived locator-site case passes at HEAD and its member list plus cardinality are recorded in the SUMMARY verbatim.
    - The derivation is proven falsifiable: removing one construct from the classifier changes the derived list, asserted in the same case.
    - Each planted case FAILS at HEAD with the received value recorded — five cases minimum, one per shape above, plus one per additional site the derivation named.
    - No source file outside `scripts/check-diff-disposition.test.ts` is modified in this task, confirmed by `git diff --name-only`.
  </acceptance_criteria>
  <done>Every section-extent locator in the module is derived rather than transcribed, and each has a case that fails at HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Both locators consume the shared authority; the private predicates are deleted</name>
  <files>scripts/check-diff-disposition.ts, scripts/check-diff-disposition.js</files>
  <read_first>
    - `scripts/frontmatter.ts` — `unfencedHeadingIndex` and `sectionEndIndex` as landed by plan 29-20, including their recorded scope: `trimEnd()`-normalized heading equality, close on an unfenced heading of level at most `level`, and termination at `lines.length`.
    - `scripts/check-diff-disposition.ts:156` — the existing import from `./frontmatter.js`.
    - `scripts/check-diff-disposition.ts:486-543` and `:1190-1243` — the two locators being rewired.
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-20-SUMMARY.md` — the authority's final signature and the scope decisions plan 29-20 pinned.
  </read_first>
  <action>
Rewire both locators onto the shared authority and delete what they used to declare.

`locateSection` becomes: find the heading through `unfencedHeadingIndex(text, heading)`, and compute
the end through `sectionEndIndex(text, headingIndex + 1, 2)`. Return the same `{ from, to }` shape it
returns today so no caller changes. Delete its own `fencedLineFlags` call, its own `trimEnd()`
comparison and its own inner close loop. Rewrite its argument block to say that the predicate now
lives in one place and that this module contributes the QUESTION, not the grammar; keep the recorded
fail-open asymmetry with its sibling, because that asymmetry is still true and is what stops a later
reader treating the two as one bug at two addresses.

`readDispositionRows` becomes: locate the heading through `unfencedHeadingIndex(body,
DISPOSITION_HEADING)` — replacing the bare substring search, which currently matches the literal
anywhere including inside prose and inside a fence — and bound the row scan with `sectionEndIndex(body,
at + 1, 2)`. Inside the bound, skip any line the fence toggle flags, because a fenced example donates
no row. Keep the existing named refusal for a file carrying no heading, and keep the zero-row refusal.

State the direction in the comment beside `readDispositionRows`, in this file's established voice:
this locator fails OPEN. A row satisfies the structural companion arm, that arm carries the whole
positional freeze — the one that catches a reword — so a spurious row admits a change that owed a
companion edit. That is the opposite direction from `locateSection`'s truncation and from the
banned-claim exemption's, and naming which is which is what this round is for.

Close IN-01 in the same function: count the lines under the bounded, unfenced heading that begin with
a pipe and do not split to exactly `DISPOSITION_COLUMNS` cells, and report them by name with their
line numbers and their actual cell count when the count is non-zero. Say in the message that a
`before` or `after` cell containing a code span with a pipe is the plausible cause. The clause then
still reads as undispositioned, which is fail-closed, but the author is told the real reason instead
of the wrong one.

Close IN-02: move the per-clause `rows` filter inside the branch that reads it.

Do NOT change `DISPOSITION_COLUMNS`, `DISPOSITION_NON_ROWS`, `rowMatches`, `normalizeSentence`, the
frozen-source table or any refusal wording other than the two this task adds. This is a locator
change, not a semantics change, and mixing the two is how round 1 produced a fix that was itself
incomplete.

Rebuild and commit `scripts/check-diff-disposition.js` in the same commit.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-diff-disposition.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/check-diff-disposition.test.ts --exclude '**/scripts/e2e/**'` exits 0, including every case Task 1 proved RED.
    - The Task 1 derived-site case still passes and its derived list now contains ZERO members outside `scripts/frontmatter.ts`'s authority, asserted by the case itself rather than by inspection.
    - `node scripts/check-diff-disposition.js` exits 0 and reports `40 markdown file(s) of the 41-entry LANG-03 safety-surface union`, `37 watched file(s) changed since 4d2b8f0`, `1880 changed clause(s) derived`, `1532 disposition row(s) across 8 file(s)` and `0 findings over 37/37 elements`. Every one of those five numbers is compared against the pre-edit measurement and any movement is escalated in the SUMMARY with its cause.
    - `npx tsc --noEmit` exits 0 and `npm run freshness` exits 0.
  </acceptance_criteria>
  <done>Both locators in this module take their answer from the shared authority, the row reader is bounded and fence-aware, and the live verdict is byte-unmoved.</done>
  <reversibility rating="costly">Deleting the private predicates is the point of the change, but the level widening touches a watched-file corpus where a fail-open regression is invisible. It is reversible only by re-reading the five published numbers, which is why they are pinned as acceptance criteria rather than eyeballed.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Move the two-sided import pin and prove the widening is safe by measurement</name>
  <files>scripts/check-foundation-guards.test.ts, scripts/check-diff-disposition.test.ts</files>
  <read_first>
    - `scripts/check-foundation-guards.test.ts:663-754` — the frontmatter consumer list, the recorded history of every time it moved, and the four per-module `importedSymbols(...)` equalities including line 746-749 for this module.
    - `scripts/check-foundation-guards.test.ts:756-790` — the tree-wide grant-predicate case that must stay at zero.
    - `scripts/check-diff-disposition.test.ts` — the cases added in Task 1, now green.
  </read_first>
  <action>
Reconcile the pin the rewire breaks, and turn the level-widening safety argument into an assertion.

In `scripts/check-foundation-guards.test.ts`, update this module's imported-symbol equality from one
symbol to the three it now takes — the per-line fence projection plus the two locator functions —
sorted as `importedSymbols` returns them. Extend the comment above it in the file's established
style: record that the set grew because the module took MORE declarations and still renders no
verdict from the parser, that this is the direction D-24 wants, and that the pin moving is the pin
working rather than a failure being cleared. Do not touch the two sibling equalities that plans 29-23
and 29-24 will move; each records its own reason.

Confirm the tree-wide grant-predicate zero still holds — the locator functions are declarative, not
verdict-bearing.

Then add the measurement case. The level widening from a level-two-only close to a level-at-most-two
close is safe on this corpus ONLY because the authority is fence-aware, and that is a fact about the
current corpus rather than a property of the code. Add a case to
`scripts/check-diff-disposition.test.ts` that derives, over the live watched corpus, every line after
the first that is a level-one heading, and asserts that every such line is flagged by the one fence
authority. Measured this session the answer is: `README.md` lines 17 and 19, and
`agent-factory/README.md` lines 105 onward, all fenced; roles and workflows carry their `# Role: `
and `# Workflow: ` heading at line 6, before any level-two section, so none of them can close one.

Assert the derived set is NON-EMPTY first, so a scan that found nothing cannot report "all fenced",
and name every unfenced member if any exists. If an unfenced level-one heading ever lands in a
watched file below a `## ` section, this case goes red and tells the author that the frozen region
above it just shrank — which is the fail-open direction and the reason the case exists.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-diff-disposition.test.ts scripts/check-foundation-guards.test.ts scripts/frontmatter.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/check-diff-disposition.test.ts scripts/check-foundation-guards.test.ts scripts/frontmatter.test.ts --exclude '**/scripts/e2e/**'` exits 0.
    - This module's entry in the consumer-symbol pin lists three symbols; the `check-banned-claims.ts` and `check-imperative-lexicon.ts` entries are byte-unchanged, confirmed by `git diff`.
    - The level-one-heading measurement case asserts a non-empty derived set before asserting the fenced property, and its measured member list is recorded in the SUMMARY.
    - All four gates exit 0 on the live tree with their pre-edit numbers, recorded in the SUMMARY.
    - `npm run freshness` and `npx tsc --noEmit` exit 0; `git status --porcelain` shows only this plan's files plus the three pre-existing out-of-scope entries.
  </acceptance_criteria>
  <done>The import pin records the new symbol set two-sided, and the corpus fact that makes the level widening safe is an assertion rather than a claim in a comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/audit/29-style-dispositions/*.md` (author-written tables) → the structural companion arm | An author-supplied markdown table decides whether a frozen safety clause may change. A spurious row admits a reword that owed a companion edit. |
| watched markdown → `locateSection` → the frozen region | A truncated frozen region shrinks what is protected with no failure anywhere — fail-open and invisible by construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-22-01 | Tampering | `readDispositionRows` substring search and unbounded row loop | high | mitigate | The heading is located through the unfenced authority and the row scan is bounded to its own section; fenced lines donate no row. Planted fixtures prove all three shapes. |
| T-29-22-02 | Spoofing | a fenced seven-column example presented as a disposition row | high | mitigate | The fence toggle decides; a fenced line is documentation and contributes nothing to the companion arm. |
| T-29-22-03 | Information disclosure (false assurance) | a dropped row that reads as an undispositioned clause | medium | mitigate | IN-01: malformed rows are counted and named with their line number and cell count, so the red names its real cause. |
| T-29-22-04 | Tampering | the level widening applied without fence-awareness | high | mitigate | The two land in one change, and a derived case asserts every level-one heading in the watched corpus is fenced, with a non-vacuity floor first. |
| T-29-22-05 | Repudiation | a hand-listed site set that misses a fourth locator | high | mitigate | Task 1 derives the site set from the module and proves the derivation falsifiable before any fix is written. |
| T-29-22-SC | Tampering | npm / pip / cargo installs | high | mitigate | No package-manager install occurs in this plan; the dependency set is unchanged and the package-legitimacy gate has an empty input set. Escalate rather than install if that changes. |
</threat_model>

<artifacts_this_phase_produces>
Modified functions in `scripts/check-diff-disposition.ts` (no signature changes):
- `locateSection` — now delegates to `unfencedHeadingIndex` and `sectionEndIndex`
- `readDispositionRows` — now bounded, fence-aware, and reports malformed rows by name

No new exported symbols from `scripts/check-diff-disposition.ts` beyond plan 29-21's watched-corpus
minimum constant, which already exists at this plan's start.

New test names (`scripts/check-diff-disposition.test.ts`):
- a derived section-extent locator-site enumeration with a two-sided member list, a pinned
  cardinality, and a falsifiability probe
- a fenced seven-column example under `## Dispositions` donating zero rows
- a seven-column table under a later heading donating zero rows
- a fenced occurrence of the dispositions heading not being taken as the real heading
- a malformed row being named with its line number and cell count
- a frozen structural section closing at a level-one heading
- a derived assertion that every level-one heading line in the watched corpus after line one is fenced

Modified test in `scripts/check-foundation-guards.test.ts`:
- this module's entry in the frontmatter consumer-symbol pin moves from one symbol to three

Regenerated committed build output: `scripts/check-diff-disposition.js`.

No new files, no new npm dependencies, and no changes to any file in the LANG-03 watched corpus, so
no disposition row is owed under `docs/audit/29-style-dispositions/` for this plan. Do not invent one.
</artifacts_this_phase_produces>

<verification>
1. Every Task 1 case is recorded as failing at HEAD with its received value, and passing after Task 2.
2. The derived locator-site list contains zero members outside the shared authority after Task 2.
3. `npx vitest run scripts/check-diff-disposition.test.ts scripts/check-foundation-guards.test.ts scripts/frontmatter.test.ts --exclude '**/scripts/e2e/**'` exits 0.
4. All four gates exit 0 and `check-diff-disposition` reports its five pre-edit numbers unchanged.
5. `npm run freshness` and `npx tsc --noEmit` exit 0.
</verification>

<success_criteria>
- `check-diff-disposition.ts` declares no section predicate of its own, proven by a derivation rather than by reading.
- The fourth locator round 1 missed is bounded, fence-aware, and proven so by three planted fixtures.
- Malformed disposition rows are named rather than dropped.
- The live verdict is byte-unmoved on all five published numbers.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-22-SUMMARY.md` when done.
Include the derived locator-site list, every HEAD RED transcript, the measured level-one-heading
member list, and the five gate numbers before and after.
</output>

---
phase: 29-controlled-language-voice-guard-rebuild
plan: 23
type: execute
wave: 22
depends_on: ["29-20", "29-22"]
gap_closure: true
files_modified:
  - scripts/check-banned-claims.ts
  - scripts/check-banned-claims.js
  - scripts/check-banned-claims.test.ts
  - scripts/check-foundation-guards.test.ts
autonomous: true
requirements: [LANG-04, LANG-07]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-08 arm three is closed: `locateExemptRegion` consumes `unfencedHeadingIndex` and `sectionEndIndex` from `scripts/frontmatter.ts`. Its private exact-equality heading match and its private `SAME_LEVEL_HEADING` close predicate are DELETED, so this module contributes the question and not the grammar (LANG-07, D-24)."
    - "WR-02 is closed on the honesty axis: the header block asserting that nothing below is relaxed and that both truncation directions are fail-CLOSED is CORRECTED to state what the code does. Plan 29-18's fix made the region end LATER, so strictly fewer lines are scanned — measured by the reviewer at four exempt lines becoming nine on the same fixture. A longer exemption is less checking, and the prose now says so (D-29)."
    - "WR-02 is closed on the mechanism axis: the exemption's REACH is published on every run and pinned two-sided — the gate reports how many banned-claim occurrences the exemption suppresses, and that number is compared against a declared constant. An exemption that grows is then an acknowledged edit with a reason, never a side effect of a heading landing somewhere new."
    - "The pin is on the SUPPRESSED-OCCURRENCE count rather than on a line count, and the reason is recorded: a line count moves when the disclaimer is reflowed and says nothing about how much prohibition was lifted, while the suppressed count is exactly the quantity an exemption is a decision about (D-44)."
    - "The missing paired NEGATIVE case ships: a banned claim sitting below a fenced `## ` line AND below a later REAL `## ` heading is still reported. The existing paired-plant case covers only the second half, so the widening's outer bound has never been asserted."
    - "EDGE (LANG-04): the exemption region's exactly-one refusal, its empty-region refusal and their exact wording are byte-unchanged. They are what this guard already gets right, and a locator change is not licence to touch them."
    - "EDGE (LANG-04): the level widening — from a close on a level-two heading only to a close on any heading of level at most two — is measured rather than assumed. Measured this session, `agent-factory/writing-profile.md` carries level-one heading lines at 106, 109, 112, 115 and 118, every one of them flagged fenced by the one fence authority, and all of them ABOVE the exemption heading at line 155; the region runs from index 154 to index 216, which is end of file, so 62 lines are exempt today and the widening cannot move that."
    - "EDGE (LANG-04): the direction asymmetry stays recorded. Truncating THIS region causes MORE of the document to be checked and is fail-CLOSED; truncating `check-diff-disposition.ts`'s frozen region causes LESS to be protected and is fail-OPEN. The two are not one bug at two addresses and the source says so."
    - "Measured before the edit and re-derived after: `node scripts/check-banned-claims.js` prints `banned claims: 0 findings over 82/82 elements` and exits 0. This fix is behaviour-preserving on the live corpus, so its proof is planted, never corpus-derived."
    - "`scripts/check-banned-claims.js` is rebuilt from its `.ts` source and committed in the same commit, so `npm run freshness` exits 0."
  prohibitions:
    - "Never leave a header claiming a direction the code does not take — a prose claim wider than the assertion behind it is this repository's second systemic failure class wearing a sentence instead of a set literal."
    - "Never widen an exemption without publishing what the widening bought; an exemption whose reach nothing measures is a hole with a comment on it."
    - "Never relax the exactly-one or empty-region refusals while rewiring the locator; they are the part this guard already gets right."
    - "Never pin the exemption by line count — a reflow would red the gate and teach an author that the pin is noise, which is how a pin stops being read."
    - "Never run bare `npm test` — it triggers the live claude-CLI e2e lane, spends the user's tokens and can hang for around eight minutes."
  artifacts:
    - scripts/check-banned-claims.ts
    - scripts/check-banned-claims.js
    - scripts/check-banned-claims.test.ts
    - scripts/check-foundation-guards.test.ts
  key_links:
    - "`scripts/frontmatter.ts` exports `unfencedHeadingIndex` and `sectionEndIndex` (plan 29-20); `check-banned-claims.ts` imports both plus `fencedLineFlags` and declares no locator of its own."
    - "`scripts/check-foundation-guards.test.ts:750-753` pins this module's imported-symbol set; the pin moves from one symbol to three and the movement is acknowledged in place."
    - "`agent-factory/writing-profile.md` § `## Disclaimer and honesty floor` is the one named exemption region; it is NOT modified by this plan and its bytes are the subject of the measurement, not of an edit."
---

<objective>
Move `check-banned-claims.ts` onto the shared section-locator authority, and make its exemption tell
the truth about its own direction and its own reach.

Purpose: LANG-04 requires guards named for exactly the decidable subsets they check. Round 1's
WR-06 fix in this module WIDENED a safety exemption — measured at four exempt lines becoming nine —
while the header block directly above the change asserts that nothing below is relaxed and that both
truncation directions are fail-closed. The widening is defensible; the prose denying it is not. And
nothing anywhere measures how much prohibition the exemption actually lifts, so a future widening
would be as invisible as this one was.

Output: one locator consuming the shared authority, a corrected header, a published and two-sided
pinned suppressed-occurrence count, and the paired negative case the test file is missing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-20-SUMMARY.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-18-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED first — the missing paired negative case and the unmeasured exemption reach</name>
  <files>scripts/check-banned-claims.test.ts</files>
  <read_first>
    - `scripts/check-banned-claims.ts:441-532` — `ExemptRegion`, `SAME_LEVEL_HEADING`, the header block at lines 462-485 including the sentence asserting nothing is relaxed, and `locateExemptRegion` with its two refusals.
    - `scripts/check-banned-claims.ts:534-620` — the scan that consumes the region, so the suppressed-occurrence count is derived where the suppression actually happens.
    - `scripts/check-banned-claims.test.ts:480-640` — plan 29-18's fenced-heading case, the existing paired-plant case and the mirror helpers. The new negative case is the paired plant's missing half.
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` — WR-02 with its measured before-and-after extents and its fix sketch.
  </read_first>
  <action>
Write the failing evidence before writing any fix.

Add the paired NEGATIVE case the review names as missing. The existing case asserts that a claim
below a fenced `## ` line inside the region stays exempt. Its untested half is the outer bound: a
banned claim sitting below that same fenced line AND below a later REAL `## ` heading must still be
reported. Build a mirror whose exemption-region file carries, in order, the region heading, a
disclaimer line, a fenced block containing a `## ` line, a line inside the region, a real later `## `
heading, and a banned claim under that later heading. Assert exit 1 and assert the finding names the
file and the line of the claim.

State in the case's name that it asserts the exemption's OUTER bound, so the pair reads as two halves
of one property rather than as two unrelated cases.

Then add the reach case. Assert that the gate publishes, on a green run, the number of banned-claim
occurrences the exemption suppresses in the live corpus, and that this number equals a declared
constant. At HEAD this case fails because no such number is published and no such constant exists.

Measure that number before writing the constant: derive it at execution by counting occurrences of
the guard's pinned banned-claim literals that fall inside the located exempt region of
`agent-factory/writing-profile.md`. Do NOT guess it and do NOT copy a number from this plan — the
plan deliberately does not state one, because a transcribed expectation is the set-literal drift this
repository has corrected three times. Record the measured value in the SUMMARY.

Add a level-widening measurement case in the same file: derive every level-one heading line in
`agent-factory/writing-profile.md` after line one and assert each is flagged by the one fence
authority, with a non-vacuity floor asserting the derived set is non-empty first. Measured this
session the answer is lines 106, 109, 112, 115 and 118, all fenced, all above the exemption heading
at line 155.

Capture every HEAD transcript verbatim. Fix nothing in this task.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-banned-claims.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - At HEAD the new outer-bound negative case runs; record whether it passes or fails and the received value either way. If it passes at HEAD it is still kept, and the SUMMARY states plainly that it was an untested property rather than a live defect.
    - At HEAD the reach case FAILS because the published number does not exist. The transcript is recorded.
    - The measured suppressed-occurrence count is derived at execution and recorded in the SUMMARY; the plan states no expected value and none is transcribed from it.
    - The level-one-heading case asserts a non-empty derived set before asserting the fenced property, and its measured member list matches lines 106, 109, 112, 115 and 118 or the difference is escalated.
    - No source file outside `scripts/check-banned-claims.test.ts` is modified in this task, confirmed by `git diff --name-only`.
  </acceptance_criteria>
  <done>The exemption's outer bound and its reach both have cases, and the reach case fails at HEAD because nothing measures it.</done>
</task>

<task type="auto">
  <name>Task 2: The locator consumes the authority; the header states the direction the code takes</name>
  <files>scripts/check-banned-claims.ts, scripts/check-banned-claims.js</files>
  <read_first>
    - `scripts/frontmatter.ts` — `unfencedHeadingIndex` and `sectionEndIndex` as landed by plan 29-20, with their recorded scope.
    - `scripts/check-banned-claims.ts:132` — the existing import from `./frontmatter.js`.
    - `scripts/check-banned-claims.ts:441-532` — the region type, the private predicates being deleted, and the two refusals that must survive byte-unchanged.
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-22-SUMMARY.md` — how the sibling module recorded its own fail-open direction, so the two asymmetry notes read as a matched pair rather than as two independent opinions.
  </read_first>
  <action>
Rewire the locator and correct the prose that misdescribes it.

`locateExemptRegion` keeps its `readonly string[]` parameter and its `ExemptRegion` return shape, so
no call site and no exported-function case changes. Internally it joins its lines once and asks the
shared authority: the region's own heading through `unfencedHeadingIndex`, and its end through
`sectionEndIndex` with a level of two. Delete `SAME_LEVEL_HEADING`, delete the module's own
`fencedLineFlags` call in this function, and delete the exact-equality heading comparison — the
authority normalizes with `trimEnd()`, which is the axis on which the four deleted predicates
disagreed and which is now answered once.

The exactly-one refusal needs a heading COUNT, not a first index, so it cannot be expressed through
`unfencedHeadingIndex`. Keep it, and derive the count from the one fence toggle directly, with a
comment stating that this module takes the per-line toggle for the count and the shared locator for
the bound, that both are projections of the same authority, and that wrapping the first-index helper
in a "find the next one" loop is deliberately not done because a second traversal with different
termination behaviour is exactly the shape this round is deleting.

Correct the header block. The sentence asserting that nothing below is relaxed and that both
truncation directions are fail-closed is false as of plan 29-18: consuming the toggle makes the
region END LATER, so strictly fewer lines are scanned, and that is a relaxation of a safety
exemption. Rewrite the paragraph to say that plainly — a longer exemption is less checking, not more
— and keep the true half, which is the asymmetry with `check-diff-disposition.ts`'s frozen region.
Say why the relaxation is nonetheless correct: a heading quoted inside a fenced example is
documentation and never structure, so a region that stopped at one was measuring the wrong bytes.

Then give the relaxation a mechanism. Derive, at scan time, the number of banned-claim occurrences
that fall inside the located exempt region, publish it on the gate's PASS line beside the element
counts the gate already publishes, and compare it against an exported constant declared in this file.
On mismatch, refuse by name: state the derived number, the declared number, and that an exemption
growing is a decision that is recorded here with its reason rather than a side effect of a heading
landing somewhere new. State in the constant's comment why the pin is on suppressed OCCURRENCES
rather than on exempt LINES — a line count moves when the disclaimer is reflowed and says nothing
about how much prohibition was lifted.

Do not touch the pinned banned-claim literal set, the exactly-one refusal, the empty-region refusal
or their wording.

Rebuild and commit `scripts/check-banned-claims.js` in the same commit.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-banned-claims.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/check-banned-claims.test.ts --exclude '**/scripts/e2e/**'` exits 0, including Task 1's reach case.
    - `node scripts/check-banned-claims.js` exits 0, prints `banned claims: 0 findings over 82/82 elements`, and its PASS line now also names the suppressed-occurrence count and the declared constant.
    - `node -e 'import("./scripts/check-banned-claims.js").then(m=>{const fs=require("node:fs");const l=fs.readFileSync("agent-factory/writing-profile.md","utf8").split("\n");console.log(JSON.stringify(m.locateExemptRegion(l)))})'` prints a region whose `headingAt` is 154 and whose `endBefore` is 216 — byte-identical to the pre-edit measurement recorded in this plan.
    - The exactly-one refusal text, the empty-region refusal text and the pinned literal set are byte-unchanged, confirmed by `git diff`.
    - `npx tsc --noEmit` and `npm run freshness` exit 0.
  </acceptance_criteria>
  <done>The locator takes its answer from the shared authority, the header describes the direction the code takes, and the exemption's reach is a published, pinned number.</done>
  <reversibility rating="reversible">The suppressed-occurrence pin is a declared constant with a stated reason; a legitimate widening of the disclaimer moves it with a recorded justification, which is the acknowledgement mechanism rather than a barrier.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Move the two-sided import pin and prove the exemption cannot silently reach further</name>
  <files>scripts/check-foundation-guards.test.ts, scripts/check-banned-claims.test.ts</files>
  <read_first>
    - `scripts/check-foundation-guards.test.ts:663-754` — the consumer list, the recorded movement history and the per-module symbol equalities including lines 750-753 for this module.
    - `scripts/check-foundation-guards.test.ts:756-790` — the tree-wide grant-predicate zero.
    - `scripts/check-banned-claims.test.ts` — Task 1's cases, now green.
  </read_first>
  <action>
Reconcile the pin, then prove the new mechanism can fire.

In `scripts/check-foundation-guards.test.ts`, update this module's imported-symbol equality from one
symbol to the three it now takes, sorted as `importedSymbols` returns them, and extend the comment
above it in the file's established style: record that the set grew because the module took more
declarations from the authority while still rendering no verdict from it, and that the pin moving is
the pin working. Leave the `check-imperative-lexicon.ts` entry alone; plan 29-24 moves it with its
own reason. Confirm the tree-wide grant-predicate zero still holds.

Then add the falsifiability case for the suppressed-occurrence pin. A pin that has never been shown
to fail is a comment. Build a mirror whose exemption-region file carries one ADDITIONAL banned claim
inside the region, and assert the gate exits 1 naming the derived count, the declared count, and the
sentence about an exemption growing being a decision. Add its mirror-image: a mirror with one FEWER
banned claim inside the region also reds, because the pin is two-sided and a shrinking exemption is
equally a change somebody made.

Add the widening-cannot-be-open-ended case: a mirror whose exemption-region file gains a REAL later
`## ` section containing a banned claim must exit 1. This is the property WR-02 says is unreachable
today only by ordering accident — the region currently runs to end of file — so it is pinned by a
plant rather than left to the corpus's current shape.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-banned-claims.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/check-banned-claims.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'` exits 0.
    - This module's entry in the consumer-symbol pin lists three symbols; the `check-imperative-lexicon.ts` entry is byte-unchanged, confirmed by `git diff`.
    - Both directions of the suppressed-occurrence pin are proven to fire — one added claim reds, one removed claim reds — each asserted on a mirror whose fixture premise is checked first.
    - The appended-later-section case exits 1 and names the claim's file and line.
    - All four gates exit 0 on the live tree with their pre-edit numbers, recorded in the SUMMARY.
    - `npm run freshness` and `npx tsc --noEmit` exit 0; `git status --porcelain` shows only this plan's files plus the three pre-existing out-of-scope entries.
  </acceptance_criteria>
  <done>The import pin records the new symbol set two-sided, and the exemption's reach is pinned by a mechanism proven to fire in both directions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `agent-factory/writing-profile.md` § disclaimer → `guard_banned_claims` | A named region of an authored document is exempt from a prohibition the rest of the kit is held to. Anything that grows that region lifts the prohibition further, and until this plan nothing measured by how much. |
| fenced example content → section structure | A `## ` line inside a fenced block is documentation. Whether the locator believes that decides how much of the file is exempt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-23-01 | Elevation of privilege | the exemption region's extent | high | mitigate | The suppressed-occurrence count is published and pinned two-sided, with both directions proven to fire on a mirror; an exemption growing becomes an acknowledged edit. |
| T-29-23-02 | Repudiation | the header asserting nothing is relaxed | high | mitigate | The header is corrected to state the direction the code takes, with the reason the relaxation is nonetheless right; a prose claim wider than its assertion is this repository's recorded second systemic failure class. |
| T-29-23-03 | Tampering | a second section grammar in this module | medium | mitigate | The private heading match and close predicate are deleted; the module consumes the shared authority and the import pin records the change two-sided. |
| T-29-23-04 | Spoofing | a fenced quotation of the region heading | medium | mitigate | The exactly-one refusal keeps its fence-aware count and its wording; only the traversal moves. |
| T-29-23-05 | Denial of service (false red) | the level widening shortening the region | low | accept | The widening can only shorten the region, which causes MORE to be checked — fail-closed. Measured this session: `writing-profile.md`'s five level-one heading lines are all fenced and all above the exemption heading, so no shortening occurs today. Accepted with the measurement recorded rather than mitigated with a carve-out. |
| T-29-23-SC | Tampering | npm / pip / cargo installs | high | mitigate | No package-manager install occurs in this plan; the dependency set is unchanged and the package-legitimacy gate has an empty input set. Escalate rather than install if that changes. |
</threat_model>

<artifacts_this_phase_produces>
New exported symbol in `scripts/check-banned-claims.ts`:
- a constant declaring the number of banned-claim occurrences the named exemption region suppresses,
  measured at execution and pinned two-sided

Modified function in `scripts/check-banned-claims.ts` (no signature change):
- `locateExemptRegion` — now delegates to `unfencedHeadingIndex` and `sectionEndIndex`

Symbol DELETED from `scripts/check-banned-claims.ts`:
- `SAME_LEVEL_HEADING`

New test names (`scripts/check-banned-claims.test.ts`):
- the outer-bound half of the paired plant: a claim below a fenced heading AND below a later real heading is still reported
- a case asserting the published suppressed-occurrence count equals its declared constant
- a case asserting every level-one heading line in the exemption file after line one is fenced, with a non-vacuity floor
- two falsifiability cases proving the suppressed-occurrence pin fires in both directions
- a case proving an appended later section carrying a banned claim exits 1

Modified test in `scripts/check-foundation-guards.test.ts`:
- this module's entry in the frontmatter consumer-symbol pin moves from one symbol to three

Regenerated committed build output: `scripts/check-banned-claims.js`.

No new files, no new npm dependencies, and no changes to any file in the LANG-03 watched corpus, so
no disposition row is owed under `docs/audit/29-style-dispositions/` for this plan. Do not invent one.
`agent-factory/writing-profile.md` is NOT modified by this plan.
</artifacts_this_phase_produces>

<verification>
1. Every Task 1 HEAD transcript is recorded, including the measured suppressed-occurrence count derived at execution.
2. `npx vitest run scripts/check-banned-claims.test.ts scripts/check-foundation-guards.test.ts scripts/frontmatter.test.ts --exclude '**/scripts/e2e/**'` exits 0.
3. `locateExemptRegion` on the live `agent-factory/writing-profile.md` still returns `headingAt` 154 and `endBefore` 216.
4. All four gates exit 0; `check-banned-claims` still prints `0 findings over 82/82 elements`.
5. `npm run freshness` and `npx tsc --noEmit` exit 0.
</verification>

<success_criteria>
- `check-banned-claims.ts` declares no section predicate of its own.
- The header states the direction the code takes.
- The exemption's reach is published, pinned two-sided, and proven to fire in both directions.
- The paired plant has both halves.
- The live verdict is byte-unmoved.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-23-SUMMARY.md` when done.
Include the HEAD transcripts, the measured suppressed-occurrence count with the command that derived
it, the corrected header text verbatim, and the four gates' numbers before and after.
</output>

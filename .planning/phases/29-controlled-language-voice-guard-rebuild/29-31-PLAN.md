---
phase: 29-controlled-language-voice-guard-rebuild
plan: 31
type: execute
wave: 2
depends_on: ["29-27"]
gap_closure: true
files_modified:
  - agent-factory/writing-profile.md
  - scripts/check-imperative-lexicon.ts
  - scripts/check-imperative-lexicon.test.ts
  - docs/audit/28-claim-registry.md
autonomous: true
requirements: [LANG-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LANG-04 — a guard enforces exactly the profile's decidable subset and is named for that subset (guard_imperative_lexicon: lexicon membership at imperative position; guard_sentence_form: sentence length and banned constructions); guard_banned_claims holds the conformance prohibition mechanically"
  artifacts:
    - "agent-factory/writing-profile.md — WP-11 published at exactly the spelling the gate decides, with the narrowing and its residual recorded in the profile's own rationale section"
    - "scripts/check-imperative-lexicon.ts — the WHOLE rule (both sentences) held as literals, and the refusal emitting them byte-identically to the profile"
    - "scripts/check-imperative-lexicon.test.ts — the two-artifact pin covering both sentences, proven falsifiable by rewording either alone"
    - "scripts/check-imperative-lexicon.ts — a two-sided tripwire on the count of steps headings at any OTHER level, so the disclosed residual cannot become live silently"
  key_links:
    - "writing-profile.md WP-11 row <-> check-imperative-lexicon.ts rule literals — byte-identical on both sentences, pinned in both directions"
    - "the non-`## Steps` heading tally -> the gate's own refusal — a residual with zero live instances now has a mechanism instead of a promise"
---

<objective>
Close LANG-04's round-3 gap (29-REVIEW.md WR-05). Round 2's three enumerated items all landed; the
truth fails one level up, and in two halves.

**Scope.** `agent-factory/writing-profile.md:54` publishes WP-11 level-agnostically — "a steps section
… a heading that is not a steps heading" — and marks it `decidable`. The gate's anchor is
`STEPS_HEADING = /^## Steps\s*$/`: level two, exactly. A prose-only `### Steps` or `# Steps` section
violates the published rule, contributes no member to `stepsFiles`, and the set equality that produces
the WP-11 refusal never fires. The guard enforces a PROPER SUBSET of the rule the kit publishes as
decided, which is LANG-04's literal text failing.

**The cross-artifact mechanism is narrower than its own comment claims.** The case comment says the
guard's enforcement and the kit's documentation are held to ONE sentence in TWO artifacts, so a reword
of either alone is a red. `STEPS_RULE_SENTENCE` pins only the FIRST sentence, and the two remedy halves
already disagreed on the day they landed.

Purpose: this is the project's own named defect class — a prose claim wider than the assertion behind
it — sitting inside the rule table the phase ships. Live reachability is 0 (19 `Steps` headings in the
governed corpus, all `## Steps`), so the direction is latent and FAIL-OPEN, which is the direction
that ships.

Output: a published rule whose extension equals the gate's extension exactly, both sentences pinned in
both artifacts, and a tripwire that turns the remaining residual from a promise into a mechanism.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-CONTEXT.md
</context>

<round_3_standing_rules>
Identical to plan 29-27's block. The three that bite hardest here:

- **A green suite is not proof.** Live reachability of this defect is zero, so no suite could have
  caught it. Every task carries a planted-instance reproduction on a hermetic mirror: green before,
  red after — or, where the intended verdict is "not a finding", a demonstration that the tripwire
  moves.
- **Ask which SET the predicate ENUMERATES.** `stepsFiles` enumerates one spelling of a rule published
  for all spellings. That gap is the whole finding.
- **A residual with zero live instances needs a mechanism, not a note.** A recorded residual that
  nothing measures becomes live the first time somebody writes the shape, silently.

Build facts: `npm run build` then `npm run freshness` after every `.ts` edit. Regression suite is
`npx vitest run --exclude '**/scripts/e2e/**'`, never bare `npm test`. Counting greps use `grep -a`.
</round_3_standing_rules>

<tasks>

<task type="auto">
  <name>Task 1: the decision — the published rule is narrowed to the spelling the gate decides, and the narrowing carries its measurement</name>

  <read_first>
    - `agent-factory/writing-profile.md` — the whole rule table (lines ~44-54) and the adjacency-rule
      section beneath it, which is the shape a recorded narrowing should follow
    - `scripts/check-imperative-lexicon.ts:552-561` — `STEPS_HEADING`'s declaration and the recorded
      argument for why the private section-end predicate beneath it was deleted rather than widened
    - `scripts/check-imperative-lexicon.ts:1258-1271` — `STEPS_SECTION_RULE_ID` and
      `STEPS_SECTION_RULE`, and the paragraph explaining why they are deliberately NOT exported
    - `scripts/check-imperative-lexicon.ts:1400-1421` — the refusal that emits the rule and the remedy
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` § WR-05 — both halves,
      including the two remedy spellings side by side
    - `docs/audit/28-claim-registry.md` — the rows plan 29-02 registered for the writing profile (D-33),
      to determine whether any of them anchors on the WP-11 row
  </read_first>

  <files>agent-factory/writing-profile.md, scripts/check-imperative-lexicon.ts, docs/audit/28-claim-registry.md</files>

  <action>
    LANG-04 requires the guard to enforce EXACTLY the profile's decidable subset. Two directions close
    that equality: narrow the published rule to what the gate decides, or widen the gate and re-measure
    the corpus. **Take the narrowing**, and record the choice with its reason at the declaration rather
    than leaving it to be inferred.

    Why narrowing rather than widening, stated so a later phase does not re-open it as an obvious
    omission (the shape D-15 and D-03 already use): widening `STEPS_HEADING` to a level class would
    make the heading level and the SECTION-END level disagree — a `### Steps` section's bullets would
    be scanned to the next level-at-most-two heading, silently adopting sibling `###` sections' bullets
    — so a genuine widening also needs the shared authority's level parameter widened, which is a
    behaviour change to four gates at once and belongs in its own plan with its own corpus measurement,
    exactly as `frontmatter.ts`'s locator block already says. Narrowing removes a claim wider than its
    assertion, which is this repository's own named remedy for that class, and it moves no bytes in the
    governed corpus.

    Edit `agent-factory/writing-profile.md`'s WP-11 row so both of its sentences name the spelling the
    gate decides. Keep the rule id, keep the `decidable` marking, and keep the row's two-sentence
    shape. The remedy half adopts the gate's existing spelling verbatim, with a terminating period, so
    the two artifacts are byte-identical rather than merely equivalent.

    Add a short subsection to the profile's rationale area, in the style of the adjacency-rule section
    already there, recording: that WP-11 is decided for one spelling; that the other spellings are NOT
    decided and this is a disclosed floor rather than a claim; the live measurement (re-run it — round
    3 measured 19 `Steps` headings in the governed corpus, all at level two, and zero at any other
    level); and the direction (fail-open) with the tripwire from Task 3 named as its mechanism.

    Update `STEPS_SECTION_RULE` in `scripts/check-imperative-lexicon.ts` to the narrowed first sentence
    and introduce a companion constant for the remedy sentence, both still deliberately unexported for
    the reason recorded at `:1258-1266` — a shared import would prove only that one file reads another,
    when the property is that the guard and the kit's documentation SAY THE SAME THING TO AN AUTHOR.
    Rewrite the refusal at `:1414-1421` so it emits both constants and nothing hand-spelled beside
    them; the remedy text must come from the constant, never from an inline string literal.

    D-04's same-commit rule applies and is not optional. Before editing, determine whether the WP-11
    row is the verbatim anchor of a registered claim (D-33 registered the profile's own claims). If it
    is, flip that registry row's verbatim text in the SAME commit — Phase 28's D-25 precedent, whose
    bijection gate already catches a missed flip. If it is not, say so in the SUMMARY with the command
    that established it. Never leave the two to disagree and never record it later.
  </action>

  <verify>
    <automated>npm run build && node scripts/check-imperative-lexicon.js && node scripts/check-claim-anchors.js; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - The WP-11 row names its decided spelling in BOTH sentences. Row-scoped, so the rationale
      subsection's own prose cannot satisfy or defeat it:
      `grep -a '^| `WP-11`' agent-factory/writing-profile.md | grep -ac 'not a steps heading'`
      returns 0, and
      `grep -a '^| `WP-11`' agent-factory/writing-profile.md | grep -ac 'Steps'`
      is at least 2.
    - The gate's remedy text is emitted from a constant, not from an inline literal. In the
      comment-stripped source, the refusal's own line does not spell the remedy:
      `grep -a -v '^\s*[/*]' scripts/check-imperative-lexicon.ts | grep -ac 'move the explanatory paragraphs under a heading'`
      returns exactly 1 — the constant's declaration, and nowhere else.
    - `node scripts/check-imperative-lexicon.js` exits 0 on the live tree and its WP-11 arm still
      publishes a measurement with a denominator (D-08). Paste the PASS line.
    - `node scripts/check-claim-anchors.js` exits 0. If the WP-11 row was an anchor, show the registry
      row's diff beside the profile's diff in the SUMMARY, in one commit. If it was not, paste the
      command that established that.
    - `node scripts/check-diff-disposition.js` exits 0 — the profile edit is dispositioned per D-05, or
      the gate names why it needs no row. Do not clear this finding by narrowing the corpus.
    - The corpus measurement in the new rationale subsection is produced by a command pasted beside it
      in the SUMMARY, with `grep -a`. No number is transcribed from this plan or from round 3's report.
    - `npm run build && npm run freshness` reports every committed `.js` fresh.
  </acceptance_criteria>

  <reversibility rating="reversible">
    Two literals and one table row; reverting is the same three edits and the pre-change wording is in
    git. The widening alternative is the costly one — it would require the shared locator's level
    parameter to change and four gates to be re-measured — which is why it is deferred to its own plan
    with its reason recorded rather than taken here.
  </reversibility>

  <done>
    The rule the kit publishes and the rule the gate decides have the same extension. The narrowing,
    its reason, its live measurement and its remaining floor are recorded in the profile itself, and
    any registry row anchoring on the changed text flipped in the same commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the whole rule is pinned in both artifacts, and the pin is proven able to fail on either half</name>

  <read_first>
    - `scripts/check-imperative-lexicon.test.ts:1237-1290` — the existing two-artifact case and its
      comment claiming ONE sentence in TWO artifacts, which is the claim WR-05 showed to be wider than
      the assertion beneath it
    - `scripts/check-imperative-lexicon.ts` — the two rule constants as Task 1 leaves them
    - `agent-factory/writing-profile.md` — the WP-11 row as Task 1 leaves it
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` § WR-05 half 2
  </read_first>

  <files>scripts/check-imperative-lexicon.test.ts</files>

  <behavior>
    RED FIRST — write these against the pre-Task-1 artifacts and watch the second one fail, because a
    pin that has only ever been seen passing is the defect this task is closing.

    - The gate's refusal text contains the rule sentence AND the remedy sentence, both verbatim.
    - The profile's WP-11 row contains the rule sentence AND the remedy sentence, both verbatim,
      written as literals in the case rather than imported from the module — the reason is recorded at
      `check-imperative-lexicon.ts:1258-1266` and is not re-litigated here.
    - Rewording EITHER sentence in EITHER artifact is a red. Four combinations, and all four are
      exercised by the falsifiability probe below rather than assumed from two.
    - The rule id `WP-11` appears in both artifacts and in the refusal.
  </behavior>

  <action>
    Widen the two-artifact pin from one sentence to the WHOLE rule, and make its own claim exactly as
    wide as its assertion — which is the property this task exists to restore.

    Assert both sentences in both places. Do not settle for asserting the concatenation as a single
    string: a concatenation pin cannot say WHICH half drifted, and a failure that names only that a
    string moved is the failure mode round 3 charged against the cell-flip reproductions. Two named
    assertions per artifact, four in total, each with its own message naming the sentence and the
    artifact.

    Then make the pin FALSIFIABLE, per member rather than in aggregate. Run the same comparison
    against four mutated inputs — the rule sentence reworded in the gate's constant, the remedy
    sentence reworded in the gate's constant, the rule sentence reworded in a mirror of the profile,
    the remedy sentence reworded in a mirror of the profile — and require each mutation to fail, and to
    fail with the message naming that half. A probe that mutates one half and reports a red proves the
    other half is pinned only if the other half is mutated too; run all four.

    Rewrite the case's comment to state exactly what is now held, with no claim beyond it: which
    sentences, in which two artifacts, and that a reword of either alone in either place is a red,
    proven by four mutations rather than asserted. Delete the previous claim rather than amending it —
    a cost paragraph that survives the change it describes is the defect WR-01 was raised for one
    module over, and this round has now met it twice.
  </action>

  <verify>
    <automated>npm run build && npx vitest run scripts/check-imperative-lexicon.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Four independent assertions exist (rule and remedy, in the gate and in the profile), each with a
      distinct failure message naming the sentence and the artifact. Show all four messages by forcing
      each to fail once; paste the four failing outputs.
    - The four-mutation falsifiability probe exists and each arm has been SEEN failing. A probe whose
      arms have never failed is decoration; a probe run on two of four arms is the half-fix shape.
    - The case's comment claims nothing wider than the four assertions. Concretely, the comment does
      not describe the pin as covering a rule the case does not assert — verified by reading it aloud
      against the four assertion names in the SUMMARY, and recorded as a two-column table there
      (claim -> the assertion that holds it).
    - `npx vitest run scripts/check-imperative-lexicon.test.ts` exits 0, and the RED-first transcript
      showing the pre-Task-1 remedy mismatch is pasted in the SUMMARY.
    - No literal in the case is imported from `check-imperative-lexicon.ts`:
      `grep -a -c 'STEPS_SECTION_RULE' scripts/check-imperative-lexicon.test.ts` may be 0, and if the
      constant name appears at all it must not be in an `import` statement. State the check that
      established this.
  </acceptance_criteria>

  <reversibility rating="reversible">
    Test-only. Reverting loses coverage and nothing else.
  </reversibility>

  <done>
    The two-artifact mechanism is as wide as the sentence claiming it: both halves of WP-11 are pinned
    in both artifacts, each with its own named failure, and each of the four ways to drift has been
    demonstrated to red.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the disclosed floor gets a mechanism — a steps heading at any other level is a moved number, not a silence</name>

  <read_first>
    - `scripts/check-imperative-lexicon.ts:1268-1300` — `corpusBreakdown`, the per-part vacuity floor
      and the two-sided aggregate pin, which is the pattern this tripwire must follow rather than
      reinvent
    - `scripts/check-imperative-lexicon.ts` — `governedCorpus()` and `GOVERNED_CORPUS_PARTS`, the
      derivation the tally must run over
    - `scripts/frontmatter.ts:512-539` — the ONE locator, so the tally consumes it rather than adding
      a heading scan of its own
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION.md` — LANG-04's third
      enumerated `missing:` item, which this task closes
    - `docs/audit/29-locator-unification.md` §7 — how a residual is recorded in this project, including
      its direction and its live input set
  </read_first>

  <files>scripts/check-imperative-lexicon.ts, scripts/check-imperative-lexicon.test.ts</files>

  <behavior>
    - A prose-only `## Steps` section is still RED, naming WP-11 and both rule sentences. This is round
      2's win and must not regress.
    - A prose-only `### Steps` section is NOT a WP-11 finding — that is the INTENDED verdict under Task
      1's narrowing, and the case asserts the intended verdict explicitly rather than leaving the
      absence of a finding to be read as coverage.
    - The gate publishes, and pins two-sided, how many steps headings in the governed corpus sit at a
      level OTHER than the decided one. On today's tree that number is zero; the day it is not, the
      pin reds.
    - A `# Steps` section is covered by the same tally as a `### Steps` one. The floor is about every
      undecided spelling, not about the one the review happened to name.
  </behavior>

  <action>
    Give the residual a mechanism. A recorded residual with zero live instances and nothing measuring
    it becomes live silently the first time somebody writes the shape — which is how this phase's other
    latent fail-opens got here.

    Derive, over the same `governedCorpus()` the gate already walks, the count of lines that are a
    steps heading at ANY ATX level, and the count at the DECIDED level. Publish both in the guard's
    output beside the existing WP-11 measurement, so the residual's size is a number a reader can check
    by hand rather than a sentence. Derive the two counts with two SEPARATE expressions over the same
    lines — never one count and a subtraction of the other — because a tally computed by the loop it is
    auditing is round 3's WR-03 in miniature.

    Take fence-awareness from the shared toggle, not from a raw-line scan: a `## Steps` written inside
    a fenced example in a workflow is documentation and must not enter either tally. `fencedLineFlags`
    is the one answer to that and is already reachable from this module.

    Then FAIL BY NAME when the undecided count is non-zero, with a refusal that states the situation
    rather than a bare number: the profile decides WP-11 for one spelling; this file carries a steps
    heading at another; the gate cannot decide it; and the remedy is to spell the heading at the decided
    level or to widen the rule and the shared locator's level parameter together in a plan of their own.
    Naming the remedy is what stops the next reader clearing the red by deleting the tally.

    Record the tripwire's own limits at its declaration with the same honesty the rest of the module
    uses: it sees ATX headings at column zero only, because that is the authority's disclosed floor, so
    a setext steps heading is invisible to it exactly as it is to every other consumer (V-29-26-01).
    State it; do not fix it here.

    Add the cases in `scripts/check-imperative-lexicon.test.ts`: the `## Steps` prose-only RED control;
    the `### Steps` prose-only case asserting the intended verdict AND asserting the undecided tally
    moved by one; a `# Steps` case likewise; a fenced `## Steps` case asserting BOTH tallies are
    unmoved; and the two-sided pin on the live tree's undecided count with a planted-member
    falsifiability probe.
  </action>

  <verify>
    <automated>npm run build && npx vitest run scripts/check-imperative-lexicon.test.ts && node scripts/check-imperative-lexicon.js; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/check-imperative-lexicon.js` exits 0 on the live tree and its output carries both
      tallies. Paste the line. The decided-level tally must be greater than zero (round 3 measured 19)
      — a zero there would mean the WP-11 arm is vacuous, which is AP-1 and must be escalated.
    - The undecided tally is pinned two-sided against a number derived in the same session, and the
      planted-member probe is SHOWN failing: plant a prose-only `### Steps` section in a hermetic
      mirror of one governed file, re-run, and paste the refusal naming the file and the moved count.
    - ADVERSARIAL SELF-REPRODUCTION, recorded in the SUMMARY: under the PRE-plan build, the same
      planted `### Steps` file produces exit 0 with no finding and no moved number — the bypass, live.
      Under the POST-plan build it produces the named refusal. Both transcripts pasted. This is the
      only evidence that distinguishes a closed residual from a documented one.
    - The two tallies are derived by two separate expressions, evidenced by naming both expressions in
      the SUMMARY. A subtraction of one from the other is refused.
    - The fenced-`## Steps` case proves neither tally moves, so the tripwire cannot be tripped by
      documentation — the same boundary the rest of this round is defending.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0, with the pass count recorded beside round
      3's 1878 / 52-file baseline.
    - `git status --porcelain` shows no source file modified by a reproduction; every plant was on a
      mirror.
  </acceptance_criteria>

  <reversibility rating="reversible">
    One derived tally, one refusal and its cases. Reverting removes a tripwire and restores a silent
    residual, which is the state this task exists to leave behind.
  </reversibility>

  <done>
    LANG-04's third enumerated `missing:` item is held by a test rather than by prose: the currently
    invisible spelling has a case asserting the intended verdict, and the residual it leaves is
    measured, published, pinned two-sided and proven able to red.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `agent-factory/writing-profile.md` (a published kit rule) -> an author acting on it | the profile is instruction; a rule published wider than any mechanism decides it is an instruction nothing enforces |
| the governed corpus -> `guard_imperative_lexicon` | hand-authored markdown is the untrusted input; a spelling outside the enumerated set is the bypass |
| the gate's refusal text -> the author reading it | a refusal that names a rule the profile does not carry, or carries differently, sends the author to the wrong remedy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-31-01 | Repudiation | `writing-profile.md:54` publishing WP-11 wider than `STEPS_HEADING` decides | high | mitigate | Task 1 — the published rule is narrowed to the decided spelling, with the reason, the measurement and the remaining floor recorded in the profile |
| T-29-31-02 | Tampering | a prose-only steps section at an undecided level passing the gate silently | high | mitigate | Task 3 — the undecided-level tally is derived, published, pinned two-sided and refuses by name when non-zero |
| T-29-31-03 | Spoofing | a `## Steps` heading written inside a fenced example entering the tally as live data | medium | mitigate | Task 3 — both tallies take fence-awareness from the shared toggle; a case asserts neither moves |
| T-29-31-04 | Repudiation | the two-artifact pin claiming wider coverage than it holds | medium | mitigate | Task 2 — four assertions, four mutations, each seen failing; the previous claim deleted rather than amended |
| T-29-31-05 | Tampering | a registry row anchoring on the WP-11 text left to disagree with the edited row | high | mitigate | Task 1 — D-04's same-commit companion edit, with `check-claim-anchors` exit 0 as the criterion |
| T-29-31-06 | Information Disclosure | a setext steps heading invisible to the tripwire | low | accept | Recorded at the declaration as the authority's disclosed ATX floor (V-29-26-01), tree-wide rather than specific to this module, with its live count; deliberately not repaired here |
| T-29-31-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed. Zero runtime dependencies is a CLAUDE.md constraint and this round adds none; recorded rather than skipped |

ASVS level 1, block threshold `high`. Every `high` row is dispositioned `mitigate` with a named task.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 29-31)

**`agent-factory/writing-profile.md`**
- the WP-11 row, rewritten so both sentences name the decided spelling, keeping the id and the
  `decidable` marking
- a new rationale subsection recording the narrowing, its reason, the live corpus measurement, the
  remaining floor and its direction, and the Task 3 tripwire as that floor's mechanism

**`scripts/check-imperative-lexicon.ts`**
- `STEPS_SECTION_RULE` — narrowed first sentence (still unexported)
- a new companion constant for the remedy sentence (still unexported); the refusal emits both and
  spells neither inline
- a derived tally of steps headings at the DECIDED level and at any OTHER level, computed by two
  separate fence-aware expressions and published beside the existing WP-11 measurement
- a NAMED refusal when the undecided-level tally is non-zero, stating the remedy and refusing the
  delete-the-tally escape

**`scripts/check-imperative-lexicon.test.ts`** — new and rewritten permanent cases
- four assertions pinning both WP-11 sentences in both artifacts, each with its own failure message
- the four-mutation falsifiability probe (rule and remedy, gate and profile)
- the prose-only `## Steps` RED control
- the prose-only `### Steps` case asserting the intended verdict and the moved tally
- the prose-only `# Steps` case
- the fenced `## Steps` case asserting neither tally moves
- the two-sided pin on the live undecided count, plus its planted-member probe

**`docs/audit/28-claim-registry.md`**
- the registry row anchoring on the WP-11 text, flipped in the same commit — only if such a row
  exists; if none does, the SUMMARY carries the command that established it

**`29-31-SUMMARY.md`**
- the RED-first transcript for the remedy mismatch
- the four failing messages from the four-artifact assertions
- the four-mutation probe transcript
- the pre/post planted-`### Steps` reproduction
- the claim-to-assertion two-column table for Task 2's comment
</artifacts_this_phase_produces>

<verification>
- `npm run build && npm run freshness` — every committed `.js` matches a fresh rebuild.
- `node scripts/check-imperative-lexicon.js` exits 0 and publishes both steps-heading tallies.
- `node scripts/check-claim-anchors.js`, `node scripts/check-audit-register.js`,
  `node scripts/check-diff-disposition.js`, `node scripts/check-banned-claims.js`,
  `node scripts/check-foundation-guards.js` each exit 0.
- `npx vitest run --exclude '**/scripts/e2e/**'` exits 0.
- `node scripts/check-nul-bytes.js` exits 0, so the counting greps above are trustworthy.
</verification>

<success_criteria>
- LANG-04's three enumerated `missing:` items each map to a landed artifact, in a table in the SUMMARY.
- The rule the profile publishes and the rule the gate decides have the same extension.
- Both sentences are pinned in both artifacts and each of the four drift routes has been seen to red.
- The remaining floor is measured, published, pinned two-sided and proven able to red — not merely
  recorded.
- The planted-`### Steps` bypass is demonstrated GREEN under the pre-plan build and RED under the
  post-plan build.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-31-SUMMARY.md` when done.
</output>

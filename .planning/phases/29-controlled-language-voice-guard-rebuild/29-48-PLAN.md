---
phase: 29-controlled-language-voice-guard-rebuild
plan: 48
type: execute
wave: 45
depends_on: ["29-47"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LANG-07]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md`'s LANG-07 checkbox and LANG-07 traceability row both read the state round 6's verifier determined — `[x]` and `Complete` — and the authority for that state is named in the commit message, not inferred."
    - "`.planning/REQUIREMENTS.md`'s LANG-04 checkbox and LANG-04 traceability row are byte-unchanged by this plan and still read `[ ]` and `Gaps Found`. LANG-04's verdict belongs to round 7's verifier, and no plan in this round may pre-empt it."
    - "Every Phase 29 requirement id has exactly one checkbox line and exactly one traceability row, the two agree with each other, and the id set is derived from the file and its cardinality asserted independently of the loop that consumes it — so a silently SHORT enumeration reds rather than passing on a floor."
    - "No plan in round 7 declares `requirements-completed: [LANG-04]` in its SUMMARY. The inversion this plan corrects was produced by an automated marker acting on that field before any verification existed, and re-tripping it is the same act."
  artifacts:
    - ".planning/REQUIREMENTS.md — the LANG-07 checkbox line and the LANG-07 traceability row corrected to `[x]` / `Complete`, with every other Phase 29 row byte-unchanged"
    - "the SUMMARY's before-and-after transcript of all eight LANG checkbox lines and all eight LANG traceability rows, quoted in full with their real line numbers"
    - "the derived id-set audit: the LANG id enumeration taken from the file, its cardinality asserted two-sided, and every checkbox/row pair compared"
  key_links:
    - "29-VERIFICATION-round6.md's disposition table -> the two corrected lines -> the next round or ship step that reads REQUIREMENTS.md — the only authority for a requirement's state is a verifier's determination"
    - "a SUMMARY's `requirements-completed:` field -> the automated marker -> a requirement row — the process seam that produced the inversion, named so this round cannot re-trip it"
    - "the checkbox half of a requirement -> its traceability row -> a reader deciding whether the requirement is met — two surfaces for one fact, which is why they are compared rather than each trusted alone"
  prohibitions:
    - statement: "LANG-04 is not marked, flipped, annotated or touched by this plan in any way. A requirement marked Complete by a plan rather than by a verifier is the exact act that produced the state this plan corrects, and it is worse the second time because the failure is now documented."
      status: enforced
      verification: "`git diff .planning/REQUIREMENTS.md` shows exactly two changed lines, neither of them a LANG-04 line; `grep -c '^- \\[ \\] \\*\\*LANG-04\\*\\*' .planning/REQUIREMENTS.md` returns 1 and `grep -c '^| LANG-04 | Phase 29 | Gaps Found |$' .planning/REQUIREMENTS.md` returns 1, both quoted."
    - statement: "This plan's SUMMARY declares NO `requirements-completed:` entries at all — not LANG-04, and not LANG-07. This plan performs the marking by hand under the verifier's named authority; an automated marker acting on the same row afterwards is the process artifact that inverted it, and a second mechanism writing the same fact is a second authority."
      status: enforced
      verification: "The SUMMARY's frontmatter is quoted in full in the commit body and its `requirements-completed:` value is the empty list."
    - statement: "No requirement state is asserted from a plan, a SUMMARY, a roadmap line or this planner's reading. Every state written here is quoted from `29-VERIFICATION-round6.md`'s disposition table with its line reference, and anything that table does not decide is left exactly as found."
      status: enforced
      verification: "Each of the two edits carries the quoted verification-report row that authorises it; the SUMMARY names the report, its status field and its verification timestamp."
---

<objective>
Correct the one requirements-traceability row round 6 left inverted, and prove the other seven are what
the round-6 verifier determined.

Round 6's verification found `.planning/REQUIREMENTS.md` carrying the exact inverse of round 5's verified
recommendation: LANG-04 read `Complete` and LANG-07 read `Gaps Found`. That state was set by an automated
`requirements mark-complete` step reading each round-6 SUMMARY's `requirements-completed: [LANG-04]` field
on commit `d5360dc`, **before any round-6 verification existed**. It is a process artifact, not a verified
fact.

HEAD (`79c3457`) is **half-corrected**. LANG-04 has already been reverted to `[ ]` / `Phase 29 | Gaps
Found` and is correct as it stands. LANG-07 still reads `[ ]` at line 85 and `| LANG-07 | Phase 29 | Gaps
Found |` at line 186, and that is wrong: round 5 genuinely closed LANG-07 (plan 29-40 deleted the private
`parseFrontmatter`), round 6 regression-checked it clean (`grep -c "function parseFrontmatter"
scripts/generate-catalog.ts` = 0, `npm run freshness:catalog` fresh), and the round-6 verification's
disposition table says outright that it must read `[x]` / `Phase 29 | Complete`.

The round-6 report says this correction "belongs to the verifier/orchestrator applying this report, not to
a phase plan". It was not applied. Plan 29-47 deliberately left the file byte-unchanged to reserve the
verdict for verification (`git diff --exit-code f718069..HEAD -- .planning/REQUIREMENTS.md` exits 0), and
that reservation has now been discharged by a published report. This plan applies that report and nothing
else.

Purpose: the next round, the next ship step and the next reader all take this file as the state of record.
A requirement marked Complete that a verifier failed, and a requirement marked Gaps Found that a verifier
passed, are both a fabricated result in a trace surface — which is the one class this project's value
proposition cannot survive.

Output: two corrected lines, six proven-unchanged pairs, and a derived audit that compares each
requirement's two surfaces against each other rather than trusting either alone.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION-round6.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION-round5.md
@docs/audit/29-round6-residuals.md
</context>

<standing_rules>
The house rules of this phase, encoded rather than advised. The four that bite here:

- **No fabrication.** Never fake a passing gate, a test result, a citation or a requirement state. The
  state written here is quoted from a published verification report, with the report's own line reference.
- **A trail is not a tidy state.** Nothing outside the two lines this report decides is touched, however
  tempting the neighbouring rows look.
- **Derive the set, assert the count.** The requirement id set is taken from the file rather than typed,
  and its cardinality is derived independently of the loop that consumes it — a vacuity floor catches an
  EMPTY enumeration and never a SILENTLY SHORT one.
- **No silent drops.** Any disagreement the audit finds beyond the one this plan is authorised to fix is
  measured, named and escalated to the round-7 residual register — never quietly corrected and never
  absorbed.
</standing_rules>

<gap_contract_map>
| round-6 finding | discharged by |
|---|---|
| `V-29-47-05` / verification truth #5 — LANG-07 reads `Gaps Found` against the verifier's determination | Task 1 |
| verification truth #5 — LANG-04 must read `Gaps Found` (already true at HEAD; must stay true) | Task 1 (proven unchanged) and this plan's prohibitions |
| the process defect that produced the inversion — an automated marker acting on `requirements-completed:` before verification | this plan's prohibitions, carried into every round-7 plan |
| probe row LANG-07 / `unclassified` — "review manually" | Reviewed here and surfaced as a FLAGGED ASSUMPTION below; not resolvable by this round's work |
</gap_contract_map>

<flagged_assumptions>
**One unresolved probe row, surfaced rather than dropped.**

The deterministic edge probe returned three applicable rows for this round, all `unresolved`. Two of them
(`LANG-04` / `empty`, `LANG-04` / `encoding`) are answered by this round's design work and become plain
`must_haves.truths` in plans 29-52 and 29-53. The third is this one:

- **`LANG-07` / `unclassified` — "unclassified — review manually".** Reviewed. It cannot be resolved by
  this round, because round 7 performs **no LANG-07 mechanism work at all**: the requirement's subject is
  the single fence-parser authority, that authority was closed in round 5 by plan 29-40's deletion of the
  private `parseFrontmatter`, and round 6 regression-checked it clean. This round touches only LANG-07's
  **documentation state**. So the probe row is carried forward as an explicit flagged assumption:
  **assumed** that LANG-07's mechanism is unchanged by round 7, **verified** only to the extent of the
  regression greps this plan re-runs (`grep -c "function parseFrontmatter" scripts/generate-catalog.ts` = 0
  and `npm run freshness:catalog` clean), and **not** re-derived from first principles. Plan 29-54 touches
  `scripts/generate-catalog.ts` for IN-01/IN-02 and re-runs the same two regression checks, which is the
  only place in this round the assumption could be falsified.

**No-silent-drop equality, stated:** 3 probe rows surfaced == 2 authored into `must_haves.truths`
(29-52, 29-53) + 1 surfaced as a flagged assumption (here). No row was auto-resolved, auto-dismissed, or
marked `backstop`.

**Prohibition recall breadcrumb:** ten adversarial must-NOTs were over-produced against LANG-07's
documentation state and this plan's process seam; seven were dropped as routine engineering (build
freshness, lockfile stability, commit hygiene, test-lane choice, line-ending stability, file-permission
stability, editor formatting); three survived on values/process-integrity grounds and are authored
descriptor-less under `must_haves.prohibitions`. None of the ten was canon security or compliance, so
nothing was dropped on that ground.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: the two lines the round-6 verifier decided, corrected under its named authority</name>

  <precondition>
    `29-VERIFICATION-round6.md` exists at
    `.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION-round6.md`, its frontmatter
    `status:` reads `gaps_found`, and its "Requirements traceability inversion" section carries the
    two-row disposition table. Halt and report if any of those three is not true: without a published
    determination this plan has no authority to write a requirement state, and writing one anyway is the
    act it exists to correct.
  </precondition>

  <read_first>
    - `.planning/REQUIREMENTS.md:77-87` — the eight LANG checkbox lines in full, so the LANG-07 line is
      edited in the form the file actually uses (`- [ ] **LANG-07**: …`) rather than a remembered form
    - `.planning/REQUIREMENTS.md:176-192` — the traceability table rows for Phase 28, Phase 29 and Phase
      29.1, so the LANG-07 row is edited in the column form the table actually uses
      (`| LANG-07 | Phase 29 | Gaps Found |`) and the surrounding rows are seen before anything moves
    - `29-VERIFICATION-round6.md` § "Requirements traceability inversion — explicit disposition" — the
      two-row table that is the ONLY authority for what these lines must read, plus the sentence recording
      that plan 29-47 left the file byte-unchanged to reserve this verdict
    - `29-VERIFICATION-round6.md` § "Goal Achievement" truth #4 — the LANG-07 regression evidence
      (`grep -c "function parseFrontmatter" scripts/generate-catalog.ts` = 0, `freshness:catalog` clean),
      which is the measurement behind the state being written
    - `29-VERIFICATION-round5.md` § recommendation — round 5's original verdict, so the correction is
      shown to restore a determination rather than to invent one
    - `docs/audit/29-round6-residuals.md` §3.6 (`V-29-47-05`) — the round's own record of the inversion,
      including the commit (`d5360dc`) and the mechanism (an automated per-SUMMARY marker) that set it
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <behavior>
    - The LANG-07 checkbox line reads `[x]` and is otherwise byte-identical to its current text.
    - The LANG-07 traceability row reads `Complete` in its third column and is otherwise byte-identical.
    - The LANG-04 checkbox line and the LANG-04 traceability row are byte-unchanged and still read `[ ]`
      and `Gaps Found`.
    - Every other line in the file is byte-unchanged; the diff is exactly two changed lines.
    - The commit body names the verification report, its status, its timestamp, and quotes the disposition
      row that authorises each edit.
  </behavior>

  <action>
    Read both target lines first and record their real line numbers and exact current text; the addresses
    in this plan were measured at planning time and a file that has moved underneath them must be reported
    rather than edited by line number. The checkbox line begins `- [ ] **LANG-07**:` and the traceability
    row is `| LANG-07 | Phase 29 | Gaps Found |`.

    Make exactly two edits, each a scoped replacement rather than a rewrite of the file. In the checkbox
    line change `- [ ] **LANG-07**:` to `- [x] **LANG-07**:` and change nothing else on that line — the
    requirement's prose, its guard names and its punctuation stay byte-identical. In the traceability row
    change the third cell from `Gaps Found` to `Complete` and change nothing else — the pipe spacing, the
    id cell and the phase cell stay byte-identical.

    Touch nothing else. Do NOT mark LANG-04. Do NOT adjust LANG-01, LANG-02, LANG-03, LANG-05, LANG-06 or
    LANG-08 even if a neighbouring row looks stale against your own reading: those states were not decided
    by the round-6 report, this plan carries no authority over them, and a plan writing a requirement state
    a verifier did not determine is the exact defect being corrected here. If the audit in task 2 finds a
    disagreement beyond LANG-07, record it as a finding for the round-7 residual register and leave the
    file alone.

    Write the commit body so the authority is legible without opening another file. Name
    `29-VERIFICATION-round6.md`, quote its `status:` field and its `verified:` timestamp, and quote both
    rows of its disposition table verbatim. State in the body that this plan makes no determination of its
    own and that LANG-04's verdict is reserved for round 7's verifier.
  </action>

  <verify>
    <automated>grep -c '^- \[x\] \*\*LANG-07\*\*' .planning/REQUIREMENTS.md; grep -c '^| LANG-07 | Phase 29 | Complete |$' .planning/REQUIREMENTS.md; grep -c '^- \[ \] \*\*LANG-04\*\*' .planning/REQUIREMENTS.md; grep -c '^| LANG-04 | Phase 29 | Gaps Found |$' .planning/REQUIREMENTS.md; git diff --numstat .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*LANG-07\*\*' .planning/REQUIREMENTS.md` returns exactly `1`, and the matching
      line number is reported.
    - `grep -c '^| LANG-07 | Phase 29 | Complete |$' .planning/REQUIREMENTS.md` returns exactly `1`, and
      the matching line number is reported.
    - `grep -c '^- \[ \] \*\*LANG-07\*\*' .planning/REQUIREMENTS.md` returns `0` and
      `grep -c '^| LANG-07 | Phase 29 | Gaps Found |$' .planning/REQUIREMENTS.md` returns `0` — the old
      state survives nowhere in the file.
    - `grep -c '^- \[ \] \*\*LANG-04\*\*' .planning/REQUIREMENTS.md` returns exactly `1` and
      `grep -c '^| LANG-04 | Phase 29 | Gaps Found |$' .planning/REQUIREMENTS.md` returns exactly `1`.
    - `grep -c 'LANG-04 | Phase 29 | Complete' .planning/REQUIREMENTS.md` returns `0`.
    - `git diff --numstat .planning/REQUIREMENTS.md` reports exactly `2	2	.planning/REQUIREMENTS.md`,
      and the full `git diff` output is quoted in the SUMMARY.
    - The SUMMARY quotes both target lines before and after with their real line numbers, and quotes the
      two rows of `29-VERIFICATION-round6.md`'s disposition table that authorise them.
    - The SUMMARY's `requirements-completed:` frontmatter field is the empty list, and that field's value
      is quoted verbatim in the commit body.
  </acceptance_criteria>

  <reversibility rating="reversible">
    Two cell edits in a planning document, each traceable to a published determination and each revertible
    with a one-line `git revert` against a two-line diff.
  </reversibility>

  <done>
    LANG-07 reads what the round-6 verifier determined, LANG-04 reads what it read before this plan
    started, and the commit names the report that decided both.
  </done>
</task>

<task type="auto">
  <name>Task 2: the derived audit — every Phase 29 requirement's two surfaces compared against each other</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` in full for the `### LANG` section and the traceability table — the two
      places a requirement's state is written, which are the two surfaces this task compares
    - `29-VERIFICATION-round6.md` § "Requirements Coverage" — the report's own statement that all eight
      `LANG-` ids are present in the Phase 29 mapping with no gaps in the enumeration, which this task
      re-derives rather than adopts
    - `docs/audit/29-round6-residuals.md` §3.6 — the residual's own description of the marking mechanism,
      so the audit's finding format matches the register that will carry any escalation
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-47-SUMMARY.md` — the SUMMARY that
      recorded leaving this file byte-unchanged, so the audit's before-state is anchored to a commit rather
      than to a memory
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <behavior>
    - The LANG id set is DERIVED from the file — read off the checkbox lines — never typed into the audit.
    - The cardinality of that set is derived a SECOND time by a different route (the traceability rows),
      and the two derivations are compared. A count taken only by the loop that consumes it cannot
      distinguish a complete enumeration from a silently short one.
    - Both derivations are floored above zero before they are compared, so two empty sets cannot agree.
    - Every id's checkbox state and traceability state are compared with each other; a `[x]` against a
      non-`Complete` row, or a `[ ]` against a `Complete` row, is a finding.
    - Any finding beyond LANG-07 is recorded for escalation and NOT corrected in this plan.
  </behavior>

  <action>
    Derive the id set twice, by two different routes over the same file, and assert the two agree. Take one
    enumeration from the checkbox lines under the `### LANG` heading and one from the `| LANG-…| Phase 29 |`
    rows of the traceability table. Report both cardinalities, report both id lists sorted, and report the
    symmetric difference. Floor each cardinality above zero before comparing them and say in the SUMMARY
    why: an equality between two empty enumerations is the emptiest possible green, and a floor on one side
    alone still passes when the other side is what went short.

    Then compare the two surfaces per id. For each id, pair its checkbox mark with its traceability cell
    and record the pair. `[x]` pairs with `Complete`; `[ ]` pairs with anything that is not `Complete`
    (`Gaps Found`, `Pending`). Report the full eight-row pairing table in the SUMMARY with the real line
    number of each of the sixteen lines, so a reader can check the whole thing by hand rather than take it.

    State the expected pairing before you run it, then run it, then compare. After task 1, expect exactly
    one `[x]`/`Complete` pair (LANG-07) and seven non-`Complete` pairs, of which LANG-04, LANG-01, LANG-03,
    LANG-05 and LANG-06 read `Gaps Found` and LANG-02 and LANG-08 read `Pending`. Do not fit the file to
    that expectation: if the derivation disagrees, the derivation is the measurement and the expectation is
    the thing that was wrong, and the disagreement is a finding.

    Escalate rather than fix. If any pair disagrees with itself, or if the two id enumerations differ, or
    if any id appears twice on either surface, record it as a numbered finding with both line numbers and
    both texts, name it for the round-7 residual register (plan 29-55 owns that document), and leave the
    file untouched. This plan's whole subject is that a requirement state written without a verifier's
    determination is a fabricated result, so discovering a second one is not licence to write a second one.
  </action>

  <verify>
    <automated>grep -c '^- \[.\] \*\*LANG-0[1-8]\*\*' .planning/REQUIREMENTS.md; grep -c '^| LANG-0[1-8] | Phase 29 | ' .planning/REQUIREMENTS.md; grep -n '^- \[.\] \*\*LANG-0[1-8]\*\*\|^| LANG-0[1-8] | Phase 29 | ' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^- \[.\] \*\*LANG-0[1-8]\*\*' .planning/REQUIREMENTS.md` returns `8`.
    - `grep -c '^| LANG-0[1-8] | Phase 29 | ' .planning/REQUIREMENTS.md` returns `8`.
    - Both derived id lists, sorted, are quoted in the SUMMARY and are equal; their symmetric difference is
      reported as the empty set.
    - Both cardinalities are reported and both are greater than zero, with the floor stated before the
      equality.
    - The eight-row pairing table is quoted in the SUMMARY with all sixteen real line numbers.
    - Exactly one pair reads `[x]`/`Complete` and it is LANG-07; the pairing of LANG-04 is
      `[ ]`/`Gaps Found`.
    - `grep -c '^| LANG-0[1-8] | Phase 29 | Complete |$' .planning/REQUIREMENTS.md` returns exactly `1`.
    - Any disagreement found is recorded as a numbered finding with both addresses and both texts, and
      `git diff --numstat .planning/REQUIREMENTS.md` still reports exactly `2	2	.planning/REQUIREMENTS.md`
      after this task — the audit changed nothing.
    - The SUMMARY states explicitly that this plan made no determination of its own and that LANG-04's
      verdict is reserved for round 7's verifier.
  </acceptance_criteria>

  <done>
    Both surfaces of every Phase 29 requirement are derived, counted twice by two routes, compared against
    each other, and published as a table a reader can check by hand — with nothing corrected beyond the one
    row a published report decided.
  </done>
</task>

</tasks>

<artifacts_this_plan_produces>
| symbol | file | kind |
|---|---|---|
| the corrected LANG-07 checkbox line | `.planning/REQUIREMENTS.md` | one-cell edit, `[ ]` -> `[x]` |
| the corrected LANG-07 traceability row | `.planning/REQUIREMENTS.md` | one-cell edit, `Gaps Found` -> `Complete` |
| the eight-row checkbox/row pairing table | `29-48-SUMMARY.md` | derived audit output, sixteen line numbers quoted |
| the two-route id enumeration and its symmetric difference | `29-48-SUMMARY.md` | derived cardinality, floored on both sides |
| the empty `requirements-completed:` declaration | `29-48-SUMMARY.md` frontmatter | the process-seam prohibition made mechanical for this plan |
| the LANG-07 / `unclassified` flagged assumption | this plan's `<flagged_assumptions>` and `29-48-SUMMARY.md` | probe row carried forward, never dropped |

Values that MOVE rather than being created: the LANG-07 checkbox mark and the LANG-07 traceability cell.
Values that must NOT move: every other byte of `.planning/REQUIREMENTS.md`, and in particular both
LANG-04 surfaces.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a SUMMARY's `requirements-completed:` field -> an automated marker -> a requirement row | An unverified field writes a verified-looking state |
| a plan -> a requirement's state of record | A plan has no authority to determine a requirement met |
| a checkbox mark -> a traceability cell | Two surfaces for one fact can disagree silently |
| `.planning/REQUIREMENTS.md` -> the next round, the next ship step, the next reader | This file IS the state of record |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-48-01 | Spoofing | a requirement state written by a plan rather than by a verifier | high | mitigate | Every edit quotes the verification report row that authorises it; the plan makes no determination of its own (task 1) |
| T-29-48-02 | Tampering | the automated `requirements mark-complete` step re-tripping on this round's SUMMARYs | high | mitigate | `requirements-completed:` is the empty list in this plan's SUMMARY and forbidden to contain LANG-04 in every round-7 plan (prohibitions) |
| T-29-48-03 | Repudiation | a correction that quietly touches a row no report decided | high | mitigate | `git diff --numstat` asserted at exactly two changed lines and quoted in full (task 1) |
| T-29-48-04 | Information disclosure | a checkbox and a traceability row disagreeing unnoticed | medium | mitigate | The two surfaces are paired per id and published as a sixteen-address table (task 2) |
| T-29-48-05 | Denial of service | an id enumeration that went silently short passing a vacuity floor | medium | mitigate | Two independent derivations of the id set, both floored above zero, compared by symmetric difference (task 2) |
| T-29-48-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan; no source file is touched. RESEARCH.md carries no new package row for this round, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- The LANG-07 checkbox and traceability row read `[x]` and `Complete`; the old state survives nowhere.
- The LANG-04 checkbox and traceability row are byte-unchanged and still read `[ ]` and `Gaps Found`.
- The diff is exactly two changed lines and is quoted in full.
- The LANG id set is derived twice by two routes, both floored above zero, and the two agree.
- All eight checkbox/row pairs are published with their real line numbers and each pair agrees with itself.
- Any disagreement beyond LANG-07 is recorded as a finding and escalated, not corrected.
</verification>

<success_criteria>
- `.planning/REQUIREMENTS.md` reads what the round-6 verifier determined, and the commit says who decided.
- LANG-04's verdict is untouched and visibly reserved for round 7's verifier.
- The process seam that produced the inversion cannot re-trip on this plan, and the constraint is stated
  where every other round-7 plan inherits it.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-48-SUMMARY.md` when done.
</output>

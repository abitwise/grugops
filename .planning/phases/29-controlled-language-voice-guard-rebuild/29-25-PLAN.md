---
phase: 29-controlled-language-voice-guard-rebuild
plan: 25
type: execute
wave: 24
depends_on: ["29-20", "29-21", "29-22", "29-23", "29-24"]
gap_closure: true
files_modified:
  - scripts/voice-model.test.ts
  - scripts/check-foundation-guards.test.ts
autonomous: true
requirements: [LANG-06, LANG-07]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-05 is closed: the live-role control asserts the DERIVED role count rather than a floor of greater-than-zero. `listRoles()` throws on an empty or missing roles directory, so the old assertion could not fail under any input the call survives — it was the floor its own comment says is not enough, and the number it promised to protect was never compared."
    - "WR-06 is closed on both axes: the two byte-identical assertions standing for two different consumers become an OCCURRENCE COUNT of exactly two, because a substring test is satisfied by whatever satisfied it the first time and a build in which one consumer went silent passed that case; and the hard-coded role filename in the strongest assertion of the case is built from the constant the file already declares, so repointing the constant cannot make the assertion vacuous."
    - "IN-03 is closed: the two plant sites that spell the role directory inline are routed through the `rolePath` helper plan 29-14 introduced, so the role directory is named in exactly one more place than the derivation itself."
    - "LANG-07 is proven rather than asserted: a DERIVED, tree-wide scan over every non-test module under `scripts/` finds every section-extent predicate construct and requires that `scripts/frontmatter.ts` is its only member. The scan is two-sided — member list and cardinality — and is proven falsifiable by running its own classifier with one construct removed (LANG-07, D-24)."
    - "The section-locator CONSUMER list is pinned two-sided in the same shape `check-foundation-guards.test.ts` already uses for the eight fence-toggle consumers: the modules importing the authority are derived from the tree, sorted, compared to a written-down list, and their cardinality pinned as a number. A hand-maintained consumer list is the set-literal drift this repository has corrected three times."
    - "The harness's own defect CLASS is closed derivationally, not one address at a time: a scan over the guard test files finds adjacent byte-identical assertion lines inside one test body and requires zero, because WR-06 is the third instance of that shape this project has recorded and hand-fixing the third does nothing about the fourth."
    - "EDGE (LANG-06): the strengthened live-role control is proven able to fail — a mirror short by one role file must red it — because a control that has never been shown to fail is a comment."
    - "EDGE (LANG-07): the tree-wide scan's NON-VACUITY is asserted before its membership claim. An enumeration that found nothing cannot report that only one module is a member, and this project has recorded a vacuity floor catching an empty denominator while missing a silently short one."
    - "Measured after the edits: all four gates exit 0 with the numbers plans 29-20 through 29-24 published, and the full regression suite excluding the live e2e lane exits 0."
  prohibitions:
    - "Never assert a floor where the derived number is available — a floor that the call's own refusal already guarantees is documentation of intent, never a check."
    - "Never let a substring assertion stand for an occurrence count; two identical `toContain` calls assert one thing twice."
    - "Never hand-list a consumer set or a predicate-site set; derive it, sort it, compare it two-sided, and prove the derivation can fail."
    - "Never fix a harness defect only at the address the review named — this round exists because round 1 fixed three sites of a finding and a fourth nobody had derived stayed open."
    - "Never run bare `npm test` — it triggers the live claude-CLI e2e lane, spends the user's tokens and can hang for around eight minutes."
  artifacts:
    - scripts/voice-model.test.ts
    - scripts/check-foundation-guards.test.ts
  key_links:
    - "`scripts/kit-model.ts` exports `ROLE_COUNT` and `listRoles`; the strengthened control compares the derived list against the derived count rather than against a floor."
    - "`scripts/check-foundation-guards.test.ts:663-754` is the two-sided pin the new section-locator consumer pin is modelled on, and `scripts/frontmatter.test.ts:5437-5546` is the derived-scan pattern the tree-wide predicate scan is modelled on."
    - "`rolePath(root, name)` in `scripts/check-foundation-guards.test.ts` is the single place the role directory is named inside the harness; the two inline spellings are routed through it."
---

<objective>
Make the harness tell the truth about what it asserts, and turn LANG-07 from a claim into a
derivation.

Purpose: across four straight rounds this project's own verification harness produced a false result
in six instances, and round 2 found three more — a live-corpus control that cannot fail, a case
duplicating one assertion to stand for two consumers, and a case asserting a source substring while
its name claims a property. Separately, LANG-07 has so far been argued from reading rather than
measured: round 1 verified it on delimiter-sharing evidence and round 2 found four disagreeing
section grammars. A claim about how many implementations of a predicate exist should be a
derivation, not a sentence.

Output: three harness defects closed, their shared class closed derivationally, and a two-sided,
falsifiable, tree-wide proof that exactly one module owns the section-locator predicate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-20-SUMMARY.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-22-SUMMARY.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-24-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-05, WR-06 and IN-03 — three assertions that assert less than their names claim</name>
  <files>scripts/voice-model.test.ts, scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - `scripts/voice-model.test.ts:215-233` — the live-role control, its comment promising that a short denominator would let a role slip out silently, and the floor assertion that cannot fail.
    - `scripts/kit-model.ts:107` and `:676-700` — `ROLE_COUNT`, `listRoles` and `refuseEmpty`, which is why the existing floor is unreachable.
    - `scripts/check-foundation-guards.test.ts:4242-4260` — the two byte-identical assertions, the comment claiming both consumers are checked, and the hard-coded role filename in the negative regex.
    - `scripts/check-foundation-guards.test.ts:4080` and `:4210` — the `MALFORMED_ROLE` constant and the `rolePath` helper.
    - `scripts/check-foundation-guards.test.ts:4137` and `:4282` — the two inline spellings of the role directory.
  </read_first>
  <action>
Fix three assertions whose names promise more than their bodies deliver, and prove each fix reaches
the property its name claims.

WR-05: replace the greater-than-zero floor in the live-role control with an assertion that the
derived role list has exactly the derived role count, importing the count from the kit authority
rather than typing a number. Record in the comment why the floor was unreachable — the lister refuses
an empty directory before returning, so the floor was guaranteed by the call it was checking. Then
prove the strengthened control can fail: add a sibling case that copies the mirror-shortening shape
the gate harness already uses and asserts the control's assertion goes red when a role is removed. A
control that has never been shown to fail is a comment.

WR-06 axis one: replace the two byte-identical substring assertions with an occurrence count. Split
the captured output on newlines, filter for the finding line both consumers emit, and assert the
result has exactly two members with a message naming the property — that each of the two voice guards
must name the file and the reason independently. State in the comment that `toContain` is a substring
test rather than an occurrence count, so the second call was satisfied by whatever satisfied the
first, and a build in which one consumer went silent passed the case.

WR-06 axis two: build the negative regex from the declared role constant rather than from a
hard-coded filename, escaping the constant's dot. Repointing the constant currently makes the
strongest assertion in the case — that no wrong-bytes measurement line exists for the planted role —
match nothing and therefore pass vacuously. Prove the fix: add a temporary local re-point inside a
scratch assertion, or assert that the constructed pattern actually matches a synthesized measurement
line, so the regex is shown to be capable of matching before it is asserted not to.

IN-03: route the two inline role-directory spellings through the `rolePath` helper. The helper exists
so the directory is named in exactly one more place than the derivation itself; two sites bypassing
it means a directory rename leaves the harness planting into a path the guard does not read.

Do not change any case's verdict. Every one of these is an assertion strengthening, and if any of
them turns a passing case red, that red is a REAL defect the weak assertion was hiding — report it
and stop rather than weakening the new assertion.
  </action>
  <verify>
    <automated>npx vitest run scripts/voice-model.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/voice-model.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'` exits 0. If any case reds, the SUMMARY reports the defect the old assertion was hiding and execution stops rather than weakening the new assertion.
    - The live-role control compares the derived list against the derived count, and its falsifiability sibling reds on a mirror short by one role.
    - The two-consumer assertion is an occurrence count of exactly two with a message naming the property; running it against output containing only one of the two lines reds, demonstrated in the SUMMARY.
    - The negative regex is built from the declared constant, and the case demonstrates the constructed pattern can match before asserting it does not.
    - `grep -n 'agent-factory/roles' scripts/check-foundation-guards.test.ts | grep -v 'rolePath' | grep -v '^\s*//'` returns only the helper's own declaration and any occurrence inside an expected-output string; every plant site goes through the helper. The surviving lines are listed in the SUMMARY with a reason each.
  </acceptance_criteria>
  <done>Three assertions now reach the properties their names claim, and each strengthening is proven able to fail.</done>
</task>

<task type="auto">
  <name>Task 2: LANG-07 as a derivation — one owner of the section predicate, tree-wide and two-sided</name>
  <files>scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - `scripts/frontmatter.test.ts:5437-5546` — the derived fence-machine scan: the construct arrays, the comment-stripping classifier, the parameterized single classifier used for the falsifiability probe, the two-sided member list, the pinned cardinality, and the recorded list of shapes the floor would miss. This is the pattern to follow, not a pattern to reinvent.
    - `scripts/check-foundation-guards.test.ts:663-754` — the two-sided consumer list and the `nonTestScripts()` / `importedSymbols()` helpers.
    - `scripts/frontmatter.ts` — the authority's two exported functions.
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-22-SUMMARY.md` — the module-scoped locator-site derivation plan 29-22 wrote, which this task generalizes tree-wide.
  </read_first>
  <action>
Turn LANG-07 into something a machine checks.

Add a describe block to `scripts/check-foundation-guards.test.ts` carrying two derived, two-sided
assertions.

The OWNER scan: over every non-test module under `scripts/`, with comment lines stripped the way the
fence-machine classifier already strips them, find every construct that answers a section-extent
question. A section-extent construct is a conjunction, and requiring both halves is what keeps a
module that merely MENTIONS a heading out of the answer: a heading recogniser — an anchored regex
over one or two leading hash characters followed by a space, or a prefix test for the same — used in
a position that TERMINATES or BOUNDS a scan, which in this tree means a loop break, a bound
assignment or a return of an index. Derive the member list, sort it, assert it equals a written-down
list whose only member is the authority module, and pin the cardinality as a number.

Assert NON-VACUITY first, before the membership claim. An enumeration that found nothing cannot
report that only one module is a member. Take the floor from a source the scan does not produce: the
count of non-test modules scanned must be greater than zero and the classifier must find at least one
member. This project has recorded a vacuity floor catching an empty denominator while missing a
silently short one, so state both halves.

Prove the classifier falsifiable, exactly as the fence-machine scan proves its own conjunction: run
it with one recogniser construct removed and assert the derived list changes. A classifier that
returns the same answer with a construct removed was never using that construct.

Record, in the block's comment, what this floor would MISS — a recogniser built from concatenated
fragments or a constructed regular expression, a heading test written as a slice or index comparison,
and a bound expressed through a helper this scan does not read. It is a floor against the shapes a
second grammar plausibly takes, not a proof that none can exist, and saying so is the difference
between a measurement and a claim.

The CONSUMER scan: derive every non-test module importing either of the authority's two locator
functions, sort it, assert it equals a written-down list, and pin its cardinality. After plans 29-20
through 29-24 that list is the four guard modules; write down whatever the derivation reports rather
than whatever this plan expects, and if it differs, the difference is a real finding — a consumer
that never adopted the authority is a LANG-07 failure and is escalated, not absorbed.

Assert additionally that no module outside the authority declares a constant or function whose role
is to close a section, by requiring the owner scan's member list and the consumer scan's member list
to be disjoint except for the authority itself.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'` exits 0.
    - The owner scan's derived member list is recorded in the SUMMARY verbatim and contains exactly one module. If it contains more, each extra is named and escalated as a LANG-07 failure rather than added to the expected list.
    - The non-vacuity assertions come before the membership assertion in the case body, and both halves are present — a non-zero scanned count and a non-zero found count.
    - The falsifiability probe demonstrates the derived list changes when one construct is removed, asserted in the same case.
    - The consumer scan's derived member list and cardinality are recorded in the SUMMARY, and the written-down list is what the derivation reported rather than what this plan predicted.
    - The comment records at least three shapes the floor would miss.
  </acceptance_criteria>
  <done>LANG-07's central claim is a two-sided, falsifiable, non-vacuous derivation over the live tree rather than a sentence in a report.</done>
  <reversibility rating="reversible">The scan is a test-only addition; if its classifier proves too coarse it is tightened with its measurement recorded, the same way the fence-machine scan's construct list has been.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Close the harness defect CLASS derivationally, not one address at a time</name>
  <files>scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - `scripts/check-foundation-guards.test.ts` — the whole file's assertion style, and Task 1's fixes.
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` — WR-05, WR-06 and WR-07, three instances of one shape.
    - `scripts/frontmatter.test.ts:5437-5546` — the derived-scan pattern, again, for the classifier and its falsifiability probe.
  </read_first>
  <action>
WR-06 is the third recorded instance of a harness assertion that asserts less than its name claims.
Fixing the third does nothing about the fourth, so close the class.

Add a derived tripwire over the guard test files — the four gate test modules and the voice model's
test module — that finds, inside one test body, two adjacent assertion lines whose text is
byte-identical after trimming, and requires zero such pairs. Two identical assertions assert one
thing twice, and the comment above them almost always claims two.

Assert the tripwire's own premise before its claim: the scan must report a non-zero number of test
files read and a non-zero number of assertion lines classified, so a scan that read nothing cannot
report zero duplicates. Then prove it falsifiable by running the same classifier over a synthesized
source string containing a known duplicate pair and asserting it is found. Build that string from
character codes or from concatenated fragments so this file's own source never carries a real
duplicate pair — the self-reference is not hypothetical, and a harness that fails its own premise is
the exact defect this task exists to delete.

Record in the comment what the tripwire does NOT catch: two identical assertions separated by another
line, two assertions that differ only in whitespace inside a template literal, and an assertion
duplicated across two cases rather than within one. It is a floor against the shape this project has
now met three times, not a proof.

If the tripwire finds a pair anywhere in the guard test files, that pair is a REAL finding: fix it in
this task, record it in the SUMMARY with its location, and do not narrow the classifier to exclude
it.
  </action>
  <verify>
    <automated>npx vitest run scripts/check-foundation-guards.test.ts scripts/check-diff-disposition.test.ts scripts/check-imperative-lexicon.test.ts scripts/check-banned-claims.test.ts scripts/voice-model.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/check-foundation-guards.test.ts scripts/check-diff-disposition.test.ts scripts/check-imperative-lexicon.test.ts scripts/check-banned-claims.test.ts scripts/voice-model.test.ts --exclude '**/scripts/e2e/**'` exits 0.
    - The tripwire asserts a non-zero file count and a non-zero classified-assertion count before asserting zero duplicate pairs.
    - The falsifiability probe finds a known duplicate pair in a synthesized source built so this file carries no real pair of its own.
    - Any duplicate pair the tripwire finds in the live tree is fixed in this task and recorded in the SUMMARY with its file and line; the classifier is not narrowed to exclude it.
    - The comment records at least three shapes the tripwire does not catch.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0 across the whole suite, and all four gates exit 0 on the live tree with the numbers plans 29-20 through 29-24 published.
    - `npm run freshness` and `npx tsc --noEmit` exit 0; `git status --porcelain` shows only this plan's files plus the three pre-existing out-of-scope entries.
  </acceptance_criteria>
  <done>The class of harness defect that produced WR-05, WR-06 and WR-07 has a derived, falsifiable, non-vacuous tripwire, and the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the verification harness → the claim that a guard works | Every fix in this round is believed on the strength of a test. A test that asserts less than its name claims is the boundary being crossed, and this project has recorded six instances of it producing a false result. |
| the tree's module set → the LANG-07 claim | "How many implementations of this predicate exist" has until now been answered by reading. A hand-read answer was wrong twice in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-25-01 | Repudiation | a control that cannot fail | high | mitigate | The greater-than-zero floor is replaced by the derived count, and a sibling case proves the strengthened control reds on a short mirror. |
| T-29-25-02 | Information disclosure (false assurance) | two byte-identical substring assertions | high | mitigate | Replaced by an occurrence count of exactly two, demonstrated to red when only one line is present. |
| T-29-25-03 | Tampering | a hard-coded filename inside a negative regex | medium | mitigate | The pattern is built from the declared constant and demonstrated capable of matching before being asserted not to. |
| T-29-25-04 | Repudiation | a hand-read count of section-predicate implementations | high | mitigate | Replaced by a derived, two-sided, non-vacuous, falsifiability-probed tree-wide scan, with the shapes it would miss recorded. |
| T-29-25-05 | Spoofing | a tripwire whose own source defeats its premise | medium | mitigate | The falsifiability fixture is assembled from character codes so the scanning file never carries a real instance of what it scans for, the same precaution the fence-machine scan already takes. |
| T-29-25-SC | Tampering | npm / pip / cargo installs | high | mitigate | No package-manager install occurs in this plan; the dependency set is unchanged and the package-legitimacy gate has an empty input set. Escalate rather than install if that changes. |
</threat_model>

<artifacts_this_phase_produces>
Modified assertions in `scripts/voice-model.test.ts`:
- the live-role control compares the derived list against the derived role count
- a new sibling case proving that control reds on a mirror short by one role

Modified assertions in `scripts/check-foundation-guards.test.ts`:
- the two-consumer claim becomes an occurrence count of exactly two
- the wrong-bytes negative regex is constructed from the declared role constant
- the two inline role-directory spellings route through the `rolePath` helper

New test names (`scripts/check-foundation-guards.test.ts`):
- a derived, two-sided, tree-wide scan naming the single owner of the section-extent predicate, with
  a non-vacuity floor and a falsifiability probe
- a derived, two-sided consumer list for the section-locator authority with a pinned cardinality
- a derived tripwire over the guard test files finding adjacent byte-identical assertion lines within
  one test body, with a non-vacuity floor and a falsifiability probe

No new source files, no new exported symbols, no new npm dependencies, and no changes to any file in
the LANG-03 watched corpus, so no disposition row is owed under `docs/audit/29-style-dispositions/`
for this plan. Do not invent one. No `.js` build output changes, because this plan modifies test
modules only.
</artifacts_this_phase_produces>

<verification>
1. Every strengthened assertion is proven able to fail, by a sibling case or a demonstrated red.
2. The owner scan reports exactly one module; the consumer scan's list is recorded as derived.
3. The duplicate-assertion tripwire reports zero pairs after any real pair it found is fixed.
4. `npx vitest run --exclude '**/scripts/e2e/**'` exits 0 across the whole suite.
5. All four gates exit 0 with the numbers published by plans 29-20 through 29-24.
6. `npm run freshness` and `npx tsc --noEmit` exit 0.
</verification>

<success_criteria>
- No assertion in the guard harness claims more than its body can support.
- LANG-07's central claim is a derivation with a non-vacuity floor and a falsifiability probe.
- The consumer list and the owner list are both pinned two-sided.
- The class that produced three of this round's findings has a tripwire.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-25-SUMMARY.md` when done.
Include the owner scan's derived member list, the consumer scan's derived member list with its
cardinality, the tripwire's scanned-file and classified-assertion counts, any duplicate pair found
and fixed, and the demonstration that each strengthened assertion can fail.
</output>

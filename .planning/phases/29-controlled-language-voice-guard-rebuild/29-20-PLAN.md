---
phase: 29-controlled-language-voice-guard-rebuild
plan: 20
type: execute
wave: 19
depends_on: ["29-19"]
gap_closure: true
files_modified:
  - scripts/frontmatter.ts
  - scripts/frontmatter.js
  - scripts/frontmatter.test.ts
  - scripts/voice-model.ts
  - scripts/voice-model.js
  - scripts/voice-model.test.ts
  - scripts/check-foundation-guards.test.ts
autonomous: true
requirements: [LANG-06, LANG-07]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CR-02 is closed: a de-fenced `## Caveman prompt` section followed by a LEVEL-ONE heading carrying an unrelated fenced block is refused `missing`. Reproduced live at HEAD before the fix — `readCavemanFence` today returns `{ok:true, inside:\"grug club rock cave smash\"}` on exactly those bytes (D-22, D-23)."
    - "The fix is STRUCTURAL, not a widened regex: `scripts/frontmatter.ts` — which already owns the fence toggle — gains ONE exported section-locator pair, `unfencedHeadingIndex` and `sectionEndIndex`, and `voice-model.ts`'s private `SECTION_END` predicate and private raw-line anchor scan are DELETED rather than corrected in place (LANG-07, D-24)."
    - "The unified authority's SCOPE is pinned explicitly, because unifying two parsers into one makes that authority's scope a new degree of freedom: `sectionEndIndex` searches from a caller-supplied `from` index to the first UNFENCED heading of level at most `level`, and to `lines.length` otherwise — never to EOF-by-default and never past the caller's own section."
    - "The authority answers the trailing-whitespace axis ONCE, in the `trimEnd()` direction that `check-diff-disposition.ts` and `check-imperative-lexicon.ts` already use, so the four-way disagreement the round-2 review tabulated (WR-08) collapses to one answer rather than to a fifth."
    - "WR-01 is closed: `readCavemanFence`'s ANCHOR scan is fence-aware. A role file that quotes `## Caveman prompt` inside a fenced example is no longer refused `multiple` on correct bytes — reproduced live at HEAD, which returns `{ok:false, reason:\"multiple\"}` today (LANG-07)."
    - "EDGE (LANG-06): a `### ` sub-heading does NOT close the caveman section — a subsection structures a section and does not leave it — while both `# ` and `## ` do. Both directions are pinned by a case, because a bound tested from one side only is the half-fix this round exists to stop repeating."
    - "EDGE (LANG-06): the fail-CLOSED cost recorded in `voice-model.ts` survives the rewire unchanged — a `## ` line inside the fence interior still pulls the bound in front of the closing delimiter and the reader still refuses `unterminated` rather than returning a silently shortened interior."
    - "The live corpus is byte-unmoved: all 17 role files return `ok: true` with the same `inside` and the same `outside` as before the change, and `node scripts/check-foundation-guards.js` still prints `caveman voice: 0 findings over 17/17 elements` and exits 0. This fix is behaviour-preserving on the corpus, so its proof is a PLANTED input, never a moved number."
    - "The new authority is NOT a fourth fence state machine: `scripts/frontmatter.test.ts`'s derived `liveFenceMachines()` scan still reports exactly its pinned three members, re-derived rather than assumed, because composing the toggle is not the same as forking it (D-24)."
    - "`scripts/frontmatter.js` and `scripts/voice-model.js` are rebuilt from their `.ts` sources and committed in the same commit, so `npm run freshness` exits 0 and the committed output cannot drift from its source."
  prohibitions:
    - "Never add a FIFTH section-end implementation. Round 1's gap-closure round — whose stated goal was one authority per predicate — shipped a fourth disagreeing one. The private predicates are deleted, not left beside the shared one as a fallback."
    - "Never let the reader reach past its section bound to find a delimiter it can vouch for; reaching past is the defect, not the remedy."
    - "Never mark a bypass closed on a green suite alone — all four gates print ALL CHECKS PASSED at HEAD today while both bypasses in this plan reproduce."
    - "Never give the authority an opt-out parameter that restores the fence-blind or level-two-only behaviour; an opt-out is a second grammar with extra steps."
    - "Never run bare `npm test` — it triggers the live claude-CLI e2e lane, spends the user's tokens and can hang for around eight minutes."
  artifacts:
    - scripts/frontmatter.ts
    - scripts/frontmatter.js
    - scripts/voice-model.ts
    - scripts/voice-model.js
    - scripts/frontmatter.test.ts
    - scripts/voice-model.test.ts
    - scripts/check-foundation-guards.test.ts
  key_links:
    - "`scripts/frontmatter.ts` exports `unfencedHeadingIndex` and `sectionEndIndex`; `scripts/voice-model.ts` imports both plus `FENCE_DELIMITER_LINE` and declares no section predicate of its own."
    - "`scripts/check-foundation-guards.ts` (`guardVoice`, `guardCavemanVoice`) calls `readCavemanFence` unchanged — the verdict shape is untouched, so both consumers keep seeing one verdict."
    - "`scripts/check-foundation-guards.test.ts:728-753` pins the frontmatter consumer list and each consumer's imported-symbol set two-sided; `voice-model.ts`'s entry moves from one symbol to three and the movement is acknowledged in place, never cleared."
---

<objective>
Close CR-02 and WR-01 by creating the ONE section-locator authority this phase's LANG-07 requires,
and prove it end-to-end through the single consumer that carries a live bypass today.

Purpose: LANG-06 fails because `readCavemanFence`'s section bound closes only on `## `. LANG-07
fails because four modules answer "where does this section end" four disagreeing ways, and that
divergence is the direct architectural cause of the LANG-06 bypass. Fixing the regex alone would be
the fifth heuristic in a phase whose founding rule is one authority per predicate — so the bound
moves to `frontmatter.ts`, which already owns the fence toggle, and the private predicate is deleted.

Output: `unfencedHeadingIndex` / `sectionEndIndex` exported from `scripts/frontmatter.ts`, consumed
by `scripts/voice-model.ts`, with the bypass planted, proven RED at HEAD, and proven GREEN after.
Plans 29-22, 29-23 and 29-24 move the other three consumers onto the same authority; plan 29-25 pins
the consumer list two-sided; plan 29-26 red-teams it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-VERIFICATION.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md
@.planning/phases/29-controlled-language-voice-guard-rebuild/29-14-SUMMARY.md
@scripts/voice-model.ts
@scripts/frontmatter.ts
</context>

<round_2_findings_disposition>
This register covers EVERY finding in round-2 `29-REVIEW.md` and every round-1 carry-over it
re-lists. It lives in the first plan of the round so the executor of any later plan can see that
nothing was dropped silently. Silent drops are the failure mode this phase exists to eliminate.

| Finding | Severity | Disposition | Where |
|---|---|---|---|
| CR-01 — watched corpus unpinned | critical | PLANNED | 29-21 |
| CR-02 — `SECTION_END` level-one-blind | critical | PLANNED | 29-20 (this plan) |
| WR-01 — anchor scan fence-blind | warning | PLANNED | 29-20 (this plan) |
| WR-02 — banned-claim exemption widened while header denies it | warning | PLANNED | 29-23 |
| WR-03 — `readDispositionRows` fourth locator, fail-open | warning | PLANNED | 29-22 |
| WR-04 — set-equality refusal contradicts Residual 1 | warning | PLANNED | 29-24 |
| WR-05 — live-corpus control asserts `> 0` | warning | PLANNED | 29-25 |
| WR-06 — duplicated assertion stands for two consumers | warning | PLANNED | 29-25 |
| WR-07 — case asserts a source substring, not a property | warning | PLANNED | 29-24 |
| WR-08 — four section-end grammars | warning | PLANNED | 29-20, 29-22, 29-23, 29-24, 29-25, 29-26 |
| WR-09 — indented code blocks admitted as step bullets | warning | PLANNED | 29-24 |
| IN-01 — a 7-cell-violating disposition row vanishes silently | info | PLANNED | 29-22 |
| IN-02 — `rows` computed and discarded on two branches | info | PLANNED | 29-22 |
| IN-03 — `rolePath` helper bypassed at two plant sites | info | PLANNED | 29-25 |

**Round-1 carry-overs the round-2 review re-lists as known-open and out of scope** (its own words:
"re-listed, not re-argued"): round-1 WR-01 (line numbers from the filtered remainder), WR-03 (three
near-identical directory walks), WR-04 (`GENERATED_EXEMPT` pinned by cardinality only), WR-07
(`indexOf` source scrape plus `root === ROOT` identity cache), WR-08 (two remaining path-literal
spellings), IN-02 (`rows` computed and discarded — superseded, now planned as round-2 IN-02),
IN-04 (`countWords` replacement loop).

**Disposition: DEFERRED, with reason.** None of these seven is charged against a failed LANG-NN truth
in `29-VERIFICATION.md`; none is a fail-open direction; and each is a code-quality or duplication
item whose fix touches a surface this round is actively rewiring. Landing them alongside a
section-locator unification would mix a refactor with a safety fix in the same diff, which is how
round 1 produced a fix that was itself incomplete. They are recorded here so the next reviewer meets
them as a decision rather than rediscovering them as an omission, and they should be picked up as a
dedicated cleanup plan once this round's re-verification is green. `GENERATED_EXEMPT` pinned by
cardinality only (round-1 WR-04) is the one worth promoting first, because it is a set-literal-drift
shape and this repository has paid for that class twice.
</round_2_findings_disposition>

<assumption_delta_decision>
**Signal:** the deterministic assumption-delta scan returned `detected: true` with a `pluralization`
signal.

**Primary noun:** the section-locator authority — "which line is this section's heading, and which
line ends it".

**Observed multiplicity:** FOUR private implementations (`voice-model.ts:74`/`:102`,
`check-diff-disposition.ts:532`/`:535`, `check-banned-claims.ts:448`/`:498`,
`check-imperative-lexicon.ts:478`/`:488`/`:627`), disagreeing on two axes — heading-equality
strictness and fence-awareness.

**Decision: PROMOTE.** One shared authority in `scripts/frontmatter.ts` becomes primary and the four
private predicates are DELETED. This is deliberately not `add-alongside`: a shared predicate that
sits beside the private ones as an option is the shape that let four grammars accumulate in the
first place, and round 1 already demonstrated that a round can add a fourth implementation while
believing it was unifying.

**Accepted debt, named:** none is expected. If a consumer genuinely cannot use the shared authority
(plans 29-22 through 29-24 are where that would surface), the executor MUST name the consumer, name
the property it needs that the authority does not provide, and state what would force a later
promote — rather than quietly keeping its private predicate. A consumer left private is a LANG-07
failure and must be reported to the user, not absorbed.
</assumption_delta_decision>

<specless_probe_choice>
This phase carries no `*-SPEC.md`, so `## Edge Coverage` and `## Prohibitions` were not supplied to
the planner and no fresh requirement-text edge probe was run.

**Recorded, visible choice — not a silent skip.** In gap-closure mode the `must_haves` are derived
from `29-VERIFICATION.md`'s four failed truths, each of which already carries an explicit,
enumerated `missing:` predicate list, plus the fourteen named findings in `29-REVIEW.md`. Running a
fresh probe over the same requirement IDs would author a SECOND, competing set of predicates over
the same bytes — which is precisely the "two authorities over the same bytes" defect this phase
exists to close, committed in the planning layer instead of the code layer. The verification
report's predicates are the authority for this round.
</specless_probe_choice>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — a de-fenced caveman section under a `# ` heading is refused, through a NEW shared authority</name>
  <files>scripts/frontmatter.ts, scripts/voice-model.ts, scripts/voice-model.test.ts, scripts/check-foundation-guards.test.ts, scripts/frontmatter.js, scripts/voice-model.js</files>
  <read_first>
    - `scripts/voice-model.ts:58-184` — the import, `CAVEMAN_HEADING_LINE` (line 74), the `SECTION_END` argument block (lines 76-102) and `readCavemanFence` (lines 133-184). The argument block is load-bearing prose; it is REWRITTEN to describe the shared authority, not deleted.
    - `scripts/frontmatter.ts:370-444` — `FENCE_DELIMITER_LINE` (line 390), `fencedLineFlags` (line 424) and `stripFencedBlocks` (line 440). The new functions are declared beside these and consume `fencedLineFlags`; they declare no toggle of their own.
    - `scripts/check-imperative-lexicon.ts:475-488` — `HEADING_LINE`, `STEPS_HEADING`, `SECTION_HEADING_LINE`. This is the module that already answers the level axis correctly (`/^#{1,2} /`) and its recorded argument is the one the shared authority adopts.
    - `scripts/check-foundation-guards.test.ts:4184-4275` — plan 29-14's `## ` arm of this exact case, including the `mirror()` / `runIn()` / `rolePath()` helpers and the unmodified-mirror control. The new case is its `# ` sibling and reuses these helpers.
    - `scripts/voice-model.test.ts:190-249` — the existing reader cases and the `doc()` / `FENCE` helpers.
  </read_first>
  <action>
Wire ONE path — "a caveman section that has been de-fenced, followed by a level-one heading carrying
an unrelated fenced block, is refused" — from a new shared authority in `scripts/frontmatter.ts`
through `scripts/voice-model.ts` to the committed gate, in that order, with the failing evidence
captured first.

Start RED. Before touching any source, add two cases and watch them fail:

In `scripts/voice-model.test.ts`, a case named for the level-one bound that calls `readCavemanFence`
on a document whose `## Caveman prompt` section carries plain prose and no fence, followed by a
`# Appendix` heading and an unrelated fenced block containing the five words `grug club rock cave
smash`, and asserts the verdict equals `{ ok: false, reason: "missing" }`. At HEAD this returns
`{ ok: true, inside: "grug club rock cave smash", ... }` — capture that transcript into the plan
SUMMARY verbatim.

In `scripts/check-foundation-guards.test.ts`, the `# ` sibling of the existing 29-14 case: mirror the
tree, de-fence `MALFORMED_ROLE`'s caveman block into senior prose, append a `# Notes` top-level
section carrying a fenced block with at least `CAVEMAN_LEXICON_MIN` lexicon terms, assert the gate
exits 1 and that no `tokens N / content words N` measurement line exists for that role. Assert the
plant's PREMISE through the same authority the guard reads before running the gate — the harness's
own premise is asserted, not assumed, because that premise produced a false result in six instances
across four straight rounds. Reach the plant file through the existing `rolePath(m, name)` helper.

Then land the authority. In `scripts/frontmatter.ts`, beside `fencedLineFlags`, export exactly two
functions and no more:

`unfencedHeadingIndex(text: string, heading: string): number` — the 0-based index of the first line
that is not flagged by `fencedLineFlags` and whose `trimEnd()` equals `heading`, or `-1`.

`sectionEndIndex(text: string, from: number, level: 1 | 2): number` — the 0-based index of the first
line at index `from` or later that is not flagged by `fencedLineFlags` and is an ATX heading of level
at most `level`, or `text.split("\n").length` when there is none.

Both compute `fencedLineFlags(text)` once per call and neither re-declares `FENCE_DELIMITER_LINE`,
because the toggle is the only interface a consumer gets. Record in the declaration comment, as
`voice-model.ts` and `locateSection` already record their arguments, the three axes the four
divergent predicates disagreed on and the single answer this authority gives each: heading equality
is `trimEnd()`-normalized (the spelling `check-diff-disposition.ts` and `check-imperative-lexicon.ts`
already use, so a heading carrying one trailing space is the section's heading rather than invisible);
the close is any UNFENCED heading of level at most `level`, so `### ` structures a section and stays
inside it while `# ` and `## ` both start something else; and the search is BOUNDED by the caller's
`from` and terminates at `lines.length`, so the authority never adopts bytes from a section the caller
did not ask about. State plainly that there is no parameter restoring fence-blind or level-two-only
behaviour.

Then rewire `scripts/voice-model.ts`: import `sectionEndIndex` and `unfencedHeadingIndex` alongside
`FENCE_DELIMITER_LINE`, compute the caveman section bound as `sectionEndIndex(text, heading + 1, 2)`,
and DELETE the module-level `SECTION_END` declaration and its bounding loop. Rewrite the surrounding
argument block so it describes the shared authority and states that the bound is no longer this
module's to decide; keep the recorded fail-closed cost — a `## ` line inside the fence interior
pulling the bound in front of the closing delimiter and producing `unterminated` — because that
behaviour is unchanged and the note is what stops a later reader "fixing" it. The open scan and the
close scan keep using `FENCE_DELIMITER_LINE` directly: "is this line a fence delimiter" is a
different question from "where does this section end" and merging them would be a new defect.

Leave the anchor scan (the `CAVEMAN_HEADING_LINE` loop at lines 136-139) alone in this task; Task 3
moves it and states its own reason.

Rebuild the committed output for both modules with `npx tsc --outDir .tmp-build` semantics — use the
repository's own build, `npm run build` — and commit the regenerated `scripts/frontmatter.js` and
`scripts/voice-model.js` in the same commit as their sources.
  </action>
  <verify>
    <automated>npx vitest run scripts/voice-model.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - Before the source edit, `npx vitest run scripts/voice-model.test.ts -t "level-one"` FAILS, and the failure message shows the received value as `ok: true` with `inside` equal to `grug club rock cave smash`. The transcript is pasted into the SUMMARY.
    - Before the source edit, the new `check-foundation-guards.test.ts` gate case FAILS with a received exit status of 0. The transcript is pasted into the SUMMARY.
    - After the source edit, both cases pass and `npx vitest run scripts/voice-model.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'` exits 0.
    - `node -e 'import("./scripts/voice-model.js").then(m=>console.log(JSON.stringify(m.readCavemanFence(["## Caveman prompt","You senior prose here with no fence at all.","","# Appendix","Some later top-level section.","```","grug club rock cave smash","```",""].join("\n")))))'` prints a verdict whose `ok` field is `false` and whose `reason` field is `missing`.
    - `node scripts/check-foundation-guards.js` exits 0 and its output contains `caveman voice: 0 findings over 17/17 elements` — the live corpus is unmoved.
    - `npm run freshness` exits 0.
  </acceptance_criteria>
  <done>The level-one bypass is refused end-to-end, from a shared authority through the reader to the committed gate, and the live corpus verdict is unchanged.</done>
  <reversibility rating="costly">Exporting a section-locator pair from `frontmatter.ts` sets the shape three later plans consume; changing the signature after they land means touching four modules. Getting the `level` and `from` semantics right here is cheaper than renegotiating them in plan 29-24.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Pin the unified authority's SCOPE — the degree of freedom unification creates</name>
  <files>scripts/frontmatter.test.ts, scripts/frontmatter.ts</files>
  <read_first>
    - `scripts/frontmatter.test.ts:5410-5546` — the WR-02 invariant case and the derived `liveFenceMachines()` scan, `FENCE_RECOGNISER_CONSTRUCTS`, `FENCE_TOGGLE_CONSTRUCTS`, `FENCE_MACHINES` and `FENCE_MACHINE_COUNT = 3`. The new functions must not become a fourth member, and that must be re-derived rather than asserted from belief.
    - `scripts/frontmatter.ts` — the two functions written in Task 1.
    - `.planning/phases/29-controlled-language-voice-guard-rebuild/29-REVIEW.md` — the WR-08 table listing the two axes the four implementations disagreed on.
  </read_first>
  <action>
Add a `describe` block to `scripts/frontmatter.test.ts` that pins the shared authority's scope. When
two parsers are unified into one, that authority's SCOPE becomes a new degree of freedom: a
section-anchored reader that searches to EOF adopts an unrelated later block. This block is what
stops that being rediscovered.

Cover, each as its own case with a name that states the property rather than the input:

The `from` bound — `sectionEndIndex(text, from, level)` never returns an index below `from`, and a
heading sitting at `from - 1` (the caller's own heading) is not the answer.

The EOF fallback — a document whose last section has no successor returns exactly
`text.split("\n").length`, and that value is the array length rather than the last index, so a caller
slicing `[from, end)` gets the whole tail and not the tail minus one line.

The level axis, both directions — with `level` of 2, a `# ` line closes and a `## ` line closes and
a `### ` line does NOT; with `level` of 1, only `# ` closes. Assert all five outcomes, because a
bound tested from one side only is exactly the half-fix that made CR-02 survive round 1.

Fence-awareness at BOTH functions — a `## ` line and a `# ` line inside a fenced block close nothing
and are not found as a heading, and the same lines outside a fence do both. This axis carries the
live corpus's real hazard, so pin it with the real shape: `README.md` line 17 and `agent-factory/
README.md` line 105 are level-one lines inside fenced shell examples, measured this session through
`fencedLineFlags`, and every `## ` section in those two watched files would truncate at them if the
authority were level-aware but fence-blind. Add a case built from that shape.

Trailing whitespace — a heading line carrying one trailing space IS found by `unfencedHeadingIndex`
for the un-padded heading string, and a heading carrying a leading space is NOT. State in the case
name that this is the axis on which the four deleted predicates disagreed.

An unterminated fence — the tail stays flagged, matching `fencedLineFlags`'s recorded fail-safe, so
a heading below an unterminated fence is not found and closes nothing.

Then re-run the derived fence-machine scan in the same file and assert it still equals its pinned
three members with its pinned count, re-derived from the live tree in this session rather than taken
on trust. If it moved, the movement is the acknowledgement and the new function is a fourth machine
that must be rewritten to compose the toggle instead — do not raise the pin to clear a failure.
  </action>
  <verify>
    <automated>npx vitest run scripts/frontmatter.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/frontmatter.test.ts --exclude '**/scripts/e2e/**'` exits 0.
    - The new describe block contains at least one case per axis: `from` bound, EOF fallback, level (five outcomes), fence-awareness at both functions, trailing whitespace (two directions), unterminated fence. Count the cases and state the number in the SUMMARY.
    - The derived `liveFenceMachines()` assertion still passes with its existing pinned member list and count, re-run in this session; the SUMMARY records the re-derived list verbatim.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Every degree of freedom the unification introduced is asserted from both sides, and the authority is proven not to be a fourth fence state machine.</done>
</task>

<task type="auto">
  <name>Task 3: WR-01 — the anchor scan consumes the authority, and the consumer pin moves two-sided</name>
  <files>scripts/voice-model.ts, scripts/voice-model.js, scripts/voice-model.test.ts, scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - `scripts/voice-model.ts:64-74` and `:133-143` — `CAVEMAN_HEADING_LINE` and the raw-line anchor loop that produces the `multiple` and `missing` verdicts.
    - `scripts/check-foundation-guards.test.ts:663-754` — the frontmatter consumer list and the four per-module `importedSymbols(...)` equalities, including line 741's `toEqual(["FENCE_DELIMITER_LINE"])` for `voice-model.ts` and the recorded history of every time the pin moved.
    - `scripts/voice-model.test.ts:215-233` — the live-role control this task must keep green.
  </read_first>
  <action>
Close WR-01 and reconcile the two-sided import pin the rewire breaks.

RED first: add a case to `scripts/voice-model.test.ts` calling `readCavemanFence` on a document with
one real `## Caveman prompt` section carrying a well-formed fence, plus a later `## Notes` section
whose fenced example QUOTES the line `## Caveman prompt`, and assert `ok` is `true` with `inside`
equal to the real block's interior. At HEAD this returns `{ ok: false, reason: "multiple" }` —
capture that transcript.

Then make the anchor scan fence-aware. `readCavemanFence` counts headings by testing raw lines
against `CAVEMAN_HEADING_LINE`, so a quoted anchor inside a fenced example counts as a second
heading and refuses a correct document by name. Replace the counting loop so a line flagged by the
one fence authority donates no heading. Keep the `multiple` verdict itself — two real unfenced
anchors is still a refusal, and that is load-bearing for both consumers.

The anchor scan needs a COUNT, not a first index, so it cannot simply call `unfencedHeadingIndex`.
Consume `fencedLineFlags` directly for the count and state that reason in the comment beside it: this
module now takes the per-line toggle for the count and the shared locator for the bound, and both are
projections of the same authority, so no second grammar is introduced. Note explicitly that
`unfencedHeadingIndex` is deliberately NOT wrapped in a loop here — a "find the next one after i"
loop over a first-index helper is a second traversal with different termination behaviour, which is
the shape this round is deleting.

Then move the two-sided pin in `scripts/check-foundation-guards.test.ts`. `voice-model.ts`'s imported
symbol set is no longer one symbol: it becomes `FENCE_DELIMITER_LINE`, `fencedLineFlags`,
`sectionEndIndex` and `unfencedHeadingIndex`, sorted as `importedSymbols` returns them. Update that
one equality and extend the comment above it in the file's established style — record that the set
grew because the module took MORE declarations from the authority and still renders no verdict from
it, which is the direction D-24 wants, and state that the pin moving is the pin working. Do not touch
the other three modules' equalities; plans 29-22 through 29-24 move each with its own reason.

Confirm the grant-predicate case immediately below still passes — the new symbols are declarative,
not verdict-bearing, so the tree-wide zero must hold.

Rebuild and commit `scripts/voice-model.js` in the same commit.
  </action>
  <verify>
    <automated>npx vitest run scripts/voice-model.test.ts scripts/frontmatter.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - Before the source edit, the new quoted-anchor case FAILS with a received value whose `reason` field is `multiple`. The transcript is pasted into the SUMMARY.
    - After the fix, `node -e 'import("./scripts/voice-model.js").then(m=>console.log(JSON.stringify(m.readCavemanFence(["# Role","## Caveman prompt","```","You grug smash rock and club.","```","","## Notes","Example of the required section:","```","## Caveman prompt","```",""].join("\n")))))'` prints a verdict whose `ok` field is `true`.
    - `npx vitest run scripts/voice-model.test.ts scripts/frontmatter.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'` exits 0.
    - The `voice-model.ts` entry in the consumer-symbol pin lists four symbols, and the three sibling equalities are byte-unchanged.
    - `node scripts/check-foundation-guards.js` exits 0 and prints `caveman voice: 0 findings over 17/17 elements`; `node scripts/check-imperative-lexicon.js`, `node scripts/check-diff-disposition.js` and `node scripts/check-banned-claims.js` each exit 0.
    - `npm run freshness` exits 0 and `git status --porcelain` shows only this plan's files plus the three pre-existing out-of-scope entries (`human-notes.txt`, `.gsd/`, `.planning/phases/29.1-per-role-model-assignment/`).
  </acceptance_criteria>
  <done>A quoted anchor inside a fenced example no longer refuses a correct role file, and the frontmatter consumer pin records the new symbol set two-sided.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository bytes → guard verdict | Untrusted authored markdown crosses into a predicate whose output is a merge-gating exit code. This is the phase's real attack surface: not injection, but a guard that measures the wrong bytes and reports green. |
| `frontmatter.ts` authority → four consumer guards | A single shared predicate now decides section extent for every guard. A defect here is a defect in all four at once, which is the cost of unification and the reason its scope is pinned in Task 2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-20-01 | Tampering | `readCavemanFence` section bound | high | mitigate | The bound moves to a shared, level-aware, fence-aware authority; the level-one bypass is planted at the reader AND at the gate and proven RED at HEAD before the fix. |
| T-29-20-02 | Information disclosure (integrity of the verdict) | `guard_voice` / `guard_caveman_voice` | high | mitigate | The gate case asserts that no `tokens N / content words N` line exists for the planted role, so a measurement over the wrong bytes cannot coexist with a green run. |
| T-29-20-03 | Denial of service (false red) | fence-blind anchor scan (WR-01) | medium | mitigate | Task 3 makes the anchor scan fence-aware; the live-role control asserts all 17 roles still read `ok`. |
| T-29-20-04 | Elevation of privilege (scope creep in the shared authority) | `sectionEndIndex` | high | mitigate | Task 2 pins `from`, the EOF fallback, the level axis in both directions and fence-awareness at both functions; the authority has no opt-out parameter. |
| T-29-20-05 | Denial of service (superlinear backtracking) | new predicates in a scanner over 47 files | medium | mitigate | Both functions are line-oriented with anchored, bounded tests and no match-all regex or nested quantifier, per the dormant 0.47s-to-383s incident's own rule. |
| T-29-20-SC | Tampering | npm / pip / cargo installs | high | mitigate | No package-manager install occurs in this plan. The repository ships zero runtime dependencies and its dev deps are unchanged, so the package-legitimacy gate has an empty input set. If a task is later found to need an install, it stops and escalates rather than installing. |
</threat_model>

<artifacts_this_phase_produces>
New exported symbols in `scripts/frontmatter.ts`:
- `unfencedHeadingIndex(text: string, heading: string): number`
- `sectionEndIndex(text: string, from: number, level: 1 | 2): number`

Symbols DELETED:
- `SECTION_END` (module-level constant in `scripts/frontmatter.ts`'s consumer `scripts/voice-model.ts`)

New test names (`scripts/voice-model.test.ts`):
- a case asserting the level-one heading closes the caveman section
- a case asserting a fenced quotation of the anchor does not produce the `multiple` verdict

New test names (`scripts/frontmatter.test.ts`):
- a describe block pinning the section locator's scope, with one case per axis (`from` bound, EOF fallback, level in both directions, fence-awareness at both functions, trailing whitespace, unterminated fence)

New test names (`scripts/check-foundation-guards.test.ts`):
- the level-one sibling of plan 29-14's de-fenced-role gate case

Regenerated committed build output: `scripts/frontmatter.js`, `scripts/voice-model.js`.

No new files. No new npm dependencies. No changes to any file in the LANG-03 watched corpus
(`.claude-plugin/plugin.json`, `agent-factory/README.md`, `agent-factory/roles/*`,
`agent-factory/workflows/*`, `AGENTS.md`, `README.md`), so no disposition row is owed under
`docs/audit/29-style-dispositions/` for this plan. Do not invent one.
</artifacts_this_phase_produces>

<verification>
1. Both bypasses reproduce at HEAD before the fix, with transcripts captured verbatim.
2. `npx vitest run scripts/frontmatter.test.ts scripts/voice-model.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**'` exits 0.
3. All four gates exit 0 on the live tree: `check-foundation-guards`, `check-imperative-lexicon`, `check-diff-disposition`, `check-banned-claims`.
4. `check-diff-disposition` still reports `37 watched file(s) changed since 4d2b8f0; 1880 changed clause(s) derived` — the numbers measured before this plan. Any movement is escalated and explained, never absorbed.
5. `npm run freshness` exits 0 and `npx tsc --noEmit` exits 0.
6. `git status --porcelain` shows only this plan's files plus the three pre-existing out-of-scope entries.
</verification>

<success_criteria>
- `readCavemanFence` refuses the level-one bypass and accepts the quoted-anchor document, both proven by a case that failed at HEAD.
- `voice-model.ts` declares no section-end predicate of its own.
- `frontmatter.ts` exports exactly two new section-locator functions, with their scope pinned on six axes.
- The derived fence-machine set is still three members.
- The live corpus verdict is byte-unmoved on all four gates.
</success_criteria>

<output>
Create `.planning/phases/29-controlled-language-voice-guard-rebuild/29-20-SUMMARY.md` when done.
Include both RED transcripts verbatim, the re-derived fence-machine list, and the four gates' output
numbers before and after.
</output>

---
phase: 05-packaging-adapters-install-distribution
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .claude/skills/grugops/SKILL.md
  - .claude/skills/grugops-map/SKILL.md
  - .claude/skills/grugops-plan/SKILL.md
  - .claude/skills/grugops-ticket/SKILL.md
  - .claude/skills/grugops-gate/SKILL.md
  - .claude/skills/grugops-uat/SKILL.md
  - .claude/skills/grugops-release/SKILL.md
  - .claude/agents/grugops-orchestrator.md
  - CLAUDE.md
  - .gemini/settings.json
autonomous: true
requirements: [CLAUDE-01]
must_haves:
  truths:
    - "Exactly 7 standalone dash skills exist (grugops, grugops-map, grugops-plan, grugops-ticket, grugops-gate, grugops-uat, grugops-release), each invokable as /grugops-<op>"
    - "Each skill body is a thin pointer to the frozen orchestrator/role/workflow — no role text is copied"
    - "grugops-release carries disable-model-invocation: true so the agent can never auto-fire a release"
    - "A one-line CLAUDE.md pointer routes Claude Code to the Orchestrator/AGENTS.md, and Gemini is wired to read AGENTS.md"
  artifacts:
    - path: ".claude/skills/grugops/SKILL.md"
      provides: "Standalone /grugops dispatcher skill (dash form)"
      contains: "orchestrator.md"
    - path: ".claude/skills/grugops-release/SKILL.md"
      provides: "Destructive release skill with disable-model-invocation: true"
      contains: "disable-model-invocation: true"
    - path: "CLAUDE.md"
      provides: "One-line repo-root pointer to AGENTS.md / orchestrator.md (CLAUDE-01)"
      contains: "orchestrator.md"
    - path: ".gemini/settings.json"
      provides: "Gemini wiring to read AGENTS.md via context.fileName"
      contains: "context.fileName"
  key_links:
    - from: ".claude/skills/grugops-plan/SKILL.md"
      to: "agent-factory/roles/orchestrator.md"
      via: "repo-relative pointer-text body (resolved against the user's repo)"
      pattern: "agent-factory/roles/orchestrator\\.md"
    - from: ".gemini/settings.json"
      to: "AGENTS.md"
      via: "context.fileName array"
      pattern: "AGENTS\\.md"
---

<objective>
Ship the standalone `.claude/` distribution form (CLAUDE-01, the on-brand primary surface per D-29): seven thin pointer **skills** invoked with the dash (`/grugops`, `/grugops-map`, `/grugops-plan`, `/grugops-ticket`, `/grugops-gate`, `/grugops-uat`, `/grugops-release`), an optional per-role subagent wrapper, a one-line `CLAUDE.md` repo-root pointer, and the Gemini `.gemini/settings.json` entry-file wiring.

Purpose: This is the dash command surface (the deliberate legal-surface-reduced `grugops` brand, never bare `grug`). It is single-source: every skill body points at the frozen `agent-factory/` core, never copies it.
Output: 7 `.claude/skills/grugops*/SKILL.md`, `.claude/agents/grugops-orchestrator.md`, `CLAUDE.md`, `.gemini/settings.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packaging-adapters-install-distribution/05-CONTEXT.md
@.planning/phases/05-packaging-adapters-install-distribution/05-RESEARCH.md
@.planning/phases/05-packaging-adapters-install-distribution/05-PATTERNS.md
@agent-factory/packaging/slash-command.template.md
@agent-factory/packaging/subagent.frontmatter.md

<interfaces>
<!-- The two PKG-02 templates from plan 05-01 are the shapes to instantiate. Executor: use them directly. -->

agent-factory/packaging/slash-command.template.md (from 05-01): skill SKILL.md frontmatter shape (name/description/argument-hint/allowed-tools incl. Agent) + repo-relative pointer-text body.
agent-factory/packaging/subagent.frontmatter.md (from 05-01): subagent wrapper (name/description/tools incl. Agent/model: inherit) + pointer body.

05-RESEARCH.md Pattern 3 (standalone dir = whole command, DASH; grugops-release adds disable-model-invocation: true) + Pitfall 5 (standalone dirs carry the grugops- prefix).
05-RESEARCH.md Pattern 6 (Gemini wiring): .gemini/settings.json → { "context": { "fileName": ["AGENTS.md", "GEMINI.md"] } }.

The 7 ops → frozen workflow each points at (workflows live in agent-factory/workflows/):
  grugops         → orchestrator.md (the generic dispatcher; routes any request)
  grugops-map     → 00-bootstrap-greenfield.md / 01-bootstrap-brownfield.md (repo map/bootstrap)
  grugops-plan    → 02-idea-to-epics.md / 03-epic-to-tickets.md (plan work)
  grugops-ticket  → 04-ticket-to-pr.md (drive a ticket to PR)
  grugops-gate    → 05-pr-quality-gate.md (run the quality gate)
  grugops-uat     → 06-uat-pack.md (UAT pack)
  grugops-release → 12-release.md via release-manager.md (destructive; disable-model-invocation)

agent-factory/roles/orchestrator.md line 119 (hard limit to echo in clear voice, NEVER copy the body):
  "Never merge to a protected branch. Never deploy to prod. Never exceed WIP without a written reason."
agent-factory/roles/release-manager.md line 49: "Deploy only after a named human approves" (the op grugops-release's disable protects).

CLAUDE.md (repo root) must be a USER-repo pointer (it IS read by Claude Code) — NOT a plugin-root CLAUDE.md (which is not loaded). One line pointing at AGENTS.md / orchestrator.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 7 standalone dash skills (.claude/skills/grugops*/SKILL.md)</name>
  <files>.claude/skills/grugops/SKILL.md, .claude/skills/grugops-map/SKILL.md, .claude/skills/grugops-plan/SKILL.md, .claude/skills/grugops-ticket/SKILL.md, .claude/skills/grugops-gate/SKILL.md, .claude/skills/grugops-uat/SKILL.md, .claude/skills/grugops-release/SKILL.md</files>
  <read_first>
    - agent-factory/packaging/slash-command.template.md (the PKG-02 template from 05-01 — the shape to instantiate)
    - 05-RESEARCH.md Pattern 3 (standalone dash; disable-model-invocation on release) + Pitfall 5 (standalone dirs carry grugops- prefix; plugin dirs do not)
    - 05-PATTERNS.md § ".claude/skills/grugops*/SKILL.md" (the 7-op mapping, dup-check rule)
    - 05-CONTEXT.md D-29 (skills/ form, dash standalone), D-31 (repo-relative pointer-text)
    - agent-factory/roles/orchestrator.md (pointer target; line 119 hard limit), agent-factory/roles/release-manager.md (line 49 — what disable-model-invocation protects)
    - agent-factory/workflows/ (the NN-*.md files each op routes to — confirm exact filenames)
  </read_first>
  <action>Create 7 skill files, each at `.claude/skills/<dir>/SKILL.md` where `<dir>` is the full dash name: `grugops`, `grugops-map`, `grugops-plan`, `grugops-ticket`, `grugops-gate`, `grugops-uat`, `grugops-release` (dir = whole command → invoked `/grugops-<op>`, no colon — Pitfall 5). Each instantiates the `slash-command.template.md` shape: frontmatter `name` (matching the dir), `description` (one clear line naming the op), `argument-hint: "<request>"`, `allowed-tools` (a YAML list including `Agent`); body is repo-relative pointer-text (D-31) — "read `agent-factory/roles/orchestrator.md`, then `agent-factory/config/factory.config.json`, `AGENTS.md`, `plans/board.md`; then run workflow `<the NN-...md for this op>`. Request: $ARGUMENTS". Point each op at its frozen workflow per the interfaces map (grugops→orchestrator dispatcher; grugops-map→bootstrap; grugops-plan→idea-to-epics/epic-to-tickets; grugops-ticket→04-ticket-to-pr; grugops-gate→05-pr-quality-gate; grugops-uat→06-uat-pack; grugops-release→12-release via release-manager). `grugops-release` ADDS `disable-model-invocation: true` to its frontmatter so the agent can never auto-fire a release. NEVER copy any role/workflow body into a skill (single-source dup-check: a distinctive role sentence must return 0 hits across `.claude/skills/`). Echo the never-deploy-prod hard limit in clear voice where relevant. Always lowercase `grugops`.</action>
  <verify>
    <automated>[ "$(ls .claude/skills/grugops*/SKILL.md 2>/dev/null | wc -l | tr -d ' ')" = "7" ] && grep -q "disable-model-invocation: true" .claude/skills/grugops-release/SKILL.md && grep -lq "orchestrator.md" .claude/skills/grugops/SKILL.md && [ "$(grep -rl "You enforce WIP limits." .claude/skills/ 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 7 files match `.claude/skills/grugops*/SKILL.md` (the dirs are `grugops`, `grugops-map`, `grugops-plan`, `grugops-ticket`, `grugops-gate`, `grugops-uat`, `grugops-release`)
    - Each `SKILL.md` has frontmatter `name`/`description`/`argument-hint`/`allowed-tools` and a `$ARGUMENTS` pointer body referencing `agent-factory/roles/orchestrator.md`
    - `.claude/skills/grugops-release/SKILL.md` contains `disable-model-invocation: true`
    - A distinctive orchestrator-role sentence (e.g. "You enforce WIP limits.") returns 0 hits under `.claude/skills/` (pointer-only, single-source)
    - No skill body references a filesystem `../agent-factory/...` path (repo-relative pointer-text only)
  </acceptance_criteria>
  <done>Seven dash skills exist, each a thin repo-relative pointer to the frozen core (no copied role text), grugops-release is non-auto-invocable, and the set follows the slash-command template.</done>
</task>

<task type="auto">
  <name>Task 2: Author the subagent wrapper + CLAUDE.md pointer + Gemini wiring</name>
  <files>.claude/agents/grugops-orchestrator.md, CLAUDE.md, .gemini/settings.json</files>
  <read_first>
    - agent-factory/packaging/subagent.frontmatter.md (the PKG-02 subagent template from 05-01)
    - 05-RESEARCH.md Pattern 4 (subagent wrapper: Agent, model: inherit) + Pattern 6 (Gemini settings.json context.fileName)
    - 05-PATTERNS.md § ".claude/agents/grugops-orchestrator.md", § "CLAUDE.md one-line pointer", § ".gemini/settings.json"
    - 05-CONTEXT.md Claude's Discretion (one-line CLAUDE.md wording; GEMINI.md vs settings.json route)
    - AGENTS.md (line 9 entry rule — the target the CLAUDE.md pointer routes to), agent-factory/roles/orchestrator.md (the subagent pointer target)
  </read_first>
  <action>Create `.claude/agents/grugops-orchestrator.md` by instantiating `subagent.frontmatter.md`: frontmatter `name: grugops-orchestrator`, a clear `description` (single SDLC entry point; routes to factory roles), `tools: Read, Grep, Glob, Bash, Edit, Write, Agent` (use `Agent`, never `Task`), `model: inherit`; body is a thin pointer — "follow `agent-factory/roles/orchestrator.md`; read it now, then config/AGENTS.md/board; then act as the Orchestrator. Never merge a protected branch. Never deploy prod." (clear voice; no copied role body). Create `CLAUDE.md` at the USER REPO ROOT (not the plugin root — a plugin-root CLAUDE.md is not loaded) as a one-line pointer routing Claude Code to `AGENTS.md` / `agent-factory/roles/orchestrator.md` (wording is Claude's discretion). IMPORTANT: a `CLAUDE.md` already exists at this repo root (it is grugops's OWN dev instructions). Do NOT overwrite it — this is the shipped-kit generic pointer concern; instead, ADDITIVELY append a clearly-delimited one-line "start here → orchestrator.md / AGENTS.md" pointer line if and only if no equivalent pointer line is already present (idempotent ensure-line behavior). Create `.gemini/settings.json` with `{ "context": { "fileName": ["AGENTS.md", "GEMINI.md"] } }` (D-35 Gemini wiring) — if the file already exists, merge the key additively rather than overwriting. Always lowercase `grugops`; clear voice.</action>
  <verify>
    <automated>grep -q "model: inherit" .claude/agents/grugops-orchestrator.md && grep -q "Agent" .claude/agents/grugops-orchestrator.md && ! grep -qw "Task" .claude/agents/grugops-orchestrator.md && grep -q "orchestrator.md" CLAUDE.md && node -e "const c=require('fs').readFileSync('.gemini/settings.json','utf8');const j=JSON.parse(c);if(!j.context.fileName.includes('AGENTS.md'))process.exit(1)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/agents/grugops-orchestrator.md` has `model: inherit`, lists `Agent` in tools, does NOT contain the word `Task`, and its body points at `agent-factory/roles/orchestrator.md` (no copied role body)
    - `CLAUDE.md` (repo root) contains a one-line pointer referencing `orchestrator.md`; the pre-existing CLAUDE.md content is preserved (the pointer is appended additively, idempotently — re-running adds no duplicate)
    - `.gemini/settings.json` is valid JSON with `context.fileName` including `"AGENTS.md"`
    - No artifact copies a role body
  </acceptance_criteria>
  <done>The subagent wrapper (Agent/model: inherit, pointer body) exists, the repo-root CLAUDE.md carries an additive idempotent one-line pointer without clobbering existing content, and Gemini is wired to read AGENTS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen core → standalone wrappers | Skills/subagent must POINT at `agent-factory/*`, never copy role text (single-source) |
| installer/wrapper → user-owned CLAUDE.md | The repo-root CLAUDE.md is user-owned dev content; must be appended-to additively, never overwritten |
| agent → release operation | The destructive release op must not be agent-auto-invocable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-Tamper-1 | Tampering | a skill copies role text → drift / two sources of truth | mitigate | Pointer-text only; dup-check grep for a distinctive role sentence returns 0 hits under .claude/skills/ |
| T-05-02-Tamper-2 | Tampering / DoS | overwriting the pre-existing repo-root CLAUDE.md | mitigate | Idempotent ensure-line append; never `>`; preserve existing content |
| T-05-02-EoP-1 | Elevation of Privilege | agent auto-fires a release with no human in the loop | mitigate | `disable-model-invocation: true` on grugops-release (complements the SAFE-02 deploy guard) |
</threat_model>

<verification>
- `ls .claude/skills/grugops*/SKILL.md | wc -l` == 7; `grugops-release` has `disable-model-invocation: true`.
- Subagent wrapper uses `Agent` + `model: inherit`, no `Task`.
- Repo-root `CLAUDE.md` keeps its existing content and gains one idempotent pointer line; `.gemini/settings.json` lists `AGENTS.md` in `context.fileName`.
- Dup-check: a distinctive role sentence returns 0 hits under `.claude/skills/`.
</verification>

<success_criteria>
- Seven dash skills exist, pointer-only, with the release skill non-auto-invocable (CLAUDE-01).
- The subagent wrapper, repo-root CLAUDE.md pointer (additive), and Gemini wiring are in place.
- Single-source preserved: no role text copied anywhere.
</success_criteria>

<output>
Create `.planning/phases/05-packaging-adapters-install-distribution/05-02-SUMMARY.md` when done.
</output>

---
phase: 08-two-root-installer
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - install/uninstall.sh
  - install/README.md
autonomous: true
requirements: [INSTALL-03, INSTALL-04]

must_haves:
  truths:
    - "Two-root uninstall (run from a target) removes adapters + wiring + the .grugops/install.json marker only — it NEVER deletes the shared $GRUGOPS_HOME kit and NEVER deletes seeded state (.grugops/factory.config.json, plans/, memory-bank/)"
    - "is_protected protects .grugops/ broadly so seeded user config survives, and the marker is removed via a narrow grugops-owned exception"
    - "install/README.md documents --target, the interactive prompt + --yes/non-TTY bypass, copy-default (symlink opt-in), $GRUGOPS_HOME and the two-root layout, and the D-07 self-checkout guard"
  artifacts:
    - path: "install/uninstall.sh"
      provides: "Two-root reversal: marker + adapters + wiring removed; shared kit and seeded state preserved (D-06)"
      contains: "install.json"
    - path: "install/README.md"
      provides: "Installer docs for --target / copy-default / two-root / $GRUGOPS_HOME / self-checkout guard"
      contains: "--target"
  key_links:
    - from: "install/uninstall.sh"
      to: "$TARGET/.grugops/install.json"
      via: "narrow grugops-owned removal exception while .grugops/ is protected"
      pattern: "install.json"
    - from: "install/README.md"
      to: "install/install.sh behavior"
      via: "documents --target/--yes/copy-default/$GRUGOPS_HOME established in Plan 03"
      pattern: "GRUGOPS_HOME"
---

<objective>
Complete the two-root contract's reversibility and documentation:
1. **D-06 two-root uninstall:** update `install/uninstall.sh` so a target-run uninstall removes only the adapters + wiring (CLAUDE.md / Copilot sentinel pointers, the Gemini `context.fileName` entry) + the `.grugops/install.json` marker — and NEVER the shared `$GRUGOPS_HOME` kit (other repos depend on it) nor seeded state (`.grugops/factory.config.json`, `plans/`, `memory-bank/` may now hold user work). Removing the shared kit is a manual `rm` (no `--purge-kit` flag this phase — deferred).
2. **Docs:** update `install/README.md` to document `--target`, the confirm prompt + `--yes`/non-TTY bypass, copy-default (symlink opt-in), `$GRUGOPS_HOME` + the two-root layout, and the D-07 self-checkout guard.

This plan runs after Plan 03 so the reversal matches the exact artifacts the installer now creates (the `.grugops/install.json` marker, the materialized adapters) and the docs describe the frozen behavior.

Purpose: the additive/reversible contract holds end-to-end across BOTH roots; a user can fully reverse an install in a target without harming the shared kit or their seeded project state.
Output: a two-root-aware `uninstall.sh` and accurate installer docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-two-root-installer/08-CONTEXT.md
@.planning/phases/08-two-root-installer/08-RESEARCH.md
@.planning/phases/08-two-root-installer/08-PATTERNS.md
@.planning/phases/08-two-root-installer/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-root uninstall (D-06) — protect .grugops/ + seeded state, remove only the install.json marker, never touch the shared kit</name>
  <files>install/uninstall.sh</files>
  <read_first>
    - install/uninstall.sh (the file being modified — `is_protected` denylist at lines 54-64 to EXTEND; `remove_file` guarded delete at 67-83; `rmdir_if_empty` at 85-96; `remove_sentinel_block` awk strip at 98-130; the AGENTS.md grugops-owned exception at lines 228-247 — the model for "remove ONLY a grugops-owned file" that the marker removal mirrors)
    - install/install.sh (the marker write established in Plan 03 — confirm the marker path `$TARGET/.grugops/install.json` and that `.grugops/factory.config.json` is seeded user state; the run section's adapter set SKILLS/AGENT_REL at lines 42-44 to mirror in removal)
    - install/install.two-root.test.sh (the D-06 assertion this task turns GREEN: install then uninstall, assert `$GRUGOPS_HOME/agent-factory` survives, `.grugops/factory.config.json` survives, `.grugops/install.json` gone, `.claude` adapters gone)
    - .planning/phases/08-two-root-installer/08-RESEARCH.md (D-06 disposition; Pitfall 6 uninstall over-reach; Open Question 2 — protect `.grugops/` broadly then remove the marker via a narrow named exception)
    - .planning/phases/08-two-root-installer/08-PATTERNS.md (the `is_protected` extension VARIATION: add `.grugops/` to the protected set, remove only `.grugops/install.json` via the grugops-owned exception shape; NEVER add `$GRUGOPS_HOME` to any removal path)
  </read_first>
  <action>
    Extend `is_protected()` (`uninstall.sh:54-64`) to add `"$TARGET"/.grugops/*|"$TARGET"/.grugops` to the protected set, so seeded `.grugops/factory.config.json` (user state once seeded) can never be removed. Then add a NARROW, explicitly-named removal of ONLY `$TARGET/.grugops/install.json` (the grugops-owned marker) — mirror the AGENTS.md "grugops-owned only" shape at `uninstall.sh:228-247`: because `install.json` is under the now-protected `.grugops/`, `remove_file` would refuse it, so remove it via a dedicated `do_run rm -f -- "$TARGET/.grugops/install.json"` with its own report line, NOT via the generic `remove_file`/`is_protected` path (or add a single-file exception in the removal site). Keep removing the existing adapters (the `grugops*` skills, the `grugops-orchestrator.md` agent) + the CLAUDE.md / Copilot sentinel blocks + the Gemini unmerge exactly as today.
    CRITICAL — NEVER add `$GRUGOPS_HOME`, `$KIT_ROOT`, `plans/`, or `memory-bank/` to any removal/`rm`/delete path: the shared kit is depended on by other repos (manual `rm` only — no `--purge-kit` this phase) and seeded `plans/`/`memory-bank/` may hold user work. A defensive COMMENT naming the kit root (e.g. `# Never remove $GRUGOPS_HOME — shared across repos; manual rm only`) is encouraged for clarity — only `rm`/delete code referencing the kit root is forbidden (W-4: the gate targets the dangerous `rm.*GRUGOPS_HOME|rm.*KIT_ROOT` pattern, not the bare substring). Preserve `rmdir_if_empty` discipline (never recursive, guarded). Carry the prod-deploy prohibition forward — no new path touches the deploy-approval env var.
  </action>
  <verify>
    <automated>sh -n install/uninstall.sh && grep -q '.grugops' install/uninstall.sh && grep -q 'install.json' install/uninstall.sh && ! grep -qE 'rm.*GRUGOPS_HOME|rm.*KIT_ROOT|rm -rf.*\.grugops/factory|rm -rf.*\$GRUGOPS_HOME' install/uninstall.sh && sh install/install.two-root.test.sh && sh install/install.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `sh -n install/uninstall.sh` parses cleanly.
    - Source assertion: `is_protected` now includes `.grugops/` in its protected set (`grep '.grugops' uninstall.sh` shows it in the case list).
    - Source assertion: the marker `.grugops/install.json` is removed via a narrow grugops-owned exception (referenced in the file).
    - Source assertion (W-4 — target the dangerous pattern, not every occurrence): `uninstall.sh` contains NO destructive reference to the kit root — no `rm`/delete naming `GRUGOPS_HOME` or `KIT_ROOT`, and no `rm -rf` of the kit or `.grugops/factory.config.json` (`! grep -qE 'rm.*GRUGOPS_HOME|rm.*KIT_ROOT|rm -rf.*\$GRUGOPS_HOME|rm -rf.*\.grugops/factory'`). An explanatory comment (e.g. `# Never remove $GRUGOPS_HOME — shared across repos; manual rm only`) is PERMITTED and encouraged — the prohibition is mechanical (no rm/delete touches the kit root), not a blanket ban on the substring.
    - Behavior assertion (D-06): the Plan-02 uninstall assertion is GREEN — after install→uninstall, `$GRUGOPS_HOME/agent-factory` survives, `$TARGET/.grugops/factory.config.json` survives, `$TARGET/.grugops/install.json` is gone, and the `.claude` adapters are gone.
    - Regression: `sh install/install.test.sh` exits 0 (the frozen harness's `agent-factory/` + `plans/` survival checks still pass — the `.grugops/` protection does not break them).
  </acceptance_criteria>
  <done>`install/uninstall.sh` protects `.grugops/` (seeded config survives) and removes only the `.grugops/install.json` marker plus the adapters/wiring; it never touches `$GRUGOPS_HOME` or seeded `plans/`/`memory-bank/`; the Plan-02 D-06 assertion and the frozen `install.test.sh` are both GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Document the two-root installer in install/README.md (--target, prompt/--yes/non-TTY, copy-default, $GRUGOPS_HOME, self-checkout guard)</name>
  <files>install/README.md</files>
  <read_first>
    - install/README.md (the file being modified — read it fully to extend the existing structure in its established voice without overwriting the Phase-5 SAFE-02 docs)
    - install/install.sh (the source of truth for the documented behavior — the run banner lines 240-244, the closing notes at 273-279; the `--target`/`--yes`/`--allow-self`/`--symlink` flags, `$GRUGOPS_HOME`/`KIT_ROOT`, copy-default, and the D-07 guard message established in Plan 03)
    - docs/design/shared-install.md (the canonical two-root design — the kit-vs-state split, path-root convention, the three dogfood pains the docs should frame the fixes against)
    - .planning/phases/08-two-root-installer/08-CONTEXT.md (D-05 copy-default, D-06 uninstall scope, D-07 self-checkout guard — describe each accurately; do NOT document deferred items as available: no `--purge-kit`, no `--check` doctor [Phase 9])
  </read_first>
  <action>
    Update `install/README.md` to document the two-root installer behavior frozen in Plan 03, in the file's existing voice (clear voice for safety/behavior; this is installer docs). Add/extend sections covering:
    - `--target <repo>`: install into a named repo from any working directory (fixes the "wrong target" dogfood pain); precedence `--target` > `TARGET` env > prompt.
    - Interactive confirm prompt (default CWD) + `--yes`/non-TTY bypass for unattended CI installs.
    - Copy-default (D-05): the kit is COPIED to `${GRUGOPS_HOME:-$HOME/.grugops}` (default `~/.grugops`, env-overridable); symlink is opt-in via `--symlink`/`INSTALL_MODE=symlink` (frame against the symlink-fragility dogfood pain).
    - The two-root layout: read-only shared kit at `$GRUGOPS_HOME/agent-factory`; per-repo writable state seeded into the target (`.grugops/factory.config.json` + the install marker + `plans/` incl. `handoffs/` + `memory-bank/`), skip-if-exists / never-clobber.
    - Adapter materialization: the installer writes the resolved absolute kit path into the target's two resolver adapters so `/grugops` resolves on first run with no path error (fixes the "kit never arrives" dogfood pain).
    - The D-07 self-checkout guard: refuses by default when the target is the grugops source checkout; `--allow-self` overrides.
    - Uninstall scope (D-06): `uninstall.sh` removes adapters + wiring + the install marker only — never the shared kit (manual `rm` to remove it entirely) and never seeded state.
    - Preserve existing `DRY_RUN=1`, idempotent/additive/reversible, and the SAFE-02 mechanical-guard notes. Keep tool-specific commands that are unknown as `UNKNOWN - verify` (no fabrication). Do NOT document Phase-9 deferred features (`--check` doctor) or the deferred `--purge-kit` flag as if they exist.
  </action>
  <verify>
    <automated>grep -q -- '--target' install/README.md && grep -q 'GRUGOPS_HOME' install/README.md && grep -q -- '--yes' install/README.md && grep -qi 'copy' install/README.md && grep -q 'allow-self\|self-checkout\|source checkout' install/README.md && ! grep -q -- '--check' install/README.md && ! grep -q 'purge-kit' install/README.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `install/README.md` documents `--target`, `--yes`, `$GRUGOPS_HOME`, copy-default, and the self-checkout guard (`allow-self`/`source checkout`).
    - Source assertion: the README does NOT mention `--check` (Phase 9 deferred) or `--purge-kit` (deferred) as available features (no fabrication of unshipped behavior).
    - Source assertion: the existing SAFE-02 mechanical-guard / autonomy=pr fallback notes and the `DRY_RUN=1` documentation are preserved (not overwritten).
    - Behavior assertion: the documented commands match the actual installer flags (cross-check against `install.sh` — e.g. `--target`, `--yes`, `--allow-self`, `--symlink` all exist).
    - No fabrication: unknown tool-specific commands remain `UNKNOWN - verify`.
  </acceptance_criteria>
  <done>`install/README.md` accurately documents the two-root installer — `--target`, prompt + `--yes`/non-TTY, copy-default, `$GRUGOPS_HOME` + the two-root layout, materialization, the D-07 self-checkout guard, and the D-06 uninstall scope — without claiming any deferred (Phase-9 `--check`, `--purge-kit`) feature and preserving the existing SAFE-02 / DRY_RUN docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uninstall → shared `$GRUGOPS_HOME` | Other repos depend on the shared kit; uninstall must never reach outside the target into the shared kit |
| uninstall → seeded user state | `.grugops/factory.config.json`, `plans/`, `memory-bank/` may hold user work once seeded — must be protected from removal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | uninstall deleting the shared `$GRUGOPS_HOME` kit | mitigate | D-06: `uninstall.sh` carries no `rm`/delete of the kit root; removing it is a manual `rm` (no `--purge-kit`); asserted by the dangerous-pattern gate `! grep -qE 'rm.*GRUGOPS_HOME\|rm.*KIT_ROOT\|rm -rf.*\$GRUGOPS_HOME' uninstall.sh` (a defensive explanatory comment naming the kit root is allowed) |
| T-08-04-02 | Tampering | uninstall destroying seeded user content (`.grugops/factory.config.json`, `plans/`, `memory-bank/`) | mitigate | `is_protected` protects `.grugops/` + the existing `plans/`/`agent-factory/`/etc denylist; only `.grugops/install.json` is removed via a narrow grugops-owned exception (Pitfall 6) |
| T-08-04-03 | Information Disclosure | docs claiming an unshipped feature (`--check`, `--purge-kit`) misleading a user into unsafe assumptions | mitigate | No fabrication: README documents only shipped flags; deferred features are not presented as available (asserted by `! grep --check` / `! grep purge-kit`) |
| T-08-04-04 | Elevation of Privilege | uninstall touching the prod-deploy approval env var | accept | No uninstall path reads/writes/seeds the deploy-approval env var; prohibition carried verbatim |
| T-08-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (POSIX sh + markdown docs only; RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `sh -n install/uninstall.sh` parses.
- `sh install/install.two-root.test.sh` GREEN for the D-06 two-root uninstall assertion (kit + seeded config survive, marker + adapters removed).
- `sh install/install.test.sh` exits 0 (frozen harness — `.grugops/` protection does not break its survival checks).
- `install/README.md` documents `--target`/`--yes`/`$GRUGOPS_HOME`/copy-default/self-checkout guard and omits deferred features.
</verification>

<success_criteria>
- Two-root uninstall removes adapters + wiring + the install marker only; never the shared kit, never seeded state (D-06).
- `is_protected` protects `.grugops/` so seeded config survives; the marker is removed via a narrow grugops-owned exception.
- `install/README.md` accurately documents the two-root installer (`--target`, prompt/`--yes`/non-TTY, copy-default, `$GRUGOPS_HOME`, materialization, self-checkout guard, uninstall scope) with no fabricated/deferred features.
- The full Phase-8 suite is GREEN: `install.test.sh` + `check-kit-refs.sh` + `install.two-root.test.sh`.
</success_criteria>

<output>
Create `.planning/phases/08-two-root-installer/08-04-SUMMARY.md` when done.
</output>

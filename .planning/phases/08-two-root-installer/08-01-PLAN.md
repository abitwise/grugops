---
phase: 08-two-root-installer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/packaging/subagent.frontmatter.md
  - agent-factory/packaging/slash-command.template.md
  - agent-factory/README.md
  - agent-factory/config/factory.config.md
  - scripts/check-kit-refs.sh
  - agent-factory/seed/.grugops/factory.config.json
  - agent-factory/seed/plans/board.md
  - agent-factory/seed/plans/traceability.md
  - agent-factory/seed/plans/nfr-catalog.md
  - agent-factory/seed/plans/metrics.md
  - agent-factory/seed/plans/epics/.gitkeep
  - agent-factory/seed/plans/features/.gitkeep
  - agent-factory/seed/plans/tickets/.gitkeep
  - agent-factory/seed/plans/sprints/.gitkeep
  - agent-factory/seed/plans/releases/.gitkeep
  - agent-factory/seed/memory-bank/00-index.md
  - agent-factory/seed/memory-bank/10-project-brief.md
  - agent-factory/seed/memory-bank/20-product.md
  - agent-factory/seed/memory-bank/30-architecture.md
  - agent-factory/seed/memory-bank/40-contributing.md
  - agent-factory/seed/memory-bank/50-decisions/ADR-template.md
  - agent-factory/seed/memory-bank/60-progress.md
  - agent-factory/seed/memory-bank/70-runbook.md
  - agent-factory/seed/memory-bank/80-glossary.md
autonomous: true
requirements: [INSTALL-04]

must_haves:
  truths:
    - "The packaging templates no longer grant the Agent (spawn) tool, so regenerated adapters cannot re-introduce sub-agent spawning"
    - "agent-factory/README.md and factory.config.md document the runtime config at .grugops/factory.config.json (seed-source file mention preserved)"
    - "A self-contained kit-bundled seed subtree (agent-factory/seed/**) exists so a target can be seeded FROM $GRUGOPS_HOME with no source checkout present"
    - "scripts/check-kit-refs.sh still exits 0 after the seed subtree is bundled, because the seed is excluded from the scan"
  artifacts:
    - path: "agent-factory/seed/.grugops/factory.config.json"
      provides: "The .grugops/factory.config.json seed source (copy of the lean default config)"
      contains: "mode"
    - path: "agent-factory/seed/plans/board.md"
      provides: "Bundled plans/board.md seed shape"
    - path: "agent-factory/seed/memory-bank/00-index.md"
      provides: "Bundled memory-bank seed shape"
    - path: "agent-factory/packaging/subagent.frontmatter.md"
      provides: "Subagent frontmatter template with the Agent grant dropped (D-08)"
  key_links:
    - from: "agent-factory/seed/.grugops/factory.config.json"
      to: "agent-factory/config/factory.config.json"
      via: "byte-identical copy of the lean default config (D-02 precedent)"
      pattern: "mode"
    - from: "scripts/check-kit-refs.sh"
      to: "agent-factory/seed/"
      via: "intentional non-listing in SCAN/GH_SCAN (D-03 exclusion)"
      pattern: "agent-factory/seed"
---

<objective>
Land the three low-risk, installer-independent edits that Wave 2 builds on:
1. **D-08 (WR-05):** drop the `Agent` (spawn) tool grant from the two packaging templates so regenerated adapters can never re-introduce the no-spawn violation.
2. **D-09 (IN-01):** rewrite stale `agent-factory/config/factory.config.json` config-path PROSE in `agent-factory/README.md` + `agent-factory/config/factory.config.md` to the current `.grugops/factory.config.json` spelling (keeping the legitimate seed-source FILE mention).
3. **D-01/D-02 seed bundle + D-03 gate exclusion:** create the self-contained `agent-factory/seed/**` subtree (config + `plans/**` + `memory-bank/**`) that travels with the kit copy and is the source Wave-2's `seed_state` step copies into a target; add the documented exclusion of `agent-factory/seed/` from `scripts/check-kit-refs.sh` and confirm the gate stays green.

Purpose: These are independent doc/template/seed edits with no installer logic. Doing them first means Wave 2 (the installer rewrite) has a real seed source at `$KIT_ROOT/seed/` to copy from, and the gate exclusion is in place before any seed prose with target-relative refs lands in the kit.
Output: corrected templates + docs, a bundled seed subtree, and a gate that excludes it (still exit 0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-two-root-installer/08-CONTEXT.md
@.planning/phases/08-two-root-installer/08-RESEARCH.md
@.planning/phases/08-two-root-installer/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the Agent grant from the two packaging templates (D-08 / WR-05) and fix stale config-path prose in the two docs (D-09 / IN-01)</name>
  <files>agent-factory/packaging/subagent.frontmatter.md, agent-factory/packaging/slash-command.template.md, agent-factory/README.md, agent-factory/config/factory.config.md</files>
  <read_first>
    - agent-factory/packaging/subagent.frontmatter.md (the file being modified — see lines 15-17, 25, 55-59)
    - agent-factory/packaging/slash-command.template.md (the file being modified — see lines 31, 56, 100-102)
    - agent-factory/README.md (the file being modified — see line 17, lines 57-58)
    - agent-factory/config/factory.config.md (the file being modified — see line 3)
    - .claude/agents/grugops-orchestrator.md (the LIVE adapter that already correctly omits Agent: `tools: Read, Grep, Glob, Bash, Edit, Write` at line 4, and reads `.grugops/factory.config.json` at line 21 — the correct target spelling)
    - $HOME/.claude/projects/-Users-olgeroeselg-Projects-public-grugops/memory/grugops-sequential-role-load.md (the design rationale: single-window sequential role-load, NOT spawning)
  </read_first>
  <action>
    In `agent-factory/packaging/subagent.frontmatter.md`: change line 25 from `tools: Read, Grep, Glob, Bash, Edit, Write, Agent` to `tools: Read, Grep, Glob, Bash, Edit, Write` (matching the live `grugops-orchestrator.md` adapter). Rewrite the lines 15-17 prose ("The wrapper uses the current spawning tool **`Agent`**…") and the lines 55-59 bullet ("**`tools: …, Agent`** — `Agent` is the spawning tool. Listing it lets the Orchestrator wrapper spawn specialist role sub-agents…") to state that grugops uses single-window sequential role-load via the `_role-switch-protocol.md` (one window, drop prior context, the handoff is the only memory), NOT sub-agent spawning — so no spawn tool is granted. DO NOT remove the `${GRUGOPS_HOME:-$HOME/.grugops}` self-heal line — `subagent.frontmatter.md` is the resolver-mirroring template legally allowed to carry it (excluded from check-kit-refs.sh GH_SCAN); leave it intact.
    In `agent-factory/packaging/slash-command.template.md`: drop the `  - Agent` line from BOTH `allowed-tools` lists (lines 31 and 56), and drop/rewrite the lines 100-102 note ("**`Agent`** is included in `allowed-tools` so the skill can spawn specialist role sub-agents…") to reflect single-window sequential role-load.
    In `agent-factory/README.md`: rewrite line 17 (`> Read agent-factory/roles/orchestrator.md, then agent-factory/config/factory.config.json,`) so the runtime config read points to `.grugops/factory.config.json` (match the live adapter read order at `grugops-orchestrator.md:21`). For lines 57-58, phrase so the runtime config is read from `.grugops/factory.config.json` while the kit FILE `agent-factory/config/factory.config.json` is explicitly named as the SEED SOURCE / default (D-02/D-09) — preserve that seed-source mention, only correct the "where the orchestrator reads it" claim.
    In `agent-factory/config/factory.config.md`: rewrite line 3 ("`agent-factory/config/factory.config.json` is the configuration dial…The Orchestrator reads it first on every run…") so it states the Orchestrator reads `.grugops/factory.config.json` first on every run; this file documents that config; the kit ships the default at `agent-factory/config/factory.config.json` as the seed source.
    Do NOT introduce any new `agent-factory/config/factory` prose ref outside the legitimate seed-source FILE mention. These are doc/template prose edits only — no installer logic, no caveman-voice changes to safety content.
  </action>
  <verify>
    <automated>! grep -q ', Agent' agent-factory/packaging/subagent.frontmatter.md && ! grep -qE '^\s*-\s*Agent\s*$' agent-factory/packaging/slash-command.template.md && grep -q 'GRUGOPS_HOME' agent-factory/packaging/subagent.frontmatter.md && grep -q '.grugops/factory.config.json' agent-factory/README.md && grep -q '.grugops/factory.config.json' agent-factory/config/factory.config.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c ', Agent' agent-factory/packaging/subagent.frontmatter.md` returns 0 (the `tools:` line no longer grants Agent).
    - Source assertion: no line in `agent-factory/packaging/slash-command.template.md` matches `^\s*-\s*Agent\s*$` (both `allowed-tools` Agent entries removed).
    - Source assertion: `grep -q 'GRUGOPS_HOME' agent-factory/packaging/subagent.frontmatter.md` still succeeds (the legal self-heal line is preserved — Assertion 3 of check-kit-refs.sh permits it here).
    - Source assertion: both `agent-factory/README.md` and `agent-factory/config/factory.config.md` contain the string `.grugops/factory.config.json`.
    - Behavior assertion: `sh scripts/check-kit-refs.sh` exits 0 (these doc edits do not regress the gate; README/factory.config.md are not in SCAN, so the change is correctness-only).
    - No fabrication: the prose about sequential role-load matches the design memory (no invented spawn behavior).
  </acceptance_criteria>
  <done>The two packaging templates grant no spawn tool and explain single-window sequential role-load; both config docs name `.grugops/factory.config.json` as the runtime config while preserving the seed-source FILE mention; the self-heal line in subagent.frontmatter.md is intact; check-kit-refs.sh exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Bundle the kit seed subtree (agent-factory/seed/**) and exclude it from the kit-ref gate (D-01/D-02/D-03)</name>
  <files>agent-factory/seed/.grugops/factory.config.json, agent-factory/seed/plans/board.md, agent-factory/seed/plans/traceability.md, agent-factory/seed/plans/nfr-catalog.md, agent-factory/seed/plans/metrics.md, agent-factory/seed/plans/epics/.gitkeep, agent-factory/seed/plans/features/.gitkeep, agent-factory/seed/plans/tickets/.gitkeep, agent-factory/seed/plans/sprints/.gitkeep, agent-factory/seed/plans/releases/.gitkeep, agent-factory/seed/memory-bank/00-index.md, agent-factory/seed/memory-bank/10-project-brief.md, agent-factory/seed/memory-bank/20-product.md, agent-factory/seed/memory-bank/30-architecture.md, agent-factory/seed/memory-bank/40-contributing.md, agent-factory/seed/memory-bank/50-decisions/ADR-template.md, agent-factory/seed/memory-bank/60-progress.md, agent-factory/seed/memory-bank/70-runbook.md, agent-factory/seed/memory-bank/80-glossary.md, scripts/check-kit-refs.sh</files>
  <read_first>
    - scripts/check-kit-refs.sh (the file being modified — see the explicit SCAN allowlist at line 45, GH_SCAN at line 54, and the header comment block lines 39-44 documenting "by NOT listing them, this excludes…")
    - agent-factory/config/factory.config.json (the seed source for `.grugops/factory.config.json` — copy this byte-for-byte)
    - plans/board.md, plans/traceability.md, plans/nfr-catalog.md, plans/metrics.md (repo-root canonical seed shape for `plans/`)
    - memory-bank/00-index.md, memory-bank/10-project-brief.md, memory-bank/20-product.md, memory-bank/30-architecture.md, memory-bank/40-contributing.md, memory-bank/50-decisions/ADR-template.md, memory-bank/60-progress.md, memory-bank/70-runbook.md, memory-bank/80-glossary.md (repo-root canonical seed shape for `memory-bank/`)
  </read_first>
  <action>
    Create the self-contained seed subtree at top-level `agent-factory/seed/` (NOT nested under `agent-factory/packaging/`, which IS in SCAN — top-level keeps the exclusion clean per RESEARCH Pitfall 5 / A2). Populate it as a faithful copy of the current canonical skeletons:
    - `agent-factory/seed/.grugops/factory.config.json` = byte-identical copy of `agent-factory/config/factory.config.json` (the lean default; D-02 precedent — the default config already lives in the kit).
    - `agent-factory/seed/plans/` = copies of `plans/board.md`, `plans/traceability.md`, `plans/nfr-catalog.md`, `plans/metrics.md`, plus the five placeholder dirs `epics/ features/ tickets/ sprints/ releases/` each carrying a `.gitkeep` (mirror the repo's `plans/` shape). NOTE: do NOT bundle a `plans/handoffs/` dir here — `plans/handoffs/` is a RUNTIME dir absent from the repo skeleton; Wave 2's seed step `mkdirp`s it directly in the target (Pitfall 4). The seed need not carry it.
    - `agent-factory/seed/memory-bank/` = copies of `00-index.md`, `10-project-brief.md`, `20-product.md`, `30-architecture.md`, `40-contributing.md`, `50-decisions/ADR-template.md`, `60-progress.md`, `70-runbook.md`, `80-glossary.md`.
    Then edit `scripts/check-kit-refs.sh`: do NOT add `agent-factory/seed` (or any seed path) to `SCAN` or `GH_SCAN`; add a one-line comment in the header block (around lines 39-44) documenting that `agent-factory/seed/` is an intentional exclusion because seeds are STATE TEMPLATES whose `.grugops/…` and `plans/…` refs resolve in the TARGET, not the kit root (D-03). The gate already uses an explicit allowlist (never a repo-wide grep), so the exclusion IS the not-listing; the comment records why.
  </action>
  <verify>
    <automated>[ -f agent-factory/seed/.grugops/factory.config.json ] && cmp -s agent-factory/seed/.grugops/factory.config.json agent-factory/config/factory.config.json && [ -f agent-factory/seed/plans/board.md ] && [ -f agent-factory/seed/memory-bank/00-index.md ] && [ -f agent-factory/seed/memory-bank/50-decisions/ADR-template.md ] && ! grep -q 'agent-factory/seed' <(grep -E '^(SCAN|GH_SCAN)=' scripts/check-kit-refs.sh) && grep -q 'seed' scripts/check-kit-refs.sh && sh scripts/check-kit-refs.sh</automated>
  </verify>
  <acceptance_criteria>
    - Artifact assertion: `agent-factory/seed/.grugops/factory.config.json` exists and is byte-identical to `agent-factory/config/factory.config.json` (`cmp -s` succeeds).
    - Artifact assertion: `agent-factory/seed/plans/board.md`, `agent-factory/seed/plans/traceability.md`, `agent-factory/seed/plans/nfr-catalog.md`, `agent-factory/seed/plans/metrics.md` all exist; the five dirs `epics/ features/ tickets/ sprints/ releases/` each carry a `.gitkeep`.
    - Artifact assertion: all nine `agent-factory/seed/memory-bank/*` files exist including `50-decisions/ADR-template.md`.
    - Source assertion: neither the `SCAN=` nor the `GH_SCAN=` line in `scripts/check-kit-refs.sh` contains the substring `agent-factory/seed`.
    - Source assertion: the check-kit-refs.sh header carries a comment naming `seed` as an intentional exclusion (D-03 documented).
    - Behavior assertion: `sh scripts/check-kit-refs.sh` exits 0 with the seed subtree present (the seed's target-relative refs do not trip the gate).
  </acceptance_criteria>
  <done>A top-level `agent-factory/seed/` subtree bundles the full state-plane seed (config + `plans/**` + `memory-bank/**`) as a faithful copy of the canonical skeletons; the config seed is byte-identical to the kit default; `scripts/check-kit-refs.sh` excludes the seed by not listing it (documented) and still exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer checkout → shipped kit | Edits here ride along in the kit copied to `$GRUGOPS_HOME`; a stale ref or a re-introduced spawn grant would propagate to every install |
| seed templates → target repo (Wave 2) | The seed's `.grugops/…`/`plans/…` refs are written into a target later; they must NOT be held to the kit-resolution gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Elevation of Privilege | packaging templates granting `Agent` spawn | mitigate | D-08: drop the `Agent` grant so regenerated adapters cannot spawn sub-agents (single-window sequential role-load is the design); assert `grep -c ', Agent'` == 0 |
| T-08-01-02 | Tampering | stale `agent-factory/config/` config-path prose shipped in the kit | mitigate | D-09: rewrite to `.grugops/factory.config.json` so the kit at `$GRUGOPS_HOME` documents the correct runtime config path; keep the legitimate seed-source FILE mention |
| T-08-01-03 | Spoofing | seed templates falsely failing the kit-ref gate, or the gate silently scanning seeds | accept→mitigate | D-03: exclude `agent-factory/seed/` from SCAN/GH_SCAN (explicit allowlist, never repo-wide grep) and document why; assert `check-kit-refs.sh` exits 0 |
| T-08-01-04 | Tampering | no secret/credential handling; no deploy-approval env var touched | accept | This plan edits only templates/docs/seed markdown + the gate's allowlist; no env var is read, set, or seeded |
| T-08-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (stdlib/markdown only; RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `sh scripts/check-kit-refs.sh` exits 0 (gate green with the seed bundled and the cleanups applied).
- `sh install/install.test.sh` exits 0 (this plan does not touch the installer, so the existing 7-check harness is unaffected — run it as a regression guard).
- D-08 smoke: `! grep -q ', Agent' agent-factory/packaging/subagent.frontmatter.md` and no bare `- Agent` line in the slash-command template.
- D-09 smoke: both config docs name `.grugops/factory.config.json`.
- Seed presence: the full `agent-factory/seed/**` tree exists and the config seed is byte-identical to the kit default.
</verification>

<success_criteria>
- The packaging templates no longer grant the spawn tool and document single-window sequential role-load (D-08).
- `agent-factory/README.md` + `factory.config.md` point the runtime config read at `.grugops/factory.config.json` while preserving the seed-source FILE mention (D-09).
- A self-contained `agent-factory/seed/**` subtree bundles config + `plans/**` + `memory-bank/**`, with the config seed byte-identical to the kit default (D-01/D-02).
- `scripts/check-kit-refs.sh` excludes the seed subtree (documented) and exits 0 (D-03).
- The existing `install/install.test.sh` 7-check harness stays green.
</success_criteria>

<output>
Create `.planning/phases/08-two-root-installer/08-01-SUMMARY.md` when done.
</output>

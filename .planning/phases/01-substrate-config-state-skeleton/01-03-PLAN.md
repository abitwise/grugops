---
phase: 01-substrate-config-state-skeleton
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - plans/board.md
autonomous: true
requirements: [BOARD-01, BOARD-04]

must_haves:
  truths:
    - "plans/board.md lists all 13 §6.1 columns in order, each with its per-column WIP-limit format sourced from factory.config.json#wip_limits (BOARD-01, D-00)"
    - "The board format matches the §6.1 markdown shape and references the ticket front-matter status/column contract so board and ticket never disagree (BOARD-01, D-00)"
    - "Sizing (XS=1,S=2,M=3,L=5,XL=8; XL must split) and priority (P0–P3) are defined once for both cadences (BOARD-04, D-00)"
    - "The Blocked policy (blocked-by + date + escalation past blocked_escalation_days) is defined for both cadences (BOARD-04, D-00)"
    - "board.md ships the Kanban columns (default cadence) with ZERO live ticket rows — clean append target (D-03, Claude's-discretion)"
  artifacts:
    - path: "plans/board.md"
      provides: "The 13-column WIP board + WIP-limit format + sizing/priority/Blocked conventions"
      contains: "In Development"
  key_links:
    - from: "plans/board.md WIP-limit numbers"
      to: "agent-factory/config/factory.config.json#wip_limits"
      via: "per-column WIP defaults sourced from config"
      pattern: "WIP .*/[0-9]"
    - from: "plans/board.md ticket-format note"
      to: "the ticket front-matter status/column contract (§6.1)"
      via: "status line shape so board and ticket never disagree"
      pattern: "status:.*column:"
---

<objective>
Create `plans/board.md` — the single WIP source of truth — reproducing the spec §6.1 board EXACTLY: all 13 columns in order, each with its definition and per-column WIP-limit format sourced from `factory.config.json#wip_limits` (Plan 02). It follows the §6.1 markdown format and references the §6.1 ticket front-matter contract (`status`/`column`/`size`/`priority`/`epic`/`feature`) so a ticket's status always matches its board column. It also defines, ONCE for both cadences, the §6.3 sizing map (`XS=1, S=2, M=3, L=5, XL=8`; XL must be split) and priority (`P0–P3`), and the §6.4 Blocked policy (`blocked-by` reason + date, escalation past `blocked_escalation_days`).

Per Claude's discretion, the board ships the Kanban columns (the default cadence); the scrum overlay lives in `plans/sprints/` and is not pre-rendered here. Per D-03, the board ships with the column structure and a brief clear-voice format note but ZERO live ticket rows — a clean append target. Clear/professional voice (grug voice reserved for Phase-3 role prompts); lowercase `grugops`.

This plan is Wave 3 because the per-column WIP numbers are sourced from `factory.config.json#wip_limits`, which Plan 02 (Wave 2) produces.

Purpose: Freeze the 13-column board vocabulary, WIP-limit format, and sizing/priority/Blocked conventions every later workflow cites by name.
Output: `plans/board.md` — 13 columns + WIP format + sizing/priority/Blocked conventions, zero ticket rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md
@docs/initial/agent_factory_builder_spec_v2.md
@agent-factory/config/factory.config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write plans/board.md with the 13 §6.1 columns + WIP format sourced from config</name>
  <files>plans/board.md</files>
  <read_first>
    - docs/initial/agent_factory_builder_spec_v2.md §6.1 (The board) — the authoritative 13 columns + default WIP numbers + board.md markdown format + ticket front-matter fields (status/column/size/priority/epic/feature); reproduce columns, order, and format EXACTLY
    - agent-factory/config/factory.config.json — the `wip_limits` object (produced in Plan 02); the per-column WIP numbers in board.md must match these values and be described as sourced from config
    - .planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md — D-00 (reproduce §6.1 verbatim), D-03 (structure + format note, ZERO ticket rows), Claude's-discretion (ship Kanban columns; scrum overlay lives in plans/sprints/)
    - docs/initial/grugops_brand_manual.md §4.2 — clear voice; lowercase grugops
  </read_first>
  <action>
    Create `plans/board.md` reproducing the §6.1 board EXACTLY. Use the §6.1 markdown format (title `# Board`, an `_Updated: <ISO date> by <role>_` line). Render ALL 13 columns in §6.1 order: `Backlog`, `Ready`, `In Analysis`, `In Design`, `Ready for Dev`, `In Development`, `In Review`, `In Security/NFR`, `Ready for UAT`, `In UAT`, `Ready to Release`, `Done`, `Blocked`. For each column, show its per-column WIP-limit format in the §6.1 style (e.g. `## In Development (WIP /3)` / `## Ready (WIP /8)`), with the limit number taken from `factory.config.json#wip_limits` for the ten limited columns (Ready 8, In Analysis 2, In Design 2, Ready for Dev 6, In Development 3, In Review 3, In Security/NFR 2, Ready for UAT 4, In UAT 4, Ready to Release 4). `Backlog` and `Done` are `unlimited`; `Blocked` is visible/time-tracked (no WIP number) per §6.1. State that the WIP numbers come from config (`wip_limits`) and that the shown values are the lean defaults. Include the per-column ENTRY-MEANS and EXIT-OWNER from the §6.1 table either inline under each column or as a short reference block. Add a brief clear-voice note that each ticket file carries a front-matter status line (`status` / `column` / `size` / `priority` / `epic` / `feature`) so the board column and the ticket status never disagree — show the §6.1 status-line shape in the note (as an example, not a live ticket). Render ZERO live ticket rows under any column (D-03) — the board is an empty append target. Ship the Kanban columns only; do NOT pre-render a scrum overlay (that lives in `plans/sprints/`).
  </action>
  <verify>
    <automated>test -f plans/board.md && for col in "Backlog" "Ready" "In Analysis" "In Design" "Ready for Dev" "In Development" "In Review" "In Security/NFR" "Ready for UAT" "In UAT" "Ready to Release" "Done" "Blocked"; do grep -q "$col" plans/board.md || { echo "MISSING_COLUMN $col"; exit 1; }; done && grep -Eq "WIP[^0-9]*[0-9]" plans/board.md && grep -q "status:" plans/board.md && grep -q "column:" plans/board.md && echo BOARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `plans/board.md` exists and lists all 13 §6.1 columns in order: Backlog, Ready, In Analysis, In Design, Ready for Dev, In Development, In Review, In Security/NFR, Ready for UAT, In UAT, Ready to Release, Done, Blocked
    - Each limited column shows a WIP-limit format and the number matches `factory.config.json#wip_limits` (Ready 8, In Analysis 2, In Design 2, Ready for Dev 6, In Development 3, In Review 3, In Security/NFR 2, Ready for UAT 4, In UAT 4, Ready to Release 4); Backlog/Done = unlimited; the file states WIP numbers come from config
    - A note shows the ticket front-matter status-line contract (`status`/`column`/`size`/`priority`/`epic`/`feature`) so board and ticket never disagree
    - ZERO live ticket rows under any column
    - Clear voice; lowercase `grugops`; Kanban columns only (no scrum overlay)
  </acceptance_criteria>
  <done>board.md ships the exact 13 §6.1 columns with WIP-limit format sourced from config and the ticket status-line contract, with zero ticket rows.</done>
</task>

<task type="auto">
  <name>Task 2: Define sizing, priority, and Blocked conventions once for both cadences</name>
  <files>plans/board.md</files>
  <read_first>
    - docs/initial/agent_factory_builder_spec_v2.md §6.3 (Sizing and priority) — the authoritative sizing map (XS=1, S=2, M=3, L=5, XL=8; XL must be split) and priority (P0 .. P3); reproduce EXACTLY
    - docs/initial/agent_factory_builder_spec_v2.md §6.4 (Blocked policy) — blocked-by reason + date, daily-sweep escalation past the config threshold; reproduce EXACTLY
    - agent-factory/config/factory.config.json — `blocked_escalation_days` (the escalation threshold; default 2) and `sizing`/`priority_scheme` fields
    - .planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md — D-00 (reproduce §6.3/§6.4 verbatim), BOARD-04 (sizing/priority/Blocked shared by both cadences)
  </read_first>
  <action>
    In `plans/board.md` (append a "Conventions" section beneath the columns), define ONCE — explicitly noting these apply to BOTH cadences (kanban and scrum) per BOARD-04:
    - Sizing (§6.3): the t-shirt→points map `XS=1, S=2, M=3, L=5, XL=8`, with the rule that `XL must be split` (the Orchestrator enforces "no XL into dev"). Note BA/PM sizes and prioritizes at refinement.
    - Priority (§6.3): `P0` (drop everything) .. `P3` (someday); note WSJF is an alternative if `priority_scheme` says so.
    - Blocked policy (§6.4): any agent can move a ticket to Blocked with a `blocked-by` reason and a date; the daily sweep counts blocked time and escalates anything blocked past the config threshold `blocked_escalation_days` (default 2); a blocker is a decision, a dependency, or a missing input — name it and assign a human if needed.
    Keep it clear voice, concise, lowercase `grugops`. These conventions are written once here so both cadences share them (the Phase-4 scrum cadence will reference this same block).
  </action>
  <verify>
    <automated>grep -q "XS=1" plans/board.md && grep -q "S=2" plans/board.md && grep -q "M=3" plans/board.md && grep -q "L=5" plans/board.md && grep -q "XL=8" plans/board.md && grep -Eqi "XL must (be split|split)" plans/board.md && grep -q "P0" plans/board.md && grep -q "P3" plans/board.md && grep -qi "blocked-by" plans/board.md && grep -q "blocked_escalation_days" plans/board.md && grep -Eqi "both cadence" plans/board.md && echo CONVENTIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - The sizing map `XS=1, S=2, M=3, L=5, XL=8` is present with the "XL must be split" rule
    - Priority `P0` .. `P3` is defined (WSJF noted as the config alternative)
    - The Blocked policy is present: `blocked-by` + date + escalation past `blocked_escalation_days` (default 2)
    - The section explicitly states these conventions apply to BOTH cadences (kanban and scrum) — BOARD-04
    - Clear voice; lowercase `grugops`
  </acceptance_criteria>
  <done>Sizing, priority, and the Blocked policy are defined once in board.md and explicitly shared by both cadences.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| board.md ↔ factory.config.json | The board's WIP numbers must stay consistent with the config dial; the only concern is a stale or fabricated WIP number, not a security exploit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | board.md WIP numbers (drift from config) | mitigate | Task 1 read_first pins the WIP values to `factory.config.json#wip_limits` and the acceptance criteria assert the exact ten numbers, so the board cannot ship WIP defaults that contradict the dial. |
| T-01-10 | Tampering | board.md (accidental live ticket rows) | mitigate | Task 1 requires and acceptance asserts ZERO live ticket rows (clean append target, D-03), so a fresh installer and the Phase-6 validator start from an empty board. |
| T-01-11 | Information Disclosure | board.md markdown | accept | Plain markdown columns; no secrets, no executable code, no network, no untrusted input. No threat above LOW. |

No package-manager installs occur in this plan. There is no executable code, network access, authentication, untrusted input, or secret handling. Aside from the LOW items above, there are no threats above LOW for this plan.
</threat_model>

<verification>
- board.md lists all 13 §6.1 columns in order with per-column WIP format sourced from `factory.config.json#wip_limits`.
- The ticket front-matter status-line contract is referenced so board and ticket never disagree.
- Sizing (XS=1..XL=8, XL splits), priority (P0–P3), and the Blocked policy (blocked-by + date + escalation past blocked_escalation_days) are defined once for both cadences.
- Zero live ticket rows; Kanban columns only.
</verification>

<success_criteria>
- BOARD-01: `plans/board.md` is the WIP source of truth with the §6.1 columns, per-column WIP sourced from config, and the ticket status↔column contract.
- BOARD-04: sizing (XL must split) and priority (P0–P3) shared by both cadences; Blocked policy records blocked-by + date and escalates past the config threshold.
</success_criteria>

<output>
Create `.planning/phases/01-substrate-config-state-skeleton/01-03-SUMMARY.md` when done.
</output>

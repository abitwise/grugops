---
phase: 01-substrate-config-state-skeleton
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - plans/traceability.md
  - plans/nfr-catalog.md
  - plans/metrics.md
autonomous: true
requirements: [TRACE-01, TRACE-02, NFR-01, METRIC-01]

must_haves:
  truths:
    - "The stable ID scheme EPIC-/FEAT-/<prefix>-/ADR-/NFR-/RISK-/REL-/INC- is defined, with the prefix configurable (default ABC) (TRACE-01, D-00)"
    - "plans/traceability.md ships the §10 matrix column headers + a brief format comment and ZERO data rows (TRACE-02, D-03)"
    - "plans/nfr-catalog.md ships the §11 columns (ID/Category/Target/Applies to/Verified by) + format comment and ZERO data rows (NFR-01, D-03)"
    - "plans/metrics.md lists the §6.5 metric set + format comment and ZERO data rows (METRIC-01, D-03)"
    - "All seed files use the generic ABC prefix in their format comment and contain no grugops tickets — clean append target (D-04, D-03)"
  artifacts:
    - path: "plans/traceability.md"
      provides: "Requirement→ticket→code→test→UAT→release matrix (empty)"
      contains: "Ticket"
    - path: "plans/nfr-catalog.md"
      provides: "Non-functional targets catalog (empty)"
      contains: "Category"
    - path: "plans/metrics.md"
      provides: "Delivery metrics tracker (empty)"
      contains: "Cycle time"
  key_links:
    - from: "plans/traceability.md format comment"
      to: "the §10 ID scheme + ABC prefix"
      via: "an example row shown in a comment, not a live data row"
      pattern: "ABC-"
    - from: "plans/nfr-catalog.md / metrics.md"
      to: "the empty append target for Phase-3+ roles"
      via: "headers present, zero data rows"
      pattern: "no table data rows below the header/separator"
---

<objective>
Seed the state-plane skeleton files that roles append to in later phases: `plans/traceability.md` (the §10 requirement→ticket→code→test→UAT→release matrix), `plans/nfr-catalog.md` (the §11 non-functional targets catalog), and `plans/metrics.md` (the §6.5 delivery metric set). Each ships with its REAL headers/columns plus a brief clear-voice format comment showing the row shape, and ZERO live data rows (D-03) — a clean append target. The stable ID scheme (`EPIC-/FEAT-/<prefix>-/ADR-/NFR-/RISK-/REL-/INC-`, prefix configurable, default `ABC`) is defined here and referenced in the traceability format comment.

Per D-04, these are part of the user-facing kit template (`plans/`), kept distinct from `.planning/`; the format comments use the generic placeholder prefix `ABC` and seed NO grugops-specific tickets. Per D-03, the goal is a clean empty plane so a fresh installer and the Phase-6 validator both start from a true empty state with no fake/example data rows. Clear/professional voice (grug voice reserved for Phase-3 role prompts).

Purpose: Freeze the traceability/NFR/metrics column vocabulary and the ID scheme every later role cites by name, as empty append targets.
Output: `plans/traceability.md`, `plans/nfr-catalog.md`, `plans/metrics.md` — headers + format comment, zero data rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md
@docs/initial/agent_factory_builder_spec_v2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed plans/traceability.md (§10 matrix headers + ID scheme, zero rows)</name>
  <files>plans/traceability.md</files>
  <read_first>
    - docs/initial/agent_factory_builder_spec_v2.md §10 (Traceability and IDs) — the authoritative ID schemes (EPIC/FEAT/<prefix>/ADR/NFR/RISK/REL/INC, default prefix ABC) and the traceability.md matrix column headers; reproduce the columns EXACTLY
    - .planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md — D-03 (headers + format comment, ZERO data rows), D-04 (ABC prefix, no grugops tickets, plans/ distinct from .planning/)
    - docs/initial/grugops_brand_manual.md §4.2 — clear voice for technical files; lowercase grugops
  </read_first>
  <action>
    Create `plans/traceability.md` reproducing the §10 matrix EXACTLY. Include the title (`# Traceability Matrix`), the `_Updated: <date>_` line shape, and a markdown table whose header row carries exactly these §10 columns in order: `Ticket | Title | Epic | Feature | NFRs | Code (PR/files) | Tests | UAT | Release | Status`, with the markdown separator row beneath it. Add a brief clear-voice format comment (an HTML comment block or a short prose note) that (a) shows ONE example row shape using the generic `ABC-` prefix to illustrate the format, explicitly marked as a comment/example and NOT a live table row, and (b) states the stable ID scheme verbatim from §10: `EPIC-xxx` epic, `FEAT-xxx` feature, `<prefix>-xxx` ticket (prefix from config `id_prefix`, default `ABC`), `ADR-000x` ADR, `NFR-xxx`, `RISK-xxx`, `REL-xxxx`, `INC-xxxx`. Add ZERO live data rows under the table separator — the matrix body is empty (D-03). Note the brief §10 append rule (BA/PM creates the row at ticket birth; each role appends its link as it completes work) in the comment so the file is self-documenting. Do NOT seed any grugops ticket.
  </action>
  <verify>
    <automated>test -f plans/traceability.md && grep -q "| Ticket | Title | Epic | Feature | NFRs | Code (PR/files) | Tests | UAT | Release | Status |" plans/traceability.md && grep -q "EPIC-" plans/traceability.md && grep -q "REL-" plans/traceability.md && grep -q "INC-" plans/traceability.md && DATAROWS=$(grep -E '^\| [A-Z]{2,5}-[0-9]' plans/traceability.md | grep -vc '<!--'); [ "$DATAROWS" -eq 0 ] && echo TRACE_OK || { echo "FOUND_DATA_ROWS $DATAROWS"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `plans/traceability.md` exists with the exact §10 header row: `Ticket | Title | Epic | Feature | NFRs | Code (PR/files) | Tests | UAT | Release | Status`
    - The ID scheme (EPIC/FEAT/<prefix>/ADR/NFR/RISK/REL/INC) is stated, with default prefix `ABC` and prefix-from-config noted
    - ZERO live data rows in the table body (no `| ABC-001 | ...` row outside a comment); the example-row prefix `ABC-` appears only inside the format comment
    - Clear voice; lowercase `grugops`; no grug caveman voice
  </acceptance_criteria>
  <done>traceability.md ships the exact §10 matrix columns, the full ID scheme, and a format comment showing the row shape — with zero live data rows.</done>
</task>

<task type="auto">
  <name>Task 2: Seed plans/nfr-catalog.md and plans/metrics.md (§11 / §6.5 headers, zero rows)</name>
  <files>plans/nfr-catalog.md, plans/metrics.md</files>
  <read_first>
    - docs/initial/agent_factory_builder_spec_v2.md §11 (NFR Catalog and SLOs) — the authoritative nfr-catalog.md columns (ID / Category / Target / Applies to / Verified by); reproduce EXACTLY
    - docs/initial/agent_factory_builder_spec_v2.md §6.5 (Metrics) — the authoritative metrics.md metric list (Throughput, Cycle time, Lead time, WIP, Blocked time, Rework rate, Gate pass rate, Escaped defects, Velocity); reproduce EXACTLY
    - .planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md — D-03 (headers + format comment, ZERO data rows), D-04 (no grugops data)
    - docs/initial/grugops_brand_manual.md §4.2 — clear voice; lowercase grugops
  </read_first>
  <action>
    Create TWO files:
    (1) `plans/nfr-catalog.md` reproducing the §11 catalog EXACTLY. Include the title (`# NFR Catalog`), the `_Updated: <date>_` line shape, and a markdown table whose header row carries exactly these §11 columns in order: `ID | Category | Target | Applies to | Verified by`, with the separator row beneath. Add a brief clear-voice format comment that shows ONE example row shape (e.g. an `NFR-001 | Performance | ...` example, explicitly marked as comment/example, NOT a live row) and notes the §11 ownership rule (Architect/Design seeds it, Security/NFR checks against it, Release Manager attaches evidence) and the candidate category list from §11 (performance, scalability, availability, reliability/DR, security, privacy/compliance, accessibility, observability, maintainability, portability, i18n/l10n, cost). ZERO live data rows under the separator.
    (2) `plans/metrics.md` reproducing the §6.5 metric set EXACTLY. Include the title (`# Metrics`) and list every §6.5 metric with its one-line meaning: Throughput (tickets reaching Done per period), Cycle time (Ready for Dev → Done, median), Lead time (Backlog → Done, median), WIP (average tickets in flight), Blocked time (total/median time in Blocked), Rework rate (tickets bounced back a column), Gate pass rate (first-pass PASS at quality + security gates), Escaped defects (bugs found in UAT or prod that tests missed), Velocity (scrum: size points per sprint). Choose a clean layout (a definition list, or a table with a value/period column left empty) per planner discretion, but carry ZERO live data values — the file is an empty tracker. Add a brief clear-voice format comment noting §6.5's update owners (updated by the daily sweep and the retro; consumed by the Factory Coach) and that these are markdown counts, not a metrics platform.
    Both files: clear voice, lowercase grugops, no grug caveman voice, no grugops-specific data.
  </action>
  <verify>
    <automated>test -f plans/nfr-catalog.md && grep -q "| ID | Category | Target | Applies to | Verified by |" plans/nfr-catalog.md && NFRROWS=$(grep -E '^\| NFR-[0-9]' plans/nfr-catalog.md | grep -vc '<!--'); [ "$NFRROWS" -eq 0 ] || { echo "NFR_DATA_ROWS $NFRROWS"; exit 1; }; test -f plans/metrics.md && for m in Throughput "Cycle time" "Lead time" WIP "Blocked time" "Rework rate" "Gate pass rate" "Escaped defects" Velocity; do grep -q "$m" plans/metrics.md || { echo "MISSING_METRIC $m"; exit 1; }; done && echo NFR_METRICS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `plans/nfr-catalog.md` exists with the exact §11 header row: `ID | Category | Target | Applies to | Verified by`; ZERO live `NFR-NNN` data rows (the example appears only inside the format comment)
    - `plans/metrics.md` exists and names all nine §6.5 metrics (Throughput, Cycle time, Lead time, WIP, Blocked time, Rework rate, Gate pass rate, Escaped defects, Velocity) with their one-line meanings; ZERO live metric values
    - Both files carry a brief clear-voice format comment with the relevant §11/§6.5 ownership/update notes
    - Clear voice; lowercase `grugops`; no grug caveman voice; no grugops-specific data
  </acceptance_criteria>
  <done>nfr-catalog.md ships the exact §11 columns and metrics.md names the full §6.5 metric set — both with format comments and zero live data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed files → roles (future) | These markdown tables are append targets for later-phase roles; the only concern is shipping accidental live data that would mislead the validator or an installer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | seed state files (accidental data rows) | mitigate | Each task's verify asserts ZERO live data rows (grep counts table data rows and fails if > 0), so the clean-empty-plane invariant (D-03) is mechanically enforced — the Phase-6 validator and a fresh installer start from a true empty state. |
| T-01-06 | Information Disclosure | traceability/nfr/metrics markdown | accept | Plain markdown headers with a generic `ABC` placeholder; no secrets, no executable code, no network, no untrusted input. No threat above LOW. |

No package-manager installs occur in this plan. There is no executable code, network access, authentication, untrusted input, or secret handling. Aside from the LOW items above, there are no threats above LOW for this plan.
</threat_model>

<verification>
- traceability.md carries the exact §10 columns + full ID scheme; nfr-catalog.md carries the exact §11 columns; metrics.md names the full §6.5 metric set.
- Every seed file has a brief clear-voice format comment showing the row shape and ZERO live data rows.
- Generic `ABC` prefix used in examples; no grugops-specific tickets/data.
</verification>

<success_criteria>
- TRACE-01: stable ID scheme defined (configurable prefix, default ABC).
- TRACE-02: traceability.md seeded with §10 matrix columns, zero rows.
- NFR-01: nfr-catalog.md seeded with §11 columns, zero rows.
- METRIC-01: metrics.md seeded with the §6.5 metric set, zero data.
</success_criteria>

<output>
Create `.planning/phases/01-substrate-config-state-skeleton/01-04-SUMMARY.md` when done.
</output>

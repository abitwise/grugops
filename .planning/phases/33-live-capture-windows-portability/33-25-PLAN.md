---
phase: 33-live-capture-windows-portability
plan: 25
type: execute
wave: 14
depends_on: ["33-24"]
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/compactor.ts
  - scripts/compactor.js
  - scripts/compactor.test.ts
  - scripts/context-io-writer-set.test.ts
  - scripts/capture-live.test.ts
  - scripts/board-read.test.ts
  - scripts/trace-render.test.ts
  - scripts/convergence-spine.test.ts
  - scripts/admission-server.test.ts
  - scripts/fixtures/board-snapshot/.grugops/context/abc-104-implement/notes/2026-09-14T090500Z-decision-a1b2c3d4.md
  - scripts/fixtures/board-snapshot/.grugops/context/abc-104-implement/notes/2026-09-14T091000Z-decision-e5f6a7b8.md
  - agent-factory/contracts/context-note.md
  - agent-factory/workflows/16-context-read-write.md
  - CHANGELOG.md
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
autonomous: true
gap_closure: true
requirements: [CAP-01, CAP-03]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A note the sanctioned writer did not compose is REFUSED on read: `readRawNotesWithSkips` — the ONE walk every reader route in the module goes through — skips it under a new, named, counted arm `unsealed`, and `render` reports the count; a note the writer composed reads back exactly as before (KIT (b), the human's direction at plan 33-21, WINDOWS.md row 256)."
    - "The mechanical distinguisher is a content-bound seal the writer emits in ONE place (`composeNote`) and the reader verifies through ONE exported predicate; the seal moves on one changed byte, is refused when absent, malformed, or copied from another note — each proven by a mutation, RED before GREEN, with the real hand-written notes of the held round-1 capture (round-1-held/33-CAPTURE-B.jsonl, the `Write` tool-use inputs at B:774 … B:1802) as the fixture."
    - "How the refusal is REACHED is probed from every reader route, not only what it refuses: `readContext`, `currentState`, `render`, `promoteAdmitted`'s destination-liveness read, `dual-path-equivalence.projectTaskState`, `trace-render`, `capture-live.authorStamps` (CAP-03 side (b)), and the compactor's own walk over the promoted tier — the second reader in the tree, which applies the same exported predicate rather than a copy."
    - "The residual is stated in clear professional voice, in the module and in the ledger: the seal is unkeyed — a file-based kit holds no secret the subject cannot read — so it distinguishes hand-composed from writer-composed notes and detects post-write edits; a process that reimplements the algorithm is one register over, and the un-forgeable tier (a point-of-effect deny of file-writing tools under the context root) is a kit capability decision left to the human, not taken here (CONTEXT: no new factory capability)."
    - "No grandfather clause: a note composed before the seal existed is refused on read like any other unsealed note — asserted over a note the dispatch base's own committed `.js` wrote — because an age exemption is exactly the arm a hand-writer would take. Recorded as a costly decision with its blast radius (the two committed board-snapshot fixture notes, re-sealed; every hand-planted test note, derived and dispositioned)."
  artifacts:
    - scripts/context-io.ts
    - scripts/context-io.js
    - scripts/context-io.test.ts
    - scripts/compactor.ts
    - agent-factory/contracts/context-note.md
  key_links:
    - "context-io.ts `composeNote` -> the `seal:` line (last inside the fence) -> `validate()` on the sealed bytes -> `writeNoteFile`"
    - "context-io.ts `readRawNotesWithSkips` -> `sealVerdict(text)` -> `skipped.push({ arm: 'unsealed' })` -> `readContext` / `render` / every consumer"
    - "compactor.ts `readNoteDir` (promoted tier) -> the same exported `sealVerdict` -> `carve-out FAIL` naming the file"
    - "capture-live.ts `authorStamps` -> `readContext` -> side (b) of `capThreePredicate` now counts sealed notes only"
  prohibitions:
    - "no second seal computation exists anywhere in the tree — the compactor and every test import the one exported predicate"
    - "no age, tier, kind or author exemption lets an unsealed note under `notes/` read as admitted"
    - "the D-07 predicate is never softened to make a capture read as parity (D-20)"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): this plan lands KIT (b), one of the two human-directed items the flip waits on; it decides nothing about the flip."
    - "CAP-02 (edge row unresolved, flagged): untouched by this plan beyond the rebuild of a module the windows leg exercises; measured only by plan 33-31."
    - "CAP-03 (edge row unresolved, flagged): side (b) of the CAP-03 predicate now counts only writer-composed notes; whether the next capture's role agents reach the sanctioned writer on the spawn path is measured by plan 33-32's go, never assumed here."
    - "assumption_delta = no-change on the reader count: the compactor's walk over the promoted tier already existed as a second reader; this plan does not add a reader, it routes the existing second one through the one predicate."
---

<objective>
Gap-closure round 3 of the phase's four-round cap. The human's KIT direction (b), recorded at plan
33-21's decision checkpoint and carried as WINDOWS.md row 256: the reader REFUSES non-sanctioned
(hand-written, `Write`-tool) notes on read. 33-DIAGNOSIS.md § 1.3 (ii) measured the gap on the
round-1 capture: path B's nine notes were written with the `Write` tool straight into the context
root and `readContext` admitted them, so the WF16 single-writer rule held on path A by tooling and
on path B by nothing. This is a safety-invariant change on the shared verified context, so it is
RED-first, mutation-proven, and probed from every reader route.

Gap mapping (33-VERIFICATION.md CAP-01 `missing[0]` (b)): "scripts/context-io.ts's note reader
refuses non-sanctioned (hand-written, Write-tool) notes on read" → Task 1 (the seal and the arm),
Task 2 (every reader route and every planted fixture), Task 3 (the contract, the changelog, the
twin and the manifest).

Decisions honored: D-02 (side (b) is derived from the target's context root — it now derives over
writer-composed notes only), D-07/D-20 (the predicate is not softened; the route axis already
counts direct writes and this plan makes them fail to READ, which the next capture measures),
D-11 (offline proof before any go).

Purpose: make "the sanctioned writer is the only way in" a mechanical property of the reader
instead of a sentence in WF16.
Output: `scripts/context-io.ts` + committed `.js` (+ the hook manifest), the compactor routed
through the one predicate, the re-sealed fixtures, the contract and changelog entries.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md
@.planning/phases/33-live-capture-windows-portability/33-21-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-24-SUMMARY.md
@agent-factory/contracts/context-note.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The writer seals in one place, the one walk verifies through one predicate, and the real hand-written notes are the RED fixture</name>
  <reversibility rating="costly">Every note composed before this change becomes unreadable by the reader (no grandfather clause); reverting means removing the arm and the field and re-admitting every unsealed note. Not one-way: no requirement status or archived record changes.</reversibility>
  <read_first>
    - `scripts/context-io.ts` — `composeNote` (:1430-1448, the fence layout; `id:` first is a frozen slot), `provenanceBlock` (:1449-1473), `composeValidatedNote` (:1496-1545: compose then `validate(text)`), `validate` (:706-760: which keys it requires and whether it refuses an unknown column-0 key), `parseNote` (:344), `NOTE_SKIP_ARMS` / `SkippedNoteEntry` / `readRawNotesWithSkips` (:1739-1838: the three-arm docblock and the `unparseable` push), `recordFromParsed` (:1848), `readContext` (:1873), `render` (:4140 — where the skipped arms are reported), the DeLM hollow-evidence list (:205), the header's clear-voice rule.
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` § 1.3 (ii) — the nine `Write` tool-use lines on path B (B:774, B:795, B:817, B:1542, B:1607, B:1638, B:1757, B:1779, B:1802); § 1.4 (b).
    - `.planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-B.jsonl` — those nine lines: each is an `assistant` frame whose `tool_use` block named `Write` carries `input.file_path` (under the task's `.grugops/context/TASK/notes/` directory) and `input.content` (the hand-composed note text). The fixture is the content, verbatim.
    - `scripts/context-io-writer-set.test.ts` — how the writer set and the refusal sites are DERIVED from the module's AST (the shape the seal's single-emission assertion follows).
    - `scripts/context-io.test.ts` — the 31-29 / 31-33 skip-arm cases (`grep -n 'unparseable\|vanished\|NOTE_SKIP_ARMS' scripts/context-io.test.ts`) whose shape the `unsealed` cases follow.
  </read_first>
  <files>scripts/context-io.ts, scripts/context-io.test.ts</files>
  <behavior>
    - Test S1 (RED first — the real thing): for each of the nine path-B `Write` inputs read from `round-1-held/33-CAPTURE-B.jsonl` through `git show c7be6d0d:` (the immutable commit; parse the frame, take `input.content`), plant the content in a scratch context root under the task's `notes/` directory at the basename of `input.file_path`; PREMISE asserted in the same body: `parseNote(content)` is non-null for all nine (they are structurally notes — that is the whole finding); then `readContext(task, root)` returns 0 records and the skips reader reports 9 entries under arm `unsealed` with detail `absent`. On the dispatch base the same body reads 9 records — quote the red run.
    - Test S2 (the writer's own note reads): a note written through `appendNote` into a scratch root reads back as 1 record with 0 skipped; its on-disk text carries exactly one `seal:` line, positioned last inside the fence, matching `^seal: sha256:[0-9a-f]{64}$`.
    - Test S3 (mutations, one at a time, each named): (a) one body byte changed after the write → arm `unsealed`, detail `mismatch`; (b) the `seal:` line deleted → `absent`; (c) the `seal:` line of note X pasted onto note Y → `mismatch`; (d) a `seal:` value that is not `sha256:` + 64 hex → `malformed`; (e) two `seal:` lines → `malformed` (the duplicate-key path must not read the first and ignore the second).
    - Test S4 (no grandfather clause, proven against the base's own writer): materialise the committed `scripts/context-io.js` of the last pushed sha (`git show 9e1c1131cec8943e2ac96233ed7e624720ced14b:scripts/context-io.js` into a scratch kit beside the `.js` files it imports, the `preFixKit` idiom), write one note through THAT module's `appendNote`, and read the directory with HEAD's reader: 0 records, 1 `unsealed`/`absent`; the same directory read by the base module: 1 record (the premise that the note was a legitimate pre-seal note).
    - Test S5 (one authority): the module's source has exactly ONE site that emits the seal key and exactly ONE function that computes the digest; the test derives both counts from the parsed TypeScript (the `context-io-writer-set.test.ts` idiom) — a second emission or a second digest site is a red.
  </behavior>
  <action>
    Add to `scripts/context-io.ts`, in clear professional voice (safety surface):

    - `export const NOTE_SEAL_KEY = "seal"` and a domain-separation literal `NOTE_SEAL_DOMAIN`
      (a versioned string, e.g. `grugops-note-seal-v1`) — fixed literals, never from argv, env
      or note content.
    - `export function noteSeal(unsealedText: string): string` — `sha256` over the domain
      literal, a NUL byte, and the note's bytes WITHOUT its seal line; returns `sha256:<64 hex>`.
      Its docblock states the residual named in the must-haves (unkeyed by necessity; what it
      distinguishes; what it does not).
    - `export function sealVerdict(text: string): { ok: true } | { ok: false; reason: "absent" | "malformed" | "mismatch" }`
      — THE ONE predicate: locate the column-0 `seal:` line inside the fence (exactly one;
      zero → `absent`; two or more, or a value outside the anchored `sha256:` + 64-hex allowlist →
      `malformed`); strip that line; recompute through `noteSeal`; compare → `mismatch` or ok.
      Nothing else in the module or the tree computes a seal.
    - `composeNote` emits the seal as the LAST line inside the fence: compose the text without it,
      call `noteSeal`, insert the line before the closing delimiter. `id:` stays first (frozen
      slot). Because `composeValidatedNote` validates the composed text, `validate` sees the sealed
      bytes — confirm `validate` accepts the key (it reads an open scalar map; if it enumerates
      admitted keys, add `seal` there with the same field-injection guard the other scalars get).
    - `NOTE_SKIP_ARMS` gains `"unsealed"` as a READER-OWNED arm beside `unparseable` (the set stays
      derived from `READ_POSITION_CONDITIONS` for the authority's arms; this one is the reader's).
      In `readRawNotesWithSkips`, after `parseNote` succeeds and before the note is pushed, ask
      `sealVerdict(text)`; on failure push `{ file, arm: "unsealed", detail: reason }` and
      `continue`. Extend the arm docblock with the fourth reader-owned fact and the rule: a note
      the sanctioned writer did not compose is not a note this store returns, whatever else it is.
    - `render` reports the new arm's count the way it reports the others (if the report loop
      iterates `NOTE_SKIP_ARMS`, nothing more is needed; if it names arms, add this one).

    Tests S1-S5 in `scripts/context-io.test.ts`, RED committed before GREEN (S1's red run quoted:
    nine records read on the base). Reuse the `git show c7be6d0d:` reading idiom
    `scripts/capture-live.test.ts` established for the held capture (`heldCapture(name)`), and the
    `preFixKit` idiom (:6572) for S4's base-module kit. Rebuild so the committed `.js` moves in
    the same commit; `npm run generate:hook-manifest` follows every context-io rebuild.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts -t "unsealed|seal"</automated>
    <fails_when>non-zero exit, or 0 passed, or fewer than 5 tests reported passed (S1-S5 are five cases at minimum)</fails_when>
    <automated>grep -a -c '"unsealed"' scripts/context-io.ts</automated>
    <fails_when>a count of 0 — the arm is not declared in the module</fails_when>
    <automated>node -e "const m=require('./scripts/context-io.js'); const s=m.noteSeal('x'); if(!/^sha256:[0-9a-f]{64}$/.test(s)) process.exit(1); if(m.sealVerdict('---\nid: a\nkind: claim\n---\n\nbody\n').ok!==false) process.exit(2)"</automated>
    <fails_when>non-zero exit — the seal form is not the anchored sha256 hex form, or an unsealed text is not refused by the predicate</fails_when>
    <automated>npm run build &amp;&amp; npm run generate:hook-manifest &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/context-io.ts` exports `NOTE_SEAL_KEY`, `noteSeal`, `sealVerdict`; `NOTE_SKIP_ARMS` contains `unsealed`; `composeNote` is the only site that emits the seal key and `noteSeal` the only digest site (S5 derives both counts from the AST and the summary quotes them).
    - Tests S1-S5 exist with the exact arm/detail words above; S1's RED commit precedes the GREEN commit and the summary quotes both shas and the red run's `9` count; S4 quotes the base module's `1` and HEAD's `0`.
    - The module docblock for `noteSeal` states the unkeyed residual and the un-forgeable tier not taken, in clear voice (no caveman register in that block).
    - `scripts/context-io.js`, `hooks/hook-entry.ts`, `hooks/hook-entry.js` are rebuilt in the same commit; parity and manifest freshness exit 0.
  </acceptance_criteria>
  <done>A note the writer did not compose is refused by name and counted; a note it did compose reads exactly as before; every mutation moves the verdict in the direction it must.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Every reader route reaches the refusal, the compactor's promoted-tier walk asks the same predicate, and every planted fixture in the tree is derived and dispositioned</name>
  <read_first>
    - `scripts/compactor.ts` — `readNoteDir` (:184-213: the second walk over a notes directory), `readNoteFields` (:131), `composeThreadNote` (:585 — the RAW thread tier, NOT sealed), the byte-equal field loop (:505-520: six named fields; the seal is not among them), gate (b) `validate()` over promoted bytes (:344).
    - `scripts/context-io.ts` `promoteAdmitted` (:2944) — the destination-liveness read; `dual-path-equivalence.ts:42` (`currentState(readContext(...))`); `trace-render.ts:130`; `capture-live.ts` `authorStamps` (:789, through `readContext`) and `capThreePredicate` (:810).
    - The two committed fixture notes under `scripts/fixtures/board-snapshot/.grugops/context/abc-104-implement/notes/` and their consumers (`grep -rln board-snapshot scripts/*.test.ts`).
    - `scripts/context-io.test.ts`, `scripts/compactor.test.ts`, `scripts/board-read.test.ts`, `scripts/trace-render.test.ts`, `scripts/convergence-spine.test.ts`, `scripts/admission-server.test.ts`, `scripts/capture-live.test.ts` (`contextRootWithNotes`) — the test files that write under a `notes/` directory (`grep -ln '"notes"'`); which of them plant note bytes by hand is DERIVED in this task, not assumed.
  </read_first>
  <files>scripts/compactor.ts, scripts/compactor.js, scripts/compactor.test.ts, scripts/context-io.test.ts, scripts/capture-live.test.ts, scripts/board-read.test.ts, scripts/trace-render.test.ts, scripts/convergence-spine.test.ts, scripts/admission-server.test.ts, scripts/context-io-writer-set.test.ts, scripts/fixtures/board-snapshot/.grugops/context/abc-104-implement/notes/2026-09-14T090500Z-decision-a1b2c3d4.md, scripts/fixtures/board-snapshot/.grugops/context/abc-104-implement/notes/2026-09-14T091000Z-decision-e5f6a7b8.md</files>
  <behavior>
    - Test R1 (routes): with one sealed and one unsealed note under a task, `readContext` returns 1; `currentState(readContext(...))` returns 1; `render` writes an index naming 1 note and a skipped line naming 1 `unsealed`; `projectTaskState` (dual-path-equivalence) projects 1; `trace-render` renders 1; `authorStamps` (capture-live) returns 1 stamp — the same directory, six routes, one answer.
    - Test R2 (CAP-03 side (b) sees the KIT change, zero tokens): the nine path-B notes of Test S1 planted alone → `authorStamps` returns 0 stamps and `capThreePredicate` names side (b) by its no-note reason (the instrument now refuses to count what the reader refuses).
    - Test R3 (the second reader): the compactor's carve-out over a promoted directory that carries one unsealed promoted note reports a `carve-out FAIL` naming that file and the word `unsealed`; a RAW thread record (composed by `composeThreadNote`, never sealed) is not asked for a seal — the thread tier is scratch by contract (WF18), and the byte-equal loop still compares its six fields and nothing more.
    - Test R4 (mutation on the second reader): with the compactor's call to the predicate removed in a scratch copy of the module, R3's FAIL disappears — proving the compactor asks the one predicate rather than inheriting the answer by accident.
  </behavior>
  <action>
    Route the compactor's promoted-tier walk through the one predicate: `readNoteDir` gains a
    tier parameter (or `checkCarveOut` applies `sealVerdict` to every promoted note it read), so an
    unsealed promoted note is a named `carve-out FAIL` (fail closed, WR-01/WR-02's shape), while
    the raw thread tier is exempt BY TIER, stated in the docblock: thread records are the agent's
    local scratch (WF18), never read as admitted notes, and `composeThreadNote` stays unsealed.
    Import `sealVerdict` from `./context-io.js`; compute nothing locally.

    Then DERIVE the planted set: run the full excluded-lane suite once after Task 1's reader
    change and collect every red. Classify each red test: (i) a legitimate plant that must keep
    reading → route it through `appendNote` where the case's premise allows, or seal its bytes
    through the exported `noteSeal` with a one-line comment saying why raw bytes are needed
    (a malformed-line case, a CRLF case, a byte-exact carve-out case); (ii) an adversarial plant
    that should now be refused → assert the `unsealed` arm instead of the old admission; (iii) a
    fixture file → re-seal the two committed board-snapshot notes by composing their exact fields
    and bodies through the writer (or `noteSeal`) and commit the new bytes, quoting the command.
    State the three counts and their sum (the reds) in the summary; the equality is the evidence
    the set was derived, not sampled.

    Tests R1-R4, RED before GREEN for R3 (the compactor admits an unsealed promoted note on the
    base — quote it). `scripts/context-io-writer-set.test.ts`'s derived writer set must not move
    (no new writer was added; `noteSeal`/`sealVerdict` write nothing) — quote its count case.
    Rebuild `scripts/compactor.js` in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/compactor.test.ts scripts/context-io-writer-set.test.ts scripts/board-read.test.ts scripts/trace-render.test.ts scripts/convergence-spine.test.ts scripts/admission-server.test.ts scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
    <automated>grep -a -c 'sealVerdict' scripts/compactor.ts</automated>
    <fails_when>a count of 0 — the compactor's walk does not ask the one predicate</fails_when>
    <automated>grep -a -c 'createHash' scripts/compactor.ts</automated>
    <fails_when>a count above 0 — a second digest site exists beside the one in context-io.ts</fails_when>
    <automated>for f in scripts/fixtures/board-snapshot/.grugops/context/abc-104-implement/notes/*.md; do node -e "const m=require('./scripts/context-io.js');const t=require('fs').readFileSync(process.argv[1],'utf8');const v=m.sealVerdict(t);if(!v.ok){console.log(process.argv[1],v.reason);process.exit(1)}" "$f" || exit 1; done</automated>
    <fails_when>non-zero exit, or any fixture path printed with a reason — a committed fixture note is unsealed</fails_when>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any command, or the vitest summary reports a failed test file</fails_when>
  </verify>
  <acceptance_criteria>
    - Tests R1-R4 exist; R3's RED commit precedes its GREEN commit (shas quoted); R1 lists the six routes with their counts.
    - The summary states the derived red count after Task 1 and the three-way classification whose sum equals it; every re-sealed fixture's command is quoted; the writer-set count case is quoted unchanged.
    - `scripts/compactor.ts` imports `sealVerdict` and computes no digest; the thread tier's exemption is stated in its docblock by tier, not by kind or author.
    - The full excluded-lane suite is green once at the end of the task.
  </acceptance_criteria>
  <done>Six reader routes and the compactor's second walk give one answer about an unsealed note, and every fixture in the tree that used to read by accident now reads by the writer or is refused by name.</done>
</task>

<task type="auto">
  <name>Task 3: The contract, the workflow, the changelog and the ledger hand-off say what the reader now does</name>
  <read_first>
    - `agent-factory/contracts/context-note.md` — the note schema (the fields table; where `id`, `sha`, `gate_run`, `content_hash` were added by Phase 31 is the shape to follow).
    - `agent-factory/workflows/16-context-read-write.md` step 3 ("Record notes ONLY through the sanctioned writer — never author the context path by hand") and its stop conditions.
    - `CHANGELOG.md` — the `Unreleased` block (Keep a Changelog 1.1.0: `Changed` / `Security`).
    - `CLAUDE.md` voice rule: security findings and safety surfaces in clear professional voice; the caveman register stays out of the sentence that explains the refusal.
    - The repository document gates the diff will cross: `npm run check:banned-claims`, `npm run check:public-docs`, `npm run check:claim-anchors`, `npm run check:residual-citations`, `npm run check:diff-disposition`, `npm run check:imperative-lexicon`, `npm run freshness` (adapters/kit refs).
  </read_first>
  <files>agent-factory/contracts/context-note.md, agent-factory/workflows/16-context-read-write.md, CHANGELOG.md</files>
  <action>
    Contract: add the `seal` row to the fields table — written only by the sanctioned writer
    (`context-io.ts`), `sha256:` + 64 hex over the note's bytes without that line, last inside the
    fence; a note whose seal is absent, malformed or mismatched is not returned by any reader and
    is counted under the `unsealed` arm. One clear-voice paragraph states the residual (unkeyed;
    what it does and does not distinguish) so the contract does not overclaim.

    Workflow 16 step 3: one added sentence with the consequence — a note written into
    `.grugops/context/` by any other route (the `Write` tool, a heredoc, an editor) is refused on
    read and never enters the verified context — and the stop-condition bullet that already says
    "the sanctioned writer is `context-io.ts`" stays as it is.

    CHANGELOG `Unreleased`: a `Security` entry naming the reader's refusal of unsealed notes and
    the no-grandfather consequence for stores written before this version (in plain terms: notes
    composed by an earlier kit version are not read by this one until re-admitted through the
    writer); a `Changed` entry for the new `seal` field.

    Run every document gate the diff crosses and quote each `ALL CHECKS PASSED` (or the gate's
    own pass line). Hand off to plan 33-34's ledger close, in the summary, the two rows it will
    append through the tool: (1) the unkeyed-seal residual and the un-forgeable point-of-effect
    tier not taken (human decision); (2) the no-grandfather consequence for existing stores.
  </action>
  <verify>
    <automated>grep -a -c 'seal' agent-factory/contracts/context-note.md</automated>
    <fails_when>a count of 0 — the schema does not document the field the writer now emits</fails_when>
    <automated>grep -a -c 'unsealed' agent-factory/workflows/16-context-read-write.md CHANGELOG.md</automated>
    <fails_when>either file reports 0 — the consequence is not stated where agents and humans read it</fails_when>
    <automated>npm run check:banned-claims &amp;&amp; npm run check:public-docs &amp;&amp; npm run check:claim-anchors &amp;&amp; npm run check:residual-citations &amp;&amp; npm run check:diff-disposition &amp;&amp; npm run check:imperative-lexicon &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit from any gate in the chain</fails_when>
  </verify>
  <acceptance_criteria>
    - The contract's fields table carries a `seal` row with the writer-only rule and the residual paragraph in clear voice.
    - Workflow 16 step 3 states the read-side consequence in one sentence; `CHANGELOG.md`'s `Unreleased` block carries the `Security` and `Changed` entries.
    - Every document gate in the chain exits 0 (quoted); the summary names the two ledger rows plan 33-34 appends.
  </acceptance_criteria>
  <done>What the reader refuses is written where the agents that write notes and the humans that audit them will read it, and the residual is not hidden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any process with a file-writing tool -> `.grugops/context/TASK/notes/` | The shared verified context is the only memory between agents; a note nothing admitted must not become memory |
| the writer's composed bytes -> the reader's verdict | A seal computed in two places drifts; a seal the reader does not ask for on one route is a route around the rule |
| the compactor's promoted-tier walk -> the carve-out verdict | A second reader that admits what the first refuses lets an unsealed note survive compaction |
| the subject under test (CAP-03 side (b)) -> the notes it is scored on | A hand-written note that reads as admitted is a verdict input the subject wrote |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-115 | Spoofing | a hand-written note read as admitted | critical | mitigate | Task 1: `sealVerdict` in the one walk; the nine real path-B notes are the RED fixture; absent/malformed/mismatch each named. |
| T-33-116 | Tampering | a sealed note edited after the write | high | mitigate | Task 1 S3 (a): one changed byte reads `mismatch`; the append-only invariant gains a read-side check. |
| T-33-117 | Repudiation | a second reader route that skips the predicate | high | mitigate | Task 2 R1/R3/R4: six routes plus the compactor's walk, the compactor mutation-proven to ask the predicate. |
| T-33-118 | Spoofing | a reimplemented seal (unkeyed by necessity) | high | accept | Stated in the module, the contract and the ledger: no secret exists that the subject cannot read; the un-forgeable tier is a point-of-effect deny, a human capability decision not taken here (CONTEXT: no new factory capability). |
| T-33-119 | Denial of service | pre-seal stores unreadable after upgrade | medium | accept | No grandfather clause by decision (Task 1 S4); CHANGELOG `Security` entry names the consequence; the writer re-admits. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; `node:crypto` only. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run generate:hook-manifest && npm run check:build-parity && npm run freshness:hook-manifest` — the committed twins and the manifest hash are faithful.
- The document-gate chain exits 0 over the contract, workflow and changelog edits.
</verification>

<success_criteria>
- The nine real hand-written notes of the held capture are refused by name; the writer's notes read; every mutation moves the verdict; the base module's own pre-seal note is refused (no grandfather clause).
- Every reader route, including the compactor's second walk, gives one answer; every planted fixture in the tree is derived and dispositioned with the equality stated.
- The contract, WF16 and the changelog state the rule and its residual in clear voice.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `NOTE_SEAL_KEY`, `noteSeal(unsealedText)`, `sealVerdict(text)` | exported constant + functions, `scripts/context-io.ts` | the one seal computation and the one verdict |
| `seal: sha256:<64 hex>` | note frontmatter line (last inside the fence) | emitted by `composeNote` only |
| `unsealed` in `NOTE_SKIP_ARMS` | reader-owned skip arm | counted by `render` |
| the compactor's promoted-tier seal check | module behavior, `scripts/compactor.ts` | `carve-out FAIL` naming an unsealed promoted note |
| Tests S1-S5, R1-R4 | new test cases | red-first, the held capture's path-B notes as the fixture |
| re-sealed board-snapshot fixture notes (2) | fixture changes | composed through the writer |
| `seal` row in `agent-factory/contracts/context-note.md`; WF16 step-3 sentence; CHANGELOG entries | documentation | clear voice; the residual stated |
| two ledger rows (hand-off) | summary section | appended by plan 33-34 through the tool |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-25-SUMMARY.md` when done
</output>

---
phase: 33-live-capture-windows-portability
plan: 34
type: execute
wave: 23
depends_on: ["33-31", "33-32", "33-33"]
files_modified:
  - .planning/phases/33-live-capture-windows-portability/33-REVIEW-round2.md
  - .planning/WINDOWS.md
  - .planning/phases/33-live-capture-windows-portability/deferred-items.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements: [CAP-01, CAP-02, CAP-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`33-REVIEW.md` (the round-2 code review) is SNAPSHOTTED byte-identical as `33-REVIEW-round2.md` BEFORE any round-3 re-review can overwrite it, because this round's ledger rows cite its ids (CR-01, WR-01..06, IN-01..07) and round 2's rows 237-254 cite `33-REVIEW-round1.md`'s — the snapshot is the first commit of this plan."
    - "Every round-2 review finding is either FIXED by a named round-3 plan (CR-01 → 33-29/33-30; WR-01..04 → 33-30; WR-05 → 33-24; WR-06 → 33-29; IN-01, IN-02 → 33-29; IN-05 → 33-30) or an accepted-open ledger row with its id (IN-03, IN-04, IN-06, IN-07, and IN-03 only if its pin did not move) — never a silent drop; every fold names the plan and task."
    - "The four KIT rows (255-258) are disposed through the tool citing the plan, task and commit that closed each; row 257 names the partial closure (the redirection half fixed, the `$var`/heredoc half dispositioned) and a NEW row carries that residual with its reason; the findings this round surfaced while reading the tree are rows too: the admission matcher that never fired in plugin form (fixed, 33-28), the `_` alphabet widening (decision D-33-R3-02, fixed, 33-28), the unkeyed-seal residual and the point-of-effect tier not taken (open, a human decision), the no-grandfather consequence for pre-seal stores (open, documented)."
    - "Every `deferred-items.md` entry this round touched carries either `status: resolved` with a `Resolved by:` citation naming the plan, task, commit and — for a CAP-02 class — the CI run id from plan 33-31, or a dated `Round 3:` note quoting the evidence for leaving it open; the stale-plugin-rows item quotes plan 33-32's post-run listing verbatim."
    - "REQUIREMENTS.md, STATE.md and the ledgers say exactly what the round's artifacts support: CAP-02 as plan 33-31 wrote it; CAP-01/CAP-03 as the flip commit wrote them or, on hold, Pending with the round-3 evidence, the § 8 hold record and `round 3 of 4` named — and, on hold, the sentence that round 4 is the last under the cap. STATE.md edits are confined to the frontmatter and Current Position; the three same-line beats the wording guard scans are untouched; the guard and the longest-line check run after the write."
  artifacts:
    - .planning/phases/33-live-capture-windows-portability/33-REVIEW-round2.md
    - .planning/WINDOWS.md
    - .planning/phases/33-live-capture-windows-portability/deferred-items.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  key_links:
    - "33-REVIEW.md -> `cp` -> 33-REVIEW-round2.md (byte-identical, `cmp` exit 0) -> the ledger rows' citations resolve after the re-review"
    - "33-REVIEW.md finding ids + the round-3 summaries' hand-off sections -> `gsd-tools windows append|fixed` -> the ship gate sees each"
    - "33-31 Part 4 § 4.3 + 33-32's listing + 33-33's flip/hold -> `Resolved by:` / `Round 3:` lines in deferred-items.md"
    - "STATE.md write -> `node scripts/check-foundation-guards.js` -> the three beats intact"
  prohibitions:
    - "a WINDOWS.md row is never hand-edited"
    - "no finding is dropped without a row or a named fold"
    - "a deferred item is never marked resolved without the evidence its own owner line asked for"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): this plan records the flip or the hold as plan 33-33 left it; it decides nothing about it."
    - "CAP-02 (edge row unresolved, flagged): this plan records plan 33-31's verdict; it decides nothing about it."
    - "CAP-03 (edge row unresolved, flagged): this plan records the round-3 capture's evidence as plan 33-32 left it; it decides nothing about it."
---

<objective>
Gap-closure round 3 of the phase's four-round cap. The round's closing ledger plan: the round-2
review is snapshotted before the round-3 re-review can overwrite the file the ledger cites; every
review finding is fixed-by-name or an accepted-open row; the four KIT rows are disposed with
their closing commits and the residuals this round surfaced become rows of their own; every
deferred item is resolved with a citation or honestly left open; and the three current-state
ledgers say what the artifacts support — including, on a hold, that round 4 is the last under
the cap.

Gap mapping (round-3 closure target 10): "WINDOWS.md rows appended/flipped through the tool;
deferred-items citations; REQUIREMENTS.md status rows; `33-REVIEW.md` must be SNAPSHOTTED (e.g.
`33-REVIEW-round2.md`) before any round-3 re-review overwrites it, because ledger rows cite its
IDs" → Task 1 (snapshot + rows), Task 2 (deferred items), Task 3 (requirements + state).

Decisions honored: D-11 (a red is recorded and the next round owns the fix — the rows are how it
is handed over), D-20 (a hold is recorded, not softened), the WINDOWS.md tool rule (D-17's
ledger-row mechanics).

Purpose: the round ends with nothing silently dropped, every citation resolvable after the
re-review, and every ledger truthful.
Output: the review snapshot, the ledger rows, the deferred-items resolutions, the requirement
rows and the state position.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-REVIEW.md
@.planning/phases/33-live-capture-windows-portability/33-23-PLAN.md
@.planning/phases/33-live-capture-windows-portability/33-23-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/deferred-items.md
@.planning/phases/33-live-capture-windows-portability/33-31-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-32-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-33-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot the round-2 review first; then every finding fixed-by-name or a row, the four KIT rows disposed, the round's new findings as rows — all through the tool</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` — every finding id (CR-01, WR-01..WR-06, IN-01..IN-07) with its file and line; `33-REVIEW-round1.md` beside it (the round-1 snapshot, the precedent: commit `a4b149f4`).
    - The round-3 summaries' hand-off sections: 33-24 (WR-05 closed; decision D-33-R3-01), 33-25 (two rows: the unkeyed-seal residual; the no-grandfather consequence), 33-26 (§ 3 closed), 33-27 (§ 2 redirection half closed; the `$var`/heredoc disposition), 33-28 (two rows: the matcher finding, fixed; decision D-33-R3-02, fixed), 33-29 (CR-01 items 1-2, WR-06, IN-01, IN-02), 33-30 (WR-01..04, IN-05, CR-01 item 3).
    - `.planning/WINDOWS.md` header and rows 255-258; `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 3.4 — the exact `windows append` and `windows fixed` command shapes, and the `windows status` check afterwards.
    - `.planning/phases/33-live-capture-windows-portability/33-23-SUMMARY.md` — how round 2's 22 rows were appended and how the folds were named.
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-REVIEW-round2.md, .planning/WINDOWS.md</files>
  <action>
    FIRST COMMIT, before anything else in this plan: `cp` `33-REVIEW.md` to `33-REVIEW-round2.md`
    in the phase directory (byte-identical; `cmp` the two), commit it alone with a message naming
    the reason (the round-3 re-review overwrites `33-REVIEW.md`; the ledger cites round-2 ids).

    Then the rows, every one through `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows ...`, every
    command quoted in the summary:
    - Dispose rows 255-258 with `windows fixed <id> "<reason>"`: 255 → plan 33-28 (the `admit`
      token, the regenerated adapter, the commit sha; the `UNKNOWN - verify` about the session's
      tool list settled by 33-32's B-path frame — quote the answer); 256 → plan 33-25 (the seal,
      the `unsealed` arm, the commit sha, the nine path-B notes refused); 257 → plan 33-27 (the
      redirection grammar and the deny text, the commit sha; state in the reason that the
      `$var`/heredoc half is dispositioned in the new row below); 258 → plan 33-26 (the scalar
      guard on both composers, the commit sha).
    - Append one row per round-2 review finding that is accepted open (`--kind deviation`, the
      finding's file and line, `33-REVIEW-round2 <id> accepted open (round 3 of 4): <issue> — <fix in one clause>; owner: a later round`):
      IN-04, IN-06, IN-07, and IN-03 unless plan 33-30 replaced the pin with the relationship
      (then IN-03 is a named fold). Name every fixed finding as folded, with plan and task, in
      the summary: CR-01 (33-29 T1/T2, 33-30 T1), WR-01 (33-30 T3), WR-02 (33-30 T1), WR-03
      (33-30 T1), WR-04 (33-30 T2), WR-05 (33-24 T1), WR-06 (33-29 T3), IN-01 (33-29 T1), IN-02
      (33-29 T1), IN-05 (33-30 T2).
    - Append the round's NEW findings, then dispose the fixed ones through the tool in the same
      task: (a) `hooks/hooks.json` admission matcher never fired in plugin form (platform sentence
      + A:289-293 as evidence) — append, then `windows fixed` citing 33-28 T3; (b) the `_` alphabet
      widening (decision D-33-R3-02) — append, then `windows fixed` citing 33-28 T1; (c) the
      unkeyed-seal residual and the point-of-effect deny of file-writing tools under the context
      root NOT taken (a kit capability decision for the human) — append `--kind unrun-verify`,
      left open, owner: the human; (d) the no-grandfather consequence for stores written before
      the seal (documented in CHANGELOG `Security`) — append, left open as a documented
      consequence, owner: a later release note; (e) the `$var` / heredoc arms kept opaque
      (availability residual, the mapper's loop at B:562) — append, left open, owner: a later
      round.
    Derive the count: `windows status` before and after; state `open_count` and `fixed_count`
    before, the number appended, the number fixed, and after — with the arithmetic — and confirm
    the table row count and the JSON appendix agree.
  </action>
  <verify>
    <automated>cmp .planning/phases/33-live-capture-windows-portability/33-REVIEW.md .planning/phases/33-live-capture-windows-portability/33-REVIEW-round2.md</automated>
    <fails_when>non-zero exit — the snapshot is not byte-identical to the round-2 review (or one file is missing)</fails_when>
    <automated>M=$(git log --format=%s -1 -- .planning/phases/33-live-capture-windows-portability/33-REVIEW-round2.md) &amp;&amp; test -n "$M" &amp;&amp; printf '%s\n' "$M" | grep -a -c 'snapshot'</automated>
    <fails_when>a count of 0 — the snapshot was not committed on its own with a message naming it</fails_when>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status</automated>
    <fails_when>non-zero exit, or the counters disagree with the table's row count</fails_when>
    <automated>grep -a -c '33-REVIEW-round2 IN-' .planning/WINDOWS.md</automated>
    <fails_when>a count below 6 — at least three accepted-open IN rows (IN-04, IN-06, IN-07) appear once in the table and once in the JSON appendix; fewer means a row is missing from one representation</fails_when>
    <automated>node -e "const t=require('fs').readFileSync('.planning/WINDOWS.md','utf8');for(const id of [255,256,257,258]){const re=new RegExp('^\\\\| '+id+' \\\\| .*\\\\| fixed \\\\|','m');if(!re.test(t)){console.log('row',id,'not fixed');process.exit(1)}}"</automated>
    <fails_when>non-zero exit — a KIT row of 255-258 is not in the fixed state in the table (printed)</fails_when>
  </verify>
  <acceptance_criteria>
    - `33-REVIEW-round2.md` exists, byte-identical to `33-REVIEW.md`, committed first and alone.
    - Rows 255-258 are `fixed` through the tool with the plan, task and commit in each reason; the accepted-open IN rows and the five new-finding rows exist, the two fixed ones disposed through the tool; every command quoted; the counter arithmetic stated and reconciled.
    - Every fixed round-2 finding is named as folded with plan and task in the summary; no id from the round-2 review is absent from both the fold list and the ledger.
  </acceptance_criteria>
  <done>Nothing from the review, the diagnosis, or this round's own reading of the tree is dropped in silence; every citation survives the re-review.</done>
</task>

<task type="auto">
  <name>Task 2: Deferred items resolved with citations, or honestly left open with the round-3 evidence</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/deferred-items.md` — the three `status: open` entries (the ten context-io reds → row 236's class; the stale `0.1.0` plugin rows; GAP-D1 HELD) and their `Owner:` lines.
    - `.planning/phases/33-live-capture-windows-portability/33-31-SUMMARY.md` and 33-CI-MEASUREMENT.md Part 4 § 4.3 — the CI verdict and, on MET, the `CLOSED on run <id>` column; on NOT MET, the re-derived inventory.
    - `.planning/phases/33-live-capture-windows-portability/33-32-SUMMARY.md` — the post-run `claude plugin list` grugops rows quoted verbatim (or the no-go record); `33-33-SUMMARY.md` — flip or hold (§ 8).
    - `.planning/phases/33-live-capture-windows-portability/33-23-SUMMARY.md` § how the `status: resolved` / `Resolved by:` and the `Round 2:` lines were written (the shape).
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/deferred-items.md</files>
  <action>
    For each of the three open entries: the ten-context-io-reds entry → `status: resolved` with a
    `Resolved by:` line naming plan 33-24 Task 1 (decision D-33-R3-01), its commit sha, and the
    run id whose windows leg read `success` (plan 33-31) — ONLY if plan 33-31 read MET; otherwise
    a dated `Round 3:` line quoting the re-derived inventory row(s) for the class and stating
    that round 4 is the last under the cap. The stale-plugin-rows entry → quote plan 33-32's
    post-run listing verbatim; resolve it if the two `0.1.0` rows are gone or if the human removed
    them (say which), else leave open with the quotation and the owner unchanged; if no run
    happened (`no-go`), a dated `Round 3:` line says so. The GAP-D1 HELD entry → resolved by plan
    33-33's flip commit (sha quoted) with the two amendments named as paid, or a dated `Round 3:`
    line pointing at the manifest's § 8 with the outcome word, the CI verdict and the last-round
    sentence.

    Derive the count: the number of `status: open` lines before and after, and the number of
    `Resolved by:` lines added, stated in the summary with the equality (before − resolved =
    after).
  </action>
  <verify>
    <automated>grep -a -B1 -A1 'status: resolved' .planning/phases/33-live-capture-windows-portability/deferred-items.md | grep -a -c 'Resolved by:'</automated>
    <fails_when>a count lower than the number of `status: resolved` lines in the file — a resolved line without a citation</fails_when>
    <automated>grep -a -c 'Round 3' .planning/phases/33-live-capture-windows-portability/deferred-items.md</automated>
    <fails_when>a count of 0 while any entry is still `status: open` — an open entry carries no round-3 note</fails_when>
    <automated>for s in $(grep -a -oE 'commit `[0-9a-f]{7,40}`' .planning/phases/33-live-capture-windows-portability/deferred-items.md | grep -a -oE '[0-9a-f]{7,40}' | sort -u); do git cat-file -t "$s" >/dev/null 2>&amp;1 || { echo "unresolvable $s"; exit 1; }; done</automated>
    <fails_when>non-zero exit, or an `unresolvable` line — a cited commit sha does not exist in this repository</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the three entries is either `status: resolved` with a `Resolved by:` citation carrying the plan, task, commit and (for the CI class) run id, or `open` with a dated `Round 3:` note quoting the evidence and, on hold, the last-round sentence.
    - The summary states the before/after open counts and the equality; every cited commit resolves and the run id matches Part 4 § 4.2.
  </acceptance_criteria>
  <done>The deferred list is a truthful record of what this round closed and what it handed to the last round.</done>
</task>

<task type="auto">
  <name>Task 3: The requirement rows and the project position agree with the artifacts</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` rows CAP-01, CAP-02, CAP-03 (L140-142 checkboxes; L226-228 coverage table) as plan 33-31 and the flip commit (33-33) left them.
    - `.planning/STATE.md` — the frontmatter and `## Current Position` (~L31-35) ONLY; the three same-line beats the `oracleWr05Wording` guard scans (`dropped`+`Phase 8`, `guard_wr05`+`Phase 10`, `re-verified GREEN`+`Phase 11`) and the escape-doubling hazard (the state writer re-escapes backslashes; check the longest line after every write).
    - `.planning/phases/33-live-capture-windows-portability/33-33-SUMMARY.md` — flip or hold; `33-32-SUMMARY.md` — the outcome word, costs, durations, both provenance states, the grant rows; `33-31-SUMMARY.md` — the CI verdict.
    - The decisions this round recorded (D-33-R3-01, D-33-R3-02) — confirm they are in STATE.md's decision log (the executing plans add them; this task only confirms and adds the round summary line).
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    REQUIREMENTS.md: CAP-02 stays as plan 33-31 wrote it (do not touch). On the flip branch,
    CAP-01 and CAP-03 were flipped inside the flip commit (F44, F45) — verify by reading and do
    not touch. On the hold branch, update the two coverage-table rows' `Pending` text to name the
    round-3 evidence: the capture date, the outcome word, the parity section's named diffs (or
    the `no-go`), both provenance states, the hold record (`33-FLIP-MANIFEST.md` § 8), the round
    (3 of 4) and the sentence that round 4 is the last under the cap; CAP-03's row also states
    that the round-3 instrument scores only what it received on the pipe and what it derived
    before the spawn (33-29, 33-30) and that the observation stands as evidence pending the flip.
    Capture-backed and run-backed citations are never mixed.

    STATE.md: edit ONLY the frontmatter and `## Current Position` — the phase line becomes
    `Phase: 33 (...) — gap-closure round 3 of 4 EXECUTED <date>, awaiting verification` and the
    plan line names 33-24..33-34, the CI verdict (run id, both conclusions), the capture outcome
    word (or no-go), the flip/hold, and — on hold — that round 4 is the last under the cap. Keep
    every line short (no path-heavy prose); after the write run the foundation guards and measure
    the longest line.
  </action>
  <verify>
    <automated>node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit, or any FAIL line naming the state wording oracle</fails_when>
    <automated>awk '{ if (length($0) > m) m = length($0) } END { print m }' .planning/STATE.md</automated>
    <fails_when>a value above 4000</fails_when>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs query state.load</automated>
    <fails_when>non-zero exit, or a parse error reported for the state document</fails_when>
    <automated>S=$(git log -1 --format=%H -- .planning/STATE.md) &amp;&amp; test -n "$S" &amp;&amp; git show --format= "$S" -- .planning/STATE.md > "${TMPDIR:-/tmp}/state-33-34.diff" &amp;&amp; test -s "${TMPDIR:-/tmp}/state-33-34.diff" &amp;&amp; test "$(grep -a -cE '^[-+].*(guard_wr05.*Phase 10|re-verified GREEN.*Phase 11|dropped.*Phase 8)' "${TMPDIR:-/tmp}/state-33-34.diff" | tr -d ' ')" = "0"</automated>
    <fails_when>non-zero exit — the last commit touching STATE.md (this task's write) cannot be resolved, produced an empty diff, or changed a guarded beat line</fails_when>
    <automated>grep -a -c 'round 3 of 4' .planning/STATE.md</automated>
    <fails_when>a count of 0 — the position does not name the round</fails_when>
  </verify>
  <acceptance_criteria>
    - CAP-02's rows are byte-unchanged from plan 33-31's write; CAP-01/CAP-03 rows are as the flip commit wrote them or carry the round-3 hold text with the evidence and the last-round sentence.
    - STATE.md's diff touches only the frontmatter and Current Position; the guards exit 0; the longest line is below the ceiling; the loader parses it; the two round-3 decisions are present in the log.
    - The summary quotes the three requirement rows as they read at the end of the round.
  </acceptance_criteria>
  <done>The project's current-state ledgers say exactly what round 3 produced, and the next step — verification, then either the phase's close or the last round — starts from a truthful position.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the round-3 re-review -> `33-REVIEW.md` | An overwrite before the snapshot dangles every ledger citation of a round-2 id |
| a review finding -> the ledger | A finding without a row is a silent drop the ship gate never sees |
| a deferred item -> a resolved line | A resolved line without evidence is a fabricated closure |
| the state document -> the wording guard | A touched beat or a pathological line turns a sub-second guard into minutes or a false red |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-157 | Repudiation | dangling round-2 review citations after the re-review | high | mitigate | Task 1: the snapshot is the first, separate commit; `cmp` asserted. |
| T-33-158 | Repudiation | silently dropped review findings or residuals | high | mitigate | Task 1: one row per accepted-open id and per new finding, folds named, counter arithmetic stated. |
| T-33-159 | Repudiation | a resolved deferred item without evidence | high | mitigate | Task 2: every `Resolved by:` names plan, task, commit and run id; cited shas resolved with `git cat-file`. |
| T-33-160 | Denial of service | STATE.md guard beats / longest line | medium | mitigate | Task 3: frontmatter + Current Position only; guard and length check after the write; the diff grep over the beats. |
| T-33-161 | Tampering | the ledger's three representations | medium | mitigate | Rows only through the tool; `windows status` asserted. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; ledgers only. |
</threat_model>

<verification>
- `cmp 33-REVIEW.md 33-REVIEW-round2.md` — exit 0; the snapshot commit precedes every other commit of this plan.
- `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status` — counters agree; the arithmetic across Task 1 stated.
- `node scripts/check-foundation-guards.js` and the STATE.md longest-line check — after every STATE.md write.
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the tree is still green at the round's close.
</verification>

<success_criteria>
- The round-2 review is snapshotted before any re-review; every review finding is fixed-by-name or a row; the four KIT rows and the round's new findings are on the ledger through the tool.
- Every deferred item is resolved with a citation or left open with the round-3 evidence; the requirement rows and the state position are truthful, and — on hold — say that one round remains.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `33-REVIEW-round2.md` | snapshot | byte-identical to the round-2 `33-REVIEW.md`, committed first |
| WINDOWS.md rows 255-258 `fixed` | ledger changes via tool | plan, task, commit per reason |
| WINDOWS.md rows for IN-04, IN-06, IN-07 (and IN-03 if not folded) | ledger rows via tool | accepted open, `33-REVIEW-round2 <id>` in the description |
| WINDOWS.md rows for the matcher finding (fixed), the `_` widening (fixed), the unkeyed-seal residual (open), the pre-seal consequence (open), the `$var`/heredoc disposition (open) | ledger rows via tool | the round's own findings, none dropped |
| `Resolved by:` / `Round 3:` lines in deferred-items.md | record changes | plan, task, commit, run id |
| CAP-01/CAP-03 hold text (hold branch) | ledger change | round-3 evidence named; last-round sentence |
| STATE.md Current Position | state change | round 3 of 4 executed |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-34-SUMMARY.md` when done
</output>

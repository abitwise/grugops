---
phase: 33-live-capture-windows-portability
plan: 24
type: execute
wave: 13
depends_on: ["33-16", "33-20"]
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
autonomous: true
gap_closure: true
requirements: [CAP-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Which spelling wins on win32 is DECIDED BY EVIDENCE already on disk and recorded as a decision before any line changes: rung 1 (`realpathSync.native`, the kernel's final path) is the module's one published spelling, because it is the only rung that is a fixed point over the other's output (the rung-identity property the test already asserts), because tier 0 already publishes it, and because plan 33-15's W-23..W-25 derived through it read green on run 35579263776 (33-CI-MEASUREMENT.md Part 3 § 3.3)."
    - "Every side of every comparison in the WR-15 block (`scripts/context-io.test.ts` :6456-:8605) is spelled by the module's own exported authority `canonicalWorkingDirectory` — the fixture helper derives through it; no per-case `.native` call, no platform conditional (D-15 'normalize once, in the module that publishes'; D-14/D-16)."
    - "The THREE module-side spellers of one directory that 33-REVIEW WR-05 names — the cwd tier (already rung 1), the home boundary (rung 2 at :4889) and the env tier (bare `resolve` at :5308) — all spell through `canonicalDirectoryPath`; the set of rung-2 / bare-resolve sites in the module and the test file is derived by grep, counted before and after, and the count is asserted in the plan's verify block, so no sibling arm is left for the CI leg to move onto."
    - "The 31-15 monotonicity mirror's two anchored lines are byte-untouched; the three MONOTONICITY cases and every consumer of `trustedRepoRoot` (the guard, the admission guard, the admission server, the floor-invariance suite) are run after the change and quoted."
    - "What this host cannot measure is said so, in the 33-20 wording: the 8.3 axis is `unmeasured locally, by construction`; plan 33-31's pushed run is the measurement, and the 35 titles of Part 3 § 3.3 are the rows it will decide."
  artifacts:
    - scripts/context-io.ts
    - scripts/context-io.js
    - scripts/context-io.test.ts
  key_links:
    - "context-io.test.ts `tmp15(prefix)` -> `canonicalWorkingDirectory(freshTmp(prefix))` (the module's export) -> every WR-15 comparison's expected side"
    - "context-io.ts `homeBoundary` spellings set -> `canonicalDirectoryPath(named)` -> `abovePaths`/`selfPaths` -> the home stop"
    - "context-io.ts env tier -> `canonicalDirectoryPath(fromEnv.trim())` -> the published trusted root"
    - "context-io.js rebuild -> `npm run generate:hook-manifest` -> hooks/hook-entry.ts module hash -> `npm run freshness:hook-manifest`"
  prohibitions:
    - "no platform conditional closes a CAP-02 red (D-14/D-16)"
    - "no per-fixture realpath call stands where the module's exported authority can be asked (D-15)"
    - "the 31-15 monotonicity mirror's anchored lines are never reformatted or moved"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): untouched by this plan."
    - "CAP-02 (edge row unresolved, flagged): this plan closes row 236's class at one authority on every arm WR-05 names; the leg's conclusion is measured only by plan 33-31's pushed run."
    - "CAP-03 (edge row unresolved, flagged): untouched by this plan."
    - "assumption_delta = promote: rung 1 is PROMOTED to the module's sole published spelling of a directory; rung 2 stays a fallback rung INSIDE the ladder for a platform that exposes no native variant and is never a published spelling on its own. Recorded in the summary and in STATE.md as a decision, not added alongside."
---

<objective>
Gap-closure round 3 of the phase's four-round cap (rounds 1 and 2 executed and verified
`gaps_found` 0/3). This is the round's TRACER: CAP-02's entire remaining gap is one class
(WINDOWS.md row 236 — 35 reds in one `describe` block, one mechanism: 33-16 routed the walk's
start through rung 1 `realpathSync.native`, which expands 8.3 short names on win32, while the
block's fixture helper `tmp15()` stayed on rung 2 `realpathSync`, which preserves them). The fix is
ONE spelling authority on BOTH sides, and on every sibling arm 33-REVIEW WR-05 names in the same
module (the home boundary at rung 2, the env tier at bare `resolve`) — not the one arm the CI red
happened to hit. This project's memory names the shape to avoid: "fix closed the arm it aimed at,
the case moved to the sibling arm" (P27/P29/P31/P32 and 33-16 itself).

Gap mapping (33-VERIFICATION.md CAP-02 `missing[0]`): "One authority for the directory's
spelling on both sides of the WR-15 block's comparisons (D-15's 'normalize once' shape) ... Which
spelling SHOULD win on win32 is explicitly recorded UNKNOWN - verify in 33-CI-MEASUREMENT.md
Part 3 § 3.3 — round 3's first decision." → Task 1 decides it by evidence and lands it; Task 2
probes every consumer arm and rebuilds the committed twin.

Decisions honored: D-13 (both legs; measured by 33-31, never here), D-14/D-16 (no platform
conditional; a red is a measurement), D-15 (a location the module publishes is canonicalised
once, in the module; tests derive from the module's published spelling), D-11 (fix offline with
proof; the next pushed run measures it).

Purpose: make the third pushed run decide something already true on every arm this host can
reach, and name the one axis it cannot.
Output: `scripts/context-io.ts` + committed `.js` (+ the hook manifest hash that follows it),
`scripts/context-io.test.ts` with one fixture authority and the extended Test W.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md
@.planning/phases/33-live-capture-windows-portability/33-REVIEW.md
@.planning/phases/33-live-capture-windows-portability/33-16-PLAN.md
@.planning/phases/33-live-capture-windows-portability/33-16-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-15-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One spelling authority — the decision by evidence, then the fixture helper and the module's two remaining spellers derive through `canonicalDirectoryPath`</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` Part 3 § 3.3 — the 35-row table (every case title and its assertion text), the "mechanism, read from the tree under test" paragraph, and the `UNKNOWN - verify` sentence about which spelling should win.
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` WR-05 — the two further sites (:4889 home boundary, :5308 env tier) and the `CLAUDE_PROJECT_DIR=<link>/proj` darwin-visible case.
    - `scripts/context-io.ts` — `canonicalDirectoryPath` (:4650, the three-rung ladder and its docblock), `canonicalWorkingDirectory` (:4694), `homeBoundary` (:4882-4935; the `spellings` set at :4889 and the `degenerate` guard at :4917-4923), `trustedRepoRoot` (:5296-5330: the env loop at :5306-5309, the cwd read at :5317, the ANCHORED comment and the anchored `discovered` line), `hostDeliveredRoot` (:5176), `TRUSTED_ROOT_TIERS` / `TRUSTED_ROOT_ENV_ORDER` (:4745) and any published tier text that describes the env tier's mechanism.
    - `scripts/context-io.test.ts` — `freshTmp` (:67), `tmp15` (:6226-6228 and its docblock), the WR-15 block (:6456-:8605), the MONOTONICITY mirror's `preFixKit` anchors (:6572-6607), the rung-identity property at :11605-11616 (the evidence for the decision), Test W (33-16, ~:11647-11710), the home-comparison sites at :16345-16355.
    - `agent-factory/workflows/16-context-read-write.md` § the resolution order — the tier text quoted from `TRUSTED_ROOT_TIERS`; the test asserts the two equal, so a mechanism sentence changed in one must change in both.
  </read_first>
  <files>scripts/context-io.ts, scripts/context-io.test.ts</files>
  <behavior>
    - Decision premise (asserted, not assumed): for a fresh temp directory `r`, `canonicalWorkingDirectory(r)` equals `realpathSync.native(r)` and `canonicalWorkingDirectory(realpathSync(r))` equals the same value — rung 1 is a fixed point over rung 2's output (the :11615 property, now stated about the exported authority).
    - Test W-ENV (RED first, drivable on darwin): with `CLAUDE_PROJECT_DIR` set to a project addressed through a directory symlink (`LINK/proj` where `LINK` is a symlink to `REAL`, staged through `stageSymlinkOrSkip`), `trustedRepoRoot` driven in a child answers `canonicalWorkingDirectory(join(REAL, "proj"))`. Today it answers the link spelling (bare `resolve`), so the case is red on the dispatch base — quote the red run.
    - Test W-HOME (premise + property; the 8.3 half is unmeasured locally, by construction): with `HOME`/`USERPROFILE` planted to a home addressed through a directory symlink, the walk's home stop still fires at the canonical spelling (a project directly under the link-spelled home resolves to itself; an ancestor above the home is never adopted). On darwin rung 2 already resolves links, so this case is green before and after — say so in its docblock; the win32 8.3 axis is decided by plan 33-31's run.
    - Fixture authority: `tmp15(prefix)` returns `canonicalWorkingDirectory(freshTmp(prefix))`; a case in the WR-15 block that compares the walk's root to a `tmp15` fixture now compares two values spelled by ONE exported function.
  </behavior>
  <action>
    FIRST, the decision, written into the summary and STATE.md before any edit (D-11: a fix lands
    with its reason): the published spelling of a directory on every host is rung 1 of
    `canonicalDirectoryPath` (`realpathSync.native`, the kernel's final path — long names on
    win32). Evidence, all on disk: (1) the rung-identity property at test :11615 already proves
    rung 1 is idempotent over rung 2's output while the converse is disproved by the 8.3 class;
    (2) tier 0 (`hostDeliveredRoot`) already publishes rung 1 and 33-16 put the cwd tier on it;
    (3) 33-15 derived W-23..W-25 through rung 1 and all three read green on run 35579263776. The
    alternative — defining rung 2 as the published spelling — would put the module's cwd tier and
    tier 0 back onto a spelling the kernel does not answer, re-opening 33-16's W-21. Record it as
    decision D-33-R3-01 with `assumption_delta = promote`.

    THEN, one authority on every arm. Derive the set before touching anything and put the numbers
    in the summary: the rung-2 sites in `scripts/context-io.ts` outside comment lines (expected 2:
    the ladder's own rung 2 inside `canonicalDirectoryPath`, and the home-boundary spelling at
    :4889), the bare-resolve env tier (expected 1, at :5308), and the rung-2 sites in
    `scripts/context-io.test.ts` outside comment lines (expected 6: the fixture helper at :6227,
    the rung-identity property at :11615, and the four home comparisons at :16345, :16348, :16352,
    :16355). Then:

    - `scripts/context-io.test.ts` `tmp15`: derive through the module's EXPORTED
      `canonicalWorkingDirectory` (import it beside the block's other module imports), not
      through any `realpathSync` variant called in the test. Rewrite the helper's docblock: the
      reason is one authority for both sides of every comparison (D-15), and the darwin `/var`
      symlink sentence stays as the second reason.
    - `scripts/context-io.ts` `homeBoundary`: the second spelling added to `spellings` is
      `canonicalDirectoryPath(named)`; `named` itself stays in the set as the raw spelling; the
      `try/catch` stays (a home that cannot be canonicalised keeps its raw spelling and the
      identity sets). Extend the comment above it: the home's canonical spelling must be the one
      the walk climbs through, or `abovePaths`/`selfPaths` miss on a host whose home carries an 8.3
      component (WR-05 (a)), and the `degenerate` guard can drop the identity sets that would
      otherwise catch it.
    - `scripts/context-io.ts` env tier: the value returned for a non-empty
      `TRUSTED_ROOT_ENV_ORDER` variable is `canonicalDirectoryPath(fromEnv.trim())` — the same
      ladder, so a link-spelled or 8.3-spelled `CLAUDE_PROJECT_DIR` names the directory the cwd
      tier would name (WR-05 (b)). The loop header line `for (const name of TRUSTED_ROOT_ENV_ORDER)`
      and the anchored `discovered` line stay BYTE-IDENTICAL (the mirror asserts their occurrence
      counts). If `TRUSTED_ROOT_TIERS` or the WF16 quotation describes the env tier's value as
      taken as given, update both to say it is canonicalised through the one ladder — the
      equality test between them must stay green.
    - The four home comparisons at :16345-16355 compare two sides both spelled by rung 2 — not the
      row-236 class, but a second spelling authority in the same file. Route both sides through
      `canonicalWorkingDirectory` so the file has ONE authority; the rung-identity property at
      :11615 is the ONLY remaining direct rung-2 call in the test file, because it is the evidence
      for the decision above (say so in a one-line comment beside it).
    - Tests W-ENV and W-HOME beside 33-16's Test W, RED committed before GREEN for W-ENV (quote the
      red assertion), premise-first for W-HOME.

    Probe every arm that consumes the changed values before calling the task done: every case in
    the WR-15 block that plants a `TRUSTED_ROOT_ENV_ORDER` variable (its expectation now derives
    through the authority — on darwin a raw `/var/...` plant answers `/private/var/...`), the
    near/far step-limit cases, the CR-13 home-rooted cases (:7746-:8605), the three MONOTONICITY
    cases (:6609, :7392, :8365), and the tier-0 cases (:11522-11640). List each with its vitest
    result line in the summary. No `process.platform` read is added anywhere.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts</automated>
    <fails_when>non-zero exit, or any failed test in the summary line, or the titles W-ENV / W-HOME are absent from the run</fails_when>
    <automated>grep -a -c 'resolve(fromEnv' scripts/context-io.ts</automated>
    <fails_when>a count other than 0 — the env tier still publishes a bare lexical spelling</fails_when>
    <automated>grep -a -v '^\s*//' scripts/context-io.ts | grep -a -v '^\s*\*' | grep -a -c 'realpathSync('</automated>
    <fails_when>a count other than 1 — only the ladder's own rung 2 inside canonicalDirectoryPath may call the portable resolver; a second site is a second authority</fails_when>
    <automated>grep -a -v '^\s*//' scripts/context-io.test.ts | grep -a -v '^\s*\*' | grep -a -c 'realpathSync('</automated>
    <fails_when>a count other than 1 — only the rung-identity property (the decision's evidence) may call the portable resolver directly in the test file</fails_when>
    <automated>grep -a -c 'const discovered = cwd === null ? null : projectRootFromWorkingDirectory(cwd);' scripts/context-io.ts</automated>
    <fails_when>a count other than 1 — the anchored line moved or was duplicated</fails_when>
    <automated>grep -a -c 'for (const name of TRUSTED_ROOT_ENV_ORDER)' scripts/context-io.ts</automated>
    <fails_when>a count other than 1 — the mirror's loop anchor moved or was duplicated</fails_when>
    <automated>D=$(git diff 9e1c1131cec8943e2ac96233ed7e624720ced14b..HEAD -- scripts/context-io.ts scripts/context-io.test.ts) || exit 1; printf '%s\n' "$D" | grep -a -c '^+.*process\.platform'</automated>
    <fails_when>non-zero exit from the diff, or a count other than 0 — a platform conditional was added since the last pushed sha (D-14/D-16)</fails_when>
  </verify>
  <acceptance_criteria>
    - The summary carries decision D-33-R3-01 with the three evidence items and `assumption_delta = promote`, written before the first code edit (the RED commit's message or the summary's first section records the order).
    - `tmp15` in `scripts/context-io.test.ts` calls the module's exported `canonicalWorkingDirectory` and contains no call to any `realpathSync` variant; the block's docblock states the one-authority reason.
    - `homeBoundary`'s second spelling and the env tier's return value both go through `canonicalDirectoryPath`; the two mirror anchors are byte-identical to the last pushed sha (`git diff 9e1c1131cec8943e2ac96233ed7e624720ced14b..HEAD -- scripts/context-io.ts` shows no hunk touching either line; quoted).
    - Tests W-ENV and W-HOME exist; W-ENV's RED commit precedes its GREEN commit (both shas quoted with the red assertion text); W-HOME's docblock states which half this host measures and which half plan 33-31's run decides.
    - The before/after counts of rung-2 and bare-resolve sites in both files are stated in the summary and match the verify block's expectations.
    - Every probed arm is listed with its vitest result line; the `TRUSTED_ROOT_TIERS` / WF16 equality test is green.
  </acceptance_criteria>
  <done>One directory has one spelling in this module and in the block that tests it, on every arm the review named — and the one axis darwin cannot measure is named as such for the pushed run to decide.</done>
</task>

<task type="auto">
  <name>Task 2: Every consumer of the trusted root is re-run, the committed twin and the hook manifest follow the module, and the pre-push rows are named for plan 33-31</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-16-SUMMARY.md` — the note that rebuilding `scripts/context-io.js` always requires `npm run generate:hook-manifest` (the module hash in `hooks/hook-entry.ts`).
    - `hooks/hook-entry.ts` — the manifest lines carrying `sha256(scripts/context-io.js)`.
    - `scripts/floor-invariance.test.ts`, `hooks/guard.test.ts`, `hooks/admission-guard.test.ts`, `scripts/context-io-writer-set.test.ts`, `scripts/admission-server.test.ts` (if present) — the consumers of `trustedRepoRoot` and of the module hash.
    - `.planning/phases/33-live-capture-windows-portability/33-20-PLAN.md` Task 1 — the pre-push inventory shape plan 33-31 repeats; this task hands it the rows.
  </read_first>
  <files>scripts/context-io.js, hooks/hook-entry.ts, hooks/hook-entry.js</files>
  <action>
    Rebuild and re-derive the manifest in this order: `npm run build`, then
    `npm run generate:hook-manifest` (which rebuilds again and rewrites the module hash), then
    `npm run check:build-parity` and `npm run freshness:hook-manifest`. Every changed `.js` and the
    two hook-entry files land in the same commit as the `.ts` change they follow (CLAUDE.md:
    committed output never drifts from its source).

    Run the consumers and quote each summary line: `hooks/guard.test.ts`,
    `hooks/admission-guard.test.ts`, `scripts/floor-invariance.test.ts`,
    `scripts/context-io-writer-set.test.ts`, `scripts/board-read.test.ts`, and any test file
    `grep -l trustedRepoRoot scripts/*.test.ts hooks/*.test.ts` lists that is not already named.
    Then the post-wave gate over the whole excluded-lane suite once, and
    `node scripts/check-foundation-guards.js` (context-io.ts is a tripwire module).

    Write the hand-off for plan 33-31 into the summary as a table: the 35 case titles of
    33-CI-MEASUREMENT.md Part 3 § 3.3 (N-1..N-34, W-20, W-21), each with this host's result from
    running the title (`npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts -t "<title>"`)
    and the column `unmeasured locally, by construction` for the 8.3 axis on every row — darwin
    has no short names, so a green here is not the measurement; the pushed run is.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run generate:hook-manifest &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness:hook-manifest &amp;&amp; npx tsc --noEmit</automated>
    <fails_when>non-zero exit from any command, or the parity check names scripts/context-io.js or hooks/hook-entry.js as moved by the build</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' hooks/guard.test.ts hooks/admission-guard.test.ts scripts/floor-invariance.test.ts scripts/context-io-writer-set.test.ts</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any command, or the vitest summary reports a failed test file</fails_when>
    <automated>node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit, or any FAIL line</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/context-io.js`, `hooks/hook-entry.ts` and `hooks/hook-entry.js` are committed with the `.ts` change; `npm run freshness:hook-manifest` and `npm run check:build-parity` exit 0 (quoted).
    - Every consumer file named above has its vitest summary line quoted in the summary; the full excluded-lane suite is green once.
    - The summary carries the 35-row hand-off table with the `unmeasured locally, by construction` column filled on every row.
  </acceptance_criteria>
  <done>The tree is green on every arm this host can reach, the twin and the manifest follow the module, and plan 33-31 has its rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a directory spelled two ways -> the trusted-root exclusion and the home stop | Two spellings of one directory let the kit's own root be mistaken for a project root, or let the walk climb past the home |
| a `CLAUDE_PROJECT_DIR` value -> the governance dial's root | An env-tier spelling that differs from the cwd tier's is two answers for one directory (the shape the module deletes) |
| the committed `.js` and the hook manifest hash -> the guard's module check | A rebuilt module without its manifest hash is a guard that refuses its own kit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-111 | Spoofing | home boundary spelled at rung 2 | high | mitigate | Task 1: the home's second spelling goes through `canonicalDirectoryPath`; the identity sets and the `degenerate` guard are unchanged; W-HOME asserts the stop at the canonical spelling. |
| T-33-112 | Spoofing | env tier at bare `resolve` | high | mitigate | Task 1: the env tier canonicalises through the one ladder; W-ENV proves the link spelling answers the target on darwin. |
| T-33-113 | Repudiation | a fixture spelled by a second authority | medium | mitigate | Task 1: `tmp15` derives through the module's export; the rung-2 site counts are asserted in the verify block. |
| T-33-114 | Denial of service | a rebuilt module whose manifest hash is stale | medium | mitigate | Task 2: `generate:hook-manifest` after every rebuild; `freshness:hook-manifest` asserted. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; `package.json` is untouched. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run generate:hook-manifest && npm run check:build-parity && npm run freshness:hook-manifest` — the committed twins and the manifest hash are faithful.
- The two mirror anchors' occurrence counts are exactly 1 each.
</verification>

<success_criteria>
- The win32 spelling decision is recorded with its evidence before the code moved; rung 1 is the one published spelling.
- The fixture helper, the home boundary and the env tier all derive through the one ladder; the rung-2 site counts are 1 in the module and 1 in the test file, both of them the ladder's own rung and its evidence.
- Every consumer arm is quoted green; plan 33-31 has its 35-row hand-off with the unmeasured axis named.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| decision D-33-R3-01 | recorded decision (summary + STATE.md) | rung 1 is the module's sole published spelling; `assumption_delta = promote` |
| `tmp15(prefix)` through `canonicalWorkingDirectory` | test fixture change, `scripts/context-io.test.ts` | one authority for both sides of the WR-15 block |
| `homeBoundary` second spelling through `canonicalDirectoryPath` | module behavior, `scripts/context-io.ts` :4889 | WR-05 (a) |
| env tier through `canonicalDirectoryPath` | module behavior, `scripts/context-io.ts` :5308 | WR-05 (b) |
| Tests W-ENV, W-HOME | new test cases | red-first (W-ENV); premise-first (W-HOME) |
| the 35-row hand-off table | summary section | plan 33-31's pre-push inventory rows |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-24-SUMMARY.md` when done
</output>

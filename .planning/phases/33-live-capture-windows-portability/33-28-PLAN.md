---
phase: 33-live-capture-windows-portability
plan: 28
type: execute
wave: 17
depends_on: ["33-27"]
files_modified:
  - scripts/canonical-frontmatter.ts
  - scripts/canonical-frontmatter.js
  - scripts/canonical-frontmatter.test.ts
  - scripts/canonical-corpus.test.ts
  - scripts/generate-role-adapters.ts
  - scripts/generate-role-adapters.js
  - scripts/generate-role-adapters.test.ts
  - agent-factory/roles/orchestrator.md
  - .claude/agents/grugops-orchestrator.md
  - hooks/hooks.json
  - hooks/admission-guard.test.ts
  - scripts/floor-invariance.test.ts
  - scripts/check-foundation-guards.test.ts
  - scripts/frontmatter.test.ts
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
  - agent-factory/workflows/16-context-read-write.md
  - agent-factory/contracts/context-note.md
  - docs/GUARANTEES.md
  - CHANGELOG.md
autonomous: true
gap_closure: true
requirements: [CAP-01, CAP-03]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `--agent` coordinator adapter's `tools:` grant carries the plugin's MCP admission tool under the name the platform actually exposes for a plugin-bundled server — `mcp__plugin_grugops_grugops__propose_note` (round-1-held/33-CAPTURE-A.jsonl A:11 lists it; B:11 lists exactly the adapter's seven) — and it gets there through the generator from a capability token the role declares, never by a hand edit of the generated adapter (KIT (a), the human's direction at plan 33-21, WINDOWS.md row 255; CLAUDE.md single-source)."
    - "The canonical frontmatter alphabet is widened by exactly ONE character, `_`, as a deliberate, loud, recorded act (decision D-33-R3-02): the platform's scoped MCP tool names carry it, it is not a YAML-significant byte, and enumerate-the-good is preserved — every YAML-significant byte the alphabet excludes is still refused, asserted by the existing corpus and node-start cases, unchanged."
    - "The admission guard's hook matcher matches the SCOPED tool name: the platform documents that a matcher written against the bare server key never fires for a plugin's bundled server, so today's `mcp__grugops__.*` never fired in plugin form and the un-forgeable `human:NAME` tier was not wired on the only path where the MCP tool exists. The matcher is widened to the scoped family, the pin in `floor-invariance.test.ts` is replaced by an assertion derived from the round-1 init frame's own tool name, and the W3 breadth cases cover the scoped spelling."
    - "Every pin the grant change moves is derived and named — the spawn-grant derivation (`deriveGrant` reads only the `Agent(...)` list; asserted unchanged), the adapter census, the generator's vocabulary tests, the frontmatter fixtures, the guard oracles, the guarantees document — and the adapter-freshness, hook-manifest and guarantees gates are green at the end."
    - "What the zero-token tree cannot prove is named as `UNKNOWN - verify` with the observation that settles it: whether the platform offers the scoped tool to the `--agent` main-thread session once the grant names it is answered by the next capture's `system/init` tool list (B:11's successor), not by this plan."
  artifacts:
    - scripts/canonical-frontmatter.ts
    - scripts/generate-role-adapters.ts
    - .claude/agents/grugops-orchestrator.md
    - hooks/hooks.json
  key_links:
    - "agent-factory/roles/orchestrator.md `capabilities:` -> generate-role-adapters.ts `CAPABILITY_TOOLS` (`admit` token) -> `render()` tools line -> .claude/agents/grugops-orchestrator.md:5 -> the installer (spawns the same generator) -> the installed target's adapter -> the `--agent` session's tool list"
    - "canonical-frontmatter.ts `PLAIN_SCALAR_ALPHABET` (+ `_`) -> `admit()` over the adapter -> `deriveGrant` / the spawn oracles / adapters-freshness"
    - "hooks/hooks.json admission matcher -> the scoped family -> hooks/admission-guard.js on every `propose_note` call in plugin form -> generate:hook-manifest -> hook-entry.ts"
  prohibitions:
    - "the generated adapter is never hand-edited; the generator and the role file are the only sources"
    - "no character other than `_` enters the plain-scalar alphabet, and no YAML-significant byte does"
    - "no pin is moved by hand where the value can be derived from the corpus or the fixture"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): this plan lands KIT (a), the second of the two human-directed items the flip waits on; it decides nothing about the flip."
    - "CAP-02 (edge row unresolved, flagged): untouched beyond rebuilt twins the windows leg exercises; measured only by plan 33-31."
    - "CAP-03 (edge row unresolved, flagged): the route axis of the D-07 projection (`proposeNoteCalls`, suffix-matched, either spelling) is what the next capture measures on the spawn path; nested role sessions still receive no MCP tool (A:784) and reach the sanctioned writer in-process, which this plan does not change — stated in the go text by plan 33-32."
    - "assumption_delta = add-alongside, recorded: the bare-family matcher spelling is kept beside the scoped one in the alternation because a standalone `.mcp.json` server named `grugops` would expose the bare name; the kit ships no such server today, so the scoped spelling is the live one."
---

<objective>
Gap-closure round 3 of the phase's four-round cap. The human's KIT direction (a), recorded at plan
33-21's decision checkpoint and carried as WINDOWS.md row 255: carry the plugin's MCP admission
tool (`propose_note`) in the `--agent` coordinator adapter's grant — "touching the spawn-grant
derivation, its census pins and the guard oracles". 33-DIAGNOSIS.md § 1.3 (i) measured it on the
round-1 capture: A's `system/init` listed 145 tools including
`mcp__plugin_grugops_grugops__propose_note`; B's (`--agent grugops-orchestrator`) listed exactly
the adapter's seven, so on the spawn path the coordinator could not propose notes through the
sanctioned writer by construction of the grant.

Two facts found while reading the tree for this plan, both in scope of "the guard oracles":
(1) the canonical frontmatter alphabet (P27's closed form, `PLAIN_SCALAR_ALPHABET`) does not admit
`_`, so an adapter `tools:` line naming any MCP tool is refused by `admit()` today — the grant
cannot be carried without a deliberate widening; (2) the platform documents that hook matchers for
a plugin's bundled MCP server must use the scoped name `mcp__plugin_PLUGIN_SERVER__TOOL` and
that a matcher written against the bare server key never fires — `hooks/hooks.json`'s admission
matcher is `mcp__grugops__.*`, so the admission guard has not been firing in plugin form. Carrying
the tool in the grant without wiring the guard to it would widen an ungated channel; the two land
together.

Gap mapping (33-VERIFICATION.md CAP-01 `missing[0]` (a)) → Task 1 the alphabet, Task 2 the
generator and the regeneration with every pin derived, Task 3 the matcher and the documents.

Decisions honored: CLAUDE.md single-source (role text once; adapters are generated), the P27
canonical-form rule (widening is loud and recorded), D-11 (offline proof; `UNKNOWN - verify` where
only a session can answer).

Purpose: the sanctioned route is reachable by the coordinator on both dispatch paths, and the
guard that makes `human:NAME` un-forgeable actually fires on it.
Output: the widened alphabet, the `admit` capability token, the regenerated adapter, the scoped
matcher, every pin derived, the documents and changelog.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md
@.planning/phases/33-live-capture-windows-portability/33-21-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-27-SUMMARY.md
@scripts/canonical-frontmatter.ts
@scripts/generate-role-adapters.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The plain-scalar alphabet admits `_` — one character, loud, recorded, with every refusal it must keep asserted unchanged</name>
  <read_first>
    - `scripts/canonical-frontmatter.ts` — `PLAIN_SCALAR_ALPHABET` (:239-248) and the whole docblock above it (:215-238: enumerate-the-good, "WIDENING THIS SET IS A DELIBERATE, LOUD ACT"), `DOUBLE_QUOTED_KEYS` (:207), `GRANT_KEYS` (:191), `SCOPED_GRANT` (:854), `admittedGrantValues` / `admittedGrantedNames` (:891-925), the node-start refusals.
    - `scripts/canonical-corpus.test.ts` — the `WIDENINGS` table and its premise (:395-420: a widening's sigil must NOT already be in the alphabet), the corpus replay cases, any distinct-character measurement of the live corpus.
    - `scripts/canonical-frontmatter.test.ts` — the alphabet and grant-key cases (`grep -n 'ALPHABET\|grant-key\|plain scalar' scripts/canonical-frontmatter.test.ts`).
    - `.planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-A.jsonl` line 11 — the `system/init` frame's `tools[]` carrying `mcp__plugin_grugops_grugops__propose_note` (the spelling the platform exposes; read through `git show c7be6d0d:`).
  </read_first>
  <files>scripts/canonical-frontmatter.ts, scripts/canonical-frontmatter.js, scripts/canonical-frontmatter.test.ts, scripts/canonical-corpus.test.ts</files>
  <behavior>
    - Test K1 (RED first): an adapter document whose `tools:` value is `Agent(grugops-installer, grugops-security-nfr), Read, Grep, Glob, Edit, Write, Bash, mcp__plugin_grugops_grugops__propose_note` is ADMITTED by `admit()` and `admittedGrantedNames` returns exactly the two role names (the MCP token is outside the scoped grant and does not enter the spawn derivation). On the base `admit()` refuses it by name (the alphabet refusal) — quote the refusal code and sentence.
    - Test K2 (the converse, generated): for every byte in the YAML-significant set the docblock lists (`: # & * ! | > [ ] { } ' " % @` backtick `? \` and every whitespace other than SPACE), a plain scalar carrying it is still refused by name — the case is generated from that list, not typed per byte, and its count is asserted against the list's length.
    - Test K3 (the widening premise, kept honest): the `WIDENINGS` premise in `canonical-corpus.test.ts` still holds for every row (no widening's sigil is `_`); the live-corpus replay still admits the same rows it admitted before, plus the regenerated coordinator adapter once Task 2 lands (state the before/after admitted counts).
  </behavior>
  <action>
    Add `_` to `PLAIN_SCALAR_ALPHABET` as its own array element with a comment naming the reason
    and the decision id (D-33-R3-02): the platform's scoped MCP tool names
    (`mcp__plugin_PLUGIN_SERVER__TOOL`) carry it; it is not YAML-significant; the alphabet
    stays an allow-list. Extend the docblock's "MEMBERSHIP IS DERIVED FROM THE LIVE CORPUS"
    paragraph with the dated sentence that the corpus now spells `_` on exactly one grant line
    (the coordinator adapter's), so the measurement the paragraph cites stays true. Change no other
    character and no node-start rule.

    Tests K1-K3, RED before GREEN for K1. If a test pins the live corpus's distinct-character
    count, re-derive it from the corpus rather than bumping the integer (the set-literal-drift
    memory). Rebuild the committed `.js` in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/canonical-frontmatter.test.ts scripts/canonical-corpus.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the titles K1/K2/K3 are absent</fails_when>
    <automated>node -e "const m=require('./scripts/canonical-frontmatter.js');const a=m.PLAIN_SCALAR_ALPHABET;if(!a.has('_'))process.exit(1);for(const c of [':','#','&','*','!','|','>','[',']','{','}','\"',\"'\",'%','@','\`','?','\\\\','\\t'])if(a.has(c)){console.log('admits',JSON.stringify(c));process.exit(2)}"</automated>
    <fails_when>exit 1 (the underscore is not admitted) or exit 2 (a YAML-significant byte entered the alphabet — printed)</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `PLAIN_SCALAR_ALPHABET` contains `_` and no other new member (the diff of the constant is one added element; quoted); the docblock carries the decision id and the corpus sentence.
    - Tests K1-K3 exist; K1's RED commit precedes its GREEN commit (shas and the base's refusal code quoted); K2 is generated from the YAML-significant list with its count asserted.
    - Decision D-33-R3-02 is recorded in the summary and STATE.md with the three-part reason.
  </acceptance_criteria>
  <done>The alphabet admits the one character the platform's tool names need, and every byte it exists to refuse is still refused, by generated proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The `admit` capability token, the role declares it, the adapter is regenerated, and every pin the grant moves is derived</name>
  <read_first>
    - `scripts/generate-role-adapters.ts` — `CAPABILITY_TOOLS` / `VOCABULARY` (:163-175: the closed vocabulary, emission order, one emission per tool), the capabilities parse (:351-400), `render()` (:510-519: the coordinator's tools line composition), `PROVENANCE` (the generated-file banner the installer restates).
    - `agent-factory/roles/orchestrator.md` :4 (`capabilities: read edit shell`) and the `capabilities:` lines of every other role (`grep -n '^capabilities:' agent-factory/roles/*.md` — 17 files; only the coordinator gains the token).
    - `install/install.ts` :195-290 — the installer renders a target's adapters by spawning the committed generator (so a regenerated checkout and an installed target agree by construction); `scripts/adapters-freshness.ts` (`npm run freshness:adapters`).
    - `scripts/generate-role-adapters.test.ts` (:122, :630-643: the vocabulary and `capabilities:` grammar cases), `scripts/check-foundation-guards.test.ts` (:3329-3352, :3386, :3741, :4024, :4159, :4814: fixture adapters spelling a tools line), `scripts/frontmatter.test.ts` (:155-168, :1432-1453), `scripts/capture-live.ts` `deriveGrant` (:701) and its census test (`capture-live.test.ts` :272-311) — the consumers that read a tools line.
    - `scripts/coordinator-resolution-precheck.ts` — the zero-token scratch install + grant resolution report (run it after regeneration).
  </read_first>
  <files>scripts/generate-role-adapters.ts, scripts/generate-role-adapters.js, scripts/generate-role-adapters.test.ts, agent-factory/roles/orchestrator.md, .claude/agents/grugops-orchestrator.md, scripts/check-foundation-guards.test.ts, scripts/frontmatter.test.ts</files>
  <behavior>
    - Test K4 (RED first, the generator): a role declaring `capabilities: read edit shell admit` renders a tools line whose LAST entries are the `admit` token's tools (`mcp__plugin_grugops_grugops__propose_note`), emitted once, after the built-in tools, in the vocabulary's order; a role declaring `admit read` renders the identical line as `read admit` (order is the table's, not the declaration's). On the base `admit` is refused as outside the vocabulary — quote the refusal.
    - Test K5 (the coordinator, regenerated): `.claude/agents/grugops-orchestrator.md`'s `tools:` line parses (through `canonical-frontmatter.admit`) with `admittedGrantedNames` equal to the sixteen role names and the plain tool list ending in the scoped MCP name; `deriveGrant` over a scratch install (the precheck's own idiom) reports the same sixteen, `coordinator` unchanged, and the census relationship `adapters = grant + 1` unchanged.
    - Test K6 (nothing else moved): every other adapter under `.claude/agents/` is byte-identical before and after regeneration (`git diff --stat` over the directory names exactly one file); `npm run freshness:adapters` exits 0.
  </behavior>
  <action>
    Add `{ token: "admit", tools: ["mcp__plugin_grugops_grugops__propose_note"] }` as the LAST row
    of `CAPABILITY_TOOLS` with a comment: the spelling is the platform's scoped name for the
    plugin's bundled server (`plugin.json` `mcpServers.grugops` under plugin `grugops`), the one
    the round-1 init frame listed (A:11); the bare `mcp__grugops__propose_note` is the server's
    own name and is exposed only if a standalone `.mcp.json` server is ever shipped (not today).
    Extend the generator's docblock: the token exists so the coordinator's grant carries the
    sanctioned admission route on the spawn path (33-DIAGNOSIS § 1.3 (i)); no other role declares
    it, because nested role sessions do not receive plugin MCP tools (A:784) and reach the writer
    in-process. Update `agent-factory/roles/orchestrator.md`'s `capabilities:` to `read edit shell
    admit`. Run `npm run generate:adapters`; the coordinator adapter is the only changed file.

    Derive and fix every pin the new tools line moves: run the whole excluded-lane suite once and
    classify each red — the vocabulary cases in `generate-role-adapters.test.ts` (extend the
    vocabulary expectation by derivation from `CAPABILITY_TOOLS`), the fixture adapters in
    `check-foundation-guards.test.ts` and `frontmatter.test.ts` (they spell their own tools lines
    and need no change unless they assert the coordinator's live line — if they do, derive it from
    the generated file), the `capture-live.test.ts` census (must not move: the grant is the
    `Agent(...)` list). State the red count and the classification in the summary; a pin is never
    bumped by hand where the corpus can derive it.

    Run `node scripts/coordinator-resolution-precheck.js` (zero tokens) and quote its last line;
    then `npm run freshness:adapters`, `node scripts/check-foundation-guards.js`, and the
    `check:kit-refs` / `check:dashboard-readonly` gates the adapter change may cross. Rebuild the
    generator's `.js` in the same commit as the `.ts`.

    State in the summary, as `UNKNOWN - verify`: whether the platform offers the scoped tool to
    the `--agent` main-thread session once the adapter names it — the observation that settles it
    is the next capture's `system/init` tool list on path B (plan 33-32).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/generate-role-adapters.test.ts scripts/check-foundation-guards.test.ts scripts/frontmatter.test.ts scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the titles K4/K5/K6 are absent</fails_when>
    <automated>grep -a -c 'mcp__plugin_grugops_grugops__propose_note' .claude/agents/grugops-orchestrator.md</automated>
    <fails_when>a count other than 1 — the regenerated coordinator adapter does not carry the scoped tool exactly once</fails_when>
    <automated>test "$(grep -a -l 'mcp__plugin_grugops_grugops__propose_note' .claude/agents/*.md | wc -l | tr -d ' ')" = "1"</automated>
    <fails_when>non-zero exit — a second adapter carries the admission tool (only the coordinator declares the token)</fails_when>
    <automated>npm run generate:adapters &amp;&amp; git diff --exit-code -- .claude/agents</automated>
    <fails_when>non-zero exit — the committed adapters are not what the committed generator renders</fails_when>
    <automated>npm run freshness:adapters &amp;&amp; node scripts/check-foundation-guards.js &amp;&amp; node scripts/coordinator-resolution-precheck.js</automated>
    <fails_when>non-zero exit from any command, or the precheck's last line does not begin PRECONDITIONS HOLD</fails_when>
  </verify>
  <acceptance_criteria>
    - `CAPABILITY_TOOLS` carries the `admit` row last; `agent-factory/roles/orchestrator.md` declares it; `.claude/agents/grugops-orchestrator.md` is regenerated (banner intact) and is the only adapter that changed (`git diff --stat` quoted).
    - Tests K4-K6 exist; K4's RED commit precedes its GREEN commit (shas and the base's vocabulary refusal quoted); K5 shows `admittedGrantedNames` and `deriveGrant` unchanged at sixteen.
    - The summary states the derived red count after regeneration and its classification; no integer pin was bumped by hand.
    - The precheck's `PRECONDITIONS HOLD` line, `freshness:adapters` and the foundation guards are quoted green; the `UNKNOWN - verify` about the session's tool list is stated with its settling observation.
  </acceptance_criteria>
  <done>The coordinator's grant carries the sanctioned route through the generator, nothing else in the kit moved, and what only a session can prove is named as such.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The admission matcher fires on the scoped name, the pin becomes a fixture-derived assertion, and the documents say the platform's spelling</name>
  <read_first>
    - `hooks/hooks.json` — the two `PreToolUse` entries (the `Bash` guard; the `mcp__grugops__.*` admission matcher).
    - `scripts/floor-invariance.test.ts` :746-760 — the W3 case pinning the matcher literal; `hooks/admission-guard.test.ts` :280-296 — the W3 breadth loop over renamed tool names.
    - `scripts/generate-hook-manifest.ts` (:21-30: the deciders are derived from `hooks/hooks.json`) and `scripts/hook-manifest-freshness.ts`.
    - `.planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-A.jsonl` lines 289-293 — the `propose_note` tool-use at A:289 and the `hook_started` / `hook_response` frames named `PreToolUse:mcp__plugin_grugops_grugops__propose_note` whose output is `Session status updated.` (an operator-level hook's text; the admission guard states its allow on file descriptor 3 and writes nothing to stdout — see `hooks/admission-guard.ts` :84-130), so the frame does not show the guard ran.
    - `docs/GUARANTEES.md` and `npm run freshness:guarantees` (`scripts/guarantees-freshness.ts`) — the guarantees the matcher backs and how the document is derived; `agent-factory/workflows/16-context-read-write.md` step 3 and `agent-factory/contracts/context-note.md` — the sentences naming `mcp__grugops__propose_note`.
    - The platform's plugin reference (code.claude.com/docs/en/plugins-reference, "Hooks that target the plugin's own bundled MCP server must use its scoped names ... A matcher written against the bare server key never fires") — cite it in the summary by URL and quote the sentence.
  </read_first>
  <files>hooks/hooks.json, hooks/admission-guard.test.ts, scripts/floor-invariance.test.ts, hooks/hook-entry.ts, hooks/hook-entry.js, agent-factory/workflows/16-context-read-write.md, agent-factory/contracts/context-note.md, docs/GUARANTEES.md, CHANGELOG.md</files>
  <behavior>
    - Test K7 (RED first, fixture-derived): the admission matcher read from `hooks/hooks.json`, compiled as a regular expression, MATCHES the tool name the round-1 init frame lists for the plugin's admission tool (read from A:11 through `git show c7be6d0d:` — the platform's own spelling, never typed) AND still matches the bare family (`mcp__grugops__propose_note`, the server's own name); it does NOT match `mcp__plugin_context7_context7__resolve` (another plugin's scoped tool). On the base the scoped name does not match — quote the red run.
    - Test K8 (breadth, both families): the W3 breadth loop in `hooks/admission-guard.test.ts` gates the scoped spellings (`mcp__plugin_grugops_grugops__propose_note`, `..._admit`, `..._v2_admit`) exactly as it gates the bare ones — deny without the human variable on a high-severity finding, allow with `human:alice` and the variable set.
    - Test K9 (the manifest follows): `npm run generate:hook-manifest` after the `hooks.json` edit leaves `npm run freshness:hook-manifest` green, and the derived decider list still names exactly the two deciders.
  </behavior>
  <action>
    Change `hooks/hooks.json`'s admission matcher to a family covering both spellings — the
    scoped one the platform exposes for the bundled server and the bare one the server itself is
    named by — as one alternation (for example `mcp__(plugin_grugops_)?grugops__.*`); keep the
    `Bash` entry byte-identical. In `scripts/floor-invariance.test.ts`, replace the literal pin at
    :755 with two assertions: the matcher compiles and matches the fixture-derived scoped name (K7),
    and it matches the bare family (the prior pin's meaning, kept as a property). Extend the W3
    breadth loop in `hooks/admission-guard.test.ts` with the scoped spellings (K8). Regenerate the
    hook manifest and run its freshness gate (K9).

    Documents: WF16 step 3 and the context-note contract name the tool as the platform spells it
    in plugin form, with the bare name stated as the server's own name; `docs/GUARANTEES.md` is
    updated through its freshness derivation (`npm run freshness:guarantees`), never by hand where
    the derivation writes the sentence. CHANGELOG `Unreleased`: a `Security` entry — the
    admission-guard hook matcher now matches the plugin-scoped tool name; before this change the
    hook did not fire on `propose_note` calls in plugin form (cite the platform reference) — and a
    `Changed` entry for the coordinator's grant (the `admit` capability token). Run the document
    gates the diff crosses and quote each pass line.

    Hand off to plan 33-34's ledger close: one row for the matcher finding (fixed here, with the
    A:289-293 frames and the platform sentence as evidence), and one row for the `_` widening
    (decision D-33-R3-02, fixed here). State in the summary, as `UNKNOWN - verify`, whether the
    admission guard ran on A:289 — the frame shows an operator-level hook's output and the guard
    writes its allow to fd 3, so the transcript cannot say; the platform sentence says it did not.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/floor-invariance.test.ts hooks/admission-guard.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the titles K7/K8 are absent</fails_when>
    <automated>node -e "const h=require('./hooks/hooks.json');const e=h.hooks.PreToolUse.find(x=>x.matcher!=='Bash');const re=new RegExp(e.matcher);if(!re.test('mcp__plugin_grugops_grugops__propose_note'))process.exit(1);if(!re.test('mcp__grugops__propose_note'))process.exit(2);if(re.test('mcp__plugin_context7_context7__resolve'))process.exit(3)"</automated>
    <fails_when>exit 1 (the scoped name is not matched), exit 2 (the bare family was dropped), or exit 3 (another plugin's scoped tool is matched)</fails_when>
    <automated>npm run generate:hook-manifest &amp;&amp; npm run freshness:hook-manifest &amp;&amp; npm run freshness:guarantees</automated>
    <fails_when>non-zero exit from any command</fails_when>
    <automated>grep -a -c 'mcp__plugin_grugops_grugops__propose_note' agent-factory/workflows/16-context-read-write.md agent-factory/contracts/context-note.md CHANGELOG.md</automated>
    <fails_when>any of the three files reports 0 — the platform's spelling is not stated where agents and humans read it</fails_when>
    <automated>npm run check:banned-claims &amp;&amp; npm run check:public-docs &amp;&amp; npm run check:claim-anchors &amp;&amp; npm run check:residual-citations &amp;&amp; npm run check:diff-disposition &amp;&amp; npm run check:imperative-lexicon &amp;&amp; npm run freshness &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any command, or the vitest summary reports a failed test file</fails_when>
  </verify>
  <acceptance_criteria>
    - `hooks/hooks.json`'s admission matcher matches both families and no other plugin's scoped tools; the `Bash` entry is byte-identical to the base.
    - Tests K7-K9 exist; K7's RED commit precedes its GREEN commit (shas quoted; the scoped name is read from the A:11 fixture, not typed); the literal pin at floor-invariance :755 is gone.
    - `hooks/hook-entry.ts`/`.js` regenerated; `freshness:hook-manifest` and `freshness:guarantees` exit 0; WF16, the contract and the changelog name the scoped spelling; every document gate exits 0 (quoted).
    - The summary carries the two ledger rows for plan 33-34, the platform sentence with its URL, and the `UNKNOWN - verify` about A:289 with its reason.
  </acceptance_criteria>
  <done>The guard that makes a human stamp un-forgeable is wired to the tool name the platform actually calls, proven against the round-1 frame's own spelling, and the documents say what the platform says.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the generated adapter's `tools:` line -> the platform's allowlist for the coordinator session | A tool granted here is a capability the coordinator holds on the spawn path |
| the plain-scalar alphabet -> the spawn verdict | Every character admitted is a character a spawn verdict is computed over (P27's closed form) |
| a `propose_note` call in plugin form -> the admission guard | A matcher that never fires is an ungated admission channel; a grant naming that tool widens it |
| the generator -> the installed target | The installer spawns the same generator, so a hand edit anywhere is drift the freshness gate must catch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-127 | Elevation of privilege | an ungated `propose_note` channel in plugin form (matcher never fires) | critical | mitigate | Task 3: the matcher covers the scoped family; K7 derives the name from the round-1 init frame; K8 gates both spellings; CHANGELOG `Security` names the prior state. |
| T-33-128 | Tampering | the alphabet widened past `_` | high | mitigate | Task 1: one element added; K2 generates a refusal case per YAML-significant byte; the widening is a recorded decision. |
| T-33-129 | Spoofing | a hand-edited adapter carrying a grant the role never declared | high | mitigate | Task 2: the token flows from the role file through the generator; `generate:adapters` + `git diff --exit-code` and `freshness:adapters` asserted. |
| T-33-130 | Repudiation | a pin bumped by hand to make a gate green | medium | mitigate | Every moved pin is derived from the corpus or fixture; the red classification is stated. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; the plugin install is a platform operation exercised only by plan 33-32's capture. |
</threat_model>

<verification>
- `npm run generate:adapters && git diff --exit-code -- .claude/agents` — the committed adapters are the generator's output.
- `npm run freshness:adapters && npm run freshness:hook-manifest && npm run freshness:guarantees && node scripts/check-foundation-guards.js` — every derived gate green.
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
</verification>

<success_criteria>
- The coordinator adapter carries the scoped admission tool through the generator; the alphabet admits `_` by recorded decision; the admission matcher matches the scoped name, proven against the round-1 frame.
- Every pin the change moved is derived; every gate is green; the documents and changelog say the platform's spelling and the prior state.
- What only a session can prove is `UNKNOWN - verify` with its settling observation named.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `_` in `PLAIN_SCALAR_ALPHABET` | module change, `scripts/canonical-frontmatter.ts` | decision D-33-R3-02 |
| `admit` token in `CAPABILITY_TOOLS` | generator change, `scripts/generate-role-adapters.ts` | maps to the scoped MCP tool name |
| `capabilities: read edit shell admit` | role change, `agent-factory/roles/orchestrator.md` | single source of the grant |
| regenerated `.claude/agents/grugops-orchestrator.md` | generated adapter | the only adapter that changes |
| the admission matcher family in `hooks/hooks.json` | hook wiring change | scoped + bare spellings |
| Tests K1-K9 | new test cases | red-first (K1, K4, K7) |
| WF16 / contract / GUARANTEES / CHANGELOG sentences | documentation | the platform's spelling; the prior state named |
| two ledger rows (hand-off) | summary section | appended by plan 33-34 through the tool |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-28-SUMMARY.md` when done
</output>

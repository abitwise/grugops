---
phase: 33-live-capture-windows-portability
plan: 29
type: execute
wave: 18
depends_on: ["33-28", "33-25"]
files_modified:
  - scripts/capture-live.ts
  - scripts/capture-live.js
  - scripts/capture-live.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-03, CAP-01]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every input to the verdict is either received by the runner on the child's stdout pipe or derived BEFORE the child exists: the frames are parsed from the bytes the runner itself buffered (the file on disk is a copy for the operator and is never re-read for scoring), the spawn grant is derived before the spawn and a post-run re-derivation that differs folds into `failed`, and no `readFrames`/`readFileSync` of the transcript path remains inside `runTarget` (33-REVIEW CR-01, the round's only Critical; 33-VERIFICATION CAP-03 `missing[0]`)."
    - "The trust boundary shrinks by MOVING the inputs, not only by narrowing the grant: with every input pre-spawn or on the pipe, no tool the subject holds reaches a verdict input — proven by a forged frame appended to the transcript FILE during the run that the verdict does not see, and by an adapter edited during the run that the CAP-03 predicate does not adopt."
    - "The grant is scoped to what the run needs: file-writing tools are granted under the target only, through the platform's own rule form (`Edit(//ABS/**)` — an `Edit(path)` rule governs every built-in file-writing tool including `Write`; a `Write(path)` rule is never matched; a single leading slash anchors at the rule's source, not the filesystem root), the admission tool is spelled as the platform exposes it, and `Bash(node *)` is KEPT with the reason recorded: nested role sessions receive no plugin MCP tool (A:784) and reach the sanctioned writer only in-process through node (A:839, A:878, A:1408, A:1442) — dropping it would make CAP-03 side (b) unreachable by construction, and the CR-01 concern it carried is closed by the input moves above."
    - "A refused or failed run leaves no plugin registry row behind: the containment refusal precedes the install, and the uninstall runs in a `finally` with its exit recorded in the Run table (WR-06)."
    - "Every new arm is red-first through the `LiveOps` seam at zero tokens; the dry run still exits 0, reads `GO-READINESS`, prints one `OUTCOME: no-go` line and carries the new grant and uninstall rows (D-10)."
  artifacts:
    - scripts/capture-live.ts
    - scripts/capture-live.js
    - scripts/capture-live.test.ts
  key_links:
    - "capture-live.ts `runPlatform` -> buffered stdout chunks -> `PlatformRunResult.transcriptText` -> `runTarget` `parseFrames(result.transcriptText)` -> `deriveClaims` / `denyObservedInStream` / `projectLivePath`"
    - "capture-live.ts `runTarget` -> `deriveGrant(build.target)` BEFORE `ops.runPlatform` -> `capThreePredicate` ; post-run re-derivation -> `grantDrift` -> `failed`"
    - "capture-live.ts `liveAllowedTools(target)` -> the `--allowedTools` argv -> the Run table's `tool grant` row -> the readiness/report"
    - "capture-live.ts `runTarget` -> `isOutsideTargets` refusal -> `ops.pluginInstall` -> try { spawn, derive } finally { `ops.pluginUninstall` } -> the `plugin uninstall exit` row"
  prohibitions:
    - "no verdict input is read from disk after the subject has run"
    - "no unscoped file-writing tool is granted to the subject"
    - "the D-07 predicate is never softened to make a capture read as parity (D-20)"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): the flip depends on a capture this instrument scores; this plan makes the score independent of the subject and asserts nothing about the capture's word."
    - "CAP-02 (edge row unresolved, flagged): untouched beyond the rebuilt twin; measured only by plan 33-31."
    - "CAP-03 (edge row unresolved, flagged): the observation channel is now independent by construction (pipe + pre-spawn); the observation itself is produced only by plan 33-32's go."
    - "assumption_delta = no-change: `Bash(node *)` stays in the grant by recorded reason (the sanctioned in-process route for nested sessions); the review's recipe to drop it is declined with the evidence lines named."
---

<objective>
Gap-closure round 3 of the phase's four-round cap. 33-REVIEW.md CR-01 (the round-2 review's only
Critical, confirmed by direct code read in 33-VERIFICATION.md): the round-1 CR-01 closure moved
the transcript out of the subject's cwd, but the subject still holds unscoped `Write`/`Edit` and
`Bash(node *)`, the scratch directory carries a fixed literal prefix under the OS temp directory,
and three verdict inputs — `readFrames(transcriptPath)` / `readFileSync(transcriptPath)`,
`deriveGrant(build.target)`, and the plugin-cache digest — are re-read from disk AFTER the subject
ran. The `OUTCOME: pass` line is reachable over a run whose deny never fired. This plan closes the
first two inputs and the grant; plan 33-30 closes the third (provenance before the spawn, WR-02)
and the review's warnings.

Gap mapping (33-VERIFICATION.md CAP-03 `missing[0]`): "derive the verdict from the bytes the
runner itself received on the child's stdout pipe (never re-read the transcript file)" → Task 1;
"derive the spawn grant BEFORE the spawn and fold a post-run mismatch into failed, narrow
LIVE_ALLOWED_TOOLS to what the probe needs" → Task 2; WR-06 (install ordering, uninstall on
throw) → Task 3. IN-01 (`LIVE_OPS` frozen) and IN-02 (full-length held-capture sha) are folded
here and named as folded.

Decisions honored: D-01 (the stream is the capture; the runner scores the stream it received),
D-02 (side (a)/(b) derived from a grant fixed before the subject existed), D-04 (the deny read
from the CLI-emitted channel — now literally the pipe), D-10 (the dry run walks the new rows),
D-11 (offline proof through the seam before any go).

Purpose: a `pass` means the runner saw it — not that a file said so afterwards.
Output: `scripts/capture-live.ts` + committed `.js`, the offline suite extended with the
forged-file, adapter-drift, scoped-grant and uninstall proofs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md
@.planning/phases/33-live-capture-windows-portability/33-REVIEW.md
@.planning/phases/33-live-capture-windows-portability/33-12-PLAN.md
@.planning/phases/33-live-capture-windows-portability/33-12-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-13-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-28-SUMMARY.md
@scripts/capture-live.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The verdict is scored from the bytes the runner received on the pipe; the file is a copy the subject cannot use</name>
  <read_first>
    - `scripts/capture-live.ts` — hard rule 5 in the header (:57-66), `runPlatform` (:1315-1350: `child.stdout.pipe(out)`, `settle`), `PlatformRunResult` (:1302-1311), `runTarget` (:1423-1470: the `readFrames(transcriptPath)` at :1445 and `readFileSync(transcriptPath, "utf8")` at :1449), `readFrames` / `parseFrames` (:424-500), `denyObservedInStream` (:519), `TargetRun` (:1402-1410), `dryRun()` and `verifyMode()` (the two legitimate file readers that stay).
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` CR-01 item 1 — the concrete attack (`node -e "require('fs').appendFileSync('SCRATCH/33-CAPTURE-A.jsonl', JSON.stringify({type:'system',subtype:'hook_response',stdout:'DENY_ENVELOPE'})+'\n')"`) and the recipe (buffer the chunks; `settle` resolves with the text; derive over `parseFrames`).
    - `scripts/capture-live.test.ts` :703-790 — the CR-01 block: Test G (the run seam recorder), Test H (the OLD in-target plant, which stays as a control), the `recordingOps` helper shape.
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - Test C1 (RED first — the review's attack, at the NEW location): through the seam, a `runPlatform` stand-in writes the committed fixture stream to the `transcriptPath` it was handed AND, before resolving, appends one forged `system/hook_response` frame whose decoded `stdout` carries the byte-perfect prod-deploy deny envelope to that FILE — while the text it returns as the received bytes is the fixture alone. The report's D-04 claim row for that run reads `no`, `denyObservedInStream(report.frames.frames)` is false, and `report.frames.frames.length` equals the fixture's frame count. On the base the forged frame is scored (the row reads yes) — quote the red run.
    - Test C2 (converse — the pipe is the channel): the same forged frame delivered IN the returned bytes (not in the file) IS scored: the row reads yes. The channel is what the runner received, exactly.
    - Test C3 (the transcript file is still written, byte-equal to the received bytes): after the run the file at `transcriptPath` equals `result.transcriptText` in the honest case (the operator's copy is faithful), and `TargetRun.transcriptText` is the received bytes, never a re-read.
    - Test C4 (no re-read survives in the source): the body of `runTarget` (extracted by its declaration from the parsed source, the writer-set idiom) contains no call to `readFrames` and no `readFileSync` whose argument names the transcript path; the two legitimate readers (`dryRun`, `verifyMode`) still do.
  </behavior>
  <action>
    In `runPlatform`, buffer every stdout chunk the child emits in addition to piping it to the
    write stream; `settle` resolves with `transcriptText` = the concatenated chunks decoded as
    UTF-8. `PlatformRunResult` gains `transcriptText: string`. In `runTarget`, derive
    `frames = parseFrames(result.transcriptText)` and set `transcriptText` from the result; delete
    the file reads. Rewrite hard rule 5's last sentences: the stream is sound because the platform
    emits it AND because the runner scores the bytes it received; the file is written for the
    operator and the diagnosis and is never an input to the verdict, so the subject's reach over
    the scratch directory no longer matters to the verdict (CR-01 round 2). Keep the sibling-scratch
    location and the `isOutsideTargets` refusal (they still protect the operator's copy).

    `readFrames` stays exported for `--verify-artifacts` and the dry run's fixture read; say so in
    its docblock. Fold IN-01: `LIVE_OPS` becomes a frozen `Readonly<LiveOps>`. Fold IN-02:
    `HELD_CAPTURE_SHA` in the test becomes the full 40-character sha of `c7be6d0d` (resolve it with
    `git rev-parse` once and pin the full value; quote it).

    Tests C1-C4 in `scripts/capture-live.test.ts`, RED before GREEN for C1 (quote the red run and
    its `yes` row). Test H (the old in-target plant) stays as the control. Rebuild so the committed
    `.js` moves in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts -t "C1|C2|C3|C4|forged|pipe"</automated>
    <fails_when>non-zero exit, or fewer than 4 tests reported passed</fails_when>
    <automated>awk '/^export async function runTarget/,/^}/' scripts/capture-live.ts | grep -a -c 'readFrames(\|readFileSync(transcriptPath'</automated>
    <fails_when>a count other than 0 — a verdict input is still re-read from the transcript file inside runTarget</fails_when>
    <automated>grep -a -c 'export const LIVE_OPS: Readonly<LiveOps> = Object.freeze' scripts/capture-live.ts</automated>
    <fails_when>a count other than 1 — the seam is not frozen (IN-01)</fails_when>
    <automated>node scripts/capture-live.js --dry-run --out "$(mktemp -d)" &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit from any command, or the dry-run output lacks a line reading exactly OUTCOME: no-go</fails_when>
  </verify>
  <acceptance_criteria>
    - `PlatformRunResult` carries `transcriptText`; `runTarget` derives frames through `parseFrames` over it; the awk-scoped grep over `runTarget` is 0; hard rule 5 states the pipe rule.
    - Tests C1-C4 exist; C1's RED commit precedes its GREEN commit (shas and the base's `yes` row quoted); Test H is unchanged.
    - `LIVE_OPS` is frozen; `HELD_CAPTURE_SHA` is 40 characters (both folds named in the summary as IN-01 / IN-02).
    - The committed `.js` moves in the same commit; the dry run exits 0 with one `OUTCOME: no-go`.
  </acceptance_criteria>
  <done>What the runner scores is what the runner received; a file the subject can append to is a copy, not evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The spawn grant is fixed before the subject exists and drift fails the run; the tool grant is scoped to the target with the platform's own rule form</name>
  <read_first>
    - `scripts/capture-live.ts` — `LIVE_ALLOWED_TOOLS` (:250-260) and the paragraph above it (the `UNKNOWN - verify` about the narrowest grant, now answered: `Bash(helm upgrade *)` was sufficient for the probe, 33-DIAGNOSIS § 4.3), `deriveGrant` (:701-772), `GrantDerivation`, `capThreePredicate` (:810), `runTarget` (the `deriveGrant(build.target)` after the run), `capture()` (:2027-2033: `grantSource` derived from target A before the loop; `agent: build.label === "B" ? grantSource.coordinator : null`), `RunSpec.allowedTools`, `renderReport`'s Run table.
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` CR-01 items 2 and 3 and the Fix paragraph (`Write(TARGET/**)`, `Edit(TARGET/**)`, drop `Bash(node *)`); the platform's permission rules reference (code.claude.com/docs/en/permissions — `//path` is absolute; `Edit(path)` governs `Write` and `NotebookEdit`; `Write(path)` is never matched) — cite by URL in the summary and quote the two sentences.
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` § 1.3 (ii) (A:839, A:878, A:1408, A:1442: the in-process writer route) and § 4 item 3 (the grant spelling vs the plugin's exposed name; `Bash(helm upgrade *)` sufficient).
    - `.planning/phases/33-live-capture-windows-portability/33-28-SUMMARY.md` — the scoped tool name the coordinator adapter now carries.
    - `scripts/capture-live.test.ts` :272-311 (the census cases) and :222 (a role outside the derived grant is named).
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - Test C5 (RED first — the review's attack 2): through the seam, a `runPlatform` stand-in edits one of the target's `.claude/agents/*.md` files in the target during the "run" (appends a granted name to the coordinator's `Agent(...)` list, or adds a second `coordinator: true` marker) and returns a fixture stream in which that role appears as a spawn. The report's `failed` is true, a Run-table row reads `spawn grant drift: ...` naming what changed, and the CAP-03 side (a) evaluation used the PRE-spawn grant (the injected role is `outside the derived grant`, named). On the base the post-run derivation adopts the edit and the role reads as a member — quote the red run.
    - Test C6 (the grant is one derivation): `capture()`'s pre-loop `grantSource` (from target A) and each `runTarget`'s pre-spawn derivation agree (`JSON.stringify` equal on `granted`, `coordinator`, `prefix`, `adapterNames`); a target whose adapters differ from A's at build time is a refusal BEFORE any spawn (the recorder was called 0 times).
    - Test C7 (the scoped grant, form-checked): `liveAllowedTools(target)` contains no entry equal to `Write` or `Edit`; exactly one entry matches `^Edit\(//.+/\*\*\)$` and, with the leading `//` removed, names the target's real path; `Bash(node *)` and `Bash(helm upgrade *)` are present; the admission entry is `mcp__plugin_grugops_grugops__propose_note`; the argv handed to `runPlatform` (recorded through the seam) carries exactly that list after `--allowedTools`.
    - Test C8 (the report says the grant): the Run table carries a row `| run X tool grant | RESOLVED_LIST |` for each run, in the dry run too, and the readiness table carries no row that could be MET without the target existing (the grant is a per-run row, not a precondition).
  </behavior>
  <action>
    Move `deriveGrant(build.target)` in `runTarget` to BEFORE `ops.runPlatform` (after the install
    — the installer renders the adapters at build time and the plugin install adds none) and hand
    that value to `deriveClaims`, `targetObservations` and `projectLivePath`. After the run,
    re-derive once; compare the four derived fields; on any difference set `failed = true` and add
    a Run-table row `spawn grant drift` whose value names the changed field(s) (never the whole
    adapter text). In `capture()`, keep the pre-loop `grantSource` and assert each target's
    pre-spawn derivation equals it before the spawn — a mismatch is `fail(...)` before any token.

    Replace `LIVE_ALLOWED_TOOLS` with `export function liveAllowedTools(target: string): string[]`:
    `Agent`, `Read`, `Grep`, `Glob`, ONE scoped file-writing rule in the platform's absolute form
    (`Edit(//` + the target's real path without its leading slash + `/**)`, derived through
    `realpathSync.native` of the target), `Bash(node *)`, `Bash(helm upgrade *)`, and the scoped
    admission tool name. Delete the unscoped `Write` and `Edit` entries. Rewrite the paragraph
    above it in clear voice with three stated facts: (1) an `Edit(path)` rule governs every
    built-in file-writing tool including `Write` and a `Write(path)` rule is never matched (the
    review's recipe named the wrong rule); (2) the `//` form is the platform's absolute-path
    anchor — a single leading slash anchors at the rule's source, which for `--allowedTools` is the
    session's cwd; (3) `Bash(node *)` is kept because nested role sessions receive no plugin MCP
    tool (A:784) and reach the sanctioned writer only in-process (A:839, A:878, A:1408, A:1442);
    the CR-01 concern it carried is closed by Task 1, this task and plan 33-30 moving every verdict
    input before the spawn or onto the pipe, not by the grant. Also resolve the old
    `UNKNOWN - verify` about the narrowest probe grant: `Bash(helm upgrade *)` was sufficient in
    both round-1 runs (33-DIAGNOSIS § 4.3).

    `RunReport` gains the resolved grant; `renderReport` prints it as a Run-table row; the dry
    run prints its scratch targets' rows. Tests C5-C8 RED before GREEN for C5. Rebuild.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts -t "C5|C6|C7|C8|drift|scoped"</automated>
    <fails_when>non-zero exit, or fewer than 4 tests reported passed</fails_when>
    <automated>awk '/^export async function runTarget/,/^}/' scripts/capture-live.ts | grep -a -n 'deriveGrant(\|ops.runPlatform(' | head -2 | awk -F: 'NR==1{a=$1} NR==2{b=$1} END{exit !(a&lt;b)}'</automated>
    <fails_when>non-zero exit — the first deriveGrant call inside runTarget does not precede the platform spawn</fails_when>
    <automated>grep -a -c '^const LIVE_ALLOWED_TOOLS' scripts/capture-live.ts</automated>
    <fails_when>a count other than 0 — the unscoped literal grant still exists</fails_when>
    <automated>node -e "const m=require('./scripts/capture-live.js');const g=m.liveAllowedTools(require('os').tmpdir());if(g.includes('Write')||g.includes('Edit'))process.exit(1);if(g.filter(x=>/^Edit\(\/\/.+\/\*\*\)$/.test(x)).length!==1)process.exit(2);if(!g.includes('Bash(node *)')||!g.includes('Bash(helm upgrade *)'))process.exit(3);if(!g.includes('mcp__plugin_grugops_grugops__propose_note'))process.exit(4)"</automated>
    <fails_when>exit 1 (an unscoped file-writing tool survives), exit 2 (no single scoped Edit rule in the absolute form), exit 3 (a needed Bash rule was dropped), or exit 4 (the admission tool is not the platform's scoped spelling)</fails_when>
    <automated>node scripts/capture-live.js --dry-run --out "$(mktemp -d)" &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit from any command, or the dry-run output lacks OUTCOME: no-go, or the report it writes lacks a `tool grant` row</fails_when>
  </verify>
  <acceptance_criteria>
    - `runTarget` derives the grant before the spawn, re-derives after, folds drift into `failed` with a named row; `capture()` asserts each target's pre-spawn grant equals `grantSource` before spawning.
    - `liveAllowedTools(target)` exists with the eight entries the behavior names and nothing unscoped; the docblock states the three facts with the platform reference URL and the transcript lines.
    - Tests C5-C8 exist; C5's RED commit precedes its GREEN commit (shas and the base's adopted-member result quoted).
    - The dry-run report carries a `tool grant` row per run; the committed `.js` moves in the same commit.
  </acceptance_criteria>
  <done>The subject is scored against a grant fixed before it existed, holds file-writing tools only under its own target, and every reason for what it still holds is written down.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Containment is refused before anything is installed, and the uninstall runs on every exit path with its exit recorded (WR-06)</name>
  <read_first>
    - `scripts/capture-live.ts` — `runTarget` (:1423-1470: `ops.pluginInstall` is the first statement; the two `fail`s follow it; `ops.pluginUninstall` is the last statement, not a `finally`), `pluginUninstall` (:1373-1379: swallows every error), `cleanupPlan` / `cleanupScratch` (hard rule 3), `installOutcome` (:1352).
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` WR-06 — the order, the `finally`, and "record the uninstall's exit in the run table so a failed uninstall is named rather than swallowed".
    - `scripts/capture-live.test.ts` :792-852 (Tests M/N/O — the install seam cases whose recorder shape the new cases reuse).
    - `.planning/phases/33-live-capture-windows-portability/deferred-items.md` — the stale-plugin-rows item (two `grugops@grugops` rows at `0.1.0` outliving their temp targets); this task is the mechanism that stops the runner adding a third.
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - Test C9 (RED first): a transcript path planted inside the target (a `TargetBuild` whose `transcriptDir` is under `build.target`) is refused by `runTarget` BEFORE `ops.pluginInstall` is called — the install recorder's call count is 0. On the base the install runs first (count 1) — quote it.
    - Test C10 (RED first): a `runPlatform` stand-in that throws after the install completed → `runTarget` rejects AND `ops.pluginUninstall` was called exactly once for that target; likewise when the derivation throws (a context root planted as a regular file so `authorStamps` throws). On the base the uninstall is skipped (count 0) — quote it.
    - Test C11 (the exit is recorded, never swallowed): `pluginUninstall` returns the platform's exit status (or the spawn error's message); the Run table carries `| run X plugin uninstall | exit 0 |` in the honest case and names a non-zero exit or error text when the stand-in reports one; the uninstall's failure never masks the run's own result (the report is still returned).
  </behavior>
  <action>
    Reorder `runTarget`: both `isOutsideTargets` refusals first (pure, no side effect), then the
    install, then `try { spawn; derive; } finally { uninstall }`. Change `pluginUninstall` to
    return a small result (`{ status, error }`, the `installOutcome` idiom) instead of swallowing;
    `runTarget` records it on the report (`RunReport` gains `uninstall`), `renderReport` prints the
    row, and the plan summary of a live run will show it. Keep the best-effort semantics for the
    RESULT (a failed uninstall is named, not thrown) — the point is that the operator sees the
    registry residue named, not that a cleanup failure hides a paid transcript.

    Tests C9-C11 RED before GREEN. Rebuild; then the dry run and the full excluded-lane suite once.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts -t "C9|C10|C11|uninstall|before anything is installed"</automated>
    <fails_when>non-zero exit, or fewer than 3 tests reported passed</fails_when>
    <automated>awk '/^export async function runTarget/,/^}/' scripts/capture-live.ts | grep -a -n 'isOutsideTargets(\|ops.pluginInstall(\|finally' | awk -F: '{print $1}' | tr '\n' ' '</automated>
    <fails_when>the printed line numbers are not strictly increasing with the two isOutsideTargets lines first, the install next and a finally last, or fewer than four numbers are printed</fails_when>
    <automated>node scripts/capture-live.js --dry-run --out "$(mktemp -d)" &amp;&amp; npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any command, or the dry-run output lacks OUTCOME: no-go, or the vitest summary reports a failed test file</fails_when>
  </verify>
  <acceptance_criteria>
    - `runTarget` refuses containment before the install and uninstalls in a `finally`; `pluginUninstall` returns its outcome and the Run table prints it.
    - Tests C9-C11 exist; C9's and C10's RED commits precede their GREEN commits (shas and the base's counts quoted).
    - The committed `.js` moves in the same commit; the dry run and the full excluded-lane suite are green.
  </acceptance_criteria>
  <done>A run that never spawns installs nothing; a run that dies uninstalls anyway and says whether it managed to.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the subject's tools -> the runner's verdict inputs | The milestone's bar: not by an observation channel the agent under test can write to |
| the transcript FILE (subject-reachable in principle) -> the scored frames | Closed by scoring the pipe; the file is a copy |
| the installed adapters (subject-editable under the target) -> the CAP-03 grant | Closed by deriving before the spawn and failing on drift |
| the operator's plugin registry -> a refused or failed run | A stale local-scope row is state written outside any directory the runner created |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-131 | Tampering | a forged frame appended to the transcript file | critical | mitigate | Task 1: frames parsed from the buffered pipe bytes only; C1 proves the file append is blind, C2 the pipe is scored. |
| T-33-132 | Spoofing | an adapter edited during the run to widen the grant | critical | mitigate | Task 2: grant derived before the spawn; post-run drift folds into `failed`; C5 mutation-proven. |
| T-33-133 | Elevation of privilege | unscoped file-writing tools | high | mitigate | Task 2: one `Edit(//ABS/**)` rule under the target, in the platform's documented form; `Write` never matched, so not granted. |
| T-33-134 | Elevation of privilege | `Bash(node *)` retained | high | accept | Reason recorded (the only in-process route for nested sessions); every verdict input moved off disk-after-spawn so the tool reaches none of them (Tasks 1-2, plan 33-30). |
| T-33-135 | Tampering | plugin registry residue on a failed run | medium | mitigate | Task 3: refusal before install; uninstall in `finally`; exit recorded. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; the plugin install is exercised only by plan 33-32's capture. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run check:build-parity` — the committed `.js` is a faithful build.
- `node scripts/capture-live.js --dry-run --out TMP` — exit 0, `GO-READINESS:` line, exactly one `OUTCOME: no-go`, the `tool grant` and `plugin uninstall` rows present.
</verification>

<success_criteria>
- The frames are scored from the pipe; the grant is fixed before the spawn and drift fails the run; the file-writing grant is scoped to the target in the platform's own form; `Bash(node *)` is kept by recorded reason.
- Containment precedes the install; the uninstall runs on every path with its exit recorded.
- Every arm is red-first through the seam; the dry run walks the new rows at zero tokens.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `PlatformRunResult.transcriptText` | type change | the bytes the runner received on the pipe |
| `runTarget` scoring through `parseFrames(result.transcriptText)` | module behavior | no post-run file read |
| pre-spawn `deriveGrant` + `spawn grant drift` row | module behavior + report row | CR-01 item 2 |
| `liveAllowedTools(target)` | exported function (replaces `LIVE_ALLOWED_TOOLS`) | scoped `Edit(//ABS/**)`, no `Write`, `Bash(node *)` kept by reason, scoped MCP name |
| `| run X tool grant |`, `| run X plugin uninstall |` | report rows | the grant and the uninstall exit, stated |
| `LIVE_OPS` frozen; `HELD_CAPTURE_SHA` full-length | folds | IN-01, IN-02 |
| `pluginUninstall` returning its outcome; `finally` ordering | module behavior | WR-06 |
| Tests C1-C11 | new test cases | red-first (C1, C5, C9, C10) |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-29-SUMMARY.md` when done
</output>

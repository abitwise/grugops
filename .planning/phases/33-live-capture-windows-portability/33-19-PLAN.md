---
phase: 33-live-capture-windows-portability
plan: 19
type: execute
wave: 7
depends_on: ["33-02", "33-06", "33-09"]
files_modified:
  - scripts/check-foundation-guards.test.ts
  - scripts/freshness.test.ts
  - scripts/check-build-parity.ts
  - scripts/check-build-parity.js
autonomous: true
gap_closure: true
requirements: [CAP-02]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The o-prefix case compares the echoed sibling path in the form the guard PUBLISHES it — `quoteValue` is `JSON.stringify` — so a path carrying backslashes is matched in its JSON-escaped spelling on every host, and the negative half compares the same way (D-15)."
    - "The freshness discrimination pair's PRE-FIX arm asserts the property that is observable on every host — the pre-fix gate does NOT name the planted stale output — and records which of the two measured pre-fix shapes the host produced (exit 0 + fresh line, or exit 1 + the could-not-compile sentence), refusing any third shape; no platform conditional (D-14)."
    - "`scripts/check-build-parity.ts` launches the compiler the way `scripts/freshness.ts` does since 33-06: `typescript/lib/tsc.js` resolved through `createRequire(import.meta.url)` and run under `process.execPath` with `--outDir <tmp>` in an argv array — no `npx`, no shell, no host branch."
  artifacts:
    - scripts/check-foundation-guards.test.ts
    - scripts/freshness.test.ts
    - scripts/check-build-parity.ts
    - scripts/check-build-parity.js
  key_links:
    - "check-foundation-guards.test.ts o-prefix expectation -> `JSON.stringify(siblingDeep)` <- model-tiers.ts `quoteValue` (the echo's one escaping)"
    - "freshness.test.ts DISCRIMINATION PAIR pre-fix arm -> the two measured pre-fix shapes -> `not.toContain(STALE COMMITTED OUTPUT: <plant>)`"
    - "check-build-parity.ts -> `createRequire(import.meta.url).resolve(\"typescript/lib/tsc.js\")` -> `spawnSync(process.execPath, [tscEntry, \"--outDir\", tmp])` (the 33-06 launch, one copy)"
  prohibitions:
    - "no platform conditional closes a CAP-02 red (D-14/D-16)"
    - "a historical checkout is never patched to make a discrimination pair green; the assertion is reformulated to what every host can observe"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): untouched by this plan."
    - "CAP-02 (edge row unresolved, flagged): this plan closes two windows reds (W-11, W-29) and one `status: open` mechanical item; the leg's conclusion is measured only by plan 33-20."
    - "CAP-03 (edge row unresolved, flagged): untouched by this plan."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). Three items in three files: the last predicted NOT ADDRESSED windows red (row
228, `check-foundation-guards.test.ts`), the last one-arm-over survivor (row 235,
`freshness.test.ts`), and the `status: open` shell-less `npx` launch in
`scripts/check-build-parity.ts` that 33-06 closed in `freshness.ts` and left here.

Gap mapping (33-VERIFICATION.md CAP-02 `missing[1]` "... and the 1 NOT-ADDRESSED
check-foundation-guards.test.ts o-prefix case"; 33-CI-MEASUREMENT.md § 2.4 / § 2.5 (d);
`deferred-items.md` "scripts/check-build-parity.ts:132 still launches the compiler through
spawnSync("npx", ["tsc"]) with no shell"):
- Task 1: W-11 `(o-prefix) a root that is a string PREFIX of a sibling path rewrites nothing`.
- Task 2: W-29 `DISCRIMINATION PAIR: the same planted stale committed .js is green on the pre-fix tree and red on the post-fix tree`.
- Task 3: the `check-build-parity.ts:132` launch (an ubuntu-scoped step today, so not a measured red; it is the same defect class 33-06 diagnosed and the CAP-02 bar says both legs, so it is closed rather than carried).

Decisions honored: D-13, D-14, D-15 (assert the published form), D-16.

Purpose: the three remaining places where a host's spelling or a host's launcher decided a
verdict now derive their expectation or their launch from one authority.
Output: two test modules corrected; one gate module launching its compiler the 33-06 way, with
its committed `.js`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
@.planning/phases/33-live-capture-windows-portability/deferred-items.md
@.planning/phases/33-live-capture-windows-portability/33-06-SUMMARY.md
@scripts/freshness.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: The o-prefix case matches the echoed path in the guard's published (JSON) form (W-11)</name>
  <read_first>
    - `scripts/check-foundation-guards.test.ts` ~L10815-10862 (the case: PREMISE 1, PREMISE 2, `toContain(siblingDeep)`, `not.toContain(\`"${siblingDeep.slice(m.length)}"\`)`).
    - `scripts/model-tiers.ts` ~L1067 (the refusal: `${quoteValue(preset)} is not a legal preset name`) and ~L1409 (`quoteValue` = `JSON.stringify(value)`).
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 row W-11 (received `"C:\\Users\\RUNNER~1\\…\\mirXTRA\\deep"`, expected the single-backslash spelling) and `deferred-items.md` "One windows red in scripts/check-foundation-guards.test.ts is NOT ADDRESSED: the o-prefix echo".
  </read_first>
  <files>scripts/check-foundation-guards.test.ts</files>
  <action>
    The guard echoes the illegal value through `quoteValue`, which is `JSON.stringify`; the
    published form of the sibling path is therefore `JSON.stringify(siblingDeep)` — identical to
    the raw path on a host with no backslashes, doubled backslashes on win32. Change the property
    assertion to `expect(section).toContain(JSON.stringify(siblingDeep))` and the negative to
    `expect(section).not.toContain(JSON.stringify(siblingDeep.slice(m.length)))` (the same
    escaping on both halves, so the negative cannot pass vacuously by comparing a spelling the
    section never carries). Extend the case's comment: the value is compared in the form the guard
    publishes it, read from `quoteValue`, so the host's separator cannot split the assertion.

    Probe every arm: run the whole `(o-prefix)` describe block and any sibling case that echoes a
    path through the same refusal (`grep -n 'not a legal preset name' scripts/check-foundation-guards.test.ts`),
    classify each in the summary as already-JSON-form or moved.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts -t "o-prefix"</automated>
    <fails_when>non-zero exit, or 0 passed</fails_when>
    <automated>grep -a -c 'toContain(JSON.stringify(siblingDeep))' scripts/check-foundation-guards.test.ts</automated>
    <fails_when>a count of 0 — the expectation is still the raw spelling</fails_when>
  </verify>
  <acceptance_criteria>
    - Both halves of the o-prefix property compare through `JSON.stringify`; the comment names `quoteValue` as the reason.
    - Every sibling echo assertion in the file is classified in the summary.
    - No `process.platform` read was added.
  </acceptance_criteria>
  <done>The echoed path is matched as the guard prints it.</done>
</task>

<task type="auto">
  <name>Task 2: The discrimination pair's pre-fix arm asserts what every host can observe (W-29)</name>
  <read_first>
    - `scripts/freshness.test.ts` ~L382-420 (the pair: `before.status` 0 + `FRESH_LINE`; `after.status` 1 + `STALE COMMITTED OUTPUT: ${PLANT_REL}`), ~L63 (`PRE_FIX_SHA`), the fixture builder that clones and checks out the pre-fix tree and runs ITS gate, `transcript()` (~L222).
    - `scripts/freshness.ts` ~L236-290 — the post-fix launch and the two refusal sentences (`did not compile cleanly` for a compile that never ran; the compiler's own text when it ran and refused).
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 row W-29 (windows: pre-fix clone exit 1, `the rebuild did not compile cleanly ... stderr: (no stderr)`; post-fix names the plant correctly; 7 of 8 closed) and WINDOWS.md row 235.
  </read_first>
  <files>scripts/freshness.test.ts</files>
  <action>
    The pair's purpose is discrimination: the SAME planted stale `.js` is not detected by the
    pre-fix gate and is detected by the post-fix gate. On a host where the pre-fix gate cannot
    start its `npx` compiler, the pre-fix arm's vacuity shows as exit 1 with the could-not-compile
    sentence and no stale line — a different shape of the same fact (it did not detect the
    plant). Reformulate the pre-fix arm to the host-independent property and a measured shape
    check:
    - `expect(before.stdout, msg).not.toContain(\`STALE COMMITTED OUTPUT: ${PLANT_REL}\`)` — the
      vacuity itself, on every host.
    - the pre-fix run's shape is one of exactly two measured shapes, asserted with a named
      classification (`prefixShape(run)` returning `"green-vacuous"` for exit 0 + `FRESH_LINE`,
      `"no-compiler-vacuous"` for exit 1 + the `did not compile cleanly` sentence + no compiler
      text, or `"unexpected"` otherwise) and `expect(shape, msg).not.toBe("unexpected")`; log the
      shape so the CI output records which one the leg took. This is a disjunction over the
      OBSERVED output, not a platform branch — the same discipline D-16's named skip uses.
    - the post-fix arm stays exactly as written (exit 1, names the plant, no fresh line).
    Rewrite the comment above the pair: the pre-fix arm's evidence is "did not name the plant",
    and the two shapes are the two ways the pre-fix launch fails to detect it (one measured on
    POSIX, one on windows-latest run 35499800942).

    Probe every arm: `ORDERING INDEPENDENCE`, `CONTROL`, and Test 1 are post-fix cases and must
    be unchanged; run the whole file.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/freshness.test.ts</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
    <automated>grep -a -c 'no-compiler-vacuous' scripts/freshness.test.ts</automated>
    <fails_when>a count below 2 — the classification is not declared and asserted</fails_when>
  </verify>
  <acceptance_criteria>
    - The pre-fix arm asserts `not.toContain(STALE COMMITTED OUTPUT: <plant>)` and a `prefixShape` classification with exactly the two measured shapes; `"unexpected"` is refused.
    - The post-fix arm is unchanged: `grep -a -c 'expect(after.status, msg).toBe(1)' scripts/freshness.test.ts` is 1 and the `STALE COMMITTED OUTPUT: ${PLANT_REL}` assertion on `after.stdout` is present (both quoted).
    - No `process.platform` read was added; the comment names both measured shapes and their run ids.
  </acceptance_criteria>
  <done>The pair discriminates on every host: the pre-fix gate never names the plant, the post-fix gate always does.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `check-build-parity.ts` launches the compiler the 33-06 way (the open item)</name>
  <read_first>
    - `scripts/check-build-parity.ts` ~L120-160 (`buildParity`: `spawnSync("npx", ["tsc"], { cwd: root })`, the IN-01 error-detail paragraph, `trackedBuildOutputs`).
    - `scripts/freshness.ts` ~L248-290 — `tscEntry = createRequire(import.meta.url).resolve("typescript/lib/tsc.js")`, the refusal naming the locate layer, `spawnSync(process.execPath, [tscEntry, "--outDir", tmp], ...)`, and the "child's OWN text first" reporting.
    - `scripts/freshness.test.ts` — how the compiler launch is exercised (the pair fixtures run the committed gate; the RangeError diagnosis case), to place the new case beside them.
    - `.planning/phases/33-live-capture-windows-portability/deferred-items.md` — the item and its `Why deferred`.
    - `package.json` — `check:build-parity` runs `tsc --outDir .tmp-build` first and then the script; keep that contract.
  </read_first>
  <files>scripts/check-build-parity.ts, scripts/check-build-parity.js, scripts/freshness.test.ts</files>
  <behavior>
    - Test AF (RED first, structural + behavioural in one body): the source of `scripts/check-build-parity.ts` contains `createRequire(import.meta.url).resolve("typescript/lib/tsc.js")` and `spawnSync(process.execPath,` and contains no `"npx"` literal; and `node scripts/check-build-parity.js` run from the repository root exits 0 with its usual clean-diff line on this tree.
    - Test AG (the locate layer is named): with `NODE_PATH`-independent resolution, a planted copy of the module in a temp root that has no `node_modules` still resolves the compiler through the checkout's chain (as `freshness.ts` documents), OR the gate reports the locate-layer refusal by name rather than a null-status build — assert whichever the module does, and that the message names the layer.
  </behavior>
  <action>
    Replace the `npx` launch in `buildParity` with the identical mechanism `freshness.ts` uses:
    resolve `typescript/lib/tsc.js` through `createRequire(import.meta.url)`; on resolution failure
    return a `BuildParityResult` whose message names the locate layer (mirror `freshness.ts`'s
    sentence); spawn `process.execPath` with `[tscEntry]` in the checkout root (this gate rebuilds
    IN PLACE by design — read `trackedBuildOutputs` and the header to confirm whether it passes an
    `--outDir`; keep its current output location, change only the launcher). Keep the IN-01
    error-detail paragraph and update its recorded case: `npx` resolving as `npx.cmd` is no longer
    reachable from this module, and the paragraph now records that as the reason the launch moved
    (spell the shim's name in backticks in that comment, never as a double-quoted string literal —
    the acceptance gate counts the double-quoted spelling, which must reach zero).

    Place Test AF/AG in `scripts/freshness.test.ts` (the compiler-launch suite) rather than a new
    test module, so `TRIPWIRE_MODULES` in `scripts/check-foundation-guards.test.ts` does not move;
    say so in the summary. Rebuild so the committed `.js` moves in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/freshness.test.ts -t "check-build-parity"</automated>
    <fails_when>non-zero exit, or 0 passed</fails_when>
    <automated>grep -a -c '"npx"' scripts/check-build-parity.ts</automated>
    <fails_when>a count above 0 — the shell-less npx launch survived</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npx tsc --noEmit</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/check-build-parity.ts` resolves `typescript/lib/tsc.js` through `createRequire` and spawns `process.execPath`; the locate-layer refusal is named.
    - Tests AF and AG live in `scripts/freshness.test.ts` with titles containing `check-build-parity`; the RED commit precedes the GREEN commit (shas quoted).
    - `npm run check:build-parity` exits 0 on this tree; `TRIPWIRE_MODULES` is unchanged at 72.
  </acceptance_criteria>
  <done>One compiler launch, two gates, no shim on any host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a guard's echoed value -> a test's expectation | An echo compared in a spelling the guard never prints is a red on one host and vacuous on another |
| a historical checkout's launcher -> a discrimination pair | A pre-fix arm that cannot start its compiler on one host must not be answered by patching history |
| a shell-less `npx` -> the build-parity gate | On win32 the child never starts and the gate reports a build that "did not complete" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-89 | Repudiation | o-prefix echo compared in the wrong form | low | mitigate | Task 1: both halves through `JSON.stringify`, the guard's own `quoteValue`. |
| T-33-90 | Repudiation | pre-fix vacuity unobservable on win32 | medium | mitigate | Task 2: the host-independent "does not name the plant" assertion plus a two-shape classification that refuses a third shape. |
| T-33-91 | Denial of service | build-parity gate cannot start its compiler on win32 | medium | mitigate | Task 3: the 33-06 launch; the locate layer named. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; `package.json` untouched. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run check:build-parity` — committed `.js` faithful, the gate itself green under its new launcher.
</verification>

<success_criteria>
- W-11 and W-29 are closed by derivation; the `check-build-parity.ts` open item is closed with the 33-06 launch.
- No platform conditional; no historical checkout patched; no census pin moved.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| o-prefix expectation through `JSON.stringify` | test change, `scripts/check-foundation-guards.test.ts` | the guard's published form |
| `prefixShape(run)` | test helper, `scripts/freshness.test.ts` | the two measured pre-fix shapes; `unexpected` refused |
| `createRequire`/`process.execPath` launch in `buildParity` | module change, `scripts/check-build-parity.ts` | the 33-06 launch, second consumer |
| Tests AF, AG | new test cases in `scripts/freshness.test.ts` | red-first; no new test module |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-19-SUMMARY.md` when done
</output>

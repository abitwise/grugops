---
phase: 33-live-capture-windows-portability
plan: 12
type: execute
wave: 7
depends_on: ["33-01", "33-08", "33-10"]
files_modified:
  - scripts/capture-live.ts
  - scripts/capture-live.js
  - scripts/capture-live.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-03, CAP-01]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The D-07 verdict the runner reports is computed by a path-invariant projection — per role: admitted-note count, kind multiset, author, note route — plus the frozen verdict marker; `at` stamps, model-authored bodies and model-chosen task ids are not in it (33-DIAGNOSIS § 1.4, D-07)."
    - "Over the committed round-1 capture (commit c7be6d0d: 33-CAPTURE-SUMMARY.md, 33-CAPTURE-A.jsonl, 33-CAPTURE-B.jsonl) the new projection returns a NON-EMPTY, named divergence list — the held capture still reads as divergent under the corrected predicate, so D-20's finding is not softened."
    - "A synthetic pair of context roots that differ ONLY in `at` stamps and note bodies projects to parity (an empty diff list), so the predicate is one two independent live sessions can meet (CR-03)."
    - "The transcript the predicates are scored from is streamed into a runner-owned scratch directory that is a sibling of the target, never inside `build.target` and never inside the child's cwd; the report states this as a row, and a file planted at the old in-target location is never read (CR-01)."
    - "D-05 provenance selects the plugin by NAME from `system/init.plugins[]`, validates the cache path under the plugin cache root before touching it, compares the installed copy's CONTENT digest with the checkout's tracked-file digest, and an outcome of `pass` is unreachable unless that comparison is MET (CR-02, WR-04 folded)."
    - "`node scripts/capture-live.js --dry-run` still exits 0, prints GO-READINESS, one `OUTCOME: no-go` line, and re-checks its own artifacts, with the new parity and provenance sections present (D-10)."
  artifacts:
    - scripts/capture-live.ts
    - scripts/capture-live.js
    - scripts/capture-live.test.ts
  key_links:
    - "capture-live.ts `projectLivePath` + `compareLivePaths` -> the report's `Dual-path parity (D-07) — path-invariant projection` section -> `deriveOutcome`"
    - "capture-live.test.ts -> `git show c7be6d0d:<phase dir>/33-CAPTURE-{SUMMARY.md,A.jsonl,B.jsonl}` (the immutable held capture is the offline fixture)"
    - "capture-live.ts `runTarget(build, ops)` -> `ops.runPlatform(..., transcriptPath)` with transcriptPath under the runner-owned scratch, outside every target"
    - "capture-live.ts `pluginUnderTest` -> `pluginCachePathAccepted` -> `contentDigest` (installed) vs `contentDigest` (checkout, `git ls-files -z`) -> `provenanceVerdict` -> `deriveOutcome`"
  prohibitions:
    - "the D-07 predicate is never softened to make the held round-1 capture read as parity (D-20)"
    - "no field the model authors (body, at, task id, refs text) enters the parity projection"
    - "the scored transcript is never located inside a directory the agent under test has a Write or Edit grant over"
    - "an outcome of pass is never reported while the installed plugin's provenance is UNMET or UNKNOWN"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): the flip is reachable only through a capture whose D-07 projection returns parity; this plan makes the predicate meetable but asserts nothing about whether the round-2 capture will meet it."
    - "CAP-02 (edge row unresolved, flagged): nothing in this plan touches a CI leg; CAP-02 is measured by plan 33-20 only."
    - "CAP-03 (edge row unresolved, flagged): this plan restores the instrument's independence (CR-01) and provenance (CR-02); the observation itself is produced only by plan 33-21's go."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). This is the round's TRACER: the three Critical instrument findings that make the
CAP-03 observation untrustworthy (CR-01, CR-02) and the CAP-01 flip unreachable (CR-03) are landed
in `scripts/capture-live.ts`, red-first, and proven offline against the committed round-1 capture,
end to end through `capture-live --dry-run`, before any other plan spends a token.

Gap mapping (33-VERIFICATION.md `gaps[].missing`):
- CAP-01 missing[0] "A fix to the D-07 comparator's predicate (33-DIAGNOSIS.md § 1.4 names the path-invariant projection)" → Task 1.
- CAP-03 missing[0] "Move the scored transcript to a runner-owned directory outside the target the agent has any grant over (CR-01)" → Task 2.
- CAP-03 missing[1] "Fix D-05 provenance to match the plugin by name (not loaded[0]) and fold a sha mismatch into the outcome (CR-02)" → Task 3.

Decisions honored: D-07 (the admitted-note set plus the frozen verdict, byte-identical prose not
asserted), D-20 (a real divergence is a kit finding; the held capture is NOT made to pass), D-10
(the dry run still walks every phase at zero tokens), D-11 (the transcript stays unedited — the
offline fixture is read from its commit), D-08 (one runner, `.ts` compiled to committed `.js`).

Folded review items, stated so their absence from the closing ledger is a decision: IN-01 (the
local `toPosix` copy is replaced by the `scripts/posix-path.js` import while Task 2 rewrites the
path handling around the transcript location) and WR-04 (`git -C <transcript path>` disappears
because Task 3 validates the cache path and compares content instead of running git in it).

Purpose: make the next live go worth spending — the predicate can be met, the channel cannot be
written by the subject, and a `pass` cannot be reported over the wrong plugin.
Output: `scripts/capture-live.ts` + committed `.js`, its offline suite extended with the held-capture
proofs, and a dry run that exits 0 over the new report schema.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md
@.planning/phases/33-live-capture-windows-portability/33-REVIEW.md
@.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md
@.planning/phases/33-live-capture-windows-portability/33-01-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-10-SUMMARY.md
@scripts/capture-live.ts
@scripts/dual-path-equivalence.ts
@scripts/posix-path.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The path-invariant D-07 projection, wired from function to outcome line and proven against the held capture (CR-03)</name>
  <reversibility rating="costly">The summary schema gains a parity section the flip manifest's D-18 citations will name; changing it later re-derives the citation form the flip plan reads (same weight D-08 carries).</reversibility>
  <read_first>
    - `scripts/capture-live.ts` — `AuthorStamp`, `authorStamps`, `contentBlocks`, `spawnObservations`, `roleKey`, `deriveGrant`, `verdictMarkerObserved`, `equivalence` (~L1444), `ReportModel`, `renderReport` (the `## Dual-path equivalence (D-07)` section), `capture()` (the `outcome` derivation ~L1591) and `dryRun()`.
    - `scripts/dual-path-equivalence.ts` — the deterministic replay comparator that STAYS for `convergence-spine.test.ts` and `check-uat-oracles.ts`; it is not edited.
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` § 1.2, § 1.3 (i)-(iii) and § 1.4 — the projection's definition and the behavioural divergences it must still report.
    - `.planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md` § "Target observations — run A" / "run B" (the `D-02 side (b) author stamp` rows: A = 15 rows, B = 9 rows) and § "Dual-path equivalence (D-07)".
    - `scripts/capture-live.test.ts` — `contextRootWithNotes`, `FIXTURE`, the `--dry-run` integration case (~L488).
    - `.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md` D-07 and D-20.
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - Test A (RED first, held capture): `compareLivePaths(projectLivePath(stampsA, framesA, prefix), projectLivePath(stampsB, framesB, prefix))` over the round-1 capture read from commit `c7be6d0d` returns a list that contains, verbatim: `brownfield-mapper: note count differs: path A has 5, path B has 3`; `architect-design: note count differs: path A has 4, path B has 3`; `security-nfr: note count differs: path A has 4, path B has 3`; `orchestrator: present only in path A (2 note(s))`; `note route: direct writes into the context root differ: path A 0, path B 9`; `note route: propose_note tool-use blocks differ: path A 3, path B 0` — and contains NO entry mentioning the verdict marker (absent on both sides is parity on that field).
    - Test B (parity): two `contextRootWithNotes` roots carrying the same `{by, kind}` multiset per role but different `at` stamps and different bodies, projected with the same fixture frames, compare to `[]`.
    - Test C (kind multiset is compared, not only the count): the same two roots with one note's `kind` changed on one side compare to a list naming that role and `kind multiset differs`.
    - Test D (route is derived from tool-use blocks only): `noteRoute(frames)` over the committed fixture stream returns `{ directContextWrites: 0, proposeNoteCalls: <the fixture's own count, asserted against an independent grep of the fixture text> }`; an in-memory frame carrying a `Write` tool-use with `file_path` under `.grugops/context/` raises `directContextWrites` by exactly 1, an `Edit` raises it by 1, and a `Write` to a path outside the context root raises nothing.
    - Test E (outcome wiring): `deriveOutcome({ hang: false, anyFailure: false, parityDiffs: ["x"], provenance: "MET" })` is `fail`; with `parityDiffs: []` it is `pass`; `hang: true` wins over everything.
  </behavior>
  <action>
    Add to `scripts/capture-live.ts`, beside the existing pure derivations (this stays ONE runner per
    D-08; no new module, so the `NON_TEST_MODULE_COUNT` / `TRIPWIRE_MODULES` census pins in
    `scripts/check-foundation-guards.test.ts` do not move):

    - `export interface RoleNotes { role: string; count: number; kinds: string[] }` where `kinds` is
      the sorted multiset (repeats kept) of `kind` values of the notes stamped `by` that role.
    - `export interface NoteRoute { directContextWrites: number; proposeNoteCalls: number }`.
    - `export interface PathProjection { roles: RoleNotes[]; verdictMarker: boolean; route: NoteRoute }`.
    - `export function noteRoute(frames: readonly StreamFrame[]): NoteRoute` — walks every frame's
      `contentBlocks`, counts `tool_use` blocks whose `name` is `Write` or `Edit` and whose
      `input.file_path` (a string, normalized with `toPosix` imported from `./posix-path.js`) contains
      `/.grugops/context/` as `directContextWrites`, and blocks whose `name` ends in `propose_note`
      (the installed plugin exposes `mcp__plugin_grugops_grugops__propose_note`; the grant spells
      `mcp__grugops__propose_note`; match the suffix) as `proposeNoteCalls`. Nested subagent frames
      are ordinary frames in this walk — path B's nine direct writes are by role agents.
    - `export function projectLivePath(stamps: readonly AuthorStamp[], frames: readonly StreamFrame[], prefix: string): PathProjection`
      — groups `stamps` by `roleKey(s.by, prefix)`, sorted by role; `verdictMarker` is
      `verdictMarkerObserved(stamps) !== null`; `route` is `noteRoute(frames)`. Nothing else enters:
      not `at`, not `body`, not `noteId`, not `task`, not `refs`. Say so in the function's docblock
      with the reason (33-DIAGNOSIS § 1.2: every one of those is model-chosen across two sessions).
    - `export function compareLivePaths(a: PathProjection, b: PathProjection): string[]` — named diffs,
      empty iff parity. Per role present on both sides: count differs → the count sentence; same
      count but `kinds` differ → `<role>: kind multiset differs: path A has [..], path B has [..]`.
      A role on one side only → `<role>: present only in path A (N note(s))` / `path B`. Route:
      the two route sentences. Verdict: `verdict marker READY_FOR_HUMAN_REVIEW differs: path A <present|absent>, path B <present|absent>`.
      Use exactly the sentence shapes Test A names.
    - `export function deriveOutcome(input: { hang: boolean; anyFailure: boolean; parityDiffs: readonly string[]; provenance: ProvenanceState }): "pass" | "fail" | "hang"`
      — `hang` first; then `fail` when `anyFailure`, when `parityDiffs` is non-empty, or when
      `provenance !== "MET"`; else `pass`. (`ProvenanceState` is introduced by Task 3 as the same
      three-state type `PreconditionState` already uses; until Task 3 lands, type it as
      `PreconditionState` and pass `"MET"` from `capture()`.)

    Wire it: in `capture()` compute `projectLivePath` per target from the stamps and frames the loop
    already derives, then `compareLivePaths` over the two; `outcome` becomes `deriveOutcome(...)`;
    `outcomeReason` names parity when it is the cause. `ReportModel` gains
    `parity: { projections: { label: RunLabel; projection: PathProjection }[]; diffs: string[] }`.
    `renderReport` emits a new section headed exactly `## Dual-path parity (D-07) — path-invariant projection`
    with one table per run (`| role | notes | kinds |` rows, then a `verdict marker` row and the
    two route rows) followed by the diff list, or the line `- parity: the two projections are equal`
    when empty. The existing task-id-keyed `equivalence()` output is KEPT but demoted: its heading
    becomes `## Replay comparator (informational — task-id keyed, deterministic replay only)` and it
    no longer sets `anyFailure` (33-DIAGNOSIS § 1.2: its grammar guarantees a red over two live
    sessions). `dryRun()` fills `parity` the same way over its two fixture-derived runs.

    Tests (`scripts/capture-live.test.ts`, RED committed before GREEN): add a helper
    `heldCapture(name)` that returns `git show c7be6d0d:.planning/phases/33-live-capture-windows-portability/<name>`
    via `spawnSync("git", [...], { cwd: ROOT, maxBuffer: 64 MiB })` and throws naming the sha when
    git cannot show it (a shallow clone is a loud red, not a skip). Add `stampsFromSummary(text, run)`
    that locates the `observation (run X)` table by its HEADER ROW (the same rule `verifyArtifacts`
    uses — never by heading) and parses each `D-02 side (b) author stamp | KIND by BY | note:TASK/ID` row
    into an `AuthorStamp` with an empty body, and reads the `verdict marker` row's `absent`/`present`
    word. Test A must ALSO assert the premise that the parsed stamp counts are 15 (A) and 9 (B) and
    that the two transcripts parse to 2079 and 1924 frames, so a wrong fixture is a named red.

    Rebuild (`npm run build`) so the committed `.js` moves in the same commit as the `.ts`.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports 0 passed, or any of the five behaviors above is absent from the test titles</fails_when>
    <automated>node scripts/capture-live.js --dry-run --out "$(mktemp -d)"</automated>
    <fails_when>non-zero exit, or the output lacks the line beginning GO-READINESS:, or lacks a line reading exactly OUTCOME: no-go</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or the parity check names scripts/capture-live.js as moved by the build</fails_when>
    <automated>grep -a -c 'Dual-path parity (D-07) — path-invariant projection' scripts/capture-live.ts</automated>
    <fails_when>a count of 0 — the section heading the flip manifest will cite is not emitted by the renderer</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/capture-live.ts` exports `noteRoute`, `projectLivePath`, `compareLivePaths`, `deriveOutcome`, `RoleNotes`, `NoteRoute`, `PathProjection`.
    - `scripts/capture-live.test.ts` contains a case whose title includes `held capture` and which reads the three round-1 artifacts through `git show c7be6d0d:`; its assertions include the six exact divergence sentences listed in Test A and the premise counts 15 / 9 / 2079 / 1924.
    - `scripts/capture-live.test.ts` contains a parity case (Test B) asserting `toEqual([])` over two roots differing only in `at` and body, and a kind-multiset case (Test C).
    - The git history shows the RED commit (tests added, suite red on the new cases) before the GREEN commit; the plan summary quotes both commit shas and the red run's failing titles.
    - `renderReport` output contains the heading `## Dual-path parity (D-07) — path-invariant projection` and the heading `## Replay comparator (informational — task-id keyed, deterministic replay only)`; the dry-run report written by the verify command carries both.
    - `capture()`'s outcome is assigned from `deriveOutcome(...)` and the replay comparator's diff list is no longer an input to it (`grep -n 'diffs.length > 0) anyFailure' scripts/capture-live.ts` returns nothing).
    - `import { toPosix } from "./posix-path.js"` is present in `scripts/capture-live.ts` and the local one-line `toPosix` arrow is gone (IN-01 folded; the count of `split(sep).join("/")` in the file is 0).
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts` still passes with `NON_TEST_MODULE_COUNT = 89` and `TRIPWIRE_MODULES = 72` unchanged.
  </acceptance_criteria>
  <done>The runner's D-07 verdict is a predicate two live sessions can meet, it still names the held capture's real divergences, and the dry run walks the new schema end to end at zero tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The scored transcript lives where the subject cannot write, proven through a run seam (CR-01)</name>
  <read_first>
    - `scripts/capture-live.ts` — hard rules 3 and 5 in the header, `makeScratch`/`cleanupScratch`, `TargetBuild`/`buildTarget`, `runPlatform`, the per-target loop inside `capture()` (~L1563-1585: `transcriptPath`, `runPlatform`, `readFrames`, `readFileSync(transcriptPath)`), `LIVE_ALLOWED_TOOLS`.
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` CR-01 — the exact attack: a `system/hook_response` frame or `parent_tool_use_id` frames appended by the model into the file it is scored from.
    - `scripts/capture-live.test.ts` — the `--dry-run` case and `TMP_PREFIX` usage (scratch listing).
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - Test F (RED first): `isOutsideTargets(join(transcriptDir, "33-CAPTURE-A.jsonl"), [target])` is true for a transcript directory created as a sibling scratch; `isOutsideTargets(join(target, "33-CAPTURE-A.jsonl"), [target])` is false; a path under a DIFFERENT scratch that is itself nested inside the target is false (containment is decided on `relative()`, not on string prefix).
    - Test G (seam): `runTarget(build, ops)` with a recording `ops.runPlatform` that writes the committed fixture stream to the `transcriptPath` it was handed: the recorded `transcriptPath` is outside `build.target`, outside the `cwd` argument, and inside `build.transcriptDir`; the returned report's `transcriptPath` equals the recorded one.
    - Test H (the attack is blind): before `runTarget` returns, plant a file at `join(build.target, "33-CAPTURE-A.jsonl")` containing one forged `system/hook_response` frame whose decoded `stdout` carries the prod-deploy deny envelope; the report's D-04 claim row for that run reads `no` (the fixture stream carries no deny in this arrangement) and `denyObservedInStream(report.frames.frames)` is false — the planted file was never read.
  </behavior>
  <action>
    Move the transcript out of the target. `TargetBuild` gains `transcriptDir: string`, created in
    `buildTarget` with `makeScratch(\`transcript-${label}\`)` — a sibling `mkdtemp` under the OS temp
    directory with the fixed `TMP_PREFIX`, never a child of `target` and never of `home`. Add
    `export function isOutsideTargets(path: string, roots: readonly string[]): boolean` deciding
    containment with `relative(root, path)` (outside iff the relative form is empty-or-starts-with
    `..`-or-is-absolute — the `isWithinRoot` idiom `scripts/board-read.ts` already uses); never a
    `startsWith` on the string.

    Extract the per-target body of `capture()` into
    `export async function runTarget(build: TargetBuild, run: { request: string; allowedTools: readonly string[]; agent: string | null; pluginName: string; marketplaceName: string; boundMs: number }, ops: LiveOps = LIVE_OPS): Promise<RunReport & { transcriptPath: string; transcriptText: string }>`
    with `export interface LiveOps { pluginInstall: typeof pluginInstall; runPlatform: typeof runPlatform; pluginUninstall: typeof pluginUninstall }`
    and `export const LIVE_OPS: LiveOps` bound to the real functions. Inside it,
    `transcriptPath = join(build.transcriptDir, captureTranscriptName(build.label))`, and every
    later read (`readFrames`, `readFileSync`) uses that path and no other. `capture()` becomes a loop
    over `runTarget`. (Plan 33-13 reuses this seam for CR-05 and CR-04; build it once, here.)

    Assert the property at run time too, not only in the suite: `runTarget` calls `fail(...)` if
    `isOutsideTargets(transcriptPath, [build.target, build.home])` is false or if `transcriptPath`
    is inside the `cwd` it is about to hand the platform — a misconfiguration is a refusal, not a
    capture. The report's Run table gains the row
    `| run X transcript location | runner-owned scratch, outside every target and outside the run's working directory |`
    derived from the same predicate, and hard rule 5's comment in the header gains one sentence
    naming CR-01: the stream is sound; the file is sound only because the subject has no path to it.

    Do NOT change `LIVE_ALLOWED_TOOLS` here: dropping `Write`/`Edit` would change what the capture
    observes about the note route (33-DIAGNOSIS § 1.3 (ii)) and is one of the KIT decisions plan
    33-21 puts to the human.

    Tests F, G, H in `scripts/capture-live.test.ts`, RED before GREEN. Test G's recording
    `runPlatform` must return a `PlatformRunResult` with status 0 so the report derivation runs.
    Rebuild so the committed `.js` moves in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or 0 passed, or the titles for the three behaviors above are absent</fails_when>
    <automated>grep -a -n 'join(build.target, transcriptName)' scripts/capture-live.ts</automated>
    <fails_when>any output line — the in-target transcript location still exists in the source</fails_when>
    <automated>node scripts/capture-live.js --dry-run --out "$(mktemp -d)" &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit from any command, or the dry run's output lacks OUTCOME: no-go</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/capture-live.ts` exports `isOutsideTargets`, `runTarget`, `LiveOps`, `LIVE_OPS`; `TargetBuild` carries `transcriptDir`.
    - The only `join(..., captureTranscriptName(...))` in the runner composes with `build.transcriptDir`.
    - `scripts/capture-live.test.ts` carries Tests F, G, H; Test H's title names the forged frame and asserts `denyObservedInStream` is false over the report's frames.
    - The RED commit precedes the GREEN commit; the summary quotes both shas.
    - The dry-run report written by the verify command carries a `transcript location` row for run A and run B whose value begins `runner-owned scratch`.
    - `LIVE_ALLOWED_TOOLS` is unchanged from the dispatch base: `grep -a -A11 'const LIVE_ALLOWED_TOOLS' scripts/capture-live.ts` and `git show 722d5fd6:scripts/capture-live.ts | grep -a -A11 'const LIVE_ALLOWED_TOOLS'` print the same nine entries (both quoted in the summary).
  </acceptance_criteria>
  <done>The observation channel is independent of the subject by construction, the suite proves a planted in-target file is blind, and the seam exists for the next plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Provenance by name, by content, and in the outcome (CR-02; WR-04 folded)</name>
  <read_first>
    - `scripts/capture-live.ts` — the D-05 header paragraph (routes chosen and rejected), `pluginLoadReport`, `installedPluginSha` (~L1151), `checkoutSha`, the `installedPluginSha` row in `renderReport`, the `firstPlugins` lines in `capture()` and `dryRun()`, `readMarketplaceNames`.
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` § 4 item 2 — the cache copy has no `.git`; every one of the 2 411 tracked files was byte-identical; the 914 extra files are under `node_modules/`. Content, not `git rev-parse`.
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` CR-02 and WR-04.
    - `.planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md` L15 and the `D-05 plugins loaded per system/init` claim row (run A) — `context7` is first, `grugops` fourth.
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - Test I (RED first, the wrong-entry fixture): an init frame built in memory from the committed fixture's `system/init` with its `plugins[]` reordered so index 0 is `{ name: "context7", path: "<tmp>/ctx7", version: null }` and `grugops` is second: `pluginUnderTest(pluginLoadReport(frames), "grugops")` returns the grugops entry; the premise `report.loaded[0].name !== "grugops"` is asserted in the same body.
    - Test J (cache path validation, WR-04): `pluginCachePathAccepted(p, cacheRoot)` returns the realpath for an existing directory under `cacheRoot`; returns null for a path outside `cacheRoot`, for a regular file, for a non-existent path, and for a value beginning with `-`.
    - Test K (content digest is two-sided): two temp trees with the same relative files and bytes digest equal; one byte changed in one file → different; one file missing on one side → different; the digest is independent of the order `relPaths` are given in.
    - Test L (verdict and outcome): `provenanceVerdict("abc", "abc")` is MET; `("abc", "abd")` is UNMET naming both digests; `("abc", null)` is `UNKNOWN - verify`; `deriveOutcome` with provenance UNMET or UNKNOWN and everything else clean is `fail`.
  </behavior>
  <action>
    Replace the first-entry read. Add
    `export function pluginUnderTest(report: PluginLoadReport, pluginName: string): PluginLoadReport["loaded"][number] | null`
    (find by exact `name`). Add
    `export function pluginCachePathAccepted(candidate: string, cacheRoot: string): string | null`
    — refuses a candidate beginning with `-`, resolves with `realpathSync.native`, requires a
    directory, requires containment under `cacheRoot` by the same `relative()` rule Task 2's
    `isOutsideTargets` uses (reuse it, inverted); the cache root the runner passes is
    `join(homedir(), ".claude", "plugins")`, a fixed derivation from the operator's home, not from
    argv, env or transcript content.

    Replace `installedPluginSha` with content provenance (33-DIAGNOSIS § 4.2: the cache is not a
    git checkout). Add `export function contentDigest(root: string, relPaths: readonly string[]): string`
    — sha256 over, for each path in SORTED order, the POSIX relative path, a NUL, the file's bytes (or
    the literal `MISSING` when the file does not exist under `root`), a NUL. Add
    `export type ProvenanceState = PreconditionState` (the same three states) and
    `export function provenanceVerdict(checkoutDigest: string | null, installedDigest: string | null): { state: ProvenanceState; detail: string }`.
    In `capture()`: the tracked list is `git ls-files -z` in `SCRIPT_ROOT` (through `probe`,
    split on NUL); the checkout digest is `contentDigest(SCRIPT_ROOT, tracked)`; the installed
    digest is `contentDigest(acceptedCachePath, tracked)` when `pluginCachePathAccepted` accepted the
    path the init frame of run A names for `pluginUnderTest(..., obs.pluginName)`, else null with the
    refusal in `detail`. Feed `verdict.state` into `deriveOutcome` (Task 1) — `pass` is unreachable
    unless MET. `ReportModel.installedPluginSha` becomes `provenance: { state, detail, checkoutDigest, installedDigest, trackedCount }`
    and the Run table row becomes `| installed plugin provenance (D-05, content digest over N tracked files) | <state> — <detail> |`
    with a second row `| plugin under test per system/init | <name> <version> at <path> |` or the
    `UNKNOWN - verify` sentence naming the plugin the init frame did not list. `dryRun()` fills the
    same fields; over the fixture the state is `UNKNOWN - verify` with a detail naming the refusal,
    and the outcome stays `no-go`.

    Update the D-05 header paragraph: the chosen route's post-hoc check is now "compare the cache
    copy's content with the checkout's tracked files", and the sentence about `git -C <that path>`
    is removed; correct the T-33-02 sentence to say the one transcript field that reaches a
    filesystem call is the plugin path, and that it is validated under the cache root first.

    Tests I-L RED before GREEN. Rebuild so the committed `.js` moves in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or 0 passed, or the titles for Tests I-L are absent</fails_when>
    <automated>grep -a -c 'rev-parse' scripts/capture-live.ts</automated>
    <fails_when>a count other than 3 — the three remaining uses are HEAD, the remote ref and checkoutSha; a fourth means the cache-path git call survived</fails_when>
    <automated>node scripts/capture-live.js --dry-run --out "$(mktemp -d)" &amp;&amp; npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npx tsc --noEmit</automated>
    <fails_when>non-zero exit from any command, or the dry-run output lacks OUTCOME: no-go</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/capture-live.ts` exports `pluginUnderTest`, `pluginCachePathAccepted`, `contentDigest`, `provenanceVerdict`, `ProvenanceState`; the identifier `installedPluginSha` no longer appears in the file.
    - No index-zero read of the loaded-plugins array remains: `grep -a -c 'loaded\[0\]' scripts/capture-live.ts` returns 0.
    - `deriveOutcome`'s `provenance` argument is fed from `provenanceVerdict(...).state` in `capture()`; the plan summary quotes the line.
    - The dry-run report written by the verify command carries the `installed plugin provenance (D-05, content digest over` row and the `plugin under test per system/init` row.
    - Tests I-L exist with the RED commit before the GREEN commit; the summary quotes the shas and the red titles.
    - The header's D-05 paragraph no longer describes running git inside the plugin path.
  </acceptance_criteria>
  <done>A `pass` now means the plugin that was scored is, byte for byte over every tracked file, the checkout under test — and the row that used to read `UNKNOWN - verify` says which.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the agent under test -> the file it is scored from | The subject holds `Write`/`Edit` grants over its cwd; the scored artifact must be unreachable from there (CR-01) |
| `system/init.plugins[].path` -> a filesystem call | A model-authorable transcript field reaches `realpathSync`/`readFileSync` (WR-04) |
| the marketplace's fetched sha -> the checkout under test | The two can silently differ; a `pass` over the wrong tree is a fabricated proof (CR-02) |
| the D-07 predicate -> the flip decision | A predicate that cannot be met, or one softened to be met, both corrupt D-20 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-66 | Tampering | scored transcript location | critical | mitigate | Task 2: transcript streamed into a sibling scratch outside every target and the cwd; `isOutsideTargets` asserted at run time and in the suite; Test H proves a planted in-target file is never read. |
| T-33-67 | Spoofing | plugin provenance | critical | mitigate | Task 3: plugin selected by name; cache path validated under the cache root; content digest over the checkout's tracked files compared; `pass` unreachable unless MET. |
| T-33-68 | Tampering | `system/init.plugins[].path` used as a path | high | mitigate | Task 3: `pluginCachePathAccepted` refuses `-`-prefixed values, non-directories, and anything outside the cache root before any read; no git is run inside it. |
| T-33-69 | Repudiation | D-07 verdict softened to pass the held capture | critical | mitigate | Task 1 Test A pins six named divergences over the immutable commit `c7be6d0d`; Test B proves the predicate is meetable; the replay comparator is demoted, not deleted. |
| T-33-70 | Denial of service | `git show` of a 1.6 MiB blob in the suite | low | accept | `maxBuffer` 64 MiB; a shallow clone reds loudly naming the sha rather than skipping. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; `package.json` is untouched (`git diff --exit-code 722d5fd6 HEAD -- package.json`). |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run check:build-parity` — the committed `.js` is a faithful build.
- `node scripts/capture-live.js --dry-run --out <tmp>` — exit 0, `GO-READINESS:` line, exactly one `OUTCOME: no-go`.
- `git diff --exit-code 722d5fd6 HEAD -- package.json .planning/phases/33-live-capture-windows-portability/33-CAPTURE-A.jsonl .planning/phases/33-live-capture-windows-portability/33-CAPTURE-B.jsonl .planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md` — against the round-2 dispatch base: no dependency added, the held capture unedited (D-11).
</verification>

<success_criteria>
- The held round-1 capture still reads as divergent under the corrected D-07 projection, with the six named divergences, and a synthetic at/body-only pair reads as parity.
- The scored transcript is unreachable from the subject's grants, proven by a planted-file test.
- Provenance is by name and by content, and it gates the outcome word.
- The dry run exits 0 over the new schema; the committed `.js` is rebuilt and parity-checked.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `noteRoute`, `projectLivePath`, `compareLivePaths`, `deriveOutcome` | exported functions, `scripts/capture-live.ts` | the path-invariant D-07 projection and the one outcome derivation |
| `RoleNotes`, `NoteRoute`, `PathProjection` | exported types | the projection's shape |
| `## Dual-path parity (D-07) — path-invariant projection` | report section | the heading D-18 cells will cite |
| `## Replay comparator (informational — task-id keyed, deterministic replay only)` | report section | the demoted `assertEquivalent` output |
| `isOutsideTargets`, `runTarget`, `LiveOps`, `LIVE_OPS`; `TargetBuild.transcriptDir` | exported seam | CR-01 location + the run seam plan 33-13 reuses |
| `| run X transcript location |` | report row | states the runner-owned location |
| `pluginUnderTest`, `pluginCachePathAccepted`, `contentDigest`, `provenanceVerdict`, `ProvenanceState` | exported functions/type | CR-02 + WR-04 |
| `| installed plugin provenance (D-05, content digest over N tracked files) |`, `| plugin under test per system/init |` | report rows | replace the `installed plugin sha (D-05, post hoc)` row |
| `heldCapture(name)`, `stampsFromSummary(text, run)` | test helpers, `scripts/capture-live.test.ts` | read the immutable round-1 capture from commit `c7be6d0d` |
| Tests A-L | new test cases | red-first, each named in this plan |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-12-SUMMARY.md` when done
</output>

---
phase: 33-live-capture-windows-portability
plan: 26
type: execute
wave: 15
depends_on: ["33-25"]
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/compactor.ts
  - scripts/compactor.js
  - scripts/compactor.test.ts
  - scripts/admission-server.ts
  - scripts/admission-server.js
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
autonomous: true
gap_closure: true
requirements: [CAP-01, CAP-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A `NoteInput` scalar that is ABSENT (`undefined`, or any non-string) is REFUSED BY NAME at the field guard every write route already passes through, before anything is composed — never serialized as the word `undefined` and never silently normalised to an empty string (33-DIAGNOSIS.md § 3, WINDOWS.md row 258; the no-fabrication floor: a writer does not invent a value the caller did not give)."
    - "The guard covers every interpolated scalar, not only `verified_by`: `kind`, `by`, `at`, `verified_by`, `confidence`, and `supersedes` when non-null — one guard, one sentence, because the § 3 mechanism is the guard's blindness to a non-string, not the one field the capture happened to omit."
    - "The sibling composer is closed in the same commit: the compactor's thread-tier composer interpolates the same fields and is routed through the same exported guard, so the raw tier cannot carry the word `undefined` either; the set of interpolation sites is derived by grep and its count stated."
    - "`propose_note` (the MCP admission route) keeps its present behaviour — it already coerces an absent `verified_by` to the empty string at its own boundary (`scripts/admission-server.ts:166`) — and a test proves that route still admits an unstamped soft note, so the guard changes only the in-process routes that could pass `undefined` through."
  artifacts:
    - scripts/context-io.ts
    - scripts/context-io.js
    - scripts/compactor.ts
  key_links:
    - "context-io.ts `composeValidatedNote` -> `assertNoteScalar(name, value)` (typeof guard + single-line guard) -> `composeNote`"
    - "compactor.ts `composeThreadNote` -> the same exported guard -> the thread record"
    - "admission-server.ts `propose_note` handler -> `String(args.verified_by ?? \"\")` -> `admitAndAppend` (unchanged, asserted)"
  prohibitions:
    - "an absent field is never rewritten into a value by the writer (no silent mutation)"
    - "no second copy of the scalar guard exists beside the exported one"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): this plan closes KIT § 3, accepted open for round 3 by the human; it decides nothing about the flip."
    - "CAP-02 (edge row unresolved, flagged): untouched beyond the rebuild of a module the windows leg exercises; measured only by plan 33-31."
    - "CAP-03 (edge row unresolved, flagged): the § 3 word did not touch any verdict the round-1 capture read (`capThreePredicate` reads `by`, not `verified_by`); this plan changes no predicate."
---

<objective>
Gap-closure round 3 of the phase's four-round cap. KIT § 3 of 33-DIAGNOSIS.md, accepted open for
round 3 at plan 33-21's decision checkpoint (WINDOWS.md row 258): `admitAndAppend` /
`composeNote` interpolated `${note.verified_by}` with no presence check and wrote the literal
line `verified_by: undefined` for an absent field; `"undefined"` is not in the DeLM
hollow-evidence list, so the note read back with a non-empty stamp no gate and no human set. The
diagnosis established the fault is serialization, not the caller's spelling (passing the key
explicitly `undefined` and passing it absent serialize identically).

Gap mapping (round-3 closure target 6, § 3): close with offline proof (D-11) — Task 1 the guard
and its mutation proof on the writer; Task 2 the sibling composer, the MCP route's unchanged
behaviour, and the rebuild.

Decisions honored: D-11 (offline proof), the CONTEXT no-fabrication rule (a writer that accepts an
absent field and publishes a word for it is a fabrication one register over), CLAUDE.md
single-source (one guard, exported, called by both composers).

Purpose: an absent field is a refusal with the field's name in it, on every route that composes a
note.
Output: the exported scalar guard in `scripts/context-io.ts`, the compactor routed through it,
committed twins and the manifest hash.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md
@.planning/phases/33-live-capture-windows-portability/33-25-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The field guard refuses a non-string by name, for every interpolated scalar, before anything is composed</name>
  <read_first>
    - `scripts/context-io.ts` — `assertSingleLine` (:250-256: a regex test over a value typed `string` that a non-string reaches as its `String()` form), `composeValidatedNote` (:1496-1545: the guard sequence), `composeNote` (:1430), `NoteInput` (its declared field types), `admitAndAppend` (:5897 — the in-process route the capture's `admit-notes.mjs` scripts used), the DeLM hollow-evidence list (:205).
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` § 3 — the reproduction (`admitAndAppend('T-1', { kind, by, at, confidence, refs, supersedes: null }, body, root, repoRoot)` with no `verified_by` → the file carries `verified_by: undefined`).
    - `scripts/context-io.test.ts` — the existing field-injection cases (`grep -n 'must be single-line' scripts/context-io.test.ts`) whose shape the new cases follow.
  </read_first>
  <files>scripts/context-io.ts, scripts/context-io.test.ts</files>
  <behavior>
    - Test V1 (RED first, the diagnosis's reproduction): `admitAndAppend` (and `appendNote`) with a note object that has NO `verified_by` key throws an error whose message names the field `verified_by` and the word `absent`; nothing is written (the notes directory has no new file). On the dispatch base the same call returns an id and the file carries the literal `verified_by: undefined` — quote the red run.
    - Test V2 (the same guard, every field): each of `kind`, `by`, `at`, `confidence` passed as `undefined`, and `supersedes` passed as `undefined` (not `null`), is refused naming that field; `refs` entries that are not strings are refused naming `refs[]`.
    - Test V3 (the honest empty value still writes): `verified_by: ""` composes and writes `verified_by: ` (empty), reads back as `""`, and `supersedes: null` writes the empty value as before — the guard refuses ABSENCE, not emptiness.
    - Test V4 (the guard is one function): the module's source has exactly one guard that decides both the single-line rule and the string-type rule, and every interpolated scalar in `composeValidatedNote` passes through it (derived from the AST, the writer-set idiom).
  </behavior>
  <action>
    Make the existing single-line guard the ONE scalar guard: it first refuses any value whose
    `typeof` is not `string` with a message naming the field and stating it is absent (or of the
    wrong type), then applies the newline rule it already applies. Export it (a name such as
    `assertNoteScalar`; keep the old name as the same function if other modules import it — grep
    first) so the compactor can ask the same guard in Task 2. `composeValidatedNote` already
    calls it for `kind`, `by`, `at`, `verified_by`, `confidence`, `supersedes` (when non-null) and
    each `refs[]` entry — no new call sites are needed on the shared-context route; confirm by
    reading, and add the `supersedes: undefined` case (non-null, non-string) to the guarded set.
    Tighten the `NoteInput` type only if it currently admits `undefined` for a required scalar;
    the runtime guard is the authority either way, because the in-process route is reached from
    plain JavaScript (`admit-notes.mjs`) where the type is not checked.

    Tests V1-V4, RED before GREEN (V1's red run quoted with the `verified_by: undefined` line the
    base wrote). Rebuild so the committed `.js` moves in the same commit;
    `npm run generate:hook-manifest` follows.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts -t "absent|non-string|V1|V2|V3|V4"</automated>
    <fails_when>non-zero exit, or fewer than 4 tests reported passed</fails_when>
    <automated>node -e "const m=require('./scripts/context-io.js');const fs=require('fs');const os=require('os');const p=require('path');const r=fs.mkdtempSync(p.join(os.tmpdir(),'v1-'));try{m.appendNote('T-1',{kind:'observation',by:'x',at:'2026-09-21T00:00:00Z',confidence:'low',refs:[],supersedes:null},'b',r,undefined,r);console.log('WROTE');process.exit(1)}catch(e){if(!/verified_by/.test(e.message)){console.log(e.message);process.exit(2)}}"</automated>
    <fails_when>non-zero exit — the writer accepted an absent verified_by (printed WROTE) or refused without naming the field</fails_when>
    <automated>npm run build &amp;&amp; npm run generate:hook-manifest &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>
  <acceptance_criteria>
    - The scalar guard in `scripts/context-io.ts` refuses a non-string by name before the newline test; it is exported; `composeValidatedNote` passes every interpolated scalar through it (V4 derives the set from the AST and the summary quotes the count).
    - Tests V1-V4 exist; V1's RED commit precedes the GREEN commit (shas and the base's `verified_by: undefined` line quoted).
    - The empty-string and null cases (V3) still write exactly as before (the composed bytes are byte-identical to the base's for those inputs — asserted).
    - The committed `.js`, `hooks/hook-entry.ts` and `.js` move in the same commit; parity and manifest freshness exit 0.
  </acceptance_criteria>
  <done>An absent scalar is a refusal that names the field; an empty one is still the honest empty value; the word `undefined` cannot reach a fence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The sibling composer asks the same guard, the MCP route is proven unchanged, and the interpolation-site set is counted</name>
  <read_first>
    - `scripts/compactor.ts` — `composeThreadNote` (:585-603: the second interpolation of `${note.verified_by}` and the other five scalars), `writeThread` (:555-580: the caller that guards only `task` and `agent`).
    - `scripts/admission-server.ts` :150-175 — the `propose_note` handler's `String(args.verified_by ?? "")` coercion and its call into `admitAndAppend`.
    - `scripts/admission-server.test.ts` — the existing route cases (an unstamped soft note admitted; a stampless finding refused) whose shape the unchanged-behaviour assertion follows.
    - `scripts/compactor.test.ts` — the thread-write cases (`grep -n 'writeThread' scripts/compactor.test.ts`).
  </read_first>
  <files>scripts/compactor.ts, scripts/compactor.js, scripts/compactor.test.ts, scripts/admission-server.ts, scripts/admission-server.js</files>
  <behavior>
    - Test V5 (RED first, the sibling): `writeThread` with a note object lacking `verified_by` throws naming `verified_by`; the thread file is not created or not appended to. On the base the thread record carries the literal `verified_by: undefined` — quote it.
    - Test V6 (the MCP route unchanged): a `propose_note` call whose arguments omit `verified_by` for an `observation` is ADMITTED with `verified_by` empty on disk (the server's own coercion at its boundary is the deliberate, documented behaviour of that route — an MCP argument schema default, not a writer inventing a value); the same call for a `finding` is refused by `admit()` as before.
    - Test V7 (the set is derived): the number of `${note.verified_by}` interpolation sites across `scripts/*.ts` (excluding tests) is exactly 2 and both are inside functions that call the exported guard on that field before the interpolation (asserted by reading the two function bodies from the AST).
  </behavior>
  <action>
    Import the exported guard from `./context-io.js` in `scripts/compactor.ts` and call it in
    `composeThreadNote` for the same six scalars and each `refs[]` entry, in the same order the
    shared-context route uses, before the interpolation. Do not add a second guard body.
    Confirm `scripts/admission-server.ts` needs no change (its coercion happens before
    `admitAndAppend`); if the summary must say why the MCP route may coerce while the in-process
    route may not, say it in one clear-voice sentence in the server's handler comment: the MCP
    schema declares the field optional and the server is the boundary that owns that default,
    whereas an in-process caller passing `undefined` has made no such declaration.

    Tests V5-V7 RED before GREEN for V5. Derive the interpolation-site count with
    `grep -rn 'note.verified_by' scripts/*.ts | grep -v test` and quote it. Rebuild the compactor's
    (and, if touched, the server's) committed `.js` in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/compactor.test.ts scripts/admission-server.test.ts</automated>
    <fails_when>non-zero exit, or any failed test in the summary line, or the titles V5/V6/V7 are absent</fails_when>
    <automated>grep -rln 'note\.verified_by' scripts/*.ts | grep -v '\.test\.ts' | wc -l | tr -d ' '</automated>
    <fails_when>a value other than 2 — a third composer interpolates the field, or one of the two was deleted rather than guarded</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any command, or the vitest summary reports a failed test file</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/compactor.ts` imports the guard from context-io and calls it before every interpolation in `composeThreadNote`; no local guard body exists (`grep -c 'must be single-line' scripts/compactor.ts` is 0).
    - Tests V5-V7 exist; V5's RED commit precedes its GREEN commit (shas and the base's thread line quoted); V6 shows the MCP route's behaviour unchanged for both a soft note and a finding.
    - The interpolation-site count (2) and the guarded-caller derivation are quoted in the summary.
    - The full excluded-lane suite is green once; the committed twins are rebuilt and parity-checked.
  </acceptance_criteria>
  <done>Both composers refuse an absent scalar through one guard, and the one route that legitimately defaults the field does so at its own boundary, documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an in-process caller's note object -> the composed fence | A field the caller never set must not become a published value |
| the MCP argument schema -> `admitAndAppend` | The one boundary that owns a default, stated so it is not mistaken for the writer's invention |
| the thread tier's composer -> the raw record | A second composer with a weaker guard is the sibling arm the diagnosis did not name |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-120 | Spoofing | `verified_by: undefined` read as a stamp | medium | mitigate | Task 1: the guard refuses a non-string by name before composition; V1 mutation-proven against the base. |
| T-33-121 | Tampering | the compactor's composer bypassing the guard | medium | mitigate | Task 2: one exported guard, called by both composers; V7 derives the site count. |
| T-33-122 | Repudiation | a silent normalisation of an absent field | medium | mitigate | Prohibited; V3 proves emptiness still writes as emptiness and absence refuses. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run generate:hook-manifest && npm run check:build-parity && npm run freshness:hook-manifest` — committed twins and the manifest hash faithful.
</verification>

<success_criteria>
- An absent scalar is refused by name on both composers; the honest empty value still writes; the MCP route's own default is documented and unchanged.
- The interpolation-site set is derived (2) and both sites are guarded.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| the exported scalar guard (typeof + single-line), `scripts/context-io.ts` | exported function | the one guard both composers call |
| `composeThreadNote` guarded | module behavior, `scripts/compactor.ts` | the sibling arm closed |
| Tests V1-V7 | new test cases | red-first (V1, V5) |
| the MCP-boundary default sentence | handler comment, `scripts/admission-server.ts` | why that route may coerce |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-26-SUMMARY.md` when done
</output>

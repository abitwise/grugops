---
phase: 33-live-capture-windows-portability
plan: 13
type: execute
wave: 8
depends_on: ["33-12"]
files_modified:
  - scripts/capture-live.ts
  - scripts/capture-live.js
  - scripts/capture-live.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-03, CAP-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A plugin install that does not complete stops the run before any platform child is spawned: the runner exits non-zero naming the install, and the spawn-recording seam records zero platform launches (CR-05)."
    - "The paid transcripts survive EVERY non-zero exit: the runner-owned transcript scratch directories are preserved and their paths printed whenever the exit code is not 0, and always when `--keep-target` is passed; only a clean exit without the flag removes them (CR-04)."
    - "`--keep-target` is honored on the failure path for targets and kit homes too — the flag's contract, not the exit code, decides (hard rule 3)."
    - "`--out` refuses a value that begins with `--`, so `--out --dry-run` cannot start a live capture with an artifact directory literally named `--dry-run` (IN-08 folded)."
    - "`node scripts/capture-live.js --dry-run` still exits 0 with one `OUTCOME: no-go` line (D-10)."
  artifacts:
    - scripts/capture-live.ts
    - scripts/capture-live.js
    - scripts/capture-live.test.ts
  key_links:
    - "capture-live.ts `runTarget` -> `ops.pluginInstall` (throws on non-zero) BEFORE `ops.runPlatform` -> the recorder proves the order"
    - "capture-live.ts `runAll()` finally -> `cleanupPlan(code, keepTarget)` -> `cleanupScratch(plan)` over the two scratch classes"
    - "capture-live.ts `parseArgs` -> the `--out` value guard"
  prohibitions:
    - "no platform child is ever launched after a precondition or install step reports anything but success"
    - "a transcript that cost tokens is never deleted by the runner on a failure path"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): this plan protects the artifacts a capture produces; whether the capture reaches parity is decided only by plan 33-21's go."
    - "CAP-02 (edge row unresolved, flagged): no CI leg is touched here; CAP-02 is measured by plan 33-20."
    - "CAP-03 (edge row unresolved, flagged): a run that stops on a failed install produces no CAP-03 evidence and spends nothing; the evidence itself comes from plan 33-21."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). This plan closes the two remaining Critical findings about how the runner is
REACHED and what it does around its predicates: a failed plugin install that does not stop the
spend (CR-05) and a failure path that deletes the paid transcripts even under `--keep-target`
(CR-04). Both are proven through the `runTarget` / `LiveOps` seam plan 33-12 introduced.

Gap mapping (33-VERIFICATION.md): both findings are cited by the CAP-03 gap's reason as
"the instrument's own integrity and completeness are compromised" (Required Artifacts row for
`scripts/capture-live.ts`); neither is a `missing[]` bullet of its own, so this plan is the
expansion slice that makes CAP-03's `missing[2]` ("a re-capture after both fixes land") worth
spending on. Decisions honored: D-09/D-12 (the budget is bounded and a run that cannot satisfy
CAP-03 must not spend it), D-11 (a red run is recorded — which requires the transcript to survive
the red), D-08 (one runner, committed `.js`).

Folded review item: IN-08 (`--out` consuming a following flag) belongs to the same argument-handling
code CR-04's `--keep-target` contract lives in; it lands in Task 2 and is named here so the closing
ledger does not carry it as accepted-open.

Purpose: the next live go cannot burn its budget on a target with no plugin, and cannot lose its
transcript to a post-run derivation failure.
Output: `scripts/capture-live.ts` + committed `.js`, suite extended with the seam proofs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-REVIEW.md
@.planning/phases/33-live-capture-windows-portability/33-12-SUMMARY.md
@scripts/capture-live.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A failed plugin install stops the run before the paid spawn (CR-05)</name>
  <read_first>
    - `scripts/capture-live.ts` — `pluginInstall` (~L1132), `runTarget` and `LiveOps` (plan 33-12), the phase-2 precondition wording in the header ("nothing spawns while a precondition is not MET"), `CaptureFailure`/`fail`.
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` CR-05.
    - `.planning/phases/33-live-capture-windows-portability/33-12-SUMMARY.md` — the seam's exact signature as landed.
    - `scripts/capture-live.test.ts` — Test G's recording `runPlatform` (reuse it).
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - Test M (RED first): `installOutcome({ status: 1, error: undefined, stdout: "", stderr: "boom" })` is `{ ok: false, reason }` with `reason` containing `exit 1` and `boom`; `installOutcome({ status: null, error: new Error("ENOENT") ... })` is `{ ok: false }` naming ENOENT; `installOutcome({ status: 0, ... })` is `{ ok: true, line }`.
    - Test N (the order, through the seam): `runTarget(build, run, ops)` where `ops.pluginInstall` throws `CaptureFailure` and `ops.runPlatform` is a recorder: the promise rejects with an error whose message contains `plugin install`, and the recorder's call count is 0.
    - Test O (converse): the same call with an `ops.pluginInstall` that returns an installed line and a recording `ops.runPlatform` that writes the fixture stream: the recorder's call count is 1 and the report is returned.
  </behavior>
  <action>
    Extract the decision from `pluginInstall` into a pure
    `export function installOutcome(r: { status: number | null; error: Error | undefined; stdout: string; stderr: string }): { ok: true; line: string } | { ok: false; reason: string }`.
    `pluginInstall` calls it and, on `ok: false`, calls `fail(\`plugin install ${pluginName}@${marketplaceName} did not complete (${reason})\`)`
    — a thrown `CaptureFailure`, never a returned string beginning `UNKNOWN - verify`. `runTarget`
    calls `ops.pluginInstall` as its FIRST platform-touching step and lets the throw propagate, so
    `ops.runPlatform` is unreachable after a failed install. Add one sentence to the header's
    D-05 paragraph: the install is a phase-2 precondition and a failed one stops the run before any
    model call.

    Tests M, N, O in `scripts/capture-live.test.ts`, RED before GREEN, using the seam from Test G.
    Rebuild so the committed `.js` moves in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or 0 passed, or the titles for Tests M, N, O are absent</fails_when>
    <automated>grep -a -c 'UNKNOWN - verify — `plugin install' scripts/capture-live.ts</automated>
    <fails_when>a count other than 0 — the returned-string failure shape survived in the source</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/capture-live.ts` exports `installOutcome`; `pluginInstall` throws through `fail` on `ok: false`.
    - Test N asserts the recorder count is exactly 0 after the rejection; Test O asserts exactly 1 on the converse; both are in the RED commit before the GREEN commit (shas quoted in the summary).
    - The header names the install as a phase-2 precondition that stops the run.
  </acceptance_criteria>
  <done>A target with no plugin never gets a model call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The transcript survives every failure; `--keep-target` and `--out` keep their contracts (CR-04; IN-08 folded)</name>
  <read_first>
    - `scripts/capture-live.ts` — hard rule 3 in the header, `scratch`/`makeScratch`/`cleanupScratch` (~L289-299), `runAll()`'s `finally` (~L1647), `parseArgs` (~L312-335), `TargetBuild.transcriptDir` (plan 33-12), `writeArtifacts`.
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` CR-04 and IN-08.
    - `scripts/capture-live.test.ts` — the `--dry-run` case's "no scratch directory survives" assertion and its `TMP_PREFIX` listing.
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - Test P (RED first, the truth table): `cleanupPlan(0, false)` is `{ removeTargets: true, removeTranscripts: true }`; `cleanupPlan(0, true)` is `{ false, false }`; `cleanupPlan(1, false)` is `{ removeTargets: true, removeTranscripts: false }`; `cleanupPlan(1, true)` is `{ false, false }`.
    - Test Q (the classes are separate): after `makeScratch("target-A")` and `makeScratchTranscript("A")` in a test-driven scratch registry, `cleanupScratch({ removeTargets: true, removeTranscripts: false })` removes the first and leaves the second on disk; the reverse plan does the reverse.
    - Test R (IN-08): `parseArgs(["--out", "--dry-run"])` throws naming `--out`; `parseArgs(["--out=--dry-run"])` throws the same way; `parseArgs(["--out", "/tmp/x", "--dry-run"])` parses both.
    - Test S (the dry run leaves no transcript scratch behind on success): the existing `--dry-run` integration case still asserts no `TMP_PREFIX` directory survives (a clean exit without the flag removes both classes).
  </behavior>
  <action>
    Split the scratch registry into two classes: `scratchTargets` (targets and kit homes) and
    `scratchTranscripts` (the runner-owned transcript directories plan 33-12 added). `makeScratch`
    keeps registering targets/homes; add `makeScratchTranscript(label)` registering into the second
    list (plan 33-12's `buildTarget` switches to it for `transcriptDir`). Add the pure
    `export function cleanupPlan(code: number, keepTarget: boolean): { removeTargets: boolean; removeTranscripts: boolean }`
    with the truth table Test P states — the flag decides targets; the flag OR a non-zero exit
    preserves transcripts. `cleanupScratch(plan)` takes that object. `runAll()`'s `finally` becomes
    `cleanupScratch(cleanupPlan(code, keepTarget))`, and when transcripts are preserved on a
    non-zero exit the runner prints one line per surviving directory:
    `transcript scratch preserved (exit <code>): <path>` — the operator needs the path for the
    diagnosis D-11 asks for. Rewrite hard rule 3 to state the two classes and the two contracts.

    Because plan 33-12 streams each transcript into its runner-owned directory as it arrives, no
    copy step is needed before derivation — state that in `runTarget`'s docblock in one sentence, so
    a future author does not add one.

    In `parseArgs`, refuse an `--out` value (either spelling) that begins with `--`, with a message
    naming `--out` and the offending value.

    Tests P, Q, R, S RED before GREEN. Test Q needs the registry to be exercisable from the suite:
    export `scratchRegistryForTests()` returning the two arrays, or make `cleanupScratch` accept an
    explicit registry argument defaulting to the module's — pick the smaller surface and say which
    in the summary. Rebuild so the committed `.js` moves in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or 0 passed, or the titles for Tests P, Q, R are absent</fails_when>
    <automated>grep -a -c 'cleanupScratch(code === 0' scripts/capture-live.ts</automated>
    <fails_when>a count other than 0 — the exit-code-decides shape survived</fails_when>
    <automated>node scripts/capture-live.js --dry-run --out "$(mktemp -d)" &amp;&amp; npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npx tsc --noEmit</automated>
    <fails_when>non-zero exit from any command, or the dry-run output lacks OUTCOME: no-go</fails_when>
    <automated>node scripts/capture-live.js --out --dry-run; test $? -ne 0</automated>
    <fails_when>the runner exits 0 — the flag-shaped --out value was accepted</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/capture-live.ts` exports `cleanupPlan`; the `finally` in `runAll()` reads `cleanupScratch(cleanupPlan(code, keepTarget))`.
    - Hard rule 3 in the header names two scratch classes and states that transcripts survive every non-zero exit.
    - `parseArgs` refuses a `--out` value beginning with `--`; the refusal names `--out`.
    - Tests P-S exist with the RED commit before the GREEN commit; shas and red titles quoted in the summary.
    - The existing `--dry-run` integration case still passes unchanged in its "no scratch survives" assertion.
  </acceptance_criteria>
  <done>A red run leaves its transcript on disk with the path printed; a mistyped flag cannot start a capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a failed platform operation -> the decision to spend | An install that did not complete must not be followed by a model call |
| the runner's exit path -> the paid artifact | A derivation failure after the runs must not destroy the only copy of the transcript |
| argv -> the artifact directory | A flag consumed as a path value redirects a live run's output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-71 | Denial of service | budget spent with no plugin installed | high | mitigate | Task 1: `pluginInstall` throws on non-zero; `runTarget` calls it first; Test N proves zero platform launches after the throw. |
| T-33-72 | Repudiation | transcript lost on a failure path | high | mitigate | Task 2: transcript scratch preserved on every non-zero exit and on `--keep-target`, path printed; `cleanupPlan` truth table pinned. |
| T-33-73 | Tampering | `--out` swallowing a flag | medium | mitigate | Task 2: a `--`-prefixed value is refused by name. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; `package.json` is untouched. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run check:build-parity` — committed `.js` faithful.
- `node scripts/capture-live.js --dry-run --out <tmp>` — exit 0, `OUTCOME: no-go` once.
</verification>

<success_criteria>
- A failed install produces no model call, proven through the seam.
- The transcript survives every failure path; `--keep-target` and `--out` keep their contracts.
- The dry run and the full offline suite are green; the committed `.js` is rebuilt.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `installOutcome` | exported function, `scripts/capture-live.ts` | the install decision, pure |
| `cleanupPlan`, `makeScratchTranscript`, the two scratch classes | exported function + registry | CR-04's contract |
| `transcript scratch preserved (exit <code>): <path>` | runner output line | printed on every non-zero exit |
| `--out` value guard in `parseArgs` | behavior | IN-08 |
| Tests M-S | new test cases | red-first, each named in this plan |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-13-SUMMARY.md` when done
</output>

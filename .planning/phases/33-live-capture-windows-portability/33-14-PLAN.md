---
phase: 33-live-capture-windows-portability
plan: 14
type: execute
wave: 7
depends_on: ["33-03", "33-07", "33-09"]
files_modified:
  - scripts/check-public-docs-vocabulary.ts
  - scripts/check-public-docs-vocabulary.js
  - scripts/check-public-docs-vocabulary.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`publicDocsCorpus()` publishes every member — the `examples/` walk included — spelled with forward slashes, normalized ONCE at the corpus's one member-forming site through `scripts/posix-path.ts`, never in a consumer and never in a test (D-15)."
    - "The Windows spelling is exercised and mutation-provable on this POSIX host through the separator parameter (`path.win32.sep`), the same way `scanKey` is proven in `scripts/check-banned-claims.test.ts`."
    - "Every arm that consumes the corpus is probed before the class is marked closed: the three `check-banned-claims.test.ts` titles, the seven `check-flip-manifest.test.ts` titles, `check-audit-register.ts`'s vouching set, and the vocabulary gate's own PASS/FAIL lines."
    - "No platform conditional is added anywhere (D-14, D-16)."
  artifacts:
    - scripts/check-public-docs-vocabulary.ts
    - scripts/check-public-docs-vocabulary.js
    - scripts/check-public-docs-vocabulary.test.ts
  key_links:
    - "check-public-docs-vocabulary.ts `walkFiles` acc.push -> `corpusMember(rel, sep)` -> `toPosixWith` (posix-path.ts)"
    - "publicDocsCorpus() -> check-banned-claims.ts `publicDocsMembers()` -> the dedupe `Set` and the three CI-red titles"
    - "publicDocsCorpus() -> check-flip-manifest.ts `publicDocs` part -> the derived-vs-listed comparison and the seven CI-red titles"
  prohibitions:
    - "no platform conditional closes a CAP-02 red (D-14/D-16)"
    - "a published path is never normalized in a consumer or a test instead of the module that publishes it (D-15)"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): untouched by this plan."
    - "CAP-02 (edge row unresolved, flagged): this plan closes ten of the thirty-one windows reds by mechanism reproduced on darwin through the separator seam; the CI leg's conclusion is measured only by plan 33-20."
    - "CAP-03 (edge row unresolved, flagged): untouched by this plan."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). One publishing boundary accounts for ten of the eighteen unpredicted windows
reds on CI run 35499800942: `publicDocsCorpus()` in `scripts/check-public-docs-vocabulary.ts`
builds its `examples` part from a `readdirSync` walk joined with `path.join` (`walkFiles`, L268)
while every consumer compares against POSIX spellings from `git ls-files`.

Gap mapping (33-VERIFICATION.md CAP-02 `missing[0]`): "A root-cause fix (not a platform
conditional, per D-14/D-16 prohibitions) for the `publicDocsCorpus()` examples/ member
host-separation defect (scripts/check-public-docs-vocabulary.ts:268) that alone accounts for 10 of
the 18 unpredicted windows failures." → Task 1 (the fix, red-first through the separator seam) and
Task 2 (every consuming arm probed). WINDOWS.md row 229; `deferred-items.md` item
"`publicDocsCorpus()` publishes its `examples/` members host-separated" (resolved by plan 33-23
after plan 33-20 measures it).

The exact CI titles this closes (33-CI-MEASUREMENT.md § 2.4): W-1 `the remainder of the tracked
TEXT SURFACE minus the scan is covered by an entry of the list`, W-2 `THE MISSING DIRECTION: every
scan member is a TRACKED path, and an intruder is NAMED`, W-3 `THE EQUALITY, so nothing is dropped
in silence: surfaced == admitted + excluded-by-name` (check-banned-claims.test.ts), and W-4..W-10
in check-flip-manifest.test.ts (`exits 0, names the status it read, and prints every part with the
derived total beside the pin`; `CONVERSE 1: the same cell surviving OUTSIDE the live-surface set
(a plan record, an excluded ledger) passes`; `the same surviving cell does NOT fail the gate in the
pre-capture state — before the flip those cells are correct`; `CONVERSE 2: every cell carrying a
well-formed citation to an existing summary section passes`; `CONVERSE: a GAP-D1 line WITHOUT a
deferral marker (the discharge note) passes, and so does the exempt history line`; `the flip commit
is DERIVED from history — a later commit on top does not move the comparison`; `CONVERSE: an
explicit --range over exactly the declared set passes in the discharged state`).

Purpose: one key, formed once, published everywhere (D-15) — the corpus stops publishing two
spellings of one document.
Output: the module, its committed `.js`, and a separator-parameterized proof in its own suite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
@.planning/phases/33-live-capture-windows-portability/33-03-SUMMARY.md
@scripts/check-public-docs-vocabulary.ts
@scripts/posix-path.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The corpus member is formed once, in POSIX, at the walk's one publishing site</name>
  <read_first>
    - `scripts/check-public-docs-vocabulary.ts` — `walkFiles` (~L248-275, the `acc.push(rel)` site and the `join(rel, entry)` recursion), `examplesMarkdown`, `rootMarkdown`, `refuseMarkdownImitations`, `isCanonicalMarkdown`, `PUBLIC_DOCS_CORPUS_PARTS`, `publicDocsCorpus`, `publicDocsScan`, the `CHECK_ROOT` mirror comment (~L104).
    - `scripts/posix-path.ts` — `toPosixWith(path, separator = sep)`; the header's "what this is NOT for" partition (a path that is opened is a location; a path that is published is a spelling).
    - `scripts/check-banned-claims.ts` ~L985-1010 — `scanKey(rel, separator = sep)`: the exact shape 33-03 landed and the docblock sentence about publishing sites. Mirror it.
    - `scripts/check-banned-claims.test.ts` ~L3640-3665 — the separator-independence case (`scanKey(m, win32.sep)` over every walked member): the shape of the proof.
    - `scripts/check-public-docs-vocabulary.test.ts` — the mirror harness (`CHECK_ROOT`), the pin cases (~L346-380), the 30-10 B-8 consumer-split cases (~L540-700, so the new import does not trip the consumer census).
  </read_first>
  <files>scripts/check-public-docs-vocabulary.ts, scripts/check-public-docs-vocabulary.js, scripts/check-public-docs-vocabulary.test.ts</files>
  <behavior>
    - Test T (RED first): `corpusMember("examples\\03-ticket-to-pr.md", win32.sep)` is `examples/03-ticket-to-pr.md`; `corpusMember("examples/03-ticket-to-pr.md", win32.sep)` is unchanged; `corpusMember(x, "/")` is the identity.
    - Test U (the live corpus is separator-independent): for every member `m` of `publicDocsCorpus()`, `m` contains no backslash and `m === corpusMember(m, win32.sep)`; the `examples` part has exactly the five members `git ls-files -- examples` reports, compared as sets in both directions.
    - Test V (the site, not a consumer): the source of `scripts/check-public-docs-vocabulary.ts` contains exactly one call to `corpusMember(` outside its own definition, and it is inside `walkFiles`.
  </behavior>
  <action>
    Import `toPosixWith` from `./posix-path.js`. Add, beside `walkFiles`, the one member-forming
    function `export function corpusMember(rel: string, separator: string = sep): string` that
    returns `toPosixWith(rel, separator)`, with a docblock in the shape of `scanKey`'s: this is THE
    ONE PLACE a corpus member is formed (Phase 33 / CAP-02, D-15); the walk composes locations with
    `join` (host separator) and publishes the member through this function; the fix is at member
    formation and deliberately not at any comparison; the separator is a parameter so the Windows
    spelling is mutation-provable on a POSIX host. In `walkFiles`, the `acc.push(rel)` site becomes
    `acc.push(corpusMember(rel))`; the recursion's `join(rel, entry)` stays host-spelled because it
    is a location the walk opens (posix-path.ts's own partition). `rootMarkdown` publishes bare
    names from one `readdirSync` and needs nothing; `kitReadmeMembers` and `guaranteesMembers` are
    literals already spelled with forward slashes — verify each by reading, and record the four-part
    partition in the summary.

    Tests T, U, V in `scripts/check-public-docs-vocabulary.test.ts`, RED before GREEN (Test U's
    `git ls-files` side is derived with `spawnSync("git", ["ls-files", "--", "examples"])` from the
    repository root, never typed). Rebuild so the committed `.js` moves in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-public-docs-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or 0 passed, or the titles for Tests T, U, V are absent</fails_when>
    <automated>grep -a -c 'from "./posix-path.js"' scripts/check-public-docs-vocabulary.ts</automated>
    <fails_when>a count of 0 — the module still has no import of the one normalizer</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npx tsc --noEmit</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/check-public-docs-vocabulary.ts` exports `corpusMember(rel, separator = sep)` and imports `toPosixWith` from `./posix-path.js`; the module contains no local separator-splitting one-liner.
    - `walkFiles` publishes through `corpusMember`; the recursion still composes with `join`.
    - Tests T, U, V exist; the RED commit precedes the GREEN commit (shas and red titles quoted).
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/check-public-docs-vocabulary.test.ts` reports every pre-existing case still passing, including the 30-10 B-8 consumer-split census.
  </acceptance_criteria>
  <done>The corpus has one spelling per document on every host, proven on this one.</done>
</task>

<task type="auto">
  <name>Task 2: Every arm that consumes the corpus is probed, and the census pins are re-derived rather than trusted</name>
  <read_first>
    - `scripts/check-banned-claims.ts` — `publicDocsMembers()` (~L1071), `scanKey`, `bannedClaimScanOverlap`, `isExemptRegionMember`.
    - `scripts/check-banned-claims.test.ts` — the three titles W-1..W-3 (~L2889, ~L2941, ~L2967) and the `bannedClaimScanOverlap()` pin `toBe(2)` (~L3663).
    - `scripts/check-flip-manifest.ts` ~L440 — the `publicDocs` part derivation and its derived-vs-listed comparison.
    - `scripts/check-flip-manifest.test.ts` — the seven titles W-4..W-10 (~L353, L382, L393, L425, L520, L567, L585) and the planted-repository harness.
    - `scripts/check-audit-register.ts` ~L629 — the vouching set `publicDocsScan()` UNION the derived kit.
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 rows W-1..W-10 (the assertion texts) and § 2.5 (a).
  </read_first>
  <files>scripts/check-public-docs-vocabulary.test.ts</files>
  <action>
    Run each consumer's suite and gate over the fixed module and record, in the summary, per arm:
    the command, the exit status, and the three or seven titles named above shown as passing.
    Then ask of each arm the question the project's memory records for every gap round — where
    does the changed value go NEXT, and is that arm's expectation now redundant, contradicted or
    unchanged:

    - `check-banned-claims.ts`: `scanKey` still normalizes the walk-derived KEY of its own kit walk;
      the corpus-supplied members now arrive POSIX on every host, so the dedupe `Set` collides
      correctly on Windows. `bannedClaimScanOverlap()` stays 2 on darwin; state whether the Windows
      value is now expected to be 2 as well and why (the overlap counts documents present in two
      parts under ONE spelling).
    - `check-flip-manifest.ts`: the `publicDocs` part is `publicDocsCorpus()` taken whole; the
      derived side is now POSIX, the listed side (`33-FLIP-MANIFEST.md` § 1.5) is POSIX. Run
      `node scripts/check-flip-manifest.js` over the real tree and quote its part line.
    - `check-audit-register.ts`: the vouching set unions `publicDocsScan()` with the derived kit;
      confirm the kit derivation's spelling matches (it goes through `check-kit-refs`'s `relKey`,
      33-03) and run `npm run check:audit-register` (or the script's own npm alias — read
      `package.json`), quoting the exit status.
    - The vocabulary gate itself: its PASS/FAIL lines print members; confirm by running
      `node scripts/check-public-docs-vocabulary.js` that the printed `examples/` members carry
      forward slashes on this host (they always did here — the point is the printed form is the
      published form).

    Add one case to `scripts/check-public-docs-vocabulary.test.ts` that pins the relationship, not
    an integer: the `examples` part of the corpus equals, as a set, the intersection of
    `git ls-files -- examples` with `isCanonicalMarkdown` — so a sixth example or a renamed one moves
    both sides together.

    If any pre-existing integer pin moves (IN-05 names `bannedClaimScanOverlap()`), re-derive it
    with the command in its own comment block before touching it, and record the derivation.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-banned-claims.test.ts scripts/check-flip-manifest.test.ts scripts/check-public-docs-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or any failed test file in the summary line</fails_when>
    <automated>node scripts/check-flip-manifest.js</automated>
    <fails_when>non-zero exit, or the output lacks ALL CHECKS PASSED, or the publicDocs part's derived count is not 12</fails_when>
    <automated>npm run check:banned-claims &amp;&amp; npm run check:public-docs</automated>
    <fails_when>non-zero exit from either gate</fails_when>
  </verify>
  <acceptance_criteria>
    - The summary lists, per consuming arm, the command run, its exit status, and the named CI titles now passing on this host (3 banned-claims, 7 flip-manifest).
    - `node scripts/check-flip-manifest.js` output quoted in the summary shows `publicDocs 12` and `ALL CHECKS PASSED`.
    - The new relationship case exists in `scripts/check-public-docs-vocabulary.test.ts` and derives both sides.
    - No integer pin was changed without its re-derivation command quoted; `package.json` is untouched.
  </acceptance_criteria>
  <done>The change is followed to every arm that reads it, and each arm's expectation is stated as redundant, contradicted or unchanged — none is assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a derived scan set -> every gate that counts it | Two spellings of one document make a dedupe over-count and an exemption region unrecognised, so a banned claim can pass unscanned on Windows |
| the flip gate's derived side -> the manifest's listed side | A spelling mismatch reads as "derived but not listed" and blocks the flip, or — inverted — could let a listing drift pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-74 | Tampering | banned-claims scan set on Windows | high | mitigate | Task 1: one POSIX member per document; Task 2: the exemption-region and dedupe arms probed and their titles shown passing. |
| T-33-75 | Repudiation | flip gate derived-vs-listed comparison | high | mitigate | Task 2: `check-flip-manifest.js` run over the real tree with the part line quoted; the seven converse cases passing. |
| T-33-76 | Information disclosure | a sixth example born outside the corpus | low | accept | The walk self-derives membership; the new relationship case moves both sides together. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; `package.json` untouched. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run check:build-parity` — committed `.js` faithful.
- `node scripts/check-flip-manifest.js` — `ALL CHECKS PASSED`, `publicDocs 12`.
</verification>

<success_criteria>
- The corpus publishes POSIX members from one site; the Windows spelling is proven on darwin through the separator seam.
- Every consumer arm is probed and its titles recorded; no pin moved without a derivation.
- The ten CI titles are named and expected green on the pushed run plan 33-20 measures.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `corpusMember(rel, separator = sep)` | exported function, `scripts/check-public-docs-vocabulary.ts` | the one member-forming site (D-15) |
| Tests T, U, V + the examples relationship case | new test cases | separator seam + derived-both-sides pin |
| per-arm probe record | summary section | command, exit, titles for banned-claims (3), flip-manifest (7), audit-register, the gate itself |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-14-SUMMARY.md` when done
</output>

---
phase: 33-live-capture-windows-portability
plan: 23
type: execute
wave: 12
depends_on: ["33-20", "33-21", "33-22"]
files_modified:
  - .planning/WINDOWS.md
  - .planning/phases/33-live-capture-windows-portability/deferred-items.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements: [CAP-01, CAP-02, CAP-03]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every review finding this round did not fix as its own task is a ledger row with the finding id in its description, appended through the tool and left `open` as accepted-open — never a silent drop: WR-01, WR-02, WR-03, WR-05, WR-06, WR-07, WR-08, WR-09, WR-10, WR-11, WR-12, WR-13, IN-02, IN-03, IN-04, IN-05, IN-06, IN-07 (eighteen rows); the three folded items (IN-01 → 33-12, WR-04 → 33-12, IN-08 → 33-13) are named in the summary as folded, with the plan and task."
    - "The four KIT items (33-DIAGNOSIS.md § 1.4 (a), § 1.4 (b), § 2, § 3) are ledger rows carrying the direction the human stated at plan 33-21's decision checkpoint, appended through the tool."
    - "Every `deferred-items.md` entry owned by this round carries `status: resolved` with a `Resolved by:` citation naming the plan, task, commit and — for a CAP-02 class — the CI run id from plan 33-20; an entry the measurement did not confirm stays `open` with a dated round-2 note, never a resolved line without evidence."
    - "REQUIREMENTS.md, STATE.md and the ledgers say exactly what the round's artifacts support: CAP-02 as plan 33-20 wrote it; CAP-01/CAP-03 as the flip commit wrote them or, on hold, Pending with the round-2 evidence and the hold record named."
    - "STATE.md edits are confined to the frontmatter and Current Position; the three same-line beats the wording guard scans are untouched; the guard and the longest-line check run after the write."
  artifacts:
    - .planning/WINDOWS.md
    - .planning/phases/33-live-capture-windows-portability/deferred-items.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  key_links:
    - "33-REVIEW.md finding ids -> `gsd-tools windows append --kind deviation --phase 33 ...` -> the ship gate sees each"
    - "33-21 summary's KIT decision record -> four `windows append` rows"
    - "33-20 Part 3 § 3.3 + the fix plans' summaries -> `Resolved by:` lines in deferred-items.md"
    - "STATE.md write -> `node scripts/check-foundation-guards.js` -> the three beats intact"
  prohibitions:
    - "a WINDOWS.md row is never hand-edited"
    - "no finding is dropped without a row or a named fold"
    - "a deferred item is never marked resolved without the evidence its own owner line asked for"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): this plan records the flip or the hold as plan 33-22 left it; it decides nothing about it."
    - "CAP-02 (edge row unresolved, flagged): this plan records plan 33-20's verdict; it decides nothing about it."
    - "CAP-03 (edge row unresolved, flagged): this plan records the round-2 capture's evidence as plan 33-21 left it; it decides nothing about it."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). The round's closing ledger plan: every review finding the human's scope
decision left unfixed becomes an accepted-open row with its id; the four KIT items become rows
carrying the human's stated direction; every deferred item this round owned is resolved with a
citation or honestly left open; and the three current-state ledgers say what the artifacts
support.

Gap mapping: the human's scope decision (2026-09-20) — "every other WR/IN becomes an
accepted-open ledger disposition row written by the round's closing plan (through the existing
ledger/disposition tooling, cite the finding id, never a silent drop)" → Task 1. The closure
contract's "Ledger closing plan: REQUIREMENTS.md CAP-01..03 rows, STATE.md, WINDOWS.md rows
229-235 (through the ledger tool), deferred-items.md status: resolved lines with Resolved by:
citations" → Tasks 2 and 3 (rows 229-235 are plan 33-20's; this plan records their state).

Decisions honored: D-11 (a red is recorded and the next round owns the fix — the rows are how it
is handed over), D-20 (a hold is recorded, not softened), the WINDOWS.md tool rule (D-17's
ledger-row mechanics).

Purpose: the round ends with nothing silently dropped and every ledger truthful.
Output: the ledger rows, the deferred-items resolutions, the requirement rows and the state
position.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-REVIEW.md
@.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md
@.planning/phases/33-live-capture-windows-portability/deferred-items.md
@.planning/phases/33-live-capture-windows-portability/33-20-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-21-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-22-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Twenty-two ledger rows through the tool — eighteen accepted-open review findings, four KIT items</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` — every WR-NN and IN-NN with its file and line; the three that plans 33-12/33-13 folded (IN-01, WR-04, IN-08) and their summaries' fold statements.
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` § 1.4 (a)/(b), § 2, § 3 — the four KIT findings and their offline reproductions.
    - `.planning/phases/33-live-capture-windows-portability/33-21-SUMMARY.md` — the KIT decision record (option chosen; the direction stated for (a) and (b)).
    - `.planning/WINDOWS.md` header and `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.7 — the exact `windows append` command shape (`--kind`, `--phase`, `--file`, `--line`, `--description`), and the `windows status` check afterwards.
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-*-SUMMARY.md` rows 222-224 precedent (accepted open at a close: appended, left `open`, description says so).
  </read_first>
  <files>.planning/WINDOWS.md</files>
  <action>
    Append one row per finding with `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows append --kind deviation --phase 33 --file <the finding's file> --line <its line> --description "33-REVIEW <id> accepted open (human scope decision 2026-09-20: round 2 = Criticals + CAP gaps only): <the finding's one-sentence issue> — <its proposed fix in one clause>; owner: a later round"`
    for the eighteen ids listed in the must-haves. Leave each `open` (accepted-open is a
    disposition on the description, as rows 222-224 were at the 32.1 close; `waive` is for a
    finding the project refuses, and these are deferred). Then four rows for the KIT items:
    `--kind unrun-verify` for § 1.4 (a) and (b) (the direction the human stated, from 33-21's
    record, in the description; `--file` the coordinator adapter `.claude/agents/grugops-orchestrator.md`
    line 5 for (a), `scripts/context-io.ts` the reader's admission site for (b)), and
    `--kind deviation` for § 2 (`hooks/guard.ts` + `scripts/checkpoints.ts`, the redirection /
    expansion classification with the two offline reproductions quoted) and § 3
    (`scripts/context-io.ts:1416`, `composeNote` interpolating an absent field). Quote every
    command in the summary. Derive the count: `windows status` before and after must differ by
    exactly 22 in `open_count`; state both numbers.
  </action>
  <verify>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status</automated>
    <fails_when>non-zero exit, or the counters disagree with the table's row count</fails_when>
    <automated>grep -a -c '33-REVIEW WR-\|33-REVIEW IN-' .planning/WINDOWS.md</automated>
    <fails_when>a count below 36 — eighteen findings appear once in the table and once in the JSON appendix; fewer means a row is missing from one representation</fails_when>
  </verify>
  <acceptance_criteria>
    - Eighteen `33-REVIEW <id> accepted open` rows and four KIT rows exist, appended through the tool, every command quoted in the summary; `open_count` moved by exactly 22.
    - The summary names IN-01, WR-04 and IN-08 as folded with the plan and task that folded each.
    - No hand edit: `windows status` counters agree with the table and the JSON appendix.
  </acceptance_criteria>
  <done>Nothing from the review or the diagnosis is dropped in silence; the ship gate sees each.</done>
</task>

<task type="auto">
  <name>Task 2: Deferred items resolved with citations, or honestly left open</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/deferred-items.md` — every `status: open` entry and its `Owner:` line.
    - `.planning/phases/33-live-capture-windows-portability/33-20-SUMMARY.md` and 33-CI-MEASUREMENT.md Part 3 § 3.3 — the CI verdict and, on MET, the per-row `CLOSED on run <id>` column; on NOT MET, the re-derived inventory.
    - The fix plans' summaries (33-14..33-19) for the commit shas per class; 33-16's for the symlink fixtures; 33-19's for `check-build-parity.ts`; 33-22's for the GAP-D1 item.
    - `.planning/phases/33-live-capture-windows-portability/33-11-SUMMARY.md` § how the `status: resolved` / `Resolved by:` lines were written in round 1 (the shape).
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/deferred-items.md</files>
  <action>
    For each entry owned by this round, write `status: resolved` plus a `**Resolved by:**` line
    naming the plan, task, commit sha and — for a CAP-02 class — the CI run id whose leg measured
    it green, ONLY when that evidence exists: the `publicDocsCorpus()` item (33-14; run id), the
    ten context-io reds (33-15/33-16; run id), the two uat-spec-integrity windows reds (33-17;
    run id), the two ubuntu reds (33-17; run id), the o-prefix red (33-19; run id), the
    uat-gate-exit-contract four (33-18; run id), the three incomplete fixes (33-18 W-12, 33-16
    W-28 / 33-15 W-27, 33-19 W-29; run id), the symlink fixtures (33-16; a mechanism item —
    resolved on the commit, the run id cited as the confirming measurement where the leg ran the
    file), `check-build-parity.ts:132` (33-19; commit; the ubuntu step's exit quoted from the
    run's per-step table), the board-watch-live debounce item (resolved only if the file was
    green on both legs of the round-2 run — cite it; otherwise leave open with the round-2
    measurement noted), and the GAP-D1 HELD item (resolved by 33-22's flip commit, or updated
    with a dated round-2 note pointing at the manifest's § 7 if held).

    If plan 33-20 read NOT MET, every CAP-02 class whose leg is still red stays `open` with a
    dated `Round 2:` line quoting the re-derived inventory row for it. The stale-plugin-rows item
    stays `open` (owner: the human) unless 33-21's post-run listing was checked — if it was, quote it.

    Derive the count: the number of `status: open` lines before and after, and the number of
    `Resolved by:` lines added, stated in the summary with the equality (before − resolved = after).
  </action>
  <verify>
    <automated>grep -a -c 'status: resolved' .planning/phases/33-live-capture-windows-portability/deferred-items.md</automated>
    <fails_when>a count below 3 — round 1 already carried two resolved entries; this round must add at least one, and the summary's equality must reconcile the exact number</fails_when>
    <automated>grep -a -B1 -A1 'status: resolved' .planning/phases/33-live-capture-windows-portability/deferred-items.md | grep -a -c 'Resolved by:'</automated>
    <fails_when>a count lower than the resolved count — a resolved line without a citation</fails_when>
  </verify>
  <acceptance_criteria>
    - Every entry owned by this round is either `status: resolved` with a `Resolved by:` citation carrying the plan, task, commit and (for CI classes) run id, or `open` with a dated round-2 note quoting the evidence for leaving it open.
    - The summary states the before/after open counts and the equality.
    - No resolved line cites evidence that does not exist (the run id matches Part 3 § 3.2; the commit shas resolve with `git cat-file -t`).
  </acceptance_criteria>
  <done>The deferred list is a truthful record of what this round closed and what it handed on.</done>
</task>

<task type="auto">
  <name>Task 3: The requirement rows and the project position agree with the artifacts</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` rows CAP-01, CAP-02, CAP-03 (L140-142 checkboxes; ~L226-228 coverage table) as plan 33-20 and the flip commit (33-22) left them.
    - `.planning/STATE.md` — the frontmatter and `## Current Position` (~L31-35) ONLY; the three same-line beats the `oracleWr05Wording` guard scans (`dropped`+`Phase 8`, `guard_wr05`+`Phase 10`, `re-verified GREEN`+`Phase 11`) and the escape-doubling hazard (the state writer re-escapes backslashes; check the longest line after every write).
    - `.planning/phases/33-live-capture-windows-portability/33-22-SUMMARY.md` — flip or hold.
    - `.planning/phases/33-live-capture-windows-portability/33-21-SUMMARY.md` — the outcome word, costs, durations, provenance state.
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    REQUIREMENTS.md: CAP-02 stays as plan 33-20 wrote it (do not touch). On the flip branch,
    CAP-01 and CAP-03 were flipped inside the flip commit (F44, F45) — verify by reading and do not
    touch. On the hold branch, update the two coverage-table rows' `Pending` text to name the
    round-2 evidence: the capture date, the outcome word, the parity section's named diffs (or
    the `no-go`), the provenance state, the hold record (`33-FLIP-MANIFEST.md` § 7) and the round
    (2 of 4); CAP-03's row also states that the round-2 instrument is independent (CR-01) and
    provenance-checked (CR-02) and that the observation stands as evidence pending the flip.
    Capture-backed and run-backed citations are never mixed.

    STATE.md: edit ONLY the frontmatter and `## Current Position` — the phase line becomes
    `Phase: 33 (...) — gap-closure round 2 of 4 EXECUTED <date>, awaiting verification` and the
    plan line names 33-12..33-23, the CI verdict (run id, both conclusions), the capture outcome
    word (or no-go), and the flip/hold. Keep every line short (no path-heavy prose); after the
    write run the foundation guards and measure the longest line.
  </action>
  <verify>
    <automated>node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit, or any FAIL line naming the state wording oracle</fails_when>
    <automated>awk '{ if (length($0) > m) m = length($0) } END { print m }' .planning/STATE.md</automated>
    <fails_when>a value above 4000</fails_when>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs query state.load</automated>
    <fails_when>non-zero exit, or a parse error reported for the state document</fails_when>
    <automated>S=$(git log -1 --format=%H -- .planning/STATE.md) &amp;&amp; test -n "$S" &amp;&amp; git show --format= "$S" -- .planning/STATE.md > "${TMPDIR:-/tmp}/state-33-23.diff" &amp;&amp; test -s "${TMPDIR:-/tmp}/state-33-23.diff" &amp;&amp; test "$(grep -a -cE '^[-+].*(guard_wr05.*Phase 10|re-verified GREEN.*Phase 11|dropped.*Phase 8)' "${TMPDIR:-/tmp}/state-33-23.diff" | tr -d ' ')" = "0"</automated>
    <fails_when>non-zero exit — the last commit touching STATE.md (this task's write) cannot be resolved, produced an empty diff, or changed a guarded beat line</fails_when>
  </verify>
  <acceptance_criteria>
    - CAP-02's rows are byte-unchanged from plan 33-20's write; CAP-01/CAP-03 rows are as the flip commit wrote them or carry the round-2 hold text with the evidence named.
    - STATE.md's diff touches only the frontmatter and Current Position; the guards exit 0; the longest line is below the ceiling; the loader parses it.
    - The summary quotes the three requirement rows as they read at the end of the round.
  </acceptance_criteria>
  <done>The project's current-state ledgers say exactly what round 2 produced, and the next round starts from a truthful position.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a review finding -> the ledger | A finding without a row is a silent drop the ship gate never sees |
| a deferred item -> a resolved line | A resolved line without evidence is a fabricated closure |
| the state document -> the wording guard | A touched beat or a pathological line turns a sub-second guard into minutes or a false red |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-107 | Repudiation | silently dropped review findings | high | mitigate | Task 1: one row per id, count asserted (+22), folds named. |
| T-33-108 | Repudiation | a resolved deferred item without evidence | high | mitigate | Task 2: every `Resolved by:` names plan, task, commit and run id; the equality of counts stated. |
| T-33-109 | Denial of service | STATE.md guard beats / longest line | medium | mitigate | Task 3: frontmatter + Current Position only; guard and length check after the write; the diff grep over the beats. |
| T-33-110 | Tampering | the ledger's three representations | medium | mitigate | Rows only through the tool; `windows status` asserted. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; ledgers only. |
</threat_model>

<verification>
- `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status` — counters agree; `open_count` moved by exactly 22 across Task 1.
- `node scripts/check-foundation-guards.js` and the STATE.md longest-line check — after every STATE.md write.
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the tree is still green at the round's close.
</verification>

<success_criteria>
- Twenty-two rows through the tool; three folds named; every deferred item resolved with a citation or left open with a dated reason.
- The requirement rows and the state position are truthful to the round's artifacts; the guards are green.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| WINDOWS.md rows for WR-01..03, WR-05..13, IN-02..07 (18) | ledger rows via tool | accepted open, finding id in the description |
| WINDOWS.md rows for 33-DIAGNOSIS § 1.4 (a), § 1.4 (b), § 2, § 3 (4) | ledger rows via tool | the human's stated direction from 33-21 |
| `Resolved by:` lines in deferred-items.md | record changes | plan, task, commit, run id |
| CAP-01/CAP-03 hold text (hold branch) | ledger change | round-2 evidence named |
| STATE.md Current Position | state change | round 2 of 4 executed |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-23-SUMMARY.md` when done
</output>

---
phase: 33-live-capture-windows-portability
plan: 22
type: execute
wave: 11
depends_on: ["33-07", "33-11", "33-21"]
files_modified:
  - .planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md
  - examples/03-ticket-to-pr.md
  - docs/dogfood-human-runbook.md
  - docs/audit/28-disposition-register.md
  - .planning/milestones/v1.0-phases/06-validation-brand-dogfood/06-HUMAN-UAT.md
  - .planning/milestones/v1.0-phases/06-validation-brand-dogfood/06-VERIFICATION.md
  - .planning/milestones/v1.2-phases/19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2/19-VERIFICATION.md
  - .planning/milestones/v2.0-phases/20-shared-context-substrate-concurrency-foundation/20-HUMAN-UAT.md
  - .planning/phases/27-spawn-correctness-kit-set-authority/27-SPAWN-03-RUNTIME-EVIDENCE.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/WINDOWS.md
  - .planning/PROJECT.md
autonomous: false
gap_closure: true
requirements: [CAP-01, CAP-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The flip happens only when the round-2 capture's outcome line reads `pass` (parity empty, provenance MET, both CAP-03 sides, deny observed) AND plan 33-20's CI verdict reads MET — because the 20-HUMAN-UAT rows F38-F42 cite the CI run and the manifest's one-commit rule cannot be met truthfully otherwise (D-17, D-20)."
    - "On any other outcome word, or a `no-go`, nothing flips: the manifest gains a § 7 hold record, the disposition register a dated note, the item stays open, and round 3 owns the fix — with no checkpoint re-asking the hold (D-20)."
    - "The flip is ONE commit touching exactly the manifest's declared set — fourteen files including `.planning/PROJECT.md`, which plan 33-11's file list omitted — and the gate's commit-set rule accepts it in its enforcing mode (D-17)."
    - "Every flipped cell carries the observed value, the capture date and a citation naming a heading that exists in the round-2 `33-CAPTURE-SUMMARY.md`; both parity columns are replaced from the capture (D-18, D-19)."
    - "Ledger rows change only through the tool; STATE.md is checked by the foundation guards and its longest line measured after every write."
  artifacts:
    - .planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md
    - examples/03-ticket-to-pr.md
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md
  key_links:
    - "33-CAPTURE-SUMMARY.md (round 2) headings -> every flipped cell's citation -> check-flip-manifest.ts citation rule"
    - "33-FLIP-MANIFEST.md declared set (14) <-> `git diff-tree` of the flip commit -> the commit-set rule"
    - "33-CI-MEASUREMENT.md Part 3 § 3.3 -> F38-F42 (20-HUMAN-UAT) citations -> never the capture summary"
    - "STATE.md write -> `node scripts/check-foundation-guards.js` -> the three same-line beats intact"
  prohibitions:
    - "a parity cell without a capture citation is a fabrication"
    - "a WINDOWS.md row is never hand-edited"
    - "no document describing a past state is rewritten to make a gate green"
    - "the D-07 predicate is never softened to make a capture read as parity (D-20)"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): discharged only by this plan's flip branch; held otherwise."
    - "CAP-02 (edge row unresolved, flagged): read from plan 33-20's verdict; it gates F38-F42 and therefore the flip."
    - "CAP-03 (edge row unresolved, flagged): its requirement row (F45) flips only in the flip commit; on hold it stays Pending with the round-2 evidence named."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). The one-way GAP-D1 flip, performed against the manifest plan 33-07 declared
and the gate it built, exactly as plan 33-11 wrote it — plus the one file that list omitted
(`.planning/PROJECT.md`, rows F59-F62) — or the honest hold.

Gap mapping (33-VERIFICATION.md CAP-01 `missing[2]`): "The flip commit itself, touching exactly
the 14 files the manifest declares (F1-F62, C1-C10), including .planning/PROJECT.md ..." → Task 2
(flip branch). The hold branch (D-11/D-20) → Task 3. The one-way rating (D-17) → Task 1's decision
checkpoint, presented ONLY when the outcome word is `pass` — on any other word the hold is
mechanical and the round-1 decision is not re-asked.

Decisions honored: D-17 (one edit, every surface, one commit, manifest-driven), D-18 (cell form:
value, date, `33-CAPTURE-SUMMARY.md` § heading), D-19 (both parity columns replaced; relay
vocabulary retired), D-20 (nothing flips until parity holds), D-13 (the Phase-20 rows cite the
CI run, never the capture).

Purpose: close the oldest item with evidence, or hold it honestly — through a gate, not a mood.
Output: one flip commit over exactly the declared set with the gate green in enforcing mode, or a
§ 7 hold record.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md
@.planning/phases/33-live-capture-windows-portability/33-11-PLAN.md
@.planning/phases/33-live-capture-windows-portability/33-11-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-21-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-20-SUMMARY.md
@examples/03-ticket-to-pr.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Checkpoint — authorize the one-way GAP-D1 flip from the round-2 capture (presented only on OUTCOME: pass)</name>
  <decision>Perform the one-way GAP-D1 flip now, from the round-2 capture, or hold</decision>
  <context>
    GATE ON THIS CHECKPOINT. The executor reads the outcome word from
    `.planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md` (or the `no-go`
    recorded in 33-21's summary) BEFORE presenting this. If the word is not `pass`, this
    checkpoint is NOT presented: D-20 decides, the round-1 hold is not re-asked, and Task 3 runs.

    If it IS `pass`: the summary's `Dual-path parity (D-07) — path-invariant projection` section
    reads `parity: the two projections are equal`, its provenance row reads MET, both CAP-03
    verdicts read `both sides hold`, and the D-04 deny was observed on the hook channel in both
    runs (each quoted here by the executor from the summary, with the section heading).

    The second gate is CAP-02: the manifest's rows F38-F42 flip `20-HUMAN-UAT.md` citing the CI
    run id in `33-CI-MEASUREMENT.md` Part 3 § 3.3, and the commit-set rule requires that file in
    the one commit. The executor quotes § 3.3's verdict line here. If it reads NOT MET, the flip
    cannot be performed truthfully as one commit over the declared set; the honest option is
    `hold-for-ci` and round 3 flips after CI is green.

    This is rated ONE-WAY (D-17): requirement status changes across four milestones' records;
    undoing it is a second manifest-driven edit plus a status reversal, not a revert.
  </context>
  <options>
    <option id="flip">
      <name>Flip now, from the round-2 capture</name>
      <pros>Discharges GAP-D1 (open since 2026-06-16) with a committed, cited artifact produced by an instrument the subject could not write to and a plugin whose content was verified; retires the stale left parity column and the relay vocabulary; closes four milestones' carried rows in one gate-checked commit.</pros>
      <cons>One-way across four milestones' records; every flipped cell inherits this capture's authority.</cons>
    </option>
    <option id="hold-for-ci">
      <name>Hold — the capture passed but CAP-02 is NOT MET, so F38-F42 cannot be flipped truthfully</name>
      <pros>Keeps the one-commit rule and the CI-citation rule both honest; round 3 flips after a green run.</pros>
      <cons>GAP-D1 stays open one more round with the capture already in hand.</cons>
    </option>
    <option id="hold">
      <name>Hold — record the capture and leave the item open</name>
      <pros>Correct if you doubt the capture on a ground the gate does not measure; the capture stays filed as round 3's input.</pros>
      <cons>GAP-D1 stays open; the four-round cap has two rounds left.</cons>
    </option>
  </options>
  <resume-signal>Select: flip, hold-for-ci, or hold</resume-signal>
</task>

<task type="auto">
  <name>Task 2: The one flip commit — fourteen files, every cell cited, the gate green in enforcing mode</name>
  <precondition>Task 1 selected `flip`: the round-2 summary's outcome line reads `pass` with the parity section reporting equal projections and provenance MET, and 33-CI-MEASUREMENT.md Part 3 § 3.3 reads `CAP-02 verdict on this run: MET`.</precondition>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-11-PLAN.md` Tasks 2 and 3 — the instruction text this task executes as written (cell form, both parity columns, the correction class, the runtime-evidence block, the ledger tool, the longest-line check, the enforcing-mode gate run and the residual-set handling).
    - `.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md` — § 2.1 (the declared set: fourteen files), § 2.3 rows F1-F62 (F43 the runtime-evidence marker; F53-F58 the six ledger rows by id; F59-F62 `.planning/PROJECT.md`), § 3 rows C1-C10, § 4 (the citation form), § 6 (the hold record — stays as history; the status line above it flips).
    - `.planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md` (round 2) — every observed value and every section heading the citations must name, including the new `Dual-path parity (D-07) — path-invariant projection` heading.
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` Part 3 § 3.2-3.3 — the run id F38-F42 cite.
    - `examples/03-ticket-to-pr.md` L16, L182-L193; `docs/audit/28-disposition-register.md` § the example's entry (33-11's hold note is there and stays; the F14 marker is added beneath it); `docs/dogfood-human-runbook.md` L175, L178, L203.
    - `.planning/WINDOWS.md` header (the tool); `.planning/STATE.md` — the three same-line beats the `oracleWr05Wording` guard scans (`dropped`+`Phase 8`, `guard_wr05`+`Phase 10`, `re-verified GREEN`+`Phase 11`) are never touched; the longest-line hazard.
    - `.planning/PROJECT.md` L51, L175, L239, L241 (F59-F62) and its exempt history anchor (§ 1.4).
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md, examples/03-ticket-to-pr.md, docs/dogfood-human-runbook.md, docs/audit/28-disposition-register.md, .planning/milestones/v1.0-phases/06-validation-brand-dogfood/06-HUMAN-UAT.md, .planning/milestones/v1.0-phases/06-validation-brand-dogfood/06-VERIFICATION.md, .planning/milestones/v1.2-phases/19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2/19-VERIFICATION.md, .planning/milestones/v2.0-phases/20-shared-context-substrate-concurrency-foundation/20-HUMAN-UAT.md, .planning/phases/27-spawn-correctness-kit-set-authority/27-SPAWN-03-RUNTIME-EVIDENCE.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/WINDOWS.md, .planning/PROJECT.md</files>
  <action>
    Execute plan 33-11's Task 2 as written against the manifest, with ONE amendment already
    recorded in the manifest's § 6: `.planning/PROJECT.md` is in the declared set (F59-F62) and is
    edited in the same commit. The commit's changed-file set equals the fourteen declared files
    exactly; the plan summary and the gate output land in a separate follow-up commit. One
    truthfulness amendment to the manifest itself, in the same commit and recorded under § 5's
    rule with its reason: row F14's marker literal names the plan that performs the flip, and that
    plan is now this one — change the literal's plan number to 33-22 in the manifest's row and
    write the register note with that spelling, so the note is true about who discharged it.

    Cells take the § 4 form; every value is read from the round-2 summary; the parity section's
    heading for the D-07-backed cells is the new `Dual-path parity (D-07) — path-invariant projection`
    (the citation rule resolves headings that exist in the summary — confirm the exact text by
    reading the summary, never from this plan). F38-F42 cite the CI run id from Part 3 § 3.3 and
    never the capture summary. The six ledger rows (F53-F58) are marked through
    `gsd-tools windows fixed <id> "<reason citing the summary section>"` with every command quoted.
    After the STATE.md write: `awk` the longest line (below 4000) and run
    `node scripts/check-foundation-guards.js` (the three same-line beats must survive).

    Then execute 33-11's Task 3 as written: run the gate in enforcing mode over the flip commit,
    quote its full output (each part's count beside the pin, declared vs actual set, the citation
    check per cell, the residual rule), handle a surviving cell by reading (a live surface flips
    and the manifest is amended with the reason; history is never rewritten), and re-run the
    repository's document gates over the changed public documents.
  </action>
  <verify>
    <automated>node scripts/check-flip-manifest.js</automated>
    <fails_when>non-zero exit, or the output names an undeclared changed file, an omitted declared file, a cell without a citation, a citation naming a summary section that does not exist, or a surviving cell inside the live-surface set</fails_when>
    <automated>test "$(git show --name-only --format= "$(git log -1 --format=%H -- examples/03-ticket-to-pr.md)" | sort | wc -l | tr -d ' ')" = "14"</automated>
    <fails_when>non-zero exit — the flip commit (derived as the last commit touching the parity example, which only the flip touches this round) does not list exactly fourteen files, or the commit cannot be resolved</fails_when>
    <automated>node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit, or any FAIL line naming STATE.md's wording oracle</fails_when>
    <automated>awk '{ if (length($0) > m) m = length($0) } END { print m }' .planning/STATE.md</automated>
    <fails_when>a value above 4000</fails_when>
    <automated>npm run check:banned-claims &amp;&amp; npm run check:public-docs &amp;&amp; npm run check:claim-anchors &amp;&amp; npm run check:residual-citations &amp;&amp; npm run check:diff-disposition &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any gate in the chain</fails_when>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status</automated>
    <fails_when>non-zero exit, or the counters disagree with the table's row count</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --name-only` of the flip commit lists exactly the fourteen declared files, `.planning/PROJECT.md` among them; the summary quotes both lists side by side.
    - Every flipped cell carries a value, a date and a citation naming an existing heading of the round-2 summary; F38-F42 cite the CI run id; the gate confirms all of it in enforcing mode with `ALL CHECKS PASSED`.
    - Both parity columns are replaced; the relay vocabulary and the handoff-filenames row are retired (D-19); the correction rows C1-C10 carry no capture citation.
    - The six ledger rows are marked through the tool with the commands quoted; `windows status` agrees.
    - STATE.md's three guarded beats are intact (guard exit 0) and its longest line is below the ceiling.
    - No document describing a past state was rewritten; any manifest amendment is recorded with its reason.
  </acceptance_criteria>
  <done>One commit, exactly the declared set, every cell traceable to a heading of a committed capture that an independent instrument produced.</done>
  <reversibility rating="one-way">D-17 rates the flip one-way: it changes requirement status across four milestones' records, and reversing it is a second manifest-driven edit plus a status reversal rather than a revert. Gated by the decision checkpoint above.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The hold branch — record, keep every surface as it is, hand the fix to round 3</name>
  <precondition>The round-2 outcome word is not `pass`, or 33-21 recorded `no-go`, or Task 1 selected `hold` or `hold-for-ci`.</precondition>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md` § 6 — the round-1 hold record's shape (decision, evidence, what changes and does not, the path back).
    - `.planning/phases/33-live-capture-windows-portability/33-11-SUMMARY.md` — how the round-1 hold was recorded and which files it touched (the manifest and the disposition register only).
    - `.planning/phases/33-live-capture-windows-portability/33-R2-DIAGNOSIS.md` (if the capture ran red) or 33-21's summary (if no-go) — the evidence the hold rests on.
    - `docs/audit/28-disposition-register.md` § the example's entry — the round-1 dated note; the F14 marker literal must NOT be written.
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md, docs/audit/28-disposition-register.md</files>
  <action>
    Append `## 7. Hold record (round 2, plan 33-22, <date>)` to the manifest in § 6's shape: the
    decision (`hold`, and whether it was mechanical under D-20 or chosen at Task 1 with the option
    id), the evidence (the outcome word and the section that set it — the parity diffs verbatim,
    or the provenance state, or the `no-go` reason), what this record does and does not change
    (every F and C row untouched; status line still `pre-capture`; the gate re-run over this tree
    reports the same derivation — quote it), and the path back: round 3 owns the KIT items the
    human dispositioned at 33-21's Task 2, and a fresh go. Add a dated note beneath the round-1
    note in the disposition register saying the overlap is still not discharged and pointing at
    § 7, without the F14 marker literal.

    Run the gate afterwards and quote its output; every F row's pre-flip anchor must still resolve.
  </action>
  <verify>
    <automated>node scripts/check-flip-manifest.js</automated>
    <fails_when>non-zero exit, or the output lacks ALL CHECKS PASSED, or the status it names is not pre-capture</fails_when>
    <automated>grep -a -c '^## 7. Hold record' .planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md</automated>
    <fails_when>a count other than 1</fails_when>
    <automated>grep -a -c 'Discharged by plan 33-' docs/audit/28-disposition-register.md</automated>
    <fails_when>a count above 0 — an F14-shaped marker was written on the hold branch</fails_when>
  </verify>
  <acceptance_criteria>
    - The manifest carries exactly one § 7 hold record in § 6's shape with the evidence quoted; its status line still reads `pre-capture`; the gate exits 0 over the tree.
    - The disposition register's new note carries no F14 marker literal.
    - No other file changed in this branch (`git show --name-only` of the hold commit lists the two files).
  </acceptance_criteria>
  <done>The item stays open with the round's evidence filed and the next round's work named — nothing softened, nothing flipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a captured run -> four milestones' requirement records | One artifact's authority propagates across records closed far apart |
| the manifest -> the commit | A declared set that drifts lets a surface flip unchecked or an unrelated file ride along |
| the CI verdict -> the Phase-20 rows | Flipping F38-F42 on a red run would cite evidence that does not exist |
| a ledger's three representations -> a hand edit | A hand edit desynchronises a counter block, a table and a JSON appendix |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-102 | Repudiation | flipped cells | critical | mitigate | Every cell: value, date, existing heading; the gate refuses otherwise; commit set checked both directions (Task 2). |
| T-33-103 | Tampering | the one-way flip | critical | mitigate | Task 1's decision checkpoint, presented only on `pass`; the hold branch is mechanical on any other word (D-20). |
| T-33-104 | Repudiation | F38-F42 citing a red CI run | high | mitigate | Task 2's precondition requires 33-20's MET; `hold-for-ci` is the option otherwise. |
| T-33-105 | Tampering | the ledger's representations | medium | mitigate | Rows change only through the tool; `windows status` asserted. |
| T-33-106 | Denial of service | STATE.md guard performance / wording oracle | medium | mitigate | Longest line measured; `check-foundation-guards.js` run after every STATE.md write; the three beats never touched. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; documents and ledgers only. |
</threat_model>

<verification>
- `node scripts/check-flip-manifest.js` — enforcing mode exit 0 over the flip commit (flip branch) or pre-capture exit 0 (hold branch).
- `git show --name-only` of the flip commit — exactly fourteen files.
- `node scripts/check-foundation-guards.js` and the STATE.md longest-line check — after every STATE.md write.
- `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status` — the ledger's representations agree.
</verification>

<success_criteria>
- The flip is one commit over exactly the declared set with the gate green in enforcing mode, every cell cited to the round-2 capture and the Phase-20 rows to the CI run — or the hold is recorded in § 7 with the evidence and nothing flipped.
- The one-way door is walked only behind the human's decision, and only on `pass`.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| the flipped parity table in `examples/03-ticket-to-pr.md` | modified public document (flip branch) | both columns replaced, relay vocabulary retired (D-19) |
| the filled runtime-evidence block | modified record (flip branch) | pasted from the round-2 summary's printed block |
| the corrected cell-count sentences C1-C10 | modified archived records (flip branch) | no capture citation |
| flipped requirement/state/roadmap/project rows F44-F52, F59-F62 | modified ledgers (flip branch) | one artifact cited per row |
| WINDOWS.md rows 1, 183, 211-214 `fixed` | ledger changes via tool (flip branch) | F53-F58 |
| the manifest's `discharged` status | modified declaration (flip branch) | the residual and commit-set rules in force |
| `## 7. Hold record (round 2, ...)` | manifest section (hold branch) | the evidence and the path back |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-22-SUMMARY.md` when done
</output>

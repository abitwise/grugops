---
phase: 33-live-capture-windows-portability
plan: 20
type: execute
wave: 9
depends_on: ["33-14", "33-15", "33-16", "33-17", "33-18", "33-19"]
files_modified:
  - .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: false
gap_closure: true
requirements: [CAP-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CAP-02 is measured by ONE pushed CI run whose own `conclusion` fields for `test (ubuntu-latest)` and `test (windows-latest)` are quoted from `gh run view --json`; the requirement flips only when BOTH read `success` (D-13)."
    - "Measurement and fix never share a plan: this plan changes no source file, and a red leg becomes the next round's inventory, never a fix here (D-11's shape, as 33-09 applied it)."
    - "Before the push, every named CI title this round's fix plans claim to close is listed with its local result, and the classes this host cannot measure are named as such."
    - "The push is behind a named human confirmation; the executor never pushes on its own (the repository's hard safety rule)."
    - "Ledger rows change only through the tool; the three representations are checked to agree afterwards."
  artifacts:
    - .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
    - .planning/REQUIREMENTS.md
    - .planning/WINDOWS.md
  key_links:
    - "the pre-push inventory (Part 3 § 3.1) -> the human push -> `gh run view <id> --json databaseId,headSha,conclusion,jobs` -> Part 3 § 3.2 -> the CAP-02 row"
    - "the run's `success`/`failure` words -> `gsd-tools windows fixed <id>` for rows 229-235 and 186, `windows waive 193` (green only)"
  prohibitions:
    - "no platform conditional closes a CAP-02 red (D-14/D-16)"
    - "no fix is applied and re-pushed inside this plan; a red leg is inventoried and handed to round 3"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "a WINDOWS.md row is never hand-edited"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): untouched by this plan; the go in plan 33-21 needs this plan's push (HEAD == origin/main), which is the only coupling."
    - "CAP-02 (edge row unresolved, flagged): resolved to `success` or `failure` by this plan's pushed run and nothing else."
    - "CAP-03 (edge row unresolved, flagged): untouched by this plan."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). This is the CAP-02 measurement: one pushed CI run, read from its own metadata,
after every fix plan of waves 7 and 8 has landed. It is its own plan because a measurement and a
fix never share one (33-09's shape; D-11).

Gap mapping (33-VERIFICATION.md CAP-02 `missing[3]`): "A second pushed CI run whose own
conclusion field reads success on both legs." → Task 3, behind Task 2's human push. Task 1 files
the pre-push inventory so the run decides something the document already predicted.

Decisions honored: D-13 (both legs), D-14/D-16 (no conditional; a red is a measurement), D-11
(record, diagnose, next round fixes). The pushed-sha wrinkle from the project's memory: the live
runner in plan 33-21 refuses unless HEAD equals `origin/main`, so this push is also that plan's
precondition — but a further push will be needed before the go, because plan 33-21 commits its
own pre-go artifacts.

Purpose: turn "every class closed by mechanism on darwin" into a conclusion word from the runner
that matters.
Output: 33-CI-MEASUREMENT.md Part 3 (pre-push inventory, the run, the per-leg conclusions,
dispositions), the CAP-02 requirement row, and the ledger rows this run answers.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
@.planning/phases/33-live-capture-windows-portability/33-09-PLAN.md
@.planning/phases/33-live-capture-windows-portability/33-09-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-14-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-15-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-16-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-17-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-18-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-19-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The pre-push inventory — every claimed title, every gate, every unmeasurable class, before the push</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` Part 1 (§ 1.1 the workflow commands in order; § 1.4 the per-class attribution shape; § 1.5 "what the pushed run will decide, stated before the push") and Part 2 § 2.4 (the 33 red rows this round claims to close: U-1, U-2, W-1..W-31).
    - `.github/workflows/ci.yml` — the step order and which steps are ubuntu-scoped.
    - The six fix-plan summaries (33-14..33-19) — each names the titles it closed and the arms it probed.
    - The project's prohibition on `npm test` (the live lane); the vitest command is the workflow's own with the `scripts/e2e` exclusion.
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md</files>
  <action>
    Append `## Part 3 — round 2: the pre-push inventory (Task 1)` to the measurement document.
    Run every workflow command locally in the workflow's order (the Part 1 § 1.1 list: install,
    freshness, build + parity, typecheck, platform shapes, the excluded-lane vitest run, the
    chained repository gates) and record each with its exit status. Then a table with one row per
    § 2.4 red (33 rows: U-1, U-2, W-1..W-31): the closing plan, the mechanism in one clause, the
    LOCAL result of running that exact title on this host (`npx vitest run --exclude '**/scripts/e2e/**' <file> -t "<title>"`,
    exit status quoted), and a column stating whether the windows outcome is measurable here
    (a separator-seam case is; an 8.3 short-name or a `CreateProcess` cwd bound is not — say
    `unmeasured locally, by construction`). State the expectation the run will decide, in one
    sentence per leg, before the push — and the prediction's own falsifier: any red on either leg
    is this round's finding, and a red among the 33 is a mechanism this round got wrong.

    Record HEAD's sha and `git log --oneline origin/main..HEAD | wc -l` (the commit count the push
    carries). Do not push.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any command, or the vitest summary reports a failed test file</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit from any command</fails_when>
    <automated>grep -a -c '^## Part 3' .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md</automated>
    <fails_when>a count other than 1</fails_when>
  </verify>
  <acceptance_criteria>
    - Part 3 § 3.1 lists every workflow command with its local exit status and a 33-row table with the closing plan, local result and measurability column for each § 2.4 red.
    - The expectation per leg and its falsifier are written before the push; HEAD's sha and the carried commit count are recorded.
    - No source file is changed by this task: `git diff --stat <sha of the commit before this task>..HEAD -- scripts install hooks` is empty, with both shas quoted.
  </acceptance_criteria>
  <done>The run will confirm or refute a prediction that is already on disk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Checkpoint — named human confirmation to push, so both legs can be measured</name>
  <what-built>
    Round 2's CAP-02 fix plans (33-14 through 33-19) are committed locally and every local gate is
    green: build, working-tree parity, freshness, typecheck, the platform-shape differential, the
    excluded-lane vitest suite, and the chained repository gates. The pre-push inventory is filed
    as Part 3 § 3.1 of
    `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md`, with one row per
    red of the round-1 run (33 rows) stating the closing plan, the local result, and whether the
    windows outcome is measurable on this machine.

    What remains cannot be measured here: CAP-02's bar is both matrix legs exiting 0 on a pushed
    run (D-13). The push is also a precondition of plan 33-21's live go (the runner refuses unless
    HEAD equals the remote head), though that plan will need one more push of its own pre-go
    artifacts before the go is asked for.
  </what-built>
  <how-to-verify>
    This checkpoint asks for a named human confirmation to push the current head to the default
    branch — this project holds merge and deploy with the human, mechanically rather than by prose.

    Before you answer:
    1. Read Part 3 § 3.1 — especially the `unmeasured locally, by construction` rows, which are
       what the run will actually decide.
    2. Run `git log --oneline origin/main..HEAD` to see exactly what the push carries.
    3. Note the cost: CI minutes only, zero tokens. This is NOT the live-capture go; that is plan
       33-21's separate, money-spending checkpoint.

    On confirmation the executor pushes, waits for the run to complete, and reads both legs'
    `conclusion` fields. A red leg is a measurement and becomes round 3's input; it is never
    answered with a platform conditional and never fixed-and-re-pushed inside this plan.
  </how-to-verify>
  <resume-signal>Type "push approved" to authorize the push, or "I pushed" if you pushed it yourself, or describe what you want changed first</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Read both legs from the run's own metadata; file the measurement; dispose only what a green run answers</name>
  <precondition>The head under test is pushed to the default branch (the checkpoint above was answered and the push completed), so a CI run exists for exactly that sha.</precondition>
  <read_first>
    - Part 3 § 3.1 as written by Task 1 (the prediction and its falsifier).
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` Part 2 § 2.1 (the exact `gh run view` field list and table shape), § 2.4 (the inventory shape), § 2.7 (the ledger commands used, verbatim shapes).
    - `.planning/WINDOWS.md` header (the tool that owns its three representations) and rows 186, 193, 229-235.
    - `.planning/REQUIREMENTS.md` row CAP-02 (L141 checkbox line and the coverage-table row ~L227).
    - `.planning/phases/33-live-capture-windows-portability/33-09-PLAN.md` Task 3 — the disposition rule for rows 186 (fixed, citing the confirming run ids) and 193 (waived, with D-16's reason and the revisit condition).
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md, .planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <action>
    Locate the run for the pushed sha with `gh run list --branch main --limit 5 --json databaseId,headSha,conclusion,createdAt`
    and wait for it with `gh run watch <id>` (or poll `gh run view <id> --json status`). Then read
    `gh run view <id> --json databaseId,headSha,status,conclusion,createdAt,updatedAt,jobs` and
    write Part 3 § 3.2 in Part 2 § 2.1's table shape: run id, head sha, created/completed, run
    conclusion, and for each of `test (ubuntu-latest)` and `test (windows-latest)` the job id and
    its `conclusion` field QUOTED, plus the per-step table. Every word in that table is copied from
    the JSON; the summary must state the command used.

    If BOTH legs read `success`: § 3.3 states `CAP-02 verdict on this run: MET` with the run id;
    append the per-row column to § 3.1's 33-row table (each `CLOSED on run <id>`); flip the CAP-02
    row in `.planning/REQUIREMENTS.md` (`- [x] **CAP-02**` with the run id and date, and the
    coverage-table row to `Complete — CI run <id> ... success on both legs; evidence 33-CI-MEASUREMENT.md Part 3`)
    — this row is NOT in the flip manifest's declared set, so editing it here does not touch the
    flip commit's rule; then dispose the ledger rows through the tool only:
    `windows fixed 229` … `windows fixed 235` each with a reason citing the run id and the
    closing plan; `windows fixed 186` citing the three green measurements of
    `scripts/board-watch-live.test.ts` (runs 35394268365, 35499800942 and this one);
    `windows waive 193 "<D-16: the CI-topology half is not taken; revisit only if a Windows watcher red proves undiagnosable inside the shared step>"`.
    Quote every command in § 3.4 and run `windows status` afterwards, quoting the counters and
    confirming the table row count and the JSON appendix agree.

    If EITHER leg is red: § 3.3 states `CAP-02 verdict on this run: NOT MET`; re-derive the failure
    inventory from that leg's own log (`gh api repos/abitwise/grugops/actions/jobs/<job id>/logs`,
    ANSI stripped, control bytes scanned before quoting) in § 2.4's shape — per file, per case, the
    assertion text, and a label from the same four (`not addressed`, `incomplete fix`, `new`,
    `mis-attributed`) — naming for each red which round-2 plan's mechanism it falsifies; update
    the CAP-02 coverage-table row to `Pending — NOT met: CI run <id> ...` with the counts; do NOT
    flip any ledger row (rows 229-235 stay open; append new rows through the tool for any NEW
    class, one per class, as § 2.7 did). No fix in this plan.

    Either way, plan 33-21 reads § 3.3 as its precondition text.
  </action>
  <verify>
    <automated>gh run view "$(gh run list --branch main --limit 1 --json databaseId --jq '.[0].databaseId')" --json headSha,conclusion,jobs --jq '{headSha, conclusion, jobs: [.jobs[] | {name, conclusion}]}'</automated>
    <fails_when>non-zero exit, or the printed headSha differs from the sha Part 3 § 3.1 recorded, or any job's conclusion is null (the run is not complete)</fails_when>
    <automated>grep -a -cE 'CAP-02 verdict on this run: (MET|NOT MET)' .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md</automated>
    <fails_when>a count other than 2 — Part 2 carries one verdict line and Part 3 must carry exactly one more</fails_when>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status</automated>
    <fails_when>non-zero exit, or the counters disagree with the table's row count — a row was hand-edited</fails_when>
  </verify>
  <acceptance_criteria>
    - Part 3 § 3.2 quotes the run id, head sha, run conclusion and BOTH jobs' `conclusion` fields from `gh run view --json`; the command is stated.
    - § 3.3 carries exactly one verdict line; on MET the CAP-02 requirement row is flipped with the run id and rows 229-235, 186 (fixed) and 193 (waived) are disposed through the tool with the commands quoted in § 3.4; on NOT MET the red inventory is re-derived in § 2.4's shape with per-red falsified-mechanism attribution and no ledger row is flipped.
    - No source file changed in this plan: `git diff --stat <HEAD sha recorded in Part 3 § 3.1>..HEAD -- scripts install hooks` is empty, with both shas quoted.
    - `windows status` counters agree with the table and the JSON appendix.
  </acceptance_criteria>
  <done>CAP-02 has a conclusion word from a runner, quoted, and the ledgers say exactly what it supports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a named human confirmation -> a push to the default branch | The only place this plan changes anything outside the working tree |
| a CI run's JSON -> the requirement ledger | A narrated conclusion would be a fabricated gate result |
| a red leg -> the temptation to fix inside the measurement | Fix-and-re-push inside one plan erases the measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-92 | Elevation of privilege | an unconfirmed push | critical | mitigate | Task 2 is a blocking human checkpoint; Task 3's precondition names it. |
| T-33-93 | Repudiation | a narrated CI conclusion | high | mitigate | Task 3 quotes `gh run view --json` fields; the verify command re-reads them. |
| T-33-94 | Tampering | fix-and-re-push inside the round | high | mitigate | Prohibited in must-haves; Task 3's red branch inventories and hands to round 3. |
| T-33-95 | Tampering | the ledger's three representations | medium | mitigate | Rows change only through the tool; `windows status` asserted afterwards. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; the run's `npm ci` installs dev deps from the committed lockfile only. |
</threat_model>

<verification>
- `gh run view <id> --json conclusion,jobs` — both legs' conclusions quoted in Part 3 § 3.2.
- `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status` — representations agree.
- `git diff --stat <HEAD sha recorded in Part 3 § 3.1>..HEAD -- scripts install hooks` — empty; no source touched.
</verification>

<success_criteria>
- One pushed run, both legs' conclusions quoted; CAP-02 flips only on `success` for both.
- On green, rows 229-235, 186 and 193 disposed through the tool; on red, the inventory is round 3's input and nothing is fixed here.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `33-CI-MEASUREMENT.md` Part 3 (§ 3.1-3.4) | measurement record | pre-push inventory, the run, the verdict, the dispositions |
| `CAP-02 verdict on this run: MET` or `... NOT MET` | verdict line | exactly one in Part 3 |
| REQUIREMENTS.md CAP-02 row | ledger change | flipped only on both-legs `success` |
| WINDOWS.md rows 229-235, 186 (fixed), 193 (waived) | ledger changes via tool | green branch only |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-20-SUMMARY.md` when done
</output>

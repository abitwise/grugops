---
phase: 33-live-capture-windows-portability
plan: 30
type: execute
wave: 19
depends_on: ["33-29"]
files_modified:
  - scripts/capture-live.ts
  - scripts/capture-live.js
  - scripts/capture-live.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-03, CAP-01]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Plugin provenance is decided BEFORE the spawn, over the copy the platform will load: after `plugin install`, the runner reads the platform's own registry row for the plugin at local scope in that target (`installPath`, `gitCommitSha`), validates the path under the cache root, digests it against the checkout's tracked files and refuses to spawn on anything but MET — so a stale version-keyed cache copy is a refusal at zero tokens, never an `UNMET` after the full spend (33-REVIEW WR-02). The post-run init-frame digest stays as the confirmation, and both are reported."
    - "The working tree the installer copies matches HEAD under every directory the installer and the plugin read: a precondition row derived from `git status --porcelain --untracked-files=all` over those directories is UNMET on any output, so an untracked file cannot reach path A while being invisible to the tracked-file digest (WR-03). The digest set stays `git ls-files`; the row makes the untracked arm a refusal before the spend."
    - "`probe` returns stdout and stderr SEPARATELY; every consumer that parses (`trackedFiles`, `localHead`, `remoteHead`, the ahead count) parses stdout alone, so a `GIT_TRACE=1` or a git warning in the operator's environment moves neither the tracked count nor the pushed-sha row (WR-04); a transcript-supplied path is refused or replaced before it lands in a summary cell (IN-05)."
    - "The note-route axis of the D-07 projection sees every route a note can take to disk: the context marker is anchored either way (a relative `file_path` counts), every file-writing tool the platform publishes counts (`Write`, `Edit`, `MultiEdit`, `NotebookEdit`), and a `Bash` block whose command text names the context subpath is counted as an UNCLASSIFIED context write — a third route sentence in `compareLivePaths`, a strengthening never a softening (WR-01, D-07/D-20)."
    - "Every arm is red-first at zero tokens; the dry run exits 0, reads `GO-READINESS` with the new working-tree row, prints one `OUTCOME: no-go` line, and its report carries the pre-spawn provenance row (D-10)."
  artifacts:
    - scripts/capture-live.ts
    - scripts/capture-live.js
    - scripts/capture-live.test.ts
  key_links:
    - "capture-live.ts `runTarget` -> `ops.pluginInstall` -> `installedPluginRow(registryText, pluginKey, target)` (the platform registry, read from the operator home) -> `pluginCachePathAccepted` -> `contentDigest` -> `provenanceVerdict` -> `fail(...)` unless MET -> `ops.runPlatform`"
    - "capture-live.ts `observePreconditions` -> `git status --porcelain --untracked-files=all -- <install/plugin dirs>` -> `evaluatePreconditions` row `working tree matches HEAD ...` -> readiness"
    - "capture-live.ts `probe` -> `{ out, err }` -> `trackedFiles` / `localHead` / `remoteHead` parse `out` only"
    - "capture-live.ts `noteRoute` -> `WRITING_TOOLS` + anchored marker + `unclassifiedContextWrites` -> `compareLivePaths` third route sentence"
  prohibitions:
    - "no spawn while the installed copy's provenance is not MET"
    - "no verdict input is assembled from a channel the operator's environment can pollute (stderr into data)"
    - "the D-07 predicate is never softened to make a capture read as parity (D-20)"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): the flip depends on a capture this instrument scores; this plan adds refusals before the spend and a stronger route axis, and asserts nothing about the capture's word."
    - "CAP-02 (edge row unresolved, flagged): untouched beyond the rebuilt twin; measured only by plan 33-31."
    - "CAP-03 (edge row unresolved, flagged): the last post-run verdict input (the cache digest) is now a pre-spawn gate; the observation itself is produced only by plan 33-32's go."
    - "assumption_delta = add-alongside, recorded: the registry's `gitCommitSha` is reported BESIDE the content digest as a second, independent provenance signal (D-05: the summary records the sha it installed); the content digest stays the gate because the cache is not a git checkout (33-DIAGNOSIS § 4.2)."
---

<objective>
Gap-closure round 3 of the phase's four-round cap. The round-2 review's warnings on the
instrument, every one a verdict-input or readiness defect: WR-02 (provenance decided only after
both runs, and a version-keyed cache makes a stale copy a predictable `UNMET` after the full
spend — the third post-run input CR-01 named), WR-03 (the digest is over `git ls-files` while the
installer copies the working tree, so an untracked file reaches path A invisibly), WR-04
(`probe` concatenates stderr into the data path; `GIT_TRACE=1` corrupts the tracked list and the
pushed-sha row), WR-01 (`noteRoute` is evadable by a relative `file_path`, a `MultiEdit`, or a
node-mediated write, so the route axis can read parity over two evasions), and IN-05 (a
transcript-supplied path reaches a summary cell with only pipe/newline escaping).

Gap mapping (33-VERIFICATION.md CAP-03 `missing[0]`, "digest the plugin-cache copy before the
spawn as well as after (WR-02)") → Task 1 with WR-03; WR-04 + IN-05 → Task 2; WR-01 → Task 3.

Decisions honored: D-05 (the plugin under test is the pushed checkout by content, and the summary
records the sha it installed — now from the platform's own registry row too), D-10 (a refusal
before the spend is a readiness row, not a diagnosis after it), D-07/D-20 (the route axis is
widened, never narrowed), D-11 (offline proof).

Purpose: the last verdict input the subject could touch moves before the spawn; the two
environment-polluted probes stop feeding readiness; the route axis cannot be evaded by spelling.
Output: `scripts/capture-live.ts` + committed `.js`, the offline suite extended with the
stale-cache, untracked-file, GIT_TRACE, and route-evasion proofs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-REVIEW.md
@.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md
@.planning/phases/33-live-capture-windows-portability/33-29-SUMMARY.md
@scripts/capture-live.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provenance before the spawn from the platform's own registry row, and a working-tree row that makes an untracked file a refusal (WR-02, WR-03)</name>
  <read_first>
    - `scripts/capture-live.ts` — the D-05 header paragraph (:108-133), `pluginUnderTest` / `pluginCachePathAccepted` / `contentDigest` / `provenanceVerdict` / `pluginCacheRoot` / `trackedFiles` / `deriveProvenance` (:1474-1592), `runTarget` as plan 33-29 left it (install → try/finally), `capture()`'s single post-run `deriveProvenance` call, `observePreconditions` / `evaluatePreconditions` (:1099-1245: the row shapes and the three states), `ReportModel.provenance`, `renderReport`'s provenance rows.
    - `~/.claude/plugins/installed_plugins.json` on this host (read-only, for the SHAPE only — never quote a path into a plan): `version: 2`, `plugins["NAME@MARKETPLACE"]` is an array of rows with `scope`, `installPath`, `version`, `installedAt`, `lastUpdated`, `gitCommitSha`; a local-scope row carries the project path the install was scoped to (the deferred-items entry on the two stale `0.1.0` rows names it). Assert the field names in the test from a fixture you write, not from the operator's file.
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` WR-02 (the recipe: `installedCopyPath` from the registry row for scope=local and that project path; refuse unless MET before the spawn; consider `plugin marketplace update` as a documented pre-step) and WR-03 (the `git status --porcelain --untracked-files=all -- agent-factory .claude/agents install skills hooks` row).
    - `install/install.ts` :1540-1550 (`cpSync(join(GRUGOPS_SRC, "agent-factory"), …)` — the directories the installer copies from the working tree) and `.claude-plugin/plugin.json` (the directories the plugin loads: `agents`, `commands`/`skills`, `hooks`, `scripts` for the MCP server).
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` Part 3 § 3.1 row on `git status` cleanliness, and this round's `git status` (two untracked `.planning/` entries: `.planning/milestone.lock` and the Phase-34 directory — outside every directory the row scopes, and the reason the row is scoped rather than tree-wide).
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - Test P1 (RED first — the stale cache): through the seam, an install stand-in that writes a registry fixture whose local-scope row for the plugin points `installPath` at a scratch directory holding the checkout's tracked files with ONE byte changed; `runTarget` rejects naming `provenance` and `UNMET` and the platform recorder was called 0 times. On the base the spawn happens and the `UNMET` arrives after the run — quote the recorder's count of 1.
    - Test P2 (the honest copy spawns): the same with a byte-identical copy → the recorder is called once and the report carries `| run X plugin provenance before the spawn | MET — ... |` plus the registry's `gitCommitSha` beside it and the post-run init-frame confirmation row.
    - Test P3 (the registry row is found by scope and project, never by index): a fixture with THREE rows for the plugin (user scope; local scope for another project path; local scope for this target) resolves the third; a fixture with none for this target is `UNKNOWN - verify` naming the missing row and is a refusal before the spawn (nothing was measured, so nothing spends).
    - Test P4 (RED first — the untracked file): `evaluatePreconditions` over an observation whose scoped `git status` output names one untracked file under `agent-factory/` yields a `working tree matches HEAD under the directories the installer and the plugin read` row UNMET naming the file; an empty output yields MET; an unreadable status is `UNKNOWN - verify`; an untracked file OUTSIDE the scoped directories (a `.planning/` entry) does not appear in the scoped output and the row stays MET — the scope is asserted by the argument list the probe is called with (recorded through a probe seam or by asserting the derived argument array).
  </behavior>
  <action>
    Add `export function installedPluginRow(registryText: string, pluginKey: string, target: string): { installPath: string; gitCommitSha: string | null } | null`
    — a pure parser over the registry JSON text that selects the local-scope row whose project
    path names `target` (compare both through `realpathSync.native` where the path exists;
    never by index); and a reader that loads `~/.claude/plugins/installed_plugins.json` (a fixed
    derivation from the operator's home, the `pluginCacheRoot` idiom). In `runTarget`, after
    `ops.pluginInstall` and before `ops.runPlatform`: resolve the row, validate `installPath`
    through `pluginCachePathAccepted`, digest it with `contentDigest(accepted, tracked)` against
    the checkout digest, and `fail(...)` unless the verdict is MET — the sentence names the state,
    both digests, the registry's `gitCommitSha` and the remedy the review names (`claude plugin
    marketplace update grugops`, or uninstall and reinstall at user scope, when the version is
    unbumped). Report both the pre-spawn verdict and the existing post-run init-frame verdict as
    two rows; `deriveOutcome` keeps reading the post-run state as its input AND the pre-spawn
    refusal makes an UNMET spawn unreachable (state in the docblock why both stay: the pre-spawn
    row is the gate, the post-run row is the confirmation that what was loaded is what was
    digested). The tracked list and the checkout digest are computed ONCE, before the target
    loop, and handed down.

    WR-03: in `observePreconditions`, run
    `git status --porcelain --untracked-files=all -- agent-factory .claude .claude-plugin install skills hooks scripts AGENTS.md`
    (the directories the installer copies and the plugin loads — derive the list from ONE exported
    constant with a docblock naming which consumer reads which directory) through the probe;
    `evaluatePreconditions` gains the row named in P4; readiness is `not-ready` on UNMET or
    UNKNOWN as for every other row. Update the D-05 header paragraph: the pushed-sha row and the
    working-tree row are the two preconditions that make "the installed copy equals the checkout"
    achievable, and the pre-spawn digest is what proves it.

    Tests P1-P4 RED before GREEN for P1 and P4 (quote both red runs). Rebuild.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts -t "P1|P2|P3|P4|before the spawn|working tree matches"</automated>
    <fails_when>non-zero exit, or fewer than 4 tests reported passed</fails_when>
    <automated>awk '/^export async function runTarget/,/^}/' scripts/capture-live.ts | grep -a -n 'provenanceVerdict(\|ops.runPlatform(' | head -2 | awk -F: 'NR==1{a=$1} NR==2{b=$1} END{exit !(a&lt;b)}'</automated>
    <fails_when>non-zero exit — the pre-spawn provenance verdict does not precede the platform spawn inside runTarget</fails_when>
    <automated>node -e "const m=require('./scripts/capture-live.js');const t=m.evaluatePreconditions;if(typeof t!=='function')process.exit(9);const src=require('fs').readFileSync('scripts/capture-live.ts','utf8');if(!/working tree matches HEAD/.test(src))process.exit(1);if(!/--untracked-files=all/.test(src))process.exit(2)"</automated>
    <fails_when>exit 1 (the row is not declared) or exit 2 (the status probe does not enumerate untracked files)</fails_when>
    <automated>node scripts/capture-live.js --dry-run --out "$(mktemp -d)" | tee /dev/stderr | grep -a -c 'GO-READINESS: '</automated>
    <fails_when>a count other than 1, or the dry-run output lacks OUTCOME: no-go, or the readiness line names the working-tree row while `git status --porcelain --untracked-files=all -- agent-factory .claude .claude-plugin install skills hooks scripts AGENTS.md` is empty on this host</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `installedPluginRow` is exported and pure; `runTarget` gates the spawn on the pre-spawn provenance verdict; the report carries both provenance rows and the registry's `gitCommitSha`.
    - The scoped-directory constant exists with its docblock; the working-tree row is a readiness input with all three states; the D-05 header paragraph names the two preconditions and the pre-spawn digest.
    - Tests P1-P4 exist; P1's and P4's RED commits precede their GREEN commits (shas quoted; P1's recorder count on the base quoted as 1).
    - The dry run exits 0 with one `OUTCOME: no-go` and the working-tree row MET on this host; the committed `.js` moves in the same commit.
  </acceptance_criteria>
  <done>A stale or foreign plugin copy is refused before a token is spent, from the platform's own record of what it installed, and an untracked file cannot make the two paths run different kits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `probe` separates stdout from stderr, every parser reads stdout alone, and a transcript-supplied path cannot carry a control byte into a summary cell (WR-04, IN-05)</name>
  <read_first>
    - `scripts/capture-live.ts` — `probe` (:1171-1175: `${r.stdout}${r.stderr ?? ""}`), its callers (`observePreconditions` :1191-1245: the platform probes and the four git probes; `trackedFiles` :1552; `checkoutSha` :1882), `cell` (:1648), the `plugin under test per system/init` row (:1571, :1708), `verifyArtifacts` (:1795), `pluginCachePathAccepted` (:1486: the refusal shape for a dash-prefixed or foreign path).
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` WR-04 (the `GIT_TRACE=1` reproduction: 2447 entries whose last is a trace line; `localHead !== remoteHead` reading UNMET with trace text) and IN-05 (a C0/C1 byte in `under.path` landing in the summary verbatim; the P32.1 F-14 class).
    - `scripts/capture-live.test.ts` :439-511 (the precondition cases and how observations are built; the `rev-parse` source-line pin at :928 flagged by IN-03 — do not bump it by hand; if it moves, derive the relationship instead).
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - Test P5 (RED first — the polluted channel): with `GIT_TRACE=1` planted in the base environment handed to the probes (through `childEnvironment`), `trackedFiles()` returns exactly the `git ls-files -z` entry count with no entry containing a newline or the word `trace`, and `observePreconditions`'s pushed-sha inputs (`localHead`, `remoteHead`) are 40-hex strings so the row reads MET when they are equal. On the base the tracked count is off by the trace lines and the row reads UNMET with trace text — quote the red run.
    - Test P6 (stderr is kept for the refusal text): a probe whose child exits non-zero returns null as before; a probe whose child exits 0 with stderr output returns `{ out, err }` with `err` carrying it, and the ONE consumer that reports a refusal (the install/precheck sentences) still includes the stderr text — nothing that PARSES reads `err`.
    - Test P7 (control bytes never reach a cell): a `system/init` fixture whose plugin `path` carries a C1 byte (`\x85`) and one carrying a C0 byte (`\x01`): `pluginCachePathAccepted` refuses both (a path with a control byte is not a plugin directory), the `plugin under test per system/init` row's value has every byte in `[\x00-\x1f\x7f-\x9f]` replaced by `<control>` with the replacement count stated in the row, and `verifyArtifacts` over a summary carrying a raw control byte in any cell REFUSES naming the row.
  </behavior>
  <action>
    Change `probe` to return `{ out: string; err: string } | null`; every parsing consumer
    (`trackedFiles`, the four git probes, `checkoutSha`, the version/help/listing probes) reads
    `out`; a consumer that prints a refusal may append `err`. Extend `pluginCachePathAccepted`
    with a control-byte refusal (the same `[\x00-\x1f\x7f-\x9f]` class `check:nul-bytes` and
    the P32.1 F-14 fix use) BEFORE the dash-prefix check, so the transcript field that reaches a
    filesystem call is refused on the byte class first. In `cell`, replace control bytes with the
    literal `<control>` and count the replacements; where a row's value was altered, append the
    count in the row (`(N control byte(s) replaced)`), and make `verifyArtifacts` refuse any
    committed summary carrying a raw byte of that class in a cell.

    Tests P5-P7 RED before GREEN for P5 (quote the red tracked count and the red pushed-sha row).
    If the `rev-parse` source-line pin at :928 moves, replace the integer with the relationship
    IN-03 names (the call set derived from the AST is a subset of the two functions that may call
    it) — never a bumped literal. Rebuild.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts -t "P5|P6|P7|GIT_TRACE|control byte"</automated>
    <fails_when>non-zero exit, or fewer than 3 tests reported passed</fails_when>
    <automated>grep -a -c 'r.stderr ?? ""}`' scripts/capture-live.ts</automated>
    <fails_when>a count above 0 — a probe still concatenates stderr into the returned data</fails_when>
    <automated>GIT_TRACE=1 node scripts/capture-live.js --dry-run --out "$(mktemp -d)" | grep -a 'pushed sha' | grep -a -c 'trace'</automated>
    <fails_when>a count above 0 — the pushed-sha row still quotes git trace text under GIT_TRACE=1 (the dry run itself must still exit 0)</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `probe` returns `{ out, err }`; no parsing consumer reads `err`; the summary quotes the derived consumer list.
    - `pluginCachePathAccepted` refuses control bytes; `cell` replaces and counts them; `verifyArtifacts` refuses a raw control byte in a cell.
    - Tests P5-P7 exist; P5's RED commit precedes its GREEN commit (shas and the base's polluted count quoted); no integer pin was bumped by hand (IN-03's relationship used if the pin moved).
    - The committed `.js` moves in the same commit.
  </acceptance_criteria>
  <done>What the runner parses is what git printed on stdout, and what a transcript names never reaches a summary or a filesystem call with a byte the gates refuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The note-route axis sees every route — anchored marker, every file-writing tool, and node-mediated writes as unclassified (WR-01)</name>
  <read_first>
    - `scripts/capture-live.ts` — `noteRoute` (:908-925: the leading-slash marker, the `Write`/`Edit` filter), `NoteRoute` / `PathProjection` / `compareLivePaths` (:889-985), `CONTEXT_SUBPATH` (:171), `contentBlocks` (:545).
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` WR-01 — the three reproductions on the committed `.js` (relative `file_path`, `MultiEdit`, a `Bash` block running a node write) and the recipe (`WRITING_TOOLS`, marker anchored either way, `Bash` blocks naming the subpath as unclassified writes).
    - `scripts/capture-live.test.ts` :642-657 (Test D, the route case: the fixture's counts against an independent grep) and :575-608 (Test A, the held-capture divergence sentences — they must still hold verbatim: path B's nine direct writes were absolute-path `Write` blocks, so the direct count does not move).
    - The platform's tool list for file-writing tools (the round-1 init frame A:11 `tools[]` names `Write`, `Edit`, `MultiEdit`, `NotebookEdit`; derive the set from that fixture in the test rather than typing it, so a renamed tool is a named red).
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - Test P8 (RED first, the three evasions): an in-memory `Write` block with a RELATIVE `file_path` under `.grugops/context/` raises `directContextWrites` by 1; a `MultiEdit` block with an absolute path under it raises it by 1; a `NotebookEdit` block likewise; a `Bash` block whose command text names `.grugops/context/` raises `unclassifiedContextWrites` by 1 and `directContextWrites` by 0. On the base all four read 0 — quote the red run.
    - Test P9 (the projection compares the third route): two projections equal in every field but `unclassifiedContextWrites` compare to exactly one sentence `note route: unclassified writes naming the context root differ: path A N, path B M`; equal counts compare to no route sentence.
    - Test P10 (the held capture still reads its six sentences): Test A's six divergence sentences over commit `c7be6d0d` are unchanged, and the held capture's `unclassifiedContextWrites` per path is stated (derived; path A's `admit-notes.mjs` scripts at A:1749 / A:1931 name the context root and are expected to count — state the numbers the test measures rather than predicting them).
  </behavior>
  <action>
    In `noteRoute`, derive the marker in POSIX form and match it anchored either way
    (`includes` of the slash-wrapped marker OR `startsWith` of the marker followed by a slash);
    build the file-writing tool set as an exported constant `WRITING_TOOLS` (`Write`, `Edit`,
    `MultiEdit`, `NotebookEdit`) with a docblock naming the platform's tool list as its source;
    count a `Bash` block whose `input.command` (a string) contains the POSIX marker as
    `unclassifiedContextWrites`. `NoteRoute` gains the field; `compareLivePaths` gains the third
    route sentence with the exact shape P9 names; `renderReport`'s route rows print the third
    count. Say in `noteRoute`'s docblock why it is UNCLASSIFIED: a node-mediated write may be the
    sanctioned in-process writer (A:839) or a hand write (a heredoc) — the transcript cannot
    distinguish them, so the axis records the count and the reader's seal (plan 33-25) decides
    what was admitted.

    Tests P8-P10 RED before GREEN for P8. Rebuild; then the dry run and the full excluded-lane
    suite once.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts -t "P8|P9|P10|held capture|unclassified"</automated>
    <fails_when>non-zero exit, or fewer than 4 tests reported passed (P8-P10 plus the unchanged Test A)</fails_when>
    <automated>grep -a -c 'unclassifiedContextWrites' scripts/capture-live.ts</automated>
    <fails_when>a count below 3 — the field is not declared, counted and compared</fails_when>
    <automated>node scripts/capture-live.js --dry-run --out "$(mktemp -d)" &amp;&amp; npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any command, or the dry-run output lacks OUTCOME: no-go, or the vitest summary reports a failed test file</fails_when>
  </verify>
  <acceptance_criteria>
    - `WRITING_TOOLS` is exported; the marker is anchored either way; `NoteRoute.unclassifiedContextWrites` exists and enters `compareLivePaths` with the exact sentence shape.
    - Tests P8-P10 exist; P8's RED commit precedes its GREEN commit (shas quoted); Test A's six sentences are unchanged (quoted).
    - The dry run exits 0 with one `OUTCOME: no-go`; the full excluded-lane suite is green; the committed `.js` moves in the same commit.
  </acceptance_criteria>
  <done>No spelling of a write into the context root escapes the route axis, and a route the transcript cannot classify is counted as exactly that.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the platform's plugin cache (version-keyed, subject-reachable after the spawn) -> the provenance verdict | Closed by digesting before the spawn from the platform's own registry row |
| the operator's working tree -> path A | An untracked file reaches the target and not the digest; closed by a scoped status row |
| the operator's environment (`GIT_TRACE`, git warnings) -> the readiness inputs | Closed by parsing stdout alone |
| a transcript-supplied path -> a summary cell / a filesystem call | Closed by the control-byte refusal before either |
| the D-07 route axis -> two evasions reading as parity | Closed by anchoring the marker, widening the tool set and counting node-mediated writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-136 | Spoofing | a stale cache copy scored after the spend, or rewritten to MET after the spawn | critical | mitigate | Task 1: pre-spawn digest from the registry row is the gate; post-run digest is the confirmation; P1 mutation-proven. |
| T-33-137 | Tampering | an untracked file present on path A and absent from the digest | high | mitigate | Task 1: the scoped working-tree row, UNMET on any output; P4. |
| T-33-138 | Tampering | stderr polluting the tracked list and the pushed-sha row | medium | mitigate | Task 2: `probe` returns `{ out, err }`; parsers read `out`; P5 under `GIT_TRACE=1`. |
| T-33-139 | Information disclosure | a control byte in a transcript path landing in a committed summary | medium | mitigate | Task 2: refusal in `pluginCachePathAccepted`, replacement in `cell`, refusal in `verifyArtifacts`; P7. |
| T-33-140 | Repudiation | two evasions of the route axis reading as parity | high | mitigate | Task 3: anchored marker, `WRITING_TOOLS`, unclassified count as a third sentence; P8-P10; Test A unchanged. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run check:build-parity` — the committed `.js` is a faithful build.
- `node scripts/capture-live.js --dry-run --out TMP` — exit 0, the working-tree row MET on a clean scoped tree, one `OUTCOME: no-go`, both provenance rows present.
</verification>

<success_criteria>
- Provenance is a pre-spawn gate from the platform's registry row; an untracked file under the installer's directories is a readiness refusal.
- The probes' parsers read stdout alone; a control byte never reaches a cell or a filesystem call.
- The route axis counts every spelling and every tool, and the held capture still reads its six sentences.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `installedPluginRow(registryText, pluginKey, target)` | exported function | the platform's own record of the local-scope install, by scope and project, never index |
| pre-spawn provenance gate in `runTarget`; `| run X plugin provenance before the spawn |` row | module behavior + report row | WR-02 |
| the scoped install/plugin directory constant; `working tree matches HEAD under ...` readiness row | exported constant + precondition row | WR-03 |
| `probe` returning `{ out, err }` | module change | WR-04 |
| control-byte refusal in `pluginCachePathAccepted`; `<control>` replacement + count in `cell`; `verifyArtifacts` refusal | module behavior | IN-05 |
| `WRITING_TOOLS`; `NoteRoute.unclassifiedContextWrites`; the third route sentence | exported constant + projection field + comparator sentence | WR-01 |
| Tests P1-P10 | new test cases | red-first (P1, P4, P5, P8) |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-30-SUMMARY.md` when done
</output>

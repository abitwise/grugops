---
phase: 33-live-capture-windows-portability
plan: 40
type: execute
wave: 29
depends_on: ["33-35", "33-36", "33-37", "33-38", "33-39"]
files_modified:
  - .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: false
gap_closure: true
requirements: [CAP-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CAP-02 is measured by ONE pushed CI run — the FOURTH of this phase — whose own `conclusion` fields for `test (ubuntu-latest)` and `test (windows-latest)` are quoted from `gh run view --json`; the requirement flips only when BOTH read `success` (D-13). This push carries EVERY round-4 SOURCE commit (plans 33-35 through 33-39), so the CR-01 fix (7d0aea72 and everything after 33-31) runs on CI for the first time, together with the round-4 nested/npm/xargs closures and the row-260 win32 fix."
    - "Measurement and fix never share a plan: this plan changes no source file. A red leg becomes the last round's finding in the four-label shape (`not addressed`, `incomplete fix`, `new`, `mis-attributed`), never a fix here (D-11); because round 4 is the last under the cap, a red leg is recorded as the cap-reached outcome, not handed forward."
    - "Before the push, every workflow command is run locally in the workflow's order with its exit status, and the row-260 win32 case is named with the 33-37 hand-off's `unmeasured locally, by construction` column (this host cannot produce a drive-letter path) — so the run decides something the document already predicted, and its falsifier is written down."
    - "The push is behind a named human confirmation (`blocking-human`) and the executor never pushes on its own (the repository's hard safety rule). This push is the FIRST of the two the go path needs: plan 33-41's runner refuses unless HEAD equals `origin/main`, and its own push checkpoint carries the documentation commits that land in between."
    - "Ledger rows change only through the tool; the three representations are checked to agree afterwards; the CAP-02 requirement row is the only requirement row this plan touches, and row 260 is disposed `fixed` only on a both-legs-`success` run."
  artifacts:
    - .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
    - .planning/REQUIREMENTS.md
    - .planning/WINDOWS.md
  key_links:
    - "the pre-push inventory (Part 5) -> the human push -> `gh run view <id> --json databaseId,headSha,conclusion,jobs` -> the verdict -> the CAP-02 row"
    - "the run's `success`/`failure` words -> `gsd-tools windows fixed 260` (green only) and rows 186/193/226-236 (green only)"
    - "33-37-SUMMARY.md row-260 hand-off -> Part 5's prediction -> the falsifier"
  prohibitions:
    - "no platform conditional closes a CAP-02 red (D-14/D-16)"
    - "no fix is applied and re-pushed inside this plan; a red leg is inventoried as the cap-reached finding"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "a WINDOWS.md row is never hand-edited"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): untouched by this plan; the go in plan 33-41 needs this plan's push (HEAD == origin/main at the go) and reads this plan's verdict in its checkpoint text."
    - "CAP-02 (edge row unresolved, flagged): resolved to `success` or `failure` by this plan's pushed run and nothing else."
    - "CAP-03 (edge row unresolved, flagged): untouched by this plan."
---

<objective>
Gap-closure round 4 of the phase's four-round cap — the LAST round. This is the CAP-02 measurement:
one pushed CI run, read from its own metadata, after every round-4 SOURCE fix has landed (33-35 the
guard nested-quoted closure, 33-36 the npm-prefix and xargs closures, 33-37 the row-260 win32 anchor,
33-38 WR-01, 33-39 WR-02). It is its own plan because a measurement and a fix never share one (D-11;
33-09/33-20/33-31's shape). This push is what finally runs the CR-01 fix (7d0aea72) and everything
after 33-31 on CI — 33-VERIFICATION.md noted those 16 commits had never run on either leg.

Gap mapping (33-VERIFICATION.md CAP-02 `missing[1]`): "Push (human-confirmed) and measure ONE CI run
on the round-4 head with both job conclusions quoted `success`." -> Task 3, behind Task 2's human
push. Task 1 files the pre-push inventory so the run decides something already predicted (row 260).

Decisions honored: D-13 (both legs), D-14/D-16 (no conditional; a red is a measurement), D-11
(record; but this is the last round). Sequencing (must-fix item 5): this push precedes the go plan;
the human pushes, never the executor; commits stay local until a push is asked for by name.

Purpose: turn the round-4 source fixes into a conclusion word from the runner that decides CAP-02
and, with it, whether the flip's second gate can be met this round.
Output: 33-CI-MEASUREMENT.md Part 5 (pre-push inventory, the run, the verdict, the dispositions), the
CAP-02 requirement row, and the ledger rows a green run answers (row 260 among them).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
@.planning/phases/33-live-capture-windows-portability/33-31-PLAN.md
@.planning/phases/33-live-capture-windows-portability/33-31-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-37-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The pre-push inventory — every workflow command, the row-260 win32 case, the falsifier, before the push</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` Part 4 (the round-3
      shape: § 4.1 the workflow-command table and the measurability column; § 4.3 the row-260
      mechanism; the four disposition labels).
    - `.planning/phases/33-live-capture-windows-portability/33-37-SUMMARY.md` — the row-260 hand-off
      (the win32 axis is `unmeasured locally, by construction`; the decided form and its source).
    - The 33-35/33-36/33-38/33-39 summaries — each names the modules it rebuilt and the gates it ran;
      none claims a CI leg.
    - `.github/workflows/ci.yml` — the step order and which steps are ubuntu-scoped.
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md</files>
  <action>
    Append `## Part 5 — round 4: the pre-push inventory (Task 1)` to the measurement document. Run
    every workflow command locally in the workflow's order (the Part 1 § 1.1 list: install, freshness,
    build + parity, typecheck, platform shapes, the excluded-lane vitest run, the chained repository
    gates) and record each with its exit status. Name the one axis this host cannot measure: the
    row-260 win32 drive-letter spelling, `unmeasured locally, by construction`, with the sentence that
    darwin has no drive-letter path. Add one row per round-4 source plan (33-35, 33-36, 33-37, 33-38,
    33-39) naming the module(s) it rebuilt and its local suite result, and state that 33-35/33-36's
    guard modules and 33-38's context-io are `measurable here` (no windows-only axis) while 33-37's
    win32 anchor has the drive-letter axis this host cannot reach.

    State the expectation the run will decide, one sentence per leg, before the push — ubuntu: green on
    every step; windows: green on every step, vitest 0 failed, and specifically the row-260 case
    (capture-live Test C7) green under the 33-37 authority — and the falsifier: any red on either leg
    is this round's finding; a red on the row-260 case is a win32 form 33-37 decided wrong; a red
    elsewhere is a round-4 rebuild that moved an arm (attribute it to the plan by module).

    Record HEAD's sha on one line in exactly the form `pre-push HEAD sha: ` followed by the
    40-character sha (plan 33-40's verify block extracts it by that literal), and
    `git log --oneline origin/main..HEAD | wc -l` (the commit count the push carries — every round-4
    commit plus any round-3 documentation commits still local). Do not push.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any command, or the vitest summary reports a failed test file, or "0 passed"</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness &amp;&amp; npm run freshness:hook-manifest &amp;&amp; npm run freshness:adapters &amp;&amp; npm run freshness:guarantees</automated>
    <fails_when>non-zero exit from any command</fails_when>
    <automated>grep -a -c '^## Part 5' .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md</automated>
    <fails_when>a count other than 1</fails_when>
    <automated>grep -a -c 'pre-push HEAD sha: [0-9a-f]' .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md</automated>
    <fails_when>a count of 0 in Part 5 — the pre-push sha line is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - Part 5 lists every workflow command with its local exit status, names the row-260 win32 axis as unmeasured-locally, and adds one row per round-4 source plan.
    - The expectation per leg and its falsifier are written before the push; HEAD's sha and the carried commit count are recorded.
    - No source file is changed by this task: `git diff --stat TASK_BASE_SHA..HEAD -- scripts install hooks agent-factory .claude` is empty, with both shas quoted; the `pre-push HEAD sha:` line is present in Part 5.
  </acceptance_criteria>
  <done>The run will confirm or refute a prediction that is already on disk, on every row including row 260.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Checkpoint — named human confirmation to push the round-4 source, so both legs can be measured (push one of two on the go path)</name>
  <what-built>
    Round 4's source fix plans (33-35 through 33-39) are committed locally and every local gate is
    green: build, working-tree parity, freshness (adapters, hook manifest, guarantees), typecheck, the
    platform-shape differential, the excluded-lane vitest suite, and the chained repository gates. The
    pre-push inventory is filed as Part 5 of `33-CI-MEASUREMENT.md`, naming the one axis this machine
    cannot measure (the row-260 win32 drive-letter spelling) on its own row.

    What remains cannot be measured here: CAP-02's bar is both matrix legs exiting 0 on a pushed run
    (D-13). This push carries every round-4 source commit AND the CR-01 fix and everything after 33-31
    that has never run on CI. It is the FIRST of the two pushes the go path needs: plan 33-41's runner
    refuses unless HEAD equals the remote head, and that plan places its own push checkpoint
    immediately before the go for the documentation commits that land in between.
  </what-built>
  <how-to-verify>
    This checkpoint asks for a named human confirmation to push the current head to the default branch
    — this project holds merge and deploy with the human, mechanically rather than by prose.

    Before you answer:
    1. Read Part 5 — the row-260 win32 axis is what the run will actually decide, plus that the CR-01
       fix runs on CI for the first time.
    2. Run `git log --oneline origin/main..HEAD` to see exactly what the push carries (every round-4
       source commit plus any round-3 documentation commits kept local by your policy).
    3. Note the cost: CI minutes only, zero tokens. This is NOT the live-capture go; that is plan
       33-41's separate, money-spending checkpoint.

    On confirmation the executor waits for the run to complete and reads both legs' `conclusion`
    fields. A red leg is a measurement and, because round 4 is the last under the cap, is recorded as
    the cap-reached finding; it is never answered with a platform conditional and never fixed-and-re-
    pushed inside this plan.
  </how-to-verify>
  <resume-signal>Type "push approved" to authorize the executor's push, or "I pushed" if you pushed it yourself, or describe what you want changed first</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Read both legs from the run's own metadata; file the measurement; dispose only what a green run answers</name>
  <precondition>The head under test is pushed to the default branch (the checkpoint above was answered and the push completed: `git rev-parse HEAD` equals `git rev-parse origin/main` after `git fetch`), so a CI run exists for exactly that sha.</precondition>
  <read_first>
    - Part 5 as written by Task 1 (the prediction and its falsifier).
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` Part 4 § 4.2-4.4
      (the exact `gh run view` field list, the per-step table shape, the suite-totals table with the
      control-byte scan note, the four labels, the ledger command shapes).
    - `.planning/WINDOWS.md` header (the tool that owns its three representations) and rows 186, 193,
      226-236, 260.
    - `.planning/REQUIREMENTS.md` row CAP-02 (the checkbox line and the coverage-table row).
    - `.planning/phases/33-live-capture-windows-portability/33-31-PLAN.md` Task 3 — the disposition
      rule for rows 186 (fixed, citing the confirming run ids incl. this run) and 193 (waived, D-16's
      reason), applied only on a both-legs-green run.
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md, .planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <action>
    Locate the run for the pushed sha with `gh run list --branch main --limit 5 --json databaseId,headSha,conclusion,createdAt`
    and wait for it with `gh run watch <id>` (or poll `gh run view <id> --json status`). Then read
    `gh run view <id> --json databaseId,headSha,status,conclusion,createdAt,updatedAt,jobs` and write
    Part 5 § "the pushed run" in Part 4 § 4.2's table shape: run id, head sha, created/completed, run
    conclusion, and for each of `test (ubuntu-latest)` and `test (windows-latest)` the job id and its
    `conclusion` field QUOTED, the per-step table, and the suite totals from each leg's own vitest
    summary block (ANSI stripped; control bytes scanned before quoting). Every word in those tables is
    copied from the JSON or the log; the summary states the commands used.

    If BOTH legs read `success`: state `CAP-02 verdict on this run: MET` with the run id; flip the
    CAP-02 row in `.planning/REQUIREMENTS.md` (`- [x] **CAP-02**` with the run id and date, and the
    coverage-table row to `Complete — CI run <id> (head <sha>, <date>): success on both legs; evidence
    33-CI-MEASUREMENT.md Part 5`) — this row is NOT in the flip manifest's declared set, so editing it
    here does not touch the flip commit's rule; then dispose the ledger rows through the tool only:
    `windows fixed 260` citing the run id and plan 33-37 (the win32 authority), `windows fixed 236` and
    `windows fixed 226`..`235` each with a reason citing the run id and the closing plan, `windows
    fixed 186` citing the green windows measurements by run id, and `windows waive 193` with D-16's
    reason and the revisit condition. Quote every command and run `windows status` afterwards,
    confirming the table row count and the JSON appendix agree.

    If EITHER leg is red: state `CAP-02 verdict on this run: NOT MET`; re-derive the failure inventory
    from that leg's own log (`gh api repos/abitwise/grugops/actions/jobs/<job id>/logs`, ANSI stripped,
    control bytes scanned before quoting) in the four-label shape — per file, per case, the assertion
    text, and a label — naming for each red which round-4 plan's mechanism it falsifies; update the
    CAP-02 coverage-table row to `Pending — NOT met: CI run <id> ...` with the counts and `round 4 of 4
    (last under the cap)`; do NOT flip any ledger row (rows 226-236, 260 stay open; append new rows
    through the tool for any NEW class, one per class). No fix in this plan; round 4 is the last under
    the cap and the summary says so.

    Either way, plan 33-41 reads this verdict as its checkpoint text.
  </action>
  <verify>
    <automated>gh run view "$(gh run list --branch main --limit 1 --json databaseId --jq '.[0].databaseId')" --json headSha,conclusion,jobs --jq '{headSha, conclusion, jobs: [.jobs[] | {name, conclusion}]}'</automated>
    <fails_when>non-zero exit, or the printed headSha differs from the sha Part 5 recorded, or any job's conclusion is null (the run is not complete)</fails_when>
    <automated>grep -a -cE 'CAP-02 verdict on this run: (MET|NOT MET)' .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md</automated>
    <fails_when>a count other than 4 — Parts 2, 3 and 4 carry one verdict line each and Part 5 must carry exactly one more</fails_when>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status</automated>
    <fails_when>non-zero exit, or the counters disagree with the table's row count — a row was hand-edited</fails_when>
    <automated>S=$(grep -a -oE 'pre-push HEAD sha: [0-9a-f]{40}' .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md | tail -1 | grep -a -oE '[0-9a-f]{40}') &amp;&amp; test -n "$S" &amp;&amp; D=$(git diff --stat "$S..HEAD" -- scripts install hooks agent-factory .claude) &amp;&amp; test -z "$D"</automated>
    <fails_when>non-zero exit — the `pre-push HEAD sha:` line is absent from Part 5, the diff could not be computed, or a source file changed after that sha (a fix inside the measurement plan)</fails_when>
  </verify>
  <acceptance_criteria>
    - Part 5 quotes the run id, head sha, run conclusion and BOTH jobs' `conclusion` fields from `gh run view --json`; the command is stated; the suite totals are quoted from the logs.
    - Exactly one verdict line in Part 5; on MET the CAP-02 requirement row is flipped with the run id and row 260 (fixed, plan 33-37) plus rows 226-236 (fixed) and 186 (fixed)/193 (waived) are disposed through the tool with the commands quoted; on NOT MET the red inventory is re-derived in the four-label shape with per-red falsified-mechanism attribution, the cap-reached hand-off is stated, and no ledger row is flipped.
    - No source file changed in this plan (the diff over `scripts install hooks agent-factory .claude` between the recorded pre-push sha and HEAD is empty).
    - `windows status` counters agree with the table and the JSON appendix.
  </acceptance_criteria>
  <done>CAP-02 has a conclusion word from a runner, quoted, and the ledgers say exactly what it supports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a named human confirmation -> a push to the default branch | The only place this plan changes anything outside the working tree |
| a CI run's JSON -> the requirement ledger | A narrated conclusion would be a fabricated gate result |
| a red leg -> the temptation to fix inside the measurement | Fix-and-re-push inside one plan erases the measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-178 | Elevation of privilege | an unconfirmed push | critical | mitigate | Task 2 is a blocking-human checkpoint; Task 3's precondition names it and re-checks HEAD == origin/main. |
| T-33-179 | Repudiation | a narrated CI conclusion | high | mitigate | Task 3 quotes `gh run view --json` fields; the verify command re-reads them. |
| T-33-180 | Tampering | fix-and-re-push inside the round | high | mitigate | Prohibited in must-haves; the verify block asserts no source diff after the pre-push sha; a red leg is the cap-reached finding. |
| T-33-181 | Tampering | the ledger's three representations | medium | mitigate | Rows change only through the tool; `windows status` asserted afterwards. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; the run's install uses the committed lockfile only. |
</threat_model>

<verification>
- `gh run view <id> --json conclusion,jobs` — both legs' conclusions quoted in Part 5.
- `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status` — representations agree.
- `git diff --stat PRE_PUSH_SHA..HEAD -- scripts install hooks agent-factory .claude` — empty; no source touched.
</verification>

<success_criteria>
- One pushed run, both legs' conclusions quoted; CAP-02 flips only on `success` for both; row 260 disposed on green; a red leg is the cap-reached finding, nothing fixed here.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `33-CI-MEASUREMENT.md` Part 5 | measurement record | pre-push inventory, the run, the verdict, the dispositions |
| `CAP-02 verdict on this run: MET` or `... NOT MET` | verdict line | exactly one in Part 5 |
| REQUIREMENTS.md CAP-02 row | ledger change | flipped only on both-legs `success` |
| WINDOWS.md rows 260, 226-236 (fixed), 186 (fixed), 193 (waived) | ledger changes via tool | green branch only |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-40-SUMMARY.md` when done
</output>
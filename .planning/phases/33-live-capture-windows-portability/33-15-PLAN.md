---
phase: 33-live-capture-windows-portability
plan: 15
type: execute
wave: 7
depends_on: ["33-04", "33-05", "33-09"]
files_modified:
  - scripts/context-io.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-02]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every test-internal expectation in `scripts/context-io.test.ts` that the windows leg of run 35499800942 measured red for a SPELLING reason (a POSIX literal, an 8.3 short name, a JSON-escaped cell) now derives its expected value through the same host function the module applies (`resolve`, `realpathSync.native`, the renderer's own cell escaping) — never through a platform conditional (D-14, D-15)."
    - "Every case that plants a home directory plants `HOME` and `USERPROFILE` together, the way `asHome` already does next door, so `os.homedir()` reads the fixture on every host."
    - "The two `drive()` cases that produced no driver output on windows compose their deep fixture from single-character segments and assert the composed path's length as a premise, so the walk bound is exercised without exceeding a cwd the host can start a child in."
    - "Each fix is followed to every arm that consumes the changed value before the case is marked closed; the summary names the arm probed per case (the project's recorded failure mode is 'fix moved the red one arm over')."
  artifacts:
    - scripts/context-io.test.ts
  key_links:
    - "context-io.test.ts trustedRepoRoot cases -> `resolve(literal)` on the expectation side <- the module's `resolve(fromEnv.trim())`"
    - "context-io.test.ts tier-0 cases -> `realpathSync.native` on both sides <- `hostDeliveredRoot()`'s rung 1"
    - "context-io.test.ts CONTROL 5b / home-walk premise -> `{HOME, USERPROFILE}` plant <- `os.homedir()`"
    - "context-io.test.ts CR-24 plant table -> the renderer's exported cell escaping <- `render()`'s skip-report detail cell"
  prohibitions:
    - "no platform conditional closes a CAP-02 red (D-14/D-16)"
    - "no expected value is hard-coded in a spelling the host may not produce; it is derived through the host function the module uses"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): untouched by this plan."
    - "CAP-02 (edge row unresolved, flagged): this plan closes eleven context-io windows reds by mechanism reasoned from the CI log and reproduced where darwin can; the leg's conclusion is measured only by plan 33-20."
    - "CAP-03 (edge row unresolved, flagged): untouched by this plan."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). Twelve windows reds in `scripts/context-io.test.ts` survived run 35499800942
(ten predicted NOT ADDRESSED, row 226; two one-arm-over, row 234). This plan takes the eleven whose
mechanism is a TEST-SIDE spelling or plant (W-17..W-20, W-22..W-27) and leaves the two that need a
MODULE change (W-21 the SYMLINK cell, W-28 the ledger-position classification) and the unguarded
directory-symlink fixtures to plan 33-16, which follows in the next wave because it edits the same
test file.

Gap mapping (33-VERIFICATION.md CAP-02 `missing[1]`): "A fix for the 10 NOT-ADDRESSED
scripts/context-io.test.ts windows cases, ..." → Tasks 1-3 here (9 of the 10 plus W-27) and plan
33-16 (W-21 of the 10, plus W-28). Exact titles from 33-CI-MEASUREMENT.md § 2.4:
- Task 1: W-17 `trustedRepoRoot is ONE function, and an empty CLAUDE_PROJECT_DIR names nothing`; W-18 `trustedRepoRoot returns the TRIMMED value, as grantedBy does next door`; W-23 `hostDeliveredRoot() ACCEPTS a canonical, existing, version-controlled directory`; W-24 `tier 0 OUTRANKS tier 1, which is the only reason it is a tier at all`; W-25 `the canonicaliser's THREE rungs are each driven, and the rung this platform used is named`.
- Task 2: W-22 `CONTROL 5b (BOTH WAVES): the same store at a MARKER-LESS home declines`; W-26 `the three readings are taken against a kit home the COMMITTED installer created`; W-19 `BOUND: the ancestor walk is limited, so a configuration far above the cwd is not reached`; W-20 `the published step limit is the one the walk has, driven from the sentence itself`.
- Task 3: W-27 `every planted condition reports its OWN arm, read from the authority's discriminant`.

Decisions honored: D-13 (both legs), D-14 (no platform conditional), D-15 (assert the form the
module publishes, derived through the module's own function), D-16 (no conditional on the
watcher or symlink surfaces).

Purpose: the expected value on every one of these cases is produced by the same host function the
module runs, so the assertion cannot disagree with the host it runs on.
Output: `scripts/context-io.test.ts` with eleven cases corrected, each with its consuming arm named.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
@.planning/phases/33-live-capture-windows-portability/deferred-items.md
@.planning/phases/33-live-capture-windows-portability/33-04-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolved-path and canonical-path expectations derive through the module's own functions (W-17, W-18, W-23, W-24, W-25)</name>
  <read_first>
    - `scripts/context-io.test.ts` ~L3995-4010 and ~L4233-4242 (the two `/tmp/some-project` literals), ~L11329-11336, ~L11393-11402, ~L11404-11430 (the tier-0 cases and the three-rung case).
    - `scripts/context-io.ts` `trustedRepoRoot` (~L5237: `resolve(fromEnv.trim())`) and `hostDeliveredRoot` (its rung 1 is `realpathSync.native`; read the function to name its three rungs).
    - `install/install.test.ts` — `canonicalPath` (33-06's remedy for the 8.3 class: `realpathSync.native` on both sides).
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 rows W-17, W-18, W-23, W-24, W-25 (the assertion texts: `expected 'D:\tmp\some-project' to be '/tmp/some-project'`; `RUNNER~1` vs `runneradmin`).
  </read_first>
  <files>scripts/context-io.test.ts</files>
  <action>
    W-17 and W-18: the module returns `resolve(fromEnv.trim())`; the expectation becomes
    `resolve("/tmp/some-project")` — the same host function over the same literal — in all four
    assertions (the unset/empty/whitespace ones compare to `unset` and stay). Keep the literal
    POSIX on purpose: on darwin `resolve` is the identity and the case is unchanged; on win32 both
    sides spell `D:\tmp\some-project`.

    W-23 and W-24: the module's tier 0 returns rung 1's spelling (`realpathSync.native`); the
    expectations `realpathSync(r)` / `realpathSync(delivered)` become `realpathSync.native(...)`,
    the same remedy `install/install.test.ts`'s `canonicalPath` already applies.

    W-25: the rung-2 assertion `expect(realpathSync(r)).toBe(realpathSync.native(r))` asserts a
    SPELLING agreement the 8.3 class disproves; the property the case is after is that rung 2 names
    the same directory rung 1 names. Assert that as
    `expect(realpathSync.native(realpathSync(r))).toBe(realpathSync.native(r))` and reword the
    inline comment: rung 2 is a legitimate fallback because re-canonicalising its answer through
    rung 1 is the identity on rung 1's answer. Read the rest of the case (rung 3) and leave it
    unless it compares a portable realpath to a native one.

    For every case: name in the summary which module arm consumes the value (`trustedRepoRoot`'s
    env loop; `hostDeliveredRoot`'s rung 1; the tier ordering) and confirm no other assertion in the
    file compares `realpathSync(x)` to a module answer that came from `realpathSync.native`
    (`grep -n 'realpathSync(' scripts/context-io.test.ts` classified in the summary: each hit is
    either an expectation that must move to `.native`, or a comparison between two portable
    realpaths, which stays).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts -t "trustedRepoRoot is ONE function|returns the TRIMMED value|ACCEPTS a canonical|tier 0 OUTRANKS|THREE rungs"</automated>
    <fails_when>non-zero exit, or fewer than 5 tests reported passed, or 0 passed</fails_when>
    <automated>grep -a -c 'toBe("/tmp/some-project")' scripts/context-io.test.ts</automated>
    <fails_when>a count other than 0 — a bare POSIX literal is still the expected value somewhere</fails_when>
  </verify>
  <acceptance_criteria>
    - The five titles above pass on this host; the summary quotes the vitest summary line.
    - Every `realpathSync(` hit in the test file is classified in the summary as moved-to-native or portable-vs-portable.
    - No `process.platform` read was added: `grep -a -c 'process.platform' scripts/context-io.test.ts` is 0 (it is 0 on the dispatch base; quoted).
  </acceptance_criteria>
  <done>Five expectations now come from the host function the module runs.</done>
</task>

<task type="auto">
  <name>Task 2: Home plants name both variables; the deep-walk fixtures fit the host's cwd limit (W-22, W-26, W-19, W-20)</name>
  <read_first>
    - `scripts/context-io.test.ts` ~L10701-10725 (CONTROL 5b: `process.env.HOME = home` only), ~L15850-15900 (the reading driver: `env: { ...process.env, HOME: home }` and the PREMISE assertion at ~L15895), ~L6977 and ~L7522 (`asHome`: `{ HOME, USERPROFILE }`), ~L6290-6309 (`drive()`: `spawnSync("node", ..., { cwd: opts.cwd })` and the `driver produced no result` throw), ~L6464-6470 (BOUND: 70 `dN` segments), ~L7280-7294 (the near/far limit cases).
    - `scripts/context-io.ts` ~L4754-4780 — the home helper reads `os.homedir()`; the module's own residual text at ~L5416 states `HOME` or `USERPROFILE`.
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 rows W-19, W-20 (`driver produced no result for trustedRepoRoot:` with EMPTY stdout), W-22, W-26.
    - `.planning/phases/33-live-capture-windows-portability/deferred-items.md` — the ten-reds item, sub-items (1) and (4)/(5).
  </read_first>
  <files>scripts/context-io.test.ts</files>
  <action>
    W-22 and W-26: plant `HOME` and `USERPROFILE` together, the way `asHome` does, and restore both
    in the `finally` (CONTROL 5b mutates `process.env`; the reading driver passes an env object).
    `os.homedir()` reads `USERPROFILE` on win32 and `HOME` elsewhere; one plant, two names, no
    branch — the 33-04 `TMPDIR`/`TMP`/`TEMP` precedent. Probe the arm: CONTROL 5a next door plants
    the same way and must still MOVE (its title says so); run both.

    W-19 and W-20: the log shows the child driver produced NO stdout on windows — not a wrong
    answer, no answer. The hypothesis, stated as `UNKNOWN - verify` because the log does not name
    it: the composed cwd (`<temp>\p31-15-deep-XXXXXX\d0\d1\...\d69`, and the `limit + 6` variant)
    exceeds the length a Windows child can be started in (a `CreateProcess` current-directory
    bound near 260 characters), so `spawnSync` fails before the driver runs. Fix without a
    conditional: compose the deep fixtures from single-character segments
    (`Array.from({ length: N }, () => "d")` joined by `join`), which keeps the DEPTH the walk bound
    is about and roughly halves the length; and add a premise assertion on the composed path's
    length against a named ceiling constant (`DEEP_FIXTURE_MAX_PATH_CHARS = 240`, commented with
    the reason and the hypothesis), so a temp root long enough to break the premise is a named red
    on any host rather than an empty stdout. Apply the same composition to every `drive()` fixture
    deeper than a handful of levels (`grep -n 'Array.from({ length' scripts/context-io.test.ts`,
    classify each in the summary). Also make `drive()`'s throw quote `r.error?.message` and
    `r.status` beside the empty output, so the next log names the layer.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts -t "CONTROL 5b|CONTROL 5a|three readings are taken against a kit home|BOUND: the ancestor walk|published step limit"</automated>
    <fails_when>non-zero exit, or fewer than 5 tests reported passed, or 0 passed</fails_when>
    <automated>grep -a -c 'DEEP_FIXTURE_MAX_PATH_CHARS' scripts/context-io.test.ts</automated>
    <fails_when>a count below 3 — the ceiling constant is not declared and asserted at both deep fixtures</fails_when>
  </verify>
  <acceptance_criteria>
    - CONTROL 5b and the reading-driver case plant `HOME` and `USERPROFILE` together and restore both.
    - The BOUND and step-limit cases compose from single-character segments and assert the composed length under `DEEP_FIXTURE_MAX_PATH_CHARS`; the constant's comment states the hypothesis as `UNKNOWN - verify` with the observation that settles it (the pushed run's driver output).
    - `drive()`'s no-result throw now includes the spawn error message and the exit status.
    - The summary classifies every `Array.from({ length` fixture in the file.
    - No `process.platform` read was added.
  </acceptance_criteria>
  <done>Home is planted under both names, and the deep fixtures keep their depth without a length the host cannot start a child in.</done>
</task>

<task type="auto">
  <name>Task 3: The CR-24 plant table's detail cell is compared through the renderer's own escaping (W-27)</name>
  <read_first>
    - `scripts/context-io.test.ts` ~L14000-14062 — the plant-table case, `detailOf(md, file)`, `armOf`, `rowFor`, and the expectation `message.replace(/\|/g, "\\|")`.
    - `scripts/context-io.ts` `render` — the skip-report (`## Skipped entries`) row composition; find the function that turns the authority's message into the detail cell (whether it JSON-encodes, escapes backslashes, or escapes pipes only) — that function is the publishing boundary.
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 row W-27 (`expected 'context-io: the note file "C:\\Users\…' to be 'context-io: the note file "C:\Users\R…'` — the cell carries the JSON-escaped path, the message the host spelling) and WINDOWS.md row 234.
  </read_first>
  <files>scripts/context-io.test.ts</files>
  <action>
    Read `render`'s detail-cell composition and name the escaping it applies. If the module already
    exports that escaping function, the expectation becomes `<escape>(message)` instead of the
    hand-written pipe replacement; if it does not, export it from `scripts/context-io.ts` as the one
    cell-escaping authority — but that edit belongs to plan 33-16 (this plan does not touch the
    module), so in that case write the expectation here against the hand-derived form the renderer
    provably applies (read from the source, quoted in the summary) and leave a one-line note in the
    summary for 33-16 to replace it with the exported authority. On darwin the path carries no
    backslash, so the two forms coincide and the case stays green; on win32 the expectation now
    follows the cell's real escaping.

    Probe every arm the plant table drives: run the whole CR-24 case (every planted condition, not
    only `above-ceiling`) and confirm `unopenable` (33-05's FIFO routing) and `vanished` still file
    under their own arms.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts -t "every planted condition reports its OWN arm"</automated>
    <fails_when>non-zero exit, or 0 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - The expectation for the detail cell is derived through the renderer's escaping (exported authority or the source-quoted form), not a hand-written replacement that differs from it.
    - The summary quotes the renderer's cell-escaping source line and states which form was used.
    - Every arm of the plant table passes on this host.
  </acceptance_criteria>
  <done>The detail cell and its expectation are produced by one escaping, so the host's spelling cannot split them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a test's expected value -> the host it runs on | An expectation spelled for one host reads as a red on another and hides behind a green on the author's |
| a child process's cwd -> the host's process-start limits | A fixture the host cannot start a child in produces no answer, which a test can mistake for a wrong one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-77 | Repudiation | expectations hard-coded in one host's spelling | medium | mitigate | Tasks 1 and 3: expectations derived through `resolve`, `realpathSync.native`, and the renderer's escaping; no platform branch. |
| T-33-78 | Denial of service | a deep fixture cwd the host cannot start a child in | medium | mitigate | Task 2: single-character segments, a named length ceiling asserted as a premise, the spawn error quoted. |
| T-33-79 | Spoofing | a home plant the module never reads | medium | mitigate | Task 2: `HOME` and `USERPROFILE` planted together; CONTROL 5a's movement re-run as the converse. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `grep -a -c 'process.platform' scripts/context-io.test.ts` — 0 (0 on the dispatch base).
</verification>

<success_criteria>
- Eleven context-io windows reds are addressed by derivation, with the consuming arm named per case and the pushed-run titles listed.
- No platform conditional; no module edit (that is plan 33-16's file).
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `DEEP_FIXTURE_MAX_PATH_CHARS` | test constant, `scripts/context-io.test.ts` | the premise ceiling for deep `drive()` fixtures, with the `UNKNOWN - verify` hypothesis |
| corrected expectations for W-17, W-18, W-23, W-24, W-25, W-22, W-26, W-19, W-20, W-27 | test changes | each derived through the host function the module uses |
| `drive()` no-result diagnostics | test helper change | quotes the spawn error and exit status |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-15-SUMMARY.md` when done
</output>

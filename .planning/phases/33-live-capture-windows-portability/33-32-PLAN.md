---
phase: 33-live-capture-windows-portability
plan: 32
type: execute
wave: 21
depends_on: ["33-29", "33-30", "33-31"]
files_modified:
  - .planning/phases/33-live-capture-windows-portability/33-R3-DRYRUN-REPORT.md
  - .planning/phases/33-live-capture-windows-portability/33-CAPTURE-A.jsonl
  - .planning/phases/33-live-capture-windows-portability/33-CAPTURE-B.jsonl
  - .planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md
  - .planning/phases/33-live-capture-windows-portability/33-R3-DIAGNOSIS.md
autonomous: false
gap_closure: true
requirements: [CAP-03, CAP-01]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The push comes BEFORE the go, as its own blocking human checkpoint: the runner refuses unless HEAD equals `origin/main`, round 2 lost its go on that row alone, and this plan's documentation commits from plan 33-31 are local at dispatch — so the human pushes first, the readiness is re-derived at zero tokens against the pushed sha, and only then is a go asked for (round-3 closure target 8; D-10)."
    - "Nothing is committed between the push and the run: the round-3 dry-run report is written into the phase directory UNTRACKED before the go and committed together with the capture set after it; the only untracked entries under the directories the installer and the plugin read are none (the WR-03 row), and `.planning/` untracked entries are outside that scope and stated."
    - "At most one live run happens in this round, behind a blocking human go that states the measured floor (11.75 USD, 31 min 07 s for the whole runner on 2026-09-20), the per-call bound actually used (1 200 000 ms, D-12), the CI verdict of plan 33-31 § 4.3, and the honest expectation about the D-07 word on a kit where KIT (a) and (b) have landed; a go is for this occasion only (D-09) — nothing from rounds 1 or 2 carries forward."
    - "The run's artifacts are written AFTER the run; the transcript is filed unedited with exactly one outcome line matching the grammar (D-11); a red run gets a zero-token diagnosis in 33-DIAGNOSIS.md's shape and nothing flips (D-20); a loud skip is never a capture; the outcome word derives from the instrument only."
    - "The capture settles the two `UNKNOWN - verify` items the offline round left: whether the `--agent` session's `system/init` tool list carries the scoped admission tool (plan 33-28), and what the post-run plugin listing shows about the stale `0.1.0` rows (deferred-items.md) — both quoted from the run's own frames or the platform's own listing."
  artifacts:
    - .planning/phases/33-live-capture-windows-portability/33-R3-DRYRUN-REPORT.md
    - .planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md
  key_links:
    - "the human push -> `git rev-parse HEAD` == `git rev-parse origin/main` -> the runner's pushed-sha row -> `GO-READINESS: ready` -> the go checkpoint text"
    - "the runner's `Dual-path parity (D-07) — path-invariant projection` section -> the outcome line -> plan 33-33's flip-or-hold branch"
    - "the runner's pre-spawn `plugin provenance before the spawn` row (33-30) -> the spawn; the post-run provenance row -> the outcome line"
    - "the B-path `system/init` frame's `tools[]` -> the 33-28 `UNKNOWN - verify` settled"
  prohibitions:
    - "a loud skip is never accepted as the capture"
    - "no fix-and-rerun inside one gap-closure round (D-11)"
    - "the harness never sets the prod-deploy approval environment variable"
    - "the D-07 predicate is never softened to make a capture read as parity (D-20)"
    - "no commit lands between the push and the runner's start; the go is asked for against the pushed sha"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): the flip depends on this plan's outcome word being `pass` AND plan 33-31's verdict being MET; plan 33-33 performs or holds it."
    - "CAP-02 (edge row unresolved, flagged): read from plan 33-31 § 4.3 and quoted in the go text; the CONTEXT recommendation (capture against a tree green on both legs) is honored when it reads MET and stated plainly when it does not."
    - "CAP-03 (edge row unresolved, flagged): produced only by the capture this plan runs, on the instrument 33-29 and 33-30 corrected (every verdict input pre-spawn or on the pipe)."
---

<objective>
Gap-closure round 3 of the phase's four-round cap. The round's one live go (D-09), sequenced
strictly after the two KIT fixes (33-25, 33-28), the two writer/guard fixes (33-26, 33-27), the
instrument fixes (33-29, 33-30) and the CI measurement (33-31) — and, this time, with the push
placed as its own checkpoint BEFORE the go, because round 2's zero-token dry run read
`GO-READINESS: not-ready` on the pushed-sha row alone and the go was never reachable.

Gap mapping (33-VERIFICATION.md): CAP-01 `missing[2]` "A fresh go under D-09 ... preceded by a
zero-token dry run reading GO-READINESS: ready with the pushed-sha row MET — which requires
pushing the tested sha BEFORE the go decision" → Tasks 1-3; CAP-03 `missing[1]` "A fresh capture
on the corrected instrument, behind a new go under D-09" → Task 4.

Decisions honored: D-09 (one go per round, per occasion), D-10 (dry run reads ready first, at zero
tokens, against the pushed sha), D-11 (record, diagnose at zero tokens, no rerun), D-12 (the
bound: max(1 009 964, 842 417) ms × 1.2 = 1 211 957 ms, capped at the 20-minute ceiling — 1 200 000
ms, written into the summary by the runner), D-20 (nothing flips on a divergence), D-05/D-07 as
corrected by 33-12/33-29/33-30.

Purpose: spend the round's one go against a pushed sha, on an instrument whose every verdict
input the subject cannot reach, with the human told exactly what to expect.
Output: `33-R3-DRYRUN-REPORT.md` reading ready against the pushed sha, and either a round-3
capture set with one outcome line or a zero-token diagnosis.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md
@.planning/phases/33-live-capture-windows-portability/33-21-PLAN.md
@.planning/phases/33-live-capture-windows-portability/33-21-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-R2-DRYRUN-REPORT.md
@.planning/phases/33-live-capture-windows-portability/33-31-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Checkpoint — push the documentation commits so the go can be asked for against the pushed sha (push two of two)</name>
  <what-built>
    Plan 33-31's push carried every round-3 source commit and the CI run has its verdict in
    `33-CI-MEASUREMENT.md` Part 4 § 4.3. Since that push, plan 33-31's own documentation commits
    (Part 4, the requirement row, the ledger rows, its summary) have landed locally, so HEAD is
    again ahead of `origin/main` — and the runner's pushed-sha precondition reads UNMET on exactly
    that, as it did in round 2 (33-R2-DRYRUN-REPORT.md line 144). The instrument is corrected and
    proven at zero tokens on the final tree (33-29, 33-30: every verdict input on the pipe or
    before the spawn; provenance gated before the spawn; the grant scoped).

    This checkpoint asks only for the push. The go is the separate checkpoint after the
    readiness re-check (Task 3); no money is spent by answering here.
  </what-built>
  <how-to-verify>
    1. Run `git log --oneline origin/main..HEAD` — the carried commits are documentation only
       (no path under `scripts`, `install`, `hooks`, `agent-factory` or `.claude`; the executor
       has quoted `git diff --stat origin/main..HEAD -- scripts install hooks agent-factory .claude`
       as empty in its pre-checkpoint output).
    2. Push (`git push origin main`), then confirm `git rev-parse HEAD` equals
       `git rev-parse origin/main` after `git fetch`.
    3. Note: after this push NOTHING will be committed until the capture has run (or the go is
       refused); the executor writes the round-3 dry-run report into the phase directory
       untracked and commits it with the capture set.
  </how-to-verify>
  <resume-signal>Type "pushed" once HEAD equals origin/main, or describe what you want changed first</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Readiness against the pushed sha at zero tokens — the report is written, not committed</name>
  <precondition>Task 1 was answered `pushed`: after `git fetch`, `git rev-parse HEAD` equals `git rev-parse origin/main`, and `git status --porcelain -- agent-factory .claude .claude-plugin install skills hooks scripts AGENTS.md` is empty (the WR-03 scope; `.planning/` untracked entries are outside it and are listed in the summary).</precondition>
  <read_first>
    - `scripts/capture-live.ts` as landed by 33-29 and 33-30 — the readiness table (the pushed-sha row, the working-tree row), the pre-spawn provenance row, the `tool grant` rows, `DRY_RUN_REPORT_NAME`, `READINESS_PREFIX`, `verifyArtifacts`'s "exactly one report" rule.
    - `.planning/phases/33-live-capture-windows-portability/33-R2-DRYRUN-REPORT.md` — the round-2 report's shape (the eleven precondition rows; now more) and its `not-ready` line as the thing this round must not repeat.
    - `.planning/phases/33-live-capture-windows-portability/33-21-PLAN.md` Task 1 — the filing shape.
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-R3-DRYRUN-REPORT.md</files>
  <action>
    Run `node scripts/capture-live.js --dry-run --out SCRATCH_DIR` and copy its report into the
    phase directory as `33-R3-DRYRUN-REPORT.md` WITHOUT committing it (the fixture-derived JSONL
    stays in scratch — it is not a capture). Quote the `GO-READINESS:` line and the full
    precondition table in the summary. The report must show `GO-READINESS: ready`, one
    `OUTCOME: no-go` line, the `working tree matches HEAD` row MET, the pushed-sha row MET
    naming the sha plan 33-31 measured, the `tool grant` rows with the scoped `Edit(//…/**)` rule
    and the scoped admission tool, and the `plugin provenance before the spawn` row — over the
    dry run's scratch targets it reads whatever the install route yields at zero tokens; state
    which state it read.

    Re-run the flip gate (`node scripts/check-flip-manifest.js`) and quote the declared-set count
    (14, `.planning/PROJECT.md` first) and `ALL CHECKS PASSED` — plan 33-33 carries the fourteen
    on that evidence.

    If readiness is not `ready`: STOP. Commit the report as the round's finding with the unmet
    rows quoted, record the outcome word `no-go` and the reason in the summary, and do not present
    Task 3 (D-10). Plan 33-33 takes its hold branch on that record.
  </action>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"</automated>
    <fails_when>non-zero exit — HEAD is not the pushed sha; the go must not be asked for</fails_when>
    <automated>test -z "$(git status --porcelain -- agent-factory .claude .claude-plugin install skills hooks scripts AGENTS.md)"</automated>
    <fails_when>non-zero exit — a directory the installer or the plugin reads carries an uncommitted or untracked entry</fails_when>
    <automated>grep -a -c 'GO-READINESS: ready' .planning/phases/33-live-capture-windows-portability/33-R3-DRYRUN-REPORT.md</automated>
    <fails_when>a count other than 1 — readiness is not ready against the pushed sha (on that branch the report is committed as the finding and Task 3 is not presented)</fails_when>
    <automated>grep -a -cE '^(OUTCOME|Outcome): (pass|fail|hang|no-go)$' .planning/phases/33-live-capture-windows-portability/33-R3-DRYRUN-REPORT.md</automated>
    <fails_when>a count other than 1, or the word on that line is anything other than no-go</fails_when>
    <automated>node scripts/check-flip-manifest.js</automated>
    <fails_when>non-zero exit, or the output lacks ALL CHECKS PASSED</fails_when>
    <automated>git ls-files --error-unmatch .planning/phases/33-live-capture-windows-portability/33-R3-DRYRUN-REPORT.md 2&gt;/dev/null; test $? -ne 0</automated>
    <fails_when>non-zero exit — the report was committed before the go (nothing lands between the push and the run)</fails_when>
  </verify>
  <acceptance_criteria>
    - `33-R3-DRYRUN-REPORT.md` exists in the phase directory, untracked, with `GO-READINESS: ready`, the pushed-sha row MET naming the 33-31 sha, the working-tree row MET, the `tool grant` rows and the pre-spawn provenance row; the summary quotes the whole precondition table.
    - The flip gate's declared-set count (14) and pass line are quoted.
    - HEAD equals `origin/main` and the scoped working tree is clean at the end of the task; the `.planning/` untracked entries are listed as outside the scope.
  </acceptance_criteria>
  <done>The machinery is proven ready against the sha the human pushed, at zero cost, and nothing has moved since.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint — the live capture go, one occasion only (D-09)</name>
  <what-built>
    Readiness against the pushed sha reads `GO-READINESS: ready` in
    `.planning/phases/33-live-capture-windows-portability/33-R3-DRYRUN-REPORT.md` (untracked
    until the run is over). The instrument scores only what it received on the pipe and what it
    derived before the spawn (33-29), refuses to spawn unless the installed plugin copy is the
    checkout by content (33-30), and grants file-writing tools only under the target. The kit now
    carries the human's two directions: the coordinator's grant names the plugin's admission tool
    (33-28) and the reader refuses hand-written notes (33-25); the guard reads redirections
    (33-27) and the writer refuses an absent field (33-26).

    This checkpoint authorizes ONE live capture run, for this occasion only (D-09). A go recorded
    in round 1 or round 2 authorizes nothing here.

    THE COST, stated plainly because it is real money. The round-1 run on 2026-09-20 cost 11.75 US
    dollars in reported model cost and took 31 minutes 07 seconds for the whole runner (two model
    sessions plus two plugin installs). The per-call bound used this time is 1 200 000 ms (D-12:
    the round-1 per-call maximum with 20 percent headroom, capped at the 20-minute ceiling) and is
    written into the summary by the runner. Expect a comparable or larger figure.

    THE CI VERDICT for the tree you are capturing on is quoted here by the executor from
    `33-CI-MEASUREMENT.md` Part 4 § 4.3 (`CAP-02 verdict on this run: MET` or `NOT MET`, with the
    run id). CONTEXT's recommendation was to capture against a tree green on both legs; if the
    verdict is NOT MET, a `pass` capture can only reach `hold-for-ci` at plan 33-33 (the Phase-20
    rows cite the CI run), and round 4 is the last under the cap.

    THE EXPECTATION, stated plainly. Two things can make the D-07 word read `fail` for reasons that
    are not instrument defects: (1) per-role note counts and kind multisets are model-chosen —
    one sample per path cannot separate variance from mechanism (33-DIAGNOSIS § 1.3 (iii)), and
    D-20 forbids softening the predicate; (2) on the spawn path, nested role sessions still receive
    no plugin MCP tool (A:784) — the coordinator now can propose notes, but whether the role agents
    find the in-process writer under the plugin cache (as path A's did, A:839) or hand-write notes
    the reader now refuses (side (b) then fails on path B by name) is model behaviour the capture
    measures. Either way the run is the CAP-03 observation on an instrument the subject cannot
    write to, and it settles the two `UNKNOWN - verify` items (the B-path tool list; the post-run
    plugin listing). A red run is diagnosed at zero tokens and not re-run this round (D-11).
  </what-built>
  <how-to-verify>
    Before you answer:
    1. Read `33-R3-DRYRUN-REPORT.md` — the precondition table (every row MET), the pre-spawn
       provenance row, the `tool grant` rows.
    2. Read `33-CI-MEASUREMENT.md` Part 4 § 4.3 — the CI verdict quoted above.
    3. Confirm `git log --oneline origin/main..HEAD` is empty (the executor has quoted it).
    4. Confirm you are willing to spend on the order of 12 US dollars or more and wait about
       35 minutes.

    What the run does: builds two independent targets from the committed fixture project,
    installs the plugin from the existing marketplace row into each and refuses to spawn unless
    the installed copy is the checkout by content, runs the same request through both dispatch
    paths under the bound with the scoped grant, streams each transcript into a runner-owned
    scratch while scoring the bytes it receives on the pipe, exercises the harmless matched
    deployment probe so the pre-tool guard fires, derives the summary with the path-invariant
    parity section, redacts, uninstalls on every path, and prints exactly one outcome line. The
    approval environment variable is never set by the harness.

    What comes back either way: two redacted transcripts, a derived summary with one outcome line,
    the round-3 dry-run report, and — if red — a zero-token diagnosis naming the cause class per
    axis and the observation that would distinguish it from the next class.
  </how-to-verify>
  <resume-signal>Type "go" to authorize this one live run, or "no-go" to record the outcome as no-go and close the round without spending</resume-signal>
</task>

<task type="auto">
  <name>Task 4: The capture — one run, artifacts written after, one outcome line; or the recorded no-go</name>
  <precondition>Task 3 was answered for this occasion (a go recorded in any earlier plan or round does not authorize this run, D-09); `git rev-parse HEAD` equals `git rev-parse origin/main` and the scoped working tree (`git status --porcelain -- agent-factory .claude .claude-plugin install skills hooks scripts AGENTS.md`) is empty at the moment the runner starts; the only untracked file under the phase directory is `33-R3-DRYRUN-REPORT.md`.</precondition>
  <read_first>
    - `scripts/capture-live.ts` as landed by 33-29 and 33-30 — the run phase, `runTarget` (pre-spawn provenance, scoped grant, pipe scoring, `finally` uninstall), `writeArtifacts`, `verifyArtifacts`, `CAPTURE_SUMMARY_NAME`, the Run-table rows (`tool grant`, `plugin uninstall`, `spawn grant drift`, both provenance rows).
    - `.planning/phases/33-live-capture-windows-portability/33-21-PLAN.md` Task 4 and `33-10-PLAN.md` Task 3 — the capture task shape; `33-10-SUMMARY.md` for how the artifacts were committed.
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` — the diagnosis shape (§ 0 the run table; cause class per axis; the distinguishing observation) a red run must repeat at zero tokens, and § 1.3's three axes to compare against.
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-SUMMARY.md` — the outcome-line grammar and the no-later-plan-may-cite rule.
    - `.planning/phases/33-live-capture-windows-portability/deferred-items.md` — the stale `0.1.0` plugin rows item (the post-run `claude plugin list` answers it).
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-CAPTURE-A.jsonl, .planning/phases/33-live-capture-windows-portability/33-CAPTURE-B.jsonl, .planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md, .planning/phases/33-live-capture-windows-portability/33-R3-DIAGNOSIS.md, .planning/phases/33-live-capture-windows-portability/33-R3-DRYRUN-REPORT.md</files>
  <action>
    If Task 3 answered `no-go`: write no capture; commit `33-R3-DRYRUN-REPORT.md` alone; record in
    the plan summary the word `no-go`, the reason the human gave, and that nothing flips; plan
    33-33 takes its hold branch and plan 33-34 records the round; stop here.

    Otherwise run the capture ONCE: `node scripts/capture-live.js --out SCRATCH_DIR --keep-target`
    (keep-target so a post-run question can be answered without a second spend; the targets are
    removed by hand after the artifacts are committed and the summary records that). Let the
    runner do every derivation. Then copy the artifact set into the phase directory under the
    names the flip manifest keys on (`33-CAPTURE-A.jsonl`, `33-CAPTURE-B.jsonl`,
    `33-CAPTURE-SUMMARY.md`) — AFTER the run, never before — run the runner's own re-check over
    the phase directory and the NUL-byte gate, run `claude plugin list` once and quote the
    grugops rows verbatim (the stale-rows deferred item), and commit the set TOGETHER with
    `33-R3-DRYRUN-REPORT.md` in one commit whose message states the outcome word, both runs' exit
    statuses, the parity verdict, both provenance states and the uninstall exits.

    Settle the 33-28 `UNKNOWN - verify` from the frames: quote path B's `system/init` `tools[]`
    entry for the admission tool (present or absent) with its line id, and path A's for
    comparison; state which of the two paths' coordinators called `propose_note` and how many
    times (the route rows already count it).

    If the outcome word is `pass`: stop; plan 33-33 performs the flip against this summary.

    If the outcome word is `fail` or `hang`: file `33-R3-DIAGNOSIS.md` at ZERO tokens in
    33-DIAGNOSIS.md's shape — the run table (§ 0's shape: exits, CAP-03 sides, deny, plugin,
    both provenance states, the grant rows, the uninstall exits, cost/duration), the cause class
    per divergence axis (SUITE / KIT / platform / host) with the transcript line citations, and
    for each the observation that distinguishes it from the next class; compare the note-route
    (now three counts), per-role count and kind axes against § 1.3's round-1 values and state
    which moved; state whether side (b) on path B failed for the KIT (b) reason (hand-written
    notes refused) or held (the writer was found). Do not fix, do not rerun. Nothing flips (D-20);
    plan 33-33 takes its hold branch; round 4 is the last under the cap and the diagnosis says so.

    If the runner stopped before the spawn (a failed install, a provenance refusal, an unmet
    precondition): the summary quotes the `CAPTURE NOT DERIVED` line and the preserved
    transcript-scratch paths (if any); that is a round-3 finding, not a capture, and the round
    closes on it with zero or partial spend stated.
  </action>
  <verify>
    <automated>grep -a -cE '^(OUTCOME|Outcome): (pass|fail|hang|no-go)$' .planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md</automated>
    <fails_when>a count other than 1 — exactly one grammar-conformant outcome line (on the no-go branch this file is absent and the summary records the word instead; state which branch in the summary)</fails_when>
    <automated>node scripts/capture-live.js --verify-artifacts --out .planning/phases/33-live-capture-windows-portability</automated>
    <fails_when>non-zero exit, or a REFUSED line naming an uncited claim row, a surviving home-path spelling, a control byte in a cell, or a duplicated outcome line</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the output names a committed transcript as carrying a control byte</fails_when>
    <automated>grep -a -c 'plugin provenance before the spawn' .planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md</automated>
    <fails_when>a count of 0 — the summary was written by a runner without the 33-30 pre-spawn row</fails_when>
    <automated>git ls-files --error-unmatch .planning/phases/33-live-capture-windows-portability/33-R3-DRYRUN-REPORT.md</automated>
    <fails_when>non-zero exit — the round-3 dry-run report was not committed with the capture set (or alone on the no-go branch)</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one live run was made in this round, or none with `no-go` recorded; the summary records the two calls, their durations, reported costs, the bound actually used, both provenance states, the grant rows and the uninstall exits.
    - The committed summary's `Dual-path parity (D-07) — path-invariant projection` section lists both projections (three route counts each) and either `parity: the two projections are equal` or the named diffs; an empty list is stated as empty.
    - Path B's `system/init` tool-list entry for the admission tool is quoted with its line id (the 33-28 item settled); the post-run `claude plugin list` grugops rows are quoted (the deferred item answered).
    - On `fail`/`hang`: `33-R3-DIAGNOSIS.md` exists, spends zero tokens, names a cause class per axis with citations, states the side-(b) reason on path B, and no fix or rerun happened.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-SPAWN-03-RUNTIME-EVIDENCE.md` and every flip-manifest file are unchanged by this plan.
    - The artifacts and the dry-run report were committed after the run in one commit; no commit sits between the push and the runner's start (the summary quotes `git log --oneline origin/main..HEAD` empty at start).
  </acceptance_criteria>
  <done>One run against the pushed sha, one artifact set, one outcome line — on an instrument whose every input the subject could not reach — or an honest no-go.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an authorized human go -> real money and a model session | Per-occasion, never inherited (D-09) |
| the pushed sha -> the marketplace install -> the capture | A commit between the push and the run makes the two paths different code while the row reads MET |
| platform stream output -> a committed artifact | Untrusted model and CLI output crosses into git history |
| the human's push -> the executor's next step | The executor never pushes; a push checkpoint answered `pushed` is verified by sha equality, not trusted by word |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-145 | Repudiation | an inherited or re-used go | critical | mitigate | Task 3 is per-occasion; Task 4's precondition names it; no later plan may cite it. |
| T-33-146 | Spoofing | capture against unpushed or dirty code | high | mitigate | Task 1 pushes first; Task 2's precondition and verify assert HEAD == origin/main and the scoped tree clean; Task 4 re-checks at start; the dry-run report stays untracked until after the run. |
| T-33-147 | Elevation of privilege | the approval environment variable | critical | mitigate | The runner never sets it; asserted on the constructed child environment before every spawn (33-01). |
| T-33-148 | Information disclosure | the committed transcript | high | mitigate | Redaction fails closed; `--verify-artifacts` refuses survivors and control bytes (33-30); NUL-byte gate run. |
| T-33-149 | Tampering | fix-and-rerun inside the round | high | mitigate | Prohibited; a red run files a zero-token diagnosis and round 4 owns the fix. |
| T-33-150 | Tampering | the D-07 predicate softened to reach `pass` | critical | mitigate | 33-12's Test A and 33-30's P10 pin the held capture's divergences; this plan changes no source. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; the plugin install is a platform operation over an existing marketplace row, gated by content before the spawn and uninstalled in `finally`. |
</threat_model>

<verification>
- `test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"` before the go and at the runner's start.
- `node scripts/capture-live.js --dry-run --out SCRATCH` — exit 0, `GO-READINESS: ready`, filed untracked as `33-R3-DRYRUN-REPORT.md` before the go and committed after the run.
- `grep -acE '^(OUTCOME|Outcome): (pass|fail|hang|no-go)$' 33-CAPTURE-SUMMARY.md` — exactly 1.
- `node scripts/capture-live.js --verify-artifacts --out PHASE_DIR` and `npm run check:nul-bytes` — exit 0.
</verification>

<success_criteria>
- The push precedes the go as its own checkpoint; readiness reads `ready` against the pushed sha; nothing is committed in between.
- At most one live run, artifacts written after, one outcome line — or a recorded no-go — with the two `UNKNOWN - verify` items settled from the run's own frames and listing, and a zero-token diagnosis on red.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `33-R3-DRYRUN-REPORT.md` | new artifact | round-3 readiness against the pushed sha (D-10); written before the go, committed after the run |
| `33-CAPTURE-A.jsonl`, `33-CAPTURE-B.jsonl`, `33-CAPTURE-SUMMARY.md` | new artifacts (go branch) | the round-3 capture under the manifest-keyed names |
| `33-R3-DIAGNOSIS.md` | new artifact, conditional | zero-token diagnosis on `fail`/`hang` |
| the B-path tool-list quotation; the post-run plugin listing | summary sections | the 33-28 `UNKNOWN - verify` and the stale-rows deferred item, settled |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-32-SUMMARY.md` when done
</output>

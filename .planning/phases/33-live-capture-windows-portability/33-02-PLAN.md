---
phase: 33-live-capture-windows-portability
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - vitest.config.ts
  - scripts/runnable-ref/uat-spec-integrity.test.ts
  - scripts/runnable-ref/uat-spec-integrity.ts
  - scripts/runnable-ref/uat-spec-integrity.js
autonomous: true
requirements: [CAP-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A test slower than five seconds is printed as a warning and still runs to completion; it is never failed for being slow (D-14)."
    - "No bound in `vitest.config.ts` is conditional on the host operating system (D-14)."
    - "A setup hook that exceeds ten seconds no longer dies at the inherited bound — `hookTimeout` is set alongside `testTimeout`, the gap RESEARCH found in D-14."
    - "An absent `.temp` scratch root and an empty one are the same fact for the premise that asserts nothing was left behind, so a fresh runner with no `.temp` is green on both legs (D-13)."
    - "The deepest-directory probe derives its depth from what the host platform actually permits and reports a refusal rather than throwing, on any platform."
  artifacts:
    - vitest.config.ts
    - scripts/runnable-ref/uat-spec-integrity.test.ts
  key_links:
    - "vitest.config.ts `testTimeout`/`hookTimeout` -> every test file in the regression lane on both matrix legs"
    - "uat-spec-integrity.test.ts `.temp` premise -> the same classification the scanner already holds (`SKIPPED_DIRECTORIES`, `could-hide-evidence`)"
  prohibitions:
    - "no platform conditional converts an unmeasured Windows outcome into a green"
    - "`npm test` is never run in this phase"
    - "no runtime dependency is added to package.json"
    - "no hand-typed set stands where the set can be derived and its cardinality asserted"
---

<objective>
Close the two failure classes that are red on BOTH matrix legs: the inherited five-second test bound
and the absent `.temp` scratch root.

D-13 makes both legs green the CAP-02 bar, and the ubuntu leg's 12 failures are 9 in this one file
plus 3 that are themselves five-second cut-offs. D-14 fixes the mechanism — raise the global bound
so a slow test warns rather than fails, with no host-operating-system branch anywhere. RESEARCH
found the gap D-14 does not cover: one breach is `Error: Hook timed out in 10000ms` in a `beforeAll`,
which `testTimeout` does not govern, so `hookTimeout` lands here too.

Purpose: remove the two causes that make every other Windows measurement ambiguous, so the remaining
classes can be diagnosed on their own evidence.
Output: three new bounds in the root vitest config, a `.temp` premise that treats absent and empty
alike, and a depth probe that cannot throw.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-RESEARCH.md
@.planning/phases/33-live-capture-windows-portability/33-PATTERNS.md
</context>

<flagged_planner_assumptions>
The deterministic edge probe classified **CAP-02** as `unclassified` and it stays **unresolved** —
surfaced here as an explicit planner assumption rather than auto-resolved:

- **CAP-02 edge, unresolved:** the probe could not classify the boundary of "the CI leg is green",
  because the failing set's true durations were never measured (each was cut at the inherited
  5000 ms bound, and vitest reports elapsed time rather than the bound — `GREEN 4` reported
  33354 ms while bounded at 5000 ms). The planner's assumption, carried from RESEARCH assumption
  A3: a `testTimeout` of 180000 ms clears every timed-out test on both legs. This is a CHOICE ABOVE
  A MEASURED FLOOR of 85568 ms (the slowest completed test, windows leg, run 35394268365), not an
  arithmetic derivation. It is cheap to falsify: the next CI run reports the new slowest test, and
  plan 33-09 re-measures it.
</flagged_planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Three bounds in the root vitest config, each with its measured justification</name>
  <read_first>
    - `vitest.config.ts` (whole file) — every existing option carries a measured justification
      comment; the two new ones arrive the same way. `test` currently holds only `exclude` and
      `fileParallelism`, so the inherited test bound is vitest's 5000 ms default.
    - `.planning/phases/33-live-capture-windows-portability/33-RESEARCH.md`
      § "Slowest measured CI test durations — the input to D-14's bound" (all three facts) and
      Pitfalls 4 and 5.
    - `.planning/phases/33-live-capture-windows-portability/33-PATTERNS.md` § "`vitest.config.ts`".
  </read_first>
  <files>vitest.config.ts</files>
  <action>
    Add exactly three keys to the `test` object: `testTimeout: 180_000`, `hookTimeout: 120_000` and
    `slowTestThreshold: 5_000`. Leave `exclude` and `fileParallelism` byte-unchanged.

    Write the justification comment in the file's established style, stating these facts and no
    others: the slowest COMPLETED test on any leg measured 85568 ms (windows, run 35394268365, the
    gate-plantable corpus case), and it passes today only because it carries an explicit per-test
    argument; the failing set's true durations are UNMEASURED because vitest reports elapsed time on
    a cut test, so 180000 ms is a choice above a measured floor rather than a derivation; the
    separate hook bound exists because one breach is a setup hook dying at 10000 ms in
    `scripts/check-foundation-guards.test.ts`, which the test bound does not govern; and the slow
    threshold of 5000 ms exists so that every test that used to die at the old default is now
    PRINTED as slow and still completes — the user's rule is to warn, not to prevent the run (D-14).
    State in the comment that no bound here may become conditional on the host operating system, and
    why: a conditional bound converts an unmeasured outcome into a green.

    Do not introduce any host-operating-system predicate into this file.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts</automated>
    <fails_when>non-zero exit, or the output contains "Hook timed out", or the summary reports 0 passed</fails_when>
    <automated>grep -c 'testTimeout\|hookTimeout\|slowTestThreshold' vitest.config.ts</automated>
    <fails_when>a count below 3, or a non-zero exit from grep (no match at all)</fails_when>
    <automated>grep -c 'process\.platform' vitest.config.ts || true</automated>
    <fails_when>any count above 0 on the filtered file — a host-OS predicate reached a bound</fails_when>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` declares `testTimeout: 180_000`, `hookTimeout: 120_000` and
      `slowTestThreshold: 5_000` inside the `test` object.
    - `grep -c 'process\.platform' vitest.config.ts` is 0.
    - The justification comment names the 85568 ms floor, the run id 35394268365, and the reason the
      bound is a choice rather than a derivation.
    - The `exclude` array's three entries and the `fileParallelism` value are byte-identical to the
      pre-task values quoted in the summary; only the three new keys were added.
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts` exits 0
      and its output contains no "Hook timed out" line.
  </acceptance_criteria>
  <done>The three bounds are in the root config with a measured justification and no host-OS branch, and the previously hook-bounded file runs clean.</done>
</task>

<task type="auto">
  <name>Task 2: An absent scratch root and an empty one are the same fact</name>
  <read_first>
    - `scripts/runnable-ref/uat-spec-integrity.test.ts:8930-8945` — the premise that reads the real
      listing and asserts length 0; on a fresh runner the read throws before the assertion.
    - `scripts/runnable-ref/uat-spec-integrity.ts:80` (`SKIPPED_DIRECTORIES` contains the scratch
      root) and `:126` (its disclosure class is `could-hide-evidence`).
    - `scripts/runnable-ref/uat-spec-integrity.ts:2084-2091` — the scanner's own tolerant boundary:
      the read sits in a `try`, an unreadable directory pushes a refusal and the walk continues.
    - `.gitignore:19` — the scratch root is gitignored, so it exists nowhere on a fresh checkout.
    - `.planning/phases/33-live-capture-windows-portability/33-RESEARCH.md` Pitfall 10 (why creating
      the directory in CI is the wrong boundary) and Open Question 5 (whether the freshness clone
      failures are downstream of the same absence).
    - `.planning/phases/33-live-capture-windows-portability/33-PATTERNS.md` § "`.temp` absence".
  </read_first>
  <files>scripts/runnable-ref/uat-spec-integrity.test.ts</files>
  <action>
    Change the premise so that a directory that does not exist and a directory that exists with zero
    entries produce the SAME verdict: nothing was left behind. Put the listing inside a `try`, treat
    a read error whose code is ENOENT as a zero-length listing, and re-throw any other error so a
    permissions problem is still a finding. State in the comment that the directory is already a
    classified member of the skipped set with a `could-hide-evidence` disclosure class, so a
    directory the scanner has already decided to skip must not need to exist for the scan to
    complete.

    Do not create the directory in CI and do not add a setup step that creates it: that would make
    the leg green while leaving the premise brittle for every other absent root, and it would
    diverge CI from a fresh developer clone.

    Then re-check RESEARCH Open Question 5 before plan 33-06 runs: record in the plan summary whether
    the eight `scripts/freshness.test.ts` Windows failures are downstream of this same absence, by
    stating what the freshness suite does when its working directory under the scratch root does not
    exist. A one-line finding either way; do not fix that file here.
  </action>
  <verify>
    <automated>rm -rf .temp &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts</automated>
    <fails_when>non-zero exit, or the output contains "ENOENT", or the summary reports 0 passed</fails_when>
    <automated>mkdir -p .temp &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports 0 passed — the empty-directory case must stay green too</fails_when>
  </verify>
  <acceptance_criteria>
    - With the scratch root removed, the file's suite exits 0 and its output contains no ENOENT line.
    - With the scratch root present and empty, the same suite still exits 0 — both states are green,
      which is what "absent and empty are the same fact" means.
    - With a stray file planted under the scratch root, the premise still FAILS: the tolerance is for
      absence only, and the executor records the planted-file reproduction in the summary.
    - No new step creating the scratch root appears in `.github/workflows/ci.yml`; the summary quotes
      `git status --porcelain -- .github/workflows/ci.yml` as empty.
    - The plan summary carries a one-line finding on whether the freshness suite's eight Windows
      failures are downstream of the same absence.
  </acceptance_criteria>
  <done>A fresh checkout with no scratch root runs this suite green on both legs, and a planted stray file still reds it.</done>
</task>

<task type="auto">
  <name>Task 3: The deepest-directory probe derives its depth and cannot throw</name>
  <read_first>
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` — the case named "GREEN 4: a directory tree
      as deep as this platform permits is derived without a throw", plus the two neighbouring cases
      that expect specific TypeScript diagnostic codes.
    - `scripts/runnable-ref/uat-spec-integrity.ts` — the walk `deriveSpecPaths` uses, and its
      refusal-pushing boundary at `:2084-2091`.
    - `.planning/phases/33-live-capture-windows-portability/33-RESEARCH.md` § "New Windows failure
      classes NOT in CONTEXT.md" item 8 (the `RangeError` and the two diagnostic-code mismatches),
      and § Metadata (this class is MEDIUM confidence, not reproduced offline).
    - `.planning/phases/33-live-capture-windows-portability/33-PATTERNS.md` § "No Analog Found" row 2.
  </read_first>
  <files>scripts/runnable-ref/uat-spec-integrity.test.ts, scripts/runnable-ref/uat-spec-integrity.ts, scripts/runnable-ref/uat-spec-integrity.js</files>
  <action>
    First establish WHERE the stack exhaustion originates, and record the answer in the summary: in
    the fixture construction that builds the tree, or in the module's recursive walk that reads it.
    Reproduce offline by building a tree whose total path length approaches the host's limit and by
    building one whose nesting depth approaches the recursion limit, and report which of the two
    reproduces.

    If the walk is the origin, give it an explicit depth bound derived by probing the host — create
    progressively deeper directories until the create refuses, and take the refusal depth as the
    platform's answer — and make the walk push a named refusal at that bound instead of recursing
    further, in the same style as the existing unreadable-directory boundary. If the fixture is the
    origin, bound the fixture's construction by the same derived depth. Either way, the case's own
    name already promises "as deep as this platform permits", so the depth is derived at run time
    and never a literal, and the derived depth is printed so a reader can see which number the host
    gave.

    For the two diagnostic-code expectations, replace the pinned code numbers with an assertion over
    the compiler's reported diagnostics that holds on any host: assert that the set of reported
    codes is non-empty and that each reported code belongs to the compiler's declared set for the
    construct under test, and print the codes actually seen. A platform that reports a different
    code for the same refusal is a measurement, not a failure, provided the refusal happened.

    Do not add a host-operating-system branch to any of this (D-16's rule for the watcher applies
    here too: a red on a platform is the measurement arriving).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts -t "as deep as this platform permits"</automated>
    <fails_when>non-zero exit, or the output contains "RangeError", or "No test files found" appears, or the summary reports 0 passed</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or the parity gate names scripts/runnable-ref/uat-spec-integrity.js as stale</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - The depth probe prints the derived depth it used; no depth literal appears in the assertion.
    - The case exits without a `RangeError` on this host, and the summary records which of the two
      origins reproduced offline.
    - The diagnostic-code assertions name the codes actually seen in their failure message rather
      than only the expected ones.
    - `grep -c 'process\.platform' scripts/runnable-ref/uat-spec-integrity.test.ts` does not increase
      relative to the pre-task count recorded in the summary.
    - The committed `.js` twin is rebuilt and committed in the same commit if the module changed.
  </acceptance_criteria>
  <done>The deepest-path probe answers with the host's own limit and reports refusals instead of throwing, on every platform.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem -> the spec scanner | Untrusted directory shapes (absent, unreadable, pathologically deep) cross into a recursive walk |
| CI runner config -> every gate test | A timeout bound decides whether a safety gate's test is allowed to complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Denial of service | recursive spec walk | medium | mitigate | Derive the host's depth limit at run time and push a named refusal at that bound instead of recursing until the stack exhausts (Task 3). |
| T-33-09 | Repudiation | timeout bounds | high | mitigate | No host-OS conditional on any bound; a slow test warns and completes, so a platform-specific slowness can never be silently converted into a green (Task 1, asserted by a grep count of 0). |
| T-33-10 | Tampering | the scratch-root premise | medium | mitigate | Tolerate ENOENT only; any other read error is re-thrown, and a planted stray file still reds the premise (Task 2 acceptance). |
| T-33-11 | Information disclosure | the scratch root's disclosure class | low | accept | The directory's `could-hide-evidence` classification is unchanged; this plan tolerates its absence and does not relax what the scanner does when it is present. |
| T-33-12 | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed. RESEARCH § Package Legitimacy Audit records zero installs for this phase; a new dependency would surface in the build gate. |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**'` — the full regression lane with the new bounds.
- `npm run build && npm run check:build-parity && npm run typecheck`.
- `rm -rf .temp && npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts`
  — the fresh-runner reproduction, green.
- `grep -c 'process\.platform' vitest.config.ts` — 0.
</verification>

<success_criteria>
- Both the absent-scratch-root class and the five-second-bound class are closed at their boundary,
  not at a CI setup step and not behind a platform conditional.
- The hook bound RESEARCH found missing from D-14 is set.
- The deepest-path probe reports rather than throws, with a derived depth.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `testTimeout: 180_000` in `vitest.config.ts` | new config key | choice above the 85568 ms measured floor |
| `hookTimeout: 120_000` in `vitest.config.ts` | new config key | the gap RESEARCH found in D-14 |
| `slowTestThreshold: 5_000` in `vitest.config.ts` | new config key | warn, never prevent (D-14) |
| the ENOENT-tolerant scratch-root premise | modified test behavior | absent and empty are one fact |
| the derived platform depth bound | new derivation | printed, never a literal |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-02-SUMMARY.md` when done
</output>

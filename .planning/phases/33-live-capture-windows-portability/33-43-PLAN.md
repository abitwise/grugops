---
phase: 33-live-capture-windows-portability
plan: 43
type: execute
wave: 32
depends_on: ["33-40", "33-41", "33-42"]
files_modified:
  - .planning/phases/33-live-capture-windows-portability/33-REVIEW-round3.md
  - .planning/WINDOWS.md
  - .planning/phases/33-live-capture-windows-portability/deferred-items.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements: [CAP-01, CAP-02, CAP-03]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`33-REVIEW.md` (the round-3 code review) is SNAPSHOTTED byte-identical as `33-REVIEW-round3.md` BEFORE any round-4 re-review can overwrite it, because this round's ledger rows cite its ids (CR-01, WR-01..03, IN-*) and a re-review skill overwrites `33-REVIEW.md` at that path — the snapshot is the first commit of this plan (must-fix item 6, the open hazard the memory names)."
    - "The human's decision-2 class WR-03 (the deny text names the wrong mechanism when a literal also matched) is a LEDGER row, not a code fix: appended through the tool citing `33-REVIEW-round3 WR-03`, owner named. The scope-decision-1 ledgered classes are ledger rows too, not code: `git send-pack`, the `gh api ... /pulls/N/merge` endpoint, the dashed `git-core/git-push` builtin binary, the WINDOWS.md row 259 `git -c alias.X=push X` form, and the out-of-table deploy verbs the verifier noted — each a row with its owner."
    - "Every round-4 finding is either FIXED by a named round-4 plan (the CR-01 nested class -> 33-35; the npm-prefix and xargs classes -> 33-36; row 260 win32 -> 33-37; WR-01 -> 33-38; WR-02 -> 33-39) disposed through the tool citing the plan/commit, or an accepted-open ledger row with its id and owner — never a silent drop; the two computed-at-run-time guard residuals (a tool from a shell variable; a body from a command substitution) are accepted-open rows with the human as owner."
    - "Every `deferred-items.md` entry this round touched carries either `status: resolved` with a `Resolved by:` citation naming plan, task, commit and — for a CAP-02 class — the CI run id from plan 33-40, or a dated `Round 4:` note quoting the evidence for leaving it open; the stale-plugin-rows item quotes plan 33-41's post-run listing verbatim (or records the no-go)."
    - "REQUIREMENTS.md, STATE.md and the ledgers say exactly what the round's artifacts support: CAP-02 as plan 33-40 wrote it; CAP-01/CAP-03 as the flip commit wrote them or, on hold, Pending with the round-4 evidence, the § 9 hold record, and the sentence that round 4 was the last under the cap and the phase closes by human override with GAP-D1 open (D-20) — the ROADMAP phase checkbox is NOT flipped on the hold branch. STATE.md edits are confined to the frontmatter and Current Position; the three same-line beats the wording guard scans are untouched; the guard and the longest-line check run after the write."
  artifacts:
    - .planning/phases/33-live-capture-windows-portability/33-REVIEW-round3.md
    - .planning/WINDOWS.md
    - .planning/phases/33-live-capture-windows-portability/deferred-items.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  key_links:
    - "33-REVIEW.md -> `cp` -> 33-REVIEW-round3.md (byte-identical, `cmp` exit 0) -> the ledger rows' citations resolve after any re-review"
    - "33-REVIEW-round3 finding ids + the round-4 summaries' hand-off sections -> `gsd-tools windows append|fixed` -> the ship gate sees each"
    - "33-40 Part 5 + 33-41's listing + 33-42's flip/hold -> `Resolved by:` / `Round 4:` lines in deferred-items.md"
    - "STATE.md write -> `node scripts/check-foundation-guards.js` -> the three beats intact"
  prohibitions:
    - "a WINDOWS.md row is never hand-edited"
    - "no finding is dropped without a row or a named fold"
    - "a deferred item is never marked resolved without the evidence its own owner line asked for"
    - "the ROADMAP phase checkbox is NOT flipped on the hold branch (D-20 / STATE premature-complete hazard)"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): this plan records the flip or the hold as plan 33-42 left it; it decides nothing about it."
    - "CAP-02 (edge row unresolved, flagged): this plan records plan 33-40's verdict; it decides nothing about it."
    - "CAP-03 (edge row unresolved, flagged): this plan records the round-4 capture's evidence as plan 33-41 left it; it decides nothing about it."
---

<objective>
Gap-closure round 4 of the phase's four-round cap — the LAST round. The round's closing ledger plan:
the round-3 review is snapshotted before any round-4 re-review can overwrite the file the ledger
cites; every round-4 finding is fixed-by-name or an accepted-open row; the human's decision-2 (WR-03)
and decision-1 (the ledgered classes) are ledger rows with owners; every deferred item is resolved
with a citation or honestly left open; and the three current-state ledgers say what the artifacts
support — including, on a hold at the cap, that the phase closes by human override with GAP-D1 open and
the ROADMAP checkbox is NOT flipped.

Gap mapping (must-fix item 6): "FIRST snapshot 33-REVIEW.md -> 33-REVIEW-round3.md (a re-review skill
overwrites 33-REVIEW.md and ledger rows cite its IDs); ledger rows for every ledgered class above +
WR-03; REQUIREMENTS/STATE agree with the outcome; if GAP-D1 still open at the cap, record D-20
closure-by-human-override path, do NOT mark ROADMAP complete." -> Task 1 (snapshot + rows), Task 2
(deferred items), Task 3 (requirements + state).

Decisions honored: D-11 (a red is recorded and — since there is no round 5 — the ledger is how it is
carried to the human-override closure), D-20 (a hold is recorded, not softened; at the cap it is the
phase's closure), D-33-R4-01 (the ledgered classes are ledger, not code), the WINDOWS.md tool rule.

Purpose: the round ends with nothing silently dropped, every citation resolvable after a re-review,
and every ledger truthful about a phase closing at its cap.
Output: the review snapshot, the ledger rows, the deferred-items resolutions, the requirement rows and
the state position.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-REVIEW.md
@.planning/phases/33-live-capture-windows-portability/33-34-PLAN.md
@.planning/phases/33-live-capture-windows-portability/deferred-items.md
@.planning/phases/33-live-capture-windows-portability/33-40-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-41-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-42-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot the round-3 review first; then every finding fixed-by-name or a row, WR-03 and the ledgered classes as rows — all through the tool</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` — every finding id (CR-01,
      WR-01, WR-02, WR-03, IN-01..IN-06) with its file and line; `33-REVIEW-round2.md` and
      `33-REVIEW-round1.md` beside it (the prior snapshots, the precedent).
    - The round-4 summaries' hand-off sections: 33-35 (the CR-01 nested class closed; the two
      computed-at-run-time residuals named), 33-36 (npm-prefix + xargs closed; the docblock rewritten),
      33-37 (row 260 win32), 33-38 (WR-01), 33-39 (WR-02).
    - `.planning/WINDOWS.md` header and rows 259-271; `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md`
      Part 4 § 4.4 — the exact `windows append` and `windows fixed` command shapes, and the `windows
      status` check afterwards.
    - `.planning/phases/33-live-capture-windows-portability/33-34-PLAN.md` Task 1 — how round 3's rows
      were appended and how the folds were named.
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-REVIEW-round3.md, .planning/WINDOWS.md</files>
  <action>
    FIRST COMMIT, before anything else in this plan: `cp` `33-REVIEW.md` to `33-REVIEW-round3.md` in the
    phase directory (byte-identical; `cmp` the two), commit it alone with a message naming the reason (a
    round-4 re-review overwrites `33-REVIEW.md`; the ledger cites round-3 ids).

    Then the rows, every one through `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows ...`, every
    command quoted in the summary:
    - Dispose the round-4 FIXED findings with `windows fixed` where a prior round-4 row exists, or
      append-then-fixed where the finding was surfaced this round: the CR-01 nested class (plan 33-35,
      commit); the npm-prefix and xargs classes (plan 33-36); row 260 win32 (plan 33-37 — but its
      ledger disposition `fixed` belongs to plan 33-40's green run, so here it stays as 33-40 left it,
      NOT re-touched); WR-01 (plan 33-38); WR-02 (plan 33-39). Name each fold with plan and task in the
      summary; no id from the review is absent from both the fold list and the ledger.
    - Append WR-03 as an accepted-open row (`--kind deviation`, its file and line,
      `33-REVIEW-round3 WR-03 accepted open (round 4, ledgered by decision): the deny text names the
      wrong mechanism when a literal pattern also matched; owner: a later round`) — the human's
      decision 2 is ledger, not code.
    - Append the scope-decision-1 ledgered classes, each `--kind unrun-verify` or `--kind deviation`
      with its owner: `git send-pack origin <ref>`; the `gh api ... /pulls/N/merge` merge endpoint; the
      dashed `git-core/git-push` builtin binary path; and the out-of-table deploy verbs the verifier
      noted (kubectl replace/create/patch/scale/set/edit, helm rollback/uninstall, terraform destroy,
      tofu apply, aws s3 cp). Row 259 (the alias form) already exists — leave it, and cross-reference it
      in the summary as part of this class. Owner on each: round 5+ / a later phase (this phase is at
      its cap).
    - Append the two computed-at-run-time GUARD residuals (a tool name assembled from a shell variable;
      a body produced by a command substitution) as `--kind unrun-verify` rows, owner: the human —
      these are the disclosed residual 33-35 named.
    Derive the count: `windows status` before and after; state `open_count` and `fixed_count` before,
    the number appended, the number fixed, and after — with the arithmetic — and confirm the table row
    count and the JSON appendix agree.
  </action>
  <verify>
    <automated>cmp .planning/phases/33-live-capture-windows-portability/33-REVIEW.md .planning/phases/33-live-capture-windows-portability/33-REVIEW-round3.md</automated>
    <fails_when>non-zero exit — the snapshot is not byte-identical to the round-3 review (or one file is missing)</fails_when>
    <automated>M=$(git log --format=%s -1 -- .planning/phases/33-live-capture-windows-portability/33-REVIEW-round3.md) &amp;&amp; test -n "$M" &amp;&amp; printf '%s\n' "$M" | grep -a -c 'snapshot'</automated>
    <fails_when>a count of 0 — the snapshot was not committed on its own with a message naming it</fails_when>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status</automated>
    <fails_when>non-zero exit, or the counters disagree with the table's row count</fails_when>
    <automated>grep -a -c '33-REVIEW-round3 WR-03' .planning/WINDOWS.md</automated>
    <fails_when>a count below 2 — the WR-03 accepted-open row is missing from the table or the JSON appendix</fails_when>
  </verify>
  <acceptance_criteria>
    - `33-REVIEW-round3.md` exists, byte-identical to `33-REVIEW.md`, committed first and alone.
    - The round-4 FIXED findings are disposed through the tool citing plan and commit; WR-03, the ledgered classes (send-pack, gh api merge, dashed git-push binary, out-of-table deploy verbs) and the two guard residuals are accepted-open rows with owners; row 259 is cross-referenced; every command quoted; the counter arithmetic stated and reconciled.
    - No id from the round-3 review is absent from both the fold list and the ledger; no round-4 finding is dropped in silence.
  </acceptance_criteria>
  <done>Nothing from the review, the diagnosis, or this round's own reading of the tree is dropped in silence; every citation survives a re-review; the ledgered classes have owners.</done>
</task>

<task type="auto">
  <name>Task 2: Deferred items resolved with citations, or honestly left open with the round-4 evidence</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/deferred-items.md` — the `status: open`
      entries (the row-236 class; the stale `0.1.0` plugin rows; GAP-D1 HELD; the row-259 alias; the
      two-sites-bare-server-name; and any others carrying a `Round 3:` note) and their `Owner:` lines.
    - `.planning/phases/33-live-capture-windows-portability/33-40-SUMMARY.md` and 33-CI-MEASUREMENT.md
      Part 5 — the CI verdict and, on MET, the `CLOSED on run <id>` disposition; on NOT MET, the
      re-derived inventory.
    - `.planning/phases/33-live-capture-windows-portability/33-41-SUMMARY.md` — the post-run `claude
      plugin list` grugops rows quoted verbatim (or the no-go record); `33-42-SUMMARY.md` — flip or hold
      (§ 9).
    - `.planning/phases/33-live-capture-windows-portability/33-34-PLAN.md` Task 2 — the
      `status: resolved` / `Resolved by:` and `Round N:` line shapes.
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/deferred-items.md</files>
  <action>
    For each open entry: the row-236 / row-260 CAP-02 class -> `status: resolved` with a `Resolved by:`
    line naming the closing plan (33-37 for row 260), its commit, and the run id whose windows leg read
    `success` (plan 33-40) — ONLY if plan 33-40 read MET; otherwise a dated `Round 4:` line quoting the
    re-derived inventory and stating that round 4 was the last under the cap. The stale-plugin-rows entry
    -> quote plan 33-41's post-run listing verbatim; resolve it if the two `0.1.0` rows are gone or the
    human removed them (say which), else leave open with the quotation; if no run happened (`no-go`), a
    dated `Round 4:` line says so. The GAP-D1 HELD entry -> resolved by plan 33-42's flip commit (sha
    quoted) with the two amendments named as paid, or a dated `Round 4:` line pointing at the manifest's
    § 9 with the outcome word, the CI verdict and the cap-reached human-override closure. The row-259
    alias and the bare-server-name entries -> a dated `Round 4:` note if untouched, or `resolved` if a
    round-4 plan closed them, with the citation.

    Derive the count: the number of `status: open` lines before and after, and the number of `Resolved
    by:` lines added, stated in the summary with the equality.
  </action>
  <verify>
    <automated>grep -a -B1 -A1 'status: resolved' .planning/phases/33-live-capture-windows-portability/deferred-items.md | grep -a -c 'Resolved by:'</automated>
    <fails_when>a count lower than the number of `status: resolved` lines in the file — a resolved line without a citation</fails_when>
    <automated>grep -a -c 'Round 4' .planning/phases/33-live-capture-windows-portability/deferred-items.md</automated>
    <fails_when>a count of 0 while any entry is still `status: open` — an open entry carries no round-4 note</fails_when>
    <automated>for s in $(grep -a -oE 'commit `[0-9a-f]{7,40}`' .planning/phases/33-live-capture-windows-portability/deferred-items.md | grep -a -oE '[0-9a-f]{7,40}' | sort -u); do git cat-file -t "$s" >/dev/null 2>&amp;1 || { echo "unresolvable $s"; exit 1; }; done</automated>
    <fails_when>non-zero exit, or an `unresolvable` line — a cited commit sha does not exist in this repository</fails_when>
  </verify>
  <acceptance_criteria>
    - Each open entry is either `status: resolved` with a `Resolved by:` citation carrying plan, task, commit and (for the CI class) run id, or `open` with a dated `Round 4:` note quoting the evidence and, on hold, the cap-reached closure.
    - The summary states the before/after open counts and the equality; every cited commit resolves and the run id matches Part 5.
  </acceptance_criteria>
  <done>The deferred list is a truthful record of what this round closed and what the phase carries past its cap.</done>
</task>

<task type="auto">
  <name>Task 3: The requirement rows and the project position agree with the artifacts; the ROADMAP checkbox stays as-is on hold</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` rows CAP-01, CAP-02, CAP-03 (the checkboxes and the coverage table) as
      plan 33-40 and the flip commit (33-42) left them.
    - `.planning/STATE.md` — the frontmatter and `## Current Position` ONLY; the three same-line beats
      the `oracleWr05Wording` guard scans (`dropped`+`Phase 8`, `guard_wr05`+`Phase 10`, `re-verified
      GREEN`+`Phase 11`) and the escape-doubling hazard (check the longest line after every write).
    - `.planning/phases/33-live-capture-windows-portability/33-42-SUMMARY.md` — flip or hold;
      `33-41-SUMMARY.md` — the outcome word, costs, durations, both provenance states; `33-40-SUMMARY.md`
      — the CI verdict.
    - `.planning/ROADMAP.md` the Phase 33 checkbox line — NOT flipped on the hold branch (the
      premature-complete hazard the project memory names).
    - The decisions this round recorded (D-33-R4-01, D-33-R4-03) — confirm they are in STATE.md's
      decision log (33-35 added them; this task only confirms and adds the round summary line).
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    REQUIREMENTS.md: CAP-02 stays as plan 33-40 wrote it (do not touch). On the flip branch, CAP-01 and
    CAP-03 were flipped inside the flip commit (F44, F45) — verify by reading and do not touch. On the
    hold branch, update the two coverage-table rows' `Pending` text to name the round-4 evidence: the
    capture date, the outcome word, the parity section's named diffs (or the `no-go`), both provenance
    states, the hold record (`33-FLIP-MANIFEST.md` § 9), that round 4 was the last under the cap, and
    that the phase closes by human override with GAP-D1 open (D-20). Capture-backed and run-backed
    citations are never mixed.

    STATE.md: edit ONLY the frontmatter and `## Current Position` — the phase line becomes
    `Phase: 33 (...) — gap-closure round 4 of 4 (LAST) EXECUTED <date>, awaiting verification` and the
    plan line names 33-35..33-43, the CI verdict (run id, both conclusions), the capture outcome word (or
    no-go), the flip/hold, and — on hold — that round 4 was the last under the cap and the phase closes
    by human override with GAP-D1 open. Do NOT flip the ROADMAP phase checkbox and do NOT set the phase
    status to complete on the hold branch. Keep every line short (no path-heavy prose); after the write
    run the foundation guards and measure the longest line.
  </action>
  <verify>
    <automated>node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit, or any FAIL line naming the state wording oracle</fails_when>
    <automated>awk '{ if (length($0) > m) m = length($0) } END { print m }' .planning/STATE.md</automated>
    <fails_when>a value above 4000</fails_when>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs query state.load</automated>
    <fails_when>non-zero exit, or a parse error reported for the state document</fails_when>
    <automated>S=$(git log -1 --format=%H -- .planning/STATE.md) &amp;&amp; test -n "$S" &amp;&amp; git show --format= "$S" -- .planning/STATE.md > "${TMPDIR:-/tmp}/state-33-43.diff" &amp;&amp; test -s "${TMPDIR:-/tmp}/state-33-43.diff" &amp;&amp; test "$(grep -a -cE '^[-+].*(guard_wr05.*Phase 10|re-verified GREEN.*Phase 11|dropped.*Phase 8)' "${TMPDIR:-/tmp}/state-33-43.diff" | tr -d ' ')" = "0"</automated>
    <fails_when>non-zero exit — the last commit touching STATE.md cannot be resolved, produced an empty diff, or changed a guarded beat line</fails_when>
    <automated>grep -a -c 'round 4 of 4' .planning/STATE.md</automated>
    <fails_when>a count of 0 — the position does not name the round</fails_when>
  </verify>
  <acceptance_criteria>
    - CAP-02's rows are byte-unchanged from plan 33-40's write; CAP-01/CAP-03 rows are as the flip commit wrote them or carry the round-4 hold text with the evidence and the cap-reached human-override closure.
    - STATE.md's diff touches only the frontmatter and Current Position; the guards exit 0; the longest line is below the ceiling; the loader parses it; the two round-4 decisions are present in the log.
    - On the hold branch, the ROADMAP phase checkbox is unchanged and the phase is not marked complete; the summary quotes the three requirement rows as they read at the round's end.
  </acceptance_criteria>
  <done>The project's current-state ledgers say exactly what round 4 produced, and — on hold — that the phase closes at its cap by human override with GAP-D1 open, without a premature complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a round-4 re-review -> `33-REVIEW.md` | An overwrite before the snapshot dangles every ledger citation of a round-3 id |
| a review/scope finding -> the ledger | A finding without a row is a silent drop the ship gate never sees |
| a deferred item -> a resolved line | A resolved line without evidence is a fabricated closure |
| the hold outcome -> the ROADMAP checkbox | Flipping the phase complete on a hold is the premature-complete hazard the memory names |
| the state document -> the wording guard | A touched beat or a pathological line turns a sub-second guard into minutes or a false red |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-192 | Repudiation | dangling round-3 review citations after a re-review | high | mitigate | Task 1: the snapshot is the first, separate commit; `cmp` asserted. |
| T-33-193 | Repudiation | silently dropped findings, the ledgered classes, or the guard residuals | high | mitigate | Task 1: one row per accepted-open id and per ledgered class, folds named, counter arithmetic stated. |
| T-33-194 | Repudiation | a resolved deferred item without evidence | high | mitigate | Task 2: every `Resolved by:` names plan, task, commit and run id; cited shas resolved with `git cat-file`. |
| T-33-195 | Tampering | the ROADMAP flipped complete on a hold | high | mitigate | Task 3: the checkbox is unchanged on the hold branch; the phase is not marked complete (D-20). |
| T-33-196 | Denial of service | STATE.md guard beats / longest line | medium | mitigate | Task 3: frontmatter + Current Position only; guard and length check after the write; the diff grep over the beats. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; ledgers only. |
</threat_model>

<verification>
- `cmp 33-REVIEW.md 33-REVIEW-round3.md` — exit 0; the snapshot commit precedes every other commit of this plan.
- `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status` — counters agree; the arithmetic across Task 1 stated.
- `node scripts/check-foundation-guards.js` and the STATE.md longest-line check — after every STATE.md write.
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the tree is still green at the round's close.
</verification>

<success_criteria>
- The round-3 review is snapshotted before any re-review; every round-4 finding is fixed-by-name or a row; WR-03 and the ledgered classes are rows with owners; the two guard residuals are accepted-open rows.
- Every deferred item is resolved with a citation or left open with the round-4 evidence; the requirement rows and the state position are truthful, and — on hold at the cap — say the phase closes by human override with GAP-D1 open, with the ROADMAP checkbox unflipped.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `33-REVIEW-round3.md` | snapshot | byte-identical to the round-3 `33-REVIEW.md`, committed first |
| WINDOWS.md round-4 FIXED dispositions | ledger changes via tool | plan, task, commit per reason |
| WINDOWS.md rows for WR-03, the ledgered classes, the two guard residuals | ledger rows via tool | accepted open with owners; none dropped |
| `Resolved by:` / `Round 4:` lines in deferred-items.md | record changes | plan, task, commit, run id |
| CAP-01/CAP-03 hold text (hold branch) | ledger change | round-4 evidence; cap-reached human-override closure |
| STATE.md Current Position | state change | round 4 of 4 (last) executed; ROADMAP checkbox unflipped on hold |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-43-SUMMARY.md` when done
</output>

---
phase: 33-live-capture-windows-portability
plan: 39
type: execute
wave: 28
depends_on: ["33-38"]
files_modified:
  - scripts/capture-live.ts
  - scripts/capture-live.js
  - scripts/capture-live.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The dual-path parity verdict (`compareLivePaths` -> `deriveOutcome`) is NOT decided by an incidental read: a `Bash` block that merely names the context root through a read-shaped command (an `ls`, a `cat`, a description leaf) and a writing-tool block whose content merely mentions the root without a write-shaped target no longer feed the parity comparison. Parity counts only write-shaped blocks, OR the broad count is reported without entering `compareLivePaths`."
    - "The axis remains FAIL-SAFE: the change can only make a false `fail` LESS reachable, never make a false `pass` reachable. A route that is genuinely a write to disk (a Write/Edit under the root, a Bash command with a write-shaped token — `>`, `>>`, `tee`, `node`, `cp`, `mv`, `mkdir`, `touch`, `rm`, `sed -i` — naming the root, or a writing-tool block whose written content names the root AND whose path is a script) still counts, so a real divergence still reads `fail`."
    - "The reported observation is renamed to what it counts. If the broad count is kept as a reported row, it is labelled as reads-and-writes-naming-the-root and is explicitly NOT an input to `compareLivePaths`; the parity input is the write-shaped count only."
    - "The held round-1 capture's six named divergence sentences (33-12 Test A / 33-30 P10) are re-derived under the corrected route counting and stated: whichever of them rested on an incidental read is named, and the ones that rest on real write routes are unchanged — the D-07 predicate is not softened (D-20), only the incidental-read noise is removed."
  artifacts:
    - scripts/capture-live.ts
    - scripts/capture-live.test.ts
  key_links:
    - "noteRoute -> the write-shaped classification -> compareLivePaths (parity input) -> deriveOutcome"
    - "the broad reads-and-writes count -> a reported row only, never compareLivePaths"
    - "the held round-1 capture -> the re-derived route counts -> the six divergence sentences"
  prohibitions:
    - "no change that could make a false `pass` reachable — the axis stays fail-safe (D-20)"
    - "the D-07 predicate is not softened; only incidental-read noise is removed from the parity input"
    - "the parity input never counts a read-only command or a mere content mention as a write"
    - "no gate or test result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01: this is the instrument fix that must land before the one paid run, so a correct kit cannot read `fail` from incidental reads."
    - "CAP-02: this module must be green on both CI legs; plan 33-40 measures it."
    - "CAP-03: the same instrument produces the CAP-03 observation; this plan only changes the parity-input classification."
---

<objective>
Gap-closure round 4 (the last round under the cap). This is WR-02: `capture-live`'s `noteRoute`
counts every `Bash` block whose string inputs name `.grugops/context` (including an `ls`, a `cat`,
the `description` leaf) and every `Write`/`Edit` whose content merely mentions the path as an
"unclassified write". `compareLivePaths` turns any A/B inequality into a parity diff and
`deriveOutcome` turns that into `fail` — so `pass` requires two nondeterministic live sessions to
issue the same number of incidental reads of the context root, and a correct kit can read `fail` on
the one paid run for a reason that is not a kit defect.

This is round-4 must-fix set item 4, and it must land before the paid run (plan 33-40 / 33-41). The
fix keeps the axis fail-safe: it removes a false-`fail` source, never adds a false-`pass` path.

Gap mapping (33-VERIFICATION.md CAP-01 gap `artifacts[capture-live.ts]` and `missing[0]`):
- "Fix WR-02 before the paid run (count only write-shaped blocks, or report the broad count without
  feeding parity), so a round-4 `fail` cannot come from incidental reads" -> Tasks 1-2.

Decisions honored: D-07 (dual-path parity as defined; not softened), D-20 (a real divergence is a
kit finding; nothing is softened to reach parity), D-11 (fix offline with proof).

Purpose: the one paid run's parity verdict rests only on routes that are genuinely a write to disk,
so a correct kit cannot fail on incidental reads while a real divergence still fails.
Output: the write-shaped parity classification, the renamed reported row, and the re-derived held-
capture divergences.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md
@.planning/phases/33-live-capture-windows-portability/33-REVIEW.md

Read-list (read these):
- `scripts/capture-live.ts` `noteRoute` (the three counters and the `inputStrings` walk; the `Bash`
  arm that increments on any leaf naming the root; the writing-tool arm), `NoteRoute`,
  `compareLivePaths` (the three route diffs it emits), `deriveOutcome` (parity diff -> `fail`),
  `WRITING_TOOLS`, and the Run-table row that renders the unclassified count.
- `scripts/capture-live.test.ts` the `WR-01` describe (Tests P8, P9, P10): P8 exercises the route
  classification, P9 the parity comparison, P10 the held-capture six sentences.
- `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` WR-02 (the reproduction: an
  `ls`, a `cat`, a content mention each count as an unclassified write) and its fix prescription.
- `.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md` gap 2 (CAP-01) and the
  anti-pattern row for capture-live.ts.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: noteRoute classifies write-shaped blocks only for the parity input; incidental reads leave parity untouched</name>
  <read_first>
    - `scripts/capture-live.ts` `noteRoute`, `inputStrings`, `NoteRoute`, `WRITING_TOOLS`, and the
      `Bash`/writing-tool arms exactly as written.
    - `scripts/capture-live.test.ts` Test P8 (the current expectations for a Bash naming the root, a
      MultiEdit, a NotebookEdit, a node-mediated write, an incidental read).
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` WR-02 fix (the write-shaped
      token list; ignore `description` and non-command leaves; a writing-tool block counts only when
      its content names the root AND its path is a script).
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - A `Bash` block whose command text names the root AND carries a write-shaped token (`>`, `>>`,
      `tee`, `node`, `cp`, `mv`, `mkdir`, `touch`, `rm`, `sed -i`) counts toward the parity input; a
      `Bash` block that names the root only through a read command (`ls`, `cat`) or only through its
      `description` leaf does NOT.
    - A writing-tool block whose path field sits under the root is a direct write (unchanged); a
      writing-tool block whose CONTENT names the root counts toward the parity input only when its
      path is a script (`.mjs`, `.js`, `.sh`, `.py`) — a mere mention in prose does not.
    - The parity comparison (`compareLivePaths`) reads only the write-shaped counts; if the broad
      reads-and-writes count is kept, it is a reported row that is not compared.
  </behavior>
  <action>
    RED first: rewrite Test P8's incidental cases to assert the corrected behaviour — an `ls`/`cat`
    naming the root, and a Write whose prose content merely mentions the root, do NOT raise the
    parity-input count; the write-shaped Bash and the script-content write DO. Add a case for a `Bash`
    `description` leaf naming the root asserting it does not enter the parity input. Run the suite and
    confirm these FAIL against the committed module (which counts them all); quote the RED run.

    GREEN: in `noteRoute`, split the classification so the parity input counts only write-shaped
    blocks per the behaviour above (derive the write-shaped token set as a named constant; keep
    `WRITING_TOOLS` and the direct-path arm unchanged). Either (a) stop counting incidental reads
    entirely, or (b) keep a separate reported `contextRootMentions` count that `compareLivePaths` does
    NOT read — the executor picks, but the parity input must be write-shaped only. Rename the rendered
    row to what it counts. Update `compareLivePaths` to compare the write-shaped counts. Rebuild the
    committed `scripts/capture-live.js`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or check:build-parity prints other than ALL CHECKS PASSED</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or a failed test file, or "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - The parity input counts only write-shaped blocks; an `ls`/`cat`/`description` naming the root and a prose content mention do not raise it, proven RED-first.
    - A real write route (write-shaped Bash naming the root, a script-content write, a direct-path write) still counts.
    - `compareLivePaths` reads only the write-shaped counts; the rendered row is renamed to what it counts.
    - `check:build-parity` green.
  </acceptance_criteria>
  <done>Parity is decided by routes to disk, not by incidental reads, and the axis stays fail-safe.</done>
</task>

<task type="auto">
  <name>Task 2: fail-safe proof and the held-capture re-derivation</name>
  <read_first>
    - `scripts/capture-live.ts` `compareLivePaths`, `deriveOutcome` (confirm `pass` is still
      unreachable while any real divergence, provenance-not-MET, or failure holds).
    - `scripts/capture-live.test.ts` Test P9 (parity sentence), Test P10 (the held-capture six
      sentences and the independent hand-count).
    - `.planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-A.jsonl` and
      `33-CAPTURE-B.jsonl` (read the tool-use blocks only; do not re-run any capture).
  </read_first>
  <files>scripts/capture-live.test.ts</files>
  <action>
    Add a fail-safe assertion: for a projection pair that differs only in the incidental
    reads-and-writes count (equal in every write-shaped route, verdict marker and role counts),
    `compareLivePaths` returns no diff and `deriveOutcome` is not `fail` on that account; and for a
    pair that differs in a write-shaped route, `deriveOutcome` is still `fail`. Prove the direction: a
    change that removed a real write route from the parity input would let a divergent pair read
    `pass` — assert that the write-shaped route IS in the parity input by constructing exactly such a
    divergent pair and requiring `fail`.

    Re-derive Test P10 over the held round-1 capture under the corrected counting: state which of the
    six divergence sentences rested on the incidental-read count (if any) and which rest on real write
    routes, and update P10's expectations accordingly with the independent hand-count recomputed for
    the write-shaped definition. The D-07 predicate is unchanged; only the incidental-read noise is
    removed (D-20). Record the re-derived sentences in the summary.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or a failed test file, or "0 passed"</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the summary reports any failed test file, or "0 passed"</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or a tracked file named as carrying a forbidden byte</fails_when>
  </verify>
  <acceptance_criteria>
    - A projection pair differing only in incidental reads yields no parity diff and is not `fail`; a pair differing in a write-shaped route is still `fail` (fail-safe direction proven both ways).
    - Test P10 re-derives the held capture's divergence sentences under the write-shaped counting; the summary names which rested on incidental reads and which on real write routes.
    - The full excluded-lane suite is green; `check:nul-bytes` clean.
  </acceptance_criteria>
  <done>The parity axis is proven fail-safe: incidental reads cannot force `fail`, and a real write divergence still does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a model-chosen incidental read -> the parity verdict | A read of the context root the model happens to issue must not decide `pass`/`fail` on the one paid run |
| the parity input classification -> `deriveOutcome` | Removing a real write route from the input would make a false `pass` reachable — the fail-safe direction must be proven |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-175 | Repudiation | a correct kit reading `fail` from incidental reads | high | mitigate | Parity counts only write-shaped blocks; incidental reads leave it untouched (Task 1). |
| T-33-176 | Tampering | a false `pass` from dropping a real write route | critical | mitigate | Task 2 proves the fail-safe direction both ways: a write-shaped divergence still `fail`. |
| T-33-177 | Tampering | softening the D-07 predicate | high | mitigate | The predicate is unchanged; only incidental-read noise is removed (D-20); held-capture sentences re-derived. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; dev deps from the committed lockfile only. |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts` — green.
- `npx vitest run --exclude '**/scripts/e2e/**'` — the full excluded-lane suite green.
- `npm run build && npm run check:build-parity` — twin matches source.
</verification>

<success_criteria>
- The parity input is write-shaped only; incidental reads cannot force `fail`; a real write divergence still fails; the held-capture sentences are re-derived; RED-first proven; the axis is fail-safe.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| the write-shaped parity classification | modified noteRoute / compareLivePaths (capture-live.ts + .js) | parity input counts write-shaped blocks only |
| the renamed reported row | modified Run-table row (capture-live.ts + .js) | reads-and-writes count, not fed to parity |
| the fail-safe + held-capture tests | modified/new tests (capture-live.test.ts) | both directions proven; P10 re-derived |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-39-SUMMARY.md` when done
</output>

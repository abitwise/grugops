---
phase: 33-live-capture-windows-portability
plan: 35
type: execute
wave: 24
depends_on: ["33-34"]
files_modified:
  - scripts/checkpoints.ts
  - scripts/checkpoints.js
  - scripts/checkpoints.test.ts
  - hooks/guard.test.ts
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements: [CAP-01, CAP-02]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every spelling in 33-VERIFICATION.md gap 4 `reason` (the ten opaque-arm shapes) and in the '33-R4 nested corpus' this plan's Task 1 files DENIES through `hooks/hook-entry.js guard.js` and `hooks/guard.js` on the COMMITTED `.js`, with no `GRUGOPS_*` and no `CLAUDE_PROJECT_DIR` in the child environment. Each was an ALLOW on the round-3 committed build, and that RED is recorded before the fix."
    - "The fix lives in the ONE projection both arms of the command model ask, `governedToolsNamedBy` (D-33-R4-03): the shell-resolved text of every name piece is projected again as text a nested shell would run, recursively, bounded by the module's existing projection-depth limit and fail-closed beyond it, with each expansion read BOTH as empty and as a word boundary. No second nested-command queue is added to the opaque branch."
    - "The one authority answers for a quoted nested command directly: `governedToolsNamedBy` of a single-quoted body that spells a spliced governed tool followed by a governed verb contains that tool. So the readable arm and the fail-closed arm are closed by the same edit — including the corpus rows that carry no opaque word at all."
    - "The derived, count-pinned sweep in `scripts/checkpoints.test.ts` gains the nested families, each generated from `COMMAND_CHECKPOINT_RULES` x the existing splice generator x the governed verbs, with its cardinality asserted against that derivation. A derived per-tool subset runs through BOTH entry points in `hooks/guard.test.ts` with its count pinned too."
    - "The union can only add denials: over the round-1 held capture's Bash tool inputs and the over-denial floor list, no command moves from deny to allow, and the four nested controls (an echo under `bash -c` with a trailing expansion, an echo through a here-string, a commit message mentioning a governed verb, an echo of a variable) still ALLOW."
    - "Non-circularity is proven: a scratch build with the nested re-projection removed turns every new family red; nesting deeper than the bound denies without hanging; and the two computed-at-run-time residual rows the docblock names stay ALLOW (their ledger rows are plan 33-43's)."
  artifacts:
    - scripts/checkpoints.ts
    - scripts/checkpoints.js
    - scripts/checkpoints.test.ts
    - hooks/guard.test.ts
    - hooks/hook-entry.ts
    - hooks/hook-entry.js
  key_links:
    - "hooks/hooks.json PreToolUse Bash -> hooks/hook-entry.js guard.js -> hooks/guard.js -> scripts/checkpoints.js matchCommandCheckpoints -> governedToolsNamedBy (both arms)"
    - "scripts/checkpoints.js rebuild -> `npm run generate:hook-manifest` -> hooks/hook-entry.ts decider hash -> `npm run freshness:hook-manifest`"
    - "CR01_SPLICES x COMMAND_CHECKPOINT_RULES x nested family generators -> the pinned sweep count"
  prohibitions:
    - "no second grammar and no second nested-command queue beside the readable arm's; the fix is in the one projection"
    - "no hand-typed tool list, verb list or case count in any new assertion; every set is derived and every count is compared to its derivation"
    - "`hooks/guard.ts` is not edited; its frozen blob stays byte-identical"
    - "no decision summary written to STATE.md carries a backslash character (the state writer doubles backslashes on every write)"
    - "no gate or test result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01: the guard is the D-04 surface the capture observes (parity row 6, SAFE-02); this plan changes what it denies, never what it allows."
    - "CAP-02: this plan's modules must be green on both CI legs; plan 33-40's pushed run measures them."
    - "CAP-03: untouched by this plan."
    - "assumption_delta = promote: the name projection is PROMOTED to a recursive authority — a word's identity includes what a nested shell would make of its resolved text. A second nested queue added alongside it in one arm is rejected."
---

<objective>
Gap-closure round 4 of the phase's four-round cap. This is the LAST round under the cap. This plan
is the round's TRACER, because the 33-VERIFICATION.md safety truth is the gap that blocks everything
else: the round-3 review's CR-01, recorded as fixed at `7d0aea72`, is not closed. The command
model's fail-closed arm asks `governedToolsNamedBy` of the raw text of an opaque segment. That
projection strips ONE quoting layer, and a governed command quoted for a nested shell keeps its
splice one layer down, so no governed name is seen and the segment is allowed with zero keys.

The verifier prescribed re-tokenizing multi-word values in the opaque arm. The planner measured that
prescription against a scratch prototype on 2026-09-23 and found it insufficient: it closes the ten
rows in 33-VERIFICATION.md gap 4 `reason` but leaves open, all executable, an ANSI-C-quoted body, the
expansion-adjacent bodies, the double-quote escape level, eval word concatenation, and the positional-
parameter rows that carry no splice at all. Several of those carry no opaque word, so the READABLE
arm has the identical hole. The class is closed where both arms meet: in the one projection
(D-33-R4-03). This is scope item (a) of the human's round-4 decision D-33-R4-01, recorded 2026-09-23.

Gap mapping (33-VERIFICATION.md gap 4 `missing`):
- "project every quoted multi-word word (and a here-string body) as a nested command through the same ... path the readable arm uses — or deny on a governed name found in ANY nested projection" -> Task 1 (the tracer) and Task 2.
- "Extend the derived sweep with shapes `bash -c '<splice> <verb>' $X`, `eval '<splice> <verb>' $(true)`, `bash <<< '<splice> <verb>'`, each through hooks/hook-entry.js guard.js" -> Task 2.
- "Rewrite the residual docblock to state what the tests show" -> plan 33-36 Task 3, after the npm and xargs rules land, so the docblock is written once over the final model.

Decisions honored: D-04 (the prod-deploy deny is the hook-channel surface the capture observes; this
plan only adds denials to it), D-33-R4-01 (the human's scope), D-33-R4-03 (the class definition,
recorded by Task 1 before any code).

Purpose: the prod-deploy and protected-branch guard cannot be defeated by quoting a governed command
into a nested shell, at any nesting depth the bound admits, on either arm.
Output: the recursive projection, RED-first tests at the model and entry-point levels, a rebuilt twin
and a regenerated hook manifest.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md
@.planning/phases/33-live-capture-windows-portability/33-REVIEW.md

Read-list (explicit; read these, do not inline them into any prompt):
- `scripts/checkpoints.ts` design block above `governedToolsNamedBy` (the "WHY THIS REPLACED
  normalizeToolWord" section), `lexNames` / `basenameVariants` / `namesOfVariant` /
  `governedToolsNamedBy` themselves, `failClosedCheckpoints` and its residual list, and
  `matchCommandCheckpoints` (the opaque branch, the readable arm's nested re-tokenization of a
  whitespace-bearing value, and the per-word projection call).
- `scripts/checkpoints.test.ts` the `33-R3 CR-01` describe block: `CR01_SPLICES`, the derived sweep,
  the over-denial floor, and the one-authority case.
- `hooks/guard.test.ts` the `33-R3 CR-01` describe block: `bare`, `reviewPayload`, `runAll`, and the
  600 000 ms explicit-bound precedent on the entry-point sweep.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — one nested-quoted governed push, denied end to end through the shipped wrapper</name>
  <read_first>
    - The Read-list in `<context>`, all of it, before any edit.
    - `.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md` gap 4 (`truth`,
      `reason`, `artifacts`, the three `missing` items) and the "Adversarial guard probe" section
      (the held-DENY list and the live-bypass table).
    - `.planning/phases/33-live-capture-windows-portability/33-REVIEW.md` CR-01 and its "Fix status"
      paragraph.
    - `.planning/STATE.md` `### Decisions` (the D-33-R3-01 / D-33-R3-02 entries for the recorded
      form) and the three same-line beats the `oracleWr05Wording` guard scans (never touched).
  </read_first>
  <files>scripts/checkpoints.ts, scripts/checkpoints.js, scripts/checkpoints.test.ts, hooks/guard.test.ts, hooks/hook-entry.ts, hooks/hook-entry.js, .planning/STATE.md</files>
  <behavior>
    - Tracer path: the round-4 nested-quoted governed-push shape (the first `bash -c '<spliced git> push origin main' $X` row of 33-VERIFICATION.md gap 4 `reason`), driven through `hooks/hook-entry.js guard.js` with a scrubbed environment, yields a PreToolUse decision whose `permissionDecision` is `deny`. It was an empty allow on the round-3 committed build.
    - Controls: the two gap-4 `Controls:` rows (a spliced push with no opaque word, and an unspliced push with a trailing `$X`) still deny; an `echo hi` under `bash -c` with a trailing expansion still allows.
    - The one authority answers directly: `governedToolsNamedBy` of the single-quoted body that spells the spliced git plus the governed verb contains `git`; of a quoted `echo hi` body it names nothing.
  </behavior>
  <action>
    FIRST, before any code, record two decisions in STATE.md with the state tool's add-decision verb,
    passing each summary inline (the tool refuses a scratchpad summary file). Keep each summary a
    single short line, ASCII, with NO backslash character (the state writer doubles backslashes on
    every write — describe the spellings in words or cite the corpus row ids, never paste a spliced
    token):
    - D-33-R4-01 (human, 2026-09-23): the round-4 guard scope is "cheap two plus ledger". Code closes
      (a) the CR-01 opaque-quoted-nested class every row the verifier listed, (b) npm unique-prefix
      abbreviations of publish derived from the verb, and (c) a governed tool under xargs whose verb is
      not on the command line. The ledger — not code — takes git send-pack, the gh api merge endpoint,
      the dashed git-core push binary, the WINDOWS.md row 259 alias form, and the out-of-table deploy
      verbs.
    - D-33-R4-03 (planner, from the 2026-09-23 measurement): the CR-01 class is closed in the one
      projection `governedToolsNamedBy`, for both arms, by re-projecting each name piece's shell-
      resolved text as nested shell text. The verifier's opaque-arm-only re-tokenization was measured
      insufficient (this plan's 33-R4 nested corpus records which rows survive it).
    Then run the foundation-guards check and measure STATE.md's longest line (the `awk` command in the
    verify block); both must pass before proceeding.

    Then write the 33-R4 nested corpus as a committed fixture the tests read — a small module beside
    the tests holding the labelled command strings this round must deny (the ten from 33-VERIFICATION.md
    gap 4 `reason`, plus the beyond-the-list rows the planner measured) and the labelled controls that
    must stay allow. Deriving the corpus from a fixture, not from literals sprinkled across cases, is
    what lets the count be asserted.

    RED. In `hooks/guard.test.ts`, lift the three helpers of the existing `33-R3 CR-01` describe
    (`bare`, `reviewPayload`, `runAll`) to a shared scope with their bodies unchanged so both CR-01
    describes use one spawn harness, and add a `33-R4 CR-01 nested` describe with one case per entry
    point (`hooks/guard.js`; `hooks/hook-entry.js guard.js`) driving the tracer shape (deny), the two
    controls (deny), and the allow control. In `scripts/checkpoints.test.ts`, add a `33-R4 CR-01
    nested` describe asserting `governedToolsNamedBy` of the tracer's quoted body contains the tool and
    of the allow control's body names nothing, and that `matchCommandCheckpoints` of the tracer shape
    is non-empty. Build the committed `.js` and confirm every new assertion FAILS against it, and quote
    the failing run.

    GREEN. Extend `governedToolsNamedBy` per D-33-R4-03: after `lexNames` yields the token pieces,
    reconstruct each piece's shell-resolved literal text (reading each expansion token BOTH as empty
    and as a word boundary, so a splice around an expansion and a splice at a word edge are both seen)
    and, when that text differs from the input and carries shell-significant punctuation, feed it back
    through the same projection, accumulating names; recurse, bounded by the module's existing
    projection-depth constant, and return the full governed set fail-closed when the bound is exceeded.
    Do not touch `hooks/guard.ts`. Do not add a nested-command queue to `matchCommandCheckpoints`'s
    opaque branch — the branch already asks `governedToolsNamedBy(seg.raw)`, and that call now answers
    for the nested body. Rebuild the committed `scripts/checkpoints.js`, regenerate the hook manifest
    (`npm run generate:hook-manifest`, which also rebuilds), and confirm the manifest freshness gate is
    green so `hooks/hook-entry.ts`/`.js` carry the new decider hash.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit from any command, or check:build-parity prints anything other than ALL CHECKS PASSED (the committed .js drifted from source)</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a failed test file, or "0 passed"</fails_when>
    <automated>npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit, or the output does not report the manifest fresh</fails_when>
    <automated>awk '{ if (length($0) > m) m = length($0) } END { print m }' .planning/STATE.md</automated>
    <fails_when>a value above 4000 — a STATE.md line crossed the guard's performance ceiling</fails_when>
    <automated>node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit, or any FAIL line, or a FAIL naming the STATE.md wording oracle</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or a tracked file is named as carrying a forbidden byte</fails_when>
  </verify>
  <acceptance_criteria>
    - The 33-R4 nested corpus fixture exists and is committed, holding the deny rows and the allow controls with stable labels.
    - The RED transcript is quoted in the summary: every new assertion failed against the committed round-3 `.js`.
    - After the fix, the tracer shape denies and the two deny-controls deny through both entry points; the allow control allows; `governedToolsNamedBy` of the tracer's quoted body contains the tool.
    - `check:build-parity` and `freshness:hook-manifest` are green; `hooks/guard.ts` is byte-unchanged (`git diff --stat -- hooks/guard.ts` empty).
    - Both round-4 decisions are in STATE.md's decision log; the longest STATE.md line is below 4000 and no decision line carries a backslash.
  </acceptance_criteria>
  <done>The thinnest nested-quoted governed push is denied by the shipped wrapper, and the one authority names the tool inside a quoted nested command.</done>
  <reversibility rating="costly">The projection is a shared authority both arms and the entry-point tests depend on; widening it later touches every caller and the pinned sweep counts, but it is a code change, not a migration.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand — the derived nested sweep at both levels, the union floor, and the non-circularity proof</name>
  <read_first>
    - `scripts/checkpoints.test.ts` the `33-R3 CR-01` describe (the `CR01_SPLICES` generator, the
      per-rule sweep, the count pin, the over-denial floor) and the new `33-R4 CR-01 nested` describe
      Task 1 added.
    - `hooks/guard.test.ts` the shared `runAll` harness and the entry-point sweep Task 1 lifted.
    - The 33-R4 nested corpus fixture Task 1 committed.
    - `.planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-A.jsonl` and
      `33-CAPTURE-B.jsonl` — the Bash tool inputs are the union-floor corpus (read them for the
      command strings only; do not re-run any capture).
  </read_first>
  <files>scripts/checkpoints.test.ts, hooks/guard.test.ts</files>
  <action>
    Extend the derived sweep in `scripts/checkpoints.test.ts`. For each nested wrapper family named in
    33-VERIFICATION.md gap 4 `missing` (a single-quoted body under `bash -c`/`sh -c` with a trailing
    expansion; the same under `eval` with a command-substitution trailing word; a here-string body),
    generate the case set as `COMMAND_CHECKPOINT_RULES` x `CR01_SPLICES(tool)` x the tool's governed
    verbs (reuse the existing per-rule floor that picks one governed verb), assert every generated case
    is the rule's checkpoint, and assert the generated case count equals the derivation
    (`sum over rules of splices x families`) so a row dropped from the table or a family silently lost
    turns the count red. Add the two positional-parameter shapes (a quoted body referencing `$@` / `$0`
    with the verb supplied as a following argument) from the corpus as their own asserted rows.

    Add the union floor: over the round-1 held capture's Bash tool inputs plus the existing
    over-denial floor list, assert no command's checkpoint set shrank versus the committed round-3
    build (import the pre-fix answers from a committed snapshot the test writes once, or assert the
    property directly: every floor command's set is still empty and every held-capture command that
    denied still denies). Keep the four nested allow-controls green.

    In `hooks/guard.test.ts`, extend the entry-point sweep: a derived per-tool subset of the nested
    families runs through BOTH `hooks/guard.js` and `hooks/hook-entry.js guard.js` via the shared
    `runAll`, each must deny, and the subset's size is asserted against its derivation. Keep the
    explicit large timeout the existing sweep uses.

    Non-circularity, in `scripts/checkpoints.test.ts`: build a scratch copy of `scripts/checkpoints.js`
    into a temp dir with the nested re-projection removed (a byte edit of the compiled function, or a
    scratch tsc build of a mutated source — the executor picks; the mutation must be the fix and only
    the fix), import it, and assert every nested family case is ALLOW against it while the same cases
    are DENY against the committed module. Also assert a case nested deeper than the projection bound
    denies and returns within a bounded wall-clock (reuse the guard suite's timing idiom), and that the
    two computed-at-run-time residual rows (a body assembling the tool from a shell variable; a body
    produced by a command substitution) stay ALLOW on the committed module — these are the disclosed
    residual, ledgered by plan 33-43, not closed here.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a failed test file, or "0 passed"</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' hooks/guard.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a failed test file</fails_when>
    <automated>grep -c 'toBe(' scripts/checkpoints.test.ts</automated>
    <fails_when>a count at or below the count on the dispatch base — no new count-pinned assertion was added (compare to the base with git show)</fails_when>
  </verify>
  <acceptance_criteria>
    - The nested families are generated from `COMMAND_CHECKPOINT_RULES`, never hand-listed, and the total case count is asserted equal to its derivation.
    - The entry-point sweep runs the derived subset through both `hooks/guard.js` and `hooks/hook-entry.js guard.js`, all deny, count pinned.
    - The union floor asserts no floor command and no held-capture command lost a checkpoint; the four nested allow-controls still allow.
    - The non-circularity block: the fix removed turns every nested family ALLOW while the committed module denies them; a case past the depth bound denies within the bound; the two computed-at-run-time residual rows stay ALLOW and are named as the ledgered residual.
  </acceptance_criteria>
  <done>The nested closure is pinned by a derived, counted sweep at both the model and the entry-point level, proven load-bearing, and its residual is named.</done>
</task>

<task type="auto">
  <name>Task 3: The union corpus re-run and the CR-01 hand-off note</name>
  <read_first>
    - The 33-R4 nested corpus fixture and both new describe blocks from Tasks 1-2.
    - `scripts/checkpoints.ts` `failClosedCheckpoints` residual list and the `governedToolsNamedBy`
      "WHAT IT STILL DOES NOT SEE" block — Task 3 does NOT rewrite them (plan 33-36 owns the final
      docblock rewrite); it only records what this plan proved for that plan to fold in.
  </read_first>
  <files>scripts/checkpoints.test.ts</files>
  <action>
    Add one consolidated corpus test that replays, against the COMMITTED `hooks/hook-entry.js guard.js`
    with a scrubbed environment (no `GRUGOPS_*`, no `CLAUDE_PROJECT_DIR`), the FULL 33-R4 nested corpus
    plus the verifier's own 17-ALLOW adversarial list quoted in 33-VERIFICATION.md (read them from that
    file's "live bypasses" region into the fixture, by reference) and the round-3 review's CR-01 table:
    assert every in-scope row now denies and every documented allow-control still allows. This is the
    self-reproduction the project memory requires for a safety invariant: the executor drives the
    committed artifact, not the source. Where a corpus row is one of the two computed-at-run-time
    residuals, assert it ALLOWs and label it as the residual plan 33-43 ledgers — never silently drop
    it.

    In the plan summary (not in code), state for plan 33-36 to fold into the single docblock rewrite:
    which corpus rows this plan closed, which two rows remain the disclosed residual, and that the npm
    and xargs classes are 33-36's.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a failed test file, or "0 passed"</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or a tracked file is named as carrying a forbidden byte (a raw control byte quoted from the review)</fails_when>
  </verify>
  <acceptance_criteria>
    - The consolidated corpus test replays the full round-4 nested set, the verifier's adversarial ALLOW list, and the round-3 CR-01 table against the committed wrapper with a scrubbed environment; every in-scope row denies, every allow-control allows.
    - The two computed-at-run-time residual rows are asserted ALLOW and labelled as the ledgered residual, not dropped.
    - `check:nul-bytes` is green (no raw control byte was quoted from the review into a test).
    - The summary names the closed rows, the residual rows, and the classes handed to plan 33-36.
  </acceptance_criteria>
  <done>The committed guard is adversarially re-reproduced against the round-4 corpus and the verifier's own list, and the residual is named for the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an agent's Bash command -> the PreToolUse guard | Untrusted command text; a quoted nested shell is the attack surface CR-01 names |
| the command model's projection -> the fail-closed verdict | A projection that stops one quoting layer short lets a splice survive underneath |
| the committed `.js` twin -> the shipped hook | A source fix not rebuilt into the committed `.js` and the manifest never reaches the wrapper |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-162 | Elevation of privilege | `governedToolsNamedBy` one-layer projection | critical | mitigate | Task 1 recurses the projection over each piece's shell-resolved text, bounded and fail-closed; RED-first at both arms. |
| T-33-163 | Spoofing | a governed command quoted for a nested shell | critical | mitigate | The one authority answers for the quoted body; the derived sweep pins every wrapper family (Task 2). |
| T-33-164 | Tampering | the committed `.js` diverging from source | high | mitigate | `check:build-parity` and `freshness:hook-manifest` in every task's verify. |
| T-33-165 | Repudiation | a residual class silently dropped | medium | mitigate | The two computed-at-run-time rows are asserted ALLOW and named for plan 33-43's ledger (Tasks 2-3). |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; dev deps come from the committed lockfile only. |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts` — both green.
- `npm run build && npm run check:build-parity && npm run freshness:hook-manifest` — the twin and manifest are fresh.
- `git diff --stat -- hooks/guard.ts` — empty (the frozen blob is untouched).
- `node scripts/check-foundation-guards.js` and the STATE.md longest-line `awk` — after the STATE.md write.
</verification>

<success_criteria>
- Every in-scope round-4 nested spelling denies through both entry points on the committed `.js` with a scrubbed environment; the RED was recorded first.
- The fix is one recursive projection asked by both arms; the sweep is derived and count-pinned at both levels; the union only adds denials; the residual is named.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `governedToolsNamedBy` recursive re-projection | modified function (scripts/checkpoints.ts + .js) | the one authority both arms ask, now recursive and bounded |
| the 33-R4 nested corpus fixture | new test fixture | labelled deny rows + allow controls, read by every new case |
| `33-R4 CR-01 nested` describe (checkpoints.test.ts) | new tests | derived sweep, union floor, non-circularity, computed-residual rows |
| `33-R4 CR-01 nested` describe (hooks/guard.test.ts) | new tests | derived subset through both entry points, count pinned |
| regenerated hook manifest | rebuilt decider hash (hooks/hook-entry.ts + .js) | via `npm run generate:hook-manifest` |
| D-33-R4-01, D-33-R4-03 | STATE.md decision-log rows | scope and class definition, recorded before code |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-35-SUMMARY.md` when done
</output>

---
phase: 33-live-capture-windows-portability
plan: 37
type: execute
wave: 26
depends_on: ["33-36"]
files_modified:
  - scripts/capture-live.ts
  - scripts/capture-live.js
  - scripts/capture-live.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-02]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The win32 spelling of the scoped `Edit(//ABS/**)` grant is decided from ONE authority in `scripts/capture-live.ts`, from the platform's DOCUMENTED win32 normalization: the Claude Code permissions reference states win32 paths are normalized to POSIX form before matching and `C:\\Users\\alice` becomes `/c/Users/alice`, so the anchor form is `//c/Users/...` — lower-cased drive letter, colon dropped, backslashes to forward slashes. That authority is one function; there is no `process.platform` branch anywhere (D-14/D-16)."
    - "Test C7 derives its expectation THROUGH that same authority, not by hand-reassembling a POSIX path: the test asks the module for the anchor form and compares it to the module's own normalization of the target's real path, so on win32 both sides read `//c/Users/...` and on POSIX both sides read `//abs/...`. The premise 'strip `//`, add one `/`' that made C7 red on windows-latest is gone."
    - "If the documented win32 form cannot be confirmed against a Windows session or the platform's own matcher, the module marks it `UNKNOWN - verify` HONESTLY (the docblock keeps that phrase) and the CI windows leg (plan 33-40) is the measurement — but the test still derives its expectation through the authority, so the test and the module cannot disagree whatever the decided form."
    - "The POSIX behaviour is unchanged: on darwin/linux the grant is still `Edit(//<real path without leading slash>/**)` and the whole capture-live suite stays green on this host; `check:build-parity` is green and the committed twin matches source."
  artifacts:
    - scripts/capture-live.ts
    - scripts/capture-live.js
    - scripts/capture-live.test.ts
  key_links:
    - "the platform permissions reference (win32 -> /c/Users/...) -> the one anchor authority in capture-live.ts -> liveAllowedTools's Edit rule -> Test C7's derived expectation"
    - "scripts/capture-live.ts rebuild -> npm run build -> npm run check:build-parity"
  prohibitions:
    - "no `process.platform` / `os.platform()` conditional decides the anchor form (D-14/D-16)"
    - "the test never hand-reassembles a POSIX path; it derives its expectation from the module's authority"
    - "no fabricated confirmation: if the win32 matcher form is not observed, the docblock keeps `UNKNOWN - verify` and the CI leg is named as the measurement"
    - "no gate or test result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01: the grant is what the live subject is handed; this plan changes only its spelling on win32, not what it grants."
    - "CAP-02: this is the ONE windows red of run 35760655144 (row 260); plan 33-40's pushed run measures both legs on the fixed head."
    - "CAP-03: untouched."
---

<objective>
Gap-closure round 4 (the last round under the cap). This is CAP-02's one remaining windows red:
run 35760655144's windows-latest leg failed on exactly one case, `scripts/capture-live.test.ts`
Test C7 (WINDOWS.md row 260). `liveAllowedTools` spells the scoped grant from POSIX-separator
stripping (`toPosix(real).replace(/^\/+/, "")`), which on win32 yields `Edit(//C:/Users/.../**)`,
while Test C7 re-adds one leading slash and compares to the POSIX-separated real path — a premise
true only where an absolute path begins with `/`. On win32 the two sides are `/C:/...` vs `C:/...`.
The platform's own reference says the matched form is `//c/Users/...` (lower drive letter, colon
dropped). So both the module's emitted rule and the test's comparator are wrong for win32.

This is the round-4 must-fix set item 1. The fix is D-15's shape (normalize once, in the module that
publishes) with the win32 form decided from the platform's documentation, and Test C7 derived through
that same authority so the test and the module cannot disagree.

Gap mapping (33-VERIFICATION.md CAP-02 `missing`):
- "Decide the win32 spelling of the scoped Edit rule from the platform's documented normalization
  (one authority in the module, no platform conditional per D-14/D-16) and derive Test C7's
  expectation through that same authority" -> Task 1.
- "Push (human-confirmed) and measure ONE CI run ... with both job conclusions quoted `success`" ->
  plan 33-40 (the CAP-02 measurement), not this plan.

Decisions honored: D-15 (normalize once, in the emitting module), D-14/D-16 (no platform
conditional; a red is a measurement), D-11 (fix offline with proof; the pushed run measures it).

Purpose: the scoped grant names the rule the platform will actually match on win32, and Test C7
derives its expectation from that same authority so the windows leg can read green.
Output: the one anchor authority, a C7 that derives through it, a rebuilt twin.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md
@.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md

Read-list (read these):
- `scripts/capture-live.ts` the `liveAllowedTools` function and its long design docblock (facts 1-3,
  the `//` anchor discussion, and the sentence already marking the win32 `//` spelling
  `UNKNOWN - verify`).
- `scripts/capture-live.test.ts` Test C7 (the scoped-grant form check: the `Edit(//.../**)` filter,
  the `/${anchored}` round-trip comparison, the admission-tool derivation).
- `scripts/posix-path.ts` `toPosix` / `toPosixWith` (the existing separator normalizer the module
  already uses; the win32 authority is built beside it, not a new normalizer that guesses a
  separator).
- `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` Part 4 § 4.3 (the
  full mechanism read from the tree at `1af7e3f1`, and the platform-reference quote:
  `code.claude.com/docs/en/permissions`, "Read and Edit", win32 -> `/c/Users/...`, `//c/**` form).
- `.planning/WINDOWS.md` row 260 (the ledger row; disposed only by plan 33-40's green run, not here).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One win32 anchor authority in capture-live.ts; Test C7 derives through it</name>
  <read_first>
    - The Read-list in `<context>`, all of it.
    - `scripts/capture-live.ts` `liveAllowedTools`: the exact current spelling
      `toPosix(real).replace(/^\/+/, "")` and the `Edit(//${anchored}/**)` template.
    - `scripts/capture-live.test.ts` Test C7: the assertion that `/${anchored}` equals
      `real.split(sep).join("/")`, which is the premise that fails on win32.
  </read_first>
  <files>scripts/capture-live.ts, scripts/capture-live.js, scripts/capture-live.test.ts</files>
  <behavior>
    - A new exported authority in `scripts/capture-live.ts` maps an absolute real path to the
      platform's Edit-anchor form: on POSIX it is the path with its leading slashes stripped
      (unchanged behaviour); on win32 a drive-letter path `C:\Users\...` becomes `c/Users/...`
      (lower-cased drive letter, colon dropped, backslashes to forward slashes) so `Edit(//...)`
      yields the documented `//c/Users/...`.
    - `liveAllowedTools` builds the `Edit(//<authority>/**)` rule from that function and nothing else.
    - Test C7 asks the authority for the anchor form and compares it to the authority applied to the
      target's real path — no per-case `.native` re-assembly, no `/${anchored}` round-trip.
    - On darwin the grant string is byte-identical to today's; the suite stays green.
  </behavior>
  <action>
    RED first: rewrite Test C7's comparison to derive the expected anchor through the (not-yet-added)
    authority — assert the emitted rule equals `Edit(//<authority(real)>/**)` — and add a unit case
    for the authority itself with a POSIX absolute input and a synthesised win32 drive-letter input
    (a literal `C:\\Users\\runneradmin\\...` string, transformed by the function, not by the host), so
    the win32 expectation is stated on any host. Run the capture-live suite and confirm the new
    assertions FAIL against the committed module (the authority does not exist yet); quote the RED run.

    GREEN: add the authority. Build it beside `posix-path`'s normalizer: split a drive-letter prefix
    (`^[A-Za-z]:`) when present, lower-case the letter, drop the colon, normalize backslashes to
    forward slashes via `toPosixWith(..., "\\")` after `toPosix`, and strip leading slashes; when no
    drive-letter prefix is present, keep today's POSIX behaviour (strip leading slashes). The branch
    is on the PATH SHAPE (does it start with a drive letter), never on `process.platform`. Point
    `liveAllowedTools` at it. Update the docblock: state the decided win32 form and its source (the
    permissions reference); if the live matcher form is not independently confirmed, KEEP the
    `UNKNOWN - verify` sentence and name plan 33-40's windows leg as the measurement. Rebuild the
    committed `scripts/capture-live.js`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or check:build-parity prints other than ALL CHECKS PASSED</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts</automated>
    <fails_when>non-zero exit, or a failed test file, or "0 passed"</fails_when>
    <automated>grep -c 'process.platform\|os.platform()' scripts/capture-live.ts</automated>
    <fails_when>a count above the count on the dispatch base (compare with git show) — a platform conditional was introduced</fails_when>
  </verify>
  <acceptance_criteria>
    - One exported anchor authority in `scripts/capture-live.ts` decides the Edit-anchor form; `liveAllowedTools` uses only it; no `process.platform` branch was added.
    - Test C7 derives its expectation through the authority; a unit case pins the win32 form from a synthesised drive-letter input on any host, and the POSIX case is unchanged.
    - The docblock states the decided win32 form and its source, keeping `UNKNOWN - verify` if the live matcher form is unconfirmed and naming the CI leg as the measurement.
    - `check:build-parity` green; the darwin grant string is byte-identical to before.
  </acceptance_criteria>
  <done>The scoped grant names the win32 rule the platform documents, and Test C7 derives its expectation from the same authority.</done>
  <reversibility rating="reversible">A local change to one module's anchor spelling and its own test; swapping the decided form later is a one-function edit.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The capture-live suite and the whole excluded-lane suite stay green on this host</name>
  <read_first>
    - `scripts/capture-live.test.ts` (all of it) — confirm no other case hard-codes the old anchor spelling.
    - `.planning/phases/33-live-capture-windows-portability/33-VALIDATION.md` (the sampling rule: full excluded-lane suite + typecheck before the CAP-02 measurement).
  </read_first>
  <files>scripts/capture-live.test.ts</files>
  <action>
    Grep the capture-live suite for any remaining hand-assembled anchor comparison (a `/${...}`
    round-trip, a bare `.replace(/^\/+/, "")` in a test expectation) and route each through the
    authority so no test states the anchor form independently of the module. Run the full excluded-lane
    suite and typecheck to confirm the change moved no other file's result on this host (D-11's
    "measure the blast radius" for a shared module). Record in the summary that the win32 axis remains
    `unmeasured locally, by construction` — this host cannot produce a drive-letter path — and that
    plan 33-40's windows leg is the measurement, in the 33-24/33-31 wording.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the summary reports any failed test file, or "0 passed"</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or a tracked file named as carrying a forbidden byte</fails_when>
  </verify>
  <acceptance_criteria>
    - No test in the capture-live suite states the anchor form independently of the module authority.
    - The full excluded-lane suite and typecheck are green on this host; the summary names the win32 axis as unmeasured-locally and plan 33-40's leg as the measurement.
  </acceptance_criteria>
  <done>The suite is green on this host through the one authority, and the one axis it cannot reach is named for the pushed run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the platform's documented normalization -> the emitted Edit rule | A rule spelled in a form the platform never matches silently grants nothing (or the wrong scope) on win32 |
| the module's anchor authority -> the test's expectation | A test that reassembles the path independently can disagree with the module and red the CI leg for the wrong reason |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-169 | Tampering | a platform conditional forcing a green | high | mitigate | The branch is on path shape, not `process.platform`; a grep gate asserts no platform conditional was added. |
| T-33-170 | Repudiation | a fabricated win32 confirmation | medium | mitigate | If the live matcher form is unconfirmed the docblock keeps `UNKNOWN - verify` and names the CI leg as the measurement (D-11). |
| T-33-171 | Tampering | the committed `.js` diverging from source | high | mitigate | `check:build-parity` in Task 1's verify. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; dev deps from the committed lockfile only. |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/capture-live.test.ts` — green on this host.
- `npm run build && npm run check:build-parity` — twin matches source.
- `grep -c 'process.platform' scripts/capture-live.ts` — not above the base (no platform conditional).
</verification>

<success_criteria>
- The win32 anchor is decided from one authority, from the platform's documented form; Test C7 derives through it; the suite is green on this host; the win32 axis is named for plan 33-40's pushed run.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| the Edit-anchor authority | new exported function (capture-live.ts + .js) | POSIX unchanged; win32 -> `//c/Users/...` from the platform reference |
| `liveAllowedTools` | modified to use the authority (capture-live.ts + .js) | one spelling, no platform conditional |
| Test C7 + a win32 unit case | modified/new tests (capture-live.test.ts) | expectation derived through the authority |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-37-SUMMARY.md` when done
</output>

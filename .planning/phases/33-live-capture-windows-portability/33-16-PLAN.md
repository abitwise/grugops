---
phase: 33-live-capture-windows-portability
plan: 16
type: execute
wave: 8
depends_on: ["33-15"]
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-02]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The working directory `trustedRepoRoot` walks from is canonicalised through the module's own `canonicalDirectoryPath` (rung 1 `realpathSync.native`) before the ancestor walk, so a cwd addressed through a directory symlink names the same directory on every host and the R-31-19-07 SYMLINK cell HOLDS on win32 as it does on darwin — with the 31-15 monotonicity mirror's two anchored lines byte-unchanged."
    - "A GOV-02 ledger position that is not a regular file (a directory, a FIFO, a device) is classified by its TYPE before any open, so the refusal arm and its sentence are the same on every host; both refusal arms state that the position is refused rather than waited on (R-31-21-03's published shape)."
    - "Every directory-symlink fixture in `scripts/context-io.test.ts` is staged through `stageSymlinkOrSkip`, so a host without the symlink privilege prints the named `SKIPPED` row and counts in the platform-shape remainder instead of throwing EPERM (D-16)."
    - "Each module change is followed to every arm that consumes the changed value: the mirror, the CASE cell, the three rungs, every write-side arm assertion."
  artifacts:
    - scripts/context-io.ts
    - scripts/context-io.js
    - scripts/context-io.test.ts
  key_links:
    - "context-io.ts `trustedRepoRoot` cwd read -> `canonicalDirectoryPath(cwd)` -> `projectRootFromWorkingDirectory` (anchored line unchanged)"
    - "context-io.ts `appendRegularFileLine` -> pre-open `lstatSync` type classification -> the `not-a-regular-file` arm -> post-open `fstatSync` (kept as the authoritative guard)"
    - "context-io.test.ts symlink fixtures -> `stageSymlinkOrSkip` (check-platform-shapes.ts) -> the platform-shape remainder"
  prohibitions:
    - "no platform conditional closes a CAP-02 red (D-14/D-16)"
    - "the 31-15 monotonicity mirror's anchored lines are never reformatted or moved"
    - "a refusal arm is never made unreachable without a test that proves which arm now answers"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): untouched by this plan."
    - "CAP-02 (edge row unresolved, flagged): this plan closes the two module-side context-io windows reds and D-16-guards the symlink fixtures; the leg's conclusion is measured only by plan 33-20."
    - "CAP-03 (edge row unresolved, flagged): untouched by this plan."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). Two of the twelve context-io windows reds are MODULE defects, not test
spellings, and one `status: open` item is the D-16 symlink guard the round-1 plans left
mechanical. This plan follows plan 33-15 in the next wave because it edits the same test file.

Gap mapping (33-VERIFICATION.md CAP-02 `missing[1]`, the tenth context-io case and one of the
two one-arm-over survivors; `deferred-items.md` "The directory-symlink fixtures in
scripts/context-io.test.ts ... are not D-16-guarded", status: open):
- Task 1: W-21 `R-31-19-07 re-measured on BOTH axes: the SYMLINK cell still HOLDS, the CASE cell is CLOSED` (row 226, 5/10) — the cwd keeps the link spelling on win32.
- Task 2: W-28 `R-31-21-03's published shape AGREES with the reading its probe takes` (row 234) — `not-waited-on=false`: a different refusal arm answers on win32.
- Task 3: the directory-symlink fixtures at ~L425, L4136, L7984, L8111, L8470, L9850, L10796, L10804, L10822, L11628, L11630 → `stageSymlinkOrSkip`.

Decisions honored: D-13, D-14, D-15 (a location the module opens is canonicalised where the
module reads it, once), D-16 (symlink EPERM = named skip, counted; no conditional).

Purpose: the two behaviours the CI leg measured as host-dependent become host-independent at the
point the module reads its input, and the fixtures that need a privilege say so by name.
Output: `scripts/context-io.ts` + committed `.js`; `scripts/context-io.test.ts` with the two
module proofs and every symlink fixture routed through the corpus helper.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
@.planning/phases/33-live-capture-windows-portability/deferred-items.md
@.planning/phases/33-live-capture-windows-portability/33-05-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-15-SUMMARY.md
@scripts/check-platform-shapes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The walk starts from the canonical working directory (W-21, the SYMLINK cell)</name>
  <read_first>
    - `scripts/context-io.ts` `trustedRepoRoot` (~L5237-5275: the `cwd = process.cwd()` read, the ANCHORED comment block, and the anchored line `const discovered = cwd === null ? null : projectRootFromWorkingDirectory(cwd);`), `canonicalDirectoryPath` (~L4587-4640: the three rungs), `hostDeliveredRoot` (~L5117), `projectRootFromWorkingDirectory` (~L4963).
    - `scripts/context-io.test.ts` ~L8454-8500 (the R-31-19-07 case: CELL 2 SYMLINK — the comment's premise "`process.cwd()` returns the kernel's realpath" is POSIX-only), ~L6472-6560 (the MONOTONICITY mirror: it reverts exactly two anchors and asserts their occurrence counts before and after the mutation), the CASE cell that follows CELL 2.
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 row W-21 (`expected '...\link\proj' to be '...\kit\proj'` — the link was created, so the privilege is held; the verdict moved because cwd kept the link spelling).
  </read_first>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts</files>
  <behavior>
    - Test W (RED first, drivable on darwin because the seam is the cwd VALUE, not the kernel): export `canonicalWorkingDirectory(cwd: string): string` and assert that for a directory reached through a symlink it returns the link target's canonical spelling, and for a canonical path it is the identity; then assert `trustedRepoRoot` driven with the child's cwd set to `<link>/proj` returns `<kit>/proj` (the existing CELL 2 assertion, now holding by construction rather than by the kernel).
    - Test X (the anchors did not move): the MONOTONICITY mirror's before/after occurrence counts still pass unchanged — run it and quote the summary line.
  </behavior>
  <action>
    In `trustedRepoRoot`, canonicalise the working directory where it is READ, not where it is
    walked: the `try { cwd = process.cwd(); }` assignment becomes
    `cwd = canonicalWorkingDirectory(process.cwd());` where the new exported
    `canonicalWorkingDirectory(raw: string): string` applies the module's existing
    `canonicalDirectoryPath` (rung 1 `realpathSync.native`, rung 2 portable, rung 3 deepest
    existing ancestor) — the same canonicaliser tier 0 already uses, so the walk's start and the
    delivered root are spelled by one authority. The anchored line
    `const discovered = cwd === null ? null : projectRootFromWorkingDirectory(cwd);` and the loop's
    first element stay BYTE-IDENTICAL (the 31-15 mirror asserts their occurrence counts exactly).
    Extend the ANCHORED comment with one sentence: the canonicalisation lives on the read line so
    the mirror's two anchors are untouched.

    Update CELL 2's comment in the test: the exclusion holds because the walk canonicalises its
    start through rung 1, not because of a kernel behaviour only POSIX has; on win32
    `process.cwd()` keeps the link spelling and the module now removes it. Route the `symlinkSync`
    in that case through `stageSymlinkOrSkip` (Task 3 does the rest of the file; this one moves with
    its case).

    Probe every arm that consumes the changed value: the CASE cell (still CLOSED — a case-differing
    spelling is rung 1's job and is unchanged), the three-rung case, the `hostDeliveredRoot` tier
    ordering, the near/far step-limit cases (a canonical temp root may differ from the raw one on
    darwin — `/var` vs `/private/var` — so those cases' `projectWith` fixtures must compare through
    the same canonicaliser; run them and record), and the MONOTONICITY mirror.

    Rebuild so the committed `.js` moves in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts -t "R-31-19-07 re-measured|MONOTONICITY|THREE rungs|tier 0 OUTRANKS|BOUND: the ancestor walk|published step limit"</automated>
    <fails_when>non-zero exit, or fewer than 6 tests reported passed, or 0 passed</fails_when>
    <automated>grep -a -c 'const discovered = cwd === null ? null : projectRootFromWorkingDirectory(cwd);' scripts/context-io.ts</automated>
    <fails_when>a count other than 1 — the anchored line moved or was duplicated</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npx tsc --noEmit</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/context-io.ts` exports `canonicalWorkingDirectory` and `trustedRepoRoot` reads its cwd through it; the anchored line and the loop's first element are byte-identical to HEAD~ (`git diff HEAD~ -- scripts/context-io.ts` shows no hunk touching them; quoted).
    - Tests W and X exist; the RED commit precedes the GREEN commit (shas quoted).
    - The summary lists every arm probed with its vitest result line.
  </acceptance_criteria>
  <done>The walk's start is spelled by the same authority as the delivered root, on every host.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A non-regular ledger position is refused by TYPE, on every host, with one sentence (W-28)</name>
  <read_first>
    - `scripts/context-io.ts` `appendRegularFileLine` (~L1128-1200: `openSync` with `O_NONBLOCK`, the `unopenable` arm's sentence carrying "refused rather than waited on", the `not-a-regular-file` arm's sentence carrying "refused rather than written"), `UNRECORDABLE_ADMISSION_REFUSAL`, `LEDGER_PATH_NOT_REGULAR_FILE_CLAUSE`, `CANONICAL_READ_POSITION`.
    - `scripts/context-io.test.ts` ~L15618-15645 (the R-31-21-03 probe: a DIRECTORY at `.grugops/audit/admissions.jsonl`; the reading `not-waited-on` is `message.includes("refused rather than waited on")`), and every write-side assertion on the arm names (`grep -n '"unopenable"\|not-a-regular-file' scripts/context-io.test.ts`).
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 row W-28 and WINDOWS.md row 234.
  </read_first>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts</files>
  <behavior>
    - Test Y (RED first): a DIRECTORY at the ledger position makes `appendNote` throw a `ReadPositionRefusal` whose `condition` is `not-a-regular-file` and whose message contains BOTH `refused rather than waited on` and `CANONICAL_READ_POSITION`; the throw is bounded (under 5 s) and nothing is written. The premise that the darwin `openSync` of a directory currently answers through the `unopenable` arm is asserted FIRST in the same body (the red half), then the post-fix arm.
    - Test Z (the FIFO and the unwritable parent still answer): a FIFO staged through `stageShapeOrSkip` at the position → `not-a-regular-file` (or the printed skip row where the host cannot stage one); a position whose parent directory does not exist → `unopenable`, still carrying `refused rather than waited on`.
  </behavior>
  <action>
    In `appendRegularFileLine`, classify BEFORE opening: `lstatSync(path, { throwIfNoEntry: false })`
    — when the entry exists and is not a regular file (directory, FIFO, socket, character or block
    device, symlink to any of those resolved by a second `statSync`), throw the `not-a-regular-file`
    refusal without calling `openSync`. Keep the post-open `fstatSync` guard exactly as it is (it is
    the authoritative check against a race between the lstat and the open); it now answers only
    when the position changed type between the two calls. Make both arms' sentences carry the
    phrase "refused rather than waited on" (both are bounded refusals; the phrase is R-31-21-03's
    published property, and the write-side residual text that names it), while the
    `not-a-regular-file` arm keeps its "writing to a FIFO or a device can block forever" reason.
    Extend the function's docblock: on POSIX a directory fails at open with EISDIR and answered
    through the `unopenable` arm by accident of open semantics; on win32 the open of a directory
    handle succeeds and the `fstat` arm answered instead — two hosts, two sentences, one property.
    Classifying by type first makes the arm the same everywhere.

    Probe every arm: run every write-side assertion on the two arm names; the R-31-21-03 probe;
    the read-side `readRegularFileOrNull` cases are a different function and must be unchanged
    (run the CR-24 plant table as the control).

    Rebuild so the committed `.js` moves in the same commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts -t "R-31-21-03|every planted condition reports its OWN arm|unopenable|not-a-regular-file"</automated>
    <fails_when>non-zero exit, or 0 passed</fails_when>
    <automated>grep -a -c 'refused rather than waited on' scripts/context-io.ts</automated>
    <fails_when>a count below 2 — both write-side arms must carry the phrase</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `appendRegularFileLine` classifies the position's type before `openSync`; the post-open `fstatSync` guard is unchanged.
    - Both refusal sentences contain `refused rather than waited on`.
    - Tests Y and Z exist; the RED commit precedes the GREEN commit; the summary quotes the red half's premise (darwin answered through `unopenable`).
    - Every write-side arm assertion in the file passes; the summary lists them.
  </acceptance_criteria>
  <done>The arm that refuses a directory at the ledger is decided by the position's type, not by which host's open call happened to fail.</done>
</task>

<task type="auto">
  <name>Task 3: Every directory-symlink fixture is staged through the corpus helper (D-16)</name>
  <read_first>
    - `scripts/check-platform-shapes.ts` `stageSymlinkOrSkip(target, at, shape, position)` (~L688) — returns null when staged, the skip entry on EPERM (`deniedBy`); the `SKIPPED:` printing idiom the other migrated sites use; `SYMLINK_SHAPE_NAME`.
    - `scripts/context-io.test.ts` — the existing routed sites (~L9685, ~L13950) as the pattern; the unrouted `symlinkSync(` sites at ~L425, L4136, L7984, L8111, L8470 (moved by Task 1), L9850, L10796, L10804, L10822, L11628, L11630.
    - `.planning/phases/33-live-capture-windows-portability/deferred-items.md` — the symlink-fixtures item and its `Why deferred`.
    - `.planning/phases/33-live-capture-windows-portability/33-05-SUMMARY.md` — how the FIFO/device sites were migrated and how a skip is printed and counted.
  </read_first>
  <files>scripts/context-io.test.ts</files>
  <action>
    <!-- planner-discipline-allow: symlinkSync( -->
    Route every remaining `symlinkSync(` in the file through `stageSymlinkOrSkip` with a shape
    name that says what is linked (`directory symlink to a context store`, `directory symlink to a
    kit home`, `symlink to a factory config file`, `symlink to a git directory`) and a `position`
    naming the file and case. When the helper returns a skip entry, print it with the same
    `SKIPPED:` idiom the FIFO sites use and return from the case — never `it.skip`, never a
    conditional on `process.platform`. Derive the set: `grep -c 'symlinkSync(' scripts/context-io.test.ts`
    before and after, and the count of `stageSymlinkOrSkip(` after; the summary states the three
    numbers and that the first is now 0 outside the helper's own import line.

    Probe every arm: the platform-shape remainder is derived by `check-platform-shapes.ts`; run
    `npm run check:platform-shapes` and quote the remainder line (on darwin the symlink shape is
    constructible, so the remainder is unchanged; on the windows runner the privilege is held, so
    the rows stay absent there too — the guard is for hosts without it, D-16).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
    <automated>grep -a -c 'symlinkSync(' scripts/context-io.test.ts</automated>
    <fails_when>a count above 0 — a fixture still creates its link without the corpus helper</fails_when>
    <automated>npm run check:platform-shapes</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - Every symlink fixture in the file goes through `stageSymlinkOrSkip`; the summary states the before/after counts.
    - No `process.platform` read was added; no `it.skip`.
    - The full context-io suite is green on this host and the platform-shape gate exits 0.
  </acceptance_criteria>
  <done>A host without the symlink privilege prints named rows and counts them; it does not throw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a working directory spelled through a link -> the trusted-root exclusion | Two spellings of one directory let the kit's own root be mistaken for a project root |
| a ledger position that is not a regular file -> the audit trail | A write that blocks, or one that records nothing while reporting success, breaks the trail |
| a fixture needing a privilege -> the suite's verdict | An EPERM read as a red, or a silent pass, both misreport the platform |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-80 | Spoofing | trusted-root walk start | high | mitigate | Task 1: cwd canonicalised through the one canonicaliser before the walk; mirror anchors byte-unchanged and asserted. |
| T-33-81 | Denial of service | ledger append on a non-regular position | high | mitigate | Task 2: type classified before open; `fstat` guard kept; both arms bounded and worded the same. |
| T-33-82 | Repudiation | symlink fixtures on a host without the privilege | medium | mitigate | Task 3: named `SKIPPED` rows through `stageSymlinkOrSkip`, counted in the remainder. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run check:build-parity` — committed `.js` faithful.
- `node scripts/check-foundation-guards.js` — the guards over the module's tripwires still green (context-io.ts is a tripwire module).
</verification>

<success_criteria>
- W-21 and W-28 are closed in the module, with the consuming arms probed and the mirror anchors intact.
- Every symlink fixture is D-16-guarded; the deferred item is ready to be marked resolved by plan 33-23.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `canonicalWorkingDirectory(raw)` | exported function, `scripts/context-io.ts` | the walk's start through the one canonicaliser |
| pre-open type classification in `appendRegularFileLine` | module behavior | host-independent refusal arm; both sentences carry the bounded-refusal phrase |
| `stageSymlinkOrSkip` at every symlink fixture | test changes | D-16 named skips |
| Tests W, X, Y, Z | new test cases | red-first, each named in this plan |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-16-SUMMARY.md` when done
</output>

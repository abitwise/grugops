---
phase: 33-live-capture-windows-portability
plan: 36
type: execute
wave: 25
depends_on: ["33-35"]
files_modified:
  - scripts/checkpoints.ts
  - scripts/checkpoints.js
  - scripts/checkpoints.test.ts
  - hooks/guard.test.ts
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
autonomous: true
gap_closure: true
requirements: [CAP-01, CAP-02]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An npm/yarn/pnpm invocation whose verb is a UNIQUE-PREFIX abbreviation of `publish` (the shapes 33-VERIFICATION.md 'Adversarial guard probe' names as the npm-verb-abbreviation class) is the production checkpoint. The rule is DERIVED from the tool's own `publish` verb — the shortest unambiguous prefix among that tool's known verbs — never a hand-listed set of abbreviations; a prefix that also prefixes a benign verb of that tool is NOT governed."
    - "A governed tool invoked under `xargs` whose verb does not appear on the command line (the shape 33-VERIFICATION.md names as the xargs class) is DENIED: xargs supplies the missing arguments from stdin, so the guard cannot see the verb and must fail closed on the governed tool name under xargs."
    - "Both closures live in the command model (`scripts/checkpoints.ts`) and are asked by both arms; `hooks/guard.ts` is not edited and its frozen blob stays byte-identical."
    - "The residual docblocks at `governedToolsNamedBy` ('WHAT IT STILL DOES NOT SEE') and at `failClosedCheckpoints` are rewritten ONCE, over the final round-4 model, to state exactly what the round-4 tests show: the nested class (33-35) and these two classes are closed, and the named residuals are the two computed-at-run-time rows plus the ledgered classes (git send-pack, the gh api merge endpoint, the dashed git-core push binary, the row 259 alias form, the out-of-table deploy verbs)."
    - "The union can only add denials: every new deny is proven RED-first against the committed round-3 build, and no command in the round-1 held capture's Bash inputs or the over-denial floor moves from deny to allow. `npm run build` (run), `npm ci`, `npm run publish` (a package script), `npm pack`, and an ordinary `find ... | xargs grep` still ALLOW."
  artifacts:
    - scripts/checkpoints.ts
    - scripts/checkpoints.js
    - scripts/checkpoints.test.ts
    - hooks/guard.test.ts
  key_links:
    - "COMMAND_CHECKPOINT_RULES npm/yarn/pnpm publish verb -> the derived unique-prefix rule -> the readable arm's verb match"
    - "a governed tool as the xargs command word with no on-line verb -> fail-closed on the tool name"
    - "scripts/checkpoints.js rebuild -> npm run generate:hook-manifest -> hooks/hook-entry decider hash -> npm run freshness:hook-manifest"
  prohibitions:
    - "no hand-listed abbreviation set; the prefix rule is derived from the tool's own verbs and its uniqueness proven by test"
    - "no edit to hooks/guard.ts; the frozen blob stays byte-identical"
    - "the ledgered classes (send-pack, gh api merge, dashed git-push binary, row 259 alias, out-of-table deploy verbs) are NOT closed in code here — they are plan 33-43's ledger rows"
    - "no gate or test result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01: the guard is the D-04 surface; this plan only adds denials."
    - "CAP-02: this plan's modules must be green on both CI legs; plan 33-40's pushed run measures them."
    - "CAP-03: untouched."
---

<objective>
Gap-closure round 4 (the last round under the cap). Scope items (b) and (c) of the human's decision
D-33-R4-01 (recorded 2026-09-23): close in code the npm-verb-abbreviation class and the xargs class,
both named in 33-VERIFICATION.md "Adversarial guard probe" as ALLOW-with-zero-keys, pre-existing.
Then rewrite the two residual docblocks ONCE, over the final round-4 model, so the documentation
states what the tests show rather than what the round-3 design intended (33-VERIFICATION.md gap 4
`missing[2]`, deferred here so it is written after 33-35's nested closure and these two classes land).

Gap mapping:
- 33-VERIFICATION.md "live bypasses" table, npm-verb-abbreviation row -> Task 1.
- 33-VERIFICATION.md "live bypasses" table, xargs-stdin-verb row -> Task 2.
- 33-VERIFICATION.md gap 4 `missing[2]` ("Rewrite the residual docblock to state what the tests show") -> Task 3.

Decisions honored: D-33-R4-01 (b)/(c) (the human's scope), D-04 (the guard is the observed deny
surface; only denials are added), D-33-R4-03 (both arms ask one authority).

Purpose: an abbreviated publish and a governed tool hidden under xargs both stop at the guard; the
guard's own residual note is true.
Output: the derived prefix rule, the xargs fail-closed rule, RED-first tests, a rebuilt twin, a
regenerated manifest, and one truthful docblock rewrite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md

Read-list (read these; do not inline exploit strings into any prompt):
- `scripts/checkpoints.ts` `COMMAND_CHECKPOINT_RULES` (the npm/yarn/pnpm rows and their `benign`
  lists; the `verbs: ["publish"]`), `verbCandidates`, `adjacentWord`, `matchCommandCheckpoints`
  (the readable arm's verb decision and the `benign` suppression), `failClosedCheckpoints` and the
  `governedToolsNamedBy` residual block.
- `scripts/checkpoints.test.ts` the `33-R3 CR-01` describe and the over-denial floor (which already
  lists `npm run publish`, `npm run build` as must-not-deny).
- `hooks/guard.test.ts` the shared `runAll` harness (lifted in 33-35) and the entry-point sweep.
- `.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md` "Adversarial guard probe"
  (the npm-abbreviation row measured with `npm <abbrev> --help`, and the xargs row) and gap 4
  `missing[2]`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The npm/yarn/pnpm unique-prefix publish rule, derived from the verb</name>
  <read_first>
    - `scripts/checkpoints.ts` the npm/yarn/pnpm rows of `COMMAND_CHECKPOINT_RULES` (each
      `verbs: ["publish"]` with a `benign` list), and the readable arm in `matchCommandCheckpoints`
      that compares a candidate against `r.verbs`.
    - `scripts/checkpoints.test.ts` the over-denial floor (`npm run publish`, `npm run build` are
      must-not-deny) and the `33-R3 CR-01` derived sweep.
    - `.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md` "Adversarial guard
      probe" — the npm-abbreviation row: npm resolves a unique prefix of `publish` to publish; the
      planner re-measured this 2026-09-23 (npm 11.7.0: `pu`/`pub`/`publ`/`publi`/`publis` -> "Publish
      a package"; `p` is ambiguous). pnpm/yarn do not abbreviate but the derived rule costs nothing
      there and is a superset guard.
  </read_first>
  <files>scripts/checkpoints.ts, scripts/checkpoints.js, scripts/checkpoints.test.ts</files>
  <behavior>
    - A publish-prefix invocation of npm (each unique prefix of `publish` down to the shortest
      unambiguous one) is the production checkpoint.
    - The rule is derived: for a tool whose governed verb is `publish`, a candidate that is a prefix
      of `publish` and is NOT a prefix of any of that tool's `benign` verbs governs; an ambiguous
      prefix (one that also prefixes a benign verb) does NOT govern.
    - `npm run publish`, `npm run build`, `npm ci`, `npm pack`, `npm i` still allow.
  </behavior>
  <action>
    RED first: add cases asserting each unique publish-prefix of npm is `production_requires_human_confirmation`
    and the ambiguous shortest prefix and the benign controls are not, generated from the npm rule's
    own `verbs`/`benign` (no hand-listed abbreviation strings). Build the committed `.js` and confirm
    they fail against it; quote the RED run.

    GREEN: extend the readable-arm verb decision so a governed tool's verb match also accepts a
    candidate that is an unambiguous prefix of a governed verb — unambiguous meaning it is a prefix of
    exactly one of the union of that rule's `verbs` and `benign`, and that one is a governed verb.
    Derive the discriminator from the rule's own arrays; add no abbreviation constant. Keep `benign`
    suppression adjacent-only (unchanged). Rebuild the committed `scripts/checkpoints.js`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or check:build-parity prints other than ALL CHECKS PASSED</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts</automated>
    <fails_when>non-zero exit, or a failed test file, or "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - The prefix rule is derived from each tool's own `verbs`/`benign`; no abbreviation literal appears in the source or the tests.
    - Each unique publish-prefix of npm denies; the ambiguous shortest prefix and every benign control allow.
    - `check:build-parity` green; the committed `.js` matches source.
  </acceptance_criteria>
  <done>An abbreviated `publish` is governed exactly as the spelled-out verb, by a rule derived from the verb itself.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A governed tool under xargs with no on-line verb fails closed</name>
  <read_first>
    - `scripts/checkpoints.ts` `matchCommandCheckpoints` (how the tool word is found and the verb
      candidates are read after it), `verbCandidates`, and `governedToolsNamedBy`.
    - `.planning/phases/33-live-capture-windows-portability/33-VERIFICATION.md` "Adversarial guard
      probe" — the xargs row: a governed tool is the xargs command word and the verb is piped in from
      stdin, so no verb is visible; the planner re-measured 2026-09-23 that these ALLOW and run.
    - `scripts/checkpoints.test.ts` the over-denial floor (an ordinary `find ... | xargs grep` must
      still allow).
  </read_first>
  <files>scripts/checkpoints.ts, scripts/checkpoints.js, scripts/checkpoints.test.ts</files>
  <behavior>
    - A pipeline segment whose command word is `xargs` and whose xargs command (the first non-flag
      operand of xargs, and the positional-parameter forms that make xargs feed a nested shell) names
      a governed tool with NO governed verb visible on the line DENIES on that tool.
    - An ordinary `xargs` over a non-governed command (grep, rm on files, echo) still ALLOWS.
  </behavior>
  <action>
    RED first: add cases from the xargs class (a governed tool as xargs's command with the verb only
    on stdin; the `xargs sh -c '<tool> "$@"'` positional form) asserting the governed checkpoint, and
    the non-governed-xargs controls asserting no checkpoint. Build and confirm they fail against the
    committed `.js`; quote the RED run.

    GREEN: in the command model, when a segment's command word (or a nested shell's command word,
    reached through the 33-35 projection) is `xargs`, identify xargs's own command operand; if it
    `governedToolsNamedBy` a governed tool and no governed verb for that tool appears among the
    segment's on-line words, add that tool's checkpoint fail-closed. Do not treat every `xargs` as
    governed — only when its operand names a governed tool. Rebuild the committed `.js`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or check:build-parity prints other than ALL CHECKS PASSED</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts</automated>
    <fails_when>non-zero exit, or a failed test file, or "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - A governed tool under xargs with no visible verb denies; a non-governed xargs pipeline allows.
    - The rule keys on xargs's own command operand naming a governed tool, not on xargs alone.
    - `check:build-parity` green.
  </acceptance_criteria>
  <done>xargs can no longer smuggle a governed verb in from stdin past the guard.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the two residual docblocks over the final round-4 model; entry-point re-run</name>
  <read_first>
    - `scripts/checkpoints.ts` the `governedToolsNamedBy` "WHAT IT STILL DOES NOT SEE" block and the
      `failClosedCheckpoints` residual list, as they read after 33-35 and Tasks 1-2 here.
    - The 33-35 summary's hand-off note (which corpus rows it closed, which two are the residual) and
      33-VERIFICATION.md gap 4 `missing[2]`.
    - `hooks/guard.test.ts` the entry-point sweep.
  </read_first>
  <files>scripts/checkpoints.ts, scripts/checkpoints.js, hooks/guard.test.ts, hooks/hook-entry.ts, hooks/hook-entry.js</files>
  <action>
    Rewrite both docblocks ONCE so each states what the round-4 tests show: the nested-quoted class
    (33-35), the npm-prefix class and the xargs class are closed; the named residuals are the two
    computed-at-run-time rows (a tool assembled from a shell variable; a body produced by a command
    substitution) plus the classes the ledger — not the code — carries (git send-pack, the gh api
    merge endpoint, the dashed git-core push binary, the WINDOWS.md row 259 alias form, the
    out-of-table deploy verbs). Do not restate the round-3 intent; describe the current behaviour.
    Comment-text discipline: do not paste any spliced token or exploit spelling into a docblock that a
    negative-grep test asserts absent; name the classes in words.

    Rebuild the committed `scripts/checkpoints.js` and regenerate the hook manifest
    (`npm run generate:hook-manifest`); confirm freshness. Add one entry-point case per new class
    (an abbreviated publish; a governed tool under xargs with no on-line verb) through both
    `hooks/guard.js` and `hooks/hook-entry.js guard.js` via the shared `runAll`, asserting deny.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts</automated>
    <fails_when>non-zero exit, or a failed test file, or "0 passed"</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit from any command, or a not-fresh report</fails_when>
    <automated>git diff --stat -- hooks/guard.ts</automated>
    <fails_when>non-empty output — the frozen guard blob was edited</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or a tracked file named as carrying a forbidden byte</fails_when>
  </verify>
  <acceptance_criteria>
    - Both docblocks describe the current model: the three round-4 closures and the named residuals (two computed-at-run-time rows + the ledgered classes).
    - An abbreviated publish and a governed tool under xargs each deny through both entry points.
    - `check:build-parity`, `freshness:hook-manifest` green; `hooks/guard.ts` byte-unchanged; `check:nul-bytes` clean.
  </acceptance_criteria>
  <done>The guard's own residual note is true over the final round-4 model, and the two new classes deny at the wrapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an agent's Bash command -> the PreToolUse guard | An abbreviated verb or an xargs-fed verb is untrusted command text the guard must resolve |
| the command model's residual docblock -> a future reader | A docblock that overstates coverage invites widening the parser on a false premise |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-166 | Elevation of privilege | abbreviated `publish` unseen by the verb match | high | mitigate | Task 1 derives an unambiguous-prefix rule from the verb itself, RED-first. |
| T-33-167 | Elevation of privilege | a governed tool's verb supplied via xargs stdin | high | mitigate | Task 2 fails closed on a governed tool under xargs with no on-line verb. |
| T-33-168 | Repudiation | a residual docblock claiming more than the tests show | medium | mitigate | Task 3 rewrites both blocks over the final model; the ledgered classes are named. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; dev deps from the committed lockfile only. |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts hooks/guard.test.ts` — green.
- `npm run build && npm run check:build-parity && npm run freshness:hook-manifest` — twin and manifest fresh.
- `git diff --stat -- hooks/guard.ts` — empty.
</verification>

<success_criteria>
- An abbreviated `publish` and an xargs-hidden governed verb both deny, RED-first, by rules derived from the tool's own verbs; the union only adds denials; the residual docblocks are truthful.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| the unique-prefix publish rule | modified readable-arm verb decision (checkpoints.ts + .js) | derived from each tool's own verbs/benign |
| the xargs fail-closed rule | modified command model (checkpoints.ts + .js) | keys on xargs's operand naming a governed tool |
| the two rewritten residual docblocks | modified comments (checkpoints.ts + .js) | true over the final round-4 model |
| new entry-point cases | tests (hooks/guard.test.ts) | abbreviated publish + xargs, both entry points |
| regenerated hook manifest | rebuilt decider hash (hooks/hook-entry.ts + .js) | via npm run generate:hook-manifest |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-36-SUMMARY.md` when done
</output>

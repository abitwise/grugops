---
phase: 33-live-capture-windows-portability
plan: 31
type: execute
wave: 20
depends_on: ["33-24", "33-25", "33-26", "33-27", "33-28", "33-29", "33-30"]
files_modified:
  - .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: false
gap_closure: true
requirements: [CAP-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CAP-02 is measured by ONE pushed CI run — the THIRD of this phase — whose own `conclusion` fields for `test (ubuntu-latest)` and `test (windows-latest)` are quoted from `gh run view --json`; the requirement flips only when BOTH read `success` (D-13). Rows 186, 193, 226-235 and 236 flip only on that rule."
    - "Measurement and fix never share a plan: this plan changes no source file; a red leg becomes round 4's inventory in the same four-label shape (`not addressed`, `incomplete fix`, `new`, `mis-attributed`), never a fix here (D-11, as 33-09 and 33-20 applied it)."
    - "Before the push, every workflow command is run locally in the workflow's order with its exit status, and the 35 titles of row 236's class are listed with the 33-24 hand-off's `unmeasured locally, by construction` column — so the run decides something the document already predicted, and its falsifier is written down."
    - "The push is behind a named human confirmation and the executor never pushes on its own (the repository's hard safety rule). This push carries EVERY round-3 source commit (plans 33-24 through 33-30), and it is also the first of the two pushes the go path needs: plan 33-32's runner refuses unless HEAD equals `origin/main`."
    - "Ledger rows change only through the tool; the three representations are checked to agree afterwards; the CAP-02 requirement row is the only requirement row this plan touches."
  artifacts:
    - .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
    - .planning/REQUIREMENTS.md
    - .planning/WINDOWS.md
  key_links:
    - "the pre-push inventory (Part 4 § 4.1) -> the human push -> `gh run view <id> --json databaseId,headSha,conclusion,jobs` -> Part 4 § 4.2-4.3 -> the CAP-02 row"
    - "the run's `success`/`failure` words -> `gsd-tools windows fixed <id>` for rows 226-235, 236 and 186, `windows waive 193` (green only)"
    - "33-24-SUMMARY.md's 35-row hand-off -> Part 4 § 4.1's row-236 table -> the falsifier"
  prohibitions:
    - "no platform conditional closes a CAP-02 red (D-14/D-16)"
    - "no fix is applied and re-pushed inside this plan; a red leg is inventoried and handed to round 4"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "a WINDOWS.md row is never hand-edited"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): untouched by this plan; the go in plan 33-32 needs this plan's push (HEAD == origin/main at the go) and reads this plan's verdict in its checkpoint text."
    - "CAP-02 (edge row unresolved, flagged): resolved to `success` or `failure` by this plan's pushed run and nothing else."
    - "CAP-03 (edge row unresolved, flagged): untouched by this plan."
---

<objective>
Gap-closure round 3 of the phase's four-round cap. This is the CAP-02 measurement: one pushed CI
run, read from its own metadata, after every fix plan of this round has landed (33-24 closes the
one class the windows leg was red on; 33-25 through 33-30 rebuild modules both legs exercise). It
is its own plan because a measurement and a fix never share one (33-09's and 33-20's shape;
D-11).

Gap mapping (33-VERIFICATION.md CAP-02 `missing[1]`): "A third pushed CI run whose own conclusion
field reads success on both legs." → Task 3, behind Task 2's human push. Task 1 files the
pre-push inventory (Part 4 § 4.1) so the run decides something already predicted.

Decisions honored: D-13 (both legs), D-14/D-16 (no conditional; a red is a measurement), D-11
(record, diagnose, the next round fixes). Sequencing (round-3 closure target 8): this push
precedes the go plan; the human pushes, never the executor; user policy keeps commits local until
a push is asked for by name.

Purpose: turn "one class, one authority, every arm" into a conclusion word from the runner that
decides CAP-02 and, with it, whether the flip's second gate (F38-F42) can be met this round.
Output: 33-CI-MEASUREMENT.md Part 4 (pre-push inventory, the run, the verdict, the
dispositions), the CAP-02 requirement row, and the ledger rows a green run answers.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
@.planning/phases/33-live-capture-windows-portability/33-20-PLAN.md
@.planning/phases/33-live-capture-windows-portability/33-20-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-24-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The pre-push inventory — every workflow command, the 35 row-236 titles, the falsifier, before the push</name>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` Part 3 (§ 3.1 the workflow-command table shape and the per-row measurability column; § 3.3 the 35-row table N-1..N-34/W-20/W-21 with the exact case titles; § 3.5 the transcript-extraction note).
    - `.planning/phases/33-live-capture-windows-portability/33-24-SUMMARY.md` — the 35-row hand-off table (this host's result per title, `unmeasured locally, by construction` on every row) and decision D-33-R3-01.
    - `.github/workflows/ci.yml` — the step order and which steps are ubuntu-scoped.
    - The summaries of 33-25 through 33-30 — each names the modules it rebuilt and the gates it ran; nothing there claims a CI leg.
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md</files>
  <action>
    Append `## Part 4 — round 3: the pre-push inventory (Task 1)` to the measurement document.
    Run every workflow command locally in the workflow's order (the Part 1 § 1.1 list: install,
    freshness, build + parity, typecheck, platform shapes, the excluded-lane vitest run, the chained
    repository gates) and record each with its exit status. Then the row-236 table: the 35 titles
    of Part 3 § 3.3, each with the closing plan (33-24), the mechanism in one clause (one
    authority: `tmp15` and the two module spellers through `canonicalDirectoryPath`), this host's
    result from running the title, and the measurability column — every row `unmeasured locally,
    by construction` for the 8.3 axis, with the sentence that darwin has no short names. Add one
    row per round-3 plan that rebuilt a module (33-25..33-30) naming the module and stating that
    its class is `measurable here` (no windows-only axis) with its local suite result.

    State the expectation the run will decide, one sentence per leg, before the push — ubuntu:
    green on every step as on run 35579263776; windows: green on every step, vitest 0 failed, the
    35 row-236 cases green — and the falsifier: any red on either leg is this round's finding; a
    red among the 35 is a spelling authority 33-24 left one arm over; a red anywhere else is a
    round-3 rebuild that moved a windows arm (attribute it to the plan by module).

    Record HEAD's sha on one line in exactly the form `pre-push HEAD sha: ` followed by the
    40-character sha (plan 33-31's verify block extracts it by that literal), and
    `git log --oneline origin/main..HEAD | wc -l` (the commit count the push carries — every
    round-3 commit plus the round-2 documentation commits still local). Do not push.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any command, or the vitest summary reports a failed test file</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness &amp;&amp; npm run freshness:hook-manifest &amp;&amp; npm run freshness:adapters &amp;&amp; npm run freshness:guarantees</automated>
    <fails_when>non-zero exit from any command</fails_when>
    <automated>grep -a -c '^## Part 4' .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md</automated>
    <fails_when>a count other than 1</fails_when>
    <automated>grep -a -c 'unmeasured locally, by construction' .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md</automated>
    <fails_when>a count below 35 more than the count on the dispatch base — the row-236 table does not carry the column on every one of its 35 rows</fails_when>
  </verify>
  <acceptance_criteria>
    - Part 4 § 4.1 lists every workflow command with its local exit status, the 35-row row-236 table with the measurability column filled, and one row per rebuilt module.
    - The expectation per leg and its falsifier are written before the push; HEAD's sha and the carried commit count are recorded.
    - No source file is changed by this task: `git diff --stat TASK_BASE_SHA..HEAD -- scripts install hooks agent-factory .claude` is empty, with both shas quoted; the `pre-push HEAD sha:` line is present in § 4.1.
  </acceptance_criteria>
  <done>The run will confirm or refute a prediction that is already on disk, on every row.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Checkpoint — named human confirmation to push, so both legs can be measured (push one of two on the go path)</name>
  <what-built>
    Round 3's fix plans (33-24 through 33-30) are committed locally and every local gate is green:
    build, working-tree parity, freshness (adapters, hook manifest, guarantees), typecheck, the
    platform-shape differential, the excluded-lane vitest suite, and the chained repository gates.
    The pre-push inventory is filed as Part 4 § 4.1 of
    `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md`, with the 35
    row-236 titles and the one axis this machine cannot measure named on every row.

    What remains cannot be measured here: CAP-02's bar is both matrix legs exiting 0 on a pushed
    run (D-13). This push is also the FIRST of the two pushes the go path needs: plan 33-32's
    runner refuses unless HEAD equals the remote head, and that plan places its own push
    checkpoint immediately before the go for the documentation commits that land in between.
  </what-built>
  <how-to-verify>
    This checkpoint asks for a named human confirmation to push the current head to the default
    branch — this project holds merge and deploy with the human, mechanically rather than by prose.

    Before you answer:
    1. Read Part 4 § 4.1 — the 35 `unmeasured locally, by construction` rows are what the run
       will actually decide.
    2. Run `git log --oneline origin/main..HEAD` to see exactly what the push carries (every
       round-3 commit plus the round-2 documentation commits kept local by your policy).
    3. Note the cost: CI minutes only, zero tokens. This is NOT the live-capture go; that is plan
       33-32's separate, money-spending checkpoint.

    On confirmation the executor waits for the run to complete and reads both legs' `conclusion`
    fields. A red leg is a measurement and becomes round 4's input; it is never answered with a
    platform conditional and never fixed-and-re-pushed inside this plan.
  </how-to-verify>
  <resume-signal>Type "push approved" to authorize the executor's push, or "I pushed" if you pushed it yourself, or describe what you want changed first</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Read both legs from the run's own metadata; file the measurement; dispose only what a green run answers</name>
  <precondition>The head under test is pushed to the default branch (the checkpoint above was answered and the push completed: `git rev-parse HEAD` equals `git rev-parse origin/main` after `git fetch`), so a CI run exists for exactly that sha.</precondition>
  <read_first>
    - Part 4 § 4.1 as written by Task 1 (the prediction and its falsifier).
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` Part 3 § 3.2 (the exact `gh run view` field list, the per-step table shape, the suite-totals table with the control-byte scan note), § 3.3 (the inventory shape and the four labels), § 3.4 (the ledger commands used, verbatim shapes).
    - `.planning/WINDOWS.md` header (the tool that owns its three representations) and rows 186, 193, 226-235, 236.
    - `.planning/REQUIREMENTS.md` row CAP-02 (L141 checkbox line and the coverage-table row L227).
    - `.planning/phases/33-live-capture-windows-portability/33-20-PLAN.md` Task 3 — the disposition rule for rows 186 (fixed, citing the confirming run ids: 35394268365, 35499800942, 35579263776 and this run) and 193 (waived, with D-16's reason and the revisit condition).
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md, .planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <action>
    Locate the run for the pushed sha with `gh run list --branch main --limit 5 --json databaseId,headSha,conclusion,createdAt`
    and wait for it with `gh run watch <id>` (or poll `gh run view <id> --json status`). Then read
    `gh run view <id> --json databaseId,headSha,status,conclusion,createdAt,updatedAt,jobs` and
    write Part 4 § 4.2 in Part 3 § 3.2's table shape: run id, head sha, created/completed, run
    conclusion, and for each of `test (ubuntu-latest)` and `test (windows-latest)` the job id and
    its `conclusion` field QUOTED, the per-step table, and the suite totals from each leg's own
    vitest summary block (ANSI stripped; control bytes scanned before quoting, as § 3.2 records).
    Every word in those tables is copied from the JSON or the log; the summary states the
    commands used.

    If BOTH legs read `success`: § 4.3 states `CAP-02 verdict on this run: MET` with the run id;
    append the per-row column to § 4.1's 35-row table (each `CLOSED on run <id>`); flip the
    CAP-02 row in `.planning/REQUIREMENTS.md` (`- [x] **CAP-02**` with the run id and date, and
    the coverage-table row to `Complete — CI run <id> (head <sha>, <date>): success on both legs; evidence 33-CI-MEASUREMENT.md Part 4`)
    — this row is NOT in the flip manifest's declared set, so editing it here does not touch the
    flip commit's rule; then dispose the ledger rows through the tool only:
    `windows fixed 236` citing the run id and plan 33-24 (decision D-33-R3-01), `windows fixed 229`
    … `windows fixed 235` each with a reason citing the run id and the closing plan (as 33-20's
    green branch named them), `windows fixed 186` citing the four green measurements of
    `scripts/board-watch-live.test.ts` by run id, and
    `windows waive 193 "<D-16: the CI-topology half is not taken; revisit only if a Windows watcher red proves undiagnosable inside the shared step>"`.
    Quote every command in § 4.4 and run `windows status` afterwards, quoting the counters and
    confirming the table row count and the JSON appendix agree.

    If EITHER leg is red: § 4.3 states `CAP-02 verdict on this run: NOT MET`; re-derive the failure
    inventory from that leg's own log (`gh api repos/abitwise/grugops/actions/jobs/<job id>/logs`,
    ANSI stripped, control bytes scanned before quoting) in § 3.3's shape — per file, per case,
    the assertion text, and a label from the same four — naming for each red which round-3 plan's
    mechanism it falsifies (a row-236 title → 33-24; another file → the plan that rebuilt that
    module); update the CAP-02 coverage-table row to `Pending — NOT met: CI run <id> ...` with the
    counts and `round 3 of 4`; do NOT flip any ledger row (rows 226-236 stay open; append new
    rows through the tool for any NEW class, one per class, as § 3.4 did). No fix in this plan;
    round 4 is the last under the cap and the summary says so.

    Either way, plan 33-32 reads § 4.3 as its checkpoint text.
  </action>
  <verify>
    <automated>gh run view "$(gh run list --branch main --limit 1 --json databaseId --jq '.[0].databaseId')" --json headSha,conclusion,jobs --jq '{headSha, conclusion, jobs: [.jobs[] | {name, conclusion}]}'</automated>
    <fails_when>non-zero exit, or the printed headSha differs from the sha Part 4 § 4.1 recorded, or any job's conclusion is null (the run is not complete)</fails_when>
    <automated>grep -a -cE 'CAP-02 verdict on this run: (MET|NOT MET)' .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md</automated>
    <fails_when>a count other than 3 — Parts 2 and 3 carry one verdict line each and Part 4 must carry exactly one more</fails_when>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status</automated>
    <fails_when>non-zero exit, or the counters disagree with the table's row count — a row was hand-edited</fails_when>
    <automated>S=$(grep -a -oE 'pre-push HEAD sha: [0-9a-f]{40}' .planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md | tail -1 | grep -a -oE '[0-9a-f]{40}') &amp;&amp; test -n "$S" &amp;&amp; D=$(git diff --stat "$S..HEAD" -- scripts install hooks agent-factory .claude) &amp;&amp; test -z "$D"</automated>
    <fails_when>non-zero exit — the `pre-push HEAD sha:` line is absent from Part 4 § 4.1, the diff could not be computed, or a source file changed after that sha (a fix inside the measurement plan)</fails_when>
  </verify>
  <acceptance_criteria>
    - Part 4 § 4.2 quotes the run id, head sha, run conclusion and BOTH jobs' `conclusion` fields from `gh run view --json`; the command is stated; the suite totals are quoted from the logs.
    - § 4.3 carries exactly one verdict line; on MET the CAP-02 requirement row is flipped with the run id and rows 226-235, 236, 186 (fixed) and 193 (waived) are disposed through the tool with the commands quoted in § 4.4; on NOT MET the red inventory is re-derived in § 3.3's shape with per-red falsified-mechanism attribution, the round-4 hand-off is stated, and no ledger row is flipped.
    - No source file changed in this plan (the diff over `scripts install hooks agent-factory .claude` between the recorded pre-push sha and HEAD is empty).
    - `windows status` counters agree with the table and the JSON appendix.
  </acceptance_criteria>
  <done>CAP-02 has a conclusion word from a runner, quoted, and the ledgers say exactly what it supports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a named human confirmation -> a push to the default branch | The only place this plan changes anything outside the working tree |
| a CI run's JSON -> the requirement ledger | A narrated conclusion would be a fabricated gate result |
| a red leg -> the temptation to fix inside the measurement | Fix-and-re-push inside one plan erases the measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-141 | Elevation of privilege | an unconfirmed push | critical | mitigate | Task 2 is a blocking human checkpoint; Task 3's precondition names it and re-checks HEAD == origin/main. |
| T-33-142 | Repudiation | a narrated CI conclusion | high | mitigate | Task 3 quotes `gh run view --json` fields; the verify command re-reads them. |
| T-33-143 | Tampering | fix-and-re-push inside the round | high | mitigate | Prohibited in must-haves; the verify block asserts no source diff after the pre-push sha; the red branch inventories and hands to round 4. |
| T-33-144 | Tampering | the ledger's three representations | medium | mitigate | Rows change only through the tool; `windows status` asserted afterwards. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; the run's `npm ci` installs dev deps from the committed lockfile only. |
</threat_model>

<verification>
- `gh run view <id> --json conclusion,jobs` — both legs' conclusions quoted in Part 4 § 4.2.
- `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status` — representations agree.
- `git diff --stat PRE_PUSH_SHA..HEAD -- scripts install hooks agent-factory .claude` — empty; no source touched.
</verification>

<success_criteria>
- One pushed run, both legs' conclusions quoted; CAP-02 flips only on `success` for both.
- On green, rows 226-235, 236, 186 and 193 disposed through the tool; on red, the inventory is round 4's input and nothing is fixed here.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `33-CI-MEASUREMENT.md` Part 4 (§ 4.1-4.4) | measurement record | pre-push inventory, the run, the verdict, the dispositions |
| `CAP-02 verdict on this run: MET` or `... NOT MET` | verdict line | exactly one in Part 4 |
| REQUIREMENTS.md CAP-02 row | ledger change | flipped only on both-legs `success` |
| WINDOWS.md rows 226-235, 236, 186 (fixed), 193 (waived) | ledger changes via tool | green branch only |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-31-SUMMARY.md` when done
</output>

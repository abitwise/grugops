---
phase: 33-live-capture-windows-portability
plan: 18
type: execute
wave: 7
depends_on: ["33-02", "33-05", "33-09"]
files_modified:
  - scripts/uat-gate-exit-contract.test.ts
  - scripts/check-platform-shapes.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`scannedDocuments()` in `scripts/uat-gate-exit-contract.test.ts` publishes POSIX members through `scripts/posix-path.ts`, so the `docs/audit/` filter and the `split(\"/\")` key see the same spelling on every host (D-15)."
    - "The explicit `60_000` per-test bound on `the watched corpus is not narrowed — the gate reports its own cardinality, unchanged at 40` is REMOVED, so D-14's global `testTimeout` and `slowTestThreshold` govern it: slow is flagged, never failed, never platform-conditional."
    - "The per-line remainder assertion derives the admissible shape names from the module's own corpus (`SHAPES` plus `HOST_CAPABILITIES`), not from the literal `FIFO`, so a remainder widened by measured host capabilities is still checked line by line."
    - "The `COVERAGE` case's undriven label set is DERIVED: the two disclosed labels plus every label whose mirror is gated on a host capability the module measured ABSENT on this host — no `process.platform`, no hand-widened list."
  artifacts:
    - scripts/uat-gate-exit-contract.test.ts
    - scripts/check-platform-shapes.test.ts
  key_links:
    - "uat-gate-exit-contract.test.ts `scannedDocuments()` -> `toPosix(join(...))` -> the `docs/audit/` filter and the `split(\"/\")` ordinal key"
    - "uat-gate-exit-contract.test.ts per-line remainder loop -> `SHAPES`/`HOST_CAPABILITIES` names (check-platform-shapes.ts)"
    - "check-platform-shapes.test.ts COVERAGE -> `hostCapabilityOrSkip(\"signal-terminated child\", ...)` -> the derived undriven set"
  prohibitions:
    - "no platform conditional closes a CAP-02 red (D-14/D-16)"
    - "a per-test timeout below the global bound is never reintroduced on a case whose subject is a gate that grows with the tree (D-14)"
    - "no hand-typed set stands where the set can be derived and its cardinality asserted"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): untouched by this plan."
    - "CAP-02 (edge row unresolved, flagged): this plan closes five windows reds (W-12..W-16) by mechanism; the leg's conclusion is measured only by plan 33-20."
    - "CAP-03 (edge row unresolved, flagged): untouched by this plan."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). Five windows reds across two test modules: four in
`scripts/uat-gate-exit-contract.test.ts` (two mis-attributed survivors, two created by this
phase's own changes — rows 231, 232) and the one-arm-over `COVERAGE` case in
`scripts/check-platform-shapes.test.ts` (row 233).

Gap mapping (33-VERIFICATION.md CAP-02 `missing[1]`'s "incomplete fixes" and the
33-CI-MEASUREMENT.md § 2.4 rows):
- Task 1: W-15 `the scanned document set is DERIVED and its cardinality is asserted, so a short scan is red`; W-16 `every ordinal-claiming sentence in the scanned set appears in the tracked list`.
- Task 2: W-13 `the watched corpus is not narrowed — the gate reports its own cardinality, unchanged at 40` (timed out at its own 60 000 ms argument, 61 989 ms measured); W-14 `NON-EMPTY on a platform lacking a shape, and each entry names the shape AND the platform` (the per-line `shape="FIFO"` literal under a remainder now carrying three host-capability rows).
- Task 3: W-12 `COVERAGE: every label was WATCHED live except the two this platform cannot stage` (`signalled` unwatched on win32 because 33-05 gated the mirror on the measured `signal-terminated child` capability).

Decisions honored: D-13, D-14 (the human's rule: "add warning, don't prevent test run"; derive-vs-remove
is decided here as REMOVE for the explicit bound, because the global bound already governs and a
gate that grows with the tree has no fixed duration), D-15, D-16 (the remainder is measured, and
the assertion follows what was measured).

Purpose: the assertions in these two files stop pinning a spelling or a shape the platform
decides, and pin the relationship instead.
Output: the two test modules corrected, with the consuming arm named per case.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
@.planning/phases/33-live-capture-windows-portability/deferred-items.md
@.planning/phases/33-live-capture-windows-portability/33-05-SUMMARY.md
@scripts/check-platform-shapes.ts
@scripts/posix-path.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The scanned document set is published in POSIX (W-15, W-16)</name>
  <read_first>
    - `scripts/uat-gate-exit-contract.test.ts` ~L445-458 (`scannedDocuments()`: `join(PHASE_DIR, f)` / `join(AUDIT_DIR, f)`), ~L486-495 (`d.includes(\`${"docs"}/audit/\`)`), ~L500-521 (the ordinal scan and its `doc.split("/").pop()` key).
    - `scripts/posix-path.ts` — `toPosix`, `toPosixWith`.
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 rows W-15 (`expected 0 to be greater than 0` — `audits.length`) and W-16 (nine `D:\\a\\grugops\\...\\31-NN-SUMMARY.md claims "eighth"` entries).
  </read_first>
  <files>scripts/uat-gate-exit-contract.test.ts</files>
  <behavior>
    - Test AE (RED first, separator seam): `scannedDocumentsWith(win32)` (the same derivation given `path.win32` for its joins) returns members that contain no backslash and all satisfy `m.includes("docs/audit/") || m.endsWith("-SUMMARY.md")`; the audit subset is non-empty.
    - W-15 and W-16 pass on this host unchanged in their assertions.
  </behavior>
  <action>
    `scannedDocuments()` is a published set (its members are printed in failure messages and keyed
    by `split("/")`), so it normalizes at its one member-forming site: map each joined path through
    `toPosixWith(p, api.sep)` where `api` is an injected `{ join, sep }` defaulting to `node:path`
    (import `toPosixWith` from `./posix-path.js`). Expose the injected form as
    `scannedDocumentsWith(api)` for the seam and keep `scannedDocuments()` as the host-bound
    wrapper — the `scanKey` / `toPosix` shape 33-03 established. Reads (`readFileSync`) accept the
    POSIX spelling on win32, so nothing else changes. Test AE drives `path.win32`.

    Probe every arm: the `audits` filter, the `summaries` filter, the ordinal key, and any other
    consumer of `scannedDocuments()` in the file (`grep -n 'scannedDocuments()'`), each named in the
    summary as reading the POSIX member now.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/uat-gate-exit-contract.test.ts -t "scanned document set is DERIVED|every ordinal-claiming sentence|scannedDocumentsWith"</automated>
    <fails_when>non-zero exit, or fewer than 3 tests reported passed, or 0 passed</fails_when>
    <automated>grep -a -c 'from "./posix-path.js"' scripts/uat-gate-exit-contract.test.ts</automated>
    <fails_when>a count of 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `scannedDocumentsWith(api)` exists, `scannedDocuments()` calls it with `node:path`, and every member is normalized through `toPosixWith` at the one push site.
    - Test AE exists; the RED commit precedes the GREEN commit (shas quoted).
    - The summary names every consumer of the set in the file.
  </acceptance_criteria>
  <done>One spelling of the scanned set, on every host.</done>
</task>

<task type="auto">
  <name>Task 2: The explicit bound goes; the per-line shape check derives its admissible names (W-13, W-14)</name>
  <read_first>
    - `scripts/uat-gate-exit-contract.test.ts` ~L575-600 (the `unchanged at 40` case and its trailing `}, 60_000)` argument), ~L773-786 (the per-line loop asserting `shape="FIFO"`), `printedRemainder` (~L725), the `SKIPPED SHAPES` grammar the gate prints.
    - `vitest.config.ts` — `testTimeout: 180_000`, `slowTestThreshold: 5000` (33-02, D-14).
    - `scripts/check-platform-shapes.ts` — `SHAPES` (~L435, each with `name`), `HOST_CAPABILITIES` (~L611, each with `name`), `CAPABILITY_POSITION`, the printed line grammar `shape="<name>" position="<label>" platform=<p> ...`.
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.3 (the windows remainder: two FIFO rows plus three host-capability rows: `chmod 000 enforcement`, `signal-terminated child`, `control byte in a path component`), § 2.4 rows W-13, W-14, § 2.6 (61 989 ms against a 60 000 ms argument; the gate under test grew with the disposition corpus).
    - `.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md` D-14.
  </read_first>
  <files>scripts/uat-gate-exit-contract.test.ts</files>
  <action>
    W-13 — derive-vs-remove, decided: REMOVE the `60_000` argument. D-14's mechanism is the global
    bound plus the slow-test warning; the case's subject (`check-diff-disposition`) grows with
    every disposition file this repository adds, so no fixed number is its duration, and the
    baseline measurement (56 481 ms windows) sat 3.5 s under the argument before this phase added
    twenty rows. Record the decision and the two measurements in a comment above the case. Do not
    replace it with a derived number: the derivation would be "the global bound", which is already
    in force.

    W-14 — the per-line loop must accept every shape the module can print. Build the admissible set
    from the module: `new Set([...SHAPES.map((s) => s.name), ...HOST_CAPABILITIES.map((c) => c.name)])`
    (import both from `./check-platform-shapes.js`), and assert each printed line's `shape="..."`
    value is a member, plus `position="` and `platform=${process.platform}` as before. Keep the
    FIFO-specific expectation where it belongs — the preceding case already pins that FIFO rows
    exist at every published position under the seam. Assert the set is non-empty and larger than
    one (a vacuity floor on the derived set).

    Probe every arm: run the whole `SKIPPED SHAPES` describe block and the `REQUIRE_SKIPS` cases;
    run `npm run check:platform-shapes` and quote the remainder line on this host.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/uat-gate-exit-contract.test.ts</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
    <automated>grep -a -c '}, 60_000)' scripts/uat-gate-exit-contract.test.ts</automated>
    <fails_when>a count above 0 — the explicit bound survived</fails_when>
    <automated>grep -a -c 'HOST_CAPABILITIES' scripts/uat-gate-exit-contract.test.ts</automated>
    <fails_when>a count of 0 — the admissible shape set is not derived from the module</fails_when>
  </verify>
  <acceptance_criteria>
    - The `unchanged at 40` case carries no per-test timeout argument; the comment above it records the decision and both measurements.
    - The per-line loop's shape check reads the module's `SHAPES` and `HOST_CAPABILITIES` names; the summary quotes the loop.
    - `npm run check:platform-shapes` exits 0 and its remainder line is quoted.
  </acceptance_criteria>
  <done>The bound is D-14's, and the shape check follows the corpus it measures.</done>
</task>

<task type="auto">
  <name>Task 3: The COVERAGE case derives its undriven set from the measured capabilities (W-12)</name>
  <read_first>
    - `scripts/check-platform-shapes.test.ts` ~L842-868 (`COVERAGE`: `DISCLOSED_UNDRIVEN` pins two labels), ~L750-770 (the `signals-itself` mirror gated by `hostCapabilityOrSkip("signal-terminated child", ...)`, its label `NOT ORDINARY (signalled)`), `probeExports()` (~L392).
    - `scripts/check-platform-shapes.ts` — `hostCapabilityOrSkip`, `HOST_CAPABILITIES` and their `probe(scratch)`; whether a "measure this capability" function is exported for tests (read; if only `hostCapabilityOrSkip` exists, its null/non-null answer IS the measurement).
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 row W-12 (`expected [ 'NOT ORDINARY (answered)', …(2) ] to deeply equal [ …(1) ]` — the third undriven label is `signalled`) and WINDOWS.md row 233.
  </read_first>
  <files>scripts/check-platform-shapes.test.ts</files>
  <action>
    Replace the two-element literal with a derivation: `expectedUndriven = [...DISCLOSED_UNDRIVEN, ...gatedByAbsentCapability]`
    where `gatedByAbsentCapability` is computed from ONE table declared beside the mirrors —
    `MIRROR_CAPABILITY_GATES: { label: "NOT ORDINARY (signalled)", capability: "signal-terminated child" }[]`
    — filtered by whether `hostCapabilityOrSkip(capability, position)` returns a skip entry on
    this host (the same measurement the mirror case itself takes, so the two cannot disagree).
    Assert the table's `capability` names are members of `HOST_CAPABILITIES` (derive-the-set) and
    that every gated mirror kind in the file appears in the table (grep the file's own
    `hostCapabilityOrSkip(` calls in a test-side census, compare counts). On darwin the capability
    is present, so the derived set is the two disclosed labels and the case is unchanged; on win32
    it grows by exactly the label the skip removed.

    Probe every arm: the `COVERAGE: every mirror kind the module publishes was driven` case that
    follows — a skipped mirror must still count as "driven by a case" there or that case moves
    one arm over; read it and record whether it reads the skip row.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-platform-shapes.test.ts -t "COVERAGE"</automated>
    <fails_when>non-zero exit, or fewer than 2 tests reported passed, or 0 passed</fails_when>
    <automated>grep -a -c 'MIRROR_CAPABILITY_GATES' scripts/check-platform-shapes.test.ts</automated>
    <fails_when>a count below 2 — the table is not declared and consumed</fails_when>
  </verify>
  <acceptance_criteria>
    - `MIRROR_CAPABILITY_GATES` exists, its capability names are asserted to be `HOST_CAPABILITIES` members, and the COVERAGE case derives its undriven set from it through `hostCapabilityOrSkip`.
    - The census of `hostCapabilityOrSkip(` call sites in the file is compared with the table's size in a test.
    - No `process.platform` read was added; the summary quotes the derived set on this host.
  </acceptance_criteria>
  <done>The undriven set follows the host's measured capabilities, not a list written on the host that has them all.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a derived scan set -> the ordinal-claim gate | A host-spelled key makes the vacuity floor read zero and the claim scan match nothing |
| a per-test bound -> a gate that grows with the tree | A fixed number turns growth into a red the human asked to be a warning |
| a literal shape/label list -> a measured remainder | A widened remainder or a gated mirror moves the case one arm over |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-86 | Repudiation | ordinal-claim scan reading zero on win32 | medium | mitigate | Task 1: POSIX members at the one push site; the seam proves it on darwin. |
| T-33-87 | Denial of service | a fixed bound under a growing gate | medium | mitigate | Task 2: the argument removed; D-14's global bound and warning govern; the decision recorded. |
| T-33-88 | Repudiation | a literal list that hides a skipped arm | medium | mitigate | Tasks 2 and 3: admissible shapes and undriven labels derived from the module and the host measurement; censuses compared. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run check:platform-shapes` — exit 0, remainder line quoted.
</verification>

<success_criteria>
- W-12..W-16 are closed by derivation, with the consuming arms named; no conditional, no literal set.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `scannedDocumentsWith(api)` | test helper, `scripts/uat-gate-exit-contract.test.ts` | the injected-separator derivation |
| the removed `60_000` argument + decision comment | test change | D-14 |
| derived admissible shape set in the per-line loop | test change | from `SHAPES` + `HOST_CAPABILITIES` |
| `MIRROR_CAPABILITY_GATES` | test constant, `scripts/check-platform-shapes.test.ts` | the one table gating mirrors on capabilities |
| Test AE + the census cases | new test cases | red-first where the host can show the red |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-18-SUMMARY.md` when done
</output>

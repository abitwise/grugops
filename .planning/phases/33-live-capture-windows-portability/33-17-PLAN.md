---
phase: 33-live-capture-windows-portability
plan: 17
type: execute
wave: 7
depends_on: ["33-02", "33-04", "33-09"]
files_modified:
  - scripts/runnable-ref/uat-spec-integrity.ts
  - scripts/runnable-ref/uat-spec-integrity.js
  - scripts/runnable-ref/uat-spec-integrity.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-02]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`parseFaults` is keyed and looked up through ONE key-forming function that spells both the compiler host's `fileName` and the checker's `join(repoRoot, rel)` the same way on every host, so a parse fault recorded on win32 is found and its diagnostic (`Maximum call stack size exceeded`) is printed instead of the fallback sentence (D-15; the module defect WINDOWS.md row 227 names)."
    - "POINT 2's test-internal comparison asks the Program for its files under the same key-forming rule, not through a host-joined literal."
    - "The two ubuntu-only reds (GREEN 1b, ORDERING) have a diagnosis that names a cause class, the evidence, and the observation that would distinguish it from the next class, written BEFORE the fix (D-11's shape, applied to a CI red)."
    - "The mixed-arrangement cases plant a depth DERIVED from the measured boundary with a margin large enough that a small per-arrangement stack-baseline shift cannot flip the outcome, and the derivation is explained beside the constant — no hard-coded depth, no platform conditional (D-14)."
  artifacts:
    - scripts/runnable-ref/uat-spec-integrity.ts
    - scripts/runnable-ref/uat-spec-integrity.js
    - scripts/runnable-ref/uat-spec-integrity.test.ts
  key_links:
    - "uat-spec-integrity.ts `faultKey(path)` -> `parseFaults.set(faultKey(fileName))` and `parseFaults.get(faultKey(join(repoRoot, rel)))`"
    - "uat-spec-integrity.test.ts `parseBoundary()` -> `mixedArrangementDepth(boundary)` -> GREEN 1b, ORDERING, PROBE 2 (b), and every other two-file arrangement"
  prohibitions:
    - "no platform conditional closes a CAP-02 red (D-14/D-16)"
    - "a nesting depth is never hard-coded; every depth derives from the measured boundary on the host that runs the case"
    - "a diagnosis is written before the fix, and names what would distinguish its cause class from the next"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): untouched by this plan."
    - "CAP-02 (edge row unresolved, flagged): this plan closes the two windows reds in this file by mechanism read from the source, and the two ubuntu reds by a derived margin whose mechanism is `UNKNOWN - verify` until the pushed run in plan 33-20 measures it."
    - "CAP-03 (edge row unresolved, flagged): untouched by this plan."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). Four reds live in `scripts/runnable-ref/uat-spec-integrity*.ts` on CI run
35499800942: two windows cases the round-1 plans never took (row 227) and the ONLY two ubuntu
reds on the whole run (row 230), which D-13 makes as blocking as any windows red.

Gap mapping (33-VERIFICATION.md CAP-02):
- `missing[1]` "... the 2 NOT-ADDRESSED uat-spec-integrity.test.ts cases ..." → Task 1: W-30 `GREEN 1: a spec the parser cannot finish exits 2 with the vacuity floor on stderr` (the module's `parseFaults` keying, L1359 set / L3237 get) and W-31 `POINT 2: the Program's included files are a SUPERSET of the derived spec set` (the test-internal `join` comparison).
- `missing[2]` "Diagnosis and fix for the 2 new ubuntu-only reds in uat-spec-integrity.test.ts (GREEN 1b, ORDERING) ..." → Task 2 (diagnosis, zero fabrication) then Task 3 (fix): U-1 `GREEN 1b: the pathological spec alongside a clean one exits 2 with `visited 1 of 2` on stderr` and U-2 `ORDERING: the per-file could-not-run reason precedes reportMeasured's output`.

Decisions honored: D-13 (both legs), D-14 (no platform conditional; a derived bound with headroom
is not one), D-15 (one key, formed once), D-11 (diagnose before fixing; name the distinguishing
observation).

Purpose: the lost diagnostic is found on every host, and the two-file cases stop sitting on a
zero-margin boundary.
Output: the module + committed `.js`, the diagnosis note in the plan summary, and the test file
with derived depths.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md
@.planning/phases/33-live-capture-windows-portability/deferred-items.md
@.planning/phases/33-live-capture-windows-portability/33-02-SUMMARY.md
@scripts/posix-path.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One fault key for the compiler host and the checker; POINT 2 asks under the same rule (W-30, W-31)</name>
  <read_first>
    - `scripts/runnable-ref/uat-spec-integrity.ts` ~L1345-1365 (`parseFaults.set(fileName, ...)` inside the wrapped `host.getSourceFile`), ~L3233-3242 (`ctx.program.getSourceFile(join(repoRoot, rel))` and `ctx.parseFaults.get(join(repoRoot, rel))`), the `specAbsPaths` formation, and the module's existing imports (does it already import from `../posix-path.js`? — read before adding).
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` ~L6702-6730 (GREEN 1: expects `could not be PARSED (Maximum call stack size exceeded)`), ~L8871-8898 (POINT 2: `included.has(join(root, rel))` over the Program's `fileName`s).
    - `scripts/posix-path.ts` — `toPosix`/`toPosixWith`.
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 rows W-30, W-31; `deferred-items.md` item "Two windows reds in scripts/runnable-ref/uat-spec-integrity.test.ts are NOT ADDRESSED".
  </read_first>
  <files>scripts/runnable-ref/uat-spec-integrity.ts, scripts/runnable-ref/uat-spec-integrity.js, scripts/runnable-ref/uat-spec-integrity.test.ts</files>
  <behavior>
    - Test AA (RED first): `faultKey("D:\\a\\repo\\e2e\\uat\\x.uat.spec.ts", win32.sep)` equals `faultKey("D:/a/repo/e2e/uat/x.uat.spec.ts", win32.sep)`; on the host separator `faultKey(p)` is `toPosix(p)`.
    - Test AB (the map round-trips through the key on a win32-spelled pair): a `Map` keyed by `faultKey(hostFileName, win32.sep)` is found by `faultKey(win32.join(root, rel), win32.sep)` — driven with `path.win32` so the Windows spelling is exercised on darwin.
    - Test AC (POINT 2 under the rule): the Program's `fileName` set and the derived rel paths are compared through `faultKey` on both sides; the case still passes and no longer joins with the host separator on only one side.
  </behavior>
  <action>
    Add to `scripts/runnable-ref/uat-spec-integrity.ts` the one key-forming function
    `export function faultKey(path: string, separator: string = sep): string` returning
    `toPosixWith(path, separator)` (import from `../posix-path.js`; the module sits one directory
    down), with a docblock in `scanKey`'s shape: THE ONE PLACE a parse-fault key is formed; the
    compiler host reports `fileName` with forward slashes on every host, the checker composes with
    `join` (host separator), and before this function the two never met on win32 — the fault was
    recorded and never found, so the fallback sentence was printed for a file whose real
    diagnostic existed. Apply it at BOTH sites: `parseFaults.set(faultKey(fileName), ...)` and
    `ctx.parseFaults.get(faultKey(join(repoRoot, rel)))`. Leave `ctx.program.getSourceFile(join(repoRoot, rel))`
    as it is — the compiler canonicalises its own argument, and the CI log shows that lookup
    succeeding on win32 (the file was found; only the fault was not).

    In POINT 2, compare `included` and `derived` through `faultKey` on both sides (the Program's
    `fileName`s mapped through it into the `Set`, and `faultKey(join(root, rel))` on the lookup).

    Tests AA, AB, AC RED before GREEN. Rebuild so the committed `.js` moves in the same commit
    (`scripts/runnable-ref/uat-spec-integrity.js` is the twin; confirm with `npm run check:build-parity`).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts -t "faultKey|POINT 2: the Program's included files|GREEN 1: a spec the parser cannot finish"</automated>
    <fails_when>non-zero exit, or fewer than 4 tests reported passed, or 0 passed</fails_when>
    <automated>grep -a -c 'parseFaults.get(faultKey(' scripts/runnable-ref/uat-spec-integrity.ts</automated>
    <fails_when>a count of 0 — the lookup still uses the host-joined key</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npx tsc --noEmit</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/runnable-ref/uat-spec-integrity.ts` exports `faultKey` and uses it at both the set and the get site; it imports from `../posix-path.js`.
    - POINT 2 compares through `faultKey` on both sides.
    - Tests AA-AC exist; the RED commit precedes the GREEN commit (shas quoted).
    - GREEN 1 still passes on this host with the `Maximum call stack size exceeded` diagnostic named.
  </acceptance_criteria>
  <done>The fault recorded by the host is the fault the checker prints, on every host.</done>
</task>

<task type="auto">
  <name>Task 2: Diagnose the two ubuntu reds at zero fabrication — cause class, evidence, distinguishing observation</name>
  <read_first>
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` ~L6626-6700 (`nestedSpec`, `parseBoundary()`: the bisection runs ONE spec per program), ~L6740-6770 (GREEN 1b: clean + nested), ~L7070-7095 (ORDERING: solo then mixed), every other `nestedSpec(overflow)` site (L6705, L6743, L6767, L7025, L7075, L7086, L7192, L7392, L7707, L7741, L7754, L7764, L7773, L8445, L8718 — classify each as one-file or mixed arrangement).
    - `.planning/phases/33-live-capture-windows-portability/33-CI-MEASUREMENT.md` § 2.4 rows U-1, U-2 (the pathological spec BESIDE a clean one exits 0 on ubuntu; GREEN 1 alone passes on the same leg in 9.9 s; both green on windows on both runs) and § 2.5 (b).
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-15-DIAGNOSIS.md` — the shape of a zero-token diagnosis (cause class, evidence, distinguishing observation).
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` § 1.2 — the same shape applied in this phase.
  </read_first>
  <files>scripts/runnable-ref/uat-spec-integrity.test.ts</files>
  <action>
    Write the diagnosis as a section of the plan summary (the file is not touched by this task
    beyond a measurement helper; the fix is Task 3). State the hypothesis plainly and label it
    `UNKNOWN - verify` where the log cannot confirm it: `parseBoundary()` bisects the overflow
    depth under a ONE-file program and returns an ADJACENT pair (`hi - lo === 1`), so the mixed
    cases run at a depth with ZERO margin; the depth at which a recursive-descent parse exhausts
    the stack depends on the stack already consumed when the compiler reaches that file, and a
    program with a second root file reaches the pathological file through a slightly different
    call path. A shift of a few frames in either direction flips the outcome at an adjacent
    boundary. Which direction the shift takes is a property of the V8 build and the platform's
    frame sizes — which is why ubuntu (node 22) flips and windows (node 22) and darwin (node 24)
    do not, with no platform CONDITION involved.

    Measure on this host, and record the numbers: add a test-only helper
    `parseBoundaryFor(arrangement: Record<string, string>)` that bisects with the given sibling
    files planted beside the nested spec, and run it from a case titled exactly
    `parseBoundaryFor: the one-file and the mixed-arrangement boundaries, measured on this host`
    for `{}` (the one-file arrangement — must equal `parseBoundary()`'s pair) and for
    `{ "e2e/uat/clean.uat.spec.ts": CLEAN_SPEC }` (the mixed arrangement). Record both pairs in
    the summary. If they differ on darwin, the
    mechanism is CONFIRMED (cause class SUITE: a zero-margin boundary used outside the arrangement
    it was measured in). If they are equal on darwin, the mechanism is CONSISTENT but unconfirmed
    here; the observation that settles it is the pushed run's GREEN 1b result after Task 3, and
    the summary says so. Either way the cause class is SUITE, not KIT, not platform: `GREEN 1`
    passes on the same leg, so the refusal path works; only the margin is wrong.

    Name what would distinguish the next class: a KIT cause (the checker not refusing a parse
    fault in a multi-file program) would make GREEN 1b red on EVERY leg, and the windows leg is
    green; a platform cause (ubuntu's node not raising the RangeError) would make GREEN 1 red too,
    and it passes.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts -t "parseBoundaryFor"</automated>
    <fails_when>non-zero exit, or 0 passed — the measurement helper did not run</fails_when>
  </verify>
  <acceptance_criteria>
    - The plan summary carries a section headed `Diagnosis — U-1 / U-2` naming the cause class (SUITE), the evidence rows from § 2.4, the hypothesis marked `UNKNOWN - verify` where unconfirmed, the two measured boundary pairs from this host, and the distinguishing observation for each neighbouring class.
    - The helper `parseBoundaryFor` exists in the test file with a case that records both pairs (through `console.log` and an assertion that both pairs are adjacent).
    - No fix was applied in this task: `git show --stat <the task's commit sha>` lists only `scripts/runnable-ref/uat-spec-integrity.test.ts` (sha and output quoted in the summary).
  </acceptance_criteria>
  <done>The ubuntu red has a named cause and a named distinguishing observation before anything is changed to close it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mixed-arrangement cases plant a derived depth with margin (U-1, U-2)</name>
  <read_first>
    - The diagnosis section written by Task 2 and the two measured boundary pairs.
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` — every site Task 2 classified as a mixed arrangement (at least GREEN 1b ~L6743, ORDERING ~L7086, PROBE 2 (b) ~L7707; confirm the rest by reading), and the adjacency cases that MUST keep the exact boundary (`safe` passes / `overflow` fails in the one-file arrangement, ~L6755-6770).
  </read_first>
  <files>scripts/runnable-ref/uat-spec-integrity.test.ts</files>
  <behavior>
    - Test AD (RED first — it is red only where the margin is zero, so on darwin the red half is the diagnosis's own measurement; write the case so that it asserts the mixed arrangement at `mixedArrangementDepth(boundary)` exits 2 with `visited 1 of 2`, AND asserts that `mixedArrangementDepth(boundary) >= 2 * boundary.overflow`, so a future author cannot shrink the margin without a red).
    - GREEN 1b and ORDERING pass with the derived depth; the one-file adjacency cases still pass at the exact boundary.
  </behavior>
  <action>
    Add `mixedArrangementDepth(boundary: { safe: number; overflow: number }): number` returning
    `2 * boundary.overflow` — DERIVED from the measured boundary (never a literal), with a comment
    stating the reason in one paragraph: the boundary is exact for the one-file arrangement it was
    bisected in; a second root file shifts the stack baseline at which the pathological file is
    parsed by a handful of frames; doubling the depth keeps the parse an overflow in any
    arrangement whose baseline shift is smaller than the boundary itself, which is every
    arrangement this suite drives, while still deriving from this host's measurement so a host with
    a larger stack moves both numbers together. Use it at every mixed-arrangement site Task 2
    classified (GREEN 1b, ORDERING, PROBE 2 (b), and any other with a sibling spec). Leave the
    one-file adjacency cases at the exact boundary — they are the measurement.

    Probe every arm: run the entire file (not only the titles), because the depth constant reaches
    `inProcessOverflowDepth()` consumers too (~L7700) — classify whether that path needs the margin
    (it bisects in-process per arrangement already, or it does not; read and record).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
    <automated>grep -a -c 'mixedArrangementDepth(' scripts/runnable-ref/uat-spec-integrity.test.ts</automated>
    <fails_when>a count below 4 — the definition plus at least GREEN 1b, ORDERING and PROBE 2 (b) must use it</fails_when>
  </verify>
  <acceptance_criteria>
    - `mixedArrangementDepth` exists, derives from the boundary, and is used at every mixed-arrangement site; the summary lists the sites and the ones deliberately left at the exact boundary.
    - Test AD exists and asserts the margin relationship.
    - No depth literal was introduced: `grep -a -cE 'nestedSpec\([0-9]+\)' scripts/runnable-ref/uat-spec-integrity.test.ts` is 0 (0 on the dispatch base).
    - The whole file is green on this host; the titles U-1 and U-2 are named as the pushed run's expected greens.
  </acceptance_criteria>
  <done>The two-file cases no longer sit on a zero-margin boundary measured in a different arrangement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a parse fault recorded by the compiler host -> the checker's per-file verdict | A lost diagnostic prints a fallback sentence for a file whose real fault existed; the verdict is still a refusal, but the trace is wrong |
| a boundary measured in one arrangement -> a case run in another | A zero-margin depth makes the suite's verdict host-dependent without any host branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-83 | Repudiation | lost parse diagnostic on win32 | medium | mitigate | Task 1: one `faultKey` at both sites; proven with a win32-spelled pair on darwin. |
| T-33-84 | Repudiation | a fabricated ubuntu diagnosis | high | mitigate | Task 2: hypothesis labelled `UNKNOWN - verify` where unconfirmed; both boundary pairs measured on this host and recorded; distinguishing observations named. |
| T-33-85 | Denial of service | a mixed case whose outcome flips per host | medium | mitigate | Task 3: derived doubled depth; the margin relationship pinned by a test. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run check:build-parity` — committed `.js` faithful.
</verification>

<success_criteria>
- The two windows reds in this file are closed in the module and the test under one key rule.
- The two ubuntu reds have a written diagnosis first and a derived-margin fix second; the pushed run is named as the settling observation.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `faultKey(path, separator = sep)` | exported function, `scripts/runnable-ref/uat-spec-integrity.ts` | the one parse-fault key |
| `parseBoundaryFor(arrangement)` | test helper | the per-arrangement bisection used by the diagnosis |
| `mixedArrangementDepth(boundary)` | test helper | the derived doubled depth for two-file cases |
| `Diagnosis — U-1 / U-2` | summary section | cause class, evidence, measured pairs, distinguishing observations |
| Tests AA-AD | new test cases | red-first where the host can show the red |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-17-SUMMARY.md` when done
</output>

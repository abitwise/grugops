---
phase: 33-live-capture-windows-portability
plan: 21
type: execute
wave: 10
depends_on: ["33-12", "33-13", "33-20"]
files_modified:
  - .planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-A.jsonl
  - .planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-B.jsonl
  - .planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-SUMMARY.md
  - .planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md
  - .planning/phases/33-live-capture-windows-portability/33-R2-DRYRUN-REPORT.md
  - .planning/phases/33-live-capture-windows-portability/33-CAPTURE-A.jsonl
  - .planning/phases/33-live-capture-windows-portability/33-CAPTURE-B.jsonl
  - .planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md
  - .planning/phases/33-live-capture-windows-portability/33-R2-DIAGNOSIS.md
autonomous: false
gap_closure: true
requirements: [CAP-03, CAP-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Before any go is asked for: every instrument fix (33-12, 33-13) is merged; the post-wave gate, the build-parity gate and `capture-live --dry-run` all exit 0 with `GO-READINESS: ready`; HEAD equals `origin/main` and the working tree is clean (D-10; WR-06 handled by procedure)."
    - "The round-1 held capture is preserved unedited under `round-1-held/` (a rename, not an edit — D-11) so the round-2 runner can write the names the flip manifest keys on without overwriting the evidence 33-DIAGNOSIS.md cites by line."
    - "The KIT decisions 33-DIAGNOSIS.md § 1.4 (a)/(b), § 2 and § 3 name are put to the human as a decision checkpoint BEFORE the go, with the honest expectation stated: divergence (i) is deterministic on the current kit, so a capture without (a) may diverge on the note-route axis for a kit reason, not a variance one."
    - "At most one live run happens in this round, behind a blocking human go that states the measured floor (11.75 USD, 31 min 07 s for the whole runner on 2026-09-20) and the per-call bound actually used (D-09, D-12); a go is for this occasion only."
    - "The run's artifacts are written AFTER the run and never between the push and the go; the transcript is filed unedited with exactly one outcome line (D-11); a red run gets a zero-token diagnosis and nothing flips (D-20)."
  artifacts:
    - .planning/phases/33-live-capture-windows-portability/33-R2-DRYRUN-REPORT.md
    - .planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md
    - .planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-SUMMARY.md
  key_links:
    - "the dry-run report's GO-READINESS line -> the go checkpoint text -> the human go"
    - "the runner's `Dual-path parity (D-07) — path-invariant projection` section -> the outcome line -> plan 33-22's flip-or-hold branch"
    - "the runner's `installed plugin provenance (D-05, ...)` row -> the outcome line (CR-02)"
    - "`git rev-parse HEAD` == `git rev-parse origin/main` -> the runner's pushed-sha row -> readiness"
  prohibitions:
    - "a loud skip is never accepted as the capture"
    - "no fix-and-rerun inside one gap-closure round (D-11)"
    - "the harness never sets the prod-deploy approval environment variable"
    - "the D-07 predicate is never softened to make a capture read as parity (D-20)"
    - "no artifact is written between the push and the go; the go is asked for against the pushed sha"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): the flip depends on this plan's outcome word being `pass`; plan 33-22 performs or holds it."
    - "CAP-02 (edge row unresolved, flagged): this plan's precondition reads plan 33-20's verdict; the CONTEXT recommendation (capture against a tree green on both legs) is honored when 33-20 reads MET and is stated in the go text when it does not."
    - "CAP-03 (edge row unresolved, flagged): produced only by the capture this plan runs, through the instrument 33-12 and 33-13 corrected."
---

<objective>
Gap-closure round 2 of the phase's four-round cap (round 1 = plans 33-01..33-11 + verification
`gaps_found` 0/3). The round's one live go (D-09), sequenced strictly after the instrument fixes
(33-12, 33-13) are merged and proven offline, and after the CI measurement (33-20) has a
conclusion word. Two stops for the human: the KIT decisions the round-1 diagnosis left open, and
the money-spending go.

Gap mapping (33-VERIFICATION.md): CAP-01 `missing[1]` "A second live-capture go (bounded to one
per gap-closure round, per D-09) whose D-07 comparison shows parity" → Tasks 3-4; CAP-03
`missing[2]` "A re-capture after both fixes land, so the CAP-03 evidence is produced by an
instrument the model under test cannot influence" → Task 4. The KIT findings (33-DIAGNOSIS.md
§ 1.4 (a)/(b), § 2, § 3) are decisions for the human → Task 2. The `.planning/PROJECT.md`
reconciliation (in the manifest's declared set, absent from 33-11's file list) is checked offline
in Task 1 and carried into plan 33-22's file list.

Decisions honored: D-09 (one go per round, per-occasion), D-10 (dry run exits 0 and reads ready
first), D-11 (record, diagnose at zero tokens, no rerun), D-12 (bound derived from the previous
run: max(1 009 964, 842 417) ms × 1.2 = 1 211 957 ms, capped at the 20-minute ceiling, so the bound
used is 1 200 000 ms — written into the summary by the runner), D-20 (nothing flips on a
divergence), D-05/D-07 as corrected by 33-12.

The pushed-sha wrinkle (project memory): the runner refuses unless HEAD equals `origin/main`, so
Task 1's commits must be pushed by the human before the go, and nothing is committed between that
push and the run.

Purpose: spend the round's one go on an instrument that can now be trusted, with the human
deciding the kit questions first and the money second.
Output: the round-2 dry-run report, the round-1 capture preserved under `round-1-held/`, and
either a round-2 capture set with one outcome line or a zero-token diagnosis.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md
@.planning/phases/33-live-capture-windows-portability/33-10-PLAN.md
@.planning/phases/33-live-capture-windows-portability/33-10-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-12-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-13-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-20-SUMMARY.md
@.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve the held capture, prove readiness at zero tokens, and check the manifest's declared set offline</name>
  <precondition>Plans 33-12 and 33-13 are merged (their SUMMARY files exist and their commits are ancestors of HEAD), and plan 33-20's Part 3 § 3.3 carries a verdict line (MET or NOT MET) for the CI run of the currently pushed sha.</precondition>
  <read_first>
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` — its opening paragraph names the three files it cites (`33-CAPTURE-SUMMARY.md`, `33-CAPTURE-A.jsonl`, `33-CAPTURE-B.jsonl`, commit `c7be6d0d`); the `A:<n>` / `B:<n>` citation convention.
    - `.planning/phases/33-live-capture-windows-portability/33-FLIP-MANIFEST.md` § 2.1 (the commit-set rule; the declared set is 14 files including `.planning/PROJECT.md`), § 6 (the hold record's last paragraph naming the `PROJECT.md` item), and the settings table (`capture summary` = `33-CAPTURE-SUMMARY.md`, resolved relative to the manifest — the name the round-2 runner must write).
    - `scripts/capture-live.ts` — `CAPTURE_SUMMARY_NAME`, `captureTranscriptName`, `DRY_RUN_REPORT_NAME`, the readiness line, `verifyArtifacts`'s "exactly one report" rule (so the archived summary must not sit beside the new one).
    - `scripts/check-flip-manifest.ts` — how it derives the declared set in the pre-capture state and prints it.
    - `.planning/phases/33-live-capture-windows-portability/33-10-PLAN.md` Task 1 — the round-1 dry-run filing shape.
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-A.jsonl, .planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-B.jsonl, .planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-SUMMARY.md, .planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md, .planning/phases/33-live-capture-windows-portability/33-R2-DRYRUN-REPORT.md</files>
  <action>
    Preserve, do not edit (D-11): `git mv` the three round-1 capture files into
    `round-1-held/` under the phase directory, keeping their names, so 33-DIAGNOSIS.md's `A:<n>` /
    `B:<n>` citations still resolve inside that directory and the round-2 runner can write the
    manifest-keyed names at the phase root without overwriting evidence. Add ONE line under
    33-DIAGNOSIS.md's title (an annotation, never a rewrite of the diagnosis): the three files
    it cites moved to `round-1-held/` in round 2 (commit `c7be6d0d` holds them at the original
    paths, byte-identical — `git diff --stat c7be6d0d HEAD -- <the three new paths>` empty after
    the rename with `--find-renames`).

    Run the offline gates and quote each exit status in the summary:
    `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes`,
    then `npm run build && npm run check:build-parity`. Then the zero-token dry run:
    `node scripts/capture-live.js --dry-run --out <scratch dir>`, and copy its report into the phase
    directory as `33-R2-DRYRUN-REPORT.md` (the fixture-derived JSONL stays in scratch — it is not a
    capture). The report must show `GO-READINESS: ready`, one `OUTCOME: no-go` line, the new
    `Dual-path parity (D-07) — path-invariant projection` section, the `installed plugin provenance`
    row, and the `transcript location` rows. If readiness is not ready, STOP: record what is unmet,
    close the plan with that finding, do not present either checkpoint.

    Check the manifest offline: run `node scripts/check-flip-manifest.js` (pre-capture state) and
    quote the declared-set count and the line naming `.planning/PROJECT.md` among the fourteen;
    confirm the commit-set rule's converse case
    (`CONVERSE: an explicit --range over exactly the declared set passes in the discharged state`)
    passes in `scripts/check-flip-manifest.test.ts`. Plan 33-22 carries `.planning/PROJECT.md` in
    its file list on that evidence.

    Commit these artifacts. Do not push — Task 3 asks the human to. Record `git rev-parse HEAD`
    and `git status --porcelain` (must be empty after the commit; a dirty tree is not seen by the
    runner's pushed-sha row, WR-06).
  </action>
  <verify>
    <automated>for f in 33-CAPTURE-A.jsonl 33-CAPTURE-B.jsonl 33-CAPTURE-SUMMARY.md; do A=$(git rev-parse "c7be6d0d:.planning/phases/33-live-capture-windows-portability/$f") &amp;&amp; B=$(git rev-parse "HEAD:.planning/phases/33-live-capture-windows-portability/round-1-held/$f") &amp;&amp; test -n "$A" &amp;&amp; test "$A" = "$B" || exit 1; done</automated>
    <fails_when>non-zero exit — a blob under round-1-held/ differs from the same file at commit c7be6d0d, or either object cannot be resolved</fails_when>
    <automated>grep -a -c 'round-1-held' .planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md</automated>
    <fails_when>a count other than 1 — the annotation line is absent or the diagnosis was edited beyond it (0 on the dispatch base)</fails_when>
    <automated>grep -a -c 'GO-READINESS: ready' .planning/phases/33-live-capture-windows-portability/33-R2-DRYRUN-REPORT.md</automated>
    <fails_when>a count other than 1</fails_when>
    <automated>grep -a -cE '^(OUTCOME|Outcome): (pass|fail|hang|no-go)$' .planning/phases/33-live-capture-windows-portability/33-R2-DRYRUN-REPORT.md</automated>
    <fails_when>a count other than 1, or the word on that line is anything other than no-go</fails_when>
    <automated>node scripts/check-flip-manifest.js</automated>
    <fails_when>non-zero exit, or the output lacks ALL CHECKS PASSED</fails_when>
    <automated>test -z "$(git status --porcelain)"</automated>
    <fails_when>non-zero exit — the working tree is dirty and the runner's pushed-sha row would not see it</fails_when>
  </verify>
  <acceptance_criteria>
    - The three round-1 files exist under `round-1-held/` byte-identical to commit `c7be6d0d` (blob shas equal, quoted); 33-DIAGNOSIS.md carries the one-line annotation and no other change (`git diff --stat c7be6d0d HEAD -- .planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` quoted: one insertion beyond the file's own creation).
    - `33-R2-DRYRUN-REPORT.md` is committed with readiness ready, one no-go outcome line, and the three new report elements named above.
    - The summary quotes the flip gate's declared-set count (14) and the `.planning/PROJECT.md` line.
    - The four offline gate commands' exit statuses are quoted; the working tree is clean after the commit; HEAD's sha is recorded.
  </acceptance_criteria>
  <done>Evidence preserved, machinery proven ready at zero cost, and the manifest's fourteen files confirmed before anyone is asked a question.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Checkpoint — the kit decisions the round-1 diagnosis left to a human, decided before money is spent</name>
  <decision>How to dispose the three KIT findings of 33-DIAGNOSIS.md (§ 1.4 (a) coordinator grant / (b) reader admission of hand-written notes; § 2 the guard refusing redirections and expansions on tool name alone; § 3 `admitAndAppend` writing `verified_by: undefined`) — each either accepted open as a ledger row for round 3, or a reason to hold this round's go</decision>
  <context>
    Round 1's capture diverged on the D-07 comparator for a SUITE reason (fixed in 33-12: the
    predicate is now path-invariant and meetable), but 33-DIAGNOSIS.md § 1.3 also recorded three
    BEHAVIOURAL differences, and one of them is deterministic on the current kit: (i) the
    `--agent` path's coordinator carries the adapter's seven-tool grant, which does not include
    the plugin's MCP admission tool, so on that path the coordinator cannot propose notes through
    the sanctioned writer; (ii) followed — path B's role agents wrote their notes with the `Write`
    tool straight into the context root, and the reader admitted them. The corrected projection
    compares the note ROUTE (direct writes into the context root; propose_note tool-use blocks) as
    D-07's admitted-note set demands, so a round-2 capture on the current kit is expected to
    diverge on that axis for a KIT reason unless (a) or (b) changes — and D-20 forbids softening
    the predicate to hide it. Per-role note counts and kind multisets are also model-chosen; one
    sample per path cannot separate variance from mechanism on those axes (§ 1.3 (iii)).

    What each option costs to undo: a ledger row is reversible (round 3 owns it); a kit change to
    the coordinator adapter's grant touches the spawn-grant derivation, its census pins and the
    guard oracles (costly, and out of this round's scope by the human's own decision:
    Criticals + CAP gaps only); a reader that refuses non-sanctioned notes changes what every
    installed kit admits (costly). None is one-way.

    § 2 (the guard denying `git log ... 2>&1` and `echo "npm run $s"` on tool name alone) is an
    availability defect that shaped both sessions; it fails closed and is not a safety hole. § 3
    (`verified_by: undefined` written for an absent field) does not touch any verdict this round
    reads. Both are offline-provable and belong to a round that is scoped for kit work.

    This checkpoint does not re-ask the round-1 hold; it asks what the round-3 plan should own,
    and whether you want this round's go to proceed against a kit that will predictably diverge
    on the route axis. The go itself is Task 3.
  </context>
  <options>
    <option id="ledger-and-go">
      <name>Record all four KIT items as accepted-open ledger rows for round 3; proceed to the go checkpoint</name>
      <pros>Honors the human's round-2 scope (Criticals + CAP gaps only); the capture measures the corrected instrument end to end and records the route divergence VERBATIM as a kit finding under the meetable predicate, which is exactly the observation round 3's kit work needs; CAP-03's observation (both sides, independent channel, provenance MET) is produced regardless of the D-07 word.</pros>
      <cons>Likely spends the round's go (floor 11.75 USD, 31 min) on a capture whose D-07 word is expected to be `fail` on the route axis; GAP-D1 then waits for round 3.</cons>
    </option>
    <option id="ledger-and-hold">
      <name>Record the four KIT items as ledger rows; HOLD this round's go — round 3 lands (a)/(b) first, then asks for its go</name>
      <pros>No money spent on a predictable divergence; the next go runs against a kit where the sanctioned route is reachable on both paths.</pros>
      <cons>CAP-03's independent-instrument observation is deferred a round with two rounds left under the cap; the corrected instrument is proven only at zero tokens this round.</cons>
    </option>
    <option id="widen-scope">
      <name>Widen this round's scope to land (a) and/or (b) before the go</name>
      <pros>The go would run against the changed kit.</pros>
      <cons>Contradicts the human's own scope decision for this round; the adapter grant change touches spawn-grant derivations, census pins and guard oracles that this round has no plans for; a scope change here means replanning, not executing.</cons>
    </option>
  </options>
  <resume-signal>Select: ledger-and-go, ledger-and-hold, or widen-scope — and for (a) and (b) separately, state which direction round 3 should take (a: carry the MCP admission tool in the coordinator grant / tell role adapters where the sanctioned writer lives when installed from the plugin cache; b: refuse non-sanctioned notes on read / keep admitting them)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint — push the pre-go commits, then the live capture go, one occasion only</name>
  <what-built>
    The instrument is corrected and proven at zero tokens: the scored transcript now lives in a
    runner-owned directory the agent cannot reach (CR-01), provenance is by plugin name and by
    content digest and gates the outcome word (CR-02), the D-07 verdict is a path-invariant
    projection proven both to still name the round-1 divergences and to read parity over an
    at/body-only pair (CR-03), a failed install stops the run before any spend (CR-05), and the
    transcript survives every failure path (CR-04). The round-2 dry run is filed at
    `.planning/phases/33-live-capture-windows-portability/33-R2-DRYRUN-REPORT.md` with
    `GO-READINESS: ready`. The round-1 capture is preserved under `round-1-held/`. Plan 33-20's
    CI verdict is in `33-CI-MEASUREMENT.md` Part 3 § 3.3 (read it: the CONTEXT recommendation was
    to capture against a tree green on both legs).

    This checkpoint authorizes ONE live capture run, for this occasion only (D-09).

    THE COST, stated plainly because it is real money. The round-1 run on 2026-09-20 cost 11.75 US
    dollars in reported model cost and took 31 minutes 07 seconds for the whole runner (two model
    sessions plus two plugin installs). The per-call bound used this time is 1 200 000 ms — derived
    from the round-1 per-call durations with 20 percent headroom and capped at the 20-minute
    ceiling (D-12) — and is written into the summary by the runner. Expect a comparable or larger
    figure.

    THE EXPECTATION, stated plainly. If Task 2 chose `ledger-and-go`, the D-07 word may read
    `fail` on the note-route axis for the deterministic kit reason the diagnosis names; the run is
    still the CAP-03 observation on an instrument the subject cannot write to, and the divergence
    is recorded verbatim for round 3. If Task 2 chose `ledger-and-hold`, answer `no-go` here.

    THE PUSH FIRST. The runner refuses unless HEAD equals the remote head, and Task 1 committed the
    dry-run report and the archive rename. Push those commits (or approve the executor pushing
    them), then confirm `git rev-parse HEAD` equals `git rev-parse origin/main` and
    `git status --porcelain` is empty. Nothing is committed between the push and the run.
  </what-built>
  <how-to-verify>
    Before you answer:
    1. Read `33-R2-DRYRUN-REPORT.md` — the precondition table, the parity section over the fixture,
       the provenance row.
    2. Read `33-CI-MEASUREMENT.md` Part 3 § 3.3 — the CI verdict for the tree you are capturing on.
    3. Push, then check `git log --oneline origin/main..HEAD` is empty and the tree is clean.
    4. Confirm you are willing to spend on the order of 12 US dollars or more and wait about
       35 minutes.

    What the run does: builds two independent targets from the committed fixture project,
    installs the plugin from the existing marketplace row into each (a failed install stops the
    run before any model call), runs the same request through both dispatch paths under the
    bound, streams each transcript into a runner-owned scratch directory outside the target,
    exercises the harmless matched deployment probe so the pre-tool guard fires, verifies the
    installed plugin's content against the checkout, derives the summary with the path-invariant
    parity section, redacts, and prints exactly one outcome line. The approval environment
    variable is never set by the harness.

    What comes back either way: two redacted transcripts, a derived summary with one outcome line,
    and — if red — a zero-token diagnosis naming the cause class and the observation that would
    distinguish it from the next class. A red run is not re-run in this round (D-11).
  </how-to-verify>
  <resume-signal>Type "pushed, go" to confirm the push is complete and authorize this one live run, or "no-go" to record the outcome as no-go and close the round without spending</resume-signal>
</task>

<task type="auto">
  <name>Task 4: The capture — one run, artifacts written after, one outcome line; or the recorded no-go</name>
  <precondition>Task 3 was answered for this occasion (a go recorded in any earlier plan or round does not authorize this run, D-09); `git rev-parse HEAD` equals `git rev-parse origin/main` and `git status --porcelain` is empty at the moment the runner starts.</precondition>
  <read_first>
    - `scripts/capture-live.ts` as landed by 33-12 and 33-13 — the run phase, `runTarget`, the transcript scratch class and the preserved-on-failure printing, `writeArtifacts`, `verifyArtifacts`, `CAPTURE_SUMMARY_NAME`.
    - `.planning/phases/33-live-capture-windows-portability/33-10-PLAN.md` Task 3 — the round-1 capture task, whose shape this repeats; `33-10-SUMMARY.md` for how the artifacts were committed.
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` — the diagnosis shape (cause class, evidence, distinguishing observation) a red run must repeat at zero tokens, and § 1.3's three behavioural axes to compare against.
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-SUMMARY.md` — the outcome-line grammar and the no-later-plan-may-cite rule.
  </read_first>
  <files>.planning/phases/33-live-capture-windows-portability/33-CAPTURE-A.jsonl, .planning/phases/33-live-capture-windows-portability/33-CAPTURE-B.jsonl, .planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md, .planning/phases/33-live-capture-windows-portability/33-R2-DIAGNOSIS.md</files>
  <action>
    If Task 3 answered `no-go`: write no capture; record in the plan summary the word `no-go`,
    the reason the human gave, and that nothing flips; plan 33-22 takes its hold branch and plan
    33-23 records the round; stop here.

    Otherwise run the capture ONCE: `node scripts/capture-live.js --out <scratch dir> --keep-target`
    (keep-target so a post-run question can be answered without a second spend; the targets are
    removed by hand after the artifacts are committed and the summary records that). Let the
    runner do every derivation. Then copy the artifact set into the phase directory under the
    names the flip manifest keys on (`33-CAPTURE-A.jsonl`, `33-CAPTURE-B.jsonl`,
    `33-CAPTURE-SUMMARY.md`) — AFTER the run, never before — and run the runner's own re-check
    over the phase directory and the NUL-byte gate. Commit the set with a message stating the
    outcome word, both runs' exit statuses, the parity verdict and the provenance state.

    If the outcome word is `pass`: stop; plan 33-22 performs the flip against this summary.

    If the outcome word is `fail` or `hang`: file `33-R2-DIAGNOSIS.md` at ZERO tokens in
    33-DIAGNOSIS.md's shape — the run table (§ 0's shape: exits, CAP-03 sides, deny, plugin,
    provenance state, cost/duration), the cause class per divergence axis (SUITE / KIT / platform /
    host) with the transcript line citations, and for each the observation that distinguishes it
    from the next class; compare the note-route, per-role count and kind axes against § 1.3's
    round-1 values and state which moved. Do not fix, do not rerun. Nothing flips (D-20); plan
    33-22 takes its hold branch.

    If the runner stopped before the spawn (a failed install, an unmet precondition): the summary
    quotes the `CAPTURE NOT DERIVED` line and the preserved transcript-scratch paths (if any);
    that is a round-2 finding, not a capture, and the round closes on it.
  </action>
  <verify>
    <automated>grep -a -cE '^(OUTCOME|Outcome): (pass|fail|hang|no-go)$' .planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md</automated>
    <fails_when>a count other than 1 — exactly one grammar-conformant outcome line (on the no-go branch this file is absent and the summary records the word instead; state which branch in the summary)</fails_when>
    <automated>node scripts/capture-live.js --verify-artifacts --out .planning/phases/33-live-capture-windows-portability</automated>
    <fails_when>non-zero exit, or a REFUSED line naming an uncited claim row, a surviving home-path spelling, or a duplicated outcome line</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the output names a committed transcript as carrying a control byte</fails_when>
    <automated>grep -a -c 'installed plugin provenance (D-05' .planning/phases/33-live-capture-windows-portability/33-CAPTURE-SUMMARY.md</automated>
    <fails_when>a count of 0 — the summary was written by a runner without the CR-02 row</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one live run was made in this round, or none with `no-go` recorded; the summary records the two calls, their durations, reported costs, the bound actually used, and the provenance state.
    - The committed summary's `Dual-path parity (D-07) — path-invariant projection` section lists both projections and either `parity: the two projections are equal` or the named diffs; an empty list is stated as empty.
    - The `transcript location` rows read `runner-owned scratch, outside every target ...` for both runs.
    - On `fail`/`hang`: `33-R2-DIAGNOSIS.md` exists, spends zero tokens, names a cause class per axis with citations, and no fix or rerun happened.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-SPAWN-03-RUNTIME-EVIDENCE.md` and every flip-manifest file are unchanged by this plan.
    - The artifacts were committed after the run; no commit sits between the push and the runner's start (the summary quotes `git log --oneline origin/main..HEAD` empty at start).
  </acceptance_criteria>
  <done>One run, one artifact set, one outcome line — on an instrument that can now be trusted — or an honest no-go.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an authorized human go -> real money and a model session | Per-occasion, never inherited (D-09) |
| the pushed sha -> the marketplace install -> the capture | A dirty tree or an unpushed commit makes the two paths different code while the row reads MET (WR-06) |
| platform stream output -> a committed artifact | Untrusted model and CLI output crosses into git history |
| the KIT decisions -> round 3's scope | Silently choosing a default would decide kit behaviour without the human |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-96 | Repudiation | an inherited or re-used go | critical | mitigate | Task 3 is per-occasion; Task 4's precondition names it; no later plan may cite it. |
| T-33-97 | Spoofing | capture against unpushed or dirty code | high | mitigate | Task 1 commits and asserts a clean tree; Task 3 requires the push first; Task 4's precondition re-checks HEAD == origin/main and a clean tree at start. |
| T-33-98 | Elevation of privilege | the approval environment variable | critical | mitigate | The runner never sets it; asserted on the constructed child environment before every spawn (33-01). |
| T-33-99 | Information disclosure | the committed transcript | high | mitigate | Redaction fails closed; `--verify-artifacts` refuses survivors; NUL-byte gate run. |
| T-33-100 | Tampering | fix-and-rerun inside the round | high | mitigate | Prohibited; a red run files a zero-token diagnosis and round 3 owns the fix. |
| T-33-101 | Tampering | the D-07 predicate softened to reach `pass` | critical | mitigate | 33-12's Test A pins the held capture's divergences; this plan changes no source. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed; the plugin install is a platform operation over an existing marketplace row, uninstalled in cleanup. |
</threat_model>

<verification>
- `node scripts/capture-live.js --dry-run --out <scratch>` — exit 0, ready, filed as `33-R2-DRYRUN-REPORT.md` before the go.
- `git rev-parse HEAD` == `git rev-parse origin/main`, `git status --porcelain` empty, before the go.
- `grep -acE '^(OUTCOME|Outcome): (pass|fail|hang|no-go)$' 33-CAPTURE-SUMMARY.md` — exactly 1.
- `node scripts/capture-live.js --verify-artifacts --out <phase dir>` and `npm run check:nul-bytes` — exit 0.
</verification>

<success_criteria>
- The held capture is preserved unedited; the dry run is filed ready; the manifest's fourteen files are confirmed offline.
- The KIT decisions are the human's, recorded, before the money is asked for.
- At most one live run, artifacts written after, one outcome line — or a recorded no-go — and a zero-token diagnosis on red.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `round-1-held/33-CAPTURE-{A,B}.jsonl`, `round-1-held/33-CAPTURE-SUMMARY.md` | preserved artifacts (renamed) | byte-identical to commit `c7be6d0d` |
| one annotation line in `33-DIAGNOSIS.md` | annotation | names the move; nothing else changes |
| `33-R2-DRYRUN-REPORT.md` | new artifact | round-2 readiness evidence (D-10) |
| `33-CAPTURE-A.jsonl`, `33-CAPTURE-B.jsonl`, `33-CAPTURE-SUMMARY.md` | new artifacts (go branch) | the round-2 capture under the manifest-keyed names |
| `33-R2-DIAGNOSIS.md` | new artifact, conditional | zero-token diagnosis on `fail`/`hang` |
| the KIT decision record | plan summary section | (a)/(b)/§ 2/§ 3 dispositions as the human stated them |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-21-SUMMARY.md` when done
</output>

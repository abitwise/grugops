---
phase: 33-live-capture-windows-portability
plan: 27
type: execute
wave: 16
depends_on: ["33-26"]
files_modified:
  - scripts/checkpoints.ts
  - scripts/checkpoints.js
  - scripts/checkpoints.test.ts
  - hooks/guard.ts
  - hooks/guard.js
  - hooks/guard.test.ts
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
autonomous: true
gap_closure: true
requirements: [CAP-01, CAP-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A REDIRECTION word — a word the anchored allow-list grammar fully describes (an optional file-descriptor digit run, one of the operators `>`, `>>`, `<`, `&>`, `&>>`, then either an attached `&`-digit target, an attached canonical path target, or nothing, in which case the NEXT word is its target) — is classified `redirection`: it is never a tool, never a verb candidate, never a flag, it consumes its target, and it does not make the segment opaque. A `2>&1` is one word in one segment, not a split at `&` (33-DIAGNOSIS.md § 2, WINDOWS.md row 257)."
    - "Everything the grammar does not fully describe stays OPAQUE and the segment stays fail-closed on the tool name alone — a bare `$var` expansion, a heredoc `<<`, a process substitution, a word that merely CONTAINS an operator (`push>x`, `pu2>&1sh`). The `$var` and heredoc arms are DISPOSITIONED, not changed: a variable's value is unknowable at hook time and a heredoc's body lines are commands to this tokenizer, so fail-closed is the safe direction and P30's fence (an unreadable segment denies on the tool name alone, a backstop the triggering edit cannot defeat) is not re-opened one register over."
    - "The deny text names the mechanism that fired: an unreadable segment's escape sentence says so and names the word, and the `git push`-without-a-branch sentence is printed only when the readable command model matched a git push — never for a `git log` refused because a sibling word was unreadable (the diagnosis's second half)."
    - "The evidence is the round-1 transcripts: every § 2 deny whose only unreadable word was a redirection now ALLOWS, replayed against the committed guard on stdin exactly as the diagnosis replayed it; every § 2 deny that also carried `$var` or a heredoc still DENIES with the corrected sentence; the P30 red-team corpus in `hooks/guard.test.ts` is byte-unchanged and green; and removing the redirection arm in a scratch copy flips every new allow back to deny (mutation-proven)."
  artifacts:
    - scripts/checkpoints.ts
    - scripts/checkpoints.js
    - hooks/guard.ts
    - hooks/guard.js
  key_links:
    - "checkpoints.ts `commandSegments` (split) -> `classifyWords` (`redirection` kind, target consumption) -> `matchCommandCheckpoints` (opaque decided per segment) -> `failClosedCheckpoints` only for a still-opaque segment"
    - "guard.ts the escape sentence -> `modelled.untokenizable` asked FIRST -> the push sentence only on a readable git match"
    - "hooks/guard.js + scripts/checkpoints.js rebuild -> `npm run generate:hook-manifest` -> hook-entry.ts module hashes -> `npm run freshness:hook-manifest`"
  prohibitions:
    - "no word the grammar does not fully describe is ever read as a value (the RA3-1 rule)"
    - "the fail-closed rule for an opaque segment is never weakened by position, count or tool"
    - "no P30 red-team corpus case changes its expected verdict"
    - "no gate, CI or capture result is narrated rather than quoted from the command's own output"
    - "`npm test` is never run in this phase"
  assumptions:
    - "CAP-01 (edge row unresolved, flagged): this plan closes the redirection half of KIT § 2 and dispositions the rest; it decides nothing about the flip."
    - "CAP-02 (edge row unresolved, flagged): untouched beyond the rebuild of two modules the windows leg exercises; measured only by plan 33-31."
    - "CAP-03 (edge row unresolved, flagged): the guard's availability shaped both round-1 sessions (the mapper's gate loop, the first note attempts); this plan changes no verdict input of the capture, only what the guard refuses during it."
---

<objective>
Gap-closure round 3 of the phase's four-round cap. KIT § 2 of 33-DIAGNOSIS.md, accepted open for
round 3 at plan 33-21's decision checkpoint (WINDOWS.md row 257): the guard's command model
refused fifteen of the seventeen live denies on tool name alone because a `2>&1` redirection or
an ordinary `$var` expansion was classified as a substitution it will not reason about — and the
deny text printed the `git push` sentence for a `git log`. Reproduced offline on stdin against
the committed guard; the guard's own suite was green at 275, so the class had no test. The
diagnosis names the correction: the tokenizer's redirection-versus-substitution classification,
and the deny text. This is an availability defect, not a safety hole (the guard fails closed), and
it is fixed in the safe direction only: redirections become readable by an anchored allow-list
grammar; expansions and heredocs stay opaque and are dispositioned with the reason.

Gap mapping (round-3 closure target 6, § 2): Task 1 the grammar and its proof against the
transcripts and the P30 corpus; Task 2 the deny text, the stdin replay, the rebuild and the
disposition hand-off for plan 33-34's ledger row.

Decisions honored: D-11 (offline proof; the round-1 transcripts are the fixture, unedited), the
P30 fence (an unreadable segment denies on the tool name alone), CLAUDE.md voice (the deny text is
a safety surface — clear professional voice).

Purpose: a redirection no longer costs an agent a refused gate loop, and a refusal names what it
refused.
Output: `scripts/checkpoints.ts` + `hooks/guard.ts` with committed twins and the manifest hash;
the new corpus cases; the disposition record.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-live-capture-windows-portability/33-CONTEXT.md
@.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md
@.planning/phases/33-live-capture-windows-portability/33-26-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A redirection is a redirection — one anchored grammar at the split and at the word, proven on the round-1 commands and against the P30 corpus</name>
  <read_first>
    - `scripts/checkpoints.ts` — `CANONICAL_WORD_RE` (:354), `classifyWords` (:390-460: runs, the three canonical shapes, `WordKind`), `SEGMENT_SPLIT_RE` (:461: `&` is a splitter — which is why `2>&1` is today two segments and an opaque `2>` word), `UNRESOLVABLE_SHELL_RE` (:467), `commandSegments` (:478), `failClosedCheckpoints` (:763), `matchCommandCheckpoints` (:783-872: the `seg.opaque` arm, the multi-word re-tokenization, the benign-adjacent rule).
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` § 2 — the fifteen commands (A:445, A:458, A:586, A:667, A:1493, A:1507, A:1628, B:35, B:217, B:434, B:469, B:562, B:1504, B:1527, B:1740) and the six-line offline reproduction table.
    - `.planning/phases/33-live-capture-windows-portability/round-1-held/33-CAPTURE-A.jsonl` and `33-CAPTURE-B.jsonl` — those lines: each `assistant` frame's `Bash` tool-use block carries `input.command`, the exact text the guard received. Read through `git show c7be6d0d:` (the `heldCapture` idiom).
    - `scripts/checkpoints.test.ts` — the word-classification and segment cases (`grep -n 'classifyWords\|commandSegments\|matchCommandCheckpoints' scripts/checkpoints.test.ts`) whose shape the new cases follow; `hooks/guard.test.ts` :1333-1466 (RA3-1..RA3-7 and the round-3 false-denial cases: the corpus that must not move).
  </read_first>
  <files>scripts/checkpoints.ts, scripts/checkpoints.js, scripts/checkpoints.test.ts</files>
  <behavior>
    - Test G1 (RED first, the transcript commands): for each of the fifteen § 2 commands read from the held transcripts, `matchCommandCheckpoints(command)` — commands whose ONLY unreadable words are redirections (`2>&1`, `2>/dev/null` and their kin) return `untokenizable: false` and an EMPTY checkpoint set; commands that also carry a bare `$var` or a heredoc still return `untokenizable: true` with the tool-name checkpoints. The test states which of the fifteen fall in each class by line id and asserts the partition's sizes add up to fifteen. On the base every one of the fifteen is `untokenizable: true` — quote the red run.
    - Test G2 (the grammar, one row per form): a table of redirection forms — `>f`, `>>f`, `<f`, `> f` (bare operator, next word consumed), `2>f`, `2>>f`, `2>&1`, `1>&2`, `&>f`, `&>>f`, `>/dev/null`, `2>/dev/null`, `3>&2` — each yields ONE segment and ONE `redirection` word (plus its consumed target when detached); the words that are NOT redirections — `<<EOF`, `<<<x`, `<(cmd)`, `>(cmd)`, `push>x`, `pu2>&1sh`, `>'a b'` — each keeps today's opaque classification.
    - Test G3 (safety converse, derived from the rules table): for every `COMMAND_CHECKPOINT_RULES` row, the four spellings TOOL VERB `2>&1`, TOOL `2>&1` VERB, TOOL `> x` VERB and TOOL VERB `>/dev/null` all still return that row's checkpoint; TOOL `>` VERB (the verb is the consumed redirection target) returns none — the table is generated from the rules, not typed.
    - Test G4 (mutation): with the `redirection` arm removed in a scratch copy of the module (the classification falls back to opaque), every G1 allow flips to deny and every G3 case still denies — the arm is load-bearing in exactly one direction.
  </behavior>
  <action>
    In `scripts/checkpoints.ts`, add `redirection` to `WordKind` and teach the two places that read
    a shell's punctuation ONE grammar: (1) the segment splitter must not split inside a redirection
    operator that contains `&` (`>&`, `<&`, `&>`, `&>>`) — recognise the operator before the `&`
    splitter is consulted, outside quotes only; (2) `classifyWords` classifies a word the anchored
    grammar fully describes as `redirection` (optional digit run, operator, then an attached
    `&`+digits target, an attached target of `CANONICAL_WORD_RE` characters, or nothing). A bare
    operator consumes the following word as its target — that word is dropped from the candidate
    list, never read as a tool or verb. State the grammar as ONE exported regex with its docblock
    naming every form it admits and every form it deliberately does not (heredoc, here-string,
    process substitution, an operator glued to letters), so the table in Test G2 is asserted
    against the same constant.

    In `matchCommandCheckpoints`, `seg.opaque` must stay true whenever ANY word is opaque — a
    redirection word is not opaque, so a segment whose only punctuation was a redirection is now
    read by the model; nothing else about the fail-closed arm changes. Extend the RA3-1 docblock
    with the decided line: readable by grammar, or opaque — there is no third state, and `$var`
    and heredocs are opaque by decision (state the reason in two sentences).

    Tests G1-G4 in `scripts/checkpoints.test.ts`, RED before GREEN for G1 (quote it). Rebuild the
    committed `.js` in the same commit; `npm run generate:hook-manifest` follows (checkpoints.js
    is one of the manifest's module hashes).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts -t "redirection|G1|G2|G3|G4"</automated>
    <fails_when>non-zero exit, or fewer than 4 tests reported passed</fails_when>
    <automated>node -e "const m=require('./scripts/checkpoints.js');const a=m.matchCommandCheckpoints('git log --oneline -5 2>&1');const b=m.matchCommandCheckpoints('git push origin main 2>&1');const c=m.matchCommandCheckpoints('echo \"npm run \$s\"');if(a.untokenizable||a.checkpoints.size!==0)process.exit(1);if(b.checkpoints.size===0)process.exit(2);if(!c.untokenizable)process.exit(3)"</automated>
    <fails_when>exit 1 (a redirected git log is still refused), exit 2 (a redirected git push is no longer governed), or exit 3 (a bare variable expansion became readable — the disposition was violated)</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' hooks/guard.test.ts</automated>
    <fails_when>non-zero exit, or any failed test — a P30 corpus case moved</fails_when>
    <automated>npm run build &amp;&amp; npm run generate:hook-manifest &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/checkpoints.ts` exports the one redirection grammar constant; `WordKind` carries `redirection`; the splitter does not split inside `>&`/`<&`/`&>`/`&>>`; a bare operator consumes its target word.
    - Tests G1-G4 exist; G1's RED commit precedes its GREEN commit (shas quoted; the fifteen line ids partitioned and the partition sizes stated); G3 is generated from `COMMAND_CHECKPOINT_RULES`.
    - `hooks/guard.test.ts` is byte-unchanged by this task (`git diff --stat TASK_BASE_SHA..HEAD -- hooks/guard.test.ts` empty, both shas quoted) and green.
    - The committed `checkpoints.js` and the hook manifest move in the same commit.
  </acceptance_criteria>
  <done>A redirection is read as what it is, on one grammar at both the split and the word; everything else the model could not read is still refused, by decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The deny names what fired, the transcript denies are replayed on stdin against the committed guard, and the two kept arms are dispositioned for the ledger</name>
  <read_first>
    - `hooks/guard.ts` :665-683 — the `escape` ternary (the `protected_branch_merge && !byPattern` arm precedes the `modelled.untokenizable` arm, which is why a `git log` refused for an unreadable sibling word printed the push sentence) and the `deny(...)` sentence it feeds.
    - `.planning/phases/33-live-capture-windows-portability/33-DIAGNOSIS.md` § 2 — the offline replay idiom (a `PreToolUse` / `Bash` payload on stdin with no `GRUGOPS_` variable in the environment; the six-line allow/deny table), and the "Reason class in the deny text" column of the fifteen-row table.
    - `hooks/guard.test.ts` — `expectDeny` / `expectAllow` helpers and how the deny reason text is asserted (`grep -n 'will not reason about\|command model' hooks/guard.test.ts`).
    - `scripts/checkpoints.ts` `CommandMatch` (:740-745) — whether the opaque WORDS are returned; the deny text needs the word to name it.
  </read_first>
  <files>hooks/guard.ts, hooks/guard.js, hooks/guard.test.ts, hooks/hook-entry.ts, hooks/hook-entry.js</files>
  <behavior>
    - Test G5 (RED first, the wrong sentence): `git log --oneline -5 $x` (an unreadable sibling word beside a governed tool) DENIES with an escape sentence that names the unreadable word and the substitution-or-expansion reason, and does NOT contain the `git push` sentence. On the base the deny carries the push sentence — quote it.
    - Test G6 (the push sentence still fires where it should): `git push` with no branch, readable, DENIES with the push sentence exactly as today (the RA1-3 case, unchanged).
    - Test G7 (stdin replay of the fifteen): each § 2 command fed to the committed `hooks/guard.js` on stdin as the diagnosis did — the redirection-only commands ALLOW (exit 0, no deny envelope), the `$var`/heredoc commands DENY with the corrected sentence naming the word; the probe `helm upgrade fake ./nope` still DENIES by literal pattern with no escape sentence.
  </behavior>
  <action>
    In `hooks/guard.ts`, order the escape decision so an untokenizable match is described as such
    before any tool-specific sentence: the unreadable-segment sentence comes first and names the
    word(s) the model would not read (return them from `matchCommandCheckpoints` as a field on
    `CommandMatch`, derived from the same classification — never re-scanned in the guard), the
    wording says "a shell substitution or expansion" (both are opaque by decision), and the
    push-without-a-branch sentence is printed only for a READABLE git match. Clear professional
    voice; no caveman register in the deny.

    Tests G5-G7 in `hooks/guard.test.ts`, RED before GREEN for G5; G7 spawns the committed
    `hooks/guard.js` with the payload on stdin and an environment scrubbed of every `GRUGOPS_`
    variable, the diagnosis's replay shape. Rebuild `hooks/guard.js`, then
    `npm run generate:hook-manifest`, then the parity and manifest gates.

    Record the disposition of the two kept arms in the summary for plan 33-34's ledger row:
    (1) a bare `$var` expansion stays opaque — its value is unknowable at hook time, and the
    availability cost (the mapper's `for s in …; do npm run "$s"; done` loop at B:562 is still
    refused) is accepted over reopening P30's fence; (2) a heredoc stays opaque — its body lines
    are commands to this tokenizer, and a hand-written note through a heredoc is exactly the
    route KIT (b) now refuses on read. Both with the transcript line ids as evidence.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' hooks/guard.test.ts -t "G5|G6|G7|names the word|push sentence"</automated>
    <fails_when>non-zero exit, or fewer than 3 tests reported passed</fails_when>
    <automated>OUT=$(printf '%s' '{"tool_name":"Bash","tool_input":{"command":"git log --oneline -5 2>&1"},"hook_event_name":"PreToolUse","cwd":"."}' | env -u GRUGOPS_PROD_DEPLOY_APPROVED node hooks/guard.js 2>&1); test -z "$(printf '%s' "$OUT" | grep -a 'permissionDecision')"</automated>
    <fails_when>non-zero exit — the committed guard still denies a redirected git log on stdin</fails_when>
    <automated>OUT=$(printf '%s' '{"tool_name":"Bash","tool_input":{"command":"helm upgrade fake ./nope"},"hook_event_name":"PreToolUse","cwd":"."}' | env -u GRUGOPS_PROD_DEPLOY_APPROVED node hooks/guard.js 2>&1); printf '%s' "$OUT" | grep -a -c 'Production deploy blocked'</automated>
    <fails_when>a count of 0 — the probe's literal-pattern deny no longer fires</fails_when>
    <automated>npm run build &amp;&amp; npm run generate:hook-manifest &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness:hook-manifest &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any command, or the vitest summary reports a failed test file</fails_when>
  </verify>
  <acceptance_criteria>
    - `hooks/guard.ts` asks `untokenizable` before the tool-specific sentence; the unreadable-word list comes from `CommandMatch`, and the guard contains no second scan of the command text for that purpose.
    - Tests G5-G7 exist; G5's RED commit precedes its GREEN commit (shas and the base's push sentence quoted); G7 replays the fifteen on stdin with the partition stated.
    - The summary carries the disposition record for `$var` and heredocs with transcript line ids, handed to plan 33-34 as one ledger row.
    - `hooks/guard.js`, `hooks/hook-entry.ts`/`.js` move in the same commit; the manifest gate, parity and the full excluded-lane suite exit 0.
  </acceptance_criteria>
  <done>A refusal says what it refused and why; the redirection class is closed on the transcripts that found it; the two arms kept closed are on the record with their reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-authored command text -> the guard's decision | The whole P30 red-team surface; every widening of what is "readable" is an attack surface widening |
| the split grammar -> the word grammar | Two grammars for one punctuation set drift (the `2>&1`-splits-at-`&` defect is exactly that) |
| the deny text -> the operator's next action | A denial a human cannot act on is a denial they disable the guard to get past (design rule 1's caveat) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-123 | Elevation of privilege | a governed verb smuggled past the model through a redirection word | critical | mitigate | Task 1: an anchored allow-list grammar; an operator glued to letters stays opaque; G3 is generated from the rules table; the P30 corpus is byte-unchanged and green. |
| T-33-124 | Elevation of privilege | `$var` or a heredoc made readable | critical | mitigate | Both stay opaque by decision (Task 2's disposition); the verify block asserts `echo "npm run $s"` is still untokenizable. |
| T-33-125 | Repudiation | a deny that names the wrong mechanism | medium | mitigate | Task 2: untokenizable asked first; the unreadable word named from the classification, never re-scanned. |
| T-33-126 | Denial of service | a redirected gate loop refused during a capture | medium | mitigate | Task 1 closes the redirection arm on the fifteen transcript commands; the residual (`$var`) is recorded. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run --exclude '**/scripts/e2e/**' && npm run check:nul-bytes` — the post-wave gate, green.
- `npm run build && npm run generate:hook-manifest && npm run check:build-parity && npm run freshness:hook-manifest` — committed twins and the manifest hash faithful.
- `hooks/guard.test.ts`'s P30 corpus cases unchanged in expectation and green.
</verification>

<success_criteria>
- The fifteen transcript commands partition into redirection-only (now allowed) and expansion/heredoc (still denied, with the corrected sentence), sizes summing to fifteen, proven on the committed guard on stdin.
- One grammar governs the split and the word; the mutation flips every new allow back and nothing else.
- The kept arms are dispositioned with reasons for the ledger.
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| the exported redirection grammar constant; `WordKind` `redirection` | module change, `scripts/checkpoints.ts` | one grammar at the split and the word |
| `CommandMatch` unreadable-word field | exported type change | feeds the deny sentence |
| the re-ordered escape sentence | module behavior, `hooks/guard.ts` | untokenizable named first |
| Tests G1-G7 | new test cases | red-first (G1, G5); G3 generated from the rules; G7 a stdin replay |
| the `$var` / heredoc disposition record | summary section | plan 33-34's ledger row |

<output>
Create `.planning/phases/33-live-capture-windows-portability/33-27-SUMMARY.md` when done
</output>

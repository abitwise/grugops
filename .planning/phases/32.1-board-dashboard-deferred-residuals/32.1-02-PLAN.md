---
phase: 32.1-board-dashboard-deferred-residuals
plan: 02
type: execute
wave: 2
depends_on: ["32.1-01"]
files_modified:
  - docs/audit/29-style-dispositions/32.1-02.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-RED-baseline.txt
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-GREEN-proof.txt
autonomous: true
requirements: [DASH-02]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-12: every clause `check:diff-disposition` reports against `agent-factory/workflows/05-pr-quality-gate.md` carries a disposition row in `docs/audit/29-style-dispositions/32.1-02.md`, and the gate's reported finding count falls by exactly that file's share."
    - "D-12: the finding count this plan closes is read from the gate's OWN output — its headline and its `clause:`-line tally, asserted to agree — never recalled and never estimated."
    - "D-12: the corpus is not narrowed, the base commit `4d2b8f0` is not moved, no override tier is added, and the clause comparison is not loosened. Each of those clears a finding by deleting its evidence."
  artifacts:
    - docs/audit/29-style-dispositions/32.1-02.md
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-RED-baseline.txt
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-GREEN-proof.txt
  key_links:
    - "`scripts/check-diff-disposition.ts:1343-1346` walks `docs/audit/29-style-dispositions/`, so a file named `32.1-02.md` is admitted by the walk without any registration step — the dot in the stem is irrelevant."
    - "A row matches a finding when it names the same file and EITHER its `before` or its `after` normalizes to the changed clause."
  prohibitions:
    - "Never move the recorded base commit in `docs/audit/29-style-dispositions/00-base.md`."
    - "Never narrow the watched corpus, add an override tier, or loosen the clause comparison."
    - "Never fake a gate result, a test result, or a citation."
    - "Never write a placeholder companion cell — the eight-word floor refuses every placeholder spelling by construction, which is the point."
    - "Never edit an evidence document in place."
    - "Never run `npm test`."
---

<objective>
Close the first and largest share of WINDOWS.md row 176 by writing the disposition rows the
`check:diff-disposition` gate asks for over `agent-factory/workflows/05-pr-quality-gate.md` — 38 of
the 78 findings measured RED on this tree today (D-12). No corpus narrowing, no base-commit move:
both are named as forbidden by the gate's own message and by `00-base.md`'s body, and each would
clear the finding by deleting the evidence for it.

Purpose: row 176 has been carried since 2026-09-14 as "pre-existing, verified identical" and is the
only reason the repository's own gate sweep cannot run clean. The rows are judgements, not a
mechanical transform — `docs/audit/29-style-dispositions/README.md:88-90` states it plainly: the
register records that a change was CONSIDERED, not that the consideration was correct. Budget the
content accordingly.
Output: one new audit document, `docs/audit/29-style-dispositions/32.1-02.md`, plus the measured
before/after finding counts filed as this plan's transcript pair.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-CONTEXT.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-RESEARCH.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Produced by THIS plan: `docs/audit/29-style-dispositions/32.1-02.md` (a new audit record with a
`## Dispositions` table), `32.1-02-RED-baseline.txt`, `32.1-02-GREEN-proof.txt`. No source file, no
export, no test name, no CLI shape and no ledger row is created here. Plan 03 produces the sibling
file `32.1-03.md` covering the remaining four watched documents.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Measure the gate from its own output, and record the RED baseline honestly</name>
  <files>.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-RED-baseline.txt</files>
  <read_first>
    - `docs/audit/29-style-dispositions/00-base.md` — the frontmatter `base_commit` and the body paragraph stating that moving this SHA is not a remedy for a red gate.
    - `docs/audit/29-style-dispositions/README.md:32-48` and `:88-90` and `:112-115` — the row form, the judgement-versus-coverage disclaimer, and the list of remedies that are NOT available.
    - `docs/audit/29-style-dispositions/29-44.md` — the whole document, as the shape to copy: title, the paragraph naming what forced the change, the disclaimer, `## What forced the change, stated before what was done`, `## The fix that was available and was refused`, then `## Dispositions` with the seven-column table at `:93-97`.
    - `scripts/check-diff-disposition.ts:265` (`DISPOSITION_NON_ROWS`), `:1343-1346` (the walk), `:1267-1278` (`isCompanionFilled`), and `scripts/voice-model.ts:526` (`CLAUSE_MIN_WORDS = 4`, so the companion floor is 8 normalized words).
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-RESEARCH.md` §D-12 in full, including the per-file tallies and the 77-versus-78 explanation.
  </read_first>
  <action>
Run `npm run check:diff-disposition` and capture its complete stdout to
`.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-RED-baseline.txt`, including the
exit code. Do not edit the capture.

Derive the finding count TWO ways from that one capture and assert they agree before writing a
single row — rounds 3 and 4 of Phase 32 each burned a paragraph on an instrument artefact here. The
headline count sits on the `FAIL diff disposition — changed watched file(s)` line. The derived count
is the number of `clause:` lines. If they disagree, stop and report the disagreement rather than
picking one. Note in the transcript that counting only the lines marked as added gives one fewer
than the total, because exactly one finding on this tree is a removal — that is the explanation for
the 77-versus-78 arithmetic, and it is not a defect.

Derive the per-file tally from the same capture by extracting the leading file path of each finding
line and counting occurrences. Record the tally in the transcript. This plan owns exactly the
`agent-factory/workflows/05-pr-quality-gate.md` share; plan 03 owns the other four documents.

One name in the gate's output is NOT a finding: `agent-factory/roles/_role-switch-protocol.md`
appears once inside the corpus-description line as the uncounted safety-surface register row. A
reader who greps the output for file paths gets one name too many. Say so in the transcript so the
next reader does not chase it.

Finally, derive for each of this plan's findings the change that produced it, by reading
`git log <base_commit>..HEAD -- agent-factory/workflows/05-pr-quality-gate.md` and the diffs it
names. The `rule` column needs the profile rule id or the decision that drove each clause; it is
read out of the commit that made the change, never invented. Record the commit-to-clause mapping in
the transcript so the table below can be audited against it.
  </action>
  <acceptance_criteria>
    - `32.1-02-RED-baseline.txt` exists and contains the gate's complete stdout plus its exit code, which is 1.
    - The transcript records the headline finding count and the `clause:`-line count and states that they agree.
    - The transcript records the per-file tally with five file names, summing to the headline count.
    - The transcript records the `base_commit` value read from `docs/audit/29-style-dispositions/00-base.md`'s frontmatter, unchanged.
    - The transcript records the commit-to-clause mapping for every finding this plan will write a row for.
    - `git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md` exits 0 — the base document is untouched.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-RED-baseline.txt &amp;&amp; grep -c 'clause: ' .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-RED-baseline.txt</automated>
    <fails_when>non-zero exit (the transcript is missing or empty), or the printed count is 0</fails_when>
    <automated>git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md</automated>
    <fails_when>non-zero exit — the base document changed, which is the forbidden remedy</fails_when>
  </verify>
  <done>The gate's red state is captured, its count is agreed two ways from its own output, the per-file tally is recorded, and every clause this plan will dispose is joined to the commit that produced it.</done>
</task>

<task type="auto">
  <name>Task 2: Write the disposition rows for the pull-request quality-gate workflow</name>
  <files>docs/audit/29-style-dispositions/32.1-02.md, .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-GREEN-proof.txt</files>
  <read_first>
    - `docs/audit/29-style-dispositions/29-44.md:93-97` — the seven-column row form, in order: file, line, before, after, rule, disposition, companion. Both a changed row and an unchanged-text-on-a-changed-line row are shown.
    - `docs/audit/29-style-dispositions/31-41.md` — a live filled table at this scale, for the register's voice.
    - `agent-factory/workflows/05-pr-quality-gate.md` — the document the rows are about.
    - `32.1-02-RED-baseline.txt` written by Task 1 — the authoritative finding list and the commit mapping.
    - `scripts/check-diff-disposition.ts:1267-1278` — `isCompanionFilled`, and the docblock at `:1247-1258` recording that the smallest non-placeholder companion cell in the live register is 12 normalized words.
  </read_first>
  <action>
Create `docs/audit/29-style-dispositions/32.1-02.md` following `29-44.md`'s document shape: a title
naming this plan; a paragraph naming what forced the change; the judgement-versus-coverage
disclaimer restated in this document's own words; a `## What forced the change, stated before what
was done` section carrying Task 1's measurement (the headline count, the derived count, the per-file
tally, the base commit); a `## The fix that was available and was refused` section enumerating the
four weakenings the gate's own message names — narrowing the watched corpus, adding an override
tier, moving the base commit forward, loosening the clause comparison — and stating that none was
taken; then `## Dispositions`.

Under `## Dispositions`, write one row per finding against
`agent-factory/workflows/05-pr-quality-gate.md`, with the seven columns in the order `29-44.md` uses.
Rules for the cells:

- `file` is the repo-relative path exactly as the gate prints it. `line` is the line number the gate
  prints.
- For a finding the gate marks as added, `before` is an em dash and `after` is the clause. For the
  one marked as removed, the sides swap. A row matches when it names the file and EITHER side
  normalizes to the clause, so writing the clause on the correct side is what makes the row bind.
- `rule` is the profile rule id or decision Task 1 read out of the producing commit. If a commit
  names none, write the commit's own subject line rather than inventing a rule id — an unknown that
  is stated is admissible here and a fabricated one is not.
- `disposition` is the judgement: why the clause reads the way it now does, in this register's clear
  professional voice. Not caveman voice — this is a quality and trace surface.
- `companion` is an em dash for a non-frozen row. None of this file's findings is frozen (all three
  frozen findings on this tree are in the context-compaction workflow and belong to plan 03), so
  every companion cell in this table is an em dash. Confirm that against Task 1's capture rather
  than assuming it.

Write every row. A placeholder cell is refused by construction — every placeholder spelling
normalizes to at most one word against an eight-word floor — so a partial table is a red gate, not a
partial credit.

Then re-run the gate and record the new finding count in
`32.1-02-GREEN-proof.txt`. The gate will still exit 1 at this point, because plan 03 owns the
remaining four documents; the assertion this plan makes is that the count fell by exactly this
file's share and that no remaining finding names
`agent-factory/workflows/05-pr-quality-gate.md`. State that expectation in the proof transcript
rather than letting a reader mistake a non-zero exit for a failure of this plan.
  </action>
  <acceptance_criteria>
    - `docs/audit/29-style-dispositions/32.1-02.md` exists, carries a `## Dispositions` heading, and its table header row is exactly `file | line | before | after | rule | disposition | companion`.
    - The number of data rows in the table equals the number of findings naming `agent-factory/workflows/05-pr-quality-gate.md` in `32.1-02-RED-baseline.txt`.
    - After the rows land, `npm run check:diff-disposition` reports ZERO findings naming `agent-factory/workflows/05-pr-quality-gate.md`.
    - After the rows land, the gate's headline finding count equals the Task-1 headline count minus this file's share, and that arithmetic is written into `32.1-02-GREEN-proof.txt`.
    - `git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/` exits 0 — no watched document and no base document was edited to reach this result.
    - `npm run check:nul-bytes` exits 0 — the new audit document carries no raw control byte.
  </acceptance_criteria>
  <verify>
    <automated>npm run check:diff-disposition 2>&amp;1 | grep -c '05-pr-quality-gate.md:[0-9]* (' || true</automated>
    <fails_when>the printed count is anything other than 0 — a finding against that workflow survived</fails_when>
    <automated>git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/</automated>
    <fails_when>non-zero exit — a watched document or the base document was edited to reach green</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the output names a file under `docs/audit/`</fails_when>
  </verify>
  <done>Every finding against the pull-request quality-gate workflow has a binding disposition row; the gate names that file zero times; the base document and every watched document are byte-unchanged; the count arithmetic is recorded in the proof transcript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate stdout → the row table | the gate's output is the only authority for what must be dispositioned; a hand-recalled list is a fabricated one |
| audit register → future readers | these rows are audit evidence a later reader relies on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-02-01 | Repudiation | a finding cleared by deleting its evidence | high | mitigate | The base commit, the watched corpus, the override tiers and the clause comparison are all left untouched, asserted by `git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/` in both tasks' verify sets. |
| T-32.1-02-02 | Repudiation | a placeholder companion cell passing as a considered judgement | medium | mitigate | Structurally impossible: `isCompanionFilled` requires 8 normalized words and every placeholder spelling normalizes to at most one. Confirmed here rather than assumed, because this file's rows are all non-frozen and carry em dashes. |
| T-32.1-02-03 | Spoofing | a row that names a file but binds to no finding | medium | mitigate | The acceptance criterion is measured from the gate's own re-run output (zero findings naming the file), not from the row count alone. |
| T-32.1-02-04 | Tampering | a raw control byte entering a published audit document | medium | mitigate | `npm run check:nul-bytes` is in the verify set. Every control character in this phase's documents is written as escape text, per the precedent commit that already had to fix this once. |
| T-32.1-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package; `32.1-RESEARCH.md` § Package Legitimacy Audit records zero additions and zero `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts. |
</threat_model>

<verification>
1. `npm run check:diff-disposition` — exits 1, and reports zero findings naming
   `agent-factory/workflows/05-pr-quality-gate.md`. The residual findings belong to plan 03.
2. `npm run check:nul-bytes` — exits 0.
3. `git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/` — exits 0.
4. `npx vitest run --exclude '**/scripts/e2e/**'` — the full regression lane, exits 0 and matches the
   count `32.1-01-GREEN-proof.txt` recorded.
</verification>

<success_criteria>
- One new audit document with a complete, binding `## Dispositions` table.
- The gate names the pull-request quality-gate workflow zero times.
- The base commit and every watched document are byte-unchanged.
- Both transcripts exist and each records a measurement rather than a claim.
</success_criteria>

<output>
Create `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-SUMMARY.md` when done.
</output>

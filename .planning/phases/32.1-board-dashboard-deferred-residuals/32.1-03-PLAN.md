---
phase: 32.1-board-dashboard-deferred-residuals
plan: 03
type: execute
wave: 3
depends_on: ["32.1-02"]
files_modified:
  - docs/audit/29-style-dispositions/32.1-03.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-RED-baseline.txt
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-GREEN-proof.txt
autonomous: true
requirements: [DASH-02]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-12: every remaining clause `check:diff-disposition` reports — over the UAT-pack, context-read-write, task-claim and context-compaction workflows — carries a binding disposition row, and `npm run check:diff-disposition` exits 0."
    - "D-12: each of the three FROZEN findings carries a companion cell of at least eight normalized words naming a same-commit companion edit, and the frozen set is identified from the gate's own output rather than recalled."
    - "D-12: WINDOWS.md row 176 reaches its terminal disposition as closed-with-reproduction — the gate is measured RED before (78 findings over 39 elements) and green after, both counts read from the gate's own output."
  artifacts:
    - docs/audit/29-style-dispositions/32.1-03.md
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-RED-baseline.txt
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-GREEN-proof.txt
  key_links:
    - "`scripts/check-diff-disposition.ts:1343-1346` admits `32.1-03.md` by walking the directory; `DISPOSITION_NON_ROWS` at `:265` excludes only `00-base.md` and `README.md`."
    - "The three frozen findings are all inside `agent-factory/workflows/18-context-compaction.md`'s `## Stop conditions` region; `isCompanionFilled` at `:1267-1278` is what decides whether their companion cells bind."
  prohibitions:
    - "Never move the recorded base commit in `docs/audit/29-style-dispositions/00-base.md`."
    - "Never narrow the watched corpus, add an override tier, or loosen the clause comparison."
    - "Never fake a gate result, a test result, or a citation."
    - "Never write a placeholder companion cell."
    - "Never edit an evidence document in place."
    - "Never run `npm test`."
---

<objective>
Finish WINDOWS.md row 176 (D-12): write the disposition rows for the remaining four watched
documents — the UAT-pack workflow, the context read/write workflow, the task-claim workflow and the
context-compaction workflow — and take `check:diff-disposition` from red to exit 0. Three of these
findings are FROZEN, sitting inside the context-compaction workflow's stop-conditions region, and a
frozen finding binds only when its companion cell carries a real same-commit companion edit of at
least eight normalized words.

Purpose: row 176 is the one ledger item that blocks the repository's whole gate sweep, and it has
been carried since 2026-09-14 on the strength of "pre-existing, verified identical". The remedy the
gate itself names is to write the row. Plan 02 closed the largest single share; this plan closes the
rest and is the one that gets to assert exit 0.
Output: `docs/audit/29-style-dispositions/32.1-03.md`, the green gate, and the measured before/after
counts that make row 176's terminal disposition a reproduction rather than a claim.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-CONTEXT.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-RESEARCH.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-PATTERNS.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Produced by THIS plan: `docs/audit/29-style-dispositions/32.1-03.md` (a new audit record with a
`## Dispositions` table carrying the three frozen rows and their companion cells),
`32.1-03-RED-baseline.txt`, `32.1-03-GREEN-proof.txt`. This plan produces the evidence that closes
ledger row 176; the row's status transition itself is written by plan 11, which owns every ledger
mutation in this phase.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The twenty-five plus ten plus two — the three non-frozen documents</name>
  <files>docs/audit/29-style-dispositions/32.1-03.md, .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-RED-baseline.txt</files>
  <read_first>
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-RED-baseline.txt` and `32.1-02-GREEN-proof.txt` — plan 02's measurement, including the per-file tally and the base commit, so this plan does not re-derive what is already recorded.
    - `docs/audit/29-style-dispositions/32.1-02.md` — the sibling document whose shape and voice this one matches.
    - `docs/audit/29-style-dispositions/29-44.md:93-97` — the seven-column row form, in order.
    - `agent-factory/workflows/06-uat-pack.md`, `agent-factory/workflows/16-context-read-write.md`, `agent-factory/workflows/17-task-claim.md` — the three documents these rows are about.
    - `docs/audit/29-style-dispositions/README.md:88-90`, `:112-115` — the judgement disclaimer and the list of remedies that are not available.
  </read_first>
  <action>
Re-run `npm run check:diff-disposition` and capture its complete stdout to
`32.1-03-RED-baseline.txt` with its exit code. This is a fresh measurement, not a copy of plan 02's:
plan 02's rows have landed, so the count has already moved, and the number this plan must close is
whatever the gate reports NOW. Derive the count two ways from this capture as plan 02 did — the
headline and the `clause:`-line tally — and assert they agree before writing a row.

Create `docs/audit/29-style-dispositions/32.1-03.md` with the same document shape plan 02 used: a
title naming this plan, the paragraph naming what forced the change, the judgement-versus-coverage
disclaimer, `## What forced the change, stated before what was done` carrying this plan's own
measurement, `## The fix that was available and was refused` naming the four weakenings and stating
that none was taken, then `## Dispositions`.

Write one row per finding against the UAT-pack workflow, the context read/write workflow and the
task-claim workflow. Same cell rules as plan 02: the file path and line exactly as the gate prints
them; the clause on the side matching whether the gate marks it added or removed; a `rule` cell read
out of the producing commit via `git log <base_commit>..HEAD -- <file>` rather than invented; a
`disposition` cell carrying the judgement in clear professional voice; and an em dash for the
`companion` cell, because none of these three documents carries a frozen finding. Confirm that last
point against this task's own capture rather than assuming it — the frozen marker appears in the
gate's finding line, so it is readable, not inferable.

Leave the context-compaction workflow's three frozen findings for Task 2. Do not start a row for
them here; a frozen row with an em dash companion is a row that does not bind, and a half-written
one is worse than none.
  </action>
  <acceptance_criteria>
    - `32.1-03-RED-baseline.txt` exists, carries the gate's complete stdout and exit code 1, and records the headline count and the `clause:`-line count agreeing.
    - `docs/audit/29-style-dispositions/32.1-03.md` exists with a `## Dispositions` heading and a header row of exactly `file | line | before | after | rule | disposition | companion`.
    - After this task, `npm run check:diff-disposition` reports zero findings naming `agent-factory/workflows/06-uat-pack.md`, zero naming `agent-factory/workflows/16-context-read-write.md`, and zero naming `agent-factory/workflows/17-task-claim.md`.
    - Every remaining finding after this task names `agent-factory/workflows/18-context-compaction.md`, and there are exactly three of them.
    - `git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run check:diff-disposition 2>&amp;1 | grep -cE '(06-uat-pack|16-context-read-write|17-task-claim)\.md:[0-9]+ \(' || true</automated>
    <fails_when>the printed count is anything other than 0 — a finding against one of the three documents survived</fails_when>
    <automated>git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/</automated>
    <fails_when>non-zero exit — a watched document or the base document was edited to reach this result</fails_when>
  </verify>
  <done>The three non-frozen documents have complete, binding disposition rows; the only findings the gate still reports are the three frozen ones in the context-compaction workflow; nothing watched and nothing base was edited.</done>
</task>

<task type="auto">
  <name>Task 2: The three frozen findings, each with a real companion edit, and the gate at exit 0</name>
  <files>docs/audit/29-style-dispositions/32.1-03.md, .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-GREEN-proof.txt</files>
  <read_first>
    - `agent-factory/workflows/18-context-compaction.md`, the `## Stop conditions` section — the frozen region the three findings sit in.
    - `scripts/check-diff-disposition.ts:1267-1278` — `isCompanionFilled` and the docblock at `:1247-1258` recording the measured floor: across 1532 live rows the smallest non-placeholder companion cell is 12 normalized words, and the smallest one standing on a live structural intersection is 20.
    - `scripts/voice-model.ts:526` — `CLAUSE_MIN_WORDS = 4`; the companion floor is twice that.
    - `docs/audit/29-style-dispositions/29-44.md` — a filled frozen row, for the companion cell's register and length.
    - `docs/audit/29-style-dispositions/00-base.md:22-31` — the fail-closed paragraph and the sentence stating that moving the SHA is not a remedy.
    - `32.1-03-RED-baseline.txt` from Task 1 — the exact three clauses and the region they belong to.
  </read_first>
  <action>
Add three rows to `docs/audit/29-style-dispositions/32.1-03.md` under the same `## Dispositions`
heading, one per frozen finding in `agent-factory/workflows/18-context-compaction.md`'s stop-conditions
region. Take the clause text verbatim from the gate's own capture.

The `companion` cell is what makes a frozen row bind. It must name the same-commit companion edit —
the other place the repository states the same fact, and why the two move together. For these three
clauses that companion is the operation count the compaction workflow states for each repository
shape, and the sentence must say where the count is DERIVED rather than restating the number. Write
each cell as a real sentence of at least twelve normalized words; the mechanical floor is eight and
the live register's smallest honest cell is twelve, so eight is the refusal threshold and not the
target. An em dash, the word none, a single-word glyph or any placeholder spelling normalizes to at
most one word and is refused by construction — that refusal is the control, not an obstacle.

Then run `npm run check:diff-disposition` and capture its complete stdout with exit code to
`32.1-03-GREEN-proof.txt`. Write into that transcript the full arithmetic that makes row 176's
closure a reproduction: the count measured before plan 02 started, the count after plan 02, the
count after Task 1 of this plan, and the count now. Each number read from a gate capture, none
recalled.

Record in the SUMMARY that ledger row 176's evidence is complete and that the row's status
transition is owned by plan 11, which is the only plan in this phase permitted to write to the
ledger — hand-editing the ledger throws a malformed-ledger error because the frontmatter counters
are cross-checked against the entries.
  </action>
  <acceptance_criteria>
    - `docs/audit/29-style-dispositions/32.1-03.md` carries exactly three rows naming `agent-factory/workflows/18-context-compaction.md`, each with a companion cell of at least twelve normalized words.
    - `npm run check:diff-disposition` exits 0 and its PASS line carries the measurement.
    - `32.1-03-GREEN-proof.txt` records four counts in order — before plan 02, after plan 02, after Task 1, and now — each attributed to the capture it was read from, with the final one being zero findings.
    - `git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/ .planning/WINDOWS.md` exits 0 — no watched document, no base document and no ledger row was touched to reach exit 0.
    - `npm run check:nul-bytes` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run check:diff-disposition</automated>
    <fails_when>non-zero exit, or "CHECK(S) FAILED" in the Result block</fails_when>
    <automated>git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/ .planning/WINDOWS.md</automated>
    <fails_when>non-zero exit — a watched document, the base document or the ledger was edited to reach green</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the output names a file under `docs/audit/`</fails_when>
  </verify>
  <done>All three frozen findings carry binding companion cells; the gate exits 0; the four-count arithmetic is filed; row 176's evidence is complete and its ledger transition is handed to plan 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate stdout → the row table | the gate's output is the only authority for what must be dispositioned |
| frozen region → companion cell | a frozen clause is a safety-surface sentence; its companion cell is the evidence that it did not move alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-03-01 | Repudiation | a frozen safety clause changed without its companion | high | mitigate | Each of the three frozen rows carries a companion cell of twelve or more normalized words naming the same-commit companion edit; `isCompanionFilled`'s eight-word floor is the mechanical refusal and is asserted by the gate exiting 0. |
| T-32.1-03-02 | Repudiation | exit 0 reached by deleting the evidence | high | mitigate | `git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/ .planning/WINDOWS.md` is in the verify set for Task 2, so a green reached by moving the base, editing a watched document or waiving the row reds instead. |
| T-32.1-03-03 | Spoofing | a count recalled rather than measured | medium | mitigate | The four-count arithmetic in `32.1-03-GREEN-proof.txt` attributes every number to the capture it was read from, and Task 1 re-measures rather than copying plan 02's capture. |
| T-32.1-03-04 | Tampering | a raw control byte entering a published audit document | medium | mitigate | `npm run check:nul-bytes` is in both tasks' verify sets. |
| T-32.1-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package; `32.1-RESEARCH.md` § Package Legitimacy Audit records zero additions and zero `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts. |
</threat_model>

<verification>
1. `npm run check:diff-disposition` — exits 0.
2. `npm run check:nul-bytes` — exits 0.
3. `git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/ .planning/WINDOWS.md` — exits 0.
4. `npx vitest run --exclude '**/scripts/e2e/**'` — the full regression lane, exits 0.
</verification>

<success_criteria>
- `check:diff-disposition` exits 0 for the first time since 2026-09-14.
- Every frozen finding carries a binding companion cell.
- Row 176's closure is a reproduction: four measured counts, each from a named capture.
- Nothing watched, nothing base, and no ledger row was edited to get there.
</success_criteria>

<output>
Create `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-SUMMARY.md` when done.
</output>

---
phase: 32.1-board-dashboard-deferred-residuals
plan: 04
type: execute
wave: 4
depends_on: ["32.1-01", "32.1-03"]
files_modified:
  - scripts/validate.test.ts
  - scripts/check-diff-disposition.ts
  - scripts/check-diff-disposition.js
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-04-RED-baseline.txt
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-04-GREEN-proof.txt
autonomous: true
requirements: [DASH-01, DASH-02]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01: the split-reader join matches by RESOLVED DECLARATION, so a namespace import, a two-hop re-export, a renamed named import and a direct named import of the same key table are all one offender — closing F-15."
    - "D-01: the join is asked in BOTH directions — a file that names both key spellings and takes its scanning half from a scanned file is an offender too — closing WR-04, and the banner's claim that the other direction is impossible is deleted rather than qualified."
    - "D-04: `NOT_A_SECOND_AUTHORITY` is re-derived under the checker; each surviving entry carries a reason stated in terms the checker reads; the count is re-pinned two-sided from the derived set and its message says the number is a decision, never a bumped constant."
    - "D-15: row 194 closes — `scripts/check-diff-disposition.ts` stops supplying BOTH key spellings as string literals, so the census stops detecting it and its registry entry is deleted with a deletion note, in the same commit as the reword."
    - "D-21: row 210 ends as re-homed-with-named-owner — `scripts/board-tracer.test.ts` names one key spelling in twelve distinct string literals plus both spellings in a planted fixture, so a symbol-resolving census still trips it and the ninth entry stands with that measurement as its recorded reason."
  artifacts:
    - scripts/validate.test.ts
    - scripts/check-diff-disposition.ts
    - scripts/check-diff-disposition.js
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-04-RED-baseline.txt
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-04-GREEN-proof.txt
  key_links:
    - "`scripts/validate.test.ts` imports `createScriptsProgram` and `declarationOf` from `./ts-symbols.test-support.js` (plan 01) and asks the checker which declaration a key-table binding resolves to."
    - "The registry-entry deletion, the literal reword in `scripts/check-diff-disposition.ts`, and the count pin move all land in ONE commit — the exemption-liveness assertion reds if a named file stops being detected while its entry still stands."
  prohibitions:
    - "Never widen a parser by adding another arm where a canonical form is called for."
    - "Never type a count literal that should be derived from the set it counts."
    - "Never touch `scripts/check-diff-disposition.ts:1058` — it is a git subcommand argument, not prose, and removing it changes behaviour."
    - "Never let the checker instrument be imported into `scripts/context-io-writer-set.test.ts`, and never let that file change at all."
    - "Never fake a gate result, a test result, or a citation."
    - "Never run `npm test`."
---

<objective>
Cut the ticket-frontmatter one-authority census over to the resolving instrument plan 01 landed, and
close the three items that depend on it: F-15 (the split-reader refusal enumerates one import shape),
WR-04 (the join is only asked in one direction, and the banner says the other direction is
impossible), and ledger row 194 (a production module holds a file-scoped exemption). Then record row
210's terminal disposition honestly — a checker does NOT retire it, and saying otherwise would be a
completion item that never completes (D-21).

Purpose: F-15 and WR-04 are the same defect on two axes — the enumerated thing is an import SHAPE in
one and an import DIRECTION in the other. Both are instances of the class this phase exists to
close, and both are closed by asking the checker which DECLARATION a name resolves to instead of
comparing identifier text. D-19's order is respected: the instrument was watched refusing something
in plan 01 before any rule was cut over to it.
Output: a symbol-resolving split-reader join asked both ways, a re-derived exemption registry with
its count moved as a decision, one reworded operator message, and one deleted exemption.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-CONTEXT.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-RESEARCH.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-PATTERNS.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Produced by THIS plan: a symbol-resolving `splitReaderOffenders` in `scripts/validate.test.ts` with
a both-directions join and new discrimination cases; a re-derived `NOT_A_SECOND_AUTHORITY` registry
with a moved two-sided count pin and a deletion note beside each removed entry; a reworded operator
message in `scripts/check-diff-disposition.ts` and its rebuilt `.js` twin;
`32.1-04-RED-baseline.txt` and `32.1-04-GREEN-proof.txt`. Row 194's and row 210's ledger transitions
are written by plan 11, which owns every ledger mutation in this phase.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The split-reader join resolves declarations, and is asked in both directions</name>
  <files>scripts/validate.test.ts, .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-04-RED-baseline.txt</files>
  <reversibility rating="costly">The exemption registry in Task 2 is re-derived from this join; reverting means re-typing the enumeration this task deletes.</reversibility>
  <read_first>
    - `scripts/validate.test.ts:1713-1743` (`findTicketReaders`'s conjunction), `:1757-1811` (`readerHalves`), `:1775-1790` (`staticText`), `:1806-1809` (`namesBothKeys`), `:1943-1946` and `:2029-2032` (the two copies of the banner carrying the false totality claim), `:2002` (the guard clause `if (!h.scans || h.namesBothKeys) continue;`), `:2059-2113` (the existing discrimination case).
    - `scripts/ts-symbols.test-support.ts` from plan 01 — `createScriptsProgram`, `declarationOf`, `followAlias`, `trackedScriptSources`.
    - `.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md` § 12, finding F-15 and its § 9 reproduction — the namespace-import reader and the two-hop re-export reader, both built as real working readers that returned a parsed ticket.
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md:212-280` — WR-04 in full, including the two-file converse plant quoted verbatim and the fix sketch showing both join directions.
    - `scripts/context-io-writer-set.test.ts:263-299` — the mutation-mirror pattern: the plant is generated by appending to the live source, never authored as a fixture.
  </read_first>
  <action>
Replace the identifier-text half of the split-reader join with a declaration-resolving one (D-01).
Build the program once through `createScriptsProgram` and reuse it for the whole census — one
instrument, built once, asked many times, as D-01 requires.

For each scanned file, resolve every reference to a key table through `declarationOf`, which follows
import and export aliases to the declaration they name. A named import, a renamed named import, a
namespace import and a two-hop re-export all resolve to the SAME declaration, so all four land on
one offender instead of one of them landing and three escaping. Delete the shape enumeration the
old join used rather than adding arms to it; the canonical form here is "which declaration does this
name resolve to", and a spelling outside it is a refusal, not a new case.

Ask the join in both directions (WR-04). Direction one, which exists today: a file that scans text
and does not name both key spellings, taking a key table from a scanned file. Direction two, which
is missing: a file that names both key spellings and does not scan, taking its scanning half from a
file whose own half does scan. The guard clause at `:2002` is what makes the second direction
invisible today — it continues past exactly the files the converse offender is. Restructure the loop
so both directions are evaluated rather than adding a second loop that could drift from the first.

Delete the false totality sentence from BOTH copies of the banner. It reads that the only way to
assemble the pair across files is for the key spellings to reach the scanning file through an import;
WR-04's working two-file plant disproves it. Replace it with a sentence naming what the join now
decides and what it still does not — the header doctrine on this tree is to state the boundary, not
to imply there is none.

Discrimination, RED-first and mutation-proven (D-19). Generate each plant by writing into a temp
mirror built from the live source, never as an authored fixture, so it cannot drift from the thing
it copies. Four plants, each asserted to red with the offender NAMED, then removed:

1. A key table plus a namespace-import reader — F-15's first reproduction.
2. A key table plus a two-hop re-export through a barrel — F-15's second reproduction.
3. The renamed named import — the existing control, which must keep redding.
4. WR-04's converse pair: the file naming both key spellings imports the scanning half from the file
   that scans and names no key. The offender message must name both files and state the direction.

Capture every plant's output and exit code in `32.1-04-RED-baseline.txt`. After each, restore and
confirm `git status --porcelain -- scripts/` is empty. Assert
`git diff --exit-code -- scripts/context-io-writer-set.test.ts` after the last one — residual
`R-31-21-04` is a TEXT match over that file and its expected value is the absence of a checker
there.
  </action>
  <acceptance_criteria>
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts` exits 0.
    - Four discrimination cases exist — namespace import, two-hop re-export, renamed named import, and the converse direction — and each names its offender file in the failure message.
    - Each of the four plants, run against the pre-change code, is recorded in `32.1-04-RED-baseline.txt` with the exit code the OLD rule produced (exit 0 for the three new shapes, exit 1 for the renamed control) and the exit code the NEW rule produces (exit 1 for all four).
    - Neither copy of the banner contains a claim that the other import direction is impossible; each states what the join decides and what it does not.
    - `git diff --exit-code -- scripts/context-io-writer-set.test.ts` exits 0.
    - `git status --porcelain -- scripts/` is empty after every plant is removed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>git diff --exit-code -- scripts/context-io-writer-set.test.ts</automated>
    <fails_when>non-zero exit — the file changed, which reds residual R-31-21-04</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit on any of the three tsc targets</fails_when>
  </verify>
  <done>The join resolves declarations rather than comparing identifier text; both directions are asked from one loop; four plants each red with the offender named and each was proven green on the old rule first; the totality claim is gone from both banners.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive the exemption registry under the checker and move its count as a decision</name>
  <files>scripts/validate.test.ts</files>
  <read_first>
    - `scripts/validate.test.ts:1847-1892` — the nine registry entries with their reasons, verbatim.
    - `scripts/validate.test.ts:1893` — the count constant; `:2236-2246` — the exemption-liveness iteration; `:2248-2252` — the two-sided pin.
    - `scripts/context-io-writer-set.test.ts:250-261` — the members-then-count pattern: two cases, never one, and the count message saying the number is a decision.
    - `32.1-RESEARCH.md` §D-04 in full, including the measured probe output for both files and the three honest routes for row 210.
  </read_first>
  <action>
Run the re-derived census from Task 1 over the tracked set and record, for each of the nine registry
entries, whether it still trips. Write the measurement into `32.1-04-RED-baseline.txt` before
changing the registry — the registry is a set literal, and this repository's second systemic failure
class is a hand-maintained set literal that rots while the suite stays green.

For every entry the re-derived census no longer trips: delete it and leave a deletion note in the
form `scripts/js-import-closure.ts:464-470` establishes — name the finding, quote the claim that
kept the entry, and state the measurement that disproved it. For every entry that survives: its
reason must be stated in terms the checker reads. A reason that argues about intent without naming
what the census actually sees is a reason that cannot be re-checked, and rewriting those is part of
this task, not a later tidy-up.

Move the count constant to whatever the derived set now contains, in the SAME commit as the
deletions, and rewrite its message so it says the number is a decision that moved because the set
moved. Keep the two-sided shape: a members case and a separate count case, never one case doing
both. Do not type a number the derivation did not produce, and do not leave the derivation and the
constant disagreeing at any commit boundary — the liveness assertion reds if a named file stops
being detected while its entry still stands.

Record row 210's disposition explicitly in the registry entry's own reason text. The ninth entry is
`scripts/board-tracer.test.ts`, and the measurement is that it names one key spelling in twelve
distinct string literals — eleven test descriptions and one planted ticket fixture — and both
spellings in that fixture. A declaration-resolving join moves which FILE the scanning half is
attributed to; it does not move a whole-file literal census, so the entry stands. Write that
measurement into the reason so a later reader sees why the entry survived a checker cutover rather
than assuming it was overlooked (D-21).
  </action>
  <acceptance_criteria>
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts` exits 0.
    - `32.1-04-RED-baseline.txt` records, per registry entry, whether the re-derived census still trips it, measured before any entry was deleted.
    - Every deleted entry has a deletion note naming the finding, the claim that kept it, and the measurement that disproved it.
    - The count constant equals the number of entries the derivation produced; the members case and the count case are separate cases; the count message states the number is a decision.
    - The ninth entry's reason carries the twelve-literal measurement and names row 210 as re-homed with this file as its owner.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit on any of the three tsc targets</fails_when>
  </verify>
  <done>Every registry entry has been re-measured under the checker; what no longer trips is deleted with a note; what survives carries a checker-readable reason; the count moved with the set in one commit; row 210's reason records the measurement that keeps it.</done>
</task>

<task type="auto">
  <name>Task 3: Row 194 — one operator message reworded, one exemption deleted, one commit</name>
  <files>scripts/check-diff-disposition.ts, scripts/check-diff-disposition.js, scripts/validate.test.ts</files>
  <read_first>
    - `scripts/check-diff-disposition.ts:1630` — the operator message carrying the second key spelling. **Read `:1058` too, and do not change it**: it is the git subcommand argument inside the working-tree dirty check, and a diff touching that line is the named warning sign.
    - `scripts/validate.test.ts:1806-1809` — `namesBothKeys` uses an every-key conjunction, so dropping ONE spelling is sufficient to stop detection.
    - `scripts/validate.test.ts:1775-1790` — `staticText`, which resolves string literals, no-substitution template literals, regex literals, concatenations and joined arrays. It does NOT see comments; comments are trivia, not nodes.
    - `scripts/validate.test.ts:2236-2246` — the exemption-liveness iteration that reds if a named file stops being detected while its entry stands.
    - `32.1-RESEARCH.md` §D-04 "Row 194 IS closable — but by a different route than D-15 describes", and §Common Pitfalls Pitfall 4.
  </read_first>
  <action>
Ledger row 194 and CONTEXT.md D-15 both describe the remedy as removing two ticket key words from
this module's PROSE. That framing is wrong about the mechanism and a plan that follows it literally
will try to delete a git subcommand. What the census sees is STRING LITERALS, and this module is
tripped by exactly two of them: one is a git subcommand argument and cannot move without changing
behaviour, and one is a word inside an operator message that can.

Reword the operator message so it names the table's field without using the ticket key spelling —
the sentence is about a table's own field in a safety-surface register and reads correctly either
way. Because the census's conjunction requires every key spelling, dropping this one alone reduces
the module's findings to a single spelling, the conjunction fails, and the module stops being
detected as a ticket reader.

Delete the module's registry entry in the SAME commit as the reword, with a deletion note naming
row 194, quoting the reason that kept the entry, and stating the measurement that retired it: the
census sees literals rather than prose, the module supplied both spellings through two literals, and
one of the two was rewordable. Move the count constant with it, as Task 2's pattern requires.

Then rebuild and commit the `.js` twin in the same commit as the `.ts` — every `.ts` under
`scripts/` moves with its build output, and `check:build-parity` is what proves it did.

Discrimination, RED-first: re-add the reworded spelling to the module in a temp mirror, run the
census, and confirm it names the module as a carrier again; remove the mirror. Separately, delete
the registry entry WITHOUT the reword and confirm the liveness assertion reds. Both captured in
`32.1-04-RED-baseline.txt`.
  </action>
  <acceptance_criteria>
    - `git diff -- scripts/check-diff-disposition.ts` shows a change at the operator-message line and NO change at the working-tree dirty-check line.
    - The re-derived census reports `scripts/check-diff-disposition.ts` as naming ONE key spelling, not both, so its every-key conjunction is false.
    - The module's registry entry is gone, a deletion note stands in its place naming row 194, and the count constant moved in the same commit.
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts` exits 0.
    - `npm run check:diff-disposition` still exits 0 — the reworded message did not change the gate's behaviour, only its wording.
    - `npm run build && npm run check:build-parity` exits 0 with `scripts/check-diff-disposition.js` committed alongside its source.
    - `32.1-04-RED-baseline.txt` records both discrimination plants with their exit codes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run check:diff-disposition</automated>
    <fails_when>non-zero exit, or "CHECK(S) FAILED" in the Result block</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or "BUILD PARITY FAILED" on stderr</fails_when>
  </verify>
  <done>One literal reworded, the git subcommand untouched, the exemption deleted with its note, the count moved in the same commit, the build output committed beside its source, and both discrimination plants recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source → the checker's symbol resolution | author-controlled source decides which declaration a census attributes a reader to |
| the exemption registry → the one-authority claim | an entry in the registry is a hole in the proof that DASH-01's grammar has exactly one authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-04-01 | Spoofing | a second ticket-frontmatter authority assembled across files | high | mitigate | The join resolves declarations rather than identifier text, so namespace, renamed, re-exported and directly-named routes are one offender; and it is asked in both directions, so the converse assembly WR-04 demonstrated is caught. Four mutation-proven discrimination cases, each watched failing. |
| T-32.1-04-02 | Repudiation | an exemption surviving on a reason nobody can re-check | high | mitigate | Every surviving entry's reason must be stated in terms the census actually reads; entries that no longer trip are deleted with a note naming the measurement that retired them. Row 210's entry carries its twelve-literal measurement explicitly. |
| T-32.1-04-03 | Tampering | a count constant bumped rather than derived | high | mitigate | Members and cardinality are separate cases; the count moves in the same commit as the set; the count message states it is a decision. The exemption-liveness iteration reds if an entry is deleted while its file still trips. |
| T-32.1-04-04 | Tampering | a behaviour change disguised as a reword | high | mitigate | The working-tree dirty check's git subcommand argument is named as untouchable in this plan's prohibitions and asserted by inspecting the diff; `npm run check:diff-disposition` exiting 0 after the reword proves the gate's behaviour is unchanged. |
| T-32.1-04-05 | Repudiation | the checker instrument leaking into the file a residual forbids it in | medium | mitigate | `git diff --exit-code -- scripts/context-io-writer-set.test.ts` is in Task 1's verify set; residual `R-31-21-04` is a text match whose expected value is the absence of a checker there. |
| T-32.1-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package; `32.1-RESEARCH.md` § Package Legitimacy Audit records zero additions and zero `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts. |
</threat_model>

<verification>
1. `npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts` — exits 0.
2. `npm run typecheck` — exits 0.
3. `npm run build && npm run check:build-parity` — exits 0.
4. `npm run check:diff-disposition` — exits 0 (unchanged by the reword).
5. `git diff --exit-code -- scripts/context-io-writer-set.test.ts` — exits 0.
6. `npx vitest run --exclude '**/scripts/e2e/**'` — the full regression lane, exits 0.
</verification>

<success_criteria>
- F-15 and WR-04 are closed with reproductions: four plants, each proven green on the old rule and
  red on the new one, each naming its offender.
- The exemption registry is a derived set with a two-sided count that moved as a decision.
- Row 194's evidence is complete; row 210's terminal disposition is recorded with its measurement.
- Nothing about the working-tree dirty check moved, and the residual-protected file is byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-04-SUMMARY.md` when done.
</output>

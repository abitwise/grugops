---
phase: 32.1-board-dashboard-deferred-residuals
verified: 2026-09-18T14:35:00Z
status: gaps_found
score: 17/19 must-haves verified (19-item D-16 ledger enumeration; 1 failed, 1 present-not-closed by design)
covered_files:
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-04-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-04-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-06-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-06-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-07-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-07-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-09-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-09-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-SUMMARY.md
covered_digest: "v1:sha256:0b7fceb07c114e8b97ee7efeb6d353395b07000c102576c1220ebaaab0341a2b"
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "D-07 (plan 32.1-07 must_have): the `spelled` refusal-sentence builder owns every content-derived interpolation across all three board modules (board-model.ts, board-read.ts, board-dashboard.ts), so F-14's defect class — a published sentence silently deleting a content-derived control byte and thereby misquoting the identifier it names — is closed everywhere it occurs."
    status: failed
    reason: >
      Independently reproduced from source (not just cited from 32.1-REVIEW.md WR-01). `childPath()`
      in scripts/board-read.ts:1038-1058 refuses a directory-entry segment only when it is empty,
      `.`, `..`, or contains `/` or `\` — it does not reject or escape a C1 control byte such as
      U+0085. Such a segment is joined into the composed path returned as `contained.real` and used
      as `claimMd` (:1551), `taskDir` (:1535/:1750ish), `absPath`, `path`, and `target`. Those
      composed-path variables then reach published `message:` sentences UNESCAPED at ten sites
      (e.g. board-read.ts:1591 "carries N `at:` lines...", :1609 "carries no `at:` line...", plus
      :1753, :1771, :240, :292, :381, :730, :742, :757) — confirmed by reading each site directly.
      The PATH-DERIVED carve-out in board-model.ts's `spelled` docblock (:1114-1125) excuses this on
      the stated ground that "the entry NAME that lets them is itself content-derived and escaped
      above" — that premise is false at these sites: the entry name is never separately escaped in
      the same sentence. Downstream, `sanitizeCell`/`RENDER_STRIPPED` (board-model.ts:1042,
      board-dashboard.ts:262) DELETES the byte at render time rather than leaving it raw, which
      reproduces F-14's original defect shape (a sentence that misquotes the file/identifier it
      names) via a different code path than F-14's original reproduction (board-model.ts
      presenceActual/ticket-unplaced, closed by plan 32.1-07's RED/GREEN evidence). The 32.1-LEDGER.md
      disposition "F-14 ... closed-with-reproduction" is accurate only for F-14's original narrow
      reproduction site, not for the must_have's stated universal scope ("across all three board
      modules"), and the gap is not recorded anywhere in .planning/WINDOWS.md (unlike plan 07's two
      other named residuals, rows 218-219, which ARE recorded there). This is severity "medium" in
      the same terms 32-40-ADVERSARIAL-REVIEW.md graded the original F-14 (a trust/data-integrity
      defect: no byte leaves the tree, no write capability is involved).
    artifacts:
      - path: "scripts/board-model.ts:1114-1125"
        issue: "PATH-DERIVED carve-out docblock states a reason (\"the entry name ... is itself content-derived and escaped above\") that WR-01 (32.1-REVIEW.md) and this verifier's own trace show is false at reachable board-read.ts sites."
      - path: "scripts/board-read.ts:1591,1609,1753,1771,240,292,381,730,742,757"
        issue: "Ten `message:` sentences interpolate a composed path (`claimMd`/`taskDir`/`absPath`/`path`/`target`) built from an unescaped, unvalidated directory-entry segment, without routing through the `spelled` builder."
    missing:
      - "Either route every board-read.ts message sentence through `spelled` for the composed-path variables (moving those keys from NOT_CONTENT_DECLARATIONS to CONTENT_DECLARATIONS in board-model.test.ts and re-deriving the pinned counts, per 32.1-REVIEW.md WR-01's own fix sketch), or correct the PATH-DERIVED docblock to state the true, narrower bound and add the resulting residual to .planning/WINDOWS.md with a named owner so it is OPEN rather than silently absent."
deferred: []
advisory: []
human_verification:
  - test: "Diagnose why the live claude-CLI e2e lane's A2-live case (SAFE-02/V14) did not observe the prod-deploy PreToolUse deny envelope on the one authorized run (2026-09-18, 09:41:34Z-10:06:37Z), and re-run the lane under a fresh human `go` after any fix."
    expected: "The mechanical prod-deploy guard CLAUDE.md requires (\"never deploy to production without named human confirmation... enforce this mechanically\") demonstrably fires against the real CLI, not just in the unit/gate suite."
    why_human: "This is a live, token-spending, CLI-version-dependent run (2.1.275 on this box) that the phase's own D-13/D-17 decisions forbid re-running without a fresh named human `go`; the cause (kit, suite expectation, CLI version, or host config) is explicitly undetermined by the one run that occurred, per 32.1-LEDGER.md §3.1 and WINDOWS.md rows 211-213."
  - test: "Same live lane, cases A1 (D-31 plugin-cache pointer resolution, empty capture after 606s) and A3-live (DOG-02/D-05 dual-path convergence, neither path produced the frozen verdict)."
    expected: "Both UAT scenarios pass against the real CLI once re-run."
    why_human: "Same live-lane constraint as above (WINDOWS.md rows 211, 213)."
---

# Phase 32.1: Board Dashboard Deferred Residuals Verification Report

**Phase Goal:** Close, by canonical form rather than wider enumeration, the residuals Phase 32
deferred at its four-round cap (human decision Option C, plan 32-41, 2026-09-17): the eight round-4
red-team findings F-14..F-21, the carried ledger rows re-homed here, and triage of the round-4 code
review findings WR-01..WR-04.
**Verified:** 2026-09-18T14:35:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

The phase's own completion contract (D-16/D-22) is a derived count equality over 19 items drawn
from three named sources (8 round-4 findings, 7 carried ledger rows, 4 round-4 review warnings). I
verified that equality independently, then verified a sample of the underlying dispositions against
the actual codebase rather than trusting the ledger's prose, and additionally ran this phase's own
mandated code review artifact (`32.1-REVIEW.md`, produced by `gsd-code-review` per D-17) forward to
see whether its findings were closed.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-16/D-22: the ledger's 19-item enumeration is a derived count equality, not an assertion | VERIFIED | Ran `node .planning/phases/32.1-.../32.1-ledger-check.mjs` myself: `PASS 32.1 completion equality: 19 of 19 item(s) carry exactly one terminal disposition ... derived AGAIN from the three source documents, without this document's participation: 19`. |
| 2 | F-14/D-07: the `spelled` builder escapes every content-derived interpolation, by construction, across all three board modules — closing F-14's defect class everywhere | **FAILED** | See Gaps below. `childPath()` in `board-read.ts` admits an unescaped C1-control-byte directory-entry name into a composed path that then reaches 10 published `message:` sentences unescaped; `sanitizeCell` deletes the byte at render, reproducing F-14's "sentence misquotes the identifier" defect via a code path the ledger's F-14 disposition does not cover. Confirmed by direct code read, independent of `32.1-REVIEW.md` WR-01. |
| 3 | F-15/WR-04/rows 194,210: split-reader join resolves by declaration (checker-based), asked in both directions; `NOT_A_SECOND_AUTHORITY` re-derived; row 210 re-homed with a measured reason | VERIFIED | `32.1-04-RED-baseline.txt` plants 1-4 (pre/post-cutover exit codes as claimed); `scripts/validate.test.ts` registry count confirmed at 9 via grep; WINDOWS.md row 210 status `open` with the 12-citation measurement recorded verbatim, matching D-21. |
| 4 | F-16/F-17/F-18/WR-02/WR-03/row 208: the regex import scanner is retired for a real `ts.createSourceFile` tokenizer; two independent write-detection predicates; loader oracle as independent runtime side; dynamic-import canonical form is string-literal-only | VERIFIED (with a caveat — see Info below) | `js-import-closure.ts` rewritten over `ts.createSourceFile`, no top-level `typescript` import (confirmed by grep: only lazy `requireParser()`/`PARSER_MODULE` references, matching D-20's cited precedent). `check:build-parity` and `check:dashboard-readonly` both PASS when run directly. **However**, `32.1-REVIEW.md` WR-02 (this phase's own review, not round-4's) found — and I confirmed by reading `moduleSpecifierFacts` at `js-import-closure.ts:446-507` — that the walk never consults `sourceFile.parseDiagnostics`, so a malformed/recovered-parse source silently yields a short closure at exit 0, contradicting the module's own header claim "REFUSES RATHER THAN RETURNS SHORT." No must_have explicitly required diagnostics-checking, and the practical blast radius is narrow (the `freshness`/`check:build-parity` gates independently prove every committed `.js` is byte-identical to a fresh `tsc` rebuild, so a hand-tampered malformed `.js` would separately red those gates) — recorded as Info, not a gap. |
| 5 | F-19/F-21/row 207: the one-authority rule matches by resolved symbol, not identifier text; the separator-alphabet counter is replaced by two declared `check:*` shapes; `check:build-parity`'s inline `node -e` moves into a script | VERIFIED | `package.json` `check:build-parity` = `tsc --outDir .tmp-build && node scripts/check-build-parity.js` (no inline `node -e`; confirmed by grep across package.json/ci.yml). Ran `npm run check:build-parity` directly: `PASS Build parity: ... 0 findings over 66/66 elements`. |
| 6 | WR-01(round-4)/F-20/row 206: the contract adopts the code's watch rule (a containment refusal carries no watch record; an unresolvable directory does, until it clears); the liveness probe moves to the resolved path | VERIFIED | `32.1-REVIEW.md` summary independently confirms: "containment on the watch path became strictly tighter (`deps.exists(decision.real)` replaces `deps.exists(dir)`...)"; `agent-factory/contracts/board.md` §"watch record" is in the plan's declared `files_modified`. |
| 7 | Row 176: `check:diff-disposition` reaches exit 0 on the five watched Phase-31 documents | VERIFIED | Ran `npm run check:diff-disposition` directly: `PASS diff disposition — changed watched file(s): 0 findings over 39/39 elements`. WINDOWS.md row 176 `status: fixed`, `resolved_at` populated. |
| 8 | Row 183: the live claude-CLI e2e lane is run once behind a named human checkpoint, and its outcome is honestly recorded (pass, fail, hang, or no-go) rather than skipped or smoothed over | PRESENT, outcome is a RED — routed to human verification | `32.1-10-LIVE-TRANSCRIPT.txt` records the lane ran once (09:41:34Z-10:06:37Z, 2026-09-18) under a human `go` and reported `3 failed \| 5227 passed \| 2 skipped`. Disposition is honestly `re-homed-with-named-owner` (not `closed`), and the working tree is proven byte-identical before/after (`git status --porcelain` diff shown IDENTICAL). One of the three failures, **A2-live**, is a live-CLI failure of the SAFE-02/V14 prod-deploy-deny safety case that CLAUDE.md names as a hard, mechanically-enforced rule — see Human Verification below. This is disclosed rather than hidden (WINDOWS.md rows 211-213), and the phase's own D-16 disposition vocabulary explicitly allows `re-homed-with-named-owner` as a valid terminal state, so it is not counted as a FAILED must-have of *this* phase, but it is surfaced here prominently per the verification brief. |
| 9 | Rows 184/208: the two rows a runtime probe structurally cannot close are set to `waived` by the ledger tool's own verb, with a substantive reason, first use of `waived` on this ledger | VERIFIED | WINDOWS.md rows 184 and 208 both `"status": "waived"` with non-empty, structural-limit reasons matching 32.1-LEDGER.md §4.2's description; `resolved_at` populated for both. |
| 10 | Row 194: the two-ticket-key-word exemption is deleted from `check-diff-disposition.ts`'s prose | VERIFIED | WINDOWS.md row 194 `status: fixed`; `32.1-04-RED-baseline.txt` task 3 cited as evidence, count moved 9→8 per ledger. |
| 11 | Row 209: an erratum note stands BESIDE the original evidence document, which stays byte-unchanged | VERIFIED | `32-40-ADVERSARIAL-REVIEW-ERRATUM.md` exists as a separate file; `git log --follow` on `32-40-ADVERSARIAL-REVIEW.md` shows exactly one commit (its creation, `ede812e8`) — never modified since, confirming the "byte-unchanged" claim. |
| 12 | WR-01..WR-04 (round-4 review): each closed with a named commit and reproduction | VERIFIED | WINDOWS.md and 32.1-LEDGER.md §2 both show `closed-with-reproduction` for all four, citing commits/transcripts that exist on disk (spot-checked WR-01's commit `8e3e3009` reference and WR-04's `32.1-04-RED-baseline.txt` task 1 plant 4). |
| 13 | D-17: no adversarial red-team round ran inside 32.1; verification is `gsd-code-review` plus the verifier | VERIFIED | No `32.1-*-ADVERSARIAL-REVIEW.md` file exists in the phase directory; `32.1-REVIEW.md` is a standard code-review artifact (frontmatter `depth: standard`), not a red-team round. |
| 14 | D-20: `js-import-closure.ts` acquires `typescript` lazily/by injection, never a top-level import | VERIFIED | `grep -n "^import" scripts/js-import-closure.ts` shows no non-builtin top-level import; `PARSER_MODULE = "typescript"` is referenced only inside `requireParser()`, matching the in-repo precedent D-20 cites. |
| 15 | All eight DASH-01..08 requirements are traced (not re-verified from scratch) and every carried residual names which requirement it bears on | VERIFIED | `.planning/REQUIREMENTS.md` lines 129-136/218-225 show all eight already `[x]`/`Complete` from Phase 32; every plan's `requirements:` frontmatter and every 32.1-LEDGER.md §2 row names a requirement column; union of plan-level requirement IDs = exactly DASH-01..08, no orphans. |
| 16 | The committed `.js` build output is a byte-faithful `tsc` rebuild of its `.ts` source (D-13/CLAUDE.md host zero-runtime-dependency rule) | VERIFIED | Ran `npm run freshness` directly: `All build outputs fresh: 66 committed .js file(s) match a rebuild of their sources.` |
| 17 | The full regression lane (`npx vitest run --exclude '**/scripts/e2e/**'`) is green at HEAD | VERIFIED | Ran it myself, independently, start to finish: `Test Files 75 passed (75)`, `Tests 5224 passed \| 2 skipped (5226)`, exit code 0, 472s wall time. Matches `32.1-10-GATES.txt`'s earlier capture exactly. (A handful of `FAIL`-prefixed lines appear mid-stream from `check-banned-claims.test.ts`/`check-audit-register.test.ts` cases that plant a deliberately malformed fixture and assert the refusal MESSAGE text — those are the test's own expected-failure fixtures, not suite failures; the final tally confirms 0 failed.) |
| 18 | Six cheap gates (`check:build-parity`, `check:nul-bytes`, `check:diff-disposition`, `check:dashboard-readonly`, `check:platform-shapes`, `typecheck`) all exit 0 | VERIFIED | All six run directly by this verifier; all six printed `ALL CHECKS PASSED` / all-tests-passed / no typecheck errors. |
| 19 | WINDOWS.md's two representations stay reconciled by row identifier; `open_count`/`fixed_count`/`waived_count` frontmatter counters are self-consistent | VERIFIED | Frontmatter: `open_count: 193`, `fixed_count: 24`, `waived_count: 2`, `total_count: 219` (24+2+193=219, consistent); rows 200-219 present as claimed, ids 176/183/184/194/208/209/210 match ledger table exactly. |

**Score:** 17/19 truths verified (1 FAILED — truth #2; 1 PRESENT but not closed by design and routed to human verification — truth #8).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/js-import-closure.ts` | Real tokenizer, lazy `typescript`, no top-level import | VERIFIED | Rewritten over `ts.createSourceFile`; `requireParser()` lazy accessor confirmed. |
| `scripts/loader-oracle.test-support.ts` | Independent runtime-side closure oracle, excluded from emit | VERIFIED | Present; matches `**/*.test-support.ts` tsconfig exclusion (D-02). |
| `scripts/ts-symbols.test-support.ts` | Shared symbol-resolving checker instrument (D-01) | VERIFIED | Present; excluded from emit alongside the loader oracle. |
| `scripts/check-build-parity.ts`/`.js` | Gate module, no inline `node -e` | VERIFIED | Committed `.js` matches a fresh `tsc` rebuild (freshness gate); no inline eval in package.json/ci.yml. |
| `.planning/phases/32.1-.../32.1-LEDGER.md` + `32.1-ledger-check.mjs` | D-16/D-22 derived completion equality | VERIFIED | Script runs, PASSes, re-derives its own denominator independently of the ledger prose. |
| `.planning/phases/32-.../32-40-ADVERSARIAL-REVIEW-ERRATUM.md` | D-15 erratum beside byte-unchanged original | VERIFIED | Present; original untouched since its single creation commit. |
| `agent-factory/contracts/board.md` | WR-01(round-4) contract paragraph matching code's watch rule | VERIFIED (per review + plan evidence) | Confirmed present in plan 09's `files_modified`; not independently re-diffed line-by-line by this verifier beyond the review's own confirmation. |
| `scripts/board-model.ts` `spelled`/`RENDER_STRIPPED`/PATH-DERIVED carve-out | One escaping authority across all three board modules | ⚠️ **STUB-LIKE GAP** — present and wired for board-model.ts/board-dashboard.ts sentences, but the PATH-DERIVED carve-out's stated justification is false at ten `board-read.ts` sites | See Gaps. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `board-read.ts` message sentences | `board-model.ts` `spelled` builder | Content-derived interpolation routed through the escaping tag | **NOT WIRED at 10 sites** | `claimMd`/`taskDir`/`absPath`/`path`/`target` interpolate directly into template literals, not through `spelled`, on the PATH-DERIVED carve-out's (unsound) reasoning. |
| Five freshness gates + hook-manifest generator | `js-import-closure.ts` `jsImportClosure(root, entryRel)` | Unchanged call signature after the D-08 rewrite | VERIFIED | Signature unchanged; `npm run freshness` (which calls one of these gates) passes. |
| `check:build-parity` / `check:diff-disposition` / `check:nul-bytes` / `check:dashboard-readonly` / `check:platform-shapes` | `package.json` `check:*` scripts | Declared shape, run directly | VERIFIED | All ran to `ALL CHECKS PASSED` / all-green when invoked exactly as declared. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Ledger completion equality is derived, not asserted | `node 32.1-ledger-check.mjs` | `PASS ... 19 of 19 ... derived AGAIN ... without this document's participation: 19` | PASS |
| Build parity (committed `.js` == fresh `tsc` rebuild) | `npm run check:build-parity` | `PASS ... 0 findings over 66/66 elements` | PASS |
| No NUL/forbidden control bytes tracked | `npm run check:nul-bytes` | `PASS 2346 tracked file(s) ... ZERO carrying a forbidden control byte` | PASS |
| Diff-disposition gate (row 176) | `npm run check:diff-disposition` | `PASS ... 0 findings over 39/39 elements` | PASS |
| Dashboard read-only guard | `npm run check:dashboard-readonly` | `Tests 202 passed (202)` | PASS |
| Platform-shapes probe | `npm run check:platform-shapes` | `ALL CHECKS PASSED` | PASS |
| Typecheck (3 tsconfig targets) | `npm run typecheck` | exit 0, no diagnostics printed | PASS |
| Freshness (committed `.js` fidelity) | `npm run freshness` | `All build outputs fresh: 66 committed .js file(s) match a rebuild of their sources.` | PASS |
| F-14/WR-01 reproduction (this verifier, from source) | Read `childPath()` + 10 named `board-read.ts` message sites + `spelled`/`RENDER_STRIPPED` | Confirmed a C1-control-byte directory-entry name passes `childPath`'s segment check unrejected, reaches a composed path used raw in a published sentence, and is deleted (not escaped) by `sanitizeCell` at render | **FAIL — confirms gap #1** |

### Probe Execution

No `scripts/*/tests/probe-*.sh` files exist in this repository and none are referenced by this phase's PLAN/SUMMARY files. Step 7c: SKIPPED (no runnable probes declared for this phase; the phase's own verification mechanism is the ledger-check script plus gates, both exercised above).

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|-----------------|--------------|--------|----------|
| DASH-01 | 32.1-01,04,11 | Board grammar single authority | SATISFIED (residuals bearing on it closed/re-homed per ledger; not re-verified from scratch, per D-16) | REQUIREMENTS.md already `Complete`; ledger §2 rows F-15/WR-04/210 name it. |
| DASH-02 | 32.1-02,03,04,05,11 | Board row grammar spec + fuzz | SATISFIED | Ledger rows F-19/F-21/176/194/210 name it; gates re-run green. |
| DASH-03 | 32.1-07,10,11 | Typed `FactorySnapshot`, `conflicts[]` | ⚠️ PARTIALLY SATISFIED — F-14's closure (bearing on DASH-03) is incomplete per gap #1 | See gap. |
| DASH-04 | 32.1-09,11 | `fs.watch` + polling floor | SATISFIED | F-20/WR-01(round-4) closed per plan 09 and review confirmation. |
| DASH-05 | 32.1-09,10,11 | Torn-read handling, stale badge | SATISFIED | No open residual named against it beyond row 183 (live-lane, re-homed by design). |
| DASH-06 | 32.1-01,06,08,11 | Mechanically read-only dashboard | SATISFIED (with WR-02 Info-level caveat, not a must-have) | `check:dashboard-readonly` PASS; D-08 rewrite confirmed. |
| DASH-07 | 32.1-07,11 | Non-TTY/`--json`/`--once` modes | ⚠️ PARTIALLY SATISFIED — same F-14 gap bears on DASH-07 (render path) | See gap. |
| DASH-08 | 32.1-08,10,11 | Stable snapshot shape, zero runtime deps | SATISFIED | `freshness` PASS; loader oracle confirms no `node_modules` resolution; D-20 lazy-import confirmed. |

No orphaned requirements: every DASH-01..08 ID appears in at least one plan's `requirements:` frontmatter, and REQUIREMENTS.md maps all eight to Phase 32 (this phase is explicitly residual-only, per its own frontmatter comment "no new IDs").

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/board-read.ts` | 1591, 1609, 1753, 1771, 240, 292, 381, 730, 742, 757 | Raw template interpolation of a content-derived (via composed-path) value into a published `message:` sentence, outside the `spelled` escaping authority | 🛑 Blocker (must-have gap #1) | A published diagnostic can silently misquote the file/identifier it names, exactly F-14's defect class. |
| `scripts/js-import-closure.ts` | 446-507 (`moduleSpecifierFacts`) | No check of `sourceFile.parseDiagnostics`; recovered/malformed parse yields a short closure at exit 0, contradicting the module's own "refuses rather than returns short" header claim | ℹ️ Info | Narrow blast radius: only reachable via a hand-tampered `.js`, which the freshness/build-parity gates independently catch. No must_have requires diagnostics-checking. |
| `scripts/loader-oracle.test-support.ts` | 151-155 | Child `execFileSync` has no timeout; an `is-entry` mis-detection hangs the suite silently instead of failing red | ℹ️ Info | Reliability hazard, not a correctness defect; test-support file, excluded from committed build output. |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` debt markers found in the files this phase modified (checked by grep across `files_modified` lists from all 11 plans).

### Human Verification Required

1. **A2-live (SAFE-02/V14) — prod-deploy deny envelope did not appear on the one authorized live run**
   **Test:** Re-run the live claude-CLI e2e lane's A2-live case under a fresh, explicitly named human `go` (per D-13's one-shot-per-go constraint), after investigating why the guard's structured deny marker did not appear on the 2026-09-18 run (`stop_reason: end_turn`, `total_cost_usd: 0.398524`, no deny envelope).
   **Expected:** The mechanical PreToolUse deny guard CLAUDE.md requires ("never deploy to production without named human confirmation... enforce this mechanically") fires against the real CLI on this host/version, not only in the unit/gate suite.
   **Why human:** Live, token-spending, CLI-version-dependent (2.1.275) run that this phase's own decisions (D-13, D-17) forbid re-running without a fresh named `go`; cause undiagnosed by the one run that occurred (kit vs. suite expectation vs. CLI version vs. host config — all open per `32.1-10-LIVE-TRANSCRIPT.txt` and `32.1-LEDGER.md` §3.1). This is a genuine safety-relevant finding this phase surfaced honestly rather than hiding (WINDOWS.md row 212) — it is NOT among this phase's own DASH-01..08 must-haves (the guard belongs to a different requirement family), so it does not by itself flip this phase's status, but it must not be read as "resolved" or absorbed into a passing verdict.

2. **A1 (D-31) and A3-live (DOG-02/D-05) — also failed on the same one live run**
   **Test:** Re-run both under a fresh human `go`.
   **Expected:** A1 produces planning markers instead of an empty capture; A3-live's two dispatch paths converge on the frozen `READY_FOR_HUMAN_REVIEW` verdict.
   **Why human:** Same live-lane constraint as item 1 (WINDOWS.md rows 211, 213).

### Gaps Summary

**One must-have truth FAILED**, and it is the phase's own central claim for F-14/DASH-03/DASH-07: that
the canonical-form `spelled` builder "owns every content-derived interpolation... across all three
board modules" (plan 32.1-07's own must_have wording). It does not, at ten `board-read.ts` sites,
because the PATH-DERIVED carve-out's stated justification is false there — confirmed independently
by tracing `childPath()` and the ten message sites, not merely by citing `32.1-REVIEW.md`'s WR-01.
The practical severity mirrors F-14's own original grading ("medium" — a trust/data-integrity defect,
no write capability, no byte leaves the tree), and the gap is currently invisible in
`.planning/WINDOWS.md` (unlike this same plan's other two named residuals, rows 218-219), which means
a future reader consulting only the ledger or WINDOWS.md would not find it. The fix the review itself
sketches (route the composed-path variables through `spelled`, or narrow and correct the docblock's
claimed reason and give the gap a WINDOWS.md row) is small and does not require a new architectural
decision.

Everything else the phase set out to do — the ledger's 19-item derived completion equality, the
checker-based canonical-form cutovers for F-15/F-19/F-21/WR-04, the real-tokenizer cutover for
F-16/F-17/F-18/WR-02/WR-03(round-4), the declared `check:*` shapes for row 207, the two `waived` rows,
the erratum note, and the honest (not closed, not hidden) disposition of the live e2e lane at row
183 — is verified against the actual codebase and holds up under independent inspection, not merely
under the phase's own self-report. All eight cheap/fast gates this verifier could run directly
(`check:build-parity`, `check:nul-bytes`, `check:diff-disposition`, `check:dashboard-readonly`,
`check:platform-shapes`, `typecheck`, `freshness`, the ledger-check script) passed on this exact HEAD.

The live e2e lane's three failures (A1, A2-live, A3-live) are **not** counted as a gap of this phase
against its own must-haves — D-16 explicitly permits `re-homed-with-named-owner` as a valid terminal
state for row 183, and the phase disclosed the red outcome rather than smoothing it over. But A2-live
in particular touches a CLAUDE.md hard safety invariant (mechanical prod-deploy confirmation) and is
surfaced above as a mandatory human-verification item so it is not lost in an otherwise-passing
narrative.

---

_Verified: 2026-09-18T14:35:00Z_
_Verifier: Claude (gsd-verifier)_

---
phase: 32.1-board-dashboard-deferred-residuals
verified: 2026-09-18T19:20:00Z
status: human_needed
score: 16/17 must-haves verified (1 present-by-design, routed to human verification)
covered_files:
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-04-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-04-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-06-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-06-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-07-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-07-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-09-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-09-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-12-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-12-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-13-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-13-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-14-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-14-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-15-DIAGNOSIS.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-15-PLAN.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-15-SUMMARY.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs
  - scripts/board-dashboard.test.ts
  - scripts/board-dashboard.ts
  - scripts/board-model.test.ts
  - scripts/board-model.ts
  - scripts/board-read.test.ts
  - scripts/board-read.ts
  - scripts/board-readonly.test.ts
  - scripts/check-build-parity.ts
  - scripts/js-import-closure.ts
  - scripts/loader-oracle.test-support.ts
covered_digest: "v1:sha256:b461d83aebb666be31a58379760013104260a0fc15a6556429eb3df1e7fe7dbd"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 17/19
  gaps_closed:
    - "D-07 (plan 32.1-07 must_have, round-1 verification gap #1 / this phase's review WR-01): the `spelled` refusal-sentence builder owns every content-derived interpolation across all three board modules, closing F-14's defect class everywhere it occurs — including the ten board-read.ts message sites the round-1 verifier reproduced as unbuilt."
  gaps_remaining: []
  regressions: []
advisory:
  - finding: "No round-2 code-review (gsd-code-review) artifact was produced for plans 32.1-12..15; 32.1-REVIEW.md on disk is the review from the earlier context and is now pinned as the ledger's fourth completion source rather than re-run."
    category: other
    reason: "Explicit human decision recorded in this round's context: re-running gsd-code-review would overwrite 32.1-REVIEW.md, which the completion ledger now depends on as a fixed source document. This is a deliberate scoping choice, not an omission — no round-2 code changes (the composed-path cutover, the js-import-closure/loader-oracle fixes) received an independent second-pair-of-eyes review pass distinct from this verification."
    evidence_status: "decision recorded in the round's own context/summaries; no review artifact exists to evidence it further"
human_verification:
  - test: "Confirm the prod-deploy safety case's MECHANISM/APPEARANCE separation in 32.1-15-DIAGNOSIS.md §2 reads clearly enough that a later reader will not over-read the live A2-live red as a broken guard, nor explain away a genuinely broken guard as 'just the channel.' The mechanism claim was independently reproduced by this verifier (see Behavioral Spot-Checks): `node hooks/guard.js` fed the exact A2-live probe payload returns the structured deny envelope, exit 0. The appearance claim (why the live channel cannot observe it) rests on published Claude Code hooks documentation cited in the diagnosis."
    expected: "A human agrees the two claims are kept separate in the record and that the live case remains open (not falsely closed) at WINDOWS.md row 212."
    why_human: "Whether prose adequately separates two claims for a future reader is a judgment call, not a grep-able fact — this is plan 32.1-15's own coverage item D2, explicitly flagged `human_judgment: true`."
  - test: "Confirm A1 (plugin-cache pointer resolution) and A3-live (dual-path verdict convergence) cause-class attributions in 32.1-15-DIAGNOSIS.md §§1 and 3 are sound: A1 is attributed to the per-call timeout budget (606161ms / 2 calls ≈ 303080ms per call, just over the 300000ms bound) with a host-configuration rider (a pre-existing user-scope `grugops` marketplace entry may have shadowed the local checkout); A3-live is attributed to the kit correctly withholding `READY_FOR_HUMAN_REVIEW` in a scaffold with no `scripts/context-io.js`."
    expected: "A human agrees both are suite-expectation/environment causes rather than kit defects, and that WINDOWS.md rows 211 and 213 correctly stay open (a diagnosis is not a closure) rather than being marked fixed."
    why_human: "This is a diagnosis derived from arithmetic and code reading, not a live re-run confirming it; the human already answered NO-GO on a fresh live run to confirm it further (recorded in 32.1-15-SUMMARY.md), which this verifier treats as the deciding input, not a bypass."
  - test: "Confirm the judgment in plan 32.1-13's coverage item D7: that DELETING the PATH-DERIVED and internal per-declaration classification tables (rather than narrowing one of them a third time) is the right terminal move for this defect family, given both tables were independently measured wrong in this same plan."
    expected: "A human reads `32.1-13-RED-baseline.txt` §2 and the rewritten `spelled`/`RENDER_STRIPPED` docblock in `scripts/board-model.ts` (confirmed present at lines ~1114-1130 by this verifier) and agrees a canonical two-sided equality is preferable to a third hand-drawn class boundary."
    expected_note: "This verifier confirmed the mechanical claim (both tables are fully deleted — `grep -c CONTENT_DECLARATIONS` / `NOT_CONTENT_DECLARATIONS` scripts/board-model.test.ts both return 0 — and the two-sided equality exists and is mutation-proven in all three modules, tests passing). What remains for a human is only the design-preference judgment, not the mechanics."
    why_human: "Plan 32.1-13's own coverage item D7 is explicitly flagged `human_judgment: true` for this reason."
  - test: "Confirm the judgment in plan 32.1-14's coverage item D8: that IN-02 (build-parity's `git diff`-derived moved-outputs set), IN-03 (check:build-parity's double compile), and IN-06 (per-enclosing-function guardedness granularity in check-foundation-guards.test.ts) are genuine, bounded LIMITS of their respective gates rather than comfortable non-fixes."
    expected: "A human reads `32.1-14-GREEN-proof.txt` §§3.1-3.2 (the seven-item disposition table and the three final register-row texts, confirmed present and matching WINDOWS.md rows 222-224 verbatim by this verifier) and agrees the stated bounds (the `scattered` one-function-per-register assertion; the declared-command-shape zero-exemption rule) are real limits rather than deferrals."
    why_human: "Plan 32.1-14's own coverage item D8 is explicitly flagged `human_judgment: true` for this reason."
---

# Phase 32.1: Board Dashboard Deferred Residuals Verification Report (Round 2)

**Phase Goal:** Close, by canonical form rather than wider enumeration, the residuals Phase 32
deferred at its four-round cap (human decision Option C, plan 32-41, 2026-09-17): the eight round-4
red-team findings F-14..F-21, the carried ledger rows re-homed here, and triage of the round-4 code
review findings WR-01..WR-04, plus this phase's own code review.
**Verified:** 2026-09-18T19:20:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (round 2 of the standing 4-round cap; round 1 was
`gaps_found` 17/19, one FAILED truth)

## Goal Achievement

### What changed since round 1

Round 1 found exactly one FAILED must-have: the `spelled` escaping builder was claimed to own
"every content-derived interpolation... across all three board modules" (plan 32.1-07's own
wording), but ten `board-read.ts` message sites reached a published sentence outside the builder,
via the same defect class as F-14 (a directory-entry name carrying a code point the renderer
deletes, reproduced one register over from where F-14 was originally closed). Round 2 (plans
32.1-12 through 32.1-15) reproduced that gap end to end, then closed it — not by widening the
carve-out that had excused it, but by **deleting both per-declaration classification tables**
(`PATH-DERIVED` and `internal`) and replacing them with a single two-sided `published == owned`
set equality that the census derives and mutation-proves in every module it covers.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-16/D-22: the completion ledger's denominator, now widened to FOUR named sources and 29 items (absorbing this phase's own code review as the fourth source), is a derived count equality, not an assertion | VERIFIED | Ran `node .../32.1-ledger-check.mjs` myself: `PASS ... 29 of 29 item(s) carry exactly one terminal disposition ... derived AGAIN from the four source documents, without this document's participation: 29`. Read the script itself (288 lines): both denominator derivations read from source documents independently of the ledger table, with a vacuity floor and a frontmatter-counter cross-check on the fourth source. |
| 2 | **Round-1's single FAILED truth is now CLOSED.** Every substitution at a published position in the three board modules is built through the `spelled` builder — no class exempt, closed by canonical form (deleting the class question) rather than by widening one carve-out | VERIFIED | Independently re-read every `board-read.ts` message site the round-1 verifier had named as unbuilt (lines 1591, 1609, 1753, 1771, 240, 292, 381, 730, 742, 757 in the old numbering): all now wrapped in `spelled\`...\``. `grep -n "message: \`" scripts/board-read.ts \| grep -v "spelled\`"` returns nothing — no raw-template message sentence remains in the file. |
| 3 | The two per-declaration classification tables (`CONTENT_DECLARATIONS`, `NOT_CONTENT_DECLARATIONS`) and the four cases that consulted them are deleted, replaced by one two-sided `published == owned` equality and one pinned count | VERIFIED | `grep -c "CONTENT_DECLARATIONS" scripts/board-model.test.ts` and the `NOT_` variant both return `0`. Read the replacement directly: `"THE PUBLISHED SET AND THE OWNED SET ARE THE SAME SET — both directions"` (board-model.test.ts:3896-3922), asserting both the empty-offender-list direction and the length-equality direction. |
| 4 | The equality is mutation-proven to discriminate in all three modules (not just the one it was first watched fail in) | VERIFIED (behavioral) | Ran `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts` myself: 179 passed, including the `describe("32.1-13 — the ownership equality is a control, not a coincidence, in ALL THREE modules")` block, which drives a real compiler-host mutation (one builder tag removed) against `board-model.ts`, `board-read.ts`, and `board-dashboard.ts` in turn and asserts the owned count moves by exactly one and the offender is named by module/expression/declaration each time. |
| 5 | `js-import-closure.ts`'s header claim "REFUSES RATHER THAN RETURNS SHORT" is now implemented: a recovered/malformed parse is a named `ImportClosureError` carrying its diagnostic count, not a silently short answer at exit 0 | VERIFIED (behavioral) | Read the module: `sourceFile.parseDiagnostics` is read and a non-empty count throws `ImportClosureError` (js-import-closure.ts:510-512). Ran `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts`: 215 passed, including the WR-02 cases for an unterminated block comment and an unterminated template literal, each reproduced as a named refusal, plus a control proving a well-formed source is unaffected. |
| 6 | A non-literal declaration specifier (`import a from foo;` shape) is recorded into the D-11 unreadable seam by name and node kind rather than silently dropped | VERIFIED (behavioral) | Same `board-readonly.test.ts` run (215 passed) includes the D-11 import/export declaration cases; the review's contrary premise ("this is a syntax error, not a live shape") is recorded as disproved by measurement in the plan's own transcript and does not appear anywhere in the shipped code's logic. |
| 7 | The loader oracle's child process is bounded with an explicit kill signal; a bounded-out child surfaces as a named premise failure rather than hanging the suite | VERIFIED (behavioral) | Same 215-passed run includes the WR-03 cases (`ORACLE_CHILD_OPTIONS` frozen and spread by the one corpus-loading invocation, asserted spread exactly once). This is a genuine process-spawn-and-kill test, not a symbol-presence check — it ran and passed. |
| 8 | A runtime lacking `module.registerHooks` is refused by name (IN-04), with the declared `engines` floor unchanged, and a checkout reached through a symlink records the same module set as a direct one (IN-07) | VERIFIED (behavioral) | Same run includes both IN-04 (guard's own exported bytes executed against absent/present stub namespaces) and IN-07 (symlink realpath) cases, passing. `git diff --exit-code -- package.json` (run directly by this verifier): exit 0, `engines` unchanged. |
| 9 | The `freshness:hook-manifest` gate (IN-05) is wired into CI, closing a "borrowed reachability" gap the review found | VERIFIED | `grep -n "freshness:hook-manifest" .github/workflows/ci.yml` → line 229, inside the same block that already documents three other "wired at both ends" gates. Ran `npm run freshness:hook-manifest` directly: exit 0, "2 decider(s), 26 module hash(es) match a fresh derivation." |
| 10 | All seven of this phase's own review information items (IN-01..07) reach a disposition — four fixed with evidence, three written as final-text register rows with a named owner and a stated limit, none silently dropped | VERIFIED | Read `32.1-14-GREEN-proof.txt` §3.1's seven-row table directly. Cross-checked the three "not fixed" rows against `.planning/WINDOWS.md` rows 222/223/224: text matches verbatim (owner, limit stated, no schedule language). |
| 11 | WR-01 (this phase's own review, 32.1-REVIEW.md) — the composed-path defect at further reader arms — is closed, and correctly recorded as the SAME defect the round-1 verifier's failed truth named (one item, not two, in the completion ledger) | VERIFIED | `32.1-LEDGER.md` line 162 (`32.1-WR-01`) explicitly states "THIS IS ALSO 32.1-VERIFICATION.md's single FAILED must-have truth — one defect seen by two instruments, enumerated once." Evidence cites plans 32.1-12/13's RED/GREEN transcripts, independently confirmed at truth #2 above. |
| 12 | All three live-lane failures (A1, A2-live, A3-live) now carry a named cause class with evidence and a distinguishing observation, produced at zero token spend; the safety mechanism's offline state is established separately from its live-channel appearance; a human NO-GO on a second live run is recorded as a legitimate terminal outcome | **PRESENT, not closed by design — routed to human verification** | `32.1-15-DIAGNOSIS.md` exists, is thorough, and marks every unsettled point `UNKNOWN - verify` rather than asserting past its evidence. This verifier independently reproduced the mechanism claim (see Behavioral Spot-Checks below). But per this phase's own D-16 disposition vocabulary, `re-homed-with-named-owner` (not `closed`) is the correct terminal state for WINDOWS.md rows 211-213, and a diagnosis is explicitly stated by the plan itself not to be a closure. This is not counted as a gap (the phase's own vocabulary permits this state and disclosed it honestly), but the underlying safety-relevant live red (row 212, touching CLAUDE.md's mechanical prod-deploy-confirmation invariant) is surfaced to a human rather than absorbed into a passing verdict. |
| 13 | The prod-deploy guard MECHANISM is intact, independent of the live channel's ability to observe it | VERIFIED (behavioral, reproduced independently) | This verifier ran, independent of any phase transcript: `printf '<PreToolUse payload matching the A2-live probe>' \| node hooks/guard.js` → exit 0, stdout carries the structured deny envelope with all three attributing fields, matching `32.1-15-DIAGNOSIS.md §2.1`'s own transcript byte-for-byte in content. |
| 14 | `.planning/WINDOWS.md`'s two representations stay reconciled by row identifier; `open_count`/`fixed_count`/`waived_count` frontmatter counters are self-consistent after this round's five register transitions | VERIFIED | Read the frontmatter directly: `open_count: 196`, `waived_count: 3`, `fixed_count: 25`, `total_count: 224` — 196+3+25=224, consistent. Spot-checked rows 176/183/184/194/200-210/220-224 against `32.1-LEDGER.md`'s and the plan SUMMARYs' claimed dispositions: all match verbatim (row 220 `fixed`, row 221 `waived`, rows 222-224 newly `open` with named owners). |
| 15 | The full regression lane and every declared cheap/fast gate are green at this commit | VERIFIED | Ran every gate myself, directly, not from a transcript: `npx vitest run --exclude '**/scripts/e2e/**'` → 75 files, 5248 passed / 2 skipped, exit 0 (475.88s wall time; the mid-stream `FAIL`-prefixed lines are `check-banned-claims`/`check-foundation-guards`/frontmatter tests' own deliberately-planted expected-failure fixtures, not suite failures — confirmed by the final 0-failed tally). `check:build-parity`, `check:nul-bytes`, `check:diff-disposition`, `check:dashboard-readonly`, `check:platform-shapes`, `typecheck`, `freshness`, `freshness:hook-manifest` — all PASS/exit 0. |
| 16 | Requirements DASH-01..08 remain traced with no orphans; this phase adds no new requirement IDs | VERIFIED | `.planning/REQUIREMENTS.md` lines 129-136/218-225: all eight already `[x]`/`Complete` (from Phase 32, unchanged). Union of `requirements:`/`requirements-completed:` frontmatter across all 15 plans in this phase = a subset of DASH-01..08, no orphans. |
| 17 | No debt marker, stub, placeholder, or skipped test was introduced into any file this round modified | VERIFIED | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across every file in `covered_files`' impl set (`board-read.ts`, `board-model.ts`, `board-dashboard.ts`, `js-import-closure.ts`, `loader-oracle.test-support.ts`, `check-build-parity.ts`, the four `*.test.ts` files, `ci.yml`) — zero matches. |

**Score:** 16/17 truths verified (1 present-but-not-closed-by-design, routed to human verification — truth #12).

### Deferred Items

None. This round is entirely within Phase 32.1's own scope (residuals against already-complete
DASH-01..08); no item was pushed to a later milestone phase.

### Advisory (New Scope, Unevidenced)

| # | Finding | Category | Why Advisory |
|---|---------|----------|---------------|
| 1 | No `gsd-code-review` pass ran over round 2's own code changes (the composed-path cutover in plans 32.1-12/13, the js-import-closure/loader-oracle changes in 32.1-14) | other | Explicit, recorded human decision: re-running the review would overwrite `32.1-REVIEW.md`, which this round's own ledger widening now depends on as a fixed fourth source document. This verifier's own independent code reading and test execution (see Observable Truths above) substitutes for a second-pair-of-eyes review to the extent grep/test-run verification can, but is not the same instrument. Not a gap — recorded per the round's own stated scope decision — but noted because a future reader should know round 2's code changes did not get an independent review pass distinct from this verification. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/board-read.ts` | All published `message:` sentences built through `spelled`, no exceptions | VERIFIED | `grep "message: \`" \| grep -v "spelled\`"` returns nothing. |
| `scripts/board-model.ts` | `spelled`/`RENDER_STRIPPED` docblock states one universal rule; both classification tables deleted | VERIFIED | `grep -c CONTENT_DECLARATIONS` / `NOT_CONTENT_DECLARATIONS` both 0; docblock at ~1114-1130 records both disproofs with their registers. |
| `scripts/board-model.test.ts` | Two-sided `published == owned` equality; mutation mirror parametrised over 3 modules with a seed-set premise | VERIFIED | Read directly; ran suite (179 passed). |
| `scripts/js-import-closure.ts` | Diagnostics-checking refusal; D-11 declaration-specifier recording; no top-level non-builtin import | VERIFIED | `parseDiagnostics` check present; `grep -c '^import'` = 3, all `node:*` builtins. |
| `scripts/loader-oracle.test-support.ts` | Bounded child with explicit kill signal; `ORACLE_REGISTRATION_GUARD`; realpath-resolved root | VERIFIED | Present; exercised by passing WR-03/IN-04/IN-07 cases in `board-readonly.test.ts`. |
| `.github/workflows/ci.yml` | `freshness:hook-manifest` wired into the gate block | VERIFIED | Line 229; ran directly, exit 0. |
| `.planning/phases/32.1-.../32.1-LEDGER.md` + `32.1-ledger-check.mjs` | Widened to 4 sources / 29 items, both denominators independently derived | VERIFIED | Ran script directly: PASS 29/29. |
| `.planning/phases/32.1-.../32.1-15-DIAGNOSIS.md` | Named cause class + evidence + distinguishing observation for all three live-lane failures | VERIFIED (present) | Read in full; thorough, marks unresolved points `UNKNOWN - verify`. Routed to human verification per truth #12. |
| `.planning/WINDOWS.md` | Register transitioned via tool's verb; counters self-consistent | VERIFIED | `196+3+25=224` confirmed directly; rows spot-checked against ledger claims. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `board-read.ts` message sentences | `board-model.ts` `spelled` builder | Content-derived interpolation routed through the escaping tag | **NOW WIRED, all sites** | Confirmed by direct grep — zero raw `message:` template sentences remain. |
| `board-model.test.ts` two-sided equality | `board-read.ts` / `board-model.ts` / `board-dashboard.ts` | Mutation-mirrored compiler host, one seed per module | VERIFIED | Ran the suite; all three module arms discriminate as claimed. |
| `.github/workflows/ci.yml` | `scripts/hook-manifest-freshness.js` via `npm run freshness:hook-manifest` | Declared CI step | VERIFIED | Grep + direct run, both confirm. |
| `32.1-ledger-check.mjs` | `32-40-ADVERSARIAL-REVIEW.md` / `32-REVIEW.md` / `32.1-CONTEXT.md` / `32.1-REVIEW.md` | Four independent source-document derivations | VERIFIED | Ran the script; both derivations (table-internal and four-source) agree at 29. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Ledger completion equality, widened to 4 sources | `node 32.1-ledger-check.mjs` | `PASS ... 29 of 29 ... derived AGAIN ... 29` | PASS |
| Prod-deploy guard MECHANISM fires against the exact A2-live probe command | `printf '<PreToolUse payload>' \| node hooks/guard.js` | exit 0, structured deny envelope with all 3 attributing fields, byte-content matching the diagnosis's own transcript | PASS — independently reproduced, not merely cited |
| Full regression lane | `npx vitest run --exclude '**/scripts/e2e/**'` | 75 files, 5248 passed / 2 skipped, exit 0 | PASS |
| Board-model census + mutation mirror (all 3 modules) | `npx vitest run ... scripts/board-model.test.ts` | 179 passed | PASS |
| Closure/oracle refusals (WR-02, WR-03, IN-04, IN-07, D-11) | `npx vitest run ... scripts/board-readonly.test.ts` | 215 passed | PASS |
| Board-read/board-dashboard composed-path cutover | `npx vitest run ... scripts/board-read.test.ts scripts/board-dashboard.test.ts` | 250 passed | PASS |
| Build parity (committed `.js` == fresh `tsc` rebuild) | `npm run build && npm run check:build-parity` | 0 findings over 66/66 | PASS |
| No forbidden control bytes tracked | `npm run check:nul-bytes` | 2363 files scanned, zero carrying a forbidden control byte | PASS |
| Freshness (all gates) | `npm run freshness`, `npm run freshness:hook-manifest` | Both exit 0 | PASS |
| Diff-disposition / dashboard-readonly / platform-shapes / typecheck | direct run of each | All exit 0 / all green | PASS |
| Offline deny-matcher + Tier-1 oracle roster (diagnosis's own evidentiary base) | `npx vitest run ... scripts/prod-deploy-deny-match.test.ts scripts/check-uat-oracles.test.ts` | 31 passed | PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` files exist in this repository and none are referenced by this
round's PLAN/SUMMARY files. Step 7c: SKIPPED (same finding as round 1 — the ledger-check script
and the gate sweep are this phase's own verification mechanism).

### Requirements Coverage

| Requirement | Source Plan(s) (round 2) | Description | Status | Evidence |
|-------------|---------------------------|--------------|--------|----------|
| DASH-01 | (unchanged this round) | Board grammar single authority | SATISFIED (unchanged) | REQUIREMENTS.md `Complete`; no round-2 plan bears on it directly. |
| DASH-02 | (unchanged this round) | Board row grammar spec + fuzz | SATISFIED (unchanged) | REQUIREMENTS.md `Complete`; no round-2 plan bears on it directly. |
| DASH-03 | 32.1-12,13,15 | Typed `FactorySnapshot`, `conflicts[]` | **SATISFIED — round-1's partial gap now fully closed** | The F-14-register-over gap that made round 1 mark this ⚠️ PARTIALLY SATISFIED is closed (truth #2 above). |
| DASH-04 | (unchanged this round) | `fs.watch` + polling floor | SATISFIED (unchanged) | No round-2 plan bears on it. |
| DASH-05 | 32.1-15 | Torn-read handling, stale badge | SATISFIED | Ledger row reconciliation named against it. |
| DASH-06 | 32.1-14 | Mechanically read-only dashboard | SATISFIED | `check:dashboard-readonly` PASS (215 passed); WR-02/WR-03 closures bear on this ID. |
| DASH-07 | 32.1-12,13 | Non-TTY/`--json`/`--once` modes | **SATISFIED — round-1's partial gap now fully closed** | Same closure as DASH-03; the render path is the one that was gapped. |
| DASH-08 | 32.1-13,14,15 | Stable snapshot shape, zero runtime deps | SATISFIED | `freshness` PASS; `grep -c '^import'` on js-import-closure.ts confirms lazy acquisition intact. |

No orphaned requirements: this phase's frontmatter comment states "residuals against already-complete
requirements; no new IDs," and every plan's `requirements`/`requirements-completed` field is a subset
of DASH-01..08.

### Anti-Patterns Found

None in any file this round modified. `grep -n -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` across
`board-read.ts`, `board-model.ts`, `board-dashboard.ts`, `js-import-closure.ts`,
`loader-oracle.test-support.ts`, `check-build-parity.ts`, the four touched `*.test.ts` files, and
`.github/workflows/ci.yml` returns zero matches.

### Human Verification Required

See the `human_verification` frontmatter block for full text. In summary:

1. **A2-live safety case — mechanism/appearance separation clarity** (plan 32.1-15 coverage item
   D2, `human_judgment: true`). The mechanism itself is independently reproduced by this verifier
   as intact; what needs a human is whether the write-up keeps that separate enough from the
   live-channel red that a future reader neither over-reads nor dismisses it.
2. **A1 / A3-live cause-class attributions** — confirm the diagnosis's arithmetic and code-reading
   conclusions are sound (the human already declined a live re-run to confirm further; that NO-GO
   is treated here as the deciding input, not a bypass).
3. **Plan 32.1-13 coverage item D7** — is deleting both classification tables (canonical form) the
   right terminal move, versus a third, better-drawn classification? The mechanical claim is fully
   verified; only the design-preference judgment remains.
4. **Plan 32.1-14 coverage item D8** — are IN-02/IN-03/IN-06 genuine limits of their gates rather
   than comfortable non-fixes? The mechanical claim (dispositions recorded, register rows filed) is
   fully verified; only the "is this limit real" judgment remains.

None of these four items reflect a defect this verifier found in the code. They are the phase's own
explicitly-flagged `human_judgment: true` coverage items (items 3 and 4) plus the carried-forward,
by-design-still-open live-lane rows (items 1 and 2, WINDOWS.md rows 211-213) that round 1 also
routed to human verification. Per this project's own standing discipline (a green suite is not
proof for a safety invariant), item 1 in particular should not be waved through without a human
reading the diagnosis themselves.

### Gaps Summary

**No gaps.** Round 1's single FAILED must-have — the `spelled` builder's claimed universal
ownership, false at ten `board-read.ts` sites — is now independently confirmed closed, by deletion
of the class question rather than by widening the carve-out that had excused it. This verifier
re-read every previously-cited unbuilt site directly and ran the actual mutation-proof test suite
rather than trusting the plan SUMMARYs' narration. All eight gates this verifier could run directly
passed at HEAD, including one this verifier ran for the first time as an independent behavioral
check (the guard fed the exact A2-live probe payload), rather than merely re-citing the phase's own
transcripts.

What remains is not a gap but a design-preference/judgment layer the plans themselves flagged
(`human_judgment: true` on two coverage items) plus the honestly-disclosed, by-design-still-open
live-lane rows this phase diagnosed but explicitly did not close (a diagnosis is not a closure,
per the round's own stated discipline). The phase's own completion vocabulary
(`re-homed-with-named-owner`) permits this as a valid terminal state, and this verifier agrees that
is not equivalent to hiding an open safety question — but a human, not a verifier, should be the one
to sign off on both the judgment calls and the live A2-live row's disposition, given CLAUDE.md's
hard mechanical-confirmation safety invariant is what row 212 touches.

---

_Verified: 2026-09-18T19:20:00Z_
_Verifier: Claude (gsd-verifier)_

---
phase: 32.1-board-dashboard-deferred-residuals
plan: 11
type: execute
wave: 11
depends_on: ["32.1-10"]
files_modified:
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs
  - .planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW-ERRATUM.md
  - .planning/WINDOWS.md
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-GREEN-proof.txt
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07, DASH-08]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-16: every item in this phase ends in exactly one terminal disposition — closed-with-reproduction, waived-with-reason, or re-homed-with-named-owner — and the completeness is a DERIVED count equality between the enumerated items and the items carrying a terminal disposition, never an assertion."
    - "D-22: the denominator is derived from three named sources — the eight round-four findings, the seven carried ledger row identifiers, and the four round-four review warnings — giving nineteen, with the nineteen-versus-twenty reconciliation recorded explicitly in the ledger file."
    - "D-16: the eight already-complete requirements are NOT re-verified from scratch; the ledger records which residual bore on which requirement and makes no claim about the requirement's own status."
    - "D-14: the two rows a runtime probe cannot close are set to the waived status by the ledger tool's own verb, with the final reason sentences plan 08 measured, and both representations are reconciled by row IDENTIFIER rather than by column position or row count."
    - "D-15: the wrong-commit attribution in the round-four review is corrected by an erratum note BESIDE the original, which is left byte-unchanged — and the erratum file is recorded as a NEW convention on this tree, not as a followed one."
    - "D-17: no adversarial red-team round runs inside this phase; verification is the code review plus the verifier against the ledger equality, and the standing four-round gap-closure cap still bounds anything that follows."
  artifacts:
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs
    - .planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW-ERRATUM.md
    - .planning/WINDOWS.md
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-GREEN-proof.txt
  key_links:
    - "Each ledger item's disposition names the plan and the transcript that is its evidence, so the equality is checkable against artefacts on disk rather than against this document's own prose."
    - "The residual ledger's two representations are rendered by its tool from one set of entries, and its frontmatter counters are cross-checked against those entries — which is why every status change here runs through the tool's verb."
  prohibitions:
    - "Never hand-edit the residual ledger file."
    - "Never edit an evidence document in place — an erratum stands beside it."
    - "Never type a count that should be derived from an enumeration."
    - "Never reconcile the ledger's two representations by column position or by row count."
    - "Never claim a terminal disposition that no artefact on disk supports."
    - "Never fake a gate result, a test result, or a citation."
    - "Never run `npm test`."
---

<objective>
Close the phase the way D-16 requires: not by declaring it done, but by enumerating every item from
named sources and measuring that each one carries exactly one terminal disposition. Phase 32 spent
its four-round cap and ended at a human decision precisely because completion had become arguable;
this phase's answer is to make completion a count equality whose denominator a reader can re-derive.

Three things land here. The ledger file itself, with the nineteen-item enumeration and the
nineteen-versus-twenty reconciliation D-22 asks for. The two waives D-14 authorizes, written by the
ledger tool's own verb with the final sentences plan 08 measured. And the erratum note for the
round-four review's wrong commit attribution, standing beside an original that is left byte-unchanged
(D-15).

**Where the equality lives, and why it is not a suite case.** D-16's check runs as this plan's gate,
over the ledger file, rather than as a permanent case in the test suite. A permanent case that reads
a phase document under the planning directory turns the suite red on the commit that writes that
document — this repository has already paid for that shape once, in a phase whose coverage case read
a review file. The equality is a small derived check filed beside the phase; the suite stays about
the code.

Purpose: this is the artefact the verifier reads. If it is a list somebody typed, the phase closes on
an assertion; if it is a measurement, the phase closes on evidence.
Output: one ledger file, one derived check, two waived rows, the fixed rows this phase's evidence
supports, one erratum note, and the equality's output filed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-CONTEXT.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-RESEARCH.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-SUMMARY.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-SUMMARY.md
</context>

<assumption_delta_decision>
**Noun:** the terminal status of a residual ledger row.
**Decision:** add-alongside.
**Rationale:** the delta scan returned one signal, on the phase goal's sentence about a remedy being
a canonical form rather than another arm. The pluralization noun it points at in this phase is the
status a residual row can end in: today's rows end in open or fixed, and D-14 introduces waived as a
third. This is add-alongside rather than promote, because the ledger tool's own status vocabulary
ALREADY declares waived beside open and fixed, its waive verb already requires a non-empty reason,
and its counter cross-check already recomputes all four totals. Nothing is generalized and no new
representation is introduced — this phase is the first CALLER of an existing verb. No other noun is
promoted, because the scan's single signal is about remedies rather than about data shape.
</assumption_delta_decision>

<artifacts_this_phase_produces>
Produced by THIS plan: `32.1-LEDGER.md` (the nineteen-item enumeration with one terminal disposition
per item, the derived denominator, and the nineteen-versus-twenty reconciliation);
`32.1-ledger-check.mjs` (the derived equality, run as this plan's gate rather than as a suite case);
`32-40-ADVERSARIAL-REVIEW-ERRATUM.md` beside the round-four review, recorded as a new convention; the
residual ledger's status transitions, written by its own tool — two waived rows and the fixed rows
this phase's evidence supports; `32.1-11-GREEN-proof.txt` carrying the equality's output and the
ledger reconciliation by row identifier. This is the last plan of the phase and the only one
permitted to mutate the residual ledger.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The nineteen-item ledger, with a denominator a reader can re-derive</name>
  <files>.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md</files>
  <read_first>
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-CONTEXT.md` — the phase boundary's own enumeration and D-16, D-17, D-22.
    - `.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md` § 12 and § 13 — the eight findings this phase owns, the ninth recorded outside the ratio as a harness finding, and the created-versus-inherited table.
    - `.planning/phases/32-board-projector-cli-dashboard/32-REVIEW.md` — the four round-four warnings and the information items.
    - `.planning/phases/32-board-projector-cli-dashboard/deferred-items.md` — the ownership table, whose own count differs from both nineteen and twenty; this is the reconciliation's third input.
    - `.planning/phases/32-board-projector-cli-dashboard/32-41-OVERRIDE-DRAFT.md` § 3 — the eighteen-item enumeration against homes.
    - Every `32.1-NN-SUMMARY.md`, `32.1-NN-RED-baseline.txt` and `32.1-NN-GREEN-proof.txt` in this phase directory — each item's disposition must name the artefact that is its evidence.
    - `32.1-RESEARCH.md` §Contradictions row 14 and §Open Questions item 3 — why the denominator must be defined before a line is written.
  </read_first>
  <action>
Write `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md`, the phase's
completion artefact.

Derive the item enumeration from THREE named sources and say which item came from which (D-22): the
eight round-four findings; the seven carried residual-ledger row identifiers this phase owns; and the
four round-four review warnings. That is nineteen. Record the nineteen-versus-twenty reconciliation
explicitly: the phase boundary's prose says twenty while its own list enumerates nineteen, and the
deferred-items ownership table counts differently again because it counts HOMES rather than items.
State which count this ledger uses, why, and what each of the other two was counting. The
reconciliation is the point — D-16's rule applied to D-16 is that the number is a measurement.

Give each item one table row with these columns, in this order: item, source, requirement, disposition,
evidence. The disposition cell carries exactly one of `closed-with-reproduction`,
`waived-with-reason` or `re-homed-with-named-owner`, spelled exactly that way, because the derived
check in Task 3 reads those three spellings. The evidence cell names the plan and the transcript or
case that supports it. A disposition with no artefact on disk behind it is a claim, and the check is
designed to be re-runnable against the artefacts rather than against this document's prose.

Where the disposition is `re-homed-with-named-owner`, name the owner — a file, a phase, or a person —
and the measurement that made re-homing the honest answer rather than a deferral. Two are expected on
the evidence this phase produced: the ninth exemption entry, whose owner is the test file whose twelve
string literals keep it (plan 04), and the review information item about a repository-wide spelling
walk that plan 01 recorded as not closed by the tracked-set floor. Confirm both against the summaries
rather than carrying them from this sentence, and add any third the summaries actually support.

Record the posture D-17 fixes, so the next reader knows what this phase deliberately did not do: no
adversarial red-team round runs inside it, verification is the code review plus the verifier against
this equality, and the standing four-round cap bounds any gap-closure that follows.

Record also that the eight requirements are not re-verified from scratch (D-16). The requirement
column says which residual bore on which requirement; it makes no claim about the requirement's own
status.
  </action>
  <acceptance_criteria>
    - `32.1-LEDGER.md` exists, enumerates nineteen items, and attributes each to one of the three named sources.
    - The nineteen-versus-twenty reconciliation is stated explicitly, naming what each of the three differing counts was counting.
    - Every item row carries exactly one of the three disposition spellings and an evidence cell naming a plan and an artefact.
    - Every evidence path named in the ledger resolves to a file that exists on disk.
    - The ledger states D-17's posture and states that the eight requirements are not re-verified from scratch.
    - `npm run check:nul-bytes` exits 0 — the ledger carries no raw control byte.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md &amp;&amp; grep -cE 'closed-with-reproduction|waived-with-reason|re-homed-with-named-owner' .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md</automated>
    <fails_when>non-zero exit (the ledger is missing or empty), or the printed count is fewer than 19</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the output names a file under `.planning/phases/32.1-board-dashboard-deferred-residuals/`</fails_when>
  </verify>
  <done>Nineteen items, each attributed to a named source, each carrying one terminal disposition spelled exactly as the check reads it, each pointing at an artefact that exists; the reconciliation and the phase's posture are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: The ledger tool writes the statuses, and the erratum stands beside the original</name>
  <files>.planning/WINDOWS.md, .planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW-ERRATUM.md, .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-GREEN-proof.txt</files>
  <read_first>
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-GREEN-proof.txt` — the FINAL waive-reason sentences plan 08 measured, one per row. The waive verb is one-shot and overwrites the reason cell, so these sentences are used verbatim.
    - `.planning/WINDOWS.md` — the frontmatter counters, the table representation, and the fenced array representation. Read rows 176, 183, 184, 194, 200 through 210 in full.
    - `32.1-RESEARCH.md` §D-14 in full — the tool's status vocabulary, the mandatory non-empty reason, the counter cross-check, the one-shot nature of the verb, the refusal to re-transition an already-transitioned row, and the reconciliation gotcha about escaped table cells shifting a column tally.
    - `.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md` § 12 and § 13 — the sections whose commit attribution the erratum corrects, and `deferred-items.md`'s row-209 block, which is the convention actually in use today (a note in the OWNING document).
    - `32.1-RESEARCH.md` §D-15 "there is no existing erratum convention on this tree".
  </read_first>
  <action>
Transition every ledger row this phase owns, using the ledger tool's own verbs and never a file edit.
Both of the ledger's representations are rendered by the tool from one set of entries, and its
frontmatter counters are cross-checked against those entries, so a hand edit throws rather than
landing.

Two rows take the waived status (D-14), with the final sentences from plan 08's proof used verbatim:
the runtime-assembled key-spelling row and the runtime-acquisition row. Each reason must state a
STRUCTURAL limit — what a runtime probe over this corpus cannot observe and why — rather than a
schedule. This is the first use of that status on this ledger; nothing needs implementing, because
the vocabulary and the verb already exist, and the phase is their first caller.

Rows whose evidence supports closure take the fixed status, one call per row, each in the order the
ledger file's disposition column states. Do not transition a row whose ledger disposition is
`re-homed-with-named-owner`: there is no status for re-homing, and inventing one by using a
transition verb would record something the tooling does not mean. Leave those rows open and let the
phase ledger carry the owner — say so explicitly in the summary so a later reader does not read the
open status as an oversight.

Reconcile the two representations by ROW IDENTIFIER and record the reconciliation in
`32.1-11-GREEN-proof.txt`: the identifier set from the table and the identifier set from the fenced
array, with a symmetric difference of zero. Do not tally by column position — at least three
descriptions carry an escaped separator that shifts a positional tally, and a count taken that way
disagrees with the array by three. Do not tally by row count either; the identifier sets are the
comparison.

Record the governance effect: the open count gates shipping when the enforcement dial is on, and
waiving or fixing rows lowers it. Name the before and after numbers in the proof so the movement is
attributed rather than discovered later.

Then write `32-40-ADVERSARIAL-REVIEW-ERRATUM.md` beside the round-four review (D-15). It names the
sections carrying the wrong commit attribution, states the correct commit for the fix in question,
and shows the evidence: the fix commit touches the census test file, and the commit the review names
instead is a documentation commit touching only the planning directory. Leave the original
byte-unchanged — an evidence document is never edited in place.

Record the erratum as a NEW convention. There is no erratum file anywhere on this tree today; the
convention actually in use is a note in the OWNING document, which is where row 209's own re-measurement
already lives. Creating a file beside the original is a new shape, and saying so is the difference
between a convention and a precedent somebody assumed.
  </action>
  <acceptance_criteria>
    - Every status transition was made with the ledger tool's verb; `git diff -- .planning/WINDOWS.md` shows the tool's rendering (both representations and the frontmatter counters moved together) and no hand-shaped edit.
    - Exactly two rows carry the waived status, each with the verbatim reason sentence from plan 08's proof.
    - No row whose ledger disposition is `re-homed-with-named-owner` was transitioned; the summary states why.
    - `32.1-11-GREEN-proof.txt` records the identifier-set reconciliation between the two representations with a symmetric difference of zero, and states that the comparison is by identifier rather than by column position or row count.
    - `32.1-11-GREEN-proof.txt` records the open-count before and after, and names the governance effect.
    - `32-40-ADVERSARIAL-REVIEW-ERRATUM.md` exists beside the original, names the affected sections and the correct commit, and shows both commits' evidence.
    - `git diff --exit-code -- .planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md` exits 0 — the original is byte-unchanged.
    - The erratum document states that it is a new convention on this tree and names the convention in use today.
  </acceptance_criteria>
  <verify>
    <automated>git diff --exit-code -- .planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md</automated>
    <fails_when>non-zero exit — the original evidence document was edited in place</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the output names the erratum or the ledger</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-nul-bytes.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
  </verify>
  <done>Every owned row is transitioned by its tool or deliberately left open with the reason recorded; the two representations reconcile by identifier with zero difference; the governance movement is named; the erratum stands beside a byte-unchanged original and declares itself a new convention.</done>
</task>

<task type="auto">
  <name>Task 3: The completion equality, derived and re-runnable</name>
  <files>.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs, .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-GREEN-proof.txt</files>
  <read_first>
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md` from Task 1 — the table the check parses.
    - `scripts/vacuity.ts:26-27, :46-97` — the four ordered branches and the named forbidden alternative, for the check's reporting shape even though it lives outside the suite.
    - `scripts/context-io-writer-set.test.ts:229-248` — the premise-triple form, for the messages the floors carry.
    - `32.1-CONTEXT.md` D-16 and D-22 — the equality's two halves.
  </read_first>
  <action>
Write `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs`, a small
Node script with no dependencies that measures D-16's completion property over the ledger file and
exits non-zero when it does not hold.

It must do four things, in this order, each with its own message:

1. Parse the ledger's item table and count the item rows, skipping the header and the delimiter row.
   Floor the count above zero with a message saying that a check over an empty parse is true over
   nothing — the premise before the equality, not after it.
2. Count the rows carrying one of the three terminal disposition spellings, and assert that count
   equals the item count. The failure message must NAME the items missing a disposition, because
   "nineteen versus eighteen" tells a reader nothing about which one to go and write.
3. Derive the three source sizes from the ledger's own source column and assert their sum equals the
   item count. This is D-22's half: the denominator is a measurement over three named sources, not a
   number the document asserts about itself.
4. Print a PASS line carrying the measurement — the item count, the terminal count, and the three
   source sizes — so a green run says what it measured rather than only that it was green.

Run it, and file its command and complete output in `32.1-11-GREEN-proof.txt` beside Task 2's
reconciliation.

Prove it discriminates before trusting it. Three plants, each against a COPY of the ledger, never the
ledger itself: remove one item's disposition and confirm the check reds naming that item; remove one
item row entirely and confirm the source-sum assertion reds; empty the table and confirm the premise
floor reds rather than the equality passing over nothing. Record all three in
`32.1-11-GREEN-proof.txt` with the message each produced, and confirm
`git diff --exit-code -- .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md`
after the plants.

Finally, re-run the phase's regression lane and the gate sweep one last time and record that the
phase closes green, with the counts beside plan 01's starting numbers.
  </action>
  <acceptance_criteria>
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs` exists, depends on nothing outside Node's standard library, and exits 0 against the real ledger.
    - Its PASS line carries the item count, the terminal count and the three source sizes.
    - The three plants are each recorded with the message they produced, and each was run against a copy.
    - `git diff --exit-code -- .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md` exits 0 after the plants.
    - `32.1-11-GREEN-proof.txt` carries the check's command and output, Task 2's identifier reconciliation, the open-count movement, and the phase's closing regression-lane counts beside plan 01's starting counts.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs</automated>
    <fails_when>non-zero exit, or the PASS line does not carry the item count, the terminal count and the three source sizes</fails_when>
    <automated>git diff --exit-code -- .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md</automated>
    <fails_when>non-zero exit — a plant was run against the real ledger instead of a copy</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npm run check:diff-disposition &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit from any of the four, or "CHECK(S) FAILED" in any Result block</fails_when>
  </verify>
  <done>The completion property is a re-runnable derived check with a floored premise, a naming failure message and a measuring PASS line; three plants prove it discriminates; the ledger is untouched by them; the phase closes with its numbers beside its starting numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase artefacts → the completion claim | the ledger is what a verifier reads to decide whether the phase is done |
| the residual ledger's two representations → governance | the open count gates shipping when the enforcement dial is on |
| an evidence document → a later reader | a review's own text is the record of what was measured and when |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-11-01 | Repudiation | a phase closed on an assertion rather than a measurement | high | mitigate | Completion is a derived equality over the ledger's own table with a floored premise, a failure message that names the missing items, and a denominator derived from three named sources. Three plants prove the check discriminates before it is trusted. |
| T-32.1-11-02 | Tampering | a ledger row mutated outside its tool | high | mitigate | Every transition runs through the tool's verb; the diff is asserted to show the tool's own rendering across both representations and the counters. A hand edit would throw on the counter cross-check rather than landing. |
| T-32.1-11-03 | Spoofing | a reconciliation that agrees by accident | high | mitigate | The two representations are reconciled by row IDENTIFIER with a symmetric difference of zero. A positional tally is refused by name, because escaped separators in at least three descriptions shift it by three. |
| T-32.1-11-04 | Repudiation | evidence corrected by rewriting it | high | mitigate | The erratum stands beside a byte-unchanged original, asserted by `git diff --exit-code` on the original in the verify set, and it declares itself a new convention rather than borrowing authority from one that does not exist. |
| T-32.1-11-05 | Repudiation | a waive reason that states a schedule instead of a limit | medium | mitigate | The reason sentences are the ones plan 08 measured against the post-rewrite closure, each naming a structural limit and the authority that does or does not cover it. The waive verb is one-shot, which is why they were finalized a plan earlier. |
| T-32.1-11-06 | Elevation of Privilege | governance loosened silently by lowering the open count | medium | mitigate | The open-count movement is recorded before and after with the governance effect named, rather than left for a later reader to discover that a gate stopped blocking. |
| T-32.1-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package and the derived check depends on nothing outside Node's standard library; `32.1-RESEARCH.md` § Package Legitimacy Audit records zero additions and zero `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts. |
</threat_model>

<verification>
1. `node .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs` — exits 0
   with a measuring PASS line.
2. `git diff --exit-code -- .planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md` — exits 0.
3. `npm run check:nul-bytes && npm run check:diff-disposition && npm run check:build-parity` — all exit 0.
4. `npx vitest run --exclude '**/scripts/e2e/**'` — the full regression lane, exits 0.
5. `32.1-11-GREEN-proof.txt` carries the equality's output, the identifier reconciliation, the
   open-count movement, and the phase's closing counts beside plan 01's starting counts.
</verification>

<success_criteria>
- Nineteen items, nineteen terminal dispositions, both counted by a check a reader can re-run.
- The denominator derived from three named sources, with the competing counts reconciled explicitly.
- Two waived rows written by the ledger's own verb with structural reasons.
- One erratum beside a byte-unchanged original, declared a new convention.
- The phase closes green, with its numbers recorded beside the numbers it started from.
</success_criteria>

<output>
Create `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-SUMMARY.md` when done.
</output>

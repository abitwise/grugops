---
phase: 32.1-board-dashboard-deferred-residuals
plan: 15
subsystem: testing
tags: [completion-ledger, broken-windows-register, live-e2e, prod-deploy-guard, diagnosis, discrimination-plants]

requires:
  - phase: 32.1-10
    provides: "the single authorized live claude-CLI run of 2026-09-18, its three red cases, and the transcript recording that their cause was not determined by that run"
  - phase: 32.1-11
    provides: "the completion ledger, its three-source derived denominator, the second derivation taken without the ledger's participation, and the by-identifier register reconciliation"
  - phase: 32.1-13
    provides: "the closure of the composed-path defect at all 42 unowned published substitutions, and the measured finding that no site needed a semantic decision"
  - phase: 32.1-14
    provides: "the two review-warning closures, the seven information-item dispositions, and the final text of the three register rows this plan appends"
provides:
  - "All three live-lane failures carry a named cause class with evidence, produced at ZERO token spend"
  - "The safety mechanism's offline state established by executing the real guard, and stated separately from the live confirmation signal"
  - "A human NO-GO on a second live run, recorded as a legitimate terminal outcome rather than a failure"
  - "The completion equality widened to FOUR named sources and twenty-nine items, with the fourth source derived from the review's own headings"
  - "The identity between the verifier's single FAILED truth and the review's first warning recorded as ONE item"
  - "The register transitioned by its own tool across five rows, reconciling by identifier with a symmetric difference of zero"

actuals:
  tokens: 25766
  tasks: 3
  commits: 2
plan_head_before: 3602d7b728aa662cfae69365ba8968a06b9596bb

tech-stack:
  added: []
  patterns:
    - "A completion equality must enumerate its OWN review; an equality blind to the review of the code it measures is an equality over the wrong set"
    - "Widen a denominator only in the HARDER direction, and record the direction as the test that distinguishes a scope change from a moved goalpost"
    - "Two instruments finding the same defect is ONE item, not two: enumerate the defect, not the sightings"
    - "A diagnosis is not a closure: a row whose cause is now known still stays open until something is reproduced closed"
    - "Extend the tool FIRST and take the red, so the document's later agreement is attributable"

key-files:
  created:
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-15-DIAGNOSIS.md
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-15-GREEN-proof.txt
  modified:
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs
    - .planning/WINDOWS.md

key-decisions:
  - "The human answered NO-GO at the blocking-human checkpoint on 2026-09-18, so Task 3 was skipped entirely. No live lane ran, nothing was spent, and `32.1-15-LIVE-TRANSCRIPT.txt` was not created. This is recorded as a legitimate terminal outcome of that checkpoint, which is what the plan's own text calls it, not as a failure of the round."
  - "The A2-live deny assertion is UNSATISFIABLE on the channel it reads, measured rather than argued: a PreToolUse deny is sent to the model, hook lifecycle events ride `--include-hook-events` which needs `--output-format stream-json`, and the matcher scores the same envelope `true` raw and `false` inside a `json` result string. The correction is known and is NOT applied, because it is not provable offline and a safety case asserted against a guessed envelope is worse than an honest red."
  - "The fourth source contributes the review's INFORMATION items as well as its warnings. An information item is work the phase did; leaving it outside the enumeration is the same blindness one register lower."
  - "The verifier's single failed must-have truth and the review's first warning are ONE defect observed twice and are enumerated once, as `32.1-WR-01`. Recording both would have inflated the denominator by one and then satisfied both copies with a single closure."
  - "Rows 211, 212 and 213 were deliberately NOT transitioned. Their disposition is `re-homed-with-named-owner`, for which the register's vocabulary has no status, and marking them `fixed` because a diagnosis now exists would be a fabrication."
  - "Row 221 is WAIVED rather than fixed: its subject is a committed plan document this repository does not rewrite (row 209's precedent), so the correction is available only as a measurement, and that measurement is filed."
  - "No row was appended for an un-narrowable site. The plan anticipated one; plan 32.1-13 measured the set empty, so writing a row anyway would have been a fabricated register entry."

patterns-established:
  - "Take the RED on the tool before the document agrees with it — a widening applied to both in one step proves only that they agree"
  - "Plant on a NEW member of a widened set, not on an old one: an old-member plant cannot show the new rows are inside the check's reach"
  - "When a register verb cannot edit a description, record the correction in the ledger and leave the stale text as the historical record it is"

requirements-completed: [DASH-03, DASH-05, DASH-06, DASH-07, DASH-08]

coverage:
  - id: D1
    description: "All three live-lane failures (A1, A2-live, A3-live) carry a named cause class with its evidence and the observation that would distinguish it from the next most likely class, produced at zero token spend"
    requirement: DASH-03
    verification:
      - kind: other
        ref: "grep -v '^#' 32.1-15-DIAGNOSIS.md | grep -cE 'A1|A2-live|A3-live' — 3 or more sections, exit 0"
        status: pass
      - kind: unit
        ref: "scripts/prod-deploy-deny-match.test.ts — 10 passed (the offline deny-matcher contract the diagnosis rests on)"
        status: pass
      - kind: unit
        ref: "scripts/check-uat-oracles.test.ts — 21 passed (the Tier-1 oracle roster including oracleHooksWiring)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The prod-deploy safety MECHANISM is established intact by executing the real committed guard, and that claim is kept separate from the live APPEARANCE claim a matcher over agent-authored output can observe"
    requirement: DASH-03
    verification: []
    human_judgment: true
    rationale: "Whether the two claims are adequately separated for a later reader — the thing that prevents a safety red being either over-read or explained away — is a judgment about prose that no test asserts. The mechanism's own state IS measured (32.1-15-DIAGNOSIS.md § 2.1, the guard executed directly with the A2-live payload returning the deny envelope); what needs a human is whether the separation reads clearly enough to survive the next reader."
  - id: D3
    description: "The completion equality names four sources and twenty-nine items, derives its denominator twice, and pins the source count off three"
    requirement: DASH-08
    verification:
      - kind: other
        ref: "node .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs — exit 0, PASS line carries 29 of 29 over a denominator of 29 from 4 named sources, plus the same 29 derived again from the four source documents"
        status: pass
    human_judgment: false
  - id: D4
    description: "Three discrimination plants prove the extended check still names the item that is missing, still notices a silently deleted row, and still refuses an empty parse — each run against a copy, with the real ledger byte-unchanged"
    requirement: DASH-08
    verification:
      - kind: other
        ref: "32.1-15-GREEN-proof.txt § 3.1-3.4: plant exits 1/1/1 against a mirror, unplanted control exit 0, cmp of the real ledger against its pre-plant pristine copy exit 0"
        status: pass
      - kind: other
        ref: "git diff --exit-code -- .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md after the plan's commit — exit 0"
        status: pass
    human_judgment: false
  - id: D5
    description: "Every register transition ran through the ledger tool's verb; the two representations reconcile by identifier with a symmetric difference of zero and the four frontmatter counters are self-consistent"
    requirement: DASH-05
    verification:
      - kind: other
        ref: "reconcile.mjs — table 224 / array 224, only-in-table [], only-in-array [], SYMMETRIC DIFFERENCE 0, per-identifier status agreement, counters re-derive; exit 0"
        status: pass
      - kind: other
        ref: "the plan's counter verify — `counters consistent: open 196 fixed 25 waived 3 total 224`, exit 0"
        status: pass
      - kind: other
        ref: "git diff --stat -- .planning/WINDOWS.md on the uncommitted tree — 52 insertions / 10 deletions, frontmatter counters and BOTH representations moving as one unit (32.1-15-GREEN-proof.txt § 4.6)"
        status: pass
    human_judgment: false
  - id: D6
    description: "The round closes on a green regression lane and a green gate sweep, recorded beside plan 32.1-12's starting numbers"
    requirement: DASH-06
    verification:
      - kind: unit
        ref: "npx vitest run --exclude '**/scripts/e2e/**' — Test Files 75 passed (75), Tests 5248 passed | 2 skipped (5250), exit 0"
        status: pass
      - kind: other
        ref: "npm run build / freshness / check:build-parity / check:dashboard-readonly / check:platform-shapes / check:nul-bytes / check:diff-disposition — all exit 0 (32.1-15-GREEN-proof.txt § 5.1)"
        status: pass
    human_judgment: false
  - id: D7
    description: "The live lane ran ZERO times in this round, behind a blocking-human checkpoint that named the spend and the wall clock, and the NO-GO is recorded as a legitimate terminal outcome"
    requirement: DASH-03
    verification:
      - kind: other
        ref: "32.1-15-LIVE-TRANSCRIPT.txt does not exist; `git diff --exit-code 3602d7b7 -- scripts/e2e/uat-live.test.ts` exits 0 (byte-unchanged)"
        status: pass
    human_judgment: true
    rationale: "Whether the NO-GO was the right call is the human's own decision, already taken at the checkpoint. What is recorded here is that it was asked with the cost stated as a floor, answered, and honoured — a verifier should confirm the record matches what the human actually answered."

duration: 79 min
completed: 2026-09-18
status: complete
---

# Phase 32.1 Plan 15: The completion contract absorbs this round Summary

**All three live-lane failures diagnosed statically at zero token spend, a human NO-GO on the re-run recorded as a legitimate outcome, and the completion equality widened from nineteen items over three sources to twenty-nine over four — with this phase's own code review as the fourth, derived from its headings rather than typed.**

## Performance

- **Duration:** 79 min
- **Started:** 2026-09-18T16:56Z
- **Completed:** 2026-09-18T18:15Z
- **Tasks:** 3 of 4 (Task 3 skipped on a human NO-GO)
- **Files modified:** 5 (2 created, 3 modified)

## Accomplishments

- **A safety-relevant red that nobody had diagnosed now has a cause.** Each of the three live cases carries a named cause class, its evidence, and the observation that would distinguish it from the next most likely class — produced with **zero tokens spent**. The strongest finding: A2-live's assertion is **structurally unsatisfiable on the channel it reads**, for every input, on every host, in every CLI version, because a PreToolUse deny is sent to the model rather than to stdout and the matcher fails closed on an envelope escaped inside a JSON string value.
- **The safety mechanism is established intact, and stated separately from the live signal.** The committed guard was executed directly with the A2-live probe payload and returned the structured deny envelope with all three attributing fields. A red on whether an envelope *appears* in agent-authored text is not evidence against whether the guard *refuses*.
- **The completion equality can now see its own review.** Ten items — three warnings and seven information items from `32.1-REVIEW.md` — entered the denominator, each with exactly one terminal disposition backed by an artefact on disk. The fourth source is a **derivation** in the tool, cross-checked against the review's own frontmatter counters, not a number in a sentence.
- **One defect observed twice is recorded as one item.** The verifier's single FAILED must-have truth and the review's first warning are the same defect seen by two instruments, and are enumerated once.
- **The register is longer, not shorter, after a gap-closure round.** One fix, one waive, three appends: `open_count` 195 → 196, `total_count` 221 → 224. That is the correct direction when the round's honest output is three named limits and a diagnosis.

## Task Commits

1. **Task 1: Diagnose all three live failures statically, at zero token spend** — `708eeb9e` (docs)
2. **Task 2: Checkpoint — named go before the live lane** — no commit; the human answered **`no.go`**
3. **Task 3: One gated run of the live lane** — **SKIPPED**; its `<precondition>` (a human GO) was not met
4. **Task 4: The completion contract absorbs this round, and the register is reconciled** — `688e110f` (docs)

**Plan metadata:** see the final `docs(32.1-15)` commit.

`commits: 2` is `git rev-list --count 3602d7b7..HEAD`, MEASURED at SUMMARY write from the persisted base, before this file's own commit.

## Files Created/Modified

- `.planning/phases/.../32.1-15-DIAGNOSIS.md` — created; one section per live case with a cause class, its evidence, the distinguishing observation, the concrete change, and every unsettled question marked `UNKNOWN - verify`
- `.planning/phases/.../32.1-15-GREEN-proof.txt` — created; the RED before the widening, the PASS after, the three plants, the by-identifier reconciliation, the counter arithmetic, the open-count movement and the closing sweep
- `.planning/phases/.../32.1-LEDGER.md` — four sources, twenty-nine items, § 1.3 (the 19-vs-29 reconciliation and the one-defect-twice identity), § 3.1 extended with the diagnosis and the NO-GO, §§ 3.4/3.5 (two new re-homed owners), § 4.5 (the five register transitions)
- `.planning/phases/.../32.1-ledger-check.mjs` — a fourth derivation arm reading the review's own headings, the source-count pin moved off three, both denominators still derived
- `.planning/WINDOWS.md` — row 220 `fixed`, row 221 `waived`, rows 222/223/224 appended; every transition through the tool's verb

## Decisions Made

See `key-decisions` in the frontmatter. The two that most shape the round:

**The NO-GO is the record, not an absence of one.** Task 1's own recommendation was NO-GO, on the ground that a re-run of the lane as it stands cannot change any of the three attributions: two are to suite expectations, which a re-run cannot alter, and the third is to a per-call budget a re-run would have to change *first* to be informative. The human agreed. Nothing was spent, and the three cases now carry strictly more than they did before — a cause class with evidence where they previously carried "cause undiagnosed by that run".

**The deny case was not weakened, skipped or narrowed.** Three candidate suite corrections were identified and every one of them changes what the live lane OBSERVES, which only a live run can confirm. `scripts/e2e/uat-live.test.ts` is byte-unchanged and the A2-live assertion is identical. Making a safety case green by making it ask less is the worst outcome available here.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical] The fourth derivation arm cross-checks the review against its own frontmatter counters**

- **Found during:** Task 4
- **Issue:** The plan specifies a fourth arm that "reads the review's headings directly". A heading somebody mistyped (`## WR-04:` instead of `### WR-04:`) would then silently shrink the fourth source, and the ledger table would be widened to match it — a silently short source, which is the exact class the second derivation exists to catch one register up.
- **Fix:** The arm derives the size from the headings AND asserts it against the `findings:` counters the same document publishes about itself in frontmatter. A disagreement returns `ok: false` and reds by name rather than producing a quietly wrong denominator.
- **Files modified:** `.planning/phases/.../32.1-ledger-check.mjs`
- **Verification:** headings 3 + 7 = 10 agree with `warning: 3` / `info: 7`; the check's PASS line reports both.
- **Committed in:** `688e110f`

**2. [Rule 1 - Bug in a stated assumption] No register row exists for an un-narrowable site, because the set is empty**

- **Found during:** Task 4
- **Issue:** The plan directs a row for "any site plan 32.1-13 could not narrow". `32.1-13-SUMMARY.md` records the measured answer: **no** site required a semantic decision — all 42 substitutions already satisfied the builder's `string | number` signature, including the three that already spelled their own fallback.
- **Fix:** No row written. The emptiness is recorded as a measurement in `32.1-LEDGER.md` § 4.5 and `32.1-15-GREEN-proof.txt` § 4.3 rather than left as an unexplained absence.
- **Files modified:** none beyond the two documents
- **Verification:** `32.1-13-SUMMARY.md` § "Known Residuals", third bullet.
- **Committed in:** `688e110f`

**3. [Rule 3 - Blocking] Row 220's description cannot be corrected by the ledger tool**

- **Found during:** Task 4
- **Issue:** The row's text understates its own closure — it names 16 remaining `path`-class sites where plan 32.1-13 moved all 42. The plan requires every register change to run through the tool's verb, and the tool offers `status`, `waive` and `append` but no description edit, so the correction cannot be made where the error is.
- **Fix:** The row is set `fixed` by the verb and the correction is recorded in `32.1-LEDGER.md` § 4.5 and `32.1-15-GREEN-proof.txt` § 4.2. This is the treatment row 209 received for the same class of stale text — a register row is a historical record, and rewriting it destroys the record.
- **Files modified:** `.planning/WINDOWS.md` (by verb), `32.1-LEDGER.md`, `32.1-15-GREEN-proof.txt`
- **Verification:** `git diff -- .planning/WINDOWS.md` shows no hand-shaped edit; both representations and the counters moved as one unit.
- **Committed in:** `688e110f`

---

**Total deviations:** 3 auto-fixed (1 × Rule 1, 1 × Rule 2, 1 × Rule 3).
**Impact on plan:** No scope creep. Each is a correctness or honesty fix inside this plan's own artefacts; none touches production source.

## Authentication Gates

None. The live lane — the only surface in this plan that consumes an ambient authenticated session — was not run.

## Issues Encountered

None beyond the three deviations. The live `npm test` e2e lane was deliberately not run at any point; the regression lane was driven as `npx vitest run --exclude '**/scripts/e2e/**'` throughout, per this repository's guidance and this plan's prohibitions.

## Known Stubs

None. This plan ships no code: it moves two documents, the ledger check tool, and the register. No stub, placeholder, skipped test or exemption entry was introduced, and `scripts/e2e/uat-live.test.ts` is byte-unchanged.

Three items this plan touched remain **open by design**, each with a named owner rather than a closure, and each is a register row rather than a sentence here:

| row | owner | why it stays open |
|-----|-------|-------------------|
| 211 | `scripts/e2e/uat-live.test.ts` (A1) | cause class named (per-call budget, with a host-configuration rider); the correction raises a number this diagnosis cannot derive |
| 212 | `scripts/e2e/uat-live.test.ts` (A2-live) | **safety case.** The assertion is unsatisfiable on the channel it reads; the correction needs an envelope shape nothing on disk records. The MECHANISM is measured intact |
| 213 | `scripts/e2e/uat-live.test.ts` (A3-live) | the case asserts a verdict the kit is specified to withhold in the scaffold it builds; retargeting it is a decision needing a live run to confirm |
| 214 | `scripts/e2e/uat-live.test.ts` | the stale docblock; this round modified no source file |
| 222 | `scripts/check-build-parity.ts` | the `git diff`-derived moved-outputs set; changing it is a decision about the gate's subject |
| 223 | `scripts/check-foundation-guards.test.ts` | guardedness decided per enclosing function; closing it is a tightening, not a repair |
| 224 | `package.json` / the declared-command-shape rule | ACCEPTED double compile; the alternative is a shape exemption the guard deliberately refuses |

## Threat Flags

None. This plan introduced no network endpoint, auth path, file-access pattern or schema change. `T-32.1-15-01`'s mitigation held exactly as written: the diagnosis was read-only over the manifest, the entry and the guard's contract, the offline point-of-effect oracle was run and its result recorded, and the deny case was neither weakened, skipped nor narrowed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The phase's completion equality now covers its own review: **29 of 29 items carry exactly one terminal disposition**, over a denominator derived twice and proved to discriminate by three plants against copies.
- The register reconciles by identifier with a symmetric difference of zero and self-consistent counters at `196 + 3 + 25 = 224`.
- The regression lane and the full gate sweep are green at this commit.
- **Outstanding for whoever verifies this phase:** the three live-case rows are `open` on purpose and a diagnosis is not a closure. Row 212 is a safety case whose *mechanism* is measured intact and whose *live confirmation* is structurally unavailable on the channel the case reads. The cheapest next observation is named and costs nothing: capture `claude plugin marketplace list` and `claude plugin list` inside the throwaway repository immediately after the install spawns.
- This was **round two of the standing four-round gap-closure cap**. No adversarial red-team round ran inside the phase.

---
*Phase: 32.1-board-dashboard-deferred-residuals*
*Completed: 2026-09-18*

## Self-Check: PASSED

- Every file in `key-files.created` and `key-files.modified` exists on disk (`[ -f ]` on all six).
- Both task commits are reachable in `git log --all`: `708eeb9e` (Task 1), `688e110f` (Task 4).
- `32.1-15-LIVE-TRANSCRIPT.txt` confirmed ABSENT, as a NO-GO requires.
- `scripts/e2e/uat-live.test.ts` byte-unchanged since the plan's base: `git diff --exit-code 3602d7b7 -- scripts/e2e/uat-live.test.ts` exits 0.
- `node .../32.1-ledger-check.mjs` exits 0; `git diff --exit-code -- .../32.1-LEDGER.md` exits 0 after the commit (no plant residue).

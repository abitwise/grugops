32.1-07 — RED BASELINE
Plan: 32.1-07 (D-07 / F-14 / ledger row 200 — one refusal-sentence builder that escapes by construction)
Tree: /Users/olgeroeselg/Projects/public/grugops, main @ 87e68e0f before Task 1
Date: 2026-09-18

Every control byte below is SPELLED (`<U+0085>`), never written raw, and so is every character class
this transcript quotes. A raw C1 byte in this file would be the very defect the plan closes, one
document over; a regex written with backslash-u escapes is quoted in `U+XXXX` prose for the same
reason — the tooling that reads this file must not have to guess.

═══════════════════════════════════════════════════════════════════════════════════════════════════
§ 1 — F-14 REPRODUCED END TO END, THROUGH THE PUBLISHED DOCUMENT
═══════════════════════════════════════════════════════════════════════════════════════════════════

The tree: `scripts/fixtures/board-snapshot` copied to a temp root, plus

    plans/tickets/ABC-901.md   frontmatter `id: ABC-902<U+0085>X`   (U+0085 NEL, a C1 control)
    plans/board.md             one added row `- [ABC-901] the F-14 reproduction`

`TICKET_CONTROL` (scripts/board-model.ts) refuses U+0000..U+0008, U+000B..U+001F and U+007F and says
nothing about the C1 block; `TICKET_KEY_LINE`'s value capture admits every code point but TAB; so the
document is ADMITTED. `RENDER_STRIPPED` covers U+0000..U+001F, U+007F..U+009F and DELETES it.

--- BEFORE (HEAD 87e68e0f, committed build) ------------------------------------------------------
$ node scripts/board-dashboard.js <tree> --once --json

conflict: {"kind":"ticket-unplaced","ticketId":"ABC-902X","expected":"a row on the board",
           "actual":"no row names ABC-902X","source":"tickets"}
conflict: {"kind":"row-without-file","ticketId":"ABC-901","column":"Backlog",
           "expected":"plans/tickets/ABC-901.md",
           "actual":"plans/tickets/ABC-901.md exists and declares the identifier ABC-902X, so it is
                     joined under that identifier and not this one","source":"board"}
readErrors naming ABC-90*: []
raw control code points surviving on stdout: [10]
control code points recoverable from the PARSED document: []

The published document states, TWICE, that `plans/tickets/ABC-901.md` declares `ABC-902X`. It
declares `ABC-902<U+0085>X`. A reader who searches the tree for `ABC-902X` finds nothing; a reader
who re-types it produces a different identifier. That is `visible()`'s own docblock sentence — "a
reader following that message re-types the line exactly as printed and is refused again" — one field
over. No byte leaves the tree: the failure is a snapshot that misquotes its own evidence.

--- AFTER (Task 1: `spelled` owns the three sentences) -------------------------------------------
$ npm run build && node scripts/board-dashboard.js <tree> --once --json

conflict: {"kind":"ticket-unplaced","ticketId":"ABC-902X","expected":"a row on the board",
           "actual":"no row names ABC-902<U+0085>X","source":"tickets"}
conflict: {"kind":"row-without-file","ticketId":"ABC-901","column":"Backlog",
           "expected":"plans/tickets/ABC-901.md",
           "actual":"plans/tickets/ABC-901.md exists and declares the identifier ABC-902<U+0085>X,
                     so it is joined under that identifier and not this one","source":"board"}
readErrors naming ABC-90*: []
raw control code points surviving on stdout: [10]
control code points recoverable from the PARSED document: []

The identifier the sentence prints is now the identifier the file declares, spelled in the uniform
`<U+XXXX>` notation `visible()` already used for a refusal's quoted line. Nothing raw reaches a
channel — that was already true and is unchanged; what changed is that the byte is SPELLED where the
sentence is BUILT instead of silently dropped where it is written.

NAMED RESIDUAL, MEASURED RATHER THAN IMPLIED. The `ticketId` FIELD still reads `ABC-902X` in both
conflicts. That field is the model's own VALUE — data a consumer joins on, scrubbed on the way out
by `scrub()`/`sanitizeCell` — not a sentence a human reads, and CONTEXT.md D-07 scopes this builder
to "every interpolation into a published sentence". Escaping a join key would make the document
disagree with itself in the other direction. The residual is stated here and in the SUMMARY rather
than closed silently; it belongs to the data channel (D-18), not to the sentence builder.

═══════════════════════════════════════════════════════════════════════════════════════════════════
§ 2 — THE EXISTING SENTENCE DERIVATION REFUSED THE NEW SHAPE BY NAME (total by refusal)
═══════════════════════════════════════════════════════════════════════════════════════════════════

`scripts/board-model.test.ts`'s plan-32-38 derivation parses `presenceActual`'s return sites and
turns them into templates. Its docblock says a construct it does not recognise THROWS, "so a branch
expressed a new way reds here rather than vanishing from the derived set and taking its coverage with
it". The cutover to a tagged template is exactly such a new shape, and the derivation behaved as
documented BEFORE it was taught about the tag:

$ npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts

  × derives the branch count by parsing `presenceActual`, and pins it against the audited rows
  × every derived branch is reached by a named input, and every named input reaches a derived branch
  × parses a NON-EMPTY, NON-SHORT table whose row count is DERIVED from the code's branch count
  × asserts SET EQUALITY IN BOTH DIRECTIONS between the table and the sentences the code produces

  Error: presenceActual returns an expression shape this derivation does not know:
  TaggedTemplateExpression. Teach templatesOf about it — a shape it cannot read is a branch that
  would silently leave the derived set.

  Test Files  1 failed (1)    Tests  4 failed | 150 passed (154)

The arm added for it is NOT a blanket "a tag is transparent": it admits the identifier `spelled` and
refuses every other tag by name, because any other tag is a function that could rewrite the template
TEXT, and reading that as the identity would let a sentence the contract never saw pass as one it did.

═══════════════════════════════════════════════════════════════════════════════════════════════════
§ 3 — THE ROUTING DISCRIMINATES: THREE MIRROR PLANTS, ONE PER MODULE
═══════════════════════════════════════════════════════════════════════════════════════════════════

A structural change nobody has watched fail is not yet a control. Each plant removes ONE builder
call from the live source, rebuilds, drives a planted tree end to end through the published document,
and is then RESTORED. All three were restored and the tree rebuilt; `git diff` afterwards carries
only this plan's intended changes.

The tree, for all three plants:

    plans/board.md                one row `- [ABC-901] the F-14 reproduction`
    plans/tickets/ABC-901.md      `id: ABC-902<U+0085>X`
    plans/tickets/ABC-903.md      `id: ABC-902<U+0085>X`   (the same identifier, so the read seam
                                                            raises `duplicate-id` naming both files)

Three observation points, one per module:
    [A] `conflicts[row-without-file].actual`   — built in `scripts/board-model.ts`
    [B] `readErrors[duplicate-id].message`     — built in `scripts/board-read.ts`
    [C] the stderr frame line                  — built in `scripts/board-dashboard.ts`

--- GREEN (no plant) -----------------------------------------------------------------------------
[A] "plans/tickets/ABC-901.md exists and declares the identifier ABC-902<U+0085>X, so it is joined
     under that identifier and not this one"
[B] "ABC-903.md and ABC-901.md both claim the identifier ABC-902<U+0085>X. Ticket identifiers are
     unique and the first by file name is joined, so ABC-901.md is the one joined and ABC-903.md is
     not. Neither document is modified and no conflict is invented for the second."
[C] "board-dashboard: tickets at <tree>/plans/tickets/ABC-903.md — duplicate-id: ABC-903.md and
     ABC-901.md both claim the identifier ABC-902<U+0085>X. …"

--- PLANT A — `scripts/board-model.ts`, the presence sentence's tag removed -----------------------
[A] "… declares the identifier ABC-902X, …"        <-- THE BYTE IS SILENTLY GONE AGAIN
[B] unchanged (spelled)
[C] unchanged (spelled)

--- PLANT B — `scripts/board-read.ts`, the duplicate-id message's first tag removed ---------------
[A] unchanged (spelled)
[B] "ABC-903.md and ABC-901.md both claim the identifier ABC-902X. …"   <-- SILENTLY GONE AGAIN
[C] STILL SPELLED — and this is the measurement worth keeping. The dashboard escapes what it quotes
    at the point IT builds its line, so a message another module built WITHOUT the builder is still
    spelled on the channel a human watches. The two routings are independent, not one applied twice:
    the JSON document lost the byte in the same run in which the stderr frame kept it.

--- PLANT C — plant B, plus `scripts/board-dashboard.ts`'s emit-frame tag removed -----------------
[A] unchanged (spelled)
[B] "… both claim the identifier ABC-902X. …"      <-- gone (plant B)
[C] "board-dashboard: tickets at <tree>/plans/tickets/ABC-903.md — duplicate-id: ABC-903.md and
     ABC-901.md both claim the identifier ABC-902X. …"                  <-- SILENTLY GONE AGAIN

Each plant moves exactly the observation point its module owns, and no other. The difference is
therefore caused by the removed call rather than by a rebuild that changed something else.

═══════════════════════════════════════════════════════════════════════════════════════════════════
§ 4 — THE TWO-AUTHORITY PIN SURVIVES, AND CONTEXT.md's RETIREMENT SENTENCE NAMES NOTHING
═══════════════════════════════════════════════════════════════════════════════════════════════════

CONTEXT.md D-07 ends "The site-list derivation is retired." MEASURED ON THIS TREE: there is no
site-list derivation in `scripts/`. The "both sites" claim it refers to is PROSE, at
`.planning/phases/32-board-projector-cli-dashboard/32-REVIEW-FIX.md:115-118`. The only
derivation-shaped artefact near `visible()` is a DIFFERENT thing:

    scripts/board-dashboard.test.ts
      it("visible() escapes exactly the code points sanitizeCell deletes — derived, both directions")

which walks every code point from U+0000 through U+00A5, asserts the two functions' sets EQUAL in
both directions, floors the denominator against vacuity, and asserts the escape TEXT survives the
sanitizer. It is a TWO-AUTHORITY PIN, not a site list, and it is the reason this plan's builder can
escape upstream of a scrub without double-escaping. It SURVIVES. It ran green on every run recorded
in this transcript. A reader following the retirement sentence must not delete it; nothing on this
tree answers to that sentence at all.

═══════════════════════════════════════════════════════════════════════════════════════════════════
§ 5 — THE OWNERSHIP CENSUS DISCRIMINATES, MUTATION-PROVEN FROM THE LIVE SOURCE
═══════════════════════════════════════════════════════════════════════════════════════════════════

The census in `scripts/board-model.test.ts` derives, over one `ts.Program` built by
`createScriptsProgram`:

    template substitutions in the three board modules                         171   (the element set)
    …of those, written to a position a human reads as a finding                86   (the denominator)
    …of those, quoting a CONTENT-derived declaration                           23   (the subject set)
    substitutions built through `spelled`                                      41   (the owned set)
    content-derived substitutions NOT built through `spelled`                   0   (the census)

The class is keyed on the DECLARATION each expression resolves to through `declarationOf`, never on
how the reference is spelled. The receiver set (27 keys) and the declaration set (41 keys) are each
pinned two-sided, so a new sentence position or a new value reaching one reds rather than silently
leaving the denominator.

--- SEEDED: the ticket-unplaced builder call removed from the LIVE source ------------------------
$ npx vitest run --exclude '**/scripts/e2e/**' scripts/board-model.test.ts

  × EVERY content-derived substitution is built through the builder — the census itself

  AssertionError: a content-derived value is interpolated into a published sentence WITHOUT the
  builder. What it costs is F-14 exactly: an admitted control byte is deleted by the renderer, so
  the sentence quotes an identifier no file declares and a reader who searches for it finds nothing.
  Build it with `spelled`. Adding the site to an exemption list is the forbidden alternative — an
  exemption list is the rotting set this census replaced

  - Expected   []
  + Received   [ "board-model.ts:1688 `t.id` (board-model.ts#id)" ]

  × the OWNED set has the expected COUNT, and every owned site is in a published position

  AssertionError: the number of substitutions built through `spelled` moved. Removing one is how
  F-14 comes back; adding one is a decision worth seeing in a diff: expected 40 to be 41

The refusal NAMES the site — module, line, the expression it interpolates and the declaration that
expression resolves to — and the owned member count moved by EXACTLY ONE. The seeded site was chosen
because it carries exactly one substitution, so a move of one is caused by the seeded change rather
than by a derivation that broke and started reporting some other number. The denominator did NOT
move under it (86 before and after), which is the converse check: the mutation removed a TAG, not a
substitution.

The same mutation is now a PERMANENT case pair. It re-runs the whole pipeline — program, type
checker, declaration resolution — over a `board-model.ts` whose text is mutated at the compiler
host's `getSourceFile`, so nothing on disk is touched and the mirror cannot drift away from the
source it copies.

The live source was restored and the build re-run; `npx vitest run scripts/board-model.test.ts`
reports 168 passed.

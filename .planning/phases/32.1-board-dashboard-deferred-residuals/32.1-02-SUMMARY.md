---
phase: 32.1-board-dashboard-deferred-residuals
plan: 02
subsystem: infra
tags: [audit-register, guard-diff-disposition, lang-03, style-dispositions, windows-row-176]

requires:
  - phase: 31-autonomous-manual-testing
    provides: "the two commits that introduced all 38 undispositioned clauses (f311c9f6, d7b61839) and the decisions D-16 and D-03 they cite"
  - phase: 29-controlled-language
    provides: "docs/audit/29-style-dispositions/ — the register, its README contract, the recorded base commit 4d2b8f0, and the guard_diff_disposition gate itself"
provides:
  - "docs/audit/29-style-dispositions/32.1-02.md — 38 binding disposition rows over agent-factory/workflows/05-pr-quality-gate.md, the largest single share of WINDOWS.md row 176"
  - "the gate's finding count moved 78 -> 40 with `over 39 elements` unchanged, so the fall is provably from rows and not from a narrowed comparison"
  - "32.1-02-RED-baseline.txt — the gate's RED capture with its count derived three independent ways from that one capture, the per-file tally, the 77-versus-78 explanation, and the commit-to-clause mapping for all 38 rows"
  - "32.1-02-GREEN-proof.txt — the count arithmetic filed as a measurement, with the non-zero exit explained rather than left to be misread"
affects: [32.1-03]

actuals:
  tokens: 28113
  tasks: 2
  commits: 2
plan_head_before: 4ae60b98a05e8200984df4a282160876f6dfcd0b

tech-stack:
  added: []
  patterns:
    - "Attribute a clause to its introducing commit with the REPOSITORY'S OWN segmenter — import `normalizeSentence`/`segmentClauses` from the committed `scripts/voice-model.js` and diff the clause SET between consecutive commits, rather than eyeballing a word diff or trusting a commit subject"
    - "Pre-flight a disposition table against the gate's own matcher before running the gate: assert `normalizeSentence(after) === clause` for every row, so a row that would bind to nothing is caught as a row rather than as a surviving finding"
    - "Derive the count twice from ONE capture and assert the two agree before acting on either — the instrument-artefact guard that Phase 32 rounds 3 and 4 each paid a paragraph for"

key-files:
  created:
    - docs/audit/29-style-dispositions/32.1-02.md
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-RED-baseline.txt
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-GREEN-proof.txt
  modified: []

key-decisions:
  - "Four rows carry a REAL `before` rather than the em dash the plan's cell rule prescribed for an (added) finding. README.md defines that column as `the clause as it read before the change`, and a genuine predecessor existed for rows 1, 2, 26 and 35 — read out of the introducing commit's word diff. An em dash there would have been the less faithful reading of the column, and binding is unaffected because the gate matches on either side."
  - "The `after` cell holds the VERBATIM source sentence, not the gate's normalized print of it. The normalized form would have bound trivially and told a human reader nothing; the verbatim form was pre-flighted through `normalizeSentence` for all 38 rows before the gate was re-run."
  - "`d7b61839`'s three rows carry the commit's own subject line in the `rule` column because its body names no decision id and no profile rule id. A stated unknown is admissible in this register; a rule id invented to fill a cell is not."
  - "The two decision ids cited (`D-16`, `D-03`) were verified present in `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` (lines 104 and 40) BEFORE being written into any cell — a decision id read out of a commit message is still a citation, and this repository does not ship unchecked citations."
  - "`over 39 elements` is asserted UNCHANGED across both captures, alongside the headline fall. The count falling is consistent with rows being written AND with the comparison seeing less; the element count holding steady is what distinguishes them."

patterns-established:
  - "A proof transcript whose subject exits non-zero states the expected exit and the reason IN THE TRANSCRIPT, above the capture — an `EXIT=1` at the foot of a GREEN proof is otherwise read as the plan failing"
  - "Confirm a structural property (here: that none of this plan's findings is frozen) against the capture rather than inheriting it from research, and say in the document that it was confirmed rather than assumed"

requirements-completed: [DASH-02]

coverage:
  - id: D1
    description: "38 binding disposition rows over agent-factory/workflows/05-pr-quality-gate.md, seven columns in the register's order, parsed by the gate as 38 rows with zero refusals"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "npm run check:diff-disposition 2>&1 | grep -c '05-pr-quality-gate.md:[0-9]* (' -> 0"
        status: pass
      - kind: other
        ref: "readDispositionRows() over the live register: 38 rows sourced from 32.1-02.md, 0 refusals naming it, 0 companion cells that are not an em dash, line values {31,35,49}"
        status: pass
    human_judgment: false
  - id: D2
    description: "The gate's headline finding count fell by exactly this file's share — 78 - 38 = 40 — with `over 39 elements` unchanged on both sides"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "32.1-02-RED-baseline.txt headline (78) vs 32.1-02-GREEN-proof.txt headline (40); per-file tally after = 25+10+2+3 = 40, four names where there were five"
        status: pass
    human_judgment: false
  - id: D3
    description: "No evidence was deleted to reach the result: the base commit, the watched corpus, the override tiers and the clause comparison are all untouched"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/ -> exit 0"
        status: pass
      - kind: other
        ref: "npm run check:nul-bytes -> exit 0, 2310 tracked files scanned as raw bytes"
        status: pass
      - kind: other
        ref: "npx vitest run --exclude '**/scripts/e2e/**' -> exit 0, 75 files / 5170 passed / 2 skipped, identical to 32.1-01-GREEN-proof.txt"
        status: pass
    human_judgment: false
  - id: D4
    description: "The commit-to-clause mapping: every one of the 38 rows joined to the commit that introduced its clause, derived with the repository's own segmenter"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "32.1-02-RED-baseline.txt DERIVATION 4 — 38 mapping lines, group counts 25/3/7/3 summing to 38; D-16 and D-03 verified at 31-CONTEXT.md:104 and :40"
        status: pass
    human_judgment: false
  - id: D5
    description: "The substance of the 38 dispositions — whether each judgement about why a clause reads the way it does is CORRECT"
    verification: []
    human_judgment: true
    rationale: "This directory's README states it outright: the register records that a change was considered, not that the consideration was correct. No gate reaches it and no additional check would — the question is what the sentence means to an agent reading it. LANG-03's manual verification is a named human reading these rows against the diff they describe. That reading has not happened and this plan does not claim it has."
  - id: D6
    description: "The D-19 transcript pair: 32.1-02-RED-baseline.txt and 32.1-02-GREEN-proof.txt, each recording a measurement rather than a claim"
    verification: []
    human_judgment: true
    rationale: "Whether a transcript records enough for a later reader to reproduce the measurement is a judgment about evidence quality. A test counting its sections would be a census over a document rather than a check on it."

duration: 19 min
completed: 2026-09-17
status: complete
---

# Phase 32.1 Plan 02: Disposition Rows for the Pull-Request Quality Gate Summary

**38 binding disposition rows over `agent-factory/workflows/05-pr-quality-gate.md`, each joined to the commit that introduced its clause by the repository's own segmenter — moving `guard_diff_disposition` from 78 findings to 40 with `over 39 elements` unchanged, so the fall is provably from rows written and not from evidence deleted.**

## Performance

- **Duration:** 19 min
- **Started:** 2026-09-17T19:13:04Z
- **Completed:** 2026-09-17T19:32:54Z
- **Tasks:** 2
- **Files created:** 3 (no file modified)

## Accomplishments

- **The largest single share of WINDOWS.md row 176 is closed, by writing rows.** 38 of the 78 findings the gate reports on this tree are against one document, and all 38 now carry a row that binds. The gate names `agent-factory/workflows/05-pr-quality-gate.md` **zero** times. Four names remain in the per-file tally where there were five, and the one that left did so because rows were written, not because it stopped being watched.
- **The count was derived three ways from ONE capture before a row was written.** Headline (`78 finding(s)`), `clause:`-line count (78), and finding-header count (78) — asserted to agree. Rounds 3 and 4 of Phase 32 each spent a paragraph on an instrument artefact at exactly this spot; this plan spent a grep instead.
- **The 77-versus-78 arithmetic is explained rather than rounded.** Counting only `(added)` lines gives 77 because exactly one finding in the corpus is a REMOVAL. It is not in this file — all 38 here are `(added)` — so every row puts its clause on the `after` side.
- **The commit-to-clause mapping was derived, not eyeballed.** For each of the eight commits touching the file since base `4d2b8f0`, the file's content at that commit was segmented with the committed `scripts/voice-model.js` — the exact `normalizeSentence` + sentence/clause split the gate uses — and each clause attributed to the first commit containing it whose predecessor does not. All 38 land on two commits: 35 on `f311c9f6`, 3 on `d7b61839`. A four-insertion commit introducing 35 clauses is not an anomaly: the three lines it touches are each ONE source line carrying dozens of sentences, which is also why 38 findings sit on only three line numbers.
- **Decision ids were verified before being cited.** `f311c9f6`'s body names `D-16` and `D-03`; both were confirmed present in `31-CONTEXT.md` (lines 104 and 40) before either reached a cell. `d7b61839`'s body names none, so its three rows carry that commit's subject line — a stated unknown, not an invented rule id.
- **The frozen set was checked against the capture, not assumed away.** All three `FROZEN by structuralSections` findings are in `18-context-compaction.md`'s `## Stop conditions` region and belong to plan 03. That is why every `companion` cell here is an em dash, and the document says it was confirmed rather than presumed.
- **Nothing was weakened to get there.** The four forbidden remedies — narrowing the corpus, adding an override tier, moving the base commit, loosening the clause comparison — are each enumerated in the document with what taking it would have cost, and none was taken. `git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/` exits 0.

## Task Commits

1. **Task 1: measure the gate from its own output, and record the RED baseline honestly** — `45332c3c` (docs)
2. **Task 2: write the disposition rows for the pull-request quality-gate workflow** — `9c4c30b0` (docs)

## Files Created/Modified

- `docs/audit/29-style-dispositions/32.1-02.md` — the audit record: title, what forced the change (with the three-way count derivation, the per-file tally, the two capture gotchas and the base commit), where the clauses came from (the attribution table and the decision ids), the four refused weakenings with their costs, an explicit "what this document does not claim", and `## Dispositions` carrying 38 seven-column rows.
- `.planning/phases/.../32.1-02-RED-baseline.txt` — the unedited gate capture plus four derivations: the count taken three ways, the per-file tally with the non-finding name called out, the base commit, and the 38-line commit-to-clause mapping with its group counts.
- `.planning/phases/.../32.1-02-GREEN-proof.txt` — the arithmetic (`78 - 38 = 40`, measured 40), the zero-mentions proof, the untouched-evidence checks, the regression lane, and the unedited AFTER capture.

## Decisions Made

See `key-decisions` in the frontmatter. The two a later reader most needs:

1. **The `after` cell holds the verbatim source sentence, not the gate's normalized print of it.** Writing the normalized form would have bound trivially and told a human nothing — and a register whose rows only a machine can read defeats the purpose of a register. The verbatim form was pre-flighted through `normalizeSentence` for all 38 rows (and checked for pipes, which would silently split a row into 8 cells and drop it) before the gate was re-run.
2. **`over 39 elements` is asserted unchanged alongside the headline fall.** A falling finding count is consistent with rows being written AND with the comparison seeing less than it saw before. The element count holding steady at 39 on both sides is what tells those two apart, and it is the assertion a reader checking this plan's honesty should look at first.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Four rows carry a real `before` where the plan's cell rule said em dash**

- **Found during:** Task 2 (writing the seven-column rows)
- **Issue:** The plan's cell rule reads *"For a finding the gate marks as added, `before` is an em dash and `after` is the clause."* Applied literally, four of the 38 rows would have recorded an em dash where a genuine predecessor sentence exists: row 1 (`The lint, UI/E2E and test-integrity steps are config-dialed under quality` gained a fourth member), row 2 (a colon-terminated order sentence split in two and gained `uat-spec-integrity`), row 26 (`...at either probe stage.` gained `, which only the flag above reaches`) and row 35 (the `emit-verdict` invocation gained the `<sha>` slot). `docs/audit/29-style-dispositions/README.md` defines that column as *"the clause as it read before the change"*, so an em dash there records a falsehood about the change — in an audit register whose entire value is that a later reader can trust it.
- **Fix:** Those four cells carry the predecessor text, read out of the introducing commit's `git diff --word-diff` rather than reconstructed. The remaining 34 are pure additions and carry an em dash. The choice and its reasoning are stated in the document itself, in the paragraph beginning *"Four rows carry a real `before`"*, so the divergence is visible to a reader of the register and not only to a reader of this summary.
- **Verification:** Binding is unaffected — the gate matches on `before OR after`, every row was pre-flighted with `normalizeSentence(after) === clause`, and the re-run reports zero findings naming the file. A second pre-flight assertion confirmed no `before` cell normalizes to the SAME clause as its own `after` (which would mean the reworded text had not actually changed).
- **Committed in:** `9c4c30b0` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Confined to four cells; it makes the register more faithful to its own documented column semantics and changes nothing about what binds. No scope creep — no source file, test, export, CLI shape or ledger row was touched, exactly as `<artifacts_this_phase_produces>` specifies.

## Issues Encountered

None. Both tasks' verification sets passed first time, and the gate's post-row count matched the predicted arithmetic exactly (predicted 40, measured 40) on the first re-run.

One thing worth flagging for plan 03 rather than recording as a problem: the gate still exits **1**, and will until plan 03's rows land. `32.1-02-GREEN-proof.txt` states that expectation above its capture precisely so an `EXIT=1` at the foot of a file named "GREEN-proof" is not misread as this plan having failed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Plan 32.1-03 can start immediately.** It owns the remaining 40 findings across four documents: `06-uat-pack.md` (25), `16-context-read-write.md` (10), `18-context-compaction.md` (3) and `17-task-claim.md` (2). The one-file-per-plan rule means it writes `32.1-03.md` and cannot contend with this register file.
- **Three of plan 03's findings are FROZEN** — all in `18-context-compaction.md`'s `## Stop conditions` region (lines 70-79), confirmed against this plan's capture. Each owes a filled `companion` cell of at least 8 normalized words; a placeholder is refused by construction, since every placeholder spelling normalizes to at most one word.
- **The attribution method is reusable as-is.** The mapping script pattern in `32.1-02-RED-baseline.txt` DERIVATION 4 — segment each commit's version of the file with the committed `scripts/voice-model.js`, attribute to the first commit containing a clause whose predecessor does not — generalizes to plan 03's four documents without change.
- **WINDOWS.md row 176 is NOT yet closable.** This plan closes 38/78 of it. The row stays open until plan 03's rows land and the gate reports zero findings, at which point the count arithmetic in both proof transcripts is what the closure cites.

## Self-Check: PASSED

- `docs/audit/29-style-dispositions/32.1-02.md` — FOUND on disk
- `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-RED-baseline.txt` — FOUND on disk
- `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-02-GREEN-proof.txt` — FOUND on disk
- Commit `45332c3c` — FOUND in `git log`
- Commit `9c4c30b0` — FOUND in `git log`
- `commits: 2` MEASURED via `git rev-list --count 4ae60b98..HEAD`, not narrated
- Plan verification re-run at close-out: gate exit 1 with **0** findings naming the file; `check:nul-bytes` exit 0; `git diff --exit-code -- 00-base.md agent-factory/` exit 0; regression lane exit 0 at 75 files / 5170 passed / 2 skipped
- Both tasks' `<acceptance_criteria>` re-run and passing; the only initially-ambiguous one (data-row count) was an arithmetic error in the check command, not the document — the gate's own `readDispositionRows()` parses exactly 38 rows from this file with 0 refusals

---
*Phase: 32.1-board-dashboard-deferred-residuals*
*Completed: 2026-09-17*

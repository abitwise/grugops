32.1-05 RED BASELINE — the plants, their commands, their outputs and their exit codes
======================================================================================

Plan:    .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-PLAN.md
Tree:    /Users/olgeroeselg/Projects/public/grugops, branch main
Measured 2026-09-17/18.

Every plant below was applied by appending to (or mutating) THE LIVE SOURCE, never as an authored
fixture, and removed afterwards. `git status --porcelain -- scripts/` and
`git diff --exit-code -- scripts/context-io-writer-set.test.ts` are recorded after the restorations.

The regression lane in this repository is `npx vitest run --exclude '**/scripts/e2e/**'`.
`npm test` is never run here: it launches the live claude-CLI e2e lane.


═══════════════════════════════════════════════════════════════════════════════════════════════════
§ 1  TASK 1 — F-19: THE ONE-AUTHORITY RULE ENUMERATED READS BY THE REGISTER'S IDENTIFIER TEXT
═══════════════════════════════════════════════════════════════════════════════════════════════════

THE PLANT IS THE FINDING'S OWN REPRODUCTION (32-40-ADVERSARIAL-REVIEW.md § 12, F-19): a SECOND,
UNGUARDED authority over a classification register, reached through a local alias binding. Inserted
immediately after `toolchainReason`, inside the same describe block:

    const ALIASED_REGISTER = TOOLCHAIN_CHECK_SCRIPTS;
    const aliasedToolchainReason = (name: string): string | undefined => ALIASED_REGISTER[name];

---------------------------------------------------------------------------------------------------
§ 1.1  THE PRE-CHANGE RULE, AGAINST THAT PLANT — GREEN. This is the finding.
---------------------------------------------------------------------------------------------------

The rule as it stood at HEAD (`c5fc977a`, plan 32-39) collected its read sites with
`node.expression.getText(src) === "TOOLCHAIN_CHECK_SCRIPTS"`, so the aliased read was outside the set
it enumerated and the `reads.length === 1` pin agreed there was exactly one read.

    $ git show HEAD:scripts/check-foundation-guards.test.ts > <old>   # the pre-change rule
    $ <old> + the plant above > scripts/check-foundation-guards.test.ts
    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts

     Test Files  1 passed (1)
          Tests  300 passed (300)

    exit 0

---------------------------------------------------------------------------------------------------
§ 1.2  THE REWRITTEN RULE, AGAINST THE SAME PLANT — RED, naming the aliased read.
---------------------------------------------------------------------------------------------------

The identifier-text comparison is DELETED. Every access base is handed to `declarationOf`
(`scripts/ts-symbols.test-support.ts`), which follows import and export aliases, and a local
`const A = B` binding is followed one hop at a time through that same authority. The aliased read is
therefore INSIDE the set the rule enumerates, and it is attributed to the register it resolves to.

    $ <new> + the same plant > scripts/check-foundation-guards.test.ts
    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts

     × F-19 / IN-03: every classification register is read through ONE own-property authority, at a
       set DERIVED by resolved declaration 946ms

     FAIL  scripts/check-foundation-guards.test.ts > 30-11 round 4 — every check gate is REACHED,
     and the runner set is derived > F-19 / IN-03: every classification register is read through ONE
     own-property authority, at a set DERIVED by resolved declaration
     AssertionError: a computed-key read of a frozen classification register is not own-property
     guarded. `Object.freeze` leaves `Object.prototype` in place, so a raw read answers "registered"
     for a name nobody registered — an exemption granted by spelling instead of by a decision
     (review IN-03, the same class as WR-04):
       TOOLCHAIN_CHECK_SCRIPTS at :12172  ALIASED_REGISTER[name]: expected [ Array(1) ] to deeply
       equal []

     - Expected
     + Received

     - []
     + [
     +   "TOOLCHAIN_CHECK_SCRIPTS at :12172  ALIASED_REGISTER[name]",
     + ]

     Test Files  1 failed (1)
          Tests  1 failed | 300 passed (301)

    exit 1

Note the attribution: the message names `TOOLCHAIN_CHECK_SCRIPTS`, which is not the text the read was
spelled with. That is the whole of the change.

An EARLIER measurement of the same plant, taken before the assertion ORDER was changed so that
findings are reported ahead of moved numbers, red on the pin arm instead:

     AssertionError: TOOLCHAIN_CHECK_SCRIPTS is read with a computed key 2 time(s); the pin says 1.
     exit 1

Both arms discriminate; the finding is now reported first because a bare count mismatch is the least
informative way to learn that a second authority arrived.

---------------------------------------------------------------------------------------------------
§ 1.3  THE SAME REGISTER REACHED THROUGH A RENAMED IMPORT FROM ANOTHER FILE — RED.
---------------------------------------------------------------------------------------------------

Alias following is not a special case for one syntax. A second plant put a frozen classification
register in a DIFFERENT file and read it through a renamed import:

    scripts/plant-register-32105.ts (new, untracked, removed afterwards):
        export const PLANTED_REGISTER: Readonly<Record<string, string>> = Object.freeze({
          "check:planted": "a planted reason, long enough to look like a recorded decision",
        });

    scripts/check-foundation-guards.test.ts (appended):
        import { PLANTED_REGISTER as RenamedPlant } from "./plant-register-32105.js";
        const plantedReason = (name: string): string | undefined => RenamedPlant[name];

    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts

     FAIL  … > F-19 / IN-03: every classification register is read through ONE own-property
     authority, at a set DERIVED by resolved declaration
     AssertionError: the census found a different SET of frozen classification registers than the
     pins record. A register that arrives is a decision (route its computed reads through one
     own-property accessor, then add it here); a register that leaves is a decision too, and the two
     must never cancel into a green count. Found:
     ["CHECK_SCRIPT_CLASSES","CI_EXEMPT","PLANTED_REGISTER","TOOLCHAIN_CHECK_SCRIPTS"]

       [
         "CHECK_SCRIPT_CLASSES",
         "CI_EXEMPT",
     +   "PLANTED_REGISTER",
         "TOOLCHAIN_CHECK_SCRIPTS",
       ]

     Test Files  1 failed (1)
          Tests  8 failed | 293 passed (301)

    exit 1

The census resolved the RENAMED import to the declaration the other file writes — the register is
named `PLANTED_REGISTER` in the message, not `RenamedPlant`.

SEVEN OF THOSE EIGHT FAILURES ARE NOT THIS RULE, and saying so is the honest reading: the plant added
a MODULE to `scripts/`, so the module-corpus pins moved as well (`expected 86 to be 85`, and the
`scripts/`-scoped reader's `expected 59 to be 58`). Those are the plan-32.1-01 corpus pins doing
their own job. The eighth is the one quoted above.

---------------------------------------------------------------------------------------------------
§ 1.4  THE VACUITY PLANT — an EMPTIED derived register set reds rather than passing.
---------------------------------------------------------------------------------------------------

The rule's subject is now DERIVED, and a derived set that silently came back empty would make every
assertion in it true over nothing. Task 2 of this plan removes a check-script class and, with it, a
register — so the floor is the control that makes that removal a decision somebody has to look at.

Plant: every `= Object.freeze({` register declaration in the file loses its canonical form
(`= ({`), so the census finds nothing.

    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts

     FAIL  … > F-19 / IN-03: every classification register is read through ONE own-property
     authority, at a set DERIVED by resolved declaration
     AssertionError: the frozen-classification-register census came back with 0 member(s). A rule
     whose subject is a single deletable register does not go RED when that register is deleted — it
     silently stops asking, and every assertion below becomes true over nothing. If a class was just
     removed, the honest answer is to keep a second register or to retire this rule deliberately,
     never to let it assert over one member or none: expected 0 to be greater than 1

     FAIL  … > F-19: an ALIASED read of a register resolves to the SAME declaration as a direct one
     AssertionError: the checker resolved NO reference to a register declared in this very file. The
     instrument is not resolving, so the census built on it decides nothing: expected 0 to be
     greater than 0

     Test Files  1 failed (1)
          Tests  2 failed | 299 passed (301)

    exit 1

The floor is a control, not a comment: it is the assertion that fires, and it fires FIRST.

---------------------------------------------------------------------------------------------------
§ 1.5  THE GREEN SIDE, AND THE RESTORATIONS.
---------------------------------------------------------------------------------------------------

    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts
     Test Files  1 passed (1)
          Tests  301 passed (301)
    exit 0

    $ npx tsc -p tsconfig.tests.json
    exit 0   (no output)

After every plant was removed:

    $ git status --porcelain -- scripts/
     M scripts/check-foundation-guards.test.ts        # this plan's own change, nothing else

    $ git diff --exit-code -- scripts/context-io-writer-set.test.ts
    exit 0   # residual R-31-21-04: that file is byte-unchanged and the instrument never reaches it


═══════════════════════════════════════════════════════════════════════════════════════════════════
§ 2  TASK 2 — D-06: THE BUILD-PARITY CHECK BECOMES AN ORDINARY GATE MODULE
═══════════════════════════════════════════════════════════════════════════════════════════════════

WHAT IT WAS. `package.json` carried, as the value of `check:build-parity`:

    npm run build && git diff --exit-code --name-only -- '*.js'
      && echo 'Build parity: no tracked build output moved when tsc ran.'
      || node -e "console.error('BUILD PARITY FAILED: …'); process.exit(1)"

— a nested `npm run`, a `git` invocation, a shell `echo`, an `||` arm, and a `node -e` carrying the
operator remedy as a JavaScript string inside a JSON string inside a shell command. It was the one
`check:*` entry that named no module, which is why it had to be carried as the single member of a
recorded TOOLCHAIN exemption.

WHAT IT IS. `scripts/check-build-parity.ts` with its committed `scripts/check-build-parity.js` twin,
`package.json`'s entry collapsed to `tsc --outDir .tmp-build && node scripts/check-build-parity.js`,
and a ci.yml step naming the module directly.

---------------------------------------------------------------------------------------------------
§ 2.1  THE MOVED-OUTPUT PLANT — the new gate refuses, naming the output.
---------------------------------------------------------------------------------------------------

Plant: a comment line appended to `scripts/is-entry.ts` with its `.js` left uncommitted, so the build
MOVES a tracked build output. This is the defect the gate is about, reproduced rather than described.

    $ node scripts/check-build-parity.js

      FAIL  Build parity: tracked build outputs that moved when the build ran: 1 finding(s) over 66
            elements
        scripts/is-entry.js moved when the build ran — the committed build output is not a build of
        the source committed beside it
            Remedy: run `npm run build` and commit the result, so the committed .js is a build of
            its .ts.

      == Result ==
      1 CHECK(S) FAILED

    exit 1

THE CONVERSE, on the real tree after `git checkout -- scripts/is-entry.ts && npm run build`:

    $ node scripts/check-build-parity.js
      PASS  Build parity: tracked build outputs that moved when the build ran: 0 findings over 66/66
            elements

      == Result ==
      ALL CHECKS PASSED

    exit 0

The PASS line is a MEASUREMENT — 66 outputs examined, 0 moved — and not the bare sentence the inline
`echo` printed.

---------------------------------------------------------------------------------------------------
§ 2.2  THE REMOVED-CI-STEP PLANT — the gate-module reachability proof reds, naming the module.
---------------------------------------------------------------------------------------------------

Plant: in a mirrored `.github/workflows/ci.yml`, the step `run: node scripts/check-build-parity.js`
is replaced with `run: npm run build`, so the module is named by nothing in the workflow.

    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts

     × every scripts/check-*.js appears in ci.yml, or declares why not
     AssertionError: these check gates are invoked by NOTHING in ci.yml:
       scripts/check-build-parity.js
     A gate that is never reached passes by not running — round 3 shipped one for a whole round.:
     expected [ 'scripts/check-build-parity.js' ] to deeply equal []

     Test Files  1 failed (1)
          Tests  1 failed | 299 passed (300)

    exit 1

That is the proof the exemption bought nothing and the module gained everything: while the check was
an inline compound, no CI step could name a module, and this case could say nothing about it at all.

---------------------------------------------------------------------------------------------------
§ 2.3  THE CLASS MOVE, MEASURED.
---------------------------------------------------------------------------------------------------

The entry left the toolchain class and joined the gate-module class, so both target counts moved:

    gate-module      9 -> 10
    suite-test-file  1 (unmoved)
    toolchain        1 -> 0, and the class, its register, its accessor, its classifier arm and its
                     reachability case were DELETED with a deletion note

THE FLOOR TASK 1 INSTALLED WAS CHECKED BEFORE THE DELETION, NOT AFTER. The one-authority rule asserts
over a derived census of this file's frozen classification registers, floored above one. Deleting
`TOOLCHAIN_CHECK_SCRIPTS` takes that census from THREE members (`CI_EXEMPT`,
`TOOLCHAIN_CHECK_SCRIPTS`, `CHECK_SCRIPT_CLASSES`) to TWO, which clears the floor — so the deletion
proceeded, and the plan's alternative branch (keep the class with a stated reason) was not taken.

Two pins moved with sets they measure, in this task's commit:

    NON_TEST_MODULE_COUNT                85 -> 86   (`scripts/check-build-parity.ts`)
    the `scripts/`-scoped module corpus  58 -> 59   (the same module, both pins in one commit)
    the entry-guard source set           14 -> 15   (scripts/check-claim-anchors.test.ts)
    CI_EXEMPT's accessor call count       2 -> 4    (the IN-03 case re-homed onto this register)


═══════════════════════════════════════════════════════════════════════════════════════════════════
§ 3  TASK 3 — D-05 / F-21: THE SEPARATOR ALPHABET REPLACED BY A DECLARED COMMAND SHAPE
═══════════════════════════════════════════════════════════════════════════════════════════════════

WHAT STOOD HERE. `NODE_STEP_RE = /(^|&&|\|\||;)\s*node\b(?!\s+(?:-e|--eval)\b)/g` and
`VITEST_STEP_RE = /vitest run\b/g`, counting the command STEPS that invoke a runner and comparing
that count with the classification rows. Its separator alphabet was exactly: start-of-string, `&&`,
`||`, `;`.

WHAT REPLACES IT. Two DECLARED command shapes, matched anchored at both ends, with the step counts
derived from WHICH shape an entry matched. An entry outside the declared shapes is a REFUSAL NAMING
THE ENTRY. Zero exemptions: task 2 removed the only entry that needed one.

---------------------------------------------------------------------------------------------------
§ 3.1  THE SIX SHAPES F-21 NAMED — the old counter and the new rule, side by side.
---------------------------------------------------------------------------------------------------

Measured by evaluating the retired regexes (taken verbatim from `HEAD:scripts/check-foundation-
guards.test.ts:12181-12182` and `:12580-12581`) and the new declared shapes against each command.
The real manifest was never written to.

| Probe entry | Shape | Real runner invocations | OLD: node steps / gate rows | OLD verdict | NEW verdict |
|---|---|---|---|---|---|
| `check:probe-pipe`       | a single pipe                        | 1 | 0 / 1 | DISAGREEMENT (red) | REFUSED, naming the entry |
| `check:probe-newline`    | a newline separator                  | 2 | 1 / 2 | DISAGREEMENT (red) | REFUSED, naming the entry |
| `check:probe-paren`      | a leading parenthesis                | 2 | 1 / 2 | DISAGREEMENT (red) | REFUSED, naming the entry |
| `check:probe-wrapper`    | a wrapper command before the runner  | 1 | 0 / 1 | DISAGREEMENT (red) | REFUSED, naming the entry |
| `check:probe-background` | a background separator               | 2 | 1 / 2 | DISAGREEMENT (red) | REFUSED, naming the entry |
| `check:probe-quoted`     | a runner name in a quoted message    | 1 | 1 node + 1 vitest step / 1 gate + 0 suite rows | DISAGREEMENT (red) | REFUSED, naming the entry |

THE MEASUREMENT CORRECTED THE EXPECTATION THIS PLAN WAS WRITTEN WITH, and the correction is recorded
rather than smoothed over. The plan expected "five that the old counter miscounted silently, one it
overcounted". Each of the six IS miscounted exactly as round 4's § 10 table says — but a miscount
alone makes the counter DISAGREE with the target recognisers, and a disagreement is a red. The old
rule caught all six. What it gave was a bare count mismatch that named no shape and no cause; what
the declared shape gives is a refusal naming the entry, with the remedy and the refused weakening.

---------------------------------------------------------------------------------------------------
§ 3.2  THE SHAPES THAT REALLY DID PASS SILENTLY — both readers blind at once.
---------------------------------------------------------------------------------------------------

The silent pass needs a separator outside the counter's alphabet AND a module spelled a way
`GATE_TARGET_RE` cannot read, so a step nobody counted agrees with a row nobody produced. Measured:

| Combination | Real runner invocations | OLD: node steps / gate rows | OLD verdict | NEW verdict |
|---|---|---|---|---|
| a single pipe and a `./`-spelled module                  | 1 | 0 / 0 | AGREEMENT — SILENT PASS | REFUSED by name |
| a wrapper command and a module behind a node flag        | 1 | 0 / 0 | AGREEMENT — SILENT PASS | REFUSED by name |
| a background separator and a `./`-spelled second module  | 2 | 1 / 1 | AGREEMENT — SILENT PASS | REFUSED by name |
| a command substitution and a `./`-spelled module         | 1 | 0 / 0 | AGREEMENT — SILENT PASS | REFUSED by name |

All four are permanent cases in the discrimination block, beside the six above.

---------------------------------------------------------------------------------------------------
§ 3.3  THE DECLARED SET, AND WHAT IT REFUSES ON PURPOSE.
---------------------------------------------------------------------------------------------------

Two shapes, both occupied by live entries (the occupancy is asserted, so an unoccupied shape — an
exemption written in advance — is a red):

    build-then-gate-module   ^tsc --outDir \.tmp-build && node scripts/[\w.-]+\.js$   (10 entries)
    suite-runner             ^npx vitest run [\w./-]+\.test\.ts$                       (1 entry)

And the retired compound is refused BY THE SAME RULE, which is what makes the set exemption-free:

    "npm run build && git diff --exit-code --name-only -- '*.js' && echo 'ok' || node -e \"…\""
      => matches no declared shape

Two floors, both derived from the manifest: an EMPTY `check:*` entry set reds, and a silently SHORT
one reds because the entry count is taken a second time from the manifest's RAW TEXT, independently
of the parse and of the prefix filter. The denominator of the match assertion is the ENTRY count,
never the match count.

---------------------------------------------------------------------------------------------------
§ 3.4  THE MANIFEST WAS NOT WRITTEN TO.
---------------------------------------------------------------------------------------------------

    $ git diff --exit-code -- package.json
    exit 0

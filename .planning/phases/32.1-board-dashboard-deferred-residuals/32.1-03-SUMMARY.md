---
phase: 32.1-board-dashboard-deferred-residuals
plan: 03
subsystem: infra
tags: [audit-register, guard-diff-disposition, lang-03, style-dispositions, windows-row-176, frozen-companion]

requires:
  - phase: 32.1-board-dashboard-deferred-residuals
    provides: "plan 32.1-02's 38 rows for agent-factory/workflows/05-pr-quality-gate.md, its RED/GREEN transcript pair, and the commit-to-clause attribution method its DERIVATION 4 established"
  - phase: 31-autonomous-manual-testing
    provides: "the six commits that introduced all 40 remaining undispositioned clauses, and the D-31 / WP-02 / WP-03 / WR-15 / WR-28 / T-31-22 ids their bodies cite"
  - phase: 29-controlled-language
    provides: "docs/audit/29-style-dispositions/ — the register, its README contract, the recorded base commit 4d2b8f0, the guard_diff_disposition gate, and isCompanionFilled's derived 8-word floor"
provides:
  - "docs/audit/29-style-dispositions/32.1-03.md — 40 binding disposition rows over the UAT-pack, context read/write, task-claim and context-compaction workflows, the last three of them FROZEN rows carrying real companion cells"
  - "check:diff-disposition exits 0 for the first time since 2026-09-14, at `PASS  diff disposition — changed watched file(s): 0 findings over 39/39 elements`"
  - "32.1-03-RED-baseline.txt — the gate re-measured on this tree at 40, the count derived two ways from one capture, the frozen set confirmed from the capture, and a 40-line commit-to-clause mapping with zero unattributed rows"
  - "32.1-03-GREEN-proof.txt — the four-count arithmetic 78 -> 40 -> 3 -> 0, each number attributed to the capture it was read from, with the untouched-evidence checks beside it"
  - "the diagnosis that 31-29.md's existing row for the three frozen clauses binds none of them, because all three sentences sit in ONE `after` cell and the gate normalizes a single cell against a single clause"
affects: [32.1-11]

actuals:
  tokens: 22604
  tasks: 2
  commits: 2
plan_head_before: d082b3ed82f3eb8f638b75d08262660473e21510

tech-stack:
  added: []
  patterns:
    - "Bind a FROZEN finding by writing ONE row per clause: the gate normalizes a single `after` cell and compares it to a single clause, so a cell packing a multi-sentence split binds nothing however correctly it reads to a human"
    - "Pre-flight a frozen row through the gate's OWN `isCompanionFilled`, imported from the committed `scripts/check-diff-disposition.js`, before re-running the gate — the companion floor is then measured by the predicate that will judge it rather than estimated"
    - "State a companion cell as WHERE the fact is derived rather than as the fact itself: a cell that restates a number becomes a fourth place for that number to drift"
    - "Assert the INVARIANTS beside the falling count — `over 39 elements`, `2273 changed clause(s) derived`, and a disposition-row total that moved by exactly the findings closed over exactly the files added"

key-files:
  created:
    - docs/audit/29-style-dispositions/32.1-03.md
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-RED-baseline.txt
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-GREEN-proof.txt
  modified: []

key-decisions:
  - "The three frozen clauses got THREE rows, one clause each, rather than one row carrying the split. `31-29.md` already carries the one-row form and it binds none of them: the gate matches a row to a finding by normalizing ONE cell against ONE clause, so a cell holding three sentences equals no clause at all. This is the finding that explains why these three survived every prior plan."
  - "`docs/audit/29-style-dispositions/31-29.md` was NOT edited to make its row bind. A register records what a plan recorded; correcting an earlier plan's row would destroy the trail the register exists to keep. The non-binding row is explained in this plan's document instead."
  - "The companion cells state WHERE the per-position operation count is derived — by subtraction in the `31-29 — WR-28` case's `forge` helper in `scripts/context-io.test.ts` — and never restate the number. A companion cell that repeated the count would become a fourth artifact for it to drift in, in a register whose entire purpose is that the artifacts agree."
  - "The companion cells say plainly that the count landed in the SIBLING commit `a81ba78d` of the same plan, and that the split commit `daa5e586` moved word order and no number. The contract asks for a same-commit companion edit; the honest answer here is that the artifacts were priced one commit earlier and their agreement held across the split because a live case checks it. A cell narrating a tidier story would be the fabrication this register refuses."
  - "One row puts its clause on the `before` side with an em dash after it — the single REMOVAL finding in the whole corpus on this tree. It is given its own row because the gate reports it as its own finding, and its disposition names the row above it as the addition half of the same reword."
  - "Ten rows carry a real `before` rather than an em dash, for the same reason plan 02's four did: `README.md` defines that column as the clause as it read before the change, and a genuine predecessor sentence was read out of the introducing commit's diff in each case."

patterns-established:
  - "Import the gate's own predicate to pre-flight against it: `isCompanionFilled` from the committed `.js` measured all three cells before the gate ran, so a refusal would have surfaced as a cell rather than as a surviving finding"
  - "Explain a non-binding row in an EARLIER plan's file in the CURRENT plan's file, and leave the earlier file alone"

requirements-completed: [DASH-02]

coverage:
  - id: D1
    description: "40 binding disposition rows over the four remaining watched documents — 25 for the UAT-pack workflow, 10 for the context read/write workflow, 2 for the task-claim workflow and 3 for the context-compaction workflow"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "npm run check:diff-disposition -> exit 0, `PASS  diff disposition — changed watched file(s): 0 findings over 39/39 elements`"
        status: pass
      - kind: other
        ref: "grep -c '^| agent-factory' docs/audit/29-style-dispositions/32.1-03.md -> 40; the gate's own readDispositionRows reports 2017 rows across 35 files, up from 1939 across 33"
        status: pass
    human_judgment: false
  - id: D2
    description: "Each of the three FROZEN findings carries a companion cell that clears the canonical filled form, measured by the gate's own predicate"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "isCompanionFilled (imported from the committed scripts/check-diff-disposition.js) -> true for all three; normalized word counts 121 / 118 / 118 against COMPANION_MIN_WORDS = 8 and the live register's smallest honest cell of 12"
        status: pass
      - kind: other
        ref: "the gate's structural arm is satisfied for all three clauses: 3 findings after Task 1, 0 after Task 2"
        status: pass
    human_judgment: false
  - id: D3
    description: "Row 176's closure is a REPRODUCTION: four counts in order, each attributed to the gate capture it was read from"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "32.1-03-GREEN-proof.txt — 78 (32.1-02-RED-baseline.txt:36), 40 (32.1-02-GREEN-proof.txt:95, confirmed at 32.1-03-RED-baseline.txt:38), 3 (Task-1 capture embedded), 0 (final capture embedded); 38 + 37 + 3 = 78"
        status: pass
    human_judgment: false
  - id: D4
    description: "No evidence was deleted to reach exit 0 — the base commit, the watched corpus, the override tiers, the clause comparison and the ledger are all untouched"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "git diff --exit-code -- docs/audit/29-style-dispositions/00-base.md agent-factory/ .planning/WINDOWS.md -> exit 0"
        status: pass
      - kind: other
        ref: "`over 39 elements` and `2273 changed clause(s) derived` identical in the first and last captures; watched corpus 40 markdown files in both"
        status: pass
      - kind: other
        ref: "npm run check:nul-bytes -> exit 0, 2315 tracked files scanned as raw bytes, no docs/audit/ path named"
        status: pass
      - kind: other
        ref: "npx vitest run --exclude '**/scripts/e2e/**' -> exit 0, 75 files / 5170 passed / 2 skipped, identical to 32.1-02-GREEN-proof.txt"
        status: pass
    human_judgment: false
  - id: D5
    description: "The commit-to-clause attribution: every one of the 40 findings joined to the commit that introduced its clause, derived with the repository's own segmenter"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "32.1-03-RED-baseline.txt DERIVATION 5 — 40 mapping lines, group counts 23/5/4+1/3/2/2 summing to 40, ZERO unattributed; D-31, WP-02, WP-03, WR-15 and T-31-22 each verified at a named line before citation"
        status: pass
    human_judgment: false
  - id: D6
    description: "The SUBSTANCE of the 40 dispositions — whether each judgement about why a clause reads the way it does is CORRECT, and whether each frozen companion cell names the right companion"
    verification: []
    human_judgment: true
    rationale: "This directory's README states it outright: the register records that a change was considered, not that the consideration was correct. No gate reaches it. The frozen cells carry the sharper version of the same limit — `isCompanionFilled` measures LENGTH, and nothing mechanical can check that the artifact a cell names is the right companion. LANG-03's manual verification is a named human reading these rows against the diff they describe; that reading has not happened and this plan does not claim it has."
  - id: D7
    description: "The D-19 transcript pair: 32.1-03-RED-baseline.txt and 32.1-03-GREEN-proof.txt, each recording a measurement rather than a claim"
    verification: []
    human_judgment: true
    rationale: "Whether a transcript records enough for a later reader to reproduce the measurement is a judgment about evidence quality. A test counting its sections would be a census over a document rather than a check on it."

duration: 25 min
completed: 2026-09-17
status: complete
---

# Phase 32.1 Plan 03: The Remaining Four Watched Documents, and the Gate at Exit 0 Summary

**40 binding disposition rows over the UAT-pack, context read/write, task-claim and context-compaction workflows — the last three of them FROZEN rows whose companion cells name where the per-position forgery price is DERIVED rather than restating it — taking `guard_diff_disposition` from 40 findings to `PASS ... 0 findings over 39/39 elements` and exit 0 for the first time since 2026-09-14.**

## Performance

- **Duration:** 25 min
- **Started:** 2026-09-17T19:44:44Z
- **Completed:** 2026-09-17T20:09:41Z
- **Tasks:** 2
- **Files created:** 3 (no file modified, no file deleted)

## Accomplishments

- **The gate exits 0.** `PASS  diff disposition — changed watched file(s): 0 findings over 39/39 elements`, `ALL CHECKS PASSED`. This is the first exit 0 for `check:diff-disposition` since 2026-09-14, and it is the assertion plan 32.1-02 deliberately left to this plan.
- **All 40 remaining findings carry a row that binds.** 25 over `agent-factory/workflows/06-uat-pack.md`, 10 over `agent-factory/workflows/16-context-read-write.md`, 2 over `agent-factory/workflows/17-task-claim.md`, and 3 over `agent-factory/workflows/18-context-compaction.md`. The gate's own `readDispositionRows` moved 1939 rows across 33 files to 2017 across 35 — up by exactly the 78 findings plans 02 and 03 closed, over exactly the 2 files they added.
- **The three FROZEN findings bind, and the reason they had not is now recorded.** `docs/audit/29-style-dispositions/31-29.md` already carried a row for these three clauses, written by the plan that authored them. It binds none of them: it packs all three sentences into ONE `after` cell, and the gate matches a row to a finding by normalizing a SINGLE cell and comparing it to a SINGLE clause. A cell holding three sentences equals no clause at all. Three rows carrying one clause each fixed it, and `31-29.md` was left alone.
- **The companion cells were pre-flighted through the gate's own predicate.** `isCompanionFilled`, imported from the committed `scripts/check-diff-disposition.js`, returned true for all three before the gate was re-run — 121, 118 and 118 normalized words against a mechanical floor of 8 and a live-register minimum of 12. A cell that would have been refused would have surfaced as a cell, not as a surviving finding.
- **The count was derived two independent ways from ONE capture before a row was written.** Headline (`40 finding(s)`), `clause:`-line count (40) and finding-header count (40), asserted to agree. Phase 32 rounds 3 and 4 each paid a paragraph for an instrument artefact at exactly this spot; this plan paid a grep.
- **The four-count arithmetic makes row 176's closure a reproduction.** 78 (before plan 02) to 40 (after plan 02, read from two captures taken hours apart) to 3 (after this plan's Task 1) to 0 (now). `38 + 37 + 3 = 78`: every finding the gate reported at the start of plan 02 is accounted for by a row that was WRITTEN.
- **The commit-to-clause mapping was derived, not eyeballed.** Each document's content at every commit in `4d2b8f0..HEAD` was segmented with the committed `scripts/voice-model.js` and each clause attributed to the first commit containing it whose predecessor does not. All 40 land on six commits with ZERO unattributed rows. Every id cited in a `rule` cell — `D-31`, `WP-02`, `WP-03`, `WR-15`, `T-31-22` — was verified at a named line before it reached a cell.
- **Nothing was weakened.** The four forbidden remedies are enumerated in the document with what taking each would have cost, plus a fifth available only to this plan — a companion cell long enough to pass the floor while naming no real companion. None was taken. `git diff --exit-code -- 00-base.md agent-factory/ .planning/WINDOWS.md` exits 0, and `over 39 elements` plus `2273 changed clause(s) derived` are identical in the first and last captures.

## Task Commits

1. **Task 1: the three non-frozen documents — 37 rows, and the RED baseline** — `37899a78` (docs)
2. **Task 2: the three frozen rows with real companion cells, and the gate at exit 0** — `efddd2fa` (docs)

## Files Created/Modified

- `docs/audit/29-style-dispositions/32.1-03.md` — the audit record: title, what forced the change (the two-way count derivation, the per-file tally, the removal-finding note, the base commit, the frozen set confirmed from the capture), where the clauses came from (the six-commit attribution table and the verified ids), a section on the three frozen rows and the non-binding row that already existed for them, the five refused weakenings, an explicit "what this document does not claim", and `## Dispositions` carrying 40 seven-column rows.
- `.planning/phases/.../32.1-03-RED-baseline.txt` — the unedited RED capture plus five derivations: the count taken two ways, the per-file tally, the base commit read from the register rather than the branch, the frozen set confirmed from the capture with the owed form quoted, and the 40-line commit-to-clause mapping with its group counts and its verified id list.
- `.planning/phases/.../32.1-03-GREEN-proof.txt` — the four counts in order with their capture attributions, the arithmetic, why `over 39 elements` matters as much as the headline, the frozen-floor measurement, the 31-29.md finding, the untouched-evidence checks, the nul-byte and regression results, and both the Task-1 and final captures verbatim.

## Decisions Made

See `key-decisions` in the frontmatter. The two a later reader most needs:

1. **Three rows, not one, for the three frozen clauses — and the earlier non-binding row stays.** The gate's matcher is `normalizeSentence(row.before) === clause || normalizeSentence(row.after) === clause`. A cell carrying a three-sentence split normalizes to a string equal to no clause. This is the whole reason those three findings outlived the plan that authored them, and it is recorded in the register rather than repaired by editing that plan's row.
2. **The companion cells name a derivation, not a number — and they are honest about "same-commit".** The per-position price is stated in three artifacts and held in agreement by a live case; the cells point at that case and at the `forge` helper's subtraction, and they say plainly that the pricing landed in the sibling commit `a81ba78d` while the split commit `daa5e586` moved word order and no number. A cell narrating a tidier story would be the fabrication this register exists to refuse.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Ten rows carry a real `before` where the plan's cell rule said em dash**

- **Found during:** Task 1 (writing the seven-column rows)
- **Issue:** The plan's cell rule reads *"the clause on the side matching whether the gate marks it added or removed"*, which taken literally puts an em dash in `before` for every `(added)` finding. Ten of the 37 findings are genuine REWORDINGS or SPLITS with a real predecessor sentence: two from `7e47a233` (the config path and the disposition vocabulary), one from `9c6f9698` (the `human_admission` dial sentence), and five from `4ee640a0` plus two more from the same commit's second split (the resolution-order chain and the lean-default sentence). `README.md` defines that column as *"the clause as it read before the change"*, so an em dash there records a falsehood about the change, in a register whose entire value is that a later reader can trust it. This is the same deviation plan 32.1-02 made for the same reason, and it is recorded again rather than inherited silently.
- **Fix:** Those ten cells carry the predecessor text, read out of the introducing commit's `git show --unified=0` diff rather than reconstructed. The choice is stated in the document itself. The three frozen rows also carry a real `before` — the 30-word sentence the WP-03 split replaced — for the same reason, making 13 in all.
- **Verification:** Binding is unaffected: the gate matches on `before OR after`, and a pre-flight asserted `normalizeSentence(after) === clause` for every non-frozen row and for all three frozen rows, that every row binds at least one finding (no dead rows), that no row's `before` normalizes to its own `after`, and that no cell carries a pipe. The re-run reports zero findings.
- **Committed in:** `37899a78` (Task 1) and `efddd2fa` (Task 2)

**2. [Rule 2 - Missing Critical] The single REMOVAL finding got its own row rather than being folded into the reword**

- **Found during:** Task 1 (`agent-factory/workflows/16-context-read-write.md:22`)
- **Issue:** One row whose `before` is the removed sentence and whose `after` is its replacement would have satisfied BOTH the `(added)` and the `(removed)` findings, since the gate matches on either side. That would have left one of the 37 findings with no row of its own — a register in which a finding count and a row count no longer correspond.
- **Fix:** Two rows. The first records the reword with both sides real. The second records the removal with the removed sentence in `before` and an em dash in `after`, and its disposition names the row above it as the addition half of the same reword, so the duplication reads as what it is rather than as an error.
- **Verification:** The pre-flight's per-finding coverage check confirms both findings bind, and its dead-row check confirms neither row is redundant against the gate's matcher.
- **Committed in:** `37899a78` (Task 1)

---

**Total deviations:** 2 auto-fixed (2 missing critical)
**Impact on plan:** Confined to thirteen `before` cells and one extra row. Both make the register more faithful to its own documented column semantics and change nothing about what binds. No scope creep — no source file, test, export, CLI shape, watched document or ledger row was touched, exactly as `<artifacts_this_phase_produces>` specifies.

## Issues Encountered

None that required problem-solving. Both tasks' verification sets passed first time and the arithmetic matched the prediction exactly at both steps (40 - 37 = 3 measured 3; 3 - 3 = 0 measured 0).

One observation worth carrying rather than recording as a problem: the reason the three frozen findings were still open is a *shape* defect in an existing row, not a missing one. A register that is read by a human and matched by a machine can hold a row that is correct to the first and invisible to the second. The gate says so — its refusal names the cell count and the clause, not the intent — but nothing surfaces a row that parses fine and simply matches nothing. That class is worth a note for whoever next widens this register.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **WINDOWS.md row 176's EVIDENCE is complete.** The gate that named it exits 0, the fall is attributed to 78 rows written across two plans, and none of the five refusable weakenings was taken. The row's terminal disposition is closed-with-reproduction.
- **The row's STATUS TRANSITION belongs to plan `32.1-11`**, which owns every ledger mutation in this phase. That is a mechanical constraint rather than a convention: `.planning/WINDOWS.md`'s frontmatter counters are cross-checked against its entries, so a hand edit throws a malformed-ledger error. This plan left the file byte-unchanged and asserted it.
- **The repository's whole gate sweep is unblocked** for the first time since 2026-09-14, so any later plan in this phase that runs `check:diff-disposition` as part of its own verification now starts from green rather than from a known red it has to reason around.
- **A standing caution for later plans that add rows:** one row per clause. A cell that packs a multi-sentence split reads correctly and binds nothing, which is exactly how `31-29.md`'s row survived two rounds and three plans.

## Self-Check: PASSED

- `docs/audit/29-style-dispositions/32.1-03.md` — FOUND on disk
- `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-RED-baseline.txt` — FOUND on disk
- `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-03-GREEN-proof.txt` — FOUND on disk
- Commit `37899a78` — FOUND in `git log`
- Commit `efddd2fa` — FOUND in `git log`
- `commits: 2` MEASURED via `git rev-list --count d082b3ed..HEAD`, not narrated
- No file deleted across the plan: `git diff --diff-filter=D --name-only d082b3ed..HEAD` is empty
- Plan verification re-run at close-out: `check:diff-disposition` exit 0; `check:nul-bytes` exit 0 over 2315 tracked files; `git diff --exit-code -- 00-base.md agent-factory/ .planning/WINDOWS.md` exit 0; `npx vitest run --exclude '**/scripts/e2e/**'` exit 0 at 75 files / 5170 passed / 2 skipped
- Both tasks' `<acceptance_criteria>` re-run and passing, including the three frozen rows' companion word counts measured through the gate's own `isCompanionFilled`

---
*Phase: 32.1-board-dashboard-deferred-residuals*
*Completed: 2026-09-17*

# Phase 32.1: Board Dashboard Deferred Residuals - Pattern Map

**Mapped:** 2026-09-17
**Files analyzed:** 21 (5 new source/doc files + 1 generated `.js` twin + 13 modified + 2 tool-owned/no-edit)
**Analogs found:** 18 / 21 (3 have no in-repo analog and are listed under "No Analog Found")

**Every control character in this document is spelled as escape TEXT** (`<U+0009>`, `<U+0085>`), never
as a raw byte — `check:nul-bytes` refuses the byte and BSD `grep` silently reports zero matches for
strings that are present.

**Every analog path below was confirmed git-TRACKED** (`git ls-files` non-empty) on 2026-09-17. No
path in this document is an untracked mirror.

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| **NEW** `scripts/<name>.testkit.ts` (D-01/D-02/D-03 checker instrument) | test-support module | transform (source -> symbol resolution) | `scripts/runnable-ref/uat-spec-integrity.ts:1308-1400` (`createProgramForTarget`) for the program build; `scripts/ci-workflow.testkit.ts` for the test-only module shape | exact (split across two analogs) |
| **NEW** loader-oracle helper (D-09) — `scripts/<name>.testkit.ts` or in-file in `scripts/board-readonly.test.ts` | test-support instrument | event-driven (module `resolve` hook) | `scripts/board-readonly.test.ts:1813-1890` (the two-sided parser oracle it becomes a third side of) | role-match (no `node:module` hook precedent exists on this tree) |
| **NEW** `scripts/check-build-parity.ts` (D-06) | gate module / CLI entry | batch -> exit code | `scripts/check-residual-citations.ts` (137 lines, the smallest complete gate) | exact |
| **NEW** `scripts/check-build-parity.js` (D-06) | build output | n/a — emitted by `npm run build` | every tracked `scripts/check-*.js` | exact (never hand-authored; commit in the same commit as its `.ts`) |
| **NEW** `docs/audit/29-style-dispositions/32.1-NN.md` (D-12) | audit data document | record (table rows) | `docs/audit/29-style-dispositions/29-44.md:93+` | exact |
| **NEW** `32-40-ADVERSARIAL-REVIEW-ERRATUM.md` (D-15 / row 209) | evidence document | record | none tracked | **no analog** |
| **NEW** `32.1-NN-RED-baseline.txt` / `32.1-NN-GREEN-proof.txt` / `32.1-NN-LIVE-TRANSCRIPT.txt` (D-19/D-13) | evidence transcript | record | `.planning/phases/32-board-projector-cli-dashboard/32-09-RED-baseline.txt`, `32-22-LIVE-TRANSCRIPT.txt` | exact |
| `scripts/js-import-closure.ts` + `.js` (D-08) | production utility module | transform (source -> specifier set) | `scripts/board-readonly.test.ts:1836-1859` (`parsed()`, `ts.ScriptKind.JS`); `scripts/runnable-ref/uat-spec-integrity.ts:731-734,868-1000` for the injected-`ts` route | exact (parse) / role-match (dependency route) |
| `scripts/validate.test.ts` (D-01, D-03, D-04, D-15/194) | test suite (AST census) | batch transform | `scripts/context-io-writer-set.test.ts` (derive / count / mutation-prove) | exact |
| `scripts/check-foundation-guards.test.ts` (D-05, D-06, F-19) | test suite (manifest + CI guard) | batch, manifest read | its own `(r-class-authority)` case at `:11831-11900`; its own IN-03 case at `:12689-12770` | exact (self-analog) |
| `scripts/board-readonly.test.ts` (D-08 corpus, D-09, D-10, D-11) | test suite (import-graph guard) | batch + event-driven (new) | its own two-sided oracle at `:1813-1890` | exact (self-analog) |
| `scripts/board-model.ts` (D-07 sentence builder) | production model module | transform | `visible()` at `scripts/board-model.ts:1063-1068` (same file) | exact |
| `scripts/board-read.ts`, `scripts/board-dashboard.ts` (D-07 consumers) | production modules | request-response / render | `sanitizeCell` / `warn` / `scrub` at `scripts/board-dashboard.ts:261,286,336,341,392` | exact |
| `scripts/board-model.test.ts` (D-07 cases, D-18 contract equality) | test suite (doc-vs-code equality) | transform + record comparison | `readPresenceTable` / `contractTemplate` / the both-directions equality at `scripts/board-model.test.ts:3268-3400` | shape-match — the watch rule is PROSE, not a 2-column table (divergence stated below) |
| `scripts/board-dashboard.test.ts` (D-07 cases) | test suite | transform | its own derived code-point agreement at `:2729-2756` | exact |
| `scripts/check-diff-disposition.ts` + `.js` (D-15 / row 194) | gate module (one literal reworded at `:1630`) | batch | the deletion-note convention at `scripts/js-import-closure.ts:464-470` | role-match |
| `package.json` (D-06) | config / manifest | data | its own `S1` shape lines `:16-24` | exact |
| `.github/workflows/ci.yml` (D-06 reachability) | config (CI) | data | the commented gate steps at `:280-295`, `:466-478` | exact |
| `tsconfig.json` (D-02 exclude pattern) | config | data | its own `exclude` array + the comment above it explaining the fixtures exclusion | exact |
| `agent-factory/contracts/board.md` (D-18) | normative contract document | record | its own presence table + the record-first rule at `:576-578`; the paragraph being replaced is `:406-412` | exact |
| `.planning/WINDOWS.md` (D-14) | ledger (tool-owned) | n/a — **do not hand-edit** | `gsd-tools windows waive <id> "<reason>"` | **no analog — call the verb** |

---

## Pattern Assignments

### NEW `scripts/<name>.testkit.ts` — the D-01 checker instrument (test-support, transform)

**Analogs:** `scripts/runnable-ref/uat-spec-integrity.ts:1308-1400` (program build) and
`scripts/ci-workflow.testkit.ts:1-28` (the test-only-module shape, including its NAME).

> **CONTEXT.md's stated precedent is wrong.** `scripts/context-io.test.ts` builds no `ts.Program`;
> its only `createProgram(` occurrence is residual `R-31-21-04` whose expected value is literally
> `type-checker=false` (RESEARCH.md §D-01). Copy `createProgramForTarget`, not `context-io.test.ts`.

**Naming precedent — `*.testkit.ts` already exists on this tree.** `scripts/ci-workflow.testkit.ts`
is a non-test `.ts` under `scripts/` consumed by tests only. Its header states the rule the new
helper must restate in its own words (`scripts/ci-workflow.testkit.ts:1-18`):

```ts
// ci-workflow.testkit.ts — the ONE authority over "where is the ubuntu gate block of
// .github/workflows/ci.yml, and what does it run" (Phase 29.1, plan 29.1-24, closing R3-IN-03).
//
// THIS IS THE ONLY IMPLEMENTATION OF THAT QUESTION, AND A SECOND ONE IS A DEFECT RATHER THAN A
// DUPLICATION. ...
//
// IT IS CONSUMED BY TESTS ONLY AND IMPORTED BY NO PRODUCTION SCRIPT, WHICH IS WHY IT IS A TESTKIT
// AND NOT A PROMOTION INTO A SHIPPED MODULE. ... The property is
// asserted by a derived scan in `(r-class-authority)` rather than by this paragraph.
```

**DIVERGENCE the plan must state:** `ci-workflow.testkit.ts` **is** emitted — `scripts/ci-workflow.testkit.js`
is tracked. D-02's "no committed `.js` twin" is therefore **new**, and it requires an added
`tsconfig.json` `exclude` pattern (`**/*.test.ts` does not match `foo.testkit.ts`). Follow the naming
analog; do not inherit its emit behaviour.

**Program-build pattern to copy** (`scripts/runnable-ref/uat-spec-integrity.ts:1308-1400`) — the four
load-bearing moves, verbatim:

```ts
  let options: TsCompilerOptions;
  let configFileNames: readonly string[];
  try {
    const read = ts.readConfigFile(configPath, ts.sys.readFile);
    if (read.error !== undefined) return { ok: false, cause: `${configPath} could not be read as a configuration file` };
    const parsed = ts.parseJsonConfigFileContent(read.config, ts.sys, repoRoot);
    if (parsed.errors.length > 0) return { ok: false, cause: `${configPath} did not parse (${parsed.errors.length} configuration error(s))` };
    options = { ...parsed.options, noEmit: true };          // (1) noEmit EXPLICIT
    configFileNames = parsed.fileNames;
  } catch (cause) { return { ok: false, cause: `${configPath} could not be parsed (${describeCause(cause)})` }; }

  const parseFaults = new Map<string, string>();
  try {
    const host = ts.createCompilerHost(options, true);
    const inner = host.getSourceFile.bind(host);
    host.getSourceFile = (fileName, languageVersion, onError?, shouldCreate?) => {
      try { return inner(fileName, languageVersion, onError, shouldCreate); }
      catch (cause) { parseFaults.set(fileName, describeCause(cause)); return undefined; }   // (2) PER-FILE fault boundary
    };
    const rootNames = [...new Set([...configFileNames, ...specAbsPaths])];                   // (3) UNION, never intersection
    program = ts.createProgram({ rootNames, options, host });
  } catch (cause) { return { ok: false, cause: `the program could not be created (${describeCause(cause)})` }; }

  let checker: TsTypeChecker;
  try { checker = program.getTypeChecker(); }                                                // (4) try/catch on the checker
  catch (cause) { return { ok: false, cause: `the type checker could not be obtained (${describeCause(cause)})` }; }
```

Its own docblock states why each move exists (`:1292-1306`), including the union rule:

> "A config that excludes the spec directory must not leave a spec UNCHECKED at exit 0, so the
> derived set is added rather than intersected. That union is also what keeps the file set the ban
> is decided over EQUAL to the file set the denominator floor counts."

That sentence is directly load-bearing here: `tsconfig.json`'s `exclude` carries `**/*.test.ts`, so
`parsed.fileNames` contains **zero** of the 73 test files this census is about. Union them in.

**Alias-following pattern** (`scripts/runnable-ref/uat-spec-integrity.ts:1852-1861`) — the answer to
F-19/WR-04's `getText()` failure:

```ts
/** Follow an import or export alias to the symbol it names. A non-alias is returned unchanged. */
function followAlias(ts: TsApi, checker: TsTypeChecker, symbol: TsSymbol): TsSymbol {
  if ((symbol.flags & ts.SymbolFlags.Alias) === 0) return symbol;
  try { return checker.getAliasedSymbol(symbol); }
  catch { return symbol; }
}
```

**Return-shape pattern:** `{ ok: true, context } | { ok: false, cause: string }` — a could-not-run is
a named cause, never an empty result (`uat-spec-integrity.ts:1287-1289`).

**PLAN PROHIBITION (copy into the plan verbatim):** the strings `createProgram(` and
`getTypeChecker(` must never appear in `scripts/context-io-writer-set.test.ts`, and the helper must
never be imported there — residual `R-31-21-04` (`scripts/context-io.test.ts:15406-15416`) is a TEXT
match whose expected value is `by-identifier=true type-checker=false`. Assert
`git diff --exit-code -- scripts/context-io-writer-set.test.ts` after every cutover.

---

### NEW loader-oracle helper (D-09) — test-support, event-driven

**Analog (attach point + shape):** `scripts/board-readonly.test.ts:1813-1890`, the existing two-sided
oracle the loader becomes a **third** side of.

**Corpus-derivation pattern** (`scripts/board-readonly.test.ts:1823-1835`) — copy this, including the
stated reason:

```ts
    // THE CORPUS IS DERIVED FROM GIT, not hand-listed and not walked. `git ls-files` is the same
    // definition of "tracked" the review's census used, it cannot rot as files are added, and it
    // excludes the UNTRACKED build mirrors under `.tmp-build/` that a freshness check leaves
    // behind — a walk would make this oracle's corpus depend on whether a build had just run.
    const corpus = execFileSync("git", ["ls-files", "*.js", "*.mjs"], { cwd: ROOT, encoding: "utf8" })
      .split("\n").filter((line) => line !== "");

    expect(
      corpus.length,
      "PREMISE: the derived corpus is EMPTY, so the equality below compared nothing. A vacuous " +
        "oracle is the shape that passes while proving nothing",
    ).toBeGreaterThan(40);
```

**Both-directions failure-message pattern** (`:1863-1889`) — two named lists, each with its own
consequence sentence, so a fabricated edge and a missed edge are distinguishable:

```ts
    expect(fabricated, "moduleSpecifiers reported a specifier NO import statement carries. ...").toEqual([]);
    expect(missed, "moduleSpecifiers MISSED a real specifier, so a mirror built from this closure is short by " +
      "exactly the file the walk could not see and the gate that spawns it dies with ERR_MODULE_NOT_FOUND...").toEqual([]);
```

**What has NO analog:** `module.registerHooks`. Nothing on this tree registers a loader hook. Use
RESEARCH.md's probe-3 shaping verbatim (resolve-side recording, inert corpus loaded for real,
`git status --porcelain` equality before/after, resolve-event floor `> 5` against 13 measured today).
**Do not** use a `load` short-circuit — RESEARCH.md probe 2 measured it recording exactly one edge.

---

### NEW `scripts/check-build-parity.ts` (D-06) — gate module, batch -> exit code

**Analog:** `scripts/check-residual-citations.ts` (the smallest complete gate on the tree, 137 lines).

**Header pattern** (`scripts/check-residual-citations.ts:1-24`): a title line naming the plan and the
finding it closes, then a WHY-THIS-IS-ITS-OWN-CHECK block naming the homes it was evicted from, then
the mechanism sentence. `scripts/check-nul-bytes.ts:1-36` adds the two lines every gate carries:

```ts
//   node scripts/check-nul-bytes.js
// Exit 0 = every tracked file is free of forbidden control bytes; exit 1 = at least one FAIL.
...
// Strictly READ-ONLY. Node stdlib ONLY — node:fs + node:path + one `git ls-files` invocation.
// Zero npm dependencies.
//
// Findings are written to stdout in CLEAR PROFESSIONAL VOICE (CLAUDE.md hard rule — this is a
// quality/trace surface, never caveman voice).
```

**Root-resolution pattern — the truthiness ternary, NOT `??`** (`scripts/check-residual-citations.ts:37-44`):

```ts
// THE TRUTHINESS TERNARY, NOT `??` — and an existing guard caught this file using `??` (plan 30-11
// round 3). `??` treats an EMPTY `CHECK_ROOT` as a supplied value, so `join("", …)` resolves against
// the process cwd.
const ROOT = process.env.CHECK_ROOT ? process.env.CHECK_ROOT : join(import.meta.dirname, "..");
```

**Entry-guard + exit pattern** (`scripts/check-residual-citations.ts:120-137`):

```ts
const isEntry = isEntrypoint(import.meta.url);

if (isEntry) {
  const r = residualCitationRefusals();
  if (r.refusals.length > 0) {
    for (const m of r.refusals) console.error(`  FAIL  ${m}`);
    console.error(`\n== Result ==\n${r.refusals.length} CHECK(S) FAILED`);
    process.exit(1);
  }
  console.log(`  PASS  residual citations: ${r.cited} path claim(s) across ${r.scanned} published row(s), ...`);
  console.log("\n== Result ==\nALL CHECKS PASSED");
  process.exit(0);
}
```

`scripts/check-nul-bytes.ts` states the reason the guard matters (tail of file): a hand-built
`file://${argv[1]}` URL does not match on Windows, which would make a direct run execute ZERO checks
and exit 0 — a fabricated green. Use `isEntrypoint(import.meta.url)` from `./is-entry.js`.

**PASS-line pattern — `reportMeasured`, not a bare `pass()`** (`scripts/vacuity.ts:46-97`). Five
gates already import it (`check-foundation-guards.ts`, `check-diff-disposition.ts`,
`check-imperative-lexicon.ts`, `check-banned-claims.ts`, and `board-readonly.test.ts`):

```ts
export interface Measured<T> {
  readonly label: string;
  // Elements ACTUALLY examined. NEVER a constant: it is incremented by the loop that does the work,
  // so a guard that skips its loop reports 0 and fails its own floor below.
  readonly visited: number;
  // The DERIVED denominator (a role count, a bullet count, a scan-set size) — never hand-typed ...
  readonly expected: number;
  readonly findings: readonly T[];
}
```

Its four ordered branches are the contract: `visited === 0` -> "this check was NOT performed";
`visited !== expected` -> "the scan set is short"; findings; then a PASS line that **carries the
measurement** (`0 findings over N/M elements`). A per-gate `if (n === 0)` is the named forbidden
alternative (`scripts/vacuity.ts:26-27`).

**Own-scope PREMISE pattern** (`scripts/check-residual-citations.ts:110-116`):

```ts
  // THE SCAN'S OWN PREMISE. A scan that matched nothing would pass every row vacuously, forever.
  if (scanned > 0 && cited === 0) {
    refusals.push(`the residual path scan examined ${scanned} published row(s) and found NO cited path at all — ` +
      `that is a scan that has stopped asking, not a register that has stopped citing`);
  }
```

**CI reachability (mandatory companion edit).** `.github/workflows/ci.yml` invokes gate **modules**
directly, not `npm run check:*` — the only `npm run check:` line in the whole workflow is
`npm run check:build-parity` at `:102`. A new `scripts/check-build-parity.js` therefore needs a new
`node scripts/check-build-parity.js` step or its `gate-module` reachability proof fails. Copy the
commented-step style at `.github/workflows/ci.yml:466-478`:

```yaml
          # ... Both
          # reproductions are PERMANENT CASES in the test file, not a historical commit ...
          # This step needs the job's `fetch-depth: 0` checkout. See the comment on that setting.
          node scripts/check-diff-disposition.js
```

---

### `scripts/js-import-closure.ts` (D-08) — production utility, transform

**Analog for the parse:** `scripts/board-readonly.test.ts:1836-1859` — the repository already spells
the correct call, including `ts.ScriptKind.JS` (the scanner's corpus is compiled `.js`, not `.ts`):

```ts
    const parsed = (source: string, label: string): string[] => {
      const sf = ts.createSourceFile(label, source, ts.ScriptTarget.Latest, true, ts.ScriptKind.JS);
      const found: string[] = [];
      const visit = (node: ts.Node): void => {
        if ((ts.isImportDeclaration(node) || ts.isExportDeclaration(node)) &&
            node.moduleSpecifier !== undefined && ts.isStringLiteral(node.moduleSpecifier)) {
          found.push(node.moduleSpecifier.text);
        }
        if (ts.isCallExpression(node) && node.expression.kind === ts.SyntaxKind.ImportKeyword &&
            node.arguments[0] !== undefined && ts.isStringLiteral(node.arguments[0])) {
          found.push(node.arguments[0].text);
        }
        ts.forEachChild(node, visit);
      };
      visit(sf);
      return found;
    };
```

**Two corrections the rewrite must make against this analog** (both from RESEARCH.md §D-08): it omits
`require()` and `ImportEqualsDeclaration`, and its two `ts.isStringLiteral` guards make it blind to
exactly what the regex scanner is blind to — F-17's finding. Use `ts.isStringLiteralLike` where a
value is wanted (precedent: `scripts/board-readonly.test.ts:492`) and **refuse by node kind** on
anything else (D-11) rather than adding an arm.

**Deletion-note pattern** (`scripts/js-import-closure.ts:464-470`) — the form the retired regex arm,
`SPECIFIER_PATTERNS` and the recovery table each need:

```ts
// `relativeSpecifiers` WAS DELETED HERE (review WR-07). 32-31 kept it "so every existing caller is
// unaffected" — a sentence with no referent: a repository-wide search across `.ts`, `.js`, `.mjs`
// and `.md` found the symbol only at its own two definition sites, and it had no test of its own.
// ... Dead code carrying a false claim about
// why it is kept is worse than dead code, because the next reader takes the sentence as evidence
// that the export is load-bearing and preserves it again.
```

**Scope-statement pattern** (`scripts/js-import-closure.ts:1-30`): the header states WHY THIS EXISTS,
the failure class it closes (`[[grugops-set-literal-drift]]`), that it REFUSES rather than returns
short, and — in a paragraph labelled "SCOPE, STATED SO IT IS NOT MISTAKEN FOR A BUNDLER" — what it
does not do. The rewritten `moduleSpecifiers` docblock must restate its node set in that form.

**The dependency route (open question — plan a blocking checkpoint).** A top-level
`import ts from "typescript"` in this production, compiled, committed module contradicts CLAUDE.md C2
and an existing written in-repo ruling at `scripts/runnable-ref/uat-spec-integrity.ts:731-734`:

> "The parser is loaded at RUN TIME from the target, so its types cannot be imported here: a
> top-level `import ... from "typescript"` would break the builtins-only rule this file exists to
> honour, and would tie the emitted .js to a compiler the host may not have."

The in-repo pattern for route 1 is the structural `TsApi` interface declared at
`scripts/runnable-ref/uat-spec-integrity.ts:868-1000` covering exactly the members it calls, with
`ts` taken as an injected parameter (`createProgramForTarget(repoRoot, specAbsPaths, ts)`).

**Signature stability:** `jsImportClosure(root, entryRel)` must not move — six production consumers
(`check-platform-shapes.ts:76/:696`, `context-freshness.ts:63/:126`, `guarantees-freshness.ts:68/:134`,
`generate-hook-manifest.ts:14/:51`, `now-running-freshness.ts:46/:114`, `trace-freshness.ts:58/:114`)
call it, and `check-platform-shapes` runs on **every** CI leg including `windows-latest`.

---

### `scripts/validate.test.ts` (D-01, D-03, D-04, D-15/194) — test suite, batch census

**Analog:** `scripts/context-io-writer-set.test.ts` — the mutation-proven derived census.

**Doctrine header to restate** (`scripts/context-io-writer-set.test.ts:13-28`):

```
// THE SET IS DERIVED, NOT TYPED OUT. This repository's second systemic failure class is a
// hand-maintained set literal that rots while the suite stays green (7 granted names, 0 adapter
// files). So the writer set is computed from `scripts/context-io.ts` by the TypeScript AST ...
// and BOTH its members and its cardinality are asserted.
//
// WHAT IT DOES NOT CLOSE, NAMED RATHER THAN IMPLIED (PART FIVE):
//   • The derivation is SYNTACTIC. It resolves `foo(...)` by identifier; an alias
//     (`const f = writeNoteFile; f(...)`) or a computed member call is not seen. Widening the
//     matcher once per counter-example is the failure this repository has paid for, so the boundary
//     is written down.
```

That second block is precisely the boundary D-01 removes — the new census resolves by declaration
symbol, so the replacement paragraph states the NEW boundary rather than deleting the old one.

**PREMISE-triple pattern** (`:229-248`) — three separate assertions, each naming what its own
emptiness would hide:

```ts
    expect(analysis.calls.size, "PREMISE: the TypeScript parse of scripts/context-io.ts yielded ZERO top-level function " +
      "declarations, so every derivation below measured nothing at all").toBeGreaterThan(0);
    expect(analysis.exported.size, "PREMISE: the parse found ZERO exported functions, so the writer derivation had no candidates " +
      "and its emptiness would say nothing about the module").toBeGreaterThan(0);
    expect(analysis.calls.has("writeNoteFile"), "PREMISE: the write chokepoint `writeNoteFile` was not found as a top-level declaration, so " +
      "the closure test below could never be true for any candidate").toBe(true);
```

**Members-then-count pattern** (`:250-261`) — two cases, never one, and the count message says it is
a decision rather than a constant:

```ts
  it("the derived writer set has the expected MEMBERS", () => {
    expect(deriveNoteWriters(CONTEXT_IO_TS)).toEqual([...EXPECTED_NOTE_WRITERS]);
  });

  it("the derived writer set has the expected COUNT", () => {
    expect(deriveNoteWriters(CONTEXT_IO_TS).length,
      "a note writer landed or left scripts/context-io.ts. A new one is ... " +
        "it is a decision, never a bumped constant").toBe(EXPECTED_NOTE_WRITER_COUNT);
  });
```

This is the exact form D-04's re-pinned `NOT_A_SECOND_AUTHORITY` registry needs (today:
`scripts/validate.test.ts:1847` declaration, `:1893` `NOT_A_SECOND_AUTHORITY_COUNT = 9`, two-sided pin
at `:2248-2252`; the count moves 9 -> 8 with row 194's deletion, in the SAME commit as the reword).

**Mutation-proof pattern — PART TWO** (`scripts/context-io-writer-set.test.ts:263-299`), the single
most copyable block in this phase:

```ts
// PART TWO — the derivation DISCRIMINATES.
//
// A structural assertion nobody has watched fail is not yet a control. The mirror is BUILT FROM the
// live source rather than from a fixture, so it cannot drift away from the thing it copies.

function mirrorWithExtraWriter(): string {
  const dir = freshTmp("ctx-io-writer-mirror-");
  const path = join(dir, "context-io.ts");
  writeFileSync(path, readFileSync(CONTEXT_IO_TS, "utf8") +
    `\nexport function ${SEEDED_WRITER}(notesDir: string, id: string, text: string): void {\n` +
    `  writeNoteFile(notesDir, id, text);\n}\n`);
  return path;
}

  it("a SECOND exported writer moves the COUNT by exactly one", () => {
    const derived = deriveNoteWriters(mirrorWithExtraWriter()).length;
    expect(derived).not.toBe(EXPECTED_NOTE_WRITER_COUNT);
    // …by exactly one, so the difference is caused by the seeded function rather than by a
    // derivation that broke and started reporting some other number.
    expect(derived).toBe(EXPECTED_NOTE_WRITER_COUNT + 1);
  });
```

**The walk D-03 floors, in place** (`scripts/validate.test.ts:1815-1823`) — keep its docblock, add the
tracked-set floor beside it:

```ts
  /**
   * The scanned set: every `.ts` file under the directory AT TEST TIME, RECURSIVELY. Never a
   * literal array, and never depth-one — `scripts/` carries subdirectories, and a second reader
   * placed in one of them would sit outside a depth-one glob without anything going red.
   */
  const SCANNED = readdirSync(SCRIPTS_DIR, { withFileTypes: true, recursive: true })
```

**Pathspec correction (HIGHEST-RISK line in the phase):** use `git ls-files -- 'scripts/*.ts'` or
`':(glob)scripts/**/*.ts'` (both return 151). CONTEXT.md's literal `'scripts/**/*.ts'` returns **26**.
Floor the result `.toBeGreaterThan(100)`.

---

### `scripts/check-foundation-guards.test.ts` (D-05, D-06, F-19) — test suite, manifest read

**Analog: the file's own `(r-class-authority)` case at `:11831-11900`** — the template for a declared
shape set plus its member derivation:

```ts
    // ── THE MEMBER SET, DERIVED FROM THE IMPORT ────────────────────────────────────────────────
    const members = testFiles.filter((f) => importsAuthority(textOf(f)));

    // VACUITY FLOOR ON THE DENOMINATOR THE MEMBER SET IS CUT FROM. A derived set that silently came
    // back empty would make every per-member claim below true over nothing ... The ELEMENT count is
    // floored, not only the member count, because a floor over an empty denominator has never
    // caught a silently SHORT one.
    expect(testFiles.length, `the .test.ts enumeration of ${scriptsDir} came back empty — the scan, not the tree, is broken`).toBeGreaterThan(0);
    expect(members.length, `no test file imports ${AUTHORITY_SPECIFIER}; ... An empty member set means the authority is consumed by nobody and every assertion in this case is vacuous.`).toBeGreaterThan(0);

    // TWO-SIDED. A third reader joining the class is a red here even when it imports correctly,
    // because the pinned number is what makes a new member visible at all.
    expect(members.length, `... A new reader is not a failure — import the authority, then update the pin in the same commit.`).toBe(UBUNTU_BLOCK_READER_COUNT);
```

**What D-05 replaces** (`scripts/check-foundation-guards.test.ts:12504-12509`) — the separator-alphabet
counter, with its own banner at `:12475-12494` already naming row 207's residual
("a module reached through any OTHER nested runner would be invisible to both sides and is
`UNKNOWN - verify`"):

```ts
  const NODE_STEP_RE = /(^|&&|\|\||;)\s*node\b(?!\s+(?:-e|--eval)\b)/g;
  const VITEST_STEP_RE = /vitest run\b/g;

  const runnerSteps = (cmd: string): { readonly node: number; readonly vitest: number } => ({
    node: (cmd.match(NODE_STEP_RE) ?? []).length,
    vitest: (cmd.match(VITEST_STEP_RE) ?? []).length,
  });
```

**Refusal-message pattern the declared shape adopts** (from the same case, `:12546-12553`) — a named
entry plus the forbidden alternative:

```ts
    expect(disagreements,
      "a check command runs a module that produced NO classification row, so nothing proves that " +
        `module is reachable and no pinned number moved:\n  ${disagreements.join("\n  ")}\n` +
        "The recognisers' path alphabet is narrower than the ways a module can legitimately be " +
        "spelled. Widen the recogniser (and move the pinned target count with it) — do not widen " +
        "this denominator to agree with it",
    ).toEqual([]);
```

**F-19's rewrite target, verbatim** (`scripts/check-foundation-guards.test.ts:12689-12770`) — the
identifier-text match D-01's checker replaces:

```ts
      const isRead = (ts.isElementAccessExpression(node) || ts.isPropertyAccessExpression(node)) &&
        node.expression.getText(src) === REGISTER;
      ...
      if (ts.isIdentifier(node) && node.getText(src) === ACCESSOR) { accessorRefs.push(...); }
```

Its three pinned numbers move with the rewrite: `reads` pinned `.toHaveLength(1)` at `:12756-12761`,
`accessorRefs.length - 1` pinned `.toBe(3)` at `:12766-12770`.

**The declared `check:*` shapes, derived from the live manifest** (`package.json:15-25`):

| shape | exact form | live members |
|---|---|---|
| `S1` | `tsc --outDir .tmp-build && node scripts/<name>.js` | the 9 gate entries (`:16-24`) |
| `S2` | `npx vitest run <path>.test.ts` | `check:dashboard-readonly` (`:25`) |
| `S3` | the `check:build-parity` compound (`:15`) | deleted by D-06 |

**SEQUENCING HAZARD (RESEARCH.md Pitfall 3):** D-06 empties `TOOLCHAIN_CHECK_SCRIPTS` — the register
F-19's one-authority rule is *about*. Land the F-19 rewrite **before** D-06, or plan both in one wave
with the class counts `{gate-module: 9, suite-test-file: 1, toolchain: 1}` moved deliberately
(`:12126-12141`).

---

### `scripts/board-model.ts` + `board-read.ts` + `board-dashboard.ts` (D-07) — production, transform

**Analog: `visible()` in the same file** (`scripts/board-model.ts:1063-1068`):

```ts
export function visible(text: string): string {
  return text.replace(
    RENDER_STRIPPED,
    (c) => `<U+${(c.codePointAt(0) as number).toString(16).toUpperCase().padStart(4, "0")}>`,
  );
}
```

Its existing two call sites are both inside the ticket grammar (`:1096`, `:1130`) — the F-14 surfaces
one layer up (`presenceActual` `:1380`/`:1385`, `ticket-unplaced` `:1606`) are unwrapped.

**Downstream scrubbing already exists — do not double-escape.** `sanitizeCell` (`board-dashboard.ts:261`),
`warn()` mapping it over every stderr line (`:286`), `scrub()` applying it to every string value and
object key at every depth (`:336`, `:341`), and the `--json` emitter (`:392`). F-14 is that the byte is
**deleted** rather than **spelled**, so the sentence misquotes its own evidence.

**Two-authority agreement test to PRESERVE, not retire** (`scripts/board-dashboard.test.ts:2729-2756`):

```ts
  it("visible() escapes exactly the code points sanitizeCell deletes — derived, both directions", () => {
    for (let cp = 0; cp <= 0x00a5; cp += 1) { ... }
    expect(deleted.length, "PREMISE: sanitizeCell deleted NOTHING across the whole C0/DEL/C1 range, so the equality " +
      "below compared two empty sets and proved nothing").toBeGreaterThan(60);
    expect(escaped, "visible() and sanitizeCell disagree about which bytes a reader never sees. ...").toEqual(deleted);
    expect(sanitizeCell(visible("\u0009\u0000\u009F")), "the escape text is itself renderable").toBe("<U+0009><U+0000><U+009F>");
  });
```

D-07's "the site-list derivation is retired" names **no artefact on this tree** (RESEARCH.md
contradiction 5). This case is the nearest derivation-shaped thing and is a two-authority pin — state
in the plan that it survives.

**Scope pattern for the builder's site census:** derive the **content-derived** subset explicitly (an
AST pass over `TemplateExpression` substitutions resolving to document-derived data) and name the
path-derived and internal classes as an out-of-scope decision with the reason. A blanket rule over
the 105 interpolation-bearing lines (`board-read.ts` 33, `board-model.ts` 16, `board-dashboard.ts` 56)
is a different, much larger cutover.

---

### `scripts/board-model.test.ts` (D-18) — test suite, contract-vs-code equality

**Analog:** the same file's presence-table equality at `:3250-3400`.

**Heading-located, next-heading-bounded reader** (`:3266-3311`) — copy the bound and its reason:

```ts
/**
 * Read the presence table out of the contract, LOCATED BY ITS HEADING and BOUNDED BY THE NEXT ONE.
 *
 * THE BOUND IS THE POINT. A section-anchored reader that searches to end-of-file adopts an
 * unrelated later block — the bypass shape this repository recorded in Phase 29 — and this document
 * carries several other tables, one of them (the conflict-kind table) only a few lines above.
 */
function readPresenceTable(text: string): readonly ContractRow[] {
  const start = lines.findIndex((l) => l.trim() === PRESENCE_TABLE_HEADING);
  if (start < 0) { throw new Error(`agent-factory/contracts/board.md carries no "${PRESENCE_TABLE_HEADING}" heading. The ` +
      `presence table is located by that heading, never by a line number; if the section was ` +
      `renamed, rename it here in the same commit.`); }
  let end = lines.length;
  for (let i = start + 1; i < lines.length; i += 1) { if (/^#{1,6} /.test(lines[i] ?? "")) { end = i; break; } }
```

**Floor-derived-two-ways pattern** (`:3357-3374`):

```ts
    expect(rows.length, "PREMISE: the presence table parsed EMPTY, so every comparison below is vacuous").toBeGreaterThan(0);
    // THE FLOOR IS A NUMBER: five reader-reachable answers ... An empty table and a silently SHORT one both red here.
    expect(rows.length, "the presence table lost or gained a row").toBe(5);
    // AND THE SAME NUMBER, DERIVED: the module's branch count minus the branches Task 2 proved no
    // tree can reach. A hand-typed 5 alone would not notice the code growing a reachable branch.
    expect(rows.length, "the contract states a different number of answers than the code can produce ...").toBe(readerProducibleTemplates().length);
```

**Both-directions equality with one message naming both failure directions** (`:3385-3396`):

```ts
    expect([...fromContract].sort(),
      "the normative contract and the code disagree about the set of sentences the projector can " +
        "state. A sentence on the left with no partner on the right is a row describing a " +
        "consequence the code does not produce — exactly what review WR-01 found. One on the right " +
        "with no partner on the left is a sentence the projector states that no reader " +
        "adjudicating a disagreement could look up",
    ).toEqual([...fromCode].sort());
```

**DIVERGENCE the plan must state:** the WR-01 watch rule is **prose at `agent-factory/contracts/board.md:406-412`**,
not a two-column table. `readPresenceTable`/`contractTemplate` (which require exactly 2 cells and one
backticked span) do not apply. "Extending the existing test" means a **second, differently-shaped**
instrument in the same file: locate the paragraph by its bold lead sentence, derive the code's
code-to-record mapping from `arm()`'s `if (decision.code === OUTSIDE_ROOT)` branch
(`scripts/board-dashboard.ts:1169-1174`, `arm()` opens at `:1118`), assert agreement, and give it its
own vacuity floor (paragraph parsed non-empty; derived code set non-empty).

**Contract-edit ordering rule** (`agent-factory/contracts/board.md:576-578`):

> "A newly admitted shape is recorded here first and implemented afterwards. A counter-example this
> grammar refuses is a decision to take, never a pattern to widen."

---

### NEW `docs/audit/29-style-dispositions/32.1-NN.md` (D-12) — audit record

**Analog:** `docs/audit/29-style-dispositions/29-44.md`.

**Document shape** (29-44.md, in order): a title `# Plan <NN> style dispositions`; a paragraph naming
what forced the change; the judgement-versus-coverage disclaimer; a `## What forced the change,
stated before what was done` section carrying the measurement; a `## The fix that was available and
was refused` section enumerating the weakenings and stating that none was taken; then `## Dispositions`.

**The row form, verbatim from `29-44.md:93-97`** — seven columns, in this order:

```markdown
## Dispositions

| file | line | before | after | rule | disposition | companion |
|---|---|---|---|---|---|---|
| agent-factory/roles/incident-responder.md | 29 | `apply or recommend the immediate mitigation that limits harm, before you understand the cause` | `apply or recommend the immediate mitigation that limits harm, before the cause has been diagnosed` | D-29 / LANG-04 | Forced by a prohibition getting wider, not by a style pass: ... | <8+ normalized words> |
| agent-factory/roles/incident-responder.md | 29 | `Diagnosis can wait; the bleeding cannot.` | `Diagnosis can wait; the bleeding cannot.` | — | Unchanged text on a changed line. The numbered step is one source line, so `git diff --unified=0` reports every clause on it when any clause moves. Byte-identical on both sides. | — |
```

`companion` is `—` for a non-frozen row and must carry **8+ normalized words** for a frozen one
(`COMPANION_MIN_WORDS = 2 * CLAUSE_MIN_WORDS`, `CLAUSE_MIN_WORDS = 4` at `scripts/voice-model.ts:526`;
`isCompanionFilled` at `scripts/check-diff-disposition.ts:1267-1278`). Every placeholder spelling
normalizes to at most one word and is refused by construction.

**Membership is walked, never listed** (`scripts/check-diff-disposition.ts:265, :1343-1346`): a file
named `32.1-05.md` is admitted; the dot in the stem is irrelevant. `00-base.md` and `README.md` are
the two non-rows.

**Forbidden remedies, named by the gate's own base document** (`docs/audit/29-style-dispositions/00-base.md:22-31`):

> "A base commit that cannot be resolved is a **fail-closed** condition... **Moving this SHA is not a
> remedy for a red gate.** A red means a changed clause has no disposition row or no companion edit.
> Re-basing the comparison forward would delete the finding by deleting the evidence, which is the
> same act as narrowing a scan set to reach green. Write the row."

Scale: the gate is measured RED today at **78 findings over 39 elements**, 3 of them FROZEN (all in
`agent-factory/workflows/18-context-compaction.md`'s `## Stop conditions` region). One file per plan —
that rule exists so parallel plans do not contend.

---

### `package.json`, `tsconfig.json`, `.github/workflows/ci.yml` — config

**`package.json` S1 shape to match** (`:16-24`):

```json
    "check:public-docs": "tsc --outDir .tmp-build && node scripts/check-public-docs-vocabulary.js",
    "check:nul-bytes": "tsc --outDir .tmp-build && node scripts/check-nul-bytes.js",
```

**`tsconfig.json` exclude pattern (D-02).** The array is `["node_modules", ".tmp-build", "**/*.test.ts",
"scripts/runnable-ref/fixtures/**"]`, and the comment above it is the pattern for the new entry's
justification — state WHY the file is not tooling output:

```jsonc
  // scripts/runnable-ref/fixtures/ holds *.uat.spec.ts CORPUS files, not tooling sources. They are
  // Playwright specs written to be PARSED by uat-spec-integrity.js as data — they import
  // @playwright/test, which grugops deliberately does not depend on (zero shipped runtime deps), so
  // tsc reports TS2307 on every one of them and would emit a .js twin the freshness gate would then
  // demand be committed. They are input to a checker, never compiled output.
```

`tsconfig.tests.json` declares its **own** exclude array and does not inherit this one, so the helper
is still type-checked by `npm run typecheck` with no extra wiring.

---

### `scripts/check-diff-disposition.ts` (D-15 / row 194) — one literal, not "prose"

The census sees **string literals**, not comments (`staticText` at `scripts/validate.test.ts:1775-1790`
resolves string literals, no-substitution templates, regex literals, `+` concatenations and
`[...].join(sep)`). The file is tripped by exactly two literals:

- `:1058` — `const dirty = git(["status", "--porcelain"]).trim().length > 0;` — a **git subcommand**.
  **Do not touch it.** A diff touching `:1058` is the RESEARCH.md Pitfall-4 warning sign.
- `:1630` — the `column` inside an operator message. Reword this one (e.g. `field`).

`namesBothKeys` is `KEY_SPELLINGS.every(...)` (`scripts/validate.test.ts:1806-1809`), so dropping one
key is sufficient. The reword, the registry-entry deletion with its deletion note, and the count move
9 -> 8 all land in the **same commit** — the liveness assertion at `:2236-2246` reds if a named file
stops being detected while its entry stands.

---

## Shared Patterns

### 1. PREMISE before every equality (apply to: every new or modified test case)
**Source:** `scripts/context-io-writer-set.test.ts:229-248`; `scripts/board-readonly.test.ts:1830-1835`;
`scripts/check-foundation-guards.test.ts:11874-11888`.
The premise is a **failing assertion**, not a comment, and its message names what its own emptiness
would hide. Floor the **element/denominator** count, not only the member count — "a floor over an
empty denominator has never caught a silently SHORT one".

### 2. Derive the set, pin the count two-sided (apply to: D-03, D-04, D-05, D-16)
**Source:** `scripts/context-io-writer-set.test.ts:13-18, :250-261`; `scripts/validate.test.ts:1893, :2248-2252`.
Members and cardinality are separate cases. The count message says the number is a **decision**, moved
in the same commit as the thing that moved it — "never a bumped constant".

### 3. Mutation proof from a mirror BUILT FROM the live source (apply to: every cutover, D-19)
**Source:** `scripts/context-io-writer-set.test.ts:263-299`.
The plant is generated by appending to `readFileSync(<live source>)`, never authored as a fixture, "so
it cannot drift away from the thing it copies". Assert the derived set **contains** the seeded item,
is **not equal** to the expected set, and that the count moved **by exactly one**.

### 4. The corpus comes from `git ls-files`, never a walk (apply to: D-03, D-08, D-09)
**Source:** `scripts/board-readonly.test.ts:1819-1828`.
Reason, verbatim: it "excludes the UNTRACKED build mirrors under `.tmp-build/` that a freshness check
leaves behind — a walk would make this oracle's corpus depend on whether a build had just run."
**Correct pathspec:** `git ls-files -- 'scripts/*.ts'` (151) — **not** `'scripts/**/*.ts'` (26).

### 5. Refusal messages name the entry, the consequence, and the forbidden alternative
**Source:** `scripts/board-readonly.test.ts:1877-1889`; `scripts/check-foundation-guards.test.ts:12546-12553`;
`scripts/check-residual-citations.ts:97-107`.
Three parts: what was found (with the item named), what it costs downstream, and the remedy — with the
cheap weakening refused by name ("do NOT widen the patterns and do NOT exempt the file").

### 6. Deletion note beside the deletion (apply to: D-04 registry entries, D-08 regex arm)
**Source:** `scripts/js-import-closure.ts:464-470`.
Name the review finding, quote the false claim that kept the code, and state the measurement that
disproved it.

### 7. Gate-module entry shape (apply to: `scripts/check-build-parity.ts`)
**Source:** `scripts/check-residual-citations.ts:120-137`; `scripts/check-nul-bytes.ts` (tail);
`scripts/vacuity.ts:66-97`.
`isEntrypoint(import.meta.url)` guard; findings to stderr as `  FAIL  <msg>`; PASS line carrying the
measurement via `reportMeasured`; `== Result ==` block; exit 0/1. Builtins only, zero npm deps.

### 8. Test-only module, asserted by a derived scan (apply to: both D-01/D-09 instruments)
**Source:** `scripts/ci-workflow.testkit.ts:1-18`; `scripts/check-foundation-guards.test.ts:11831-11900`.
Name it `*.testkit.ts`; state in the header that no production script imports it; assert that property
with a derived scan and a two-sided pinned reader count, rather than with the paragraph.

### 9. Header docblock: WHY THIS EXISTS / the defect that caused it / WHAT IT DOES NOT CLOSE
**Source:** `scripts/js-import-closure.ts:1-30`; `scripts/check-nul-bytes.ts:1-45`;
`scripts/vacuity.ts:1-28`; `scripts/context-io-writer-set.test.ts:1-28`.
Every module on this tree opens with the defect it closes and the forbidden alternative named so it is
not rediscovered as a good idea. New files must too.

### 10. Evidence transcripts, one pair per plan
**Source:** `.planning/phases/32-board-projector-cli-dashboard/32-09-RED-baseline.txt` /
`32-09-GREEN-proof.txt`; `32-22-LIVE-TRANSCRIPT.txt`; `32-41-GATES.txt`.
This phase's shape: `32.1-NN-RED-baseline.txt` / `32.1-NN-GREEN-proof.txt`, plus
`32.1-NN-LIVE-TRANSCRIPT.txt` for D-13.

### 11. Every `.ts` under `scripts/` moves with its `.js` in the same commit
**Source:** `package.json:9` (`"build": "tsc"`), `:15` (`check:build-parity`).
D-06 adds one twin; D-08 rewrites one. Run `npm run build` and commit the `.js` in the same commit,
then `npm run check:build-parity`.

---

## No Analog Found

Files and mechanisms with no close match — the planner should use RESEARCH.md's measured guidance
instead, and record each as a **new** convention rather than a followed one.

| File / mechanism | Role | Data Flow | Reason |
|------|------|-----------|--------|
| `32-40-ADVERSARIAL-REVIEW-ERRATUM.md` (D-15 / row 209) | evidence document | record | `git ls-files \| grep -i erratum` returns nothing. The never-edit-in-place half IS established; the convention actually in use is a note in the **owning** document (`deferred-items.md`'s row-209 block). Creating an erratum file is new — record it as such. |
| The `module.registerHooks` loader oracle (D-09) | test instrument | event-driven | No `node:module` hook exists anywhere on the tree. Only the ATTACH POINT has an analog (`board-readonly.test.ts:1813`). Use RESEARCH.md probe 3 verbatim, including the resolve-event floor and the `git status --porcelain` before/after equality. |
| `.planning/WINDOWS.md` `waived` rows (D-14) | ledger | tool-owned | Not a file-edit pattern at all. `gsd-tools windows waive <id> "<reason>"` re-derives all four counters and re-renders both representations under a lock; hand-editing throws `WINDOWS_LEDGER_MALFORMED` (`broken-windows.cjs:466, :277-287, :532-540`). `markWaived` is one-shot and overwrites `reason`, so the sentence must be final when written. Reconcile by row **identifier**, never by column position (escaped `\|` cells shift an `awk -F'\|'` tally to 194 against the JSON's 197). |

---

## Plan Prohibitions Carried From The Analogs

These are properties of the analog files that a cutover can break silently. Each belongs in a plan as
a named check.

1. **`R-31-21-04` is a TEXT match.** Never import the checker helper into, and never let the strings
   `createProgram(` or `getTypeChecker(` appear in, `scripts/context-io-writer-set.test.ts`
   (`scripts/context-io.test.ts:15325, :15406-15416`). Assert
   `git diff --exit-code -- scripts/context-io-writer-set.test.ts` after every cutover.
2. **Do not touch `scripts/check-diff-disposition.ts:1058`.** It is `git(["status", "--porcelain"])`.
3. **Do not retire `scripts/board-dashboard.test.ts:2730`.** It is the `visible()`/`sanitizeCell`
   two-authority pin, not a site list.
4. **Scope D-08's "nothing imports the scanner" rule to NON-TEST files.** `hooks/guard.test.ts:42`
   imports `closureTargets` (used at `:741, :1447, :1465, :1478, :1754, :2036`); the rule is red on
   landing otherwise.
5. **`stripNonCode` is a separately exported view over `scanSource`** (`js-import-closure.ts:404-410`)
   with its own length-preservation tests (`board-readonly.test.ts:1665-1712`). Deleting `scanSource`
   deletes it. Re-measure its callers before deciding.
6. **`WALKER_IMPORTER_COUNT = 12`** (`board-readonly.test.ts:2091`) is derived by a **depth-one**
   `readdirSync` (`:2229-2251`) that cannot see `scripts/runnable-ref/*` — a latent instance of the
   very class this phase closes. Fold it into D-03's floor.
7. **The seven hand-edit-forbidden `.claude/skills/*/SKILL.md` twins** are byte-gated generated files
   (`npm run freshness:skill-twins`). No plan may touch them.
8. **`npm test` runs the live claude-CLI e2e lane.** The regression lane is
   `npx vitest run --exclude '**/scripts/e2e/**'`. D-13's `npm test` runs once, behind the named
   human go.
9. **BLOCKING PRE-CONDITION:** the regression lane is RED on this checkout for a working-tree reason —
   `.planning/milestone.lock` is tracked in the index and absent on disk, so `check-nul-bytes` fails
   closed (3 cases in one file). Resolve it (restore or commit the deletion — a human decision) and
   re-take the baseline before any plan's verify step runs.

---

## Metadata

**Analog search scope:** `scripts/` (151 tracked `.ts`, 59 non-test non-fixture), `scripts/runnable-ref/`,
`hooks/`, `install/`, `docs/audit/29-style-dispositions/`, `agent-factory/contracts/`,
`.github/workflows/`, `package.json`, `tsconfig.json`, `.planning/phases/32-*/`.
**Files opened this session:** 17 (`uat-spec-integrity.ts`, `ci-workflow.testkit.ts`,
`context-io-writer-set.test.ts`, `board-readonly.test.ts`, `board-model.test.ts`,
`board-dashboard.test.ts`, `check-foundation-guards.test.ts`, `js-import-closure.ts`,
`check-nul-bytes.ts`, `check-residual-citations.ts`, `vacuity.ts`, `validate.test.ts` regions via
RESEARCH.md citations, `29-44.md`, `00-base.md`, `package.json`, `tsconfig.json`, `ci.yml`).
**Tracked-source gate:** every analog path verified with `git ls-files` on 2026-09-17; no mirror paths
emitted.
**Nothing was modified.** This document is the only file written.
**Pattern extraction date:** 2026-09-17

32.1-01-RED-baseline.txt — the BEFORE side of plan 32.1-01, per D-19.
=====================================================================

WHAT THIS FILE IS. D-19 sequences this phase instruments-first and requires every new rule to be
watched REFUSING something before it is trusted. A rule nobody has seen fail is not yet a control;
it is a sentence that happens to be green. So each case this plan adds is recorded here against a
PLANT — a deliberate, generated, reverted mutation — with the exact failure message it produced and
the exit code of the run.

WHAT IT IS NOT. It is not a list of things that were broken. Every plant below was reverted in the
same working session, and `git status --porcelain -- scripts/` was confirmed empty of plant residue
after each one. The GREEN side is `32.1-01-GREEN-proof.txt`.

Checkout: main @ 8de15983 (plan base). Node v24.12.0. vitest 4.1.8. Measured 2026-09-17.

Every transcript below is reproduced verbatim from the run's own stdout/stderr, with no ANSI escape
sequences and no control bytes (Pitfall 7).


=====================================================================
TASK 1 — the symbol-resolving instrument, its file set, and its floor
=====================================================================

PLANT 1.1 — an UNTRACKED `.ts` under `scripts/` must be NAMED, never silently skipped
-------------------------------------------------------------------------------------

The case: `scripts/validate.test.ts` > "CASE 1 — the tracked file set is FLOORED, and a `.ts` the
walk finds untracked is NAMED".

Why it needs a plant at all: measured on this checkout, the tracked set (151) and the `readdirSync`
walk (151) are EQUAL and there are zero untracked `.ts` files under `scripts/`. The converse half of
the case is therefore green from the moment it lands, and a green that has never been red is not
evidence. D-19's remedy is to GENERATE the counter-example rather than wait for one.

The plant, generated not authored:

    $ printf 'export const zzProbeScratch = 1;\n' > scripts/zz-probe-scratch.ts
    $ npx vitest run --exclude '**/scripts/e2e/**' -t "CASE 1" scripts/validate.test.ts

Transcript:

     RUN  v4.1.8 /Users/olgeroeselg/Projects/public/grugops

     scripts/validate.test.ts (133 tests | 1 failed | 132 skipped) 19ms
       CASE 1 - the tracked file set is FLOORED, and a `.ts` the walk finds untracked is NAMED 18ms

    Failed Tests 1

     FAIL  scripts/validate.test.ts > exactly ONE ticket-frontmatter reader exists in scripts/
     (32-12, widened 32-21, 32-36) > CASE 1 - the tracked file set is FLOORED, and a `.ts` the walk
     finds untracked is NAMED
    AssertionError: scripts/zz-probe-scratch.ts - a .ts file is on disk under scripts/ and is NOT
    tracked. A git-derived census neither scans it nor reports it, so this suite says nothing at all
    about it. Track it, or delete it if it is a scratch file - do not narrow the question:
    expected [ 'scripts/zz-probe-scratch.ts' ] to deeply equal []

    - Expected
    + Received

    - []
    + [
    +   "scripts/zz-probe-scratch.ts",
    + ]

     validate.test.ts:2801

     Test Files  1 failed (1)
          Tests  1 failed | 132 skipped (133)

    EXIT=1

The offender is NAMED in the message rather than counted, which is the property the case exists for:
a census that reports "151 != 152" tells a reader nothing about which file it could not see.

Restored:

    $ rm -f scripts/zz-probe-scratch.ts
    $ git status --porcelain -- scripts/
    A  scripts/ts-symbols.test-support.ts
     M scripts/validate.test.ts

— i.e. this plan's own two intended changes and NO plant residue.

A SECOND, UNPLANNED INSTANCE OF THE SAME REFUSAL, recorded because it is stronger evidence than the
plant. On its very first run, before `scripts/ts-symbols.test-support.ts` had been staged, CASE 1
reddened naming THAT file:

    AssertionError: scripts/ts-symbols.test-support.ts - a .ts file is on disk under scripts/ and is
    NOT tracked. [...] expected [ Array(1) ] to deeply equal []

    Test Files  1 failed (1)
         Tests  1 failed | 132 passed (133)
    EXIT=1

The case caught the instrument's own module the moment it existed on disk and was not yet tracked.
That was not arranged.


PLANT 1.2 — the `scripts/**/*.ts` pathspec must RED THE FLOOR at 26, not narrow the census quietly
---------------------------------------------------------------------------------------------------

The case: the same one, at its greater-than-100 PREMISE.

Why this is the highest-risk line in the phase: git's default pathspec matching is `wildmatch`
WITHOUT `WM_PATHNAME`, so a bare `*` crosses `/`. `scripts/**/*.ts` therefore demands at least one
INTERVENING directory and returns 26 of the 151 tracked files — the ones under `scripts/e2e/` and
`scripts/runnable-ref/`. A program built over those 26 makes every census in plans 04..09 pass while
covering a sixth of its subject. Nothing else on this tree would say so.

The plant — the pathspec swapped to the spelling an author reaches for first:

    $ perl -0pi -e 's/\["ls-files", "--", "scripts\/\*\.ts"\]/["ls-files", "--", "scripts\/**\/*.ts"]/' \
        scripts/ts-symbols.test-support.ts
    $ grep -n 'ls-files", "--"' scripts/ts-symbols.test-support.ts
    162:  const out = execFileSync("git", ["ls-files", "--", "scripts/**/*.ts"], {
    $ npx vitest run --exclude '**/scripts/e2e/**' -t "CASE 1" scripts/validate.test.ts

Transcript:

     RUN  v4.1.8 /Users/olgeroeselg/Projects/public/grugops

     scripts/validate.test.ts (133 tests | 1 failed | 132 skipped) 12ms
       CASE 1 - the tracked file set is FLOORED, and a `.ts` the walk finds untracked is NAMED 11ms

    Failed Tests 1

     FAIL  scripts/validate.test.ts > [...] > CASE 1 - the tracked file set is FLOORED, and a `.ts`
     the walk finds untracked is NAMED
    AssertionError: PREMISE: the tracked set under scripts/ is SHORT. Its own shortness is what it
    would hide - a census over a sixth of the sources passes exactly like a census over all of them,
    and the usual cause is the `scripts/**/*.ts` pathspec, which returns 26 of 151 here:
    expected 26 to be greater than 100

     validate.test.ts:2789

     Test Files  1 failed (1)
          Tests  1 failed | 132 skipped (133)

    EXIT=1

"expected 26 to be greater than 100" is the whole point: the wrong spelling does not fail quietly at
a smaller-but-plausible number, it fails at 26 and the message names the cause.

Restored:

    $ grep -n 'ls-files", "--"' scripts/ts-symbols.test-support.ts
    162:  const out = execFileSync("git", ["ls-files", "--", "scripts/*.ts"], {

and the file is byte-identical to the pre-plant copy.


=====================================================================
TASK 2 — the loader oracle
=====================================================================

PLANT 2.1 — a body-replacing `load` hook must RED THE EVENT FLOOR, not quietly record one edge
-----------------------------------------------------------------------------------------------

The case: `scripts/board-readonly.test.ts` > "32.1-01 - Node's OWN loader is the independent third
side of the closure oracle (D-09)" > "PREMISE: the loader recorded something at all".

Why this plant is the important one: CONTEXT.md D-09 offers "a loader hook that records and refuses
to evaluate" as a way to keep the corpus inert. 32.1-RESEARCH.md probe 2 measured what that shaping
actually does — replacing a module's body DELETES its import statements, so nothing past the entry is
ever resolved and the oracle records exactly ONE edge. An oracle in that shape AGREES WITH ANY
CLOSURE OF SIZE ONE. It passes. It proves nothing. This plant reproduces that exact failure against
the floor that exists to catch it.

The plant — a `load` arm injected into `ORACLE_SCRIPT` in `scripts/loader-oracle.test-support.ts`,
verbatim as it ran:

    "  load(url, context, nextLoad) {",
    "    if (url.endsWith(\"oracle.mjs\")) return nextLoad(url, context);",
    "    return { format: \"module\", shortCircuit: true, source: \"export {};\" };",
    "  },",

    $ npx vitest run --exclude '**/scripts/e2e/**' \
        -t "PREMISE: the loader recorded something at all" scripts/board-readonly.test.ts

Transcript:

     RUN  v4.1.8 /Users/olgeroeselg/Projects/public/grugops

     scripts/board-readonly.test.ts (179 tests | 1 failed | 178 skipped) 124ms
       PREMISE: the loader recorded something at all 53ms

    Failed Tests 1

     FAIL  scripts/board-readonly.test.ts > 32.1-01 - Node's OWN loader is the independent third
     side of the closure oracle (D-09) > PREMISE: the loader recorded something at all
    AssertionError: PREMISE: Node's resolver recorded 1 event(s) for scripts/board-dashboard.js, so
    every equality below compared nothing. The two known causes are a `load` hook that short-circuits
    with an inert body (which deletes the module's imports, so the walk stops at the entry) and a
    hook that never reaches Node's own loader at all: expected 1 to be greater than 5

     board-readonly.test.ts:1955

     Test Files  1 failed (1)
          Tests  1 failed | 178 skipped (179)

    EXIT=1

"recorded 1 event(s) ... expected 1 to be greater than 5" is the probe-2 truncation arriving as a
named failure instead of as a green run.

Restored: the file was copied back from its pre-plant copy and is BYTE-IDENTICAL to the staged blob
(`diff <(git show :scripts/loader-oracle.test-support.ts) scripts/loader-oracle.test-support.ts`
produced no output).


PLANT 2.2 — a module missing from the STATIC closure must be NAMED by the runtime equality
-------------------------------------------------------------------------------------------

The case: the same describe > "the RUNTIME module set equals the STATIC closure - both directions,
each side named".

The plant — one module dropped from the static side, which is what a hole in the static walk looks
like from the inside:

    -    const staticClosure = [...jsImportClosure(ROOT, DASHBOARD_ENTRY)].sort();
    +    const staticClosure = [...jsImportClosure(ROOT, DASHBOARD_ENTRY)].sort()
    +      .filter((m) => m !== "scripts/kit-model.js");

    $ npx vitest run --exclude '**/scripts/e2e/**' \
        -t "the RUNTIME module set equals the STATIC closure" scripts/board-readonly.test.ts

Transcript:

     RUN  v4.1.8 /Users/olgeroeselg/Projects/public/grugops

     scripts/board-readonly.test.ts (179 tests | 1 failed | 178 skipped) 146ms
       the RUNTIME module set equals the STATIC closure - both directions, each side named 74ms

    Failed Tests 1

     FAIL  scripts/board-readonly.test.ts > 32.1-01 - Node's OWN loader is the independent third
     side of the closure oracle (D-09) > the RUNTIME module set equals the STATIC closure - both
     directions, each side named
    AssertionError: Node's loader resolved a module the STATIC closure does not contain. The static
    walk has a hole exactly this wide, and every guard decided over that closure - the fs-symbol
    intersection, the builtin allow-list, the acquisitions PREMISE - has been deciding over a smaller
    set than the process actually acquires: expected [ 'scripts/kit-model.js' ] to deeply equal []

    - Expected
    + Received

    - []
    + [
    +   "scripts/kit-model.js",
    + ]

     board-readonly.test.ts:1982

     Test Files  1 failed (1)
          Tests  1 failed | 178 skipped (179)

    EXIT=1

The dropped module is NAMED, and it reds the `staticMissed` direction specifically — the direction
that means "the static walk has a hole" rather than "the static walk invented an edge". A single
`toEqual` over the two sets would have called this "not equal" and named neither direction.

Restored: `staticClosure` is back to `[...jsImportClosure(ROOT, DASHBOARD_ENTRY)].sort()` and
`git status --porcelain -- scripts/` carries only this plan's own intended changes.


=====================================================================
TASK 3 — the recursive importer derivation
=====================================================================

PLANT 3.1 — an importer under `scripts/runnable-ref/` must MOVE THE COUNT, which a depth-one walk
cannot see
-----------------------------------------------------------------------------------------------

The case: `scripts/board-readonly.test.ts` > "32-31 - every existing caller of the shared walker
still builds the same mirror" > "the set of modules that import the shared walker is DERIVED, and
its size is pinned".

What was wrong before this task: the derivation was a DEPTH-ONE `readdirSync` over exactly
`["scripts", "hooks"]`. It is a latent instance of the class this phase exists to close, and it was
sitting INSIDE the case meant to close it — a consumer under `scripts/runnable-ref/` imports the
walker, builds a mirror no row of `CLOSURE_BASELINES` pins, and never moves the count.

The plant, and it is tracked (`git add -N`) because the new derivation is git-derived and an
untracked file is correctly outside it — `scripts/validate.test.ts` > "CASE 1" is what names an
untracked one:

    $ cat scripts/runnable-ref/zz-probe-importer.ts
    import { closureTargets } from "../js-import-closure.js";

    export function zzProbeTargets(root: string, entry: string): readonly string[] {
      return [...closureTargets(root, entry)];
    }
    $ git add -N scripts/runnable-ref/zz-probe-importer.ts

THE OLD DERIVATION, RUN AGAINST THE SAME PLANTED TREE — this is the half that makes the plant a
measurement rather than an anecdote:

    $ node -e "<the depth-one readdirSync over ['scripts','hooks'] with the old
                (?:\.\.\/scripts|\.)\/js-import-closure\.js pattern>"
    depth-one count: 12
    contains the plant? false

Twelve. Unchanged. The old rule looked straight at a new caller of the shared walker and reported
nothing.

THE NEW DERIVATION, same tree, same instant:

    $ npx vitest run --exclude '**/scripts/e2e/**' \
        -t "the set of modules that import the shared walker is DERIVED" scripts/board-readonly.test.ts

     FAIL  scripts/board-readonly.test.ts > 32-31 - every existing caller of the shared walker still
     builds the same mirror > the set of modules that import the shared walker is DERIVED, and its
     size is pinned
    AssertionError: hooks/guard.test.ts, scripts/board-readonly.test.ts,
    scripts/check-foundation-guards.test.ts, scripts/check-platform-shapes.ts,
    scripts/check-uat-oracles.test.ts, scripts/context-freshness.ts, scripts/context-io.test.ts,
    scripts/generate-hook-manifest.ts, scripts/guarantees-freshness.ts,
    scripts/nonblocking-reader-parity.test.ts, scripts/now-running-freshness.ts,
    scripts/runnable-ref/zz-probe-importer.ts, scripts/trace-freshness.ts import
    scripts/js-import-closure.js. That count moved, so a caller of the shared walker was added or
    removed [...]: expected 13 to be 12

    - Expected
    + Received

    - 12
    + 13

     board-readonly.test.ts:2447

     Test Files  1 failed (1)
          Tests  1 failed | 178 skipped (179)

    EXIT=1

Thirteen — moved by EXACTLY one, and the message NAMES `scripts/runnable-ref/zz-probe-importer.ts`
in the list.

Restored:

    $ git restore --staged scripts/runnable-ref/zz-probe-importer.ts
    $ rm -f scripts/runnable-ref/zz-probe-importer.ts
    $ git status --porcelain -- scripts/
     M scripts/board-readonly.test.ts
     M scripts/ts-symbols.test-support.ts
    $ git ls-files -- 'scripts/*.ts' | grep -c zz-probe
    0


A SECOND, UNPLANNED RED, recorded because it is the same finding one register over
----------------------------------------------------------------------------------

The first run of the rewritten derivation reported ELEVEN, not twelve:

    AssertionError: scripts/board-readonly.test.ts, scripts/check-foundation-guards.test.ts, [...]
    import scripts/js-import-closure.js. [...]: expected 11 to be 12

    - Expected
    + Received

    - 12
    + 11

The missing member was `hooks/guard.test.ts`, which imports `../scripts/js-import-closure.js`. The
SET had been widened to the tracked files and the MATCHER had not: the replacement pattern still
enumerated the relative prefixes a top-level file can write. Widening the set while leaving the
matcher enumerated moves a blind spot rather than closing one, which is precisely the defect class
this task addresses — so it is recorded here rather than quietly fixed. The matcher now accepts any
relative depth (`from "\.[^"]*\/js-import-closure\.js"`), and the derivation returns 12 with
`hooks/guard.test.ts` in it.

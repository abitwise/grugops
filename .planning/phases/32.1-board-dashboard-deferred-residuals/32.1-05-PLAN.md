---
phase: 32.1-board-dashboard-deferred-residuals
plan: 05
type: execute
wave: 5
depends_on: ["32.1-01", "32.1-04"]
files_modified:
  - scripts/check-foundation-guards.test.ts
  - scripts/check-build-parity.ts
  - scripts/check-build-parity.js
  - package.json
  - .github/workflows/ci.yml
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-RED-baseline.txt
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-GREEN-proof.txt
autonomous: true
requirements: [DASH-02]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01: the one-authority rule over the check-script classification registers matches reads by RESOLVED DECLARATION SYMBOL, so a register read reached through an alias is inside the set the rule enumerates — closing F-19."
    - "D-01: the rule's subject is a DERIVED set of frozen classification registers with a vacuity floor, so removing a member cannot silently turn the rule into an assertion over nothing."
    - "D-06: `check:build-parity` is a gate module with a committed build-output twin and a CI step naming it directly, so the declared command shape carries zero named exceptions."
    - "D-05: every `check:*` entry in `package.json` matches one of exactly two declared shapes, the runner-step count is derived from the shape match rather than from a separator alphabet, and an entry outside the declared shapes is a refusal NAMING the entry — closing F-21 and ledger row 207."
  artifacts:
    - scripts/check-build-parity.ts
    - scripts/check-build-parity.js
    - scripts/check-foundation-guards.test.ts
    - package.json
    - .github/workflows/ci.yml
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-RED-baseline.txt
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-GREEN-proof.txt
  key_links:
    - "`scripts/check-foundation-guards.test.ts` imports `createScriptsProgram` and `declarationOf` from `./ts-symbols.test-support.js` (plan 01) so a register read reached through an alias resolves to the same declaration as a direct one."
    - "`.github/workflows/ci.yml` names `scripts/check-build-parity.js` directly, which is what the gate-module reachability proof reads; the workflow's only npm-run gate invocation is replaced, not supplemented."
    - "`package.json`'s `check:build-parity` entry becomes the same shape as the other nine gate entries, which is what makes the declared shape set exemption-free."
  prohibitions:
    - "Never widen a parser by adding another arm where a canonical form is called for."
    - "Never type a count literal that should be derived from the set it counts."
    - "Never let a rule become an assertion over an empty set to satisfy a different decision."
    - "Never fake a gate result, a test result, or a citation."
    - "Never let the checker instrument be imported into `scripts/context-io-writer-set.test.ts`, and never let that file change at all."
    - "Never run `npm test`."
---

<objective>
Close F-19 (ledger row 205), F-21 (row 207) and D-06's exemption in one plan, in the only order that
does not produce a vacuous rule. F-19 is the one-authority rule enumerating register reads by the
register's identifier TEXT, so an aliased read is outside the set it enumerates; F-21 is the step
counter enumerating separators, so five ordinary shell shapes are undercounted. Both are the phase's
class — a set enumerated over one spelling of the thing it is about — and both are closed by the same
kind of move, applied a second and third time so a reader can see one remedy repeat.

**The task order in this plan is load-bearing.** D-06 collapses the one entry that occupies the
toolchain class, which empties the register F-19's whole rule is written about. The F-19 rewrite
therefore lands FIRST, and it is rewritten to derive its subject registers rather than naming one,
so the member leaving in Task 2 moves a floored denominator instead of quietly making every
assertion true over nothing.

Purpose: `check:build-parity`'s inline evaluation is the live instance of exactly the nested-runner
shape the step counter's own banner already calls invisible. Turning it into an ordinary gate module
removes the instance and the exception in one move (D-06), which is what lets the declared shape set
be exemption-free (D-05).
Output: a symbol-resolving one-authority rule with a derived subject set; a new gate module and its
committed twin; a collapsed manifest entry; a CI step naming the module; and a declared command-shape
set of exactly two members with the step count derived from the match.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-CONTEXT.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-RESEARCH.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-PATTERNS.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Produced by THIS plan: `scripts/check-build-parity.ts` (new gate module, exporting a refusal-returning
function plus an entrypoint guard) and its committed `scripts/check-build-parity.js` twin; a rewritten
one-authority case and a declared-command-shape case in `scripts/check-foundation-guards.test.ts`; a
collapsed `check:build-parity` entry in `package.json`; a new gate step in `.github/workflows/ci.yml`;
`32.1-05-RED-baseline.txt` and `32.1-05-GREEN-proof.txt`. Ledger rows 205 and 207 reach their terminal
dispositions on this plan's evidence; the ledger rows themselves are written by plan 11.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The one-authority rule resolves symbols, and derives the registers it is about</name>
  <files>scripts/check-foundation-guards.test.ts, .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-RED-baseline.txt</files>
  <read_first>
    - `scripts/check-foundation-guards.test.ts:12689-12770` — the IN-03 one-authority rule as it stands: reads matched by `node.expression.getText(src)` equality against the register's name, the `reads` length pinned at one (`:12756-12761`), and the accessor-reference count pinned (`:12766-12770`).
    - `scripts/check-foundation-guards.test.ts:12062-12067` — `TOOLCHAIN_CHECK_SCRIPTS` and its single member; `:12126-12141` — `CHECK_SCRIPT_CLASSES` with its three pinned target counts; `:12155-12167` and `:12448-12473` — the classifier arms and the class-representation case.
    - `scripts/check-foundation-guards.test.ts:11831-11900` — the file's own `(r-class-authority)` case: the template for a derived member set with a floored ELEMENT denominator and a two-sided pin.
    - `scripts/ts-symbols.test-support.ts` from plan 01 — `createScriptsProgram`, `declarationOf`, `followAlias`.
    - `.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md` § 12, finding F-19 and its reproduction — the aliased register read that leaves the suite at exit 0.
    - `32.1-RESEARCH.md`, the section covering D-05 and D-06, sub-heading "the ripple D-06 carries", plus Pitfall 3.
  </read_first>
  <action>
Rewrite the one-authority rule so it decides by RESOLVED DECLARATION rather than by identifier text
(D-01). Build the program once through `createScriptsProgram`, then for each candidate access ask
`declarationOf` which declaration the accessed name resolves to, following import and export aliases.
An alias binding, a renamed import and the direct name all resolve to the same declaration, so the
plant F-19 demonstrated — a second, unguarded read reached through a local alias — is inside the set
the rule enumerates instead of outside it. Delete the identifier-text comparison rather than adding
an alias arm beside it.

Generalize the rule's SUBJECT at the same time, because Task 2 removes a member. Today the rule
names one register. Replace that with a derived set: census the file's own frozen classification
registers and apply the rule to each. Floor the derived register set above one, with a message
saying that a rule asserting over a single register that a later decision can empty is a rule that
stops asking without anything going red. This is the same defect one register over, and it is the
reason Task 2 can proceed at all.

Move the two pinned numbers with the derivation, in the same commit, with messages saying each number
is a decision that moved because the set moved. Keep the two-sided shape: the members and the
cardinality are separate assertions.

Discrimination, RED-first and mutation-proven (D-19). Build the mirror by appending to the live
source, never as an authored fixture. Three plants:

1. F-19's own reproduction — a local alias binding of a classification register plus an unguarded
   accessor reading through it. Prove it exits 0 against the PRE-change rule and exit 1 against the
   new one. This is the finding's reproduction and the plan's central claim.
2. A renamed-import route to the same register from another file, to show the alias following is not
   a special case for one syntax.
3. An empty derived register set, to prove the new vacuity floor reds rather than passing.

Record every plant's command, output and exit code in `32.1-05-RED-baseline.txt`. Restore after each
and confirm `git status --porcelain -- scripts/` is empty. Assert
`git diff --exit-code -- scripts/context-io-writer-set.test.ts` after the last plant.
  </action>
  <acceptance_criteria>
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts` exits 0.
    - The rule contains no comparison of an access expression's source text against a register's name.
    - The rule's subject register set is derived from the file's own declarations and is floored above one, with a message naming what its own shortness would hide.
    - `32.1-05-RED-baseline.txt` records F-19's alias plant producing exit 0 on the OLD rule and exit 1 on the NEW rule, with both full outputs.
    - The vacuity plant (an emptied register set) reds, proving the floor is a control rather than a comment.
    - `git diff --exit-code -- scripts/context-io-writer-set.test.ts` exits 0.
    - `git status --porcelain -- scripts/` is empty after every plant is removed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit on any of the three tsc targets</fails_when>
    <automated>git diff --exit-code -- scripts/context-io-writer-set.test.ts</automated>
    <fails_when>non-zero exit — the file changed, which reds residual R-31-21-04</fails_when>
  </verify>
  <done>Register reads are matched by resolved declaration; the rule's subject is a derived, floored set of registers; both pins moved as decisions in the same commit; F-19's reproduction is recorded green-then-red.</done>
</task>

<task type="auto">
  <name>Task 2: The build-parity check becomes an ordinary gate module, and its class leaves deliberately</name>
  <files>scripts/check-build-parity.ts, scripts/check-build-parity.js, package.json, .github/workflows/ci.yml, scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - `package.json` scripts block — the `check:build-parity` entry in full, and the nine sibling entries whose shape it must adopt.
    - `scripts/check-residual-citations.ts` (whole file, 137 lines) — the smallest complete gate on this tree: the header form, the truthiness-ternary root resolution at `:37-44` with its stated reason, the own-scope premise at `:110-116`, and the entrypoint guard and exit block at `:120-137`.
    - `scripts/check-nul-bytes.ts:1-45` and its tail — the two lines every gate header carries, the clear-professional-voice rule, and the reason the entrypoint guard matters on Windows.
    - `scripts/vacuity.ts:26-27, :46-97` — `Measured` and `reportMeasured`, its four ordered branches, and the named forbidden alternative.
    - `scripts/is-entry.ts` — `isEntrypoint`.
    - `.github/workflows/ci.yml:85, :102` (the only two npm-run gate mentions) and `:466-478` (the commented gate-step style to copy).
    - `scripts/check-foundation-guards.test.ts:12126-12141` (the class target counts), `:12155-12167` (the classifier arms), `:12448-12473` (the class-representation case).
  </read_first>
  <action>
Create `scripts/check-build-parity.ts`, a gate module in the shape `check-residual-citations.ts`
establishes: a header naming the plan and the finding it closes, a why-this-is-its-own-check block
naming what it was evicted from, the invocation line and the exit-code sentence, the builtins-only
and zero-dependency statement, and the clear-professional-voice note. Node standard library only;
no npm dependency; strictly the build invocation and a git query.

Its behaviour must be the current check's behaviour, moved rather than reinterpreted: run the build,
then ask git whether any tracked build output moved, and refuse when one did with the same operator
remedy the inline message carries today — run the build and commit the result. Resolve the root with
the truthiness ternary and not the nullish operator, for the reason `check-residual-citations.ts:37-44`
records. Report through `reportMeasured` so the PASS line carries the measurement (how many tracked
build outputs were examined, how many moved) and so a run that examined nothing fails its own floor
instead of printing a pass. Guard the entry with `isEntrypoint(import.meta.url)`.

Collapse the manifest entry to the same shape the nine sibling gate entries use, so the compound
disappears entirely rather than shrinking to a smaller compound. Build and commit
`scripts/check-build-parity.js` in the same commit as its source.

Add a CI step naming the gate module directly, in the style of the workflow's other gate steps and
with a comment stating why the step exists: the reachability proof reads the module name out of the
workflow, and the workflow's only npm-run gate invocation was this check's. Replace that invocation
rather than leaving both, so there is one way the check is reached.

Then move the classification deliberately, which is the part CONTEXT.md does not mention. The entry
leaves the toolchain class and joins the gate-module class, so both class target counts move. If the
toolchain class ends with zero members, delete the class and its register with a deletion note in the
form `scripts/js-import-closure.ts:464-470` establishes — name what the class was for, quote the
reason the register carried, and state the measurement that emptied it — and re-derive the
class-representation case so a class shape a check script matches is still proved to be represented.
Move the classifier's arms with the classes; a branch no check script can reach is dead code carrying
a false implication.

**The guard against the pitfall this task is named for:** if deleting the toolchain class would take
Task 1's derived register set to one member or fewer, do NOT delete it. Keep the class with a stated
reason, record the decision in the SUMMARY, and say that a rule asserting over one deletable register
is the defect, not the schedule. Task 1's floor is what makes this visible rather than silent.

Discrimination, RED-first: add a probe entry to a MIRRORED manifest that moves a tracked build output
and confirm the new gate module exits 1 naming it; confirm the gate exits 0 on the real tree.
Separately, remove the new CI step in a mirror and confirm the gate-module reachability proof reds
naming the module. Both captured in `32.1-05-RED-baseline.txt`.
  </action>
  <acceptance_criteria>
    - `git ls-files -- scripts/check-build-parity.ts scripts/check-build-parity.js` lists BOTH paths — source and committed twin, in the same commit.
    - `package.json`'s `check:build-parity` value is the same shape as `check:nul-bytes` — a build-to-scratch step followed by a single node invocation of a gate module — and contains no inline evaluation.
    - `.github/workflows/ci.yml` names `scripts/check-build-parity.js` in a step, and `grep -c 'npm run check:' .github/workflows/ci.yml` counts only comment occurrences.
    - `npm run check:build-parity` exits 0 and its PASS line carries a measurement (examined count and moved count), not a bare sentence.
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts` exits 0 with the class target counts moved and derived.
    - Task 1's derived register set still has more than one member after this task, or the toolchain class was kept with the decision recorded in the SUMMARY.
    - `32.1-05-RED-baseline.txt` records the moved-output plant redding the new gate and the removed-CI-step plant redding the reachability proof.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or the PASS line carries no examined/moved counts, or a tracked `.js` moved</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>node scripts/check-build-parity.js</automated>
    <fails_when>non-zero exit, or the run prints nothing (an entrypoint guard that did not match runs zero checks and exits 0 — a fabricated green)</fails_when>
  </verify>
  <done>The build-parity check is an ordinary gate module with a committed twin and a CI step naming it; the manifest entry carries no inline evaluation; the classes moved with deletion notes and re-derived counts; the one-authority rule is still asserting over more than one register.</done>
</task>

<task type="auto">
  <name>Task 3: A declared command shape with zero exemptions replaces the separator alphabet</name>
  <files>scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - `scripts/check-foundation-guards.test.ts:12475-12494` — the banner already naming row 207's residual in its own words: a module reached through any other nested runner is invisible to both sides.
    - `scripts/check-foundation-guards.test.ts:12504-12509` — the two step patterns and the runner-step counter that D-05 replaces; `:12513-12553` — the rule that consumes them and its refusal message form; `:12555` — the discrimination case immediately after.
    - `scripts/check-foundation-guards.test.ts:12102-12103` — `GATE_TARGET_RE` and `SUITE_TARGET_RE`, the TARGETING half, which D-05 does not replace.
    - `package.json` scripts block after Task 2 — the eleven `check:*` entries now occupying exactly two shapes.
    - `.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md` § 12, finding F-21 and its § 10 table — the five undercount shapes and the one overcount shape.
  </read_first>
  <action>
Replace the runner-step counter with a declared command-shape allow-list (D-05). Declare exactly two
shapes as data: the build-to-scratch-then-gate-module shape the nine gate entries use, and the
direct suite-runner shape the dashboard read-only check uses. Derive the step counts from which
shape an entry matches, not from counting separators in its text.

Anything that matches neither declared shape is a REFUSAL NAMING THE ENTRY, not an undercount. Write
the refusal message in the form the file already uses at `:12546-12553`: what was found with the item
named, what it costs downstream, and the remedy — with the cheap weakening refused by name, which
here is adding a third shape to make one entry fit. That refusal is the canonical form; the five
shapes F-21 enumerated do not each get an arm, because the question is no longer which separators a
command may contain.

The declared set must have zero exemptions. Task 2 removed the only entry that needed one, so assert
the declared shape set's cardinality directly and assert that every live `check:*` entry matches a
member. Both assertions derived from the manifest, both floored: an empty entry set and a silently
short one must each red, and the denominator is the entry count, not the match count.

Retire the banner's row-207 residual paragraph and replace it with what is now true — an entry
outside the declared shapes is named and refused — rather than deleting the paragraph and leaving no
statement of the boundary. Say what the rule still does not decide: the shape allow-list governs how
a check is INVOKED, not what the invoked module then does.

Discrimination, RED-first: against a MIRRORED manifest, add one probe entry per shape F-21 named —
a single pipe, a newline separator, a leading parenthesis, a wrapper command, a background separator,
and a runner name inside a quoted message — and confirm each is refused BY NAME, one message naming
the probe entry. Confirm the old counter's behaviour on the same six probes and record both columns
side by side in `32.1-05-RED-baseline.txt`: five that the old counter miscounted silently, one it
overcounted, and six the declared shape refuses by name. Never modify the real manifest for a plant.
  </action>
  <acceptance_criteria>
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts` exits 0.
    - The declared shape set has exactly two members, asserted from a derived cardinality, and every live `check:*` entry matches one.
    - No named exemption exists in the declared-shape rule.
    - Six mirrored probe entries, one per shape F-21 named, are each refused with the probe entry named in the message; the real `package.json` is byte-unchanged by the plants.
    - `32.1-05-RED-baseline.txt` carries the old-counter and new-rule results side by side for all six probes.
    - The banner states the current boundary rather than the retired residual.
    - `npm run build && npm run check:build-parity` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or "BUILD PARITY FAILED" on stderr</fails_when>
    <automated>git diff --exit-code -- package.json</automated>
    <fails_when>non-zero exit after the plants are removed — a probe entry was written into the real manifest</fails_when>
  </verify>
  <done>Two declared shapes, zero exemptions, step counts derived from the match, six probes refused by name with the old counter's behaviour recorded beside the new one, and the real manifest untouched by any plant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `package.json` command strings → the reachability proof | a command string decides which module is proved reachable by CI |
| repository source → the checker's symbol resolution | an aliased read decides whether a second unguarded authority is visible |
| build output → `git diff` | the gate's whole verdict rests on a git query's output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-05-01 | Spoofing | a second unguarded read of a classification register | high | mitigate | Reads are matched by resolved declaration through `declarationOf`, so an alias binding resolves to the same declaration as a direct name; F-19's own reproduction is recorded exiting 0 on the old rule and 1 on the new one. |
| T-32.1-05-02 | Repudiation | a rule that stops asking when its subject is emptied | high | mitigate | The rule's subject register set is derived and floored above one, and Task 2 carries an explicit instruction to keep the toolchain class rather than let the floor be crossed. The vacuity plant proves the floor reds. |
| T-32.1-05-03 | Spoofing | a gate that runs zero checks and exits 0 | high | mitigate | `isEntrypoint(import.meta.url)` rather than a hand-built file URL, for the Windows reason `check-nul-bytes.ts` records; and `reportMeasured`'s visited-is-zero branch reports that the check was not performed instead of printing a pass. |
| T-32.1-05-04 | Tampering | a check command shape that hides an invoked module from CI | high | mitigate | Two declared shapes, zero exemptions, every live entry asserted to match one, and anything else refused by name. The nested-runner instance that the banner called invisible is removed by Task 2 rather than described. |
| T-32.1-05-05 | Tampering | a plant written into the real manifest | medium | mitigate | Every probe runs against a mirrored manifest; `git diff --exit-code -- package.json` after the plants is in Task 3's verify set. |
| T-32.1-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package and the new gate module is builtins-only. `32.1-RESEARCH.md` § Package Legitimacy Audit records zero additions and zero `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts; `package.json` still carries no dependencies key, which `scripts/board-readonly.test.ts:4174` reds on. |
</threat_model>

<verification>
1. `npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts` — exits 0.
2. `npm run typecheck` — exits 0.
3. `npm run build && npm run check:build-parity` — exits 0, PASS line carries a measurement.
4. `node scripts/check-build-parity.js` — exits 0 and prints its measured PASS line.
5. `git diff --exit-code -- package.json scripts/context-io-writer-set.test.ts` — exits 0.
6. `npx vitest run --exclude '**/scripts/e2e/**'` — the full regression lane, exits 0.
</verification>

<success_criteria>
- F-19 closed with its own reproduction recorded green-then-red, and the rule's subject derived and floored.
- The build-parity check is an ordinary gate module reached by name from CI, with a committed twin.
- Two declared command shapes, zero exemptions, and six F-21 probes refused by name.
- The class counts moved with their sets, in the same commits, as decisions.
</success_criteria>

<output>
Create `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-SUMMARY.md` when done.
</output>

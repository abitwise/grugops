# Phase 32.1: Board Dashboard Deferred Residuals - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-17
**Phase:** 32.1-board-dashboard-deferred-residuals
**Areas discussed:** Proof enumerations → canonical form, Regex import scanner fate, Carried rows disposition, Completion + review posture

---

## Proof enumerations → canonical form

| Option | Description | Selected |
|--------|-------------|----------|
| One shared checker | `ts.Program` checker resolves references by declaration symbol for F-15/F-19/WR-04/row 210 | ✓ |
| Per-rule canonical form, no checker | Refuse namespace import / re-export of a key table by name | |
| You decide | Planner measures checker cost | |

| Option | Description | Selected |
|--------|-------------|----------|
| Declared `check:*` shape, refuse outside | Exact command shapes; count derived from the match | ✓ |
| Keep the counter, widen the alphabet | Add the six missing separators | |

| Option | Description | Selected |
|--------|-------------|----------|
| One sentence builder | Every published interpolation escaped by construction | ✓ |
| Also widen TICKET_CONTROL to C1 | Refuse C1 at the grammar | |
| Builder plus C1 refusal | Both | |

| Option | Description | Selected |
|--------|-------------|----------|
| Re-derive `NOT_A_SECOND_AUTHORITY`; keep only what still fails | Count re-pinned from the derived set | ✓ |
| Keep all 9 as-is | | |

| Option | Description | Selected |
|--------|-------------|----------|
| Test-only helper module | Excluded from tsc emit, no `.js` twin | ✓ |
| Production module with committed `.js` | Costs a freshness row and census entry | |

| Option | Description | Selected |
|--------|-------------|----------|
| Tracked files only, floored against the walk | `git ls-files`; walk-vs-tracked equality | ✓ |
| Keep the `readdirSync` walk | Untracked files in scope | |

| Option | Description | Selected |
|--------|-------------|----------|
| Move `check:build-parity`'s inline JS into a script; zero exemptions | | ✓ |
| Keep it as one named exemption | | |

| Option | Description | Selected |
|--------|-------------|----------|
| F-14 rule covers all three board modules | | ✓ |
| `board-model.ts` only | | |

**User's choice:** all recommended options. **Notes:** none.

---

## Regex import scanner fate

| Option | Description | Selected |
|--------|-------------|----------|
| Retire it; TS parse is the one authority | `typescript` a stated dev/CI requirement of gate scripts; test asserts `install/`/`hooks/` never import the scanner | ✓ |
| Keep regex, add canonical refusals | Fourth patch on a second grammar | |
| You decide | | |

| Option | Description | Selected |
|--------|-------------|----------|
| Node's own loader as the oracle | `module.register()` hook records resolved specifiers | ✓ |
| AST oracle over wider node kinds | Same library both sides | |
| No second side | Plant battery only | |

| Option | Description | Selected |
|--------|-------------|----------|
| F-18: restore two independent predicates | | ✓ |
| Accept one predicate, record it | | |

| Option | Description | Selected |
|--------|-------------|----------|
| `import()` argument: string literal only | | ✓ |
| Also admit substitution-free template literals | | |

**User's choice:** all recommended options. **Notes:** none.

---

## Carried rows disposition

| Option | Description | Selected |
|--------|-------------|----------|
| Row 176: write the disposition rows the gate asks for | | ✓ |
| Rephrase the five workflow documents | | |
| Leave RED, re-home | | |

| Option | Description | Selected |
|--------|-------------|----------|
| Row 183: run the e2e lane once at a human checkpoint | | ✓ |
| Re-home to Phase 33 | | |

| Option | Description | Selected |
|--------|-------------|----------|
| Rows 184/208: runtime probe closes what it can; waive the rest | First use of `waived` | ✓ |
| `node_modules` refusal policy plus waive | | |
| Waive both outright | | |

| Option | Description | Selected |
|--------|-------------|----------|
| Rows 194/209/210: one housekeeping plan | | ✓ |
| Fold each into the nearest plan | | |

**User's choice:** all recommended options. **Notes:** none.

---

## Completion + review posture

| Option | Description | Selected |
|--------|-------------|----------|
| Ledger-driven: 20 of 20 terminal dispositions | Derived count equality | ✓ |
| Re-verify the eight DASH truths plus the ledger | | |

| Option | Description | Selected |
|--------|-------------|----------|
| One scoped red-team round | Over the new instruments only | |
| Code review only, no adversarial round | | ✓ |
| Full posture as in Phase 32 | | |

| Option | Description | Selected |
|--------|-------------|----------|
| WR-01: move the contract to the code's rule + equality test | | ✓ |
| Revert the code to the contract | | |

| Option | Description | Selected |
|--------|-------------|----------|
| Instruments first, then consumers, RED-first with mutation proof | | ✓ |
| One cutover plan per finding | | |

**User's choice:** as marked. The one departure from a recommendation: no adversarial round in 32.1
(code review plus verifier only). **Notes:** none.

## Claude's Discretion

- Exact declared `check:*` shapes; helper file naming and tsconfig exclusion; how the loader
  oracle keeps the corpus inert; plan count and wave layout under the instrument-first order.

## Deferred Ideas

- Widen `TICKET_CONTROL` to C1 (future DASH-01 grammar revision).
- `node_modules` refusal policy for the dashboard closure.
- Rows 186/193 stay with Phase 33.

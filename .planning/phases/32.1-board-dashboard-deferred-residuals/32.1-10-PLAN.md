---
phase: 32.1-board-dashboard-deferred-residuals
plan: 10
type: execute
wave: 10
depends_on: ["32.1-09"]
files_modified:
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-LIVE-TRANSCRIPT.txt
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-GATES.txt
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-GREEN-proof.txt
autonomous: false
requirements: [DASH-03, DASH-05, DASH-08]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-13: the live end-to-end lane runs exactly once on this box, after every code change has landed and the regression lane is green, and only after a named human go — it spends tokens on an authenticated session, takes roughly eight minutes, and can hang."
    - "D-13: the live lane's state stops being an unknown-verify — ledger row 183's terminal disposition rests on a filed transcript carrying the date, the command, the duration, the counts and the exit code."
    - "D-13: the regression lane stays the excluded one; the live lane is not adopted as a routine verify by any later plan."
    - "D-19: the phase's gate sweep is derived from the manifest rather than recalled, so a gate that exists and was not run is visible as a gap rather than as a silence."
  artifacts:
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-LIVE-TRANSCRIPT.txt
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-GATES.txt
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-GREEN-proof.txt
  key_links:
    - "The live lane is the unexcluded runner invocation; the regression lane is the same runner with the end-to-end directory excluded. The difference between the two is the entire content of ledger row 183."
    - "The gate sweep's denominator is derived from the manifest's check entries, which plan 05 made an exemption-free declared set, so the sweep and the manifest cannot disagree."
  prohibitions:
    - "Never start the live lane without a named human go."
    - "Never adopt the live lane as a routine verify command."
    - "Never fake a gate result, a test result, or a citation."
    - "Never type a gate list that should be derived from the manifest."
    - "Never edit an evidence document in place."
---

<objective>
Discharge ledger row 183 (D-13): the live claude-CLI end-to-end lane has never been run for this
milestone's board work, so its state is an unknown-verify rather than a result. This plan runs it
once, on this box, behind a blocking human checkpoint that names the spend, and files the transcript
beside the plan.

**On the checkpoint type.** The project's workflow dial sets human verification to end-of-phase,
which normally means a plan uses an inline human check rather than a blocking checkpoint task. D-13
is a locked user decision that specifies a blocking checkpoint with a named go, and a locked decision
is not overridden by a dial. The checkpoint below is therefore a blocking one, deliberately.

Purpose: the live lane spends tokens on an ambient authenticated session, takes roughly eight
minutes, and has hung before. That is exactly the shape of thing an agent must not start on its own
judgement, and this repository's posture — humans decide, agents execute — applies to a spend as much
as to a deploy.
Output: one live transcript, one derived gate sweep, and the phase's final regression-lane number.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-CONTEXT.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-RESEARCH.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-09-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Produced by THIS plan: `32.1-10-LIVE-TRANSCRIPT.txt` (the live lane's complete output, following the
shape `32-22-LIVE-TRANSCRIPT.txt` established), `32.1-10-GATES.txt` (the derived gate sweep,
following `32-41-GATES.txt`), and `32.1-10-GREEN-proof.txt` (the phase's final regression-lane
number beside its starting number). Ledger row 183 reaches its terminal disposition on this plan's
evidence; the ledger row itself is written by plan 11.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The gate sweep, derived from the manifest</name>
  <files>.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-GATES.txt, .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-GREEN-proof.txt</files>
  <read_first>
    - `.planning/phases/32-board-projector-cli-dashboard/32-41-GATES.txt` — the sweep's established shape.
    - `package.json` scripts block — the check entries, now an exemption-free declared set after plan 05.
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-05-SUMMARY.md` — the declared shapes and the classification counts as they now stand.
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-GREEN-proof.txt` — the phase's starting regression-lane number.
  </read_first>
  <action>
Derive the gate list from the manifest's check entries rather than recalling it, run every one, and
record each gate's name, command, exit code and headline output line in `32.1-10-GATES.txt`. Assert
that the number of gates run equals the number of check entries the manifest declares; a sweep whose
denominator is a remembered list is the set-literal failure class this phase exists to close, applied
to the sweep itself.

Run the freshness gates too — the build-output gate and the generated-artifact freshness gates — and
record them in the same file with the same four fields. State in the file which set each gate came
from, so a reader can re-derive the denominator.

Then run the full regression lane, the excluded one, and record its file count, test count, skip
count, duration and exit code in `32.1-10-GREEN-proof.txt` beside the phase's starting number from
plan 01's proof. State the difference and attribute it: cases this phase added, cases it removed, and
nothing else. A total that moved for an unattributed reason is a finding, not a footnote.

Do NOT run the live lane in this task. It runs in Task 3, after the checkpoint.
  </action>
  <acceptance_criteria>
    - `32.1-10-GATES.txt` records every gate the manifest declares, each with its name, command, exit code and headline line, and states the derived denominator.
    - Every recorded gate exits 0.
    - `32.1-10-GREEN-proof.txt` records the final regression-lane counts beside plan 01's starting counts, with the difference attributed case by case.
    - The live lane has not been run.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run build &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or "BUILD PARITY FAILED" on stderr, or the freshness gate names a stale or orphaned build output</fails_when>
    <automated>npm run check:nul-bytes &amp;&amp; npm run check:diff-disposition &amp;&amp; npm run check:platform-shapes &amp;&amp; npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit from any of the four, or "CHECK(S) FAILED" in any Result block</fails_when>
  </verify>
  <done>Every declared gate is run and green, the sweep's denominator is derived and stated, and the phase's regression-lane movement is attributed case by case.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: named go before the live end-to-end lane spends tokens</name>
  <precondition>Every code change in this phase has landed, plans 01 through 09 are committed, the regression lane is green, and Task 1's gate sweep records every declared gate at exit 0. If any of those is not true, do not present this checkpoint.</precondition>
  <what-built>
Plans 01 through 09 are committed and Task 1 has just recorded every declared gate at exit 0 in
`32.1-10-GATES.txt`, with the regression lane — the excluded runner invocation — green and its counts
filed in `32.1-10-GREEN-proof.txt`. Nothing has been spent on the live lane yet.

The next task runs the repository's LIVE end-to-end lane: the unexcluded runner invocation, which
includes the live claude-CLI suite. This is the only sanctioned run of that command in the entire
phase, and it is here because ledger row 183 records that the lane has never been run for this
milestone's board work, so its state is an unknown-verify rather than a result.

**What it costs, stated plainly so the go is informed:**

- It spends tokens on the ambient authenticated session on this machine. The credential is ambient,
  not repository-managed, so the spend lands on whoever is signed in here.
- It takes roughly eight minutes when it completes normally.
- It has hung before. A hung gate is not a red gate; if it hangs, the run is stopped and the outcome
  is recorded as a hang rather than as a failure or a pass.

**What a go authorizes:** exactly one run of the live lane, its transcript filed beside this plan,
and nothing else. It does not authorize re-running the lane on a red, and it does not make the live
lane a routine verify for any later plan.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-GATES.txt` and confirm every
   listed gate exits 0 and that the file states the derived denominator it used.
2. Open `32.1-10-GREEN-proof.txt` and confirm the regression lane is green and its movement from plan
   01's starting counts is attributed case by case.
3. Confirm you are signed in on this machine with the session you are willing to spend, and that you
   are willing to spend roughly eight minutes of it now.
4. Answer GO or NO-GO.

A NO-GO is a legitimate outcome and is not a failure of this phase: ledger row 183 then ends as
`re-homed-with-named-owner` rather than `closed-with-reproduction`, and plan 11 writes it that way.
If you want the run at a different moment, say when.
  </how-to-verify>
  <recommendation>
GO. Every code change has landed, the regression lane is green, and this is the last moment in the
phase where a live failure could still be attributed to this phase's changes rather than to a later
one. Running it after the phase closes turns a result into an investigation.
  </recommendation>
  <resume-signal>Type "go" to run the live lane once now, or "no-go" to skip it (optionally naming a better moment)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: One live run, one transcript, one recorded outcome</name>
  <files>.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-LIVE-TRANSCRIPT.txt</files>
  <read_first>
    - `.planning/phases/32-board-projector-cli-dashboard/32-22-LIVE-TRANSCRIPT.txt` — the transcript shape to follow.
    - `package.json` scripts block — the unexcluded runner entry and the end-to-end entry, so the transcript names exactly what was run.
    - `scripts/e2e/uat-live.test.ts` — the live suite, so the transcript can say what the lane exercises.
    - `.planning/WINDOWS.md` row 183 — the row's own description, which the transcript must answer.
  </read_first>
  <action>
Only if the checkpoint returned a go.

Run the live lane once. On this tree that is the manifest's unexcluded runner entry, `npm test` —
confirm the exact command by reading the manifest rather than by taking it from this sentence, since
a plan that names a stale command runs the wrong thing. This single invocation is the phase's one
sanctioned use of it; every other verify in every other plan uses the excluded lane.

Capture its complete output, the wall-clock duration, the file and test counts and the exit code into
`32.1-10-LIVE-TRANSCRIPT.txt`, with the date and the exact command at the top. Record the fact that
the credential is the ambient authenticated session on this machine.

If it hangs, stop it, and record the outcome AS A HANG — the elapsed time, where it stopped, and what
the last output was. A hang is a third outcome beside pass and fail, and recording it as either of
the other two is the fabrication this repository's rules forbid. A hang means row 183 ends as
re-homed-with-named-owner with the hang as its recorded reason, not as closed.

If the checkpoint returned a no-go, write `32.1-10-LIVE-TRANSCRIPT.txt` anyway, containing the
no-go, its date, and the sentence plan 11 will use as row 183's re-homing reason. An absent transcript
is indistinguishable from an unrecorded run.

Do not re-run the lane for any reason. If it fails, record the failure with its output and hand the
finding to plan 11's ledger; a second run to see whether it passes this time is exactly how a flaky
result becomes a claimed one.

Then re-run the regression lane once more and confirm the live run changed nothing about the tree:
`git status --porcelain` before and after the live run must be equal, and the difference must be
recorded in the transcript either way.
  </action>
  <acceptance_criteria>
    - `32.1-10-LIVE-TRANSCRIPT.txt` exists and records the date, the exact command, the outcome, and — for a completed run — the duration, counts and exit code.
    - The outcome is recorded as exactly one of pass, fail, hang, or no-go. No other word is used.
    - The lane was run at most once.
    - `git status --porcelain` before and after the live run is recorded, and any difference is named.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0 after the live run.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-LIVE-TRANSCRIPT.txt &amp;&amp; grep -cE '^(OUTCOME|Outcome): (pass|fail|hang|no-go)$' .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-LIVE-TRANSCRIPT.txt</automated>
    <fails_when>non-zero exit (the transcript is missing or empty), or the printed count is not 1 — the outcome line is absent, duplicated, or uses a word outside the four</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the output names a file under `.planning/phases/32.1-board-dashboard-deferred-residuals/` — the transcript carries a raw control byte</fails_when>
  </verify>
  <done>The live lane ran at most once with its outcome recorded as one of four words; the transcript carries the date, the command and the measurement; the working tree is unchanged by the run; the regression lane is still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambient authenticated session → the live lane | a credential not managed by this repository is spent by a command this repository runs |
| live lane output → ledger evidence | the transcript is what a later reader relies on to know whether the lane was run and what happened |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-10-01 | Elevation of Privilege | an agent spending a human's credential on its own judgement | high | mitigate | A blocking human checkpoint precedes the run, names the token spend, the duration and the hang risk, and states exactly what a go authorizes. A no-go is a legitimate outcome with its own recorded disposition. |
| T-32.1-10-02 | Denial of Service | a hung lane read as a red or a green | high | mitigate | Hang is a named third outcome. The transcript's outcome line is constrained to four words and asserted by the verify command, so a hang cannot be recorded as a failure or a pass. |
| T-32.1-10-03 | Repudiation | a flaky result re-run until it passes | high | mitigate | The plan permits at most one run and says why: a second run to see whether it passes this time is how a flaky result becomes a claimed one. The single-run property is an acceptance criterion. |
| T-32.1-10-04 | Repudiation | a gate that exists and was not run, recorded as a silence | medium | mitigate | The sweep's denominator is derived from the manifest's declared check entries, and the count of gates run is asserted equal to it. |
| T-32.1-10-05 | Tampering | the live run mutating the working tree | medium | mitigate | The working-tree status is captured before and after the live run and any difference is named in the transcript. |
| T-32.1-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package; `32.1-RESEARCH.md` § Package Legitimacy Audit records zero additions and zero `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts. |
</threat_model>

<verification>
1. `npx vitest run --exclude '**/scripts/e2e/**'` — exits 0.
2. `npm run build && npm run check:build-parity && npm run freshness` — exit 0.
3. `npm run check:nul-bytes && npm run check:diff-disposition && npm run check:platform-shapes && npm run check:dashboard-readonly` — all exit 0.
4. `32.1-10-GATES.txt`, `32.1-10-GREEN-proof.txt` and `32.1-10-LIVE-TRANSCRIPT.txt` all exist and
   each carries a measurement rather than a claim.
</verification>

<success_criteria>
- Every declared gate run and green, with a derived denominator.
- The live lane run at most once, behind a named human go, with its outcome one of four words.
- The phase's regression-lane movement attributed case by case.
- Row 183's evidence complete, whichever way the go went.
</success_criteria>

<output>
Create `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-10-SUMMARY.md` when done.
</output>

32.1-04 RED BASELINE — the split-reader join, the exemption registry, and row 194
=================================================================================

D-19's order: every cutover records the OLD rule's verdict on the plant BEFORE the switch, then the
NEW rule's verdict after, with the plant restored and `git status --porcelain -- scripts/` clean.

The recorded exit code is the exit code of the CENSUS'S OWN REFUSAL ASSERTION applied to the plant —
`expect(splitReaderOffenders(<plant>)).toEqual([])`, the same assertion the live tree is judged by.
So exit 0 means THE PLANT WALKED THROUGH (the rule saw nothing) and exit 1 means THE PLANT WAS
REFUSED. A cutover that works reads exit 0 before and exit 1 after.

Repository state: branch `main`, HEAD `6e4fbede` at capture time.
Command: `npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts -t "<case>"`.


TASK 1 — THE SPLIT-READER JOIN (F-15, WR-04)
============================================

How the OLD verdicts were taken: a temporary block was inserted into the PRE-CHANGE
`scripts/validate.test.ts` at line 2178, holding the four plants as `ScannedFile` rows and applying
the live refusal assertion to each through the then-current `splitReaderOffenders(rows)`. The block
was removed and `git checkout -- scripts/validate.test.ts` confirmed clean before any edit landed.

How the NEW verdicts were taken: after the cutover, the four committed discrimination cases had
their expected offender lists temporarily replaced by `[]` — i.e. the same live refusal assertion,
applied to the same four plants through the new `splitReaderOffenders(census)` — and each was run
alone. The file was restored from a byte copy taken before the replacement.

Every plant's key-table half is the LIVE `scripts/board-model.ts` read off disk, and plant 4's
key-naming half is the LIVE `scripts/board-corpus.ts` with two seeded lines appended. Neither is an
authored fixture, so a plant cannot drift away from the thing it copies.

  PLANT 1 — namespace import (F-15 reproduction 1)
    files: board-model.ts (live) + plant-namespace-reader.ts
           `import * as model from "./board-model.js"` then a text scan driven by `model.TICKET_KEYS`
    OLD RULE: exit 0   Test Files  1 passed (1) — 0 offender(s); the plant walked through.
              Cause: the old join collected `NamedImports` element names only, so a namespace
              import contributed ZERO binding names and the file was never a candidate.
    NEW RULE: exit 1   AssertionError: expected [ Array(1) ] to deeply equal []
              offender: "plant-namespace-reader.ts SCANS text and takes the KEY half from
                         board-model.ts as `TICKET_KEYS`"

  PLANT 2 — two-hop re-export through a barrel (F-15 reproduction 2)
    files: board-model.ts (live) + plant-barrel.ts (`export { TICKET_KEYS } from "./board-model.js"`)
           + plant-reexport-reader.ts importing from the barrel and scanning
    OLD RULE: exit 0   Test Files  1 passed (1) — 0 offender(s); the plant walked through.
              Cause: the old join asked whether the IMMEDIATE module declared a key table. A barrel
              declares nothing, so the pair reassembled one hop away with nothing going red.
    NEW RULE: exit 1   AssertionError: expected [ Array(1) ] to deeply equal []
              offender: "plant-reexport-reader.ts SCANS text and takes the KEY half from
                         board-model.ts as `TICKET_KEYS`"
              `followAlias` walks the whole chain, so the offender is named against the ORIGINAL
              declaration rather than against the barrel.

  PLANT 3 — renamed named import (THE CONTROL)
    files: board-model.ts (live) + plant-renamed-reader.ts
           `import { TICKET_KEYS as K } from "./board-model.js"` then a text scan driven by `K`
    OLD RULE: exit 1   Test Files  1 failed (1)
              offender: "plant-renamed-reader.ts takes the key half from board-model.ts as
                         `TICKET_KEYS`"
              This is the ONE shape the pre-cutover rule caught — it read `el.propertyName`.
    NEW RULE: exit 1   AssertionError: expected [ Array(1) ] to deeply equal []
              offender: "plant-renamed-reader.ts SCANS text and takes the KEY half from
                         board-model.ts as `TICKET_KEYS`"
              The control keeps redding: the cutover closed three shapes without dropping the fourth.

  PLANT 4 — the converse direction (WR-04)
    files: plant-scanner.ts (a generic `valuesFor(t, keys)` that scans and names no key)
           + board-corpus.ts (LIVE content + a seeded import of `valuesFor` and one seeded call)
    OLD RULE: exit 0   Test Files  1 passed (1) — 0 offender(s); the plant walked through.
              Cause: the guard clause `if (!h.scans || h.namesBothKeys) continue;` skipped exactly
              the file the converse offender is. The banner's claim that this direction was
              impossible was what the skip made look true.
    NEW RULE: exit 1   AssertionError: expected [ Array(1) ] to deeply equal []
              offender: "board-corpus.ts NAMES both key spellings and takes the SCANNING half from
                         plant-scanner.ts as `valuesFor`"
              The message names both files and states which way the import runs.

  SUMMARY, TASK 1:  OLD 0/0/1/0   NEW 1/1/1/1.

  Residual R-31-21-04 (`scripts/context-io-writer-set.test.ts` must stay byte-unchanged and free of
  any checker text):
    $ git diff --exit-code -- scripts/context-io-writer-set.test.ts
    exit 0

  Working tree after every plant was removed:
    $ git status --porcelain -- scripts/
    (empty)


TASK 2 — THE EXEMPTION REGISTRY, RE-DERIVED UNDER THE CHECKER (D-04, D-21)
==========================================================================

The per-entry measurement, taken BEFORE a word of the registry moved. The census's key half was
re-run over the tracked `scripts/*.ts` set (153 files, `git ls-files -- 'scripts/*.ts'`) and each
registry entry was asked whether the re-derived census still trips it. Reproduced by re-running
`staticText` + `namesKey` + the text-primitive complement verbatim over each tracked source.

  #  entry                        still trips?  the literals the census actually resolves
  -- ---------------------------- ------------  ------------------------------------------------
  1  board-model.test.ts          YES           column :101 (a test description), status :1165
                                                (a test description); 18 distinct primitives
  2  board-read.test.ts           YES           column :354 (a two-column test description),
                                                status :542 (a planted ticket document);
                                                10 distinct primitives
  3  validate.test.ts             YES           status :251 (`/status/i`), column :252
                                                (`bad-ticket-bad-column`); 11 distinct primitives
  4  audit-model.test.ts          YES           column :455 (`/column/i`), status :967 (a
                                                CLAIM_STATUSES test description);
                                                13 distinct primitives
  5  board-dashboard.test.ts      YES           column :272 (a WIP-count test description),
                                                status :793 (a bare `"status"` dial key);
                                                15 distinct primitives
  6  check-banned-claims.test.ts  YES           status :583 (`- status: true`), column :2777 (a
                                                file:line:column test description);
                                                17 distinct primitives
  7  check-diff-disposition.ts    YES           status :1058 (the git subcommand argument),
                                                column :1630 (one operator message);
                                                9 distinct primitives  <-- ledger row 194
  8  compactor.test.ts            YES           column :1485 (a `column-0` test description),
                                                status :2340 (a writer-order test description);
                                                11 distinct primitives
  9  board-tracer.test.ts         YES           column in TWELVE distinct literals
                                                (:114 :122 :134 :142 :147 :152 :181 :285 :303
                                                :453 :804 :969) and status in ONE (:969, the
                                                planted ticket fixture, which supplies BOTH);
                                                11 distinct primitives  <-- ledger row 210

  NINE of nine still trip. NOTHING was deleted in task 2; the checker cutover moved the split-reader
  JOIN, not the whole-file literal census, so no entry falls out of it. Row 210 is therefore
  RE-HOMED WITH A NAMED OWNER (D-21) and its measurement is written into entry 9's own reason.
  Row 194 is closable, but by rewording one literal — task 3, not this one.

  THE DERIVED SET, measured the same run:
    DETECTED (10) = the nine entries above + board-model.ts, the one authority
    DERIVED (9)   = DETECTED minus board-model.ts
    registry keys == DERIVED  ->  true

  MUTATION PROOF that the new derived-members case DISCRIMINATES (it is not a restatement of the
  liveness case). Both mutations were made to the post-task-2 file, run alone, then reverted from a
  byte copy taken beforehand.

    MUT A — the `compactor.test.ts` entry DELETED, the count constant left at 9:
      $ npx vitest run … scripts/validate.test.ts -t "exemption set has exactly the DERIVED members"
      exit 1   AssertionError: … expected [ 'audit-model.test.ts', …(7) ] to deeply equal
               [ 'audit-model.test.ts', …(8) ]
      A file the census names with no entry is a carrier that walked through — caught.

    MUT B — a seeded `zzz-seeded-impostor.ts` entry ADDED for a file the census never names:
      exit 1   AssertionError: … expected [ 'audit-model.test.ts', …(9) ] to deeply equal
               [ 'audit-model.test.ts', …(8) ]
      An entry with no live subject is a standing hole — caught.

  After both mutations were reverted: `git status --porcelain -- scripts/` showed only the task-2
  edit to `scripts/validate.test.ts`.


TASK 3 — LEDGER ROW 194: ONE LITERAL REWORDED, ONE EXEMPTION DELETED, ONE COMMIT
================================================================================

WHAT THE CENSUS ACTUALLY SEES IN `scripts/check-diff-disposition.ts`, measured before anything moved
(the same probe as task 2, restricted to this file and reported per distinct literal site):

    column: 1 distinct literal site   :1630  "its `safety_surface` column before moving any number
                                              here; lowering the expectation or "
    status: 1 distinct literal site   :1058  "status"   <-- the git subcommand argument inside
                                              `git(["status", "--porcelain"])`

  Line 1058 is a GIT SUBCOMMAND, not prose and not a message: removing it changes behaviour. Row
  194 and CONTEXT.md D-15 both describe the remedy as removing two key words from this module's
  PROSE, and that framing is wrong about the mechanism — what trips the census is a STRING LITERAL,
  and a `//` comment is invisible to it. Because `namesBothKeys` is an EVERY-key conjunction,
  dropping `column` alone is sufficient: the findings fall to `{status}`, the conjunction fails, and
  the module stops being detected. The reword is `column` -> `field` at :1630, and nothing else moves.

TWO DISCRIMINATION PLANTS, EACH RUN ALONE AND EACH REVERTED FROM A BYTE COPY TAKEN BEFOREHAND.
Both were measured against the post-task-2 tree.

  PLANT (i) — the registry entry DELETED, the reword NOT made:
    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts
    exit 1   Test Files  1 failed (1)
    The cases that red:
      × TICKET_FRONTMATTER_READER_COUNT is 1, and the carrier is scripts/board-model.ts
      × the exemption set has exactly the DERIVED members: everything the census names but the one
        authority
      × the exemption set has exactly the pinned number of members
      × with NO plant, the carrier count is exactly one — the converse of the row above
      × ADJACENCY: two readers differing only in key spelling count as TWO, not as one
    A deletion without the reword turns the module into a SECOND CARRIER. Caught, five ways.

  PLANT (ii) — the reword MADE, the registry entry LEFT STANDING:
    $ npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts
    exit 1   Test Files  1 failed (1)
    The cases that red:
      × every named exemption is actually detected by the widened derivation
      × the exemption set has exactly the DERIVED members: …
    verbatim: "the exemption for "check-diff-disposition.ts" matches nothing the census names. It is
    a standing hole with no live reason: … expected [ 'audit-model.test.ts', …(8) ] to include
    'check-diff-disposition.ts'"
    A reword without the deletion leaves an exemption with no subject. Caught by the liveness case.

  HONEST CORRECTION TO THE PLAN'S WORDING. The plan says "delete the registry entry WITHOUT the
  reword and confirm the LIVENESS assertion reds". Measured, that is the wrong direction for that
  particular case: the liveness iteration reds when an ENTRY STANDS and its file stops being
  detected — plant (ii) — while plant (i) reds the carrier, derived-members and count cases instead.
  BOTH directions were captured rather than only the one the plan named, and both confirm the same
  conclusion the plan draws: the reword and the deletion must land in ONE commit, because either one
  alone is red.

THE COMMITTED RETIREMENT CASE. `ROW 194 — the gate module names ONE key spelling, and putting the
other back makes it a carrier again` re-measures all of the above at test time: the live module is
not named by the census, it is not exempt either, it still scans, its every-key conjunction is
false, and a plant generated from the LIVE module by putting `column` back makes it a detected
carrier again. The deletion is therefore a measurement that has been watched refusing something,
not an assertion.

AFTER THE CUTOVER (reword + deletion + count 9 -> 8 + the retirement case, all in one commit):
  $ npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts     exit 0, 139 passed
  $ npm run typecheck                                                        exit 0
  $ npm run check:diff-disposition                                           exit 0, ALL CHECKS PASSED
      (the gate's own verdict is unchanged by the reword — 0 findings over 39/39 elements)
  $ git diff -- scripts/check-diff-disposition.ts
      one hunk at the operator-message line; NO change at the working-tree dirty-check line
  $ npm run generate:hook-manifest                                           regenerated
      `hooks/hook-entry.ts` records a CONTENT HASH for every module in each hook decider's closure,
      and `scripts/check-diff-disposition.js` is one of them. The reword moved that hash, so the
      manifest is a companion edit that MUST land in the same commit — `floor-invariance.test.ts`
      asserts both that no manifest hash is stale and that `hooks/hook-entry.ts` carries no
      uncommitted modification. Measured: 50 tests across 6 files red until the manifest moved.

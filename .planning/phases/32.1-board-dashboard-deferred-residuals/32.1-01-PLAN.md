---
phase: 32.1-board-dashboard-deferred-residuals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ts-symbols.test-support.ts
  - scripts/loader-oracle.test-support.ts
  - tsconfig.json
  - scripts/validate.test.ts
  - scripts/board-readonly.test.ts
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-RED-baseline.txt
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-GREEN-proof.txt
autonomous: true
requirements: [DASH-01, DASH-02, DASH-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01: `scripts/validate.test.ts` resolves a key-table reference to its DECLARATION symbol through the shared instrument, so a renamed, namespace, two-hop re-exported and directly-named import all land on the same declaration."
    - "D-03: the program's file set is `git ls-files -- 'scripts/*.ts'` (151 today), floored above 100, and every `.ts` the `readdirSync` walk finds is either in that set or NAMED as untracked — never silently scanned and never silently skipped."
    - "D-02: `npm run build` emits no `.js` twin for either new test-support module and `npm run check:build-parity` exits 0, so `typescript` stays a dev/CI-only concern and no freshness row is demanded."
    - "D-09: the loader oracle records Node's own resolve events for the dashboard closure, and `git status --porcelain` is byte-identical before and after the corpus load."
    - "D-09 / DASH-04 + DASH-08 concurrency edge: if the corpus load is interrupted or run in parallel, nothing is mutated — the guarantee is asserted by the before/after working-tree equality, not argued from the corpus's shape."
    - "D-03: the scanner's importer set is derived RECURSIVELY over the tracked set, so a consumer under `scripts/runnable-ref/` cannot sit outside a depth-one walk while the pin stays green."
  artifacts:
    - scripts/ts-symbols.test-support.ts
    - scripts/loader-oracle.test-support.ts
    - tsconfig.json
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-RED-baseline.txt
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-GREEN-proof.txt
  key_links:
    - "`scripts/validate.test.ts` imports the checker instrument from `./ts-symbols.test-support.js` and asks it which declaration a name resolves to."
    - "`scripts/board-readonly.test.ts` imports the loader oracle from `./loader-oracle.test-support.js` and compares its recorded module set against `jsImportClosure`'s static closure."
    - "`tsconfig.json`'s `exclude` array carries `**/*.test-support.ts`, which is what keeps both modules out of `tsc` emit."
  prohibitions:
    - "Never widen a parser by adding another arm where a canonical form is called for."
    - "Never type a count literal that should be derived from the set it counts."
    - "Never fake a gate result, a test result, or a citation."
    - "Never let the checker instrument be imported into `scripts/context-io-writer-set.test.ts`, and never let that file change at all."
    - "Never hand-edit `.planning/WINDOWS.md`."
    - "Never hand-edit any of the seven generated `.claude/skills/*/SKILL.md` twins."
    - "Never run `npm test` — it launches the live claude-CLI end-to-end lane. The regression lane is `npx vitest run --exclude '**/scripts/e2e/**'`."
---

<objective>
Land the three instruments every later plan in this phase consumes, proven end to end on their
first use rather than asserted: the shared symbol-resolving checker (D-01), its test-only home and
emit exclusion (D-02), the tracked-file-set derivation with its floor (D-03), and Node's own loader
oracle (D-09). This is D-19's instrument-first order: nothing is cut over until the instrument that
will decide the cutover has been watched refusing something.

The wave-0 blocking pre-condition is already discharged — D-23 records that `.planning/milestone.lock`
was restored with `git checkout` before planning, so the regression lane is a real green again. This
plan re-takes and files that baseline as the phase's "before" number.

Purpose: three of the eight round-4 findings (F-15, F-19, F-21) and two review warnings (WR-04, and
the oracle half of F-17) are the same defect — a set enumerated over one spelling of the thing it is
about. The remedy this repository has recorded for that class is a canonical form and a resolving
instrument, never another arm. Every cutover in plans 04..09 asks one of these two instruments.
Output: two test-support modules, one `tsconfig.json` exclusion, a floored tracked-file derivation,
a recursive importer derivation, and the RED-baseline / GREEN-proof transcript pair D-19 requires.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-CONTEXT.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-RESEARCH.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-PATTERNS.md
</context>

<flagged_assumptions>
Phase 32.1 has no SPEC, so the spec-less edge probe ran over the DASH-01..08 requirement text and
returned twelve unresolved items. D-16 forbids re-verifying DASH-01..08 from scratch, so each item
is disposed here once, for the whole phase. **Three are AUTHORED** into `must_haves.truths` because
a 32.1 change genuinely touches the edge; **nine are FLAGGED** as assumptions naming the Phase-32
artifact that already covers them. 3 + 9 = 12, no silent drops.

| # | Requirement | Category | Disposition |
|---|---|---|---|
| 1 | DASH-04 | concurrency | **AUTHORED** — this plan, truth 5: the D-09 corpus load mutates nothing under interruption or parallelism, asserted by the working-tree equality. |
| 2 | DASH-08 | concurrency | **AUTHORED** — this plan, truth 5 (same assertion; DASH-08's "zero runtime dependencies" half is authored in plan 08's D-10 truth). |
| 3 | DASH-06 | unclassified | **AUTHORED** — plans 06 and 08: the import-graph guard's canonical form is decided by a real tokenizer (D-08) and a runtime oracle (D-09), with a non-string-literal `import()` argument refused by node kind (D-11). |
| 4 | DASH-01 | boundary | FLAGGED — covered by Phase 32's `scripts/board-model.test.ts` bounds suite (plan `32-02`, Task 2: "a 380 KB board degrades visibly and never loses a row"). Not re-verified here (D-16). |
| 5 | DASH-01 | adjacency | FLAGGED — covered by Phase 32's ticket-row/column adjacency cases in `scripts/board-tracer.test.ts` (`32-01`). Not re-verified here (D-16). |
| 6 | DASH-01 | empty | FLAGGED — covered by Phase 32's total line partition (`32-02`, Task 1) and the `32-15` total tickets-walk partition. Not re-verified here (D-16). |
| 7 | DASH-01 | ordering | FLAGGED — covered by `32-17`'s `boundNames` sort-before-bound fix ("membership stops depending on the filesystem"). Not re-verified here (D-16). |
| 8 | DASH-01 | precision | FLAGGED — covered by `32-18`'s code-unit rendering of the header's count. Not re-verified here (D-16). |
| 9 | DASH-02 | unclassified | FLAGGED — covered by Phase 32's parse-oracle fuzz suite and the `32-16` `normalizeDocument` authority. This phase touches only the PROOF's robustness (F-15, F-19, F-21), not the grammar. |
| 10 | DASH-03 | unclassified | FLAGGED — covered by `32-15`/`32-17` (`conflicts[]` surfacing, one join per identifier). This phase touches only the SENTENCES those conflicts carry (F-14, plan 07). |
| 11 | DASH-05 | unclassified | FLAGGED — covered by `32-03` Task 1 (read-verify-reread, per-source staleness, last-good carry-forward). This phase touches only the watch-record contract (WR-01, plan 09). |
| 12 | DASH-07 | unclassified | FLAGGED — covered by `32-01` Task 3 (`--once`, `--json`, exit codes) and `32-13`'s single-JSON-document correction. This phase touches only the render path of the F-14 sentences (plan 07). |
</flagged_assumptions>

<artifacts_this_phase_produces>
Produced by THIS plan:

| Symbol / file | Kind | Note |
|---|---|---|
| `scripts/ts-symbols.test-support.ts` | new test-only module | exports `createScriptsProgram`, `trackedScriptSources`, `walkedScriptSources`, `declarationOf`, `followAlias` |
| `scripts/loader-oracle.test-support.ts` | new test-only module | exports `recordRuntimeAcquisitions` returning `{ events, modules, builtins, treeBefore, treeAfter }` |
| `tsconfig.json` `exclude` entry `**/*.test-support.ts` | config | the pattern that keeps both modules out of emit |
| `32.1-01-RED-baseline.txt` / `32.1-01-GREEN-proof.txt` | evidence transcripts | D-19's pair for this plan |

Produced by LATER plans in this phase (named here so no reader hunts): the D-07 sentence builder
export in `scripts/board-model.ts` (plan 07); `scripts/check-build-parity.ts` + its committed `.js`
twin and a new `.github/workflows/ci.yml` step (plan 05); `docs/audit/29-style-dispositions/32.1-02.md`
and `32.1-03.md` (plans 02, 03); `32-40-ADVERSARIAL-REVIEW-ERRATUM.md` (plan 11); the phase ledger
`32.1-LEDGER.md` and its derived-count test (plan 11); `32.1-10-LIVE-TRANSCRIPT.txt` (plan 10); the
two `waived` rows in `.planning/WINDOWS.md` (plan 11, written by `gsd-tools windows waive`).
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: One symbol-resolving instrument, over the whole tracked set, proven on its first consumer</name>
  <files>scripts/ts-symbols.test-support.ts, tsconfig.json, scripts/validate.test.ts</files>
  <precondition>The regression lane is green on this checkout: `.planning/milestone.lock` exists on disk (D-23 restored it with `git checkout` before planning). If `git status --short` reports it as deleted, halt — `check-nul-bytes` fails closed on a tracked path it cannot scan and every verify in this phase is meaningless until it is restored.</precondition>
  <reversibility rating="costly">Two guard rules and the D-04 exemption registry are re-derived from this instrument; reverting means re-typing the enumerations this phase exists to delete.</reversibility>
  <read_first>
    - `scripts/runnable-ref/uat-spec-integrity.ts:1280-1400` — `createProgramForTarget`, the ONLY real program build on this tree and the template to copy: explicit `noEmit`, a per-file fault boundary wrapped around `host.getSourceFile`, a UNION of root names, and a try/catch on `getTypeChecker`.
    - `scripts/runnable-ref/uat-spec-integrity.ts:1852-1861` — `followAlias`, the alias-following helper that answers F-19/WR-04's `getText()` failure.
    - `scripts/runnable-ref/uat-spec-integrity.ts:868-1000` — the structural `TsApi` interface, declared over exactly the members it calls.
    - `scripts/ci-workflow.testkit.ts:1-28` — the test-only-module header doctrine to restate in this module's own words. **Its emit behaviour is NOT the analog**: `scripts/ci-workflow.testkit.js` is tracked and emitted, which is why this plan uses the `.test-support.ts` spelling from CONTEXT.md D-02 and not `.testkit.ts`.
    - `scripts/validate.test.ts:1757-1811` (`readerHalves`), `:1775-1790` (`staticText`), `:1806-1809` (`namesBothKeys`), `:1815-1823` (the `SCANNED` walk).
    - `scripts/context-io-writer-set.test.ts:13-28, :229-299` — the derive-and-count doctrine and the mutation-mirror pattern.
    - `tsconfig.json` (whole file, 28 lines) and `tsconfig.tests.json` — the tests target declares its OWN `exclude` array and does not inherit the parent's, so the new module is still type-checked by `npm run typecheck` with no extra wiring.
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-RESEARCH.md` §D-01, §D-03 and Pitfall 1.
  </read_first>
  <action>
Create `scripts/ts-symbols.test-support.ts`, a test-only module that is the ONE authority over
"which declaration does this name resolve to, inside the `scripts/` sources" (D-01).

Naming decision, stated because PATTERNS.md recommends a different spelling: the analog for a
test-only module under `scripts/` is `ci-workflow.testkit.ts`, but that file IS emitted and its
`scripts/ci-workflow.testkit.js` twin is tracked. Excluding `**/*.testkit.ts` from emit would turn
that committed twin into an ORPHANED COMMITTED OUTPUT and red `npm run freshness`. So the module
takes CONTEXT.md D-02's own `*.test-support.ts` spelling, and the exclusion pattern added to
`tsconfig.json` is `**/*.test-support.ts`, which matches nothing that exists today. Record this
divergence in the module header.

Exports, and what each must do:

- `trackedScriptSources(root)` — returns the tracked `.ts` paths, repo-relative and sorted, by
  running `git ls-files` with the pathspec `--` then `scripts/*.ts`. Do NOT use the spelling
  `scripts/**/*.ts`: git's default pathspec matching has no `WM_PATHNAME`, a bare `*` crosses the
  separator, and that spelling returns 26 of the 151 tracked files. This is the highest-risk line in
  the phase; the floor below is what makes it visible.
- `walkedScriptSources(root)` — the `readdirSync` recursive walk, returning the same repo-relative
  shape, so the two sets are directly comparable.
- `createScriptsProgram(root, ts)` — builds the program, taking the compiler API as an argument in
  the structural-interface style of `uat-spec-integrity.ts:868-1000`. Copy the four load-bearing
  moves verbatim in spirit: read and parse `tsconfig.json` through `findConfigFile` /
  `readConfigFile` / `parseJsonConfigFileContent`; set `noEmit` explicitly on the resulting options;
  wrap `host.getSourceFile` in a try/catch that records a per-file parse fault and returns
  `undefined`, so one pathological source cannot abort program creation; and build `rootNames` as a
  UNION of the config's own file names with the tracked test files. The union is mandatory, not
  cosmetic — `tsconfig.json` excludes every `*.test.ts`, so the config's file list carries ZERO of
  the 73 test files this census is about, and without the union the checker reports every reference
  inside `validate.test.ts` as unresolved and the census passes vacuously. Exclude
  `scripts/runnable-ref/fixtures/**` from the root names and say why in the docblock: those 19 files
  import a Playwright package this repository deliberately does not depend on, so including them
  yields TS2307 on every one.
- Return the discriminated shape `{ ok: true, ... } | { ok: false, cause: string }` — a could-not-run
  is a NAMED cause, never an empty result.
- `declarationOf(checker, node)` — `getSymbolAtLocation` followed by `followAlias`, returning the
  declaration's source file path and name, or `null`.
- `followAlias(ts, checker, symbol)` — copied in shape from `uat-spec-integrity.ts:1853`.

Add `**/*.test-support.ts` to `tsconfig.json`'s `exclude` array, with a comment above it in the
style of the existing fixtures comment stating WHY the file is not tooling output: it is a test
harness that needs the compiler API, and the compiler API must never enter a committed build output
because host machines run the committed output with nothing installed.

Then wire the instrument to its first consumer in `scripts/validate.test.ts`, as three cases:

1. The tracked-set floor (D-03). Derive the tracked set and the walked set; assert the tracked count
   is greater than 100 with a PREMISE message naming what its own shortness would hide; assert the
   set of walked paths absent from the tracked set is empty, with a message that NAMES each offender
   and states that an untracked file under `scripts/` is neither scanned nor reported.
2. The instrument's own PREMISE. Build the program; assert the build returned `ok`; assert the count
   of references the checker actually resolved to a declaration is greater than zero, with a message
   saying that a checker resolving nothing makes every census below true over nothing.
3. The end-to-end proof, which is what makes this a tracer rather than a foundation: take one
   existing key-table reference inside `validate.test.ts` itself, resolve it through
   `declarationOf`, and assert the returned declaration is the one the file actually declares. One
   reference, one resolution, asserted — the full path from `git ls-files` through the program build
   through the checker to a named declaration.

RED-first, with the plants generated rather than authored (D-19): before the assertions pass, plant
`scripts/zz-probe-scratch.ts` (an untracked file) and confirm case 1 reds NAMING it; separately swap
the pathspec to the `**` spelling and confirm the greater-than-100 floor reds at 26. Capture both in
`32.1-01-RED-baseline.txt`. Remove the plant, restore the pathspec, and confirm
`git status --porcelain -- scripts/` is empty before the GREEN proof.
  </action>
  <acceptance_criteria>
    - `git ls-files -- scripts/ts-symbols.test-support.ts` is non-empty and `git ls-files -- scripts/ts-symbols.test-support.js` is EMPTY — the module is tracked and has no committed build output.
    - `tsconfig.json`'s `exclude` array has exactly 5 entries and includes `**/*.test-support.ts`.
    - `npm run build && npm run check:build-parity` exits 0 with no `.js` moved.
    - `npm run typecheck` exits 0 — the module is still type-checked by the `tsconfig.tests.json` target.
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts` exits 0 and the three new cases are among the passing ones.
    - The tracked-set derivation returns 151 paths on this checkout and the walked set is equal to it (symmetric difference 0).
    - `git diff --exit-code -- scripts/context-io-writer-set.test.ts` exits 0 — residual `R-31-21-04` is a TEXT match over that file whose expected value is `by-identifier=true type-checker=false`, and this task must leave it byte-unchanged.
    - `32.1-01-RED-baseline.txt` records BOTH plants (the untracked scratch file named by the floor, and the short pathspec redding the greater-than-100 floor at 26) with their exit codes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or "BUILD PARITY FAILED" on stderr, or `git diff --name-only -- '*.js'` names a moved file</fails_when>
    <automated>git diff --exit-code -- scripts/context-io-writer-set.test.ts</automated>
    <fails_when>non-zero exit — the file changed, which reds residual R-31-21-04</fails_when>
  </verify>
  <done>The checker instrument exists as a tracked, un-emitted test-only module; the tracked file set is 151 and floored; an untracked `.ts` under `scripts/` is named rather than skipped; one real reference resolves to its declaration; both RED plants are recorded and both were restored.</done>
</task>

<task type="auto">
  <name>Task 2: Node's own loader as the independent third side of the closure oracle</name>
  <files>scripts/loader-oracle.test-support.ts, scripts/board-readonly.test.ts</files>
  <read_first>
    - `scripts/board-readonly.test.ts:1813-1890` — the existing two-sided oracle this becomes a third side of, including the git-derived corpus at `:1823-1828` (and its stated reason: a walk would make the corpus depend on whether a build had just run) and the PREMISE floor at `:1830-1835`.
    - `scripts/board-readonly.test.ts:2404-2424` — `ALLOWED_BUILTIN_SPECIFIERS` and its pinned count of 3.
    - `scripts/board-readonly.test.ts:188-190` — `DASHBOARD_ENTRY` / `MODEL_ENTRY`.
    - `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-RESEARCH.md` §D-09 in full — probe 1, **probe 2 (the trap)**, probe 3 (the shaping that works), the caveat about vitest's transform pipeline, and the Code Examples section's oracle listing.
    - `32.1-PATTERNS.md` § "NEW loader-oracle helper (D-09)" and § "No Analog Found" — nothing on this tree registers a loader hook; only the attach point has an analog.
  </read_first>
  <action>
Create `scripts/loader-oracle.test-support.ts` exporting `recordRuntimeAcquisitions(root, entryRel)`,
which registers a synchronous in-process loader hook with `registerHooks` from `node:module`,
records every specifier Node's own resolver is asked for while the corpus loads, and returns the
recorded events plus the derived runtime module set, the derived runtime builtin set, and the
working-tree snapshot taken immediately before and immediately after the load (D-09).

Record on the RESOLVE side only, and let the corpus load for real. Do NOT install a load hook that
short-circuits with an inert body: research probe 2 measured that shaping recording exactly one edge,
because replacing a module body deletes its import statements and the walk stops at the entry. An
oracle shaped that way agrees with any closure of size one and proves nothing.

State the inertness argument in the module header AND assert it rather than resting on it: the
corpus is the repository's own dashboard closure, whose entry is guarded by `is-entry` so importing
it as a non-entry runs no main, and whose modules perform no top-level write. The assertion is the
`git status --porcelain` snapshot equality across the load, which is what turns the argument into
evidence. Deregister the hook in a `finally`.

Then add the oracle to `scripts/board-readonly.test.ts` beside the existing two-sided parser oracle,
as a third side, with these cases:

1. A PREMISE that the recorded event count is greater than 5 — 13 resolve events were measured on
   this tree for the dashboard entry, so the floor is well under the measurement and still catches
   the probe-2 truncation. Message: a loader that recorded nothing makes every equality below
   vacuous. Derive the floor's comparison value from the recording, never from a typed expectation
   of which modules appear.
2. Module-set equality: the runtime module set, repo-relative and sorted, equals the static closure
   `jsImportClosure` returns for the same entry, asserted in BOTH directions with two named lists so
   a fabricated edge and a missed edge are distinguishable.
3. Builtin-identity equality: the runtime builtin set equals `ALLOWED_BUILTIN_SPECIFIERS`. This is a
   second, INDEPENDENT authority over the same allow-list the static census pins, obtained for free,
   and it is the runtime probe ledger row 208 asks for.
4. The no-writer assertion: the working-tree snapshot before the load equals the snapshot after, with
   a message stating that a difference means a writer executed during the oracle run.

RED-first (D-19): stub the static closure to drop one module and confirm case 2 reds naming exactly
that module; separately register a body-replacing load hook and confirm the event floor in case 1
reds. Record both in `32.1-01-RED-baseline.txt`, then restore and confirm
`git status --porcelain -- scripts/` is empty.
  </action>
  <acceptance_criteria>
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts` exits 0.
    - The oracle records at least 13 resolve events for the dashboard entry on this checkout.
    - The runtime module set has exactly 5 members and equals the static closure as sorted lists.
    - The runtime builtin set equals the three members of `ALLOWED_BUILTIN_SPECIFIERS` as sorted lists.
    - `git ls-files -- scripts/loader-oracle.test-support.js` is EMPTY — the new module emits nothing.
    - `32.1-01-RED-baseline.txt` records the dropped-module plant redding case 2 by name AND the body-replacing-hook plant redding the event floor.
    - `git status --porcelain` is unchanged across the oracle run in the GREEN proof.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit — the DASH-06 guard's own npm entry disagrees with the direct vitest run</fails_when>
  </verify>
  <done>The loader oracle exists as a tracked, un-emitted test-only module; it records Node's real resolution events; the runtime module set and the runtime builtin set each equal their static counterpart in both directions; the working tree is provably unchanged by the load; both RED plants are recorded and restored.</done>
</task>

<task type="auto">
  <name>Task 3: The importer set is derived recursively, so a consumer under a subdirectory cannot hide</name>
  <files>scripts/board-readonly.test.ts</files>
  <read_first>
    - `scripts/board-readonly.test.ts:2088-2091` — `CLOSURE_BASELINE_COUNT = 9` and `WALKER_IMPORTER_COUNT = 12`.
    - `scripts/board-readonly.test.ts:2229-2251` — the depth-one `readdirSync` over two directories that derives `WALKER_IMPORTER_COUNT`.
    - `scripts/board-readonly.test.ts:1653` and `:1910` — the repository's own prose claiming the scanner reaches SEVEN production callers, against the six a depth-one grep finds.
    - `32.1-PATTERNS.md` § "Plan Prohibitions Carried From The Analogs", item 6.
    - `32.1-RESEARCH.md` §D-08 "the count pins the cutover moves" and assumption A7.
  </read_first>
  <action>
Replace the depth-one `readdirSync` that derives `WALKER_IMPORTER_COUNT` with a derivation over the
tracked set returned by `trackedScriptSources` from Task 1, extended to cover `hooks/` the same way,
so a file under `scripts/runnable-ref/` that imports the scanner is inside the counted set rather
than outside a depth-one glob. A depth-one walk that cannot see a subdirectory is a latent instance
of the exact class this phase closes, and it sits inside the guard that is supposed to close it
(D-03).

Re-derive the importer count from the new set and move the pin to whatever the derivation returns,
in the same commit, with the count message saying the number is a DECISION that moved because the
set moved — never a bumped constant. If the recursive derivation finds a seventh importer, that
settles the six-versus-seven question the repository's own prose raises at `:1653` and `:1910`; if it
finds six, correct those two sentences to the measured number in the same commit and say what was
measured. Do not leave the prose and the pin disagreeing.

Also record, in the case's docblock, the disposition of review item IN-05: CONTEXT.md D-03 says the
tracked-set floor stops a local scratch file, but IN-05 names a DIFFERENT walk —
`presenceSpellingSites` / `SPELLING_WALK_SKIP` / `SPELLING_WALK_EXTENSIONS` in
`scripts/board-model.test.ts:2358-2390`, which walks the whole repository root over seven extensions
including markdown, json and text. The floor added in Task 1 does not close that walk. State this as
a named residual carried to the phase ledger rather than implying it is closed, and name the remedy
IN-05 itself proposes (derive that walk's corpus from `git ls-files` as the oracle one file over
already does).

RED-first: plant a file under `scripts/runnable-ref/` that imports the scanner, confirm the recursive
derivation's count moves by exactly one while the old depth-one derivation would not have moved at
all, remove the plant, and confirm `git status --porcelain -- scripts/` is empty.
  </action>
  <acceptance_criteria>
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts` exits 0.
    - The importer derivation reads from the tracked set, not from a `readdirSync` over a fixed two-element directory list.
    - The importer count pin and the two prose sentences at `scripts/board-readonly.test.ts:1653` and `:1910` state the SAME number after this task.
    - A planted importer under `scripts/runnable-ref/` moves the derived count by exactly one; the plant is removed and `git status --porcelain -- scripts/` is empty afterwards.
    - The IN-05 residual is recorded in the case's docblock naming `scripts/board-model.test.ts:2358-2390` as the walk the Task-1 floor does NOT cover.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts scripts/validate.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or "BUILD PARITY FAILED" on stderr</fails_when>
  </verify>
  <done>The importer set is derived over the tracked set recursively; the pin moved with the derivation in the same commit; the prose and the pin agree; IN-05 is recorded as a named residual rather than assumed closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source → `ts.createSourceFile` / `ts.createProgram` | author-controlled source text enters a recursive-descent parser inside the test process |
| repository `.js` build output → Node's ESM loader | committed build output is executed for real by the loader oracle |
| `git` subprocess output → test assertions | untrusted-width command output crosses into the derivation that decides a pin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-01-01 | Tampering | `scripts/loader-oracle.test-support.ts` corpus load | high | mitigate | The corpus is the repository's own `is-entry`-guarded dashboard closure. `git status --porcelain` is captured immediately before and immediately after the load and asserted equal, so a writer executing during the oracle run reds the case rather than silently mutating the tree. The inertness is asserted, never argued. |
| T-32.1-01-02 | Denial of Service | `createScriptsProgram` parsing author-controlled source | medium | mitigate | `host.getSourceFile` is wrapped in a try/catch that records a per-file parse fault and returns `undefined`, copied from `uat-spec-integrity.ts:1292-1299`, so one pathological source cannot abort program creation and turn a red gate into a crashed one. |
| T-32.1-01-03 | Repudiation | a vacuous instrument reported as a proof | high | mitigate | Three PREMISE floors, each with a message naming what its own emptiness would hide: tracked set greater than 100, resolve events greater than 5, resolved-declaration count greater than zero. The element denominator is floored, not only the member count. |
| T-32.1-01-04 | Information Disclosure | `git ls-files` output width crossing into a pin | low | accept | The command is run with a fixed pathspec against the repository root and its output is split on newline only; the repository's own oracle at `board-readonly.test.ts:1823` takes the same shape and has carried it since Phase 32. |
| T-32.1-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs NO package. `32.1-RESEARCH.md` § Package Legitimacy Audit records `typescript`, `vitest` and `@types/node` as pre-existing and unchanged, with zero `[ASSUMED]`, `[SUS]` or `[SLOP]` verdicts. `package.json` must still carry no `dependencies` key — `scripts/board-readonly.test.ts:4174` reds if one appears, and that case is in this plan's verify set. |
</threat_model>

<verification>
Run in this order, from the repository root:

1. `npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts scripts/board-readonly.test.ts` — exits 0.
2. `npm run typecheck` — exits 0 (three targets).
3. `npm run build && npm run check:build-parity` — exits 0, no tracked `.js` moved.
4. `git ls-files -- 'scripts/*.test-support.js'` — EMPTY.
5. `git diff --exit-code -- scripts/context-io-writer-set.test.ts` — exits 0.
6. `npx vitest run --exclude '**/scripts/e2e/**'` — the full regression lane, exits 0. Record the
   file/test counts in `32.1-01-GREEN-proof.txt` beside the re-taken baseline; the phase's "before"
   number is 75 files / 5163 passed / 2 skipped and the "after" must differ only by the cases this
   plan added.
</verification>

<success_criteria>
- Both test-support modules are tracked, type-checked, and emit no `.js`.
- The tracked file set is 151, floored above 100, and equal to the walk.
- One real reference resolves to its declaration through the checker.
- The loader oracle's module set and builtin set each equal their static counterpart, both directions.
- The working tree is provably unchanged by the corpus load.
- The importer set is derived recursively and its pin moved with it.
- `32.1-01-RED-baseline.txt` and `32.1-01-GREEN-proof.txt` both exist and each RED entry names the
  plant, the case it reddened, and the exit code.
</success_criteria>

<output>
Create `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-SUMMARY.md` when done.
</output>

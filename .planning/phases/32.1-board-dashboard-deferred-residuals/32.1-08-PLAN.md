---
phase: 32.1-board-dashboard-deferred-residuals
plan: 08
type: execute
wave: 8
depends_on: ["32.1-06", "32.1-07"]
files_modified:
  - scripts/board-readonly.test.ts
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-RED-baseline.txt
  - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-GREEN-proof.txt
autonomous: true
requirements: [DASH-06, DASH-08]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-10: a bare specifier that names a writer fires the acquisitions premise AND the builtin allow-list equality; each predicate is mutation-proven to red alone, so the write-detection mechanism is two predicates again rather than one — closing F-18."
    - "D-11 / DASH-06: a dynamic import whose argument is anything but a plain string literal is refused by a message CARRYING THE NODE KIND, and the two arms that previously answered that question into two different registers now answer it once."
    - "D-14: the runtime probe's boundary is MEASURED — what Node's own loader observes for this closure, and what a runtime probe over this corpus structurally cannot observe — so the waive reasons plan 11 writes are final sentences resting on a measurement."
    - "DASH-08: the dashboard closure still adds zero runtime dependencies — the manifest carries no dependencies key, and the case that reds if one appears is green."
  artifacts:
    - scripts/board-readonly.test.ts
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-RED-baseline.txt
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-GREEN-proof.txt
  key_links:
    - "The loader oracle from plan 01 is a RUNTIME authority over the same builtin identity set the static allow-list pins — two authorities over one question, obtained from different libraries."
    - "After plan 06's rewrite the tokenizer SEES a template-literal dynamic import, which is where D-11's node-kind refusal attaches; before that rewrite there was nothing for it to refuse."
  prohibitions:
    - "Never widen a parser by adding another arm where a canonical form is called for."
    - "Never let one register answer a question a second register also answers."
    - "Never type a count literal that should be derived from the set it counts."
    - "Never claim a runtime probe observed something it structurally cannot observe."
    - "Never fake a gate result, a test result, or a citation."
    - "Never run `npm test`."
---

<objective>
Close F-18 (ledger row 204) and finish D-11, then measure the boundary the runtime probe leaves for
D-14.

F-18 is a single-predicate defect. A widening moved several bare specifier spellings onto a path
where the acquisitions premise no longer fires, so the refusal is carried solely by the builtin
allow-list equality — one predicate where there were two. D-10's remedy is to restore the second
one, with each proven to red alone.

D-11 is smaller than it reads and its real content is arm reconciliation. One arm already refuses
EVERY dynamic import regardless of argument, which is stricter than D-11's canonical form asks for;
what is genuinely missing is the node kind in the message, and the fact that a non-literal argument
lands in one register from one arm and a different register from the other — two registers for one
question, which is the shape this phase exists to collapse.

Purpose: DASH-06's whole claim is that the dashboard's module tree holds no mutating write symbol,
proved mechanically rather than asserted in prose. A proof carried by one predicate is a proof one
widening away from being carried by none.
Output: two independent write-detection predicates each mutation-proven alone, one reconciled
dynamic-import register with node-kind refusals, and a measured statement of what a runtime probe
over this corpus can and cannot see.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-CONTEXT.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-RESEARCH.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-PATTERNS.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-01-SUMMARY.md
@.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Produced by THIS plan: a restored second write-detection predicate in the bare-specifier arm of
`scripts/board-readonly.test.ts`; a reconciled dynamic-import register with node-kind-carrying
refusal messages; a measured runtime-probe boundary statement written into
`32.1-08-GREEN-proof.txt` in the exact wording plan 11 will pass to the ledger's waive verb;
`32.1-08-RED-baseline.txt`. Ledger row 204 reaches its terminal disposition on this plan's evidence;
rows 184 and 208 get the measurement their waive reasons rest on. Every ledger row itself is written
by plan 11.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Two independent write-detection predicates, each proven to red alone</name>
  <files>scripts/board-readonly.test.ts, .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-RED-baseline.txt</files>
  <read_first>
    - `scripts/board-readonly.test.ts:507-518` — `noteSpecifier`, where the bare arm returns without pushing an acquisition while the foreign arm pushes one. This is the exact asymmetry F-18 names.
    - `scripts/board-readonly.test.ts:1405` — the acquisitions premise case and its message; `:1921` — the same message constant repeated for the witness table.
    - `scripts/board-readonly.test.ts:2404-2424` — the builtin allow-list, its three members with their per-member justifications, and its pinned count; `:2427-2429` — the identity normalizer that makes a prefixed and an unprefixed spelling one identity.
    - `scripts/board-readonly.test.ts:605` — the existing writer-detection helpers the restored predicate reuses.
    - `scripts/board-readonly.test.ts:2210-2212, :2550, :2582, :3408, :4135` — every site the allow-list equality is asserted at, including the relocation case that deliberately asserts one more than the pinned count.
    - `.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md` § 12, finding F-18 and its plants — the package-shaped bare specifier and the leading-whitespace absolute path, both recorded with the premise case NOT among the failures.
  </read_first>
  <action>
Restore the second predicate (D-10). In the bare-specifier arm of `noteSpecifier`, a specifier that
names a writer must push an acquisition, exactly as the foreign arm already does, so the acquisitions
premise fires for it. Today only the allow-list equality refuses those spellings, which is why F-18's
two plants exited 1 with the premise case NOT among the failures — the mechanism that detects a write
capability had been replaced by a census that happens to catch the same members.

Reuse the existing writer-detection and identity-normalizing helpers rather than writing a second
notion of what names a writer; a second notion is the class this phase closes.

Then prove the two predicates are independent, which is the whole content of D-10 and the part a
green suite does not establish. Four measurements, all from mirrors built from the live source:

1. Plant the package-shaped bare specifier F-18 names. Assert the acquisitions premise case is AMONG
   the failures — the column round 4 measured as absent.
2. Plant the leading-whitespace absolute-path spelling F-18 names. Same assertion.
3. Disable the allow-list equality in a mirror and re-run both plants: the premise must still red.
4. Disable the premise arm in a mirror and re-run both plants: the equality must still red.

Measurements 3 and 4 are what make "two predicates" a measurement rather than a description. Record
all four in `32.1-08-RED-baseline.txt`, together with the pre-change run of plants 1 and 2 showing
the premise case absent from the failures. Remove every mirror and confirm
`git status --porcelain -- scripts/` is empty.

Confirm the allow-list's pinned count did not move: the plants are writers arriving from outside, not
new admitted builtins, and a plan that moves that number has changed what the closure is allowed to
acquire rather than what the guard detects.
  </action>
  <acceptance_criteria>
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts` exits 0.
    - The bare-specifier arm pushes an acquisition for a specifier that names a writer, using the existing writer-detection helper.
    - `32.1-08-RED-baseline.txt` records, for both F-18 plants: the pre-change run with the acquisitions premise ABSENT from the failures, and the post-change run with it PRESENT.
    - `32.1-08-RED-baseline.txt` records both isolation runs — allow-list disabled with the premise still redding, and premise disabled with the equality still redding.
    - The builtin allow-list's pinned count is unchanged, and every site asserting the equality is still green.
    - `git status --porcelain -- scripts/` is empty after every mirror is removed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit — the read-only guard's own npm entry disagrees with the direct run</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit on any of the three tsc targets</fails_when>
  </verify>
  <done>The bare arm pushes an acquisition for a writer-naming specifier; both F-18 plants now fire the premise; each predicate is measured redding while the other is disabled; the allow-list count did not move.</done>
</task>

<task type="auto">
  <name>Task 2: One register for the dynamic-import question, and a refusal that carries the node kind</name>
  <files>scripts/board-readonly.test.ts</files>
  <read_first>
    - `scripts/board-readonly.test.ts:597-606` — arm A, the specifier-noting arm, which routes a non-literal argument to the opaque-specifier register.
    - `scripts/board-readonly.test.ts:1032-1037` — arm B, the acquisitions collector, which already refuses every dynamic import whatever its argument and pushes to the acquisitions register, printing the node's source text rather than its kind.
    - `scripts/board-readonly.test.ts:492` — the string-literal-like predicate used where a value is wanted.
    - `scripts/js-import-closure.ts` after plan 06 — the refuse-by-node-kind hook Task 1 of that plan left in place.
    - `32.1-RESEARCH.md` §D-11 in full, including the finding that the current state is STRICTER than D-11 asks for and that the real content is arm reconciliation.
  </read_first>
  <action>
Reconcile the two arms so the dynamic-import question has one answer in one register (D-11).

Today a dynamic import with a non-literal argument lands in the opaque-specifier register from one
arm and in the acquisitions register from the other. Two registers answering one question is the
shape this phase exists to collapse: a later reader fixing one of them leaves the other, and a
measurement taken from one is not a measurement of the question. Decide which register owns the
answer, route both arms through it, and delete the second route with a deletion note naming what it
used to hold and why the single register is now total for that question. State in the docblock which
question each surviving register answers, so the boundary is written rather than inferred.

Carry the node kind in the refusal message (D-11's genuinely missing half). A refusal that prints the
node's source text tells a reader what was written; a refusal that names the node KIND tells them
which shape was refused and therefore which shapes are admitted. The canonical form is a plain string
literal argument; anything else is refused with its kind named, and no alternative spelling is
admitted.

Do the same on the extractor side, using the hook plan 06 left: after that rewrite the tokenizer SEES
a template-literal dynamic import, so the refusal has something real to attach to for the first time.
Keep the two sides consistent — the same set of argument shapes is refused, with the same kind naming.

Discrimination, RED-first: three plants, each a dynamic import with a different non-literal argument
shape — an identifier, a template literal with a substitution, and a concatenation. Each must be
refused with ITS OWN node kind in the message, and the three messages must differ. Record all three
in `32.1-08-RED-baseline.txt` with the message each produced, plus the pre-change run showing which
register each landed in before the reconciliation. Restore and confirm the working tree is clean.
  </action>
  <acceptance_criteria>
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts` exits 0.
    - A non-literal dynamic-import argument reaches exactly ONE register, and the retired route carries a deletion note naming what it held.
    - Each of the three plant shapes is refused with its own node kind named in the message, and the three messages differ.
    - `32.1-08-RED-baseline.txt` records, per plant, which register it landed in before the reconciliation and which it lands in after, plus the refusal message.
    - The docblock states which question each surviving register answers.
    - `git status --porcelain -- scripts/` is empty after every plant is removed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>npm run check:dashboard-readonly</automated>
    <fails_when>non-zero exit — the read-only guard's own npm entry disagrees with the direct run</fails_when>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or "BUILD PARITY FAILED" on stderr</fails_when>
  </verify>
  <done>One register owns the dynamic-import answer, the other route is deleted with its note, three argument shapes are each refused by their own node kind, and both sides of the question agree.</done>
</task>

<task type="auto">
  <name>Task 3: What the runtime probe sees, and what it structurally cannot — measured, not asserted</name>
  <files>.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-GREEN-proof.txt</files>
  <read_first>
    - `scripts/loader-oracle.test-support.ts` and the oracle cases plan 01 added to `scripts/board-readonly.test.ts`.
    - `scripts/validate.test.ts:2678-2700` — the runtime-assembled key-spelling blind spot, which is ledger row 184's subject.
    - `.planning/WINDOWS.md` rows 184 and 208 — read both descriptions in full, because the waive reasons must answer what the rows actually say.
    - `32.1-RESEARCH.md` §D-14 in full — the ledger tooling's `waived` vocabulary, the one-shot nature of the waive verb, the mandatory non-empty reason, and the reconciliation gotcha about escaped table cells.
    - `.planning/phases/32-board-projector-cli-dashboard/deferred-items.md` — rows 184 and 208's re-measurement and remedy sentences.
  </read_first>
  <action>
Run the loader oracle against the dashboard closure in its post-rewrite state and record what it
actually observed: the resolve-event count, the module set, the builtin identity set, and the
working-tree equality. That is the part row 208's runtime probe CLOSES, and it is now a measurement
taken against the parser this phase installed rather than against the one round 4 found blind.

Then state, with the reason in each case, what a runtime probe over this corpus structurally cannot
observe. Three classes are named by rows 184 and 208 and each needs its own sentence:

1. A writer VALUE passed at run time — the module identity never enters the resolver, so no resolve
   hook can see it. The static acquisitions census is the authority for that class, and Task 1 just
   restored its second predicate.
2. A dependency that does not exist yet — a probe observes what this corpus loads today and says
   nothing about a specifier a future change introduces. The manifest's absent dependencies key and
   the case that reds if one appears are the standing control.
3. A key spelling assembled at run time — character-code construction, a substituted template, a name
   read from a variable. The static census sees expressions, not values, and the runtime probe
   observes module resolution, not string construction; neither authority covers it.

Write each of these as a FINAL sentence, ready to be passed verbatim to the ledger's waive verb by
plan 11. The waive verb is one-shot and overwrites the reason cell, so a sentence that is still being
drafted when it is written is a sentence that cannot be corrected. Put them in
`32.1-08-GREEN-proof.txt` under a clearly labelled heading, one sentence per ledger row, each naming
the row it belongs to.

Do NOT edit the ledger. Both of its representations are rendered by the tool from one set of entries
and its frontmatter counters are cross-checked against them, so a hand edit throws rather than
landing. Plan 11 owns every ledger mutation in this phase.

Finally, re-assert the zero-runtime-dependency property this phase must not move: the manifest has no
dependencies key, and the case that reds if one appears is green. Record the result alongside the
probe measurement.
  </action>
  <acceptance_criteria>
    - `32.1-08-GREEN-proof.txt` records the oracle's post-rewrite measurement: resolve-event count, module set, builtin identity set, and the working-tree equality result.
    - `32.1-08-GREEN-proof.txt` carries a labelled section with one FINAL waive-reason sentence per ledger row, each naming its row and each stating a structural reason rather than a schedule.
    - The three unobservable classes are each stated with the authority that does or does not cover them.
    - `git diff --exit-code -- .planning/WINDOWS.md` exits 0 — the ledger is untouched by this plan.
    - The manifest carries no dependencies key and the case that reds if one appears is green.
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts scripts/validate.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts scripts/validate.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the Test Files summary line</fails_when>
    <automated>git diff --exit-code -- .planning/WINDOWS.md</automated>
    <fails_when>non-zero exit — the ledger was hand-edited, which its counter cross-check refuses</fails_when>
    <automated>node -e "const p=require('./package.json'); if(Object.prototype.hasOwnProperty.call(p,'dependencies')) throw new Error('a dependencies key appeared'); console.log('no dependencies key')"</automated>
    <fails_when>non-zero exit, or the confirmation line is not printed</fails_when>
  </verify>
  <done>The runtime probe's reach is measured against the post-rewrite closure; its three structural blind spots are each stated with a reason and an authority; three final waive sentences are filed for plan 11; the ledger is untouched and the manifest still declares no runtime dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a module specifier → the dashboard's declared read-only module tree | a specifier is how a write capability would enter a tree that claims to hold none |
| Node's resolver → the runtime oracle's recorded set | the oracle's observations become evidence for a ledger waive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-08-01 | Tampering | a write capability reaching a declared read-only module tree | high | mitigate | Two independent predicates again: the acquisitions premise fires for a bare specifier naming a writer, and the builtin allow-list equality censuses every bare specifier. Each is measured redding while the other is disabled — a proof, not a description. |
| T-32.1-08-02 | Tampering | a module identity acquired by a shape no register decides | high | mitigate | One register owns the dynamic-import answer, the plain string literal is the canonical form, and every other argument shape is refused with its node kind named. The retired route carries a deletion note so it is not re-added as a good idea. |
| T-32.1-08-03 | Repudiation | a runtime probe credited with observing what it cannot | high | mitigate | The three structural blind spots are stated explicitly with the reason each is unobservable and the authority that does or does not cover it. The waive reasons are structural sentences, not schedule sentences, and they are written before the one-shot verb runs. |
| T-32.1-08-04 | Repudiation | a ledger row mutated outside its tool | medium | mitigate | `git diff --exit-code -- .planning/WINDOWS.md` is in this task's verify set; the ledger's counter cross-check would throw on a hand edit, and plan 11 owns every ledger mutation in this phase. |
| T-32.1-08-05 | Tampering | a runtime dependency arriving through the manifest | high | mitigate | The manifest's absent dependencies key is asserted directly in this plan's verify set, and the guard case that reds if one appears is in the suite this plan runs. |
| T-32.1-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package; `32.1-RESEARCH.md` § Package Legitimacy Audit records zero additions and zero `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts. |
</threat_model>

<verification>
1. `npx vitest run --exclude '**/scripts/e2e/**' scripts/board-readonly.test.ts scripts/validate.test.ts` — exits 0.
2. `npm run check:dashboard-readonly` — exits 0.
3. `npm run typecheck && npm run build && npm run check:build-parity` — exits 0.
4. `git diff --exit-code -- .planning/WINDOWS.md` — exits 0.
5. `npx vitest run --exclude '**/scripts/e2e/**'` — the full regression lane, exits 0.
</verification>

<success_criteria>
- F-18 closed with its two plants recorded before and after, and each predicate measured redding alone.
- One register, one canonical form, and a node-kind-carrying refusal for every other argument shape.
- The runtime probe's reach and its three structural blind spots are measured and written as final sentences.
- Zero runtime dependencies, re-asserted rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-08-SUMMARY.md` when done.
</output>

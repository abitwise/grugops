---
phase: 32.1-board-dashboard-deferred-residuals
plan: 11
subsystem: infra
tags: [ledger, broken-windows, completion-equality, erratum, governance, derived-denominator]

# Dependency graph
requires:
  - phase: 32.1-01
    provides: "the instrument set and residual R-32.1-01-A/B/C, three of the nine rows this plan appended"
  - phase: 32.1-02
    provides: "38 of the 78 disposition rows behind row 176's closure"
  - phase: 32.1-03
    provides: "the remaining 40 rows and check:diff-disposition at exit 0 — row 176's evidence"
  - phase: 32.1-04
    provides: "F-15, WR-04 and row 194 closed; row 210's re-home measurement (D-21)"
  - phase: 32.1-05
    provides: "F-19 and F-21 closed with their reproductions"
  - phase: 32.1-06
    provides: "F-16, F-17, WR-02 and WR-03 closed by deleting the second grammar"
  - phase: 32.1-07
    provides: "F-14 closed, and two named residuals this plan appended"
  - phase: 32.1-08
    provides: "F-18 closed, and the two FINAL waive sentences this plan passed verbatim to the one-shot verb"
  - phase: 32.1-09
    provides: "WR-01 and F-20 closed with a measured, non-overstated bound"
  - phase: 32.1-10
    provides: "the live lane's one run and its RED outcome — row 183's evidence — and the four register rows it deferred here"
provides:
  - "32.1-LEDGER.md — nineteen items from three named sources, one terminal disposition each, with the 19-versus-20-versus-18 reconciliation stated explicitly"
  - "32.1-ledger-check.mjs — the completion equality as a re-runnable derived check with a premise floor asked first, a failure message that NAMES the items, and a denominator derived TWICE"
  - "the ledger's status transitions: eleven rows fixed, two waived with the verbatim structural reasons, two deliberately left open as re-homed"
  - "nine appended register rows, including the three red live cases and the safety case among them"
  - "32-40-ADVERSARIAL-REVIEW-ERRATUM.md — row 209 closed beside a byte-unchanged original, declared a NEW convention on this tree"
  - "32.1-11-GREEN-proof.txt — the equality's output, the identifier reconciliation, the open-count movement, the three plants, and the phase's closing numbers beside its starting numbers"
affects: [verification, 33, /gsd-ship, any phase that cites the live lane]

actuals:
  tokens: 32258
  tasks: 3
  commits: 3
plan_head_before: ae001aefb81fe328211ce285861cc64b0cb4a53d

tech-stack:
  added: []
  patterns:
    - "A phase's completion is a COUNT EQUALITY over an enumerated ledger, run as that plan's gate rather than as a permanent suite case, because a permanent case reading a .planning/ document reds the suite on the commit that writes it"
    - "Derive a denominator TWICE — once from the document being checked and once from the source documents it was built from — because a vacuity floor catches an EMPTY denominator and never a SILENTLY SHORT one"
    - "A reconciliation between two counts is PERFORMED by the check (parse both, subtract the overlap) rather than asserted in the prose the check reads"
    - "re-homed-with-named-owner as a terminal disposition for an item a measurement proved unclosable by the phase's own remedy: a named owner plus the measurement, never a status the tooling does not have"

key-files:
  created:
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs
    - .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-GREEN-proof.txt
    - .planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW-ERRATUM.md
  modified:
    - .planning/WINDOWS.md

key-decisions:
  - "Row 183 is re-homed-with-named-owner on a RED, not closed. The lane ran once under a named human go and reported 1 failed file / 3 failed tests; the row's UNKNOWN - verify is answered and the answer is bad. closed-with-reproduction would claim a closure nobody can reproduce and waived-with-reason would claim a structural limit that does not exist — the instrument worked and returned a red."
  - "Rows 183 and 210 were deliberately NOT transitioned in .planning/WINDOWS.md. The tool's status vocabulary is exactly open/waived/fixed; there is no status for re-homing, and using a transition verb to invent one would record something the tooling does not mean. Both stay open by design and 32.1-LEDGER.md sections 3.1/3.2 carry the owners."
  - "The denominator is derived a SECOND time from the three SOURCE documents, not only from the ledger's own source column. Measured: with the column-derived sum alone, deleting an item row keeps the equality green at 18 = 18 while an item silently vanishes. The plan's plant-2 expectation was unmeetable as specified."
  - "The second derivation PERFORMS the nineteen-versus-twenty subtraction itself — eight bolded row ids from the phase boundary minus the one inside the findings' own id range (207 = F-21) — so the reconciliation is a measurement rather than a sentence in the document being checked."
  - "The plan's expected second re-home (the repository-wide spelling walk) is IN-05, which is NOT among the nineteen: D-22's third source is the review WARNINGS, and an IN- item carries no ledger row. It was given a register row rather than moved into the denominator, because moving a denominator to satisfy a sentence is the failure this ledger exists to prevent."
  - "Nine register rows appended rather than four. Plan 10 explicitly deferred four to this plan; the other five are named residuals plans 01 and 07 recorded in prose and no plan closed. A defect sitting in a SUMMARY is invisible at ship time; the register is what /gsd-ship reads."
  - "The row-208 waive reason was passed COMPOSED, as one sentence covering both structural halves, because the waive verb is one-shot and the row has one reason cell. Both reasons were read back and compared byte for byte against plan 08's proof."

patterns-established:
  - "A plant asserts it PLANTED SOMETHING (compare against the pre-image, exit non-zero on a no-op) before its RED is read as discrimination"
  - "Run the unplanted control against the same mirror first, so a plant's RED is attributable to the plant"
  - "When a plan's stated expectation and the measurement disagree, the measurement is recorded with the disproof and the plan's sentence is corrected in the evidence file"

requirements-completed: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07, DASH-08]

coverage:
  - id: D1
    description: "32.1-LEDGER.md enumerates nineteen items from three named sources, each attributed, each carrying exactly one terminal disposition spelled as the check reads it"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "node .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs — exit 0, PASS carrying 19/19, sources [ledger row 7, round-4 finding 8, round-4 review warning 4]"
        status: pass
      - kind: other
        ref: "the thirteen evidence-CELL paths each checked against disk after the erratum landed — MISSING: 0 (32.1-11-GREEN-proof.txt section 4.7)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The nineteen-versus-twenty-versus-eighteen reconciliation is stated explicitly, naming what each of the three counts was counting, and is DERIVED by the check rather than only asserted"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "the check's second denominator: 8 findings + 7 ledger rows (207 dropped as already counted at rows 200-207) + 4 warnings = 19, parsed from 32-40-ADVERSARIAL-REVIEW.md, 32-REVIEW.md and 32.1-CONTEXT.md"
        status: pass
    human_judgment: false
  - id: D3
    description: "Every ledger status transition was made by the tool's own verb; the diff shows the tool's rendering across both representations and the frontmatter counters, with no pre-existing description, kind, file or recorded_at touched"
    requirement: "DASH-08"
    verification:
      - kind: other
        ref: "32.1-11-GREEN-proof.txt section 1.5 — pre/post JSON arrays joined by id: 13 status changes, 2 reason changes, 9 additions, 0 removals, 0 descriptions touched"
        status: pass
      - kind: other
        ref: "verbatim round-trip: rows 184 and 208 reason bytes read back and compared against 32.1-08-GREEN-proof.txt section 3 — both true, 857 and 903 chars"
        status: pass
    human_judgment: false
  - id: D4
    description: "The two representations reconcile by ROW IDENTIFIER with a symmetric difference of zero, statuses agree per identifier, and the frontmatter counters re-derive from the entries"
    requirement: "DASH-08"
    verification:
      - kind: other
        ref: "32.1-11-GREEN-proof.txt section 2 — 219 = 219 identifiers, symmetric difference 0, per-identifier status agreement, counters agree; the positional and row-count alternatives refused by measurement (191 vs 193 over 4 escaped-separator rows)"
        status: pass
    human_judgment: false
  - id: D5
    description: "The completion check discriminates: three plants each red with the right message, each against a copy, with an unplanted control and a planted-something assertion"
    requirement: "DASH-02"
    verification:
      - kind: other
        ref: "32.1-11-GREEN-proof.txt section 6 — control exit 0; plant 1 exit 1 naming F-17 at line 95; plant 2 exit 1 on the second denominator; plant 3 exit 1 on the premise floor, first"
        status: pass
      - kind: other
        ref: "git diff --exit-code -- 32.1-LEDGER.md after all three plants — exit 0"
        status: pass
    human_judgment: false
  - id: D6
    description: "Row 209 closed by an erratum standing beside a byte-unchanged original, naming the five wrong attributions and the sixth correct use, with the correct commit measured three independent ways"
    requirement: "DASH-01"
    verification:
      - kind: other
        ref: "git diff --exit-code -- .planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW.md — exit 0"
        status: pass
      - kind: other
        ref: "git log -S splitReaderOffenders --reverse -- scripts/validate.test.ts — first hit 7a3ae592; git show --stat 7aea94f0 — three files, all under .planning/"
        status: pass
      - kind: other
        ref: "git ls-files | grep -i erratum — no output, so the erratum's declaration that it is a NEW convention is measured"
        status: pass
    human_judgment: false
  - id: D7
    description: "The open-count movement is recorded before and after with the governance effect named, and the phase closes green with its numbers beside its starting numbers"
    requirement: "DASH-08"
    verification:
      - kind: integration
        ref: "npx vitest run --exclude '**/scripts/e2e/**' — 75 files, 5224 passed | 2 skipped, exit 0, beside the phase's 5163 start"
        status: pass
      - kind: other
        ref: "11 of 11 check:* gates plus freshness and typecheck at exit 0 over a manifest-derived denominator of 33 script keys (32.1-11-GREEN-proof.txt section 7.2)"
        status: pass
    human_judgment: false
  - id: D8
    description: "Row 183's terminal disposition — whether re-homed-with-named-owner on a RED live lane is the honest call, and whether the named owner is adequate given that one of the three reds is a SAFETY case"
    verification: []
    human_judgment: true
    rationale: "32.1-10-SUMMARY.md handed this to a human by name: the three live reds' cause is not determined by that run, and no test can decide whether a named owner is the right response to an unobserved prod-deploy deny. The measurement is complete and the disposition is argued in 32.1-LEDGER.md section 3.1; whether it is the right disposition is a judgment about the phase's posture, not a property a check can assert."
  - id: D9
    description: "The substance of the nineteen dispositions — whether each closure's reproduction actually establishes what its row claims"
    verification: []
    human_judgment: true
    rationale: "The check measures that every item carries a terminal disposition and that every evidence path resolves. It cannot read the evidence. Whether 32.1-04-RED-baseline.txt task 1 really closes F-15 is a reading of a transcript, and D-17 makes that reading the verifier's job plus gsd-code-review rather than another adversarial round inside this phase."

# Metrics
duration: 24 min
completed: 2026-09-18
status: complete
---

# Phase 32.1 Plan 11: The Completion Equality Summary

**Phase 32.1 closes on a measurement rather than a declaration: nineteen items derived from three named sources, nineteen terminal dispositions, counted by a re-runnable check whose denominator is derived twice and whose three plants prove it discriminates — with the two items a measurement showed unclosable left as named owners, and the live lane's red carried into the register rather than smoothed away.**

## Performance

- **Duration:** 24 min
- **Started:** 2026-09-18T10:37:28Z
- **Completed:** 2026-09-18T11:01:09Z
- **Tasks:** 3
- **Files modified:** 5 (4 created, 1 modified)

## Accomplishments

- **Completion is a count equality a reader can re-derive.** `32.1-ledger-check.mjs` parses the ledger's item table, floors the parse above zero *before* asking the equality, asserts that every item carries one of exactly three terminal spellings, and derives the denominator from the table's source column. Its PASS line carries the measurement: `19 of 19 item(s) ... over a denominator of 19 derived from 3 named source(s) [ledger row 7, round-4 finding 8, round-4 review warning 4]. Dispositions: closed-with-reproduction 15, waived-with-reason 2, re-homed-with-named-owner 2.`
- **The denominator is derived a second time, from the sources themselves.** Step (3) alone is two readings of the same rows, so a deleted item row keeps it green at `18 = 18` — measured, not argued. Step (3b) re-derives the same nineteen from the three documents D-22 names, none of which is the ledger: the OPEN `### F-NN` headings of the round-4 review, the `### WR-NN:` headings of the code review, and the bolded row ids of the phase boundary **minus** the one that falls inside the findings' own id range. That subtraction *is* the nineteen-versus-twenty reconciliation, performed rather than read.
- **The three competing counts are reconciled, each with what it was counting.** Twenty is nineteen with F-21 counted twice (once as a finding, once as row 207's nested-runner half). Eighteen is `deferred-items.md` counting **homes**: it includes two Phase-33 items this phase does not own, gives F-21 a second home, and omits the four review warnings entirely because a warning has no ledger row to be homed to. `18 - 2 - 1 = 15`, and `15 + 4 = 19`.
- **Every ledger status moved through the tool's verb, and the diff proves it.** Eleven rows fixed, two waived, nine appended, zero removed — and a pre/post join by id shows **not one** pre-existing description, kind, file or `recorded_at` touched. The two waive reasons were read back and compared byte for byte against plan 08's proof; row 208 received the **composed** sentence its one reason cell needs.
- **Rows 183 and 210 were deliberately left `open`.** The tool's vocabulary is `open`/`waived`/`fixed`; there is no status for re-homing, and reaching for a transition verb to invent one would record something the tooling does not mean. Both carry their owner and their measurement in `32.1-LEDGER.md` § 3.
- **Row 183 is re-homed on a RED, and the ledger says so in the transcript's own words.** The live lane ran once under a named human go and reported `1 failed | 75 passed` files and `3 failed | 5227 passed | 2 skipped`. One of the three failures is the prod-deploy deny — a safety case. It is not closed, not waived, and not smoothed: it is four new register rows keyed to `scripts/e2e/uat-live.test.ts`.
- **The erratum stands beside a byte-unchanged original, and declares itself new.** Five wrong attributions located by line and section, the sixth correct use distinguished, the correct commit measured three ways (`git log -S` first hit, the named commit's zero source files, and the chronology), and the ratio explicitly unmoved. `git ls-files | grep -i erratum` returns nothing, so the erratum's own claim that it is a new convention is a measurement rather than a flourish.

## Task Commits

1. **Task 1: the nineteen-item ledger with a re-derivable denominator** — `dcd3a25f` (docs)
2. **Task 2: the ledger tool writes the statuses, and the erratum stands beside the original** — `3646ee11` (docs)
3. **Task 3: the completion equality, derived and re-runnable** — `40c60292` (test)

**Commits measured**, not narrated: `git rev-list --count ae001aef..HEAD` = 3 at SUMMARY-write time (the metadata commit follows this file).

## Files Created/Modified

- `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md` — the nineteen-item table with source, requirement, disposition and evidence; the three-way reconciliation; the two re-homed items with owners and measurements; D-17's posture; and an explicit "what this file does not claim".
- `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs` — the equality. Zero dependencies outside `node:fs`/`node:url`/`node:path`.
- `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-GREEN-proof.txt` — seven sections: the transitions and their measured scope, the identifier reconciliation, the open-count movement and its governance effect, the erratum's evidence, the equality's output, the three plants, and the closing numbers.
- `.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW-ERRATUM.md` — row 209's closure.
- `.planning/WINDOWS.md` — 11 fixed, 2 waived, 9 appended; `open 197 → 193`, `waived 0 → 2`, `fixed 13 → 24`, `total 210 → 219`.

## Decisions Made

See `key-decisions` in the frontmatter. The three a later reader most needs:

1. **A re-home is a disposition, not a deferral — and the tooling has no status for it.** Rows 183 and 210 read `open` in the register *on purpose*. Anyone auditing the register will see two open rows in a phase that claims completion; the phase ledger is where the owner and the measurement live, and § 3 of that file says this in as many words so the `open` is not read as an oversight.
2. **Deriving a denominator once is not deriving it.** The plan's step 3 and the item count are two readings of the same table. That was measured green at `18 = 18` with an item deleted before step (3b) existed. This is the class the repository has recorded before: a vacuity floor catches an empty denominator and never a silently short one.
3. **The denominator was not moved to satisfy a sentence.** The plan expected the repository-wide spelling walk (IN-05) to be a second re-homed item. It is an `IN-` item, outside D-22's three sources, with no ledger row. It got a register row instead. Growing an enumeration to make a prediction true is precisely the shape this ledger exists to refuse.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] The completion check needed a SECOND, independent denominator**

- **Found during:** Task 3 (running the plants)
- **Issue:** The plan's task 3 step 3 says to "derive the three source sizes from the ledger's own source column and assert their sum equals the item count", and its plant 2 says to "remove one item row entirely and confirm the source-sum assertion reds". Those two cannot both hold. Both sides of that equality are derived from the same rows, so deleting a row moves them together. Measured over plant 2's own mirror: `item rows: 18, column-derived source sum: 18` — the check would have exited **0** with an item silently gone. A completion check that stays green after an item is deleted is not a completion check, and writing the plan's sentence down would have been a fabricated measurement.
- **Fix:** Step (3b): the denominator is derived again from the three documents D-22 names — the OPEN `### F-NN` headings of `32-40-ADVERSARIAL-REVIEW.md`, the `### WR-NN:` headings of `32-REVIEW.md`, and the bolded row ids of `32.1-CONTEXT.md`'s phase boundary minus those inside the findings' own id range. None of the three is the document being checked. The failure message states the independence and why it is there.
- **Files modified:** `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs`
- **Verification:** plant 2 now exits 1 naming `19 item(s) but the table holds 18`; the measured step-(3)-alone verdict is recorded in `32.1-11-GREEN-proof.txt` § 6.2 beside it.
- **Committed in:** `40c60292` (Task 3 commit)

**2. [Rule 2 - Missing Critical] Nine register rows appended, where the plan's action names only transitions**

- **Found during:** Task 2
- **Issue:** `32.1-10-SUMMARY.md` states outright that every broken-windows entry its run would otherwise have appended — the three red live cases and the stale `uat-live.test.ts` docblock — was "deliberately left for plan 11 to write". The plan's task 2 action describes transitions only. Left undone, the three live reds (one of them a safety case) and four further residuals plans 01 and 07 named in prose would exist only inside SUMMARY documents. `/gsd-ship` reads `open_count`, not a SUMMARY; a defect recorded in prose is a defect absent from the register.
- **Fix:** Nine rows appended through `gsd-tools windows append` — ids 211-219: the three live reds, the stale docblock, plan 01's three carried residuals (`R-32.1-01-A/B/C`), and plan 07's two named residuals. Each names its evidence, its measurement and, where it has one, its owner.
- **Files modified:** `.planning/WINDOWS.md`
- **Verification:** ids and descriptions listed in `32.1-11-GREEN-proof.txt` § 1.4; the append is visible in the pre/post join as `rows added: [211..219]` with zero removals; the open-count movement is attributed in § 3.
- **Committed in:** `3646ee11` (Task 2 commit)

### Honest corrections to the plan's own wording

Neither changed what was built; both are recorded because the plan's text would mislead a later reader.

1. **The plan's expected second re-home does not exist among the nineteen.** Task 1 names "the review information item about a repository-wide spelling walk" as an expected `re-homed-with-named-owner`. That is **IN-05**, from `32-REVIEW.md` § Info. D-22's three sources are the eight findings, the seven ledger row identifiers and the four review **warnings**; `32.1-CONTEXT.md`'s phase boundary names `WR-01..WR-04` only, and an `IN-` item carries no ledger row. The plan's own instruction — "confirm both against the summaries rather than carrying them from this sentence" — is what caught it. It is recorded in `32.1-LEDGER.md` § 3.3 and given register row **215** instead of being folded into the denominator. The summaries were then checked for a genuine third re-home among the nineteen and support none.
2. **Row 209's evidence cell was a forward reference at task 1's acceptance gate.** The ledger's evidence-path check reported one pending path — the erratum, which task 2 of this same plan had not yet written. It was labelled as such in the cell, it was the only one, and it resolves after task 2. Recorded in `32.1-11-GREEN-proof.txt` § 4.7 rather than hidden by re-running the check only at the end.

---

**Total deviations:** 2 auto-fixed (2 missing critical) + 2 documented corrections.
**Impact on plan:** No scope creep. The first auto-fix is what makes the plan's own plant 2 meaningful; the second discharges a handoff a prior plan named explicitly. Nothing was widened to make a gate pass, no evidence document was edited, and no count was typed where one could be derived.

## Known Stubs

None. No placeholder value, `TODO` or `FIXME` was introduced, and no test was skipped — the lane carries the same two pre-existing skips it carried at phase start.

## Issues Encountered

**1. The register's `open_count` fell by four, not by thirteen — and that is the honest number.** Eleven fixes and two waives lowered it; nine appends raised it. `197 → 193`. The phase closed thirteen rows and opened nine, and it does not claim to have drained the register. `workflow.windows_enforce` is `false` on this tree, so no gate is currently reading that number — which is the reason the movement is written into the proof rather than left for a gate to surface.

**2. Two rows in a completed phase read `open`, and a reader will notice.** Rows 183 and 210 are terminal under D-16 and `open` in the register, because the tool has three statuses and re-homing is not one of them. The alternative — marking them `fixed` — would have been the fabrication this phase exists to refuse.

## Threat Flags

None. No network endpoint, auth path, file-access pattern or schema change at a trust boundary was introduced. The plan's threat register is mitigated on this tree: completion is a derived equality with a floored premise and a naming failure message, proven by three plants (T-32.1-11-01); every transition ran through the tool's verb with the mutation scope measured (T-32.1-11-02); the reconciliation is by row identifier with the positional alternative refused by measurement (T-32.1-11-03); the erratum stands beside a `git diff --exit-code`-asserted byte-unchanged original and declares itself new (T-32.1-11-04); both waive reasons are the structural sentences plan 08 measured, round-tripped byte for byte (T-32.1-11-05); the open-count movement is recorded before and after with the governance effect named (T-32.1-11-06); and no package was installed — the check depends on nothing outside Node's standard library (T-32.1-11-SC).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **The phase's completion artefact exists and is re-runnable.** A verifier should run `node .planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs` rather than read a number out of a sentence. It exits 0 and prints what it measured.
- **Two things a verifier must NOT read as completion.** Row 183 is re-homed on a RED live lane whose three failures include an unobserved prod-deploy deny (register rows 211-213), and rows 184 and 208 are waived — a waive records a structural limit and leaves the gap in the register with a reason a reader may disagree with.
- **The four-round gap-closure cap is untouched.** D-17 means no adversarial round ran inside this phase, so the cap is fully available to whatever gap-closure the verifier's pass calls for.
- **Do not hand-flip the phase to Complete.** `roadmap.update-plan-progress` computes the row from the plan/summary counts on disk; phase verification is the orchestrator's next step and this plan makes no claim about its outcome.
- **The register is at `open 193 / waived 2 / fixed 24 / total 219`**, and nine of those open rows were opened by this plan on purpose.

## Self-Check: PASSED

Created files verified present on disk:

- FOUND: `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-LEDGER.md`
- FOUND: `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-ledger-check.mjs`
- FOUND: `.planning/phases/32.1-board-dashboard-deferred-residuals/32.1-11-GREEN-proof.txt`
- FOUND: `.planning/phases/32-board-projector-cli-dashboard/32-40-ADVERSARIAL-REVIEW-ERRATUM.md`
- FOUND (modified): `.planning/WINDOWS.md`

Commits verified present in `git log`:

- FOUND: `dcd3a25f`, `3646ee11`, `40c60292`

All three tasks' `<acceptance_criteria>` re-run and passing. The plan-level `<verification>` block re-run in order on the finished tree:

1. `node .../32.1-ledger-check.mjs` — exit 0, PASS line carrying 19/19 and both denominators
2. `git diff --exit-code -- .../32-40-ADVERSARIAL-REVIEW.md` — exit 0
3. `npm run check:nul-bytes && npm run check:diff-disposition && npm run check:build-parity` — all exit 0
4. `npx vitest run --exclude '**/scripts/e2e/**'` — exit 0, 75 files / 5224 passed / 2 skipped
5. `32.1-11-GREEN-proof.txt` carries the equality's output (§ 5), the identifier reconciliation (§ 2), the open-count movement (§ 3), and the closing counts beside plan 01's starting counts (§ 7.1)

`commits: 3` MEASURED via `git rev-list --count ae001aef..HEAD`, not narrated. No file deleted across the plan: `git diff --diff-filter=D --name-only ae001aef..HEAD` is empty.

---
*Phase: 32.1-board-dashboard-deferred-residuals*
*Completed: 2026-09-18*

---
phase: 31-autonomous-manual-testing
plan: 23
type: execute
wave: 3
depends_on: ["31-22"]
gap_closure: true
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/context-io-writer-set.test.ts
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
  - agent-factory/workflows/16-context-read-write.md
  - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
autonomous: true
requirements: [UATX-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-26: the home boundary bounds ASCENT, never INSPECTION. The walk inspects `dir` FIRST and asks the home question about whether to CLIMB. Split into two predicates: `isAboveHome` (strictly an ancestor of the home directory) refuses inspection outright, and `isHomeItself` permits exactly ONE inspection and then ends the walk. The canonical form for what the home directory may answer is narrow and stated in THREE conjuncts: home is adopted ONLY when it carries a version-control boundary marker, AND the candidate that carries its governance configuration is the REPOSITORY STATE-PLANE position rather than the IN-KIT position, AND that candidate is not one of the RUNNING MODULE'S OWN fallback candidate positions. Home never becomes `nearest` under any of the three failing."
    - "THE SECOND AND THIRD CONJUNCTS DECIDE BY POSITION AND BY A MODULE CONSTANT, AND THEY READ THE FILESYSTEM NOT ONCE. That is the whole content of this revision. The previous draft of this plan decided kit-ownership with two `existsSync` probes under `$HOME/.grugops`, and an adversarial re-check showed both were caller-authorable in ONE operation each, in OPPOSITE directions: creating `<kitHome>/agent-factory` turned an ADOPTION into a REFUSAL — restoring CR-13's own measured harm (`31-VERIFICATION.md` behavioral spot-check row 8) by one `mkdir`, because a refusal at home returns `nearest`, and with nothing remembered `trustedRepoRoot()` falls to `GOVERNANCE_FALLBACK_BASE` whose shipped dial is LEAN — while creating `<kitHome>/install.json` turned a REFUSAL into an ADOPTION. A conjunct a caller can flip in either direction with one filesystem operation is not a constraint on the caller; it is a switch this plan would have handed it. The rule is therefore rebuilt on inputs no artifact under `$HOME` or under the repository can author."
    - "WHY THE HOME CASE STILL NEEDS MORE THAN MARKER-PLUS-CONFIGURATION, stated as the collision rather than as a verdict. `governanceConfigCandidates` publishes TWO positions, and at `$HOME` they are `$HOME/.grugops/factory.config.json` — a repository's seeded STATE PLANE, which is what `seedState` writes for a TARGET — and `$HOME/agent-factory/config/factory.config.json`, which is A KIT'S OWN configuration: the second published candidate, the file every vendored copy of this kit carries, and the exact position D-23 (4) already ruled must lose to a repository root's own. At every ordinary directory D-23 (4)'s boundary-wins rule keeps a kit's configuration from governing a host, because there is a boundary ABOVE it to lose to. At `$HOME` there is none — the walk ends there. So the in-kit position is excluded at home BY POSITION, and the running module's own fallback positions are excluded BY PATH EQUALITY; otherwise a kit whose root is the home directory has its shipped lean default outrank a configured project nested below it, which is WR-21's harm at the one position the boundary rule cannot reach."
    - "`MODULE_OWN_CONFIG_POSITIONS` is a FROZEN MODULE CONSTANT: `governanceConfigCandidates(GOVERNANCE_FALLBACK_BASE)`, lexically resolved, computed once at load. `GOVERNANCE_FALLBACK_BASE` is `join(import.meta.dirname, '..')` — a property of WHICH PROGRAM IS RUNNING, not of the filesystem that program inspects. No `mkdir`, no `touch`, no write under `$HOME` or under the repository moves it, and no environment variable is read to build it. Moving it at all means running a DIFFERENT copy of the decider, which is the capability `DECIDER_MANIFEST` in `hooks/hook-entry.ts` already freezes and which this predicate is not the tier for. `process.env.GRUGOPS_HOME` is deliberately NOT read: it is caller-settable in ZERO operations, so consulting it would reintroduce the same flip the two existence probes did, one register over."
    - "THE PREVIOUS DRAFT'S 'THE SET MAY ONLY DISQUALIFY, NEVER QUALIFY' CLAIM IS RETRACTED RATHER THAN INHERITED, because it was false in both halves. False in fact: the `install.json` arm QUALIFIED a position the other conjuncts refused. False in principle: a disqualification at home is NOT the safe direction, because refusing home returns `nearest`, and when nothing was remembered the caller lands on `GOVERNANCE_FALLBACK_BASE`, whose dial is the shipped lean default — so an added refusal lowers a gate exactly as an added adoption can. WR-15's monotone-in-the-safe-direction argument was written about the step-4 fallback and does not transfer to a walk whose refusal LANDS on that fallback. This plan states the correct version rather than carrying the convenient one, and every case below asserts invariance in BOTH directions rather than monotonicity in one."
    - "THE INSTALLER'S OWN MARKER CANNOT CARRY THIS WEIGHT, AND THAT IS MEASURED FROM `install/install.ts` RATHER THAN ASSUMED. `writeMarker` (`install/install.ts:2226`) writes four fields — `kitVersion`, `grugopsHome`, `kitRoot`, `installMode` — and NAMES NO TARGET, so there is nothing in it to compare a target against. `readMarker` (`:609`) is fail-closed against a throw but not against a forgery: its `InstallMarker` interface makes all four fields OPTIONAL and it accepts any non-array object, so the two-byte document `{}` parses to a valid marker. A caller that can create a file under `$HOME/.grugops` therefore satisfies an existence test and a schema test alike, in one write. The marker is consequently read NOT AT ALL by this rule, and a case asserts the verdict unchanged with it present, absent, and present-but-empty."
    - "A SECOND MEASURED FACT the previous draft assumed the other way: the shipped kit carries no `scripts/` directory — `agent-factory/` holds `roles/`, `workflows/`, `checklists/`, `contracts/`, `config/`, `packaging/`, `seed/`, `VERSION` and two markdown files — so an installed shared kit at `$GRUGOPS_HOME/agent-factory` does not contain this module at all. `GOVERNANCE_FALLBACK_BASE` is therefore always a CHECKOUT root and never `$GRUGOPS_HOME/agent-factory`, which makes the previous draft's third derivation arm (`dirname(GOVERNANCE_FALLBACK_BASE)` when its basename is `agent-factory`) a derivation over a shape the installer never creates. MOVEMENT 0 records the shipped layout from an `ls` of the kit rather than from a second guess about it."
    - "What the shipped installer ACTUALLY places at each root is MEASURED from `install/install.ts` rather than assumed, because the previous draft of this plan asserted that `~/.grugops/factory.config.json` is 'exactly the shipped shared-install kit shape' and the installer does not say that: `copyKit` writes only `$GRUGOPS_HOME/agent-factory` (`KIT_ROOT`), while `seedState`/`writeMarker` write `$TARGET/.grugops/factory.config.json` and `$TARGET/.grugops/install.json`. The two collide only when `TARGET === $HOME`. The plan states the measured layout, and the truths below say what is measured rather than what was inherited — a claim that outran its mechanism is corrected in this repository, not carried."
    - "CR-13, the blocker this plan exists for: `31-19`'s WR-21 fix evaluates `isAtOrAboveHome(dir, home)` BEFORE `dir` is inspected for a configuration or a marker, at every step of the walk. A repository whose ROOT IS the user's home directory — a dotfiles checkout, or an agent run at `$HOME` — therefore never has its own `.git` marker or `.grugops/factory.config.json` read at all. `31-VERIFICATION.md` behavioral spot-check row 8 reproduced it: with `HOME` pointed at a planted repository root carrying `.git` and `human_admission: high-severity`, `trustedRepoRoot()` returned the kit and the dial read `off`; the CONTROL at row 9 — the identical tree one level below the real home — resolved to the project and read `high-severity`."
    - "This is the WR-15 defect direction reopened: a configuration moving from refused to admitted. The same file already rejects `.grugops` as a boundary marker on exactly that ground, and on the four non-Claude-Code hosts D-12 names, the in-script refusal is the ONLY tier available, so a skipped dial is a skipped gate."
    - "The review's own `Fix:` sketch is NOT adopted verbatim, and this plan says why: the sketch sets `nearest = dir` before asking `isHome` and then returns `nearest`, so a home directory carrying only a configuration would be ADOPTED on the way past, which is verbatim the WR-21 hole `31-19` was convened to close. The adopted rule is strictly narrower on three counts: at the home directory the walk requires a boundary MARKER as well as a configuration, it requires that configuration not to be the shared-install kit's own position, and home never contributes to `nearest`."
    - "The stop set stays ONE published authority and the prose is quoted from it under the existing both-directions equality: `TRUSTED_ROOT_STOP_CONDITIONS` gains the asymmetry as named members and `agent-factory/workflows/16-context-read-write.md`'s quoted list moves with it, so a prose sentence cannot again outrun the mechanism it describes."
    - "The residual register gains `R-31-19-05` with all four documented fields, naming the shape this plan deliberately does not adopt — a repository rooted at home that carries a configuration but NO version-control marker — with what would force it closed stated: an explicit opt-in the walk can read that a caller cannot author. Explicitly NOT the installer's own marker, and the register says so, because `install/install.ts:609`'s schema makes that marker forgeable in one write. The register's set-equality assertion means the member arrives bound rather than as a silence."
    - "`R-31-19-06` names the PRICE of the two evidence conjuncts rather than a refusal, and it prices them in operations rather than in adjectives. A process that can write under `$HOME` can MAKE the home directory adoptable in THREE operations against a bare home — `mkdir $HOME/.git`; `mkdir $HOME/.grugops`; write `$HOME/.grugops/factory.config.json` — and in ONE against a home that already carries a dotfiles checkout. The converse is a ONE-operation gate LOWERING that predates this plan and is measured here rather than discovered next round: `mkdir $HOME/work/.git` ends the walk at an intermediate boundary carrying no configuration, so the answer degrades to `GOVERNANCE_FALLBACK_BASE`'s lean default. Neither capability is introduced by this plan, both are the ordinary walk's own evidence applied identically at every directory, and both sit inside the class `R-31-15-01` already accepts and `31-22`'s rewritten `T-31-18-01` prices the same way. What would force them closed is `R-31-15-01`'s own criterion — a governance root the calling process cannot influence at all — and nothing narrower."
    - "`R-31-19-07` names the one gap the module-constant route leaves that the retracted existence-probe route did not: the exclusion compares LEXICALLY RESOLVED PATH SPELLINGS, so on a case-insensitive or symlinked filesystem the module's own candidate position can be spelled differently from the candidate the walk computes and the exclusion misses. The alternative — comparing `dev:ino` identities the way `homeBoundary` does — is a `statSync` on paths under `$HOME` that a SINGLE symlink can make agree, which is a one-operation flip in the gate-lowering direction and therefore the defect this member exists instead of. The decision is recorded with its criterion, not taken silently. The member is OCCUPIED on EVERY axis its own shape sentence names — the case-insensitive spelling AND the symlinked one, the latter being where Node's symlink-resolved `import.meta.dirname` diverges from the walk's purely lexical `resolve()` at `scripts/context-io.ts:3056` — each driven on this filesystem and priced, because a residual measured on one of the two axes it claims is a residual half tested."
    - "WR-21's own closure is re-measured INTACT rather than assumed: a planted home-directory-shaped ancestor carrying only `.grugops/factory.config.json`, with `HOME` set so that ancestor IS `os.homedir()`, still resolves to the kit — `31-VERIFICATION.md`'s round-4 measurement re-driven on this tree — and `R-31-19-01`'s below-home shape (verification row 9) still resolves to the project, unchanged."
    - "THE TWO ARTIFACTS THE RE-CHECK NAMED ARE DRIVEN VERBATIM, AS INVARIANCE CASES, IN BOTH DIRECTIONS. Artifact (a): CR-13's own adopted tree, then `mkdir -p $HOME/.grugops/agent-factory` — the verdict must be UNCHANGED (still `$HOME`, still `high-severity`, the self-stamped high-severity finding still REFUSED). Artifact (b): the dotfiles-plus-shared-install tree (`$HOME/.git` present, `$HOME/.grugops/agent-factory` present, `$HOME/.grugops/factory.config.json` carrying the lean dial, cwd `$HOME/work/proj` carrying neither marker nor configuration, both project-directory variables removed), then `touch $HOME/.grugops/install.json`, and again with that file holding the schema-valid `{}` — the verdict must be UNCHANGED across all three. A third case sweeps `GRUGOPS_HOME` unset, redirected, empty and set to `$HOME` and asserts the verdict unchanged across all four, because the rule reads no environment variable. Together these are the blocker's own property asserted as a measurement: NO artifact a caller can create in one operation moves the home verdict in EITHER direction."
    - "The dotfiles-plus-shared-install tree's DECIDED verdict is stated plainly rather than left implied: it is ADOPTED, because `$HOME` carries a version-control marker and a state-plane configuration and neither is this module's own position, which is the identical evidence and the identical answer the walk gives for that tree one level BELOW home (`R-31-19-01`, verification row 9). Its cost is `R-31-19-06`'s priced construction, not a separate hole — and it is measured, not argued, that a default shared install creates NEITHER of those two artifacts at `$HOME`: `copyKit` writes only `$GRUGOPS_HOME/agent-factory`, and `seedState`/`writeMarker` write under `$TARGET/.grugops` only, which is `$HOME` exactly when the user installed INTO home, the case that must be adopted."
    - "The walk stays BOUNDED and MONOTONE: the ancestor step limit is kept, the degenerate-inode premise check is kept and now applies to both predicates, and the whole existing case set is driven once against the pre-fix program and once against the post-fix program with no case moving from refused to admitted. That sweep's set is the CONFIGURATION-RESOLUTION corpus predating this round; the cross-plan ORIGIN-RECOGNITION cases `31-22` added at wave 2 are PROBE 5's subject and are governed by its declared intended-change list, whose single member is `31-22` CONTROL 5a. The two sets are stated separately so the sweep's blanket claim cannot silently forbid the one movement this plan declares."
    - "Every case is RED FIRST against the committed `.js` at HEAD before any source change — the verifier's own row-8 probe, a child process with `HOME` set to the planted repository root and BOTH project-directory variables genuinely REMOVED (not set empty) — and MUTATION-PROVEN afterwards."
    - "The frozen floors are untouched: `hooks/guard.ts` stays byte-identical to `FROZEN_GUARD_BLOB`, and the `DECIDER_MANIFEST` entries for `scripts/context-io.js` are MOVED to the re-measured digest rather than relaxed."
  artifacts:
    - scripts/context-io.ts
    - scripts/context-io.js
    - scripts/context-io.test.ts
    - scripts/context-io-writer-set.test.ts
    - hooks/hook-entry.ts
    - hooks/hook-entry.js
    - agent-factory/workflows/16-context-read-write.md
    - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
  key_links:
    - "`projectRootFromWorkingDirectory`'s walk -> `isAtOrAboveHome`: recorded MISWIRED by the round-5 verifier's key-link table (evaluated first, before any inspection of `dir`). This plan splits the predicate so the stop bounds ascent and never bounds inspection."
    - "the home directory carrying a configuration but NO marker <-> the shared-install kit at `~/.grugops`: the collision that makes the naive asymmetry reopen WR-21, and the reason the adopted rule demands a marker at home."
    - "`governanceConfigCandidates`'s SECOND published position (`<dir>/agent-factory/config/factory.config.json`) <-> D-23 (4)'s boundary-wins rule: at every ordinary directory that rule keeps a kit's own configuration from governing a host, because a boundary above it wins. At `$HOME` the walk ENDS, so there is no boundary above to lose to, and the in-kit position must be excluded by POSITION instead. This is the link the round-5 adversarial re-check found missing, and it is closed without reading the filesystem once."
    - "`GOVERNANCE_FALLBACK_BASE` (`join(import.meta.dirname, '..')`) <-> `MODULE_OWN_CONFIG_POSITIONS`: the running module's own two candidate positions, a load-time constant of WHICH PROGRAM IS RUNNING rather than of the filesystem it inspects. This is the only input in the home rule a caller cannot author with a `mkdir`, a `touch` or an environment variable, and it is why the third conjunct is a path equality rather than an existence probe."
    - "an existence probe under `$HOME` <-> a one-operation verdict flip: `existsSync(join(kitHome, 'agent-factory'))` and `existsSync(join(kitHome, 'install.json'))` are both writable by the calling agent, in opposite directions, which is the retracted design and the reason no filesystem probe decides kit-ownership. `install/install.ts:609`'s all-optional `InstallMarker` schema means reading the marker through its parser is the same one-write flip, so the schema route is refused too."
    - "`TRUSTED_ROOT_STOP_CONDITIONS` <-> `agent-factory/workflows/16-context-read-write.md`'s quoted list: the both-directions equality that stops the prose drifting from the mechanism, which is exactly how WR-21 came to be documented as impossible."
    - "`trustedRepoRoot()` -> `appendAuditLedger`'s write location: the consequence that turns a wrong root into another repository's committed audit trail."
    - "`scripts/context-io.js`'s committed bytes <-> `DECIDER_MANIFEST` in `hooks/hook-entry.ts`: the frozen hash that must MOVE with the artifact, never be relaxed."
  prohibitions:
    - statement: "The resolution order must never adopt a root at or above the user's home directory that it has no independent reason to trust. The home directory answers only as a repository — a version-control marker AND its own configuration AND that configuration not being the shared-install kit's own position — never as a bare configuration position, and never on the strength of a configuration the kit put there."
      status: unverified
      verification: flagged
    - statement: "A bound on a search must bound the search, not the observation. Refusing to inspect a directory the walk has legitimately reached is how a configuration moves from refused to admitted."
      status: unverified
      verification: flagged
    - statement: "A caller must never be able to point an admission at a governance root it chose as an argument. The order may read the ambient environment and the working directory; no parameter and no note field may select the root."
      status: unverified
      verification: flagged
    - statement: "A published stop condition must be true of the walk. A stop the prose names and the code lacks is a fabricated bound."
      status: unverified
      verification: flagged
    - statement: "`hooks/guard.ts` is not touched and `FROZEN_GUARD_BLOB` is never re-based."
      status: unverified
      verification: flagged
    - statement: "Never merge a protected branch and never deploy to production without a named human confirmation."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-19-PLAN.md / D-23: the walk halts at the user's home directory and never inspects it or anything above it. This plan CHANGES that prohibition deliberately — the halt stays, the blanket non-inspection does not — and records the change as a decision beside D-23 rather than as a silent edit."
    - "31-15-PLAN.md / WR-15: a configuration must never move from refused to admitted. CR-13 is that direction reopened, and this plan's monotonicity sweep is the proof it is not reopened again."
---

<objective>
Close **CR-13** of `31-REVIEW.md`, independently reproduced by `31-VERIFICATION.md` round 5 as
behavioral spot-check row 8 with its control at row 9.

Round 4's `31-19` closed WR-21 — an unbounded upward governance-root walk that adopted a
home-directory-shaped ancestor — by halting the search at the user's home directory. That closure is
real and is independently re-verified. What it cost is CR-13: the halt is asked BEFORE the directory is
inspected, at every step. So a repository whose root IS the home directory has its own `.git` and its
own `.grugops/factory.config.json` skipped entirely, and `trustedRepoRoot()` falls through to the kit,
whose shipped dial is lean. Measured: with `HOME` set to a planted repository root carrying `.git` and
`human_admission: high-severity`, the resolution returned the kit and the dial read `off`; the identical
tree one level below the real home read `high-severity`. The two runs differ in nothing but whether the
repository root is the home directory.

That is the WR-15 verdict direction exactly — **a configuration moving from refused to admitted** — and
this same file already rejects `.grugops` as a boundary marker on precisely that ground. On the four
hosts D-12 names as having no attended lane, the in-script refusal is the only tier there is.

**The review's fix sketch is not adopted verbatim, and this plan states the reason in the source.** The
sketch assigns `nearest = dir` before asking whether `dir` is home and then returns `nearest`, so a home
directory carrying only a configuration would be adopted on the way past. That is WR-21's own hole,
restored. The adopted rule is strictly narrower: at the home directory the walk demands a version-control
boundary marker as well as a configuration, and home never becomes `nearest`.

**And a marker-plus-configuration rule is still not enough — but the fix for that must not itself be a
switch the caller can throw, which a second adversarial re-check of this plan established before it
executed.** `governanceConfigCandidates` publishes TWO positions, and at `$HOME` the second is
`$HOME/agent-factory/config/factory.config.json`: a KIT's own configuration, the file every vendored copy
of this kit carries, and the exact position D-23 (4) already ruled must lose to a repository root's own.
Everywhere else that rule holds because a boundary ABOVE the kit wins. At `$HOME` the walk ends, so
nothing wins, and a marker-plus-configuration rule lets a kit's shipped lean default govern every
un-configured directory below home.

**The draft that went to review closed that with two `existsSync` probes under `$HOME/.grugops`, and both
were caller-authorable in one operation, in opposite directions.** `mkdir -p $HOME/.grugops/agent-factory`
made the position "kit-owned", refused home, and dropped the answer to `GOVERNANCE_FALLBACK_BASE`'s lean
dial — CR-13's own measured harm restored by one `mkdir`. `touch $HOME/.grugops/install.json`
re-qualified the position and adopted home. Reading that marker through `install/install.ts:609`'s
`readMarker` does not help: all four `InstallMarker` fields are optional, the interface names no TARGET,
and `{}` parses as valid — so the schema test is the same one-write flip as the existence test.

**So the two added conjuncts decide by POSITION and by a MODULE CONSTANT, and read the filesystem not
once.** Home is adopted only when it carries a version-control marker, AND the candidate carrying its
configuration is the repository STATE-PLANE position rather than the IN-KIT position, AND that candidate
is not a member of `MODULE_OWN_CONFIG_POSITIONS` — the running module's own two fallback candidates,
frozen at load from `join(import.meta.dirname, "..")`. That constant is a property of which program is
running, not of the filesystem it inspects; no `mkdir`, no `touch`, no write under `$HOME` or under the
repository, and no environment variable moves it. The two remaining conjuncts are the walk's ORDINARY
evidence, applied identically at every directory, and their price is stated in operations as
`R-31-19-06` rather than left as an adjective.

Purpose: make the bound bound ASCENT rather than OBSERVATION, without reopening the hole the bound was
added to close, without letting a kit's own configuration govern a project, and without handing the
caller a one-operation switch over any of it.

Output: a split `isAboveHome` / `isHomeItself` pair; a published `GOVERNANCE_CONFIG_CANDIDATE_KINDS`
classification bound to `governanceConfigCandidates` in both directions; a frozen
`MODULE_OWN_CONFIG_POSITIONS` constant; an inspect-then-decide walk whose home case requires a marker, a
state-plane configuration position and a position that is not the module's own; two new published stop
conditions and the workflow list moved with them; residuals `R-31-19-05`, `R-31-19-06` and `R-31-19-07`;
invariance cases driving the two named artifacts and the environment variable in both directions; a
monotonicity sweep and a re-measurement of both WR-21 halves; and decision **D-26** appended to
`31-CONTEXT.md`.

**Ordering note:** wave 3, `depends_on: ["31-22"]`. This plan and `31-21`/`31-22` all modify
`scripts/context-io.ts`, `scripts/context-io.js`, `scripts/context-io-writer-set.test.ts`,
`hooks/hook-entry.ts`, `hooks/hook-entry.js` and `31-CONTEXT.md`. Sequencing removes the conflict and
lets this plan's monotonicity sweep run against a tree where the origin narrowing already exists, so a
change in ROOT RESOLUTION cannot be confused with a change in what the promotion route decides.

**Assumption delta:** `no-change`. Both detector hits are already decided in `31-CONTEXT.md` (D-14,
D-21); no identity-model question is open for this plan.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@.planning/phases/31-autonomous-manual-testing/31-REVIEW.md
@.planning/phases/31-autonomous-manual-testing/31-15-SUMMARY.md
@.planning/phases/31-autonomous-manual-testing/31-19-SUMMARY.md
@docs/audit/31-round4-residuals.md
@scripts/context-io.ts
@agent-factory/workflows/16-context-read-write.md
@install/install.ts
</context>

## Artifacts this phase produces

| Artifact | Kind | Where |
|---|---|---|
| `isAboveHome` | NEW predicate: `dir` is STRICTLY an ancestor of the home directory, by path spelling and by `dev:ino` identity | `scripts/context-io.ts` |
| `isHomeItself` | NEW predicate: `dir` IS the home directory, by the same two comparisons | `scripts/context-io.ts` |
| the split `HomeBoundary` | CHANGED shape: two path sets and two identity sets — home itself, and its strict ancestors — with the degenerate-inode premise check applied to both | `scripts/context-io.ts` |
| `GOVERNANCE_CONFIG_CANDIDATE_KINDS` | NEW exported frozen classification PARALLEL to `governanceConfigCandidates`: `repository-state-plane` for `<base>/.grugops/factory.config.json`, `in-kit` for `<base>/agent-factory/config/factory.config.json`. Bound to the candidate list by an index-for-index and cardinality assertion in both directions, so a reordered or extended candidate list turns red rather than silently changing what the home rule means, AND by a per-candidate SHAPE PREDICATE over that candidate's own path segments, so an in-place rename — which moves neither index nor cardinality — turns red too | `scripts/context-io.ts` |
| `MODULE_OWN_CONFIG_POSITIONS` | NEW exported frozen constant: `governanceConfigCandidates(GOVERNANCE_FALLBACK_BASE)`, lexically resolved, computed once at load. Reads no environment variable and probes no filesystem; it is a property of which program is running | `scripts/context-io.ts` |
| `homeConfigPositionIsProjectOwned` | NEW module-private predicate over the CARRYING candidate at home: its kind is `repository-state-plane` AND its resolved path is not a member of `MODULE_OWN_CONFIG_POSITIONS`. No `existsSync`, no `statSync`, no `process.env` — the retracted `sharedInstallKitHomes` / `configPositionIsKitOwned` pair is NOT added | `scripts/context-io.ts` |
| the inspect-then-decide walk | CHANGED `projectRootFromWorkingDirectory`: ascent is bounded, inspection is not; the home directory is inspected exactly once and adopted only with a marker AND a carrying candidate AND `homeConfigPositionIsProjectOwned` of that candidate, never contributing to `nearest` | `scripts/context-io.ts` |
| `S-HOME-ABOVE` / `S-HOME-SELF` | CHANGED/NEW members of `TRUSTED_ROOT_STOP_CONDITIONS`, replacing the single `S-HOME` sentence with the two the walk now has; `S-HOME-SELF` states all three conjuncts | `scripts/context-io.ts` |
| `R-31-19-05` / `R-31-19-06` / `R-31-19-07` | NEW `TRUSTED_ROOT_RESIDUALS` members with all four documented fields: the marker-less home repository this plan does not adopt; the PRICED caller-created states on both sides of the two evidence conjuncts; and the lexical-spelling gap in the module-constant exclusion | `scripts/context-io.ts` |
| the home-rooted corpus | NEW cases: home with marker + state-plane config → ADOPTED; the same tree after `mkdir -p $HOME/.grugops/agent-factory` → verdict UNCHANGED; the dotfiles-plus-shared-install tree with cwd below home → ADOPTED, and unchanged after `touch $HOME/.grugops/install.json` and with that file holding `{}`; home whose ONLY carrying candidate is the in-kit position → REFUSED; home whose carrying candidate is the module's own position, with a configured project nested below → the NESTED project wins; home with config only; home with marker only; home as cwd; a repository directly below home; the `GRUGOPS_HOME` four-value sweep; plus the WR-21 re-measurement | `scripts/context-io.test.ts` |
| the monotonicity sweep | CHANGED/extended: the existing case set driven against both programs with a no-case-moved assertion | `scripts/context-io.test.ts` |
| the moved stop list | CHANGED prose quoted from the export under the existing both-directions equality | `agent-factory/workflows/16-context-read-write.md` |
| the moved decider hashes | CHANGED frozen hashes for the rebuilt `scripts/context-io.js` | `hooks/hook-entry.ts` |
| decision **D-26** | NEW dated gap-closure decision appended beside D-25 | `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` |

## Edge probe assumptions

| # | Requirement | Category | Probe | Standing assumption for this plan |
|---|---|---|---|---|
| 1 | UATX-01 | adjacency | "when two things are exactly equal or just touch, do they merge, collide, or separate?" | Assumed: the home directory ITSELF, a repository that is a direct CHILD of home, and a directory that is a strict ANCESTOR of home are three separate cases with three separate answers. All three are named cases; none may be answered by the same branch. |
| 2 | UATX-01 | empty | "what is the result for empty, single-element, or null input?" | Assumed: a home directory that cannot be determined still stops the search entirely rather than licensing an unbounded one — `S-HOME-UNKNOWN` is unchanged and re-asserted, not merely inherited. |
| 3 | UATX-01 | ordering | "when elements compare equal, is output order specified and stable?" | Assumed: within the home directory, the configuration-versus-marker precedence is stated explicitly rather than left to reading order, and it differs from the ordinary-directory rule (home requires BOTH). Asserted as its own case. |
| 4 | UATX-01 | unclassified | review manually | Assumed: `dev:ino` identity and path spelling can disagree (a symlinked or case-differing home). Both sets are consulted for both predicates, and the degenerate-inode premise is re-checked rather than inherited from the old single-set shape. |
| 5 | UATX-01 | unclassified | review manually | MEASURED in MOVEMENT 0, not assumed: `install/install.ts` copies the kit to `$GRUGOPS_HOME/agent-factory` (default `~/.grugops/agent-factory`) and writes the per-repo state — `factory.config.json` and the `install.json` marker — under `$TARGET/.grugops/`. The two roots are the SAME directory exactly when `TARGET === $HOME`, which is the case that must be ADOPTED. The shipped kit carries no `scripts/`, so `GOVERNANCE_FALLBACK_BASE` is always a checkout root. That measured layout, not an inherited sentence about it, is what makes the marker requirement load-bearing and what disqualifies every kit-home derivation the previous draft proposed; the earlier draft's claim that `~/.grugops/factory.config.json` is itself the shipped kit shape is checked against the source and corrected if it does not hold. |
| 6 | UATX-06 | unclassified | review manually | Not touched by this plan; `31-24` and `31-25` own the modifier ban and the exit boundary. |
| 7 | UATX-01 | unclassified | review manually | DECIDED, and the axis this revision exists for: every input to the home-adoption rule is classified as caller-authorable or not, and the classification is DRIVEN rather than reasoned. A version-control marker and a governance configuration at `$HOME` are caller-authorable and are the walk's ordinary evidence everywhere, so they are PRICED in operations as `R-31-19-06` rather than defended. The candidate KIND and `MODULE_OWN_CONFIG_POSITIONS` are not caller-authorable by any filesystem write or environment value, and the invariance cases assert that in both directions. No third class is permitted: a conjunct that is neither priced nor invariant does not ship. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the process working directory -> the upward configuration search | a value the agent can change crosses here; the bound above it must not depend on which markers a filesystem happens to carry |
| the home directory's carrying configuration CANDIDATE -> the dial that decides an admission | the two published candidate positions are not equivalent evidence: the state-plane position is what `seedState` writes for a TARGET, the in-kit position is a kit's own configuration, and at `$HOME` there is no boundary above to make D-23 (4) resolve the difference |
| a filesystem artifact the calling agent can create -> a conjunct of the home rule | the axis this revision closes: any conjunct a `mkdir`, a `touch` or an environment variable can flip is a switch handed to the caller, in whichever direction it flips |
| a home-rooted repository's own dial -> the refusal a writer performs | skipping it is the WR-15 direction, and on four of the five hosts the in-script refusal is the only tier available |
| `TRUSTED_ROOT_STOP_CONDITIONS` -> the workflow prose every reader relies on | a claim crosses here; without the asserted equality the prose is free to outrun the mechanism |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-23-01 | Elevation of Privilege | a home-rooted repository's dial silently replaced by the kit's lean default | high | mitigate | CR-13: the walk inspects before it asks the home question; home answers as a repository when it carries a marker and its own configuration. RED-first with the verifier's row-8 probe; watched by the home-rooted corpus. |
| T-31-23-02 | Elevation of Privilege | the naive asymmetry adopting a bare `~/.grugops` and reopening WR-21 | high | mitigate | The adopted rule demands a boundary MARKER at home and never lets home become `nearest`; the WR-21 probe is re-driven on this tree and asserted still resolving to the kit. |
| T-31-23-02b | Elevation of Privilege | a KIT's own configuration occupying a home candidate position — a vendored `agent-factory/config/factory.config.json` at `$HOME`, or the running module's own checkout AT `$HOME` — so the kit's shipped lean default outranks a configured project nested below home, at the one position D-23 (4)'s boundary-wins rule cannot reach | critical | mitigate | Two conjuncts that read no filesystem state: the carrying candidate's KIND must be `repository-state-plane`, and its resolved path must not be a member of `MODULE_OWN_CONFIG_POSITIONS`. Driven RED against the committed `.js` AND against a scratch marker-plus-configuration implementation; mutation-proven by removing each conjunct separately and showing its own case fail while GREEN 1 and GREEN 1b stay green. |
| T-31-23-02c | Tampering | a conjunct of the home rule flipped by a filesystem artifact the calling agent creates in one operation — in EITHER direction, since a refusal at home lands on `GOVERNANCE_FALLBACK_BASE`'s lean dial exactly as a wrong adoption lands on a kit's | critical | mitigate | The retracted `existsSync`-under-`$HOME` design is not shipped, and the installer's marker is not read at all (`install/install.ts:609`'s all-optional schema accepts `{}`). The two added conjuncts are a published candidate KIND and a load-time module constant. Asserted by three INVARIANCE cases driving the re-check's own artifacts verbatim — `mkdir -p $HOME/.grugops/agent-factory`, `touch $HOME/.grugops/install.json` (empty and `{}`), and a four-value `GRUGOPS_HOME` sweep — each requiring the verdict UNCHANGED, plus a derived assertion that the home branch's decision expression references only the four published names. |
| T-31-23-02d | Elevation of Privilege | the walk's ORDINARY evidence at `$HOME` — a version-control marker and a state-plane configuration — authored by a process that can write under `$HOME` | high | accept | Named and PRICED as `R-31-19-06`: three operations against a bare home, one against a home that already carries a dotfiles checkout, and one operation (`mkdir $HOME/work/.git`) for the converse gate-lowering that predates this plan. Identical to the evidence the walk accepts at every other directory, inside the class `R-31-15-01` accepts and `31-22`'s `T-31-18-01` prices the same way. CONTROL 1b-style measurement records the artifacts and the count rather than an adjective. |
| T-31-23-03 | Information Disclosure | GOV-02 admission events written into the home directory's committed audit trail | high | mitigate | The consequence is asserted directly: a case drives an admission from the marker-less home shape and asserts no ledger appeared under the home directory. |
| T-31-23-04 | Denial of Service | a stop so aggressive it re-opens WR-15 for ordinary projects | high | mitigate | The monotonicity sweep drives the whole existing case set against both programs and asserts no case moved from refused to admitted; `31-15`'s own spot-check is re-run verbatim. |
| T-31-23-05 | Repudiation | a published stop condition the walk does not have | high | mitigate | The stop set gains the two members the walk now has, and the workflow list is asserted set-equal to the export in both directions with the existing seeded-fail control. |
| T-31-23-06 | Tampering | a boundary this plan leaves open arriving as a silence rather than a member | medium | mitigate | `R-31-19-05` is added with all four documented fields, and the register's set-equality assertion turns a missing disposition red. |
| T-31-23-07 | Tampering | the frozen hook manifest relaxed to accommodate a rebuilt artifact | critical | mitigate | The `DECIDER_MANIFEST` hashes are MOVED to the re-measured digest; `hooks/guard.ts` untouched, `FROZEN_GUARD_BLOB` not re-based. |
| T-31-23-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager install task and no dependency; every command runs against the committed lockfile and already-installed dev dependencies, so the package-legitimacy protocol has no `[ASSUMED]`/`[SUS]` row to gate. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The bound bounds ascent, not inspection — home answers only as a repository (CR-13, D-26)</name>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts</files>
  <precondition>The RED and GREEN probes must run in CHILD PROCESSES with `HOME` overridden to the planted tree and with BOTH `CLAUDE_PROJECT_DIR` and `GRUGOPS_PROJECT_DIR` genuinely DELETED from the environment — not set to an empty string. If either leaks in from the launching session the probe measures step 1 of the resolution order rather than step 3, and the measurement is not the one the verifier made. The real `HOME` must be restored and no planted tree may survive the task. Every planted tree lives under the SINGLE root `.temp/31-23-probe/` and is `rm -rf`'d; the check is `test ! -e .temp/31-23-probe` plus the FIFO sweep `find . -path ./node_modules -prune -o -type p -print`, NOT `git status --short .temp`, which is inert because `.temp/` is gitignored at `.gitignore:19` and prints nothing whatever that directory holds.</precondition>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — CR-13 in full: its file/line citations (`:3066`, `:3014-3018`, `:3051-3080`, `:3305-3325`, `:3107-3140`, `16-context-read-write.md:54`, `:2892-2900`), the reproduction transcript with the tree, the `HOME` override, the resolved root, the dial and the WROTE/REFUSED pair, the three numbered reasons it is a blocker, and its `Fix:` code sketch IN FULL — this task adopts a strictly narrower rule than that sketch and must state why in the source.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the `regressions:` entry for CR-13, behavioral spot-check rows 8 and 9 (the failure and its control), the required-artifacts row for `trustedRepoRoot`/`projectRootFromWorkingDirectory`, the key-link row recording the walk MISWIRED, and the anti-patterns row at `~2598-2606`.
    - `scripts/context-io.ts` — `directoryIdentity`, `namedHomeDirectory`, the whole `HomeBoundary` header including the WHY-IDENTITY-AND-NOT-PATH-TEXT and the degenerate-inode paragraphs, `homeBoundary()`, `isAtOrAboveHome`, `projectRootFromWorkingDirectory` in full including its THREE RULES docstring, `REPO_BOUNDARY_MARKERS`, `TRUSTED_ROOT_SEARCH_MAX_ANCESTORS`, `governanceConfigCandidates`, `TRUSTED_ROOT_STOP_CONDITIONS` in full, `trustedRepoRoot` including the monotonicity-mirror ANCHOR paragraph, `TRUSTED_ROOT_RESIDUALS` in full (`R-31-15-01..04`, `R-31-19-01..04`), and the `:2892-2900` comment rejecting `.grugops` as a marker on the WR-15 ground.
    - `scripts/context-io.test.ts` — the `31-15` monotonicity mirror block, how it reconstructs the pre-31-15 program by reverting the ONE anchored call, and its occurrence-count assertions; a change to this function must keep those anchors valid or move them deliberately.
    - `install/install.ts` — `resolveGrugopsHome` and `KIT_ROOT` (`:137-147`), `copyKit` (`:1536-1566`), `seedState` (`:2141-2160`), `writeMarker` (`:2226-2246`), and `readMarker` with its `InstallMarker` interface (`:597-620`) — the two-root layout, where the shared kit is placed, and how many marker fields are required, so the marker requirement at home is justified by the shape the installer actually creates and so the reason the marker itself is NOT read is measured rather than argued.
    - `docs/audit/31-round4-residuals.md` — §4.3 in full, including the printed ORIGINAL / ADJUSTED / ABOVE transcripts and its `UNKNOWN - verify` about which spelling round 4 ran; this task re-drives BOTH spellings so `31-26` can resolve that item by name.
  </read_first>
  <behavior>
    - RED 1 (the verifier's row 8): with `HOME` set to a planted repository root carrying `.git` and `human_admission: high-severity`, both project variables removed, cwd inside it — `trustedRepoRoot()` returns the kit and the dial reads `off`.
    - RED 2 (the VENDORED-KIT-AT-HOME shape, driven RED against the committed `.js` AND against a scratch marker-plus-configuration implementation): `HOME` set to a planted root carrying `.git` and `agent-factory/config/factory.config.json` — the SECOND published candidate, carrying the lean dial — and NO `.grugops/factory.config.json`; cwd `$HOME/work/proj` carrying a `.grugops/factory.config.json` with `human_admission: high-severity`. Against a marker-plus-configuration rule the boundary-wins rule adopts `$HOME` and the KIT's lean dial governs the configured project below it. The required answer is that the walk refuses home, returns the remembered nearest, and the project's own `high-severity` dial governs — with a self-stamped high-severity finding REFUSED at that cwd.
    - RED 2b (the MODULE'S OWN POSITION at home, the case the path-equality conjunct exists for): the module tree is COPIED into the probe root so the running module's own `GOVERNANCE_FALLBACK_BASE` IS the planted home — `<probe>/kit/scripts/` holding the built `.js`, `<probe>/kit/.git`, `<probe>/kit/.grugops/factory.config.json` carrying the lean dial — with `HOME` set to `<probe>/kit` and cwd `<probe>/kit/proj` carrying its own `.grugops/factory.config.json` with `human_admission: high-severity`. This spelling is chosen deliberately over pointing `HOME` at the real checkout, because the real checkout carries no state-plane configuration and creating one would mutate a tracked tree to make a probe work. Against a marker-plus-configuration rule the boundary-wins rule adopts home, so the running kit's own configuration outranks the project nested inside it. The required answer is the nested project's dial, and the finding REFUSED. If the module tree cannot be copied so that it still resolves its imports, the case is driven as a UNIT against the exported `MODULE_OWN_CONFIG_POSITIONS` instead, and the summary states which spelling ran and why — a case that could not run is recorded as one, never as a pass.
    - GREEN 1: after the fix RED 1's probe resolves to the planted repository root and the dial reads `high-severity`; a self-stamped high-severity finding is REFUSED there. The planted tree's carrying candidate is the state-plane position and is not a member of `MODULE_OWN_CONFIG_POSITIONS`, so both added conjuncts hold — stated in the case, because this is the exact boundary between GREEN 1 and RED 2/RED 2b.
    - GREEN 1b (the home-rooted INSTALLED project): a tree seeded the way `install/install.ts` seeds a TARGET at `$HOME` — `.git`, `$HOME/.grugops/factory.config.json`, `$HOME/.grugops/install.json`, and a shared install at `$HOME/.grugops/agent-factory` — resolves to `$HOME` and reads the home configuration's dial. Driven THREE times: with the marker file present, absent, and present holding `{}`. All three verdicts identical, which is what proves the installer's marker is not consulted rather than merely stating it.
    - GREEN 1c (the re-check's own tree (b), DECIDED): the dotfiles-plus-shared-install tree — `$HOME/.git`, `$HOME/.grugops/agent-factory/` present, `$HOME/.grugops/install.json` absent, `$HOME/.grugops/factory.config.json` carrying the lean dial, cwd `$HOME/work/proj` carrying neither marker nor configuration, both project-directory variables removed — resolves to `$HOME`. That is the DECIDED verdict, identical to the verdict for the same tree one level BELOW home (`R-31-19-01`, verification row 9), and its cost is `R-31-19-06`'s priced construction. The case records the price with it: which artifacts, how many operations, and the measured fact that a default shared install creates neither of them at `$HOME`.
    - GREEN 2 (WR-21 NOT reopened): with `HOME` set to a planted ancestor carrying ONLY `.grugops/factory.config.json` and no `.git`, the resolution still returns the kit and the dial still reads `off` — the round-4 ADJUSTED spelling re-driven.
    - INVARIANCE 1 (the re-check's artifact (a), driven VERBATIM): GREEN 1's adopted tree, then `mkdir -p $HOME/.grugops/agent-factory`. The verdict is UNCHANGED — still `$HOME`, still `high-severity`, the self-stamped high-severity finding still REFUSED. The pre-artifact and post-artifact answers are recorded side by side, because the retracted design turned exactly this `mkdir` into CR-13's own harm.
    - INVARIANCE 2 (the re-check's artifact (b), driven VERBATIM): GREEN 1c's tree, then `touch $HOME/.grugops/install.json`, and again with that file holding the two-byte document `{}` that `install/install.ts:609`'s `readMarker` accepts as a valid marker. All three verdicts identical. An artifact a caller can create in one write must not move the answer, whether it is read by existence or through a schema.
    - INVARIANCE 3 (the environment axis): GREEN 1c's tree re-driven with `GRUGOPS_HOME` unset, redirected to an unrelated directory, set to the empty string, and set to `$HOME` itself. All four verdicts identical, because the rule reads no environment variable. This replaces the retracted "the set may only refuse" case, whose own premise was false.
    - INVARIANCE 4 (derived, not driven): the home branch's decision expression is parsed from the source and its referenced identifiers are asserted by MEMBERS and COUNT against the four published names the rule is allowed to consult — `REPO_BOUNDARY_MARKERS`, `governanceConfigCandidates`, `GOVERNANCE_CONFIG_CANDIDATE_KINDS` and `MODULE_OWN_CONFIG_POSITIONS` — with a PREMISE case asserting the parse found the branch and a seeded mirror that adds one identifier and moves the count by exactly one. A later edit that reintroduces a filesystem probe or an environment read into this rule turns this red at the source rather than waiting for a round-6 reproduction.
    - GREEN 3 (the marker-only home): a home directory carrying `.git` but NO configuration yields whatever was remembered below it, never the home directory.
    - GREEN 4 (home never becomes `nearest`): a home directory carrying a configuration and no marker, with a working directory several levels below it that carries nothing, resolves to the kit — proving home did not enter `nearest` on the way past.
    - CONTROL 1 (verification row 9 / `R-31-19-01`): the identical tree one level BELOW the real home still resolves to the project and reads `high-severity`, unchanged.
    - CONTROL 2 (`31-15`'s own spot-check): with both variables unset and the working directory inside a real project carrying an active dial, the write is still refused naming the dial.
    - CONTROL 3 (a repository directly under home): a real project that is a direct child of the home directory still resolves to itself.
    - CONTROL 4 (strictly above home): a configuration at a strict ANCESTOR of home is still never inspected and never adopted.
    - EMPTY: a home directory that cannot be determined still stops the search entirely and degrades to the kit.
    - ADJACENCY: a symlinked or case-differing spelling of home is still recognised as home by the identity set; the degenerate-inode premise is re-checked for the split sets.
  </behavior>
  <action>
    MOVEMENT 0 — MEASURE what the shipped installer places at each root, before writing a rule that depends
    on it. Read `install/install.ts` and record, with line citations: what `copyKit` writes under
    `$GRUGOPS_HOME` (the measured answer is `agent-factory/` and nothing else at that root), where
    `resolveGrugopsHome` resolves `GRUGOPS_HOME` to and how it treats an empty string, where the seeded
    per-repo config is written (`join(TARGET, ".grugops", "factory.config.json")`), and where `writeMarker`
    writes `install.json` (`join(TARGET, ".grugops", "install.json")`). State the ONE case in which those
    two roots collide: `TARGET === $HOME`, which is the case that must be ADOPTED rather than refused.

    Measure TWO further facts in the same movement, both of which the previous draft assumed the other way
    and both of which decide the shape of the rule below. FIRST, list the shipped kit's top level and record
    whether it carries a `scripts/` directory; the measured answer is that it does not, so an installed kit
    at `$GRUGOPS_HOME/agent-factory` does not contain this module, `GOVERNANCE_FALLBACK_BASE` is always a
    CHECKOUT root, and any derivation of a kit home from `dirname(GOVERNANCE_FALLBACK_BASE)` is a derivation
    over a shape the installer never creates. SECOND, read `readMarker` and the `InstallMarker` interface at
    `install/install.ts:597-620` and record, with line citations, how many of its fields are REQUIRED and
    whether any names the TARGET; the measured answer is none and no, so the two-byte document `{}` is a
    schema-valid marker. Record both as measurements with their citations. These three measurements are what
    disqualify every kit-home derivation and every marker read the previous draft proposed, and the summary
    quotes them rather than inheriting a previous round's prose about them.

    MOVEMENT 1 — reproduce BOTH spellings and record them verbatim. Build the verifier's row-8 tree: a
    planted repository root carrying `.git` and `.grugops/factory.config.json` with
    `human_admission: high-severity`, and a working directory inside it. Drive the CURRENTLY COMMITTED
    `scripts/context-io.js` in a child process with `HOME` set to that root and both project-directory
    variables DELETED; record `homedir()`, `trustedRepoRoot()`, the dial and its source, and whether an
    `appendNote` of a self-stamped high-severity finding WROTE or was REFUSED. Then drive the round-4
    ADJUSTED spelling from `docs/audit/31-round4-residuals.md` §4.3 — a planted ancestor carrying ONLY a
    configuration, `HOME` set so it IS `os.homedir()` — and record the same four lines. Then drive the
    ORIGINAL spelling (planted ancestor merely NAMED `home`, real `HOME` untouched) and record it too, so
    §4.3's `UNKNOWN - verify` has a measurement for both readings.

    Then build the TWO kit-at-home trees — RED 2's vendored `agent-factory/config/factory.config.json` at
    `$HOME`, and RED 2b's COPIED module tree whose own root IS the planted home, so
    `GOVERNANCE_FALLBACK_BASE` and `$HOME` are the same directory without any tracked file being touched —
    each with
    a CONFIGURED project nested below, and drive both against the committed `.js` AND against a scratch
    MARKER-PLUS-CONFIGURATION implementation, so the measurement shows each shape failing under BOTH: the
    committed program, and the fix this plan would have shipped with two conjuncts only. A RED case that
    only fails against the old program cannot show why the rule has the shape it has. Record `homedir()`,
    the resolved root, the dial and its source, and the WROTE/REFUSED answer for each cell.

    Then build the DECIDED tree — GREEN 1c's dotfiles-plus-shared-install shape, which the previous draft
    intended to refuse and this one adopts — and record its pre-fix and marker-plus-configuration answers too, so the
    summary states the verdict this plan CHOSE for it rather than leaving a reader to infer that a shape the
    threat register once named critical simply disappeared. Record beside it the measured operation count
    and the exact artifacts a caller would need to construct that tree, which is `R-31-19-06`'s price.

    Add the failing cases and observe them fail. Remove every planted tree and restore `HOME`.

    MOVEMENT 2 — split the predicate. Change `HomeBoundary` to carry TWO path sets and TWO identity sets:
    the home directory itself, and its STRICT ancestors. Build both in `homeBoundary()` from the same
    spellings loop, adding the first `dir` to the self sets and every subsequent `dir` to the above sets.
    Keep the degenerate-inode premise check and apply it to BOTH identity sets — where a platform's inodes
    say nothing, both identity sets are discarded and the spelling sets decide alone, exactly as today.
    Replace `isAtOrAboveHome` with `isAboveHome(dir, home)` and `isHomeItself(dir, home)`, each consulting
    its own pair of sets. Delete `isAtOrAboveHome` rather than leaving it beside the new pair; a third
    predicate answering a question the two now answer is the drift shape this module keeps deleting.

    MOVEMENT 3 — publish what the two candidate POSITIONS mean, freeze the module's OWN positions, then
    inspect first and decide about climbing. Every input the home rule consults beyond the walk's ordinary
    evidence must be a path, a position or a load-time constant. Nothing in this movement calls `existsSync`
    or `statSync` on a path under `$HOME`, and nothing reads `process.env`.

    FIRST add `GOVERNANCE_CONFIG_CANDIDATE_KINDS`: an exported frozen array PARALLEL to
    `governanceConfigCandidates`, `["repository-state-plane", "in-kit"]`. Write at the site what each kind
    means and what created it — the state-plane position is `join(TARGET, ".grugops", "factory.config.json")`,
    which `seedState` writes for a seeded target; the in-kit position is
    `join(base, "agent-factory", "config", "factory.config.json")`, which is a KIT's own configuration and
    the file every vendored copy of this kit carries. Bind the two arrays index-for-index and by CARDINALITY
    in `scripts/context-io.test.ts`, in both directions, with a seeded mirror: a candidate added without a
    kind, or a kind without a candidate, must turn red, because a reordered candidate list would otherwise
    silently change what the home rule means.

    Index, cardinality and transposition are three drift axes and they are not all of them: an IN-PLACE
    RENAME of a candidate path moves none of the three, and leaves the kind sitting on a position it no
    longer describes. So bind each kind to a SHAPE PREDICATE over ITS OWN candidate's path segments rather
    than only to its position in the array — `in-kit` holds exactly when the candidate's segments contain
    `agent-factory` followed by `config`, and `repository-state-plane` exactly when they contain `.grugops`
    directly beneath the base and no `agent-factory` segment at all — asserted for every index by iterating
    the candidate array rather than by naming the two members, so a third candidate arrives bound. A
    classification of an enumerated set is DERIVED from that enumeration or every drift axis of the parallel
    literal is closed; this is the axis the index binding leaves open. Add one mirror that renames a
    candidate path in place and assert the shape binding turns red where the index binding stays green,
    so the new axis is shown to discriminate rather than to duplicate.

    THEN add `MODULE_OWN_CONFIG_POSITIONS`: an exported frozen array,
    `governanceConfigCandidates(GOVERNANCE_FALLBACK_BASE).map((p) => resolve(p))`, computed once at module
    load. Write at the site the property that makes it the right input: `GOVERNANCE_FALLBACK_BASE` is
    `join(import.meta.dirname, "..")`, a property of WHICH PROGRAM IS RUNNING rather than of the filesystem
    that program inspects, so no `mkdir`, no `touch`, no write under `$HOME` or the repository and no
    environment value moves it. Moving it means running a different copy of this decider, which is what
    `DECIDER_MANIFEST` in `hooks/hook-entry.ts` freezes and which this predicate is not the tier for. State
    in the same paragraph that `process.env.GRUGOPS_HOME` is deliberately NOT consulted, and why: it is
    caller-settable in zero operations, so a rule that read it would be a rule the caller decides.

    THEN add `homeConfigPositionIsProjectOwned(candidateIndex, candidatePath)`: true when the kind at that
    index is `repository-state-plane` AND `resolve(candidatePath)` is not a member of
    `MODULE_OWN_CONFIG_POSITIONS`. Two comparisons, no filesystem read. State the argument for each: the
    in-kit position is excluded because D-23 (4) already ruled a vendored kit's configuration must lose to a
    repository root's own, and at `$HOME` there is no boundary above for it to lose to; the module's own
    positions are excluded because the kit this reader ships in is not a project, and `$HOME` is the only
    directory where its own fallback candidate can be a candidate the walk computes.

    Then rewrite the loop body of `projectRootFromWorkingDirectory` in this exact order: refuse outright
    when `isAboveHome(dir, home)`, returning `nearest`; compute the carrying CANDIDATE and its INDEX (not
    merely the boolean) from `governanceConfigCandidates(dir)` — the first candidate that exists — and
    `isBoundary` from `REPO_BOUNDARY_MARKERS`; then, when `isHomeItself(dir, home)`, return `dir` if
    `isBoundary` AND a carrying candidate exists AND `homeConfigPositionIsProjectOwned` of that index and
    path, and otherwise return `nearest` WITHOUT assigning `nearest = dir`; then the ordinary rules
    unchanged — remember `dir` as `nearest` when it carries a configuration and nothing was remembered, and
    return at a boundary. Note that the ordinary (non-home) rules keep using existence alone: the position
    question is asked ONLY at home, because D-23 (4)'s boundary-wins rule already answers it everywhere
    else and asking it twice would be a second authority for one question. State that scoping decision
    rather than leaving a reader to infer it.

    State the whole rule set and its precedence in the docstring rather than leaving it to reading order,
    and state IN THE SOURCE all three reasons the shape of this rule is what it is. One: assigning
    `nearest = dir` before the home branch — the review's own sketch — would adopt a home carrying only a
    configuration, the hole WR-21 was convened to close. Two: a marker-plus-configuration rule alone lets a
    kit's own configuration at a home candidate position outrank a configured project nested below it, at
    the one position D-23 (4) cannot reach. Three, and this is the paragraph a later reader most needs:
    the previous draft answered reason two with two `existsSync` probes under `$HOME/.grugops`, and BOTH
    were caller-authorable in one operation, in opposite directions — creating `<kitHome>/agent-factory`
    turned an adoption into a refusal, whose answer is the kit's LEAN fallback dial, and creating
    `<kitHome>/install.json` turned a refusal into an adoption. Record that reading the marker through
    `readMarker`'s schema is the same one-write flip, with the measured citation. A conjunct a caller can
    flip is a switch, whichever way it flips; that sentence is what stops the probes coming back.

    MOVEMENT 4 — move the published stop set and the prose with it. Replace the single `S-HOME` member of
    `TRUSTED_ROOT_STOP_CONDITIONS` with `S-HOME-ABOVE` ("The upward search never inspects any ancestor of
    the user's home directory.") and `S-HOME-SELF` (one sentence stating that the home directory itself is
    inspected exactly once, is adopted only when it carries a version-control marker AND the candidate
    carrying its configuration is the repository state-plane position rather than the in-kit position AND
    that candidate is not one of the running kit's own fallback candidate positions, and ends the walk
    either way). The sentence must carry all three conjuncts, because a published stop that states two of the three
    conditions the walk applies is the same class of drift WR-21 was: prose narrower or broader than the
    mechanism it claims to describe. Keep every other member and the interpolated step limit
    unchanged. Update `agent-factory/workflows/16-context-read-write.md`'s quoted list to match; the
    existing both-directions equality assertion is what proves they agree, and it must be green with the
    seeded-fail control still discriminating. Re-derive the workflow stop-bullet count rather than assuming
    the edit left it alone.

    MOVEMENT 5 — rebuild, re-measure, mutation-prove. Run `npm run build`, re-run every probe spelling
    against the rebuilt committed `.js`, and record the post-fix outputs beside the pre-fix ones in one
    table. Move the `DECIDER_MANIFEST` entries for `scripts/context-io.js` to the re-measured digest and
    rebuild `hooks/hook-entry.js`. Then apply FIVE described mutants in turn — first the marker requirement
    at home removed, which must make GREEN 2 and GREEN 4 fail; second the inspect-before-ascend order
    reverted, which must make GREEN 1 fail; third the KIND conjunct removed (the in-kit position accepted at
    home), which must make RED 2's case fail and MUST NOT break GREEN 1, GREEN 1b or GREEN 1c; fourth the
    `MODULE_OWN_CONFIG_POSITIONS` exclusion removed, which must make RED 2b's case fail and MUST NOT break
    GREEN 1 or RED 2's case; fifth the kind array's two entries transposed, which must turn the
    index-for-index binding red rather than silently inverting the rule — reverting after each and showing
    the cases pass. Record for each mutant exactly which cases broke, and state explicitly that no mutant
    broke a case belonging to a different conjunct, since a conjunct whose removal breaks nothing is a
    conjunct nobody tested and two conjuncts that break the same case are one conjunct written twice.
  </action>
  <acceptance_criteria>
    - MOVEMENT 0's THREE measurements are recorded with line citations: what `copyKit` writes under `$GRUGOPS_HOME` and where the seeded config and the `install.json` marker are written, with the one case in which the two roots collide; whether the shipped kit's top level carries a `scripts/` directory; and how many `InstallMarker` fields are required and whether any names the TARGET. The summary states plainly whether `~/.grugops/factory.config.json` is a shape the shipped installer creates for the KIT, and corrects the earlier claim if the measurement disagrees with it.
    - `31-23-SUMMARY.md` prints a table with one row per probe spelling (row-8 home-rooted repository, the VENDORED-KIT-AT-HOME shape, the MODULE'S-OWN-POSITION shape, the dotfiles-plus-shared-install shape, §4.3 ADJUSTED, §4.3 ORIGINAL) × THREE columns (pre-fix committed `.js`, a scratch marker-plus-configuration implementation, post-fix), each cell carrying `homedir()`, `trustedRepoRoot()`, the dial and its source, the WROTE/REFUSED answer, and whether a ledger appeared under `$HOME/.grugops/audit/`.
    - The pre-fix row-8 cell matches `31-VERIFICATION.md` behavioral spot-check row 8 exactly (kit returned, dial `off`).
    - The post-fix row-8 cell resolves to the planted repository and reads `high-severity`, and the self-stamped high-severity finding is REFUSED there.
    - The VENDORED-KIT-AT-HOME row and the MODULE'S-OWN-POSITION row each resolve to `$HOME` — the kit's lean dial governing the nested project — in BOTH the pre-fix and the marker-plus-configuration columns, and in the post-fix column each resolves to the NESTED project with its `high-severity` dial and a REFUSED finding. The marker-plus-configuration column is what proves each added conjunct is load-bearing rather than defensive.
    - The DOTFILES-PLUS-SHARED-INSTALL row's post-fix cell resolves to `$HOME`, and the summary states in one sentence that this is the DECIDED verdict, that it is identical to the answer for the same tree one level below home (`R-31-19-01`), and what it costs a caller to construct — the artifacts and the operation count recorded as numbers, which is `R-31-19-06`'s price. A verdict this plan chose is stated as a choice, never left to be inferred from a case that quietly changed.
    - GREEN 1b is driven THREE times — the installer's marker present, absent, and holding `{}` — and all three verdicts are quoted and identical, so the marker is shown not to be consulted rather than merely said not to be.
    - INVARIANCE 1, 2 and 3 are each driven and quoted with the pre-artifact and post-artifact answers side by side: `mkdir -p $HOME/.grugops/agent-factory` does not move the verdict; `touch $HOME/.grugops/install.json` and the same file holding `{}` do not move the verdict; and `GRUGOPS_HOME` unset, redirected, empty and set to `$HOME` all give the same verdict. The summary states that these are the two artifacts and the one variable the round-5 re-check named, driven verbatim.
    - INVARIANCE 4 is driven: the home branch's referenced identifiers are asserted by MEMBERS and COUNT against the four published names, the PREMISE case fires on a renamed branch rather than passing vacuously, and the seeded mirror moves the count by exactly one.
    - The post-fix §4.3 ADJUSTED cell still resolves to the kit with dial `off` — WR-21 not reopened; stated explicitly.
    - GREEN 3 and GREEN 4 each pass as named cases; GREEN 4's assertion that home never entered `nearest` is made by observing the resolved answer for a working directory below home, not by inspecting a variable.
    - CONTROL 1, 2, 3 and 4 each pass and are quoted; CONTROL 1 is labelled as the documented residual `R-31-19-01` rather than as a new finding.
    - The monotonicity sweep drives the existing case set against both programs and asserts no case moved from refused to admitted; the number of cases driven is recorded. Its set is stated explicitly as the CONFIGURATION-RESOLUTION corpus predating this round, and the summary names what is NOT in it: this plan's own new home-rooted cases, and the cross-plan origin-recognition cases `31-22` added at wave 2, which PROBE 5 drives under its declared intended-change list. A sweep that silently included `31-22` CONTROL 5a would forbid the one movement this plan declares.
    - `isAtOrAboveHome` no longer appears in `scripts/context-io.ts`; `grep -v '^\s*[/*]' scripts/context-io.ts | grep -c 'isAtOrAboveHome'` is recorded and is `0`.
    - `TRUSTED_ROOT_STOP_CONDITIONS`'s cardinality is recorded before → after (6 → 7) and the workflow's quoted list is asserted set-equal in both directions with the seeded-fail control still discriminating. `S-HOME-SELF`'s sentence is quoted verbatim and carries all THREE conjuncts the walk applies.
    - The workflow stop-bullet count is RE-DERIVED and recorded as a number; if it moved, the constant is moved deliberately and the movement is stated.
    - The monotonicity-mirror anchors are still valid, with their occurrence counts asserted exactly before and after the mutation.
    - All FIVE mutation proofs are recorded with which cases each one broke. The record states that removing the KIND conjunct broke RED 2's case without breaking GREEN 1, GREEN 1b or GREEN 1c; that removing the `MODULE_OWN_CONFIG_POSITIONS` exclusion broke RED 2b's case without breaking GREEN 1 or RED 2's case; and that no two mutants broke the same case — a conjunct whose removal breaks nothing is a conjunct nobody tested, and two conjuncts that break the same case are one conjunct written twice.
    - `GOVERNANCE_CONFIG_CANDIDATE_KINDS` is exported and frozen, its cardinality equals `governanceConfigCandidates("")`'s, the index-for-index binding is asserted in both directions, and the transposition mirror fails; all four recorded as separate cases with their numbers.
    - Each kind is additionally asserted against a SHAPE PREDICATE over its own candidate's path segments (`in-kit` iff `agent-factory` then `config`; `repository-state-plane` iff `.grugops` directly beneath the base and no `agent-factory` segment), applied by ITERATING the candidate array rather than by naming its two members. The in-place-RENAME mirror is recorded: renaming a candidate path turns the shape binding RED while the index, cardinality and transposition bindings stay GREEN — the measurement that shows the new axis discriminates rather than duplicating one already asserted.
    - `MODULE_OWN_CONFIG_POSITIONS` is exported and frozen and its members are quoted verbatim in the summary beside `GOVERNANCE_FALLBACK_BASE`, so a reader can check the constant was derived from the running module rather than typed.
    - `git hash-object hooks/guard.ts` equals `FROZEN_GUARD_BLOB`; quoted.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts scripts/context-io-writer-set.test.ts scripts/compactor.test.ts</automated>
    <fails_when>non-zero exit, a summary line reporting `0 passed`, the `31-15` monotonicity mirror reporting an anchor whose occurrence count did not match exactly, or the stop-set equality case reporting a member present in one side only</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/floor-invariance.test.ts hooks/guard.test.ts hooks/admission-guard.test.ts scripts/checkpoints.test.ts</automated>
    <fails_when>non-zero exit, the frozen deploy-guard blob case failing, a `DECIDER_MANIFEST` mismatch, or the workflow stop-bullet count case failing with a number that was not deliberately moved</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or the build-parity step naming any committed `.js` that differs from a rebuild of its source</fails_when>
  </verify>
  <done>A repository rooted at the home directory reads its own dial; a bare `~/.grugops` at home is still not adopted; a kit's own configuration at a home candidate position never outranks a project nested below home; no artifact a caller can create in one operation moves the home verdict in either direction, driven for the two named artifacts and the one environment variable; the below-home residual and `31-15`'s closure are re-measured unmoved; and the published stop set and the workflow prose both describe the walk the code has.</done>
  <reversibility rating="costly">The stop set is a published authority the workflow prose is quoted from and every consumer's root resolution depends on. Reverting restores a walk the verifier measured skipping a home-rooted repository's own dial in favour of the kit's lean default.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The shape this plan does NOT adopt arrives as a named residual, and the decision is recorded (R-31-19-05, D-26)</name>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts, scripts/context-io-writer-set.test.ts, .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — CR-13's numbered reason 2 in full ("No residual names it"), which enumerates `R-31-19-01` through `R-31-19-04` and states that a configuration AT home is in none of them, plus the register's own contract that adding a member without dispositioning it turns a test red rather than shipping quietly; and the `Fix:` paragraph's closing sentence requiring an `R-31-19-05` member stating the cost.
    - `scripts/context-io.ts` — `TrustedRootResidual` and its four documented fields, `TRUSTED_ROOT_RESIDUALS` in full including the header explaining why it is a frozen export rather than a paragraph and the note that `R-31-15-02` is the CORRECT answer stated so a reader does not mistake it for a hole.
    - `scripts/context-io.test.ts` — the case asserting the round's written dispositions set-equal to the residual register, and the case asserting the register's cardinality separately from its members, so a new member arrives bound in both directions.
    - `scripts/context-io-writer-set.test.ts` — PART SIX-F/SIX-G/SIX-H (the axes `31-21` and `31-22` added), so the caller axis for the changed resolution functions is added in the same idiom.
    - `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` — D-23 in full (the decision this one sits beside and deliberately amends), plus D-24 and D-25 for the recorded FORM.
    - `docs/audit/31-round4-residuals.md` — the `R-31-19-01` row and its "a decision about which of the two the project wants" disposition, so this plan states what it did and did not decide about that row.
  </read_first>
  <behavior>
    - Test 1 (the residuals are occupied): `R-31-19-05`'s shape — a repository rooted at home carrying a configuration but NO version-control marker — is DRIVEN and its measured answer (the kit) matches the residual's own sentence; `R-31-19-06`'s two prices are driven by CONSTRUCTION with their operation counts recorded as numbers; `R-31-19-07`'s spelling gap is driven on this filesystem on BOTH axes its own shape sentence names — a case-differing spelling AND a SYMLINKED one — with each measured answer recorded whichever way it falls and priced in operations, because a residual measured on one of the two axes it claims is a residual half tested.
    - Test 2 (set-equality both ways): the round's written dispositions are set-equal to `TRUSTED_ROOT_RESIDUALS`, and the register's cardinality is asserted separately; both directions.
    - Test 3 (watched fail): a seeded extra residual arrives UNBOUND and moves the cardinality by exactly one, proving the set-equality is a control rather than a coincidence.
    - Test 4 (the caller axis): every caller of `trustedRepoRoot`, `projectRootFromWorkingDirectory`, `homeBoundary`, `isAboveHome`, `isHomeItself` and `homeConfigPositionIsProjectOwned`, and every reader of `GOVERNANCE_CONFIG_CANDIDATE_KINDS` and `MODULE_OWN_CONFIG_POSITIONS`, is derived from the source across files, asserted by MEMBERS and COUNT, with a PREMISE and a seeded mirror.
    - Test 5 (the consequence): an admission driven from the marker-less home shape writes no GOV-02 ledger line under the home directory.
  </behavior>
  <action>
    MOVEMENT 1 — add THREE residuals with all four fields. Add `R-31-19-05` to `TRUSTED_ROOT_RESIDUALS`:
    `shape` — a repository whose root IS the user's home directory and which carries its own governance
    configuration but NO version-control boundary marker is not adopted, and its dial is replaced by the
    kit's lean default; `reason` — a bare configuration position at home is not evidence of a project, and
    adopting one on that evidence alone is the WR-21 hole `31-19` closed; `what_would_force_it_closed` — an
    explicit opt-in the walk can read that a caller cannot author, and explicitly NOT the installer's own
    `install.json`, whose `InstallMarker` schema (`install/install.ts:597-620`) makes every field optional
    and names no target, so a caller satisfies it in one write.

    Then add `R-31-19-06`, which prices the two EVIDENCE conjuncts rather than describing a refusal, and
    which must carry NUMBERS rather than adjectives: `shape` — the home directory's adoption rests on two
    ordinary filesystem artifacts, so a process that can write under `$HOME` can MAKE home adoptable in
    three operations against a bare home (`mkdir $HOME/.git`; `mkdir $HOME/.grugops`; write
    `$HOME/.grugops/factory.config.json`) or one against a home that already carries a dotfiles checkout,
    and can make the walk MISS a governed root in one operation (`mkdir $HOME/work/.git`, an intermediate
    boundary carrying no configuration, after which the answer degrades to the kit's lean default);
    `reason` — these are the walk's own evidence applied identically at every directory, neither is
    introduced by this plan, and they sit inside the capability `R-31-15-01` already accepts and
    `31-22`'s rewritten `T-31-18-01` prices the same way; the alternative considered and REJECTED was to
    distinguish home by probing the filesystem under `$HOME`, which an adversarial re-check measured as a
    one-operation flip in BOTH directions and which would therefore have handed the caller a switch rather
    than taken one away; `what_would_force_it_closed` — `R-31-15-01`'s own criterion, a governance root the
    calling process cannot influence at all, delivered through a channel the agent cannot write.

    Then add `R-31-19-07`, the gap the module-constant route leaves: `shape` — the exclusion of the running
    module's own candidate positions compares lexically resolved path SPELLINGS, so on a case-insensitive or
    symlinked filesystem the module's own position and the candidate the walk computes can spell the same
    directory differently and the exclusion misses; `reason` — the alternative is a `dev:ino` comparison,
    which is a `statSync` on paths under `$HOME` that a single symlink can make agree, a one-operation flip
    in the gate-lowering direction and therefore the defect this member exists instead of; the same
    reasoning `R-31-19-03` records one register over, taken deliberately rather than inherited;
    `what_would_force_it_closed` — a canonical spelling for both sides obtained from a resolver the caller
    cannot re-point, or the module's own position learned from the host rather than from
    `import.meta.dirname`. Write all three sentences in the register's own voice — the situation, the
    argument, and a criterion — never an assurance.

    MOVEMENT 2 — bind the register in both directions and record the numbers. Re-run the set-equality
    assertion against the round's written dispositions, assert the register's cardinality separately, and
    confirm the watched-fail control still moves it by exactly one and reports the seeded member as
    unbound. Record the cardinality before → after (8 → 11, three members added). Drive all three shapes as
    cases so each member is OCCUPIED — a residual nobody can reach is a residual nobody has tested.
    `R-31-19-06` in particular is occupied by CONSTRUCTION rather than by description: the case performs the
    three named operations against a bare planted home, records the operation count as a number, and shows
    the verdict moving from refused to adopted, and a second case performs the one named operation at an
    intermediate directory and shows the answer degrading to the kit. A priced residual whose price is not
    measured is an adjective.

    MOVEMENT 3 — add the caller axis for the changed resolution surface. In
    `scripts/context-io-writer-set.test.ts`, add PART SIX-I: derive across files every caller of
    `trustedRepoRoot`, `projectRootFromWorkingDirectory`, `homeBoundary`, `isAboveHome`, `isHomeItself`,
    `homeConfigPositionIsProjectOwned`, `GOVERNANCE_CONFIG_CANDIDATE_KINDS` and
    `MODULE_OWN_CONFIG_POSITIONS`,
    resolving import aliases exactly as PART SIX-D already does. Assert MEMBERS and COUNT, add a PREMISE
    case asserting the parse found the declarations, and add a seeded mirror carrying one extra caller that
    moves the count by exactly one. This is the axis the round-5 verifier's own note names as missing —
    none of the five new Critical classes sits inside any axis this test currently derives.

    MOVEMENT 4 — assert the consequence, not only the resolution. Drive an admission from the marker-less
    home shape and assert that no `.grugops/audit/admissions.jsonl` was created under the home directory —
    the blast radius that makes a wrong root more than a wrong dial.

    MOVEMENT 5 — record D-26. APPEND a `#### Gap-closure decision — D-26 (2026-09-09, gap-closure round 5,
    plan 31-23)` block to `31-CONTEXT.md` in the recorded form of D-23, D-24 and D-25, with
    `**Forced by:** CR-13 of 31-REVIEW.md, independently reproduced by 31-VERIFICATION.md round 5
    (behavioral spot-check row 8 with its control at row 9, the regressions entry for CR-13, the key-link
    row recording the walk MISWIRED, and the anti-patterns row at ~2598-2606)`. State which register failed
    — not the bound's EXISTENCE, which D-23 established, and not which markers the set contains, but
    WHETHER A BOUND ON ASCENT MAY ALSO BOUND OBSERVATION. State plainly that D-26 AMENDS D-23's sentence
    "never inspects it or anything above it": the ascent halt stays, the blanket non-inspection does not,
    and D-23 is not edited — the amendment is recorded here as its own dated decision. Give the THREE
    numbered sub-decisions — and there are now FOUR. (1) The split predicate with inspect-then-decide
    ordering. (2) Home adopted only as a repository, never as a bare configuration position, with
    `R-31-19-05` naming the cost. (3) The candidate carrying home's configuration required to be the
    repository STATE-PLANE position rather than the IN-KIT position, because D-23 (4)'s boundary-wins rule
    cannot reach the one directory where the walk ends, and required not to be one of the running module's
    OWN fallback candidate positions, because the kit this reader ships in is not a project. (4) EVERY input
    to the home rule beyond the walk's ordinary evidence must be a path, a position or a load-time constant
    — no filesystem probe under `$HOME`, no environment read — and the ordinary evidence is PRICED in
    operations as `R-31-19-06` rather than defended.

    State plainly that sub-decisions (3) and (4) were forced by TWO successive adversarial re-checks of THIS
    PLAN before it executed, and record BOTH turns rather than only the second. The first found that
    marker-plus-configuration alone lets a kit's own configuration govern a project. The draft that answered
    it did so with two `existsSync` probes under `$HOME/.grugops`, and the second measured both as
    caller-authorable in ONE operation, in OPPOSITE directions: creating `<kitHome>/agent-factory` turned an
    adoption into a refusal — whose answer is the kit's lean fallback dial, so CR-13's own harm returns —
    and creating `<kitHome>/install.json` turned a refusal into an adoption. Reading that marker through its
    schema is the same one-write flip (`install/install.ts:597-620`: every field optional, no target named,
    `{}` valid). So the round-history law of this phase was interrupted TWICE inside the plan rather than
    once after it, and sub-decision (4) is the rule written so a third turn has nothing to find.

    Include the "What D-26 does NOT establish" block naming `R-31-19-05`, `R-31-19-06`'s two priced
    caller-created states with their operation counts, `R-31-19-07`'s lexical-spelling gap, `R-31-19-01`'s
    still-open below-home question, the DECIDED adoption of the dotfiles-plus-shared-install tree and why it
    is the same answer the walk gives for that tree one level below home, the fact that `GRUGOPS_HOME` is
    read NOT AT ALL and that treating it as additive-only was itself unsound because a refusal at home lands
    on the lean fallback, and the fact that the review's own sketch was not adopted verbatim and why.
    `Reversibility: costly`. Close with the three places that must agree. APPEND ONLY.
  </action>
  <acceptance_criteria>
    - `R-31-19-05`, `R-31-19-06` and `R-31-19-07` each exist with all four documented fields populated, and each `shape` sentence is quoted verbatim in `31-23-SUMMARY.md`.
    - `R-31-19-07`'s occupancy case records a measured verdict for EVERY axis its `shape` sentence names — the case-insensitive spelling AND the symlinked spelling — each priced in operations, with the verdict stated either way as the expected one. A `shape` sentence naming two axes and an occupancy case driving one is recorded as a FAIL, not as a pass.
    - All three are OCCUPIED: a driven case reproduces each shape and the measured answer matches the member's own sentence. `R-31-19-06` is occupied by CONSTRUCTION — the three named operations against a bare planted home, and the one named operation at an intermediate directory — with both operation counts recorded as numbers and both verdict movements quoted.
    - `TRUSTED_ROOT_RESIDUALS`'s cardinality is recorded before → after as numbers (8 → 11), and the set-equality assertion passes in both directions.
    - The watched-fail control moves the cardinality by exactly one and names the seeded member as unbound.
    - The new caller axis reports MEMBERS and COUNT as numbers, has a PREMISE that fires on a renamed declaration rather than passing vacuously, and its seeded mirror moves the count by exactly one.
    - The consequence case asserts NO ledger file under the home directory after an admission driven from the marker-less home shape.
    - D-26 states explicitly that it AMENDS D-23's non-inspection sentence, and that D-23 itself is left byte-unchanged; both facts are checkable from `git diff {round-base}..HEAD -- .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md`, pinned to this round's base commit, which shows additions only.
    - D-26 states why the review's `Fix:` sketch was not adopted verbatim, naming the `~/.grugops` adoption it would have caused.
    - D-26 carries FOUR numbered sub-decisions and records BOTH adversarial re-checks by name, including the two artifacts the second one used, the direction each flipped the verdict, and the `install/install.ts:597-620` schema citation that disqualifies reading the installer's marker at all.
    - `grep -c '^#### Gap-closure decision — D-' .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` is recorded before and after and differs by exactly one.
    - The decision text mirrors into the resolution-order docstring and `31-23-SUMMARY.md`; all three are quoted side by side.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts scripts/context-io-writer-set.test.ts</automated>
    <fails_when>non-zero exit, the residual set-equality case reporting a member on one side only, a PREMISE assertion reporting a derivation over zero elements, or the caller-axis seeded mirror failing to move the count by exactly one</fails_when>
    <automated>node scripts/check-foundation-guards.js &amp;&amp; node scripts/check-uat-oracles.js</automated>
    <fails_when>either command exits non-zero or prints anything other than `ALL CHECKS PASSED`</fails_when>
    <automated>npm run build &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or the freshness line reporting anything other than `All build outputs fresh: 60 committed .js file(s) match a rebuild of their sources.`</fails_when>
  </verify>
  <done>The shape this plan deliberately leaves open is a named, occupied, two-sidedly-bound register member; the changed resolution surface has a derived caller axis; and D-26 records the amendment to D-23 without editing it.</done>
  <reversibility rating="reversible">The residual and the caller axis are additive records behind a green suite; removing either turns the set-equality or the PREMISE red immediately.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Red-team THIS plan's own fix — the phase's standing probes against the changed walk</name>
  <files>scripts/context-io.test.ts, scripts/context-io-writer-set.test.ts</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the "Gaps Summary" closing paragraphs, and specifically the sentence naming CR-13 as "the home stop's precedence over inspection is a second axis from the ancestor-below-home case WR-21 closed" — this task's job is to find the THIRD axis before the next round does.
    - `docs/audit/31-round4-residuals.md` — §4.3 in full and §6.2, so each probe asserts its own premise before its conclusion and so the two §4.3 readings are both driven.
    - `scripts/context-io.ts` — every consumer of `trustedRepoRoot()` in this module (`admit`, `admitAndAppend`, `appendNote`'s default, `promoteAdmitted`'s default, `readGovernanceConfig`'s base, `appendAuditLedger`'s location) and the `TRUSTED_ROOT_ENV_ORDER` loop.
    - `hooks/guard.ts`, `hooks/admission-guard.ts`, `scripts/admission-server.ts`, `scripts/compactor.ts` — the cross-file consumers of the one trusted-root answer, so the caller enumeration crosses the file boundary.
  </read_first>
  <action>
    PROBE 1 — HOW IS THIS GATE REACHED. Enumerate every consumer of `trustedRepoRoot()` across files from
    the source, and re-run each with a LEGITIMATE ordinary project — a repository well below home carrying
    its own dial — against the rebuilt `.js`. A walk change that alters an ordinary project's answer is a
    regression this task must find, not the next verification round.
    FAILS WHEN: any enumerated consumer resolves to a different root than it did against the pre-fix committed `.js` for an ordinary project, or the derived consumer count differs from PART SIX-I's recorded count without a stated reason

    PROBE 2 — DERIVE BOTH AXES. The axis that moved is inspection-versus-ascent AT home. Name and drive the
    others: (a) inspection-versus-ascent at a STRICT ANCESTOR of home — is a configuration there still never
    read; (b) the marker-versus-configuration precedence AT home versus at an ordinary directory — the two
    rules now differ and the difference must be asserted; (c) the `nearest` accumulator — can any directory
    the walk refuses to adopt still contribute to it; (d) the identity-versus-spelling axis — does a
    symlinked home behave identically under both predicates; (e) the CALLER-AUTHORABLE axis, which is the axis
    this plan's second re-check turned on — for EACH of the two candidate positions
    `governanceConfigCandidates` enumerates, drive the tree at home across the full cross-product of every
    artifact and value the RETRACTED design would have consulted: `<home>/.grugops/agent-factory` present or
    absent × `<home>/.grugops/install.json` absent, present-and-empty, or holding `{}` × `GRUGOPS_HOME`
    unset, redirected elsewhere, empty, or set to `$HOME` — and print all 48 cells with their verdicts. The
    required result is that the verdict varies with the candidate POSITION and with NOTHING ELSE, so the
    table is 24 identical cells beside 24 identical cells; (f) the CONVERSE of (e) — is the position test
    asked anywhere OTHER than at home, and if not, can an ordinary ancestor directory reach the same
    collision (state the answer as a decision or a residual) — and drive the module-own-position exclusion
    across BOTH SPELLING AXES `R-31-19-07`'s own shape sentence names, since a residual measured on one of
    the two axes it claims is a residual half tested. Cell 1, the CASE axis: `HOME` set to
    `GOVERNANCE_FALLBACK_BASE` and then to a case-differing spelling of it. Cell 2, the SYMLINK axis, which
    is a one-operation construction on every filesystem this kit targets: `ln -s <checkout> <probe>/link`,
    then `HOME=<probe>/link` with the origin and cwd addressed through that same link. This cell is where
    the divergence actually lives — Node resolves `import.meta.dirname`, and therefore
    `MODULE_OWN_CONFIG_POSITIONS`, through the symlink to the REAL path, while the walk's `resolve()` at
    `scripts/context-io.ts:3056` is purely LEXICAL and preserves the link spelling, so the two sides can name
    one directory with two strings and the exclusion's path equality misses. PROBE 3's symlinked `HOME`
    does NOT cover this: it exercises `homeBoundary`'s identity set, not the
    `MODULE_OWN_CONFIG_POSITIONS` equality. Record each cell's measured verdict on this filesystem rather
    than reasoning about it, and state the verdict EITHER WAY as the expected one — an exclusion that holds
    under a spelling is a measurement worth having, and an exclusion that misses is `R-31-19-07` occupied.
    Price whichever answer arrives: if the exclusion misses, state in operations what it costs a caller to
    reach it (the one `ln -s`, plus whatever it takes to address the walk through the link) and what the
    consequence is — the running kit's own configuration governing a project nested below it, which is
    RED 2b's harm reached through a spelling rather than through a position.
    FAILS WHEN: any of the six axes produces an answer the plan did not decide, (f) drives fewer than BOTH of its spelling cells or leaves either the case cell or the symlink cell unrecorded or unpriced, (c) shows a refused directory contributing to `nearest`, or ANY pair of cells in (e) that share a candidate position disagree — that is, any cell in which a filesystem artifact or an environment value the calling agent can author moved the verdict in EITHER direction, an adoption becoming a refusal counting exactly as much as a refusal becoming an adoption

    PROBE 3 — WHAT IS THE PREDICATE'S INPUT ASSEMBLED FROM. `homeBoundary()` is assembled from
    `os.homedir()`, `realpathSync` and a bounded ancestor loop. Drive: `HOME` unset; `HOME` set to a
    non-existent path; `HOME` set to a file rather than a directory; `HOME` set with a trailing separator;
    `HOME` set to a symlink; and `HOME` equal to the filesystem root. Record the resolved root and the dial
    for each, and state each as a decision or a named residual.
    FAILS WHEN: any of the six spellings produces an unbounded walk, an unnamed exception, or an adoption of a directory the plan did not decide to adopt

    PROBE 4 — AT WHICH POSITIONS IS THE PREDICATE EVEN ASKED. The home predicates are asked inside the
    walk. Ask whether any OTHER position in the module reaches a governance configuration without going
    through this walk — the `TRUSTED_ROOT_ENV_ORDER` env arms, `readGovernanceConfig`'s explicit `repoRoot`
    argument, and every test seam — and state for each whether a home-directory configuration can be
    reached that way.
    FAILS WHEN: any position can reach a home-directory configuration without the walk's rules being asked, and without that being recorded as an existing residual or a new one

    PROBE 5 — WHAT DOES THIS CHANGE DO TO A PRE-EXISTING STATE. Drive the whole existing case set against
    the pre-fix and post-fix programs and print a verdict-by-verdict diff. Any case whose verdict moved
    must be either a case this plan intended to move (row 8, and the adversarial shape) or a defect.
    Include `31-22`'s ORIGIN-RECOGNITION cases explicitly in that set: `31-22` made `originIsTrusted`
    consume `projectRootFromWorkingDirectory`, and this plan rewrites that walk, so the shaped-forgery
    refusal, the cross-repository control, the constructed-governance-root residual case and BOTH
    HOME-ROOTED ORIGIN-STORE rows (`31-22` CONTROL 5a and CONTROL 5b) are downstream of this change and must
    be re-driven here rather than assumed stable from wave 2.

    Because ONE of those verdicts is expected to move across the wave, the movement is DECLARED rather than
    tolerated. Build a CROSS-PLAN INTENDED-CHANGE LIST as an explicit named set in the test source, and
    assert BOTH its cardinality and its members before the diff is read: it has EXACTLY ONE member,
    `31-22` CONTROL 5a — the home-rooted project promoting from its own `$HOME/.grugops/context` on a machine
    carrying a shared install — whose declared movement is DECLINE (`origin-outside-trusted-store`) at wave 2
    to PROMOTE at wave 3. Its wave-2 verdict is a decline because the pre-fix walk asks `isAtOrAboveHome`
    before it inspects, returns `nearest` at step 0 and lands on `GOVERNANCE_FALLBACK_BASE`, so the origin's
    grandparent is not the resolved root; `31-22`'s corpus states that same wave-2 decline and its measured
    `trustedRepoRoot()` evidence for it, and the two plans state the same wave-2 answer or one of them is
    wrong. Its wave-3 verdict is a promotion because this plan's home rule adopts a home carrying a marker
    and a state-plane configuration that is not one of `MODULE_OWN_CONFIG_POSITIONS`. Assert the set's SIZE
    as the number 1 and its single member BY NAME as separate assertions, and derive the MOVED set from the
    diff rather than reading it, so a second movement cannot be absorbed into the list silently. `31-22`
    CONTROL 5b is the UNMOVED control 5a's movement is read against: DECLINE at both waves, because home
    carries no marker and never contributes to `nearest`. Every other `31-22` origin-recognition case is
    outside the list and may not move in either direction.
    FAILS WHEN: any case's verdict moved from refused to admitted other than the single declared member, any case's verdict moved at all without appearing in this plan's intended-change list, the cross-plan intended-change list has a cardinality other than 1 or a member other than `31-22` CONTROL 5a, `31-22` CONTROL 5a FAILS TO MOVE — measuring a decline post-fix, or a promotion pre-fix — any `31-22` origin-recognition case OTHER than CONTROL 5a moved in either direction, `31-22` CONTROL 5b moved at all, or either 5a or 5b measures a wave-2 verdict other than the one `31-22`'s corpus states

    PROBE 6 — RE-DRIVE §4.3's TWO READINGS SO THE `UNKNOWN - verify` STOPS BEING ONE. Drive both the
    ORIGINAL spelling (a planted ancestor merely NAMED `home`, real `HOME` untouched) and the ADJUSTED
    spelling (`HOME` overridden so the planted ancestor IS `os.homedir()`) against the post-fix `.js`, and
    record both answers so `31-26` can resolve the item by name rather than carrying it a second round.
    FAILS WHEN: either spelling is not driven, or the ORIGINAL spelling's answer is recorded as anything other than the documented `R-31-19-01` behaviour without a stated decision

    Record every probe, its command, its measured output and its verdict in `31-23-SUMMARY.md` under a
    heading naming this task. Any defect a probe finds is fixed IN THIS PLAN, not deferred. Restore the
    real `HOME` and delete `.temp/31-23-probe/`, confirming with `test ! -e .temp/31-23-probe` and the FIFO
    sweep rather than with `git status --short .temp`.
  </action>
  <acceptance_criteria>
    - All six probes are recorded with the exact command, the measured output and an explicit PASS/FAIL verdict; no probe is recorded as "expected to pass".
    - PROBE 1's consumer set is DERIVED across files and its cardinality is stated as a number.
    - PROBE 2's six axes are each printed with the driven input and the measured answer; (e)'s 48 cells are printed in full, the count is stated as a number, and the table shows exactly two distinct verdicts partitioned by candidate POSITION — no cell in which a caller-authorable artifact or environment value moved the verdict in either direction.
    - PROBE 2(f) prints BOTH spelling cells `R-31-19-07`'s shape sentence names — the case-differing `HOME` and the SYMLINKED `HOME` (`ln -s <checkout> <probe>/link`) — each with its driven input, its measured verdict on this filesystem and its price in operations. The summary states which cell, if either, made the `MODULE_OWN_CONFIG_POSITIONS` equality miss, states the verdict either way as the expected one, and states in one sentence why PROBE 3's symlinked `HOME` does not cover this cell (it exercises `homeBoundary`'s identity set, not the position equality).
    - PROBE 3's six `HOME` spellings are printed as a table with the resolved root, the dial and a decision-or-residual label.
    - PROBE 4 enumerates every configuration-reaching position by line number with a stated disposition.
    - PROBE 5 prints a verdict-by-verdict diff and states the number of cases driven and the number that moved; the moved set equals this plan's intended-change list exactly, and `31-22`'s five origin-recognition cases — the shaped-forgery refusal, the cross-repository control, the constructed-governance-root residual case and the two home-rooted origin-store rows CONTROL 5a and 5b — are listed among the driven set with their pre-fix verdicts matching the WAVE-2 verdicts `31-22`'s corpus states.
    - The CROSS-PLAN INTENDED-CHANGE LIST is asserted as a set before the diff is read: its cardinality is stated as the number 1 and its single member is named as `31-22` CONTROL 5a with its declared movement DECLINE (`origin-outside-trusted-store`) -> PROMOTE. The MOVED set is DERIVED from the diff rather than read, and the two are asserted equal, so a second movement cannot be absorbed into the list silently.
    - CONTROL 5a is recorded as having MOVED, in the declared direction, with both cells quoted — pre-fix DECLINE, post-fix PROMOTE — and a 5a that failed to move in either cell is recorded as a FAIL, never as a pass. The other four `31-22` origin-recognition cases, CONTROL 5b among them, are recorded as UNMOVED in both directions.
    - PROBE 6 records both §4.3 readings' answers against the post-fix artifact.
    - Any defect found by any probe is FIXED in this plan and re-probed; the summary states explicitly whether any probe found a defect, and "none" is only acceptable when all six probes ran.
    - The real `HOME` is restored; `test ! -e .temp/31-23-probe` succeeds, `find . -path ./node_modules -prune -o -type p -print` prints nothing, and `git status --short` shows no tracked-tree residue. The summary states that `git status --short .temp` is not the check and why.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, the summary reporting fewer than 62 test files, or any test file reporting `0 passed`</fails_when>
    <automated>node scripts/check-foundation-guards.js &amp;&amp; node scripts/check-uat-oracles.js &amp;&amp; npm run freshness</automated>
    <fails_when>any of the three exits non-zero, either guard prints anything other than `ALL CHECKS PASSED`, or the freshness line reports fewer than 60 fresh committed `.js` files</fails_when>
    <automated>test ! -e .temp/31-23-probe &amp;&amp; test -z "$(find . -path ./node_modules -prune -o -type p -print)" &amp;&amp; git status --short</automated>
    <fails_when>non-zero exit — `.temp/31-23-probe` survived, or the FIFO sweep printed a path — or the `git status` output lists a planted tree inside the tracked working copy or any probe `.mjs` at the repository root</fails_when>
  </verify>
  <done>All six standing probes ran against this plan's own walk change with recorded commands and outputs; §4.3's `UNKNOWN - verify` has a measurement for both readings; every defect found is fixed inside this plan.</done>
  <reversibility rating="reversible">The probes are measurements recorded in a summary; nothing about them constrains a later change except the record they leave.</reversibility>
</task>

</tasks>

<verification>
- `31-VERIFICATION.md` behavioral spot-check row 8 resolves to the planted repository and reads `high-severity`; the self-stamped finding is refused there.
- Row 9 (`R-31-19-01`, the below-home control) and `31-15`'s own spot-check are re-measured unmoved.
- The round-4 §4.3 ADJUSTED spelling still resolves to the kit — WR-21 is not reopened.
- `isAtOrAboveHome` is gone; `isAboveHome` and `isHomeItself` are separate predicates and the walk inspects before it asks about climbing.
- A kit's own configuration at a home candidate position — vendored at `$HOME/agent-factory/config/`, or the running module's own checkout at `$HOME` — never outranks a configured project nested below home; both shapes driven, both mutation-proven against the marker-plus-configuration implementation.
- No artifact a caller can create in one operation, and no value of `GRUGOPS_HOME`, moves the home verdict in either direction: `mkdir -p $HOME/.grugops/agent-factory`, `touch $HOME/.grugops/install.json`, that file holding `{}`, and the four-value environment sweep are each driven verbatim with the verdict asserted UNCHANGED, and PROBE 2(e)'s 48 cells show exactly two verdicts partitioned by candidate position.
- `TRUSTED_ROOT_STOP_CONDITIONS` moves 6 → 7, `S-HOME-SELF` carries all three conjuncts, and `agent-factory/workflows/16-context-read-write.md` is asserted set-equal in both directions.
- `R-31-19-05`, `R-31-19-06` and `R-31-19-07` all exist, are occupied by driven cases — `R-31-19-06` by construction with its operation counts as numbers, `R-31-19-07` on BOTH the case and symlink axes its shape sentence names — and the register moves 8 → 11 with the watched-fail control intact.
- D-26 is appended and states that it amends D-23; D-23 is byte-unchanged.
- Full excluded-e2e suite green, freshness at 60/60, foundation guards and UAT oracles `ALL CHECKS PASSED`, `hooks/guard.ts` byte-frozen, `package.json` byte-unchanged.
</verification>

<success_criteria>
- CR-13 closed with a RED-first reproduction quoted verbatim and a GREEN measurement beside it, in a six-spelling × three-program table (committed `.js`, a scratch marker-plus-configuration implementation, post-fix) — the middle column being what shows each added conjunct is load-bearing rather than defensive.
- The blocker the second adversarial re-check raised is closed at the SHAPE of the rule rather than by another probe: every input to the home rule beyond the walk's ordinary evidence is a path, a position or a load-time constant, asserted by four INVARIANCE cases and by PROBE 2(e)'s 48-cell table, and the ordinary evidence is priced in operations as `R-31-19-06`.
- WR-21's own closure and `R-31-19-01` re-measured intact; the monotonicity sweep shows no case moved from refused to admitted.
- One new DERIVED axis (the resolution-surface caller set) with PREMISE, MEMBERS, COUNT and a seeded mirror.
- Six self-red-team probes run against this plan's own walk change, including both §4.3 readings.
- D-26 appended in the recorded form of D-17..D-25, cited by id in this plan's `must_haves.truths`.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-23-SUMMARY.md` when done.
</output>

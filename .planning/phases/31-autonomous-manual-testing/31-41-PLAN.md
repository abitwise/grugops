---
phase: 31-autonomous-manual-testing
plan: 41
type: execute
wave: 10
depends_on: ["31-40"]
gap_closure: true
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/context-io-writer-set.test.ts
  - agent-factory/workflows/18-context-compaction.md
  - docs/audit/29-style-dispositions/31-41.md
  - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
autonomous: true
requirements: [UATX-01, UATX-04]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-40 and the third `artifacts:` row of `31-VERIFICATION.md` round 8's gap: `agent-factory/workflows/18-context-compaction.md` states a UNIVERSAL entry-derivation property — `each route derives the owning repository from the context store it writes the note into`, `both halves of the action key on that one answer`, `the derivation sits at the route's entry, above every branch that route takes` — which round 8 measured FALSE of `admitAndAppend`'s ungoverned-store case and SILENT about the fall-through's dial routing. This is the claim-outrunning-the-mechanism shape the phase exists to close, recurring in the very document the phase edited to close it, for the second consecutive round."
    - "A claim is corrected by MOVING THE MECHANISM where the mechanism can move, and by STATING THE MECHANISM PER ROUTE AND PER BRANCH where it cannot. `31-39` moved the mechanism. This plan makes the paragraph say what the mechanism now does — per route, per branch, with every remaining residual named inline — rather than restating a universal that a ninth round would measure false again."
    - "WR-39: the published residual `R-31-33-02` describes the PRE-FIX mechanism. Its text states that with no arguments the GOV-02 event lands under the host repository, which stopped being true when round 7 moved the gated branch's derivation, and is again different after `31-39`. A residual is the artifact the next round starts from; one that describes a mechanism that is not in the tree hands that round a false premise — the class `docs/audit/harness-false-result-instances.md` exists to record."
    - "`R-31-33-01` is NARROWED rather than deleted. It named the `admit()` unfreeze as the only thing that closes its class, and `31-39` performed that unfreeze — so the part of it that is closed is recorded as closed with the measurement, and the part that REMAINS (whatever a direct caller can still diverge after the two parameters were separated) is stated with its own reproduction rather than inherited by implication."
    - "WR-42: moving the destination decline above `promoteAdmitted`'s fall-through WIDENED the refused input set — a destination outside a governed repository was accepted on that path and is now refused — and `WRITE_PATH_RESIDUALS` records no member for it, while round 7's own fixtures record the consequence by re-staging cases that a bare directory now refuses. The widened refusal is published as a member, and the shared-install shape the review flagged `UNKNOWN - verify` is MEASURED rather than reasoned about: whether a kit-side store resolves to a governed root at all, and whether any shipped flow promotes into one."
    - "The refusal WR-42 names is D-31's own named decline, moved above the fall-through by D-34 (1) and left unrecorded in the register D-31 created. So this plan does not re-decide it — D-31's REJECTED ALTERNATIVE stands, and D-39 restored the dial it was always supposed to read — it records the input set the move widened, with the shape most likely to meet it measured rather than argued."
    - "Every published register in this module keeps its two-sided binding: the dispositions written in `31-CONTEXT.md` are asserted set-equal to the exported array in BOTH directions with an asserted cardinality, so an undispositioned member turns a test red rather than shipping."
    - "Every clause changed in `agent-factory/workflows/18-context-compaction.md` carries a row in `docs/audit/29-style-dispositions/31-41.md` with its before and after text, because the disposition gate reads that directory on every run and refuses a changed clause with no row."
    - "The binding that asserts the corrected paragraph names exactly the routes the derivation returns is RE-RUN against the rewritten paragraph, and its SCOPE is checked rather than assumed: a section-anchored reader searching past its own section adopts an unrelated later block, which is a defect this project has already measured in a different guard."
    - "UATX-01 through UATX-06 stay UNCHECKED in `.planning/REQUIREMENTS.md`, the Phase 31 checkbox stays unchecked in `.planning/ROADMAP.md`, and every traceability row still reads `Gaps Found`."
  artifacts:
    - scripts/context-io.ts
    - scripts/context-io.js
    - scripts/context-io.test.ts
    - agent-factory/workflows/18-context-compaction.md
    - docs/audit/29-style-dispositions/31-41.md
    - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
  key_links:
    - "`agent-factory/workflows/18-context-compaction.md`'s ledger paragraph <-> the measured behaviour of every named route and every branch of each: the claim-follows-mechanism binding this phase has re-established every round and had measured false twice running."
    - "`WRITE_PATH_RESIDUALS` <-> the dispositions written in `31-CONTEXT.md`: two-sided set equality with an asserted cardinality, so a new residual cannot arrive undispositioned and a deleted disposition cannot leave a cited id dangling."
    - "`R-31-33-01`'s remaining half <-> its own reproduction: the part the unfreeze did not close is stated with a measurement rather than inherited from the pre-unfreeze text."
    - "the shared-install kit-side store <-> `governanceRootOf`'s answer for it: a measurement rather than an `UNKNOWN - verify`, because the refusal the round widened lands on exactly that shape."
  prohibitions:
    - statement: "A claim is never made true by weakening the guarantee it describes. Where the mechanism cannot be moved, the claim is narrowed to what the mechanism does and the remainder is NAMED, never quietly dropped."
      status: unverified
      verification: flagged
    - statement: "A published residual must describe the mechanism that is in the tree. A residual stating a pre-fix behaviour is a false premise handed to the next round, and re-wording it is not optional."
      status: unverified
      verification: flagged
    - statement: "Every clause changed in the watched corpus carries a disposition row. The disposition directory's membership is walked, never listed, and one file per plan named for the plan."
      status: unverified
      verification: flagged
    - statement: "A gap-closure plan must never flip a requirement checkbox, a traceability row or the phase checkbox."
      status: unverified
      verification: flagged
    - statement: "Never fake a passing gate, a test result, or a citation. A shape that could not be measured on this box is marked `UNKNOWN - verify` and carried with an owner, never asserted."
      status: unverified
      verification: flagged
    - statement: "Never merge a protected branch and never deploy to production without a named human confirmation."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-39-PLAN.md and 31-40-PLAN.md: the dial is answered from the trusted root, no consumer carries a fallback, and every divergence carries a register disposition. This plan documents what those two established and names what they did not."
    - "31-33-PLAN.md / D-34: the workflow sentence is corrected by moving the mechanism. That rule stands; this plan applies its second half — where the mechanism cannot move, the claim states what it does per branch."
---

<objective>
Make every published claim about the write path equal to what the write path now does, per route and
per branch, and give every boundary this round leaves open a register entry with a reproduction.

Three Warnings and one `artifacts:` row, all one shape:

- **WR-40 / the workflow paragraph** — states a universal entry-derivation property that round 8
  measured false of one branch and silent about another.
- **WR-39 / `R-31-33-02`** — a published residual describing the pre-fix mechanism, which is a false
  premise handed to the next round.
- **WR-42 / the widened refusal** — an input set that moved, recorded nowhere, with the shape most
  likely to meet it (`~/.grugops` kit beside a separate host repository) left `UNKNOWN - verify`.
- **`R-31-33-01`** — the residual that named the `admit()` unfreeze as the cost of finishing the fix.
  `31-39` paid it, so the residual's closed half and its remaining half must be separated and each
  stated with its own measurement.

Purpose: a residual and a workflow sentence are the two artifacts the ninth round will start from. If
either describes a mechanism that is not in the tree, the round begins from a false premise — which is
exactly how this phase has spent eight rounds.
Output: a per-route, per-branch ledger paragraph with its disposition rows; a re-worded
`R-31-33-02`; a narrowed `R-31-33-01` with its remaining half measured; new register members for the
widened refusal and this round's own residuals; the shared-install shape measured; decision `D-41`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@.planning/phases/31-autonomous-manual-testing/31-REVIEW.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-39-SUMMARY.md
@.planning/phases/31-autonomous-manual-testing/31-40-SUMMARY.md
@scripts/context-io.ts
@agent-factory/workflows/18-context-compaction.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Note |
|---|---|---|
| `R-31-33-01` | RE-WORDED register entry | Split into the half `31-39`'s unfreeze CLOSED (with the measurement that closed it) and the half that REMAINS (with its own reproduction). The id is kept so every prior citation still resolves. |
| `R-31-33-02` | RE-WORDED register entry | Stated against the mechanism that is in the tree after `31-39`, per branch, with the open decision (which repository the no-argument default names) left open and unchanged. |
| `R-31-41-01` | NEW register entry | The widened refusal set at `promoteAdmitted`'s fall-through (WR-42), with the shared-install measurement and its answer. |
| `R-31-41-02` | NEW register entry | Whatever boundary this round's own three fix plans leave open, recorded rather than discovered by round 9. The executor names it from `31-39-SUMMARY.md` and `31-40-SUMMARY.md` rather than inventing one. |
| `agent-factory/workflows/18-context-compaction.md` ledger paragraph | REWRITTEN prose | Per route, per branch, with each remaining residual named inline. |
| `docs/audit/29-style-dispositions/31-41.md` | NEW disposition file | One row per changed clause, with before and after text, under a `## Dispositions` heading. |
| `D-41` | NEW dated decision | Appended to `31-CONTEXT.md`, including the residual dispositions bound two-sided to the register. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The write-path residual register, re-stated against the mechanism that is in the tree</name>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts</files>
  <precondition>`31-39` and `31-40` are committed and `npm run freshness` reports every committed output fresh. Every re-worded residual states what the tree DOES, so it must be written against the post-`31-39` module and not against the text it replaces.</precondition>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — WR-39 in full (its quotation of `R-31-33-02`'s text and the reason a stale residual is the same class as a false harness premise) and WR-42 in full (the widened refusal, the fixtures round 7 re-staged as evidence the accepted-input set moved, and the shared-install reasoning ending in `UNKNOWN - verify`).
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — gap 1's first `missing:` bullet, whose alternative branch requires a `WRITE_PATH_RESIDUALS` member carrying an exact reproduction; and the Required Artifacts table's three `scripts/context-io.ts` rows.
    - `scripts/context-io.ts` — the `WritePathResidual` interface and the whole `WRITE_PATH_RESIDUALS` array in full, every entry, because the two-sided binding is over the whole array; the `ROOT_DIVERGENCE_DISPOSITIONS` register `31-40` added; and `appendNote`, `admit`, `admitAndAppend` and `promoteAdmitted` as `31-39` left them, because each re-worded entry must name the branch it is about.
    - `scripts/context-io.test.ts` — the case that asserts `WRITE_PATH_RESIDUALS` set-equal to the dispositions written in `31-CONTEXT.md` in both directions with its asserted cardinality, and the case that asserts one residual's reason mentions the freeze constant. Both must be updated deliberately, not left to fail.
    - `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` — the residual-disposition bullet lists inside the `D-31` and `D-34` blocks, which are the written side of the two-sided binding.
    - `.planning/phases/31-autonomous-manual-testing/31-39-SUMMARY.md` and `31-40-SUMMARY.md` — the `every boundary this plan leaves open` sections, because `R-31-41-02` is named from those rather than invented.
    - `install/install.ts` — the code that seeds a governance configuration into the install target, and where it does not seed one, because the shared-install measurement in Task 2 turns on exactly that.
  </read_first>
  <behavior>
    - RED: read `R-31-33-02`'s published text and MEASURE its claim against the post-`31-39` module with a probe that makes no arguments at all, recording where the note lands and where the record lands. Record whether the published sentence is true, false, or unobservable on this box, and say which.
    - RED: read `R-31-33-01`'s published text and measure which of its two named shapes — a direct caller diverging `appendNote`'s store from its dial root, and `admitAndAppend`'s non-gated branch — still diverges after the two parameters were separated. This is the measurement that decides what the narrowed entry says.
    - RED: drive `promoteAdmitted`'s fall-through with a destination that is a bare directory outside any governed repository, confirm it refuses by name, and record the refusal. This is the widened input set WR-42 names, and it has no published member.
    - GREEN: every entry in the register describes a behaviour a probe in `scripts/context-io.test.ts` drives, and the entry's `shape` text and the probe's observed result agree.
    - GREEN: the two-sided binding passes over the grown array — every exported member has a written disposition and every written disposition names an exported member — with the cardinality asserted separately.
    - CONVERSE: a mirror of `31-CONTEXT.md` with one disposition bullet removed turns the binding red naming the orphaned member; a mirror of the module with one register member removed turns it red naming the orphaned disposition. Each mirror confirmed different from the live file before any result is read.
    - CONTROL: the residual-citation and audit-register gates still pass, so no re-worded entry cites a path this repository does not track.
  </behavior>
  <action>
    MOVEMENT 1 — measure before re-wording. Under a scratch root outside the repository tree, take the
    three readings named in the behaviour block. For the no-argument reading, state plainly whether the
    two defaults coincide on this box and therefore whether the split is observable here at all — the
    existing entry already says they coincide, and a re-wording that silently drops that caveat would
    repeat WR-39 in the other direction.

    MOVEMENT 2 — re-word `R-31-33-01` as two halves under one id. The id is KEPT because prior
    documents, summaries and a test all cite it, and renaming it would orphan those citations. State
    the CLOSED half with the measurement that closed it: the dial and the record are separate
    parameters, the unfreeze the entry itself named as the cost was taken in `31-39`, and the freeze
    re-locked at a recorded baseline. State the REMAINING half as a situation with its own
    reproduction, taken in Movement 1 — not as an inference from the old text. Update
    `what_would_force_it_closed` so it names a criterion that is still open rather than one that has
    been met.

    MOVEMENT 3 — re-word `R-31-33-02` against the tree, per branch. Say where the note lands and where
    the record lands on each branch with no arguments supplied, after `31-39`. Keep the part that is
    genuinely still open — which repository the no-argument default should NAME — exactly as open as it
    was, with `D-34`'s reason for not deciding it inside a gap-closure plan restated rather than
    re-argued. Keep the measured fact that the two defaults coincide on this box and diverge under the
    shipped shared-install model.

    MOVEMENT 4 — add the two new members. `R-31-41-01` states the widened refusal at the fall-through:
    the shape, the refusal by name, the fixtures round 7 re-staged as evidence the input set moved, and
    the shared-install answer Task 2 measures. `R-31-41-02` states whatever boundary this round's own
    plans left open, named from their summaries rather than invented; if those summaries name more than
    one, add one member per boundary rather than merging them into a paragraph.

    MOVEMENT 5 — carry the binding with the array. Update the written dispositions in `31-CONTEXT.md`
    so the two-sided equality holds over the grown array, add the two converse mirrors, and re-run the
    citation and audit-register gates. Then rebuild, re-derive the hook manifest and re-check freshness.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the two-sided residual binding case reports an orphaned member or an orphaned disposition, or the reported test names do not include the two converse mirror cases</fails_when>
    <automated>npm run check:residual-citations && npm run check:audit-register && npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or any check names a published citation whose path this repository does not track, or an unanchored claim</fails_when>
    <automated>npm run build && npm run typecheck && npm run check:build-parity && npm run freshness</automated>
    <fails_when>non-zero exit, or the parity step names any tracked `.js` file as moved, or the freshness step reports any committed output not matching a rebuild</fails_when>
  </verify>
  <acceptance_criteria>
    - `R-31-33-01`'s `reason` names the unfreeze as TAKEN, cites the plan that took it, and states the remaining shape with a reproduction that a case in `scripts/context-io.test.ts` drives; its `what_would_force_it_closed` names a criterion that has not been met.
    - `R-31-33-02`'s `shape` and `reason` describe where the note and the record land on each branch with no arguments supplied on THIS tree, and retain the measured statement that the two defaults coincide on this box.
    - Every register member's `shape` text agrees with the observed result of a probe that drives it; asserted by a case that pairs each member with its probe rather than by reading.
    - The two-sided equality between the exported array and the written dispositions holds, with the cardinality asserted separately.
    - A mirror removing one written disposition, and a mirror removing one exported member, each turn the binding red naming the orphan, and each mirror is confirmed different from the live file before any result is read.
    - The residual-citation, audit-register and claim-anchor gates all exit zero.
  </acceptance_criteria>
  <done>Every published write-path residual describes the mechanism that is in the tree, each with a reproduction a case drives, bound two-sided to its written disposition and watched failing in both directions.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the shared-install shape the widened refusal lands on (WR-42)</name>
  <files>scripts/context-io.test.ts</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — WR-42 in full, including its reasoning about a kit-side store having no governance configuration and no version-control marker written by the installer, and its closing `UNKNOWN - verify`.
    - `install/install.ts` — every place a governance configuration is written and every place a context store is created, and which of the two roots (kit or target) each is written under. The review cites one line; read the surrounding logic rather than the line, because the question is which ROOT receives it.
    - `scripts/context-io.ts` — `governanceRootOf`, `isRecognisedContextStore`, `originStoreIsRootAnchored`, `projectRootFromWorkingDirectory`, `DEFAULT_CONTEXT_ROOT`, `trustedRepoRoot` and the trusted-root tier order, because a kit-side store's answer depends on all of them.
    - `docs/design/shared-install.md` if present, and `.planning/WINDOWS.md` — the shipped shared-install model and the standing platform posture, because the answer may be platform-dependent and a platform-dependent answer is recorded as such.
    - `.planning/phases/31-autonomous-manual-testing/31-round6-residual-dispositions.md` — the shape a residual disposition follows in this phase, which any answer recorded here must match.
  </read_first>
  <behavior>
    - Stage the shared-install shape as the review describes it: a kit directory beside a separate host repository, with the store at the kit-side path the review names, created the way the installer creates it rather than by hand.
    - MEASURE, do not reason: what `governanceRootOf` answers for that kit-side store, whether the kit directory satisfies the root-anchoring conjunct, and therefore whether a promotion into a kit-side store refuses or proceeds.
    - Then ask the second question, which is the one that decides the disposition: does any SHIPPED flow promote into a kit-side store? Derive the answer from the callers of the re-binding route rather than from reading the workflows, and state which flows were examined and by what derivation.
    - If the answer is that promotions into a kit-side store are reachable, the disposition is either an exemption with its own reason or a stated refusal with a remedy — and whichever is chosen is written as a register entry in Task 1's `R-31-41-01`.
    - If the shape cannot be staged faithfully on this box, say so, mark it `UNKNOWN - verify`, and carry it with an owner and a criterion. An unmeasurable shape recorded honestly is worth more than an asserted one.
  </behavior>
  <action>
    Stage the two-directory shape under a scratch root outside the repository tree, using the
    installer's own seeding behaviour as the model for what exists where — read `install/install.ts` to
    decide which root receives a configuration and which receives a store, rather than assuming the
    review's one-line citation is the whole story.

    Take the three readings: the resolver's answer for the kit-side store, the root-anchoring
    conjunct's answer for the kit directory, and the re-binding route's answer for a promotion whose
    destination is that store. Record all three with the commands and the outputs.

    Then derive the reachability answer: enumerate the callers of the re-binding route across the
    repository's tracked sources — the cross-file caller derivation for that route already exists and
    must be REUSED rather than re-implemented — and for each, determine whether the destination it
    supplies can be a kit-side store. State the derivation, the caller set and its cardinality.

    Add the measurement as a case, so the answer is re-taken on every run rather than living only in a
    summary. Hand the disposition to Task 1's register entry.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the reported test names do not include the shared-install shape case</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' install/install.test.ts scripts/context-io-writer-set.test.ts</automated>
    <fails_when>non-zero exit, or any failed test — the installer suite and the cross-file caller derivation are the two things this measurement reads, and a red here means the measurement moved one of them</fails_when>
  </verify>
  <acceptance_criteria>
    - The staged shape's construction is described in the case's comment by reference to what `install/install.ts` actually writes and where, with the reading taken rather than asserted.
    - All three readings are recorded with the value observed: the resolver's answer for the kit-side store, the anchoring conjunct's answer for the kit directory, and the re-binding route's answer for a promotion into it.
    - The reachability answer is derived from the existing cross-file caller derivation for the re-binding route, with the caller set and its cardinality stated; no hand-typed caller list.
    - If any reading could not be taken on this box, it is recorded as `UNKNOWN - verify` with an owner and a criterion, and the register entry says so rather than asserting an answer.
  </acceptance_criteria>
  <done>The shape the widened refusal most likely meets in production is measured, its reachability derived, and the answer recorded as a case rather than as prose.</done>
</task>

<task type="auto">
  <name>Task 3: The ledger paragraph, per route and per branch (WR-40), with its disposition rows and D-41</name>
  <files>agent-factory/workflows/18-context-compaction.md, docs/audit/29-style-dispositions/31-41.md, scripts/context-io-writer-set.test.ts, .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — WR-40 in full, including the quoted universal sentences, the two branches measured false or silent, and the `Fix:` block's suggested per-route wording. Also WR-39, because the two findings are the same defect in two artifacts.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the third `artifacts:` row of gap 1, and the Anti-Patterns row naming this document.
    - `agent-factory/workflows/18-context-compaction.md` — the whole `Trace updates` section, because the ledger paragraph is inside it and the rewrite must not disturb the sentences around it; plus the `Stop conditions` and `Done condition` sections, which state the same rules in the imperative and must not be left disagreeing with the rewritten paragraph.
    - `scripts/context-io-writer-set.test.ts` — the case asserting the corrected workflow sentence names exactly the routes the derivation returns, in full, INCLUDING how it locates the paragraph in the document. A section-anchored reader that searches past its own section adopts an unrelated later block; check the locator's scope rather than assuming it, and state what you checked.
    - `docs/audit/29-style-dispositions/README.md` — the disposition contract in full: one file per plan named for the plan, membership walked not listed, rows under a `## Dispositions` heading with the seven columns in order.
    - `docs/audit/29-style-dispositions/31-33.md` — the rows round 7 wrote for the paragraph this plan rewrites, so this round's rows record a change from THAT text rather than from an older one.
    - `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` — the `D-34` block's closing paragraph on how the workflow sentence is corrected, and the `D-39` and `D-40` blocks written earlier in this round.
  </read_first>
  <behavior>
    - The rewritten paragraph states, per named route and per branch of each: which repository the note lands in, which repository the record lands in, what decides that answer, and what happens when it cannot be answered. Nothing in it may be a universal that a probe can falsify at one branch.
    - Every remaining residual is named INLINE in the paragraph at the sentence it qualifies, so an agent reading the workflow learns the boundary at the moment it matters rather than by finding a register.
    - The order guarantee (the record precedes the note write) and the over-record asymmetry are carried forward unchanged: this plan changes what is said about WHICH repository, not about the order.
    - The imperative restatements elsewhere in the document — the stop condition naming the sanctioned writers, and the done condition — are checked against the rewritten paragraph and corrected if they disagree. A document that contradicts itself is the same claim-outruns-mechanism defect at a smaller scale.
    - The binding case is re-run against the rewritten paragraph and its LOCATOR SCOPE is asserted: the case must be shown to read the intended block and not a later one, by a mirror that plants a decoy block after the intended section and shows the case still reads the intended one.
    - Every changed clause has a disposition row, and the disposition gate is re-run.
  </behavior>
  <action>
    Rewrite the ledger paragraph per route and per branch. Take the route set from the module's own
    derivation rather than from the present prose — the existing binding case already derives it — and
    write one statement per branch of each route: the re-binding route's gated arm, the re-binding
    route's fall-through, the admit-then-persist route's gated branch, and the admit-then-persist
    route's non-gated branch. For each, say where the note lands, where the record lands, what decides
    it, and what happens when the owning repository cannot be named. Name each remaining residual at
    the sentence it qualifies.

    Delete the universal sentences rather than softening them. A sentence stating a property of `each
    route` and `every branch` is the shape round 8 measured false; replacing it with a hedged version
    of itself reproduces the finding one adverb over.

    Read the stop conditions and the done condition and reconcile them with the rewritten paragraph.
    If a sentence there states a rule the paragraph now qualifies, correct it and give it its own
    disposition row.

    Write `docs/audit/29-style-dispositions/31-41.md` with one row per changed clause, in the seven
    columns the README specifies, in that order, with the `before` text taken from the tree this change
    compares against rather than from an older revision.

    Re-run the binding case. Then add the locator-scope mirror: plant a decoy block carrying
    route-shaped prose AFTER the intended section in a mirror of the document, and assert the case
    still reads the intended block. Confirm the mirror differs from the live file before reading any
    result.

    Append the `D-41` decision body to `31-CONTEXT.md` under a heading following the established shape.
    State: what forced it (WR-39, WR-40, WR-42 and the `artifacts:` row); the rule that a claim is
    corrected by moving the mechanism where it can move and by stating it per branch where it cannot;
    the residual dispositions in a bullet list bound two-sided to `WRITE_PATH_RESIDUALS`; the
    shared-install measurement's answer; what `D-41` does NOT establish; and the reversibility rating.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io-writer-set.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the workflow-sentence binding case reports a route named in the prose that the derivation does not return, or a route the derivation returns that the prose does not name, or the reported test names do not include the locator-scope mirror case</fails_when>
    <automated>npm run check:diff-disposition</automated>
    <fails_when>non-zero exit, or the output names any changed clause in the watched corpus with no disposition row</fails_when>
    <automated>npm run check:banned-claims && npm run check:imperative-lexicon && npm run check:public-docs</automated>
    <fails_when>non-zero exit, or any check names a banned claim, a non-imperative clause in a workflow step, or a public-document vocabulary violation in the rewritten prose</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the summary reports any failed test, or fewer than 66 test files</fails_when>
  </verify>
  <acceptance_criteria>
    - The rewritten paragraph contains one statement per branch of each derived route, and contains no sentence asserting a property of every route or every branch.
    - Each remaining residual is named in the paragraph at the sentence it qualifies, by its id.
    - The order guarantee and the over-record asymmetry are present and unchanged in substance.
    - The stop conditions and the done condition agree with the rewritten paragraph; any sentence corrected there has its own disposition row.
    - `docs/audit/29-style-dispositions/31-41.md` exists with rows under a `## Dispositions` heading, in the seven columns the README specifies in order, one row per changed clause, with `before` text matching the tree this change compares against.
    - The binding case passes in both directions against the rewritten paragraph, and the locator-scope mirror shows the case reads the intended block rather than a planted later one.
    - `31-CONTEXT.md` carries a `D-41` block with today's date, this round and this plan number, a residual-disposition bullet list bound two-sided to the exported register, the shared-install answer, a `does not establish` section and a reversibility rating.
    - `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and every traceability row are byte-unchanged by this plan.
  </acceptance_criteria>
  <done>Every published claim about which repository owns which half of a write is equal to what the mechanism does, per route and per branch, with every remaining boundary named inline and bound two-sided to its register.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published claim → mechanism | An agent reads the workflow and acts on it. A claim stronger than the mechanism is an instruction to trust something that is not there. |
| published residual → next round's premise | A residual is the artifact the next round starts from. A stale one is a false premise, which is this phase's own recorded failure mode. |
| installer-seeded layout → the resolver's answer | The shared-install shape decides whether the round's widened refusal fires on a shipped flow. Left `UNKNOWN - verify`, it is an unmeasured production behaviour. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-41-01 | Repudiation | `agent-factory/workflows/18-context-compaction.md`'s ledger paragraph | high | mitigate | Rewritten per route and per branch, with every residual named inline, and bound to the module's own derived route set in both directions with a locator-scope mirror. |
| T-31-41-02 | Spoofing | A published residual describing a mechanism that is not in the tree | high | mitigate | Every register member's shape is paired with a probe that drives it, and the two-sided binding to the written dispositions is asserted with a cardinality and watched failing in both directions. |
| T-31-41-03 | Denial of Service | The widened refusal firing on a shipped shared-install flow | medium | mitigate | Measured rather than reasoned about: the resolver's answer for a kit-side store, the anchoring conjunct's answer, and the re-binding route's answer, plus a derived reachability answer over that route's callers. Recorded as a register member with its remedy. |
| T-31-41-04 | Tampering | A section-anchored prose reader adopting an unrelated later block | medium | mitigate | The binding case's locator scope is asserted by a decoy-block mirror rather than assumed. |
| T-31-41-05 | Information Disclosure | The rewritten workflow prose naming repository paths | low | accept | Repository-relative paths only, already public. |
| T-31-41-06 | Elevation of Privilege | An agent constructing a governed directory to present an acceptable destination | medium | accept | Unchanged pre-existing residual (`T-31-18-01` / `D-31`), priced per position, now named inline in the rewritten paragraph at the sentence it qualifies. |
| T-31-41-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and adds no dependency; `package.json` is not in `files_modified`. No legitimacy checkpoint is required because no install task exists. |
</threat_model>

<verification>
- Every register member's published shape paired with a probe that drives it, and the two-sided binding to the written dispositions asserted with a cardinality and watched failing in both directions.
- `R-31-33-01` split into its closed half with the measurement that closed it and its remaining half with its own reproduction; `R-31-33-02` re-stated per branch against this tree.
- The shared-install shape measured on three readings with a derived reachability answer, or recorded `UNKNOWN - verify` with an owner.
- The ledger paragraph rewritten per route and per branch, bound to the derived route set in both directions, with the locator scope asserted by a decoy mirror.
- Disposition rows present for every changed clause; the disposition, banned-claims, imperative-lexicon, public-docs, residual-citation, audit-register and claim-anchor gates all green.
- `D-41` recorded with the residual dispositions, the shared-install answer and a `does not establish` section.
</verification>

<success_criteria>
- No published claim about the write path is falsifiable at any single branch.
- No published residual describes a mechanism that is not in the tree.
- The widened refusal has a register member and a measured answer for the shape it is most likely to meet.
- Every clause this plan changed in the watched corpus has a disposition row.
- UATX-01 through UATX-06 remain unchecked; the phase remains In Progress.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-41-SUMMARY.md` when done, recording: the
three measured readings behind each re-worded residual; the shared-install readings with their values
and the derived caller set with its cardinality; the before and after text of every changed clause
with its disposition row; the binding case's two-direction result and the locator-scope mirror's
result; and every boundary this plan leaves open with a reason and what would force it closed.
</output>

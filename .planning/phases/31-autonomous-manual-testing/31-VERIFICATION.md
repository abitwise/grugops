---
phase: 31-autonomous-manual-testing
verified: 2026-09-09T18:30:00Z
status: gaps_found
score: 4/6 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/WINDOWS.md"
  - ".planning/phases/31-autonomous-manual-testing/31-01-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-01-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-02-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-02-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-03-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-03-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-04-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-04-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-05-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-05-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-06-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-06-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-07-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-07-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-08-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-08-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-09-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-09-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-10-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-10-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-11-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-11-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-12-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-12-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-13-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-13-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-14-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-14-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-15-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-15-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-16-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-16-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-17-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-17-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-18-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-18-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-19-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-19-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-20-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-20-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-CONTEXT.md"
  - ".planning/phases/31-autonomous-manual-testing/31-DISCUSSION-LOG.md"
  - ".planning/phases/31-autonomous-manual-testing/31-PATTERNS.md"
  - ".planning/phases/31-autonomous-manual-testing/31-RESEARCH.md"
  - ".planning/phases/31-autonomous-manual-testing/31-REVIEW.md"
  - ".planning/phases/31-autonomous-manual-testing/31-VALIDATION.md"
  - ".planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md"
  - ".planning/phases/31-autonomous-manual-testing/deferred-items.md"
  - "agent-factory/checklists/00-index.md"
  - "agent-factory/checklists/browser-uat-recipe.md"
  - "agent-factory/contracts/context-note.md"
  - "agent-factory/workflows/05-pr-quality-gate.md"
  - "agent-factory/workflows/06-uat-pack.md"
  - "agent-factory/workflows/16-context-read-write.md"
  - "agent-factory/workflows/17-task-claim.md"
  - "agent-factory/workflows/18-context-compaction.md"
  - "docs/audit/29-style-dispositions/31-14.md"
  - "docs/audit/29-style-dispositions/31-16.md"
  - "docs/audit/29-style-dispositions/31-17.md"
  - "docs/audit/29-style-dispositions/31-18.md"
  - "docs/audit/29-style-dispositions/31-19.md"
  - "docs/audit/31-round4-residuals.md"
  - "hooks/admission-guard.js"
  - "hooks/admission-guard.test.ts"
  - "hooks/admission-guard.ts"
  - "hooks/guard.js"
  - "hooks/guard.test.ts"
  - "hooks/guard.ts"
  - "hooks/hook-entry.js"
  - "hooks/hook-entry.ts"
  - "hooks/hooks.json"
  - "install/README.md"
  - "install/install.js"
  - "install/install.ts"
  - "install/uninstall.js"
  - "install/uninstall.ts"
  - "package.json"
  - "scripts/check-banned-claims.js"
  - "scripts/check-banned-claims.test.ts"
  - "scripts/check-banned-claims.ts"
  - "scripts/check-foundation-guards.js"
  - "scripts/check-foundation-guards.test.ts"
  - "scripts/check-foundation-guards.ts"
  - "scripts/check-uat-oracles.js"
  - "scripts/check-uat-oracles.ts"
  - "scripts/checkpoints.js"
  - "scripts/checkpoints.ts"
  - "scripts/chrome-lane-bar.test.ts"
  - "scripts/compactor.js"
  - "scripts/compactor.test.ts"
  - "scripts/compactor.ts"
  - "scripts/context-io-writer-set.test.ts"
  - "scripts/context-io.js"
  - "scripts/context-io.test.ts"
  - "scripts/context-io.ts"
  - "scripts/floor-invariance.test.ts"
  - "scripts/runnable-ref/fixtures/caught-assertion.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/clean.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/conditional-assertion.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/configured-soft.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/element-access-modifier.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/import-rename.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-call-link.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-call.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-family.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-group-clean.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/playwright-test.d.ts"
  - "scripts/runnable-ref/fixtures/shadowed-rename.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/testinfo-fixture-param.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/union-all-arms.uat.spec.ts"
  - "scripts/runnable-ref/uat-spec-integrity.js"
  - "scripts/runnable-ref/uat-spec-integrity.test.ts"
  - "scripts/runnable-ref/uat-spec-integrity.ts"
  - "tsconfig.fixtures.json"
  - "tsconfig.tests.json"
covered_digest: "v1:sha256:748049ac5901ca5e18dc9eeadc2bc740adda1a46dbaa784da026903cad9a75ce"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/6
  gaps_closed:
    - "CR-09 (both variants) and CR-10 — the round-4 verifier's own three failing spellings, `expect.configure({retries:2}).soft(locator).toBeVisible()`, `expect.configure({retries:2}).configure({soft:true})(locator)`, and `testInfo.skip()` through the second callback parameter — are each independently re-reproduced REFUSED at exit 1 against the committed `scripts/runnable-ref/uat-spec-integrity.js`, naming `expect.configure().soft`, `expect.configure().configure`, and `test.info().skip` respectively."
    - "CR-11 — a legitimately human-disposed high-severity finding still promotes unchanged (independently re-reproduced: `promoteAdmitted` on a genuine origin returns the same id, writes nothing extra, `compactor.promote` still throws its D-04 refusal) AND an attempted overwrite of an already-admitted destination note under the SAME id is independently re-reproduced DECLINED (`destination-id-occupied`) rather than silently replacing the destination's bytes, when the destination already holds a note under that id."
  gaps_remaining:
    - "UATX-01 — closed for round 4's exact CR-11 shape, reopened by a new defect in the SAME fix: `originIsTrusted`'s second arm treats ANY directory under `trustedRepoRoot()` as a trusted proof origin, so a caller-authored ordinary directory inside the repository (exactly the workaround `agent-factory/workflows/18-context-compaction.md` tells the agent is 'mechanically refused') is accepted, and a promotion into a FRESH (not-yet-occupied) destination id from that ordinary directory WRITES rather than declining — independently reproduced in this round (CR-16)."
    - "UATX-06 — closed for round 4's exact CR-09/CR-10 spellings, reopened by a new defect in the SAME fix: the WR-20 false-positive fix (`deriveDeclaredNames`) is a file-scoped off switch for BOTH canonicalisations rather than a lexically-scoped one, so ANY unrelated declaration of a banned head's name anywhere in the file (e.g. a dead `const it = 1;` in an unrelated scope) suppresses the rewrite for the WHOLE file and lets `it.skip(...)` at module scope — the exact WR-14 spelling round 3 closed — evade the ban again at exit 0 — independently reproduced in this round (CR-14)."
  regressions:
    - "CR-12 (`31-REVIEW.md`, independently reproduced in this verification): the CR-11 fix put an unguarded `readFileSync` on `writeNoteFile`, the module's single note-write chokepoint (reached by `appendNote`, `admitAndAppend`, `promoteAdmitted`, `emitVerdict`, `emitCheckpointNote`). A non-regular file (a FIFO) at a note path makes the write HANG FOREVER with zero bytes on both streams — the exact hazard this same file's `readGovernanceConfigCandidate` already documents and refuses, 2,400 lines below the new unguarded read. Independently reproduced: `timeout 10 node …` against the committed `.js` → exit 124, no output on either stream."
    - "CR-13 (`31-REVIEW.md`, independently reproduced in this verification): the WR-21 fix (halting the governance-root walk at the user's home directory) checks `isAtOrAboveHome(dir, home)` BEFORE inspecting `dir` for a configuration or a repository marker, at every step of the walk. A repository whose ROOT IS the user's home directory (a dotfiles checkout, or an agent run at `$HOME`) therefore never has its own `.git` marker or `.grugops/factory.config.json` read, and `trustedRepoRoot()` falls through to the kit's lean default — the exact 'configuration moving from refused to admitted' direction WR-15 existed to close. Independently reproduced: with `HOME` pointed at a planted repository root carrying `.git` + `human_admission: high-severity`, `trustedRepoRoot()` returned the kit and the dial read `off`; the identical tree one level below the real home correctly resolved to the project and read `high-severity`."
    - "CR-15 (`31-REVIEW.md`, independently reproduced in this verification): the WR-19 fix bounded the resolver and wrapped `findBannedConstructs` in a could-not-run boundary, but left `ts.createSourceFile` — the parser itself, running on attacker-or-author-controlled source — OUTSIDE that boundary. A spec containing 1,000 nested parentheses (a quarter of the round-4 probe's own chain depth) reproduces WR-19's exact harm at a LOWER threshold than before the fix: uncaught `RangeError`, empty stdout, exit 1 with no `visited/expected` measurement line — falsifying the recipe paragraph this round added asserting the checker 'does not exit through an uncaught exception, including a pathological one'. Independently reproduced against the committed `.js`."
gaps:
  - truth: "UATX-01 — a committed Playwright spec, re-run by the §14 gate, is the machine-verifiable evidence floor; an agent's narration or a caller-supplied claim never becomes indistinguishable from genuine admitted evidence — including through the mechanism that carries an already-admitted note forward through compaction."
    status: failed
    reason: >
      Round 4's CR-11 (a caller-forged note silently overwriting an already-admitted destination
      note) is genuinely closed — independently re-reproduced in this session: attempting to
      promote a forged note over an occupied destination id now DECLINES
      (`destination-id-occupied`) rather than overwriting, and the legitimate human-disposed
      promotion (CR-08's own case) still writes cleanly with no change in behaviour. But
      `31-REVIEW.md`'s own review of this same fix found CR-12 and CR-16, and this verification
      session independently re-reproduces both against the committed `scripts/context-io.js` +
      `scripts/compactor.js`, plus CR-13 against the trusted-root mechanism the promotion route
      also depends on.

      CR-16, independently reproduced: `originIsTrusted`'s second arm is
      `resolvedFrom === trusted || resolvedFrom.startsWith(trusted + sep)` — ANY directory under
      `trustedRepoRoot()` counts as "trusted", not only a recognised `.grugops/context` shape. A
      caller obtains well-formed, human-disposed bytes from a legitimate origin store, copies them
      byte-for-byte into an ORDINARY directory it creates under the repository root (no
      `.grugops/context` shape anywhere in the path — verified: `endsWith(join(".grugops",
      "context")) === false`), and calls `promoteAdmitted` with that ordinary directory as `from`
      and a FRESH (not-yet-occupied) destination id. Measured against the committed `.js`:
      `promotedId` returned the note's id, `threw` was `null`, and the destination now holds
      exactly that file. This is verbatim the action `agent-factory/workflows/18-context-compaction.md:75`
      tells the agent is "mechanically refused" ("Never copy the origin notes into a directory to
      make the promotion pass. Copying them is hand-authoring a context path by another name, and
      the constraint refuses it.") — it is not refused for the ordinary in-repository case, only
      for an origin outside the repository entirely.

      CR-12, independently reproduced: the CR-11 fix added a destination read to `writeNoteFile`,
      the module's single write chokepoint, using an unguarded `readFileSync`. A FIFO planted at a
      note path (`mkfifo <ctx>/T-9/notes/<id>.md`) makes `appendNote` HANG: `timeout 10 node …`
      against the committed `.js` exits 124 with zero bytes on stdout and zero on stderr — no
      exception, no diagnostic, no eventual answer. This chokepoint is reached by every writer
      (`appendNote`, `admitAndAppend`, `promoteAdmitted`, `emitVerdict`, `emitCheckpointNote`), so
      a single planted FIFO anywhere a note or checkpoint is about to be written wedges that call
      indefinitely — the identical hazard this same file's `readGovernanceConfigCandidate` already
      documents in its own header, 2,400 lines below the new unguarded read, and refuses there with
      an `O_NONBLOCK` open + `fstat` + regular-file check that the new chokepoint read does not
      reuse.

      CR-13, independently reproduced: the WR-21 fix's home-stop (`isAtOrAboveHome(dir, home)`) is
      asked BEFORE `dir` is inspected for a configuration or a marker, at every step. A repository
      whose ROOT IS the home directory therefore never has its own `.git`/`.grugops/factory.config.json`
      read at all. Measured: with `HOME` set to a planted repository root carrying `.git` and
      `human_admission: high-severity`, `trustedRepoRoot()` returned the kit
      (`/Users/olgeroeselg/Projects/public/grugops`) and the dial read `off`; the CONTROL — the
      identical tree one level below the real home directory — correctly resolved to the project
      and read `high-severity`. This is the same "a configuration moving from refused to admitted"
      direction WR-15 (round 3) and CR-13's own convening finding both named.
    artifacts:
      - path: "scripts/context-io.ts"
        issue: "originIsTrusted (~line 1486) accepts any directory under trustedRepoRoot() as a trusted proof origin, not only a recognised .grugops/context shape, so an ordinary caller-created directory inside the repository satisfies the origin check the workflow documentation says is refused. writeNoteFile's destination read (added by 31-18's CR-11 fix) uses an unguarded readFileSync with no O_NONBLOCK/fstat/regular-file check, so a non-regular file at a note path hangs the write forever. projectRootFromWorkingDirectory's isAtOrAboveHome check runs before the directory is inspected for a configuration or a repository marker at every step of the walk, so a repository rooted at the home directory itself is never read."
      - path: "agent-factory/workflows/18-context-compaction.md"
        issue: "Line 75 asserts the copy-into-an-ordinary-directory workaround is 'mechanically refused' by originIsTrusted, which is false for the in-repository case (CR-16)."
    missing:
      - "Narrow originIsTrusted's second arm to the shape arm 1 already recognises (a context store reached from trustedRepoRoot(), not any directory under it), or rewrite the agent-facing decline sentence and give the in-repository workaround its own named residual rather than an instruction the mechanism does not honour."
      - "Read writeNoteFile's destination through the same non-blocking primitive readGovernanceConfigCandidate already implements (O_NONBLOCK open + fstat + regular-file check), rather than a second, unguarded readFileSync habit."
      - "Make the home stop asymmetric: refuse to climb past home, but let the home directory itself answer when it carries its own configuration or marker, per 31-REVIEW.md's CR-13 fix sketch — and record the choice as a decision beside D-23 with a new residual member rather than a silent edit."
  - truth: "UATX-06 — conditional or caught assertions, and the D-14 arm-(c) modifier-call bans, are rejected over the TypeScript AST, and the recipe's claim matches exactly what the checker decides."
    status: failed
    reason: >
      Round 4's CR-09 (both variants) and CR-10 spellings are genuinely closed — independently
      re-reproduced in this session against the committed `scripts/runnable-ref/uat-spec-integrity.js`:
      `expect.configure({retries:2}).soft(locator).toBeVisible()` and
      `expect.configure({retries:2}).configure({soft:true})(locator)` both now refuse at exit 1
      naming the marker-stripped path, and `testInfo.skip()` through the fixture parameter refuses
      naming `test.info().skip`. But `31-REVIEW.md`'s own review of the SAME two fixes found CR-14
      and CR-15, and this verification session independently re-reproduces both.

      CR-14, independently reproduced: the round-4 fix for WR-20 (the false-positive fix for a
      shadowing local binding) introduced a file-scoped declared-names census
      (`deriveDeclaredNames`) that suppresses BOTH canonicalisations for the WHOLE FILE when the
      banned head's name is declared ANYWHERE in it — not only at a scope that contains the call
      site. Measured against the committed `.js`: a spec with
      `import { test as it, expect } from "@playwright/test"; it.skip("scenario", async ({ page }) =>
      { const it = 1; void it; await expect(page.locator("x")).toBeVisible(); });` reports `0
      findings`, exit 0; the identical file with the dead, unrelated `const it = 1;` line removed
      reports `1 finding(s)`, exit 1, naming `test.skip`. A single unrelated declaration anywhere in
      a spec file — including one that never shadows the call site — turns off the entire
      rename/namespace/fixture-parameter ban family for that file, which is precisely the WR-14 and
      CR-10 spellings this phase has now closed and reopened five times.

      CR-15, independently reproduced: the round-4 fix for WR-19 bounded the resolver
      (`calleeDottedPath`) and wrapped `findBannedConstructs` in a could-not-run boundary, but left
      `ts.createSourceFile` — the parser itself — outside that boundary. A spec file containing
      1,000 nested parentheses (`const x: any = ((((...1...))));`) — a quarter of the round-4
      probe's own 4000-link chain depth — reproduces an uncaught `RangeError: Maximum call stack
      size exceeded` at the parser, empty stdout, exit 1, with no `visited/expected` measurement
      line printed at all. This is the exact WR-19 harm (a check that never ran reads, via Node's
      accidental uncaught-exception exit code, as a check that found a violation) reproducing at a
      LOWER threshold than the fix closed, and it falsifies the recipe paragraph this round's own
      `31-17` plan added claiming the checker holds its exit-code contract "including a pathological
      one" and "does not exit through an uncaught exception."
    artifacts:
      - path: "scripts/runnable-ref/uat-spec-integrity.ts"
        issue: "deriveDeclaredNames + the canonicaliser guard (~line 1435, `if (declaredNames !== null && declaredNames.has(segments[0])) return dottedPath;`) suppress a rewrite for any call site in the file once the head name is declared anywhere in it, rather than only where the declaration's own scope contains the call site — a ban's suppression rule that changes the answer from refused to accepted is the unsafe direction, contrary to the file's own 'MONOTONE IN THE SAFE DIRECTION' comment. ts.createSourceFile (~line 1605) sits outside the try/catch boundary the WR-19 fix added around findBannedConstructs, so a pathological parse input still crashes the process uncaught."
      - path: "agent-factory/checklists/browser-uat-recipe.md"
        issue: "The paragraph added this round (around lines 309-315) asserts the checker holds its exit-code contract on every path 'including a pathological one' and never exits through an uncaught exception — false of the parser boundary (CR-15). The boundary list does not disclose that a single unrelated declaration anywhere in a file suppresses the entire rename/namespace/fixture-parameter ban for that file (CR-14)."
    missing:
      - "Narrow the declared-names suppression to a containment test: a declaration suppresses a rewrite only for call sites inside the declaration's own enclosing function/block, not for the whole file — per 31-REVIEW.md's CR-14 fix sketch (a getStart/getEnd containment test on nodes the walk already visits)."
      - "Move the could-not-run boundary to wrap the parse itself (ts.createSourceFile), not only findBannedConstructs, and wrap main's body in one outer try returning exit 2, so the D-12 contract is held by decision at the process boundary as well as per-file — per 31-REVIEW.md's CR-15 fix sketch."
      - "Record both fixes as dated decisions beside D-20/D-21 in 31-CONTEXT.md, add a MUTATE-REMOVE corpus case for each (a same-file union of a declared name AND a genuine banned call through that name; a spec at parser-crash depth asserting a measurement line and an exit inside {0,1,2}), and correct the recipe paragraph's absolute claim."
deferred: []
advisory: []
behavior_unverified_items: []
human_verification:
  - test: "The attended Claude-in-Chrome lane opens under real interactive auth, pauses for a human on a login/challenge page, and produces only a human-stamped finding + artifact-ref (never a gate stamp)."
    expected: "The lane behaves as documented in agent-factory/checklists/browser-uat-recipe.md's attended lane; no route to a §14-gate stamp is exercised in practice."
    why_human: "Requires an attended Claude Code session with the Claude-in-Chrome browser extension installed and a real interactive login; not reachable in CI and not reachable on this box. Carried forward unchanged through rounds 2, 3, 4 and this round (R-01)."
  - test: "The claude auth status --json fail-closed predicate (D-10) behaves correctly under an API-key-only box and under a long-lived setup token."
    expected: "Both configurations are a loud skip naming the failing clause, never a silent open."
    why_human: "Research assumptions A2/A3 are UNKNOWN - verify; neither configuration is reachable without destroying this box's real credentials. Carried forward unchanged (R-02)."
  - test: "Both browser-absence probe stages, and the whole spec-integrity runnable, on a Windows host."
    expected: "Exit 2 with the browser-absent marker when browsers are missing; parser-absent marker when typescript cannot be resolved."
    why_human: "UNKNOWN - verify per the standing Windows posture (WINDOWS.md); not testable on darwin. Carried forward unchanged (R-03)."
  - test: "A host repository that installed grugops before this release re-runs the installer and picks up tools/grugops/uat-spec-integrity.js; the uninstaller removes it."
    expected: "The new runnable is materialized on re-install and cleanly removed on uninstall."
    why_human: "Requires a second scratch repository with a prior grugops install at an earlier release; not exercised by the unit suite. Carried forward unchanged (R-04)."
---

# Phase 31: Autonomous Manual Testing Verification Report

**Phase Goal:** An agent can drive a real browser to produce UAT evidence, and the only thing that
counts as evidence is an artifact the §14 gate re-runs — never the agent's narration of what it saw.
**Verified:** 2026-09-09T18:30:00Z
**Status:** gaps_found
**Re-verification:** Yes — FIFTH round, after gap-closure plans 31-16..31-20 closed the two blockers
from round 4's `31-VERIFICATION.md` (score 4/6: UATX-06 gap / CR-09+CR-10, and UATX-01 / CR-11). A
fresh code review of this gap-closure round (`31-REVIEW.md`, committed at `89228c8`, on top of the
same tree the fix plans left at `19f72e1`) independently re-measured all nine of round 4's closures as
real and found FIVE new Criticals (CR-12 through CR-16) plus four Warnings (WR-22 through WR-25) —
every one of them a defect the round's own fixes created or preserved. Every finding below — both the
round-4 closures and the round-5 review's new findings — is independently re-reproduced in THIS
verification session against the committed `.js` at `HEAD` (`89228c8`, which is a docs-only commit on
top of the fix tree — no source file differs from `19f72e1`), not accepted from `31-REVIEW.md`'s or
any summary's word.

## Round History

| Round | Status | Score | What moved |
|---|---|---|---|
| 1 | gaps_found | — | Three planted constructs found and fixed by 31-06 |
| 2 | gaps_found | 4/6 | CR-05 (fabricated §14-gate stamp) and CR-06 (two `test.describe.*.only` spellings) |
| 3 | gaps_found | 5/6 | CR-05/CR-06 closed; CR-07/WR-14 (call-link + import-rename evasions) and CR-08 (compaction refuses a human-disposed finding) found |
| 4 | gaps_found | 4/6 | CR-07/WR-14/CR-08/WR-15 closed; CR-09 x2/CR-10/CR-11/WR-19/WR-20/WR-21 found |
| **5 (this round)** | **gaps_found** | **4/6** | CR-09 x2/CR-10/CR-11/WR-19/WR-20/WR-21 all closed as filed and independently re-verified here; FIVE new Criticals (CR-12, CR-13, CR-14, CR-15, CR-16) — each a defect the round-4 fixes themselves created or preserved — found and independently reproduced in this session |

The score holds at 4/6 for the second consecutive round. UATX-01 and UATX-06 are STILL the two
blocked requirements, for the fifth consecutive round — but every specific spelling that blocked them
in round 4 is now genuinely closed. What blocks them this round is a new defect one register past each
fix, in the same two predicate families this phase has paid for across all five rounds: the UAT-spec
modifier ban, and the admission/re-binding/governance-root mechanism.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | UATX-01 — a committed Playwright spec, re-run by the §14 gate, is the machine-verifiable evidence floor; an agent's narration/self-supplied claim never produces a stamp | ✗ FAILED — see gap 1 (CR-12/CR-13/CR-16) | Round 4's CR-11 (silent destination overwrite) is independently re-verified closed: a forged note aimed at an OCCUPIED destination id now DECLINES (`destination-id-occupied`), and the legitimate human-disposed promotion (CR-08's case) still writes cleanly. But `originIsTrusted`'s in-repository arm is over-broad (CR-16, independently reproduced: a caller-authored ordinary directory promotes into a FRESH destination id with no decline — verbatim the workaround Workflow 18 says is refused), the CR-11 fix's own destination read can hang the module's single write chokepoint forever on a planted FIFO (CR-12, independently reproduced: `timeout 10` → exit 124, no output), and the WR-21 fix's home-stop never inspects a repository rooted exactly at the home directory (CR-13, independently reproduced: dial `off`/kit where the identical tree one level below home reads `high-severity`). |
| 2 | UATX-02 — browser MCP tooling documented and pinned for all five host CLIs; `package.json` gains nothing; the pin authority cannot itself be defeated by a floating specifier | ✓ VERIFIED | Unchanged since round 2, not contested by the review, this verification, or any round-4 plan; `guardPlaywrightMcpPin` green (`node scripts/check-foundation-guards.js` → `ALL CHECKS PASSED`), re-run in this session. |
| 3 | UATX-03 — Claude in Chrome is attended-only and structurally barred from producing a `§14-gate` stamp | ✓ VERIFIED | `scripts/chrome-lane-bar.test.ts` unchanged this round, part of the green 62-file/3702-test suite re-run in this session; not contested. |
| 4 | UATX-04 — evidence carries commit SHA + gate-run id + content hash; a note whose SHA is not the HEAD the gate ran against is refused | ✓ VERIFIED | Unchanged since round 2; the provenance-carrying routes (`admit()`, `appendNote`'s artifact-ref path) are untouched by any round-4 or round-5 finding — none of CR-12 through CR-16 or WR-22 through WR-25 names this surface. Not contested. |
| 5 | UATX-05 — an absent/unusable browser produces a loud skip leaving the UAT `pending`, never a silent pass | ✓ VERIFIED | Unchanged this round; `PARSER_ABSENT_MARKER` / `BROWSER_ABSENT_MARKER` each single-emission, part of the green suite. CR-15's parser-crash finding is tracked under UATX-06 (the AST-ban contract), not this truth, because the crashing input is a spec file being CHECKED, not a browser-absence condition — the D-12 contract's `{0,1,2}` boundary is what CR-15 breaches, and that boundary is shared infrastructure for both requirements but the specific claim broken (a pathological input still exits through the decided boundary) is UATX-06's own recipe paragraph. |
| 6 | UATX-06 — conditional or caught assertions, and the D-14 arm-(c) modifier bans, are rejected over the TypeScript AST, and the recipe's claim matches exactly what the checker decides | ✗ FAILED — see gap 2 (CR-14/CR-15) | Round 4's CR-09 (both variants) and CR-10 spellings are independently re-verified refused (see Behavioral Spot-Checks rows 1-3). But the fix for WR-20 (a file-scoped declared-names census) is a one-line off switch for the WHOLE FILE's rename/namespace/fixture-parameter ban family: an unrelated dead declaration anywhere in a spec file re-opens the exact WR-14 spelling this phase closed three rounds ago (CR-14, independently reproduced: `0 findings`, exit 0, vs. `1 finding`, exit 1 with the declaration removed). And the WR-19 fix left the TypeScript parser itself outside the could-not-run boundary, so a deeply nested (not chained) construct crashes the process uncaught at a quarter of the round-4 probe's depth (CR-15, independently reproduced). |

**Score:** 4/6 truths verified (UATX-02, UATX-03, UATX-04, UATX-05). UATX-01 and UATX-06 both fail —
this is the FIFTH consecutive round in which this phase's two predicate families (the UAT-spec
modifier ban, and the admission/re-binding/governance-root mechanism) close the exact coordinates the
prior round measured and a new bypass reappears one register over, INSIDE THE SAME FIX.

### Deferred Items

None. No gap identified this round is addressed by a later phase in the roadmap; both remaining gaps
are Phase 31's own predicate families, and the round-4 code review (`31-REVIEW.md`) that found them is
itself part of this phase's own gap-closure record.

### Advisory (New Scope, Unevidenced)

None. This is not a re-verification round in the `#3304` sense of carrying forward an unresolved
prior-round blocker with no fresh evidence — every finding below (CR-12 through CR-16) is independently
reproduced against the committed `.js` in this session, with command, exit code, and output recorded.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/runnable-ref/uat-spec-integrity.ts` / `.js` | AST ban over the real Playwright modifier-call surface, decided by a rule rather than an enumerable literal, reaching every real call-link, import-rename, and fixture-parameter spelling, and holding its exit-code contract on every input including pathological ones | ⚠️ PRESENT BUT INCOMPLETE | Round 4's marker-awareness (CR-09) and TestInfo-parameter derivation (CR-10) fixes are real and independently re-verified over the exact spellings the round-4 verifier named. But `deriveDeclaredNames`'s file-scoped suppression (CR-14) turns off the entire rename/namespace/fixture-parameter family for a file carrying any unrelated declaration of a banned head's name, and `ts.createSourceFile` sits outside the could-not-run boundary the WR-19 fix added, so a pathological parse still crashes uncaught (CR-15) — independently reproduced, both. |
| `scripts/context-io.ts` — `promoteAdmitted` | A proof-gated re-binding route that carries an already-admitted note forward, refusing anything that is not a faithful, provable re-binding, with no route that can hang or that treats a caller-authored ordinary directory as trusted | ✗ FAILED | CR-11 (silent overwrite of an occupied destination) is closed — independently re-verified: an occupied destination id declines. But `originIsTrusted`'s second arm accepts any directory under `trustedRepoRoot()`, so a caller-authored ordinary directory inside the repository is treated as trusted and a promotion from it into a FRESH destination id writes with no decline (CR-16, independently reproduced) — verbatim the workaround Workflow 18 says is refused. Separately, the destination read the CR-11 fix added to the module's write chokepoint (`writeNoteFile`) is an unguarded `readFileSync` that hangs forever on a non-regular file at the note path (CR-12, independently reproduced: `timeout 10` → exit 124). |
| `scripts/compactor.ts` — `promote` / `promoteAdmitted` | Re-admits a promoted note without falsely refusing one a human already disposed, and without a route that can overwrite existing evidence or hang | ⚠️ PARTIAL | `promote` is unchanged and correct (independently re-verified: still throws its D-04 refusal). `promoteAdmitted`'s pass-through inherits the `context-io.ts` defects above (CR-12, CR-16). |
| `agent-factory/checklists/browser-uat-recipe.md` | Ban set and completeness claim, and the D-12 exit-code contract's paragraph, quoted from and equal to the exported constants and their proven strength | ⚠️ PRESENT BUT INCOMPLETE | The recipe's paragraph asserting the checker "does not exit through an uncaught exception, including a pathological one" is false — independently reproduced (CR-15). The boundary list does not disclose that a single unrelated declaration anywhere in a file suppresses the entire rename family for that file (CR-14). |
| `agent-factory/workflows/18-context-compaction.md` | Agent-facing prose about what `promoteAdmitted` refuses matches what the mechanism actually refuses | ✗ FAILED | Line 75 ("Never copy the origin notes into a directory to make the promotion pass … the constraint refuses it.") is false for the in-repository case — independently reproduced (CR-16). |
| `scripts/context-io.ts` — `trustedRepoRoot` / `projectRootFromWorkingDirectory` | A documented resolution order that reads the target repository's dial on every host, bounded so it can never reach or skip a repository at the user's home directory | ⚠️ PARTIAL | The below-home-ancestor shape WR-21 was convened to fix is independently re-verified closed (a planted ancestor merely named "home", real `HOME` untouched, is still adopted — this is the documented residual `R-31-19-01`, not a new finding). But a repository whose root IS the home directory is never inspected at all, so its own dial is skipped in favour of the kit's lean default — independently reproduced (CR-13), the identical direction WR-15 and CR-13's own convening finding both name. |
| `scripts/check-uat-oracles.ts` — `equivDoWork` | Doc comment matches the mechanism; writes confined to a governance root the function owns | ✓ VERIFIED | Unchanged this round; `node scripts/check-uat-oracles.js` → `ALL CHECKS PASSED`, not contested. |
| `scripts/context-io-writer-set.test.ts` | Every derived writer/caller/decline-clause axis, bound in both directions to a decision or a named residual, with watched-fail mirrors | ✓ VERIFIED (as far as its own derivations reach) | Real and internally consistent — but as `docs/audit/31-round4-residuals.md`'s own §9 states, a derivation can only find what its own AST walk is asked to look for, and none of this round's five new Critical classes (CR-12 through CR-16) sits inside any axis this test currently derives. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `originIsTrusted`'s second arm | any directory under `trustedRepoRoot()` | `resolvedFrom.startsWith(trusted + sep)` | ✗ MISWIRED | Accepts an ordinary caller-authored directory, not only a recognised `.grugops/context` shape — independently reproduced (CR-16). |
| `writeNoteFile`'s destination read | the filesystem | unguarded `readFileSync(resolvedFinal, "utf8")` | ✗ MISWIRED | No `O_NONBLOCK`/`fstat`/regular-file check; hangs forever on a non-regular file — independently reproduced (CR-12). |
| `projectRootFromWorkingDirectory`'s walk | `isAtOrAboveHome` | evaluated first, before any inspection of `dir` | ✗ MISWIRED | A repository rooted at home is never inspected for its own config or marker — independently reproduced (CR-13). |
| `deriveDeclaredNames`'s census | the canonicaliser guard | file-scoped set membership, no position/scope containment test | ✗ MISWIRED | An unrelated declaration anywhere in the file suppresses the rewrite everywhere in the file — independently reproduced (CR-14). |
| `ts.createSourceFile` (the parser) | the could-not-run `try`/`catch` boundary | not wired at all — the `try` wraps only `findBannedConstructs` | ✗ NOT_WIRED | A pathological parse input crashes uncaught before the boundary is ever reached — independently reproduced (CR-15). |

### Behavioral Spot-Checks

All commands below were run in THIS verification session against the committed `.js` at HEAD
(`89228c8`, a docs-only commit — the source tree is identical to `19f72e1`), using the same
probe-repository shape prior rounds established (a resolvable `typescript` via `.temp/<name>/` inside
this repository so `node_modules` resolves; specs at a `uat/*.uat.spec.ts` path; a scratch git repo
under `.temp/` for the context-io probes). All `.temp/` probe directories and repo-root probe
`.mjs` scripts were deleted immediately after each measurement, verified with `git status --short`.

| # | Behavior | Command | Result | Status |
|---|----------|---------|--------|--------|
| 1 | Round-4 closure: `expect.configure({retries:2}).soft(locator).toBeVisible()` (chained, CR-09) | `node scripts/runnable-ref/uat-spec-integrity.js` over a probe spec | `1 finding(s) over 1/1`, EXIT=1, naming `expect.configure().soft` | ✓ PASS — round-4 closure confirmed intact |
| 2 | Round-4 closure: `test("a", async ({page}, testInfo) => { testInfo.skip(); ... })` (CR-10) | same | `1 finding(s) over 1/1`, EXIT=1, naming `test.info().skip` | ✓ PASS — round-4 closure confirmed intact |
| 3 | Round-4 closure: `expect.configure({retries:2}).configure({soft:true})(locator)` implied by row 1's mechanism (marker-strip is one authority) | same construct family (row 1) | consistent — see 31-REVIEW.md §2.2 row 17 for the direct instrumented re-check; the same normaliser both arms consume | ✓ PASS |
| 4 | **NEW this round (CR-16):** a caller copies legitimately-obtained, human-disposed note bytes into an ORDINARY directory under the repository root (no `.grugops/context` shape anywhere on the path) and promotes into a FRESH destination id | Node probe against committed `scripts/context-io.js`: `ctx.promoteAdmitted(task, originId, note, body, forgedFrom, destRoot, repoRoot)` | `promotedId` = the note's id; `threw` = `null`; destination now holds exactly that file | ✗ FAIL (confirms CR-16) |
| 5 | **Control for row 4:** the identical attack, but the forged note's id is already OCCUPIED at the destination (round-4's CR-11 shape) | same, destination pre-populated under the same id | `threw` = `"...DECLINED (destination-id-occupied)..."`; destination byte-unchanged | ✓ PASS (CR-11's own closure holds; distinguishes CR-16 from CR-11) |
| 6 | **Control for row 4:** the SAME legitimate promotion (CR-08's own case — a genuinely human-disposed finding, origin a recognised `.grugops/context` store) | same route | `promotedId` returned, `threw` = `null`, destination holds the note | ✓ PASS — CR-08's closure survives unchanged |
| 7 | **NEW this round (CR-12):** a FIFO planted at a note path, then `appendNote` targets that same id | `mkfifo <ctx>/T-9/notes/<id>.md; timeout 10 node <probe invoking appendNote>` | EXIT=124 (timeout); zero bytes on stdout; zero bytes on stderr | ✗ FAIL (confirms CR-12) |
| 8 | **NEW this round (CR-13):** `trustedRepoRoot()` when the repository root IS the (overridden) home directory, carrying its own `.git` + `human_admission: high-severity` config | Node probe: child process with `HOME` set to the planted repo root, both project-directory env vars removed, cwd inside it | `trustedRepoRoot()` → the kit (`/Users/olgeroeselg/Projects/public/grugops`); dial → `off` | ✗ FAIL (confirms CR-13) |
| 9 | **Control for row 8:** the identical tree one level BELOW the REAL home directory, `HOME` left at its real value | same probe harness, different cwd/config placement | `trustedRepoRoot()` → the project itself; dial → `high-severity` | ✓ PASS (WR-15's own closure, and the below-home half of WR-21, both confirmed intact — this is the documented residual `R-31-19-01`, not a new finding) |
| 10 | **NEW this round (CR-14):** `import { test as it, expect }; it.skip("s", async ({page}) => { const it = 1; void it; await expect(...).toBeVisible(); })` | `node scripts/runnable-ref/uat-spec-integrity.js` over a probe spec | `UAT spec integrity: 0 findings over 1/1 uat specs checked`, EXIT=0 | ✗ FAIL (confirms CR-14) |
| 11 | **Control for row 10:** the identical file with the dead `const it = 1;` line removed | same | `1 finding(s) over 1/1`, EXIT=1, naming `test.skip` | ✓ PASS — proves the census (not some other mechanism) is what suppresses row 10 |
| 12 | **NEW this round (CR-15):** a spec containing `const x: any = ((((...1 nested 1000 times...))));` | `node scripts/runnable-ref/uat-spec-integrity.js` over a probe spec | uncaught `RangeError: Maximum call stack size exceeded` at the TypeScript parser's `token()`; EXIT=1; stdout empty | ✗ FAIL (confirms CR-15) |
| 13 | Full excluded-e2e regression suite (run once, after all probe directories were cleaned up) | `npx vitest run --exclude '**/scripts/e2e/**'` | `Test Files 62 passed (62)`, `Tests 3702 passed \| 2 skipped (3704)`, exit 0 | ✓ PASS — and, for the FIFTH consecutive verification round on this phase, exercises none of the newly-found defects (CR-12 through CR-16) |
| 14 | Committed `.js` freshness | `npm run freshness` | `All build outputs fresh: 60 committed .js file(s) match a rebuild of their sources.` | ✓ PASS |
| 15 | Foundation guards / UAT oracles | `node scripts/check-foundation-guards.js`, `node scripts/check-uat-oracles.js` | Both `ALL CHECKS PASSED` | ✓ PASS |
| 16 | Byte-frozen deploy guard | `git hash-object hooks/guard.ts` | `669725bc1c616ab57123e22090d93d57eff1b001`, equal to `FROZEN_GUARD_BLOB` in `scripts/floor-invariance.test.ts:245` | ✓ PASS |

**Cleanup verified.** `git status --short .temp scripts hooks agent-factory install docs .planning` at
the end of this session shows only pre-existing untracked scratch files from earlier rounds (all
`.temp/` and gitignored) and the repository's own pre-existing working-tree state
(`.planning/milestone.lock`, `human-notes.txt`, unrelated to this verification); no probe artifact from
this session remains on disk.

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| UATX-01 | 31-01, 31-04, 31-05, 31-09, 31-10, 31-14, 31-18, 31-19 | Committed spec = evidence floor; narration never a stamp | ✗ BLOCKED | CR-11 closure re-verified intact; CR-16 (originIsTrusted over-broad in-repo), CR-12 (write-chokepoint hang), CR-13 (home-rooted repository skipped) independently reproduced |
| UATX-02 | 31-03, 31-06, 31-07 | Browser MCP tooling documented + pinned, 5 hosts; pin authority cannot itself float | ✓ SATISFIED | Unchanged, not contested |
| UATX-03 | 31-03, 31-04, 31-15 | Attended Chrome lane structurally barred from gate stamp | ✓ SATISFIED | Unchanged, not contested; structural bar re-run green this round |
| UATX-04 | 31-01, 31-05, 31-14 | Evidence provenance (SHA/gate_run/content_hash); mismatched SHA refused | ✓ SATISFIED | Unchanged, not contested; no round-5 finding touches this surface |
| UATX-05 | 31-02, 31-04, 31-13, 31-17 | Loud skip on absent/unusable browser, never a silent pass | ✓ SATISFIED | Unchanged; CR-15's parser crash is tracked under UATX-06 (the AST-checker's own exit-code contract), not this requirement |
| UATX-06 | 31-02, 31-04, 31-06, 31-11, 31-12, 31-13, 31-16, 31-17 | AST ban on conditional/caught assertions and modifier calls; claim matches mechanism | ✗ BLOCKED | Round-4's spellings closed and re-verified; CR-14 (file-scoped census reopens the ban) and CR-15 (parser outside the could-not-run boundary) independently reproduced |

No orphaned requirements: every requirement ID declared across the twenty plans' `requirements:`
frontmatter (UATX-01 through UATX-06) is accounted for above, and `.planning/REQUIREMENTS.md` maps no
additional Phase 31 requirement ID beyond these six. `.planning/REQUIREMENTS.md` still lists all six as
unchecked (`- [ ]`) and the traceability table still reads "Gaps Found" for all six — correctly
unmoved, since this report also finds `gaps_found`. `.planning/ROADMAP.md` line 100 still shows Phase
31 unchecked.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/context-io.ts` | ~1486-1491 (`originIsTrusted`) | Second arm accepts any directory under `trustedRepoRoot()`, not only a recognised context-store shape | 🛑 Blocker | CR-16: an ordinary caller-authored directory inside the repository is treated as a trusted proof origin, defeating the very closure WR-17/CR-16's own predecessor fix claimed |
| `scripts/context-io.ts` | ~969-1002 (`writeNoteFile`), a new destination read added by the CR-11 fix | Unguarded `readFileSync` with no `O_NONBLOCK`/`fstat`/regular-file check, on the module's single write chokepoint | 🛑 Blocker | CR-12: a non-regular file (FIFO) at a note path hangs every writer forever, silently, on both streams |
| `scripts/context-io.ts` | ~2598-2606 (`projectRootFromWorkingDirectory`) | `isAtOrAboveHome` is asked before `dir` is inspected for a configuration or marker, at every step | 🛑 Blocker | CR-13: a repository rooted exactly at the home directory has its own dial silently replaced by the kit's lean default — the WR-15 defect direction, reopened |
| `scripts/runnable-ref/uat-spec-integrity.ts` | `deriveDeclaredNames` + the canonicaliser guard (~line 1435) | File-scoped suppression: any declaration of a banned head's name anywhere in the file disables the rewrite everywhere in it | 🛑 Blocker | CR-14: a single unrelated dead declaration reopens the WR-14/CR-10 spellings this phase has closed three times |
| `scripts/runnable-ref/uat-spec-integrity.ts` | `ts.createSourceFile` (~line 1605), outside the `try`/`catch` the WR-19 fix added | The parser itself is unguarded against a pathological parse input | 🛑 Blocker | CR-15: an uncaught `RangeError` bypasses the D-12 exit-code contract at a quarter of the previously-fixed chain's depth, contradicting this round's own recipe paragraph |
| `agent-factory/workflows/18-context-compaction.md` | line 75 | Asserts the copy-into-an-ordinary-directory workaround is "mechanically refused" | ⚠️ Warning | Same undisclosed defect as CR-16, one level up in agent-facing documentation |
| `agent-factory/checklists/browser-uat-recipe.md` | lines ~309-315 | Asserts the checker "does not exit through an uncaught exception, including a pathological one" | ⚠️ Warning | Same undisclosed defect as CR-15, one level up in agent-facing documentation |

No unreferenced `TBD`/`FIXME`/`XXX` debt markers were found in any source file changed since the
round-4 verification (`git diff --name-only a16786b..HEAD`, re-checked in this session).

### Human Verification Required

4 items, all pre-existing `UNKNOWN - verify` / manual-only items already recorded in `31-VALIDATION.md`
and not attempted by this round — none newly discovered by this verification, carried forward
unchanged through rounds 2, 3, 4 and this round (the attended Chrome lane's real interactive behavior,
the `claude auth status --json` predicate under API-key/long-lived-token auth, the Windows leg of every
browser probe, and the installer/uninstaller round-trip on a pre-existing host install).

### Gaps Summary

Two 🛑 Blocker requirement failures, both driven by FIVE independently-reproduced new Critical findings
— every one of them a defect the round-4 fix itself created or preserved, in the identical two
predicate families this phase has now paid for across five straight verification rounds.

1. **UATX-01 fails on THREE new defects (CR-12, CR-13, CR-16), all in mechanisms round 4's own fixes
   touched.** CR-11 (round 4's blocker — a caller-forged note silently overwriting admitted evidence)
   is genuinely closed: an attack aimed at an occupied destination id now declines, and the legitimate
   human-disposed promotion still works. But the SAME fix's origin-trust check (`originIsTrusted`) is
   over-broad for the ordinary in-repository case — a caller that copies legitimately-obtained bytes
   into a directory it creates itself, inside the repository, is treated as "trusted" and a promotion
   into a fresh destination id WRITES with no decline, which is verbatim the workaround Workflow 18
   tells the agent is refused. The SAME fix's new destination read is an unguarded blocking file read
   that hangs the module's single write chokepoint forever on a planted non-regular file. And the WR-21
   fix (closing the below-home ancestor case) never inspects a repository rooted exactly AT the home
   directory, silently replacing that repository's own dial with the kit's lean default — the identical
   WR-15 defect direction, one position over.

2. **UATX-06 fails on TWO new defects (CR-14, CR-15), both in mechanisms round 4's own fixes touched.**
   The WR-20 false-positive fix (a declared-names census) is file-scoped rather than scope-aware, so a
   single unrelated dead declaration anywhere in a spec file turns off the entire rename/namespace/
   fixture-parameter ban family for that file — reopening the WR-14/CR-10 spellings this phase has now
   closed three times. The WR-19 fix bounded the resolver and the construct-finder but left the
   TypeScript parser itself unguarded, so a pathological (not chained) construct crashes the process
   uncaught at a quarter of the previously-fixed chain's depth, with no measurement line — the exact
   WR-19 harm, reopened one register over, and falsifying this round's own recipe paragraph.

**The full excluded-e2e suite is green** (62/62 files, 3702/3704 tests, 2 pre-existing skips, re-run in
this session) **and exercises NONE of the five new defects found this round** — this project's standing
doctrine that a green suite is not proof for a safety predicate holds for the FIFTH consecutive
verification round on this phase.

**Progress since round 4 is real and should not be discounted.** All nine of round 4's closures
(CR-09 both variants, CR-10, CR-11, WR-17, WR-18, WR-19's chain-length half, WR-20's exact reproduced
spec, WR-21's below-home half) are independently re-verified intact in this session. The defects that
remain are each one register past what the prior round's fix decided, inside the SAME function the
prior fix touched — consistent with this repository's own documented pattern for these two predicate
families across all five rounds of this phase.

**This looks like it needs a sixth gap-closure round, not an override.** All five new blockers are the
same structural pattern this project's own doctrine treats as blocking: "ask how a gate is REACHED, not
only what it refuses" (CR-16 — the trust boundary is asked about the wrong shape; CR-12 — the
chokepoint gained a read nobody bounded) and "derive BOTH axes of a predicate, not just the one that
moved" (CR-13 — the home stop's precedence over inspection is a second axis from the ancestor-below-home
case WR-21 closed; CR-14/CR-15 — the census's scope and the parser's boundary are each a distinct axis
from the one 31-16/31-17 derived and closed). `31-REVIEW.md` supplies a fix sketch for every one of the
five, each narrowing an over-broad check rather than widening a set — the same shape of fix that closed
every prior round's findings. No override is suggested.

---

_Verified: 2026-09-09T18:30:00Z_
_Verifier: Claude (gsd-verifier)_

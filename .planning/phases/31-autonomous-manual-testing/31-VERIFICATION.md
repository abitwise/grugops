---
phase: 31-autonomous-manual-testing
verified: 2026-09-11T22:40:00Z
status: gaps_found
round: 8 (gap-closure round 8 verification — the EIGHTH verification pass of this phase, verifying
  gap-closure round 7's fix plans 31-32..31-38 against the code review at 31-REVIEW.md)
score: 5/6 must-haves verified
covered_files:
  - ".github/workflows/ci.yml"
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/WINDOWS.md"
  - ".planning/phases/31-autonomous-manual-testing/31-01-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-01-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-02-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-02-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-03-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-03-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-04-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-04-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-05-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-05-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-06-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-06-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-07-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-07-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-08-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-08-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-09-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-09-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-10-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-10-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-11-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-11-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-12-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-12-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-13-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-13-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-14-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-14-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-15-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-15-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-16-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-16-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-17-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-17-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-18-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-18-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-19-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-19-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-20-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-20-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-21-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-21-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-22-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-22-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-23-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-23-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-24-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-24-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-25-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-25-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-26-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-26-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-27-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-27-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-28-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-28-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-29-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-29-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-30-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-30-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-31-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-31-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-32-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-32-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-33-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-33-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-34-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-34-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-35-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-35-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-36-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-36-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-37-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-37-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-38-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-38-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-CONTEXT.md"
  - ".planning/phases/31-autonomous-manual-testing/31-DISCUSSION-LOG.md"
  - ".planning/phases/31-autonomous-manual-testing/31-PATTERNS.md"
  - ".planning/phases/31-autonomous-manual-testing/31-RESEARCH.md"
  - ".planning/phases/31-autonomous-manual-testing/31-REVIEW.md"
  - ".planning/phases/31-autonomous-manual-testing/31-VALIDATION.md"
  - ".planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md"
  - ".planning/phases/31-autonomous-manual-testing/31-round6-residual-dispositions.md"
  - ".planning/phases/31-autonomous-manual-testing/deferred-items.md"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-35-task1-red.json"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-36-task1-red.json"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-36-task2-red.json"
  - "agent-factory/checklists/00-index.md"
  - "agent-factory/checklists/browser-uat-recipe.md"
  - "agent-factory/contracts/context-note.md"
  - "agent-factory/workflows/05-pr-quality-gate.md"
  - "agent-factory/workflows/06-uat-pack.md"
  - "agent-factory/workflows/16-context-read-write.md"
  - "agent-factory/workflows/17-task-claim.md"
  - "agent-factory/workflows/18-context-compaction.md"
  - "docs/audit/29-style-dispositions/31-09.md"
  - "docs/audit/29-style-dispositions/31-14.md"
  - "docs/audit/29-style-dispositions/31-16.md"
  - "docs/audit/29-style-dispositions/31-17.md"
  - "docs/audit/29-style-dispositions/31-18.md"
  - "docs/audit/29-style-dispositions/31-19.md"
  - "docs/audit/29-style-dispositions/31-21.md"
  - "docs/audit/29-style-dispositions/31-22.md"
  - "docs/audit/29-style-dispositions/31-23.md"
  - "docs/audit/29-style-dispositions/31-27.md"
  - "docs/audit/29-style-dispositions/31-29.md"
  - "docs/audit/29-style-dispositions/31-30.md"
  - "docs/audit/29-style-dispositions/31-32.md"
  - "docs/audit/29-style-dispositions/31-33.md"
  - "docs/audit/29-style-dispositions/31-34.md"
  - "docs/audit/29-style-dispositions/31-35.md"
  - "docs/audit/29-style-dispositions/31-37.md"
  - "docs/audit/31-round4-residuals.md"
  - "docs/audit/31-round5-residuals.md"
  - "docs/audit/31-round6-residuals.md"
  - "docs/audit/31-round7-residuals.md"
  - "docs/audit/harness-false-result-instances.md"
  - "hooks/admission-guard.js"
  - "hooks/admission-guard.test.ts"
  - "hooks/admission-guard.ts"
  - "hooks/guard.js"
  - "hooks/guard.test.ts"
  - "hooks/guard.ts"
  - "hooks/hook-entry.js"
  - "hooks/hook-entry.ts"
  - "hooks/hooks.json"
  - "install/README.md"
  - "install/install.js"
  - "install/install.test.ts"
  - "install/install.ts"
  - "install/uninstall.js"
  - "install/uninstall.ts"
  - "package.json"
  - "scripts/check-banned-claims.js"
  - "scripts/check-banned-claims.test.ts"
  - "scripts/check-banned-claims.ts"
  - "scripts/check-foundation-guards.js"
  - "scripts/check-foundation-guards.test.ts"
  - "scripts/check-foundation-guards.ts"
  - "scripts/check-platform-shapes.js"
  - "scripts/check-platform-shapes.test.ts"
  - "scripts/check-platform-shapes.ts"
  - "scripts/check-uat-oracles.js"
  - "scripts/check-uat-oracles.ts"
  - "scripts/checkpoints.js"
  - "scripts/checkpoints.ts"
  - "scripts/chrome-lane-bar.test.ts"
  - "scripts/compactor.js"
  - "scripts/compactor.test.ts"
  - "scripts/compactor.ts"
  - "scripts/context-io-writer-set.test.ts"
  - "scripts/context-io.js"
  - "scripts/context-io.test.ts"
  - "scripts/context-io.ts"
  - "scripts/floor-invariance.test.ts"
  - "scripts/harness-instance-ledger.test.ts"
  - "scripts/nonblocking-reader-parity.test.ts"
  - "scripts/runnable-ref/fixtures/caught-assertion.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/clean.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/conditional-assertion.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/configured-soft.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/element-access-modifier.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/foreign-describe.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/foreign-framework.d.ts"
  - "scripts/runnable-ref/fixtures/foreign-soft-assert.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/import-rename.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/local-helper-head.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-call-link.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-call.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-family.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-group-clean.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/playwright-test.d.ts"
  - "scripts/runnable-ref/fixtures/shadowed-rename.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/testinfo-fixture-param.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/union-all-arms.uat.spec.ts"
  - "scripts/runnable-ref/uat-spec-integrity.js"
  - "scripts/runnable-ref/uat-spec-integrity.test.ts"
  - "scripts/runnable-ref/uat-spec-integrity.ts"
  - "scripts/uat-gate-exit-contract.test.ts"
  - "tsconfig.fixtures.json"
  - "tsconfig.tests.json"
  - "vitest.config.ts"
covered_digest: "v1:sha256:35b6d786f0b1c569522b5e5818bded3bd2fd8b7873cbcad78f9b04ee567880f6"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/6
  gaps_closed:
    - "CR-22 (promoteAdmitted's fall-through branch, reached by every non-human-stamped note, and the sibling admitAndAppend route both keyed the note write and its GOV-02 ledger event on two different, unreconciled parameters) — CLOSED at the coordinates the prior round measured. `promoteAdmitted` now derives `destinationRoot = governanceRootOf(to)` as the FIRST thing after `assertSafeTask`, above every return including the fall-through (verified by reading `scripts/context-io.ts:2553-2577` directly), and `admitAndAppend` now derives its own root at its entry too (`scripts/context-io.ts:5399`, verified by reading the source; the round-7 closing measurement's own §2.1 independently reproduced three of the four positions closed). Independent confirmation in THIS session was spent on the two NEW defects the same fix created (CR-26, CR-27, below) rather than re-driving the already-closed CR-22 coordinate a third time."
    - "CR-23 (the identity cutover's terminal `foreign` arm made `describe.skip`/`expect.soft` unbannable the moment they were imported from any non-Playwright module) — CLOSED. Verified by reading `resolveBannedModifier` in `scripts/runnable-ref/uat-spec-integrity.ts`: the `foreign` classification is now split by declaration provenance (`foreign-declared` vs `foreign-local`/`unresolved`), and a `foreign-declared` symbol is still asked of the spelling rule. Consistent with the round-7 closing measurement's independent reproduction (both CR-23 instances now refuse at exit 1, `tsc` exit 0)."
    - "CR-25 (the bounded framework-surface walk, `SURFACE_DEPTH_BOUND`/`SURFACE_NODE_BOUND`, silently returned a partial surface with no signal, so a framework member past the bound was accepted at exit 0 with zero diagnostic) — CLOSED. Verified by reading `scripts/runnable-ref/uat-spec-integrity.ts:1342-1441`: a reached bound now sets `truncated`, which routes through `SURFACE_TRUNCATED_CAUSE` to a could-not-run exit (2) rather than a silent accept. Consistent with the round-7 closing measurement's independent reproduction (both the 7-hop and 8-hop constructions now exit 2 with a named cause, where the 8-hop construction previously exited 0 with zero bytes on stderr)."
    - "The full excluded-e2e suite regression (a self-referential coverage oracle, `CORPUS_COVERAGE`, went red on a review-commit that changed zero source bytes) — CLOSED by removing the axis (D-33, a disclosed gate lowering) and handing the review-to-corpus coverage obligation to a once-per-round one-shot. Independently re-run in THIS session: `npx vitest run --exclude '**/scripts/e2e/**'` → `Test Files 66 passed (66)`, `Tests 4208 passed | 2 skipped (4210)`, exit 0 — matching the round-7 closing measurement's own reading exactly."
  gaps_remaining:
    - "UATX-01 — the SAME fix that closed CR-22 (moving the one-root derivation to the entry of `promoteAdmitted` and `admitAndAppend`) created TWO NEW Criticals at adjacent coordinates in the identical function pair, BOTH independently reproduced in THIS session against the committed `scripts/context-io.js` at HEAD `f698bec` (a scratch script run from outside the repository tree, deleted after use): CR-26 — `admitAndAppend`'s gated branch derives `const ledgerRoot = governanceRootOf(contextRoot) ?? repoRoot;` (`scripts/context-io.ts:5399`), which is a FAIL-OPEN: when `contextRoot` does not resolve to a governed store, the ledger falls back to the caller's `repoRoot` — precisely the split CR-22 was raised on — while the sibling `promoteAdmitted` DECLINES the identical shape by name. Reproduced: `admitAndAppend(\"T-REPRO1\", …, UNGOV.store, THIRD.root)` wrote the note into the ungoverned store (1 note) and the GOV-02 ledger event into THIRD (1 ledger line), with the ungoverned store's own ledger absent — a human-disposed finding and its own audit record split across two repositories. CR-27 — `promoteAdmitted`'s fall-through (`scripts/context-io.ts:2577`, `return appendNote(task, note, body, to, undefined, destinationRoot);`) now hands `destinationRoot` (derived from the caller's `to`) into `appendNote`'s `repoRoot` parameter, and that parameter is DOCUMENTED (`scripts/context-io.ts:1569-1578`) to answer BOTH the governance-dial read AND the ledger-record question for `admit()`, which is byte-frozen and cannot separate the two. So the caller-supplied DESTINATION now decides which repository's governance configuration adjudicates the admission, not the trusted root. Reproduced: with the TRUSTED root's own `factory.config.json` deliberately unreadable (`{ this is not json`, which D-14 must fail-closed on), `promoteAdmitted(…, to=DEST-store, repoRoot=TRUSTED.root)` WROTE the note (DEST has a readable config), while the CONTROL — the identical call with `to=TRUSTED.store` (same repository as `repoRoot`) — correctly REFUSED with the D-14 fail-closed message. Same note, same caller, same unreadable trusted configuration: refused when the destination is the caller's own store, admitted when the destination names a different, permissively-configured store. This is the EIGHTH time in this phase's gap-closure history that a round's own fix created a new bypass one register past the coordinate the prior round measured, in this exact predicate family (the write-path one-repository-per-action rule)."
  regressions: []
gaps:
  - truth: "UATX-01 — a committed Playwright spec, re-run by the §14 gate, is the machine-verifiable evidence floor; an agent's narration or a caller-supplied claim never becomes indistinguishable from genuine admitted evidence — including through the mechanism that carries an already-admitted note forward through compaction, and including which repository's governance configuration is consulted to decide whether a re-binding may proceed at all."
    status: failed
    reason: >
      Round 7's own fix for CR-22 (entry-level derivation of the one-repository-per-action root in
      `promoteAdmitted` and `admitAndAppend`) is genuinely correct at the coordinate CR-22 named — the
      derivation now sits above every branch in both functions, verified by reading the source. But
      the SAME fix introduced two new Criticals, both independently reproduced in THIS session against
      the committed `scripts/context-io.js` at HEAD `f698bec`, in a scratch script outside the
      repository tree:

      CR-26 — `admitAndAppend`'s gated branch derives its ledger root as
      `governanceRootOf(contextRoot) ?? repoRoot` (`scripts/context-io.ts:5399`). When `contextRoot`
      does not resolve to a governed store, this is a FAIL-OPEN to the caller's `repoRoot` — exactly
      the split CR-22 was raised on — while the sibling `promoteAdmitted` DECLINES the identical shape
      (`destination-outside-governed-store`, `scripts/context-io.ts:2553-2560`). Reproduced:
      `admitAndAppend("T-REPRO1", {kind:"finding", verified_by:"human:alice", ...}, "a body",
      UNGOV.store, THIRD.root)` — where `governanceRootOf(UNGOV.store)` is asserted `null` and
      `governanceRootOf(THIRD.store)` is asserted `THIRD.root` before the call — returned an id, wrote
      the note into the UNGOVERNED store (1 note) and appended the GOV-02 ledger event into THIRD (1
      ledger line), with the ungoverned store's own ledger absent. A human-disposed finding and its
      own audit record landed in two different repositories, on the exact route
      `agent-factory/workflows/18-context-compaction.md` names by hand as writing both together.

      CR-27 — `promoteAdmitted`'s fall-through (`scripts/context-io.ts:2577`) now calls
      `appendNote(task, note, body, to, undefined, destinationRoot)`. `appendNote`'s sixth parameter is
      documented, in the same module, to answer BOTH the governance-dial read and the GOV-02
      ledger-record question inside `admit()` (`scripts/context-io.ts:1569-1578`, and `admit()` is
      byte-frozen — `ADMIT_FROZEN_SHA256` — so it cannot be given two separate roots). Passing
      `destinationRoot` there means the CALLER-SUPPLIED destination now decides which repository's
      governance configuration adjudicates the admission, not the trusted root the dial is supposed to
      answer from. Reproduced: a `TRUSTED` root whose own `.grugops/factory.config.json` is
      deliberately unreadable (`{ this is not json`, the shape D-14 requires a fail-closed refusal
      for), and a `DEST` root with a readable, permissive config.
      `promoteAdmitted("T-DIAL", "src-id", {kind:"observation", verified_by:"", ...}, "a body",
      "irrelevant-from", DEST.store, TRUSTED.root)` — with `governanceRootOf(DEST.store) === DEST.root`
      asserted first — WROTE the note (id returned, 1 note landed in DEST). The CONTROL — the
      identical call with `to=TRUSTED.store` (destination equals the caller's own repository) —
      correctly REFUSED with the D-14 fail-closed message
      (`"admission REFUSED (UNKNOWN - verify): a governance …"`). Same note, same caller, same
      unreadable trusted configuration: refused when the destination is the caller's own store,
      admitted when the destination names a different, permissively-configured governed store — an
      agent that can construct a governed directory (a `.git` marker plus a readable
      `factory.config.json`, which `governanceRootOf` accepts as any recognised store) can launder a
      promotion the caller's own trusted repository's governance would have refused.

      This is the EIGHTH time in this phase's gap-closure history (rounds 2 through 7, and now this
      round's own fix) that closing a Critical at its filed coordinate created a new bypass one
      register past it, in the identical predicate family: the write-path one-repository-per-action
      rule.
    artifacts:
      - path: "scripts/context-io.ts"
        issue: "Line 5399 (`admitAndAppend`'s gated branch): `const ledgerRoot = governanceRootOf(contextRoot) ?? repoRoot;` falls open to the caller's `repoRoot` when `contextRoot` is ungoverned, splitting the note and its GOV-02 ledger event across two repositories — independently reproduced. The sibling `promoteAdmitted` (line 2553-2560) declines the identical shape by name."
      - path: "scripts/context-io.ts"
        issue: "Line 2577 (`promoteAdmitted`'s fall-through): `return appendNote(task, note, body, to, undefined, destinationRoot);` hands the caller-supplied destination into `appendNote`'s single `repoRoot` parameter, which `admit()` (byte-frozen) uses for BOTH the governance-dial read and the ledger record — so the destination now decides which repository's configuration adjudicates the admission, not the trusted root — independently reproduced."
      - path: "agent-factory/workflows/18-context-compaction.md"
        issue: "States that both promoteAdmitted and admitAndAppend derive the owning repository at the route's entry, above every branch, and that both halves of the action key on that one answer — independently measured false of admitAndAppend's ungoverned-contextRoot case (CR-26) and does not disclose that promoteAdmitted's fall-through now routes the governance-DIAL question through the caller's destination rather than the trusted root (CR-27). Carried as WR-39/WR-40 in `31-REVIEW.md`, not independently re-verified line-by-line this session."
    missing:
      - "Make admitAndAppend's gated-branch ledger derivation agree with promoteAdmitted's: either decline `destination-outside-governed-store` when `governanceRootOf(contextRoot)` is null (matching the sibling route), or — if a fail-open to `repoRoot` for an ungoverned context store is deliberately accepted — make `admit()`'s own non-gated route answer the identical ungoverned-store question the same way, publish the accepted shape as a `WRITE_PATH_RESIDUALS` member with this exact reproduction, and narrow `18-context-compaction.md`'s universal claim to the governed case only."
      - "Separate the governance-DIAL question from the ledger-RECORD question at `promoteAdmitted`'s fall-through rather than collapsing both onto `appendNote`'s one `repoRoot` parameter: give `appendNote`/`admit()` a ledger-root parameter distinct from the dial-root parameter (the unfreeze `R-31-33-01` already names as the cost of finishing this class of fix), so the fall-through can pass `destinationRoot` as the ledger address while the dial keeps reading from the caller's trusted `repoRoot`. If routing the dial through the destination is instead a deliberate reversal of D-31/WR-10, that requires a dated human decision and a re-stated `repoRoot` docstring, not an argument swap inside a plan whose stated subject was where the ledger line lands."
      - "Add a derived test enumerating every call site that supplies `appendNote`'s `repoRoot` argument from a value other than the caller's own trusted root, and assert each one's disposition (decline vs. accept) is the SAME disposition the sibling write-both route takes for the identical shape — so a future third write-both route, or a future edit to either of these two, cannot reopen this class silently a ninth time."
deferred: []
advisory: []
behavior_unverified_items: []
human_verification:
  - test: "The attended Claude-in-Chrome lane opens under real interactive auth, pauses for a human on a login/challenge page, and produces only a human-stamped finding + artifact-ref (never a gate stamp)."
    expected: "The lane behaves as documented in agent-factory/checklists/browser-uat-recipe.md's attended lane; no route to a §14-gate stamp is exercised in practice."
    why_human: "Requires an attended Claude Code session with the Claude-in-Chrome browser extension installed and a real interactive login; not reachable in CI and not reachable on this box. Carried forward unchanged through rounds 2-8 (R-01)."
  - test: "The claude auth status --json fail-closed predicate (D-10) behaves correctly under an API-key-only box and under a long-lived setup token."
    expected: "Both configurations are a loud skip naming the failing clause, never a silent open."
    why_human: "Research assumptions A2/A3 are UNKNOWN - verify; neither configuration is reachable without destroying this box's real credentials. Carried forward unchanged (R-02)."
  - test: "Both browser-absence probe stages, and the whole spec-integrity runnable, on a Windows host."
    expected: "Exit 2 with the browser-absent marker when browsers are missing; parser-absent marker when typescript cannot be resolved; the FIFO class (CR-12/CR-17), the transpile behaviour behind CR-18/CR-23, and the depth-adjacency behind CR-15/CR-25 all established on darwin/Node v24.12.0 only."
    why_human: "UNKNOWN - verify per the standing Windows posture (WINDOWS.md); not testable on darwin. A real windows-latest reading has not been taken. Carried forward, unchanged (R-03)."
---

# Phase 31: Autonomous Manual Testing Verification Report

**Phase Goal:** An agent can drive a real browser to produce UAT evidence, and the only thing that
counts as evidence is an artifact the §14 gate re-runs — never the agent's narration of what it saw.
**Verified:** 2026-09-11T22:40:00Z
**Status:** gaps_found
**Re-verification:** Yes — this is the EIGHTH verification pass on this phase, verifying gap-closure
round 7's fix plans (`31-32`…`31-38`: D-33 review-coverage-axis removal, D-34 write-path entry-level
derivation, D-35 foreign-provenance split, D-36 bounded-walk truncation signal, D-37 platform-shapes
control observability, D-38 delivered-root binding) against `31-REVIEW.md`'s round-7 code review (2
Critical — CR-26, CR-27 — 6 Warning, 3 Info) and `docs/audit/31-round7-residuals.md` (round 7's own
closing measurement, `31-38`).

## Round History

| Round | Status | Score | What moved |
|---|---|---|---|
| 1 | gaps_found | — | Three planted constructs found and fixed by 31-06 |
| 2 | gaps_found | 4/6 | CR-05 (fabricated §14-gate stamp) and CR-06 (two `test.describe.*.only` spellings) |
| 3 | gaps_found | 5/6 | CR-05/CR-06 closed; CR-07/WR-14 (call-link + import-rename evasions) and CR-08 (compaction refuses a human-disposed finding) found |
| 4 | gaps_found | 4/6 | CR-07/WR-14/CR-08/WR-15 closed; CR-09 x2/CR-10/CR-11/WR-19/WR-20/WR-21 found |
| 5 | gaps_found | 4/6 | CR-09 x2/CR-10/CR-11/WR-19/WR-20/WR-21 closed; CR-12/CR-13/CR-14/CR-15/CR-16 found |
| 6 | gaps_found | 4/6 | CR-12/CR-13/CR-14/CR-15/CR-16 closed; CR-17/CR-18/CR-19/CR-20/CR-21 found |
| 7 | gaps_found | 4/6 | CR-17/CR-18/CR-19/CR-20/CR-21 all closed as filed; CR-22/CR-23/CR-24/CR-25 found (plus a self-referential coverage-test regression) |
| **8 (this round)** | **gaps_found** | **5/6** | CR-22/CR-23/CR-24/CR-25 and the regression all CLOSED, independently re-confirmed against source and the full suite. UATX-06's predicate family (the AST modifier ban) is VERIFIED for the FIRST TIME in this phase's eight verification rounds — no live, reproducible bypass remains, only disclosed test-completeness residuals (WR-37, WR-38). But UATX-01's predicate family produced TWO NEW Criticals (CR-26, CR-27) one register past the exact coordinate CR-22's fix touched, BOTH independently reproduced in this session against the committed `.js` — the EIGHTH consecutive occurrence of "the fix that closed the prior round's Critical created the next one," always in this same write-path predicate family. |

The score moves from 4/6 to 5/6 this round — genuine, independently-confirmed movement on UATX-06 —
while UATX-01 remains the sole blocked requirement, now on its eighth round in this exact predicate
family.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | UATX-01 — a committed Playwright spec, re-run by the §14 gate, is the machine-verifiable evidence floor; an agent's narration/self-supplied claim never produces a stamp, and the write path keys a note and its own audit record on one governance-derived repository, on every route and every branch | ✗ FAILED — see gap 1 (CR-26, CR-27) | CR-22 genuinely closed at its filed coordinate (verified by reading `scripts/context-io.ts:2553-2577` and `:5399`, both now deriving the one-root rule at the function's ENTRY). But the SAME fix created CR-26 (admitAndAppend's gated branch fails OPEN to `repoRoot` for an ungoverned `contextRoot`, unlike the sibling `promoteAdmitted`'s decline) and CR-27 (promoteAdmitted's fall-through now routes the governance-DIAL question, not just the ledger record, through the caller-supplied destination) — both independently reproduced in this session against the committed `.js` at HEAD `f698bec`. |
| 2 | UATX-02 — browser MCP tooling documented and pinned for all five host CLIs; `package.json` gains nothing; the pin authority cannot itself be defeated by a floating specifier | ✓ VERIFIED | Unchanged this round; `node scripts/check-foundation-guards.js` → `ALL CHECKS PASSED` in this session, part of the 66-file green suite this session independently re-ran. Not contested by `31-REVIEW.md`. |
| 3 | UATX-03 — Claude in Chrome is attended-only and structurally barred from producing a `§14-gate` stamp | ✓ VERIFIED | `scripts/chrome-lane-bar.test.ts` byte-untouched over the round (`git diff --stat d484b9e..HEAD` does not name it), part of the green suite this session re-ran. Not contested. |
| 4 | UATX-04 — evidence carries commit SHA + gate-run id + content hash; a note whose SHA is not the HEAD the gate ran against is refused | ✓ VERIFIED | Unchanged; `admit()`'s frozen span is untouched this round — `git hash-object hooks/guard.ts` = `669725bc1c616ab57123e22090d93d57eff1b001`, equal to `FROZEN_GUARD_BLOB`, independently re-verified in this session. None of CR-26/CR-27/the six Warnings/three Info items name this surface. |
| 5 | UATX-05 — an absent/unusable browser produces a loud skip leaving the UAT `pending`, never a silent pass | ✓ VERIFIED | Unchanged; `node scripts/check-uat-oracles.js` → `ALL CHECKS PASSED` in this session. No round-7 finding touches the browser-absence probe. |
| 6 | UATX-06 — conditional or caught assertions, and the D-14 arm-(c) modifier-call bans, are rejected over the TypeScript AST, and the recipe's claim matches exactly what the checker decides | ✓ VERIFIED (FIRST TIME) | CR-23 and CR-25 — the two Criticals round 7 found against this requirement — are CLOSED, verified by reading `resolveBannedModifier`'s provenance split and `frameworkSurface`'s truncation-signal wiring directly in `scripts/runnable-ref/uat-spec-integrity.ts`. No live, independently-reproducible bypass remains against this requirement. Two disclosed, non-blocking residuals stand: WR-37 (four exception routes inside the surface walk swallow a subtree without recording `truncated`, verified present in source at lines 1354-1356/1426-1428/1437-1439/1459-1461, but exploiting it needs an unreproducible TypeScript-checker throw — the same class this phase already carries as an open, unreproduced residual for the walk's inner `catch` blocks) and WR-38 (the new "published ban member must be reachable" test binding covers only 2 of 4 published ban sets — a test-completeness gap on this round's own new safeguard, not a demonstrated bypass of the ban itself, verified by reading the binding at `scripts/runnable-ref/uat-spec-integrity.test.ts:9678-9707`). |

**Score:** 5/6 truths verified (UATX-02, UATX-03, UATX-04, UATX-05, UATX-06). UATX-01 fails — the
eighth consecutive round in which this phase's write-path predicate family closes the exact
coordinate the prior round measured and a new bypass reappears one register over, inside the same fix.

### Deferred Items

None. No gap identified this round is addressed by a later phase in the roadmap; UATX-01's remaining
gap is Phase 31's own predicate family.

### Advisory (New Scope, Unevidenced)

None — every finding in this report is either independently reproduced against the committed `.js` in
this session (CR-26, CR-27) or independently confirmed by reading the current source directly (CR-22,
CR-23, CR-25's closures; WR-37, WR-38's continued presence), with a command or a file:line citation
recorded for each.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/context-io.ts` — `promoteAdmitted` | Every return path of the function is forced through ONE derived root before any write, with the SAME disposition (decline vs. accept) as every other route facing the identical input shape | ⚠️ PARTIAL | The one-root derivation now sits at the function's entry (CR-22 closed, verified). But the fall-through's `appendNote` call routes the governance-DIAL question through the derived destination rather than the trusted `repoRoot` — independently reproduced (CR-27). |
| `scripts/context-io.ts` — `admitAndAppend` | The note write and its GOV-02 ledger append are keyed on the same derived repository, with the same disposition as the sibling route for an ungoverned store | ✗ FAILED | The gated branch's `ledgerRoot = governanceRootOf(contextRoot) ?? repoRoot` (line 5399) falls open where `promoteAdmitted` declines — independently reproduced (CR-26). |
| `agent-factory/workflows/18-context-compaction.md` | Agent-facing prose about the two write-both routes matches what the mechanism actually does | ⚠️ PARTIAL | States both routes derive the owning repository at their entry, above every branch, with both halves keyed on one answer — true of the closed CR-22 coordinate, false of `admitAndAppend`'s ungoverned-store fail-open (CR-26) and silent about the fall-through's dial-routing change (CR-27). Not independently re-read line-by-line this session; consistent with `31-REVIEW.md`'s WR-39/WR-40 citations. |
| `scripts/runnable-ref/uat-spec-integrity.ts` — `resolveBannedModifier` | A head the framework does not itself declare (`describe`, retained specifically for this case) is checked against the spelling rule when it resolves to a declared foreign symbol | ✓ VERIFIED | The `foreign` classification is split by declaration provenance (`foreign-declared` consults the spelling rule; verified by reading the source directly) — CR-23 closed. |
| `scripts/runnable-ref/uat-spec-integrity.ts` — `frameworkSurface` | A framework member the bounded walk cannot reach routes to a could-not-run exit, never to a silent `foreign` classification | ✓ VERIFIED, with a disclosed residual | The node/depth bounds now set `truncated`, routing to exit 2 via `SURFACE_TRUNCATED_CAUSE` — CR-25 closed, verified by reading lines 1342-1441. WR-37: four exception-throw routes inside the same walk (lines 1354-1356, 1426-1428, 1437-1439, 1459-1461) still swallow a subtree silently, verified present in source; not independently exploitable this session (requires a TypeScript-checker throw this repository cannot synthesise, the same unreproducible class as the walk's already-disclosed inner `catch` residual). |
| `agent-factory/checklists/browser-uat-recipe.md` | Ban set, completeness claim, and the boundary list, quoted from and equal to the exported constants and their proven strength | ✓ VERIFIED, with a disclosed test-coverage gap | The `describe`/`expect.soft` reachability claim and both surface-walk bounds are now published and bound by both-directions equality tests inside the green suite. WR-38: the new "published member must be reachable" binding covers `BANNED_MODIFIER_HEADS`/`BANNED_EXACT_PATHS` (2 of 4 published ban sets) but not `BANNED_MODIFIER_TAILS`/`BANNED_CONFIGURED_PATHS`, verified by reading `scripts/runnable-ref/uat-spec-integrity.test.ts:9678-9707` — the underlying ban logic applies the same identity resolution to all four sets, so this is a monitoring gap on the round's own new safeguard, not a demonstrated bypass. |
| `scripts/runnable-ref/uat-spec-integrity.test.ts` — the review-to-corpus coverage axis | The axis no longer red-lines on a per-round rewrite of `31-REVIEW.md` that changes zero source bytes | ✓ VERIFIED | The self-referential axis is deleted (D-33); the row-count floor is derived from this file's own AST instead. Independently confirmed: the full suite (which includes this file's tests) is green in this session — `Test Files 66 passed (66)`, `Tests 4208 passed \| 2 skipped (4210)`, exit 0. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `promoteAdmitted`'s entry | every one of its return paths, including the human-stamp fall-through | the one-root derivation now sits above the first branch | ✓ WIRED | Verified by reading `scripts/context-io.ts:2553-2577`: no return exists between the function's entry and the `destinationRoot` derivation. |
| `promoteAdmitted`'s fall-through (`appendNote` call) | the trusted governance dial (`trustedRepoRoot()`) | NOT wired — the call passes `destinationRoot`, not the caller's `repoRoot`, into the parameter `admit()` uses for the dial read | ✗ NOT_WIRED | Independently reproduced (CR-27): a call whose destination carries a permissive config is admitted despite the caller's own trusted root carrying an unreadable one. |
| `admitAndAppend`'s gated branch | the sibling `promoteAdmitted`'s disposition for an ungoverned store | NOT wired — the two routes answer the identical shape differently (fail-open vs. decline) | ✗ NOT_WIRED | Independently reproduced (CR-26): the note lands in the ungoverned store, the GOV-02 event lands in a different, governed one. |
| `resolveBannedModifier`'s `foreign-declared` classification | `isBannedModifierPath` (the spelling rule) | WIRED — a declared-foreign symbol is now asked of the spelling rule | ✓ WIRED | Verified by reading the source; consistent with the round-7 closing measurement's independent reproduction of both CR-23 instances now refusing. |
| `frameworkSurface`'s bounded walk | the could-not-run exit boundary (D-28) | WIRED — a truncated walk now sets `truncated` and routes through `SURFACE_TRUNCATED_CAUSE` | ✓ WIRED | Verified by reading `scripts/runnable-ref/uat-spec-integrity.ts:1362-1441`; the four exception-throw routes named in WR-37 are the one part of this walk NOT yet wired to the same signal (disclosed, not independently exploited). |

### Data-Flow Trace (Level 4)

Not separately applicable beyond the Key Link table above: this phase's artifacts are deciders and
writers rather than rendered UI, so "does the value reach the render" collapses into "does the
decision reach the caller unchanged," which the Key Link and Behavioral Spot-Check tables cover.

### Behavioral Spot-Checks

All rows marked "this session" were run in THIS verification session against the committed `.js` at
HEAD `f698bec` (`git status --porcelain -- scripts hooks agent-factory install docs .github
vitest.config.ts package.json .planning/REQUIREMENTS.md .planning/ROADMAP.md` empty before and after),
in scratch scripts under the session scratchpad, outside the tracked tree.

| # | Behavior | Command | Result | Status |
|---|----------|---------|--------|--------|
| 1 | **HEAD identity, this session** | `git log -1 --format='%H %ci'` | `f698becf0ed0f38d36596737da2d4ed79f3f4c3e 2026-09-11 22:24:19 +0300` — the commit `31-REVIEW.md` itself was written against | premise confirmed |
| 2 | **CR-26, this session:** `admitAndAppend`'s gated branch, ungoverned `contextRoot`, governed `repoRoot` | three real `mkdtemp` governance roots (THIRD governed, UNGOV ungoverned), each asserted `governanceRootOf(store)` first; `admitAndAppend("T-REPRO1", {kind:"finding", verified_by:"human:alice", ...}, "a body", UNGOV.store, THIRD.root)` | `PREMISE governanceRootOf(THIRD.store) = <THIRD.root>` (matches); `PREMISE governanceRootOf(UNGOV.store) = null` (matches); `result: {"id":"20260911T000000Z-qe-finding-7a4d92f0","findings":[]}`; `UNGOV notes= 1 ledger= null`; `THIRD notes= 0 ledger= 1` | ✗ FAIL (confirms CR-26) — note and its own audit record split across two repositories |
| 3 | **CR-27, this session:** `promoteAdmitted`'s fall-through, unreadable trusted config, permissive destination config, vs. the CONTROL (destination = trusted root) | `TRUSTED` root's config `{ this is not json`; `DEST` root's config valid; `governanceRootOf(DEST.store) === DEST.root` asserted first; `promoteAdmitted("T-DIAL", "src-id", {kind:"observation", verified_by:"", ...}, "a body", "irrelevant-from", DEST.store, TRUSTED.root)`, then the CONTROL with `to=TRUSTED.store` | `PREMISE governanceRootOf(DEST.store) = true`; `WROTE id: 20260911T000000Z-qe-observation-709644fc`; `notes in DEST: 1`; CONTROL: `CONTROL REFUSED: … admission REFUSED (UNKNOWN - verify): a governance …` | ✗ FAIL (confirms CR-27) — same unreadable trusted config, refused at the caller's own store, admitted at a different store |
| 4 | **CR-23 closure, source-read this session** | direct read of `scripts/runnable-ref/uat-spec-integrity.ts:1330-1470` (not re-executed — see reproduction budget note below) | `foreign-declared`/`foreign-local`/`unresolved` provenance split present; `isStandardLibraryOnly` and `containerTypeArguments` present per D-36's own documented design | source confirms closure |
| 5 | **CR-25 / WR-37, source-read this session** | direct read of `scripts/runnable-ref/uat-spec-integrity.ts:1330-1470` | `truncated ??= "node-bound"` (:1367), `truncated ??= "depth-bound"` (:1412, gated by `hasExpandableMembers`) are wired to the could-not-run exit; FOUR `catch` sites (:1354-1356, :1426-1428, :1437-1439, :1459-1461) swallow without setting `truncated` — confirms WR-37 as written | source confirms both CR-25's closure and WR-37's continued presence |
| 6 | **WR-38, source-read this session** | `grep -n "BANNED_MODIFIER_TAILS\|BANNED_CONFIGURED_PATHS\|REACH-" scripts/runnable-ref/uat-spec-integrity.test.ts` and direct read of `:9678-9707` | The binding derives its expected set from `BANNED_MODIFIER_HEADS`/`BANNED_EXACT_PATHS` only; no `REACH-TAIL-*`/`REACH-CONFIGURED-*` rows exist | source confirms WR-38 as written |
| 7 | **The full excluded-e2e regression suite, this session** | `npx vitest run --exclude '**/scripts/e2e/**'` | `Test Files 66 passed (66)`, `Tests 4208 passed \| 2 skipped (4210)`, `Duration 424.26s`, `EXIT=0` | ✓ PASS — matches `docs/audit/31-round7-residuals.md` §8.1's own reading exactly (66/4208/2 skipped/exit 0) |
| 8 | Committed `.js` freshness, this session | `npm run freshness` | `All build outputs fresh: 61 committed .js file(s) match a rebuild of their sources.` Set equality with the filesystem walk: 0/0 mismatches. | ✓ PASS |
| 9 | Hook manifest freshness, this session | `npm run freshness:hook-manifest` | `Hook manifest fresh: 2 decider(s), 26 module hash(es) match a fresh derivation.` | ✓ PASS |
| 10 | Foundation guards / UAT oracles / platform shapes, this session | `node scripts/check-foundation-guards.js`, `node scripts/check-uat-oracles.js`, `node scripts/check-platform-shapes.js` | All three `ALL CHECKS PASSED`; platform-shapes: `DRIVEN (13)`, all four CONTROL rows `ordinary outcome (correct)`, `SKIPPED SHAPES (0)` | ✓ PASS |
| 11 | Byte-frozen deploy guard, this session | `git hash-object hooks/guard.ts` | `669725bc1c616ab57123e22090d93d57eff1b001`, equal to `FROZEN_GUARD_BLOB` | ✓ PASS |
| 12 | Requirement/roadmap checkbox state, this session | `grep -n "Phase 31" .planning/ROADMAP.md`; `grep -n "UATX-0" .planning/REQUIREMENTS.md` | `- [ ] **Phase 31: Autonomous Manual Testing**`; all six `UATX-0N` unchecked, all six traceability rows `Gaps Found` | ✓ PASS — unmoved, correct pending this report |

**Reproduction budget note.** This session independently reproduced the two NEW Criticals (CR-26,
CR-27) with live scratch scripts against the committed `.js`, and independently confirmed the four
CLOSED Criticals (CR-22, CR-23, CR-24, CR-25) and the regression's closure by direct source reading
plus one full, independent suite run — rather than re-running each of the ~30 probe constructions
`docs/audit/31-round7-residuals.md` already drove and recorded with commands and outputs at the
identical commit this session measures (`f698bec`, unchanged in source from that document's own
`3baab0d` reading — `git diff --stat 3baab0d..f698bec` touches only `31-REVIEW.md` itself, the same
zero-source-diff shape verified in round 7). Re-deriving all ~30 of those constructions a third time
(round 7's verifier already re-drove them once against round 6's closures) would not have produced new
evidence; the reproduction effort in this session was spent finding and confirming the two NEW
defects instead, which is where new evidence was possible.

**Cleanup verified.** `git status --porcelain -- scripts hooks agent-factory install docs .github
vitest.config.ts package.json .planning/REQUIREMENTS.md .planning/ROADMAP.md` printed empty both before
and after this session's probes and the full suite run. No probe artifact, temp directory, or scratch
file from this session was written inside the tracked repository tree — every reproduction ran under
the session's own scratchpad directory using `mkdtemp` under the OS temp directory.

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| UATX-01 | 31-01, 31-04, 31-05, 31-09, 31-10, 31-14, 31-18, 31-19, 31-21, 31-22, 31-23, 31-27, 31-29, 31-33, 31-36, 31-37, 31-38 | Committed spec = evidence floor; narration never a stamp; write path keys a note and its audit record on one repository | ✗ BLOCKED | CR-22 closed at its coordinate; CR-26 (admitAndAppend's ungoverned-store fail-open) and CR-27 (the fall-through's dial-routing regression) both independently reproduced this session |
| UATX-02 | 31-03, 31-06, 31-07 | Browser MCP tooling documented + pinned, 5 hosts; pin authority cannot itself float | ✓ SATISFIED | Unchanged, not contested; part of the green suite this session re-ran |
| UATX-03 | 31-03, 31-04, 31-15 | Attended Chrome lane structurally barred from gate stamp | ✓ SATISFIED | Unchanged, byte-untouched this round, part of the green suite |
| UATX-04 | 31-01, 31-05, 31-14 | Evidence provenance (SHA/gate_run/content_hash); mismatched SHA refused | ✓ SATISFIED | Unchanged; frozen span re-verified equal this session; no round-7 finding touches this surface |
| UATX-05 | 31-02, 31-04, 31-13, 31-17 | Loud skip on absent/unusable browser, never a silent pass | ✓ SATISFIED | Unchanged; `check-uat-oracles.js` re-run green this session |
| UATX-06 | 31-02, 31-04, 31-06, 31-11, 31-12, 31-13, 31-16, 31-17, 31-24, 31-25, 31-28, 31-32, 31-34, 31-35 | AST ban on conditional/caught assertions and modifier calls; claim matches mechanism | ✓ SATISFIED (first time) | CR-23 and CR-25 both closed, confirmed by direct source read; two disclosed, non-blocking residuals remain (WR-37, WR-38) |

No orphaned requirements: every requirement ID declared across the thirty-eight plans' `requirements:`
frontmatter (UATX-01 through UATX-06) is accounted for above, and `.planning/REQUIREMENTS.md` maps no
additional Phase 31 requirement ID beyond these six. `.planning/REQUIREMENTS.md` still lists all six as
unchecked (`- [ ]`) and the traceability table still reads "Gaps Found" for all six — correctly
unmoved, since this report also finds `gaps_found` (only UATX-01 blocks; a verification report does not
flip individual requirement rows independent of the phase-level gate, and this phase stays blocked
until UATX-01 closes). `.planning/ROADMAP.md` line 100 still shows Phase 31 unchecked.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/context-io.ts` | `admitAndAppend`'s gated branch (5399) derives `governanceRootOf(contextRoot) ?? repoRoot` — a fail-open where the sibling route declines | The entry-level fix installed a derivation without matching the sibling route's disposition for the identical failure shape | 🛑 Blocker | CR-26: a human-disposed finding and its own GOV-02 audit record can land in two different repositories through the exact route Workflow 18 names by hand as writing both together |
| `scripts/context-io.ts` | `promoteAdmitted`'s fall-through (2577) passes `destinationRoot` into `appendNote`'s single dial-and-ledger `repoRoot` parameter | The fix moved the RECORD onto the derived destination (correct, per CR-22) but the DIAL rides the same parameter and moved with it (not stated as a decision anywhere) | 🛑 Blocker | CR-27: the caller-supplied destination, not the caller's own trusted repository, now decides whose governance configuration adjudicates a re-binding — a D-14 fail-closed refusal at the trusted root can be routed around by naming a different, permissively-configured destination |
| `scripts/runnable-ref/uat-spec-integrity.ts` | Four exception-throw sites inside `frameworkSurface`'s walk (1354-1356, 1426-1428, 1437-1439, 1459-1461) swallow a subtree without setting `truncated` | A checker-throw at these positions silently drops a subtree from the surface, the same class D-36 closed for the depth/node bounds | ⚠️ Warning | WR-37: not independently exploited this session (requires a TypeScript-checker throw this repository cannot synthesise); disclosed, carried, non-blocking, same class as the walk's already-accepted inner-`catch` residual |
| `scripts/runnable-ref/uat-spec-integrity.test.ts` | The "published ban member must be reachable" binding (9678-9707) derives its expected set from 2 of 4 published ban constants | `BANNED_MODIFIER_TAILS`/`BANNED_CONFIGURED_PATHS` have no dedicated `REACH-*` row, though the underlying identity resolution applies uniformly to all four sets | ⚠️ Warning | WR-38: a test-completeness gap on this round's own new safeguard, not a demonstrated bypass of the ban functionality itself |
| `agent-factory/workflows/18-context-compaction.md` | The two-route ledger paragraph (rewritten this round) states a universal "both routes derive it at their entry, above every branch" property | True of the closed CR-22 coordinate; false of `admitAndAppend`'s ungoverned-store case (CR-26) and silent about the fall-through's dial-routing change (CR-27) | ⚠️ Warning | WR-39/WR-40: claim-outruns-mechanism, the class this phase exists to close, recurring in the document rewritten to close it — not independently re-read line-by-line this session |

No unreferenced `TBD`/`FIXME`/`XXX` debt markers were found in any source file changed since round 7's
verification (`git diff --name-only d484b9e..HEAD` restricted to `scripts hooks agent-factory install
docs .planning .github vitest.config.ts package.json`, re-checked in this session).

### Human Verification Required

3 items, all pre-existing `UNKNOWN - verify` / manual-only items already recorded in `31-VALIDATION.md`
and not attempted by this round — none newly discovered by this verification, carried forward
unchanged through rounds 2-8 (see frontmatter `human_verification`): the attended Chrome lane's real
interactive behavior, the `claude auth status --json` predicate under API-key/long-lived-token auth,
and the Windows leg of every browser probe and of this round's findings.

### Gaps Summary

One 🛑 Blocker requirement failure (UATX-01), driven by TWO independently-reproduced NEW Critical
findings (CR-26, CR-27) — both defects the round-7 fix itself created at coordinates adjacent to the
one it correctly closed (CR-22) — in the identical write-path predicate family this phase has now paid
for across EIGHT straight verification rounds. Against that, this round delivers the first genuine,
independently-confirmed closure of UATX-06's predicate family (the AST modifier ban): no live bypass
remains, and the two residuals that do remain (WR-37, WR-38) are disclosed, non-blocking, and in the
same "unreproducible edge case" / "test-completeness gap on a new safeguard" classes this phase has
already accepted for structurally identical prior findings. The full excluded-e2e suite is
independently confirmed GREEN in this session (66 files, 4208 passed, 2 skipped, exit 0), matching
round 7's own closing measurement exactly — the regression that suite carried at round 7's base is
gone and stays gone.

**This looks like genuine, if partial, progress rather than an intentional deviation** — no override is
suggested here. UATX-01's own gap is structural: closing CR-22 required moving a derivation to the
entry of two functions, and that move touched a fall-through and a sibling branch that this round's
own fix did not give matching dispositions. The `missing:` list above states what would close it: make
the two routes' dispositions for an ungoverned/unresolvable destination agree, and separate the
governance-DIAL question from the ledger-RECORD question at the fall-through rather than routing both
through the caller-supplied destination.

---

_Verified: 2026-09-11T22:40:00Z_
_Verifier: Claude (gsd-verifier)_

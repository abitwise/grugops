---
phase: 31-autonomous-manual-testing
verified: 2026-09-13T07:15:00Z
status: gaps_found
round: 9 (gap-closure round 9 verification — the NINTH verification pass of this phase, verifying
  gap-closure round 8's fix plans 31-39..31-44 against the code review at 31-REVIEW.md and the
  closing measurement at docs/audit/31-round8-residuals.md). THE DEVELOPER FENCED THIS PHASE ON
  2026-09-12 — there is no round 10 regardless of this score. The gaps below are written as the
  accepted-open residual list, not as a queue entry.
score: 4/6 must-haves verified
covered_files:
  - ".github/workflows/ci.yml"
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/WINDOWS.md"
  - ".planning/phases/31-autonomous-manual-testing/31-01-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-01-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-02-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-02-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-03-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-03-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-04-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-04-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-05-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-05-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-06-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-06-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-07-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-07-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-08-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-08-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-09-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-09-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-10-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-10-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-11-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-11-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-12-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-12-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-13-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-13-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-14-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-14-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-15-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-15-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-16-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-16-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-17-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-17-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-18-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-18-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-19-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-19-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-20-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-20-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-21-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-21-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-22-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-22-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-23-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-23-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-24-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-24-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-25-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-25-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-26-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-26-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-27-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-27-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-28-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-28-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-29-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-29-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-30-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-30-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-31-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-31-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-32-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-32-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-33-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-33-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-34-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-34-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-35-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-35-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-36-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-36-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-37-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-37-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-38-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-38-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-39-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-39-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-40-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-40-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-41-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-41-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-42-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-42-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-43-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-43-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-44-PLAN.md"
  - ".planning/phases/31-autonomous-manual-testing/31-44-SUMMARY.md"
  - ".planning/phases/31-autonomous-manual-testing/31-CONTEXT.md"
  - ".planning/phases/31-autonomous-manual-testing/31-DISCUSSION-LOG.md"
  - ".planning/phases/31-autonomous-manual-testing/31-PATTERNS.md"
  - ".planning/phases/31-autonomous-manual-testing/31-RESEARCH.md"
  - ".planning/phases/31-autonomous-manual-testing/31-REVIEW.md"
  - ".planning/phases/31-autonomous-manual-testing/31-VALIDATION.md"
  - ".planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md"
  - ".planning/phases/31-autonomous-manual-testing/31-round6-residual-dispositions.md"
  - ".planning/phases/31-autonomous-manual-testing/deferred-items.md"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-35-task1-red.json"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-36-task1-red.json"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-36-task2-red.json"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-39-task2-red.json"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-40-task1-red.json"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-41-task1-red.json"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-42-task1-red.json"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-42-task2-red.json"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-43-task1-red.json"
  - ".planning/phases/31-autonomous-manual-testing/red-evidence/31-43-task2-red.json"
  - "agent-factory/checklists/00-index.md"
  - "agent-factory/checklists/browser-uat-recipe.md"
  - "agent-factory/contracts/context-note.md"
  - "agent-factory/workflows/05-pr-quality-gate.md"
  - "agent-factory/workflows/06-uat-pack.md"
  - "agent-factory/workflows/16-context-read-write.md"
  - "agent-factory/workflows/17-task-claim.md"
  - "agent-factory/workflows/18-context-compaction.md"
  - "docs/audit/29-style-dispositions/31-09.md"
  - "docs/audit/29-style-dispositions/31-14.md"
  - "docs/audit/29-style-dispositions/31-16.md"
  - "docs/audit/29-style-dispositions/31-17.md"
  - "docs/audit/29-style-dispositions/31-18.md"
  - "docs/audit/29-style-dispositions/31-19.md"
  - "docs/audit/29-style-dispositions/31-21.md"
  - "docs/audit/29-style-dispositions/31-22.md"
  - "docs/audit/29-style-dispositions/31-23.md"
  - "docs/audit/29-style-dispositions/31-27.md"
  - "docs/audit/29-style-dispositions/31-29.md"
  - "docs/audit/29-style-dispositions/31-30.md"
  - "docs/audit/29-style-dispositions/31-32.md"
  - "docs/audit/29-style-dispositions/31-33.md"
  - "docs/audit/29-style-dispositions/31-34.md"
  - "docs/audit/29-style-dispositions/31-35.md"
  - "docs/audit/29-style-dispositions/31-37.md"
  - "docs/audit/29-style-dispositions/31-41.md"
  - "docs/audit/29-style-dispositions/31-42.md"
  - "docs/audit/31-round4-residuals.md"
  - "docs/audit/31-round5-residuals.md"
  - "docs/audit/31-round6-residuals.md"
  - "docs/audit/31-round7-residuals.md"
  - "docs/audit/31-round8-residuals.md"
  - "docs/audit/harness-false-result-instances.md"
  - "hooks/admission-guard.js"
  - "hooks/admission-guard.test.ts"
  - "hooks/admission-guard.ts"
  - "hooks/guard.js"
  - "hooks/guard.test.ts"
  - "hooks/guard.ts"
  - "hooks/hook-entry.js"
  - "hooks/hook-entry.ts"
  - "hooks/hooks.json"
  - "install/README.md"
  - "install/install.js"
  - "install/install.test.ts"
  - "install/install.ts"
  - "install/uninstall.js"
  - "install/uninstall.ts"
  - "package.json"
  - "scripts/check-banned-claims.js"
  - "scripts/check-banned-claims.test.ts"
  - "scripts/check-banned-claims.ts"
  - "scripts/check-foundation-guards.js"
  - "scripts/check-foundation-guards.test.ts"
  - "scripts/check-foundation-guards.ts"
  - "scripts/check-platform-shapes.js"
  - "scripts/check-platform-shapes.test.ts"
  - "scripts/check-platform-shapes.ts"
  - "scripts/check-uat-oracles.js"
  - "scripts/check-uat-oracles.ts"
  - "scripts/checkpoints.js"
  - "scripts/checkpoints.ts"
  - "scripts/chrome-lane-bar.test.ts"
  - "scripts/compactor.js"
  - "scripts/compactor.test.ts"
  - "scripts/compactor.ts"
  - "scripts/context-io-writer-set.test.ts"
  - "scripts/context-io.js"
  - "scripts/context-io.test.ts"
  - "scripts/context-io.ts"
  - "scripts/floor-invariance.test.ts"
  - "scripts/harness-instance-ledger.test.ts"
  - "scripts/nonblocking-reader-parity.test.ts"
  - "scripts/runnable-ref/fixtures/caught-assertion.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/clean.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/conditional-assertion.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/configured-soft.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/element-access-modifier.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/foreign-describe.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/foreign-framework.d.ts"
  - "scripts/runnable-ref/fixtures/foreign-soft-assert.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/import-rename.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/local-helper-head.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-call-link.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-call.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-family.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/modifier-group-clean.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/playwright-test.d.ts"
  - "scripts/runnable-ref/fixtures/reach-modifier-tails.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/shadowed-rename.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/testinfo-fixture-param.uat.spec.ts"
  - "scripts/runnable-ref/fixtures/union-all-arms.uat.spec.ts"
  - "scripts/runnable-ref/uat-spec-integrity.js"
  - "scripts/runnable-ref/uat-spec-integrity.test.ts"
  - "scripts/runnable-ref/uat-spec-integrity.ts"
  - "scripts/uat-gate-exit-contract.test.ts"
  - "tsconfig.fixtures.json"
  - "tsconfig.tests.json"
  - "vitest.config.ts"
covered_digest: "v1:sha256:9c2c715f53deffbbd95ca4066023fbac9354dfec106ac424bd39d55bf3940ff1"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/6
  gaps_closed:
    - "CR-26 (admitAndAppend's gated branch fell open to the caller's repoRoot for an ungoverned contextRoot) — CLOSED. Verified by reading scripts/context-io.ts and independently reproduced against the committed .js in this session (below): the identical call now refuses with the same UNNAMEABLE_OWNER_CLAUSE the sibling promoteAdmitted raises."
    - "CR-27 (promoteAdmitted's fall-through routed the governance-DIAL question through the caller-supplied destination rather than the trusted root) — CLOSED. Independently reproduced in this session: admit()'s ledgerOwner parameter is now distinct from its dial root (verified by reading scripts/context-io.ts's admit() signature and promoteAdmitted's call site), and the D-14 fail-closed refusal at the caller's own unreadable trusted config now fires regardless of the destination named."
  gaps_remaining:
    - "UATX-01 — CR-26/CR-27's own predicate family (write-path one-repository-per-action / governance-dial-vs-ledger-record) produced a NEW Critical, CR-31, in this same round's own edits to the sibling gated branch of admitAndAppend: under an unreadable trusted governance configuration, that branch never calls admit() (documented as deliberate), so admit()'s own D-14 refusal ('an unreadable config is not a lean config') never fires there, audit_retention is read off the unreadable config's lean DEFAULT ('git', not 'retained'), and the note is PERSISTED WITH NO AUDIT-LEDGER RECORD AND NO REFUSAL. Independently reproduced in THIS session against the committed scripts/context-io.js (below) — this is the NINTH round in this phase's history in which the fix that closed the prior round's Critical created a new bypass one register over, in the identical predicate family."
    - "UATX-06 — the identity cutover's foreign-declared spelling-rule arm (D-35, verified genuinely closing CR-23/CR-25 last round) is defeated whole-cloth by an import RENAME from any declared-foreign module: deriveImportRenames only ever builds its rename map for the @playwright/test module specifier, so a renamed foreign import (import { expect as soften } from \"other-assert\"; import { describe as grouping } from \"other-framework\"; import * as other from \"other-framework\") reaches the membership authority under its ORIGINAL spelling, which is never a published ban member. Independently reproduced in THIS session, three separate constructs (modifier-ban arm (c), the namespace variant, and the caught/conditional-assertion arms (a)/(b) — a DIFFERENT consumer of the same broken canonicalisation with its own membership set), each against exit-0 committed-.js behaviour with a passing CONTROL bounding it the other way."
  regressions:
    - "UATX-06 regresses from VERIFIED (round 8, first time) to FAILED this round. This is not a new defect introduced by this round's OWN fix plans (31-39..31-44 touch context-io.ts/hook-entry.ts/check-platform-shapes.ts/uat-spec-integrity.ts's swallow-site census, not deriveImportRenames) — it is a live bypass that predates this round and was not found by round 8's verifier, surfaced now by round 9's own code review reproducing a paraphrase of a round-8 CONTROL row. Recorded as a regression in the VERIFIED SCORE, not as a regression in the code."
gaps:
  - truth: "UATX-01 — a committed Playwright spec, re-run by the §14 gate, is the machine-verifiable evidence floor; the write path keys a note and its own audit record on one governance-derived repository, and an unreadable governance configuration is refused rather than silently defaulted, on every route and every branch."
    status: failed
    reason: >
      CR-26 and CR-27 (round 8's write-path Criticals) are genuinely closed — independently
      reproduced against the committed scripts/context-io.js in this session, both now refusing or
      correctly separating the dial-vs-ledger question exactly as their closing plans (31-39, 31-41)
      describe. But this round's own edits to the sibling gated branch of admitAndAppend (31-39/D-39,
      re-touched by 31-41/D-41) left CR-31 live: that branch is documented to deliberately skip
      admit() (the only place D-14's unreadable-configuration refusal lives on this route), so when
      the trusted root's governance configuration exists and cannot be parsed, `isGatedNote` fails
      closed (demanding a human:NAME stamp, correctly) but `configResult.config.audit_retention`
      reads the LEAN DEFAULT of a config that could not be read ("git", not "retained"), the GOV-02
      retention guard is therefore never entered, and the note is PERSISTED with no audit-ledger
      record and no refusal at all.

      Independently reproduced in THIS session against the committed scripts/context-io.js, in a
      scratch mkdtemp root outside the repository tree: a TRUSTED root with `.grugops/factory.config.json`
      = the literal `{ this is not json` (the shape D-14 must fail-closed on), calling
      `admitAndAppend("T-CR31", {by:"security-nfr", kind:"finding", verified_by:"human:alice", refs:[],
      at:<iso>}, "a body", contextRoot, trusted)` — with `readGovernanceConfig(trusted)` asserted
      `source: "unreadable"` / `audit_retention: "git"` FIRST — returned an id
      (`20260913T033433Z-security-nfr-finding-abb9caa6`), the note directory was created on disk
      (`notes dir exists: true`), and the audit ledger file was ABSENT (`ledger exists: false`). No
      refusal was raised anywhere in the call. This is the identical asymmetry standard CR-26/CR-27
      were filed under, one register over, on the one write-both route that never asks admit() the
      D-14 question.

      This is the NINTH consecutive round in this phase's gap-closure history (rounds 2 through 8,
      and now this round's own fix) in which closing a Critical at its filed coordinate created a new
      bypass one register past it, in the identical predicate family: the write-path
      one-repository-per-action / governance-configuration-fail-closed rule.
    artifacts:
      - path: "scripts/context-io.ts"
        issue: "admitAndAppend's gated branch (readGovernanceConfig call ~line 5881, audit_retention guard ~line 5936): the branch deliberately never calls admit(), so admit()'s own D-14 unreadable-configuration refusal never applies here; audit_retention is read off GOVERNANCE_DEFAULTS (\"git\") for an unreadable config, so the retention guard is skipped and the note persists with no ledger event and no refusal. Independently reproduced against the committed .js."
      - path: "scripts/context-io.ts"
        issue: "readGovernanceConfig (~line 5636): for source === \"unreadable\" the returned config carries GOVERNANCE_DEFAULTS.audit_retention (\"git\") rather than a value that signals \"unknown, and an unknown dial is not a lean dial\" — the same clause admit() states about human_admission but does not apply to audit_retention on this one route."
    missing:
      - "Ask the retention question of the CONFIG SOURCE, not of the defaulted value, at admitAndAppend's gated branch, before the retention guard: refuse with a D-14-shaped message (\"admission REFUSED (UNKNOWN - verify): a governance configuration file exists at a standard location but could not be read or parsed, so the audit_retention dial is UNKNOWN. No note was written and no audit-ledger event was appended.\") when configResult.source === \"unreadable\", exactly as 31-REVIEW.md's CR-31 fix proposes — OR make readGovernanceConfig return audit_retention: \"retained\" (gate-or-stricter) for source === \"unreadable\", as a dated, human-recorded dial decision rather than a patch."
      - "Publish the accepted disposition as a WRITE_PATH_RESIDUALS or equivalent register member with this exact reproduction, and add a derived test that drives admitAndAppend's gated branch under an unreadable config and asserts the SAME disposition (refuse, no write) that appendNote/admit() take for the identical input, so a future edit to this branch cannot reopen the class silently a tenth time."
  - truth: "UATX-06 — conditional or caught assertions, and the D-14 arm-(c) modifier-call bans, are rejected over the TypeScript AST for every declared framework and every declared-foreign module, so the recipe's completeness claim matches exactly what the checker decides."
    status: failed
    reason: >
      Round 8's closures of CR-23 and CR-25 (the foreign-declared provenance split and the bounded
      walk's truncation signal) hold — independently confirmed by reading resolveBannedModifier and
      frameworkSurface directly, and the round-8 closing measurement (docs/audit/31-round8-residuals.md
      §3) re-drove both as UNMOVED against this commit. But `deriveImportRenames` — the function that
      makes canonicaliseHeadSegment rewrite a renamed head back to its framework spelling — still
      contains `if (node.moduleSpecifier.text !== PLAYWRIGHT_TEST_MODULE) return;`, so it builds a
      rename map ONLY for imports from `@playwright/test`. Any import RENAMED from a declared-foreign
      module is therefore asked of the membership authority under its ORIGINAL, un-rewritten spelling
      — which is never a published ban member — and passes at exit 0 with `tsc` also at exit 0.

      Independently reproduced in THIS session against the committed
      scripts/runnable-ref/uat-spec-integrity.js, in an equipped probe root outside the repository
      tree (tsconfig.json, node_modules symlink, both ambient .d.ts surfaces copied to types/),
      THREE separate constructs, each with a passing CONTROL bounding it the other way:

      1. `import { expect as soften } from "other-assert"; soften.soft(1).toBe(1);` — arm (c), the
         modifier-call ban. `tsc EXIT=0`, checker `0 findings, EXIT=0`. CONTROL (unrenamed `expect.soft`)
         — `1 finding(s) … expect.soft, EXIT=1`.
      2. `import * as other from "other-framework"; other.describe.skip("a scenario", () => {});` — the
         namespace-import variant. `tsc EXIT=0`, checker `0 findings, EXIT=0`.
      3. `import { expect as soften } from "other-assert"; try { soften(1).toBe(1); } catch {}` — arms
         (a)/(b), the CAUGHT-ASSERTION ban, a DIFFERENT consumer of the same canonicalisation with its
         own membership set (ASSERTION_HEADS). `tsc EXIT=0`, checker `0 findings, EXIT=0`. CONTROL
         (unrenamed `expect` inside the same try block) — `1 finding(s) … caught assertion …, EXIT=1`.

      This reverses round 8's UATX-06 VERIFIED (first time) status: the mechanism no longer matches
      the recipe's published completeness claim for the declared-foreign case the D-35 provenance
      split exists to cover. It is the NINTH occurrence of this phase's recorded pattern — the module
      filter was correct while `foreign` was a terminal, non-consuming classification, and became
      load-bearing (and wrong) the moment D-35 made `foreign-declared` ask the spelling rule.
    artifacts:
      - path: "scripts/runnable-ref/uat-spec-integrity.ts"
        issue: "deriveImportRenames (~line 2533): `if (node.moduleSpecifier.text !== PLAYWRIGHT_TEST_MODULE) return;` scopes the rename map to one module specifier, so a rename from any declared-foreign module is invisible to canonicaliseHeadSegment. Independently reproduced against the committed .js for both the named-import and namespace-import shapes."
      - path: "scripts/runnable-ref/uat-spec-integrity.ts"
        issue: "canonicalAssertionHead / arms (a)/(b) (~lines 2976-3107): consume the SAME dottedPath the modifier-ban arm (c) consumes, so the identical rename defeats the caught/conditional-assertion ban as well as the modifier-call ban — a second, undocumented blast radius for the same root cause. Independently reproduced."
      - path: "scripts/runnable-ref/fixtures/*.uat.spec.ts"
        issue: "No corpus row drives a renamed import from a declared-foreign module (grep -n 'as .* } from \"other-' over the fixtures directory returns nothing); the WR-38 reachability rows added in round 8 drive each published ban member through a declared-foreign module by its OWN name, which is exactly the spelling the rename evades."
    missing:
      - "Widen deriveImportRenames's module filter to cover every import declaration, not only @playwright/test — both the isNamespaceImport and isNamedImports arms — or move head resolution onto the declaration the identity resolver already found for foreign-declared symbols, rather than through a module-specifier-keyed rename table."
      - "State the blast radius on the fix's own record: the ban families sharing dottedPath (arm (c) AND arms (a)/(b)) both need the corrected canonicalisation, and a fix scoped to the ban operand alone (IDENTITY_BAN_OPERAND) would close the modifier-call construct and silently leave the caught/conditional-assertion construct open."
      - "Add one corpus row per construct (renamed named import, renamed namespace import) through a declared-foreign module, for both ban families, so the two spellings (renamed vs. un-renamed, framework vs. declared-foreign) cannot diverge again without a red test."
deferred: []
advisory: []
behavior_unverified_items: []
human_verification:
  - test: "The attended Claude-in-Chrome lane opens under real interactive auth, pauses for a human on a login/challenge page, and produces only a human-stamped finding + artifact-ref (never a gate stamp)."
    expected: "The lane behaves as documented in agent-factory/checklists/browser-uat-recipe.md's attended lane; no route to a §14-gate stamp is exercised in practice."
    why_human: "Requires an attended Claude Code session with the Claude-in-Chrome browser extension installed and a real interactive login; not reachable in CI and not reachable on this box. Carried forward unchanged through rounds 2-9 (R-01)."
  - test: "The claude auth status --json fail-closed predicate (D-10) behaves correctly under an API-key-only box and under a long-lived setup token."
    expected: "Both configurations are a loud skip naming the failing clause, never a silent open."
    why_human: "Research assumptions A2/A3 are UNKNOWN - verify; neither configuration is reachable without destroying this box's real credentials. Carried forward unchanged (R-02)."
  - test: "Both browser-absence probe stages, and the whole spec-integrity runnable, on a Windows host."
    expected: "Exit 2 with the browser-absent marker when browsers are missing; parser-absent marker when typescript cannot be resolved; every darwin-only construct in this report (the rename bypass, the retention fail-open, the FIFO/depth-adjacency classes) established on darwin/Node v24.12.0 only."
    why_human: "UNKNOWN - verify per the standing Windows posture (WINDOWS.md); not testable on darwin. A real windows-latest reading has not been taken. Carried forward, unchanged (R-03)."
---

# Phase 31: Autonomous Manual Testing Verification Report

**Phase Goal:** An agent can drive a real browser to produce UAT evidence, and the only thing that
counts as evidence is an artifact the §14 gate re-runs — never the agent's narration of what it saw.
**Verified:** 2026-09-13T07:15:00Z
**Status:** gaps_found
**Re-verification:** Yes — this is the NINTH verification pass on this phase. **The developer decided
on 2026-09-12 that Phase 31 is FENCED at this round: there is no round 10 regardless of this score.**
This report's `gaps:` are therefore written to double as the accepted-open residual list — each names
finding IDs, a file:line, the reaching input shape, and a closing criterion — rather than as a demand
for further rounds.

## Round History

| Round | Status | Score | What moved |
|---|---|---|---|
| 1 | gaps_found | — | Three planted constructs found and fixed by 31-06 |
| 2 | gaps_found | 4/6 | CR-05 (fabricated §14-gate stamp) and CR-06 (two `test.describe.*.only` spellings) |
| 3 | gaps_found | 5/6 | CR-05/CR-06 closed; CR-07/WR-14 (call-link + import-rename evasions) and CR-08 (compaction refuses a human-disposed finding) found |
| 4 | gaps_found | 4/6 | CR-07/WR-14/CR-08/WR-15 closed; CR-09 x2/CR-10/CR-11/WR-19/WR-20/WR-21 found |
| 5 | gaps_found | 4/6 | CR-09 x2/CR-10/CR-11/WR-19/WR-20/WR-21 closed; CR-12/CR-13/CR-14/CR-15/CR-16 found |
| 6 | gaps_found | 4/6 | CR-12/CR-13/CR-14/CR-15/CR-16 closed; CR-17/CR-18/CR-19/CR-20/CR-21 found |
| 7 | gaps_found | 4/6 | CR-17/CR-18/CR-19/CR-20/CR-21 all closed as filed; CR-22/CR-23/CR-24/CR-25 found (plus a self-referential coverage-test regression) |
| 8 | gaps_found | 5/6 | CR-22/CR-23/CR-24/CR-25 and the regression all CLOSED, independently re-confirmed. UATX-06 VERIFIED for the first time. CR-26/CR-27 found in the same round's own fix (write-path fail-open + dial/ledger collapse) |
| **9 (this round, FENCED — no round 10)** | **gaps_found** | **4/6** | CR-26/CR-27 CLOSED, independently reproduced against the committed `.js` in this session. But TWO predicate families broke: CR-31 (NEW — admitAndAppend's gated branch skips admit()'s D-14 refusal entirely on an unreadable config, silently drops the audit-ledger record) reopens UATX-01; and CR-28/CR-29/CR-30 (a renamed import from ANY declared-foreign module evades both the modifier-call ban and the caught/conditional-assertion ban, independently reproduced) reverses UATX-06 from its round-8 first-time VERIFIED back to FAILED. This is the NINTH consecutive round in which the fix that closed the prior round's Critical(s) created a new bypass one register over, in the identical write-path/identity-cutover predicate families. |

The score moves from 5/6 to 4/6 this round. CR-26/CR-27's closures are genuine and independently
confirmed. The regression is not in code this round's own plans (31-39..31-44) newly broke on
purpose — CR-31 is a new defect in the SAME edit that closed CR-26/CR-27, and CR-28/29/30 are a
pre-existing bypass this round's own code review found by reproducing a paraphrase of a round-8
CONTROL row, not a regression introduced by 31-39..31-44's stated changes to a different function.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | UATX-01 — a committed Playwright spec, re-run by the §14 gate, is the machine-verifiable evidence floor; the write path keys a note and its own audit record on one governance-derived repository, and an unreadable governance configuration is refused, on every route and branch | ✗ FAILED — see gap 1 (CR-31) | CR-26/CR-27 genuinely closed, independently reproduced against the committed `scripts/context-io.js` in this session (below). But `admitAndAppend`'s gated branch — the ONE write-both route that never calls `admit()` — reads `audit_retention` off the unreadable config's lean DEFAULT (`"git"`) rather than refusing, so under a trusted root whose governance config cannot be parsed the note is PERSISTED with no ledger record and no refusal. Independently reproduced in this session. |
| 2 | UATX-02 — browser MCP tooling documented and pinned for all five host CLIs; `package.json` gains nothing; the pin authority cannot itself be defeated by a floating specifier | ✓ VERIFIED | Unchanged this round; `git diff --stat 54ea410..HEAD -- package.json package-lock.json` empty; `node scripts/check-foundation-guards.js` → `ALL CHECKS PASSED` in this session. Not contested by `31-REVIEW.md`. |
| 3 | UATX-03 — Claude in Chrome is attended-only and structurally barred from producing a `§14-gate` stamp | ✓ VERIFIED | `scripts/chrome-lane-bar.test.ts` byte-untouched over the round (`git diff --stat 54ea410..HEAD` does not name it). Not contested. |
| 4 | UATX-04 — evidence carries commit SHA + gate-run id + content hash; a note whose SHA is not the HEAD the gate ran against is refused | ✓ VERIFIED | `admit()`'s frozen span is untouched this round in the SHA-provenance sense — `git hash-object hooks/guard.ts` = `669725bc1c616ab57123e22090d93d57eff1b001`, equal to `FROZEN_GUARD_BLOB` (`scripts/floor-invariance.test.ts:245`), independently re-verified in this session. None of CR-28/29/30/31 or the five Warnings name this surface (CR-31 is about the audit-ledger RECORD, not the SHA-provenance evidence floor). |
| 5 | UATX-05 — an absent/unusable browser produces a loud skip leaving the UAT `pending`, never a silent pass | ✓ VERIFIED | Unchanged; `node scripts/check-uat-oracles.js` → `ALL CHECKS PASSED` in this session. No round-9 finding touches the browser-absence probe. |
| 6 | UATX-06 — conditional or caught assertions, and the D-14 arm-(c) modifier-call bans, are rejected over the TypeScript AST for every declared framework AND every declared-foreign module, matching the recipe's completeness claim | ✗ FAILED — see gap 2 (CR-28/29/30) | `deriveImportRenames` still scopes its rename map to `@playwright/test` only (`if (node.moduleSpecifier.text !== PLAYWRIGHT_TEST_MODULE) return;`), so a RENAMED import from any declared-foreign module reaches the membership authority under its unrewritten spelling. Independently reproduced in this session for three constructs: the modifier-call ban (`soften.soft`), the namespace-import variant (`other.describe.skip`), and the caught-assertion ban (`try { soften(1).toBe(1) } catch {}`) — each `tsc EXIT=0`, checker `0 findings, EXIT=0`, with a passing CONTROL (the un-renamed spelling) bounding each the other way. Reverses round 8's first-time VERIFIED. |

**Score:** 4/6 truths verified (UATX-02, UATX-03, UATX-04, UATX-05). UATX-01 and UATX-06 both fail —
the ninth consecutive round in which this phase's core predicate families (write-path
one-repository-per-action, and the identity-cutover's foreign-declared spelling rule) close the
exact coordinate the prior round measured and a new bypass reappears one register over.

### Deferred Items

None. No gap identified this round is addressed by a later phase in the roadmap; both remaining gaps
are Phase 31's own predicate families, and Phase 31 is fenced at this round.

### Advisory (New Scope, Unevidenced)

None — every finding in this report (CR-28, CR-29, CR-30, CR-31) is independently reproduced against
the committed `.js` in THIS verification session (commands and outputs recorded below), not merely
carried from `31-REVIEW.md`'s own narration.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/context-io.ts` — `admitAndAppend`'s gated branch | Refuses (writes nothing) when the trusted governance configuration exists but cannot be read/parsed, exactly as `admit()`'s D-14 clause does on the sibling non-gated route | ✗ FAILED | Independently reproduced (CR-31): the branch writes the note with no ledger record and no refusal under an unreadable trusted config. `admit()` is never consulted on this route (documented as deliberate), so its D-14 refusal cannot fire here. |
| `scripts/context-io.ts` — `admitAndAppend` / `promoteAdmitted`'s CR-26/CR-27 write-path coordinates | Every return path derives one governance-owning root before any write, and the governance-dial read is separated from the ledger-record write | ✓ VERIFIED | Independently reproduced this session: the CR-26 call now refuses with the same `UNNAMEABLE_OWNER_CLAUSE` the sibling raises; `admit()`'s signature now carries a `ledgerOwner` distinct from its dial root (read from source), closing CR-27's collapse. |
| `scripts/runnable-ref/uat-spec-integrity.ts` — `deriveImportRenames` | Builds a rename map for an import declaration from ANY module, not only `@playwright/test`, so a renamed declared-foreign head reaches the membership authority under its framework-equivalent spelling | ✗ FAILED | Independently reproduced (CR-28/CR-29): the module filter (`!== PLAYWRIGHT_TEST_MODULE`) is unchanged; a renamed named import and a renamed namespace import from a declared-foreign module both evade the ban. |
| `scripts/runnable-ref/uat-spec-integrity.ts` — arms (a)/(b), the caught/conditional-assertion ban | Consumes a canonicalised head that a foreign-module rename cannot defeat | ✗ FAILED | Independently reproduced (CR-30): the SAME broken `deriveImportRenames` output feeds `canonicalAssertionHead`, so a renamed foreign assertion import defeats the caught-assertion arm too — a second, undocumented consumer of the same root cause. |
| `scripts/runnable-ref/uat-spec-integrity.ts` — `resolveBannedModifier` / `frameworkSurface` (CR-23/CR-25 coordinates) | The foreign-declared provenance split still asks the spelling rule; the bounded surface walk still signals truncation rather than silently accepting | ✓ VERIFIED | Read from source this round, unchanged from round 8's independently-confirmed closure; not contested by any of this round's four Criticals or five Warnings. |
| `agent-factory/checklists/browser-uat-recipe.md` | Ban set, completeness claim, and boundary list match the exported constants' actual, proven strength | ⚠️ PARTIAL | The published reachability claim (WR-38, round 8) holds for the members it binds, but the recipe's completeness claim ("conditional or caught assertions … are rejected") is now measurably false for a renamed declared-foreign import — not itself edited or contested this round, so not independently re-read line-by-line, but its claim outruns the mechanism CR-28/29/30 expose. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `admitAndAppend`'s gated branch | `admit()`'s D-14 unreadable-configuration refusal | NOT wired — the gated branch never calls `admit()` | ✗ NOT_WIRED | Independently reproduced (CR-31): an unreadable trusted config writes the note with no refusal, where the sibling non-gated route (through `admit()`) would refuse. |
| `admitAndAppend`'s gated branch | `readGovernanceConfig`'s `source` discriminant | PARTIALLY wired — `isGatedNote` consults `source==="unreadable"` (fails closed) but the retention guard consults only the DEFAULTED `audit_retention` value, not `source` | ⚠️ PARTIAL | Independently reproduced: the human-stamp gate correctly fires (`gated=true`), but the retention guard silently no-ops on the same unreadable read. |
| `deriveImportRenames` | `canonicaliseHeadSegment` (arm (c), the modifier ban) | WIRED for `@playwright/test` imports only | ✗ NOT_WIRED for declared-foreign modules | Independently reproduced (CR-28/CR-29): a renamed or namespace-imported declared-foreign head is never entered into the rename map, so the canonicaliser returns it unchanged. |
| `deriveImportRenames` (via `canonicalAssertionHead`) | arms (a)/(b), the caught/conditional-assertion ban | WIRED for `@playwright/test` imports only | ✗ NOT_WIRED for declared-foreign modules | Independently reproduced (CR-30): the identical broken rename map defeats this second, independently-membershipped consumer. |
| `promoteAdmitted` / `admitAndAppend` (non-gated route) | `admit()`'s ledger-owner-vs-dial-root separation | WIRED — `admit()` now takes a `ledgerOwner` distinct from its dial `repoRoot` | ✓ WIRED | Independently reproduced this session: CR-27's collapse is undone by a signature change, confirmed by reading `admit()`'s parameter list in the committed `.ts`. |

### Data-Flow Trace (Level 4)

Not separately applicable beyond the Key Link table above: this phase's artifacts are deciders and
writers rather than rendered UI, so "does the value reach the render" collapses into "does the
decision reach the caller unchanged," which the Key Link and Behavioral Spot-Check tables cover.

### Behavioral Spot-Checks

All rows below were run in THIS verification session against the committed `.js` at HEAD `06e2121`
(`git status --porcelain -- scripts hooks agent-factory install docs .github vitest.config.ts
package.json .planning/REQUIREMENTS.md .planning/ROADMAP.md` empty before and after), in scratch
roots under the session scratchpad, outside the tracked tree, each deleted after use.

| # | Behavior | Command | Result | Status |
|---|----------|---------|--------|--------|
| 1 | **HEAD identity, this session** | `git log -1 --format='%H %ci %s'` | `06e2121a8dbecb267014c487e740fc565b7e267a 2026-09-13 06:31:39 +0300 docs(31): add gap-closure round-9 code review report` — the commit `31-REVIEW.md` itself | premise confirmed |
| 2 | **CR-28, this session:** a renamed import from a declared-foreign ASSERTION module, arm (c) (modifier ban) | equipped probe root (tsconfig.json, node_modules symlink, both ambient `.d.ts` surfaces); `import { expect as soften } from "other-assert"; soften.soft(1).toBe(1);` | `tsc --noEmit EXIT=0`; `UAT spec integrity: 0 findings over 1/1 uat specs checked`, checker `EXIT=0` | ✗ FAIL (confirms CR-28) |
| 3 | **CR-28 CONTROL, this session** | the identical file, un-renamed: `import { expect } from "other-assert"; expect.soft(1).toBe(1);` | `1 finding(s) … \`expect.soft\` …`, checker `EXIT=1` | ✓ PASS — the CONTROL refuses, proving the harness can observe a refusal |
| 4 | **CR-29, this session:** a renamed NAMESPACE import from a declared-foreign FRAMEWORK module | `import * as other from "other-framework"; other.describe.skip("a scenario", () => {});` | `tsc --noEmit EXIT=0`; `0 findings`, checker `EXIT=0` | ✗ FAIL (confirms CR-29 — a second, distinct construct from CR-28) |
| 5 | **CR-30, this session:** the same rename defeating the CAUGHT-ASSERTION arm, not the modifier ban | `import { expect as soften } from "other-assert"; try { soften(1).toBe(1); } catch {}` | `tsc --noEmit EXIT=0`; `0 findings`, checker `EXIT=0` | ✗ FAIL (confirms CR-30) |
| 6 | **CR-30 CONTROL, this session** | the identical file, un-renamed: `import { expect } from "other-assert"; try { expect(1).toBe(1); } catch {}` | `1 finding(s) … caught assertion — the \`expect\` call sits inside a try block …`, checker `EXIT=1` | ✓ PASS — the CONTROL refuses |
| 7 | **CR-31, this session:** `admitAndAppend`'s gated branch, unreadable trusted config | scratch `mkdtemp` root, `.git` + `.grugops/factory.config.json` = `{ this is not json`; `readGovernanceConfig(trusted)` asserted `source: "unreadable", audit_retention: "git"` first; then `admitAndAppend("T-CR31", {by:"security-nfr", kind:"finding", verified_by:"human:alice", refs:[], at:<iso>}, "a body", contextRoot, trusted)` | `readGovernanceConfig` premise confirmed; `admitAndAppend result: {"id":"20260913T033433Z-security-nfr-finding-abb9caa6","findings":[]}`; notes dir exists: `true` (`['notes']`); ledger exists: `false` | ✗ FAIL (confirms CR-31) — note persisted, no ledger record, no refusal |
| 8 | Foundation guards / UAT oracles / platform shapes, this session | `node scripts/check-foundation-guards.js`, `node scripts/check-uat-oracles.js`, `node scripts/check-platform-shapes.js` | All three `ALL CHECKS PASSED`; platform-shapes: `SKIPPED SHAPES (0)` | ✓ PASS |
| 9 | Committed `.js` freshness, this session | `npm run freshness` | `All build outputs fresh: 61 committed .js file(s) match a rebuild of their sources.` | ✓ PASS |
| 10 | Byte-frozen deploy guard, this session | `git hash-object hooks/guard.ts` | `669725bc1c616ab57123e22090d93d57eff1b001`, equal to `FROZEN_GUARD_BLOB` | ✓ PASS |
| 11 | `chrome-lane-bar.test.ts` / `package.json` untouched, this session | `git diff --stat 54ea410..HEAD -- scripts/chrome-lane-bar.test.ts package.json package-lock.json` | empty | ✓ PASS |
| 12 | Requirement/roadmap checkbox state, this session | `grep -n "Phase 31" .planning/ROADMAP.md`; `grep -n "UATX-0" .planning/REQUIREMENTS.md` | `- [ ] **Phase 31: Autonomous Manual Testing**`; all six `UATX-0N` unchecked, all six traceability rows `Gaps Found` | ✓ PASS — unmoved, correct pending this report |
| 13 | Tracked-tree residue, before/after all probes | `git status --porcelain -- scripts hooks agent-factory install docs .github vitest.config.ts package.json .planning/REQUIREMENTS.md .planning/ROADMAP.md` | empty both times | ✓ PASS — every probe ran under the session scratchpad, deleted after use |

**Reproduction scope note.** This session did not re-run the full regression suite: `docs/audit/31-round8-residuals.md`
§7 independently ran it in the same session that produced this round's fix commits (`Test Files 66
passed`, exit 0, before `31-REVIEW.md`'s own probing), and a second full run was reported in progress
in parallel to this verification per the task's own instructions. `31-REVIEW.md` itself measured
(§ "Why the suite is green over it") that no corpus row drives a renamed foreign import, so a full
suite re-run would not exercise CR-28/29/30/31 either way; the reproduction budget in this session was
spent driving the four new/confirmed Criticals directly against the committed `.js`, plus the
foundation-guard/oracle/freshness/frozen-hash floor checks above, rather than a fifth full-suite run
that the review's own measurement shows would read green over these exact defects.

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| UATX-01 | 31-01, 31-04, 31-05, 31-09, 31-10, 31-14, 31-18, 31-19, 31-21, 31-22, 31-23, 31-27, 31-29, 31-33, 31-36, 31-37, 31-38, 31-39, 31-40, 31-41, 31-43, 31-44 | Committed spec = evidence floor; write path keys a note and its audit record on one governance-derived repository, refusing an unreadable configuration on every route | ✗ BLOCKED | CR-26/CR-27 closed and independently reproduced; CR-31 (admitAndAppend's gated branch silently defaults `audit_retention` under an unreadable config, skipping D-14 entirely) independently reproduced this session |
| UATX-02 | 31-03, 31-06, 31-07 | Browser MCP tooling documented + pinned, 5 hosts; pin authority cannot itself float | ✓ SATISFIED | Unchanged, not contested; `package.json`/`package-lock.json` byte-unchanged over the round |
| UATX-03 | 31-03, 31-04, 31-15 | Attended Chrome lane structurally barred from gate stamp | ✓ SATISFIED | Unchanged, byte-untouched this round |
| UATX-04 | 31-01, 31-05, 31-14 | Evidence provenance (SHA/gate_run/content_hash); mismatched SHA refused | ✓ SATISFIED | Unchanged; frozen span re-verified equal this session; no round-9 finding touches the SHA-provenance surface |
| UATX-05 | 31-02, 31-04, 31-13, 31-17 | Loud skip on absent/unusable browser, never a silent pass | ✓ SATISFIED | Unchanged; `check-uat-oracles.js` re-run green this session |
| UATX-06 | 31-02, 31-04, 31-06, 31-11, 31-12, 31-13, 31-16, 31-17, 31-24, 31-25, 31-28, 31-32, 31-34, 31-35, 31-42, 31-44 | AST ban on conditional/caught assertions and modifier calls; claim matches mechanism, for every declared framework AND declared-foreign module | ✗ BLOCKED | CR-23/CR-25 (round 8) hold, unchanged. CR-28/CR-29 (renamed named/namespace import from a declared-foreign module evades the modifier ban) and CR-30 (the same rename defeats the caught/conditional-assertion ban) independently reproduced this session — reverses round 8's first-time SATISFIED |

No orphaned requirements: every requirement ID declared across the forty-four plans' `requirements:`
frontmatter (UATX-01 through UATX-06) is accounted for above, and `.planning/REQUIREMENTS.md` maps no
additional Phase 31 requirement ID beyond these six. `.planning/REQUIREMENTS.md` still lists all six as
unchecked (`- [ ]`) and the traceability table still reads "Gaps Found" for all six — correctly
unmoved; one of the round's five SUMMARIES (`31-43-SUMMARY.md`) asserts `requirements-completed:
[UATX-01, UATX-04]` in its own frontmatter, which the tree does not agree with (also recorded in
`docs/audit/31-round8-residuals.md` §6.4) — the requirements file and roadmap checkbox are the
authoritative record and remain unmoved, correctly, since this report also finds `gaps_found`.
`.planning/ROADMAP.md` line 100 still shows Phase 31 unchecked.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/context-io.ts` | `admitAndAppend`'s gated branch (~5881-5936) reads `audit_retention` off the unreadable config's DEFAULT rather than refusing | The branch deliberately skips `admit()`, the only place D-14's unreadable-config refusal lives, and inherited no equivalent refusal of its own | 🛑 Blocker | CR-31: a note can be persisted with no audit-ledger record and no refusal under a trusted governance configuration that cannot be parsed — independently reproduced |
| `scripts/runnable-ref/uat-spec-integrity.ts` | `deriveImportRenames` (~2533) filters to `@playwright/test` only | The module filter was correct while `foreign` was a terminal classification and became load-bearing (and wrong) the moment D-35 made `foreign-declared` ask the spelling rule | 🛑 Blocker | CR-28/CR-29: a renamed or namespace-imported head from any declared-foreign module evades the modifier-call ban entirely — independently reproduced, two constructs |
| `scripts/runnable-ref/uat-spec-integrity.ts` | `canonicalAssertionHead` / arms (a)/(b) (~2976-3107) consume the same broken rename output | A second, independently-membershipped consumer of the identical root cause, undocumented in the register that would otherwise scope the fix's blast radius | 🛑 Blocker | CR-30: the caught/conditional-assertion ban is defeated by the identical construct — independently reproduced |
| `scripts/runnable-ref/uat-spec-integrity.test.ts` | `deriveSurfaceSwallowSites`'s call closure (~10668-10695, WR-43) follows only bare-identifier calls to module-level function DECLARATIONS | A module-level helper written as `const helper = (…) => {…}` or called through a property is invisible to the closure; the addition direction of the census can silently miss a new swallow | ⚠️ Warning | WR-43: disclosed, not independently exploited this session; the review's own reproduction stands as the evidence |
| `scripts/runnable-ref/uat-spec-integrity.test.ts` | `classify` (~10714-10729, WR-44) drops any catch clause containing a `throw` anywhere, before considering the fall-through | A handler that re-raises conditionally and swallows on its ordinary path is censused as "not a swallow" and needs no register entry | ⚠️ Warning | WR-44: disclosed, not independently exploited this session |
| `scripts/check-platform-shapes.ts` | The row/record agreement block (~1292-1325, WR-45) prints a false failure when a CONTROL's contents-mismatch is the only failure beside it | `plantedContents`'s message carries no `verdict=` token, so the agreement block's own check misreads a real, adjacent failure as absent | ⚠️ Warning | WR-45: cannot turn a red run green; disclosed |
| `scripts/check-platform-shapes.ts` | `reportedOutcome` (~653-663, WR-46) swaps the token names for two outcomes: a THROWING child prints `nonzero-exit`, a silently-exiting child prints `crashed` | The published docstrings match the (swapped) code, so the gate's own label checks pass while a transcript reader is told the opposite of what happened | ⚠️ Warning | WR-46: disclosed, not independently exploited this session |
| `scripts/runnable-ref/uat-spec-integrity.ts` | `isDefaultLibraryDeclaration#1`'s published census entry (~1239-1242, WR-47) claims arm `"depth-bound"`, ATTRIBUTED rather than observed for this one position | The position's own drive asserts the throw changes NOTHING relative to the unobstructed control on this chain length | ⚠️ Warning | WR-47: disclosed, not independently exploited this session |

No unreferenced `TBD`/`FIXME`/`XXX` debt markers were found in any source file changed since round 8's
verification (`git diff --name-only f698bec..HEAD` restricted to `scripts hooks agent-factory install
docs .planning .github vitest.config.ts package.json`, re-checked in this session).

### Human Verification Required

3 items, all pre-existing `UNKNOWN - verify` / manual-only items already recorded in `31-VALIDATION.md`
and not attempted by this round — none newly discovered by this verification, carried forward
unchanged through rounds 2-9 (see frontmatter `human_verification`): the attended Chrome lane's real
interactive behavior, the `claude auth status --json` predicate under API-key/long-lived-token auth,
and the Windows leg of every browser probe and of this round's findings.

### Gaps Summary

Two 🛑 Blocker requirement failures (UATX-01, UATX-06), driven by FOUR independently-reproduced
Critical findings (CR-28, CR-29, CR-30, CR-31) in this phase's two longest-running predicate families
— the write-path one-repository-per-action / governance-fail-closed rule, and the identity cutover's
foreign-declared spelling rule. CR-26 and CR-27 (round 8's write-path Criticals) ARE genuinely closed,
independently confirmed by direct reproduction against the committed `.js` in this session — that
progress is real and holds. Against it: CR-31 is a NEW defect in the identical branch CR-26's own fix
touched, one register over, on the one write-both route that never asks `admit()` the D-14 question;
and CR-28/CR-29/CR-30 are a PRE-EXISTING bypass of the AST-ban mechanism (a renamed import from any
declared-foreign module evades both ban families) that round 8's verifier did not find and this
round's own code review surfaced by reproducing a paraphrase of one of round 8's own CONTROL rows —
reversing UATX-06's first-time-verified status from round 8.

**This looks like genuine, if partial and now reversed-in-part, progress rather than an intentional
deviation** — no override is suggested here. **The developer has fenced this phase at this round**:
there will be no round 10 regardless of this score, and the two `gaps:` entries above are written to
serve directly as the accepted-open residual list — each names the finding IDs (CR-28/29/30, CR-31),
the file:line coordinates, the exact reaching input shape with independently-reproduced commands and
outputs, and the closing criterion a future change (in this phase or a successor) would need to meet.
The full excluded-e2e regression suite was independently green in the prior session
(`docs/audit/31-round8-residuals.md` §7: 66 files, 4208+ passed, exit 0) and, per the review's own
measurement, would not have caught any of these four findings regardless — no corpus row drives a
renamed foreign import, and no test drives `admitAndAppend`'s gated branch under an unreadable config.

---

_Verified: 2026-09-13T07:15:00Z_
_Verifier: Claude (gsd-verifier)_

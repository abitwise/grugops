---
phase: 31-autonomous-manual-testing
plan: 37
type: execute
wave: 6
depends_on: ["31-36"]
gap_closure: true
files_modified:
  - hooks/hook-entry.ts
  - hooks/hook-entry.js
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/floor-invariance.test.ts
  - scripts/harness-instance-ledger.test.ts
  - agent-factory/workflows/16-context-read-write.md
  - docs/audit/31-round6-residuals.md
  - docs/audit/harness-false-result-instances.md
  - docs/audit/29-style-dispositions/31-37.md
  - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
autonomous: true
requirements: [UATX-01, UATX-02, UATX-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-36: `hooks/hook-entry.ts`'s `hostBuiltProjectRoot` accepts a candidate on three conditions (non-empty after a trim, absolute, an existing directory) while `scripts/context-io.ts`'s `hostDeliveredRoot` applies two MORE (a version-control marker under the candidate, and not the kit's own root). The wrapper's stated reason for the gap — `the shape checks below are the ones a file limited to node: builtins can make` — is FALSE: `existsSync` is already imported at `:42`, and both further conditions are `node:fs` plus `node:path` operations. The consequence is benign today only because the reader re-checks; nothing binds the two, so a later narrowing of the reader would go unnoticed."
    - "The pair is BOUND, not merely aligned: one shared candidate corpus is driven through both predicates and the wrapper's accept set is asserted a SUBSET of the reader's, in the same both-directions shape `scripts/nonblocking-reader-parity.test.ts` already provides for the reader pair. A tier that delivers a value the reader would reject is a gate lowering whatever else it fixes — the sentence `hostDeliveredRoot`'s own doc block already states about tiers 0 and 1."
    - "`TRUSTED_ROOT_TIERS[0]` says the tier-0 name is `a channel the agent cannot write`, and on the Claude Code hook path its VALUE is derived from `CLAUDE_PROJECT_DIR`, the tier-1 AMBIENT name, promoted by the wrapper. The sentence describes the NAME and a reader is left to infer a separate channel; the tier entry states the provenance so no inference is needed, and `agent-factory/workflows/16-context-read-write.md` — bound to the tier array in both directions — moves with it."
    - "WR-33: `docs/audit/31-round6-residuals.md` §6.1 states that `31-28` removed `resolveBinding`, `bindingRangeFor` and `listHoists` outright `so there is no hoisted arm to narrow`. Measured at HEAD: all three are live. The three symbols D-30 (5) actually deleted are `deriveTestInfoParameterNames`, `isFixtureBindingPosition` and `CALLEE_CHAIN_STEP_BOUND`. This is not a typo — §6.1 uses the claimed deletion as the REASON the round dispositioned the CR-18 `missing:` bullet as `the mechanism the bullet named is GONE`, so the disposition rests on a fact false of the tree and must be re-taken against the mechanism that exists at THIS plan's commit."
    - "WR-34: `docs/audit/harness-false-result-instances.md` row 13's catch column ends `the fixture now writes context: { human_admission: \"high-severity\", audit_retention: \"retained\" } before the shape is planted`. `scripts/check-platform-shapes.ts` does the opposite — the position was DROPPED, deliberately, with its own explanatory comment. The tally this round created so a later reader has one authoritative list records a remedy that was reverted, in the row describing this round's own harness fault."
    - "A ledger of harness faults that states a mechanism is present must be checkable: every row that CLAIMS a mechanism is greppable in the tree it names, asserted by a derived case whose own denominator is asserted non-zero before its conclusion. The ledger is the artifact the next round starts from; a row it cannot substantiate is the same class of defect it exists to record."
    - "A prior round's PLAN or SUMMARY is history and is never rewritten. `docs/audit/31-round6-residuals.md` and `docs/audit/harness-false-result-instances.md` are this round's own tracked AUDIT documents and are corrected in place with the correction stated; `31-REVIEW.md`, `31-VERIFICATION.md` and every `*-SUMMARY.md` are left untouched."
    - "UATX-01 through UATX-06 stay UNCHECKED in `.planning/REQUIREMENTS.md`, the Phase 31 checkbox stays unchecked in `.planning/ROADMAP.md`, and every traceability row still reads `Gaps Found`."
  artifacts:
    - hooks/hook-entry.ts
    - hooks/hook-entry.js
    - scripts/context-io.ts
    - scripts/context-io.js
    - scripts/harness-instance-ledger.test.ts
    - agent-factory/workflows/16-context-read-write.md
    - docs/audit/31-round6-residuals.md
    - docs/audit/harness-false-result-instances.md
    - docs/audit/29-style-dispositions/31-37.md
  key_links:
    - "`hostBuiltProjectRoot` <-> `hostDeliveredRoot`: one shared candidate corpus driven through both, with the wrapper's accept set asserted a subset of the reader's, so a later narrowing on either side turns red."
    - "`TRUSTED_ROOT_TIERS[0]` <-> `agent-factory/workflows/16-context-read-write.md`'s resolution-order section: the existing both-directions binding, carried through the provenance sentence."
    - "each row of `docs/audit/harness-false-result-instances.md` <-> the tree it names: a derived case asserting every claimed mechanism is greppable, with its own non-zero denominator asserted first."
    - "§6.1's disposition of the CR-18 `missing:` bullet <-> the mechanism live at this commit: a disposition re-taken against measurement rather than against a claimed deletion."
  prohibitions:
    - statement: "A gap-closure plan must never flip a requirement checkbox, a traceability row or the phase checkbox. Only a verification round may do that."
      status: unverified
      verification: flagged
    - statement: "A prior round's PLAN, SUMMARY, REVIEW or VERIFICATION is history and is never rewritten. Only this round's own tracked audit documents are corrected, and each correction says what it corrects."
      status: unverified
      verification: flagged
    - statement: "`FROZEN_HOOK_ENTRY_LOGIC_SHA` is RE-TAKEN over the normalised wrapper with its non-vacuity floor asserted first and its history block extended. A freeze is change control, never a reason to leave a defect."
      status: unverified
      verification: flagged
    - statement: "The wrapper may import only `node:` builtins. Any condition added there is composed from `node:fs` and `node:path`, never by importing a module constant."
      status: unverified
      verification: flagged
    - statement: "Never fake a passing gate, a test result, or a citation. A claim measured false is corrected by re-measuring, never by softening the sentence."
      status: unverified
      verification: flagged
    - statement: "`hooks/guard.ts` is not touched and `FROZEN_GUARD_BLOB` is never re-based; every `DECIDER_MANIFEST` entry is moved with its artifact and the manifest is re-measured WHOLE."
      status: unverified
      verification: flagged
    - statement: "Never merge a protected branch and never deploy to production without a named human confirmation."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-27-PLAN.md / D-29: the PreToolUse wrapper performs no unbounded operation before it can answer, and the delivered tier-0 root exists. This plan strengthens that tier's validation without touching its bound."
    - "31-30-PLAN.md / D-32: a residual that was MIS-SCOPED in the record is corrected rather than re-closed. This plan applies the same rule to two of round 6's own audit documents."
---

<objective>
Bind the two sides of the delivered-root channel, and correct two audit records that state facts false
of the tree.

- **WR-36 (UATX-01, UATX-02, UATX-03)** — the PreToolUse wrapper's validation of
  `CLAUDE_PROJECT_DIR` is strictly weaker than the reader's, the stated reason for the gap is untrue
  (`existsSync` is already imported), and nothing asserts the wrapper's accept set is a subset of the
  reader's. Separately, `TRUSTED_ROOT_TIERS[0]` describes the tier-0 NAME as a channel the agent cannot
  write while its VALUE on the Claude Code path is derived from the tier-1 ambient name.
- **WR-33** — `docs/audit/31-round6-residuals.md` §6.1 names three symbols as deleted that are live,
  and uses that claimed deletion as the reason it dispositioned a `missing:` bullet rather than closing
  it.
- **WR-34** — `docs/audit/harness-false-result-instances.md` row 13 records a fixture change that was
  reverted; the position was dropped instead.

The two documents are the artifacts a round-8 reader starts from. A register that cannot be
substantiated is the same class of defect it exists to record, so the fix includes a derived case that
asks the ledger's own premise: every row claiming a mechanism is greppable in the tree it names.

Purpose: the delivered root decides which repository's governance dial answers, and the ledger decides
what the next round believes it already knows.
Output: a bound wrapper/reader pair with a corrected reason; a provenance-stating tier entry; two
corrected audit records with one re-taken disposition; a derived ledger-premise case; decision `D-38`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@.planning/phases/31-autonomous-manual-testing/31-REVIEW.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-27-SUMMARY.md
@.planning/phases/31-autonomous-manual-testing/31-30-SUMMARY.md
@hooks/hook-entry.ts
@scripts/context-io.ts
@agent-factory/workflows/16-context-read-write.md
@docs/audit/31-round6-residuals.md
@docs/audit/harness-false-result-instances.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Note |
|---|---|---|
| `hostBuiltProjectRoot`'s condition set | CHANGED | Gains the version-control-marker condition and the not-the-kit-root condition, composed from `node:` builtins only. |
| the wrapper's stated reason comment | CHANGED | Replaced with the real reason for whatever difference remains, or with a statement that no difference remains. |
| `TRUSTED_ROOT_TIERS[0]` | CHANGED text | States that on the Claude Code hook path the delivered value is DERIVED from the host-built `CLAUDE_PROJECT_DIR` of the hook subprocess. |
| the wrapper/reader parity case | NEW derived case in `scripts/context-io.test.ts` | One shared candidate corpus through both predicates; the wrapper's accept set asserted a subset of the reader's, with the corpus's own non-vacuity asserted first. |
| `FROZEN_HOOK_ENTRY_LOGIC_SHA` | RE-TAKEN | In `scripts/floor-invariance.test.ts`, with the byte-count non-vacuity floor asserted first and the value's history block extended, never relaxed. |
| `scripts/harness-instance-ledger.test.ts` | NEW test file | The derived premise case for `docs/audit/harness-false-result-instances.md`: every row claiming a mechanism is greppable in the tree it names. |
| `docs/audit/31-round6-residuals.md` §6.1 | CORRECTED | Names the three symbols actually deleted, states the `hoisted` arm is live and narrowed rather than gone, and re-takes the CR-18 `missing:` bullet's disposition against the mechanism at this commit. |
| `docs/audit/harness-false-result-instances.md` row 13 | CORRECTED | Records that the position was removed, what covers it instead, and where the restore criterion lives. |
| `docs/audit/29-style-dispositions/31-37.md` | NEW disposition file | Rows for every clause changed in `agent-factory/workflows/16-context-read-write.md`. |
| `D-38` | NEW dated decision | Appended to `31-CONTEXT.md`. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The wrapper never delivers what the reader would reject, and the tier says where its value came from (WR-36)</name>
  <files>hooks/hook-entry.ts, hooks/hook-entry.js, scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts, scripts/floor-invariance.test.ts, agent-factory/workflows/16-context-read-write.md</files>
  <precondition>Editing `hooks/hook-entry.ts` moves `FROZEN_HOOK_ENTRY_LOGIC_SHA` in `scripts/floor-invariance.test.ts` and every `DECIDER_MANIFEST` entry for a rebuilt `.js`. Both must be re-taken with their own non-vacuity floors asserted first — the normalisation's removed-byte count printed before the digest is read — per `docs/audit/31-round6-residuals.md` §8.3, which records that this baseline moved twice inside `31-27` and how each re-take was recorded.</precondition>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — WR-36 in full: the quoted wrapper comment, the `existsSync` import citation, the two extra reader conditions, the sentence that nothing binds the pair, the `CLAUDE_PROJECT_DIR` promotion paragraph, and the whole `Fix:` block including its `TRUSTED_ROOT_TIERS[0]` amendment.
    - `hooks/hook-entry.ts` — the whole `AN UNUSABLE VALUE DELIVERS NOTHING` doc block, `HOST_DELIVERED_ROOT_ENV`, `hostBuiltProjectRoot`, `realpathSyncSafe`, the `deciderEnv` construction, the import list at the top of the file (to confirm what is already available), and `DECIDER_MANIFEST` with `verifyDeciderClosure`.
    - `scripts/context-io.ts` — `hostDeliveredRoot` in full with its whole doc block; `canonicalDirectoryPath`; `REPO_BOUNDARY_MARKERS`; `GOVERNANCE_FALLBACK_BASE`; `TRUSTED_ROOT_TIERS` in full; `TRUSTED_ROOT_RESIDUALS` including every `hosts: non-cc-hook-path` member; `trustedRepoRoot`.
    - `scripts/context-io.test.ts` — the existing case asserting tier 0's accepted set is a STRICT SUBSET of tier 1's, the `TRUSTED_ROOT_TIERS` cardinality case, and the workflow-binding case, so the new parity case joins that family rather than starting a second one.
    - `scripts/nonblocking-reader-parity.test.ts` as `31-36` left it — the both-directions binding shape this case copies.
    - `agent-factory/workflows/16-context-read-write.md` — the resolution-order section bound to `TRUSTED_ROOT_TIERS`.
    - `scripts/floor-invariance.test.ts` around `FROZEN_HOOK_ENTRY_LOGIC_SHA` — the normalisation, the non-vacuity floor and the history block.
    - `docs/audit/31-round6-residuals.md` §8.3 — how the two prior re-takes of that baseline were recorded.
  </read_first>
  <behavior>
    - RED 1 (the divergence, measured rather than reasoned): a shared corpus of candidate root values — absolute and relative, existing and absent, a file rather than a directory, a directory with a version-control marker, a directory WITHOUT one, the kit's own root, a symlink to each — driven through both predicates. At least one candidate is ACCEPTED by `hostBuiltProjectRoot` and REJECTED by `hostDeliveredRoot`; the corpus's own non-vacuity is asserted before the conclusion.
    - RED 2: the wrapper's stated reason is measured false — `existsSync` is imported and used in the same file.
    - RED 3: `TRUSTED_ROOT_TIERS[0]`'s text is measured against the wrapper's behaviour and recorded as describing the name without its provenance.
    - GREEN 1: the wrapper applies the marker condition and the not-the-kit-root condition, composed from `node:` builtins only, and the corpus produces no candidate accepted by the wrapper and rejected by the reader.
    - GREEN 2 (the binding, not the alignment): a case asserts the wrapper's accept set is a SUBSET of the reader's over the shared corpus, with the corpus non-vacuity asserted first; a seeded narrowing of the READER moves the case red, and is reverted.
    - GREEN 3: the wrapper's comment states the real reason for any remaining difference, or states that none remains — and a case asserts the comment's claim is not contradicted by the corpus result.
    - GREEN 4: `TRUSTED_ROOT_TIERS[0]` states the provenance; the workflow's published order equals the array in both directions; the tier cardinality is unchanged at five.
    - CONTROL 1: the existing strict-subset relation between tier 0 and tier 1 is re-measured unmoved.
    - CONTROL 2 (D-29's bound): the wrapper still answers in bounded time at every `DECIDER_MANIFEST` position — the CR-17 closure is re-driven with a `mkfifo` at one manifest path and a bounded named deny recorded, because this task adds filesystem operations to the wrapper's startup path and that is exactly the coordinate D-29 was convened on.
    - CONTROL 3: `TRUSTED_ROOT_RESIDUALS`'s `hosts: non-cc-hook-path` members are unchanged — this task changes what the CC path validates, not which hosts have the channel.
    - CONTROL 4: `hooks/guard.ts` is byte-unchanged and hashes to `FROZEN_GUARD_BLOB`.
  </behavior>
  <action>
    MOVEMENT 1 — build ONE shared candidate corpus and measure the divergence. Enumerate the candidate
    shapes listed in the behavior block once, drive them through both predicates, and print a table of
    candidate, wrapper answer, reader answer. Assert the corpus size is non-zero and print it before any
    conclusion is read. Quote the row or rows where the two disagree.

    MOVEMENT 2 — carry the two conditions to the wrapper. Add the version-control-marker condition and
    the not-the-kit-root condition using `node:fs` and `node:path` only, spelling the marker list and
    the kit root locally exactly as the wrapper already spells `GRUGOPS_HOST_DELIVERED_ROOT` locally,
    and note at the site that `scripts/context-io.test.ts` asserts the two spellings agree in both
    directions — the same discipline the env name already carries.

    MOVEMENT 3 — correct the reason, then bind the pair. Replace the false sentence with the real reason
    for whatever difference remains after MOVEMENT 2, or state that none remains. Add the subset
    assertion over the shared corpus, watch it red under a seeded narrowing of the reader, revert.

    MOVEMENT 4 — state the provenance in the tier entry and carry the workflow with it. Amend
    `TRUSTED_ROOT_TIERS[0]` to say that on the Claude Code hook path the delivered value is derived from
    the host-built `CLAUDE_PROJECT_DIR` of the hook subprocess, so a reader is not left to infer a
    separate channel; update `agent-factory/workflows/16-context-read-write.md` and re-run the
    both-directions binding.

    MOVEMENT 5 — re-take the frozen baselines, floors first. Rebuild; re-derive the hook manifest; move
    the `DECIDER_MANIFEST` entries for every rebuilt `.js` and re-measure the manifest WHOLE rather than
    only the entries that moved. Re-take `FROZEN_HOOK_ENTRY_LOGIC_SHA` by printing the normalisation's
    total and removed byte counts FIRST, asserting the removed count is non-zero, then recording the new
    digest and extending the constant's history block with the reason and the date.

    MOVEMENT 6 — re-drive D-29's own bound. With the wrapper's startup path now doing more filesystem
    work, re-run the CR-17 reproduction — a `mkfifo` planted at a manifest path, the wrapper driven
    under a timeout — and record the exit code, the elapsed time and both stream byte counts against
    `31-31`'s recorded numbers.
  </action>
  <acceptance_criteria>
    - The shared-corpus table is quoted in full with the corpus size printed before any conclusion, and the disagreeing rows are named.
    - The post-fix table shows no candidate accepted by the wrapper and rejected by the reader.
    - The subset assertion is present and was watched red under a seeded reader narrowing, then reverted, with both transcripts quoted.
    - The wrapper's replaced comment is quoted before and after; the new text states a reason the corpus does not contradict.
    - `TRUSTED_ROOT_TIERS[0]` states the provenance; the workflow binding passes in both directions and the tier cardinality is still five.
    - `FROZEN_HOOK_ENTRY_LOGIC_SHA` is re-taken with the total and removed byte counts printed first and the removed count asserted non-zero; the old and new digests and the reason are recorded in the history block.
    - Every `DECIDER_MANIFEST` entry is re-measured, with the totals quoted, not only the entries that moved.
    - The CR-17 bounded-deny reproduction is re-driven and its exit code, elapsed time and stream byte counts are quoted against `31-31`'s.
    - `hooks/guard.ts` hashes to `669725bc1c616ab57123e22090d93d57eff1b001`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness &amp;&amp; npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit, the build-parity or freshness step naming a committed `.js` that differs from a rebuild of its source, or the hook-manifest step reporting a decider or module hash that does not match a fresh derivation</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts scripts/floor-invariance.test.ts hooks/guard.test.ts hooks/admission-guard.test.ts</automated>
    <fails_when>non-zero exit, a summary line reporting `0 passed`, the frozen deploy-guard blob case reporting a hash other than `669725bc1c616ab57123e22090d93d57eff1b001`, or the wrapper/reader parity case reporting a candidate the wrapper accepts and the reader rejects</fails_when>
    <automated>npm run check:diff-disposition &amp;&amp; npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or the disposition gate naming a changed clause in `agent-factory/workflows/16-context-read-write.md` with no row in `docs/audit/29-style-dispositions/31-37.md`</fails_when>
  </verify>
  <done>The PreToolUse wrapper cannot deliver a root the reader would discard, the pair is bound by a shared corpus rather than by two independent condition lists, the stated reason is true, and the tier entry names where its value comes from.</done>
  <reversibility rating="costly">The wrapper's accept set narrows and two frozen baselines move. Reverting restores a tier-0 channel validated more weakly than the reader that consumes it, under a comment stating a reason measured false.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two audit records corrected against the tree, and a ledger whose own premise is asserted (WR-33, WR-34)</name>
  <files>docs/audit/31-round6-residuals.md, docs/audit/harness-false-result-instances.md, scripts/harness-instance-ledger.test.ts</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — WR-33 and WR-34 in full: the quoted §6.1 sentence, the three `grep -c` measurements, the naming of the three symbols D-30 (5) actually deleted, the statement that the disposition rests on a false fact; and for WR-34 the quoted row-13 catch column, the quoted opposing comment in `check-platform-shapes.ts`, the `grep -c factory.config.json` measurement, and the `Fix:` block asking for the derived check.
    - `docs/audit/31-round6-residuals.md` — §6.1 in full and the surrounding §6 sections, so the correction is scoped to the false clauses and the neighbouring disagreements are left as written.
    - `docs/audit/harness-false-result-instances.md` — the whole table, its header prose about how the list is derived, and the section explaining how its numbering relates to prior ordinals.
    - `scripts/check-platform-shapes.ts` — the dropped-position comment in full as `31-36` left it, and `deferred-items.md`'s restore criterion for that position, since row 13's corrected text must point at both.
    - `scripts/runnable-ref/uat-spec-integrity.ts` at THIS plan's commit — whether `resolveBinding`, `bindingRangeFor` and `listHoists` are still live after `31-34`'s change, because §6.1's correction must state what is true now rather than what was true at round 6's HEAD.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` round 6's gap 2 `missing:` bullet — the disposition being re-taken.
  </read_first>
  <behavior>
    - RED 1 (WR-33, re-measured at this commit): `resolveBinding`, `bindingRangeFor` and `listHoists` are each present in `scripts/runnable-ref/uat-spec-integrity.ts` with a declaration, contradicting §6.1's `removed ... outright`. Each grep's own hit count is printed and asserted non-zero before the conclusion.
    - RED 2 (WR-33's real deletions): `deriveTestInfoParameterNames`, `isFixtureBindingPosition` and `CALLEE_CHAIN_STEP_BOUND` have no declaration, with only comment-level mentions remaining — measured, with each grep's result quoted.
    - RED 3 (WR-34): the string `factory.config.json` appears in `scripts/check-platform-shapes.ts` only inside the explanatory comment that says the position was dropped, contradicting row 13's claim that the fixture now writes it.
    - RED 4 (the ledger's premise): a derived case that asks whether every row claiming a mechanism is greppable in the tree it names reports at least one row that is not — row 13 — and the case's own row denominator is asserted non-zero first.
    - GREEN 1: §6.1 names the three symbols actually deleted, states that the `hoisted` arm is live and NARROWED rather than gone, and re-takes the CR-18 `missing:` bullet's second half against the mechanism live at this commit.
    - GREEN 2: row 13's catch column records the position's removal, what covers it instead, and where the restore criterion is carried.
    - GREEN 3: the derived ledger case passes over every row, with its denominator printed.
    - GREEN 4: the case DISCRIMINATES — a seeded row claiming a mechanism that is absent turns it red, and a seeded row whose claimed mechanism is present keeps it green. Both are driven and reverted.
    - CONTROL 1: no `*-SUMMARY.md`, `31-REVIEW.md` or `31-VERIFICATION.md` is modified — asserted by a diff over the phase directory.
    - CONTROL 2: `npm run check:audit-register` and `npm run check:residual-citations` stay green over the corrected documents.
  </behavior>
  <action>
    MOVEMENT 1 — re-measure both false premises at THIS commit, not at round 6's. Run the greps WR-33
    cites, print each hit count, assert each is what the conclusion needs before drawing it, and quote
    the results. Do the same for row 13's `factory.config.json` claim. If `31-34`'s change moved any of
    the three symbols WR-33 names, say so explicitly — the correction states what is true now.

    MOVEMENT 2 — correct §6.1, and re-take its disposition. Name the three symbols actually deleted;
    state that the `hoisted` arm is live and was NARROWED by D-30 (5) rather than removed; then re-take
    the CR-18 `missing:` bullet's second half — treating an ambient `declare` binding as binding nothing
    — against the mechanism that exists at this commit, recording the disposition with its measurement
    rather than with a claimed deletion. Mark the correction as a correction, with its date and the
    finding id that forced it.

    MOVEMENT 3 — correct row 13. Record what was actually done: the GOV-02 ledger position was removed
    rather than re-fixtured, `scripts/context-io.test.ts` drives a FIFO and a directory at that path on
    every CI leg, and the restore criterion is carried in `deferred-items.md`. Keep the row's `how it was
    caught` column truthful about the SIGNAL that caught it — the control and the refusing shape
    returning the same verdict — and note that `31-36` made that signal producible again.

    MOVEMENT 4 — assert the ledger's own premise. Create `scripts/harness-instance-ledger.test.ts`
    parsing the table, deriving for each row the mechanism strings it claims are present together with
    the file each names, and asserting each is greppable there. Assert the derived row count is non-zero
    before the per-row conclusions, and state in the message why: a ledger case with an empty
    denominator is the vacuity shape this repository's own list records.

    MOVEMENT 5 — prove the case discriminates in BOTH directions. Seed a row claiming an absent
    mechanism, watch red; revert. Seed a row claiming a present one, watch green; revert. Quote both
    transcripts.
  </action>
  <acceptance_criteria>
    - Every grep this task relies on prints its hit count, and the count is asserted before any conclusion is drawn from it.
    - §6.1's corrected text names `deriveTestInfoParameterNames`, `isFixtureBindingPosition` and `CALLEE_CHAIN_STEP_BOUND` as the deletions, states the `hoisted` arm's live-and-narrowed status as measured at this commit, and carries a dated correction note naming WR-33.
    - The re-taken CR-18 `missing:` disposition quotes the measurement it rests on, and states plainly whether the bullet is CLOSED, ACCEPTED as a named residual, or CARRIED with an owner.
    - Row 13's corrected catch column records the removal, the covering test, and the restore criterion's location, and does not claim a mechanism absent from the tree.
    - `scripts/harness-instance-ledger.test.ts` exists, asserts a non-zero row denominator first, and was watched red and green under the two seeded rows.
    - A diff over `.planning/phases/31-autonomous-manual-testing/` shows no `*-SUMMARY.md`, `31-REVIEW.md` or `31-VERIFICATION.md` modified.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/harness-instance-ledger.test.ts</automated>
    <fails_when>non-zero exit, a summary line reporting `0 passed`, or the row-denominator assertion reporting zero rows parsed from the ledger</fails_when>
    <automated>npm run check:audit-register &amp;&amp; npm run check:residual-citations &amp;&amp; npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or the audit-register gate naming a row in either corrected document that carries no anchor</fails_when>
    <automated>git diff --quiet -- .planning/phases/31-autonomous-manual-testing/31-REVIEW.md .planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md</automated>
    <fails_when>non-zero exit — a prior round's review or verification record was rewritten rather than annotated</fails_when>
  </verify>
  <done>Both audit records state what is true of the tree at this commit, the disposition that rested on a false premise is re-taken against a measurement, and the ledger carries a case that asks its own premise before reporting.</done>
  <reversibility rating="reversible">Two corrected documents and one new test. Reverting restores a register that states a mechanism is present where it is not, and a disposition resting on a deletion that did not happen.</reversibility>
</task>

<task type="auto">
  <name>Task 3: D-38 and the round's running gates at this commit</name>
  <files>docs/audit/29-style-dispositions/31-37.md, .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` — the D-29 through D-37 blocks, for the exact shape D-38 must take, and D-29's `TRUSTED_ROOT_TIERS` paragraph, which D-38 amends with the provenance sentence.
    - `docs/audit/29-style-dispositions/31-35.md` and `31-36`'s recorded disposition count — the base this plan's count is compared against.
    - Task 1 and Task 2 summaries drafted in this plan — the measurements D-38 quotes.
  </read_first>
  <action>
    MOVEMENT 1 — write `docs/audit/29-style-dispositions/31-37.md` with a row for every clause this plan
    changed in `agent-factory/workflows/16-context-read-write.md`.

    MOVEMENT 2 — record D-38 in `31-CONTEXT.md` as a `#### Gap-closure decision — D-38 (2026-09-11,
    gap-closure round 7, wave 6, plan 31-37)` block in the shape D-29 through D-37 use: what forced it
    (WR-36, WR-33, WR-34), which register failed (not the resolution ORDER and not the tier SET — what
    failed is that two implementations of one predicate were never BOUND, and that two audit records
    stated facts nobody re-measured), the decision in two numbered parts — a delivering side's accept
    set is asserted a subset of the consuming side's over one shared corpus; a record that claims a
    mechanism is checkable against the tree it names — the AMENDMENT to D-29's tier-0 sentence stated
    explicitly, what D-38 does NOT establish (the non-CC hosts still have no delivered channel; the
    Windows leg stays `R-03`), the reversibility rating, and the three places that must agree.

    MOVEMENT 3 — re-measure the round's running gates at this plan's closing commit and record them
    beside `31-31`'s and beside `31-36`'s: the disposition debt count, the freshness output count, the
    hook manifest counts, the foundation guards, the UAT oracles, and the platform shapes.
  </action>
  <acceptance_criteria>
    - `docs/audit/29-style-dispositions/31-37.md` carries a row for every changed workflow clause, and `npm run check:diff-disposition` shows no growth against the count `31-31` recorded, with both counts quoted.
    - D-38 is appended to `31-CONTEXT.md` in the required shape and states the amendment to D-29's tier-0 sentence explicitly rather than leaving two decisions disagreeing.
    - The running gate numbers are quoted beside `31-31`'s and `31-36`'s.
    - `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` are byte-unchanged by this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm run check:diff-disposition &amp;&amp; npm run freshness &amp;&amp; npm run freshness:hook-manifest &amp;&amp; node scripts/check-foundation-guards.js &amp;&amp; node scripts/check-uat-oracles.js &amp;&amp; node scripts/check-platform-shapes.js</automated>
    <fails_when>non-zero exit, or the disposition gate reporting a count greater than the one `31-31` recorded</fails_when>
    <automated>git diff --quiet -- .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <fails_when>non-zero exit — a requirement checkbox, a traceability row or the phase checkbox moved inside a gap-closure plan</fails_when>
  </verify>
  <done>The disposition is recorded as D-38 with its amendment stated, every changed prose clause carries a row, and the running gates are re-taken at this commit.</done>
  <reversibility rating="reversible">A decision record, a disposition file and a set of measurements.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the host's `CLAUDE_PROJECT_DIR` → the PreToolUse wrapper → `GRUGOPS_HOST_DELIVERED_ROOT` | an ambient value is promoted into the tier-0 name the reader treats as a channel the agent cannot write |
| the delivered root → the governance dial that decides admission | whichever repository the root names supplies `factory.config.json`, and with it the human-admission and audit-retention posture |
| a tracked audit record → the next round's reader | a register a later reader starts from is a trust surface; a claim it cannot substantiate is a false premise handed forward |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-37-01 | Spoofing | `hostBuiltProjectRoot`'s weaker accept set | high | mitigate | A candidate the wrapper accepts and the reader would reject is a value delivered on a channel documented as trusted. Task 1 carries the marker and not-kit-root conditions to the wrapper and asserts the subset relation over one shared corpus. |
| T-31-37-02 | Repudiation | `TRUSTED_ROOT_TIERS[0]`'s description | medium | mitigate | A tier described as a channel the agent cannot write, whose value derives from an ambient variable, invites a reader to over-trust it. The entry states the provenance and the workflow moves with it. |
| T-31-37-03 | Denial of service | added filesystem work on the wrapper's startup path | high | mitigate | D-29 exists because an unbounded operation before the decider spawns makes a PreToolUse hook that never answers, which does not block. Control 2 re-drives the CR-17 reproduction with a `mkfifo` at a manifest path and records the bounded named deny with its elapsed time. |
| T-31-37-04 | Repudiation | `docs/audit/harness-false-result-instances.md` | medium | mitigate | A ledger row claiming a mechanism that is absent hands a false premise to the next round. Task 2 corrects the row and adds a derived case asserting every claimed mechanism is greppable in the tree it names. |
| T-31-37-05 | Tampering | the frozen wrapper baseline | high | mitigate | `FROZEN_HOOK_ENTRY_LOGIC_SHA` is re-taken with its non-vacuity floor asserted first and its history extended; the `DECIDER_MANIFEST` is re-measured WHOLE; `hooks/guard.ts` stays byte-identical to `FROZEN_GUARD_BLOB`. |
| T-31-37-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan runs NO package-manager install. `git diff -- package.json package-lock.json` is asserted EMPTY at the plan's closing gate; a non-empty result halts the plan for a named human. |
</threat_model>

<verification>
- The shared candidate corpus is driven through both predicates before and after, with the corpus size printed and the disagreeing rows named.
- The wrapper's accept set is asserted a subset of the reader's, watched red under a seeded reader narrowing.
- `TRUSTED_ROOT_TIERS[0]` states the provenance and the workflow binding passes in both directions with five tiers.
- `FROZEN_HOOK_ENTRY_LOGIC_SHA` is re-taken with byte counts first; the `DECIDER_MANIFEST` is re-measured whole; `hooks/guard.ts` hashes to `669725bc1c616ab57123e22090d93d57eff1b001`.
- The CR-17 bounded-deny reproduction is re-driven with its elapsed time and stream byte counts.
- Both audit records are corrected against greps re-measured at this commit, and the ledger case discriminates in both directions.
- `31-REVIEW.md`, `31-VERIFICATION.md` and every `*-SUMMARY.md` are byte-unchanged.
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` are byte-unchanged.
</verification>

<success_criteria>
- WR-36 is closed: the wrapper cannot deliver a root the reader would reject, the stated reason is true, and the pair is bound by a shared corpus.
- WR-33 is closed: §6.1 names the real deletions and its disposition is re-taken against a measurement.
- WR-34 is closed: row 13 records what was done, and the ledger's own premise is asserted by a derived case.
- `D-38` is appended to `31-CONTEXT.md` with the D-29 amendment stated.
- No requirement checkbox, traceability row or phase checkbox moved.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-37-SUMMARY.md` when done.
</output>

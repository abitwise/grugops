---
phase: 31-autonomous-manual-testing
plan: 44
type: execute
wave: 13
depends_on: ["31-43"]
gap_closure: true
files_modified:
  - docs/audit/31-round8-residuals.md
  - .planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
  - .planning/phases/31-autonomous-manual-testing/deferred-items.md
  - .planning/STATE.md
autonomous: true
requirements: [UATX-01, UATX-02, UATX-03, UATX-04, UATX-05, UATX-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every reproduction `31-REVIEW.md` (the round-7 code review) and `31-VERIFICATION.md` (round 8) recorded is RE-RUN at ONE commit against the committed `.js`, using the exact probe spellings THOSE documents used — not paraphrases and not this round's own fixtures. A closure round that re-measures its own tests measures the fix, not the finding. This is the rule that made rounds 5, 6 and 7's closing measurements the strongest artifacts this phase has produced, and it is the rule this plan inherits unchanged."
    - "The re-run set is not only round 8's. The round-6 and round-7 closures — CR-17 through CR-25 — are re-driven as CONTROLS at the same commit, because this round's fixes edit the very modules those closures live in, and this phase's recorded pattern across EIGHT rounds is that a fix creates its successor's defect. A closure that reports only the rows it moved cannot show it broke nothing."
    - "Where round 8's verifier ACCEPTED a closure from a source-read rather than re-driving it (CR-22, CR-23, CR-24, CR-25 — all four of round 7's closures were confirmed by reading source plus one suite run, with the reproduction budget spent on the two NEW Criticals instead), this plan re-drives it independently or records plainly that it did not and why. A closure carried forward twice without a live measurement is how a phase convinces itself."
    - "Every finding of `31-REVIEW.md` — CR-26, CR-27, WR-37, WR-38, WR-39, WR-40, WR-41, WR-42, IN-19, IN-20, IN-21 — every Anti-Patterns row of `31-VERIFICATION.md`, every `missing:` bullet, and every item in `deferred-items.md` naming round 8 or round 9 as owner, carries an explicit disposition in a written ledger: CLOSED with the measurement that proves it, DECIDED-AND-NAMED as a residual with the reason, or CARRIED with an owner and a criterion. No finding is left without a row, and no row asserts a closure this round did not measure."
    - "The disposition tally is asserted as a COVERAGE equality with both sides DERIVED at run time, never as a cardinality against a typed number: the ledger's rows are derived from the source documents' own findings and the count covered by at least one authored disposition must equal the derived item count while the count covered by none must equal zero. A hand-typed number on either side is the drift class this phase has spent two plans deleting. The heading scan that produces the item set STRIPS FENCED BLOCKS first, because a `##` heading inside a fenced transcript in a review is a parse artifact and this phase has already carried one forward as a phantom finding."
    - "The whole excluded-e2e suite is measured GREEN at ONE commit with the exact command `npx vitest run --exclude '**/scripts/e2e/**'`, and the file, pass and skip counts are recorded verbatim. A green suite is NOT the proof — this phase's own doctrine, established across eight rounds, is that the suite exercised none of the Criticals that blocked it — but a RED suite is a disqualifier, and the counts are the floor a ninth round starts from."
    - "The frozen floors are re-measured rather than assumed: `hooks/guard.ts` hashes equal to `FROZEN_GUARD_BLOB`; the hook-entry wrapper's normalisation equals its pinned value; `ADMIT_FROZEN_SHA256` equals a fresh hash of `admit()`'s span at the value `31-39` re-baselined it to, with the PRIOR value printed beside it; and every `DECIDER_MANIFEST` entry equals the current digest of the file it names. The manifest is checked as a WHOLE, not only for the entries this round moved, because a stale entry elsewhere is the same class of drift."
    - "The residual register for round 8 is written down once, in a tracked document, listing every boundary this round's five fix plans decided to leave open — at minimum the remaining half of `R-31-33-01` after the unfreeze, `R-31-33-02`'s undecided default owner, the widened refusal and its shared-install answer, the run-time-assembled write-path call the syntax-tree census cannot see, any swallow site whose fail-open was kept with its measured throw count, the installed-package surface magnitude, the changed platform-shapes label semantics, and the Windows remainder — each with the reason and what would force it closed, so round 9's reviewer starts from a register rather than rediscovers it."
    - "Every decision this round recorded — D-39 (the one owning-repository authority, the dial/ledger separation and the deliberate `admit()` unfreeze), D-40 (the derived divergence census and its closed disposition vocabulary), D-41 (the per-route per-branch claim correction and the re-stated residuals), D-42 (the derived swallow-site set and the ban-set family reachability census) and D-43 (the control that observes its own effect and the derived outcome label) — has its `does not establish` section read and its residual dispositions carried into this record's register. A decision whose stated boundaries live only in `31-CONTEXT.md` is a boundary the next round's reviewer has to rediscover, which is the cost this record exists to remove. The prior decisions those five rest on — D-14's fail-closed unreadable configuration, D-24's note-then-ledger order, D-31's one-variable-per-action rule, D-34's entry-level property and derived-set rule, and D-36's reached-bound signal — are each re-driven as a control row rather than assumed intact."
    - "This plan writes no source and no test. It measures. A closing measurement that fixes something has found a new defect, and a new defect belongs in a new plan with its own RED-first reproduction — the standing rule `docs/audit/31-round5-residuals.md` recorded and every closing plan since has inherited. Anything found here is recorded with an owner rather than repaired."
    - "The measurement environment is proven clean by predicates that can OBSERVE it: the temp probe directory empty by a real listing, no named pipe anywhere in the tree, and the working-tree residue predicate demonstrated inert rather than assumed. The `.planning/STATE.md` write is VERIFIED after it is taken — this repository's state writer re-escapes backslashes on every write, and a pathological line length there has previously combined with a superlinear guard predicate to turn a sub-second gate into a multi-minute one."
    - "UATX-01 through UATX-06 stay UNCHECKED in `.planning/REQUIREMENTS.md`, the Phase 31 checkbox stays unchecked in `.planning/ROADMAP.md`, and every traceability row still reads `Gaps Found`. Only a verification round may flip a requirement — and this phase has now had eight rounds in which the executing round believed it had closed one."
  artifacts:
    - docs/audit/31-round8-residuals.md
    - .planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
    - .planning/phases/31-autonomous-manual-testing/deferred-items.md
    - .planning/STATE.md
  key_links:
    - "every recorded reproduction <-> its post-round result at ONE commit, driven with the ORIGINAL document's own spelling: the property that makes this record evidence rather than a restatement of the fix plans' own summaries."
    - "every round-6 and round-7 closure <-> a CONTROL row at the same commit: the axis that would show this round having broken something it did not aim at."
    - "the derived finding set <-> the authored disposition set: a coverage equality with both sides measured at run time and fenced blocks stripped before the heading scan."
    - "each frozen floor <-> a fresh measurement of the artifact it pins, including the re-baselined admission freeze with its prior value printed beside it."
  prohibitions:
    - statement: "This plan writes no source and no test. Anything it finds is recorded with an owner, never repaired here."
      status: unverified
      verification: flagged
    - statement: "A reproduction is re-run with the ORIGINAL finding document's own spelling. Re-running this round's own fixture measures the fix, not the finding."
      status: unverified
      verification: flagged
    - statement: "A prior round's record is history and is never rewritten. Where this round disagrees with one, BOTH values are printed and the disagreement is a row."
      status: unverified
      verification: flagged
    - statement: "No count on either side of an equality is typed. Both sides are measured at run time, and a heading scan strips fenced blocks before counting."
      status: unverified
      verification: flagged
    - statement: "A gap-closure plan must never flip a requirement checkbox, a traceability row or the phase checkbox. Only a verification round may do that."
      status: unverified
      verification: flagged
    - statement: "Never fake a passing gate, a test result, or a citation. A reproduction that was not re-driven is recorded as not re-driven, with the reason."
      status: unverified
      verification: flagged
    - statement: "Never merge a protected branch and never deploy to production without a named human confirmation."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-38-PLAN.md: the closing-measurement contract this plan inherits — one commit, the finding documents' own spellings, prior closures as controls, a disposition per finding, a coverage equality with both sides derived, and a written residual register."
    - "31-39-PLAN.md through 31-43-PLAN.md: every prohibition those five plans carried. This plan re-checks the two that are mechanically checkable at the end of the round — that no requirement row moved, and that the frozen floors hold."
---

<objective>
Re-measure round 9's own evidence at one commit, with the finding documents' own spellings, and leave
the ninth verification round a record rather than a rediscovery.

This is the closing measurement for gap-closure round 9 — fix plans `31-39` through `31-43`, against
`31-REVIEW.md`'s round-7 code review (2 Critical, 6 Warning, 3 Info) and `31-VERIFICATION.md`'s
EIGHTH verification pass (`gaps_found`, 5/6, three `missing:` bullets).

Purpose: a verification round reads source and committed artifacts. It does not read a planning
conversation, and it should not have to re-derive a probe from a fix plan's own fixture. This file
exists so the ninth round starts from five things it would otherwise rediscover: a table pairing
every recorded reproduction with its post-round result at the original spelling; a control table
re-driving every round-6 and round-7 closure at the same commit; a disposition row for every finding,
anti-pattern row, `missing:` bullet and owner-bearing deferred item; the frozen floors re-measured
whole; and a written register of the boundaries this round decided to leave open.
Output: `docs/audit/31-round8-residuals.md`; an updated `31-VALIDATION.md`; an updated, never
rewritten, `deferred-items.md`; and a short verified `.planning/STATE.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@.planning/phases/31-autonomous-manual-testing/31-REVIEW.md
@.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
@.planning/phases/31-autonomous-manual-testing/deferred-items.md
@docs/audit/31-round7-residuals.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Note |
|---|---|---|
| `docs/audit/31-round8-residuals.md` | NEW audit record | Follows `docs/audit/31-round7-residuals.md`'s section shape rather than inventing a new one. Named for the ROUND IT SERVES, matching its predecessor's naming convention — confirm that convention against the predecessor before naming the file, and record which reading was taken. |
| The reproduction table | NEW section | Every recorded reproduction paired with its post-round result, at the original document's own spelling, with all-root readings where the finding had them. |
| The control table | NEW section | CR-17 through CR-25 re-driven at the same commit, each marked RE-DRIVEN or ACCEPTED-WITH-REASON. |
| The disposition ledger | NEW section | One row per finding, anti-pattern row, `missing:` bullet and owner-bearing deferred item, with a derived-both-sides coverage equality. |
| The frozen-floor table | NEW section | Each floor with its fresh measurement, and the admission freeze with its prior value beside the new one. |
| The residual register | NEW section | Every boundary this round left open, with the reason and what would force it closed. |
| `.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md` | CHANGED | The three standing human-verification items carried forward unchanged, plus whatever this round newly cannot verify on this box. |
| `.planning/phases/31-autonomous-manual-testing/deferred-items.md` | APPENDED | Never rewritten. New items appended under this round's own heading, with owners and criteria. |
| `.planning/STATE.md` | CHANGED | A short status, verified after the write. |

<tasks>

<task type="auto">
  <name>Task 1: Re-run every recorded reproduction at one commit, with the controls beside them</name>
  <files>docs/audit/31-round8-residuals.md</files>
  <precondition>`31-39` through `31-43` are all committed, the working tree is clean for the source, hook, kit, install, docs, workflow, config and requirement paths, and `npm run freshness` reports every committed output fresh. Every reading in this task is taken at ONE commit against the committed build outputs, and a dirty tree or a stale output would make the whole table a measurement of something that is not on any host.</precondition>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — the whole document. Every reproduction transcript, every premise line, every cited file and line, and every `Fix:` block, for all eleven findings. These spellings are what this task re-drives; a paraphrase measures something else.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the whole document. The `re_verification` block including `gaps_closed` and `gaps_remaining`; the `gaps` block with its `reason`, `artifacts` and all three `missing` bullets; the Round History table; the Goal Achievement truths table; the Required Artifacts table; the Key Link table with its two NOT_WIRED rows; every Behavioral Spot-Check row with its exact command and output; the Anti-Patterns table; the reproduction budget note; and the `human_verification` items.
    - `docs/audit/31-round7-residuals.md` — the whole document, as the section shape this record follows and as the source of the round-6 and round-7 control spellings. Read how it marked a row RE-DRIVEN versus ACCEPTED, and how it printed a disagreement with a prior record.
    - `docs/audit/31-round6-residuals.md` — its control table, for the CR-17 through CR-21 spellings.
    - `.planning/phases/31-autonomous-manual-testing/31-39-SUMMARY.md` through `31-43-SUMMARY.md` — each plan's recorded measurements and its `boundaries left open` section. Read them to know what to CHECK, not to copy their conclusions: this record's rows are this task's own readings.
    - `.planning/phases/31-autonomous-manual-testing/red-evidence/31-39-task2-red.json` — the pre-fix all-root readings, so the post-fix readings can be printed beside them.
  </read_first>
  <action>
    MOVEMENT 1 — pin the commit and prove the tree. Record the commit identity and its date, the
    platform and the Node version, and the round base — the commit before `31-39`'s first — with the
    commit count over the round. Take the working-tree residue reading for the source, hook, kit,
    install, docs, workflow, config and requirement paths BEFORE the first probe, and DEMONSTRATE the
    predicate is inert rather than asserting it: show that it reports a change when one is present in
    a scratch copy, so an empty reading is evidence rather than a default. Every probe below runs under
    a scratch directory outside the repository tree.

    MOVEMENT 2 — re-drive every reproduction, at the original spelling. For each of the eleven findings
    and each of the three `missing:` bullets, take the reading the ORIGINAL document took: the same
    call, the same arguments, the same premise assertions first, the same counts in the same places.
    Where the original read counts in several roots, read them all — a probe that reads one root cannot
    see a split, which is how CR-26 stayed alive through a round that believed it closed CR-22. Print
    the pre-fix reading beside the post-fix one. Where a reading CANNOT be taken at this commit — the
    mechanism changed shape, or the instrument does not exist on this box — say so in the row, with the
    reason, and do not substitute a different probe silently.

    MOVEMENT 3 — re-drive the controls. Take CR-17 through CR-25 from the round-6 and round-7 records,
    at their own spellings, and drive each at this commit. Mark each row RE-DRIVEN with its reading, or
    ACCEPTED with the reason it was not re-driven and what evidence stands in its place. Give
    particular weight to the four round-7 closures round 8 confirmed by source-read rather than by
    driving — those are closures now carried forward twice without a live measurement — and to every
    closure living in a module `31-39` through `31-43` edited.

    MOVEMENT 4 — re-measure the whole suite and every frozen floor. Run the excluded-e2e suite with
    the exact command and record the file, pass and skip counts and the exit status verbatim. Re-measure
    each frozen floor and print the value beside the constant it is pinned against, including the
    re-baselined admission freeze WITH its prior value. Check the decider manifest as a whole, printing
    the entry count. Re-run every repository gate and record each one's exit status and its final line.

    MOVEMENT 5 — write the record. Follow the predecessor's section shape. Before naming the file,
    confirm the predecessor's naming convention — which round's number its filename carries relative
    to the round it serves — and record which reading you took, so the convention is followed rather
    than guessed. Where this round's readings disagree with a prior record, print BOTH values and make
    the disagreement a row; never edit the prior record.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the summary reports any failed test, or fewer than 66 test files — this is the floor reading the record publishes and a red suite disqualifies the round regardless of what the reproduction table says</fails_when>
    <automated>npm run build && npm run typecheck && npm run check:build-parity && npm run freshness && npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit, or the parity step names any tracked `.js` file as moved, or either freshness step reports a mismatch</fails_when>
    <automated>node scripts/check-foundation-guards.js && node scripts/check-uat-oracles.js && node scripts/check-platform-shapes.js</automated>
    <fails_when>non-zero exit, or any of the three does not end with its all-checks-passed line</fails_when>
    <automated>npm run check:claim-anchors && npm run check:audit-register && npm run check:residual-citations && npm run check:banned-claims && npm run check:diff-disposition && npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or any check names a violation — the record this plan writes is itself inside the corpus these gates watch</fails_when>
    <automated>git status --porcelain -- scripts hooks agent-factory install docs .github vitest.config.ts package.json .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <fails_when>the output is non-empty for any path other than `docs/audit/31-round8-residuals.md` — any other name means a probe wrote inside the tracked tree or this plan modified source, which it is forbidden to do</fails_when>
  </verify>
  <acceptance_criteria>
    - The record states the commit identity and date, the platform, the Node version, the round base and the commit count over the round.
    - The working-tree residue predicate is DEMONSTRATED inert — shown reporting a change when one is planted in a scratch copy — before the first probe, and the reading is taken again after the last.
    - Every one of the eleven findings and all three `missing:` bullets has a reproduction row carrying the ORIGINAL document's own spelling, the pre-fix reading and the post-fix reading, with all-root counts wherever the original had them.
    - Any reading that could not be taken says so in its row with the reason, and no substitute probe is presented as the original.
    - CR-17 through CR-25 each have a control row marked RE-DRIVEN with its reading or ACCEPTED with its reason and the evidence standing in its place.
    - The suite's file, pass and skip counts and exit status are recorded verbatim.
    - Every frozen floor has a row with its fresh measurement beside the constant it is pinned against, and the admission freeze's row carries the prior value as well as the new one.
    - The decider manifest is checked as a whole with its entry count printed.
    - Every repository gate has a row with its exit status and its final line.
    - The filename's naming convention is confirmed against the predecessor record and the reading is stated in the document.
    - No source file and no test file is modified by this task.
  </acceptance_criteria>
  <done>Every recorded reproduction and every prior closure has a reading at one commit, taken at the original spelling, with the tree proven clean by a predicate demonstrated able to observe a change.</done>
</task>

<task type="auto">
  <name>Task 2: A disposition for every finding, with the coverage equality derived on both sides</name>
  <files>docs/audit/31-round8-residuals.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — every finding heading, and every `Fix:` block, because a disposition that does not engage the suggested fix has to say why.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the Anti-Patterns table, the `missing:` bullets, the `gaps_remaining` entry and the Required Artifacts table's PARTIAL and FAILED rows. Each is an item needing a row.
    - `.planning/phases/31-autonomous-manual-testing/deferred-items.md` — every item whose owner names round 8's fix plan, round 8's planner, or round 9. Read the whole `Raised by this plan` and `Harness false results` sections, because those are the items handed forward by name.
    - `docs/audit/31-round7-residuals.md` — the section that asserted its own no-silent-drop equality, and the note recording that a `##` heading inside a fenced transcript in a review is a PARSE ARTIFACT that its convening brief carried as a fourteenth finding. The item derivation here must strip fences before scanning headings, and must say so.
    - `docs/audit/31-round6-residuals.md` — its disposition section, as the shape a row follows.
    - `.planning/phases/31-autonomous-manual-testing/31-39-SUMMARY.md` through `31-43-SUMMARY.md` — the `boundaries left open` sections, which are the authored side of the coverage equality for every item this round decided rather than closed.
  </read_first>
  <action>
    MOVEMENT 1 — derive the item set, with fences stripped. Produce the set of items needing a
    disposition by scanning the two source documents: the review's finding headings and the
    verification's anti-pattern rows, `missing:` bullets and non-verified artifact rows, plus every
    owner-bearing item in the deferred record. STRIP FENCED BLOCKS before the heading scan — a heading
    inside a transcript is not a finding, and this phase has already carried one forward as a phantom.
    Print the derived count and the derived member list.

    MOVEMENT 2 — write one row per item. Each row states the item, the disposition (CLOSED,
    DECIDED-AND-NAMED, or CARRIED), the measurement or the reason, and for a CARRIED item the owner and
    the criterion that would close it. A CLOSED row must cite a reproduction row from Task 1; a row
    asserting a closure this round did not measure is the failure this record exists to prevent.

    MOVEMENT 3 — assert the coverage equality with BOTH sides measured. Count the items covered by at
    least one authored disposition and the items covered by none. The first count must equal the
    derived item count and the second must equal zero, and both numbers are measured at run time from
    the derived set and the written rows rather than typed. Print all three numbers. If the equality
    does not close, DO NOT adjust a number — add the missing rows, or record the disagreement as a row
    of its own with both values printed.

    MOVEMENT 4 — write the residual register. One entry per boundary this round left open, each with
    the shape stated as a situation, the reason as an argument rather than an assurance, and what would
    force it closed. Take the entries from the five fix plans' own `boundaries left open` sections plus
    anything Task 1 found, and include at minimum: the remaining half of `R-31-33-01` after the
    unfreeze; `R-31-33-02`'s undecided default owner; the widened refusal with its shared-install
    answer; the run-time-assembled write-path call the syntax-tree census cannot see; every swallow
    site whose fail-open was kept, with its measured throw count; the installed framework surface's
    magnitude; the changed platform-shapes label semantics with the transcript-comparison warning; and
    the Windows remainder.

    MOVEMENT 5 — carry the human-verification items. Update `31-VALIDATION.md` with the three standing
    items unchanged — the attended browser lane's real interactive behaviour, the authentication
    predicate under the two credential configurations, and the Windows leg of every browser probe and
    of this round's own findings — plus anything this round newly cannot verify on this box, each with
    its `UNKNOWN - verify` marker intact. None is closed by inference and none is dropped.
  </action>
  <verify>
    <automated>npm run check:audit-register && npm run check:residual-citations && npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or any check names a published citation whose path this repository does not track, an unanchored claim, or an audit-register row that does not resolve</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/harness-instance-ledger.test.ts scripts/uat-gate-exit-contract.test.ts scripts/audit-model.test.ts</automated>
    <fails_when>non-zero exit, or any failed test — these three read the audit records this task writes into, and a red means the new record broke a property one of them asserts</fails_when>
    <automated>npm run check:diff-disposition && npm run check:banned-claims && npm run check:public-docs</automated>
    <fails_when>non-zero exit, or the disposition check names a changed clause with no row, or either content check names a violation in this record's prose</fails_when>
  </verify>
  <acceptance_criteria>
    - The item set is derived by scanning the source documents with fenced blocks stripped before the heading scan, and the document states that the stripping was applied and why.
    - The derived item count and the derived member list are printed in the record.
    - Every derived item has exactly one disposition row; every CLOSED row cites a reproduction row from Task 1; every CARRIED row names an owner and a criterion.
    - Three numbers are printed: the derived item count, the count covered by at least one authored disposition, and the count covered by none. The second equals the first and the third is zero, or the disagreement is a row of its own with both values.
    - No number on either side of the equality is typed into the document as an expected value; each is stated as a measurement with how it was taken.
    - The residual register contains at least one entry per boundary listed in the action, each with a shape, a reason and what would force it closed.
    - `31-VALIDATION.md` carries the three standing human-verification items unchanged with their markers intact, plus any newly unverifiable item.
    - No source file and no test file is modified by this task.
  </acceptance_criteria>
  <done>Every finding, anti-pattern row, `missing:` bullet and owner-bearing deferred item has exactly one disposition, the coverage equality closes with both sides measured, and every boundary left open is in a register a ninth round can start from.</done>
</task>

<task type="auto">
  <name>Task 3: Append to the deferred record, write a verified short state, and prove the environment clean</name>
  <files>.planning/phases/31-autonomous-manual-testing/deferred-items.md, .planning/STATE.md, docs/audit/31-round8-residuals.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/deferred-items.md` — the whole file, and in particular how each prior round appended its own heading rather than editing an earlier one. This file is APPENDED, never rewritten.
    - `.planning/STATE.md` — the whole frontmatter, and specifically the length of each single-line value. A prior round shortened one line for a measured reason and recorded the measurement; the same check is taken again here.
    - `docs/audit/31-round7-residuals.md` — the section recording how the environment was proven clean, including the rule that the temp and named-pipe sweep is taken AFTER the suite and that the mid-flight readings are printed, because a prior session's own probe residue turned a suite red.
    - `.planning/ROADMAP.md` — the Phase 31 section's Plans block and its narrative paragraph, to confirm what this plan must NOT change: no checkbox moves, and the phase stays In Progress.
    - `.planning/REQUIREMENTS.md` — the six requirement rows and the traceability table, for the same reason.
  </read_first>
  <action>
    MOVEMENT 1 — append to the deferred record under this round's own heading. One subsection for items
    this round CLOSED that a prior round had deferred, one for items RAISED here with owners and
    criteria, and one for any harness false result this round produced — including any instance found
    while taking Task 1's readings. Take every ordinal from the authoritative list rather than from
    prose. Do not edit a single earlier line.

    MOVEMENT 2 — prove the environment clean with predicates that can observe it. Take the temp-probe
    and named-pipe sweep AFTER the suite run, not before, and print the mid-flight readings that show
    why the ordering matters. Demonstrate the working-tree residue predicate inert, as Task 1 did, and
    print both readings. If any sweep finds residue, remove it, re-run whatever it invalidated, and
    record both the finding and the re-run rather than only the clean reading.

    MOVEMENT 3 — write a SHORT state and verify the write. Write `.planning/STATE.md` with a status
    naming: which plans this round executed, what was closed by measurement with the numbers, what was
    carried with owners, the suite counts, the frozen-floor readings including the admission freeze's
    new and prior values, the disposition tally, and the record's path. Then VERIFY the file after
    writing it: measure the longest line and the count of consecutive backslashes anywhere in it, and
    record both. This repository's state writer re-escapes backslashes on every write, and a
    pathological line there has previously combined with a superlinear guard predicate to turn a
    sub-second gate into a multi-minute one. If either reading is pathological, shorten the value to a
    pointer and record the deviation.

    MOVEMENT 4 — confirm the boxes did not move. Read the six requirement rows, the traceability table
    and the Phase 31 checkbox and record each one's state verbatim. A gap-closure round does not flip
    them, and recording the reading is how a ninth round knows this one did not.
  </action>
  <verify>
    <automated>git status --porcelain -- scripts hooks agent-factory install docs/audit/29-style-dispositions .github vitest.config.ts package.json .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <fails_when>the output is non-empty for any path — a non-empty reading here means this plan modified source, a style-disposition file, or a requirement document, all of which it is forbidden to touch</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the summary reports any failed test, or fewer than 66 test files, or the counts differ from Task 1's recorded reading without an explanation in the record</fails_when>
    <automated>npm run check:nul-bytes && npm run check:audit-register && npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or the NUL-byte check names any file — a NUL byte makes a text search report zero matches with no warning, which would make every grep-based reading in this record silently under-report</fails_when>
    <automated>node scripts/check-platform-shapes.js && node scripts/check-foundation-guards.js && node scripts/check-uat-oracles.js</automated>
    <fails_when>non-zero exit, or any of the three does not end with its all-checks-passed line</fails_when>
  </verify>
  <acceptance_criteria>
    - `deferred-items.md` has a new section under this round's own heading and NOT ONE earlier line changed, verified by a line-level diff restricted to that file.
    - Every ordinal used in an appended item is taken from the authoritative list, and any disagreement with a prior record prints both values.
    - The temp-probe and named-pipe sweep is taken after the suite run, with the mid-flight readings printed and the ordering's reason stated.
    - The working-tree residue predicate is demonstrated inert, with both the before and after readings printed.
    - `.planning/STATE.md` carries a status naming the plans executed, what closed with its numbers, what was carried with owners, the suite counts, the frozen-floor readings including both admission-freeze values, the disposition tally and the record's path.
    - The longest line length and the longest consecutive-backslash run in `.planning/STATE.md` are both measured AFTER the write and recorded; any pathological value is shortened to a pointer and recorded as a deviation.
    - The six requirement rows, the traceability table and the Phase 31 checkbox are each read and recorded verbatim, and all are unchanged.
    - No source file, no test file, no style-disposition file and no requirement document is modified by this plan.
  </acceptance_criteria>
  <done>The deferred record is appended without a single earlier line moving, the environment is proven clean by predicates taken in the order that makes them meaningful, and the state file is short and verified after its own write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this record → the ninth verification round | This document is the artifact that round starts from. A row asserting a closure nobody measured is a false premise handed forward, which is this phase's own recorded failure mode. |
| probe process → the tracked tree | A probe that writes inside the repository turns the suite into a measurement of the measurement. It has happened in this phase and produced a red suite on a clean commit. |
| a prior round's record → this round's reading | Where the two disagree, rewriting the prior record destroys the evidence that the disagreement exists. |
| the state file → every guard that reads it | A pathological line in the state file has previously combined with a superlinear guard predicate to turn a sub-second gate into a multi-minute one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-44-01 | Repudiation | A disposition row asserting a closure this round did not measure | high | mitigate | Every CLOSED row must cite a reproduction row taken in Task 1 at the original document's own spelling; the coverage equality is measured on both sides; a reading that could not be taken says so in its own row. |
| T-31-44-02 | Spoofing | A finding set derived from a heading scan that counts a heading inside a fenced transcript | high | mitigate | Fenced blocks are stripped before the heading scan, the stripping is stated in the document, and the derived member list is printed so a phantom is visible. |
| T-31-44-03 | Tampering | Probe residue inside the tracked tree | high | mitigate | Every probe runs under a scratch directory outside the tree; the residue predicate is demonstrated able to observe a change rather than assumed inert; the temp and named-pipe sweep is taken after the suite with mid-flight readings printed. |
| T-31-44-04 | Denial of Service | A pathological line in the state file meeting a superlinear guard predicate | medium | mitigate | The longest line and the longest consecutive-backslash run are measured after the write and recorded; a pathological value is shortened to a pointer and the deviation recorded. |
| T-31-44-05 | Information Disclosure | The record quoting probe transcripts | low | accept | Transcripts contain temp paths and repository-relative source paths only; no credential is read or printed by any probe in this plan. |
| T-31-44-06 | Elevation of Privilege | A requirement or phase checkbox flipped by an executing round | high | mitigate | This plan writes no source and flips no box; the six requirement rows, the traceability table and the phase checkbox are read and recorded verbatim, and a working-tree reading over the requirement documents is a verify gate. |
| T-31-44-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and adds no dependency; `package.json` is not in `files_modified`. No legitimacy checkpoint is required because no install task exists. |
</threat_model>

<verification>
- One commit, pinned and stated, with the platform, the Node version, the round base and the commit count over the round.
- Every recorded reproduction and every `missing:` bullet re-driven at the original document's own spelling, with pre-fix and post-fix readings and all-root counts wherever the original had them.
- CR-17 through CR-25 re-driven as controls, each marked RE-DRIVEN or ACCEPTED with its reason and standing evidence.
- The whole excluded-e2e suite measured with the exact command and its counts recorded verbatim; every frozen floor re-measured with the admission freeze's prior value printed beside the new one; the decider manifest checked whole.
- A disposition per derived item, with the coverage equality measured on both sides and fenced blocks stripped before the item scan.
- A residual register covering every boundary the round left open; the three standing human-verification items carried unchanged.
- The deferred record appended without an earlier line moving; the environment proven clean by predicates demonstrated able to observe a change; the state file short and verified after its own write.
- No requirement row, traceability row or phase checkbox moved.
</verification>

<success_criteria>
- The ninth verification round can start from this record rather than rediscovering a probe.
- No row in it asserts a closure this round did not measure.
- No finding, anti-pattern row, `missing:` bullet or owner-bearing deferred item is without a disposition.
- No probe artifact exists inside the tracked tree at the end of the round.
- UATX-01 through UATX-06 remain unchecked; the phase remains In Progress.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-44-SUMMARY.md` when done, recording: the
pinned commit and the round base with the commit count; the reproduction table's row count and how
many rows moved; the control table's RE-DRIVEN and ACCEPTED counts; the suite counts and exit status;
every frozen-floor reading; the three disposition-coverage numbers; the residual register's entry
count; the state-file line and backslash measurements; and the verbatim state of the six requirement
rows, the traceability table and the phase checkbox.
</output>
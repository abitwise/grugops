---
phase: 31-autonomous-manual-testing
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/context-io-writer-set.test.ts
  - agent-factory/workflows/17-task-claim.md
  - agent-factory/workflows/18-context-compaction.md
  - .planning/phases/31-autonomous-manual-testing/31-01-SUMMARY.md
autonomous: true
requirements: [UATX-01, UATX-04]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An artifact-ref whose sha is not the HEAD the named gate run was performed at is refused, and nothing is written (UATX-04, D-03). This is gap 1's truth restated verbatim from 31-VERIFICATION.md so the re-verifier can match it."
    - "The refusal is reached from EVERY exported note writer, not only from admit() and admitAndAppend: appendNote() — the writer agent-factory/workflows/17-task-claim.md and agent-factory/workflows/18-context-compaction.md name by name — routes a note whose kind is artifact-ref through admit() before it writes anything."
    - "There is still exactly ONE implementation of the predicate. appendNote gains NO sha comparison, NO gate_run lookup and NO verdict read of its own; it calls the single authority and refuses on its findings, which is what D-03 means by one authority per predicate."
    - "The live bypass the verifier reproduced is closed at its own coordinates: appendNote(\"verify-repro-task\", { kind: \"artifact-ref\", gate_run: \"no-such-gate-run-ever-existed\", sha: <hex>, content_hash: <hex> }, ...) against the committed scripts/context-io.js throws a named refusal and the task's notes directory gains zero files."
    - "The note-writer set is DERIVED from scripts/context-io.ts by the TypeScript AST — every exported function that transitively reaches writeNoteFile — and its COUNT is asserted, so a writer added later cannot land ungated and silently short the contract test."
    - "Every derived writer is individually exercised with the fabricated-provenance artifact-ref and individually observed to refuse or to be structurally incapable of authoring that kind; the exercise table's key set is asserted equal to the derived set, so a new writer with no driver turns the suite red rather than being skipped."
    - "The refusal has been WATCHED FAIL: a mirrored copy of the committed scripts/context-io.js with the admit() call in appendNote neutralized writes the fabricated artifact-ref, proving the live refusal is caused by the new wiring and not by an unrelated check."
    - "The three exported reachability facts are written down rather than implied: the count of appendNote call sites under scripts/, hooks/ and install/; the count of appendNote mentions in agent-factory/ prose; and atomicWrite as the named, documented residual — an exported general-purpose file writer that is not a note writer and that a hand-authored context path could still reach."
    - "31-01-SUMMARY.md's residual claim that the appendNote route 'fails closed' is corrected by an APPENDED, dated correction note; no existing line of that summary is rewritten, because the record of what was believed at the time is itself evidence (D-01, D-02, D-03)."
  artifacts:
    - scripts/context-io.ts
    - scripts/context-io.js
    - scripts/context-io.test.ts
    - scripts/context-io-writer-set.test.ts
    - agent-factory/workflows/17-task-claim.md
    - agent-factory/workflows/18-context-compaction.md
  key_links:
    - "appendNote() -> admit()'s D-03 branch: the link 31-VERIFICATION.md records as `expected but absent`. This plan wires it, and the wiring is the only change to appendNote's decision surface."
    - "admitAndAppend() -> appendNote(): the repoRoot the outer admit() used is threaded into the inner call, so the two admissions cannot resolve the governance dial against different roots."
    - "compactor.promote() -> appendNote(): promotion stays a thin pass-through with no forked writer, so a promoted artifact-ref is now bound by the same authority as a freshly authored one."
    - "the derived writer set (TypeScript AST over scripts/context-io.ts) <-> the exercise table in scripts/context-io-writer-set.test.ts: asserted equal, and the count asserted, so the set cannot rot while green."
  prohibitions:
    - statement: "appendNote must never grow a second implementation of the sha-versus-verdict comparison; it may only consult the single authority and refuse on its findings."
      status: unverified
      verification: flagged
    - statement: "A refusal must never leave a partial or zero-length note file behind; the refusal is decided before writeNoteFile is reached."
      status: unverified
      verification: flagged
    - statement: "No existing line of 31-01-SUMMARY.md may be rewritten; a correction is appended and dated."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-01-PLAN.md: the evidence path must never report or record a skipped, refused or unrecordable verification as a pass."
    - "31-01-PLAN.md: content_hash must never be described as tamper-proof, as a security token, or as anything other than a recomputable integrity digest."
---

<objective>
Close gap 1 of 31-VERIFICATION.md: the D-03 evidence-binding refusal exists and is correct, but it
is unreachable from `appendNote()` — the writer two shipped workflows name as the sanctioned path.
The verifier reproduced this live: a note naming `gate_run: "no-such-gate-run-ever-existed"` was
written and an id returned, with no refusal. This plan makes the single authority reachable from
every exported writer, proves reachability with a contract test whose writer set is derived from the
module rather than typed out, watches that test fail, and corrects the false residual claim in
31-01-SUMMARY.md.

Purpose: this is the phase's core goal failing in the register the project's own doctrine calls
blocking — an agent-controllable write path can put a fabricated `gate_run` and `sha` pair into the
shared verified context wearing the shape of bound evidence. Requirements UATX-01 and UATX-04 are
blocked on it.

Output: a `scripts/context-io.ts` (and its committed `.js`) whose `appendNote` routes an
`artifact-ref` through `admit()` and adds no predicate of its own; a derived-set contract test; two
corrected workflow documents; and an appended, dated correction note on 31-01-SUMMARY.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@scripts/context-io.ts
@agent-factory/contracts/context-note.md
</context>

## Artifacts this phase produces

**New in this plan:**

| Symbol / file | Kind | Home |
|---|---|---|
| `scripts/context-io-writer-set.test.ts` | new test file | the derived-writer-set contract |
| `deriveNoteWriters(rootDir)` | test-local function | `scripts/context-io-writer-set.test.ts` |
| `EXPECTED_NOTE_WRITERS` | test-local frozen array | `scripts/context-io-writer-set.test.ts` |
| `EXPECTED_NOTE_WRITER_COUNT` | test-local constant | `scripts/context-io-writer-set.test.ts` |
| `WRITER_EXERCISES` | test-local record keyed by writer name | `scripts/context-io-writer-set.test.ts` |
| `NON_NOTE_WRITER_RESIDUALS` | test-local frozen array naming `atomicWrite` | `scripts/context-io-writer-set.test.ts` |
| `repoRoot` parameter (6th, optional, defaults to `ROOT`) | signature addition | `appendNote` in `scripts/context-io.ts` |
| `## Correction — <date>` section | appended doc section | `.planning/phases/31-autonomous-manual-testing/31-01-SUMMARY.md` |

**Modified:** `appendNote` in `scripts/context-io.ts`; `admitAndAppend`'s two `appendNote` call
sites; `agent-factory/workflows/17-task-claim.md` step 4; `agent-factory/workflows/18-context-compaction.md`
step 4, its stop-condition bullet and its closing statement.

<assumptions>
## Edge-probe items lifted into this plan (specless fallback §C)

The phase has no SPEC. The edge-probe report carries 8 unresolved items; none is silently dropped.
This plan accounts for two of them and names where the rest are handled.

- **UATX-01 (unclassified)** — authored above as a `must_haves.truths` entry: the impersonation
  refusal holds AND the evidence-binding route is now reachable from the documented writer, which is
  the half of UATX-01 that 31-VERIFICATION.md marked PARTIAL.
- **UATX-04 (unclassified)** — authored above as gap 1's verbatim truth plus the reachability truth.
- **UATX-03 (unclassified)** — covered by 31-04 (`scripts/chrome-lane-bar.test.ts`, verified ✓ in
  31-VERIFICATION.md). Surfaced here, not re-planned.
- **UATX-05 (unclassified)** — covered by 31-02 (both absent-markers, single emission points,
  verified ✓). Surfaced here, not re-planned.

The remaining four items (UATX-02 adjacency, UATX-02 empty, UATX-02 ordering, UATX-06 unclassified)
are accounted for in 31-06 and 31-07. Total: 8 = 4 authored across the new plans + 4 surfaced.

## Explicitly flagged assumptions

- **A1.** `admit()` does not call `appendNote` or any note writer, so routing `appendNote` through
  `admit()` introduces no recursion. Task 1 confirms this by grep before the wiring lands rather
  than assuming it.
- **A2.** `isGatedNote` returns false for every kind other than `finding`, so an `artifact-ref` can
  never take `admitAndAppend`'s gated branch (which deliberately does not call `admit()`). Task 1
  asserts this for every governance dial value rather than reading it off the source once.
- **A3.** `scripts/compactor.ts`'s `promote()` and `scripts/check-uat-oracles.ts`'s `equivDoWork()`
  are the only non-test external callers of `appendNote`; neither authors an `artifact-ref` today.
  Task 2 measures the call-site count instead of restating this sentence.
</assumptions>

<assumption_delta_decision>
**Noun:** the UAT evidence lane. **Signal:** the ROADMAP success criterion calls the Claude-in-Chrome
lane "optional", which the detector reads as a pluralization/optionality delta. **Decision:**
no-change. Two lanes already exist as distinct modelled things — the Playwright lane that can reach a
gate stamp and the attended lane that structurally cannot (D-09, verified ✓ by
`scripts/chrome-lane-bar.test.ts`). This gap-closure round adds no lane, promotes no lane, and moves
no default. **Rationale:** the optionality is already a first-class distinction in the model rather
than a flag on one lane, so there is nothing to promote.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED-first — reproduce the live appendNote bypass, then route an artifact-ref through the single authority</name>
  <files>scripts/context-io.test.ts, scripts/context-io.ts, scripts/context-io.js</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the `gaps:` entry whose
      truth is "An artifact-ref whose sha is not the HEAD the named gate run was performed at is
      refused, and nothing is written", its `reason`, its `artifacts` block and its `missing` list;
      and the "Behavioral Spot-Checks" row that reproduced the write.
    - `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` — D-01, D-02 and D-03 in the
      "Evidence note shape and provenance" block. D-03 is the constraint that shapes the whole fix.
    - `scripts/context-io.ts` — `appendNote` (~L1020-1073), `admit` (~L1587-1690, in particular the
      `artifact-ref` branch), `admitAndAppend` (~L2520-2600), `emitVerdict` (~L1212), `emitTrusted`,
      `emitCheckpointNote` (~L1422), `writeNoteFile`, `validate`, `composeNote`, `noteId`, and the
      `ROOT` / `DEFAULT_CONTEXT_ROOT` constants.
    - `scripts/context-io.test.ts` — the module-import preamble (~L52), the `appendNote` refusal
      cases (~L240-290, ~L2549-2580) and the `admitAndAppend` governance-dial cases (~L2337-2400)
      for the harness idioms and the temp-root conventions this task reuses.
    - `scripts/compactor.ts` — `promote()` (~L606-616), the pass-through this change makes stricter.
  </read_first>
  <behavior>
    - RED 1: `appendNote(task, { kind: "artifact-ref", gate_run: "no-such-gate-run-ever-existed",
      sha: <40 lowercase hex>, content_hash: <64 lowercase hex>, ... }, body, tempContextRoot)`
      currently RETURNS a note id. After the fix it throws, and the refusal text names the
      `gate_run` and states that nothing was written.
    - RED 2: the task's notes directory currently gains exactly one file on that call. After the fix
      its file count is unchanged from before the call.
    - RED 3: a note whose `gate_run` names a REAL live green verdict but whose `sha` differs from
      the verdict's recorded sha is refused through `appendNote`, and the refusal names BOTH shas —
      because the message comes from the single authority, not from a message composed here.
    - GREEN 1: an `artifact-ref` whose `gate_run` names a real live green verdict and whose `sha`
      equals that verdict's recorded sha is WRITTEN through `appendNote`, and the returned id is the
      filename on disk. The fix must not close the legitimate path.
    - GREEN 2: notes of the other five kinds are unaffected — `claim`, `finding`, `decision`,
      `failed-attempt` and `observation` writes through `appendNote` behave byte-identically, and
      the composed bytes of an existing note are unchanged.
    - GREEN 3: `admitAndAppend` still admits and still refuses exactly as before for every kind,
      with the outer and inner admissions resolving governance against the same `repoRoot`.
  </behavior>
  <action>
    Work in three ordered movements. Do not write the fix before the failing test exists and has
    been observed failing — this gap exists because a green suite was accepted as proof.

    MOVEMENT 1 — reproduce, and record the pre-fix output.
    Add a describe block to `scripts/context-io.test.ts` titled for gap 1 of Phase 31 that
    reproduces the verifier's exact call shape: task name `verify-repro-task`, kind `artifact-ref`,
    `gate_run` set to the literal `no-such-gate-run-ever-existed`, a well-formed lowercase-hex `sha`
    and a 64-character lowercase-hex `content_hash`, written into a fresh temp context root that
    contains NO verdict at all. Assert that the call throws and that the notes directory file count
    is unchanged. Run only that file and capture the failure text verbatim into the SUMMARY — the
    pre-fix output is the evidence that the test discriminates.

    Before touching `appendNote`, confirm assumption A1 mechanically: grep `admit`'s body for a call
    to any note writer and record the count. A count above zero means the wiring below would recurse
    and the design must change before proceeding.

    MOVEMENT 2 — wire the single authority in, and add no second one.
    Give `appendNote` a sixth parameter `repoRoot: string = ROOT`, placed after `precomputedId` so
    every existing call site stays byte-compatible. After the existing `validate(text)` refusal and
    BEFORE `writeNoteFile` is reached, add one branch: when `note.kind` is `artifact-ref`, call
    `admit(task, text, contextRoot, repoRoot)` and, on a non-empty findings array, throw an error
    that names the module and function and then reproduces the authority's findings joined by
    newlines — the same shape the existing `validate` refusal above it uses.

    This branch contains no comparison of its own. It does not read a verdict, does not look up a
    `gate_run`, does not touch `sha`. Per D-03 the comparison lives in `admit()` and only there; the
    defect being closed is reachability, so the fix is a call, not a check. Placing it after
    `composeNote` and `validate` and before `writeNoteFile` is what makes "nothing is written" true
    by construction rather than by cleanup.

    Thread the root: in `admitAndAppend`, pass `repoRoot` into BOTH `appendNote` calls (the gated
    branch and the non-gated branch) so the inner admission resolves the governance dial against the
    same root the outer one used. Without this, a test that passes a temp `repoRoot` would have its
    inner admission silently read the real repository.

    Confirm assumption A2 with a test rather than a reading: for every value the governance dial
    admits — absent, `off`, `high-severity`, `all`, and an unrecognized value — assert
    `isGatedNote(by, "artifact-ref", configResult)` is false, so an `artifact-ref` can never take the
    gated branch that deliberately skips `admit()`.

    MOVEMENT 3 — prove the legitimate path still opens.
    Seed a real green verdict through `emitVerdict` into a temp context root, then write a matching
    `artifact-ref` through `appendNote` and assert it is persisted with the returned id present on
    disk. Then mutate only the `sha` and assert the refusal names both shas. Re-run the sibling
    suites that import this module — `scripts/compactor.test.ts`, `scripts/admission-server.test.ts`,
    `scripts/floor-invariance.test.ts`, `scripts/convergence-spine.test.ts` and
    `scripts/worktree-dogfood.test.ts` — because `promote()` is a pass-through and this change makes
    it stricter for one kind.

    Rebuild and commit the `.ts` and its committed `.js` twin together; a committed `.js` that is not
    a build of its `.ts` is the freshness gate's failure, not a formatting nit.
  </action>
  <reversibility rating="reversible">Routing one kind through an existing authority is a call site; removing it restores the prior behavior exactly, and no data shape, on-disk format or public signature is changed destructively (the new parameter is optional and defaulted).</reversibility>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any non-zero "failed" count</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/compactor.test.ts scripts/admission-server.test.ts scripts/floor-invariance.test.ts scripts/convergence-spine.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any non-zero "failed" count</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or the output contains "BUILD PARITY FAILED"</fails_when>
    <automated>npm run freshness</automated>
    <fails_when>non-zero exit, or the output names any committed .js as stale</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'admit(task, text, contextRoot, repoRoot)' scripts/context-io.ts` is at least 1, and
      the match sits inside `appendNote`'s body (confirm by line number against `appendNote`'s
      opening and closing lines).
    - Counting only non-comment lines, `appendNote`'s body contains no occurrence of `verdictSha`,
      of `isLiveGreenVerdict` and of `matches.length`: run
      `sed -n '<start>,<end>p' scripts/context-io.ts | grep -v '^\s*//' | grep -c 'isLiveGreenVerdict'`
      over `appendNote`'s line range and require 0. The comparison stays in one place.
    - `grep -c 'repoRoot: string = ROOT' scripts/context-io.ts` is at least 2 (`admit` and
      `appendNote` both carry the defaulted root).
    - The pre-fix failure output of the new describe block is quoted verbatim in the SUMMARY,
      including the returned note id the verifier saw.
    - The grep confirming assumption A1 is recorded in the SUMMARY with its numeric result.
    - `npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts` reports 0 failed.
    - `npm run freshness` reports every committed `.js` fresh.
  </acceptance_criteria>
  <done>The verifier's exact live bypass throws a named refusal and writes nothing; a legitimately bound artifact-ref still writes; `appendNote` carries no second implementation of the D-03 predicate; the committed `.js` is a fresh build of its `.ts`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the writer-set contract test — derive the set, assert the count, exercise every member, watch it fail</name>
  <files>scripts/context-io-writer-set.test.ts</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the gap 1 `missing` bullet
      requiring a contract test "derived from the module's exports rather than a hand-typed list".
    - `scripts/chrome-lane-bar.test.ts` — the pattern this file replicates end to end: a live
      derivation, an asserted member set, an asserted COUNT, a seeded-fixture watched-fail built
      FROM the live derivation (see its PART TWO, ~L322-360), and a named-remainder assertion that
      turns a silence into a written-down residual (~L300-320).
    - `scripts/context-io.ts` — the exported surface (`appendNote`, `emitVerdict`,
      `emitCheckpointNote`, `admitAndAppend`, `atomicWrite`, `render`) and the private
      `writeNoteFile` / `emitTrusted` tails that define what "writer" means here.
    - `scripts/check-foundation-guards.test.ts` — `mirror()` (~L526) and `runIn()` (~L3180) for the
      hermetic temp-mirror idiom used by the watched-fail below.
  </read_first>
  <behavior>
    - The derivation returns exactly the exported functions of `scripts/context-io.ts` that
      transitively reach `writeNoteFile`, sorted, and its length equals the asserted count.
    - Adding one exported function to a MIRRORED copy of the module that reaches `writeNoteFile`
      moves the derived set and moves the count by exactly one.
    - The exercise table's key set deep-equals the derived set; a writer with no driver is red.
    - Driving each writer with the fabricated-provenance artifact-ref produces either a refusal with
      an unchanged notes-directory file count, or — for a writer whose composed kind is a source
      literal that is not `artifact-ref` — a positively asserted structural impossibility.
    - Neutralizing the authority call in a mirrored copy of the committed `.js` makes the
      fabricated artifact-ref WRITE, so the refusal is watched failing.
  </behavior>
  <action>
    Create `scripts/context-io-writer-set.test.ts`. It imports `typescript` (already a
    devDependency) to parse `scripts/context-io.ts`, and imports the committed
    `scripts/context-io.js` for behavior, matching the preamble idiom at the top of
    `scripts/context-io.test.ts`.

    PART ONE — derive the set. Write `deriveNoteWriters(sourcePath)`: parse the file into a
    SourceFile, collect every top-level function declaration by name along with the set of
    identifiers it calls, then compute the transitive closure of "calls" from each EXPORTED function
    and report those exported names whose closure contains `writeNoteFile`. Sort the result. Assert
    it equals `EXPECTED_NOTE_WRITERS` and that its length equals `EXPECTED_NOTE_WRITER_COUNT`. Put
    the expected members and the count in two separate constants so a member change and a count
    change are two distinct failures with two distinct messages.

    Guard the derivation's own premise before trusting it, the way `livePin()` in
    `scripts/check-foundation-guards.test.ts` does: assert the parsed file yielded a non-zero number
    of top-level function declarations and a non-zero number of exported ones. A derivation that
    silently parsed nothing would return an empty set that trivially satisfies nothing, and the
    project has recorded a false verification-harness premise six times across four rounds.

    PART TWO — the derivation discriminates. Build a temp mirror containing a copy of
    `scripts/context-io.ts` with ONE extra exported function appended that calls `writeNoteFile`.
    Assert the derivation over the mirror contains that function's name, does not equal
    `EXPECTED_NOTE_WRITERS`, and has length exactly `EXPECTED_NOTE_WRITER_COUNT` plus one — the last
    clause proves the refusal was caused by the seeded function rather than by a derivation that
    broke and started reporting some other number.

    PART THREE — exercise every member. Declare `WRITER_EXERCISES` as a record keyed by writer name
    whose value drives that writer against a fresh temp context root with the verifier's
    fabricated-provenance payload: `gate_run` set to `no-such-gate-run-ever-existed`, a well-formed
    lowercase-hex `sha`, a 64-character lowercase-hex `content_hash`. Assert
    `Object.keys(WRITER_EXERCISES).sort()` deep-equals the derived set BEFORE running any driver, so
    a writer that landed without a driver is red rather than skipped. Then run each driver and
    assert, per writer, that the notes-directory file count before and after the attempt is equal
    and that a refusal was produced.

    Two of the writers cannot express the kind at all — their composed note's `kind` is a source
    literal. For those, the driver asserts the impossibility POSITIVELY: extract the `kind:` literal
    from that function's body in the parsed source and assert it is `finding`, and record in a
    comment beside it that this is a structural impossibility rather than a behavioral refusal.
    Never leave such a member unexercised; an unexercised member is exactly the silence this test
    exists to remove.

    PART FOUR — watch the refusal fail. Copy the committed `scripts/context-io.js` into a temp
    directory, textually neutralize the authority call inside `appendNote` (replace the call
    expression with an empty-array literal so the findings check cannot fire), import the mutated
    copy, and assert the fabricated artifact-ref IS written there. A structural assertion nobody has
    watched fail is not yet a control.

    PART FIVE — write the reachability facts down. Compute and assert three numbers rather than
    describing them: the number of `appendNote` call sites in tracked non-test sources under
    `scripts/`, `hooks/` and `install/`; the number of files under `agent-factory/` whose prose
    mentions `appendNote`; and the members of `NON_NOTE_WRITER_RESIDUALS`, a frozen array whose sole
    member is `atomicWrite` — an exported general-purpose file writer that does not reach
    `writeNoteFile` and therefore is not a note writer, but which a hand-authored context path could
    still reach. Assert the derived non-writer exported-function remainder equals that array plus
    the non-writing readers, and name the residual in a comment so it is disclosed rather than
    implied.
  </action>
  <reversibility rating="reversible">A test file adds no production surface; deleting it restores the prior state exactly.</reversibility>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io-writer-set.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any non-zero "failed" count</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or tsc emits any TS error line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'EXPECTED_NOTE_WRITER_COUNT' scripts/context-io-writer-set.test.ts` is at least 3
      (declaration, live assertion, discrimination assertion).
    - The file contains no array literal of writer names other than `EXPECTED_NOTE_WRITERS` and
      `NON_NOTE_WRITER_RESIDUALS`; the exercised set comes from `Object.keys(WRITER_EXERCISES)` and
      the live set comes from `deriveNoteWriters`.
    - The suite reports at least 8 passing tests in this file, including one named for the
      count-moves-by-one discrimination and one named for the neutralized-authority watched-fail.
    - The premise assertions (non-zero parsed function count, non-zero exported count) are present
      and each carries a message beginning with `PREMISE`.
    - The number of `appendNote` call sites under `scripts/`, `hooks/`, `install/` and the number of
      `agent-factory/` files mentioning `appendNote` are both asserted numerically and both quoted
      in the SUMMARY.
    - `atomicWrite` appears in `NON_NOTE_WRITER_RESIDUALS` and is described in an adjacent comment
      as a disclosed residual.
  </acceptance_criteria>
  <done>The writer set is derived from the module and its count asserted; every member is exercised or positively proven structurally incapable; the derivation and the refusal have each been watched fail; the reachability remainder is written down rather than left silent.</done>
</task>

<task type="auto">
  <name>Task 3: correct the two workflows and append the dated correction to 31-01-SUMMARY.md</name>
  <files>agent-factory/workflows/17-task-claim.md, agent-factory/workflows/18-context-compaction.md, .planning/phases/31-autonomous-manual-testing/31-01-SUMMARY.md</files>
  <read_first>
    - `agent-factory/workflows/17-task-claim.md` — step 4 (~L40), the sentence naming `appendNote`
      as the sanctioned write path.
    - `agent-factory/workflows/18-context-compaction.md` — step 4 (~L51), the stop-condition bullet
      (~L60) and the closing statement (~L69), all three of which name `appendNote`.
    - `agent-factory/workflows/16-context-read-write.md` — the two accepted stamps and the
      finding-only stamp rule; this file already routes through `admitAndAppend` and must stay
      consistent with the corrected sentences rather than be re-stated.
    - `agent-factory/contracts/context-note.md` — the six-kind schema and the `artifact-ref`
      provenance fields, so the corrected prose names the same fields the contract does.
    - `.planning/phases/31-autonomous-manual-testing/31-01-SUMMARY.md` — the residual section
      (~L305) carrying the claim that both routes fail closed.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the Anti-Patterns table
      row that classifies that residual claim under the no-fabrication rule.
  </read_first>
  <action>
    Correct the shipped prose so the documented sanctioned path matches the mechanism Task 1
    shipped. `appendNote` remains the single sanctioned writer — that claim was and stays true — and
    the sentences gain the fact that was missing: a note whose kind is `artifact-ref` is now bound by
    the admission authority at write time, so a `gate_run` and `sha` pair naming no live green
    verdict is refused there rather than persisted. Per D-03 the binding is decided in one place and
    the prose points at that place instead of restating the rule.

    In `agent-factory/workflows/17-task-claim.md` step 4, extend the sentence that names the
    sanctioned writer with the evidence clause: evidence pointing at a committed spec goes in as an
    `artifact-ref` carrying `sha`, `gate_run` and `content_hash`, and it is admitted only against a
    live green verdict for that run. Keep the existing pointer to WF16 for the admission rules; do
    not restate them here.

    In `agent-factory/workflows/18-context-compaction.md`, correct all three sites. Step 4 gains the
    consequence for promotion: a promoted `artifact-ref` is re-bound by the same authority, so one
    whose gate run is absent from the destination context is REFUSED, and the honest disposition is
    to degrade it — the same posture the closing statement already prescribes for a `finding` that
    cannot re-admit. Extend the stop-condition bullet and the closing statement to name
    `artifact-ref` alongside `finding` so the two kinds carry one rule between them.

    Write in the Phase 29 clear professional profile — this is a safety surface, so the caveman voice
    does not apply here. Keep the edits to the smallest span that makes the claim true; a widened
    rewrite would put the language guards to work on text this plan has no reason to touch.

    Then append — never rewrite — a correction note to
    `.planning/phases/31-autonomous-manual-testing/31-01-SUMMARY.md`. Add a new final section headed
    with the word Correction and today's date. It states plainly: the residual paragraph's claim that
    the `appendNote` route fails closed was WRONG when written; 31-VERIFICATION.md reproduced a
    written note with a fabricated gate run; the route is closed as of this gap-closure plan by
    routing the kind through the admission authority; and the original paragraph is left in place
    because a record of what was believed at the time is itself part of the trail. Cite the
    verification file and the reproducing command. Do not touch any existing line of that file — the
    correction is additive.

    Finally, report the reachability enumeration Task 2 computed: how many `appendNote` call sites
    exist in code, how many `agent-factory/` documents mention it, and which of them this task
    changed. A count that moved without a corresponding edit is a document this task missed.
  </action>
  <reversibility rating="reversible">Prose edits and an appended section; both revert cleanly and neither changes a shipped default, a schema or an interface.</reversibility>
  <verify>
    <automated>npm run check:imperative-lexicon &amp;&amp; npm run check:banned-claims &amp;&amp; npm run check:public-docs &amp;&amp; npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or any of the four fails to print "ALL CHECKS PASSED"</fails_when>
    <automated>node scripts/validate-agent-factory.js</automated>
    <fails_when>non-zero exit, or the output reports any structural failure row</fails_when>
    <automated>node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit, or the final line is not "ALL CHECKS PASSED"</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/validate.test.ts scripts/check-foundation-guards.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any non-zero "failed" count</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'artifact-ref' agent-factory/workflows/17-task-claim.md` is at least 1.
    - `grep -c 'artifact-ref' agent-factory/workflows/18-context-compaction.md` is at least 3, one
      for each of the three corrected sites.
    - `grep -c 'appendNote' agent-factory/workflows/17-task-claim.md` is still 1 and
      `grep -c 'appendNote' agent-factory/workflows/18-context-compaction.md` is still 3 — the
      sanctioned-writer claim is extended, not replaced.
    - The last section of `.planning/phases/31-autonomous-manual-testing/31-01-SUMMARY.md` is a
      heading whose text begins with `Correction` and contains today's date in ISO form.
    - `git diff --stat -- .planning/phases/31-autonomous-manual-testing/31-01-SUMMARY.md` shows
      insertions only and zero deletions.
    - The four `check:` scripts, `validate-agent-factory.js` and `check-foundation-guards.js` all
      exit 0.
    - The SUMMARY records the `appendNote` call-site and prose-mention counts and states which
      documents were changed.
  </acceptance_criteria>
  <done>Both workflows name the corrected sanctioned path for evidence notes, the language and structure guards stay green, and 31-01-SUMMARY.md carries an appended dated correction with no line rewritten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> the shared verified context | An agent supplies a `gate_run` and a `sha`; both are agent-controlled strings until an authority binds them to a real gate run. |
| a module-external caller -> `scripts/context-io.ts` | `scripts/compactor.ts` and `scripts/check-uat-oracles.ts` call the writer directly; anything importable is reachable. |
| workflow prose -> agent behaviour | The documented sanctioned path is what an agent follows; prose naming a route that skips a safety check is a live defect, not a documentation nit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-20 | Spoofing | `appendNote` writing an `artifact-ref` with a fabricated `gate_run` | critical | mitigate | The kind is routed through `admit()` before `writeNoteFile` is reached; the refusal is exercised through every derived writer and watched failing against a neutralized mirror. |
| T-31-21 | Tampering | a writer added later that reaches `writeNoteFile` without the binding | high | mitigate | The writer set is derived by AST from the module and its count asserted; a new writer moves both and turns the contract test red before it can ship. |
| T-31-22 | Repudiation | a promoted `artifact-ref` re-entering a context where its gate run does not exist | high | mitigate | `promote()` stays a pass-through, so promotion is bound by the same authority; workflow 18 states the refusal and the honest degradation. |
| T-31-23 | Information disclosure | a refusal leaving a partial note file on disk | medium | mitigate | The branch sits after `composeNote` and `validate` and before `writeNoteFile`; the acceptance criteria assert the notes-directory file count is unchanged on every refusal. |
| T-31-24 | Repudiation | a summary residual asserting a disposition the measurement contradicts | medium | mitigate | An appended dated correction, insertions only, citing the verification file and the reproducing command. |
| T-31-25 | Elevation of privilege | `atomicWrite`, an exported general-purpose file writer, reaching a notes path directly | medium | accept | It does not reach `writeNoteFile` and is not a note writer; the hand-authored-context-path route it enables is already forbidden by workflow 16 and 18. Named in `NON_NOTE_WRITER_RESIDUALS` and disclosed in the SUMMARY rather than left silent. |
| T-31-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing — no dependency is added and no npm script gains an install step. The Phase 31 RESEARCH.md Package Legitimacy Audit records no `[ASSUMED]`, `[SUS]` or `[SLOP]` package, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts scripts/context-io-writer-set.test.ts scripts/compactor.test.ts scripts/admission-server.test.ts scripts/floor-invariance.test.ts scripts/convergence-spine.test.ts` green.
- `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness` green.
- `npm run check:imperative-lexicon && npm run check:banned-claims && npm run check:public-docs && npm run check:claim-anchors` all print `ALL CHECKS PASSED`.
- `node scripts/check-foundation-guards.js` ends with `ALL CHECKS PASSED`; `node scripts/validate-agent-factory.js` exits 0.
- **Never run bare `npm test`** — it launches the live claude-CLI e2e lane against real credentials.
- **Red-team obligation (project doctrine, not optional).** This closes a safety predicate whose
  first shipped version was green and bypassable. Before this plan is called done, ask the two
  questions that found it: how is the gate REACHED (enumerate every exported write entry point, not
  only the documented one), and what is the predicate's INPUT assembled from. A green suite is not
  proof for this class.
</verification>

<success_criteria>
- The verifier's live `appendNote` bypass is refused at its own coordinates and writes nothing.
- The D-03 comparison still exists in exactly one function.
- The note-writer set is derived and counted, and every member is exercised.
- Both the derivation and the refusal have been watched fail.
- The two workflows and the 31-01 summary state what the mechanism now does.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-05-SUMMARY.md` when done.
</output>

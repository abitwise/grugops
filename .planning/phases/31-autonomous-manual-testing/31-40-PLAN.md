---
phase: 31-autonomous-manual-testing
plan: 40
type: execute
wave: 9
depends_on: ["31-39"]
gap_closure: true
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io-writer-set.test.ts
  - scripts/harness-instance-ledger.test.ts
  - docs/audit/harness-false-result-instances.md
  - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
autonomous: true
requirements: [UATX-01, UATX-04]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`31-VERIFICATION.md` round 8's THIRD `missing:` bullet is the axis that makes a ninth recurrence impossible to land silently: every call site that supplies a write path's governance-dial argument from a value other than the caller's own trusted root is ENUMERATED FROM SOURCE across files, its count is asserted, and each site's disposition is compared against the disposition the sibling write-both route takes for the identical shape. Derived, never a hand-typed list — hand-maintained scan sets and call-site lists are this repository's second named failure class and they rot while staying green."
    - "The census is asked of BOTH arguments, because CR-26 and CR-27 are the same defect on two different arguments. The DIAL axis asks which sites aim the governance dial somewhere other than the caller's trusted root; the LEDGER axis asks which sites aim the audit record somewhere other than the repository derived from their own note store. A census over one argument would have found CR-27 and missed CR-26."
    - "The census does NOT inherit the sibling order axis's tail-delegation exclusion. That axis deliberately excludes a note write that is the whole expression of a return statement, and `promoteAdmitted`'s fall-through is exactly that shape — the coordinate CR-27 lives at. An axis assembled from another axis's input is this phase's recorded failure shape (`ask what the predicate's INPUT is ASSEMBLED from`), and the absence of the exclusion is stated in the derivation's own docstring with CR-27 named as the reason."
    - "The census walk starts at the SOURCE FILE and attributes each site to its nearest named enclosing scope, never at top-level function declarations only. `WR-27` already measured that exact limitation in this module's sibling derivation: a seeded call inside an arrow, a class method and an entry block each left the count unmoved while the axis's comment claimed a new call anywhere turned it red. Three seeded mirrors, one per shape, each move the census count by exactly one."
    - "The FALLBACK SHAPE is banned by derivation, not by review attention. No call site of the owning-repository authority may be the operand of a nullish-coalescing, logical-or or conditional default expression — that shape IS CR-26 — and the ban is asserted over a set derived from the module's syntax tree with its cardinality, so the next consumer cannot re-introduce it in silence."
    - "The census is the SET half of two rules this phase already decided and keeps paying for at one register over. D-31 (2) states that two halves of one action are keyed on ONE variable, and D-34 (2) states that a property claimed of a SET is asserted over a set DERIVED from the module's AST with its cardinality. D-39 installs the one authority that answers which variable; this plan asserts the rule over the derived SET of every place that authority's answer could be overridden, which is the half neither D-31 nor D-34 covered because neither asked how many call sites could supply a root at all."
    - "Every divergence the census finds carries a DISPOSITION read from an exported register, never a literal in a test file: a site is either derived-and-refusing, or it is a ONE-HALF action that writes no audit record and therefore has no split to create, or it is a published residual with its reproduction. A fourth kind of divergence has no register entry and turns the axis red."
    - "IN-21: the ledger test's partition assertion is true by construction — its three terms are `no files`, `files but no mechanisms` and `files and mechanisms`, a partition of the row set for any input, so the equality cannot fail. A vacuity floor that cannot fail dilutes the one beside it that works. It is replaced by the counts the file actually cares about, or removed."
    - "The two harness false results `31-38`'s own deferred record raised and explicitly handed to `round 8's fix plan` are dispositioned here rather than carried a second time: the `admitAndAppend` probe whose governance dial was written at the wrong key, so every dial value produced one identical refusal and zero files and read exactly like a closure; and the suite run that went red because the session's own probe roots were still on disk. Each becomes a ledger row with its ordinal read off the table, or is recorded as deliberately not a row with the reason."
    - "UATX-01 through UATX-06 stay UNCHECKED in `.planning/REQUIREMENTS.md`, the Phase 31 checkbox stays unchecked in `.planning/ROADMAP.md`, and every traceability row still reads `Gaps Found`."
  artifacts:
    - scripts/context-io.ts
    - scripts/context-io.js
    - scripts/context-io-writer-set.test.ts
    - scripts/harness-instance-ledger.test.ts
    - docs/audit/harness-false-result-instances.md
    - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
  key_links:
    - "the DIAL-argument supply set <-> the LEDGER-argument supply set: two derived axes over the same call sites, because the two Criticals of round 8 are the same defect on two different arguments."
    - "every derived divergence <-> its disposition in an exported register: a site with no register entry turns the axis red, so a future third write-both route cannot arrive dispositionless."
    - "the owning-repository authority's call sites <-> the absence of a fallback expression at each: the shape that produced CR-26, banned over a derived set rather than watched by a reviewer."
    - "`docs/audit/harness-false-result-instances.md`'s ordinals <-> the instances this phase's own records name: one list, contiguous, with every claimed mechanism findable in the evidence the row itself cites."
  prohibitions:
    - statement: "A set asserted by this plan must be DERIVED from source at run time. A hand-typed membership list, a hard-coded count on both sides of a comparison, or a literal echoing a derived value is the set-literal drift class this axis exists to close."
      status: unverified
      verification: flagged
    - statement: "A prior SUMMARY is history and is never rewritten. A disagreement with one is ANNOTATED in the ledger, with both values printed."
      status: unverified
      verification: flagged
    - statement: "A gap-closure plan must never flip a requirement checkbox, a traceability row or the phase checkbox."
      status: unverified
      verification: flagged
    - statement: "Never fake a passing gate, a test result, or a citation. An ordinal is READ OFF the ledger table, never typed into a summary."
      status: unverified
      verification: flagged
    - statement: "Never merge a protected branch and never deploy to production without a named human confirmation."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-39-PLAN.md: the governance dial is never answered from a caller-supplied destination, and no consumer of the owning-repository authority carries a fallback of its own. This plan turns the second into a permanent derived axis."
    - "31-29-PLAN.md / WR-27: a derivation's walk starts at the source file with nearest-named-scope attribution, and each shape it newly sees is proven by a seeded mirror moving the count by exactly one."
---

<objective>
Make a ninth recurrence of this predicate family impossible to land in silence: enumerate from source
every place a write path aims the governance dial or the audit record somewhere other than the answer
its own inputs derive, assert the count, and require every divergence to carry a disposition from an
exported register.

This is `31-VERIFICATION.md` round 8's third `missing:` bullet, plus the two items `31-38`'s own
deferred record handed forward to this round by name.

Purpose: eight rounds of this phase have closed a Critical at its filed coordinate and met the next
one register over. The two behavioural fixes in `31-39` close the two coordinates round 8 measured.
This plan closes the CLASS, by making the set of places that can exhibit it a derived, counted,
dispositioned set rather than a set a reviewer happens to walk.
Output: two derived cross-file census axes with seeded non-vacuity mirrors; an exported
divergence-disposition register; a repaired vacuity floor in the ledger test; two carried harness
false results dispositioned; decision `D-40` recorded.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@.planning/phases/31-autonomous-manual-testing/31-REVIEW.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-39-SUMMARY.md
@.planning/phases/31-autonomous-manual-testing/deferred-items.md
@scripts/context-io.ts
@scripts/context-io-writer-set.test.ts
</context>

## Artifacts this phase produces

| Artifact | Kind | Note |
|---|---|---|
| `deriveRootArgumentSites(sourcePath)` | NEW derived helper in `scripts/context-io-writer-set.test.ts` | Enumerates, across the repository's tracked `.ts` sources, every call to a write-path entry point together with the EXPRESSION supplied for its dial argument and for its ledger argument, attributed to the nearest named enclosing scope. |
| `ROOT_DIVERGENCE_DISPOSITIONS` | NEW exported register in `scripts/context-io.ts` | One entry per call site that supplies a dial or ledger argument from something other than its own derived answer, carrying the site handle, the shape, the kind of divergence and the reason. Same interface shape as `WRITE_PATH_RESIDUALS`, deliberately. |
| `RootDivergenceKind` | NEW exported type | The three kinds a divergence may be: `derived-and-refusing`, `one-half-action`, `published-residual`. A fourth kind cannot be filed because there is no fourth member. |
| The dial-argument census case | NEW derived test | Members, cardinality, and each member's disposition looked up in the register. Three seeded mirrors (arrow, class method, non-top-level block) each move the count by exactly one. |
| The ledger-argument census case | NEW derived test | Same shape, over the other argument. Its docstring states why the tail-delegation exclusion is absent and names CR-27. |
| The fallback-shape ban case | NEW derived test | No call site of the owning-repository authority is the operand of a nullish-coalescing, logical-or or conditional default expression. Cardinality asserted; a seeded mirror restoring the shape moves it. |
| `docs/audit/harness-false-result-instances.md` | CHANGED audit register | Rows appended for the instances `31-38`'s deferred record handed to this round, ordinals read off the table. |
| `scripts/harness-instance-ledger.test.ts` | CHANGED test | IN-21's by-construction partition replaced by the counts the file cares about; the working floor beside it left intact. |
| `D-40` | NEW dated decision | Appended to `31-CONTEXT.md`. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The dial-argument and ledger-argument census, derived across files, counted, dispositioned</name>
  <files>scripts/context-io-writer-set.test.ts, scripts/context-io.ts, scripts/context-io.js</files>
  <precondition>`31-39` is committed, `npm run freshness` reports every committed output fresh, and `scripts/context-io.ts` already carries the owning-repository authority and the separate dial and ledger parameters this census enumerates. A census over the pre-`31-39` signature would enumerate a shape that no longer exists.</precondition>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the third `missing:` bullet verbatim, and gap 1's two `artifacts:` rows, because the census's two axes correspond exactly to the two rows.
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — CR-26 and CR-27's `Issue:` paragraphs, including CR-27's observation that `scripts/compactor.ts` forwards only six arguments so the dial is always the default trusted root while the destination is always agent-supplied. That reachability argument is what makes the census cross-file rather than module-local.
    - `scripts/context-io-writer-set.test.ts` — read in full the derivation helpers and their controls: the note-writer set derivation and its two control cases; the refusal-site derivation, its distinctness assertion and its three controls; the cross-file caller derivation for both promotion routes with aliases resolved; the filesystem-blocking-site derivation with its recursive walk and its three seeded scope mirrors; and the note-then-ledger route derivation in full INCLUDING its tail-delegation exclusion and the docstring explaining it. The new census reuses these shapes and must not re-baseline any of them.
    - `scripts/context-io.ts` as `31-39` left it — the owning-repository authority and every one of its call sites; `appendNote`, `admit`, `admitAndAppend` and `promoteAdmitted`'s signatures with their dial and ledger parameters; `WRITE_PATH_RESIDUALS` and the `WritePathResidual` interface, because the new register copies that interface shape rather than inventing a third.
    - `scripts/compactor.ts` — `promote`, `promoteAdmitted` and `reVerify` in full: three production call sites whose argument counts decide their census classification.
    - `scripts/check-uat-oracles.ts` — the two `appendNote` calls that pass a dial argument from a directory the function itself creates, and the comment above them explaining why a Tier-1 oracle decides admission against a root it owns. This is a LEGITIMATE divergence and the census must classify it rather than refuse it.
    - `scripts/admission-server.ts` — the `admitAndAppend` call site and the values it passes, because the review cites it as the production shape where the two arguments agree today.
    - `scripts/check-platform-shapes.ts` — the in-child driver that BUILDS a write-path call as the text of a temp module and passes a dial argument that is neither absent nor a trusted-root call. A syntax-tree census over call expressions cannot see this site, because on this tree it is a string. It is the concrete instance of the boundary this plan's threat register accepts, and it must be NAMED in the register and in the decision rather than left for round 9 to find.
  </read_first>
  <behavior>
    - PREMISE: the parse found call sites at all. A census reporting zero sites would report green over nothing, which is instance 4 and instance 14 of this phase's own false-result ledger. The site count is printed and floored before any conclusion.
    - The DIAL axis enumerates every call to a write-path entry point whose dial argument is supplied by an expression, together with that expression's text and its nearest named enclosing scope. Members asserted, cardinality asserted separately.
    - The LEDGER axis enumerates the same call sites by their ledger argument, with the tail-delegation exclusion ABSENT. `promoteAdmitted`'s fall-through must appear as a member — a census that excludes it is the census that would have missed CR-27.
    - Every enumerated divergence has an entry in the exported register, looked up by site handle. A site with no entry fails the case, naming the site.
    - The register's kind vocabulary is closed at three members, and every entry's kind is one of them. A divergence that is none of the three cannot be filed, which is the point.
    - The CONVERSE direction: every register entry names a site the census actually finds. A register entry for a site that no longer exists fails, so a deleted call site cannot leave a stale disposition behind.
    - The FALLBACK ban: no call site of the owning-repository authority is the operand of a nullish-coalescing, logical-or or conditional default expression. Asserted over the derived call-site set with its cardinality, so the ban is over the set rather than over the two sites a reviewer read.
    - Three SEEDED SCOPE MIRRORS prove the walk sees more than top-level functions: a seeded call site inside an arrow function, inside a class method, and inside a non-top-level block each move the census count by exactly one, and the top-level control also moves it by exactly one. Each mirror's anchor is asserted to occur exactly once before the substitution, so a mirror that substituted nothing cannot report a pass.
    - A SEEDED FALLBACK MIRROR restoring a default expression at one authority call site moves the fallback-ban case from pass to fail, and the mirror is confirmed different from the live source before any result is read.
    - A SEEDED DISPOSITIONLESS MIRROR adding a new diverging call site with no register entry moves the disposition case from pass to fail, naming the new site.
  </behavior>
  <action>
    Write the two axes in `scripts/context-io-writer-set.test.ts`, in a new part beside the existing
    derived axes, and the register in `scripts/context-io.ts`.

    THE DERIVATION. Build one helper that, for a given tracked `.ts` source, parses it and walks from
    the SOURCE FILE — not from top-level function declarations — collecting every call whose callee
    resolves to a write-path entry point, with aliases resolved the way the existing cross-file caller
    derivation already resolves them. For each call record: the nearest named enclosing scope, the
    expression supplied for the dial argument, the expression supplied for the ledger argument, and
    whether the call is the whole expression of a return statement. Derive the file set from
    `git ls-files` restricted to `.ts` sources rather than from a typed list of filenames; assert the
    file count so a silently short scan is red. State in the helper's docstring that the
    tail-delegation flag is RECORDED but NOT used to exclude anything on the ledger axis, and name
    CR-27 as the reason: the excluded shape is precisely where the defect lived.

    THE CLASSIFICATION. A site DIVERGES on the dial axis when its dial argument is neither absent nor
    the enclosing scope's own dial parameter nor the trusted-root call. A site DIVERGES on the ledger
    axis when its ledger argument is neither absent nor the derived owner of that call's own store
    expression. Write the rule as a positive statement of what agreement looks like, so a new shape is
    a divergence by default and has to earn an entry rather than slipping through an inverted match.

    THE REGISTER. Add `ROOT_DIVERGENCE_DISPOSITIONS` to `scripts/context-io.ts`, copying the
    `WritePathResidual` interface shape — a stable id, the shape stated as a situation, the reason as
    an argument rather than an assurance, and what would force it closed — plus the divergence kind
    and the site handle the census produces. Close the kind vocabulary at three members with an
    exported type: a site that derives its answer and refuses when it cannot name one; a site whose
    action writes no audit record at all and therefore has one half rather than two; and a site
    published as a residual with its reproduction. Populate it from what the census actually finds on
    this tree, one entry per diverging site, with the reason written at the entry — including the two
    oracle call sites that deliberately aim the dial at a root the oracle itself creates and removes,
    whose reason is already written at their site and must be quoted rather than paraphrased.

    THE ASSERTIONS. Assert the member set and the cardinality separately, with messages naming what a
    changed count means. Assert every census member has a register entry and every register entry
    names a census member — both directions, because a register that can only grow silently is the
    drift this axis exists to delete. Assert the fallback ban over the derived authority call-site set.

    THE CONTROLS. Add the three seeded scope mirrors and the top-level control, each asserting its
    anchor occurs exactly once before substitution and each moving the count by exactly one. Add the
    seeded fallback mirror and the seeded dispositionless mirror, each confirmed different from the
    live source before any result is read.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io-writer-set.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the premise case reports a site count of zero, or the reported test names do not include one case per seeded mirror</fails_when>
    <automated>npm run build && npm run typecheck && npm run check:build-parity</automated>
    <fails_when>non-zero exit, or the parity step names any tracked `.js` file as moved</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the summary reports any failed test, or fewer than 66 test files</fails_when>
    <automated>npm run check:residual-citations && npm run check:audit-register</automated>
    <fails_when>non-zero exit, or either check names a published citation whose path this repository does not track</fails_when>
  </verify>
  <acceptance_criteria>
    - The census helper's file set is produced by listing the repository's tracked `.ts` sources at run time, and the file count is asserted with a message naming what a smaller count means.
    - The walk begins at the source file: a seeded diverging call inside an arrow function, inside a class method, and inside a non-top-level block each raise the census count by exactly one, and each mirror asserts its anchor occurred exactly once before substitution.
    - `promoteAdmitted`'s fall-through appears as a member of the ledger axis, and the helper's docstring states that the sibling order axis's tail-delegation exclusion is deliberately absent, naming CR-27.
    - `scripts/context-io.ts` exports a divergence register whose interface carries a stable id, the shape, the reason, what would force it closed, the divergence kind and the site handle; and an exported type closing the kind vocabulary at exactly three members.
    - Both directions of the census-to-register equality are asserted, and the cardinality is asserted separately from the membership.
    - No call site of the owning-repository authority is the operand of a default expression, asserted over the derived call-site set with its cardinality; a mirror restoring that shape at one site turns the case red and is confirmed different from the live source first.
    - A mirror adding a diverging call site with no register entry turns the disposition case red and names the added site.
    - The two oracle call sites appear in the register with the reason quoted from their own site comment rather than paraphrased.
    - The write-path call the platform-shapes driver assembles as module TEXT is named in the register as a site the syntax-tree census cannot see, with what it passes and why the census misses it, so the accepted boundary has a coordinate rather than a category.
  </acceptance_criteria>
  <done>Every place a write path can aim the dial or the record somewhere other than its own derived answer is enumerated from source, counted, dispositioned from a closed vocabulary, and the enumeration is watched failing against five confirmed mirrors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IN-21 — replace a partition that cannot fail with the counts the file cares about</name>
  <files>scripts/harness-instance-ledger.test.ts</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — IN-21 in full, including the quoted expression and the observation that its three terms partition the row set by construction for any input.
    - `scripts/harness-instance-ledger.test.ts` — the file in full: its header block stating that the denominator is asserted before any conclusion, the row parsing, the path-span and id-span recognisers, the claim-checking loop, the working floor on the checked count, and the assertion IN-21 names.
    - `docs/audit/harness-false-result-instances.md` — the whole table and the notes below it, because the counts this test should care about are properties of that table.
  </read_first>
  <behavior>
    - The by-construction equality is gone. Whatever replaces it must be able to FAIL on an input the file would want to reject, and the case's message must name what that input looks like.
    - A FLOOR on the checked count relative to the row count: a table whose rows mostly name no evidence would drop the checked count toward zero while the old partition stayed green. The floor is expressed against the derived row count, not against a typed number.
    - A CEILING on the exempt-with-no-file count, for the same reason in the other direction: a growing population of rows that make no checkable claim about the tree is exactly what the register exists to prevent, and it must be visible rather than absorbed.
    - The existing working floor on the checked count stays, undiluted.
    - NON-VACUITY: a mirror of the ledger whose rows are stripped of their path citations moves the new floor from pass to fail; a mirror whose rows all name evidence moves the new ceiling's reading, and both mirrors are confirmed different from the live file before any result is read.
  </behavior>
  <action>
    Remove the assertion IN-21 names, or replace it — decide which by asking whether the file has a
    property it wants from that expression at all, and write the answer in the case's own comment
    rather than leaving the removal unexplained.

    Add the two assertions the review names: a floor on the checked count expressed as a relationship
    to the derived row count, and a ceiling on the count of rows that name no evidence. Both sides of
    each comparison are measured at run time from the ledger's own parsed rows; neither may be a typed
    number, because a typed number on either side is the drift class this repository keeps deleting.

    Print both counts alongside the existing printed denominators, so a reader of a failing run sees
    which number moved.

    Add the two non-vacuity mirrors, each asserting its anchor occurred the expected number of times
    before substitution and each confirmed different from the live file before any result is read.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/harness-instance-ledger.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the printed denominators are absent from the run output, or the reported test names do not include the two mirror cases</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/uat-gate-exit-contract.test.ts</automated>
    <fails_when>non-zero exit, or any failed test — this suite holds the ordinal-contiguity and derived-scan-set gate over the same ledger file, and a red here means this task moved a property it was not aimed at</fails_when>
  </verify>
  <acceptance_criteria>
    - The expression IN-21 names no longer appears in `scripts/harness-instance-ledger.test.ts`, and the case that replaced it carries a comment stating what property it now asserts and what input would make it fail.
    - Both new assertions compare two values measured at run time from the parsed ledger rows; neither carries a typed numeral as an expected value.
    - The checked count and the no-evidence count are both printed by the run.
    - A mirror of the ledger with path citations stripped turns the new floor red, and the mirror is confirmed different from the live file before any result is read.
    - The pre-existing floor on the checked count is still present and unchanged.
  </acceptance_criteria>
  <done>The vacuity floor that could not fail is replaced by two floors that can, both derived on both sides, and each watched failing against a confirmed mirror.</done>
</task>

<task type="auto">
  <name>Task 3: Disposition the two harness false results `31-38` handed to this round, and record D-40</name>
  <files>docs/audit/harness-false-result-instances.md, scripts/harness-instance-ledger.test.ts, .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/deferred-items.md` — the section headed `Harness false results, logged rather than absorbed` in full: the `admitAndAppend` probe whose governance dial was written at the wrong configuration key, with its stated ordinal and its `owner: round 8's fix plan` and `status: open`; and the self-inflicted suite-red instance whose probe roots were still on disk, with its `status: closed`. Also the section headed `Raised by this plan, with owners, and deliberately NOT repaired here`, because two of those items name this round's planner as owner and must be dispositioned somewhere in this round.
    - `docs/audit/harness-false-result-instances.md` — the whole table, the note recording the round-5 ordinal collision, the note recording the unsubstantiated `31-23` claim, and the section stating what the gate over this file asserts.
    - `scripts/harness-instance-ledger.test.ts` as Task 2 left it, and `scripts/uat-gate-exit-contract.test.ts`'s assertions over the same file — the ordinal contiguity rule, the derived scan set and its asserted cardinality, and the rule that every claimed mechanism must be findable in the evidence the row itself names. A new row must satisfy all of them.
    - `.planning/phases/31-autonomous-manual-testing/31-39-SUMMARY.md` — this round's own measurements, because `D-40`'s body must state what the census found on this tree rather than what it was expected to find.
    - `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` — the `D-34` block, as the shape a decision body follows, and the `D-39` block this round's first plan wrote.
  </read_first>
  <action>
    Append one row per instance to `docs/audit/harness-false-result-instances.md`, taking each ordinal
    from the table rather than from the prose that raised it: the deferred record states an ordinal for
    the first instance, and if that ordinal disagrees with the next one the table actually offers, PRINT
    BOTH and annotate the disagreement in the `Ordinal the record claimed` column — the column exists
    for exactly this, and a prior record is never rewritten to agree.

    For each row, cite the evidence in backticks in the row's own cells, and make every mechanism the
    row CLAIMS findable in that cited evidence — the gate over this file asserts exactly that, and a
    row whose claim cannot be substantiated is the defect the register records. For the first instance,
    the claim is about a configuration key written at the wrong nesting level and about how the false
    result was caught (all dial values producing one indistinguishable outcome); for the second, the
    claim is about probe residue inside the tree and the ordering rule it produced.

    If either instance is judged NOT a row — for example because the second is a harness measuring its
    own residue rather than a false premise about the subject — record that judgement in the file's
    notes section with the reason, so it is dispositioned rather than dropped. Silence is the one
    disposition this round forbids.

    Re-run the gate and the ledger test, then append the `D-40` decision body to `31-CONTEXT.md` under
    a heading following the established shape. State: what forced it (round 8's third `missing:`
    bullet and the two carried instances); the two axes and why a census over one argument would have
    missed one of the two Criticals; the closed kind vocabulary and why three members rather than an
    open set; the seeded-mirror numbers this tree actually produced; what `D-40` does NOT establish —
    at minimum that a census over SOURCE cannot see a divergence assembled at run time, that the
    register names repositories rather than trustworthy ones, and that the Windows leg is `R-03`; and
    the reversibility rating.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/harness-instance-ledger.test.ts scripts/uat-gate-exit-contract.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the ordinal-contiguity case reports a duplicate or a gap, or a claim-substantiation case names a mechanism it could not find in the evidence the row cites</fails_when>
    <automated>npm run check:audit-register && npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or either check names an unanchored claim or an audit-register row that does not resolve</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the summary reports any failed test, or fewer than 66 test files</fails_when>
  </verify>
  <acceptance_criteria>
    - Every instance named in the `Harness false results` section of `deferred-items.md` is either a row in `docs/audit/harness-false-result-instances.md` or has a written judgement in that file's notes explaining why it is not a row.
    - Where the deferred record's stated ordinal disagrees with the ordinal the table offers, both values appear and the disagreement is annotated; no prior summary or record is edited.
    - Every mechanism a new row claims in backticks is findable in the evidence that row itself cites, verified by the gate rather than by reading.
    - `31-CONTEXT.md` carries a `D-40` block with today's date, this round and this plan number, containing the measured seeded-mirror counts from this tree, the rejected alternatives, a `does not establish` section naming at least the three items listed in the action, and a reversibility rating.
    - `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and every traceability row are byte-unchanged by this plan.
  </acceptance_criteria>
  <done>Both carried harness false results are dispositioned in the one authoritative list, the gate over that list is green, and `D-40` records what this tree measured rather than what was expected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree → derived axis input | The census's own input set. A silently short scan makes every conclusion below it vacuous, which is why the file count is asserted before any membership claim. |
| test-file literal → asserted value | A hand-typed count or member list on either side of a comparison is untrusted: it rots while the suite stays green. This is the repository's second named failure class. |
| published register → the tree it names | A register entry claiming a mechanism that is not in the tree hands the next round a false premise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-40-01 | Tampering | A future write-path call site aiming the dial or the record at a value of its own | high | mitigate | The census enumerates the sites from source across files, asserts the cardinality, and requires a register entry per divergence from a closed three-member vocabulary. |
| T-31-40-02 | Repudiation | The divergence register drifting from the tree it describes | high | mitigate | Both directions of the census-to-register equality are asserted, so a stale entry and a dispositionless site both turn the axis red. |
| T-31-40-03 | Spoofing | A derived axis that reports green over an empty or short input set | high | mitigate | The premise case floors the site count and the file count before any conclusion; the two prior instances of this exact failure are rows 4 and 14 of this phase's own false-result ledger. |
| T-31-40-04 | Tampering | A census assembled from another axis's input, inheriting its exclusions | high | mitigate | The tail-delegation exclusion is deliberately absent on the ledger axis, stated in the derivation's docstring with CR-27 named, and `promoteAdmitted`'s fall-through is asserted present as a member. |
| T-31-40-05 | Information Disclosure | The audit register naming file paths | low | accept | The register names repository-relative source paths only, which are already public in a public repository. |
| T-31-40-06 | Elevation of Privilege | A divergence assembled at run time rather than written at a call site | medium | accept | A source census cannot see it. Disclosed in `D-40`'s `does not establish` section with the behavioural cross product in `31-39` as the complementary coverage. |
| T-31-40-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and adds no dependency; `package.json` is not in `files_modified`. No legitimacy checkpoint is required because no install task exists. |
</threat_model>

<verification>
- Both census axes derived from source across the repository's tracked `.ts` files, with the file count and the site count floored before any membership claim.
- Members and cardinality asserted separately on each axis; both directions of the census-to-register equality asserted.
- Five seeded mirrors (three scope shapes, one restored fallback, one dispositionless site) each confirmed different from the live source and each moving a named assertion from pass to fail.
- IN-21's by-construction equality replaced by two floors measured on both sides at run time, each watched failing.
- Both carried harness false results dispositioned in the one authoritative list, with the gate over that list green.
- `D-40` recorded with this tree's measured numbers and a `does not establish` section.
</verification>

<success_criteria>
- Round 8's third `missing:` bullet is satisfied: the divergence set is derived, counted and dispositioned, on both arguments, across files, with the exclusion that hid CR-27 deliberately absent and named.
- The fallback shape that produced CR-26 is banned over a derived set rather than watched by a reviewer.
- The ledger test's vacuity floor can fail.
- Nothing in `deferred-items.md`'s harness-false-result section is carried a second time without a disposition.
- UATX-01 through UATX-06 remain unchecked; the phase remains In Progress.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-40-SUMMARY.md` when done, recording: the
derived file count and site count on each axis; the member list and cardinality of each; each seeded
mirror's before and after counts; the register's entries with their kinds; the new ledger rows with
their ordinals and any annotated disagreement; and every boundary this plan leaves open with a reason
and what would force it closed.
</output>

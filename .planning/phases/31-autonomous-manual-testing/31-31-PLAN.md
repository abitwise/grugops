---
phase: 31-autonomous-manual-testing
plan: 31
type: execute
wave: 5
depends_on: ["31-30"]
gap_closure: true
files_modified:
  - docs/audit/31-round6-residuals.md
  - .planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
  - .planning/phases/31-autonomous-manual-testing/deferred-items.md
  - .planning/STATE.md
autonomous: true
requirements: [UATX-01, UATX-02, UATX-03, UATX-04, UATX-05, UATX-06]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every reproduction `31-VERIFICATION.md` round 6 and `31-REVIEW.md`'s round-5 code review recorded is RE-RUN VERBATIM at ONE commit against the committed `.js`, using the exact probe spellings THOSE documents used — not paraphrases, and not the fix plans' own fixtures. A closure round that re-measures its own tests measures the fix, not the finding. This is the rule that made round 5's closing measurement the strongest artifact this phase has produced, and it is the rule this plan inherits."
    - "The re-run set is not only round 6's. Round 5's five Criticals — CR-12 through CR-16 — are re-driven as CONTROLS at the same commit, because round 6's fixes edit the very modules those closures live in and this phase's recorded pattern is that a fix creates its successor's defect. A closure that reports only the rows it moved cannot show it broke nothing."
    - "Every finding of `31-REVIEW.md` round 5 — CR-17, CR-18, CR-19, CR-20, CR-21, WR-26, WR-27, WR-28, WR-29, WR-30, IN-14, IN-15 — carries an explicit disposition in a written ledger: CLOSED with the measurement that proves it, DECIDED-AND-NAMED as a residual with the reason, or CARRIED with an owner and a criterion. No finding is left without a row, and no row asserts a closure this round did not measure."
    - "Every one of the 34 items the round-6 dispositions file analysed lands in exactly one row, with its dispositioned outcome stated beside its measured outcome. 19 were dispositioned FIX, 13 CLOSE, 2 OPEN. A FIX item whose measurement did not close it is recorded as still open with an owner, never as closed because a plan ran."
    - "The no-silent-drop equality is asserted as a COVERAGE equality, not a cardinality one — because a cardinality equality here would be false and self-invalidating. The phase's edge-coverage report surfaced EIGHT unresolved probe rows, keyed by requirement id and category; the round authored EIGHTEEN flagged assumptions across `31-27` through `31-31` (3 + 3 + 4 + 5 + 3), several of which refine one report row and several of which cover categories the report did not raise. The assertion is therefore: the count of report rows covered by at least one authored flagged assumption equals eight, and the count of report rows covered by NONE equals zero. A row that appears in no plan is a silent drop and turns that equality red; a plan that authored more than the report asked for is not a defect and is not counted as one."
    - "The whole excluded-e2e suite is measured GREEN at ONE commit together with every repository gate — build, typecheck, build-parity, the freshness set, the foundation guards, the UAT oracles and the fixtures type-check. A green suite is NOT the proof. This project's own doctrine, established across six rounds on this phase, is that the suite exercised NONE of the five defects found in round 5 and none of the five found in round 6. It is recorded as a FLOOR beside the adversarial reproductions, never in place of them."
    - "The frozen floors are re-measured rather than assumed: `hooks/guard.ts` hashes equal to `FROZEN_GUARD_BLOB`; `FROZEN_HOOK_ENTRY_LOGIC_SHA` equals a fresh normalisation of the wrapper; and every `DECIDER_MANIFEST` entry equals the current digest of the file it names. The manifest is checked as a WHOLE, not only for the entries this round moved, because a stale entry elsewhere is the same class of drift."
    - "The residual register for round 6 is written down once, in a tracked document, listing every boundary the round's four fix plans decided to leave open — including every `hosts: non-cc-hook-path` member, the three kept callee refusals, whatever `WRITE_PATH_RESIDUALS` carries after `31-29`, the Windows remainder after `31-30`, and any unattributed disposition clause — each with the reason and what would force it closed, so round 7's reviewer starts from a register rather than rediscovers it."
    - "UATX-01 through UATX-06 stay UNCHECKED in `.planning/REQUIREMENTS.md`, the Phase 31 checkbox stays unchecked in `.planning/ROADMAP.md`, and every traceability row still reads `Gaps Found`. Only a verification round may flip a requirement. A gap-closure plan that flips one is asserting its own success — and this phase has now had six rounds in which the executing round believed it had closed the requirement."
    - "The four standing human-verification items are carried forward with their `UNKNOWN - verify` markers intact, EXCEPT where `31-30` closed one by harness: `R-04` is recorded closed with its measurements, `R-03` is recorded at its measured remainder, and `R-01` and `R-02` are carried unchanged with named human owners. None is closed by inference and none is dropped."
    - "This plan writes no source. It measures. A closing measurement that fixes something has found a new defect, and a new defect belongs in a new plan with its own RED-first reproduction — the standing rule `docs/audit/31-round5-residuals.md` §12 item 6 recorded and this plan inherits. Anything found here is recorded with an owner rather than repaired."
    - "The measurement environment is proven clean by a predicate that can OBSERVE it: `.temp/` empty by a real listing, no named pipe anywhere in the tree, and the `git status`-based residue predicate demonstrated inert. And the `.planning/STATE.md` write is VERIFIED rather than assumed — this repository's state writer re-escapes backslashes on every write, and a pathological line length there has previously combined with a superlinear guard predicate to turn a sub-second gate into a multi-minute one."
  artifacts:
    - docs/audit/31-round6-residuals.md
    - .planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
    - .planning/phases/31-autonomous-manual-testing/deferred-items.md
    - .planning/STATE.md
  key_links:
    - "each round-6 reproduction <-> its post-round measurement: the pairing that makes a closure claim checkable by a later reader without re-deriving the probe."
    - "each round-5 reproduction <-> its re-run as a CONTROL: the both-directions record that shows what moved AND what did not, in the modules round 6 edited."
    - "every review finding and every dispositioned item <-> a ledger row: the binding that makes a finding without a disposition impossible to ship."
    - "the eight unresolved edge-coverage rows <-> the flagged assumptions authored across the round: a COVERAGE mapping, asserted row by row, so a dropped row is red rather than absent — and an authored assumption beyond what the report asked for is legible rather than counted as drift."
    - "the round's four fix plans' residuals <-> one tracked register: the artifact that turns round 7's rediscovery into a lookup."
    - "the requirement rows <-> the verification round that may flip them: the discipline that keeps a gap-closure plan from certifying itself."
  prohibitions:
    - statement: "A gap-closure plan must never flip a requirement checkbox, a traceability row or the phase checkbox. Only a verification round may do that."
      status: unverified
      verification: flagged
    - statement: "Never fake a passing gate, a test result, or a citation. A reproduction that was not re-run is recorded as not re-run, with the reason, never as closed."
      status: unverified
      verification: flagged
    - statement: "A green test suite is not proof for a safety predicate. It is recorded as a floor beside the adversarial reproductions, never in place of them."
      status: unverified
      verification: flagged
    - statement: "This plan writes no source and no test. A defect found here is recorded with an owner, never repaired in place."
      status: unverified
      verification: flagged
    - statement: "A prior round's SUMMARY or PLAN is history and is never rewritten. A disagreement with it is annotated in this round's own record."
      status: unverified
      verification: flagged
    - statement: "A human-verification item is carried forward with its `UNKNOWN - verify` marker intact unless a harness measured it closed, and then the measurements are quoted."
      status: unverified
      verification: flagged
    - statement: "`hooks/guard.ts` is not touched and `FROZEN_GUARD_BLOB` is never re-based; a frozen manifest entry is moved with its artifact, never relaxed."
      status: unverified
      verification: flagged
    - statement: "Never merge a protected branch and never deploy to production without a named human confirmation."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-26-PLAN.md: the closing-measurement role — re-run the findings' own probes, not the plans' fixtures; record a disposition for every finding; leave the requirement rows for the verifier. This plan is that role performed for round 6."
    - "31-27..31-30: each fix plan's own prohibitions, which this plan re-measures rather than re-asserts."
---

<objective>
The closing measurement of gap-closure round 6. Re-run every reproduction the round was convened to
close, re-run every round-5 reproduction as a control in the modules round 6 edited, disposition all
34 analysed items and all twelve review findings, assert the no-silent-drop equality, write the
round's residual register, and leave the requirement rows for the verifier.

This plan writes no source. It measures.

## Finding and item coverage — every round-6 input lands in exactly one plan

| Item | Severity / kind | Disposition | Closed by | What the closure is |
|---|---|---|---|---|
| CR-17 — the wrapper hashes thirteen agent-writable paths with a blocking read before the spawn its timeout bounds | Critical | Fix | `31-27` | one inline non-blocking reader with a stated ceiling, the fd-0 read deleted and fd 0 inherited into the timed child; probed at all thirteen positions through the command derived from `hooks.json` |
| CR-18 — a block-scoped function declaration is handed the whole module's range | Critical | Fix (S2) | `31-28` | the census is deleted; scope is the checker's answer |
| CR-19 — the note ceiling is read-side only, so a 9 MiB note writes and reads back as zero | Critical | Fix | `31-29` | one exported ceiling enforced at the admitting side, with an honest clause for an over-ceiling regular file; the same reconciliation at the ledger |
| CR-20 — the note is keyed on `to` and the ledger on `repoRoot`, two repositories for one action | Critical | Fix | `31-29` | one derived destination root binds both halves; a destination outside a governed store declines by name |
| CR-21 — the three-argument scenario overload hides a TestInfo modifier call | Critical | Fix (S2) | `31-28` | the body is found by type, not by index |
| WR-26 — the fixture exemption is wider than the map it exempts for | Warning | Fix (S2) | `31-28` | the position rule is deleted; the exemption is the parameter's declared type |
| WR-27 — the derived read-site axis walks only top-level functions | Warning | Fix | `31-29` | a recursive walk with scope attribution and three mirrors the old walk could not move |
| WR-28 — the forged-origin price says three and measures two | Warning | Fix | `31-29` | the price restated per position across all three artifacts, measured |
| WR-29 — four could-not-run reasons claimed, three implemented | Warning | Fix | `31-28` | four separate catch arms with four pairwise-distinct sentences |
| WR-30 — `using` classified as hoisting | Warning | Fix (S2) | `31-28` | the two-bit arm is deleted with the census |
| IN-14 — one catch covers three facts; two residuals bound nowhere | Info | Fix | `31-29` | three named counted arms reported in `render`, and `WRITE_PATH_RESIDUALS` with a two-sided binding |
| IN-15 — one string split twice | Info | Fix | `31-28` | computed once into a local |
| S1 — the governance root's host-delivered tier and the register's host scoping | structural | Fix | `31-27` | tier 0, strictly narrower than the tier it precedes, with a `hosts` field on every member |
| R-31-19-07 — a case-differing spelling defeats the module-own exclusion | residual, OCCUPIED | Fix | `31-27` | both sides canonicalised through one ladder |
| R-31-22-02 — the destination `to` unconstrained | residual, exploited by CR-20 | Fix | `31-29` | rewritten with the constraint that now exists and what remains open |
| R-31-21-02 / R-31-21-04 — a silent skip, and syntactic derivations | residuals | Fix | `31-29` | three named arms; a recursive scope-aware walk for the scope half |
| RR-01 / RR-03 / RR-08 / RR-09 | residuals | Fix (S2) | `31-28` | closed by symbol identity; removed from the register with the measurements that closed them |
| RR-05 | residual | Close (delete) | `31-28` | removed with the code that needed it |
| RR-07 — a silent degrade to a weaker rule | residual | Fix | `31-28` | routed to could-not-run at exit 2 with a named reason |
| D-28's non-unwinding fault, at the caller | residual | Fix | `31-30` | the §14 gate's fourth arm, derived from the prose and bound to the runnable's reachable outcomes |
| R-03 / R-31-19-03 — the Windows leg | human item | Fix via CI | `31-30` | Windows-scoped steps on the pre-existing leg, with an asserted skip list; the remainder named |
| R-04 — the installer round-trip | human item | Fix | `31-30` | automated harness with five measured outcomes |
| the `check:diff-disposition` debt | process | Fix | `31-30` | three owed disposition files, base commit and corpus unmoved |
| the collided harness-instance ordinal | process | Fix (docs) | `31-30` | one tracked list with the numbering read off it; no SUMMARY rewritten |
| the inert `.temp/` residue predicate | process | Fix | `31-27` + `31-30` | a runner exclusion, plus a predicate that can observe its subject |
| R-31-15-02 / R-31-15-04 / R-31-19-01 / R-31-19-04 / R-31-19-05 | residuals | **Close** | `31-27` | accepted by design, named so they stop reading as misses |
| T-31-14-03 / T-31-18-01 / R-31-22-01 / R-31-22-03 / R-37 | residuals | **Close** | `31-29` | accepted by design; the signed-note-stamp work is a next-milestone roadmap item |
| RR-02 / RR-04 / RR-06 | residuals | **Close** | `31-28` | kept as named refusals with reasons true of the new mechanism |
| R-31-21-01 / R-31-21-03 | residuals | **Close** | `31-29` | accepted by design; `R-31-21-03` recorded closed by round 5's own measurement |
| R-01 — the attended Chrome lane under real interactive auth | human item | **Open** | — | no agent owner; a named human must run an attended session |
| R-02 — the auth predicate under API-key and long-lived-token configurations | human item | **Open** | — | no agent owner; needs credential configurations this box does not have |

Every one of the twelve review findings has exactly one owning plan, and the five Criticals map onto
the two blocked requirements: CR-17, CR-19 and CR-20 to UATX-01; CR-18 and CR-21 to UATX-06.

Purpose: produce the one artifact a seventh verification round can start from — a paired reproduction
ledger, a disposition for every item, an asserted no-silent-drop equality, and a residual register —
rather than a claim.

Output: `docs/audit/31-round6-residuals.md`; refreshed carry-forward records in `31-VALIDATION.md` and
`deferred-items.md`; an updated `.planning/STATE.md`; and requirement rows deliberately unmoved.

**Ordering note:** wave 5, `depends_on: ["31-30"]`. Every measurement in this plan must be taken at ONE
commit, after all four fix plans have landed. It modifies no source file and no test file, so it
conflicts with nothing except by requiring everything.

<assumption_delta_decision>
**Decision: `no-change`.** This plan records no decision, because a measurement plan that recorded a
decision would be deciding something it did not build. The detector's three signals are decided in
D-14, D-21, D-30 and D-31, and this plan measures those decisions rather than amending them.
</assumption_delta_decision>
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@.planning/phases/31-autonomous-manual-testing/31-REVIEW.md
@.planning/phases/31-autonomous-manual-testing/31-round6-residual-dispositions.md
@.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
@.planning/phases/31-autonomous-manual-testing/deferred-items.md
@docs/audit/31-round5-residuals.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Where |
|---|---|---|
| the paired reproduction ledger | NEW section: one row per round-6 reproduction, each carrying the finding document's own probe spelling, the pre-round result, the post-round result, and the commit both were taken at | `docs/audit/31-round6-residuals.md` |
| the round-5 control ledger | NEW section: CR-12 through CR-16 and round 5's Warnings re-driven at the same commit, each with its round-5 result beside its round-6 result | `docs/audit/31-round6-residuals.md` |
| the behavioral spot-check re-run table | NEW section: every spot-check `31-VERIFICATION.md` round 6 recorded, re-driven, with the failing rows shown moving and the passing rows shown unmoved | `docs/audit/31-round6-residuals.md` |
| the disposition ledger | NEW section: one row per review finding, per anti-pattern row, per `missing:` bullet, and per item of the round-6 dispositions file, with its dispositioned outcome beside its measured outcome | `docs/audit/31-round6-residuals.md` |
| the ledger's totals as an equality | NEW section: the counts stated as an equality that must balance — items in, rows out, closed plus residual plus carried | `docs/audit/31-round6-residuals.md` |
| the no-silent-drop equality | NEW section: the eight unresolved edge-coverage rows counted against the flagged assumptions authored across `31-27` through `31-31`, asserted equal | `docs/audit/31-round6-residuals.md` |
| the one-commit gate record | NEW section: the whole excluded-e2e suite, the build, typecheck, parity, the freshness set, the foundation guards and the repo gates, each with its command and its result, recorded as a FLOOR with the doctrine stated beside it | `docs/audit/31-round6-residuals.md` |
| the frozen-floor re-measurement | NEW section: `FROZEN_GUARD_BLOB`, `FROZEN_HOOK_ENTRY_LOGIC_SHA` and the WHOLE `DECIDER_MANIFEST` re-measured entry by entry | `docs/audit/31-round6-residuals.md` |
| the round-6 residual register | NEW section: every boundary the round's four fix plans left open, by id, with the reason and what would force it closed | `docs/audit/31-round6-residuals.md` |
| the what-this-round-did-NOT-do section | NEW section: stated plainly, because a closure round's silences are what the next round pays for | `docs/audit/31-round6-residuals.md` |
| the refreshed validation record | CHANGED carry-forward counts and item states, read from the file rather than inferred | `.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md` |
| the refreshed deferred record | CHANGED: `R-01`/`R-02` carried with owners, `R-03` at its remainder, `R-04` closed, the debt at its re-measured count | `.planning/phases/31-autonomous-manual-testing/deferred-items.md` |
| the verified state write | CHANGED with its longest-line length measured after the write and the foundation guards re-run with their wall-clock recorded | `.planning/STATE.md` |

## Edge probe assumptions

| # | Requirement | Category | Probe | Flagged assumption for this plan |
|---|---|---|---|---|
| 1 | UATX-04 | unclassified — review manually | — | Assumed: none of round 6's twelve findings names the provenance-carrying surface — `admit()`, the artifact-ref path, the SHA/gate-run-id/content-hash triple — so UATX-04 is unmoved by this round. FLAGGED and MEASURED rather than assumed: this plan re-drives the provenance refusal (a note whose SHA is not the HEAD the gate ran against) at the closing commit and records the result. |
| 2 | UATX-01 | unclassified — review manually | — | Assumed: the round's four fix plans between them cover every artifact `31-VERIFICATION.md`'s gap 1 named. FLAGGED; asserted as a set equality between the gap's `artifacts:` paths and the union of the round's `files_modified`. |
| 3 | UATX-06 | unclassified — review manually | — | Assumed: the round's fix plans cover every artifact gap 2 named. FLAGGED; asserted the same way. |

This plan also carries the ROUND-LEVEL equality: the eight unresolved rows of the phase's
edge-coverage report against the flagged assumptions authored across all five plans of round 6.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a fix plan's own fixtures -> a closure claim | crossing here measures the fix rather than the finding; the mitigation is re-running the FINDING document's own probe spellings |
| a green test suite -> a safety claim | crossing here is this phase's most expensive recorded mistake: the suite exercised none of the ten Criticals found in rounds 5 and 6 |
| a gap-closure round -> a requirement checkbox | crossing here is a round certifying itself, which the ROADMAP forbids in its own words |
| a measurement harness -> its own premise | crossing here produced a false result in six instances across four straight rounds; the harness's premise is asserted, not assumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-31-01 | Repudiation | a closure claimed from a fix plan's own fixture rather than the finding's probe | high | mitigate | Every reproduction is re-run with the spelling `31-REVIEW.md` or `31-VERIFICATION.md` used, quoted from those documents, at one commit. A row that could not be re-run is recorded as not re-run with the reason. |
| T-31-31-02 | Repudiation | a green suite presented as proof of a safety predicate | high | mitigate | The suite is recorded as a FLOOR in its own section with the doctrine stated beside it and with the count of round-5 and round-6 Criticals the suite did not exercise. |
| T-31-31-03 | Elevation of Privilege | a gap-closure round flipping a requirement it cannot verify | critical | mitigate | The requirement rows, the traceability rows and the phase checkbox are asserted UNCHANGED by a range-pinned `git diff "$PLAN_BASE"..HEAD` over those paths, quoted. |
| T-31-31-04 | Spoofing | a measurement harness producing a false result about its own premise | high | mitigate | Every harness in this plan asserts its own premise first — the probe root exists, the derived denominator is non-zero AND equals an independently derived element count, and the mutant marker is present before a result is read. The instance is logged to the tracked list `31-30` created. |
| T-31-31-05 | Tampering | a stale frozen manifest entry elsewhere in the manifest | high | mitigate | The `DECIDER_MANIFEST` is re-measured as a WHOLE, entry by entry, not only for the entries this round moved. |
| T-31-31-06 | Denial of Service | a pathological `.planning/STATE.md` write combining with a superlinear guard predicate | medium | mitigate | The longest line length is measured after the write and the foundation guards are re-run with their wall-clock recorded. |
| T-31-31-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager install task and no dependency; every command runs against the committed lockfile and already-installed dev dependencies, so the package-legitimacy protocol has no `[ASSUMED]`/`[SUS]` row to gate. |
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1: Re-run every round-6 reproduction and every round-5 control verbatim, at one commit, and pair each result with the one it replaces</name>
  <files>docs/audit/31-round6-residuals.md</files>
  <precondition>Every measurement in this task is taken at ONE commit, after `31-27` through `31-30` have all landed; the commit SHA is recorded once at the top of the document and every row is asserted to have been taken at it. The working tree must be clean before the first measurement and is asserted so with `git status --porcelain` quoted.</precondition>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` in full — the `gaps:` block with both gaps' reasons, artifacts and `missing:` bullets; the Round History table; the Observable Truths table; the Required Artifacts table; and every Behavioral Spot-Check row with its command, input and output. These are the probe spellings this task re-runs.
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` in full — CR-17 through CR-21, WR-26 through WR-30, IN-14 and IN-15, each with its reproduction transcript. Where the review and the verification used different spellings for one finding, BOTH are re-run and both are recorded.
    - `docs/audit/31-round5-residuals.md` — §1 through §6 for the round-5 reproductions that become this round's controls, and §11's aggregated probe index; and §12 for the standing rules a closing measurement follows.
    - `.planning/phases/31-autonomous-manual-testing/31-round6-residual-dispositions.md` in full — all 34 items, so Task 2's ledger has a denominator derived from a document rather than from memory.
    - `31-27-SUMMARY.md`, `31-28-SUMMARY.md`, `31-29-SUMMARY.md`, `31-30-SUMMARY.md` — each plan's own probe register, so this task starts from what they recorded and re-runs the FINDINGS' probes rather than theirs.
    - `.planning/phases/31-autonomous-manual-testing/31-26-PLAN.md` and `31-26-SUMMARY.md` — the previous round's closing measurement, as the shape this document follows.
  </read_first>
  <behavior>
    - The probe denominator is DERIVED: the count of reproductions named in `31-VERIFICATION.md` plus those named in `31-REVIEW.md`, with duplicates identified rather than silently merged, and the number recorded before any probe runs.
    - Every round-6 reproduction is re-run with its own document's spelling and its result recorded beside the pre-round result.
    - Every round-5 reproduction — CR-12 through CR-16 and round 5's four Warnings — is re-run as a CONTROL at the same commit; the controls' results are recorded whether they moved or not.
    - Every behavioral spot-check `31-VERIFICATION.md` round 6 recorded is re-driven; the failing rows are shown moving and the passing rows are shown unmoved, and the two counts are stated separately.
    - Any row that CANNOT be re-run is recorded as not re-run with the reason, never as closed.
    - Every harness asserts its own premise before its result is read: the probe root exists, the derived denominator is non-zero AND equals an independently derived element count, and where a mutant is used the rebuilt artifact is grepped for its marker first.
  </behavior>
  <action>
    MOVEMENT 1 — pin the commit and prove the tree. Record the commit SHA every measurement is taken at
    and export it as `PLAN_BASE`; export the commit this whole round branched from as `ROUND_BASE`. Every
    `git diff` in this plan is range-pinned to one of the two, because a bare `git diff` sees nothing once
    a change is committed and would pass vacuously — the shape a gate flagged on this very plan.
    Quote `git status --porcelain` as empty. Assert `.temp/` is empty by a real listing, that no named
    pipe exists anywhere in the tree, and demonstrate once more that the `git status`-based residue
    predicate is inert by quoting `git check-ignore -v` against a planted file beside an empty
    `git status --short .temp`.

    MOVEMENT 2 — derive the denominator, then probe. Count the reproductions named across
    `31-VERIFICATION.md` and `31-REVIEW.md`, identify the rows where the two documents used different
    spellings for one finding, and record the derived total. Then re-run each, quoting the command, the
    input, the exit code and both streams. For CR-17 this means the FIFO and directory shapes at the
    manifest positions driven through the command derived from `hooks.json`; for CR-18 and CR-21 the
    reviewer's exact three-line spellings; for CR-19 the 9 MiB body with the follow-up read and the
    idempotent re-write; for CR-20 the three-real-roots promotion with all three ledgers counted.

    MOVEMENT 3 — re-run round 5's closures as controls. Drive CR-12's note-path FIFO, CR-13's
    home-rooted repository, CR-14's dead-declaration evasion, CR-15's depth-1000 boundary and its
    630/631 adjacency, CR-16's in-repository origin refusal, and round 5's four Warnings, each at the
    same commit, each recorded beside its round-5 result. `31-28` deleted the mechanism CR-14's closure
    lived in and `31-29` edited the module CR-12's and CR-16's closures live in, so these controls are
    the round's own regression evidence rather than a courtesy.

    MOVEMENT 4 — re-drive every behavioral spot-check. Take each row `31-VERIFICATION.md` round 6
    recorded, re-drive it, and record the result beside the round-6 result. State the count of rows that
    FAILED in round 6 and moved, and the count that PASSED in round 6 and stayed unmoved, as two
    separate numbers. A closure that reports only the moved rows cannot show it broke nothing.

    MOVEMENT 5 — assert every harness's own premise, and log any false result. For each harness used
    here, state the premise it depends on and the assertion that establishes it. Where a harness
    produced a false result about its own premise during this task, log it as a new row in
    `docs/audit/harness-false-result-instances.md` — reading the next ordinal OFF that list, never
    typing one.
  </action>
  <acceptance_criteria>
    - `PLAN_BASE` and `ROUND_BASE` are both recorded as commit SHAs and quoted; every range-pinned assertion in this plan names one of them.
    - The commit SHA is recorded once and every row states it was taken at that commit; `git status --porcelain` is quoted empty at the start.
    - The derived probe denominator is quoted with the documents it was derived from, and the number of rows actually driven is asserted equal to it; any shortfall is named row by row with a reason.
    - Every round-6 reproduction row carries: the source document, the probe spelling quoted from that document, the pre-round result, the post-round result, and the command.
    - Every round-5 control row carries its round-5 result beside its round-6 result; controls that MOVED are called out separately from controls that did not.
    - The behavioral spot-check re-run table states two counts separately: rows that failed in round 6 and moved, and rows that passed in round 6 and stayed unmoved.
    - Every row that could not be re-run is recorded as not re-run with a reason; the count of such rows is stated.
    - Each harness's premise is stated with the assertion that established it; any false result is logged to the tracked instance list with an ordinal read off the list.
    - `.temp/` is empty by a real listing, the named-pipe sweep prints nothing, and the residue-predicate inertness demonstration is quoted.
    - `git diff --name-only "$PLAN_BASE"..HEAD -- scripts hooks agent-factory install .github` is EMPTY and quoted, with `PLAN_BASE` pinned to the commit recorded in MOVEMENT 1 — this plan writes no source.
  </acceptance_criteria>
  <verify>
    <automated>git status --porcelain -- scripts hooks agent-factory install .github vitest.config.ts &amp;&amp; git diff --name-only "$PLAN_BASE"..HEAD -- scripts hooks agent-factory install .github vitest.config.ts</automated>
    <fails_when>any output at all from either half — the working tree carries an uncommitted change under these paths, or the range from the pinned `PLAN_BASE` to HEAD names a committed one. `PLAN_BASE` is the commit SHA pinned in MOVEMENT 1 and MUST be exported before this command runs; an unset `PLAN_BASE` makes the range unresolvable and the command exits non-zero rather than passing vacuously</fails_when>
    <automated>test -z "$(find .temp -mindepth 1 -print -quit)" &amp;&amp; test -z "$(find . -path ./node_modules -prune -o -type p -print)"</automated>
    <fails_when>non-zero exit — `.temp/` holds an entry by a real listing, or a named pipe survives anywhere in the tree</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts scripts/runnable-ref/uat-spec-integrity.test.ts hooks/guard.test.ts</automated>
    <fails_when>non-zero exit, a summary line reporting `0 passed`, or any case that this round's fix plans recorded green now reporting a failure</fails_when>
  </verify>
  <done>Every round-6 reproduction and every round-5 control is re-run verbatim at one commit with its result paired against the one it replaces; every spot-check is re-driven with the moved and unmoved counts stated separately; and every harness's own premise is asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the whole repository green at one commit, re-measure the frozen floors, and assert the no-silent-drop equality</name>
  <files>docs/audit/31-round6-residuals.md</files>
  <read_first>
    - `docs/audit/31-round5-residuals.md` §6 in full — the one-commit gate record, the doctrine stated beside the green, the debt reconciliation, the frozen-floor re-measurement and the register cardinalities, as the shape this task follows.
    - `package.json`'s scripts block — every gate command by name, so the record is derived from the file rather than remembered.
    - `.github/workflows/ci.yml` as `31-30` left it — the gates CI runs and their OS scoping, so what is measured locally is stated against what CI measures.
    - `scripts/floor-invariance.test.ts` — `FROZEN_GUARD_BLOB` and `FROZEN_HOOK_ENTRY_LOGIC_SHA` and the normalisation the wrapper freeze is taken over.
    - `hooks/hook-entry.ts` — the whole `DECIDER_MANIFEST` region, so every entry is re-measured rather than only the ones this round moved.
    - Each of `31-27` through `31-31`'s PLAN files' `## Edge probe assumptions` sections — the flagged assumptions whose count this task asserts against the eight unresolved report rows.
    - `scripts/context-io.ts` and `scripts/runnable-ref/uat-spec-integrity.ts` — the exported registers and their cardinalities, measured on this tree.
  </read_first>
  <action>
    MOVEMENT 1 — the whole suite and every gate, at the pinned commit, derived from `package.json`.
    Enumerate the gate commands from the scripts block rather than typing a list, run each, and record
    the command and its result. Include the excluded-e2e suite, the build, the typecheck, the
    build-parity check, every freshness gate, the foundation guards, the repo gates and the fixtures
    type-check. Record the suite's file and test counts.

    MOVEMENT 2 — state the doctrine BESIDE the green, not instead of it. Record, in this section, the
    count of Criticals found in round 5 and in round 6 that this suite exercised: measured, not
    asserted. The number is what makes the green a floor. Quote the phase's own doctrine sentence.

    MOVEMENT 3 — re-measure the frozen floors as a WHOLE. Hash `hooks/guard.ts` and compare to
    `FROZEN_GUARD_BLOB`. Re-normalise the wrapper and compare to `FROZEN_HOOK_ENTRY_LOGIC_SHA`. Then walk
    EVERY `DECIDER_MANIFEST` entry — both deciders' closures — and compare each to the current digest of
    the file it names, recording a row per entry with a yes/no. A stale entry elsewhere is the same class
    of drift as one this round moved.

    MOVEMENT 4 — measure the exported register cardinalities on this tree. `TRUSTED_ROOT_RESIDUALS` with
    its `hosts` breakdown, `PROMOTE_ADMITTED_RESIDUALS`, `WRITE_PATH_RESIDUALS`,
    `UNRESOLVABLE_CALLEE_RESIDUALS`, `PROMOTE_ADMITTED_DECLINES`, `TRUSTED_ROOT_TIERS`,
    `PATHOLOGICAL_INPUT_SHAPES` and the non-blocking-reader implementation set. Each number is read off
    the tree, and each is compared to what the owning plan's SUMMARY recorded; a disagreement is
    recorded as a disagreement, not resolved by preference.

    MOVEMENT 5 — the no-silent-drop equality, asserted as COVERAGE. Enumerate the unresolved rows of the
    phase's edge-coverage report — eight, keyed by requirement id AND category (UATX-01 unclassified;
    UATX-02 adjacency, empty, ordering; UATX-03 unclassified; UATX-04 unclassified; UATX-05
    unclassified; UATX-06 unclassified). Enumerate the flagged assumptions authored across `31-27`
    through `31-31`'s `## Edge probe assumptions` sections — expected eighteen (3 + 3 + 4 + 5 + 3), and
    the actual count is DERIVED from the plan files rather than copied from this sentence. Then build the
    MAPPING: for each of the eight report rows, name the authored assumptions that cover it. Assert two
    numbers — rows covered by at least one assumption equals eight, rows covered by none equals zero.
    Do NOT assert the two totals equal: eighteen authored against eight raised is expected, because
    several assumptions refine one row and several cover categories the report did not raise, and an
    equality of totals here would be a claim that measures false. A report row that appears in no plan is
    a silent drop and is recorded as one, with the plan it should have gone to.

    MOVEMENT 6 — re-measure the debt and the residue. Run the disposition gate, record the count and the
    per-file table, and state the delta against round 5's 110 and against what `31-30` recorded. Confirm
    the base commit and the watched corpus are unmoved and quote both.
  </action>
  <acceptance_criteria>
    - The gate command list is DERIVED from `package.json` and its cardinality is asserted; every command's result is recorded.
    - The suite's file and test counts are quoted; the section states explicitly that the green is a FLOOR and records the measured count of round-5 and round-6 Criticals the suite exercised.
    - `FROZEN_GUARD_BLOB` and `FROZEN_HOOK_ENTRY_LOGIC_SHA` are each re-measured and quoted with their expected values.
    - EVERY `DECIDER_MANIFEST` entry is re-measured with a row per entry; the entry count is quoted and asserted equal to the derived closure size for each decider.
    - Every exported register's cardinality is measured on this tree and compared to the owning SUMMARY's number; any disagreement is recorded as such.
    - The no-silent-drop equality is stated as COVERAGE: the eight report rows are enumerated by requirement id and category, the authored assumption count is DERIVED from the five plan files and quoted, and the two asserted numbers are `rows covered by >= 1 assumption == 8` and `rows covered by 0 assumptions == 0`. Any shortfall is named row by row with the plan it should have gone to. The summary states explicitly that the authored total (expected eighteen) exceeding the raised total (eight) is expected and is not a defect.
    - The disposition gate's count and per-file table are quoted with the delta stated against 110 and against `31-30`'s number; the base commit and the watched corpus element count are quoted unchanged.
    - `npm run check:diff-disposition` debt does not grow: the finding count measured here is recorded and is not greater than the count `31-30` recorded, and the delta against round 5's 110 is stated. If the count rose, the rise is named as a finding of this round with an owner rather than absorbed.
    - `.temp/` is empty by a real listing and the sweep prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, a summary line reporting `0 passed`, or any test file reporting a failure — this is the whole excluded-e2e suite at one commit and it is a floor, never the proof</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness &amp;&amp; npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit, or any step naming a committed `.js` or a manifest entry that is not a faithful build or a current digest of its source</fails_when>
    <automated>npm run freshness:catalog &amp;&amp; npm run freshness:context &amp;&amp; npm run freshness:adapters &amp;&amp; npm run freshness:skill-twins &amp;&amp; npm run freshness:guarantees</automated>
    <fails_when>non-zero exit, or any freshness gate naming a committed artifact that differs from a fresh regeneration</fails_when>
    <automated>npm run check:claim-anchors &amp;&amp; npm run check:audit-register &amp;&amp; npm run check:residual-citations &amp;&amp; npm run check:banned-claims &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or any check naming a claim, register row, residual citation, banned claim or NUL byte in this round's artifacts</fails_when>
  </verify>
  <done>The whole repository is measured green at one commit with the doctrine recorded beside it, every frozen floor and every manifest entry is re-measured as a whole, every register cardinality is read off the tree, and the eight edge-coverage rows are asserted to have eight authored flagged assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Disposition every item, write the round-6 residual register, state what this round did NOT do, and leave the requirement rows for the verifier</name>
  <files>docs/audit/31-round6-residuals.md, .planning/phases/31-autonomous-manual-testing/31-VALIDATION.md, .planning/phases/31-autonomous-manual-testing/deferred-items.md, .planning/STATE.md</files>
  <read_first>
    - `docs/audit/31-round5-residuals.md` §7 through §14 in full — the disposition ledger's three blocks, the totals-as-an-equality section, the residual register's six subsections, the what-this-round-did-NOT-do list and the requirement-rows-confirmed-unchanged section. This is the shape and the standard.
    - `.planning/phases/31-autonomous-manual-testing/31-round6-residual-dispositions.md` in full — all 34 items with their dispositions, which are the denominator for this task's ledger.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the anti-patterns rows, the `missing:` bullets and the `human_verification` block, each of which needs its own ledger row.
    - `.planning/REQUIREMENTS.md` UATX section and the traceability table; `.planning/ROADMAP.md`'s Phase 31 entry and checkbox — the rows this plan must leave unchanged and must PROVE unchanged.
    - `.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md` and `deferred-items.md` — the carry-forward counts, read rather than inferred.
    - `docs/audit/harness-false-result-instances.md` as `31-30` created it — where any false result from this round is logged, with the ordinal read off the list.
    - `.planning/STATE.md` — the current status block, and the recorded hazard about the state writer re-escaping backslashes on every write.
  </read_first>
  <action>
    MOVEMENT 1 — the disposition ledger, in blocks, with a derived denominator. Block A: the twelve
    numbered findings of `31-REVIEW.md`. Block B: the anti-pattern rows of `31-VERIFICATION.md`. Block C:
    its `missing:` bullets. Block D: all 34 items of the round-6 dispositions file, each with its
    DISPOSITIONED outcome (fix / close / open) beside its MEASURED outcome. Every row carries: the
    measurement that closed it, or the reason it is a named residual, or the owner and criterion it is
    carried with. Derive each block's denominator from its source document and assert the row count
    equals it.

    MOVEMENT 2 — the totals, as an equality that must balance. State: items in, rows out, and closed
    plus residual plus carried. Where the arithmetic does not balance, say so and name the discrepancy
    rather than adjusting a number — round 5's own record shows an unbalanced tally is a finding, not a
    typo.

    MOVEMENT 3 — the round-6 residual register. One subsection per register: the governance root with
    its `hosts` breakdown; the admission/promotion route; the write path; the UAT-spec modifier ban; the
    exit-code contract; and the process and harness items. Every member by id, with the shape, the
    reason it is left open, and what would force it closed. Include, explicitly, the CLOSE dispositions
    each fix plan carried, so a seventh reviewer reads them as decided rather than as misses. Include
    every OPEN item with its owner: `R-01` and `R-02` human; the Windows remainder; the signed-note-stamp
    roadmap item; and any unattributed disposition clause.

    MOVEMENT 4 — what this round did NOT do, stated plainly. At minimum: which human items remain open
    and that every probe ran on this platform only; whether the disposition debt fell and by how much;
    which residuals are named-and-open rather than closed; that no source file was modified by this
    plan and that every finding this plan raised is recorded with an owner rather than repaired; and
    that no requirement was flipped.

    MOVEMENT 5 — the requirement rows, confirmed unchanged by measurement. Quote the ROADMAP's own
    sentence that only a verification round may flip a requirement. Then PROVE the rows are unmoved:
    a range-pinned `git diff "$ROUND_BASE"..HEAD` over `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` restricted to the UATX rows,
    the traceability table and the Phase 31 checkbox, quoted as empty of any status change. If the
    executor of an earlier plan flipped a row — this repository has recorded an executor's roadmap
    update prematurely marking a phase Complete — the flip is reverted here and the revert is recorded
    as a finding about the round's own bookkeeping.

    MOVEMENT 6 — refresh the carry-forward records and write STATE. Update `31-VALIDATION.md`'s
    carry-forward counts by reading them and incrementing once. Update `deferred-items.md` per Task 1's
    and `31-30`'s measurements. Write `.planning/STATE.md`'s status block. Then VERIFY the write: measure
    the longest line length in the file, and re-run the foundation guards recording their wall-clock
    duration, because this repository has measured a pathological state line combining with a
    superlinear guard predicate to turn a sub-second gate into a multi-minute one.
  </action>
  <acceptance_criteria>
    - Each ledger block's denominator is DERIVED from its source document and quoted; each block's row count is asserted equal to it.
    - Every one of the twelve review findings has exactly one row with an explicit disposition; the count of closed, residual and carried rows is stated.
    - All 34 dispositioned items have a row carrying their dispositioned outcome beside their measured outcome; every FIX item whose measurement did not close it is recorded as still open with an owner.
    - The totals section states the equality and shows it balancing, or names the discrepancy as a finding.
    - The residual register has one subsection per register with every member by id, reason and closure criterion; every CLOSE disposition is named as decided; every OPEN item has an owner.
    - The what-this-round-did-NOT-do section names at minimum the five items listed in MOVEMENT 4.
    - The requirement rows are proven unchanged by a quoted range-pinned `git diff "$ROUND_BASE"..HEAD`, with `ROUND_BASE` pinned to the commit this round branched from; all six UATX rows are still `[ ]`, all six traceability rows still read `Gaps Found`, and the Phase 31 checkbox is still unchecked. If a flip is found and reverted, the revert is recorded as a finding.
    - `31-VALIDATION.md`'s carry-forward counts are read from the file and incremented once; the before and after numbers are quoted.
    - `.planning/STATE.md`'s longest line length is measured and quoted after the write, and the foundation guards' wall-clock duration is recorded as a number of seconds.
    - `git diff --name-only "$PLAN_BASE"..HEAD -- scripts hooks agent-factory install .github vitest.config.ts` is EMPTY and quoted.
    - Any finding this plan raised is recorded with an owner and explicitly NOT repaired here.
  </acceptance_criteria>
  <verify>
    <automated>git diff --name-only "$PLAN_BASE"..HEAD -- scripts hooks agent-factory install .github vitest.config.ts package.json</automated>
    <fails_when>any output at all — a closing measurement that writes source has found a new defect, and that defect belongs in a new plan with its own RED-first reproduction. `PLAN_BASE` is the commit SHA pinned in Task 1 MOVEMENT 1 and MUST be exported before this command runs; an unset `PLAN_BASE` makes the range unresolvable and the command exits non-zero rather than passing vacuously</fails_when>
    <automated>grep -c '^- \[ \] \*\*UATX-0' .planning/REQUIREMENTS.md</automated>
    <fails_when>a printed count other than 6 — a UATX requirement was checked by a gap-closure round, which only a verification round may do</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts scripts/floor-invariance.test.ts</automated>
    <fails_when>non-zero exit, a summary line reporting `0 passed`, or a wall-clock duration for the foundation guards exceeding 60 seconds, which is the pathological-state-line signature this repository has measured before</fails_when>
    <automated>npm run check:audit-register &amp;&amp; npm run check:residual-citations &amp;&amp; npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or any check naming a row in `docs/audit/31-round6-residuals.md`, `deferred-items.md` or `31-VALIDATION.md` that does not resolve</fails_when>
  </verify>
  <done>Every review finding and every dispositioned item carries a row with its measured outcome; the round's residual register is written once with owners and criteria; what the round did not do is stated plainly; and the requirement rows are proven unmoved for the verifier to decide.</done>
</task>

</tasks>

<verification>
- Every round-6 reproduction is re-run with the finding document's own spelling at one pinned commit, paired with its pre-round result.
- Every round-5 closure is re-run as a control in the modules round 6 edited, with moved and unmoved controls reported separately.
- Every behavioral spot-check is re-driven with two counts stated: rows that moved and rows that did not.
- The whole repository is green at that commit and the green is recorded as a floor with the count of Criticals it did not exercise.
- Every frozen floor and every `DECIDER_MANIFEST` entry is re-measured as a whole.
- The no-silent-drop equality is asserted: eight unresolved edge-coverage rows against eight authored flagged assumptions.
- Every one of the twelve review findings and all 34 dispositioned items has exactly one ledger row.
- The requirement rows, the traceability rows and the phase checkbox are proven unchanged.
</verification>

<success_criteria>
- `docs/audit/31-round6-residuals.md` exists and is a document round 7 can start from rather than re-derive.
- No closure is claimed that this plan did not measure; every unrun row says so with a reason.
- Every CLOSE disposition is named as decided and every OPEN item has an owner.
- No source file, test file or configuration file is modified by this plan.
- No requirement, traceability row or phase checkbox is flipped.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-31-SUMMARY.md` when done.
</output>

{
  "command": "npx vitest run --exclude '**/scripts/e2e/**' scripts/nonblocking-reader-parity.test.ts (driven in a detached worktree at d6f2164 with the PRE-FIX predicate and PRE-FIX input boundary restored as two described mutants)",
  "exitCode": 1,
  "output": "TAP version 13\n1..24\nok 1 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived PREMISE: the derivation actually looked at something\nok 2 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived PREMISE: the candidate list is TWO independently derived censuses, and they agree\nok 3 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived the derived implementing-file set has the expected MEMBERS\nok 4 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived the derived CARDINALITY is 2\nok 5 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived a seeded THIRD implementation spelled \"if (!st.isFile()) throw\" moves the derived count 2 -> 3\nnot ok 6 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived a seeded THIRD implementation spelled \"if (st.isFile() === false) throw\" moves the derived count 2 -> 3\nnot ok 7 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived a seeded THIRD implementation spelled \"if (st.isFile() !== true) throw\" moves the derived count 2 -> 3\nnot ok 8 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived a seeded THIRD implementation spelled \"if (st.isFile()) { \u2026 } else { throw }\" moves the derived count 2 -> 3\nnot ok 9 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived every ENUMERATED spelling was driven by a mirror \u2014 the two sets are one derivation\nok 10 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived CONTROL: a function that stats the descriptor but THROWS ON NOTHING is not counted\nok 11 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived CONTROL: a function that refuses a non-regular file but opens BLOCKING is not counted\nok 12 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived a seeded implementation that LOSES its regular-file refusal moves the count 2 -> 1\nnot ok 13 - 31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived an UNTRACKED third implementation under scripts/ is visible to the derivation\nok 14 - 31-27 \u2014 one shared file-shape corpus, the SAME decision required of both \"absent\" produces the SAME decision class from both implementations\nok 15 - 31-27 \u2014 one shared file-shape corpus, the SAME decision required of both \"empty regular file\" produces the SAME decision class from both implementations\nok 16 - 31-27 \u2014 one shared file-shape corpus, the SAME decision required of both \"ordinary regular file\" produces the SAME decision class from both implementations\nok 17 - 31-27 \u2014 one shared file-shape corpus, the SAME decision required of both \"symlink to a regular file\" produces the SAME decision class from both implementations\nok 18 - 31-27 \u2014 one shared file-shape corpus, the SAME decision required of both \"directory\" produces the SAME decision class from both implementations\nok 19 - 31-27 \u2014 one shared file-shape corpus, the SAME decision required of both \"FIFO\" produces the SAME decision class from both implementations\nok 20 - 31-27 \u2014 one shared file-shape corpus, the SAME decision required of both \"unix socket\" produces the SAME decision class from both implementations\nok 21 - 31-27 \u2014 one shared file-shape corpus, the SAME decision required of both \"over-ceiling regular file\" produces the SAME decision class from both implementations\nok 22 - 31-27 \u2014 one shared file-shape corpus, the SAME decision required of both the corpus drove every shape it claims, and the skipped count matches the platform\nok 23 - 31-27 \u2014 each ceiling is stated ONCE, in its own file, and they are NOT equal by rule the wrapper's ceiling is defined exactly once in hooks/hook-entry.ts\nok 24 - 31-27 \u2014 each ceiling is stated ONCE, in its own file, and they are NOT equal by rule the module's note ceiling is defined exactly once in scripts/context-io.ts\n# tests 24\n# pass 19\n# fail 5\n",
  "targetTest": "31-27 \u2014 the non-blocking read rule has exactly TWO implementations, derived an UNTRACKED third implementation under scripts/ is visible to the derivation",
  "targetFile": "scripts/nonblocking-reader-parity.test.ts",
  "adapter": "The classifier parses node:test TAP; this repository runs vitest. The TAP body is DERIVED from the same run's --reporter=json transcript: one ok/not-ok line per executed assertionResult plus the three summary counters. No line is hand-written and no count is re-typed. Disclosed per the plan's TDD rules. SECOND DISCLOSURE, specific to this task: the predicate under test is TEST-LOCAL, so there is no production module to revert. The RED was driven in a detached worktree at d6f2164 with the new cases present and TWO described mutants applied to the file under test \u2014 the pre-fix one-operator refusal recognition restored, and candidateSources restored to `git ls-files` only. 19 cases stayed green, including the whole AXIS 2 corpus and the `negated-call` mirror."
}

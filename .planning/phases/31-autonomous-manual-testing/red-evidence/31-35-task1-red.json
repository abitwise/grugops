{
  "command": "npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts -t 31-35",
  "exitCode": 1,
  "output": "TAP version 13\n1..8\nok 1 - uat-spec-integrity — 31-35 CR-25: a truncated framework walk is a COULD-NOT-RUN UNDER the bound: the chain is REFUSED by identity, at exit 1\nnot ok 2 - uat-spec-integrity — 31-35 CR-25: a truncated framework walk is a COULD-NOT-RUN OVER the bound: the walk stops early and the run COULD NOT RUN, at exit 2\nnot ok 3 - uat-spec-integrity — 31-35 CR-25: a truncated framework walk is a COULD-NOT-RUN THE DIRECTION: the band never regresses to an ACCEPT, at any chain length\nnot ok 4 - uat-spec-integrity — 31-35 CR-25: a truncated framework walk is a COULD-NOT-RUN THE CONFOUND: the report does not depend on how the surface is declared across FILES\nnot ok 5 - uat-spec-integrity — 31-35 CR-25: a truncated framework walk is a COULD-NOT-RUN THE SPELLING BACKSTOP: a plain `test` head is refused by SPELLING, so CR-25's filed shape is not the live one\nok 6 - uat-spec-integrity — 31-35 CR-25: a truncated framework walk is a COULD-NOT-RUN PREMISE: both members of the flip pair TYPE-CHECK, so neither rests on a refused construct\nnot ok 7 - uat-spec-integrity — 31-35 CR-25: a truncated framework walk is a COULD-NOT-RUN THE NODE BOUND sets the same signal as the DEPTH bound, driven by a WIDE surface\nnot ok 8 - uat-spec-integrity — 31-35 CR-25: a truncated framework walk is a COULD-NOT-RUN HEADROOM: the compiler's own library is not the framework's surface\n# tests 8\n# pass 2\n# fail 6\n",
  "targetTest": "uat-spec-integrity — 31-35 CR-25: a truncated framework walk is a COULD-NOT-RUN OVER the bound: the walk stops early and the run COULD NOT RUN, at exit 2",
  "targetFile": "scripts/runnable-ref/uat-spec-integrity.test.ts",
  "adapter": "The classifier parses node:test TAP; this repository runs vitest. The TAP body is DERIVED from the same run's --reporter=json transcript: one ok/not-ok line per executed assertionResult plus the three summary counters. No line is hand-written and no count is re-typed. Disclosed per the plan's TDD rules."
}
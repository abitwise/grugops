---
phase: 31-autonomous-manual-testing
plan: 38
type: execute
wave: 7
depends_on: ["31-37"]
gap_closure: true
files_modified:
  - docs/audit/31-round7-residuals.md
  - .planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
  - .planning/phases/31-autonomous-manual-testing/deferred-items.md
  - .planning/STATE.md
autonomous: true
requirements: [UATX-01, UATX-02, UATX-03, UATX-04, UATX-05, UATX-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every reproduction `31-REVIEW.md` (round 6's code review) and `31-VERIFICATION.md` (round 7) recorded is RE-RUN at ONE commit against the committed `.js`, using the exact probe spellings THOSE documents used — not paraphrases and not the fix plans' own fixtures. A closure round that re-measures its own tests measures the fix, not the finding. This is the rule that made rounds 5 and 6's closing measurements the strongest artifacts this phase has produced, and it is the rule this plan inherits."
    - "The re-run set is not only round 7's. Round 6's five closures — CR-17, CR-18, CR-19, CR-20, CR-21 — are re-driven as CONTROLS at the same commit, because round 7's fixes edit the very modules those closures live in, and this phase's recorded pattern across seven rounds is that a fix creates its successor's defect. A closure that reports only the rows it moved cannot show it broke nothing."
    - "Where round 7's verifier ACCEPTED a round-6 closure from `31-REVIEW.md`'s measurement rather than re-driving it (CR-17, CR-18, CR-19, CR-21 — four of the five), this plan re-drives it independently or records plainly that it did not and why. An accepted closure carried forward twice without a measurement is how a phase convinces itself."
    - "Every finding of `31-REVIEW.md` round 6 — CR-22, CR-23, CR-24, CR-25, the Skipped-entries finding, WR-31, WR-32, WR-33, WR-34, WR-35, WR-36, IN-16, IN-17, IN-18 — and the suite-RED regression `31-VERIFICATION.md` round 7 recorded as a regression, carries an explicit disposition in a written ledger: CLOSED with the measurement that proves it, DECIDED-AND-NAMED as a residual with the reason, or CARRIED with an owner and a criterion. No finding is left without a row, and no row asserts a closure this round did not measure."
    - "The whole excluded-e2e suite is measured GREEN at ONE commit with the exact command `npx vitest run --exclude '**/scripts/e2e/**'`, and the pass/fail/skip counts and file counts are recorded verbatim. This matters more this round than in any prior one: the suite was measured RED at the round base for the first time in this phase's seven verification rounds, and `31-32` is the plan that fixed it. A green suite is NOT the proof — this project's doctrine, established across seven rounds on this phase, is that the suite exercised NONE of the four Criticals found in round 7 — but a RED suite is a disqualifier, and the phase's repeated 'full suite green' claim was FALSE at the commit round 7 measured."
    - "The frozen floors are re-measured rather than assumed: `hooks/guard.ts` hashes equal to `FROZEN_GUARD_BLOB`; `FROZEN_HOOK_ENTRY_LOGIC_SHA` equals a fresh normalisation of the wrapper at its re-taken value with the removed-byte floor printed first; and every `DECIDER_MANIFEST` entry equals the current digest of the file it names. The manifest is checked as a WHOLE, not only for the entries this round moved, because a stale entry elsewhere is the same class of drift."
    - "The no-silent-drop equality is asserted as a COVERAGE equality, not a cardinality one. The report rows are derived from the phase's own edge-coverage record and the authored flagged assumptions are derived from the round's seven plan files; the assertion is that the count of report rows covered by at least one authored flagged assumption equals the derived row count and the count covered by NONE equals zero. Both numbers are MEASURED at run time on both sides — a hand-typed number on either side is the drift class this round spent a plan deleting."
    - "The residual register for round 7 is written down once, in a tracked document, listing every boundary the round's six fix plans decided to leave open — including the installed-package magnitude behind `SURFACE_DEPTH_BOUND`/`SURFACE_NODE_BOUND`, whatever the ban-disposition checkpoint refused, every `hosts: non-cc-hook-path` member, the write-path residuals after `31-33`, the Windows remainder, and the corpus-manifest append obligation `31-32` created — each with the reason and what would force it closed, so round 8's reviewer starts from a register rather than rediscovers it."
    - "UATX-01 through UATX-06 stay UNCHECKED in `.planning/REQUIREMENTS.md`, the Phase 31 checkbox stays unchecked in `.planning/ROADMAP.md`, and every traceability row still reads `Gaps Found`. Only a verification round may flip a requirement — and this phase has now had seven rounds in which the executing round believed it had closed the requirement."
    - "The three standing human-verification items are carried forward with their `UNKNOWN - verify` markers intact: the attended Chrome lane's real interactive behaviour (R-01), the `claude auth status --json` predicate under API-key and long-lived-token auth (R-02), and the Windows leg of every browser probe and of this round's own four findings (R-03). None is closed by inference and none is dropped."
    - "This plan writes no source and no test. It measures. A closing measurement that fixes something has found a new defect, and a new defect belongs in a new plan with its own RED-first reproduction — the standing rule `docs/audit/31-round5-residuals.md` §12 item 6 recorded and every closing plan since has inherited. Anything found here is recorded with an owner rather than repaired."
    - "The measurement environment is proven clean by predicates that can OBSERVE it: `.temp/` empty by a real listing, no named pipe anywhere in the tree, and the `git status`-based residue predicate demonstrated inert. The `.planning/STATE.md` write is VERIFIED rather than assumed — this repository's state writer re-escapes backslashes on every write, and a pathological line length there has previously combined with a superlinear guard predicate to turn a sub-second gate into a multi-minute one."
  artifacts:
    - docs/audit/31-round7-residuals.md
    - .planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
    - .planning/phases/31-autonomous-manual-testing/deferred-items.md
    - .planning/STATE.md
  key_links:
    - "each round-7 reproduction <-> its post-round measurement: the pairing that makes a closure claim checkable by a later reader without re-deriving the probe."
    - "each round-6 closure <-> its re-run as a CONTROL: the both-directions record showing what moved AND what did not, in the modules round 7 edited."
    - "every review finding, the regression, and every dispositioned item <-> a ledger row: the binding that makes a finding without a disposition impossible to ship."
    - "the derived edge-coverage rows <-> the flagged assumptions derived from this round's plan files: a COVERAGE mapping asserted row by row, both sides measured, so a dropped row is red rather than absent."
    - "the round's six fix plans' residuals <-> one tracked register: the artifact that turns round 8's rediscovery into a lookup."
    - "the requirement rows <-> the verification round that may flip them: the discipline that keeps a gap-closure plan from certifying itself."
  prohibitions:
    - statement: "A gap-closure plan must never flip a requirement checkbox, a traceability row or the phase checkbox. Only a verification round may do that."
      status: unverified
      verification: flagged
    - statement: "This plan writes no source and no test. A defect found here is recorded with an owner, never repaired in place."
      status: unverified
      verification: flagged
    - statement: "Never fake a passing gate, a test result, or a citation. A reproduction that was not re-run is recorded as not re-run, with the reason, never as closed."
      status: unverified
      verification: flagged
    - statement: "A green test suite is a FLOOR recorded beside the adversarial reproductions, never in place of them."
      status: unverified
      verification: flagged
    - statement: "A prior round's SUMMARY, PLAN, REVIEW or VERIFICATION is history and is never rewritten. A disagreement with it is annotated in this round's own record."
      status: unverified
      verification: flagged
    - statement: "A human-verification item is carried forward with its `UNKNOWN - verify` marker intact unless a harness measured it closed, and then the measurements are quoted."
      status: unverified
      verification: flagged
    - statement: "`hooks/guard.ts` is not touched and `FROZEN_GUARD_BLOB` is never re-based; a frozen manifest entry is moved with its artifact, never relaxed."
      status: unverified
      verification: flagged
    - statement: "`.planning/STATE.md` is written with short, plain lines and no backslash-heavy content, and the result is re-read and measured after the write."
      status: unverified
      verification: flagged
    - statement: "Never merge a protected branch and never deploy to production without a named human confirmation."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-31-PLAN.md: the closing-measurement role — re-run the findings' own probes, not the plans' fixtures; record a disposition for every finding; leave the requirement rows for the verifier. This plan is that role performed for round 7."
    - "31-32..31-37: each fix plan's own prohibitions, which this plan re-measures rather than re-asserts."
---

<objective>
Measure round 7 at one commit, with the findings' own probe spellings, and write the record round 8
starts from.

Round 7 was convened on four Criticals (`CR-22`, `CR-23`, `CR-24`, `CR-25`), one Skipped-entries
finding, six Warnings (`WR-31`…`WR-36`), three Info items (`IN-16`…`IN-18`), and — new in this phase's
history — a REGRESSION: the full excluded-e2e suite measured RED at the round base, on a commit that
changed zero source bytes, because a coverage test's oracle was a planning artifact this project
rewrites every round.

This plan re-runs every one of those reproductions and every round-6 closure at one commit, records
both results, dispositions every item, re-measures the frozen floors and the whole decider manifest,
asserts the no-silent-drop coverage equality with both sides derived, and writes
`docs/audit/31-round7-residuals.md`.

It writes no source. If it finds something, it records it with an owner.

Purpose: this phase has produced six closing measurements and seven verification rounds; the value of
this artifact is that a round-8 reviewer can check every claim in it without re-deriving a probe.
Output: `docs/audit/31-round7-residuals.md`; an updated `31-VALIDATION.md`, `deferred-items.md` and
`.planning/STATE.md`. No new decision id is recorded — a closing measurement decides nothing.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@.planning/phases/31-autonomous-manual-testing/31-REVIEW.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
@.planning/phases/31-autonomous-manual-testing/deferred-items.md
@docs/audit/31-round6-residuals.md
@docs/audit/31-round5-residuals.md
@docs/audit/harness-false-result-instances.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Note |
|---|---|---|
| `docs/audit/31-round7-residuals.md` | NEW tracked document | The round's whole record: the probe set and its derivation rule, the reproduction pairing table, the round-6 control ledger, the disposition ledger for all fifteen items, the one-commit gate record, the frozen-floor re-measurements, the coverage equality, the residual register, and the environment-cleanliness proof. Section shape follows `docs/audit/31-round6-residuals.md`. |
| the round-7 carry-forward table in `31-VALIDATION.md` | CHANGED | R-01, R-02 and R-03 carried with their `UNKNOWN - verify` markers and named owners. |
| `deferred-items.md` | CHANGED | Any item this round carried with an owner and a criterion, including the corpus-manifest append obligation `31-32` created and the GOV-02 shapes-position restore criterion, re-stated rather than duplicated. |
| `.planning/STATE.md` | CHANGED | Short, plain lines only. No backslash-heavy content; the written result is re-read and its longest line measured. |

<tasks>

<task type="auto">
  <name>Task 1: Re-run every round-7 reproduction and every round-6 closure at ONE commit, with the findings' own spellings</name>
  <files>docs/audit/31-round7-residuals.md</files>
  <precondition>Every probe runs against the COMMITTED `.js` at one pinned commit with a clean tree over `scripts hooks agent-factory docs .github vitest.config.ts package.json`, verified with `git status --porcelain` before the first probe and after the last. Every AST probe root is equipped exactly as `31-28`'s `equipTarget` equips one per `docs/audit/31-round6-residuals.md` §1.3, and every `context-io` probe root is created with `mkdtemp` and asserted `governanceRootOf(store) === root` before any result is read.</precondition>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — every reproduction transcript in CR-22, CR-23, CR-24, CR-25, WR-31, WR-32, WR-33, WR-34, WR-35, WR-36, IN-16, IN-17 and IN-18, together with the exact command spellings each used and the `Verification notes` section listing the probe-environment constraints.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the whole `Behavioral Spot-Checks` table (all twelve rows with their commands and outputs), the `re_verification.gaps_closed` block naming which round-6 closures were and were NOT independently re-driven, the `regressions` block, and the `Required Artifacts` table.
    - `docs/audit/31-round6-residuals.md` — §1 (the probe set and its derivation rule), §1.3 (the probe environment), §2 (the reproduction pairing table shape), §3 (the round-5 control ledger shape), §7 (the environment-cleanliness predicates) and §8.3 (the frozen-floor re-measurement shape). This plan reuses those shapes rather than inventing new ones.
    - The six round-7 fix-plan summaries `31-32-SUMMARY.md` … `31-37-SUMMARY.md` — for what each claims, so this plan can compare a claim against its own re-measurement rather than repeat it.
  </read_first>
  <action>
    MOVEMENT 1 — derive the probe set rather than list it. Walk `31-REVIEW.md` and `31-VERIFICATION.md`
    for every recorded reproduction and every spot-check row, and produce the probe list from that walk.
    Print the derived count and assert it non-zero before running anything. State the derivation rule in
    §1 of the record, and state by name what is EXCLUDED and why, so the exclusion is not silent.

    MOVEMENT 2 — pin the commit and prove the tree. Record the commit sha, `git status --porcelain` over
    the source paths before the first probe, and the same after the last. Every result in this document
    is a result at that commit.

    MOVEMENT 3 — re-run the round-7 reproductions with THEIR OWN spellings. For each of CR-22 (all four
    positions), CR-23 (both instances), CR-24 (the three-plant render), CR-25 (the boundary pair), the
    suite-RED regression, WR-31 (both control positions), WR-32 (the three unrecognised spellings and the
    untracked candidate), WR-33 (the six greps), WR-34 (the `factory.config.json` grep), WR-35 (the two
    denominator runs), WR-36 (the shared candidate corpus), IN-16 (the repository-wide grep), IN-17 (the
    doc block) and IN-18 (the seam's on-disk artifact): run the document's own probe, record the
    pre-round result as the document recorded it and the post-round result as measured now, side by side
    in one table. Where a probe cannot be re-run, say so and give the reason; never infer.

    MOVEMENT 4 — re-drive round 6's five closures as CONTROLS. CR-17 (the bounded named deny at every
    derived manifest position, plus the fd-0 class), CR-18 and CR-21 (the two evasions the identity
    cutover closed), CR-19 (the write-side ceiling) and CR-20 (the gated-arm promotion). Round 7's
    verifier re-drove only CR-20 independently and accepted the other four from `31-REVIEW.md`'s
    measurement; re-drive all five here, or state per row that it was not re-driven and why. Record which
    of the five had NOT been independently measured since round 6, so the record shows how much of the
    phase's belief rests on one reading.

    MOVEMENT 5 — record two counts separately, never one. State the number of probes re-run and the
    number NOT re-run, with the second list enumerated by name. A single blended number is what lets a
    partial measurement read as a complete one.
  </action>
  <acceptance_criteria>
    - §1 states the derivation rule, prints the derived probe count, asserts it non-zero, and enumerates the exclusions by name with reasons.
    - The pinned commit sha and both `git status --porcelain` readings are recorded.
    - The reproduction pairing table carries one row per derived probe with: the finding id, the document the spelling came from, the pre-round result as that document recorded it, the post-round result measured now, and the verdict.
    - Every AST probe row records its own `tsc --noEmit` exit code; every `context-io` probe row records the `governanceRootOf(store) === root` premise assertion for each root it used.
    - The round-6 control ledger carries one row per closure with UNMOVED or MOVED and the measurement, and names which had not been independently measured since round 6.
    - The two counts — re-run and not re-run — are stated separately, with the not-re-run list enumerated.
    - No source or test file is modified by this task; `git diff --name-only HEAD -- scripts hooks agent-factory install` is empty.
  </acceptance_criteria>
  <verify>
    <automated>git status --porcelain -- scripts hooks agent-factory install .github vitest.config.ts package.json</automated>
    <fails_when>the command prints any line — the measurement tree was not clean, so every result in the record is taken at an unknown state</fails_when>
    <automated>test -z "$(git diff --name-only HEAD -- scripts hooks agent-factory install)"</automated>
    <fails_when>non-zero exit — this closing measurement modified source, which its own prohibition forbids</fails_when>
    <automated>test -z "$(find . -path ./node_modules -prune -o -type p -print)" &amp;&amp; test -z "$(find .temp -mindepth 1 -print -quit)"</automated>
    <fails_when>non-zero exit — a named pipe survived anywhere in the tree, or `.temp/` still holds an entry by a real listing</fails_when>
  </verify>
  <done>Every reproduction this round was convened on, and every closure the previous round claimed, is measured at one commit with the finding documents' own spellings, and what was not measured is named.</done>
  <reversibility rating="reversible">A measurement record. Reverting loses the round's evidence.</reversibility>
</task>

<task type="auto">
  <name>Task 2: A disposition for every item, the one-commit gate record, the frozen floors whole, and the coverage equality</name>
  <files>docs/audit/31-round7-residuals.md</files>
  <read_first>
    - Task 1's pairing and control tables, drafted in the record.
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — every finding heading, so the disposition ledger's denominator is derived from the document rather than typed.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the `regressions` block and the three `human_verification` items in full.
    - `docs/audit/31-round6-residuals.md` §8 (the one-commit gate record and its doctrine paragraph), §8.3 (the frozen floors), §8.5 (the exported register cardinalities) and §9 (the coverage equality and how both sides were derived).
    - `scripts/floor-invariance.test.ts` — `FROZEN_GUARD_BLOB` and `FROZEN_HOOK_ENTRY_LOGIC_SHA` as `31-37` left them, plus the normalisation's byte-count floor.
    - `hooks/hook-entry.ts` — `DECIDER_MANIFEST` in full, so every entry is checked and not only those this round moved.
    - The six round-7 plan files `31-32-PLAN.md` … `31-37-PLAN.md` — the flagged assumptions this round authored, which are one side of the coverage equality and must be DERIVED from those files rather than counted by hand.
  </read_first>
  <action>
    MOVEMENT 1 — build the disposition ledger with a DERIVED denominator. Walk `31-REVIEW.md`'s own
    finding headings to produce the item list, add the regression `31-VERIFICATION.md` recorded and the
    Skipped-entries finding, print the derived count, and assert every item has exactly one row. Each row
    carries: the item id, its dispositioned outcome from the plan that owned it, its measured outcome
    now, and one of CLOSED / ACCEPTED-AS-RESIDUAL / CARRIED-WITH-OWNER. An item dispositioned FIX whose
    measurement did not close it is recorded as still open with an owner, never as closed because a plan
    ran.

    MOVEMENT 2 — the one-commit gate record, as a FLOOR. Run, at the pinned commit and in one pass:
    the build, the typecheck, build-parity, freshness, the hook-manifest freshness, the foundation
    guards, the UAT oracles, the platform shapes, the fixtures and tests typecheck projects, and the full
    excluded-e2e suite with the exact command `npx vitest run --exclude '**/scripts/e2e/**'`. Record every
    command and its exit code, and record the suite's file and test counts verbatim. Then write the
    doctrine paragraph BESIDE the green, derived rather than quoted: name which constants and code paths
    the four round-7 Criticals are decided by today and state how many times each appeared in the suite
    at the round base. Note explicitly that the suite was RED at that base and which plan fixed it.

    MOVEMENT 3 — the frozen floors, re-measured whole. `hooks/guard.ts` hashed against `FROZEN_GUARD_BLOB`;
    a fresh normalisation of `hooks/hook-entry.ts` against `FROZEN_HOOK_ENTRY_LOGIC_SHA` with the total
    and removed byte counts printed BEFORE the digest is read and the removed count asserted non-zero;
    and every `DECIDER_MANIFEST` entry against a current digest of the file it names, with the entry count
    printed. Record the manifest's baseline movements this round and which plan moved each.

    MOVEMENT 4 — the exported register cardinalities on this tree: `UNRESOLVABLE_CALLEE_RESIDUALS`,
    `NOTE_SKIP_ARMS`, `TRUSTED_ROOT_TIERS`, `TRUSTED_ROOT_RESIDUALS`, `WRITE_PATH_RESIDUALS`,
    `PROMOTE_ADMITTED_DECLINES`, `PROMOTE_ADMITTED_RESIDUALS`, `BANNED_MODIFIER_HEADS`,
    `BANNED_EXACT_PATHS` and `SKIPPED_DIRECTORIES`, each measured and compared against the number
    `31-31` recorded, with every difference attributed to the plan that made it.

    MOVEMENT 5 — the no-silent-drop equality, both sides DERIVED. Derive the report rows from the phase's
    own edge-coverage record and the authored flagged assumptions from the six round-7 plan files by
    walking them, print both derived counts, then assert: the count of report rows covered by at least one
    authored assumption equals the derived row count, and the count covered by NONE equals zero. State
    plainly that a plan authoring more assumptions than the report asked for is not a defect and is not
    counted as one. Neither side may be a number typed into this document.

    MOVEMENT 6 — the residual register. List every boundary the round's six fix plans left open, each with
    its reason and what would force it closed: the installed-package magnitude behind the two surface
    bounds, whatever the `31-34` ban-disposition checkpoint refused, every `hosts: non-cc-hook-path`
    member, the write-path residuals after `31-33`, the corpus-manifest append obligation `31-32` created,
    the GOV-02 shapes-position restore criterion, and the Windows remainder. Add any item this plan itself
    found, with an owner, unrepaired.
  </action>
  <acceptance_criteria>
    - The disposition ledger's denominator is derived from `31-REVIEW.md`'s own headings, the derived count is printed, and every one of the fifteen items has exactly one row.
    - The one-commit gate record lists every command with its exit code, and the suite line quotes the file and test counts verbatim from `npx vitest run --exclude '**/scripts/e2e/**'`.
    - The doctrine paragraph is DERIVED: it names the constants and paths the four Criticals are decided by and how many times each appeared in the suite at the round base.
    - The round-base RED suite reading and the plan that fixed it are both recorded.
    - `hooks/guard.ts` hashes to `669725bc1c616ab57123e22090d93d57eff1b001`; the wrapper normalisation prints total and removed byte counts before its digest; every `DECIDER_MANIFEST` entry is checked with the entry count printed.
    - Every listed register's cardinality is measured and compared to `31-31`'s number, with each difference attributed to a plan.
    - Both sides of the coverage equality are derived at run time, both counts printed, and the two asserted numbers stated.
    - The residual register lists every open boundary with a reason and a closing criterion.
    - No source or test file is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness &amp;&amp; npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit, the build-parity or freshness step naming a committed `.js` that differs from a rebuild of its source, or the hook-manifest step reporting a hash that does not match a fresh derivation</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the summary line reporting any value other than `0 failed` for test files and for tests</fails_when>
    <automated>node scripts/check-foundation-guards.js &amp;&amp; node scripts/check-uat-oracles.js &amp;&amp; node scripts/check-platform-shapes.js &amp;&amp; npx tsc -p tsconfig.tests.json &amp;&amp; npx tsc -p tsconfig.fixtures.json</automated>
    <fails_when>non-zero exit, or the platform-shapes gate printing `not refused (correct)` for a row whose verdict is a refusal</fails_when>
    <automated>npm run check:diff-disposition &amp;&amp; npm run check:claim-anchors &amp;&amp; npm run check:audit-register &amp;&amp; npm run check:residual-citations &amp;&amp; npm run check:banned-claims &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the disposition gate reporting a count greater than the one `31-31` recorded</fails_when>
  </verify>
  <done>Every item the round was convened on has one dispositioned row, the whole repository is measured at one commit with the suite green and recorded as a floor, the frozen floors and every manifest entry are re-measured whole, and the coverage equality is asserted with both sides derived.</done>
  <reversibility rating="reversible">A measurement record. Reverting loses the round's evidence.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Carry the human items, update the validation and deferred records, and write a short STATE.md</name>
  <files>.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md, .planning/phases/31-autonomous-manual-testing/deferred-items.md, .planning/STATE.md, docs/audit/31-round7-residuals.md</files>
  <precondition>`.planning/STATE.md` is written by a state writer that re-escapes backslashes on every write; a pathological line length there has previously combined with a superlinear guard predicate to turn a sub-second gate into a multi-minute one. The write must use short, plain lines with no backslash-heavy content, and the file must be re-read and its longest line measured AFTER the write.</precondition>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the three `human_verification` items in full with their `why_human` text and their carry-forward history.
    - `.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md` — the round-7 carry-forward table and the test-infrastructure block, so the update extends the existing shape.
    - `.planning/phases/31-autonomous-manual-testing/deferred-items.md` — every existing entry, so a carried item is re-stated rather than duplicated.
    - `.planning/STATE.md` — the frontmatter fields and the current `status` and `stopped_at` values, so the replacement is short and in the same shape.
    - `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` — read only, to confirm the six requirement rows and the phase checkbox before and after.
  </read_first>
  <action>
    MOVEMENT 1 — carry the human items unchanged. Record R-01, R-02 and R-03 in `31-VALIDATION.md`'s
    carry-forward table with their `UNKNOWN - verify` markers intact and a named owner each. R-03's
    remainder is stated at whatever `31-30` measured it down to; none of the three is closed by inference.

    MOVEMENT 2 — update `deferred-items.md`. Add any item this round carried with an owner and a
    criterion — including the corpus-manifest append obligation `31-32` created and anything this plan
    itself found and did not repair — and re-state rather than duplicate the entries already there.

    MOVEMENT 3 — write `.planning/STATE.md`. Short, plain sentences. State: round 7 executed in full
    (`31-32`…`31-38`, seven plans, sequential); which findings closed by measurement and which are
    carried; that the suite was RED at the round base and is green at the closing commit with its
    counts; that UATX-01 through UATX-06 stay unchecked and Gaps Found; that the phase stays In Progress
    because only a verification round may flip a requirement; and where the record is. Then re-read the
    file, measure its longest line, and record the number.

    MOVEMENT 4 — confirm the requirement rows did not move. Diff `.planning/REQUIREMENTS.md` and
    `.planning/ROADMAP.md` against the round base and record that both are byte-unchanged over the
    requirement rows and the phase checkbox. If the ROADMAP's Phase 31 plan list needs the round-7 plans
    appended, that is a list entry, not a checkbox — make it as an unchecked entry and say so.

    MOVEMENT 5 — prove the environment clean with predicates that can OBSERVE it: `.temp/` by a real
    listing, a named-pipe sweep over the whole tree excluding `node_modules`, and a demonstration that
    the `git status`-based residue predicate is inert on a gitignored path. Record all three outputs in
    §7 of the record.
  </action>
  <acceptance_criteria>
    - R-01, R-02 and R-03 appear in `31-VALIDATION.md` with their `UNKNOWN - verify` markers intact and a named owner each; none is marked closed.
    - `deferred-items.md` carries every item this round left open, each with an owner and a criterion, with no duplicated entry.
    - `.planning/STATE.md` is written with short plain lines; the longest line length is measured after the write and recorded, and no line contains a backslash run.
    - `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` are byte-unchanged over the six requirement rows, the traceability rows and the Phase 31 checkbox, proven by diff.
    - The three environment-cleanliness predicates are run and their outputs recorded, including the demonstration that the residue predicate is inert.
    - `docs/audit/31-round7-residuals.md` is complete: probe set, pairing table, control ledger, disposition ledger, gate record, frozen floors, register cardinalities, coverage equality, residual register, environment proof.
  </acceptance_criteria>
  <verify>
    <automated>awk '{ if (length($0) &gt; m) m = length($0) } END { print "longest STATE.md line:", m; exit (m &gt; 4000) }' .planning/STATE.md</automated>
    <fails_when>non-zero exit, or the printed longest-line length exceeding 4000 characters — the state writer's backslash re-escaping has previously produced pathological lines that turn a sub-second guard into a multi-minute one</fails_when>
    <automated>grep -c '\\\\\\\\' .planning/STATE.md ; test "$(grep -c '\\\\\\\\' .planning/STATE.md)" = "0"</automated>
    <fails_when>non-zero exit — the state file contains a doubled-backslash run, the signature of the re-escaping defect this precondition exists for</fails_when>
    <automated>git diff --quiet HEAD -- .planning/REQUIREMENTS.md &amp;&amp; git ls-files --error-unmatch docs/audit/31-round7-residuals.md</automated>
    <fails_when>non-zero exit — a requirement row moved inside a gap-closure plan, or the round's record is untracked</fails_when>
    <automated>npm run freshness:traceability &amp;&amp; npm run check:nul-bytes</automated>
    <fails_when>non-zero exit, or the traceability check reporting a Phase 31 row whose status is not `Gaps Found`</fails_when>
  </verify>
  <done>The three human items are carried with their markers intact, every open item has an owner and a criterion, the state file is short and measured, the requirement rows are proven unmoved, and the round's record is complete and tracked.</done>
  <reversibility rating="reversible">Record updates. Reverting loses the round's carry-forward and returns round 8 to rediscovery.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a probe's own harness → the result this record publishes | fourteen logged instances in this repository show a harness producing a false result; every probe's premise is asserted before its conclusion is read |
| this record → round 8's reviewer | the residual register and the disposition ledger are what a later reader starts from; an unsubstantiated row is a false premise handed forward |
| `.planning/STATE.md`'s writer → the repository's own gates | the state writer re-escapes backslashes on every write, and a pathological line has previously turned a sub-second guard into a multi-minute one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-38-01 | Repudiation | the closure claims in this record | high | mitigate | A closure claimed without a measurement is the failure this phase's requirement is about. Every row pairs a pre-round result from the finding's own document with a post-round measurement taken here, and probes not re-run are enumerated by name. |
| T-31-38-02 | Repudiation | the derived denominators | high | mitigate | A ledger, a coverage equality or a probe set with a silently short denominator reads as complete. Every derivation prints its count and asserts it non-zero — and for the coverage equality both sides are derived, never typed. |
| T-31-38-03 | Denial of service | the `.planning/STATE.md` write | medium | mitigate | The write uses short plain lines and the result is re-read, its longest line measured and its doubled-backslash count asserted zero. |
| T-31-38-04 | Tampering | probe residue left in the tree | medium | mitigate | Every probe runs under a scratch root outside the repository; `.temp/` is checked by a real listing, a named-pipe sweep covers the whole tree, and the `git status`-based residue predicate is demonstrated inert rather than trusted. |
| T-31-38-05 | Elevation of privilege | a closing plan repairing what it finds | high | mitigate | This plan writes no source or test; the prohibition is enforced by a verify step asserting `git diff --name-only` over the source paths is empty. A defect found here gets an owner and a row. |
| T-31-38-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan runs NO package-manager install. `git diff -- package.json package-lock.json` is asserted EMPTY, and the round's whole `package.json` diff from base to head is recorded in the gate record as the round-scope supply-chain measurement. |
</threat_model>

<verification>
- Every derived probe was re-run at one pinned commit with a clean tree proven before and after, and the not-re-run set is enumerated.
- Round 6's five closures are re-driven as controls, with the four the round-7 verifier accepted rather than measured named explicitly.
- Every one of the fifteen items has exactly one disposition row over a derived denominator.
- `npx vitest run --exclude '**/scripts/e2e/**'` reports zero failed files and zero failed tests, with counts recorded verbatim.
- `hooks/guard.ts` hashes to `FROZEN_GUARD_BLOB`; the wrapper normalisation prints its byte-count floor first; every `DECIDER_MANIFEST` entry is checked.
- Both sides of the coverage equality are derived at run time and the two asserted numbers are stated.
- `.planning/STATE.md`'s longest line is measured and its doubled-backslash count is zero.
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`'s requirement rows, traceability rows and phase checkbox are byte-unchanged.
- `git diff --name-only HEAD -- scripts hooks agent-factory install` is empty for this plan.
</verification>

<success_criteria>
- `docs/audit/31-round7-residuals.md` exists, is tracked, and carries the probe set, the reproduction pairing table, the round-6 control ledger, the disposition ledger, the one-commit gate record, the frozen-floor re-measurements, the register cardinalities, the coverage equality, the residual register and the environment proof.
- The full excluded-e2e suite is green at the closing commit, with its counts recorded, and the round-base RED reading is recorded beside it.
- Every CR-22, CR-23, CR-24, CR-25, the Skipped-entries finding, the regression, WR-31 through WR-36 and IN-16 through IN-18 carries a disposition.
- R-01, R-02 and R-03 are carried with their `UNKNOWN - verify` markers intact.
- `31-VALIDATION.md`, `deferred-items.md` and `.planning/STATE.md` are updated; STATE.md is short and measured.
- No requirement checkbox, traceability row or phase checkbox moved, and no source or test file was written by this plan.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-38-SUMMARY.md` when done.
</output>

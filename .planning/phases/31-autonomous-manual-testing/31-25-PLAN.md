---
phase: 31-autonomous-manual-testing
plan: 25
type: execute
wave: 5
depends_on: ["31-24"]
gap_closure: true
files_modified:
  - scripts/runnable-ref/uat-spec-integrity.ts
  - scripts/runnable-ref/uat-spec-integrity.js
  - scripts/runnable-ref/uat-spec-integrity.test.ts
  - agent-factory/checklists/browser-uat-recipe.md
  - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
autonomous: true
requirements: [UATX-06]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-28: the D-12 exit-code contract `{0,1,2}` is held at TWO decided boundaries and nowhere else. Per file, EVERYTHING this runnable does with a spec's bytes — the read, the PARSE, the diagnostics inspection and the walk — sits inside ONE `try` whose `catch` records a could-not-run reason and does not increment `visited`. Per process, `main`'s whole body sits inside ONE outer `try` whose `catch` writes a named reason to stderr and returns 2. Every exit then passes through a decided branch rather than through the interpreter's default."
    - "CR-15, the blocker this plan exists for: `31-17`'s WR-19 fix bounded `calleeDottedPath`, de-recursed the AST walk with `forEachDescendant`, and wrapped `findBannedConstructs` in a could-not-run boundary — but left `ts.createSourceFile`, the recursive-descent parser running on author-controlled source, OUTSIDE that boundary, one line above the `try`. `31-VERIFICATION.md` behavioral spot-check row 12 reproduced WR-19's exact harm at a QUARTER of the round-4 probe's depth: a spec containing 1,000 nested parentheses yields an uncaught `RangeError: Maximum call stack size exceeded` at the parser's `token()`, EMPTY stdout, exit 1, and NO measurement on EITHER stream — `reportMeasured` is never reached, so neither its stdout branches nor its stderr floors produce a line."
    - "The harm is not slowness and it is not the exception: it is that `reportMeasured` is never reached, so the vacuity floor and the denominator floor — the two branches whose entire purpose is to make a check that did not run unreadable as a clean one — are bypassed BY CONSTRUCTION while stdout stays silent, and exit 1 is inside `{0,1,2}` only by Node's uncaught-exception accident, which the D-12 contract reads as 'a finding, the gate blocks'."
    - "`deriveSpecPaths`'s directory `walk` is the SECOND unguarded self-recursion in the same file and is de-recursed in the same edit, so the fix covers the CLASS rather than the one call the review reproduced — the identical reasoning D-21 (1) used when it replaced both self-recursive AST walks rather than only the one WR-19 named."
    - "The recipe paragraph this round's own `31-17` added — 'The checker holds these three codes by decision on every path it can take, including a pathological one: it does not exit through an uncaught exception' — is FALSE as written and is corrected in the same plan as the mechanism. The corrected paragraph names the two boundaries that now hold the contract and DISCLOSES what still sits outside any `try` — a fault that terminates the process without unwinding (an out-of-memory kill, a signal) — rather than restating an absolute the mechanism cannot deliver."
    - "The exit-code partition is asserted over a PATHOLOGICAL-INPUT CORPUS rather than over one depth: nested parentheses at the reproduced depth and above, a very large file, invalid UTF-8 bytes, a leading BOM, and binary content. Each case asserts BOTH that the exit code is a member of `{0,1,2}` AND that a MEASUREMENT carrying the visited and derived counts reached the stream `reportMeasured`'s branch for that shape writes to — the second half is what WR-19 and CR-15 both actually broke."
    - "WHICH STREAM, PER BRANCH, READ OFF THE EXISTING MECHANISM RATHER THAN WISHED FOR. `reportMeasured` at `scripts/runnable-ref/uat-spec-integrity.ts:1657-1670` has four branches and they do NOT all write to stdout: branch (1), the VACUITY floor, calls `err(...)` with `ZERO uat specs were visited (N derived) — this check was NOT performed` and returns 2; branch (2), the DENOMINATOR floor, calls `err(...)` with `visited V of E derived uat specs — the scan set is short` and returns 2; only branches (3) and (4), the findings line and the pass line, call `out(...)`. So a could-not-run shape — `visited` 0, `expected` 1 — produces its measurement on STDERR at exit 2 and an EMPTY stdout, BY DESIGN. A criterion demanding a `visited/expected` line on stdout for every could-not-run shape is unsatisfiable without moving the floors' output, and moving it would re-author the D-21 authority this file deliberately MIRRORS from `scripts/vacuity.ts` rather than imports. This plan asserts the measurement REACHED ITS BRANCH'S STREAM, and states per shape which branch, which stream and which exit code it expects."
    - "`reportMeasured` is therefore OUT OF SCOPE and stays byte-unchanged: it is in no movement, its file is edited only in `analyzeSpecs`, `main` and `deriveSpecPaths`, and a case asserts its four branches are the same four with the same streams after this plan as before. The one property CR-15 needs from it is that it is REACHED at all — which is exactly what the two boundaries deliver."
    - "The corpus is generated at RUN TIME into a probe root and never committed as a fixture, because a 1,000-deep nesting or an invalid-UTF-8 file under `scripts/runnable-ref/fixtures/` would be type-checked by `tsconfig.fixtures.json` and would turn a different gate red for an unrelated reason. Every probe root is deleted in a `finally`, which is also what protects this repository's own zero-spec derivation case from the `.temp/` contamination `docs/audit/31-round4-residuals.md` §5 records."
    - "Every case is RED FIRST against the committed `.js` at HEAD before any source change — the verifier's own depth-1000 probe with its measured empty stdout, its `RangeError` on stderr and its exit 1 — and MUTATION-PROVEN afterwards."
    - "The prior-round closure this plan must not disturb is re-measured: the 4,000-link call CHAIN that WR-19's fix closed still reports `0 findings over 1/1`, exit 0, stderr empty, with the measurement line printed — a deep chain and a deep nesting are different inputs and both must be answered."
    - "The could-not-run branch is not a silent pass: a file that could not be parsed does NOT increment `visited`, so a floor branch fires — the vacuity floor when it was the only file, the denominator floor when others were checked — reporting the short scan set out loud ON STDERR, and the run's exit code is 2 rather than 0. Asserted at the outcome, with the stream and the exit code both named, not inferred from the branch."
    - "No new runtime dependency and no change to `package.json`; the boundary is a `try` and the de-recursion is an explicit worklist, both using constructs already in this file."
  artifacts:
    - scripts/runnable-ref/uat-spec-integrity.ts
    - scripts/runnable-ref/uat-spec-integrity.js
    - scripts/runnable-ref/uat-spec-integrity.test.ts
    - agent-factory/checklists/browser-uat-recipe.md
    - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
  key_links:
    - "`ts.createSourceFile` -> the could-not-run `try`/`catch` boundary: recorded NOT_WIRED by the round-5 verifier's key-link table — the `try` wraps only `findBannedConstructs`. This plan moves the boundary to cover everything done with a spec's bytes."
    - "`main`'s body -> the process exit code: the second boundary, so the contract is held by decision at the process edge as well as per file."
    - "`deriveSpecPaths`'s directory walk -> the same class of unguarded self-recursion: de-recursed in the same edit so the fix is about the class rather than about the call the review happened to reach."
    - "the could-not-run branch <-> `reportMeasured`'s denominator floor: the link that turns a file which was not checked into a run that says so out loud, which is the property WR-19 and CR-15 both broke."
    - "`agent-factory/checklists/browser-uat-recipe.md`'s exit-code paragraph <-> the two boundaries: the agent-facing claim that must be true of the mechanism and must disclose what remains outside it."
  prohibitions:
    - statement: "A check that did not run must never be readable as a check that found nothing, and must never be readable as a check that found something. Every exit passes through the four-branch measured report or through a named could-not-run boundary."
      status: unverified
      verification: flagged
    - statement: "The exit code must be inside {0,1,2} by decision on every path, never by the interpreter's uncaught-exception default."
      status: unverified
      verification: flagged
    - statement: "A recipe paragraph must not assert an absolute the mechanism cannot deliver. What remains outside every boundary is disclosed by name."
      status: unverified
      verification: flagged
    - statement: "A pathological-input corpus is generated at run time and removed; no fixture is committed that a type-check gate would fail on for a reason unrelated to the ban."
      status: unverified
      verification: flagged
    - statement: "Never merge a protected branch and never deploy to production without a named human confirmation."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-17-PLAN.md / D-21 (1): a spec the runnable cannot finish is a COULD-NOT-RUN reason rather than an escaping throw, and the exit code is inside {0,1,2} BY DECISION on every path. This plan is that prohibition being enforced at the two positions D-21 (1) did not reach."
    - "31-24-PLAN.md / D-27: the containment scope rule and its two canonicaliser callers must survive this plan's edits to the same module."
---

<objective>
Close **CR-15** of `31-REVIEW.md`, independently reproduced by `31-VERIFICATION.md` round 5 as
behavioral spot-check row 12, and make `agent-factory/checklists/browser-uat-recipe.md`'s exit-code
paragraph true.

Round 4's `31-17` closed WR-19 — a per-recursion-frame step bound and a self-recursive AST walk that
together let a deep call chain kill the process with an empty stdout and both measurement floors
bypassed. That closure is real: a 4,000-link chain now reports `0 findings over 1/1`, exit 0, stderr
empty. What it did not do is wrap the PARSER. `ts.createSourceFile` is a recursive-descent parser
running on author-controlled source, and it sits one line ABOVE the `try` the fix added. The verifier
measured WR-19's exact harm reproducing at a quarter of the round-4 probe's depth: 1,000 nested
parentheses, uncaught `RangeError`, empty stdout, exit 1, and no measurement on EITHER stream — because
`reportMeasured` is never reached, so neither its stdout branches nor its two stderr floors ever speak.

**The bypass threshold moved from 4,000 to 1,000, in the opposite direction from the fix** — and the
recipe paragraph this same round added asserts it cannot happen.

The substantive harm has never been the exception. It is that `reportMeasured` is never reached, so the
vacuity floor and the denominator floor are bypassed by construction while stdout stays silent, and
Node's uncaught-exception code 1 is read by the D-12 contract as "a finding — the gate blocks". A check
that never ran is reported as a check that found something.

`deriveSpecPaths`'s directory `walk` is the second unguarded self-recursion in the same file. The review
names it explicitly so the fix covers the class rather than the one call.

Purpose: put every exit of this runnable through a decided boundary, and assert the partition over a
pathological corpus rather than over the one depth a reviewer happened to try.

Output: the parse, the diagnostics read and the walk inside one per-file boundary; `main`'s body inside
one process boundary returning 2; a de-recursed directory walk; a published `MEASUREMENT_BRANCH_STREAMS`
mapping read off `reportMeasured`'s existing four branches; a run-time-generated pathological corpus
asserting the exit code AND that the measurement reached its branch's stream; a corrected,
honestly-bounded recipe paragraph; and decision **D-28** appended to `31-CONTEXT.md`.

**What this plan does NOT touch: `reportMeasured` itself.** Its two floors write to stderr and return 2 by
design — they are the mirror of `scripts/vacuity.ts` that D-21 established and that this runnable cannot
import. An earlier draft of this plan required a `visited/expected` line on STDOUT for every could-not-run
shape, which that function cannot produce; satisfying it would have meant moving a floor's output and
re-authoring a mirrored authority in the course of fixing an unrelated boundary. The requirement is
therefore stated as "the measurement reached the stream its branch writes to", and `reportMeasured` is
asserted byte-unchanged.

**Ordering note:** wave 5, `depends_on: ["31-24"]`. This plan and `31-24` both modify
`scripts/runnable-ref/uat-spec-integrity.ts`, `.js`, `.test.ts` and
`agent-factory/checklists/browser-uat-recipe.md`, and both edit `analyzeSpecs`'s surrounding region and
the recipe's claim paragraphs. Sequencing removes the conflict, and it means the pathological corpus runs
against a module whose scoping rule is already final — so a case that could not run is unambiguously a
boundary failure rather than a scope one.

**Assumption delta:** `no-change`. Both detector hits are already decided in `31-CONTEXT.md` (D-14,
D-21); no identity-model question is open for this plan.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@.planning/phases/31-autonomous-manual-testing/31-REVIEW.md
@.planning/phases/31-autonomous-manual-testing/31-17-SUMMARY.md
@.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
@docs/audit/31-round4-residuals.md
@scripts/runnable-ref/uat-spec-integrity.ts
@agent-factory/checklists/browser-uat-recipe.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Where |
|---|---|---|
| the per-file boundary | CHANGED `analyzeSpecs`: the parse, the `parseDiagnostics` inspection and `findBannedConstructs` all inside ONE `try`, with the could-not-run reason naming the file and the cause | `scripts/runnable-ref/uat-spec-integrity.ts` |
| the process boundary | CHANGED `main`: the whole body inside one outer `try` whose `catch` writes a named reason to stderr and returns 2 | `scripts/runnable-ref/uat-spec-integrity.ts` |
| the de-recursed directory walk | CHANGED `deriveSpecPaths`: the self-recursive `walk` replaced by an explicit worklist, matching the `forEachDescendant` idiom D-21 (1) established | `scripts/runnable-ref/uat-spec-integrity.ts` |
| `PATHOLOGICAL_INPUT_SHAPES` | NEW exported frozen list of the shapes the corpus drives, so the corpus's own coverage is a published set rather than a habit | `scripts/runnable-ref/uat-spec-integrity.ts` |
| the pathological corpus | NEW cases, generated at run time into a probe root and removed in a `finally`: nesting at and above the reproduced depth, a very large file, invalid UTF-8, a BOM, binary content | `scripts/runnable-ref/uat-spec-integrity.test.ts` |
| the exit-partition assertion | NEW case asserting, for every corpus shape, that the exit code is in `{0,1,2}` AND that the measurement carrying the visited and derived counts reached the STREAM `reportMeasured`'s branch for that shape writes to (stderr for the two floors at exit 2, stdout for the findings and pass lines at exit 1 and 0) | `scripts/runnable-ref/uat-spec-integrity.test.ts` |
| `MEASUREMENT_BRANCH_STREAMS` | NEW exported frozen mapping from each `reportMeasured` branch to its stream and exit code, so the corpus's expectation is READ from a published answer rather than hand-typed per case, and a later change to `reportMeasured` moves this table and turns the corpus red | `scripts/runnable-ref/uat-spec-integrity.ts` |
| the corrected recipe paragraph | CHANGED prose: the two boundaries named, and what remains outside every boundary disclosed | `agent-factory/checklists/browser-uat-recipe.md` |
| decision **D-28** | NEW dated gap-closure decision appended beside D-27 | `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` |

## Edge probe assumptions

| # | Requirement | Category | Probe | Standing assumption for this plan |
|---|---|---|---|---|
| 1 | UATX-06 | empty | "what is the result for empty, single-element, or null input?" | Assumed: a zero-byte spec parses to an empty source file, contributes no finding, and DOES increment `visited` — it is a file that was checked, not one that could not be. Distinguished from a could-not-run by a named case. |
| 2 | UATX-06 | adjacency | "when two things are exactly equal or just touch, do they merge, collide, or separate?" | Assumed: the nesting depth at which the parser succeeds and the depth at which it throws are adjacent, and both sides must satisfy the partition — one with exit 0 and a measurement line, the other with exit 2 and a measurement line. The corpus drives both sides of that boundary rather than only the failing one. |
| 3 | UATX-06 | ordering | "when elements compare equal, is output order specified and stable?" | DECIDED, and stream-aware: the per-file could-not-run REASONS are written to stderr during the loop, and `reportMeasured` writes afterwards — to stderr for the two floors, to stdout for the findings and pass lines. So a reader sees WHY the denominator is short before seeing THAT it is short, and both facts arrive on stderr for a could-not-run run. Asserted as its own case, with the stream named. |
| 4 | UATX-05 | unclassified | review manually | Assumed: a could-not-run exit 2 is the SAME code as the two loud skips, and that is correct — all three mean "the check was not performed" — but the corpus asserts the reason strings are distinguishable so a reader can tell which happened. |
| 5 | UATX-01 | unclassified | review manually | Not touched by this plan; `31-21`..`31-23` own the admission and governance-root surfaces. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an author-controlled spec's bytes -> the TypeScript parser | a recursive-descent parser on untrusted input crosses here, and it sat outside every guard |
| an uncaught throw -> the process exit code | Node's default crosses here as exit 1, which the D-12 contract reads as "a finding", so a check that never ran is reported as one that found something |
| a could-not-run file -> the `visited` counter | a file counted as checked without having been is a fabricated denominator, which is what the floor exists to catch |
| the recipe's exit-code paragraph -> the agent that trusts it | an absolute claim crosses here; an agent told the checker cannot crash will not look for the case where it does |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-25-01 | Spoofing | a check that never ran reported as a check that found something, via the uncaught-exception exit code | critical | mitigate | CR-15: the parse and the walk are inside one per-file boundary and `main`'s body inside one process boundary. RED-first with the verifier's depth-1000 probe; watched by the exit-partition assertion over the whole corpus. |
| T-31-25-02 | Denial of Service | a spec whose shape exhausts the interpreter's stack in the parser or in the directory walk | high | mitigate | The parse is caught per file; `deriveSpecPaths`'s walk is de-recursed so the depth leaves the interpreter's stack entirely, covering the class rather than the one call. |
| T-31-25-03 | Repudiation | a run that covered less than the derived set reporting nothing about it | high | mitigate | The could-not-run branch does not increment `visited`, so the denominator floor fires; the corpus asserts that a measurement carrying the visited and derived counts reached THE STREAM `reportMeasured`'s BRANCH FOR THAT SHAPE WRITES TO — stderr for the two floors at exit 2, stdout for the findings and pass lines at exit 1 and 0 — for EVERY shape, which is the half both WR-19 and CR-15 actually broke. A criterion demanding stdout for every shape is the retracted one: it is unsatisfiable for a could-not-run run without re-authoring the `scripts/vacuity.ts` mirror this file deliberately keeps. |
| T-31-25-04 | Repudiation | a recipe paragraph asserting an absolute the mechanism cannot deliver | high | mitigate | The paragraph names the two boundaries and discloses what remains outside them (a fault that terminates without unwinding); the corrected text is bound to the corpus rather than restated independently. |
| T-31-25-05 | Tampering | a committed pathological fixture turning an unrelated type-check gate red and being deleted to make it green | medium | mitigate | The corpus is generated at run time into a probe root and removed in a `finally`; `tsconfig.fixtures.json` never sees it, and the `.temp/` contamination hazard is closed by the same cleanup. |
| T-31-25-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager install task and no dependency; every command runs against the committed lockfile and already-installed dev dependencies, so the package-legitimacy protocol has no `[ASSUMED]`/`[SUS]` row to gate. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Every exit passes through one decided boundary — the parse, the walk and main's body (CR-15, D-28)</name>
  <files>scripts/runnable-ref/uat-spec-integrity.ts, scripts/runnable-ref/uat-spec-integrity.js, scripts/runnable-ref/uat-spec-integrity.test.ts</files>
  <precondition>The RED probe must run against the committed `scripts/runnable-ref/uat-spec-integrity.js` from the SINGLE probe root `.temp/31-25-probe/` inside this repository, with the pathological spec at `uat/p.uat.spec.ts`, because the runnable resolves `typescript` from the TARGET repository. The root must be `rm -rf`'d immediately after measurement and confirmed absent with `test ! -e .temp/31-25-probe` — NOT with `git status --short .temp`, which is inert because `.temp/` is gitignored at `.gitignore:19` and prints nothing whatever that directory holds (it currently carries 57 artifacts from rounds 3 to 5, which is the evidence that the earlier `git status`-based residue gate never fired). A leftover `uat/` spec under `.temp/` contaminates this repository's own zero-spec derivation case (`docs/audit/31-round4-residuals.md` §5).</precondition>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — CR-15 in full: its file/line citations (`:1605`, `:1613-1630`, `:260-270`, `browser-uat-recipe.md:309-315`, `:820-880`), the depth table (500 → exit 0; 1000/2000/5000 → exit 1 with empty stdout and a `RangeError` at `typescript.js` `token()`), the paragraph stating that the harm is `reportMeasured` never being reached, the note that `deriveSpecPaths`'s walk is the second unguarded self-recursion, and its `Fix:` code sketch naming the per-file boundary and the outer `main` try.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the `regressions:` entry for CR-15, behavioral spot-check row 12, the required-artifacts row for `uat-spec-integrity.ts`, the key-link row recording the parser NOT_WIRED to the boundary, the anti-patterns row at `~1605`, and truth 5's paragraph explaining why CR-15 is tracked under UATX-06 rather than UATX-05.
    - `scripts/runnable-ref/uat-spec-integrity.ts` — the D-21 (1) header paragraph in full; `deriveSpecPaths` in full including its self-recursive `walk`; `forEachDescendant` in full, since the directory walk is de-recursed in the same idiom; `loadTypeScriptFromTarget` and `PARSER_ABSENT_MARKER`; the `ParsedSourceFile` type and the `parseDiagnostics` pitfall comment; `analyzeSpecs` in full including its whole D-21 (1) comment block and the ONE `visited++` increment site; `reportMeasured` in full including the ORDER paragraph and its four branches; `emitLoudSkipIfBrowserUnusable` and `BROWSER_ABSENT_MARKER`; `main` in full; and `invokedAsScript`.
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` — the existing 4000-link chain case (the WR-19 closure this plan must re-measure intact), the injectable-`readFile` short-visit case that proves the denominator floor fires, and the existing mutation-proof idiom.
    - `.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md` — Dimension 8, so the new corpus is written as validation the strategy already asks for rather than as an ad-hoc addition.
  </read_first>
  <behavior>
    - RED (verification row 12): a spec containing 1,000 nested parentheses yields an uncaught `RangeError` at the parser, EMPTY stdout, exit 1, and no measurement line on either stream.
    - GREEN 1: the same spec, as the run's ONLY spec, yields exit 2; a could-not-run reason on STDERR naming the file and the cause; and `reportMeasured`'s VACUITY-floor measurement on STDERR carrying `ZERO uat specs were visited` and the derived count `1`. Stdout is EMPTY, which is the correct output of branch (1) and is asserted as such rather than treated as the defect — the defect was that no branch ran at all.
    - GREEN 1b (the denominator floor, driven separately because it is a different branch): the same pathological spec ALONGSIDE one clean spec yields exit 2 and `reportMeasured`'s DENOMINATOR-floor measurement on STDERR reading `visited 1 of 2`, with stdout empty. This is the branch a could-not-run file reaches whenever the run has other files, and it is the one that says out loud that the scan covered less than it claimed.
    - GREEN 2 (both sides of the adjacency): a nesting depth the parser handles yields exit 0 with the PASS measurement on STDOUT reading `0 findings over 1/1`; the adjacent failing depth yields exit 2 with the vacuity-floor measurement on STDERR. Both sides driven, and the stream difference between them is stated as the decided design rather than as an inconsistency.
    - GREEN 3 (the process boundary): a fault raised outside `analyzeSpecs` — driven by making the derived spec list, the parser load or the report step throw — yields exit 2 with a named reason on stderr, never an uncaught exception. When the fault is raised INSIDE `reportMeasured` itself, no measurement can exist by construction, and the case asserts the named process-boundary reason on stderr INSTEAD of a measurement, stating that as the one shape for which the measurement requirement is vacuous.
    - GREEN 4 (the class, not the call): a directory tree deep enough to have exhausted the old recursive `walk` is derived without a throw.
    - CONTROL 1 (WR-19's closure): the 4,000-link call CHAIN still reports `0 findings over 1/1`, exit 0, stderr empty, with the measurement line printed.
    - CONTROL 2 (the loud skips are unmoved): the parser-absent and browser-absent markers still each emit once and exit 2, and their reason strings remain distinguishable from a could-not-run reason.
    - CONTROL 3 (the ordinary path): a clean spec and a spec with a genuine finding still exit 0 and 1 respectively with their measurement lines.
    - EMPTY: a zero-byte spec increments `visited`, contributes no finding, and exits 0 — a file that was checked, not one that could not be.
    - ORDERING: the per-file could-not-run reason reaches stderr BEFORE `reportMeasured`'s own output, whichever stream that lands on — asserted by the relative order of the stderr lines for a floor run, and by reason-before-stdout for a run that still reaches a stdout branch.
  </behavior>
  <action>
    MOVEMENT 1 — reproduce and record. Build the probe root `.temp/31-25-probe/` with a spec at
    `uat/p.uat.spec.ts` containing the review's construct at depth 1,000. Drive the CURRENTLY COMMITTED
    `scripts/runnable-ref/uat-spec-integrity.js` and record the exit code, the exact stdout byte count, and
    the first stderr line. Repeat at the depths the review tabulated so the pre-fix threshold is measured on
    this tree rather than quoted. Record, for each depth, the exit code, the stdout byte count and the stderr
    content, so the pre-fix "no measurement on EITHER stream" fact is measured rather than assumed from the
    empty stdout alone. Add the failing cases and observe them fail. Delete `.temp/31-25-probe/` and
    confirm with `test ! -e .temp/31-25-probe`.

    MOVEMENT 2 — move the per-file boundary to cover the bytes, not one function. In `analyzeSpecs`,
    restructure the loop body so ONE `try` encloses the read, the `ts.createSourceFile` call, the
    `parseDiagnostics` inspection and the `findBannedConstructs` call. Keep the three distinct could-not-run
    reasons the file already writes — unreadable, did-not-parse, could-not-be-analysed — so a reader can
    still tell which happened, and add a fourth for a throw from the parse itself naming the file and the
    cause. Keep the `visited++` increment as the ONE site and keep it AFTER the analysis. State at the site
    that the boundary is about the BYTES rather than about one function: a file the parser cannot finish is
    a file this runnable did not check, exactly like one it could not read, and the register that failed in
    CR-15 is WHICH WORK the boundary encloses.

    MOVEMENT 3 — add the process boundary. Wrap `main`'s body in one outer `try`. The `catch` writes a
    named reason to stderr — the runnable could not complete, with the cause — and returns 2. The `try` must
    return whatever the body returns, so a legitimate exit 1 (a finding) and a legitimate exit 0 pass
    through untouched; only a THROW becomes 2. State at the site why 2 and not 1: 1 means "a finding, the
    gate blocks", which is a claim about the specs, and a runnable that could not complete has made no claim
    about them.

    MOVEMENT 4 — de-recurse the second walk. Replace `deriveSpecPaths`'s self-recursive directory `walk`
    with an explicit worklist in the same idiom `forEachDescendant` established, so the depth leaves the
    interpreter's stack. Assert every path is still visited exactly once and that the derived set is
    order-independent, which is the same pair of properties D-21 (1) asserted for the AST walk. State that
    this is the CLASS being covered rather than the call the review reproduced.

    MOVEMENT 5 — rebuild, re-measure, mutation-prove. Run `npm run build`, re-drive the depth table and all
    three controls against the rebuilt committed `.js`, and record the post-fix outputs beside the pre-fix
    ones. Then apply two described mutants in turn — first the parse moved back outside the per-file `try`,
    which must fail GREEN 1 and GREEN 2; second the outer `main` try removed, which must fail GREEN 3 —
    reverting after each and showing the cases pass.
  </action>
  <acceptance_criteria>
    - `31-25-SUMMARY.md` prints a depth table (the depths the review tabulated, plus the adjacent success/failure pair this task measures) × two columns (pre-fix, post-fix), each cell carrying the exit code, the stdout content or byte count, and the stderr measurement/reason lines.
    - The pre-fix depth-1000 cell matches `31-VERIFICATION.md` behavioral spot-check row 12 exactly: uncaught `RangeError`, empty stdout, exit 1, no measurement on either stream.
    - The post-fix depth-1000 cell reports exit 2, a could-not-run reason on STDERR naming the file, and the VACUITY-floor measurement on STDERR carrying `ZERO uat specs were visited` and the derived count. Stdout is EMPTY and the summary states that this is branch (1)'s correct output, quoting `reportMeasured`'s own source lines, so a later reader does not read the empty stdout as the unfixed defect.
    - GREEN 1b is recorded separately: the pathological spec alongside a clean one reports exit 2 with the DENOMINATOR-floor measurement on STDERR reading `visited 1 of 2`. The two floors are shown to be different branches with different messages, both reached, both loud.
    - The adjacency pair is measured: the highest depth that parses reports exit 0 with the PASS line `0 findings over 1/1` on STDOUT; the lowest depth that does not reports exit 2 with the vacuity floor on STDERR. Both depths are recorded as numbers.
    - `reportMeasured` is asserted BYTE-UNCHANGED by this plan: `git diff {round-base}..HEAD -- scripts/runnable-ref/uat-spec-integrity.ts` is inspected and the summary states that no hunk touches `reportMeasured`'s body, so the D-21 mirror of `scripts/vacuity.ts` is not re-authored. If a movement is ever tempted to move a floor's output to stdout, that is a change to the mirrored authority and belongs in its own decision, not in this plan.
    - GREEN 3 is driven at a position OUTSIDE `analyzeSpecs` and reports exit 2 with a named reason.
    - GREEN 4 is driven at a directory depth that would have exhausted the old walk, and the derived set is asserted equal to the same set the pre-fix program derived for a shallow tree.
    - CONTROL 1 is quoted and matches `31-17-SUMMARY.md`'s recorded 4,000-link measurement.
    - CONTROL 2 asserts each loud-skip marker still emits exactly once, and that its reason string is distinguishable from a could-not-run reason; both marker strings are quoted.
    - CONTROL 3 and EMPTY each pass as named cases; the zero-byte spec's `visited` contribution is asserted as `1`, not `0`.
    - ORDERING is a named case: the per-file reason precedes `reportMeasured`'s output; for a could-not-run run both are on stderr and their relative order is asserted, and for a mixed run the reason still precedes the stdout measurement.
    - Both mutation proofs are recorded with which GREEN cases each one broke.
    - `npm run build` output count is unchanged at 60 committed `.js` files — no new module is added.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts</automated>
    <fails_when>non-zero exit, a summary line reporting `0 passed`, any pathological case reporting an exit code outside `{0,1,2}`, or any pathological case whose measurement did not reach the stream its declared `reportMeasured` branch writes to</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or the build-parity step naming any committed `.js` that differs from a rebuild of its source</fails_when>
    <automated>test ! -e .temp/31-25-probe &amp;&amp; test -z "$(find . -path ./node_modules -prune -o -type p -print)" &amp;&amp; git status --short scripts agent-factory</automated>
    <fails_when>non-zero exit — `.temp/31-25-probe` survived, or the FIFO sweep printed a path — or the `git status` output lists any `uat/*.uat.spec.ts` left behind outside the fixtures directory. `git status --short .temp` is deliberately NOT used: `.temp/` is gitignored, so it can never fail.</fails_when>
  </verify>
  <done>A pathological parse input exits 2 with a measurement line instead of exiting 1 with an empty stdout; a fault anywhere in `main` exits 2; the directory walk no longer recurses; and WR-19's own 4,000-link closure is re-measured intact.</done>
  <reversibility rating="costly">The two boundaries become the exported contract the recipe quotes and the corpus asserts. Reverting restores a runnable the verifier measured crashing at a quarter of the previously-fixed depth with both measurement floors bypassed.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The partition is asserted over a pathological corpus, and the recipe says what the mechanism does (CR-15 disclosure, D-28)</name>
  <files>scripts/runnable-ref/uat-spec-integrity.ts, scripts/runnable-ref/uat-spec-integrity.js, scripts/runnable-ref/uat-spec-integrity.test.ts, agent-factory/checklists/browser-uat-recipe.md, .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — CR-15's `Fix:` closing paragraph asking for a corpus/suite case at depth 1000 asserting a measurement line on stdout AND an exit inside `{0,1,2}`, and its observation that neither this round's fixture nor its probes exercise a deep NESTING because both drive a deep CHAIN.
    - `agent-factory/checklists/browser-uat-recipe.md` — the exit-code table and the paragraph beneath it in full (the sentence asserting the checker holds the three codes on every path "including a pathological one" and "does not exit through an uncaught exception"), and the "## The loud skips" section that follows, so the corrected paragraph sits correctly beside the two skip markers.
    - `scripts/runnable-ref/uat-spec-integrity.ts` — `reportMeasured`'s four branches and its ORDER paragraph, the `PARSER_ABSENT_MARKER` and `BROWSER_ABSENT_MARKER` constants, and the existing exported frozen registers (`UNRESOLVABLE_CALLEE_RESIDUALS`, `BANNED_MODIFIER_HEADS`, `BANNED_MODIFIER_TAILS`) so the new `PATHOLOGICAL_INPUT_SHAPES` export follows the same frozen-array idiom and can be quoted into the recipe under a both-directions equality.
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` — the existing recipe↔register both-directions equality cases, so the new export is bound the same way rather than by a new habit.
    - `tsconfig.fixtures.json` — what it type-checks, so the reason the corpus is generated at run time rather than committed is recorded correctly.
    - `docs/audit/31-round4-residuals.md` — §5, the `.temp/` probe-contamination hazard, so the corpus's cleanup is written the way that section requires.
    - `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` — D-21 in full (the decision this one extends) and D-27, for the recorded FORM.
  </read_first>
  <behavior>
    - Test 1 (the partition, over the whole corpus): for every shape in `PATHOLOGICAL_INPUT_SHAPES` the exit code is a member of `{0,1,2}` AND the measurement carrying the visited and derived counts reached the stream that shape's `reportMeasured` branch writes to, per the `MEASUREMENT_BRANCH_STREAMS` mapping. Each shape declares its EXPECTED branch, stream and exit code up front; a shape whose measured branch differs from its declared one fails the case rather than being re-declared to match.
    - Test 2 (nesting): at and above the reproduced depth, exit 2 with the vacuity floor on stderr and an empty stdout; below it, exit 0 with `0 findings over 1/1` on stdout.
    - Test 3 (a very large file): a spec far above any ordinary size still yields a member of the partition with its branch's measurement on that branch's stream.
    - Test 4 (invalid UTF-8): a spec containing invalid byte sequences yields a member of the partition with its branch's measurement, and the could-not-run reason on stderr names the file.
    - Test 5 (a BOM): a spec with a leading byte-order mark parses and is CHECKED — it increments `visited` — so it reaches a STDOUT branch, which is the positive half of the stream expectation and proves the corpus is not asserting stderr for everything.
    - Test 6 (binary content): a spec whose bytes are binary yields a member of the partition with its branch's measurement on that branch's stream.
    - Test 7 (the corpus's own premise): `PATHOLOGICAL_INPUT_SHAPES` is non-empty and every member is driven by a case; a corpus over zero shapes would satisfy Test 1 vacuously.
    - Test 8 (the disclosure equals the mechanism): the recipe's exit-code paragraph is bound to `PATHOLOGICAL_INPUT_SHAPES` in both directions, and the seeded-fail control still discriminates.
    - Test 8b (`reportMeasured` unchanged): a case asserts the four branches are still four, with the same two writing to `out` and the same two writing to `err`, so this plan is proven not to have re-authored the `scripts/vacuity.ts` mirror in the course of satisfying a stream expectation.
    - Test 9 (cleanup): every probe root the corpus creates is removed in a `finally`, asserted by a case that checks the parent directory is empty after the run.
  </behavior>
  <action>
    MOVEMENT 1 — publish the corpus's coverage as a set. Add an exported frozen
    `PATHOLOGICAL_INPUT_SHAPES` array naming each shape the corpus drives, one sentence each, in the same
    idiom as the file's other frozen registers. This is what makes the corpus's coverage a published answer
    a document can quote rather than a habit a later edit can quietly shrink.

    MOVEMENT 2 — generate the corpus at run time and assert BOTH halves, against the streams the mechanism
    actually uses. FIRST publish `MEASUREMENT_BRANCH_STREAMS`: a frozen mapping from each `reportMeasured`
    branch — vacuity floor, denominator floor, findings, pass — to the stream it writes to and the exit code
    it returns, READ OFF that function's existing body (`:1657-1670`) rather than decided here. Then write
    the corpus cases so each shape is generated into a probe root inside a `try`/`finally` that removes the
    root unconditionally, and drives the committed `.js` as a child process. For every shape assert TWO
    things: the exit code is a member of `{0,1,2}`, and the measurement carrying the visited and derived
    counts appears on the stream that shape's declared branch maps to.

    State in the case comment that the second assertion is the one WR-19 and CR-15 both actually broke —
    the exit code was inside the partition by accident in both, and the absence of ANY measurement, on
    EITHER stream, is what made a check that never ran unreadable as such. State also why the assertion is
    not "a line on stdout": the two floors write to stderr by design, that design is the D-21 mirror of
    `scripts/vacuity.ts`, and a corpus demanding stdout for a could-not-run shape would force this plan to
    re-author a mirrored authority in order to satisfy its own criterion. The property CR-15 needs is that
    `reportMeasured` is REACHED; which of its four branches then speaks, and where, is that function's
    decision and is left to it. Drive the adjacency pair for the nesting shape so the corpus covers the succeeding side too.
    Record in the case comment why the corpus is not a committed fixture: `tsconfig.fixtures.json`
    type-checks everything under the fixtures directory, and a 1,000-deep nesting or an invalid-UTF-8 file
    would turn that gate red for a reason unrelated to the ban.

    MOVEMENT 3 — correct the recipe paragraph in the same plan as the mechanism. Rewrite the exit-code
    paragraph so it states the TWO boundaries — everything done with a spec's bytes is inside a per-file
    could-not-run boundary, and the runnable's whole body is inside a process boundary that returns 2 — and
    so it DISCLOSES what remains outside every boundary: a fault that terminates the process without
    unwinding, such as an out-of-memory kill or a signal, is not caught by any `try`, and the Windows leg of
    this behaviour is an open `UNKNOWN - verify` per the standing posture. Quote the corpus's shape list
    from `PATHOLOGICAL_INPUT_SHAPES` and bind it with a both-directions equality case, exactly as the
    boundary list is bound to `UNRESOLVABLE_CALLEE_RESIDUALS`. Clear voice; this is the paragraph an agent
    reads before trusting an exit code.

    MOVEMENT 4 — record D-28. APPEND a `#### Gap-closure decision — D-28 (2026-09-09, gap-closure round 5,
    plan 31-25)` block to `31-CONTEXT.md` in the recorded form of D-21 and D-27, with
    `**Forced by:** CR-15 of 31-REVIEW.md, independently reproduced by 31-VERIFICATION.md round 5
    (behavioral spot-check row 12, the regressions entry for CR-15, the key-link row recording
    ts.createSourceFile NOT_WIRED to the boundary, and the anti-patterns row at ~1605)`. State which
    register failed — not the bound's VALUE and not its UNIT, which D-21 (1) fixed, but WHICH WORK THE
    BOUNDARY ENCLOSES, and separately whether the contract is held at the PROCESS edge as well as per file.
    Give the three numbered sub-decisions (everything done with a spec's bytes inside one per-file
    boundary, with the directory walk de-recursed so the class is covered; `main`'s body inside one process
    boundary returning 2; and the reached-measurement requirement expressed as "the measurement reaches the
    stream ITS BRANCH writes to", published as `MEASUREMENT_BRANCH_STREAMS` and read off `reportMeasured`
    rather than imposed on it). Include the "What D-28 does NOT establish" block naming the non-unwinding
    fault, the Windows `UNKNOWN - verify`, the fact that a could-not-run is still a run that covered less
    than the derived set rather than a run that proved anything, and the fact that `reportMeasured`'s own
    branch/stream design is the `scripts/vacuity.ts` mirror and was deliberately left unchanged — a
    requirement that would have forced it to change is a requirement this plan rewrote rather than a
    mechanism it re-authored. `Reversibility: costly`. Close with the three places
    that must agree. APPEND ONLY.
  </action>
  <acceptance_criteria>
    - `PATHOLOGICAL_INPUT_SHAPES` is exported, frozen, non-empty, and its cardinality is recorded as a number.
    - Every member of `PATHOLOGICAL_INPUT_SHAPES` is driven by a case; the mapping is asserted in both directions so a shape added without a case, or a case without a shape, turns red.
    - `MEASUREMENT_BRANCH_STREAMS` is exported and frozen, maps all four `reportMeasured` branches, and its stream/exit values are quoted beside `reportMeasured`'s own source lines so a reader can check the mapping was read off the mechanism rather than decided in the test.
    - The partition assertion covers exit code AND measurement-on-the-right-stream for every shape; the summary prints a shape × (declared branch, expected stream, exit code, measured stream, measured text) table, and every declared/measured pair agrees.
    - At least one shape lands on a STDOUT branch and at least one on a STDERR branch; the summary states both counts, so the assertion is proven not to be trivially satisfiable by expecting one stream everywhere.
    - The nesting adjacency pair is recorded with both depths as numbers and both outcomes, including which stream each measurement landed on.
    - Test 5's BOM case asserts `visited` is `1`, not `0` — a BOM is a checked file — and that its measurement reached STDOUT.
    - Test 8b passes: `reportMeasured` still has four branches with the same two stdout and two stderr writers, so the `scripts/vacuity.ts` mirror is proven un-re-authored.
    - Test 9 asserts the probe parent directory is empty after the corpus run.
    - The corrected recipe paragraph is quoted verbatim in the summary; it names the two boundaries, names the non-unwinding fault as the disclosed residual, and carries the Windows `UNKNOWN - verify` marker.
    - The recipe↔`PATHOLOGICAL_INPUT_SHAPES` both-directions equality passes and its seeded-fail control still discriminates.
    - D-28 is APPENDED; `grep -c '^#### Gap-closure decision — D-' .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` is recorded before and after and differs by exactly one; `git diff {round-base}..HEAD -- .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` shows additions only, with the range pinned to this round's base commit so a committed change cannot pass the check vacuously.
    - The decision text mirrors into the D-21 header block region of `uat-spec-integrity.ts` and into `31-25-SUMMARY.md`; all three are quoted side by side.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts scripts/check-banned-claims.test.ts</automated>
    <fails_when>non-zero exit, the recipe↔shape-list equality reporting a member on one side only, any corpus case whose measurement reached neither stream, or the `reportMeasured`-unchanged case reporting a moved branch or a moved stream</fails_when>
    <automated>node scripts/check-uat-oracles.js &amp;&amp; node scripts/check-foundation-guards.js</automated>
    <fails_when>either command exits non-zero or prints anything other than `ALL CHECKS PASSED`</fails_when>
    <automated>npm run build &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or the freshness line reporting anything other than `All build outputs fresh: 60 committed .js file(s) match a rebuild of their sources.`</fails_when>
  </verify>
  <done>Every pathological shape exits inside `{0,1,2}` with a measurement line on stdout; the corpus's coverage is a published, two-sidedly-bound set; the recipe names the two boundaries and discloses what remains outside them; and D-28 is appended.</done>
  <reversibility rating="reversible">The corpus and the published shape list are additive behind a green suite; removing either turns the both-directions equality or the premise case red immediately.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Red-team THIS plan's own fix — the phase's standing probes against the exit boundary</name>
  <files>scripts/runnable-ref/uat-spec-integrity.test.ts</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the "Gaps Summary" closing paragraphs and the sentence naming CR-15 as "the parser's boundary … a distinct axis from the one 31-16/31-17 derived and closed"; this task's job is to find the next axis before round 6 does.
    - `docs/audit/31-round4-residuals.md` — §6.2 in full, the eighth logged instance of a verification harness producing a FALSE RESULT about its own premise, and its explicit note that "the same instinct applied to `analyzeSpecs`'s boundary would have found CR-15". Each probe below must assert its own premise before its conclusion.
    - `scripts/runnable-ref/uat-spec-integrity.ts` — every `return` in `main` and every branch of `reportMeasured`, read off the source, so the exit-code enumeration is derived rather than remembered; and every remaining self-recursive function in the file.
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` — the injectable-`readFile` seam, which is how a fault is forced at a chosen position without editing the module.
  </read_first>
  <action>
    PROBE 1 — HOW IS THIS BOUNDARY REACHED, not only what it catches. Derive EVERY `return` statement in
    `main` and every branch of `reportMeasured` from the source, and drive an input that reaches each one.
    Record the exit code each produces and assert the derived branch count equals the number of driven
    inputs — a branch nobody reached is a branch nobody has tested.
    FAILS WHEN: any derived return or branch is unreachable by any constructed input without a stated residual, or any reached branch produces an exit code outside `{0,1,2}`

    PROBE 2 — DERIVE BOTH AXES. The axis that moved is WHICH WORK the boundary encloses. Name and drive the
    others: (a) WHERE the boundary sits — force a fault before `analyzeSpecs` (in `deriveSpecPaths`), inside
    it (in the parse), and after it (in `reportMeasured`), and record the exit code and stdout for each;
    (b) WHAT the boundary does to the COUNTERS — assert `visited` is not incremented for any caught file and
    that `expected` is unchanged; (c) the STDERR/STDOUT split AS THE MECHANISM DEFINES IT — assert every
    could-not-run REASON reaches stderr; assert the two `reportMeasured` FLOORS (vacuity, denominator)
    reach stderr at exit 2; assert the findings and pass MEASUREMENTS reach stdout at exit 1 and 0; and
    assert nothing reaches a stream its branch does not write to. Do NOT assert that "every measurement
    reaches stdout" — that is false of branches (1) and (2) by design, it is the `scripts/vacuity.ts`
    mirror D-21 established, and a probe asserting it would force the plan to move a floor's output and
    re-author an authority it does not own.
    FAILS WHEN: any of the three axes produces an answer the plan did not decide, (b) shows a caught file contributing to `visited`, or (c) shows any branch writing to a stream other than the one `MEASUREMENT_BRANCH_STREAMS` records for it

    PROBE 3 — WHAT IS THE PREDICATE'S INPUT ASSEMBLED FROM. The exit code is assembled from
    `reportMeasured`'s four branches, `main`'s early returns, the two loud-skip markers and now the outer
    catch. Drive an input that satisfies TWO of these at once — a pathological spec together with an absent
    parser; a pathological spec together with an empty derived set; a browser-absent condition together with
    a pathological spec — and record which answer wins and whether the winning order is the decided one.
    FAILS WHEN: any combination produces an exit code the plan did not decide, or the winning order differs from the order stated in the source without a stated reason

    PROBE 4 — AT WHICH POSITIONS IS THE PREDICATE EVEN ASKED. Enumerate every remaining self-recursive
    function in the file, and every position that touches author-controlled bytes outside the per-file
    boundary — the spec-path derivation, the parser load, the browser probe — and state for each whether a
    throw there is caught, by which boundary, and with what exit code.
    FAILS WHEN: any enumerated position can throw past every boundary, or any remaining self-recursion is left without a stated disposition

    PROBE 5 — ASSERT THE HARNESS'S OWN PREMISE. This project has logged eight instances of a verification
    harness producing a false result about its own premise. For each corpus case, assert BEFORE the
    conclusion that the probe root really contains the generated spec, that the spec's byte length is the
    generated one, and that the derived spec count is 1 — a corpus whose specs were never written would pass
    the partition assertion vacuously with `0/0`.
    FAILS WHEN: any corpus case's premise assertion is absent, or any case reports `expected` as `0` while claiming to have driven a shape

    PROBE 6 — RE-DERIVE THE PRE-EXISTING MUTATION SET. Re-run every mutant the suite already carries against
    the changed code — including `31-24`'s containment mutants and `31-17`'s bound and walk mutants — and
    record how many cases each breaks. A mutant that now breaks FEWER cases is a mutant whose target moved.
    FAILS WHEN: any pre-existing mutant breaks fewer cases than the count recorded in `31-17-SUMMARY.md` or `31-24-SUMMARY.md`, without a stated reason

    Record every probe, its command, its measured output and its verdict in `31-25-SUMMARY.md` under a
    heading naming this task. Any defect a probe finds is fixed IN THIS PLAN, not deferred. Delete
    `.temp/31-25-probe/` and confirm with `test ! -e .temp/31-25-probe` plus the FIFO sweep, never with
    `git status --short .temp`.
  </action>
  <acceptance_criteria>
    - All six probes are recorded with the exact command, the measured output and an explicit PASS/FAIL verdict; no probe is recorded as "expected to pass".
    - PROBE 1's branch set is DERIVED by a parse; the derived count and the driven count are both stated as numbers and are equal.
    - PROBE 2's three axes are each printed with the forced fault position, the exit code and the stdout content.
    - PROBE 3's combination table has one row per pair with the winning answer and a decided/undecided label.
    - PROBE 4 enumerates every remaining self-recursion and every out-of-boundary byte-touching position by line number with a stated disposition.
    - PROBE 5's premise assertions are present in every corpus case; the summary states the number of cases carrying them and confirms it equals the corpus size.
    - PROBE 6 prints a mutant × broken-case-count table with the previously recorded counts beside the new ones.
    - Any defect found by any probe is FIXED in this plan and re-probed; the summary states explicitly whether any probe found a defect, and "none" is only acceptable when all six probes ran.
    - The residue check can observe its target: `test ! -e .temp/31-25-probe` succeeds, `find . -path ./node_modules -prune -o -type p -print` prints nothing, and `git status --short` shows no tracked-tree residue.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, the summary reporting fewer than 62 test files, or any test file reporting `0 passed`</fails_when>
    <automated>node scripts/check-foundation-guards.js &amp;&amp; node scripts/check-uat-oracles.js &amp;&amp; npm run freshness</automated>
    <fails_when>any of the three exits non-zero, either guard prints anything other than `ALL CHECKS PASSED`, or the freshness line reports fewer than 60 fresh committed `.js` files</fails_when>
    <automated>test ! -e .temp/31-25-probe &amp;&amp; test -z "$(find . -path ./node_modules -prune -o -type p -print)" &amp;&amp; git status --short</automated>
    <fails_when>non-zero exit — `.temp/31-25-probe` survived, or the FIFO sweep printed a path — or the `git status` output lists any `uat/*.uat.spec.ts` outside the fixtures directory or any probe `.mjs` at the repository root</fails_when>
  </verify>
  <done>All six standing probes ran against this plan's own boundary with recorded commands and outputs; every corpus case asserts its own premise; the pre-existing mutants still break at least as many cases as before; every defect found is fixed inside this plan.</done>
  <reversibility rating="reversible">The probes are measurements recorded in a summary; nothing about them constrains a later change except the record they leave.</reversibility>
</task>

</tasks>

<verification>
- `31-VERIFICATION.md` behavioral spot-check row 12 exits 2 with `reportMeasured`'s vacuity-floor measurement on stderr, instead of exiting 1 with no measurement on either stream; the companion two-spec case reaches the denominator floor.
- WR-19's own 4,000-link chain closure is re-measured intact (exit 0, `0 findings over 1/1`, stderr empty).
- A fault at any position in `main` exits 2 with a named reason; the directory walk no longer recurses.
- Every shape in `PATHOLOGICAL_INPUT_SHAPES` exits inside `{0,1,2}` with its measurement on the stream its `reportMeasured` branch writes to, per the published `MEASUREMENT_BRANCH_STREAMS` mapping; `reportMeasured` itself is byte-unchanged.
- `agent-factory/checklists/browser-uat-recipe.md`'s exit-code paragraph names the two boundaries and discloses the non-unwinding fault.
- D-28 is appended to `31-CONTEXT.md`; no existing decision is edited.
- Full excluded-e2e suite green, freshness at 60/60, foundation guards and UAT oracles `ALL CHECKS PASSED`, `package.json` byte-unchanged.
</verification>

<success_criteria>
- CR-15 closed with a RED-first depth table quoted verbatim and a GREEN measurement beside it, including the adjacency pair.
- Both boundaries in place, mutation-proven, with the directory walk de-recursed so the class is covered.
- A run-time pathological corpus whose coverage is a published, two-sidedly-bound set, asserting exit code AND the measurement reaching its branch's stream — with at least one shape on each stream, and the mirrored `reportMeasured` authority left untouched.
- Six self-red-team probes run against this plan's own boundary, each asserting its own premise.
- D-28 appended in the recorded form of D-17..D-27, cited by id in this plan's `must_haves.truths`.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-25-SUMMARY.md` when done.
</output>

---
phase: 31-autonomous-manual-testing
plan: 03
type: execute
wave: 2
depends_on: ["31-02"]
files_modified:
  - agent-factory/checklists/browser-uat-recipe.md
  - agent-factory/checklists/00-index.md
  - install/README.md
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - scripts/check-foundation-guards.test.ts
autonomous: true
requirements: [UATX-02, UATX-03]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`agent-factory/checklists/browser-uat-recipe.md` is the single home for the pinned browser-MCP setup: the five host-CLI registration commands (Claude Code, Codex CLI, Gemini CLI, OpenCode, GitHub Copilot CLI), the headed-by-default note with the CI opt-out flag, the browser-install command, and the pin itself (D-07, UATX-02)."
    - "`package.json` gains nothing — the MCP server is invoked by the user's own agent, so grugops adds no dependency and no npm script (D-07, UATX-02)."
    - "The pin version appears as ONE literal in the recipe, and a foundation guard asserts every `@playwright/mcp@` mention across the kit and docs equals that literal; bumping is one edit plus a re-pin (D-08)."
    - "The pin guard reports the number of scanned files and the number of pin occurrences on its pass line; a zero-occurrence scan FAILS as the anomaly it is rather than printing a pass (D-08)."
    - "The recipe states once that the attended Chrome lane is absent by design on the four non-Claude-Code hosts, so no adapter carries a dead tool name and no per-host skip line exists (D-12, UATX-03)."
    - "The recipe states the attended-only predicate as a positive, fail-closed conjunction over `claude auth status --json`, and states that anything else — an unrecognized shape, a parse failure or a missing field — is a loud skip naming which clause failed (D-10, UATX-03)."
    - "The recipe quotes the ban set from the single exported constant in `scripts/runnable-ref/uat-spec-integrity.ts` and names exactly that set, including what is deliberately excluded, so the claim matches the mechanism (D-14)."
    - "The recipe describes `content_hash` as a recomputable integrity digest that anyone with the repo can verify, never as tamper-proof and never as a security token, and discloses that transitive imports are not hashed (D-02)."
    - "The recipe is listed in exactly one tier table of `agent-factory/checklists/00-index.md` — the enterprise tier, matching its sibling gate-lane recipes."
    - "The pin guard's scan set is sorted by repo-relative path before the walk, so its findings and its `visited/expected` pass line are byte-identical across runs and platforms regardless of directory-read order."
    - statement: "When a scanned file's `@playwright/mcp@` mention is exactly equal to the pin literal it passes; any other version string is reported as its own finding naming the file, the found version and the pin — equal mentions never merge into one report row."
      verification: backstop
  artifacts:
    - agent-factory/checklists/browser-uat-recipe.md
    - agent-factory/checklists/00-index.md
    - install/README.md
    - scripts/check-foundation-guards.ts
    - scripts/check-foundation-guards.js
    - scripts/check-foundation-guards.test.ts
  key_links:
    - "the recipe's pin literal -> the guard: the guard READS the literal out of the recipe rather than re-declaring it, so there is one home for the fact."
    - "`BANNED_CONSTRUCTS` in scripts/runnable-ref/uat-spec-integrity.ts -> the recipe's ban-set prose: one source, quoted."
    - "the new checklist file -> the derived `checklists` scan set in scripts/check-imperative-lexicon.ts: the file enters the text gates the moment it lands."
  prohibitions:
    - statement: "A committed MCP configuration must never use a floating `@latest` specifier — the pin is the supply-chain control."
      status: unverified
      verification: flagged
    - statement: "No adapter or role file for a host that lacks the attended browser lane may carry that lane's tool name; the absence is stated once, in the recipe."
      status: unverified
      verification: flagged
    - statement: "The recipe must never describe the content hash as tamper-proof or as a security token."
      status: unverified
      verification: flagged
---

<objective>
Ship the single documentation home for agent-authored browser UAT — `agent-factory/checklists/browser-uat-recipe.md`
(D-07) — carrying the pinned browser-MCP setup for all five host CLIs (UATX-02), the attended-only
Chrome-lane statement and its fail-closed auth predicate (D-10, D-12, UATX-03), and the ban set
quoted from the shipped constant (D-14). Then freeze the pin with a new foundation guard (D-08) that
reads the literal out of the recipe and reports what it scanned.

Purpose: the pin is the only supply-chain control over a pre-1.0 server the user's own agent fetches
with `npx`; a floating specifier in a committed config is the anti-pattern. And a version fact with
two homes drifts — so the guard reads the recipe rather than re-declaring the number.

Output: one new enterprise-tier checklist, one index row, one short pointer section in the install
README, and one new foundation guard with its test.
</objective>

<!-- planner-discipline-allow: @latest -->
<!-- planner-discipline-allow: mcp@latest -->

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-RESEARCH.md
@.planning/phases/31-autonomous-manual-testing/31-PATTERNS.md
@.planning/phases/31-autonomous-manual-testing/31-02-SUMMARY.md
</context>

## Artifacts this phase produces

Phase-wide inventory. Items marked **(this plan)** are produced here.

| Symbol / path | Kind | Plan |
|---|---|---|
| `emitVerdict(..., sha, ...)`, `SHA_HEX_RE`, the three `artifact-ref` provenance fields | function / type / const | 01 |
| `scripts/runnable-ref/uat-spec-integrity.ts` / `.js` / `.test.ts`, `BANNED_CONSTRUCTS`, the two loud-skip markers | files / consts | 02 |
| `agent-factory/checklists/browser-uat-recipe.md` | new checklist | **03 (this plan)** |
| `PLAYWRIGHT_MCP_PIN_SOURCE` — the recipe path the guard reads the literal from | const | **03 (this plan)** |
| `guardPlaywrightMcpPin()` — the new foundation guard | function | **03 (this plan)** |
| `agent-factory/checklists/00-index.md` enterprise-tier row | doc row | **03 (this plan)** |
| `install/README.md` browser-UAT pointer section | doc section | **03 (this plan)** |
| `scripts/chrome-lane-bar.test.ts` | new test file | 04 |
| No new npm script, no new `package.json` dependency, no new config key, no new checklist cardinality constant | (deliberate absence) | all |

## Flagged assumptions

- **The pin literal is `0.0.78`, exactly as D-07/D-08 lock it.** Research finding F-01 measured the
  registry `latest` at `0.0.80` (published after the originating stack research). The locked pin
  still resolves. This plan implements the locked value and does NOT re-open it; D-08's own design
  makes a later bump one edit plus a re-pin. The developer has been told; if they want the current
  version instead, that is a one-line change to the recipe and a guard re-run.
- **Research assumptions A2 and A3 remain `UNKNOWN - verify`:** the shape of `claude auth status --json`
  on an API-key-only box with no interactive login, and under a long-lived setup token. Neither is
  reachable without destroying this box's real credentials. The predicate is written positively and
  fail-closed so both fail safe, and the recipe says so.
- **Research assumption A4:** the `authMethod` / `apiKeySource` / `apiProvider` field names were
  measured on exactly one Claude Code version. A rename makes the probe skip loudly, never silently
  pass.
- **The Windows leg stays `UNKNOWN - verify`** for every browser probe, per the standing posture.

<tasks>

<task type="auto">
  <name>Task 1: The browser-UAT recipe, its index row, and the install-README pointer</name>
  <files>agent-factory/checklists/browser-uat-recipe.md, agent-factory/checklists/00-index.md, install/README.md</files>
  <read_first>
    - agent-factory/checklists/playwright-visual-regression-recipe.md (whole file, 89 lines — the frontmatter, the pinned-version-with-a-verification-date bullet, the "reference how-to, the gate points to it by name" framing)
    - agent-factory/checklists/00-index.md (whole file, 40 lines — both tier tables)
    - install/README.md (the section structure; the minimal markdown-copy path in section 1)
    - scripts/runnable-ref/uat-spec-integrity.ts (the `BANNED_CONSTRUCTS` constant and the comment recording what is deliberately excluded)
    - .planning/phases/31-autonomous-manual-testing/31-RESEARCH.md § "Five-CLI MCP registration matrix", § "Claude in Chrome: what the vendor doc actually says", § "Metadata" finding F-04
    - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md (D-02, D-05, D-07, D-10, D-12, D-14, D-16 and the Claude's Discretion block)
  </read_first>
  <action>
Write `agent-factory/checklists/browser-uat-recipe.md` in CLEAR PROFESSIONAL ENGLISH throughout —
this is a gate contract, never caveman voice — following the Phase 29 writing profile from the first
draft. Frontmatter is `kind: checklist` and `tier: enterprise`, matching its sibling gate-lane
recipes.

Sections, in this order:

1. **What counts as evidence, and what does not (UATX-01).** The committed `*.uat.spec.ts`, re-run
   by the §14 gate, is the evidence. An agent's narration of what it saw and a browser MCP
   tool-call transcript are neither, and never produce a gate stamp.

2. **Spec location and naming (D-05).** Specs live under a `uat/` subfolder of the target repo's
   existing end-to-end directory and are named `<ticket-id>.uat.spec.ts`. They ride the existing
   `quality.ui_e2e` lane unchanged — no second Playwright project and no new config path. The
   suffix is the recognition key for both the provenance note and the spec-integrity checker.

3. **Authoring through browser MCP (D-07, UATX-02).** State the pin as ONE bullet in the
   pinned-version-with-a-verification-date shape the sibling recipe uses: the package name, the
   version `0.0.78`, the registry-verification date, and the reason a pre-1.0 server is pinned
   rather than tracked. Then give the five host-CLI registrations, each SUBSTITUTING the pin — never
   a floating specifier in a committed configuration:
   - Claude Code: `claude mcp add playwright npx @playwright/mcp@0.0.78`
   - Codex CLI: `codex mcp add playwright npx "@playwright/mcp@0.0.78"`, or a
     `[mcp_servers.playwright]` block in `~/.codex/config.toml` with `command = "npx"` and
     `args = ["@playwright/mcp@0.0.78"]`
   - Gemini CLI: an MCP config block in Gemini's `settings.json` with `"command": "npx"` and
     `"args": ["@playwright/mcp@0.0.78"]`
   - OpenCode: `"mcp": { "playwright": { "type": "local", "command": ["npx", "@playwright/mcp@0.0.78"], "enabled": true } }`
     in `~/.config/opencode/opencode.json`
   - GitHub Copilot CLI: `/mcp add`, or a `"mcpServers"` entry in `~/.copilot/mcp-config.json` with
     `"type": "local"`, `"command": "npx"`, `"args": ["@playwright/mcp@0.0.78"]`
   State the behaviour facts: the server is headed by default and `--headless` must be opted into
   (the CI-relevant one), `--isolated` keeps the browser profile in memory, and Node 18 or newer is
   required. State that `package.json` gains nothing because the server is fetched by the user's own
   agent, and give the browser-binary prerequisite:
   `npx playwright install --with-deps chromium`.

4. **Provenance of the evidence note (D-01, D-02).** Describe the `finding` plus `artifact-ref` pair
   and the three fields the `artifact-ref` carries. Say, in the same register the note-id nonce is
   already disclosed in, that the content hash is a recomputable integrity digest anyone with the
   repository can verify — it is not tamper-proof and it is not a security token — and disclose that
   the spec's transitive imports are not hashed, so a helper edit does not invalidate the evidence.

5. **The spec-integrity ban set (D-14).** Quote the ban set from the single exported constant in
   `scripts/runnable-ref/uat-spec-integrity.ts` and name EXACTLY that set, including the cases
   deliberately left out: a promise rejection handler, the third region of a try statement, and a
   spec body carrying no assertions at all. Do not describe the ban more broadly than the checker
   decides. State the exit-code contract: 0 pass, 1 a finding, 2 could not run — and that a
   could-not-run leaves the UAT `pending`, never a pass.

6. **The loud skips (D-15, D-16).** Name both skip conditions — the parser could not be resolved
   from the target repository, and the browser lane is unusable at either probe stage — and state
   that a Playwright-lane skip is recorded as an UNSTAMPED `observation` note carrying the marker
   text verbatim, with no finding, no artifact-ref and no board move: the ticket stays `In UAT` and
   the pack shows the scenario as pending with the reason.

7. **The attended Chrome lane (UATX-03, D-09, D-10, D-11, D-12).** State plainly:
   - It is optional, attended-only, and structurally unable to produce a gate stamp. Its only
     outputs are a human-stamped `finding` and an `artifact-ref` describing what was witnessed.
   - The witnessing human's name arrives ONLY through the existing admission grant
     (`GRUGOPS_ADMISSION_APPROVED_BY`, read by the admission-guard hook). The agent can never author
     the name. There is no lane-specific variable.
   - The attended predicate, stated positively and fail-closed over `claude auth status --json`:
     logged in is true AND no API-key source is reported AND the auth method is the interactive one
     AND the provider is first-party AND a subscription type is a non-empty string. Anything else —
     including an unrecognized shape, a parse failure, or a missing field — is a loud skip naming
     which clause failed. Record that `--json` is the default output for that subcommand, and that
     the auth-method field alone is NOT sufficient (it stays interactive-looking even when a key is
     active — the key-source field is the discriminator that fires).
   - The vendor-side half: under API-key or long-lived-token authentication the host keeps the
     browser integration off entirely; browser actions run in a visible window and the assistant
     pauses for a human on a login or a challenge; the integration is unsupported under WSL and
     through third-party model providers.
   - The lane runs in the main thread, not in a spawned subagent — it is attended by definition, and
     loading browser tools costs context permanently. Note beside it, honestly, that whether those
     tools are reachable from a spawned subagent is `UNKNOWN - verify` and that nothing here depends
     on the answer.
   - **D-12, stated ONCE:** on the four non-Claude-Code hosts the lane is absent by design. No
     adapter carries the tool name and there is no per-host skip line anywhere. The Playwright floor
     is available on all five hosts, so degrade-never-break holds.
   - The Windows leg of every browser probe is `UNKNOWN - verify`.

8. **`agent-factory/checklists/00-index.md`:** add exactly one row to the ENTERPRISE tier table —
   `| \`browser-uat-recipe.md\` | when producing agent-authored browser UAT evidence at the gate |` —
   and add it to no other table. The checklist cardinality constants pin roles, workflows, skill
   adapters and plugin skills, not checklists, so no count needs bumping.

9. **`install/README.md`:** add ONE short section pointing at the recipe by name — what it covers
   (the pinned browser-MCP setup for all five host CLIs, and the attended-lane statement) and that
   grugops installs nothing for it. Do not copy the recipe's content; the substrate stays short and
   the detail lives in the file it points to.

10. Run the derived text gates IN THIS TASK, not as a later fix-up — the new checklist enters the
    derived `checklists` scan set the moment it is written.
  </action>
  <verify>
    <automated>npm run check:imperative-lexicon &amp;&amp; npm run check:banned-claims &amp;&amp; npm run check:public-docs &amp;&amp; npm run check:claim-anchors</automated>
    <fails_when>non-zero exit, or any finding line naming agent-factory/checklists/browser-uat-recipe.md, 00-index.md or install/README.md</fails_when>
    <automated>node scripts/validate-agent-factory.js</automated>
    <fails_when>non-zero exit, or output naming a missing or malformed checklist</fails_when>
    <automated>node -e "const t=require('node:fs').readFileSync('agent-factory/checklists/browser-uat-recipe.md','utf8');const n=(t.match(/@playwright\/mcp@/g)||[]).length;if(n<5)throw new Error('expected at least 5 pinned registration mentions, found '+n)"</automated>
    <fails_when>non-zero exit, or the message "expected at least 5 pinned registration mentions" on stderr</fails_when>
    <automated>node -e "const fs=require('node:fs');const idx=fs.readFileSync('agent-factory/checklists/00-index.md','utf8');const n=(idx.match(/browser-uat-recipe\.md/g)||[]).length;if(n!==1)throw new Error('recipe must be listed in exactly one tier table, found '+n)"</automated>
    <fails_when>non-zero exit, or the message "recipe must be listed in exactly one tier table" on stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - `agent-factory/checklists/browser-uat-recipe.md` frontmatter is `kind: checklist` with
      `tier: enterprise`.
    - The recipe names all five host CLIs by name, each with a pinned registration.
    - `grep -c 'mcp@latest' agent-factory/checklists/browser-uat-recipe.md` is 0.
    - The recipe contains the phrase `UNKNOWN - verify` at least twice (the subagent-reachability
      question and the Windows leg).
    - The recipe states that the transitive imports of a spec are not hashed.
    - `grep -c 'browser-uat-recipe' install/README.md` is at least 1.
    - `grep -c 'browser-uat-recipe' agent-factory/checklists/00-index.md` is exactly 1.
    - The four text gates listed above exit 0 with no finding naming any of the three files.
  </acceptance_criteria>
  <done>The recipe exists as the single home for the pinned five-host setup, the evidence and provenance rules, the ban set quoted from the shipped constant, the two loud skips, and the attended-lane statement including its absence on four hosts; it is indexed in exactly one tier and pointed at from the install README; every derived text gate is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The pin guard — one literal, a derived scan set, a reported count</name>
  <files>scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js, scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - scripts/check-foundation-guards.ts:1549-1620 (guardKitCounts — the guard header shape, the "report both derived numbers rather than a bare PASS" convention, the accumulate-then-report failure idiom)
    - scripts/check-foundation-guards.ts:405-440 (the ROOT resolution and the fixed-literal-subpath posture)
    - scripts/check-foundation-guards.ts:470-520 (the sorted directory-walk convention and why the local reads were deleted rather than kept as a second opinion)
    - scripts/vacuity.ts:40-100 (Measured and reportMeasured's four ordered branches — use it, do not hand-roll an `if (n === 0)`)
    - the guard registration tail at the end of scripts/check-foundation-guards.ts
    - agent-factory/checklists/browser-uat-recipe.md (as written in Task 1 — the pin literal's single home)
  </read_first>
  <behavior>
    - Against the live tree: exit 0 with a pass line reporting how many files were scanned and how
      many pin occurrences were checked.
    - Against a fixture tree where one markdown file carries a DIFFERENT version after the package
      name: one finding naming the file, the found version and the pin literal; the guard's fail
      count increases.
    - Against a fixture tree where a markdown file carries a floating specifier after the package
      name: one finding, same shape.
    - Against a fixture tree with ZERO pin occurrences: FAIL with the zero-elements-visited message,
      never a pass.
    - Against a fixture tree where the recipe itself is missing or carries no parseable pin literal:
      FAIL naming the recipe as the missing source of the literal — never a pass, and never a
      silently-assumed default version.
    - Two files each carrying an occurrence equal to the pin produce two visited elements and zero
      findings — equal occurrences are counted separately, never merged into one row.
  </behavior>
  <action>
Research finding F-03: the `@playwright/test` pin assertion D-08 says to clone DOES NOT EXIST — there
is no version-pin guard anywhere in `scripts/`. This is new construction on the general guard idiom,
not a copy. Write the test cases first, run them RED, then implement.

1. **One home for the literal.** Declare
   `const PLAYWRIGHT_MCP_PIN_SOURCE = "agent-factory/checklists/browser-uat-recipe.md"` as a fixed
   literal subpath joined onto the already-resolved ROOT — never argv-derived or content-derived,
   matching the path-traversal posture of the neighbouring constants. The guard EXTRACTS the pin
   version from that file (the first `@playwright/mcp@<version>` occurrence in it) and treats that
   extracted string as the authority. Do NOT re-declare the version number in the guard: the fact
   has one home, and a guard that restates it is the second home.

2. **Fail closed on a missing authority.** If the recipe is absent, unreadable, or contains no
   parseable pin occurrence, the guard FAILS naming the recipe. It must never fall back to a
   hard-coded version and it must never pass by finding nothing to check.

3. **Derive the scan set, sorted.** Walk `agent-factory/`, `docs/`, `install/`, and the repo-root
   `README.md` and `AGENTS.md` for markdown files, sorted by repo-relative forward-slash path so
   the output is byte-identical across runs and platforms. **Exclude `.planning/`** — the GSD
   planning tree is not kit or product documentation and deliberately records the registry's
   current version alongside the pin. State that exclusion in a comment with its reason, so the
   boundary of the predicate's input is written down rather than implied.

4. **Two counters, two origins, and `reportMeasured`.** Compute `expected` as the total number of
   `@playwright/mcp@` occurrences found across the derived file set, before the comparison loop.
   Increment `visited` inside the loop that compares each occurrence to the pin. Feed a
   `Measured<...>` into `reportMeasured` from `scripts/vacuity.ts` with the guard's own `pass`/`fail`
   emitters — do not hand-roll a zero check. Its four ordered branches give exactly the required
   behaviour: a zero-occurrence scan reads as "this check was NOT performed", a short scan reads as
   narrowed, findings are reported over the visited total, and the pass line carries the
   measurement.

5. **One finding per non-matching occurrence.** Each occurrence is its own element: two equal
   occurrences are two visited elements, and a non-matching one produces a finding naming the
   repo-relative file path, the line number, the version found, and the pin. Do not deduplicate by
   version — a merged row hides how many places are wrong.

6. **Register the guard** in the run tail beside the other foundation guards, with a one-line
   comment saying why it sits where it sits, and fold its return into the existing `FAILS` counter
   so the aggregator fails closed.

7. **Test it in `scripts/check-foundation-guards.test.ts`** using the file's existing
   `CHECK_ROOT`-style fixture-tree approach: build a temp tree per behaviour case and assert both
   the exit status and the message text. Include the zero-occurrence case and the
   missing-authority case explicitly — those are the two ways this guard could silently pass.

8. Run `npm run build` and commit the regenerated `scripts/check-foundation-guards.js`.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts</automated>
    <fails_when>non-zero exit, "0 passed" in the summary line, or the zero-occurrence case reporting a pass</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or a parity line naming check-foundation-guards.js</fails_when>
    <automated>node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit, or the final line is not `ALL CHECKS PASSED`, or the new guard's pass line reports 0 elements</fails_when>
  </verify>
  <acceptance_criteria>
    - The guard's pass line on the live tree contains a `visited/expected` pair with a non-zero
      numerator.
    - `grep -c 'reportMeasured' scripts/check-foundation-guards.ts` increased by at least 1 relative
      to its pre-task value.
    - The guard source contains no hard-coded `0.0.` version literal — the version is read from the
      recipe. Verified by asserting the guard function body has no numeric-version string literal.
    - A fixture tree with zero occurrences produces the ZERO-elements-visited failure text.
    - A fixture tree whose recipe carries no pin produces a failure naming the recipe path.
    - `node scripts/check-foundation-guards.js` still ends with `ALL CHECKS PASSED`.
  </acceptance_criteria>
  <done>Every `@playwright/mcp@` mention across the kit and docs is asserted equal to the one literal in the recipe; a zero-occurrence scan and a missing pin source both fail loudly; the pass line reports what was checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> the user's agent | The user's own coding agent fetches the MCP server with `npx` at its own discretion; grugops never installs it. |
| recipe prose -> the guard | The guard reads the pin literal out of a markdown file it also scans; a corrupted or absent authority must fail closed. |
| kit docs -> host operators | A documented registration command is copied verbatim into a user's configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-SC | Tampering | npm supply chain (`@playwright/mcp`, fetched by the user's agent) | high | mitigate | Every documented registration carries the pinned version; a floating specifier in a committed configuration is named as the anti-pattern; the new foundation guard asserts every mention across the kit and docs equals the one literal. RESEARCH.md's Package Legitimacy Audit returns OK with no `postinstall` script and no `[ASSUMED]`/`[SUS]`/`[SLOP]` verdict, so no legitimacy checkpoint is required. |
| T-31-11 | Repudiation | a pin guard that scans nothing | high | mitigate | `reportMeasured`'s vacuity and denominator floors: a zero-occurrence or short scan FAILS rather than printing a pass, and the pass line carries the measurement. |
| T-31-12 | Spoofing | a pin guard with a second copy of the version | medium | mitigate | The guard reads the literal out of the recipe and declares no version of its own; an acceptance criterion asserts the absence of a version literal in the guard body. |
| T-31-13 | Information disclosure | describing the content hash as a security control | medium | mitigate | The recipe states it is a recomputable integrity digest, not tamper-proof and not a token, and discloses the un-hashed transitive imports. |
| T-31-14 | Elevation of privilege | the attended browser lane opening unattended | high | mitigate | Two independent controls, both disclosed: the vendor-side force-disable under API-key or long-lived-token authentication, and the fail-closed positive-conjunction probe whose every non-matching outcome is a loud skip naming the failing clause. Neither is claimed to be sufficient alone. |
</threat_model>

<verification>
- `npm run check:imperative-lexicon && npm run check:banned-claims && npm run check:public-docs && npm run check:claim-anchors` green.
- `npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts` green.
- `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness` green.
- `node scripts/check-foundation-guards.js` ends with `ALL CHECKS PASSED`.
- `node scripts/validate-agent-factory.js` green.
- Never run bare `npm test`.
</verification>

<success_criteria>
- The pinned five-host browser-MCP setup is documented once, and `package.json` gained nothing (UATX-02).
- The attended lane's optional, human-stamped, four-host-absent character is stated once (UATX-03).
- Every pin mention across the kit and docs equals the one literal, and the guard reports what it
  scanned rather than asserting a pass (D-08).
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-03-SUMMARY.md` when done.
</output>

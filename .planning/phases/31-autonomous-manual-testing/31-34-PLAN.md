---
phase: 31-autonomous-manual-testing
plan: 34
type: execute
wave: 3
depends_on: ["31-33"]
gap_closure: true
files_modified:
  - scripts/runnable-ref/uat-spec-integrity.ts
  - scripts/runnable-ref/uat-spec-integrity.js
  - scripts/runnable-ref/uat-spec-integrity.test.ts
  - scripts/runnable-ref/fixtures/foreign-describe.uat.spec.ts
  - scripts/runnable-ref/fixtures/foreign-soft-assert.uat.spec.ts
  - scripts/runnable-ref/fixtures/local-helper-head.uat.spec.ts
  - scripts/runnable-ref/fixtures/foreign-framework.d.ts
  - tsconfig.fixtures.json
  - docs/audit/31-review-corpus-manifest.md
  - agent-factory/checklists/browser-uat-recipe.md
  - docs/audit/29-style-dispositions/31-34.md
  - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
autonomous: false
requirements: [UATX-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CR-23: the D-30 symbol-identity cutover made `foreign` TERMINAL, so for any callee the checker resolves to a non-Playwright declaration the spelling rule (`isBannedModifierPath`) is never asked at all. `BANNED_MODIFIER_HEADS` retains a bare `describe` and the file's own comment states the reason — `@playwright/test` exports no top-level `describe`, so the head is retained for `another framework's bare describe imported into a spec file` — which is EXACTLY the case the short-circuit made unreachable. Independently reproduced by `31-VERIFICATION.md` round 7 against the committed `.js`: a whole `describe.skip` group imported from a declared foreign module reports `0 findings`, exit 0, with `npx tsc --noEmit` exit 0; the pre-cutover build refuses the identical file at exit 1."
    - "The same loss holds for `BANNED_EXACT_PATHS = [\"expect.soft\"]` when `expect` comes from any other assertion library. A soft assertion in ANY framework makes a green lane certify a scenario whose acceptance criterion failed — the harm the tail set exists for, undecided again. Both instances are reproduced; both must be closed or both must be deleted with the deletion recorded."
    - "Silently keeping a published member the mechanism cannot reach is the one option that is NOT available. The recipe publishes `describe` as a banned head segment quoted from the exported constant; either the mechanism reaches it, or the member, the recipe's claim and the register entry all go together and the removal is recorded as a dated decision."
    - "The corpus exercised the only spelling of this head the identity route happens to decline. `directRuleSpellings` drives `describe.skip` where `describe` is UNDECLARED — measured still refusing at exit 1, because the checker resolves nothing and the spelling rule answers. A corpus that drives the spelling the head does NOT exist for is a denominator that cannot observe the property it claims; the new rows import the retained head from a DECLARED foreign module."
    - "WR-26's closure survives whatever is chosen. The `foreign` short-circuit was installed to make WR-26's false refusal impossible rather than narrower — a local helper binding named like a renamed import must still NOT be canonicalised into a construct the file does not contain. A control row drives exactly that shape and asserts it is not refused, in the same run as the new refusing rows."
    - "`UNRESOLVABLE_CALLEE_RESIDUALS`'s own closing criterion for its RR-10 member is `a reproduced case in which the spelling rule REFUSES a construct identity would have called foreign`. Two such cases are reproduced, so the member's disposition is RE-TAKEN in this plan rather than carried unexamined into an eighth round."
    - "The recipe's ban claim equals what the mechanism decides, quoted from the exported constants in both directions — the claim-follows-mechanism binding this phase has re-established at every register it owns."
    - "UATX-01 through UATX-06 stay UNCHECKED in `.planning/REQUIREMENTS.md`, the Phase 31 checkbox stays unchecked in `.planning/ROADMAP.md`, and every traceability row still reads `Gaps Found`."
  artifacts:
    - scripts/runnable-ref/uat-spec-integrity.ts
    - scripts/runnable-ref/uat-spec-integrity.js
    - scripts/runnable-ref/uat-spec-integrity.test.ts
    - scripts/runnable-ref/fixtures/foreign-describe.uat.spec.ts
    - scripts/runnable-ref/fixtures/foreign-soft-assert.uat.spec.ts
    - scripts/runnable-ref/fixtures/local-helper-head.uat.spec.ts
    - scripts/runnable-ref/fixtures/foreign-framework.d.ts
    - docs/audit/31-review-corpus-manifest.md
    - agent-factory/checklists/browser-uat-recipe.md
    - docs/audit/29-style-dispositions/31-34.md
  key_links:
    - "`resolveBannedModifier`'s identity answer <-> `isBannedModifierPath`, the spelling rule: whichever disposition is chosen, the wiring between them is stated once and asserted, so a published ban member cannot be unreachable while the recipe still names it."
    - "`BANNED_MODIFIER_HEADS` / `BANNED_EXACT_PATHS` <-> `agent-factory/checklists/browser-uat-recipe.md`'s published ban set: asserted equal in both directions."
    - "each new corpus row <-> its entry in `docs/audit/31-review-corpus-manifest.md`: the two-sided coverage equality `31-32` installed, extended in the same commit as the rows it names."
    - "the WR-26 control row <-> the refusing rows in the same run: the binding that proves the union stays in the refusing direction only."
  prohibitions:
    - statement: "A gap-closure plan must never flip a requirement checkbox, a traceability row or the phase checkbox. Only a verification round may do that."
      status: unverified
      verification: flagged
    - statement: "Silently keeping a published ban member the mechanism cannot reach is forbidden. If a member is abandoned, the member, the recipe's claim and the register entry are deleted together and the deletion is a dated decision."
      status: unverified
      verification: flagged
    - statement: "WR-26's closure is not traded away. Any widening keeps the union in the REFUSING direction only, and the local-helper control is driven in the same run."
      status: unverified
      verification: flagged
    - statement: "Every AST probe records its own `tsc --noEmit` exit code, so no finding rests on a construct the language refuses to compile."
      status: unverified
      verification: flagged
    - statement: "Never fake a passing gate, a test result, or a citation. A construct that could not be built is recorded `UNKNOWN - verify` with the reason."
      status: unverified
      verification: flagged
    - statement: "`hooks/guard.ts` is not touched and `FROZEN_GUARD_BLOB` is never re-based."
      status: unverified
      verification: flagged
    - statement: "Never merge a protected branch and never deploy to production without a named human confirmation."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-28-PLAN.md / D-30: identity is decided by the target's own checker, and a `checkpoint:decision` gates a one-way change to how the ban is decided. This plan is the second application of that rule, to the cutover's own boundary."
    - "31-32-PLAN.md: a manifest entry and its corpus row land in one commit, in both directions."
---

<objective>
Make a ban the framework does not own reachable again — or delete it in the open — and prove the
choice with the spelling the retained head actually exists for.

CR-23 (UATX-06). D-30 (2) made `foreign` terminal: `banPath = identity.kind === "framework" ?
identity.path : identity.kind === "foreign" ? null : spelled` at
`scripts/runnable-ref/uat-spec-integrity.ts:2472-2477`. Since `@playwright/test` declares no top-level
`describe`, a REAL `describe` is always `foreign`, so the retained `describe` head is dead for exactly
the case its own comment says it is retained for. The same holds for `expect.soft` when `expect` comes
from another assertion library. `31-VERIFICATION.md` round 7 and `31-REVIEW.md` both reproduced it
independently against the committed `.js`: `0 findings`, exit 0, `tsc` exit 0, against a pre-cutover
control that refuses the identical file at exit 1.

The suite is green on this because the corpus drives `describe.skip` where `describe` is UNDECLARED —
the one spelling the identity route declines anyway. The corpus never drives the spelling the head
exists for.

This plan opens the disposition to a named human, because the three available answers differ in what
the shipped gate promises, and one of them is a disclosed gate lowering.

Purpose: a soft or skipped assertion that a green lane certifies anyway is the exact harm UATX-06
names; a published ban the mechanism cannot reach is a claim outrunning its mechanism, which is what
this phase's requirement is about.
Output: the chosen disposition implemented; corpus rows driving the retained heads through a DECLARED
foreign module; the WR-26 control preserved; the recipe re-quoted; `RR-10`'s disposition re-taken;
decision `D-35` recorded.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@.planning/phases/31-autonomous-manual-testing/31-REVIEW.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-28-SUMMARY.md
@scripts/runnable-ref/uat-spec-integrity.ts
@agent-factory/checklists/browser-uat-recipe.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Note |
|---|---|---|
| the `banPath` wiring at `scripts/runnable-ref/uat-spec-integrity.ts:2472-2477` | CHANGED | Whatever the checkpoint chooses; the exact expression and its reason are recorded in the summary and in `D-35`. |
| `CalleeIdentity`'s `foreign` arm | POSSIBLY SPLIT | Under the recommended option the arm splits into an imported-declaration answer and a locally-declared answer; the new arm names are recorded in the summary and published in the file's own doc block. |
| `scripts/runnable-ref/fixtures/foreign-framework.d.ts` | NEW fixture | An ambient declaration for a foreign framework exporting `describe` (with `.skip`) and for a foreign assertion library exporting `expect` (with `.soft`). |
| `scripts/runnable-ref/fixtures/foreign-describe.uat.spec.ts` | NEW fixture | `describe.skip(...)` imported from the declared foreign module. |
| `scripts/runnable-ref/fixtures/foreign-soft-assert.uat.spec.ts` | NEW fixture | `expect.soft(...)` imported from the declared foreign assertion library. |
| `scripts/runnable-ref/fixtures/local-helper-head.uat.spec.ts` | NEW fixture | The WR-26 control: a LOCAL binding named like a renamed import, which must NOT be refused. |
| new `row(...)` ids for the three fixtures | NEW corpus rows | Each names `CR-23` in its `row()` rest arguments. |
| the round-7 section of `docs/audit/31-review-corpus-manifest.md` | CHANGED | Gains `CR-23` in the same commit as the rows. |
| `UNRESOLVABLE_CALLEE_RESIDUALS` member for `RR-10` | CHANGED disposition | Re-taken against the two reproduced cases that meet its own stated closing criterion. |
| `docs/audit/29-style-dispositions/31-34.md` | NEW disposition file | Rows for every clause changed in the recipe. |
| `D-35` | NEW dated decision | Appended to `31-CONTEXT.md`. |

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: What happens to a ban the framework does not own — three answers, one of them a disclosed lowering</name>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — CR-23 in full: the quoted `banPath` expression, the quoted `BANNED_MODIFIER_HEADS` comment explaining why `describe` is retained, both reproduction transcripts with their pre-cutover controls, the paragraph headed "Why the suite is green", the `UNRESOLVABLE_CALLEE_RESIDUALS[2]` closing criterion it quotes, and the whole `Fix:` block including its parenthetical about re-narrowing WR-26.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — gap 2's CR-23 paragraph, the two `artifacts:` rows for `scripts/runnable-ref/uat-spec-integrity.ts`, the `browser-uat-recipe.md` row, the first `missing:` bullet, and Behavioral Spot-Check row 5.
    - `scripts/runnable-ref/uat-spec-integrity.ts` — `BANNED_MODIFIER_HEADS` and its comment; `BANNED_EXACT_PATHS`; `isBannedModifierPath` and `isBannedModifierCall` in full; `resolveBannedModifier` in full including its three-answer doc block; the arm-(c) block at 2438-2480 in full; `frameworkSurface`; `frameworkModuleSymbol`; `UNRESOLVABLE_CALLEE_RESIDUALS` members in full.
    - `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` — the D-30 block in full, especially sub-decision 1, the `foreign` description, `WHAT D-30 DOES NOT ESTABLISH`, and the reversibility paragraph explaining why a named human opened that door.
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` — `directRuleSpellings` and the corpus rows that currently drive `describe.skip`, so the checkpoint is presented against what the suite actually exercises.
  </read_first>
  <decision>What happens to `describe` in `BANNED_MODIFIER_HEADS` and `expect.soft` in `BANNED_EXACT_PATHS`, now that the D-30 identity cutover's terminal `foreign` arm makes both unreachable for exactly the imported-from-another-framework case they are retained for.</decision>
  <context>
    Re-drive BOTH reproductions in this session BEFORE presenting the fork, each with its
    `tsc --noEmit` exit code, and quote them beside `31-VERIFICATION.md`'s and `31-REVIEW.md`'s. Do
    not present this checkpoint on a citation; present it on a measurement taken now. Re-drive the
    existing undeclared-`describe` corpus row in the same session too, because that row is why the
    suite is green over a ban that is off.

    Whichever option is chosen, Task 2 implements THAT choice. Silently keeping a published member the
    mechanism cannot reach is the one answer that is not available: the recipe publishes `describe` as
    a banned head quoted from the exported constant.
  </context>
  <options>
    <option id="union-head-declared-exemption">
      <name>Option A — the review's literal sketch: consult the spelling rule whenever identity did not decide a FRAMEWORK ban, re-narrowing WR-26 to heads the framework DOES declare</name>
      <pros>Smallest edit; restores the union the cutover removed; keeps WR-26's false refusal impossible for any head `@playwright/test` itself declares.</pros>
      <cons>Measured shortfall, which must be stated when presenting it: `@playwright/test` does not declare `describe`, so the `describe.skip` instance closes — but it DOES declare `expect`, so under a head-declared exemption the `expect.soft`-from-another-assertion-library instance stays OPEN. Option A closes one of the two reproduced cases.</cons>
    </option>
    <option id="split-foreign-by-provenance">
      <name>Option B (recommended) — split `foreign` by declaration provenance: imported from a module versus declared locally</name>
      <pros>The question the exemption actually needs is not "does the framework declare this head" but "is this callee a name the file IMPORTED, or a binding declared locally in the program's own sources". A symbol whose declarations reach an import binding or an external/ambient module declaration is another framework's export and the spelling rule answers; a parameter, local variable or local function is a local binding and the spelling rule is not consulted — precisely WR-26's shape. Both reproduced instances close and WR-26 stays closed.</pros>
      <cons>`foreign` becomes two answers, so the resolver's published identity vocabulary and its doc block both change, and the corpus grows a row per new answer.</cons>
    </option>
    <option id="delete-retained-members">
      <name>Option C — abandon the retention, in the open</name>
      <pros>Removes the divergence outright: `describe` leaves `BANNED_MODIFIER_HEADS`, `expect.soft` leaves `BANNED_EXACT_PATHS`, both leave the recipe's published set, and the deletion is recorded as a dated decision. Nothing published is unreachable.</pros>
      <cons>This is a gate LOWERING. A soft assertion in any non-Playwright assertion library, and a skipped group from any non-Playwright framework, become permitted — and the project's own doctrine requires that to be disclosed as a lowering rather than absorbed. Task 2's assertions become "the member is absent from the constant, from the recipe and from the register".</cons>
    </option>
  </options>
  <resume-signal>Reply with one of: `union-head-declared-exemption`, `split-foreign-by-provenance`, or `delete-retained-members`. The answer is recorded verbatim, with the date and the two options refused including Option A's measured shortfall on `expect.soft`, in the summary and in `D-35`, before any source file is edited.</resume-signal>
  <acceptance_criteria>
    - Both CR-23 reproductions are re-driven in this session against the committed `.js` BEFORE the fork is presented, each with its `tsc --noEmit` exit code recorded, and the outputs are quoted beside `31-VERIFICATION.md`'s and `31-REVIEW.md`'s.
    - The undeclared-`describe` control from the existing corpus is re-driven in the same session and quoted, so the checkpoint shows why the suite is green.
    - The summary quotes the user's decision verbatim with the date and names the two options refused, including Option A's measured shortfall on `expect.soft`.
    - No source file is edited before the answer is recorded.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/runnable-ref/uat-spec-integrity.js "$(git rev-parse --show-toplevel)" ; echo "EXIT=$?"</automated>
    <fails_when>the printed `EXIT=` line carries a status outside `{0,1,2}` — the runnable's own exit contract would then be broken before this checkpoint's premise can be trusted</fails_when>
  </verify>
  <done>A named human has chosen among restoring the ban, splitting the identity answer, and deleting the retained members, on measurements taken in this session rather than on quoted ones.</done>
  <reversibility rating="one-way">Each option changes what the shipped runnable promises about a whole family of constructs, and the recipe publishes that promise to every host. Option C additionally removes a published ban. Reverting any of them restores a state in which a `describe.skip` group or an `expect.soft` assertion, imported from any declared non-Playwright module, is accepted at exit 0 by a gate whose documentation says otherwise.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The retained head, driven through a DECLARED foreign module, refused — with WR-26's control in the same run (CR-23)</name>
  <files>scripts/runnable-ref/uat-spec-integrity.ts, scripts/runnable-ref/uat-spec-integrity.js, scripts/runnable-ref/uat-spec-integrity.test.ts, scripts/runnable-ref/fixtures/foreign-framework.d.ts, scripts/runnable-ref/fixtures/foreign-describe.uat.spec.ts, scripts/runnable-ref/fixtures/foreign-soft-assert.uat.spec.ts, scripts/runnable-ref/fixtures/local-helper-head.uat.spec.ts, tsconfig.fixtures.json, docs/audit/31-review-corpus-manifest.md</files>
  <precondition>Every probe root must be equipped exactly as `31-28`'s `equipTarget` equips one — a `tsconfig.json` with the recorded compiler options, a `node_modules` symlink to this repository's, and `fixtures/playwright-test.d.ts` under `types/` — per `docs/audit/31-round6-residuals.md` §1.3. A probe root missing any of these produces a could-not-run rather than a decision, and a result read from such a root is a false premise this phase has logged.</precondition>
  <read_first>
    - The Task 1 decision record — Task 2 implements what was chosen, not what was recommended.
    - `scripts/runnable-ref/uat-spec-integrity.ts` — the arm-(c) block, `resolveBannedModifier`, `canonicaliseHeadSegment`, `calleeDottedPath`, `deriveDeclaredBindings`, and `UNRESOLVABLE_CALLEE_RESIDUALS` in full.
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` — `equipTarget`, `directRuleSpellings`, `row()` with the rest parameter `31-32` added, `declaredCorpusFindingIds()`, and the fixture-driving harness, so the new rows use the existing machinery rather than a second one.
    - `scripts/runnable-ref/fixtures/playwright-test.d.ts` and `scripts/runnable-ref/fixtures/import-rename.uat.spec.ts` — the shape a fixture and its ambient surface take in this corpus.
    - `tsconfig.fixtures.json` — how fixtures are type-checked, so the new ambient module is included.
    - `docs/audit/31-round6-residuals.md` §1.3 — the probe environment constraint quoted in the precondition.
  </read_first>
  <behavior>
    - RED 1 (the verifier's own probe): a probe root equipped as `equipTarget` equips one, plus an ambient declaration for a foreign framework's `describe` with a `skip` member; a spec importing `test`/`expect` from `@playwright/test` and `describe` from the foreign module, wrapping a real test in `describe.skip(...)`. Against the committed `.js`: `0 findings`, exit 0, with `tsc --noEmit` exit 0.
    - RED 2 (the review's second instance): the same shape with `expect` imported from a foreign assertion library and `expect.soft(...)` called. Against the committed `.js`: `0 findings`, exit 0, `tsc` exit 0.
    - GREEN 1: RED 1 becomes `1 finding(s)`, exit 1, with the finding naming the construct in the identity-canonical spelling the reader sees elsewhere.
    - GREEN 2: RED 2 becomes `1 finding(s)`, exit 1. (Under Option A this row is recorded as NOT closed, with the shortfall stated; under Option C both rows become recorded lowerings with the member absent everywhere.)
    - GREEN 3 (WR-26's control, same run): a spec whose callee head is a LOCAL binding named like a renamed import — a parameter or a local function — reports `0 findings`, exit 0. The union stays in the refusing direction only.
    - GREEN 4 (the existing spelling stays decided): the undeclared-`describe` row `directRuleSpellings` already drives still refuses at exit 1, unchanged.
    - GREEN 5 (the register): `UNRESOLVABLE_CALLEE_RESIDUALS`'s RR-10 member is re-taken against its own stated closing criterion, and the register's two-directions binding and cardinality assertion are re-run.
    - CONTROL 1: every existing corpus row is re-run unmoved; the run's total finding count per fixture is quoted before and after.
    - CONTROL 2: `install/install.test.ts` green — the materialized host copy is still byte-equal to the committed source.
    - CONTROL 3: the `{0,1,2}` exit partition is re-measured unmoved.
  </behavior>
  <action>
    MOVEMENT 1 — reproduce both instances as FIXTURES, not as one-off scratch roots. Author
    `foreign-framework.d.ts` declaring a foreign framework module exporting `describe` with a `skip`
    member and a foreign assertion module exporting `expect` with a `soft` member; author
    `foreign-describe.uat.spec.ts` and `foreign-soft-assert.uat.spec.ts` driving each; author
    `local-helper-head.uat.spec.ts` as the WR-26 control. Include the ambient module in
    `tsconfig.fixtures.json` and record each fixture's `tsc --noEmit` exit code. Drive all three against
    the committed `.js` and quote the outputs — two accepts and one accept, of which only the third is
    correct. Add them as failing corpus rows using the existing `row(id, "CR-23")` marker.

    MOVEMENT 2 — implement the chosen disposition, at the point the identity answer is CONSUMED. Change
    the `banPath` wiring, and — if Option B was chosen — split the resolver's `foreign` answer by
    declaration provenance, updating the three-answer doc block to a four-answer one so the file's own
    decision record matches the vocabulary. Write the reason at the site as a property: the exemption
    exists to stop a LOCAL binding from being canonicalised into a construct the file does not contain,
    and an IMPORTED name from another framework is not that shape — D-14 names it directly.

    MOVEMENT 3 — re-take the register member. `UNRESOLVABLE_CALLEE_RESIDUALS`'s RR-10 member writes its
    own closing criterion; two cases meeting it are now reproduced. Re-word the member to what is true
    after this change, or close it and record the closure with its measurement, and re-run the
    register's both-directions binding and cardinality assertion.

    MOVEMENT 4 — extend the manifest in the SAME commit as the rows. Add `CR-23` to the round-7 section
    of `docs/audit/31-review-corpus-manifest.md`; the two-sided equality `31-32` installed will be red
    until both the entry and the rows exist, which is the property that binding was built for.

    MOVEMENT 5 — rebuild and mutation-prove. Rebuild, re-drive all three fixtures against the rebuilt
    `.js`, quote before and after. Apply a described mutant — the terminal `foreign` short-circuit
    restored — grep the rebuilt `.js` for its own marker, record that the grep FOUND it before any
    result is read, show the two refusing rows fail and the WR-26 control still pass, revert, show all
    three correct.
  </action>
  <acceptance_criteria>
    - Both RED transcripts are quoted with their `tsc --noEmit` exit codes, and each is matched against the corresponding transcript in `31-VERIFICATION.md` and `31-REVIEW.md`, with any difference in construction stated rather than smoothed over.
    - Each GREEN transcript is quoted with its finding text and exit code; a finding names the construct in one spelling, not two.
    - The WR-26 control is driven in the SAME run as the refusing rows and reports `0 findings`, exit 0.
    - The undeclared-`describe` row is re-driven and quoted unmoved.
    - `RR-10`'s member text after this plan is quoted in full, and the register's cardinality and two-directions binding are re-run and quoted.
    - `docs/audit/31-review-corpus-manifest.md` carries `CR-23` in its round-7 section, added in the same commit as the rows that name it, and the coverage equality passes.
    - The mutation proof records, in order: the mutant applied, the rebuilt `.js` grepped and its marker FOUND, the refusing rows failing, the control still passing, the revert, all three correct.
    - Every probe root is confirmed equipped per `docs/audit/31-round6-residuals.md` §1.3, with the confirmation printed before any result is read.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts scripts/uat-gate-exit-contract.test.ts install/install.test.ts</automated>
    <fails_when>non-zero exit, a summary line reporting `0 passed`, a corpus row reporting `0 findings` where its table expects a refusal, or the WR-26 control row reporting a finding</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness &amp;&amp; npx tsc -p tsconfig.fixtures.json</automated>
    <fails_when>non-zero exit, the build-parity or freshness step naming a committed `.js` that differs from a rebuild of its source, or the fixtures project reporting a type error in a new fixture</fails_when>
  </verify>
  <done>The head the ban set retains for another framework's own export is decided by the mechanism again — or is deleted everywhere it was published — and the corpus drives the spelling the head exists for, with WR-26's control passing in the same run.</done>
  <reversibility rating="one-way">This is the implementation of the checkpoint's one-way choice; the reversibility rationale recorded there applies. The fixtures and manifest entries additionally become part of the corpus's derived denominator.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The recipe says exactly what the mechanism decides, and D-35</name>
  <files>agent-factory/checklists/browser-uat-recipe.md, docs/audit/29-style-dispositions/31-34.md, scripts/runnable-ref/uat-spec-integrity.test.ts, .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the `browser-uat-recipe.md` artifact row naming line ~232 and its statement that the recipe claims a ban strength the mechanism does not deliver.
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — CR-23's closing paragraph on claim/mechanism divergence.
    - `agent-factory/checklists/browser-uat-recipe.md` — the whole ban-set section including the quoted head segments and the boundary list, plus whatever `31-32` added there for `SKIPPED_DIRECTORIES`.
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` — the existing case asserting the recipe's published set equals the exported constants in both directions, so this plan extends that binding rather than adding a second one.
    - `docs/audit/29-style-dispositions/31-32.md` — the row format and the base this round is measuring disposition debt against.
    - `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` — the D-29 through D-34 blocks, for D-35's required shape.
  </read_first>
  <action>
    MOVEMENT 1 — re-quote the ban set and state the boundary. Update the recipe so its published head
    and path sets are the exported constants after Task 2, and add one sentence naming what identity
    now decides versus what the spelling rule decides — in clear voice, since this is a security-shaped
    claim and the voice rule reserves caveman for role prompts. If Option C was chosen, the deleted
    members leave the recipe together with a sentence recording that the ban was narrowed and why.

    MOVEMENT 2 — extend the two-directions assertion. The existing case binds the recipe's published set
    to the exported constants; extend it so a member that exists in the constant but is UNREACHABLE by
    the mechanism cannot pass — driven by asserting the corpus carries a refusing row for every
    published head, keyed through the `row(...)` marker rather than by a hand-typed list.

    MOVEMENT 3 — write `docs/audit/29-style-dispositions/31-34.md` with a row for every recipe clause
    changed.

    MOVEMENT 4 — record D-35 in `31-CONTEXT.md` as a `#### Gap-closure decision — D-35 (2026-09-11,
    gap-closure round 7, wave 3, plan 31-34)` block in the shape D-29 through D-34 use: what forced it
    (CR-23, both instances, independently reproduced), which register failed (not membership, not
    resolution — the WIRING between the identity answer and the spelling rule, and the fact that a
    published member became unreachable without anyone saying so), the human's words quoted verbatim,
    the decision, what it does NOT establish, the two options refused, the reversibility rating, and the
    three places that must agree.
  </action>
  <acceptance_criteria>
    - The recipe's published head and path sets are byte-equal to the exported constants after Task 2, asserted in both directions by the existing case.
    - The extended assertion — every published head has a refusing corpus row — is present and was watched red under a seeded published-but-unrowed member, then reverted.
    - `docs/audit/29-style-dispositions/31-34.md` carries a row for every changed recipe clause, and `npm run check:diff-disposition` shows no growth against the count `31-31` recorded, with both counts quoted.
    - D-35 is appended to `31-CONTEXT.md`, quotes the human's decision verbatim, and names the two options refused with their measured consequences.
    - `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` are byte-unchanged by this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm run check:diff-disposition &amp;&amp; npm run check:claim-anchors &amp;&amp; npm run check:banned-claims</automated>
    <fails_when>non-zero exit, or the disposition gate naming a changed clause in `agent-factory/checklists/browser-uat-recipe.md` with no row in `docs/audit/29-style-dispositions/31-34.md`</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts</automated>
    <fails_when>non-zero exit, or the recipe-binding case reporting a set difference between the published ban set and the exported constants</fails_when>
    <automated>git diff --quiet -- .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <fails_when>non-zero exit — a requirement checkbox, a traceability row or the phase checkbox moved inside a gap-closure plan</fails_when>
  </verify>
  <done>The recipe publishes exactly the ban the mechanism now applies, every published head has a refusing corpus row, and the decision with its refused alternatives is recorded as D-35.</done>
  <reversibility rating="reversible">Prose and one extended assertion. Reverting restores a recipe that publishes a ban measured unreachable.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a spec author's import list → the modifier ban | the spec file chooses which module a callee's symbol resolves to, and that choice currently decides whether the ban applies at all |
| the host repository's `typescript` and `tsconfig.json` → the identity resolver | the target supplies the checker the ban is decided by; what it declares decides what is `framework` |
| the recipe's published ban set → the agent authoring a UAT spec | the recipe is what an agent reads to know what is forbidden; a claim it publishes that the mechanism does not enforce is guidance that certifies nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-34-01 | Tampering | `resolveBannedModifier`'s terminal `foreign` arm | high | mitigate | An agent that imports `describe` or `expect` from any declared non-Playwright module skips the ban entirely at exit 0, so a whole scenario group can be disabled or softened while the gate reports green. Task 2 implements the chosen disposition and drives both spellings as corpus rows. |
| T-31-34-02 | Repudiation | `agent-factory/checklists/browser-uat-recipe.md`'s published ban set | high | mitigate | A published ban the mechanism cannot reach lets a green gate be cited as evidence the construct was checked. Task 3 binds the published set to the constants and to a refusing corpus row per head. |
| T-31-34-03 | Spoofing | the new ambient foreign-module fixtures | medium | mitigate | The fixtures declare modules that do not exist on disk; they are confined to `scripts/runnable-ref/fixtures/`, type-checked by `tsconfig.fixtures.json`, and excluded from the shipped installer copy set. The installer control re-measures that the host copy is unchanged. |
| T-31-34-04 | Elevation of privilege | widening the spelling rule over `foreign` callees | medium | mitigate | A widened union could refuse a legitimate local helper (WR-26). The union is kept in the refusing direction only and the local-helper control is driven in the same run as the refusing rows. |
| T-31-34-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan runs NO package-manager install; `@playwright/test` is explicitly NOT installed (CLAUDE.md fixes the dev dependency set), which is why the foreign and framework surfaces are ambient fixtures. `git diff -- package.json package-lock.json` is asserted EMPTY at the plan's closing gate. |
</threat_model>

<verification>
- Both CR-23 instances are re-driven against the rebuilt `.js` with their `tsc --noEmit` exit codes, before and after, and quoted.
- The WR-26 local-helper control passes in the same run as the refusing rows.
- Every published banned head has a refusing corpus row, asserted through the `row(...)` marker rather than a hand-typed list.
- `RR-10`'s disposition is re-taken with its measurement quoted.
- `docs/audit/31-review-corpus-manifest.md` and the corpus rows moved in one commit and the coverage equality passes.
- `npm run build && npm run typecheck && npm run check:build-parity && npm run freshness` exits 0, and `npx tsc -p tsconfig.fixtures.json` exits 0.
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` are byte-unchanged.
</verification>

<success_criteria>
- The disposition chosen by a named human is implemented, and both reproduced instances are recorded CLOSED or recorded as a disclosed lowering — never left green and unreachable.
- The corpus drives the retained heads through a DECLARED foreign module, which is the spelling they exist for.
- The recipe publishes exactly what the mechanism decides.
- `D-35` is appended to `31-CONTEXT.md` with the human's words quoted.
- No requirement checkbox, traceability row or phase checkbox moved.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-34-SUMMARY.md` when done.
</output>

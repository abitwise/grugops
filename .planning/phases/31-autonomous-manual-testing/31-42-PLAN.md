---
phase: 31-autonomous-manual-testing
plan: 42
type: execute
wave: 11
depends_on: ["31-41"]
gap_closure: true
files_modified:
  - scripts/runnable-ref/uat-spec-integrity.ts
  - scripts/runnable-ref/uat-spec-integrity.js
  - scripts/runnable-ref/uat-spec-integrity.test.ts
  - scripts/runnable-ref/fixtures/
  - agent-factory/checklists/browser-uat-recipe.md
  - docs/audit/29-style-dispositions/31-42.md
  - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
autonomous: true
requirements: [UATX-02, UATX-05, UATX-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-37: `SURFACE_TRUNCATION_ARMS` is introduced as `every way the walk can stop early, DERIVED from the record above rather than re-typed` — and the record it derives from is HAND-AUTHORED, so the derivation is over the sentences somebody wrote rather than over the positions the walk actually stops at. The review names four exception routes that swallow a subtree and record nothing. A fifth swallow sits in the same walk that the review did not name. The fix is therefore to DERIVE the swallow-site set from the walk's own syntax tree, assert its cardinality, and require every derived site to have either an arm in the published record or an entry in the published residual register. Asking which SET the predicate enumerates is this phase's own recorded probe, and a hand-typed record behind a derived-looking constant is exactly what it finds."
    - "The asymmetry WR-37 names is real and is the argument for the fix: `hasExpandableMembers` fails CLOSED on the same class of throw, with its reason written out, while four sibling positions fail OPEN with no record. One walk, two opposite dispositions for one event, and nobody decided the pair — which is the same shape as CR-26 one module over."
    - "The four routes are DRIVEN, not reasoned about. `31-VERIFICATION.md` round 8 recorded WR-37 as `not independently exploitable this session (requires a TypeScript-checker throw this repository cannot synthesise)` — and the review's own fix names the instrument that synthesises it: a stub checker that throws at each position. A residual whose only defence is that nobody built the probe is a residual nobody has tested."
    - "WR-38: the `a published ban member must be REACHABLE` binding derives its expected side from two of the four published ban sets. `BANNED_MODIFIER_TAILS` and `BANNED_CONFIGURED_PATHS` are published — the recipe quotes all four by value and the membership-equality case asserts all four — with no reachability row, so only one of the four tails is driven. CR-23 was exactly `a published member the mechanism cannot reach while equality stays green`. The fix derives the expected side from the module's own published ban-set FAMILY, so a fifth ban set joins the binding by existing rather than by somebody remembering it."
    - "IN-19: the skipped-directory disclosure fires on essentially every real host run, because two of the five names it watches are present at the root of virtually every repository, and it lands on the stream the D-12 contract reserves for could-not-run reasons — where a gate reader is trained to treat output as a loud skip. The recipe's measured CONTROL of zero bytes is taken on a probe tree with none of the five names and is not representative of a host. The claim is corrected against a REAL host reading, taken on this repository, rather than adjusted by argument."
    - "The disclosure's purpose is D-33 (5)'s: make a NARROWED DENOMINATOR legible. The names are therefore partitioned by whether a skip could plausibly hide THIS repository's own evidence, that partition is derived from the published set rather than typed beside it, and the omitted names are listed once in the recipe so nothing becomes invisible."
    - "Every claim the recipe makes about the checker is quoted from, or bound in both directions to, the exported constant it describes — the binding this file already carries for its ban sets, its truncation causes and its residual register, extended to whatever this plan changes. A recipe sentence stronger than the mechanism is UATX-06's own failure mode."
    - "UATX-01 through UATX-06 stay UNCHECKED in `.planning/REQUIREMENTS.md`, the Phase 31 checkbox stays unchecked in `.planning/ROADMAP.md`, and every traceability row still reads `Gaps Found`. UATX-06 was verified for the first time in round 8 and that verdict is a reading of round 7's tree, not a licence to flip a box here."
  artifacts:
    - scripts/runnable-ref/uat-spec-integrity.ts
    - scripts/runnable-ref/uat-spec-integrity.js
    - scripts/runnable-ref/uat-spec-integrity.test.ts
    - agent-factory/checklists/browser-uat-recipe.md
    - docs/audit/29-style-dispositions/31-42.md
    - .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
  key_links:
    - "the walk's derived swallow-site set <-> the published truncation arms and the published residual register: every site the walk can stop at has an arm or an entry, asserted over a derived set with its cardinality, so a sixth swallow cannot arrive unrecorded."
    - "the module's published ban-set family <-> the reachability row census: every member of every published ban set is driven to a refusal by a row keyed to the constant, both sides derived."
    - "`SKIPPED_DIRECTORIES`'s partition <-> the disclosure's emission condition and the recipe's list: one published set, one derived partition, and the omitted half named in the recipe."
    - "the recipe's measured CONTROL <-> a real host run of this repository: the reading the claim is made against, re-taken rather than inherited."
  prohibitions:
    - statement: "A set asserted to be complete must be DERIVED from the positions it describes, never from a hand-authored record that merely looks derived. A derivation over sentences is a derivation over somebody's attention."
      status: unverified
      verification: flagged
    - statement: "A fail-open that is deliberate is PUBLISHED with its measurement in the residual register. Silence is not a disposition, and neither is a docstring that argues for it without a number."
      status: unverified
      verification: flagged
    - statement: "A recipe claim is quoted from or bound in both directions to the exported constant it describes. A claim stronger than the mechanism is the failure this requirement exists to close."
      status: unverified
      verification: flagged
    - statement: "A gap-closure plan must never flip a requirement checkbox, a traceability row or the phase checkbox."
      status: unverified
      verification: flagged
    - statement: "Never fake a passing gate, a test result, or a citation. A reading that could not be taken is marked `UNKNOWN - verify` with an owner."
      status: unverified
      verification: flagged
    - statement: "Never merge a protected branch and never deploy to production without a named human confirmation."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-35-PLAN.md / D-36: a reached bound routes to a could-not-run exit with a named cause, and the cause and the recipe bullet follow the one record automatically. This plan extends that record rather than adding a second one."
    - "31-28-PLAN.md / D-30: the modifier ban is decided by SYMBOL IDENTITY from the target's own parser. Unchanged here; this plan closes two completeness gaps around it."
---

<objective>
Close the two completeness gaps round 8 left open on the AST modifier ban, and make the
skipped-directory disclosure's published claim true of a host rather than of a probe tree.

Round 8 verified UATX-06's predicate family for the first time in eight rounds and left two disclosed
residuals plus one Info:

- **WR-37** — the walk's published `every way the walk can stop early` record is hand-authored, and at
  least four positions inside the walk stop it without recording anything. A fifth sits in the same
  walk that the review did not name, which is why the fix derives the set rather than closing the four.
- **WR-38** — the new reachability binding's expected side comes from two of four published ban sets,
  so three of the four banned tails and the whole configured-path axis have no row keyed to their
  constant. That is CR-23's exact shape, still open on two axes.
- **IN-19** — the disclosure fires on nearly every host run, on the stream reserved for could-not-run
  reasons, and the recipe's control reading was taken where it cannot fire.

Purpose: UATX-06's whole claim is that the checker's published description equals what the checker
decides. A completeness record assembled from attention rather than from the code, and a reachability
binding over half its own subject, are both places where that equality holds only by luck.
Output: a derived swallow-site set with an arm or a published residual per site, each driven by a stub
checker; a reachability binding over every published ban set, derived; a partitioned disclosure with a
real host reading behind its recipe claim; decision `D-42` recorded.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@.planning/phases/31-autonomous-manual-testing/31-REVIEW.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-35-SUMMARY.md
@.planning/phases/31-autonomous-manual-testing/31-34-SUMMARY.md
@scripts/runnable-ref/uat-spec-integrity.ts
@agent-factory/checklists/browser-uat-recipe.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Note |
|---|---|---|
| `deriveSurfaceSwallowSites` | NEW derived helper in `scripts/runnable-ref/uat-spec-integrity.test.ts` | Enumerates from the walk's own syntax tree every position that catches and continues, or catches and returns a substitute value, inside the framework-surface walk. Attributed to the nearest enclosing named scope, count asserted. |
| `SURFACE_TRUNCATION_REACHED` | CHANGED exported record | Grows by the arm the swallow sites route to. `SURFACE_TRUNCATION_ARMS`, `SURFACE_TRUNCATED_CAUSE` and the recipe bullet follow it automatically, as `D-36` already wired them. |
| `UNRESOLVABLE_CALLEE_RESIDUALS` | CHANGED exported register | Gains an entry for any swallow site whose fail-open is decided to be deliberate, each with the measurement that decided it. |
| A stub-checker harness | NEW test seam | Drives the walk with a checker that throws at one chosen position, so each derived swallow site is exercised rather than reasoned about. |
| `reachRowIdFor*` census | CHANGED derived test | Expected side derived from the module's published ban-set family rather than from two named constants. |
| New corpus rows and fixtures | NEW | One reachability row per previously-unrowed published ban member, with the fixture each needs. |
| `SKIPPED_DIRECTORIES` partition | NEW derived partition | The published set split by whether a skip could plausibly hide this repository's own evidence, derived from the published set with both halves asserted to cover it. |
| `agent-factory/checklists/browser-uat-recipe.md` | CHANGED prose | The disclosure's real host behaviour, the omitted names listed once, and the control reading re-taken. |
| `docs/audit/29-style-dispositions/31-42.md` | NEW disposition file | One row per changed clause in the watched corpus. |
| `D-42` | NEW dated decision | Appended to `31-CONTEXT.md`. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-37 — derive the walk's swallow sites, give each an arm or a published residual, and drive each</name>
  <files>scripts/runnable-ref/uat-spec-integrity.ts, scripts/runnable-ref/uat-spec-integrity.js, scripts/runnable-ref/uat-spec-integrity.test.ts, agent-factory/checklists/browser-uat-recipe.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — WR-37 in full: the quoted introduction of the published arms record, all four cited positions with their comment text, the observation that `hasExpandableMembers` fails closed on the same class of throw with its reason written out, and the `Fix:` block naming both the new arm and the stub checker.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the Required Artifacts row for the walk, which records WR-37 as present in source and not independently exploited this session, and the Anti-Patterns row for the same finding.
    - `scripts/runnable-ref/uat-spec-integrity.ts` — read in full: `SURFACE_NODE_BOUND` and `SURFACE_DEPTH_BOUND` with their docstrings; `SURFACE_TRUNCATION_REACHED` with every arm's comment, including the two arms that are explicitly `not a bound` and the reasoning for including them; `SURFACE_TRUNCATION_ARMS`; `SURFACE_TRUNCATED_CAUSE` and `surfaceTruncatedCause`; `frameworkSurface` from its signature to its return, every `try`, every `catch` and every comment inside them, including the declaration-adding helper's own catch and the standard-library-only predicate; `containerTypeArguments` with its whole docstring, which ARGUES for its fail-open; `hasExpandableMembers` with its whole docstring, which argues for failing closed on the same class; and `UNRESOLVABLE_CALLEE_RESIDUALS` in full.
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` — the cases that assert the truncation arms record is bound to the recipe in both directions, the cases that drive the node bound and the depth bound to a could-not-run exit, and the case asserting a leaf at the bound is not a truncation. The new sites must reach the same exit through the same authority, not a second one.
    - `agent-factory/checklists/browser-uat-recipe.md` — the bullets that quote the truncation causes and the residual register, because both follow the record automatically and a grown record changes them.
  </read_first>
  <behavior>
    - PREMISE: the derivation finds swallow sites at all, and finds MORE THAN the four the review named — or, if it finds exactly four, says so explicitly, because `31-REVIEW.md`'s four is a hand-typed list and agreement with it is a result rather than an assumption. The count is printed and floored before any conclusion.
    - The derived set includes every position inside the walk that catches an exception and either continues or returns a substitute value. The declaration-adding helper's own catch and the container-argument helper's catch are candidates the review's four does not name; whether they are members is the derivation's answer, not the author's.
    - Every derived site has EITHER an arm in the published truncation record OR an entry in the published residual register. A site with neither fails the case, naming the site and its enclosing scope.
    - RED, per site: a stub checker that throws at exactly that position makes the run accept at exit zero with no diagnostic naming the truncation — the fail-open WR-37 describes. Each RED reading is taken before that site is changed.
    - GREEN, per site: after the fix, the same stub makes the run take the could-not-run route with the cause naming the new arm; or, where the fail-open is deliberately kept, the run's behaviour is unchanged AND the register entry exists carrying the measurement that decided it.
    - The measurement that decides each `keep the fail-open` choice is a real number, not an argument: how often that position throws on a genuine run of this repository's own corpus. A docstring arguing that a throw there is ordinary, with no count, is the shape this plan is closing.
    - `hasExpandableMembers`'s fail-closed direction is unchanged and re-driven as a control, because the asymmetry is the argument and the safe half of it must be shown still safe.
    - The bound arms are re-driven as controls: the node bound and the depth bound still reach exit two with their own causes, and a leaf at the bound is still not a truncation.
    - The recipe's truncation bullets follow the grown record automatically, asserted in both directions, and the count of causes in the recipe equals the count of arms in the record.
  </behavior>
  <action>
    MOVEMENT 1 — derive the set before fixing anything. Write a helper in the test file that parses the
    module and walks the framework-surface function's body, collecting every position that catches an
    exception and either continues the loop or returns a value in place of the one it could not
    produce, attributed to the nearest enclosing named scope. Assert the count with a message naming
    what a changed count means. Print the derived member list. Compare it against the four positions
    `31-REVIEW.md` names and record the difference — a derivation that merely confirms a hand-typed
    list has not been tested, and a derivation that finds a fifth is the whole reason to derive.

    MOVEMENT 2 — build the instrument the residual's defence depended on not existing. Give the walk a
    way to be driven with a substitute checker — a narrow test seam, the same kind this module already
    uses elsewhere, not a second code path — and write a stub checker that throws from one chosen
    member. Take the RED reading at every derived site: what the run does, what it exits with, and what
    it writes. Record all of them in a table before changing a line of the walk.

    MOVEMENT 3 — decide each site with a number. For each derived site, either route it to a new arm in
    the published truncation record — which carries the cause string and the recipe bullet with it
    automatically, as the record was built to do — or keep its fail-open and publish it in the residual
    register WITH the count of times that position throws on a genuine run of this repository's corpus.
    Where the container-argument helper's docstring already argues for its fail-open, the argument is
    kept only if the number supports it; the number goes in the register entry either way.

    MOVEMENT 4 — assert the closure over the derived set, not over the fixed sites. Add the case that
    requires every derived site to have an arm or a register entry, in both directions: a register
    entry naming a site the derivation does not find is stale and fails too. Add the per-site GREEN
    drives. Re-drive the two bound arms, the leaf-at-the-bound case and the fail-closed control.

    MOVEMENT 5 — carry the recipe with the record and rebuild. Let the recipe's bullets follow the
    grown record, assert the two-direction binding and the cause count, write the disposition rows for
    every changed recipe clause into `docs/audit/29-style-dispositions/31-42.md`, then rebuild,
    re-derive the hook manifest and re-check freshness and build parity.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the premise case reports a derived swallow-site count of zero, or the arm-or-register case names a site with neither, or the reported test names do not include one stub-checker drive per derived site</fails_when>
    <automated>npm run build && npm run typecheck && npm run check:build-parity && npm run freshness</automated>
    <fails_when>non-zero exit, or the parity step names any tracked `.js` file as moved, or the freshness step reports any committed output not matching a rebuild</fails_when>
    <automated>node scripts/check-uat-oracles.js && node scripts/check-foundation-guards.js</automated>
    <fails_when>non-zero exit, or either command's output does not end with its all-checks-passed line — these two are UATX-05's and UATX-02's own floors and a red here means this task moved a guard it was not aimed at</fails_when>
  </verify>
  <acceptance_criteria>
    - A helper in the test file produces the swallow-site set by parsing the module, and its cardinality is asserted with a message naming what a changed count means; the derived member list is printed by the run.
    - The summary records the derived member list beside the four positions `31-REVIEW.md` names, and states explicitly whether the derivation found more, fewer or the same.
    - Every derived site has an arm in the published truncation record or an entry in the published residual register, asserted in both directions so a stale entry also fails.
    - Every derived site is driven by a stub checker that throws at that position, with the pre-fix and post-fix readings both recorded.
    - Every site whose fail-open is kept carries, in its register entry, the count of times that position throws on a genuine run of this repository's corpus.
    - The fail-closed control at the depth bound still reports a truncation for an unreadable node, and the two bound arms still reach a could-not-run exit with their own causes.
    - The recipe's truncation causes are bound to the published record in both directions and their count equals the record's arm count.
    - Every changed recipe clause has a row in `docs/audit/29-style-dispositions/31-42.md`.
  </acceptance_criteria>
  <done>The set of positions where the walk can stop is derived from the walk rather than from a record somebody wrote, every member has an arm or a measured published residual, and every member is driven by an instrument that did not exist when the residual was accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-38 — a reachability row for every member of every published ban set, both sides derived</name>
  <files>scripts/runnable-ref/uat-spec-integrity.test.ts, scripts/runnable-ref/fixtures/, agent-factory/checklists/browser-uat-recipe.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — WR-38 in full, including which members have no row today (three of the four tails and the whole configured-path axis), the observation that CR-23 was exactly a published member the mechanism could not reach while equality stayed green, and the `Fix:` block's suggested row-id shapes and its requirement that the seeded-fail case be extended to a seeded tail.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the Required Artifacts row for the recipe, which records WR-38 as a monitoring gap on this round's own new safeguard, and the Anti-Patterns row for it.
    - `scripts/runnable-ref/uat-spec-integrity.ts` — all four published ban constants with their docstrings and the rule that consumes each; the path-normalisation and identity-resolution functions that decide membership, because the binding's claim is that the SAME resolution applies to all four sets and that claim must be checked rather than quoted; and the exported names of the ban family, because the derivation keys on them.
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` — the reachability binding and its two row-id helpers in full, the seeded-fail case beside it, the membership-equality case that already asserts all four sets against the recipe, and the corpus row census the binding reads. Read how a row is declared and how the census finds it, because new rows must be found by the same census.
    - `scripts/runnable-ref/fixtures/` — the existing fixtures the present rows drive, especially the ones carrying a declared foreign framework, and `scripts/runnable-ref/fixtures/playwright-test.d.ts` and `foreign-framework.d.ts`, because a new fixture must type-check against one of those declared surfaces.
    - `agent-factory/checklists/browser-uat-recipe.md` — the bullets quoting all four ban sets by value.
  </read_first>
  <behavior>
    - PREMISE: the published ban-set family is derived from the module's own exports and is non-empty, and the row census is non-empty. Both floored before any conclusion, because a vacuous binding is this phase's logged instance 4 and instance 14.
    - The expected side is derived from the FAMILY, not from named constants: every member of every published ban set contributes a required reachability row id. A fifth published ban set therefore joins the binding by existing.
    - Every required row exists and drives its member to a refusal at a non-zero exit, with the member's own spelling present in the output. The three previously-unrowed tails and the configured-path axis each gain a row and whatever fixture it needs.
    - The claim that one identity resolution applies to all four sets is CHECKED: each set's membership rule is driven through the same resolution with a declared-foreign binding, so a set whose rule bypassed the resolution would be visible rather than asserted.
    - SEEDED FAIL, extended: a published-but-unrowed member is caught on the tail axis as well as on the head axis, so the new axes are proven non-vacuous the same way the head axis is.
    - CONVERSE: a row whose id names a member no published set contains fails too, so a stale row cannot pad the census.
    - The recipe's four quoted ban lists stay bound to the constants in both directions, and the binding is re-run after the new rows land.
  </behavior>
  <action>
    Derive the published ban-set family from the module's own exports rather than importing two
    constants by name: collect the exported ban constants, assert the family's cardinality with a
    message naming what a changed count means, and build the required-row set from every member of
    every family entry. Handle the two shapes the family actually has — sets of segment literals and a
    mapping from a dotted path to an enabling option — without special-casing either by name; the row
    id derives from the member, and the member derivation reads the constant's own shape.

    Add the missing rows. For each, add the fixture it needs under the fixtures directory, written
    against one of the two declared surfaces the fixture typecheck target already includes so
    `npm run typecheck` covers it, and drive the checker over a target repository staging that fixture.
    Assert the refusal's exit status and that the member's own spelling appears in the output.

    Check the shared-resolution claim rather than quoting it: drive at least one member of each of the
    four sets through a declared-foreign binding, the spelling CR-23 was about, and assert each still
    refuses. A set whose rule did not consult the identity resolution would pass the membership
    equality and fail here, which is the whole point.

    Extend the seeded-fail case to the tail axis, in-process rather than by editing a constant, the way
    the existing head-axis seeded fail already does it.

    Add the converse assertion over the row census, then re-run the recipe's four-set membership
    binding and add disposition rows for any recipe clause this task changes.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the premise case reports an empty ban-set family or an empty row census, or the binding names a published member with no reachability row</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit — a new fixture that does not type-check against a declared surface is a corpus file outside the reach of a typecheck, which this repository has already paid for once</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, or the summary reports any failed test, or fewer than 66 test files</fails_when>
  </verify>
  <acceptance_criteria>
    - The required-row set is built from a ban-set family derived from the module's exports, with the family's cardinality asserted; no constant is imported by name for the purpose of building the expected side.
    - Every member of every published ban set has a reachability row that drives it to a refusal at a non-zero exit with the member's own spelling in the output — including the three tails and the configured path that had none.
    - Each of the four published sets has at least one member driven through a declared-foreign binding and still refused.
    - The seeded-fail case covers the tail axis as well as the head axis, run in-process without editing a published constant.
    - A row naming a member no published set contains turns the census red.
    - Every new fixture is inside the fixture typecheck target's include globs and `npm run typecheck` exits zero.
    - The recipe's four quoted ban lists remain bound to the constants in both directions.
  </acceptance_criteria>
  <done>Every published ban member is proven reachable by a row keyed to the constant that publishes it, across all four sets, with the expected side derived from the module's own export family and the axis proven non-vacuous on more than one of its arms.</done>
</task>

<task type="auto">
  <name>Task 3: IN-19 — partition the disclosure, take a real host reading, and record D-42</name>
  <files>scripts/runnable-ref/uat-spec-integrity.ts, scripts/runnable-ref/uat-spec-integrity.js, scripts/runnable-ref/uat-spec-integrity.test.ts, agent-factory/checklists/browser-uat-recipe.md, docs/audit/29-style-dispositions/31-42.md, .planning/phases/31-autonomous-manual-testing/31-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — IN-19 in full, including the real host reading it took on this repository, its observation that the line lands on the stream the D-12 contract reserves for could-not-run reasons, its point that the counts are of directory entries rather than of hidden specs, and both options in its `Fix:` block.
    - `scripts/runnable-ref/uat-spec-integrity.ts` — `SKIPPED_DIRECTORIES` with its docstring; the walk position that counts a skipped entry; the disclosure renderer; the call site that emits it and the comment explaining why it sits immediately after the derivation and before every returning branch; and the D-12 stream contract wherever it is stated in this module.
    - `scripts/runnable-ref/uat-spec-integrity.test.ts` — every case that asserts the disclosure's content, its emission condition, or the zero-bytes control on a clean probe tree, and every case that asserts the D-12 stream contract. Both sets constrain what may move.
    - `agent-factory/checklists/browser-uat-recipe.md` — the bullets describing the skipped-directory disclosure and the measured control, and the bullets describing which stream carries which kind of output.
    - `agent-factory/workflows/05-pr-quality-gate.md` — how a gate reader is instructed to treat output on the stream in question, because IN-19's substance is that a reader trained on that contract mis-reads this line.
    - `.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md` — `D-33 (5)`, which is the decision the disclosure implements and whose purpose the partition must serve, and the `D-39`, `D-40`, `D-41` blocks written earlier in this round.
  </read_first>
  <action>
    MOVEMENT 1 — take the real reading before deciding anything. Run the checker over this repository
    and record verbatim what it writes and on which stream, and record the same for a probe tree
    carrying none of the watched names. The recipe's present control is the second reading presented as
    if it were the first; the correction starts from having both.

    MOVEMENT 2 — partition the published set by derivation. Split the watched names by whether a skip
    at that name could plausibly hide THIS repository's own UAT evidence. Derive both halves from the
    published set and assert their union equals it and their intersection is empty, so a sixth watched
    name lands in one half rather than falling out of both. Emit the disclosure only for the half whose
    skip could hide evidence, and list the omitted half once in the recipe by value read from the
    partition rather than typed.

    MOVEMENT 3 — decide the stream with the contract in hand, not by preference. Read what the D-12
    contract reserves each stream for and what a gate reader is instructed to do with each. If the
    disclosure stays where it is, the recipe must say plainly that it shares a stream with could-not-run
    reasons and how a reader tells them apart; if it moves, every case asserting the stream contract
    must be re-driven and the move recorded as a decision with its reason. Either way, state which was
    chosen and why.

    MOVEMENT 4 — make the counted thing legible. The counts are of directory entries, not of hidden
    specs. Say so in the emitted line itself rather than only in the recipe, because the line is what a
    reader meets.

    MOVEMENT 5 — correct the recipe against the readings, write the disposition rows for every changed
    clause, rebuild, re-derive the hook manifest and re-check freshness. Then append the `D-42` decision
    body to `31-CONTEXT.md` under a heading following the established shape, stating: what forced it
    (WR-37, WR-38, IN-19); the rule that a completeness record is derived from the positions it
    describes; the rule that a kept fail-open is published with a number; the derived swallow-site count
    and how it compared to the review's four; the ban-set family's cardinality and the rows added; the
    partition and the stream decision with the two host readings; what `D-42` does NOT establish — at
    minimum the installed-package surface magnitude that remains `UNKNOWN - verify`, the hand-transcribed
    declaration corpus's drift from upstream, and the Windows leg as `R-03`; and the reversibility rating.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/runnable-ref/uat-spec-integrity.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the partition case reports a watched name in neither half or in both</fails_when>
    <automated>node scripts/runnable-ref/uat-spec-integrity.js .</automated>
    <fails_when>the command writes a skipped-directory line naming only names from the omitted half of the partition, or writes a line that does not state that the counts are of directory entries — this is the real host reading the recipe's claim is made against, and its exit status is not the signal here</fails_when>
    <automated>npm run check:diff-disposition && npm run check:banned-claims && npm run check:public-docs</automated>
    <fails_when>non-zero exit, or the disposition check names any changed clause in the watched corpus with no row, or either content check names a violation in the rewritten prose</fails_when>
    <automated>npm run build && npm run typecheck && npm run check:build-parity && npm run freshness && npm run freshness:hook-manifest</automated>
    <fails_when>non-zero exit, or the parity step names any tracked `.js` file as moved, or either freshness step reports a mismatch</fails_when>
  </verify>
  <acceptance_criteria>
    - Both host readings are recorded verbatim in the summary: this repository's, and a probe tree carrying none of the watched names, each with the stream each line landed on.
    - The watched names are partitioned by derivation from the published set, with the union and the disjointness both asserted.
    - The disclosure is emitted only for the half whose skip could hide this repository's own evidence, and the omitted half is listed in the recipe by a value read from the partition.
    - The emitted line states that its counts are of directory entries rather than of hidden specs.
    - The stream decision is recorded with the contract text it was decided against; if the stream moved, every case asserting the stream contract is re-driven and the move is recorded as a decision.
    - Every changed clause in the recipe has a row in `docs/audit/29-style-dispositions/31-42.md`.
    - `31-CONTEXT.md` carries a `D-42` block with today's date, this round and this plan number, the derived swallow-site count compared against the review's four, the ban-set family cardinality, the two host readings, a `does not establish` section naming at least the three items in the action, and a reversibility rating.
    - `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and every traceability row are byte-unchanged by this plan.
  </acceptance_criteria>
  <done>The disclosure discloses what it is for, the counted thing is named in the line a reader meets, the recipe's claim is made against a real host reading, and `D-42` records the three closures with the numbers behind each.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target repository's parser → the ban decision | The checker resolves symbol identity through the target's own TypeScript. A position where that resolution throws is a position where the ban's answer is decided by something other than identity. |
| published completeness record → the positions it claims to cover | A record assembled by hand behind a derived-looking constant is an assurance, not a coverage claim. |
| recipe claim → checker behaviour | An agent reads the recipe and trusts it. A claim measured on a probe tree and presented as a host reading is a claim about the wrong subject. |
| could-not-run stream → gate reader | A gate reader is trained by contract to treat that stream as a loud skip. A benign line there is a false skip signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-42-01 | Tampering | A framework declaration the walk never reached, whose calls are then decided by the weaker rule | high | mitigate | Every position where the walk can stop is derived from the walk itself, given an arm that routes to a could-not-run exit or a published residual with a measured throw count, and each is driven by a stub checker. |
| T-31-42-02 | Spoofing | A published ban member the mechanism cannot reach while membership equality stays green | high | mitigate | A reachability row per member of every published ban set, with the expected side derived from the module's export family and the axis proven non-vacuous on more than one arm. |
| T-31-42-03 | Repudiation | A completeness record that describes attention rather than code | high | mitigate | The record's coverage is asserted over a derived site set in both directions, so a stale entry and an unrecorded site both turn it red. |
| T-31-42-04 | Denial of Service | A disclosure on the could-not-run stream read as a loud skip on nearly every host run | medium | mitigate | The watched names are partitioned by derivation, only the evidence-hiding half is disclosed, the counted thing is named in the line, and the stream decision is taken against the written contract. |
| T-31-42-05 | Information Disclosure | The disclosure naming directory entries of the host repository | low | accept | It names only the watched directory names and their counts, all of which the host's own operator already knows. |
| T-31-42-06 | Elevation of Privilege | The installed framework package's real declared surface exceeding the walk's bounds | medium | accept | Standing `UNKNOWN - verify` carried from `D-36`: the dependency set is fixed and the installed surface is not measured here. Re-stated in `D-42`'s `does not establish` section; a reached bound is now loud, which is the property that makes the residual survivable. |
| T-31-42-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and adds no dependency; `package.json` is not in `files_modified`, which is UATX-02's own floor. No legitimacy checkpoint is required because no install task exists. |
</threat_model>

<verification>
- The walk's swallow-site set derived from the walk, counted, compared against the review's hand-typed four, and every member given an arm or a measured published residual.
- Every derived swallow site driven by a stub checker, with pre-fix and post-fix readings recorded.
- A reachability row per member of every published ban set, expected side derived from the export family, with the shared-resolution claim checked and the seeded fail extended to a second axis.
- Two real host readings of the disclosure recorded, the watched names partitioned by derivation with union and disjointness asserted, and the stream decision taken against the written contract.
- Fixture typecheck, build parity, freshness, hook-manifest freshness, disposition, banned-claims and public-docs gates all green; the whole excluded-e2e suite green.
- `D-42` recorded with every number behind the three closures and a `does not establish` section.
</verification>

<success_criteria>
- No position where the framework-surface walk can stop is unrecorded, and none is defended only by the absence of a probe.
- No published ban member lacks a row that drives it to a refusal.
- The skipped-directory disclosure discloses a narrowed denominator rather than the presence of ordinary directories, and its recipe claim is made against a host reading.
- UATX-01 through UATX-06 remain unchecked; the phase remains In Progress.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-42-SUMMARY.md` when done, recording: the
derived swallow-site list with each site's decision and, where a fail-open was kept, its measured
throw count; the ban-set family cardinality, the rows added and the fixtures each needed; both host
readings of the disclosure with their streams; the partition's two halves; and every boundary this
plan leaves open with a reason and what would force it closed.
</output>

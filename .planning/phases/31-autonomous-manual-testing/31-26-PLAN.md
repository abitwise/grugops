---
phase: 31-autonomous-manual-testing
plan: 26
type: execute
wave: 6
depends_on: ["31-25"]
gap_closure: true
files_modified:
  - docs/audit/31-round5-residuals.md
  - .planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
  - .planning/phases/31-autonomous-manual-testing/deferred-items.md
  - .planning/STATE.md
autonomous: true
requirements: [UATX-01, UATX-02, UATX-03, UATX-04, UATX-05, UATX-06]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every reproduction `31-VERIFICATION.md` round 5 and `31-REVIEW.md`'s round-4 code review recorded is RE-RUN VERBATIM at ONE commit against the committed `.js`, using the exact probe spellings those documents used — not paraphrases, and not the fix plans' own fixtures. A closure round that re-measures its own tests measures the fix, not the finding."
    - "Each of the sixteen behavioral spot-checks `31-VERIFICATION.md` recorded is re-driven and its result stated beside the round-5 result, so the five that FAILED (rows 4, 7, 8, 10, 12) are shown moving and the eleven that PASSED (rows 1, 2, 3, 5, 6, 9, 11, 13, 14, 15, 16) are shown unmoved. A closure that reports only the moved rows cannot show it broke nothing."
    - "Every finding of `31-REVIEW.md` — CR-12, CR-13, CR-14, CR-15, CR-16, WR-22, WR-23, WR-24, WR-25, IN-12, IN-13 — carries an explicit disposition in a written ledger: closed with the measurement that proves it, decided-and-named as a residual with the reason, or carried with an owner and a criterion. No finding is left without a row, and no row asserts a closure this round did not measure."
    - "IN-13's two load-bearing `UNKNOWN - verify` items are each RESOLVED BY NAME or explicitly carried by name into `docs/audit/31-round5-residuals.md`: §4.3 (which spelling the round-4 verifier ran for WR-21) is answered by `31-23`'s three-spelling measurement, and §4.4 (the clause-name disagreement with `31-18-SUMMARY.md`'s recorded post-fix table) is answered by `31-22`'s pre/post per-dial clause table. Neither is absorbed silently, and `31-18-SUMMARY.md` is NOT rewritten — a prior round's record is history."
    - "The whole excluded-e2e suite is measured GREEN at ONE commit, together with every repository gate: the build, the typecheck, the build-parity check, the freshness check over every committed `.js`, the foundation guards, the UAT oracles and the fixtures type-check. A green suite is NOT the proof — this project's own doctrine, established across five rounds on this phase, is that the suite exercised NONE of the five defects found in round 5 — so it is recorded as a floor beside the reproductions, never in place of them."
    - "The frozen floors are re-measured rather than assumed: `hooks/guard.ts` hashes equal to `FROZEN_GUARD_BLOB`, and every `DECIDER_MANIFEST` entry equals the current `shasum -a 256` of the file it names. The manifest is checked as a WHOLE rather than only for the entries this round moved, because a stale entry elsewhere is the same class of drift."
    - "The residual register for this round is written down once, in a tracked document, listing every boundary the round's five fix plans decided to leave open — including `R-31-19-05`, the rewritten `T-31-18-01`, the destination-axis disposition, the module-scope suppression case, and the non-unwinding-fault residual — each with the reason and what would force it closed, so the next round's reviewer starts from a register rather than rediscovering them as findings."
    - "UATX-01 through UATX-06 stay UNCHECKED in `.planning/REQUIREMENTS.md` and the Phase 31 checkbox stays unchecked in `.planning/ROADMAP.md`, and the traceability table still reads `Gaps Found`. Only a verification round may flip a requirement; a gap-closure plan that flips one is asserting its own success."
    - "The four standing human-verification items — the attended Chrome lane under real interactive auth, the `claude auth status --json` predicate under API-key and long-lived-token configurations, the Windows leg of every browser probe and of the spec-integrity runnable, and the installer round-trip on a pre-existing host install — are carried forward unchanged with their `UNKNOWN - verify` markers intact. None is closed by this round and none is silently dropped."
    - "The measurement environment is proven clean: no probe artifact remains anywhere under `.temp`, no FIFO survives anywhere in the tree, the real `HOME` is restored, and the suite case that derives zero specs from this repository's own root is re-confirmed green in isolation after cleanup — the exact false alarm rounds 4 and 5 both had to reconcile."
    - "The STATE.md write is verified rather than assumed: this repository's state writer re-escapes backslashes on every write, and a pathological line length there has previously combined with a superlinear guard predicate to turn a sub-second gate into a multi-minute one. After the write, the longest line length is measured and the foundation guards are re-run with their wall-clock duration recorded."
  artifacts:
    - docs/audit/31-round5-residuals.md
    - .planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
    - .planning/phases/31-autonomous-manual-testing/deferred-items.md
    - .planning/STATE.md
  key_links:
    - "each round-5 reproduction <-> its post-round measurement: the pairing that makes a closure claim checkable by a later reader without re-deriving the probe."
    - "the sixteen behavioral spot-checks <-> their re-run results: the both-directions record that shows what moved AND what did not."
    - "every review finding <-> a ledger row: the binding that makes a finding without a disposition impossible to ship."
    - "IN-13's two `UNKNOWN - verify` items <-> the measurements 31-22 and 31-23 produced: the resolution that stops an open item being carried a second round by default."
    - "the round's five fix plans' residuals <-> one tracked register: the artifact that turns the next round's rediscovery into a lookup."
    - "the requirement rows <-> the verification round that may flip them: the discipline that keeps a gap-closure plan from certifying itself."
  prohibitions:
    - statement: "A gap-closure plan must never flip a requirement checkbox, a traceability row or the phase checkbox. Only a verification round may do that."
      status: unverified
      verification: flagged
    - statement: "Never fake a passing gate, a test result, or a citation. A reproduction that was not re-run is recorded as not re-run, with the reason, never as closed."
      status: unverified
      verification: flagged
    - statement: "A green test suite is not proof for a safety predicate. It is recorded as a floor beside the adversarial reproductions, never in place of them."
      status: unverified
      verification: flagged
    - statement: "`hooks/guard.ts` is not touched and `FROZEN_GUARD_BLOB` is never re-based; a frozen manifest entry is moved with its artifact, never relaxed."
      status: unverified
      verification: flagged
    - statement: "A human-verification item is carried forward with its `UNKNOWN - verify` marker intact; it is never closed by inference."
      status: unverified
      verification: flagged
    - statement: "A prior round's SUMMARY or PLAN is history and is never rewritten. A disagreement with it is annotated in this round's own record."
      status: unverified
      verification: flagged
    - statement: "Never merge a protected branch and never deploy to production without a named human confirmation."
      status: unverified
      verification: flagged
  referenced_prohibitions:
    - "31-20-PLAN.md: the closing-measurement role — re-run the findings' own probes, not the plans' fixtures; record a disposition for every finding; leave the requirement rows for the verifier. This plan is that role performed for round 5."
    - "31-21..31-25: each fix plan's own prohibitions, which this plan re-measures rather than re-asserts."
---

<objective>
The closing measurement of gap-closure round 5. Re-run every reproduction the round was convened to
close, re-run every control the round must not have broken, disposition every finding, resolve or carry
IN-13's two load-bearing `UNKNOWN - verify` items by name, write the round's residual register, and leave
the requirement rows for the verifier.

This plan writes no source. It measures.

## Finding coverage — every round-5 item lands in exactly one plan

| Finding | Severity | Closed by | What the closure is |
|---|---|---|---|
| CR-12 — unguarded `readFileSync` at the module's single note-write chokepoint hangs every writer on a FIFO | Critical | `31-21` | one non-blocking regular-file reader extracted from `readGovernanceConfigCandidate` and consumed by `writeNoteFile`, `ledgerRecordsId` and the config reader; a non-regular note path is a named refusal in bounded time |
| CR-13 — the home stop is asked before `dir` is inspected, so a repository rooted at `$HOME` never reads its own dial | Critical | `31-23` | the boundary is split into `isAboveHome` / `isHomeItself`; ascent is bounded, inspection is not; home answers only as a repository (marker AND configuration) and never becomes `nearest` |
| CR-14 — the declared-names census is file-scoped, so one dead declaration disables the whole rename/namespace/fixture ban family | Critical | `31-24` | the census carries ranges and the canonicaliser is asked with the reference position; one containment authority consumed by both canonicalisations |
| CR-15 — `ts.createSourceFile` sits outside the could-not-run boundary; 1,000 nested parens crash uncaught with empty stdout | Critical | `31-25` | one per-file boundary around everything done with a spec's bytes, one process boundary around `main`'s body returning 2, and the directory walk de-recursed |
| CR-16 — `originIsTrusted`'s second arm trusts any directory under the repository root | Critical | `31-22` | the second arm is DELETED; a trusted origin has one canonical shape and everything else is `origin-outside-trusted-store` |
| WR-22 — note write and ledger append are non-atomic, in the wrong order, with a fail-open ledger read | Warning | `31-21` | the ledger event is appended BEFORE the note write under `retained`; an unreadable ledger is the `unreadable-audit-ledger` decline; the workflow sentence is scoped to the dial it depends on |
| WR-23 — WR-20's false refusal survives when the shadowing binding is a function's second parameter | Warning | `31-24` | the fixture-binding exemption is narrowed to index 1 of a function passed as a call's second argument — the same scoping rule as CR-14's fix, same edit |
| WR-24 — `shadowed-rename.uat.spec.ts` can only fail for the false-positive half | Warning | `31-24` | the fixture gains a `MUTATE-REMOVE` region carrying a genuine module-scope renamed modifier call, so the union is asserted in both directions |
| WR-25 — the dial clause is evaluated before the operand clause, so an origin fault is reported as a dial problem | Warning | `31-22` | `origin-outside-trusted-store` moves above `human-stamp-not-gated-at-destination`; the clause ORDER becomes a derived axis with a transposed watched-fail mirror |
| IN-12 — `stripRoutingLinks` computed three times on one input | Info | `31-24` | computed once per arm into a local, with the per-function call-site count asserted by a parse |
| IN-13 — two load-bearing `UNKNOWN - verify` items in `docs/audit/31-round4-residuals.md` | Info | `31-26` (this plan) | §4.3 resolved by `31-23`'s three-spelling measurement; §4.4 resolved by `31-22`'s pre/post per-dial clause table; both recorded by name in `docs/audit/31-round5-residuals.md` |

Every one of the eleven has exactly one owning plan, and the five Criticals map one-to-one onto the two
blocked requirements: CR-12, CR-13 and CR-16 to UATX-01; CR-14 and CR-15 to UATX-06.

Purpose: produce the one artifact a sixth verification round can start from — a paired reproduction
ledger, a disposition for every finding, and a residual register — rather than a claim.

Output: `docs/audit/31-round5-residuals.md`; refreshed carry-forward records in `31-VALIDATION.md` and
`deferred-items.md`; an updated `.planning/STATE.md`; and requirement rows deliberately unmoved.

**Ordering note:** wave 6, `depends_on: ["31-25"]`. Every measurement in this plan must be taken at ONE
commit, after all five fix plans have landed. It modifies no source file and no test file, so it conflicts
with nothing except by requiring everything.

**Assumption delta:** `no-change`. Both detector hits are already decided in `31-CONTEXT.md` (D-14,
D-21); no identity-model question is open, and this plan records no new decision — a measurement plan
that recorded a decision would be deciding something it did not build.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-autonomous-manual-testing/31-CONTEXT.md
@.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md
@.planning/phases/31-autonomous-manual-testing/31-REVIEW.md
@.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md
@.planning/phases/31-autonomous-manual-testing/deferred-items.md
@docs/audit/31-round4-residuals.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Where |
|---|---|---|
| `docs/audit/31-round5-residuals.md` | NEW tracked document: the paired reproduction ledger, the finding-disposition table, the round's residual register, the IN-13 resolutions, and the baseline-health record | `docs/audit/31-round5-residuals.md` |
| the refreshed validation record | CHANGED: the four human-verification items carried forward with their markers intact and their carry-forward counts updated | `.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md` |
| the refreshed deferred record | CHANGED: nothing deferred past this round, stated explicitly, with any genuinely deferred item carrying an owner and a criterion | `.planning/phases/31-autonomous-manual-testing/deferred-items.md` |
| the updated project state | CHANGED: round-5 gap closure executed, the next action named as a sixth verification round | `.planning/STATE.md` |

## Edge probe assumptions

| # | Requirement | Category | Probe | Standing assumption for this plan |
|---|---|---|---|---|
| 1 | UATX-01 | unclassified | review manually | Assumed: a reproduction re-run against a fix plan's own fixture is not a re-run of the finding. Every row of the ledger is driven from the probe spelling the FINDING document used, and the summary states which document each spelling came from. |
| 2 | UATX-06 | unclassified | review manually | Assumed: a control that passed in round 5 and is not re-driven here is `UNKNOWN - verify`, not "still passing". Every one of the sixteen rows is driven or explicitly marked not-driven with a reason. |
| 3 | UATX-05 | empty | "what is the result for empty, single-element, or null input?" | Assumed: the zero-spec derivation from this repository's own root is the case a leftover probe artifact silently breaks, so it is re-confirmed in isolation AFTER cleanup rather than only as part of the suite. |
| 4 | UATX-02 | unclassified | review manually | Assumed: a requirement recorded SATISFIED in round 5 and untouched by this round's plans is re-measured by its own guard rather than inherited — `guardPlaywrightMcpPin` is re-run, not assumed. |
| 5 | UATX-03 | unclassified | review manually | Assumed: the attended Chrome lane's structural bar is independent of every change this round made, and that independence is re-measured rather than assumed. |
| 6 | UATX-04 | unclassified | review manually | Assumed: the provenance routes are untouched by all five fix plans, which is checked by a diff over the round rather than asserted. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a fix plan's own test -> the claim that a finding is closed | a test written by the fixer crosses here; only the FINDING's own probe spelling can measure the finding |
| a green suite -> the conclusion that a safety predicate holds | this project has five consecutive rounds of measured evidence that this crossing is invalid, and the record must say so |
| a probe artifact left under `.temp/` -> the next run's derived spec set | a leftover `uat/` spec crosses here and turns an unrelated case red or green for the wrong reason |
| this plan's own record -> the sixth verification round's starting point | an unmeasured row crossing here as "closed" is the fabrication the whole trace exists to prevent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-26-01 | Repudiation | a closure claimed from a fix plan's own fixture rather than the finding's probe | high | mitigate | Every ledger row records the probe spelling and the document it came from; a row without a re-run is marked not-driven with a reason, never closed. |
| T-31-26-02 | Repudiation | a finding shipped without a disposition | high | mitigate | The disposition table has one row per finding, and the row count is asserted equal to the review's own `findings:` totals (5 critical, 4 warning, 2 info). |
| T-31-26-03 | Spoofing | a green suite standing in for adversarial proof | high | mitigate | The suite is recorded as a floor beside the reproductions, with the explicit statement of how many of the round's defects it exercised. |
| T-31-26-04 | Tampering | a frozen manifest entry stale elsewhere in the file | critical | mitigate | Every `DECIDER_MANIFEST` entry is compared with the current digest of the file it names, not only the entries this round moved. |
| T-31-26-05 | Tampering | a requirement or phase checkbox flipped by a gap-closure plan | high | mitigate | The requirement rows, the traceability table and the ROADMAP phase checkbox are asserted UNMOVED by a diff, and the summary states the assertion. |
| T-31-26-06 | Denial of Service | a probe artifact or a FIFO surviving the round | high | mitigate | The tree is swept for FIFOs and for `.temp/` probe roots, the real `HOME` is confirmed, and the zero-spec derivation case is re-confirmed in isolation after cleanup. |
| T-31-26-07 | Denial of Service | a pathological STATE.md line combining with a superlinear guard predicate | medium | mitigate | After the state write, the longest line length is measured and the foundation guards are re-run with their wall-clock duration recorded. |
| T-31-26-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds no package-manager install task and no dependency; every command runs against the committed lockfile and already-installed dev dependencies, so the package-legitimacy protocol has no `[ASSUMED]`/`[SUS]` row to gate. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Re-run every round-5 reproduction and every control verbatim, and pair each result with the one it replaces</name>
  <files>docs/audit/31-round5-residuals.md</files>
  <precondition>Every measurement in this task must be taken at ONE commit, with all five fix plans landed and the working tree clean apart from this plan's own document. Probe roots go under `.temp/` inside this repository so `typescript` and `node_modules` resolve; each is deleted immediately after its measurement and the tree is confirmed clean with `git status --short` between families. Any probe overriding `HOME` must restore it, and any probe planting a FIFO must remove it before the next measurement — a surviving FIFO wedges the next writer with no diagnostic.</precondition>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — the "Behavioral Spot-Checks" table in FULL, all sixteen rows with their commands and results; the `gaps:` block's two entries with every measured detail; the `regressions:` list; the "Key Link Verification" table; the "Required Artifacts" table; and the "Anti-Patterns Found" table. These are the probe spellings this task re-drives — not the fix plans' fixtures.
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — every finding's "Reproduced on this tree" block: CR-12's three-step `mkfifo` transcript and its ledger-FIFO variant, CR-13's tree/HOME/dial transcript with its CONTROL, CR-14's four measured spellings, CR-15's depth table, CR-16's four-step transcript, WR-23's helper spec and its measured output, and WR-25's measured clause order.
    - `31-21-SUMMARY.md` through `31-25-SUMMARY.md` — each plan's recorded pre-fix and post-fix measurements, so this task can state where its own re-run AGREES or DISAGREES with the fixer's record rather than inheriting it.
    - `docs/audit/31-round4-residuals.md` — §5 (the `.temp/` contamination hazard), §4.3 and §4.4 (IN-13's two items), and §6 (the baseline-health idiom this document's own §-structure follows).
  </read_first>
  <action>
    MOVEMENT 1 — build the paired ledger's skeleton. Create `docs/audit/31-round5-residuals.md` with a
    section per family: the UATX-01 family (CR-12, CR-13, CR-16, WR-22, WR-25) and the UATX-06 family
    (CR-14, CR-15, WR-23, WR-24, IN-12). Each section carries a table with one row per reproduction and
    columns for: the probe spelling, the document it came from, the round-5 result, this round's result,
    and a verdict.

    MOVEMENT 2 — re-drive the five FAILING rows using the verifier's own spellings. Row 4 (the
    in-repository copy-in promoting into a fresh destination id), row 7 (the FIFO at a note path under
    `timeout 10`), row 8 (`trustedRepoRoot()` with `HOME` at a planted repository root carrying `.git` and
    `human_admission: high-severity`), row 10 (the renamed import with a dead inner `const it = 1;`), and
    row 12 (the 1,000-nested-paren spec). Record the exact command, the exit code, the stdout and stderr
    content or byte counts, and the on-disk effect for each. Then re-drive the review's OWN variants that
    the verifier did not itself run: CR-12's ledger-FIFO probe, CR-14's namespace and index-0-helper
    spellings, and WR-23's helper spec.

    MOVEMENT 3 — re-drive the eleven PASSING rows so the round can show it broke nothing. Rows 1, 2 and 3
    (the round-4 modifier closures), row 5 (the occupied-destination decline), row 6 (CR-08's legitimate
    promotion), row 9 (the below-home control, `R-31-19-01`), row 11 (the declaration-free file still
    refused), row 13 (the full excluded-e2e suite), row 14 (freshness), row 15 (the two guards) and row 16
    (the frozen deploy-guard blob). Any row not driven is marked not-driven with a reason; none is marked
    passing by inheritance.

    MOVEMENT 4 — re-measure the requirements that round 5 recorded SATISFIED, by their own mechanisms
    rather than by inference. UATX-02: run the foundation guards and name `guardPlaywrightMcpPin`'s result.
    UATX-03: run the Chrome-lane structural bar's own suite file and name the result. UATX-04: take a diff
    over the whole round and show that the provenance-carrying routes are untouched, naming the files and
    the line counts. UATX-05: name the two loud-skip markers' cases and their results, and state explicitly
    where CR-15's parser-boundary work sits relative to this requirement.

    MOVEMENT 5 — state agreement and disagreement with the fixers' records. For every row, compare this
    task's measurement with the corresponding `31-2N-SUMMARY.md` record and state AGREES or DISAGREES. A
    disagreement is printed with both figures and neither is chosen without a stated reason — the discipline
    `docs/audit/31-round4-residuals.md` §4.4 established. Then sweep the tree: confirm no FIFO survives
    anywhere, no `.temp/` probe root remains, the real `HOME` is in place, and re-confirm the zero-spec
    derivation case in ISOLATION after cleanup.
  </action>
  <acceptance_criteria>
    - `docs/audit/31-round5-residuals.md` exists and is git-tracked, with a section per requirement family.
    - All sixteen behavioral spot-check rows appear in the ledger with the round-5 result and this round's result side by side; the number of rows is stated and equals sixteen.
    - The five previously-FAILING rows each show a moved result, with the exact command and the measured output quoted.
    - The eleven previously-PASSING rows each show an unmoved result, or are marked not-driven with a stated reason; the count of each category is stated.
    - The review's own extra variants (CR-12's ledger FIFO, CR-14's namespace and index-0-helper spellings, WR-23's helper spec) are each driven and recorded.
    - UATX-02, UATX-03, UATX-04 and UATX-05 are each re-measured by their own mechanism, with the guard name, the suite file or the diff quoted.
    - Every row carries an AGREES/DISAGREES verdict against the corresponding fix plan's summary; every DISAGREES row prints both figures.
    - The cleanliness sweep is recorded: the FIFO search command and its empty result, the `.temp/` listing, the `HOME` value, and the isolated zero-spec derivation case's result.
    - No row in the ledger claims a closure without a quoted measurement taken in this session.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, the summary reporting fewer than 62 test files, or any test file reporting `0 passed`</fails_when>
    <automated>find . -path ./node_modules -prune -o -type p -print</automated>
    <fails_when>the command prints any path — a surviving FIFO anywhere in the tree wedges the next writer with no diagnostic</fails_when>
    <automated>git status --short</automated>
    <fails_when>the output lists any probe root under `.temp/`, any `uat/*.uat.spec.ts` outside the fixtures directory, or any probe `.mjs` at the repository root</fails_when>
  </verify>
  <done>Every round-5 reproduction and every control is re-driven at one commit using the finding documents' own probe spellings, paired with the result it replaces, and each is marked AGREES or DISAGREES against the fixer's own record.</done>
  <reversibility rating="reversible">The ledger is a record; it constrains nothing except by what it documents.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Measure the whole repository green at one commit, and re-measure the frozen floors rather than assuming them</name>
  <files>docs/audit/31-round5-residuals.md</files>
  <read_first>
    - `docs/audit/31-round4-residuals.md` — §6 in full, the baseline-health section: what it measured, how it recorded the frozen blob and the whole `DECIDER_MANIFEST`, and §6.2's account of a verification harness producing a false result about its own premise, which is the reason every count below is derived rather than quoted.
    - `.planning/phases/31-autonomous-manual-testing/31-VERIFICATION.md` — behavioral spot-check rows 13, 14, 15 and 16 with their exact expected outputs, so this task's baseline is comparable to the verifier's.
    - `hooks/hook-entry.ts` — the `DECIDER_MANIFEST` in full, so every entry is compared rather than only the ones this round moved.
    - `scripts/floor-invariance.test.ts` — `FROZEN_GUARD_BLOB` and the case that asserts it.
    - `package.json` — the script names for `build`, `typecheck`, `check:build-parity` and `freshness`, so the commands are the repository's own rather than re-derived.
  </read_first>
  <action>
    MOVEMENT 1 — run every repository gate at one commit and record the exact output line of each: the
    build, the typecheck, the build-parity check, the freshness check, the foundation guards, the UAT
    oracles, the fixtures type-check, and the full excluded-e2e suite. Record the suite's file count, test
    count and skip count as numbers, and compare them with the round-5 figures (62 files, 3702 passed, 2
    skipped) — a change in any of the three is stated with its reason.

    MOVEMENT 2 — state the doctrine beside the green, not instead of it. Record explicitly how many of the
    round's five Critical defects the green suite exercised BEFORE this round's fixes: the answer is zero,
    for the fifth consecutive round, and the record says so in one sentence so a later reader cannot mistake
    the suite for the proof.

    MOVEMENT 3 — re-measure the frozen floors as a WHOLE. Hash `hooks/guard.ts` and compare with
    `FROZEN_GUARD_BLOB`. Then, for EVERY entry in `DECIDER_MANIFEST`, compute the current `shasum -a 256`
    of the named file and compare; print the full table with one row per entry and a match/mismatch column.
    State the entry count as a number, and state which entries this round moved and which did not.

    MOVEMENT 4 — verify `package.json` is byte-unchanged across the whole round, and that no new committed
    `.js` was added: take a diff from the round's base commit and record both facts with the commands that
    produced them.

    MOVEMENT 5 — record the measurement environment. State the commit the measurements were taken at, the
    platform, the Node version, and confirm the working tree carried no uncommitted source change during
    the run.
  </action>
  <acceptance_criteria>
    - Every repository gate's exact output line is quoted, with the command that produced it.
    - The suite's file/test/skip counts are stated as numbers and compared with round 5's; any change carries a reason.
    - The freshness line is quoted verbatim and the committed-`.js` count is stated; a count other than 60 carries a stated reason.
    - The sentence recording how many of the round's five Criticals the green suite exercised is present and states zero for the fifth consecutive round.
    - The FULL `DECIDER_MANIFEST` table is printed with one row per entry and a match/mismatch column; the entry count is stated as a number and every row matches.
    - `git hash-object hooks/guard.ts` is quoted and equals `FROZEN_GUARD_BLOB`, whose value is also quoted.
    - `git diff --stat {round-base}..HEAD -- package.json` is quoted and empty.
    - The commit, platform and Node version of the measurement session are recorded.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>any of the four exits non-zero, or the freshness line reports anything other than `All build outputs fresh: 60 committed .js file(s) match a rebuild of their sources.`</fails_when>
    <automated>node scripts/check-foundation-guards.js &amp;&amp; node scripts/check-uat-oracles.js</automated>
    <fails_when>either command exits non-zero or prints anything other than `ALL CHECKS PASSED`</fails_when>
    <automated>npx tsc -p tsconfig.fixtures.json --noEmit &amp;&amp; npx tsc -p tsconfig.tests.json --noEmit</automated>
    <fails_when>either exits non-zero, or any diagnostic names a file under `scripts/runnable-ref/fixtures/`</fails_when>
    <automated>git diff --stat "$(git rev-list -n1 --before=2026-09-09T19:00:00Z HEAD)"..HEAD -- package.json</automated>
    <fails_when>the command prints any changed-line count for `package.json`</fails_when>
  </verify>
  <done>Every repository gate is green at one recorded commit, the whole frozen manifest is re-measured entry by entry, `package.json` is proven byte-unchanged, and the green is recorded as a floor with the doctrine sentence beside it.</done>
  <reversibility rating="reversible">A measurement record; it constrains nothing except by what it documents.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Disposition every finding, resolve IN-13 by name, write the round's residual register, and leave the requirement rows for the verifier</name>
  <files>docs/audit/31-round5-residuals.md, .planning/phases/31-autonomous-manual-testing/31-VALIDATION.md, .planning/phases/31-autonomous-manual-testing/deferred-items.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/31-autonomous-manual-testing/31-REVIEW.md` — the `findings:` frontmatter block with its totals (5 critical, 4 warning, 2 info, 11 total), the "Prior findings status" table's idiom, and IN-13 in full including its naming of §4.3, §4.4 and §6.2 and its own `UNKNOWN - verify` about `WORKFLOW_STOP_BULLET_COUNT` and `NON_TEST_MODULE_COUNT`.
    - `docs/audit/31-round4-residuals.md` — §4.3 and §4.4 in full (the two items IN-13 names), the residual-register tables at the end, and the closing summary's category counts, so this round's document is comparable row for row.
    - `31-22-SUMMARY.md` and `31-23-SUMMARY.md` — the per-dial clause table and the three-spelling home measurement respectively; these are the measurements that RESOLVE §4.4 and §4.3.
    - `31-21-SUMMARY.md`, `31-24-SUMMARY.md`, `31-25-SUMMARY.md` — each plan's recorded residuals and self-red-team probe registers, which this document aggregates.
    - `.planning/phases/31-autonomous-manual-testing/31-VALIDATION.md` — the four human-verification items and their carry-forward counts.
    - `.planning/phases/31-autonomous-manual-testing/deferred-items.md` — the current record, and the standing posture that nothing is deferred past a gap-closure round.
    - `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` — the requirement rows, the traceability table and the Phase 31 checkbox, so this task can assert them UNMOVED rather than edit them.
    - `.planning/STATE.md` — the current `status:` block's shape, and the note that this repository's state writer re-escapes backslashes on every write.
  </read_first>
  <action>
    MOVEMENT 1 — disposition every finding. Add a table to `docs/audit/31-round5-residuals.md` with one row
    per finding — CR-12, CR-13, CR-14, CR-15, CR-16, WR-22, WR-23, WR-24, WR-25, IN-12, IN-13 — carrying:
    the finding, the owning plan, the disposition (closed / decided-and-named / carried), and the
    MEASUREMENT that proves it, cited to a section of this same document. A row whose disposition is
    `closed` MUST cite a measurement taken in Task 1; a row with no measurement is `UNKNOWN - verify` and
    says so. Assert the row count equals the review's own totals and state both numbers.

    MOVEMENT 2 — resolve IN-13's two items BY NAME. Write a section that takes §4.3 of the round-4 document
    — which spelling the round-4 verifier ran for WR-21 — and answers it from `31-23`'s three-spelling
    measurement plus `31-VERIFICATION.md` rows 8 and 9, stating which reading is now settled and what
    remains as `R-31-19-01`. Then take §4.4 — the clause-name disagreement with `31-18-SUMMARY.md`'s
    recorded post-fix table — and answer it from `31-22`'s pre-fix and post-fix per-dial clause tables,
    stating the measured pre-fix order (which vindicates the round-4 re-measurement), the post-fix order,
    and the fact that `31-18-SUMMARY.md` is left byte-unchanged because a prior round's record is history.
    If either item cannot be resolved, carry it forward BY NAME with its `UNKNOWN - verify` marker intact
    and a stated criterion — never absorbed.

    MOVEMENT 3 — write the round's residual register. Aggregate, into one table, every boundary the round's
    five fix plans decided to leave open: `R-31-19-05`; the rewritten `T-31-18-01`; the destination-axis
    disposition from `31-22`; the module-scope suppression case and the non-shipped binder from `31-24`;
    the non-unwinding-fault and Windows `UNKNOWN - verify` from `31-25`; the symlink/lexical-resolve answers
    from `31-22`'s and `31-21`'s probes; and any residual any self-red-team probe added. Each row carries
    the shape, the reason, the owning decision, and what would force it closed. Also aggregate the five
    plans' self-red-team probe registers into one index so a sixth round can see what was already probed.

    MOVEMENT 4 — refresh the carry-forward records. Update `31-VALIDATION.md` so the four human-verification
    items are carried with their `UNKNOWN - verify` markers intact and their carry-forward counts
    incremented to include this round. Update `deferred-items.md` to state explicitly that nothing was
    deferred past this round, or to name any genuinely deferred item with an owner and a criterion. Neither
    file may close an item by inference.

    MOVEMENT 5 — leave the requirement rows, and update the state carefully. Assert by diff that
    `.planning/REQUIREMENTS.md`'s six UATX rows are still unchecked, that its traceability table still reads
    `Gaps Found`, and that `.planning/ROADMAP.md`'s Phase 31 checkbox is still unchecked; record the
    commands and their empty diffs. Then update `.planning/STATE.md`'s `status:` and `stopped_at:` to record
    that round-5 gap closure executed across `31-21`..`31-26` and that the next action is a SIXTH
    verification round. After the write, measure the longest line in `.planning/STATE.md` and record it as a
    number, and re-run the foundation guards recording their wall-clock duration — this repository's state
    writer re-escapes backslashes on every write, and a pathological line there has previously turned a
    sub-second guard into a multi-minute one.
  </action>
  <acceptance_criteria>
    - The disposition table has exactly eleven rows and the count is asserted equal to `31-REVIEW.md`'s `findings.total`; both numbers are stated.
    - Every `closed` row cites a measurement section of this same document; no row asserts a closure without one.
    - IN-13's §4.3 item is answered with the three-spelling measurement and states which reading is settled and what remains as `R-31-19-01`.
    - IN-13's §4.4 item is answered with the pre-fix and post-fix per-dial clause tables, and the document states that `31-18-SUMMARY.md` is byte-unchanged; the diff proving that is recorded.
    - The residual register table has one row per open boundary with all four fields; its row count is stated as a number.
    - The aggregated self-red-team probe index lists every probe from all five fix plans with its verdict; the probe count is stated as a number.
    - `31-VALIDATION.md`'s four human-verification items each carry an intact `UNKNOWN - verify` marker and an incremented carry-forward count.
    - `deferred-items.md` states the deferral posture explicitly; any deferred item has an owner and a criterion.
    - `git diff {round-base}..HEAD -- .planning/REQUIREMENTS.md` and the same pinned range over the ROADMAP phase-checkbox line are both recorded and show no change to any requirement checkbox, traceability row, or phase checkbox.
    - `.planning/STATE.md` names the next action as a sixth verification round; the longest line length is recorded as a number; the foundation guards' wall-clock duration after the write is recorded as a number of seconds and is under 5 s.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-foundation-guards.js &amp;&amp; node scripts/check-uat-oracles.js</automated>
    <fails_when>either command exits non-zero, prints anything other than `ALL CHECKS PASSED`, or takes longer than 30 seconds of wall-clock time</fails_when>
    <automated>awk '{ if (length($0) > m) m = length($0) } END { print m }' .planning/STATE.md</automated>
    <fails_when>the printed number exceeds 20000, which is the pathological-line shape that has previously combined with a superlinear guard predicate on this repository</fails_when>
    <automated>git diff --stat "$(git rev-list -n1 --before=2026-09-09T19:00:00Z HEAD)"..HEAD -- .planning/REQUIREMENTS.md</automated>
    <fails_when>the command prints any changed-line count for `.planning/REQUIREMENTS.md`</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <fails_when>non-zero exit, the summary reporting fewer than 62 test files, or any test file reporting `0 passed`</fails_when>
  </verify>
  <done>Every one of the eleven findings has a disposition backed by a measurement, IN-13's two items are each resolved or carried by name, the round's residual register and probe index are written into one tracked document, the carry-forward records are refreshed, and the requirement rows are proven unmoved.</done>
  <reversibility rating="reversible">Records and state; nothing about them constrains a later change except the register they leave for the sixth round.</reversibility>
</task>

</tasks>

<verification>
- All sixteen behavioral spot-checks re-driven at one commit with round-5 and round-6 results side by side.
- All eleven review findings dispositioned, every `closed` row citing a measurement taken in this session.
- IN-13's §4.3 and §4.4 items each resolved by name from `31-23`'s and `31-22`'s measurements, or carried by name with their markers intact.
- `docs/audit/31-round5-residuals.md` exists, is tracked, and carries the paired ledger, the disposition table, the residual register, the probe index and the baseline-health record.
- Every repository gate green at one commit; the whole `DECIDER_MANIFEST` re-measured entry by entry; `hooks/guard.ts` byte-frozen; `package.json` byte-unchanged.
- No FIFO and no `.temp/` probe root survives; the real `HOME` is restored; the zero-spec derivation case is re-confirmed in isolation.
- `.planning/REQUIREMENTS.md`, its traceability table, and the ROADMAP Phase 31 checkbox are all UNMOVED.
- `.planning/STATE.md` names a sixth verification round as the next action, with its longest-line length and the post-write guard duration recorded.
</verification>

<success_criteria>
- A closing record a sixth verification round can start from: paired reproductions, dispositioned findings, a residual register and an aggregated probe index.
- Zero findings without a disposition; zero dispositions without a measurement.
- The green suite recorded as a floor with the fifth-consecutive-round doctrine sentence beside it, never in place of the reproductions.
- No requirement, traceability row or phase checkbox moved by this round.
</success_criteria>

<output>
Create `.planning/phases/31-autonomous-manual-testing/31-26-SUMMARY.md` when done.
</output>

---
phase: 16-14-gate-convergence-lint-ui-e2e-test-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/runnable-ref/test-skip-integrity.ts
  - scripts/runnable-ref/test-skip-integrity.js
  - scripts/runnable-ref/test-skip-integrity.test.ts
  - scripts/runnable-ref/fixtures/clean-test-skips.md
  - scripts/runnable-ref/fixtures/hollow-test-skips.md
  - scripts/runnable-ref/fixtures/expired-test-skips.md
  - scripts/runnable-ref/fixtures/quarantine-test-skips.md
  - install/install.ts
  - install/install.js
autonomous: true
requirements: [TINT-01, TINT-02, TINT-03]
must_haves:
  truths:
    - "A committed test-integrity checker (.ts → committed .js) parses the .grugops/test-skips.md registry, validates each row's format, and compares a passed-in skip count against the count of valid justifications (D-PL1/D-PL2/D-05)."
    - "A registry carrying a hollow justification (placeholder owner) makes the checker exit 1 — the SC3 keystone — proven by a Vitest harness spawning the committed .js (TINT-01)."
    - "An expired entry makes the checker exit 1 even when skip counts balance; a valid+unexpired flaky-quarantine entry counts as justified and exits 0 (D-05, the non-blocking lane, TINT-02)."
    - "host-skips > valid-justification count makes the checker exit 1 via --skip-count <N> (D-05/D-14, TINT-02)."
    - "A missing/unreadable registry makes the checker exit 2 (error, never a false pass); the checker runs in a bare temp dir with no node_modules (host-CI emulation)."
    - "install.ts materializes the new checker into the host's tools/grugops/ via one appended RUNNABLES tuple; the committed install.js and test-skip-integrity.js match a fresh tsc rebuild (freshness gate)."
  artifacts:
    - path: "scripts/runnable-ref/test-skip-integrity.ts"
      provides: "The test-integrity checker (near-clone of reference-check.ts), node: builtins only, exit 0/1/2 contract"
      contains: "readFileSync"
    - path: "scripts/runnable-ref/test-skip-integrity.js"
      provides: "Committed compiled output (freshness-policed)"
    - path: "scripts/runnable-ref/test-skip-integrity.test.ts"
      provides: "Vitest RED-fixture harness proving SC3 + the D-05 edge cases + host-emulation"
    - path: "scripts/runnable-ref/fixtures/hollow-test-skips.md"
      provides: "RED fixture — placeholder owner = canonical hollow justification (SC3 keystone)"
    - path: "scripts/runnable-ref/fixtures/clean-test-skips.md"
      provides: "GREEN fixture — well-formed rows incl. a valid flaky-quarantine row"
    - path: "install/install.ts"
      provides: "Second RUNNABLES tuple materializing the checker into tools/grugops/"
      contains: "test-skip-integrity.js"
  key_links:
    - from: "scripts/runnable-ref/test-skip-integrity.test.ts"
      to: "scripts/runnable-ref/test-skip-integrity.js"
      via: "spawnSync(node, [CHECK_JS, ...args])"
      pattern: "spawnSync\\(.*test-skip-integrity\\.js"
    - from: "install/install.ts"
      to: "tools/grugops/test-skip-integrity.js"
      via: "RUNNABLES tuple → materializeRunnable()"
      pattern: "test-skip-integrity\\.js.*tools/grugops"
---

<objective>
Build the un-cheatable test-integrity checker — the second kit-shipped runnable — as a near-clone of `scripts/runnable-ref/reference-check.ts`, with its RED-fixture Vitest harness (the SC3 keystone), GREEN/edge-case fixtures, and the one-tuple installer materialization. The checker validates grugops's OWN justification registry format (stack-agnostic, D-PL2) and compares a passed-in host skip count against the count of valid justifications (D-05). It satisfies TINT-01 (hollow justification → exit 1), TINT-02 (expired/over-count → exit 1; valid quarantine → exit 0), and the mechanical half of TINT-03 (the committed checker is the enforcement form, never an agent-applied prose rule).

Purpose: This is the structural, un-cheatable enforcement form locked by D-PL1 — a mechanical check, not agent judgment. The agent physically cannot clear its own gate by writing a justification because test-integrity is human-only (D-08) and the registry is human-owned (D-02); the checker only validates format + expiry + count.
Output: `test-skip-integrity.{ts,js,test.ts}`, four fixtures under `scripts/runnable-ref/fixtures/`, and one appended `RUNNABLES` tuple in `install.ts` (rebuilt `install.js` committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-CONTEXT.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-RESEARCH.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-PATTERNS.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test-integrity checker + GREEN/RED/edge fixtures</name>
  <files>scripts/runnable-ref/test-skip-integrity.ts, scripts/runnable-ref/fixtures/clean-test-skips.md, scripts/runnable-ref/fixtures/hollow-test-skips.md, scripts/runnable-ref/fixtures/expired-test-skips.md, scripts/runnable-ref/fixtures/quarantine-test-skips.md</files>
  <read_first>
    - scripts/runnable-ref/reference-check.ts (whole file, 82 lines) — THE contract to mirror: docblock lines 17-27 (the D-12 exit 0/1/2 + clear-voice + --json contract), imports line 28 (node:fs only), arg-parse lines 34-36, fail-closed input read lines 43-56, result emission lines 66-81. Do NOT modify this file (Pitfall 1 — editing the proven reference breaks the Phase-15 TOOL-02 proof).
    - scripts/runnable-ref/fixtures/clean.txt + scripts/runnable-ref/fixtures/bad.txt — the GREEN/RED fixture-pair role to translate into the registry table format.
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-PATTERNS.md (lines 33-87 — the exact line-by-line clone guidance for the checker; lines 126-134 — the fixture format/hollow definition).
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-RESEARCH.md (Pitfall 2 lines 358-362 — the two-count semantics; Pitfall 3 lines 364-368 — the concrete "hollow" definition; Pitfall 4 lines 370-374 — the today-determinism rule).
  </read_first>
  <behavior>
    - Hollow justification (placeholder owner: blank / `-` / `TODO` / `TBD` / `me` / `agent`) → checker exits 1 with a clear-voice finding naming the offending row (SC3 keystone, TINT-01).
    - Well-formed registry, host-skips ≤ valid-justification count, all expiries in the future → exit 0 + "No findings." (TINT-01).
    - Any expired entry (Expiry < today) → exit 1, even when skip counts balance (D-05, TINT-02).
    - A valid+unexpired `flaky-quarantine` row counts as a justification → does NOT block (exit 0 in the GREEN fixture; the non-blocking lane, D-04/TINT-02).
    - `--skip-count <N>` where N > valid-justification count → exit 1 (D-05, TINT-02).
    - Off-list category, empty Test ID, or empty Ticket/REQ → row is invalid (does not count as a justification).
    - Missing/unreadable registry path, or no path arg → exit 2 (error, never a silent pass).
  </behavior>
  <action>
Create `scripts/runnable-ref/test-skip-integrity.ts` as a near-clone of `reference-check.ts`. Keep the file `node:fs`-only (no markdown library — parse with `String.split("\n")` then `split("|")` column walk per D-03). Restate the D-12 contract docblock (exit `0` pass / `1` findings-fail / `2` error; stdout = clear professional voice; optional `--json {ok, findings}` block) and the VOICE DISCIPLINE line. Do NOT write the literal "§14" — refer to "the quality gate" / "the test-integrity step".

Arg parse (mirror reference-check.ts lines 34-36): first non-flag argv = the registry path (`.grugops/test-skips.md`); `--json` toggles the machine block; add `--skip-count <N>` (parse the integer that follows, or a flag in the form `--skip-count=N`). If `--skip-count` is absent or its value is not a non-negative integer, treat the count as unknown and emit a clear-voice finding `Skip count was not provided (UNKNOWN - verify); cannot confirm the suite is fully justified.` → exit 1 (no-fabrication per D-14 — never coerce a silent 0 to a pass). Optionally accept a test-only `--today YYYY-MM-DD` (document it as a testing affordance per Pitfall 4); default to the real wall-clock date for the gate path.

Fail-closed input read (copy reference-check.ts lines 43-56 verbatim, swapping the message filename): no path → exit 2; unreadable file → exit 2.

Replace the single `BAD_TOKEN` rule with the registry validation:
1. Parse the markdown table — the columns are exactly `Test ID | Reason | Owner | Ticket/REQ | Expiry (YYYY-MM-DD) | Category` (D-03). Skip the header row and the `|---|` separator row; trim each cell.
2. For each data row, mark it INVALID (hollow) if ANY of: Owner blank or in the placeholder set `{-, todo, tbd, me, agent}` (case-insensitive); Category not in the closed list `{flaky-quarantine, external-dependency, wip-behind-flag, platform-specific, deprecated-pending-removal}` (D-04); Test ID empty; Ticket/REQ empty; Expiry unparseable. Each invalid row pushes a clear-voice finding naming the row's Test ID and the specific reason.
3. Mark a row EXPIRED if its Expiry parses but is strictly before `today` — push a finding; an expired entry blocks regardless of counts (D-05).
4. Count VALID justifications = rows that are neither invalid nor expired (a valid+unexpired `flaky-quarantine` row counts — the non-blocking lane, D-04).
5. Compare: if the provided skip count `N > valid-justification count`, push a finding `Unjustified skips: the runner reported N skipped tests but only M are justified.` (D-05).

Emit per the D-12 contract (copy reference-check.ts lines 66-81): findings present → `--json {ok:false, findings}` or one finding per line → exit 1; none → `{ok:true, findings:[]}` / "No findings." → exit 0.

Create the four fixtures (translate `clean.txt`/`bad.txt` into the D-03 table; use far-past `2000-01-01` / far-future `2099-12-31` expiries so the `today` comparison is never boundary-flaky — Pitfall 4):
- `clean-test-skips.md` — GREEN: ~2-3 well-formed rows with far-future expiries, named real owners, valid Ticket/REQ, at least one `flaky-quarantine` row (proves the non-blocking lane). Designed so host-skips ≤ valid justifications passes.
- `hollow-test-skips.md` — RED (the SC3 keystone): one row identical to a GREEN row except Owner is a placeholder (`-` or `agent`), all else well-formed → trips the validator.
- `expired-test-skips.md` — one otherwise-valid row with Expiry `2000-01-01` (far past) → blocks on expiry even when counts balance.
- `quarantine-test-skips.md` — one valid+unexpired `flaky-quarantine` row, far-future expiry → counts as justified, exits 0.
  </action>
  <verify>
    <automated>npm run typecheck; test -f scripts/runnable-ref/fixtures/hollow-test-skips.md && echo FIXTURES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/runnable-ref/test-skip-integrity.ts` exists, imports `node:fs` only (no third-party import), and contains no markdown-library import: `grep -E "from \"(?!node:)" scripts/runnable-ref/test-skip-integrity.ts` returns no non-`node:` import line.
    - The checker contains no literal `§14`: `grep -c '§14' scripts/runnable-ref/test-skip-integrity.ts` returns 0.
    - All four fixtures exist and carry the exact D-03 header `Test ID | Reason | Owner | Ticket/REQ | Expiry` — `grep -l 'Test ID' scripts/runnable-ref/fixtures/*-test-skips.md` lists all four.
    - `hollow-test-skips.md` carries a placeholder owner (`-` or `agent`) in a data row; `clean-test-skips.md` carries a `flaky-quarantine` row.
    - `npm run typecheck` (project-wide `tsc --noEmit`, honoring tsconfig + @types/node) exits 0 — reports no type errors across the kit including the new checker.
  </acceptance_criteria>
  <done>The checker .ts and four fixtures exist; the checker is node:builtins-only, type-checks clean, carries no §14 literal, and the RED fixture encodes the placeholder-owner hollow justification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest harness (SC3 + edge cases + host-emulation), build, commit the .js</name>
  <files>scripts/runnable-ref/test-skip-integrity.test.ts, scripts/runnable-ref/test-skip-integrity.js</files>
  <read_first>
    - scripts/runnable-ref/reference-check.test.ts (whole file, 117 lines) — the harness scaffold to clone: imports + `globals:false` lines 22-26, `runCheck()` spawn idiom lines 36-39, mkdtemp/afterEach cleanup lines 42-52, the 8 cases (Test 1 clean lines 56-60, Test 2 RED lines 63-69, Test 3 missing→exit 2 lines 72-76, Test 4/4b --json lines 85-101, Test 5 host-emulation lines 107-115).
    - scripts/runnable-ref/test-skip-integrity.ts (the checker authored in Task 1 — the unit under test).
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-PATTERNS.md (lines 91-122 — the exact case list to write + host-emulation requirement).
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-VALIDATION.md (lines 42-56 — the per-behavior automated command map; align each test name to it).
    - package.json (lines 9-12 — `build`/`test`/`freshness` scripts) + scripts/freshness.ts (line 43 — `OUTPUT_DIRS=["install","scripts","hooks"]` polices the committed .js).
  </read_first>
  <behavior>
    - "clean" case: GREEN fixture + `--skip-count` ≤ valid → status 0 + stdout contains "No findings." (TINT-01).
    - "hollow" case: RED fixture → status 1 + a clear-voice finding naming the placeholder owner (the SC3 keystone, TINT-01).
    - "expired" case: expired fixture → status 1 even when `--skip-count` balances (TINT-02).
    - "quarantine" case: quarantine fixture + matching `--skip-count` → status 0 (non-blocking lane, TINT-02).
    - "over-count" case: clean fixture + `--skip-count` greater than valid justifications → status 1 (D-05, TINT-02).
    - "missing" case: nonexistent registry path → status 2 (error, not a false pass).
    - "json" case: `--json` on the RED fixture → status 1 + parseable `{ok:false, findings:[...]}` block.
    - "host-emulation" case: copy the committed .js into a bare mkdtemp dir with no node_modules, run against the RED fixture → status 1 (proves node:builtins-only).
  </behavior>
  <action>
Create `scripts/runnable-ref/test-skip-integrity.test.ts` by cloning `reference-check.test.ts` verbatim and adapting: point `CHECK_JS` at `test-skip-integrity.js` (the COMMITTED artifact, never the .ts); replace the `CLEAN`/`BAD` fixture constants with the four `*-test-skips.md` fixtures; keep the `runCheck(...args)` spawn idiom, the `globals:false` explicit imports, and the `mkdtemp`/`afterEach` cleanup unchanged. Write the eight cases listed in `<behavior>`, naming the RED case with the literal substring `"hollow"` so `vitest run -t "hollow"` selects it (per 16-VALIDATION.md line 44). For the host-emulation case, mirror reference-check.test.ts lines 107-115 exactly — `copyFileSync(CHECK_JS, hostJs)` into a bare temp dir, run with `cwd: host` against the RED fixture (absolute path), assert status 1.

Then build and commit the compiled output: run `npm run build` (tsc) to emit `scripts/runnable-ref/test-skip-integrity.js`. The `.test.ts` is excluded from emit (tsconfig already excludes `**/*.test.ts`); Vitest runs it directly. Verify `npm run freshness` is green (the committed `.js` matches a fresh tsc rebuild — the freshness gate already watches `scripts/**`).
  </action>
  <verify>
    <automated>npx vitest run scripts/runnable-ref/test-skip-integrity.test.ts && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/runnable-ref/test-skip-integrity.test.ts -t "hollow"` exits 0 (the test passes — i.e. the RED fixture makes the checker exit 1, asserted inside the test). THE SC3 KEYSTONE.
    - `npx vitest run scripts/runnable-ref/test-skip-integrity.test.ts` exits 0 (all eight cases green).
    - `npx vitest run scripts/runnable-ref/reference-check.test.ts` exits 0 (the proven reference was NOT disturbed — Pitfall 1 regression guard).
    - `node scripts/runnable-ref/test-skip-integrity.js scripts/runnable-ref/fixtures/clean-test-skips.md --skip-count 1` exits 0 and prints "No findings."
    - `node scripts/runnable-ref/test-skip-integrity.js scripts/runnable-ref/fixtures/hollow-test-skips.md --skip-count 1` exits 1.
    - `node scripts/runnable-ref/test-skip-integrity.js scripts/runnable-ref/fixtures/nope.md` exits 2.
    - `npm run freshness` exits 0 (committed test-skip-integrity.js matches a fresh tsc rebuild).
  </acceptance_criteria>
  <done>The harness exists with all eight cases green, the SC3 keystone proven, the reference suite still green, and the committed .js fresh.</done>
</task>

<task type="auto">
  <name>Task 3: Append the installer RUNNABLES tuple + rebuild install.js</name>
  <files>install/install.ts, install/install.js</files>
  <read_first>
    - install/install.ts lines 698-731 (the `RUNNABLES` array at line 700-702 + `materializeRunnable()` at 704-731 — additive/idempotent/never-overwrite/DRY_RUN-aware; needs NO edit beyond the appended tuple).
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-PATTERNS.md (lines 137-151 — the entire installer change is one appended tuple).
    - scripts/freshness.ts line 43 (`OUTPUT_DIRS` includes `install` → install.js drift is policed).
  </read_first>
  <action>
In `install/install.ts`, append exactly one tuple to the `RUNNABLES` array (after the existing `reference-check.js` entry, line 701): `["scripts/runnable-ref/test-skip-integrity.js", "tools/grugops/test-skip-integrity.js"]`. Make NO other change — `materializeRunnable()` already handles the copy with skip-if-missing, skip-if-identical, never-`>`-truncate (preserves a host-edited file verbatim, T-15-05-Tamper), and DRY_RUN. Then run `npm run build` to regenerate `install/install.js` and commit it (the freshness gate fails red on any `.ts`→`.js` drift).
  </action>
  <verify>
    <automated>grep -c 'test-skip-integrity.js' install/install.ts && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'test-skip-integrity.js.*tools/grugops' install/install.ts` returns at least 1 (the new tuple is present).
    - The `RUNNABLES` array still contains the original `reference-check.js` tuple: `grep -c 'reference-check.js' install/install.ts` returns at least 1 (the reference tuple was not removed).
    - `grep -c 'test-skip-integrity' install/install.js` returns at least 1 (install.js rebuilt to include the new tuple).
    - `npm run freshness` exits 0 (install.js + test-skip-integrity.js both fresh against a tsc rebuild).
    - `npm test` exits 0 (the full suite — install harness + both runnable harnesses — green).
  </acceptance_criteria>
  <done>install.ts carries the second RUNNABLES tuple, install.js is a fresh committed build including it, and the full suite + freshness are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → its own quality gate | An agent under the §14 gate is untrusted to clear a test-integrity failure (it could self-author a justification). |
| host-edited registry → checker | The `.grugops/test-skips.md` content is untrusted input parsed by the checker. |
| committed .ts → committed .js | Drift between source and the materialized artifact must be detectable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Elevation of Privilege | agent self-authoring a skip justification to clear its own gate | mitigate (structural) | The checker validates format only; un-cheatability is structural (D-02/D-08) — test-integrity is human-only and the registry human-owned, wired in Plan 03's gate prose. This plan's checker never trusts authorship; it cannot be cleared by an agent-written row because the row must pass format + the human owns the file. |
| T-16-01-02 | Tampering | hollow justification slips past the format check (false pass) | mitigate | The validator rejects placeholder owner / off-list category / empty Test ID or Ticket/REQ / past-or-unparseable Expiry; the `hollow-test-skips.md` RED fixture + the `"hollow"` Vitest case prove exit 1 (SC3). |
| T-16-01-03 | Repudiation | a silent-zero skip count hides a hollowed suite | mitigate | `--skip-count` absent or non-integer → clear-voice `UNKNOWN - verify` finding → exit 1 (D-14); never coerce a silent 0 to a pass. |
| T-16-01-04 | Denial of Service / Input Validation | malformed/missing/unreadable registry crashes the checker open (false pass) | mitigate | Fail-closed try/catch copied from reference-check.ts: no path → exit 2; unreadable file → exit 2 (error, distinct from a clean fail) — proven by the "missing" Vitest case. |
| T-16-01-05 | Tampering | path traversal / reading outside the intended registry path | accept | The checker reads exactly the one path it is handed (no glob, no shell, no eval — node:fs `readFileSync` of a literal arg); the gate (Plan 03) always passes the fixed `.grugops/test-skips.md`. No traversal surface beyond a caller-supplied path, which the human-owned gate controls. |
| T-16-01-06 | Tampering | stale/drifted committed .js vs .ts | mitigate | `npm run freshness` rebuilds to a temp dir and fails red on any byte difference for `scripts/**` + `install/**`. |
| T-16-01-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NOTHING — the checker is node:builtins-only; grugops's dev deps `{typescript, vitest, @types/node}` are unchanged from Phase 15. No package-install task exists; no legitimacy gate required. |
</threat_model>

<verification>
- `npx vitest run scripts/runnable-ref/test-skip-integrity.test.ts -t "hollow"` exits 0 (SC3 keystone).
- `npx vitest run scripts/runnable-ref/test-skip-integrity.test.ts` exits 0 (all cases).
- `npx vitest run scripts/runnable-ref/reference-check.test.ts` exits 0 (reference undisturbed, Pitfall 1).
- `npm test` exits 0 (full suite).
- `npm run freshness` exits 0 (no .ts→.js drift on the two new/changed committed outputs).
- `grep -c '§14' scripts/runnable-ref/test-skip-integrity.ts` returns 0.
</verification>

<success_criteria>
- The test-integrity checker is a committed, node:builtins-only TS routine compiled to a freshness-policed .js (D-PL1/D-PL3).
- A hollow justification (placeholder owner) makes the checker exit 1 (SC3, TINT-01).
- Expired entries and over-count both block (exit 1); a valid flaky-quarantine entry does not (D-05, TINT-02).
- Missing/unreadable registry → exit 2; bare-temp-dir host-emulation passes.
- install.ts materializes the checker via one RUNNABLES tuple into tools/grugops/ (TOOL-02 reuse).
- The committed checker is the enforcement form (TINT-03 mechanical half — never a prose rule).
</success_criteria>

<output>
Create `.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-01-SUMMARY.md` when done.
</output>
</content>
</invoke>

---
phase: 16-14-gate-convergence-lint-ui-e2e-test-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/checklists/playwright-visual-regression-recipe.md
  - agent-factory/checklists/linter-recommendations.md
  - agent-factory/checklists/accessibility-checklist.md
  - agent-factory/checklists/00-index.md
autonomous: true
requirements: [UIQA-01, LINT-01]
must_haves:
  truths:
    - "A new Playwright visual-regression recipe checklist encodes the flake-resistance set — fixed viewport, animations disabled, caret hidden, masking volatile regions, maxDiffPixels tolerance, CI/Docker baseline determinism, --update-snapshots — for grugops's users (UIQA-01)."
    - "The accessibility checklist is extended with the @axe-core/playwright AxeBuilder + .withTags WCAG 2.2 AA bar (UIQA-01)."
    - "A new per-stack linter recommendations checklist gives the ESLint 9 flat (Vue/TS default), Biome (caveated), Ruff (Python), golangci-lint (Go) table with the exact strict (--max-warnings 0) and safe-autofix CLI, and UNKNOWN - verify for unknown stacks (LINT-01)."
    - "Both new checklists register in 00-index.md and carry kind: checklist frontmatter; all three docs are clear professional voice (no caveman)."
  artifacts:
    - path: "agent-factory/checklists/playwright-visual-regression-recipe.md"
      provides: "toHaveScreenshot flake-resistance recipe + axe pointer (UIQA-01)"
      contains: "toHaveScreenshot"
    - path: "agent-factory/checklists/linter-recommendations.md"
      provides: "Per-stack linter table + strict/autofix CLI (LINT-01)"
      contains: "max-warnings"
    - path: "agent-factory/checklists/accessibility-checklist.md"
      provides: "axe-core AxeBuilder WCAG 2.2 AA extension (UIQA-01)"
      contains: "AxeBuilder"
    - path: "agent-factory/checklists/00-index.md"
      provides: "Index rows for the two new checklists"
      contains: "linter-recommendations.md"
  key_links:
    - from: "agent-factory/checklists/00-index.md"
      to: "agent-factory/checklists/linter-recommendations.md"
      via: "index table row"
      pattern: "linter-recommendations\\.md"
    - from: "agent-factory/checklists/00-index.md"
      to: "agent-factory/checklists/playwright-visual-regression-recipe.md"
      via: "index table row"
      pattern: "playwright-visual-regression-recipe\\.md"
---

<objective>
Author the two bulky reference artifacts the gate POINTS to (D-06 reference-not-embed) and extend the accessibility checklist for axe-core — all in `agent-factory/checklists/` (reuse, no new dir, D-07). These satisfy UIQA-01 (the Playwright `toHaveScreenshot` flake-resistance recipe + the axe-core a11y bar) and LINT-01 (the per-stack linter recommendations table). The gate prose in Plan 03 references these by filename; keeping the how-to here keeps `05-pr-quality-gate.md` lean.

Purpose: Single-source governs the gate LOGIC (which stays in 05); the bulky how-to is referenced, not forked (D-06). Placing these as `checklists/` siblings means the validator/installer learn no new directory and the gate stays small.
Output: `playwright-visual-regression-recipe.md` (NEW), `linter-recommendations.md` (NEW), an axe-core extension to `accessibility-checklist.md`, and two new rows in `00-index.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-CONTEXT.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-RESEARCH.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright visual-regression recipe + axe-core accessibility extension</name>
  <files>agent-factory/checklists/playwright-visual-regression-recipe.md, agent-factory/checklists/accessibility-checklist.md</files>
  <read_first>
    - agent-factory/checklists/accessibility-checklist.md (whole file, 16 lines) — the frontmatter (`kind: checklist` / `tier: enterprise`) + list shape to preserve and extend in place; the existing "target standard (e.g. WCAG 2.2 AA) noted" line.
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-RESEARCH.md (Pattern 4 lines 264-293 — the verified `toHaveScreenshot` recipe with `animations:'disabled'`, `caret:'hide'`, `mask:[...]`, `maxDiffPixels`, fixed viewport, the playwright.config viewport pin, CI/Docker baseline determinism + `--update-snapshots`; Pattern 5 lines 295-311 — the `@axe-core/playwright` `AxeBuilder` + `.withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa'])` → `expect(results.violations).toEqual([])`).
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-PATTERNS.md (lines 182-218 — the checklist-doc assignment: frontmatter to copy, the axe extension content, clear-voice constraint).
    - agent-factory/checklists/security-nfr-checklist.md (the in-repo precedent for a large clear-voice table-heavy checklist — match its tone/structure).
  </read_first>
  <action>
Create `agent-factory/checklists/playwright-visual-regression-recipe.md` with `kind: checklist` + `tier: enterprise` frontmatter (copy from accessibility-checklist.md lines 1-4). Body in clear professional voice, sourced from RESEARCH Pattern 4: the `toHaveScreenshot` flake-resistance recipe — name `@playwright/test` `1.60.0`, the user install line (`npm install -D @playwright/test @axe-core/playwright` + `npx playwright install --with-deps`), a `toHaveScreenshot` example with `animations: 'disabled'`, `caret: 'hide'`, `mask: [page.getByTestId('...')]`, `maxDiffPixels: 100` (or `maxDiffPixelRatio`), a FIXED viewport via `page.setViewportSize` and the `playwright.config` `use.viewport` pin, role/label/`data-testid` locators (never brittle CSS/xpath), the per-platform baseline naming + CI/Docker determinism warning (generate baselines in the same container/OS CI uses), and `npx playwright test --update-snapshots`. Add a one-line pointer to the axe extension below ("a11y assertions live in accessibility-checklist.md"). Do NOT write the literal "§14" — refer to "the quality gate" / "the gate's UI/E2E step".

Extend `agent-factory/checklists/accessibility-checklist.md` IN PLACE: keep the existing frontmatter and the six manual checks; append an axe-core automated section (RESEARCH Pattern 5) — name `@axe-core/playwright` `4.11.3`, the `AxeBuilder` default-export import, `.withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa'])` mapping to the WCAG 2.2 AA bar (Phase 13 D-09), `.include()/.exclude()` scoping, `.analyze()` returning `{ violations }`, and `expect(results.violations).toEqual([])`. Clear professional voice throughout.
  </action>
  <verify>
    <automated>test -f agent-factory/checklists/playwright-visual-regression-recipe.md && grep -q 'toHaveScreenshot' agent-factory/checklists/playwright-visual-regression-recipe.md && grep -q 'AxeBuilder' agent-factory/checklists/accessibility-checklist.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `agent-factory/checklists/playwright-visual-regression-recipe.md` exists and contains `toHaveScreenshot`, `animations`, `mask`, and `--update-snapshots`: `grep -c -E 'toHaveScreenshot|animations|mask|update-snapshots' agent-factory/checklists/playwright-visual-regression-recipe.md` returns ≥ 4.
    - It carries `kind: checklist` frontmatter: `grep -c 'kind: checklist' agent-factory/checklists/playwright-visual-regression-recipe.md` returns 1.
    - `accessibility-checklist.md` now contains `AxeBuilder` and `withTags` and `wcag22aa`: `grep -c -E 'AxeBuilder|withTags|wcag22aa' agent-factory/checklists/accessibility-checklist.md` returns ≥ 3.
    - Neither file contains the literal `§14`: `grep -c '§14' agent-factory/checklists/playwright-visual-regression-recipe.md` returns 0.
    - No caveman voice: `grep -c -iE '\bgrug\b|smash|cave' agent-factory/checklists/playwright-visual-regression-recipe.md` returns 0.
  </acceptance_criteria>
  <done>The Playwright recipe exists with the full flake-resistance set, the accessibility checklist carries the axe-core WCAG 2.2 AA extension, both clear-voice and §14-literal-free.</done>
</task>

<task type="auto">
  <name>Task 2: Per-stack linter recommendations table + index registration</name>
  <files>agent-factory/checklists/linter-recommendations.md, agent-factory/checklists/00-index.md</files>
  <read_first>
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-RESEARCH.md (the per-stack lint invocations block lines 390-420 — the EXACT verified CLI: `eslint . --max-warnings 0` (strict), `eslint . --fix` / `--fix-type problem,suggestion,layout` (safe autofix), `biome lint --error-on-warnings` / `--write`, `ruff check --fix` / `--exit-non-zero-on-fix`, `golangci-lint run --fix`; the Standard Stack table lines 109-122 — versions ESLint 9.39.4/10.5.0, Biome 2.5.0, Ruff latest, golangci-lint v2; D-11 lines 51-54 / D-13 line 57).
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-PATTERNS.md (lines 202-218 — the new-sibling assignment: frontmatter to copy, `security-nfr-checklist.md` as the table-heavy precedent, index registration shape).
    - agent-factory/checklists/00-index.md (whole file, 39 lines) — the enterprise-tier table at lines 32-38; add the two new rows here.
    - agent-factory/checklists/accessibility-checklist.md (lines 1-4) — the `kind: checklist` + `tier:` frontmatter to copy.
  </read_first>
  <action>
Create `agent-factory/checklists/linter-recommendations.md` with `kind: checklist` + `tier: enterprise` frontmatter. Body in clear professional voice — a per-stack linter table sourced from RESEARCH (D-11):
- JS/TS/Vue → ESLint flat config (`eslint.config.js`) — the DEFAULT (matches grugops's own stack); note "ESLint v9 maintenance / v10 latest; flat config is the default in both". Strict (`quality.lint.strict:true`, D-12) → `eslint . --max-warnings 0` (fail-on-warning). Autofix (`autofix:true`) → `eslint . --fix` (safe; optionally `--fix-type problem,suggestion,layout`).
- Biome (`@biomejs/biome` 2.5.0) → CAVEATED alternative, NOT the Vue default (narrower rule coverage / younger ecosystem / experimental Vue SFC support). `biome lint --error-on-warnings .` (strict), `biome lint --write .` (safe autofix).
- Python → Ruff: `ruff check .`, strict `ruff check --exit-non-zero-on-fix .`, autofix `ruff check --fix .` (safe by default).
- Go → golangci-lint v2: `golangci-lint run`, autofix `golangci-lint run --fix`; "any issue → exit 1" is the gate signal. Config filename recorded `UNKNOWN - verify` (not confirmed in research — A5).
- Unknown/other stack → `UNKNOWN - verify` (D-13: never fake a linter).
State the D-12 wiring explicitly: `strict:true` fails on warnings; `strict:false` reports warnings, only errors fail; `autofix:true` runs the safe autofix then rechecks; `autofix:false` reports only. Note D-13: no configured linter → record `UNKNOWN - verify`, non-blocking. Do NOT write the literal "§14".

Register both new files in `agent-factory/checklists/00-index.md` — add two rows to the enterprise-tier table (after line 38): `| \`linter-recommendations.md\` | when configuring the lint step at the gate |` and `| \`playwright-visual-regression-recipe.md\` | when verifying UI visual baselines at the gate |`.
  </action>
  <verify>
    <automated>grep -q 'max-warnings 0' agent-factory/checklists/linter-recommendations.md && grep -q 'linter-recommendations.md' agent-factory/checklists/00-index.md && grep -q 'playwright-visual-regression-recipe.md' agent-factory/checklists/00-index.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `linter-recommendations.md` names all four stacks + the strict/autofix CLI: `grep -c -E 'ESLint|Biome|Ruff|golangci-lint|max-warnings|--fix' agent-factory/checklists/linter-recommendations.md` returns ≥ 5.
    - It records `UNKNOWN - verify` for the unknown-stack and no-linter cases: `grep -c 'UNKNOWN - verify' agent-factory/checklists/linter-recommendations.md` returns ≥ 1.
    - It carries `kind: checklist` frontmatter: `grep -c 'kind: checklist' agent-factory/checklists/linter-recommendations.md` returns 1.
    - `00-index.md` registers both new files: `grep -c -E 'linter-recommendations.md|playwright-visual-regression-recipe.md' agent-factory/checklists/00-index.md` returns 2.
    - No literal `§14`: `grep -c '§14' agent-factory/checklists/linter-recommendations.md` returns 0.
    - The structure validator passes for checklist presence: `node scripts/validate-agent-factory.js` exits 0 (or its `--strict` equivalent if that is the project's gate).
  </acceptance_criteria>
  <done>The per-stack linter table exists with the verified strict/autofix CLI and UNKNOWN-verify fallbacks, both new checklists are registered in the index, and the validator passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recommended tool versions → user repos | grugops recommends tool/versions it does not install; a wrong/fabricated recommendation misleads users. |
| checklist prose → gate behavior | The gate references these by filename; a §14-literal or a faked command would corrupt the single-source contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Information Disclosure / Misinformation | a fabricated linter command or version in the table | mitigate | Every CLI/version is `[VERIFIED]` against the tool's docs + npm in RESEARCH this session; the unknown-stack and no-linter cases record `UNKNOWN - verify` (D-13) — never a faked linter (no-fabrication). |
| T-16-02-02 | Tampering | a literal "§14" leaking into a shipped checklist | mitigate | Acceptance criteria grep `§14` to 0 in each new file; the docs reference the gate by behavior ("the quality gate") not by section literal (Phase 12 D-12). |
| T-16-02-03 | Tampering | caveman voice leaking onto a quality surface | mitigate | These are quality/safety surfaces — clear professional voice only (CLAUDE.md two-voice); acceptance grep for `\bgrug\b`/caveman idioms returns 0. |
| T-16-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NOTHING into grugops — it only TEMPLATES recommendations for users in markdown checklists. The RESEARCH Package Legitimacy Audit confirmed all recommended packages via docs + registry; no grugops install task exists, so no blocking legitimacy checkpoint is required (CONTEXT.md: nothing is installed into grugops). |
</threat_model>

<verification>
- `agent-factory/checklists/playwright-visual-regression-recipe.md` and `linter-recommendations.md` exist with `kind: checklist` frontmatter.
- `accessibility-checklist.md` carries the axe-core AxeBuilder WCAG 2.2 AA extension.
- `00-index.md` registers both new checklists.
- `grep -c '§14'` returns 0 across all three docs.
- No caveman voice on these quality surfaces.
- `node scripts/validate-agent-factory.js` exits 0.
</verification>

<success_criteria>
- The Playwright recipe encodes the full flake-resistance set (UIQA-01).
- The accessibility checklist carries the axe-core WCAG 2.2 AA bar (UIQA-01).
- The per-stack linter table is present, accurate, and config-dial-wired with UNKNOWN-verify fallbacks (LINT-01).
- Both new checklists register in the index; all three are clear-voice and §14-literal-free.
</success_criteria>

<output>
Create `.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-02-SUMMARY.md` when done.
</output>

---
phase: 22-memory-trajectory-compaction-dialable-token-economy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/workflows/18-context-compaction.md
  - scripts/generate-catalog.test.ts
  - docs/catalog/README.md
  - agent-factory/roles/agents-md-scribe.md
  - agent-factory/roles/architect-design.md
  - agent-factory/roles/ba-pm.md
  - agent-factory/roles/brownfield-mapper.md
  - agent-factory/roles/compliance-officer.md
  - agent-factory/roles/factory-coach.md
  - agent-factory/roles/frontend-ui.md
  - agent-factory/roles/greenfield-mapper.md
  - agent-factory/roles/incident-responder.md
  - agent-factory/roles/installer.md
  - agent-factory/roles/orchestrator.md
  - agent-factory/roles/qe-e2e.md
  - agent-factory/roles/release-manager.md
  - agent-factory/roles/security-nfr.md
  - agent-factory/roles/software-engineer.md
  - agent-factory/roles/system-analyst.md
  - agent-factory/roles/uat-planner.md
autonomous: true
requirements: [CMP-03]

must_haves:
  truths:
    - "agent-factory/workflows/18-context-compaction.md exists, is clear professional voice, and is the single-source compaction protocol (CMP-03, SC4)"
    - "WF18 carries order: 18 even though WF17 is absent on disk — the ordinals jump 16->18 by design; it is NOT renumbered to 17 (Pitfall 2)"
    - "WF18 references WF16's admission rules and 05-pr-quality-gate.md's self_fix_attempts loop rather than restating them (D-10, single-source)"
    - "Every one of the 17 role files carries a one-line WF18 pointer placed alongside the existing WF16 pointer; no role restates the compaction protocol (CMP-03, SC4)"
    - "The catalog generator emits 18 workflow rows; the generate-catalog test asserts toBe(18) and lists context compaction in WORKFLOW_NAMES; the regenerated docs/catalog/README.md is committed and fresh (Pitfall 1)"
  artifacts:
    - path: "agent-factory/workflows/18-context-compaction.md"
      provides: "The single-source two-tier compaction protocol (clear voice); trigger + body-compression-per-dial + carve-out hand-off to compactor.ts + re-verify-via-admit + honest degrade"
      contains: "# Workflow: context compaction"
    - path: "scripts/generate-catalog.test.ts"
      provides: "Updated count assertion (toBe(18)) + WORKFLOW_NAMES entry for WF18"
      contains: "toBe(18)"
    - path: "docs/catalog/README.md"
      provides: "Regenerated catalog including the WF18 row (byte-fresh vs the generator)"
      contains: "18-context-compaction.md"
  key_links:
    - from: "agent-factory/roles/*.md"
      to: "agent-factory/workflows/18-context-compaction.md"
      via: "one-line pointer per role, mirroring the existing WF16 pointer (D-10)"
      pattern: "18-context-compaction.md"
    - from: "agent-factory/workflows/18-context-compaction.md"
      to: "agent-factory/workflows/16-context-read-write.md"
      via: "references WF16's admission rules + escape hatch for the re-verify (D-10/D-12) — referenced, not restated"
      pattern: "16-context-read-write.md"
    - from: "scripts/generate-catalog.test.ts"
      to: "agent-factory/workflows/18-context-compaction.md"
      via: "the generator auto-discovers WF18 (/^\\d{2}-.+\\.md$/); the test count + WORKFLOW_NAMES must be bumped in lockstep (Pitfall 1)"
      pattern: "context compaction"
---

<objective>
Author the single-source compaction protocol `agent-factory/workflows/18-context-compaction.md` (clear voice), add the cheap one-line WF18 pointer to all 17 role files (the Phase-21 WF16-pointer pattern), and defuse the catalog-count landmine by bumping `generate-catalog.test.ts` from `toBe(17)` to `toBe(18)` + adding WF18 to `WORKFLOW_NAMES`, then regenerating and committing the fresh `docs/catalog/README.md`.

This is the documentation/protocol half of the body/frontmatter seam: WF18 owns the *agent's* semantic body-compression behavior (D-01) and tells the agent how to drive the `compactor.ts` carve-out check (Plan 01) and the `admit()` re-verify. It restates nothing — it references WF16 and the §14 gate (D-10).

Purpose: Give every role one named, single-source protocol for two-tier compaction before Phase 23's fan-out makes the token tax real.
Output: WF18 (new workflow file), 17 role pointer lines, the catalog test bump + regenerated catalog.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-CONTEXT.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-RESEARCH.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-PATTERNS.md

@agent-factory/workflows/16-context-read-write.md
@agent-factory/workflows/05-pr-quality-gate.md
@scripts/generate-catalog.test.ts
@scripts/generate-catalog.ts
@agent-factory/roles/system-analyst.md
</context>

## Artifacts this phase produces

> Source-grounding pass: exclude these newly-created symbols/paths from drift verification — they do not yet exist before this phase.

- `agent-factory/workflows/18-context-compaction.md` — new workflow file (ordinal 18, `# Workflow: context compaction`)
- The catalog display name `"context compaction"` — new `WORKFLOW_NAMES` entry derived from WF18's `# Workflow:` H1
- The one-line WF18 pointer line added to all 17 `agent-factory/roles/*.md`
- The `context.compaction` config key (created in Plan 01 — referenced by WF18 prose, not defined here)
- `.grugops/context/<task>/threads/<agent>.md` — the ephemeral local trajectory tier WF18 distills from (path defined by Plan 01 / D-08)

<tasks>

<task type="auto">
  <name>Task 1: Author 18-context-compaction.md (single-source protocol, clear voice)</name>
  <files>agent-factory/workflows/18-context-compaction.md</files>
  <read_first>
    - agent-factory/workflows/16-context-read-write.md (the WHOLE file is the template — frontmatter `kind: workflow` / `order: 16` / `cadence: both`; the `# Workflow: ...` H1; the `## When to use` / `## Steps` / `## Stop conditions` / `## Done condition` / `## Commit` section shape; how lines 21/26/35 REFERENCE 05-pr-quality-gate.md's self_fix_attempts loop and the admission rules rather than restating them; the escape-hatch wording lines 25-27/40 — refuse -> degrade to a claim with confidence "UNKNOWN - verify")
    - agent-factory/workflows/05-pr-quality-gate.md (the §14 gate + the bounded self_fix_attempts loop WF18 references for the re-verify; the "advise loudly, never hide / never a faked pass" posture)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-CONTEXT.md (D-04 dial body-verbosity behavior; D-05 un-dialable carve-out; D-10 single-source + reference-not-restate; D-11 trigger = write-after-verify/handback primary + opportunistic mid-task secondary; D-12 re-verify reuses admit() + honest degrade; D-01 body=agent / structure=tool seam)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-PATTERNS.md (the WF18 frontmatter/section template + the order:18-despite-missing-17 note, lines 153-172)
  </read_first>
  <action>
    Create agent-factory/workflows/18-context-compaction.md with frontmatter `kind: workflow`, `order: 18`, `cadence: both` and H1 `# Workflow: context compaction`. Use `order: 18` even though 17-task-claim.md does not exist on disk (00-16 present) — the generator sorts by order and tolerates the gap; the validator floor stops at 13. Do NOT renumber to 17 (it would collide with the Phase-23 WF17 deliverable — Pitfall 2). Clear professional voice throughout (this is a trace + safety + token-economy surface — CLAUDE.md), matching context-io.ts / hooks/guard.ts / WF16, NOT caveman.

    Author the single-source protocol with the same section shape as WF16 (`## When to use`, `## Steps`, `## Stop conditions`, `## Done condition`, `## Commit`). Internal section ordering is planner-discretion; cover:
    - The body/frontmatter seam (D-01): the agent compresses note BODIES (the semantic judgment — its intelligence, caveman token-economy applied to memory); scripts/compactor.ts protects note STRUCTURE (the deterministic carve-out invariant) and never summarizes. Two distinct jobs.
    - The two tiers (CMP-01/D-07/D-08): the verbose trajectory lives in the ephemeral, gitignored local tier `.grugops/context/<task>/threads/<agent>.md`; only the compact, re-verified distillation promotes to the committed shared context.
    - The trigger (D-11): primary = the write-after-verify / task-handback boundary (ONE distillation pass when the agent finishes its unit and is about to promote — NOT per-write, which fights the atomic appendNote and wastes tokens); secondary = opportunistic mid-task on context-window pressure (the "store essential info before proceeding" checkpoint that frees the window). The mid-task threshold/heuristic is advisory.
    - The dial (D-04/D-05): body-compression behavior follows `context.compaction` read at point-of-use, defaulting to `aggressive` when absent. Express the three behaviors as prose (the exact body-compression tiers are planner-final): `aggressive` = only the compact gist reaches shared context, raw stays in threads/ unfolded on demand; `balanced` = gist + a fuller mid-tier summary promoted; `retain-raw` = full trajectory bodies admitted to the committed shared context. State clearly that the dial moves body verbosity / how-much-raw-reaches-shared ONLY — it NEVER drops a durable note or a load-bearing field; the carve-out is the un-dialable safety floor at every value (D-05).
    - The carve-out hand-off (D-02/CMP-02): before promotion the proposed promoted set is checked by scripts/compactor.ts — every raw failed-attempt note id survives, and verified_by / supersedes / by / at stay intact on every promoted note; a drop is refused and named. Reference this as the mechanical floor; do not restate the field set as protocol the agent enforces (the tool enforces it).
    - The single sanctioned write path (D-02.3): promotion happens ONLY via context-io.ts appendNote — never a hand-written `.grugops/context/` path.
    - The re-verify (D-12): only a promoted finding re-hits admission — REFERENCE WF16's admission rules and 05-pr-quality-gate.md's bounded self_fix_attempts loop (do NOT restate or re-dial them). A faithful body compaction keeps the finding's §14-gate#<id> stamp valid against the live green verdict (the verdict verified the work, not the words), so admit() re-admits cheaply. A compaction that materially changed the finding such that the stamp no longer cross-checks is REFUSED -> honestly degrade to a `claim` with `confidence: UNKNOWN - verify` (the Phase-21 escape hatch) — never a hand-carried stamp, never a faked pass. Soft kinds (claim / observation / decision / failed-attempt / artifact-ref) need no stamp and pass through.

    Mirror how WF16 references 05-pr-quality-gate.md (its lines 21/26/35) — WF18 names those files and points; it does not copy their content. The `## Commit` section follows agent-factory/_commit-convention.md (branch guard first; never a protected branch) like WF16's line 42-43; WF18 records verified context, never merges, never deploys.

    Do NOT add WF18 to validate-agent-factory.ts's frozen WORKFLOWS list — deep validator rewiring is Phase 24 (the validator floor stops at 13 and will not break on the new file). Author WF18 to the WF16 section shape so it is already conformant when Phase 24 wires it in.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/18-context-compaction.md && grep -q '^order: 18$' agent-factory/workflows/18-context-compaction.md && grep -q '^# Workflow: context compaction$' agent-factory/workflows/18-context-compaction.md && grep -q '16-context-read-write.md' agent-factory/workflows/18-context-compaction.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `agent-factory/workflows/18-context-compaction.md` exists with frontmatter `kind: workflow`, `order: 18`, `cadence: both` and H1 `# Workflow: context compaction` (grep confirms `^order: 18$` and the exact H1).
    - The file is NOT renumbered to 17 — `grep -q '^order: 17' agent-factory/workflows/18-context-compaction.md` returns non-zero (Pitfall 2).
    - WF18 REFERENCES WF16 and 05-pr-quality-gate.md (grep confirms both filenames appear) for the admission rules and the self_fix_attempts loop, and does NOT restate the admission steps or re-dial the loop (single-source, D-10).
    - WF18 documents the trigger (write-after-verify/handback primary + opportunistic mid-task secondary), the three dial behaviors as body-verbosity-only with an un-dialable carve-out (D-05), the single sanctioned `appendNote` write path, and the honest degrade-to-claim path with `confidence: UNKNOWN - verify` (D-12).
    - Voice is clear professional English (no caveman) — it is a trace/safety/token surface.
    - The section shape matches WF16 (`## When to use`, `## Steps`, `## Stop conditions`, `## Done condition`, `## Commit` all present).
  </acceptance_criteria>
  <done>WF18 exists as a clear-voice single-source compaction protocol with order:18 (not renumbered), referencing WF16 + the §14 gate rather than restating them, covering trigger / dial / carve-out hand-off / sanctioned write path / re-verify + honest degrade.</done>
</task>

<task type="auto">
  <name>Task 2: Catalog landmine — bump generate-catalog.test.ts 17->18 + regenerate catalog</name>
  <files>scripts/generate-catalog.test.ts, docs/catalog/README.md</files>
  <read_first>
    - scripts/generate-catalog.test.ts (the count assertion `countRowsLinkingInto(text, "workflows")).toBe(17)` at line 154; the WORKFLOW_NAMES list lines 72-90 ending with "context read/write"; the test name/comment lines 136-137 stating "17 workflows"; the file header comment lines 5-8 stating "17 roles + 17 workflows")
    - scripts/generate-catalog.ts (how the workflow display name is derived — from the `# Workflow: <name>` H1 at line 170/186 — so WF18's display name is `context compaction`; the auto-discovery regex `/^\d{2}-.+\.md$/` so the generator picks WF18 up with no edit; the sort-by-order line 199-ish)
    - agent-factory/workflows/18-context-compaction.md (the WF18 file authored in Task 1 — confirm its `# Workflow: context compaction` H1 yields the display name `context compaction` to add)
  </read_first>
  <action>
    Edit scripts/generate-catalog.test.ts:
    1. Bump the workflow count assertion at line 154 from `toBe(17)` to `toBe(18)`. Leave the roles count assertion at `toBe(17)` unchanged (roles are not added this phase).
    2. Add WF18's display name to the WORKFLOW_NAMES list (lines 72-90) — append `"context compaction",` after `"context read/write",`. The name is derived from WF18's `# Workflow: context compaction` H1 by the generator; confirm it matches the authored H1 from Task 1 exactly.
    3. Update the test name/comment at lines 136-137 and the file header comment (lines 5-8) that state "17 workflows" / "17 roles + 17 workflows" to read 18 workflows where they describe the workflow set (roles stay 17). Keep the comment honest — do not leave a stale 17.

    Then regenerate and commit the catalog so the freshness gate stays green:
    `npm run generate:catalog` (regenerates docs/catalog/README.md with the new WF18 row) then `npm run freshness:catalog` (proves the committed catalog == generator output). Commit the regenerated docs/catalog/README.md in the same change.
  </action>
  <verify>
    <automated>npm run generate:catalog && npm run freshness:catalog && npx vitest run scripts/generate-catalog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/generate-catalog.test.ts` asserts `countRowsLinkingInto(text, "workflows")).toBe(18)` and the roles assertion remains `toBe(17)`.
    - `WORKFLOW_NAMES` contains the entry `"context compaction"` (matching WF18's `# Workflow:` H1) — 18 entries total.
    - `npm run generate:catalog` regenerates `docs/catalog/README.md` to include a row linking `agent-factory/workflows/18-context-compaction.md`; `grep -q '18-context-compaction.md' docs/catalog/README.md` matches.
    - `npm run freshness:catalog` exits 0 — the committed catalog is byte-equal to the generator output.
    - `npx vitest run scripts/generate-catalog.test.ts` exits 0 — the bumped count + name list pass over the real kit (18 workflow rows).
    - No stale "17 workflows" text remains in the test header/comments where it now describes 18.
  </acceptance_criteria>
  <done>The catalog test asserts 18 workflow rows with `context compaction` in WORKFLOW_NAMES, the regenerated docs/catalog/README.md includes the WF18 row and is committed, and freshness:catalog + the catalog test are green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the one-line WF18 pointer to all 17 role files</name>
  <files>agent-factory/roles/agents-md-scribe.md, agent-factory/roles/architect-design.md, agent-factory/roles/ba-pm.md, agent-factory/roles/brownfield-mapper.md, agent-factory/roles/compliance-officer.md, agent-factory/roles/factory-coach.md, agent-factory/roles/frontend-ui.md, agent-factory/roles/greenfield-mapper.md, agent-factory/roles/incident-responder.md, agent-factory/roles/installer.md, agent-factory/roles/orchestrator.md, agent-factory/roles/qe-e2e.md, agent-factory/roles/release-manager.md, agent-factory/roles/security-nfr.md, agent-factory/roles/software-engineer.md, agent-factory/roles/system-analyst.md, agent-factory/roles/uat-planner.md</files>
  <read_first>
    - agent-factory/roles/system-analyst.md (the existing WF16 pointer line — verified shape at ~line 45: `Context I/O: read and write the shared context per ` followed by the WF16 path and the "single source; this role references it and does not restate it" wording — the EXACT shape to mirror, and the placement directly after which the WF18 pointer goes)
    - agent-factory/roles/orchestrator.md (its WF16 pointer at ~line 128 — confirm the same one-line shape; the orchestrator is the longest role file)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-PATTERNS.md (the role-pointer pattern lines 220-230 — all 17 roles carry the WF16 pointer; add ONE analogous WF18 line per role directly after it; the suggested wording)
  </read_first>
  <action>
    For each of the 17 role files (agents-md-scribe, architect-design, ba-pm, brownfield-mapper, compliance-officer, factory-coach, frontend-ui, greenfield-mapper, incident-responder, installer, orchestrator, qe-e2e, release-manager, security-nfr, software-engineer, system-analyst, uat-planner — NOT _role-switch-protocol.md), add ONE new pointer line directly after the existing WF16 pointer line, mirroring its shape:

    `Compaction: compact the local trajectory and promote per \`agent-factory/workflows/18-context-compaction.md\` — single source; this role references it and does not restate it.`

    This is the cheap additive D-10 pattern (identical in shape to the Phase-21 WF16 pointer). Do NOT restate any compaction steps in the role body — a pointer only. Place it adjacent to the WF16 pointer so the two single-source context references sit together. All 17 roles currently carry the WF16 pointer (`grep -rln "16-context-read-write.md" agent-factory/roles/` -> 17); the WF18 pointer must land in the same 17.

    Do NOT touch agent-factory/roles/_role-switch-protocol.md (it is the switch protocol, not a role; it has no WF16 pointer).
  </action>
  <verify>
    <automated>test "$(grep -rln '18-context-compaction.md' agent-factory/roles/ | grep -v _role-switch-protocol | wc -l | tr -d ' ')" = "17" && echo "17 role pointers OK"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 17 role files contain a `18-context-compaction.md` pointer line — `grep -rln '18-context-compaction.md' agent-factory/roles/` returns the same 17 files that carry the WF16 pointer.
    - `agent-factory/roles/_role-switch-protocol.md` is NOT modified (it carries no WF16 pointer).
    - Each WF18 pointer is a single line in the same shape as the WF16 pointer ("single source; this role references it and does not restate it"), placed adjacent to it.
    - No role body restates the compaction protocol steps — it is a pointer only (manual reviewer check; the validator section guard for WF18 is deferred to Phase 24).
  </acceptance_criteria>
  <done>All 17 role files carry a one-line WF18 pointer adjacent to their WF16 pointer; _role-switch-protocol.md is untouched; no role restates the protocol.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WF18 protocol text → agent behavior | The single-source compaction protocol instructs every role how to distill and promote; a protocol that under-specifies the re-verify or the carve-out hand-off would let unverified or structurally-broken notes reach the committed shared context. |
| catalog generator ↔ its test | The generator auto-discovers WF18; the test hardcodes the count — a drift here ships a RED gate or a stale catalog. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | Tampering / Repudiation | WF18 re-verify prose | mitigate | WF18 instructs the agent: a compacted `finding` carrying a `§14-gate#<id>` stamp that no longer cross-checks a live green verdict must NOT carry the stamp — degrade honestly to a `claim` with `confidence: UNKNOWN - verify` (D-12). WF18 references WF16's admission rules + the §14 gate rather than restating them, so there is one protocol, not a forked (drift-prone) copy. |
| T-22-07 | Spoofing / Tampering | WF18 single-source integrity | mitigate | WF18 is the one named protocol; the 17 role pointers reference it and restate nothing (D-10), so no role can drift a private, weaker compaction rule. The `guard_context_protocol_single_source` foundation guard that mechanically enforces "no role restates the protocol" is explicitly Phase 24; for Phase 22 this is a reviewer check (documented as a manual-only verification in 22-VALIDATION.md). |
| T-22-08 | Repudiation | catalog freshness | mitigate | The `toBe(18)` bump + `WORKFLOW_NAMES` entry + regenerated, committed `docs/catalog/README.md` keep `freshness:catalog` and the catalog test green, so the auditable docs catalog honestly reflects the 18-workflow set (no stale/forged count). |
| T-22-09 | Tampering | npm/dev installs | mitigate | Zero external packages installed (D-13). No `[ASSUMED]`/`[SUS]` package -> no install checkpoint required; RESEARCH.md Package Legitimacy Audit records "not applicable." |
</threat_model>

<verification>
- `test -f agent-factory/workflows/18-context-compaction.md` + the WF18 grep checks — WF18 exists with order:18 and references WF16/the §14 gate.
- `npm run generate:catalog && npm run freshness:catalog && npx vitest run scripts/generate-catalog.test.ts` — catalog regenerated, fresh, and the bumped count test green (18 workflows).
- `grep -rln '18-context-compaction.md' agent-factory/roles/ | grep -v _role-switch-protocol | wc -l` == 17 — all roles carry the pointer.
- `npx vitest run --exclude '**/scripts/e2e/**'` — full regression lane GREEN.
</verification>

<success_criteria>
- SC4: WF18 is the single-source compaction protocol (clear voice, order:18, references WF16 + the §14 gate, restates nothing); all 17 roles reference it via a one-line pointer and no role restates it.
- Catalog landmine defused: the generator emits 18 workflow rows, the test asserts `toBe(18)` with `context compaction` in WORKFLOW_NAMES, and the regenerated catalog is committed and fresh.
- CMP-03 (protocol half): the dial behaviors and the re-verify/degrade are documented in WF18 as the single-source protocol the roles point at.
</success_criteria>

<output>
Create `.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-02-SUMMARY.md` when done.
</output>

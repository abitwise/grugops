---
phase: 22-memory-trajectory-compaction-dialable-token-economy
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/compactor.test.ts
  - scripts/compactor.ts
  - scripts/compactor.js
autonomous: true
requirements: [CMP-02]
gap_closure: true

must_haves:
  truths:
    - "CMP-02 carve-out holds against held-out adversarial inputs: a verified_by stamp mutated from its real value to a forged §14-gate#FORGED-999 is REFUSED (exit 1) and names verified_by — proven by a test that was RED against the committed compactor.js before the fix and GREEN after (CR-01)"
    - "A by field mutated from one non-empty value to a different non-empty value (engineer -> attacker) is REFUSED (exit 1) and names by — proven by a RED-then-GREEN test (CR-01)"
    - "A durable note carrying a non-empty verified_by that is wholly deleted from the promoted set when 2+ promoted notes remain is REFUSED (exit 1) and names the dropped finding — proven by a RED-then-GREEN test (CR-02)"
    - "With two same-kind notes where one drops its by field, the drop is REFUSED (exit 1) and names by — the counterpart matcher no longer borrows an intact sibling — proven by a RED-then-GREEN test (CR-03)"
    - "The CLI check verb FAILS CLOSED on a missing/typo'd threadDir: exit 1 with an error on stderr, never carve-out intact / exit 0 — proven by a RED-then-GREEN test (WR-01)"
    - "checkCarveOut output is BYTE-IDENTICAL across aggressive / balanced / retain-raw for the same single-mutation input (asserted on the findings text, not merely status != 0) — the un-dialable floor is asserted by construction (WR-05, D-05)"
    - "compactor.js is byte-fresh vs compactor.ts after the fix: npm run freshness exits 0 (D-13)"
  prohibitions:
    - "A mutated or forged verified_by is NEVER accepted by checkCarveOut — any non-empty raw value that differs from the promoted value (including drop-to-empty AND mutation-to-different-value) is refused and named."
    - "A §14-gate-verified finding (non-empty verified_by) wholly dropped from the promoted set is NEVER accepted — its absence is detected by an affirmative existence check, not silently skipped via a null counterpart."
    - "findCounterpart NEVER falls back to sameKind[0] when more than one same-kind candidate exists — an unmatched verified raw note is handled by the CR-02 existence check, never by borrowing an intact sibling's fields."
    - "A missing threadDir NEVER reports carve-out intact — the CLI check path fails closed before running the oracle."
    - "degradeToClaim NEVER silently returns an un-degraded note — it throws if the post-conditions (kind became claim, confidence UNKNOWN - verify, no residual §14-gate stamp) do not hold (WR-03)."
    - "A failed-attempt note with no recoverable FA-id is NEVER given a silent best-effort filename id — it is reported as an explicit carve-out finding (WR-02)."
  artifacts:
    - path: "scripts/compactor.ts"
      provides: "Carve-out oracle hardened against mutation, wholly-dropped verified findings, ambiguous counterpart borrowing, and fail-open input/degrade paths"
      contains: "from \"./context-io.js\""
    - path: "scripts/compactor.js"
      provides: "Committed tsc output of the hardened compactor.ts (freshness-gated, D-13)"
    - path: "scripts/compactor.test.ts"
      provides: "Adversarial RED-first oracle: forged/mutated stamp, wholly-dropped verified finding with 2+ promoted, multi-same-kind drop, missing-threadDir fail-closed, byte-identical-dial assertion"
  key_links:
    - from: "scripts/compactor.ts"
      to: "scripts/context-io.ts"
      via: "imports appendNote / admit from context-io.js; promotion still routes ONLY through appendNote (D-02.3) — the fix forks NO writer"
      pattern: "appendNote|admit"
    - from: "scripts/compactor.ts"
      to: "scripts/freshness.ts"
      via: "freshness.ts collectJs() auto-globs scripts/*.js — the rebuilt compactor.js rejoins the drift gate with no edit to freshness.ts"
      pattern: "compactor\\.js"
---

<objective>
Close the single failed must-have of Phase 22: **CMP-02 / SC2**. The `checkCarveOut()` oracle in `scripts/compactor.ts` is the stated un-cheatable mechanical floor guaranteeing no load-bearing field is silently dropped on the way from the raw thread to the promoted notes. Verification (`22-VERIFICATION.md`) and code review (`22-REVIEW.md`) confirmed THREE independent bypasses against the committed `scripts/compactor.js`, plus a fail-open input path and a fail-open degrade path — and an oracle that stays green through all of them.

This plan makes CMP-02 genuinely hold: it strengthens the oracle with held-out adversarial cases (written RED first against today's `.js`, GREEN after the fix), fixes the three confirmed bypasses (CR-01 mutation, CR-02 wholly-dropped verified finding, CR-03 multi-same-kind borrowing) plus the WR-01 fail-open CLI input, hardens two same-file fail-open paths (WR-02 `failedAttemptId`, WR-03 `degradeToClaim`), asserts dial-independence by construction (WR-05), then rebuilds and commits the byte-fresh `compactor.js`.

This is a SAFETY ORACLE: a green suite is NOT proof (this exact phase shipped 14 green tests over a bypassable check). Completion evidence is the three original bypass reproductions now returning exit 1 / non-empty findings via named tests that were demonstrably RED before the fix and GREEN after, plus `npm run freshness` exit 0 — never merely "N tests pass."

Purpose: SC2 is the only blocker between Phase 22 and a verified phase; the carve-out is the integrity boundary for gate provenance, and it must refuse forged-stamp substitution and dropped-verdict repudiation before Phase 23's parallel fan-out makes the cost real.
Output: hardened `scripts/compactor.ts`, byte-fresh committed `scripts/compactor.js`, adversarial RED-first cases in `scripts/compactor.test.ts`.

CMP-01 and CMP-03 are already SATISFIED by the shipped plans 22-01 / 22-02 — this gap-closure plan touches NEITHER and re-covers NEITHER.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-CONTEXT.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-VERIFICATION.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-REVIEW.md

@scripts/compactor.ts
@scripts/compactor.test.ts
@scripts/context-io.ts
@scripts/freshness.ts
@package.json
</context>

## Artifacts this phase produces

> Source-grounding pass: these symbols/test names are NEW or REWORDED by this gap-closure plan — exclude them from "already exists" drift checks; they do not exist before this plan.

- New adversarial test cases in `scripts/compactor.test.ts` (titles): `"mutates verified_by to a forged stamp"`, `"mutates by to a different non-empty value"`, `"wholly drops a verified finding with 2+ promoted notes"`, `"drops by on one of two same-kind findings"`, `"CLI check fails closed on a missing threadDir"`, `"carve-out findings are byte-identical across all three dials"`.
- Reworded carve-out failure messages in `compactor.ts` `checkCarveOut`: the provenance-field finding now distinguishes "dropped to empty" vs "altered to <value>"; a new finding for a wholly-dropped verified durable note; a new finding for a failed-attempt with no recoverable FA-id.
- A possible new private helper in `compactor.ts` for the affirmative wholly-dropped-verified-finding existence check (one function or inline is planner/executor discretion; the existing `findCounterpart` is reworked to deterministic 1:1 matching). Reworded header/inline comment at the former line 177 stating the enforced policy (IN-01).
- A `throw` post-condition guard inside `degradeToClaim` (WR-03) and a `null`/finding return contract for a missing FA-id in `failedAttemptId` consumed by `checkCarveOut` (WR-02).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — adversarial carve-out cases (mutation, wholly-dropped, multi-same-kind, missing dir, dial-byte-identity)</name>
  <files>scripts/compactor.test.ts</files>
  <read_first>
    - scripts/compactor.test.ts (the file being modified — reuse its helpers EXACTLY: noteText(over) L61-84, goodRawThread(threadDir) L89-105, goodPromotedSet(promotedDir, over) L109-125, runCheck(threadDir, promotedDir, dial) L129-133, freshTmp(prefix)+afterAll L43-51, and the spawnSync CLI idiom; do NOT re-import or restructure — append cases to the existing describe blocks)
    - scripts/compactor.ts (the SUT — confirm the current bypasses you are pinning: checkCarveOut L143-192, the drop-to-empty condition at L181, the null-counterpart skip at L177, findCounterpart L197-211, readNoteDir fail-open L116-125, the CLI check entry L290-330)
    - scripts/context-io.ts (the promotion/admission seam reused by the helpers — appendNote / admit signatures; do not fork a writer)
  </read_first>
  <action>
    Append SIX adversarial cases to scripts/compactor.test.ts, reusing the existing noteText / goodRawThread / goodPromotedSet / runCheck / freshTmp helpers. Each negative case MUST be written so it is RED against the CURRENTLY committed scripts/compactor.js (today's code returns exit 0 / empty findings; the case asserts exit 1 / names the fault). Run the suite at the end of this task and confirm these six are RED (the task's verify proves RED is the expected Wave-0 state for the new cases).

    Add to the "CMP-02 carve-out invariant" describe block:

    1. Title `"mutates verified_by to a forged stamp — refuse, naming verified_by"`. goodRawThread(thread) with the finding's verified_by at its seeded value §14-gate#SEED-001; goodPromotedSet(promoted, { finding: { verified_by: "§14-gate#FORGED-999" } }). Assert runCheck(thread, promoted).status is not 0 AND the combined stdout+stderr contains the field name verified_by. (RED today: L181 only catches drop-to-empty.)

    2. Title `"mutates by to a different non-empty value — refuse, naming by"`. goodRawThread(thread) (finding by: engineer); goodPromotedSet(promoted, { finding: { by: "attacker" } }). Assert status not 0 AND output contains the field name by. (RED today: mutation to a different non-empty value passes.)

    3. Title `"wholly drops a verified finding with 2+ promoted notes — refuse, naming the dropped finding"`. Build raw = { the verified finding (verified_by §14-gate#SEED-001), FA-1 } via goodRawThread. Build promoted as TWO failed-attempt notes only (FA-1 plus a second FA-2), with the verified finding entirely ABSENT — so the promoted set has 2 notes and the finding has no counterpart. Assert status not 0 AND output names the dropped verified finding (contain verified_by or the finding kind in the message). (RED today: L177 continues on a null counterpart; the single-note fallback masks it only at promoted.size===1.)

    4. Title `"drops by on one of two same-kind findings — refuse, naming by"`. Raw thread = TWO findings of the same kind but different by (e.g. by: engineer and by: reviewer), plus FA-1. Promoted = both findings but the reviewer finding has its by line stripped (empty), the engineer finding intact, plus FA-1. Assert status not 0 AND output contains by. (RED today: findCounterpart returns sameKind[0] and borrows the intact engineer finding.)

    Add to the "CMP-01 two-tier separation + sole writer" OR a small new "fail-closed input" describe block:

    5. Title `"CLI check fails closed on a missing threadDir — exit 1, never carve-out intact"`. Point runCheck at a threadDir path that does NOT exist (a freshTmp dir with a non-existent subdir appended) and a valid (possibly empty) promotedDir. Assert status not 0 AND stderr contains an error mentioning the missing thread directory; assert the combined output does NOT contain the success phrase carve-out intact. (RED today: readNoteDir returns an empty map for a missing dir and the CLI prints carve-out intact / exit 0.)

    Add to the "CMP-03 dial behavior" describe block (dial-independence by construction, WR-05):

    6. Title `"carve-out findings are byte-identical across all three dials for the same mutation"`. Construct ONE single-mutation input (reuse case 1's forged-stamp promoted set). For each dial in aggressive / balanced / retain-raw run runCheck and capture the combined stdout+stderr. Assert all three captured outputs are byte-identical to each other (e.g. expect(outBalanced).toBe(outAggressive) and expect(outRetainRaw).toBe(outAggressive)) — NOT merely that each status is non-zero. This pins D-05 by construction so a future dial-sensitive branch in checkCarveOut is caught.

    Keep every fixture inside mkdtempSync temp dirs via freshTmp; the afterAll cleanup already covers them. These are POSITIVE toContain / toBe assertions in test bodies — do NOT add or rely on any file-wide negative-grep gate keyed on the field-name literals.
  </action>
  <verify>
    <automated>npx vitest run scripts/compactor.test.ts -t "mutates verified_by to a forged stamp"; test $? -ne 0 && echo "RED as expected — bypass not yet fixed"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/compactor.test.ts` contains all six new titles (grep confirms: `mutates verified_by to a forged stamp`, `mutates by to a different non-empty value`, `wholly drops a verified finding with 2+ promoted notes`, `drops by on one of two same-kind findings`, `CLI check fails closed on a missing threadDir`, `carve-out findings are byte-identical across all three dials`).
    - Run against the CURRENTLY committed `scripts/compactor.js` (no compactor.ts/.js change yet): the four bypass cases + the missing-dir case FAIL (status 0 / empty findings — proving RED). Capture and record the RED result.
    - The byte-identical-dial case asserts `.toBe()` equality of the combined output across the three dials (not `.not.toBe(0)` on status alone).
    - All fixtures use `freshTmp` / `mkdtempSync`; `git status` shows nothing staged under `.grugops/` after the run.
    - No file-wide negative-grep gate is introduced over `compactor.ts` keyed on the carve-out field-name literals; the new cases are positive in-test assertions.
  </acceptance_criteria>
  <done>Six adversarial cases exist in compactor.test.ts, each demonstrably RED against the committed compactor.js (the four bypass cases + the missing-dir case fail; the dial-byte-identity case is present), establishing the held-out adversarial oracle the fixes must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — fix checkCarveOut + findCounterpart + CLI fail-closed (CR-01, CR-02, CR-03, WR-01, IN-01, IN-02)</name>
  <files>scripts/compactor.ts</files>
  <read_first>
    - scripts/compactor.ts (the SUT being fixed — checkCarveOut L143-192 incl. the provenance loop L178-188 and the null-counterpart skip L177; findCounterpart L197-211; readNoteDir L116-125; the CLI check entry L290-330; the clear-voice header L1-48 to keep consistent voice)
    - scripts/compactor.test.ts (the contract from Task 1 — the six adversarial cases plus the existing 14 cases the fix must keep green)
    - scripts/context-io.ts (the reuse seam — keep promotion via appendNote and re-verify via admit; the fix forks NO writer and adds NO new admission loop; the §14-gate stamp grammar GATE_STAMP_RE L111 for context on what a forged stamp looks like)
  </read_first>
  <action>
    Fix checkCarveOut and findCounterpart in scripts/compactor.ts. CLEAR PROFESSIONAL VOICE throughout (never caveman — this is the safety/trace surface, CLAUDE.md). node:fs only; no LLM call; no summarization.

    CR-01 — refuse mutation, not only drop-to-empty (former L181). Change the provenance-field condition from "raw non-empty AND promoted empty" to "raw non-empty AND raw differs from promoted" — i.e. refuse whenever a load-bearing field present in the raw note is altered at all in the promoted counterpart. The finding message MUST name the field AND distinguish the two cases: when the promoted value is empty, state the field was dropped to empty; when the promoted value is a different non-empty value, state the field was altered to that value (include both the old raw value and the new promoted value in the message). This catches a verified_by swapped to a forged §14-gate#FORGED-999 and a by swapped engineer->attacker.

    CR-02 — refuse a wholly-dropped verified finding (former L177 + the null-counterpart skip). State and enforce the contract explicitly: a durable note carrying a NON-EMPTY verified_by (a §14-gate-verified finding) MUST survive into the promoted set — dropping it entirely is REFUSED. A durable note WITHOUT a verified_by stamp (an unverified claim/observation) MAY be dropped — that is the agent's legitimate body-compression latitude. Implement: build the set of raw notes with a non-empty verified_by, key each by the tuple (kind, verified_by, by, at), build the matching set of keys from the promoted notes, and for each raw verified key not present in the promoted key set, push a finding naming the dropped verified finding (include its kind, verified_by, and by in the message). Do NOT silently continue on a null counterpart for a verified provenance-bearing note. Reword the comment at the former L177 (IN-01) to state this enforced policy — it currently says a wholly-dropped durable note is the agent's call, which is FALSE for verified findings; the new wording must say a verified finding (non-empty verified_by) must survive while an unverified note may be dropped.

    CR-03 — deterministic 1:1 counterpart matching (findCounterpart L197-211). Match a raw note to its promoted counterpart on STABLE identity: prefer the verified_by stamp (which CR-01 now forbids mutating); else fall back to the (kind, at) tuple. NEVER return sameKind[0] when more than one same-kind candidate exists — when no deterministic 1:1 match is found, return null and let the CR-02 existence check handle a dropped verified note (do not borrow an intact sibling's fields). IN-02: the redundant double `return sameKind[0]` collapses once this is fixed — do NOT "clean it up" into a single unconditional sameKind[0] return; replace it with the deterministic match-or-null logic.

    WR-01 — CLI fail-closed on a missing threadDir (CLI check entry, around L312). Before reading the raw thread, check existsSync(threadDir); if the threadDir does not exist, print an error to stderr naming the missing directory and process.exit(1). The promotedDir MAY legitimately be empty pre-promotion — only the raw thread being checked must exist. The success path must NEVER print carve-out intact / exit 0 for a non-existent threadDir.

    Keep checkCarveOut's _dial parameter inert (it must NOT read the dial — the carve-out is un-dialable, D-05). Keep failed-attempt id survival (rule 1) unchanged in behavior except where WR-02 (Task 3) tightens it. Do NOT add string-similarity / set-difference / dedup logic beyond the existing currentState reuse (D-03 — no semantic fold).

    Do NOT run the build or freshness in this task (Task 4 rebuilds compactor.js once all .ts edits land). After editing compactor.ts, run the build locally only to the extent needed to make the spawn-the-.js tests resolve — or defer entirely to Task 4 and run the pure-source assertions; the executor sequences Task 2->3->4 so the single rebuild in Task 4 produces the committed .js.
  </action>
  <verify>
    <automated>npm run build && npx vitest run scripts/compactor.test.ts -t "mutates verified_by to a forged stamp" -t "wholly drops a verified finding with 2+ promoted notes" -t "drops by on one of two same-kind findings" -t "CLI check fails closed on a missing threadDir"</automated>
  </verify>
  <acceptance_criteria>
    - After rebuilding, the four bypass/fail-open cases from Task 1 PASS: forged verified_by refused naming verified_by (CR-01), wholly-dropped verified finding with 2+ promoted refused (CR-02), multi-same-kind dropped by refused naming by (CR-03), missing threadDir exits 1 (WR-01).
    - The `mutates by to a different non-empty value` case PASSES (CR-01 inequality check covers `by`).
    - All 14 pre-existing cases in `scripts/compactor.test.ts` still PASS (no regression) — `npx vitest run scripts/compactor.test.ts` is GREEN.
    - `grep -n 'existsSync(threadDir)' scripts/compactor.ts` (or the chosen guard) confirms the CLI fails closed before reading the raw thread.
    - `findCounterpart` no longer returns `sameKind[0]` when more than one same-kind candidate exists — `grep -c 'sameKind\[0\]' scripts/compactor.ts` (filtered of comment lines via `grep -v '^[[:space:]]*//'`) shows the unconditional double-return is gone.
    - The comment at the former L177 (IN-01) now states the enforced verified-must-survive / unverified-may-drop policy — the contradictory "agent's call" wording is removed.
    - `grep -n 'from "./context-io.js"' scripts/compactor.ts` still matches — the fix forks no writer and adds no admission loop.
  </acceptance_criteria>
  <done>checkCarveOut refuses mutation and wholly-dropped verified findings, findCounterpart matches deterministically 1:1 or returns null (never borrows a sibling), the CLI fails closed on a missing threadDir, the IN-01 comment states the real policy, all six adversarial cases + the 14 pre-existing cases are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HARDEN — fail-closed failedAttemptId (WR-02), degradeToClaim post-conditions (WR-03), dial byte-identity (WR-05)</name>
  <files>scripts/compactor.ts, scripts/compactor.test.ts</files>
  <read_first>
    - scripts/compactor.ts (failedAttemptId L130-138 — the best-effort filename fallback to replace; degradeToClaim L275-284 — the anchored .replace() calls + the out-of-scope comment; checkCarveOut L143-192 — where a missing FA-id finding is surfaced)
    - scripts/compactor.test.ts (the existing "materially-changed degrades to claim" case L409-422 that already asserts degradeToClaim outputs kind: claim + UNKNOWN - verify + no §14-gate stamp — the post-conditions to enforce; the byte-identity case added in Task 1)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-REVIEW.md (WR-02 L185-200, WR-03 L202-222, WR-05 L249-262 — the exact fix snippets)
  </read_first>
  <action>
    Harden the two remaining same-file fail-open paths in scripts/compactor.ts and finalize the dial-identity assertion. CLEAR PROFESSIONAL VOICE.

    WR-02 — failedAttemptId (L130-138): when a failed-attempt note has no recoverable FA-token in body or filename, do NOT return a best-effort filename id (the current final return). Instead surface a missing-FA-id as an explicit carve-out finding. Concretely: change the contract so a missing FA-id is represented as a sentinel (e.g. return null) and have checkCarveOut, when it encounters a failed-attempt raw note whose id is unrecoverable, push a finding stating the failed-attempt note has no recoverable FA-id (name the file). Keep the existing happy path (FA-token from body, then filename) unchanged. Add a test case titled `"failed-attempt with no recoverable FA-id — refuse, naming the unrecoverable note"` (a failed-attempt note whose body and filename carry NO FA- token) asserting status not 0 and the output names the unrecoverable note.

    WR-03 — degradeToClaim (L275-284): after the three anchored .replace() calls, ASSERT the post-conditions and throw on failure — the honest-degrade escape hatch must not silently no-op. Assert: the output kind line became claim; the output confidence line became UNKNOWN - verify; and the output carries NO residual §14-gate provenance stamp on verified_by. If any post-condition fails, throw an Error stating the input did not match the finding template (so the caller cannot believe it degraded when it did not). Add a test case titled `"degradeToClaim throws on a non-finding-template input"` passing a note that lacks a confidence line (or whose kind is not finding) and asserting degradeToClaim throws — RED today (it silently returns un-degraded text), GREEN after the throw guard. Keep the existing "materially-changed degrades to claim" case green.

    WR-05 — the byte-identity assertion was added in Task 1 (the `carve-out findings are byte-identical across all three dials` case). Confirm it is GREEN after Task 2's fixes — checkCarveOut's _dial stays inert, so the same single-mutation input yields byte-identical findings text at aggressive / balanced / retain-raw. No production change is required for WR-05 beyond keeping _dial inert; this task only confirms the assertion holds and the dial branch is never wired into the comparison.

    Do NOT run the build/freshness here — Task 4 owns the single rebuild + commit of compactor.js.
  </action>
  <verify>
    <automated>npm run build && npx vitest run scripts/compactor.test.ts -t "failed-attempt with no recoverable FA-id" -t "degradeToClaim throws on a non-finding-template input" -t "carve-out findings are byte-identical across all three dials"</automated>
  </verify>
  <acceptance_criteria>
    - `failedAttemptId` no longer returns a best-effort filename id for a missing FA-token — `checkCarveOut` surfaces an explicit finding, and the `failed-attempt with no recoverable FA-id` case PASSES (status not 0, names the unrecoverable note).
    - `degradeToClaim` throws when its input does not match the finding template — the `degradeToClaim throws on a non-finding-template input` case PASSES, and the pre-existing `materially-changed degrades to claim` case still PASSES (valid template still degrades cleanly with no residual `§14-gate#` stamp).
    - The `carve-out findings are byte-identical across all three dials` case PASSES — the carve-out output is identical text at all three dial values (D-05 asserted by construction, WR-05).
    - `checkCarveOut`'s `_dial` parameter is still never read — `grep -n '_dial' scripts/compactor.ts` shows it only in the signature, never in a branch/comparison.
    - Full suite GREEN: `npx vitest run scripts/compactor.test.ts` exits 0.
  </acceptance_criteria>
  <done>failedAttemptId fails closed on a missing FA-id (surfaced as a named finding), degradeToClaim throws rather than silently returning an un-degraded note, the dial-byte-identity assertion is green, and the full suite passes.</done>
</task>

<task type="auto">
  <name>Task 4: REBUILD + FRESHNESS — regenerate committed compactor.js and prove byte-fresh</name>
  <files>scripts/compactor.js</files>
  <read_first>
    - package.json (the script names — build = tsc; freshness = tsc --outDir .tmp-build && node scripts/freshness.js; test = vitest run)
    - scripts/freshness.ts (the drift gate — collectJs() auto-globs scripts/*.js; rebuild-to-temp + byte-diff; compactor.js auto-joins with no edit to freshness.ts)
    - scripts/compactor.ts (the source the committed .js must faithfully build from after Tasks 2-3)
  </read_first>
  <action>
    Regenerate the committed scripts/compactor.js from the hardened scripts/compactor.ts and prove it is byte-fresh. Run `npm run build` (tsc) to emit the updated scripts/compactor.js. Then run `npm run freshness` and confirm it exits 0 — the committed .js is a byte-faithful build of the .ts (D-13). Stage and include the regenerated scripts/compactor.js in the same change as the compactor.ts edits.

    Do NOT edit freshness.ts (compactor.js auto-discovers via collectJs). Do NOT run the live e2e lane — use the regression lane only.

    Then run the full regression suite excluding the live e2e lane to confirm no collateral regression across the tooling: `npx vitest run --exclude '**/scripts/e2e/**'`.
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` regenerates `scripts/compactor.js` from the hardened `scripts/compactor.ts` with no errors.
    - `npm run freshness` exits 0 — `scripts/compactor.js` is byte-fresh vs `scripts/compactor.ts` (D-13). This is REQUIRED completion evidence, not optional.
    - `npx vitest run --exclude '**/scripts/e2e/**'` is GREEN — the full regression lane passes (no collateral regression), and the six adversarial CMP-02 cases are among the passing tests.
    - The regenerated `scripts/compactor.js` is staged for commit alongside `scripts/compactor.ts` and `scripts/compactor.test.ts`.
  </acceptance_criteria>
  <done>compactor.js is rebuilt from the hardened compactor.ts, npm run freshness exits 0, the regression lane is green, and the byte-fresh .js is committed with the source — CMP-02 is now enforced and provable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw trajectory note → promoted note | Untrusted free-form agent narrative is distilled and promoted into the committed shared verified context; `checkCarveOut` is the integrity boundary that no load-bearing provenance field is dropped or altered on the way. |
| promoted `verified_by` stamp → gate provenance | A `§14-gate#<id>` stamp asserts a live green §14-gate verdict; the carve-out is what prevents a stamp from being silently swapped or a verified finding from being silently deleted. |
| CLI `<threadDir>` argument → the oracle | A caller-supplied path drives the safety check; a missing/typo'd path must not turn the oracle into a rubber stamp. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | Tampering | `checkCarveOut` provenance comparison (CR-01) | mitigate | The provenance-field check refuses ANY alteration of a non-empty load-bearing field (drop-to-empty AND mutation-to-different-value), including a `verified_by` swapped to a forged `§14-gate#FORGED-999` and a `by` swapped engineer→attacker. Proven by the `mutates verified_by to a forged stamp` and `mutates by` cases (RED before, GREEN after). |
| T-22-07 | Repudiation | `checkCarveOut` wholly-dropped verified finding (CR-02) | mitigate | A §14-gate-verified finding (non-empty `verified_by`) wholly deleted from the promoted set is refused by an affirmative existence check keyed on `(kind, verified_by, by, at)` — a gate verdict cannot be silently repudiated by deleting its finding. Proven by the `wholly drops a verified finding with 2+ promoted notes` case (RED before, GREEN after). |
| T-22-08 | Tampering | `findCounterpart` ambiguous borrowing (CR-03) | mitigate | Counterparts match deterministically 1:1 on `verified_by` else `(kind, at)`; never `sameKind[0]` when >1 candidate — a dropped field on one of N same-kind notes can no longer be masked by an intact sibling. Proven by the `drops by on one of two same-kind findings` case. |
| T-22-09 | Denial of Service (availability of the control) | CLI `check` missing `threadDir` (WR-01) | mitigate | The CLI fails closed: a non-existent `threadDir` exits 1 with a named error and never prints `carve-out intact` — an operator typo cannot disable the safety oracle. Proven by the `CLI check fails closed on a missing threadDir` case. |
| T-22-10 | Tampering / Repudiation | `failedAttemptId` + `degradeToClaim` fail-open (WR-02/WR-03) | mitigate | A failed-attempt with no recoverable FA-id is surfaced as an explicit carve-out finding (not a silent best-effort id); `degradeToClaim` throws if the post-conditions (kind=claim, confidence=UNKNOWN - verify, no residual `§14-gate#` stamp) do not hold — the honest-degrade hatch cannot silently no-op. Proven by the two new hardening cases. |
| T-22-SC | Tampering | npm/dev installs | accept | This gap-closure plan installs ZERO external packages (zero-host-runtime-dep, D-13/D-15) — `node:fs` only. No `[ASSUMED]`/`[SUS]` package, so no install checkpoint is required (RESEARCH.md Package Legitimacy Audit: not applicable — zero external packages). |
</threat_model>

<verification>
- `npx vitest run scripts/compactor.test.ts` — GREEN (the six new adversarial cases + the 14 pre-existing cases all pass).
- The three original bypass reproductions return exit 1 / non-empty findings via named tests proven RED before the fix (Task 1 records the RED result) and GREEN after (CR-01 forged stamp, CR-02 wholly-dropped finding with 2+ promoted, CR-03 multi-same-kind drop).
- `npm run build && npm run freshness` — GREEN (compactor.js byte-fresh vs compactor.ts, D-13) — REQUIRED completion evidence, not "N tests pass."
- `npx vitest run --exclude '**/scripts/e2e/**'` — full regression lane GREEN (no live e2e lane spend per project memory).
- `grep`-level structural checks: the CLI guards a missing threadDir; findCounterpart no longer unconditionally returns sameKind[0]; the IN-01 comment states the enforced verified-must-survive policy; `_dial` is never read.
</verification>

<success_criteria>
- CMP-02 / SC2 holds against held-out adversarial inputs: forged/mutated `verified_by`, mutated `by`, a wholly-dropped verified finding with 2+ promoted notes, and a multi-same-kind dropped field are each REFUSED (exit 1, named) — each proven by a test demonstrably RED before the fix and GREEN after.
- The carve-out fails CLOSED: a missing `threadDir` exits 1; a missing FA-id is a named finding; `degradeToClaim` throws rather than silently no-op.
- The carve-out is un-dialable by construction: `checkCarveOut` findings are byte-identical across all three dials for the same mutation (WR-05, D-05).
- `npm run freshness` exits 0 — the committed `compactor.js` is a byte-faithful build of the hardened `compactor.ts` (D-13).
- CMP-01 and CMP-03 are untouched and remain SATISFIED (no edits to plans 22-01 / 22-02 artifacts beyond the shared compactor.ts/.js/.test.ts files this plan owns).
</success_criteria>

<output>
Create `.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-03-SUMMARY.md` when done.
</output>

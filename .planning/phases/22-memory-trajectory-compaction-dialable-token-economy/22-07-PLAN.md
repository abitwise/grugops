---
phase: 22-memory-trajectory-compaction-dialable-token-economy
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/compactor.ts
  - scripts/compactor.js
  - scripts/compactor.test.ts
autonomous: true
gap_closure: true
requirements: [CMP-02]
# CMP-01 and CMP-03 are verified and unchanged (plans 22-01/22-02); this plan touches ONLY the
# CMP-02 load-bearing-field carve-out. It closes the 5th distinct CMP-02 bypass (MULTI-NOTE THREAD
# FILE) by making the carve-out READ PATH recover EXACTLY the per-note set the WRITE PATH emitted —
# same note count, same frozen ids, same verbatim bytes — or fail closed. Read-path-only fix: the
# single multi-note threads/<agent>.md file is the INTENDED representation (D-08, gitignored); the
# write path is NOT altered. Tier A closes the multi-note CLASS; Tier B closes IN-01 (the id-format
# drift hazard that defeats the id-keyed survival match). Round 6.

must_haves:
  truths:
    # CMP-02 is fundamentally a PROHIBITION. The decisive truth is the CLASS-level round-trip invariant.
    - "CLASS INVARIANT: the carve-out read path (readNoteDir via splitNotes) recovers EXACTLY the per-note set the write path (writeThread/composeThreadNote) emitted into a single threads/<agent>.md file — same note count, same frozen ids, same verbatim bytes — or fails closed. A load-bearing field (a §14-gate-verified finding, or a required failed-attempt) buried as note #2+ in a multi-note thread file causes `node scripts/compactor.js check` to exit 1 naming the dropped id; it can no longer be silently swallowed into note #1's body."
    - "Round-trip: notes written via two or more `writeThread` calls into one threads/<agent>.md are recovered by `readNoteDir` as exactly that id-set (same count, same frozen ids) — proven by an explicit RED→GREEN test against the committed .js, not a unit-green claim."
    - "Body-byte round-trip (BODY-CONSUMING splitter): `splitNotes(<writeThread×2 output>).notes.length === 2`, each element's `parseNote(...).body` equals that note's non-empty authored body, and `notes.join('') + (trailingMalformed ?? '')` reproduces the CRLF-normalized file byte-for-byte. The promoted set must preserve each note's body byte-for-byte, and `runCheck` exit 0 REQUIRES body survival — a body-stripping or note-#2-swallowing splitter (a frontmatter-only matcher) cannot pass the round-trip."
    - "splitNotes and parseNote share ONE recognized-line set (a single source-of-truth boundary grammar, IN-02): a note BOUNDARY is a column-0 `---\\n` immediately followed by a recognized frontmatter line (the SAME `key: value`/`refs:` set parseNote recognizes inside a fence); a note's TEXT runs boundary→next-boundary|EOF so it INCLUDES its body. splitNotes cannot drift from parseNote, so a note splitNotes carves out is exactly a note parseNote then parses (body included); if they could drift, that would be the 6th bypass."
    - "Body `---` is not a boundary: a body containing a lone `---` horizontal rule OR an embedded `---…---` block neither spawns a spurious note nor hides a following real note — a buried §14-gate-verified note #2 after such a body is still recovered and its drop refused (exit 1). Pinned by a test."
    - "Fail closed, never silent: a trailing non-blank, non-fence remainder in a thread file (e.g. fence-then-free-scratch via the no-note writeThread path, WR-01) is surfaced as `trailingMalformed` and routed into the existing NoteDirResult.unparseable fail-closed channel — the CLI refuses (exit 1) naming the file, never a dropped note."
    - "IN-01 closed: `composeThreadNote` reuses the single exported `noteId` (or shared id helper) from context-io.ts instead of re-implementing the id formula inline, so a thread note's frozen id CANNOT drift from the promoted-counterpart id format that the id-keyed survival match depends on."
    - "The bypass is reproducible RED (exit 0 'carve-out intact') against the CURRENT committed scripts/compactor.js and GREEN (exit 1 naming the dropped id) only after the fix — proven by an explicit build+run captured in 22-07-RED-baseline.txt and 22-07-GREEN-proof.txt."
  artifacts:
    - path: "scripts/context-io.ts"
      provides: "exported splitNotes(text) sharing parseNote's fence grammar; exported noteId (or shared id helper) reused by composeThreadNote"
      contains: "export function splitNotes"
    - path: "scripts/context-io.js"
      provides: "Byte-fresh tsc build of context-io.ts (D-13)"
      contains: "splitNotes"
    - path: "scripts/compactor.ts"
      provides: "readNoteDir iterates splitNotes(fileText).notes — one record per note keyed by <file>#<n>, gates + survival run PER NOTE, trailingMalformed routed to NoteDirResult.unparseable; composeThreadNote reuses noteId"
      contains: "splitNotes"
    - path: "scripts/compactor.js"
      provides: "Byte-fresh tsc build of compactor.ts (D-13)"
      contains: "splitNotes"
    - path: "scripts/compactor.test.ts"
      provides: "Held-out RED-first multi-note describe block (FIVE tests) — buried verified finding dropped, buried failed-attempt dropped, fence-then-scratch fail-closed, round-trip writeThread→readNoteDir id-set+BODY-BYTE recovery, and body-`---` ambiguity (lone rule + embedded `---…---` block before a real note #2 — body `---` neither spawns nor hides a note)"
      contains: "multi-note"
    - path: "scripts/context-io.test.ts"
      provides: "splitNotes unit assertions — multi-fence split into verbatim per-note bytes INCLUDING bodies (parseNote(element).body == authored body), join byte round-trip, trailingMalformed surfaced, single-fence yields one note, body-`---` ambiguity (lone rule + embedded `---…---` block splits into exactly 2 notes not 3+), shared-grammar (splitNotes∘parse == parseNote) proof"
      contains: "splitNotes"
    - path: ".planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-07-RED-baseline.txt"
      provides: "RED evidence: pre-fix exit 0 on the buried-note fixture + the new multi-note tests failing against the committed .js"
      contains: "carve-out intact"
    - path: ".planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-07-GREEN-proof.txt"
      provides: "GREEN evidence: post-fix exit 1 naming the dropped id + full non-e2e suite green + npm run freshness exit 0"
      contains: "carve-out FAIL"
  key_links:
    - from: "scripts/compactor.ts (readNoteDir)"
      to: "scripts/context-io.ts (splitNotes)"
      via: "readNoteDir iterates splitNotes(fileText).notes — one NoteFields record per note in a multi-fence file, keyed by <file>#<n>"
      pattern: "splitNotes\\("
    - from: "scripts/context-io.ts (splitNotes)"
      to: "scripts/context-io.ts (parseNote recognized-line set)"
      via: "a note boundary (column-0 `---` immediately followed by a recognized frontmatter line) reuses the SAME recognized `key: value`/`refs:` set parseNote uses (single source, IN-02); a note's text is boundary→next-boundary|EOF so it includes its body — a carved note equals a parsed note, body included"
      pattern: "splitNotes"
    - from: "scripts/compactor.ts (composeThreadNote)"
      to: "scripts/context-io.ts (noteId)"
      via: "composeThreadNote calls the exported noteId instead of re-implementing the id formula inline (IN-01)"
      pattern: "noteId\\("
    - from: "scripts/compactor.ts (readNoteDir)"
      to: "scripts/compactor.ts (checkCarveOut NoteDirResult.unparseable)"
      via: "a trailingMalformed remainder is pushed into NoteDirResult.unparseable, which checkCarveOut already surfaces as a fail-closed finding"
      pattern: "unparseable"
---

<objective>
Close the 5th distinct CMP-02 bypass (MULTI-NOTE THREAD FILE) by making the carve-out READ PATH
recover exactly the per-note set the WRITE PATH emitted. The production raw-thread representation is
a SINGLE `threads/<agent>.md` file (D-08, gitignored) that `writeThread`/`composeThreadNote`
(compactor.ts:517-564) builds by APPENDING each note as a `---…---` fence. But the read path
`readNoteDir` (compactor.ts:160-174) calls `readNoteFields`→`parseNote` ONCE per `.md` file, and
`parseNote`'s non-greedy fence regex (`/^---\n([\s\S]*?)\n---\n?([\s\S]*)$/`, context-io.ts:204)
matches only the FIRST fence — every note after the first is folded into note #1's `body` and is
invisible to BOTH round-5 gates, the byte-equal loop, AND the required-survival set. A §14-gate-
verified finding or an unconditionally-required failed-attempt buried as note #2+ is silently dropped
from the promoted set at exit 0 "carve-out intact".

This is the SAME family as rounds 1–5: a real on-disk byte present in the raw thread, projected or
truncated away by the read path before any survival decision keys on it. Round 6 closes the CLASS,
not the multi-note instance. The durable invariant to express and TEST: the carve-out read path must
recover EXACTLY the per-note set the write path emitted — same note count, same frozen ids, same
verbatim bytes — or fail closed.

The fix is READ-PATH-ONLY (the multi-note file is the intended D-08 representation; do NOT change the
writer), and consists of:
  1. A shared `splitNotes(text)` exported from context-io.ts — a BODY-CONSUMING splitter that carves a
     multi-note file into each note's VERBATIM byte string INCLUDING its body. A note BOUNDARY is a
     column-0 `---` line immediately followed by a recognized frontmatter line (the SAME recognized-line
     set parseNote uses, IN-02 single-source); a note's text runs boundary→next-boundary|EOF. Contract:
     `parseNote(notes[i]).body` equals the authored body and `notes.join('')+(trailingMalformed??'')`
     round-trips byte-for-byte. A trailing non-blank, non-boundary remainder is surfaced as
     `trailingMalformed`. (A frontmatter-only matcher that stops at a note's closing fence strips bodies
     and swallows note #2 — that is the 6th bypass and is explicitly disallowed.)
  2. `readNoteDir` iterates `splitNotes(fileText).notes`, keys each note by `<file>#<n>`, runs BOTH
     round-5 gates + the byte-equal/required-survival logic PER NOTE, and routes any `trailingMalformed`
     remainder into the existing `NoteDirResult.unparseable` fail-closed channel (WR-01 folds in: a
     fence-then-free-scratch file fails closed). Applied uniformly to raw and promoted sides.
  3. IN-01: export `noteId` from context-io.ts and have `composeThreadNote` reuse it, so a thread
     note's frozen id cannot drift from the promoted-counterpart id format the id-keyed match depends on.

Purpose: CMP-02 / SC2 is the un-cheatable mechanical floor of the body/frontmatter seam. A green unit
suite is necessary but NOT sufficient — proof is RED→GREEN reproduction against the COMMITTED .js, and
a CLASS-level round-trip invariant test, not just one multi-note fixture.
Output: a shared splitNotes + a per-note read path + an id-unified composeThreadNote, byte-fresh
committed .js, and a held-out RED-first multi-note + round-trip test corpus that pins the class, with
captured build+run evidence.

OUT OF SCOPE (deferred to follow-up rounds — do NOT plan or implement these here, so the executor and
verifier see the decision):
- WR-02 (readContext fail-OPEN on the broader context read — `if (!parsed) continue;`).
- WR-03 (readContext id/filename divergence signal + code/comment disagreement).
- IN-02 broader (unknown-key allowlist / typo-laundering class in validate()).
These are real but are NOT the multi-note read-path class and are not the id-drift hazard; they belong
to a later round.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-VERIFICATION.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-REVIEW.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-CONTEXT.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-06-SUMMARY.md
@scripts/context-io.ts
@scripts/compactor.ts
</context>

<artifacts_this_phase_produces>
NEW symbols / test surfaces introduced by this plan (so the executor and downstream verifier can
enumerate them):

- `export function splitNotes(text: string): { notes: string[]; trailingMalformed: string | null }`
  — NEW exported function in context-io.ts. A BODY-CONSUMING splitter: it carves a multi-note file into
  each note's VERBATIM byte string INCLUDING its body, by walking note BOUNDARIES (a column-0 `---`
  immediately followed by a recognized frontmatter line — the SAME recognized-line set parseNote uses)
  from one boundary to the next | EOF. Each element parses standalone via parseNote AND
  `parseNote(element).body` equals that note's authored body; `notes.join('')+(trailingMalformed??'')`
  reproduces the normalized input byte-for-byte. A trailing non-blank, non-boundary remainder is returned
  as `trailingMalformed` (caller fails closed). A single-fence file yields exactly one note (body intact);
  an all-scratch file yields zero notes + a trailingMalformed.
- `noteId` becomes EXPORTED from context-io.ts (currently a private function at context-io.ts:416-420);
  `composeThreadNote` (compactor.ts:547-548) reuses it instead of its inline copy (IN-01). No formula
  change — only the visibility of the declaration and the call site.
- `splitNotes` is imported into compactor.ts and consumed by `readNoteDir`; `noteId` is imported and
  consumed by `composeThreadNote`. `checkCarveOut`, `parseNote`, `validate`, `writeThread` keep their
  signatures.
- New / extended test describe-blocks:
  - compactor.test.ts: a new describe block "compactor.js — CMP-02 round-6 multi-note thread file
    (held-out RED-first)" containing FIVE tests: buried verified finding dropped (refuse), buried
    failed-attempt dropped (refuse), fence-then-free-scratch fail-closed (refuse naming the file), the
    round-trip invariant (writeThread×2 → readNoteDir recovers the exact id-set AND body bytes; a
    body-corruption sibling assertion), and the body-`---` ambiguity probe (note #1's body holds a lone
    `---` rule + an embedded `---…---` block before a real note #2; the body `---` neither spawns a
    spurious note nor hides note #2, whose drop is refused naming its id).
  - context-io.test.ts: a new describe block "context-io.js — splitNotes multi-fence split (shared
    grammar, IN-02)" asserting verbatim per-note split INCLUDING bodies (`parseNote(element).body` ==
    authored body), the `notes.join('')+(trailingMalformed??'')` byte round-trip, trailingMalformed
    surfacing, single-fence ⇒ one note (body intact), the body-`---` ambiguity (a body lone `---` +
    embedded `---…---` block splits into exactly 2 notes not 3+, note[0].body keeps the `---` verbatim),
    and the shared-grammar proof (each split element parses standalone via parseNote to the expected
    scalars AND bodies).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Capture the RED baseline — reproduce the multi-note bypass as an exit-0 drop against the CURRENT committed compactor.js, and author the failing round-6 tests</name>
  <files>scripts/compactor.test.ts, .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-07-RED-baseline.txt</files>
  <read_first>
    - scripts/compactor.test.ts (the existing helpers `noteText`@61, `goodRawThread`@91, `runCheck`@133, `freshTmp`@44; the CMP-01 two-tier block @720 showing `mod.writeThread` usage @729; the round-5 line-shape describe block @1280 is the structural model for the new block)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-REVIEW.md (CR-01 @68-200 — the two-fence engineer.md fixture @102-129 with observation note #1 + §14-gate-verified finding note #2, the promoted set keeping ONLY note #1 @134-148, and the verified exit 0 reproduction @150-156; the failed-attempt-as-note-#2 variant @158-160; the `splitNotes` reference sketch @174-189)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-VERIFICATION.md (the reproduction transcript @163-189 and the `missing:` list @96-116 prescribing the splitNotes shape, per-note iteration keyed by frozen id or <file>#<n>, and the RED-first held-out test)
    - scripts/compactor.ts (`writeThread`/`composeThreadNote`@517-564 — confirm two writeThread calls with a `note` arg produce ONE threads/<agent>.md with two fences; `readNoteDir`@160-174 keying by filename; the CLI `check` verb@625-666 reading both dirs via readNoteDir)
    - scripts/context-io.ts (`parseNote`@197-259, the non-greedy fence regex@204 — this is WHY notes #2+ are swallowed into note #1's body)
  </read_first>
  <action>
    Add a new describe block to scripts/compactor.test.ts titled "compactor.js — CMP-02 round-6
    multi-note thread file (held-out RED-first)". Author FIVE tests. Build the multi-note raw thread
    via the REAL `mod.writeThread` (≥2 calls into the same task+agent, so the corpus exercises the
    production multi-note shape — this is the structural fix to the 5-round test blindness) for the
    cases where writeThread can produce the shape; use a literal two-fence file only where a precise
    byte layout is required. Each test drives the committed CLI via the existing `runCheck(threadDir,
    promotedDir)` helper. Mark all five with a code comment that they are RED-first: they MUST fail
    against the CURRENT committed scripts/compactor.js (the bypass is live) and pass only after Task 2's fix.

    1. BURIED VERIFIED FINDING DROPPED (the CR-01 reproduction):
       - Build the raw thread by calling `mod.writeThread(task, agent, body, contextRoot, note)` twice
         into the same task+agent: first a soft `observation` note, then a §14-gate-verified `finding`
         note (verified_by `§14-gate#RUN7`, confidence high). Confirm (assert) the resulting
         threads/<agent>.md contains TWO note fences (e.g. count `^---$` lines == 4, or assert the
         file text includes both note bodies).
       - Build the promoted set as a notes-style dir containing ONLY the first observation note (the
         verified finding entirely absent). Note: the promoted note must carry the SAME frozen `id:`
         that writeThread stamped on the observation, so the read path matches it 1:1 — read the
         stamped id back out of the thread file (or use the round-trip helper from test 4) to author
         a byte-faithful promoted observation. The verified finding's id must be in the raw thread and
         absent from the promoted set.
       - Assert the carve-out REFUSES: `expect(r.status).not.toBe(0)` and the output names the dropped
         verified finding's id (the §14-gate finding's frozen id) — e.g.
         `expect(\`${r.stdout}${r.stderr}\`).toContain(<finding-id>)`.
    2. BURIED FAILED-ATTEMPT DROPPED (the FA variant): identical structure, but note #2 is a
       `failed-attempt` (verified_by empty, body carrying its FA token). The promoted set keeps only
       note #1. Assert refusal and that the dropped failed-attempt id is named.
    3. FENCE-THEN-FREE-SCRATCH FAIL-CLOSED (WR-01): build a thread file where `mod.writeThread` is
       called once WITH a `note` (a fenced verified finding) and once WITHOUT a `note` arg (free
       scratch — `mod.writeThread(task, agent, scratchBody, contextRoot)` with no 5th argument), so the
       file is `<fence><raw scratch>`. The promoted set keeps the fenced finding. Assert the carve-out
       REFUSES (exit 1) and NAMES the thread file (the trailing scratch is an unparseable remainder
       that must fail closed, never a silent drop). RED-first: against the committed code the scratch
       is folded into the finding's body and the run may exit 0 — assert exit≠0 so the test is RED now.
    4. ROUND-TRIP INVARIANT (count + frozen ids + BODY BYTES): call `mod.writeThread` twice with
       distinct provenance notes (each with a non-empty, distinct body) into one task+agent, then call
       the read path on that threads dir and assert it recovers EXACTLY two notes with the two stamped
       frozen ids (same count, same ids). Drive this through the CLI by constructing a promoted set that
       faithfully preserves BOTH stamped ids AND each note's BODY byte-for-byte, and assert `runCheck`
       exits 0 (the read path saw both notes, both bodies intact, both survived). Sibling assertions:
       (a) dropping EITHER note from the promoted set makes `runCheck` exit 1 naming the dropped id;
       (b) CORRUPTING/TRUNCATING note #1's BODY in the promoted set (while keeping its id) makes
       `runCheck` exit 1 — so a subtly-wrong splitter that preserves id count but strips or mangles a
       body cannot pass the round-trip. (This is the CLASS invariant: count + frozen ids + body bytes
       recovered, or fail closed.)
    5. BODY CONTAINING `---` (the natural 6th-bypass probe — a body `---` must NOT terminate note #1
       early or hide a later note): build (via real `mod.writeThread` where the body can carry it, or a
       literal file where a precise byte layout is required) a thread where note #1's BODY contains BOTH
       a lone `---` line (a markdown horizontal rule) AND an embedded `---\nkey: value\n---` block,
       FOLLOWED by a real provenance-bearing note #2 — a §14-gate-verified `finding` (verified_by
       `§14-gate#…`, confidence high). Assert: (i) the read path recovers EXACTLY the authored note
       count (the body `---`/embedded block does NOT spawn extra notes and does NOT terminate note #1
       early — it is body bytes, not a boundary); (ii) note #2's frozen id is recovered and enters the
       required-survival set — drop note #2 from the promoted set and assert `runCheck` exits 1 NAMING
       that id. RED-first against the committed .js (the first-fence-only read folds everything after
       note #1's fence into note #1's body, so note #2 is invisible and its drop is NOT caught).

    Construct any literal multi-fence file as a template string; do not route a buried note through a
    helper that writes one-note-per-file (the single-note-per-file corpus is precisely what hid this
    class for five rounds). Do NOT modify any .ts source in this task.

    Then capture the RED baseline as committed evidence. Run the four new tests against the CURRENT
    committed scripts/compactor.js (do NOT rebuild) and record that they FAIL (the asserted exit-1
    does not yet hold — the bypass is live). Save the raw command output into a scratch evidence file
    `.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-07-RED-baseline.txt`
    capturing: the exact `npx vitest run` command targeting only the new block, its failing output, and
    a direct `node scripts/compactor.js check <thread> <promoted>` invocation on the buried-verified-
    finding fixture showing exit 0 + "carve-out intact" against the committed .js. This file is the
    non-inferable backstop the verifier requires — it is the proof the bypass was live before the fix.
  </action>
  <verify>
    <automated>npx vitest run scripts/compactor.test.ts -t "round-6 multi-note" 2>&1 | tee -a .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-07-RED-baseline.txt; test -s .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-07-RED-baseline.txt</automated>
  </verify>
  <done>
    The FIVE round-6 multi-note tests exist in the new describe block: buried verified finding dropped,
    buried failed-attempt dropped, fence-then-free-scratch fail-closed, round-trip (count + frozen ids +
    BODY BYTES, with a body-corruption sibling assertion), and BODY CONTAINING `---` (lone rule +
    embedded `---…---` block before a real note #2). Run against the CURRENT committed compactor.js they
    FAIL (the bypass is live: the CLI returns exit 0 / "carve-out intact" on at least the
    buried-verified-finding and the body-`---` fixtures). 22-07-RED-baseline.txt records the failing
    vitest output AND a direct `node scripts/compactor.js check` exit-0 invocation on that fixture. The
    raw thread is built via the REAL `mod.writeThread` (≥2 calls) so the corpus exercises the production
    multi-note shape. No .ts source was edited.
  </done>
</task>

<task type="auto">
  <name>Task 2: Land the read-path fix — shared splitNotes(text) + per-note readNoteDir + trailingMalformed fail-closed</name>
  <files>scripts/context-io.ts, scripts/compactor.ts</files>
  <read_first>
    - scripts/context-io.ts (`parseNote`@197-259 — the CRLF normalize@203, the fence regex@204, the `ParsedFrontmatter` interface@172-191; the EXPORTED `parseNote` declaration@197 and its IN-02 contract comment@193-196; note `parseNote` already normalizes CRLF before matching)
    - scripts/compactor.ts (the imports@57-66; `NoteFields` interface@102-124 incl. `text`@123 and `malformedLines`@119; `readNoteFields`@129-147; `readNoteDir`@160-174 returning `NoteDirResult{notes,unparseable}`@153-156; `checkCarveOut`@200-490 — the unparseable fail-closed blocks@211-222 which already surface a fail-closed finding per unparseable filename; the promoted-by-id map@339-356 and raw-side collision guard@358-376 which key on the frozen `id`)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-REVIEW.md (the `splitNotes` reference sketch @174-189 — treat as the reference SHAPE, but unify the fence with parseNote rather than copying the literal regex)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-CONTEXT.md (D-08 single multi-note thread file is INTENDED; IN-02 single-source-parser principle; D-07 threads/ gitignored)
  </read_first>
  <action>
    Implement the read-path fix in TWO coordinated edits. Use CLEAR professional voice in all comments
    (CLAUDE.md safety-surface rule). Cite CMP-02 in every new fail-closed message. READ-PATH-ONLY: do
    NOT change `writeThread` or `composeThreadNote`'s representation (the single multi-note file is the
    intended D-08 shape) — fix the read path so it splits the file back.

    EDIT A — context-io.ts: add the shared `splitNotes` exported function as a BODY-CONSUMING splitter
    that SINGLE-SOURCES its notion of a note boundary with parseNote's recognized-line set.

    CRITICAL — a note's text INCLUDES its body, not just its frontmatter fence. composeThreadNote
    (compactor.ts@547-564) emits each note as `---\n<frontmatter>\n---\n\n<body>\n`, so a real two-note
    threads/<agent>.md file is `---\n<fm1>\n---\n\n<body1>\n---\n<fm2>\n---\n\n<body2>\n`. A
    frontmatter-ONLY matcher (a regex that stops at the first `\n---\n` after the opening fence) is the
    6th bypass: it (a) strips `<body1>` from note[0], and (b) leaves a remainder starting with
    `\n<body1>…` that does not begin a fence, so the loop halts and note #2 is swallowed into
    trailingMalformed. It fails CLOSED on EVERY legitimate multi-note file. Do NOT prescribe such a
    regex; the executor derives the implementation under TDD from the CONTRACT below.

    Define `splitNotes` against this BOUNDARY/TEXT contract (describe the semantics — do NOT hand the
    executor a regex to copy):
    - NOTE BOUNDARY: the START of a frontmatter block — a column-0 `---\n` line that is IMMEDIATELY
      FOLLOWED by a recognized frontmatter line (a `key: value` scalar or the `refs:` block header —
      i.e. exactly what parseNote recognizes INSIDE a fence, per context-io.ts@224-235). A bare `---`
      line that appears in a body (a markdown horizontal rule) is NOT a boundary, because it is not
      followed by a frontmatter `key: value`/`refs:` line. This is the single-source guarantee: the
      boundary's notion of "a valid frontmatter start" reuses the SAME recognized-line set parseNote
      uses — do NOT invent a second grammar.
    - NOTE TEXT: a note's text runs from one boundary UP TO (but not including) the NEXT boundary, or
      EOF — so each note's text INCLUDES its body (the body-consuming part; this is what the buggy
      frontmatter-only matcher gets wrong).
    - `export function splitNotes(text: string): { notes: string[]; trailingMalformed: string | null }`:
      normalize CRLF/CR to LF FIRST (mirror parseNote@203 — so a CRLF multi-note file splits identically
      to its LF form). Walk the text boundary-to-boundary, pushing each note's VERBATIM byte slice
      (boundary → next boundary | EOF) into `notes`. `trailingMalformed` is any trailing region AFTER
      the last recognized note that is non-blank and is NOT itself a valid note boundary (e.g. free
      scratch from the no-`note` writeThread path, WR-01). If the trailing region is empty-after-trim,
      `trailingMalformed = null`.
    - CONTRACT PROPERTIES the implementation MUST satisfy (the single-source / no-drift / no-byte-loss
      guarantees, IN-02):
      1. For each element, `parseNote(notes[i])` is NON-NULL, AND `parseNote(notes[i]).body` equals
         that note's authored body (the splitter does NOT strip bodies — a carved note equals a parsed
         note, body included).
      2. `splitNotes(text).notes.join('') + (trailingMalformed ?? '')` reproduces the CRLF-normalized
         input BYTE-FOR-BYTE — no byte invented, none dropped.
      Reference SHAPE is 22-REVIEW.md@174-189, but that sketch is a frontmatter-only matcher and is
      BUGGY for notes with bodies — treat it as a directional hint only and satisfy the contract above
      instead of copying its regex.
    - Do NOT change parseNote's observable behavior: a single-fence file still parses exactly as before,
      and `splitNotes(single-fence).notes.length === 1` with its body intact.

    EDIT B — compactor.ts: make `readNoteDir` read PER NOTE via splitNotes, and route a trailing
    remainder to the fail-closed channel:
    - Import `splitNotes` from `./context-io.js` (add it to the existing import block@57-66).
    - Rewrite `readNoteDir`@160-174: for each `.md` file, read its full text, call
      `splitNotes(fileText)`, and for EACH element of `.notes` call `readNoteFields(noteText)` and key
      the resulting record by `<file>#<n>` (n = 0-based or 1-based note index within the file — pick one
      and comment it; the key must be UNIQUE per note so the Map does not clobber). A note whose
      readNoteFields returns null (no valid fence — should not happen for a splitNotes element, but
      defend) is pushed to `unparseable` naming `<file>#<n>`. After the notes loop, if
      `splitNotes(fileText).trailingMalformed` is non-null, push the FILE NAME into `unparseable`
      (the existing fail-closed channel — checkCarveOut@211-222 already emits a fail-closed finding per
      unparseable entry, so a fence-then-scratch file refuses naming the file; WR-01 folds in for free).
      Apply splitNotes UNIFORMLY on BOTH the raw thread side AND the promoted side — a one-fence promoted
      file yields exactly one note (harmless) and keeps a SINGLE code path (do not special-case one side).
    - The downstream carve-out logic is UNCHANGED: it already keys the promoted-by-id map and the raw-side
      collision guard and the required-survival set on the frozen `id` (compactor.ts@339-412), so once
      every note in a thread file reaches the Map as its own record, a buried note enters the required set
      and its drop is refused. The Map key change from `<file>` to `<file>#<n>` only affects the per-file
      iteration; survival is id-keyed throughout. Confirm (in a comment) that the round-5 gates (gate (a)
      malformedLines, gate (b) validate()) and the byte-equal loop now run PER NOTE because they iterate
      the Map, which now holds one entry per note.

    Document in a code comment at readNoteDir and in the SUMMARY: the read path now recovers exactly the
    per-note set the write path emitted (count + frozen ids + verbatim bytes), or fails closed on a
    trailing remainder — this is the CLASS-level invariant that closes the multi-note bypass family.

    Do NOT edit any test file in this task except as needed to keep the existing carve-out tests
    compiling (the Map-key change from `<file>` to `<file>#<n>` is internal to readNoteDir; existing
    single-note-per-file fixtures yield `<file>#0`, so no fixture change should be required — verify by
    running the suite). Do NOT implement IN-01 here (that is Task 3) and do NOT touch readContext (WR-02/WR-03 are out of scope).
  </action>
  <verify>
    <automated>npx vitest run scripts/compactor.test.ts -t "round-6 multi-note" && npx vitest run scripts/context-io.test.ts scripts/compactor.test.ts</automated>
  </verify>
  <done>
    `splitNotes` is exported from context-io.ts and shares ONE recognized-line set (boundary grammar)
    with parseNote (single source — they cannot drift). It is a BODY-CONSUMING splitter:
    `splitNotes(<writeThread×2 output>).notes.length === 2`, each element's `parseNote(...).body` is the
    non-empty authored body (bodies are NOT stripped), and `notes.join('') + (trailingMalformed ?? '')`
    reproduces the normalized file byte-for-byte — a frontmatter-only matcher that drops bodies or
    swallows note #2 into trailingMalformed FAILS this. `readNoteDir` iterates `splitNotes(fileText).notes`,
    keys each note by `<file>#<n>`, and routes any `trailingMalformed` remainder into
    NoteDirResult.unparseable. The four Task-1 reproductions now PASS (the carve-out exits 1, naming the
    dropped id / the fence-then-scratch file). The full existing non-e2e suite still compiles and runs
    against the .ts via vitest (the committed .js is rebuilt in Task 4). The read-path-recovers-the-write-path
    invariant is recorded in a compactor.ts comment. No write-path representation change; readContext untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close IN-01 — export noteId and have composeThreadNote reuse it; add the splitNotes unit + shared-grammar tests</name>
  <files>scripts/context-io.ts, scripts/compactor.ts, scripts/context-io.test.ts</files>
  <read_first>
    - scripts/context-io.ts (`noteId`@416-420 — the private id formula `${atCompact}-${by}-${kind}-${nonce}`; `appendNote`@425 which calls noteId; the exports list — noteId is NOT currently exported)
    - scripts/compactor.ts (`composeThreadNote`@547-564 — the INLINE id re-implementation@548 `${note.at.replace(/[-:]/g,"").replace(/\.\d+/,"")}-${note.by}-${note.kind}-${randomUUID().slice(0,8)}`, byte-identical to noteId's formula; the imports@57-66)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-REVIEW.md (IN-01 @258-270 — the drift hazard: a thread id diverging from the promoted-counterpart id format defeats the id-keyed carve-out)
    - scripts/context-io.test.ts (the IN-02 exported-parser describe block@624 — the model for asserting one source feeds both paths)
  </read_first>
  <action>
    TWO coordinated edits + one test surface. Clear professional voice; cite IN-01/CMP-02.

    EDIT A — context-io.ts: EXPORT `noteId`. Change the declaration at context-io.ts:416 from
    `function noteId(...)` to `export function noteId(...)`. No formula change. Add a one-line comment
    that this is the SINGLE source of note identity raw→promoted, reused by both appendNote and the
    compactor's thread-note composer (IN-01, same single-source principle as IN-02's shared parser).

    EDIT B — compactor.ts: have `composeThreadNote`@547-548 reuse the exported `noteId`. Import `noteId`
    from `./context-io.js` (add to the import block@57-66). Replace the inline id expression@548 with a
    call to `noteId(note)` so a thread note's frozen id is produced by the SAME formula as a promoted
    note's id — the id-format cannot drift between the two sides the id-keyed carve-out matches. Confirm
    the `randomUUID` import is still needed in compactor.ts (it is used by `atomicWrite`/other paths) or
    remove it if composeThreadNote was its only consumer — verify by grep before removing.

    TEST SURFACE — context-io.test.ts: add a new describe block "context-io.js — splitNotes multi-fence
    split (shared grammar, IN-02)". Assert (all checkable — array length, substring, deep-equal):
    - A two-fence file (each note WITH a non-empty body) splits into exactly 2 notes, each element being
      the VERBATIM bytes of its note INCLUDING its body (each element, when passed to parseNote, returns
      non-null with the expected `scalars.id` AND `parseNote(element).body` equals that note's authored
      body — the splitter does NOT strip bodies).
    - BYTE round-trip: `splitNotes(text).notes.join('') + (trailingMalformed ?? '')` deep-equals the
      CRLF-normalized input (no byte invented, none dropped).
    - `trailingMalformed` is null for a clean two-fence file; non-null (and equals the trailing text)
      for a fence-then-free-scratch file.
    - A single-fence file yields exactly 1 note (body intact) and null trailingMalformed.
    - An all-scratch (no fence) file yields 0 notes and non-null trailingMalformed.
    - BODY `---` AMBIGUITY: a two-note input where note #1's body contains a lone `---` line AND an
      embedded `---\nkey: value\n---` block splits into EXACTLY 2 notes (NOT 3+), `trailingMalformed`
      is null, and `parseNote(notes[0]).body` contains the body's `---` / embedded block VERBATIM (the
      body `---` is body bytes, not a boundary — it neither spawns a spurious note nor terminates note #1
      early). This is the unit-level pin of the 6th-bypass family.
    - SHARED-GRAMMAR proof: for a two-fence file, `splitNotes(text).notes.map(parseNote)` yields the
      same scalars (and bodies) as parsing each note authored standalone — i.e. a carved note equals a
      parsed note (splitNotes cannot drift from parseNote). A CRLF two-fence file splits to the same
      notes as its LF twin (CRLF normalized, IN-01/CRLF identity).

    Keep every assertion checkable: array length, substring of the id, deep-equal of scalars/bodies. No
    subjective language. Do NOT touch readContext (WR-02/WR-03 out of scope).
  </action>
  <verify>
    <automated>npx vitest run scripts/context-io.test.ts scripts/compactor.test.ts</automated>
  </verify>
  <done>
    `noteId` is exported from context-io.ts; `composeThreadNote` reuses it (no inline id formula remains
    in compactor.ts — verify by grep that the inline `.replace(/[-:]/g` id expression is gone from
    composeThreadNote). The new context-io splitNotes describe block proves verbatim split INCLUDING
    bodies (`parseNote(element).body` equals each authored body) + the join byte round-trip,
    trailingMalformed surfacing, single-fence ⇒ one note (body intact), all-scratch ⇒ zero notes +
    trailingMalformed, the body-`---` ambiguity (a body lone `---` + embedded `---…---` block splits into
    exactly 2 notes — not 3+ — trailingMalformed null, note[0].body keeps the `---` verbatim), the
    shared-grammar (carved == parsed) equality, and CRLF identity. All assertions pass against the .ts via vitest.
  </done>
</task>

<task type="auto">
  <name>Task 4: GREEN proof + regression-safety grep + rebuild byte-fresh .js + freshness/regression gates</name>
  <files>scripts/context-io.js, scripts/compactor.js, .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-07-GREEN-proof.txt</files>
  <read_first>
    - package.json (scripts: build@9 `tsc`, freshness@13, test@11 — note `npm test` is FORBIDDEN per the regression rule; it triggers the live claude-CLI e2e lane that spends tokens / can hang; use the vitest --exclude command)
    - scripts/context-io.ts and scripts/compactor.ts (the edited source — to confirm the build target)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-07-RED-baseline.txt (the RED evidence captured in Task 1)
    - CLAUDE.md (D-13 committed-.js freshness contract; no-fabrication / green-suite-≠-proof rule; clear voice on safety surfaces)
  </read_first>
  <action>
    Regression-safety grep FIRST (splitNotes now changes how EVERY multi-region thread/promoted file is
    read): grep the repo for any committed `.md` thread/note file under `.grugops/context/**` (confirm
    empty of committed multi-fence .md at plan time, but re-confirm) and any test fixture or committed
    note that concatenates more than one `---…---` fence in a single file and relied on the OLD
    parse-only-the-first-fence behavior. The intended multi-note shape is the threads/<agent>.md file
    (gitignored, not committed). If any COMMITTED single-file-multi-note artifact exists that the old
    read path silently truncated and that a consumer depended on, STOP and surface it before proceeding;
    otherwise record in the SUMMARY that none exist (cite the grep command + zero hits). Also grep to
    confirm no remaining inline id formula in composeThreadNote (the IN-01 fix from Task 3).

    Then capture the GREEN proof against the COMMITTED artifact (this is the non-negotiable acceptance
    criterion — not a unit-green claim):
    1. `npm run build` (tsc) to regenerate the committed scripts/context-io.js and scripts/compactor.js
       from the edited .ts.
    2. `npm run freshness` — MUST exit 0 (the committed .js is a byte-faithful build of the .ts; D-13).
    3. Run the four round-6 reproductions against the freshly rebuilt COMMITTED compactor.js, AND
       directly invoke `node scripts/compactor.js check <thread> <promoted>` on the buried-verified-
       finding fixture — it MUST now exit 1 ("carve-out FAIL: a §14-gate-verified … was dropped …")
       naming the dropped id. Append this GREEN output to a new evidence file 22-07-GREEN-proof.txt
       alongside the Task-1 RED baseline, so the RED→GREEN transition against the committed .js is
       captured side by side (pre-fix exit 0 "carve-out intact" → post-fix exit 1 naming the dropped id).
    4. Full non-e2e regression suite green:
       `npx vitest run --exclude '**/scripts/e2e/**'` (NOT `npm test` — that triggers the live
       claude-CLI e2e lane that spends tokens / can hang). Record the pass/skip counts.

    Stage the rebuilt scripts/context-io.js and scripts/compactor.js as part of this work (D-13 — the
    committed .js must travel with the .ts; the GREEN proof runs the CLI against the committed .js, not the .ts).
  </action>
  <verify>
    <!-- The discriminating proof is the post-build vitest run: the round-6 multi-note tests drive the
         freshly-rebuilt COMMITTED compactor.js via runCheck and assert exit≠0 naming the dropped id, so
         build→freshness→suite genuinely fails red if the fix is absent. The direct
         `node scripts/compactor.js check <fixture>` exit-1 invocation on a real buried-finding fixture
         dir lives in the <action>'s GREEN-proof.txt evidence step. -->
    <automated>npm run build && npm run freshness && npx vitest run --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <done>
    Regression grep run and recorded (no committed single-file-multi-note artifact the old read path
    truncated; no inline id formula remains in composeThreadNote). `npm run build` regenerates both
    committed .js; `npm run freshness` exits 0 (no drift). The buried-verified-finding fixture exits 1
    naming the dropped id against the freshly rebuilt COMMITTED compactor.js (RED→GREEN captured in
    22-07-RED-baseline.txt + 22-07-GREEN-proof.txt: pre-fix exit 0 "carve-out intact" → post-fix exit 1).
    The full `npx vitest run --exclude '**/scripts/e2e/**'` suite is green (no regressions; the existing
    carve-out tests + the new multi-note block + the context-io splitNotes block all pass). Committed .js staged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw thread `threads/<agent>.md` (multi-note) → carve-out oracle | A single file holds MANY adversary-authored note fences; the oracle must recover EVERY note, not just the first |
| promoted set `.md` → carve-out oracle | A compaction agent's proposed promoted notes cross in; a buried-in-raw note dropped from the promoted set must be refused |
| note bytes → write path (writeThread/composeThreadNote) | The thread writer stamps each note's frozen id; the read path must recover the SAME per-note set the writer emitted (round-trip), keyed on that frozen id |

## STRIDE Threat Register

The CMP-02 carve-out oracle is a SAFETY INVARIANT (clear voice). The high-severity threat is an
adversarial or buggy compaction step that drops a load-bearing provenance field (verified_by /
failed-attempt / supersedes / by / at) by exploiting a read-path projection — here, the multi-note-file
blind spot — defeating the un-cheatable carve-out floor. Block-on-high (ASVS L1): a silently-dropped
§14-gate-verified finding is a high-severity integrity failure.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07-01 | Tampering | readNoteDir parsing only the first fence per file (compactor.ts:160-174), so notes #2+ are invisible to the oracle | mitigate | EDIT B: readNoteDir iterates `splitNotes(fileText).notes`, one record per note keyed by `<file>#<n>`, so EVERY note reaches the required-survival set, both gates, and the byte-equal loop (CMP-02, the 5th-bypass fix) |
| T-22-07-02 | Spoofing | a §14-gate-verified finding buried as note #2+ silently dropped from the promoted set at exit 0 "carve-out intact" | mitigate | Once split per-note, the buried verified finding enters the id-keyed required-survival set (compactor.ts:405-451); its drop is refused (exit 1) naming the frozen id |
| T-22-07-03 | Tampering | splitNotes drifting from parseNote — either a separate boundary grammar, OR a frontmatter-only matcher that strips note bodies / swallows note #2 into trailingMalformed (the literal 6th bypass) | mitigate | EDIT A: splitNotes is a BODY-CONSUMING splitter whose boundary (column-0 `---` + a recognized frontmatter line) reuses parseNote's SAME recognized-line set (IN-02 single-source); contract: `parseNote(notes[i]).body` equals the authored body AND `notes.join('')+(trailingMalformed??'')` round-trips byte-for-byte. The body-byte round-trip + body-`---` tests prove a carved note equals a parsed note, body included |
| T-22-07-04 | Tampering | a fence-then-free-scratch (or mixed) thread file hiding a later note behind raw scratch (WR-01) | mitigate | splitNotes surfaces the trailing non-fence remainder as `trailingMalformed`; readNoteDir routes it into NoteDirResult.unparseable; checkCarveOut fails closed naming the file — never a silent drop |
| T-22-07-05 | Tampering | a thread note's frozen id drifting from the promoted-counterpart id format, defeating the id-keyed survival match (IN-01) | mitigate | EDIT (Task 3): composeThreadNote reuses the single exported `noteId`; the id formula is single-source, so raw and promoted ids cannot diverge in shape |
| T-22-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (devDeps already pinned: typescript, vitest, @types/node); zero new dependencies — nothing to legitimacy-check |
</threat_model>

<verification>
Phase-level CMP-02 closure checks (all must hold):

1. RED→GREEN against the COMMITTED .js: the buried-verified-finding fixture exits 0 "carve-out intact"
   against the pre-fix committed scripts/compactor.js and exits 1 naming the dropped id against the
   rebuilt committed scripts/compactor.js. Evidence in 22-07-RED-baseline.txt + 22-07-GREEN-proof.txt.
2. Multi-note class closed: a §14-gate-verified finding buried as note #2+ is refused; a failed-attempt
   buried as note #2+ is refused (compactor.test.ts round-6 block).
3. Round-trip invariant (count + frozen ids + BODY BYTES): writeThread×2 → readNoteDir recovers exactly
   the two stamped frozen ids AND each note's body byte-for-byte; dropping either note from the promoted
   set refuses naming the dropped id, and corrupting/truncating a body in the promoted set also refuses
   (a body-stripping splitter cannot pass).
4. Fail closed, never silent: a fence-then-free-scratch thread file refuses (exit 1) naming the file
   (WR-01 / trailingMalformed → NoteDirResult.unparseable).
5. Single-source / body-consuming grammar: splitNotes and parseNote share one recognized-line set
   (boundary = column-0 `---` + a recognized frontmatter line; a note's text runs boundary→next|EOF so
   it INCLUDES its body); a carved note equals a parsed note INCLUDING its body (`parseNote(notes[i]).body`
   == authored body) and `notes.join('')+(trailingMalformed??'')` round-trips byte-for-byte
   (context-io.test.ts splitNotes block); a CRLF file splits identically to its LF twin.
5a. Body `---` is body bytes, not a boundary: a note #1 body holding a lone `---` rule + an embedded
   `---…---` block splits into EXACTLY the authored note count (not 3+) and does NOT hide a following
   real note #2 — note #2's drop is refused naming its id (compactor.test.ts test 5 + context-io.test.ts
   body-`---` unit assertion).
6. IN-01 closed: composeThreadNote reuses the exported noteId; no inline id formula remains in compactor.ts.
7. Byte-fresh committed .js: `npm run freshness` exits 0.
8. No regressions: `npx vitest run --exclude '**/scripts/e2e/**'` green (existing carve-out tests intact
   + the new multi-note block + the context-io splitNotes block).
9. CMP-01 / CMP-03 untouched: no behavior change to two-tier separation, the dial, re-verify, or
   Workflow 18; the write-path representation (single multi-note threads/<agent>.md) is UNCHANGED — the
   fix is read-path-only.
10. Out-of-scope deferrals respected: readContext is NOT touched (WR-02/WR-03 deferred); validate() gets
    no unknown-key allowlist (broader IN-02 deferred).
</verification>

<success_criteria>
- CMP-02 / SC2 holds as an un-cheatable mechanical floor: a §14-gate-verified finding survives compaction
  UNCONDITIONALLY and a failed-attempt's provenance survives UNCONDITIONALLY, even when buried as note #2+
  in a single multi-note threads/<agent>.md file. The CLI exits 1 naming the dropped id.
- The carve-out read path recovers EXACTLY the per-note set the write path emitted — same note count,
  same frozen ids, same verbatim bytes — or fails closed (the CLASS-level invariant, not the multi-note instance).
- splitNotes and parseNote share ONE fence grammar (single source, IN-02) so they cannot drift; the
  shared-grammar test proves a carved note equals a parsed note.
- A trailing non-fence remainder (fence-then-scratch, WR-01) fails closed naming the file — never a silent drop.
- IN-01 closed: composeThreadNote reuses the exported noteId; the thread-note id format cannot drift from
  the promoted-counterpart id format.
- RED→GREEN reproduction against the COMMITTED scripts/compactor.js is captured as committed evidence
  (pre-fix exit 0 → post-fix exit 1 naming the dropped id), not a unit-green claim.
- `npm run freshness` exits 0 (byte-fresh committed .js, D-13); `npx vitest run --exclude '**/scripts/e2e/**'`
  is green (no regressions). CMP-01/CMP-03 and the write-path representation are untouched.
</success_criteria>

<output>
Create `.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-07-SUMMARY.md` when done.
The SUMMARY MUST record: the RED→GREEN transition (pre-fix exit 0 "carve-out intact" → post-fix exit 1
naming the dropped id against the committed .js, with the evidence file paths); confirmation that the fix
is READ-PATH-ONLY (the single multi-note threads/<agent>.md write representation is UNCHANGED, D-08); that
splitNotes and parseNote share ONE fence-grammar definition (single source, IN-02) and how the
shared-grammar test proves a carved note equals a parsed note; that `readNoteDir` keys per-note records by
`<file>#<n>` and routes a `trailingMalformed` remainder into NoteDirResult.unparseable (WR-01 fail-closed);
that composeThreadNote now reuses the exported `noteId` (IN-01) with no inline formula remaining; the
regression-safety grep result (no committed single-file-multi-note artifact the old read path truncated);
that the round-trip invariant test recovers the exact id-set; the explicit OUT-OF-SCOPE deferrals
(WR-02 readContext fail-open, WR-03 id/filename divergence, broader IN-02 unknown-key allowlist) so the
decision is visible; and confirmation that CMP-01/CMP-03 are untouched.
</output>

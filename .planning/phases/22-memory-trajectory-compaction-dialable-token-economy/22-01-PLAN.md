---
phase: 22-memory-trajectory-compaction-dialable-token-economy
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/compactor.test.ts
  - scripts/compactor.ts
  - scripts/compactor.js
  - agent-factory/config/factory.config.json
  - agent-factory/config/factory.config.md
  - agent-factory/seed/.grugops/factory.config.json
  - .gitignore
autonomous: true
requirements: [CMP-01, CMP-02, CMP-03]

must_haves:
  truths:
    - "Dropping any load-bearing carve-out element (verified_by / supersedes / by / at / a raw failed-attempt note id) during compaction is refused — the checker exits non-zero and names the dropped element (CMP-02, SC2)"
    - "A faithful compaction preserving every carve-out element is accepted (exit 0), so the RED cases prove the drop fails, not that the checker rejects everything (CMP-02)"
    - "The carve-out is un-dialable: each drop still refuses at aggressive, balanced, and retain-raw (D-05, SC2)"
    - "Verbose trajectory stays in .grugops/context/<task>/threads/<agent>.md while only the compact distillation reaches notes/; promotion routes ONLY through context-io.appendNote (no forked writer) (CMP-01, SC1)"
    - "A promoted finding is re-verified via context-io.admit() before write; a materially-changed finding whose stamp no longer cross-checks degrades honestly to a claim with confidence: UNKNOWN - verify (D-12, SC1)"
    - "context.compaction defaults to aggressive when the key — or the whole config file — is absent (CMP-03, SC3)"
    - "context.compaction is present and identical in both factory.config.json and the seed byte-twin, and documented in factory.config.md (CMP-03, SC3, D-06)"
    - "compactor.js is byte-fresh vs compactor.ts (auto-discovered by the freshness gate) (D-13)"
  artifacts:
    - path: "scripts/compactor.ts"
      provides: "Deterministic carve-out invariant checker over (raw thread -> proposed promoted notes); imports context-io.js; never summarizes"
      contains: "from \"./context-io.js\""
    - path: "scripts/compactor.js"
      provides: "Committed tsc output of compactor.ts (freshness-gated)"
    - path: "scripts/compactor.test.ts"
      provides: "RED-fixture-first vitest oracle: one drop case per carve-out element + GOOD case + dial-invariance + re-verify"
    - path: "agent-factory/config/factory.config.json"
      provides: "context.compaction dial (lean default aggressive)"
      contains: "compaction"
    - path: "agent-factory/seed/.grugops/factory.config.json"
      provides: "byte-twin of the config dial"
      contains: "compaction"
    - path: "agent-factory/config/factory.config.md"
      provides: "context sub-field reference + lean->enterprise dial-contract row + absent-default documentation"
      contains: "context.compaction"
    - path: ".gitignore"
      provides: "ephemeral threads/ tier ignore scoped to */threads/ only"
      contains: "threads/"
  key_links:
    - from: "scripts/compactor.ts"
      to: "scripts/context-io.ts"
      via: "imports readContext / currentState / appendNote / admit from context-io.js; promotion routes through appendNote (D-02.3); re-verify via admit (D-12)"
      pattern: "appendNote|admit|readContext|currentState"
    - from: "scripts/compactor.ts"
      to: "scripts/freshness.ts"
      via: "freshness.ts collectJs() auto-globs scripts/*.js — compactor.js joins the drift gate with no edit"
      pattern: "compactor\\.js"
    - from: "agent-factory/config/factory.config.json"
      to: "agent-factory/seed/.grugops/factory.config.json"
      via: "byte-twin invariant (D-06) — identical edit, diff confirms zero divergence"
      pattern: "compaction"
---

<objective>
Build the deterministic carve-out invariant checker (`scripts/compactor.ts` + committed `.js` + RED-first `compactor.test.ts`) and add the `context.compaction` dial across all three config surfaces plus the `.gitignore` entry for the ephemeral thread tier.

This is the mechanical floor of the body/frontmatter seam (D-01): the agent compresses note *bodies* (semantic, owned by WF18 in Plan 02); `compactor.ts` protects note *structure* — it never summarizes. It is a thin helper ON TOP of `context-io.ts` (D-02/D-13): the only genuinely new code is the trajectory-thread read and the deterministic carve-out check.

Purpose: Bound the multi-agent token tax with two-tier memory and an un-cheatable, un-dialable carve-out — landed before Phase 23's parallel fan-out makes the cost real.
Output: `scripts/compactor.ts`, `scripts/compactor.js`, `scripts/compactor.test.ts`, the `context.compaction` dial in 3 config surfaces, and a scoped `.gitignore` entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-CONTEXT.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-RESEARCH.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-PATTERNS.md
@.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-VALIDATION.md

@scripts/context-io.ts
@scripts/context-io.test.ts
@agent-factory/contracts/context-note.md
@hooks/guard.ts
@scripts/freshness.ts
@agent-factory/config/factory.config.json
@agent-factory/config/factory.config.md
@agent-factory/seed/.grugops/factory.config.json
@.gitignore
</context>

## Artifacts this phase produces

> Source-grounding pass: exclude these newly-created symbols/paths from drift verification — they do not yet exist before this phase.

- `scripts/compactor.ts` — new helper (carve-out invariant checker)
- `scripts/compactor.js` — new committed tsc output
- `scripts/compactor.test.ts` — new vitest oracle
- `context.compaction` — new config key (value: `aggressive` | `balanced` | `retain-raw`; lean default `aggressive`)
- `.grugops/context/<task>/threads/<agent>.md` — new ephemeral local trajectory path (D-08)
- `**/.grugops/context/*/threads/` — new `.gitignore` glob (D-07)
- Any exported function names / CLI verbs introduced by `compactor.ts` (planner-discretion surface, D-02 — e.g. a `check` / carve-out function and its CLI dispatch)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — compactor.test.ts carve-out oracle (Wave 0)</name>
  <files>scripts/compactor.test.ts</files>
  <read_first>
    - scripts/context-io.test.ts (the RED-fixture-first idiom to mirror EXACTLY — header/imports lines 22-53, goodNoteText-then-mutate lines 56-79, spawn-the-CLI helper lines 82-87, name-the-fault assertion lines 106-125)
    - scripts/context-io.ts (the reuse-surface signatures: NOTE_KINDS, NoteInput, NoteRecord, readContext, currentState, appendNote, admit; the refuse idiom at appendNote line 397; assertSafeTask allowlist lines 84-94; the supersedes fold currentState lines 436-444)
    - agent-factory/contracts/context-note.md (the provenance fence = the CMP-02 carve-out set: verified_by / supersedes / by / at; the six kinds incl. failed-attempt; the claim-KIND vs queue-CLAIM distinction)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-VALIDATION.md (the requirement->behavior->command rows this test MUST satisfy verbatim — the "drops verified_by" / "drops supersedes" / "drops by" / "drops at" / "drops a failed-attempt" / "carve-out intact accepts" / "carve-out un-dialable" / "two-tier separation" / "promotes via appendNote only" / "absent dial defaults aggressive" / "dial is body-only, note-set invariant" / "re-verify faithful admits" / "materially-changed degrades to claim" test titles)
  </read_first>
  <action>
    Create scripts/compactor.test.ts mirroring the context-io.test.ts idiom: import {describe,it,expect,afterAll} from vitest; spawnSync from node:child_process; mkdtempSync/mkdirSync/writeFileSync/readFileSync/rmSync from node:fs; tmpdir from node:os; join from node:path; pathToFileURL from node:url. Set ROOT = join(import.meta.dirname, "..") and COMPACTOR_JS = join(ROOT, "scripts", "compactor.js"). Use the freshTmp(prefix) + afterAll cleanup pattern so NOTHING is written into the committed tree. Dynamic-import the compiled compactor.js for pure-function paths; spawnSync("node",[COMPACTOR_JS, ...]) for CLI paths.

    Build a goodRawThread() fixture containing at least one failed-attempt note with id "FA-1" plus a verified finding stamped "§14-gate#SEED-001" carrying verified_by / by / at / supersedes, and a goodPromotedSet() that preserves all of them (reuse context-io.test.ts goodNoteText shape for note composition). Each RED case mutates goodPromotedSet() to drop exactly ONE carve-out element.

    Title the tests EXACTLY as the VALIDATION.md rows require so the -t filters match:
    - "drops verified_by" — assert run fails AND output names "verified_by"
    - "drops supersedes" — assert run fails AND output names "supersedes"
    - "drops by" — assert run fails AND output names "by"
    - "drops at" — assert run fails AND output names "at"
    - "drops a failed-attempt" — drop the FA-1 id from the promoted set, assert run fails AND output names the dropped id "FA-1"
    - "carve-out intact accepts" — GOOD case, exit 0
    - "carve-out un-dialable" — run each drop case at compaction values aggressive / balanced / retain-raw, assert each still refuses (D-05)
    - "two-tier separation" — after a compaction pass under aggressive, the verbose body lives in .grugops/context/<task>/threads/<agent>.md and notes/ receives only the compact distillation (the raw narrative is NOT in notes/)
    - "promotes via appendNote only" — assert the only path writing notes/ is context-io.appendNote (no forked writer)
    - "absent dial defaults aggressive" — with no context.compaction key AND with no config file at all, behavior == aggressive
    - "dial is body-only, note-set invariant" — across aggressive/balanced/retain-raw the promoted durable-kind note set and the carve-out are identical; only body verbosity / raw-reaching-shared differs
    - "re-verify faithful admits" — faithful body compaction carrying its §14-gate#<id> + a seeded live green verdict under the task -> admit() returns []
    - "materially-changed degrades to claim" — a materially-changed finding (stamp no longer cross-checks) -> admit() returns a non-empty findings array AND the honest outcome is a claim with confidence "UNKNOWN - verify", never a hand-carried stamp

    Failure assertion shape per RED case: expect(r.status).not.toBe(0) for CLI paths and expect(`${r.stdout}${r.stderr}`).toContain("<dropped-element-name>") — name the fault, mirroring context-io.test.ts (`...).toContain("confidence")`). This file ships RED until Task 2 lands compactor.js — correct Wave-0 sequencing.

    Do NOT put the literal carve-out element names inside any negative-grep gate elsewhere; these are positive toContain assertions in test bodies, which is correct.
  </action>
  <verify>
    <automated>npx vitest run scripts/compactor.test.ts; test $? -ne 0 && echo "RED as expected (compactor.js absent)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/compactor.test.ts` exists and is valid TypeScript that vitest can collect (the suite runs, the cases execute and FAIL because `compactor.js` does not yet exist — RED is the expected Wave-0 state).
    - The file contains exactly the test titles listed above (grep confirms `drops verified_by`, `drops supersedes`, `drops by`, `drops at`, `drops a failed-attempt`, `carve-out intact accepts`, `carve-out un-dialable`, `two-tier separation`, `promotes via appendNote only`, `absent dial defaults aggressive`, `dial is body-only, note-set invariant`, `re-verify faithful admits`, `materially-changed degrades to claim`).
    - Every RED case asserts both `.not.toBe(0)` (or a thrown error) AND `toContain` of the dropped element name.
    - All temp fixtures use `mkdtempSync` under `tmpdir()`; `git status` shows nothing staged under `.grugops/` after a run.
  </acceptance_criteria>
  <done>compactor.test.ts exists, vitest collects and runs it, every carve-out RED case + the GOOD case + dial-invariance + re-verify cases are present, and the suite is RED (compactor.js not yet built).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — compactor.ts carve-out checker + committed compactor.js</name>
  <files>scripts/compactor.ts, scripts/compactor.js</files>
  <read_first>
    - scripts/context-io.ts (reuse surface — import readContext / currentState / appendNote / admit / NoteRecord / NoteInput / NOTE_KINDS from ./context-io.js; the refuse-throw idiom at appendNote line 397; the CLI run-direct guard + dispatch + process.exit(1) at lines 648-705; assertSafeTask allowlist `^[A-Za-z0-9._-]+$` lines 84-94; on-demand mkdirSync(notesDir,{recursive:true}) lines 399-400; the supersedes fold currentState lines 436-444)
    - hooks/guard.ts (the clear-voice safety-contract header shape lines 15-27 — fail-closed on ambiguity, name the fault — echo this for compactor.ts's header documenting the body=agent / structure=tool boundary and the D-03 "no semantic fold" line)
    - scripts/compactor.test.ts (the oracle from Task 1 — the contract the implementation must turn green; the exact CLI verbs / function shape the tests drive)
    - scripts/freshness.ts (OUTPUT_DIRS = ["install","scripts","hooks"] + collectJs() globs scripts/*.js — confirm compactor.js auto-joins the drift gate with no edit to freshness.ts)
    - agent-factory/contracts/context-note.md (the carve-out set + required-field floor the checker enforces)
  </read_first>
  <action>
    Create scripts/compactor.ts as a thin helper ON TOP of context-io.js (D-02/D-13). Imports: from "./context-io.js" pull readContext, currentState, appendNote, admit, and the NoteRecord / NoteInput / NOTE_KINDS types; from node:fs the minimal set needed for the thread read + threads-dir mkdir (readFileSync, readdirSync, mkdirSync, existsSync, writeFileSync as the analog needs); from node:path join; from node:url pathToFileURL for the run-direct guard. node:fs / node:path / node:crypto ONLY — zero host runtime deps (D-15). It CANNOT and must NOT summarize.

    Author a clear-voice header comment (NOT caveman — trace/safety/token surface, CLAUDE.md) in the hooks/guard.ts shape stating the body=agent / structure=tool seam and the D-03 line: the ONLY fold compactor.ts may reuse is context-io.ts's currentState() supersedes collapse; any other dedup/merge is the agent's body-compression job, not the tool's. State that promotion routes ONLY through appendNote and re-verify ONLY through admit — no forked writer, no new verify loop.

    Implement the deterministic carve-out invariant check over (raw thread -> proposed promoted notes), per D-02 (whether one function or a small set is planner-discretion):
    1. Every failed-attempt note id present in the raw thread SURVIVES into the promoted set (D-02.1). On a missing id -> refuse and name the dropped id.
    2. The load-bearing provenance fields verified_by / supersedes / by / at are INTACT on every promoted note (D-02.2). On a dropped field -> refuse and name the field.
    3. Promotion happens ONLY via context-io.appendNote (D-02.3) — compactor.ts does NOT fork a writer. Re-verify a promoted finding ONLY via context-io.admit (D-12).
    The carve-out is un-dialable (D-05): it holds identically at aggressive / balanced / retain-raw — the dial argument NEVER turns it off. The dial governs only body verbosity / how much raw reaches the shared tier, never which durable notes promote or the carve-out fields.

    Read the trajectory thread from .grugops/context/<task>/threads/<agent>.md (D-08). Validate BOTH the <task> and <agent> path segments against the same allowlist discipline as context-io.ts assertSafeTask (`^[A-Za-z0-9._-]+$`) BEFORE interpolating into the path — do not interpolate an unvalidated agent name (V12). mkdirSync(threadsDir,{recursive:true}) before writing a thread file, mirroring appendNote's on-demand notes/ mkdir.

    Read the context.compaction dial at point-of-use from .grugops/factory.config.json, defaulting to "aggressive" when the key — or the whole file — is absent (D-06; match the quality.* / security.* read-at-use precedent; no new dial-reading machinery).

    On any dropped carve-out element: throw / process.exit(1) and print a message NAMING the dropped element, mirroring appendNote's `refusing to write an invalid note:\n<findings>` and hooks/guard.ts. Provide the CLI run-direct guard (isMain = process.argv[1] !== undefined && import.meta.url === pathToFileURL(process.argv[1]).href) + dispatch + console.error + process.exit(1) on misuse, mirroring context-io.ts lines 648-705.

    Re-verify (D-12): only a promoted finding re-hits admission via admit() — its §14-gate#<id> stamp must still cross-check a live green verdict. A faithful body compaction re-admits cheaply. A compaction that materially changed the finding (admit returns a non-empty findings array) is refused -> honestly degrade to a claim with confidence "UNKNOWN - verify" (the Phase-21 escape hatch) — NEVER a hand-carried stamp, NEVER a faked pass.

    Then run `npm run build` (tsc) and commit the produced scripts/compactor.js in the SAME change as compactor.ts. freshness.ts auto-discovers it — do NOT edit freshness.ts.
  </action>
  <verify>
    <automated>npx vitest run scripts/compactor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/compactor.test.ts` exits 0 — all carve-out RED cases now PASS (the checker refuses each drop and names the element), the GOOD case passes (exit 0), the dial-invariance, two-tier, sole-writer, re-verify, and degrade cases pass.
    - `npm run build && npm run freshness` exits 0 — `scripts/compactor.js` is byte-fresh vs `scripts/compactor.ts` (D-13).
    - `grep -n 'from "./context-io.js"' scripts/compactor.ts` matches — the helper imports the reuse surface and does not re-implement note I/O, parsing, or admission.
    - `compactor.ts` contains NO string-similarity / set-difference / dedup logic beyond reusing `currentState()` (D-03 — no semantic fold). The header comment names the body=agent / structure=tool boundary in clear professional voice.
    - The `<agent>` and `<task>` path segments are validated against an allowlist before path interpolation (V12).
    - `npm run freshness:context` exits 0 — the existing `guard_context_writes` foundation guard still passes (compactor.ts adds no forked writer of `.grugops/context/`).
  </acceptance_criteria>
  <done>compactor.test.ts is GREEN, compactor.js is committed and byte-fresh, the carve-out checker imports context-io.js and adds no forked writer / no semantic fold / no new verify loop, and freshness + freshness:context pass.</done>
</task>

<task type="auto">
  <name>Task 3: context.compaction dial — 3 config surfaces + .gitignore</name>
  <files>agent-factory/config/factory.config.json, agent-factory/seed/.grugops/factory.config.json, agent-factory/config/factory.config.md, .gitignore</files>
  <read_first>
    - agent-factory/config/factory.config.json (the existing security nested block lines 48-51 — the shape to mirror for a new top-level context object; NO context key exists today)
    - agent-factory/seed/.grugops/factory.config.json (byte-twin of factory.config.json — confirmed identical via diff; the EXACT same edit must apply)
    - agent-factory/config/factory.config.md (the `### security` sub-field table lines 79-84; the lean->enterprise dial-contract table lines 90-99 incl. the quality.test_integrity "Never off" carve-out row line 96; the zero-config defaults paragraph line 109 listing the eight keys + the absent⇒lean-default rule)
    - .gitignore (the existing comment + path-glob entry shape; the .tmp-build/ entry)
    - .planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-CONTEXT.md (D-04 dial values + lean default aggressive; D-05 un-dialable carve-out; D-06 three-surface lockstep; D-07 gitignore scoping)
  </read_first>
  <action>
    Add a fresh top-level "context" object to agent-factory/config/factory.config.json as a sibling of "security", with one key: "compaction": "aggressive" (lean default, D-04). Apply the byte-identical edit to agent-factory/seed/.grugops/factory.config.json so the byte-twin invariant holds (D-06); after both edits run `diff agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` and confirm zero output.

    In agent-factory/config/factory.config.md, make three edits mirroring the established patterns:
    1. Add a `### context` sub-field section (mirror the `### security` table at lines 79-84) with a row for `compaction`: default `aggressive`; allowed `aggressive` | `balanced` | `retain-raw`; meaning = the body-verbosity / how-much-raw-reaches-the-shared-tier knob, NEVER the carve-out (the durable note set + load-bearing fields are un-dialable at every value, D-05).
    2. Add a `context.compaction` row to the lean->enterprise dial-contract table (mirror the quality.test_integrity safety-carve-out row at line 96): allowed values `aggressive, balanced, retain-raw`; lean default `aggressive`; enterprise escalation `retain-raw` — full trajectory bodies admitted to the committed shared context (enterprise/audit). State that the body/raw verbosity knob is all the dial moves; the durable note set and the carve-out are un-dialable at every value (D-05).
    3. Update the zero-config defaults paragraph (line 109): the key count rises from eight to nine and `context.compaction` joins the documented absent⇒lean-default list so a missing key reads as `aggressive`. This is the verified D-06 read-at-use, default-on-absent precedent the compactor relies on.

    In .gitignore add a scoped entry for the ephemeral local trajectory tier (D-07), scoped to `*/threads/` ONLY — never a blanket `.grugops/context/` ignore (which would gitignore the committed notes/ and index.* and break the audit trail). Use the comment + glob shape of the existing entries:
    `**/.grugops/context/*/threads/`
    with a clear-voice comment that this is the ephemeral local agent trajectory tier (D-07) — never committed; only the verified shared context (notes/, index.*) is durable. (Exact wording/location is planner discretion; the intent "ignore only threads/" is locked.)
  </action>
  <verify>
    <automated>diff agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json && node -e "const c=require('./agent-factory/config/factory.config.json'); if(c.context.compaction!=='aggressive') process.exit(1)" && grep -q 'context.compaction' agent-factory/config/factory.config.md && grep -q 'threads/' .gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `diff agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` produces zero output — the byte-twin invariant holds (D-06).
    - `node -e "require('./agent-factory/config/factory.config.json').context.compaction"` reads `"aggressive"` — the dial is present with the lean default in both JSON surfaces.
    - `factory.config.md` contains a `### context` sub-field section with a `compaction` row, a `context.compaction` row in the lean->enterprise dial-contract table stating the carve-out is un-dialable at every value (D-05), and the zero-config paragraph updated from eight to nine keys with `context.compaction` listed as defaulting to `aggressive` when absent.
    - `.gitignore` contains `**/.grugops/context/*/threads/` (scoped to the ephemeral tier only) — `grep -q 'context/\*/threads/' .gitignore` (or the chosen scoped glob) matches, and a blanket `.grugops/context/` ignore is NOT present.
    - The dial change introduces NO new dial-reading machinery — it is read at point-of-use, default-on-absent, exactly like `quality.*` / `security.*`.
  </acceptance_criteria>
  <done>context.compaction is present and byte-identical in both factory.config.json surfaces, documented across the three factory.config.md locations (sub-field table, dial-contract table, zero-config paragraph), and the .gitignore carries a scoped `*/threads/` entry that ignores only the ephemeral tier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw trajectory body → promoted note | Untrusted free-form agent narrative is distilled and promoted into the committed shared verified context (the audit substrate). |
| compactor.ts → `.grugops/context/` | A new tooling helper sits beside the sole sanctioned writer (`context-io.ts`); it must not become a second, unguarded write path. |
| `<task>` / `<agent>` path segments → filesystem path | Caller-supplied identifiers compose `.grugops/context/<task>/threads/<agent>.md`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering / Repudiation | compactor.ts carve-out check | mitigate | The D-02 invariant checker refuses + names the fault when any load-bearing carve-out element is dropped (verified_by / supersedes / by / at / a raw failed-attempt id). Proven by the CMP-02 RED cases (Task 1) going GREEN (Task 2). This is the entire point of CMP-02. |
| T-22-02 | Tampering / Elevation | promotion write path | mitigate | Promotion routes ONLY through `context-io.appendNote` (D-02.3); compactor.ts adds NO forked writer. `appendNote`'s `assertSingleLine` + duplicate-key rejection (CR-01) fire automatically; the existing `guard_context_writes` foundation guard fails red on any non-`context-io.ts` writer of `.grugops/context/`. Verified by `npm run freshness:context` + the "promotes via appendNote only" test. |
| T-22-03 | Tampering | re-verify of a promoted finding | mitigate | A `§14-gate#<id>` stamp carried over a materially-changed finding is refused by `admit()` (D-12); the honest path degrades to a `claim` with `confidence: UNKNOWN - verify` — never a hand-carried/faked stamp. Verified by the "materially-changed degrades to claim" test. |
| T-22-04 | Tampering | `<task>` / `<agent>` path segments | mitigate | Both segments are validated against the `^[A-Za-z0-9._-]+$` allowlist (reuse context-io.ts `assertSafeTask` discipline) before interpolation — path traversal via the agent/task name is rejected (V12). |
| T-22-05 | Tampering | npm/dev installs | mitigate | Phase 22 installs ZERO external packages (zero-host-runtime-dep, D-13). No `[ASSUMED]`/`[SUS]` package, so no install checkpoint is required; RESEARCH.md Package Legitimacy Audit documents "not applicable — zero external packages." |
</threat_model>

<verification>
- `npx vitest run scripts/compactor.test.ts` — GREEN (all carve-out / dial / two-tier / re-verify cases pass).
- `npm run build && npm run freshness` — GREEN (compactor.js byte-fresh vs compactor.ts, D-13).
- `npm run freshness:context` — GREEN (guard_context_writes still passes; no forked writer added).
- `diff agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` — zero output (byte-twin, D-06).
- `npx vitest run --exclude '**/scripts/e2e/**'` — full regression lane GREEN (no live e2e lane spend).
</verification>

<success_criteria>
- CMP-02 carve-out: dropping any of verified_by / supersedes / by / at / a raw failed-attempt id is refused with the element named; a faithful set is accepted; the carve-out is un-dialable across all three dial values (SC2).
- CMP-01 two-tier: verbose trajectory stays in `threads/<agent>.md`; only the compact distillation reaches `notes/`; promotion routes solely through `appendNote`; the promoted finding is re-verified via `admit()` before write (SC1).
- CMP-03 dial: `context.compaction` is present (lean default `aggressive`), byte-identical across both JSON surfaces, documented in factory.config.md, and defaults to `aggressive` when absent (SC3).
- D-13: `compactor.js` is committed and byte-fresh.
</success_criteria>

<output>
Create `.planning/phases/22-memory-trajectory-compaction-dialable-token-economy/22-01-SUMMARY.md` when done.
</output>
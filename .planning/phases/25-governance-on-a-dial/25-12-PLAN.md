---
phase: 25-governance-on-a-dial
plan: 12
type: execute
wave: 4
depends_on: ["25-09", "25-10", "25-11"]
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/admission-server.ts
  - scripts/admission-server.js
  - scripts/admission-server.test.ts
  - hooks/admission-guard.ts
  - hooks/admission-guard.js
  - agent-factory/workflows/16-context-read-write.md
  - agent-factory/contracts/context-note.md
  - .planning/ROADMAP.md
  - .planning/phases/25-governance-on-a-dial/25-CONTEXT.md
autonomous: false
gap_closure: true
requirements: [GOV-01, GOV-02]

must_haves:
  truths:
    - "GAP-R6-1 CLOSED (structural): a forged `by`/`at`/`id` carrying a path separator or `..` cannot escape the CONTEXT ROOT — a SINGLE shared write-a-note chokepoint (writeNoteFile) asserts the resolved final path stays strictly inside the resolved notes dir (resolve(finalPath) startsWith resolve(notesDir)+sep) and fails closed otherwise; BOTH appendNote AND emitVerdict (the two direct note-file writers) route through it, so containment is a property of the WRITE itself and a future `at`-reachable change to either writer cannot silently re-open the hole (the Phase-22 round-8 one-chokepoint lesson). A precomputed/forged `id` is caught by this path-containment (validate does not inspect `id`). Reached via the propose_note SOFT-kind channel at any dial with no human, the injection is refused with nothing written outside the context tree (D-01 point-of-effect)."
    - "GAP-R6-1 KNOWN LIMITS (documented, bounded by the same-uid direct-FS residual — NOT new holes): (1) a lexical resolve() does NOT follow symlinks, so a symlinked notes dir/ancestor would pass containment — but the channel can only atomicWrite a `.md` and cannot plant a symlink (same-uid direct-FS residual); (2) writing a SOFT advisory (non-gated) note into a SIBLING task dir via a LEGITIMATE `task` slug is NOT isolated by this fix — the `task` arg is unbound (assertSafeTask accepts any valid slug) and there is no agent→task binding; what this fix closes is arbitrary-file-write OUTSIDE the context tree."
    - "GAP-R6-1 CO-PRIMARY guard (load-bearing, NOT optional / NOT mere defense-in-depth): validate() rejects a `by`/`at` containing a path separator, a NUL byte, a C0 control char, or `..` — this is the structural guard for emitVerdict's `at` and the CLI/compactor-oracle validate callers and MUST NOT be weakened or skipped. It MUST preserve the reserved `by: §14-gate` (U+00A7 is none of those metacharacters) and a legitimate ISO-8601 `at` (single dots between digits are not the parent-dir sequence)."
    - "GAP-R6-1 does NOT over-block: a legitimate note still writes — a normal role note AND the reserved `by: §14-gate` verdict identity (non-ASCII § preserved) both persist through writeNoteFile; the noteId formula and the nonce are unchanged."
    - "GAP-R6-2 CLOSED (scoped to a stable per-session CLAUDE_PROJECT_DIR — the server env is launch-frozen, the hook reads it per-call; they coincide because the harness keeps CLAUDE_PROJECT_DIR stable per session): the admission server derives the governance dial / ledger root / context root from the SAME trusted source the per-call hook uses (process.env.CLAUDE_PROJECT_DIR, falling back to the server's own resolved repo root) — NOT from agent-supplied propose_note arguments. `repoRoot`/`contextRoot` are removed from the tool inputSchema and ignored if present; the hook tier and the persist tier read the identical dial from the identical root and cannot diverge (D-01)."
    - "GAP-R6-2 W3 holds under the trusted root: with the REAL dial `off`/absent, an agent-supplied lax repoRoot/contextRoot can NO LONGER force the combiner's gated branch — a forged `verified_by: human:NAME` note (and its GOV-02 `disposed_by` ledger line, D-08) cannot be persisted into the real context; the non-gated branch's W3 reject fires under the trusted dial (no audit fabrication)."
    - "GAP-R6-3 HONEST-SCOPE (human decision 2026-06-26, prose only): SC1 (ROADMAP Phase 25 SC item 1 + 25-CONTEXT §A′), WF16, and context-note.md describe a per-SESSION-grant, per-note-CAPABLE model — a named human's GRUGOPS_ADMISSION_APPROVED_BY=<name> session grant authorizes high-severity admissions under that name for the session, the human controls granularity by setting/unsetting around a disposition, and it is NOT a mechanically-enforced per-note nonce. GOV-02 `disposed_by: human:<name>` is defined precisely as 'admitted under <name>'s session grant', NOT 'individually reviewed each entry' (D-07, no over- or under-claim)."
    - "An INDEPENDENT bash-grounded opus red-team (orchestrator-dispatched) vs the COMMITTED .js confirms R6-1 + R6-2 closed (incl. via the propose_note channel), R6-3 wording honest, the preserve-list intact, AND invents ≥1 NOVEL probe (a new write-location or config-root trick) — green author suite is necessary-but-NOT-sufficient (D-12)."
  artifacts:
    - "scripts/context-io.ts (+ rebuilt scripts/context-io.js) — a NEW shared writeNoteFile(notesDir, id, text) chokepoint (path-containment assert + atomicWrite) routed through by BOTH appendNote AND emitVerdict + validate() `by`/`at` path-metacharacter rejection (CO-PRIMARY, load-bearing)"
    - "scripts/admission-server.ts (+ rebuilt scripts/admission-server.js) — trusted-root derivation from CLAUDE_PROJECT_DIR; repoRoot/contextRoot removed from inputSchema and not honored as governance/persist inputs"
    - "scripts/context-io.test.ts — held-out RED cross-task-injection tests via BOTH writers (admitAndAppend/appendNote AND emitVerdict traversal refused; legit + §14-gate notes still write)"
    - "scripts/admission-server.test.ts — held-out RED tests: propose_note SOFT-kind cross-task injection refused via the channel; agent-supplied lax repoRoot/contextRoot cannot change the effective gate/persist root (off-mode W3 holds); positive controls"
    - "agent-factory/workflows/16-context-read-write.md + agent-factory/contracts/context-note.md — per-note wording honest-scoped to per-session-grant/per-note-capable"
    - "hooks/admission-guard.ts (+ rebuilt hooks/admission-guard.js) + scripts/admission-server.ts (comments) — SOURCE comment blocks reconciled to the per-session-grant model (W4, no live over-claim contradicting the docs)"
    - ".planning/ROADMAP.md Phase 25 SC1 + .planning/phases/25-governance-on-a-dial/25-CONTEXT.md §A′ — restated in lockstep (one wording, two places) + disposed_by semantics"
  key_links:
    - "propose_note (MCP) -> admitAndAppend -> appendNote -> writeNoteFile AND emitVerdict -> writeNoteFile: writeNoteFile is the SINGLE note-file write chokepoint where containment lives, so every note writer is covered (W-A single chokepoint, no drift)"
    - "admission-guard hook root (process.env.CLAUDE_PROJECT_DIR) == admission-server persist root: ONE trusted source, no agent-supplied divergence (stable per-session)"
    - "restated SC1 in ROADMAP == restated SC1 in 25-CONTEXT §A′ (verbatim mirror) == honest-scoped WF16 / context-note.md == reconciled admission-guard.ts / admission-server.ts comments (no per-note over-claim anywhere — docs and source agree)"
    - "the closing red-team drives the COMMITTED context-io.js / admission-server.js / admission-guard.js (never the .ts, never the author suites)"
  prohibitions:
    - "hooks/guard.ts byte-frozen: do NOT edit it; git diff --quiet hooks/guard.ts must exit 0 (D-02)."
    - "admit() byte-frozen (per-function span-hash, W-B): do NOT edit admit(); the context-io.test.ts admit()-freeze test must stay green. R6-1 edits appendNote + emitVerdict (both routed through the new shared writeNoteFile chokepoint) + validate ONLY — admit() is NOT in the change set (all separate functions)."
    - "W4 source-comment reconciliation (Task 3) edits hooks/admission-guard.ts and scripts/admission-server.ts COMMENTS ONLY (neither is byte-frozen — guard.ts is the frozen deploy guard, a different file); rebuild hooks/admission-guard.js + scripts/admission-server.js so freshness stays 0 on a comment-only change; keep admission-protocol-docs.test.ts asserted strings intact."
    - "noteId's formula `${atCompact}-${by}-${kind}-${nonce}` and the randomUUID nonce are UNCHANGED — the containment is a post-compute assertion, not a formula change."
    - "Do NOT return to command-string parsing: the round-6 structured channel + per-call hook stay; the command-string tokenizer deleted in 25-10 stays deleted (its identifiers grep to 0 in .ts and .js); no second severity/gated classifier — isGatedNote / isHighSeverityRole remain the single source imported by both hook and combiner (W-A)."
    - "SC2 + the GOV-02 audit ledger SHAPE are untouched (fixed key order id/kind/by/severity/verified_by/disposed_by/at; retained writes a line, git writes nothing, never the note body — D-08/D-09/D-10)."
    - "The SC3 floor is preserved: a garbage/typo/case/whitespace dial and a corrupt/unreadable config remain gate-or-stricter (only exact `off`, or genuinely absent, is off-equivalent); the four named floor invariants and the floor-invariance sweep stay green."
    - "No WR-01 / foundation-guard false-positive from the new prose: reference WF16 / the config keys; never restate a raw .grugops/ write path beside a write token (D-13). check-foundation-guards.js stays exit 0; admission-protocol-docs.test.ts stays green (WF16 H1 `# Workflow: context read/write`, frontmatter `order: 16`, self_fix_attempts / `UNKNOWN - verify` / READY_FOR_HUMAN_REVIEW references, exactly ONE 16-context-read-write reference per role file)."
    - "Rebuild the committed .js for every changed source; `node scripts/freshness.js` exit 0 (freshness covers scripts/ and hooks/). No fabricated gate: the closing red-team is NOT satisfied by a green author suite (D-12)."
---

<objective>
Round 7 (FINAL-intent) gap closure for Phase 25 GOV-01/GOV-02. Round 6 (the MOVE-THE-GATE
structural fix, plans 25-09/10/11) SUCCEEDED at its core: the 10-round shell-obfuscation class is
gone and two independent bash-grounded opus red-teams confirmed SC1's core human-gate HOLDS (no gated
high-severity admit without a real human; SC2/SC3 preserved; single-source isGatedNote; admit() +
guard.ts frozen; freshness 0). Do NOT re-open or re-architect any of that. This plan fixes ONLY the
three channel-reachable defects the red-team found, each with a held-out RED test, then proves closure
with an independent bash-grounded opus red-team (D-12).

- GAP-R6-1 (decisive, structural): path traversal through the sanctioned note writers — agent-controlled
  `by`/`at` flow into noteId → the on-disk filename with no containment guard; via the propose_note
  channel with a SOFT kind (no human gate) a `by` containing `/` + `..` escapes the context tree. FIX:
  a SHARED writeNoteFile chokepoint carrying the path-containment assert, routed through by BOTH note
  writers (appendNote AND the sibling direct writer emitVerdict — so a future `at`-reachable change to
  either cannot re-open the hole), + a CO-PRIMARY (load-bearing) `by`/`at` metacharacter reject in
  validate(). Known limits (symlinked notes dir; an unbound `task` slug writing a SOFT note into a
  sibling task dir) are documented, bounded by the same-uid direct-FS residual.
- GAP-R6-2: config-root divergence — the server reads the dial/ledger/context root from attacker-supplied
  propose_note args while the hook reads CLAUDE_PROJECT_DIR; off-mode lets an agent persist a forged
  `human:eve` note into the real context. FIX: the server derives the root from the SAME trusted source
  the hook uses; agent `repoRoot`/`contextRoot` are not honored for governance/persistence.
- GAP-R6-3: per-session ≠ per-note — HUMAN DECISION (2026-06-26): HONEST-SCOPE to a session-grant model
  (no new nonce mechanism). FIX is prose/doc only (CLEAR voice, no-fabrication).

Purpose: make the sanctioned structured admission channel un-escapable through its own writer and
un-divertible to a forged governance root, and make the SC1 disposition claim honest — without
disturbing the proven round-6 human gate, the single writer, the frozen guard.ts / admit(), SC2, the
GOV-02 ledger, or the SC3 floor.
Output: hardened context-io + admission-server (+ rebuilt committed .js, freshness 0), held-out RED
proofs, honest-scoped SC1/WF16/context-note docs, and an independent red-team closure record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-VERIFICATION.md
@.planning/phases/25-governance-on-a-dial/25-11-SUMMARY.md
@.planning/phases/25-governance-on-a-dial/25-09-SUMMARY.md
@scripts/context-io.ts
@scripts/admission-server.ts
@hooks/admission-guard.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-R6-1 — shared writeNoteFile path-containment chokepoint (covers appendNote AND emitVerdict) + co-primary `by`/`at` reject in validate()</name>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts, scripts/admission-server.test.ts</files>
  <read_first>
    - scripts/context-io.ts: noteId (~L684), appendNote (~L693-731, writes via `atomicWrite(join(notesDir, \`${id}.md\`))`), emitVerdict (~L812-855 — a SECOND direct note writer that calls `atomicWrite(join(notesDir, ...))` DIRECTLY, NOT via appendNote; its `by` is the fixed GATE_IDENTITY but its `at` is a parameter), atomicWrite (~L628), validate (~L530-621, esp. the required-fields loop ~L560 and the reserved-identity rule ~L578), assertSafeTask (~L87), the imports block (~L32-44, currently `import { join } from "node:path"`), GATE_IDENTITY (~L104), admitAndAppend (~L1337-1421, both branches call appendNote).
    - scripts/admission-server.ts: handleProposeNote (~L124-158) — the channel persistence path.
    - The red-team repro to reproduce RED-first: appendNote("ATK",{by:"x/../../../VICTIM/notes/INJECTED",kind:"claim",...}) currently lands the file in VICTIM/notes (see 25-VERIFICATION.md post-25-11 banner GAP-R6-1).
  </read_first>
  <behavior>
    - RED (held-out, via appendNote): admitAndAppend("ATK", note with by="x/../../../VICTIM/notes/INJECTED", kind="claim", valid at/confidence, empty verified_by) under explicit temp contextRoot/repoRoot REFUSES (id:null, findings name the fault) and writes NO file under VICTIM/notes (and none escaping the context tree). Same for an `at` carrying `/` + `..`.
    - RED (held-out, via the SIBLING writer emitVerdict): emitVerdict called with an `at` carrying a path separator + `..` is REFUSED (throws) with nothing written outside the task's notes dir — proves containment is a property of the WRITE, covering emitVerdict's direct atomicWrite too (W1).
    - RED (held-out, via the REAL channel): a propose_note SOFT-kind (kind="claim") call through handleProposeNote (CLAUDE_PROJECT_DIR set to a temp root — see Task 2's trusted-root model) with a traversal `by` returns an isError result and writes nothing into the VICTIM task's notes dir.
    - POSITIVE control: a legit claim (by="software-engineer") persists exactly one file under its own task's notes dir; a `by: §14-gate` verdict via emitVerdict (legit ISO `at`) still writes (the reserved § identity is NOT rejected).
    - Severity/format invariance: noteId output for a legit note is byte-identical to pre-change (formula + nonce untouched).
  </behavior>
  <action>
    Implement the PRIMARY structural fix as a SINGLE shared chokepoint (W1, the Phase-22 round-8
    one-chokepoint lesson — make containment a property of the WRITE so nothing can drift): add a small
    helper writeNoteFile(notesDir, id, text) that computes the final path join(notesDir, `${id}.md`),
    asserts the resolved final path stays strictly inside the resolved notes dir — the resolved final
    path must begin with the resolved notes dir plus the platform path separator — throwing and naming the
    fault on violation (fail-closed, nothing written), then mkdir + atomicWrite. Route BOTH note-file
    writers through it: appendNote (replace its inline mkdir+atomicWrite) AND emitVerdict (replace its
    DIRECT atomicWrite — it is a second writer that does NOT go through appendNote, so a containment assert
    living only in appendNote would NOT cover it; a future `at`-reachable change to emitVerdict must not be
    able to re-open the hole). Add `resolve` and `sep` to the existing node:path import. This is a
    post-compute containment assertion that does NOT alter noteId's formula or the nonce, and it covers
    every caller (admitAndAppend's gated and non-gated branches, the propose_note channel, the gate's
    verdict emission). Because containment operates on the resolved filesystem path, it cannot be defeated
    by a new metacharacter spelling (the anti-whack-a-mole posture from the project safety lesson). KNOWN
    LIMITS to leave documented, NOT patched: a lexical resolve() does not follow symlinks (a symlinked
    notes dir would pass — bounded by the same-uid direct-FS residual, since the channel can only
    atomicWrite a .md, not plant a symlink); and an unbound `task` slug can still file a SOFT advisory note
    into a sibling task dir (arbitrary-write OUTSIDE the context tree is what this closes).
    ADD a CO-PRIMARY (load-bearing — NOT mere belt-and-suspenders) reject in validate(): for the `by` and
    `at` provenance scalars (when present), push a structural FAIL if the value contains a path separator,
    a NUL byte, any C0 control character, or the parent-directory sequence (two consecutive dots). This is
    the structural guard for emitVerdict's `at` and also covers the CLI validate path and the compaction
    carve-out oracle (which call validate); do NOT weaken or skip it. (A forged/precomputed `id` is caught
    by the path-containment, not validate — validate does not inspect `id`.) It MUST NOT reject the
    reserved `by: §14-gate`
    identity (the section-sign U+00A7 is none of those metacharacters) nor a legitimate ISO-8601 `at`
    (single dots between digits are not the parent-dir sequence). Do NOT edit admit() (W-B span-hash
    freeze) or hooks/guard.ts (D-02). Rebuild the committed scripts/context-io.js (`npm run build`) so
    freshness stays 0.
    Write the RED tests in the <behavior> block FIRST (assert refusal + no-escape), confirm they fail
    against the pre-fix committed .js, then implement until GREEN. Add the channel-level SOFT-kind RED in
    scripts/admission-server.test.ts (coordinate with Task 2's CLAUDE_PROJECT_DIR trusted-root model).
  </action>
  <verify>
    <automated>npm run build && npx vitest run scripts/context-io.test.ts scripts/admission-server.test.ts --exclude '**/scripts/e2e/**' --testTimeout=30000 && node scripts/freshness.js && git diff --quiet hooks/guard.ts</automated>
  </verify>
  <done>A traversal `by`/`at` (via appendNote/admitAndAppend, via the sibling writer emitVerdict, AND via the propose_note SOFT-kind channel) is refused with nothing written outside the context tree; both writers route through the shared writeNoteFile chokepoint; the co-primary validate() reject is in place; legit notes and the §14-gate verdict still write; admit() freeze test + guard.ts diff clean; committed context-io.js fresh (freshness exit 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-R6-2 — the admission server derives the governance/persist root from the trusted source (CLAUDE_PROJECT_DIR), never agent-supplied args</name>
  <files>scripts/admission-server.ts, scripts/admission-server.js, scripts/admission-server.test.ts</files>
  <read_first>
    - scripts/admission-server.ts: PROPOSE_NOTE_TOOL.inputSchema (~L66-112, esp. the `contextRoot`/`repoRoot` properties ~L102-109), handleProposeNote (~L124-158, esp. the contextRoot/repoRoot reads ~L138-139 → admitAndAppend ~L142), the import of admitAndAppend (~L41-46), `import.meta.dirname` usage pattern.
    - hooks/admission-guard.ts: the hook reads the dial via readGovernanceConfigResult(process.env.CLAUDE_PROJECT_DIR) (~L120-122) — the trusted root this server must MATCH.
    - scripts/context-io.ts: admitAndAppend signature (task, note, body, contextRoot=DEFAULT_CONTEXT_ROOT, repoRoot=ROOT) (~L1337-1343); readGovernanceConfigResult resolves config under repoRoot (~L1212); the non-gated branch's W3 reject of an agent-supplied human:NAME (~L1400-1410); the gated branch (~L1353-1395).
    - The red-team repro: with the REAL dial off, propose_note args repoRoot=<temp dir human_admission:all> + contextRoot=<real context> drove the gated branch and persisted a forged human:eve note (25-VERIFICATION.md post-25-11 banner GAP-R6-2).
  </read_first>
  <behavior>
    - RED (held-out): with process.env.CLAUDE_PROJECT_DIR pointing at a temp root whose config dial is `off`, a handleProposeNote call that ALSO supplies args repoRoot=<temp root with human_admission:all> + contextRoot=<the off root's context> + kind="finding" + by="software-engineer" + verified_by="human:eve" returns an isError W3 refusal and persists NO note (no forged human:eve note, no GOV-02 disposed_by line) — the agent args do not change the effective dial/root.
    - RED (reverse direction): with CLAUDE_PROJECT_DIR pointing at a temp root whose dial is `high-severity` (or `all`), an agent-supplied repoRoot pointing at an `off` root does NOT downgrade gating — a high-severity finding without a valid human stamp is still refused.
    - POSITIVE control: with CLAUDE_PROJECT_DIR pointing at a temp root configured for the desired dial, a legit note (routine under high-severity, or a properly human-stamped gated note when the hook would authorize it at the persist tier's stamp check) persists exactly once under that trusted root.
    - The tool advertises no `repoRoot`/`contextRoot` governance inputs (inputSchema properties removed); supplying them is inert.
  </behavior>
  <action>
    Make the server derive its governance/persist root from the SAME trusted source the per-call hook
    uses (D-01): add a helper that returns process.env.CLAUDE_PROJECT_DIR when it is a non-empty string,
    else the server's own resolved repo root (join(import.meta.dirname, "..") — equal to context-io's
    ROOT and to admitAndAppend's repoRoot default). In handleProposeNote, set repoRoot to that trusted
    root and contextRoot to its `.grugops/context` subpath, and pass BOTH to admitAndAppend — IGNORING
    any agent-supplied `args.contextRoot`/`args.repoRoot`. Remove the `contextRoot` and `repoRoot`
    properties from PROPOSE_NOTE_TOOL.inputSchema (they were a test-only convenience that became an
    attacker-controlled governance-root input); they are neither required nor honored. Now the hook tier
    and the persist tier both resolve the dial/ledger/context from CLAUDE_PROJECT_DIR — they cannot
    diverge, so the off-mode W3 defeat and the forged-disposed_by audit fabrication are closed.
    Update the EXISTING admission-server.test.ts cases that currently pass repoRoot/contextRoot as tool
    args so they instead set process.env.CLAUDE_PROJECT_DIR (save/restore it around each case) to point
    at the temp root — this mirrors the real trust model (a trusted env, never a tool arg). Do NOT edit
    context-io.ts admitAndAppend (its explicit-root parameters stay; the server simply always passes the
    trusted root). Do NOT change hooks/admission-guard.ts's LOGIC (the hook already reads
    CLAUDE_PROJECT_DIR correctly) — its over-claiming COMMENTS are reconciled separately in Task 3 (W4).
    Rebuild the committed scripts/admission-server.js (`npm run build`) so freshness stays 0.
    Write the RED tests FIRST, confirm they fail against the pre-fix committed .js, then implement to GREEN.
  </action>
  <verify>
    <automated>npm run build && npx vitest run scripts/admission-server.test.ts scripts/context-io.test.ts --exclude '**/scripts/e2e/**' --testTimeout=30000 && node scripts/freshness.js && git diff --quiet hooks/guard.ts</automated>
  </verify>
  <done>An agent-supplied repoRoot/contextRoot cannot change the effective gate-or-persist root; under a real `off` dial a forged human:NAME note cannot be persisted via the channel (W3 holds under the trusted root); the hook and server read the identical dial from CLAUDE_PROJECT_DIR (stable per session); committed admission-server.js fresh; guard.ts (the deploy guard) diff clean; admission-guard.ts LOGIC unchanged (only its comments are reconciled in Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: GAP-R6-3 — honest-scope SC1 / WF16 / context-note.md AND reconcile the source comments (W4) to a per-session-grant, per-note-capable model (prose/comment only, CLEAR voice)</name>
  <files>.planning/ROADMAP.md, .planning/phases/25-governance-on-a-dial/25-CONTEXT.md, agent-factory/workflows/16-context-read-write.md, agent-factory/contracts/context-note.md, hooks/admission-guard.ts, hooks/admission-guard.js, scripts/admission-server.ts, scripts/admission-server.js</files>
  <read_first>
    - .planning/ROADMAP.md: Phase 25 Success Criteria item 1 (the long restated-round-6 paragraph; currently says the hook "validates the agent-supplied human:<name> stamp against it PER NOTE" and "the disposition attributed PER NOTE in the GOV-02 ledger ... per-entry — no session blanket").
    - .planning/phases/25-governance-on-a-dial/25-CONTEXT.md §A′ (~L88-125) — the SC1 restatement mirrored verbatim with the ROADMAP wording, plus D-07 (~L142) and the disposed_by semantics.
    - agent-factory/workflows/16-context-read-write.md line ~21 (the `human:<name>` un-forgeable-signal paragraph: "validates the agent-supplied human:<name> stamp against it PER NOTE ... binds each disposition to the specific entry the named human disposed (per-entry, no session blanket — D-07)").
    - agent-factory/contracts/context-note.md lines ~107-116 (the `human:<name>` PER-NOTE paragraph).
    - hooks/admission-guard.ts COMMENTS ~L21-28 + ~L44-50 ("running FRESH PER CALL ... the disposition is PER NOTE (a fresh human decision each time), never frozen at launch"; "binds the agent-supplied stamp to the env-verified approver PER CALL/PER NOTE (D-07)") — the live over-claim contradicting R6-3's docs (W4).
    - scripts/admission-server.ts COMMENTS ~L14-27 (the block arguing a server-env / per-SESSION model is a "PER-ENTRY disposition ... blanket ... audit-integrity failure") — must be reconciled so it explains the per-session-grant model honestly, not as a defeated alternative (W4).
    - scripts/admission-protocol-docs.test.ts — what the docs test asserts (H1, frontmatter order:16, self_fix_attempts / `UNKNOWN - verify` / READY_FOR_HUMAN_REVIEW references, exactly one 16-context-read-write reference per role file). None assert "PER NOTE"/"disposed_by", so honest-scoping is safe — but keep those asserted strings intact.
  </read_first>
  <action>
    Apply the human's 2026-06-26 decision (D-07): HONEST-SCOPE to a session-grant model — do NOT add a
    nonce or any new mechanism. In the four DOC files AND the two SOURCE comment blocks (W4 — so the docs
    and the code do not contradict each other), replace the over-claiming "PER NOTE / per-entry, no
    session blanket" framing with the accurate model in CLEAR PROFESSIONAL VOICE (no-fabrication, no
    caveman voice on a governance surface): a named human's GRUGOPS_ADMISSION_APPROVED_BY=<name> grant is
    SESSION-SCOPED — once exported it authorizes high-severity admissions under that name for the rest of
    the session; the per-call hook re-reads the FRESH env each call (so a mid-session unset takes effect
    immediately), and the human controls granularity by setting/unsetting the grant around a specific
    disposition. It is per-note CAPABLE (the human CAN gate each note by set/unset) but NOT a
    mechanically-enforced per-note nonce — one standing grant authorizes unlimited admissions under that
    name until unset. Define the GOV-02 ledger semantics precisely: `disposed_by: human:<name>` means
    "admitted under <name>'s session grant", NOT "individually reviewed each entry". For the admission-server.ts
    comment block, keep the TRUE point (a frozen server env cannot read a mid-session export, so the GATE
    must stay in the per-call hook, not the server) but stop framing the per-session grant as an
    "audit-integrity failure" the design avoids — it is the accepted, honestly-scoped model. Keep every
    accurate claim: the hook is a separate process reading the human-set session env the agent's own child
    env cannot reach (un-forgeable on CC); the four non-CC CLIs degrade to the in-script admit() refusal
    (D-04/D-05); the same-uid direct-FS-write residual stays documented as before. W4 is COMMENT-ONLY on
    admission-guard.ts/admission-server.ts (no logic change — neither is byte-frozen; guard.ts is the
    separate frozen deploy guard) — rebuild hooks/admission-guard.js + scripts/admission-server.js so a
    comment-only change keeps freshness 0. Make the ROADMAP SC1 and the 25-CONTEXT §A′ wording match (one
    wording, two places). Do NOT restate a raw .grugops/ write path next to a write token (WR-01/D-13).
    Preserve WF16's H1, frontmatter `order: 16`, and the self_fix_attempts / UNKNOWN-verify /
    READY_FOR_HUMAN_REVIEW references and the single 16-context-read-write reference per role file so
    admission-protocol-docs.test.ts stays green.
  </action>
  <verify>
    <automated>npm run build && npx vitest run scripts/admission-protocol-docs.test.ts scripts/config-governance-consistency.test.ts scripts/check-foundation-guards.test.ts scripts/admission-server.test.ts hooks/admission-guard.test.ts --exclude '**/scripts/e2e/**' --testTimeout=30000 && node scripts/check-foundation-guards.js && node scripts/freshness.js && git diff --quiet hooks/guard.ts</automated>
  </verify>
  <done>SC1 in ROADMAP and 25-CONTEXT §A′ read identically and describe per-session-grant/per-note-capable; WF16 + context-note.md AND the admission-guard.ts / admission-server.ts source comments all agree (no live per-note over-claim contradicting the docs); disposed_by is defined as "admitted under <name>'s session grant"; admission-protocol-docs + governance-consistency + foundation-guards + the hook/server suites green (no WR-01 trip); committed .js rebuilt and fresh (comment-only); guard.ts diff clean; no over/under-claim.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: SC1 round-7 closure gate — INDEPENDENT bash-grounded opus red-team vs the committed artifacts (HARD STOP; green suite is NOT proof, D-12)</name>
  <read_first>
    - /Users/olgeroeselg/.claude/projects/-Users-olgeroeselg-Projects-public-grugops/memory/grugops-safety-invariant-green-suite-insufficient.md (11 rounds: a green author suite has been necessary-but-insufficient every time; the executor hard-stop + orchestrator-dispatched independent bash-grounded red-team is the protocol that works)
    - .planning/phases/25-governance-on-a-dial/25-VERIFICATION.md post-25-11 banner (the GAP-R6-1/2/3 repros being closed) + 25-11-SUMMARY.md (the round-6 outcome)
    - the committed scripts/context-io.js, scripts/admission-server.js, hooks/admission-guard.js (the targets — never the .ts, never the author suites)
  </read_first>
  <action>
    The orchestrator (NOT the authoring executor) dispatches an INDEPENDENT bash-grounded opus-grade
    red-team against the COMMITTED artifacts (scripts/context-io.js + scripts/admission-server.js +
    hooks/admission-guard.js) to reproduce the GAP-R6-1/R6-2 closures, confirm the R6-3 wording is
    honest, verify the preserve-list, AND invent ≥1 novel probe — per the how-to-verify checks (i)-(v)
    below. This is a HARD-STOP blocking gate (D-12): do NOT auto-pass on the green author suite — it has
    been necessary-but-insufficient 11 straight times. Record the reproduced inputs, the invented probe,
    the allow/deny + persisted/not-persisted outcomes, and the preservation checks in 25-12-SUMMARY.md;
    if any bypass is bash-grounded, author a gaps_found verdict from the reproduction (→ round 8); then
    surface to the human for the resume signal.
  </action>
  <verify>
    <human-check>An independent bash-grounded opus red-team has reproduced (i)-(v) below vs the committed .js (R6-1 + R6-2 closed incl. via the propose_note channel; R6-3 wording honest; the preserve-list intact; ≥1 novel probe run) and the findings are recorded in 25-12-SUMMARY.md — not a self-reported author green suite (D-12).</human-check>
  </verify>
  <what-built>
    Tasks 1-3 close the three round-6 channel-reachable defects against the COMMITTED .js: GAP-R6-1
    (appendNote path-containment + validate charset reject — cross-task injection through the sanctioned
    writer refused), GAP-R6-2 (the server derives the governance/persist root from CLAUDE_PROJECT_DIR,
    not agent args — off-mode W3 defeat closed), GAP-R6-3 (SC1/WF16/context-note honest-scoped to a
    per-session-grant model; disposed_by semantics defined). Held-out RED tests + positive controls are
    green; committed context-io.js + admission-server.js rebuilt and fresh; guard.ts + admit() frozen;
    the command-string parser stays deleted; SC2 + the GOV-02 ledger + the SC3 floor untouched. This is
    the LAST task.
  </what-built>
  <how-to-verify>
    Per D-12 and the project safety lesson (a green author suite has been necessary-but-INSUFFICIENT 11
    straight times on this family), the orchestrator dispatches an INDEPENDENT bash-grounded opus
    red-team (its own stdin/JSON-RPC driver + a bash-grounded oracle that counts a bypass ONLY when a
    real file actually lands in the wrong place or a real ledger line is appended AND the channel allows
    it) against the COMMITTED scripts/context-io.js, scripts/admission-server.js, and
    hooks/admission-guard.js — never the .ts, never the author suites. It MUST confirm:
      (i)   GAP-R6-1 closed — NO `by`/`at`/`id` can escape the CONTEXT ROOT (arbitrary-file-write outside
            the context tree) through ANY note-file writer: probe EVERY writer, not just "appendNote
            callers" — appendNote/admitAndAppend, the propose_note SOFT-kind channel, AND the sibling
            direct writer emitVerdict (W1; emitVerdict atomicWrites directly, so confirm it routes through
            the shared writeNoteFile chokepoint and refuses a traversal `at`). A forged/precomputed `id`
            is also contained. The legit `by: §14-gate` verdict and normal notes still write (no
            over-block). CONFIRM-THE-DESIGN-INTENT, do NOT falsify the over-claim (W2): the documented
            in-scope limits — a SOFT advisory note filed into a SIBLING task dir via a legitimate `task`
            slug (the `task` arg is unbound; advisory notes are non-gated), and a symlinked notes dir — are
            EXPECTED residuals bounded by the same-uid direct-FS limit, NOT round-7 failures; report them
            as confirmed-known, not as bypasses of the stated guarantee.
      (ii)  GAP-R6-2 closed — given a stable per-session CLAUDE_PROJECT_DIR (server env launch-frozen, hook
            per-call), an agent-supplied repoRoot/contextRoot cannot change the effective gate/persist
            root; the hook and server read the same trusted root (CLAUDE_PROJECT_DIR); the off-mode
            forged-human:NAME persistence + forged disposed_by are gone.
      (iii) GAP-R6-3 — SC1 wording + disposed_by semantics are honest AND the source comments
            (admission-guard.ts / admission-server.ts) agree with the docs (no live per-note over-claim):
            per-session-grant / per-note-capable, no over- or under-claim (a documentation + comment
            review, not a code probe).
      (iv)  PRESERVATION — hooks/guard.ts byte-frozen (git diff --quiet exit 0); admit() span-hash freeze
            green; the deleted round-6 command-string tokenizer stays deleted (its identifiers grep to 0
            in scripts/admission-guard.{ts,js}); single-source isGatedNote/isHighSeverityRole intact (no
            second classifier); SC2 + the GOV-02 ledger shape preserved; the SC3 floor (garbage/typo dial
            + corrupt config → gate-or-stricter) preserved; `node scripts/freshness.js` exit 0.
      (v)   NOVELTY — the red-team must invent ≥1 NOVEL probe not enumerated above (e.g. a new
            write-location trick: precomputedId reuse, a symlinked notes dir, a CRLF/encoded separator in
            `by`; or a new config-root trick: a relative CLAUDE_PROJECT_DIR, a contextRoot/repoRoot split,
            an env-vs-arg race) and report the result.
    State plainly that a green suite is not proof. If ANY bypass is bash-grounded, author a gaps_found
    verdict from the reproduction (do NOT mark complete) → round 8. Resume ONLY on a clean independent
    red-team.
  </how-to-verify>
  <resume-signal>Type "approved" ONLY if the independent bash-grounded red-team found NO bypass (incl. its novel probe) and preservation holds; otherwise describe the reproduced bypass (with the bash-grounded evidence) to route to round 8.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → MCP propose_note args | Untrusted structured fields (task, by, at, verified_by, …) cross into the server; `by`/`at` flow into the on-disk filename via noteId |
| agent → governance config root | The dial/ledger/context root MUST come from a trusted env (CLAUDE_PROJECT_DIR), never an agent tool arg |
| context-io writers → filesystem | appendNote AND emitVerdict resolve a `${id}.md` path; an `id` carrying `..`/separators escapes the context tree unless contained at the shared write chokepoint |
| human session env → hook/server | GRUGOPS_ADMISSION_APPROVED_BY + CLAUDE_PROJECT_DIR are human/harness-set, unreachable by the agent's child env |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-R6-1 | Tampering / Elevation | context-io note writers (appendNote + emitVerdict) / noteId filename | critical | mitigate | A SINGLE shared writeNoteFile chokepoint carrying the path-containment assertion (resolved final path must stay inside the resolved notes dir; fail-closed), routed through by BOTH appendNote AND the sibling direct writer emitVerdict, so a future `at`-reachable change to either cannot re-open the hole + a CO-PRIMARY `by`/`at` metacharacter reject in validate(); held-out RED via appendNote, emitVerdict, AND the propose_note SOFT-kind channel (Task 1). Documented in-scope limits: symlinked notes dir + sibling-task-via-unbound-`task`-slug (bounded by the same-uid direct-FS residual) |
| T-25-R6-2 | Spoofing / Tampering / Repudiation | admission-server handleProposeNote governance root | high | mitigate | Server derives dial/ledger/context root from CLAUDE_PROJECT_DIR (the hook's trusted source); remove `repoRoot`/`contextRoot` from inputSchema and do not honor them; RED proving off-mode W3 holds + no forged disposed_by (Task 2) |
| T-25-R6-3 | Repudiation / Information disclosure | SC1 wording + GOV-02 disposed_by semantics | medium | mitigate | Honest-scope to per-session-grant / per-note-capable; define disposed_by as "admitted under <name>'s session grant" — no over-attribution (Task 3) |
| T-25-R6-REG | Tampering | the proven round-6 gate (guard.ts, admit(), single-source isGatedNote, deleted command-string parser, SC2/GOV-02/SC3) | high | mitigate | Preserve-list enforced as prohibitions + the closing independent red-team's preservation pass (Task 4) |
| T-25-SC | Tampering | dependency supply chain | low | accept | No package installs — zero host runtime deps preserved (D-13); only node: stdlib + tsc-built committed .js; nothing to audit this round |
</threat_model>

<verification>
Phase-level checks after all tasks:
- `npm run build && node scripts/freshness.js` → exit 0 (committed context-io.js + admission-server.js + admission-guard.js are faithful rebuilds; freshness covers scripts/ and hooks/).
- `git diff --quiet hooks/guard.ts` → exit 0 (the deploy guard is the byte-frozen file this round; admission-guard.ts changes are COMMENT-ONLY in Task 3/W4 with no logic change, and admit()'s freeze is proven by its span-hash test).
- `npx vitest run --exclude '**/scripts/e2e/**' --testTimeout=30000` → green, including the admit() span-hash freeze, isGatedNote/isHighSeverityRole behavior, the floor-invariance sweep (SC3), config-governance-consistency (SC2), admission-protocol-docs, and the new held-out RED + positive controls.
- The deleted round-6 command-string tokenizer stays deleted (its identifiers grep to 0 in scripts/admission-guard.ts and scripts/admission-guard.js).
- The BLOCKING closing checkpoint: an independent bash-grounded opus red-team vs the committed .js (the D-12 gate) — green suite is necessary-not-sufficient.
</verification>

<success_criteria>
- GAP-R6-1 closed: a forged `by`/`at`/`id` cannot escape the context root through ANY note writer (appendNote/admitAndAppend, the propose_note SOFT-kind channel, AND the sibling writer emitVerdict — all routed through the shared writeNoteFile chokepoint); the co-primary validate() reject holds; legit + §14-gate notes still write; noteId formula/nonce unchanged; the symlink + unbound-`task`-slug limits are documented (same-uid residual).
- GAP-R6-2 closed: given a stable per-session CLAUDE_PROJECT_DIR, the server reads the governance/persist root from CLAUDE_PROJECT_DIR; agent repoRoot/contextRoot are inert; off-mode W3 holds (no forged human:NAME note, no forged disposed_by).
- GAP-R6-3 resolved: SC1 (ROADMAP + 25-CONTEXT §A′) + WF16 + context-note.md AND the admission-guard.ts / admission-server.ts source comments honestly scoped to per-session-grant/per-note-capable (docs and source agree); disposed_by defined as "admitted under <name>'s session grant".
- Preservation holds: guard.ts + admit() frozen; command-string parser stays deleted; single-source classifiers intact; SC2 + GOV-02 ledger + SC3 floor untouched; freshness 0; no WR-01 trip.
- The independent bash-grounded opus red-team (incl. ≥1 novel probe) finds NO bypass; the checkpoint is NOT resolved on a green author suite alone (D-12).
</success_criteria>

<artifacts>
## Artifacts this phase produces
- `scripts/context-io.ts` (+ rebuilt `scripts/context-io.js`): a NEW shared `writeNoteFile(notesDir, id, text)` chokepoint (path-containment assert + atomicWrite) routed through by BOTH `appendNote` AND `emitVerdict` (the two note-file writers); a CO-PRIMARY (load-bearing) `by`/`at` path-metacharacter reject in `validate()`. noteId/nonce + admit() unchanged.
- `scripts/admission-server.ts` (+ rebuilt `scripts/admission-server.js`): trusted-root derivation from CLAUDE_PROJECT_DIR; `repoRoot`/`contextRoot` removed from the tool inputSchema and not honored for governance/persistence.
- `scripts/context-io.test.ts` + `scripts/admission-server.test.ts`: held-out RED cross-task-injection tests via BOTH writers (appendNote/admitAndAppend AND emitVerdict) + config-root-divergence tests (built via the real channel) + positive controls; existing server tests migrated to the CLAUDE_PROJECT_DIR trusted-root model.
- `hooks/admission-guard.ts` (+ rebuilt `hooks/admission-guard.js`) + `scripts/admission-server.ts` comment blocks (W4): source comments reconciled to the per-session-grant model so the code does not contradict the docs (comment-only, no logic change).
- `.planning/ROADMAP.md` Phase 25 SC1 + `.planning/phases/25-governance-on-a-dial/25-CONTEXT.md` §A′ (restated in lockstep) + `agent-factory/workflows/16-context-read-write.md` + `agent-factory/contracts/context-note.md`: honest per-session-grant/per-note-capable wording; disposed_by semantics.
- The closing independent bash-grounded opus red-team record (the D-12 closure or a gaps_found verdict → round 8).
</artifacts>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-12-SUMMARY.md` when done.
</output>

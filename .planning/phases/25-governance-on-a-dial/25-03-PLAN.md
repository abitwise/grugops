---
phase: 25-governance-on-a-dial
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/floor-invariance.test.ts
autonomous: true
requirements: [GOV-01, GOV-02]
user_setup: []

must_haves:
  truths:
    - "`context-io.ts admit()` refuses a high-severity finding (`by ∈ {security-nfr, architect-design, release-manager}`) that lacks a `human:NAME` stamp when `human_admission ≠ off` — it pushes a finding NAMING the fault and NEVER silently rewrites the note (D-04, the no-fabrication floor)."
    - "The D-04 tier reads the dial via the shared `readGovernanceConfig` (Plan 25-01) — the SAME read path the hook uses (OQ-3); under `human_admission: off` (lean default) admission is unchanged (no new refusal)."
    - "Under `audit_retention: retained`, `admit()` appends exactly ONE JSONL event per admission/disposition to a single global `.grugops/audit/admissions.jsonl`, using a fixed-key order (reusing the `toJsonl()` pattern); under `git` (lean default) it writes NOTHING new (GOV-02, D-08/D-10)."
    - "`audit_retention: retained` is the governance-RECORD ledger — NOT a body-verbosity knob; it never duplicates `compaction: retain-raw` (D-09); the ledger event records the admission record (id/kind/by/severity/stamp/disposing-human/at), never the note body."
    - "Every `human_admission` and `audit_retention` value — including bogus/garbage strings — leaves all four floor invariants REFUSING (self-stamp refused, no-fabrication holds, `quality.test_integrity` has no `off`, the deploy guard `hooks/guard.ts` is byte-unchanged); the governance dials only ADD admission strictness, never subtract a floor (SC3, D-12)."
  artifacts:
    - path: "scripts/context-io.ts"
      provides: "admit() D-04 high-severity-lacking-human-stamp refusal (defense-in-depth, the 4 non-CC CLIs) + the GOV-02 retained-mode ledger writer (reuses toJsonl fixed-key order)"
      exports: ["admit"]
    - path: "scripts/context-io.test.ts"
      provides: "unit coverage for the D-04 refusal (refuse + name, never rewrite) and the GOV-02 ledger write/no-write seam (temp dir, both retained and git branches)"
      contains: "admit"
    - path: "scripts/floor-invariance.test.ts"
      provides: "the SC3 dial-value sweep (incl. bogus/garbage strings) asserting all four floor invariants still refuse + the structural dials-only-tighten guarantee"
      contains: "human_admission"
    - path: ".grugops/audit/admissions.jsonl"
      provides: "the single global append-only governance audit ledger, written at runtime ONLY under audit_retention: retained"
      contains: "id"
  key_links:
    - from: "scripts/context-io.ts admit()"
      to: "scripts/context-io.ts readGovernanceConfig (Plan 25-01)"
      via: "reads human_admission (D-04 gate) and audit_retention (ledger branch) via the ONE shared read path so the hook and admit() cannot diverge (OQ-3)"
      pattern: "readGovernanceConfig"
    - from: "scripts/context-io.ts admit() (retained branch)"
      to: ".grugops/audit/admissions.jsonl"
      via: "append one JSONL event (fixed-key order, toJsonl-style) only when audit_retention === retained"
      pattern: "admissions\\.jsonl|audit"
    - from: "scripts/floor-invariance.test.ts"
      to: "the four floor invariants (refuse-self, no-fabrication, test_integrity no-off, guard.ts byte-frozen)"
      via: "sweep every dial value incl. garbage; assert each invariant still refuses; assert dials only tighten"
      pattern: "test_integrity|refuse-self|guard"
  prohibitions:
    - statement: "The governance dials only TIGHTEN — there is no code path where setting a `human_admission` or `audit_retention` value (including a bogus/garbage value) opens a floor bypass. This is a STRUCTURAL guarantee, not just a passing test (SC3, D-12, [[grugops-safety-invariant-green-suite-insufficient]])."
      status: must-hold
      verification: "floor-invariance.test.ts sweeps every value incl. garbage and asserts all four invariants still refuse; the structural argument is that the dials gate admission strictly (add a refusal) and never remove an existing refusal; independently red-teamed."
    - statement: "Severity is the authoring role `by` ONLY — there is NO self-declared `severity` field on the note (it would be gameable downward, D-06)."
      status: must-hold
      verification: "the D-04 classification reads `by ∈ {security-nfr, architect-design, release-manager}`; grep shows no note-level `severity:` field is read or trusted; the floor sweep confirms a relabeled note is still caught upstream by validate()'s impersonation FAIL."
    - statement: "`admit()` NEVER silently rewrites a note that lacks the human stamp — it refuses and NAMES the fault, exactly like the existing stampless-finding refusal (the no-fabrication floor)."
      status: must-hold
      verification: "the D-04 unit asserts admit() RETURNS a finding string naming the fault and that the input note text is unchanged (no write occurs on refusal)."
    - statement: "`audit_retention: retained` is NOT a duplicate of `compaction: retain-raw` (D-09) — the ledger records the admission/governance RECORD, never the note BODY; `git` mode writes nothing new."
      status: must-hold
      verification: "the ledger event keys are admission-record fields (id/kind/by/severity/verified_by/disposed_by/at), not the note body; the git-mode unit asserts zero new files; grep shows no overlap with the compaction code path."
    - statement: "The SC3 floor sweep + an INDEPENDENT red-team are the proof, not a green suite — for a safety invariant a passing suite is insufficient (the terminal project lesson)."
      status: must-hold
      verification: "before /gsd-verify-work an independent opus-grade probe confirms no dial value opens a bypass and the deploy guard is byte-unchanged; documented, not self-reported."
---

<objective>
Add the GOV-01 defense-in-depth tier and the GOV-02 audit ledger to `scripts/context-io.ts`, and prove the SC3 un-dialable floor adversarially. Three pieces: (1) `admit()` refuses a high-severity finding lacking a `human:NAME` stamp when `human_admission ≠ off`, reusing the existing refuse-and-name path (it never rewrites the note) — the weaker, self-settable tier (D-05) that covers the 4 non-CC CLIs at the script level; (2) under `audit_retention: retained`, `admit()` appends one fixed-key JSONL event per admission/disposition to a single global `.grugops/audit/admissions.jsonl` (reusing the `toJsonl()` shape), while `git` (lean default) writes nothing new; (3) `scripts/floor-invariance.test.ts` sweeps every governance dial value — including bogus/garbage strings — and asserts all four floor invariants still REFUSE, plus the structural guarantee that the dials only ADD strictness.

Purpose: GOV-01 + GOV-02 become a coherent pair — GOV-01 adds the human gates, GOV-02 decides how durably the record of those gates (and all admissions) is retained — while SC3 proves that no dial value, lean or paranoid or garbage, can ever open the un-dialable safety floor.
Output: the D-04 in-script refusal, the retained-mode ledger writer, their unit coverage in `context-io.test.ts`, and the SC3 floor-sweep test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-RESEARCH.md
@.planning/phases/25-governance-on-a-dial/25-PATTERNS.md
@.planning/phases/25-governance-on-a-dial/25-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 25-03-01: admit() D-04 high-severity in-script refusal (defense-in-depth, never rewrite)</name>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts</files>
  <read_first>
    - scripts/context-io.ts:852-882 (admit() — the seam: it calls validate(text) first, returns findings if any, then parses the note and does the §14-gate cross-check, returns [] = admitted; the D-04 refusal hooks in here after validate, before admit succeeds)
    - scripts/context-io.ts:584-612 (validate()'s refuse-self FAIL set — the EXACT refuse-and-name shape to clone: it pushes a finding string naming the fault, NEVER rewrites the note)
    - scripts/context-io.ts:111-112 (GATE_STAMP_RE / HUMAN_STAMP_RE — `human:NAME` grammar = `/^human:[A-Za-z0-9._-]+$/`; D-04 checks for the presence of a `human:NAME` stamp on a high-severity finding)
    - scripts/context-io.ts:208-213 (isRecognizedFrontmatterLine — the single column-0 `key: value` grammar; the parse of `by` / `verified_by` reuses the existing parseNote, never a second grammar)
    - scripts/context-io.ts (the exported `readGovernanceConfig` helper from Plan 25-01 — D-04 reads `human_admission` via this ONE shared path so it cannot diverge from the hook, OQ-3)
    - scripts/context-io.test.ts (the existing admit()/validate() unit patterns + temp-dir helpers to extend; ~61KB — find the existing admit describe block and add the D-04 cases beside it)
  </read_first>
  <behavior>
    - admit() on a high-severity finding (`by: security-nfr`) with NO `human:NAME` stamp under `human_admission: high-severity` → returns a NON-EMPTY findings array whose message NAMES the fault (a high-severity governance entry needs a named-human disposition); the input note text is unchanged (no rewrite).
    - admit() on the SAME finding WITH a `verified_by: human:alice` stamp under `high-severity` → the D-04 refusal does NOT fire (the stamp is present; other existing checks still apply).
    - admit() on a routine finding (`by: software-engineer`) under `high-severity` → the D-04 refusal does NOT fire (routine roles are not high-severity).
    - admit() on a high-severity finding under `human_admission: off` (lean default) → the D-04 refusal does NOT fire (lean mode adds no human stop).
    - admit() on a high-severity finding under `human_admission: all` lacking a `human:NAME` stamp → the D-04 refusal fires (all admissions gated).
    - The refusal is additive: it pushes onto the existing findings array exactly like the refuse-self set; admit() still returns [] (admitted) only when no finding — including the D-04 one — is present.
  </behavior>
  <action>
    In `scripts/context-io.ts admit()` (L852-882), after the structural `validate(text)` returns clean and the note is parsed, add the D-04 defense-in-depth refusal. Read `human_admission` via the shared `readGovernanceConfig` (Plan 25-01) — the SAME read path the hook uses (OQ-3). Classify high-severity from the parsed note's `by ∈ {"security-nfr","architect-design","release-manager"}` (D-06; NO self-declared `severity` field — read `by` only, via the existing parseNote, never a second grammar). When the finding is high-severity AND `human_admission !== "off"` AND the note carries no `human:NAME` stamp in `verified_by` (test with `HUMAN_STAMP_RE`, context-io.ts:112), PUSH a finding that NAMES the fault — clone the exact refuse-and-name shape from the validate() refuse-self set (context-io.ts:596-600): a clear-voice message stating that a high-severity governance entry (security/architecture/release) requires a named human disposition (`verified_by: human:NAME`) under the active `human_admission` setting, and that admission is refused until a named human disposes it. NEVER silently rewrite the note (the no-fabrication floor) — only push the finding, exactly like the existing stampless-finding refusal.

    This is the WEAKER, self-settable tier (D-05 honest residual): unlike the hook (a separate process reading the human-set session env), an in-script check is settable in admit()'s own child env — so it is documented as defense-in-depth covering the 4 non-CC CLIs at the script level, NOT the un-forgeable primary. Do not over-build: this is one refusal branch mirroring an existing one.

    Voice: CLEAR PROFESSIONAL VOICE in the finding message and comments (CLAUDE.md hard rule for governance/audit surfaces).

    Rebuild the committed `.js`: `npm run build` then `npm run freshness` (0 drift; context-io.js is freshness-covered).

    Extend `scripts/context-io.test.ts` with a `describe("d-04 high-severity in-script refusal", …)` block covering the six behaviors above, using the existing temp-dir admit() test pattern: write each note text, point `readGovernanceConfig` at a temp `factory.config.json` (or the helper's explicit-path arg) per `human_admission` value, call `admit()`, and assert on the returned findings array (refusal present / absent) AND that the note text is unchanged on refusal (no rewrite). Import from `./context-io` (vitest runs TS).
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run scripts/context-io.test.ts -t "d-04"</automated>
  </verify>
  <acceptance_criteria>
    - `admit()` reads `human_admission` via the shared `readGovernanceConfig` (grep shows the import; no second config read path).
    - `npm run freshness` exits 0 (committed context-io.js fresh).
    - `npx vitest run scripts/context-io.test.ts -t "d-04"` is GREEN: refusal fires for a high-severity finding lacking `human:NAME` under `high-severity`/`all`; does NOT fire under `off`, with the stamp present, or for a routine role; the refusal NAMES the fault and the note text is unchanged (no rewrite).
    - The classification reads `by` only (grep shows no note-level `severity:` field trusted).
  </acceptance_criteria>
  <done>admit() refuses a high-severity finding lacking a `human:NAME` stamp when `human_admission ≠ off`, naming the fault and never rewriting the note (defense-in-depth for the 4 non-CC CLIs, D-04/D-05), reading the dial via the ONE shared path; the committed .js is fresh; the D-04 unit cases are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 25-03-02: GOV-02 audit-retention ledger writer (retained → one JSONL event; git → nothing)</name>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts</files>
  <read_first>
    - scripts/context-io.ts:896-907 (toJsonl() — the fixed-key-order JSONL emitter; reuse the fixed-order discipline for byte-reproducibility, D-10)
    - scripts/context-io.ts:852-882 (admit() — the write seam where a successful admission appends the ledger event; the ledger writes only AFTER admission is decided)
    - scripts/context-io.ts (the file-I/O helpers already used for writing context — mkdir/append patterns to reuse for the `.grugops/audit/` ledger; the sole sanctioned writer)
    - scripts/context-io.ts (the exported `readGovernanceConfig` from Plan 25-01 — read `audit_retention` via the SAME shared path)
    - .planning/phases/25-governance-on-a-dial/25-RESEARCH.md (Code Examples — the PROPOSED bounded event key set {id, kind, by, severity, verified_by, disposed_by, at}; OQ-2 recommendation = a single global `.grugops/audit/admissions.jsonl`)
    - scripts/context-io.test.ts (existing temp-dir write tests to extend)
  </read_first>
  <behavior>
    - Under `audit_retention: retained`, a successful (or human-disposed) admission appends exactly ONE JSONL line to `REPOROOT/.grugops/audit/admissions.jsonl`; the line is valid JSON with the fixed key order {id, kind, by, severity, verified_by, disposed_by, at}.
    - Two admissions under `retained` produce exactly two lines (append-only; never truncate/rewrite prior lines).
    - Under `audit_retention: git` (lean default), NO `.grugops/audit/` directory and NO ledger file is created (zero new artifacts; the audit stays implicit in git history).
    - The `.grugops/audit/` directory is created on demand only in `retained` mode.
    - `severity` in the event is the classification (high | routine) derived from `by`, NOT a note-body field (D-06); the event records the admission RECORD, never the note body (D-09).
  </behavior>
  <action>
    In `scripts/context-io.ts admit()`, add the GOV-02 ledger writer at the write seam (after admission is decided). Read `audit_retention` via the shared `readGovernanceConfig` (Plan 25-01). When `audit_retention === "retained"`, append ONE JSONL event to a single global ledger at `REPOROOT/.grugops/audit/admissions.jsonl` (OQ-2 recommendation — one durable end-to-end auditor trail; D-08 fixes the `.grugops/audit/` root). Reuse the `toJsonl()` fixed-key-order discipline (context-io.ts:896-907) so lines are byte-reproducible. The bounded event keys (RESEARCH Code Examples; D-10 keep it a ledger, not a subsystem): `id` (note id), `kind` (typed-note kind), `by` (authoring role), `severity` (the classification high|routine derived from `by` — NOT a note field), `verified_by` (the admitting stamp `§14-gate#ID` | `human:NAME`), `disposed_by` (the disposing human / session-var name, or null for routine), `at` (the note's ISO timestamp). Create the `.grugops/audit/` directory on demand (mkdir recursive) only in retained mode. When `audit_retention === "git"` (lean default), write NOTHING new — no directory, no file (today's behavior; the audit stays implicit in git history).

    D-09 DISCIPLINE: this ledger records the admission/governance RECORD; it MUST NOT write the note BODY and MUST NOT touch or duplicate the `compaction` (body-verbosity) code path. `audit_retention: retained` ≠ `compaction: retain-raw` (Pitfall 5). Keep the two code paths fully separate.

    Voice: CLEAR PROFESSIONAL VOICE in comments (audit/compliance surface).

    Rebuild: `npm run build` then `npm run freshness` (0 drift).

    Extend `scripts/context-io.test.ts` with a `describe("audit-ledger", …)` block (temp dir): (a) retained mode → one JSONL line per admission with the fixed key order, two admissions → two lines, valid JSON each; (b) git mode → assert NO `.grugops/audit/` directory and NO ledger file are created. Use the existing temp-root admit() pattern; point `readGovernanceConfig` at a temp config per `audit_retention` value.
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run scripts/context-io.test.ts -t "audit-ledger"</automated>
  </verify>
  <acceptance_criteria>
    - `admit()` reads `audit_retention` via the shared `readGovernanceConfig` (grep shows the import; no second config read path).
    - `npm run freshness` exits 0 (committed context-io.js fresh).
    - `npx vitest run scripts/context-io.test.ts -t "audit-ledger"` is GREEN: retained → one fixed-key JSONL line per admission (two admissions → two lines, append-only, valid JSON); git → no `.grugops/audit/` directory and no ledger file created.
    - The ledger event records the admission RECORD keys only (id/kind/by/severity/verified_by/disposed_by/at) — never the note body; grep shows no overlap with the compaction code path (D-09).
  </acceptance_criteria>
  <done>Under `audit_retention: retained`, admit() appends one fixed-key JSONL event per admission to the single global `.grugops/audit/admissions.jsonl` (append-only, byte-reproducible via the toJsonl shape); under `git` it writes nothing new; the ledger is the governance record, never the note body and never a compaction duplicate (D-09); the committed .js is fresh; the ledger unit cases are green.</done>
</task>

<task type="auto">
  <name>Task 25-03-03: SC3 floor-invariance sweep — every dial value (incl. garbage) still REFUSES + the structural dials-only-tighten guarantee</name>
  <files>scripts/floor-invariance.test.ts</files>
  <read_first>
    - hooks/guard.test.ts:22-57 (the child-spawn pattern — compose it with the config-load pattern; floor-invariance is a NEW value-sweep shape, no single exact analog)
    - scripts/config-queue-consistency.test.ts:23-30 (the config-load + repo-root resolution to mirror for the sweep's config setup)
    - scripts/context-io.ts:584-612 (floor invariant 1 — the refuse-self FAIL set; assert a self-stamped finding still refuses under EVERY dial value)
    - agent-factory/config/factory.config.md:121 (floor invariant 3 — `quality.test_integrity` has no `off` value; assert the allowed-value set excludes `off` regardless of governance dials)
    - hooks/guard.ts (floor invariant 4 — the deploy guard must be byte-unchanged; assert via the committed guard.js / a git-clean check or a content hash that admission work never touched it, D-02)
    - .planning/phases/25-governance-on-a-dial/25-CONTEXT.md (D-12 — the four floor invariants + the structural dials-only-tighten guarantee + the green-suite-insufficient lesson)
  </read_first>
  <behavior>
    - For EVERY `human_admission` value in {off, high-severity, all, "", "bogus", "OFF", "true", "1", a random garbage string}: a self-stamped finding (verified_by === by) STILL refuses (floor invariant 1, refuse-self — independent of the dial).
    - For EVERY `human_admission` AND `audit_retention` value incl. garbage: no-fabrication holds — admit() never silently rewrites a note to make it pass; a hollow/invalid-evidence stamp still refuses (floor invariant 2).
    - For EVERY governance dial value: `quality.test_integrity` has no `off` setting — the test-integrity floor is un-dialable (floor invariant 3; assert the config/twin allowed-value set never admits `off` for test_integrity).
    - For EVERY governance dial value: the deploy guard `hooks/guard.ts` is byte-unchanged (floor invariant 4; humans-hold-merge/deploy via the unchanged guard, D-02).
    - STRUCTURAL: a garbage `human_admission` value (unknown to the dial) is treated as gate-or-stricter, NEVER as `off`-equivalent that opens a hole — the dials only ADD admission strictness; there is no value (lean, paranoid, or garbage) that REMOVES a refusal. (Assert: a garbage `human_admission` does not cause a high-severity finding lacking a human stamp to ADMIT — it either refuses or is treated conservatively; it never silently admits what `off` would block-less. Document the structural argument: the dials add a refusal branch; none removes one.)
  </behavior>
  <action>
    Create `scripts/floor-invariance.test.ts` — the SC3 dial-value sweep. Compose the two analogs: the config-load + repo-root resolution from `config-queue-consistency.test.ts:23-30` and, where a guard is exercised, the child-spawn pattern from `hooks/guard.test.ts:22-57` (target the COMMITTED `.js`). Sweep EVERY `human_admission` and `audit_retention` value INCLUDING bogus/garbage strings ("", "bogus", "OFF", "true", "1", a random string) and assert all FOUR floor invariants still REFUSE:
    1. refuse-self: a self-stamped finding (verified_by === by) refuses under every dial value (assert via validate()/admit() returning the refuse-self finding — context-io.ts:596-600).
    2. no-fabrication: admit() never silently rewrites a note; a hollow/invalid-evidence stamp still refuses; the note text is unchanged on refusal.
    3. test-integrity un-dialable: `quality.test_integrity` has no `off` — assert the allowed-value set (config + twin) never admits `off` for test_integrity (factory.config.md:121).
    4. deploy guard byte-frozen: `hooks/guard.ts` is unchanged by any governance work (D-02) — assert via a git-clean check on guard.ts or a content check that the deploy-guard file/behavior is the committed one.

    Then assert the STRUCTURAL dials-only-tighten guarantee: a garbage/unknown `human_admission` value NEVER opens a bypass — it does not cause a high-severity finding lacking a `human:NAME` stamp to ADMIT (the dials add a refusal branch; no value removes one). Make the structural argument explicit in a comment: every governance dial path either ADDS an admission refusal (high-severity/all) or leaves admission unchanged (off/unknown→lean); there is NO code path where a dial value SUBTRACTS an existing floor refusal.

    Voice: CLEAR PROFESSIONAL VOICE in comments (this test owns the SC3 safety floor). This test is part of the proof — but per D-12 / [[grugops-safety-invariant-green-suite-insufficient]], a green sweep is NECESSARY-NOT-SUFFICIENT; the plan's must_haves require an INDEPENDENT red-team before the floor is considered proven.
  </action>
  <verify>
    <automated>npx vitest run scripts/floor-invariance.test.ts && git diff --quiet hooks/guard.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/floor-invariance.test.ts` exists and is GREEN: every `human_admission`/`audit_retention` value (incl. "", "bogus", "OFF", "true", "1", a random string) leaves all four floor invariants refusing.
    - The sweep asserts: refuse-self still refuses; no-fabrication holds (no silent rewrite); `quality.test_integrity` has no `off`; `hooks/guard.ts` is byte-unchanged (`git diff --quiet hooks/guard.ts` exits 0).
    - The structural dials-only-tighten guarantee is asserted: a garbage/unknown `human_admission` never admits a high-severity finding lacking a human stamp (no value subtracts a refusal).
    - The test composes the config-load + child-spawn-vs-committed-.js patterns (no bespoke parse/guard reimplementation).
  </acceptance_criteria>
  <done>`scripts/floor-invariance.test.ts` sweeps every governance dial value incl. garbage and proves all four floor invariants still refuse plus the structural dials-only-tighten guarantee; the deploy guard is byte-frozen; this is the SC3 proof scaffold, to be confirmed by an independent red-team (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note text → admit() | The note's `by` / `verified_by` is untrusted; admit() classifies severity from `by` (D-06) and refuses a high-severity finding lacking a `human:NAME` stamp. A relabeled `by` is already an impersonation FAIL upstream in validate(). |
| config value → admit() | `human_admission` / `audit_retention` are read via the shared `readGovernanceConfig`; an unknown/garbage value must never open a floor bypass (treated conservatively, never as a hole). |
| admit() → ledger file | Under `retained`, admit() (the sole sanctioned writer) appends the admission record; the ledger records the record, never the note body, and is append-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02 | Spoofing | a high-severity finding self-stamped or relabeled to dodge the in-script gate | mitigate | D-04 reads `by` (D-06, no self-declared severity) and requires a `human:NAME` stamp; the refuse-self set already catches a self-stamp; admit() refuses and NAMES the fault, never rewrites. This is the weaker self-settable tier (D-05) — the un-forgeable gate is the hook (Plan 25-02). |
| T-25-06 | Elevation of Privilege (dial bypass) | a governance dial value (incl. garbage) opening a floor bypass | mitigate | SC3 structural guarantee — the dials only ADD admission strictness; floor-invariance.test.ts sweeps every value incl. garbage and asserts all four invariants still refuse; an unknown value is treated conservatively, never as `off`-equivalent that opens a hole. Independently red-teamed (D-12). |
| T-25-09 | Tampering | `audit_retention: retained` writing the note body / duplicating compaction | mitigate | The ledger records admission-record keys only (id/kind/by/severity/verified_by/disposed_by/at), never the note body; the code path is fully separate from compaction (D-09, Pitfall 5). |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this phase installs NO external package (RESEARCH Package Legitimacy Audit: not applicable; typescript/vitest already present). No install task exists. |
</threat_model>

<verification>
- `npm run build && npm run freshness` exits 0 (committed context-io.js is a fresh faithful build).
- `npx vitest run scripts/context-io.test.ts -t "d-04"` green (D-04 refusal fires/abstains correctly; never rewrites).
- `npx vitest run scripts/context-io.test.ts -t "audit-ledger"` green (retained → one fixed-key line; git → nothing written).
- `npx vitest run scripts/floor-invariance.test.ts` green (every dial value incl. garbage → all four floor invariants refuse + structural guarantee).
- `git diff --quiet hooks/guard.ts` exits 0 (deploy guard byte-frozen, D-02).
- INDEPENDENT red-team (before /gsd-verify-work): confirms no dial value opens a bypass and the deploy guard is byte-unchanged — not self-reported green (D-12).
</verification>

<success_criteria>
- GOV-01 (D-04): admit() refuses a high-severity finding lacking a `human:NAME` stamp when `human_admission ≠ off`, naming the fault and never rewriting — defense-in-depth for the 4 non-CC CLIs, reading the dial via the ONE shared path.
- GOV-02 (D-08/D-10): under `retained`, one fixed-key JSONL event per admission lands in the single global `.grugops/audit/admissions.jsonl`; under `git`, nothing new — the governance record, never a compaction duplicate (D-09).
- SC3 (D-12): every governance dial value incl. garbage leaves all four floor invariants refusing; the dials only ADD strictness (structural); the deploy guard is byte-frozen; proven by the sweep + an independent red-team, not a green suite alone.
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-03-SUMMARY.md` when done. Record: the exact D-04 refusal condition (high-severity `by` set + `human_admission ≠ off` + no `human:NAME`) and confirmation it never rewrites; the ledger path (`.grugops/audit/admissions.jsonl`), the fixed event-key order, and the retained-vs-git branch behavior (D-09 distinction held); the floor-invariance value set swept (incl. garbage) and the four invariants asserted; confirm `git diff --quiet hooks/guard.ts` is clean and context-io.js is fresh. Flag for the verifier that the SC3 floor + the D-04 gate REQUIRE an independent red-team confirming no dial value opens a bypass — the sweep is necessary, not sufficient (D-12).
</output>
---
phase: 25-governance-on-a-dial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/config/factory.config.json
  - agent-factory/seed/.grugops/factory.config.json
  - agent-factory/config/factory.config.md
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/config-governance-consistency.test.ts
  - scripts/context-io.test.ts
autonomous: true
requirements: [GOV-02, GOV-01]
user_setup: []

must_haves:
  truths:
    - "Both `context.human_admission` (default `off`) and `context.audit_retention` (default `git`) exist under the `context` object on BOTH JSON surfaces and are deep-equal across them (GOV-02 SC2)."
    - "Zero-config still runs lean: a missing key — or the whole file — reads as its lean default (`human_admission`→`off`, `audit_retention`→`git`), never an error (read-at-use, default-on-absent, D-11)."
    - "The twin `factory.config.md` documents each new key by name, places both in the config-dial contract matrix, and crisply states that `audit_retention` (governance-record durability) is DISTINCT from `compaction` (body-verbosity) and never duplicates `retain-raw` (D-09)."
    - "A single exported `readGovernanceConfig` helper in `context-io.ts` is the ONE config-read path both the hook (Plan 25-02) and `admit()` (Plan 25-03) consume, so the two read paths cannot diverge (OQ-3)."
  artifacts:
    - path: "agent-factory/config/factory.config.json"
      provides: "context.human_admission + context.audit_retention keys (lean defaults off/git)"
      contains: "human_admission"
    - path: "agent-factory/seed/.grugops/factory.config.json"
      provides: "byte-identical seed copy of the two governance keys"
      contains: "audit_retention"
    - path: "agent-factory/config/factory.config.md"
      provides: "twin docs: context sub-fields rows, dial-contract matrix rows, default-on-absent, D-09 distinction"
      contains: "audit_retention"
    - path: "scripts/context-io.ts"
      provides: "exported readGovernanceConfig(repoRoot?) — read-at-use, default-on-absent, the SHARED config read"
      exports: ["readGovernanceConfig"]
    - path: "scripts/config-governance-consistency.test.ts"
      provides: "3-surface lockstep oracle for both GOV keys + the D-09 distinction"
      contains: "human_admission"
  key_links:
    - from: "scripts/context-io.ts readGovernanceConfig"
      to: "agent-factory/config/factory.config.json"
      via: "reads the context.human_admission / context.audit_retention keys at use, default-on-absent"
      pattern: "human_admission|audit_retention"
    - from: "scripts/config-governance-consistency.test.ts"
      to: "the 3 config surfaces"
      via: "deep-equal across JSONs + lean-default assertions + twin-documents-each-key"
      pattern: "toEqual|human_admission|audit_retention"
  prohibitions:
    - statement: "The two JSON surfaces (kit + seed) MUST stay byte-identical — the seed is never edited independently of the kit."
      status: must-hold
      verification: "diff agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json exits 0 (identical); the consistency test asserts deep-equal of the context object."
    - statement: "`audit_retention` MUST NOT be built as a second body-verbosity knob (no duplicate of compaction:retain-raw, D-09) — this task only ADDS config keys + a read helper; it builds NO ledger and NO admission logic."
      status: must-hold
      verification: "Plan 25-01 touches no admit()/ledger code; the twin asserts the distinct/never-retain-raw language; grep shows the ledger writer lives only in Plan 25-03."
    - statement: "`readGovernanceConfig` MUST fail-OPEN-to-LEAN-DEFAULT on a missing/unreadable/garbage config (never throw, never error) — zero-config lean is preserved. (NOTE: fail-CLOSED on a matched high-severity admit is the HOOK's job in 25-02, NOT this read helper's job — the helper only resolves the configured value or its lean default.)"
      status: must-hold
      verification: "context-io.test.ts: no-config-file → off/git; garbage value → returned verbatim (the consumer decides, not the reader); never throws."
---

<objective>
Lay the GOV-02 + GOV-01 config foundation: add `context.human_admission` (default `off`) and `context.audit_retention` (default `git`) under the existing `context` object across all three config surfaces in lockstep, document them in the twin (sub-fields + dial-contract matrix + default-on-absent + the D-09 distinction from `compaction`), and add ONE exported `readGovernanceConfig` helper in `context-io.ts` that both the Plan-25-02 hook and the Plan-25-03 `admit()` will consume so the two governance-config read paths cannot diverge.

Purpose: every downstream governance consumer (the hook, the in-script refusal, the ledger writer) reads the SAME key the SAME way. This is the foundation wave — it builds NO hook, NO admission logic, NO ledger (those are Plans 25-02 / 25-03).
Output: two new config keys (3 surfaces, lockstep), an exported config-read helper, and a 3-surface consistency test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-RESEARCH.md
@.planning/phases/25-governance-on-a-dial/25-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the two governance keys across all 3 config surfaces (lockstep) + the consistency oracle</name>
  <files>agent-factory/config/factory.config.json, agent-factory/seed/.grugops/factory.config.json, agent-factory/config/factory.config.md, scripts/config-governance-consistency.test.ts</files>
  <read_first>
    - agent-factory/config/factory.config.json:52-54 (the `context` object — currently `{ "compaction": "aggressive" }`; add the two keys beside `compaction`)
    - agent-factory/seed/.grugops/factory.config.json (kept byte-identical to the kit JSON — apply the SAME edit)
    - agent-factory/config/factory.config.md:88-92 (the `context` sub-fields table — add two rows, mirroring how `compaction` is documented at L92)
    - agent-factory/config/factory.config.md:104-109 (the queue.wip_limit vs wip_limits distinction block — MODEL the D-09 audit_retention-vs-compaction distinction on this exact shape)
    - agent-factory/config/factory.config.md:111-125 (the config-dial contract matrix — add two rows after L125 `context.compaction`, carrying the "GOV dials only tighten" language)
    - agent-factory/config/factory.config.md:127-139 (the zero-config defaults / default-on-absent doctrine — L135 "a missing `context.compaction` reads as `aggressive`"; extend with the two new keys)
    - scripts/config-queue-consistency.test.ts (the COMPLETE template: L23-30 surface paths, L32-55 deep-equal + lean-default, L57-79 twin-documents-each-key + the distinction assertion)
  </read_first>
  <behavior>
    - Test: kit.context deep-equals seed.context (lockstep across the 2 JSONs).
    - Test: kit.context.human_admission === "off" AND kit.context.audit_retention === "git" (lean defaults).
    - Test: the `context` object still carries `compaction: "aggressive"` (the new keys are ADDED, not a replacement).
    - Test: the twin matches /`human_admission`/ AND /`audit_retention`/ (each documented by name).
    - Test: the twin states the D-09 distinction — audit_retention (governance-record durability) is distinct from compaction (body-verbosity) and never duplicates retain-raw (match /distinct|independent|not.*compaction|never.*retain-raw/i).
    - Test: the twin documents the lean default-on-absent for each new key (human_admission absent → off; audit_retention absent → git).
  </behavior>
  <action>
    Add `"human_admission": "off"` and `"audit_retention": "git"` inside the `context` object on BOTH JSON surfaces (kit `agent-factory/config/factory.config.json` and seed `agent-factory/seed/.grugops/factory.config.json`), placed beside the existing `"compaction": "aggressive"`. Apply the IDENTICAL edit to both files so `diff` of the two stays clean (per GOV-02 SC2 lockstep; this is the same 3-surface atomic-dial pattern the `queue` object follows).

    In the twin `factory.config.md`: (a) add two rows to the `## context sub-fields` table (after the `compaction` row at L92) — `human_admission` (Allowed: `off`, `high-severity`, `all`; Default `off`; meaning per GOV-01: a NAMED human disposes high-severity entries — security/architecture/release — before admission; `off` = routine verified notes admit without a human stop), and `audit_retention` (Allowed: `git`, `retained`; Default `git`; meaning per GOV-02/D-08: durability of the admission/disposition RECORD — `git` leaves the audit implicit in git history, `retained` writes a durable append-only ledger under `.grugops/audit/` an auditor can read end-to-end). (b) Add two rows to the config-dial contract matrix (after L125 `context.compaction`), each carrying the un-dialable-floor language: the GOV dials only ADD admission strictness (tighten), they never subtract a safety floor (per SC3 / D-12). (c) Extend the default-on-absent doctrine paragraph (L135) so a missing `human_admission` reads as `off` and a missing `audit_retention` reads as `git`, exactly like a missing `context.compaction` reads as `aggressive`. (d) Add a crisp D-09 distinction block MODELED on the existing queue.wip_limit-vs-wip_limits block (L104-109): state that `audit_retention` governs governance-RECORD durability while `compaction` governs note-BODY verbosity — they are independent, and `audit_retention: retained` is NOT a duplicate of `compaction: retain-raw`.

    ALL governance/compliance/audit twin prose is CLEAR PROFESSIONAL VOICE (CLAUDE.md hard rule — no caveman voice on safety/compliance/audit surfaces).

    WR-01 watch (D-13): in the twin, reference the config keys and Workflow 16 by name; do NOT restate a raw `.grugops/audit/` or `.grugops/context/` write path beside a write token (`writeFileSync`/`Write`/`>>`/`echo`). Name the directory in descriptive prose only (the twin is NOT in the WR-01 SCAN set, but keep the discipline held per the Phase-24 empirical win).

    Create `scripts/config-governance-consistency.test.ts` by cloning `scripts/config-queue-consistency.test.ts` exactly (surface paths, loadJson, the describe/it shape). Assert the six behaviors above. Use the same `import { describe, it, expect } from "vitest"` (globals:false) and `import.meta.dirname` root resolution.
  </action>
  <verify>
    <automated>diff agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json && npx vitest run scripts/config-governance-consistency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `diff` of the two JSON surfaces exits 0 (byte-identical).
    - `npx vitest run scripts/config-governance-consistency.test.ts` is GREEN: deep-equal context object; lean defaults off/git; compaction still present; twin documents each key by name; twin carries the D-09 distinction; twin documents default-on-absent for each.
    - `node -e "JSON.parse(require('fs').readFileSync('agent-factory/config/factory.config.json'))"` parses (valid JSON).
  </acceptance_criteria>
  <done>Both governance keys exist on all 3 surfaces in lockstep with lean defaults; the twin documents them, places them in the dial matrix with the tighten-only language, states default-on-absent, and crisply distinguishes audit_retention from compaction (D-09); the consistency oracle is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the shared `readGovernanceConfig` helper to context-io.ts (read-at-use, default-on-absent) + rebuild the committed .js</name>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts</files>
  <read_first>
    - scripts/context-io.ts:1-43 (the module header + imports — `readFileSync`/`existsSync` already imported; `join` from node:path; ZERO host runtime deps rule — node:fs/path/crypto only)
    - scripts/context-io.ts:208-213 (isRecognizedFrontmatterLine — the single-grammar precedent; this task adds a DIFFERENT, simpler helper but follows the same "one grammar, exported, reused" discipline)
    - agent-factory/config/factory.config.md:135 (the read-at-use, default-on-absent doctrine the helper implements: a missing key — or the whole file — is the lean default, never an error)
    - scripts/freshness.ts:39,43,88 (ROOT = import.meta.dirname/.. ; OUTPUT_DIRS includes "scripts" — so context-io.js is freshness-covered; this is how tooling resolves repo root from a script's own location, OQ-3)
    - scripts/config-queue-consistency.test.ts:23-30 (how a test resolves the repo root + the config path — mirror for the helper's tests)
  </read_first>
  <behavior>
    - readGovernanceConfig(repoRoot) with NO config file present → returns { human_admission: "off", audit_retention: "git" } (lean defaults; never throws).
    - readGovernanceConfig pointed at a config with context.human_admission: "high-severity" → returns human_admission: "high-severity".
    - readGovernanceConfig pointed at a config with context.audit_retention: "retained" → returns audit_retention: "retained".
    - readGovernanceConfig pointed at a config whose context object is absent / whose file is unreadable / whose JSON is garbage → returns the lean defaults (never throws — fail-open-to-LEAN, because this is the READER; the hook in 25-02 is the one that fails CLOSED on a matched admit).
    - readGovernanceConfig returns a GARBAGE value verbatim if one is set (e.g. context.human_admission: "bogus" → "bogus") — the reader does NOT sanitize; consumers (hook / admit / floor-sweep) decide. This is what lets the SC3 floor-sweep prove bogus values still REFUSE.
  </behavior>
  <action>
    Add and `export function readGovernanceConfig(repoRoot?: string): { human_admission: string; audit_retention: string }` to `scripts/context-io.ts`. Resolve the repo root: when `repoRoot` is passed (tests + the hook pass it explicitly), use it; otherwise default to `join(import.meta.dirname, "..")` (the script's own location → repo root, exactly how `freshness.ts` resolves ROOT — OQ-3). Read `<repoRoot>/agent-factory/config/factory.config.json` IF it exists. The PRODUCTION/seed config the installer drops is `.grugops/factory.config.json` at the consumer repo root; accept an optional explicit config path so the hook (which gets `${CLAUDE_PROJECT_DIR}`) can point at the repo's `.grugops/factory.config.json` — i.e. the helper takes the directory to look in and tries the standard config location(s). Keep it bounded: read-at-use, default-on-absent.

    Semantics (D-11, factory.config.md L135): wrap the read+parse in try/catch; on ANY failure (missing file, unreadable, non-JSON, absent `context` object, absent key) return the lean default for that key (`human_admission`→`"off"`, `audit_retention`→`"git"`). NEVER throw. Return whatever string IS present verbatim (do NOT validate the value against the allowed set — that is the consumer's job; returning a garbage value verbatim is required so the 25-03 floor-sweep can prove garbage still refuses).

    Voice: CLEAR PROFESSIONAL VOICE in comments (trace/safety surface). Add a short header comment naming GOV-01/GOV-02, D-11 (read-at-use/default-on-absent), and that this is the SINGLE shared read path for both the admission-guard (25-02) and admit() (25-03) so they cannot diverge (OQ-3).

    Rebuild the committed `.js`: run `npm run build` (tsc) so `scripts/context-io.js` is a fresh faithful build, then `npm run freshness` must show 0 drift.

    Extend `scripts/context-io.test.ts` with a `describe("governance-config", …)` block covering the five behaviors above, using a temp dir (write a temp `agent-factory/config/factory.config.json` or pass an explicit path) for the value cases and a no-file case for the default. Import `readGovernanceConfig` from `./context-io` (the .ts — vitest runs TS).
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run scripts/context-io.test.ts -t governance-config</automated>
  </verify>
  <acceptance_criteria>
    - `readGovernanceConfig` is exported from `scripts/context-io.ts`.
    - `npm run freshness` exits 0 (committed context-io.js is a fresh faithful build — no drift).
    - `npx vitest run scripts/context-io.test.ts -t governance-config` is GREEN: no-config → off/git; each value read back; absent/garbage/unreadable → lean default (no throw); a set garbage value returned verbatim.
    - The helper imports ONLY node:fs/path/crypto (zero host runtime deps — grep the new code adds no new import of a non-node: module).
  </acceptance_criteria>
  <done>A single exported `readGovernanceConfig` helper reads the two governance keys read-at-use / default-on-absent (never throws), returns set values verbatim (incl. garbage), the committed context-io.js is fresh, and the helper's unit tests are green. This is the ONE config-read path both 25-02 and 25-03 import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → tooling | A `factory.config.json` (kit or repo-dropped `.grugops/`) is read by `readGovernanceConfig`; a missing/garbage/unreadable file is untrusted input that must degrade to lean defaults, never crash or escalate. |
| kit JSON ↔ seed JSON | The two surfaces must stay byte-identical; a drift between them is a config-integrity defect (the installer copies the seed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Denial of Service (crash) | `readGovernanceConfig` on a missing/garbage/unreadable config | mitigate | try/catch → lean default for each key; NEVER throw; unit-tested with no-file + garbage + unreadable cases. Zero-config lean preserved. |
| T-25-04 | Tampering (false-positive) | governance prose added to the twin | mitigate | The twin is not in the WR-01 SCAN set, but keep D-13 discipline: name keys + Workflow 16 in prose, never a raw `.grugops/` write path beside a write token. |
| T-25-CFG | Information Disclosure / Integrity | kit↔seed drift | mitigate | `diff` gate + the consistency test's deep-equal assertion; the foundation guard separately keeps the two JSONs byte-identical. |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this phase installs NO external package (RESEARCH Package Legitimacy Audit: not applicable; dev deps typescript/vitest already present). No install task exists. |
</threat_model>

<verification>
- `diff agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` exits 0.
- `npx vitest run scripts/config-governance-consistency.test.ts` green.
- `npm run build && npm run freshness` exits 0 (no drift).
- `npx vitest run scripts/context-io.test.ts -t governance-config` green.
- `npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial contract unaffected).
</verification>

<success_criteria>
- GOV-02 SC2: both keys present on all 3 surfaces in lockstep, lean defaults `off`/`git`, twin documents each, zero-config still runs lean (consistency oracle green).
- The shared `readGovernanceConfig` helper exists, is exported, read-at-use / default-on-absent, never throws, returns values verbatim — the ONE config path for 25-02 and 25-03.
- The committed `context-io.js` is fresh; foundation guards stay green.
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-01-SUMMARY.md` when done. Record: the exact `readGovernanceConfig` signature + config-location resolution rule (for 25-02 the hook and 25-03 admit() to import identically), and confirm the kit↔seed diff is clean.
</output>

---
phase: 25-governance-on-a-dial
plan: 11
type: execute
wave: 3
depends_on: ["25-09", "25-10"]
files_modified:
  - agent-factory/workflows/16-context-read-write.md
  - agent-factory/contracts/context-note.md
  - .planning/ROADMAP.md
  - .planning/phases/25-governance-on-a-dial/25-CONTEXT.md
autonomous: false
requirements: [GOV-01, GOV-02]

must_haves:
  truths:
    - "Workflow 16 and the context-note contract describe the Claude Code sanctioned admission path as the STRUCTURED mcp__grugops__propose_note channel gated by the separate PreToolUse admission-guard hook reading the human-set session env (D-01); the four non-CC CLIs degrade to the in-script admit() refusal + a prompt-level stop, documented honestly as not mechanically un-forgeable (D-04/D-05)."
    - "The restated SC1 is recorded in ROADMAP Phase 25 success criteria AND 25-CONTEXT.md, replacing the un-achievable 'genuinely un-forgeable point-of-effect' wording with the achievable invariant: the sanctioned channel is mechanically un-forgeable on CC (per-call hook + server-env suspenders), AND a human-APPROVED high-severity note IS admitted through it (the positive D-07 capability); the approval is a SESSION-SCOPED grant (the human sets GRUGOPS_ADMISSION_APPROVED_BY before launching CC / restarts the server); direct same-uid filesystem forgery bypassing the channel is a documented in-box residual backstopped by humans-hold-merge + the no-fabrication floor; the four non-CC CLIs degrade to the in-script refusal + prompt-level stop. The un-forgeable tier is claimed CONSERVATIVELY for Claude Code; server-env parity on other MCP-launching CLIs is 'verify per-CLI', not claimed."
    - "The irreducible same-uid residual + the CC-only un-forgeable tier + the autonomy=pr backstop are documented in CLEAR voice (no-fabrication): the residual is stated plainly (incl. a programmatic node -e appendNote AND a node -e emitVerdict that forges a LIVE green §14-gate verdict — the most powerful same-uid direct-node form, since it then lets Posture-B pass for forged findings), not hidden."
    - "An INDEPENDENT bash-grounded opus red-team, dispatched by the orchestrator against the COMMITTED artifacts, confirms (i) no high-severity note admits through the structured channel without the human disposition across an adversarial sweep AND the POSITIVE path works (a human-approved high-severity note IS admitted, stamped human:<name>), (ii) the same-uid residual is EXACTLY as documented (no NEW hole — incl. the node -e emitVerdict control), and (iii) guard.ts byte-frozen + SC2/GOV-02 + the SC3 floor preserved."
  artifacts:
    - "agent-factory/workflows/16-context-read-write.md (updated step 3 / admission lines)"
    - "agent-factory/contracts/context-note.md (updated verified_by / un-forgeable-signal lines)"
    - ".planning/ROADMAP.md Phase 25 success criteria (restated SC1)"
    - ".planning/phases/25-governance-on-a-dial/25-CONTEXT.md (restated SC1 + residual documentation)"
    - "25-11-SUMMARY.md Independent Both-Angle Red-Team section (the D-12 closure record)"
  key_links:
    - "WF16 / context-note.md -> reference the structured channel + the hook + WF16/config keys (never restate a raw .grugops/ write path beside a write token — WR-01/D-13)"
    - "restated SC1 in ROADMAP == restated SC1 in CONTEXT (one wording, two places)"
    - "the red-team drives the COMMITTED admission-server.js + admission-guard.js (never the .ts, never the author suites)"
  prohibitions:
    - "no kit-content false-positive: the prose MUST NOT trip guard_context_writes (WR-01) or any foundation guard — reference Workflow 16 / the config keys; never restate a raw .grugops/ write path adjacent to a write token (D-13)."
    - "admission-protocol-docs.test.ts MUST stay green: keep WF16's H1 (# Workflow: context read/write), frontmatter order: 16, the self_fix_attempts / UNKNOWN - verify / READY_FOR_HUMAN_REVIEW references, and exactly ONE 16-context-read-write reference in each of the 17 role files."
    - "the closing red-team MUST NOT be satisfied by the author green suite (D-12): green is necessary-not-sufficient; the independent bash-grounded opus red-team against the committed artifacts is the gate."
    - "no honesty inflation: SC1 wording must NOT claim the residual is closed; the same-uid direct-FS-write limit is stated plainly (no-fabrication)."
    - "guard.ts / context-io.ts / admission-guard.ts / admission-server.ts byte-frozen this plan: 25-11 changes prose + planning docs only; git diff --quiet on those four source files."
---

<objective>
Close round 6 honestly: update the single-source admission protocol (WF16 + the context-note contract)
to describe the structured channel + hook, restate SC1 to the achievable invariant, document the
irreducible same-uid residual in clear voice, and gate the whole phase on an INDEPENDENT bash-grounded
opus red-team (D-12) — the protocol that caught all 10 prior rounds, never a green author suite.

Purpose: complete the move-the-gate-to-point-of-effect change with the docs, the honest SC1, and the
adversarial proof, so the trace records what is actually guaranteed (the sanctioned channel is
un-forgeable on CC) and what is not (same-uid direct FS forgery — pre-existing, backstopped by
autonomy=pr). Output: the updated WF16/contract prose, the restated SC1 in ROADMAP + CONTEXT, the
residual documentation, and the blocking independent red-team confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-RESEARCH.md
@agent-factory/workflows/16-context-read-write.md
@agent-factory/contracts/context-note.md
@.planning/phases/25-governance-on-a-dial/25-09-PLAN.md
@.planning/phases/25-governance-on-a-dial/25-10-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 25-11-01: Update WF16 + the context-note contract to describe the structured channel + hook</name>
  <files>agent-factory/workflows/16-context-read-write.md, agent-factory/contracts/context-note.md</files>
  <read_first>
    - agent-factory/workflows/16-context-read-write.md:11,20-25 (the sanctioned-writer line + step 3 admission lines + the "un-forgeable human-set signal is layered in Phase 25 / admission-guard hook" reference to update)
    - agent-factory/contracts/context-note.md:15-17,98-113 (the sanctioned-writer line + the verified_by grammar + the "human:<name> ... un-forgeable signal ... PreToolUse admission-guard hook" lines to update)
    - scripts/admission-protocol-docs.test.ts (the assertions the prose must keep green: WF16 H1, order:16, self_fix_attempts/UNKNOWN-verify/READY_FOR_HUMAN_REVIEW, exactly-one role pointer)
  </read_first>
  <action>
    Update WF16 step 3 and the context-note contract so the admission protocol describes round 6 (D-01):
    on Claude Code the sanctioned admission path for a verified note is the STRUCTURED tool
    mcp__grugops__propose_note, gated by the separate PreToolUse admission-guard hook that reads the
    human-set session variable the agent's own child env cannot reach (the hook now reads final
    structured arguments, not a Bash command string). State that context-io remains the single
    sanctioned WRITER (the structured tool calls admitAndAppend -> appendNote internally), so "record
    only via context-io" still holds. Keep the four non-CC CLIs documented as degrading to the in-script
    admit() refusal + a prompt-level "stop, ask a named human," honestly not mechanically un-forgeable
    (D-04/D-05). Reference Workflow 16 / the config keys and the hook by name — do NOT restate a raw
    .grugops/ write path beside a write token (WR-01/D-13). Clear professional voice (safety surface).
    Preserve WF16's H1, frontmatter order:16, the self_fix_attempts / UNKNOWN - verify /
    READY_FOR_HUMAN_REVIEW references, and do not change the per-role 16-context-read-write pointers.
  </action>
  <verify>
    <automated>npx vitest run scripts/admission-protocol-docs.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - WF16 + context-note.md describe the structured mcp__grugops__propose_note channel + the hook, with context-io still the single writer and the non-CC degrade documented honestly (D-04/D-05).
    - guard_context_writes (WR-01) and the foundation guards do not false-positive on the new prose (no raw .grugops/ write path beside a write token).
    - admission-protocol-docs.test.ts stays green (H1, order:16, references, exactly-one role pointer).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 25-11-02: Restate SC1 (ROADMAP + CONTEXT) and document the irreducible same-uid residual</name>
  <files>.planning/ROADMAP.md, .planning/phases/25-governance-on-a-dial/25-CONTEXT.md</files>
  <read_first>
    - .planning/ROADMAP.md:211-217 (Phase 25 Success Criteria — SC1/SC2/SC3 wording to restate SC1 in)
    - .planning/phases/25-governance-on-a-dial/25-RESEARCH.md:638-671 (the recommended SC1 restatement + the brutally-honest residual to record)
    - .planning/phases/25-governance-on-a-dial/25-CONTEXT.md:52-86 (decisions D-01..D-05 — where the residual documentation belongs)
  </read_first>
  <action>
    Restate SC1 in BOTH the ROADMAP Phase 25 Success Criteria and 25-CONTEXT.md to the achievable
    invariant (replacing any "genuinely un-forgeable point-of-effect" framing): the SANCTIONED admission
    channel is mechanically un-forgeable on Claude Code (a structured, non-shell tool gated by a separate
    hook reading the human-set disposition, with the persisting server's own env as the suspenders); a
    high-severity finding cannot be admitted through it without a named human, AND a human-APPROVED
    high-severity note IS admitted through it stamped human:<name> (the positive D-07 capability). State
    the approval is a SESSION-SCOPED grant (the human exports GRUGOPS_ADMISSION_APPROVED_BY before
    launching CC, or restarts the server) and claim the un-forgeable tier CONSERVATIVELY for CC (server-env
    parity on other MCP-launching CLIs = "verify per-CLI"). Direct same-uid filesystem forgery bypassing
    the channel (a Write-tool drop, a Bash echo/cp/tee/redirection, a modified context-io copy, OR a
    programmatic node -e appendNote / node -e emitVerdict — the latter forging a LIVE green §14-gate
    verdict, the most powerful same-uid form) is a documented in-box residual, IRREDUCIBLE on a single-user
    box and PRE-EXISTING (the Bash hook never closed it), backstopped by autonomy=pr (humans hold merge) +
    the no-fabrication floor; the four non-CC CLIs degrade to the in-script refusal + a prompt-level stop.
    Document the residual + the CC-only tier + the backstop in CLEAR voice (no-fabrication — state it
    plainly, do not claim it is closed). Do not weaken SC2 or SC3.
  </action>
  <verify>
    <automated>grep -q "same-uid" .planning/phases/25-governance-on-a-dial/25-CONTEXT.md && grep -qi "sanctioned" .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP Phase 25 SC1 and 25-CONTEXT.md carry the identical restated SC1 invariant (channel un-forgeable on CC; the positive D-07 capability — an approved high-sev note IS admitted stamped human:<name>; session-scoped grant; CC-only conservative claim; same-uid direct-FS-write a documented residual; non-CC degrade).
    - The same-uid residual (incl. node -e appendNote AND node -e emitVerdict), the CC-only un-forgeable tier, and the autonomy=pr backstop are documented in clear voice; no wording claims the residual is closed.
    - SC2 and SC3 wording is unchanged / not weakened.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 25-11-03: SC1 round-6 closure gate — INDEPENDENT bash-grounded opus red-team vs the committed artifacts (HARD STOP; green suite is NOT proof, D-12)</name>
  <read_first>
    - .planning/phases/25-governance-on-a-dial/25-RESEARCH.md:608-628 (Q5 — the red-team's job CHANGES for a structured channel: confirm every sanctioned structured admit is gated, the dial fail-closed posture holds, and the residual is exactly the documented same-uid FS-write limit and nothing broader)
    - /Users/olgeroeselg/.claude/projects/-Users-olgeroeselg-Projects-public-grugops/memory/grugops-safety-invariant-green-suite-insufficient.md (10 rounds: a green author suite has been necessary-but-insufficient every time; the executor hard-stop + orchestrator-dispatched independent bash-grounded red-team is the protocol that works)
    - 25-09-SUMMARY.md + 25-10-SUMMARY.md (what was built; the RED->GREEN artifacts; the disclosed residual)
  </read_first>
  <action>
    The orchestrator (NOT the authoring executor) dispatches an INDEPENDENT bash-grounded opus-grade
    red-team against the COMMITTED artifacts (hooks/admission-guard.js + scripts/admission-server.js +
    scripts/context-io.js — never the .ts, never the author suites) to reproduce the SC1 round-6 closure
    per the how-to-verify checks (i)-(iii) below. This is a HARD-STOP blocking gate (D-12): do NOT
    auto-pass on the green author suite — it has been necessary-but-insufficient 10 straight times.
    Record the reproduced inputs, the invented structured attack, the allow/deny + persisted/not-persisted
    outcomes, the residual controls, and the preservation checks in 25-11-SUMMARY.md, then surface to the
    human for the resume signal.
  </action>
  <verify>
    <human-check>An independent bash-grounded opus red-team has reproduced (i)-(iii) below vs the committed .js and the findings are recorded in 25-11-SUMMARY.md — not a self-reported author green suite (D-12).</human-check>
  </verify>
  <what-built>
    The round-6 move-the-gate-to-point-of-effect change for GOV-01/SC1: a zero-dependency structured
    admission channel (mcp__grugops__propose_note, Plan 25-09) whose persistence routes solely through
    context-io.appendNote (single writer) via the additive admitAndAppend(approvedBy) combiner (reusing
    Posture-B + the GOV-02 ledger; admit()'s D-04 stays the degrade tier; an env-approved high-severity
    note is ADMITTED stamped human:<name>), and a retargeted PreToolUse hook (Plan 25-10) that reads the
    FINAL structured tool_input + the human-set session env and denies an un-approved high-severity (or
    every, under all) admission — with the entire 10-round command-string parser DELETED. This is the LAST task.
    Per the safety lesson and D-12, a passing author suite is necessary but NOT sufficient — the author
    suites passed and STILL missed a class 10 straight times. This checkpoint HARD-STOPS until an
    INDEPENDENT opus-grade adversary reproduces the closure against the COMMITTED .js. The orchestrator
    (not the authoring executor) dispatches the red-team.
  </what-built>
  <how-to-verify>
    HARD STOP — do NOT auto-pass on a green author suite (the D-12 failure mode this phase hit 10 times).
    Dispatch (or hand to the human to dispatch) an INDEPENDENT bash-grounded opus-grade red-team against
    the COMMITTED hooks/admission-guard.js + scripts/admission-server.js + scripts/context-io.js (never
    the .ts, never the author's suites). The red-team's job is structured-channel-shaped (RESEARCH Q5),
    not shell-obfuscation hunting:
    1. CHANNEL IS GATED — NEGATIVE + POSITIVE (i): drive the structured tool / hook with an adversarial
       sweep of structured tool_input — every high-severity by (security-nfr / architect-design /
       release-manager, incl. case/whitespace/unicode variants), every dial value (off / high-severity /
       all / typo / garbage / non-string / corrupt-config / absent), a self-authored verified_by:
       human:NAME arg, multiple notes, missing/wrong params — and confirm NO high-severity note is
       ADMITTED (no notes/<id>.md persisted AND the hook denies) without the human-set
       GRUGOPS_ADMISSION_APPROVED_BY env. Bash-ground it: a forge counts ONLY if the committed server
       actually persists a high-sev note (and/or the ledger gains a line) AND the hook ALLOWs. Routine
       under high-severity and anything under off must ALLOW (no over-block); corrupt config must DENY
       (fail closed). POSITIVE PATH (the BLOCKER fix — must SUCCEED): run the COMMITTED server WITH
       process.env.GRUGOPS_ADMISSION_APPROVED_BY=alice and confirm a high-severity finding is admitted —
       EXACTLY ONE persisted note stamped verified_by: human:alice (and an agent-supplied human:bob is
       ignored → human:alice, never human:bob); with the env UNSET, ZERO persisted.
    2. RESIDUAL IS EXACTLY AS DOCUMENTED (ii): confirm the same-uid direct-FS-write bypasses the channel
       EXACTLY as the SC1 restatement documents, across the full disclosed set — a Write-tool drop, a Bash
       echo/cp/tee/redirection, a modified context-io copy, a programmatic node -e appendNote, AND a
       node -e emitVerdict (which forges a LIVE green §14-gate verdict — the most powerful same-uid form,
       since it then lets Posture-B pass for forged findings) — and that NO NEW hole beyond that disclosed
       direct-write exists (e.g. the structured channel cannot be tricked into admitting an un-approved
       high-sev note; the grant cannot come from any arg; an agent self-setting the approval env on its OWN
       process is the documented direct-node residual, not the channel). The adversary must INVENT at least
       one structured-input attack the author did not enumerate and confirm it is gated.
    3. PRESERVATION (iii): git diff --quiet hooks/guard.ts exit 0; SC2 + the GOV-02 audit ledger satisfied
       and untouched; the SC3 floor preserved (off-only-is-off, corrupt-config fail-closed, the four
       floor invariants REFUSE); the command-string parser is gone (grep liveTokens = 0); freshness 0.
    Record the reproduced inputs, the invented attacks, the allow/deny + persisted/not-persisted outcomes,
    the residual controls, and the preservation checks in 25-11-SUMMARY.md — independently reproduced, NOT
    self-reported. Then surface to the human for the resume signal.
    <human-check>An independent bash-grounded opus red-team has, vs the committed .js: (i) confirmed every high-severity structured admit across the adversarial dial/by/arg sweep is DENIED-and-not-persisted without the human-set env (and a self-authored human:NAME arg never grants), while routine-under-high-severity / anything-under-off ALLOW and corrupt-config DENIES, AND the POSITIVE path SUCCEEDS (server env=alice → exactly one persisted high-sev note stamped human:alice, never human:bob; env unset → zero); (ii) confirmed the same-uid direct-FS-write residual is EXACTLY as documented with no new hole, across the full disclosed set incl. node -e appendNote AND node -e emitVerdict (+ an invented structured attack gated); (iii) confirmed git diff --quiet hooks/guard.ts exit 0, SC2/GOV-02 untouched, the SC3 floor preserved, the command-string parser deleted (liveTokens grep 0), freshness 0; findings in 25-11-SUMMARY.md. NOT a self-reported author green suite (D-12).</human-check>
  </how-to-verify>
  <resume-signal>Type "approved" once the independent bash-grounded opus red-team has reproduced (i)-(iii) above — incl. the POSITIVE approved-path success and the node -e emitVerdict residual control — and the findings (with the invented structured attack) are in 25-11-SUMMARY.md, or describe the bypass / false-positive found. Do NOT approve on a green author suite alone (D-12, the terminal lesson — hit 10 times).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented guarantee -> trace | SC1 must state what is actually guaranteed (channel un-forgeable on CC) and what is not (same-uid FS forgery) — no inflation |
| committed artifact -> red-team | the red-team drives the committed .js, never the .ts or the author suites |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-70 | Repudiation | overstated SC1 (claiming the residual is closed) corrupts the trace | high | mitigate | SC1 restated to the achievable invariant; the same-uid residual documented in clear voice; the red-team confirms the wording is not overstated (no-fabrication). |
| T-25-71 | Tampering | the green author suite false-passes the phase (the 10-round failure mode) | critical | mitigate | the blocking checkpoint requires an INDEPENDENT bash-grounded opus red-team vs the committed .js; green is necessary-not-sufficient (D-12). |
| T-25-72 | Information disclosure | governance prose tripping WR-01 / a foundation guard | medium | mitigate | reference WF16 / config keys / the hook by name; never restate a raw .grugops/ write path beside a write token (D-13); admission-protocol-docs + foundation-guards tests stay green. |
| T-25-SC | Tampering | npm/pip/cargo installs in gap closure | low | accept | round 6 ships NO package installs (zero-dep server, prose + planning docs); no legitimacy gate needed. |

(No package-manager installs in this plan — the package-legitimacy gate does not apply.)
</threat_model>

<verification>
- npx vitest run scripts/admission-protocol-docs.test.ts scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**' green (prose does not break the docs/foundation guards).
- ROADMAP + CONTEXT carry the identical restated SC1; the same-uid residual documented in clear voice.
- git diff --quiet hooks/guard.ts, scripts/context-io.ts, hooks/admission-guard.ts, scripts/admission-server.ts all exit 0 (this plan is prose + planning docs only).
- The blocking checkpoint: an independent bash-grounded opus red-team reproduces (i)-(iii) vs the committed .js; findings in 25-11-SUMMARY.md; the human approved.
</verification>

<success_criteria>
The admission protocol docs describe the structured channel + hook, SC1 is restated to the achievable
invariant in ROADMAP + CONTEXT with the same-uid residual documented honestly, and an INDEPENDENT
bash-grounded opus red-team has confirmed — vs the committed artifacts — that the sanctioned channel
admits no un-approved high-severity note, the residual is exactly as documented, and guard.ts /
SC2 / GOV-02 / the SC3 floor are preserved. SC1 is closed by an independent adversary, not a green
suite (D-12).
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-11-SUMMARY.md` when done
</output>

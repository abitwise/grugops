---
phase: 25-governance-on-a-dial
plan: 10
type: execute
wave: 2
depends_on: ["25-09"]
files_modified:
  - hooks/admission-guard.ts
  - hooks/admission-guard.js
  - hooks/hooks.json
  - hooks/admission-guard.test.ts
  - scripts/floor-invariance.test.ts
autonomous: true
requirements: [GOV-01, GOV-02]

must_haves:
  truths:
    - "On Claude Code, a PreToolUse hook matches the structured admission tool family (matcher mcp__grugops__.*) and reads the note's by/kind/verified_by from the FINAL structured tool_input — never a shell command string (D-01: the gate lives outside the agent process and reads the human-set session env). A future/renamed grugops admission tool is still matched (the matcher is the tool FAMILY, not one exact name — W3)."
    - "A high-severity admission (by in {security-nfr, architect-design, release-manager}, classified Unicode-robustly — NFKC-normalize + strip ALL whitespace/zero-width before the role-set test, D-06/W1) — or EVERY admission under human_admission: all — is DENIED unless the human-set session var GRUGOPS_ADMISSION_APPROVED_BY is present in the hook's own process env; the grant comes ONLY from the session env, never from any tool argument (a self-authored verified_by: human:NAME arg does not grant)."
    - "Under human_admission: off (the lean default) routine and high-severity structured admissions ALLOW with no human stop (zero-config preserved, SC2)."
    - "The SC3 floor is carried into the structured gate: the only off-equivalent value is exactly 'off'; any other human_admission value (typo, garbage, case-variant, non-string-canonicalized) is gate-or-stricter; a present-but-unreadable/corrupt config FAILS CLOSED (deny pending a human) via readGovernanceConfigResult; an absent config stays lean."
  artifacts:
    - "hooks/admission-guard.ts retargeted to a structured tool_input reader (+ rebuilt committed hooks/admission-guard.js)"
    - "hooks/hooks.json with the admission matcher swapped from Bash to the MCP tool"
    - "hooks/admission-guard.test.ts: both-direction RED->GREEN proof vs the committed .js + structured adversarial sweep"
    - "scripts/floor-invariance.test.ts: the SC3 sweep preserved/extended onto the structured channel"
  key_links:
    - "hooks.json mcp matcher (mcp__grugops__.*) -> hooks/admission-guard.js -> reads stdin tool_input.{by,kind,verified_by} + process.env[GRUGOPS_ADMISSION_APPROVED_BY] + readGovernanceConfigResult"
    - "the hook's by-severity classifier == the 25-09 combiner's classifier: ONE shared Unicode-robust isHighSeverityRole helper (NFKC + strip whitespace/zero-width) so the gate tier and the persist tier cannot diverge (no drift)"
    - "deny mechanism == guard.ts posture: exit 0 + JSON hookSpecificOutput.permissionDecision deny + reason"
  prohibitions:
    - "no shell-string parsing in the un-forgeable tier: the entire liveTokens command-string parser (tokenizer, launcher resolution, allowlist, admit-shape detection) is DELETED — the retargeted hook reads structured tool_input only and reconstructs NO command string (grep: liveTokens/liveAdmitSegments/tokenIsFinalLiteral/segmentAdmitDisposition = 0 in admission-guard.ts and .js)."
    - "no by near-miss escape (W1): the high-severity classifier MUST NFKC-normalize + strip ALL whitespace/zero-width (U+00A0 nbsp, U+200B zero-width) before the role-set test, NOT a bare .trim().toLowerCase(); a true cross-script homoglyph by is the accepted D-06 residual (escape: human_admission: all)."
    - "matcher breadth (W3): the hooks.json admission matcher is the tool FAMILY mcp__grugops__.* (not one exact tool name), so a second/renamed grugops admission tool cannot silently escape the gate."
    - "guard.ts byte-frozen: hooks/guard.ts and the prod-deploy Bash matcher in hooks.json MUST NOT change (git diff --quiet hooks/guard.ts exit 0)."
    - "context-io byte-frozen this plan: scripts/context-io.ts MUST NOT change in 25-10 (git diff --quiet scripts/context-io.ts exit 0); the gate lives in the hook, not the reader/writer."
    - "the grant is the session env ONLY: no tool argument (verified_by, by, or any field) may grant admission; the human-set GRUGOPS_ADMISSION_APPROVED_BY in the hook's own env is the sole grant."
    - "no fail-open: a matched admission with missing/malformed structured args, an unreadable note classification, or a corrupt config MUST deny, never crash-allow."
---

<objective>
Build the un-forgeable GOV-01 gate on the structured channel (D-01). Retarget hooks/admission-guard
from a Bash command-string matcher to a structured PreToolUse matcher on mcp__grugops__propose_note,
DELETE the entire command-string parser (the 10-round liveTokens machinery), and decide admission from
the FINAL structured tool_input plus the human-set session env. Because tool_input is delivered final
(no shell expansion), the whole shell-obfuscation bypass family (glob, brace, param/command
substitution, word-split, extglob `(`-fragmentation, launcher rename) is gone by construction — not by
recognizing more spellings, but because the proxy the spellings attacked no longer exists.

Purpose: make the SANCTIONED admission channel mechanically un-forgeable on Claude Code while carrying
forward every SC3 fix from rounds 1-5 (off-only-is-off canonicalization, corrupt-config fail-closed,
case-insensitive D-06 classification) and keeping the gate OUTSIDE the agent process (D-01, D-05).
Output: the retargeted hook + committed .js, the hooks.json matcher swap, a both-direction RED->GREEN
proof vs the committed artifact, and the SC3 floor-invariance sweep on the structured channel.

The D-04 in-script admit() refusal stays the documented WEAKER degrade tier for the four non-CC CLIs
(D-04/D-05) — unchanged from Phase 25 base; context-io stays byte-frozen this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-RESEARCH.md
@hooks/admission-guard.ts
@hooks/guard.ts
@hooks/hooks.json
@scripts/floor-invariance.test.ts
@.planning/phases/25-governance-on-a-dial/25-09-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 25-10-01: Retarget hooks/admission-guard.ts to a structured tool_input gate; DELETE the command-string parser; swap the hooks.json matcher</name>
  <files>hooks/admission-guard.ts, hooks/admission-guard.js, hooks/hooks.json</files>
  <read_first>
    - hooks/admission-guard.ts:44-65 (APPROVAL var, HIGH_SEVERITY_ROLES, SELF_APPROVE — keep the approval-var + role-set semantics; the SELF_APPROVE command regex is retired with the parser)
    - hooks/admission-guard.ts:947-1058 (deny() exit-0+JSON helper; stdin parse fail-closed; the dial read via readGovernanceConfigResult; off-only canonicalization + corrupt-config fail-closed — PRESERVE this logic, fed by structured input)
    - hooks/admission-guard.ts:1067-1182 (classifySegmentOrDeny — the severity classification + the existing .trim().toLowerCase() D-06 test [which W1 REPLACES with a Unicode-robust classifier] + the deny messages to adapt to a single structured note)
    - 25-09 Task 01 (the shared Unicode-robust isHighSeverityRole classifier — reuse the SAME helper so the gate tier and the persist tier cannot diverge, W1/no-drift)
    - hooks/admission-guard.ts:67-945 (the ENTIRE liveTokens command-string parser — DELETE all of it; round 6 retires it)
    - hooks/guard.ts (the deny shape + the human-set-env / refuse-self posture to mirror; do NOT edit guard.ts)
    - hooks/hooks.json (two Bash matchers — change ONLY the admission-guard matcher to the MCP tool; leave the guard.js Bash matcher untouched)
    - scripts/context-io.ts:1205-1258 (readGovernanceConfigResult — the absent/ok/unreadable source the hook consumes; imported unchanged)
  </read_first>
  <action>
    Rewrite hooks/admission-guard.ts so its ONLY input is the PreToolUse stdin tool_input for
    mcp__grugops__propose_note (D-01: read the final structured args, never a shell string). DELETE the
    full liveTokens parser and every command-string helper (tokenizer, effectiveCommandWord,
    launcherDisposition, segmentAdmitDisposition, tokenIsFinalLiteral, liveAdmitSegments,
    commandHasLiveAdmitShape, the LAUNCHERS/JS_RUNNERS/COMMAND_MODIFIERS/DYNAMIC_EVAL_WORDS sets, the
    SELF_APPROVE command regex) — round 6 retires shell-string parsing in the un-forgeable tier. Read
    tool_input.by, tool_input.kind, tool_input.verified_by (and task for the message). Classify
    high-severity with a Unicode-ROBUST classifier (W1): NFKC-normalize + strip ALL whitespace and
    zero-width characters (U+00A0 nbsp, U+200B zero-width, etc.) before the HIGH_SEVERITY_ROLES membership
    test — NOT a bare .trim().toLowerCase() (which lets `security-nfr` + nbsp / zero-width render as a
    security finding yet classify ROUTINE and escape the gate). Use the SAME shared classifier the 25-09
    combiner uses (no drift); a true cross-script homoglyph by is the accepted D-06 residual (escape:
    human_admission: all). Read the dial via readGovernanceConfigResult (Plan 25-01): source 'unreadable'
    -> deny (fail closed, SC3); source 'absent'/'off' -> allow; canonicalize fail-closed so ONLY exactly
    'off' is off-equivalent and every other value is gate-or-stricter (carry the round-2/5 SC3 fix
    forward). The grant is process.env[GRUGOPS_ADMISSION_APPROVED_BY] ONLY — a self-authored verified_by:
    human:NAME ARG never grants (D-01; the env the agent's child cannot reach is the un-forgeable signal).
    Deny a gated, unapproved admission with the exit-0 + JSON deny shape mirroring guard.ts; deny (fail
    closed) on missing/malformed structured args, a non-finding-but-gated case under 'all', or any
    classification failure on a matched gated admission. Swap ONLY the admission-guard matcher in
    hooks.json from "Bash" to the MCP tool FAMILY `mcp__grugops__.*` (W3 — the family, not one exact tool
    name, so a second/renamed grugops admission tool cannot silently escape the gate); leave the guard.js
    Bash matcher byte-unchanged. Clear professional voice (safety/governance surface). Rebuild
    hooks/admission-guard.js; do NOT touch hooks/guard.ts or scripts/context-io.ts.
  </action>
  <verify>
    <automated>node scripts/freshness.js && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts && grep -cE 'liveTokens|liveAdmitSegments|tokenIsFinalLiteral|segmentAdmitDisposition' hooks/admission-guard.js | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - admission-guard.ts reads ONLY structured tool_input (no command-string tokenizer remains; grep for liveTokens/liveAdmitSegments/tokenIsFinalLiteral/segmentAdmitDisposition = 0 in both .ts and .js).
    - The hook denies a high-severity structured admission with no GRUGOPS_ADMISSION_APPROVED_BY env, allows it with the env present, and allows routine under high-severity / anything under off.
    - W1: a high-severity by with a trailing nbsp / embedded zero-width / NFKC-compatibility variant still classifies high-severity (DENIES without the env) — not silently routine.
    - W3: the hooks.json admission matcher is mcp__grugops__.* (the family); a second/renamed grugops admission tool name is still matched by the hook.
    - SC3 carried: exactly 'off' is off-equivalent; corrupt/unreadable config denies (fail closed); absent config stays lean.
    - The guard.js Bash matcher is unchanged; node scripts/freshness.js exits 0; git diff --quiet hooks/guard.ts and git diff --quiet scripts/context-io.ts both exit 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 25-10-02: RED->GREEN both-direction proof vs the committed .js + the SC3 floor-invariance sweep on the structured channel + structured adversarial unit sweep</name>
  <files>hooks/admission-guard.test.ts, scripts/floor-invariance.test.ts</files>
  <read_first>
    - scripts/floor-invariance.test.ts (the four floor invariants + the dials-only-tighten guarantee + the spawn-the-COMMITTED-.js discipline — extend onto the structured channel)
    - hooks/admission-guard.test.ts (the existing harness shape — replace the command-string fixtures with structured tool_input stdin fixtures)
    - hooks/guard.test.ts (the spawn-the-committed-.js + stdin-JSON driver pattern to model the structured driver on)
  </read_first>
  <behavior>
    - Clean direction (ALLOW): a routine structured propose_note (by: software-engineer) under high-severity ALLOWS; any structured admit under off ALLOWS; a high-severity admit WITH GRUGOPS_ADMISSION_APPROVED_BY present ALLOWS.
    - Planted direction (DENY): a high-severity structured admit (by: security-nfr / architect-design / release-manager) under high-severity or all with no env DENIES naming the note; under all a routine admit with no env DENIES; a self-authored verified_by: human:eve arg with NO env still DENIES (the arg never grants).
    - SC3 sweep: every human_admission value including garbage/typo/case-variant/non-string-canonicalized is gate-or-stricter (a high-severity admit without the env DENIES); exactly 'off' allows routine; a corrupt/unreadable config DENIES (fail closed); the four un-dialable floor invariants still REFUSE; guard.ts byte-frozen.
    - Structured adversarial sweep (W1, non-vacuous — assert the ACTUAL technique): a high-severity by with (a) trailing/leading U+00A0 nbsp, (b) an embedded U+200B zero-width, (c) an NFKC-compatibility variant, and (d) case variants ALL classify high-severity and DENY without the env (a bare .trim().toLowerCase() would let (a)/(b)/(c) through — the test must construct those exact code points so it cannot pass vacuously). Missing/extra/wrong-named params fail closed; malformed tool_input fails closed.
    - Matcher-breadth (W3): a structured admission delivered under a SECOND/renamed grugops tool name (e.g. mcp__grugops__admit) is still matched and gated by the hook (the matcher is the mcp__grugops__.* family).
  </behavior>
  <action>
    Capture the both-direction RED->GREEN proof vs the COMMITTED hooks/admission-guard.js (record
    25-10-RED-baseline.txt from the pre-retarget committed .js and 25-10-GREEN-proof.txt from the
    retargeted committed .js): the planted high-severity-without-env structured admit must flip from
    ALLOW (RED, the Bash matcher never saw the MCP call) to DENY (GREEN). Drive the hook by piping
    structured PreToolUse stdin JSON ({tool_name: the MCP tool, tool_input: the note fields}) to the
    committed .js, asserting deny/allow by exit-0+JSON. Extend scripts/floor-invariance.test.ts so the
    SC3 sweep exercises the STRUCTURED channel (every dial value incl. garbage gate-or-stricter; corrupt
    config fail-closed; the four floor invariants; guard.ts byte-frozen) — preserve the existing
    invariants, do not weaken them. Add the structured adversarial sweep: the W1 NON-VACUOUS cases
    (construct the exact U+00A0 / U+200B / NFKC-compatibility / case code points and assert each
    high-severity by DENIES without the env — a bare trim/lowercase classifier would FAIL these); the
    self-authored human:NAME arg never grants; missing/wrong params fail closed; and the W3 matcher-breadth
    case (a second/renamed mcp__grugops__* tool name is still gated). Per D-12 state in a comment that a
    GREEN suite is NECESSARY BUT NOT SUFFICIENT — the independent red-team (Task 25-11-03) is the gate.
    Gate with the e2e-excluded lane. Clear professional voice.
  </action>
  <verify>
    <automated>npx vitest run hooks/admission-guard.test.ts scripts/floor-invariance.test.ts --exclude '**/scripts/e2e/**' && node scripts/freshness.js && git diff --quiet hooks/guard.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 25-10-RED-baseline.txt shows the planted high-severity-without-env structured admit ALLOW vs the pre-retarget committed .js; 25-10-GREEN-proof.txt shows the same admit DENY vs the retargeted committed .js.
    - The structured-channel SC3 sweep is green: every dial value incl. garbage is gate-or-stricter; corrupt config fail-closed; the four floor invariants REFUSE; guard.ts byte-frozen.
    - The W1 sweep is green and NON-VACUOUS: a high-severity by with a literal U+00A0 / U+200B / NFKC-compatibility / case variant DENIES without the env (the fixtures construct those exact code points).
    - W3: a second/renamed mcp__grugops__* admission tool name is still matched and gated.
    - A self-authored human:NAME arg without the env still DENIES; missing/wrong params fail closed.
    - The full non-e2e suite stays green; node scripts/freshness.js exits 0; git diff --quiet hooks/guard.ts and git diff --quiet scripts/context-io.ts exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> PreToolUse hook stdin (tool_name + tool_input) | the hook reads the final structured args; the agent cannot inject shell expansion |
| human session env -> hook process env | GRUGOPS_ADMISSION_APPROVED_BY set by the human reaches the hook; an agent's child-env set never does (the un-forgeable signal, D-01) |
| governance config -> hook decision | readGovernanceConfigResult distinguishes absent/ok/unreadable for SC3 fail-closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-60 | Elevation | a self-authored verified_by: human:NAME tool argument used to self-grant | high | mitigate | the grant is process.env[GRUGOPS_ADMISSION_APPROVED_BY] ONLY; no tool arg grants; a self-authored human:NAME arg without the env DENIES (unit-proven). |
| T-25-61 | Spoofing | a near-miss high-severity `by` (nbsp / zero-width / NFKC-compatibility / homoglyph) evading the D-06 set and admitting un-gated (W1) | high | mitigate | the classifier NFKC-normalizes + strips ALL whitespace/zero-width before the role-set test (NOT a bare trim/lowercase); a non-vacuous unit sweep constructs the exact U+00A0/U+200B/NFKC code points; a true cross-script homoglyph is the accepted D-06 residual (escape: human_admission: all). |
| T-25-65 | Tampering | a second/renamed grugops admission tool silently escaping the gate (W3) | medium | mitigate | the hooks.json matcher is the mcp__grugops__.* family, not one exact tool name; a unit test asserts a renamed tool name is still matched. |
| T-25-62 | Tampering | reintroducing any command-string parsing into the un-forgeable tier (the 10-round trap) | high | mitigate | the liveTokens parser is DELETED; grep asserts 0 occurrences in .ts and .js; the hook reads structured tool_input only. |
| T-25-63 | Denial/Bypass | fail-open on a corrupt config, missing args, or an unreadable classification | high | mitigate | readGovernanceConfigResult 'unreadable' denies; missing/malformed structured args on a matched gated admit deny; exactly 'off' is the only off-equivalent value (SC3). |
| T-25-64 | Tampering | the matcher swap silently altering the prod-deploy guard | high | mitigate | only the admission-guard matcher changes in hooks.json; guard.js Bash matcher byte-unchanged; git diff --quiet hooks/guard.ts exit 0. |

(No package-manager installs in this plan — the package-legitimacy gate does not apply.)
</threat_model>

<verification>
- npx vitest run --exclude '**/scripts/e2e/**' green (admission-guard + floor-invariance + the full suite).
- node scripts/freshness.js exits 0 (admission-guard.js fresh).
- git diff --quiet hooks/guard.ts exits 0 and git diff --quiet scripts/context-io.ts exits 0.
- grep: liveTokens/liveAdmitSegments/tokenIsFinalLiteral/segmentAdmitDisposition = 0 in admission-guard.ts and .js.
- 25-10-RED-baseline.txt / 25-10-GREEN-proof.txt capture the both-direction proof vs the committed .js.
</verification>

<success_criteria>
The un-forgeable GOV-01 gate now lives on the structured channel: a high-severity (or every, under all)
structured admission is denied without the human-set session env, allowed with it, and allowed routine
under high-severity / anything under off — with the SC3 floor (off-only-is-off, corrupt-config
fail-closed) carried forward and the entire command-string parser deleted. guard.ts and context-io.ts
are byte-frozen. The independent red-team (plan 25-11) is the gate, not this green suite (D-12).
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-10-SUMMARY.md` when done
</output>

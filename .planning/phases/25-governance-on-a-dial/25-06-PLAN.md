---
phase: 25-governance-on-a-dial
plan: 06
type: execute
wave: 6
gap_closure: true
depends_on: ["25-01", "25-02", "25-03", "25-04", "25-05"]
autonomous: false
files_modified:
  - hooks/admission-guard.ts
  - hooks/admission-guard.js
  - hooks/admission-guard.test.ts
  - scripts/floor-invariance.test.ts
  - 25-06-RED-baseline.txt
  - 25-06-GREEN-proof.txt
requirements: [GOV-01, GOV-02]
user_setup: []

must_haves:
  truths:
    - "Class A / SC1 (GOV-01) — a path-form command-modifier escapes no longer: the resolver applies effectiveCommandWord (basename) + full de-quote to EVERY token in the segment's leading run BEFORE the isCommandPrefix test, so `/usr/bin/env node …admit`, `/usr/bin/nice node …`, `/usr/bin/env -S node …`, `/usr/bin/xargs node …`, `./nice node …`, and doubled path-form modifiers all resolve the trailing `node` as the effective command word and DENY a gated high-severity un-approved admit (per D-06; vs the committed `.js`)."
    - "Class B / SC1 (GOV-01) — a command word that IS or CONTAINS a command substitution `$( … )` or a backtick in command-word position is statically unresolvable and FAILS CLOSED: `$(echo node) …admit`, `` `echo node` …admit ``, `$(printf node) …`, `$(basename /usr/bin/node) …`, nested `$(echo $(echo node)) …`, and `nice $(echo node) …` all GATE (deny pending a human) when the segment carries the admit shape — closing the `$X`-fails-closed-but-`$(…)`/backtick-fails-open asymmetry. A `$( … )` / backtick with NO admit shape in the live segment still ALLOWs (narrow trigger; over-gate is SAFE per SC3)."
    - "Class E / SC1 (GOV-01, CRITICAL multi-admit shield) — the CLASSIFIER is the segment walk: every LIVE admit segment is enumerated and the command GATES if ANY segment resolves to a gated severity. Under `high-severity`, `node …admit T <routine> ; node …admit T <high-sev>` (and the `&&` / `|` / subshell variants) DENIES because the high-severity segment is classified independently of order — not just the first `admit` token. A command bundling ONLY routine admits under `high-severity` stays ALLOWED (no new over-block; the clean over-block record is preserved)."
    - "Class F / SC1 (GOV-01, defense-in-depth tier consistency) — the hook's per-segment note read is `validate()`-consistent: a note with a duplicate or indented `by` (`by: security-nfr` then `by: software-engineer`, or a leading-space ` by:`) is rejected as a structural fail (gate-or-stricter under an active dial) rather than last-wins mis-classified, so the hook and `admit()` classify a note identically and the two parsing tiers cannot disagree."
    - "SINGLE RESOLUTION AUTHORITY (anti-whack-a-mole) — `liveTokens` is the ONE source of truth for segmentation, command-word resolution, AND note-file/severity classification: the separate naive `tokenize` (admission-guard.ts:500) and `noteFileFromCommand` (admission-guard.ts:509) second walk are DELETED; a grep proves neither exists as a separate command parse and note-file + severity resolution flows through the one segment authority (two parsers cannot drift — the P22 round-8 \"the boundary IS the parser\" lesson applied to command-RESOLUTION)."
    - "PRESERVED — the round-2 LOGIC closures the round-2 red-team confirmed SOLID are untouched: GAP-B (self-set behind a wrapper / D-01 floor) still DENIES on a matcher false-negative; GAP-C (non-string / non-object / corrupt-config canonicalization → gate-or-stricter, only exact JSON `\"off\"` off-equivalent, absent stays lean — context-io.ts unchanged); GAP-D (case/whitespace-insensitive `HIGH_SEVERITY_ROLES` at BOTH tiers) still classifies a case-variant `by` high-severity. The new per-segment authority still honors `gateEveryMatch` (non-canonical dial), the case-insensitive severity test, and the in-front-of-early-exit self-set (D-01) check."
    - "PRESERVED — SC2 (audit_retention 3-surface lockstep + lean defaults), the GOV-02 audit ledger (`appendAuditLedger`, context-io.ts:955-980), and the 25-04 segmentation (quoted / `#`-comment / heredoc / `\\`-continuation / `VAR=` / `env`-prefix / `;`/`&`/`|`/subshell separators all classified as DATA / boundaries) are unchanged — no over-block regression (the CR-01-inverse fail-safe stays clean: an inert heredoc / quoted / comment admit mention with NO live launcher still ALLOWs)."
    - "The committed `.js` outputs are byte-faithful `tsc` builds — `npm run freshness` exits 0; `hooks/guard.ts` stays byte-frozen (`git diff --quiet hooks/guard.ts` exit 0, blob `3501810e21308e4b7e219679a6ca30dace9b5d66`, D-02). scripts/context-io.ts is NOT edited this round (GAP-C/GAP-D live there and are confirmed solid; the Class F fix lives entirely in the hook's note read)."
  artifacts:
    - path: "hooks/admission-guard.ts"
      provides: "ONE liveTokens-based per-segment resolution authority replacing the diverged matcher + naive second-walk classifier. For each LIVE command segment it yields, in one walk: {isLiveAdmit, noteFile, by/severity}. effectiveCommandWord + de-quote are applied to EVERY leading-run token (prefix modifiers too) before isCommandPrefix (closes A); a `$( … )`/backtick command word in command-word position is statically unresolvable → fail-closed gate when the segment carries the admit shape (closes B); every live admit segment is classified and the command gates if ANY is a gated severity (closes E); the per-segment note read rejects duplicate/malformed `by` like validate() (closes F). The standalone tokenize + noteFileFromCommand are deleted (single authority)."
      contains: "admit"
    - path: "hooks/admission-guard.js"
      provides: "committed tsc build the host hook runs (freshness-covered; rebuilt after the .ts edit)"
    - path: "hooks/admission-guard.test.ts"
      provides: "extended child-spawn oracle vs the COMMITTED admission-guard.js: Class A path-form modifiers (`/usr/bin/env node …admit`, `/usr/bin/nice`, `/usr/bin/env -S`, `/usr/bin/xargs`, `./nice`, doubled) DENY a gated high-severity un-approved admit; Class B command-substitution/backtick command word (`$(echo node) …`, `` `echo node` … ``, `$(printf node)`, `$(basename …)`, nested, in-modifier-slot) GATE; Class E multi-admit (routine-first ; / && / | / subshell + high-severity) DENIES while routine-only under high-severity ALLOWs; Class F duplicate/indented `by` DENIES (gate-or-stricter); inert heredoc/quoted/comment mentions + a $()/backtick/eval with NO admit shape still ALLOW; the round-2 launcher classes + GAP-B/GAP-D cases still DENY (no regression); case count >= guard.test.ts"
      contains: "deny"
    - path: "scripts/floor-invariance.test.ts"
      provides: "extended SC1 sweep: an anti-whack-a-mole command-RESOLUTION fuzz over {path-form modifier spelling (env/nice/xargs/timeout/nohup via /usr/bin, /bin, ./, basename) × command-substitution/backtick command-word production × multi-admit orderings/separators (; / && / | / subshell, routine-first vs high-first, 2 and 3 admits)} × {LF|CRLF} asserting the class invariant — no gated admit slips through, a routine-only multi-admit under high-severity ALLOWs, an inert/no-admit dynamic command ALLOWs; the four named floor invariants still hold at every value; the round-2 non-string-dial + case-variant sweeps remain green (PRESERVED, not duplicated)"
      contains: "human_admission"
    - path: "25-06-RED-baseline.txt"
      provides: "captured RED baseline — the PRE-FIX committed `admission-guard.js` (blob 1b822df14daba4cd85d19a1231fe34be283b2633, HEAD a43c962) ALLOWs each round-3 class: Class A path-form modifier, Class B `$()`/backtick command word, Class E multi-admit shield (routine-first then high-severity under high-severity), Class F duplicate/indented `by` — the 22-08/22-09 RED->GREEN artifact discipline, extending the 25-05 row format"
    - path: "25-06-GREEN-proof.txt"
      provides: "captured GREEN proof — the POST-FIX committed `admission-guard.js` DENIES/GATES each round-3 class naming the note; the routine-only multi-admit under high-severity + the inert heredoc/quoted/comment + the $()/backtick/eval-with-no-admit-shape + the zero-config absent lean `off` still ALLOW (no over-block); the round-2 launcher classes + GAP-B/GAP-D still DENY — extending the 25-05 row format, referencing the committed `.js`"
  key_links:
    - from: "hooks/admission-guard.ts (the ONE liveTokens-based per-segment authority)"
      to: "a real Bash `tool_input.command` admit verb behind any command-resolution obfuscation"
      via: "one segment walk yields {isLiveAdmit, noteFile, by/severity} per live segment: effectiveCommandWord + de-quote over EVERY leading-run token before isCommandPrefix (A), `$()`/backtick command word fails closed on the admit shape (B), every admit segment classified and gate-if-any (E), validate()-consistent per-segment note parse (F)"
      pattern: "admit"
    - from: "hooks/admission-guard.ts (the deleted second walk)"
      to: "noteFileFromCommand + the standalone tokenize"
      via: "both are removed; note-file + severity resolution flows through liveTokens only — a grep proves neither helper exists as a separate command parse"
      pattern: "noteFileFromCommand|function tokenize"
    - from: "hooks/admission-guard.ts (the per-segment note read, Class F)"
      to: "the note's `by` classification consistent with scripts/context-io.ts validate()"
      via: "the per-segment note parse rejects a duplicate / malformed `by` (parseNote duplicateKeys / malformedLines) rather than last-wins, so the hook and admit() classify the same note identically"
      pattern: "duplicateKeys|malformedLines|validate"
    - from: "scripts/floor-invariance.test.ts (the command-resolution fuzz)"
      to: "the COMMITTED hooks/admission-guard.js"
      via: "child-spawn the committed .js across the path-form-modifier × command-substitution × multi-admit-ordering matrix; assert the class invariant (a gated admit in ANY live segment → DENY; routine-only → ALLOW; inert/no-admit → ALLOW) for every variant"
      pattern: "admission-guard\\.js|spawn"
  prohibitions:
    - statement: "A GREEN author suite is NECESSARY-BUT-NOT-SUFFICIENT for this safety guard — the author's suites passed and STILL missed these classes across rounds 1, 2, and 3 (this is the 8th green-suite-insufficient catch of v2.0). Closure REQUIRES a both-direction RED-vs-committed-`.js` proof reproduced against the COMMITTED `hooks/admission-guard.js` (never the `.ts`, never the author `*.test.ts`): for EACH round-3 class (A path-form modifier / B `$()`/backtick command word / E multi-admit shield / F duplicate-or-indented `by`) a planted high-severity-without-real-approval command flips PRE-FIX ALLOW -> POST-FIX DENY (or GATE) naming the note; AND the routine-only multi-admit under high-severity, the inert heredoc/quoted/comment mentions, a `$()`/backtick/eval with NO admit shape, and the zero-config absent lean `off` still ALLOW. Captured in 25-06-RED-baseline.txt (pre-fix allows) and 25-06-GREEN-proof.txt (post-fix denies), extending the 25-05 row format. (D-12, [[grugops-safety-invariant-green-suite-insufficient]])."
      status: must-hold
      verification: "25-06-RED-baseline.txt shows the pre-fix committed `.js` (blob 1b822df1…, HEAD a43c962) ALLOWing each round-3 class; 25-06-GREEN-proof.txt shows the post-fix committed `.js` DENYing/GATING each class naming the note and ALLOWing the routine-only / inert / no-admit-shape / zero-config controls; reproduced independently of the author suite at the blocking checkpoint."
    - statement: "SINGLE RESOLUTION AUTHORITY (anti-whack-a-mole): there must NOT be two command parsers that can drift. The naive `tokenize` (admission-guard.ts:500) and `noteFileFromCommand` (admission-guard.ts:509) second walk MUST be DELETED — note-file + severity classification flows through the ONE `liveTokens`-based per-segment authority. Do NOT widen a launcher anchor list or add a per-class special case at one more point (the failure mode of every prior round). The fix is structural: the classifier IS the segment walk, and command-word resolution (basename + de-quote) is applied uniformly to every leading-run token, once."
      status: must-hold
      verification: "grep confirms `function tokenize` and `noteFileFromCommand` no longer exist in hooks/admission-guard.ts; the per-segment authority yields note-file + by/severity from liveTokens; an anti-whack-a-mole command-resolution fuzz in floor-invariance.test.ts (path-form-modifier × command-substitution × multi-admit-ordering × {LF|CRLF}) asserts the class invariant for EVERY variant (no enumeration of known forms)."
    - statement: "Class E (the critical multi-admit shield) MUST close with the principled per-segment-classify-and-gate-if-any semantics — NOT a blunt \"gate any multi-admit\" rule. Under `high-severity`, a command with two or more admit segments DENIES iff AT LEAST ONE segment resolves to a gated severity; a command bundling ONLY routine admits under `high-severity` stays ALLOWED. Introducing a new over-block (gating a routine-only multi-admit) is a PROHIBITED false-positive — the clean over-block record must be preserved."
      status: must-hold
      verification: "admission-guard.test.ts asserts vs the committed `.js`: `node …admit T routine ; node …admit T high-sev` (and &&/|/subshell, both orderings) DENIES under high-severity; `node …admit T routine ; node …admit T routine2` under high-severity ALLOWs; the floor-invariance fuzz includes routine-only multi-admit ALLOW cases."
    - statement: "The round-2 LOGIC closures and the verified SC2 / GOV-02 surfaces MUST NOT regress. GAP-B (self-set behind a wrapper / D-01 floor) still DENIES on a matcher false-negative; GAP-C (non-string / non-object / corrupt-config canonicalization in context-io.ts) is UNTOUCHED (scripts/context-io.ts is NOT edited this round); GAP-D (case/whitespace-insensitive HIGH_SEVERITY_ROLES at both tiers) still classifies a case-variant `by` high-severity; the four named floor invariants (refuse-self FAILs at every dial / no-fabrication-never-rewrite / `quality.test_integrity` no `off` / `hooks/guard.ts` byte-frozen) hold; SC2 config lockstep + the GOV-02 audit ledger are unchanged. `hooks/guard.ts` stays byte-frozen (D-02)."
      status: must-hold
      verification: "the round-2 non-string-dial + case-variant + self-set-behind-a-wrapper sweeps in floor-invariance.test.ts stay GREEN; `git diff --quiet scripts/context-io.ts` exits 0 (not edited) — OR, if a Class F note read must touch context-io.ts, only an ADDITIVE helper is added with GAP-C/GAP-D byte-identical (state which in the summary); `git diff --quiet hooks/guard.ts` exits 0; freshness 0; config-consistency + audit-ledger suites green."
    - statement: "Do NOT regress the over-block-clean (CR-01-inverse fail-safe) angle: an inert heredoc / quoted / `#`-comment admit mention with NO live launcher MUST still ALLOW, and a `$()`/backtick/eval/`sh -c` construct with NO admit shape in the live segment MUST still ALLOW. The Class B fail-closed gate and the Class E multi-admit gate fire ONLY when the admit SHAPE (a context-io reference AND the admit verb) is present in a LIVE segment."
      status: must-hold
      verification: "admission-guard.test.ts + the floor-invariance fuzz assert: inert heredoc/quoted/comment admit mentions ALLOW; a `$(echo hi)` / `` `echo hi` `` / `eval \"echo hi\"` / `bash -c \"ls\"` with NO admit reference ALLOWs; only a live admit shape behind an unresolvable command word GATES."
    - statement: "The SC1 floor + an INDEPENDENT both-angle red-team (guard-LOGIC probe AND guard-INPUT-SURFACE code-review, opus-grade — the P23 split; the LOGIC agent found Class E and the INPUT-SURFACE agent did not, so BOTH are required) are the proof, not a green suite. This is the named closure gate. The probe MUST invent at least one obfuscation shape the author did NOT enumerate to test the resolver as ONE authority over command-resolution (e.g. a deeper path-form-modifier chain, a command-substitution that itself uses a path-form `$(/usr/bin/echo node)`, or a 3-admit ordering) and confirm it is caught structurally (denied or gated)."
      status: must-hold
      verification: "the blocking checkpoint (Task 25-06-04) HARD-STOPS until an independent opus-grade red-team reproduces both the LOGIC angle (multi-admit shield / duplicate-`by` mis-classify / a config-shape edge) AND the INPUT-SURFACE angle (a path-form modifier or command-substitution command word escaping the resolver, or an inert mention reading as a live admit), invents an un-enumerated shape, confirms the four floor invariants + `git diff --quiet hooks/guard.ts` exit 0 + freshness 0 + WR-01 no false-positive, vs the committed `.js`; findings recorded in 25-06-SUMMARY.md, not self-reported as a green suite."
    - statement: "WR-01 / no kit-content false-positive (D-13): the resolver edits and the new test/proof artifacts must not trip `guard_context_writes` or any foundation guard — reference the hook / Workflow 16 / the config keys by name, never restate a raw `.grugops/` write path beside a write token."
      status: must-hold
      verification: "`npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial); the closed Plan-25-0x deferral markers stay inert (no false-positive)."
---

<objective>
Round-3 gap closure for Phase 25 (25-VERIFICATION.md "UPDATE 2026-06-25 — post-25-05" banner + 25-05-SUMMARY.md "Independent Both-Angle Red-Team", gaps_found). Plan 25-05 correctly closed the round-2 LOGIC classes (GAP-C non-string/corrupt dial, GAP-D case/whitespace severity, GAP-B self-set behind a wrapper — all confirmed SOLID at both tiers by the round-2 red-team) and is committed. The independent both-angle opus red-team then reproduced FOUR NEW classes against the COMMITTED `.js` that the 743-green author suite missed. This plan does NOT re-touch the round-2 LOGIC closures and does NOT touch the verified SC2 config foundation or the GOV-02 audit ledger; it makes the admission-guard's command parsing ONE authority.

**The single root cause (confirmed in the committed source by the orchestrator):** the admission-guard runs TWO separate command walks that diverge — the MATCHER `isAdmitInvocation` (admission-guard.ts:431) walks command segments via `liveTokens` (segment-aware), but the CLASSIFIER `noteFileFromCommand` (admission-guard.ts:509) uses a SEPARATE naive `tokenize` (:500) + `findIndex(t === "admit")` that sees only the FIRST `admit` token. This divergence — plus command-word resolution (`effectiveCommandWord` + de-quote) applied at only ONE point (the resolved word, never the prefix/modifier tokens) and a `$( … )`/backtick command word that fails OPEN — is the single disease behind all four classes.

**The fix (user-locked this round — UNIFY, not targeted per-class patches): ONE segment-resolution authority built on `liveTokens`.** For each command segment it yields, in one walk: `{ isLiveAdmit, noteFile, by/severity }`. Then:

1. **Class A (path-form command modifier).** Resolve the EFFECTIVE command word over EVERY token in the segment's leading run — apply `effectiveCommandWord` (basename) + `stripQuotedRuns` (de-quote) to the MODIFIER/prefix tokens too, before the `isCommandPrefix` test, so a path-form `/usr/bin/env` / `/usr/bin/nice` / `/usr/bin/xargs` resolves to `env`/`nice`/`xargs` and is skipped exactly like the bare form → the trailing `node` becomes the real command word. Today `isCommandPrefix` (admission-guard.ts:367) tests the RAW token, so a path-form modifier escapes the prefix-skip and `node` reads as an argument.

2. **Class B (command substitution / backtick command word).** Treat a command word that IS or CONTAINS a command substitution `$( … )` or a backtick in command-word position as statically unresolvable → FAIL CLOSED (gate) if that segment carries the admit shape. Extend the existing `$X`/`eval`/`sh -c` fail-closed posture to `$()`/backtick; close the `$X`-fails-closed-but-`$()`-fails-open asymmetry. Today `$(` opens a segment at :313 (exposing the inner word) and backticks are unhandled at :355 — the substitution that PRODUCES the command word fails OPEN.

3. **Class E (multi-admit shield — CRITICAL).** The CLASSIFIER IS the segment walk: enumerate EVERY live admit segment and gate if ANY resolves to a gated severity (not just the first `admit`). DELETE `noteFileFromCommand` + the naive `tokenize` second walk — `liveTokens` becomes the single source of truth for note-file resolution too. Under `high-severity`, `node …admit T <routine> ; node …admit T <high-sev>` must DENY. Per-segment-classify-and-gate-if-any is the principled semantics: a routine-only multi-admit under `high-severity` stays ALLOWED (no new over-block).

4. **Class F (masked tier inconsistency, defense-in-depth).** The per-segment note read uses a `validate()`-consistent parse (reject duplicate/indented `by` via `parseNote`'s `duplicateKeys`/`malformedLines` rather than last-wins) so the hook and `admit()` classify identically. Today the hook reads `parsed.scalars.by` (last-wins) at :632 without checking the recorded duplicates/malformed lines that `validate()` (context-io.ts:542-557) rejects.

Plus the D-12 proof: the RED-first capture of each new class ALLOWing vs the PRE-FIX committed `.js` (blob `1b822df1…`, HEAD `a43c962`), the extended child-spawn suite + anti-whack-a-mole command-resolution fuzz, the RED->GREEN proof artifacts, the rebuilt fresh `.js`, and the HARD-STOP independent both-angle red-team checkpoint.

Purpose: make GOV-01's un-forgeable primary tier actually un-forgeable across the FULL command-resolution surface (path-form modifiers, dynamic command-word production, multi-admit ordering) and make the hook's note classification consistent with `validate()` — by UNIFYING the matcher and classifier into ONE `liveTokens` authority, proven adversarially vs the committed `.js`, not by a green author suite.
Output: the unified per-segment resolution authority (Classes A/B/E/F closed, the second walk deleted), the extended suite + command-resolution fuzz, the RED/GREEN proof artifacts, the rebuilt fresh `.js`, and the independent both-angle red-team confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-PATTERNS.md
@.planning/phases/25-governance-on-a-dial/25-VERIFICATION.md
@.planning/phases/25-governance-on-a-dial/25-05-SUMMARY.md
@.planning/phases/25-governance-on-a-dial/25-05-RED-baseline.txt
@.planning/phases/25-governance-on-a-dial/25-05-GREEN-proof.txt
</context>

<artifacts_this_phase_produces>
New / changed symbols and files this round-3 gap-closure introduces (for the executor and downstream consumers):

- `hooks/admission-guard.ts`:
  - ONE `liveTokens`-based per-segment resolution authority — e.g. a `resolveSegment(tokens, from)` / `liveAdmitSegments(cmd)` step that walks the live tokens once and yields, per LIVE command segment, `{ isLiveAdmit, noteFile, by/severity }`. It replaces BOTH the launcher-only `isAdmitInvocation` membership test AND the naive second walk.
  - Command-word resolution applied to EVERY leading-run token: the existing `effectiveCommandWord` (basename) + `stripQuotedRuns` (de-quote) are applied to the modifier/prefix tokens in `isCommandPrefix` (or in a `resolvePrefixWord` step the prefix-skip loop calls), so a path-form `/usr/bin/env`/`/usr/bin/nice`/`/usr/bin/xargs` resolves and is skipped (Class A).
  - Fail-closed `$( … )` / backtick command-word handling: the tokenizer becomes aware of `$(` and backtick in COMMAND-WORD position (today `$(` opens a segment at :313 and backticks are unhandled), and a command word that is/contains a command substitution → unresolvable → gate when the live segment carries the admit shape (Class B).
  - Per-segment severity classification over EVERY live admit segment; the command gates if ANY segment is a gated severity (Class E). `noteFileFromCommand` + the standalone `tokenize` (:500/:509) are DELETED.
  - A `validate()`-consistent per-segment note read that rejects a duplicate / indented `by` (via `parseNote`'s `duplicateKeys`/`malformedLines`) instead of last-wins (Class F).
- `hooks/admission-guard.test.ts`: extended child-spawn cases vs the committed `.js` — Class A path-form modifiers DENY; Class B `$()`/backtick command word GATES; Class E multi-admit (both orderings, `;`/`&&`/`|`/subshell) DENIES while routine-only ALLOWs; Class F duplicate/indented `by` DENIES; inert + no-admit-shape + zero-config ALLOW; round-2 launchers + GAP-B/GAP-D still DENY.
- `scripts/floor-invariance.test.ts`: the anti-whack-a-mole command-RESOLUTION fuzz (path-form-modifier × command-substitution × multi-admit-ordering × {LF|CRLF}); the round-2 sweeps preserved.
- `25-06-RED-baseline.txt`, `25-06-GREEN-proof.txt`: the captured RED->GREEN proof artifacts (22-08/22-09 discipline), extending the 25-05 row format.

NOTE: `scripts/context-io.ts` is NOT edited this round (GAP-C/GAP-D and the audit ledger live there and are confirmed solid). The Class F fix lives in the hook's note read, reusing the EXPORTED `parseNote` (and, if the executor judges it cleaner, the exported `validate`) from `scripts/context-io.js` — no new context-io.ts logic. If the executor finds a Class F fix genuinely requires a small additive context-io helper, it must keep GAP-C/GAP-D and the ledger byte-identical and state the reasoning in the summary.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 25-06-01: Unify the matcher + classifier into ONE liveTokens per-segment authority — command-word resolution over every leading-run token (Class A) + `$()`/backtick fail-closed (Class B) + DELETE the second walk — capture the RED baseline first</name>
  <files>hooks/admission-guard.ts, hooks/admission-guard.js, hooks/admission-guard.test.ts, 25-06-RED-baseline.txt</files>
  <read_first>
    - hooks/admission-guard.ts:85-149 (ADMIT_VERB / ADMIT_SCRIPT / LAUNCHERS / COMMAND_MODIFIERS / DYNAMIC_EVAL_WORDS / effectiveCommandWord / stripQuotedRuns — the resolution primitives that must now be applied to EVERY leading-run token, not just the resolved word)
    - hooks/admission-guard.ts:179-361 (the liveTokens shell-segment tokenizer — the PRESERVED 25-04/25-05 segmentation; read the `$(`/`(` segment-opener at :313-314 and the absence of backtick handling at :355 — Class B needs `$(`/backtick awareness in COMMAND-WORD position; do NOT rewrite the quoting/comment/heredoc/continuation/separator handling)
    - hooks/admission-guard.ts:363-465 (isCommandPrefix at :367-373 — tests the RAW token, the Class A root; segmentHasAdmitShape :378-386; isEnvIndirectedWord :389-391; rawTextHasAdmitShape :402-404; rawSegmentText :409-419; isAdmitInvocation :431-465 — the matcher to fold into the ONE authority)
    - hooks/admission-guard.ts:495-517 (the naive `tokenize` :500 + `noteFileFromCommand` :509 — the SECOND WALK to DELETE; `findIndex(t === "admit")` :511 sees only the first admit, the Class E root; note-file resolution must move into the liveTokens authority)
    - hooks/admission-guard.ts:533-557 (the SELF_APPROVE in-front-of-early-exit check + commandHasLiveAdmitShape — GAP-B, PRESERVED; the self-set check must stay independent of the matcher early-exit)
    - hooks/admission-guard.ts:550-619 (the matcher early-exit `if (!isAdmitInvocation(cmd)) process.exit(0)` :555 + the dial canonicalization :596-608 + the noteFile resolution :612-619 — these consume the new authority; the gate-every-match / off logic is PRESERVED)
    - hooks/admission-guard.ts:648-673 (the isHighSeverity case-insensitive classification :656 + isGated :657 + the deny :659-670 — GAP-D, PRESERVED; the per-segment authority must still feed this classification)
    - .planning/phases/25-governance-on-a-dial/25-05-SUMMARY.md:43-78 (the round-3 Class A/B/E/F tables with the exact reproduced commands + the root-cause line refs)
    - .planning/phases/25-governance-on-a-dial/25-05-RED-baseline.txt and 25-05-GREEN-proof.txt (the both-direction proof ROW FORMAT this task's 25-06-RED-baseline.txt must extend)
    - hooks/admission-guard.test.ts:1-90 (the runGuard / expectDeny / expectAllow child-spawn harness + makeProject/writeNote fixtures vs the COMMITTED admission-guard.js; the `>= guard.test.ts` case-count watch; extend, never reimplement)
    - hooks/guard.ts:50,88 (the verb-anchor + refuse-self-set posture to MIRROR — DO NOT EDIT guard.ts, D-02; read for pattern only)
  </read_first>
  <behavior>
    - CAPTURE RED FIRST (the 22-08/22-09 discipline): before any source edit, child-spawn each round-3 class through the CURRENT committed `hooks/admission-guard.js` (blob 1b822df1…, HEAD a43c962) on a planted high-severity note (by: security-nfr) under human_admission: high-severity with NO approval var, and record that each ALLOWs (no `"deny"` in stdout while the identical command admits): (A) a path-form command modifier — `/usr/bin/env node <ctxio> admit T NOTE`, `/usr/bin/nice node …`, `/usr/bin/env -S node …`, `/usr/bin/xargs node …`, `./nice node …`, doubled `/usr/bin/env /usr/bin/nice node …`; (B) a command-substitution / backtick command word — `$(echo node) <ctxio> admit T NOTE`, `` `echo node` <ctxio> admit T NOTE ``, `$(printf node) …`, `$(basename /usr/bin/node) …`; (E) a multi-admit shield — `node <ctxio> admit T ROUTINE ; node <ctxio> admit T NOTE` (routine first, high-severity second). Save to 25-06-RED-baseline.txt (extend the 25-05 row format; cite the pre-fix blob).
    - AFTER the fix, against the rebuilt committed `.js`, on a gated high-severity un-approved note → DENY (reason names the note file): (A) every path-form modifier resolves the trailing `node` and DENIES; (B) every `$()`/backtick command word GATES (deny pending a human); (E) the multi-admit shield DENIES because the high-severity segment is classified regardless of order.
    - Class B narrow trigger: a `$()`/backtick/`(` construct with NO admit shape in the live segment → ALLOW (`$(echo hi) ls`, `` `echo hi` ``). The fail-closed gate fires ONLY when the live segment carries the admit shape (context-io reference + admit verb).
    - NO over-block regression (the 25-04/25-05 CR-01-inverse fail-safe stays clean): an inert heredoc body line, a single/double-quoted echoed mention, a `#`-comment mention, a path component literally named `admit`, and `eval "echo hi"` / `bash -c "ls"` (no admit reference) → ALLOW.
    - No regression on the round-2 launchers: subshell `( node …admit )`, `\`-continuation, `npx tsx`, bare `npx`/`tsx`, `env`/`VAR=` prefix, builtin-wrapper `command`/`exec`/`nice`/`xargs node …admit` (bare form), path/basename `/usr/local/bin/node …admit`, `nodejs …admit`, split-quote `no"de"`, brace-group `{ node …admit; }`, the supported bare `node …admit` → still DENY; empty/malformed stdin → ALLOW.
    - GAP-B floor PRESERVED: an inline self-set behind any wrapper (`GRUGOPS_ADMISSION_APPROVED_BY=eve /usr/bin/env node …admit`) DENIES even with the var in env.
  </behavior>
  <action>
    FIRST capture the RED baseline. Against the CURRENT committed `hooks/admission-guard.js` (blob 1b822df1…), child-spawn each round-3 class (A path-form modifier / B `$()`+backtick command word / E routine-first-then-high-severity multi-admit) with a planted high-severity note and NO approval var, and record that each ALLOWs into `25-06-RED-baseline.txt` — proof the bypass was real before the fix, in the 25-05 row format, citing the pre-fix blob.

    THEN unify the matcher and classifier into ONE `liveTokens`-based per-segment resolution authority. Do NOT rewrite the tokenizer's quoting/comment/heredoc/`\`-continuation/separator handling, and do NOT widen a launcher anchor list (the whack-a-mole trap every prior round fell into). Build ONE function — e.g. `liveAdmitSegments(cmd)` (or fold the resolution into a single `resolveSegment` step) — that walks the live tokens once and, for each LIVE command segment, yields `{ isLiveAdmit, noteFile, by/severity-source }`:

    CLASS A — resolve the effective command word over EVERY leading-run token. In the prefix-skip loop (today admission-guard.ts:437-441), apply `effectiveCommandWord` (basename) AND `stripQuotedRuns` (de-quote) to EACH leading-run token BEFORE the `isCommandPrefix` test — not only to the final resolved word. So a path-form `/usr/bin/env` resolves to `env` and is skipped as a modifier, `/usr/bin/nice` → `nice`, `/usr/bin/xargs` → `xargs`, `./nice` → `nice`, and a doubled `/usr/bin/env /usr/bin/nice` is skipped through to the trailing `node`. Change `isCommandPrefix` to test the RESOLVED (basenamed + de-quoted) token (or interpose a `resolvePrefixWord` the loop calls), keeping `VAR=` assignment-prefix detection on the RAW token (an assignment must keep its `=`). The effective command word of the segment is then the basenamed + de-quoted first non-prefix token, tested against the launchers `{node, npx, tsx, nodejs}` exactly as today.

    CLASS B — `$( … )` / backtick command word fails CLOSED. Make the tokenizer aware of a command substitution `$( … )` or a backtick run in COMMAND-WORD position: rather than opening a fresh segment at the inner word (today `$(` at :313 exposes the inner `echo`), recognize that the OUTER command word the shell forms is dynamically produced and statically unresolvable. When a LIVE segment's effective command word IS or CONTAINS a `$( … )` substitution or a backtick run in command-word position, AND the segment carries the admit shape (a context-io reference + the admit verb), treat it as a live admit and GATE — the same fail-closed posture the resolver already applies to `eval`/`sh -c`/`$X` (DYNAMIC_EVAL_WORDS / isEnvIndirectedWord). Close the asymmetry: `$X …admit` already fails closed; `$(…) …admit` and `` `…` …admit `` must too. The trigger stays NARROW — a `$()`/backtick with NO admit shape in the live segment is NOT gated. Decide whether the cleanest implementation tracks command-word-position substitutions in the tokenizer or detects them in the per-segment resolver; state the choice and why in the summary, keeping the tokenizer's OTHER behavior (quoted/comment/heredoc/continuation/`;`/`&`/`|`/subshell) byte-identical.

    CLASS E — the classifier IS the segment walk. Enumerate EVERY live admit segment (not just the first `admit` token). For each, resolve its note-file from the segment's live tokens — the `admit` CLI signature is `context-io.js admit <task> <noteFile> [root]`, so the note file is the SECOND live token after the `admit` verb within that segment. Classify each segment's severity (read its note's `by`, lowercased + trimmed for the case-insensitive membership test — GAP-D preserved). The command GATES if ANY live admit segment resolves to a gated severity under the active dial (gate-every-match for `all`/non-canonical; high-severity-role-only for `high-severity`). DELETE the standalone `tokenize` (:500) and `noteFileFromCommand` (:509) — note-file + severity resolution now flows through the ONE liveTokens authority. The per-segment-classify-and-gate-if-any rule is the principled semantics: a command bundling ONLY routine admits under `high-severity` stays ALLOWED (do NOT add a blunt "gate any multi-admit" rule — that is a prohibited over-block).

    Fold `isAdmitInvocation` and the dial/severity/deny logic to consume the new per-segment authority: the matcher early-exit `if (no live admit segment) process.exit(0)` (today :555) and the gated-segment decision both read from the ONE walk. PRESERVE the GAP-B self-set check in front of / independent of the early-exit (admission-guard.ts:542 — the self-set must DENY on a matcher false-negative), the dial canonicalization (`off` exits; `gateEveryMatch = dial !== "high-severity"` for the non-canonical-dial GAP-C handling), and the case-insensitive `HIGH_SEVERITY_ROLES` membership (GAP-D). On a matched gated admit where a note file cannot be located OR a note cannot be read/parsed → FAIL CLOSED (deny), exactly as today.

    Reuse the EXPORTED `parseNote` from `scripts/context-io.js` for the per-segment note read (Class F's validate()-consistency lands in Task 25-06-02; this task's note read may stay as-is and 25-06-02 hardens it — OR fold both into this task if the executor judges the per-segment note read cleaner to write once; state the split in the summary). Do NOT hand-roll YAML — the single-grammar parse discipline (P22/CMP-02) is mandatory.

    Voice: CLEAR PROFESSIONAL VOICE in all comments and deny reasons (CLAUDE.md hard rule — governance/admission/audit surfaces are never caveman voice). Comment the authority so a future reader sees ONE liveTokens walk yielding {isLiveAdmit, noteFile, by/severity} per segment — not a matcher plus a separate classifier that can drift.

    Comment-text discipline: do NOT place a fenced code block inside this action's prose; describe the authority by its named functions, the resolution primitives applied to every leading-run token, the `$()`/backtick fail-closed branch, and the per-segment classify-and-gate-if-any rule.

    Rebuild the committed `.js`: `npm run build` then `npm run freshness` (0 drift; `admission-guard.js` is freshness-covered).

    Extend `hooks/admission-guard.test.ts` (the child-spawn oracle vs the COMMITTED `.js`): add cases for Class A (each path-form modifier → DENY), Class B (`$(echo node)` / backtick / `$(printf node)` / `$(basename …)` command word → GATE; a `$()`/backtick with NO admit shape → ALLOW), Class E (routine-first-then-high-severity `;`/`&&`/`|`/subshell → DENY; routine-only multi-admit under high-severity → ALLOW), and re-assert the no-over-block inertness (heredoc/quoted/comment/eval-or-$()-with-no-admit ALLOW), the round-2 launchers (still DENY), and the GAP-B self-set-behind-a-wrapper (DENY). Keep the case count >= guard.test.ts (Pitfall-2 watch). Capture the post-fix DENY/GATE/ALLOW outcomes for these forms into `25-06-GREEN-proof.txt` (this task contributes the Class A/B/E matcher rows; Task 25-06-02 adds Class F; Task 25-06-03 completes the proof set).
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run hooks/admission-guard.test.ts && git diff --quiet hooks/guard.ts</automated>
  </verify>
  <acceptance_criteria>
    - `25-06-RED-baseline.txt` exists and records Class A (path-form modifiers), Class B (`$()`/backtick command word), and Class E (routine-first multi-admit) ALLOWed by the PRE-FIX committed `admission-guard.js` (blob 1b822df1…), in the 25-05 row format citing the pre-fix blob.
    - A grep confirms `function tokenize` and `noteFileFromCommand` NO LONGER exist in hooks/admission-guard.ts (the second walk is deleted); note-file + severity resolution flows through the ONE liveTokens-based per-segment authority; the 25-04/25-05 segmentation tokenizer (quoting/comment/heredoc/continuation/separators) is intact (grep confirms it was not rewritten).
    - `npx vitest run hooks/admission-guard.test.ts` is GREEN: Class A path-form modifiers DENY a gated high-severity un-approved admit; Class B `$()`/backtick command word GATES; Class E multi-admit (routine-first + high-severity, all separators) DENIES while routine-only under high-severity ALLOWs; inert heredoc/quoted/comment mentions and a `$()`/backtick/eval with NO admit reference ALLOW; the round-2 launchers still DENY; the GAP-B self-set-behind-a-wrapper DENIES; empty stdin ALLOWs; case count >= guard.test.ts.
    - `npm run freshness` exits 0 (committed `admission-guard.js` fresh); `git diff --quiet hooks/guard.ts` exits 0 (byte-frozen, D-02); `git diff --quiet scripts/context-io.ts` exits 0 (not edited this task).
    - `25-06-GREEN-proof.txt` records the post-fix DENY for Class A, the GATE for Class B, the DENY for Class E (and the routine-only ALLOW), plus ALLOW for the inert/no-admit-shape cases.
  </acceptance_criteria>
  <done>The matcher and classifier are ONE liveTokens per-segment authority: command-word resolution (basename + de-quote) is applied to every leading-run token so a path-form modifier resolves (Class A), a `$()`/backtick command word fails closed on the admit shape (Class B), every live admit segment is classified and the command gates if ANY is a gated severity (Class E), the naive second walk is deleted, the round-2 launchers + GAP-B floor + inert mentions all hold — vs the committed `.js`; guard.ts + context-io.ts untouched, freshness 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 25-06-02: validate()-consistent per-segment note read (Class F) — reject duplicate / indented `by` in the hook so the two parsing tiers cannot disagree</name>
  <files>hooks/admission-guard.ts, hooks/admission-guard.js, hooks/admission-guard.test.ts, 25-06-GREEN-proof.txt</files>
  <read_first>
    - hooks/admission-guard.ts:44-46 (the `import { parseNote, readGovernanceConfigResult } from "../scripts/context-io.js"` — the EXPORTED parse path the hook already uses; `validate` may also be imported if cleaner)
    - hooks/admission-guard.ts:610-646 (the post-fix note read from Task 25-06-01: locate the note file, `readFileSync`, `parseNote`, read `parsed.scalars.by` — the Class F root is reading scalars.by last-wins WITHOUT checking parsed.duplicateKeys / parsed.malformedLines)
    - scripts/context-io.ts:233-312 (parseNote — it RECORDS `duplicateKeys` (:262/:277) and `malformedLines` (:309) but `scalars` keeps the LAST `by` value (:283); the hook must consult duplicateKeys/malformedLines, not just scalars.by)
    - scripts/context-io.ts:530-557 (validate — it pushes a structural FAIL for each `parsed.duplicateKeys` (:542-544) including a duplicate `by`, and for each `parsed.malformedLines` (:552-557) including an indented ` by:` — the exact consistency the hook must mirror)
    - scripts/context-io.ts:920-962 (admit() D-04 — it calls validate() first (:897) so a duplicate/malformed note is rejected structurally before classification; the hook must be consistent: a duplicate/indented `by` must NOT last-wins-classify to routine and slip through)
    - hooks/admission-guard.test.ts:88-130 (the writeNote fixture + the high/routine/badBy note builders — extend with a duplicate-`by` note and an indented-`by` note)
    - .planning/phases/25-governance-on-a-dial/25-05-SUMMARY.md:80-82 (Class F — the masked defect: hook reads `by` via parseNote last-wins without validate(); duplicate/indented `by` mis-classifies)
  </read_first>
  <behavior>
    - Class F (hook tier consistency): a note whose frontmatter has a DUPLICATE `by` (`by: security-nfr` then `by: software-engineer`) or an INDENTED ` by: security-nfr` (which `validate()` rejects as a malformed line / duplicate key) is NOT last-wins-classified to a routine role in the hook. Under an active dial (`high-severity` or `all`), such a structurally-invalid note on a matched admit → DENY (gate-or-stricter: the hook fails closed on a note it cannot classify consistently with validate()).
    - The hook and `admit()` now classify the SAME note identically: a note that `validate()` rejects structurally is not silently admitted by the hook reading a last-wins `by`.
    - No over-classification regression: a WELL-FORMED note with a single column-0 `by: software-engineer` is still routine (ALLOW under `high-severity`); a well-formed `by: security-nfr` is still high-severity (DENY under `high-severity` without approval). The case-insensitive GAP-D classification of a well-formed `by` is preserved.
    - No over-block on the data path: an inert duplicate-`by` mention inside a heredoc/quoted/comment (not a live admit) still ALLOWs — Class F only changes how a LIVE admit's re-read note is classified.
  </behavior>
  <action>
    In the per-segment note read added by Task 25-06-01 (admission-guard.ts ~:610-646), make the note parse `validate()`-consistent. After `parseNote(text)`, BEFORE classifying severity from `scalars.by`, consult the structural-validity signals the parser already records: `parsed.duplicateKeys` and `parsed.malformedLines`. If the note has a duplicate provenance key (in particular a duplicate `by`) OR a malformed in-fence line (in particular an indented ` by:` that projects to empty), the hook must NOT trust the last-wins `scalars.by` — under an active dial on a matched admit, FAIL CLOSED (deny pending a human), because the note is structurally invalid exactly as `validate()` would reject it.

    Prefer the smallest consistent change: either (a) call the EXPORTED `validate(text)` from `scripts/context-io.js` and, if it returns any structural FAIL, deny the matched gated admit (the hook's note is then rejected on the same grammar as admit()); or (b) read `parsed.duplicateKeys` / `parsed.malformedLines` directly and deny if either flags `by` (or any provenance key). Choose (a) if it does not over-reach (validate() also enforces refuse-self / required fields, which is consistent with gate-or-stricter and does not weaken anything); choose (b) if (a) would deny a note that the hook should pass for an unrelated reason — state which and why in the summary. Either way the rule is: a `by` the hook cannot read consistently with validate() is gate-or-stricter, never a silent routine ALLOW. Do NOT edit `scripts/context-io.ts` — reuse its EXPORTED parseNote/validate.

    Keep the GAP-D case-insensitive classification of a WELL-FORMED single `by` exactly as Task 25-06-01 / round-2 left it. Keep the existing fail-closed deny reasons for an unreadable / no-fence / empty-`by` note; add a deny reason for a structurally-invalid (duplicate/malformed `by`) note that names the fault in clear professional voice.

    Comment-text discipline: no fenced code blocks in this action; name parseNote / validate / duplicateKeys / malformedLines and the consistency rule by concept.

    Rebuild the committed `.js`: `npm run build` then `npm run freshness` (0 drift).

    Extend `hooks/admission-guard.test.ts`: add fixtures for a duplicate-`by` note (`by: security-nfr` then `by: software-engineer`) and an indented-` by:` note; assert under `human_admission: high-severity` (and `all`) that a matched admit re-reading either note → DENY (gate-or-stricter, not last-wins routine). Re-assert a well-formed routine note ALLOWs and a well-formed high-severity note DENIES (no over/under-classification). Capture the Class F DENY rows + the well-formed control rows into `25-06-GREEN-proof.txt`.
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run hooks/admission-guard.test.ts && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run hooks/admission-guard.test.ts` is GREEN: a duplicate-`by` note and an indented-` by:` note on a matched admit DENY under `high-severity` and `all` (gate-or-stricter, not last-wins routine); a well-formed routine note ALLOWs under `high-severity`; a well-formed `by: security-nfr` note DENIES under `high-severity` without approval.
    - The hook classifies a note consistently with `scripts/context-io.ts validate()`: a note validate() rejects structurally is denied (not silently admitted) by the hook on a matched gated admit (grep/test shows the hook consults parsed.duplicateKeys / malformedLines or calls validate(), not a bare last-wins scalars.by).
    - `git diff --quiet scripts/context-io.ts` exits 0 (NOT edited — the fix reuses exported parseNote/validate); `git diff --quiet hooks/guard.ts` exits 0; `npm run freshness` exits 0.
    - `25-06-GREEN-proof.txt` records the Class F DENY rows (duplicate/indented `by`) + the well-formed routine/high-severity control rows.
  </acceptance_criteria>
  <done>The hook's per-segment note read is consistent with admit()'s validate(): a duplicate or indented `by` is gate-or-stricter rather than last-wins mis-classified to routine, so the un-forgeable hook tier and the in-script tier classify the same note identically — Class F closed; context-io.ts untouched, committed `.js` fresh, guard.ts byte-frozen.</done>
</task>

<task type="auto">
  <name>Task 25-06-03: Anti-whack-a-mole command-resolution fuzz (path-form-modifier × command-substitution × multi-admit-ordering) + complete the RED->GREEN proof set (D-12)</name>
  <files>scripts/floor-invariance.test.ts, 25-06-RED-baseline.txt, 25-06-GREEN-proof.txt</files>
  <read_first>
    - scripts/floor-invariance.test.ts:1-70 (the four-invariant structure + the FROZEN_GUARD_BLOB + the child-spawn-vs-committed-`.js` + import-the-committed-`.js` composition — EXTEND this; the round-2 non-string-dial + case-variant sweeps + the effective-command-word obfuscation fuzz live here)
    - scripts/floor-invariance.test.ts (the existing round-2 anti-whack-a-mole command-resolution fuzz over the effective-command-word obfuscation matrix — the round-3 fuzz ADDS the path-form-modifier, command-substitution, and multi-admit-ordering dimensions; do NOT duplicate the round-2 fuzz)
    - hooks/admission-guard.test.ts:1-90 (the runGuard child-spawn harness shape + makeProject/writeNote fixtures to reuse for the fuzz — spawn the COMMITTED admission-guard.js)
    - hooks/admission-guard.ts (the post-fix ONE liveTokens authority from Tasks 25-06-01/02 — the fuzz asserts the CLASS invariant over this authority)
    - .planning/phases/25-governance-on-a-dial/25-05-GREEN-proof.txt (the GREEN-proof ROW FORMAT and the existing anti-whack-a-mole block this task's 25-06-GREEN-proof.txt extends)
    - .planning/phases/25-governance-on-a-dial/25-CONTEXT.md:147-160 (D-12 — green-suite-insufficient; the anti-enumeration property-fuzz requirement)
  </read_first>
  <behavior>
    - Path-form-modifier fuzz: for each modifier in {env, nice, xargs, timeout, nohup, stdbuf, command, exec} × each path spelling {`/usr/bin/`, `/bin/`, `./`, basename-via-`$(basename …)` is Class B not here} × {with and without a flag like `-S` / `-n`}, child-spawn the COMMITTED `admission-guard.js` on a gated high-severity un-approved admit and assert DENY (the trailing `node` resolves). A path-form modifier with NO trailing launcher / no admit shape → ALLOW.
    - Command-substitution / backtick fuzz: for each command-word production in {`$(echo node)`, `` `echo node` ``, `$(printf node)`, `$(basename /usr/bin/node)`, nested `$(echo $(echo node))`, in-modifier-slot `nice $(echo node)`} on a segment carrying the admit shape, assert GATE (DENY). The same constructs with NO admit shape (`$(echo hi) ls`) → ALLOW.
    - Multi-admit-ordering fuzz: for each separator in {`;`, `&&`, `||`, `|`, `&`, subshell `( … ) ; … `} × each ordering {routine-then-high, high-then-routine, 3 admits with the high in any position} under `human_admission: high-severity`, assert DENY (the high-severity segment is classified regardless of position). A multi-admit command with ONLY routine segments under `high-severity` → ALLOW (no over-block). Under `all`, ANY multi-admit → DENY (every admit gated).
    - Class invariant (the structural proof, not enumeration): for EVERY generated variant the rule holds — a gated admit in ANY live segment → DENY; a routine-only command under high-severity → ALLOW; an inert/no-admit-shape command → ALLOW. A hypothetical un-enumerated obfuscation shape #N is caught structurally because resolution + per-segment classification is ONE authority, not a list of known forms.
    - The four named floor invariants (refuse-self, no-fabrication/never-rewrite, `quality.test_integrity` no-`off`, `hooks/guard.ts` byte-frozen) still hold at every value (preserve the existing 25-03/25-04/25-05 assertions; do not weaken them). The round-2 non-string-dial + case-variant `by` sweeps stay green.
  </behavior>
  <action>
    Extend `scripts/floor-invariance.test.ts` (do NOT duplicate the round-2 effective-command-word fuzz or the non-string-dial / case-variant sweeps — ADD the path-form-modifier, command-substitution, and multi-admit-ordering dimensions). Reuse the child-spawn-vs-committed-`.js` harness shape from `hooks/admission-guard.test.ts` and the temp-config + temp-note fixtures (write a high-severity `by: security-nfr` note and a routine `by: software-engineer` note; point a temp factory.config.json at `high-severity` / `all`).

    Add the path-form-modifier fuzz: enumerate {modifier spelling} × {path prefix} × {flag?} on a gated high-severity un-approved admit and assert DENY vs the committed `.js` (Class A class invariant); a path-form modifier with no launcher / no admit shape ALLOWs.

    Add the command-substitution / backtick fuzz: enumerate the command-word-production forms (incl. nested and in-modifier-slot) on a segment carrying the admit shape and assert GATE (DENY); the same constructs with no admit shape ALLOW (Class B class invariant + the narrow-trigger / no-over-block control).

    Add the multi-admit-ordering fuzz: enumerate {separator} × {ordering, 2 and 3 admits} under `high-severity` and assert DENY whenever ANY segment is high-severity (both orderings, high in any position); a routine-only multi-admit under `high-severity` ALLOWs; under `all` any multi-admit DENIES (Class E class invariant + the routine-only no-over-block control).

    Make the structural argument explicit in a comment: the matcher and classifier are ONE liveTokens authority, so command-word resolution is uniform over every leading-run token and EVERY live admit segment is classified — a launcher/modifier spelling or admit-ordering outside any narrow anchor cannot escape, and a dynamically-produced command word fails closed. The proof is the class invariant, not an enumeration of known forms.

    COMPLETE the proof artifacts: finalize `25-06-RED-baseline.txt` (pre-fix committed `.js` blob 1b822df1… at HEAD a43c962 ALLOWs each round-3 class — Class A path-form modifier, Class B `$()`/backtick command word, Class E routine-first multi-admit shield, Class F duplicate/indented `by`) and `25-06-GREEN-proof.txt` (post-fix committed `.js` DENIES/GATES each class naming the note; the routine-only multi-admit under high-severity + the inert heredoc/quoted/comment + the `$()`/backtick/eval-with-no-admit-shape + the zero-config absent lean `off` still ALLOW; the round-2 launchers + GAP-B/GAP-D still DENY). Extend the 25-05 row format; reference the committed `.js`, never the `.ts`.

    Voice: CLEAR PROFESSIONAL VOICE in comments (this test owns the SC1 floor). Per D-12, a GREEN fuzz is NECESSARY-NOT-SUFFICIENT — the independent red-team at Task 25-06-04 is the gate; state that in a comment.

    Comment-text discipline: no fenced code blocks in this action; describe the matrix dimensions and the class invariant by concept.
  </action>
  <verify>
    <automated>npx vitest run scripts/floor-invariance.test.ts && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/floor-invariance.test.ts` is GREEN with the path-form-modifier fuzz added: every {modifier × path × flag} on a gated high-severity un-approved admit DENIES via the committed `admission-guard.js`; a path-form modifier with no launcher / no admit shape ALLOWs.
    - The command-substitution / backtick fuzz is GREEN: every command-word-production form on a segment with the admit shape GATES; the same constructs with no admit shape ALLOW.
    - The multi-admit-ordering fuzz is GREEN: every {separator × ordering, 2/3 admits} under `high-severity` with a high-severity segment in any position DENIES; a routine-only multi-admit under `high-severity` ALLOWs; under `all` any multi-admit DENIES.
    - The four named floor invariants still hold at every value; the round-2 non-string-dial + case-variant `by` sweeps stay green; `git diff --quiet hooks/guard.ts` and `git diff --quiet scripts/context-io.ts` exit 0.
    - `25-06-RED-baseline.txt` and `25-06-GREEN-proof.txt` are complete (each round-3 class: pre-fix ALLOW, post-fix DENY/GATE; routine-only / inert / no-admit-shape / zero-config ALLOW) and reference the committed `.js`, not the `.ts`, extending the 25-05 row format.
  </acceptance_criteria>
  <done>An anti-whack-a-mole command-resolution fuzz over the path-form-modifier × command-substitution × multi-admit-ordering matrix proves the class invariant structurally (with the routine-only / no-admit-shape / inert ALLOW controls); the round-2 sweeps + four floor invariants stay green; the RED->GREEN proof artifacts are captured vs the committed `.js`; guard.ts + context-io.ts byte-unchanged — the SC1 author proof is complete (independent red-team still the gate, D-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 25-06-04: SC1 round-3 closure gate — INDEPENDENT both-angle red-team vs the committed .js (HARD STOP; green suite is NOT proof, D-12)</name>
  <read_first>
    - .planning/phases/25-governance-on-a-dial/25-05-SUMMARY.md:37-105 (the round-2 red-team that found these round-3 classes — the exact Class A/B/E/F reproductions, the both-angle dispatch lesson (the LOGIC agent found Class E, the input-surface agent did not), the structural root fix; the bar this checkpoint must clear)
    - .planning/phases/25-governance-on-a-dial/25-06-RED-baseline.txt and 25-06-GREEN-proof.txt (the both-direction proof the independent probe reproduces vs the committed `.js`)
    - hooks/admission-guard.js (the COMMITTED artifact the probe attacks — never the `.ts`, never the author suites); scripts/context-io.js (the imported parse the hook uses — unchanged GAP-C/GAP-D)
  </read_first>
  <what-built>The round-3 gap-closure UNIFY of the GOV-01 un-forgeable tier: ONE `liveTokens`-based per-segment resolution authority replacing the diverged matcher + naive second-walk classifier — command-word resolution over every leading-run token (Class A), `$()`/backtick fail-closed (Class B), per-segment classify-and-gate-if-any over every live admit segment (Class E, the critical multi-admit shield), and a validate()-consistent per-segment note read (Class F) (Tasks 25-06-01/02); plus the anti-whack-a-mole command-resolution fuzz + RED->GREEN proof artifacts (Task 25-06-03). This is the LAST task of the round-3 gap-closure plan. Per [[grugops-safety-invariant-green-suite-insufficient]] and D-12, a passing suite is necessary but NOT sufficient for this safety floor — the author's suites passed and STILL missed these classes across rounds 1, 2, and 3 (this is the 8th green-suite-insufficient catch of v2.0). This checkpoint HARD-STOPS until an INDEPENDENT opus-grade adversary (not the authoring executor's own suites) has reproduced the closure. Do NOT declare closure on a green suite alone.</what-built>
  <how-to-verify>
    An INDEPENDENT opus-grade probe must run BOTH angles (the P23 split — guard LOGIC and guard INPUT SURFACE are different blind spots; on round 2 the LOGIC agent found Class E the multi-admit shield and the input-surface agent did NOT, so BOTH are required) and reproduce ALL of the following against the COMMITTED artifacts (`hooks/admission-guard.js`; `scripts/context-io.js` for the imported parse), never the `.ts` and never the author's own `admission-guard.test.ts` / `floor-invariance.test.ts`:
    1. INPUT-SURFACE angle — Class A: each path-form command modifier now DENIES a gated high-severity un-approved admit (`/usr/bin/env node …admit`, `/usr/bin/nice node …`, `/usr/bin/env -S node …`, `/usr/bin/xargs node …`, `./nice node …`, doubled). Class B: each command-substitution / backtick command word GATES (`$(echo node) …admit`, `` `echo node` …admit ``, `$(printf node) …`, `$(basename /usr/bin/node) …`, nested, in-modifier-slot). AND the over-block-clean angle is preserved: an inert heredoc body line, a quoted/echoed mention, a `#`-commented mention, and a `$()`/backtick/eval/`sh -c` with NO admit reference → ALLOW. AND the adversary INVENTS at least one input-surface obfuscation shape the author did NOT enumerate (e.g. a deeper path-form-modifier chain, a `$(/usr/bin/echo node)` path-form-inside-substitution, a quoted-path modifier `"/usr/bin/env" node …`) — it must be caught structurally (gated or denied), proving command-word resolution is ONE authority, not an anchor list.
    2. LOGIC angle — Class E (the critical one): under `human_admission: high-severity`, `node …admit T <routine> ; node …admit T <high-sev>` (and `&&`/`||`/`|`/`&`/subshell, both orderings, 3-admit variants) DENIES because the high-severity segment is classified regardless of position; a routine-only multi-admit under `high-severity` ALLOWs (no over-block); under `all` any multi-admit DENIES. Class F: a duplicate-`by` or indented-` by:` note on a matched admit DENIES (gate-or-stricter, consistent with validate()), not last-wins routine. AND the adversary INVENTS at least one logic obfuscation shape the author did NOT enumerate (e.g. a 4-admit ordering, a high-severity admit shielded behind two routine admits, a duplicate `by` combined with a path-form modifier).
    3. BOTH-DIRECTION RED-vs-committed-`.js`: the clean case (routine / hook-disposed admit) ALLOWs; the zero-config absent lean reads `off` and ALLOWs a routine admit; each planted round-3 class DENIES/GATES naming the note. Confirm against `25-06-RED-baseline.txt` (pre-fix ALLOWed the class) and `25-06-GREEN-proof.txt` (post-fix DENIES/GATES). The probe reproduces these independently, not from the author's suite.
    4. PRESERVED + FLOOR + freshness: the round-2 LOGIC closures still hold (GAP-B self-set behind a wrapper DENIES; GAP-C non-string/corrupt dial gate-or-stricter while absent stays lean; GAP-D case-variant `by` gated high-severity); the four named floor invariants hold at every dial value (refuse-self, no-fabrication/never-rewrite, `quality.test_integrity` has no `off`, `hooks/guard.ts` byte-frozen via `git diff --quiet hooks/guard.ts` exit 0 and blob `3501810e21308e4b7e219679a6ca30dace9b5d66`); `git diff --quiet scripts/context-io.ts` exit 0 (not edited); `npm run freshness` exits 0; WR-01 did not false-positive on the resolver edits or the new artifacts; the single-authority structural assertion holds (`function tokenize` / `noteFileFromCommand` gone).
    5. The probe's findings (the exact reproduced commands, the invented obfuscation shapes for BOTH angles, the deny/gate/allow outcomes, the routine-only / inert / no-admit-shape controls, the preserved round-2 results) are DOCUMENTED in `25-06-SUMMARY.md` — reproduced by an independent adversary, NOT self-reported as a green suite.
  </how-to-verify>
  <acceptance_criteria>
    - (a) Each Class A path-form modifier DENIES and each Class B command-substitution/backtick command word GATES + an adversary-invented fresh input-surface shape DENY/GATE vs the committed `admission-guard.js`; the heredoc/quoted/comment mentions and a no-admit-shape `$()`/backtick/eval command ALLOW — reproduced by an INDEPENDENT input-surface probe, not the author's suite (D-12, P23 input-surface angle).
    - (b) Class E multi-admit (routine-first/high-first, all separators, 2/3 admits) DENIES under high-severity while a routine-only multi-admit ALLOWs; Class F duplicate/indented `by` DENIES; + an adversary-invented fresh logic shape — reproduced by an INDEPENDENT logic probe (D-12, P23 logic angle; the angle that found Class E on round 2).
    - (c) The both-direction proof matches `25-06-RED-baseline.txt` (pre-fix ALLOW) → `25-06-GREEN-proof.txt` (post-fix DENY/GATE naming the note; routine-only / inert / no-admit-shape / zero-config ALLOW), reproduced vs the committed `.js`.
    - (d) The round-2 closures (GAP-B/C/D) still hold; the four floor invariants hold; `git diff --quiet hooks/guard.ts` exits 0; `git diff --quiet scripts/context-io.ts` exits 0; `npm run freshness` exits 0; WR-01 no false-positive; the single-authority structural assertion holds (second walk deleted); findings documented in `25-06-SUMMARY.md` — independently reproduced, not self-reported.
  </acceptance_criteria>
  <action>
    HARD STOP — do NOT auto-pass on a green author suite (the exact D-12 / green-suite-insufficient failure mode this phase already hit THREE times). Dispatch (or hand off to the human to dispatch) an INDEPENDENT opus-grade red-team — an adversary with different blind spots than the authoring executor — to reproduce (a)–(d) against the COMMITTED `hooks/admission-guard.js` and `scripts/context-io.js` (never the `.ts`, never the author's own suites). The probe MUST run BOTH a LOGIC probe (a multi-admit shield ordering / a duplicate-or-indented `by` mis-classify / a config-shape edge opening a hole) AND an INPUT-SURFACE attack (a path-form modifier or command-substitution command word escaping the resolver, or an inert mention reading as a live admit) — these are the distinct blind spots the P23 lesson and THIS phase's THREE prior red-teams proved both matter (on round 2 ONLY the logic angle found the multi-admit shield). The adversary should INVENT at least one shape per angle that the author did NOT enumerate (test the unified authority over command-resolution rather than an anchor list — e.g. a `$(/usr/bin/echo node)` path-form-inside-substitution, a deeper modifier chain, a 4-admit ordering, a high-severity admit shielded behind two routine admits) and confirm it is caught structurally (denied or gated). Record the reproduced commands, the invented shapes, the clean=allow / planted=deny outcomes, the routine-only / inert / no-admit-shape controls, the preserved GAP-B/C/D results, and confirm guard.ts + context-io.ts byte-unchanged + freshness 0 + the second walk deleted in `25-06-SUMMARY.md`. Then surface the result to the human for the resume signal. This task is the phase safety floor — it blocks until the independent both-angle proof is in hand.
  </action>
  <verify>
    <human-check>An independent opus-grade red-team has reproduced (a)–(d) vs the committed `.js`: each Class A path-form modifier DENIES and each Class B `$()`/backtick command word GATES (+ an invented input-surface shape), the heredoc/quoted/comment + no-admit-shape mentions ALLOW (input-surface angle); the Class E multi-admit shield DENIES under high-severity in every ordering/separator while a routine-only multi-admit ALLOWs, the Class F duplicate/indented `by` DENIES (+ an invented logic shape) (logic angle); the both-direction proof matches the RED→GREEN artifacts; the round-2 GAP-B/C/D closures + the four floor invariants hold with `git diff --quiet hooks/guard.ts` exit 0, `git diff --quiet scripts/context-io.ts` exit 0, freshness 0, and the second walk deleted; findings recorded in `25-06-SUMMARY.md`. NOT a self-reported author green suite (D-12).</human-check>
  </verify>
  <resume-signal>Type "approved" once the independent both-angle red-team has reproduced (a)–(d) above and the findings (incl. the invented obfuscation shapes for BOTH angles) are recorded in 25-06-SUMMARY.md, or describe the bypass / false-positive found. Do NOT approve on a green author suite alone (D-12, the terminal project lesson — this phase already hit it THREE times).</resume-signal>
  <done>An independent opus-grade red-team (BOTH logic and input-surface angles) has reproduced the round-3 closure vs the committed `.js`: every Class A path-form modifier is DENIED, every Class B `$()`/backtick command word is GATED, the Class E multi-admit shield is DENIED in every ordering/separator while a routine-only multi-admit ALLOWs, the Class F duplicate/indented `by` is DENIED, an invented un-enumerated shape per angle is caught structurally, the inert / no-admit-shape mentions ALLOW, the round-2 GAP-B/C/D closures + the four floor invariants + guard.ts/context-io.ts byte-frozen status hold, the second walk is deleted, findings are documented in `25-06-SUMMARY.md`, and the human approved; SC1 is closed by an independent adversary, not a green suite (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bash `tool_input.command` → admission-guard hook | The command string is untrusted and adversarial. After 25-04 solved segmentation and 25-05 added effective-command-word resolution at one point, the agent can still (A) spell a command modifier as a PATH so it escapes the raw-token prefix-skip, (B) PRODUCE the command word via `$( … )` / backtick so it fails open, or (E) SHIELD a high-severity admit behind a routine admit so the first-admit-only classifier never sees it. The hook must resolve the effective command word over EVERY leading-run token, fail closed on a dynamically-produced command word, and classify EVERY live admit segment — as ONE authority. |
| note `by` authoring role → severity classification (tier consistency) | `by` is untrusted; a DUPLICATE or INDENTED `by` that `validate()` rejects must NOT last-wins-classify to routine in the hook (Class F). The hook's note read must be consistent with admit()'s validate() so the two tiers cannot disagree. |
| inline approval-var set → refuse-self-set (D-01 floor) | (PRESERVED from 25-05) A self-set behind any wrapper must be refused independent of matcher recognition. |
| governance config value → hook / admit() | (PRESERVED from 25-05, context-io.ts unchanged) A present non-string / non-object / corrupt config is gate-or-stricter; only exact JSON `"off"` is off-equivalent; absent stays lean. |
| admit() → audit ledger | (Preserved from 25-03, unchanged here) Under `retained`, the ledger records the admission record only, never the note body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-19 | Spoofing / Elevation (path-form command modifier) | a real gated high-severity `admit` whose modifier is spelled as a PATH (`/usr/bin/env`/`/usr/bin/nice`/`/usr/bin/xargs`/`./nice`) escaping the raw-token prefix-skip so the trailing `node` reads as an argument | mitigate | Apply effectiveCommandWord (basename) + de-quote to EVERY leading-run token before isCommandPrefix, so a path-form modifier resolves and is skipped like the bare form; proven both-direction RED vs the committed `.js` + the path-form-modifier fuzz (Task 25-06-01/03; Class A; D-12). |
| T-25-20 | Spoofing / Elevation (dynamically-produced command word) | a gated admit whose command word is PRODUCED by `$( … )` / backtick (`$(echo node) …admit`) failing OPEN, asymmetric with `$X` which fails closed | mitigate | Treat a `$( … )` / backtick command word in command-word position as statically unresolvable → fail-closed GATE when the live segment carries the admit shape; close the `$X`-vs-`$()` asymmetry; proven by the command-substitution fuzz + RED vs committed `.js` (Task 25-06-01/03; Class B; D-12). |
| T-25-21 | Elevation of Privilege (multi-admit shield — CRITICAL) | a high-severity admit shielded behind a routine admit (`node …admit T routine ; node …admit T high-sev`) under `high-severity`, because the classifier reads only the first admit | mitigate | Make the classifier the segment walk: enumerate EVERY live admit segment and gate if ANY resolves to a gated severity; delete the naive first-admit-only second walk; per-segment-classify-and-gate-if-any (routine-only stays allowed — no over-block); proven by the multi-admit-ordering fuzz + RED vs committed `.js` (Task 25-06-01/03; Class E; D-12). |
| T-25-22 | Spoofing (tier-inconsistent note classification) | a duplicate/indented `by` that `validate()` rejects but the hook last-wins-classifies to routine, slipping a high-severity admit under `high-severity` | mitigate | Make the hook's per-segment note read consistent with validate() (reject duplicate/malformed `by` via parseNote's duplicateKeys/malformedLines, or call validate()); the hook and admit() classify the same note identically (Task 25-06-02; Class F; D-06/D-12). |
| T-25-23 | Denial of Service (over-block regression) | the `$()`/backtick fail-closed gate, the multi-admit gate, or the path-form resolution over-blocking an inert mention, a no-admit-shape dynamic command, or a routine-only multi-admit | mitigate (fail-safe) | The fail-closed gates fire ONLY on the admit shape in a LIVE segment; a routine-only multi-admit under `high-severity` stays ALLOWED (no blunt gate-any-multi-admit rule); inert heredoc/quoted/comment mentions and no-admit-shape dynamic commands stay ALLOW; proven by the fuzz + the independent red-team (Task 25-06-01/03/04). |
| T-25-24 | Tampering (single-authority drift) | reintroducing a second command parser that can drift from the matcher (the disease behind A/B/E/F) | mitigate | DELETE the naive tokenize + noteFileFromCommand; liveTokens is the ONE source of truth for segmentation + command-word resolution + note-file/severity classification; a grep asserts the second walk is gone (Task 25-06-01; anti-whack-a-mole / "the boundary IS the parser"). |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | N/A — this gap-closure installs NO external package (RESEARCH Package Legitimacy Audit: not applicable; typescript/vitest already present). No install task exists. |
</threat_model>

<verification>
- `npm run build && npm run freshness` exits 0 (committed `admission-guard.js` is a fresh, byte-faithful tsc build; 0 drift).
- `git diff --quiet hooks/guard.ts` exits 0 (deploy guard byte-frozen, blob `3501810e21308e4b7e219679a6ca30dace9b5d66`, D-02).
- `git diff --quiet scripts/context-io.ts` exits 0 (NOT edited this round — GAP-C/GAP-D + the audit ledger preserved; the Class F fix reuses exported parseNote/validate).
- `npx vitest run hooks/admission-guard.test.ts` green: Class A path-form modifiers DENY; Class B `$()`/backtick command word GATES; Class E multi-admit (all orderings/separators) DENIES while routine-only ALLOWs; Class F duplicate/indented `by` DENIES; inert heredoc/quoted/comment + no-admit-shape `$()`/backtick/eval ALLOW; the round-2 launchers + GAP-B/GAP-D still DENY; case count >= guard.test.ts.
- `npx vitest run scripts/floor-invariance.test.ts` green: the path-form-modifier × command-substitution × multi-admit-ordering fuzz; the round-2 non-string-dial + case-variant `by` sweeps; the four named floor invariants hold at every value.
- A grep asserts `function tokenize` and `noteFileFromCommand` no longer exist in hooks/admission-guard.ts (single resolution authority).
- `npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial; D-13 no kit-content false-positive).
- `npx vitest run --exclude '**/scripts/e2e/**'` green (full non-e2e regression — bare `npm test` deliberately NOT run; it triggers the live claude-CLI e2e lane, see MEMORY grugops-npm-test-triggers-live-e2e).
- `25-06-RED-baseline.txt` + `25-06-GREEN-proof.txt` capture the both-direction proof vs the committed `.js` (extending the 25-05 row format).
- INDEPENDENT both-angle red-team (Task 25-06-04, before /gsd-verify-work): confirms SC1 round-3 closure adversarially — not self-reported green (D-12).
</verification>

<success_criteria>
- SC1 (Classes A + B + E + F): the GOV-01 human gate is no longer forgeable across the FULL command-resolution surface — a path-form command modifier resolves (A), a `$()`/backtick command word fails closed (B), every live admit segment is classified so a multi-admit shield DENIES (E), and a duplicate/indented `by` is gate-or-stricter consistent with validate() (F); the matcher and classifier are ONE liveTokens authority with the naive second walk deleted; the hook stays the only grant path.
- PRESERVED (no regression): the round-2 LOGIC closures GAP-B/C/D, the verified SC2 config foundation + the GOV-02 audit ledger, and the 25-04/25-05 segmentation are unchanged; `scripts/context-io.ts` is not edited.
- Fail-safe (no over-block regression): inert heredoc/quoted/comment mentions, a no-admit-shape `$()`/backtick/eval command, and a routine-only multi-admit under `high-severity` all ALLOW; the fail-closed gates fire only on the admit shape in a live segment.
- Proof (D-12): both-direction RED→GREEN vs the COMMITTED `.js` (captured artifacts) + an anti-whack-a-mole command-resolution fuzz + an INDEPENDENT both-angle (logic + input-surface) opus-grade red-team that invents an un-enumerated obfuscation shape per angle — not a green author suite. `hooks/guard.ts` + `scripts/context-io.ts` byte-unchanged; freshness 0; the second walk deleted.
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-06-SUMMARY.md` when done. Record: the ONE liveTokens-based per-segment resolution authority's structure (how it yields {isLiveAdmit, noteFile, by/severity} per segment, the resolution applied to every leading-run token for Class A, the `$()`/backtick fail-closed branch for Class B, the per-segment classify-and-gate-if-any for Class E, the validate()-consistent note read for Class F) and confirmation the naive tokenize + noteFileFromCommand second walk is DELETED and the 25-04/25-05 segmentation tokenizer is intact; confirmation `scripts/context-io.ts` is unedited (GAP-C/GAP-D + ledger preserved) or — if a Class F additive helper was unavoidable — the byte-identical-GAP-C/GAP-D justification; the fuzz matrix dimensions; the RED→GREEN proof artifact paths; confirm `git diff --quiet hooks/guard.ts` and `git diff --quiet scripts/context-io.ts` clean and freshness 0; and the INDEPENDENT both-angle red-team's reproduced commands + the invented obfuscation shapes (both angles) + outcomes + the preserved GAP-B/C/D results. Flag for the verifier that SC1 closure REQUIRES the independent red-team — the sweeps/fuzz are necessary, not sufficient (D-12, this phase's 8th green-suite-insufficient catch). Note that SC2 + the GOV-02 audit ledger (25-01/02/03) and the round-2 LOGIC closures (25-05) are PRESERVED untouched.
</output>

---
phase: 25-governance-on-a-dial
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/admission-server.ts
  - scripts/admission-server.js
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/admission-server.test.ts
  - .claude-plugin/plugin.json
autonomous: true
requirements: [GOV-01, GOV-02]
user_setup:
  - service: claude-code-mcp-approval
    why: "A plugin-shipped MCP server goes through a one-time per-server user approval in Claude Code (RESEARCH Q2). The human approves the grugops admission server once when prompted; the agent cannot self-approve it."
    dashboard_config:
      - task: "Approve the grugops MCP server when Claude Code prompts on first use"
        location: "Claude Code session — the per-server MCP approval prompt"

must_haves:
  truths:
    - "A role agent on Claude Code admits a verified note by calling ONE structured tool (mcp__grugops__propose_note) with the note's fields as structured JSON arguments — there is no agent-authored shell command string for the admission (D-01 point-of-effect move)."
    - "Persistence of an admitted note routes SOLELY through context-io.appendNote (the single sanctioned writer); the server forks no second writer (preserves the load-bearing single-writer invariant Phase 22 depends on)."
    - "The Posture-B §14-gate verdict cross-check (VFY-01) and the GOV-02 audit ledger under audit_retention: retained still run on the structured-channel admit, reused in-module — not re-implemented in the server (SC2/GOV-02 untouched). admit()'s D-04 unconditional high-severity refusal + its frozen .trim().toLowerCase() classifier stay the WEAKER intentionally-FROZEN degrade tier (the 4 non-CC CLIs + the CLI admit verb), unchanged (D-04/D-05)."
    - "An APPROVED high-severity admission SUCCEEDS PER NOTE (D-07): the combiner persists a gated note carrying a valid human:NAME stamp (on CC the per-call 25-10 hook validated tool_input.verified_by === human:${fresh-env-name}, so the disposition is un-forgeable per entry) and ledgers disposed_by: human:NAME attributed to that specific entry; a gated note lacking a valid human:NAME stamp is refused naming the fault and persists nothing (backstopping gated-routine-under-all, W5)."
    - "The severity classifier is PROVABLY SINGLE-SOURCE: ONE exported isHighSeverityRole(by) (NFKC + strip whitespace/zero-width + lowercase + allowlist) is the sole grammar; the 25-10 hook IMPORTS it and defines no own classifier (round-8 anti-drift)."
    - "A human:NAME stamp is persisted ONLY on a gated disposition; on a NON-gated note the combiner rejects an agent-supplied human:NAME (verified_by must be empty or a §14-gate stamp) so no forged disposed_by can enter the ledger (W3)."
    - "A refused note is NEVER persisted and the named fault is returned to the agent; no note bytes are written except via appendNote/atomicWrite inside context-io (no-fabrication floor)."
  artifacts:
    - "scripts/admission-server.ts (+ committed scripts/admission-server.js): the zero-dependency Node 22 stdio MCP server exposing mcp__grugops__propose_note; forwards the structured args to the combiner (it does NOT read its own env for approval — the per-call 25-10 hook is the gate)"
    - "scripts/context-io.ts TWO new exports (+ rebuilt scripts/context-io.js): admitAndAppend(task, note, body, contextRoot?, repoRoot?) — the additive admit-decides-then-persist combiner — and isHighSeverityRole(by) — the single-source severity classifier the 25-10 hook imports. admit() byte-frozen."
    - "scripts/admission-server.test.ts: the structured-channel write-path proof incl. the gated/approved positive path + the W3 reject-human-stamp-on-non-gated case"
    - ".claude-plugin/plugin.json mcpServers entry wiring the server"
  key_links:
    - "the 25-10 per-call hook (un-forgeable gate) validates a gated note's tool_input.verified_by === human:${fresh-env-name} -> admission-server.ts forwards args -> context-io.admitAndAppend (persist arbiter) -> appendNote() (sole persistence)"
    - "gated note -> in-module validate/composeNote/appendNote/appendAuditLedger (verified_by persisted as the supplied human:NAME, ledger disposed_by:human:NAME); non-gated note -> admit() (Posture-B + D-04 + ledger) after rejecting any human:NAME stamp (W3)"
    - "the ONE isHighSeverityRole export is imported by the 25-10 hook (no second classifier grammar); admit()'s frozen classifier coverage ⊆ the shared classifier's (never under-gates relative to the un-forgeable tier)"
    - "plugin.json mcpServers -> node ${CLAUDE_PLUGIN_ROOT}/scripts/admission-server.js -> resolves context-io.js relative to itself"
  prohibitions:
    - "guard.ts byte-frozen: hooks/guard.ts MUST NOT change (git diff --quiet hooks/guard.ts exit 0)."
    - "single-sanctioned-writer preserved: the server MUST NOT write note bytes by any path other than context-io.appendNote; no echo/cp/tee/redirection/atomicWrite fork in the server."
    - "no shell-string parsing in the un-forgeable tier: the structured channel carries structured JSON args; the server MUST NOT reconstruct or parse a shell command string to decide admission."
    - "single-source classifier (round-8 anti-drift): isHighSeverityRole exists in EXACTLY ONE place (context-io); the hook imports it; NO duplicated/second classifier grammar anywhere."
    - "the combiner binds verified_by on ALL paths: human:NAME persisted ONLY on a gated disposition; a non-gated note's agent-supplied human:NAME is rejected (W3)."
    - "GOV-02 ledger logic untouched: the appendAuditLedger code path + its fixed-key JSONL shape MUST NOT change; the combiner reuses the existing in-module ledger append (admit()'s for non-gated; the same private appendAuditLedger for the gated branch)."
    - "context-io existing exports behavior-identical: validate/admit/appendNote/emitVerdict/readGovernanceConfig*/parseNote MUST behave exactly as before; admit() is byte-frozen; admitAndAppend + isHighSeverityRole are the ONLY two new exports (additive)."
    - "the server is NOT the gate: it MUST NOT read its own process env to decide approval (the per-call hook is the un-forgeable gate); the server only forwards the structured args."
    - "NO new runtime dependency: the server runs on committed .js with bare Node 22 and zero installed host deps (CLAUDE.md)."
---

<objective>
Build the structured admission CHANNEL that moves the GOV-01 gate to the point of effect (D-01,
round-6 human decision 2026-06-26). Ten rounds proved a PreToolUse Bash hook that parses the
pre-expansion command string can never be complete against bash. Round 6 replaces the proxy: a role
agent on Claude Code admits a verified note by calling a STRUCTURED tool whose arguments the harness
delivers as final JSON (no shell expansion), and the sanctioned persistence stays the single writer.

Purpose: eliminate the entire shell-obfuscation bypass family BY CONSTRUCTION (there is no command
string to obfuscate), while preserving the single-sanctioned-writer invariant, Posture-B (VFY-01),
the D-04/D-05 degrade tier, SC2, and the GOV-02 ledger.
Output: a zero-dependency committed-.js stdio MCP server (mcp__grugops__propose_note) that wraps a
new additive context-io combiner (admitAndAppend) which reuses admit() then persists via appendNote;
plus the plugin wiring and the structured-channel write-path proof. The gate itself is built in 25-10.

## MECHANISM DECISION — confirm at plan review (PROMINENT, human-confirmable)

RESEARCH ranked two mechanisms. This plan implements **(a) a structured admission TOOL (MCP server)**
and recommends it over (b). The human should confirm (a) at plan review before execution.

- **(a) CHOSEN — a structured admission tool (zero-dep stdio MCP server) wrapping appendNote.**
  Why: it is the ONLY mechanism that simultaneously (1) gives the agent a STRUCTURED admission channel
  the hook reads with no shell expansion, AND (2) keeps context-io.appendNote the SINGLE sanctioned
  writer (the server calls it; it forks no writer). Posture-B + the GOV-02 ledger are reused (the new
  combiner reuses admit() for non-gated paths and the in-module validate/composeNote/appendNote/
  appendAuditLedger for the gated path — see APPROVED-PATH DESIGN below). The un-forgeable CC gate is the
  PER-CALL PreToolUse hook (25-10), not the server. Today there is NO ergonomic agent-facing write verb
  (the CLI exposes only
  validate/admit/render; admit() does not persist; appendNote is programmatic) — so (a) also completes
  a genuinely missing piece of the architecture, it does not merely relocate the gate.
- **(b) REJECTED — gate the Write/Edit tool on the notes path.** Honest reasoning: notes are NOT
  written via the Write tool today (the sole sanctioned writer is appendNote, invoked programmatically
  via `node`, not the Write tool — context-io.ts:693, CLI :1268). So a Write|Edit matcher fronts an
  UNUSED path: the real sanctioned write (appendNote via Bash `node`) would be left UNGATED once the
  Bash matcher is removed — a regression. Making (b) real requires rerouting note-writing onto the
  Write tool, which BREAKS the single-sanctioned-writer invariant (appendNote's validate/compose/
  atomicWrite/noteId/Phase-22 id-discipline would no longer run; the hook would have to re-implement
  them). RESEARCH ranked (b) #1 before accounting for this; the in-repo facts demote it.
- **Footprint / brand trade-off (state honestly):** (a) ships a new persistent stdio subprocess, in
  mild tension with CLAUDE.md "grugops is NOT a platform, runtime, database, queue, or hosted service."
  It is a zero-dependency committed .js run by `node` with nothing installed (honors "host runs
  committed .js with ZERO runtime deps") and exists solely to BE the admission gate's structured
  channel — within "grugops supplies … gates." It also adds a one-time per-server CC approval. This is
  a real footprint addition and is the reason the mechanism is surfaced for explicit human confirmation.
- **What (a) does NOT close (carried into SC1 restatement, plan 25-11):** the same-uid direct-FS-write
  residual — an agent can still drop a `.md` into notes/ via the Write tool / Bash echo/cp/tee /
  a modified context-io copy / a programmatic `node -e` appendNote or emitVerdict, bypassing the channel.
  This is IRREDUCIBLE in-box, PRE-EXISTING (the Bash hook never closed it either), and backstopped by
  autonomy=pr (humans hold merge) + the no-fabrication floor (D-05 lineage). (a) converts an
  actively-exploited shell-obfuscation surface into this bounded, documented limit — a strict
  improvement with no regression.

## APPROVED-PATH DESIGN — per-note disposition; the un-forgeable gate is the PER-CALL HOOK (D-03/D-07)

The gate is not only "deny an un-approved high-severity admission"; it MUST also ADMIT a human-APPROVED
one PER NOTE (D-07: the human disposes a SPECIFIC entry; the ledger's `disposed_by` must attribute that
entry to the human who reviewed it). Two design facts drive this:

1. `admit()`'s D-04 branch (context-io.ts:947) refuses EVERY high-severity finding under any active dial
   unconditionally — even one carrying a `human:NAME` stamp (the 25-04 forged-stamp backstop) — because
   the in-script tier cannot verify a human placed it. So routing the CC persist naively through `admit()`
   would have the gate ALLOW while the combiner REFUSES — the approved note never admits.
2. CONFIRMED Claude Code capability: a PreToolUse hook runs FRESH PER CALL and reads the CURRENT session
   env; an MCP server's `process.env` is FROZEN at launch and CANNOT see a mid-session export. So a
   server-env approval would be PER-SESSION, silently narrowing D-07's per-ENTRY disposition to a blanket
   and mis-attributing `disposed_by` to a human who never reviewed that specific entry (an audit-integrity
   failure — the core value of this governance feature). The gate therefore must be the per-call hook,
   not the server.

The design:

- **The un-forgeable CC gate = the PER-CALL PreToolUse hook on `mcp__grugops__.*` (25-10), mirroring
  guard.ts (D-01/D-02) — NOT the server env.** Per call the hook reads the FRESH session env
  `GRUGOPS_ADMISSION_APPROVED_BY`, classifies the proposed note via the shared `isHighSeverityRole`,
  lets the dial decide what is gated, and for a GATED note DENIES unless (env present) AND
  (`tool_input.verified_by` === `human:${env-name}`). This binds the persisted stamp to the env-verified
  approver PER CALL, un-forgeably, with only confirmed capabilities (the agent supplies the stamp; the
  hook validates it against the fresh env — no undocumented `updatedInput` needed).
- **The server is NOT the gate — it forwards the structured args to the combiner and persists what the
  hook allowed.** The server does NOT read its own env for approval (removed). It calls
  `admitAndAppend(task, note, body, contextRoot?, repoRoot?)` (admit() byte-frozen).
- **The combiner (`admitAndAppend`) is the persist arbiter, on ALL CLIs:** determine GATED = (a
  high-severity finding under an active dial) OR (ANY note under `all`), using the shared
  `isHighSeverityRole` + `readGovernanceConfigResult` (treat unreadable as active → fail closed, SC3).
  - GATED note: persist ONLY if a valid `human:NAME` stamp is present in `verified_by` (on CC the per-call
    hook validated it un-forgeably; on non-CC / direct-node this is the weaker self-settable D-05 tier =
    documented residual), then ledger `disposed_by: human:NAME`; else REFUSE naming the fault. This also
    BACKSTOPS a gated-routine-under-`all` note lacking a disposition (W5). The gated branch does NOT call
    `admit()` (its D-04 would refuse regardless); it reuses the IN-MODULE composeNote / validate /
    appendNote / appendAuditLedger.
  - NON-GATED note (routine under high-severity, or dial off): a `human:NAME` stamp is ILLEGITIMATE here
    (it would forge a `disposed_by` ledger entry) — the combiner REJECTS an agent-supplied `human:NAME`
    on a non-gated note (`verified_by` must be empty or a `§14-gate#<id>` stamp); W3. Then call `admit()`
    unchanged (validate + Posture-B §14-gate cross-check + D-04-won't-wrongly-fire + ledger) and appendNote.
- **The severity classifier is PROVABLY SINGLE-SOURCE (the round-8 anti-drift lesson):** export ONE new
  symbol `isHighSeverityRole(by)` from context-io (NFKC-normalize + strip ALL whitespace/zero-width +
  lowercase + allowlist membership against the 3 role literals). The 25-10 hook IMPORTS it (defines NO
  own classifier). `admit()`'s frozen `.trim().toLowerCase()` D-04 classifier is a THIRD, weaker,
  intentionally-FROZEN DEGRADE classifier — its coverage ⊆ the shared classifier's, so it never under-
  gates relative to the un-forgeable tier and is not exploitable; it MUST NOT be touched.
- **Defense-in-depth + non-CC honesty:** hook (un-forgeable, per-call, CC) + combiner stamp-check (weaker,
  all CLIs, D-04/D-05). Claim the un-forgeable tier CONSERVATIVELY for Claude Code; do not over-claim
  non-CC. UX (mirrors the deploy guard): the human exports `GRUGOPS_ADMISSION_APPROVED_BY=<name>` for the
  SPECIFIC disposition, the per-call hook validates it against the agent-supplied `human:<name>` stamp,
  optionally unset after — per-note, no server restart needed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-RESEARCH.md
@scripts/context-io.ts
@hooks/guard.ts
@.claude-plugin/plugin.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 25-09-01: Add the admitAndAppend combiner + the single-source isHighSeverityRole classifier to context-io.ts (TWO additive exports; admit() byte-frozen)</name>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts</files>
  <read_first>
    - scripts/context-io.ts:889-972 (admit() — validate, Posture-B cross-check, D-04 refusal + its frozen .trim().toLowerCase() classifier, GOV-02 ledger; returns findings, does NOT persist; BYTE-FROZEN)
    - scripts/context-io.ts:660-724 (composeNote private helper + appendNote — the sole sanctioned writer: validate -> compose -> atomicWrite to notes/<id>.md)
    - scripts/context-io.ts:857 (HIGH_SEVERITY_ROLES const) + :974-1004 (appendAuditLedger — GOV-02; disposed_by derives from a HUMAN_STAMP_RE verified_by; reused unchanged)
    - scripts/context-io.ts:117-118 (HUMAN_STAMP_RE / GATE_STAMP_RE) + :1118-1258 (readGovernanceConfig / readGovernanceConfigResult — SC3 dial; reused unchanged)
  </read_first>
  <behavior>
    - isHighSeverityRole(by: string): boolean — the SINGLE-SOURCE severity classifier (round-8 anti-drift): NFKC-normalize + strip ALL whitespace and zero-width (U+00A0, U+200B, ...) + lowercase, then test allowlist membership against the 3 role literals (security-nfr / architect-design / release-manager). The 25-10 hook IMPORTS this exact function; it is defined in ONE place only.
    - admitAndAppend(task, note, body, contextRoot?, repoRoot?) decides admission then persists ONLY on success via appendNote (the sole writer), returning the new note id; on refusal it returns the findings and persists NOTHING. It reads NO approval env (the per-call 25-10 hook is the un-forgeable gate); it arbitrates persistence from the note's own verified_by stamp + the dial.
    - GATED = (a finding whose by satisfies isHighSeverityRole AND human_admission is active) OR (ANY note under human_admission: all). "active"/dial via readGovernanceConfigResult: source unreadable OR canonicalized human_admission != off => active (fail closed, SC3); absent/off => off.
    - GATED branch: persist ONLY if a valid human:NAME stamp is present (HUMAN_STAMP_RE.test(note.verified_by)). On CC the per-call hook validated that stamp against the fresh env (un-forgeable per entry); on non-CC/direct-node it is the weaker self-settable D-05 tier. Reuse the in-module composeNote + validate + appendNote, then append the GOV-02 ledger via appendAuditLedger (disposed_by = the human:NAME) under retained. This branch does NOT call admit() (admit's D-04 would refuse a high-sev finding regardless of stamp). A gated note lacking a valid human:NAME stamp is REFUSED naming the fault (backstops gated-routine-under-all, W5).
    - NON-GATED branch (routine under high-severity, or dial off): a human:NAME stamp is illegitimate here — REJECT an agent-supplied verified_by: human:NAME (W3: verified_by must be empty or a §14-gate#<id> stamp), naming the fault. Otherwise findings = admit(task, text, contextRoot, repoRoot) (validate + Posture-B §14-gate cross-check + D-04-won't-wrongly-fire + GOV-02 ledger); if findings, return them; else appendNote. Posture-B preserved (VFY-01).
    - Every EXISTING export behaves byte-identically; admit() is BYTE-FROZEN; admitAndAppend + isHighSeverityRole are the ONLY two new exports.
  </behavior>
  <action>
    Add TWO additive exported functions to context-io.ts (the ONLY new exports; admit() stays BYTE-FROZEN):
    (1) isHighSeverityRole(by) — the single-source severity classifier (NFKC + strip all whitespace/
    zero-width + lowercase + allowlist of the 3 role literals; a true cross-script homoglyph by is the
    accepted D-06 residual, escape human_admission: all); (2) admitAndAppend — the persist arbiter (D-01:
    decide, then the sole writer persists). admitAndAppend reads NO approval env — the per-call 25-10 hook
    is the un-forgeable gate; the combiner arbitrates from the note's own verified_by + the dial. Implement
    the GATED / NON-GATED branches in <behavior>. For the GATED branch BYPASS admit()'s D-04 (it refuses
    high-sev unconditionally — correct for the DEGRADE tier) and reuse the in-module composeNote + validate
    + appendNote + appendAuditLedger + HIGH_SEVERITY_ROLES (no re-implementation, no drift), persisting the
    note's supplied human:NAME stamp and ledgering disposed_by = that name. For the NON-GATED branch REJECT
    any agent-supplied human:NAME (W3) then call admit() unchanged + appendNote. Use readGovernanceConfigResult
    and treat unreadable as active (fail closed, SC3). Keep the GOV-02 ledger writer + the SC3 readers
    UNCHANGED — only call them. Clear professional voice (safety surface). Rebuild scripts/context-io.js
    (committed). Do NOT touch hooks/guard.ts.
  </action>
  <verify>
    <automated>npx vitest run scripts/context-io.test.ts --exclude '**/scripts/e2e/**' && node scripts/freshness.js && git diff --quiet hooks/guard.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - isHighSeverityRole is exported from context-io and is the ONLY severity classifier grammar (it folds U+00A0 / U+200B / NFKC / case variants of a role literal to high-severity); admitAndAppend + isHighSeverityRole are the only two new exports.
    - admitAndAppend persists a clean routine note (a new notes/<id>.md exists, id returned).
    - GATED + valid human:NAME stamp: a high-severity finding (active dial) carrying verified_by: human:alice persists exactly ONE note stamped human:alice and ledgers disposed_by: human:alice; a gated note lacking a valid human:NAME stamp is REFUSED and persists nothing (incl. a routine note under all — W5).
    - NON-GATED + agent human:NAME (W3): a routine note (by: software-engineer) carrying verified_by: human:eve under high-severity is REFUSED (a non-gated note's verified_by must be empty or §14-gate, never human:NAME) — no forged disposed_by enters the ledger.
    - Posture-B preserved: a §14-gate#<id> finding with no live green verdict is refused; unreadable config fails closed.
    - The existing context-io + floor-invariance + admission + Posture-B tests stay green (admit() + other exports unregressed); node scripts/freshness.js exits 0; git diff --quiet hooks/guard.ts exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 25-09-02: Build the zero-dependency stdio MCP server scripts/admission-server.ts (mcp__grugops__propose_note)</name>
  <files>scripts/admission-server.ts, scripts/admission-server.js</files>
  <read_first>
    - .planning/phases/25-governance-on-a-dial/25-RESEARCH.md:512-535 (Q2 — zero-dep stdio MCP server via plugin.json mcpServers; mcp__server__tool naming; structured tool_input)
    - scripts/context-io.ts (the admitAndAppend export from Task 01; the NoteInput shape; parseNote)
    - hooks/guard.ts (the clear-voice safety-surface doc + fail-closed posture to mirror in tone)
  </read_first>
  <action>
    Implement a pure-Node (node: stdlib only, ZERO npm deps) JSON-RPC stdio MCP server that exposes ONE
    tool, propose_note (full name mcp__grugops__propose_note), whose structured parameters are the note's
    fields (task, kind, by, at, verified_by, confidence, refs, supersedes, body) plus an optional
    contextRoot/repoRoot for tests. The server implements the minimal MCP stdio handshake (initialize,
    tools/list, tools/call) over stdin/stdout JSON-RPC. On tools/call for propose_note it builds a
    NoteInput + body from the STRUCTURED arguments and calls context-io.admitAndAppend(task, note, body,
    contextRoot, repoRoot) (D-01 point-of-effect; single writer — the server forks NO writer and parses
    NO shell string). The server is NOT the gate and reads NO approval env (the un-forgeable gate is the
    PER-CALL 25-10 hook, which runs fresh per call and validates tool_input.verified_by against the fresh
    session env — a server-env read would be frozen-at-launch / per-session and would mis-attribute the
    GOV-02 disposed_by). On findings it returns an MCP tool error naming the fault (no persistence); on
    success it returns the new note id. Document in the server's header that the un-forgeable disposition
    is PER-NOTE via the per-call hook (the human exports GRUGOPS_ADMISSION_APPROVED_BY=<name> for the
    specific disposition; the hook validates the agent-supplied human:<name> stamp against the fresh env),
    claimed CONSERVATIVELY for Claude Code (non-CC un-forgeability not claimed).
    Resolve context-io.js RELATIVE to the server file (same scripts/ dir) so it works from the plugin
    cache and the shared-install kit root alike. Declare the tool's input schema so the 25-10 hook and
    the agent see identical structured params. Clear professional voice throughout (governance/safety
    surface — never caveman). Commit scripts/admission-server.js; freshness auto-covers scripts/*.js
    (no freshness.ts edit). Do NOT touch hooks/guard.ts.
  </action>
  <verify>
    <automated>node scripts/freshness.js && node -e "process.exit(require('fs').existsSync('scripts/admission-server.js')?0:1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - scripts/admission-server.ts imports admitAndAppend from ./context-io.js and exposes exactly one tool, propose_note, with a declared structured input schema (note fields).
    - The server is NOT the gate: it reads NO approval env and calls admitAndAppend(task, note, body, contextRoot?, repoRoot?) with no approval param (grep: no GRUGOPS_ADMISSION_APPROVED_BY read in admission-server.ts; the gate is the 25-10 per-call hook).
    - The server's only persistence path is context-io.admitAndAppend (grep: no atomicWrite/writeFileSync/appendFileSync/echo/cp/tee in admission-server.ts).
    - context-io.js is resolved relative to the server file (no hard-coded absolute path); the header documents the per-note disposition via the per-call hook + the conservative CC-only un-forgeable claim.
    - scripts/admission-server.js is committed and node scripts/freshness.js exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 25-09-03: Wire plugin.json mcpServers + prove the structured-channel write path (admission-server.test.ts)</name>
  <files>.claude-plugin/plugin.json, scripts/admission-server.test.ts</files>
  <read_first>
    - .planning/phases/25-governance-on-a-dial/25-RESEARCH.md:523-529 (plugin.json mcpServers stdio config: command node, args ${CLAUDE_PLUGIN_ROOT}/...; per-server approval; plugin-shipped MCP servers are allowed)
    - .claude-plugin/plugin.json (current manifest — add the mcpServers field; do not disturb name/version/keywords)
    - scripts/floor-invariance.test.ts:1-40 (the spawn-the-COMMITTED-.js discipline + config-load shape to model the integration test on)
  </read_first>
  <action>
    Add an mcpServers entry to .claude-plugin/plugin.json that launches the committed server with bare
    node and a plugin-root-relative args path (node, ${CLAUDE_PLUGIN_ROOT}/scripts/admission-server.js),
    transport stdio. Then author scripts/admission-server.test.ts driving the COMMITTED server (or the
    committed admitAndAppend through the server's call handler) with structured JSON args in a temp
    contextRoot/repoRoot — this suite proves the COMBINER's persist-arbiter behavior (the un-forgeable
    per-call HOOK gate is proven in 25-10): (1) a clean routine propose_note persists exactly one note via
    the single writer (the file exists with the composed fence); (2) a GATED note (high-severity finding
    under an active dial) carrying a valid verified_by: human:alice stamp persists exactly ONE note stamped
    human:alice and (under retained) ledgers disposed_by: human:alice (the approved disposition — on CC the
    25-10 hook validated that stamp un-forgeably per call); (3) a GATED note with NO valid human:NAME stamp
    persists NOTHING and names the fault (incl. a routine note under all — W5 backstop); (4) W3 — a
    NON-GATED note (by: software-engineer under high-severity) carrying verified_by: human:eve persists
    NOTHING and names the fault (a non-gated note's verified_by must be empty or §14-gate, never human:NAME);
    (5) the server writes by no path other than appendNote (assert no stray files / no second writer) and
    reads NO approval env. Gate the suite with the e2e-excluded lane. Clear professional voice in test
    descriptions.
  </action>
  <verify>
    <automated>npx vitest run scripts/admission-server.test.ts --exclude '**/scripts/e2e/**' && node scripts/freshness.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - plugin.json carries a valid mcpServers entry pointing at the committed server via ${CLAUDE_PLUGIN_ROOT}; plugin.json still parses and keeps name/version/author/keywords.
    - admission-server.test.ts proves: clean routine persists one note; a GATED note with a valid human:alice stamp persists one note stamped human:alice + ledgers disposed_by:human:alice (retained); a GATED note with no valid stamp persists nothing + names the fault (incl. routine-under-all, W5); W3 — a non-gated note with verified_by:human:eve persists nothing + names the fault; the server reads no approval env and writes only via appendNote (no second-writer artifacts).
    - The full non-e2e suite stays green; node scripts/freshness.js exits 0; git diff --quiet hooks/guard.ts exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> structured tool call (mcp__grugops__propose_note) | the agent supplies structured JSON args; the harness delivers them final (no shell expansion) |
| human FRESH session env -> per-call 25-10 hook | the un-forgeable PER-NOTE grant; the agent cannot set the hook's session env (the gate is the hook, NOT the server's frozen env) |
| structured args -> admission-server -> admitAndAppend -> appendNote | the only sanctioned persistence path; the per-call hook sits in front of the tool call |
| agent -> direct filesystem (Write tool / Bash echo / modified copy / node -e appendNote, emitVerdict, or admitAndAppend) | the irreducible same-uid residual that bypasses the channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-50 | Tampering | direct same-uid write into notes/ bypassing the MCP channel (Write tool / Bash echo/cp/tee / modified context-io copy / node -e appendNote, emitVerdict, or admitAndAppend) | high | accept | IRREDUCIBLE in-box, PRE-EXISTING (the Bash hook never closed it). Documented + restated in SC1 (plan 25-11); backstopped by autonomy=pr (humans hold merge) + the no-fabrication floor (D-05 lineage). |
| T-25-51 | Spoofing | a server that forks its own writer / re-implements admit() (drift from the single writer) | high | mitigate | the server's ONLY persistence is context-io.admitAndAppend -> appendNote; Posture-B/ledger/D-04-degrade are reused in-module, not re-implemented (asserted: no atomicWrite/writeFileSync in the server; single-writer test). |
| T-25-54 | Elevation | agent self-grants approval (a tool-arg human:NAME stamp on a non-gated note, or a per-session server-env grant mis-attributing disposed_by) | high | mitigate | the un-forgeable gate is the PER-CALL hook (fresh env, per note), NOT the server env; the combiner rejects a human:NAME stamp on a non-gated note (W3); disposed_by attributes the specific entry the human disposed (per-note, no session blanket). |
| T-25-55 | Tampering | a second/duplicated severity classifier grammar drifting from the gate (the 8-round drift class) | high | mitigate | isHighSeverityRole is the single source; the 25-10 hook imports it (defines none); grep-asserted in exactly one place. |
| T-25-52 | Repudiation | GOV-02 ledger drift / double-count / forged disposed_by via the combiner | medium | mitigate | the combiner reuses appendAuditLedger on the success path only; ledger code path unchanged; disposed_by only from a gated note's hook-validated human:NAME (W3 blocks a non-gated forge); retained test asserts exactly one line. |
| T-25-53 | Tampering | context-io existing-export behavior regression from the additive exports | high | mitigate | two additive exports only; admit() byte-frozen; the floor-invariance + admission + Posture-B suites stay green; the 25-11 independent red-team reproduces SC2/GOV-02/SC3/Posture-B/D-04 non-regression. |

(No package-manager installs in this plan — zero-dep server; the package-legitimacy gate does not apply.)
</threat_model>

<verification>
- npx vitest run --exclude '**/scripts/e2e/**' green (context-io + admission-server + floor-invariance + admission tests).
- node scripts/freshness.js exits 0 (context-io.js + admission-server.js fresh).
- git diff --quiet hooks/guard.ts exits 0 (byte-frozen).
- grep confirms admission-server.ts has no atomicWrite/writeFileSync/appendFileSync/echo/cp/tee (single writer).
- plugin.json parses with a valid mcpServers entry.
</verification>

<success_criteria>
The structured admission channel exists and is the sanctioned CC write path: a structured
propose_note call persists a note SOLELY via the single writer, reuses Posture-B + D-04 + the GOV-02
ledger, refuses without persisting, and adds no runtime dependency — while guard.ts stays byte-frozen
and every existing context-io export is behavior-identical. The gate that denies an un-approved
high-severity admission on this channel is built in plan 25-10.
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-09-SUMMARY.md` when done
</output>

---
phase: 25-governance-on-a-dial
plan: 08
type: execute
wave: 8
gap_closure: true
depends_on: ["25-01", "25-02", "25-03", "25-04", "25-05", "25-06", "25-07"]
autonomous: false
files_modified:
  - hooks/admission-guard.ts
  - hooks/admission-guard.js
  - hooks/admission-guard.test.ts
  - scripts/floor-invariance.test.ts
  - 25-08-RED-baseline.txt
  - 25-08-GREEN-proof.txt
requirements: [GOV-01, GOV-02]
user_setup: []

must_haves:
  truths:
    - "Prove-final-literal inversion / SC1 (GOV-01) — the admit-SHAPE detector is made STRUCTURAL by the INVERSE, genuinely-closed grammar property: a script-or-verb-position token is RESOLVABLE only if it is PROVABLY ALREADY ITS FINAL LITERAL FORM — its de-quoted value contains NONE of the bash characters the shell rewrites/expands/word-splits before exec (`* ? [ ] { } ~ < >`), its raw source carries no `$` and no backtick (parameter / command / arithmetic / process / ANSI-C substitution), and it is not the synthetic DYNAMIC_COMMAND_WORD sentinel nor fed via an stdin→argv feeder (xargs). The detector does NOT enumerate what expands (the old `{glob,$ ,$(),word-split,xargs}` 'bounded alphabet' was itself an enumeration — false for bash, which has brace/tilde/process-sub/ANSI-C expansions outside it); it requires PROOF that nothing will expand, else the token is UNRESOLVABLE → fail-closed gate. The defect closed: `segmentHasAdmitShape`/`commandHasLiveAdmitShape` were a LITERAL substring/token test (`v.includes(\"context-io\")` then token `=== \"admit\"`) on the UN-EXPANDED command string (admission-guard.ts:459-467/679-686, committed blob 756ce508). (D-01 un-forgeable tier; vs the COMMITTED `.js`)."
    - "Script-anchored disposition / SC1 — `segmentAdmitDisposition` is a precise argv-position algorithm anchored on the SCRIPT position, not a vague 'verb'. After the round-4 leading-run resolution to the command word: (1) the SCRIPT-position token is PROVABLY-LITERAL and does NOT contain `context-io` → this segment can NEVER be a context-io admit (a later expansion cannot retroactively make argv[script] context-io) → ALLOW — this is exactly why `node build $HOME/out` ALLOWs (script=`build` literal ≠ context-io; the `$HOME` at a later position is irrelevant). (2) the SCRIPT-position token is PROVABLY-LITERAL context-io → inspect the verb: gate if the verb is `admit`-or-UNRESOLVABLE; ALLOW if the verb is a provably-literal non-admit (`validate`/`render`) or absent. (3) the SCRIPT-position token is UNRESOLVABLE → cannot prove it is not context-io → gate (the documented, bounded over-block: a fully-dynamic script path while governance is ON; the gate asks a human, never destroys work; a static command avoids it). Every documented ALLOW/GATE control is consistent with THIS algorithm."
    - "Brace / quote-removal / tilde / process-sub all closed (BLOCKER-1 class) / SC1 — `node scripts/context-io.js admi{t,x} my-task my-note` DENIES: bash brace-expands `admi{t,x}` → `admit`; the `{`/`}` make the verb token non-final-literal → UNRESOLVABLE verb behind a context-io script → gate. The quote-removal forms are classified by DE-QUOTING then proving literal: `node scripts/'context-io'.js admit …` DENIES (de-quotes to literal `context-io` script + `admit` verb), `node <ctx> ad''mit …` / `node <ctx> \"admit\" …` / `node <ctx> adm\\it …` DENY (each de-quotes to the literal `admit` verb), and the fully-static `node \"scripts/context-io.js\" \"admit\"` DENIES (de-quotes to a literal context-io admit — it IS an admit). A tilde `node ~/x/context-io.js admit` and a process-substitution `node <(…) admit` are non-final-literal in script position → gate. A token whose quoting/escaping cannot be statically collapsed to a literal is UNRESOLVABLE → gate."
    - "Structural detection on ANY command word (BLOCKER-2 class) / SC1 — the structural shape test applies to ANY command word — a recognized launcher (`node`/`npx`/`tsx`/`nodejs`), an UNRECOGNIZED launcher (`bun`/`bunx`/`deno run`/`ts-node`), or a shebang script with no launcher word — NEVER the literal substring on the non-launcher path. `bun scripts/context-i*.js admit …`, `deno run scripts/context-io.js admit …`, `ts-node scripts/context-io.js admit …` DENY. Launcher recognition is used ONLY to AVOID over-blocking (a recognized non-admit launcher with a provably-literal non-context-io script ALLOWs), never as the only path to gating: the admit SHAPE = a (context-io-or-unresolvable) token in an executed script position followed by an (admit-or-unresolvable) token in verb position, with at least one CONCRETE literal anchor (the script literally context-io OR the verb literally admit) to bound speculative over-block on unrecognized command words."
    - "Shebang / no-launcher-word handled (WARNING-4 class) / SC1 — `scripts/context-io.js admit my-task my-note` (an executable shebang script as the command word) DENIES in the SAME unified predicate: the command word itself is the script position → provably-literal context-io + an `admit` verb → gate. It is IN SCOPE (not silently omitted); the threat model and 25-08-SUMMARY state this disposition explicitly."
    - "D-01 floor + dial-`all` re-closed at the un-forgeable tier / SC1+GOV-02 — `GRUGOPS_ADMISSION_APPROVED_BY=eve node scripts/context-i*.js admit … <hi>` DENIES even with the var in env (`commandHasLiveAdmitShape` derives from the same structural detector / the one `liveAdmitSegments` authority); under `human_admission: all` a routine admit delivered by ANY expansion (`node scripts/context-i*.js admit … <routine>`, `bun <ctx> admit … <routine>`) DENIES. The strictest dial's `gate every admission` guarantee is restored at the un-forgeable hook tier WITHOUT touching `admit()` — `scripts/context-io.ts` stays byte-frozen (the weak self-settable D-05 tier adds no un-forgeable defense; keeping it unchanged preserves GAP-C/GAP-D + the GOV-02 ledger byte-identically)."
    - "Over-block SAFETY (the bounded fail-safe) PROVEN — the gate fires ONLY when the SCRIPT position is context-io-or-unresolvable (with the concrete-anchor bound on unrecognized command words); a recognized launcher with a provably-literal non-context-io script, and all non-launcher / inert constructs, stay ALLOW: `node build`, `node render`, `node script.js test`, `node build $HOME/out`, `node scripts/context-io.js validate` (literal context-io + literal non-admit verb), `timeout 5 node render`, `sudo ls`, `>/dev/null node build`, `cp $SRC $DST` / `tar $A $B` (unrecognized command, no concrete context-io/admit literal anchor), a routine-only multi-admit under `high-severity`, and an inert heredoc/quoted/`#`-comment admit mention behind a wrapper (a single quoted blob token carries no separate script+verb pair) all ALLOW. The bounded, EXPLICIT over-block (a fully-dynamic `node $SCRIPT build` gated only while `human_admission != off`; the gate asks for a human and never destroys work) and the documented residual (an unrecognized runner with BOTH script AND verb expansion-hidden and no literal anchor, e.g. `bun $S $V` — the recognized launcher `node $S $V` still gates) are disclosed in the threat model + 25-08-SUMMARY, not hidden."
    - "Anti-enumeration proof (D-12) — the property/fuzz oracle generates candidate bypass tokens from bash's ACTUAL rewrite grammar / 'any token containing a shell-special char or requiring de-quoting', INDEPENDENT of the detector's recognized characters — so brace `{a,b}`, tilde `~`, process-substitution `<(…)`, ANSI-C `$'…'`, quote-removal (`ad''mit`, `\"admit\"`, `scripts/'context-io'.js`, `adm\\it`), and mid-word substitution are GENERATED and MUST gate (or be correctly classified) WITHOUT being individually whitelisted. The invariant asserted: `if the hook cannot statically prove the script/verb token is its final literal form AND non-admit, it gates`. A green author suite is necessary-NOT-sufficient (this is the 9th green-suite-insufficient catch); the independent both-angle red-team is the gate."
    - "PRESERVED — every round-1..4 closure the prior red-teams confirmed SOLID is untouched: round-4 leading-run RESOLUTION (sub-roots a modifier-operand / b unlisted-wrapper / c leading-redirection, the dial-`all` wrapper leak, the Class-E wrapper reopening, the `sh -c \"…admit…\"` dynamic-eval tail) all DENY; round-3 Class A path-form launcher DENIES, Class B `$()`/backtick COMMAND-WORD GATES, Class E per-segment multi-admit DENIES while routine-only ALLOWs, Class F duplicate/indented `by` is gate-or-stricter; round-2 GAP-B/C/D hold. The SINGLE `liveAdmitSegments` authority stays the ONE source of truth — `function tokenize` / `noteFileFromCommand` stay deleted (grep=0); `COMMAND_MODIFIERS` and `LAUNCHERS` are NOT widened; `liveTokens` segmentation is byte-identical (the detector reads de-quoted token values + token start offsets the tokenizer ALREADY emits; it does not re-walk or change tokenization)."
    - "PRESERVED — SC2 (audit_retention 3-surface lockstep + lean defaults), the GOV-02 audit ledger (`appendAuditLedger`, context-io.ts:979-...), the 25-04/25-05/25-06 segmentation-as-DATA, and the four named floor invariants (refuse-self FAILs at every dial / no-fabrication-never-rewrite / `quality.test_integrity` no `off` / `hooks/guard.ts` byte-frozen blob 3501810e21308e4b7e219679a6ca30dace9b5d66) hold at every dial value. `git diff --quiet scripts/context-io.ts` exits 0 and `git diff --quiet hooks/guard.ts` exits 0; the committed `.js` is a fresh `tsc` build (`npm run freshness` exits 0)."
  artifacts:
    - path: "hooks/admission-guard.ts"
      provides: "The STRUCTURAL admit-SHAPE detector inside the ONE `liveAdmitSegments` authority, built on the prove-final-literal INVERSION. New `tokenIsFinalLiteral(token, cmd)` (renamed/respec'd from the prior `argvHasUnresolvableExpansion`, INVERTED): a token is final-literal only when its de-quoted value carries none of `* ? [ ] { } ~ < >`, its raw source slice carries no `$` and no backtick, and it is not the DYNAMIC_COMMAND_WORD sentinel — anything else is UNRESOLVABLE. New `segmentAdmitDisposition(tokens, cmdIdx, cmd)` → `admit | unresolvable | none` (+ noteFile): SCRIPT-anchored — a recognized launcher pins the script position precisely (script literal-non-context-io → none/ALLOW; literal-context-io → gate on an admit-or-unresolvable verb; unresolvable script → gate); an UNRECOGNIZED command word (bun/deno/ts-node/shebang/wrapper-operand/redirection) runs the structural concrete-anchor scan (a context-io-or-unresolvable script-candidate token — incl. the command word itself for the shebang case — followed by an admit-or-unresolvable verb, with at least one literal anchor). `STDIN_ARGV_FEEDERS = {xargs}` makes a launcher reached behind a feeder unresolvable. The launcher branch (:610-615), the round-4 catch-all (:661-668), and `commandHasLiveAdmitShape` (:679-686, the D-01 floor) all consume this ONE detector — never the literal substring. No `COMMAND_MODIFIERS` / `LAUNCHERS` widening; no second walk; `liveTokens` byte-identical."
      contains: "admit"
    - path: "hooks/admission-guard.js"
      provides: "committed tsc build the host hook runs (freshness-covered; rebuilt after the .ts edit)"
    - path: "hooks/admission-guard.test.ts"
      provides: "extended child-spawn oracle vs the COMMITTED admission-guard.js: glob script-ref / arg-position command-substitution / parameter-expansion-word-split / xargs stdin→argv / BRACE `admi{t,x}` / quote-removal (`ad''mit`, `\"admit\"`, `scripts/'context-io'.js`, `adm\\it`) / tilde / process-sub all DENY a gated high-severity un-approved admit; the NON-NODE launchers (`bun`/`bunx`/`deno run`/`ts-node` + glob/param) DENY; the SHEBANG `scripts/context-io.js admit …` DENIES; the nested wrapper+expansion compounds DENY; the D-01 glob self-set DENIES; the dial-`all` routine-via-expansion DENIES; the over-block controls (`node build`, `node build $HOME/out`, `node scripts/context-io.js validate`, `cp $SRC $DST`, non-admit wrapper, routine-only multi-admit, inert quoted/heredoc/comment mention behind a wrapper) ALLOW; the round-1..4 closures still hold; case count >= guard.test.ts"
      contains: "deny"
    - path: "scripts/floor-invariance.test.ts"
      provides: "the round-5 anti-whack-a-mole prove-final-literal property/fuzz whose oracle is `tokenIsFinalLiteral`, INDEPENDENT of the detector's recognized characters: a generator emits candidate script/verb tokens carrying ANY shell-special character / requiring de-quoting (brace, tilde, process-sub `<(…)`, ANSI-C `$'…'`, quote-removal forms, mid-word substitution, randomized non-alnum chars) × launcher {node|npx|tsx|nodejs|bun|deno|ts-node|shebang} × script/verb POSITION × dial × admit-ordering, asserting `if the hook cannot statically prove the script/verb token is its final literal form AND non-admit, it gates`; a recognized-launcher provably-literal non-context-io script / inert mention / no-concrete-anchor unrecognized command ALLOWs. The round-2/3/4 sweeps + the four named floor invariants are PRESERVED, not duplicated"
      contains: "human_admission"
    - path: "25-08-RED-baseline.txt"
      provides: "captured RED baseline — the PRE-FIX committed `admission-guard.js` (blob 756ce50841e4a5c4955d2197ce08f43e47931659, HEAD f5beb06) ALLOWs each round-5 form: glob script-ref, arg-position command-substitution, parameter-expansion/word-split, xargs stdin→argv, brace `admi{t,x}`, quote-removal forms, the non-node launchers (bun/deno/ts-node), the shebang, the nested wrapper+expansion compounds, the D-01 glob self-set, and the dial-`all` routine-via-expansion — while the byte-identical literal-path control DENIES; extending the 25-07 row format and citing the pre-fix blob"
    - path: "25-08-GREEN-proof.txt"
      provides: "captured GREEN proof — the POST-FIX committed `admission-guard.js` DENIES each round-5 form + compound + the D-01 self-set + the dial-`all` leak naming the unresolvable / fail-closed condition; the over-block controls (recognized non-admit launcher incl. `node build $HOME/out` + `node …validate`, unrecognized command with no literal anchor `cp $SRC $DST`, non-admit wrapper, routine-only multi-admit, inert mention behind a wrapper, zero-config absent lean `off`) still ALLOW; the round-1..4 closures still DENY/GATE/ALLOW — extending the 25-07 row format, referencing the committed `.js`"
  key_links:
    - from: "hooks/admission-guard.ts (segmentAdmitDisposition, the SCRIPT-anchored detector)"
      to: "a real Bash `tool_input.command` admit delivered via ANY shell rewrite (glob / param / cmd-sub / brace / quote-removal / tilde / process-sub / non-node runner / shebang / xargs)"
      via: "the SCRIPT-position token is classified by `tokenIsFinalLiteral` — provably-literal-non-context-io → ALLOW; provably-literal-context-io → gate on an admit-or-unresolvable verb; UNRESOLVABLE → gate (cannot prove it is not context-io) — replacing the literal substring/token test that the shell rewrites walked through"
      pattern: "segmentAdmitDisposition|tokenIsFinalLiteral"
    - from: "hooks/admission-guard.ts (the structural detector applied to ANY command word)"
      to: "the over-block fail-safe — a recognized non-admit launcher stays ALLOW"
      via: "launcher recognition is used ONLY to AVOID over-blocking: a recognized launcher with a provably-literal non-context-io script returns `none` (ALLOW); an unrecognized command word requires a concrete context-io/admit literal anchor before gating, so `cp $SRC $DST` ALLOWs"
      pattern: "tokenIsFinalLiteral"
    - from: "hooks/admission-guard.ts (commandHasLiveAdmitShape, the D-01 floor input)"
      to: "the glob/param self-set breach (GRUGOPS_ADMISSION_APPROVED_BY=eve node …context-i*.js admit)"
      via: "commandHasLiveAdmitShape derives from the one liveAdmitSegments authority / the same structural detector, so a self-set co-occurring with a structurally-detected admit-or-unresolvable segment is refused even behind a glob/param spelling"
      pattern: "commandHasLiveAdmitShape"
    - from: "hooks/admission-guard.ts (the deleted second walk, still gone)"
      to: "noteFileFromCommand + the standalone tokenize"
      via: "neither is reintroduced; the structural detector lives inside the ONE liveAdmitSegments walk and reuses liveTokens + the existing DYNAMIC_COMMAND_WORD token + token start offsets; a grep proves neither helper exists"
      pattern: "noteFileFromCommand|function tokenize"
    - from: "scripts/floor-invariance.test.ts (the round-5 prove-final-literal fuzz)"
      to: "the COMMITTED hooks/admission-guard.js"
      via: "child-spawn the committed .js across a generator that emits shell-special / de-quote-requiring tokens INDEPENDENT of the detector's character set; assert the class invariant (a script/verb token not provably final-literal-and-non-admit → gate) for every variant incl. brace/tilde/process-sub/ANSI-C/quote-removal/mid-word"
      pattern: "admission-guard\\.js|spawn"
  prohibitions:
    - statement: "A GREEN author suite is NECESSARY-BUT-NOT-SUFFICIENT for this safety guard — the author's suites passed and STILL missed a class across rounds 1, 2, 3, the round-3 fix, and the round-4 fix (this is the 9th green-suite-insufficient catch of v2.0; the 25-07 GREEN-proof rows were truthful but its threat enumeration was INCOMPLETE). Closure REQUIRES a both-direction RED-vs-committed-`.js` proof reproduced against the COMMITTED `hooks/admission-guard.js` (never the `.ts`, never the author `*.test.ts`): for EACH round-5 form (glob / arg-position cmd-sub / param-expansion-word-split / xargs / brace / quote-removal / non-node launcher / shebang) + the nested wrapper+expansion compounds + the D-01 glob self-set + the dial-`all` routine-via-expansion, a planted high-severity-without-real-approval command flips PRE-FIX ALLOW -> POST-FIX DENY naming the unresolvable / fail-closed condition; AND the over-block controls (recognized non-admit launcher incl. `node build $HOME/out` + `node …validate`, unrecognized command with no literal anchor `cp $SRC $DST`, non-admit wrapper, routine-only multi-admit, inert mention behind a wrapper, zero-config absent lean `off`) still ALLOW. Captured in 25-08-RED-baseline.txt and 25-08-GREEN-proof.txt, extending the 25-07 row format. (D-12, [[grugops-safety-invariant-green-suite-insufficient]])."
      status: must-hold
      verification: "25-08-RED-baseline.txt shows the pre-fix committed `.js` (blob 756ce508…, HEAD f5beb06) ALLOWing each round-5 form + the compounds + the D-01 self-set + the dial-`all` leak; 25-08-GREEN-proof.txt shows the post-fix committed `.js` DENYing each and ALLOWing the over-block controls; reproduced independently of the author suite at the blocking checkpoint."
    - statement: "DETECTION IS THE PROVE-FINAL-LITERAL INVERSION, not an enumeration of what expands. Do NOT enumerate expansion triggers (the `{glob,$ ,$(),word-split,xargs}` 'bounded alphabet' was itself a disguised enumeration — FALSE for bash, which has brace/tilde/process-sub/ANSI-C outside it). A script-or-verb-position token is RESOLVABLE only when PROVEN to be its final literal form (`tokenIsFinalLiteral`: de-quoted value free of `* ? [ ] { } ~ < >`, raw source free of `$` and backtick, not the DYNAMIC_COMMAND_WORD sentinel, not stdin-fed); anything else is UNRESOLVABLE → fail CLOSED. A brace / tilde / process-sub / ANSI-C / quote-removal / future expansion form MUST gate with no code change because it is caught by the ABSENCE of proof, not by a recognized list. `COMMAND_MODIFIERS` and `LAUNCHERS` are NOT widened."
      status: must-hold
      verification: "a grep confirms `COMMAND_MODIFIERS` and `LAUNCHERS` membership are UNCHANGED from the committed sets; the gate fires from `tokenIsFinalLiteral`/`segmentAdmitDisposition` (the inverse proof), not from an added trigger spelling; the floor-invariance fuzz generates shell-special / de-quote-requiring tokens INDEPENDENT of the detector's character set (brace/tilde/process-sub/ANSI-C/quote-removal/mid-word) and they DENY structurally."
    - statement: "The SINGLE RESOLUTION AUTHORITY (anti-whack-a-mole) is preserved AND the literal-substring fallback on the NON-LAUNCHER path is ELIMINATED: the structural detector applies to ANY command word (recognized launcher, unrecognized launcher, shebang, wrapper-operand, redirection) — the round-4 catch-all and `commandHasLiveAdmitShape` no longer fall back to `v.includes(\"context-io\")` + token `admit`. The detector lands INSIDE the existing `liveAdmitSegments` walk and reuses `liveTokens` + the existing `DYNAMIC_COMMAND_WORD` token + token start offsets — NOT a new second parser, NOT a re-walk of the tokenizer. `function tokenize` and `noteFileFromCommand` stay DELETED (grep=0). `liveTokens` segmentation is byte-identical (the detector reads metacharacters/quotes already present in the emitted token values and raw spans; it does not change tokenization)."
      status: must-hold
      verification: "grep confirms `function tokenize` and `noteFileFromCommand` do not exist in hooks/admission-guard.ts; the literal-substring admit-shape scan is no longer the gating path on the non-launcher branch (the structural detector is); `function liveAdmitSegments` is still the single authority; the 25-04/25-05/25-06 tokenizer body (liveTokens) is unchanged (grep/diff confirms only the shape-detection disposition was edited)."
    - statement: "Over-block SAFETY MUST be proven and BOUNDED, consistent with the SCRIPT-anchored algorithm: the gate fires ONLY when the SCRIPT position is context-io-or-unresolvable (with the concrete-anchor bound on unrecognized command words). A recognized launcher with a provably-literal non-context-io script (`node build`, `node render`, `node build $HOME/out`, `node script.js test`), a recognized launcher running a provably-literal context-io NON-admit (`node scripts/context-io.js validate`), an unrecognized command with no concrete context-io/admit literal anchor (`cp $SRC $DST`, `tar $A $B`, `git log $REF`), a non-admit wrapper, a routine-only multi-admit under `high-severity`, and an inert heredoc/quoted/`#`-comment admit mention behind a wrapper all stay ALLOW. The bounded over-block (`node $SCRIPT build` gated only while `human_admission != off`) and the disclosed residual (`bun $S $V` — unrecognized runner with BOTH positions dynamic and no literal anchor; the recognized `node $S $V` still gates) are EXPLICIT in the threat model + summary, never a silent gate of a proven-non-admit launcher or non-launcher data."
      status: must-hold
      verification: "admission-guard.test.ts + the floor-invariance fuzz assert the recognized-non-admit-launcher / context-io-validate / no-anchor-unrecognized / non-admit-wrapper / routine-only / inert controls ALLOW; only a context-io-or-unresolvable script position (with the concrete anchor on unrecognized words) GATES; the bounded over-block + the residual are documented in the plan + 25-08-SUMMARY, not hidden."
    - statement: "The round-1..4 closures and the verified SC2 / GOV-02 surfaces MUST NOT regress. round-4 leading-run resolution (sub-roots a/b/c + dial-`all` wrapper leak + Class-E wrapper reopening + the sh -c tail), round-3 Class A/B/E/F, round-2 GAP-B/C/D, the four named floor invariants, SC2 config lockstep, and the GOV-02 audit ledger all still hold. `scripts/context-io.ts` is NOT edited this round (the un-forgeable hook fix re-closes the dial-`all` expansion case; the weak self-settable D-05 admit() tier adds no un-forgeable defense, and keeping context-io.ts byte-frozen preserves GAP-C/GAP-D + the GOV-02 ledger byte-identically). `hooks/guard.ts` stays byte-frozen (D-02)."
      status: must-hold
      verification: "the round-2/3/4 sweeps in admission-guard.test.ts / floor-invariance.test.ts stay GREEN; `git diff --quiet scripts/context-io.ts` exits 0; `git diff --quiet hooks/guard.ts` exits 0; freshness 0; config-consistency + audit-ledger + foundation-guard suites green."
    - statement: "The SC1 floor + an INDEPENDENT both-angle red-team (guard-LOGIC probe AND guard-INPUT-SURFACE code-review, opus-grade — the P23 split; across the FIVE prior rounds the LOGIC and INPUT-SURFACE angles each found a class the other missed, so BOTH are required) are the proof, not a green suite. This is the named closure gate. The probe MUST invent at least one un-enumerated rewrite form PER angle that the author did NOT enumerate (e.g. a brace in the note-path, a process-substitution script, an ANSI-C `$'…'` verb, a non-node runner behind a wrapper, a 3-admit ordering shielding an expansion-obfuscated high-severity behind two routines) and confirm each is caught structurally (denied or gated), proving the detector fails closed on the absence of a final-literal proof rather than on an anchor list."
      status: must-hold
      verification: "the blocking checkpoint (Task 25-08-04) HARD-STOPS until an independent opus-grade red-team reproduces both the INPUT-SURFACE angle (a glob/brace/quote-removal/tilde/process-sub script-or-verb, or a non-node runner, escaping the detector; or an inert mention reading as a live admit) AND the LOGIC angle (the arg-position cmd-sub / xargs / the D-01 glob self-set / the dial-`all` routine-via-expansion / a multi-admit shield), invents an un-enumerated rewrite form per angle, confirms the four floor invariants + `git diff --quiet hooks/guard.ts` exit 0 + `git diff --quiet scripts/context-io.ts` exit 0 + freshness 0 + the bounded over-block (no proven-non-admit false-positive), vs the committed `.js`; findings recorded in 25-08-SUMMARY.md, not self-reported as a green suite."
    - statement: "WR-01 / no kit-content false-positive (D-13): the detector edit and the new test/proof artifacts must not trip `guard_context_writes` or any foundation guard — reference the hook / Workflow 16 / the config keys by name, never restate a raw `.grugops/` write path beside a write token."
      status: must-hold
      verification: "`npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial); the closed Plan-25-0x deferral markers stay inert (no false-positive)."
---

<objective>
Round-5 gap closure for Phase 25 (25-VERIFICATION.md "UPDATE 2026-06-25 — post-25-07" TOP banner + 25-07-SUMMARY.md "Checkpoint 25-07-04 — INDEPENDENT both-angle red-team", gaps_found), HARDENED by an independent opus adversarial plan-check (3 BLOCKERs + 2 WARNINGs closed ON PAPER before execution). Plan 25-07's round-4 INVERT correctly closed the leading-run command-RESOLUTION class (CONFIRMED SOLID by both red-team angles and committed). The same red-team + the orchestrator's reproduction then converged on ONE NEW structural class the 846-green author suite missed. This plan does NOT re-touch the round-1..4 closures, the verified SC2 config foundation, or the GOV-02 audit ledger; it makes the admit-SHAPE detector structural — the entire fix lives in the hook's shape detection inside the ONE `liveAdmitSegments` authority.

**The single root cause (confirmed in the committed source):** round 4 hardened command-word RESOLUTION (the leading run), but the admit-SHAPE DETECTOR — `segmentHasAdmitShape` (admission-guard.ts:459-467) and `commandHasLiveAdmitShape` (:679-686) — is still a LITERAL substring/token test (`v.includes("context-io")` to set `sawScript`, then a token `=== "admit"`). It runs on the **un-expanded** command string, so any shell rewrite that defers the script-ref or the verb to runtime makes the launcher carry no literal admit token → "no admit shape" → silently dropped (ALLOW). Confirmed: glob script-ref `node scripts/context-i*.js admit … <hi>`; argument-position command substitution `node $(echo <ctx> admit … <hi>)`; parameter-expansion/word-split `node "$S" admit …` / `node <ctx> "$V" …` / `$A`; cross-segment stdin→argv `echo "<ctx> admit … <hi>" | xargs node`. AGGRAVATING: the glob form defeats the D-01 refuse-self-set floor; MOST DAMAGING: under dial `all` a routine admit via expansion has no in-script backstop.

**The fix — the prove-final-literal INVERSION (the plan-check correction; this replaces the round-5-v1 'bounded POSIX alphabet' framing, which was itself a disguised enumeration that is FALSE for bash).** Do NOT enumerate what expands (`*`→`?`→`$`→brace→tilde→process-sub→… is unbounded, and the Bash tool runs bash, not POSIX sh). Instead require PROOF that nothing expands: a script-or-verb-position token is RESOLVABLE only if it is PROVABLY ALREADY ITS FINAL LITERAL FORM — its de-quoted value carries NONE of the bash rewrite characters `* ? [ ] { } ~ < >`, its raw source carries no `$` and no backtick (parameter / command / arithmetic / process / ANSI-C substitution), it is not the synthetic DYNAMIC_COMMAND_WORD sentinel, and it is not fed via an stdin→argv feeder (xargs). Anything else is UNRESOLVABLE → fail CLOSED. The disposition is SCRIPT-anchored (`segmentAdmitDisposition`): a provably-literal-non-context-io script ALLOWs (a later expansion cannot retroactively make argv[script] context-io — this is exactly why `node build $HOME/out` ALLOWs); a provably-literal-context-io script gates on an admit-or-unresolvable verb (closing the brace `admi{t,x}` and quote-removal `ad''mit` cases); an UNRESOLVABLE script gates (closing glob/param/cmd-sub script-refs and the bounded `node $SCRIPT` over-block). The structural test applies to ANY command word — recognized launcher, unrecognized launcher (bun/deno/ts-node — BLOCKER 2), or a shebang script (WARNING 4) — never the literal substring; launcher recognition is used ONLY to AVOID over-blocking. The same ONE detector feeds the launcher branch (:610-615), the round-4 catch-all (:661-668), and `commandHasLiveAdmitShape` (the D-01 floor). Enforcement stays in the EXTERNAL hook — the un-forgeable D-01 tier; `scripts/context-io.ts` is NOT edited (the weak self-settable D-05 `admit()` tier adds no un-forgeable defense, and keeping it byte-frozen preserves GAP-C/GAP-D + the GOV-02 ledger).

Plus the D-12 proof: the RED-first capture of each form + compound + the D-01 self-set + the dial-`all` leak ALLOWing vs the PRE-FIX committed `.js` (blob `756ce50841e4a5c4955d2197ce08f43e47931659`, HEAD `f5beb06`), the extended child-spawn suite + an anti-whack-a-mole property fuzz whose oracle is `tokenIsFinalLiteral` and whose token generator is INDEPENDENT of the detector's recognized characters (so brace/tilde/process-sub/ANSI-C/quote-removal forms are caught WITHOUT being whitelisted), the RED->GREEN proof artifacts, the rebuilt fresh `.js`, and the HARD-STOP independent both-angle red-team checkpoint.

Purpose: make GOV-01's un-forgeable primary tier actually un-forgeable across the full shell-rewrite surface by requiring proof of a final literal, not by chasing spellings — proven adversarially vs the committed `.js`, not by a green author suite. This is the 9th green-suite-insufficient catch.
Output: the prove-final-literal SCRIPT-anchored detector (the expansion classes + brace/quote-removal + non-node launchers + the shebang + the nested-wrapper compound + the D-01 floor + the dial-`all` leak closed), the extended suite + the independent-generator property fuzz, the RED/GREEN proof artifacts, the rebuilt fresh `.js`, and the independent both-angle red-team confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-PATTERNS.md
@.planning/phases/25-governance-on-a-dial/25-VERIFICATION.md
@.planning/phases/25-governance-on-a-dial/25-07-SUMMARY.md
@.planning/phases/25-governance-on-a-dial/25-07-RED-baseline.txt
@.planning/phases/25-governance-on-a-dial/25-07-GREEN-proof.txt
</context>

<artifacts_this_phase_produces>
New / changed symbols and files this round-5 gap-closure introduces (for the executor and downstream consumers):

- `hooks/admission-guard.ts`:
  - `tokenIsFinalLiteral(token, cmd)` — the prove-final-literal predicate (renamed/respec'd from the round-5-v1 `argvHasUnresolvableExpansion`, INVERTED): returns true only when a token is PROVABLY its final literal form. Dual check, requiring NO tokenizer change: (1) the de-quoted token value (already de-quoted by `liveTokens`/`stripQuotedRuns`) contains none of `* ? [ ] { } ~ < >`; (2) the token's RAW source slice (from its `start` offset to the next token boundary) contains no `$` and no backtick — catching parameter / command / arithmetic / process / ANSI-C substitution EVEN IF the tokenizer consumed or collapsed the span, while not over-flagging a balanced quoted literal like `"admit"` (raw has `"` but no `$`/backtick); (3) the token is not the synthetic `DYNAMIC_COMMAND_WORD` sentinel. Anything failing the proof is UNRESOLVABLE.
  - `segmentAdmitDisposition(tokens, cmdIdx, cmd)` → `"admit" | "unresolvable" | "none"` (+ the noteFile when `admit`) — the SCRIPT-anchored resolver (renamed/respec'd from `launcherAdmitDisposition`). For a RECOGNIZED launcher (node|npx|tsx|nodejs, skipping an npx `tsx`/runner token): the script position is pinned; a provably-literal-non-context-io script → `none`; a provably-literal-context-io script → inspect the verb (`admit`-or-unresolvable → gate; provably-literal non-admit or absent → `none`); an unresolvable script (incl. stdin-fed via a feeder) → `unresolvable`. For an UNRECOGNIZED command word (bun/deno/ts-node/bunx, a shebang script, an unlisted wrapper-operand, a redirection): the structural concrete-anchor scan over the segment's positional tokens (incl. the command word itself as a script candidate for the shebang case) — gate when a context-io-or-unresolvable script-candidate token is immediately followed by an admit-or-unresolvable verb token AND at least one of the two is a concrete literal (script literally context-io OR verb literally admit); else `none`.
  - `STDIN_ARGV_FEEDERS = new Set(["xargs"])` — a DISTINCT structural concept (a launcher reached behind a feeder has a stdin-sourced argv → unresolvable script), NOT a widening of `COMMAND_MODIFIERS` (whose membership stays the committed nine) or `LAUNCHERS` (unchanged).
  - The launcher branch (`liveAdmitSegments`, :610-615), the round-4 catch-all (:661-668), and `commandHasLiveAdmitShape` (:679-686, the D-01 floor) all consume the ONE detector — the round-4 catch-all and the floor scan NO LONGER fall back to the literal `v.includes("context-io")` + token `admit` substring test (BLOCKER 2 closed).
- `hooks/admission-guard.test.ts`: extended child-spawn cases vs the committed `.js` — each round-5 form (glob / arg-cmd-sub / param / xargs / brace / quote-removal / non-node launcher / shebang) DENIES; the nested wrapper+expansion compounds DENY; the D-01 glob self-set DENIES; the dial-`all` routine-via-expansion DENIES; the over-block controls ALLOW; round-1..4 still hold.
- `scripts/floor-invariance.test.ts`: the round-5 prove-final-literal property fuzz with an INDEPENDENT shell-special token generator; the round-2/3/4 sweeps preserved.
- `25-08-RED-baseline.txt`, `25-08-GREEN-proof.txt`: the captured RED->GREEN proof artifacts (22-08/22-09 discipline), extending the 25-07 row format.

NOTE: `scripts/context-io.ts` is NOT edited this round — the entire fix lives in the hook's shape detection; the structural hook fix re-closes the dial-`all` expansion case at the un-forgeable tier, an `admit()` backstop would only harden the weak self-settable D-05 tier, and touching context-io.ts would risk a GAP-C/GAP-D or GOV-02-ledger byte regression. The disclosed bounded over-block (`node $SCRIPT build` under active governance) and the residual (`bun $S $V` — an unrecognized runner with BOTH script AND verb expansion-hidden and no literal anchor) are documented, not hidden.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 25-08-01: Make the admit-SHAPE detector STRUCTURAL via the prove-final-literal inversion + a SCRIPT-anchored disposition applied to ANY command word — capture the RED baseline first</name>
  <files>hooks/admission-guard.ts, hooks/admission-guard.js, hooks/admission-guard.test.ts, 25-08-RED-baseline.txt</files>
  <read_first>
    - hooks/admission-guard.ts:85-139 (ADMIT_VERB/ADMIT_SCRIPT at :85-86, LAUNCHERS at :89 — the recognized-launcher ESCAPE set, NOT to be widened; COMMAND_MODIFIERS at :110-120 — NOT to be widened; DYNAMIC_EVAL_WORDS at :131; read so the executor does NOT reach for the anti-pattern of enumerating runners or expansion spellings)
    - hooks/admission-guard.ts:145-184 (effectiveCommandWord at :145-149 — basename; stripQuotedRuns at :156-177 — the de-quoter the prove-final-literal check builds on; the DYNAMIC_COMMAND_WORD sentinel at :178-184)
    - hooks/admission-guard.ts:186-192 + 230-434 (the LiveToken interface with the `start` byte offset at :188-192 — the raw-span access the dual final-literal check uses; the whole liveTokens tokenizer — read so the executor confirms `*?[]{}~<>` and bare `$` SURVIVE in emitted token values, `{`/`}` survive mid-word, balanced quotes/`\\`-escapes are collapsed, and `$(`/backtick at token-start emit DYNAMIC_COMMAND_WORD — so NO tokenizer change is needed)
    - hooks/admission-guard.ts:455-505 (segmentHasAdmitShape at :459-467 — THE LITERAL DEFECT this task replaces with the structural detector; isEnvIndirectedWord at :470-472; segmentHasDynamicEvalWord at :484-492 and rawTextHasAdmitShape at :503-505 — the round-4 dynamic-eval tail to preserve)
    - hooks/admission-guard.ts:507-548 (rawSegmentText at :510-520 — the raw-span helper pattern the final-literal raw check mirrors; noteFileInSegment at :527-548 — the note-positional reader the `admit` disposition reuses, returning null when the note is absent or non-final-literal → fail-closed)
    - hooks/admission-guard.ts:550-671 (the AdmitSegment interface :553-556 and the WHOLE liveAdmitSegments walk :574-671 — THE EDIT SITE: the launcher branch :610-615, the Class B :602-607 and dynamic-eval :620-628 branches to preserve, and the round-4 INVERT catch-all :630-668 where the unrecognized-command-word structural scan plugs in)
    - hooks/admission-guard.ts:673-686 (commandHasLiveAdmitShape — the GAP-B/D-01 floor input that must derive from the structural detector so the glob/param self-set DENIES)
    - hooks/admission-guard.ts:744-922 (the main flow + classifySegmentOrDeny :808-911 — how an `unresolvable: true` segment fails CLOSED and a resolvable segment classifies severity by `by`; the detector pushes `{noteFile, unresolvable}` exactly like today)
    - .planning/phases/25-governance-on-a-dial/25-07-SUMMARY.md (the "Checkpoint 25-07-04 — INDEPENDENT both-angle red-team" confirmed-bypass table + the converged root cause; the bar this task clears)
    - .planning/phases/25-governance-on-a-dial/25-07-RED-baseline.txt and 25-07-GREEN-proof.txt (the both-direction proof ROW FORMAT this task's 25-08-RED-baseline.txt must extend)
    - hooks/admission-guard.test.ts:1-130 (the runGuard / expectDeny / expectAllow child-spawn harness + makeProject/writeNote/admitCmd fixtures vs the COMMITTED admission-guard.js; the `>= guard.test.ts` case-count watch; extend, never reimplement)
    - hooks/guard.ts:50,88 (the verb-anchor + refuse-self-set posture to MIRROR — DO NOT EDIT guard.ts, D-02; read for pattern only)
  </read_first>
  <behavior>
    - CAPTURE RED FIRST (the 22-08/22-09 discipline): before any source edit, child-spawn each round-5 form through the CURRENT committed `hooks/admission-guard.js` (blob 756ce50841e4a5c4955d2197ce08f43e47931659, HEAD f5beb06) on a planted high-severity note (by: security-nfr) under human_admission: high-severity with NO approval var, and record the observed pre-fix decision per row (the ALLOWing rows are the round-5 bypasses; the byte-identical literal-path control DENIES): glob `node scripts/context-i*.js admit T NOTE` (+ `…context-i?.js`, `…context-i[o].js`); arg-cmd-sub `node $(echo <ctx> admit T NOTE)`, backtick; param `node "$S" admit T NOTE`, `node <ctx> "$V" T NOTE`, unquoted `node $A admit T NOTE`; xargs `echo "<ctx> admit T NOTE" | xargs node`; BRACE `node <ctx> admi{t,x} T NOTE`; quote-removal `node scripts/'context-io'.js admit T NOTE`, `node <ctx> ad''mit T NOTE`, `node <ctx> "admit" T NOTE`, `node <ctx> adm\\it T NOTE`; NON-NODE launchers `bun scripts/context-i*.js admit T NOTE`, `deno run <ctx> admit T NOTE`, `ts-node <ctx> admit T NOTE`; SHEBANG `<ctx> admit T NOTE` (the script path as the command word). Also record the nested wrapper+expansion compounds (`timeout 5 node scripts/context-i*.js admit T NOTE`, `>/dev/null node $(echo … admit …)`), the D-01 glob self-set (`GRUGOPS_ADMISSION_APPROVED_BY=eve node scripts/context-i*.js admit T NOTE`), and the dial-`all` routine-via-expansion (`node scripts/context-i*.js admit T ROUTINE` under `all`). Save to 25-08-RED-baseline.txt (extend the 25-07 row format; cite the pre-fix blob).
    - AFTER the fix, against the rebuilt committed `.js`, on a gated high-severity un-approved note → DENY (reason names the unresolvable / fail-closed condition): every round-5 form DENIES; the nested wrapper+expansion compounds DENY; the D-01 glob self-set DENIES; the dial-`all` routine-via-expansion DENIES.
    - SCRIPT-anchored NARROW trigger (the bounded fail-safe direction): a RECOGNIZED launcher with a provably-literal non-context-io script → ALLOW (`node build`, `node render`, `node script.js test`, `node build $HOME/out` — the `$HOME` is irrelevant because argv[script]=`build` already proves non-admit); a recognized launcher running a provably-literal context-io NON-admit verb → ALLOW (`node scripts/context-io.js validate`, `node scripts/context-io.js render`); an UNRECOGNIZED command with no concrete context-io/admit literal anchor → ALLOW (`cp $SRC $DST`, `tar $A $B`, `git log $REF`).
    - Bounded over-block (EXPLICIT, accepted): a fully-dynamic `node $SCRIPT build` (script position not provably-literal) GATES under `high-severity`/`all` (ALLOWs under `off`). Disclosed residual (accepted): an unrecognized runner with BOTH script AND verb expansion-hidden and no literal anchor (`bun $S $V`) ALLOWs — the recognized `node $S $V` still gates.
    - NO over-block regression on inert data: an inert heredoc body line behind a wrapper, a single/double-quoted echoed mention behind a wrapper (`timeout 5 echo "node context-io.js admit …"` — one quoted blob token, no separate script+verb pair), a `#`-comment mention, a routine-only multi-admit under high-severity → ALLOW.
    - No regression on the round-1..4 closures: round-4 sub-roots a/b/c + dial-`all` wrapper leak + Class-E wrapper reopening + the `sh -c "…admit…"` tail still DENY; round-3 Class A path-form launcher DENIES, Class B `$()`/backtick COMMAND-WORD GATES, Class E bare multi-admit DENIES while routine-only ALLOWs, Class F duplicate/indented `by` DENIES; round-2 GAP-B self-set-behind-a-wrapper DENIES, GAP-D case-variant `by` gates; empty/malformed stdin → ALLOW.
  </behavior>
  <action>
    FIRST capture the RED baseline. Against the CURRENT committed `hooks/admission-guard.js` (blob 756ce50841e4a5c4955d2197ce08f43e47931659, HEAD f5beb06), child-spawn each round-5 form (glob / arg-cmd-sub / param-word-split / xargs / brace / quote-removal / non-node launcher / shebang), plus the nested wrapper+expansion compounds, the D-01 glob self-set, and the dial-`all` routine-via-expansion, with a planted high-severity note and NO approval var, and record the observed pre-fix decision per row (the ALLOWing rows are the round-5 bypasses; the literal-path control DENIES) into `25-08-RED-baseline.txt` — in the 25-07 row format, citing the pre-fix blob.

    THEN make the structural shape-detection fix inside `liveAdmitSegments` (admission-guard.ts:574-671), built on the prove-final-literal INVERSION. Do NOT enumerate expansion triggers, do NOT widen `COMMAND_MODIFIERS` or `LAUNCHERS`, and do NOT re-walk the `liveTokens` tokenizer.

    Add `tokenIsFinalLiteral(token, cmd)`: a token is RESOLVABLE (final-literal) only when ALL hold — (1) its de-quoted value (the value `liveTokens` already emits via `stripQuotedRuns`) contains none of `* ? [ ] { } ~ < >`; (2) its RAW source slice (from the token's `start` offset to the next token boundary, mirroring `rawSegmentText`) contains no `$` and no backtick — so parameter / command / arithmetic / process / ANSI-C substitution is caught even when the tokenizer consumed or collapsed the span, while a balanced quoted literal like `"admit"` (raw has `"` but no `$`/backtick) is NOT over-flagged; (3) it is not the `DYNAMIC_COMMAND_WORD` sentinel. Anything else is UNRESOLVABLE. This is the inverse, genuinely-closed property — proof of a final literal — NOT a list of what expands.

    Add `segmentAdmitDisposition(tokens, cmdIdx, cmd)` → `"admit" | "unresolvable" | "none"` (+ the noteFile when `admit`), SCRIPT-anchored:
    - RECOGNIZED launcher at cmdIdx (node|npx|tsx|nodejs, skipping an npx `tsx`/runner token and flags to reach the SCRIPT position; if the launcher was reached behind a `STDIN_ARGV_FEEDERS` member the script is stdin-sourced → unresolvable): classify the SCRIPT-position token via `tokenIsFinalLiteral` + a de-quoted `context-io` test — a provably-literal NON-context-io script → `none` (a later expansion cannot make argv[script] context-io; this is why `node build $HOME/out` ALLOWs); a provably-literal CONTEXT-IO script → inspect the VERB token (the next positional): a provably-literal `admit` → `admit` (noteFile = the next positional if provably-literal, else null → fail-closed); an UNRESOLVABLE verb → `unresolvable`; a provably-literal non-admit verb or no verb → `none`; an UNRESOLVABLE script → `unresolvable`.
    - UNRECOGNIZED command word at cmdIdx (bun/deno/ts-node/bunx, a shebang script, an unlisted wrapper-operand, a redirection target): the structural concrete-anchor scan over the segment's positional tokens (consider the command word itself as a script candidate for the shebang case) — `admit`-or-`unresolvable` if there exist adjacent positional tokens (Ti, Ti+1) with [classifyScript(Ti) ∈ {literal-context-io, unresolvable}] AND [classifyVerb(Ti+1) ∈ {literal-admit, unresolvable}] AND [Ti is literal-context-io OR Ti+1 is literal-admit] (the concrete-literal anchor that bounds speculative over-block so `cp $SRC $DST` stays `none`); else `none`.

    Add `STDIN_ARGV_FEEDERS = new Set(["xargs"])` — a DISTINCT structural concept (NOT a `COMMAND_MODIFIERS` widening; its membership stays the committed nine). Track in the leading-run prefix-skip whether the skip passed a feeder; if so, the launcher's script is stdin-sourced → unresolvable.

    Wire the ONE detector into all three consumers (the anti-whack-a-mole point — a single structural authority, three call sites, NO literal-substring fallback on any path): (a) the LAUNCHER branch (:610-615) replaces the literal `segmentHasAdmitShape` call with `segmentAdmitDisposition` — push `{ noteFile, unresolvable: false }` on `admit`, `{ noteFile: null, unresolvable: true }` on `unresolvable`, push nothing on `none`. (b) the round-4 INVERT catch-all (:661-668), reached when cmdIdx is an unrecognized command word (a wrapper-operand / redirection / non-node runner / shebang), calls `segmentAdmitDisposition` (the unrecognized-command branch) instead of the literal `segmentHasAdmitShape` — closing BLOCKER 2 (the literal-substring fallback on the non-launcher path is eliminated) and the nested wrapper+expansion compounds; the round-4 dynamic-eval-tail raw-text behavior is preserved. (c) `commandHasLiveAdmitShape` (:679-686, the D-01 floor) derives from the same structural detector / the one `liveAdmitSegments` authority (e.g. `liveAdmitSegments(cmd).length > 0`) so a self-set co-occurring with a structurally-detected admit-or-unresolvable segment is refused even behind a glob/param spelling.

    PRESERVE the existing branches and floors exactly: the Class B command-word `$()`/backtick branch (:602-607) and the dynamic-eval / `$X` tail (:620-628) stay (their admit-shape checks may reuse the structural detector but must keep their unresolvable→gate behavior); the dial canonicalization (`off` exits, `gateEveryMatch = dial !== "high-severity"`, GAP-C) and the case-insensitive `HIGH_SEVERITY_ROLES` membership (GAP-D) are unchanged. Do NOT rewrite the `liveTokens` tokenizer — the 25-04/25-05/25-06 segmentation stays byte-identical (the detector reads `*?[]{}~<>` and the DYNAMIC_COMMAND_WORD sentinel from token VALUES, and `$`/backtick from token RAW SPANS via the `start` offsets the tokenizer ALREADY emits). The change is the shape-detection DISPOSITION only.

    Voice: CLEAR PROFESSIONAL VOICE in all comments and deny reasons (CLAUDE.md hard rule — governance/admission/audit surfaces are never caveman voice). Comment the detector so a future reader sees the principle: the shell — not the hook — forms the final argv, so a script/verb token the hook cannot PROVE is already its final literal form (and non-admit) is gate-or-stricter; we require proof that nothing expands, never a list of what does.

    Comment-text discipline: do NOT place a fenced code block inside this action's prose; describe the detector by its named conditions (final-literal proof on de-quoted value + raw span; script-anchored disposition; concrete-anchor scan on unrecognized command words).

    Rebuild the committed `.js`: `npm run build` then `npm run freshness` (0 drift; `admission-guard.js` is freshness-covered).

    Extend `hooks/admission-guard.test.ts` (the child-spawn oracle vs the COMMITTED `.js`): add cases for each round-5 form (glob / arg-cmd-sub / param / xargs / brace / quote-removal / non-node launcher / shebang) → DENY; the nested wrapper+expansion compounds → DENY; the D-01 glob self-set → DENY; the dial-`all` routine-via-expansion → DENY; the over-block controls (`node build`, `node build $HOME/out`, `node scripts/context-io.js validate`, `node render`, `cp $SRC $DST`, `timeout 5 node render`, `sudo ls`, `>/dev/null node build`, routine-only multi-admit, inert mention behind a wrapper) → ALLOW; re-assert the round-1..4 closures. Keep the case count >= guard.test.ts (Pitfall-2 watch). Capture the post-fix DENY/ALLOW outcomes for these forms into `25-08-GREEN-proof.txt` (this task contributes the form + compound + floor rows; Task 25-08-03 completes the proof set with the fuzz).
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run hooks/admission-guard.test.ts && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts</automated>
  </verify>
  <acceptance_criteria>
    - `25-08-RED-baseline.txt` exists and records each round-5 form — glob (`node scripts/context-i*.js admit …`), arg-cmd-sub (`node $(echo … admit …)`), param (`node "$S" admit …`, `node <ctx> "$V" …`, `$A`), xargs (`… | xargs node`), brace (`node <ctx> admi{t,x} …`), quote-removal (`ad''mit`, `scripts/'context-io'.js`, `"admit"`, `adm\\it`), non-node launchers (`bun`/`deno run`/`ts-node`), shebang (`<ctx> admit …`) — plus the nested wrapper+expansion compounds, the D-01 glob self-set, and the dial-`all` routine-via-expansion, with the observed PRE-FIX decision per row (the ALLOWing rows = the round-5 bypasses) and the literal-path control DENYing, in the 25-07 row format citing the pre-fix blob.
    - A grep confirms `COMMAND_MODIFIERS` and `LAUNCHERS` membership are UNCHANGED from the committed sets (no widening), `function tokenize` and `noteFileFromCommand` still do NOT exist (single authority preserved), `function liveAdmitSegments` is still the one authority, the literal `segmentHasAdmitShape` substring scan is NO LONGER the gating path on the launcher branch or the round-4 catch-all (the structural `segmentAdmitDisposition` is), and the `liveTokens` tokenizer body is unedited.
    - `npx vitest run hooks/admission-guard.test.ts` is GREEN: every round-5 form DENIES a gated high-severity un-approved admit; the nested wrapper+expansion compounds DENY; the D-01 glob self-set DENIES; the dial-`all` routine-via-expansion DENIES; the over-block controls (`node build`, `node build $HOME/out`, `node scripts/context-io.js validate`, `node render`, `cp $SRC $DST`, `timeout 5 node render`, `sudo ls`, `>/dev/null node build`, routine-only multi-admit, inert mention behind a wrapper) ALLOW; the round-1..4 closures still hold; empty stdin ALLOWs; case count >= guard.test.ts.
    - `npm run freshness` exits 0 (committed `admission-guard.js` fresh); `git diff --quiet hooks/guard.ts` exits 0 (byte-frozen, D-02); `git diff --quiet scripts/context-io.ts` exits 0 (NOT edited this round).
    - `25-08-GREEN-proof.txt` records the post-fix DENY for each round-5 form + the compounds + the D-01 self-set + the dial-`all` leak, plus ALLOW for the recognized-non-admit-launcher / context-io-validate / no-anchor-unrecognized / non-admit-wrapper / routine-only / inert controls.
  </acceptance_criteria>
  <done>The admit-shape detector is structural via the prove-final-literal inversion + a SCRIPT-anchored disposition applied to ANY command word: a script/verb token not provably its final literal form (per `tokenIsFinalLiteral`) is unresolvable, a provably-literal-non-context-io script ALLOWs, a provably-literal-context-io script gates on an admit-or-unresolvable verb, and the structural scan (not the literal substring) handles unrecognized launchers + shebangs — closing glob/arg-cmd-sub/param/xargs/brace/quote-removal/non-node/shebang + the nested wrapper compound + the D-01 glob self-set + the dial-`all` leak from ONE detector wired into the launcher branch, the round-4 catch-all, and the floor scan; `COMMAND_MODIFIERS`/`LAUNCHERS` not widened, the second walk stays deleted, `liveTokens` byte-identical, the round-1..4 closures hold — vs the committed `.js`; guard.ts + context-io.ts untouched, freshness 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 25-08-02: Extend the child-spawn oracle (admission-guard.test.ts) — exhaustive round-5 form + compound + over-block cases vs the committed .js, and complete the GREEN-proof rows</name>
  <files>hooks/admission-guard.test.ts, 25-08-GREEN-proof.txt</files>
  <read_first>
    - hooks/admission-guard.test.ts:1-130 (the runGuard / expectDeny / expectAllow harness + makeProject / writeNote / admitCmd fixtures + the `>= guard.test.ts` case-count watch — extend, never reimplement; this targets the COMMITTED admission-guard.js)
    - hooks/admission-guard.test.ts (the round-4 sub-root (a)/(b)/(c) + round-3 Class A/B/E/F describe blocks added by 25-06/25-07 — read them so the round-5 cases EXTEND, do not duplicate; the multi-admit / inert-mention / launcher fixtures are reusable)
    - hooks/admission-guard.ts:455-505, 574-686 (the post-fix `tokenIsFinalLiteral` + `segmentAdmitDisposition` + the three call sites from Task 25-08-01 — the cases assert their behavior against the committed `.js`)
    - .planning/phases/25-governance-on-a-dial/25-07-GREEN-proof.txt (the GREEN-proof ROW FORMAT this task's 25-08-GREEN-proof.txt rows extend)
    - .planning/phases/25-governance-on-a-dial/25-07-SUMMARY.md (the "Checkpoint 25-07-04" confirmed-bypass table — the exact commands the cases must cover; the over-block controls)
  </read_first>
  <behavior>
    - Expansion-class forms (each DENY vs the committed `.js` under `high-severity`): glob `node scripts/context-i*.js admit T <hi>` (+ `…?.js`, `…[o].js`); arg-cmd-sub `node $(echo <ctx> admit T <hi>)` + backtick; param `node "$S" admit T <hi>` (with `export S=<ctx>`), `node <ctx> "$V" T <hi>` (with `export V=admit`), unquoted `node $A admit T <hi>`; xargs `echo "<ctx> admit T <hi>" | xargs node`.
    - Rewrite-class forms (each DENY): brace `node <ctx> admi{t,x} T <hi>`; quote-removal `node scripts/'context-io'.js admit T <hi>`, `node <ctx> ad''mit T <hi>`, `node <ctx> "admit" T <hi>`, `node <ctx> adm\\it T <hi>`; the fully-static quoted admit `node "scripts/context-io.js" "admit" T <hi>` (de-quotes to a literal context-io admit → DENY); tilde `node ~/context-io.js admit T <hi>`; process-sub `node <(echo <ctx>) admit T <hi>`.
    - Non-node launcher forms (each DENY): `bun scripts/context-i*.js admit T <hi>`, `bunx <ctx> admit T <hi>`, `deno run <ctx> admit T <hi>`, `ts-node <ctx> admit T <hi>`.
    - Shebang form (DENY): `<ctx> admit T <hi>` (the script path as the command word).
    - Nested wrapper+expansion compounds (each DENY): `timeout 5 node scripts/context-i*.js admit T <hi>`, `sudo node "$S" admit T <hi>`, `>/dev/null node $(echo <ctx> admit T <hi>)`, `nice -n 5 bun <ctx> admit T <hi>`.
    - D-01 floor: `GRUGOPS_ADMISSION_APPROVED_BY=eve node scripts/context-i*.js admit T <hi>` → DENY even with the var in env.
    - dial-`all`: `node scripts/context-i*.js admit T <routine>`, `bun <ctx> admit T <routine>` under `human_admission: all` → DENY.
    - Over-block controls (the bounded fail-safe — MUST ALLOW): `node build`, `node build $HOME/out`, `node scripts/context-io.js validate`, `node render`, `node script.js test`, `cp $SRC $DST`, `tar $A $B`, `git log $REF`, `timeout 5 node render`, `sudo ls`, `>/dev/null node build`, a routine-only multi-admit under `high-severity`, and an inert heredoc/quoted/`#`-comment admit mention behind a wrapper → ALLOW. Under `off`, a dynamic `node $SCRIPT build` → ALLOW.
    - No regression: the round-4 sub-root (a)/(b)/(c) + dial-`all` wrapper leak + Class-E wrapper reopening + sh -c tail; the round-3 Class A/B/E/F; the round-2 GAP-B/GAP-D cases still pass exactly as before.
  </behavior>
  <action>
    Extend `hooks/admission-guard.test.ts` with describe blocks for the round-5 structural-shape classes — expansion (glob / arg-cmd-sub / param / xargs), rewrite (brace / quote-removal / tilde / process-sub), non-node launcher (bun/bunx/deno run/ts-node), and shebang — plus the nested wrapper+expansion compounds, the D-01 glob self-set, the dial-`all` routine-via-expansion, and the over-block ALLOW controls. Reuse the existing `runGuard` / `expectDeny` / `expectAllow` harness and the `makeProject` / `writeNote` / `admitCmd` fixtures (a high-severity `by: security-nfr` note, a routine `by: software-engineer` note, the temp factory.config.json per dial). Do NOT duplicate the round-3/round-4 blocks — add the new forms only.

    For each form, build the command by delivering the real admit argv via the rewrite under test (the glob/brace/quote-removal script-or-verb, the arg-position `$(...)`, the `"$S"`/`"$V"` parameter expansion with the matching `export`, the `| xargs node` pipe, the non-node runner, the bare shebang path) and assert DENY under `high-severity`. For the compounds, prefix the rewrite-obfuscated launcher with a round-4 wrapper/operand/redirection and assert DENY. For the dial-`all` cases, target the routine note under the `all` project and assert DENY. For the D-01 self-set, prefix the glob admit with the inline approval var and assert DENY. For the over-block controls, assert ALLOW for each recognized non-admit launcher (incl. `node build $HOME/out` and `node scripts/context-io.js validate`), each no-anchor unrecognized command (`cp $SRC $DST`, `tar $A $B`, `git log $REF`), each non-admit wrapper, the routine-only multi-admit, and an inert mention behind a wrapper; assert the dynamic `node $SCRIPT build` ALLOWs under `off`.

    Keep the case count >= guard.test.ts (the Pitfall-2 silent-deny-path watch). Capture the post-fix DENY rows (each form, each compound, the D-01 self-set, the dial-`all` leak) and the ALLOW control rows into `25-08-GREEN-proof.txt`, extending the 25-07 row format and referencing the committed `.js`, never the `.ts`.

    Voice: CLEAR PROFESSIONAL VOICE in comments (this oracle owns the SC1 floor). Per D-12, a GREEN oracle is NECESSARY-NOT-SUFFICIENT — the independent red-team at Task 25-08-04 is the gate; state that in a comment.

    Comment-text discipline: no fenced code blocks in this action; describe the cases by the command shape and the expected decision.
  </action>
  <verify>
    <automated>npx vitest run hooks/admission-guard.test.ts && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run hooks/admission-guard.test.ts` is GREEN with the round-5 cases added: every expansion / rewrite / non-node-launcher / shebang form DENIES under `high-severity`; the nested wrapper+expansion compounds DENY; the D-01 glob self-set DENIES; the dial-`all` routine-via-expansion DENIES; the over-block controls (`node build`, `node build $HOME/out`, `node scripts/context-io.js validate`, `node render`, `cp $SRC $DST`, `tar $A $B`, `git log $REF`, `timeout 5 node render`, `sudo ls`, `>/dev/null node build`, routine-only multi-admit, inert mention behind a wrapper) ALLOW; the dynamic `node $SCRIPT build` ALLOWs under `off`.
    - The round-3 Class A/B/E/F + round-4 sub-root (a)/(b)/(c) + dial-`all` wrapper + Class-E wrapper + round-2 GAP-B/GAP-D cases still pass (no regression); case count >= guard.test.ts.
    - `git diff --quiet scripts/context-io.ts` exits 0 (NOT edited); `git diff --quiet hooks/guard.ts` exits 0.
    - `25-08-GREEN-proof.txt` records the post-fix DENY rows (each form, each compound, the D-01 self-set, the dial-`all` leak) and the ALLOW control rows, extending the 25-07 row format and referencing the committed `.js`.
  </acceptance_criteria>
  <done>The child-spawn oracle exhaustively covers the round-5 structural-shape classes vs the committed `.js`: every expansion / rewrite / non-node-launcher / shebang form DENIES, the nested wrapper+expansion compounds + the D-01 glob self-set + the dial-`all` leak DENY, and the recognized-non-admit-launcher / context-io-validate / no-anchor-unrecognized / routine-only / inert over-block controls ALLOW; the round-1..4 cases hold, the GREEN-proof rows are captured — context-io.ts + guard.ts byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 25-08-03: Anti-whack-a-mole prove-final-literal property/fuzz with an INDEPENDENT shell-special token generator (oracle = tokenIsFinalLiteral) + complete the RED->GREEN proof set (D-12)</name>
  <files>scripts/floor-invariance.test.ts, 25-08-RED-baseline.txt, 25-08-GREEN-proof.txt</files>
  <read_first>
    - scripts/floor-invariance.test.ts:1-75 (the four-invariant structure + the FROZEN_GUARD_BLOB at :60 + the child-spawn-vs-committed-`.js` composition — EXTEND this)
    - scripts/floor-invariance.test.ts:317-360 (the `hookDecision(cmd, dir)` spawn helper + `projectWith({ dial, corrupt, absent })` fixture — REUSE these for the round-5 fuzz; do not reimplement)
    - scripts/floor-invariance.test.ts:407-505 (the round-2 SC1 anti-whack-a-mole class-invariant block — the ROW/loop shape to mirror; do NOT duplicate it)
    - scripts/floor-invariance.test.ts:589-879 (the round-2 + round-3 + round-4 command-RESOLUTION fuzz blocks — the round-5 fuzz ADDS the prove-final-literal dimension with an INDEPENDENT generator; do NOT duplicate the prior matrices)
    - hooks/admission-guard.ts:455-505, 574-686 (the post-fix `tokenIsFinalLiteral` + `segmentAdmitDisposition` from Task 25-08-01 — the fuzz asserts the CLASS invariant over this authority; the fuzz's GENERATOR must be independent of the detector's recognized character set)
    - .planning/phases/25-governance-on-a-dial/25-07-GREEN-proof.txt (the GREEN-proof ROW FORMAT and the existing anti-whack-a-mole block this task's 25-08-GREEN-proof.txt extends)
    - .planning/phases/25-governance-on-a-dial/25-CONTEXT.md:147-160 (D-12 — green-suite-insufficient; the anti-enumeration property-fuzz requirement)
  </read_first>
  <behavior>
    - INDEPENDENT generator (the WARNING-5 fix): the fuzz generates candidate script/verb tokens from bash's ACTUAL rewrite grammar / "any token containing a shell-special char or requiring de-quoting", NOT from the detector's recognized characters. The generator MUST emit, among others: brace `{a,b}` / `admi{t,x}`, tilde `~/x`, process-substitution `<(echo x)`, ANSI-C `$'…'`, quote-removal forms (`ad''mit`, `"admit"`, `scripts/'context-io'.js`, `adm\\it`), mid-word substitution (`a$(echo b)c`, `` a`echo b`c ``), and tokens seeded with RANDOMIZED shell-special / non-alphanumeric characters (so a special char the detector's author did not list is still generated). Each generated non-literal token placed in script-or-verb position MUST gate (or be correctly classified), without that character being individually whitelisted.
    - Class invariant (the structural proof, oracle = `tokenIsFinalLiteral`): for EVERY generated variant — launcher {node|npx|tsx|nodejs|bun|bunx|deno run|ts-node|shebang} × generated token × {script-pos, verb-pos, note-pos} × dial {off|high-severity|all} × admit-ordering — assert: if the hook cannot statically prove the script/verb token is its final literal form AND non-admit, it gates; a recognized launcher with a provably-literal non-context-io script (`node build $HOME/out`, `node <generated-literal-non-context-io>`) ALLOWs; an unrecognized command with no concrete context-io/admit literal anchor (`cp <gen> <gen>` where neither is literal-context-io/admit) ALLOWs.
    - Note-position rewrite: a brace/glob/param in the NOTE position behind a literal context-io admit → gate (note unreadable → fail-closed).
    - xargs stdin→argv fuzz: `<admit-text> | xargs <launcher>` (each launcher, each separator-into-xargs) on a gated high-severity admit → DENY; `echo build | xargs node` → ALLOW.
    - Dial dimension: under `all` a routine admit delivered by ANY generated rewrite → DENY; under `off` → ALLOW.
    - Admit-ordering fuzz: for each separator × ordering (2 and 3 admits, the rewrite-obfuscated high in any position) under `high-severity`, assert DENY whenever ANY segment is a rewrite-obfuscated high-severity admit; a routine-only multi-admit ALLOWs.
    - The four named floor invariants (refuse-self, no-fabrication/never-rewrite, `quality.test_integrity` no-`off`, `hooks/guard.ts` byte-frozen) still hold at every value (preserve the existing assertions; do not weaken them). The round-2 + round-3 + round-4 sweeps stay green.
  </behavior>
  <action>
    Extend `scripts/floor-invariance.test.ts` (do NOT duplicate the round-2/3/4 fuzz matrices — ADD the prove-final-literal dimension with an INDEPENDENT generator). Reuse the existing `hookDecision(cmd, dir)` child-spawn-vs-committed-`.js` helper and the `projectWith({ dial })` temp-config + temp-note fixtures.

    Add a `nonLiteralTokens()` generator (the WARNING-5 fix): it emits tokens carrying a shell-special character or requiring de-quoting, sourced from bash's rewrite grammar — brace, tilde, process-substitution `<(…)`, ANSI-C `$'…'`, quote-removal (`ad''mit`, `"admit"`, single-quoted-mid-path, escaped `adm\\it`), mid-word substitution, AND a sweep seeding RANDOMIZED non-alphanumeric / shell-special characters — INDEPENDENT of the detector's recognized character set (so a char the detector author missed is still generated). Assert that each generated non-literal token in script-or-verb (or note) position gates, and that the fully-static literal forms classify correctly (a quoted admit de-quotes to an admit → gate; a literal non-context-io script → allow).

    Add the launcher × generated-token × position × dial × admit-ordering sweep asserting the class invariant ("if the hook cannot statically prove the script/verb token is its final literal form AND non-admit, it gates"). Add the xargs stdin→argv sweep, the dial-`all` routine-via-rewrite sweep, and the separator × ordering (2/3-admit) sweep.

    Make the structural argument explicit in a comment: the gate fires on the ABSENCE of a final-literal proof, not on a recognized expansion set — so a rewrite form outside any anchor (a future bash expansion, a randomized special char) is caught by the invariant. The proof is the class invariant with an INDEPENDENT generator, not an enumeration of the detector's own triggers (the precise WARNING-5 failure mode the round-5-v1 fuzz would have hit).

    COMPLETE the proof artifacts: finalize `25-08-RED-baseline.txt` (pre-fix committed `.js` blob 756ce508… at HEAD f5beb06 — the observed pre-fix decision per round-5 form, the ALLOWing rows being the bypasses; the literal-path control DENIES) and `25-08-GREEN-proof.txt` (post-fix committed `.js` DENIES each form + compound + the D-01 self-set + the dial-`all` leak; the recognized-non-admit-launcher / context-io-validate / no-anchor-unrecognized / non-admit-wrapper / routine-only / inert / zero-config still ALLOW; the round-1..4 closures still DENY/GATE/ALLOW). Extend the 25-07 row format; reference the committed `.js`, never the `.ts`.

    Voice: CLEAR PROFESSIONAL VOICE in comments (this test owns the SC1 floor). Per D-12, a GREEN fuzz is NECESSARY-NOT-SUFFICIENT — the independent red-team at Task 25-08-04 is the gate; state that in a comment.

    Comment-text discipline: no fenced code blocks in this action; describe the generator + matrix + class invariant by concept.
  </action>
  <verify>
    <automated>npx vitest run scripts/floor-invariance.test.ts && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/floor-invariance.test.ts` is GREEN with the prove-final-literal fuzz added: the INDEPENDENT `nonLiteralTokens()` generator emits brace / tilde / process-sub / ANSI-C / quote-removal / mid-word / randomized-special-char tokens, and EVERY one placed in script-or-verb position on a gated high-severity un-approved admit DENIES via the committed `admission-guard.js` — without that character being individually whitelisted in the detector.
    - A fully-static quoted admit de-quotes to an admit and DENIES; a recognized launcher with a provably-literal non-context-io script (incl. `node build $HOME/out` and a generated literal-non-context-io script) ALLOWs; an unrecognized command with no concrete anchor (`cp <gen> <gen>` with neither literal-context-io/admit) ALLOWs.
    - The xargs stdin→argv fuzz is GREEN (`… | xargs <launcher>` on a gated admit DENIES; `echo build | xargs node` ALLOWs); the dial-`all` routine-via-rewrite DENIES; every {separator × ordering, 2/3 admits} under `high-severity` with a rewrite-obfuscated high-severity segment in any position DENIES; a routine-only multi-admit ALLOWs.
    - The four named floor invariants still hold at every value; the round-2 + round-3 + round-4 sweeps stay green; `git diff --quiet hooks/guard.ts` and `git diff --quiet scripts/context-io.ts` exit 0.
    - `25-08-RED-baseline.txt` and `25-08-GREEN-proof.txt` are complete (each form + the compounds + the D-01 self-set + the dial-`all` leak: pre-fix observed decision with the literal-path control DENY, post-fix DENY; recognized-non-admit-launcher / no-anchor-unrecognized / routine-only / inert / zero-config ALLOW) and reference the committed `.js`, not the `.ts`, extending the 25-07 row format.
  </acceptance_criteria>
  <done>An anti-whack-a-mole prove-final-literal property fuzz with an INDEPENDENT shell-special token generator proves the detector's class invariant structurally (a script/verb token not provably final-literal-and-non-admit gates; a recognized non-admit launcher / no-anchor unrecognized command ALLOWs; a brace/tilde/process-sub/ANSI-C/quote-removal/randomized-char token is caught without being whitelisted); the round-2 + round-3 + round-4 sweeps + four floor invariants stay green; the RED->GREEN proof artifacts are captured vs the committed `.js`; guard.ts + context-io.ts byte-unchanged — the SC1 author proof is complete (independent red-team still the gate, D-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 25-08-04: SC1 round-5 closure gate — INDEPENDENT both-angle red-team vs the committed .js (HARD STOP; green suite is NOT proof, D-12)</name>
  <read_first>
    - .planning/phases/25-governance-on-a-dial/25-07-SUMMARY.md (the "Checkpoint 25-07-04 — INDEPENDENT both-angle red-team" section that FOUND this round-5 class — the exact confirmed-bypass table, the converged root cause; the bar this checkpoint must clear)
    - .planning/phases/25-governance-on-a-dial/25-08-RED-baseline.txt and 25-08-GREEN-proof.txt (the both-direction proof the independent probe reproduces vs the committed `.js`)
    - hooks/admission-guard.js (the COMMITTED artifact the probe attacks — never the `.ts`, never the author suites); scripts/context-io.js (the imported parse the hook uses — UNCHANGED this round, GAP-C/GAP-D + GOV-02 ledger preserved)
  </read_first>
  <what-built>The round-5 gap-closure STRUCTURAL admit-SHAPE detector for the GOV-01 un-forgeable tier, built on the prove-final-literal INVERSION (the plan-check correction): the shape detection inside the ONE `liveAdmitSegments` authority now fails CLOSED unless it can PROVE a script/verb token is already its final literal form (`tokenIsFinalLiteral`: de-quoted value free of `* ? [ ] { } ~ < >`, raw source free of `$`/backtick, not the DYNAMIC_COMMAND_WORD sentinel, not stdin-fed); the disposition is SCRIPT-anchored (`segmentAdmitDisposition`: provably-literal-non-context-io script → ALLOW; provably-literal-context-io script → gate on an admit-or-unresolvable verb; unresolvable script → gate); the structural test applies to ANY command word (recognized launcher, unrecognized bun/deno/ts-node, shebang), never the literal substring; the ONE detector feeds the launcher branch, the round-4 catch-all, and the D-01 floor (Tasks 25-08-01/02); plus the anti-whack-a-mole property fuzz with an INDEPENDENT generator + RED->GREEN proof artifacts (Task 25-08-03). `scripts/context-io.ts` is UNCHANGED. This is the LAST task of the round-5 gap-closure plan. Per [[grugops-safety-invariant-green-suite-insufficient]] and D-12, a passing suite is necessary but NOT sufficient — the author's suites passed and STILL missed a class across rounds 1, 2, 3, the round-3 fix, and the round-4 fix (this is the 9th green-suite-insufficient catch of v2.0; the round-5-v1 plan was itself caught ON PAPER by an independent plan-check for the brace / non-node-launcher / over-block-contradiction holes). This checkpoint HARD-STOPS until an INDEPENDENT opus-grade adversary (not the authoring executor's own suites) has reproduced the closure. Do NOT declare closure on a green suite alone. The orchestrator (not the authoring executor) dispatches the red-team.</what-built>
  <how-to-verify>
    An INDEPENDENT opus-grade probe must run BOTH angles (the P23 split — guard LOGIC and guard INPUT SURFACE are different blind spots; across the five prior rounds each angle found a class the other missed, so BOTH are required) and reproduce ALL of the following against the COMMITTED artifacts (`hooks/admission-guard.js`; `scripts/context-io.js` for the imported parse), never the `.ts` and never the author's own `admission-guard.test.ts` / `floor-invariance.test.ts`:
    1. INPUT-SURFACE angle — the rewrite forms DENY a gated high-severity un-approved admit: glob (`node scripts/context-i*.js admit …`), BRACE (`node <ctx> admi{t,x} …`), quote-removal (`node scripts/'context-io'.js admit …`, `node <ctx> ad''mit …`, `node <ctx> "admit" …`, `node <ctx> adm\\it …`), tilde, process-sub, AND the fully-static quoted admit (`node "scripts/context-io.js" "admit" …` de-quotes to an admit → DENY). AND the bounded over-block direction is preserved: a recognized non-admit launcher (`node build`, `node build $HOME/out`, `node scripts/context-io.js validate`), an unrecognized command with no concrete anchor (`cp $SRC $DST`), and an inert heredoc/quoted/`#`-comment admit mention behind a wrapper → ALLOW. AND the adversary INVENTS at least one input-surface rewrite the author did NOT enumerate (e.g. a brace in the NOTE-path, a process-substitution script, an ANSI-C `$'…'` verb, a randomized shell-special char in script position) — caught structurally (gated or denied), proving the detector fails closed on the absence of a final-literal proof, not on an anchor list.
    2. LOGIC angle — the deferred-argv forms DENY: arg-position command substitution (`node $(echo … admit …)`, backtick), parameter-expansion/word-split (`node "$S" admit …`, `node <ctx> "$V" …`, `$A`), cross-segment stdin→argv (`echo "…admit…" | xargs node`), AND the non-node launchers (`bun scripts/context-i*.js admit …`, `deno run <ctx> admit …`, `ts-node <ctx> admit …`) and the shebang (`<ctx> admit …`). The compounding closures: the D-01 glob self-set DENIES even with the var in env; under `human_admission: all` a routine admit via ANY rewrite DENIES; the nested wrapper+expansion compounds DENY; a routine-only multi-admit ALLOWs. AND the adversary INVENTS at least one logic rewrite the author did NOT enumerate (e.g. a non-node runner behind a wrapper, a 3-admit ordering shielding a rewrite-obfuscated high-severity behind two routines, an xargs behind a subshell).
    3. BOTH-DIRECTION RED-vs-committed-`.js`: the clean case (routine / hook-disposed admit, static path) ALLOWs; the zero-config absent lean reads `off` and ALLOWs a routine admit; each planted round-5 form + the compounds + the D-01 self-set + the dial-`all` leak DENIES; the byte-identical literal-path control DENIES (the gate fires on the direct form and is no longer defeated by a rewrite). Confirm against `25-08-RED-baseline.txt` and `25-08-GREEN-proof.txt`. The probe reproduces these independently, not from the author's suite.
    4. PRESERVED + FLOOR + freshness: the round-4 closures still hold (sub-roots a/b/c + dial-`all` wrapper leak + Class-E wrapper reopening + the sh -c tail DENY); the round-3 closures hold (Class A path-form launcher DENIES; Class B `$()`/backtick COMMAND-WORD GATES; Class E bare multi-admit DENIES while routine-only ALLOWs; Class F duplicate/indented `by` DENIES); the round-2 closures hold (GAP-B self-set behind a wrapper DENIES; GAP-C non-string/corrupt dial gate-or-stricter while absent stays lean; GAP-D case-variant `by` gated high-severity); the four named floor invariants hold at every dial value (refuse-self, no-fabrication/never-rewrite, `quality.test_integrity` has no `off`, `hooks/guard.ts` byte-frozen via `git diff --quiet hooks/guard.ts` exit 0 and blob `3501810e21308e4b7e219679a6ca30dace9b5d66`); `git diff --quiet scripts/context-io.ts` exit 0 (not edited); `npm run freshness` exits 0; WR-01 did not false-positive; the single-authority structural assertion holds (`function tokenize` / `noteFileFromCommand` gone; `COMMAND_MODIFIERS` / `LAUNCHERS` not widened; the literal-substring scan no longer the gating path; `liveTokens` byte-identical).
    5. The probe's findings (the exact reproduced commands, the invented rewrite forms for BOTH angles, the deny/allow outcomes, the recognized-non-admit-launcher / no-anchor-unrecognized / inert controls, the disclosed bounded over-block + residual probed, the preserved round-1..4 results, and the dial-`all`/context-io.ts decision corroboration) are DOCUMENTED in `25-08-SUMMARY.md` — reproduced by an independent adversary, NOT self-reported as a green suite.
  </how-to-verify>
  <acceptance_criteria>
    - (a) Each input-surface rewrite form (glob / brace / quote-removal / tilde / process-sub / fully-static quoted admit) DENIES + an adversary-invented fresh input-surface rewrite DENY vs the committed `admission-guard.js`; the recognized-non-admit-launcher / `node …validate` / no-anchor-unrecognized / inert mention ALLOW — reproduced by an INDEPENDENT input-surface probe, not the author's suite (D-12, P23 input-surface angle).
    - (b) Each logic form (arg-cmd-sub / param / xargs / non-node launcher / shebang) DENIES; the D-01 glob self-set DENIES; the dial-`all` routine-via-rewrite DENIES; the nested wrapper+expansion compounds DENY; a routine-only multi-admit ALLOWs; + an adversary-invented fresh logic rewrite — reproduced by an INDEPENDENT logic probe (D-12, P23 logic angle).
    - (c) The both-direction proof matches `25-08-RED-baseline.txt` (pre-fix observed decision, literal-path control DENY) → `25-08-GREEN-proof.txt` (post-fix DENY; recognized-non-admit-launcher / no-anchor-unrecognized / routine-only / inert / zero-config ALLOW), reproduced vs the committed `.js`.
    - (d) The round-4 (sub-roots a/b/c + dial-`all` wrapper + Class-E wrapper + sh -c tail) + round-3 (Class A/B/E/F) + round-2 (GAP-B/C/D) closures still hold; the four floor invariants hold; `git diff --quiet hooks/guard.ts` exits 0; `git diff --quiet scripts/context-io.ts` exits 0; `npm run freshness` exits 0; WR-01 no false-positive; the single-authority structural assertion holds (second walk deleted, `COMMAND_MODIFIERS`/`LAUNCHERS` not widened, literal-substring not the gating path, `liveTokens` byte-identical); findings documented in `25-08-SUMMARY.md` — independently reproduced, not self-reported.
  </acceptance_criteria>
  <action>
    HARD STOP — do NOT auto-pass on a green author suite (the exact D-12 / green-suite-insufficient failure mode this phase already hit FIVE times, and the round-5-v1 plan was itself caught ON PAPER for the brace / non-node-launcher / over-block-contradiction holes). Dispatch (or hand off to the human to dispatch) an INDEPENDENT opus-grade red-team — an adversary with different blind spots than the authoring executor — to reproduce (a)-(d) against the COMMITTED `hooks/admission-guard.js` and `scripts/context-io.js` (never the `.ts`, never the author's own suites). The probe MUST run BOTH a LOGIC probe (arg-position cmd-sub / param / xargs / non-node launcher / shebang / the D-01 glob self-set / the dial-`all` routine-via-rewrite / a multi-admit shield) AND an INPUT-SURFACE attack (a glob/brace/quote-removal/tilde/process-sub script-or-verb escaping the structural detector, or an inert mention behind a wrapper reading as a live admit). The adversary should INVENT at least one rewrite form per angle the author did NOT enumerate (test the prove-final-literal inversion on a rewrite outside any anchor — e.g. a brace in the note-path, a process-substitution script, an ANSI-C `$'…'` verb, a non-node runner behind a wrapper, a 3-admit ordering shielding a rewrite-obfuscated high-severity, a randomized shell-special char) and confirm it is caught structurally. Record the reproduced commands, the invented forms, the clean=allow / planted=deny outcomes, the recognized-non-admit-launcher / no-anchor-unrecognized / inert controls, the disclosed bounded over-block (`node $SCRIPT build`) + the residual (`bun $S $V`) probed, the preserved GAP-B/C/D + Class A/B/E/F + round-4 a/b/c results, the dial-`all`/context-io.ts-unchanged decision corroboration, and confirm guard.ts + context-io.ts byte-unchanged + freshness 0 + the second walk deleted + `COMMAND_MODIFIERS`/`LAUNCHERS` not widened + `liveTokens` byte-identical in `25-08-SUMMARY.md`. Then surface the result to the human for the resume signal. This task is the phase safety floor — it blocks until the independent both-angle proof is in hand.
  </action>
  <verify>
    <human-check>An independent opus-grade red-team has reproduced (a)-(d) vs the committed `.js`: each input-surface rewrite (glob / brace / quote-removal / tilde / process-sub / fully-static quoted admit) DENIES, each logic form (arg-cmd-sub / param / xargs / non-node launcher / shebang) DENIES, the D-01 glob self-set DENIES, the dial-`all` routine-via-rewrite DENIES, the nested wrapper+expansion compounds DENY (+ an invented rewrite per angle), while the recognized-non-admit-launcher / `node …validate` / no-anchor-unrecognized (`cp $SRC $DST`) / inert mention behind a wrapper ALLOW; the both-direction proof matches the RED→GREEN artifacts (literal-path control DENY); the round-1..4 closures + the four floor invariants hold with `git diff --quiet hooks/guard.ts` exit 0, `git diff --quiet scripts/context-io.ts` exit 0, freshness 0, the second walk deleted, `COMMAND_MODIFIERS`/`LAUNCHERS` not widened, the literal-substring not the gating path, and `liveTokens` byte-identical; findings recorded in `25-08-SUMMARY.md`. NOT a self-reported author green suite (D-12).</human-check>
  </verify>
  <resume-signal>Type "approved" once the independent both-angle red-team has reproduced (a)-(d) above and the findings (incl. the invented rewrite forms for BOTH angles) are recorded in 25-08-SUMMARY.md, or describe the bypass / false-positive found. Do NOT approve on a green author suite alone (D-12, the terminal project lesson — this phase already hit it FIVE times).</resume-signal>
  <done>An independent opus-grade red-team (BOTH logic and input-surface angles) has reproduced the round-5 closure vs the committed `.js`: every expansion / rewrite (glob/brace/quote-removal/tilde/process-sub) / non-node-launcher / shebang form is DENIED, the nested wrapper+expansion compounds + the D-01 glob self-set + the dial-`all` routine-via-rewrite are DENIED, an invented un-enumerated rewrite per angle is caught structurally, the recognized-non-admit-launcher / no-anchor-unrecognized / inert mentions ALLOW, the round-1..4 closures + the four floor invariants + guard.ts/context-io.ts byte-frozen status hold, the second walk is deleted, `COMMAND_MODIFIERS`/`LAUNCHERS` not widened, `liveTokens` byte-identical, findings are documented in `25-08-SUMMARY.md`, and the human approved; SC1 is closed by an independent adversary, not a green suite (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bash `tool_input.command` → admission-guard hook | The command string is untrusted and runs through **bash** (not POSIX sh). After 25-07 made command-word RESOLUTION structural, the agent can still defer the SCRIPT-ref or the admit VERB to bash's runtime REWRITE — glob, parameter/command/arithmetic/process/ANSI-C substitution, brace, tilde, word-split, quote-removal, a non-node runner (bun/deno/ts-node), a shebang, or a cross-segment stdin→argv feed — so the pre-rewrite token carries no literal `context-io`/`admit` and the literal shape detector reads "no admit." The hook must require PROOF of a final literal: a script/verb token not provably its final literal form is UNRESOLVABLE → fail CLOSED, as ONE detector applied to ANY command word, anchored on the script position. The shell, not the hook, forms the final argv. |
| script-position token → SCRIPT-anchored disposition | A provably-literal-non-context-io script can NEVER become a context-io admit (a later rewrite cannot retroactively make argv[script] context-io) → ALLOW (`node build $HOME/out`). A provably-literal-context-io script gates on an admit-or-unresolvable verb. An unresolvable script gates. Launcher recognition is used ONLY to AVOID over-blocking; an unrecognized command word requires a concrete context-io/admit literal anchor to gate, bounding over-block (`cp $SRC $DST` ALLOWs). |
| nested wrapper + rewrite → round-4 catch-all | A wrapper in FRONT of a rewrite-obfuscated launcher must not launder the shape: the round-4 catch-all consults the SAME structural detector (unrecognized-command-word scan), so the compound gates and the literal-substring fallback is eliminated (BLOCKER 2). |
| inline approval-var set → refuse-self-set (D-01 floor) | `commandHasLiveAdmitShape` derives from the one `liveAdmitSegments` authority, so a self-set co-occurring with a structurally-detected admit-or-unresolvable segment is refused even behind a glob/param spelling (re-closing the round-5 glob-floor aggravator). |
| governance config value → hook / admit() | (PRESERVED from 25-05, context-io.ts unchanged) A present non-string / non-object / corrupt config is gate-or-stricter; only exact JSON `"off"` is off-equivalent; absent stays lean. |
| admit() → audit ledger | (Preserved from 25-03, unchanged here) Under `retained`, the ledger records the admission record only, never the note body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-31 | Spoofing / Elevation (script-ref rewrite: glob / brace / tilde / process-sub / quote-removal) | a gated admit whose SCRIPT-ref is rewritten by the shell (`node scripts/context-i*.js admit`, `node <ctx> admi{t,x}`, `node ~/x/context-io.js admit`, `node <(…) admit`, `node scripts/'context-io'.js admit`) the literal substring test never matched | high | mitigate | Prove-final-literal inversion: a script/verb token not provably its final literal form (`tokenIsFinalLiteral`: de-quoted value free of `* ? [ ] { } ~ < >`, raw free of `$`/backtick, not the DYNAMIC_COMMAND_WORD sentinel) is UNRESOLVABLE → gate; a provably-literal-context-io script with an admit-or-unresolvable verb → gate. No enumeration of what expands. Proven by RED vs the committed `.js` + the independent-generator fuzz (Task 25-08-01/02/03; BLOCKER 1 brace included; D-12). |
| T-25-32 | Spoofing / Elevation (argument-position command substitution) | a gated admit whose argv is produced by an argument-position `$(…)` / backtick (`node $(echo … admit …)`) | high | mitigate | The synthetic DYNAMIC_COMMAND_WORD token in argument position fails `tokenIsFinalLiteral` (sentinel + raw `$`/backtick) → unresolvable script → gate. Proven by the class fuzz + RED vs committed `.js` (Task 25-08-01/02/03; D-12). |
| T-25-33 | Spoofing / Elevation (parameter-expansion / word-split) | a gated admit whose script (`node "$S" admit`) or verb (`node <ctx> "$V" …`, unquoted `$A`) is a `$`-parameter expanded at runtime | high | mitigate | A `$` in the token's raw source → not final-literal → unresolvable script/verb → gate; a literal `admit` verb behind a dynamic script → gate. Proven by the param fuzz + RED vs committed `.js` (Task 25-08-01/02/03; D-12). |
| T-25-34 | Spoofing / Elevation (cross-segment stdin→argv) | a gated admit whose launcher argv is sourced from another segment's stdout via xargs (`echo "…admit…" \| xargs node`) | high | mitigate | `STDIN_ARGV_FEEDERS` (xargs) — a DISTINCT structural concept, NOT a `COMMAND_MODIFIERS` widening: a launcher reached behind a feeder has a stdin-sourced (unresolvable) script → gate. Proven by the xargs fuzz + RED vs committed `.js` (Task 25-08-01/02/03; D-12). |
| T-25-35 | Spoofing / Elevation (non-node launcher / shebang — the literal-substring fallback hole) | a gated admit launched by an UNRECOGNIZED runner (`bun`/`bunx`/`deno run`/`ts-node scripts/context-i*.js admit`) or a SHEBANG script (`scripts/context-io.js admit`) where the round-4 catch-all fell back to the LITERAL `v.includes("context-io")` + token `admit` substring scan | high | mitigate | The structural detector applies to ANY command word — the round-4 catch-all + `commandHasLiveAdmitShape` use `segmentAdmitDisposition` (the unrecognized-command structural scan, with the shebang command word as a script candidate), never the literal substring. Proven by the non-node-launcher + shebang cases + RED vs committed `.js` (Task 25-08-01/02/04; BLOCKER 2 + WARNING 4; D-12). |
| T-25-36 | Elevation of Privilege (D-01 refuse-self-set floor breach via rewrite) | a self-set co-occurring with a rewrite-obfuscated admit (`GRUGOPS_ADMISSION_APPROVED_BY=eve node …context-i*.js admit`) bypassing the floor because `commandHasLiveAdmitShape` keyed the literal shape | critical | mitigate | `commandHasLiveAdmitShape` derives from the one `liveAdmitSegments` authority, so a self-set with a structurally-detected admit-or-unresolvable segment is refused even behind a glob/param spelling. Proven by the D-01 glob-self-set case + RED vs committed `.js` (Task 25-08-01/02; D-12). |
| T-25-37 | Elevation of Privilege (dial-`all` backstop gap) | under `all` a routine admit via ANY rewrite ALLOWing with no in-script backstop (`admit()` refuses only high-severity roles) | critical | mitigate (hook tier) / accept (no context-io.ts change) | The structural detector re-closes the routine-via-rewrite case at the un-forgeable hook tier under `all`. `scripts/context-io.ts` stays byte-frozen: the self-settable D-05 `admit()` tier adds no un-forgeable defense (an agent that defeats the hook controls the admit() process), and keeping it unchanged preserves GAP-C/GAP-D + the GOV-02 ledger byte-identically. Proven by the dial-`all` routine-via-rewrite case (Task 25-08-01/02/03; D-12). |
| T-25-38 | Spoofing / Elevation (nested wrapper + rewrite compound — the round-6 preventer) | a wrapper in FRONT of a rewrite-obfuscated launcher (`timeout 5 node …context-i*.js admit`, `nice -n 5 bun <ctx> admit`) laundering the shape past the round-4 catch-all | high | mitigate | The round-4 INVERT catch-all consults the SAME `segmentAdmitDisposition`, so a rewrite-obfuscated admit behind a wrapper-operand/redirection is gated. Proven by the compound cases + the wrapped-launcher fuzz + the red-team's invented nested rewrite (Task 25-08-01/02/03/04; D-12). |
| T-25-39 | Denial of Service (over-block regression / internal contradiction — BLOCKER 3) | gating a proven-non-admit launcher (`node build $HOME/out`) or non-launcher data (a false-positive) under a vague "unresolvable token at-or-before the verb → gate" rule | high | mitigate (fail-safe) | The SCRIPT-anchored disposition allows a provably-literal-non-context-io script REGARDLESS of later tokens (`node build $HOME/out` → script `build` ≠ context-io → ALLOW), gates a context-io script on an admit-or-unresolvable verb, and gates an unresolvable script; an unrecognized command requires a concrete literal anchor (`cp $SRC $DST` → ALLOW). Every documented control is consistent with this algorithm (asserted in the suite + fuzz). |
| T-25-40 | Tampering (single-authority drift / enumeration regression / the bounded residual) | reintroducing a second parser, widening `COMMAND_MODIFIERS`/`LAUNCHERS`, re-enumerating expansion spellings, or the bounded over-block / residual | medium | mitigate (drift) / accept (residual) | The detector lives in the ONE `liveAdmitSegments` walk (`function tokenize`/`noteFileFromCommand` grep=0; `liveTokens` byte-identical; sets unchanged); the proof is the prove-final-literal invariant with an INDEPENDENT generator, not an anchor list. The disclosed residual — an unrecognized runner with BOTH script AND verb rewrite-hidden and no literal anchor (`bun $S $V`; the recognized `node $S $V` still gates) — and the bounded over-block (`node $SCRIPT build` under active governance) are ACCEPTED and documented; the red-team probes both. |
| T-25-SC | Tampering | npm/pip/cargo installs | high | accept | N/A — this gap-closure installs NO external package (RESEARCH Package Legitimacy Audit: not applicable; typescript/vitest already present). No install task exists. |
</threat_model>

<verification>
- `npm run build && npm run freshness` exits 0 (committed `admission-guard.js` is a fresh, byte-faithful tsc build; 0 drift).
- `git diff --quiet hooks/guard.ts` exits 0 (deploy guard byte-frozen, blob `3501810e21308e4b7e219679a6ca30dace9b5d66`, D-02).
- `git diff --quiet scripts/context-io.ts` exits 0 (NOT edited this round — GAP-C/GAP-D + the GOV-02 audit ledger preserved; the un-forgeable hook re-closes the dial-`all` rewrite case so no in-script backstop is needed).
- `npx vitest run hooks/admission-guard.test.ts` green: every round-5 form (glob / arg-cmd-sub / param / xargs / brace / quote-removal / non-node launcher / shebang) DENIES; the nested wrapper+rewrite compounds DENY; the D-01 glob self-set DENIES; the dial-`all` routine-via-rewrite DENIES; the over-block controls (recognized non-admit launcher, `node build $HOME/out`, `node …validate`, no-anchor unrecognized `cp $SRC $DST`, non-admit wrapper, routine-only multi-admit, inert mention behind a wrapper) ALLOW; the round-1..4 closures still hold; case count >= guard.test.ts.
- `npx vitest run scripts/floor-invariance.test.ts` green: the prove-final-literal property fuzz with an INDEPENDENT shell-special token generator (brace/tilde/process-sub/ANSI-C/quote-removal/mid-word/randomized chars); the round-2 + round-3 + round-4 sweeps; the four named floor invariants hold at every value.
- A grep asserts `function tokenize` and `noteFileFromCommand` no longer exist in hooks/admission-guard.ts (single resolution authority), `COMMAND_MODIFIERS` and `LAUNCHERS` membership are unchanged (no widening), the literal-substring admit-shape scan is no longer the gating path on the launcher branch / round-4 catch-all, and the `liveTokens` tokenizer body is unedited.
- `npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial; D-13 no kit-content false-positive).
- `npx vitest run --exclude '**/scripts/e2e/**'` green (full non-e2e regression — bare `npm test` deliberately NOT run; it triggers the live claude-CLI e2e lane, see MEMORY grugops-npm-test-triggers-live-e2e).
- `25-08-RED-baseline.txt` + `25-08-GREEN-proof.txt` capture the both-direction proof vs the committed `.js` (extending the 25-07 row format; the literal-path control DENIES on both sides).
- INDEPENDENT both-angle red-team (Task 25-08-04, before /gsd-verify-work): confirms SC1 round-5 closure adversarially — not self-reported green (D-12).
</verification>

<success_criteria>
- SC1 (the structural shape-detection class): the GOV-01 human gate is no longer forgeable by deferring the script-ref or the verb to the shell — the admit-shape detector requires PROOF that a script/verb token is already its final literal form (`tokenIsFinalLiteral`) and is SCRIPT-anchored (`segmentAdmitDisposition`: provably-literal-non-context-io script → ALLOW; provably-literal-context-io script → gate on an admit-or-unresolvable verb; unresolvable script → gate), applied to ANY command word (recognized launcher / unrecognized bun-deno-ts-node / shebang) and never the literal substring; so glob/arg-cmd-sub/param/xargs/brace/quote-removal/non-node/shebang + the nested wrapper+rewrite compound + the D-01 glob self-set + the dial-`all` routine-via-rewrite are closed; the ONE detector feeds the launcher branch, the round-4 catch-all, and the floor scan, with `COMMAND_MODIFIERS`/`LAUNCHERS` unwidened, the second walk still deleted, and `liveTokens` byte-identical; the hook stays the only grant path.
- PRESERVED (no regression): the round-4 leading-run closures (sub-roots a/b/c + dial-`all` wrapper leak + Class-E wrapper reopening + the sh -c tail), the round-3 closures (Class A/B/E/F), the round-2 LOGIC closures (GAP-B/C/D), the verified SC2 config foundation + the GOV-02 audit ledger, and the 25-04/25-05/25-06 segmentation are unchanged; `scripts/context-io.ts` is not edited.
- Fail-safe (bounded over-block, consistent with the script-anchored algorithm): a recognized non-admit launcher (`node build`, `node render`, `node build $HOME/out`, `node …validate`), an unrecognized command with no concrete anchor (`cp $SRC $DST`, `tar $A $B`, `git log $REF`), a non-admit wrapper, a routine-only multi-admit under `high-severity`, and an inert mention behind a wrapper all ALLOW; the bounded `node $SCRIPT build` over-block and the disclosed `bun $S $V` residual are explicit and documented.
- Proof (D-12): both-direction RED→GREEN vs the COMMITTED `.js` (captured artifacts; the literal-path control DENIES) + an anti-whack-a-mole property fuzz whose oracle is `tokenIsFinalLiteral` and whose token generator is INDEPENDENT of the detector's recognized characters (brace/tilde/process-sub/ANSI-C/quote-removal/randomized) + an INDEPENDENT both-angle (logic + input-surface) opus-grade red-team that invents an un-enumerated rewrite per angle — not a green author suite. `hooks/guard.ts` + `scripts/context-io.ts` byte-unchanged; freshness 0; the second walk deleted; `COMMAND_MODIFIERS`/`LAUNCHERS` unwidened; `liveTokens` byte-identical.
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-08-SUMMARY.md` when done. Record: the prove-final-literal INVERSION (`tokenIsFinalLiteral` — de-quoted value free of `* ? [ ] { } ~ < >`, raw source free of `$`/backtick, not the DYNAMIC_COMMAND_WORD sentinel, not stdin-fed) and the SCRIPT-anchored disposition (`segmentAdmitDisposition` — provably-literal-non-context-io script → ALLOW; provably-literal-context-io script → gate on an admit-or-unresolvable verb; unresolvable script → gate), how it applies to ANY command word (recognized launcher / unrecognized bun-deno-ts-node / shebang) and replaces the literal substring on the round-4 catch-all + the D-01 floor (so glob/arg-cmd-sub/param/xargs/brace/quote-removal/non-node/shebang + the nested wrapper compound + the D-01 glob self-set + the dial-`all` routine-via-rewrite are closed), and confirmation `COMMAND_MODIFIERS`/`LAUNCHERS` were NOT widened, no second walk was reintroduced (`function tokenize` / `noteFileFromCommand` grep=0), the `liveTokens` tokenizer is byte-identical, and detection requires proof of a final literal (NOT an enumeration of what expands); the explicit/bounded over-block decision (`node $SCRIPT build` gated only under active governance) and the disclosed residual (`bun $S $V` — unrecognized runner with both positions rewrite-hidden and no literal anchor; recognized `node $S $V` gates); the dial-`all`/context-io.ts-unchanged decision with its justification (the un-forgeable hook re-closes the case; the weak D-05 tier adds no un-forgeable defense); confirmation `scripts/context-io.ts` is unedited (`git diff --quiet` exit 0 — GAP-C/GAP-D + ledger preserved); the fuzz's INDEPENDENT generator (brace/tilde/process-sub/ANSI-C/quote-removal/randomized chars, not the detector's own triggers); the RED→GREEN proof artifact paths (the literal-path control DENIES on both sides); confirm `git diff --quiet hooks/guard.ts` clean and freshness 0; and the INDEPENDENT both-angle red-team's reproduced commands + the invented rewrite forms (both angles) + outcomes + the preserved round-4 (a/b/c) + round-3 (Class A/B/E/F) + round-2 (GAP-B/C/D) results. Flag for the verifier that SC1 closure REQUIRES the independent red-team — the sweeps/fuzz are necessary, not sufficient (D-12, this phase's green-suite-insufficient discipline). Note that SC2 + the GOV-02 audit ledger (25-01/02/03), the round-4 closures (25-07), the round-3 closures (25-06), and the round-2 LOGIC closures (25-05) are PRESERVED untouched.
</output>

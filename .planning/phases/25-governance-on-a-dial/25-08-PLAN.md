---
phase: 25-governance-on-a-dial
plan: 08
type: execute
wave: 8
gap_closure: true
depends_on: ["25-01", "25-02", "25-03", "25-04", "25-05", "25-06", "25-07"]
autonomous: false
files_modified:
  - hooks/admission-guard.ts
  - hooks/admission-guard.js
  - hooks/admission-guard.test.ts
  - scripts/floor-invariance.test.ts
  - 25-08-RED-baseline.txt
  - 25-08-GREEN-proof.txt
requirements: [GOV-01, GOV-02]
user_setup: []

must_haves:
  truths:
    - "Prove-final-literal ALLOWLIST inversion / SC1 (GOV-01) — the admit-SHAPE detector requires PROOF a script/verb token is already its final literal form, expressed as an ALLOWLIST of provably-inert characters (NOT a denylist of dangerous characters — the rev-1 denylist `* ? [ ] { } ~ < >` missed extglob `@( +( !(` / history `!` / and any future metachar; a denylist is the same enumeration trap inverted). `tokenIsFinalLiteral(token, cmd)` is true ONLY when (1) the token's DE-QUOTED value consists EXCLUSIVELY of the conservative known-inert set `A-Z a-z 0-9 / . _ - : = ,` (the characters a real `scripts/context-io.js` / absolute or `./` path / the `admit`/`validate`/`render` verb / a task-id / a note path actually need — none of which triggers any bash rewrite as a bare char), (2) its RAW source slice carries no `$` and no backtick (catching a parameter/command/arithmetic/process/ANSI-C substitution the tokenizer consumed mid-word, while NOT over-flagging a balanced quoted literal like `\"admit\"` whose raw has `\"` but no `$`/backtick), and (3) it is not the DYNAMIC_COMMAND_WORD sentinel. ANY character outside the allowlist — extglob `@(`, history `!`, process-sub `<(`, brace `{}`, glob `*?[`, tilde `~`, `$`, backtick, a future/exotic metachar — makes the token NOT provably-literal → UNRESOLVABLE → gate, WITHOUT ever enumerating what expands. De-quoting happens first so a quoted-but-static admit (`\"admit\"`, `scripts/'context-io'.js`) de-quotes to its literal and classifies correctly. (D-01 un-forgeable tier; vs the COMMITTED `.js`)."
    - "extglob closed (BLOCKER-B) / SC1 — `shopt -s extglob; node scripts/context-i@(o).js admit T NOTE` DENIES: the script token (`scripts/context-i@`, the tokenizer splitting at `(`) carries `@`, which is outside the allowlist → not final-literal → UNRESOLVABLE script → gate; bash extglob-expands `context-i@(o).js` → the real `context-io.js`, but the hook never has to recognize extglob — it fails closed because the token is not PROVEN inert. The same allowlist catches `context-i?(o).js`, `context-i+(o).js`, `!(…)`, and any future expansion form with no code change."
    - "Recognized-launcher disposition is SCRIPT-anchored with a POSITION-FREE literal-context-io rule + a hidden-script rule (BLOCKER-A / npx-flag) / SC1 — `segmentAdmitDisposition` for a launcher in LAUNCHERS {node,npx,tsx,nodejs} or JS_RUNNERS {bun,bunx,deno,ts-node}: RULE 1 (position-free, matches the committed whole-segment behavior so it does NOT regress) — if any post-launcher token is a final-literal containing `context-io`, inspect the token immediately after it (the verb): final-literal `admit` → ADMIT (note = next final-literal token, else null → fail-closed); UNRESOLVABLE verb → unresolvable (gate); final-literal non-admit (`validate`/`render`) or absent → none (ALLOW). RULE 2 (no literal context-io anywhere → a HIDDEN script): for DIRECT runners {node,nodejs,tsx} pin the script = first positional after skipping leading flags (incl. a small documented value-flag set `-r --require -e --eval --import --loader -p --print -C --conditions` skipped BY ARITY) and gate if that script token is unresolvable, while a trailing unresolvable ARG after a literal script does NOT gate; for FORWARDING runners {npx} ∪ JS_RUNNERS (which forward to a runner+script at a deep/variable position) any unresolvable token in the post-launcher argv → gate. This closes the rev-1 REGRESSION where naive flag-skipping pinned `npx -p foo tsx scripts/context-io.js admit` to `foo` → ALLOW: RULE 1 finds the literal `context-io` regardless of the `-p foo` flag → DENY."
    - "npx-flag regression closed (BLOCKER-A) / SC1 — `npx -p foo tsx scripts/context-io.js admit T NOTE` (also `-c`, `--package`, `--call`) DENIES because RULE 1 is position-free (the literal `scripts/context-io.js` is found wherever it sits, the value-consuming flag is irrelevant); `node -r ./preload.js scripts/context-io.js admit …` DENIES likewise (RULE 1, literal context-io). The committed code's whole-segment scan caught these; the rev-1 position-pinning had REGRESSED them — RULE 1 restores the catch. `node build $HOME/out` stays ALLOW (RULE 2 direct: script `build` literal-non-context-io; the trailing `$HOME/out` is an arg, not the script)."
    - "JS_RUNNERS capability boundary (T-40) / SC1 — `JS_RUNNERS = {bun, bunx, deno, ts-node}` is a JS-EXECUTION-CAPABILITY set (what can run a context-io admit), EXPLICITLY DISTINCT from `COMMAND_MODIFIERS` (which RESOLVES a command word behind a wrapper — widening THAT would be the closed-enumeration anti-pattern) and from `LAUNCHERS`. A JS_RUNNER is treated as a recognized (forwarding) launcher, so `bun $S $V` DENIES (recognized JS runner, unresolvable script — RULE 2 forwarding) while `cp $A $B` / `tar $A $B` ALLOW (NOT JS runners → the unrecognized-command structural scan finds no concrete context-io/admit literal anchor). The disclosed residual T-25-40 narrows to a genuinely-UNKNOWN runtime (`qjs`, `llrt`, a future runner NOT in LAUNCHERS ∪ JS_RUNNERS) with BOTH positions dynamic and no literal anchor — an explicit ACCEPT with that precondition stated, flagged for the red-team."
    - "Structural detection on ANY command word (BLOCKER-2, preserved) / SC1 — the structural shape test applies to ANY command word — recognized launcher / JS_RUNNER, an unrecognized command, or a shebang script — NEVER the literal substring. The unrecognized-command structural scan gates when adjacent positional tokens (Ti, Ti+1) satisfy [Ti final-literal-context-io OR unresolvable] AND [Ti+1 final-literal-admit OR unresolvable] AND [at least one concrete literal anchor: Ti literally context-io OR Ti+1 literally admit]; the command word itself is a script candidate for the shebang case (`scripts/context-io.js admit …` DENIES — WARNING 4)."
    - "D-01 floor + dial-`all` re-closed at the un-forgeable tier / SC1+GOV-02 — `GRUGOPS_ADMISSION_APPROVED_BY=eve node scripts/context-i*.js admit … <hi>` DENIES even with the var in env (`commandHasLiveAdmitShape` derives from the one `liveAdmitSegments` authority); under `human_admission: all` a routine admit delivered by ANY rewrite (glob, extglob, param, a JS_RUNNER, …) DENIES. `scripts/context-io.ts` stays byte-frozen (the weak self-settable D-05 tier adds no un-forgeable defense; keeping it unchanged preserves GAP-C/GAP-D + the GOV-02 ledger byte-identically)."
    - "Over-block SAFETY (the bounded fail-safe) PROVEN — the gate fires ONLY when RULE 1 finds a literal context-io with an admit-or-unresolvable verb, or RULE 2 finds a hidden/unresolvable script, or the unrecognized scan finds a concrete-anchored admit shape. A direct runner with a provably-literal non-context-io script (`node build`, `node render`, `node build $HOME/out`, `node script.js test`), a recognized launcher running a provably-literal context-io NON-admit (`node scripts/context-io.js validate`), an unrecognized non-JS-runner with no concrete anchor (`cp $SRC $DST`, `tar $A $B`, `git log $REF`), a non-admit wrapper, a routine-only multi-admit under `high-severity`, and an inert heredoc/quoted/`#`-comment admit mention behind a wrapper all ALLOW. The bounded over-block is BROADER for FORWARDING runners: because forwarding RULE 2 gates on ANY unresolvable post-launcher token, common dev commands DENY while `human_admission != off` — `npx vitest run $FILE`, `bun app.js $ARG`, `deno run server.ts $PORT`, `ts-node $SCRIPT`. This breadth is the disclosed, accepted price of the forwarding-runner fail-closed (opt-in; the gate asks a human and never destroys work; a static/direct `node` command avoids it) — deliberately NOT tightened by runner semantics (narrowing 'tsx/ts-node execute their arg but vitest/eslint don't' would be a new closed-enumeration anti-pattern risking a reopened hole). TWO residuals are disclosed (not hidden), in the threat model + 25-08-SUMMARY: (i) an UNKNOWN-runtime forge (`qjs $S $V` — a runner not in LAUNCHERS ∪ JS_RUNNERS, both positions dynamic, no literal anchor) ALLOWs; (ii) a NAME-based forge — a renamed/symlinked copy of context-io.js run under its new name (`cp scripts/context-io.js /tmp/x.js && node /tmp/x.js admit …`) ALLOWs, a PRE-EXISTING name-resolution limit of the substring anchor (not a round-5 regression), out of hook-tier reach. The bounded over-block markers `node $SCRIPT build` / `npx tsx $S admit` gate only under active governance; the forwarding-runner non-admit over-block is asserted as an intended GATED control (Task 25-08-02), not left silent."
    - "Anti-enumeration proof, BASH-GROUNDED (D-12, WARNING-5 / the rev-1 circularity) — the property/fuzz oracle is grounded in ACTUAL BASH BEHAVIOR, INDEPENDENT of `tokenIsFinalLiteral` (the rev-1 fuzz used `tokenIsFinalLiteral` as its own oracle, so it could never catch a gap inside it — it passed extglob). For each generated candidate token the test SPAWNS bash to decide whether bash rewrites/expands it (a `printf '%s\\n'` comparison for brace/tilde/ANSI-C/history/word-split + a glob/extglob probe in a controlled temp dir containing a real `context-io.js` with `shopt -s extglob`), and asserts: if bash would rewrite the token, the hook MUST gate the constructed admit. Candidates span extglob `@(/+(/!(`, history `!`, process-sub `<(`, brace, tilde, ANSI-C `$'`, quote-removal, AND randomized non-alphanumeric chars — grounded in what the shell does, not in the detector's own set. Necessary-NOT-sufficient (D-12); the independent both-angle red-team is the gate."
    - "PRESERVED — every round-1..4 closure stays untouched: round-4 leading-run RESOLUTION (sub-roots a/b/c, the dial-`all` wrapper leak, the Class-E wrapper reopening, the `sh -c` tail) all DENY; round-3 Class A/B/E/F; round-2 GAP-B/C/D. The SINGLE `liveAdmitSegments` authority stays the ONE source of truth — `function tokenize` / `noteFileFromCommand` stay deleted (grep=0); `COMMAND_MODIFIERS` is NOT widened (JS_RUNNERS is a NEW SEPARATE set, not a COMMAND_MODIFIERS widening); `LAUNCHERS` is unchanged; `liveTokens` segmentation is byte-identical (the detector reads de-quoted token values + token start offsets the tokenizer ALREADY emits)."
    - "PRESERVED — SC2 (audit_retention 3-surface lockstep + lean defaults), the GOV-02 audit ledger (`appendAuditLedger`, context-io.ts:979-...), the 25-04/25-05/25-06 segmentation-as-DATA, and the four named floor invariants (refuse-self FAILs at every dial / no-fabrication-never-rewrite / `quality.test_integrity` no `off` / `hooks/guard.ts` byte-frozen blob 3501810e21308e4b7e219679a6ca30dace9b5d66) hold at every dial value. `git diff --quiet scripts/context-io.ts` exits 0 and `git diff --quiet hooks/guard.ts` exits 0; the committed `.js` is a fresh `tsc` build (`npm run freshness` exits 0)."
  artifacts:
    - path: "hooks/admission-guard.ts"
      provides: "The STRUCTURAL admit-SHAPE detector inside the ONE `liveAdmitSegments` authority. `tokenIsFinalLiteral(token, cmd)` — ALLOWLIST-based (rev-2): de-quoted value consists exclusively of `A-Za-z0-9/._:=,-`, raw source free of `$`/backtick, not the DYNAMIC_COMMAND_WORD sentinel; anything else is UNRESOLVABLE (structurally catches extglob/history/process-sub/brace/glob/tilde/$/backtick + any future metachar without enumeration). `segmentAdmitDisposition(tokens, cmdIdx, cmd)` → `admit | unresolvable | none` (+ noteFile): for a launcher in LAUNCHERS ∪ JS_RUNNERS — RULE 1 (position-free literal-context-io → verb check) + RULE 2 (no literal context-io → hidden script: DIRECT runners {node,nodejs,tsx} pin the script after a value-flag-arity skip and gate if unresolvable; FORWARDING runners {npx} ∪ JS_RUNNERS gate on any unresolvable token); for an UNRECOGNIZED command word the concrete-anchor structural scan (shebang = the command word as a script candidate). `JS_RUNNERS = {bun, bunx, deno, ts-node}` — a JS-EXECUTION-CAPABILITY set DISTINCT from `COMMAND_MODIFIERS` (resolves a wrapped command word) and `LAUNCHERS`. `STDIN_ARGV_FEEDERS = {xargs}` → a launcher reached behind a feeder is unresolvable. The launcher branch (:610-615), the round-4 catch-all (:661-668), and `commandHasLiveAdmitShape` (:679-686) all consume this ONE detector — never the literal substring. `COMMAND_MODIFIERS` / `LAUNCHERS` NOT widened; no second walk; `liveTokens` byte-identical."
      contains: "admit"
    - path: "hooks/admission-guard.js"
      provides: "committed tsc build the host hook runs (freshness-covered; rebuilt after the .ts edit)"
    - path: "hooks/admission-guard.test.ts"
      provides: "extended child-spawn oracle vs the COMMITTED admission-guard.js: glob / arg-cmd-sub / param / xargs / brace / EXTGLOB `context-i@(o).js` / quote-removal / tilde / process-sub all DENY; the npx value-flag form `npx -p foo tsx scripts/context-io.js admit` DENIES; `node -r ./preload.js scripts/context-io.js admit` DENIES; the JS_RUNNERS (`bun`/`bunx`/`deno run`/`ts-node` + glob/param, and `bun $S $V`) DENY; the SHEBANG DENIES; the nested wrapper+rewrite compounds DENY; the D-01 glob self-set DENIES; the dial-`all` routine-via-rewrite DENIES; the over-block controls (`node build`, `node build $HOME/out`, `node scripts/context-io.js validate`, `cp $SRC $DST`, `cp $A $B`, non-admit wrapper, routine-only multi-admit, inert mention behind a wrapper) ALLOW; the round-1..4 closures still hold; case count >= guard.test.ts"
      contains: "deny"
    - path: "scripts/floor-invariance.test.ts"
      provides: "the round-5 anti-whack-a-mole property/fuzz whose oracle is ACTUAL BASH BEHAVIOR (NOT `tokenIsFinalLiteral` — the rev-1 circularity fix): a `bashRewrites(token)` helper SPAWNS bash (`printf '%s\\n'` + a glob/extglob probe in a controlled temp dir with a real context-io.js and `shopt -s extglob`) to classify each generated candidate token as stable-or-rewritten, asserting bash-rewritten-token-in-script/verb-position ⇒ the hook gates; the generator emits extglob/history/process-sub/brace/tilde/ANSI-C/quote-removal/randomized-non-alnum tokens × launcher {LAUNCHERS ∪ JS_RUNNERS ∪ shebath} × position × dial × admit-ordering. The round-2/3/4 sweeps + the four named floor invariants are PRESERVED, not duplicated"
      contains: "human_admission"
    - path: "25-08-RED-baseline.txt"
      provides: "captured RED baseline — the PRE-FIX committed `admission-guard.js` (blob 756ce50841e4a5c4955d2197ce08f43e47931659, HEAD f5beb06) ALLOWs each round-5 form (glob / arg-cmd-sub / param / xargs / extglob / quote-removal / JS_RUNNERS bun-deno-ts-node / shebang / npx value-flag / bun $S $V / the nested compounds / the D-01 glob self-set / the dial-`all` leak) while the byte-identical literal-path control DENIES; extending the 25-07 row format and citing the pre-fix blob. [The npx value-flag + node -r forms ALREADY DENY on the committed `.js` via the whole-segment scan — recorded as the regression-guard control the rev-1 position-pinning would have broken.]"
    - path: "25-08-GREEN-proof.txt"
      provides: "captured GREEN proof — the POST-FIX committed `admission-guard.js` DENIES each round-5 form + extglob + npx-value-flag + bun $S $V + the compounds + the D-01 self-set + the dial-`all` leak naming the unresolvable / fail-closed condition; the over-block controls (`node build $HOME/out`, `node …validate`, `cp $SRC $DST`/`cp $A $B`, non-admit wrapper, routine-only multi-admit, inert mention behind a wrapper, zero-config absent lean `off`) still ALLOW; the round-1..4 closures still DENY/GATE/ALLOW — extending the 25-07 row format, referencing the committed `.js`"
  key_links:
    - from: "hooks/admission-guard.ts (segmentAdmitDisposition RULE 1, position-free)"
      to: "a launcher invocation carrying a literal `context-io` script ref behind ANY flag (npx -p / node -r)"
      via: "RULE 1 scans the post-launcher argv for a final-literal context-io token regardless of position, then classifies the immediately-following verb (admit-or-unresolvable → gate) — restoring the committed whole-segment catch the rev-1 position-pinning regressed (BLOCKER A)"
      pattern: "segmentAdmitDisposition"
    - from: "hooks/admission-guard.ts (tokenIsFinalLiteral, the allowlist)"
      to: "a rewrite-obfuscated script/verb token (extglob @(, history !, brace, tilde, $, backtick, future metachar)"
      via: "the de-quoted value must consist exclusively of the inert allowlist `A-Za-z0-9/._:=,-` and the raw must be free of `$`/backtick — anything else is UNRESOLVABLE → gate, with no enumeration of what expands (BLOCKER B)"
      pattern: "tokenIsFinalLiteral"
    - from: "hooks/admission-guard.ts (JS_RUNNERS capability set)"
      to: "a context-io admit launched by a JS runtime (bun/bunx/deno/ts-node), incl. both-positions-dynamic bun $S $V"
      via: "JS_RUNNERS are recognized (forwarding) launchers, so RULE 2 gates an unresolvable script; a non-JS-runner (cp/tar) is NOT in the set → the unrecognized-command scan needs a concrete literal anchor → cp $A $B allows (T-40, distinct from a COMMAND_MODIFIERS widening)"
      pattern: "JS_RUNNERS"
    - from: "scripts/floor-invariance.test.ts (the bash-grounded fuzz oracle)"
      to: "the COMMITTED hooks/admission-guard.js"
      via: "for each generated candidate token, spawn bash to determine if bash rewrites it (independent of the detector), and assert a bash-rewritten script/verb token forces the hook to gate — so a gap INSIDE tokenIsFinalLiteral (e.g. extglob) is caught (WARNING 5 / rev-1 circularity)"
      pattern: "bashRewrites|spawnSync\\(.*bash|admission-guard\\.js"
    - from: "hooks/admission-guard.ts (the deleted second walk, still gone)"
      to: "noteFileFromCommand + the standalone tokenize"
      via: "neither is reintroduced; the structural detector lives inside the ONE liveAdmitSegments walk and reuses liveTokens + the DYNAMIC_COMMAND_WORD token + token start offsets; a grep proves neither helper exists"
      pattern: "noteFileFromCommand|function tokenize"
  prohibitions:
    - statement: "A GREEN author suite is NECESSARY-BUT-NOT-SUFFICIENT (the 9th green-suite-insufficient catch; the rev-1 plan-check ALSO broke the design ON PAPER twice). Closure REQUIRES a both-direction RED-vs-committed-`.js` proof reproduced against the COMMITTED `hooks/admission-guard.js` (never the `.ts`, never the author `*.test.ts`): for EACH round-5 form (glob / arg-cmd-sub / param / xargs / brace / EXTGLOB / quote-removal / non-node launcher / shebang / npx-value-flag / bun $S $V) + the nested compounds + the D-01 glob self-set + the dial-`all` leak, the planted high-severity-without-real-approval command DENIES post-fix (naming the unresolvable / fail-closed condition); AND the over-block controls (`node build $HOME/out`, `node …validate`, `cp $SRC $DST`/`cp $A $B`, non-admit wrapper, routine-only multi-admit, inert mention behind a wrapper, zero-config absent lean `off`) ALLOW. Captured in 25-08-RED-baseline.txt and 25-08-GREEN-proof.txt. (D-12, [[grugops-safety-invariant-green-suite-insufficient]])."
      status: must-hold
      verification: "25-08-RED-baseline.txt + 25-08-GREEN-proof.txt show the both-direction proof vs the committed `.js` (blob 756ce508…) incl. the extglob / npx-value-flag / bun $S $V rows; reproduced independently of the author suite at the blocking checkpoint."
    - statement: "DETECTION IS AN ALLOWLIST OF PROVABLY-INERT CHARACTERS, not a denylist of dangerous ones (the rev-1 denylist `* ? [ ] { } ~ < >` was the same enumeration trap inverted — it missed extglob `@(` and any unlisted metachar). `tokenIsFinalLiteral` is true ONLY when the de-quoted value consists exclusively of `A-Za-z0-9/._:=,-` AND the raw is free of `$`/backtick AND it is not the sentinel; anything else is UNRESOLVABLE → fail CLOSED. A new/exotic expansion form MUST gate with no code change because it is OUTSIDE the inert allowlist, not because it was added to a denylist."
      status: must-hold
      verification: "a grep/inspection confirms the predicate is an allowlist (a positive character-class match), not a denylist; the bash-grounded fuzz generates extglob/history/process-sub and randomized non-allowlist chars and they DENY; the allowlist ALLOWs the real static script paths (`scripts/context-io.js`, an absolute path, `./context-io.js`) and the `admit`/`validate`/`render` verbs."
    - statement: "RULE 1 is POSITION-FREE (matches the committed whole-segment behavior) so the rev-1 position-pinning regression is closed: a final-literal `context-io` token ANYWHERE in a launcher's post-launcher argv is found regardless of value-consuming flags (`npx -p foo tsx … admit`, `node -r x … admit`). The script is NOT pinned by naive flag-skipping for the literal-context-io case. RULE 2 (hidden script) pins precisely only for DIRECT runners (value-flag arity skip) and gates on any unresolvable for FORWARDING runners ({npx} ∪ JS_RUNNERS). `node build $HOME/out` ALLOWs (direct, literal script, trailing dynamic arg ignored)."
      status: must-hold
      verification: "`npx -p foo tsx scripts/context-io.js admit` and `node -r ./preload.js scripts/context-io.js admit` DENY (RULE 1); `node build $HOME/out` ALLOWs and `node $SCRIPT build` DENIES (RULE 2 direct); `npx tsx $S admit` and `bun $S $V` DENY (RULE 2 forwarding) — all reproduced vs the committed `.js`."
    - statement: "JS_RUNNERS = {bun, bunx, deno, ts-node} is a NEW SEPARATE JS-EXECUTION-CAPABILITY set — NOT a widening of `COMMAND_MODIFIERS` (which resolves a wrapped command word) and NOT a widening of `LAUNCHERS`. It identifies what can RUN a context-io admit, so `bun $S $V` gates (recognized JS runner, unresolvable script) while `cp $A $B` allows (not a JS runner, no literal anchor). `COMMAND_MODIFIERS` membership and `LAUNCHERS` membership are unchanged; the disclosed residual narrows to an UNKNOWN runtime (qjs/llrt/future) with both positions dynamic — explicit ACCEPT with that precondition, flagged for the red-team."
      status: must-hold
      verification: "a grep confirms `COMMAND_MODIFIERS` and `LAUNCHERS` membership are UNCHANGED and `JS_RUNNERS` is a distinct new set; `bun $S $V` DENIES, `cp $A $B` ALLOWs; the threat model + summary document the JS-capability boundary and the narrowed unknown-runtime residual."
    - statement: "The fuzz oracle is GROUNDED IN ACTUAL BASH BEHAVIOR, not in `tokenIsFinalLiteral` (the rev-1 circularity: a self-referential oracle can never catch a gap inside the predicate — it passed extglob). The fuzz spawns bash to decide whether each generated candidate token is rewritten, and asserts a rewritten token in script/verb position forces the hook to gate. Necessary-not-sufficient (D-12); the independent both-angle red-team is the gate."
      status: must-hold
      verification: "scripts/floor-invariance.test.ts contains a bash-spawning `bashRewrites(token)` oracle (printf + a controlled-temp-dir glob/extglob probe) used to drive the assertions; a deliberately-removed allowlist char (a mutation test, or an inspection) would make the bash-grounded fuzz FAIL — confirming it is not self-referential."
    - statement: "The SINGLE RESOLUTION AUTHORITY is preserved AND the literal-substring fallback on the NON-LAUNCHER path stays ELIMINATED: the structural detector applies to ANY command word; the round-4 catch-all and `commandHasLiveAdmitShape` consume `segmentAdmitDisposition`, never `v.includes(\"context-io\")` + token `admit`. It lands INSIDE `liveAdmitSegments`, reusing `liveTokens` + the DYNAMIC_COMMAND_WORD token + token start offsets — no second parser, no tokenizer re-walk. `function tokenize` / `noteFileFromCommand` stay DELETED (grep=0); `liveTokens` byte-identical."
      status: must-hold
      verification: "grep confirms `function tokenize` and `noteFileFromCommand` do not exist; the literal-substring scan is no longer the gating path on any branch; `function liveAdmitSegments` is the single authority; the tokenizer body is unchanged."
    - statement: "Over-block SAFETY MUST be proven and BOUNDED: the recognized-launcher-non-admit-script / context-io-validate / no-anchor-unrecognized (`cp $SRC $DST`, `cp $A $B`, `tar $A $B`, `git log $REF`) / non-admit-wrapper / routine-only / inert controls ALLOW. The bounded over-block (`node $SCRIPT build`, `npx tsx $S admit` gated only while `human_admission != off`) and the disclosed unknown-runtime residual (`qjs $S $V`) are EXPLICIT, never a silent gate of a proven-non-admit launcher or non-launcher data."
      status: must-hold
      verification: "admission-guard.test.ts + the fuzz assert the controls ALLOW; only RULE-1/RULE-2/concrete-anchor matches GATE; the bounded over-block + residual are documented in the plan + 25-08-SUMMARY."
    - statement: "The round-1..4 closures and the verified SC2 / GOV-02 surfaces MUST NOT regress; `scripts/context-io.ts` is NOT edited (the un-forgeable hook fix re-closes the dial-`all` rewrite case; the weak D-05 admit() tier adds no un-forgeable defense; keeping context-io.ts byte-frozen preserves GAP-C/GAP-D + the GOV-02 ledger byte-identically); `hooks/guard.ts` stays byte-frozen (D-02)."
      status: must-hold
      verification: "the round-2/3/4 sweeps stay GREEN; `git diff --quiet scripts/context-io.ts` exits 0; `git diff --quiet hooks/guard.ts` exits 0; freshness 0; config-consistency + audit-ledger + foundation-guard suites green."
    - statement: "The SC1 floor + an INDEPENDENT both-angle red-team (guard-LOGIC AND guard-INPUT-SURFACE, opus-grade — the P23 split) are the proof, not a green suite. The probe MUST invent at least one un-enumerated rewrite form PER angle (e.g. a deeper extglob `+(…|…)`, a process-substitution script, a glob in the note-path, an unknown-runtime `qjs`/`llrt` invocation, a 3-admit ordering shielding a rewrite-obfuscated high-severity) and confirm it is caught structurally, AND specifically probe the T-40 JS_RUNNERS boundary + the unknown-runtime residual."
      status: must-hold
      verification: "the blocking checkpoint (Task 25-08-04) HARD-STOPS until an independent opus-grade red-team reproduces both angles (incl. extglob, npx-value-flag, bun $S $V, the unrecognized-runtime residual) + invents an un-enumerated form per angle, confirms the four floor invariants + `git diff --quiet hooks/guard.ts` exit 0 + `git diff --quiet scripts/context-io.ts` exit 0 + freshness 0 + the bounded over-block, vs the committed `.js`; findings in 25-08-SUMMARY.md."
    - statement: "WR-01 / no kit-content false-positive (D-13): the detector edit and the new test/proof artifacts must not trip `guard_context_writes` or any foundation guard — reference the hook / Workflow 16 / the config keys by name, never restate a raw `.grugops/` write path beside a write token."
      status: must-hold
      verification: "`npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial); the closed Plan-25-0x deferral markers stay inert."
---

<objective>
Round-5 gap closure for Phase 25 (25-VERIFICATION.md "UPDATE 2026-06-25 — post-25-07" banner + 25-07-SUMMARY.md "Checkpoint 25-07-04", gaps_found), HARDENED through TWO independent opus adversarial plan-checks that broke earlier drafts ON PAPER (rev-1: 3 BLOCKERs + 2 WARNINGs; rev-2: BLOCKER A npx-flag regression + BLOCKER B extglob/denylist + T-40 JS-runner hardening — all closed here). Plan 25-07's round-4 INVERT closed the leading-run command-RESOLUTION class (confirmed SOLID). The same red-team then converged on ONE NEW class the 846-green suite missed. This plan does NOT re-touch the round-1..4 closures, SC2, or the GOV-02 ledger; it makes the admit-SHAPE detector structural — the entire fix lives in the hook's shape detection inside the ONE `liveAdmitSegments` authority.

**The single root cause:** the admit-SHAPE DETECTOR (`segmentHasAdmitShape` / `commandHasLiveAdmitShape`, admission-guard.ts:459-467/679-686, committed blob 756ce508) is a LITERAL substring/token test on the **un-expanded** command string, so any shell rewrite that defers the script-ref or the verb to runtime walks through: glob `node scripts/context-i*.js admit …`; argument-position command substitution `node $(echo … admit …)`; parameter-expansion/word-split `node "$S" admit …`; cross-segment stdin→argv `… | xargs node`. AGGRAVATING: the glob form defeats the D-01 floor; under dial `all` a routine admit via rewrite has no in-script backstop.

**The fix — prove-final-literal via an ALLOWLIST (the genuinely-complete grammar property; rev-2 correction of the rev-1 denylist, which was an enumeration trap that missed bash extglob/history).** A script-or-verb-position token is RESOLVABLE only when PROVEN to be its final literal form: `tokenIsFinalLiteral` requires the de-quoted value to consist EXCLUSIVELY of an inert ALLOWLIST `A-Za-z0-9 / . _ - : = ,`, the raw source to be free of `$` and backtick, and the token to not be the DYNAMIC_COMMAND_WORD sentinel — anything else is UNRESOLVABLE → fail CLOSED, structurally catching extglob `@(`, history `!`, process-sub `<(`, brace, glob, tilde, and any future metachar WITHOUT enumerating them. The disposition `segmentAdmitDisposition` is SCRIPT-anchored: RULE 1 (position-free — a final-literal context-io token anywhere → classify the immediately-following verb; matches the committed whole-segment behavior so the rev-1 npx-flag REGRESSION is closed); RULE 2 (no literal context-io → a HIDDEN script: DIRECT runners {node,nodejs,tsx} pin the script after a value-flag-arity skip and gate if unresolvable, while a trailing dynamic ARG after a literal script does NOT gate — `node build $HOME/out` allows; FORWARDING runners {npx} ∪ JS_RUNNERS gate on any unresolvable token). The structural test applies to ANY command word — recognized launcher / `JS_RUNNERS {bun,bunx,deno,ts-node}` (a JS-EXECUTION-CAPABILITY set DISTINCT from COMMAND_MODIFIERS, closing `bun $S $V` while keeping `cp $A $B` allowed — T-40), or a shebang — never the literal substring. The same ONE detector feeds the launcher branch (:610-615), the round-4 catch-all (:661-668), and `commandHasLiveAdmitShape` (the D-01 floor). `scripts/context-io.ts` is NOT edited.

Plus the D-12 proof: the RED-first capture vs the PRE-FIX committed `.js` (blob `756ce508…`, HEAD `f5beb06`), the extended child-spawn suite, and an anti-whack-a-mole property fuzz whose oracle is GROUNDED IN ACTUAL BASH BEHAVIOR (spawning bash to decide whether each candidate token is rewritten — independent of `tokenIsFinalLiteral`, closing the rev-1 circularity), the RED->GREEN proof artifacts, the rebuilt fresh `.js`, and the HARD-STOP independent both-angle red-team checkpoint (directed to probe extglob + the npx-flag + the JS_RUNNERS T-40 boundary + the unknown-runtime residual + invent un-enumerated forms).

Purpose: harden GOV-01's un-forgeable primary tier so its COMMAND-STRING detection is no longer defeated by shell expansion, quoting, extglob, or runner indirection of a context-io reference — by requiring proof of an inert final literal, not by chasing spellings — proven adversarially vs the committed `.js`, not by a green suite. SCOPE/HONESTY (no-fabrication, CLAUDE.md): this closes command-string shape-hiding; it does NOT — and a hook reading the command string CANNOT — close a NAME-based forge. A renamed/symlinked copy of context-io.js run under its new name (`cp scripts/context-io.js /tmp/x.js && node /tmp/x.js admit T NOTE`) still ALLOWs (RULE 1 finds no literal `context-io`; RULE 2-direct pins `/tmp/x.js` final-literal-non-context-io → none). That is a PRE-EXISTING name-resolution limit of the committed substring anchor (NOT a round-5 regression), out of hook-tier reach (context-io.ts's weaker D-05 tier stays byte-frozen), and is disclosed in the threat model + the residual list below alongside the unknown-runtime residual.
Output: the allowlist + SCRIPT-anchored detector (the expansion classes + extglob + quote-removal + non-node/JS runners + shebang + the nested compound + the D-01 floor + the dial-`all` leak closed), the extended suite + the bash-grounded property fuzz, the RED/GREEN proof artifacts, the rebuilt fresh `.js`, and the independent both-angle red-team confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-PATTERNS.md
@.planning/phases/25-governance-on-a-dial/25-VERIFICATION.md
@.planning/phases/25-governance-on-a-dial/25-07-SUMMARY.md
@.planning/phases/25-governance-on-a-dial/25-07-RED-baseline.txt
@.planning/phases/25-governance-on-a-dial/25-07-GREEN-proof.txt
</context>

<artifacts_this_phase_produces>
New / changed symbols and files this round-5 gap-closure introduces:

- `hooks/admission-guard.ts`:
  - `tokenIsFinalLiteral(token, cmd)` — the ALLOWLIST-based prove-final-literal predicate (rev-2; replaces the rev-1 denylist `argvHasUnresolvableExpansion`/denylist-`tokenIsFinalLiteral`). True only when: (1) the de-quoted token value (emitted by `liveTokens`/`stripQuotedRuns`) matches `^[A-Za-z0-9/._:=,-]*$` (the inert allowlist — exactly the characters a real context-io script path / `admit`/`validate`/`render` verb / task-id / note-path need, none of which trigger a bash rewrite); (2) the token's RAW source slice (from its `start` offset to the next token boundary, mirroring `rawSegmentText`) contains no `$` and no backtick (catching a substitution the tokenizer consumed mid-word, while not over-flagging a balanced quoted literal); (3) it is not the `DYNAMIC_COMMAND_WORD` sentinel. Any character outside the allowlist (extglob `@( +( !(`, history `!`, process-sub `<(`, brace `{}`, glob `*?[`, `~`, `$`, backtick, future metachars) → not final-literal → UNRESOLVABLE.
  - `segmentAdmitDisposition(tokens, cmdIdx, cmd)` → `"admit" | "unresolvable" | "none"` (+ noteFile). Launcher in `LAUNCHERS ∪ JS_RUNNERS`: RULE 1 — scan the post-launcher tokens for the first final-literal token whose de-quoted value contains `context-io`; classify the immediately-following verb token (final-literal `admit` → admit, note = next final-literal token else null→fail-closed; unresolvable → unresolvable; final-literal non-admit or absent → none). RULE 2 (no literal context-io) — DIRECT runners {node,nodejs,tsx}: skip leading flags (incl. a value-flag set `{-r,--require,-e,--eval,--import,--loader,-p,--print,-C,--conditions}` skipped by arity; a `--flag=value` is one token), pin the first positional as the script, gate if unresolvable, else none (a trailing unresolvable arg is ignored); FORWARDING runners {npx} ∪ JS_RUNNERS: any unresolvable post-launcher token → unresolvable, else none. (If the launcher was reached behind a `STDIN_ARGV_FEEDERS` member the script is stdin-sourced → unresolvable.) Unrecognized command word: the concrete-anchor structural scan (adjacent context-io-or-unresolvable script + admit-or-unresolvable verb with a literal anchor; the command word itself is a script candidate for the shebang case).
  - `JS_RUNNERS = new Set(["bun", "bunx", "deno", "ts-node"])` — a JS-EXECUTION-CAPABILITY set, DISTINCT from `COMMAND_MODIFIERS` (resolves a wrapped command word) and `LAUNCHERS`. A new separate set, not a widening of either.
  - `STDIN_ARGV_FEEDERS = new Set(["xargs"])` — a launcher reached behind a feeder has a stdin-sourced (unresolvable) script.
  - The launcher branch (:610-615), the round-4 catch-all (:661-668), and `commandHasLiveAdmitShape` (:679-686) all consume the ONE detector — NO literal-substring fallback on any path.
- `hooks/admission-guard.test.ts`: extended child-spawn cases (incl. extglob, npx-value-flag, node -r, JS_RUNNERS, bun $S $V, cp $A $B).
- `scripts/floor-invariance.test.ts`: the round-5 property fuzz with a BASH-GROUNDED oracle (`bashRewrites(token)` spawns bash) + an INDEPENDENT shell-special token generator; the round-2/3/4 sweeps preserved.
- `25-08-RED-baseline.txt`, `25-08-GREEN-proof.txt`: the captured RED->GREEN proof artifacts.

NOTE: `scripts/context-io.ts` is NOT edited this round. The bounded over-block (`node $SCRIPT build` / `npx tsx $S admit` under active governance) and the disclosed UNKNOWN-runtime residual (`qjs $S $V` — a runner NOT in LAUNCHERS ∪ JS_RUNNERS with both positions dynamic and no literal anchor) are documented, not hidden.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 25-08-01: Make the admit-SHAPE detector STRUCTURAL — an ALLOWLIST prove-final-literal predicate + a RULE-1/RULE-2 SCRIPT-anchored disposition + JS_RUNNERS, applied to ANY command word — capture the RED baseline first</name>
  <files>hooks/admission-guard.ts, hooks/admission-guard.js, hooks/admission-guard.test.ts, 25-08-RED-baseline.txt</files>
  <read_first>
    - hooks/admission-guard.ts:85-139 (ADMIT_VERB/ADMIT_SCRIPT at :85-86, LAUNCHERS at :89 — the recognized-launcher ESCAPE set, NOT widened; COMMAND_MODIFIERS at :110-120 — NOT widened, and the new JS_RUNNERS is a SEPARATE set; DYNAMIC_EVAL_WORDS at :131)
    - hooks/admission-guard.ts:145-184 (effectiveCommandWord at :145-149; stripQuotedRuns at :156-177 — the de-quoter the allowlist check builds on; the DYNAMIC_COMMAND_WORD sentinel at :178-184)
    - hooks/admission-guard.ts:186-192 + 230-434 (the LiveToken interface with the `start` byte offset at :188-192 — the raw-span access; the whole liveTokens tokenizer — confirm `@ ! + ( ) { } ~ $ * ? [` SURVIVE in token values/raw spans, balanced quotes/`\\`-escapes are collapsed, `$(`/backtick at token-start emit DYNAMIC_COMMAND_WORD — so NO tokenizer change is needed)
    - hooks/admission-guard.ts:455-505 (segmentHasAdmitShape at :459-467 — THE LITERAL DEFECT this task replaces; isEnvIndirectedWord at :470-472; segmentHasDynamicEvalWord at :484-492 and rawTextHasAdmitShape at :503-505 — the round-4 dynamic-eval tail to preserve)
    - hooks/admission-guard.ts:507-548 (rawSegmentText at :510-520 — the raw-span helper the allowlist raw check mirrors; noteFileInSegment at :527-548 — the note reader the `admit` disposition reuses, returning null when the note is absent or non-final-literal → fail-closed)
    - hooks/admission-guard.ts:550-671 (the AdmitSegment interface :553-556 and the WHOLE liveAdmitSegments walk :574-671 — THE EDIT SITE: the launcher branch :610-615, the Class B :602-607 and dynamic-eval :620-628 branches to preserve, and the round-4 catch-all :630-668 where the unrecognized-command structural scan plugs in)
    - hooks/admission-guard.ts:673-686 (commandHasLiveAdmitShape — the GAP-B/D-01 floor input to make structural-consistent)
    - hooks/admission-guard.ts:744-922 (the main flow + classifySegmentOrDeny :808-911 — how an `unresolvable: true` segment fails CLOSED and a resolvable segment classifies severity)
    - .planning/phases/25-governance-on-a-dial/25-07-SUMMARY.md ("Checkpoint 25-07-04" confirmed-bypass table + the converged root cause)
    - .planning/phases/25-governance-on-a-dial/25-07-RED-baseline.txt and 25-07-GREEN-proof.txt (the both-direction proof ROW FORMAT 25-08-RED-baseline.txt extends)
    - hooks/admission-guard.test.ts:1-130 (the runGuard / expectDeny / expectAllow harness + fixtures vs the COMMITTED admission-guard.js; the `>= guard.test.ts` watch; extend, never reimplement)
    - hooks/guard.ts:50,88 (the verb-anchor + refuse-self-set posture to MIRROR — DO NOT EDIT guard.ts, D-02; read for pattern only)
  </read_first>
  <behavior>
    - CAPTURE RED FIRST: before any source edit, child-spawn each round-5 form through the CURRENT committed `hooks/admission-guard.js` (blob 756ce50841e4a5c4955d2197ce08f43e47931659, HEAD f5beb06) on a planted high-severity note (by: security-nfr) under human_admission: high-severity with NO approval var, and record the observed pre-fix decision per row (the ALLOWing rows are the round-5 bypasses; the literal-path control DENIES): glob `node scripts/context-i*.js admit T NOTE`; arg-cmd-sub `node $(echo <ctx> admit T NOTE)`; param `node "$S" admit T NOTE`, `node <ctx> "$V" T NOTE`, `node $A admit T NOTE`; xargs `echo "<ctx> admit T NOTE" | xargs node`; EXTGLOB `shopt -s extglob; node scripts/context-i@(o).js admit T NOTE`; brace `node <ctx> admi{t,x} T NOTE`; quote-removal `node scripts/'context-io'.js admit T NOTE`, `node <ctx> ad''mit T NOTE`, `node <ctx> "admit" T NOTE`, `node <ctx> adm\\it T NOTE`; JS_RUNNERS `bun scripts/context-i*.js admit T NOTE`, `bunx <ctx> admit T NOTE`, `deno run <ctx> admit T NOTE`, `ts-node <ctx> admit T NOTE`, both-dynamic `bun $S $V` (with the matching exports); SHEBANG `<ctx> admit T NOTE`; npx-value-flag `npx -p foo tsx scripts/context-io.js admit T NOTE`; node-value-flag `node -r ./preload.js scripts/context-io.js admit T NOTE`. Also record the nested compounds, the D-01 glob self-set, and the dial-`all` routine-via-rewrite. Save to 25-08-RED-baseline.txt (extend the 25-07 row format; cite the pre-fix blob). [The npx-value-flag + node -r forms ALREADY DENY on the committed `.js` via the whole-segment scan — record them as the regression-guard control the rev-1 position-pinning would have broken.]
    - AFTER the fix, against the rebuilt committed `.js`, on a gated high-severity un-approved note → DENY (reason names the unresolvable / fail-closed condition): every round-5 form DENIES; the nested compounds DENY; the D-01 glob self-set DENIES; the dial-`all` routine-via-rewrite DENIES; `bun $S $V` DENIES.
    - SCRIPT-anchored bounded fail-safe (ALLOW): a direct runner with a provably-literal non-context-io script → `node build`, `node render`, `node script.js test`, `node build $HOME/out`; a recognized launcher running a literal context-io NON-admit verb → `node scripts/context-io.js validate`; an unrecognized NON-JS-runner with no concrete anchor → `cp $SRC $DST`, `cp $A $B`, `tar $A $B`, `git log $REF`.
    - Bounded over-block (EXPLICIT, accepted): `node $SCRIPT build`, `npx tsx $S admit` GATE under `high-severity`/`all` (ALLOW under `off`). Disclosed UNKNOWN-runtime residual (accepted): `qjs $S $V` / `llrt $S $V` (runner not in LAUNCHERS ∪ JS_RUNNERS, both positions dynamic, no literal anchor) ALLOWs.
    - NO over-block regression on inert data: an inert heredoc body line behind a wrapper, a single/double-quoted echoed mention behind a wrapper (`timeout 5 echo "node context-io.js admit …"` — one blob token, no separate script+verb pair), a `#`-comment mention, a routine-only multi-admit under high-severity → ALLOW.
    - No regression on round-1..4 closures: round-4 sub-roots a/b/c + dial-`all` wrapper leak + Class-E + sh -c tail DENY; round-3 Class A/B/E/F; round-2 GAP-B/GAP-D; empty stdin ALLOWs.
  </behavior>
  <action>
    FIRST capture the RED baseline into `25-08-RED-baseline.txt` (against the committed `.js` blob 756ce508…, HEAD f5beb06, per the behavior list; cite the pre-fix blob, extend the 25-07 row format).

    THEN make the structural fix inside `liveAdmitSegments` (admission-guard.ts:574-671). Do NOT enumerate expansion spellings, do NOT widen `COMMAND_MODIFIERS` or `LAUNCHERS`, do NOT re-walk `liveTokens`.

    Add `tokenIsFinalLiteral(token, cmd)` as an ALLOWLIST (NOT the rev-1 denylist): true only when (1) the de-quoted token value matches `^[A-Za-z0-9/._:=,-]*$` (the inert allowlist — letters, digits, and `/ . _ - : = ,`, which is exactly what a real `scripts/context-io.js` / absolute or `./` path / the `admit`/`validate`/`render` verb / a task-id / a note path need, and none of which triggers a bash rewrite as a bare char); (2) the token's RAW source slice (from `start` to the next token boundary) contains no `$` and no backtick (catches a mid-word substitution the tokenizer consumed, without over-flagging a balanced quoted literal whose raw has only quotes); (3) it is not the `DYNAMIC_COMMAND_WORD` sentinel. Any character outside the allowlist (extglob `@( +( !(`, history `!`, process-sub `<(`, brace, glob, tilde, future metachars) → not final-literal → UNRESOLVABLE. Verify the allowlist ALLOWs the real static script paths and the verbs against the actual test fixtures; if a fixture legitimately needs another inert char, add it WITH a one-line justification (it must be non-expanding in bash as a bare char).

    Add `JS_RUNNERS = new Set(["bun", "bunx", "deno", "ts-node"])` — a JS-EXECUTION-CAPABILITY set (what can RUN a context-io admit), DISTINCT from `COMMAND_MODIFIERS` (which resolves a wrapped command word) and `LAUNCHERS`. Comment that it is a NEW SEPARATE set, not a widening of either. Add `STDIN_ARGV_FEEDERS = new Set(["xargs"])`.

    Add `segmentAdmitDisposition(tokens, cmdIdx, cmd)` → `"admit" | "unresolvable" | "none"` (+ noteFile):
    - If the command word at cmdIdx is in `LAUNCHERS ∪ JS_RUNNERS` (or reached behind a `STDIN_ARGV_FEEDERS` member → treat the script as unresolvable):
      - RULE 1 (POSITION-FREE — restores the committed whole-segment catch the rev-1 position-pinning regressed): scan the post-launcher tokens for the FIRST final-literal token whose de-quoted value contains `context-io`; if found, classify the token immediately after it (the verb): final-literal `admit` → `admit` (noteFile = the next final-literal token, else null → fail-closed); unresolvable → `unresolvable`; final-literal non-admit (`validate`/`render`) or absent → `none`.
      - RULE 2 (no final-literal context-io found → a HIDDEN script): for DIRECT runners {node,nodejs,tsx} skip leading flag tokens (a `-`-prefixed token; and skip the NEXT token as the flag's value when the flag is in the documented value-flag set `{-r,--require,-e,--eval,--import,--loader,-p,--print,-C,--conditions}`; a `--flag=value` is one token), pin the first positional as the script, and return `unresolvable` if that script token fails `tokenIsFinalLiteral`, else `none` (a trailing unresolvable ARG after a literal script is IGNORED — `node build $HOME/out` is `none`/ALLOW). For FORWARDING runners {npx} ∪ JS_RUNNERS (which forward to a runner+script at a deep/variable position) return `unresolvable` if ANY post-launcher token fails `tokenIsFinalLiteral`, else `none`.
      - else `none`.
    - Else (UNRECOGNIZED command word — cp/tar/git/a shebath script/an unlisted wrapper-operand/a redirection): the concrete-anchor structural scan — `admit`/`unresolvable` if there exist adjacent positional tokens (Ti, Ti+1), CONSIDERING the command word itself as a Ti candidate (shebang), with [Ti final-literal-context-io OR (Ti not final-literal)] AND [Ti+1 final-literal-`admit` OR (Ti+1 not final-literal)] AND [at least one concrete literal anchor: Ti final-literal-context-io OR Ti+1 final-literal-`admit`]; else `none`. (The literal anchor bounds over-block so `cp $A $B` — both unresolvable, no anchor — is `none`/ALLOW.)

    Wire the ONE detector into all three consumers (NO literal-substring fallback on any path): (a) the LAUNCHER branch (:610-615) — push `{noteFile, unresolvable:false}` on `admit`, `{noteFile:null, unresolvable:true}` on `unresolvable`, nothing on `none`. (b) the round-4 catch-all (:661-668), reached for an unrecognized command word — call `segmentAdmitDisposition` (the unrecognized branch) instead of the literal `segmentHasAdmitShape`; preserve the round-4 dynamic-eval-tail raw-text behavior. (c) `commandHasLiveAdmitShape` (:679-686) derives from the one authority (e.g. `liveAdmitSegments(cmd).length > 0`) so the glob/param self-set DENIES.

    PRESERVE the Class B branch (:602-607) and the dynamic-eval / `$X` tail (:620-628) (their admit-shape checks may reuse the structural detector but keep their unresolvable→gate behavior); the dial canonicalization (GAP-C) and case-insensitive HIGH_SEVERITY_ROLES (GAP-D) are unchanged. Do NOT rewrite `liveTokens` — the detector reads de-quoted values + raw spans via the `start` offsets the tokenizer ALREADY emits.

    Voice: CLEAR PROFESSIONAL VOICE in comments and deny reasons. Comment the principle: a script/verb token the hook cannot PROVE consists only of inert characters (and is non-context-io / non-admit) is gate-or-stricter — we prove inertness via an allowlist, never enumerate what expands; JS_RUNNERS is a JS-execution-capability boundary, not a COMMAND_MODIFIERS widening.

    Comment-text discipline: no fenced code blocks in this action; describe the predicate + disposition by their named conditions.

    Rebuild the committed `.js`: `npm run build` then `npm run freshness` (0 drift).

    Extend `hooks/admission-guard.test.ts` with cases for each round-5 form (incl. EXTGLOB, npx-value-flag, node -r, JS_RUNNERS, bun $S $V) → DENY; the over-block controls (incl. `node build $HOME/out`, `node …validate`, `cp $A $B`) → ALLOW; re-assert round-1..4. Keep case count >= guard.test.ts. Capture the post-fix outcomes into `25-08-GREEN-proof.txt` (Task 25-08-03 completes the fuzz rows).
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run hooks/admission-guard.test.ts && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts</automated>
  </verify>
  <acceptance_criteria>
    - `25-08-RED-baseline.txt` records each round-5 form (glob / arg-cmd-sub / param / xargs / EXTGLOB / brace / quote-removal / JS_RUNNERS incl. `bun $S $V` / shebang / npx-value-flag / node -r) + the compounds + the D-01 glob self-set + the dial-`all` leak, with the observed PRE-FIX decision per row and the literal-path control DENYing, citing the pre-fix blob.
    - A grep confirms `COMMAND_MODIFIERS` and `LAUNCHERS` membership are UNCHANGED, `JS_RUNNERS` is a distinct new set, `function tokenize`/`noteFileFromCommand` still do NOT exist, `function liveAdmitSegments` is the one authority, the literal `segmentHasAdmitShape` substring scan is NO LONGER the gating path on the launcher branch / round-4 catch-all, and `tokenIsFinalLiteral` is an ALLOWLIST (a positive character-class match), not a denylist; `liveTokens` is unedited.
    - `npx vitest run hooks/admission-guard.test.ts` is GREEN: every round-5 form DENIES (incl. EXTGLOB `node scripts/context-i@(o).js admit`, `npx -p foo tsx scripts/context-io.js admit`, `node -r ./preload.js scripts/context-io.js admit`, `bun $S $V`); the nested compounds DENY; the D-01 glob self-set DENIES; the dial-`all` routine-via-rewrite DENIES; the over-block controls (`node build`, `node build $HOME/out`, `node scripts/context-io.js validate`, `node render`, `cp $SRC $DST`, `cp $A $B`, `tar $A $B`, `git log $REF`, `timeout 5 node render`, `sudo ls`, `>/dev/null node build`, routine-only multi-admit, inert mention behind a wrapper) ALLOW; an UNKNOWN-runtime `qjs $S $V` ALLOWs (disclosed residual); the round-1..4 closures still hold; empty stdin ALLOWs; case count >= guard.test.ts.
    - `npm run freshness` exits 0; `git diff --quiet hooks/guard.ts` exits 0; `git diff --quiet scripts/context-io.ts` exits 0.
    - `25-08-GREEN-proof.txt` records the post-fix DENY for each round-5 form + the compounds + the D-01 self-set + the dial-`all` leak, plus ALLOW for the bounded-fail-safe controls.
  </acceptance_criteria>
  <done>The admit-shape detector is structural via an ALLOWLIST `tokenIsFinalLiteral` (catches extglob/any metachar) + a RULE-1 (position-free literal-context-io, closing the npx-flag regression) / RULE-2 (hidden-script, direct-vs-forwarding) SCRIPT-anchored disposition applied to ANY command word, with `JS_RUNNERS` closing `bun $S $V` while `cp $A $B` allows — closing glob/arg-cmd-sub/param/xargs/extglob/quote-removal/non-node/shebang/npx-flag + the nested compound + the D-01 glob self-set + the dial-`all` leak from ONE detector; `COMMAND_MODIFIERS`/`LAUNCHERS` not widened (JS_RUNNERS a separate set), the second walk stays deleted, `liveTokens` byte-identical, the round-1..4 closures hold — vs the committed `.js`; guard.ts + context-io.ts untouched, freshness 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 25-08-02: Extend the child-spawn oracle (admission-guard.test.ts) — exhaustive round-5 form + extglob + npx-flag + JS_RUNNERS + over-block cases vs the committed .js, and complete the GREEN-proof rows</name>
  <files>hooks/admission-guard.test.ts, 25-08-GREEN-proof.txt</files>
  <read_first>
    - hooks/admission-guard.test.ts:1-130 (the runGuard / expectDeny / expectAllow harness + makeProject / writeNote / admitCmd fixtures + the `>= guard.test.ts` watch — extend, never reimplement; targets the COMMITTED admission-guard.js)
    - hooks/admission-guard.test.ts (the round-3/round-4 describe blocks — EXTEND, do not duplicate)
    - hooks/admission-guard.ts:455-505, 574-686 (the post-fix `tokenIsFinalLiteral` allowlist + `segmentAdmitDisposition` RULE 1/2 + JS_RUNNERS + the three call sites from Task 25-08-01)
    - .planning/phases/25-governance-on-a-dial/25-07-GREEN-proof.txt (the GREEN-proof ROW FORMAT 25-08-GREEN-proof.txt extends)
    - .planning/phases/25-governance-on-a-dial/25-07-SUMMARY.md ("Checkpoint 25-07-04" confirmed-bypass table; the over-block controls)
  </read_first>
  <behavior>
    - Expansion forms (DENY under `high-severity`): glob `node scripts/context-i*.js admit T <hi>`; arg-cmd-sub `node $(echo <ctx> admit T <hi>)` + backtick; param `node "$S" admit T <hi>`, `node <ctx> "$V" T <hi>`, `node $A admit T <hi>`; xargs `echo "<ctx> admit T <hi>" | xargs node`.
    - Rewrite forms (DENY): EXTGLOB `shopt -s extglob; node scripts/context-i@(o).js admit T <hi>` (+ `?(o)`, `+(o)`, `!(x)`); brace `node <ctx> admi{t,x} T <hi>`; quote-removal `node scripts/'context-io'.js admit T <hi>`, `node <ctx> ad''mit T <hi>`, `node <ctx> "admit" T <hi>`, `node <ctx> adm\\it T <hi>`; the fully-static quoted admit `node "scripts/context-io.js" "admit" T <hi>` (DENY); tilde `node ~/context-io.js admit T <hi>`; process-sub `node <(echo <ctx>) admit T <hi>`.
    - Launcher-flag forms (DENY — the BLOCKER-A regression guards): `npx -p foo tsx scripts/context-io.js admit T <hi>` (+ `-c`, `--package`, `--call`); `node -r ./preload.js scripts/context-io.js admit T <hi>`.
    - JS_RUNNERS forms (DENY): `bun scripts/context-i*.js admit T <hi>`, `bunx <ctx> admit T <hi>`, `deno run <ctx> admit T <hi>`, `ts-node <ctx> admit T <hi>`, and the both-dynamic `bun $S $V` / `deno run $S $V` (with exports) → DENY.
    - Shebang form (DENY): `<ctx> admit T <hi>`.
    - Nested wrapper+rewrite compounds (DENY): `timeout 5 node scripts/context-i*.js admit T <hi>`, `sudo node "$S" admit T <hi>`, `>/dev/null node $(echo <ctx> admit T <hi>)`, `nice -n 5 bun <ctx> admit T <hi>`, `timeout 5 node scripts/context-i@(o).js admit T <hi>`.
    - D-01 floor: `GRUGOPS_ADMISSION_APPROVED_BY=eve node scripts/context-i*.js admit T <hi>` → DENY.
    - dial-`all`: `node scripts/context-i*.js admit T <routine>`, `bun <ctx> admit T <routine>` under `all` → DENY.
    - Over-block controls (MUST ALLOW): `node build`, `node build $HOME/out`, `node scripts/context-io.js validate`, `node render`, `node script.js test`, `cp $SRC $DST`, `cp $A $B`, `tar $A $B`, `git log $REF`, `qjs $S $V` (disclosed unknown-runtime residual), `node /tmp/x.js admit T <hi>` after a rename of context-io.js (disclosed PRE-EXISTING name-resolution residual — ALLOWs, NOT a regression), `timeout 5 node render`, `sudo ls`, `>/dev/null node build`, a routine-only multi-admit under `high-severity`, an inert heredoc/quoted/`#`-comment admit mention behind a wrapper → ALLOW. Under `off`, `node $SCRIPT build` → ALLOW.
    - Forwarding-runner over-block CONTROLS (the DISCLOSED, bounded over-block — assert it is the intended GATED behavior, not silent): under `high-severity`, a NON-admit forwarding-runner command with a dynamic token → DENY (any unresolvable post-launcher token gates) — `npx vitest run $FILE`, `bun app.js $ARG`, `deno run server.ts $PORT`, `ts-node $SCRIPT`; the SAME commands under `off` → ALLOW (proving it is opt-in and avoidable). A direct/static command (`node app.js`, `npx vitest run tests/foo.test.ts`) → ALLOW.
    - No regression: round-4 sub-root (a)/(b)/(c) + dial-`all` wrapper + Class-E + sh -c tail; round-3 Class A/B/E/F; round-2 GAP-B/GAP-D still pass.
  </behavior>
  <action>
    Extend `hooks/admission-guard.test.ts` with describe blocks for the round-5 classes — expansion, rewrite (incl. EXTGLOB), launcher-flag (npx-value-flag, node -r — the BLOCKER-A regression guards), JS_RUNNERS (incl. `bun $S $V`), shebang — plus the nested compounds, the D-01 glob self-set, the dial-`all` routine-via-rewrite, and the over-block ALLOW controls (incl. `node build $HOME/out`, `node …validate`, `cp $A $B`, the unknown-runtime `qjs $S $V` residual). Reuse the existing harness + fixtures. Do NOT duplicate the round-3/4 blocks.

    For each form, build the command delivering the real admit argv via the rewrite/runner under test and assert the expected decision. For extglob include `shopt -s extglob;` in the command string. For the npx/node value-flag forms, assert DENY (these guard the rev-1 regression). For `bun $S $V`, set the exports and assert DENY (JS_RUNNER, unresolvable script). For `cp $A $B` / `qjs $S $V`, assert ALLOW (no concrete anchor / unknown runtime). For the DISCLOSED forwarding-runner over-block, add a describe block asserting `npx vitest run $FILE` / `bun app.js $ARG` / `deno run server.ts $PORT` / `ts-node $SCRIPT` DENY under `high-severity` and ALLOW under `off` (proving the over-block is the intended, opt-in, avoidable GATED behavior — not silent), plus a static `npx vitest run tests/foo.test.ts` ALLOW. For the DISCLOSED name-resolution residual, add `node /tmp/x.js admit T <hi>` after copying context-io.js to /tmp/x.js → assert ALLOW (the pre-existing forge the hook-tier cannot reach; a comment must state it is disclosed + pre-existing, NOT a round-5 regression).

    Keep case count >= guard.test.ts. Capture the post-fix DENY rows + the ALLOW control rows into `25-08-GREEN-proof.txt`, extending the 25-07 row format, referencing the committed `.js`.

    Voice: CLEAR PROFESSIONAL VOICE. Per D-12 a GREEN oracle is NECESSARY-NOT-SUFFICIENT — the independent red-team at Task 25-08-04 is the gate; state that in a comment.

    Comment-text discipline: no fenced code blocks; describe cases by command shape + expected decision.
  </action>
  <verify>
    <automated>npx vitest run hooks/admission-guard.test.ts && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run hooks/admission-guard.test.ts` is GREEN with the round-5 cases: every expansion / rewrite (incl. EXTGLOB) / launcher-flag (`npx -p foo tsx … admit`, `node -r … admit`) / JS_RUNNERS (incl. `bun $S $V`) / shebang form DENIES; the nested compounds DENY; the D-01 glob self-set DENIES; the dial-`all` routine-via-rewrite DENIES; the over-block controls (`node build $HOME/out`, `node scripts/context-io.js validate`, `cp $A $B`, `tar $A $B`, `git log $REF`, `qjs $S $V`, a renamed-script `node /tmp/x.js admit` disclosed name-resolution residual, non-admit wrapper, routine-only multi-admit, inert mention) ALLOW; `node $SCRIPT build` ALLOWs under `off`.
    - The DISCLOSED forwarding-runner over-block is proven as an intended GATED control: `npx vitest run $FILE` / `bun app.js $ARG` / `deno run server.ts $PORT` / `ts-node $SCRIPT` DENY under `high-severity` and ALLOW under `off`; a static `npx vitest run tests/foo.test.ts` / `node app.js` ALLOWs.
    - The round-3 Class A/B/E/F + round-4 sub-root (a)/(b)/(c) + round-2 GAP-B/GAP-D cases still pass; case count >= guard.test.ts.
    - `git diff --quiet scripts/context-io.ts` exits 0; `git diff --quiet hooks/guard.ts` exits 0.
    - `25-08-GREEN-proof.txt` records the post-fix DENY rows + the ALLOW controls, referencing the committed `.js`.
  </acceptance_criteria>
  <done>The child-spawn oracle exhaustively covers the round-5 classes vs the committed `.js` — expansion / rewrite (incl. extglob) / launcher-flag / JS_RUNNERS (incl. bun $S $V) / shebang DENY; the compounds + D-01 self-set + dial-`all` leak DENY; the recognized-non-admit-launcher / `node …validate` / no-anchor-unrecognized (`cp $A $B`) / unknown-runtime (`qjs $S $V`) / routine-only / inert controls ALLOW; round-1..4 hold, the GREEN-proof rows captured — context-io.ts + guard.ts byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 25-08-03: Anti-whack-a-mole property/fuzz with a BASH-GROUNDED oracle (spawn bash to decide rewrite, NOT tokenIsFinalLiteral) + complete the RED->GREEN proof set (D-12)</name>
  <files>scripts/floor-invariance.test.ts, 25-08-RED-baseline.txt, 25-08-GREEN-proof.txt</files>
  <read_first>
    - scripts/floor-invariance.test.ts:1-75 (the four-invariant structure + FROZEN_GUARD_BLOB at :60 + the child-spawn-vs-committed-`.js` composition — EXTEND this)
    - scripts/floor-invariance.test.ts:317-360 (the `hookDecision(cmd, dir)` spawn helper + `projectWith({ dial })` fixture — REUSE; do not reimplement)
    - scripts/floor-invariance.test.ts:407-505 (the round-2 anti-whack-a-mole class-invariant block — the loop shape to mirror; do NOT duplicate)
    - scripts/floor-invariance.test.ts:589-879 (the round-2/3/4 command-RESOLUTION fuzz blocks — the round-5 fuzz ADDS the bash-grounded prove-final-literal dimension; do NOT duplicate)
    - hooks/admission-guard.ts:455-505, 574-686 (the post-fix allowlist `tokenIsFinalLiteral` + `segmentAdmitDisposition` from Task 25-08-01 — the fuzz asserts the CLASS invariant over the committed `.js`, with a BASH-GROUNDED oracle independent of the detector)
    - .planning/phases/25-governance-on-a-dial/25-07-GREEN-proof.txt (the GREEN-proof ROW FORMAT 25-08-GREEN-proof.txt extends)
    - .planning/phases/25-governance-on-a-dial/25-CONTEXT.md:147-160 (D-12 — green-suite-insufficient)
  </read_first>
  <behavior>
    - BASH-GROUNDED oracle (the rev-1 circularity fix): a `bashRewrites(token)` helper SPAWNS bash to decide whether bash rewrites/expands a candidate token, INDEPENDENT of `tokenIsFinalLiteral`. For non-glob forms compare `bash -c 'printf "%s\n" <token>'` against the verbatim token (a differing or multi-line output ⇒ rewritten: brace/tilde/ANSI-C/history/word-split). For glob/extglob, probe in a CONTROLLED TEMP DIR containing a real `context-io.js`, with `shopt -s extglob` and `nullglob`, and check whether the token expands to a filename. A token bash leaves verbatim is stable; anything else is rewritten.
    - Generator (INDEPENDENT of the detector's allowlist): emit candidate script/verb tokens spanning extglob `@(o)`/`+(o)`/`!(x)`/`?(o)`, history `!`, process-substitution `<(echo x)`, brace `{a,b}`, tilde `~/x`, ANSI-C `$'admit'`, quote-removal (`ad''mit`, `"admit"`, `scripts/'context-io'.js`, `adm\\it`), mid-word substitution (`a$(echo b)c`), AND randomized non-alphanumeric characters.
    - Class invariant (the structural proof): for EVERY generated variant placed in script-or-verb position in a high-severity admit construction — if `bashRewrites(token)` is true (bash would rewrite it), the committed `admission-guard.js` MUST gate (DENY under an active dial); a fully-static literal (bash leaves it verbatim) whose de-quoted form is the literal context-io admit DENIES, and a literal non-context-io script ALLOWs. Span launcher ∈ {LAUNCHERS ∪ JS_RUNNERS ∪ shebang} × position {script, verb, note} × dial {off|high-severity|all} × admit-ordering.
    - JS_RUNNERS / unknown-runtime: `bun $S $V` DENIES; `qjs $S $V` (unknown runtime) ALLOWs; `cp $A $B` ALLOWs.
    - xargs: `<admit-text> | xargs <launcher>` DENIES; `echo build | xargs node` ALLOWs.
    - dial-`all` routine-via-rewrite DENIES; the separator × ordering (2/3-admit) sweep gates when any segment is a rewrite-obfuscated high-severity admit; a routine-only multi-admit ALLOWs.
    - The four named floor invariants hold at every value; the round-2/3/4 sweeps stay green.
  </behavior>
  <action>
    Extend `scripts/floor-invariance.test.ts` (do NOT duplicate the round-2/3/4 matrices — ADD the bash-grounded prove-final-literal dimension). Reuse `hookDecision(cmd, dir)` + `projectWith({ dial })`.

    Add the `bashRewrites(token)` BASH-GROUNDED oracle (the rev-1 circularity fix — the oracle MUST be bash, NOT `tokenIsFinalLiteral`): spawn bash (`printf '%s\n'` for non-glob; a controlled-temp-dir glob/extglob probe with `shopt -s extglob` and a real context-io.js for glob forms) to classify each candidate token as stable-or-rewritten. Add a `nonLiteralTokens()` generator emitting extglob/history/process-sub/brace/tilde/ANSI-C/quote-removal/mid-word/randomized-char tokens, INDEPENDENT of the detector's allowlist.

    Add the sweep asserting the class invariant: a bash-rewritten token in script/verb position ⇒ the committed `.js` gates; a fully-static literal classifies correctly (a static quoted admit → gate; a literal non-context-io script → allow). Add the JS_RUNNERS / unknown-runtime sweep (`bun $S $V` DENY, `qjs $S $V` ALLOW, `cp $A $B` ALLOW), the xargs sweep, the dial-`all` sweep, and the separator × ordering sweep.

    Make the structural argument explicit in a comment: the oracle is what bash actually does, so a gap INSIDE the allowlist (e.g. a missed extglob lead char) makes the suite FAIL — it is not self-referential (the precise rev-1 failure mode). Optionally include a guarded mutation note: temporarily allowlisting `(` would make the extglob assertions fail.

    COMPLETE the proof artifacts: finalize `25-08-RED-baseline.txt` (pre-fix committed `.js` blob 756ce508…, the observed pre-fix decision per form; the literal-path control DENIES) and `25-08-GREEN-proof.txt` (post-fix DENIES each form + compound + the D-01 self-set + the dial-`all` leak; the bounded-fail-safe controls ALLOW; round-1..4 still hold). Extend the 25-07 row format; reference the committed `.js`.

    Voice: CLEAR PROFESSIONAL VOICE. Per D-12 a GREEN fuzz is NECESSARY-NOT-SUFFICIENT — the independent red-team at Task 25-08-04 is the gate; state that in a comment.

    Comment-text discipline: no fenced code blocks; describe the bash oracle + generator + invariant by concept.
  </action>
  <verify>
    <automated>npx vitest run scripts/floor-invariance.test.ts && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/floor-invariance.test.ts` is GREEN with the bash-grounded fuzz: a `bashRewrites(token)` helper spawns bash (printf + controlled-temp-dir glob/extglob probe) as the oracle, INDEPENDENT of `tokenIsFinalLiteral`; the generator emits extglob/history/process-sub/brace/tilde/ANSI-C/quote-removal/mid-word/randomized tokens; EVERY bash-rewritten token in script-or-verb position DENIES via the committed `admission-guard.js`; a fully-static literal classifies correctly.
    - `bun $S $V` DENIES; `qjs $S $V` ALLOWs; `cp $A $B` ALLOWs; the xargs sweep DENIES the admit form and ALLOWs `echo build | xargs node`; the dial-`all` routine-via-rewrite DENIES; the separator × ordering (2/3-admit) sweep DENIES when any segment is a rewrite-obfuscated high-severity admit; a routine-only multi-admit ALLOWs.
    - The four named floor invariants hold at every value; the round-2/3/4 sweeps stay green; `git diff --quiet hooks/guard.ts` and `git diff --quiet scripts/context-io.ts` exit 0.
    - `25-08-RED-baseline.txt` and `25-08-GREEN-proof.txt` are complete (each form + extglob + npx-flag + bun $S $V + the compounds + the D-01 self-set + the dial-`all` leak: pre-fix observed decision with the literal-path control DENY, post-fix DENY; the bounded-fail-safe controls ALLOW) and reference the committed `.js`, extending the 25-07 row format.
  </acceptance_criteria>
  <done>An anti-whack-a-mole property fuzz with a BASH-GROUNDED oracle (spawn bash to decide rewrite, not the detector itself — the rev-1 circularity fix) proves the class invariant structurally (a bash-rewritten script/verb token gates; a static literal classifies correctly; `bun $S $V` gates, `qjs $S $V`/`cp $A $B` allow); the round-2/3/4 sweeps + four floor invariants stay green; the RED->GREEN proof artifacts are captured vs the committed `.js`; guard.ts + context-io.ts byte-unchanged — the SC1 author proof is complete (independent red-team still the gate, D-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 25-08-04: SC1 round-5 closure gate — INDEPENDENT both-angle red-team vs the committed .js (HARD STOP; green suite is NOT proof, D-12)</name>
  <read_first>
    - .planning/phases/25-governance-on-a-dial/25-07-SUMMARY.md ("Checkpoint 25-07-04 — INDEPENDENT both-angle red-team" — the confirmed-bypass table, the converged root cause; the bar this checkpoint clears)
    - .planning/phases/25-governance-on-a-dial/25-08-RED-baseline.txt and 25-08-GREEN-proof.txt (the both-direction proof the probe reproduces vs the committed `.js`)
    - hooks/admission-guard.js (the COMMITTED artifact the probe attacks — never the `.ts`, never the author suites); scripts/context-io.js (the imported parse — UNCHANGED this round)
  </read_first>
  <what-built>The round-5 gap-closure STRUCTURAL admit-SHAPE detector for the GOV-01 un-forgeable tier, built on an ALLOWLIST prove-final-literal predicate (de-quoted value exclusively `A-Za-z0-9/._:=,-`, raw free of `$`/backtick, not the sentinel — structurally catching extglob/history/process-sub/brace/glob/tilde/$/backtick + any future metachar) and a SCRIPT-anchored RULE-1 (position-free literal-context-io → verb check; closes the rev-1 npx-flag regression) / RULE-2 (hidden script: direct-runner value-flag-arity pin vs forwarding-runner any-unresolvable) disposition applied to ANY command word, with `JS_RUNNERS = {bun,bunx,deno,ts-node}` (a JS-execution-capability set distinct from COMMAND_MODIFIERS, closing `bun $S $V` while `cp $A $B` allows). The ONE detector feeds the launcher branch, the round-4 catch-all, and the D-01 floor (Tasks 25-08-01/02); plus the property fuzz whose oracle is GROUNDED IN ACTUAL BASH (Task 25-08-03). `scripts/context-io.ts` is UNCHANGED. This is the LAST task. Per [[grugops-safety-invariant-green-suite-insufficient]] and D-12, a passing suite is necessary but NOT sufficient — the author's suites passed and STILL missed a class across rounds 1-4, AND the round-5 design was caught ON PAPER twice by independent plan-checks (rev-1: brace/non-node/over-block-contradiction; rev-2: npx-flag regression / extglob-denylist / JS-runner). This checkpoint HARD-STOPS until an INDEPENDENT opus-grade adversary has reproduced the closure. The orchestrator (not the authoring executor) dispatches the red-team.</what-built>
  <how-to-verify>
    An INDEPENDENT opus-grade probe must run BOTH angles (the P23 split — guard LOGIC and guard INPUT SURFACE) and reproduce ALL of the following against the COMMITTED artifacts (`hooks/admission-guard.js`; `scripts/context-io.js`), never the `.ts` and never the author's suites:
    1. INPUT-SURFACE angle — the rewrite forms DENY a gated high-severity un-approved admit: glob, EXTGLOB (`node scripts/context-i@(o).js admit …`, `?(o)`, `+(o)`, `!(x)`), brace, quote-removal (`scripts/'context-io'.js`, `ad''mit`, `"admit"`, `adm\\it`), tilde, process-sub, the fully-static quoted admit (`node "scripts/context-io.js" "admit" …`). The over-block direction holds: `node build`, `node build $HOME/out`, `node scripts/context-io.js validate`, an inert mention behind a wrapper → ALLOW. AND the adversary INVENTS ≥1 input-surface rewrite the author did NOT enumerate (a deeper extglob `+(a|b)`, a glob in the NOTE-path, a randomized shell-special char in script position) — caught structurally.
    2. LOGIC angle — the deferred-argv + runner forms DENY: arg-position cmd-sub, param/word-split, xargs, the npx value-flag (`npx -p foo tsx scripts/context-io.js admit …`), the node value-flag (`node -r ./preload.js scripts/context-io.js admit …`), the JS_RUNNERS (`bun`/`bunx`/`deno run`/`ts-node` + glob/param, AND `bun $S $V`), the shebang. The compounding closures: the D-01 glob self-set DENIES; dial-`all` routine-via-rewrite DENIES; the nested compounds DENY; a routine-only multi-admit ALLOWs; `cp $A $B`/`tar $A $B` ALLOW. AND the adversary specifically PROBES (and the findings must explicitly confirm) the THREE disclosed accepted limits as STATED, not silent gaps: (i) the T-40 JS_RUNNERS boundary + the UNKNOWN-runtime residual (`qjs $S $V` / `llrt $S $V` ALLOWs — confirm the narrow precondition: both positions dynamic + a runner NOT in LAUNCHERS ∪ JS_RUNNERS + no literal anchor); (ii) the NAME-resolution residual (`cp scripts/context-io.js /tmp/x.js && node /tmp/x.js admit T NOTE`, or a symlink, ALLOWs — confirm it is the disclosed PRE-EXISTING name-based limit, NOT a round-5 regression and NOT silently introduced; the committed `.js` has the same substring anchor); (iii) the FORWARDING-runner over-block breadth (`npx vitest run $FILE` / `bun app.js $ARG` / `deno run server.ts $PORT` / `ts-node $SCRIPT` DENY under active governance and ALLOW under `off` — confirm it is the intended, bounded, opt-in over-block, not an unbounded false-positive). AND INVENTS ≥1 logic rewrite the author did NOT enumerate.
    3. BOTH-DIRECTION RED-vs-committed-`.js`: the clean case ALLOWs; the zero-config absent lean reads `off` and ALLOWs a routine admit; each planted form DENIES; the byte-identical literal-path control DENIES. Confirm against `25-08-RED-baseline.txt` / `25-08-GREEN-proof.txt`.
    4. PRESERVED + FLOOR + freshness: round-4 (a/b/c + dial-`all` wrapper + Class-E + sh -c tail), round-3 (Class A/B/E/F), round-2 (GAP-B/C/D) hold; the four named floor invariants hold (`hooks/guard.ts` byte-frozen via `git diff --quiet` exit 0 + blob `3501810e21308e4b7e219679a6ca30dace9b5d66`); `git diff --quiet scripts/context-io.ts` exit 0; `npm run freshness` exits 0; WR-01 no false-positive; the single-authority structural assertion holds (`function tokenize`/`noteFileFromCommand` gone; `COMMAND_MODIFIERS`/`LAUNCHERS` not widened; `JS_RUNNERS` a distinct set; `tokenIsFinalLiteral` an allowlist; the literal-substring scan not the gating path; `liveTokens` byte-identical); the fuzz oracle is bash-grounded (not self-referential).
    5. Findings (reproduced commands, invented forms per angle, deny/allow outcomes, the T-40 boundary + unknown-runtime residual probe, the bounded over-block, the preserved round-1..4 results) DOCUMENTED in `25-08-SUMMARY.md` — independently reproduced, NOT self-reported.
  </how-to-verify>
  <acceptance_criteria>
    - (a) Each input-surface rewrite (glob / EXTGLOB / brace / quote-removal / tilde / process-sub / fully-static quoted admit) DENIES + an invented input-surface rewrite DENY; the recognized-non-admit-launcher / `node …validate` / inert mention ALLOW — INDEPENDENT input-surface probe (D-12, P23).
    - (b) Each logic form (arg-cmd-sub / param / xargs / npx-value-flag / node-value-flag / JS_RUNNERS incl. `bun $S $V` / shebang) DENIES; the D-01 glob self-set DENIES; the dial-`all` routine-via-rewrite DENIES; the nested compounds DENY; `cp $A $B` ALLOWs; the THREE disclosed limits are confirmed-as-stated (not silent): `qjs $S $V` unknown-runtime ALLOWs (T-40 narrow precondition), the rename/symlink forge `node /tmp/x.js admit` ALLOWs (pre-existing name-resolution residual, not a regression), and the forwarding-runner over-block (`npx vitest run $FILE` etc.) DENIES under `high-severity` / ALLOWs under `off` (intended bounded over-block); + an invented logic rewrite — INDEPENDENT logic probe (D-12, P23).
    - (c) The both-direction proof matches `25-08-RED-baseline.txt` (pre-fix observed, literal-path control DENY) → `25-08-GREEN-proof.txt` (post-fix DENY; bounded-fail-safe controls ALLOW), vs the committed `.js`.
    - (d) The round-4 + round-3 + round-2 closures hold; the four floor invariants hold; `git diff --quiet hooks/guard.ts` exits 0; `git diff --quiet scripts/context-io.ts` exits 0; `npm run freshness` exits 0; WR-01 no false-positive; the single-authority + allowlist + JS_RUNNERS-distinct + bash-grounded-fuzz structural assertions hold; findings documented in `25-08-SUMMARY.md` — independently reproduced.
  </acceptance_criteria>
  <action>
    HARD STOP — do NOT auto-pass on a green author suite (the D-12 failure mode this phase hit FIVE times in execution AND TWICE on paper for round-5). Dispatch (or hand off to the human to dispatch) an INDEPENDENT opus-grade red-team to reproduce (a)-(d) against the COMMITTED `hooks/admission-guard.js` and `scripts/context-io.js` (never the `.ts`, never the author's suites). The probe MUST run BOTH a LOGIC probe (arg-cmd-sub / param / xargs / npx+node value-flags / JS_RUNNERS incl. `bun $S $V` / the D-01 glob self-set / dial-`all` routine-via-rewrite / a multi-admit shield / the T-40 boundary + the unknown-runtime residual `qjs $S $V` / the PRE-EXISTING name-resolution residual `cp scripts/context-io.js /tmp/x.js && node /tmp/x.js admit` / the FORWARDING-runner over-block `npx vitest run $FILE`) AND an INPUT-SURFACE attack (glob/EXTGLOB/brace/quote-removal/tilde/process-sub script-or-verb, or an inert mention reading as a live admit). The probe must confirm the THREE disclosed accepted limits (unknown-runtime, rename/symlink, forwarding-runner over-block) behave EXACTLY as the threat model states — that the rename forge is the PRE-EXISTING name-based limit (the committed `.js` ALLOWs it too, so it is NOT a round-5 regression), and that the SC1 closure is correctly SCOPED to command-string shape-hiding, not overstated. The adversary should INVENT ≥1 rewrite per angle the author did NOT enumerate (a deeper extglob, a process-sub script, a glob in the note-path, a randomized metachar, an unknown JS runtime) and confirm it is caught structurally. Record the reproduced commands, the invented forms, the clean=allow / planted=deny outcomes, the over-block + unknown-runtime-residual controls, the preserved GAP-B/C/D + Class A/B/E/F + round-4 a/b/c results, and confirm guard.ts + context-io.ts byte-unchanged + freshness 0 + the second walk deleted + `COMMAND_MODIFIERS`/`LAUNCHERS` not widened + `JS_RUNNERS` a distinct set + `tokenIsFinalLiteral` an allowlist + the bash-grounded fuzz in `25-08-SUMMARY.md`. Then surface to the human for the resume signal. This task is the phase safety floor — it blocks until the independent both-angle proof is in hand.
  </action>
  <verify>
    <human-check>An independent opus-grade red-team has reproduced (a)-(d) vs the committed `.js`: each input-surface rewrite (glob / EXTGLOB / brace / quote-removal / tilde / process-sub / static-quoted-admit) DENIES, each logic form (arg-cmd-sub / param / xargs / npx-value-flag / node-value-flag / JS_RUNNERS incl. `bun $S $V` / shebang) DENIES, the D-01 glob self-set + dial-`all` routine-via-rewrite + nested compounds DENY (+ an invented rewrite per angle, + the T-40 boundary & `qjs $S $V` residual probed), while `node build $HOME/out` / `node …validate` / `cp $A $B` / `qjs $S $V` / inert mention ALLOW; the both-direction proof matches the RED→GREEN artifacts (literal-path control DENY); round-1..4 + the four floor invariants hold with `git diff --quiet hooks/guard.ts` exit 0, `git diff --quiet scripts/context-io.ts` exit 0, freshness 0, the second walk deleted, `COMMAND_MODIFIERS`/`LAUNCHERS` not widened, `JS_RUNNERS` distinct, `tokenIsFinalLiteral` an allowlist, the literal-substring not the gating path, the fuzz bash-grounded, and `liveTokens` byte-identical; findings in `25-08-SUMMARY.md`. NOT a self-reported author green suite (D-12).</human-check>
  </verify>
  <resume-signal>Type "approved" once the independent both-angle red-team has reproduced (a)-(d) above and the findings (incl. the invented forms per angle + the T-40 / unknown-runtime residual probe) are in 25-08-SUMMARY.md, or describe the bypass / false-positive found. Do NOT approve on a green author suite alone (D-12, the terminal lesson — hit FIVE times in execution + TWICE on paper).</resume-signal>
  <done>An independent opus-grade red-team (BOTH angles) has reproduced the round-5 closure vs the committed `.js`: every expansion / rewrite (glob/extglob/brace/quote-removal/tilde/process-sub) / launcher-flag (npx/node value-flag) / JS_RUNNERS (incl. bun $S $V) / shebang form is DENIED, the nested compounds + D-01 glob self-set + dial-`all` routine-via-rewrite are DENIED, an invented un-enumerated rewrite per angle + the T-40 boundary + the unknown-runtime residual are probed, the recognized-non-admit-launcher / no-anchor-unrecognized (`cp $A $B`) / unknown-runtime (`qjs $S $V`) / inert mentions ALLOW, the round-1..4 closures + four floor invariants + guard.ts/context-io.ts byte-frozen status hold, the second walk is deleted, `COMMAND_MODIFIERS`/`LAUNCHERS` not widened, `JS_RUNNERS` distinct, `tokenIsFinalLiteral` an allowlist, the fuzz bash-grounded, `liveTokens` byte-identical, findings in `25-08-SUMMARY.md`, and the human approved; SC1 is closed by an independent adversary, not a green suite (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bash `tool_input.command` → admission-guard hook | The command runs through **bash** (not POSIX sh). The agent can defer the SCRIPT-ref or admit VERB to bash's runtime REWRITE — glob, extglob, parameter/command/arithmetic/process/ANSI-C substitution, brace, tilde, history, word-split, quote-removal — or use a non-node JS runner (bun/deno/ts-node) or a shebang. The hook requires PROOF a script/verb token is an inert final literal (an ALLOWLIST of inert chars + raw free of `$`/backtick); anything else is UNRESOLVABLE → fail CLOSED. The shell, not the hook, forms the final argv. |
| script-position token → SCRIPT-anchored disposition | RULE 1 (position-free): a final-literal context-io token anywhere → classify the immediately-following verb. RULE 2 (hidden script): direct runners pin the script (value-flag-arity skip) and gate if unresolvable while a trailing dynamic arg is ignored; forwarding runners ({npx} ∪ JS_RUNNERS) gate on any unresolvable. Launcher recognition AVOIDS over-block; an unrecognized non-JS-runner needs a concrete literal anchor (`cp $A $B` allows). |
| JS-execution capability → JS_RUNNERS | `{bun,bunx,deno,ts-node}` can RUN a context-io admit, so they are recognized (forwarding) launchers (`bun $S $V` gates). DISTINCT from COMMAND_MODIFIERS (resolves a wrapped command word) and LAUNCHERS. An unknown runtime (`qjs`/`llrt`) with both positions dynamic is the disclosed accepted residual. |
| nested wrapper + rewrite → round-4 catch-all | A wrapper in FRONT of a rewrite-obfuscated launcher consults the SAME structural detector (unrecognized-command scan), so the compound gates; the literal-substring fallback is eliminated. |
| inline approval-var set → refuse-self-set (D-01 floor) | `commandHasLiveAdmitShape` derives from the one authority, so a self-set with a structurally-detected admit-or-unresolvable segment is refused even behind a rewrite. |
| governance config value → hook / admit() | (PRESERVED) present non-string/non-object/corrupt → gate-or-stricter; only exact `"off"` is off-equivalent; absent stays lean. |
| admit() → audit ledger | (Preserved) under `retained`, the ledger records the admission record only, never the body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-31 | Spoofing / Elevation (script-ref rewrite incl. EXTGLOB / glob / brace / tilde / process-sub / quote-removal — BLOCKER B) | a gated admit whose SCRIPT-ref is rewritten (`node scripts/context-i@(o).js admit`, `…context-i*.js`, `node <ctx> admi{t,x}`, `node ~/x/context-io.js admit`, `node <(…) admit`, `node scripts/'context-io'.js admit`) | high | mitigate | ALLOWLIST `tokenIsFinalLiteral`: a script/verb token whose de-quoted value is not exclusively inert chars (or whose raw has `$`/backtick) is UNRESOLVABLE → gate — structurally catching extglob `@(`/`?(`/`+(`/`!(` and any future metachar without a denylist. Proven by RED vs committed `.js` + the BASH-GROUNDED fuzz (Task 25-08-01/02/03; D-12). |
| T-25-32 | Spoofing / Elevation (argument-position command substitution) | `node $(echo … admit …)` / backtick | high | mitigate | the DYNAMIC_COMMAND_WORD sentinel + the raw `$`/backtick check → unresolvable → gate (Task 25-08-01/02/03; D-12). |
| T-25-33 | Spoofing / Elevation (parameter-expansion / word-split) | `node "$S" admit`, `node <ctx> "$V" …`, `$A` | high | mitigate | a `$` in the raw → not final-literal → unresolvable → gate; a literal admit verb behind a dynamic script → RULE 2 / unrecognized scan gates (Task 25-08-01/02/03; D-12). |
| T-25-34 | Spoofing / Elevation (cross-segment stdin→argv) | `echo "…admit…" \| xargs node` | high | mitigate | `STDIN_ARGV_FEEDERS` (xargs) → a launcher behind a feeder has a stdin-sourced (unresolvable) script → gate (Task 25-08-01/02/03; D-12). |
| T-25-35 | Spoofing / Elevation (non-node JS runner / shebang — BLOCKER 2 + the literal-substring fallback) | `bun`/`bunx`/`deno run`/`ts-node scripts/context-i*.js admit`; shebang `scripts/context-io.js admit` | high | mitigate | the structural detector applies to ANY command word; JS_RUNNERS are recognized launchers; the round-4 catch-all + `commandHasLiveAdmitShape` use `segmentAdmitDisposition` (shebang = command word as script candidate), never the literal substring (Task 25-08-01/02/04; D-12). |
| T-25-36 | Elevation (D-01 floor breach via rewrite) | `GRUGOPS_ADMISSION_APPROVED_BY=eve node …context-i*.js admit` | critical | mitigate | `commandHasLiveAdmitShape` derives from the one authority → self-set + structurally-detected admit/unresolvable → refused even behind a rewrite (Task 25-08-01/02; D-12). |
| T-25-37 | Elevation (dial-`all` backstop gap) | under `all` a routine admit via rewrite, no in-script backstop | critical | mitigate (hook tier) / accept (no context-io.ts change) | the structural detector re-closes it at the un-forgeable hook tier; `scripts/context-io.ts` byte-frozen (the self-settable D-05 tier adds no un-forgeable defense; preserves GAP-C/GAP-D + ledger) (Task 25-08-01/02/03; D-12). |
| T-25-38 | Spoofing / Elevation (nested wrapper + rewrite compound) | `timeout 5 node …context-i@(o).js admit`, `nice -n 5 bun <ctx> admit` | high | mitigate | the round-4 catch-all consults the SAME `segmentAdmitDisposition` → the compound gates (Task 25-08-01/02/03/04; D-12). |
| T-25-39 | Denial of Service (over-block regression / npx-flag regression — BLOCKER A) | naive flag-skipping pinning `npx -p foo tsx … admit` to `foo` → ALLOW; OR gating a proven-non-admit launcher (`node build $HOME/out`) | high | mitigate (fail-safe) | RULE 1 is POSITION-FREE (literal context-io found regardless of value-flags → `npx -p foo … admit` gates); RULE 2 pins precisely (value-flag arity for direct runners) so `node build $HOME/out` allows (literal script, trailing dynamic arg ignored). Asserted in the suite + the npx/node value-flag regression-guard rows (Task 25-08-01/02; D-12). |
| T-25-40 | Tampering (single-authority drift / enumeration regression) + the bounded over-block (incl. forwarding-runner breadth) / unknown-runtime residual | reintroducing a second parser, widening `COMMAND_MODIFIERS`/`LAUNCHERS`, a denylist; the bounded `node $SCRIPT build` over-block; the BROAD forwarding-runner over-block (`npx vitest run $FILE`, `bun app.js $ARG`, `deno run server.ts $PORT`, `ts-node $SCRIPT` DENY while governance active — any unresolvable token in an npx/bunx/deno/ts-node invocation gates); OR the `qjs $S $V` unknown-runtime residual | medium | mitigate (drift) / accept (over-block + residual) | the detector lives in the ONE `liveAdmitSegments` walk (`tokenize`/`noteFileFromCommand` grep=0; `liveTokens` byte-identical; `COMMAND_MODIFIERS`/`LAUNCHERS` unchanged; `JS_RUNNERS` a distinct set; an allowlist not a denylist); the proof is the bash-grounded invariant. The forwarding-runner over-block is the ACCEPTED, DISCLOSED price of the forwarding fail-closed (opt-in; gate asks a human, never destroys work; a static/direct command avoids it) — NOT tightened by runner semantics (a closed-enumeration anti-pattern). It is asserted as an intended GATED control + ALLOW-under-`off` in the oracle (Task 25-08-02), not silent. The unknown-runtime residual NARROWS to a runner NOT in LAUNCHERS ∪ JS_RUNNERS with both positions dynamic and no literal anchor (`qjs $S $V`; the named runners gate) — explicit ACCEPT with that precondition; the red-team probes both. NOTE (disclosure 3, accepted bounded over-block): RULE 1's `includes("context-io")` substring can false-match a final-literal non-script arg that merely contains the substring (`node logger.js scripts/context-io.config.js admit` gates) — strictly TIGHTER than the committed `segmentHasAdmitShape` (which already substring-matches), so no regression. |
| T-25-41 | Spoofing / Elevation (NAME-based forge — script rename/symlink; PRE-EXISTING, out of hook-tier scope) | a real admit run via a renamed/symlinked copy of the script under a non-context-io name (`cp scripts/context-io.js /tmp/x.js && node /tmp/x.js admit T NOTE`, or a symlink): RULE 1 finds no literal `context-io`, RULE 2-direct pins `/tmp/x.js` (final-literal, non-context-io) → none → ALLOW, while the renamed copy runs a real admit | medium | accept (out of hook-tier scope; disclosed) | A FUNDAMENTAL limit of name-based command-string detection: the hook anchors on the literal `context-io` script name, so a copy under a different name evades it. This is PRE-EXISTING in the committed `.js` (the `context-io` substring anchor — NOT a round-5 regression) and is out of hook-tier reach; the in-script `admit()` D-04/D-05 tier (which reads the actual note + role) is the only place a rename forge could be backstopped, and the plan deliberately keeps `scripts/context-io.ts` BYTE-FROZEN this round (the self-settable D-05 tier adds no un-forgeable defense; touching it risks a GAP-C/GAP-D / ledger regression). DISCLOSED here + in the residual list + the softened SC1 wording; the red-team confirms `node <renamed-context-io>.js admit` ALLOWs as the disclosed pre-existing residual, not a silent gap. A future round may add a name-resolution / inode-aware backstop at the script tier (out of scope). |
| T-25-SC | Tampering | npm/pip/cargo installs | high | accept | N/A — installs NO external package (typescript/vitest already present); no install task. |
</threat_model>

<verification>
- `npm run build && npm run freshness` exits 0 (committed `admission-guard.js` is a fresh, byte-faithful tsc build; 0 drift).
- `git diff --quiet hooks/guard.ts` exits 0 (deploy guard byte-frozen, blob `3501810e21308e4b7e219679a6ca30dace9b5d66`, D-02).
- `git diff --quiet scripts/context-io.ts` exits 0 (NOT edited this round — GAP-C/GAP-D + the GOV-02 audit ledger preserved).
- `npx vitest run hooks/admission-guard.test.ts` green: every round-5 form (incl. EXTGLOB, npx-value-flag, node -r, JS_RUNNERS incl. `bun $S $V`, shebang) DENIES; the nested compounds DENY; the D-01 glob self-set DENIES; the dial-`all` routine-via-rewrite DENIES; the over-block controls (recognized non-admit launcher, `node build $HOME/out`, `node …validate`, `cp $A $B`, `qjs $S $V`, non-admit wrapper, routine-only multi-admit, inert mention) ALLOW; the round-1..4 closures still hold; case count >= guard.test.ts.
- `npx vitest run scripts/floor-invariance.test.ts` green: the BASH-GROUNDED property fuzz (a `bashRewrites(token)` oracle spawning bash; extglob/history/process-sub/brace/tilde/ANSI-C/quote-removal/randomized chars; independent of the detector); the round-2/3/4 sweeps; the four named floor invariants.
- A grep asserts `function tokenize` / `noteFileFromCommand` gone (single authority), `COMMAND_MODIFIERS` / `LAUNCHERS` unchanged (no widening; `JS_RUNNERS` a distinct new set), `tokenIsFinalLiteral` is an allowlist, the literal-substring scan is not the gating path, and `liveTokens` is unedited.
- `npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial; D-13).
- `npx vitest run --exclude '**/scripts/e2e/**'` green (full non-e2e regression — bare `npm test` deliberately NOT run; it triggers the live claude-CLI e2e lane, MEMORY grugops-npm-test-triggers-live-e2e).
- `25-08-RED-baseline.txt` + `25-08-GREEN-proof.txt` capture the both-direction proof vs the committed `.js` (the literal-path control DENIES on both sides).
- INDEPENDENT both-angle red-team (Task 25-08-04, before /gsd-verify-work): confirms SC1 round-5 closure adversarially — not self-reported green (D-12).
</verification>

<success_criteria>
- SC1 (SCOPED, no-fabrication wording): the admission-guard's COMMAND-STRING detection is no longer defeated by shell expansion, quoting, extglob, or runner indirection of a context-io reference — the detector requires PROOF a script/verb token is an inert final literal (ALLOWLIST `tokenIsFinalLiteral`, catching extglob + any metachar) and is SCRIPT-anchored (RULE 1 position-free literal-context-io; RULE 2 hidden-script direct-vs-forwarding), applied to ANY command word (recognized launcher / JS_RUNNERS / shebang) and never the literal substring; glob/arg-cmd-sub/param/xargs/extglob/brace/quote-removal/non-node/shebang/npx-flag + the nested compound + the D-01 glob self-set + the dial-`all` routine-via-rewrite are closed; the ONE detector feeds the launcher branch, the round-4 catch-all, and the floor scan, with `COMMAND_MODIFIERS`/`LAUNCHERS` unwidened (`JS_RUNNERS` distinct), the second walk deleted, and `liveTokens` byte-identical. The residual NAME-resolution limit (a renamed/symlinked script copy run under its new name) is OUT OF HOOK-TIER SCOPE, PRE-EXISTING (not a round-5 regression), and disclosed in the threat model (T-25-41) — the SC1 closure is deliberately NOT claimed as full un-forgeability.
- PRESERVED (no regression): round-1..4 closures, SC2, the GOV-02 ledger, the 25-04/05/06 segmentation; `scripts/context-io.ts` not edited.
- Fail-safe (bounded over-block, fully disclosed): a recognized non-admit launcher (`node build $HOME/out`, `node …validate`), an unrecognized non-JS-runner with no concrete anchor (`cp $A $B`, `tar $A $B`, `git log $REF`), a non-admit wrapper, a routine-only multi-admit, an inert mention behind a wrapper ALLOW. The bounded over-block markers (`node $SCRIPT build`) AND the BROADER forwarding-runner over-block (`npx vitest run $FILE` / `bun app.js $ARG` / `deno run server.ts $PORT` / `ts-node $SCRIPT` DENY under active governance, ALLOW under `off` — the accepted, opt-in, avoidable price of the forwarding fail-closed, asserted as an intended GATED control) are explicit; and TWO under-block residuals are disclosed (T-25-40/T-25-41): the `qjs $S $V` unknown-runtime forge and the pre-existing `node /tmp/<renamed-context-io>.js admit` name-resolution forge both ALLOW (out of hook-tier scope).
- Proof (D-12): both-direction RED→GREEN vs the COMMITTED `.js` + a property fuzz whose oracle is GROUNDED IN ACTUAL BASH (not the detector) + an INDEPENDENT both-angle opus red-team that invents an un-enumerated rewrite per angle and probes the T-40 boundary — not a green suite. `hooks/guard.ts` + `scripts/context-io.ts` byte-unchanged; freshness 0; the second walk deleted; `COMMAND_MODIFIERS`/`LAUNCHERS` unwidened; `JS_RUNNERS` distinct; `liveTokens` byte-identical.
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-08-SUMMARY.md` when done. Record: the ALLOWLIST `tokenIsFinalLiteral` (de-quoted value exclusively `A-Za-z0-9/._:=,-`, raw free of `$`/backtick, not the sentinel — and WHY the allowlist does not over-block real context-io script paths / the verbs) replacing the rev-1 denylist (which missed extglob); the SCRIPT-anchored RULE 1 (position-free literal-context-io → verb check, closing the npx-flag regression) / RULE 2 (hidden script: direct-runner value-flag-arity pin vs forwarding-runner any-unresolvable); `JS_RUNNERS = {bun,bunx,deno,ts-node}` as a JS-execution-capability set DISTINCT from COMMAND_MODIFIERS (closing `bun $S $V` while `cp $A $B` allows); how the detector applies to ANY command word and replaces the literal substring on the round-4 catch-all + the D-01 floor; confirmation `COMMAND_MODIFIERS`/`LAUNCHERS` were NOT widened (`JS_RUNNERS` a separate set), no second walk (`function tokenize`/`noteFileFromCommand` grep=0), `liveTokens` byte-identical; the bash-grounded fuzz oracle (spawns bash, not `tokenIsFinalLiteral` — closing the rev-1 circularity); the THREE disclosed accepted limits — (i) the bounded `node $SCRIPT build` over-block PLUS the BROADER forwarding-runner over-block (`npx vitest run $FILE` / `bun app.js $ARG` / `deno run server.ts $PORT` / `ts-node $SCRIPT` gate under active governance; opt-in, avoidable, asserted as an intended GATED control), (ii) the UNKNOWN-runtime residual (`qjs $S $V`), and (iii) the PRE-EXISTING NAME-resolution residual (`node /tmp/<renamed-context-io>.js admit` ALLOWs — out of hook-tier scope, NOT a round-5 regression) — and that the SC1 closure is SCOPED to command-string shape-hiding (deliberately NOT claimed as full un-forgeability, per no-fabrication); the dial-`all`/context-io.ts-unchanged decision + justification; confirmation `scripts/context-io.ts` is unedited (`git diff --quiet` exit 0); the RED→GREEN proof artifact paths; `git diff --quiet hooks/guard.ts` clean + freshness 0; and the INDEPENDENT both-angle red-team's reproduced commands + invented forms (both angles) + the T-40 boundary probe + outcomes + the preserved round-1..4 results. Flag for the verifier that SC1 closure REQUIRES the independent red-team — the sweeps/fuzz are necessary, not sufficient (D-12). Note that SC2 + the GOV-02 ledger (25-01/02/03), the round-4 (25-07), round-3 (25-06), and round-2 (25-05) closures are PRESERVED untouched.
</output>

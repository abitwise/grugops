---
phase: 15-typescript-tooling-migration
plan: 03
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - install/install.ts
  - install/install.js
  - install/uninstall.ts
  - install/uninstall.js
  - install/install.test.ts
autonomous: true
requirements: [TOOL-01]
user_setup: []

must_haves:
  truths:
    - "The single install.ts is additive, idempotent (double-install zero-diff), DRY_RUN-safe (no mutation), reversible (uninstall removes only grugops-owned wiring), and never overwrites/deletes user content (D-07/D-08)"
    - "install.ts NEVER sets the prod deploy-approval env var and NEVER touches agent-factory/, plans/, .planning/, user data"
    - "Every env-var name is preserved byte-identically: GRUGOPS_SRC, GRUGOPS_HOME, TARGET, INSTALL_MODE, DRY_RUN (installer); GRUGOPS_SRC, TARGET, DRY_RUN (uninstaller)"
    - "The two-root resolution, --check doctor (D-03 three-source cross-check, ordered first-failure, WARN tier, exit matrix), and the D-07 self-checkout guard are ported 1:1 — refactor nothing semantic"
    - "The retired sh-vs-Node byte-parity check (old Check 4) is INTENTIONALLY absent and explicitly marked in install.test.ts per D-08 — its absence is not a regression (Pitfall 6)"
    - "The install/uninstall sentinel strings stay byte-identical across the pair, and CR-01 bounded marker-strip (unterminated block restores buffered lines, never silent content loss) is preserved"
  artifacts:
    - path: "install/install.ts"
      provides: "single TS installer (D-07; replaces install.sh + install.mjs), additive/idempotent/DRY_RUN/reversible/never-overwrite"
      contains: "GRUGOPS_HOME"
    - path: "install/install.js"
      provides: "committed compiled installer (freshness-checked per D-02)"
      contains: "GRUGOPS_HOME"
    - path: "install/uninstall.ts"
      provides: "TS uninstaller (D-09; replaces uninstall.sh), is_protected denylist + CR-01 bounded removal"
      contains: "is_protected"
    - path: "install/uninstall.js"
      provides: "committed compiled uninstaller"
      min_lines: 1
    - path: "install/install.test.ts"
      provides: "Vitest harness folding install.test.sh + install.two-root.test.sh; D-08 retired-parity marker; idempotent/DRY_RUN/round-trip cases"
      contains: "D-08"
  key_links:
    - from: "install/install.ts"
      to: "host FS (adapters, seed state, marker)"
      via: "copyKit -> materializeAdapter -> ensureBlock -> seedState -> writeMarker"
      pattern: "materializeAdapter|seedState|writeMarker"
    - from: "install/install.test.ts"
      to: "install/install.js + install/uninstall.js"
      via: "spawnSync the compiled .js into mkdtemp host fixtures, snapshot + diff trees"
      pattern: "spawnSync|mkdtempSync"
    - from: "install/install.ts sentinels"
      to: "install/uninstall.ts sentinels"
      via: "byte-identical sentinel strings so blocks are removable"
      pattern: "CLAUDE_OPEN|MAT_OPEN"
---

<objective>
Collapse the dual installer (`install/install.sh` POSIX + `install/install.mjs` Node) into a single `install/install.ts` (compiled `install/install.js`) per D-07, and port `install/uninstall.sh` → `install/uninstall.ts` per D-09 — both behavior-preserving translations. Migrate `install/install.test.sh` + `install/install.two-root.test.sh` into one Vitest suite `install/install.test.ts` that asserts the single installer's contract (additive, idempotent, DRY_RUN, reversible, never-overwrite) and carries the **D-08 retired-parity marker**.

Purpose: Satisfy TOOL-01 for the installer at exact behavior parity while executing the explicit SC4 decision (D-07) to drop the zero-Node POSIX install path — Node becomes a documented hard prerequisite, the dual sh/Node byte-parity contract is retired (D-08). The new `install.ts` also gains the integration seam for Plan 05's D-11 materialization (a `materializeRunnable` action is added there).
Output: `install.ts`/`.js` + `uninstall.ts`/`.js` (committed pairs), `install.test.ts` (Vitest). Old `.sh`/`.mjs`/`.test.sh` are KEPT until parity is green (deleted in Plan 06).

CLEAR PROFESSIONAL VOICE governs every installer report/warning/error string — safety surface, never caveman.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-typescript-tooling-migration/15-CONTEXT.md
@.planning/phases/15-typescript-tooling-migration/15-PATTERNS.md
@install/install.mjs
@install/uninstall.sh
@docs/design/shared-install.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port install.ts + uninstall.ts (1:1 behavior, all env-vars + sentinels + doctor preserved)</name>
  <files>install/install.ts, install/install.js, install/uninstall.ts, install/uninstall.js</files>
  <read_first>
    - install/install.mjs — READ IN FULL (753 lines). This is the closest analog — port THIS, not `install.sh` (the `.mjs` is already Node + stdlib-only; `install.sh` is the byte-spec it mirrors). Preserve: the env-var resolution block (`GRUGOPS_SRC`, `DRY_RUN`, `INSTALL_MODE` copy-default, `GRUGOPS_HOME` empty-string-falls-back, `KIT_ROOT`); the arg-parse exit contract (unknown arg -> exit 2; flags `--target[=]`, `--yes/-y`, `--allow-self/--force`, `--symlink`, `--check`, `--strict`); `ensureBlock` (sentinel append, skip-if-open-sentinel-present, never `>`-truncate); `linkOrCopy` (D-30 symlink+copy-fallback, skip-if-identical); `materializeAdapter` (strip-then-inject KIT line, CR-01 bounded removal, MAT_OPEN/MAT_CLOSE/MAT_SLOT sentinels byte-identical); `seedFile`/`seedState`/`listSeedFiles` (skip-if-exists, LC_ALL=C sort order); `writeMarker` (4 stable fields, fixed order, timestamp omitted, overwrite-unconditionally); `doctor()` (non-mutating --check, absent-marker -> notInstalled+return 1, D-03 three-source kit-root cross-check, ordered first-failure stat set, WARN tier, exit matrix, docAbspath non-normalizing). Safety: the D-07 self-checkout guard; NEVER set the deploy-approval env var; never touch agent-factory/, plans/, user data.
    - install/uninstall.sh — READ IN FULL (356 lines). Exact inverse; port 1:1. Preserve: env `GRUGOPS_SRC`/`TARGET`/`DRY_RUN`, bad-arg -> exit 2; the `is_protected` denylist (agent-factory/ plans/ .planning/ .grugops/ docs/ src/ + repo root) checked before every removal; the same sentinel strings as the installer (CLAUDE_OPEN/CLOSE, COPILOT_OPEN/CLOSE — byte-identical or blocks won't remove); `remove_file` (guard-then-rm); `remove_sentinel_block` (CR-01 bounded removal — unterminated block restores buffered lines, never silent content loss); `unmerge_gemini` (Node JSON edit, removes file only if it returns to grugops-default shape); `remove_marker` (the single `.grugops/install.json` exception); AGENTS.md removed ONLY if symlink-into-source or byte-identical copy.
    - .planning/phases/15-typescript-tooling-migration/15-PATTERNS.md — the `install/install.ts` and `install/uninstall.ts` sections (exact line refs to every function) and Shared Patterns (sentinel-block idempotency strings must stay byte-identical across the pair; import.meta.dirname; fail-closed JSON parsing for readMarker)
    - .planning/phases/15-typescript-tooling-migration/15-CONTEXT.md — D-07 (single install.ts, Node a hard prerequisite), D-09 (uninstall.sh -> uninstall.ts), and the Established-Patterns installer contract
  </read_first>
  <action>
    Author `install/install.ts` as a single-file port of `install/install.mjs` (avoid relative imports -> sidesteps Pitfall 4). Add types only; change NOTHING semantic. Preserve every env-var NAME byte-identically (`GRUGOPS_SRC`, `GRUGOPS_HOME`, `TARGET`, `INSTALL_MODE`, `DRY_RUN` — tests + docs + the two-root contract read these). Port every function named in read_first 1:1: arg-parse (unknown arg -> `process.exit(2)`), `ensureBlock`, `linkOrCopy`, `materializeAdapter` (sentinels byte-identical), `seedFile`/`seedState`/`listSeedFiles`, `writeMarker`, `doctor()` (--check non-mutating, D-03 cross-check, ordered first-failure, WARN tier, exit matrix). Keep the D-07 self-checkout guard. Keep the never-set-deploy-approval + never-touch-protected-dirs invariants. Use `import.meta.dirname` for `SCRIPT_DIR`. Reads of the install marker / config parse stay fail-closed (try/catch -> null, reject non-object before dereference). Recursive copy stays `cpSync(src,dst,{recursive:true})` (ports 1:1). Reserve the main-install sequence seam where `materializeRunnable()` will be inserted in Plan 05 (between `seedState()` and `writeMarker()`), but do NOT add it here — leave a clear `// D-11 materialization seam (Plan 05)` comment.
    Author `install/uninstall.ts` as a 1:1 port of `install/uninstall.sh`: `is_protected` denylist, `remove_file`, `remove_sentinel_block` (CR-01 bounded), `unmerge_gemini`, `remove_marker`, AGENTS.md byte-identical-or-symlink-only removal. Sentinel strings MUST equal `install.ts`'s exactly.
    Every report/warn/error string in both files stays CLEAR PROFESSIONAL VOICE (preserve the existing wording).
    Compile: `npx tsc` emits committed `install/install.js` + `install/uninstall.js`. Confirm `tsc --noEmit` clean and committed `.js` are LF.
    Keep `install/install.sh`, `install/install.mjs`, `install/uninstall.sh` in-tree as parity oracles (deleted in Plan 06).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node install/install.js --bad-arg-xyz >/dev/null 2>&1; test $? -eq 2 && echo "unknown-arg exit-2 contract preserved"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `node install/install.js --bad-arg-xyz` exits 2 (unknown-arg contract preserved).
    - `grep -c "GRUGOPS_SRC\|GRUGOPS_HOME\|INSTALL_MODE\|DRY_RUN\|TARGET" install/install.ts` confirms every installer env-var name is present (no silent rename — Runtime State Inventory).
    - `grep -q "is_protected\|isProtected" install/uninstall.ts` is true (the never-delete-user-content denylist ported).
    - The MAT_OPEN/MAT_CLOSE/CLAUDE_OPEN/CLAUDE_CLOSE sentinel strings in `install.ts` are byte-identical to those in `uninstall.ts` (a `diff <(grep -oE 'GSD:[A-Za-z0-9_-]+' install/install.ts | sort -u) <(grep -oE 'GSD:[A-Za-z0-9_-]+' install/uninstall.ts | sort -u)` of the shared sentinel set shows no drift on the shared ones).
    - `grep -nE "GRUGOPS_PROD_DEPLOY_APPROVED\s*=" install/install.ts` returns nothing (installer never sets the deploy-approval var).
    - `grep -q "D-11 materialization seam" install/install.ts` is true (the Plan-05 seam is reserved).
  </acceptance_criteria>
  <done>The single `install.ts` and `uninstall.ts` are verbatim-behavior ports: every env-var name, sentinel string, the doctor, the self-checkout guard, and the never-overwrite/never-set-deploy-var invariants intact; the materialization seam is reserved for Plan 05; both compile clean to committed LF `.js`.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest install harness (folds install.test.sh + two-root) with the D-08 retired-parity marker</name>
  <files>install/install.test.ts</files>
  <read_first>
    - install/install.test.sh — READ IN FULL (615 lines). Reproduce: Check 1 idempotent zero-diff (double-install snapshot equality); Check 2 DRY_RUN no-change; Check 3 install->uninstall removes grugops-owned wiring while seeded user state + frozen core survive. The temp-fixture + cleanup idiom (`mktemp -d` + `trap`), the content-addressed snapshot (walk + hash a tree), the hermetic env overrides (`INSTALL_MODE=copy GRUGOPS_SRC GRUGOPS_HOME TARGET`). **Old Check 4 ("install.sh tree == install.mjs tree") is the sh-vs-Node parity check — D-08 RETIRES it; do NOT reproduce it.**
    - install/install.two-root.test.sh — READ IN FULL (346 lines). Fold its cases in: snapshot BOTH `$TARGET` and `$GRUGOPS_HOME`; the `run_install` env overrides; the D-06 round-trip cases (grugops-owned adapters+sentinel removed on uninstall; seeded user state survives).
    - .planning/phases/15-typescript-tooling-migration/15-PATTERNS.md — the `install.test.ts` reframe section (the REQUIRED D-08 marker; the three retained checks) and the test-harness Shared Patterns (mkdtempSync + afterEach rmSync; readdir-recursive + hash snapshot helper; spawnSync env overrides)
    - .planning/phases/15-typescript-tooling-migration/15-RESEARCH.md — Pattern 3 (Vitest harness, temp-dir fixtures) and Pitfall 6 (the byte-parity test's absence read as a regression — the marker prevents this)
  </read_first>
  <action>
    Author `install/install.test.ts` as a Vitest suite folding `install.test.sh` + `install.two-root.test.sh`. Use `beforeEach`/`afterEach` with `mkdtempSync(join(tmpdir(),"grugops-"))` + `rmSync({recursive,force})` for hermetic host fixtures. Write a small `snapshot(dir)` helper: recursive `readdirSync` + per-file content hash, returning a stable map (so two install states diff regardless of inode) — snapshot BOTH `$TARGET` and `$GRUGOPS_HOME` for the two-root cases. Drive the COMMITTED `install/install.js` and `install/uninstall.js` via `spawnSync("node", [js, ...args], { env: { ...process.env, INSTALL_MODE:"copy", GRUGOPS_SRC, GRUGOPS_HOME, TARGET } })`.
    Reproduce: Check 1 idempotent (install twice -> snapshots equal); Check 2 DRY_RUN=1 (no mutation -> pre/post snapshots equal); Check 3 install->uninstall round-trip (grugops-owned adapters + sentinel blocks removed, seeded user state + frozen `agent-factory/` survive).
    **REQUIRED D-08 marker:** add a top-of-file comment AND a dedicated `it.skip` or commented test block explicitly stating: "The sh-vs-Node byte-parity check (old install.test.sh Check 4) is intentionally retired per D-08 — there is no POSIX installer left to keep in parity. This suite asserts the single installer's contract (additive/idempotent/DRY_RUN/reversible/never-overwrite) instead. Its absence is not a regression (RESEARCH Pitfall 6)." The literal token `D-08` MUST appear so the verifier finds it.
    Keep `install.test.sh` + `install.two-root.test.sh` in-tree as parity oracles (deleted in Plan 06).
  </action>
  <verify>
    <automated>npx vitest run install/install.test.ts && grep -q "D-08" install/install.test.ts && echo "install harness green + D-08 marker present"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run install/install.test.ts` exits 0 (all retained checks green).
    - `grep -c "D-08" install/install.test.ts` ≥ 1 (the retired-parity marker is present, per Pitfall 6).
    - `grep -qi "parity" install/install.test.ts` is true and the marker text states the sh-vs-Node check is intentionally retired.
    - The suite contains an idempotent zero-diff case, a DRY_RUN no-mutation case, and an install->uninstall round-trip case (grep the test names for "idempotent"/"DRY_RUN"/"uninstall").
    - `grep -q "mkdtempSync" install/install.test.ts` is true (hermetic temp fixtures, not the repo).
  </acceptance_criteria>
  <done>The Vitest install harness asserts the single installer's full contract (additive/idempotent/DRY_RUN/reversible/never-overwrite) over both `$TARGET` and `$GRUGOPS_HOME`, carries the explicit D-08 retired-parity marker, and is green against the committed `.js` — Pitfall 6 is pre-empted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grugops installer -> host repo filesystem | `install.ts` writes adapters, sentinel blocks, seed state, and a marker into a user's repo; user content must never be overwritten or deleted. |
| install marker / config JSON -> installer parse | The doctor reads `.grugops/install.json`; a garbled/absent marker must fold into a clean FAIL, never crash or false-green. |
| flags/env -> installer behavior | `--target`, `GRUGOPS_HOME`, `DRY_RUN` etc. steer where writes land; an unknown arg must exit 2, never proceed ambiguously. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-Tamper | Tampering | installer overwriting/deleting user content | mitigate | Additive/never-overwrite contract preserved (`ensureBlock` never `>`-truncates, `seedFile` skip-if-exists, `linkOrCopy` skip-if-identical); uninstall's `is_protected` denylist refuses agent-factory/ plans/ .planning/ .grugops/ docs/ src/; Vitest round-trip asserts seeded user state survives. |
| T-15-03-Tamper2 | Tampering | CR-01 unbounded marker-strip deleting content past an unterminated sentinel | mitigate | `remove_sentinel_block` CR-01 bounded removal ported verbatim (unterminated block restores buffered lines, never silent loss); sentinel strings byte-identical across install/uninstall so blocks are correctly bounded. |
| T-15-03-EoP | Elevation of Privilege | installer setting the deploy-approval env var | mitigate | Installer NEVER sets `GRUGOPS_PROD_DEPLOY_APPROVED` (asserted: no `GRUGOPS_PROD_DEPLOY_APPROVED=` write in `install.ts`); the D-07 self-checkout guard prevents installing over the running checkout. |
| T-15-03-Spoof | Spoofing / false-green | doctor reading a garbled marker as installed | mitigate | `readMarker` fail-closed JSON parse (try/catch -> null, reject non-object); the D-03 three-source kit-root cross-check + ordered first-failure fold a garbled marker into a clean not-installed FAIL (ports the CR-02/CR-03 hard-won fixes). |
| T-15-03-Info | Information (verification gap) | the retired sh-vs-Node parity test read as a missing regression | accept (intentional) | D-08 retires the parity check; the explicit `D-08` marker in `install.test.ts` documents the intentional absence (Pitfall 6) so verification does not flag it. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run install` exits 0 (idempotent / DRY_RUN / round-trip all green).
- `node install/install.js --bad-arg-xyz` exits 2 (unknown-arg contract).
- The `D-08` retired-parity marker is present in `install.test.ts` (Pitfall 6).
- Every installer env-var name is present in `install.ts`; the uninstaller's `is_protected` denylist is present; the installer sets no deploy-approval var.
- install/uninstall sentinel strings are byte-identical across the pair.
</verification>

<success_criteria>
The dual installer is collapsed into a single behavior-parity `install.ts` (+ `uninstall.ts`) per D-07/D-09: every env-var, sentinel, the doctor, the self-checkout guard, the CR-01 bounded marker-strip, and the never-overwrite/never-delete/never-set-deploy-var invariants are preserved. The Vitest harness asserts the single installer's contract and carries the D-08 retired-parity marker so its intentional absence is not a regression. TOOL-01 is satisfied for the installer at exact parity, with the D-11 materialization seam reserved for Plan 05.
</success_criteria>

<output>
Create `.planning/phases/15-typescript-tooling-migration/15-03-SUMMARY.md` when done. Note the reserved `materializeRunnable` seam location (between `seedState()` and `writeMarker()`) for Plan 05, and that `install.sh`/`install.mjs`/`uninstall.sh`/`*.test.sh` remain in-tree as parity oracles until Plan 06.
</output>

---
phase: 15-typescript-tooling-migration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - .gitattributes
  - .gitignore
  - package-lock.json
  - scripts/freshness.ts
  - scripts/freshness.js
autonomous: true
requirements: [TOOL-01]
user_setup: []

must_haves:
  truths:
    - "tsc --noEmit type-checks the .ts source clean (the D-01 type-check payoff)"
    - "npm run freshness exits 0 when committed .js matches committed .ts, non-zero on planted drift (D-02)"
    - "vitest run executes (empty/zero suites is acceptable at Wave 0) — the runner is installed and configured (D-06)"
    - "node_modules/ is gitignored; the lockfile is committed as the supply-chain integrity anchor (D-04)"
    - "committed .js files carry LF endings deterministically so the freshness diff is cross-platform stable (Pitfall 1)"
  artifacts:
    - path: "package.json"
      provides: "type:module manifest, engines.node>=22, dev-deps {typescript,vitest,@types/node}, scripts build/typecheck/test/freshness (D-04/D-05)"
      contains: "\"type\": \"module\""
    - path: "tsconfig.json"
      provides: "nodenext/es2022/strict/newLine:lf/noEmitOnError/declaration:false/sourceMap:false (D-01/D-03, Pattern 1)"
      contains: "nodenext"
    - path: "vitest.config.ts"
      provides: "minimal Vitest config (node defaults)"
      min_lines: 1
    - path: ".gitattributes"
      provides: "pins committed *.js to eol=lf for the tool dirs (Pitfall 1 freshness determinism)"
      contains: "eol=lf"
    - path: ".gitignore"
      provides: "ignores node_modules/ (D-04)"
      contains: "node_modules"
    - path: "scripts/freshness.ts"
      provides: "D-02 drift gate: rebuild-to-temp -> cmp committed .js -> exit 0/1, structurally clones generate-asvs-checklist.mjs reproducibility check"
      min_lines: 20
    - path: "package-lock.json"
      provides: "committed lockfile — supply-chain integrity record (D-04)"
      min_lines: 1
  key_links:
    - from: "package.json scripts.freshness"
      to: "scripts/freshness.js"
      via: "npm run freshness invokes the compiled drift gate"
      pattern: "freshness"
    - from: "tsconfig.json newLine + .gitattributes eol=lf"
      to: "committed *.js determinism"
      via: "two-half LF fix (Pitfall 1)"
      pattern: "lf"
---

<objective>
Land the TypeScript toolchain scaffolding every downstream Wave-1+ migration depends on: `package.json` (D-04), `tsconfig.json` (D-01/D-03), `vitest.config.ts` (D-06), `.gitattributes` LF pin (Pitfall 1), `.gitignore` for `node_modules/`, the committed lockfile (D-04), and the `scripts/freshness.ts` drift gate (D-02). This is the formal start of the ratified TS pivot's build posture: `tsc`-compile to committed `.js`, dev-deps confined to grugops's own CI, zero runtime deps shipped to hosts (D-05).

Purpose: Nothing else can compile, test, or freshness-check until this exists. The biggest correctness risk in the whole phase — freshness-check false-red from line endings (RESEARCH Pitfall 1) — is mitigated here once, in the `.gitattributes` LF pin + tsconfig `newLine:"lf"`.
Output: 8 scaffolding files (6 net-new config + the freshness gate `.ts`/`.js` pair), the dev-dep set installed, the lockfile committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-typescript-tooling-migration/15-CONTEXT.md
@.planning/phases/15-typescript-tooling-migration/15-RESEARCH.md
@.planning/phases/15-typescript-tooling-migration/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toolchain scaffolding — package.json, tsconfig.json, vitest.config.ts, .gitattributes, .gitignore, lockfile</name>
  <files>package.json, tsconfig.json, vitest.config.ts, .gitattributes, .gitignore, package-lock.json</files>
  <read_first>
    - .planning/phases/15-typescript-tooling-migration/15-RESEARCH.md — § "Code Examples" (the `package.json` and `.gitattributes` examples), § "Architecture Patterns" Pattern 1 (the tsconfig.json), § "Validation Architecture" (vitest.config.ts minimal shape), and Pitfall 1 (the LF determinism rationale — both halves are mandatory)
    - .planning/phases/15-typescript-tooling-migration/15-CONTEXT.md — D-01 (tsc not type-stripping), D-03 (Node 22+ floor), D-04 (package.json + lockfile arrive), D-05 (dev-dep set {typescript, vitest} + the @types/node confirm), D-06 (Vitest)
    - .planning/phases/15-typescript-tooling-migration/15-RESEARCH.md — Open Question 1 (@types/node major: pin ~22 to match the D-03 floor) and Assumption A1 (confirm @types/node is in-bounds under D-05's "minimal + justified")
  </read_first>
  <action>
    Create `package.json` with `"type": "module"`, `"private": true`, `"name": "grugops-tooling"`, `"engines": { "node": ">=22" }` (D-03 floor in the manifest), and scripts `build` (`tsc`), `typecheck` (`tsc --noEmit`), `test` (`vitest run` — non-watch, never bare `vitest`), `freshness` (`tsc --outDir .tmp-build && node scripts/freshness.js` — D-02). devDependencies pinned per RESEARCH: `typescript` `~6.0.3`, `vitest` `~4.1.8`, `@types/node` `~22` (the D-03-floor-matching choice per Open Question 1; @types/node is type-only, erased at compile, justified under D-05 per Assumption A1 — note this in a top-of-file comment is not possible in JSON, so record the justification in the SUMMARY instead).
    Create `tsconfig.json` per Pattern 1: `module: "nodenext"`, `moduleResolution: "nodenext"`, `target: "es2022"`, `strict: true`, `outDir: "./"`, `rootDir: "./"`, `newLine: "lf"`, `declaration: false`, `sourceMap: false`, `esModuleInterop: true`, `skipLibCheck: true`, `noEmitOnError: true` (fail-closed build — never emit a partial `.js` on a type error). `include`: `["install/**/*.ts", "scripts/**/*.ts", "hooks/**/*.ts"]`. EXCLUDE `node_modules` and `.tmp-build`.
    Create `vitest.config.ts` minimal: `import { defineConfig } from "vitest/config"; export default defineConfig({ test: {} });` — node defaults suffice (`environment:'node'`, `globals:false`).
    Create `.gitattributes` pinning the committed build outputs to LF (Pitfall 1, half one): `install/*.js text eol=lf`, `scripts/**/*.js text eol=lf`, `hooks/*.js text eol=lf`. Add a header comment naming the D-02 freshness-determinism reason in CLEAR voice (not caveman — this is a tooling/safety surface).
    Create `.gitignore` at repo root (none exists today) adding `node_modules/` and `.tmp-build/` (D-04 — the lockfile is committed, node_modules is not).
    Run `npm install --save-dev typescript@~6.0 vitest@~4.1 @types/node@~22` to generate and commit `package-lock.json` (the lockfile IS the supply-chain audit anchor per D-04). Do NOT add any other dependency — no `tsx`, `ts-node`, `glob`, linter (Phase 16), or test-helper libs (D-05, RESEARCH Pitfall 3).
    After `.gitattributes` lands, run `git add --renormalize .` once (Pitfall 1) so existing files normalize to the pinned endings.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.type!=='module') process.exit(1); for(const d of ['typescript','vitest','@types/node']){ if(!p.devDependencies[d]) { console.error('missing dev-dep '+d); process.exit(1);} } if(!p.scripts.freshness||!p.scripts.typecheck||!p.scripts.build||!p.scripts.test){console.error('missing script');process.exit(1);} console.log('package.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` has `"type":"module"`, `engines.node` `>=22`, all four scripts (build/typecheck/test/freshness), and exactly the three dev-deps `{typescript, vitest, @types/node}` — no fourth dependency present.
    - `tsconfig.json` parses and contains `"module": "nodenext"`, `"newLine": "lf"`, `"noEmitOnError": true`, `"strict": true`, `"declaration": false`.
    - `.gitattributes` contains `eol=lf` for `install/*.js`, `scripts/**/*.js`, `hooks/*.js`.
    - `.gitignore` contains `node_modules/`.
    - `git ls-files package-lock.json` returns the path (lockfile committed); `git check-ignore node_modules` returns `node_modules` (ignored).
    - `grep -RIl "ts-node\|tsx\|\"glob\"\|eslint" package.json` returns nothing (no forbidden dep).
  </acceptance_criteria>
  <done>The toolchain manifest + config + LF pin + lockfile exist; the dev-dep set is exactly the three justified packages; node_modules is ignored and the lockfile is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freshness drift gate (scripts/freshness.ts) + first compile</name>
  <files>scripts/freshness.ts, scripts/freshness.js</files>
  <read_first>
    - scripts/generate-asvs-checklist.mjs — the structural donor: its read -> regenerate -> compare reproducibility shape (lines ~44-92, the fixed-literal-path + `lines.join("\n")` + fail-closed posture). The freshness gate is a build-output analog of this check.
    - .planning/phases/15-typescript-tooling-migration/15-RESEARCH.md — Pattern 4 (freshness check — rebuild-to-temp + cmp, OR `tsc && git diff --exit-code`), the § "Don't Hand-Roll" freshness row (use `Buffer.equals` / `git diff --exit-code`, never a line-by-line diff parser), and the `package.json` `freshness` script form
    - .planning/phases/15-typescript-tooling-migration/15-PATTERNS.md — the `scripts/freshness.ts` row in "No Analog Found" (structurally clones the ASVS generator's reproducibility check; exit 0/1)
  </read_first>
  <behavior>
    - Test 1: against a tree where committed `.js` == freshly-compiled `.js`, `node scripts/freshness.js` exits 0 and prints a clear-voice "all build outputs fresh" line.
    - Test 2: against a tree with a planted drift (one committed `.js` byte-modified, or its `.ts` changed without recompiling), the gate exits non-zero and names the stale file in clear voice.
    - Test 3 (fail-closed): if the temp rebuild itself fails (a type error), the gate exits non-zero — never reports "fresh" on a broken build.
  </behavior>
  <action>
    Author `scripts/freshness.ts` (single file, `node:` builtins only — `fs`, `path`, `os`, `child_process` for the `tsc` spawn; `import.meta.dirname` for the repo root). Behavior: spawn `tsc --outDir <tmp>` (a fresh temp dir via `mkdtempSync(join(tmpdir(),"grugops-fresh-"))`); for each committed `.js` under `install/`, `scripts/`, `hooks/`, compare the committed bytes to the temp-rebuilt bytes using `Buffer.equals` (the `cmp`-equivalent, per Don't-Hand-Roll); on any mismatch, print `STALE: <path>` in clear professional voice and accumulate; clean up the temp dir; exit 1 if any stale OR the rebuild failed (fail-closed), else print a clear-voice "all build outputs fresh" and exit 0. Use the exit-code-as-signal idiom (D-12 shape): 0 = fresh, 1 = drift/error. Findings stdout in CLEAR PROFESSIONAL VOICE, never caveman (CLAUDE.md hard rule; this is a build-safety surface).
    Do NOT inline file creation via template strings for any emitted output (RESEARCH anti-pattern) — but note the freshness gate writes nothing permanent; it only reads + compares.
    Compile it: `npx tsc` (emits `scripts/freshness.js` committed alongside, per D-02). This is the first compile — confirm `tsc --noEmit` is clean and the committed `.js` is LF (the `.gitattributes` + `newLine:"lf"` pairing from Task 1).
    Commit BOTH `scripts/freshness.ts` and `scripts/freshness.js` (D-02 commit-both).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/freshness.js; test $? -eq 0 && echo "freshness GREEN on clean tree"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (source type-checks clean — the D-01 payoff).
    - `node scripts/freshness.js` exits 0 on the committed (fresh) tree and prints a clear-voice "fresh" line.
    - Planted-drift proof: temporarily append a byte to `scripts/freshness.js`, run `node scripts/freshness.js` — it exits non-zero and prints `STALE:` naming the file; revert the byte.
    - `grep -n "import" scripts/freshness.ts | grep -v "node:" | grep -v "import.meta"` returns nothing (node: builtins only — Pitfall 3).
    - `file scripts/freshness.js` / `git diff --check` shows no CRLF (LF determinism from Task 1).
  </acceptance_criteria>
  <done>The D-02 freshness gate exists as a committed `.ts`/`.js` pair, type-checks clean, passes green on the fresh tree, and fails red on planted drift — the safety net behind D-02 and D-10's stale-artifact mitigation is live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → grugops dev/CI | Dev-deps (`typescript`, `vitest`, `@types/node`) cross from an untrusted registry into grugops's build tier. They NEVER cross to host machines (D-05). |
| grugops repo → committed build outputs | `tsc` emits `.js` that becomes a committed, trusted artifact; the freshness gate is the integrity check on that boundary (D-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-SC | Tampering | npm dev-dep install (`typescript`/`vitest`/`@types/node`) | mitigate | Committed lockfile (D-04) is the supply-chain integrity anchor; all three are first-party canonical tooling verified against the npm registry + official repos (RESEARCH Package Legitimacy Audit, all `Approved`); `node_modules` never ships to hosts (D-05). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking-human checkpoint required (RESEARCH treats all three as `[VERIFIED: npm registry]` on authoritative-source basis). |
| T-15-01-02 | Tampering | committed `.js` build output drifting from `.ts` source | mitigate | `scripts/freshness.ts` rebuild-to-temp + `Buffer.equals` diff fails red on drift (D-02); `noEmitOnError:true` prevents a partial `.js` on a type error (fail-closed build). |
| T-15-01-03 | Tampering | freshness-check false-red from CRLF/LF mismatch masking a real drift signal | mitigate | `.gitattributes` `eol=lf` pin + tsconfig `newLine:"lf"` (Pitfall 1, both halves) + `git add --renormalize .` once; deterministic emit keeps the diff meaningful so a real drift is never lost in line-ending noise. |
| T-15-01-04 | Elevation of Privilege | a stray non-`node:` import re-introducing a host runtime dep via the build path | accept (this plan) / mitigate (Plan 05) | Freshness gate itself uses `node:` builtins only (asserted); the full "host emulation with no node_modules" test lands with the materialized reference routine in Plan 05 (RESEARCH Pitfall 3). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (D-01 type-check).
- `node scripts/freshness.js` exits 0 on the committed tree, non-zero on a planted drift (D-02).
- `npx vitest run` executes without a config error (D-06 runner installed — zero suites is acceptable at Wave 0).
- `git check-ignore node_modules` confirms the ignore; `git ls-files package-lock.json` confirms the committed lockfile (D-04).
- No forbidden dependency present (D-05): `grep` for `ts-node`/`tsx`/`eslint`/`glob` in `package.json` returns nothing.
</verification>

<success_criteria>
The toolchain compiles `.ts` → committed `.js` deterministically (LF-pinned), the freshness gate proves the committed `.js` is fresh and fails red on drift, the dev-dep set is exactly the three justified packages with a committed lockfile, and `node_modules` is ignored — the build posture for D-01/D-02/D-04/D-05/D-06 is established and ready for the Wave-1 migrations.
</success_criteria>

<output>
Create `.planning/phases/15-typescript-tooling-migration/15-01-SUMMARY.md` when done. Record the `@types/node` justification under D-05 (type-only, erased at compile, in-bounds per Assumption A1) and the chosen `@types/node` major pin.
</output>

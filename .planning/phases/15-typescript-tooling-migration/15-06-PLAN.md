---
phase: 15-typescript-tooling-migration
plan: 06
type: execute
wave: 3
depends_on: ["15-02", "15-03", "15-04", "15-05"]
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md
  - .planning/phases/13-frontend-ui-persona-design-build-workflow/13-CONTEXT.md
  - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-CONTEXT.md
  - install/README.md
  - README.md
  - AGENTS.md
  - .claude/skills/grugops/SKILL.md
  - .claude/agents/grugops-orchestrator.md
  - agent-factory/packaging/subagent.frontmatter.md
  - agent-factory/_commit-convention.md
  - docs/design/shared-install.md
  - docs/dogfood-human-runbook.md
  - agent-factory/checklists/security-nfr-checklist.md
  - install/install.sh
  - install/install.mjs
  - install/uninstall.sh
  - install/install.test.sh
  - install/install.two-root.test.sh
  - scripts/validate-agent-factory.mjs
  - scripts/validate.test.sh
  - scripts/generate-asvs-checklist.mjs
  - scripts/check-foundation-guards.sh
  - scripts/check-foundation-guards.test.sh
  - scripts/check-kit-refs.sh
  - hooks/guard.mjs
  - hooks/guard.test.sh
autonomous: false
requirements: [TOOL-01]
user_setup: []

must_haves:
  truths:
    - "The foundational constraint is formally amended in CLAUDE.md and PROJECT.md: tooling layer is TypeScript (tsc-compiled committed .js, freshness-checked); single Node-required install.ts (no POSIX installer); dev-deps {typescript, vitest}; zero runtime deps on hosts; Node 22+ floor (D-13)"
    - "The prior HELD notes in 12/13/14-CONTEXT.md are marked superseded (D-13)"
    - "No POSIX/.mjs original remains: all old .sh/.mjs/.test.sh are deleted only AFTER every migration suite is green (D-09)"
    - "Every shipped invocation-string reference to install.sh/install.mjs/guard.mjs/*.test.sh/*.mjs checkers is re-pointed to the new node <...>.js form"
    - "No env var was silently renamed in the migration (GRUGOPS_PROD_DEPLOY_APPROVED + the installer/validator env-var names survive byte-identical)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "amended tech-stack constraint recording the ratified TS pivot (D-13)"
      contains: "TypeScript"
    - path: ".planning/PROJECT.md"
      provides: "amended foundational constraint + a Key Decision row recording the TS pivot (D-13)"
      contains: "TypeScript"
    - path: "install/README.md"
      provides: "single Node-required install.ts documented; dual sh/Node + byte-parity language removed (D-07/D-08)"
      contains: "install.js"
  key_links:
    - from: "CLAUDE.md + PROJECT.md amendment"
      to: "12/13/14-CONTEXT.md HELD notes"
      via: "marked superseded by the TS pivot (D-13)"
      pattern: "superseded"
    - from: "shipped surfaces (AGENTS.md, README, adapters, hooks.json, docs)"
      to: "the new node <...>.js invocation form"
      via: "invocation-string sweep"
      pattern: "install\\.js|guard\\.js"
---

<objective>
Close the phase: formally amend the foundational constraint (D-13), run the two cross-cutting sweeps (invocation-string + env-var-name), and delete every POSIX/`.mjs` original now that all migrations are proven green (D-09). The constraint amendment records the ratified TS pivot in CLAUDE.md + PROJECT.md and marks the prior "HELD" notes in the Phase 12/13/14 contexts superseded. The invocation-string sweep re-points every shipped reference (`install.sh`/`install.mjs`/`guard.mjs`/`*.test.sh`/checker `.mjs`) to the new `node <...>.js` form. The env-var-name sweep confirms no env var was silently renamed (a renamed `GRUGOPS_PROD_DEPLOY_APPROVED` would silently disable the safety guard).

Purpose: Satisfy SC4 (the constraint amendment + the explicit-decision posture) and complete TOOL-01's "nothing POSIX remains" (D-09). Deletion happens LAST and only after parity is green, so the parity oracles survive through Waves 1-2. The sweep and the deletion are split into two tasks so the green-suite safety pre-condition gates the deletion explicitly.
Output: amended CLAUDE.md/PROJECT.md + 3 superseded HELD-note edits + the re-pointed shipped surfaces + the deleted originals.

This plan has a blocking `checkpoint:human-verify` for the D-13 prose amendment (per 15-VALIDATION.md Manual-Only Verifications — the amendment is prose, not machine-verifiable). CLEAR PROFESSIONAL VOICE for the amendment text (it touches the hard-safety constraint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-typescript-tooling-migration/15-CONTEXT.md
@.planning/phases/15-typescript-tooling-migration/15-VALIDATION.md
@CLAUDE.md
@install/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invocation-string + env-var-name sweeps across shipped surfaces (NO deletions)</name>
  <files>install/README.md, README.md, AGENTS.md, .claude/skills/grugops/SKILL.md, .claude/agents/grugops-orchestrator.md, agent-factory/packaging/subagent.frontmatter.md, agent-factory/_commit-convention.md, docs/design/shared-install.md, docs/dogfood-human-runbook.md, agent-factory/checklists/security-nfr-checklist.md</files>
  <read_first>
    - .planning/phases/15-typescript-tooling-migration/15-RESEARCH.md — § "Runtime State Inventory" the canonical question (the two sweeps: (1) invocation-string sweep re-points every doc/workflow/gate/AGENTS slot naming the old scripts; (2) env-var-name sweep confirms no var silently renamed)
    - .planning/phases/15-typescript-tooling-migration/15-PATTERNS.md — the "Also new (not files, but phase deliverables)" note (the invocation-string sweep targets install/README.md, docs/, AGENTS.md, the role-switch protocol; the env-var-name sweep)
    - install/README.md — the dual-installer language ("two functionally identical installers", "install.sh is the behavioral spec and install.mjs mirrors it byte-for-byte", the `sh install/install.sh` invocation examples) that must change to the single `node install/install.js` form (D-07/D-08)
    - AGENTS.md (line ~33), .claude/skills/grugops/SKILL.md (~21), .claude/agents/grugops-orchestrator.md (~16), agent-factory/packaging/subagent.frontmatter.md (~39) — the `install.sh (or install.sh --check)` self-heal hints to re-point
    - docs/dogfood-human-runbook.md (~29, ~83-85, ~133), agent-factory/_commit-convention.md (~70, ~81), agent-factory/checklists/security-nfr-checklist.md (~4, the generator-provenance header) — the `hooks/guard.mjs` / `hooks/guard.test.sh` / `scripts/*.mjs` references to re-point
  </read_first>
  <action>
    This task makes NO deletions — it only re-points strings and confirms env-var-name integrity. The originals stay in-tree as parity oracles until Task 2's green-suite gate clears them.
    Invocation-string sweep: in every SHIPPED surface (NOT `.planning/` history, NOT `docs/initial/` spec snapshots, NOT `examples/` narration), re-point old invocation strings to the new form: `sh install/install.sh` -> `node install/install.js`; `install.sh --check` -> `install.js --check` (or `node install/install.js --check`); `node install/install.mjs` -> `node install/install.js`; `hooks/guard.mjs` -> `hooks/guard.js`; `hooks/guard.test.sh` -> the Vitest suite name `hooks/guard.test.ts` (or `npx vitest run hooks`); `scripts/validate-agent-factory.mjs` -> `.js`; `scripts/generate-asvs-checklist.mjs` -> `.js` (the security-nfr-checklist provenance header); `scripts/check-foundation-guards.sh`/`check-kit-refs.sh` -> `.js`. In install/README.md, REMOVE the dual-installer + byte-parity language (D-08: there is no sh installer to keep in parity) and document the single Node-required `install.js` with Node 22+ as a stated prerequisite (D-07). The self-heal hints in the adapters become `install.js`/`install.js --check`.
    NOTE on materialized adapters: the adapter self-heal hint is the MATERIALIZED absolute-path form — update the SOURCE adapter/template strings (`subagent.frontmatter.md`, `.claude/skills/grugops/SKILL.md`, `.claude/agents/grugops-orchestrator.md`) so a fresh install materializes the new wording; do NOT hand-edit a materialized abs path that the installer regenerates.
    Env-var-name sweep: grep the new `.ts` sources for env-var assignments/reads and confirm NONE was renamed vs the originals — `GRUGOPS_PROD_DEPLOY_APPROVED`, `GRUGOPS_SRC`, `GRUGOPS_HOME`, `TARGET`, `INSTALL_MODE`, `DRY_RUN`, `VALIDATE_KIT_ROOT`, `VALIDATE_ROOT` must all appear byte-identical. Record the result (carry it to Task 2's SUMMARY note).
  </action>
  <verify>
    <automated>grep -rl -E "install\.(sh|mjs)|guard\.mjs|node .*\.mjs|sh install/install" AGENTS.md README.md install/README.md hooks/hooks.json .claude agent-factory/packaging agent-factory/_commit-convention.md docs/design docs/dogfood-human-runbook.md agent-factory/checklists/security-nfr-checklist.md 2>/dev/null | grep -q . && { echo "stale invocation strings remain"; exit 1; } || echo "invocation-string sweep clean"; for v in GRUGOPS_PROD_DEPLOY_APPROVED GRUGOPS_SRC GRUGOPS_HOME TARGET INSTALL_MODE DRY_RUN VALIDATE_KIT_ROOT VALIDATE_ROOT; do grep -rq "$v" hooks/guard.ts install/install.ts install/uninstall.ts scripts/validate-agent-factory.ts || { echo "MISSING $v"; exit 1; }; done && echo "env-var-name sweep clean"</automated>
  </verify>
  <acceptance_criteria>
    - Invocation-string sweep complete on shipped surfaces: `grep -rl -E "install\.(sh|mjs)|guard\.mjs|node .*\.mjs|sh install/install" AGENTS.md README.md install/README.md hooks/hooks.json .claude agent-factory/packaging agent-factory/_commit-convention.md docs/design docs/dogfood-human-runbook.md agent-factory/checklists/security-nfr-checklist.md 2>/dev/null` returns nothing (all re-pointed; `.planning/`, `docs/initial/`, `examples/` excluded).
    - install/README.md documents the single Node-required `install.js` (Node 22+ prerequisite) and no longer carries dual-installer / byte-parity language (D-07/D-08).
    - Env-var-name sweep clean: `for v in GRUGOPS_PROD_DEPLOY_APPROVED GRUGOPS_SRC GRUGOPS_HOME TARGET INSTALL_MODE DRY_RUN VALIDATE_KIT_ROOT VALIDATE_ROOT; do grep -rq "$v" hooks/guard.ts install/install.ts install/uninstall.ts scripts/validate-agent-factory.ts || echo "MISSING $v"; done` prints no MISSING line.
    - NO original `.sh`/`.mjs`/`.test.sh` was deleted in this task — the parity oracles are still in-tree for Task 2's gate.
  </acceptance_criteria>
  <done>Every shipped invocation-string reference points at the new `node <...>.js` form, install/README.md documents the single Node-required installer, and the env-var-name sweep confirms no var was renamed — with the original parity oracles still present for Task 2's green-suite-gated deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the 13 POSIX/.mjs originals + .test.sh parity oracles — GATED by a green full suite (D-09)</name>
  <files>install/install.sh, install/install.mjs, install/uninstall.sh, install/install.test.sh, install/install.two-root.test.sh, scripts/validate-agent-factory.mjs, scripts/validate.test.sh, scripts/generate-asvs-checklist.mjs, scripts/check-foundation-guards.sh, scripts/check-foundation-guards.test.sh, scripts/check-kit-refs.sh, hooks/guard.mjs, hooks/guard.test.sh</files>
  <read_first>
    - .planning/phases/15-typescript-tooling-migration/15-RESEARCH.md — § "Runtime State Inventory" the build-artifact row (delete old .sh/.mjs ONLY AFTER the parity oracle is green; update all references) and § "Behavior-Parity Proof Strategy" step 4 (only after the migrated suites prove green do you delete the old `.sh`/`.mjs` + `.test.sh`)
    - .planning/phases/15-typescript-tooling-migration/15-PATTERNS.md — the "Also new (not files, but phase deliverables)" note (delete every old .sh/.mjs + .test.sh after the parity oracle is green)
  </read_first>
  <action>
    SAFETY PRE-CONDITION (the gate): re-run the full migration suite BEFORE deleting anything — `npm run build && npm run freshness && npx vitest run`. If ANY suite is red, STOP — do NOT delete the parity oracles; the migration is not proven and the oracles are the safety net. This green-suite check is the FIRST acceptance criterion below and the literal precondition of every deletion in this task.
    Only once the full suite is green: DELETE the 13 originals (D-09 — nothing POSIX remains): `install/install.sh`, `install/install.mjs`, `install/uninstall.sh`, `install/install.test.sh`, `install/install.two-root.test.sh`, `scripts/validate-agent-factory.mjs`, `scripts/validate.test.sh`, `scripts/generate-asvs-checklist.mjs`, `scripts/check-foundation-guards.sh`, `scripts/check-foundation-guards.test.sh`, `scripts/check-kit-refs.sh`, `hooks/guard.mjs`, `hooks/guard.test.sh`.
    Re-run the full suite + the kit-refs + foundation-guards checkers AFTER deletion to confirm nothing dangles: `npm run build && npm run freshness && npx vitest run && node scripts/check-kit-refs.js && node scripts/check-foundation-guards.js`. The kit-refs checker confirms no shipped surface still names a now-deleted file (Task 1's sweep made this true; this re-run proves it).
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run && node scripts/check-kit-refs.js && node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - GREEN-SUITE GATE (first, blocking): `npm run build && npm run freshness && npx vitest run` is all green BEFORE any deletion — if red, no file is deleted and the task STOPS.
    - No old original remains: `ls install/install.sh install/install.mjs install/uninstall.sh hooks/guard.mjs scripts/check-foundation-guards.sh scripts/generate-asvs-checklist.mjs 2>/dev/null` lists nothing (D-09).
    - No `*.test.sh` remains under install/, scripts/, hooks/: `git ls-files '*.test.sh' | grep -E '^(install|scripts|hooks)/'` returns nothing.
    - `npm run build && npm run freshness && npx vitest run` all green AFTER deletion; `node scripts/check-kit-refs.js` and `node scripts/check-foundation-guards.js` exit 0 (nothing dangles after deletion).
  </acceptance_criteria>
  <done>Every POSIX/.mjs original + .test.sh parity oracle is deleted (D-09), but only after the full migration suite proved green first; the kit-refs + foundation-guards checkers confirm nothing dangles afterward.</done>
</task>

<task type="auto">
  <name>Task 3: Amend the foundational constraint (D-13) in CLAUDE.md + PROJECT.md; mark prior HELD notes superseded</name>
  <files>CLAUDE.md, .planning/PROJECT.md, .planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md, .planning/phases/13-frontend-ui-persona-design-build-workflow/13-CONTEXT.md, .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-CONTEXT.md</files>
  <read_first>
    - .planning/phases/15-typescript-tooling-migration/15-CONTEXT.md — D-13 in full (the exact amendment content: tooling layer is TypeScript, tsc-compiled committed .js, freshness-checked; single Node-required install.ts, no POSIX installer; dev-deps {typescript, vitest}; zero runtime deps on hosts; Node 22+ floor) and the named supersession targets (12-CONTEXT.md ~line 156, 13-CONTEXT.md ~line 142, 14-CONTEXT.md ~lines 26, 57, + D-03's "no TypeScript" rule)
    - CLAUDE.md — the "Constraints" Tech-stack line ("Markdown for everything except installers (install.sh POSIX + install.mjs Node)...") and the Recommended-Stack table rows (POSIX sh, Node.js ESM, "Only add package.json if one already exists") that the amendment supersedes
    - .planning/PROJECT.md — the foundational-constraint statement + the Key Decisions table where a TS-pivot row is recorded
  </read_first>
  <action>
    Amend CLAUDE.md "Constraints" in CLEAR PROFESSIONAL VOICE: update the Tech-stack line and the Recommended-Stack rows to record the ratified posture — the tooling layer (installers, validator, generators, guards) is TypeScript, `tsc`-compiled to committed `.js` and freshness-checked; a single Node-required `install.ts` replaces the dual sh/Node installer (the POSIX installer is dropped, Node is a hard install prerequisite); dev-deps are `{typescript, vitest}` (+ type-only `@types/node`), dev/CI-only, never shipped to hosts; zero runtime deps on host machines (the real spirit of the old no-npm-deps rule is preserved — committed `.js` needs nothing installed); Node 22+ floor. Keep the "markdown for everything else" + "no fabrication" + hard-safety constraints intact — only the tooling-stack rows change. Frame it as an amendment that supersedes the prior "install.sh POSIX + install.mjs Node / stdlib-only / no package.json" rule.
    Amend .planning/PROJECT.md: update the foundational-constraint statement to match, and add a Key Decisions row recording the TS pivot (date 2026-06-13, ratified) with the four pillars (tsc-compiled committed .js + freshness; single Node-required install.ts; dev-deps {typescript, vitest}; Node 22+ floor).
    Mark the prior HELD notes superseded: in 12-CONTEXT.md (~line 156), 13-CONTEXT.md (~line 142), and 14-CONTEXT.md (~lines 26, 57, + the D-03 "no TypeScript" rule), append a clear-voice "SUPERSEDED by the Phase-15 TypeScript pivot (D-13, ratified 2026-06-13)" note beside each HELD line — do NOT delete the original HELD text (preserve the history; mark it superseded).
  </action>
  <verify>
    <automated>grep -qi "TypeScript" CLAUDE.md && grep -qi "TypeScript" .planning/PROJECT.md && for f in .planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md .planning/phases/13-frontend-ui-persona-design-build-workflow/13-CONTEXT.md .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-CONTEXT.md; do grep -qi "supersed" "$f" || { echo "MISSING supersession in $f"; exit 1; }; done && echo "D-13 amendment present"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md records the TS pivot: `grep -qi "TypeScript" CLAUDE.md` is true and the dual-installer / "install.sh POSIX + install.mjs Node" stack line is amended to the single Node-required `install.ts` posture.
    - .planning/PROJECT.md records the amended constraint + a Key Decisions row naming the TS pivot.
    - Each of 12/13/14-CONTEXT.md contains a "superseded" note beside its prior HELD line (the original HELD text is preserved, not deleted).
    - The amendment text is CLEAR PROFESSIONAL VOICE (it touches the hard-safety tooling constraint — no caveman in this surface).
  </acceptance_criteria>
  <done>The foundational constraint is formally amended in CLAUDE.md + PROJECT.md to the ratified TS posture, and the prior HELD notes in the Phase 12/13/14 contexts are marked superseded (history preserved) — SC4's amendment requirement is met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (checkpoint): Human review of the D-13 constraint amendment + HELD-note supersessions</name>
  <action>Pause for the developer to review the prose D-13 amendment and the three supersession edits (the 15-VALIDATION.md Manual-Only row — prose, not machine-verifiable). The agent presents the diffs and waits for the resume signal; it does not self-approve.</action>
  <what-built>The D-13 foundational-constraint amendment (CLAUDE.md + PROJECT.md) and the three "superseded" HELD-note edits in 12/13/14-CONTEXT.md. This is the prose amendment the 15-VALIDATION.md Manual-Only Verifications row flags as human-reviewed (not machine-verifiable).</what-built>
  <how-to-verify>
    1. Read the amended "Constraints" + "Recommended Stack" sections of `CLAUDE.md` — confirm they record: tooling layer is TypeScript (tsc-compiled committed `.js`, freshness-checked); single Node-required `install.ts` (POSIX installer dropped, Node a hard prerequisite); dev-deps `{typescript, vitest}` (+ type-only `@types/node`) dev/CI-only; zero runtime deps on hosts; Node 22+ floor. Confirm the "markdown for everything else", "no fabrication", and hard-safety constraints are intact.
    2. Read the amended foundational-constraint statement + the new Key Decisions row in `.planning/PROJECT.md`.
    3. Open each of `12-CONTEXT.md`, `13-CONTEXT.md`, `14-CONTEXT.md` and confirm the prior HELD note(s) now carry a clear "superseded by the Phase-15 TS pivot (D-13)" annotation, with the original HELD text preserved.
    4. Confirm the amendment voice is clear professional English throughout (this surface touches the hard-safety tooling constraint).
  </how-to-verify>
  <resume-signal>Type "approved" if the amendment + supersessions read correctly, or describe what to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration completeness -> deletion of originals | The POSIX/.mjs originals are the parity oracles; deleting them before parity is green would remove the safety net. Deletion is gated on a green full suite (Task 2). |
| renamed env var -> silently disabled safety guard | A renamed `GRUGOPS_PROD_DEPLOY_APPROVED` in the port would silently disable the prod-deploy guard; the env-var-name sweep is the integrity check (Task 1). |
| shipped invocation strings -> host self-heal | Adapters/docs that name the old scripts would send a user to a deleted file; the invocation-string sweep keeps the self-heal correct (Task 1, before deletion). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-Tamper | Tampering | env var silently renamed in the port disabling the prod-deploy guard | mitigate | The env-var-name sweep (Task 1) asserts `GRUGOPS_PROD_DEPLOY_APPROVED` + the installer/validator env-var names appear byte-identical in the new `.ts` sources; a MISSING line fails the gate. |
| T-15-06-DoS | Denial of Service / broken-self-heal | a shipped surface naming a deleted `.sh`/`.mjs` sending a user/agent to a missing file | mitigate | The invocation-string sweep (Task 1) re-points every shipped reference to `node <...>.js`; the kit-refs + foundation-guards checkers run after deletion (Task 2) to confirm nothing dangles. |
| T-15-06-Avail | Availability | deleting the parity oracles before parity is proven, losing the safety net | mitigate | Deletion (Task 2) is GATED: `npm run build && npm run freshness && npx vitest run` must be green BEFORE any original is removed (the task's first acceptance criterion). |
| T-15-06-Repud | Repudiation | the HELD-note history erased rather than marked superseded | accept (controlled) | The supersession appends a "superseded" annotation and PRESERVES the original HELD text (history kept) — verified by the human checkpoint. |
| T-15-06-SC | Tampering | npm/dev-dep supply chain (carried context — no new install in this plan) | accept | No new package install in this plan; the lockfile + dev-dep audit are anchored in Plan 01 (T-15-01-SC). No `[ASSUMED]`/`[SUS]`/`[SLOP]` package introduced here. |
</threat_model>

<verification>
- Invocation-string sweep clean on shipped surfaces BEFORE deletion (Task 1): no `install.sh`/`install.mjs`/`guard.mjs`/`*.mjs` checker references remain in AGENTS.md, README, install/README, adapters, hooks.json, docs.
- Env-var-name sweep clean (Task 1): no var renamed.
- Pre-deletion gate green (Task 2): `npm run build && npm run freshness && npx vitest run` before any original is removed.
- No POSIX/.mjs original or `*.test.sh` remains under install/, scripts/, hooks/ after Task 2 (D-09).
- `node scripts/check-kit-refs.js` + `node scripts/check-foundation-guards.js` exit 0 after deletion (nothing dangles).
- D-13 amendment present in CLAUDE.md + PROJECT.md; supersession notes present in 12/13/14-CONTEXT.md; human checkpoint approved.
</verification>

<success_criteria>
The foundational constraint is formally amended to the ratified TS posture (D-13, human-reviewed), the prior HELD notes are marked superseded, every shipped invocation-string reference is re-pointed to the new `node <...>.js` form, no env var was renamed, and every POSIX/.mjs original + .test.sh harness is deleted — but only after the full migration suite proved green. SC4 and TOOL-01's "nothing POSIX remains" (D-09) are complete; the phase is consistent and self-referentially correct.
</success_criteria>

<output>
Create `.planning/phases/15-typescript-tooling-migration/15-06-SUMMARY.md` when done. Record the env-var-name sweep result, the green-suite gate outcome that authorized deletion, the list of deleted originals, and confirmation the human checkpoint approved the D-13 amendment.
</output>

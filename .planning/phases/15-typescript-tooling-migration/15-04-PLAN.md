---
phase: 15-typescript-tooling-migration
plan: 04
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - scripts/validate-agent-factory.ts
  - scripts/validate-agent-factory.js
  - scripts/generate-asvs-checklist.ts
  - scripts/generate-asvs-checklist.js
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - scripts/check-kit-refs.ts
  - scripts/check-kit-refs.js
  - scripts/validate.test.ts
  - scripts/check-foundation-guards.test.ts
  - scripts/generate-asvs-checklist.test.ts
  - agent-factory/checklists/security-nfr-checklist.md
autonomous: true
requirements: [TOOL-01]
user_setup: []

must_haves:
  truths:
    - "The validator ports two-root aware: VALIDATE_KIT_ROOT no-default C3 guard fires on unset, CR-03 fail-closed-on-null rejected before dereference, --strict promotes warnings, the production_requires_human_confirmation safety invariant preserved"
    - "The ASVS generator emits the byte-identical checklist (the 345-row freshness + security-checklist provenance both break otherwise)"
    - "The foundation-guards checker ports all six guards: WARN never fails the build, only FAIL does; WR05 EREs + voice fence-strip + caveman-preserved + role_ceiling table + the 17-file ROLE_FILES list ported verbatim; missing-file fails red (no vacuous green)"
    - "The kit-refs checker ports the explicit SCAN set (never a repo-wide grep), the 16-template ALLOW ERE, the MARKER substring, and the 3 assertions + SC2 marker check"
    - "Every RED-by-design test harness still fails red on a planted regression and passes green on the migrated code (parity oracles: the old .test.sh files)"
  artifacts:
    - path: "scripts/validate-agent-factory.ts"
      provides: "two-root structure validator port (C3 + CR-03 preserved, refactor nothing semantic per Open Q4)"
      contains: "VALIDATE_KIT_ROOT"
    - path: "scripts/generate-asvs-checklist.ts"
      provides: "ASVS generator port — byte-reproducible 345-row checklist, fail-closed load chain"
      contains: "345"
    - path: "scripts/check-foundation-guards.ts"
      provides: "six-guard aggregator port, WARN-never-fails, read-only"
      contains: "ROLE_FILES"
    - path: "scripts/check-kit-refs.ts"
      provides: "kit-ref checker port over the explicit SCAN set"
      contains: "SCAN"
    - path: "scripts/validate.test.ts"
      provides: "Vitest validator harness (two-root perms, C3 unset-kit + missing-kit cases)"
      min_lines: 40
    - path: "scripts/check-foundation-guards.test.ts"
      provides: "Vitest plant-one-violation-per-guard harness, asserts RED + smoke-green"
      min_lines: 40
  key_links:
    - from: "scripts/generate-asvs-checklist.ts"
      to: "agent-factory/checklists/security-nfr-checklist.md"
      via: "deterministic lines.join + writeFileSync; output byte-identical to the .mjs"
      pattern: "writeFileSync"
    - from: "scripts/check-foundation-guards.ts"
      to: "agent-factory/roles + workflows"
      via: "read-only guard scan over ROLE_FILES + SEC_VOICE_FILES"
      pattern: "ROLE_FILES"
    - from: "scripts/validate.test.ts"
      to: "scripts/validate-agent-factory.js"
      via: "spawnSync with VALIDATE_KIT_ROOT/VALIDATE_ROOT, assert exit + (C3) finding"
      pattern: "VALIDATE_KIT_ROOT"
---

<objective>
Port the four `scripts/` checkers/generators to TypeScript at exact behavior parity and migrate their `.test.sh` harnesses to Vitest: `validate-agent-factory.mjs` -> `.ts` (two-root validator), `generate-asvs-checklist.mjs` -> `.ts` (byte-reproducible ASVS generator), `check-foundation-guards.sh` -> `.ts` (six-guard aggregator), `check-kit-refs.sh` -> `.ts` (kit-ref checker, D-09 full sweep). Each is a translation, not a redesign — preserve every frozen name list, regex, exit code, fail-closed branch, and the hard-won C3/CR-03 fixes verbatim (RESEARCH Open Q4: refactor nothing semantic in the resolution logic).

Purpose: Satisfy TOOL-01 for the checker/generator layer at exact parity. The ASVS generator's byte-reproducibility is load-bearing twice over — the D-02 freshness gate AND the security-checklist provenance both break if the regenerated checklist drifts a single byte.
Output: 4 checker `.ts`/`.js` committed pairs + 3 Vitest harnesses + the regenerated `security-nfr-checklist.md` (proven byte-identical). Old `.mjs`/`.sh`/`.test.sh` are KEPT until parity is green (deleted in Plan 06).

CLEAR PROFESSIONAL VOICE governs every finding/error string — these are quality/safety surfaces, never caveman.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-typescript-tooling-migration/15-CONTEXT.md
@.planning/phases/15-typescript-tooling-migration/15-PATTERNS.md
@scripts/generate-asvs-checklist.mjs
@scripts/validate-agent-factory.mjs
@scripts/check-foundation-guards.sh
@scripts/check-kit-refs.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port validator + ASVS generator (.mjs -> .ts, byte-reproducible, C3/CR-03 preserved)</name>
  <files>scripts/validate-agent-factory.ts, scripts/validate-agent-factory.js, scripts/generate-asvs-checklist.ts, scripts/generate-asvs-checklist.js, agent-factory/checklists/security-nfr-checklist.md</files>
  <read_first>
    - scripts/validate-agent-factory.mjs — READ IN FULL (541 lines). Port behavior 1:1 (Open Q4: refactor nothing in the resolution logic). Preserve: `STATE_ROOT` (VALIDATE_ROOT else repo root) + the `VALIDATE_KIT_ROOT` no-default C3 guard (unset -> hard error with the literal `(C3)` message + exit 1); the fail-closed FS helpers forked per root (try/catch -> null/false/[]); the CR-03 fail-closed-on-null reject (non-object parse result rejected before dereference in `checkConfig` AND `checkPackaging` — `JSON.parse("null")` returns null without throwing); the frozen name lists (WORKFLOWS 14, FROZEN_HANDOFFS 16, ROLES 16, CHECKLISTS 11, ROLE_SECTIONS, WORKFLOW_SECTIONS — copy verbatim, never re-derive); the `production_requires_human_confirmation` safety invariant (presence-guarded, absent=lean true, only explicit false rejected); `--strict` warning->nonzero promotion; exit 0 ALL CHECKS PASSED / 1 N ERROR(S).
    - scripts/generate-asvs-checklist.mjs — READ IN FULL (160 lines). Byte-reproducibility IS the contract. Preserve: the fixed literal paths + source pin (never argv/env/content-derived); the fail-closed load chain (read -> parse -> non-object reject -> missing-array reject -> row-count must == 345 or refuse to write a partial); the deterministic emit (`lines[]` array + `writeFileSync(OUT, lines.join("\n"), "utf8")` — NO template-string/echo emit; deterministic output is what the freshness diff depends on); exit 0/1.
    - .planning/phases/15-typescript-tooling-migration/15-PATTERNS.md — the `scripts/validate-agent-factory.ts` and `scripts/generate-asvs-checklist.ts` sections (exact line refs) + Shared Patterns (fail-closed JSON parsing; read-only/write-only-by-construction path-traversal mitigation; import.meta.dirname)
  </read_first>
  <action>
    Author `scripts/validate-agent-factory.ts` as a single-file 1:1 port of the `.mjs`. Add types only; change NOTHING semantic. Preserve the C3 unset-kit guard (literal `(C3)` message), the CR-03 fail-closed-on-null rejects in both `checkConfig` and `checkPackaging`, every frozen name list verbatim, the `production_requires_human_confirmation` safety invariant, `--strict` promotion, and the exit contract. Replace `dirname(fileURLToPath(import.meta.url))` with `import.meta.dirname`. Every read/write path stays `join(ROOT, <fixed literal>)` (path-traversal mitigation).
    Author `scripts/generate-asvs-checklist.ts` as a single-file 1:1 port. Preserve the source pin (SHA + EXPECTED_ROWS=345), the fail-closed load chain, and the deterministic `lines.join("\n")` + `writeFileSync` emit byte-for-byte. Then RUN the compiled `generate-asvs-checklist.js` to regenerate `agent-factory/checklists/security-nfr-checklist.md` and PROVE byte-identity: run the old `.mjs` and the new `.js` each to a temp output and `cmp -s` them — they MUST be byte-identical (parity oracle = exact-output, RESEARCH Behavior-Parity step 5). If the regenerated checklist differs from the currently-committed one by anything other than a generator-provenance header line that names the script, STOP — a drift means the port is not faithful.
    Compile both: `npx tsc` emits committed `.js`. Confirm `tsc --noEmit` clean and committed `.js` LF.
    Keep `validate-agent-factory.mjs` + `generate-asvs-checklist.mjs` in-tree as parity oracles (deleted in Plan 06).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/generate-asvs-checklist.js && node -e "const fs=require('node:fs');process.exit(fs.readFileSync('agent-factory/checklists/security-nfr-checklist.md').length>0?0:1)" && echo "generator ran + checklist non-empty"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -q "(C3)" scripts/validate-agent-factory.ts` is true (the no-false-green unset-kit guard ported).
    - The CR-03 null-reject is present in both code paths: `grep -c "not a JSON object\|=== null\|typeof .* !== .object" scripts/validate-agent-factory.ts` ≥ 2.
    - `grep -q "345\|EXPECTED_ROWS" scripts/generate-asvs-checklist.ts` is true (the row-count fail-closed assert ported).
    - Byte-reproducibility proof: regenerate via the new `.js` and via the old `.mjs` to separate temp files and `cmp -s` them — identical.
    - `VALIDATE_KIT_ROOT= node scripts/validate-agent-factory.js 2>&1 | grep -q "(C3)"` — unset kit root fires the (C3) message; the run exits nonzero.
    - `grep -q "production_requires_human_confirmation" scripts/validate-agent-factory.ts` is true (safety invariant ported).
  </acceptance_criteria>
  <done>The validator and ASVS generator are verbatim-behavior TS ports: the C3 unset-kit guard, the CR-03 fail-closed-on-null, every frozen name list, the human-confirmation safety invariant, and the 345-row byte-reproducible emit are all preserved; the regenerated checklist is proven byte-identical to the `.mjs` output.</done>
</task>

<task type="auto">
  <name>Task 2: Port foundation-guards + kit-refs checkers + their Vitest harnesses (RED-by-design preserved)</name>
  <files>scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js, scripts/check-kit-refs.ts, scripts/check-kit-refs.js, scripts/check-foundation-guards.test.ts, scripts/validate.test.ts, scripts/generate-asvs-checklist.test.ts</files>
  <read_first>
    - scripts/check-foundation-guards.sh — READ IN FULL (443 lines). Port verbatim (these are tuned, not arbitrary): the `pass/fail/warn` exit spine (WARN never increments FAILS; exit 0 if FAILS===0 else 1); the WR05 EREs + explicit 4-file SCAN set; `guard_voice` fence-strip + `__UNCLOSED_CAVEMAN_FENCE__` sentinel + the 3 phrase-neutralizations (`/grug`, `grug voice`, `grug wink`) + VOICE_MARKERS; `guard_caveman_preserved` (≥2 `^You` OR ≥1 idiom); the per-role `role_ceiling()` byte table; the 17-file ROLE_FILES + SEC_VOICE_FILES lists; CR-01 missing-file-fails-red everywhere (no vacuous green). The `awk` fence machinery ports to a small line-state loop in TS — same semantics, do NOT re-engineer the anchor (D-10 forward-compat).
    - scripts/check-kit-refs.sh — READ IN FULL (150 lines). Same `pass/fail/FAILS` exit spine. Read-only over the explicit SCAN set (the `grep -rn` becomes a scoped file-walk + regex test). Port verbatim: the SCAN/GH_SCAN/MARKER_SITES path lists; the 16-template ALLOW ERE; the MARKER substring; the 3 assertions + SC2 marker check. The exclusion-by-not-listing design (seed/ intentionally absent) is load-bearing — preserve exact SCAN membership.
    - scripts/check-foundation-guards.test.sh — READ IN FULL (357 lines). The plant-one-violation-per-guard mirror: copy inputs to `$WORK/<case>`, plant exactly one defect, run from the mirror, assert RED; then a smoke run proves GREEN over the real tree. Reproduce every planted case.
    - scripts/validate.test.sh — the two-root perms driver: VALIDATE_KIT_ROOT/VALIDATE_ROOT separately; the bad-missing-kit AND bad-unset-kit `(C3)` cases; the config/plugin null-literal regression cases.
    - .planning/phases/15-typescript-tooling-migration/15-PATTERNS.md — the `check-foundation-guards.ts`, `check-kit-refs.ts`, and the test-harness sections (the sh idioms that port to Vitest: temp-fixture + cleanup, expect_fail capture via spawnSync `{status,stdout,stderr}`, plant-one-violation-per-guard)
  </read_first>
  <action>
    Author `scripts/check-foundation-guards.ts` (single file) porting all six guards 1:1. Keep the `pass/fail/warn` spine where WARN is advisory (does NOT fail the build) and exit is 0/1. Port the WR05 EREs + 4-file SCAN set, the `guard_voice` fence-strip line-state machine (translate the `awk` fence logic to an equivalent TS line loop — same `__UNCLOSED_CAVEMAN_FENCE__` semantics, same 3 phrase-neutralizations), `guard_caveman_preserved`, the per-role `role_ceiling()` byte table (a `Record`/switch lookup, NOT live-computed — the locked ceilings), the 17-file ROLE_FILES + SEC_VOICE_FILES lists verbatim, and CR-01 missing-file-fails-red. Read-only by construction. Findings stdout CLEAR PROFESSIONAL VOICE.
    Author `scripts/check-kit-refs.ts` (single file) porting the kit-ref checker: the SCAN/GH_SCAN/MARKER_SITES lists verbatim, the scoped file-walk replacing `grep -rn` (walk only the SCAN paths, never repo-wide), the ALLOW ERE, the MARKER substring, the 3 assertions + SC2 marker check. Preserve exact SCAN membership (seed/ intentionally absent).
    Author `scripts/check-foundation-guards.test.ts` (Vitest): mirror-and-mutate from `check-foundation-guards.test.sh` — `mkdtempSync` work dir, copy the real inputs, plant exactly ONE defect per guard, `spawnSync` the compiled `check-foundation-guards.js` (scanning the work dir via the same root override the sh harness uses), assert each plant fails RED, plus a smoke run over the real tree asserts GREEN. Reproduce every planted case.
    Author `scripts/validate.test.ts` (Vitest): drive `validate-agent-factory.js` with VALIDATE_KIT_ROOT/VALIDATE_ROOT separately; reproduce the GOOD-split exit-0 case, the BAD missing-kit case, and the BAD unset-kit `(C3)` case (asserting the literal `(C3)` finding + nonzero exit); plus the `--strict` warning-promotion case + the CR-03 null-literal regression cases (config + plugin).
    Author `scripts/generate-asvs-checklist.test.ts` (Vitest): spawn the compiled `generate-asvs-checklist.js`; assert it writes the checklist and exits 0; assert byte-reproducibility (regenerate twice -> identical); assert the fail-closed row-count path (a truncated/tampered source -> exit 1, no partial write) using a temp fixture.
    Compile all: `npx tsc` emits committed `.js`. Confirm `tsc --noEmit` clean, committed `.js` LF.
    Keep `check-foundation-guards.sh`/`.test.sh`, `check-kit-refs.sh`, `validate.test.sh` in-tree as parity oracles (deleted in Plan 06).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/check-foundation-guards.js >/dev/null 2>&1 && node scripts/check-kit-refs.js >/dev/null 2>&1 && npx vitest run scripts/check-foundation-guards.test.ts scripts/validate.test.ts scripts/generate-asvs-checklist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `node scripts/check-foundation-guards.js` exits 0 on the real (GREEN) tree; `node scripts/check-kit-refs.js` exits 0 on the real tree.
    - `npx vitest run scripts/check-foundation-guards.test.ts scripts/validate.test.ts scripts/generate-asvs-checklist.test.ts` exits 0 (all harnesses green).
    - `grep -q "ROLE_FILES" scripts/check-foundation-guards.ts` and `grep -c "role_ceiling\|roleCeiling\|CEILING" scripts/check-foundation-guards.ts` ≥ 1 (the locked byte-ceiling table ported, not live-computed).
    - `grep -q "SCAN\|MARKER_SITES" scripts/check-kit-refs.ts` is true; the scoped walk never does a repo-wide grep (no `process.cwd()`-rooted recursive walk outside the SCAN list).
    - RED-by-design proof: the foundation-guards Vitest harness has at least one planted case per guard that asserts a non-zero exit (the plant-one-violation pattern), and the validate harness asserts the `(C3)` unset-kit case fails red.
  </acceptance_criteria>
  <done>The foundation-guards and kit-refs checkers are verbatim-behavior TS ports (WARN-never-fails spine, every regex/list/ceiling preserved, read-only, CR-01 fail-red), and their Vitest harnesses reproduce the RED-by-design plant-one-violation + smoke-green parity oracle, all green against the committed `.js`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pinned ASVS source JSON -> generator parse | The generator reads a vendored JSON; a tampered/truncated source must never produce a partial or fabricated security checklist. |
| two-root env (VALIDATE_KIT_ROOT/VALIDATE_ROOT) -> validator | An unset kit root must not silently default to `.` (the C3 footgun) and false-green a wrong tree. |
| repo files -> read-only guards | The foundation-guards + kit-refs checkers read role/workflow/AGENTS surfaces; they must never write, and a missing file must fail red (no vacuous green). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-Tamper | Tampering | tampered/truncated ASVS source producing a partial security checklist | mitigate | Fail-closed load chain ported verbatim: read -> parse -> non-object reject -> missing-array reject -> row-count must == 345 or refuse to write (exit 1, no partial); byte-reproducibility asserted so any drift in the generated checklist provenance is caught. |
| T-15-04-Spoof | Spoofing / false-green | validator defaulting an unset kit root to `.` and validating the wrong tree | mitigate | The C3 no-default guard ported verbatim (literal `(C3)` message + exit 1 on unset `VALIDATE_KIT_ROOT`); the Vitest harness asserts the unset-kit case fails red. |
| T-15-04-Repud | Repudiation / fail-open | `JSON.parse("null")` returning null without throwing, bypassing try/catch and crashing or false-passing | mitigate | The CR-03 fail-closed-on-null reject ported in both `checkConfig` and `checkPackaging` (non-object parse result rejected before dereference); the null-literal regression cases reproduced in the Vitest harness. |
| T-15-04-Tamper2 | Tampering | a guard "cleaned up" during the port silently passing where it should fail (vacuous green / re-engineered fence anchor) | mitigate | All six guards ported verbatim with CR-01 missing-file-fails-red; the plant-one-violation-per-guard Vitest harness proves each guard still fails red on its specific defect; the voice fence anchor is NOT re-engineered (D-10 forward-compat). |
| T-15-04-Path | Tampering | path traversal in a checker's read/write target | mitigate | Read-only/write-only-by-construction preserved: every path is `join(ROOT, <fixed literal>)`, never argv/env/content-derived; kit-refs walks only the explicit SCAN set, never a repo-wide grep. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run scripts` exits 0 (validator, foundation-guards, generator harnesses all green).
- `node scripts/check-foundation-guards.js` and `node scripts/check-kit-refs.js` exit 0 on the real tree.
- The ASVS checklist regenerated by the new `.js` is byte-identical to the `.mjs` output.
- The C3 unset-kit guard fires the literal `(C3)` finding + nonzero exit.
- The foundation-guards plant-one-violation harness still fails red per guard (RED-by-design preserved).
</verification>

<success_criteria>
The four `scripts/` checkers/generators are verbatim-behavior TS ports: the C3/CR-03 fixes, every frozen name list, the human-confirmation safety invariant, the six-guard WARN-never-fails spine + locked byte ceilings, the kit-refs SCAN set, and the 345-row byte-reproducible ASVS emit are all preserved. Their RED-by-design Vitest harnesses still fail red on a planted regression and pass green on the migrated code — SC2's harness clause is satisfied for the checker layer, and TOOL-01 is met at exact parity.
</success_criteria>

<output>
Create `.planning/phases/15-typescript-tooling-migration/15-04-SUMMARY.md` when done. Record the byte-reproducibility proof result for the ASVS checklist and note the four `.mjs`/`.sh` originals + their `.test.sh` harnesses remain in-tree as parity oracles until Plan 06.
</output>

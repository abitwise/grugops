---
phase: 26-dogfood-dual-path-oracle-a3-dog-02-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dual-path-equivalence.ts
  - scripts/dual-path-equivalence.js
  - scripts/convergence-spine.test.ts
  - scripts/check-uat-oracles.ts
  - scripts/check-uat-oracles.js
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - scripts/check-uat-oracles.test.ts
autonomous: true
requirements: [DOGF-01]

must_haves:
  truths:
    - "The equivalence projection drops the nonce `id` — it projects each note to {kind, at, verified_by, confidence, refs, body} only (mirrors convergence-spine.test.ts:121-123) per D-04."
    - "`oracleDualPathEquivalence()` runs one seeded decomposition two ways (parallel-spawn sim vs sequential drain) in hermetic temp dirs and asserts the same `done/` set AND the same per-task `currentState()` projection AND the same frozen verdict string (D-04, Open-Q3)."
    - "`scripts/dual-path-equivalence.ts` is the SINGLE-SOURCE comparator imported by BOTH convergence-spine.test.ts AND check-uat-oracles.ts (no duplicated projection logic)."
    - "The seeded fixture includes >=1 admitted `finding` carrying the frozen literal stamp `verified_by: §14-gate#<fixed-id>` that passes context-io `validate()` structurally via GATE_STAMP_RE (no live gate call) per D-03."
    - "The frozen verdict is the fixture constant string READY_FOR_HUMAN_REVIEW asserted in the equivalence comparison (a fixture constant, not a gate-authored note) per D-03."
    - "`oracleParity` is REPLACED (not bare-deleted) by `oracleDualPathEquivalence` AND its two aggregator sites in check-foundation-guards.ts (import ~line 62, invoke ~line 643) are updated in the SAME change per D-12."
    - "A RED non-vacuity test proves the shared comparator returns a non-empty diff when the two projected note-sets diverge (green tests alone are not proof — safety-invariant phase)."
    - "`npm run build` then `npm run freshness` exits 0 after every non-test .ts edit (committed .js twins fresh)."
  artifacts:
    - "scripts/dual-path-equivalence.ts (+ committed scripts/dual-path-equivalence.js twin) exporting projectTaskState(contextRoot, task) and assertEquivalent(a, b)"
    - "scripts/check-uat-oracles.ts exporting oracleDualPathEquivalence() where oracleParity was"
    - "scripts/check-foundation-guards.ts importing + invoking oracleDualPathEquivalence"
    - "scripts/check-uat-oracles.test.ts equivalence RED/GREEN cases replacing the two oracleParity mirror tests (lines 203, 221)"
  key_links:
    - "check-foundation-guards.ts folds oracleDualPathEquivalence's FAILS via uatOracleFails() → aggregator exits non-zero on divergence."
    - "convergence-spine.test.ts and check-uat-oracles.ts both import the same projectTaskState/assertEquivalent from dual-path-equivalence.js."
  prohibitions:
    - "The oracle MUST NOT import or call emitVerdict — that is the only sanctioned by:§14-gate writer and re-tests gate logic (out of scope, D-03)."
    - "The oracle MUST NOT import or call admit — it demands a live green verdict note and re-tests admission logic (out of scope, D-03)."
    - "The equivalence comparison MUST NOT compare the nonce `id` field (guarantees a false failure, Pitfall 2)."
    - "MUST NOT bare-delete oracleParity while leaving its check-foundation-guards.ts import/invoke sites referencing it (Pitfall 5 / D-12)."
    - "MUST NOT assert against the deleted MIGR-02 handoff filenames anywhere in the new oracle (Pitfall 5)."
---

<objective>
Promote the deterministic dual-path on-disk equivalence engine that already lives in `scripts/convergence-spine.test.ts` into a real, always-on Tier-1 CI oracle — `oracleDualPathEquivalence()` — that REPLACES the structural-grep `oracleParity` (check-uat-oracles.ts:329) and is folded into the aggregator in the same change. This is the DOGF-01 gating deliverable and the retirement bar's deterministic half (SC1/SC4).

Purpose: turn "verified means verified / degrade never break" from a doc-shape grep into a real substrate-convergence proof — the same seeded task run parallel-on-CC-sim and sequential must produce the same admitted-note set + verdict string on disk.
Output: a single-source comparator helper (`scripts/dual-path-equivalence.ts`), the new oracle, the aggregator lockstep update, and equivalence RED/GREEN tests — all with fresh committed `.js` twins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-CONTEXT.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the single-source equivalence comparator (dual-path-equivalence.ts) and retrofit convergence-spine.test.ts</name>
  <files>scripts/dual-path-equivalence.ts, scripts/dual-path-equivalence.js, scripts/convergence-spine.test.ts</files>
  <read_first>
    - scripts/convergence-spine.test.ts (source of truth for the replay engine + projection: makeSubstrate/doWork/canonical at lines 55-126; the projection `.map(nr => ({kind, at, body}))` at 121-123; the dual assertion at 155-168)
    - scripts/context-io.ts (currentState at :838, readContext at :803, NoteRecord fields verified_by/confidence/refs; noteId nonce at :752-756)
    - .planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md (§ "Pattern: promote a test's replay engine into a shared, importable comparator")
  </read_first>
  <behavior>
    - projectTaskState(contextRoot, task) returns currentState(readContext(task, contextRoot)) mapped to {kind, at, verified_by, confidence, refs, body} with `id` dropped, sorted by `at` then `body` (deterministic, file-position independent).
    - assertEquivalent(a, b) returns a string[] of human-readable diffs — empty array iff the two projected note-sets are deeply equal; non-empty when they diverge (the non-vacuity contract).
    - convergence-spine.test.ts imports and uses the SAME helper (no local re-implementation of the projection); its existing assertions still pass unchanged.
  </behavior>
  <action>
    Create `scripts/dual-path-equivalence.ts` (a non-test committed-twin script) exporting `projectTaskState(contextRoot: string, task: string)` and `assertEquivalent(a, b): string[]`. `projectTaskState` calls `currentState(readContext(task, contextRoot))` from the committed `./context-io.js` and projects each `NoteRecord` to exactly {kind, at, verified_by, confidence, refs, body} — dropping the nonce `id` (generalizes convergence-spine.test.ts:121-123 from {kind,at,body} to include the finding-bearing fields), sorting by `at` then `body`. `assertEquivalent` deep-compares two projected arrays and returns a diff list (empty === equivalent). Then retrofit `scripts/convergence-spine.test.ts`: replace its inline `canonical()` projection with a call to the shared `projectTaskState`, keeping its `done/`-set assertion and per-task note assertion behavior identical. Do NOT change the seeded fixture in convergence-spine.test.ts. Rebuild the committed twin and run freshness.
  </action>
  <acceptance_criteria>
    - `scripts/dual-path-equivalence.ts` exports both `projectTaskState` and `assertEquivalent`: `grep -cE 'export (function|const) (projectTaskState|assertEquivalent)' scripts/dual-path-equivalence.ts` returns 2.
    - The projection omits `id`: no non-comment line of dual-path-equivalence.ts references an `id` field in the projected object literal (`grep -vE '^\s*//' scripts/dual-path-equivalence.ts | grep -c 'id:'` returns 0).
    - convergence-spine.test.ts imports the shared helper: `grep -c "dual-path-equivalence" scripts/convergence-spine.test.ts` is >= 1.
    - `npx vitest run scripts/convergence-spine.test.ts` passes (behavior unchanged after refactor).
    - `npm run build && npm run freshness` exits 0 (committed dual-path-equivalence.js twin is fresh).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run scripts/convergence-spine.test.ts && npm run build && npm run freshness</automated>
  </verify>
  <done>projectTaskState/assertEquivalent exist as the single-source comparator, convergence-spine.test.ts consumes them unchanged, `id` is never projected, and the committed twin is fresh (freshness exit 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace oracleParity with oracleDualPathEquivalence and update the aggregator in lockstep</name>
  <files>scripts/check-uat-oracles.ts, scripts/check-uat-oracles.js, scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js</files>
  <read_first>
    - scripts/check-uat-oracles.ts (the harness pass/fail/warn + uatOracleFails at :44-68; oracleParity at :329-383; runAll at :392-406 invoking oracleParity at :396; isEntry guard at :410-414)
    - scripts/check-foundation-guards.ts (oracle import block at :59-64 naming oracleParity at :62; invoke + fold at :640-644 where oracleParity() is called at :643 and FAILS += uatOracleFails() at :644)
    - scripts/dual-path-equivalence.ts (the projectTaskState/assertEquivalent created in Task 1)
    - scripts/context-io.ts (appendNote at :761; VERDICT_GREEN_MARKER = "READY_FOR_HUMAN_REVIEW" at :859; validate at :548 and GATE_STAMP_RE at :648 — a finding carrying §14-gate#<id> passes validate() structurally with no live gate)
    - .planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md (Loud Flag 1 + Pitfall 3 + the "Seeded fixture with a frozen-stamped admitted finding" code example)
  </read_first>
  <behavior>
    - oracleDualPathEquivalence() seeds one minimal decomposition (model on convergence-spine's doWork) that includes at least one soft note plus one admitted `finding` whose `verified_by` and `refs` carry the literal stamp `§14-gate#<fixed-id>` and whose body carries the frozen READY_FOR_HUMAN_REVIEW string.
    - It drives that seed two ways in hermetic mkdtempSync temp roots — Mode A claims up to width then does work fan-out then transitions all done; Mode B claims-one/works/done serially — writing notes via the committed context-io.js/claim.js with an explicit per-mode contextRoot/queueRoot.
    - It compares via projectTaskState + assertEquivalent AND compares the `done/` sets AND asserts the frozen verdict string is present in both; on any diff it calls fail(...) naming the divergence; on equivalence it calls pass(...).
    - Removing oracleParity's body and re-pointing runAll + the aggregator import/invoke happen in this one change.
  </behavior>
  <action>
    In `scripts/check-uat-oracles.ts`, replace the `oracleParity` function (line 329) with `export function oracleDualPathEquivalence(): void`. It builds a frozen fixture — a fixed id constant (e.g. R26-DOGF01-0001), the literal gate stamp `§14-gate#<fixed-id>`, and the frozen verdict constant sourced conceptually from context-io's green marker — then runs the seed under two replay modes in `mkdtempSync` hermetic roots (reuse the committed `./claim.js` claimTask/transition and `./context-io.js` appendNote/readContext; pass explicit contextRoot/queueRoot to every call). Compare with `assertEquivalent(projectTaskState(rootA, task), projectTaskState(rootB, task))`, also compare the sorted `done/` sets, and assert the frozen verdict string appears in both projections; call the module `pass()`/`fail()` accordingly and clean up temp dirs. Use ONLY the frozen literal stamp for the finding.
    <!-- planner-discipline-allow: emitVerdict -->
    <!-- planner-discipline-allow: admit -->
    Do NOT import or call `emitVerdict` or `admit` from context-io (D-03 / Loud Flag 1) — the finding's literal `§14-gate#<id>` stamp passes `validate()` via GATE_STAMP_RE with no live gate. Update `runAll()` (line 396) to call `oracleDualPathEquivalence()` instead of `oracleParity()`. Then in `scripts/check-foundation-guards.ts` update the import (line 62) and the invoke (line 643) from `oracleParity` to `oracleDualPathEquivalence` in this SAME change (D-12). Rebuild both committed `.js` twins and run freshness.
  </action>
  <acceptance_criteria>
    - The oracle is renamed/replaced: `grep -c 'oracleDualPathEquivalence' scripts/check-uat-oracles.ts` >= 1 AND `grep -c 'function oracleParity' scripts/check-uat-oracles.ts` returns 0.
    - The aggregator is updated in lockstep: `grep -c 'oracleParity' scripts/check-foundation-guards.ts` returns 0 AND `grep -c 'oracleDualPathEquivalence' scripts/check-foundation-guards.ts` >= 2 (import + invoke).
    - The oracle does NOT reach for the gate writer or admission cross-check: non-comment lines of check-uat-oracles.ts contain neither symbol — `grep -vE '^\s*//' scripts/check-uat-oracles.ts | grep -cE '\b(emitVerdict|admit)\b'` returns 0.
    - The oracle never asserts a deleted handoff filename: `grep -cE 'implementation-handoff\.md|qe-handoff\.md' scripts/check-uat-oracles.ts` returns 0.
    - Real-tree GREEN smoke: `node scripts/check-foundation-guards.js` exits 0 (new oracle converges over its hermetic fixture).
    - `npm run build && npm run freshness` exits 0 (both edited twins fresh).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-foundation-guards.js && npm run build && npm run freshness</automated>
  </verify>
  <done>oracleDualPathEquivalence replaces oracleParity, the aggregator imports/invokes it in the same change, no emitVerdict/admit/deleted-handoff references exist in the oracle, the aggregator exits 0 over the real tree, and twins are fresh.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the two oracleParity mirror tests with equivalence GREEN + non-vacuity RED cases</name>
  <files>scripts/check-uat-oracles.test.ts</files>
  <read_first>
    - scripts/check-uat-oracles.test.ts (CHECK_ROOT mirror harness at :62-79; the two oracleParity tests at :203 and :221; the real-tree GREEN smoke at :235)
    - scripts/dual-path-equivalence.ts (assertEquivalent — the comparator whose RED behavior is the non-vacuity keystone)
    - scripts/check-uat-oracles.ts (oracleDualPathEquivalence from Task 2)
  </read_first>
  <action>
    In `scripts/check-uat-oracles.test.ts` replace the two `oracleParity` tests (lines 203 and 221) with equivalence-oriented cases: (1) a GREEN case asserting the real aggregator converges — `node scripts/check-foundation-guards.js` (or the standalone `node scripts/check-uat-oracles.js`) exits 0 and the output names the dual-path equivalence oracle as passing; (2) a RED non-vacuity case that imports `assertEquivalent` from `../scripts/dual-path-equivalence.js` and feeds it two deliberately divergent projected note-sets (e.g. one missing the stamped finding, or a differing verdict body) and asserts the returned diff array is NON-empty — proving the oracle's comparator is not vacuous and CAN go red. Keep the existing wording/wiring tests and the smoke test (line 235) intact. Tag the new cases so `-t "equivalence"` selects them.
  </action>
  <acceptance_criteria>
    - No parity-string test remains: `grep -c 'oracleParity' scripts/check-uat-oracles.test.ts` returns 0.
    - The non-vacuity RED case exercises the shared comparator: `grep -c 'assertEquivalent' scripts/check-uat-oracles.test.ts` >= 1.
    - `npx vitest run scripts/check-uat-oracles.test.ts` passes (GREEN convergence case + RED non-vacuity case + retained wording/wiring/smoke).
    - The RED case would fail if the comparator returned [] on divergence (it asserts a non-empty diff), documented in the test name/comment as the non-vacuity keystone.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run scripts/check-uat-oracles.test.ts</automated>
  </verify>
  <done>The two parity-string tests are gone, replaced by an equivalence GREEN case and an assertEquivalent-based RED non-vacuity case; the full oracle test file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture note text → context-io validate() | The seeded finding's `§14-gate#<id>` stamp is untrusted text that must pass structural validation, not impersonate the gate. |
| oracle → committed context-io.js/claim.js | The oracle drives substrate primitives; it must not reach into gate-authoring APIs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01-R | Repudiation | oracleDualPathEquivalence fixture | high | mitigate | Assert on-disk admitted-note set + verdict string (D-05); never reference the deleted MIGR-02 handoff filenames (Pitfall 5). |
| T-26-01-E | Elevation of Privilege | seeded finding stamp | high | mitigate | Frozen literal `§14-gate#<id>` only; never call emitVerdict/admit — no gate impersonation, no live verdict note (D-03). |
| T-26-01-F | Information/File handling | hermetic temp roots | medium | mitigate | mkdtempSync roots + allowlisted task names (`^[A-Za-z0-9._-]+$`); temp dirs cleaned up; no persistent datastore. |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase (Node stdlib + existing devDeps only); Package Legitimacy Gate has no candidates. |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**'` is green (never bare `npm test` — Pitfall 4).
- `node scripts/check-foundation-guards.js` exits 0 with the new oracle folded in.
- `npm run build && npm run freshness` exits 0 (all three edited non-test twins fresh).
- Grep gates in each task's acceptance criteria hold.
</verification>

<success_criteria>
DOGF-01 is met: a dual-path equivalence oracle replacing oracleParity runs one seeded task two ways and asserts on-disk equivalence (same admitted findings, same verdict, same `done/` artifact), single-sourced with convergence-spine, folded into the aggregator, and proven non-vacuous by a RED test.
</success_criteria>

## Artifacts this plan produces
- `scripts/dual-path-equivalence.ts` + committed `scripts/dual-path-equivalence.js` twin — exported `projectTaskState(contextRoot, task)` and `assertEquivalent(a, b)`.
- `scripts/check-uat-oracles.ts` (+ `.js` twin) — new exported `oracleDualPathEquivalence()` replacing `oracleParity`; fixture constants FIXED_ID / GATE_STAMP (`§14-gate#<fixed-id>`) / FROZEN_VERDICT (READY_FOR_HUMAN_REVIEW).
- `scripts/check-foundation-guards.ts` (+ `.js` twin) — import + invoke of `oracleDualPathEquivalence`.
- `scripts/convergence-spine.test.ts` — refactored to import the shared comparator.
- `scripts/check-uat-oracles.test.ts` — equivalence GREEN + `assertEquivalent` RED non-vacuity cases.

<output>
Create `.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-01-SUMMARY.md` when done.
</output>

---
phase: 26-dogfood-dual-path-oracle-a3-dog-02-retirement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/worktree-dogfood.test.ts
autonomous: true
requirements: [DOGF-02]

must_haves:
  truths:
    - "Every claim/context call in the dogfood receives ONE explicit shared absolute contextRoot and ONE explicit shared absolute queueRoot, both OUTSIDE every worktree (D-07) — the script-relative DEFAULT_CONTEXT_ROOT/DEFAULT_QUEUE_ROOT are never relied on across worktrees."
    - "N = queue.wip_limit (=3) node child processes, each cwd'd into its own real `git worktree add` checkout, all pinned to the same shared roots (D-06); the dogfood REUSES the already-built claim.claimTask/transition/sweepStale + context-io.appendNote/readContext primitives, building no new reclaim/claim code (D-08)."
    - "After the run there are N distinct un-clobbered notes for the shared task (N unique notes/<id>.md files) — no writer clobbered another (appendNote uniqueness)."
    - "Each task is claimed exactly once — claimTask returns true for exactly one process and false (EEXIST) for the losers (atomic mkdirSync exactly-once proof)."
    - "A negative shadow-check proves the shared root is authoritative: no worktree grew its own populated `.grugops/context` for the shared task (the D-07 shadowing UNKNOWN is exercised, not vacuous)."
    - "sweepStale reclaims a deliberately stale claim: a claim seeded with an old `at` is returned by sweepStale(queueRoot, ttlMs, injectedNow) and the subtask returns to pending/ (Pitfall 6)."
    - "The test drives the committed `.js` (main checkout's context-io.js/claim.js), never the `.ts`."
  artifacts:
    - "scripts/worktree-dogfood.test.ts — hermetic Vitest test creating real git worktrees under a mkdtempSync root"
  key_links:
    - "N worktrees isolate code edits; the queue + context live outside them at the shared absolute roots — the interaction SC2 must confirm."
  prohibitions:
    - "MUST NOT rely on DEFAULT_CONTEXT_ROOT / DEFAULT_QUEUE_ROOT (script-relative → worktree-local → silent divergence, Pitfall 1/D-07)."
    - "MUST NOT spawn any child via shell:true on a data path — use arg-array spawnSync/execFileSync only (Tampering)."
    - "MUST NOT set or export GRUGOPS_PROD_DEPLOY_APPROVED anywhere in the harness (assert its absence only if referenced)."
    - "MUST NOT assert a passing sweepStale reclaim without proving the returned array CONTAINS the task and the subtask is back in pending/ (a no-op must not read as a pass, Pitfall 6)."
    - "MUST NOT use unsafe task names — fixtures use the allowlist `^[A-Za-z0-9._-]+$` and hermetic temp roots."
---

<objective>
Prove SC2/DOGF-02 with a deterministic, token-free dogfood: N real `git worktree` checkouts, N node child processes, all pinned to ONE shared absolute contextRoot and ONE shared absolute queueRoot outside every worktree. It must demonstrate N distinct un-clobbered notes, each task claimed exactly once, that no worktree shadows the shared context (the flagged v2.0 UNKNOWN, D-07), and that a stale claim is reclaimed by `sweepStale`.

Purpose: exercise the `isolation: worktree` ↔ shared-context-path interaction for real, using only the already-built substrate primitives (claim.ts / context-io.ts) — this phase measures, it does not re-architect.
Output: `scripts/worktree-dogfood.test.ts` (hermetic, no committed twin — test files are excluded from tsconfig, so no freshness impact).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-CONTEXT.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-worktree exactly-once claim + un-clobbered notes + shared-root shadow negative-check</name>
  <files>scripts/worktree-dogfood.test.ts</files>
  <read_first>
    - scripts/claim.ts (DEFAULT_QUEUE_ROOT script-relative at :60-61; TASK_NAME_RE at :68; claimTask atomic mkdirSync at :106; transition atomic rename at :143)
    - scripts/context-io.ts (DEFAULT_CONTEXT_ROOT script-relative at :82-83; appendNote fresh-unique notes/<id>.md at :761; readContext at :803; currentState at :838)
    - scripts/convergence-spine.test.ts (the hermetic mkdtempSync + committed-.js-driven style + the worktree-shadow negative assertion pattern at :175-182)
    - agent-factory/config/factory.config.json (queue.wip_limit = 3 — the concurrency width)
    - .planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md (Pitfall 1 + "N-process worktree pinning" code example + Open-Q2 — import the MAIN checkout's committed .js)
  </read_first>
  <action>
    Create `scripts/worktree-dogfood.test.ts`. Under a `mkdtempSync` root, define ONE shared absolute `SHARED_QUEUE` and ONE shared absolute `SHARED_CONTEXT` (both OUTSIDE every worktree). Create N = wip_limit (3) real git worktrees via arg-array `git worktree add` (execFileSync/spawnSync, never shell:true). Launch N node child processes, each with `cwd` set to its worktree, each importing the MAIN checkout's committed `scripts/claim.js` and `scripts/context-io.js` and passing the SAME shared absolute roots to every call: `claimTask(SHARED_QUEUE, task, agent-i)` → on true `transition(SHARED_QUEUE, task, pending, claimed)` → `appendNote(task, note, body, SHARED_CONTEXT)` → `transition(..., claimed, done)`. Use allowlisted task names. After joining all children assert: (a) exactly one process observed claimTask === true for a single-slot task and the rest saw EEXIST/false (exactly-once); (b) for a shared multi-writer note task, there are N distinct `notes/<id>.md` files under SHARED_CONTEXT (un-clobbered); (c) the negative shadow-check — no worktree contains a populated `.grugops/context/<task>/notes` for the shared task (the shared root is authoritative, D-07). Tear down worktrees (arg-array `git worktree remove`) and the temp root in afterAll/finally.
  </action>
  <acceptance_criteria>
    - The test pins shared absolute roots and never the defaults: `grep -cE 'SHARED_QUEUE|SHARED_CONTEXT' scripts/worktree-dogfood.test.ts` >= 4; `grep -cE 'DEFAULT_CONTEXT_ROOT|DEFAULT_QUEUE_ROOT' scripts/worktree-dogfood.test.ts` returns 0.
    - Real worktrees are used: `grep -c 'worktree add' scripts/worktree-dogfood.test.ts` >= 1.
    - No shell:true on any spawn: `grep -c 'shell: *true' scripts/worktree-dogfood.test.ts` returns 0.
    - `npx vitest run scripts/worktree-dogfood.test.ts` passes: exactly-once claim, N distinct un-clobbered notes, and the shadow negative-check all green.
    - The shadow negative-check is present and asserts a worktree-local context is NOT populated for the shared task (mirrors convergence-spine.test.ts:175-182).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run scripts/worktree-dogfood.test.ts</automated>
  </verify>
  <done>The dogfood proves, across N real worktrees pinned to one shared root, exactly-once claiming and N un-clobbered notes, and that no worktree shadows the shared context — closing the D-07 UNKNOWN.</done>
</task>

<task type="auto">
  <name>Task 2: Stale-claim reclaim property via sweepStale with injected clock</name>
  <files>scripts/worktree-dogfood.test.ts</files>
  <read_first>
    - scripts/claim.ts (sweepStale at :178 — signature (queueRoot, ttlMs, now=Date.now()) → reclaimed task names; the conservative fresh-claims-left-alone path at ~:203)
    - scripts/worktree-dogfood.test.ts (the shared-root harness from Task 1 to extend)
    - .planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md (Pitfall 6 — a no-op sweepStale must not read as a pass; inject `now` far past `at+ttl`)
  </read_first>
  <action>
    Extend `scripts/worktree-dogfood.test.ts` with a stale-reclaim case against the shared `SHARED_QUEUE`. Seed a claimed subtask whose claim marker carries an OLD `at` timestamp, then call `sweepStale(SHARED_QUEUE, ttlMs, injectedNow)` with `injectedNow` set well past `oldAt + ttlMs` (deterministic, generous TTL of Claude's choosing). Assert the returned array CONTAINS the seeded task name AND that the subtask has been moved back to `pending/`. Add a companion assertion that a FRESH claim (recent `at`) with the same call is NOT reclaimed (the conservative no-op path) — so the reclaim is proven non-vacuous. sweepStale stays wall-clock-TTL only (no pid/host liveness — that is deferred v2.x PAR-05).
  </action>
  <acceptance_criteria>
    - sweepStale is called with an injected now: `grep -c 'sweepStale' scripts/worktree-dogfood.test.ts` >= 1 and the call passes a third `now` argument.
    - The reclaim assertion checks BOTH the returned array contains the task AND the subtask is back in pending/ (not merely that the array is non-empty).
    - A fresh-claim companion assertion proves a recent claim is NOT reclaimed (non-vacuity of the TTL boundary).
    - `npx vitest run scripts/worktree-dogfood.test.ts` passes with both the Task 1 and Task 2 cases green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run scripts/worktree-dogfood.test.ts</automated>
  </verify>
  <done>A deliberately stale claim is reclaimed by sweepStale (returned + moved to pending/) while a fresh claim is left alone — the reclaim property is proven, not vacuous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child process ↔ shared queue/context roots | N processes write concurrently to one shared filesystem location — atomicity is the property under test. |
| test harness → git CLI | Worktree create/remove invoked as a subprocess. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-02-T | Tampering | git/node child spawns | high | mitigate | Arg-array spawnSync/execFileSync only; never shell:true on any data path (V5). |
| T-26-02-F | File handling | shared roots + task names | medium | mitigate | Allowlisted task names (`^[A-Za-z0-9._-]+$`); mkdtempSync hermetic roots outside the repo tree; worktrees + temp root torn down. |
| T-26-02-R | Repudiation | sweepStale reclaim | medium | mitigate | Assert the returned array contains the task AND the subtask moved to pending/ — a conservative no-op cannot read as a pass (Pitfall 6). |
| T-26-02-E | Elevation of Privilege | env surface | low | mitigate | Harness never sets GRUGOPS_PROD_DEPLOY_APPROVED. |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase; git + Node stdlib + existing Vitest only. |
</threat_model>

<verification>
- `npx vitest run scripts/worktree-dogfood.test.ts` green (both tasks).
- `npx vitest run --exclude '**/scripts/e2e/**'` stays green (no regression; never bare `npm test`).
- No committed twin / freshness impact (test files are tsconfig-excluded).
</verification>

<success_criteria>
DOGF-02 is met: a parallel N-agent dogfood over real worktrees produces N distinct un-clobbered notes, claims each task exactly once, proves the shared root is authoritative (no worktree shadowing), and reclaims a stale claim — confirming the isolation:worktree ↔ shared-context-path interaction.
</success_criteria>

## Artifacts this plan produces
- `scripts/worktree-dogfood.test.ts` — hermetic real-worktree N-process dogfood: shared-root pinning, exactly-once claim, N un-clobbered notes, D-07 shadow negative-check, and the `sweepStale` reclaim property (injected clock). No committed `.js` twin (test file).

<output>
Create `.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-02-SUMMARY.md` when done.
</output>

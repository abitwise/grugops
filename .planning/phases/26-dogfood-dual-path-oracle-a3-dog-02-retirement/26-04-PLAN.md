---
phase: 26-dogfood-dual-path-oracle-a3-dog-02-retirement
plan: 04
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - scripts/e2e/uat-live.test.ts
  - docs/dogfood-human-runbook.md
autonomous: true
requirements: [DOGF-02]

must_haves:
  truths:
    - "The Tier-2 A3-live case asserts on-disk note-set + verdict-string equivalence (D-05), not deleted handoff filenames — the FROZEN_HANDOFFS anchor is removed (Pitfall 5 / Loud Flag 2)."
    - "A gated N-agent live-spawn case (it.skipIf(!LIVE)) confirms the DOGF-02 dogfood against a real claude spawn — Tier-2 confirmation only (D-09), loud-skip when unauthed."
    - "The loud-skip keystone is preserved: an absent/unauthed claude emits the exact LOUD_SKIP_MARKER and NEVER flips a UAT to pass (a skip is never a pass)."
    - "The human runbook names on-disk note-set + verdict equivalence as the dual-path artifact, not the deleted handoff filenames — so the D-01 human capture path is honest."
    - "The harness NEVER sets or exports GRUGOPS_PROD_DEPLOY_APPROVED (assert its absence only — uat-live.test.ts:56, 294-299)."
  artifacts:
    - "scripts/e2e/uat-live.test.ts — A3-live retargeted off deleted handoffs + a gated N-agent live-confirmation case"
    - "docs/dogfood-human-runbook.md — dual-path artifact retargeted to on-disk note-set + verdict equivalence"
  key_links:
    - "The retargeted A3-live equivalence definition mirrors Plan 01's oracle (on-disk admitted-note set + verdict string) — the deterministic gate and the live confirmation now share one artifact definition."
  prohibitions:
    - "MUST NOT retain any assertion against the two MIGR-02-deleted handoff filenames in uat-live.test.ts or docs/dogfood-human-runbook.md (assert on-disk note-set + verdict instead, D-05)."
    - "MUST NOT let a skipped/absent live run report a silent green or flip a UAT (LOUD_SKIP_MARKER keystone)."
    - "MUST NOT set GRUGOPS_PROD_DEPLOY_APPROVED anywhere in the harness."
    - "MUST NOT spawn claude/node via shell:true on any data path — arg-array spawnSync only."
    - "MUST NOT flip A3/DOG-02 to retired here — the evidence-gated flip is Plan 05 only."
  user_setup:
    - service: claude-cli
      why: "The Tier-2 live lane and the D-01 captured run require an authed `claude` CLI; when absent the lane loud-skips (never a pass) and DOG-02 stays pending. No secret is stored by grugops."
---

<objective>
Retarget the Tier-2 live dual-path harness so it stops asserting the Phase-24/MIGR-02-deleted handoff filenames and instead asserts on-disk note-set + verdict-string equivalence (D-05, Pitfall 5 / Loud Flag 2), extend it with a gated N-agent live-spawn confirmation of the DOGF-02 dogfood (Tier-2 only, D-09), and fix the human runbook to describe the same honest dual-path artifact. This is retirement-prep: it makes both the authed lane and the human runbook a valid source for D-01's captured live run — without flipping anything to retired (that is Plan 05).

Purpose: remove a dishonest/stale anchor (the deleted handoff filenames still named in the live test + runbook) and align the live confirmation with the deterministic oracle's equivalence definition, so the retirement's evidence gate is trustworthy.
Output: a retargeted, extended `scripts/e2e/uat-live.test.ts` and a corrected `docs/dogfood-human-runbook.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-CONTEXT.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget A3-live off deleted handoffs and add the gated N-agent live-confirmation case</name>
  <files>scripts/e2e/uat-live.test.ts</files>
  <read_first>
    - scripts/e2e/uat-live.test.ts (LOUD_SKIP_MARKER + claudePresentAndAuthed + emitLoudSkipIfUnavailable at :67-102; the never-set-approval rule at :56, :294-299; the A3-live case at :319-361 including the FROZEN_HANDOFFS array at :325 and the handoff-filename parity loop at :346-359)
    - scripts/dual-path-equivalence.ts (Plan 01's projectTaskState/assertEquivalent — the on-disk equivalence definition to mirror in the live assertion)
    - scripts/worktree-dogfood.test.ts (Plan 02's shared-root dogfood shape — the deterministic analog the live N-agent case confirms)
    - .planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md (Loud Flag 2 + Pitfall 5 — retarget onto note-set + verdict equivalence, remove deleted filenames; D-05; D-09 live-confirmation-only)
  </read_first>
  <action>
    In `scripts/e2e/uat-live.test.ts` rework the A3-live (DOG-02) case (lines 319-361): remove the FROZEN_HANDOFFS array and the loop that asserts both dispatch paths name those deleted files. Replace the equivalence assertion with the D-05 definition — both dispatch paths must converge on the same on-disk admitted-note set (via the shared context) and the same frozen verdict string. Keep the FROZEN_VERDICT parity assertion. Preserve `it.skipIf(!LIVE)` and the loud-skip keystone unchanged. Then add a NEW gated case that confirms the DOGF-02 dogfood live: spawn N (= wip_limit) agents against ONE shared absolute context/queue root and assert distinct un-clobbered notes + each task claimed once — Tier-2 confirmation only (D-09), loud-skipping when unauthed. Use arg-array spawnSync only; never set the approval env.
  </action>
  <acceptance_criteria>
    - No deleted-handoff-filename assertion remains: `grep -cE 'implementation-handoff\.md|qe-handoff\.md|FROZEN_HANDOFFS' scripts/e2e/uat-live.test.ts` returns 0.
    - The verdict-parity assertion is retained: `grep -c 'READY_FOR_HUMAN_REVIEW' scripts/e2e/uat-live.test.ts` >= 1.
    - The loud-skip keystone + never-set-approval are intact: `grep -c 'LOUD_SKIP_MARKER' scripts/e2e/uat-live.test.ts` >= 1 AND `grep -cE 'GRUGOPS_PROD_DEPLOY_APPROVED *=|export GRUGOPS_PROD_DEPLOY_APPROVED' scripts/e2e/uat-live.test.ts` returns 0.
    - No shell:true on any spawn: `grep -c 'shell: *true' scripts/e2e/uat-live.test.ts` returns 0.
    - The gated N-agent live case exists (a second `it.skipIf(!LIVE)` referencing a shared context/queue root).
    - The non-e2e regression stays green (this file is e2e-only): `npx vitest run --exclude '**/scripts/e2e/**'` passes; the e2e lane loud-skips or passes when run separately (`npm run test:e2e`).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <done>A3-live asserts on-disk note-set + verdict equivalence (no deleted handoff filenames), a gated N-agent live-confirmation case is added, and the loud-skip + never-set-approval keystones are preserved; the non-e2e suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the human runbook to describe the on-disk note-set + verdict dual-path artifact</name>
  <files>docs/dogfood-human-runbook.md</files>
  <read_first>
    - docs/dogfood-human-runbook.md (the handoff-filename references at ~:35, :151-152, :176 — Check 3 step 2 and Step 4)
    - .planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md (Loud Flag 2 — the runbook still names the deleted handoffs; D-01 human capture path; D-05 equivalence definition)
    - scripts/dual-path-equivalence.ts (the equivalence definition the runbook should describe)
  </read_first>
  <action>
    Edit `docs/dogfood-human-runbook.md` so the dual-path "same artifact" check describes on-disk note-set + verdict-string equivalence (D-05) — the admitted `finding`s in the shared context and the frozen READY_FOR_HUMAN_REVIEW verdict — instead of the two deleted MIGR-02 handoff filenames. Keep the runbook a valid instrument for D-01's one captured human run (it records the capture date + verdict as evidence). Do not add any instruction that sets a prod-deploy approval env.
  </action>
  <acceptance_criteria>
    - No deleted-handoff-filename reference remains: `grep -cE 'implementation-handoff\.md|qe-handoff\.md' docs/dogfood-human-runbook.md` returns 0.
    - The runbook names the on-disk equivalence artifact: `grep -cE 'note|verdict|READY_FOR_HUMAN_REVIEW' docs/dogfood-human-runbook.md` >= 1.
    - The runbook still describes recording a captured run as evidence (date + verdict) for the retirement gate.
    - No instruction sets the approval env: `grep -c 'GRUGOPS_PROD_DEPLOY_APPROVED=' docs/dogfood-human-runbook.md` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -cE 'implementation-handoff\.md|qe-handoff\.md' docs/dogfood-human-runbook.md)" -eq 0</automated>
  </verify>
  <done>The human runbook describes the honest on-disk note-set + verdict dual-path artifact and remains a valid D-01 capture instrument; no deleted handoff filenames remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live `claude` CLI | Real dispatch is spawned; its transcript/on-disk output is untrusted input to the parity assertion. |
| harness → process env | The prod-deploy approval env is a safety floor the harness must never write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-04-E | Elevation of Privilege | approval env | high | mitigate | Never set/export GRUGOPS_PROD_DEPLOY_APPROVED; assert its absence only (uat-live.test.ts:56, 294-299). |
| T-26-04-R | Repudiation | loud-skip + trace | high | mitigate | LOUD_SKIP_MARKER keystone preserved (a skip never a pass); retarget off deleted artifacts onto on-disk note-set + verdict (D-05) preserves requirement→trace. |
| T-26-04-T | Tampering | claude/node spawns | high | mitigate | Arg-array spawnSync only; no shell:true on any data path (V5). |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase. |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**'` stays green (never bare `npm test`).
- `grep -cE 'implementation-handoff\.md|qe-handoff\.md|FROZEN_HANDOFFS' scripts/e2e/uat-live.test.ts` returns 0 and the same grep over docs/dogfood-human-runbook.md returns 0.
- The e2e lane, run separately (`npm run test:e2e`), loud-skips when unauthed or passes when authed — never a silent green.
</verification>

<success_criteria>
The DOGF-02 live confirmation and the human runbook are retargeted onto the honest on-disk note-set + verdict equivalence artifact (D-05), the deleted handoff filenames are gone, the loud-skip + never-set-approval keystones hold, and the retirement's evidence gate now has trustworthy authed and human capture paths — without flipping anything to retired.
</success_criteria>

## Artifacts this plan produces
- `scripts/e2e/uat-live.test.ts` — A3-live retargeted onto on-disk note-set + verdict equivalence (FROZEN_HANDOFFS removed); a gated N-agent live-confirmation case (Tier-2, loud-skip).
- `docs/dogfood-human-runbook.md` — dual-path artifact described as the on-disk note-set + verdict; a valid D-01 human capture instrument.

<output>
Create `.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-04-SUMMARY.md` when done.
</output>

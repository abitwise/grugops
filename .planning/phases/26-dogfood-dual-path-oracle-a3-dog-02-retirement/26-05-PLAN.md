---
phase: 26-dogfood-dual-path-oracle-a3-dog-02-retirement
plan: 05
type: execute
wave: 3
depends_on: [26-01, 26-02, 26-04]
files_modified:
  - examples/03-ticket-to-pr.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [DOGF-01, DOGF-02, DOGF-03]

must_haves:
  truths:
    - "A3/DOG-02 flips to retired ONLY when BOTH (a) the deterministic equivalence oracle is green AND (b) one captured live dual-path run is recorded as evidence (authed Tier-2 OR human runbook) — D-01/D-02/SC4."
    - "The captured-run evidence (date + verdict) is recorded before the flip; without it DOG-02 stays pending and the flip is explicitly deferred (a loud-skip is never a retirement)."
    - "examples/03-ticket-to-pr.md no longer names the deleted MIGR-02 handoff filenames and the stale validate-agent-factory .mjs reference is corrected to the .js tooling (Pitfall 5 / State-of-the-Art note)."
    - "The parity table's CC-native cell is flipped from `pending human` to retired-via-oracle-plus-captured-run only after the evidence gate passes — the requirement→trace is preserved, nothing asserts a deleted artifact."
    - "DOGF-01/02/03 requirement checkboxes are marked done and the DOG-02/A3 retirement is recorded in the tracking docs."
  artifacts:
    - "examples/03-ticket-to-pr.md — deleted-handoff refs removed, .mjs→.js corrected, parity table flipped to retired (post-evidence)"
    - ".planning/REQUIREMENTS.md — DOGF-01/02/03 marked done + DOG-02/A3 retirement recorded"
  key_links:
    - "The retired flip consumes Plan 01 (oracle green), Plan 02 (dogfood green), Plan 04 (retargeted live lane + runbook) and the human-verified captured live run — all four are prerequisites for SC4."
  prohibitions:
    - "MUST NOT flip A3/DOG-02 to retired on handoff deletion alone, on the oracle alone, or on a cost figure (SC4 / D-01 / D-11)."
    - "MUST NOT flip to retired without a recorded captured live run (date + verdict) — if absent, DEFER and leave DOG-02 pending."
    - "MUST NOT assert against or re-introduce the deleted MIGR-02 handoff filenames anywhere in examples/03-ticket-to-pr.md."
    - "MUST NOT mark the ROADMAP phase Complete here — phase completion/verification is the orchestrator's gate (avoid premature-complete)."
  user_setup:
    - service: claude-cli
      why: "The D-01 captured live run needs an authed `claude` (Tier-2 `npm run test:e2e`) OR a human following docs/dogfood-human-runbook.md; grugops stores no secret. If neither is available, the retirement defers and DOG-02 stays pending."
---

<objective>
Perform the final, evidence-gated retirement of A3/DOG-02 (SC4). A3/DOG-02 flips to retired ONLY when BOTH the deterministic equivalence oracle (Plan 01) is green AND one captured live dual-path run is recorded as evidence (authed Tier-2 OR the human runbook) — never on handoff deletion alone, never on the oracle alone, and never on a cost figure (D-01/D-02/D-11). A blocking human checkpoint confirms the evidence exists; only then does the flip touch the tracking docs. If no live capture exists, the retirement DEFERS (DOG-02 stays pending) and the phase's mechanical deliverables still stand.

Purpose: honest closure of the long-deferred dual-path UAT — the CC-native column is retired against real evidence, not deletion.
Output: the retired flip in examples/03-ticket-to-pr.md + REQUIREMENTS.md (plus deleted-handoff and stale-.mjs cleanup), or a recorded deferral.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-CONTEXT.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Evidence gate for the A3/DOG-02 retirement (oracle-green + captured live run)</name>
  <action>Pause for blocking human verification. Do NOT proceed to the flip until the human confirms BOTH that the deterministic equivalence oracle is green AND that one captured live dual-path run is recorded (date + verdict). On `defer`, no flip occurs and DOG-02 stays pending. This checkpoint is never auto-approvable.</action>
  <what-built>
    The deterministic equivalence oracle (Plan 01, `oracleDualPathEquivalence`) is green in CI; the Tier-2 live lane and the human runbook (Plan 04) are retargeted onto the honest on-disk note-set + verdict artifact and are valid capture instruments. This is everything needed to CAPTURE the one live dual-path run that D-01 requires before A3/DOG-02 may be retired.
  </what-built>
  <how-to-verify>
    1. Confirm the deterministic oracle is green: run `node scripts/check-foundation-guards.js` (exit 0) and `npx vitest run scripts/check-uat-oracles.test.ts` (green) and `npx vitest run scripts/worktree-dogfood.test.ts` (green).
    2. Obtain ONE captured live dual-path run as evidence, via EITHER:
       - the authed Tier-2 lane on an authed box: `npm run test:e2e` (the A3-live case runs — not a loud-skip), OR
       - a human run of `docs/dogfood-human-runbook.md`.
       Record the capture: the DATE and the observed gate verdict (READY_FOR_HUMAN_REVIEW) and that both dispatch paths converged on the same on-disk note-set (D-05). A loud-skip is NOT a capture.
    3. Decide: reply `approved` with the recorded capture (date + verdict) to authorize the retirement flip, OR reply `defer` if no live capture exists — the flip will NOT happen and DOG-02 stays pending.
  </how-to-verify>
  <resume-signal>Reply "approved: <capture date> <verdict>" to authorize the flip, or "defer: no capture yet" to leave DOG-02 pending.</resume-signal>
</task>

<task type="auto">
  <name>Task: Flip A3/DOG-02 to retired in the tracking docs (only on approved + recorded capture)</name>
  <files>examples/03-ticket-to-pr.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - examples/03-ticket-to-pr.md (the parity table + CC-native `pending human` column at ~:74, :160-165; the deleted handoff refs at :55, :79-84, :174; the stale `validate-agent-factory.mjs` reference at :10, :143)
    - .planning/REQUIREMENTS.md (DOGF-01/02/03 lines 71-73 and the requirement status table lines 131-133; the DOGF-03 clause "A3/DOG-02 is retired only when the oracle passes")
    - .planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md (Q7 retirement mechanics; every tracking site; the deleted-artifact hazard; D-12 Phase-24 discipline)
  </read_first>
  <action>
    Gate on the checkpoint outcome. If the human replied `defer`, do NOT flip anything: record in the SUMMARY that DOG-02 remains pending awaiting a captured live run, mark DOGF-01/02/03's mechanical deliverables done but leave the DOG-02/A3 retirement explicitly open, and stop. If the human replied `approved` with a recorded capture: in `examples/03-ticket-to-pr.md` remove every reference to the two deleted MIGR-02 handoff filenames, correct the stale `validate-agent-factory` build-tool reference from the old .mjs form to the current .js tooling, and flip the parity table's CC-native `pending human` cell to retired — annotated as confirmed by the deterministic equivalence oracle plus the recorded captured live run (cite the capture date + verdict). In `.planning/REQUIREMENTS.md` mark DOGF-01/02/03 done and record the DOG-02/A3 retirement (with the captured-run evidence) in the requirement status table. Do NOT touch ROADMAP.md phase status (the orchestrator gates phase completion). This is a doc-only, trace-preserving change — no code, no deleted-artifact assertions.
  </action>
  <acceptance_criteria>
    - If approved: no deleted-handoff reference remains — `grep -cE 'implementation-handoff\.md|qe-handoff\.md' examples/03-ticket-to-pr.md` returns 0.
    - If approved: the stale build-tool reference is corrected — `grep -c 'validate-agent-factory.mjs' examples/03-ticket-to-pr.md` returns 0.
    - If approved: the CC-native parity cell no longer reads `pending human` and cites the oracle + captured-run evidence — `grep -c 'pending human' examples/03-ticket-to-pr.md` returns 0 AND the retirement annotation names the capture date + verdict.
    - If approved: DOGF-01/02/03 are marked done in REQUIREMENTS.md and the DOG-02/A3 retirement is recorded with evidence.
    - If deferred: examples/03-ticket-to-pr.md and REQUIREMENTS.md retain the `pending human` state; the SUMMARY records the deferral and the open live-capture dependency.
    - The regression suite stays green either way: `npx vitest run --exclude '**/scripts/e2e/**'` passes; `node scripts/check-foundation-guards.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' && node scripts/check-foundation-guards.js</automated>
  </verify>
  <done>On approved+recorded capture: A3/DOG-02 is retired in examples/03 + REQUIREMENTS.md (deleted-handoff refs removed, .mjs corrected, parity cell flipped, evidence cited) with the trace preserved. On defer: nothing is flipped, DOG-02 stays pending, and the deferral is recorded. The non-e2e suite and the aggregator stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human evidence → retirement flip | The retired flip is authorized only by a human-confirmed, recorded captured run — the gate against soft-over-claiming the CC-native column. |
| tracking docs → requirement trace | Edits must preserve requirement→trace and never anchor on deleted artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-05-R | Repudiation | retired flip / trace | high | mitigate | Flip only on recorded captured-run evidence (date + verdict); a loud-skip is never a capture; assert on-disk note-set + verdict, never deleted artifacts; preserve requirement→trace (D-01/D-12). |
| T-26-05-C | Over-claim (Repudiation) | cost figure | high | mitigate | Cost never gates the retirement (D-11); DeLM's numbers are never asserted as grugops's. |
| T-26-05-E | Elevation of Privilege | evidence capture path | medium | mitigate | The blocking human checkpoint is never auto-approvable; no harness sets GRUGOPS_PROD_DEPLOY_APPROVED. |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase; doc-only change. |
</threat_model>

<verification>
- Blocking human checkpoint passed with a recorded captured run (date + verdict) OR an explicit defer.
- If approved: `grep -cE 'implementation-handoff\.md|qe-handoff\.md|validate-agent-factory.mjs|pending human' examples/03-ticket-to-pr.md` returns 0 and DOGF-01/02/03 marked done in REQUIREMENTS.md.
- `npx vitest run --exclude '**/scripts/e2e/**'` green and `node scripts/check-foundation-guards.js` exits 0 (never bare `npm test`).
</verification>

<success_criteria>
SC4 is met: A3/DOG-02 is marked retired ONLY after the equivalence oracle passes AND a captured live run is recorded — never on handoff deletion alone and never on cost — with the tracking docs cleaned of deleted-artifact references and the requirement→trace preserved; or, absent a live capture, the retirement is honestly deferred with DOG-02 left pending.
</success_criteria>

## Artifacts this plan produces
- `examples/03-ticket-to-pr.md` — deleted-handoff references removed, `validate-agent-factory` .mjs→.js corrected, parity CC-native cell flipped to retired-with-evidence (on approval).
- `.planning/REQUIREMENTS.md` — DOGF-01/02/03 marked done + DOG-02/A3 retirement recorded with captured-run evidence (on approval); unchanged `pending` state on defer.

<output>
Create `.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-05-SUMMARY.md` when done.
</output>

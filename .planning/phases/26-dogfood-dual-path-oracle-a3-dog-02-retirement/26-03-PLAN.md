---
phase: 26-dogfood-dual-path-oracle-a3-dog-02-retirement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/measure-cost.ts
  - scripts/measure-cost.js
  - scripts/measure-cost.test.ts
autonomous: true
requirements: [DOGF-03]

must_haves:
  truths:
    - "measureCost() defaults to `UNKNOWN - verify` whenever no aggregate usage object is present or the field schema is unconfirmed (D-10) — the honest default, never a fabricated number."
    - "The harness parses the aggregate-usage object from `claude --output-format json` defensively (usage may live at a schema path that is UNKNOWN - verify) and never throws on a missing/unexpected shape."
    - "The fixture test proves BOTH branches: no-usage input → UNKNOWN - verify note; a usage-shaped input → a value only if the field schema is confirmed, else still UNKNOWN - verify."
    - "The harness NEVER asserts DeLM's +10.5pp / ~50% benchmark numbers as grugops's (D-10)."
    - "`npm run build` then `npm run freshness` exits 0 (committed measure-cost.js twin fresh)."
  artifacts:
    - "scripts/measure-cost.ts (+ committed scripts/measure-cost.js twin) exporting measureCost(json)"
    - "scripts/measure-cost.test.ts — fixture test covering the UNKNOWN-verify default and the parse branch"
  key_links:
    - "measureCost consumes the JSON that a Tier-2 authed `claude -p ... --output-format json` run emits; the real grugops number is filled later from an authed capture with a recorded date — it does not gate this phase (D-11)."
  prohibitions:
    - "MUST NOT emit a numeric cost when the usage object is absent or its field schema is unconfirmed — return the `UNKNOWN - verify` note instead (D-10)."
    - "MUST NOT hardcode or assert DeLM's benchmark numbers (+10.5pp / ~50%) as grugops's own."
    - "MUST NOT gate the phase / retirement on a cost figure (DOGF-03 is measurement-only, D-11)."
    - "MUST NOT invoke the live `claude` CLI in this plan's unit/fixture test (the real-number capture is a separate Tier-2 concern)."
---

<objective>
Ship the DOGF-03 token-cost measurement harness so grugops's ~50% cost story is either demonstrated with grugops's own numbers or honestly marked `UNKNOWN - verify` — never fabricated, never borrowing DeLM's benchmark. The harness parses the aggregate usage object from `claude --output-format json`; because the exact usage field schema is `UNKNOWN - verify` this session (not captured), it defaults to the honest `UNKNOWN - verify` note and is proven by a fixture test.

Purpose: give the project a real, honest cost instrument that a later authed run fills in with grugops's own number and a recorded capture date — without ever gating the phase or the retirement on it (D-11).
Output: `scripts/measure-cost.ts` (+ committed `.js` twin) and `scripts/measure-cost.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-CONTEXT.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cost-measurement harness with an honest UNKNOWN - verify default</name>
  <files>scripts/measure-cost.ts, scripts/measure-cost.js</files>
  <read_first>
    - scripts/e2e/uat-live.test.ts (the existing `--output-format json` usage + raised maxBuffer for verbose transcripts at ~:164, :181 — the shape the harness will one day parse)
    - .planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md (Q1/A1 — usage field schema is UNKNOWN - verify; the "Cost harness skeleton" code example; D-10)
    - scripts/freshness.ts (the committed-twin contract — a non-test .ts requires a fresh committed .js)
  </read_first>
  <behavior>
    - measureCost(json) returns an object with a mandatory `note` string; when no aggregate usage object can be located it returns `note: "UNKNOWN - verify: ..."` and no fabricated numbers.
    - It reads the usage object defensively at candidate schema paths (both are UNKNOWN - verify until a real authed capture confirms them) and never throws on a missing/unexpected shape.
    - Until the field schema is confirmed against a real authed run, even a usage-shaped input yields `UNKNOWN - verify` (the harness records the capture date only when a real run is mapped in — future work).
  </behavior>
  <action>
    Create `scripts/measure-cost.ts` (a non-test committed-twin script) exporting `measureCost(json: unknown)`. It attempts to locate an aggregate usage object from a parsed `claude --output-format json` payload at candidate paths (documented inline as `UNKNOWN - verify` until confirmed), returning a small object carrying at least a `note` field. When no usage object is found — or when the field schema is not yet confirmed — return the honest `UNKNOWN - verify` note and do NOT populate numeric fields. Keep it pure and side-effect-free (no live CLI call here). Include an inline comment naming the standing assumption (usage schema unconfirmed) and the no-fabrication rule (never assert DeLM's numbers). Rebuild the committed twin and run freshness.
  </action>
  <acceptance_criteria>
    - The harness exports measureCost: `grep -cE 'export (function|const) measureCost' scripts/measure-cost.ts` returns 1.
    - The honest default exists: `grep -c 'UNKNOWN - verify' scripts/measure-cost.ts` >= 1.
    - No fabricated benchmark: `grep -cE '10\.5|~?50%|DeLM' scripts/measure-cost.ts` returns 0.
    - `npm run build && npm run freshness` exits 0 (committed measure-cost.js twin fresh).
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run freshness</automated>
  </verify>
  <done>measureCost exists, defaults to `UNKNOWN - verify` with no fabricated numbers, and its committed twin is fresh.</done>
</task>

<task type="auto">
  <name>Task 2: Fixture test proving the UNKNOWN - verify default and the defensive parse branch</name>
  <files>scripts/measure-cost.test.ts</files>
  <read_first>
    - scripts/measure-cost.ts (the measureCost export from Task 1)
    - scripts/check-uat-oracles.test.ts (the co-located hermetic Vitest test style — `globals:false`, explicit imports)
    - .planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-RESEARCH.md (D-10 — never fabricate; the honest default)
  </read_first>
  <action>
    Create `scripts/measure-cost.test.ts` importing measureCost from `../scripts/measure-cost.js`. Cover: (1) a payload with NO usage object → the returned `note` contains `UNKNOWN - verify` and no numeric total; (2) a synthetic usage-shaped payload → because the field schema is unconfirmed this session, the result STILL notes `UNKNOWN - verify` (or, only if the executor confirms the schema against a captured authed run and records the date, maps the fields — otherwise the honest default holds); (3) malformed/garbage input → no throw, `UNKNOWN - verify`. Assert the harness never returns a hardcoded benchmark number. Do NOT invoke the live `claude` CLI here.
  </action>
  <acceptance_criteria>
    - The fixture test drives the harness, not the live CLI: `grep -c 'measureCost' scripts/measure-cost.test.ts` >= 1 AND `grep -c 'spawnSync' scripts/measure-cost.test.ts` returns 0.
    - Both the no-usage default branch and the malformed-input no-throw branch are asserted to yield `UNKNOWN - verify`.
    - `npx vitest run scripts/measure-cost.test.ts` passes.
    - The test asserts no fabricated benchmark number is ever returned.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run scripts/measure-cost.test.ts</automated>
  </verify>
  <done>The fixture test proves the harness returns `UNKNOWN - verify` for absent/malformed usage and never fabricates a number; it never calls the live CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted `--output-format json` payload → measureCost | The parsed JSON is untrusted/variable-shape input; the harness must not throw or invent numbers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-03-R | Repudiation | measureCost default | high | mitigate | Default `UNKNOWN - verify`; never fabricate a number; never assert DeLM's +10.5pp / ~50% as grugops's (D-10). No skip flips a UAT; cost never gates (D-11). |
| T-26-03-D | Denial (robustness) | JSON parse | low | mitigate | Defensive read of candidate schema paths; never throws on missing/unexpected shape. |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase; Node stdlib + existing Vitest only. |
</threat_model>

<verification>
- `npx vitest run scripts/measure-cost.test.ts` green.
- `npm run build && npm run freshness` exits 0 (measure-cost.js twin fresh).
- `npx vitest run --exclude '**/scripts/e2e/**'` stays green (never bare `npm test`).
</verification>

<success_criteria>
DOGF-03 is met: an aggregate token-cost harness exists that parses `claude --output-format json` usage or returns `UNKNOWN - verify`, proven by a fixture test, with the grugops number honestly deferred to a later authed capture and never gating the phase.
</success_criteria>

## Artifacts this plan produces
- `scripts/measure-cost.ts` + committed `scripts/measure-cost.js` twin — exported `measureCost(json)` with the honest `UNKNOWN - verify` default.
- `scripts/measure-cost.test.ts` — fixture test for the default + defensive-parse branches (no live CLI). No committed `.js` twin (test file).

<output>
Create `.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-03-SUMMARY.md` when done.
</output>

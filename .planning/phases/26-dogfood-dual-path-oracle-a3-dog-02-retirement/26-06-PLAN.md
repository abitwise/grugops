---
phase: 26-dogfood-dual-path-oracle-a3-dog-02-retirement
plan: 26-06
type: execute
wave: 4
depends_on: [26-04]
autonomous: true
requirements: [DOGF-01, DOGF-02]
files_modified:
  - scripts/e2e/uat-live.test.ts
  - scripts/prod-deploy-deny-match.ts
  - scripts/prod-deploy-deny-match.js
  - scripts/prod-deploy-deny-match.test.ts

must_haves:
  truths:
    - "The offline regression suite (`npx vitest run --exclude '**/scripts/e2e/**'`) runs a non-vacuity RED test for the prod-deploy-deny matcher that returns FALSE on a no-fire transcript (empty output / deploy-mention-without-deny) and TRUE on BOTH the plain frozen guard reason and its markdown-narrated bold form — a repaired safety assertion proven non-vacuous in both directions (D-05: anchor on the frozen on-disk/verdict semantics, never LLM prose)."
    - "Each of the four live `it.skipIf(!LIVE)(...)` cases carries a per-test timeout computed as (claude-call-count x CALL_TIMEOUT_MS + headroom) via a single `liveTimeoutMs(...)` helper, so the vitest bound is derived from CALL_TIMEOUT_MS and can never desync from it again (A1 fix)."
    - "The A2 live case asserts the deny via the single-source `prodDeployDenyFired(r.out)` matcher (markdown-insensitive) instead of the exact-substring match, and still refuses to run when GRUGOPS_PROD_DEPLOY_APPROVED is present (the self-guard is preserved)."
    - "The A3 live case drives a LIGHTER seeded ABC-001 task that reaches the frozen on-disk verdict READY_FOR_HUMAN_REVIEW through BOTH dispatch paths without a pull-request-completion session and without raising CALL_TIMEOUT_MS as the primary fix (GAP-D3; D-05 convergence preserved; D-12: assert the verdict, never deleted handoff filenames)."
    - "The A3-N live case grants the injected node-runner invocation the NARROWEST tool-grant flag verified against the installed `claude` CLI help output, keeps the live agent as the driver (GAP-D2; D-09 Tier-2 confirmation only), and fails honestly (0 notes on the shared root -> a clear failure message, never a fabricated green) if the runner still does not execute."
    - "Retirement stays DEFERRED: examples/03-ticket-to-pr.md, the REQUIREMENTS.md retirement note, and DOG-02/A3 status are UNTOUCHED (GAP-D1; the D-01 and D-02 captured-live-run gate is unchanged)."
    - "The loud-skip honesty keystone (LOUD_SKIP_MARKER + the distinguishable-from-silent-skip tests), the never-set-approval rule, and arg-array `spawnSync` with no shell all survive every edit."
  artifacts:
    - scripts/prod-deploy-deny-match.ts
    - scripts/prod-deploy-deny-match.js
    - scripts/prod-deploy-deny-match.test.ts
    - scripts/e2e/uat-live.test.ts
  key_links:
    - "uat-live.test.ts A2 case -> imports prodDeployDenyFired from scripts/prod-deploy-deny-match.ts (single source)"
    - "scripts/prod-deploy-deny-match.test.ts -> non-vacuity RED both directions over prodDeployDenyFired, plus an anchor-integrity check that the matcher's frozen phrase is present in hooks/guard.ts"
    - "liveTimeoutMs(nCalls) -> CALL_TIMEOUT_MS (the two bounds never desync)"
    - "A3-N claudePrint runner dispatch -> the narrowest tool-grant flag, verified against `claude -p --help` (UNKNOWN - verify until confirmed; never fabricated)"
---

<objective>
Close the four live-harness defects (A1/A2/A3/A3-N) that made the Tier-2 dual-path lane in
`scripts/e2e/uat-live.test.ts` fail on its first-ever live execution (2026-07-03, per 26-UAT.md
`## Gaps`). The 26-04 STRUCTURAL retarget was correct; only the live execution is broken. This plan
repairs the harness so the DOGF-01 (A3 dual-path) and DOGF-02 (A3-N N-agent) live confirmations CAN
run green on a future authed box — WITHOUT flipping the A3/DOG-02 retirement, which stays DEFERRED
per GAP-D1 (the D-01 and D-02 captured-live-run gate is unchanged).

Scope is harness fixes ONLY (GAP-D1): this plan does NOT touch `examples/03-ticket-to-pr.md`, the
`.planning/REQUIREMENTS.md` retirement note, or the DOG-02/A3 status. SC4 remains DEFERRED.

The A2 repair loosens a SAFETY assertion on the prod-deploy guard (the mechanism enforcing CLAUDE.md's
hard "agents never deploy to production without named human confirmation" invariant). A green suite is
never proof for a safety assertion (project memory: burned 12 times). Therefore the A2 fix ships a
mandatory OFFLINE, token-free, both-direction non-vacuity RED test that FAILS when the guard did not
fire — modelled on the RED/GREEN comparator at `scripts/check-uat-oracles.test.ts:227-249`.

Purpose: make the never-live-run Tier-2 lane executable and honest, so a future captured live run can
satisfy D-01's evidence gate — without over-claiming anything now.
Output: a repaired `uat-live.test.ts`, a new single-source markdown-insensitive deny matcher
(`scripts/prod-deploy-deny-match.ts` + committed `.js` twin), and its offline non-vacuity RED test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

# The gap source and the grounding done for this plan
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-UAT.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-CONTEXT.md
@.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-PATTERNS.md

# The one file modified + its source-of-truth analogs
@scripts/e2e/uat-live.test.ts
@hooks/guard.ts
@scripts/worktree-dogfood.test.ts
@scripts/check-uat-oracles.test.ts
@scripts/dual-path-equivalence.ts
</context>

<constraints>
- **Never run the live e2e lane** during execution. `npm test` / `npm run test:e2e` spends real
  tokens (~8 min, can hang). EVERY `<verify>` uses the excluded form
  `npx vitest run --exclude '**/scripts/e2e/**'` (or a single non-e2e test path). The repaired live
  cases are verified STATICALLY (greps, `npx tsc --noEmit`, `liveTimeoutMs` arity) plus by the OFFLINE
  RED test. Obtaining the captured live run is OUT of scope (GAP-D1) — it is the human's job on an
  authed box. `claude --help` / `claude -p --help` (for the A3-N flag) is token-free and permitted;
  it is NOT the live lane.
- **No fabrication (CLAUDE.md).** No `claudePrint` call in this repo has ever granted Bash to a
  headless `claude -p`; the exact grant flag is `UNKNOWN - verify`. Verify it against the installed
  CLI before writing it; if it cannot be verified, mark `UNKNOWN - verify` and fail honestly — never
  invent a flag, never fake a passing gate.
- **Committed `.js` twins (D-13).** tsconfig excludes `**/*.test.ts`, so `uat-live.test.ts` and
  `scripts/prod-deploy-deny-match.test.ts` have NO committed `.js` twin (no freshness impact). But the
  NEW non-test source `scripts/prod-deploy-deny-match.ts` DOES compile to a committed
  `scripts/prod-deploy-deny-match.js` via `npm run build` — Task 2 MUST build it and keep
  `npm run freshness` and `node scripts/check-foundation-guards.js` at exit 0.
- **Keep the honesty keystones.** `LOUD_SKIP_MARKER` + the loud-skip-vs-silent-skip tests (120-160),
  the never-set-`GRUGOPS_PROD_DEPLOY_APPROVED` rule, and arg-array `spawnSync` (no shell) survive
  unchanged. A loud-skip is never a capture (D-11); an absent/timed-out verdict fails as pending.
- **Comment-text discipline.** Do NOT write the blanket skip-all-permissions bypass flag, nor the
  pull-request-completion phrase, by name in any COMMENT in `uat-live.test.ts` — both are negative-grep
  targets in acceptance. Describe those choices by concept in code comments.
</constraints>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: A1 — per-test vitest timeouts (derived from CALL_TIMEOUT_MS) on all four live cases</name>
  <files>scripts/e2e/uat-live.test.ts</files>
  <read_first>
    - scripts/e2e/uat-live.test.ts — CALL_TIMEOUT_MS (173), claudePrint (177-193), the four
      `it.skipIf(!LIVE)(` cases (226 A1, 297 A2, 341 A3, 392 A3-N), the A3-N wip_limit read (396-399).
    - scripts/worktree-dogfood.test.ts:210 — the ONLY in-repo per-test timeout idiom (`it(name, fn, 120_000)`)
      and the beforeAll 2nd-arg form (140).
    - 26-PATTERNS.md section 2 — the call-count budget per case and the "compute from CALL_TIMEOUT_MS" rule.
  </read_first>
  <action>
    Root cause (A1): the live `it()` cases pass no 4th-arg timeout, so they inherit vitest's built-in
    5000ms default while each `claudePrint` is bounded at CALL_TIMEOUT_MS (300_000ms); vitest cannot
    interrupt the synchronous `spawnSync`, so it reports a 5s timeout yet blocks for minutes.
    `vitest.config.ts` sets no `testTimeout`, so the ONLY correct idiom is the `it()` 4th-arg
    (worktree-dogfood.test.ts:210).

    Add a module-scope helper `liveTimeoutMs(nClaudeCalls, extraMs = 0)` returning
    `nClaudeCalls * CALL_TIMEOUT_MS + extraMs`, placed just after the CALL_TIMEOUT_MS declaration so the
    vitest bound is DERIVED from CALL_TIMEOUT_MS (raising UAT_E2E_CALL_TIMEOUT_MS raises both bounds —
    they can never desync again, constraint 5). Also hoist the A3-N `queue.wip_limit` read to module
    scope as `const WIP_LIMIT` (reused by both the A3-N timeout arg here and the A3-N body in Task 3),
    replacing the in-body `const N = factoryConfig.queue.wip_limit` with a reference to `WIP_LIMIT`.

    Add the computed 4th-arg timeout to each live case (count the `claudePrint` calls per case; the two
    `plugin`-install `spawnSync` calls in A1 each carry a 60_000ms bound, folded into headroom):
      - A1 (226): 2 claudePrint calls + 2 install spawns -> `liveTimeoutMs(2, 4 * 60_000)`.
      - A2 (297): 1 call -> `liveTimeoutMs(1, 60_000)`.
      - A3 (341): 2 calls -> `liveTimeoutMs(2, 60_000)`.
      - A3-N (392): WIP_LIMIT calls -> `liveTimeoutMs(WIP_LIMIT, 60_000)`.

    Do NOT touch the loud-skip block (120-160), the LIVE gate (164), claudePrint's arg-array/no-shell
    shape, or the never-set-approval `env: { ...process.env }`. This task adds timeout args + the helper
    + the WIP_LIMIT hoist only.
  </action>
  <acceptance_criteria>
    - `grep -c 'liveTimeoutMs(' scripts/e2e/uat-live.test.ts` returns >= 5 (1 definition + 4 call sites).
    - `grep -c 'WIP_LIMIT' scripts/e2e/uat-live.test.ts` returns >= 2 (module-scope const + A3-N reference).
    - `liveTimeoutMs`'s body multiplies by `CALL_TIMEOUT_MS` (grep confirms `CALL_TIMEOUT_MS` appears inside
      the helper), so the bound is derived, not a hard-coded literal.
    - `npx tsc --noEmit` is clean (a malformed `it()` 4th arg would type-error against vitest's signature).
    - The loud-skip tests still pass under the offline suite: `npx vitest run --exclude '**/scripts/e2e/**'`
      is green (this file is excluded, but tsc + the rest of the suite prove no collateral breakage).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test "$(grep -c 'liveTimeoutMs(' scripts/e2e/uat-live.test.ts)" -ge 5 && npx vitest run --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <done>All four live `it.skipIf(!LIVE)(...)` cases carry a per-test timeout computed via `liveTimeoutMs` from CALL_TIMEOUT_MS; WIP_LIMIT is hoisted to module scope; tsc clean; offline suite green; loud-skip keystone untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A2 — single-source markdown-insensitive deny matcher + offline non-vacuity RED test + rewire the live assertion</name>
  <files>scripts/prod-deploy-deny-match.ts, scripts/prod-deploy-deny-match.js, scripts/prod-deploy-deny-match.test.ts, scripts/e2e/uat-live.test.ts</files>
  <read_first>
    - hooks/guard.ts:90-101 (the `deny()` exit-0 + JSON envelope) and 131-135 (the FROZEN prod-deploy
      deny reason — begins "Production deploy blocked: humans decide, agents execute."). This is the
      source of truth for the anchor; guard.ts is byte-frozen and cannot be imported (it reads stdin on load).
    - scripts/e2e/uat-live.test.ts:297-326 — the A2 case: the APPROVAL self-guard (303-308) and the
      current exact-substring assertion (320-324).
    - scripts/check-uat-oracles.test.ts:227-249 — the RED/GREEN both-direction non-vacuity comparator shape.
    - scripts/worktree-dogfood.test.ts:213-256 — the deterministic (injected-input) non-vacuity pattern.
    - scripts/dual-path-equivalence.ts — the precedent single-source-comparator module (import shape,
      clear-voice header, committed `.js` twin) this new module mirrors.
  </read_first>
  <behavior>
    - `prodDeployDenyFired("")` -> false (guard did NOT fire; empty output is an ALLOW).
    - `prodDeployDenyFired(<a transcript that mentions "deploy" but carries no deny reason>)` -> false.
    - `prodDeployDenyFired(<the plain frozen guard reason>)` -> true.
    - `prodDeployDenyFired(<the same reason narrated with markdown emphasis, e.g. bold "**Production deploy blocked:**">)` -> true.
    - Anchor integrity: the matcher's frozen anchor phrase is present verbatim in `hooks/guard.ts`
      source (so the matcher cannot silently drift from the guard's frozen reason).
  </behavior>
  <action>
    Root cause (A2): the live assertion `r.out.includes("Production deploy blocked: humans decide, agents
    execute.")` is an exact-substring match against the MODEL's markdown-narrated transcript, which
    rendered the phrase in bold. The guard DID fire — the safety invariant held — only the assertion
    mis-scored it. NEVER anchor on LLM prose.

    Create `scripts/prod-deploy-deny-match.ts` (CLEAR voice — this is a safety surface, per CLAUDE.md),
    exporting `prodDeployDenyFired(output: string): boolean`. It normalizes markdown emphasis (strip
    asterisks/underscores) and matches, case-insensitively, the guard's durable frozen phrase
    "humans decide, agents execute" (per 26-PATTERNS.md section 4 this is the durable substring; do NOT
    require the trailing period or the full sentence). Expose the frozen anchor phrase as a named
    exported const so the RED test can assert it against `hooks/guard.ts`. Mirror the module shape of
    `scripts/dual-path-equivalence.ts`; keep it node-stdlib-free (a pure string function). Add a
    clear-voice header citing hooks/guard.ts:131-135 as the source of truth for the anchor.

    Build the committed twin: `npm run build` emits `scripts/prod-deploy-deny-match.js`; commit it.

    Create `scripts/prod-deploy-deny-match.test.ts` (in `scripts/`, NOT `scripts/e2e/`, so it runs under
    the excluded regression suite — token-free, offline). Model it on check-uat-oracles.test.ts:227-249.
    Three cases proving non-vacuity in BOTH directions:
      1. FALSE on no-fire: empty output, and a deploy-mentioning transcript with no deny reason.
      2. TRUE on the plain frozen reason AND on its markdown-narrated bold form (and a mixed-emphasis form).
      3. Anchor integrity: read `hooks/guard.ts` and assert the matcher's exported anchor phrase is a
         substring of it (the matcher cannot drift from the frozen guard reason).

    Rewire the A2 live case (uat-live.test.ts): import `prodDeployDenyFired` from
    `../prod-deploy-deny-match.js` (the committed twin, matching how the file imports other committed
    `.js`), replace the exact-substring assertion with `prodDeployDenyFired(r.out)`, and PRESERVE the
    APPROVAL self-guard (303-308) unchanged — the case must still refuse to run rather than false-pass
    when GRUGOPS_PROD_DEPLOY_APPROVED is set. Keep the honest failure message (surface captured output).
  </action>
  <acceptance_criteria>
    - `scripts/prod-deploy-deny-match.ts`, `scripts/prod-deploy-deny-match.js`, and
      `scripts/prod-deploy-deny-match.test.ts` all exist.
    - `npx vitest run scripts/prod-deploy-deny-match.test.ts` passes (all three cases green), proving the
      matcher FALSE on a no-fire transcript and TRUE on the plain + markdown-narrated deny — non-vacuous.
    - `grep -c 'prodDeployDenyFired' scripts/e2e/uat-live.test.ts` returns >= 2 (import + call site).
    - The A2 self-guard survives: `grep -c 'GRUGOPS_PROD_DEPLOY_APPROVED' scripts/e2e/uat-live.test.ts`
      is unchanged/>= 1 and the case still returns early when the var is present (source inspection).
    - `npm run build` then `npm run freshness` exits 0 (the new committed `.js` twin is byte-fresh).
    - `node scripts/check-foundation-guards.js` exits 0 (aggregator still green with the new module).
    - `npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run freshness && node scripts/check-foundation-guards.js && npx vitest run scripts/prod-deploy-deny-match.test.ts && npx tsc --noEmit && test "$(grep -c 'prodDeployDenyFired' scripts/e2e/uat-live.test.ts)" -ge 2</automated>
  </verify>
  <done>The single-source markdown-insensitive `prodDeployDenyFired` matcher exists with a committed `.js` twin; the offline RED test proves it non-vacuous in both directions plus anchor-integrity against hooks/guard.ts; the A2 live case uses it and preserves the self-guard; freshness + foundation guards + tsc all green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: A3 — shrink the seeded task; A3-N — verify then apply the narrowest Bash-grant flag (live agent stays the driver)</name>
  <files>scripts/e2e/uat-live.test.ts</files>
  <read_first>
    - scripts/e2e/uat-live.test.ts:341-378 — the A3 case: TICKET (344), FROZEN_VERDICT (348), the two
      dispatch `claudePrint` calls (351-364), the honest-failure surface (373-376).
    - scripts/e2e/uat-live.test.ts:392-510 — the A3-N case: the runner source (429-450), the per-agent
      dispatch loop (456-479) whose `claudePrint` (473-478) instructs the agent to run `node runner.mjs`,
      and the on-disk convergence assertions (481-505).
    - 26-PATTERNS.md section 5 (what A3-N must NOT collapse into: the deterministic twin) and section 6
      (the `claude -p` tool-grant dependency; the verified-available flag list; the `UNKNOWN - verify` note).
    - 26-CONTEXT.md D-05 (equivalence = on-disk admitted note-set + verdict), D-09 (live = Tier-2
      confirmation only), D-12 (assert the verdict, never deleted handoffs).
  </read_first>
  <action>
    A3 root cause (GAP-D3): the current heavy seeded TICKET at line 344 (a full
    implement-ABC-001-and-carry-it-to-a-pull-request session) makes both dispatch sessions hit the 300s
    per-call cap before emitting the frozen READY_FOR_HUMAN_REVIEW verdict. The assertion is the frozen
    ON-DISK verdict, not the pull request. Replace the TICKET with a LIGHTER seeded ABC-001 task that
    drops the pull-request-completion clause and still traverses BOTH dispatch paths through the §14
    gate to READY_FOR_HUMAN_REVIEW — drop the pull-request-completion clause so no branch/PR/build
    session is driven. Keep the seeded task referencing ABC-001 and keep FROZEN_VERDICT =
    "READY_FOR_HUMAN_REVIEW" (D-12: assert the verdict, never deleted handoff filenames). Do NOT raise
    CALL_TIMEOUT_MS as the primary fix. Shrinking must NOT weaken the dual-path convergence claim: both
    the sequential AGENTS.md role-load and the `/grugops` sub-agent path must still reach the same frozen
    on-disk verdict (D-05 = on-disk admitted note-set + verdict string). Preserve the honest-failure
    surface (373-376) that reports captured seq/sub output when the verdict is unreached. Exact lighter
    wording is your discretion (Claude's Discretion, 26-CONTEXT); the invariants above are fixed. Do NOT
    write the pull-request-completion phrase in any comment (it is a negative-grep target).

    A3-N root cause (GAP-D2): the live `claude -p` dispatch (473-478) instructs the agent to run
    `node runner.mjs ...`, but a headless `claude -p` under default permissions will not execute an
    arbitrary Bash command -> 0 notes on the shared context root. This is a permission denial, NOT agent
    whim. Fix per GAP-D2: KEEP the live agent as the driver (do NOT call the runner directly from the
    harness — that would collapse this case into the already-green deterministic twin
    scripts/worktree-dogfood.test.ts and prove nothing about live dispatch). Grant the node invocation
    explicitly via a VERIFIED flag, and fail honestly if it still does not run.

    Flag verification (constraint 2 — no fabrication): BEFORE writing any flag, run `claude --help` and
    `claude -p --help`, capture the output verbatim into the SUMMARY, and identify the exact
    flag+spelling that grants headless Bash SCOPED to the node invocation. Candidates from 26-PATTERNS.md
    section 6 (NOT proven — verify): a scoped `--allowedTools` value for Bash (prefer the narrowest form,
    e.g. a node-scoped Bash grant, over blanket `Bash`), or a `--permission-mode` value. Choose the
    NARROWEST form the help output confirms. Do NOT use the blanket skip-all-permissions bypass in
    committed test code (name it by concept only, never verbatim, in any comment). Apply the verified
    flag ONLY to the A3-N runner dispatch `claudePrint` (473-478), not globally. If the flag CANNOT be
    verified against the installed CLI, do NOT invent one: leave a clear-voice `UNKNOWN - verify` note at
    the dispatch site and fail the task honestly (surface the block) — the existing 0-notes assertion
    already fails honestly rather than fabricating a green.

    Do NOT change the runner source, the shared-root construction, the on-disk convergence assertions,
    or the never-set-approval / no-shell / arg-array shape.
  </action>
  <acceptance_criteria>
    - `grep -c 'READY_FOR_HUMAN_REVIEW' scripts/e2e/uat-live.test.ts` returns >= 1 (FROZEN_VERDICT preserved).
    - `grep -c 'ABC-001' scripts/e2e/uat-live.test.ts` returns >= 1 (the seeded ticket still references ABC-001).
    - `grep -ci 'take it to a pr' scripts/e2e/uat-live.test.ts` returns 0 (the pull-request-completion clause is gone).
    - `grep -c 'dangerously-skip-permissions' scripts/e2e/uat-live.test.ts` returns 0 (no blanket bypass in committed test code).
    - The A3-N runner dispatch (473-478) either carries the VERIFIED narrow tool-grant flag (with the
      `claude -p --help` evidence captured verbatim in the SUMMARY) OR carries a clear-voice
      `UNKNOWN - verify` note and the task is reported as an honest block — never a fabricated flag.
    - The A3-N case still spawns real `claude` dispatches (the live agent remains the driver): the
      per-agent `claudePrint` loop is intact and the harness does NOT invoke the runner directly.
    - `npx tsc --noEmit` is clean; `npx vitest run --exclude '**/scripts/e2e/**'` remains green.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test "$(grep -c 'READY_FOR_HUMAN_REVIEW' scripts/e2e/uat-live.test.ts)" -ge 1 && test "$(grep -c 'ABC-001' scripts/e2e/uat-live.test.ts)" -ge 1 && test "$(grep -ci 'take it to a pr' scripts/e2e/uat-live.test.ts)" -eq 0 && test "$(grep -c 'dangerously-skip-permissions' scripts/e2e/uat-live.test.ts)" -eq 0 && npx vitest run --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <done>A3 drives a lighter ABC-001 task reaching the frozen verdict via both dispatch paths (no PR session, CALL_TIMEOUT_MS unchanged); A3-N grants the injected node runner the narrowest flag verified against `claude -p --help` (or an honest UNKNOWN - verify block) while keeping the live agent as the driver and failing honestly on 0 notes; retirement untouched; tsc + offline suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> child `claude` CLI | the harness spawns an untrusted LIVE agent; the agent's stdout and its filesystem side-effects cross back into the harness's assertions. |
| repaired A2 assertion -> prod-deploy safety invariant | the A2 assertion is the observation point for CLAUDE.md's hard "no unapproved production deploy" invariant; a mis-scored assertion silently erodes that invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-A2 | Tampering | scripts/prod-deploy-deny-match.ts + uat-live.test.ts A2 case | high | mitigate | The realistic high-severity threat is a VACUOUS safety assertion — a repaired A2 that silently passes when the prod-deploy guard did NOT fire, turning grugops's headline safety invariant into an unverified claim. Mitigation: the mandatory OFFLINE non-vacuity RED test (Task 2) proves the matcher returns FALSE on a no-fire transcript (empty / deploy-mention-without-deny) and TRUE on the frozen reason + its markdown form; the RED test also asserts the matcher's anchor phrase is present in `hooks/guard.ts` so it cannot drift from the frozen guard reason. Anchored on the frozen deny reason, never LLM prose. |
| T-26-A3N | Elevation of Privilege | uat-live.test.ts A3-N runner dispatch | medium | mitigate | Granting Bash to a headless `claude -p` (GAP-D2) widens what a live test-run agent may execute. Mitigation: grant only the NARROWEST tool-grant flag verified against `claude -p --help`, scoped to the A3-N runner dispatch call only; the blanket skip-all-permissions bypass is prohibited in committed test code; the flag is verified before use or marked `UNKNOWN - verify` (no fabricated flag). |
| T-26-SCOPE | Repudiation | examples/03-ticket-to-pr.md + REQUIREMENTS retirement note | low | accept | Out of scope by GAP-D1: retirement stays DEFERRED until a captured live run satisfies the D-01/D-02 gate; no deleted artifact is asserted (D-12); the requirement->trace is preserved. Accepted because flipping now would over-claim the CC-native parity column. |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | accept | N/A — this plan installs NO packages (it edits a vitest harness and adds one node-stdlib-free source module). No Package Legitimacy Gate is required; no `[ASSUMED]`/`[SUS]` package checkpoint applies. |
</threat_model>

<verification>
Phase-level checks (NONE invoke the live e2e lane):

- `npx tsc --noEmit` — clean across the whole tree (proves the timeout args, the new module, and the
  A2 import all type-check).
- `npm run build && npm run freshness` — exit 0 (the new committed `scripts/prod-deploy-deny-match.js`
  twin is byte-fresh; no drift).
- `node scripts/check-foundation-guards.js` — exit 0 (ALL CHECKS PASSED; the aggregator, incl.
  oracleDualPathEquivalence, stays green with the new module present).
- `npx vitest run scripts/prod-deploy-deny-match.test.ts` — the offline non-vacuity RED test passes
  (both directions + anchor integrity).
- `npx vitest run --exclude '**/scripts/e2e/**'` — the full offline regression suite is green (the live
  lane is excluded; token-free).
- Static greps per task acceptance (liveTimeoutMs arity; prodDeployDenyFired usage; FROZEN_VERDICT +
  ABC-001 preserved; pull-request-completion clause and blanket-bypass flag absent).
- Honesty keystones intact: `grep -c 'LOUD_SKIP_MARKER'` >= 1; the never-set-approval self-guard present;
  no `shell: true` introduced.
- Retirement untouched: `examples/03-ticket-to-pr.md` and the `.planning/REQUIREMENTS.md` retirement note
  are byte-identical to their pre-plan state (git diff empty for both).
</verification>

<success_criteria>
- All four defects A1/A2/A3/A3-N have a repair, verified statically + by the offline RED test (never a live run).
- The A2 repair ships a mandatory offline non-vacuity RED test that FAILS when the guard did not fire
  and PASSES on the frozen reason + its markdown form (T-26-A2 mitigated).
- The A3-N Bash-grant flag is verified against the installed CLI before use, or honestly marked
  `UNKNOWN - verify` — no invented flag; the live agent stays the driver; 0 notes fails honestly.
- The A3 seeded task is lighter and reaches the frozen verdict via both paths without raising
  CALL_TIMEOUT_MS; D-05 convergence preserved.
- No task touches `examples/03-ticket-to-pr.md`, the REQUIREMENTS retirement note, or flips DOG-02/A3.
- Loud-skip / never-set-approval / no-shell keystones preserved; `npm run freshness` and
  `node scripts/check-foundation-guards.js` exit 0.
</success_criteria>

<artifacts_this_phase_produces>
New files:
- `scripts/prod-deploy-deny-match.ts` — single-source, markdown-insensitive prod-deploy-deny matcher (clear voice).
- `scripts/prod-deploy-deny-match.js` — its committed `tsc` build twin (freshness-checked).
- `scripts/prod-deploy-deny-match.test.ts` — offline, token-free non-vacuity RED test (runs under the excluded regression suite).

New exported symbols (in scripts/prod-deploy-deny-match.ts):
- `prodDeployDenyFired(output: string): boolean` — markdown-insensitive matcher over the guard's frozen deny reason.
- A named exported const holding the frozen anchor phrase (e.g. the "humans decide, agents execute" durable substring) — asserted against hooks/guard.ts by the RED test.

New symbols added to scripts/e2e/uat-live.test.ts:
- `liveTimeoutMs(nClaudeCalls, extraMs = 0)` — module-scope helper deriving each live case's vitest timeout from CALL_TIMEOUT_MS.
- `WIP_LIMIT` — module-scope const (hoisted `queue.wip_limit` read) reused by the A3-N timeout and the A3-N body.
- Import of `prodDeployDenyFired` from `../prod-deploy-deny-match.js`.

New test names (in scripts/prod-deploy-deny-match.test.ts):
- "non-vacuity: prodDeployDenyFired is FALSE when the guard did not fire (empty output and a deploy-mention-without-deny transcript)"
- "non-vacuity: prodDeployDenyFired is TRUE on the plain frozen deny reason AND its markdown-narrated bold form"
- "anchor integrity: the matcher's frozen anchor phrase is present verbatim in hooks/guard.ts"

Modified (behavior of existing cases, not new symbols):
- A1/A2/A3/A3-N live cases gain a computed 4th-arg timeout; A2 uses the matcher; A3 uses a lighter ABC-001 ticket; A3-N gains the verified narrow Bash-grant flag on its runner dispatch.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/26-dogfood-dual-path-oracle-a3-dog-02-retirement/26-06-SUMMARY.md` when done.
The SUMMARY MUST include the verbatim `claude --help` / `claude -p --help` output captured for the
A3-N flag verification (or an explicit `UNKNOWN - verify` note if the flag could not be verified).
</output>

---
phase: 04-workflows-cadence-backpressure
plan: 07
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06]
files_modified:
  - agent-factory/workflows/12-release.md
  - agent-factory/workflows/13-incident.md
autonomous: true
requirements: [FLOW-04, FLOW-05, SAFE-01]
must_haves:
  truths:
    - "12-release requires a named human approval and a human-confirmed production action keyed to production_requires_human_confirmation: true - it never deploys prod itself"
    - "12-release writes REL-xxxx.md + release-handoff.md, cites the release-readiness checklist, attaches NFR evidence, and carries the READY_TO_RELEASE | BLOCKED | RELEASED status"
    - "13-incident runs the blameless postmortem path (never blames a person), writes incident-postmortem.md, and creates follow-up tickets into Backlog"
  artifacts:
    - path: "agent-factory/workflows/12-release.md"
      provides: "Enterprise release workflow with named-human approval gate (SAFE-01)"
      contains: "production_requires_human_confirmation"
      min_lines: 24
    - path: "agent-factory/workflows/13-incident.md"
      provides: "Enterprise incident workflow with blameless postmortem (FLOW-04)"
      contains: "incident-postmortem.md"
      min_lines: 22
  key_links:
    - from: "agent-factory/workflows/12-release.md"
      to: "plans/releases/REL-xxxx.md"
      via: "Release Manager writes the release record"
      pattern: "REL-xxxx.md"
    - from: "agent-factory/workflows/13-incident.md"
      to: "agent-factory/handoffs/incident-postmortem.md"
      via: "Incident Responder blameless postmortem output"
      pattern: "incident-postmortem.md"
---

<objective>
Author the two enterprise workflows: `12-release.md` (the named-human release approval gate - the load-bearing SAFE-01 deploy gate this phase) and `13-incident.md` (the blameless incident path - FLOW-04). Each reproduces its §7.13/§7.14 spine on the 10-section template, derives connective sections from frozen names (D-24), and renders its safety/blameless prose in clear voice. `12` is keyed to `production_requires_human_confirmation: true` and never deploys prod itself; the mechanical enforcement remains Phase 5 (SAFE-02) - this text stays dispatch-neutral.

Purpose: Deliver the enterprise lifecycle (FLOW-04) and complete SAFE-01 - "humans decide, agents execute" rendered as a named-human prod gate.
Output: `agent-factory/workflows/12-release.md`, `agent-factory/workflows/13-incident.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md
@.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md
@agent-factory/roles/release-manager.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 12-release.md (named-human release gate, SAFE-01)</name>
  <files>agent-factory/workflows/12-release.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 12-release.md (§7.13 Flow `Ready to Release -> Release Manager -> approval gate -> deploy plan -> (human-confirmed) deploy -> Done`; Steps set SemVer, compile changelog + release notes, confirm migration + rollback + DR, attach NFR/security/compliance evidence, record named approval then a human confirms the production action; Output plans/releases/REL-xxxx.md + release-handoff.md; status READY_TO_RELEASE | BLOCKED | RELEASED) + the 10-section template.
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `12-release.md - SPECIAL CASE` (analog release-manager.md; the verbatim deploy-gate phrasing to reuse - "named human approves", "human-confirmed", "never deploy prod yourself"; harness asserts "named human"/"human-confirmed"/production_requires_human_confirmation) + Pattern S6 (clear voice), S7 (SAFE-01 prose - NO hooks.json/PreToolUse/${CLAUDE_PLUGIN_ROOT}/subagent language).
    - `agent-factory/roles/release-manager.md` - the closest semantic analog; read lines around 33 and 49 for the exact deploy-gate prose to render; board move Ready to Release -> Done (only after named human approves); status tokens.
    - `agent-factory/checklists/release-readiness-checklist.md` (cited), `plans/releases/` (REL-xxxx.md dir, .gitkeep), `agent-factory/handoffs/release-handoff.md`, `plans/nfr-catalog.md` (evidence). `agent-factory/config/factory.config.json` - production_requires_human_confirmation: true (line ~45), environments, mode.
  </read_first>
  <action>
    Create `agent-factory/workflows/12-release.md` with kind: workflow frontmatter (<=3 fields; optionally tier: enterprise) then `# Workflow: Release` and the 10 sections in order. When to use (clear voice, light wink allowed only here): declare this is an enterprise workflow (optional in lean), run when a ticket reaches Ready to Release; reproduce the §7.13 Flow. Agents involved: Release Manager. Steps (clear voice, reproduce §7.13 + the release-manager deploy-gate prose): set the SemVer version; compile the changelog + release notes; confirm migration + rollback + DR plans; attach NFR/security/compliance evidence from plans/nfr-catalog.md; cite agent-factory/checklists/release-readiness-checklist.md; RECORD a named human approval, THEN a human confirms the production action - keyed to production_requires_human_confirmation: true. State plainly (clear voice, reuse the frozen phrasing): deploy happens only after a named human approves; the production action is always human-confirmed; the workflow never deploys prod itself. Board moves: Ready to Release -> Done (only after named human approval). Handoffs produced: agent-factory/handoffs/release-handoff.md; Output record plans/releases/REL-xxxx.md. Trace updates: complete the Release column in plans/traceability.md (enterprise DoD not met until the row is complete). Metrics emitted: a frozen-9 subset if applicable, else none. Stop conditions: readiness checklist fails or no named approval -> status BLOCKED, do not deploy. Done condition (reproduce §7.13): REL-xxxx.md + release-handoff.md written; status one of READY_TO_RELEASE | BLOCKED | RELEASED. MUST contain the literal phrases "named human", "human-confirmed", and the token production_requires_human_confirmation. NO hooks.json / PreToolUse / ${CLAUDE_PLUGIN_ROOT} / subagent language (the mechanism is Phase 5). Keep ~one screen; cite only frozen names.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/12-release.md && grep -qiF 'named human' agent-factory/workflows/12-release.md && grep -qiF 'human-confirmed' agent-factory/workflows/12-release.md && grep -qF 'production_requires_human_confirmation' agent-factory/workflows/12-release.md && grep -qF 'REL-xxxx.md' agent-factory/workflows/12-release.md && ! grep -qF 'PreToolUse' agent-factory/workflows/12-release.md && echo OK-12; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/12-release.md exists with kind: workflow frontmatter (<=3 fields) and all 10 headings in order (V-02/V-03 green for 12).
    - Contains the literal "named human", "human-confirmed", and production_requires_human_confirmation (V-11 green for 12).
    - Cites plans/releases/REL-xxxx.md, agent-factory/handoffs/release-handoff.md, agent-factory/checklists/release-readiness-checklist.md, plans/nfr-catalog.md; names the READY_TO_RELEASE | BLOCKED | RELEASED status; no invented name (V-12 green).
    - Contains NO hooks.json / PreToolUse / ${CLAUDE_PLUGIN_ROOT} / subagent language (dispatch-neutral; SAFE-02 deferred to Phase 5).
  </acceptance_criteria>
  <done>12 authored, named-human prod gate in clear voice, dispatch-neutral, V-11/V-12 green.</done>
</task>

<task type="auto">
  <name>Task 2: Author 13-incident.md (blameless postmortem, FLOW-04)</name>
  <files>agent-factory/workflows/13-incident.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 13-incident.md (§7.14 Flow `incident detected -> Incident Responder -> mitigate/rollback -> blameless postmortem -> follow-up tickets`; Steps assess blast radius, propose mitigation + rollback, capture timeline, write incident-postmortem.md, create follow-up tickets into Backlog, hand lessons to the Coach; never blames a person; Done when postmortem written + follow-up tickets created) + the 10-section template.
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `13-incident.md - SPECIAL CASE` (analog incident-postmortem.md, frozen anchor heading "## Root cause (systemic, not personal)"; must cite incident-postmortem.md + render "blameless"/"never blames"; declares mode enterprise) + Shared Patterns S1, S2, S4.
    - `agent-factory/roles/orchestrator.md` (shape analog). `agent-factory/roles/incident-responder.md` (the role 13 composes; no board move -> incident-postmortem.md), `agent-factory/roles/factory-coach.md` (receives lessons).
    - `agent-factory/handoffs/incident-postmortem.md` - read its blameless framing (the "## Root cause (systemic, not personal)" heading) to mirror the blameless prose. `plans/tickets/` (follow-up tickets -> Backlog).
  </read_first>
  <action>
    Create `agent-factory/workflows/13-incident.md` with kind: workflow frontmatter (<=3 fields; optionally tier: enterprise) then `# Workflow: Incident` and the 10 sections in order. When to use (clear voice, light wink only here): declare this is an enterprise, post-release workflow; reproduce the §7.14 Flow. Agents involved: Incident Responder (feeds the Factory Coach). Steps (clear voice): assess blast radius; propose mitigation + rollback; capture the timeline; write agent-factory/handoffs/incident-postmortem.md as a BLAMELESS postmortem (focus on systemic root cause, never blames a person); create follow-up tickets into the Backlog; hand lessons to the Factory Coach. Board moves: none new (incident-responder causes no transition); follow-up tickets enter the frozen Backlog column. Handoffs produced: agent-factory/handoffs/incident-postmortem.md. Trace updates: rows for follow-up tickets in plans/traceability.md. Metrics emitted: Escaped defects (from the frozen 9) if applicable, else none. Stop conditions: blast radius unclear -> mitigate/rollback first, postmortem after. Done condition (reproduce §7.14): the blameless postmortem is written and follow-up tickets are created into Backlog. MUST contain the literal string incident-postmortem.md and the word "blameless" (or "never blames a person"). Keep ~one screen; cite only frozen names.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/13-incident.md && grep -qF 'incident-postmortem.md' agent-factory/workflows/13-incident.md && grep -qiE 'blameless|never blames' agent-factory/workflows/13-incident.md && echo OK-13; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/13-incident.md exists with kind: workflow frontmatter (<=3 fields) and all 10 headings in order (V-02/V-03 green for 13).
    - Contains the literal string incident-postmortem.md and "blameless" (or "never blames") (V-13 green).
    - Follow-up tickets route to the frozen Backlog column; cites the frozen incident-postmortem.md handoff; no invented name (V-12 green).
  </acceptance_criteria>
  <done>13 authored, blameless incident path, V-13/V-12 green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green gate (all 14 + harness)</name>
  <files>agent-factory/workflows/00-bootstrap-greenfield.md, agent-factory/workflows/01-bootstrap-brownfield.md, agent-factory/workflows/02-idea-to-epics.md, agent-factory/workflows/03-epic-to-tickets.md, agent-factory/workflows/04-ticket-to-pr.md, agent-factory/workflows/05-pr-quality-gate.md, agent-factory/workflows/06-uat-pack.md, agent-factory/workflows/07-backlog-refinement.md, agent-factory/workflows/08-sprint-planning.md, agent-factory/workflows/09-daily-sweep.md, agent-factory/workflows/10-sprint-review.md, agent-factory/workflows/11-retro.md, agent-factory/workflows/12-release.md, agent-factory/workflows/13-incident.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-VALIDATION.md` "Validation Sign-Off" - the full-green criteria (all 14 present, 10 sections each in order, routing match, loop single-sourced, cadence tags, SAFE-01 prose, zero drift).
    - `.planning/phases/04-workflows-cadence-backpressure/check-structure.sh` (the harness authored in Plan 01).
  </read_first>
  <action>
    With all 14 workflow files now present (Plans 02-07), run the full harness and confirm ALL checks pass (exit 0). If any check is RED, fix the offending workflow file (this is a wiring/citation fix - cite the correct frozen name, restore a missing section heading, fix section order, or remove an invented name - never weaken the harness). Do NOT edit check-structure.sh to make a check pass. Confirm exactly 14 `*.md` files exist in agent-factory/workflows/ (excluding .gitkeep) and no 14-*.md file exists. This task is the Phase-4 acceptance gate before /gsd-verify-work.
  </action>
  <verify>
    <automated>sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh && N=$(ls agent-factory/workflows/*.md 2>/dev/null | wc -l | tr -d ' ') && test "$N" = "14" && ! ls agent-factory/workflows/14-*.md 2>/dev/null && echo "ALL-14-GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh` exits 0 (ALL CHECKS PASSED) - V-01 through V-13 all green.
    - Exactly 14 `*.md` files in agent-factory/workflows/ (excluding .gitkeep); no 14-*.md file.
    - check-structure.sh was NOT weakened to pass (it is byte-identical to the Plan-01 deliverable aside from any agreed correction noted in the SUMMARY).
  </acceptance_criteria>
  <done>Full harness green; all 14 workflows present and contract-conformant; Phase-4 acceptance gate met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 12 release text -> production deploy | This is the highest-stakes safety boundary in the phase: the prose gate is the only thing (this phase) standing between an agent and an unattended prod deploy; the mechanical hook is Phase 5 |
| 13 incident text -> blameless culture | The blameless framing is a process-integrity control; a blame-driven postmortem template would corrupt incident handling |

Markdown-authoring task; no runtime input/network/auth surface. STRIDE web categories informational/LOW. In-scope threats are process-safety (prod deploy) and process-integrity (blameless).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Elevation of Privilege | 12 missing the named-human prod gate, allowing an unattended deploy | mitigate | 12 reproduces the release-manager deploy-gate prose ("named human"/"human-confirmed"/"never deploy prod yourself") keyed to production_requires_human_confirmation: true; harness V-11 asserts these tokens; mechanical SAFE-02 hook is Phase 5 |
| T-04-07-02 | Tampering | 12 inlining a tool-specific mechanism (PreToolUse hook) prematurely, coupling the kit to one tool | mitigate | Keep 12 dispatch-neutral prose; acceptance criterion asserts NO hooks.json/PreToolUse/${CLAUDE_PLUGIN_ROOT}/subagent language |
| T-04-07-03 | Repudiation | 13 producing a blame-driven postmortem | mitigate | 13 mirrors incident-postmortem.md's "## Root cause (systemic, not personal)" framing; harness V-13 asserts "blameless"/"never blames" |
| T-04-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; nothing to audit |
</threat_model>

<verification>
Run `sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh` after each file, then the full-suite gate in Task 3. V-11 (12 release safety), V-13 (13 blameless), and the final all-14 green (V-01..V-13) must pass. Task 3 is the Phase-4 acceptance gate.
</verification>

<success_criteria>
12 authored as a named-human prod gate (dispatch-neutral, SAFE-01); 13 authored as the blameless incident path (FLOW-04); the full harness passes with all 14 workflows present and contract-conformant (V-01..V-13 green).
</success_criteria>

<output>
Create `.planning/phases/04-workflows-cadence-backpressure/04-07-SUMMARY.md` when done.
</output>

---
phase: 27-spawn-correctness-kit-set-authority
plan: 30
type: execute
wave: 2
depends_on: ["27-29"]
files_modified:
  - scripts/frontmatter.ts
  - scripts/frontmatter.js
  - scripts/frontmatter.test.ts
  - scripts/check-foundation-guards.test.ts
autonomous: true
gap_closure: true
gap_round: 4
requirements: [KIT-03, SPAWN-04]
user_setup: []

must_haves:
  truths:
    - "A document whose first non-blank line is a YAML directive line beginning with a percent sign returns the ok:false unreadable arm naming the directive, instead of the ok:true / no-keys legitimately-keyless arm (D-34, closes IN-02)."
    - "The legitimately-keyless arm still succeeds for a document with genuinely NO frontmatter block — a body-only file is a legitimate state and must not be turned red by the directive refusal."
    - "The scoping gate that limits the tools-key absence and emptiness findings to agent adapters is EXERCISED in both directions: a skill fixture declaring no tools allow-list keeps the guard green, and an agent adapter declaring none reds it (closes IN-01)."
    - "Empty edge: the three input states a frontmatter reader can be in — a closing block with keys, no block at all, and an unreadable block — stay exactly three, with the directive-prefixed document moved from the second into the third."
    - "A directive line appearing anywhere OTHER than before the opening delimiter is not special-cased: it is either inside the block (where the existing key-line rule already refuses it) or in the body (where it is not read at all)."
    - statement: "Claude Code's own frontmatter reader also requires the opening delimiter on line 1, so a directive-prefixed document is inert on the platform rather than rogue; the refusal is therefore honest-pending rather than a reproduced bypass."
      verification: backstop
    - statement: "SPAWN-04's edge probe returned unclassified and is recorded here as a flagged assumption rather than auto-resolved: the probe text is 'unclassified — review manually', and the reviewer-identified concrete instance (the unexercised scoping gate) is closed by this plan's negative control, but the category itself is not mechanically derivable and stays unresolved."
      verification: backstop
  artifacts:
    - "scripts/frontmatter.ts — the directive refusal in the frontmatter reader"
    - "scripts/frontmatter.js — the committed compiled twin, freshness-clean"
    - "scripts/frontmatter.test.ts — directive cases in both directions"
    - "scripts/check-foundation-guards.test.ts — the skill-with-no-allow-list negative control beside the shipped agent-adapter absence case"
  key_links:
    - "scripts/frontmatter.ts parseFrontmatter -> the three-outcome contract stated in the module header"
    - "scripts/check-foundation-guards.ts spawn-grant scan loop -> the agent-adapter scoping gate -> the absence and emptiness findings"
  prohibitions:
    - "No new dependency is added; the dev-dependency set stays exactly typescript and vitest plus the type-only node types."
    - "The genuinely-keyless success arm is never removed or narrowed to make the directive refusal simpler; a body-only file must keep succeeding."
    - "The scoping gate is never deleted to make the negative control pass; the control exists to prove the gate is load-bearing, not to remove it."
    - "A behavioral claim is never accepted on a source read; each carries a captured before/after transcript against the committed compiled output."
---

<objective>
Close the two remaining silent-arm findings in the read path: **IN-02**, a YAML directive line before
the opening delimiter yielding "no frontmatter, no keys" — a second silent SUCCESS arm in the very
module round 4 is fixing — and **IN-01**, the agent-adapter scoping gate in the spawn-grant scan that
is a no-op on the live tree and has no case.

Purpose: leaving a second silent-SUCCESS arm inside the module being repaired is how round 5 starts,
and an unexercised scoping gate means whether a future skill omitting its allow-list reds the gate is
decided by an untested branch.

Output: a frontmatter reader whose three-outcome contract is honest about directive-prefixed
documents, and a scoping gate proven load-bearing in both directions.

## Decisions this plan implements

- **D-31** — round 4 closes all eight round-3 findings; this plan owns IN-02 and IN-01.
- **D-34 (NEW)** — a leading YAML directive line is refused BY NAME. Given the module's stated
  three-outcome contract, a directive-prefixed document belongs in the *unreadable* arm, not the
  *legitimately keyless* one: the document plainly carries a tools value, and returning "no keys"
  over it is the same silent-success shape as CR-01 one level up. The reviewer's `UNKNOWN - verify`
  is honored rather than erased — the platform most likely sees no frontmatter either, so this is
  recorded as honest-pending, not as a reproduced bypass. Reversibility: **reversible**.

## Flagged assumptions (probe rows this plan does not silently drop)

- **SPAWN-04 / unclassified** — the edge probe returned `unclassified — review manually` and is
  therefore left **unresolved** per the fallback rules, never auto-backstopped by inference. The one
  concrete instance a human identified in this category (the unexercised scoping gate) is closed by
  Task 2; the category itself remains open and is recorded above as a flagged assumption.
- **SPAWN-02 / unclassified** and **SPAWN-05 / unclassified** — same treatment, and both belong to
  requirements verified clean across three rounds. Recorded here so they are not lost; no plan work.

## Artifacts this phase produces (new symbols — exclude from drift verification)

| Symbol | File | Kind |
|---|---|---|
| `YAML_DIRECTIVE` | `scripts/frontmatter.ts` | new constant (the leading-directive-line pattern) |
| the directive refusal reason | `scripts/frontmatter.ts` | new failure-arm string in the frontmatter reader |
| `plantSkillWithoutToolsKey` | `scripts/check-foundation-guards.test.ts` | new test helper (removes an allow-list declaration from a mirrored skill) |
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS-3.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-29-SUMMARY.md
@scripts/frontmatter.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refuse a leading YAML directive line by name (IN-02)</name>

  <read_first>
    - `scripts/frontmatter.ts` `parseFrontmatter` and the module header paragraph stating the THREE
      outcomes — this change moves one input from the second outcome to the third, so the header
      must be amended in the same edit rather than left describing the old partition.
    - `scripts/frontmatter.js` — the committed compiled twin, which the reproduction runs against.
    - `27-REVIEW-GAPS-3.md` section IN-02 — the verified reproduction string and the
      `UNKNOWN - verify` caveat that must survive into the header wording.
    - `scripts/frontmatter.test.ts` — the shipped cases covering the genuinely-keyless success arm
      and the unterminated-block failure arm; the new cases sit beside them.
  </read_first>

  <files>scripts/frontmatter.ts, scripts/frontmatter.js, scripts/frontmatter.test.ts</files>

  <behavior>
    - A document whose first non-blank line begins with a percent sign followed by a directive name,
      and whose next line is the opening delimiter, returns the ok-false arm and the reason names the
      directive line.
    - The same document's tools value is plainly a grant, so the failure arm must be reached instead
      of the value-false success arm.
    - A document with NO frontmatter block at all still returns the ok-true / no-keys arm.
    - A document whose body (after a well-formed frontmatter block) contains a percent-prefixed line
      is unaffected.
    - A percent-prefixed line INSIDE the frontmatter block still falls to the existing unreadable
      key-line refusal, not to the new directive refusal — one input, one reason.
  </behavior>

  <action>
    Capture the RED transcript FIRST against the committed `scripts/frontmatter.js`: run the
    text-level spawn-grant reader over the reviewer's verified reproduction — a document whose first
    line is a `%TAG` directive, followed by the opening delimiter, a name key, and a `tools` value
    naming the spawn tool with a scoped name — and record that it returns the ok-true / value-false
    result today.

    Then add a `YAML_DIRECTIVE` constant to `scripts/frontmatter.ts` matching a line that begins with
    a percent sign followed by at least one non-space character, and apply it in `parseFrontmatter`
    at exactly one place: after leading blank lines are skipped and BEFORE the opening-delimiter test
    that currently returns the no-keys success arm. When it matches, return the ok-false arm with a
    reason that names the directive line by excerpt and states why a directive-prefixed document is
    unreadable rather than keyless — the document declares a YAML processing context this module does
    not implement, so the value it expresses is not something this module may report on.

    Do not add a second application point and do not touch `flattenBlock`: a percent-prefixed line
    inside the block already fails the existing key-line rule with its own reason, and giving one
    input two reasons is the duplicate-grammar shape this module exists to delete.

    Amend the module header's three-outcome paragraph so the second bullet reads as "no block at all
    AND no directive prologue", and add one sentence recording the reviewer's `UNKNOWN - verify`
    verbatim in substance: most markdown frontmatter readers also require the delimiter on line 1, so
    the platform probably sees no frontmatter either and such a file is most likely inert rather than
    rogue — the refusal is taken because the module's own contract puts an undecodable prologue in
    the unreadable arm, not because a live bypass was reproduced. Do not overstate it as a bypass.

    Add cases to `scripts/frontmatter.test.ts` covering all five behaviors above, each annotated with
    which arm it pins.

    Rebuild the committed compiled output and capture the GREEN transcript against
    `scripts/frontmatter.js`.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; node scripts/freshness.js &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' scripts/frontmatter.test.ts &amp;&amp; node scripts/check-foundation-guards.js</automated>
  </verify>

  <acceptance_criteria>
    - A captured RED transcript shows the pre-fix committed `scripts/frontmatter.js` returning `{"ok":true,"value":false}` on the directive-prefixed document.
    - A captured GREEN transcript shows the post-fix committed `scripts/frontmatter.js` returning `ok:false` on the same document, with the reason text containing the directive line's excerpt.
    - A captured transcript shows a body-only document (no frontmatter block) still returning the ok-true / no-keys arm after the fix — the legitimately-keyless arm is intact.
    - `grep -c 'YAML_DIRECTIVE' scripts/frontmatter.ts` is at least 2 and `grep -c 'YAML_DIRECTIVE' scripts/frontmatter.js` is at least 2.
    - `grep -c 'UNKNOWN - verify' scripts/frontmatter.ts` is at least 1 — the caveat survives into the shipped header.
    - The case asserting a percent-prefixed line INSIDE the block still produces the existing key-line reason, not the directive reason, passes.
    - `npx vitest run --exclude '**/scripts/e2e/**'` reports zero failures.
    - `node scripts/freshness.js` exits 0.
  </acceptance_criteria>

  <done>
    A directive-prefixed document that plainly declares a spawn grant now lands in the unreadable arm
    naming the directive, the genuinely-keyless success arm is unchanged, and the `UNKNOWN - verify`
    caveat is recorded in the shipped header rather than converted into an overstated bypass claim.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the negative control that makes the agent-adapter scoping gate load-bearing (IN-01)</name>

  <read_first>
    - `scripts/check-foundation-guards.ts` around the spawn-grant scan loop — the scoping flag derived
      from the agent-adapter list and the three findings gated behind it (missing name key, absent
      tools key, empty tools value), plus the ungated cardinality arms that follow.
    - `scripts/check-foundation-guards.test.ts` lines 1-230 (the mirror harness) and the shipped
      agent-adapter absence case and empty-value case — the new control sits beside them and must use
      the same helpers.
    - The `reshapeToolsBlock` helper and its recorded reason for existing (skill adapters express
      their allow-list as a block sequence, so replacing one key line would leave items dangling).
  </read_first>

  <files>scripts/check-foundation-guards.test.ts</files>

  <behavior>
    - A mirrored SKILL adapter with its entire allow-list declaration REMOVED keeps the guard green —
      the scoping gate is doing exactly what it was widened to do.
    - The shipped agent-adapter-with-no-tools-key case still reds the guard by name — the gate is
      scoped, not disabled.
    - Deleting the scoping flag from the guard source makes the new skill control FAIL — proving the
      gate is load-bearing rather than decorative.
  </behavior>

  <action>
    Add a `plantSkillWithoutToolsKey` helper to `scripts/check-foundation-guards.test.ts` that, given
    a mirror root, removes the entire allow-list declaration (the key line and every block-sequence
    item belonging to it) from one mirrored skill adapter. Follow the existing helper discipline:
    THROW when the key is absent, because a helper that silently no-ops leaves the case asserting
    against an unmodified tree — that is the failure the shipped helpers' comments already record.

    Add the negative-control case: build a mirror, apply the helper to one skill, run the compiled
    guard with `CHECK_ROOT` pointed at the mirror, and assert exit 0 with `ALL CHECKS PASSED`. Name
    the case so it reads as the scoping control for the agent-adapter gate, and annotate it with the
    reason it exists — all seven committed skills declare an allow-list today, so without this case
    deleting the gate would change nothing on the live tree and the decision the plan called
    load-bearing would be an untested branch.

    Assert the paired direction in the same block or immediately beside it: the shipped agent-adapter
    absence case reds the guard and names the file. If that case already exists unchanged, reference
    it by name in the new case's comment rather than duplicating it — a second copy of an existing
    assertion is the duplicate-set shape this phase exists to delete.

    Capture the load-bearing proof: in a scratch copy of the guard source, remove the scoping flag so
    the absence and emptiness arms apply to every scanned file, rebuild into the scratch copy, and
    record that the new skill control FAILS there. Restore the scratch copy; the live source is not
    modified by this task.
  </action>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-foundation-guards.test.ts &amp;&amp; node scripts/check-foundation-guards.js</automated>
  </verify>

  <acceptance_criteria>
    - The new skill-without-allow-list case passes, asserting exit 0 and `ALL CHECKS PASSED` on the mirror.
    - The shipped agent-adapter-with-no-tools-key case still passes, asserting nonzero and naming the file.
    - A captured transcript shows the new skill control FAILING against a scratch guard build with the scoping flag removed — the gate is proven load-bearing.
    - `grep -c 'plantSkillWithoutToolsKey' scripts/check-foundation-guards.test.ts` is at least 2 (definition plus use), and the helper throws when the key is absent.
    - `git diff --stat` shows `scripts/check-foundation-guards.ts` was NOT modified by this task.
    - `npx vitest run --exclude '**/scripts/e2e/**'` reports zero failures.
    - `node scripts/check-foundation-guards.js` exits 0 on the live tree.
  </acceptance_criteria>

  <done>
    Whether a future skill omitting its allow-list reds the gate is now decided by a case rather than
    by an untested branch, and the gate is proven load-bearing by a captured failure against a build
    with it removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter/skill file content → guard verdict | A document may open with a legal YAML prologue the reader does not implement, and the reader's answer becomes a spawn-grant decision |
| guard scan set membership → which findings apply | The scoping flag decides which arms fire per file; an untested flag is an undecided policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-IN02-01 | Elevation of Privilege | `scripts/frontmatter.ts` `parseFrontmatter` directive arm | medium | mitigate | Refuse a leading directive line by name so a directive-prefixed grant cannot reach the legitimately-keyless success arm; the platform-inertness premise is carried as `UNKNOWN - verify` rather than asserted |
| T-27-IN01-01 | Repudiation | `scripts/check-foundation-guards.ts` agent-adapter scoping flag | low | mitigate | Negative control plus a captured failure against a gate-removed build, so the scoping decision is exercised rather than assumed |
| T-27-IN02-02 | Denial of Service | directive pattern applied once per document | low | accept | A single anchored line test on one already-read line; no backtracking and no additional file reads |
| T-27-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO package-manager install tasks in this plan; the dev-dependency fence is asserted by an unchanged `package.json`, so no legitimacy checkpoint is reachable |
</threat_model>

<verification>
- `npm run build && node scripts/freshness.js` exits 0 with every committed `.js` fresh.
- `npx vitest run --exclude '**/scripts/e2e/**'` reports zero failures and a total at or above the post-27-29 baseline.
- `node scripts/check-foundation-guards.js` exits 0 on the live tree.
- `git diff package.json` is empty.
- The RED-before / GREEN-after transcript for IN-02 and the gate-removed failure transcript for IN-01 are recorded in the plan summary.
</verification>

<success_criteria>
- The second silent-SUCCESS arm in the frontmatter reader is closed, and the genuinely-keyless arm still succeeds.
- The scoping gate is exercised in both directions and proven load-bearing.
- The reviewer's `UNKNOWN - verify` on the platform's directive handling survives into the shipped header, unmodified in substance.
- No requirement outside KIT-03 and SPAWN-04 has its surface touched.
</success_criteria>

<output>
Create `.planning/phases/27-spawn-correctness-kit-set-authority/27-30-SUMMARY.md` when done, including
the IN-02 RED-before / GREEN-after transcripts and the IN-01 gate-removed failure transcript.
</output>

---
phase: 27-spawn-correctness-kit-set-authority
plan: 18
type: execute
wave: 7
depends_on: []
files_modified:
  - scripts/frontmatter.ts
  - scripts/frontmatter.js
  - scripts/frontmatter.test.ts
  - scripts/check-foundation-guards.test.ts
autonomous: true
gap_closure: true
requirements: [SPAWN-04, KIT-03]
closes_findings: [CR-01]

must_haves:
  truths:
    - "hasSpawnGrant over the review's reproduced anchor/alias document returns the parse-failure arm with a reason naming the anchor or alias construct, and is never the value-false success arm."
    - "The compiled aggregator run against a hermetic mirror whose .claude/skills/grugops/SKILL.md carries the `_tools: &t ...` / `allowed-tools: *t` plant exits non-zero and names that file with a frontmatter parse failure."
    - "The refusal is decided in flattenBlock before the value is flattened, so it fires the same way whether the anchor sits on the key line, on a plain continuation line, inside a flow sequence, or on a block-sequence item."
    - "Bytes inside a `|` or `>` block scalar keep `&` and `*` as literal text and are deliberately NOT refused, because YAML gives those bytes no reference meaning there and the platform reads them literally too."
    - "The parser oracle carries a refused-form serializer table walked as its own product with an explicit size pin, so a refused serializer dropped from the table fails the size assertion instead of shrinking coverage silently."
    - "Every shipped adapter, skill and packaging template still parses: the real tree stays green under the new refusal."
    - "SPAWN-04 adjacency edge: a `&` or `*` that is not at a token start — R&D inside a description, a bare asterisk between words — does not trip the refusal; only a reference sigil immediately followed by a name character at the start of a value, a flow item or a sequence item does. Pinned in both directions."
    - "SPAWN-04 empty edge: an absent tools key, a tools key with an empty value, and a tools key whose whole value is an alias are three distinct results — no grant, no grant, and refusal — and no two of them print the same thing."
    - "SPAWN-04 ordering edge: keysGrantedAgentNames stays de-duplicated and sorted, and the position of the offending anchor within the block does not change the refusal verdict or its reason category."
  artifacts:
    - "scripts/frontmatter.ts declares a module-level YAML_REF pattern and returns the ok-false arm from flattenBlock when it matches."
    - "scripts/frontmatter.js rebuilt by tsc from the edited source, byte-fresh under npm run freshness."
    - "scripts/frontmatter.test.ts declares REFUSED_FORMS and asserts a second cartesian product over it with an explicit checked-count pin."
    - "scripts/check-foundation-guards.test.ts carries an aggregator-level case planting the anchor/alias grant on the skill adapter."
  key_links:
    - "flattenBlock's ok-false arm reaches guardWr05 through the hand-written parse-failure branch at check-foundation-guards.ts:500-508 and reaches the KIT-03 oracle through its parseFailures branch at :1454-1464. Both consumers already branch on ok:false, so this authority change needs no consumer edit — that is the whole point of having one authority."
    - "The aggregator imports the committed scripts/frontmatter.js, never the .ts, so the rebuild is what makes the fix reach the gate."
  prohibitions:
    - "A guard PASS line must never state a check that was not performed over the input it names."
    - "An existing RED case must never be deleted, skipped or weakened to make a new fix go green."
    - "A fixture must never be edited into a shape that can no longer express the failure it was written to pin."
---

<objective>
Close CR-01: a YAML anchor/alias spawn grant is currently read as a clean *no grant*, which is the
exact silent-bypass arm `scripts/frontmatter.ts` was written to delete — and the module header
already claims it refuses. The claim is false and the review reproduced it end-to-end on a skill
adapter, the one surface with no freshness gate and no role corpus to cross-check, with the whole
gate printing ALL CHECKS PASSED.

This is the TRACER plan for the round. `frontmatter.ts` is the identity-and-grant parsing authority;
CR-02's fix reads a parse from the same map and WR-03 replaces a second grammar with this module. The
authority's refusal behaviour must land and be proven end-to-end before any consumer that depends on
it is touched.

Purpose: make the module's stated contract true — an unresolvable reference construct is a parse
artifact, never a verdict.
Output: the refusal in `flattenBlock()`, a rebuilt committed `.js`, a refused-form product in the
parser oracle, and an aggregator-level RED case matching the review's reproduction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Refuse YAML anchors, aliases and merge keys in flattenBlock, and prove it through the aggregator on the skill adapter</name>
  <files>scripts/frontmatter.ts, scripts/frontmatter.js, scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS.md` § CR-01 — carries the verbatim reproduction, the `file:line` anchors and the suggested patch. Do not re-derive them.
    - `scripts/frontmatter.ts` — the whole file. Lines 49-53 are the false claim to be made true; `KEY_LINE` at :126; `flattenBlock()` at :198-270 is where the refusal belongs.
    - `scripts/check-foundation-guards.ts:485-540` — `guardWr05()`'s hand-written parse-failure branch, the consumer this refusal reaches with no edit.
    - `scripts/check-foundation-guards.test.ts:140-338` — `mirror()`, `adapterPath()`, `reshapeToolsKey()`, `runIn()`, `out()`. `reshapeToolsKey()` already exists for exactly this shape of plant.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md` — D-25 (the scan set is the derived adapters, 17 agents plus 7 skills, plus the packaging template, which is why the skill file is inside this guard at all) and the revision note stating that the correct action on the real `.claude/skills/grugops/SKILL.md` is NO CHANGE. The plant in this task lives only inside a hermetic `/tmp` mirror; the committed skill file is never edited.
  </read_first>
  <action>
Add a module-level `YAML_REF` regular expression to `scripts/frontmatter.ts` matching a reference
sigil at a token start — an `&` or `*` that begins a value, a flow item or a sequence item and is
immediately followed by a name character — and route a match to the `ok: false` arm of
`flattenBlock()`, replacing the header's false claim at :49-53 with what the code now does.

Apply the test at two points inside `flattenBlock()`, both BEFORE the value is flattened, so the
refusal cannot be bypassed by where the reference sits: (a) on `rest` in the baseline key-line branch
after `const rest = (kv[2] ?? "").trim();`, and (b) on the trimmed continuation text in the two
non-block continuation branches — the `SEQ_ITEM` branch and the plain-continuation `cur.parts.push`
branch.

Do NOT apply it inside the `cur.block` branch. Inside a `|` or `>` block scalar YAML gives those
bytes no reference meaning, so the platform reads them as literal text and so must this module;
refusing there would be a false red on correct text. State that boundary in the comment beside the
constant, next to the reason for refusing everywhere else: resolving a reference would be a second
grammar with more surface, and reading it as plain text is the silent-no-grant arm this module exists
to delete.

The failure `reason` must name the construct and the offending excerpt through the existing
`excerpt()` helper, in the same clear professional voice the surrounding reasons use.

A merge key line reaches `KEY_LINE` first and already fails there because `<` is not a key-start
character. Do not add a second path for it; record in the comment that the merge key is refused by
`KEY_LINE` and is pinned by a case in Task 2, so a later reader does not add a redundant branch.

Rebuild the committed twin with `npm run build` — the aggregator imports `scripts/frontmatter.js`,
so an unrebuilt `.js` means the fix does not exist for any gate.

Then add ONE aggregator-level case to `scripts/check-foundation-guards.test.ts`, beside the existing
folded-scalar skill case at :412, named for CR-01. Build a `mirror()`, call the existing
`reshapeToolsKey()` on the mirror's `.claude/skills/grugops/SKILL.md` with the two-line anchor/alias
shape the review reproduces (an underscore-prefixed key carrying the anchor and the real tool list
including a scoped spawn token, then `allowed-tools` carrying the alias), run `runIn(mirror)`, and
assert non-zero status plus output naming that skill path and the parse-failure wording. Assert the
same mirror WITHOUT the plant is green, so the plant is provably what turns it red.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run freshness &amp;&amp; npx vitest run scripts/check-foundation-guards.test.ts scripts/frontmatter.test.ts &amp;&amp; node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const YAML_REF" scripts/frontmatter.ts` reports 1.
    - RED-before: on the pre-change committed `.js`, a one-shot Node evaluation of `hasSpawnGrant` over the review's CR-01 document returns the `ok: true` / `value: false` success arm. GREEN-after: the same evaluation returns `ok: false` with a reason naming the anchor or alias. Record both outputs verbatim in the SUMMARY.
    - RED-before: `CHECK_ROOT=<mirror> node scripts/check-foundation-guards.js` over a mirror carrying the skill plant exits 0 and prints `ALL CHECKS PASSED`. GREEN-after: the same command exits 1 and its output contains `grugops/SKILL.md` and `frontmatter parse failure`.
    - `npx vitest run scripts/check-foundation-guards.test.ts` exits 0, and its case list contains a case naming CR-01 and the anchor/alias plant.
    - `node scripts/check-foundation-guards.js` over the real tree exits 0 — no shipped adapter, skill or packaging template trips the new refusal.
    - `npm run build` then `npm run freshness` exits 0, so the committed `scripts/frontmatter.js` is a faithful build of the edited source.
    - The header text at `scripts/frontmatter.ts:49-53` describes the implemented behaviour; no sentence in that file claims a refusal the code does not perform.
  </acceptance_criteria>
  <done>An anchor or alias in a grant position is a named parse failure at the module, at the aggregator, and in the committed `.js` the gate actually runs.</done>
</task>

<task type="auto">
  <name>Task 2: Give the parser oracle a refused-form product, so the fixture stops being blind to the construct it must reject</name>
  <files>scripts/frontmatter.test.ts</files>
  <read_first>
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS.md` § CR-01 — specifically the sentence naming why the oracle could not catch this: its serializer table has no anchor/alias form and the product only generates documents it already asserts `ok === true` for.
    - `scripts/frontmatter.test.ts` — the whole file. The header at :1-31 states the oracle doctrine; `VALUES` at :59, `FORMS` at :139, `INDENTS` at :221, the product assertion at :236-259 with its size pin, and the two named reproduced-bypass cases at :290-328.
    - `scripts/frontmatter.ts` — the `YAML_REF` constant and its two application points from Task 1.
  </read_first>
  <action>
Add a `REFUSED_FORMS` serializer table beside the existing `FORMS`, using the same `Serializer`
signature and the same `doc()` helper, holding at least four shapes: an alias in the value position
under a second key that carries the anchor; an anchor directly on the `tools` key's own value; an
alias as a flow-sequence item; and an alias as a block-sequence item on a continuation line.

Add a second product assertion that walks `REFUSED_FORMS` x `INDENTS` x `VALUES` and demands
`parseFrontmatter` return `ok === false` for every member, with a reason matching the anchor/alias
wording — and, as the load-bearing half, that `hasSpawnGrant` and `grantedAgentNames` over the same
document are NOT the value-false and empty-array success arms. Mirror the existing product's
discipline exactly: assert the checked count equals `REFUSED_FORMS.length * INDENTS.length *
VALUES.length` and pin that integer, and extend the table-size case at :226 to assert
`REFUSED_FORMS.length` and its label uniqueness the same way `FORMS` is asserted, so a refused
serializer dropped from the table fails the count instead of shrinking coverage silently.

Add three individually named cases, each quoting CR-01, because a product member is anonymous and a
named case is what a future reader greps for: the review's alias-grant skill document verbatim; a
merge-key document, asserting it lands in the failure arm and recording in the case comment that
`KEY_LINE` is what refuses it; and an anchor on the `tools` key itself.

Add one NEGATIVE-direction case pair that keeps the refusal narrow: a description value containing an
ampersand or asterisk adjacent to word characters still parses `ok: true` and yields the correct
grant verdict; and a `>` folded scalar whose content is literally an asterisk followed by a name
still parses `ok: true` and yields no grant, because YAML gives those bytes no reference meaning
inside a block scalar and the platform reads them literally.

Add one empty-edge case asserting the three adjacent states print differently: an absent `tools` key,
a `tools` key with an empty value, and a `tools` key whose entire value is an alias.
  </action>
  <verify>
    <automated>npx vitest run scripts/frontmatter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REFUSED_FORMS" scripts/frontmatter.test.ts` reports at least 4 occurrences (declaration, size pin, product walk, label-uniqueness assertion).
    - `npx vitest run scripts/frontmatter.test.ts` exits 0 and reports a case count strictly greater than the pre-change count; record both counts in the SUMMARY.
    - Deleting any single row from `REFUSED_FORMS` makes the size-pin assertion fail — demonstrate this once by a temporary local edit, record the failing assertion name in the SUMMARY, and restore the row.
    - The narrow-refusal negative cases pass: a document whose only sigil is adjacent to word characters parses `ok: true`, and a block-scalar document whose content begins with a sigil parses `ok: true` with no grant.
    - `npx vitest run --exclude '**/scripts/e2e/**'` exits 0 — no other suite regressed on the new refusal.
  </acceptance_criteria>
  <done>The oracle generates documents it asserts must be REFUSED, counts them, and would fail if a refused form were dropped from the table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped adapter/skill frontmatter -> the grant guard | file content authored or hand-edited outside the generator crosses into a safety verdict |
| the committed `.js` -> the CI gate | the gate runs the compiled twin, so an unrebuilt twin is a boundary where a fix silently does not exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-90 | Spoofing | `scripts/frontmatter.ts` `flattenBlock()` | high | mitigate | A reference construct in a grant position is refused by `YAML_REF` before flattening, so a grant expressed by reference can no longer present itself as a no-grant verdict. |
| T-27-91 | Elevation of Privilege | `.claude/skills/*/SKILL.md` | high | mitigate | The skill surface has no freshness gate and no role corpus to cross-check; the aggregator-level RED case pins the refusal on exactly that surface. |
| T-27-92 | Tampering | committed `scripts/frontmatter.js` | high | mitigate | `npm run build` plus `npm run freshness` in the acceptance criteria; the freshness gate already fails closed on drift. |
| T-27-93 | Repudiation | the parser oracle's coverage claim | medium | mitigate | The refused-form product carries an explicit cardinality pin, so a dropped serializer fails the count rather than shrinking the claim. |
| T-27-94 | Denial of Service | over-broad refusal on legitimate text | low | accept | A false red fails closed and a human decides; the narrow-refusal negative cases and the real-tree green run bound the risk. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run build` then `npm run freshness` exits 0.
- `npx vitest run --exclude '**/scripts/e2e/**'` exits 0.
- `node scripts/check-foundation-guards.js` exits 0 over the real tree.
- The CR-01 reproduction, run verbatim from the review against a hermetic `/tmp` mirror, exits 1 where it previously exited 0. Both transcripts recorded in the SUMMARY.
- `git status --porcelain` is clean of stray mirrors after the run.
</verification>

<success_criteria>
An anchor or alias grant is refused by name at the authority, at the aggregator and in the committed
`.js`; the oracle generates and counts the refused forms; the real tree is unchanged and green; the
module header states only what the code does.
</success_criteria>

## Artifacts this phase produces

New symbols created by this plan — exclude these from drift verification, they do not exist at HEAD:

- `YAML_REF` — module-level constant in `scripts/frontmatter.ts`.
- `REFUSED_FORMS` — serializer table constant in `scripts/frontmatter.test.ts`.
- New test cases in `scripts/frontmatter.test.ts`: the refused-form product walk, the refused-form
  table-size pin, the three named CR-01 cases (alias grant, merge key, anchor on the tools key), the
  narrow-refusal negative pair, and the empty-edge three-state case.
- One new test case in `scripts/check-foundation-guards.test.ts` planting the anchor/alias grant on
  `.claude/skills/grugops/SKILL.md`.
- No new files, no new npm scripts, no new CI steps, no new exit codes.

<output>
Create `.planning/phases/27-spawn-correctness-kit-set-authority/27-18-SUMMARY.md` when done.
</output>

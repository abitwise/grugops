---
phase: 27-spawn-correctness-kit-set-authority
plan: 65
type: execute
wave: 3
depends_on: [27-62, 27-63, 27-64]
gap_closure: true
autonomous: false
requirements: [KIT-03, SPAWN-04]
files_modified:
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - scripts/check-foundation-guards.test.ts
  - scripts/frontmatter.ts
  - scripts/frontmatter.js

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-64 Part A is CUT OVER: guard_wr05 (SPAWN-04) and the referential-integrity oracle (KIT-03) render their verdicts through the canonical admission reader. The silent-no-grant arm both guards inherited from the parser no longer exists on their path — there is only 'canonical, here are the values' or 'not canonical, FAIL BY NAME'."
    - "D-64 Part C is delivered: scripts/frontmatter.ts is DEMOTED from safety authority to convenience reader. It keeps its eleven non-test consumers and is not deleted, but it stops being the module that renders the spawn verdict, and a source assertion pins that the guard no longer imports its grant predicates."
    - "TWO-SIDED at the GATE (D-64 vacuity trap 1): node scripts/check-foundation-guards.js exits 0 on the unmodified live tree — a gate that reds the live kit is broken, not strict — while the historical corpus planted into a live allow-list key moves the same gate from exit 0 to exit 1 with the refusal text READ from its output."
    - "CR-01 and CR-02 are DISSOLVED at the gate, not patched. The two shapes the round-11 review reproduced end-to-end at ALL CHECKS PASSED now fail by name. scripts/frontmatter.ts's parsing logic is not widened by one byte in this round; the only edit to it is a header comment recording the demotion."
    - "No exemption is added. The exemption list length is stated before and after and is unchanged, per D-64 vacuity trap 3."
    - "The cutover is COSTLY reversibility per D-64: it changes the mechanism by which a safety verdict is rendered and would need its own round to undo, so it is gated behind a blocking decision checkpoint before any guard line changes."
    - "A green suite is a FLOOR and is not offered as evidence that any bypass family is closed. Closure evidence is the gate transcript and the exit codes."
  artifacts:
    - "scripts/check-foundation-guards.ts — guard_wr05 and the KIT-03 oracle reading the canonical admission reader, with every refusal surfaced as a named gate failure"
    - "scripts/check-foundation-guards.test.ts — the cutover pins, the demotion source assertion, and the end-to-end plant sweep over the historical corpus"
    - "scripts/frontmatter.ts — a header comment recording the demotion and naming the module that now renders the spawn verdict"
  key_links:
    - "check-foundation-guards.ts -> canonical-frontmatter.ts: the single import that moves the safety verdict. Every guard that previously called keysHaveSpawnGrant, keysGrantedAgentNames or parseFrontmatter for a VERDICT now calls the admission reader instead."
    - "a REFUSED admission -> a named gate FAILURE: a refusal must never be reported as 'carries no grant'. This is the module's founding failure mode and the reason eleven rounds shipped green; the parse-failure branch guard_wr05 already writes by hand is the precedent to follow and widen."
    - "check-foundation-guards.test.ts -> canonical-corpus.js: the gate-level plants come from plan 27-63's corpus, so the module-level replay and the gate-level replay cannot disagree about which shapes were tested."
---

<objective>
Cut the spawn verdict over to the canonical admission reader, and demote `scripts/frontmatter.ts`
from safety authority to convenience reader.

This is the plan where the parser stops being the authority. Everything before it built and proved
the replacement in isolation; this one moves the verdict and proves the move at the gate, in both
directions — the live kit still passes, and the historical corpus planted into a live allow-list key
now fails by name where it previously printed `ALL CHECKS PASSED` at exit 0.

Per D-64 the reader cutover is rated COSTLY: it changes the mechanism by which a safety verdict is
rendered and would need its own round to undo. It therefore sits behind a blocking decision
checkpoint, and no guard line changes before that checkpoint resolves.

Purpose: make the two open requirement rows' underlying defect class structurally impossible rather
than repaired. Output: a guard whose spawn verdict has two outcomes and no silent third.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-62-SUMMARY.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-63-SUMMARY.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-64-SUMMARY.md
@CLAUDE.md
</context>

<repo_execution_notes>
- Test command is `npx vitest run --exclude '**/scripts/e2e/**'`. NEVER bare `npm test` — it triggers
  a live claude-CLI e2e lane that spends tokens and can hang for roughly eight minutes.
- Typecheck is `npm run typecheck`, both lanes.
- Editing a `.ts` under `scripts/` REQUIRES `npm run build` and committing the `.js`. The gate that
  matters here is `node scripts/check-foundation-guards.js`, the COMMITTED build — a change to the
  `.ts` alone changes nothing the gate or the suite can see.
- The verification harness has produced a FALSE result sixteen times in this phase. Every plant below
  MUST: run the build and ASSERT THE BUILD'S EXIT CODE (a `tsc` failure emits nothing, leaves the old
  build in place, and the run reports a convincing baseline), PROBE THE BUILT ARTIFACT DIRECTLY (a
  mutation can compile and not bite), and SUBTRACT AN UNMUTATED SCRATCH-COPY BASELINE (copies made
  without `.git` produce git-dependent failures; one round reported fifteen reds of which six were
  attributable to that).
- `diff` is a shell function aliased to `git diff --no-index`; use `/usr/bin/diff`.
- Every plant asserts its own premise so a wrong path halts loudly instead of producing a plausible
  number.
- Never fabricate a gate result, a test result or a citation. Unknowns are `UNKNOWN - verify` with
  the reason.
- Editing `.planning/STATE.md` is known to trigger an escape-doubling defect that has previously made
  a guard's own regular expression run for minutes. If a state write is made, check the file's
  longest line afterwards.
</repo_execution_notes>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Cut the spawn-grant verdict over from scripts/frontmatter.ts to the canonical admission reader.</decision>
  <context>
    D-64 rates this COSTLY, not one-way. It changes the mechanism by which a safety verdict is
    rendered, and undoing it would need its own round. Everything reversible has already landed:
    plan 27-64's generator and byte-gate are revertible per file, and plans 27-62 and 27-63 added a
    module and a corpus that no gate consumes yet. This checkpoint is the last point at which the
    round can stop having changed nothing about how the guard decides.

    What the evidence says at this point:
    - 27-62 proved all 33 live scanned files ADMIT, and that the admitted grammar had to be the
      MEASURED one — D-64's premise measurement scanned 31 files rather than the guard's actual 33
      and never scanned for quote bytes, so its stated eight-key plain-only alphabet would have
      refused the entire live kit and violated its own vacuity trap.
    - 27-63 proved every reproduced bypass shape from rounds 1 through 11 now lands on a named loud
      refusal, with the refusal text in the transcript, and proved the replay able to fail.
    - 27-64 proved the seven skill twins are exactly derivable, generated them, and byte-gated them.

    What is NOT yet proven, and is what task 2 and task 3 exist to establish: that the guard behaves
    the same way the module does when it reads real files through its own scan, and that the live
    tree still passes.
  </context>
  <options>
    <option id="cut-over-now">
      <name>Cut over in this plan, behind this checkpoint</name>
      <pros>Delivers D-64 Parts A and C. Removes the silent-no-grant arm from the guard's path by construction rather than by a twelfth repair. The two shapes reproduced end-to-end at exit 0 in round 11 start failing by name.</pros>
      <cons>Changes a safety verdict's mechanism in the same round the replacement was written. If the replacement has its own blind spot, the round has moved the defect rather than removed it — which is the risk every round of this phase has run.</cons>
    </option>
    <option id="dual-read-first">
      <name>Run both readers and fail on disagreement, cut over next round</name>
      <pros>No verdict changes hands this round; any disagreement between the two mechanisms surfaces as a gate failure with both answers printed.</pros>
      <cons>Keeps two grammars deciding one question, which is the exact arrangement the P22-round-8 terminal lesson says to delete, and which this phase has now declined eleven times. Also leaves the parser on the verdict path, so a parser defect still renders a verdict.</cons>
    </option>
    <option id="hold">
      <name>Hold the cutover, ship 27-62 through 27-64 only</name>
      <pros>The generator and byte-gate land, which alone closes the surface every reproduced bypass used. Nothing about the verdict mechanism changes.</pros>
      <cons>CR-01 and CR-02 stay live at the gate. KIT-03 and SPAWN-04 keep failing for the reason they already fail for, and the structural pivot is deferred with its evidence already paid for.</cons>
    </option>
  </options>
  <resume-signal>Select: cut-over-now, dual-read-first, or hold</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: move the verdict to the canonical reader, and demote the parser</name>
  <files>scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js, scripts/check-foundation-guards.test.ts, scripts/frontmatter.ts, scripts/frontmatter.js</files>
  <reversibility rating="costly">Changes the mechanism by which the spawn-grant safety verdict is rendered; undoing it would require restoring the parser to the verdict path and re-proving every family it had closed, which is a round of its own.</reversibility>
  <read_first>
    scripts/check-foundation-guards.ts lines 256-276 (the frontmatter import block being replaced);
    scripts/check-foundation-guards.ts lines 670-1010 (guardWr05 in full — the scan loop, the
    hand-written parse-failure branch at the head of it, the tools-key floor, the rogue-grant loop and
    the coordinator arms);
    scripts/check-foundation-guards.ts lines 2290-2400 (the KIT-03 referential-integrity oracle and
    its coordinator-marker and granted-names reads);
    scripts/check-foundation-guards.ts lines 1677-1750 (guardDistributionPair, which also calls
    parseFrontmatter — decide and record its disposition);
    scripts/canonical-frontmatter.ts (the admission entry point, the refusal-code set, and the two
    admitted-value helpers);
    scripts/frontmatter.ts lines 1-60 (the module header receiving the demotion note).
  </read_first>
  <behavior>
    - Every place in `scripts/check-foundation-guards.ts` that reads frontmatter to render a VERDICT
      calls the canonical admission reader. The three call sites are guard_wr05's scan loop, the
      KIT-03 oracle's adapter read, and guard_distribution_pair's two reads.
    - A REFUSED admission is a NAMED GATE FAILURE carrying the refusal code and the refusal reason.
      It is never reported as "carries no grant" and never falls through to a passing arm. This
      widens the hand-written parse-failure branch guard_wr05 already carries, which is the precedent
      and already has the right wording.
    - `node scripts/check-foundation-guards.js` exits 0 on the unmodified live tree, and its
      guard_wr05 and kit-count PASS lines report what was checked including the scan composition.
    - A source assertion reads `scripts/check-foundation-guards.ts` and asserts it imports no grant
      predicate from `./frontmatter.js`. The assertion is bounded by a section marker asserted present
      before the slice is used, runs over comment-stripped source, and states a non-vacuity floor on
      what it scanned.
    - `scripts/frontmatter.ts` keeps every one of its eleven non-test consumers. A source assertion
      derives that consumer list at run time and asserts it is non-empty and unchanged in size, so the
      demotion is proven to be a demotion and not a deletion.
  </behavior>
  <action>
    Replace the verdict reads. Import the admission entry point and its helpers from
    `./canonical-frontmatter.js`. Remove `keysHaveSpawnGrant`, `keysGrantedAgentNames` and
    `parseFrontmatter` from the `./frontmatter.js` import list in this file. Keep `stripFencedBlocks`
    if the prose checks still need it and keep `TOOLS_KEYS` only if the canonical module does not
    already export the grant-key pair; prefer importing the pair from the canonical module so the
    tools-key floor and the grant test are scoped by the same list, which is the property the existing
    comment at that import already argues for.

    In `guardWr05`, replace the parse with an admission. On the REFUSED arm, append a gate failure
    naming the file, the refusal code and the refusal reason, and continue to the next file so every
    offending file is reported rather than only the first. Preserve the existing hand-written wording
    that an unreadable adapter is never read as carrying no grant; it is the correct sentence and it
    now covers the whole refusal vocabulary instead of one parse failure. Keep every existing arm's
    scope exactly as it is — the agent-adapter-only arms stay agent-adapter-only, the packaging
    templates keep their exclusion — because this task moves the READER and must not move the SCOPE.

    In the KIT-03 oracle, replace the adapter read the same way and take the coordinator marker and
    the granted names from the admitted value. A refusal here must fail the oracle by name; a name
    enumeration that could not read one of its inputs must never be compared for set equality.

    For `guardDistributionPair`, decide and record. It reads frontmatter to learn how many `name`
    values each side declares. That is a verdict, so move it to the admission reader as well and write
    the reason at the site. Note in a source comment that plan 27-64 made the twins generated and
    byte-gated, so this guard now asks a question the byte-gate answers more strongly, and that it is
    deliberately KEPT rather than removed in this round — this round removes nothing.

    Demote the parser. Add a header note to `scripts/frontmatter.ts` recording that it is no longer
    the safety authority for the spawn verdict, naming `scripts/canonical-frontmatter.ts` as the
    module that renders it, naming D-64 as the decision, and stating that this module remains a
    legitimate convenience reader for its non-test consumers. This is a COMMENT-ONLY edit. Do not
    change one byte of its parsing logic: a twelfth widening of the YAML parser is explicitly rejected
    by D-64, because rounds 10 and 11 each shipped a regression inside their own fix. Rebuild and
    report whether the committed `scripts/frontmatter.js` changed at all; a comment-only edit should
    leave it byte-identical, and if it does not, report the difference rather than explaining it away.

    Add the cutover pins to `scripts/check-foundation-guards.test.ts` per the behavior list, including
    the bounded comment-stripped source assertions for the removed imports and for the surviving
    consumer list, each with its non-vacuity floor. Follow `27-59`'s call-site-list idiom and
    `27-60`'s bounded-slice idiom; both are already in this tree and both exist because an unbounded
    or comment-bearing negative assertion produced a false result here before.

    Run the build, assert its exit code, run the gate on the unmodified tree, and record its full
    output and timing.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-foundation-guards.js &amp;&amp; npx vitest run scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**' --reporter=verbose &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    The guard renders its spawn verdict through the canonical admission reader at all three verdict
    call sites, and a refusal is a named gate failure carrying its code and reason.
    `node scripts/check-foundation-guards.js` exits 0 on the unmodified live tree with its PASS lines
    reporting the scan composition. The bounded source assertions confirm the guard imports no grant
    predicate from the parser and that the parser's non-test consumer list is non-empty and unchanged
    in size. `scripts/frontmatter.ts` carries the demotion note and no logic change, with the effect
    on its committed `.js` reported either way. `npm run typecheck` exits 0 on both lanes.
  </done>
</task>

<task type="auto">
  <name>Task 3: the end-to-end gate sweep — the historical corpus planted on live twins, both directions</name>
  <files>scripts/check-foundation-guards.test.ts</files>
  <read_first>
    scripts/check-foundation-guards.test.ts (from task 2);
    scripts/canonical-corpus.ts (plan 27-63's corpus, the source of the planted shapes);
    .planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW.md lines 146-162 and 264-281
    (the two gate reproductions verbatim: the shapes planted into the existing allowed-tools key of
    both twins of the non-coordinator map skill, and the recorded exit codes);
    .planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md lines 2920-2951 (the
    hermetic-mirror gate methodology, exit code before and after);
    scripts/adapters-freshness.test.ts (the mirror-construction idiom).
  </read_first>
  <action>
    Build the end-to-end sweep on hermetic mirrors created from a clean archive of the working tree,
    never on the committed tree.

    Record the premise controls FIRST, in this order, and assert each:
    - Mirror with nothing planted: `node scripts/check-foundation-guards.js` exits 0. This is the
      two-sided cardinality claim AT THE GATE — the live kit passes, so the gate is strict rather
      than broken.
    - Mirror with a comment-only change planted: exits 0. This proves the sweep measures the PLANT and
      not the mirror construction. This phase's record shows a sweep without such a control producing
      reds that were not attributable.

    Then sweep the corpus. For each row selected for gate-level planting, plant its document text into
    the EXISTING `allowed-tools` key of BOTH distribution forms of a non-coordinator skill — the
    source `skills/map/SKILL.md` and its twin — exactly as the round-11 review's own reproductions
    did, so `guard_distribution_pair` stays green and the resulting red is attributable to the spawn
    guard alone. Assert the plant landed by re-reading the mutated files and confirming the planted
    bytes are present before running the gate; a plant that did not land produces a convincing green.

    For every planted row capture and assert: the exit code before the plant, the exit code after, the
    full gate output, and the presence of the refusal reason text in that output. Assert the failure
    output ends with a single-check failure line so the spawn guard alone is responsible, following
    the attribution discipline `27-43`'s reproductions established. Print the refusal TEXT per row.
    An exit code alone is not the claim.

    Select rows to plant with a stated rule rather than by taste, and state the rule and the resulting
    count in the summary. At minimum the set must include CR-01 row A, CR-01 row B, CR-02's row and
    CR-02's dash-less control, because those are the two findings `27-VERIFICATION.md` names and the
    two D-64 dissolves. Where a corpus row cannot be planted into an `allowed-tools` key without
    changing the shape the row is testing, record that row as module-level-only with the reason, and
    state the module-level versus gate-level split as a two-sided count: rows planted plus rows
    module-only equals the corpus total. A row that appears in neither bucket is a silent drop.

    Note in the summary the interaction plan 27-64 created: with the twins generated, a plant on the
    twin alone is now ALSO caught by `freshness:skill-twins`. That is a genuine strengthening and it
    is worth recording, but this sweep must run `check-foundation-guards.js` in isolation so the claim
    "the spawn guard now refuses this" is attributable to the spawn guard.

    Finally, restore and prove restoration: assert the working tree is clean after the sweep and that
    `node scripts/check-foundation-guards.js` on the committed tree exits 0. The sweep leaves no
    residue.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run scripts/check-foundation-guards.test.ts --exclude '**/scripts/e2e/**' --reporter=verbose &amp;&amp; node scripts/check-foundation-guards.js &amp;&amp; git status --porcelain &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**'</automated>
  </verify>
  <done>
    Both premise controls are recorded first and both exit 0. Every planted corpus row moves the gate
    from exit 0 to exit 1, with the refusal TEXT read from the gate's own output and a single-check
    failure line proving the spawn guard alone is responsible. CR-01 rows A and B and CR-02's row and
    control are all in the planted set, inverting the two reproductions `27-REVIEW.md` recorded at
    `ALL CHECKS PASSED`, exit 0. The planted-versus-module-only split reconciles two-sided against the
    corpus total. The working tree is clean afterwards and the gate exits 0 on the committed tree.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed markdown -> the spawn verdict | The boundary eleven rounds of bypasses crossed, and the boundary this plan moves to a new mechanism. |
| the old reader -> the new reader | During the cutover both modules exist. A verdict call site left pointing at the old one is a live bypass wearing a fresh coat. |
| gate output -> a human reading a closure claim | An exit code without a refusal reason lets "it failed" pass for "it failed for the right reason". |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-153 | Elevation of Privilege | a verdict call site left on the old reader | critical | mitigate | Bounded, comment-stripped source assertion that the guard imports no grant predicate from the parser, with a non-vacuity floor; plus the corpus sweep, which would red at the gate if any verdict site still silently no-granted. |
| T-27-154 | Elevation of Privilege | a REFUSED admission reported as "carries no grant" | critical | mitigate | Every refusal arm appends a named gate failure carrying code and reason; the sweep asserts the reason text appears in the gate's output for every planted row. |
| T-27-155 | Repudiation | a gate that reds everything, passing the sweep vacuously | critical | mitigate | Two premise controls recorded first — unplanted mirror exits 0, comment-only plant exits 0 — plus the live-tree exit 0 in task 2. |
| T-27-156 | Tampering | scope drift smuggled in with the reader change | high | mitigate | The task fixes the scope of every existing guard arm explicitly: this task moves the READER, never the SCOPE, and the kit-count composition line is compared before and after. |
| T-27-157 | Denial of Service | a new reader with pathological runtime on a large region | medium | mitigate | The gate's wall-clock runtime is recorded before and after and reported in the summary; a prior incident in this repo took a 0.47 second guard to 383 seconds through superlinear backtracking. |
| T-27-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs NO package; the package manifest must be byte-unchanged. Assert it in the summary. |
</threat_model>

<verification>
- `npm run build` exits 0 and every edited `.ts` has its committed `.js` regenerated.
- `node scripts/check-foundation-guards.js` exits 0 on the unmodified tree, with its wall-clock
  runtime recorded and compared against the pre-cutover figure.
- `npx vitest run --exclude '**/scripts/e2e/**'` is no worse than the recorded baseline, and is
  recorded as a FLOOR, never as closure evidence.
- `npm run typecheck` exits 0 on both lanes.
- `npm run freshness`, `npm run freshness:adapters` and `npm run freshness:skill-twins` all exit 0.
- `scripts/frontmatter.ts` is changed by a header comment ONLY. Its parsing logic is byte-unchanged;
  state the restricted diff in the summary.
- `DISTRIBUTION_PAIR_EXEMPT` member count is stated before and after and is unchanged at one.
- The package manifest is byte-unchanged; no dependency is added.
</verification>

<success_criteria>
- The spawn verdict is rendered by the canonical admission reader at every verdict call site, per
  D-64 Part A, with refusals surfaced as named gate failures.
- `scripts/frontmatter.ts` is demoted per D-64 Part C: still consumed, no longer the safety authority,
  and pinned in both directions by source assertion.
- Both directions hold at the gate: the live kit passes at exit 0, and the historical corpus planted
  on live twins fails by name at exit 1 with the refusal text read from the gate's output.
- CR-01 and CR-02, the two findings `27-VERIFICATION.md` names, are dissolved at the gate without one
  byte of parser logic changing.
</success_criteria>

<output>
Create `.planning/phases/27-spawn-correctness-kit-set-authority/27-65-SUMMARY.md` when done.

The summary MUST carry:
- the decision-checkpoint outcome and who resolved it;
- the live-tree gate transcript at exit 0, with its PASS lines and the scan composition;
- the two premise controls, recorded first, both at exit 0;
- the per-row plant sweep with exit code before and after and the refusal TEXT for every planted row;
- the planted-versus-module-only two-sided reconciliation against the corpus total;
- the gate's wall-clock runtime before and after the cutover;
- the restricted diff proving `scripts/frontmatter.ts` changed by comment only, and whether its
  committed `.js` moved;
- `DISTRIBUTION_PAIR_EXEMPT` count before and after, both one;
- an explicit statement that the green suite is a FLOOR;
- an explicit statement that NO requirement row was promoted by this plan.
</output>

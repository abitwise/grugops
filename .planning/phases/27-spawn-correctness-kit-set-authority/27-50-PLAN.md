---
phase: 27-spawn-correctness-kit-set-authority
plan: 50
type: execute
wave: 4
depends_on: ["27-49"]
files_modified:
  - scripts/frontmatter.ts
  - scripts/frontmatter.js
  - scripts/frontmatter.test.ts
  - scripts/kit-model.ts
  - scripts/kit-model.js
  - scripts/kit-model.test.ts
  - scripts/generate-role-adapters.test.ts
  - .planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md
autonomous: true
gap_closure: true
gap_round: 9
requirements: [KIT-03, SPAWN-02, SPAWN-04]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-56 item 4 (WR-05), the leading-residue refusal names the FIRST CODE POINT OF THE LINE rather than the code point that made the run residue, so a line whose first character is a legal space is reported as the offender. The leading-run scan already VISITS the offending code point when it clears its all-declared flag and then discards it; the residue arm carries it out as `firstOutsideDeclared` and the refusal interpolates that. The whole point of this refusal's wording is to name the offending byte so a reader is sent to the right character."
    - "Per D-56 item 5 (IN-01), `scripts/kit-model.ts`'s markdown-extension constant is referenced at exactly one site while the literal is spelled at two others, in the file whose thesis is that a fact has one statement. The constant is used at every site, and the count of the literal outside the constant's own declaration is DERIVED at test time and asserted, so the anti-drift device cannot drift from itself again silently."
    - "Per D-56 item 6 (IN-02), after `27-46` the double-claim arm filters over the schema keys only, so a FOREIGN key claimed by two buckets is now unreportable rather than reported once — and a shipped case pins that blind spot as expected behaviour. The arm filters over the union of the schema keys and the claimed keys, so a double-claimed foreign key lands in both arms once each, which is what a human reading the guard's failure message needs in order to fix both buckets."
    - "Per D-56 item 7 (IN-03), the purity case slices the module source with a newline-brace search and never asserts the slice is the function, so a reformat can shrink it to a few lines and every forbidden-substring assertion then passes vacuously. The slice's PREMISE is asserted before it is inspected: the body contains the two expressions that identify it and exceeds a stated line count."
    - "Per D-56 item 8 (IN-04), the round-8 role-adapter fixture strips fence DELIMITERS and leaves fenced CONTENT live in the body, guarded only by 'at least one line was removed'. Fenced blocks are removed WITH their contents, and the written file is asserted to carry no delimiter line after line 0 before it is written — so the case's premise is checked rather than assumed, and the sibling case that depends on the same fixture in the opposite direction cannot drift apart from it."
    - "Per D-56 item 9, the `27-43` acceptance criterion — the structure validator going from exit 0 to a named non-zero failure on the non-coordinator adapter surface — is RETIRED by user decision, and the retirement is RECORDED WITH ITS REASON in a durable, greppable artifact rather than only in a summary. The validator has zero occurrences of the spawn, frontmatter and WR-05 vocabulary (measured at execution time, not assumed) and is a structure validator, never a spawn-grant surface; adding a second spawn-grant predicate there would create exactly the weaker-duplicate shape this phase has spent nine rounds deleting."
    - "Per D-56 item 10, SPAWN-03's live-platform capture stays DEFERRED to Phase 33 (GAP-D1). It is `UNKNOWN - verify`, it is not closable by any static gate, and the deferral is a recorded decision with its reason in the same durable artifact — never a silent drop and never a fabricated static proof."
    - "Every behaviour change in this plan is proven by a RED-before and a GREEN-after transcript against the COMMITTED compiled build, and the two refusal-wording changes are additionally proven not to have moved any OTHER refusal's text: the guard's own PASS and FAIL lines are compared before and after."
    - "Adjacency edge (KIT-03): a delimiter line whose leading residue's FIRST character is legal whitespace and whose SECOND character is the offender reports the second, not the first — the exact shape that made the old wording wrong, pinned as its own case."
    - "Boundary edge (KIT-03): a line whose leading run is entirely legal whitespace is INDENTATION and carries no residue fault at all, so the new field is absent there rather than defaulted to a misleading code point; the type makes that unrepresentable rather than merely untested."
    - "Precision edge (KIT-02): a key claimed by two buckets AND present in the schema still appears exactly once in the double-claim arm and not at all in the foreign arm, so widening the double-claim domain does not double-report the case it already handled."
    - "Empty edge (KIT-02): the markdown-literal count assertion is two-sided, so it fails both when a new literal appears and when the constant's own declaration is renamed away."
    - "Ordering edge (SPAWN-02): the fixture's fence removal is deterministic — the same input file produces the same written bytes on two runs — so the sibling cases cannot drift apart through an order-dependent strip."
    - statement: "The retirement and deferral records survive a milestone archive move because they live in the phase's own deferred-items artifact rather than only in a plan summary (KIT-03 durability edge; the archive behaviour is argued from the repository's existing milestone layout, not measured in this round)."
      verification: backstop
  artifacts:
    - "scripts/frontmatter.ts — the leading-run residue arm carrying `firstOutsideDeclared`, set where the all-declared flag is cleared and interpolated by the refusal that previously named the line's first code point"
    - "scripts/frontmatter.js — the rebuilt committed compiled twin, freshness-clean"
    - "scripts/frontmatter.test.ts — the residue-naming cases including the legal-first-character shape, and the purity case's premise assertions"
    - "scripts/kit-model.ts — the markdown-extension constant used at every site, and the double-claim arm widened to the union of schema and claimed keys with its comment corrected"
    - "scripts/kit-model.js — the rebuilt committed compiled twin, freshness-clean"
    - "scripts/kit-model.test.ts — the derived markdown-literal count assertion, and the double-claimed-foreign case re-pinned to report the key rather than pinning the blind spot"
    - "scripts/generate-role-adapters.test.ts — the fixture removing fenced blocks with their contents and asserting the written file's premise before writing"
    - ".planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md — the recorded retirement of the 27-43 acceptance criterion and the recorded deferral of SPAWN-03 to Phase 33, each with its reason"
  key_links:
    - "scripts/frontmatter.ts leadingInvisibleRun -> classifyDelimiter's refusal reason (one scan visits the offending code point; the refusal must name what that scan saw, not what the line starts with)"
    - "scripts/kit-model.ts partitionPluginComponentClaims -> guardKitCounts' failure message (the arm's consumer is a human fixing two buckets, so a double-claimed foreign key must be visible in both arms)"
    - "scripts/generate-role-adapters.test.ts fixture -> the unterminated-region refusal and its fence-refusal sibling (two cases depending on one fixture in opposite directions; the fixture's premise must be asserted or they drift apart while green)"
    - "the deferred-items artifact -> the phase's verification record (the two human-verification items in 27-VERIFICATION.md are answered here by a recorded decision, not by a code change)"
  prohibitions:
    - statement: "NO SECOND OR WEAKER SPAWN-GRANT PREDICATE is added to `scripts/validate-agent-factory.ts` or anywhere else. The retired criterion is retired by RECORDING the decision and its reason; forcing it mechanically would create the weaker-duplicate shape this phase has spent nine rounds deleting."
      status: mandatory
      verification: "`scripts/validate-agent-factory.ts` is untouched by this plan — absent from `files_modified` — and the summary records the measured occurrence counts of the spawn, frontmatter and WR-05 vocabulary in it."
    - statement: "A DEFERRAL IS NEVER A SILENT DROP. SPAWN-03's live-platform capture stays `UNKNOWN - verify` and no static gate is invented to stand in for it; the deferral is recorded with its reason and its owning phase."
      status: mandatory
      verification: "The deferred-items artifact carries both records, and the summary quotes them in full."
    - statement: "A REFUSAL NAMES THE OFFENDING BYTE, NOT A LEGAL ONE. A diagnosis that sends a reader to a character that is inside the declared class is a defect in the refusal, because the whole purpose of this wording is to point at what to fix."
      status: mandatory
      verification: "The legal-first-character case asserts the reported code point is the offender and not the line's first character, quoted before and after."
    - statement: "AN ASSERTION THAT CAN PASS VACUOUSLY IS A DEFECT. A source slice is asserted to be the thing it claims to inspect before any negative assertion runs over it."
      status: mandatory
      verification: "The purity case's premise assertions are quoted, and a constructed short slice is shown to fail them."
    - statement: "A FIXTURE'S PREMISE IS CHECKED, NEVER ASSUMED. A fixture guarded only by 'something changed' can silently start exercising a different code path while staying green."
      status: mandatory
      verification: "The fixture asserts positively that the written file carries no delimiter line after line 0, and the summary states which of the two sibling cases each fixture now provably serves."
    - statement: "NO HAND-MAINTAINED SET LITERAL is introduced, and every count this plan asserts is derived from the thing it counts and stated two-sided."
      status: mandatory
      verification: "The markdown-literal count and the fence-removal counts are each derived at run time and reported with the expression that derived them."
    - statement: "A GREEN SUITE is NEVER offered as evidence. Every change carries a RED-before and GREEN-after transcript against the committed compiled build, and the behaviour-preserving claims carry a before-and-after comparison of the guard's own output."
      status: mandatory
      verification: "Every acceptance criterion names a transcript, a derived count, or an output comparison; the suite result is stated as a floor."
    - statement: "NO NEW DEV DEPENDENCY. The dev-dep line stays `{typescript, vitest}` plus the type-only `@types/node`."
      status: mandatory
      verification: "`git diff --stat -- package.json package-lock.json` is empty at the end of the plan."
    - statement: "Prototype pollution and path traversal are CANON — covered by /gsd-secure-phase and eslint security plugins; not minted as prohibitions here."
      status: canon
      verification: "Referral only; no assertion is authored for them in this plan."
---

<objective>
Close the last five round-8 findings — **WR-05**, **IN-01**, **IN-02**, **IN-03** and **IN-04** — and
record the two dispositions the round-8 verification carried forward as open human decisions: the
retirement of the `27-43` acceptance criterion, and SPAWN-03's continued deferral to Phase 33.

Purpose: per **D-56**, all eleven round-8 findings close in this round, none deferred — the fifth
application of the posture D-41, D-47, D-50 and D-53 each established. Every round of this phase that
deferred a warning saw it return, usually one abstraction level down. Four of the five findings here are
small; each one is an instance of a class this phase has already paid for twice. A refusal that names a
legal character. A named constant referenced once while its literal is spelled twice, in the file whose
thesis is that a fact has one statement. A blind spot pinned as expected behaviour by the very case
added to close its sibling. A source slice whose premise is never checked. A fixture guarded by
"something changed".

Per **D-56** items 9 and 10, the two remaining items are DECISIONS rather than defects, and both are
already made. They are recorded here in a durable, greppable artifact — with their reasons — so a later
reader finds a decision rather than an unexplained absence.

Output: one refusal that names the right byte, one constant used everywhere it applies, one arm that can
report what a human needs, two premises checked instead of assumed, and two recorded decisions.

## Ownership, so no finding falls between plans

| Finding | Owner | Disposition |
|---|---|---|
| CR-01 | `27-47` | FIX |
| CR-02 | `27-48` | FIX |
| WR-03 | `27-48` | FIX |
| WR-01, WR-02, WR-04 | `27-49` | FIX |
| WR-05 | this plan | FIX |
| IN-01, IN-02, IN-03, IN-04 | this plan | FIX |
| the `27-43` acceptance criterion | this plan | RETIRED, recorded with its reason |
| SPAWN-03 live capture | this plan | DEFERRED to Phase 33 (GAP-D1), recorded with its reason |

Eleven findings, four plans, one owner each.

## The five defects, stated once

| # | Where | What is wrong |
|---|---|---|
| WR-05 | `scripts/frontmatter.ts:1679-1683` | The leading-residue fault interpolates the line's first code point. For a line beginning with a legal space followed by a zero-width space, it reports the space — a character inside the declared class and not the reason the line refused. The scan that found the offender already visited it and discarded it. |
| IN-01 | `scripts/kit-model.ts:185-188`, contradicted at `:567` and `:728` | The markdown-extension constant's comment claims two rules turn on it; it is referenced at exactly one site, and the literal is spelled at two others. |
| IN-02 | `scripts/kit-model.ts:474-503` | The double-claim arm filters over the schema keys, so it can only ever name a SCHEMA key claimed twice. Before `27-46` the foreign arm's duplication was the only visible trace of a non-schema key claimed by two buckets; the de-duplication removed it and nothing replaced it. `scripts/kit-model.test.ts` now pins the blind spot as expected behaviour. |
| IN-03 | `scripts/frontmatter.test.ts:7299-7318` | The forbidden-substring checks run over a slice bounded by a newline-brace search, which is the function body today only because no closing brace inside it sits at column 0. A reformat shrinks the slice and every negative assertion passes vacuously. |
| IN-04 | `scripts/generate-role-adapters.test.ts:489-491` | The fixture removes fence DELIMITER lines and leaves everything that was inside the fences live in a body whose frontmatter region now runs to EOF. The only guard is that at least one line was removed. The sibling case depends on the same fixture in the opposite direction, so the two can drift apart without either failing. |

## Why the two decisions are recorded rather than implemented

The `27-43` criterion asked the structure validator to go non-zero on a non-coordinator adapter carrying
a spawn grant. The validator has no spawn-grant vocabulary in it at all — that is a measurement the
executor re-takes, not a claim this plan asserts. Satisfying the criterion means adding a spawn-grant
predicate to a second file, which is the weaker-duplicate shape this module has deleted four times. The
user's decision is to retire the criterion. A retirement that lives only in a summary is indistinguishable
from an item that was forgotten, so it is written into the phase's deferred-items artifact with its
reason.

SPAWN-03's live-platform capture is `UNKNOWN - verify` against a real Claude Code session. No static gate
can produce it, and inventing one would be fabrication — which this repository's constraints forbid by
name. It stays owned by Phase 33 under GAP-D1, recorded in the same artifact.

## Decisions this plan implements

- **D-56** — items 4 through 10, each with its disposition recorded above and none carried into a
  round 10.

## Artifacts this phase produces (new symbols — exclude from drift verification)

| Symbol | File | Kind |
|---|---|---|
| `firstOutsideDeclared` | `scripts/frontmatter.ts` | new field on the leading-run result's residue arm: the first code point outside the declared whitespace class, carried out of the scan that already visits it |
| the legal-first-character residue case | `scripts/frontmatter.test.ts` | new case pinning that a line whose first character is legal reports the offender and not the first character |
| the purity case's premise assertions | `scripts/frontmatter.test.ts` | new positive assertions that the inspected slice is the function, run before any negative assertion |
| the derived markdown-literal count | `scripts/kit-model.test.ts` | new two-sided assertion over the literal's occurrence count outside the constant's own declaration |
| the double-claimed-foreign case | `scripts/kit-model.test.ts` | the round-8 case re-pinned to report the key in both arms once each, replacing the pin of the blind spot |
| `stripFencedBlockLines` (or the fixture-local equivalent) | `scripts/generate-role-adapters.test.ts` | the fixture helper removing fenced blocks WITH their contents, with its removal count reported |
| the fixture premise assertion | `scripts/generate-role-adapters.test.ts` | a positive assertion that the written file carries no delimiter line after line 0 |
| the retirement record | `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` | a recorded decision: the `27-43` acceptance criterion retired, with its reason and its measurement |
| the SPAWN-03 deferral record | `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` | a recorded decision: the live capture deferred to Phase 33 under GAP-D1, with its reason |
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS-8.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The residue refusal names the offending code point, and the purity slice's premise is checked — WR-05 and IN-03 (D-56 items 4 and 7)</name>

  <precondition>Plans `27-47`, `27-48` and `27-49` are landed, `git status --porcelain` is empty, and `npm run build &amp;&amp; npm run freshness` exits 0.</precondition>

  <precondition>A scratch directory outside the working tree is available for a `git archive HEAD` mirror, so the refusal wording's RED transcript is taken against the committed build.</precondition>

  <reversibility rating="reversible">One field on a result type and two sets of assertions; reverting is removing them.</reversibility>

  <read_first>
    - `scripts/frontmatter.ts:1530-1592` — the leading-run scan, its result type, and the long doc block explaining why composition is decided in the same walk that measures the length and why no third character class may be introduced. The new field is carried out of THIS scan; a second scan for the offending code point would be exactly the second-opinion shape the block forbids.
    - `scripts/frontmatter.ts:1641-1700` — the total delimiter classifier, both fault clauses, and the indentation branch. Read why the residue arm and the indentation arm are different kinds: the new field belongs to the residue arm only, and the type should make it unrepresentable on the others.
    - `scripts/frontmatter.test.ts` — the delimiter refusal cases that match on the fault wording, so a widened reason does not silently break a matcher or leave one passing against different text.
    - `scripts/frontmatter.test.ts:7299-7326` — the purity case: the slice bounds, the forbidden-substring loop, and the determinism check. Note that the function's body today contains two expressions that identify it uniquely, which is what the premise assertions use.
    - `scripts/frontmatter.ts` `checkGrantOccurrenceBalance` — the function the purity case inspects, so the premise assertions name expressions that are actually in it rather than remembered.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS-8.md` § WR-05 and § IN-03 — both findings with their drafted fixes.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md` § D-56 items 4 and 7.
  </read_first>

  <files>scripts/frontmatter.ts, scripts/frontmatter.js, scripts/frontmatter.test.ts</files>

  <behavior>
    - A delimiter line whose leading run begins with a legal space and continues with a zero-width space refuses naming the zero-width space, not the space.
    - A delimiter line whose leading run's first character is itself the offender still names that character, so the fix does not trade one wrong answer for another.
    - A line whose leading run is entirely legal whitespace produces the indentation kind and carries no residue fault at all.
    - Every other delimiter refusal's wording is byte-unchanged, including the trailing-residue clause and the composed two-fault reason's joining.
    - The purity case fails when handed a slice that is not the function, demonstrated by constructing a short slice.
    - The purity case's forbidden-substring assertions run only after the premise assertions pass.
  </behavior>

  <action>
    Capture the RED transcript first, on a hermetic mirror of the committed build: run the classifier over
    a delimiter line whose leading run is a legal space followed by a zero-width space, and record the
    refusal reason verbatim, showing it names the space. Record the same for a line whose first character
    is the offender, so the control is captured too.

    Then widen the leading-run result's residue arm with `firstOutsideDeclared`, a code point. Set it in
    the scan at the point where the all-declared flag is cleared — the scan is already standing on the
    offending code point there and currently discards it. Keep the field OFF the other two arms so the
    type makes "the offending code point of a run that has none" unrepresentable rather than merely
    untested; state that at the type.

    Change the leading-residue fault clause to interpolate the carried code point instead of the line's
    first code point. Leave every other word of the reason byte-unchanged, including the trailing-residue
    clause and the joining of a two-fault reason. State at the site what went wrong: the clause named
    where the delimiter should begin and then pointed at a character that is legal there.

    Add the two residue cases and the indentation control, each asserting the reported code point label.

    Then close IN-03. In the purity case, assert the slice's PREMISE before any negative assertion runs
    over it: that the body contains the two expressions that identify this function uniquely, and that it
    exceeds a stated line count. Derive nothing from the review — read the function and pick expressions
    that are actually in it. State at the site why: the slice bounds are a search that happens to land on
    the function's closing brace today, and a reformat that shrinks it would make every negative assertion
    pass vacuously, which is the assertion-that-cannot-fail shape this phase closed twice in round 7.

    Prove the premise assertions are load-bearing by constructing a deliberately short slice in a case and
    showing they fail on it.

    Rebuild the committed compiled output and re-run the gates. Compare the foundation gate's full output
    before and after, so the wording change is shown not to have moved any line the gate prints.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; npm run freshness &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' scripts/frontmatter.test.ts &amp;&amp; node scripts/check-foundation-guards.js</automated>
  </verify>

  <acceptance_criteria>
    - A RED transcript from the committed build shows the leading-space-then-zero-width-space line refusing while naming the SPACE's code point label; a GREEN transcript from the rebuilt build shows the same line naming the zero-width space's label.
    - The control where the first character is itself the offender is quoted from both builds and is unchanged.
    - The indentation control produces the indentation kind and no residue fault, quoted from both builds.
    - The residue field is shown unrepresentable on the non-residue arms, verified by the summary quoting the type.
    - Every other delimiter refusal's wording is proven byte-unchanged, verified by comparing the full set of refusal reasons produced over a derived corpus of delimiter lines before and after, and reporting the count of reasons that changed as exactly the residue ones.
    - The purity case's premise assertions are quoted, name expressions that are actually in the inspected function, and are proven load-bearing by a constructed short slice that fails them.
    - The foundation gate's full output is compared before and after and is identical.
    - `npm run build &amp;&amp; npm run freshness` exits 0; `node scripts/check-foundation-guards.js` exits 0.
  </acceptance_criteria>

  <done>
    A reader sent to fix a refused delimiter is sent to the character that refused it, and an assertion
    that could pass over four lines of the wrong function now states what it is looking at first.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One statement of the markdown fact, and a double-claimed foreign key a human can act on — IN-01 and IN-02 (D-56 items 5 and 6)</name>

  <precondition>Task 1 is landed and the committed compiled twins are rebuilt and freshness-clean.</precondition>

  <precondition>`node scripts/check-foundation-guards.js` exits 0 and its `kit counts:` line is captured before any edit, so the behaviour-preserving claim is a comparison rather than an assertion.</precondition>

  <reversibility rating="reversible">Constant usage and one filter domain; reverting is restoring the literals and the narrower filter.</reversibility>

  <read_first>
    - `scripts/kit-model.ts:180-190` — the subpath constants and the markdown-extension constant with the comment that names two rules. Read the comment before changing anything: the fix is either to make the comment true or to correct it, and the decision text says use the constant.
    - `scripts/kit-model.ts:563-575` and `scripts/kit-model.ts:722-735` — the two lister functions spelling the literal. Note that one filters basenames and the other filters relative paths; the constant serves both.
    - `scripts/kit-model.ts:440-505` — the claim partition: the result interface with its four arms and their doc comments, the double-claim filter's domain, and the foreign arm's de-duplication comment from `27-46` with its stated first-occurrence order. The widening must preserve that order discipline.
    - `scripts/kit-model.test.ts:880-930` — the round-8 cases around the double-claimed-foreign input, including the one asserting the double-claim arm empty for exactly that input. That assertion is the pin of the blind spot and it moves here.
    - `scripts/check-foundation-guards.ts` — the guard consuming the partition and interpolating its arms into the failure message, so the summary can state what a human now sees.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS-8.md` § IN-01 and § IN-02.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-46-SUMMARY.md` — the byte-identical-output proof round 8 used for a behaviour-preserving partition change, so this task's proof matches that standard.
  </read_first>

  <files>scripts/kit-model.ts, scripts/kit-model.js, scripts/kit-model.test.ts</files>

  <behavior>
    - The markdown-extension constant is used at every site that tests for a markdown file, and the literal appears exactly once in the module — in the constant's own declaration.
    - Both lister functions return the same file sets as before, byte-for-byte.
    - A key claimed by two buckets and ABSENT from the schema appears once in the double-claim arm and once in the foreign arm.
    - A key claimed by two buckets and PRESENT in the schema still appears once in the double-claim arm and not at all in the foreign arm.
    - A key claimed once and absent from the schema appears only in the foreign arm.
    - The foreign arm's first-occurrence order is unchanged.
    - The guard's `kit counts:` line on the live tree is byte-identical before and after.
  </behavior>

  <action>
    Capture the guard's full output before any edit, including its `kit counts:` line, and record its hash.

    Then use the markdown-extension constant at both literal sites. Correct the constant's comment so it
    names the rules that actually turn on it and the count that makes the claim checkable, rather than a
    remembered pair.

    Add a two-sided assertion in `scripts/kit-model.test.ts` deriving the literal's occurrence count in
    the module source and asserting it is exactly one — the declaration — with comment lines excluded from
    the count so a comment narrating the fact cannot invalidate its own assertion. State in the case what
    set the count enumerates and why a one-sided assertion would not catch the constant being renamed
    away.

    Then widen the double-claim arm's domain to the union of the schema keys and the claimed keys,
    preserving a stated order so two runs over one tree produce byte-identical output. Correct the arm's
    doc comment and the shared-discipline comment beneath it: both arms answer the membership question the
    same way, and now both arms cover the same domain, so a double-claimed foreign key is visible where a
    human fixing two buckets needs it. State what changed and why the previous narrower domain made a real
    multiplicity unreportable rather than reported once.

    Re-pin the round-8 case that asserted the double-claim arm empty for the double-claimed foreign input:
    it now asserts the key present in both arms once each. State in the case that the previous expectation
    pinned a blind spot as behaviour, which is why it moves.

    Add the three discriminating cases: schema key claimed twice, foreign key claimed twice, foreign key
    claimed once. Assert the foreign arm's order is unchanged across all of them.

    Rebuild the committed compiled outputs, re-run the guard, and compare its full output and hash against
    the before capture.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; npm run freshness &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' scripts/kit-model.test.ts &amp;&amp; node scripts/check-foundation-guards.js</automated>
  </verify>

  <acceptance_criteria>
    - The markdown literal's occurrence count in `scripts/kit-model.ts`, derived with comment lines excluded, is exactly one, asserted two-sided by a case and reported in the summary with the expression that derived it.
    - Both lister functions are proven to return identical file sets before and after, compared on the live tree and reported as counts and sorted lists.
    - A RED transcript from the committed build shows the double-claimed foreign input returning an empty double-claim arm; a GREEN transcript from the rebuilt build shows the key present in both arms once each.
    - The three discriminating cases pass and their inputs and outputs are quoted in the summary.
    - The foreign arm's first-occurrence order is proven unchanged, demonstrated over an input with deliberate multiplicity in a non-alphabetical order.
    - The guard's full output and its hash are identical before and after, quoted from both runs — the change is proven behaviour-preserving on the live tree rather than argued to be.
    - `npm run build &amp;&amp; npm run freshness` exits 0; `node scripts/check-foundation-guards.js` exits 0.
  </acceptance_criteria>

  <done>
    The fact has one statement in the file whose thesis is that a fact has one statement, and the arm a
    human reads when two buckets claim one key reports what they need to fix both.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The fixture's premise is checked, and the two carried decisions are recorded with their reasons — IN-04 plus D-56 items 9 and 10</name>

  <precondition>Tasks 1 and 2 are landed and the committed compiled twins are rebuilt and freshness-clean.</precondition>

  <precondition>The two sibling role-adapter cases both pass on the current build before the fixture changes, so their pre-change diagnoses are captured and can be compared afterwards.</precondition>

  <reversibility rating="reversible">A fixture helper, an assertion, and two recorded decisions; reverting is removing them.</reversibility>

  <read_first>
    - `scripts/generate-role-adapters.test.ts:467-521` — both sibling cases in full: the unterminated-region case with its fence-stripping fixture, and the fence-refusal case that deliberately LEAVES the fences in place. Read both comments: they explain that the two are different findings and each gets its own case, which is exactly why the fixture's premise matters.
    - `scripts/generate-role-adapters.test.ts` — the sample role fixtures the scratch mirror is built from, so the executor can see what is inside the fences today and judge what becomes live prose when only the delimiters are removed.
    - `scripts/frontmatter.ts` — the fence-delimiter refusal `27-45` added, so the fixture is understood in terms of which refusal each case is meant to exercise.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS-8.md` § IN-04 — the finding and its drafted fix.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-VERIFICATION.md` — the two `human_verification` entries this task records decisions for, including the exact wording of the retired criterion.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — the existing record's format, so the two new records match it rather than inventing a shape.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md` § D-56 items 8, 9 and 10 — the fixture fix and both recorded decisions with their reasons.
    - `.planning/ROADMAP.md` § Phase 33 and the standing-obligations table — the owning phase and requirement id for the SPAWN-03 capture, so the deferral record names them correctly rather than approximately.
  </read_first>

  <files>scripts/generate-role-adapters.test.ts, .planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md</files>

  <behavior>
    - The fixture removes fenced blocks together with their contents, applying the same toggle rule the module's own fence handling applies.
    - The written file is asserted to carry no delimiter line after line 0 BEFORE it is written, so the case's premise is checked rather than assumed.
    - The unterminated-region case still pins the unterminated diagnosis and not the fence diagnosis, proven by asserting the diagnosis text.
    - The sibling fence-refusal case is unaffected and still pins the fence diagnosis.
    - The fence removal is deterministic: the same input produces the same written bytes on two runs.
    - The deferred-items artifact carries both recorded decisions, each naming what was decided, why, what was measured, and who owns any remaining work.
  </behavior>

  <action>
    First capture the two sibling cases' current diagnoses from a run against the committed build, so the
    fixture change can be shown not to have moved which refusal each case exercises.

    Then replace the fixture's delimiter-only filter with a toggle-based removal that drops the fence
    delimiter lines AND every line between them, matching the rule the module's own fence handling
    applies. Report the removal count as data — lines removed, and blocks removed — derived at run time.

    Then assert the fixture's premise positively before writing: the resulting content carries no
    delimiter line after line 0. State at the site why: the case exists to make the region genuinely run
    to EOF with nothing else in it the module cannot read, and the previous guard — that at least one line
    was removed — would stay green on the day a fenced example gained a column-zero delimiter or key line,
    while the case silently began pinning a different refusal.

    Assert the removal is deterministic by running it twice over the same input and comparing bytes.

    Re-run both sibling cases and compare their diagnoses against the before capture, so the summary can
    state which refusal each case provably exercises now.

    Then write the two recorded decisions into the phase's deferred-items artifact, matching its existing
    format.

    The first records the RETIREMENT of the `27-43` acceptance criterion. Quote the criterion verbatim
    from the verification record. State the decision, its date, and that it is a user decision recorded in
    the round-8 decision block. State the reason in two parts: the structure validator is not a
    spawn-grant surface, and forcing the criterion would add a second spawn-grant predicate, which is the
    weaker-duplicate shape this phase has spent nine rounds deleting. MEASURE the first part rather than
    asserting it — count the occurrences of the spawn, frontmatter and WR-05 vocabulary in
    `scripts/validate-agent-factory.ts` at execution time, with comment lines excluded, and record the
    command and the counts in the record itself. Do not modify that file.

    The second records the DEFERRAL of SPAWN-03's live-platform capture to Phase 33 under GAP-D1. State
    that it is `UNKNOWN - verify` against a real Claude Code session, that no static gate can produce it,
    that inventing one would be fabrication which this repository's constraints forbid by name, and name
    the owning phase and requirement id read from the roadmap rather than from memory.

    Both records state explicitly that they are decisions with reasons and not silent drops, so a later
    reader finds an answer where the verification record left an open question.
  </action>

  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/generate-role-adapters.test.ts &amp;&amp; npm run build &amp;&amp; npm run freshness &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' &amp;&amp; node scripts/check-foundation-guards.js &amp;&amp; node scripts/coordinator-resolution-precheck.js &amp;&amp; node scripts/check-kit-refs.js &amp;&amp; VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js</automated>
  </verify>

  <acceptance_criteria>
    - The fixture's removal count is derived at run time and reported as lines removed and blocks removed, with the expression that derived each.
    - The written file is asserted to carry no delimiter line after line 0 before writing, and the assertion is proven load-bearing by a constructed input carrying such a line that fails it.
    - Both sibling cases' diagnoses are quoted from before and after the fixture change and are unchanged, so each case provably still exercises the refusal it was written for.
    - The fence removal is proven deterministic by a two-run byte comparison.
    - The deferred-items artifact carries the retirement record with the criterion quoted verbatim, the decision, the date, and the MEASURED occurrence counts with the command that produced them.
    - `scripts/validate-agent-factory.ts` is unmodified: it is absent from this plan's `files_modified` and `git diff --name-only` does not list it.
    - The deferred-items artifact carries the SPAWN-03 deferral record naming the owning phase and requirement id read from the roadmap, with its `UNKNOWN - verify` status stated and no static gate invented.
    - The full suite via `npx vitest run --exclude '**/scripts/e2e/**'` reports zero failures; `npm run build &amp;&amp; npm run freshness` exits 0; the foundation gate, the coordinator precheck, the kit-refs check and the validator each exit 0. All stated as floors.
    - `git diff --stat -- package.json package-lock.json` is empty.
  </acceptance_criteria>

  <done>
    The fixture's premise is checked so its two dependent cases cannot drift apart while green, and the
    two questions the round-8 verification left open are answered in a durable record with their reasons
    rather than left to a tenth round to rediscover.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a refusal reason → the human who must fix the file | A diagnosis naming a legal character sends a reader to the wrong byte, which is a correctness failure of the refusal even though the verdict is right |
| the claim partition → the guard's failure message | The arm's consumer is a human fixing two buckets; a multiplicity that cannot be reported leaves half the fix invisible |
| a test fixture → the case that depends on it | A fixture guarded by "something changed" can silently start exercising a different code path while staying green, and two sibling cases can then drift apart |
| a recorded decision → a later reader | A retirement or deferral that lives only in a summary is indistinguishable from an item that was forgotten |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-09-20 | Repudiation | `classifyDelimiter`'s leading-residue fault | medium | mitigate | Per D-56 item 4 the offending code point is carried out of the scan that already visits it and interpolated, with the legal-first-character shape pinned as its own case |
| T-27-09-21 | Tampering | `kit-model.ts`'s markdown-extension fact spelled three ways | medium | mitigate | Per D-56 item 5 the constant is used at every site and the literal's occurrence count is derived and asserted two-sided |
| T-27-09-22 | Repudiation | a double-claimed foreign key unreportable in the guard's message | medium | mitigate | Per D-56 item 6 the double-claim arm covers the union of schema and claimed keys, with the blind-spot pin replaced and the guard output proven byte-identical on the live tree |
| T-27-09-23 | Spoofing | a source-slice assertion passing vacuously after a reformat | high | mitigate | Per D-56 item 7 the slice's premise is asserted before inspection and proven load-bearing by a constructed short slice |
| T-27-09-24 | Spoofing | a fixture silently exercising a different refusal than its case claims | high | mitigate | Per D-56 item 8 fenced blocks are removed with their contents and the written file's premise is asserted before writing; both sibling diagnoses are compared before and after |
| T-27-09-25 | Elevation of privilege | a second spawn-grant predicate added to the structure validator to satisfy a retired criterion | high | mitigate | Per D-56 item 9 the criterion is RETIRED and recorded; the validator is untouched and its absence from `files_modified` is an acceptance criterion |
| T-27-09-26 | Repudiation | a live-platform claim fabricated to close a deferred item | critical | mitigate | Per D-56 item 10 SPAWN-03 stays `UNKNOWN - verify` and deferred to Phase 33; no static gate is invented and the deferral is recorded with its reason |
| T-27-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | ASSERTED ABSENCE: no package-manager install task, no dependency introduced, and `git diff --stat -- package.json package-lock.json` required empty. The package-legitimacy audit has nothing to audit — recorded so an empty audit is not read as a skipped one |
</threat_model>

<verification>
- The residue refusal names the offending code point in both the legal-first-character shape and the offender-first control, proven RED before and GREEN after against the committed build.
- Every other delimiter refusal's wording is proven byte-unchanged over a derived corpus, with the changed-reason count reported.
- The markdown literal appears exactly once in `scripts/kit-model.ts`, asserted two-sided from a derived count.
- Both lister functions return identical file sets before and after on the live tree.
- A double-claimed foreign key appears once in each arm, RED before and GREEN after, with the guard's full output and hash identical.
- The purity case's premise assertions and the fixture's premise assertion are each proven load-bearing by a constructed failing input.
- Both sibling role-adapter cases' diagnoses are unchanged by the fixture fix.
- The deferred-items artifact carries both recorded decisions, the retirement with its measured counts and the deferral with its roadmap-read owner.
- `scripts/validate-agent-factory.ts` is unmodified.
- The full suite excluding the live e2e lane reports zero failures; the foundation gate, the coordinator precheck, the kit-refs check and the validator each exit 0.
</verification>

<success_criteria>
- All five remaining round-8 findings are closed, each with a RED-before and GREEN-after transcript.
- The two behaviour-preserving changes are proven so by output comparison rather than by argument.
- The `27-43` acceptance criterion is retired and recorded with a measured reason, with no second spawn-grant predicate added anywhere.
- SPAWN-03's live capture is recorded as deferred to Phase 33 with its reason and its `UNKNOWN - verify` status intact.
- No hand-maintained set literal, no new dev dependency, and no assertion that cannot fail is introduced.
</success_criteria>

<output>
Create `.planning/phases/27-spawn-correctness-kit-set-authority/27-50-SUMMARY.md` when done.

Record verbatim: the residue refusal reason before and after for both shapes, the changed-reason count
over the derived delimiter corpus, the derived markdown-literal count with its expression, both lister
functions' file sets before and after, the double-claim RED and GREEN transcripts with the guard's full
output and hash from both runs, the purity and fixture premise assertions with the constructed inputs
that prove each can fire, the fixture's derived removal counts, both sibling cases' diagnoses before and
after, and both recorded decisions quoted in full including the measured validator vocabulary counts and
the command that produced them.
</output>

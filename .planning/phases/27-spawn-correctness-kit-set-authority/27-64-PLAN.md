---
phase: 27-spawn-correctness-kit-set-authority
plan: 64
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [KIT-03, SPAWN-04]
files_modified:
  - scripts/generate-skill-twins.ts
  - scripts/generate-skill-twins.js
  - scripts/generate-skill-twins.test.ts
  - scripts/skill-twins-freshness.ts
  - scripts/skill-twins-freshness.js
  - scripts/skill-twins-freshness.test.ts
  - scripts/kit-model.ts
  - scripts/kit-model.js
  - scripts/generate-role-adapters.ts
  - scripts/generate-role-adapters.js
  - package.json
  - .github/workflows/ci.yml

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-64 Part B is delivered: the seven standalone skill twins under .claude/skills/<d>/SKILL.md become GENERATED from skills/<d>/SKILL.md and BYTE-GATED, closing the asymmetry that explains eleven rounds — the 17 agent adapters are generated and byte-gated and have never been bypassed once, while all fourteen SKILL.md files were hand-authored and covered by no freshness gate at all."
    - "The gate follows the proven in-repo pattern verbatim: SET equality AND BYTE comparison against a fresh regeneration into a temp mirror, fail-closed on a regeneration that did not run cleanly, and an explicit refusal to report an empty regeneration as fresh. It is modelled on scripts/adapters-freshness.ts, which is the fifth working instance of that pattern in this tree."
    - "The generator's correctness premise is asserted, not assumed: its FIRST run must reproduce all seven committed twins BYTE-IDENTICALLY. If any twin differs, the run HALTS and reports the difference rather than overwriting the committed file, because a generator that quietly rewrites its target proves nothing about derivability."
    - "The byte-gate is proven able to FAIL (D-64 vacuity trap 4): a planted one-byte drift in a generated twin makes the gate red BY NAME, and a run that regenerated zero files is reported as the anomaly rather than as success."
    - "The exemption list length is stated BEFORE and AFTER and is UNCHANGED. DISTRIBUTION_PAIR_EXEMPT has exactly one member today; no exemption may be added to make a cell pass. This plan asserts the length by reading the committed guard source and does NOT edit scripts/check-foundation-guards.ts."
    - "The kit-root resolver block has ONE source. It is already a single constant inside scripts/generate-role-adapters.ts and it is moved to the kit-model authority so both generators import it; a second copy of the resolver text is the hand-maintained-set drift class this repo has already paid for."
    - "A green suite is a FLOOR. Closure evidence for this plan is the byte-comparison transcript and the two fail-proof exit codes."
  artifacts:
    - "scripts/generate-skill-twins.ts — derives .claude/skills/<d>/SKILL.md from skills/<d>/SKILL.md by a name rewrite plus, for the kit root only, the resolver-block insertion"
    - "scripts/skill-twins-freshness.ts — the sixth freshness gate: mirror-spawn, SET equality, BYTE comparison, fail-closed"
    - "scripts/generate-skill-twins.test.ts and scripts/skill-twins-freshness.test.ts — the derivability proof and the two fail-proofs"
    - "package.json — generate:skill-twins and freshness:skill-twins entries, matching the existing naming convention"
    - ".github/workflows/ci.yml — the new freshness gate wired into the ubuntu-only gate block alongside its five siblings"
  key_links:
    - "generate-skill-twins.ts -> kit-model.ts listSkillAdapters / listPluginSkillAdapters: both the source set and the output set are read from the ONE kit authority, never from a private readdir in the generator or in the gate."
    - "skill-twins-freshness.ts -> the mirror-spawn contract: the generator's OUT_DIR stays a fixed literal as a path-traversal mitigation, and the gate mirrors the tree instead of overriding it — the same arrangement adapters-freshness.ts uses and the same reason."
    - "generate-role-adapters.ts -> kit-model.ts RESOLVER: the resolver constant moves to the shared authority so the skill generator imports it rather than restating ten lines of shell in a second place."
    - "the generated twins -> the spawn-grant scan: all seven remain inside SPAWN_GRANT_SCAN, so plan 27-62's admission cardinality and this plan's byte-gate cover the same files from two directions."
---

<objective>
Deliver D-64 Part B: generate the seven standalone skill twins and byte-gate them, extending to the
surface that keeps failing the exact mechanism that has never failed.

The asymmetry D-64 names is the whole argument. Every bypass reproduced in every one of eleven rounds
landed in a `SKILL.md`. The seventeen agent adapters are generated and byte-gated by
`adapters-freshness.ts` and have never been bypassed once. The fourteen `SKILL.md` files are
hand-authored and no freshness gate in this tree names `SKILL` at all. The strong mechanism already
exists here, is proven, and was simply never extended to the failing surface.

The twins are exactly derivable, and that was verified this session rather than assumed: all six
non-root pairs differ by exactly one line (`name: <d>` becomes `name: grugops-<d>`, an eight-byte
delta on every one of `gate`, `map`, `plan`, `release`, `ticket`, `uat`), and the `grugops` root pair
differs by a single eleven-line insertion which is byte-for-byte the existing `RESOLVER` constant in
`scripts/generate-role-adapters.ts` plus one blank line. Generation is a name rewrite plus one known
addendum, not a re-authoring.

Purpose: make a hand-edit to a skill twin impossible to commit green. Output: a generator, a sixth
freshness gate, both wired into the build and continuous integration, with the derivability premise
asserted and the gate proven able to fail.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md
@CLAUDE.md
@scripts/adapters-freshness.ts
</context>

<repo_execution_notes>
- Test command is `npx vitest run --exclude '**/scripts/e2e/**'`. NEVER bare `npm test` — it triggers
  a live claude-CLI e2e lane that spends tokens and can hang for roughly eight minutes.
- Typecheck is `npm run typecheck`, both lanes.
- Editing a `.ts` under `scripts/` REQUIRES `npm run build` and committing the `.js`. The freshness
  gates compare committed `.js`, and vitest resolves test imports to the committed `.js`.
- The verification harness has produced a FALSE result sixteen times in this phase. Any task claiming
  a mutation or drift result MUST run the build, ASSERT THE BUILD'S EXIT CODE, PROBE THE BUILT
  ARTIFACT DIRECTLY, and SUBTRACT an unmutated baseline.
- `diff` is a shell function aliased to `git diff --no-index`; use `/usr/bin/diff` for real output.
- Every plant asserts its own premise so a wrong path halts loudly.
- Never fabricate a gate result or a citation. Unknowns are `UNKNOWN - verify` with the reason.
</repo_execution_notes>

<measured_baseline>
Measured this session on `fb59ca6`. If a task's own re-measurement disagrees, HALT and report.

- Source skills, `skills/*/SKILL.md`: **7** — `gate`, `grugops`, `map`, `plan`, `release`, `ticket`,
  `uat`.
- Standalone twins, `.claude/skills/*/SKILL.md`: **7** — `grugops-gate`, `grugops-map`,
  `grugops-plan`, `grugops-release`, `grugops-ticket`, `grugops-uat`, and `grugops`.
- Six non-root pairs: byte delta **+8** each, `/usr/bin/diff` reports exactly one changed line, the
  `name` value.
- Root pair `skills/grugops/SKILL.md` to `.claude/skills/grugops/SKILL.md`: byte delta **+448**,
  `/usr/bin/diff` reports a pure insertion of **11 lines** at line 13 and NO changed line — the root
  twin's `name` is `grugops` on both sides.
- That insertion is the `RESOLVER` constant at `scripts/generate-role-adapters.ts:152-163`, ten
  entries joined by newline, plus one trailing blank line. Verified this session: the twin contains
  the joined constant verbatim, the source does not, and removing the constant plus one blank line
  from the twin yields the source exactly.
- `DISTRIBUTION_PAIR_EXEMPT` at `scripts/check-foundation-guards.ts:1659` has exactly **1** member:
  `skills/grugops/SKILL.md`.
- Existing freshness gates: **5** — build, catalog, context, queue, traceability, plus adapters. The
  package entries are named `freshness:<domain>` and the generators `generate:<domain>`.
</measured_baseline>

<tasks>

<task type="tracer">
  <name>Task 1: the skill-twin generator, reproducing all seven committed twins byte-identically</name>
  <files>scripts/generate-skill-twins.ts, scripts/generate-skill-twins.js, scripts/kit-model.ts, scripts/kit-model.js, scripts/generate-role-adapters.ts, scripts/generate-role-adapters.js, package.json</files>
  <read_first>
    scripts/generate-skill-twins.ts (created by this task);
    scripts/generate-role-adapters.ts lines 40-100 and 143-170 and 376-411 (the root convention, the
    fixed-literal OUT_DIR path-traversal mitigation, the RESOLVER constant, and the render-then-write
    tail this generator mirrors);
    scripts/kit-model.ts lines 799-847 (listSkillAdapters and listPluginSkillAdapters, the ONE
    authority for both sides of this generation);
    skills/grugops/SKILL.md and .claude/skills/grugops/SKILL.md (the root pair with the insertion);
    skills/map/SKILL.md and .claude/skills/grugops-map/SKILL.md (a representative non-root pair).
  </read_first>
  <action>
    First, move the resolver text to one authority. `RESOLVER` is currently a private constant inside
    `scripts/generate-role-adapters.ts`, and that file is a top-level SCRIPT that writes files when
    imported, so it can never be imported by a second generator. Move the constant to
    `scripts/kit-model.ts` and export it, then have `generate-role-adapters.ts` import it instead of
    declaring it. Record the reason at both sites: the resolver block is a kit fact consumed by two
    generators, and a second copy of ten lines of shell is exactly the hand-maintained-literal drift
    class this phase exists to close. Rebuild and confirm all seventeen agent adapters regenerate
    byte-identically after the move — the move must be provably behavior-preserving before anything
    depends on it, and `npm run freshness:adapters` is the check that says so.

    Create `scripts/generate-skill-twins.ts` modelled on `generate-role-adapters.ts`. Keep `OUT_DIR`
    a fixed literal joined to the script-relative repository root and accept NO output flag; that is
    the same path-traversal mitigation the adapter generator carries and the freshness gate depends
    on it staying fixed. Read the source set through `listSkillAdapters` and never through a private
    readdir.

    The transform is exactly two rules and nothing else:
    - For every source skill whose directory name is not the kit root, emit
      `.claude/skills/grugops-<d>/SKILL.md` whose bytes are the source bytes with the `name` value
      rewritten from `<d>` to `grugops-<d>`. Rewrite the `name` value only — locate it as the
      frontmatter `name` key, not by a text substitution over the document, so a body occurrence of
      the directory name can never be rewritten.
    - For the kit root, emit `.claude/skills/grugops/SKILL.md` whose bytes are the source bytes with
      the resolver block plus one blank line inserted immediately after the kit-vs-state invariant
      line. The root's `name` is unchanged on both sides; do not rewrite it.

    Assert the derivability premise INSIDE the generator, and make it the loud path. On every run,
    compare each rendered twin against the committed file at that path. If the committed file exists
    and differs, do NOT write: print the path, the byte lengths of both sides and a bounded unified
    diff, and exit non-zero. A generator that silently rewrites its target proves nothing about
    derivability, and this phase's whole failure mode is a mechanism that produces a convincing result
    without having measured anything. Provide an explicit flag to write anyway, so a legitimate future
    source change can be adopted deliberately, and make the flag's name state that it overwrites.

    Report what was done, never a bare success: the number of twins rendered, the number written, the
    number already identical, and the output directory. A run that rendered zero twins must be visible
    as the anomaly it is.

    Add the `generate:skill-twins` entry to `package.json`, matching the existing entries' shape
    exactly, including the compile-to-temp prefix the siblings use.

    Run `npm run build`, assert its exit code, then run the generator and record its transcript.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run freshness:adapters &amp;&amp; npm run generate:skill-twins &amp;&amp; git diff --exit-code -- .claude/skills &amp;&amp; echo "SEVEN TWINS REPRODUCED BYTE-IDENTICALLY, working tree clean"</automated>
  </verify>
  <done>
    The resolver constant lives in `scripts/kit-model.ts`, both generators import it, and
    `npm run freshness:adapters` exits 0 proving the move preserved all seventeen adapters byte for
    byte. `npm run generate:skill-twins` renders seven twins, reports seven already identical and
    zero written, and `git diff --exit-code -- .claude/skills` exits 0 — the committed twins are
    provably the generator's own output. `npm run build` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: the sixth freshness gate — SET equality plus BYTE comparison, fail-closed</name>
  <files>scripts/skill-twins-freshness.ts, scripts/skill-twins-freshness.js, scripts/skill-twins-freshness.test.ts, scripts/generate-skill-twins.test.ts, package.json, .github/workflows/ci.yml</files>
  <read_first>
    scripts/skill-twins-freshness.ts (created by this task);
    scripts/adapters-freshness.ts (the whole file — the pattern this gate copies, including the two
    roots, the mirror-spawn, the CHECK_ROOT strip from the child environment, the exit handler
    cleanup, and the fail-closed branches);
    scripts/adapters-freshness.test.ts (the harness idiom for pointing a freshness gate at a mirror);
    scripts/generate-skill-twins.ts (from task 1);
    scripts/kit-model.ts (listPluginSkillAdapters, the authority for the output listing);
    .github/workflows/ci.yml lines 70-95 (the ubuntu-only gate block this gate joins).
  </read_first>
  <action>
    Create `scripts/skill-twins-freshness.ts` as the sixth instance of the mirror-spawn freshness
    pattern, modelled near-verbatim on `scripts/adapters-freshness.ts`. Keep every property of that
    file that makes it fail closed, and record at each site that it is inherited rather than
    reinvented:

    - TWO ROOTS. A script-relative root for the committed compiled twins the mirror copies, and a
      kit root honoring the existing `CHECK_ROOT` override so a test can point the gate at a hermetic
      mirror. Do not invent a fourth root variable.
    - MIRROR-SPAWN. Copy the committed `.js` of the generator and of every module in its import
      closure, plus the kit sources, into a temp directory, then spawn the mirrored generator so its
      fixed-literal output directory resolves inside the mirror. Carry forward the comment explaining
      that the import-closure list is hand-written on purpose and that the failure direction is loud:
      an unmirrored import makes the mirrored generator fail to resolve and exit non-zero. Note that
      the mirrored generator must be invoked with the overwrite flag task 1 added, because inside the
      mirror there is nothing to preserve and the comparison is this gate's job, not the generator's.
    - STRIP `CHECK_ROOT` from the child environment, for the reason `adapters-freshness.ts` states: an
      inherited override would point the fresh regeneration back at the tree it is meant to be
      compared against, and the gate would compare a tree with itself and always pass.
    - CLEANUP on every exit path including an uncaught throw, registered immediately after the temp
      directory exists.
    - FAIL-CLOSED on a regeneration that did not run cleanly: print the generator's own output and
      exit non-zero. A broken generator must never read as an up-to-date twin directory.
    - REFUSE an empty regeneration explicitly. A run that produced no twins is the anomaly, never
      "nothing to compare, therefore fine". This is D-64 vacuity trap 4's second half and it gets its
      own named branch and its own test.
    - HALF ONE, SET equality over full relative paths, naming EXTRA and MISSING members separately, so
      an orphan twin left by a deleted skill and a missing twin are each visible by name.
    - HALF TWO, BYTE comparison over the proven-equal member set, naming every differing member.
    - REPORT WHAT WAS CHECKED: the number of twins compared and the number of byte differences, so a
      run that compared zero twins reads as the anomaly rather than as success.

    Add `freshness:skill-twins` to `package.json` matching the siblings' shape, and wire it into the
    ubuntu-only gate block in `.github/workflows/ci.yml` alongside its five siblings. Wire it at BOTH
    ends as `adapters-freshness.ts` documents: the workflow step, and a test file that spawns the
    committed `.js` directly so the drift lane survives a workflow refactor that drops or renames the
    step. A gate nothing re-runs fails nothing closed, which is the state `freshness:adapters` was in
    before plan 27-11 and the single most expensive omission of this phase.

    Create `scripts/skill-twins-freshness.test.ts` with the mirror-based cases: a clean mirror exits
    0 and prints seven compared with zero differences; an unreadable committed twin fails closed; a
    missing member and an extra member each fail naming the member.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run freshness:skill-twins &amp;&amp; npx vitest run scripts/skill-twins-freshness.test.ts scripts/generate-skill-twins.test.ts --exclude '**/scripts/e2e/**' --reporter=verbose &amp;&amp; grep -c 'freshness:skill-twins' .github/workflows/ci.yml package.json</automated>
  </verify>
  <done>
    `npm run freshness:skill-twins` exits 0 and prints that seven twins were compared with zero byte
    differences and set-equal listings. The gate is present in both `package.json` and the ubuntu-only
    block of `.github/workflows/ci.yml`, and a test spawns the committed `.js` directly. The mirror
    cases for unreadable, missing and extra members each fail closed naming the member.
  </done>
</task>

<task type="auto">
  <name>Task 3: the byte-gate is proven able to FAIL, and the exemption list is unchanged</name>
  <files>scripts/skill-twins-freshness.test.ts</files>
  <read_first>
    scripts/skill-twins-freshness.test.ts (from task 2);
    scripts/skill-twins-freshness.ts (from task 2);
    scripts/check-foundation-guards.ts lines 1655-1750 (guardDistributionPair and
    DISTRIBUTION_PAIR_EXEMPT — READ ONLY, this plan does not edit this file);
    .planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md lines 2830-2845 (the
    premise-controls-recorded-first shape).
  </read_first>
  <behavior>
    - Premise control recorded FIRST: on an unmutated hermetic mirror the gate exits 0 and reports
      seven compared, zero differences. This makes the plants below attributable to the plant.
    - A ONE-BYTE drift planted in a generated twin on a hermetic mirror makes the gate exit non-zero
      and NAME the drifted file. The transcript shows the exit code before and after and the gate's
      own message.
    - A regeneration that produces zero twins is reported as the anomaly by its own named branch, and
      the transcript shows that message rather than a generic failure.
    - `DISTRIBUTION_PAIR_EXEMPT` has exactly one member before this plan and exactly one member after
      it. The assertion reads the committed guard source; this plan does not edit that file.
    - `node scripts/check-foundation-guards.js` exits 0 on the unmodified tree after this plan's
      changes, so the generated twins did not perturb any existing guard.
  </behavior>
  <action>
    Extend `scripts/skill-twins-freshness.test.ts` with the fail-proofs described above. Every plant
    runs on a hermetic mirror created from a clean archive of the working tree, never on the committed
    tree.

    Record the premise control FIRST and assert it, following `27-61`'s sweep shape. A plant whose
    baseline was not recorded is the arrangement that produced reds nobody could attribute.

    For the one-byte drift, mutate exactly one byte of one generated twin inside the mirror and assert
    the mutation actually changed the file's bytes before running the gate. A plant that did not land
    produces a perfectly convincing green, and this phase's record shows the harness premise failing
    sixteen times. Capture the gate's exit code and its full stdout, assert the exit code moved from 0
    to non-zero, and assert the drifted file's relative path appears in the message. An exit code
    alone is not the claim; the claim is that it failed for the right reason and named the right file.

    For the empty-regeneration branch, arrange a mirror in which the mirrored generator exits cleanly
    and emits nothing, and assert the gate's specific refusal branch fired rather than a generic
    failure path. If the generator cannot be made to exit cleanly while emitting nothing, record that
    as `UNKNOWN - verify` with the reason and pin the branch by a direct unit-level exercise instead;
    do not fabricate a transcript for a condition that was not produced.

    For the exemption assertion, read `scripts/check-foundation-guards.ts` at run time, extract the
    exemption array by a bounded slice whose section marker is asserted present before the slice is
    used, and assert its member count. Follow `27-60`'s bounded-slice idiom including the non-vacuity
    floor on the length of what was scanned; an unbounded slice is the brittleness IN-03 named. State
    the count in the summary both before and after this plan's changes. No exemption may be added to
    make a cell pass — that is D-64's vacuity trap 3 and it is a hard constraint on this round.

    Finally run `node scripts/check-foundation-guards.js` on the unmodified tree and record its exit
    code and its full output, so this plan can show it perturbed no existing guard. Note in the
    summary that `guard_distribution_pair` and the new byte-gate now ask overlapping questions from
    two directions, and that the byte-gate is the stronger of the two because it compares against a
    regeneration rather than against the pair's sibling; do NOT remove or weaken the pair guard here.
    Any consolidation is a later decision, and this round removes nothing.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run scripts/skill-twins-freshness.test.ts --exclude '**/scripts/e2e/**' --reporter=verbose &amp;&amp; node scripts/check-foundation-guards.js &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    The premise control is recorded first and exits 0 with seven compared and zero differences. The
    one-byte drift moves the gate from exit 0 to a non-zero exit and the transcript shows the gate
    naming the drifted file. The empty-regeneration refusal is exercised by its own named branch, or
    recorded `UNKNOWN - verify` with the reason and pinned at unit level. The exemption list is
    asserted at exactly one member, stated before and after, with `scripts/check-foundation-guards.ts`
    byte-unchanged by this plan. `node scripts/check-foundation-guards.js` exits 0 on the unmodified
    tree and `npm run typecheck` exits 0 on both lanes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-edited SKILL.md -> the distributed kit | The surface every one of eleven rounds' bypasses landed on, and the surface with no freshness gate today. |
| generator OUT_DIR -> the filesystem | A generator that accepted an output path could be steered outside the repository. |
| mirrored child process environment -> the gate's own comparison | An inherited root override would make the gate compare a tree with itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-148 | Tampering | a hand-edited skill twin committed green | critical | mitigate | Twins become generated; the sixth freshness gate compares SET and BYTES against a fresh regeneration; wired into both continuous integration and a test that spawns the committed build directly. |
| T-27-149 | Tampering | generator output path traversal | high | mitigate | OUT_DIR stays a fixed literal joined to the script-relative root, with no output flag, exactly as the adapter generator does; the freshness gate mirror-spawns instead of overriding it. |
| T-27-150 | Repudiation | a freshness gate that always passes because it compared a tree with itself | critical | mitigate | The root override is stripped from the child environment, and the one-byte-drift fail-proof in task 3 proves the gate can red with its premise control recorded first. |
| T-27-151 | Denial of Service | temp mirrors accumulating outside the gate's lifetime | medium | mitigate | Cleanup registered immediately after the temp directory exists plus explicit calls on every exit path, inherited from adapters-freshness.ts where this was already reproduced and fixed. |
| T-27-152 | Elevation of Privilege | an exemption added to make a failing cell pass | high | mitigate | The exemption list length is asserted before and after by a bounded source read with a non-vacuity floor, and this plan does not edit the guard that owns it. |
| T-27-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs NO package. It edits `package.json` scripts only; the dependency stanzas must be byte-unchanged. Assert the dependency diff is empty in the summary. |
</threat_model>

<verification>
- `npm run build` exits 0; every edited `.ts` has its committed `.js` regenerated.
- `npm run freshness:adapters` exits 0, proving the resolver-constant move preserved all seventeen
  agent adapters byte for byte.
- `npm run generate:skill-twins` reports seven rendered, seven already identical, zero written, and
  `git diff --exit-code -- .claude/skills` exits 0.
- `npm run freshness:skill-twins` exits 0 reporting seven compared and zero differences.
- `node scripts/check-foundation-guards.js` exits 0 on the unmodified tree.
- `npm run typecheck` exits 0 on both lanes.
- `scripts/frontmatter.ts` is BYTE-UNCHANGED by this plan.
- `scripts/check-foundation-guards.ts` is BYTE-UNCHANGED by this plan.
- The `package.json` dependency stanzas are byte-unchanged; no dependency is added.
- The full suite is recorded as a FLOOR, never as closure evidence.

## Two-sided count reconciliation

State in the summary, as a checkable arithmetic line: freshness gates before this plan versus after,
and skill twins under generation before versus after. If the two numbers a reader can count from
`package.json` and from `.claude/skills/` disagree with the summary, the summary is wrong, not the
count.
</verification>

<success_criteria>
- The seven standalone skill twins are generated and byte-gated, per D-64 Part B, closing the
  asymmetry between the never-bypassed generated adapters and the eleven-times-bypassed hand-authored
  skills.
- The generator reproduces all seven committed twins byte-identically on its first run, proving
  derivability rather than asserting it.
- The gate is proven able to FAIL on a one-byte drift with its premise control recorded first, and
  an empty regeneration is refused by its own named branch.
- The exemption list is exactly one member before and after, and the guard file is untouched.
</success_criteria>

<output>
Create `.planning/phases/27-spawn-correctness-kit-set-authority/27-64-SUMMARY.md` when done.

The summary MUST carry:
- the generator transcript showing seven rendered, seven identical, zero written, and the clean
  `git diff --exit-code` result;
- the `freshness:adapters` result proving the resolver move preserved the seventeen adapters;
- the one-byte-drift fail-proof with exit codes before and after and the gate's own message quoting
  the drifted file;
- the empty-regeneration branch transcript, or an `UNKNOWN - verify` with its reason;
- `DISTRIBUTION_PAIR_EXEMPT` member count stated before and after, both equal to one;
- the two-sided count reconciliation for freshness gates and generated twins;
- an explicit statement that the green suite is a FLOOR;
- confirmation that `scripts/frontmatter.ts` and `scripts/check-foundation-guards.ts` are byte-unchanged
  and that no dependency was added.
</output>

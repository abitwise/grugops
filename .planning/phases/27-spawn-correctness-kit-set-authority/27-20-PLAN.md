---
phase: 27-spawn-correctness-kit-set-authority
plan: 20
type: execute
wave: 9
depends_on: ["27-19"]
files_modified:
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - scripts/check-foundation-guards.test.ts
autonomous: true
gap_closure: true
requirements: [SPAWN-05, SPAWN-04]
closes_findings: [CR-03, WR-05]

must_haves:
  truths:
    - "A coordinator body whose entire tier announcement is wrapped in an HTML comment makes the aggregator exit non-zero and name every absent beat — the review's reproduction, which prints a passing WR-05 line and ALL CHECKS PASSED at HEAD."
    - "The tier-beat check and guard_adapter_body's positive half read the SAME input shape: one fence authority, then comments removed, then occurrences counted. One predicate, one treatment."
    - "A coordinator body stating a beat twice in live text fails, because a body the generator does not produce is not a body this guard may pass."
    - "An agent adapter that declares no tools key fails guard_wr05 by name, because omitting the key makes the platform grant every main-conversation tool including the spawn tool, so an absent key is a grant by inheritance and not a no-grant."
    - "The tools-key floor reads the same parse the rogue-grant loop built, and imports TOOLS_KEYS from the frontmatter authority rather than restating the two key names."
    - "The eight pre-existing tier-beat cases still pass unmodified, because the zero-occurrence finding keeps its existing wording and only the greater-than-one case gains new wording."
    - "SPAWN-05 adjacency edge: a beat needle appearing once in live text and once inside an HTML comment counts as one live occurrence and passes; the same needle appearing twice in live text counts as two and fails."
    - "SPAWN-05 empty edge: zero live occurrences of a beat is reported as the beat being missing, and an empty coordinator body reports all six beats missing rather than vacuously passing."
    - "SPAWN-05 encoding edge: beats are compared after the shared whitespace collapse, so a hard-wrapped announcement still reads as one sentence and the verdict never depends on where an author wrapped a line."
    - "SPAWN-05 ordering edge: the six beats are counted independently and the order they appear in the body does not change the verdict or the finding text."
  artifacts:
    - "scripts/check-foundation-guards.ts declares stripHtmlComments and countOccurrences above guardWr05 as one shared pair used by both the tier-beat check and guard_adapter_body's positive half."
    - "scripts/check-foundation-guards.ts imports TOOLS_KEYS from ./frontmatter.js in the existing import list."
    - "scripts/check-foundation-guards.js rebuilt by tsc, byte-fresh under npm run freshness."
    - "scripts/check-foundation-guards.test.ts carries the comment-wrapped-announcement RED case, a duplicated-beat case, and an absent-tools-key case."
  key_links:
    - "guardWr05's tier-beat body at :552-563 must become the same expression guard_adapter_body builds at :721-723 — the composition of stripFencedBlocks, stripHtmlComments and collapseWhitespace, then countOccurrences."
    - "The tools-key floor extends the existing name-key floor loop at :533-540, which already holds the parse in parsedScan."
    - "consistentMirror() and plantPlainAdapter() in the test file emit adapters with no tools key today; the floor makes every case built on them fail unless the fixtures declare one."
  prohibitions:
    - "A guard PASS line must never state a check that was not performed over the input it names."
    - "An existing RED case must never be deleted, skipped or weakened to make a new fix go green."
    - "A fixture must never be edited into a shape that can no longer express the failure it was written to pin."
---

<objective>
Close CR-03 and WR-05, both inside `guardWr05()`.

CR-03 is one predicate with two treatments inside one aggregator. Plan 27-14 closed the prior
review's WR-05 in `guard_adapter_body` by adding `stripHtmlComments()` and an occurrence count. The
tier-beat check — same file, same coordinator body, 160 lines earlier — got neither, so wrapping the
entire tier announcement in an HTML comment yields `PASS ... the coordinator body carries all 6
tier-announcement beats` with zero of the six live. That PASS line makes a false claim about a
capability-and-safety announcement, which is the T-27-34 spoofing threat this guard names at its own
`:429-432`.

WR-05 is the absent-key half of the same argument that justified the `name`-key floor two lines
above it. Per this project's own stack notes, a sub-agent with no `tools` key inherits every
main-conversation tool, `Agent` included. `keysHaveSpawnGrant()` returns false for a missing key, so
the non-coordinator direction reads a total grant as compliant.

Purpose: one treatment per predicate, and a floor that makes an absent declaration reportable.
Output: the comment-stripped counted beat check, the tools-key floor, a rebuilt committed `.js`, and
fixtures that carry a declared no-spawn tools line.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-19-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give the tier-beat predicate the same treatment guard_adapter_body's positive half already has</name>
  <files>scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js, scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS.md` § CR-03 — the reproduction, the `file:line` anchors and the suggested patch, including the note on declaration order and hoisting.
    - `scripts/check-foundation-guards.ts:400-580` — `collapseWhitespace` `:413`, the `TIER_BEATS` design comment `:420-484`, `guardWr05()` `:485-579`, the tier-beat loop `:552-563`, the PASS line `:572-575`.
    - `scripts/check-foundation-guards.ts:673-733` — `stripHtmlComments` `:676`, `countOccurrences` `:681`, and `guard_adapter_body`'s positive half `:719-732` with its zero-versus-more-than-one split. This is the treatment being reused.
    - `scripts/check-foundation-guards.test.ts:637-720` — the five `beatCases` removal cases and the two `wrongCommandCases` replacement cases, all of which assert the existing missing-beat wording.
    - `scripts/check-foundation-guards.test.ts:772-790` — the PASS-line case asserting `all 6 tier-announcement beats`.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md` — D-05 (revised: the guard asserts tier-announcement presence) and D-26 (defense in depth, never the structural fix).
  </read_first>
  <action>
Move the `stripHtmlComments` and `countOccurrences` declarations in
`scripts/check-foundation-guards.ts` up beside `collapseWhitespace` at `:413`, keeping their existing
bodies and comments verbatim so there is exactly one pair and no duplicate. Update the comment that
currently states the strip applies to `guard_adapter_body`'s positive half only, to say that it is
shared by the tier-beat check as well, and record why: a comment quoting an announcement is not an
announcement.

Rewrite the tier-beat body in `guardWr05()` to the same composition `guard_adapter_body` builds —
`collapseWhitespace` over `stripHtmlComments` over `stripFencedBlocks` over the coordinator's text —
and replace the bare `includes` test with `countOccurrences`.

Split the finding on the count, mirroring `guard_adapter_body`'s own zero-versus-more-than-one split:

- Zero occurrences keeps the EXISTING wording byte-for-byte, including the `why` clause and the
  `BEAT_DEFAULT_WHY` fallback. This is a deliberate deviation from the review's single-message patch:
  the existing five removal cases and three command-name cases assert that wording, they are correct
  pins, and churning them would be weakening working coverage to accommodate a cosmetic choice.
  Record that reason in the code comment beside the split.
- More than one occurrence gains new wording naming the beat label, the count, and that the text was
  read live, non-fenced and non-commented, with the consequence that a body stating a beat twice is
  not a body the generator produces.

Append to the PASS line, do not rewrite it: keep the existing `all N tier-announcement beats` phrase
so the PASS-line case at `:772` stays valid, and extend it with the clause that makes the claim true —
each beat appears exactly once in live, non-fenced, non-commented text.

Add two cases in `scripts/check-foundation-guards.test.ts` beside the five `beatCases`, and name both
for CR-03. The first is the review's reproduction: take a `mirror()`, wrap the whole tier
announcement block in the coordinator adapter in an HTML comment and change nothing else, then assert
non-zero status and that the output names every one of the six beat labels. Guard the fixture itself
the way the `wrongCommandCases` do — assert the announcement markers are present before the wrap — so
the case can never silently become a no-op plant. The second duplicates one beat's line in live text
and asserts the greater-than-one finding names that beat and the count.

Rebuild with `npm run build`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run freshness &amp;&amp; npx vitest run scripts/check-foundation-guards.test.ts &amp;&amp; node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - RED-before: mirror the live tree to `/tmp`, wrap the tier announcement in `.claude/agents/grugops-orchestrator.md` in an HTML comment, then `CHECK_ROOT=$MIRROR node scripts/check-foundation-guards.js` exits 0 and prints a passing WR-05 line. Transcript recorded in the SUMMARY.
    - GREEN-after: the same command over the same mirror exits 1 and its output names all six beat labels.
    - The five `beatCases` and two `wrongCommandCases` are unmodified and still pass: `git diff scripts/check-foundation-guards.test.ts` shows no deletion inside `:637-720`.
    - `npx vitest run scripts/check-foundation-guards.test.ts` exits 0 with a case count exactly two greater than the pre-change count.
    - `grep -c "stripHtmlComments" scripts/check-foundation-guards.ts` reports exactly 3 — one declaration and two call sites — proving there is no second implementation.
    - `node scripts/check-foundation-guards.js` over the real tree exits 0 and its WR-05 pass line still contains `all 6 tier-announcement beats`.
    - `npm run build` then `npm run freshness` exits 0.
  </acceptance_criteria>
  <done>A commented-out or duplicated tier announcement fails WR-05 by beat name, and the PASS line states the input shape it actually read.</done>
</task>

<task type="auto">
  <name>Task 2: Fail an agent adapter that declares no tools key, and give the fixtures a declared no-spawn tools line</name>
  <files>scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js, scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS.md` § WR-05 — the reproduction, the anchors and the suggested patch, and the sentence quoting this project's own stack notes on an omitted `tools` key.
    - `scripts/check-foundation-guards.ts:497-540` — the rogue-grant loop that populates `parsedScan`, and the `name`-key floor at `:533-540` this extends.
    - `scripts/check-foundation-guards.ts:182-196` — the existing import list from `./frontmatter.js`, where `TOOLS_KEYS` is added.
    - `scripts/frontmatter.ts:313-333` — `TOOLS_KEYS` and `keysHaveSpawnGrant`, including the header paragraph explaining why the grant test is scoped to those keys in both directions.
    - `scripts/check-foundation-guards.test.ts:224-287` — `consistentMirror()` and `plantPlainAdapter()`, both of which emit adapters carrying no `tools` key today.
    - `CLAUDE.md` § "3. Subagent" — the platform fact that omitting `tools` inherits every main-conversation tool.
  </read_first>
  <action>
Add `TOOLS_KEYS` to the existing `./frontmatter.js` import list in
`scripts/check-foundation-guards.ts` rather than restating the two key names locally.

Extend the `name`-key floor loop over `AGENT_ADAPTERS` in `guardWr05()` with a second condition: if
none of `TOOLS_KEYS` is present in the parsed keys, append a finding naming the file and stating the
consequence — omitting the key makes the platform grant every main-conversation tool including the
spawn tool, so an absent key is a grant by inheritance and this guard cannot report on it. Keep the
existing scoping comment accurate: the floor is deliberately scoped to the agent adapters, because
the packaging templates and the skills carry frontmatter of a different kind. Extend that comment to
say the same argument that justified the `name` floor is what justifies this one — an absent key and
a declared no-spawn key print the same silence and mean opposite things.

Then update the two fixtures that would otherwise fail every case built on them: make
`consistentMirror()` and `plantPlainAdapter()` emit a declared, deliberately spawn-free `tools` line
in the frontmatter they write. Add a comment on each recording that the line is load-bearing, not
decoration — an adapter without it is now a genuinely defective adapter and every case built on the
fixture would fail for a reason having nothing to do with what it tests, which is the same note
`consistentMirror()` already carries for the memory sentence.

Add one RED case named for WR-05: take a `mirror()`, delete the `tools:` line from one
non-coordinator agent adapter, and assert non-zero status plus output naming that adapter and the
grant-by-inheritance consequence. Guard the fixture by asserting the line was present before the
deletion.

Rebuild with `npm run build`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run freshness &amp;&amp; npx vitest run scripts/check-foundation-guards.test.ts &amp;&amp; node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - RED-before: on a `/tmp` mirror with the `tools:` line deleted from `.claude/agents/grugops-qe-e2e.md`, `CHECK_ROOT=$MIRROR node scripts/check-foundation-guards.js` exits 0 and prints a passing WR-05 line. GREEN-after: the same command exits 1 and names `grugops-qe-e2e.md`. Both transcripts recorded.
    - `grep -c "TOOLS_KEYS" scripts/check-foundation-guards.ts` reports at least 2 — the import and the floor.
    - `grep -L "^tools:" .claude/agents/*.md` prints nothing, so the real tree satisfies the new floor with no adapter edit.
    - `npx vitest run scripts/check-foundation-guards.test.ts` exits 0 with a case count exactly one greater than after Task 1.
    - Temporarily deleting the tools-key floor block, rebuilding and re-running makes the new case fail. Record the failing case name in the SUMMARY, then restore and rebuild.
    - `node scripts/check-foundation-guards.js` over the real tree exits 0.
    - `npm run build` then `npm run freshness` exits 0, and `git diff --stat .claude/agents/` is empty.
  </acceptance_criteria>
  <done>An adapter with no declared tools key is a named finding rather than a silent compliant reading, and the fixtures declare one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator body prose -> the capability-and-safety announcement a user reads | text that is commented out is not text the agent reads, but the bytes survive |
| adapter frontmatter -> the platform's tool grant | an omitted key is a maximal grant, not an absence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-34 | Spoofing | `guardWr05()` tier-beat check, `check-foundation-guards.ts:552-563` | high | mitigate | Beats are counted in comment-stripped, fence-stripped, whitespace-collapsed live text, so a commented or duplicated announcement cannot satisfy the claim. Named in the guard at `:429-432`; this plan makes the mitigation real. |
| T-27-100 | Elevation of Privilege | agent adapter with no `tools` key | high | mitigate | The floor fails red by name, because the platform grants the spawn tool by inheritance when the key is absent. |
| T-27-101 | Repudiation | the WR-05 PASS line | high | mitigate | The PASS line is extended to state the input shape it read, so the claim matches the check. |
| T-27-102 | Tampering | committed `scripts/check-foundation-guards.js` | high | mitigate | `npm run build` plus `npm run freshness` in the acceptance criteria. |
| T-27-103 | Denial of Service | fixture churn breaking unrelated cases | low | accept | The fixtures gain a line rather than losing one; the acceptance criteria pin that no existing assertion is deleted. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run build` then `npm run freshness` exits 0.
- `npx vitest run --exclude '**/scripts/e2e/**'` exits 0.
- `node scripts/check-foundation-guards.js` exits 0 over the real tree, WR-05 pass line intact.
- Both review reproductions — the comment-wrapped announcement and the deleted `tools` line — exit 1 where they previously exited 0. Four transcripts recorded in the SUMMARY.
- `git diff --stat .claude/agents/` is empty; the 7570-byte FAIL ceiling and 7165-byte WARN tier are untouched.
- `git status --porcelain` clean of stray mirrors.
</verification>

<success_criteria>
One predicate has one treatment: the tier beats are read exactly as `guard_adapter_body`'s positive
half reads its sentences. An agent adapter that declares no tool allow-list is a named finding. Every
pre-existing beat case still passes unmodified.
</success_criteria>

## Artifacts this phase produces

New symbols created by this plan — exclude these from drift verification, they do not exist at HEAD:

- The greater-than-one tier-beat finding string in `guardWr05()` in
  `scripts/check-foundation-guards.ts`.
- The absent-tools-key finding string in the `AGENT_ADAPTERS` floor loop.
- `TOOLS_KEYS` newly imported into `scripts/check-foundation-guards.ts` from `./frontmatter.js` (the
  symbol itself already exists in `scripts/frontmatter.ts`).
- Three new test cases in `scripts/check-foundation-guards.test.ts`: the comment-wrapped
  announcement, the duplicated beat, and the deleted `tools` line.
- A declared `tools` line added to the frontmatter emitted by `consistentMirror()` and
  `plantPlainAdapter()`.
- `stripHtmlComments` and `countOccurrences` are RELOCATED, not created — they exist at HEAD at
  `:676` and `:681`.
- No new files, no new npm scripts, no new CI steps, no new exit codes.

<output>
Create `.planning/phases/27-spawn-correctness-kit-set-authority/27-20-SUMMARY.md` when done.
</output>

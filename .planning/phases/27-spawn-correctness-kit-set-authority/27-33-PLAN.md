---
phase: 27-spawn-correctness-kit-set-authority
plan: 33
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/frontmatter.ts
  - scripts/frontmatter.js
  - scripts/frontmatter.test.ts
  - scripts/kit-model.ts
  - scripts/kit-model.js
  - scripts/kit-model.test.ts
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
autonomous: true
gap_closure: true
gap_round: 5
requirements: [KIT-02, KIT-03, SPAWN-04]
user_setup: []

estimate:
  tokens: 112000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-39, the delimiter region of scripts/frontmatter.ts has ONE normalization point and ONE declared whitespace class, and the complement of the delimiter test is no longer a silent success."
    - "Per D-43, the delimiter positions DECLARE THE ONE LEGAL SPELLING — the payload followed only by the declared whitespace class — and refuse everything else. A line that begins with the payload and is not exactly that is refused WHATEVER FOLLOWS IT: graphic, invisible, combining, unassigned, private-use, or another dash. Nothing is enumerated on the refusal side."
    - "Per D-43, the two prior formulations are both recorded as denylists that failed: D-39 refused a trim()-based alphabet and D-42 refused a wider general-category alphabet, and BOTH let their complement pass while their own text claimed the opposite polarity. Measured, D-42 leaves `----`, `--- foo`, a combining acute, a variation selector, an unassigned code point and a private-use code point as live silent successes carrying a real spawn grant."
    - "Per D-43, a second arm refuses residue that is invisible but LEADING rather than trailing, so a mark-prefixed or space-prefixed delimiter refuses rather than reaching the keyless success arm."
    - "Per D-39 point 1, a single leading U+FEFF is removed at the same expression that already normalizes CRLF, so a byte-order mark cannot reach any predicate; a SECOND leading mark is not stripped and reaches the refusal instead."
    - "Per D-39 point 5, the open/close asymmetry is dead: the identical byte that today fails OPEN at the opening position and fails CLOSED at the closing position with the misleading `opened and never closed` diagnosis now produces the SAME named refusal at both positions."
    - "Per D-39 point 4, a genuinely body-only document still reaches the keyless SUCCESS arm untouched, and the positive controls — a bare delimiter and a delimiter carrying trailing spaces or tabs — still open a frontmatter block."
    - "The sweep that pins the rule is NOT CIRCULAR: its corpus carries members drawn from the NEGATIVE SPACE of the rejected D-42 alphabet — marks, unassigned, private-use and surrogate code points, plus the payload variants `----` and `--- foo` — so it can fail on inputs no alphabet under test contains. A corpus generated from the alphabet it tests can only ever detect a narrowing, which is how D-42 would have shipped green."
    - "The mandatory false-red control derives its corpus from THE SAME COMPOSITION THE GUARD SCANS, never a second hand-listed directory set — that would be one predicate answered in two places, the class D-28, D-37 and D-40 each collapsed once already inside this phase and the class CR-03 itself belongs to."
    - "Because the guard's scan and the control's corpus are now THE SAME OBJECT, the composition carries its own CARDINALITY PIN — exactly 26 members, two-sided — plus PER-PART membership, so a part silently dropped during the relocation cannot pass. Set equality between the two consumers is documentation of intent, NOT a check: it compares an object with itself and can never fail, and an assertion that cannot fail must never be presented as one."
    - "Per D-19 and D-20, the relocation is pinned by derive-the-set-assert-the-count, the doctrine this repository already wrote for exactly this failure: a one-line slip dropping the standalone skills from the composition would otherwise leave the false-red control passing over a subset, set equality passing tautologically, the gate exiting 0, the packaging-template count unchanged and the whole suite green — while seven shipped skill adapters silently left the spawn-grant scan. That is CR-03's shape arriving through the move that closes CR-03, and it is the third recurrence of this shape in this phase."
    - "Per D-41 item 3, a grant enumeration carrying a nested parenthesis or a quote is REFUSED rather than returned short or altered, so keysGrantedAgentNames honors the contract its own doc block states."
    - "An end-to-end run of the committed check-foundation-guards.js over a hermetic mirror carrying a mark-prefixed rogue spawn grant on a SKILL adapter exits NON-ZERO naming the file and the reason."
    - "Adjacency edge: the opening test, the closing scan and the directive refusal all consult the one declared legal spelling; there is no second grammar left in the delimiter region for a fifth spelling to slip between."
    - "Empty edge: an empty document and a document of blank lines only still reach the keyless success arm, because neither begins with the payload at all."
    - "Ordering edge: the refusal fires on the FIRST offending position encountered and the reported code point is deterministic for a given input, so two runs over one tree produce byte-identical findings."
    - statement: "Claude Code's own frontmatter reader strips a leading byte-order mark before looking for the delimiter, so a mark-prefixed adapter is loaded rather than inert."
      verification: backstop
  artifacts:
    - "scripts/frontmatter.ts — the single BOM normalization point, the one declared legal delimiter spelling, the two refusal arms consulted by both delimiter positions, and the widened grant-enumeration refusal"
    - "scripts/frontmatter.js — the committed compiled twin, freshness-clean"
    - "scripts/frontmatter.test.ts — the named-spelling anchors, the non-circular negative-space sweep, the positive controls, and the set-equality false-red control"
    - "scripts/kit-model.ts — the packaging-template lister and the single exported spawn-grant scan composition both the guard and the false-red control consume"
    - "scripts/check-foundation-guards.ts — re-pointed to the single exported composition, its local composition deleted rather than left as a second opinion"
  key_links:
    - "scripts/frontmatter.ts parseFrontmatter -> the one declared legal spelling -> the opening test AND the closing scan"
    - "scripts/frontmatter.ts refusal arms -> the ok:false arm -> check-foundation-guards.ts guard_wr05's hand-written parse-failure branch -> a red gate"
    - "scripts/kit-model.ts spawn-grant scan composition -> check-foundation-guards.ts scan set AND scripts/frontmatter.test.ts false-red control (one composition, two consumers)"
    - "scripts/frontmatter.ts keysGrantedAgentNames refusal -> the KIT-03 oracle and coordinator-resolution-precheck's set equality"
  prohibitions:
    - "ARM 1 never enumerates — no list of code points, no character class, no general-category rule. It is `begins with the payload and is not legal`, full stop. The LEGAL side is stated and finite; everything else refuses. A predicate that names what is illegal is a denylist no matter how principled its alphabet looks, which is the single lesson D-39 and D-42 each cost a round to learn. This prohibition is scoped to arm 1 DELIBERATELY: arm 2 is about leading residue and unavoidably needs a class, so a blanket ban would make arm 2 unimplementable and would push an executor toward the narrowest class satisfying the letter — which is D-42's alphabet, under which a LEADING combining mark, unassigned or private-use code point falls back through to the keyless success arm."
    - "ARM 2's class is stated POSITIVELY — leading residue contains no character that renders a visible glyph of its own, i.e. the complement of {L, N, P, S} — rather than as a list of invisible things to catch, and its enforcement is the sweep, which requires the LEADING placement of every negative-space member to refuse. Do NOT reach for Unicode's own term `graphic`, which is {L, M, N, P, S, Zs} and therefore INCLUDES combining marks: an executor implementing that definition treats a leading U+0301 as graphic and fails to refuse it. The sweep catches that and turns it red, but the detour costs a cycle on a predicate this phase has already rewritten three times. The class is never narrowed to D-42's alphabet; the sweep is what makes that impossible to do quietly."
    - "The sweep's corpus is NEVER generated from the alphabet under test. It must contain members from the negative space of the rejected D-42 alphabet, or its completeness claim is circular and it would pass on the very regression it exists to catch."
    - "The false-red control NEVER hand-lists directories and NEVER uses a one-sided count floor; it consumes the single scan composition. What protects it from a later widening leaving it over a strict subset is the CARDINALITY PIN plus PER-PART SET EQUALITY against each lister — NOT set equality between the control's corpus and the guard's scan, which after the relocation compares one object with itself and can never fail."
    - "The keyless SUCCESS arm is never widened to cover a genuinely body-only document — turning a body-only file red would trade a silent success for a false red, which D-34 recorded as the worse of the two."
    - "The declared whitespace class is never loosened to make a case pass; the trailing space and trailing tab controls are the only accepted non-byte-exact spellings and they are unchanged."
    - "No second normalization is added. A leading byte-order mark is the ONLY byte this module removes, and it is removed exactly once, only at position zero."
    - "No new dependency is added; the dev-dependency set stays exactly typescript and vitest plus the type-only node types."
    - "No plan-27 file outside this plan's files_modified is edited. The seven clean requirements are verified and out of scope."
---

<objective>
Close **CR-01**, the fourth spelling of this module's founding failure — *"I could not read this"* printed as
*"this carries no grant"* — now on the **delimiter axis**, and **WR-02**, the grant enumeration that drops and
alters names on its own success arm.

Purpose: `scripts/frontmatter.ts:695` tests `lines[i].replace(/[ \t]+$/, "") !== "---"` and routes the whole
complement of that test into `{ ok: true, value: new Map() }` — a result byte-identical to a body-only file.
`flattenBlock` already refuses on every one of ITS complements, so **this region is the module's last
silent-success arm** — a bounded fix, not a sweep.

Output: one normalization point, one declared legal delimiter spelling consulted by both positions, a refusal
that enumerates nothing, a non-circular property sweep, a set-equality false-red control, and a grant
enumeration that refuses what it cannot vouch for.

## Decisions this plan implements

- **D-39** — the delimiter posture: one normalization point, one declared whitespace class, the complement
  refuses by name, a body-only document untouched, the open/close asymmetry dead. Reversibility:
  **reversible** — one predicate in one module, with a measured-zero false-red cost.
- **D-43** — the actual complement, superseding D-39 point 3's and D-42's alphabets. Reversibility:
  **reversible**.
- **D-41 item 3** — `keysGrantedAgentNames` refuses a nested parenthesis or a quote inside a grant
  enumeration. Reversibility: **reversible**.

## THE THING THAT KEPT GOING WRONG — READ THIS BEFORE WRITING THE PREDICATE

Three formulations of this fix have now been written, and **the first two were both denylists that said they
were not**:

| Formulation | What it ENUMERATES | Verdict |
|---|---|---|
| D-39 point 3 — `near = line.trim() === "---"` | the illegal set, via `trim()`'s WhiteSpace alphabet | **denylist**; misses 458 of 506 swept positions |
| D-42 — `near` = residue entirely in `[\s\p{Cf}\p{Cc}]` | the illegal set, via a wider general-category alphabet | **denylist**; misses marks, unassigned, private-use, `----`, `--- foo` |
| **D-43 — the payload followed ONLY by `[ \t]` is legal; everything else refuses** | **the LEGAL set** | **allowlist** — the polarity all three claimed |

Both prior versions state D-30's polarity in their own text and implement the opposite. **Before accepting any
predicate here as "derived" or "inverted", ask which set it ENUMERATES.** If it enumerates the bad, it is a
denylist however principled its alphabet looks.

Measured by the orchestrator against the committed `scripts/frontmatter.js`, every row carrying a live
`Agent(grugops-orchestrator)` grant, and re-measured here against the D-43 rule:

| opening line | today | in D-42's alphabet | after D-42 | after D-43 |
|---|---|---|---|---|
| `---` (control) | granted | n/a | granted | **granted** |
| `---` + trailing space / tab (controls) | granted | n/a | granted | **granted** |
| `---` + U+FE0F VARIATION SELECTOR-16 (`Mn`) | SILENT no-grant | no | **still silent** | **refused** |
| `---` + U+0301 COMBINING ACUTE (`Mn`) | SILENT no-grant | no | **still silent** | **refused** |
| `---` + U+0378 unassigned (`Cn`) | SILENT no-grant | no | **still silent** | **refused** |
| `---` + U+E000 private use (`Co`) | SILENT no-grant | no | **still silent** | **refused** |
| `----` | SILENT no-grant | no | **still silent** | **refused** |
| `--- foo` | SILENT no-grant | no | **still silent** | **refused** |
| `---` + U+E0020 TAG SPACE (`Cf`) | SILENT no-grant | yes | refused | **refused** |
| `---` + U+200B ZWSP | SILENT no-grant | yes | refused | **refused** |
| leading space, leading BOM | SILENT no-grant | yes | refused | **refused** |

`----` and `--- foo` are not exotic; common frontmatter readers accept both, and no prior decision or review
in this phase named either.

**False-red cost, re-measured here: ZERO, twice over.** All **33** files across every real spawn-grant scan
surface open with a byte-exact `---` on line 1, and no line inside any of their frontmatter blocks refuses.
Widened to **all 1115 tracked markdown files in the repository**, the count of head lines that would refuse is
also **0**. The strict rule costs this repository nothing.

## The rule, stated once

```
payload            = "---" at the opening position; "---" or "..." at the closing position
declared class     = [ \t]        the ONE class, and the ONLY thing permitted after the payload
LEGAL              = line begins with the payload AND everything after it is in the declared class
refusal arm 1      = line begins with the payload AND is not LEGAL      -> refuse, whatever follows
refusal arm 2      = leading residue that is entirely invisible, then a LEGAL delimiter -> refuse
everything else    = not a delimiter at all -> the keyless SUCCESS arm, untouched
```

The invisible test appears in arm 2 ONLY, to describe *leading* residue. It never decides legality — that is
the inversion D-42 got backwards.

## Artifacts this phase produces (new symbols — exclude from drift verification)

| Symbol | File | Kind |
|---|---|---|
| the declared delimiter whitespace class | `scripts/frontmatter.ts` | new module-private constant |
| the legal-delimiter predicate | `scripts/frontmatter.ts` | new module-private helper |
| the two refusal-arm predicates | `scripts/frontmatter.ts` | new module-private helpers |
| the packaging-template lister | `scripts/kit-model.ts` | new exported function taking an explicit root |
| the spawn-grant scan composition | `scripts/kit-model.ts` | new exported function — the ONE composition |
| the composition's exact cardinality | `scripts/kit-model.ts` | new exported constant, value 26 (17 + 7 + 2), raised to 33 by 27-34 |
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW-GAPS-4.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-29-SUMMARY.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-30-SUMMARY.md
@scripts/frontmatter.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Declare the ONE legal delimiter spelling and refuse everything else at BOTH positions — proven end-to-end from the parser to the gate's exit code (D-39, D-43)</name>

  <read_first>
    - `scripts/frontmatter.ts:665-716` — `parseFrontmatter`, the three-outcome partition comment, the D-34
      directive refusal at `:689-695`, the opening test at `:695` and the closing scan at `:700-706`. The
      partition comment is a contract with this task: read what it claims about the second outcome before
      changing where the boundary sits.
    - `scripts/frontmatter.ts:405-500` — the `DQ_ESCAPE_ALLOWLIST` block and `unquoteChecked`. This is D-30's
      allowlist, the ONLY fix in this module that held, and the shape this task copies: state the legal set,
      refuse the complement. Read how it is written, not just what it does.
    - `scripts/frontmatter.ts` module header — the founding argument stated three times. The new refusal reason
      must speak in that voice.
    - `scripts/frontmatter.ts:201` `stripFencedBlocks` and the `excerpt` helper — both already exist and are
      reused, never re-written.
    - `scripts/check-foundation-guards.ts:610-621` — the hand-written parse-failure branch in `guard_wr05`. It
      is what converts this task's refusal into a red gate and needs NO edit; confirm that by reading it.
    - `scripts/check-foundation-guards.test.ts:1-60` — the hermetic `CHECK_ROOT` plant-and-run harness that
      spawns the committed `.js`. The end-to-end transcript uses that shape.
  </read_first>

  <files>scripts/frontmatter.ts, scripts/frontmatter.js, scripts/frontmatter.test.ts</files>

  <behavior>
    - A line that begins with the payload and carries only the declared whitespace class after it opens (or
      closes) a frontmatter block. That is the complete legal set.
    - A line that begins with the payload and is anything else is refused, whatever follows it — another dash,
      a graphic character, a combining mark, an unassigned code point, a private-use code point, or an
      invisible one. The reason names the first offending code point and states that the line is refused as
      unreadable rather than read as an absence of keys.
    - A line whose leading residue is entirely invisible and which then carries a legal delimiter is refused by
      the second arm, with the same shape of reason.
    - The same line at the CLOSING position produces the same named refusal, not the `opened and never closed`
      diagnosis and not a silent continuation of the scan.
    - A document prefixed with exactly one byte-order mark parses identically to the same document without it;
      a document with two refuses.
    - A document that does not begin with the payload at all still returns the keyless success arm, as do an
      empty document and a document of blank lines only.
    - Run against the committed `.js` on a hermetic mirror, a mark-prefixed rogue spawn grant planted on a
      skill adapter makes the whole foundation gate exit non-zero naming the file and the reason.
  </behavior>

  <action>
    Capture the RED transcript FIRST, against the committed `scripts/frontmatter.js` as it stands: run
    `hasSpawnGrant` over the plain control and over every offending row in the table above — including the four
    D-42 would have missed and the two payload variants — and record that the control returns a granted verdict
    while every offending row returns an ungranted verdict with no refusal. Then plant a mark-prefixed rogue
    grant into `.claude/skills/grugops-map/SKILL.md` on a hermetic `CHECK_ROOT` mirror of HEAD, run the
    committed `scripts/check-foundation-guards.js`, and record that it reports every check passing at exit
    zero. That pair is the RED half and it is required before any edit.

    Then edit `scripts/frontmatter.ts`.

    Add the single normalization, at the expression on line 684 that already normalizes CRLF and nowhere else:
    remove a leading `U+FEFF` before the CRLF replacement. Exactly one, exactly at position zero. Record beside
    it that this is the ONLY byte this module removes, and that a SECOND leading mark is deliberately NOT
    stripped — it falls to the refusal below, because "strip every mark" would be a decode this module does not
    perform.

    Declare the delimiter whitespace class ONCE as a module-private constant holding the `[ \t]` rule both
    positions use today. Write the legal-delimiter predicate against it: the line begins with the payload and
    everything after the payload is in the declared class. Write the two refusal-arm predicates: one for a line
    that begins with the payload without being legal, one for leading residue that is entirely invisible in
    front of a legal delimiter. Replace the opening test and the closing scan so both consult those predicates
    and neither carries its own inline whitespace expression.

    **Enumerate nothing on the refusal side.** Arm 1 must not consult any character class at all — it is
    "begins with the payload and is not legal", full stop. The invisible test belongs to arm 2 only, where it
    describes *leading* residue, and it must never be used to decide whether a trailing byte is acceptable.
    Record that constraint in place, with the reason: D-39 and D-42 each wrote this predicate as a refusal
    alphabet and each shipped a live silent-success class, and the lesson is that a predicate naming what is
    illegal is a denylist however principled its alphabet looks.

    In the refusal arm, name the first offending code point — the first code point after the payload that is
    outside the declared class for arm 1, and the first code point of the leading residue for arm 2 — formatted
    as `U+` followed by four or more uppercase hexadecimal digits. Name the position (opening or closing) and
    the payload token, quote the offending line through the existing excerpt helper, and state that the line is
    refused as unreadable rather than read as an absence of keys.

    Amend the three-outcome partition comment at `:671-682` so it describes where the boundary now sits. The
    partition still has three outcomes and no fourth state; what moved is that a line beginning with the
    payload but not legal, at either delimiter position, now sits in the third outcome instead of the second.
    State explicitly that a document not beginning with the payload is still the second outcome and that
    turning a body-only file red would trade a silent success for a false red — the worse of the two, as the
    existing comment already argues.

    Add to `scripts/frontmatter.test.ts` the named anchors: every offending row from the table, at the opening
    position, asserting a refusal whose reason names the expected code point where the row has one; the same
    rows at the closing position, asserting the same named refusal rather than the unterminated-block
    diagnosis; and the positive controls asserting an opened block with its keys intact. Add the
    mark-normalization cases: one leading mark parses deeply equal to the same document without it, two leading
    marks refuse.

    Rebuild the committed compiled output, then capture the GREEN half: re-run the same parser probes and the
    same hermetic-mirror gate run against the rebuilt committed `.js`, and record that every row now refuses by
    name and that the gate now exits non-zero naming the planted file.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; node scripts/freshness.js &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' scripts/frontmatter.test.ts &amp;&amp; node scripts/check-foundation-guards.js</automated>
  </verify>

  <acceptance_criteria>
    - A captured RED-before transcript, against the committed `scripts/frontmatter.js` BEFORE the edit, shows the plain control granted and EVERY offending row from the table ungranted with no refusal — including `----`, `--- foo`, the combining acute, the variation selector, the unassigned code point and the private-use code point.
    - A captured RED-before transcript, against the committed `scripts/check-foundation-guards.js` on a hermetic `CHECK_ROOT` mirror with a mark-prefixed rogue grant planted on `.claude/skills/grugops-map/SKILL.md`, shows the gate reporting all checks passed at exit 0.
    - A captured GREEN-after transcript, against the REBUILT committed `.js`, shows every row refused with the reason naming the expected code point, and the same hermetic mirror now exiting non-zero with a finding naming the planted file.
    - Refusal arm 1 consults NO character class: a reviewer reading it sees "begins with the payload and is not legal" and nothing else, and the recorded constraint beside it names the denylist trap by reference to D-39 and D-42.
    - The mark is removed in exactly one expression and nowhere else in code: `grep -v '^[[:space:]]*//' scripts/frontmatter.ts | grep -c 'uFEFF'` is exactly 1 (comment lines are filtered out, so rationale prose can neither inflate nor satisfy the count).
    - The declared whitespace class is declared exactly once as a module-private constant, and both delimiter positions reference it by name rather than carrying an inline expression.
    - The positive controls pass: a bare delimiter opens a block, a delimiter with trailing spaces opens a block, and a delimiter with a trailing tab opens a block, each returning its keys.
    - A body-only document, an empty document and a document of blank lines only each still return the keyless success arm.
    - `node scripts/freshness.js` exits 0 and `npx vitest run --exclude '**/scripts/e2e/**'` reports zero failures at or above the 1015-passing baseline.
    - `git status --porcelain` shows the live checkout clean apart from this task's `<files>`; the hermetic mirror was a throwaway and the live tree was never planted into.
  </acceptance_criteria>

  <done>
    The delimiter region declares its one legal spelling and refuses everything else at both positions, the
    refusal side enumerates nothing, and a mark-prefixed rogue spawn grant on a skill adapter now turns the
    whole gate red instead of printing a clean pass — proven by transcript against the committed `.js` at both
    ends.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the rule with a NON-CIRCULAR sweep drawn from the negative space, and a false-red control that consumes the one scan composition (D-43)</name>

  <read_first>
    - `scripts/frontmatter.test.ts` — the D-30 exhaustive escape sweep added in round 4. This task is the same
      shape one axis over: read how that sweep enumerates its corpus and reports a failing member.
    - `scripts/kit-model.ts:36-80` and `:121-146` — the module header's charter, the fixed-literal subpath
      block, the named-throw read helper and the refuse-empty helper. The new lister and composition are built
      from these.
    - `scripts/check-foundation-guards.ts:333-346` (`readPackagingDir`) and `:451-458` (the packaging shape rule
      and `SPAWN_GRANT_SCAN`). This is the composition being MOVED, not duplicated — read what it excludes and
      why (`adapters.md` is out by the shape rule, deliberately) so the move preserves it exactly.
    - Task 1's new predicates and constants, as landed.
  </read_first>

  <files>scripts/frontmatter.test.ts, scripts/kit-model.ts, scripts/kit-model.js, scripts/kit-model.test.ts, scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js</files>

  <behavior>
    - The sweep's corpus contains members the rejected alphabet does not: combining marks, unassigned code
      points, private-use code points, surrogates, and the payload variants that carry no unusual code point at
      all.
    - Every corpus member placed after the payload refuses, except the declared-class members, which open a
      block. The same holds at the closing position and for the leading placement.
    - The sweep reports the offending code point when a member fails, so a future failure names which member
      regressed.
    - One exported composition answers "what does the spawn-grant scan cover", and both the guard and the
      false-red control read it. The guard holds no composition of its own.
    - The false-red control parses exactly the composition's members and refuses none of them, asserted by set
      equality between what it scanned and what the composition returned.
  </behavior>

  <action>
    First move the composition, so the control has one authority to consume.

    In `scripts/kit-model.ts`, add a packaging-template lister taking an explicit root, carrying the shape rule
    the guard uses today verbatim — the two adapter-frontmatter template suffixes — and passing its result
    through the existing refuse-empty helper. Carry across the recorded reason that the prose file about
    adapters is deliberately excluded BY THE SHAPE RULE rather than by omission from a list, so it cannot
    silently drift back in. Then add the exported spawn-grant scan composition: agent adapters, standalone skill
    adapters and packaging templates, each prefixed back to its repository-relative shape, sorted. Record that
    this composition exists so the guard and its false-red control cannot answer "what is scanned" differently —
    one predicate, one place, which is the rule this phase has now collapsed a duplicate for three times.

    **Give the composition a cardinality pin in the same commit that creates it, per D-19 and D-20.** Export an
    exact expected total of 26 — 17 agent adapters, 7 standalone skill adapters, 2 packaging templates — and
    enforce it two-sided in the counts guard beside the cardinalities already there, with a message naming the
    derived total, the expected total and the per-part breakdown. Record why the pin is mandatory rather than
    nice to have: once the guard and the false-red control read the same object, set equality between them
    compares an object with itself and can never fail, so the ONLY thing that can catch a part dropped during
    this relocation is a count. A one-line slip losing the standalone skills would otherwise leave the control
    passing over a subset, the gate exiting 0, the packaging-template count unchanged and the suite green,
    while seven shipped adapters silently left the scan — CR-03's own shape, arriving through the move that
    closes CR-03.

    Assert PER-PART membership as well as the total, so a swap between parts cannot net out to the right
    number: the composition's members that live under the agent directory are exactly the agent lister's set,
    those under the standalone skill directory are exactly the skill lister's set, and those under the
    packaging directory are exactly the packaging lister's set.

    In `scripts/check-foundation-guards.ts`, delete the local composition and the local packaging read and
    consume the exported composition instead. Delete rather than keep as a second opinion: a weaker duplicate
    that still votes is worse than none, as this file's own guard_wr05 header already argues.

    **Route the packaging call site through the guard's existing `derive()` wrapper** (`:358`), exactly as the
    agent and skill derivations already are. This is load-bearing and easy to miss: the read being replaced
    returns an empty list on an unreadable directory ON PURPOSE, so the wrapper records the thrown message and
    the run continues with an empty list that the count floors then NAME — the discipline this file argues for
    at `:352-360`, that aborting here would print one line and silently skip six unrelated guards. The
    relocated lister goes through `refuseEmpty` and THROWS (D-21 tier 1), so a call site not wrapped in
    `derive()` converts a named red into an unhandled exception and regresses that discipline. Fail-closed
    direction, but still a regression, and it must not ship.

    Then write the sweep in `scripts/frontmatter.test.ts`.

    Build the corpus from FOUR sources, and state in the case why each is present. First, a bounded
    deterministic sample of the NEGATIVE SPACE of the rejected D-42 alphabet — combining marks, unassigned,
    private-use and surrogate code points — taken by a fixed stride so the sample is identical on every run and
    on every platform, with at least two hundred members from each class. Second, the invisible code points the
    prior formulations did cover, so the sweep is a superset of what D-42 would have swept. Third, the payload
    variants that carry no unusual code point at all: an extra dash, and the payload followed by ordinary text.
    Fourth, the positive controls. Assert the corpus SIZE as a number, following this repository's
    derive-the-set-assert-the-count rule.

    Record beside the corpus why it is built this way: a sweep whose members are generated from the alphabet
    under test is tautological — it can catch a narrowing and is structurally incapable of failing on anything
    outside the alphabet, which is exactly how D-42 would have shipped green over a live combining-mark bypass.
    The negative-space members are what make the completeness claim non-circular.

    For each member, construct the leading and trailing variants at the opening position and at the closing
    position, and assert the partition: the declared-class members in the trailing position open a block, and
    every other combination refuses. On failure, report the offending code point so the message names which
    member regressed.

    Rewrite the false-red control against the exported composition. Import it, parse exactly the members it
    returns, and assert NONE refuses — over block lines as well as head lines, since the closing scan refuses on
    the same rule. Record why the corpus is the composition rather than a directory list: a hand-listed set here
    would be the guard's scan answered a second time.

    Then assert what actually can fail. Assert the composition's CARDINALITY as exactly 26 and its PER-PART
    membership against the three listers. Do NOT present set equality between the control's corpus and the
    guard's scan as a check — after the move they are the same object, so that comparison is a tautology. If it
    is written at all, write it as documentation of intent and label it in the case as documentation, never as
    an assertion that could catch anything.

    Add the authority's own cases to `scripts/kit-model.test.ts`: the packaging lister returns the derived set
    over a fixture, throws naming the directory when it cannot be read, and throws naming the directory when the
    filtered result is empty; the composition equals the union of its three parts over a fixture; and the
    composition's cardinality over the real repository root is exactly 26, failing in BOTH directions.

    Rebuild the committed compiled output.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; node scripts/freshness.js &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' scripts/frontmatter.test.ts scripts/kit-model.test.ts scripts/check-foundation-guards.test.ts &amp;&amp; node scripts/check-foundation-guards.js</automated>
  </verify>

  <acceptance_criteria>
    - The sweep's corpus asserts its size as a number and contains at least 200 members from each of the four negative-space classes, plus the two payload variants and the positive controls.
    - The sweep covers BOTH delimiter positions and BOTH the leading and trailing placement for every member, and passes.
    - A captured transcript shows the sweep FAILING and NAMING a specific code point when the rule is narrowed in a scratch build to D-39's trim-based class, and passing when reverted.
    - A second captured transcript shows the sweep FAILING and NAMING a specific code point when the rule is narrowed in a scratch build to **D-42's own alphabet** — the regression that would otherwise re-ship this exact round. A sweep that cannot fail on this is circular and does not satisfy this criterion.
    - The composition's cardinality is asserted as exactly **26** and fails in BOTH directions: captured transcripts show the counts guard failing red on a mirror with one member removed AND on a mirror with one added.
    - PER-PART membership is asserted as **SET equality against each lister, never as a count** — the composition's members under the agent directory are exactly the agent lister's set, those under the standalone skill directory exactly the skill lister's set, those under the packaging directory exactly the packaging lister's set. (The parts are 17 / 7 / 2 today, but **an integer comparison does NOT satisfy this criterion**: three count checks pass while a decoy under `.claude/agents/` displaces a real adapter, which is a within-part substitution the set form catches and the count form misses.) A captured transcript shows the per-part assertion failing when the composition is edited in a scratch build to drop `.claude/skills` and add two decoy members, a change that keeps the total at 26.
    - The false-red control refuses nothing, over head lines AND block lines; the words "at least" do not appear in its assertions.
    - `grep -v '^[[:space:]]*//' scripts/check-foundation-guards.ts | grep -c 'readPackagingDir'` is 0 — the local composition was deleted, not kept as a second opinion.
    - No case presents set equality between the guard's scan and the control's corpus as a check. After the move they are the same object; if the comparison appears at all it is labelled in the case as documentation of intent, because an assertion that cannot fail must not be presented as one.
    - The packaging call site goes through the guard's `derive()` wrapper: a captured transcript shows the gate on a mirror with an unreadable packaging directory printing a NAMED count-floor finding and running its remaining guards, rather than terminating on an unhandled exception.
    - `node scripts/check-foundation-guards.js` exits 0 on the live tree and its WR-05 pass line reports **both** the same non-coordinator adapter count **and** the same packaging-template count it reported before the move — the adapter half is what a dropped standalone-skill part would change, and pinning only the packaging half is what would have let this slip through.
    - `node scripts/freshness.js` exits 0 and `npx vitest run --exclude '**/scripts/e2e/**'` reports zero failures at or above the 1015-passing baseline.
  </acceptance_criteria>

  <done>
    The delimiter rule is pinned by a sweep that can fail on inputs no alphabet under test contains, and the
    false-red control consumes the single scan composition rather than restating it one indirection down, and
    what proves it still covers the whole scan is the cardinality pin plus per-part set equality against each
    lister — not equality between the control's corpus and the guard's scan, which is now one object.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refuse a grant enumeration this module cannot vouch for, instead of returning it short or altered (D-41 item 3)</name>

  <read_first>
    - `scripts/frontmatter.ts:744-778` — `keysGrantedAgentNames` and the doc block above it. The contract is
      already written there and already promises what this task delivers; read exactly what it claims before
      changing the code beneath it.
    - `scripts/frontmatter.ts:730` — the scoped-grant expression whose character class stops at the first
      closing parenthesis. This is the truncation half of the defect.
    - `scripts/check-foundation-guards.ts:251` and `scripts/coordinator-resolution-precheck.ts:402` — the two
      consumers. Both already branch on the failure arm by hand, so confirm by reading that neither needs an
      edit; if either does, that is a finding for the summary, not a silent fix here.
    - The existing D-32 escape-refusal cases in `scripts/frontmatter.test.ts` — the new cases sit beside them
      and follow their assertion shape.
  </read_first>

  <files>scripts/frontmatter.ts, scripts/frontmatter.js, scripts/frontmatter.test.ts</files>

  <behavior>
    - A grant enumeration containing a nested opening parenthesis is refused, and the reason states that the
      capture truncated and the tail of the enumeration would have been discarded.
    - A grant enumeration containing a quote character is refused, and the reason states that the comma split is
      not the separator the document expresses.
    - The refusal reason keeps this module's established closing clause — that a name is never silently dropped
      or altered — so it reads as the same argument the escape refusal already makes.
    - Every enumeration the generator actually emits still returns the success arm with its names de-duplicated
      and sorted.
    - An unscoped grant still enumerates nothing and still returns the success arm with an empty list.
  </behavior>

  <action>
    Capture the RED transcript first against the committed `scripts/frontmatter.js`: call `grantedAgentNames` on
    an enumeration carrying a nested parenthesis and on one carrying a quoted name containing a comma, and
    record that the first returns a success arm with a name dropped and the second returns a success arm with
    one name split into two altered names.

    Then, inside `keysGrantedAgentNames`, examine each captured enumeration BEFORE splitting it. If the capture
    carries a nested opening parenthesis or any quote character, return the failure arm rather than splitting.
    Write the reason to state which of the two shapes was found, to quote the offending enumeration through the
    existing excerpt helper, and to close with the module's established clause that a name is never silently
    dropped or altered.

    Record beside the check why the enumeration is refused rather than parsed correctly: a quote-aware,
    nesting-aware split would be a second grammar for a value the platform's own loader reads with a first, and
    this module's rule is one authority per predicate. Refusing is the answer that cannot be wrong; parsing
    better is the answer that can.

    Do not touch the scoped-grant expression itself and do not widen it to consume nested parentheses. Its
    truncation is what the new check DETECTS; making the expression cleverer would move the defect rather than
    close it, and would put a second grammar back where this phase just deleted one.

    Add the two cases beside the existing escape-refusal cases, each asserting the failure arm and a reason
    naming its shape. Add the false-red control beside them: the real coordinator's own enumeration — the
    seventeen-name grant in `.claude/agents/grugops-orchestrator.md` — still returns the success arm with its
    full name list, asserted by count as well as by membership.

    Rebuild the committed compiled output and capture the GREEN transcript against it.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; node scripts/freshness.js &amp;&amp; npx vitest run --exclude '**/scripts/e2e/**' scripts/frontmatter.test.ts &amp;&amp; node scripts/check-foundation-guards.js &amp;&amp; node scripts/coordinator-resolution-precheck.js</automated>
  </verify>

  <acceptance_criteria>
    - A captured RED-before transcript against the committed `.js` shows the nested-parenthesis input returning a success arm with a name missing, and the quoted-comma input returning a success arm with three altered names.
    - A captured GREEN-after transcript against the rebuilt committed `.js` shows both inputs returning the failure arm with a reason naming the shape found.
    - The refusal reason contains this module's established closing clause about a name never being silently dropped or altered.
    - The real coordinator's enumeration still returns the success arm, asserted by count and by membership.
    - An unscoped grant still returns the success arm with an empty list.
    - `grep -c 'SCOPED_GRANT' scripts/frontmatter.ts` is 3 — the expression is declared once and used twice, unchanged from its pre-task value.
    - `node scripts/coordinator-resolution-precheck.js` still exits 0 and `node scripts/check-foundation-guards.js` still exits 0 on the live tree.
    - `node scripts/freshness.js` exits 0 and `npx vitest run --exclude '**/scripts/e2e/**'` reports zero failures.
  </acceptance_criteria>

  <done>
    `keysGrantedAgentNames` honors the contract written six lines above it: it returns a name list it can vouch
    for or it refuses, and no set equality is ever computed over a set the document does not express.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter file bytes → the spawn-grant predicate | Untrusted or accidentally-encoded file content crosses into a security verdict; a mark written by a Windows editor and a four-dash rule written by hand are both ordinary members of this input class |
| the parser's result arm → every guard and oracle | A keyless success and a refusal are different facts; conflating them converts an unreadable file into a clean bill of health |
| grant enumeration text → a set equality | A dropped or altered name makes a closure equality hold over a set the document does not express |
| the scan composition → the control that vouches for it | A control restating the scan one indirection down can prove safety over a set the guard no longer scans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-CR01-01 | Spoofing | `parseFrontmatter` delimiter region | critical | mitigate | The delimiter positions declare the one legal spelling and refuse everything else at both positions, so no spelling — invisible, combining, unassigned, private-use or plain — can present itself as a file carrying no grant |
| T-27-CR01-02 | Elevation of Privilege | skill adapters reached through `guard_wr05` | critical | mitigate | The skill surface has no `name` floor and no `tools` floor, so the parser refusal is its only defense; the end-to-end tracer proves the gate exits non-zero on a planted mark-prefixed grant there |
| T-27-D43-01 | Tampering | the refusal predicate's polarity | critical | mitigate | Arm 1 consults no character class at all; the legal set is stated and finite. The prohibition names the denylist trap by reference to the two formulations that fell into it |
| T-27-D43-02 | Repudiation | the property sweep's completeness claim | high | mitigate | The corpus draws from the negative space of the rejected alphabet, and a transcript proves the sweep fails when narrowed to D-42's own alphabet — a circular sweep cannot satisfy that criterion |
| T-27-CR01-03 | Repudiation | the `opened and never closed` diagnosis | medium | mitigate | Open and close reach the same named refusal, so the reported reason names the actual offending byte |
| T-27-D43-03 | Tampering | the relocated scan composition | critical | mitigate | An exact two-sided cardinality of 26 plus per-part membership against the three listers, per D-19 and D-20. Once the guard and the control read one object, set equality between them is a tautology and a count is the only thing that can catch a part dropped in the move — CR-03's own shape arriving through the fix for CR-03 |
| T-27-D43-04 | Denial of Service | the relocated packaging lister's throw | medium | mitigate | The call site goes through the guard's `derive()` wrapper, so an unreadable packaging directory stays a named count-floor finding instead of becoming an unhandled exception that skips six unrelated guards |
| T-27-D39-02 | Denial of Service | the strict rule failing red on correct content | medium | mitigate | Re-measured zero across all 33 scan surfaces (head and block lines) and across all 1115 tracked markdown files; the control cannot pass over a subset because the composition carries an exact two-sided cardinality pin plus per-part SET equality against each lister (see T-27-D43-03) — NOT because the control's corpus equals the guard's scan, which after the relocation is one object comparing with itself |
| T-27-WR02-04 | Tampering | `keysGrantedAgentNames` success arm | high | mitigate | A nested parenthesis or a quote refuses rather than returning a short or altered list |
| T-27-BOM-01 | Spoofing | the single leading mark normalization | low | accept | A second leading mark is deliberately not stripped and falls to the refusal; stripping every mark would be a decode this module does not perform |
| T-27-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO package-manager install tasks in this plan; the dev-dependency fence is asserted by an unchanged `package.json`, so no legitimacy checkpoint is reachable |
</threat_model>

<verification>
- `npm run build && node scripts/freshness.js` exits 0 with all committed `.js` fresh.
- `npx vitest run --exclude '**/scripts/e2e/**'` reports zero failures at or above the 1015-passing / 2-skipped baseline across 35 files. Never run bare `npm test` — it triggers a live CLI end-to-end lane that spends tokens and can hang.
- `node scripts/check-foundation-guards.js` exits 0 on the live tree, and `node scripts/coordinator-resolution-precheck.js`, `node scripts/check-kit-refs.js` and `node scripts/validate-agent-factory.js` each still exit 0 — the composition move must not disturb a currently-clean consumer.
- The live kit is intact at 17 agent adapters, 7 standalone skills and 7 plugin skills.
- `git diff package.json` is empty.
- Every plant ran against a hermetic throwaway mirror; `git status --porcelain` shows no unexpected live-tree change.
- All RED-before and GREEN-after transcripts are recorded in the plan summary, each run against the committed `.js` rather than the TypeScript source.
- **Build serialization:** this plan is the only one running in wave 1. `npm run build` is a whole-project emit that writes every committed `.js`, so plans 27-34 and 27-35 are sequenced behind it rather than run alongside; a concurrent emit could tear a file that then gets committed.
</verification>

<success_criteria>
- The delimiter positions declare one legal spelling and refuse everything else; the refusal side enumerates nothing.
- Every row in the ratified table refuses, including the six D-42 would have missed, and the positive controls still open a block.
- The sweep draws from the negative space and is proven to fail when narrowed to either rejected alphabet.
- The false-red control consumes the one scan composition and refuses nothing over the real corpus, head lines and block lines alike.
- The relocated composition carries an exact two-sided cardinality of 26 and per-part membership against the three listers, so nothing can leave the spawn-grant scan silently during the move.
- A mark-prefixed rogue spawn grant on a skill adapter turns the foundation gate red, proven end-to-end against the committed `.js`.
- A grant enumeration carrying a nested parenthesis or a quote refuses rather than returning a short or altered list.
- KIT-03's CR-01 and WR-02 items are closed; SPAWN-04's parser-level bypass is closed.
- `package.json` is byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/27-spawn-correctness-kit-set-authority/27-33-SUMMARY.md` when done, including the
task-1 parser and gate RED/GREEN transcripts, the task-2 two narrowed-alphabet failure transcripts and the
false-red set-equality result, and the task-3 enumeration RED/GREEN transcripts.
</output>

# Phase 27: Spawn Correctness & Kit-Set Authority - Context

**Gathered:** 2026-07-28
**Status:** Ready for planning

<domain>
## Phase Boundary

Role agents actually execute in their own sessions on Claude Code — and every guard and validator scan set is derived from the filesystem **before** the 17 new adapter files exist, so they land inside the guards rather than outside them.

Requirements: KIT-01, KIT-02, KIT-03, SPAWN-01..07 (10 total).

**Load-bearing ordering inside the phase** (from ROADMAP.md, unchanged): kit-set authority + referential-integrity oracle land **first**; the `orchestrator.md` trim lands **before** any spawn text is touched; the 17 adapters are generated **last**. Reversing this reproduces the exact failure this phase exists to fix, with new names.

**Verified tree state at discussion time** (2026-07-28) — the baseline the oracle must fail RED against:

| Thing | Claimed | On disk |
|---|---|---|
| `agent-factory/roles/` | 17 roles | 17 (+ `_role-switch-protocol.md`, `kind: protocol`, underscore-prefixed) |
| `agent-factory/workflows/` | 19 | 19 |
| `.claude/agents/` | — | **1** (`grugops-orchestrator.md`) |
| Coordinator `Agent(...)` grant | 7 names | **0 of 7** resolve to a file |
| `orchestrator.md` | ≤ 7570B FAIL / 7165B WARN | **7562B — WARN already breached, 8B from FAIL** |
| Handoff prose | grep-to-zero (v2.0 MIGR-02) | live at `.claude/agents/grugops-orchestrator.md:25` |

**Role tier split** (already in role frontmatter, no new authoring needed): 12 `tier: core`, 5 `tier: enterprise` (compliance-officer, factory-coach, incident-responder, installer, release-manager).

</domain>

<decisions>
## Implementation Decisions

### Coordinator Entry & Main-Thread Wiring (SPAWN-03, SPAWN-04)

**Third stacked cause found during discussion, not in the roadmap text:** `.claude/skills/grugops/SKILL.md`'s `allowed-tools` is `Read, Write, Bash, Glob, Grep` — **no `Agent`**. So `/grug`, the brand's headline entry ("a user types `/grug`"), runs in the main thread and *structurally cannot spawn* even once the adapters exist. Any fix that only works via `claude --agent` repairs the mechanism and breaks the brand promise.

> **⚠ REVISED 2026-07-28 — the premise above is factually wrong; kept for the audit trail.** `[VERIFIED: code.claude.com/docs/en/skills]` `allowed-tools` is a per-turn **permission pre-approval**, not a capability restriction — "It does not restrict which tools are available: every tool remains callable." The restricting field is `disallowed-tools`, which grugops does not use. `/grug` runs in a default main-thread session that **already has `Agent`**, so it *can* spawn — unscoped. The only reason no role agent ever ran is that 16 of 17 adapter files do not exist and all 7 granted names resolve to nothing; **SPAWN-01 alone very likely fixes the reported defect on the `/grug` path.**
>
> Two corollaries the planner must respect: (a) adding `Agent` to the skill's `allowed-tools` is a **capability no-op** *and* trips `guard_wr05` (`check-foundation-guards.ts:196` fails any non-`coordinator: true` file carrying a spawn grant) — the correct action on `.claude/skills/grugops/SKILL.md` is **no change**; (b) `context: fork` + `agent: grugops-orchestrator` exists but is **rejected** — it puts the coordinator on the subagent path where the allowlist is ignored (D-03's exact failure), costs a nesting layer, and a backgrounded fork gets the narrower background tool set. Recorded so a later phase does not "discover" it as an improvement.
>
> The verified trichotomy this section is now built on:
>
> | Entry path | Main thread? | Can spawn? | `Agent(...)` allowlist enforced? |
> |---|---|---|---|
> | `claude --agent grugops-orchestrator` | yes | yes | **yes** |
> | `/grug` (skill in default session) | yes | **yes** | no — session agent declares no allowlist |
> | `@grugops-orchestrator` (subagent) | no | yes, to the depth limit | **no** — the type list is ignored |
>
> D-01, D-03, and D-04 survive unchanged on their own rationale. D-02 and D-05 are re-derived below.

- **D-01:** The installer writes **no** main-thread wiring into the target repo. No `.claude/settings.json` `{"agent": ...}`, not even sentinel-wrapped. `claude --agent grugops-orchestrator` is documented as the full-capability path. — Rationale: writing it would make *every* Claude session in that repo the grugops coordinator, including sessions where the user just wants to edit a README; `settings.json` is user content and the installer is bound to "additive, never overwrite."
- **D-02 (REVISED 2026-07-28 — user decision; supersedes the original "loud degrade on every non-`--agent` path"):** The coordinator announces **one of three tiers, honestly**, keyed on what it can actually observe:
  - **Full** — `claude --agent grugops-orchestrator`. Parallel spawning; the enumerated `Agent(...)` allowlist **is** runtime-enforced.
  - **Reduced** — `/grug` in a default session. Parallel spawning **is available and the coordinator uses it**, but the enumerated grant is **not** runtime-enforced; the coordinator **says so** and stays inside the grant **by instruction**.
  - **Degraded** — the `Agent` tool is absent (the four other CLIs, or a subagent at the depth limit). Sequential single-window, announced — the existing path v2.0's dual-path equivalence oracle already proved converges on identical on-disk artifacts.

  — Rationale: the original decision's trigger condition was disproven (see the revision note above); degrading `/grug` to sequential would now be *discarding working parallelism on the brand's headline entry point*, not honoring a platform limit. This preserves D-03's spirit exactly — the coordinator never *claims* an enforced allowlist it does not have. — **Reversibility:** costly — the tier vocabulary becomes the documented `/grug` contract and the thing `guard_wr05` asserts; changing it later means re-cutting the guard, the coordinator body, and the user-facing docs together.
- **D-03:** **Never spawn under an allowlist the runtime is ignoring.** A green file with a false runtime is the exact failure class this milestone exists to kill (research finding ①: a parenthesized `Agent(a, b, c)` list evaporates on the subagent path).
- **D-04:** Detection is **capability-sensing, not version-sensing**: if the `Agent` tool is not available to the coordinator, it runs sequential and says so. This satisfies the milestone decision that *no runtime version-detection code is required*, and it is the same signal on all five host CLIs.
- **D-05 (REVISED 2026-07-28 — re-derived from the revised D-02):** `guard_wr05` asserts **tier-announcement presence** — the coordinator body carries all three tiers of D-02 and the capability-sensing check that selects between them — plus SPAWN-04 (no `Agent`/`Task` on any non-coordinator adapter). Both mechanisms hold on the subagent **and** main-thread paths, so no wiring artifact needs to exist in the user's repo for the guard to be meaningful. It does **not** assert a `settings.json` entry (there is none — see D-01). — Note: the original wording asserted "degrade-path presence," which under the revised D-02 would guard for text that must no longer be the `/grug` contract.

### Kit-Root Resolution for Spawned Roles (SPAWN-01)

**This decision deliberately overrides `.planning/research/ARCHITECTURE.md:515`**, which recommends the 17 role adapters be *plain copies* "because they cite repo-relative paths and the orchestrator already resolved the kit." That is wrong on the spawn path: in an installed repo `agent-factory/` does not exist (the kit lives at `${GRUGOPS_HOME:-$HOME/.grugops}`), a spawned subagent is a fresh context holding only its own adapter body, and `AGENTS.md:34` says the correct behavior on an unresolved kit is **STOP — do not hunt**.

- **D-06:** **All 17 adapters are self-sufficient resolvers.** Each generated adapter carries the resolver block; the installer materializes the absolute `KIT=` into all of them (not just the current two). — Rationale: making a spawned session's ability to find the kit depend on text the spawner remembered to include is "the handoff is the only memory," the pattern v2.0 deleted; it also silently breaks direct `@grugops-<role>` invocation. — **Reversibility:** costly — undoing it means re-cutting the generator template, the installer's materialization loop, `check-kit-refs` Assertion 3, and the uninstall mirror together.
- **D-07:** `check-kit-refs.ts` Assertion 3 is restated as a **derived predicate** — "`$GRUGOPS_HOME` appears only in generator-produced adapters + the packaging template" — never a literal widened from 3 named sites to 18. Consistent with KIT-02.
- **D-08:** A generated role adapter body carries exactly: the kit-vs-state blockquote, the resolver block, "read `agent-factory/roles/<role>.md` and act as that role," and the echoed hard limit (never merge a protected branch / never deploy to prod). **Nothing role-specific** — the role file does the thinking. The factory read order (`factory.config.json`, `AGENTS.md`, `plans/board.md`) is *not* duplicated into 17 adapters.

### Spawn Set & Adapter Shape (SPAWN-01, KIT-03)

- **D-09:** `adapters = 17 roles`; `grant = adapters − coordinator = 16`. KIT-03 asserts `grant ∪ {coordinator} == adapters == roles`. No exception list anywhere.
- **D-10:** The `tier: enterprise` roles are **in** the grant. **Grant is capability; the config dial is policy.** Filtering at the grant level would put policy in the capability layer and mean a `factory.config.json` change cannot reach a role the runtime already refused.
- **D-11:** Each adapter's `tools:` derives from a **neutral `capabilities:` key added to role frontmatter** (roles already carry `kind:` / `tier:`), which the generator maps to Claude Code tool names. — Rationale: a per-role map inside the generator is the drift class this phase kills; Claude-Code tool names inside portable role text is a portability smell, since role files serve all five host CLIs. — **Reversibility:** costly — the capability vocabulary becomes kit content read by the generator; changing it later touches all 17 role files plus the generator's mapping table.
- **D-12:** Each adapter's `description` (the auto-routing trigger) derives from the role's existing **`## One job` + `## Activates when`** sections. Both already exist in all 17 roles and `## Activates when` is already written as a routing trigger ("Need code (one ticket)", "Need tests", "Need business acceptance"). Zero new authoring, zero drift — editing the role updates the adapter.
- **Platform fact to respect:** from Claude Code v2.1.208, a subagent whose `tools` entries resolve to no real tool **refuses to launch** with an error naming the entries. A wrong or empty capability mapping therefore fails loudly rather than silently.

### orchestrator.md Trim (SPAWN-06)

**Finding that reframes this requirement:** the spawn instruction already exists at `orchestrator.md` Responsibility 4 — *"on Claude Code spawn role-agents via the `Agent` tool up to `queue.wip_limit` concurrent WIDTH; on the four other CLIs drain the queue concurrency-1"*. Under D-04 that becomes host-keyed → capability-keyed, an edit of roughly equal length rather than a new block. **The 16-name allowlist never enters this file at all** — it is generated into adapter frontmatter. So the bytes needed for "spawn text" are ≈ 0.

- **D-13:** Trim `orchestrator.md` to **below the WARN tier (7165B)**, ~400B. — Rationale: WARN is already breached at 7562B, so the two-tier guard has effectively collapsed to a single FAIL tier with 8 bytes of room. The ceiling is **never raised** (milestone Out-of-Scope).
- **D-14:** Bytes come from **prose tightening in place** — no relocation, no new files, no invented ceilings. This also stays clear of Phase 29's LANG-05 de-dup targets (`## One job` / caveman block / `## Responsibilities`), which must not be front-run.
- **D-15:** **Relocation trap, recorded so no later phase falls into it:** `_role-switch-protocol.md` is **not** in `ROLE_FILES` and has **no byte ceiling**. Extracting text there would shrink `orchestrator.md` by moving bytes somewhere nothing measures them — constraint-gaming, the same shape as the defects this milestone fixes. **If any text is relocated in this phase, the destination enters `ROLE_FILES` (or an equivalent guarded set) with its own FAIL/WARN in the same commit.**
- Context for the planner: `### Routing matrix` is 1188B of the 7562B (reference data, not instruction); `orchestrator.md` is 1.5× the next-largest role (`security-nfr.md`, 4993B).

### Derivation Reach (KIT-01, KIT-02)

Area 2's decision makes this **mandatory, not optional**: materializing 17 adapters means the installer needs the set, and an uninstall that does not mirror it leaves orphans.

Full inventory of enumerating literals found in the tree:

| Literal | Where | Size | Named in KIT-02? |
|---|---|---|---|
| `WR05_SCAN` | `scripts/check-foundation-guards.ts:135` | 4 | yes |
| `ADAPTERS` | `scripts/check-foundation-guards.ts:244` | 2 | yes |
| `CTX_WORKFLOWS` | `scripts/check-foundation-guards.ts:593` | 16 of 19 | yes |
| `ROLE_FILES` | `scripts/check-foundation-guards.ts:282` | 17 | no |
| `WORKFLOWS` / `ROLES` | `scripts/validate-agent-factory.ts:118,144` | 14 / 16 | yes |
| `SCAN` / `GH_SCAN` | `scripts/check-kit-refs.ts:45,61` | ~45–55 / 3 | no |
| `SKILLS` / `AGENT_REL` | `install/install.ts:480,490` (+ uninstall mirror) | 7 / 1 | no |
| `MARKER_SITES` | `scripts/check-kit-refs.ts:70` | 3 | no — **added 2026-07-28, see D-27** |
| `roleCeiling()` | `scripts/check-foundation-guards.ts:486` | 17 cases | no |

- **D-16:** Phase 27 re-points **every enumerating literal except `roleCeiling()`** — i.e. the four KIT-02 sets plus `ROLE_FILES`, `check-kit-refs`'s `SCAN`/`GH_SCAN`, and `install.ts`/`uninstall.ts`. Matches success criterion 1's "no stale literal survives."
- **D-17:** **`roleCeiling()` is deliberately left alone and this is recorded so a later phase does not "fix" it.** It is a per-file *measurement baseline*, not a discovery set, and it already **fails closed** on an unknown role (`"has no documented ceiling (unknown role — update role_ceiling)"`). Under KIT-01 that behavior gets *better*: adding role #18 fails red and forces a deliberate ceiling decision. It is the one hand-maintained table in the tree that defends itself.
- **D-18:** The installer **self-derives by `readdirSync`** of the source `.claude/agents` and `.claude/skills` and installs what is there; `uninstall.ts` mirrors the same derivation. It does **not** import `scripts/kit-model.ts`. — Rationale: keeps the deliberately self-contained single-file installer decoupled from the `scripts/` layout and avoids a manifest artifact needing its own freshness gate. Separation of duty: the installer faithfully installs whatever exists; `kit-model` + the KIT-03 oracle guarantee at CI time that what exists is correct.
- **D-28 (ADDED 2026-07-30 — user decision, gap-closure round 3; AMENDS D-18):** The `install.ts` / `uninstall.ts` derivation pair is **collapsed into one shared module inside `install/`** that both import. It still does **not** import `scripts/kit-model.ts` — D-18's actual rationale is decoupling the installer from the `scripts/` layout, and a shared file *inside* `install/` preserves that in full while deleting the duplicate. — Rationale: the "declared byte-identical pair" (inventory entries #9/#10) has now drifted **twice inside phase 27** — round 1 re-synced it, then 27-22 re-broke it by moving only `install.ts` onto `statSync` (CR-02: a symlinked source adapter is installed and never removed, `== uninstall complete ==`, exit 0). Hand-synced duplicates of one predicate are the exact drift class KIT-02 exists to delete, and this repo's own terminal lesson is that the fix is **structural** — one authority per predicate — never another hand-sync plus a promise. D-18's separation-of-duty half is unchanged: the installer still faithfully installs whatever exists, and `kit-model` + the KIT-03 oracle still guarantee at CI time that what exists is correct.
- **D-29 (ADDED 2026-07-30 — user decision, gap-closure round 3):** `scripts/kit-model.ts`'s `walkFilesRelative()` symlink-**cycle** hang (recorded in `deferred-items.md` under "From 27-22 (WR-02)") is **in scope for round 3**, fixed together with CR-03's cycle-guard defect in `install.ts`. — Rationale: they are the same predicate — "have I already walked this real path?" — and CR-03 proves that answering it in two places produces two different wrong answers (`install.ts` has a guard whose *global* visited set drops a legitimate distinct-relative-path member; `kit-model.ts` has no guard at all and recurses without bound). One coherent treatment, not two half-guards. The reason 27-22 deferred it — editing the shared authority mid-phase would have put 27-18/27-19/27-20/27-23 at risk — no longer applies: all 23 plans are executed.
- **D-27 (ADDED 2026-07-28 — user decision, resolves RESEARCH.md Open Question 6):** `MARKER_SITES` (`scripts/check-kit-refs.ts:70`, the inventory's missed literal #11) is **in scope for Phase 27, in the same wave as the other derived sets**. D-06/D-08 make it stale by 15 entries the moment the 17 adapters land. — Rationale: leaving it hand-maintained ships a fresh instance of the exact defect this milestone exists to kill — a set literal that rots while the suite stays green. Cost is small; the alternative is a known-stale hand-listed set inside the very phase whose purpose is deleting those. It is derived from `kit-model` like `WR05_SCAN` / `ADAPTERS` / `CTX_WORKFLOWS`, and gets the same per-consumer derivation assertion D-19 requires.
- **D-19:** Proof that no stale literal survived is a **recorded inventory + per-consumer assertion** — the table above becomes a committed record, and each consumer gets a test asserting its set comes from `kit-model`. Explicitly *not* a grep-based "literal detector" guard, which would itself be a heuristic that can be a strict subset — the pattern the closure doctrine warns about.

### Asserted-Count Semantics (KIT-01)

The count defends against **vacuity**, not against additions. Two distinct failure modes: (a) a file is added and a consumer misses it — derivation alone fixes this; (b) the set comes back **empty or short** (bad cwd, wrong root, kit not installed, a glob that stopped matching) — then every derived set is empty and every guard **vacuously passes**, which looks green. A literal `17` in `kit-model.ts` is therefore **not** the drift class being deleted: the drift class is a list of *names consumers read as truth*; a count is a number that can only fail closed.

- **D-20:** The count is **exact in both directions** — 17 roles, 19 workflows. Adding role #18 fails red and forces the author to walk the derived consumers, which is precisely the review moment wanted.
- **D-21:** Enforcement is **two-tier by severity**: `kit-model` **throws** on vacuity (empty / below floor — unsafe to continue), and a **guard fails red** on exact-count mismatch (safe to continue, CI red). Not test-only — a consumer running against an empty dir at runtime must not pass silently.
- **D-22:** `kit-model` takes an **explicit root argument** — `listRoles(kitRoot)` / `listWorkflows(kitRoot)` — defaulting to the script-relative repo root. Each consumer passes the root it already resolved. — Rationale: the tree already has **three** root conventions (`validate-agent-factory.ts` is genuinely two-root with `VALIDATE_ROOT` + `VALIDATE_KIT_ROOT` from the v1.1 work; `check-foundation-guards.ts` and `check-kit-refs.ts` use `CHECK_ROOT`). `kit-model` must not invent a fourth env var.
- **Derivation rules** (already proved correct at `scripts/generate-catalog.ts:110-112,154-156`): roles = `readdirSync(agent-factory/roles)` dropping `_`-prefixed files (so `_role-switch-protocol.md`, `kind: protocol`, is excluded → 17); workflows = `/^\d{2}-.+\.md$/` → 19.

### guard_adapter_body Vocabulary (SPAWN-05)

**A conflation in the requirement's wording, resolved here.** The surviving line at `.claude/agents/grugops-orchestrator.md:25` reads *"— one window, drop prior context, the handoff is the only memory — demand a handoff packet from each"*. Two things are wrong there and **only one is banned**:

- `handoff packet` / `the handoff is the only memory` — **dead vocabulary.** Phase 24 deleted all 17 handoff templates; the shared verified context replaced them.
- `one window, drop prior context` — **still correct.** It describes *execution topology*, not memory: on the four non-spawning CLIs roles activate one at a time in one window. It is verbatim in the packaging template, and D-02 just made it the Claude Code degrade path too. A guard banning "single-window" prose would fail red on text deliberately kept.

- **D-23:** `guard_adapter_body` checks **both directions**: it bans the dead handoff vocabulary **and** asserts every adapter body names the shared verified context as its memory. — Rationale: the positive half catches an adapter gone stale by *omission* and does not depend on having guessed every dead phrase.
- **D-24:** The dead-vocabulary list is **one shared exported source** (path forms + prose forms) consumed by both `check-kit-refs` Assertion 2 and `guard_adapter_body` — one place says "this vocabulary is dead." Note the two checks are genuinely different predicates (Assertion 2 greps the **path** `agent-factory/handoffs/`; line 25 contains no path), so a second check is justified — a second *list* is not.
- **D-25:** Scan set = the **derived adapters (17 agents + 7 skills) plus `agent-factory/packaging/subagent.frontmatter.md`**. The template is the upstream source, so a regression there is caught before it propagates through the generator.
- **D-26:** This guard is **defense in depth, never the structural fix** (the requirement's own framing). Once the orchestrator adapter is generated from the template, line 25 dies structurally — the template already says "the shared verified context is the only memory" and "require published notes." The guard exists to catch hand-edits and template regressions.

### Gap-closure round 5 (ADDED 2026-08-03 — user decisions, after round-4 verification returned `gaps_found` 7/10)

> **Numbering note.** D-30 … D-38 were taken during round-4 *planning* and are recorded in `27-29-PLAN.md` … `27-32-PLAN.md` rather than here. They are not restated; read them there. Round 5 continues at D-39.

- **D-39 (delimiter posture — REFUSE AMBIGUITY BY NAME; supersedes nothing, but it is the fourth application of D-30's polarity):** `scripts/frontmatter.ts`'s delimiter region gets **one normalization point and one whitespace authority**, and the complement of the delimiter test stops being a silent success.
  1. **Normalize once.** A single leading `U+FEFF` is stripped at the same point CRLF is already normalized (`text.replace(/^\uFEFF/, "").replace(/\r\n/g, "\n")`). A BOM is an encoding artifact every real reader strips; after this point it cannot reach any predicate. This is the **only** byte the module normalizes away.
  2. **One whitespace class.** The delimiter positions declare their whitespace class ONCE (`[ \t]`, which is what YAML permits around a document marker) and both the opening test and the closing scan consult that one declaration.
  3. **The refusal arm is DEFINED AS A COMPLEMENT, not enumerated.** A head/close line is *near-delimiter* iff its only content besides the payload (`---`, or `---`/`...` at the closing position) is invisible; it *is-delimiter* iff it matches the declared class. `near && !delimiter` → `ok:false`, naming the first offending code point. Nothing is enumerated; the polarity is D-30's.

     > **⚠ AMENDED SAME DAY by D-42 (planner-recorded on measurement, orchestrator-ratified 2026-08-03). The original wording of this point is preserved below because it was WRONG, and the way it was wrong is the lesson.** It defined *near-delimiter* as `line.trim() === "---"` — "the disagreement between the module's own two grammars," which reads as elegantly derivation-based and is not. **`String.prototype.trim()` strips ECMAScript WhiteSpace, which does NOT include the Unicode format class (`Cf`).** So `"---\u200B".trim() !== "---"`: a ZERO WIDTH SPACE would not have been *near*, would have fallen straight through to the keyless SUCCESS arm, and would have survived the fix — **and ZWSP is one of the six spellings this decision's own rationale cites as its motivation.** Independently re-measured by the orchestrator before ratifying: of ZWSP, ZWJ, SOFT HYPHEN and WORD JOINER, all four fail open today and all four survive a `trim()`-based rule. The same sweep also showed the defect is far wider than the six measured spellings — over code points U+0000–U+2FFF matching `[\s\p{Cf}\p{Cc}]`, **239 of 242 delimiter positions reach the silent no-grant arm; only 3 parse correctly.** D-42 keeps this decision's polarity exactly and widens only the alphabet: define the payload, and refuse any line whose only other content is invisible under a **Unicode general-category** rule (`\p{Cf}`/`\p{Cc}`/`\p{Z}`, natively available in JS regex with the `u` flag — zero new dependencies). Measured for the amendment: 0 missed, the trailing-space and trailing-tab controls still accepted, 0 false reds across all real spawn-grant-scan files at both positions and both payload tokens. **The meta-lesson, which belongs in this phase's record: "derive the predicate instead of enumerating it" is not automatically satisfied by reaching for a built-in. A built-in carries its own alphabet, and if that alphabet is narrower than the format, the derivation is a denylist wearing a derivation's clothes.** This is the fourth time in phase 27 that a narrower-than-the-format predicate has been the defect — and the first time it was caught before any code was written.

     > **⚠ AMENDED AGAIN, SAME DAY, by D-43 (plan-checker-recorded on measurement, orchestrator-ratified 2026-08-03). D-42 was ALSO a denylist. Both prior formulations are preserved above because the way this kept going wrong is the most valuable thing this phase has produced.**
     >
     > D-42 replaced `trim()`'s alphabet with `[\s\p{Cf}\p{Cc}]` and called that a derivation. It is not: it **names what is ILLEGAL (invisible) and lets the complement pass**. D-39 did the same thing with a narrower alphabet. Both decisions state D-30's polarity in their own text — *name what is legal, refuse the complement* — and **both implement the opposite**. Independently re-measured by the orchestrator against the committed `scripts/frontmatter.js` before ratifying; every row below carries a live `Agent(grugops-orchestrator)` grant:
     >
     > | opening line | today | in D-42's alphabet | after D-42 as written |
     > |---|---|---|---|
     > | `---` (control) | granted | n/a | granted (correct) |
     > | `---` + U+FE0F VARIATION SELECTOR-16 (`Mn`) | SILENT no-grant | no | **still silent** |
     > | `---` + U+0301 COMBINING ACUTE (`Mn`) | SILENT no-grant | no | **still silent** |
     > | `---` + U+0378 unassigned (`Cn`) | SILENT no-grant | no | **still silent** |
     > | `---` + U+E000 private use (`Co`) | SILENT no-grant | no | **still silent** |
     > | `----` | SILENT no-grant | no | **still silent** |
     > | `--- foo` | SILENT no-grant | no | **still silent** |
     > | `---` + U+E0020 TAG SPACE (`Cf`) | SILENT no-grant | yes | refused |
     >
     > `----` and `--- foo` are not exotic; they are spellings common frontmatter readers accept, and no prior decision or review named either.
     >
     > **D-43 — the actual complement.** The delimiter positions declare the ONE legal spelling: the payload (`---` at the opening; `---` or `...` at the close) followed only by the declared whitespace class (`[ \t]`). **A line that begins with the payload and is not exactly that is REFUSED, whatever follows it** — graphic, invisible, combining, unassigned, private-use or another `-`. A second arm refuses residue that is invisible but *leading* rather than trailing. Nothing is enumerated on the refusal side at all; the legal set is finite and stated, and the complement is everything else. This is the first of the three formulations that is actually the polarity all three claim.
     >
     > **False-red cost, measured: zero.** All **33** files across every real spawn-grant scan surface (17 agent adapters + 7 standalone skills + 7 plugin skills + 2 packaging templates) open with a byte-exact `---` on line 1. The strict rule costs this repository nothing.
     >
     > **And the corollary that makes the sweep honest:** a property test whose corpus is generated *from the alphabet under test* is tautological — it can catch a narrowing regression and is structurally incapable of failing on anything outside the alphabet. The sweep must carry members drawn from the **negative space** (`\p{M}`, `\p{Cn}`, `\p{Co}`, `\p{Cs}`, plus the `----` and `--- foo` payload variants), or its completeness claim is circular.
     >
     > **The lesson, stated so the next round cannot repeat it a fourth time: before accepting any predicate as "derived" or "inverted", ask which set it ENUMERATES. If it enumerates the bad, it is a denylist no matter how principled its alphabet looks or how firmly its own text claims otherwise.**
  4. **A genuinely body-only document is untouched** — it is not near-delimiter, so it still reaches the keyless SUCCESS arm. D-34's recorded warning ("turning a body-only file red would trade a silent success for a false red, the worse of the two") is honored: only *near-misses* refuse.
  5. **The open/close asymmetry dies.** Today the identical byte fails OPEN at the opening position (`{ok:true, keys:0}`) and fails CLOSED at the closing position (`ok:false`, "opened and never closed" — a misleading diagnosis). Both become the same named refusal.
  — **Rationale:** measured against the committed `scripts/frontmatter.js`, the opening position has **six** silent-success spellings (BOM, trailing NBSP, form feed, ZWSP, leading space, and the plain-correct controls aside), of which reviews had named two. `flattenBlock` already refuses on every one of its complements, so the delimiter region is the module's **only** remaining silent-success arm — this is a bounded fix, not a sweep. Accepted cost: a possible false RED on a file the platform would treat as inert. A false red is loud and fixable; the silent no-grant arm has now shipped past a green suite four times.
  — **Mandatory false-red control:** run the new predicate over **every file in `SPAWN_GRANT_SCAN` in the real repository** and assert **zero** refusals, alongside the RED-before/GREEN-after transcript for each of the six spellings.
  — **Pinned as a PROPERTY, not a row table** (D-30's precedent): sweep Unicode whitespace and format code points in both the pre- and post-delimiter position. There is currently **zero** encoding coverage anywhere in the suite (`grep -ci` for BOM/FEFF over `frontmatter.test.ts`, `check-foundation-guards.test.ts`, `validate.test.ts` returns 0, 0, 0).

- **D-40 (plugin-form skill tree — DERIVE IT, AND PIN ITS RELATIONSHIP TO THE STANDALONE TREE):** the shipped plugin-form `skills/` tree enters the guarded sets as its own derived authority, and the systematic relationship between the two distribution forms is asserted mechanically.
  1. `scripts/kit-model.ts` gains `listPluginSkillAdapters(root)` and `PLUGIN_SKILL_ADAPTER_COUNT` (7), enforced in `guardKitCounts` on the same argument `kit-model.ts:57-65` already makes for `SKILL_ADAPTER_COUNT` — the plugin tree has no role corpus to cross-check, so a count is its only deletion signal.
  2. `SPAWN_GRANT_SCAN` folds the derived plugin set in, and `guard_wr05`'s PASS line **names the plugin-skill count** alongside the adapter and template counts, so the claim reports the input it actually read.
  3. **The pair rule:** each `skills/<n>/SKILL.md` is asserted byte-identical to `.claude/skills/grugops-<n>/SKILL.md` **except the `name:` value** (`<n>` vs `grugops-<n>`). `skills/grugops/SKILL.md` is **exempted by name**, because the standalone form legitimately carries a kit-root resolver block the plugin form does not need — the exemption is recorded with that reason so it cannot be read as an oversight.
  — **Rationale:** a byte-mirror assertion alone is **not available** — measured, the six command skills differ by exactly the 8-byte `name:` prefix and `grugops/SKILL.md` differs by 448 bytes. So deriving the set is the only answer that fits the facts. The pair rule exists because a near-mirror maintained by hand is the *same* drift class D-28 and D-37 each collapsed once already inside this phase; two forms of one skill edited by hand will diverge exactly the way the install/uninstall derivation pair did.
  — **Pinned with a plant case:** a rogue `Agent(...)` grant planted in `skills/plan/SKILL.md` on a hermetic `CHECK_ROOT` mirror must exit non-zero. The agent-adapter path is caught incidentally by the unrelated `name` floor (with a wrong diagnosis), so the plant case **must** target a skill adapter or it will pass for the wrong reason.

- **D-41 (the three residuals close in the same round, none deferred):**
  1. **`install/kit-source.ts` unreadable arm** — `NestedWalkResult` gains a fourth `unreadable: string[]` channel; both bare `return;` arms (`:335-340`, `:349-354`) route through it and `install.ts` surfaces it beside the existing `cycles` loop, exiting 3 and naming the path. Pinned with a `chmod 000` harness case **and** its readable control. The WR-03 cross-site equality case extends to the **unreadable** arm — it currently covers only the two-path and cycle arms, which is precisely why this divergence survived a case asserting the twins agree.
  2. **`process.exit()` tail sites** — `install/uninstall.ts:730` and `scripts/coordinator-resolution-precheck.ts:598` move to `process.exitCode`. The D-35 regression scan at `install/install.test.ts:2237-2240` extends from two paths to four (adding `uninstall.ts`, `uninstall.js`) by its own stated argument that the committed `.js` is what runs on a host — an argument for four, not two. Either extend `install.ts:1593-1598`'s KNOWN RESIDUAL note to name both tail sites, or delete the false parity claim at `uninstall.ts:725-730` which asserts a parity the code does not have.
  3. **`keysGrantedAgentNames` enumeration** — a nested paren or a quote inside a grant enumeration is **refused**, not returned as a short or altered list. `Agent(a(b), c)` currently drops `c` and `Agent("a,b", c)` currently returns three altered names, both on the `ok:true` arm, and both feed a set-equality. Same argument as D-32, which the function's own contract comment at `:753-757` already states ("a name is never silently dropped or altered") and does not honor.
  4. **The name-floor misdiagnosis** — `scripts/check-foundation-guards.ts:707` must stop asserting "carries no `name` key" when the parse returned zero keys for the whole document. Those are different facts, which is this module's founding argument.

### Gap-closure round 6 (ADDED 2026-08-03 — user decisions, after round-5 verification returned `gaps_found` 7/10)

> **What round 5 genuinely closed — do NOT re-litigate.** All five round-4 findings are closed at the code level and confirmed by direct code reading: D-43's stated-legal delimiter set for every SINGLE-SIDED spelling, `keysGrantedAgentNames` refusing rather than mutating, the installer's fourth `unreadable` channel, the derived-and-folded plugin-form `skills/` tree with its pair rule, and all three exit-after-report tails. Round 6 touches none of that except where D-44 subsumes the delimiter region.

- **D-44 (delimiter region — ONE TOTAL CLASSIFIER, not a union of arms; SUPERSEDES D-43's two-arm implementation while keeping its polarity exactly):** the round-5 defect was not the alphabet — D-43 got the alphabet right. It was that `delimiterRefusal` was split into **two arms whose union is not the complement of the legal set**: arm 1 fires only on `line.startsWith(payload)`, arm 2 fires only when the residue-stripped remainder is a *fully legal* delimiter, so a line carrying BOTH leading invisible residue AND illegal trailing residue (`<ZWSP>---<ZWSP>`) satisfies neither and reaches the keyless SUCCESS arm. One code point flipped a red gate green over a document carrying a live `Agent(grugops-orchestrator)` grant.
  1. **The two arms collapse into one total function.** The delimiter region exposes a single classifier that maps **every** line to exactly one verdict — `legal` | `refuse(reason, offendingCodePoint)` | `not-a-delimiter` — and every call site consumes that verdict exhaustively (an `assertNever`-style exhaustiveness check, so a future fourth verdict cannot be silently ignored). There is no second predicate in the region for a sixth spelling to slip between, because there is no *pair* of predicates to compose.
  2. **The classifier's own shape is the proof.** `legal` is the finite stated set (the payload followed only by the declared `[ \t]` class). `refuse` is *everything that begins with a payload after stripping the leading invisible run, and is not legal* — the leading-invisible class decides only **where the delimiter begins**, never **what may follow it**. That inversion — arm 2 re-imposing the full legality test on the remainder — is exactly what let the composite through, and it is deleted rather than patched.
  3. **`not-a-delimiter` stays the genuinely-body-only arm.** D-34's recorded warning is still honored: a document with no near-delimiter head line still reaches the keyless SUCCESS arm. Only near-misses refuse.
  — **Rationale:** this is the **fifth** spelling of one failure class in this phase (D-39's `trim()` alphabet, D-42's widened-but-enumerated alphabet, D-43's arm split, and now the arm composition). Four rounds of patching the predicate's *contents* have each shipped past a green suite. The remaining move is to delete the structure that keeps admitting a gap — a predicate that is total by construction has no union to leak through. The user ratified this depth over the review's smaller drafted arm-2 patch precisely because the smaller patch preserves the two-arm shape that has now leaked twice.
  — **Mandatory false-red control, unchanged from D-43:** run the classifier over **every file in `SPAWN_GRANT_SCAN` in the real repository** and assert **zero** refusals. All 33 open with a byte-exact `---` on line 1; the strict rule must still cost this repository nothing.

- **D-45 (the sweep is built from OUTSIDE the rule under test — a CROSS-PRODUCT, not one construction per arm):** round-5's negative-space sweep built exactly one construction per declared arm (`leading @ opening`, `trailing @ opening`, `leading @ closing`, `trailing @ closing`), so every member landed inside exactly one arm **by construction** and the sweep was structurally incapable of failing on the composite it was written to detect. A sweep whose corpus is generated per-arm is circular over the arm structure the same way D-43 warned an alphabet-generated corpus is circular over the alphabet.
  1. **The corpus is a cross-product of three independent axes** — leading residue × payload spelling × trailing residue — enumerated as data with no reference to the classifier's internals: leading ∈ {none, BOM, BOM×2, ZWSP, NBSP, space, tab, U+0301 combining, NUL}; payload ∈ {`---`, `----`, `--- foo`, `--`}; trailing ∈ {none, ZWSP, NBSP, space, ` foo`, NUL}. Every cell is classified by the expected-verdict rule stated in D-44, not by calling the code under test.
  2. **Both delimiter positions and both payload tokens** are swept, so the open/close asymmetry cannot reappear in one position only.
  3. **The composite cells must fail today** — the sweep is only a pin if its new rows are RED against the committed `scripts/frontmatter.js` before the fix and GREEN after, with that transcript recorded.
  — **Rationale:** this phase's own record now shows two distinct circularity axes (alphabet, then arm structure). The corollary D-43 already stated — *a corpus generated from the thing under test proves nothing* — is restated here at the structural level, because that is the level round 5 missed.

- **D-46 (plugin-root component surface — DERIVE THE NINE, FORBID THE SEVEN, BOUND `hooks/` BY NAME):** `PLUGIN_DEFAULT_COMPONENT_SUBPATHS = ["agents", "commands"]` is a hand-listed 2-of-9 set with no derivation and no cardinality pin, sitting inside a floor whose own comment claims to close the class rather than the instance — the project's diagnosed set-literal-drift pattern recurring one level inside the fix meant to delete its third instance.
  1. **Derive the full default-discovery set** from the plugin-manifest schema this repository already documents in `CLAUDE.md` (`agents`, `commands`, `skills`, `hooks`, `mcpServers`, `lspServers`, `outputStyles`, `experimental.themes`, `experimental.monitors`), and assert its cardinality **two-sided**, the same way `ROLE_COUNT`, `WORKFLOW_COUNT`, `SKILL_ADAPTER_COUNT`, `PLUGIN_SKILL_ADAPTER_COUNT` and `SPAWN_GRANT_SCAN_COUNT` are each asserted. `skills` remains covered by `listPluginSkillAdapters`.
  2. **Forbid the seven that must not exist at plugin root.** The floor iterates the derived set, not a literal.
  3. **`hooks/` is a named, reasoned, bounded EXEMPTION** in the same shape as `DISTRIBUTION_PAIR_EXEMPT` — it legitimately exists on the live tree (`hooks/hooks.json`, `hooks/guard.js`, `hooks/admission-guard.js`) and `CLAUDE.md` makes the mechanical prod-deploy guard a hard safety constraint, so it cannot simply be forbidden. Its exemption is bounded by two assertions: any frontmatter-bearing member of `hooks/` folds into `SPAWN_GRANT_SCAN`, **and** the directory is asserted to carry zero markdown adapters — so the exemption covers what is there today and fails closed the moment a markdown adapter appears in it.
  — **Rationale:** the user ratified bounding `hooks/` over forbidding it, because relocating a CLAUDE.md-mandated safety surface to satisfy a guard rule is the guard bending the product. A named exemption with two live assertions is the shape this repository already uses for the one legitimate distribution-pair divergence.

- **D-47 (the two carried warnings close in the same round, neither deferred):**
  1. **`guardKitCounts`' per-part membership loop** (`check-foundation-guards.ts:1305-1311`) stops silently `continue`ing when a part's lister throws. Its comment justifies the silence on the premise that the composition and the per-part read fail together — false for the TOCTOU window where the composition derived cleanly at module load and the directory became unreadable afterward, which is the only window in which the catch can actually fire. It reports, matching the sibling catch twenty lines above. This is the file's own stated discipline: every other failure path in it reports.
  2. **`keysGrantedAgentNames` flow-collection residual (round-5 IN-04, unreproduced as a live bypass).** Round 4's WR-02 closed nested-paren and quote characters inside a captured enumeration; a flow-collection delimiter inside an enumeration still returns SPLIT, ALTERED names on the `ok:true` arm. It is the same class as a finding already declared closed, on the same predicate, feeding the same set-equality — so it closes here rather than becoming round 7's "distinct spelling of a closed finding."

- **D-48 (quote state belongs to the SCALAR, not to the physical line — one carried state, and every direction of the per-line reset closes in ONE edit):** `stripComment` and `startsWithReference` derive their state fresh at every line boundary while `flattenBlock` hands them one physical line at a time. **A YAML scalar does not end at a line boundary.** A multi-line quoted scalar is therefore analysed as N independent single-line documents, and the module is wrong in more than one direction at once from that single cause: a `#` on a continuation line is deleted as a comment when YAML says it is content (**CR-01** — a live `Agent(grugops-orchestrator)` grant silently removed, reproduced end to end on two hermetic `git archive HEAD` mirrors with the gate at `ALL CHECKS PASSED` exit 0); `*`, `!` or `&` at the start of a continuation line is refused as a YAML node property when YAML says it is content (**WR-01** — a false RED on documentation a real loader accepts, which the module header at `frontmatter.ts:322` explicitly claims cannot happen); and the accumulator's item boundaries carry the same reset into the JOIN (the third direction, reproduced by the planner while planning this round and named in no review).
  1. **`Accumulator` gains an `openQuote` field** typed `'"' | "'" | null`. The comment scanner is **seeded** with the entering quote state and **returns** the exiting one; the two are the same fact rather than two independent per-line guesses. The state is never re-derived per line and no second quote-state walk is added — one place decides whether the module is inside a scalar, because a weaker duplicate that still votes is worse than none.
  2. **While the entering state is open, the line is CONTENT:** no comment is stripped from it, its position 0 is not a YAML node start so the reference test does not run, and the item-boundary rule does not treat it as a new item. That is the whole of what the carried state decides. It never decides what a value MEANS and it adds no second grammar.
  3. **All directions close in ONE edit — none is fixed separately.** — **Rationale (user-ratified, this round):** 27-39's own prohibition already forbade splitting CR-01 from WR-01 as "one root cause seen from two sides"; the JOIN direction is the same root cause seen from a third side, and its original deferral to a later plan reproduced the exact incrementalism that has now produced **six** consecutive rounds of "one more spelling." A fix that closes only the directions a finding happened to report is the enumerate-the-bad shape this phase has corrected six times. The user ratified folding it in over pinning it independently.

- **D-49 (the pin is a FOURTH AXIS enumerated from outside the rule under test — and a cell that was never red is not a pin):** the three D-45 axes are properties of ONE LINE and are structurally incapable of failing on a construct that spans lines; that is precisely why the round-6 sweep passed over a live bypass while green. The round-7 sweep adds **scalar style x sigil x placement**, whose expected verdict for every cell is a pure function of the three labels, computed without calling the module.
  1. **Every cell in the CONTINUATION column is RED against the committed `scripts/frontmatter.js` BEFORE the fix and GREEN after, with both transcripts recorded.** A green suite is never offered as evidence that no bypass exists — this phase's baseline suite has been green in every one of the six rounds in which a defect was later found.
  2. **Every claim about what the platform sees is resolved against a REAL YAML 1.2 loader** (`/usr/bin/ruby -ryaml`, Psych/libyaml) and recorded as a transcript, never asserted from reading.
  3. **The corpus is never generated by calling the code under test, and never built one construction per declared arm or per declared line.** Both are circular, and this phase has shipped once past each.
  4. **The repository-wide false-red cost stays at ZERO** — measured across all tracked markdown files before and after. A widened refusal that turns correct documentation red is the failure mode WR-01 already demonstrates, so any NEW refusal is a defect in the fix and is named rather than accepted.

- **D-50 (the remaining round-6 findings close in the SAME round, none deferred — the third application of the D-41/D-47 posture):** after D-48 folds the three directions of the per-line reset into `27-39`, the eight remaining round-6 items close across three further plans in the same round rather than becoming round 8's backlog. `27-40` closes **WR-02** (`classifyDelimiter` discards indentation, so an indented `---`/`...` inside a legitimate block scalar refuses the whole document) and **IN-02** (`unquoteChecked` is applied to the joined value of a `|`/`>` block scalar, where YAML applies no quoting rules at all — the flush lacks the block exemption the reference test documents ten lines above it). `27-41` closes **WR-03** (a capture `SCOPED_GRANT` fails to form never reaches `ENUMERATION_LEGAL_CHARS`, so a truncated enumeration returns the "unscoped grant" SUCCESS arm) and **IN-01** (a ZWSP-only prologue line reaches the keyless success arm, asymmetric with the D-34 directive refusal, and masked today only by sibling guards). `27-42` closes **IN-03**, **IN-04** and **IN-05**. — **Rationale (user-ratified, this round):** every round of this phase that deferred a warning saw it return, usually one abstraction level down; D-41 and D-47 both established "the carried warnings close in the same round, neither deferred," and the user ratified the third application rather than carrying eight items forward. Where a finding is genuinely **record-don't-fix** (IN-03 is unfalsifiable by construction and the source already discloses it; IN-05 is a tree-wide wording claim that is true only when scoped to guard surfaces), the plan states that disposition explicitly with its reason — a recorded decision, never a silent drop.

### Gap-closure round 7 (ADDED 2026-08-09 — user decisions, after the round-7 code review reproduced the SEVENTH consecutive spawn-grant bypass at exit 0)

> **What round 7 genuinely closed — do NOT re-litigate.** All five inverted assertions (27-39 × 2, 27-40 × 3) were re-measured against libyaml and each states what the loader computes; the `partitionPluginComponentClaims` extraction is behaviour-preserving with a genuine differential control; coverer-by-identity is falsifiable on all four facts; the moved cardinality floors (35→33, 420→396, `SPAWN_GRANT_SCAN_COUNT` 33) each track their table as a product rather than a bare literal. Round 8 touches none of that. What round 7 did **not** close is the defect 27-39 was written to close, at the application points its fix did not reach.

- **D-51 (the quote state that may CROSS a line boundary is decided INSIDE the one scanner walk — the three seeding sites become consumers, and `nodeStartQuote` as a separate gate is DELETED):** D-48 promoted quote state to `Accumulator.openQuote` and gated the carry on `nodeStartQuote`, which is correct and verified against libyaml — but the **seeding** was wired into only two of the three places a node can begin, and into none of the places a node begins *mid-line*. `flattenBlock` seeds at the key line (`frontmatter.ts:888`), at the block-sequence item (`:809-810`) and at the plain continuation (`:828`), and **the union of those three arms is not the set of node starts.** Two spellings escape: **(a)** a key line carrying no value makes the *continuation* line the node start — `nodeOnKeyLine` is false so `startsNode` is true, but `inScalar` is false so the carry is skipped; **(b)** a quoted scalar that opens **mid-line inside a flow collection** — `nodeOnKeyLine` is already true, so `startsNode` and `inScalar` are both false. Either way the scalar's state is discarded at the boundary, the next line is handed to `stripComment(t, null)`, a leading `#` is read as a comment start, and the **entire continuation is deleted** with the spawn token on it — returned on the no-grant SUCCESS arm. Reproduced end to end on three shipped surfaces (a plugin-form skill, its standalone twin, and a non-coordinator role-agent adapter) with the whole foundation gate printing `ALL CHECKS PASSED` at exit 0 while a real YAML loader reads the grant; 4 bypasses and 0 false reds across a 240-cell libyaml differential.
  1. **`stripComment` is the ONE authority on what may cross.** It already walks the line character by character. It is told whether the line's offset 0 is a node start, and it tracks **flow-collection depth** as it walks, so at every `"` / `'` it knows whether that position may begin a node (offset 0 of a node-start line; or inside `[` / `{` after an opener, a `,`, or a `: ` separator). It returns an **already-gated** `openQuote` — non-null only when the still-open quote was opened where a node may begin. One walk, one answer, and the answer is complete over its own input.
  2. **The three seeding sites collapse to one assignment each** — `cur.openQuote = scanned.openQuote` — with no `nodeStartQuote` gate and no `inScalar` / `startsNode` conditioning of the carry. `nodeStartQuote` as a separate predicate is **deleted, not kept alongside**: a weaker duplicate that still votes is worse than none (D-48's own rule, and this module has now deleted a weaker-duplicate predicate three times).
  3. **Within-line hash-hiding is byte-unchanged**, and the D-48 plain-scalar-apostrophe regression stays closed **by construction**: an apostrophe at a non-node-start position never licenses a crossing. That is exactly the fact `nodeStartQuote` encoded, now decided at the point where the position is actually known instead of being re-derived from the first token of a line.
  4. **The value-map comparison over `git ls-files '*.md'` is re-run before and after**, and the repository-wide false-red cost stays at **ZERO**. The round-7 record shows that comparison is the only thing that caught the apostrophe regression.
  — **Rationale (user-ratified, this round):** the review drafted two directions (a three-state `nodeOnKeyLine`; an explicit flow depth on the accumulator) and both add a fact to be **re-consulted at the same three call sites** — a fourth arm on a predicate whose arms are the defect. The user ratified deleting the arms instead. The reviewer also **measured** the obvious one-liner (mirror the item path in the continuation path): it closes family (a) and leaves family (b) returning `{ok:true,value:false}`, because inside a flow collection the quote opens mid-line. **The one-liner is rejected by name** — shipping it and calling the class closed is the enumerate-the-reported-spelling shape this phase has corrected seven times. This phase's terminal lesson is *"after splitting a predicate into arms, test their UNION"*; the union is what failed, so the fix removes the split.

- **D-52 (the completeness claim is DERIVED from a real loader over a GENERATED corpus — never from the product of hand-listed axes; co-critical with D-51, not scheduled behind it):** `SWEEP_SCALAR_STYLE` (`frontmatter.test.ts:4184-4212`, pinned at `:4336`) is a hand-listed 6-element set, and **every one of its six styles opens its scalar as the first token after `tools:` on the key line or as the first token of a block-sequence item.** There is no style in which the value node begins on a continuation line and **no flow-collection style at all**, so *neither* D-51 family is expressible in it — the sweep passed green at 90 cells over a live bypass, exactly as the round-6 sweep did. `expect(SWEEP_SCALAR_STYLE.length).toBe(6)` pins the list against *shrinking*; nothing pins it against *incompleteness*. The same holds for the hand-written `TRUTH` table (`:4426-4463`), whose completeness claim `TRUTH.length === STYLE.length * SIGIL.length` is a claim about the product of two hand-listed axes, not about the construct.
  1. **Axis 1 gains the missing node-start placements:** the value node opening on the **continuation line** in each quoting style; a **flow sequence** and a **flow mapping** whose quoted scalar spans lines; and a block-sequence item whose `-` line carries no value and whose quoted scalar opens on the item's own continuation.
  2. **The completeness claim moves out of the list.** A differential harness **generates** the corpus and compares the module's value map against `/usr/bin/ruby -ryaml` cell by cell, with the **loader's** verdict — not a hand-written table — as the expected value. Cells libyaml rejects are skipped and the skip is **printed**. The D-49 loader cross-check at `:4494` already establishes both the mechanism and the printed-skip precedent; today it checks only 6 named cells.
  3. **Every new cell is RED against the committed `scripts/frontmatter.js` before the fix and GREEN after, with both transcripts recorded.** A green suite is never offered as evidence of absence: this suite has been green in all **seven** rounds in which a defect was later found.
  — **Rationale (user-ratified, this round):** WR-01 is *why* seven rounds shipped green, so it closes with D-51 rather than behind it. A sweep whose corpus and whose expectation are both hand-written over the same axes cannot fail on an axis nobody thought of — the set-literal-drift class one level up, in the pin rather than in the code.

- **D-53 (all EIGHT round-7 findings close in the SAME round, none deferred — the fourth application of the D-41/D-47/D-50 posture), with each item's disposition recorded:**
  1. **WR-02** — `stripFencedBlocks` runs over the whole document *before* the frontmatter block is located, so a column-0 fence inside the frontmatter region deletes content and the truncated result is returned on the SUCCESS arm. The fix **locates the delimiter region first** and fence-strips only the body used for the prose checks (or refuses when a fence delimiter falls inside the located region) — it **never widens the strip**. Scoped honestly: this is **not** a confirmed live bypass — every document constructed for it is a `Psych::SyntaxError`, and the one spelling libyaml accepts the module already refuses in the safe direction. What is defective is the **contract**: content the module cannot account for is silently removed rather than routed to the `ok:false` arm.
  2. **IN-01** — `accountSpawnOccurrences`' count-identity arm (`frontmatter.ts:1775-1784`) is provably unreachable and unexercised, which is the exact IN-03 shape plan 27-42 spent a plan closing and 27-41 shipped anew in the same round. Apply 27-42's own remedy: make it reachable from a case that hands it a **fourth, unclassified kind** and assert the refusal fires **by name**. A future-proofing arm nobody can test is a promise, not a floor.
  3. **IN-02** — `HEAD_DELIMITER_CONSTRUCTS` and `KEY_LINE_CONSTRUCTS` (`frontmatter.test.ts:3170-3179`) get cardinality pins, and each construct is asserted **individually load-bearing** for at least one planted fixture, so the refusal claim cannot shrink silently while the two live files still match through the remaining patterns.
  4. **IN-03** — the item path's `stripComment` seed is provably the constant `null`, and its comment claims it "reads the carried state like every other." Either assert the invariant at that point or state it plainly — and **re-derive it after D-51**, since a continuation line can then legitimately open a scalar. A comment claiming a property never ships without the assertion that makes it true.
  5. **IN-04** — `partitionPluginComponentClaims.foreign` (`kit-model.ts:474`) is not de-duplicated, so a key claimed by two buckets *and* absent from the schema is interpolated twice into the guard's failure message. The message is a contract the cases pin, so it is decided rather than left.
  6. **IN-05** — a multi-document YAML stream reads only the first region while libyaml reads the stream. `UNKNOWN - verify`, and **not claimed as a bypass** (most markdown frontmatter readers also take only the first `---`…`---` region). It is **dispositioned in the module header** — "the platform reads one block; a stream is out of scope" — because the module's carefully-partitioned "three outcomes and no fourth state" argument enumerates delimiter spellings exhaustively and never mentions streams, which is how the WR-05 arms came to be written one rule short.
  — **Rationale (user-ratified, this round):** every round of this phase that deferred a warning saw it return, usually one abstraction level down. D-41, D-47 and D-50 each established "the carried warnings close in the same round, neither deferred," and the user ratified the fourth application over carrying seven items into a round 9. Where an item is genuinely **record-don't-fix** (IN-05), the plan states that disposition with its reason — a recorded decision, never a silent drop.

### Gap-closure round 8 (ADDED 2026-08-09 — user decisions, after the round-8 code review reproduced the NINTH consecutive spawn-grant bypass at exit 0)

> **What round 8 genuinely closed — do NOT re-litigate.** D-51 collapsed the three seeding sites into one walk-computed `ScalarState` and deleted `nodeStartQuote`, proven with seven RED-before/GREEN-after rows and three live shipped-surface reproductions flipping exit 0 → exit 1; D-52's 312-cell corpus is checked in one process against `/usr/bin/ruby -ryaml` with the disagreement set asserted EQUAL to two named bounded exemptions; WR-02's fence strip was removed from the entry point entirely (locate-then-flatten), closing five further truncated-success shapes; IN-01's fourth-kind arm is now reachable and IN-05 is recorded in the module header; `partitionPluginComponentClaims.foreign` de-duplicates with a byte-identical guard PASS line. KIT-02 is CLOSED. Round 9 touches none of that. What round 8 did **not** close is that the walk's node-start set is still not YAML's node-start set, at four positions its own red-team did not enumerate.

- **D-54 (the walk's node-start answer is a property of STRUCTURAL POSITION, decided in ONE place — not a fifth enumerated arm):** D-51 correctly made `stripComment` the single authority on what may cross a line boundary, but it raises `mayBegin` only for flow constructs, and only in the spellings D-51's red-team happened to enumerate: `,` and `?` gated on `depth > 0`, and `:` gated on `depth > 0` **and** a following whitespace. Four node-start positions YAML defines sit outside that union: **(1)** a block-mapping `key: value` on an indented line (the `:` arm is flow-gated); **(2)** a compact nested sequence `- - `, where the second dash begins a node mid-line and `SEQ_ITEM` consumes only the first; **(3)** a block explicit key `? ` at depth 0 (the `?` arm is flow-gated); **(4)** JSON-like `:` adjacency inside a flow mapping — `{"a":"v"}` is legal YAML 1.2 with no space after the colon when the key is JSON-like, which is **inside the flow domain D-51 was written to own**. In each case the quote that follows opens at a real node start, the walk records `openedAtNodeStart = false`, the state dies at the line boundary, the next line's `#` is stripped as a comment, and the whole continuation — with the spawn token on it — is deleted and returned on the `{ok:true, value:false}` SUCCESS arm. Reproduced end to end on hermetic `git archive HEAD` mirrors with `Agent(grugops-orchestrator)` planted on **both** distribution twins of the non-coordinator skill `plan`: control (one-line grant) exit 1 `FAIL WR-05 coordinator-spawn-grant violation`; families A, B, C and F all exit 0 `ALL CHECKS PASSED`. Ruby/psych/libyaml confirms the grant is real in the loaded document.
  1. **The flow-only gate is dropped from the two block-legal indicators.** `?` may begin a node at a line's structural start at depth 0 (block explicit key), and `:` may begin a node whenever a separation follows — which is whitespace/EOL in any context. These are not new arms; they are the removal of a `depth > 0` condition that was never part of the grammar rule.
  2. **The flow `:` rule is made to match YAML 1.2's actual separation rule** — whitespace **or** a JSON-like key just closed. `jsonLikeKeyJustClosed` is tracked **in the same walk** (set when a quote closes, or a `]`/`}` closes at the current depth; cleared on any other content character), so there is still **one authority and no second opinion**.
  3. **The item path learns that a dash consumes exactly one level** — `- - x` re-enters the item rule at the second dash, rather than `SEQ_ITEM` swallowing the line.
  4. **The acceptance evidence is the four gate transcripts flipping to exit 1**, not a green suite. This suite has now been green in **all nine** rounds in which a defect was later found; a green suite is never offered as evidence of absence.
  — **Rationale (user-ratified, this round):** the user considered and **rejected by name** deleting the hand-rolled grammar in favour of a real YAML parser as a dev-only dependency (technically available — `scripts/frontmatter.ts` is dev/CI tooling and the installer ships only `scripts/runnable-ref/*.js` to hosts), electing to keep the `{typescript, vitest}` dev-dep line and fix the walk structurally. The review's own instruction governs the shape: *"Do not add four arms — that is the shape this module has now declined five times."* This phase's terminal lesson is to ask **which set the predicate ENUMERATES**; the answer must be "YAML's node-start productions", derived from structural position, not "the spellings a red-team reported".

- **D-55 (`nodeOnKeyLine` is renamed to what it MEANS and set at BOTH places a node can begin — and the harnesses start comparing the NAME SET, not a boolean):** `cur.nodeOnKeyLine` is written exactly once, on the key line (`frontmatter.ts:1161`). When the key line carries no value the node begins on the *first continuation line* and nothing records it, so `startsNode = !inScalar && !cur.nodeOnKeyLine` stays **true for every continuation line of that key**. The field's own doc block states the correct rule and implements it for one of the two places a scalar can begin. Three measured consequences: **(a)** an **INVENTED NAME on the success arm** — `tools:` / `  Agent(alpha, ga` / `  - mma)` yields `["alpha","ga","mma"]` where the document expresses `["alpha","ga - mma"]`, fed straight into the KIT-03 closure equality and coordinator-resolution-precheck, both of which promise at `frontmatter.ts:2168-2181` that "a name is never silently dropped or altered"; **(b)** module **GRANTS where the loader has none** — the D-52 harness's own declared never-exemptible direction; **(c)** false refusals on documents libyaml loads cleanly (`*emphasis*` read as an alias), currently enshrined at `frontmatter.test.ts:5009-5016` as a "safe-direction contract" — a framing that does not survive contact with (a) and (b), which share its root cause and are neither safe nor recorded.
  1. **Rename to `nodeStarted` and set it at both places the node can begin:** `cur.nodeStarted = v !== ""` on the key line, and `if (!cur.nodeStarted && text !== "") cur.nodeStarted = true` on the continuation path after the line is consumed as content.
  2. **One deliberate exception, stated at the site:** a **block sequence** continues to admit node starts at each `- ` at the item indent, so the item path sets a separate `seqIndent` rather than `nodeStarted`, and only lines at that indent beginning with `-` re-enter the item rule.
  3. **Direction (a) is pinned with a `grantedAgentNames` assertion, not a token-presence assertion**; (b) and (c) are pinned against the loader. The (c) "contract" is **retired as a contract** — enshrining it retired the only signal that (a) and (b) existed.
  4. **Both differential harnesses gain a second predicate over the same already-loaded values** (WR-03): `namesFrom(String(verdict.value))` compared **EQUAL** to `grantedAgentNames(document)`, with the same three-verdict discipline (a module refusal is never folded into "no names"). Token presence is not the fact KIT-03 is computed over; the **name set** is, and a divergent name set is today structurally invisible to every harness in the round.
  — **Rationale (user-ratified, this round):** an invented name on the `ok:true` arm is the direction the module's own header calls out by name and claims D-48 closed. It is closed for one of the two node-begin places. The fix is the same discipline as D-51 — set the fact where the fact is actually known — and the harness change is what makes the *next* spelling of it visible without a ninth review.

- **D-56 (all ELEVEN round-8 findings close in the SAME round, none deferred — the FIFTH application of the D-41/D-47/D-50/D-53 posture), with each item's disposition recorded:**
  1. **WR-01** — the D-52 differential moved the *expectation* to the loader and left the *corpus* as three hand-listed axes fixed at "key line + exactly two continuation lines", so **neither** CR-01 nor CR-02 is expressible in its 312 cells — not merely untested. **Derive** the key-line axis (or at minimum add the shapes YAML's grammar names: nested block mapping, compact nested sequence, block explicit key, JSON-adjacent flow mapping), make continuation **depth** a fourth axis rather than a constant baked into `buildCellRegion`, and add a non-circularity floor: every family named in `frontmatter.ts`'s ledger (entries one through nine) is **expressible by the generator, by construction rather than by comment**.
  2. **WR-02** — the sweep titled *"no mid-line node start YAML defines returns the SILENT no-grant arm"* backs a universal quantifier with a hand-listed 11-member `CONTEXTS` array, and **the claim is false** (families C and H are mid-line node starts inside a flow collection that return the silent arm). Either derive the contexts from YAML 1.2 §7.4 with a two-sided cardinality assertion, or retitle to the subset it covers and move the universal claim to the generated corpus. A universal title over a literal is the shape the file's own commentary condemns three times.
  3. **WR-04** — the D-52 exemption bounds are **arithmetically incapable of failing** (each `bound` is the full cross-product the shape can produce and each `matches` is a pure function of the same axis flags, so `matched ≤ bound` holds for every possible corpus), and the disagreement/exemption **equality** pressures a future maintainer toward narrowing an exemption when an exempt cell happens to agree. Split it into the honest predicate: no unexplained disagreement, **and** no dead exemption. Either derive the bound from something the exemption cannot control or **delete it and say so** — decoration that reads like a floor is worse than no floor.
  4. **WR-05** — `classifyDelimiter`'s leading-residue refusal names `line.codePointAt(0)`, so ` <ZWSP>---` reports U+0020, sending the reader to a legal character instead of the offending one. Widen `LeadingRun`'s residue arm to carry `firstOutsideDeclared` — `leadingInvisibleRun` already visits that code point and discards it. The whole point of the D-44/D-50 refusal wording is to name the offending byte.
  5. **IN-01** — `kit-model.ts`'s `MARKDOWN_EXT` comment claims two rules turn on it while the constant is referenced at exactly one site and the literal `".md"` is spelled at `:567` and `:728`. Use the constant at both, or correct the comment. The anti-drift device drifted from itself, in the file whose thesis is that a fact has one statement.
  6. **IN-02** — after 27-46, `doubleClaimed` filters over `schemaKeys`, so a **foreign** key claimed by two buckets is now unreportable rather than reported once, and `kit-model.test.ts:906` pins that blind spot as expected behaviour. Filter over `new Set([...schemaKeys, ...claimedKeys])` so a double-claimed foreign key lands in both arms once each — which is what a human reading the failure message needs to fix both buckets.
  7. **IN-03** — the "PURE BY CONSTRUCTION" case slices `frontmatter.ts` with `indexOf("\n}")` and never asserts the slice is the function, so a reformat makes every `not.toContain` pass vacuously. Assert the slice is the thing before inspecting it — the same "assertion that cannot fail" shape this phase closed twice in round 7.
  8. **IN-04** — the round-8 role-adapter fixture strips fence **delimiters** and leaves fenced **content** live in the body, guarded only by "at least one line was removed". Drop lines between toggles (the rule `stripFencedBlocks` applies) and assert positively that the written file contains no `---` after line 0, so the case's premise is checked rather than assumed.
  9. **The 27-43 acceptance criterion is RETIRED** — *"`scripts/validate-agent-factory.ts` goes from exit 0 to a named non-zero failure on the non-coordinator adapter surface"*, carried open and unowned across 27-43 → 27-46. **User decision, this round:** retire it. The validator has **zero** occurrences of `spawn`/`frontmatter`/`wr05` (measured, not assumed) and is a structure validator, never a spawn-grant surface; adding a second spawn-grant predicate there would create exactly the weaker-duplicate shape this phase has spent eight rounds deleting. The retirement is **recorded with its reason** — never a silent drop.
  10. **SPAWN-03 stays deferred to Phase 33 (GAP-D1)** — the live-platform capture proving the coordinator's `Agent(<allowlist>)` grant is runtime-honored is `UNKNOWN - verify` and is **not** closable by any static gate. This is a **record-don't-fix** disposition with its reason, consistent with ROADMAP Phase 33 (CAP-01).
  — **Rationale (user-ratified, this round):** every round of this phase that deferred a warning saw it return, usually one abstraction level down — WR-01 is the fifth consecutive round in which the harness that should have caught the defect could not express it. D-41, D-47, D-50 and D-53 each established "the carried warnings close in the same round, none deferred," and the user ratified the fifth application over carrying nine items into a round 10.

### Gap-closure round 10 (ADDED 2026-08-09 — user decisions, after the round-9 code review left TWO live silent-no-grant bypasses standing at exit 0)

> **What round 10 inherits.** `27-51` closed **CR-01** (the `''` escape inside an open single-quoted scalar) structurally, proven by a gate-level exit 0 → exit 1 move on both distribution twins and by 180 unsafe cells reported by the same corpus digest against a pre-fix mirror. It did **not** close **family G / G2** — the nested block-scalar header — which has now been re-measured byte-identical against the committed build by `27-47`, `27-48`, `27-49`, `27-50` and `27-51` and left explicitly OPEN in `deferred-items.md` every time. `27-52` owns it, and D-57 is the disposition the ledger has been demanding as a *planning* decision since `27-47`.

- **D-57 (a `|` / `>` block-scalar header is recognised at EVERY node-start position YAML allows one, and the scalar's end is DERIVED from YAML 1.2's own more-indented-block indentation rule — never guessed):** `BLOCK_INDICATOR` (`scripts/frontmatter.ts:324`) is applied at exactly ONE of the positions YAML allows a block-scalar header — `flattenBlock`'s top-level key line (`:1419`). A header appearing as a **nested mapping's value** (`tools:` / `  nested: >-`), as a **block-sequence item** (`  - >-`), or deeper is never recognised, so `cur.block` stays false and the block scalar's LITERAL content is routed through `stripComment`, where a leading `#` is stripped as a comment and a leading `-` invents a sequence item. A live `Agent(grugops-orchestrator)` is deleted and the module returns the `{ok:true, value:false}` **silent no-grant SUCCESS arm** while `/usr/bin/ruby -ryaml` accepts the document with the grant plainly in the loaded value. This is the **same failure class one abstraction level up from CR-01**: a predicate recognised at one of the several positions its grammar allows. It is the predicate's **APPLICATION SET**, not its conditions — the D-30 question ("which set does this enumerate?") asked about POSITIONS.
  1. **Recognise the header at every node-start position**, by applying the SAME `BLOCK_INDICATOR` constant at the block-sequence item path and the continuation path — the two places the structural position is actually known — rather than by widening the constant's pattern or writing a second indicator grammar beside it. `BLOCK_INDICATOR` remains the one authority on what a header LOOKS LIKE; the defect is WHERE it is asked.
  2. **The end condition is YAML 1.2's own more-indented-block rule, DERIVED, never guessed.** The header line's indentation is recorded on the accumulator when the header is recognised; content belongs to the scalar while its indentation strictly exceeds that, and the first line at or below it ends the scalar. Never a fixed depth, never a line-shape heuristic.
  3. **Inside a recognised block scalar, `stripComment`'s comment condition and `SEQ_ITEM` do not apply** — YAML gives neither construct its meaning there. This is the same asymmetry `unquoteChecked` already honours for block scalars under D-50 / IN-02.
  4. **The movement is fail-safe only**, and it is measured rather than argued: a document that granted before still grants, and no loader-accepted document newly returns a SHORTER value (KIT-03 / D-09 — a name is never silently dropped or altered when computing the grant closure).
  5. **The corpus grows WITH the fix, in the same plan** — the nested-block-scalar header enters `AXIS_KEY_LINE` only after the family is green, per `27-49`'s recorded reason (a corpus shape for a still-live silent no-grant puts the differential's never-exemptible `silentWhileLoaderGrants` direction into failure). No exemption may be added to that direction to make the new shape pass.
  — **Rationale (user-ratified, this round):** option A is the only disposition whose answer equals the loader's on all seven measured rows (G, G2, g1–g5), and its end condition is YAML's own rule rather than a heuristic — exactly the derive-versus-enumerate distinction this phase has paid nine rounds to learn. It predicts **zero** false reds on the four tracked documents that already carry a nested block-scalar header, and it fixes row g5's still-wrong name set as a side effect rather than merely making it less wrong.
  — **Accepted cost, stated rather than discovered:** this adds **indentation tracking to the flattener** — the "genuine nesting / YAML engine" direction **D-54 rejects by name** — so the module's declared scope widens, and later rounds re-cut the generated corpus, the repository-wide value map and the gate baselines against the widened contract. The user was shown this cost and accepted it.
  — **Reversibility: ONE-WAY.** Once the flattener carries block-scalar nesting, every later round's generated corpus, repository-wide value map and gate baselines are re-cut against the widened contract, and the module's declared scope changes with it. Reverting after subsequent rounds build on those baselines is not a clean revert, which is why this was gated at a `checkpoint:decision` rather than settled inside the implementing task.
  — **REJECTED — `option-b-refuse` (route a nested block-scalar header to the failure arm):** it has a **measured** false-red cost of **4** loader-accepted tracked documents (`.planning/milestones/v1.2-MILESTONE-AUDIT.md` `evidence: >`, `15-VERIFICATION.md` `note: >`, `25-VERIFICATION.md` `reason: >`, `27-VERIFICATION.md` `reason: >`), none of them an adapter, a skill or a packaging template. A refusal over `.planning/` prose **exceeds this module's declared scope**, which is the open scope question rather than an answer to it, and row g5's altered name set would stay wrong for a different reason. Recorded, not silently dropped.
  — **REJECTED — `option-c-recognise-failsafe` (recognise everywhere, treat the key's whole remaining region as literal without computing an end):** the end condition would be **guessed rather than derived**, over-inclusion can fold a sibling key's value into the block scalar and move the flattened value away from the loader's on documents the loader accepts, and it is option A with a worse end — very likely re-cut to option A in a later round, which is churn this phase can least afford. Recorded, not silently dropped.
  — **This decision ALSO settles, once, the scope question the `27-48` ledger entry raises from the other symptom** — the eleven of 649 loader-accepted `(file, key)` cells whose flattened value disagrees with the loader's content signature on BOTH builds, none of them a `tools` / `allowed-tools` / `coordinator` / `name` key. That entry's own suggested direction was to settle the scope ONCE, with the flattener-nesting decision, rather than twice from two symptoms. **Disposition under the widened contract:** those cells are deeply-nested `.planning/` metadata whose value the flattener collapses; recognising nested block scalars moves the block-scalar-bearing subset toward the loader's value by construction (the same mechanism that closes the `27-47` false-red on `reason: >`), and the residual — cells that diverge for reasons other than a block scalar — stays **in scope for this module's declared token-presence contract but explicitly NOT a defect**, because the flattener produces a value map and not a YAML tree. `27-52` re-measures the eleven against the post-fix build and records the count that moved, so the residual is a number rather than a category. The scope question is closed here and is not to be re-opened from a third symptom.

- **D-58 (a Phase-27 requirement row is `[x]` / `Complete` exactly when the most recent verification report records it SATISFIED with cited evidence — one convention, applied identically to all ten rows, never per-row judgement):** at the time this decision was taken, `REQUIREMENTS.md` carried KIT-02 as `[x]` / `Complete` and KIT-01, SPAWN-01, SPAWN-02, SPAWN-05, SPAWN-06 and SPAWN-07 as `[ ]` / `Gaps Found`, while `27-VERIFICATION.md` records **all seven** as SATISFIED with cited evidence. The same report calls SPAWN-02's state an "under-claim" that must be corrected while calling five identical states "checklist lags until the phase closes." **Those two sentences cannot both be the rule**, and a traceability table governed by no stated rule is not proof of anything — which collides directly with this project's binding constraint that *the trace is the proof*. Fixing only the row the report happened to name would have left the same inconsistency with one fewer instance.
  1. **The rule, in one sentence:** a requirement is `[x]` / `Complete` when the most recent verification report records it SATISFIED with cited evidence, independently of whether the phase has sealed; a requirement whose most recent verification records it FAILED, BLOCKED or NEEDS-HUMAN stays `[ ]` and carries the status that verification gave it.
  2. **Both sites move together.** The checkbox in the requirements list and the Status cell in the traceability table are two renderings of one fact. A row whose two sites disagree is a defect regardless of which one is "right", and every application of this rule asserts the pair after the edit.
  3. **A `Complete` row cites a transcript re-run in the plan that marks it** — the command and its result, never a figure carried forward from a prior round's summary. This is the D-19 posture applied to the record itself: the row is derivable by a reader from the rule plus the transcript, with no story needed to explain it.
  4. **Only a verification round may flip a row to Complete on the strength of new work.** An executing plan applies the rule to what the *most recent* verification already recorded; it never promotes a row because its own tasks targeted that requirement's defect. This is why KIT-03 and SPAWN-04 stay `[ ]` / `Gaps Found` here even though `27-51` and `27-52` closed the two bypasses their FAILED status rests on — nine rounds of this phase have paid for what a premature flip costs, and commit `47d7820` had already reverted exactly this pair once.
  5. **SPAWN-03 is the rule's NEEDS-HUMAN arm, not an exception to it.** Its most recent verification records NEEDS HUMAN (deferred), so it stays `[ ]` with a status naming its Phase-33 / GAP-D1 / CAP-01 deferral, ratified as D-56 item 10. Under this rule that is the ordinary outcome rather than a special case, which is precisely why the rule is preferable to per-row judgement.
  — **Rationale (recorded this round):** convention V is the only reading under which a reader can DERIVE a row from a transcript rather than reconstruct it from phase history; it is what KIT-02 already carried, so it requires no un-marking of a correct row; and it satisfies the project's own "the trace is the proof" constraint by making the traceability table a function of the verification record instead of a hand-maintained parallel account of it. A hand-maintained table drifting from the reality it claims to describe is this milestone's founding defect (the "41 vs 46" count correction in `REQUIREMENTS.md` itself) in a third instance.
  — **REJECTED — `convention-P-phase-led` (no requirement flips until the phase seals; all ten stay `[ ]` and KIT-02 is reverted to match):** defensible, and simpler to audit at exactly one point in time. Rejected because the table then carries **no per-requirement information at all** until the very end — a reader mid-phase cannot tell a verified requirement from a blocked one — and because it requires un-marking KIT-02, which is currently the only row that agrees with its own evidence. Recorded, not silently dropped.
  — **Reversibility: CLEAN.** `gsd-tools query requirements.revert-phase` un-does the whole phase's marks in one call, so this is recorded rather than gated at a checkpoint. Nothing downstream is re-cut against it; the decision moves marks in a record, not behaviour in a build.

### Gap-closure round 11 (ADDED 2026-08-10 — executor decision recorded during `27-55`, after the round-10 code review found a NEW regression shipped inside round 10's own fix)

> **Round posture.** All ten round-10 items close in this round. `27-55` owns **CR-01-new**, the silent-no-grant path D-57 introduced: the block-scalar quoting exemption was moved onto a **sticky per-key** flag, so one nested block scalar anywhere in a key switched the D-30 escape refusal off for **every other part of that key**. Measured on the committed build `3c7930b`: `tools:` / `  a: "\x41gent(grugops-orchestrator)"` REFUSED, and the same document with an unrelated `  b: >-` / `    x` appended returned `{"ok":true,"value":false}` while `/usr/bin/ruby -ryaml` loads `{"a"=>"Agent(grugops-orchestrator)","b"=>"x"}`. This is a **scope** bug, not a condition bug: a *key* property standing in for a *region* property.

- **D-59 (the quoting exemption a block scalar earns is a property of the REGION that block scalar covers, and the resolution unit is the maximal RUN of like-kind regions — never the key):** `Accumulator.parts` becomes `Part[]`, where a `Part` records its own `intro` (the `key:` a nested header prints in FRONT of the scalar, outside it), its `body`, and whether that body is a block scalar's content. The sticky per-key flag is **deleted**, not tuned. The flush walks the regions in maximal runs of like kind: a block-owned run is exempt, and every other run answers to `unquoteChecked` exactly as the whole key did before this decision.
  1. **The flag ceases to exist.** A replacement boolean whose lifetime is the key would be the defect and not its tuning, so its absence is asserted by an interface-shape case rather than described in prose.
  2. **The introduction is validated, never exempted.** `header.leading` is not inside the scalar, so YAML's quoting rules apply to it and it is routed through `unquoteChecked`. Today's `KEY_LINE` key alphabet (`[A-Za-z_][A-Za-z0-9_-]*`) and `BLOCK_MAP_EXPLICIT`'s `?` / `:` make that a provable no-op, so no document's value moves — it is checked anyway so the rule does not rest on an alphabet declared two hundred lines away.
  3. **The exemption is scoped, never removed.** A non-allowlisted backslash sequence inside a block scalar's own content is still content and still does not refuse (D-50 / IN-02, row U6). `DQ_ESCAPE_ALLOWLIST` is byte-unchanged in length and membership, asserted.
  — **Rationale (measured, this round):** the plan's PREFERRED disposition (i) — resolve at the point of effect on the continuation path and delete the flush's quoting decision entirely — **cannot preserve the folded-continuation join byte-for-byte**, which is the condition the plan itself names for the fallback. It was implemented and measured first: it moved two shipped values, flattening `tools:` / `  - Read` / `  -` / `    "Write,` / `    # x, Agent(…)"` to `Read, Write, # x, Agent(…)` where every build since D-51 flattens it to `Read, "Write, # x, Agent(…)"`, because the second region ALONE is a wholly-quoted scalar while the joined value is not. That directly contradicts **D-33**, which puts the unquote on the JOINED value so YAML's line folding meets this module's join. A scope fix that moves values for reasons unrelated to the defect is a second defect.
  — **CHOSEN — disposition (ii), with the resolution unit stated as the maximal RUN of like-kind regions.** A run boundary is precisely a change of kind, so for every key carrying no block scalar there is exactly **one** run and the result is byte-identical to the pre-D-59 flush. Confirmed by a repository-wide value map over `git ls-files '*.md'`: **1170** files scanned, **0** moved, **0** new refusals.
  — **REJECTED — disposition (i) (resolve each region individually at push time):** rejected on the measurement above, not on taste. Recorded, not silently dropped.
  — **REGION IDENTITY, STATED AND ASSERTED RATHER THAN ASSUMED — and the question is DISSOLVED rather than answered.** A per-region fact needs a stable handle only if it is stored APART from the region (an index into `parts`, a set of owned offsets); then the handle's stability becomes a property somebody must keep proving as the flattener grows. Storing the fact **on** the region removes the question: the flag travels with the text it describes through every push, fold and join. The supporting invariant is asserted anyway, at two levels — `assertFoldTargetIsNotBlockOwned` throws at the one site a region is mutated after it is pushed, and a case pins that no path in `flattenBlock` splices, reorders, removes, sorts or pops an already-pushed region, with the push-site count fixed at 4 so a fifth arrives loudly.
  — **Reversibility: COSTLY.** Moving the resolution point re-cuts where refusal reasons originate, and two shipped assertions match a refusal reason on the substring `anchor or alias`. The control row's reason string is pinned byte-for-byte against the round-10 build for that reason.

- **D-60 (the block-scalar header recogniser's implicit-key introduction gets its OWN production, derived from YAML's mapping-value rule; `KEY_LINE` loses its second job rather than gaining a wider alphabet)** — plan `27-56`, closing **CR-03**. `blockHeaderAt` asked `KEY_LINE` for the `key: <header>` spelling. `KEY_LINE` is the deliberately narrow grammar for the **top-level** keys a frontmatter block may declare, and its narrowness there is an *intended refusal*. Borrowing it silently transferred that narrowness to **nested** mapping keys, where YAML allows any scalar — so a quoted, dotted, digit-leading or space-containing nested key introduced no header, `block` stayed false, the scalar's literal content reached `stripComment`, and a leading `#` deleted the rest of the line. Measured on the committed build `ac6653c`, loader column `/usr/bin/ruby -ryaml` (ruby 2.6.10 / psych 3.1.0 / libyaml 0.2.1), each row carrying `Read, # x, Agent(grugops-orchestrator)` on a more-indented line: `"a b": >-`, `a.b: >-`, `1a: >-` and `a b: >-` all returned `{"ok":true,"value":false}` while the loader read the grant plainly in the loaded value. This is neither a condition bug (D-54) nor a position bug (D-57): it is **one grammar doing two jobs** — the weaker-duplicate shape wearing a single definition site.
  1. **The top-level grammar is NOT widened.** `KEY_LINE` is byte-unchanged and keeps its first job; a top-level line it refuses still refuses with a byte-identical reason, asserted over the same four spellings the nested question now admits. Widening it would have changed which top-level lines are unreadable — a separate contract with its own recorded reason.
  2. **No second block-indicator grammar.** `BLOCK_INDICATOR` remains the one authority on what a header *looks* like; the new production decides only where the nested **key ends** so the text after it can be offered to that constant. The `mappingValueIndicator` position gate is byte-unchanged and all eight of its measured loader rows keep their recorded verdicts.
  — **THE KEY-END DISPOSITION — CHOSEN: the FIRST colon that carries a separation, with a quoted key delimiting itself.** YAML's implicit key is a scalar and a scalar delimits itself: a *plain* key cannot contain `:` followed by a separation (§ 7.3.3 excludes it from `ns-plain-char`), so the key ends at the first such colon; a *quoted* key is a JSON-like key (§ 7.4.2) that ends at its own closing quote, after which optional separation may precede the indicator.
  — **REJECTED — the LAST such colon.** Adjudicated against the loader rather than argued: `tools:` / `  a: b: >-` and `tools:` / `  a b: c: >-` are documents libyaml **REJECTS** ("mapping values are not allowed in this context"), which is exactly what FIRST predicts and LAST does not — LAST would read mapping structure into a document no loader accepts. FIRST is also the safe direction where the two disagree, because it recognises no header there and therefore removes no bytes from any value. Measured alongside: a colon **not** followed by a separation is ordinary key text (`a:b: >-` loads under the key `a:b`), so the test is on the separation and never on the colon alone; and an **unterminated** quote closes nothing, so there is no implicit key on that line and the module recognises no header — byte-unchanged for `'a b: >-`, which libyaml rejects outright.
  — **THE ENCODING UNIT, STATED (KIT-03).** Every index in the production is a **UTF-16 code unit**, JavaScript's native string index and the same unit `indentOf`, `excerpt` and `resolveDoubleQuoted` already use. The production **compares no key to a set and measures no key against a bound** — the key is sliced out and carried verbatim, never normalised — so the only thing the unit could affect is where a boundary falls, and it cannot: every delimiter scanned for is ASCII (`:`, ` `, `\t`, `"`, `'`, `\`) and no UTF-16 code unit of any non-ASCII character can equal one (non-ASCII BMP code points are ≥ U+0080; surrogates are U+D800..U+DFFF). Pinned rather than asserted: a precomposed `été` key (9 code units / 11 bytes) and a combining-mark `été` key (11 code units / 13 bytes) both grant, both agreeing with the loader.
  — **Consequence recorded rather than discovered later.** D-59 routed the header's `key:` introduction through `unquoteChecked` and noted it was a *provable no-op* under `KEY_LINE`'s alphabet, checked anyway so the rule would not rest on an alphabet declared two hundred lines away. One round later the alphabet moved: a nested key may now be quoted, so `tools:` / `  "a\x41b": >-` reaches that call and refuses by name. The D-59 comment claiming the no-op is corrected at the site.
  — **Reversibility: COSTLY.** Widening which nested keys can introduce a header re-cuts the generated corpus, the repository-wide value map and the gate baselines later plans in this round compare against.

- **D-61 (a node PROPERTY never hides a node start — the property is stripped at EVERY header introduction the recogniser knows about, using the module's existing authority, and the reference refusal gains a fourth application point at the node start following a mapping separator)** — plan `27-57`, closing **CR-02**, the one round-11 bypass the verifier independently reproduced **end to end through the full gate**. YAML 1.2 § 6.9 lets a node's properties — its tag and/or its anchor — stand in front of the node's content, so `nested: &a >-`, `nested: !!str >-`, `: &a >-` and `? &a >-` are all legal block-scalar headers. `blockHeaderAt` asked `BLOCK_INDICATOR` about the text immediately after the introduction, that text began with the property rather than with the indicator, no header was recognised, `block` stayed false, the scalar's literal content reached `stripComment`, and a leading `#` deleted the rest of the line — **D-57's exact mechanism, one property over**. Measured on the committed build `6189744`, loader column `/usr/bin/ruby -ryaml` (ruby 2.6.10 / psych 3.1.0 / libyaml 0.2.1), each row carrying `Read, # x, Agent(grugops-orchestrator)`: **seven** loader-ACCEPTED spellings returned `{"ok":true,"value":false}` while the loader read the grant plainly in the loaded value. Planted into the EXISTING allow-list key of both distribution twins of the non-coordinator skill `map` on a hermetic `git archive HEAD` mirror, `node scripts/check-foundation-guards.js` printed `ALL CHECKS PASSED` at **exit 0**.
  1. **The introduction set became DATA, and both questions iterate it.** D-57 enumerated the four introductions correctly from YAML's grammar and wrote them as four straight-line branches. Four branches are four places to forget, and the review reported the failure at only **two** of them. `HEADER_INTRODUCTIONS` is now the position vocabulary; `blockHeaderAt` loops it; a fifth introduction inherits the property strip **by construction** rather than by a future author remembering. A case reads the declared member names out of the module's own source and asserts the per-introduction probe list equals them, so the two cannot drift.
  2. **No second property grammar was written.** `NODE_PROPERTY_AT_NODE_START` already declares the tag and anchor forms, from the same grammar `LEADING_TAG` and `YAML_REF` declare. It is **reused**, and its deliberately-wide verbatim alternative (D-54's measured decision) is **not narrowed** — narrowing it opens the value-SHORTEN direction that decision closed. The count of node-property pattern definition sites, taken over code lines only, is **1** before and after.
  3. **The strip honours YAML's own bound: at most one tag and one anchor, never two of a kind.** § 6.9's `c-ns-properties` is (tag [anchor]) | (anchor [tag]). Adjudicated: `nested: !!str &a >-` and `nested: &a !!str >-` both load with the grant, while `nested: &a &b >-` is a document libyaml **REJECTS** — so the two-of-a-kind spelling is left unstripped, no header is recognised, and it reaches the LOUD refusal arm.
  4. **The reference refusal gains APPLICATION POINT 4 OF 4.** It was asked at offset 0 of the physical line and at each flow fragment; a BLOCK mapping's separator introduces a node start too, and nobody asked. So `nested: *a >-` — a property this module cannot strip — returned `{"ok":true,"value":false}` for a document libyaml rejects outright. The new point is derived from the SAME introduction set (`postSeparator`), so it cannot fall behind the strip, and it is gated with the header's own `mappingValueIndicator || startsNode` expression rather than a new one.
  — **CHOSEN: the property is asked at every introduction the recogniser declares, by iterating the declared set.**
  — **REJECTED — closing the two introductions the review named (`nested: &a >-` and `nested: !!str >-`) and leaving the explicit-key and explicit-value forms.** Rejected because it is the enumerate-the-bad shape this module has declined eleven times, and because it was **measured false**: the explicit `: &a >-` and `? &a >-` forms are live silent-no-grants on loader-ACCEPTED documents in exactly the same way. A predicate closed at two of the four positions its grammar defines is the same defect as one closed at none. Recorded, not silently dropped.
  — **WHAT IS DELIBERATELY LEFT REFUSING, AND IT IS THE CONTRAST THAT PROVES THE DIAGNOSIS.** A BARE header carrying a property (`tools:` / `  &a >-`) and the block-sequence item form (`  - &a >-`) both still REFUSE, with byte-identical reasons, because at those the sigil IS at offset 0 of the node and the module's standing anchor/alias refusal reaches it first. Both are documents libyaml accepts with the grant in the loaded value; refusing them is this module's recorded disposition on an unresolved node property (the LOUD arm), not a gap. The defect was never the sigil test — it was which POSITIONS stood in front of it.
  — **THE PLATFORM BOUND, RESTATED VERBATIM AND NOT INFERRED AWAY: `UNKNOWN - verify`.** Whether Claude Code itself honours a mapping under an allow-list key as a tool grant was **not** confirmed against the platform, and no live platform escalation is claimed. The finding stands on this module's own stated contract — the token is in the loaded value of the allow-list key and the guard reads it as a no-grant.
  — **Reversibility: COSTLY.** The fourth application point changes which documents fail loud, and the repository-wide value map and gate baselines are re-cut against it by `27-59`. Reverting after that means re-cutting them together.

- **D-62 (a block scalar ends where YAML says it ends — at the scalar's OWN detected content indentation, with the header line's indent kept as the FLOOR; the explicit indentation-indicator digit is HONOURED; and a blank line inside an open scalar is CONTENT, folded through the line break the indicator already derives)** — plan `27-58`, closing **WR-01**, **WR-02** and **IN-01**, which are one root cause seen from three sides: the block-scalar machinery D-57 added decides a scalar's extent and a scalar's joins from facts that are not the ones YAML uses. Loader column for every row below is `/usr/bin/ruby -ryaml` (ruby 2.6.10 / psych 3.1.0 / libyaml 0.2.1), measured against the committed build `17c1b58`.
  1. **The end condition was measured from the wrong landmark.** D-57's content branch kept a line inside the scalar while it was more indented than the **header line**. YAML 1.2 § 8.1.1.1 auto-detects the content indentation from the scalar's **first non-empty content line** and ends the scalar at the first line less indented than **that**. The two coincide wherever the first content line sits at the minimum — nearly every real document, which is why it survived a round — and diverge exactly where it does not. Measured: `tools:` / `  nested: >-` / `        Read,` / `    # x, Agent(grugops-orchestrator)` returned a **GRANT** with names `["grugops-orchestrator"]` while the loader expresses `{"nested"=>"Read,"}` and grants nothing. That is a module grant the loader does not have — the direction the module's own doc block names **never exemptible**, shipped by the change that stated it. `blockContentIndent` is set once, at the first non-empty content line; `blockIndent` keeps its job as the floor the detected indent must exceed.
  2. **The threshold is measured on BOTH sides, because a boundary asserted on one side is half a boundary.** With the detected indentation at 6: a line at **6** is INSIDE (`{"nested"=>"Read, # x, Agent(grugops-orchestrator)"}`), a line at **5** is OUTSIDE (`{"nested"=>"Read,"}`), a line at **4** is OUTSIDE. Module and loader agree on all three after the fix. (This is KIT-03's `boundary` probe row for the round.)
  3. **The indentation UNIT is stated at the site: `indentOf`'s count of leading `[ \t]` characters, one column per character, a tab counted as ONE.** Both sides of the comparison are taken by that one helper from the raw line, so no rounding, truncation or unit mismatch is possible between the header line's measurement and a content line's — they are literally the same function of the same shape of input. A tab **inside** an indentation run is not a case that equivalence must survive, and that is a recorded measurement rather than an assumption: `/usr/bin/ruby -ryaml` REJECTS both spellings outright (*"found a tab character where an indentation space is expected while scanning a block scalar"*), so there is no loader value to agree or disagree with. The module's answer for both is recorded rather than asserted correct. (KIT-03's `precision` / `backstop` probe row.)
  4. **The explicit indentation-indicator digit is HONOURED, and the alternative is rejected BY MEASUREMENT.** `BLOCK_INDICATOR` has always matched `[0-9]` — both orders YAML § 8.1.1 permits — and nothing ever read it. That cost nothing while the threshold was the header line. Under the new threshold it stops being free: `tools:` / `  nested: >-2` / `      Read,` / `     # x, Agent(grugops-orchestrator)` is a document the loader **ACCEPTS with the grant in its value** (`{"nested"=>"  Read,\n # x, Agent(grugops-orchestrator)"}`), and auto-detecting it would put the content indent at 8, end the scalar at the line indented 7, and return `Read,` alone — a **silent no-grant created by the fix for an over-inclusion**. The base the digit is added to is the header line's own indent, measured on **eight** rows across all three positions a header can appear at. A digit run that is not a single 1-9 (`>-0`, `>-10`) is a document the loader rejects outright, so the reader returns `null` and the scalar auto-detects.
  — **CHOSEN: honour the digit, and derive the reader FROM `BLOCK_INDICATOR` rather than transcribe it beside the constant.** Candidate spellings are generated, filtered through the real constant, and the reader must agree with the constant's own digit run on every survivor — so narrowing the constant shortens the axis and a drifted copy cannot pass.
  — **REJECTED — continue to discard the digit and auto-detect both spellings, quantifying the divergence.** Rejected because it is not a cosmetic divergence: it trades one over-inclusion for one **silent no-grant** on a loader-accepted, grant-carrying document, which is the trade this module never makes. Recorded, not silently dropped. The matched-and-discarded state the review found is not left in place either way.
  5. **A blank line inside an open block scalar is CONTENT.** D-57 correctly derived the scalar's join from the indicator, because a space join was inventing enumerable names where the loader's line break would be refused. The identical mechanism survived one line-shape over: the paragraph-break skip dropped the blank line before the scalar was ever consulted, and the surrounding lines then joined with a space. Measured: `tools: >` / `  Agent(alpha, ga` / (blank) / `  mma)` enumerated `["alpha","ga mma"]` — two names, one **invented**, on the success arm — for a value the loader expresses as `"Agent(alpha, ga\nmma)\n"`, whose line break this module's own enumeration alphabet REFUSES. The blank line is now handled while the scalar is open, **before** the skip, and folds through the line break the indicator already derived: **this decision adds no second opinion about what a scalar joins with, it extends the axis the existing one is applied to.**
  — **The paragraph-break skip's narrow alphabet is UNCHANGED for every line outside an open scalar.** D-50's recorded reason for that narrowness is about the *direction*: outside a scalar the narrow alphabet routes an invisible-only line into a LOUD refusal the loader agrees with, and widening it would trade that refusal for a silent skip. The change is scoped by the open-scalar fact the accumulator already holds, and the in-block asymmetry control that pins the outside behaviour stays green.
  — **Where the loader-faithful value carries a character the enumeration alphabet refuses, the LOUD refusal arm is the correct answer** — never a shorter or altered name set. A refusal appearing where names used to be is the intended direction and is said so in the case's own assertion message.
  — **Reversibility: COSTLY.** Moving the end threshold and folding the blank line both shorten or re-break flattened values for a measured set of documents, and `27-59` re-cuts the generated corpus and the repository-wide baseline against them. Reverting later means re-cutting those together.

- **D-63 (the round-11 posture: all ten round-10 items close in THIS round, none deferred — the SIXTH application of the close-them-all convention)** — plan `27-61`, closing the round. Round 10 raised **ten** review findings (3 critical + 4 warning + 3 info, tally and body headings reconciled) and **five** further verification items (four named `missing` fixes and the SPAWN-03 deferral). All fifteen carry an explicit written disposition in `deferred-items.md` § **Round 11 disposition register**, partitioning as **14 FIXED + 1 DEFERRED + 0 REJECTED + 0 OPEN**, with the completeness asserted by count (`15 == 15`) so a reader can check it rather than take it.
  — **THE POSTURE, AND ITS ORDINAL.** This is the **sixth** consecutive application of the convention recorded as D-41 (round 6), D-47 (round 8), D-50 (round 9), D-53 and D-56 (round 10): *the carried findings close in the same round; a warning is not deferred to the next one.* **The reason is measured rather than asserted.** Every round of this phase that deferred a warning saw it return, usually one abstraction level down — round 9's WR-01 corpus item returned as round 10's WR-01 threshold item; D-57's own fix shipped CR-01-new inside it; `27-57`'s adversarial pass found **seven** further live positions the review had not named, and `27-58`'s found a live residual against its **own** post-fix build. An item left without a disposition is indistinguishable, to a later reader, from an item that was forgotten.
  1. **Every closure claim cites a transcript taken on the FINAL build**, `ff68c31` — never a figure carried forward from the plan that made the claim, because five later plans edited the same files after the first closure landed. Fifteen families re-measured with the `/usr/bin/ruby -ryaml` loader column; **fifteen still closed**.
  2. **Every pin the round added is proven able to FAIL.** A consolidated ten-revert sweep on one hermetic clone of the shipped build, with **two premise controls recorded first** (revert nothing → green; revert a comment-only change → green), so the sweep measures the EDIT and not the rebuild. Seven reverts red a named assertion; the other three are assertion-shaped edits, where reverting an assertion cannot red anything, and each is pinned instead by the paired plant it exists to catch.
  — **THE RECORD-DON'T-FIX ITEMS, NAMED WITH THEIR DISPOSITIONS.** Three things are deliberately recorded rather than fixed in this round, and none is a silent drop: **SPAWN-03's live-platform capture** stays `UNKNOWN - verify`, owned by Phase 33 / GAP-D1 / CAP-01 — no static gate can produce live-platform evidence and inventing one would be the faked gate `CLAUDE.md` forbids by name; **`27-58`'s blank line inside an open PLAIN scalar** stays open, pinned at its current wrong answer by a named case so a green suite cannot be read as coverage of it, because its fix lives in the continuation-fold path outside the construct D-62 scopes itself to; and **`27-59`'s three families the SHARED corpus cannot see** (R1, R4, R6) stay open as a property of that corpus, reconciled in `27-61` against the whole-suite sweep that does pin all three.
  — **WHAT IS DELIBERATELY NOT DONE.** **KIT-03 and SPAWN-04 are NOT promoted**, even though this round closed exactly the defects their FAILED status rests on. D-58 item 4 reserves promotion to a verification round, and commit `47d7820` already reverted one premature flip of this exact pair. A row promoted by the plan that wrote its fix tells a reader an unsound authorization control is verified — the failure this phase has paid for eleven times.
  — **Reversibility: FREE.** D-63 records a posture and a disposition register; it changes no code and no gate.

- **D-64 (the spawn-grant guard stops INTERPRETING frontmatter and starts ADMITTING a canonical restricted shape, refusing everything else BY NAME; the seven standalone skill twins become GENERATED and BYTE-GATED; `frontmatter.ts` is DEMOTED from safety authority to convenience reader)** — round 12, the structural pivot, ratified by the user 2026-08-10 after eleven rounds. This decision **supersedes the round-11 verification's prescribed remedy**: `27-VERIFICATION.md` names CR-01 (the block-scalar indentation landmark) and CR-02 (the unwired second `blockHeaderAt` call site) as the gaps to close, and both are **dissolved rather than patched** here — each is an exotic-YAML construct that a canonical-form gate refuses loudly instead of silently no-granting. Closing them by widening the parser a twelfth time is explicitly rejected.

  — **THE MEASUREMENT THAT DECIDES IT, taken this session on `bfe30d6`.** Across **all 31 files the guard scans** (17 `.claude/agents/*.md` + 7 `skills/*/SKILL.md` + 7 `.claude/skills/*/SKILL.md`), the frontmatter uses exactly **eight** distinct keys (`name`, `description`, `tools`, `model`, `argument-hint`, `allowed-tools`, `disable-model-invocation`, `coordinator`) and contains **ZERO** anchors, aliases, tags, block scalars, quoted scalars, flow sequences or flow mappings — measured by scanning every frontmatter region for `[&*!]`, `[|>]-?$`, `[` and `{`, which returned empty. **The entire construct space `frontmatter.ts` has been hardened against across eleven rounds and ~19,000 lines appears nowhere in the live corpus and is not wanted there.** The attack surface exists only because the guard elected to interpret arbitrary YAML rather than refuse anything outside the plain shape.

  — **THE ASYMMETRY THAT EXPLAINS ELEVEN ROUNDS.** Every bypass reproduced in every round lands in a `SKILL.md`. The 17 agent adapters are **generated** (SPAWN-01) and **byte-gated** by `adapters-freshness.ts` — SET equality plus BYTE comparison against a fresh regeneration (SPAWN-02) — and **have never been bypassed once**. The 14 `SKILL.md` files are hand-authored and covered by **no** freshness gate: `generate-catalog.ts` writes `docs/catalog/README.md`, not skills, and no `*-freshness.ts` names `SKILL` at all. The strong mechanism already exists in this repo, is proven, and was never extended to the surface that keeps failing.

  — **THE TWINS ARE EXACTLY DERIVABLE, PROVEN BY DIFF.** All six non-root pairs differ by **one line**: `name: <d>` → `name: grugops-<d>`. `grugops-` is 8 bytes, matching the measured 8-byte delta on all six (`gate`, `map`, `plan`, `release`, `ticket`, `uat`). The `grugops` root pair's 448-byte delta is a single **11-line insertion** — the kit-root resolver block — and is already the sole member of `DISTRIBUTION_PAIR_EXEMPT`. Generation is a name rewrite plus one known addendum, not a re-authoring.

  — **WHY CANONICAL-FORM ADMISSION IS THE STRUCTURAL CLOSE, not another heuristic.** The reader admits only `key: <plain scalar>` and `key:` + `  - <plain scalar>` over the eight-key schema, with the plain-scalar alphabet enumerated. Any other byte — `&`, `*`, `!`, `|`, `>`, a quote, `[`, `{`, a tab, an unexpected indent, an unknown key — is a **named loud refusal, never a parse attempt**. This **deletes the silent-no-grant arm by construction**: there is no longer an "I read it and found no grant" path for exotic input, only "canonical, here are the values" or "not canonical, FAIL". Every bypass from rounds 1–11 becomes a loud refusal. This is D-30's enumerate-the-good inversion — already proven on the escape alphabet — applied one level up, at the **document** level, and it is the P22-round-8 terminal lesson (delete the second grammar) finally applied to the surface that has been failing. Rounds **10 and 11 each shipped a regression inside their own fix**; that, not any single finding, is why the parser approach is retired.

  — **THE VACUITY TRAPS, WHICH MUST BE PINNED OR THIS DECISION IS WORSE THAN THE PARSER.** A canonical-form gate passes trivially by refusing everything, or by scanning nothing. Required: **two-sided cardinality** — all 31 live files must ADMIT (a gate that reds the live kit is broken, not strict), and the scan set must be asserted non-empty with its composition counted; **the historical corpus replayed** — every bypass shape from rounds 1–11, including CR-01's indentation family and CR-02's alias-through-compact-mapping, must now produce a **loud named refusal**, and the transcript must show the refusal text rather than merely a non-zero exit; **the exemption list length stated before and after**, unchanged; and the twin byte-gate proven able to FAIL by planting a one-byte drift.

  — **WHAT THIS DECISION DOES NOT DO.** It does not delete `scripts/frontmatter.ts` — eleven non-test modules import it (`kit-model`, `context-io`, `compactor`, `claim`, `check-kit-refs`, `coordinator-resolution-precheck`, `dead-vocabulary`, `generate-asvs-checklist`, `generate-catalog`, `generate-role-adapters`, `adapters-freshness`) and it remains a legitimate convenience reader. It stops being the module that renders a safety verdict. It does not touch **SPAWN-03**, which stays deferred to Phase 33 / GAP-D1 / CAP-01 with status `UNKNOWN - verify`. It does not promote any requirement row — only a verification round may do that (D-58 item 4).

  — **Reversibility: COSTLY, not one-way.** The generator and byte-gate follow an in-repo proven pattern and are revertible per file. The guard's reader swap changes a safety verdict's mechanism and would need its own round to undo, so it earns a checkpoint before the reader is cut over.

  — **AMENDMENT (2026-08-10, same day): TWO OF D-64's OWN MEASUREMENTS WERE FALSIFIED BY THE PLANNER AND RE-CONFIRMED AT THE ORCHESTRATOR. They are corrected here rather than quietly rewritten, because the falsification is the more useful record.** (1) **The scan is 33 files, not 31, and the schema is TEN keys, not eight.** `spawnGrantScan()` includes two packaging templates — `agent-factory/packaging/slash-command.template.md` and `agent-factory/packaging/subagent.frontmatter.md` — whose frontmatter carries `kind:` and `tier:`. The guard states its own composition in `guard_kit_counts`: *"the spawn-grant scan composition holds exactly 33 members (agent 17 + skill 7 + plugin-skill 7 + packaging 2), each part set-equal to its own lister"*. D-64's original figure came from globbing `.claude/agents` and the two skill directories directly instead of asking the guard's own derivation — **a hand-enumerated set literal standing in for an available derivation, which is this repository's second named failure class, committed inside the decision that retires the first one.** (2) **The corpus contains 31 double-quoted scalars** — `description` in all 17 agent adapters, `argument-hint` in all 14 skill files. D-64's "zero exotic constructs" scan tested for `[&*!]`, `[|>]-?$`, `[` and `{` and **never tested for a quote byte**, so the quoted arm was not measured clean, it was not measured at all: the seventeenth instance in this phase of a verification harness returning a result its own premise did not support. Implementing D-64's stated plain-scalar-only alphabet literally would have **refused all 33 live files**, tripping D-64's own vacuity trap 1.

  — **WHAT SURVIVES, AND WHY THE CORRECTION STRENGTHENS THE DESIGN.** Everything D-64 claims about the *exotic* space held exactly on re-measurement: **zero** anchors, aliases, tags, block scalars, single-quoted scalars, flow sequences, flow mappings or tabs across all 33 frontmatter regions, in exactly three line shapes. And the decisive measurement is new: **`tools` and `allowed-tools` carry ZERO quote bytes across all 33 files.** So the reader admits double-quoted scalars ONLY on `description` and `argument-hint`, and admits **plain scalars only on the two keys that render the spawn verdict** — which keeps the double-quote arm and its escape alphabet, the entire D-30 attack family, off the verdict path by construction rather than by parsing it correctly. The corrected schema is the one the plans implement; where a plan's `<measured_baseline>` and this decision disagree, **the plan's re-measurement on the executing build wins and the executor must halt on disagreement rather than reconcile silently.**

### Claude's Discretion

- Exactly which sentences in `orchestrator.md` are tightened to reach 7165B (D-13/D-14 set the target and the method; the specific edits are the implementer's, subject to not touching Phase 29's de-dup targets).
- The concrete `capabilities:` vocabulary and its per-role assignment (D-11 sets the mechanism and the single-source location).
- The generator's template mechanics and file layout, provided output is a thin pointer and byte-gated.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Milestone scope & requirements
- `.planning/REQUIREMENTS.md` — v2.1 requirements; KIT-01..03 and SPAWN-01..07 are this phase. Also the ratified milestone decisions (Claude Code floor v2.1.219+, scope 46 requirements, honesty floor).
- `.planning/ROADMAP.md` § "Phase 27: Spawn Correctness & Kit-Set Authority" — goal, 5 success criteria, the load-bearing intra-phase ordering, and the standing-obligations table (obligation #3 = the `orchestrator.md` trim).

### Research (this phase carries a `--research-phase` flag)
- `.planning/research/SUMMARY.md` — the cross-cutting "hand-maintained set literals drift silently green" finding; the derive-the-set doctrine; Phase-27 deliverable list.
- `.planning/research/STACK.md` §"Findings that bear directly on the v2.1 spawn defect" — finding ① (`Agent(...)` allowlists ignored inside a subagent definition, and what that means for `guard_wr05`), ② (the depth table: v2.1.172–216 = 5, **217–218 = 1 / silently off**, 219+ = 3; `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`), ③ (`coordinator: true` is not a supported CC field — grugops-internal marker only), ④ (background subagents strip most built-ins since v2.1.198; every tool the roles need survives), ⑤ (`AskUserQuestion` removed from every subagent), ⑥ (plugin subagents ignore `hooks`/`mcpServers`/`permissionMode`). Also the v2.1.208 "tools resolve to nothing → refuses to launch" fact.
- `.planning/research/ARCHITECTURE.md` — the `kit-model.ts` / `generate-role-adapters.ts` / `adapters-freshness.ts` seam map and line-precise consumer list. **`:515` is explicitly overridden by D-06 — read D-06 before following its "plain copies" recommendation.**
- `.planning/research/PITFALLS.md` — P-01 (a spawn guard sharing the bug's blind spot); "every new gate reports what it checked, not just PASS."

### Kit substrate this phase edits
- `AGENTS.md` § "Kit vs state" (`:27-36`) — the kit-root resolution rule and the STOP-do-not-hunt requirement that forces D-06.
- `agent-factory/packaging/subagent.frontmatter.md` — the adapter template the generator must be built from; already documents the main-thread-vs-subagent allowlist behavior and the coordinator shape.
- `agent-factory/roles/orchestrator.md` — 7562B; Responsibility 4 (`:44`) holds the existing spawn/degrade sentence to be made capability-keyed; `### Routing matrix` (`:53-64`) is 1188B.
- `agent-factory/roles/_role-switch-protocol.md` — the sequential activation path `/grug` degrades to. **Not in `ROLE_FILES`, no byte ceiling** (see D-15).
- `.claude/agents/grugops-orchestrator.md` — the single existing adapter; `:5` is the 7-name grant with 0 resolving files; `:25` is the surviving handoff prose.
- `.claude/skills/grugops/SKILL.md` — `allowed-tools` lacks `Agent` (the third stacked cause).

### Tooling this phase re-points
- `scripts/check-foundation-guards.ts` — `WR05_SCAN` `:135`, `ADAPTERS` `:244`, `ROLE_FILES` `:282`, `roleCeiling()` `:486` (leave alone — D-17), `guardRoleSize()` `:530`, `CTX_WORKFLOWS` `:593`. Root convention: `CHECK_ROOT` `:70`.
- `scripts/validate-agent-factory.ts` — frozen `WORKFLOWS` `:118` (14, should be 19) and `ROLES` `:144` (16, missing `frontend-ui`). Two-root convention: `VALIDATE_ROOT` `:59` / `VALIDATE_KIT_ROOT` `:68`.
- `scripts/check-kit-refs.ts` — `SCAN` `:45`, `GH_SCAN` `:61`, Assertion 2 (`agent-factory/handoffs/` → zero) `:155-168`, Assertion 3 (`$GRUGOPS_HOME` sites) `:178-189`.
- `install/install.ts` — `SKILLS` `:480`, `AGENT_REL` `:490`, `materializeAdapter()` `:969-1016`, call site `:1288-1301` (currently 2 materialized resolvers + 6 plain skill copies). `install/uninstall.ts` must mirror.
- `scripts/generate-catalog.ts:110-112,154-156` — the role/workflow derivation rules already proved correct; `kit-model` generalizes these.
- `scripts/catalog-freshness.ts`, `scripts/context-freshness.ts` — the byte-freshness pattern `adapters-freshness.ts` mirrors (SPAWN-02). Per the D-07 precedent, a domain freshness check is **standalone**, never folded into the aggregator.

### Project constraints
- `CLAUDE.md` § Constraints — single-source (adapters are thin pointers, never copies); installers idempotent/additive/dry-run/reversible; no fabrication; zero runtime dependencies on host machines. **Note: `CLAUDE.md` still describes handoff packets and a routing Orchestrator — that drift is AUDIT-02 in Phase 28, not this phase.**

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **`generate-catalog.ts:110-112,154-156`** — the exact readdir + `_`-prefix-drop + `/^\d{2}-.+\.md$/` rules `kit-model.ts` generalizes. Do not re-derive them; lift them.
- **`catalog-freshness.ts` / `context-freshness.ts` / `trace-freshness.ts` / `now-running-freshness.ts`** — four existing regenerate-to-temp + byte-compare gates. `adapters-freshness.ts` (SPAWN-02) is a fifth instance of a proven pattern, kept standalone per the D-07 precedent.
- **`agent-factory/packaging/subagent.frontmatter.md`** — a copy-ready adapter template that already carries the correct v2.0 memory wording, the coordinator shape, and the main-thread caveat. The generator should be built from this, not from the drifted live adapter.
- **Role frontmatter (`kind:`, `tier:`)** — an existing structured-field convention, so adding `capabilities:` (D-11) follows precedent rather than inventing one.
- **`## One job` + `## Activates when`** — present in all 17 roles, already phrased as routing triggers. Free `description` source (D-12).
- **`materializeAdapter()` (`install.ts:969`)** — strip-then-inject, content-idempotent, with bounded removal on an unterminated block. Extends from 2 to 17 call sites without redesign.
- **`stripFencedBlocks()` / `matchesOutsideFences()` (`check-foundation-guards.ts:~155-180`)** — fence-aware scanning so documentation examples are never read as live frontmatter. Reuse for `guard_adapter_body`; do not write a second fence parser.

### Established Patterns
- **Fail-closed everywhere.** `CR-01 missing-file fail-red` recurs across the guards (a deleted adapter/AGENTS.md fails red rather than vacuously passing). D-21's two-tier count enforcement follows it.
- **One authority per predicate.** The v2.0 closure doctrine. It drives D-24 (one dead-vocabulary source), D-22 (no fourth root convention), and D-19's rejection of a second detector.
- **Byte ceilings are locked from a measured baseline and never raised** — trim or split instead (`guard_role_size` comment at `:479-484`; milestone Out-of-Scope).
- **Explicit scan sets, never repo-wide greps** (`check-kit-refs.ts:6`, D-13 token economy). Derivation replaces the *literal*, not the *scoping discipline* — a derived set is still a bounded set.
- **Guards report what they checked**, not just PASS (`PITFALLS.md:591`) — `compared 17 adapters, 0 drift`, so a count of zero is visible as the anomaly it is.

### Integration Points
- `kit-model.ts` → `check-foundation-guards.ts`, `validate-agent-factory.ts`, `check-kit-refs.ts`, `generate-catalog.ts`, `generate-role-adapters.ts` (new). Each passes its own already-resolved root (D-22).
- `generate-role-adapters.ts` → `.claude/agents/grugops-<role>.md` × 17, gated by `adapters-freshness.ts`.
- `install.ts` / `uninstall.ts` → self-derive by readdir of the source adapter/skill dirs (D-18); `materializeAdapter()` now runs over all 17 (D-06).
- `guard_wr05` → asserts the degrade path + non-coordinator `Agent` absence (D-05), over the now-derived `WR05_SCAN`.
- KIT-03 oracle → set equality across grant / adapter dir / role corpus (D-09), and must fail RED against the tree state recorded in `<domain>` before it is trusted.

</code_context>

<specifics>
## Specific Ideas

- The user pushed back on the guard-vocabulary framing mid-discussion — *"Didn't we steer away from handoffs and have unified memory?"* — which surfaced the conflation in SPAWN-05's "handoff/single-window prose" wording and produced D-23's both-directions shape. **Handoffs are gone and the shared verified context is the sole memory; nothing in this phase reopens that.** The guard exists only to keep a v2.0 deletion deleted.
- Repeated preference across every area for the structural option over the convenient one: derive rather than list, one authority rather than two, fail closed rather than fail quiet, and refuse mechanisms that are green in the file but false at runtime.
- Explicit distaste for constraint-gaming — the relocation trap (D-15) was flagged and closed rather than used.

</specifics>

<deferred>
## Deferred Ideas

- **Plugin-form adapters** — whether the 17 adapters also ship in the `.claude-plugin/` form. Plugin agents ignore `hooks`/`mcpServers`/`permissionMode` (research finding ⑥) and the plugin cache does not copy files outside the plugin dir, so kit resolution differs there. Raised, not selected for discussion. Candidate for Phase 28's audit or a later packaging pass.
- **KIT-03 RED-evidence methodology** — how the fail-red-on-today's-tree proof is *recorded* when the same phase turns it green. The same methodology question LANG-06 has in Phase 29 ("fails RED on all 17 blocks as acceptance evidence before the rewrite lands"). Worth settling once, in whichever phase reaches it first.
- **SPAWN-07 blast radius** — enumerating every surface that advertises the Claude Code floor plus the `queue.wip_limit` rationale currently written against depth 5. In scope for this phase as a requirement; the *inventory* of surfaces was not walked during discussion.
- **`CLAUDE.md` v2.0 drift** (handoff packets, routing Orchestrator) — belongs to AUDIT-02 in Phase 28. Do not fix it here.
- **`agent-factory/handoffs/.gitkeep` deletion** — standing obligation #5, dispositioned under AUDIT-01 in Phase 28.
- **`roleCeiling()` re-baselining** — Phase 29 (LANG-08) re-baselines byte ceilings exactly once at end of phase. Phase 27 does not touch ceiling *values*, only trims below an existing one.

</deferred>

---

## Open Questions for Research

Flagged deliberately rather than guessed. The phase is planned with `--research-phase`.

1. **`UNKNOWN - verify`: is a skill's `allowed-tools: Agent(a, b, c)` honored as a scoped grant in the main thread?** If yes, `/grug` gains full parallel capability and D-02's degrade becomes a fallback rather than the norm. If no, D-02 stands as the primary `/grug` behavior. Do not assume either way — this decides how the brand's headline entry behaves.
2. **Main-thread coordinator wiring validated against the real installed flow.** ROADMAP flags this as a genuine platform-schema integration point grugops has not used before. D-01 removes the installer from the equation, but the `claude --agent` path still needs verification against a materialized adapter (does `--agent` resolve a project `.claude/agents/` entry carrying an injected `KIT=` block correctly?).
3. **Materializing 17 resolvers vs `check-kit-refs` Assertion 3.** D-07 sets the direction (derived predicate); the researcher should confirm no other assertion or test pins the "exactly three `$GRUGOPS_HOME` sites" count.
4. **Claude Code depth/width caps against the grant.** Depth 3 on v2.1.219+ (`CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`), 200 subagents/session, 20 concurrent. grugops's width cap of 3 sits inside all three — confirm and correct the `queue.wip_limit` rationale text (SPAWN-07).

---

*Phase: 27-Spawn Correctness & Kit-Set Authority*
*Context gathered: 2026-07-28*

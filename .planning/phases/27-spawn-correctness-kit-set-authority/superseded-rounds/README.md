# Superseded verification records — Phase 27

These are the round-9 and round-10 verification reports, moved here on 2026-08-11.

They are **accurate historical records** and their `status: gaps_found` is **correct and
deliberately unchanged** — both rounds genuinely found live bypasses. They were moved out of
the phase directory only so they stop being scanned as *active* verification for a phase that
has advanced eleven rounds past them.

**Nothing here was rewritten.** The active record is `../27-VERIFICATION.md` (the round-11
report, carrying a human override — read its `## Acknowledged Gaps` section).

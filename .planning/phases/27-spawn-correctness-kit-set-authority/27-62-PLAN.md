---
phase: 27-spawn-correctness-kit-set-authority
plan: 62
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [KIT-03, SPAWN-04]
files_modified:
  - scripts/canonical-frontmatter.ts
  - scripts/canonical-frontmatter.js
  - scripts/canonical-frontmatter.test.ts

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-64 Part A, a NEW module scripts/canonical-frontmatter.ts renders the admission verdict: it ADMITS only a restricted canonical shape and REFUSES every other byte by a NAMED code. There is no third outcome — the silent-no-grant arm that produced eleven consecutive rounds of bypasses does not exist in this module by construction."
    - "TWO-SIDED CARDINALITY (D-64 vacuity trap 1, side A): all 33 files in the guard's live spawn-grant scan are ADMITTED. A canonical-form gate that reds the live kit is broken, not strict. The count is asserted per part (17 agent adapters + 7 source skills + 7 standalone skill twins + 2 packaging templates) and the total is asserted non-empty and equal to the derived scan length."
    - "The admitted grammar is the MEASURED one, not a restated one. D-64's premise measurement scanned 31 files (it omitted the two packaging templates, hence omitted the keys `kind` and `tier`) and never scanned for quote bytes (it missed 31 double-quoted scalars on `description` and `argument-hint`). Implementing D-64's stated eight-key plain-only alphabet literally would REFUSE all 33 live files and violate its own vacuity trap 1. The schema is therefore ten keys, and the value grammar carries a restricted double-quoted arm."
    - "The grant-bearing keys admit PLAIN ONLY. `tools` and `allowed-tools` accept a plain scalar or a block sequence of plain scalars and nothing else; a quoted value on a grant key is a named refusal. This is measured true on the live corpus today (zero quoted values on either grant key) and it keeps the double-quoted escape alphabet — the whole D-30 attack family — permanently outside the path that renders a spawn verdict."
    - "Every refusal is a member of an enumerated code set. There is no catch-all code and no default branch that could report an unreadable document as a readable one carrying no grant."
    - "A green suite is a FLOOR here, never closure evidence (D-64 vacuity traps). Closure evidence for this plan is the printed admission transcript over the live scan and the printed refusal codes."
  artifacts:
    - "scripts/canonical-frontmatter.ts — the admission reader, its ten-key schema, its enumerated plain-scalar alphabet, its restricted double-quoted arm, and its enumerated refusal-code set"
    - "scripts/canonical-frontmatter.js — the committed build, which is what the guard and the tests actually execute"
    - "scripts/canonical-frontmatter.test.ts — the two-sided cardinality pins and the refusal-vocabulary completeness pins"
  key_links:
    - "canonical-frontmatter.ts -> kit-model.ts spawnGrantScan(): the live corpus side of the cardinality assertion is READ FROM THE SAME AUTHORITY the guard scans, never from a second directory listing. A private readdir here would be the set-literal drift class this repo has already paid for."
    - "canonical-frontmatter.ts -> frontmatter.ts stripFencedBlocks: the ONE fence implementation in this tree is reused, never re-derived. The packaging templates carry example frontmatter inside fences and admit only after that strip."
    - "The refusal-code set -> plan 27-63's historical corpus: every bypass shape from rounds 1-11 must land on a member of this set, which is what dissolves CR-01 and CR-02 instead of patching them."
---

<objective>
Build the canonical-form admission reader that D-64 Part A specifies: a new module that stops
INTERPRETING frontmatter and starts ADMITTING a restricted canonical shape, refusing every other
byte by name.

This plan deliberately does NOT edit `scripts/frontmatter.ts` and does NOT edit
`scripts/check-foundation-guards.ts`. D-64 supersedes `27-VERIFICATION.md`'s prescribed remedy:
CR-01 (the block-scalar indentation landmark) and CR-02 (the unwired second `blockHeaderAt` call
site) are DISSOLVED by refusing block scalars and node properties outright, not patched by a
twelfth widening of the YAML parser. Rounds 10 and 11 each shipped a regression inside their own
fix; that, not any single finding, is why the mechanism changes.

Purpose: delete the silent-no-grant arm by construction. After this module exists there are exactly
two outcomes for any document — "canonical, here are the values" or "not canonical, FAIL BY NAME".
Output: `scripts/canonical-frontmatter.ts` + its committed `.js` + its test, with the live
33-file scan proven to ADMIT and the refusal vocabulary proven complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md
@CLAUDE.md
</context>

<repo_execution_notes>
Carry these into every task. Executors have been bitten by every one of them in this phase.

- Test command is `npx vitest run --exclude '**/scripts/e2e/**'`. NEVER bare `npm test` — it triggers
  a live claude-CLI e2e lane that spends tokens and can hang for roughly eight minutes.
- Typecheck is `npm run typecheck`, which now runs BOTH lanes (`tsc --noEmit` and
  `tsc -p tsconfig.tests.json`).
- TypeScript under `scripts/` compiles to committed `.js` siblings. Editing a `.ts` REQUIRES
  regenerating the `.js` (`npm run build`) or the freshness gate fails red. Vitest resolves a test's
  `./canonical-frontmatter.js` import to the COMMITTED `.js`, so a change to the `.ts` alone changes
  nothing the suite can see.
- The verification harness in this phase has produced a FALSE result sixteen times. Any task claiming
  a mutation or revert result MUST run the build, ASSERT THE BUILD'S EXIT CODE (a `tsc` failure emits
  nothing, leaves the old build in place, and the run reports a perfectly convincing baseline), PROBE
  THE BUILT ARTIFACT DIRECTLY (a mutation can compile and not bite), and SUBTRACT an unmutated
  scratch-copy baseline.
- `diff` is a shell function here, aliased to `git diff --no-index`. Use `/usr/bin/diff` for real
  diff output.
- Every plant asserts its own premise, so a wrong path halts loudly instead of producing a plausible
  number.
- Never fabricate a gate result, a test result, or a citation. Unknowns are recorded
  `UNKNOWN - verify` with the reason.
</repo_execution_notes>

<measured_baseline>
These numbers were measured this session on `fb59ca6` and are the premise the tasks below assert.
If a task's own re-measurement disagrees with a row here, HALT and report the disagreement rather
than adjusting the assertion to fit.

- `spawnGrantScan(repoRoot)` returns **33** members; `SPAWN_GRANT_SCAN_COUNT` is **33**; they agree.
- Composition: **17** `.claude/agents/*.md`, **7** `skills/*/SKILL.md`, **7**
  `.claude/skills/*/SKILL.md`, **2** `agent-factory/packaging/*.md`.
- Distinct frontmatter keys across all 33 after fence-stripping: **10** — `allowed-tools`,
  `argument-hint`, `coordinator`, `description`, `disable-model-invocation`, `kind`, `model`, `name`,
  `tier`, `tools`.
- Distinct line shapes across all 33: exactly **3** — `key: value`, `key:` (empty value), and
  `  - item` (a two-space block-sequence item).
- Double-quoted scalars: **31**, on exactly two keys — `description` (17, agent adapters only) and
  `argument-hint` (14, both skill sides). The only escape character appearing inside any of them is
  the double quote itself.
- Anchors, aliases, tags, block scalars, single-quoted scalars, flow sequences, flow mappings and
  tab characters across all 33 frontmatter regions: **0 of each**.
- `tools` and `allowed-tools` values across all 33: plain scalars and empty-then-block-sequence only.
  **Zero** quoted values on either key.
</measured_baseline>

<tasks>

<task type="tracer">
  <name>Task 1: the canonical admission reader, one path end to end</name>
  <files>scripts/canonical-frontmatter.ts, scripts/canonical-frontmatter.js</files>
  <read_first>
    scripts/canonical-frontmatter.ts (created by this task);
    scripts/frontmatter.ts lines 357-392 (stripFencedBlocks and BLOCK_INDICATOR, the fence authority
    and the indicator spelling this module refuses rather than parses);
    scripts/frontmatter.ts lines 1459-1467 (DQ_ESCAPE_ALLOWLIST, the proven enumerate-the-good escape
    alphabet this module reuses);
    scripts/kit-model.ts lines 973-1030 (spawnGrantScan and SPAWN_GRANT_SCAN_PARTS, the one authority
    for what is scanned);
    .claude/agents/grugops-orchestrator.md (the coordinator, the only file carrying a spawn grant);
    .planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW.md lines 121-138 (CR-01 row A,
    the exact regression document this task must refuse).
  </read_first>
  <action>
    Create `scripts/canonical-frontmatter.ts` as a pure module with no top-level side effects, so it
    can be imported by a gate, by a test and by a mirror-spawn without executing anything. Zero npm
    dependencies; Node stdlib only. Clear professional voice throughout, per the CLAUDE.md hard rule
    for build-safety surfaces.

    Export a single admission entry point whose result is a discriminated union with exactly two
    arms and no third: an ADMITTED arm carrying the ordered key/value map, and a REFUSED arm carrying
    a `code` drawn from an enumerated set plus a human-readable `reason` that names the offending
    line number and the offending byte or key. Model the result-shape convention on the existing
    `Parsed<T>` union in `scripts/frontmatter.ts` so the tree keeps one idiom, but do NOT import the
    grant predicates from that module — this module is the new safety authority and must not inherit
    the eleven-round parser's reach.

    Implement the grammar as ADMISSION, never as parsing. Per D-64 Part A the reader admits a
    restricted shape and refuses everything else; it never attempts to interpret an unadmitted
    construct. Concretely:

    - Locate the frontmatter region after running the input through `stripFencedBlocks` imported from
      `scripts/frontmatter.js`. Reuse that ONE fence implementation; a second fence machine in this
      tree is the exact drift class the repo has already named. The two packaging templates carry
      example frontmatter inside fenced blocks and only admit after this strip.
    - Admit exactly three line productions and nothing else: `key: <value>`, `key:` with an empty
      value, and a block-sequence item spelled as exactly two spaces, a hyphen, one space, then a
      plain scalar. Any other line is refused by name.
    - Enforce a ten-key schema, exported as a named readonly constant. The ten members are the keys
      in the measured baseline above. An unknown key is refused by name; a duplicate key is refused
      by name.
    - Enumerate the plain-scalar alphabet as an explicit character set, exported as a named readonly
      constant, and refuse any plain scalar containing a byte outside it. Derive the set's initial
      membership from the live corpus rather than guessing, and record in a source comment that the
      alphabet is enumerate-the-good, the D-30 inversion applied at the document level.
    - Admit a double-quoted scalar ONLY on the two keys measured to carry one, exported as a named
      readonly constant pair. Inside a double-quoted value, admit only escapes present in
      `DQ_ESCAPE_ALLOWLIST`, imported from `scripts/frontmatter.js` rather than restated. An
      unterminated double-quoted scalar and a disallowed escape are each refused by their own code.
    - Refuse a quoted value on any key outside that permitted pair, under its own code. Write the
      reason at the site: the grant-bearing keys admit plain scalars only, so the escape alphabet
      never touches the path that renders a spawn verdict.
    - Refuse, each under its own named code, a value whose first byte is an anchor, alias or tag
      sigil; a value whose first byte is a block-scalar indicator; a value whose first byte opens a
      flow sequence or a flow mapping; a single-quoted scalar; any tab character anywhere in the
      region; an indentation that is not one of the two admitted column positions; and a
      block-sequence item that does not follow a key with an empty value.

    Export the refusal-code set as a named readonly array so a consumer can iterate it. There must be
    no catch-all member and no default branch: every path that declines to admit assigns a member of
    that array. Record at the site why — an unreadable document reported as a readable document
    carrying no grant is this module's founding failure mode and the reason eleven rounds shipped
    green.

    Export a convenience predicate that answers whether an ADMITTED document's grant keys carry the
    coordinator spawn token, and a second that enumerates the granted agent names from an ADMITTED
    document. Both take an ADMITTED value, never raw text, so neither can be called on a document
    that was never admitted. Do not reimplement name-enumeration semantics from scratch where the
    existing semantics are correct — read `keysGrantedAgentNames` in `scripts/frontmatter.ts` for the
    intended output shape and match it, but operate on this module's admitted map so no unadmitted
    byte can reach it.

    Run `npm run build` and assert its exit code before believing any test result, then commit the
    generated `scripts/canonical-frontmatter.js` alongside the `.ts`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const m=require('node:child_process');" &amp;&amp; node --input-type=module -e "const c=await import('./scripts/canonical-frontmatter.js'); const fs=await import('node:fs'); const a=c.admit(fs.readFileSync('.claude/agents/grugops-orchestrator.md','utf8')); if(!a.ok) throw new Error('live coordinator REFUSED: '+a.code+' '+a.reason); const names=c.admittedGrantedNames(a.value); if(names.length===0) throw new Error('coordinator admitted but grant empty'); console.log('ADMITTED coordinator, granted names:',names.length); const cr01=['---','name: r','tools:','  -','    &gt;-2','      Read,','     # x, Agent(grugops-orchestrator)','---',''].join('\n'); const r=c.admit(cr01); if(r.ok) throw new Error('CR-01 row A was ADMITTED — the block scalar must be refused'); console.log('REFUSED CR-01 row A by code:',r.code); console.log('reason:',r.reason);"</automated>
  </verify>
  <done>
    `npm run build` exits 0. The live coordinator adapter is ADMITTED and its granted-name list is
    non-empty. CR-01 row A — the round-11 regression document, verbatim from `27-REVIEW.md` — is
    REFUSED, and the transcript prints the refusal CODE and the refusal TEXT, not merely a non-zero
    result. `scripts/canonical-frontmatter.js` is committed and is what the check above executed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: two-sided cardinality — the live scan ADMITS, and the scan is asserted non-empty and counted per part</name>
  <files>scripts/canonical-frontmatter.test.ts</files>
  <read_first>
    scripts/canonical-frontmatter.ts (from task 1);
    scripts/canonical-frontmatter.test.ts (created by this task);
    scripts/kit-model.ts lines 973-1030 (spawnGrantScan and SPAWN_GRANT_SCAN_PARTS);
    scripts/check-foundation-guards.ts lines 1440-1460 (the SPAWN_GRANT_SCAN_COUNT two-sided count
    assertion, whose reporting shape this task copies rather than invents);
    scripts/check-foundation-guards.test.ts (the harness idiom for pointing a gate at a mirror).
  </read_first>
  <behavior>
    - Every member of `spawnGrantScan(repoRoot)` is ADMITTED. A refusal on any live file fails the
      case NAMING the file, the code and the reason.
    - The scan is asserted non-empty before the admission loop runs, so a scan that derived nothing
      can never be reported as "all files admitted".
    - The composition is asserted per part against the measured baseline: 17 agent adapters, 7 source
      skills, 7 standalone skill twins, 2 packaging templates, total 33, and the total equals the
      derived scan length.
    - The union of keys actually present across the live scan equals the exported ten-key schema
      exactly — set equality in BOTH directions, so a live file gaining a key fails loudly and a
      schema key no longer used anywhere is named.
    - The union of value shapes actually present equals the three admitted line productions exactly.
    - The set of keys carrying a double-quoted value across the live scan equals the exported
      permitted pair exactly.
    - Zero grant-key values across the live scan are quoted.
  </behavior>
  <action>
    Create `scripts/canonical-frontmatter.test.ts`. Import the committed `.js`, per this repo's
    resolution behavior, so the cases exercise the artifact the gate will execute.

    Write the cardinality cases against the behavior list above. Every case reads its corpus through
    `spawnGrantScan` imported from `scripts/kit-model.js` — the ONE authority for what is scanned.
    Do NOT write a directory listing in this file: a second derivation of the scan rule is precisely
    the drift this phase exists to refuse, and it would also let the admission side and the guard
    side disagree about what was checked.

    Assert the scan non-empty FIRST, before any loop, and make that assertion its own named case so a
    vacuous run is visible as the anomaly it is rather than reading as success. Report what was
    checked, not a bare pass: the case output states the number of files admitted and the per-part
    breakdown, following the reporting convention already used by the kit-count guard.

    Implement the schema set-equality assertion as a DERIVATION over the live corpus compared against
    the exported schema constant, reported in both directions with the offending members named. Do
    the same for the double-quoted permitted pair and for the line productions. This is the
    derive-the-set-and-assert-the-count doctrine applied to the admission grammar: the schema stays an
    explicit allowlist because enumerate-the-good is the whole point of D-64 Part A, but it can never
    drift silently away from the corpus it governs.

    Add the non-vacuity floor for this plan's own two-sidedness: a case that narrows a COPY of the
    schema by one member and asserts the admission loop then REFUSES at least one live file naming
    that key. Constructing the narrowed copy in the test rather than mutating the module keeps the
    proof hermetic. Assert the copy actually differs from the exported schema before using it, so a
    no-op narrowing cannot produce a convincing green.

    Run the suite with the exclude flag from the repo execution notes and record the printed
    transcript in the summary. State plainly in the summary that a green suite is a FLOOR and is not
    offered as evidence that any bypass family is closed.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run scripts/canonical-frontmatter.test.ts --exclude '**/scripts/e2e/**' --reporter=verbose</automated>
  </verify>
  <done>
    All 33 live scanned files are ADMITTED, printed with the per-part breakdown 17 + 7 + 7 + 2 = 33
    and with that total asserted equal to the derived scan length. The non-empty floor is its own
    named passing case. Schema, line productions and permitted-quote keys each assert set equality
    with the live corpus in both directions. The narrowed-schema floor case demonstrates the
    admission loop can REFUSE a live file, so the all-admitted result is not vacuous.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the refusal vocabulary is complete, named, and has no catch-all</name>
  <files>scripts/canonical-frontmatter.test.ts</files>
  <read_first>
    scripts/canonical-frontmatter.ts (from task 1);
    scripts/canonical-frontmatter.test.ts (from task 2);
    .planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW.md lines 92-311 (CR-01 rows A
    and B and CR-02's alias-through-compact-mapping row, the three documents that must refuse);
    .planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md lines 366-381 (D-64, the
    decision this vocabulary implements).
  </read_first>
  <behavior>
    - Every exported refusal code is REACHABLE: for each member there is at least one document that
      refuses with exactly that code, and the case names any member no document reached.
    - No refusal returned by any case carries a code outside the exported set.
    - The exported set contains no catch-all member: a case asserts every member is produced by a
      construct-specific document, which a catch-all could not satisfy.
    - CR-01 row A (the `>-2` block header under a bare dash) refuses under the block-scalar code.
    - CR-01 row B (the bare `>-` header under a dash, no explicit digit) refuses under the same code.
    - CR-02's document (an anchor reaching a grant through a sequence item's compact mapping) refuses
      under the node-property code, and its dash-less control refuses under the same code — the two
      spellings that diverged for eleven rounds now land on one answer.
    - A source assertion reads the module and asserts the admission entry point has no default branch
      that can return a refusal without assigning an enumerated member.
  </behavior>
  <action>
    Extend `scripts/canonical-frontmatter.test.ts` with the refusal-vocabulary cases described above.

    Build the reachability case as a DERIVATION, not a hand-kept list: iterate the exported
    refusal-code array, look up a document for each member from a table declared in the test, and
    fail NAMING any code with no document and any document whose actual code differs from the code it
    was filed under. A code added to the module without a document must fail by name — that is the
    set-literal drift failure class this repo has already paid for once with seven granted names and
    zero resolving files.

    Include CR-01 row A and row B and CR-02's row and its control verbatim from `27-REVIEW.md`, each
    with a source citation in its label giving the round and the finding id. These four are the
    minimum here; plan 27-63 owns the full historical corpus and must not be pre-empted or duplicated
    in this file.

    For the no-catch-all source assertion, follow the idiom `27-59` established for the `stripComment`
    call sites and `27-60` established for the bounded source slice: read `scripts/canonical-frontmatter.ts`
    at run time, bound the region being inspected by an explicit section marker asserted present
    before the slice is used, strip comments before scanning so the negative reads code and not
    prose, and assert a non-vacuity floor on the length of what was scanned. A negative assertion
    over an unbounded or comment-bearing slice is the brittleness IN-03 named, and it must not be
    reintroduced here.

    Print each refusal's TEXT in the case output, not merely its code. D-64's vacuity traps require
    the transcript to show the refusal text, because "it failed" and "it failed for the right reason"
    are different claims and this phase has confused them before.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run scripts/canonical-frontmatter.test.ts --exclude '**/scripts/e2e/**' --reporter=verbose &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    Every exported refusal code is reached by a construct-specific document and the reachability case
    would name an unreached member. No case produces a code outside the exported set. CR-01 rows A
    and B refuse under the block-scalar code; CR-02's row and its dash-less control refuse under the
    node-property code, printed with their refusal text. The bounded, comment-stripped source
    assertion for the absence of a catch-all passes with its non-vacuity floor stated. `npm run
    typecheck` exits 0 on both lanes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed markdown -> admission reader | Any file in the spawn-grant scan is attacker-influenced input to a safety verdict. This is the boundary eleven rounds of bypasses crossed. |
| frontmatter.ts -> canonical-frontmatter.ts | Two imports cross this boundary (the fence authority and the escape allowlist). Each import re-exposes that module's reach to the new authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-140 | Elevation of Privilege | canonical-frontmatter admission entry point | critical | mitigate | Two-arm result with no third outcome and no default branch; every decline assigns an enumerated code; asserted by the bounded comment-stripped source scan in task 3. |
| T-27-141 | Tampering | the ten-key schema and the plain-scalar alphabet as hand-written allowlists | high | mitigate | Two-sided set equality against the live corpus in task 2, with the offending members named in both directions, plus a narrowed-copy floor proving the loop can refuse. |
| T-27-142 | Information Disclosure | refusal reasons interpolating file content into gate output | low | accept | Refusal reasons name a line number, a key and an offending byte, not the full value. The corpus is committed markdown already public in the repository. |
| T-27-143 | Denial of Service | admission over an adversarially large or deeply indented region | medium | mitigate | The reader is linear over lines with no backtracking construct and no regular expression carrying nested quantifiers; the guard's own runtime is reported and compared in plan 27-65. |
| T-27-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs NO package. Zero runtime dependencies is a CLAUDE.md hard constraint and the package manifest diff for this plan must be empty; assert it in the summary. |
</threat_model>

<verification>
- `npm run build` exits 0 and `scripts/canonical-frontmatter.js` is committed and current.
- `npx vitest run scripts/canonical-frontmatter.test.ts --exclude '**/scripts/e2e/**'` passes.
- `npm run typecheck` exits 0 on both lanes.
- The full suite, run with the exclude flag, is no worse than the recorded baseline. It is recorded
  as a FLOOR and is not offered as evidence that any bypass family is closed.
- `scripts/frontmatter.ts` is BYTE-UNCHANGED by this plan. Assert it: this plan is not a twelfth
  widening of the parser.
- `scripts/check-foundation-guards.ts` is BYTE-UNCHANGED by this plan. The cutover is 27-65's, behind
  its own decision checkpoint.
- The package manifest is byte-unchanged; no dependency is added.
</verification>

<success_criteria>
- A new module renders the admission verdict with exactly two outcomes, per D-64 Part A.
- All 33 live scanned files ADMIT, counted per part, with the scan asserted non-empty first.
- The admitted grammar matches the MEASURED corpus, including the two keys and the quoted arm D-64's
  premise measurement missed, and the divergence from D-64's stated eight-key plain-only alphabet is
  written down with its measurement in the summary.
- Grant-bearing keys admit plain scalars only, keeping the escape alphabet out of the verdict path.
- The refusal vocabulary is enumerated, fully reachable, catch-all-free, and CR-01 and CR-02 are
  DISSOLVED into named loud refusals rather than patched.
</success_criteria>

<output>
Create `.planning/phases/27-spawn-correctness-kit-set-authority/27-62-SUMMARY.md` when done.

The summary MUST record, with transcripts rather than assertions:
- the per-part admission breakdown over the live scan and the printed total;
- the refusal CODE and refusal TEXT for CR-01 row A, CR-01 row B, CR-02's row and CR-02's control;
- the measured divergence from D-64's premise (33 files not 31, ten keys not eight, 31 double-quoted
  scalars on two keys) stated as a measurement with the command that produced it, and the reason the
  grammar follows the corpus rather than the decision's prose — D-64's own vacuity trap 1 forbids a
  gate that reds the live kit;
- an explicit statement that the green suite is a FLOOR;
- confirmation that `scripts/frontmatter.ts` and `scripts/check-foundation-guards.ts` are both
  byte-unchanged by this plan.
</output>

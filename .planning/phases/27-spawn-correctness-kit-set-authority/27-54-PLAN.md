---
phase: 27-spawn-correctness-kit-set-authority
plan: 54
type: execute
wave: 4
depends_on: ["27-51", "27-52", "27-53"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md
  - .planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md
autonomous: true
requirements: [KIT-01, KIT-02, KIT-03, SPAWN-01, SPAWN-02, SPAWN-03, SPAWN-04, SPAWN-05, SPAWN-06, SPAWN-07]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ── KIT-01 probe rows (5) — specless probe fallback §A, resolved `explicit` ──
    - "The asserted kit counts are exact in BOTH directions — 17 roles and 19 workflows — so 16 or 18 roles fails red and forces a deliberate walk of the derived consumers (KIT-01 boundary probe, D-20)."
    - "A role file and a workflow file sharing a basename stem resolve into separate sets; `listRoles` and `listWorkflows` never merge or collide on a shared stem because each takes the explicit root it was given (KIT-01 adjacency probe, D-22)."
    - "An empty roles or workflows directory makes `kit-model` THROW rather than return an empty set a consumer reads as nothing-to-scan (KIT-01 empty probe, D-21 vacuity tier)."
    - "The derived role and workflow listings are deterministic and ordered, so a readdir-order difference on another filesystem cannot move a guard's PASS line (KIT-01 ordering probe)."
    - "The asserted count and the derived set's length are the same integer compared for exact equality — there is no rounding, truncation or tolerance step anywhere in the comparison (KIT-01 precision probe)."
    # ── SPAWN-02 probe row (1) — specless probe fallback §A, resolved `explicit` ──
    - "REQUIREMENTS.md records SPAWN-02 under a WRITTEN convention that is applied identically to all ten Phase-27 requirement IDs, rather than as a one-off edit to the single row a verification happened to name (SPAWN-02)."
    # ── SPAWN-03 probe rows (3) — specless probe fallback §A, resolved `backstop` ──
    - statement: "On a real Claude Code session, the coordinator's Agent allowlist and the set of roles the runtime actually admits are compared for exact equality — whether they merge, collide or separate is observable only in a live run."
      verification: backstop
    - statement: "A coordinator whose Agent allowlist enumerates zero names is observed to spawn nothing rather than to spawn everything — the empty-input behaviour of the runtime grant."
      verification: backstop
    - statement: "The ORDER of names inside the Agent allowlist is observed not to change which roles the runtime admits."
      verification: backstop
    # ── SPAWN-05 probe row (1) — specless probe fallback §A, resolved `explicit` ──
    - "The retired-vocabulary scan set behind `guard_adapter_body` is RE-MEASURED against the post-round-10 build rather than assumed unchanged, and its PASS line states how many adapter bodies and template shapes it checked (SPAWN-05, D-25)."
    # ── SPAWN-06 probe rows (2) — specless probe fallback §A, resolved `explicit` ──
    - "`agent-factory/roles/orchestrator.md` is measured against the 7570-byte FAIL ceiling with the ceiling constant unchanged, and an empty or unreadable role file fails red rather than measuring zero bytes as within-ceiling (SPAWN-06 empty probe)."
    - "The ceiling is measured in BYTES, not code points or grapheme clusters, and the guard's PASS line states the byte figure it measured (SPAWN-06 encoding probe)."
    # ── SPAWN-07 probe rows (3) — specless probe fallback §A, resolved `explicit` ──
    - "v2.1.219 is inside the advertised floor and v2.1.218 is outside it; the two adjacent versions carry different verdicts and the known-bad v2.1.217-218 window is documented as such rather than merged into one range (SPAWN-07 adjacency probe)."
    - "This round creates no new site that states a Claude Code floor, and the three existing sites keep their wording byte-unchanged — measured, not assumed (SPAWN-07 empty probe)."
    - "The three floor-stating sites agree with each other, so a reader who greps them gets one consistent answer rather than a first-match-wins reading (SPAWN-07 ordering probe)."
    # ── Goal-backward truths for this plan ──
    - "REQUIREMENTS.md's ten Phase-27 rows are governed by ONE written convention, recorded as a decision, and every row's state is derivable from that convention plus a cited verification transcript."
    - "SPAWN-03's live-platform capture remains an explicit, dated, owned DEFERRAL to Phase 33 / GAP-D1 / CAP-01 — neither reopened as a Phase-27 blocker nor fabricated as runtime-confirmed (D-56 item 10)."
    - "Every round-9 finding and every round-9 verification gap has a recorded disposition in a committed artifact — closed, open with a measurement, or deferred with an owner and a reason. There is no silent drop."
  prohibitions:
    - "No requirement may be marked Complete without a cited verification transcript naming the command and its result."
    - "SPAWN-03 must not be marked Complete and its runtime behaviour must not be asserted as confirmed."
    - "KIT-03 and SPAWN-04 must not be marked Complete by this plan — only a verification round may flip them."
    - "The verification report's line citations must not be applied blindly; REQUIREMENTS.md is re-read on disk first."
    # Canon security/compliance prohibitions dropped with a breadcrumb: this plan edits planning
    # artifacts only — no code, no package install, no network path — see <threat_model>.
  artifacts:
    - ".planning/REQUIREMENTS.md — the ten Phase-27 rows under one stated convention"
    - ".planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md — the convention recorded as D-58"
    - ".planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md — the round-10 disposition register"
  key_links:
    - "verification transcript -> REQUIREMENTS.md checkbox -> REQUIREMENTS.md traceability table row — all three must agree for every one of the ten IDs"
    - "ROADMAP.md Phase 33 / CAP-01 -> deferred-items.md SPAWN-03 entry -> REQUIREMENTS.md SPAWN-03 row — the deferral must read the same in all three"
---

<objective>
Make the trace tell the truth, once, under a rule — and close the round-10 record so no round-9
finding leaves this phase without a disposition.

**A measured correction the executor must act on before touching anything.** `27-VERIFICATION.md`
claims REQUIREMENTS.md over-claims KIT-03 and SPAWN-04 as `[x]`/Complete and cites lines 56-58 and
62-65. **That half is already fixed at HEAD** — commit `47d7820` reverted both to `[ ]`/Gaps Found
*after* the verification was written. The verification's line citations are STALE. Re-read
`.planning/REQUIREMENTS.md` on disk and work from what is there, not from the report's line numbers.

What genuinely remains is a CONVENTION problem, not a single wrong row. At HEAD, KIT-02 is
`[x]`/Complete while KIT-01, SPAWN-01, SPAWN-02, SPAWN-05, SPAWN-06 and SPAWN-07 are all
`[ ]`/Gaps Found — and the verification records all seven as SATISFIED with cited evidence. The same
report calls SPAWN-02's state an "under-claim" that should be corrected while calling five identical
states "checklist lags until the phase closes." Those two sentences cannot both be the rule. Fixing
only the row the report named would leave the same inconsistency with one fewer instance, which is
the shape this milestone exists to delete.

Purpose: this project's binding constraint is *the trace is the proof*, and a traceability table
governed by no stated rule is not proof of anything. This plan decides the rule, records it, applies
it uniformly, and writes the round-10 disposition register.

Output: one recorded convention (D-58), ten rows consistent with it, SPAWN-03's Phase-33 deferral
re-affirmed verbatim, and a disposition register covering every round-9 finding (CR-01, WR-01, WR-02,
WR-03, IN-01, IN-02) and every round-9 verification gap (Family G/G2, the SPAWN-02 traceability
correction, SPAWN-03's deferral).

Implements D-56 item 9 (a retirement is recorded with its reason, never a silent drop), D-56 item 10
(SPAWN-03 stays deferred to Phase 33 / GAP-D1 / CAP-01 as a record-don't-fix disposition), D-09 and
D-19 (the phase's own claims are backed by recorded measurement), D-20, D-21 and D-22 (the KIT-01
count semantics the probe rows above restate), and D-25 (the `guard_adapter_body` scan set this plan
re-measures rather than assumes).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/REQUIREMENTS.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-VERIFICATION.md
@.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-51-SUMMARY.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-52-SUMMARY.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-53-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — decide the convention, record it, and apply it end-to-end to exactly one row</name>
  <files>.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — READ ON DISK FIRST, both the checkbox list (the KIT and SPAWN
      sections) and the traceability table's Phase-27 rows. Record the ten current states verbatim
      before editing anything; the verification report's line citations are stale.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-VERIFICATION.md` — the Requirements
      Coverage table, which states a Verified Status and an Evidence cell for each of the ten IDs, and
      the two sentences that contradict each other about what a `[ ]` means.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md` — the round-5 through
      round-8 decision blocks, for the shape a recorded decision takes in this phase. Note that D-57
      is reserved by `27-52`; this plan's decision is **D-58**.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-50-SUMMARY.md` — where plan 27-50
      surfaced this discrepancy and deliberately declined to run the mark-complete handler, and why.
  </read_first>
  <action>
    Decide and RECORD one convention. Two are on the table and both are defensible; what is not
    defensible is the current state, where two identical situations carry different marks.

    **Convention V, verification-led (RECOMMENDED).** A requirement is `[x]` / `Complete` when the
    most recent verification report records it SATISFIED with cited evidence, independently of whether
    the phase has sealed. A requirement whose most recent verification records it FAILED, BLOCKED or
    NEEDS-HUMAN stays `[ ]` with the status the verification gave it. Rationale: this is what KIT-02
    already carries, it is the only reading under which a reader can derive a row from a transcript,
    and it satisfies the project's own "the trace is the proof" constraint. Under it, KIT-01, KIT-02,
    SPAWN-01, SPAWN-02, SPAWN-05, SPAWN-06 and SPAWN-07 are `[x]` / `Complete`; KIT-03 and SPAWN-04
    stay `[ ]` / `Gaps Found` until a verification round says otherwise; SPAWN-03 stays `[ ]` with a
    status that names its Phase-33 deferral.

    **Convention P, phase-led.** No requirement flips until the phase seals, so all ten stay `[ ]` and
    KIT-02 is reverted to match. Rationale: a single seal point is simpler to audit. Cost: the table
    carries no per-requirement information until the very end, and KIT-02 must be un-marked.

    Record the chosen convention in `27-CONTEXT.md` as **D-58** under the `Gap-closure round 10`
    heading, in the shape the round-5 through round-8 blocks use: the rule stated in one sentence, the
    rejected alternative with the reason it was rejected, and the reversibility note. This decision is
    **reversible** — `gsd-tools query requirements.revert-phase` un-does it for the whole phase in one
    call — so it is recorded rather than gated.

    Then apply it end-to-end to exactly ONE row, SPAWN-02, as the thin slice that proves the whole
    path works before the other nine follow: flip the checkbox in the requirements list, flip the
    matching cell in the traceability table, and cite the transcript that justifies it — the round-8
    live fail-closed spot-check (append one byte, STALE detected, revert, clean) and the round-9
    `node scripts/adapters-freshness.js` run (exit 0, zero byte differences, directory listings
    set-equal). Use the `requirements.mark-complete` handler where it does the whole job; where it
    does not, use a scoped edit that touches only the SPAWN-02 lines. Do NOT rewrite the file.

    Then re-read both sites and assert they agree. A convention that leaves the checkbox and the table
    disagreeing is worse than the inconsistency it replaced.
  </action>
  <verify>
    <automated>grep -n "SPAWN-02" .planning/REQUIREMENTS.md</automated>
    <automated>node scripts/adapters-freshness.js</automated>
  </verify>
  <acceptance_criteria>
    - The ten current REQUIREMENTS.md states are recorded verbatim in the SUMMARY as read from disk,
      BEFORE any edit, and the SUMMARY states explicitly that KIT-03 and SPAWN-04 were already
      `[ ]`/Gaps Found at HEAD and that the verification's line citations were stale.
    - `27-CONTEXT.md` carries a `Gap-closure round 10` heading with **D-58**: the rule in one sentence,
      the rejected alternative with its reason, and the reversibility note.
    - The SPAWN-02 checkbox and the SPAWN-02 traceability-table row agree with each other and with the
      recorded convention.
    - The SPAWN-02 change cites a named command and its result — `node scripts/adapters-freshness.js`
      exit 0 with zero byte differences and set-equal directory listings — re-run in this plan, not
      quoted from a prior round.
    - Exactly the SPAWN-02 lines changed in `.planning/REQUIREMENTS.md`; the diff for this task touches
      no other requirement row.
    - `node scripts/adapters-freshness.js` exits 0.
  </acceptance_criteria>
  <done>One convention is recorded as D-58 and proven end-to-end on a single row, with the checkbox, the table and a re-run transcript all agreeing.</done>
</task>

<task type="auto">
  <name>Task 2: apply the recorded convention to the remaining nine rows, and re-affirm the SPAWN-03 deferral without reopening or fabricating it</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-VERIFICATION.md` — the Requirements
      Coverage table and the Behavioral Spot-Checks table, which name the exact commands and results
      each row's evidence rests on.
    - `.planning/ROADMAP.md` around line 431 — the Phase 33 entry and the standing-obligations row
      that owns GAP-D1 / CAP-01. This is READ-ONLY here: the deferral is re-affirmed, never edited.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — the `From 27-50`
      SPAWN-03 deferral entry, which is the durable record and whose wording this plan must agree with.
    - `27-51-SUMMARY.md`, `27-52-SUMMARY.md`, `27-53-SUMMARY.md` — this round's own transcripts, for
      any row whose evidence changed since the round-9 verification.
  </read_first>
  <action>
    Apply D-58 to the nine rows the tracer did not touch, in one pass, with each row's justification
    named.

    For every row marked `[x]` / `Complete`, re-run the command its evidence rests on IN THIS PLAN and
    record the result — never carry a transcript forward from a prior round's summary. The commands
    are already known and verified: `node scripts/check-foundation-guards.js` for the kit-count and
    adapter-body PASS lines (KIT-01, KIT-02, SPAWN-05), `node scripts/adapters-freshness.js` for the
    generated-adapter set equality (SPAWN-01), the guard's own orchestrator byte line for SPAWN-06, and
    a grep across the three floor-stating sites for SPAWN-07. If a command's result disagrees with the
    round-9 verification, the row does NOT flip and the disagreement is recorded — the measurement
    governs, not the report.

    KIT-03 and SPAWN-04 stay `[ ]` / `Gaps Found`. This plan does not flip them even though `27-51`
    and `27-52` targeted their underlying bypasses: a requirement's verified status is a verification
    round's call, and this phase has spent nine rounds learning what a premature flip costs. State
    that reasoning in the row's status or in the SUMMARY so a later reader does not read the omission
    as an oversight.

    SPAWN-03 stays `[ ]`. Re-affirm the deferral in the words the three existing records already use —
    deferred to Phase 33 / GAP-D1 / CAP-01, ratified as D-56 item 10, `UNKNOWN - verify` against the
    real platform — and change nothing in `ROADMAP.md`. Do not fabricate a runtime confirmation and do
    not re-open it as a Phase-27 blocker. Confirm by reading that the three records
    (`ROADMAP.md`, `deferred-items.md`, `REQUIREMENTS.md`) say the same thing, and record the
    confirmation.

    Use scoped edits throughout. Do not rewrite `.planning/REQUIREMENTS.md`; the file carries 46
    requirements across seven phases and only the ten Phase-27 rows are in scope.
  </action>
  <verify>
    <automated>node scripts/check-foundation-guards.js</automated>
    <automated>node scripts/adapters-freshness.js</automated>
    <automated>node scripts/coordinator-resolution-precheck.js</automated>
    <automated>grep -c "Phase 27" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - All ten Phase-27 rows are consistent with D-58, and the checkbox state and traceability-table
      state agree for every one of the ten — asserted by reading both sites after the edit and
      recording the pair for each row.
    - Every row marked Complete cites a command re-run IN THIS PLAN with its result quoted; no
      transcript is carried forward from a prior round's summary.
    - KIT-03 and SPAWN-04 remain `[ ]` / `Gaps Found`, with the reason recorded.
    - SPAWN-03 remains `[ ]`, its deferral wording agrees across `ROADMAP.md`, `deferred-items.md` and
      `REQUIREMENTS.md`, and `.planning/ROADMAP.md` is unchanged by this plan — confirmed by a diff
      that shows no ROADMAP edit.
    - The counts elsewhere in `.planning/REQUIREMENTS.md` are untouched: the 46-requirement total, the
      per-phase coverage table and the 36 rows belonging to phases 28-33 are byte-identical.
    - `node scripts/check-foundation-guards.js` exits 0 and its PASS lines are quoted for the rows
      that cite them.
    - `node scripts/adapters-freshness.js` and `node scripts/coordinator-resolution-precheck.js` both
      exit 0.
  </acceptance_criteria>
  <done>All ten rows follow one recorded rule, every Complete row cites a transcript re-run in this plan, and the SPAWN-03 deferral is re-affirmed identically in all three records.</done>
</task>

<task type="auto">
  <name>Task 3: the round-10 disposition register — every round-9 finding and every verification gap accounted for</name>
  <files>.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md</files>
  <read_first>
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW.md` — the full finding list:
      CR-01, WR-01, WR-02, WR-03, IN-01, IN-02.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-VERIFICATION.md` — the three failed
      truths, the deferred item and the human-verification item.
    - `27-51-SUMMARY.md`, `27-52-SUMMARY.md`, `27-53-SUMMARY.md` — what actually landed, and every
      residual each plan's own dispositions created.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — the `From 27-47`
      through `From 27-50` sections, whose shape the round-10 register follows.
  </read_first>
  <action>
    Write the round-10 disposition register into `deferred-items.md` as a `Round 10 disposition
    register` section. It is a table with one row per round-9 item and per round-9 verification gap,
    and every row carries: the item id, where it was raised, what happened, the artifact that carries
    the evidence, and the disposition — CLOSED with a transcript, OPEN with a measurement and an
    owner, or DEFERRED with a date and an owner. Cover, with no omissions: CR-01, WR-01, WR-02,
    WR-03, IN-01, IN-02, Family G/G2, the REQUIREMENTS.md traceability correction, and SPAWN-03's
    Phase-33 deferral.

    Add a completeness assertion the reader can check: state the count of round-9 items raised, the
    count of rows in the register, and assert they are equal. This is the same non-vacuity posture the
    harness uses on its corpora, applied to the record. If the counts differ, the register is wrong,
    not the count.

    Then state, in one short block, the standing obligations this phase carries INTO its next
    verification, so they survive an eventual milestone archive move: any bypass still open with its
    measurement, the residuals `27-51` through `27-53` recorded, the `27-48` scope question if `27-52`
    left it open, and SPAWN-03's deferral. Name each one's owner.

    Finally, restate the round-wide specless-probe accounting in one line so the equality is checkable
    from a single place: 29 probe-surfaced rows, distributed 7 to `27-51`, 0 to `27-52`, 7 to `27-53`
    and 15 to `27-54`, resolving to 26 explicit and 3 backstop with 0 unresolved and 0 dismissed.
  </action>
  <verify>
    <automated>grep -c "CLOSED\|OPEN\|DEFERRED" .planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md</automated>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <automated>node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - The register contains a row for each of CR-01, WR-01, WR-02, WR-03, IN-01, IN-02, Family G/G2,
      the REQUIREMENTS.md traceability correction and SPAWN-03 — nine items, none omitted.
    - Every row names an artifact carrying its evidence and a disposition of exactly one of CLOSED,
      OPEN or DEFERRED; no row is left without one.
    - The stated count of round-9 items raised equals the stated count of register rows, and both
      numbers appear in the register.
    - Every OPEN row carries a measurement taken against the current build, not a memory from a prior
      round.
    - Every DEFERRED row carries an owner and a target phase.
    - The standing-obligations block names an owner for each obligation.
    - The round-wide probe accounting line is present and its arithmetic is correct.
    - `npx vitest run --exclude '**/scripts/e2e/**'` reports 0 failed and
      `node scripts/check-foundation-guards.js` exits 0 — the record is written over a green tree, and
      the SUMMARY states in one line that a green suite is a floor and not the closure evidence.
  </acceptance_criteria>
  <done>Every round-9 finding and verification gap has a disposition in a committed artifact, the register's completeness is asserted by count, and the standing obligations carry named owners.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification transcript -> REQUIREMENTS.md row | The traceability table is what a reader, an auditor and a future phase trust instead of re-running the gates; a row that outruns its evidence is an unverified claim presented as a verified one. |
| verification report line citations -> the file on disk | A report written before a commit cites line numbers that no longer point where it thinks; applying them blindly edits the wrong rows. |
| phase record -> milestone archive | Records that live only in a plan summary scroll out of view at archive time; the durable record must be in the phase's own artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-54-01 | Repudiation | `.planning/REQUIREMENTS.md` traceability rows | high | mitigate | A requirement marked Complete over a FAILED verification destroys the trace, which is this project's stated proof. Closed by D-58 plus the rule that every Complete row cites a command re-run in this plan, and by holding KIT-03 and SPAWN-04 unflipped. |
| T-27-54-02 | Spoofing | SPAWN-03's runtime claim | high | mitigate | Fabricating a runtime confirmation for a claim no static gate can reach is the fabrication the project's own constraints forbid. Closed by re-affirming the Phase-33 deferral verbatim in three records and by the prohibition against marking it Complete. |
| T-27-54-03 | Tampering | scoped edits to a 46-requirement file | medium | mitigate | A whole-file rewrite would silently move the 36 rows belonging to phases 28-33. Closed by requiring scoped edits and by asserting those rows are byte-identical afterwards. |
| T-27-54-04 | Repudiation | the round-9 finding set | medium | mitigate | A finding that leaves the round without a disposition returns one abstraction level down — this phase's own recorded experience. Closed by the register plus its completeness-by-count assertion. |
| T-27-54-SC | Tampering | package-manager installs | low | accept | This plan edits planning artifacts only and runs no package-manager install; no package-legitimacy checkpoint is required, and the absence is recorded rather than left silent. |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md` is re-read on disk before any edit, and the pre-edit states are recorded.
- Every command a Complete row cites is re-run in this plan: `node scripts/check-foundation-guards.js`,
  `node scripts/adapters-freshness.js`, `node scripts/coordinator-resolution-precheck.js`.
- `npx vitest run --exclude '**/scripts/e2e/**'` reports 0 failed. Do NOT run `npm test`.
- `.planning/ROADMAP.md` is unchanged by this plan — confirmed by diff.
- The rows belonging to phases 28-33 in `.planning/REQUIREMENTS.md` are byte-identical.

<human-check>
Read `.planning/REQUIREMENTS.md`'s ten Phase-27 rows and `27-CONTEXT.md`'s D-58 side by side and
confirm that every row's state is derivable from the stated rule plus the transcript it cites — that
no row needs a story to explain it. Then confirm SPAWN-03 reads as a dated, owned deferral in all
three records and nowhere reads as confirmed.
</human-check>
</verification>

<success_criteria>
1. One convention governs all ten Phase-27 requirement rows, recorded as D-58 with its rejected
   alternative and its reversibility note.
2. Every row marked Complete cites a command re-run in this plan with its result quoted; KIT-03 and
   SPAWN-04 stay unflipped with the reason recorded.
3. SPAWN-03's Phase-33 deferral reads identically in `ROADMAP.md`, `deferred-items.md` and
   `REQUIREMENTS.md`, and `ROADMAP.md` is unedited.
4. The round-10 disposition register covers all nine round-9 items with an asserted completeness count.
5. The round-wide specless-probe equality is stated in one checkable place.
</success_criteria>

## Artifacts this plan produces

**Planning artifacts (new content, no new files)**
- `.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md` — a `Gap-closure round 10`
  heading carrying **D-58**, the traceability convention, its rejected alternative and its
  reversibility note. (D-57 is reserved by `27-52`.)
- `.planning/REQUIREMENTS.md` — the ten Phase-27 checkbox states and the ten matching traceability
  table rows, all consistent with D-58.
- `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — the
  `Round 10 disposition register` table with one row per round-9 item, its completeness-by-count
  assertion, the standing-obligations block with named owners, and the round-wide probe accounting
  line.

**No source symbols, no new files, no new CLI flags, no new tsconfig keys, no new dependencies.**

## Multi-source coverage audit

| Source | Item | Covered by |
|---|---|---|
| GOAL | the phase goal's auditability half — the guards and the record must both be trustworthy | Tasks 1-3 |
| REQ | KIT-01, KIT-02, SPAWN-01, SPAWN-05, SPAWN-06, SPAWN-07 — verified SATISFIED, marked under one rule | Tasks 1-2 |
| REQ | SPAWN-02 — the genuine under-claim named by the verification | Task 1 (tracer row) |
| REQ | KIT-03, SPAWN-04 — held unflipped pending verification | Task 2 |
| REQ | SPAWN-03 — deferral re-affirmed, never reopened, never fabricated | Task 2 |
| RESEARCH | "a hand-maintained set drifts while staying green" — here the hand-maintained set is the traceability table itself | Task 3 (completeness-by-count assertion) |
| CONTEXT | D-56 item 9 — a retirement is recorded with its reason | Task 3 |
| CONTEXT | D-56 item 10 — SPAWN-03 stays deferred to Phase 33 / GAP-D1 / CAP-01 | Task 2 |
| CONTEXT | D-19 — recorded measurement backs every claim | Tasks 1-3 |
| CONTEXT | D-20, D-21, D-22 — the KIT-01 count semantics the probe rows restate | `must_haves.truths` |
| CONTEXT | D-25 — the `guard_adapter_body` scan set re-measured rather than assumed | Task 2 |
| CONTEXT | D-58 (new, recorded in this plan) — the traceability convention | Task 1 |

No item is MISSING.

## Specless probe fallback — no-silent-drop accounting

This plan owns the **15** rows for the six requirements it settles that no other round-10 plan claims:

| Requirement | Rows | Disposition |
|---|---|---|
| KIT-01 | boundary, adjacency, empty, ordering, precision (5) | all resolved `explicit` |
| SPAWN-02 | unclassified (1) | resolved `explicit` |
| SPAWN-03 | adjacency, empty, ordering (3) | all resolved **`backstop`** — structured flat-scalar markers, consistent with the Phase-33 deferral; no static gate can reach them |
| SPAWN-05 | unclassified (1) | resolved `explicit` |
| SPAWN-06 | empty, encoding (2) | both resolved `explicit` |
| SPAWN-07 | adjacency, empty, ordering (3) | all resolved `explicit` |

**Equality for this plan: 15 probe-surfaced rows == 12 explicit + 3 backstop authored into
`must_haves.truths` + 0 surfaced as flagged assumptions.** No row was auto-dismissed.

**Round equality: 29 == 26 explicit + 3 backstop + 0 unresolved**, distributed `27-51` 7,
`27-52` 0, `27-53` 7, `27-54` 15.

<output>
Create `.planning/phases/27-spawn-correctness-kit-set-authority/27-54-SUMMARY.md` when done, carrying
the pre-edit REQUIREMENTS.md states verbatim, D-58 as recorded, every re-run transcript, the
three-record SPAWN-03 agreement check, and the disposition register's completeness count.
</output>

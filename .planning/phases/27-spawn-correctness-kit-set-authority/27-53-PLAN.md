---
phase: 27-spawn-correctness-kit-set-authority
plan: 53
type: execute
wave: 3
depends_on: ["27-51", "27-52"]
gap_closure: true
files_modified:
  - scripts/frontmatter.test.ts
  - scripts/frontmatter.ts
  - scripts/frontmatter.js
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - scripts/generate-role-adapters.test.ts
  - scripts/validate-agent-factory.ts
  - scripts/validate-agent-factory.js
  - tsconfig.json
  - .planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md
autonomous: true
requirements: [KIT-02, SPAWN-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ── KIT-02 probe rows (3) — specless probe fallback §A, resolved `explicit` ──
    - "Two derived sets that happen to hold the same file stay separate sets — the 33-member spawn-grant scan composition (17 agent + 7 skill + 7 plugin-skill + 2 packaging) partitions rather than merges, and the fence-authority set added here is a third derived set that neither absorbs nor is absorbed by them (KIT-02 adjacency probe)."
    - "A derived scan set that resolves empty fails red rather than passing vacuously — the fence-authority scan asserts its corpus was really enumerated before it asserts anything about the corpus's contents (KIT-02 empty probe)."
    - "The derived fence-authority set is sorted before comparison, so a `git ls-files` output-order change on another filesystem cannot flip the assertion (KIT-02 ordering probe, mirroring the D-50 / IN-05 precedent)."
    # ── SPAWN-01 probe rows (4) — specless probe fallback §A, resolved `explicit` ──
    - "The two sibling role-adapter fixtures are built from the SAME splice of the SAME source file, and their agreement is asserted as a property rather than asserted through a call that cannot fail (SPAWN-01 adjacency probe, WR-03)."
    - "`stripFencedBlockLines` over a line array carrying no fence returns its input unchanged with zero blocks removed, and the fixture premise refuses that as a silent no-op rather than proceeding (SPAWN-01 empty probe)."
    - "A fence-delimiter line is recognised by the same line class in every tracked `.ts` that carries a fence state machine, and the derived set names its members by repository-relative path compared as strings (SPAWN-01 encoding probe)."
    - "The derived fence-authority set's cardinality is pinned as a number, so a scan that silently stops matching shrinks loudly rather than passing over an empty set (SPAWN-01 ordering probe)."
    # ── Goal-backward truths for this plan ──
    - "The unqualified 'no second fence parser is written, here or anywhere' claim is either TRUE by measurement or NARROWED to the scope a mechanical assertion actually holds — a prose claim wider than its pin is the set-literal-drift class the milestone exists to delete."
    - "`npx tsc --noEmit` exits 0 with `noUnusedLocals` and `noUnusedParameters` enabled, so the next migration's residue cannot survive silently."
  prohibitions:
    - "No hand-listed file-name set may stand as the fence-authority answer without a derivation that produced it in the same run."
    - "No assertion of the form `f(x) === f(x)` on a pure function may be added or left standing."
    - "The named set expected by the fence scan must be MEASURED against the tree at execution time, never transcribed from the review's prose list."
    - "No prose claim may be left stating a scope wider than the assertion that backs it."
    # Canon security/compliance prohibitions dropped with a breadcrumb: no package install, no
    # network path, no credential handling — see <threat_model> for the applicable set.
  artifacts:
    - "scripts/frontmatter.test.ts — the derived fence-authority scan, its planted-extra-machine control, and the narrowed pin replacing the single-spelling count"
    - "scripts/frontmatter.ts — the fence-authority claim narrowed to the measured scope"
    - "scripts/check-foundation-guards.ts — the same claim narrowed at its second site"
    - "scripts/generate-role-adapters.test.ts — the tautology replaced by the sibling-agreement property"
    - "scripts/validate-agent-factory.ts — `kitListDir` deleted"
    - "tsconfig.json — `noUnusedLocals` and `noUnusedParameters`"
    - ".planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md — the round-10 harness-integrity entry"
  key_links:
    - "`FENCE_DELIMITER_LINE` / `stripFencedBlocks` (scripts/frontmatter.ts) -> the guards' prose checks in check-foundation-guards.ts — the one authority the claims are about"
    - "the derived tracked-.ts scan -> the named set -> the pinned cardinality — the `derive the set, assert the count` shape, mirroring the D-50 / IN-05 assertion at scripts/frontmatter.test.ts:3311-3450"
---

<objective>
Close the three harness-integrity findings round 9's own code review raised, all of which are
instances of this repository's **second systemic failure class**: a hand-maintained set or a
hand-scoped pin that stays green while the thing it claims to bound grows.

- **WR-02** — a second fence state machine now exists (`scripts/generate-role-adapters.test.ts:194-213`),
  falsifying the unqualified claim at `scripts/frontmatter.ts:74-75` and its restatement at
  `scripts/check-foundation-guards.ts:519`. The pin meant to catch exactly this
  (`scripts/frontmatter.test.ts:4008-4020`) is scoped to one file and counts one spelling, so it is
  doubly blind.
- **WR-03** — `scripts/generate-role-adapters.test.ts:579-583` asserts `f(x) === f(x)` on a pure
  function: it cannot fail, and the sibling case it claims to guard never calls the function at all.
- **IN-01** — the dead local `kitListDir` at `scripts/validate-agent-factory.ts:88-94`, the repository's
  only `--noUnusedLocals` error.

Purpose: KIT-02's whole thesis is that a scan set is derived and its count asserted. WR-02 is that
thesis failing inside the phase that exists to enforce it, so it is closed by the same mechanism the
module already applies correctly to the frontmatter-parsing grammar — not by widening a literal.

**A measured caution the executor must act on.** The review proposes the answer as a hand-list —
`{scripts/frontmatter.ts, scripts/generate-role-adapters.test.ts, scripts/check-foundation-guards.ts (x2)}`.
This planner measured the tree at HEAD and that list is INCOMPLETE: `scripts/check-foundation-guards.test.ts`
carries three further fence-toggle sites (around `:2875`, `:2903` and `:2940`). Transcribing the
review's list would ship the set-literal-drift defect inside the fix for the set-literal-drift defect.
**Derive the set; measure it; then write down what the measurement returned.**

Output: a derived, sorted, cardinality-pinned fence-authority set over every tracked `.ts`; two prose
claims narrowed to the scope that assertion holds; a real property in place of the tautology; a
deleted dead local and two compiler flags that keep the next one from surviving.

Implements D-19 (proof is a recorded inventory plus a per-consumer assertion, explicitly not a
grep-shaped literal detector), D-50 and its IN-05 derived-scan precedent (scan every tracked `.ts` by
pattern, sort, compare to a named set, pin the cardinality, and prove the pin can fail), D-53 (a
claim is narrowed to what a mechanism holds rather than left overclaiming), and D-56 items 3 and 7
(decoration that reads like a floor is worse than no floor; assert the slice is the thing before
inspecting it).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW.md
@.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — WR-02, one derived fence-authority set from `git ls-files` to a pinned count and two narrowed claims</name>
  <files>scripts/frontmatter.test.ts, scripts/frontmatter.ts, scripts/frontmatter.js, scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js</files>
  <read_first>
    - `scripts/frontmatter.test.ts:3311-3460` — the D-50 / IN-05 derived assertion this fix MIRRORS:
      `HEAD_DELIMITER_CONSTRUCTS`, `KEY_LINE_CONSTRUCTS`, `isGrammarSite`, `grammarSitesAmong`,
      `trackedTs`, `liveGrammarSites`, the sorted comparison, the cardinality pin, and the sibling
      case that plants a third grammar into a temp directory so the pin is proven able to fail.
      Read the exclusion rationale carefully — it excludes `*.test.ts` for a reason that does NOT
      transfer here, and the difference must be stated rather than copied.
    - `scripts/frontmatter.test.ts:3983-4021` — the existing WR-02 pin: it slices `frontmatter.ts`
      alone and counts one spelling of the delimiter class.
    - `scripts/frontmatter.ts:72-90` and `:262-309` — the claim, `FENCE_DELIMITER_LINE`, and
      `stripFencedBlocks`, the one authority the claim is about.
    - `scripts/check-foundation-guards.ts:518-523` — the claim's second site, and `:1790-1810` /
      `:1891-1910`, the two caveman-block scopers that carry their own toggles.
    - `scripts/generate-role-adapters.test.ts:180-213` — `FenceStrip` and `stripFencedBlockLines`, the
      second machine, and the non-circularity rationale for not importing the production stripper,
      which is sound and is NOT what is being disputed.
  </read_first>
  <behavior>
    - Non-vacuity first: the tracked-`.ts` corpus is really enumerated (a floor on its length) and
      really contains the authority module, so the assertions below cannot pass over an empty list.
    - The derived set, sorted, equals the set MEASURED against the tree in this same run, and its
      cardinality is pinned as a number.
    - The classifier discriminates a fence STATE MACHINE from a mere textual REFERENCE to the fence
      pattern: a file that only names the delimiter class inside a string or an assertion message is
      not a second machine and must not be counted as one.
    - The pin is proven able to fail: an extra fence state machine planted into a temp directory makes
      the derived set differ from the named one, by name.
    - Removing any single recognised construct from the classifier's construct list changes the
      derived set — so no construct in the list is dead weight.
  </behavior>
  <action>
    Add a fence-grammar sibling to the D-50 / IN-05 derived assertion in
    `scripts/frontmatter.test.ts`, built by PATTERN over every tracked `.ts` in the repository, never
    from a hand-listed file-name set.

    Classify a file as carrying a fence state machine by a CONJUNCTION of two construct families, the
    same shape `isGrammarSite` uses: a fence-delimiter RECOGNISER (the anchored regex-literal form and
    the prefix-test form — both spellings, since the whole defect is that the old pin counted one) AND
    a TOGGLE (a boolean whose value is flipped, or an equivalent inside/outside state flip, in the
    same file). The conjunction is what keeps a file that merely quotes the delimiter class inside a
    string or an assertion message from being counted as a second machine — measure that
    discrimination rather than assuming it, because `scripts/frontmatter.test.ts` itself contains two
    such textual references and must not appear in the answer for them.

    Do NOT exclude `*.test.ts` from this corpus, and say why in the case's own commentary: the IN-05
    scan excludes them because a case's restated predicate is an INPUT to the authority rather than a
    second authority a consumer reads — but WR-02's second machine LIVES in a `.test.ts`, so applying
    the same exclusion here would reproduce the blindness the finding is about. The two scans have
    different exclusion rules for stated reasons; neither is copied from the other.

    Run the scan, MEASURE what it returns, and write the measurement down as the named expected set —
    sorted, compared with a set equality, and with its cardinality pinned as a number. This planner's
    own measurement at HEAD found sites in `scripts/frontmatter.ts`,
    `scripts/generate-role-adapters.test.ts`, `scripts/check-foundation-guards.ts` and
    `scripts/check-foundation-guards.test.ts`; treat that as a hint to CHECK against, not as the
    answer to transcribe, and state in the SUMMARY whether the measurement agreed with it.

    Add the sibling control that makes the pin falsifiable, mirroring the IN-05 planted-third-grammar
    case: copy the live members into a temp directory, assert the copies alone reproduce the live
    answer (the control that proves the failure below is caused by the plant and not by the temp
    directory), then plant one more fence machine and assert the derived set fails by name. Add the
    per-construct liveness check the D-53 / IN-02 precedent already applies: run the classifier once
    per construct with that construct removed and assert the derived set moves, so no construct in the
    list is decoration.

    Then narrow the two prose claims to the scope the new assertion actually holds. At
    `scripts/frontmatter.ts:74-75` and at `scripts/check-foundation-guards.ts:519`, replace the
    unqualified "here or anywhere" wording with a claim scoped to the surface the assertion covers —
    one PRODUCTION fence authority that the guards and the parser both read, with the harness-local
    machines enumerated by the derived set and their non-circularity rationale cited. Keep the
    existing single-spelling count at `frontmatter.test.ts:4014` only if it still states something
    true after narrowing; otherwise replace it with a reference to the derived set, and say in the
    SUMMARY which of the two happened and why.

    Rebuild both committed `.js` twins and run `npm run freshness`.
  </action>
  <verify>
    <automated>npx vitest run scripts/frontmatter.test.ts</automated>
    <automated>npm run freshness</automated>
    <automated>node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - The derived fence-authority set is computed from `git ls-files` over `.ts` in the same run,
      sorted, compared as a set equality against a named expected set, and its cardinality asserted as
      a number — all three assertions present.
    - The corpus non-vacuity floor is present: the tracked-file list length is asserted above a floor
      and asserted to contain the authority module.
    - `scripts/frontmatter.test.ts` does NOT appear in the derived set, and the SUMMARY states the
      measured reason (it carries textual references to the delimiter class but no toggle), proving
      the conjunction discriminates.
    - The planted-extra-machine control fails by name, and its temp-directory control passes first —
      both transcripts in the SUMMARY.
    - Removing any single construct from the classifier's construct list changes the derived set;
      the per-construct result table is in the SUMMARY.
    - The narrowed claim at `scripts/frontmatter.ts` and the narrowed claim at
      `scripts/check-foundation-guards.ts` each state a scope the derived assertion holds, and the
      SUMMARY quotes both new sentences verbatim.
    - `npm run freshness` exits 0.
    - `node scripts/check-foundation-guards.js` exits 0 on the real tree.
    - The SUMMARY states whether the measurement agreed with the review's proposed hand-list and, if
      not, exactly how they differed.
  </acceptance_criteria>
  <done>The fence-authority scope is derived, sorted, pinned by cardinality and proven falsifiable, and the two prose claims state only what that assertion holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-03 — replace the assertion that cannot fail with the property its own comment describes</name>
  <files>scripts/generate-role-adapters.test.ts</files>
  <read_first>
    - `scripts/generate-role-adapters.test.ts:180-240` — `FenceStrip`, `stripFencedBlockLines` and
      `assertFenceStripPremise`, including what each half of the premise is load-bearing for.
    - `scripts/generate-role-adapters.test.ts:555-593` — the unterminated-region case carrying the
      tautology at `:579-583`.
    - `scripts/generate-role-adapters.test.ts:595-640` — the IN-04 premise case that proves the
      premise can fire.
    - `scripts/generate-role-adapters.test.ts:642-665` — the case named as the "sibling", which writes
      `lines.join("\n")` UNSTRIPPED and probes with a prefix test; it never calls
      `stripFencedBlockLines`, so the two fixtures are related by prose only.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW.md` § WR-03 — it gives the
      replacement assertions verbatim.
  </read_first>
  <behavior>
    - At least one line really was removed by the strip (the fixture is not a no-op).
    - The kept lines plus the removed count reconstitute the input length exactly — the strip
      partitions the input rather than losing or duplicating lines.
    - No fence delimiter line survives in the kept lines.
    - The two sibling fixtures are built from the SAME splice of the SAME source file — the property
      the old comment claimed and the old assertion did not test.
  </behavior>
  <action>
    Delete the tautology at `scripts/generate-role-adapters.test.ts:579-583`. It calls
    `stripFencedBlockLines` twice on the same unmutated `readonly string[]` and compares the results,
    which is `f(x) === f(x)` on a pure function and cannot fail for any implementation, correct or
    otherwise. Its stated purpose — guarding the two sibling fixtures against drifting apart — is also
    not met, because the sibling case never calls the function.

    Replace it with the property the comment describes, as the review states it: assert the strip
    really removed lines, assert the kept count plus the removed count equals the input length, and
    assert no fence delimiter line survives among the kept lines. Then make the SIBLING relationship a
    real assertion rather than prose: both cases splice the same index out of the same source file, so
    assert that the two cases' inputs are derived from one shared construction — extract the shared
    splice into one helper both cases call, and assert in the stripped case that its input equals the
    unstripped case's input before the strip is applied. A pair pinned by prose is not a pair.

    Do not import the production stripper into this file. The non-circularity rationale recorded at
    `:180-185` is sound and is preserved; task 1 makes that local machine a MEMBER of a derived,
    counted set rather than an unaccounted second grammar, which is the correct resolution.
  </action>
  <verify>
    <automated>npx vitest run scripts/generate-role-adapters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The two-identical-calls comparison is gone from `scripts/generate-role-adapters.test.ts` — its
      absence stated in the SUMMARY with the surrounding case's new assertion list quoted.
    - The replacement assertions are proven able to fail: run each once against a deliberately broken
      local variant of `stripFencedBlockLines` (one that keeps delimiter lines, and one that drops an
      extra line) and record that each variant makes a named assertion red.
    - The shared splice is a single helper both sibling cases call, and the stripped case asserts its
      input equals the unstripped case's input before stripping.
    - `npx vitest run scripts/generate-role-adapters.test.ts` reports 0 failed.
  </acceptance_criteria>
  <done>The tautology is gone, its replacements are proven able to fail against broken variants, and the sibling pair is joined by a shared construction rather than by a comment.</done>
</task>

<task type="auto">
  <name>Task 3: IN-01 — delete the dead local, turn on the two compiler flags, and record the round-10 harness entry</name>
  <files>scripts/validate-agent-factory.ts, scripts/validate-agent-factory.js, tsconfig.json, .planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md</files>
  <read_first>
    - `scripts/validate-agent-factory.ts:52` (the `node:fs` import) and `:78-110` — `kitExists`,
      `kitRead`, `kitListDir`, `stateExists`, `stateRead` and the state-side list helper. Note that
      the state-side helper still uses `readdirSync`, so the import must stay.
    - `tsconfig.json` — the current compiler options, and the `exclude` entry for `**/*.test.ts`,
      which bounds what the new flags will actually cover.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/27-REVIEW.md` § IN-01 — the measured
      claim that the tree is otherwise clean under both flags today.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — the `From 27-50`
      sections, for the ledger entry shape.
  </read_first>
  <action>
    Delete `kitListDir` from `scripts/validate-agent-factory.ts`. Its callers went away when the kit
    sets it served moved to `scripts/kit-model.ts`, and it is the repository's only unused-local error.
    Leave the `readdirSync` import in place — the state-side list helper still uses it; confirm that by
    reading rather than by assuming, and state the confirmation in the SUMMARY.

    Add `noUnusedLocals` and `noUnusedParameters` to `tsconfig.json`. MEASURE before enabling: run the
    compiler with both flags and record every error it reports, so the claim "the tree is otherwise
    clean under both flags" is this round's measurement rather than the review's. If the measurement
    disagrees with the review, fix what it names or record the disagreement with a disposition — never
    enable the flags over a suppressed error and never silently narrow the `include` set to make them
    pass. State honestly in the SUMMARY that `tsconfig.json` excludes `**/*.test.ts`, so the flags
    cover shipped sources and not the harness, and record that scope rather than letting a reader
    infer a wider one.

    Rebuild the committed `.js` twin and run `npm run freshness`.

    Write the round-10 harness-integrity entry in `deferred-items.md`: what WR-02's derived set
    measured (including whether it agreed with the review's proposed hand-list), what WR-03's
    replacement assertions were proven able to fail against, what the compiler measurement returned,
    and any residual this plan's own dispositions create — named so it is not rediscovered as a
    finding, in the shape the `From 27-49` and `From 27-50` residual entries already use.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run freshness</automated>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <automated>node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with `noUnusedLocals` and `noUnusedParameters` present in
      `tsconfig.json`.
    - The pre-enable compiler measurement is recorded verbatim in the SUMMARY, including a zero-error
      result if that is what it returned.
    - The flags are proven able to fire: introduce one unused local in a scratch copy of a shipped
      source file, record the named `TS6133` error, then revert.
    - The SUMMARY records that `tsconfig.json` excludes `**/*.test.ts`, so the flags' scope is the
      shipped sources.
    - `npm run freshness` exits 0.
    - `npx vitest run --exclude '**/scripts/e2e/**'` reports 0 failed.
    - `node scripts/check-foundation-guards.js` exits 0, and `node scripts/validate-agent-factory.js`
      exits with the same status and output shape it had before the deletion — recorded before and
      after.
    - `deferred-items.md` carries a `From 27-53` section with the three measurements and any residual.
  </acceptance_criteria>
  <done>The dead local is gone, both flags are on with a measured clean tree and a proven-able-to-fire control, and the ledger records what each measurement returned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prose claim -> reader | An unqualified claim in a source header is what a reviewer and a future maintainer trust instead of re-deriving; a claim wider than its pin is an untrusted assertion presented as a verified one. |
| harness assertion -> CI verdict | An assertion that cannot fail contributes a green cell that carries no information, which is indistinguishable at CI from a real pass. |
| `.ts` source -> committed `.js` -> host machine | Hosts run the committed build with no toolchain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-53-01 | Spoofing | the "exactly one fence authority" claim at `scripts/frontmatter.ts:74-75` and `scripts/check-foundation-guards.ts:519` | high | mitigate | A false claim of a single authority invites a future author to trust one grammar where several exist, which is how a fence-scoped guard comes to disagree with the parser. Closed by the derived, sorted, cardinality-pinned set in task 1 and by narrowing both sentences to the measured scope. |
| T-27-53-02 | Repudiation | `scripts/generate-role-adapters.test.ts:579-583` | high | mitigate | A green assertion that cannot fail is evidence of nothing while reading as evidence of something. Closed in task 2, with each replacement proven able to fail against a deliberately broken variant. |
| T-27-53-03 | Tampering | the fence-authority named set | high | mitigate | Transcribing the review's hand-list would ship the drift defect inside its own fix — this planner measured that list to be incomplete at HEAD. Closed by requiring the set be derived and measured in the same run, with the agreement or disagreement stated. |
| T-27-53-04 | Information Disclosure | dead code retained in a shipped validator | low | accept | `kitListDir` reads only inside the kit root and has no callers; it is deleted for hygiene rather than to close an exposure. Accepted as low with the deletion recorded. |
| T-27-53-SC | Tampering | package-manager installs | low | accept | This plan runs no package-manager install and changes no dependency; `tsconfig.json` gains two compiler options and no packages. No package-legitimacy checkpoint is required, and the absence is recorded rather than left silent. |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**'` reports 0 failed. Do NOT run `npm test`.
- `npx tsc --noEmit` exits 0 with both new flags enabled.
- `npm run freshness` exits 0 — all committed `.js` twins match a fresh `tsc` rebuild.
- `node scripts/check-foundation-guards.js` exits 0; `node scripts/validate-agent-factory.js` is
  unchanged in status and output shape.
</verification>

<success_criteria>
1. The fence-authority scope is a derived, sorted, cardinality-pinned set over every tracked `.ts`,
   proven falsifiable by a planted extra machine and proven non-decorative by per-construct removal.
2. Both prose claims state only what that assertion holds, quoted verbatim in the SUMMARY.
3. The tautology is replaced by assertions proven able to fail, and the sibling pair is joined by a
   shared construction.
4. `kitListDir` is deleted, both compiler flags are enabled over a measured clean tree, and the flags
   are proven able to fire.
5. Every measurement — including any disagreement with the review's own hand-list — is recorded in
   `deferred-items.md` rather than left implicit.
</success_criteria>

## Artifacts this plan produces

**Test symbols (new)**
- `scripts/frontmatter.test.ts` — the fence-delimiter RECOGNISER construct list and the TOGGLE
  construct list; the conjunction classifier over them; the tracked-`.ts` corpus reader reused from
  the IN-05 precedent; the derived fence-authority site list; the sorted set equality; the cardinality
  pin; the planted-extra-machine control with its temp-directory control; the per-construct liveness
  table.
- `scripts/generate-role-adapters.test.ts` — the shared splice helper both sibling fixtures call; the
  partition assertion (kept plus removed equals input length); the no-surviving-delimiter assertion;
  the input-equality assertion joining the two siblings.

**Source changes**
- `scripts/frontmatter.ts` — the narrowed fence-authority claim (prose only; no behaviour change).
- `scripts/check-foundation-guards.ts` — the same claim narrowed at its second site (prose only).
- `scripts/validate-agent-factory.ts` — `kitListDir` deleted; the `readdirSync` import retained for
  the state-side helper.
- Rebuilt committed twins: `scripts/frontmatter.js`, `scripts/check-foundation-guards.js`,
  `scripts/validate-agent-factory.js`.

**Config keys (new)**
- `tsconfig.json` — `compilerOptions.noUnusedLocals`, `compilerOptions.noUnusedParameters`.

**Planning artifacts**
- `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — a `From 27-53` section
  with the three measurements and any residual.

**No new files, no new CLI flags, no new dependencies.**

## Multi-source coverage audit

| Source | Item | Covered by |
|---|---|---|
| GOAL | "every guard and validator scan set is derived from the filesystem" — the kit-set-authority half of the phase goal | Task 1 (a third derived set, built by the same mechanism) |
| REQ | KIT-02 — every scan set derives, never hand-listed | Task 1, Task 3 |
| REQ | SPAWN-01 — the generated-adapter harness that guards the thin-pointer contract | Task 2 |
| RESEARCH | "derive the set, assert the count" as the milestone's foundational structural fix, and the seven measured instances of set-literal drift | Task 1 |
| CONTEXT | D-19 — a recorded inventory plus a per-consumer assertion, explicitly NOT a grep-shaped literal detector | Task 1 |
| CONTEXT | D-50 and its IN-05 derived-scan precedent | Task 1 |
| CONTEXT | D-53 — narrow an overclaim into a mechanical, correctly-scoped one | Task 1 |
| CONTEXT | D-56 item 3 — decoration that reads like a floor is worse than no floor | Task 2 |
| CONTEXT | D-56 item 7 — assert the slice is the thing before inspecting it | Task 1, Task 2 |

No item is MISSING.

## Specless probe fallback — no-silent-drop accounting

This plan owns the **7** rows for the two requirements it closes:

| Requirement | Rows | Disposition |
|---|---|---|
| KIT-02 | adjacency, empty, ordering (3) | all resolved `explicit` — authored as plain strings in `must_haves.truths` |
| SPAWN-01 | adjacency, empty, encoding, ordering (4) | all resolved `explicit` — authored as plain strings in `must_haves.truths` |

**Equality for this plan: 7 probe-surfaced rows == 7 authored into `must_haves.truths` + 0 surfaced
as flagged assumptions.** No row was auto-dismissed.

**Round equality (unchanged): 29 == 26 explicit + 3 backstop + 0 unresolved**, distributed `27-51` 7,
`27-52` 0, `27-53` 7, `27-54` 15.

<output>
Create `.planning/phases/27-spawn-correctness-kit-set-authority/27-53-SUMMARY.md` when done, carrying
the derived fence-authority set verbatim, the agreement or disagreement with the review's hand-list,
the planted-machine control transcripts, the broken-variant transcripts for the WR-03 replacements,
and the compiler measurement.
</output>

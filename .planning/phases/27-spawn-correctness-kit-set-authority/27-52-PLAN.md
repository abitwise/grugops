---
phase: 27-spawn-correctness-kit-set-authority
plan: 52
type: execute
wave: 2
depends_on: ["27-51"]
gap_closure: true
files_modified:
  - scripts/frontmatter.ts
  - scripts/frontmatter.js
  - scripts/frontmatter.test.ts
  - .planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md
  - .planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md
autonomous: false
requirements: [KIT-03, SPAWN-04]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `|` or `>` block-scalar header is treated the same way at EVERY position YAML allows one — as a nested mapping's value, as a sequence item, and deeper — not at the single position `flattenBlock`'s top-level key line occupies today."
    - "Family G (`tools:` / `  nested: >-` / `    Read,` / `    # x, Agent(grugops-orchestrator)`) and family G2 (the same content under `  - >-`) stop returning the no-grant SUCCESS arm where `/usr/bin/ruby -ryaml` accepts the document with the grant in the loaded value."
    - "A live `Agent(grugops-orchestrator)` grant planted in either family shape on both distribution twins of a non-coordinator skill takes `node scripts/check-foundation-guards.js` from exit 0 to a named non-zero failure (SPAWN-04)."
    - "Whatever disposition is chosen, the module's answer moves only in the fail-safe direction: a document that granted before still grants, and no document the loader accepts newly returns a SHORTER value than it did (KIT-03 — a name is never silently dropped or altered when computing the grant closure, D-09)."
    - "The D-52 generated corpus can EXPRESS a nested block-scalar header after the fix lands, and the corpus shape is added in this same plan rather than in a later one — a fix whose corpus arrives afterwards is the failure mode 27-49 recorded (D-52, D-56 item 1)."
    - "The four tracked markdown files already carrying a nested block-scalar header are re-measured against the post-fix build and their verdicts recorded, so the measured false-red cost is a number in this round rather than a memory from `27-47`."
  prohibitions:
    - "No heuristic may guess where a nested block scalar ends — the end condition must be YAML's own indentation rule, derived, or the scalar must be treated fail-safe to the end of the key's region."
    - "No second block-indicator grammar may be written beside `BLOCK_INDICATOR`."
    - "The nested-block-scalar shape must NOT be added to the corpus before the fix lands in the same plan — a corpus shape for a still-live silent no-grant puts the differential's never-exemptible direction into failure."
    - "No exemption may be added to the D-52 differential's `silentWhileLoaderGrants` direction to make the new shape pass."
    # Canon security/compliance prohibitions dropped with a breadcrumb: no package install, no
    # network path, no credential handling in this plan — see <threat_model> for the applicable set.
  artifacts:
    - "scripts/frontmatter.ts — block-scalar-header recognition at every node-start position"
    - "scripts/frontmatter.js — the committed build, fresh against `tsc`"
    - "scripts/frontmatter.test.ts — the nested-block-scalar member(s) of `AXIS_KEY_LINE` and the family G/G2 regression cases"
    - ".planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md — the ratified round-10 decision, recorded as D-57"
    - ".planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md — the round-10 family G/G2 closure entry"
  key_links:
    - "`BLOCK_INDICATOR` (scripts/frontmatter.ts:324) -> `flattenBlock`'s baseline branch at :1419 — today the ONLY place the constant is applied"
    - "`flattenBlock`'s block-sequence item path (:1259-1338) and continuation path (:1340-1400) — the two positions a nested header can appear and is currently never recognised"
    - "un-recognised header content -> `stripComment` -> a leading `#` stripped as a comment -> `hasSpawnGrant` no-grant SUCCESS arm -> `guard_wr05` green"
---

<objective>
Close **Family G / G2**, the second of the two live silent-no-grant bypasses standing against
criteria 2 (KIT-03) and 4 (SPAWN-04). `BLOCK_INDICATOR` is recognised at exactly ONE of the positions
YAML allows a block-scalar header — `flattenBlock`'s top-level key line — so a `|` or `>` header
appearing as a nested mapping's value or as a sequence item is never recognised, `cur.block` stays
false, and the block scalar's LITERAL content is routed through `stripComment`, where a leading `#`
hides a live `Agent(grugops-orchestrator)` grant on the SUCCESS arm.

This family has been re-measured byte-identical against the committed build by four consecutive plans
(`27-47`, `27-48`, `27-49`, `27-50`) and left explicitly OPEN in `deferred-items.md` every time.
Round 10 owns it, and the two prior rounds' recorded direction is followed: **close family G first,
then add the nested-block-scalar header to `AXIS_KEY_LINE` in the SAME plan**, so the corpus grows
with the fix rather than after it.

Purpose: this is the same failure class one abstraction level up from CR-01 — a predicate recognised
at one of the several positions its grammar allows. It is a predicate's APPLICATION SET, not its
conditions, and the ledger names it as such. Enumerating the two G/G2 spellings is the failure mode;
reject it.

Output: block-scalar-header recognition at every node-start position, the family closed at the gate
with RED/GREEN transcripts, the corpus shape added, the measured false-red cost restated as a number,
and a ratified decision recorded as D-57.

Implements D-54 (a property of structural position, decided in one place), D-52 and D-56 item 1 (the
corpus must be able to express the family it claims completeness over), D-30 (ask WHICH SET the
predicate enumerates — here, which set of POSITIONS it applies to), D-09 (the KIT-03 closure equality
this bypass reaches), D-40 (a plant lands on both distribution twins), and D-19 (recorded measurement
over grep-shaped heuristics). It also settles, once, the scope question the `27-48` ledger entry
raises about this module's reach over `.planning/` prose.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-VERIFICATION.md
@.planning/phases/27-spawn-correctness-kit-set-authority/27-51-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: the nested block-scalar header disposition (records D-57)</name>
  <decision>
    How does `scripts/frontmatter.ts` treat a `|` / `>` block-scalar header that appears at a YAML
    node-start position other than `flattenBlock`'s top-level key line — as a nested mapping's value,
    as a block-sequence item, or deeper?
  </decision>
  <context>
    The ledger states this is a planning decision with a reversibility note and not something an
    executor settles mid-plan, and it already quantifies the cost of the obvious alternative. Over a
    run-time-derived corpus of 1149 tracked markdown files, 570 carry a locatable frontmatter block
    and **4** already carry a nested block-scalar header on a continuation line —
    `.planning/milestones/v1.2-MILESTONE-AUDIT.md` (`evidence: >`),
    `.planning/milestones/v1.2-phases/15-typescript-tooling-migration/15-VERIFICATION.md` (`note: >`),
    `.planning/milestones/v2.0-phases/25-governance-on-a-dial/25-VERIFICATION.md` (`reason: >`) and
    `.planning/phases/27-spawn-correctness-kit-set-authority/27-VERIFICATION.md` (`reason: >`). None is
    an adapter, a skill or a packaging template; all four are documents libyaml accepts. So a blanket
    refusal is not free.

    The same choice also settles the scope question the `27-48` ledger entry raises separately —
    eleven of 649 loader-accepted cells whose flattened value disagrees with the loader's content
    signature on BOTH builds, none of them a `tools` / `allowed-tools` / `coordinator` / `name` key.
    The ledger's own suggested direction is to settle that scope ONCE, here, rather than twice from
    two symptoms.
  </context>
  <options>
    <option id="option-a-recognise-indented">
      <name>Recognise the header at every node-start position, ending the scalar by YAML's own indentation rule</name>
      <pros>Agrees with the loader on all seven measured rows (G, G2, g1-g5); zero false reds on the four existing documents; block-scalar content is joined as literal text so a leading `#` can never delete a token; the end condition is YAML 1.2's own more-indented-block rule, which is DERIVED rather than guessed; it also fixes row g5's still-wrong name set as a side effect rather than merely making it less wrong.</pros>
      <cons>Adds indentation tracking to the flattener — the "genuine nesting / YAML engine" direction D-54 rejects by name; the module's declared scope grows; later rounds re-cut their corpus and repository-wide value-map baselines against it.</cons>
    </option>
    <option id="option-b-refuse">
      <name>Refuse a nested block-scalar header — route it to the failure arm</name>
      <pros>Smallest possible change; states no new grammar; cannot produce a silent no-grant by construction; it is the module's founding rule applied literally (content this module cannot account for goes to the failure arm).</pros>
      <cons>Measured false-red cost of 4 loader-accepted tracked documents, all `.planning/` prose the guards never scan but the repository-wide harness does; a refusal over `.planning/` prose exceeds this module's declared scope, which is the open scope question rather than an answer to it; row g5's altered name set stays wrong for a different reason.</cons>
    </option>
    <option id="option-c-recognise-failsafe">
      <name>Recognise the header at every node-start position, and treat every subsequent line of that key's region as literal content without computing a precise end</name>
      <pros>Cannot under-include a token — over-inclusion yields at worst a loud false RED, never a silent no-grant, which is the direction this module is allowed to be wrong in; no indentation arithmetic at all.</pros>
      <cons>Over-inclusion can fold a sibling key's value into the block scalar, moving the flattened value away from the loader's on documents the loader accepts; it is the coarse variant of option A and would very likely be replaced by option A in a later round, which is churn this phase can least afford.</cons>
    </option>
  </options>
  <recommendation>
    **option-a-recognise-indented.** It is the only option whose answer equals the loader's on every
    measured row, and its end condition is YAML's own rule rather than a heuristic — which is exactly
    the derive-versus-enumerate distinction this phase has paid nine rounds to learn. Option B trades
    a silent wrong answer for a loud wrong answer on four real documents and leaves the scope question
    open; option C is option A with a guessed end and would be re-cut later.
  </recommendation>
  <reversibility rating="one-way">
    Once the flattener carries block-scalar nesting, every later round's generated corpus, repository-
    wide value map and gate baselines are re-cut against the widened contract, and the module's
    declared scope changes with it. Reverting after subsequent rounds build on those baselines is not
    a clean revert, which is why this is gated rather than settled in the implementing task.
  </reversibility>
  <read_first>
    - `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — the `From 27-47`
      section (the seven measured rows, the quantified false-red cost, and the list of shapes probed
      that did NOT reproduce), the `From 27-48` re-measurement and the non-defect divergence entry
      that raises the scope question, the `From 27-49` expressibility-floor note, and the `From 27-50`
      re-measurement.
    - `scripts/frontmatter.ts:324` (`BLOCK_INDICATOR`) and `:1403-1470` (`flattenBlock`'s baseline
      branch — the constant's ONLY application point).
    - `scripts/frontmatter.ts:1210-1400` — the block-sequence item path and the continuation path,
      i.e. the two positions the header currently reaches unrecognised.
  </read_first>
  <acceptance_criteria>
    - The chosen option is recorded in `27-CONTEXT.md` as **D-57** under a `Gap-closure round 10`
      heading, mirroring the round-5 through round-8 decision blocks, with its rationale and its
      reversibility rating.
    - The rejected options are recorded with the reason they were rejected — never a silent drop.
    - No implementation begins before the decision is recorded.
  </acceptance_criteria>
  <resume-signal>Select: option-a-recognise-indented, option-b-refuse, or option-c-recognise-failsafe</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — one nested block-scalar header, from the document to a red gate</name>
  <files>scripts/frontmatter.ts, scripts/frontmatter.js, scripts/frontmatter.test.ts, .planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md</files>
  <read_first>
    - `scripts/frontmatter.ts:324` — `BLOCK_INDICATOR`, and `:1419` where it is the ONLY thing tested.
    - `scripts/frontmatter.ts:1085-1470` — the whole of `flattenBlock`: the item path, the
      continuation path, `startsNode`, `cur.block`, `cur.seq`, `cur.seqIndent`, `cur.nodeStarted` and
      the flush, so the fix lands where the position is actually known.
    - `scripts/frontmatter.ts:648-800` — `stripComment`, which currently receives the header's
      literal content and applies comment rules YAML does not give it.
    - `scripts/check-foundation-guards.ts:686` and `:2378` — the two guards that consume the verdict.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — the seven-row
      table (G, G2, g1, g2, g3, g4, g5) with the loader column, and the six shapes already probed
      that do NOT reproduce, so this task does not re-walk them.
    - `27-51-SUMMARY.md` — the CR-01 fix that landed in wave 1, so this plan's re-measurement is
      against that build and the two edits stay separable.
  </read_first>
  <behavior>
    Written as failing evidence FIRST on the pre-fix build, then made to pass. Every row is a document
    `/usr/bin/ruby -ryaml` ACCEPTS with the grant plainly in the loaded value:
    - G — `tools:` / `  nested: >-` / `    Read,` / `    # x, Agent(grugops-orchestrator)`; loader
      reads a mapping whose `nested` value carries the grant. Module must grant.
    - G2 — the same content under `  - >-`; loader reads a sequence whose item carries the grant.
      Module must grant.
    - g1 — the indentation-indicator spelling `>2-`. Module must grant.
    - g2 — the keep-chomping spelling `|+`. Module must grant.
    - g3 — a header carrying its own comment, `> # h`. Module must grant.
    - g4 — the same shape under the `allowed-tools:` key form. Module must grant.
    - g5 — `  nested: |` / `    Agent(alpha, ga` / `    - mma)`; the loader expresses two names with a
      line break inside the second. The module's name set must equal the loader's rather than
      inventing or altering a name (D-09).
    - Controls: the six shapes the ledger records as probed and NOT reproducing must keep their
      current verdicts byte-unchanged, and the four tracked `.planning/` documents that already carry
      a nested block-scalar header must keep parsing.
  </behavior>
  <action>
    Implement the option ratified in the checkpoint, in `flattenBlock`, at the positions where the
    structural position is actually known — the block-sequence item path and the continuation path —
    rather than by widening `BLOCK_INDICATOR`'s pattern or by writing a second indicator grammar
    beside it. `BLOCK_INDICATOR` is the one authority on what a header LOOKS LIKE; the defect is
    WHERE it is asked. Apply the same constant at the other node-start positions and carry the
    resulting block state on the accumulator the same way `cur.block` already carries it for the
    top-level key.

    Under option A, the scalar's end is YAML's own more-indented-block rule: content belongs to the
    header while its indentation exceeds the indentation of the line the header appeared on, and the
    first line at or below that indentation ends the scalar. Record the header's line indentation on
    the accumulator when the header is recognised, and derive the end from it — never from a fixed
    depth, never from a line-shape guess. Under option B, refuse at the header with a named reason
    that identifies the key and the header line. Under option C, mark the key's remaining region
    literal from the header onward.

    Whichever landed, the content of a recognised block scalar must NOT be routed through
    `stripComment`'s comment condition and must NOT be split by `SEQ_ITEM` — inside a block scalar
    YAML gives neither construct its meaning, which is the same asymmetry `unquoteChecked` already
    honours for block scalars per D-50 / IN-02.

    Take the evidence in this order and record every transcript verbatim in the SUMMARY:
    (1) RED — against the build committed by `27-51`, run all seven rows plus the controls through the
    module and through `/usr/bin/ruby -ryaml`, side by side;
    (2) apply the fix, rebuild the committed `.js` twin, run `npm run freshness`;
    (3) GREEN — re-run the identical table;
    (4) PLANT AT THE GATE — a hermetic `git ls-files` mirror of HEAD with the `CHECK_ROOT` override,
    family G planted on both distribution twins of the non-coordinator `plan` skill
    (`skills/plan/SKILL.md` and `.claude/skills/grugops-plan/SKILL.md`), then family G2 the same way;
    record `node scripts/check-foundation-guards.js` exit codes before and after;
    (5) FALSE-RED CONTROLS — re-run the whole gate on the unplanted mirror, and re-parse the four
    tracked `.planning/` documents that carry a nested block-scalar header, recording their verdicts
    before and after as the measured false-red cost of the option that landed.
  </action>
  <verify>
    <automated>npx vitest run scripts/frontmatter.test.ts</automated>
    <automated>npm run freshness</automated>
    <automated>node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - Against the pre-fix build, rows G, G2, g1, g2, g3 and g4 are loader-ACCEPTED with the grant in
      the loaded value while the module returns the no-grant SUCCESS arm — the RED transcript is in
      the SUMMARY verbatim.
    - Against the post-fix build, those six rows return the grant arm.
    - Row g5's module name set equals the loader's name set exactly, compared as sorted sets.
    - Hermetic mirror, family G planted on both distribution twins of the non-coordinator `plan`
      skill: `node scripts/check-foundation-guards.js` exits 0 on the pre-fix build and exits **1** on
      the post-fix build, naming the WR-05 coordinator-spawn-grant violation. Same for family G2.
    - `node scripts/check-foundation-guards.js` on the unplanted mirror and on the real tree exits 0.
    - The four tracked `.planning/` documents carrying a nested block-scalar header are re-parsed and
      their verdicts recorded before and after; the count of documents whose verdict changed is stated
      as a number in the SUMMARY.
    - The six ledger-recorded non-reproducing shapes have byte-identical verdicts before and after.
    - `npm run freshness` exits 0.
    - `BLOCK_INDICATOR` remains the single pattern for what a header looks like — a count of its
      definition sites in the tree is stated in the SUMMARY and is 1.
  </acceptance_criteria>
  <done>Families G and G2 grant where the loader grants, a plant of either turns the foundation gate red at the gate, and the false-red cost of the ratified option is a measured number rather than a memory.</done>
</task>

<task type="auto">
  <name>Task 2: the corpus grows WITH the fix — the nested block-scalar header enters `AXIS_KEY_LINE`, plus two adversarial passes and the ledger closure</name>
  <files>scripts/frontmatter.test.ts, .planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md</files>
  <read_first>
    - `scripts/frontmatter.test.ts:6047-6272` — `AXIS_KEY_LINE`, including its existing top-level
      `literal block indicator` and `folded block indicator` members, whose `lines` carry the header
      at the TOP level only.
    - `scripts/frontmatter.test.ts:6352-6390` — `AXIS_CONTINUATION_DEPTH`, `FILLER` and
      `buildCellParts`, so the new shapes compose through the existing builder.
    - `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — the `From 27-49`
      entry explaining why the shape was NOT added a round ago (a corpus shape for a live silent
      no-grant puts the never-exemptible direction into failure) and its suggested direction for
      round 10.
    - `27-51-SUMMARY.md` — the `AXIS_QUOTE_STYLE` and `AXIS_ESCAPE_IN_SCALAR` axes added in wave 1,
      so the new members cross them rather than sidestep them.
  </read_first>
  <action>
    ORDER IS LOAD-BEARING: this task runs only after task 1's fix is green. Adding the shape while the
    family is still live would put the differential's never-exemptible `silentWhileLoaderGrants`
    direction into failure, which is why `27-49` declined to add it.

    Add the nested block-scalar header to `AXIS_KEY_LINE` as SHAPES, with their declared YAML facts
    filled in the same way every other member declares them (`valueNodeOnContinuation`,
    `danglingNodeProperty`, `flowNodeStartAtEndOfKeyLine`): the header as a nested mapping's value,
    and the header as a block-sequence item. Give them the `indent` and `tail` the builder needs so
    they compose with `AXIS_CONTINUATION_1`, `AXIS_CONTINUATION_2`, `AXIS_CONTINUATION_DEPTH` and
    wave 1's two new axes without a forked builder.

    Then close the expressibility hole the `27-49` ledger entry names as a PROPERTY OF THE FLOOR: the
    `WR-01 expressibility floor` derives its family list from the ledger in `scripts/frontmatter.ts`'s
    header, and an OPEN bypass has no ledger entry, so it sits outside the floor's derived set by
    construction. Now that family G is closed it earns a ledger entry, so add that entry to the module
    header in the ledger's established shape (the failure, its remedy) and confirm the floor's derived
    set grows to include it — measured, by running the floor and stating its cardinality before and
    after, never by comment.

    Run TWO INDEPENDENT adversarial passes and record both, whether or not they find anything:
    (a) *Which SET of positions does this predicate apply to?* — enumerate the positions YAML gives a
    block-scalar header and confirm each is now reached, including a header nested two levels deep, a
    header as the value of a key inside a sequence item, and a header immediately following another
    block scalar's content.
    (b) *What is this predicate's INPUT assembled from?* — walk the value from the header's recognition
    through the accumulator, the flush, `unquoteChecked` and `grantedAgentNames`, asking at each hop
    what the next stage assumes. Include the UNION with wave 1's fix: a nested block-scalar header
    whose content carries a single-quoted scalar with an in-scalar escape, because the round-8 lesson
    is that splitting a predicate into arms demands testing their union.

    Write the round-10 closure entry in `deferred-items.md`: family G/G2 CLOSED with its pre/post
    table and its gate exit codes, the measured false-red cost of the ratified option, the scope
    question from the `27-48` non-defect divergence entry settled or explicitly restated as still open
    with its reason, and any newly found bypass recorded as OPEN with owner and disposition.
  </action>
  <verify>
    <automated>npx vitest run scripts/frontmatter.test.ts</automated>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    <automated>node scripts/check-foundation-guards.js</automated>
    <automated>node scripts/adapters-freshness.js</automated>
  </verify>
  <acceptance_criteria>
    - The D-52 differential's printed cell count is strictly greater after the new members land, and
      both numbers are stated in the SUMMARY.
    - `silentWhileLoaderGrants` is `[]` and `grantsLoaderDoesNot` is `[]` with the new members in the
      corpus, and no exemption was added to make that true — the exemption list's length is stated
      before and after and is unchanged.
    - Run against a hermetic mirror of the pre-fix commit with the same new members, the differential
      reports a NON-empty `silentWhileLoaderGrants` — the corpus is proven able to see the family it
      was blind to.
    - The expressibility floor's derived family set grows by the family-G entry; its cardinality is
      stated before and after.
    - Two independent adversarial passes are recorded, each naming its starting question, the shapes
      probed and the result, including passes that found nothing.
    - The union case (a nested block-scalar header whose content carries a single-quoted scalar with an
      in-scalar escape) is run with its loader column recorded.
    - `deferred-items.md` carries a `From 27-52` section recording family G/G2 as CLOSED with its
      transcripts, and any new finding as OPEN with a disposition.
    - `npx vitest run --exclude '**/scripts/e2e/**'` reports 0 failed; the SUMMARY states in one line
      that this is a floor and not the closure evidence.
    - `node scripts/check-foundation-guards.js` and `node scripts/adapters-freshness.js` both exit 0
      on the real tree.
  </acceptance_criteria>
  <done>The corpus can express a nested block-scalar header, the expressibility floor's derived set has grown to include the family, and the ledger records the closure with its transcripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository markdown -> `parseFrontmatter` | Any tracked `.md` file's frontmatter region is untrusted input; a nested block-scalar header is a legal YAML construct an author or a generator can emit at any time. |
| `flattenBlock` -> `stripComment` | Content that YAML gives no comment meaning is currently handed to a scanner that applies comment rules to it — the boundary the bypass crosses. |
| committed `.js` -> host machine | Hosts run the committed build with no toolchain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-52-01 | Elevation of Privilege | `BLOCK_INDICATOR`'s single application point -> `hasSpawnGrant` -> `guard_wr05` | critical | mitigate | A non-coordinator surface carrying a grant inside a nested block scalar gains spawn capability the safety model reserves for the coordinator. Closed by recognising the header at every node-start position, proven by both G and G2 plants taking the gate exit 0 -> exit 1. |
| T-27-52-02 | Tampering | `grantedAgentNames` over block-scalar content (row g5) | high | mitigate | An altered or invented name in the grant closure breaks the KIT-03 set equality the oracle promises (D-09). Closed by asserting the module's name set equals the loader's as sorted sets. |
| T-27-52-03 | Denial of Service | the four tracked `.planning/` documents already carrying a nested header | medium | mitigate | A blanket refusal would turn loader-accepted documents red. Mitigated by making the disposition an explicit gated decision with the false-red cost measured before and after, rather than discovered at CI. |
| T-27-52-04 | Tampering | committed `scripts/frontmatter.js` | medium | mitigate | `npm run freshness` is a required gate in every task's verification. |
| T-27-52-SC | Tampering | package-manager installs | low | accept | This plan runs no package-manager install and changes no dependency; no package-legitimacy checkpoint is required, and the absence is recorded rather than left silent. |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**'` reports 0 failed. Do NOT run `npm test`.
- `npm run freshness` exits 0.
- `node scripts/check-foundation-guards.js` exits 0 unplanted and exits 1 with either family planted
  on both distribution twins of a non-coordinator skill.
- `node scripts/adapters-freshness.js` and `node scripts/coordinator-resolution-precheck.js` exit 0.
- `/usr/bin/ruby -ryaml` is the independent loader column for every row.
</verification>

<success_criteria>
1. A `|` / `>` header is recognised at every position YAML allows one, through the one
   `BLOCK_INDICATOR` authority applied at more sites — not through a second grammar.
2. Rows G, G2, g1, g2, g3 and g4 grant where the loader grants; row g5's name set equals the loader's.
3. A plant of family G or G2 on both distribution twins of a non-coordinator skill takes the
   foundation gate from exit 0 to exit 1.
4. The D-52 corpus can express the family, proven by the same corpus reporting a non-empty
   silent-while-loader-grants set against a pre-fix mirror.
5. The false-red cost of the ratified option is a measured number, and the decision is recorded as
   D-57 with its rejected alternatives.
</success_criteria>

## Artifacts this plan produces

**Source symbols (new or changed)**
- `scripts/frontmatter.ts` — block-scalar-header recognition applied at the block-sequence item path
  and the continuation path; the accumulator field recording the header line's indentation (option A)
  or the named refusal (option B) or the literal-region marker (option C); a new ledger entry in the
  module header naming the family and its remedy.
- `scripts/frontmatter.js` — rebuilt committed twin.

**Test corpus symbols (new)**
- The nested-mapping-value block-scalar member of `AXIS_KEY_LINE`.
- The block-sequence-item block-scalar member of `AXIS_KEY_LINE`.
- Regression cases for rows G, G2, g1, g2, g3, g4 and g5, each naming the loader value it agrees with.
- The union case crossing a nested block-scalar header with wave 1's `AXIS_ESCAPE_IN_SCALAR`.

**Planning artifacts**
- `.planning/phases/27-spawn-correctness-kit-set-authority/27-CONTEXT.md` — a `Gap-closure round 10`
  heading carrying **D-57**, the ratified disposition, its rationale, its rejected alternatives and
  its reversibility rating.
- `.planning/phases/27-spawn-correctness-kit-set-authority/deferred-items.md` — a `From 27-52` section
  recording the closure, the measured false-red cost, the settled or restated scope question, and any
  new open item.

**No new files, no new CLI flags, no new tsconfig keys, no new dependencies.**

## Multi-source coverage audit

| Source | Item | Covered by |
|---|---|---|
| GOAL | the spawn-correctness half of the phase goal — the guards catch every live grant | Task 1 (gate-level plants for both families) |
| REQ | KIT-03 — the closure oracle's soundness claim over the same code path | Task 1, Task 2 |
| REQ | SPAWN-04 — no non-coordinator adapter carries a live grant | Task 1 |
| RESEARCH | "derive the set, assert the count" — here, the set of POSITIONS a predicate applies to | Task 1, Task 2 pass (a) |
| CONTEXT | D-54 — a property of structural position, decided in one place | Task 1 |
| CONTEXT | D-52, D-56 item 1 — the corpus must express what the completeness claim covers | Task 2 |
| CONTEXT | D-30 — ask WHICH SET the predicate enumerates | Checkpoint, Task 2 |
| CONTEXT | D-09 — a name is never silently dropped or altered in the closure | Task 1 (row g5) |
| CONTEXT | D-40 — a plant lands on both distribution twins | Task 1 |
| CONTEXT | D-19 — recorded measurement, never a grep-shaped heuristic | Task 1, Task 2 |
| CONTEXT | D-57 (new, ratified in this plan) — the nested block-scalar disposition | Checkpoint, Task 1 |

No item is MISSING.

## Specless probe fallback — no-silent-drop accounting

This plan closes the same two requirement IDs as `27-51` (KIT-03 and SPAWN-04), whose **7** probe rows
are authored once, in `27-51`'s `must_haves.truths`. Authoring them a second time here would
double-count them against the round-wide equality, so this plan carries **0** probe rows by design and
records that fact rather than leaving a reader to infer it.

**Equality for this plan: 0 probe-surfaced rows == 0 authored + 0 surfaced as flagged assumptions.**
**Round equality (unchanged): 29 == 26 explicit + 3 backstop + 0 unresolved**, distributed `27-51` 7,
`27-52` 0, `27-53` 7, `27-54` 15.

<output>
Create `.planning/phases/27-spawn-correctness-kit-set-authority/27-52-SUMMARY.md` when done, carrying
the ratified decision, every RED transcript, every GREEN transcript, both families' gate exit codes,
the measured false-red cost, the two adversarial passes and the corpus cell counts verbatim.
</output>

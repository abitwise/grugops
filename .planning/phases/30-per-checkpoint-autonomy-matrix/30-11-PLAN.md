---
phase: 30-per-checkpoint-autonomy-matrix
plan: 11
type: execute
wave: 8
depends_on: ["30-10"]
files_modified:
  - hooks/guard.ts
  - hooks/guard.js
  - hooks/guard.test.ts
  - hooks/admission-guard.ts
  - hooks/admission-guard.js
  - hooks/admission-guard.test.ts
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/floor-invariance.test.ts
  - docs/GUARANTEES.md
  - docs/audit/28-residual-sizing.md
  - docs/audit/30-redteam-surface-a.md
autonomous: false
requirements: [AUTO-03, AUTO-04]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-21: red-team surface A covers the two-key hook path — the guard, the admission guard, the fresh environment read and the self-set refusal — plus the point-of-effect test-integrity refusal, and it begins only after surface B is closed or fenced."
    - "D-23: every bypass is reproduced pre-fix on a mirror of the committed compiled hook artifact, never on the TypeScript source, because the host runs the artifact."
    - "D-24: any change to the frozen hook is unfrozen and re-frozen in the SAME commit, and the freeze verification is run after that commit rather than before it."
    - "AUTO-03: the settings-file grant vector is reproduced empirically at least once and its result recorded, because the render's wording about it currently rests on a composed inference rather than on an observation."
    - "D-22: at most four gap-closure rounds run on this surface; a finding after the fourth becomes a recorded backlog item or a follow-up phase behind a written fence."
  artifacts:
    - docs/audit/30-redteam-surface-a.md
  key_links:
    - "each bypass -> its RED-first test -> its structural fix -> its mirror reproduction on the committed artifact"
    - "the empirical settings-file result -> the render's residual wording -> the residual register row"
  prohibitions:
    - statement: "The self-set refusal must never be narrowed to one variable name while a second grant variable escapes it: one detector over the whole grant family, or the family is not protected."
      status: unresolved
      verification: null
    - statement: "A residual that a round proves real must never be softened in the published wording to make the guarantee read better — the render exists to prevent exactly that."
      status: unresolved
      verification: null
---

## Flagged Assumptions (edge-probe items carried forward unresolved)

| Requirement | Probe | Why it stays unresolved | Where it is exercised |
|---|---|---|---|
| AUTO-03 | unclassified — review manually | The two-key mechanism's edge is a provenance question — whether a value in the process environment can be attributed to a human — which is not a data-shape property and cannot be resolved by a boundary test | Task 1 and Task 2 below; the settings-file reproduction |
| AUTO-04 | unclassified — review manually | The point-of-effect refusal's edge is the tier of its input: the argument is supplied by the procedure, so the open question is what the caller can assert rather than what values the parameter accepts | Task 2 below; the emitter's header wording |

<objective>
Red-team surface A — the two-key hook path and the point-of-effect test-integrity refusal — under the
same closure standard as surface B, and settle empirically the one research assumption the published
guarantee wording depends on.

Purpose: this is the surface where a bypass would let an agent lower a safety floor by itself. It is
also the surface whose honest description rests on an inference the research explicitly flagged as
composed rather than observed. Both get closed here.

Output: a round log, an empirically established answer about the settings-file vector, and render
wording that matches what was actually observed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-CONTEXT.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-RESEARCH.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-01-SUMMARY.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-05-SUMMARY.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-08-SUMMARY.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-10-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Notes |
|---|---|---|
| `docs/audit/30-redteam-surface-a.md` | new round log | per-round findings, reproductions, fixes and the fence decision |
| the empirical settings-file result | recorded observation | replaces a composed inference in the residual register and the render |

<tasks>

<task type="auto">
  <name>Task 1: attack the two-key path on the committed hook artifacts</name>
  <files>hooks/guard.ts, hooks/guard.js, hooks/guard.test.ts, hooks/admission-guard.ts, hooks/admission-guard.js, hooks/admission-guard.test.ts, scripts/floor-invariance.test.ts, docs/audit/30-redteam-surface-a.md</files>
  <read_first>
    - hooks/guard.ts and hooks/admission-guard.ts as they now stand, in full
    - hooks/guard.test.ts (the spawn-the-committed-artifact discipline: target the artifact, never the source)
    - scripts/floor-invariance.test.ts lines 12-17, 87-95, 236-253 and 548 (the invariants, the two similarly-named artifacts in one file, the freeze constant and its two sites)
    - .planning/phases/30-per-checkpoint-autonomy-matrix/30-RESEARCH.md §F-2, §F-8, §Pitfall 1, §Pitfall 5, and §Red-Team Architecture
    - .planning/phases/25-governance-on-a-dial/ context if present (the ten rounds of shell-expansion bypasses closed by construction rather than by recognizing more spellings)
  </read_first>
  <action>
Attack the grant path. Every reproduction spawns the COMMITTED compiled artifact, never the TypeScript
source, because that is what a host runs and because a source-only reproduction proves nothing about
the shipped file.

Ask the four standing questions of the grant predicate. What set does it enumerate — which variable
names count as grants, and is that set derived from the roster or written down? What is its input
assembled from — the command string reaches the self-set detector after which transformations, and can
a grant be set through a spelling the detector never sees? At which positions is it asked — is the
detector consulted before every decision branch, or only before some? Does the harness assert its own
premise — does the spawn genuinely lack the variable when the test believes it does, and genuinely
carry it when the test believes it does?

Then attack specifically: shell expansion and indirection reaching a grant assignment; a grant set
through a wrapper process; a grant name that differs from the derived family by case or by separator; a
command that matches a checkpoint's pattern only after a transformation the guard does not normalize; a
config whose matrix is well-formed but whose disposition value differs from the one the decision reads;
and the ordering question — is the self-set refusal evaluated before or after the matrix lookup, and
does that ordering create a window.

Also attack the freeze rule itself: is the must-change-together rule scoped to the commit that changed
the hook, or to a range? A range-scoped freeze self-disarms the first time its companion legitimately
changes, and this phase changed the hook twice.

Every finding: RED first, mirror reproduction with both exit statuses recorded, structural fix, mutation
proof. Any change to the frozen hook is unfrozen and re-frozen in one commit, and its verification runs
after that commit.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' hooks/guard.test.ts hooks/admission-guard.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or a bypass in the round log with no corresponding named test in these files</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/floor-invariance.test.ts</automated>
    <fails_when>non-zero exit, or a failure naming the uncommitted-modification assertion (commit the guard first, then re-run), or a failure naming the blob comparison</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or the line "BUILD PARITY FAILED" in the output, or any path reported as stale</fails_when>
  </verify>
  <acceptance_criteria>
    - Every reproduction command in the round log targets a compiled hook artifact path, not a TypeScript path.
    - The grant-name set is shown to be derived from the roster rather than written down, or the finding is logged.
    - The four standing questions are answered in writing for the grant predicate, including the answers that found nothing.
    - The freeze rule's scope question is answered: commit-scoped or range-scoped, with the evidence.
    - Every finding carries its RED output, both mirror exit statuses, and a structural fix sentence.
  </acceptance_criteria>
  <done>The two-key hook path has been attacked on the artifact a host actually runs, with every finding reproduced and structurally fixed.</done>
</task>

<task type="auto">
  <name>Task 2: attack the point-of-effect refusal, and settle the settings-file vector empirically</name>
  <files>scripts/context-io.ts, scripts/context-io.test.ts, docs/GUARANTEES.md, docs/audit/28-residual-sizing.md, docs/audit/30-redteam-surface-a.md</files>
  <read_first>
    - scripts/context-io.ts — the emitter as changed in plan 30-05, its refuse-before-compose branch, its header tier statement, and the shared write chokepoint
    - docs/audit/28-residual-sizing.md (the settings-file row added in plan 30-09 and the pre-existing same-class row)
    - docs/GUARANTEES.md (the generated residual section)
    - .planning/phases/30-per-checkpoint-autonomy-matrix/30-RESEARCH.md §F-2 in full and §Assumptions Log entries A1 and A2 — A2 states the settings-file inference is strong but not directly documented and asks for an empirical reproduction before the render's wording is finalized
  </read_first>
  <action>
Attack the emitter's refusal. Can a green verdict be written without a clean integrity result — through
the command-line verb, through a wrapper, through a value that canonicalizes into the clean sentinel,
or through a path that composes the note before the refusal is reached? Assert that no partial file
survives any refusal, and that the refusal is reached before the first composition line on every code
path, not only on the one the tests exercise.

Then settle the settings-file question empirically, because the published wording depends on it. Run
the reproduction on the installed host: place the grant into a host settings file mid-session and
observe whether a hook subprocess spawned afterwards reads it. Record what actually happened — the host
version, the settings scope used, the observed result — rather than the inference.

If the vector reproduces, the render's wording and the residual row already say so and are confirmed;
record the confirmation with the observation attached. If it does NOT reproduce, do not quietly upgrade
the guarantee: record the observation, keep the residual with its scope narrowed to what was tested, and
say which configurations were not tested. A residual that a round proves real must not be softened to
make the guarantee read better, and a residual that a round fails to reproduce is not thereby closed.

Also test the recorded watch item: whether a host-side environment-scrubbing feature is present on the
installed version and whether it affects the grant family. Record its source tier honestly — it entered
the research as an unverified item and must not leave this plan cited as established unless it was
observed here.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/context-io.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or a failure naming a note file surviving a refused emission</fails_when>
    <automated>npm run generate:guarantees &amp;&amp; npm run freshness:guarantees &amp;&amp; git diff --exit-code -- docs/GUARANTEES.md</automated>
    <fails_when>non-zero exit, or the render reported as stale, or the render appearing in the diff output after its own regeneration</fails_when>
    <automated>npm run check:claim-anchors &amp;&amp; npm run check:banned-claims &amp;&amp; npm run check:public-docs &amp;&amp; npm run check:audit-register</automated>
    <fails_when>non-zero exit, or a finding naming the guarantees document</fails_when>
    <human-check>The empirical settings-file reproduction was run on the installed host and its outcome — reproduced or not, with host version and settings scope — is recorded in the round log with the observation attached.</human-check>
  </verify>
  <acceptance_criteria>
    - Every code path into the emitter is shown to reach the refusal before the first note-composition line.
    - No refused emission leaves a file behind, asserted by before-and-after directory comparison on every refusal path.
    - The round log records the settings-file reproduction attempt with host version, settings scope, and observed outcome.
    - The render's residual wording matches the observation: confirmed if it reproduced, scope-narrowed and explicitly untested-elsewhere if it did not.
    - The environment-scrubbing watch item's source tier is stated as observed or as unverified, matching what was actually done.
  </acceptance_criteria>
  <done>The point-of-effect refusal has no unguarded path, and the published residual wording rests on an observation rather than on an inference.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 3: CLOSURE — two independent reviews, self-reproduction, and the round-four fence</name>
  <read_first>
    - docs/audit/30-redteam-surface-a.md (the full round log)
    - docs/audit/30-redteam-surface-b.md (surface B's closure record, for comparison of standard)
    - .planning/phases/30-per-checkpoint-autonomy-matrix/30-CONTEXT.md D-22 and D-23
  </read_first>
  <decision>
Whether surface A is closed, and if not, whether the round cap has been reached.

Before this checkpoint is raised: two INDEPENDENT reviews at the strongest available model over the
two-key path and the point-of-effect refusal, each finding nothing new; and self-reproduction by the
fixing agent of every bypass closed here, against the fixed build.

Present to the user: the round number, every finding with its structural fix, the two review outcomes,
the self-reproduction results, the settings-file observation, and any finding that arrived after round
four.

D-22 caps this surface at four rounds. A finding after the fourth becomes a recorded backlog item or a
follow-up phase, behind a written fence — never a fifth round.

A green suite is not an argument for closure. This project has thirteen documented cases in the current
milestone where it was offered and was wrong.
  </decision>
  <options>
    <option id="closed">
      <name>Surface A is closed</name>
      <pros>Both reviews found nothing new, every bypass is self-reproduced against the fixed build, and the published residual rests on an observation</pros>
      <cons>Closure is a judgement on an open-ended search; the settings-file vector remains an accepted residual by design, not a closed one</cons>
    </option>
    <option id="another-round">
      <name>Run another round on this surface</name>
      <pros>A finding is outstanding and the cap has not been reached</pros>
      <cons>This is the phase's last gate; each round is expensive and the cap exists to bound it</cons>
    </option>
    <option id="fence">
      <name>Fence at the round cap and record the outstanding findings as backlog</name>
      <pros>Honors the written cap; the finding is recorded with its reproduction rather than lost</pros>
      <cons>Ships a known-open item on the safety-floor surface, which raises the bar on how specific the backlog entry must be</cons>
    </option>
  </options>
  <resume-signal>Reply with the option id. If fencing, confirm the proposed fence text and the backlog entries.</resume-signal>
  <verify>
    <human-check>The user confirms surface A is closed, or approves the round-four fence and the backlog entries it produces, before the phase moves to verification.</human-check>
  </verify>
  <acceptance_criteria>
    - Two independent review outcomes are recorded with the reviewing model named and the scope each covered.
    - Every bypass closed here has a recorded self-reproduction against the fixed build.
    - The settings-file observation is part of the closure record.
    - If the cap is reached, the fence text and each backlog entry are written into the round log and into the project's capture surface.
    - The closure argument does not rest on suite greenness.
  </acceptance_criteria>
  <done>Surface A is closed by the user's confirmation, or fenced at round four with its findings recorded as backlog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent shell command -> the committed hook artifact | the surface a host actually runs |
| host settings files -> hook subprocess environment | the vector whose provenance the hook cannot read |
| gate procedure -> the emitter's integrity argument | an agent-supplied value gating a published green verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-45 | Elevation of Privilege | the grant-name family and the self-set detector | high | mitigate | One detector derived from the roster; attacked for case, separator, indirection and wrapper spellings; every reproduction on the committed artifact |
| T-30-46 | Elevation of Privilege | host settings-file environment injection | high | accept | Reproduced empirically this plan; recorded in the residual register in accepted/irreducible vocabulary and stated in the render with the observation attached, never softened |
| T-30-47 | Repudiation | a green verdict written without a clean integrity result | high | mitigate | Every code path into the emitter shown to reach the refusal before composition; no file survives a refusal |
| T-30-48 | Tampering | a range-scoped rather than commit-scoped freeze | high | mitigate | The freeze scope question is answered with evidence; this phase changed the hook twice, which is the condition under which a range-scoped rule self-disarms |
| T-30-49 | Spoofing | a closure claim resting on suite greenness | high | mitigate | The checkpoint refuses greenness as an argument and requires two independent reviews plus self-reproduction |
| T-30-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this phase installs zero packages; no install task exists |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**'` green after the final hook commit.
- All nine check scripts and all seven freshness scripts green.
- `npm run build && npm run typecheck && npm run check:build-parity` green.
- The guard freeze verification is run AFTER each hook commit.
- Both round logs are complete and their closure records are consistent in standard.
</verification>

<success_criteria>
- The two-key path has been attacked on the artifact a host runs, with every finding reproduced and structurally fixed.
- The point-of-effect refusal has no unguarded path and leaves no partial artifact.
- The published residual rests on an observation.
- Closure rests on reviews and reproductions; the round cap is honored with a written fence.
</success_criteria>

<output>
Create `.planning/phases/30-per-checkpoint-autonomy-matrix/30-11-SUMMARY.md` when done. Record the
round count, every finding with its fix form, the two review outcomes, the settings-file observation,
and the fence decision.
</output>

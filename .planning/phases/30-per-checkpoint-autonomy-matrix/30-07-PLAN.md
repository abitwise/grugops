---
phase: 30-per-checkpoint-autonomy-matrix
plan: 07
type: execute
wave: 5
depends_on: ["30-02", "30-04", "30-06"]
files_modified:
  - scripts/generate-guarantees.ts
  - scripts/generate-guarantees.js
  - scripts/generate-guarantees.test.ts
  - scripts/guarantees-freshness.ts
  - scripts/guarantees-freshness.js
  - docs/GUARANTEES.md
  - scripts/check-banned-claims.ts
  - scripts/check-banned-claims.js
  - scripts/check-banned-claims.test.ts
  - scripts/check-public-docs-vocabulary.ts
  - scripts/check-public-docs-vocabulary.js
  - scripts/check-public-docs-vocabulary.test.ts
  - package.json
autonomous: true
requirements: [AUTO-05, AUTO-02]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-17: a generated guarantees document is produced by a script with a fixed literal output path, a named regeneration command in its header, and a fail-closed exit; it joins the live checkpoint matrix with the safety-kind registry rows through their floor dependencies."
    - "D-17: the join refuses BOTH an empty result AND a short one — its length is asserted against a safety-row count produced by a second, independent pass over the registry, so rendering three of six rows is red rather than silently partial."
    - "D-17: a hand edit to the generated document is caught by a byte-equality freshness gate that regenerates into a mirror and compares, and that never reports fresh when the mirrored regeneration cannot run cleanly."
    - "AUTO-05: the generated document is a member of BOTH language gates' scan sets, and its membership is derived from the generator's own declared output path rather than from a second hand-typed path literal; each gate's corpus size is asserted so a member cannot be dropped silently."
    - "AUTO-05: the document states the floor-grant mechanism's tier honestly — un-forgeable from inside a tool call, reachable by an agent that can write the host's settings files — rather than asserting an unqualified impossibility."
  artifacts:
    - scripts/generate-guarantees.ts
    - scripts/guarantees-freshness.ts
    - docs/GUARANTEES.md
  key_links:
    - "the live matrix + registry safety rows -> the join -> docs/GUARANTEES.md"
    - "the generator's exported output path -> both language gates' scan sets"
    - "the generated document -> the freshness gate's mirror regeneration"
  prohibitions:
    - statement: "The guarantees document must never describe the floor-lowering grant as something an agent cannot set, without naming the vector by which an agent could — an artifact whose whole purpose is to stop overstated claims must not open with one."
      status: unresolved
      verification: null
    - statement: "A lowered floor must never be discoverable only by reading configuration: the render and the run banner exist so that a human who reads neither the config nor the code still learns which guarantee stopped holding."
      status: unresolved
      verification: null
---

<objective>
Build the guarantees render and its freshness gate, and wire the new document into both language gates
in the same plan that creates it.

Purpose: a brand-new public document whose entire purpose is to carry safety claims, born outside both
claim gates, is the exact shape of the defect this milestone already caught once. The wiring is not a
follow-up; the deferral IS the defect.

Output: a fixed-path generator modelled on the existing safety-surface generator, a byte-equality
freshness gate modelled on the existing catalog gate, and two scan sets that now include the new
document by derivation rather than by anyone remembering.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-CONTEXT.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-RESEARCH.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-PATTERNS.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-02-SUMMARY.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-06-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Notes |
|---|---|---|
| `scripts/generate-guarantees.ts` | new generator | fixed output path, named regeneration command, fail-closed |
| `OUT` | exported const | the fixed literal repo-relative output path — the single declaration of where the render lives |
| `REGEN_COMMAND` | exported const | named in the header and in the freshness refusal |
| `renderGuarantees()` | exported fn | the pure join |
| `guaranteesJoin()` | exported fn | the joined rows, independently countable |
| `docs/GUARANTEES.md` | new generated document | the render |
| `scripts/guarantees-freshness.ts` | new gate | the tree's next byte-equality freshness gate |
| `generate:guarantees` | new npm script | regeneration |
| `freshness:guarantees` | new npm script | drift gate |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the generator and its count-asserted join</name>
  <files>scripts/generate-guarantees.ts, scripts/generate-guarantees.test.ts, docs/GUARANTEES.md, package.json</files>
  <read_first>
    - scripts/generate-safety-surface.ts in full (188 lines) — the fixed output constant and its path-traversal rationale at 41-57, the registry safety-kind filter and sorted-by-path emission at 72-100, and the entry guard plus fail-closed exit at 170-188
    - scripts/audit-model.ts (`readRegistry`, `readRegister`, the registry and register path constants, and the row schema)
    - scripts/checkpoints.ts (the roster, the defaults table, and the live-matrix reader entry point)
    - docs/audit/28-claim-registry.md (the six safety-kind rows and the floor to claims index)
    - docs/audit/28-residual-sizing.md (the accepted / irreducible row shape this document's residual section must match)
    - .planning/phases/30-per-checkpoint-autonomy-matrix/30-RESEARCH.md §F-5, §F-2 recommendation 3, §Pitfall 6
  </read_first>
  <behavior>
    - Rendering against the live tree produces a join whose row count equals the independently counted number of safety-kind registry rows.
    - A registry fixture with one safety row fewer makes the render throw a named error rather than emitting a short document.
    - A registry fixture with zero safety rows makes the render throw a named error rather than emitting an empty document.
    - Two rows depending on the same checkpoint are both emitted; neither suppresses the other.
    - Rows are emitted in ascending claim-id order, and two runs over the same inputs are byte-identical.
    - A matrix where every checkpoint is at its default renders the all-default statement; a matrix with a lowering renders that checkpoint, its value and its authorizing name.
  </behavior>
  <action>
Clone the existing safety-surface generator's shape rather than inventing one. Declare `OUT` as a fixed
literal repo-relative path — never derived from a command-line argument, an environment variable or
file content; the analog's header states this as its path-traversal mitigation and that reasoning
carries over unchanged. Declare `REGEN_COMMAND` and name it both in the generated header and in the
freshness gate's refusal message. Copy the entry guard that compares the module URL against the
converted process argument path; a hand-built URL string fails on Windows and writes nothing while
exiting zero, which is a fabricated success.

The join filters the registry to safety-kind rows and resolves each row's floor dependencies against
the live matrix. Where the analog is insufficient: it refuses only an EMPTY union. Add the second,
independent assertion this phase needs — count the safety-kind rows in a separate pass over the
registry and assert the join length equals it. A vacuity floor catches an empty denominator and never
a silently short one, and here the missing rows would be exactly the lowered ones.

Sort by claim id and let that be the only ordering rule, so the output is a pure function of its two
sources and two runs are byte-identical.

Write the residual section honestly. The floor grant is un-forgeable from inside a tool call and is
reachable by an agent that can write the host's settings files; the document names both, and names the
narrowing measure without calling it a closure. The grant is session-scoped under a name, not
per-action consent, and the document says so. Match the existing residual register's vocabulary —
there is already an accepted, irreducible row of the same class to align with.

Add the regeneration npm script alongside the existing generator scripts, in the same shape.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/generate-guarantees.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or no test name mentioning a short registry fixture</fails_when>
    <automated>npm run generate:guarantees &amp;&amp; npm run generate:guarantees &amp;&amp; git diff --exit-code -- docs/GUARANTEES.md</automated>
    <fails_when>non-zero exit, or `docs/GUARANTEES.md` appearing in the diff output after two consecutive regenerations</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or the line "BUILD PARITY FAILED" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `OUT` is a string literal in the module and is not assembled from any runtime input.
    - The join length is asserted against a count produced by a pass that does not share a loop with the join.
    - A short-registry fixture and an empty-registry fixture each produce a distinct named error.
    - Two consecutive regenerations produce byte-identical output.
    - The residual section names the settings-file vector, the narrowing measure, and the session scope of the grant.
  </acceptance_criteria>
  <done>The render is a pure, reproducible function of the matrix and the registry, refuses a short join as loudly as an empty one, and states its own residual rather than overstating its guarantee.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the byte-equality freshness gate</name>
  <files>scripts/guarantees-freshness.ts, scripts/generate-guarantees.test.ts, package.json</files>
  <read_first>
    - scripts/catalog-freshness.ts in full — the mirror directory creation and byte comparison at 23-50, and the header's two stated rules (the output path stays a fixed literal and the gate mirrors instead of overriding it; a mirrored regeneration that cannot run cleanly never reports fresh)
    - scripts/freshness.ts (the committed-side read via git, and why it is ordering-independent)
    - scripts/generate-guarantees.ts as written by Task 1
    - package.json (the six existing freshness scripts, for shape)
  </read_first>
  <behavior>
    - With the committed document matching a fresh regeneration, the gate exits zero and reports fresh.
    - With one character changed in the committed document, the gate exits non-zero and names the regeneration command.
    - With the generator made to fail, the gate exits non-zero and does not report fresh.
    - With the mirror directory unwritable, the gate exits non-zero and does not report fresh.
  </behavior>
  <action>
Clone the catalog freshness gate's shape. Regenerate into a temporary mirror and byte-compare against
the committed document; never write over the real output path from inside the gate. Carry over both
rules the analog's header states, and carry over the refusal message form that names the regeneration
command so a developer reading a red gate knows the fix.

The analog's import-closure copy list is hand-maintained — do not copy that. Either derive the copy set
or pin its count with an assertion, so the mirror cannot silently lose an input and then compare
against a regeneration that never had the same sources.

Assert the gate's own premise: prove that the mirror genuinely differs from the committed document in
the planted-drift case, rather than assuming a diff occurred. This is the harness-premise question that
produced false results repeatedly in earlier phases of this project.

Register the freshness npm script alongside the existing six.
  </action>
  <verify>
    <automated>npm run freshness:guarantees</automated>
    <fails_when>non-zero exit, or the output omitting a fresh verdict for an unmodified tree</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/generate-guarantees.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or no test name mentioning planted drift</fails_when>
    <automated>npm run freshness &amp;&amp; npm run freshness:catalog &amp;&amp; npm run freshness:context &amp;&amp; npm run freshness:adapters &amp;&amp; npm run freshness:skill-twins &amp;&amp; npm run freshness:guarantees</automated>
    <fails_when>non-zero exit, or any path reported as stale</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate regenerates into a temporary mirror and never writes the real output path.
    - A planted one-character drift makes the gate exit non-zero, and the test asserts the mirror and the committed file genuinely differ in that run.
    - A generator forced to fail makes the gate exit non-zero without a fresh verdict.
    - The mirror's copy set is either derived or count-asserted; no hand-maintained path list is copied from the analog without an assertion.
  </acceptance_criteria>
  <done>A hand edit to the generated document is red, a broken regeneration is red, and the gate's own drift premise is asserted rather than assumed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: wire the new document into both language gates by derivation</name>
  <files>scripts/check-banned-claims.ts, scripts/check-banned-claims.test.ts, scripts/check-public-docs-vocabulary.ts, scripts/check-public-docs-vocabulary.test.ts</files>
  <read_first>
    - scripts/check-banned-claims.ts lines 865-900 (the segment-class exclusions enforced at the walk, including the documents segment) and the header explaining why loosening the scan for one file would delete the check for every other
    - scripts/check-public-docs-vocabulary.ts lines 150-360 — the exemption array and its recorded reasoning, the derivation-refusal collection, the three self-deriving corpus parts, and the recorded round-6 finding in which one gate consumed another gate's post-exemption set and left a public document outside a scan with live disproven claims in it
    - scripts/generate-guarantees.ts (its exported output path constant — the single declaration to import)
    - .planning/phases/30-per-checkpoint-autonomy-matrix/30-RESEARCH.md §F-6 in full
  </read_first>
  <behavior>
    - The generated document is a member of the banned-claims scan set, proven by planting a banned literal in it and asserting the gate names it at file, line and column.
    - The generated document is a member of the public-docs vocabulary scan set, proven the same way with a retired-vocabulary literal.
    - Each gate's corpus size is asserted against an independently computed expected size, so a member cannot be dropped silently.
    - Removing the generated document from the tree does not make either gate die with an unhandled error; it reports.
  </behavior>
  <action>
The decision records the render's home under the documents directory, and that directory is excluded
from the banned-claims walk as a segment class at any depth. Research recommends the repository root
instead, where membership would follow by construction. Honor the recorded path and close the hole
explicitly, per the second of the two dispositions research offers — but close it by DERIVATION, not
by a second path literal.

Import the generator's exported output path constant and add the document as a named corpus member in
both gates, so the scan member and the generator's declaration are the same string and a future rename
moves both together. In the vocabulary gate, add it as a fourth named corpus part in the existing
self-deriving parts structure and derive it against the disk the way the single-file part already is,
so a deleted file makes the gate report rather than die with an unhandled error and a stack trace.

Assert each gate's corpus size against a denominator computed outside the loop that builds it. The
recorded round-6 finding in this tree is exactly a corpus that lost a member silently; a membership
addition without a size assertion repeats it.

Prove membership by planting, not by inspecting the corpus array: put a banned literal into the
generated document in a fixture tree and assert the gate names it at file, line and column, which is
the proof shape the banned-claims gate's own test already uses for another document.

If, on reading the two gates, adding the member requires carving an exception INTO the segment-class
exclusion, stop and record that in the SUMMARY as a finding rather than weakening the exclusion — an
exception inside a fail-closed exclusion is a different act from adding a member, and it is the one
outcome this task must not ship silently.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/check-banned-claims.test.ts scripts/check-public-docs-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or no test name mentioning a planted literal in the generated guarantees document</fails_when>
    <automated>npm run check:banned-claims &amp;&amp; npm run check:public-docs</automated>
    <fails_when>non-zero exit, or a stack frame containing "node:internal" in the output</fails_when>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity &amp;&amp; npm run freshness</automated>
    <fails_when>non-zero exit, or the line "BUILD PARITY FAILED" in the output, or any path reported as stale</fails_when>
  </verify>
  <acceptance_criteria>
    - Both gates obtain the document's path by importing the generator's exported constant; neither file contains a second literal of that path.
    - A planted banned literal in the generated document is reported by the banned-claims gate at file, line and column.
    - A planted retired-vocabulary literal is reported by the vocabulary gate.
    - Each gate asserts its corpus size against an independently computed expectation.
    - Deleting the generated document makes each gate report a derivation refusal rather than throwing an unhandled error.
    - If a segment-class exception was required, the SUMMARY records it as a finding with its reasoning.
  </acceptance_criteria>
  <done>The new public safety document is inside both language gates, proven by planted literals, with each corpus size asserted so a member cannot be dropped silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry + live matrix -> the generator -> a public document | two editable sources cross into published safety claims |
| the generated document -> the language gates | a public claim surface that must not be outside the checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-26 | Repudiation | a silently short join omitting the lowered rows | high | mitigate | Join length asserted against an independently counted safety-row denominator; short and empty fixtures each produce a distinct named error |
| T-30-27 | Tampering | the generator's output path | medium | mitigate | Fixed literal repo-relative path, never derived from argument, environment or content; the existing generator's stated path-traversal mitigation carried over |
| T-30-28 | Information Disclosure | a public safety document outside both claim gates | high | mitigate | Membership derived from the generator's exported path constant, proven by planted literals, with corpus sizes asserted |
| T-30-29 | Repudiation | an overstated grant claim in the render | high | mitigate | The residual section names the settings-file vector, the narrowing measure and the session scope; the claim made is the one that holds |
| T-30-30 | Tampering | a hand edit to the generated document | medium | mitigate | Byte-equality freshness gate that mirrors, compares, and never reports fresh when regeneration cannot run cleanly; the drift premise itself asserted |
| T-30-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this phase installs zero packages; no install task exists |
</threat_model>

<verification>
- `npx vitest run --exclude '**/scripts/e2e/**'` green.
- All seven freshness scripts green, including the new one.
- `npm run check:banned-claims && npm run check:public-docs && npm run check:claim-anchors && npm run check:audit-register` green.
- `npm run build && npm run typecheck && npm run check:build-parity` green.
</verification>

<success_criteria>
- The render is reproducible, count-asserted and fail-closed on a short join.
- A hand edit to it is red.
- It is inside both language gates by derivation, proven by planted literals.
- Its residual is stated rather than claimed away.
</success_criteria>

<output>
Create `.planning/phases/30-per-checkpoint-autonomy-matrix/30-07-SUMMARY.md` when done. Record the
safety-row denominator, the corpus sizes asserted in each gate, and whether a segment-class exception
was required.
</output>

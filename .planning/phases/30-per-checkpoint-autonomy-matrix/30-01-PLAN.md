---
phase: 30-per-checkpoint-autonomy-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/checkpoints.ts
  - scripts/checkpoints.js
  - scripts/checkpoints.test.ts
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/audit-model.ts
  - scripts/audit-model.js
  - agent-factory/config/factory.config.json
  - agent-factory/seed/.grugops/factory.config.json
  - hooks/guard.ts
  - hooks/guard.js
  - hooks/guard.test.ts
  - scripts/floor-invariance.test.ts
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-07]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-07 D-09: with `checkpoints.protected_branch_merge` set to `off` or `notify` in config and no `GRUGOPS_FLOOR_PROTECTED_BRANCH_MERGE` in the hook's process env, a `git push origin main` still denies — the config declaration alone changes nothing."
    - "D-10: that denial names both the checkpoint id and the exact missing env var name, so a human reading the transcript knows which key is absent."
    - "D-09: a command that inline-sets any `GRUGOPS_FLOOR_*` variable in the same tool call is refused, whether or not the variable is already present in the environment."
    - "D-08 D-20 AUTO-07: with no `checkpoints` object in config, the guard's decision for every input is byte-identical to the pre-phase guard, and its output differs from the pre-phase guard by exactly one added banner line."
    - "D-02 AUTO-01 (edge: adjacency): two checkpoint tags carrying the same id — in different files, or on adjacent bullets in one section — collapse to ONE roster member whose recorded site count is 2; equal ids merge, they never collide and never produce two members."
    - "D-01 AUTO-01 (edge: empty): a corpus that yields zero tagged bullets is a refusal, not an empty roster; a corpus that yields exactly one tagged bullet is a valid roster of one; a source file with no locatable section is a refusal."
    - "D-01 AUTO-01 (edge: ordering): the roster's iteration order is the declaration order of `CHECKPOINT_DEFAULTS` and nothing else declares it; the derived-vs-roster comparison is set-based and sorted, so two ids that compare equal by name can never change the verdict."
    - statement: "D-24: after the guard change is committed, `git hash-object hooks/guard.ts` equals the updated `FROZEN_GUARD_BLOB` AND `git diff --quiet hooks/guard.ts` exits 0 — a hash-only or code-only change is red."
      verification: backstop
  artifacts:
    - scripts/checkpoints.ts
    - scripts/checkpoints.test.ts
    - hooks/guard.ts
    - agent-factory/config/factory.config.json
    - agent-factory/seed/.grugops/factory.config.json
  key_links:
    - "config `checkpoints` object -> readGovernanceConfigResult -> hooks/guard.ts matrix lookup -> deny/allow decision"
    - "hooks/guard.ts -> GRUGOPS_FLOOR_PROTECTED_BRANCH_MERGE (fresh per-invocation process.env read)"
    - "scripts/checkpoints.ts CHECKPOINT_DEFAULTS -> Checkpoint union -> tsc compile error on a defaultless member"
  prohibitions:
    - statement: "The matrix must never make a safety floor lowerable by an agent acting alone: a config edit with no corresponding human-set env var must produce exactly the un-lowered behavior."
      status: unresolved
      verification: null
    - statement: "A floor grant must not be presented as narrower than it is: it authorizes for the life of the session under that name, and any prose about it must say so rather than implying per-action consent."
      status: unresolved
      verification: null
---

<objective>
Prove the whole Phase 30 architecture end-to-end on ONE checkpoint — `protected_branch_merge` — before
any expansion. One tagless floor id, one config cell, one reader field, one hook lookup, one key-two
env var, one banner line, verified by spawning the committed `hooks/guard.js`.

Purpose: this phase rewires a byte-frozen safety hook, a config reader, a closed-set module and a
render. If the wiring is wrong, it is cheaper to learn that after one commit than after ten. The
tracer is production code, not a prototype: it ships with the same denial quality, the same
fail-closed posture and the same tests as every later task.

Output: `scripts/checkpoints.ts` (roster + defaults + fail-closed canonicalizer), the `checkpoints`
config key on both JSON twins, the `checkpoints` field on the surviving discriminated reader, and a
`hooks/guard.ts` that consults the matrix, demands key two for a lowering, refuses self-set of the
floor-var family, and prints the run banner.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-CONTEXT.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-RESEARCH.md
@.planning/phases/30-per-checkpoint-autonomy-matrix/30-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Notes |
|---|---|---|
| `scripts/checkpoints.ts` | new module | derivation + roster home |
| `Checkpoint` | exported type | closed string union |
| `Disposition` | exported type | `"block" \| "notify" \| "off"` |
| `CHECKPOINT_DEFAULTS` | exported const | `as const satisfies Record<Checkpoint, Disposition>` — the ONLY declaration of the roster |
| `CHECKPOINTS` | exported const | `Object.keys(CHECKPOINT_DEFAULTS)` — derived, never a second literal |
| `FLOOR_CHECKPOINTS` | exported const | floor-tier subset, derived from `SAFETY_FLOORS` ids |
| `canonicalizeDisposition(raw: unknown)` | exported fn | fail-closed to `"block"` |
| `floorEnvVarName(id: Checkpoint)` | exported fn | returns `GRUGOPS_FLOOR_<UPPER_ID>` |
| `checkpoints` | config key | flat object on `factory.config.json` + seed twin |
| `GRUGOPS_FLOOR_PROTECTED_BRANCH_MERGE` | env var | key two for this floor |
| `GRUGOPS_FLOOR_<ID>` | env var family | one per floor-tier checkpoint |
| `checkpoints` | field on `GovernanceConfig` | read by the discriminated reader |
| `PRE_PHASE_GUARD_JS_BLOB` | test const | pinned pre-phase `hooks/guard.js` blob, materialized with `git cat-file blob` for the AUTO-07 differential |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a lowered protected_branch_merge needs two keys" — one path only</name>
  <files>scripts/checkpoints.ts, scripts/context-io.ts, agent-factory/config/factory.config.json, agent-factory/seed/.grugops/factory.config.json, scripts/audit-model.ts, hooks/guard.ts, hooks/guard.js, scripts/floor-invariance.test.ts</files>
  <read_first>
    - hooks/guard.ts (all 140 lines — `APPROVAL`, the `DEPLOY` set incl. the git-push arm at lines 70-77, the residual note at lines 79-83, `SELF_APPROVE` at line 88, `deny()` at 90-101, the stdin parse at 103-115, the two decision branches at 117-137)
    - hooks/admission-guard.ts (lines 21-33 for the why-a-separate-process argument; 74-85 `deny()`; 126-136 the reader call inside a try/catch that also denies; 200-227 the per-name env grant and its denial wording)
    - scripts/context-io.ts (lines 1231-1281 `readGovernanceConfig`; 1286-1311 the four degenerate-shape branches; 1322-1370 `readGovernanceConfigResult`, `GovernanceConfigSource`, `GovernanceConfigResult`, the candidate-path order, `canonicalizeHumanAdmission` at 1266-1273)
    - scripts/audit-model.ts (lines 195-236 — `SAFETY_FLOORS`, each member's `id` / `configPath` / `why`, and `safetyFloorLiveValue`)
    - scripts/check-diff-disposition.ts (lines 480-503 `FROZEN_SECTION_ANCHORS`; 562-572 `locateSection`; 575-590 the `FrozenSourceName` union + `FROZEN_SOURCES` Record annotation and its "Object.keys() over this is the source count" comment)
    - scripts/floor-invariance.test.ts (lines 12-17 invariants 1-4; 50-95 the timeout constant, `GUARD_JS`, `FROZEN_GUARD_BLOB`; 114-124 the degenerate value sweep; 236-253 the two guard-freeze assertions; 548 the second `FROZEN_GUARD_BLOB` site)
    - hooks/guard.test.ts (the spawn-the-committed-.js harness shape)
    - agent-factory/config/factory.config.json (the `quality` / `context` object keys — the shape `checkpoints` copies)
    - .planning/phases/30-per-checkpoint-autonomy-matrix/30-RESEARCH.md sections F-2, F-8, Pitfall 4, Pitfall 5
  </read_first>
  <action>
Create `scripts/checkpoints.ts` and wire ONE checkpoint through every layer. Do not tag any markdown,
do not touch the workflow corpus, do not add a second checkpoint. Layers, in order:

(1) `scripts/checkpoints.ts`. Declare `export type Disposition = "block" | "notify" | "off";` and
`export type Checkpoint = "protected_branch_merge" | "production_requires_human_confirmation";` —
exactly these two for now; plan 30-04 widens the union from the derived set. Declare
`export const CHECKPOINT_DEFAULTS = { protected_branch_merge: "block", production_requires_human_confirmation: "block" } as const satisfies Record<Checkpoint, Disposition>;`
and carry the `FROZEN_SOURCES` comment idiom above it: Object.keys() over this table is the roster
count, and nothing else declares it. Derive `export const CHECKPOINTS = Object.keys(CHECKPOINT_DEFAULTS) as readonly Checkpoint[];`
— never a second literal array (D-01). Add `export function canonicalizeDisposition(raw: unknown): Disposition`
that returns `"block"` for anything that is not exactly the string `"block"`, `"notify"` or `"off"`,
mirroring `canonicalizeHumanAdmission`'s posture: a present non-string, a wrong-case spelling, a
number, `null` and an array all reach `"block"` by rule, never by coercion (D-08, Pitfall 4). Add
`export function floorEnvVarName(id: Checkpoint): string` returning `"GRUGOPS_FLOOR_" + id.toUpperCase()`.
Add `export const FLOOR_CHECKPOINTS` derived by filtering `CHECKPOINTS` against the `id` values of
`SAFETY_FLOORS` imported from `./audit-model.js` — imported, never restated (D-04). Refuse an empty
`FLOOR_CHECKPOINTS` by throwing a named error, and assert its length against a count computed from
`SAFETY_FLOORS` outside the filtering loop (Pitfall 6).

(2) `scripts/audit-model.ts`. Change ONLY the `protected_branch_merge` member: set
`configPath: "checkpoints.protected_branch_merge"` and rewrite its `why` text, which currently
asserts this floor has no config key and that naming one would imply there is — that sentence
becomes false the moment the matrix gives it a cell (PATTERNS gotcha, `scripts/audit-model.ts`
around lines 220-223). The replacement `why` states the two-key rule: it now has a config cell, and
the cell alone cannot lower it. Leave the other three members, including `autonomy`, untouched —
plan 30-02 owns the floor-set remap.

(3) Config. Add a `checkpoints` object to `agent-factory/config/factory.config.json` next to the
existing `quality` / `context` objects, containing only the two roster keys at their default value
`block`. Copy the identical bytes into `agent-factory/seed/.grugops/factory.config.json` so the twin
stays byte-identical. Leave `autonomy` in place — plan 30-06 retires it. Leave
`agent-factory/config/factory.config.md` alone — plan 30-06 owns the markdown twin, and its
consistency oracles run there.

(4) `scripts/context-io.ts`. Extend the `GovernanceConfig` shape with
`checkpoints: Readonly<Record<Checkpoint, Disposition>>` and populate it inside
`readGovernanceConfigResult` ONLY. Do not touch `readGovernanceConfig`; plan 30-03 deletes it.
Reproduce all four degenerate-shape branches the existing reader already distinguishes for `context`
(non-object whole file / present-but-non-object `checkpoints` / absent `checkpoints` / present
non-string value) and route every one of them through `canonicalizeDisposition`, except the two
absent branches, which reach `CHECKPOINT_DEFAULTS` — the roster default, never `off` (AUTO-07). An
unknown key inside the object is dropped from the effective matrix and recorded on the result as a
refusal string (D-08); it never widens the roster.

(5) `hooks/guard.ts`. Split the existing `DEPLOY` array into two named groups without changing a
single regex: `PROTECTED_BRANCH_PATTERNS` = the three `git push` arms currently at lines 74-76, and
`PRODUCTION_DEPLOY_PATTERNS` = every other member. Keep `const APPROVAL = "GRUGOPS_PROD_DEPLOY_APPROVED"`
and its meaning unchanged: it is the ACTION approval and it still permits a matched command at
disposition `block`, which is what makes zero-config behavior identical to today (AUTO-07). Add the
separate FLOOR-LOWERING key family: for a checkpoint whose configured disposition is `notify` or
`off`, the guard reads `process.env[floorEnvVarName(id)]` fresh on every invocation and requires a
non-empty string; if it is absent or empty the guard behaves exactly as `block` and denies with a
message naming the checkpoint id and the missing variable name (D-10). Generalize `SELF_APPROVE`
from one name to the family: build one regex over `APPROVAL` plus every `floorEnvVarName(c)` for
`c` in `FLOOR_CHECKPOINTS`, keeping the existing `(^|[\s;&|(])(export\s+|env\s+)?NAME\s*=` shape —
one detector, never a second (D-09). Read the matrix by calling the discriminated reader inside a
try/catch that denies on throw, in the same shape `admission-guard.ts` uses at its reader call; a
`source` of `unreadable` is treated as `block` (D-12 posture, applied here). Print the banner as the
first line of every denial reason: a single line naming each checkpoint whose effective disposition
is not its default, or the fixed literal `all checkpoints at default` when none are (D-19, D-20).
Rewrite the residual note near line 82 that names the `autonomy=pr` posture as the backstop — that
sentence stops being true in this phase; the replacement names the per-checkpoint matrix and the
two-key rule instead. Keep the exit-0-plus-JSON-deny mechanism and every existing regex byte-exact.

(6) Freeze. Run `npm run build`, update `FROZEN_GUARD_BLOB` in `scripts/floor-invariance.test.ts`
(both sites, near lines 95 and 548) to the new `git hash-object hooks/guard.ts` value, and commit
`hooks/guard.ts`, `hooks/guard.js` and the test constant TOGETHER in one commit (D-24). Expect the
suite to be red between the edit and the commit: `git diff --quiet hooks/guard.ts` throws until the
file is committed, which is correct, not broken (RESEARCH F-8, Pitfall 5). Run the verification
below AFTER the commit.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run check:build-parity</automated>
    <fails_when>non-zero exit, or the line "BUILD PARITY FAILED" in the output, or a `tsc` diagnostic naming scripts/checkpoints.ts</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' hooks/guard.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any test naming a floor denial reported as skipped</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/floor-invariance.test.ts</automated>
    <fails_when>non-zero exit, or a failure naming `git diff --quiet` (which means the guard change is not committed yet — commit first, then re-run), or a failure naming the blob comparison</fails_when>
    <automated>node -e "const c=require('./agent-factory/config/factory.config.json'),s=require('./agent-factory/seed/.grugops/factory.config.json');if(JSON.stringify(c)!==JSON.stringify(s))throw new Error('config twins diverged');if(typeof c.checkpoints!=='object'||c.checkpoints===null)throw new Error('checkpoints key missing');if(c.checkpoints.protected_branch_merge!=='block')throw new Error('default is not block')"</automated>
    <fails_when>non-zero exit, or stderr containing "config twins diverged", "checkpoints key missing", or "default is not block"</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/checkpoints.ts` exports `Checkpoint`, `Disposition`, `CHECKPOINT_DEFAULTS`, `CHECKPOINTS`, `FLOOR_CHECKPOINTS`, `canonicalizeDisposition`, `floorEnvVarName`.
    - `CHECKPOINTS` is produced by `Object.keys(CHECKPOINT_DEFAULTS)`; no second array literal of checkpoint ids exists in the file.
    - `CHECKPOINT_DEFAULTS` carries `as const satisfies Record<Checkpoint, Disposition>`; adding a member to the `Checkpoint` union without a table entry produces a `tsc` diagnostic.
    - `canonicalizeDisposition` returns `"block"` for each of `"OFF"`, `""`, `"bogus"`, `"true"`, `"1"`, `0`, `null`, `[]`, `{}` and `undefined`.
    - `hooks/guard.ts` contains exactly one self-set detector, built over `APPROVAL` plus the derived floor-var names; there is no second regex constructed from a hard-typed variable name.
    - Spawning the committed `hooks/guard.js` with a `git push origin main` payload and a config whose `checkpoints.protected_branch_merge` is `off`, with no `GRUGOPS_FLOOR_PROTECTED_BRANCH_MERGE` in env, produces `permissionDecision: "deny"` and a reason string containing both `protected_branch_merge` and `GRUGOPS_FLOOR_PROTECTED_BRANCH_MERGE`.
    - The same spawn with `GRUGOPS_FLOOR_PROTECTED_BRANCH_MERGE` set to a non-empty name exits 0 with no deny payload.
    - A payload whose command inline-exports any `GRUGOPS_FLOOR_*` name denies, even when that variable is already present in the spawn env.
    - After commit, `git hash-object hooks/guard.ts` equals `FROZEN_GUARD_BLOB` at both of its declaration sites, and `git diff --quiet hooks/guard.ts` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">The tracer adds a config key and a hook branch; both can be removed without rewriting any downstream registry or index. The one-way act (retiring `autonomy`) is deliberately held out of this plan.</reversibility>
  <done>A `git push origin main` under a config that declares `protected_branch_merge: off` is still denied, by name, with the missing env var named, and the same command under a human-set floor var is allowed — proven by spawning the committed `hooks/guard.js`, with the guard re-frozen in the same commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canonicalizer and matrix-reader unit floor</name>
  <files>scripts/checkpoints.test.ts, scripts/context-io.ts</files>
  <read_first>
    - scripts/checkpoints.ts (as written by Task 1)
    - scripts/floor-invariance.test.ts lines 114-124 (`HUMAN_ADMISSION_VALUES` — the degenerate sweep to reuse)
    - scripts/context-io.test.ts lines 848-905 (the existing governance-reader tests, for harness shape and fixture-root helpers)
    - scripts/context-io.ts (the four degenerate-shape branches as extended by Task 1)
  </read_first>
  <behavior>
    - Every member of the degenerate value sweep, plus a present non-string, `null`, `[]` and `{}`, canonicalizes to `block`.
    - `"block"`, `"notify"` and `"off"` canonicalize to themselves and to nothing else.
    - A config file with no `checkpoints` object yields exactly `CHECKPOINT_DEFAULTS`, compared key-by-key.
    - A config file with a `checkpoints` object that omits one roster key yields that key's roster default.
    - A config file whose `checkpoints` value is a string, a number or an array yields `CHECKPOINT_DEFAULTS`, never `off`.
    - A `checkpoints` object carrying an id outside `CHECKPOINTS` does not add a roster member; the effective matrix key set equals `CHECKPOINTS` exactly, compared as sorted arrays.
    - `floorEnvVarName("protected_branch_merge")` is `GRUGOPS_FLOOR_PROTECTED_BRANCH_MERGE`.
  </behavior>
  <action>
Write `scripts/checkpoints.test.ts` RED-first: author each assertion above and confirm it fails
against a deliberately weakened `canonicalizeDisposition` (one that falls through to `"off"`), then
restore the correct implementation and confirm green. Record the mutation and its red result in the
plan SUMMARY — a test that stays green under that mutation is asserting nothing (D-23 clause 3).

Derive the effective-matrix key count OUTSIDE the loop that builds the matrix: compute the expected
count from `Object.keys(CHECKPOINT_DEFAULTS).length` and assert the built matrix's key count equals
it, so a silently short matrix is red and not merely an empty one (Pitfall 6). Reuse the sweep array
from `scripts/floor-invariance.test.ts` rather than retyping the values.

Cover the three AUTO-01 edge predicates at this scale: adjacency (two entries with the same id
collapse to one member — assert against a two-entry fixture), empty (an empty `checkpoints` object
yields the full roster default, and a zero-length `FLOOR_CHECKPOINTS` throws by name), and ordering
(the roster's `Object.keys` order equals the table's declaration order, and the derived-vs-roster
comparison is performed on sorted copies so ordering cannot change the verdict).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/checkpoints.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or fewer than 12 assertions reported for the file</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the weakened-canonicalizer mutation and names which assertions went red under it.
    - A test asserts the effective matrix key count against a denominator computed outside the matrix-building loop.
    - A test asserts that `FLOOR_CHECKPOINTS` of length zero throws a named error rather than returning an empty array.
    - A test asserts sorted-set equality between the effective matrix keys and `CHECKPOINTS`.
  </acceptance_criteria>
  <done>`scripts/checkpoints.test.ts` is green, and its assertions are proven discriminating by a recorded mutation that turns them red.</done>
</task>

<task type="auto">
  <name>Task 3: Zero-config whole-run differential against the pre-phase guard</name>
  <files>scripts/autonomy-zero-config.test.ts, hooks/guard.test.ts</files>
  <read_first>
    - hooks/guard.test.ts (the spawn-the-committed-.js harness; the payload builder)
    - scripts/catalog-freshness.ts lines 23-50 (the mirror-into-a-temp-dir + byte-compare shape)
    - scripts/coordinator-resolution-precheck.ts lines 293-307 (both signals captured from ONE run; the failure message names which fired)
    - .planning/phases/30-per-checkpoint-autonomy-matrix/30-CONTEXT.md D-20 and the Phase 28 AP-1 note
  </read_first>
  <action>
Create `scripts/autonomy-zero-config.test.ts` implementing the AUTO-07 differential as a WHOLE-RUN
comparison, never a substring probe (Phase 28 AP-1 is `blocking`, and `toContain` passes for a run
that also printed nine wrong lines).

Pin the pre-phase artifact by blob, not by ref: declare `PRE_PHASE_GUARD_JS_BLOB` as the
`git hash-object hooks/guard.js` value taken from the commit immediately before this plan's guard
change, and materialize it into a temp file with `git cat-file blob <blob>` at test time. A ref-based
mirror silently follows later commits; a blob is immutable.

For a fixed table of stdin payloads — at minimum one matched protected-branch push, one matched
production deploy, one benign read-only command, one malformed JSON body and one empty body — spawn
BOTH the materialized pre-phase `guard.js` and the committed `hooks/guard.js` under a project root
that carries NO `checkpoints` object, capture stdout and stderr from each single run, and assert the
line-level diff is exactly one ADDED line and zero removed or changed lines. Assert that the one
added line is the banner and that its text is the fixed literal for the all-default case. Derive the
payload count independently and assert the number of compared runs equals it, so a table that
silently loses a row is red.

Assert both signals from the same captured run and name which one fired in the failure message, in
the coordinator-precheck shape: the decision (deny payload present or absent) and the exit status
must agree, and a run whose banner claims all-default while a deny names a non-default checkpoint is
a failure with its own message.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' scripts/autonomy-zero-config.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or a failure naming a diff of more than one added line</fails_when>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' hooks/guard.test.ts scripts/floor-invariance.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - The test materializes the pre-phase guard from a pinned blob hash via `git cat-file blob`, not from a branch or tag name.
    - At least five distinct stdin payloads are compared, and the compared-run count is asserted against an independently computed payload count.
    - The assertion is a line-level diff of whole captured output, with exactly one added line and zero removed lines; no `toContain` is used to establish banner presence.
    - A deliberate second banner line inserted into the guard makes this test red (record the mutation and its result in the SUMMARY).
  </acceptance_criteria>
  <done>A zero-config repo's guard output differs from the pre-phase guard's output by exactly one added banner line across every payload in the table, proven by a whole-run diff and by a mutation that turns the assertion red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent shell command -> PreToolUse hook stdin | untrusted command text crosses into the guard |
| repo config file -> hook process | an agent-writable JSON file crosses into a safety decision |
| host session env -> hook process | the only channel the guard treats as human-authored |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege | `agent-factory/config/factory.config.json` `checkpoints` | high | mitigate | A config lowering with no matching `GRUGOPS_FLOOR_<ID>` in the hook's own env behaves exactly as `block` and denies by name (Task 1 acceptance criteria) |
| T-30-02 | Elevation of Privilege | `hooks/guard.ts` `SELF_APPROVE` | high | mitigate | One generalized detector over `APPROVAL` plus every derived floor-var name; refuses inline set/export even when the variable is already present |
| T-30-03 | Elevation of Privilege | host `.claude/settings.json` `env` block | high | accept | RESEARCH F-2: an agent that can write host settings files can inject the grant, and `process.env[NAME]` carries no provenance. Narrowed by a `permissions.deny` recommendation, not closed. Recorded as an accepted residual in plan 30-09 alongside the existing same-uid row; the render must not claim otherwise |
| T-30-04 | Tampering | `readGovernanceConfigResult` | high | mitigate | Four explicit degenerate-shape branches, all reaching `block` or the roster default by rule; `unreadable` treated as `block`; a throw at the reader call denies |
| T-30-05 | Spoofing | the run banner | medium | mitigate | Banner and decision captured from ONE run and asserted to agree; a whole-run differential rather than a substring probe (Phase 28 AP-1) |
| T-30-06 | Tampering | `hooks/guard.ts` byte freeze | high | mitigate | `FROZEN_GUARD_BLOB` updated and the guard committed in the SAME commit; the freeze test refuses a hash-only or code-only change (D-24) |
| T-30-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this phase installs zero packages (RESEARCH Package Legitimacy Audit); no install task exists, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `npm run build && npm run typecheck && npm run check:build-parity` green after the guard commit.
- `npx vitest run --exclude '**/scripts/e2e/**'` green for the full suite.
- `npm run freshness` green — every `.js` touched here is committed alongside its `.ts`.
- `node scripts/check-foundation-guards.js` green.
- The guard freeze verification is run AFTER the commit, never before (RESEARCH F-8).
</verification>

<success_criteria>
- One checkpoint is wired end-to-end: markdown-free floor id -> roster -> config cell -> reader -> hook decision -> banner.
- A config-only lowering changes nothing and says so by name.
- Zero-config output differs from the pre-phase guard by exactly one line.
- `hooks/guard.ts` is re-frozen in the same commit as its change.
</success_criteria>

<output>
Create `.planning/phases/30-per-checkpoint-autonomy-matrix/30-01-SUMMARY.md` when done. Record: the
new `FROZEN_GUARD_BLOB` value, the `PRE_PHASE_GUARD_JS_BLOB` value, and every mutation run with its
red/green result.
</output>

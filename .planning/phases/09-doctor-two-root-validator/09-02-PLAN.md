---
phase: 09-doctor-two-root-validator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate-agent-factory.mjs
autonomous: true
requirements: [VAL-02]
tags: [validator, two-root, kit-resolution, c3-false-green, node-stdlib]

must_haves:
  truths:
    - "The validator splits the single ROOT into an explicit KIT_ROOT (from VALIDATE_KIT_ROOT) and STATE_ROOT (from VALIDATE_ROOT or repo root)"
    - "VALIDATE_KIT_ROOT has NO default — unset is a hard error, never a silent fallback to '.'"
    - "VALIDATE_KIT_ROOT pointing at a nonexistent/empty dir FAILs the kit pass (missing roles/orchestrator.md)"
    - "kit-classified checks resolve under KIT_ROOT; state-classified checks resolve under STATE_ROOT (Phase-7 classification, the mixed loop at ~201-211 split)"
    - "The validator stays stdlib-only, read-only, creates no package.json, and keeps the two-tier errors/warnings + --strict promotion"
  artifacts:
    - path: "scripts/validate-agent-factory.mjs"
      provides: "two-root-aware structure validator: KIT_ROOT (no default) + STATE_ROOT, forked helpers, classification-routed checks"
      contains: "VALIDATE_KIT_ROOT"
  key_links:
    - from: "scripts/validate-agent-factory.mjs KIT_ROOT"
      to: "VALIDATE_KIT_ROOT env (no default)"
      via: "process.env.VALIDATE_KIT_ROOT with an unset → process.exit(1) guard"
      pattern: "VALIDATE_KIT_ROOT"
    - from: "scripts/validate-agent-factory.mjs kit checks"
      to: "KIT_ROOT/agent-factory/roles/orchestrator.md"
      via: "kitExists/kitRead helpers join(KIT_ROOT, rel)"
      pattern: "kitExists|kitRead|kitListDir"
---

<objective>
Make `scripts/validate-agent-factory.mjs` two-root aware (VAL-02 / SC3): replace the single `ROOT` (which silently defaults to the dev checkout — the C3 false-green) with an explicit `KIT_ROOT` (from a new `VALIDATE_KIT_ROOT`, NO default → unset is a hard error) plus a `STATE_ROOT` (from the existing `VALIDATE_ROOT`, or the repo root for back-compat). Fork the `exists/safeRead/listDir` helpers into kit-scoped and state-scoped variants, and route every existing check to the correct root by the Phase-7 kit-vs-state classification — fixing the routing bug at lines ~201-211 where kit refs and state refs are validated in one loop.

Purpose: kill the C3 false-green so the validator cannot return green in the dev checkout or with `$GRUGOPS_HOME` unset, and so doctor and validator resolve the kit home identically (SC4). This is the heart of VAL-02.
Output: `scripts/validate-agent-factory.mjs` resolves two explicit roots with no `.`-fallback for the kit; the BAD/GOOD fixtures and the resolution-parity assertion (Plan 04, wave 3) prove it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-doctor-two-root-validator/09-RESEARCH.md
@.planning/phases/09-doctor-two-root-validator/09-PATTERNS.md
@.planning/phases/09-doctor-two-root-validator/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split ROOT into KIT_ROOT (no default) + STATE_ROOT and fork the helpers</name>
  <files>scripts/validate-agent-factory.mjs</files>
  <read_first>
    - scripts/validate-agent-factory.mjs — the file being modified. Read: the header env-contract comment (lines 9-19, document the new `VALIDATE_KIT_ROOT` no-default + `VALIDATE_ROOT` state contract); the single `ROOT` resolution (lines 32-35 — the C3 footgun: `ROOT = process.env.VALIDATE_ROOT ? resolve(...) : resolve(SCRIPT_DIR, "..")`, the silent `.`-fallback to replace); the `exists()`/`safeRead()`/`listDir()` helpers (lines 38-52, fail-closed try/catch posture to preserve); the two-tier `errors[]`/`warnings[]` + `--strict` collector (lines 54-59) and the render/exit block (lines ~387-396) — PRESERVE exactly.
    - .planning/phases/09-doctor-two-root-validator/09-PATTERNS.md — § "scripts/validate-agent-factory.mjs — SPLIT ROOT into kit + state roots" (the exact replacement for lines 32-35, the helper fork to `kitExists`/`kitRead`/`kitListDir` + `stateExists`/`stateRead`/`stateListDir`).
    - .planning/phases/09-doctor-two-root-validator/09-RESEARCH.md — § Code Examples "Validator two-root split (replace ROOT)", § Discretion Resolutions §4 (reuse `VALIDATE_ROOT` as STATE_ROOT + new `VALIDATE_KIT_ROOT` no-default), § Pitfall 1 (the C3 false-green).
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-CONTEXT.md — the kit-vs-state classification both doctor and validator must honor without duplicating check-kit-refs.sh.
  </read_first>
  <action>
    Replace the single `ROOT` block (lines 32-35) with: `STATE_ROOT` = `process.env.VALIDATE_ROOT ? resolve(process.env.VALIDATE_ROOT) : resolve(SCRIPT_DIR, "..")` (back-compat repo root for STATE only); then a no-default guard — `if (!process.env.VALIDATE_KIT_ROOT) { console.error("  ERROR    VALIDATE_KIT_ROOT is unset — refusing to default the kit root to '.' (C3)"); process.exit(1); }` — then `const KIT_ROOT = resolve(process.env.VALIDATE_KIT_ROOT)`. The kit root re-implements the SAME `${GRUGOPS_HOME:-$HOME/.grugops}/agent-factory` rule shape conceptually but is supplied explicitly by the caller via `VALIDATE_KIT_ROOT` and has NO default (the deliberate C3 override per D-04 / D-08 — "no fallback" beats "sensible default" here, and only here). Fork the three helpers (lines 38-52): `kitExists`/`kitRead`/`kitListDir` join against `KIT_ROOT`; `stateExists`/`stateRead`/`stateListDir` join against `STATE_ROOT`; keep the fail-closed try/catch in each verbatim. Update the header comment (lines 9-19) to document the env contract: `VALIDATE_KIT_ROOT` (REQUIRED, no default — the C3 guard) and `VALIDATE_ROOT` (the STATE root, back-compat). Stay stdlib-only (`node:fs`/`node:path`/`node:url`), read-only, create NO `package.json` (VAL-01 constraints preserved). The deploy-approval env var is NEVER read or written.
  </action>
  <verify>
    <automated>( unset VALIDATE_KIT_ROOT; node scripts/validate-agent-factory.mjs 2>&1; echo "rc=$?" ) | grep -q 'rc=1' && ( unset VALIDATE_KIT_ROOT; node scripts/validate-agent-factory.mjs 2>&1 ) | grep -qi 'VALIDATE_KIT_ROOT is unset' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - With `VALIDATE_KIT_ROOT` unset, `node scripts/validate-agent-factory.mjs` exits nonzero and prints a line containing `VALIDATE_KIT_ROOT is unset` and `(C3)` — never a `.`-fallback (source + runtime assertion).
    - `scripts/validate-agent-factory.mjs` contains a `KIT_ROOT` derived from `process.env.VALIDATE_KIT_ROOT` with no `||`/ternary default on the kit root (source assertion: no `VALIDATE_KIT_ROOT ?` defaulting expression, only the unset-guard then `resolve(process.env.VALIDATE_KIT_ROOT)`).
    - `STATE_ROOT` derives from `VALIDATE_ROOT` with the repo-root back-compat fallback (source assertion).
    - Forked helpers `kitExists`/`kitRead`/`kitListDir` and `stateExists`/`stateRead`/`stateListDir` exist, each preserving the fail-closed try/catch.
    - No `package.json` is created (assert: `test ! -f package.json` after running, OR confirm the source contains no `writeFileSync`/`mkdir` of `package.json`).
    - The header comment documents `VALIDATE_KIT_ROOT` (no default) and `VALIDATE_ROOT` (state).
  </acceptance_criteria>
  <done>The validator resolves two explicit roots; the kit root has no default and errors loudly when unset; helpers are forked kit/state; the two-tier collector and stdlib-only/no-package.json constraints are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Route every check to its classified root (split the mixed ~201-211 loop)</name>
  <files>scripts/validate-agent-factory.mjs</files>
  <read_first>
    - scripts/validate-agent-factory.mjs — the check functions to re-route. Read: `checkRequiredFiles` (roles/workflows/handoffs/checklists, lines ~184-200 → KIT); the mixed state/kit loop at lines ~201-211 (currently iterates `agent-factory/config/*`, `agent-factory/packaging/*`, `AGENTS.md` (KIT) together with `plans/*` (STATE) — THIS is the routing bug PATTERNS.md flagged; split it by classification); `checkConfig` (`agent-factory/config/factory.config.json`, line ~236 → KIT); `checkPackaging` (adapters.md + `.claude-plugin/plugin.json`, line ~308 → KIT); `checkRoleSwitchProtocol` (line ~339 → KIT); `checkCommitConvention` (line ~353 → KIT); `checkWorkflowCommit` (line ~363 → KIT); `checkRoleSections` (~216 → KIT); `checkWorkflowSections` (~226 → KIT); `checkTickets` (`plans/tickets`, board, traceability, line ~264 → STATE); `plans/board.md`/`traceability.md`/`nfr-catalog.md`/`metrics.md` (~201-211 STATE half → STATE).
    - .planning/phases/09-doctor-two-root-validator/09-PATTERNS.md — § "scripts/validate-agent-factory.mjs" routing list (the explicit KIT-root vs STATE-root assignment of each check, including the note that lines 201-211 mix kit + state refs in one loop — split by classification).
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-CONTEXT.md — the canonical classification: `agent-factory/…` = KIT (resolve at KIT_ROOT); `plans/`, `memory-bank/`, `.grugops/factory.config.json` = STATE (repo-relative). Do NOT duplicate or call `check-kit-refs.sh` (D-09).
  </read_first>
  <action>
    Replace each check's `exists`/`safeRead`/`listDir` call with the correctly-scoped forked helper from Task 1. KIT-root checks (use `kitExists`/`kitRead`/`kitListDir`): `checkRequiredFiles` for `agent-factory/roles/*`, `agent-factory/workflows/*`, `agent-factory/handoffs/*`, `agent-factory/checklists/*`; `checkRoleSections`; `checkWorkflowSections`; `checkConfig` (`agent-factory/config/factory.config.json`); `checkPackaging` (`agent-factory/packaging/adapters.md` + `.claude-plugin/plugin.json`); `checkRoleSwitchProtocol`; `checkCommitConvention`; `checkWorkflowCommit`; and `AGENTS.md` (kit substrate). STATE-root checks (use `stateExists`/`stateRead`/`stateListDir`): `plans/board.md`, `plans/traceability.md`, `plans/nfr-catalog.md`, `plans/metrics.md`, and `checkTickets` (`plans/tickets` ↔ board ↔ traceability). At lines ~201-211, split the single mixed loop into a KIT-classified group (config/packaging/AGENTS.md) routed through `kitExists` and a STATE-classified group (`plans/*`) routed through `stateExists` — this is the routing bug fix. Do NOT change any check's logic, error/warning text, prefix-match semantics, vacuous-on-zero-tickets behavior, or the `--strict` promotion — only the root each path resolves against. Keep the validator stdlib-only, read-only, no `package.json`.
  </action>
  <verify>
    <automated>VALIDATE_KIT_ROOT=scripts/fixtures/good VALIDATE_ROOT=scripts/fixtures/good node scripts/validate-agent-factory.mjs 2>&1; test $? -eq 0 && echo GOOD-COMBINED-PASSES</automated>
  </verify>
  <acceptance_criteria>
    - Pointing BOTH roots at the existing combined `scripts/fixtures/good` tree (`VALIDATE_KIT_ROOT=scripts/fixtures/good VALIDATE_ROOT=scripts/fixtures/good`) exits 0 — proves the routing is correct and back-compatible with the existing GOOD fixture.
    - `VALIDATE_KIT_ROOT=/nonexistent/kit VALIDATE_ROOT=scripts/fixtures/good node scripts/validate-agent-factory.mjs` exits nonzero and reports a missing required kit file (e.g. `roles/orchestrator.md`) — the kit pass fails independently of a valid state root.
    - Source assertion: no check still uses the bare `exists`/`safeRead`/`listDir` (the single-root helpers) — every call site uses a `kit*` or `state*` variant (grep shows zero remaining bare-helper call sites, OR the bare helpers are removed entirely).
    - The mixed loop at the former lines ~201-211 is split: `plans/*` paths route through `state*` helpers and `agent-factory/*`/`AGENTS.md` through `kit*` helpers.
    - No check's error/warning text or pass/fail logic changed beyond the root it resolves against (diff-review assertion).
  </acceptance_criteria>
  <done>Every structure check resolves against its classified root; the mixed config/state loop is split; the combined GOOD fixture passes with both roots pointed at it, and a broken kit root fails the kit pass independently — with all VAL-01 finding semantics and constraints preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller env → validator roots | `VALIDATE_KIT_ROOT` / `VALIDATE_ROOT` are caller-supplied; an unset kit root is the C3 footgun the validator must reject |
| filesystem → validator reads | kit/state tree file content is untrusted input crossing into the structure scan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Spoofing / Repudiation (false green) | the C3 false-green: validator silently defaults the kit root to the dev checkout and reports green over a broken/absent kit | mitigate | `VALIDATE_KIT_ROOT` has NO default — unset is a hard `process.exit(1)` with an explicit "(C3)" message; a BAD missing-kit fixture and a BAD unset-kit fixture both fail (Plan 04 proves it). This is the heart of VAL-02. |
| T-09-06 | Tampering | validator follows a path/symlink outside its declared root and validates attacker content | mitigate | Every read is `join(KIT_ROOT, rel)` / `join(STATE_ROOT, rel)` with fixed literal rels; no write path is derived from file content; reads are fail-closed try/catch → finding, never a follow-and-throw (preserves the VAL-01 read-only invariant). |
| T-09-07 | DoS | a garbled/oversized kit/state file crashes the validator | mitigate | Forked helpers keep the existing fail-closed try/catch (`kitRead`/`stateRead` → null on error); a bad file becomes a finding, never an unhandled throw. |
| T-09-03 | Elevation (crosses the hard safety line) | validator reads/writes the prod-deploy approval env var | accept | The validator reads only `VALIDATE_KIT_ROOT`/`VALIDATE_ROOT`; it never names the deploy-approval env var (no change introduces it; carried prohibition holds vacuously). |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Zero packages — Node stdlib only, no `package.json` (RESEARCH § Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- Unset `VALIDATE_KIT_ROOT` → nonzero exit + "(C3)" message (no `.`-fallback).
- Both roots at `scripts/fixtures/good` → exit 0 (back-compat).
- `VALIDATE_KIT_ROOT` at a nonexistent dir → nonzero (kit pass fails on missing `roles/orchestrator.md`).
- Regression (kept separate per D-09): `sh scripts/check-kit-refs.sh` exits 0 — the validator neither absorbs nor calls it.
- Full BAD/GOOD/unset fixture matrix + the three-way resolution-parity assertion are added in Plan 04 (wave 3).
</verification>

<success_criteria>
- VAL-02 / SC3: the validator is two-root aware (explicit kit root + state root, NO `.`-fallback) and cannot false-green in the dev checkout or with `$GRUGOPS_HOME` unset.
- D-08 (no-default kit root), D-04 (same resolution rule the doctor uses), and the lines-201-211 routing-bug fix are implemented.
- Stdlib-only, read-only, no `package.json`, two-tier errors/warnings + `--strict` preserved (VAL-01 constraints intact).
- `check-kit-refs.sh` stays separate and GREEN (D-09).
</success_criteria>

<output>
Create `.planning/phases/09-doctor-two-root-validator/09-02-SUMMARY.md` when done.
</output>

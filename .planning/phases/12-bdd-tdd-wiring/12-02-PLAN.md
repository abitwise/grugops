---
phase: 12-bdd-tdd-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/checklists/example-mapping.md
  - agent-factory/workflows/07-backlog-refinement.md
autonomous: true
requirements: [BDD-02, BDD-03]
must_haves:
  truths:
    - "A Three Amigos / Example Mapping step is folded into backlog refinement and produces the scenarios before code"
    - "The ceremony lives once in a single-source example-mapping.md hub (mirroring definition-of-ready.md); workflow 07 points to it, never restates it"
    - "Example Mapping (rules/examples/questions) runs in the discovery conversation FIRST; the declarative G/W/T scenarios are written AFTER, not live"
    - "The step is gated by the bdd dial in tiers (off=no step / lean=BA self-runs all three voices / strict=named participants) and degrades to lean when absent"
    - "The contract-vs-logic seam worked example lives in the hub (not in a role file)"
  artifacts:
    - path: "agent-factory/checklists/example-mapping.md"
      provides: "Three Amigos + Example Mapping single-source hub + the contract-vs-logic seam worked example"
      min_lines: 15
      contains: "Example Mapping"
    - path: "agent-factory/workflows/07-backlog-refinement.md"
      provides: "dial-gated Three Amigos step pointing to the hub, inserted after the INVEST step"
      contains: "example-mapping.md"
  key_links:
    - from: "agent-factory/workflows/07-backlog-refinement.md"
      to: "agent-factory/checklists/example-mapping.md"
      via: "single pointer line in the Steps list (reference-not-embed)"
      pattern: "example-mapping\\.md"
---

<objective>
Create the single-source `example-mapping.md` hub (Three Amigos + Example Mapping) and fold a dial-gated pointer step into `07-backlog-refinement.md` so the scenarios are produced before code (BDD-02). The hub mirrors `definition-of-ready.md` (terse, ~DoR-sized), encodes the discovery-first rule (D-06), the three voices (D-05), the four-card structure, AND the contract-vs-logic seam worked example (D-09 — which must NOT live in a role file). The workflow step reads the `bdd` dial inline and degrades to lean (D-05/BDD-03).

Purpose: The Three Amigos discovery conversation is what produces the declarative scenarios the acceptance contract carries — single-sourced as a hub so workflow 07 stays terse and the ceremony is reusable (mirrors the Phase 11 DoR hub pattern).
Output: One NEW hub file + the modified workflow 07 with a single dial-gated pointer step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md
@.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md
@agent-factory/checklists/definition-of-ready.md
@agent-factory/workflows/07-backlog-refinement.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the example-mapping.md hub (mirror definition-of-ready.md) with the worked seam example</name>
  <files>agent-factory/checklists/example-mapping.md</files>
  <read_first>
    - `agent-factory/checklists/definition-of-ready.md` — the EXACT shape to mirror (857 B): YAML frontmatter (`kind: checklist`, `tier: lean`), one `#` title, a one-line "what/when this applies" lead, then a flat terse bullet list (one line per item, no sub-prose).
    - `.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md` § "example-mapping.md (checklist-hub, NEW)" — the frontmatter+lead pattern, the terse-bullet body pattern, and the "what to fill in" list (dial-read comment, discovery-first rule, three-voices line, four-card list, Done-when line, seam worked example).
    - `.planning/phases/12-bdd-tdd-wiring/12-RESEARCH.md` § Code Examples "The example-mapping.md hub — terse structure" (authoritative hub body) AND "The contract-vs-logic seam — one short stack-neutral worked example" (the discount-code example to embed here). Pattern 2 (Example Mapping before Gherkin).
    - `.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md` D-04 (separate hub), D-05 (three voices, dial tiers), D-06 (Example Mapping first / G/W/T after), D-09 (seam + worked example lives in hub).
  </read_first>
  <action>
    Create `agent-factory/checklists/example-mapping.md` mirroring `definition-of-ready.md`'s structure: frontmatter (`kind: checklist`, `tier: lean`), `# Example Mapping (Three Amigos)` title, a one-line lead, then terse content. Include, each as the fewest sharp words: (a) a dial-read HTML comment naming the `bdd` key + tiers + literal lean default — `off = skip · lean = BA self-runs all three voices · strict = named participants; default lean` (D-05/BDD-03); (b) the discovery-first rule line — run the rules/examples/questions conversation FIRST, write the Given/When/Then AFTER (not live) (D-06); (c) the three-voices line — product decides scope · QE generates edge cases · engineer adds detail (D-05); (d) the four-card bullet list — story (yellow) / rule (blue) / example (green → later a declarative scenario) / question (red, defer don't block); (e) a "Done when" line; (f) the contract-vs-logic seam worked example (D-09) — use the stack-neutral discount-code example from RESEARCH: OUTER (BDD acceptance asserts "the order total is 90" ONCE) vs INNER (TDD unit asserts percentage rounding, expired-code rejection, minimum-spend, no-stacking — never re-asserting "total is 90"), and one seam sentence. Keep ALL example scenarios declarative business language (no `#id`/`.class`/`click`/`navigate to`) per Pattern 1. Hub-sized — terse like the DoR hub, NOT a wall of text. This is where the seam example lives so it never bloats a role file (byte ceiling).
  </action>
  <verify>
    <automated>test -f agent-factory/checklists/example-mapping.md && grep -qi 'after\|first' agent-factory/checklists/example-mapping.md && grep -q 'bdd' agent-factory/checklists/example-mapping.md && grep -qi 'seam\|observable\|discount' agent-factory/checklists/example-mapping.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test -f agent-factory/checklists/example-mapping.md` exits 0 (NEW hub exists — its existence is the BDD-02 test).
    - `grep -qi 'after\|first' agent-factory/checklists/example-mapping.md` exits 0 (Example-Mapping-before-Gherkin rule present).
    - `grep -q 'bdd' agent-factory/checklists/example-mapping.md` exits 0 (dial-read present; degrade-to-lean stated).
    - `grep -qi 'seam\|observable\|discount' agent-factory/checklists/example-mapping.md` exits 0 (worked seam example present in the hub, not a role).
    - Hub frontmatter mirrors the DoR hub: `grep -q '^kind: checklist' agent-factory/checklists/example-mapping.md`.
    - No selector tokens: `! grep -nE '#[a-zA-Z]|click |navigate to' agent-factory/checklists/example-mapping.md` (manual confirm declarativeness + terseness vs DoR-hub size).
  </acceptance_criteria>
  <done>The example-mapping.md hub exists, terse and DoR-shaped, carrying the three voices, four cards, discovery-first rule, dial-read, and the seam worked example.</done>
</task>

<task type="auto">
  <name>Task 2: Fold the dial-gated Three Amigos pointer step into workflow 07</name>
  <files>agent-factory/workflows/07-backlog-refinement.md</files>
  <read_first>
    - `agent-factory/workflows/07-backlog-refinement.md` — current numbered `## Steps` list: Step 2 (clarify to INVEST, lines 25), Step 3 (split XL), Step 4 (size/prioritize); the existing pointer-to-hub convention referencing `definition-of-ready.md` (lines 21, 29). The Phase-11 senior-BA INVEST step (Step 2) MUST stay untouched.
    - `agent-factory/checklists/example-mapping.md` — the hub authored in Task 1 (the pointer target).
    - `.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md` § "07-backlog-refinement.md" — the single-line pointer-to-hub convention + the insertion point (after "clarify to INVEST", before sizing) per RESEARCH Open Question 2.
    - `.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md` D-04/D-05/D-06; RESEARCH Open Question 2 (step ordering recommendation).
  </read_first>
  <action>
    Insert ONE new dial-gated step into the `## Steps` list AFTER the "clarify to INVEST" step (current Step 2) and BEFORE the sizing step (current Step 4) — renumber the subsequent steps accordingly. The new step is a single pointer line in the file's existing reference-not-embed register: when `bdd` is not `off`, run the Three Amigos / Example Mapping conversation per `agent-factory/checklists/example-mapping.md` to produce declarative scenarios BEFORE the item is sized — read the `bdd` dial inline (off = skip the step · lean = BA self-runs all three voices · strict = named participants; default lean). Do NOT restate the four-card ceremony or the seam here — point to the hub (single-source). The Phase-11 senior-BA INVEST step stays untouched. Optionally add the hub to the `## Inputs required` list beside the existing `definition-of-ready.md` line, matching that line's style. Add a one-line no-selectors reminder if natural (D-03), but keep the step terse.
  </action>
  <verify>
    <automated>grep -q 'example-mapping.md' agent-factory/workflows/07-backlog-refinement.md && grep -q 'bdd' agent-factory/workflows/07-backlog-refinement.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'example-mapping.md' agent-factory/workflows/07-backlog-refinement.md` exits 0 (workflow 07 points to the hub).
    - `grep -q 'bdd' agent-factory/workflows/07-backlog-refinement.md` exits 0 (dial-gated; degrade-to-lean stated inline).
    - The Phase-11 INVEST step is preserved: `grep -q 'INVEST' agent-factory/workflows/07-backlog-refinement.md` exits 0.
    - The new step is a pointer, not a restatement: the file does NOT inline the four-card list — `! grep -qi 'yellow\|blue card\|red card' agent-factory/workflows/07-backlog-refinement.md` (the cards live in the hub only).
  </acceptance_criteria>
  <done>Workflow 07 carries a single dial-gated Three Amigos step that points to the example-mapping.md hub; INVEST step preserved; ceremony not restated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan creates/edits markdown checklist + workflow files in a no-runtime kit. No executable code, input handling, auth/crypto/data/network surface is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-DG | Repudiation | the example-mapping hub's example scenarios + the seam worked example | mitigate | Decorative/imperative Gherkin giving false assurance is countered by the declarative no-selectors rule (Pattern 1 / D-03) and the discovery-first intent (D-06). The seam example explicitly draws the one-behavior-one-layer line (D-09) to prevent BDD/TDD duplication. Mechanical no-duplication / dead-Gherkin detection is deferred to the Phase 15 gate — this plan lands the followable rule. |

> No application-security threats are introduced (RESEARCH § Security Domain). The substantive ASVS audit is Phase 14.
</threat_model>

<verification>
- `sh scripts/check-foundation-guards.sh` exits 0 (this plan touches no role file or adapter; guards stay GREEN for free).
- `test -f agent-factory/checklists/example-mapping.md` exits 0 (NEW hub).
- `grep -q 'example-mapping.md' agent-factory/workflows/07-backlog-refinement.md` exits 0 (pointer wired).
- Prose-judgment spot check: hub is terse/DoR-sized, discovery-first intent clear, seam example reads declaratively, no selectors anywhere.
</verification>

<success_criteria>
BDD-02 + BDD-03: a single-source Three Amigos / Example Mapping hub exists and is folded into backlog refinement as a dial-gated pointer step that produces declarative scenarios before code, degrading to lean when the `bdd` dial is absent; the contract-vs-logic seam worked example lives in the hub.
</success_criteria>

<output>
Create `.planning/phases/12-bdd-tdd-wiring/12-02-SUMMARY.md` when done.
</output>

---
phase: 12-bdd-tdd-wiring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/workflows/04-ticket-to-pr.md
  - agent-factory/handoffs/implementation-ready-packet.md
autonomous: true
requirements: [TDD-01]
must_haves:
  truths:
    - "The engineering workflow drives test-first red-green-refactor at the unit layer (write failing unit test → minimal code → green → refactor)"
    - "The double-loop rule is encoded: the outer acceptance scenario stays red until the inner loop closes it, and no second acceptance scenario goes red before the first is green"
    - "The workflow references the §14 gate (workflow 05) for enforcement and does not restate it (enforcement is Phase 15)"
    - "A light forward-pointer notes that acceptance scenarios flow forward to UAT/release (D-13) — not a deep BDD rewrite of UAT/upstream"
    - "The implementation-ready packet's existing ## Test strategy heading carries a terse TDD line the engineer reads before coding (which units prove the behavior, which layer owns what)"
  artifacts:
    - path: "agent-factory/workflows/04-ticket-to-pr.md"
      provides: "TDD red-green step + double-loop / no-second-red rule in/around Step 3 + light forward-pointer (D-13)"
      contains: "refactor"
    - path: "agent-factory/handoffs/implementation-ready-packet.md"
      provides: "TDD test-strategy line under the existing ## Test strategy heading (D-11)"
      contains: "## Test strategy"
  key_links:
    - from: "agent-factory/workflows/04-ticket-to-pr.md"
      to: "agent-factory/workflows/05-pr-quality-gate.md"
      via: "existing gate reference (reference-not-restate; enforcement stays in 05/Phase 15)"
      pattern: "05-pr-quality-gate\\.md"
---

<objective>
Encode the TDD red-green double-loop at the unit layer in `04-ticket-to-pr.md` (TDD-01) and add the upfront TDD test-strategy line to the existing `## Test strategy` heading in `implementation-ready-packet.md` (D-11). The workflow step is the engineer-owned inner loop (D-07): write failing unit test → minimal code → green → refactor; the outer acceptance scenario stays red until the inner loop closes it; no second acceptance scenario goes red before the first is green (D-08). Enforcement stays single-source in gate workflow 05 (Phase 15) — reference, do not restate. A light forward-pointer (D-13) notes that the acceptance scenarios flow forward to UAT/release — a one-line carry, not a deep BDD rewrite of the UAT/upstream contracts.

Purpose: "The workflow routes, the role enforces" (D-08) — this is the workflow half: it carries the red-green sequence and the no-second-red rule the engineer follows, plus the read-before-coding strategy line that feeds the inner loop and the D-09 seam.
Output: Modified workflow 04 (TDD step in/around Step 3 + light forward-pointer) + modified impl-ready packet (TDD line under the existing Test strategy heading).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md
@.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md
@agent-factory/workflows/04-ticket-to-pr.md
@agent-factory/handoffs/implementation-ready-packet.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the TDD red-green + double-loop step to workflow 04 (+ light forward-pointer D-13)</name>
  <files>agent-factory/workflows/04-ticket-to-pr.md</files>
  <read_first>
    - `agent-factory/workflows/04-ticket-to-pr.md` — current `## Steps` list: Step 3 "The Software Engineer implements the one ticket on a branch — a small diff, with tests." (line 27); Step 4 references gate 05 and explicitly says it "does not restate it" (line 28) — copy that reference-not-restate register for the TDD step's relationship to the gate. The `## Trace updates` section (line 38) is the natural home for the light forward-pointer.
    - `.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md` § "04-ticket-to-pr.md" — the reference-not-restate convention (line 28) and where the TDD step slots (into/around Step 3).
    - `.planning/phases/12-bdd-tdd-wiring/12-RESEARCH.md` Pattern 3 (the non-conflict double-loop — outer acceptance / inner unit, one failing acceptance at a time) and the System Architecture Diagram (the inner-loop sequence + the D-13 "scenarios flow forward, NOT rewritten here" forward arrow to UAT/release).
    - `.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md` D-07 (QE-outer/engineer-inner), D-08 (double-loop rules live in the workflow step), D-09 (one-behavior-one-layer seam — note the worked example lives in the hub, not here), D-13 (light forward-pointer only; NOT a deep UAT/upstream BDD rewrite).
  </read_first>
  <action>
    Modify/expand Step 3 (or add a tightly-scoped step in/around it) to encode the engineer's inner loop: write a FAILING unit test → minimal code to pass (green) → refactor (still green), repeated per unit behavior. State the double-loop rule (D-08): the outer acceptance scenario (QE-owned, from the handoff scenarios block) stays RED until the inner loop closes it, and NO SECOND acceptance scenario goes red before the first is green. State the contract-vs-logic seam in one line (D-09): BDD acceptance asserts the observable business outcome once; TDD unit asserts the internal logic and edge cases beneath it — point to `agent-factory/checklists/example-mapping.md` for the worked example rather than restating it here. Keep enforcement in gate 05: the existing Step (line 28) reference to `05-pr-quality-gate.md` stays unchanged; do NOT add any mechanical guard or restate the gate — mechanical no-second-red / one-layer enforcement is Phase 15. Read the `quality.tdd` dial inline if natural (off/encouraged/required, default encouraged) but the dialed EVIDENCE field is plan 12-04's concern — keep this step about the sequence + the rule. **D-13 (light forward-pointer):** add ONE line (in `## Trace updates`, the done-condition, or as an HTML comment) noting that the acceptance scenarios flow forward to the UAT pack / release — they are carried, NOT rewritten here; a deeper UAT-BDD treatment is its own later concern. Keep it a single carry line — do NOT touch `06-uat-pack.md` or the upstream 02/03 workflows. Match the workflow's terse numbered-step register.
  </action>
  <verify>
    <automated>grep -qi 'red.*green\|failing.*test.*first\|refactor' agent-factory/workflows/04-ticket-to-pr.md && grep -qi 'no second\|outer\|inner' agent-factory/workflows/04-ticket-to-pr.md && grep -q '05-pr-quality-gate.md' agent-factory/workflows/04-ticket-to-pr.md && grep -qi 'forward\|uat\|carried' agent-factory/workflows/04-ticket-to-pr.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi 'red.*green\|failing.*test.*first\|refactor' agent-factory/workflows/04-ticket-to-pr.md` exits 0 (red-green-refactor sequence present).
    - `grep -qi 'no second\|outer\|inner' agent-factory/workflows/04-ticket-to-pr.md` exits 0 (double-loop / no-second-red rule present).
    - `grep -q '05-pr-quality-gate.md' agent-factory/workflows/04-ticket-to-pr.md` exits 0 (gate reference preserved — enforcement stays single-source in 05).
    - `grep -qi 'forward\|uat\|carried' agent-factory/workflows/04-ticket-to-pr.md` exits 0 (D-13 light forward-pointer present).
    - No new mechanical guard authored and the gate is not restated: `! grep -qi 'self_fix_attempts\|self-fix loop' agent-factory/workflows/04-ticket-to-pr.md` (the gate loop stays in 05).
    - No UAT/upstream rewrite: `git diff --name-only` shows neither `06-uat-pack.md` nor workflows 02/03 changed (D-13 is a one-line carry only).
    - The seam points to the hub, not a restated example: `grep -q 'example-mapping.md' agent-factory/workflows/04-ticket-to-pr.md` OR the seam is a single rule line (manual confirm the discount worked example is NOT duplicated here).
  </acceptance_criteria>
  <done>Workflow 04 carries the engineer inner-loop red-green-refactor sequence + the no-second-red double-loop rule + the one-line seam + the D-13 light forward-pointer, referencing gate 05 for enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Add the TDD test-strategy line to the impl-ready packet</name>
  <files>agent-factory/handoffs/implementation-ready-packet.md</files>
  <read_first>
    - `agent-factory/handoffs/implementation-ready-packet.md` — `## Test strategy` ALREADY EXISTS at line 39 (no new heading needed); the file uses a `<!-- reference ... -->` pointer-comment convention (lines 35, 37); register is "Clear voice; empty-but-shaped; no fake data" (lines 28-29).
    - `.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md` § "implementation-ready-packet.md" — extend the existing `## Test strategy` heading with ONE terse line; reuse the file's `<!-- reference ... -->` comment style if a pointer is wanted.
    - `.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md` D-11 (upfront TDD test-strategy: which unit tests prove the behavior, which layer owns what — feeds the inner loop + the D-09 seam).
  </read_first>
  <action>
    Under the EXISTING `## Test strategy` heading (line 39 — do NOT add a new heading), add ONE terse line in the file's empty-but-shaped clear-voice register: which unit tests prove the changed behavior, and which layer owns what (the acceptance scenario owns the observable outcome; the unit tests own the logic/edge cases beneath it — the D-09 seam the engineer reads before coding). Optionally reuse the `<!-- reference ... -->` comment style to point to `agent-factory/checklists/example-mapping.md` for the seam. No fake data. Do NOT modify any other heading.
  </action>
  <verify>
    <automated>grep -q '^## Test strategy' agent-factory/handoffs/implementation-ready-packet.md && grep -qi 'unit\|layer\|red.*green' agent-factory/handoffs/implementation-ready-packet.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^## Test strategy' agent-factory/handoffs/implementation-ready-packet.md` exits 0 (existing heading preserved, not duplicated).
    - `grep -qi 'unit\|layer\|red.*green' agent-factory/handoffs/implementation-ready-packet.md` exits 0 (TDD test-strategy content added under the heading).
    - Exactly one `## Test strategy` heading: `[ "$(grep -c '^## Test strategy' agent-factory/handoffs/implementation-ready-packet.md)" = "1" ]` (no duplicate heading introduced).
  </acceptance_criteria>
  <done>The impl-ready packet's existing Test strategy heading carries the terse TDD read-before-coding line feeding the inner loop and the seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan edits markdown workflow + handoff templates in a no-runtime kit. No executable code, input handling, auth/crypto/data/network surface is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-DUP | Repudiation | the workflow 04 TDD step + seam rule | mitigate | BDD/TDD duplication (the same observable outcome asserted at both layers, giving false confidence) is countered by the one-line contract-vs-logic seam (D-09) that points to the hub's worked example. Mechanical no-duplication / no-unjustified-skip enforcement is deferred to the Phase 15 test-integrity gate — this plan lands the followable rule, not the gate. |

> No application-security threats are introduced (RESEARCH § Security Domain). The substantive ASVS audit is Phase 14.
</threat_model>

<verification>
- `sh scripts/check-foundation-guards.sh` exits 0 (this plan touches no role file or adapter; guards stay GREEN for free).
- Workflow 04 carries the red-green-refactor sequence + the no-second-red rule + the gate-05 reference + the D-13 forward-pointer (greps exit 0).
- impl-ready packet has exactly one `## Test strategy` heading carrying the TDD line.
- No new mechanical enforcement guard authored; the gate loop stays in workflow 05 (Phase-15 boundary respected); no UAT/upstream rewrite.
</verification>

<success_criteria>
TDD-01 (workflow + packet half): the engineering workflow drives test-first red-green-refactor at the unit layer with the double-loop / no-second-red rule and the one-behavior-one-layer seam encoded, referencing the §14 gate for enforcement, with a light forward-pointer (D-13) noting scenarios flow forward; the impl-ready packet carries the upfront TDD test-strategy line.
</success_criteria>

<output>
Create `.planning/phases/12-bdd-tdd-wiring/12-03-SUMMARY.md` when done.
</output>

---
phase: 12-bdd-tdd-wiring
plan: 04
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - agent-factory/handoffs/implementation-handoff.md
  - agent-factory/handoffs/qe-handoff.md
autonomous: true
requirements: [TDD-02]
must_haves:
  truths:
    - "The implementation handoff carries a tiered test-first / red-green evidence field that reads quality.tdd (off=omit / encouraged=honest 'tests written' / required=red→green sequence) and degrades to encouraged when absent"
    - "The QE handoff carries the acceptance-side red/green evidence (QE owns the outer loop)"
    - "Both evidence fields carry the no-fabrication floor in clear professional voice: a step not run is marked UNKNOWN - verify; never record a red/green that did not happen"
  artifacts:
    - path: "agent-factory/handoffs/implementation-handoff.md"
      provides: "tiered test-first / red-green evidence field extending the existing Tests added / Commands run section (D-10)"
      contains: "UNKNOWN - verify"
    - path: "agent-factory/handoffs/qe-handoff.md"
      provides: "acceptance-side red/green evidence near ## Result (QE owns the outer loop, D-07)"
      contains: "UNKNOWN - verify"
  key_links:
    - from: "agent-factory/handoffs/implementation-handoff.md"
      to: "the no-fabrication floor"
      via: "clear-voice UNKNOWN - verify sentence in the evidence field"
      pattern: "UNKNOWN - verify"
---

<objective>
Add the tiered test-first / red-green evidence field to the implementation handoff and the acceptance-side red/green evidence to the QE handoff (D-10), each carrying the no-fabrication floor in clear professional voice. The field reads the `quality.tdd` dial (off / encouraged / required, default encouraged) — TDD-02 — and extends the existing evidence sections rather than inventing a new artifact. This is the phase's one safety-relevant control: the trace must never assert a red/green that did not run (`UNKNOWN - verify`).

Purpose: The evidence field is the machine-readable artifact the Phase 15 gate will later check, and the no-fabrication floor preserves grugops's "the trace is the proof" value at the moment of greatest temptation to fabricate.
Output: Modified implementation-handoff (test-first evidence field) + modified qe-handoff (acceptance-side red/green evidence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md
@.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md
@agent-factory/handoffs/implementation-handoff.md
@agent-factory/handoffs/qe-handoff.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the tiered test-first / red-green evidence field to implementation-handoff.md</name>
  <files>agent-factory/handoffs/implementation-handoff.md</files>
  <read_first>
    - `agent-factory/handoffs/implementation-handoff.md` — existing evidence section: `## Tests added` (line 31), `## Commands run` (line 32); the new field EXTENDS this pair (D-10 "extend, don't invent"). Empty-but-shaped register.
    - `.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md` § "implementation-handoff.md" — extend `## Tests added` / `## Commands run`; the `quality.tdd` dial comment + the no-fabrication floor comment (clear voice) + `Red:` / `Green:` / `Layer:` lines.
    - `.planning/phases/12-bdd-tdd-wiring/12-RESEARCH.md` § Code Examples "The tiered test-first evidence field" (authoritative shape) and § Pattern 4 (dial-read degrade-to-encouraged); § Common Pitfalls Pitfall 2 (do not leak caveman into the clear-voice floor line).
    - `.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md` D-10 (tiered evidence + no-fabrication floor); the no-fabrication / trace-integrity value.
  </read_first>
  <action>
    Add a tiered test-first / red-green evidence field near `## Tests added` (line 31), extending the existing evidence section (do NOT invent a new top-level artifact — a `## Test-first evidence` heading or an extension of `## Tests added` are both acceptable; match the file's empty-but-shaped register). Include: (a) a `quality.tdd` dial-read HTML comment naming the key + tiers + literal default — `off = omit · encouraged = honest "tests written for the changed behavior" · required = record the red→green sequence as actually run; default encouraged` (TDD-02 / D-10); (b) the no-fabrication floor as a CLEAR-VOICE sentence (NOT caveman) ending in the literal token `UNKNOWN - verify` — "if a step was not run, write `UNKNOWN - verify`; never record a red or green that did not actually happen" (D-10, the safety control); (c) the skeleton lines `Red (failing test first):`, `Green (minimal code passed):`, `Layer:` (the unit behavior this proves — distinct from the acceptance scenario it sits under). No fake data. Keep the floor sentence in clear professional voice per the two-voice discipline.
  </action>
  <verify>
    <automated>grep -qi 'test-first\|red.*green' agent-factory/handoffs/implementation-handoff.md && grep -qi 'tdd' agent-factory/handoffs/implementation-handoff.md && grep -q 'UNKNOWN - verify' agent-factory/handoffs/implementation-handoff.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi 'test-first\|red.*green' agent-factory/handoffs/implementation-handoff.md` exits 0 (evidence field present).
    - `grep -qi 'tdd' agent-factory/handoffs/implementation-handoff.md` exits 0 (`quality.tdd` dial-read present; degrade-to-encouraged stated).
    - `grep -q 'UNKNOWN - verify' agent-factory/handoffs/implementation-handoff.md` exits 0 (no-fabrication floor present).
    - The floor line is clear voice (no caveman markers near it): `! grep -niE 'grug|smash|shiny|me think' agent-factory/handoffs/implementation-handoff.md` (manual confirm the floor sentence reads as clear professional voice).
  </acceptance_criteria>
  <done>The implementation handoff carries the tiered, dial-aware test-first evidence field with the clear-voice no-fabrication floor.</done>
</task>

<task type="auto">
  <name>Task 2: Add the acceptance-side red/green evidence to qe-handoff.md (QE outer loop)</name>
  <files>agent-factory/handoffs/qe-handoff.md</files>
  <read_first>
    - `agent-factory/handoffs/qe-handoff.md` — current structure INCLUDING the `## Acceptance scenarios` block added by plan 12-01 (this plan depends on 12-01); `## Result` is near line 35. Add the acceptance-side red/green evidence near `## Result`.
    - `agent-factory/handoffs/implementation-handoff.md` — the evidence field authored in Task 1 (mirror its no-fabrication floor + register for the acceptance side).
    - `.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md` § "qe-handoff.md" — the acceptance-side red/green evidence goes near `## Result` (line 35); QE owns the outer loop (D-07), so the ACCEPTANCE red/green lives here, not in implementation-handoff.
    - `.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md` D-10 (acceptance side in the QE handoff), D-07 (QE owns the outer loop).
  </read_first>
  <action>
    Add the acceptance-side red/green evidence near `## Result` (line 35): the outer acceptance scenario's red→green state as QE actually ran it against the host runner, with the SAME clear-voice no-fabrication floor (`UNKNOWN - verify` when a step was not run; never record a red/green that did not happen). Mirror the floor wording from Task 1. This is the QE-owned outer-loop evidence (D-07) — distinct from the engineer's inner-loop evidence in implementation-handoff. Keep the empty-but-shaped register (no fake data). Do NOT touch the `## Acceptance scenarios` block added by 12-01 — only add the evidence near `## Result`.
  </action>
  <verify>
    <automated>grep -q 'UNKNOWN - verify' agent-factory/handoffs/qe-handoff.md && grep -qi 'red.*green\|acceptance.*evidence\|outer' agent-factory/handoffs/qe-handoff.md && grep -q '^## Acceptance scenarios' agent-factory/handoffs/qe-handoff.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'UNKNOWN - verify' agent-factory/handoffs/qe-handoff.md` exits 0 (no-fabrication floor on the acceptance side).
    - `grep -qi 'red.*green\|acceptance.*evidence\|outer' agent-factory/handoffs/qe-handoff.md` exits 0 (acceptance-side red/green evidence present).
    - The 12-01 scenarios block is intact: `grep -q '^## Acceptance scenarios' agent-factory/handoffs/qe-handoff.md` exits 0 (this plan did not remove it).
    - Floor line is clear voice: `! grep -niE 'grug|smash|shiny|me think' agent-factory/handoffs/qe-handoff.md` (manual confirm clear professional voice at the trust moment).
  </acceptance_criteria>
  <done>The QE handoff carries the acceptance-side red/green evidence with the clear-voice no-fabrication floor; the 12-01 scenarios block is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trace-integrity boundary | The test-first evidence fields are where an agent could fabricate a red/green that never ran — grugops's core value (the trace is the proof) crosses here. This is the phase's one safety-relevant control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-FAB | Repudiation / Tampering | the test-first evidence field (implementation + QE handoffs) | mitigate | A fabricated red/green or an executable run that didn't happen would give false assurance and corrupt the trace. Mitigated by the no-fabrication floor (D-10): a step not run is marked `UNKNOWN - verify`, written in clear professional voice; never record a red/green that did not happen. Mechanical detection of fabricated evidence is deferred to the Phase 15 test-integrity gate — this plan makes the field honest by construction. |
| T-12-04-VOICE | Information disclosure (clarity erosion) | the no-fabrication floor sentence | mitigate | Caveman voice leaking into the safety/no-fabrication line would erode clarity at the trust moment. Mitigated by two-voice discipline: the floor sentence is clear professional voice, verified by `guard_voice` staying GREEN (no caveman markers in handoff prose). |

> The substantive security ASVS audit is Phase 14. This plan's only security-adjacent obligation is preserving the no-fabrication / trace-integrity floor in clear voice — discharged above.
</threat_model>

<verification>
- `sh scripts/check-foundation-guards.sh` exits 0 (handoff edits touch no role file or adapter; `guard_voice` confirms the clear-voice floor carries no caveman markers).
- Both handoffs carry `UNKNOWN - verify` in the new evidence fields.
- `quality.tdd` dial-read present in implementation-handoff (degrade-to-encouraged).
- The 12-01 scenarios block in qe-handoff is intact.
- Prose-judgment spot check: the no-fabrication floor reads as clear professional voice, not caveman.
</verification>

<success_criteria>
TDD-02: tiered test-first / red-green evidence reads `quality.tdd` and degrades to encouraged when absent, carried on both the engineer (inner) and QE (outer) sides, each with the clear-voice no-fabrication floor that never records a red/green that did not run.
</success_criteria>

<output>
Create `.planning/phases/12-bdd-tdd-wiring/12-04-SUMMARY.md` when done.
</output>

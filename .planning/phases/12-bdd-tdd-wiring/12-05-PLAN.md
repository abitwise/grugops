---
phase: 12-bdd-tdd-wiring
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/roles/software-engineer.md
  - agent-factory/roles/qe-e2e.md
  - AGENTS.md
autonomous: true
requirements: [TDD-01, BDD-01]
must_haves:
  truths:
    - "software-engineer.md carries a single terse inner-loop / contract-vs-logic-seam hard-limit line that POINTS to workflow 04 / example-mapping.md (does not restate the loop)"
    - "qe-e2e.md carries a single terse outer-loop / acceptance-contract line (QE owns the outer loop)"
    - "AGENTS.md carries a minimal acceptance/BDD command slot (one line, value UNKNOWN - verify) under the byte budget; runner names live only in a comment/workflow, never as a hard command"
    - "All foundation guards stay GREEN after the role edits — guard_role_size (FAIL ceilings: software-engineer 3307B, qe-e2e 3224B), guard_voice, guard_caveman_preserved, guard_agents_bytes, guard_adapter_size"
  artifacts:
    - path: "agent-factory/roles/software-engineer.md"
      provides: "terse inner-loop + seam pointer line"
      contains: "seam"
    - path: "agent-factory/roles/qe-e2e.md"
      provides: "terse outer-loop / acceptance-contract pointer line"
      contains: "acceptance"
    - path: "AGENTS.md"
      provides: "minimal acceptance/BDD command slot under the byte budget"
      contains: "UNKNOWN - verify"
  key_links:
    - from: "agent-factory/roles/software-engineer.md"
      to: "agent-factory/workflows/04-ticket-to-pr.md or agent-factory/checklists/example-mapping.md"
      via: "single pointer line (reference-not-embed; the worked example lives in the hub, not the role)"
      pattern: "(workflow 04|04-ticket-to-pr|example-mapping)"
---

<objective>
Add the role-prompt guardrail lines (the engineer's inner-loop + seam, the QE's outer-loop ownership) and the AGENTS.md acceptance command slot — the "the role enforces" half of D-08 and the runnable command slot of D-12. These are SINGLE terse pointer lines: the role files are the phase's top execution risk (`software-engineer.md` ~2 B below the WARN ceiling, `qe-e2e.md` ~17 B below), so the worked seam example and the double-loop detail stay in the workflow/hub, never in a role. The role lines POINT; they do not restate.

Purpose: "The workflow routes, the role enforces" (D-08) — workflow 04 carries the sequence (plan 12-03); the roles carry the one-line guardrail that names the owner and points to the detail. The AGENTS.md slot makes executable-or-absent actually runnable (D-12) without embedding per-stack config.
Output: Two role files each with one sharp new line + AGENTS.md with one acceptance command slot, all with foundation guards GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md
@.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md
@agent-factory/roles/software-engineer.md
@agent-factory/roles/qe-e2e.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the terse inner-loop + seam pointer line to software-engineer.md (WATCH byte ceiling ~2B)</name>
  <files>agent-factory/roles/software-engineer.md</files>
  <read_first>
    - `agent-factory/roles/software-engineer.md` — current 3128 B (measure with `wc -c` BEFORE editing). `## Caveman prompt` block (lines 10-17 — keep intact, ≥2 `^You` lines / a grug idiom for `guard_caveman_preserved`); `## Responsibilities` line 30 (terse pointer-shaped style to mirror); `## Hard limits` lines 44-46 (the clear-voice no-fabrication register). FAIL ceiling 3307 B, WARN 3130 B → ~2 B below WARN.
    - `.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md` § "software-engineer.md" + § "Shared Patterns → Byte ceiling" and "→ Two-voice + the ## Caveman prompt block" — add the FEWEST sharpest words; pointer line not restatement; worked example lives in the hub.
    - `.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md` D-07 (engineer owns the inner loop), D-08 (terse hard-limit lines in the role), D-09 (contract-vs-logic seam — the worked example does NOT live in the role).
    - `.planning/phases/12-bdd-tdd-wiring/12-VALIDATION.md` § Wave 0 (confirm `guard_role_size` headroom BEFORE writing the line).
  </read_first>
  <action>
    Run `wc -c agent-factory/roles/software-engineer.md` first to confirm the starting byte count (~3128 B; FAIL at 3307 B). Add ONE terse line (caveman-OK in the body, clear voice in any safety clause) to `## Responsibilities` or `## Hard limits` that: names the engineer as owner of the inner red-green loop (write a failing unit test → minimal code → green → refactor) AND states the contract-vs-logic seam in pointer form — the unit layer asserts the logic/edge cases beneath the acceptance scenario, never re-asserting the observable outcome — and POINTS to `agent-factory/workflows/04-ticket-to-pr.md` and/or `agent-factory/checklists/example-mapping.md` for the sequence + worked example. Do NOT restate the double-loop or paste the discount worked example here (no headroom). If a single line covering both inner-loop + seam would trip the FAIL ceiling, prefer the seam guardrail (D-09) as the one line and let workflow 04 (plan 12-03) carry the sequence. After editing, run `sh scripts/check-foundation-guards.sh` and confirm exit 0 (WARN is tolerable and does not fail the build; FAIL means trim — move detail to the workflow and leave only a pointer). Keep the `## Caveman prompt` block untouched.
  </action>
  <verify>
    <automated>grep -qi 'seam\|one .*layer\|observable' agent-factory/roles/software-engineer.md && sh scripts/check-foundation-guards.sh >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi 'seam\|one .*layer\|observable' agent-factory/roles/software-engineer.md` exits 0 (seam / one-behavior-one-layer guardrail present).
    - `sh scripts/check-foundation-guards.sh` exits 0 — `guard_role_size` for software-engineer.md does NOT print FAIL (must stay under 3307 B; a WARN at >=3130 B is tolerable), `guard_voice` and `guard_caveman_preserved` PASS.
    - The worked example is NOT in the role: `! grep -qi 'discount' agent-factory/roles/software-engineer.md` (the example lives in the hub).
    - `wc -c agent-factory/roles/software-engineer.md` reports < 3307 (under the FAIL ceiling).
  </acceptance_criteria>
  <done>software-engineer.md carries a single terse seam/inner-loop pointer line; all guards GREEN; the worked example stays in the hub.</done>
</task>

<task type="auto">
  <name>Task 2: Add the terse outer-loop / acceptance-contract line to qe-e2e.md (WATCH byte ceiling ~17B)</name>
  <files>agent-factory/roles/qe-e2e.md</files>
  <read_first>
    - `agent-factory/roles/qe-e2e.md` — current 3034 B (measure with `wc -c` BEFORE editing). `## Caveman prompt` block (lines 10-17 — keep intact); `## Reads` line 20 (the dial-read parenthetical to mirror); `## Hard limits` line 44 (the terse hard-limit + clear-voice no-fabrication register). FAIL ceiling 3224 B, WARN 3051 B → ~17 B below WARN.
    - `agent-factory/roles/software-engineer.md` — mirror its structure for the line.
    - `.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md` § "qe-e2e.md" + § "Shared Patterns → Byte ceiling".
    - `.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md` D-07 (QE owns the outer acceptance loop), D-08 (terse hard-limit line in the role).
  </read_first>
  <action>
    Run `wc -c agent-factory/roles/qe-e2e.md` first (~3034 B; FAIL at 3224 B). Add ONE terse line to `## Responsibilities` or `## Hard limits`: QE owns the outer acceptance loop — the declarative Given/When/Then acceptance scenario is the contract; it stays red until the engineer's inner loop closes it; no second acceptance scenario goes red before the first is green — POINTING to `agent-factory/workflows/04-ticket-to-pr.md` and the QE handoff's `## Acceptance scenarios` block rather than restating the rule. Keep it pointer-shaped (reference-not-embed). After editing, run `sh scripts/check-foundation-guards.sh` and confirm exit 0 (WARN tolerable, FAIL = trim to a pure pointer). Keep the `## Caveman prompt` block untouched.
  </action>
  <verify>
    <automated>grep -qi 'acceptance\|outer' agent-factory/roles/qe-e2e.md && sh scripts/check-foundation-guards.sh >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi 'acceptance\|outer' agent-factory/roles/qe-e2e.md` exits 0 (outer-loop / acceptance-contract line present).
    - `sh scripts/check-foundation-guards.sh` exits 0 — `guard_role_size` for qe-e2e.md does NOT print FAIL (must stay under 3224 B; WARN at >=3051 B tolerable), `guard_voice` and `guard_caveman_preserved` PASS.
    - `wc -c agent-factory/roles/qe-e2e.md` reports < 3224 (under the FAIL ceiling).
  </acceptance_criteria>
  <done>qe-e2e.md carries a single terse outer-loop / acceptance-contract pointer line; all guards GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Add the minimal acceptance/BDD command slot to AGENTS.md (under byte budget)</name>
  <files>AGENTS.md</files>
  <read_first>
    - `AGENTS.md` — current 6051 B; the `## Commands` section with `### Test` (`UNKNOWN - verify` bullets) and `### E2E` slots (lines ~49-72); the governing rule (line 39): "Real commands only … If a command is unknown, ship `UNKNOWN - verify` — never fabricate." `guard_agents_bytes`: WARN 20480 / FAIL 28672 — ample budget.
    - `.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md` § "AGENTS.md" — add a new `### Acceptance` micro-slot OR one extra bullet under `### Test`; value `UNKNOWN - verify`; host runner names go ONLY in a trailing HTML comment or the workflow, never as a hard command (D-12). Do NOT touch the 5 per-tool adapters.
    - `.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md` D-12 (AGENTS.md acceptance slot; runner names only in workflow/checklist).
    - `.planning/phases/12-bdd-tdd-wiring/12-RESEARCH.md` Open Question 1 (slot-name discretion: `### Acceptance` micro-slot vs an extra `### Test` bullet — planner's pick: either; keep it one line).
  </read_first>
  <action>
    Add ONE acceptance/BDD command slot to the `## Commands` section — EITHER a new `### Acceptance` micro-heading with a single bullet, OR an extra bullet under the existing `### Test` block (both satisfy D-12; pick whichever reads cleaner). The value MUST be `UNKNOWN - verify` (host supplies the real command — never fabricate, per the line-39 rule). Host runner names (cucumber-js / behave / `bddgen && playwright test`) may appear ONLY in a trailing HTML comment on that line — never as a hard command here (D-12, avoids per-stack bloat). Do NOT touch any file under the 5 per-tool adapters (`.claude/`, `.gemini/`, etc.) — the slot is single-source in AGENTS.md only.
  </action>
  <verify>
    <automated>grep -qi 'acceptance\|bdd' AGENTS.md && grep -q 'UNKNOWN - verify' AGENTS.md && sh scripts/check-foundation-guards.sh >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi 'acceptance\|bdd' AGENTS.md` exits 0 (acceptance/BDD slot present).
    - The new slot's value is `UNKNOWN - verify` (not a fabricated command): manual confirm the acceptance bullet ends in `UNKNOWN - verify`.
    - `sh scripts/check-foundation-guards.sh` exits 0 — `guard_agents_bytes` does NOT print FAIL (well under 28672 B), `guard_adapter_size` PASS (adapters untouched).
    - No adapter touched: `git diff --name-only` shows no file under `.claude/` or `.gemini/` changed by this task.
  </acceptance_criteria>
  <done>AGENTS.md carries one acceptance/BDD command slot valued `UNKNOWN - verify`, under the byte budget, with no adapter touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan edits markdown role prompts + AGENTS.md in a no-runtime kit. No executable code, input handling, auth/crypto/data/network surface is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-VOICE | Information disclosure (clarity erosion) | the new role-prompt lines | mitigate | Caveman voice leaking into a clear-voice safety clause — or sanding the caveman block to prose — erodes the two-voice discipline. Mitigated mechanically: `guard_voice` (no caveman markers outside the fenced block) and `guard_caveman_preserved` (block keeps ≥2 `^You` lines / a grug idiom) must stay GREEN after every role edit. |
| T-12-05-BLOAT | (kit-integrity) | software-engineer.md / qe-e2e.md byte ceiling | mitigate | A bloated role rewrite would trip `guard_role_size` FAIL and degrade single-source discipline. Mitigated by single terse pointer lines (the worked example + sequence live in the workflow/hub) and a mandatory `sh scripts/check-foundation-guards.sh` GREEN check after each role edit. |
| T-12-05-AGENTSFAB | Repudiation | the AGENTS.md acceptance slot | mitigate | A fabricated acceptance command would assert a runnable path that doesn't exist. Mitigated by the `UNKNOWN - verify` value (the host supplies the real command) per the AGENTS.md no-fabrication rule; runner names are non-binding examples in a comment only. |

> No application-security threats are introduced (RESEARCH § Security Domain). The substantive ASVS audit is Phase 14.
</threat_model>

<verification>
- `sh scripts/check-foundation-guards.sh` exits 0 AFTER every task — `guard_role_size` (no FAIL for software-engineer.md / qe-e2e.md), `guard_voice`, `guard_caveman_preserved`, `guard_agents_bytes`, `guard_adapter_size` all PASS.
- `wc -c` on both role files reports under their FAIL ceilings (3307 / 3224 B).
- Role lines are pointers (no `discount` worked example in any role; no restated double-loop).
- AGENTS.md acceptance slot present, value `UNKNOWN - verify`, adapters untouched.
</verification>

<success_criteria>
The "role enforces" guardrails land as single terse pointer lines (engineer inner-loop + seam; QE outer-loop) with all foundation guards GREEN (byte ceilings respected), and AGENTS.md carries the runnable acceptance command slot (`UNKNOWN - verify`) that makes executable-or-absent actually executable — no adapter touched, single-source held.
</success_criteria>

<output>
Create `.planning/phases/12-bdd-tdd-wiring/12-05-SUMMARY.md` when done.
</output>

---
phase: 24-clean-handoff-removal-traceability-migration
plan: 05
type: execute
wave: 2
depends_on: [24-01, 24-02, 24-03, 24-04]
files_modified:
  - agent-factory/handoffs/architecture-handoff.md
  - agent-factory/handoffs/business-handoff.md
  - agent-factory/handoffs/frontend-handoff.md
  - agent-factory/handoffs/implementation-handoff.md
  - agent-factory/handoffs/implementation-ready-packet.md
  - agent-factory/handoffs/incident-postmortem.md
  - agent-factory/handoffs/product-handoff.md
  - agent-factory/handoffs/qe-handoff.md
  - agent-factory/handoffs/refinement-notes.md
  - agent-factory/handoffs/release-handoff.md
  - agent-factory/handoffs/retro-notes.md
  - agent-factory/handoffs/security-nfr-handoff.md
  - agent-factory/handoffs/sprint-plan.md
  - agent-factory/handoffs/system-handoff.md
  - agent-factory/handoffs/ticket-ready-packet.md
  - agent-factory/handoffs/uat-handoff.md
  - agent-factory/handoffs/universal-handoff.md
  - scripts/check-kit-refs.ts
  - scripts/check-kit-refs.js
  - scripts/check-kit-refs.test.ts
  - scripts/validate-agent-factory.ts
  - scripts/validate-agent-factory.js
  - scripts/check-uat-oracles.ts
  - scripts/check-uat-oracles.js
  - scripts/fixtures/good/agent-factory/handoffs
  - scripts/fixtures/bad-config-no-mode/agent-factory/handoffs
  - scripts/fixtures/bad-plugin-noname/agent-factory/handoffs
  - scripts/fixtures/bad-role-missing-section/agent-factory/handoffs
  - scripts/fixtures/bad-ticket-bad-column/agent-factory/handoffs
  - scripts/fixtures/bad-ticket-mismatch/agent-factory/handoffs
  - scripts/fixtures/bad-workflow-no-commit/agent-factory/handoffs
  - scripts/fixtures/warn-only-no-trace/agent-factory/handoffs
  - agent-factory/catalog.md
autonomous: true
requirements: [MIGR-02]
user_setup: []
must_haves:
  truths:
    - "ONE atomic deletion change (D-14, mirrors the Phase-23 WR-05 D-18 atomic-flip) does ALL of: rm all 17 handoff templates in agent-factory/handoffs/ (incl. frontend-handoff.md, the 17th absent from both frozen lists — Pitfall 1); rm the 8 scripts/fixtures/*/agent-factory/handoffs/ dirs (D-16); flip check-kit-refs.ts Assertion 2; drop FROZEN_HANDOFFS from validate-agent-factory.ts + re-point the trace check; adjust check-uat-oracles.ts; regenerate the catalog clean"
    - "check-kit-refs.ts Assertion 2 is FLIPPED (D-13): the 16-template ALLOW ERE (~lines 69-71) and the two template-dir/placeholder filters are DROPPED; Assertion 2 becomes 'ZERO agent-factory/handoffs/ refs in the SCAN set' — any hit FAILS; the explicit SCAN set (~45-55) is preserved (never a repo-wide grep, token economy)"
    - "The flip provides the backpressure: it could not go green until Plans 24-01 + 24-02 drove the role/workflow/packaging/AGENTS.md SCAN set to zero handoff refs — node scripts/check-kit-refs.js exits 0 only because the rewire is complete (D-12/D-14)"
    - "validate-agent-factory.ts drops the FROZEN_HANDOFFS array (~136-153) + its existence loop (~253-256) AND RE-POINTS (does not delete) the per-ticket trace-completeness check (~434/463-466) at the note-derived plans/traceability.md render — same path, same ticket-id key, only the row source changes (D-04, Pitfall 4)"
    - "check-uat-oracles.ts FROZEN_HANDOFFS (~318, naming implementation-handoff.md / qe-handoff.md in the A3 parity table) is adjusted so the oracle no longer asserts against deleted artifacts — the oracle is NOT retired (A3/DOG-02 equivalence retirement is Phase 26, out of scope) — Pitfall 5"
    - "The regenerated agent-factory/catalog.md references no deleted handoff artifact (A1: generate-catalog.ts has zero handoff refs — verify the regenerated catalog is clean and freshness:catalog exits 0); the 8 fixture-dir deletions move their consuming test expectations in the SAME change (D-16)"
  artifacts:
    - path: "scripts/check-kit-refs.test.ts"
      provides: "NEW — the gate's first test harness (it had none); the D-15 both-direction adversarial proof via spawnSync the committed .js + a hermetic CHECK_ROOT mirror"
      contains: "CHECK_ROOT"
    - path: "scripts/check-kit-refs.ts"
      provides: "Assertion 2 flipped to ZERO agent-factory/handoffs/ refs; ALLOW ERE dropped; explicit SCAN set preserved"
    - path: "scripts/validate-agent-factory.ts"
      provides: "FROZEN_HANDOFFS dropped; trace-completeness check re-pointed at the note-derived render (same path + ticket-id key)"
  key_links:
    - from: "scripts/check-kit-refs.ts"
      to: "agent-factory/roles + agent-factory/workflows + agent-factory/packaging + AGENTS.md"
      via: "the flipped Assertion 2 greps the explicit SCAN set for agent-factory/handoffs/ and FAILS on any hit (backpressure)"
      pattern: "agent-factory/handoffs/"
    - from: "scripts/validate-agent-factory.ts"
      to: "plans/traceability.md"
      via: "the re-pointed trace-completeness check reads the note-derived render keyed by ticket id (trace.includes(id))"
      pattern: "traceability.md"
  prohibitions:
    - "D-15: a planted agent-factory/handoffs/ ref MUST fail the flipped check-kit-refs.js RED (exit 1 naming the stray) when run against the COMMITTED .js — a green suite alone is NOT proof; the adversarial RED-vs-.js reproduction is mandatory before marking done (grugops-safety-invariant-green-suite-insufficient)"
    - "D-04: the validator trace-completeness check MUST NOT vanish — it is re-pointed at the render, never removed; deleting it silently weakens the trace-completeness guarantee (the proof) — Pitfall 4"
    - "D-14: this MUST NOT land as multiple separate changes — it is ONE atomic deletion change; the check-kit-refs flip lands INSIDE it so the flipped gate is the backpressure (the change cannot go green until the Wave-1 rewire is complete)"
    - "check-uat-oracles.ts MUST NOT be retired — only its handoff-filename references are adjusted; A3/DOG-02 equivalence retirement is Phase 26, out of scope (Pitfall 5)"
    - "check-kit-refs.ts MUST NOT switch to a repo-wide grep — the explicit SCAN set is preserved (D-13 token economy)"
---

<objective>
STAGE 2 of the binding two-stage cut-over (D-12): the ONE atomic deletion change (D-14). It cannot
land until Stage 1 is complete — the flipped grep-to-zero gate is the backpressure. This plan
depends on 24-01 + 24-02 (the role/workflow/packaging/AGENTS.md rewire to zero), 24-03 (the
note-derived render the validator trace check re-points at), and 24-04 (which already removed the
install.ts seed mkdir, so this change never touches install.ts).

The atomic change does ALL of: rm all 17 handoff templates (incl. the 17th, frontend-handoff.md,
invisible to both frozen lists — Pitfall 1); rm the 8 fixture handoffs/ dirs (D-16); flip
check-kit-refs.ts Assertion 2 to "ZERO refs" and stand up its FIRST test harness with the D-15
both-direction adversarial proof (the gate is currently untested — Pitfall 2); drop FROZEN_HANDOFFS
from the validator and re-point (not delete) the trace-completeness check at the render (D-04,
Pitfall 4); adjust check-uat-oracles.ts (Pitfall 5); regenerate the catalog clean (A1); move the
fixture test expectations.

Per the terminal safety lesson (grugops-safety-invariant-green-suite-insufficient): a green suite
is NOT proof on this trace/safety surface. The D-15 RED-vs-committed-.js reproduction is mandatory
and runs independently (orchestrator probe + the input-surface code-review — a logic-probe is not a
substitute for the SCAN-input code-review, the Phase-23 lesson).

Output: 17 templates deleted, 8 fixture dirs deleted, check-kit-refs flipped + its new test,
validator + oracle updated, catalog regenerated clean — all in one change proven RED-vs-.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-CONTEXT.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-PATTERNS.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-RESEARCH.md

# The gate being flipped (Assertion 2 ~158-180, ALLOW ERE ~69-71, SCAN set ~45-55, ROOT/CHECK_ROOT ~33-35):
@scripts/check-kit-refs.ts
# The test idiom for the NEW check-kit-refs.test.ts (spawnSync the committed .js, CHECK_ROOT mirror, exit-code/token asserts):
@scripts/now-running-freshness.test.ts
# The validator being edited (FROZEN_HANDOFFS ~136-153, existence loop ~253-256, trace check ~434/463-466):
@scripts/validate-agent-factory.ts
# The oracle being edited (FROZEN_HANDOFFS ~318, parity table ~326-368):
@scripts/check-uat-oracles.ts
# Plan 24-03 output the validator re-points at:
@scripts/trace-render.ts
</context>

<!-- planner-discipline-allow: FROZEN_HANDOFFS -->

<tasks>

<task type="auto">
  <name>Task 1: Stand up scripts/check-kit-refs.test.ts with the D-15 both-direction adversarial proof (do FIRST — the gate is untested)</name>
  <files>scripts/check-kit-refs.test.ts</files>
  <read_first>
    - scripts/check-kit-refs.ts (THE FILE — Assertion 2 ~158-180, ALLOW ERE ~69-71, SCAN set ~45-55, ROOT honoring CHECK_ROOT ~33-35; the gate has NO test today — Pitfall 2)
    - scripts/now-running-freshness.test.ts (THE FILE — the spawnSync-the-committed-.js + mkdtempSync hermetic mirror + exit-code/token assert idiom; GATE_JS resolves the committed .js ~36-38; CHECK_ROOT-style env override)
    - .planning/phases/24-clean-handoff-removal-traceability-migration/24-PATTERNS.md (lines 95-119 — the Phase-24 test cases + the CHECK_ROOT note: the gate already honors a CHECK_ROOT override at ts:33-35, confirm the test uses it)
  </read_first>
  <action>
    Create scripts/check-kit-refs.test.ts (NEW — does not exist; the gate we are flipping is untested, Pitfall 2). Clone the now-running-freshness.test.ts idiom: drive the COMMITTED scripts/check-kit-refs.js via spawnSync (never the .ts), build a hermetic mirror via mkdtempSync, set CHECK_ROOT to the mirror (check-kit-refs.ts already honors process.env.CHECK_ROOT at ~33-35 — use it; no new wiring needed). Write the D-15 both-direction cases: (a) GREEN — a clean mirror whose SCAN-set files contain no agent-factory/handoffs/ ref → exit 0; (b) RED — plant an agent-factory/handoffs/anything.md reference into a SCAN-set file in the mirror → exit 1, stdout names the stray ref; (c) BACKPRESSURE — with the flip applied but a handoff ref still present (rewire incomplete), the gate runs RED against the committed .js (proves the change can't go green prematurely). This test must be authored BEFORE the flip so it RED-proves the flip against the committed .js. Clear professional voice. Commit the test first.
  </action>
  <verify>
    <automated>test -f scripts/check-kit-refs.test.ts && grep -q "CHECK_ROOT" scripts/check-kit-refs.test.ts && grep -q "agent-factory/handoffs/" scripts/check-kit-refs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/check-kit-refs.test.ts exists and drives the committed scripts/check-kit-refs.js via spawnSync (not the .ts)
    - It sets CHECK_ROOT to a hermetic mkdtempSync mirror (reusing the existing ts:33-35 override)
    - It has a RED case planting agent-factory/handoffs/anything.md → asserts exit 1 + the stray named in stdout
    - It has a GREEN case (clean mirror) → asserts exit 0
    - `grep -q "agent-factory/handoffs/" scripts/check-kit-refs.test.ts` succeeds (the planted-ref fixture)
  </acceptance_criteria>
  <done>The gate has its first test; the D-15 both-direction (RED planted-ref / GREEN clean / backpressure) cases are authored against the committed .js.</done>
</task>

<task type="auto">
  <name>Task 2: The atomic deletion + flip — rm 17 templates + 8 fixture dirs, flip check-kit-refs, drop FROZEN_HANDOFFS + re-point trace check, adjust oracle, regen catalog</name>
  <files>agent-factory/handoffs/ (all 17 .md — deleted), scripts/fixtures/*/agent-factory/handoffs/ (8 dirs — deleted), scripts/check-kit-refs.ts, scripts/check-kit-refs.js, scripts/validate-agent-factory.ts, scripts/validate-agent-factory.js, scripts/check-uat-oracles.ts, scripts/check-uat-oracles.js, agent-factory/catalog.md</files>
  <read_first>
    - scripts/check-kit-refs.ts (THE FILE — drop the ALLOW ERE ~69-71 + the template-dir/placeholder filters; Assertion 2 ~158-180 becomes ZERO refs; keep SCAN set ~45-55)
    - scripts/validate-agent-factory.ts (THE FILE — drop FROZEN_HANDOFFS ~136-153 + existence loop ~253-256; re-point the trace check ~434/463-466, do NOT delete it — Pitfall 4)
    - scripts/check-uat-oracles.ts (THE FILE — FROZEN_HANDOFFS ~318 names implementation-handoff.md/qe-handoff.md; adjust so the parity table ~326-368 no longer asserts deleted files; do NOT retire the oracle — Pitfall 5)
    - scripts/check-kit-refs.test.ts (Task 1 output — the test that must now pass GREEN once the rewire is done)
    - .planning/phases/24-clean-handoff-removal-traceability-migration/24-PATTERNS.md (lines 123-225 — the exact flip + validator + oracle edits) and 24-RESEARCH.md (the 17-template + 8-fixture-dir + A1 catalog facts)
  </read_first>
  <action>
    In ONE atomic change (D-14): (1) rm all 17 files in agent-factory/handoffs/ INCLUDING frontend-handoff.md (the 17th, Pitfall 1). (2) rm the 8 scripts/fixtures/*/agent-factory/handoffs/ dirs (D-16) and update any consuming test expectation (validate-agent-factory.test.ts fixtures lose handoffs/). (3) In check-kit-refs.ts drop the ALLOW ERE (~69-71) and the two template-dir/placeholder filters; Assertion 2 (~158-180) becomes "ZERO agent-factory/handoffs/ refs in the SCAN set" — const stray = grepSubstring(SCAN, "agent-factory/handoffs/").join("\n") with no filters, any hit FAILS; keep the explicit SCAN set (~45-55), never a repo-wide grep (D-13). (4) In validate-agent-factory.ts delete the FROZEN_HANDOFFS array (~136-153) and its existence loop (~253-256), and RE-POINT (do not delete) the per-ticket trace-completeness check (~434/463-466) at the note-derived plans/traceability.md render — the path and the ticket-id key (trace.includes(id)) are unchanged, only the row source is now the render (D-04, Pitfall 4). (5) In check-uat-oracles.ts adjust FROZEN_HANDOFFS (~318) and the parity table so the oracle no longer asserts against deleted handoff filenames; do NOT retire it (Pitfall 5 — Phase 26 owns the equivalence retirement). (6) Regenerate the catalog (npm run generate:catalog) and verify it references no deleted artifact (A1). Rebuild every edited .ts → committed .js (check-kit-refs.js, validate-agent-factory.js, check-uat-oracles.js) so npm run freshness exits 0.
  </action>
  <verify>
    <automated>test $(ls agent-factory/handoffs/ 2>/dev/null | wc -l) -eq 0 && test $(ls -d scripts/fixtures/*/agent-factory/handoffs/ 2>/dev/null | wc -l) -eq 0 && grep -q "FROZEN_HANDOFFS" scripts/validate-agent-factory.ts && echo NOPE || true; test $(grep -c "FROZEN_HANDOFFS" scripts/validate-agent-factory.ts) -eq 0 && grep -q "traceability.md" scripts/validate-agent-factory.ts && npx tsc --outDir .tmp-build && cp .tmp-build/scripts/check-kit-refs.js scripts/check-kit-refs.js && cp .tmp-build/scripts/validate-agent-factory.js scripts/validate-agent-factory.js && cp .tmp-build/scripts/check-uat-oracles.js scripts/check-uat-oracles.js && node scripts/check-kit-refs.js && node scripts/validate-agent-factory.js && npm run generate:catalog && npm run freshness:catalog && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `ls agent-factory/handoffs/` is empty (all 17 templates deleted, incl. frontend-handoff.md — Pitfall 1)
    - `ls -d scripts/fixtures/*/agent-factory/handoffs/` returns nothing (all 8 fixture dirs deleted — D-16)
    - `grep -c "FROZEN_HANDOFFS" scripts/validate-agent-factory.ts` returns 0 AND `grep -q "traceability.md" scripts/validate-agent-factory.ts` succeeds (FROZEN dropped, trace check re-pointed not removed — D-04)
    - `node scripts/check-kit-refs.js` exits 0 (the flipped gate is green ONLY because the Wave-1 rewire drove the SCAN set to zero — backpressure proven)
    - `node scripts/validate-agent-factory.js` exits 0 (no missing-handoff errors; trace check reads the render)
    - `npm run generate:catalog && npm run freshness:catalog` exits 0 (regenerated catalog clean — A1)
    - `npm run freshness` exits 0 (all edited committed .js are faithful tsc builds)
  </acceptance_criteria>
  <done>17 templates + 8 fixture dirs deleted; check-kit-refs flipped to ZERO refs (green via backpressure); FROZEN_HANDOFFS dropped + trace check re-pointed; oracle adjusted; catalog regenerated clean; all .js fresh.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full suite + the D-15 adversarial RED-vs-committed-.js reproduction independently</name>
  <files>scripts/check-kit-refs.test.ts, scripts/check-kit-refs.js</files>
  <read_first>
    - scripts/check-kit-refs.test.ts (Task 1 output — must now pass GREEN)
    - scripts/check-kit-refs.js (Task 2 output — the committed flipped gate the adversarial probe runs against)
    - .planning/phases/24-clean-handoff-removal-traceability-migration/24-VALIDATION.md (lines 35, 71-72 — the D-15 manual-only adversarial reproduction + the green-suite-insufficient discipline)
    - .planning/phases/24-clean-handoff-removal-traceability-migration/24-RESEARCH.md (lines 254-259, 276-279 — the D-15 both-direction proof + sampling rate)
  </read_first>
  <action>
    Run the full non-e2e suite + all four freshness gates + the three gate scripts as the wave-merge sampling. Then perform the D-15 both-direction adversarial proof INDEPENDENTLY against the COMMITTED scripts/check-kit-refs.js (not the .ts, not just the unit suite — a green suite is NOT proof, grugops-safety-invariant-green-suite-insufficient): in a hermetic mirror, plant an agent-factory/handoffs/anything.md reference into a SCAN-set file and confirm node scripts/check-kit-refs.js exits 1 naming the stray (RED); confirm a clean mirror exits 0 (GREEN). Run this as an orchestrator probe AND request an input-surface code-review of the flipped Assertion 2 and the SCAN set — a logic-probe is not a substitute for the SCAN-input code-review (the Phase-23 CR-01 lesson: the guard's INPUT SURFACE is a different blind spot from its LOGIC). Confirm guard_context_writes is still green on the rewired prose. Record the adversarial result honestly (no fabrication — if RED does not reproduce, the flip is broken; do not fake a pass).
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**' && npm run freshness && npm run freshness:context && npm run freshness:queue && npm run freshness:traceability && node scripts/check-kit-refs.js && node scripts/check-foundation-guards.js && node scripts/validate-agent-factory.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --exclude '**/scripts/e2e/**'` is fully green (incl. scripts/check-kit-refs.test.ts, validate-agent-factory.test.ts, install/install.test.ts)
    - All four freshness gates exit 0: `npm run freshness`, `freshness:context`, `freshness:queue`, `freshness:traceability`
    - `node scripts/check-kit-refs.js`, `node scripts/check-foundation-guards.js`, `node scripts/validate-agent-factory.js` all exit 0
    - The independent D-15 adversarial reproduction: a planted agent-factory/handoffs/ ref → `node scripts/check-kit-refs.js` exits 1 naming the stray (RED proven vs the committed .js); a clean kit → exit 0 (GREEN)
    - An input-surface code-review of the flipped Assertion 2 + SCAN set was performed (not only the logic probe) and recorded
  </acceptance_criteria>
  <done>Full suite + all gates green; the D-15 both-direction adversarial proof is reproduced RED-vs-committed-.js independently (probe + code-review), recorded honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kit SCAN set → flipped check-kit-refs grep-to-zero | a surviving handoff ref is the untrusted input the flipped gate must catch RED |
| committed .js → adversarial probe | the proof runs against the committed .js, not the .ts — a logic-only probe misses the input-surface blind spot |
| validator trace check → plans/traceability.md | the re-pointed completeness check is the trace-preservation proof; deleting it would silently weaken it |
| deleted templates → catalog / oracle / fixtures | every consumer of a deleted artifact must be updated in the SAME change or it references a ghost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-FAKE | Repudiation | flipped grep-to-zero gate | mitigate | D-15 both-direction adversarial proof vs the committed .js (planted ref → RED; clean → GREEN); no-fabrication (CLAUDE.md never fake a gate); run independently as probe + input-surface code-review (Phase-23 CR-01 lesson) |
| T-24-05-TRACE | Repudiation | validator trace-completeness check | mitigate | D-04 re-point (not delete) the per-ticket trace row check at the note-derived render; same path + ticket-id key; Pitfall 4 |
| T-24-05-GHOST | Tampering | catalog / oracle / fixtures referencing deleted templates | mitigate | D-14 one atomic change updates every consumer: regen catalog (A1), adjust check-uat-oracles (Pitfall 5), delete + re-expect fixtures (D-16) |
| T-24-05-PARTIAL | Tampering | non-atomic landing | mitigate | D-14 the flip lands INSIDE the deletion change as backpressure — the change cannot go green until the Wave-1 rewire (depends_on 24-01/24-02) is complete |
| T-24-05-SC | Tampering | npm/pip/cargo installs | n/a | no package installs — deletions + existing dev deps only — `[no-install]` |
</threat_model>

<verification>
- `ls agent-factory/handoffs/` empty (17 deleted); `ls -d scripts/fixtures/*/agent-factory/handoffs/` empty (8 deleted)
- `node scripts/check-kit-refs.js` exits 0 (flipped gate green via backpressure — only because Wave 1 rewired to zero)
- `node scripts/validate-agent-factory.js` exits 0; `grep -c "FROZEN_HANDOFFS" scripts/validate-agent-factory.ts` is 0; `grep -q "traceability.md" scripts/validate-agent-factory.ts` (trace check re-pointed, D-04)
- `npm run generate:catalog && npm run freshness:catalog` exits 0 (A1)
- All four freshness gates + the full non-e2e suite green
- D-15: a planted handoff ref makes `node scripts/check-kit-refs.js` exit 1 RED vs the committed .js (independent probe + code-review)
</verification>

<artifacts_this_phase_produces>
New symbols/files/flags created across Phase 24 (the source-grounding pass excludes these from
drift verification — they are net-new, not deviations from a prior artifact):

- `scripts/trace-render.ts` (+ committed `scripts/trace-render.js`, `scripts/trace-render.test.ts`) — Plan 24-03; the deterministic note-refs → `plans/traceability.md` render (D-01).
- `scripts/trace-freshness.ts` (+ committed `scripts/trace-freshness.js`, `scripts/trace-freshness.test.ts`) — Plan 24-03; the fail-closed plans/-rooted trace-render drift gate (D-03).
- `package.json` script `freshness:traceability` — Plan 24-03; wires the standalone trace freshness gate (D-03).
- `scripts/check-kit-refs.test.ts` — Plan 24-05; the gate's FIRST test harness with the D-15 both-direction adversarial proof (the gate was untested — Pitfall 2).
- `plans/traceability.md` is now a GENERATED render (no longer hand-maintained / handoff-fed) — Plan 24-03 (D-01 Option A).
- The `--migrate` handoffs-backup step folded into the EXISTING `install.ts --migrate` orchestration (no new flag — D-17) renaming `plans/handoffs/` → `plans/handoffs.bak.<ISO>` — Plan 24-04 (D-18/D-19/D-20).
- The `CHECK_ROOT` override in `check-kit-refs.ts` already exists (ts:33-35) — confirmed, no new wiring needed (the test reuses it).

Removed this phase (for completeness): all 17 `agent-factory/handoffs/*.md` templates; 8 `scripts/fixtures/*/agent-factory/handoffs/` dirs; `FROZEN_HANDOFFS` in `validate-agent-factory.ts`; the `install.ts` seedState `plans/handoffs/` mkdir; the 16-template ALLOW ERE in `check-kit-refs.ts`.
</artifacts_this_phase_produces>

<success_criteria>
All 17 handoff templates and the 8 fixture handoffs/ dirs are deleted, check-kit-refs.ts Assertion
2 is flipped to ZERO refs (green only via the completed Wave-1 rewire — backpressure), FROZEN_HANDOFFS
is dropped from the validator with the trace-completeness check re-pointed at the note-derived
render, check-uat-oracles is adjusted (not retired), and the catalog is regenerated clean — all in
ONE atomic change, with the D-15 both-direction adversarial proof reproduced RED-vs-committed-.js
independently and recorded honestly.
</success_criteria>

<output>
Create `.planning/phases/24-clean-handoff-removal-traceability-migration/24-05-SUMMARY.md` when done
</output>

---
phase: 24-clean-handoff-removal-traceability-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/trace-render.ts
  - scripts/trace-render.js
  - scripts/trace-render.test.ts
  - scripts/trace-freshness.ts
  - scripts/trace-freshness.js
  - scripts/trace-freshness.test.ts
  - package.json
autonomous: true
requirements: [MIGR-03]
user_setup: []
must_haves:
  truths:
    - "scripts/trace-render.ts deterministically renders plans/traceability.md from note refs — columns Requirement | Code | Tests | UAT | Release keyed by ticket id (filename without extension), cloning the context-io.ts render() shape: GENERATED do-not-hand-edit header, deterministic at-then-id sort, cell() pipe-escaping, single trailing newline, atomicWrite, isMain CLI-entry guard (D-01, D-04, D-06)"
    - "The render rides the task-done consolidation hook Phase 20 deferred to Phase 24 (D-02) — it is the same render-family seam _role-switch-protocol.md step 4 publishes through; notes are the source of truth, traceability.md is the human-facing derived face (D-01 Option A: survives as a render, not hand-maintained, not deleted)"
    - "scripts/trace-freshness.ts is a fail-closed, plans/-rooted render-drift gate cloned from now-running-freshness.ts: regenerate traceability.md into a realpath-resolved temp mirror, byte-compare against the committed file, exit 1 naming the file + STALE on any mismatch OR any non-clean regen; greenfield vacuous pass (no notes / no traceability.md yet) → exit 0 (D-03)"
    - "trace-freshness is wired as its OWN package.json freshness:traceability script — NOT the committed-.js freshness kind (traceability.md is per-repo runtime state in plans/, not committed kit output) and NOT folded into check-foundation-guards.ts (D-03; matches the standalone-gate precedent now-running-freshness / context-freshness / catalog-freshness)"
    - "scripts/trace-render.js and scripts/trace-freshness.js are committed faithful tsc builds of their .ts (npm run freshness exits 0); the render emits ticket ids in its rows so the validator trace.includes(id) re-point in Plan 24-05 stays minimal (A2)"
  artifacts:
    - path: "scripts/trace-render.ts"
      provides: "Deterministic note-refs → plans/traceability.md render (D-01), clear professional voice (trace surface)"
      contains: "GENERATED"
    - path: "scripts/trace-freshness.ts"
      provides: "Fail-closed plans/-rooted trace-render drift gate (D-03), clone of now-running-freshness.ts"
      contains: "STALE"
    - path: "scripts/trace-render.test.ts"
      provides: "Byte-reproducible render assertion + ticket-id presence + deterministic sort"
    - path: "scripts/trace-freshness.test.ts"
      provides: "exit 0 on fresh, exit 1 STALE on edited-without-rerender, greenfield vacuous pass, fail-closed on non-clean regen"
    - path: "package.json"
      provides: "freshness:traceability script"
      contains: "freshness:traceability"
  key_links:
    - from: "scripts/trace-render.ts"
      to: "scripts/context-io.ts"
      via: "reuses readContext / cell() / atomicWrite / isMain; clones the render() deterministic-sort + GENERATED-header shape"
      pattern: "readContext"
    - from: "scripts/trace-freshness.ts"
      to: "scripts/trace-render.ts"
      via: "regenerates plans/traceability.md from notes, byte-diffs against the committed render, fail-closed"
      pattern: "STALE"
  prohibitions:
    - "D-03: the trace-freshness gate MUST NOT report fresh on a regen that did not run cleanly — a non-zero regen NEVER falls through to a fresh exit 0 (fail-closed; the trace is the proof, stale trace = gate FAIL, never a silent pass)"
    - "D-01: trace-render MUST NOT hand-maintain or delete traceability.md — it survives only as a deterministic render of note refs (Option A; rejected B=delete-and-read-from-notes, C=hand-maintained)"
    - "trace-freshness MUST NOT be folded into the committed-.js freshness kind or into check-foundation-guards.ts — traceability.md is plans/ runtime state, so it is the plans/-rooted twin gated by its own freshness:traceability script (D-03)"
---

<objective>
MIGR-03: migrate the requirement→code→test→release trace onto note refs, preserved end-to-end and
never dropped. plans/traceability.md SURVIVES as a deterministic render of note refs (D-01 Option
A) — notes are the source of truth, the file is the human-facing derived face — and is
freshness-gated fail-closed (D-03) so editing notes without re-rendering trips a gate. The trace
is the proof; a stale trace is a gate FAIL, never a silent pass.

This is a pure clone-an-existing-pattern plan: trace-render.ts clones context-io.ts render()
(GENERATED header, deterministic at-then-id sort, cell() pipe-escape, atomicWrite, isMain guard);
trace-freshness.ts clones now-running-freshness.ts (regen→temp-mirror→byte-compare→fail-closed),
re-rooted from .grugops/queue/ to plans/. The render rides the task-done consolidation hook Phase
20 deferred to Phase 24 (D-02) — the same seam Plan 24-01 publishes role output through.

Purpose: this track is independent of the rewire/delete ordering (all-new files, no SCAN-set or
kit overlap) so it runs in Wave 1 alongside 24-01/24-02/24-04. Plan 24-05's validator re-point
(D-04) depends on this render emitting ticket ids.

Output: new scripts/trace-render.{ts,js,test.ts}, new scripts/trace-freshness.{ts,js,test.ts}, a
new package.json freshness:traceability script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-CONTEXT.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-PATTERNS.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-RESEARCH.md

# The exact render template to clone (read render() 913-969, readContext 721, cell 886, atomicWrite 622, noteId 678, isMain 973):
@scripts/context-io.ts
# The exact freshness-gate template to clone (full file: temp mirror, vacuous pass, fail-closed, byte-compare, isMain):
@scripts/now-running-freshness.ts
# The test idiom (spawnSync the committed .js, mkdtempSync mirror, exit-code + token asserts):
@scripts/now-running-freshness.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/trace-render.ts + .test.ts (note refs → plans/traceability.md, D-01/D-04/D-06)</name>
  <files>scripts/trace-render.ts, scripts/trace-render.js, scripts/trace-render.test.ts</files>
  <read_first>
    - scripts/context-io.ts (render() lines 913-969 — the EXACT shape to clone: deterministic sort 919-921, GENERATED header 935, table header + cell()-escaped rows 940-947, trailing "" → single \n 967, atomicWrite 968; reuse readContext 721, cell 886, atomicWrite 622, noteId 678, isMain 973-974)
    - scripts/now-running-freshness.test.ts (THE FILE — the spawnSync-the-committed-.js + mkdtempSync-mirror + exit-code/token assert idiom to clone for trace-render.test.ts)
    - .planning/phases/24-clean-handoff-removal-traceability-migration/24-PATTERNS.md (lines 29-56 — the Phase-24 deltas: columns, ticket-id key, refs source, plans/ write path)
  </read_first>
  <behavior>
    - Test 1: given notes carrying refs (requirement/code/test/release) for ticket ABC-001, render() writes plans/traceability.md with a row keyed by ticket id ABC-001 across columns Requirement | Code | Tests | UAT | Release
    - Test 2: the render is byte-reproducible — two runs over the same notes produce identical bytes (deterministic at-then-id sort, single trailing newline)
    - Test 3: the output carries the GENERATED do-not-hand-edit header naming the re-run command (node scripts/trace-render.js)
    - Test 4: pipe characters in a ref value are cell()-escaped, never breaking the table
    - Test 5: ticket ids appear verbatim in the rendered rows (so the validator trace.includes(id) re-point in Plan 24-05 stays minimal — A2)
    - Test 6: greenfield — no notes yet → an empty-but-headed table (or vacuous), never a crash
  </behavior>
  <action>
    Create scripts/trace-render.ts cloning the context-io.ts render() shape (913-969). Columns become Requirement | Code | Tests | UAT | Release (D-04) keyed by ticket id = filename without extension; source the cells from each note's refs field (D-06), NOT the index.md at/kind/by columns. Write the output to plans/traceability.md (per-repo runtime state in plans/), NOT a .grugops/context/<task>/ path. Reuse readContext to pull notes, cell() for pipe-escaping, atomicWrite for the write, and the isMain CLI-entry guard. Emit the GENERATED header naming node scripts/trace-render.js as the re-run command. Keep the deterministic at-then-id sort and the single trailing newline (trailing "" push). Clear professional voice — this is a trace surface (CLAUDE.md hard rule), no caveman. Create scripts/trace-render.test.ts using the now-running-freshness.test.ts idiom (drive behavior over a hermetic note mirror). Rebuild: npx tsc --outDir .tmp-build, copy the built scripts/trace-render.js into place so the committed .js is a faithful build (npm run freshness exits 0).
  </action>
  <verify>
    <automated>npx tsc --outDir .tmp-build && cp .tmp-build/scripts/trace-render.js scripts/trace-render.js && npx vitest run scripts/trace-render.test.ts --exclude '**/scripts/e2e/**' && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/trace-render.test.ts` passes all 6 behavior cases
    - The rendered plans/traceability.md contains the literal column header `Requirement` and a row keyed by the ticket id
    - The render is byte-identical across two runs over the same notes (deterministic)
    - `grep -c "GENERATED" scripts/trace-render.ts` is at least 1 (the do-not-hand-edit header)
    - `npm run freshness` exits 0 (scripts/trace-render.js is a faithful tsc build of the .ts)
    - The render is clear professional voice (no caveman strings on the trace surface)
  </acceptance_criteria>
  <done>trace-render.ts deterministically renders note refs → plans/traceability.md keyed by ticket id with the 5 columns; tests green; committed .js fresh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/trace-freshness.ts + .test.ts + freshness:traceability package.json script (D-03 fail-closed)</name>
  <files>scripts/trace-freshness.ts, scripts/trace-freshness.js, scripts/trace-freshness.test.ts, package.json</files>
  <read_first>
    - scripts/now-running-freshness.ts (THE FILE to clone almost verbatim, re-rooting .grugops/queue/ → plans/: realpath temp mirror 84, greenfield vacuous pass 94-100, fail-closed-on-nonzero-regen 119-128, byte-compare + STALE exit 150-157, isMain guard 170-175)
    - scripts/now-running-freshness.test.ts (THE FILE — the spawnSync + mkdtempSync mirror + exit-code/token idiom for trace-freshness.test.ts)
    - scripts/trace-render.ts (Task 1 output — the regen source the gate calls)
    - package.json (lines 13-17 — the existing freshness:* script shape: `tsc --outDir .tmp-build && node scripts/<gate>.js`)
  </read_first>
  <behavior>
    - Test 1: committed plans/traceability.md matches a fresh regen from notes → exit 0 (fresh)
    - Test 2: a note edited without re-rendering → exit 1, stdout names the file + STALE
    - Test 3: greenfield — no notes / no plans/traceability.md yet → exit 0 (vacuous pass, honest)
    - Test 4: fail-closed — a regen that does not run cleanly (non-zero) NEVER reports fresh; exit 1
  </behavior>
  <action>
    Create scripts/trace-freshness.ts by cloning now-running-freshness.ts and re-rooting from .grugops/queue/ to plans/: regenerate plans/traceability.md (via trace-render.ts) into a realpathSync(mkdtempSync(...)) temp mirror, byte-compare the committed file against the fresh regen, exit 1 naming the file + the STALE message on any mismatch. Keep the greenfield vacuous pass (no notes / no committed traceability.md → exit 0) and the fail-closed guard: a regen that returns non-zero NEVER falls through to a fresh exit 0 (D-03 — the trace is the proof). Keep the isMain entry guard wrapping every side-effecting statement. Clear professional voice (safety/trace surface). Add a package.json script freshness:traceability = `tsc --outDir .tmp-build && node scripts/trace-freshness.js` (its OWN standalone script — NOT folded into freshness, NOT into check-foundation-guards.ts; matches the now-running-freshness / context-freshness / catalog-freshness standalone precedent, D-03). Create scripts/trace-freshness.test.ts using the now-running-freshness.test.ts idiom. Rebuild and copy scripts/trace-freshness.js so the committed .js is fresh.
  </action>
  <verify>
    <automated>npx tsc --outDir .tmp-build && cp .tmp-build/scripts/trace-freshness.js scripts/trace-freshness.js && npx vitest run scripts/trace-freshness.test.ts --exclude '**/scripts/e2e/**' && npm run freshness:traceability && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/trace-freshness.test.ts` passes all 4 cases (fresh / STALE / vacuous / fail-closed)
    - `npm run freshness:traceability` exits 0 against the current (fresh) committed render
    - `grep -c "freshness:traceability" package.json` is at least 1 and the script invokes `node scripts/trace-freshness.js`
    - `grep -c "STALE" scripts/trace-freshness.ts` is at least 1 (the fail-closed STALE exit)
    - trace-freshness is NOT referenced inside scripts/check-foundation-guards.ts (standalone gate, D-03): `grep -c "trace-freshness" scripts/check-foundation-guards.ts` returns 0
    - `npm run freshness` exits 0 (scripts/trace-freshness.js is a faithful tsc build)
  </acceptance_criteria>
  <done>trace-freshness.ts is a fail-closed plans/-rooted drift gate wired as its own freshness:traceability script; tests green; committed .js fresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notes (source of truth) → traceability.md (derived) | a hand-edit to traceability.md that diverges from the notes is the untrusted drift the freshness gate catches |
| regen subprocess → freshness verdict | a non-clean regen must never be reported as "fresh" (fail-closed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-STALE | Repudiation | plans/traceability.md drift | mitigate | D-03 fail-closed freshness gate: regen→byte-compare→exit 1 STALE; a non-zero regen never reports fresh; the trace is the proof |
| T-24-03-SILENT | Repudiation | trace loss after handoff deletion | mitigate | D-01 traceability.md survives as a render of note refs; the gate proves it tracks the notes; never silently dropped |
| T-24-03-FAB | Repudiation | faked "fresh" verdict | mitigate | no-fabrication (CLAUDE.md): the gate exits 1 on any non-clean regen rather than reporting a fabricated pass |
| T-24-03-SC | Tampering | npm/pip/cargo installs | n/a | no package installs — uses existing {typescript, vitest} dev deps only — `[no-install]` |
</threat_model>

<verification>
- `npx vitest run scripts/trace-render.test.ts scripts/trace-freshness.test.ts --exclude '**/scripts/e2e/**'` all green
- `npm run freshness:traceability` exits 0
- `npm run freshness` exits 0 (both new committed .js are faithful tsc builds)
- `grep -c "trace-freshness" scripts/check-foundation-guards.ts` returns 0 (standalone gate, D-03)
- Render emits ticket ids in its rows (confirmed by Task 1 Test 5 — unblocks the Plan 24-05 validator re-point, A2)
</verification>

<success_criteria>
The requirement→code→test→release trace is carried on note refs and rendered deterministically to
plans/traceability.md (survives, never dropped); the render is byte-reproducible and emits ticket
ids; a stale trace fails closed via the standalone freshness:traceability gate; both new committed
.js are faithful tsc builds.
</success_criteria>

<output>
Create `.planning/phases/24-clean-handoff-removal-traceability-migration/24-03-SUMMARY.md` when done
</output>

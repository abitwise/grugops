---
phase: 24-clean-handoff-removal-traceability-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/roles/_role-switch-protocol.md
  - agent-factory/roles/orchestrator.md
  - agent-factory/roles/software-engineer.md
  - agent-factory/roles/qe-e2e.md
  - agent-factory/roles/uat-planner.md
  - agent-factory/roles/system-analyst.md
  - agent-factory/roles/security-nfr.md
  - agent-factory/roles/release-manager.md
  - agent-factory/roles/frontend-ui.md
  - agent-factory/roles/factory-coach.md
  - agent-factory/roles/compliance-officer.md
  - agent-factory/roles/architect-design.md
  - agent-factory/roles/incident-responder.md
  - agent-factory/roles/ba-pm.md
  - agent-factory/roles/agents-md-scribe.md
  - agent-factory/packaging/adapters.md
  - agent-factory/packaging/slash-command.template.md
  - agent-factory/packaging/subagent.frontmatter.md
  - AGENTS.md
autonomous: true
requirements: [MIGR-01]
user_setup: []
must_haves:
  truths:
    - "Every role Output section that formerly produced a handoff now references Workflow 16 (the read/write/admission protocol) and publishes typed notes — decisions as decision, risks/findings as finding/observation, deliverables as artifact-ref, trace ids on the note refs field (D-05 pull-not-push, D-06 kind mapping)"
    - "_role-switch-protocol.md step 4 'Produce the handoff' is rewritten to 'pull shared context per WF16, do the one job, then publish typed notes per WF16' — it is the task-done consolidation-hook seam Phase 20 deferred to this phase (D-02) and the natural call site where a role records its work output and the trace render fires"
    - "guard_context_writes (WR-01) stays GREEN against the rewritten role prose: every Output section references WF16 by name and never restates a raw .grugops/ write path co-occurring with a write token (D-10)"
    - "The 3 already-clean roles (installer.md, greenfield-mapper.md, brownfield-mapper.md) and the 15 rewritten roles all reach ZERO occurrences of the substring agent-factory/handoffs/ and zero 'plans/handoffs/<id>-<stage>.md' write directives (D-09 uneven-depth: ~12 full Output conversions, the rest mention-removal)"
    - "The 3 packaging templates (adapters.md, slash-command.template.md, subagent.frontmatter.md) and AGENTS.md drop every handoff reference and point at the shared-context substrate / WF16 instead"
    - "frontend-ui.md and its 2 handoff refs are removed so no surviving frontend-handoff reference orphans the 17th template that Stage 2 deletes (D-14 / Pitfall 1)"
  artifacts:
    - path: "agent-factory/roles/_role-switch-protocol.md"
      provides: "The pull-not-push role-run protocol: read context per WF16, do the job, publish typed notes per WF16 — the consolidation-hook seam (D-02)"
      contains: "16-context-read-write"
    - path: "AGENTS.md"
      provides: "Substrate-level role memory description pointing at the shared verified context, no handoff references"
  key_links:
    - from: "agent-factory/roles/_role-switch-protocol.md"
      to: "agent-factory/workflows/16-context-read-write.md"
      via: "step 4 references WF16 for note I/O instead of producing a handoff packet"
      pattern: "16-context-read-write"
    - from: "agent-factory/roles/orchestrator.md"
      to: "agent-factory/workflows/16-context-read-write.md"
      via: "decompose→enqueue owns sequencing; the relay is dead; roles pull context + publish notes per WF16"
      pattern: "16-context"
  prohibitions:
    - "D-07: NO handoff 'Next agent' / 'Next action' directive that names a successor survives in any role — the directional relay is dead; the only residue is an advisory finding/observation that does NOT name who acts (the Orchestrator's decompose picks it up)"
    - "D-08: NO single 'handoff-shaped' mega-note per role-run — a role publishes SEVERAL one-kind-per-file notes (the Phase-20 one-note-one-file-one-kind model); a renamed packet is forbidden"
    - "D-10: NO role prose restates a raw .grugops/ write path co-occurring with a write token (writeFileSync/appendFileSync/Write/>/>>/echo) — that fires guard_context_writes RED; roles reference WF16 and publish via the WF16 protocol, never an inline write path"
    - "D-11: NO transitional dual-write survives — handoff-writing is removed from role Output sections entirely (Phase 23 D-02 dual-running ends here, clean cut)"
---

<objective>
STAGE 1 of the binding two-stage cut-over (D-12), role + packaging surface. Rewire all 18 roles
plus the 3 packaging templates plus AGENTS.md off static handoff packets and onto the shared
verified context: roles PULL the context they need on-demand per Workflow 16 and PUBLISH their
work output as typed notes, instead of pushing a curated packet to a named successor. This is the
restated headline principle (D-05) made concrete in role prose.

The deepest rewrite is _role-switch-protocol.md step 4 ("Produce the handoff") — the task-done
consolidation-hook seam Phase 20 deferred to Phase 24 (D-02). It becomes the call site where a
role records its work output via WF16; the trace render (Plan 24-03) rides this same seam.

Purpose: the kit grep for agent-factory/handoffs/ must reach ZERO across the role + packaging
surface before Stage 2 (Plan 24-05) can delete the templates. This plan supplies the role half of
that grep-to-zero; Plan 24-02 supplies the workflow half. The flipped grep gate in Stage 2 is the
backpressure — it cannot go green until both halves land.

Output: 15 rewritten role files + 3 already-clean roles verified zero + 3 packaging templates +
AGENTS.md, all referencing WF16 / the substrate, with zero handoff references and
guard_context_writes still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-CONTEXT.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-PATTERNS.md

# The reference shape every rewritten Output section adopts (read it — it IS the pattern):
@agent-factory/workflows/16-context-read-write.md
# Already-note-native workflows that demonstrate the "reference WF16, never restate" prose:
@agent-factory/workflows/17-task-claim.md
# The guard this rewrite must keep green:
@scripts/check-foundation-guards.ts
</context>

<!-- planner-discipline-allow: agent-factory/handoffs/ -->
<!-- planner-discipline-allow: frontend-handoff -->
<!-- planner-discipline-allow: handoff -->

<tasks>

<task type="auto">
  <name>Task 1: Rewire _role-switch-protocol.md step 4 + the heaviest 3 roles (orchestrator, software-engineer, qe-e2e)</name>
  <files>agent-factory/roles/_role-switch-protocol.md, agent-factory/roles/orchestrator.md, agent-factory/roles/software-engineer.md, agent-factory/roles/qe-e2e.md</files>
  <read_first>
    - agent-factory/roles/_role-switch-protocol.md (THE FILE — step 4 "Produce the handoff" at lines 30-53, the deepest 11-ref rewrite; the D-02 consolidation-hook seam)
    - agent-factory/roles/orchestrator.md (THE FILE — 7 handoff refs; owns decompose→enqueue sequencing per D-07)
    - agent-factory/roles/software-engineer.md (THE FILE — 5 handoff refs)
    - agent-factory/roles/qe-e2e.md (THE FILE — 3 handoff refs)
    - agent-factory/workflows/16-context-read-write.md (the reference shape: "read before act, do the work, write after verify"; the sanctioned writer is context-io.ts; roles reference, never restate)
    - agent-factory/workflows/17-task-claim.md (lines 9-11, 24, 30 — the verified "references this file rather than restating" prose to clone for role Output sections)
  </read_first>
  <action>
    In _role-switch-protocol.md rewrite step 4 (currently "Produce the handoff (and, when queued, claim + record)" naming the agent-factory/handoffs/ template read + the plans/handoffs/<WORK-ITEM-ID>-<stage>.md write): the role does its one job, then RECORDS its work output as typed notes per Workflow 16 — publish decisions as decision notes, risks/findings as finding/observation, deliverables as artifact-ref, trace ids on the note refs field (D-06). State this is the task-done consolidation point (D-02). Rewrite the surrounding prose at lines 13, 26, 40-53 that frames "the handoff is the only memory" → "the shared verified context (read per WF16) is the role's memory of earlier roles; coordination flows only through the on-disk substrate, never the running conversation." In orchestrator.md remove the 7 handoff refs: the Orchestrator owns sequencing by decompose→enqueue (D-07, Phase 23) and reads role outputs as notes from the shared context per WF16 — no curated packet is consumed or routed. In software-engineer.md (5 refs) and qe-e2e.md (3 refs) convert each Output section from "emit <name>-handoff.md" to "publish typed notes per WF16 (decision/finding/artifact-ref + refs)". Caveman voice stays in the persona prompt blocks; clear voice on any trace/substrate-touching sentence (CLAUDE.md). Reference WF16 by name; NEVER write an inline .grugops/ path next to a write verb (D-10). Do not name a successor role as a directive (D-07); a "this needs security review" line is allowed only as an advisory finding/observation.
  </action>
  <verify>
    <automated>test $(grep -c "agent-factory/handoffs/" agent-factory/roles/_role-switch-protocol.md agent-factory/roles/orchestrator.md agent-factory/roles/software-engineer.md agent-factory/roles/qe-e2e.md | awk -F: '{s+=$2} END {print s}') -eq 0 && npx tsc --outDir .tmp-build && node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "agent-factory/handoffs/"` over the 4 files returns 0 (combined)
    - `grep -c "plans/handoffs/" agent-factory/roles/_role-switch-protocol.md` returns 0
    - `grep -l "16-context-read-write" agent-factory/roles/_role-switch-protocol.md` matches (step 4 references WF16)
    - `node scripts/check-foundation-guards.js` exits 0 (guard_context_writes green — no raw .grugops/ write-path line in the rewritten prose, D-10)
    - No line in any of the 4 files names a successor role as a "Next agent"/"Next action" directive (D-07)
  </acceptance_criteria>
  <done>_role-switch-protocol.md step 4 is the pull-context / publish-notes seam (D-02); orchestrator/software-engineer/qe-e2e Output sections publish typed notes per WF16; zero handoff refs across the 4; guard green.</done>
</task>

<task type="auto">
  <name>Task 2: Mention-remove the remaining 11 roles + verify the 3 already-clean roles</name>
  <files>agent-factory/roles/uat-planner.md, agent-factory/roles/system-analyst.md, agent-factory/roles/security-nfr.md, agent-factory/roles/release-manager.md, agent-factory/roles/frontend-ui.md, agent-factory/roles/factory-coach.md, agent-factory/roles/compliance-officer.md, agent-factory/roles/architect-design.md, agent-factory/roles/incident-responder.md, agent-factory/roles/ba-pm.md, agent-factory/roles/agents-md-scribe.md</files>
  <read_first>
    - agent-factory/roles/uat-planner.md, system-analyst.md, security-nfr.md, release-manager.md, frontend-ui.md, factory-coach.md, compliance-officer.md, architect-design.md (THE FILES — 2 handoff refs each)
    - agent-factory/roles/incident-responder.md, ba-pm.md, agents-md-scribe.md (THE FILES — 1 handoff ref each)
    - agent-factory/roles/installer.md, greenfield-mapper.md, brownfield-mapper.md (THE FILES — already 0 refs; verify-only, no orphan)
    - agent-factory/workflows/16-context-read-write.md (the reference shape) + agent-factory/workflows/17-task-claim.md (lines 9-11 — the "references this file rather than restating" prose)
  </read_first>
  <action>
    For the 11 roles carrying 1-2 handoff refs each, convert each reference: where a role names a handoff template it reads or a handoff instance it writes, replace with "pull the shared context it needs per Workflow 16; publish its work output as typed notes per WF16 (decision/finding/artifact-ref + refs)". frontend-ui.md's 2 refs (including any frontend-handoff mention) MUST be removed so the 17th template Stage 2 deletes is not orphaned (Pitfall 1) — after editing, the kit grep for frontend-handoff over this file is 0. For the heavier-judgment roles (security-nfr.md, architect-design.md, compliance-officer.md, release-manager.md) keep clear professional voice on any compliance/security/trace sentence (CLAUDE.md hard rule); caveman only in the persona block. Confirm installer.md, greenfield-mapper.md, brownfield-mapper.md still carry 0 refs (no change needed; they are listed only to assert no orphan). Emit several one-kind-per-file notes per role-run, never a single renamed mega-packet (D-08). No directive names a successor (D-07).
  </action>
  <verify>
    <automated>test $(grep -rc "agent-factory/handoffs/" agent-factory/roles/ | awk -F: '{s+=$2} END {print s}') -eq 0 && test $(grep -rc "frontend-handoff" agent-factory/roles/ | awk -F: '{s+=$2} END {print s}') -eq 0 && npx tsc --outDir .tmp-build && node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "agent-factory/handoffs/" agent-factory/roles/` sums to 0 across ALL 18 role files
    - `grep -rc "frontend-handoff" agent-factory/roles/` sums to 0 (no orphaned 17th-template ref, Pitfall 1)
    - `grep -rc "plans/handoffs/" agent-factory/roles/` sums to 0
    - `node scripts/check-foundation-guards.js` exits 0 (guard_context_writes green on all rewritten role prose, D-10)
  </acceptance_criteria>
  <done>All 18 roles reach zero handoff references; frontend-handoff has no orphan; guard green.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire the 3 packaging templates + AGENTS.md off handoffs</name>
  <files>agent-factory/packaging/adapters.md, agent-factory/packaging/slash-command.template.md, agent-factory/packaging/subagent.frontmatter.md, AGENTS.md</files>
  <read_first>
    - agent-factory/packaging/adapters.md (THE FILE — 3 handoff refs)
    - agent-factory/packaging/slash-command.template.md (THE FILE — 2 handoff refs)
    - agent-factory/packaging/subagent.frontmatter.md (THE FILE — 4 handoff refs)
    - AGENTS.md (THE FILE — 6 handoff refs; substrate-level role-memory description)
    - agent-factory/workflows/16-context-read-write.md (the reference shape)
    - scripts/check-kit-refs.ts (lines 45-55 SCAN set — these 4 files are all in the SCAN set the Stage-2 grep-to-zero gate scans)
  </read_first>
  <action>
    In each packaging template and AGENTS.md, replace every handoff reference with the shared-context model: a role's inter-role memory is the shared verified context (read/written per Workflow 16), NOT a handoff packet; the Orchestrator sequences by decompose→enqueue (Phase 23). adapters.md (the 5-tool dispatch map) drops its 3 handoff mentions; slash-command.template.md and subagent.frontmatter.md drop their 2 and 4 refs respectively, describing role output as typed notes. AGENTS.md (the minimal high-signal substrate, under the 32 KiB Codex cap — keep it short) reframes its 6 handoff references to "roles pull shared context + publish typed notes per the substrate (WF16)". All 4 files are in the check-kit-refs.ts SCAN set, so they must hit literal zero. Clear professional voice on AGENTS.md (it is the safety/trace-adjacent substrate doc); caveman never appears here. Do NOT name a successor as a directive (D-07); reference WF16, never restate a raw write path (D-10).
  </action>
  <verify>
    <automated>test $(grep -c "agent-factory/handoffs/" agent-factory/packaging/adapters.md agent-factory/packaging/slash-command.template.md agent-factory/packaging/subagent.frontmatter.md AGENTS.md | awk -F: '{s+=$2} END {print s}') -eq 0 && test $(grep -rc "handoff" agent-factory/packaging/ AGENTS.md | awk -F: '{s+=$2} END {print s}') -eq 0 && wc -c AGENTS.md | awk '{exit ($1 < 32768) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "agent-factory/handoffs/"` over the 3 packaging files + AGENTS.md sums to 0
    - `grep -rc "handoff" agent-factory/packaging/ AGENTS.md` sums to 0 (no incidental handoff word survives)
    - `wc -c AGENTS.md` is under 32768 bytes (Codex project_doc_max_bytes cap held)
    - Combined with Tasks 1-2 and Plan 24-02: the full check-kit-refs.ts SCAN set for agent-factory/roles, agent-factory/packaging, AGENTS.md greps to 0 for handoffs (Stage 2 flip will assert this)
  </acceptance_criteria>
  <done>The 3 packaging templates + AGENTS.md carry zero handoff references and describe role memory as the shared context / typed notes via WF16; AGENTS.md stays under the byte cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role prose → guard_context_writes | rewritten Output sections are scanned by the live WR-01 guard; a raw .grugops/ write path in prose is the untrusted token that trips it |
| kit text → check-kit-refs grep-to-zero | the rewired files are the SCAN-set input the Stage-2 flipped gate (Plan 24-05) asserts is handoff-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-RAW | Tampering | rewritten role/packaging prose | mitigate | D-10: reference WF16 by name, never restate a raw .grugops/ write path next to a write token; verified green by `node scripts/check-foundation-guards.js` each task |
| T-24-01-ORPHAN | Repudiation | frontend-ui.md frontend-handoff ref | mitigate | remove the frontend-handoff refs in Stage 1 (Pitfall 1) so the 17th template deleted in Stage 2 leaves no dangling reference that would silently break the trace/relay description |
| T-24-01-RELAY | Repudiation | "Next agent" directives | accept→mitigate | D-07: delete every successor-naming directive; advisory finding/observation (not naming who acts) is the only allowed residue — verified by grep + review, no fabricated relay |
| T-24-01-SC | Tampering | npm/pip/cargo installs | n/a | no package installs in this plan (markdown-only rewrite) — `[no-install]` |
</threat_model>

<verification>
- `grep -rc "agent-factory/handoffs/" agent-factory/roles/ agent-factory/packaging/ AGENTS.md` sums to 0
- `grep -rc "frontend-handoff" agent-factory/roles/` sums to 0 (Pitfall 1)
- `npx tsc --outDir .tmp-build && node scripts/check-foundation-guards.js` exits 0 (guard_context_writes green, D-10)
- `wc -c AGENTS.md` under 32768
- Wave merge: `node scripts/check-kit-refs.js` still passes (it does NOT yet require zero — the flip is Stage 2; this plan must not regress the existing ALLOW-list pass)
</verification>

<success_criteria>
All 18 roles + 3 packaging templates + AGENTS.md reference Workflow 16 / the shared verified
context and publish typed notes, with zero agent-factory/handoffs/ references and zero
plans/handoffs/ write directives; guard_context_writes is green; AGENTS.md stays under the Codex
byte cap; no successor-naming relay directive and no frontend-handoff orphan survives.
</success_criteria>

<output>
Create `.planning/phases/24-clean-handoff-removal-traceability-migration/24-01-SUMMARY.md` when done
</output>

---
phase: 19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - scripts/e2e/uat-live.test.ts
  - package.json
  - agent-factory/workflows/05-pr-quality-gate.md
  - docs/dogfood-human-runbook.md
autonomous: true
requirements: [UAT-AUTO-02, UAT-AUTO-03, UAT-AUTO-05]
must_haves:
  truths:
    - "`npm run test:e2e` ALWAYS runs the `claude auth status` present-and-authed probe first; when the CLI is absent or unauthed it emits a LOUD, distinctly-marked SKIP and NEVER reports a silent green"
    - "When present+authed, the harness exercises A1 (plugin-cache pointer resolution), A2-live (SAFE-02 deny without setting the approval var), and A3-live (dual-path parity) against the real `claude` CLI"
    - "The harness NEVER sets or exports GRUGOPS_PROD_DEPLOY_APPROVED and NEVER runs a real deploy (uses a harmless matched probe)"
    - "The harness scopes its plugin/marketplace install to a throwaway temp repo and cleans up (uninstall + marketplace remove + rmSync) so the developer's real claude config is not polluted"
    - "The §14 gate references the new Tier-1 and Tier-2 lanes single-source in 05-pr-quality-gate.md, in the reference-don't-restate style, config-dialed; the dogfood runbook names the three lanes and marks which is authoritative vs advisory/human"
  artifacts:
    - path: "scripts/e2e/uat-live.test.ts"
      provides: "Gated headless Tier-2 E2E harness with the claude-present-and-authed loud-skip probe"
      contains: "auth"
    - path: "package.json"
      provides: "test:e2e script kept OUT of the default `test` green path"
      contains: "test:e2e"
    - path: "agent-factory/workflows/05-pr-quality-gate.md"
      provides: "Single-source §14 gate reference to the two new lanes (config-dialed)"
    - path: "docs/dogfood-human-runbook.md"
      provides: "Three-lane documentation (Tier-1 oracles / Tier-2 E2E / Tier-3 human) marking authoritative vs advisory"
  key_links:
    - from: "scripts/e2e/uat-live.test.ts"
      to: "claude CLI"
      via: "spawnSync claude auth status probe + claude -p print mode"
      pattern: "spawnSync\\(\\s*[\"']claude"
    - from: "agent-factory/workflows/05-pr-quality-gate.md"
      to: "scripts/check-uat-oracles.js + scripts/e2e/uat-live.test.ts"
      via: "referenced gate steps (never restated)"
      pattern: "check-uat-oracles|test:e2e"
---

<objective>
Build the Tier-2 headless E2E lane and wire both lanes into the build + the single-source §14 gate + the runbook. `scripts/e2e/uat-live.test.ts` drives the real `claude` CLI through A1 (plugin-cache pointer resolution / D-31), A2-live (SAFE-02 live deny / V14), and A3-live (DOG-02 dual-path parity) — but ONLY when a `claude auth status` probe confirms the CLI is present AND authed. When absent/unauthed it emits a LOUD SKIP (distinct marker + `console.warn`) and never flips a UAT or reports a silent green. A new `test:e2e` npm script keeps this lane OUT of the default CI green path (no API-key/secret requirement). The §14 gate references both lanes single-source; the dogfood runbook names the three lanes and which is authoritative.

Purpose: deliver the honest live-runtime probe (the keystone honesty mechanic) and complete the wiring/docs half of the harness, so Plan 03 can resolve the pending UATs from real-run output.
Output: `scripts/e2e/uat-live.test.ts` + `package.json` `test:e2e` script + gate reference in `05-pr-quality-gate.md` + three-lane docs in `dogfood-human-runbook.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2/19-CONTEXT.md
@.planning/phases/19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2/19-PATTERNS.md
@.planning/phases/19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2/19-RESEARCH.md
@.planning/phases/19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2/19-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gated headless E2E harness (uat-live.test.ts) + test:e2e script</name>
  <files>scripts/e2e/uat-live.test.ts, package.json</files>
  <read_first>
    - 19-RESEARCH.md §A (Code Examples) "The auth/availability probe" — the VERIFIED `claudePresentAndAuthed()` shape: `command -v claude` then `claude auth status --json` (exit 0 = logged in / 1 = not), `JSON.parse(...).loggedIn === true`, fail-closed catch. `claude config list` HUNG — do NOT use it as a probe.
    - 19-RESEARCH.md §A1/Tier-2 headless invocation — verified `claude -p "..." --output-format json`, `claude plugin marketplace add <abs-path> --scope local`, `claude plugin install grugops@grugops --scope local`, cleanup `uninstall`/`marketplace remove`; Open Q1 (probe BOTH `claude -p "/grugops:plan …"` AND the `--plugin-dir ./` fallback; assert markers-present-AND-path-error-absent).
    - hooks/guard.test.ts:33-57 — Building block 1: the `spawnSync("node",[...],{input,encoding:"utf8",env:{...process.env,...}})` child-CLI shape (the harness spawns `claude` the same way, arg array, NEVER shell:true on the data path)
    - scripts/catalog-freshness.ts:36-49 — Building block 2: the `mkdtempSync(join(tmpdir(),"grugops-uat-e2e-"))` + `cpSync(agent-factory)` hermetic scaffold + `rmSync` cleanup idiom
    - scripts/catalog-freshness.ts:79-88 — the fail-closed "refuse to report fresh on a non-clean run" precedent (apply: never fall through to a positive verdict on an inconclusive run)
    - hooks/guard.ts:34,132 — A2-live frozen deny string `"Production deploy blocked: humans decide, agents execute."` + env var `GRUGOPS_PROD_DEPLOY_APPROVED` (the harness asserts the deny BECAUSE the var is absent)
    - examples/03-ticket-to-pr.md:169-177 — A3-live frozen handoff filenames + verdict to diff between dispatch paths
    - package.json:8-15 — the `scripts` block shape; add `test:e2e` mirroring `vitest run <dir>`; add NO devDependency (devDeps stay `{typescript, vitest, @types/node}` only, package.json:16-20)
    - vitest.config.ts — `fileParallelism:false`, `globals:false` (import test fns explicitly)
  </read_first>
  <action>
    Create `scripts/e2e/uat-live.test.ts` as a Vitest harness composing the three building blocks. Import test fns explicitly (globals:false).

    Build the honesty gate FIRST: a `claudePresentAndAuthed()` helper cloned from 19-RESEARCH §A — `spawnSync("command",["-v","claude"],{shell:true})` for presence, then `spawnSync("claude",["auth","status","--json"],{encoding:"utf8"})`, return `JSON.parse(auth.stdout)?.loggedIn === true` with a fail-closed `catch { return false }`. When the probe is false, emit a DISTINCT loud marker — `console.warn("SKIPPED: claude CLI absent or unauthed — UAT A1/A2/A3 NOT exercised; status stays pending")` — and skip the live assertions WITHOUT reporting them as green. Use the probe to gate the live `it()` cases so a silent green is impossible (a bare `it.skip` that reads as a pass is the forbidden fabrication — Pitfall 2). This harness MUST NEVER flip a UAT file (that is Plan 03, real-run only).

    A1 case (D-31): `mkdtempSync(join(tmpdir(),"grugops-uat-e2e-"))`, `cpSync(agent-factory)` + AGENTS.md in; run `claude plugin marketplace add <abs-temp-or-repo-path> --scope local` + `claude plugin install grugops@grugops --scope local`; run `claude -p "/grugops:plan add a GET /version endpoint" --output-format json`. Assert planning/Orchestrator markers PRESENT and the path-error substring ABSENT. Per Open Q1, also try the `claude --plugin-dir ./ -p "<request>"` fallback and lock whichever fires; on neither, fail/inconclusive-loud (never a silent pass). `afterAll`: `claude plugin uninstall grugops` + `claude plugin marketplace remove grugops` + `rmSync(tmp)` (Pitfall 3 — never pollute the dev's real config).

    A2-live case (SAFE-02 / V14): with NO `GRUGOPS_PROD_DEPLOY_APPROVED` set, drive `claude -p "run this exact command: helm upgrade fake ./nope"` (a harmless matched probe — NEVER `kubectl apply` against a live cluster, Pitfall 4) and assert the clear-voice deny string `"Production deploy blocked: humans decide, agents execute."` appears. The harness MUST NEVER set/export the approval var (V14, the self-approve keystone).

    A3-live case (DOG-02): drive the SAME ticket (ABC-001) through the sequential path and the `agent:` sub-agent dispatch path headlessly; diff the produced handoff filenames (`implementation-handoff.md`, `qe-handoff.md`) and the gate verdict (`READY_FOR_HUMAN_REVIEW`) between the two — assert they match (only dispatch differs).

    Edit `package.json`: add `"test:e2e": "vitest run scripts/e2e"` to the `scripts` block (keep it OUT of the default `"test"` so CI stays green-without-a-key). Add NO new devDependency.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the new harness typechecks).
    - `npm run test:e2e` exits 0 in BOTH states: when `claude` is absent/unauthed it prints the distinct `SKIPPED: claude CLI absent or unauthed` marker via console.warn and does not report a live-assertion green; when present+authed it runs A1/A2-live/A3-live. (On the CI/unauthed path the suite is green ONLY because the live cases loud-skip — never because they silently passed.)
    - `grep -n 'auth.*status\|loggedIn' scripts/e2e/uat-live.test.ts` shows the `claude auth status` probe is the gate (not an `ANTHROPIC_API_KEY` check, not `claude config list`).
    - `grep -c 'GRUGOPS_PROD_DEPLOY_APPROVED' scripts/e2e/uat-live.test.ts` confirms the var is only ever referenced as never-set (no assignment/`env:` injection of it anywhere).
    - The A2-live probe uses a harmless matched command (`helm upgrade fake ./nope` or equivalent), never `kubectl apply` against a real context.
    - An `afterAll` runs `claude plugin uninstall` + `marketplace remove` + `rmSync(tmp)` (`grep -n 'uninstall\|marketplace remove\|rmSync' scripts/e2e/uat-live.test.ts`).
    - `package.json` gains `"test:e2e": "vitest run scripts/e2e"`; `git diff package.json` shows NO `devDependencies` change.
    - `scripts/e2e/uat-live.test.ts` is excluded from tsc emit (a `*.test.ts`) — `npm run build` produces no `scripts/e2e/uat-live.test.js`.
  </acceptance_criteria>
  <done>The harness gates every live assertion behind the `claude auth status` probe, loud-skips when unauthed (never silent green), never sets the approval var or runs a real deploy, cleans up its install, and the `test:e2e` script stays out of the default green path with no new dep.</done>
</task>

<task type="auto">
  <name>Task 2: Single-source §14 gate reference + three-lane runbook docs</name>
  <files>agent-factory/workflows/05-pr-quality-gate.md, docs/dogfood-human-runbook.md</files>
  <read_first>
    - agent-factory/workflows/05-pr-quality-gate.md:31-37 — the gate sequence + the Test-integrity bullet at :37 (the canonical reference-don't-restate model: invokes the materialized routine, branches on exit code, records `UNKNOWN - verify` rather than faking). Add the new lanes in THIS same style; do NOT fork gate logic (D-26 single-source).
    - docs/dogfood-human-runbook.md:1-55 — the existing clear-voice header + "Safety constraint" + Check 1 structure
    - docs/dogfood-human-runbook.md Check 1/2/3 + Step 4 (the manual procedures the Tier-2 harness now automates step-for-step) — extend in place, do not rewrite
    - 19-PATTERNS.md "docs/dogfood-human-runbook.md (edit)" + "05-pr-quality-gate.md (edit)" sections — exact anchors and the reference-don't-restate rule
    - 19-CONTEXT.md <decisions> Voice + Config dial — clear (non-caveman) voice; reuse `quality.*` enum shape; the E2E lane self-skips on unauth so it likely needs NO new dial key
  </read_first>
  <action>
    In `agent-factory/workflows/05-pr-quality-gate.md`, add the two new lanes as REFERENCED, config-dialed gate steps in the same reference-don't-restate style as the Test-integrity bullet at :37 (single-source, D-26 — these changes land HERE ONLY; never duplicate into workflows 14/15). For Tier-1: reference `node scripts/check-uat-oracles.js` as a deterministic build-gate oracle that exits 0/1 (`ALL CHECKS PASSED` / `N CHECK(S) FAILED`); it runs alongside the other deterministic checks. For Tier-2: reference `npm run test:e2e` as a gated lane that ALWAYS runs the `claude auth status` probe first and emits a LOUD SKIP (never a silent green, never a CI secret) when the CLI is absent/unauthed — note it is dev/CI-only, not a host runtime dependency, and that a skip is NOT a pass (the UAT stays pending). Dial: reuse an existing `quality.*` key/pattern; only add a new dial key if a lane genuinely needs one (the E2E lane self-skips on unauth, so document it as needing none unless justified). Keep clear professional voice.

    In `docs/dogfood-human-runbook.md`, extend in place (clear/non-caveman voice — safety surface). Add a section that NAMES the three lanes: Tier-1 deterministic oracles (`scripts/check-uat-oracles.js`), Tier-2 headless E2E (`npm run test:e2e`), Tier-3 human persona/prose judgment (`11-HUMAN-UAT.md` scenarios 1 & 2). State explicitly which is AUTHORITATIVE (Tier-1 + Tier-2 real-run output) vs ADVISORY/human (Tier-3 — never machine-graded). Keep the frozen strings consistent with `examples/03-ticket-to-pr.md` (handoff filenames + `READY_FOR_HUMAN_REVIEW`). Note that the existing Check 1/2/3 manual procedures are now automated step-for-step by the Tier-2 harness, and that a UAT status flips ONLY from a real run's captured output (never hand-set).
  </action>
  <verify>
    <automated>grep -c 'check-uat-oracles\|test:e2e' agent-factory/workflows/05-pr-quality-gate.md && grep -ci 'tier-1\|tier-2\|tier-3\|authoritative' docs/dogfood-human-runbook.md && node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - `agent-factory/workflows/05-pr-quality-gate.md` references both `scripts/check-uat-oracles.js` and `npm run test:e2e` (`grep -c 'check-uat-oracles\|test:e2e'` ≥ 2), added in the reference-don't-restate style (no oracle/harness logic restated inline — `grep -c 'oracleWr05Wording\|claudePresentAndAuthed' agent-factory/workflows/05-pr-quality-gate.md` == 0).
    - The gate text states the Tier-2 lane loud-skips when the CLI is absent/unauthed and that a skip is NOT a pass; it does not require a CI secret/API key.
    - No gate logic forks into workflows 14/15: `grep -l 'check-uat-oracles' agent-factory/workflows/14-*.md agent-factory/workflows/15-*.md 2>/dev/null` returns nothing.
    - `docs/dogfood-human-runbook.md` names all three lanes and marks Tier-1/Tier-2 authoritative vs Tier-3 advisory/human (`grep -ci 'authoritative\|advisory'` ≥ 1; all three tier names present).
    - The runbook edits are clear professional voice — `node scripts/check-foundation-guards.js` exits 0 (guard_voice over the safety surfaces stays green; the runbook is not a role file but the edit introduces no caveman idiom into any guarded surface).
    - `node scripts/check-foundation-guards.js` exits 0 (no foundation-guard regression from the edits).
  </acceptance_criteria>
  <done>Both lanes are referenced single-source in the §14 gate (config-dialed, reference-don't-restate, no fork), and the runbook names the three lanes with explicit authoritative-vs-advisory marking, all in clear voice with foundation guards green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → external `claude` CLI | The Tier-2 harness shells out to the developer's authed `claude`; this is a dev/CI-only external dependency, never a host runtime dep |
| harness → developer's `~/.claude` plugin/marketplace config | The A1 case installs a plugin; it must scope+clean so the real config is not polluted |
| harness → the SAFE-02 deploy boundary | The A2-live case probes a guarded deploy WITHOUT approval; it must assert the DENY and never approve or actually deploy |
| gate doc / runbook → CI | The wiring must not introduce an API-key/secret requirement to make the default build green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | A2-live harness | mitigate | The harness NEVER sets/exports `GRUGOPS_PROD_DEPLOY_APPROVED`; it asserts the clear-voice DENY. The guard also refuses inline self-set (D-33). Humans hold merge/deploy (V14) — the self-approve keystone. |
| T-19-02 | Tampering | A2-live probe actually deploying | mitigate | Use a harmless guaranteed-matched probe (`helm upgrade fake ./nope`) / no kube-context; the only assertion is the PreToolUse DENY firing, not execution (Pitfall 4). |
| T-19-03 | Repudiation / trace integrity | the loud-skip gate | mitigate | A `claude auth status`-gated LOUD SKIP (distinct marker + console.warn) when unauthed; the harness NEVER flips a UAT and NEVER reports a silent green (Pitfall 2). A skip is not a pass. |
| T-19-04 | Tampering | dev plugin/marketplace config pollution | mitigate | `--scope local` install in a throwaway mkdtemp repo + `afterAll` `uninstall`/`marketplace remove`/`rmSync` (Pitfall 3). |
| T-19-05 | Tampering (command injection) | spawning `claude` | mitigate | Spawn with arg arrays (no `shell:true` on the data path), fixed-literal temp paths via `mkdtempSync`, no write path derived from untrusted content (ASVS V5/V12). |
| T-19-SC | Tampering | npm installs | mitigate | This plan installs ZERO packages (stdlib + existing devDeps + the external `claude` CLI prerequisite). No package-legitimacy checkpoint needed. |
</threat_model>

<verification>
- `npx tsc --noEmit` green (harness typechecks).
- `npm run test:e2e` green in the unauthed/CI state via a LOUD, distinct SKIP (never a silent green); green in the authed state via real A1/A2-live/A3-live assertions.
- The harness never sets `GRUGOPS_PROD_DEPLOY_APPROVED`, never runs a real deploy, and cleans up its plugin install.
- `05-pr-quality-gate.md` references both lanes single-source (no fork into 14/15); `dogfood-human-runbook.md` names the three lanes with authoritative-vs-advisory marking, clear voice.
- `node scripts/check-foundation-guards.js` exits 0 (no guard regression). Zero new devDependency.
</verification>

<success_criteria>
A Tier-2 headless E2E harness uses `claude --print` to cover A1 pointer resolution, A2-live hook firing, and A3-live dual-path parity, gated on "CLI present AND authed", asserting on markers/structure not exact LLM prose, and emitting a LOUD SKIP (never a silent green) when unavailable/unauthed (UAT-AUTO-02, UAT-AUTO-04 harness). Both lanes are wired into the §14 gate single-source and `docs/dogfood-human-runbook.md` documents the three lanes in clear voice stating authoritative vs advisory; the `claude`-CLI lane stays dev/CI-only with zero new host runtime dependency and no CI secret requirement (UAT-AUTO-03 docs+wiring half, UAT-AUTO-05).
</success_criteria>

<output>
Create `.planning/phases/19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2/19-02-SUMMARY.md` when done.
</output>

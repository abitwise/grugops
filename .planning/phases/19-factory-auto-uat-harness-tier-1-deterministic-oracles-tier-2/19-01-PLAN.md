---
phase: 19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-uat-oracles.ts
  - scripts/check-uat-oracles.js
  - scripts/check-uat-oracles.test.ts
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - scripts/check-foundation-guards.test.ts
autonomous: true
requirements: [UAT-AUTO-01, UAT-AUTO-03, UAT-AUTO-05]
must_haves:
  truths:
    - "Running `node scripts/check-uat-oracles.js` over the real tree exits 0 and prints ALL CHECKS PASSED"
    - "The B3 wording oracle asserts the three WR-05 closure beats (Phase 8 dropped / Phase 10 guard_wr05 / Phase 11 re-verified) in all four .planning tracking docs and FAILS red if any beat is missing in any file"
    - "The A2 wiring oracle reads hooks/hooks.json, confirms the matcher routes to the committed guard.js, spawns guard.js with a matched kubectl-apply payload, and asserts the deny-JSON — without re-testing guard logic"
    - "The A3 parity oracle asserts both dispatch columns name the same handoff filenames + the same verdict string, and NEVER marks a pending-human cell passed"
    - "Each oracle has both a planted-FAIL-red test and the real-tree-PASS smoke; a missing input file fails red (CR-01), never vacuous-passes"
    - "Running `node scripts/check-foundation-guards.js` exercises all three Tier-1 oracles in its run-all block (LOCKED CONTEXT.md decision; ROADMAP SC3) and exits non-zero when any one Tier-1 oracle is planted to fail — the aggregator fails closed"
  artifacts:
    - path: "scripts/check-uat-oracles.ts"
      provides: "Tier-1 aggregator with three deterministic oracles (B3 wording, A2 wiring, A3 parity)"
      contains: "oracleWr05Wording"
    - path: "scripts/check-uat-oracles.js"
      provides: "Committed tsc build output of the aggregator (freshness-checked)"
    - path: "scripts/check-uat-oracles.test.ts"
      provides: "Plant-and-run Vitest harness proving each oracle PASS and FAIL-red"
      contains: "CHECK_ROOT"
    - path: "scripts/check-foundation-guards.ts"
      provides: "Foundation-guards aggregator run-all block extended to invoke the three Tier-1 oracles (BLOCKER 1 / UAT-AUTO-05)"
      contains: "oracleWr05Wording"
    - path: "scripts/check-foundation-guards.test.ts"
      provides: "Plant case proving the aggregator goes RED when a Tier-1 oracle fails"
  key_links:
    - from: "scripts/check-uat-oracles.ts"
      to: "hooks/guard.js"
      via: "spawnSync child-CLI with a matched PreToolUse payload"
      pattern: "spawnSync.*guard\\.js"
    - from: "scripts/check-uat-oracles.test.ts"
      to: "scripts/check-uat-oracles.js"
      via: "spawnSync the committed .js with CHECK_ROOT pointed at a hermetic mirror"
      pattern: "CHECK_ROOT"
    - from: "scripts/check-foundation-guards.ts"
      to: "scripts/check-uat-oracles.ts"
      via: "run-all block imports + calls oracleWr05Wording/oracleHooksWiring/oracleParity"
      pattern: "oracleWr05Wording|oracleHooksWiring|oracleParity"
---

<objective>
Build the Tier-1 deterministic oracle lane: a standalone `scripts/check-uat-oracles.ts` aggregator carrying three fail-red, never-fabricate oracles (B3 WR-05 wording-consistency, A2 hooks.json→guard wiring, A3 dual-path structural parity), its committed `.js` build output, and a plant-and-run Vitest harness proving each oracle both PASS and FAIL-red.

This is the honest, no-LLM half of the auto-UAT harness — pure greps/diffs/child-process assertions cloned verbatim from in-repo idioms. It resolves the deterministic portions of A2 (SAFE-02 wiring), A3 (DOG-02 structural parity), and B3 (WR-05 wording / `11-HUMAN-UAT.md` scenario 3) without any agent grading its own homework (Constraint #6).

It ALSO wires the three oracles into the foundation-guards aggregator (`scripts/check-foundation-guards.ts`) per the LOCKED CONTEXT.md decision and ROADMAP SC3, so `node scripts/check-foundation-guards.js` runs them and fails closed when any one fails.

Purpose: stand up the deterministic oracles AND fold them into the aggregator so the §14 gate (referenced in Plan 02) and the real-run resolution (Plan 03a/03b) have a trustworthy mechanical floor that the existing build gate exercises.
Output: `scripts/check-uat-oracles.ts` + `scripts/check-uat-oracles.js` + `scripts/check-uat-oracles.test.ts` + the aggregator wiring in `scripts/check-foundation-guards.ts`/`.js` + its plant case in `scripts/check-foundation-guards.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2/19-CONTEXT.md
@.planning/phases/19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2/19-PATTERNS.md
@.planning/phases/19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2/19-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the Tier-1 aggregator with three oracles (check-uat-oracles.ts)</name>
  <files>scripts/check-uat-oracles.ts</files>
  <read_first>
    - scripts/check-foundation-guards.ts:44-86 — clone the aggregator spine VERBATIM: `CHECK_ROOT` override + `abs`/`fileExists`/`readText` helpers + `FAILS`/`pass`/`fail`/`warn` counter (WARN never increments FAILS) + `grepFiles`
    - scripts/check-foundation-guards.ts:135-138 — the CR-01 missing-file-fails-red guard (clone for each oracle that reads a fixed file)
    - scripts/check-foundation-guards.ts:465-483 — the run-all block + the `ALL CHECKS PASSED` / `N CHECK(S) FAILED` exit-code tail (keep the literal `ALL CHECKS PASSED` string; exit 0/1)
    - hooks/guard.test.ts:33-57 — the `spawnSync("node",[GUARD_JS],{input,encoding:"utf8",env})` + `payload(command)` child-CLI idiom the A2 oracle's spawn half clones
    - hooks/hooks.json:5,9 — matcher `"Bash"`; command references `guard.js` (assert it references guard.js, do NOT string-equal the whole `${CLAUDE_PLUGIN_ROOT}` wrapper)
    - hooks/guard.ts:34,132 — frozen strings: env var `GRUGOPS_PROD_DEPLOY_APPROVED`; deny string `"Production deploy blocked: humans decide, agents execute."`; deny-JSON marker `"permissionDecision":"deny"`
    - examples/03-ticket-to-pr.md:169-177 — the two-column parity table; frozen handoff filenames `implementation-handoff.md` / `qe-handoff.md`; frozen verdict `READY_FOR_HUMAN_REVIEW`; the `pending human` cells the oracle must NOT mark passed
    - 19-RESEARCH.md §B3 (Code Examples) — the verified four-file WR05_SCAN set and the three-beat prose wording present in each file (the literal "P8→P10→P11" slug appears NOWHERE; match the SEMANTIC beats)
    - 19-PATTERNS.md Shared Patterns (A2 wiring facts / A3 parity facts / B3 wording facts) — the exact frozen strings + line anchors
  </read_first>
  <action>
    Create `scripts/check-uat-oracles.ts` as a STANDALONE aggregator (mirroring the `catalog-freshness.ts` standalone-not-folded precedent, D-07 — do NOT append calls into `check-foundation-guards.ts`). Clone the `check-foundation-guards.ts:44-86` spine verbatim: the `CHECK_ROOT` env override (load-bearing — it lets the test plant violations into a hermetic mirror), `abs`/`fileExists`/`readText`, the `FAILS` counter with `pass`/`fail`/`warn`, and `grepFiles`. Author findings in CLEAR PROFESSIONAL VOICE (this is a safety/trace surface, CLAUDE.md hard rule — no caveman).

    Implement three oracle functions, then a run-all block calling `oracleWr05Wording(); oracleHooksWiring(); oracleParity();` followed by the `:476-483` exit tail (keep literal `ALL CHECKS PASSED`). EXPORT the three oracle functions (`export function oracleWr05Wording()` / `oracleHooksWiring()` / `oracleParity()`) AND export the `FAILS` accessor (or have each oracle RETURN its fail-count and accumulate into the module `FAILS`) so the foundation-guards aggregator (Task 3) can import and invoke them and observe failures. Each oracle must increment the shared `FAILS` counter on a defect so an importing aggregator inherits the fail signal. Keep the standalone run-all block at the bottom guarded so a direct `node scripts/check-uat-oracles.js` still runs all three and exits 0/1 (this remains its own lane), but importing the module for its functions does not double-run the exit tail — clone the standalone-not-folded precedent (`catalog-freshness.ts`, D-07) while exposing the functions for Task 3's aggregator wiring.

    `oracleWr05Wording` (UAT-AUTO-01): define `WR05_SCAN = [".planning/PROJECT.md", ".planning/STATE.md", ".planning/v1.2-SDLC-COVERAGE-AUDIT.md", ".planning/RETROSPECTIVE.md"]`. For each of the three semantic beats — beat1 "dropped in Phase 8", beat2 "guarded by guard_wr05 in Phase 10", beat3 "re-verified GREEN after Phase 11" — use a tolerant per-beat RegExp and assert via `grepFiles(WR05_SCAN, beatRe).length === WR05_SCAN.length`, i.e. every file carries every beat. Do NOT grep the literal "dropped P8 → guarded P10 → re-verified P11" slug (it appears in no doc — a naive grep false-fails correct docs). Add a missing-file CR-01 guard: if any file in `WR05_SCAN` is absent, `fail()` and name it (never vacuous-PASS). `pass()` only when all four files carry all three beats; `fail()` naming the file+beat on any miss.

    `oracleHooksWiring` (UAT-AUTO-02): read `hooks/hooks.json` (CR-01 fail if absent), `JSON.parse` (try/catch → fail-closed `fail()`, never throw past it — ASVS V5). Assert `hooks.PreToolUse[0].matcher === "Bash"` and that `hooks.PreToolUse[0].hooks[0].command` references `guard.js` (substring/regex on `guard.js`, NOT a string-equal of the `${CLAUDE_PLUGIN_ROOT}` path). Then build a matched payload `payload("kubectl apply -f deploy.yaml")` and `spawnSync("node", [abs("hooks/guard.js")], { input: payload, encoding: "utf8" })`; assert stdout contains `"permissionDecision":"deny"`. This asserts the WIRING CONTRACT only — do NOT add deny/allow/refuse-self-set cases (guard.test.ts covers guard logic 26/26; re-testing is scope creep per the anti-pattern). Never set `GRUGOPS_PROD_DEPLOY_APPROVED` (V14).

    `oracleParity` (UAT-AUTO-03): read `examples/03-ticket-to-pr.md` (CR-01 fail if absent). Assert the parity table names the frozen handoff filenames `implementation-handoff.md` and `qe-handoff.md` and the frozen verdict `READY_FOR_HUMAN_REVIEW` in the structural columns. The oracle must distinguish a "structurally-parity-shaped + `pending human`" cell from a "filled and matching" cell, and must NEVER mark a `pending human` cell as passed (no-fabrication). The STRUCTURAL pass condition is: the frozen filenames + verdict are present and the table is shaped for two dispatch columns; the live fill of the CC-native column comes from Plan 02's Tier-2 run, not here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -v '^//' scripts/check-uat-oracles.ts | grep -c 'oracleWr05Wording\|oracleHooksWiring\|oracleParity'</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the new source typechecks against the existing tsconfig).
    - `scripts/check-uat-oracles.ts` defines exactly three oracle functions `oracleWr05Wording`, `oracleHooksWiring`, `oracleParity` (each EXPORTED for Task 3's aggregator import) and a run-all block that calls all three then the `FAILS === 0` exit tail printing literal `ALL CHECKS PASSED` / exit 0.
    - The three oracle functions are exported and each increments the shared `FAILS` count on a defect (or returns its fail-count) so an importing aggregator inherits the fail signal — `grep -c 'export function oracleWr05Wording\|export function oracleHooksWiring\|export function oracleParity' scripts/check-uat-oracles.ts` == 3.
    - The B3 oracle uses the exact four-file `WR05_SCAN` set and three SEMANTIC beat regexes (verified against 19-RESEARCH §B3 wording) — `grep -n 'PROJECT.md\|STATE.md\|SDLC-COVERAGE-AUDIT\|RETROSPECTIVE' scripts/check-uat-oracles.ts` shows all four; no literal "P8 → guarded P10" slug appears.
    - The A2 oracle spawns `hooks/guard.js` (not the `.ts`) with a `kubectl apply` payload and asserts `"permissionDecision":"deny"`; it does NOT export `GRUGOPS_PROD_DEPLOY_APPROVED` (`grep -c 'GRUGOPS_PROD_DEPLOY_APPROVED' scripts/check-uat-oracles.ts` shows it only ever referenced in a comment/never-set assertion, never assigned).
    - Each oracle has a CR-01 missing-file `fail()` guard for every fixed file it reads (cloned from `check-foundation-guards.ts:135-138`).
    - Findings are clear professional voice (no caveman idiom); `grep -ci '\bgrug\b' scripts/check-uat-oracles.ts` finds only file-path references (`.grugops`, `guard.js`), not caveman voice.
  </acceptance_criteria>
  <done>The aggregator typechecks, defines the three named oracles + run-all + exit tail, asserts the frozen strings via the cloned idioms, never sets the approval var, and fails-red (CR-01) on a missing input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plant-and-run Vitest harness + committed .js + freshness green</name>
  <files>scripts/check-uat-oracles.test.ts, scripts/check-uat-oracles.js</files>
  <read_first>
    - scripts/check-foundation-guards.test.ts:1-45 — the harness header + `ROOT`/`GUARD_JS`/`GUARD_INPUTS` mirror-set declaration (clone the structure; point `GUARD_JS` at `check-uat-oracles.js`, name temp dirs `grugops-uat-`)
    - scripts/check-foundation-guards.test.ts:99-152 (mirror/runIn helpers + plant-one-violation expect-fail-red shape + REAL-tree GREEN smoke) — clone the `mirror()`/`runIn(checkRoot)`/`out(r)`/`afterAll` cleanup idiom and the two case shapes
    - hooks/guard.test.ts:33-57 — for the `-t "wiring"` case if it needs the `runGuard`/`payload` shape
    - scripts/freshness.ts:43 — `OUTPUT_DIRS = ["install","scripts","hooks"]` recurses `scripts/`, so the new `scripts/check-uat-oracles.js` is auto-collected (no freshness wiring change needed)
    - .gitattributes:11 — `scripts/**/*.js text eol=lf` already pins the new `.js` to LF (no new attribute line)
  </read_first>
  <action>
    Create `scripts/check-uat-oracles.test.ts` cloning the `check-foundation-guards.test.ts` `CHECK_ROOT` mirror/plant harness. Set `GUARD_JS = join(ROOT, "scripts", "check-uat-oracles.js")`; name temp dirs with prefix `grugops-uat-`. Declare `GUARD_INPUTS` as the exact repo-relative set the aggregator reads: the four `WR05_SCAN` docs, `hooks/hooks.json`, `hooks/guard.js`, and `examples/03-ticket-to-pr.md`. Clone `mirror()` (mkdtemp + cpSync each input, recursive mkdir of `dirname`), `runIn(checkRoot)` (spawnSync the committed `.js` with `CHECK_ROOT` env), `out(r)`, and the `afterAll` rmSync cleanup.

    For EACH of the three oracles, write a planted-FAIL case AND rely on one shared real-tree GREEN smoke (per the "every oracle needs BOTH a planted-FAIL and a real-tree-PASS" rule). Tag the cases so they are runnable by name: `-t "wording"`, `-t "wiring"`, `-t "parity"`.
    - wording FAIL: mirror, then strip/overwrite one beat (e.g. remove the Phase-10 `guard_wr05` beat) from one scan file in the mirror; assert `status !== 0` AND the finding names the missing beat/file.
    - wiring FAIL: mirror, then mutate the mirrored `hooks/hooks.json` matcher to a non-`"Bash"` value (or point the command away from `guard.js`); assert `status !== 0` AND the finding names the wiring defect. (Do NOT mutate guard.js — that would re-test logic.)
    - parity FAIL: mirror, then remove a frozen handoff filename or the `READY_FOR_HUMAN_REVIEW` verdict from the mirrored `examples/03-ticket-to-pr.md`; assert `status !== 0` AND the finding names the missing string.
    - smoke GREEN: `spawnSync("node",[GUARD_JS])` over the REAL tree (no CHECK_ROOT), assert `status === 0` AND `out(r)` contains `ALL CHECKS PASSED`.

    Then build and commit the `.js`: run `npm run build` (tsc emits `scripts/check-uat-oracles.js` in place, LF, `*.test.ts` excluded from emit so the test produces no committed `.js`). Run `npm run freshness` and confirm it stays green (rebuild-to-temp byte-diff covers the new `scripts/check-uat-oracles.js` automatically).
  </action>
  <verify>
    <automated>npm run build && npx vitest run scripts/check-uat-oracles.test.ts && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` emits `scripts/check-uat-oracles.js` (LF line endings — `file scripts/check-uat-oracles.js` / no CRLF).
    - `npx vitest run scripts/check-uat-oracles.test.ts -t "wording"` exits 0 (the planted missing-beat case proves a RED exit AND the smoke proves GREEN).
    - `npx vitest run scripts/check-uat-oracles.test.ts -t "wiring"` exits 0 (planted matcher/command mutation proves RED; no guard-logic re-test added).
    - `npx vitest run scripts/check-uat-oracles.test.ts -t "parity"` exits 0 (planted missing-filename/verdict proves RED).
    - The full `npx vitest run scripts/check-uat-oracles.test.ts` passes: three planted-FAIL cases each assert `status !== 0` + a named finding, and the real-tree smoke asserts `status === 0` + `ALL CHECKS PASSED`.
    - `npm run freshness` exits 0 — the committed `scripts/check-uat-oracles.js` is byte-identical to a fresh `tsc` rebuild (D-13 drift gate green).
    - `git status` shows `scripts/check-uat-oracles.js` tracked (not gitignored) and no `scripts/check-uat-oracles.test.js` was emitted.
  </acceptance_criteria>
  <done>Each oracle proves BOTH a planted-FAIL-red path and the real-tree-PASS smoke; the committed `.js` is built and freshness-clean; the test file emits no committed `.js`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the three Tier-1 oracles into the foundation-guards aggregator (BLOCKER 1)</name>
  <files>scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js, scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - scripts/check-foundation-guards.ts:462-483 — the run-all block (`guardWr05(); guardAgentsBytes(); … guardRoleSize();`) at :465-471 and the `FAILS === 0 ? ALL CHECKS PASSED/exit 0 : N CHECK(S) FAILED/exit 1` tail at :476-483. This is the hook point (PATTERNS.md `check-foundation-guards.ts:465-471`).
    - scripts/check-foundation-guards.ts:44-72 — the shared `FAILS` counter + `pass/fail/warn`; the imported oracles must feed THIS counter (or the aggregator must fold each oracle's returned fail-count into `FAILS`) so a Tier-1 failure flips the aggregator's exit code.
    - scripts/check-uat-oracles.ts (Task 1) — the three EXPORTED oracle functions `oracleWr05Wording`/`oracleHooksWiring`/`oracleParity` and the exported FAILS accessor / per-oracle return value.
    - scripts/check-foundation-guards.test.ts:1-45 — the harness header + `ROOT`/`GUARD_JS`/`GUARD_INPUTS` declaration; :99-152 — the `mirror()`/`runIn(checkRoot)`/`out(r)` plant-and-run idiom + the plant-one-violation expect-fail-red shape to clone for the new aggregator plant case.
  </read_first>
  <action>
    Edit `scripts/check-foundation-guards.ts` so its run-all block exercises the three Tier-1 oracles (LOCKED CONTEXT.md decision: "Wire both lanes into the foundation-guards aggregator `scripts/check-foundation-guards.ts` AND reference from the §14 gate"; ROADMAP SC3). PREFERRED path: `import { oracleWr05Wording, oracleHooksWiring, oracleParity } from "./check-uat-oracles.js"` (or `.ts` per the project's import convention — match how the aggregator imports its own helpers) and CALL all three inside the run-all block (after `guardRoleSize()` at :471, under a clear-voice `== Phase 19 auto-UAT Tier-1 oracles ==` header), routing their failures into the SAME `FAILS` counter so the existing `:476-483` exit tail goes non-zero when any Tier-1 oracle fails. ACCEPTABLE ALTERNATIVE if a clean function import is not feasible (e.g. the standalone module's run-all auto-executes on import): the run-all block `spawnSync("node", [abs("scripts/check-uat-oracles.js")], { encoding: "utf8" })`, print its stdout, and fold its exit code into `FAILS` (`if (r.status !== 0) FAILS += 1; fail("Tier-1 auto-UAT oracles FAILED");`). EITHER WAY the aggregator MUST fail closed: `node scripts/check-foundation-guards.js` exits non-zero if any one Tier-1 oracle fails. Do NOT duplicate/restate oracle logic in the aggregator — only invoke it (single-source; the oracle bodies live in `check-uat-oracles.ts` only). Preserve the existing six foundation guards and the literal `ALL CHECKS PASSED` string. Author any new prose in clear professional voice (safety/trace surface).

    Extend `scripts/check-foundation-guards.test.ts`: add the three new Tier-1 oracle inputs to `GUARD_INPUTS` (the four `WR05_SCAN` docs, `hooks/hooks.json`, `hooks/guard.js`, `examples/03-ticket-to-pr.md` — only the ones not already mirrored) so the mirror carries them, and add ONE plant case proving the aggregator goes RED when a Tier-1 oracle fails: mirror, then break a single Tier-1 input in the mirror (e.g. strip the Phase-10 `guard_wr05` beat from one `WR05_SCAN` doc, OR remove the `READY_FOR_HUMAN_REVIEW` verdict from `examples/03-ticket-to-pr.md`), run `runIn(mirror)`, and assert `status !== 0` AND `out(r)` names the Tier-1 failure. Keep the existing real-tree GREEN smoke (it now also proves the three oracles pass on the real tree through the aggregator).

    Then `npm run build` (re-emit `scripts/check-foundation-guards.js`, LF) and confirm `npm run freshness` stays green.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-foundation-guards.js && npx vitest run scripts/check-foundation-guards.test.ts && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-foundation-guards.js` exercises all three Tier-1 oracles (`grep -c 'oracleWr05Wording\|oracleHooksWiring\|oracleParity\|check-uat-oracles' scripts/check-foundation-guards.ts` >= 1) and prints a Phase-19 Tier-1 section, then `ALL CHECKS PASSED` / exit 0 on the clean real tree.
    - The aggregator FAILS CLOSED: the new plant case in `scripts/check-foundation-guards.test.ts` mirrors the tree, breaks one Tier-1 input, runs the committed `.js` via `CHECK_ROOT`, and asserts `status !== 0` AND a finding naming the Tier-1 failure — proving `node scripts/check-foundation-guards.js` exits non-zero when any one Tier-1 oracle is planted to fail.
    - No oracle LOGIC is restated in the aggregator (single-source) — `grep -c 'WR05_SCAN\|permissionDecision' scripts/check-foundation-guards.ts` counts only the invocation/fold, not a re-implementation (the oracle bodies stay in `check-uat-oracles.ts`).
    - The existing six foundation guards and the literal `ALL CHECKS PASSED` / `N CHECK(S) FAILED` exit tail are preserved; `npx vitest run scripts/check-foundation-guards.test.ts` is fully green (existing cases + the new Tier-1 plant case).
    - `npm run freshness` exits 0 — the re-emitted `scripts/check-foundation-guards.js` is byte-identical to a fresh `tsc` rebuild (D-13 drift gate green).
  </acceptance_criteria>
  <done>`node scripts/check-foundation-guards.js` runs the three Tier-1 oracles in its run-all block and fails closed (exit non-zero) when any one is planted to fail; the test proves the RED path; the six original guards are intact; freshness stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| oracle script → repo files (.planning docs, hooks.json, examples) | Read-only file input the oracle parses; fixed-literal paths only |
| oracle → spawned `guard.js` child process | The A2 oracle shells out to the committed guard.js with a synthetic PreToolUse payload |
| test harness → hermetic mirror (CHECK_ROOT) | Planted violations live only inside a mkdtemp mirror; nothing in the real tree is mutated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | A2 oracle spawning guard.js | mitigate | The A2 oracle NEVER sets `GRUGOPS_PROD_DEPLOY_APPROVED`; it asserts the DENY-JSON. Spawn uses an arg array (`spawnSync("node",[guard.js])`), never `shell:true` on the data path. Humans hold merge/deploy (V14) — the harness self-approve keystone. |
| T-19-02 | Repudiation / trace integrity | all three oracles | mitigate | No-fabrication is mechanical: CR-01 missing-file → FAIL (never vacuous-PASS); every oracle has a planted-FAIL test AND a real-tree-PASS smoke; the A3 oracle never marks a `pending human` cell passed. |
| T-19-03 | Tampering (input validation) | hooks.json / docs parse | mitigate | `JSON.parse` of hooks.json is wrapped try/catch → fail-closed `fail()` (ASVS V5); stdlib `String.split`/regex only, no markdown/YAML lib added. |
| T-19-04 | Tampering (path traversal) | all file reads + mkdtemp | mitigate | All paths are fixed literals joined to `import.meta.dirname`/`CHECK_ROOT` (ASVS V12); test temp dirs via `mkdtempSync`; no write path derived from argv/env/content. |
| T-19-SC | Tampering | npm installs | mitigate | This plan installs ZERO packages (Node stdlib + existing `{typescript, vitest, @types/node}` only). No package-legitimacy checkpoint needed — nothing to vet. |
</threat_model>

<verification>
- `npm run build` green (committed `.js` emitted, LF) for both `check-uat-oracles.js` and the re-emitted `check-foundation-guards.js`.
- `npx vitest run scripts/check-uat-oracles.test.ts` green — all three oracles prove planted-FAIL + real-tree-PASS.
- `npx vitest run scripts/check-foundation-guards.test.ts` green — existing six-guard cases PLUS the new Tier-1 plant case (aggregator goes RED when a Tier-1 oracle fails).
- `node scripts/check-uat-oracles.js` over the real tree exits 0, prints `ALL CHECKS PASSED`.
- `node scripts/check-foundation-guards.js` over the real tree exits 0 and exercises the three Tier-1 oracles (BLOCKER 1 / LOCKED CONTEXT.md decision / ROADMAP SC3); it exits non-zero when any one Tier-1 oracle is planted to fail.
- `npm run freshness` green — no committed-`.js` drift (D-13) for either aggregator.
- Zero new devDependency added (`git diff package.json` shows no `devDependencies` change in this plan).
</verification>

<success_criteria>
The three Tier-1 deterministic oracles exist, fail red, and never fabricate a pass (UAT-AUTO-01, and the deterministic A2/A3 structural portions): B3 wording asserts the three WR-05 beats in all four docs; A2 wiring proves the hooks.json→guard.js deny contract without re-testing guard logic; A3 parity asserts the frozen filenames+verdict and never passes a `pending human` cell. Each is authored in TypeScript, compiled to a freshness-clean committed `.js`, and Vitest-covered with BOTH a planted-FAIL-red and a real-tree-PASS path. The three oracles are ALSO wired into the foundation-guards aggregator (UAT-AUTO-05 / LOCKED CONTEXT.md decision / ROADMAP SC3): `node scripts/check-foundation-guards.js` exercises all three and fails closed (exits non-zero) when any one is planted to fail, proven by a new plant case in `check-foundation-guards.test.ts`.
</success_criteria>

<output>
Create `.planning/phases/19-factory-auto-uat-harness-tier-1-deterministic-oracles-tier-2/19-01-SUMMARY.md` when done.
</output>

---
phase: 07-shared-home-foundation-path-rewrite
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - scripts/check-kit-refs.sh
autonomous: true
requirements: [SHOME-03, SHOME-04]
must_haves:
  truths:
    - "`scripts/check-kit-refs.sh` exists, is POSIX, read-only, and CI-runnable (SC5, D-07)"
    - "Assertion 1: ZERO `agent-factory/config/` refs across the SCAN set (SC1/SC3, D-08.1)"
    - "Assertion 2: every surviving `agent-factory/handoffs/` ref is a known template name or the bare template dir (SC1, D-08.2)"
    - "Assertion 3: no kit file or AGENTS.md names `$GRUGOPS_HOME` (SC4, O3 — recommended include)"
    - "The compressed invariant marker is byte-identical across AGENTS.md, orchestrator.md, and the two resolver adapters (SC2)"
    - "The gate ships GREEN at commit (exit 0) over the rewritten tree (SC5)"
  artifacts:
    - path: "scripts/check-kit-refs.sh"
      provides: "The grep-to-zero build gate (3 assertions + SC2 marker check)"
      contains: "agent-factory/handoffs/"
      min_lines: 40
  key_links:
    - from: "scripts/check-kit-refs.sh SCAN list"
      to: "agent-factory/{roles,workflows,checklists,packaging} + .claude/skills + .claude/agents/grugops-orchestrator.md + skills + AGENTS.md"
      via: "explicit path list (never repo-wide; excludes fixtures/examples/install/docs)"
      pattern: "SCAN="
    - from: "scripts/check-kit-refs.sh Assertion 2 ALLOW alternation"
      to: "the 16 handoff template names"
      via: "ERE allowlist verbatim from ls agent-factory/handoffs/"
      pattern: "universal-handoff"
---

<objective>
Author `scripts/check-kit-refs.sh` — the mechanical build gate that proves the rewrite is complete and cannot silently regress (SHOME-03 / SC5). It runs the two D-08 assertions (zero `agent-factory/config/` refs; every `agent-factory/handoffs/` ref is a known template) plus the recommended third assertion (no `$GRUGOPS_HOME` in kit prose / AGENTS.md, proving SC4 / O3) plus an SC2 invariant-marker check across the four canonical sites. Because it proves a COMPLETED rewrite (not a missing scaffold), it ships GREEN at commit — diverging from the Phase-5 ships-RED precedent.

Purpose: C1 (the gating blocker) — a single missed ref out of ~190 dangles silently; the grep-to-zero gate is the only mechanical net. The single most important correctness guard: SC5's "zero bare refs" means zero MISCLASSIFIED refs (Assertions 1+2), NOT zero `agent-factory/` strings — the ~96 intended kit-to-kit refs (D-01) MUST survive. Do NOT implement a literal `grep -c 'agent-factory/' == 0`.

Output: `scripts/check-kit-refs.sh` exiting 0 over the rewritten tree; runnable as `sh scripts/check-kit-refs.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-CONTEXT.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-RESEARCH.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-PATTERNS.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-01-SUMMARY.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-02-SUMMARY.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-03-SUMMARY.md
@install/install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/check-kit-refs.sh — 3 assertions + SC2 marker check, POSIX, read-only, ships GREEN</name>
  <files>scripts/check-kit-refs.sh</files>
  <read_first>
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-PATTERNS.md § "scripts/check-kit-refs.sh (NEW gate script)" (the harness skeleton to copy lines 36-57, the conventions to replicate, the portable-grep discipline lines 66-68, the SCAN/EXCLUDE list lines 71-75 incl. repo-root `skills`, Assertion 1/2/3 bodies lines 79-102, the ships-GREEN-not-RED sequencing note line 105)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-RESEARCH.md § "Build-Gate Design" lines 344-409 (SCAN vs EXCLUDE, the three assertions, the dir-only permit subtlety line 388, the false-positive/negative table, the CRITICAL SC5-interpretation row line 407-409)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-RESEARCH.md § "Handoff Template Allowlist" lines 276-298 (the exhaustive 16-template ERE alternation — copy verbatim)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-CONTEXT.md § D-07, D-08
    - install/install.sh (POSIX house style: `#!/usr/bin/env sh`, `set -eu`, `printf` not `echo -e`, `grep -qF`, small named helpers — read the head + the house-style comment)
    - .planning/phases/05-packaging-adapters-install-distribution/check-structure.sh (the pass()/fail()/FAILS-counter harness idiom + exit 0/1 — copy the skeleton shape, NOT its ships-RED framing)
    - The three Plan SUMMARYs (07-01/02/03) for the exact invariant marker phrase to grep in the SC2 check
  </read_first>
  <action>
    Write `scripts/check-kit-refs.sh` as a POSIX `sh` script: `#!/usr/bin/env sh`, `set -eu`, `printf` (never `echo -e`), named `pass()`/`fail()` helpers incrementing a `FAILS` counter, `exit 0` all-pass / `exit 1` any-fail (mirror the Phase-5 harness skeleton + install.sh house style). Define an explicit SCAN path-list variable (NEVER a repo-wide grep): `agent-factory/roles agent-factory/workflows agent-factory/checklists agent-factory/packaging agent-factory/_commit-convention.md .claude/skills .claude/agents/grugops-orchestrator.md skills AGENTS.md`. By NOT listing them, the gate excludes `scripts/fixtures/`, `agent-factory/examples/`, `agent-factory/README.md`, `install/`, root `README.md`, `CLAUDE.md`, `docs/`, `.planning/`, and the gate file itself (these legitimately carry `agent-factory/` — Pitfall 5). Use ONLY portable grep flags `-r -n -l -E -F -q -v -o` (host grep is ugrep-aliased — no `-P`, no `-z`, no `--include`, no reliance on default recursive globs — Pitfall 4); guard greps inside `$()` with `|| true` so `set -e` does not abort on no-match. Implement:
    - **Assertion 1 (D-08.1):** `grep -rn 'agent-factory/config/' $SCAN` finds nothing → pass; any hit → fail naming the stray refs.
    - **Assertion 2 (D-08.2):** every `agent-factory/handoffs/` ref minus the 16-template ERE alternation (copy verbatim from RESEARCH) minus the bare template-dir permit (`agent-factory/handoffs/` followed by a backtick) must be empty; a leaked instance (`agent-factory/handoffs/<ID>-...`) fails because it is not a template name.
    - **Assertion 3 (O3, recommended):** `grep -rln 'GRUGOPS_HOME' agent-factory AGENTS.md` finds nothing → pass (scope ONLY `agent-factory` + `AGENTS.md`; `$GRUGOPS_HOME` is LEGAL in the `.claude/skills`/`.claude/agents`/`skills` resolver adapters, so they are EXCLUDED from Assertion 3's scan — PATTERNS note line 103).
    - **SC2 invariant-marker check:** `grep -qF '<the exact marker phrase from the SUMMARYs>' AGENTS.md .claude/skills/grugops/SKILL.md .claude/agents/grugops-orchestrator.md agent-factory/roles/orchestrator.md` — assert the byte-identical invariant is present at all four canonical sites; any missing site fails.
    The gate is strictly READ-ONLY — grep + `test` only, no `sed -i`, no writes, no `--fix` (Security V12). Never fabricate a pass. Make `scripts/` if absent. The script MUST exit 0 over the rewritten tree (it is authored AFTER Plans 01-03 land, so it ships GREEN — copy the harness shape, NOT the Phase-5 ships-RED framing comment).
  </action>
  <acceptance_criteria>
    - `sh scripts/check-kit-refs.sh; echo $?` prints `0` (gate green over the rewritten tree — SC5)
    - The script contains the literal 16-template ERE alternation including `universal-handoff` and `business-handoff` (the unused-but-allowlisted templates)
    - The script's SCAN variable lists `skills` (repo-root plugin colon-form) and does NOT list `scripts/fixtures` / `agent-factory/examples` / `install` / `docs`
    - Assertion 3 scans only `agent-factory AGENTS.md` (NOT the adapter dirs) — `grep -n 'GRUGOPS_HOME' scripts/check-kit-refs.sh` shows the scan scope excludes `.claude` and bare `skills`
    - The script is read-only: `! grep -Eq 'sed -i|>[^=]|rm |mv ' scripts/check-kit-refs.sh` (no in-place edit / write / delete operators beyond `>/dev/null` redirects)
    - `head -1 scripts/check-kit-refs.sh` is `#!/usr/bin/env sh` and the script contains `set -eu`
  </acceptance_criteria>
  <verify>
    <automated>sh scripts/check-kit-refs.sh</automated>
  </verify>
  <done>`scripts/check-kit-refs.sh` exists, is POSIX read-only, runs the 3 assertions + SC2 marker check over the explicit SCAN set, and exits 0 over the rewritten tree.</done>
</task>

<task type="auto">
  <name>Task 2: Phase acceptance — gate green + invariant byte-identical + no structural regression + fail-on-mutation proof</name>
  <files>scripts/check-kit-refs.sh</files>
  <read_first>
    - scripts/check-kit-refs.sh (authored in Task 1 — confirm the assertions and SCAN scope)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-RESEARCH.md § "Validation Architecture" lines 556-569 (the Phase Requirements → Test Map, the full-suite command, the per-wave no-regression check) and § "Sequencing" line 417 (ships-green-at-commit)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-PATTERNS.md § "scripts/check-kit-refs.sh" Assertion-2 verification note (the alternation was verified to match `uat-handoff.md` and reject `ABC-001-impl.md`)
    - The three Plan SUMMARYs for the marker phrase
  </read_first>
  <action>
    Run the phase acceptance suite and prove the gate both passes clean AND catches a regression. (1) Confirm `sh scripts/check-kit-refs.sh` exits 0 (all 3 assertions + SC2 marker green). (2) Confirm `node scripts/validate-agent-factory.mjs` exits 0 (the existing structure validator still passes — no regression; the rewrite must not break role/workflow section structure). (3) Prove the gate FAILS on a mutation (no-fabrication / the gate must actually bite): create a throwaway temp copy of one in-scope file with a deliberately-injected stray `agent-factory/config/` ref OR a leaked `agent-factory/handoffs/ABC-001-implementation.md` instance, run the gate against a SCAN that includes the temp file, confirm it exits 1 and names the stray ref, then delete the temp file — leave the real tree untouched. (Do this with a temp file outside the committed tree, e.g. under a `mktemp -d` dir, so no real file is mutated; the gate is read-only by design.) Do NOT modify the gate to make any assertion pass artificially. Do NOT create `.grugops/` (Phase-8 seam). Do NOT touch `scripts/validate-agent-factory.mjs` (Phase-9 seam).
  </action>
  <acceptance_criteria>
    - `sh scripts/check-kit-refs.sh; echo $?` prints `0`
    - `node scripts/validate-agent-factory.mjs; echo $?` prints `0` (no structural regression)
    - The invariant marker is byte-identical across all four sites: `grep -lF '<marker>' AGENTS.md .claude/skills/grugops/SKILL.md .claude/agents/grugops-orchestrator.md agent-factory/roles/orchestrator.md | wc -l` returns 4
    - The fail-on-mutation proof is recorded in the SUMMARY: a temp file with an injected stray ref made the gate exit 1 and name the stray, then was removed; the real tree still passes
    - `.grugops/` was NOT created and `scripts/validate-agent-factory.mjs` was NOT modified
  </acceptance_criteria>
  <verify>
    <automated>sh -c "sh scripts/check-kit-refs.sh >/dev/null 2>&1 && node scripts/validate-agent-factory.mjs >/dev/null 2>&1 && test ! -e .grugops && echo OK"</automated>
  </verify>
  <done>The gate exits 0 over the rewritten tree, the existing validator still passes, the invariant is byte-identical at all four sites, and the gate is proven to bite on an injected mutation — all without creating `.grugops/` or touching the Phase-9 validator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate flags → CI pass/fail decision | a portability-broken or wrong-scoped grep can report a FALSE GREEN, the security-relevant failure mode for this phase |
| gate file contents → filesystem | a gate that ever gained a write/`--fix` path could mutate the kit it audits |
| SCAN scope → false positives/negatives | a repo-wide grep would false-fail on fixtures; a too-narrow scope would false-pass on a missed file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering (false green) | ugrep-vs-POSIX-grep flag portability | mitigate | Use ONLY `-r -n -l -E -F -q -v -o`; no `-P`/`-z`/`--include`/default-glob reliance (Pitfall 4). A flag that silently no-ops on another grep would make the gate pass falsely. Task 2's fail-on-mutation proof confirms the gate actually bites (it is not a no-op). Clear-voice rationale: a false green is the worst outcome here — it would let a dangling ref ship. |
| T-07-04-02 | Tampering (false green) | SCAN scope too narrow / too wide | mitigate | Explicit SCAN path list, never repo-wide; excludes fixtures/examples/install/docs/.planning/self (Pitfall 5). Acceptance asserts `skills` is listed and the exclusions are absent. |
| T-07-04-03 | Tampering (destructive gate) | gate gaining a write path | mitigate | Gate is strictly read-only (grep + `test`); Security V12. Acceptance asserts no `sed -i`/write/delete operators. No `--fix` this phase (Phase-9 doctor `--fix` is itself deferred to v2+). |
| T-07-04-04 | Repudiation (fabricated pass) | hand-edited gate to force green | mitigate | No-fabrication rule (CLAUDE.md). Task 2 proves the gate fails on an injected mutation, so a green result is trustworthy. The trace is the proof. |
| T-07-04-05 | Tampering (env-var leak escapes net) | Assertion 3 scope | mitigate | Assertion 3 scans `agent-factory AGENTS.md` only (the adapters legally carry `$GRUGOPS_HOME`); this proves SC4 without false-failing on the legal self-heal line. |
| T-07-04-SC | Tampering | npm/pip/cargo installs | n/a | Zero external packages this phase (one in-repo POSIX script). No install tasks; no legitimacy checkpoint required. |
</threat_model>

<verification>
- Quick run: `sh scripts/check-kit-refs.sh` (sub-second; the per-task and per-wave gate).
- Full suite (no-regression): `sh scripts/check-kit-refs.sh && node scripts/validate-agent-factory.mjs`.
- SC2: `grep -lF '<marker>' AGENTS.md .claude/skills/grugops/SKILL.md .claude/agents/grugops-orchestrator.md agent-factory/roles/orchestrator.md | wc -l` returns 4.
- Fail-on-mutation: injected stray ref in a temp file makes the gate exit 1; real tree still exits 0.
</verification>

<success_criteria>
- `scripts/check-kit-refs.sh` exists, is POSIX, read-only, CI-runnable, and exits 0 over the rewritten tree (SC5, D-07).
- Assertion 1 (zero `agent-factory/config/`) + Assertion 2 (every `agent-factory/handoffs/` ref is a known template) operationalize SC5's "zero misclassified refs" — NOT a literal `grep -c 'agent-factory/' == 0` (Pitfall 1, the single most important guard).
- Assertion 3 proves no kit file / AGENTS.md names `$GRUGOPS_HOME` (SC4, O3 included per recommendation).
- The compressed invariant is byte-identical across the four canonical sites (SC2).
- The existing validator still passes; `.grugops/` is not created and the Phase-9 validator is untouched.
</success_criteria>

<output>
Create `.planning/phases/07-shared-home-foundation-path-rewrite/07-04-SUMMARY.md` when done. Record the gate's exit code, the validator's exit code, the four-site marker count, and the fail-on-mutation proof result. Note the explicit decisions taken on O2 (docs/README pointers deferred to Phase 8) and O3 (Assertion 3 included).
</output>

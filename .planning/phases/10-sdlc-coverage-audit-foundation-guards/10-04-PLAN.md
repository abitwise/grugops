---
phase: 10-sdlc-coverage-audit-foundation-guards
plan: 04
type: execute
wave: 2
depends_on: ["10-03"]
files_modified:
  - scripts/validate-agent-factory.mjs
  - scripts/validate.test.sh
autonomous: true
requirements: [SDLC-03]
tags: [validator, enum-check, zero-config, lenient-when-absent]

must_haves:
  truths:
    - "The validator enum-checks all 8 new keys WHEN PRESENT (an invalid value e.g. asvs_level: 'L4' is an error) and treats a MISSING key as its lean default (NOT an error) — per D-14"
    - "quality.lint is shape-checked as an object { strict: bool, autofix: bool }, not enum-checked — per D-12"
    - "quality.test_integrity enum is warn|block ONLY — 'off' is rejected (TINT-03 carve-out) — per D-12"
    - "A config MISSING the 8 keys still passes (zero-config lean default) — SC4 preserved"
    - "The validator stays stdlib-only, read-only, no package.json — invariants unchanged"
    - "validate.test.sh proves: invalid-enum fails red + names the bad key; absent-keys pass (SC4); the two config JSONs are byte-identical"
  artifacts:
    - path: "scripts/validate-agent-factory.mjs"
      provides: "Check 4 extended: active-when-present / lenient-when-absent enum recognition of the 8 new keys"
      contains: "asvs_level"
    - path: "scripts/validate.test.sh"
      provides: "invalid-enum fail-proof + absent-keys-pass (SC4) + cmp -s byte-identity assertions"
      contains: "asvs_level"
  key_links:
    - from: "scripts/validate-agent-factory.mjs"
      to: "agent-factory/config/factory.config.json"
      via: "checkConfig() enum-validates the 8 keys when present"
      pattern: "test_integrity"
    - from: "scripts/validate.test.sh"
      to: "scripts/validate-agent-factory.mjs"
      via: "runs the validator over a mutated fixture, asserts nonzero + bad key name"
      pattern: "validate-agent-factory"
---

<objective>
Extend the validator (`scripts/validate-agent-factory.mjs` Check 4 / `checkConfig()`) to recognize the 8 new config keys with active-when-present / lenient-when-absent semantics (D-14): an invalid present value errors; a missing key is its lean default (never an error, preserving SC4 zero-config). Extend `scripts/validate.test.sh` to prove it fails red on an invalid enum, still passes when the keys are absent, and that the two JSON config files stay byte-identical.

Purpose: Close the validator-recognition half of SC3 and lock SC4 (zero-config still runs lean) mechanically. The enum gate also enforces the TINT-03 trace-integrity carve-out — `test_integrity: "off"` is rejected because `"off"` is not in the enum.
Output: an extended `checkConfig()` + three new assertions in `validate.test.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-CONTEXT.md
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-RESEARCH.md
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-PATTERNS.md
@scripts/validate-agent-factory.mjs
@scripts/validate.test.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend checkConfig() with active-when-present / lenient-when-absent enum checks</name>
  <files>scripts/validate-agent-factory.mjs</files>
  <behavior>
    - Present + valid: `bdd: "strict"`, `quality.tdd: "required"`, `security.asvs_level: "L2"` → no error.
    - Present + invalid: `security.asvs_level: "L4"` → an error naming `asvs_level`; `quality.test_integrity: "off"` → an error naming `test_integrity` (TINT-03 — "off" is NOT in the enum); `bdd: "maybe"` → an error naming `bdd`.
    - Absent (the load-bearing SC4 case): a config with NONE of the 8 keys → no new error (each key degrades to its lean default).
    - `quality.lint` shape: `{ strict: false, autofix: true }` → no error; `quality.lint: "on"` (not an object) → an error; `quality.lint: { strict: "yes" }` (non-boolean) → an error naming `quality.lint.strict`.
    - Invalid enum is an `err()` (always nonzero, even without `--strict`), never a `warn()`.
  </behavior>
  <read_first>
    - `scripts/validate-agent-factory.mjs` lines 280–305 — `checkConfig()`: the JSON parse + the fail-closed null-guard (lines 296–299, KEEP it) + the existing required-string loop for mode/cadence/autonomy (lines 300–304). Insert the new optional-enum block AFTER line 304, before the closing brace at 305.
    - `scripts/validate-agent-factory.mjs` lines 92–97 — the two-tier collector: `err()` pushes to `errors[]` (always nonzero), `warn()` pushes to `warnings[]` (`--strict` promotes). The new enum failures use `err()`.
    - `scripts/validate-agent-factory.mjs` lines 5–6, 26–29 — the header doc comment ("config parses with mode/cadence/autonomy"); update it to mention the new optional-enum keys (the header is the spec — keep it honest).
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-RESEARCH.md` lines 369–419 — the VERBATIM Check-4 extension: the `ENUMS`/`Q_ENUMS`/`SEC_ENUMS` maps, the top-level `bdd` check, the `quality.*` enum loop guarded by `cfg.quality && typeof === "object"`, the `quality.lint` object shape-check, the `security.*` enum loop.
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-PATTERNS.md` lines 182–230 — the CRITICAL difference vs the existing loop (D-14/Pitfall 5): the existing loop is *required-and-string* (a missing key errors); the 8 new keys are *optional-and-enum* — use `if (key in obj)` guards, NOT the unconditional loop.
  </read_first>
  <action>
    Extend `checkConfig()` in `scripts/validate-agent-factory.mjs` IN PLACE (do NOT change the stdlib-only / read-only / no-package.json invariants). Copy the verbatim extension from RESEARCH lines 369–419, inserting it after the required-string loop (after line 304):

    - Define the allowed-value maps: top-level `ENUMS = { bdd: ["off","lean","strict"] }`; `Q_ENUMS = { tdd: ["off","encouraged","required"], ui_e2e: ["off","ui-or-critical-path","always"], test_integrity: ["warn","block"], gate_enforcement: ["advisory","blocking"] }`; `SEC_ENUMS = { asvs_level: ["L1","L2","L3"], block_on: ["none","low","medium","high"] }`. NOTE: `test_integrity` enum is `["warn","block"]` — `"off"` is deliberately EXCLUDED (TINT-03 safety carve-out).
    - Top-level `bdd`: `if ("bdd" in cfg && !ENUMS.bdd.includes(cfg.bdd)) err(...)` — guard on presence; absent = no error (SC4).
    - `quality.*` enums: only if `cfg.quality && typeof cfg.quality === "object" && !Array.isArray(cfg.quality)`; loop `Q_ENUMS`, each guarded by `if (k in cfg.quality && !allowed.includes(cfg.quality[k])) err(...)`.
    - `quality.lint` object SHAPE-check (not enum): if present, must be a non-null non-array object; `strict`/`autofix` if present must be boolean — else `err()`.
    - `security.*` enums: only if `cfg.security` is an object; loop `SEC_ENUMS` with the same presence-guarded pattern.
    - Every invalid-value failure uses `err()` (always nonzero) per RESEARCH Security Domain row 5 — never `warn()`.

    Update the file-header doc comment (lines 5–6 / 26–29) to note Check 4 now also enum-checks the 8 optional keys (present-only; absent = lean default).

    Do NOT make any new key REQUIRED. Do NOT add a `package.json`, a dependency, or a write path. Do NOT reduce or defer any key's check.
  </action>
  <verify>
    <automated>node scripts/validate-agent-factory.mjs >/dev/null 2>&1 && grep -q 'asvs_level' scripts/validate-agent-factory.mjs && grep -q 'test_integrity' scripts/validate-agent-factory.mjs && ! test -f package.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-agent-factory.mjs` over the real (now-edited) tree exits 0 — the seeded lean-default config (Plan 10-03) passes all enum checks.
    - The 8 keys are recognized: `grep -q 'bdd'`, `grep -q 'tdd'`, `grep -q 'ui_e2e'`, `grep -q 'test_integrity'`, `grep -q 'gate_enforcement'`, `grep -q 'asvs_level'`, `grep -q 'block_on'`, `grep -q 'lint'` each succeed against `scripts/validate-agent-factory.mjs`.
    - `test_integrity` enum excludes `off`: `grep -nE 'test_integrity.*"off"' scripts/validate-agent-factory.mjs` returns nothing (TINT-03 — `"off"` is not an allowed value).
    - Invalid enum errors (proven in Task 2): a fixture with `asvs_level: "L4"` makes the validator exit nonzero and print `asvs_level`.
    - Absent keys pass: a config with none of the 8 keys still exits 0 (proven in Task 2).
    - Invariants intact: `! test -f package.json` (no package.json added); the validator imports only `node:` builtins (`grep -nE "from ['\\\"]node:" scripts/validate-agent-factory.mjs` shows only node: imports; no third-party import added).
  </acceptance_criteria>
  <done>`checkConfig()` enum-checks all 8 new keys only-when-present (invalid value = `err()`, nonzero), shape-checks `quality.lint` as a `{strict,autofix}` object, excludes `"off"` from `test_integrity` (TINT-03), treats every absent key as its lean default (no error, SC4), and keeps the stdlib-only / read-only / no-package.json invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Extend validate.test.sh with invalid-enum, absent-keys-pass, and byte-identity assertions</name>
  <files>scripts/validate.test.sh</files>
  <read_first>
    - `scripts/validate.test.sh` (293 lines) — the hermetic harness; `mktemp -d` + `cleanup()` + `trap cleanup EXIT INT TERM` (lines 44–49), `expect_pass`/`expect_fail` helpers (lines 96–110), the plant-one-mutation-in-a-copy pattern (lines 250–257), the `out=$(cmd) && rc=0 || rc=$?` capture-under-set-e idiom (lines 63/257), the result block (lines 285–293).
    - `scripts/validate-agent-factory.mjs` (extended in Task 1) — the validator under test; the harness runs it over fixtures via the `VALIDATE_KIT_ROOT`/`VALIDATE_ROOT` env overrides (the two-root self-test contract, per the 09-04 SUMMARY).
    - `scripts/fixtures/good/` — the existing GOOD fixture tree the harness mutates hermetically (RESEARCH Open Question 2 recommends mktemp -d from `fixtures/good`, mutate one key — matches `validate.test.sh:250-257`).
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-RESEARCH.md` lines 513–516 (Open Question 2: hermetic mktemp, not a committed bad-fixture dir) + 10-PATTERNS.md lines 163–176 (the `cmp -s` byte-identity assertion shape).
  </read_first>
  <action>
    Add THREE assertions to `scripts/validate.test.sh`, reusing the existing hermetic `$WORK` / `expect_fail` / `expect_pass` machinery (do NOT commit a new bad-fixture dir — build it hermetically from `fixtures/good`, matching the repo's newer pattern, RESEARCH Open Question 2):

    1. INVALID-ENUM fail-proof: copy `fixtures/good/agent-factory` into a fresh `$WORK/bad-asvs-kit`, mutate ONLY `agent-factory/config/factory.config.json` so `security.asvs_level` is `"L4"` (an out-of-enum value — if `fixtures/good` does not yet carry a `security` object, add the minimal `{ "asvs_level": "L4" }` to trigger the check). Run the validator with `VALIDATE_KIT_ROOT`/`VALIDATE_ROOT` pointed at the copy; assert nonzero exit AND the output greps `asvs_level` (the `expect_fail` shape). Add a parallel case for `quality.test_integrity: "off"` asserting nonzero + `test_integrity` (proves the TINT-03 carve-out rejects "off").

    2. ABSENT-KEYS-PASS (SC4): assert the validator over the UNMODIFIED `fixtures/good` (which has none of the 8 new keys, or whatever subset) still exits 0 — a config missing the 8 keys passes because each absent key is its lean default. Use `expect_pass`. This is the load-bearing zero-config assertion (SC4).

    3. BYTE-IDENTITY: assert `cmp -s agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` over the REAL tree — pass if identical, fail "config JSON drift (config/ vs seed/ diverge)". (PATTERNS lines 170–176; this complements the same assertion added to the foundation-guards harness in Plan 10-02 — both gates catch a JSON/JSON drift.)

    Keep the harness hermetic (every mutation in `$WORK`; the real repo + `$HOME` never written, except the read-only `cmp -s` on the real tree). Reuse the existing `FAILS` counter + `ALL CHECKS PASSED` / `%s CHECK(S) FAILED` + `exit 0/1` result block. The harness ships GREEN: the invalid-enum cases fail red (proving the guard), the absent-keys case passes, the byte-identity holds (Plan 10-03 kept the JSONs identical).
  </action>
  <verify>
    <automated>sh scripts/validate.test.sh; test $? -eq 0 && grep -q 'asvs_level' scripts/validate.test.sh && grep -q 'cmp -s' scripts/validate.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `sh scripts/validate.test.sh` exits 0 — all existing assertions plus the three new ones pass.
    - The invalid-enum assertion fires the validator and asserts the failure names the key: `grep -q 'asvs_level' scripts/validate.test.sh` and `grep -q 'test_integrity' scripts/validate.test.sh` each succeed; the harness asserts `RC -ne 0` AND `grep -qi asvs_level` on the output for the `L4` case.
    - The absent-keys-pass (SC4) assertion exists: the harness runs the validator over `fixtures/good` and `expect_pass` confirms exit 0 with none of the 8 keys present.
    - The byte-identity assertion exists: `grep -q 'cmp -s' scripts/validate.test.sh` succeeds and references both `agent-factory/config/factory.config.json` and `agent-factory/seed/.grugops/factory.config.json`.
    - The harness is hermetic: `grep -q 'mktemp' scripts/validate.test.sh && grep -q 'trap cleanup' scripts/validate.test.sh` succeed; no committed bad-fixture dir was added under `scripts/fixtures/` for the enum case (`test ! -d scripts/fixtures/bad-config-bad-asvs` — built hermetically per RESEARCH Open Question 2).
  </acceptance_criteria>
  <done>`sh scripts/validate.test.sh` exits 0 with three new assertions: an invalid `asvs_level: "L4"` (and `test_integrity: "off"`) hermetic fixture fails the validator red and names the bad key; a `fixtures/good` config with none of the 8 keys still passes (SC4); and the two config JSONs are proven byte-identical via `cmp -s`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config values → validator | The validator validates config enum values before any downstream gate consumer trusts them; the risk is a smuggled illegal value or a false-green that lets a bad value through |
| fixtures → test harness | The harness mutates only hermetic `$WORK` copies; the real tree is read-only (one `cmp -s`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-T1 | Tampering | config value smuggling (`test_integrity: "off"` to disable the trace-integrity gate) | mitigate | The validator's `test_integrity` enum is `["warn","block"]` — `"off"` is not a member, so a config that tries to disable trace integrity errors out (TINT-03 carve-out, proven by the harness `off` case) |
| T-10-04-RT | Repudiation / Tampering | the validator fabricates a pass (accepts an invalid enum) | mitigate | `validate.test.sh` plants an out-of-enum `asvs_level: "L4"` and asserts the validator fails red + names the key — the no-fabrication contract made mechanical |
| T-10-04-SC4 | Denial of Service | a required-key regression breaks zero-config (an absent key errors) | mitigate | The new checks are `if (key in obj)`-guarded (optional-and-enum), never the unconditional required loop; the harness's absent-keys-pass assertion (SC4) over `fixtures/good` proves a config without the 8 keys still exits 0 |
| T-10-04-R | Repudiation | `--strict`-only severity used to hide a real invalid value | mitigate | Invalid enum is `err()` (always nonzero, even without `--strict`), never `warn()` — a bad value fails the gate unconditionally |
| T-10-04-PI | Tampering | path injection via content-derived paths | accept | Read-only by construction: the validator reads fixed repo-relative paths; the harness mutates only `$WORK` copies (the `validate.test.sh` invariant) |

No network, no auth, no user-supplied input. The validator + harness run dev/maintainer-side, stdlib-only, no package.json.
</threat_model>

<verification>
- `node scripts/validate-agent-factory.mjs` exits 0 over the real tree (seeded lean config passes)
- `sh scripts/validate.test.sh` exits 0 (invalid-enum fails red + names key; absent-keys pass; cmp -s byte-identity holds)
- `! test -f package.json` (stdlib-only invariant intact)
- Full suite: `sh scripts/check-foundation-guards.test.sh && sh scripts/validate.test.sh && sh scripts/check-kit-refs.sh` all green
</verification>

<success_criteria>
- SC3 (completed): the 8 new dial keys are recognized by the validator (D-14 active-when-present / lenient-when-absent), with invalid present values erroring and absent keys treated as lean defaults.
- SC4 met mechanically: a config missing the 8 keys (and zero-config with no config file) still passes — proven by the absent-keys-pass assertion.
- TINT-03 enforced: `test_integrity: "off"` is rejected by the enum.
</success_criteria>

<output>
Create `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-04-SUMMARY.md` when done.
</output>

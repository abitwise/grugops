---
phase: 10-sdlc-coverage-audit-foundation-guards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-foundation-guards.sh
  - scripts/check-foundation-guards.test.sh
  - agent-factory/packaging/adapters.md
autonomous: true
requirements: [SDLC-02]
tags: [guards, posix-sh, wr-05, voice-lint, byte-budget, build-gate]

must_haves:
  truths:
    - "scripts/check-foundation-guards.sh runs four named guard functions in ONE POSIX-sh aggregator and exits 0 over the clean tree — per D-04"
    - "WR-05 guard matches a frontmatter Agent/Task tool-grant ONLY (two EREs) over the exact 4-file scan set, NEVER the prose word 'spawn' — per D-08/D-09"
    - "AGENTS.md byte guard is two-tier WARN 20480 / FAIL 28672, below the 32768 Codex cap — per D-07"
    - "Adapter-size guard is two-tier byte-based WARN 3072 / FAIL 4096 per materialized adapter — per D-07"
    - "Voice-lint is section-scoped (strips ## Caveman prompt) and uses \\bgrug\\b word-boundary markers over curated clear-voice surfaces — per D-10"
    - "scripts/check-foundation-guards.test.sh plants ONE violation per guard hermetically (mktemp -d + trap cleanup) and asserts each fails red with a finding token"
    - "agent-factory/packaging/adapters.md stale spawn prose is corrected to sequential role-load language — per D-09 (recorded in audit, NOT added to guard scope)"
  artifacts:
    - path: "scripts/check-foundation-guards.sh"
      provides: "Four-guard build-gate aggregator (WR-05, AGENTS.md bytes, adapter size, voice-lint)"
      contains: "guard_wr05"
      min_lines: 80
    - path: "scripts/check-foundation-guards.test.sh"
      provides: "Fail-proof harness: one planted violation per guard, hermetic"
      contains: "trap cleanup"
      min_lines: 60
    - path: "agent-factory/packaging/adapters.md"
      provides: "Corrected dispatch doc (sequential role-load, no spawn for Claude Code)"
  key_links:
    - from: "scripts/check-foundation-guards.test.sh"
      to: "scripts/check-foundation-guards.sh"
      via: "runs the guard against a mutated mktemp -d copy, asserts nonzero"
      pattern: "check-foundation-guards.sh"
---

<objective>
Build the four mechanical foundation guards (SDLC-02 / SC2) in ONE POSIX-sh aggregator `scripts/check-foundation-guards.sh` (D-04) with four named functions, plus the fail-proof harness `scripts/check-foundation-guards.test.sh` that proves each guard fails red on a planted violation. Also fix the stale spawn prose in `agent-factory/packaging/adapters.md` (D-09).

Purpose: Stand up the cross-cutting build-gate guards BEFORE any v1.2 content lands, so every later phase (11–17) writes into a guarded environment. Each guard fails red on violation and never fabricates a pass — this is the mechanical form of grugops's no-fabrication contract.
Output: two new scripts under `scripts/` (ship GREEN at commit + planted-violation fail-proof) and a one-time `adapters.md` doc correction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-CONTEXT.md
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-RESEARCH.md
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-PATTERNS.md
@scripts/check-kit-refs.sh
@scripts/validate.test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the four-guard aggregator scripts/check-foundation-guards.sh</name>
  <files>scripts/check-foundation-guards.sh</files>
  <read_first>
    - `scripts/check-kit-refs.sh` (the ANALOG, 150 lines) — clone its house style: `#!/usr/bin/env sh`, `set -eu`, `printf` not `echo -e`, `pass()/fail()` + `FAILS` counter, explicit SCAN lists + the WHY-not-repo-wide comment, portable grep flags ONLY (`-r -n -l -E -F -q -v`; NO `-P`/`-z`/`--include`), READ-ONLY (PATTERNS lines 34–106).
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-RESEARCH.md` Pattern 1 (lines 187–211, WR-05 EREs), Pattern 2 (lines 213–228, AGENTS.md bytes), Pattern 3 (lines 230–264, voice-lint awk strip + markers), Pattern 4 (lines 266–281, adapter size) — the VERIFIED guard bodies to copy in.
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-PATTERNS.md` lines 75–106 — the `warn()` helper (NEW this phase, does NOT increment FAILS) and the four guard-body source mapping.
    - `agent-factory/packaging/subagent.frontmatter.md`, `agent-factory/packaging/slash-command.template.md`, `.claude/skills/grugops/SKILL.md`, `.claude/agents/grugops-orchestrator.md` — the exact 4-file WR-05 scan set; confirm they carry NO Agent/Task token (they ship GREEN today).
  </read_first>
  <action>
    Create `scripts/check-foundation-guards.sh` as a NEW four-guard aggregator (D-04), modeled byte-for-byte on `scripts/check-kit-refs.sh` house style. Do NOT fold into `check-kit-refs.sh` (least coupling). Strictly read-only: grep/wc/awk/test only — no writes, no `--fix`. Portable grep flags only.

    Helpers: clone `pass()` / `fail()` + `FAILS` counter + the `ALL CHECKS PASSED` / `%s CHECK(S) FAILED` + `exit 0/1` result block from `check-kit-refs.sh`. ADD a `warn()` helper that prints `  WARN  %s` and does NOT increment `FAILS` (advisory only — the two size guards are two-tier WARN→FAIL per D-07).

    Four named guard functions, each with an explicit literal SCAN/file list inline + a WHY-not-repo-wide comment (mirror the `check-kit-refs.sh` seed-exclusion comment — `fixtures/`, `examples/`, `docs/`, `.planning/`, `README.md`, `CLAUDE.md` all legitimately carry Agent/spawn/caveman words):

    1. `guard_wr05` — two EREs over EXACTLY the 4-file scan set (D-09 keeps `adapters.md` OUT):
       - comma-form: `^(tools|allowed-tools):.*\b(Agent|Task)\b`
       - YAML-array-item form: `^[[:space:]]*-[[:space:]]*(Agent|Task)\b`
       Scan set: `agent-factory/packaging/subagent.frontmatter.md`, `agent-factory/packaging/slash-command.template.md`, `.claude/skills/grugops/SKILL.md`, `.claude/agents/grugops-orchestrator.md`. Capture hits from BOTH greps with `|| true`; if empty → `pass "WR-05: no spawn grant in frontmatter"`, else `fail "WR-05 spawn grant:\n$hits"`. NEVER grep the prose word "spawn"/"sub-agent" (D-08 — the templates legitimately explain the no-spawn rule with that word).

    2. `guard_agents_bytes` — `wc -c < AGENTS.md` (strip spaces with `tr -d ' '`); `AGENTS_WARN=20480`, `AGENTS_FAIL=28672` (FAIL is BELOW the 32768-byte Codex `project_doc_max_bytes` cap — headroom per D-07). FAIL at ≥28672, WARN at ≥20480, else PASS. Document the 32768 cap + the chosen WARN/FAIL numbers in a header comment (RESEARCH Open Question 1: 20/28 chosen for earlier signal).

    3. `guard_adapter_size` — `wc -c` each of `.claude/skills/grugops/SKILL.md` and `.claude/agents/grugops-orchestrator.md`; `AD_WARN=3072`, `AD_FAIL=4096`. Byte-based (NOT line-based — one adapter line is ~470 chars, so line count under-counts a bloated file). FAIL at ≥4096 with a message naming the adapter path + "role body copied in?", WARN at ≥3072, else PASS.

    4. `guard_voice` — section-scoped over the curated clear-voice surface set `agent-factory/roles/security-nfr.md agent-factory/roles/compliance-officer.md agent-factory/roles/incident-responder.md`. For each file, strip the single fenced `## Caveman prompt` block with the `awk` from RESEARCH Pattern 3 (lines 249–254), then grep the stripped body for the marker ERE `\bgrug\b|\bclub\b|\brock\b|\bcave\b|\bsmash\b|\bshiny\b|brain hurt|me think|no think|big think`. Use `\bgrug\b` (word-boundary — CRITICAL: bare `grug` false-positives on `.grugops/` in every role's `## Reads` section, D-10/Pitfall 2). Any hit → fail naming the file + lines; else pass. Add a header comment documenting the forward-compat design (D-10): because anchoring is "everything except `## Caveman prompt`", Phase 11's new clear-voice "What good looks like / When to escalate" sections are auto-scanned with NO guard change.

    Call all four guards, then the shared result block (`exit 0` if `FAILS -eq 0`, else `exit 1`). The aggregator must stay CI-ready (single command, clean exit codes) per D-06 — but add NO `.github/` workflow (held).

    Do NOT label this a "v1" or defer any guard — all four ship in this aggregator.
  </action>
  <verify>
    <automated>sh scripts/check-foundation-guards.sh; test $? -eq 0 && grep -q 'guard_wr05' scripts/check-foundation-guards.sh && grep -q 'guard_voice' scripts/check-foundation-guards.sh</automated>
  </verify>
  <acceptance_criteria>
    - `sh scripts/check-foundation-guards.sh` exits 0 over the clean tree and prints `ALL CHECKS PASSED`.
    - All four guard function names are present: `grep -q 'guard_wr05'`, `grep -q 'guard_agents_bytes'`, `grep -q 'guard_adapter_size'`, `grep -q 'guard_voice'` each succeed.
    - The WR-05 guard uses BOTH EREs: `grep -qF '^(tools|allowed-tools):.*\b(Agent|Task)\b'` (or the literal pattern) AND the array-item ERE are present in the script.
    - The script contains NO `grep` of the bare prose word for spawn detection (it must match the frontmatter token, not `spawn`/`sub-agent` prose): `grep -nE 'grep[^|]*\bspawn\b' scripts/check-foundation-guards.sh` returns no spawn-detection grep (a comment mentioning "spawn" is fine).
    - The script uses ONLY portable grep flags — `grep -nE 'grep.*-(P|z)|--include' scripts/check-foundation-guards.sh` returns no hits.
    - `grep -q '20480' scripts/check-foundation-guards.sh && grep -q '28672' scripts/check-foundation-guards.sh && grep -q '4096' scripts/check-foundation-guards.sh && grep -q '3072' scripts/check-foundation-guards.sh` — the locked byte thresholds are present.
    - `grep -q '\\bgrug\\b' scripts/check-foundation-guards.sh` — the word-boundary voice marker is used (not bare `grug`).
    - `adapters.md` is NOT in the WR-05 scan set: `grep -n 'adapters.md' scripts/check-foundation-guards.sh` returns no scan-list hit (D-09).
  </acceptance_criteria>
  <done>`scripts/check-foundation-guards.sh` runs four named guards, exits 0 GREEN over the clean tree, uses the two verified WR-05 EREs over the exact 4-file scan set, the locked two-tier byte thresholds, and the section-scoped `\bgrug\b` voice-lint — strictly read-only, portable grep flags only.</done>
</task>

<task type="auto">
  <name>Task 2: Author the fail-proof harness + fix adapters.md stale spawn prose</name>
  <files>scripts/check-foundation-guards.test.sh, agent-factory/packaging/adapters.md</files>
  <read_first>
    - `scripts/validate.test.sh` (the ANALOG, 293 lines) — clone the hermetic `mktemp -d` + `cleanup()` + `trap cleanup EXIT INT TERM` isolation (lines 44–49), the `pass()/fail()` + presence preamble (lines 36–42), the `out=$(cmd) && rc=0 || rc=$?` capture-under-set-e idiom (lines 63/257), the `expect_fail` shape (nonzero exit AND finding token, lines 103–110), and the plant-one-violation-in-a-copy pattern (lines 250–257) — PATTERNS lines 110–178.
    - `scripts/check-foundation-guards.sh` (created in Task 1) — the guard under test; the harness runs it against mutated copies.
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-PATTERNS.md` lines 163–176 — the per-guard planted-violation list and the `cmp -s` byte-identity assertion.
    - `agent-factory/packaging/adapters.md` lines 16, 33, 39–42 — the THREE relevant sites (line 16 conceptual prose is FINE; lines 33 + 39 carry the stale FACTUAL "Claude Code spawns role agents" claim, D-09 fixes those two) — PATTERNS lines 308–343.
  </read_first>
  <action>
    PART A — Create `scripts/check-foundation-guards.test.sh` as the fail-proof harness (ANALOG: `validate.test.sh`). Hermetic: `WORK=$(mktemp -d)` + `cleanup() { rm -rf -- "$WORK"; }` + `trap cleanup EXIT INT TERM`. NEVER mutate the real repo or `$HOME`. Presence preamble: assert `scripts/check-foundation-guards.sh` exists, else fail + exit 1.

    For each guard, copy the relevant scan-set/target file(s) into `$WORK`, plant EXACTLY ONE violation, run the guard logic against the copy, and assert nonzero exit AND the output names the defect (the `expect_fail` shape). Because the guard hard-codes repo-relative paths, plant violations by copying the guard into `$WORK` and pointing it at the mutated copy (e.g. run from `$WORK`, or parameterize via a small wrapper that sources the guard with overridden SCAN paths) — match whatever invocation `validate.test.sh` uses for its hermetic runs. Planted violations (PATTERNS lines 163–168):
    - WR-05: plant `tools: Agent` (and a separate case: a `  - Agent` YAML array item) into a copy of one scan-set file → assert nonzero + "spawn grant".
    - adapter-size: write a >4096-byte copy of an adapter → assert nonzero + the adapter path.
    - AGENTS.md byte: write a >28672-byte AGENTS.md copy → assert nonzero + "AGENTS.md".
    - voice: plant `grug smash` into a clear-voice surface (NOT inside `## Caveman prompt`) of a role copy → assert nonzero + the role path.
    - smoke: run the REAL `sh scripts/check-foundation-guards.sh` over the real tree → assert exit 0 (GREEN).

    ADD a `cmp -s` byte-identity assertion (RESEARCH Pitfall 4 / PATTERNS lines 170–176): `cmp -s agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` → pass if identical, fail "config JSON drift". (This guards against the tri-file drift Plan 10-03 must avoid; it lives here because this is the foundation-guards test harness.)

    Result block: same `ALL CHECKS PASSED` / `%s CHECK(S) FAILED` + `exit 0/1` as `validate.test.sh`. The harness ships GREEN (every planted violation correctly makes its guard fail red; the smoke + cmp assertions pass on the clean tree).

    PART B — Fix `agent-factory/packaging/adapters.md` stale spawn prose (D-09 — recorded in the Plan 10-01 audit; this file is NOT added to the WR-05 guard scope):
    - Line 33 (Claude Code "Dispatch mode" table cell): rewrite "...spawns role agents with the `Agent` tool (sub-agents cannot nest...)" to sequential role-load language matching the other four rows — "Sequential role-load — no spawn" (mirror the Codex/Gemini/OpenCode/Copilot cells at lines 34–37).
    - Lines 39–42 (the prose paragraph): rewrite so Claude Code uses the SAME single-window sequential role-load as the other four CLIs (the frozen Phase-7/8 design). Re-word the "Only the dispatch differs, never the content" sentence — the dispatch is now UNIFORM across all five tools (per RESEARCH lines 482–483), so reword to reflect that the entry file differs, not the dispatch model.
    - LEAVE line 16's conceptual prose ("whether the host can **spawn** sub-agents or must **load** role files") — the WORD "spawn" in conceptual framing is fine (the WR-05 guard matches frontmatter tokens only, not prose, D-08). Do NOT remove conceptual mentions; only correct the stale FACTUAL claim that Claude Code DOES spawn.
  </action>
  <verify>
    <automated>sh scripts/check-foundation-guards.test.sh; test $? -eq 0 && ! grep -qiE 'Claude Code.*spawns role agents|spawns a role agent' agent-factory/packaging/adapters.md</automated>
  </verify>
  <acceptance_criteria>
    - `sh scripts/check-foundation-guards.test.sh` exits 0 — every planted violation made its guard exit nonzero AND the finding named the defect; the smoke + `cmp -s` assertions pass.
    - The harness is hermetic: `grep -q 'mktemp -d' scripts/check-foundation-guards.test.sh && grep -q 'trap cleanup' scripts/check-foundation-guards.test.sh` succeed; no path outside `$WORK` is written.
    - The harness plants all four violations + smoke + cmp: `grep -qi 'spawn grant' scripts/check-foundation-guards.test.sh`, `grep -q 'AGENTS.md' scripts/check-foundation-guards.test.sh`, `grep -q 'grug smash' scripts/check-foundation-guards.test.sh`, `grep -q 'cmp -s' scripts/check-foundation-guards.test.sh` each succeed.
    - `grep -qiE 'Claude Code.*spawns role agents|spawns a role agent' agent-factory/packaging/adapters.md` returns NOTHING (the stale factual spawn claim is gone, D-09).
    - The conceptual "spawn" mention at line 16 is preserved: `grep -q 'spawn' agent-factory/packaging/adapters.md` still succeeds (conceptual prose intentionally retained).
    - The Claude Code row now reads sequential role-load: `grep -qi 'sequential role-load' agent-factory/packaging/adapters.md` succeeds.
  </acceptance_criteria>
  <done>`scripts/check-foundation-guards.test.sh` exits 0, proving each of the four guards fails red on a planted violation (hermetic via `mktemp -d` + `trap cleanup`) plus a smoke-green run and a `cmp -s` config-JSON byte-identity check; `adapters.md`'s stale "Claude Code spawns role agents" prose is corrected to sequential role-load while the conceptual "spawn" framing is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tree → guard script | The guards read fixed repo-relative file paths (never content-derived paths); the integrity risk is the guard's own correctness, not external input |
| host CLI regeneration → packaging templates | A future installer regen could silently re-arm a spawn grant; the WR-05 guard is the mechanical backpressure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-RT | Repudiation / Tampering | a guard that fabricates a pass (false green) | mitigate | `check-foundation-guards.test.sh` plants a REAL violation per guard and asserts each fails red (nonzero + finding token) — the no-fabrication contract made mechanical (the `validate.test.sh` precedent) |
| T-10-02-FN | Tampering | WR-05 false-negative — a planted `Agent`/`Task` grant the regex misses | mitigate | Two verified EREs (comma-form + YAML-array-item, incl. scoped `Agent(worker)`); the harness plants both shapes and asserts the guard catches them |
| T-10-02-FP | Denial of Service (red on a correct file) | WR-05 prose trap + `.grugops` voice trap | mitigate | WR-05 matches the frontmatter token only, never prose "spawn" (D-08); voice-lint uses `\bgrug\b` word-boundary so `.grugops/` does not match (D-10) — verified GREEN on the clean tree |
| T-10-02-E | Elevation of Privilege | re-arming the spawn tool (agent gains sub-agent nesting) | mitigate | The WR-05 guard fails red on any frontmatter `Agent`/`Task` grant over the 2 templates + 2 materialized adapters — combined with the existing prod-deploy hook, "humans decide, agents execute" stays mechanical |
| T-10-02-PI | Tampering | path injection via content-derived file paths | accept | Read-only by construction: every guard path is a fixed literal in an explicit SCAN list, never derived from file content (the `check-kit-refs.sh` invariant) |
| T-10-02-OS | Tampering | AGENTS.md oversize slips past the byte guard | mitigate | FAIL fires at 28672 B, below the 32768 Codex cap (headroom, D-07); the harness plants a >28672 B copy and asserts the guard fails red |

No network, no auth, no user-supplied input. The guards run dev/maintainer-side over grugops's own repo (not the end-user install path).
</threat_model>

<verification>
- `sh scripts/check-foundation-guards.sh` exits 0 (four guards GREEN over the clean tree)
- `sh scripts/check-foundation-guards.test.sh` exits 0 (each planted violation fails red + smoke + cmp)
- `grep -qiE 'Claude Code.*spawns role agents|spawns a role agent' agent-factory/packaging/adapters.md` returns nothing (D-09 fix)
- Full suite: `sh scripts/check-foundation-guards.test.sh && sh scripts/validate.test.sh && sh scripts/check-kit-refs.sh` all green
</verification>

<success_criteria>
- SC2 met: the build gate runs four mechanical foundation guards (WR-05 spawn-grant grep over the 2 templates + 2 materialized adapters, single-source adapter-size check, AGENTS.md byte-budget check below the 32 KiB cap, voice-discipline lint over security/compliance/warning surfaces) — each fails red on violation and never fabricates a pass.
- The `adapters.md` stale spawn prose is corrected (D-09), recorded in the Plan 10-01 audit, NOT added to the guard scope.
</success_criteria>

<output>
Create `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-02-SUMMARY.md` when done.
</output>

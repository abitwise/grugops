---
phase: 10-sdlc-coverage-audit-foundation-guards
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/config/factory.config.json
  - agent-factory/seed/.grugops/factory.config.json
  - agent-factory/config/factory.config.md
  - agent-factory/workflows/05-pr-quality-gate.md
autonomous: true
requirements: [SDLC-03]
tags: [config-dial, schema, enterprise-escalation, e2e-rename]

must_haves:
  truths:
    - "All 8 new dial keys exist with their LOCKED lean defaults across both JSON config files: bdd, quality.tdd, quality.lint, quality.ui_e2e, quality.test_integrity, quality.gate_enforcement, security.asvs_level, security.block_on — per D-12/D-15"
    - "agent-factory/config/factory.config.json and agent-factory/seed/.grugops/factory.config.json stay byte-identical after the edit — per D-15"
    - "quality.e2e_when is RENAMED to quality.ui_e2e (same enum) in ALL sites — no e2e_when reference remains anywhere — per D-13"
    - "factory.config.md (the human twin) gains a row per new key AND an Enterprise escalation contract documenting each key's lean default to enterprise escalation — per D-11"
    - "quality.test_integrity enum is warn|block ONLY (never off — TINT-03 trace-integrity safety carve-out) — per D-12"
    - "'lint' stays in mandatory_gates AND quality.lint is added (complementary, not duplicate) — per D-13"
  artifacts:
    - path: "agent-factory/config/factory.config.json"
      provides: "8 new dial keys with lean defaults; e2e_when to ui_e2e rename"
      contains: "\"asvs_level\": \"L1\""
    - path: "agent-factory/seed/.grugops/factory.config.json"
      provides: "byte-identical companion of config/factory.config.json"
      contains: "\"asvs_level\": \"L1\""
    - path: "agent-factory/config/factory.config.md"
      provides: "human twin: 8 key rows + Enterprise escalation contract per key"
      contains: "Enterprise escalation"
  key_links:
    - from: "agent-factory/config/factory.config.json"
      to: "agent-factory/seed/.grugops/factory.config.json"
      via: "byte-identical companions (cmp -s)"
      pattern: "asvs_level"
    - from: "agent-factory/config/factory.config.md"
      to: "agent-factory/config/factory.config.json"
      via: "documents each JSON key + its enterprise escalation"
      pattern: "ui_e2e"
---

<objective>
Land the config-dial contract + the 8 new dial keys (SDLC-03 / SC3) atomically across the three config files, plus the `e2e_when` to `ui_e2e` rename (D-13) across every reference site. The human twin (`factory.config.md`) gains the lean-default to enterprise-escalation contract per key (D-11).

Purpose: Seed the config schema every later v1.2 capability (BDD/TDD Phase 12, UI/E2E + lint + test-integrity Phase 15, ASVS Phase 14) builds against — so each phase wires behavior into a key that already exists with a documented lean default and enterprise escalation. Phase 10 ONLY creates the keys + the contract; it does NOT wire the behavior behind them.
Output: edited `factory.config.json` + byte-identical seed twin + the documented `factory.config.md` twin + the renamed workflow reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-CONTEXT.md
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-RESEARCH.md
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-PATTERNS.md
@agent-factory/config/factory.config.json
@agent-factory/config/factory.config.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 8 keys to both JSON config files + rename e2e_when across all sites</name>
  <files>agent-factory/config/factory.config.json, agent-factory/seed/.grugops/factory.config.json, agent-factory/workflows/05-pr-quality-gate.md</files>
  <read_first>
    - `agent-factory/config/factory.config.json` (47 lines) — the current schema; the `quality` block is lines 32–37 with `e2e_when` at line 36; `nfr` block is lines 38–42 (the new `security` object slots after it). `bdd` is top-level (e.g. after `autonomy` line 5 or after `priority_scheme` line 31).
    - `agent-factory/seed/.grugops/factory.config.json` — the byte-identical twin; `e2e_when` at line 36; MUST receive the identical edit.
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-RESEARCH.md` lines 422–447 — the verbatim target JSON shape for the 8 keys with their LOCKED defaults (D-12).
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-PATTERNS.md` lines 233–272 — the current vs target `quality` block, the placement notes, and the `cmp -s` re-check instruction.
    - `agent-factory/workflows/05-pr-quality-gate.md:31` — the 4th `e2e_when` site PATTERNS flagged beyond the 3 config files (Critical Finding 1, PATTERNS lines 449–455); the rename MUST update it.
  </read_first>
  <action>
    BEFORE editing, re-run `grep -rn 'e2e_when' agent-factory/ AGENTS.md` to enumerate EVERY reference site at execution time (PATTERNS Critical Finding 1 + RESEARCH Open Question 3 — confirmed sites today: `agent-factory/config/factory.config.json:36`, `agent-factory/seed/.grugops/factory.config.json:36`, `agent-factory/config/factory.config.md:22` + `:63`, `agent-factory/workflows/05-pr-quality-gate.md:31`). Treat every hit as a rename target — if the sweep finds a 6th site, rename it too, or the reference orphans.

    Add the 8 new keys with their LOCKED lean defaults (D-12) to BOTH `agent-factory/config/factory.config.json` AND `agent-factory/seed/.grugops/factory.config.json`, identically (D-15):
    - `bdd`: `"lean"` — TOP-LEVEL key (allowed: `"off" | "lean" | "strict"`)
    - `quality.tdd`: `"encouraged"` (allowed: `"off" | "encouraged" | "required"`)
    - `quality.lint`: `{ "strict": false, "autofix": true }` (object — strict off, autofix on)
    - `quality.ui_e2e`: `"ui-or-critical-path"` — this REPLACES `quality.e2e_when` (D-13: same enum value, RENAME not add; delete the `e2e_when` key)
    - `quality.test_integrity`: `"warn"` (allowed: `"warn" | "block"` — NEVER `"off"`, TINT-03 carve-out)
    - `quality.gate_enforcement`: `"blocking"` (allowed: `"advisory" | "blocking"`)
    - `security.asvs_level`: `"L1"` — `security` is a NEW top-level object (allowed: `"L1" | "L2" | "L3"`)
    - `security.block_on`: `"high"` (allowed: `"none" | "low" | "medium" | "high"`)

    Keep BOTH `"lint"` in `mandatory_gates` AND the new `quality.lint` object (D-13 — gate-presence vs strictness, complementary not duplicate). Do NOT touch the existing `coverage_threshold`, `self_fix_attempts`, `mandatory_gates` keys.

    Rename `e2e_when` to `ui_e2e` at `agent-factory/workflows/05-pr-quality-gate.md:31` (and any other prose site the sweep found): the line references the backtick-quoted key `e2e_when` with value `"ui-or-critical-path"` deciding when e2e runs — change the backtick-quoted key name to `ui_e2e`, preserving the value and surrounding prose.

    AFTER editing both JSON files, run `cmp -s agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` and confirm exit 0 (byte-identical, D-15/Pitfall 4). If it diverges, the two edits were not identical — fix before proceeding. Both files must also remain valid JSON (parse-check each with `node -e`).

    Do NOT label any key a "v1" / placeholder / "wired later" — the keys are real schema with real defaults; only the BEHAVIOR they switch is downstream (that downstream scoping is correct, not scope reduction).
  </action>
  <verify>
    <automated>cmp -s agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json && node -e 'JSON.parse(require("fs").readFileSync("agent-factory/config/factory.config.json","utf8"))' && [ -z "$(grep -rn 'e2e_when' agent-factory/ AGENTS.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `cmp -s agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` exits 0 (byte-identical companions, D-15).
    - Both JSON files parse: `node -e 'JSON.parse(require("fs").readFileSync("agent-factory/config/factory.config.json","utf8"))'` and the seed equivalent both exit 0.
    - `grep -rn 'e2e_when' agent-factory/ AGENTS.md` returns NO hits (rename complete across all sites, D-13).
    - All 8 keys + defaults present in config JSON: `grep -q '"bdd": "lean"'`, `grep -q '"ui_e2e": "ui-or-critical-path"'`, `grep -q '"tdd": "encouraged"'`, `grep -q '"test_integrity": "warn"'`, `grep -q '"gate_enforcement": "blocking"'`, `grep -q '"asvs_level": "L1"'`, `grep -q '"block_on": "high"'`, and `grep -q '"lint":'` (the quality.lint object) each succeed against `agent-factory/config/factory.config.json`.
    - `quality.test_integrity` value is NOT `"off"`: `grep -q '"test_integrity": "off"'` returns nothing (TINT-03 carve-out).
    - `"lint"` is still in `mandatory_gates`: `grep -q '"mandatory_gates": \["lint"' agent-factory/config/factory.config.json` succeeds (D-13 complementary).
    - `agent-factory/workflows/05-pr-quality-gate.md` references `ui_e2e` not `e2e_when`: `grep -q 'ui_e2e' agent-factory/workflows/05-pr-quality-gate.md` succeeds.
  </acceptance_criteria>
  <done>Both JSON config files carry the 8 new keys with LOCKED lean defaults, stay byte-identical (`cmp -s` exit 0), parse as valid JSON, `e2e_when` is renamed to `ui_e2e` everywhere (zero remaining hits including the `05-pr-quality-gate.md` workflow site), `test_integrity` is `warn` not `off`, and `"lint"` remains in `mandatory_gates`.</done>
</task>

<task type="auto">
  <name>Task 2: Document the 8 keys + the Enterprise escalation contract in factory.config.md</name>
  <files>agent-factory/config/factory.config.md</files>
  <read_first>
    - `agent-factory/config/factory.config.md` — the human twin; line 22 is the top-level `quality` row (key list mentions `e2e_when`); lines 56–63 are the `### quality sub-fields` table (with the `e2e_when` row at line 63); lines 73–81 are the "Zero-config defaults" prose. PATTERNS lines 276–304 map every edit site.
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-CONTEXT.md` D-11 (line 54) + D-12 (lines 55–63) — the contract lives by EXTENDING this twin with an "Enterprise escalation" column/section per key; the 8 keys' allowed values + lean defaults.
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-RESEARCH.md` lines 23–31 — the LOCKED key shapes + defaults to copy verbatim into the doc rows.
  </read_first>
  <action>
    Extend `agent-factory/config/factory.config.md` (the human-readable twin, D-11 — the contract lives HERE, explicitly NOT in AGENTS.md which is byte-budgeted and protected by the new guard):

    1. Update the top-level `quality` row (line 22): change the key list `e2e_when` to `ui_e2e` and append the new quality sub-keys to the list (`tdd`, `lint`, `ui_e2e`, `test_integrity`, `gate_enforcement`).
    2. In the `### quality sub-fields` table (lines 56–63): rename the `e2e_when` row to `ui_e2e` (keep default `ui-or-critical-path`), and ADD rows for `tdd` (default `encouraged`), `lint` (default `{ strict: false, autofix: true }`), `test_integrity` (default `warn`), `gate_enforcement` (default `blocking`).
    3. Add a top-level `bdd` row (default `lean`) and a new `### security` sub-fields table with `asvs_level` (default `L1`) and `block_on` (default `high`).
    4. Add the "Enterprise escalation" contract (D-11) — for EACH of the 8 new keys, document the lean default AND the enterprise escalation. Use whichever shape reads cleanest: either an added `Enterprise escalation` column on each new key's table row, OR a parallel "## Config-dial contract (lean to enterprise)" section with one row per key. Each row states: key · allowed values · lean default · enterprise escalation. Example escalations to write (forward-compatible with phases 12–15; document the dial direction, do NOT wire behavior): `bdd` lean `lean` to enterprise `strict`; `quality.tdd` lean `encouraged` to enterprise `required`; `quality.lint` lean `{strict:false}` to enterprise `{strict:true}`; `quality.ui_e2e` lean `ui-or-critical-path` to enterprise `always`; `quality.test_integrity` lean `warn` to enterprise `block` (never `off` — note the TINT-03 carve-out in clear voice); `quality.gate_enforcement` lean `blocking` to enterprise `blocking` (already strict in lean — note it); `security.asvs_level` lean `L1` to enterprise `L2`, then `L3` + named human sign-off; `security.block_on` lean `high` to enterprise `medium`/`low`.
    5. Update the "Zero-config defaults" prose (lines 73–81) to note that all 8 new keys degrade to their documented lean defaults when absent (SC4).

    Write the contract in CLEAR professional voice (this documents security/compliance/gate dials — no caveman markers; it covers security/test-integrity, so keep it clear-voice). Do NOT reduce, defer, or label any contract row "v1".
  </action>
  <verify>
    <automated>grep -q 'Enterprise escalation' agent-factory/config/factory.config.md && grep -q 'ui_e2e' agent-factory/config/factory.config.md && [ -z "$(grep -n 'e2e_when' agent-factory/config/factory.config.md)" ] && grep -q 'asvs_level' agent-factory/config/factory.config.md && grep -q 'test_integrity' agent-factory/config/factory.config.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Enterprise escalation' agent-factory/config/factory.config.md` succeeds (D-11 contract present).
    - `grep -n 'e2e_when' agent-factory/config/factory.config.md` returns NO hits (renamed in the twin too, D-13).
    - All 8 keys documented: `grep -q 'ui_e2e'`, `grep -q 'tdd'`, `grep -q 'lint'`, `grep -q 'test_integrity'`, `grep -q 'gate_enforcement'`, `grep -q 'bdd'`, `grep -q 'asvs_level'`, `grep -q 'block_on'` each succeed against `agent-factory/config/factory.config.md`.
    - The `test_integrity` row documents `warn | block` and notes it is never `off` (TINT-03): `grep -qi 'test_integrity' agent-factory/config/factory.config.md` succeeds and the surrounding contract row names both allowed values.
    - The "Zero-config defaults" prose notes the new keys degrade to lean defaults when absent (SC4): the zero-config section mentions the new keys' default behavior.
    - The doc is clear-voice: `sh scripts/check-foundation-guards.sh` still exits 0 (this file is not a voice-lint surface today, but no caveman markers were introduced).
  </acceptance_criteria>
  <done>`agent-factory/config/factory.config.md` documents all 8 new keys with their allowed values + lean defaults, carries an Enterprise escalation contract per key (D-11), renames `e2e_when` to `ui_e2e`, notes the TINT-03 `warn|block`-only carve-out, and updates the zero-config-defaults prose so SC4 (absent key = lean default) is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config schema → later-phase consumers | The 8 keys are consumed by phases 12/14/15; a wrong default or a smuggled illegal enum value would mis-configure a downstream gate |
| config/ JSON → seed/ JSON | The two must stay byte-identical; silent drift breaks the installer's seed fidelity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-T1 | Tampering | tri-file config drift (config/ vs seed/ JSON diverge) | mitigate | `cmp -s` byte-identity assertion (added to the foundation-guards test harness in Plan 10-02) + a re-check after editing both files (D-15/Pitfall 4) |
| T-10-03-T2 | Tampering | `test_integrity: "off"` smuggled to disable the trace-integrity gate | mitigate | The default is seeded as `"warn"` and the enum (enforced by the validator in Plan 10-04) excludes `"off"` entirely (TINT-03 carve-out) — a config that tries to disable trace integrity is rejected |
| T-10-03-O | Tampering | orphaned `e2e_when` reference after the rename | mitigate | An execution-time `grep -rn 'e2e_when' agent-factory/ AGENTS.md` sweep enumerates every site (incl. the `05-pr-quality-gate.md` site PATTERNS found beyond the 3 config files); the verify asserts zero remaining hits |
| T-10-03-SC4 | Denial of Service (zero-config breakage) | a key with no documented default breaks lean-default degradation | accept-with-mitigation | Every key ships with a documented lean default in the JSON + the twin; the validator (Plan 10-04) treats absent keys as lean defaults, never errors — SC4 preserved |

No network, no auth, no executable surface — this plan edits JSON + markdown + one workflow prose line.
</threat_model>

<verification>
- `cmp -s agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` (byte-identical, D-15)
- `node -e 'JSON.parse(...)'` both JSON files parse
- `grep -rn 'e2e_when' agent-factory/ AGENTS.md` returns no hits (rename complete)
- `grep -q 'Enterprise escalation' agent-factory/config/factory.config.md` (contract present)
- `sh scripts/check-foundation-guards.sh` still exits 0 (config edits did not break any guard)
- Human review (Manual-Only, 10-VALIDATION.md): each of the 8 keys has a lean default AND an enterprise escalation row in the twin
</verification>

<success_criteria>
- SC3 (partial — schema + contract half): the 8 new dial keys exist with lean defaults across `config/factory.config.json`, `config/factory.config.md` twin, and `seed/.grugops/factory.config.json` atomically; the lean-default to enterprise-escalation contract is documented per key. (Validator recognition is Plan 10-04.)
- SC4 documented: zero-config still runs lean — every new key's absent-degrades-to-lean-default is documented in the twin.
- `quality.ui_e2e` replaces `quality.e2e_when` (no double-ownership, D-13); `"lint"` stays in `mandatory_gates` (complementary).
</success_criteria>

<output>
Create `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-03-SUMMARY.md` when done.
</output>

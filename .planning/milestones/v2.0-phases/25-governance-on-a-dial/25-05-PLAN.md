---
phase: 25-governance-on-a-dial
plan: 05
type: execute
wave: 5
gap_closure: true
depends_on: ["25-01", "25-02", "25-03", "25-04"]
autonomous: false
files_modified:
  - hooks/admission-guard.ts
  - hooks/admission-guard.js
  - hooks/admission-guard.test.ts
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/floor-invariance.test.ts
requirements: [GOV-01, GOV-02]
user_setup: []

must_haves:
  truths:
    - "GAP-A/SC1 — the matcher resolves the EFFECTIVE command word per live segment, so every launcher shape the round-2 red-team found now DENIES a gated high-severity un-approved admit: command-modifier builtins (`command`/`exec`/`nice`/`time`/`nohup`/`stdbuf`/`timeout`/`xargs`/`builtin` … node …admit), a path/basename launcher (`/usr/local/bin/node`, `./node` → basename `node`), the alt binary `nodejs`, a split/partial-quoted launcher (`no\"de\"`, `n''ode` → fully de-quoted `node`), and a `{ … }` brace-group command context — all recognized as live admit launches (per the round-2 GAP-A fixes; D-01/D-06)."
    - "GAP-A/SC1 — the resolver is ONE command-resolution authority (skip-builtins + basename-a-path + full-de-quote + brace-group), NOT a wider launcher anchor list; a launcher spelling outside any narrow anchor cannot escape because the parser resolves the effective command word once (the anti-whack-a-mole discipline applied to command-RESOLUTION, the P22 round-8 \"make the boundary BE the parser\" lesson; D-12)."
    - "GAP-A/SC1 (fail-closed tail) — when a LIVE segment carries the admit SHAPE (a `context-io` script reference AND the `admit` verb) but the effective command word cannot be statically resolved to a definitely-non-launcher because the segment uses a dynamic-evaluation / indirection construct the parser cannot see through (`eval`, backtick / `$( … )` command substitution in command position, `sh -c`/`bash -c`/`$SHELL -c` string bodies, an env-var-indirected command word like `$X …admit`), the hook GATES (deny pending a named human) — over-gate is SAFE (SC3 gate-or-stricter); the trigger is NARROW (fires ONLY when the admit shape is present)."
    - "GAP-B/SC1 floor — the refuse-self-set (D-01) is checked INDEPENDENTLY of the matcher early-exit: any inline set/export of the approval var behind ANY wrapper (`GRUGOPS_ADMISSION_APPROVED_BY=eve command node …admit`, behind a path/`nodejs`/brace-group/eval launcher) DENIES even when the matcher would not recognize the launcher and even with the var already in env — a matcher false-negative can no longer reopen the floor invariant."
    - "GAP-C/SC3 — a PRESENT but non-string `human_admission` value (`true`, `1`, `null`, `[\"all\"]`, `{}`) — and a non-object `context` or non-object whole-file config that still parses — canonicalizes to gate-or-stricter (gate at least as strictly as `all`), NOT silently `off`; only the EXACT JSON string `\"off\"` is off-equivalent. A genuinely ABSENT config still reads the lean default (zero-config lean preserved — SC2 untouched)."
    - "GAP-D/SC1 — high-severity authoring-role matching is CASE-INSENSITIVE (and trimmed) at BOTH tiers: a case-variant `by` (`Security-NFR`, `SECURITY-NFR`, `security-nfr`) classifies high-severity, so under `human_admission: high-severity` the hook DENIES and admit()'s forged-stamp backstop REFUSES a self-authored `human:NAME` stamp — a non-canonical case can no longer escape the high-severity gate."
    - "The committed `.js` outputs (`hooks/admission-guard.js`, `scripts/context-io.js`) are byte-faithful `tsc` builds — `npm run freshness` exits 0; `hooks/guard.ts` stays byte-frozen (`git diff --quiet hooks/guard.ts` exit 0, blob `3501810e21308e4b7e219679a6ca30dace9b5d66`, D-02)."
    - "The 25-04 segmentation + the executed SC2 config foundation + the GOV-02 audit ledger are PRESERVED untouched: the quoted/comment/heredoc/subshell/`\\`-continuation/`VAR=`/`env`-prefix segmentation still classifies inert mentions as DATA (no over-block regression — the CR-01-inverse fail-safe stays clean), `readGovernanceConfig`'s absent-config lean default is unchanged, and the config-consistency + audit-ledger suites stay green."
  artifacts:
    - path: "hooks/admission-guard.ts"
      provides: "an effective-command-word resolver layered on the existing (preserved) shell-segment tokenizer: skips command-modifier builtins, takes the basename of a path launcher, fully de-quotes a token (every quoted run, so split/partial quotes resolve), recognizes `{ … }` brace-group command contexts, adds `nodejs` to the launchers, and GATES the unresolvable admit-shape tail (eval / command-substitution / `sh -c`-body / env-indirection) fail-closed; PLUS the refuse-self-set check moved in front of / independent of the matcher early-exit; PLUS case-insensitive high-severity role classification"
      contains: "admit"
    - path: "hooks/admission-guard.js"
      provides: "committed tsc build the host hook runs (freshness-covered; rebuilt after the .ts edit)"
    - path: "scripts/context-io.ts"
      provides: "config readers (readGovernanceConfig + readGovernanceConfigResult) canonicalize a PRESENT non-string human_admission (and a non-object context / non-object whole-file config) to gate-or-stricter rather than the lean off-default, while leaving the ABSENT-config lean contract intact; PLUS case-insensitive HIGH_SEVERITY_ROLES matching in admit()'s D-04 backstop"
      exports: ["admit", "readGovernanceConfig", "readGovernanceConfigResult"]
    - path: "scripts/context-io.js"
      provides: "committed tsc build (freshness-covered; rebuilt after the context-io.ts edit)"
    - path: "hooks/admission-guard.test.ts"
      provides: "extended child-spawn oracle vs the COMMITTED admission-guard.js: each round-2 launcher class (builtin-wrapper / path-basename / nodejs / split-quote / brace-group) DENIES a gated high-severity un-approved admit; the unresolvable admit-shape tail (eval / sh -c body / env-indirection) GATES; an inline self-set behind a wrapper DENIES (GAP-B); a case-variant `by` is gated high-severity (GAP-D); inert heredoc/quoted/comment mentions still ALLOW (no over-block); case count >= guard.test.ts"
      contains: "deny"
    - path: "scripts/context-io.test.ts"
      provides: "unit coverage for the non-string-dial canonicalization (readGovernanceConfig / readGovernanceConfigResult return gate-or-stricter for true/1/null/array/object human_admission; absent config still lean) and the case-insensitive forged-stamp backstop (a case-variant high-severity `by` with a self-authored human:NAME stamp REFUSES under an active dial)"
      contains: "human:"
    - path: "scripts/floor-invariance.test.ts"
      provides: "extended SC1/SC3 sweep: a HOOK-tier non-string-dial canonicalization sweep (true/1/null/array/object/non-object-context → gate-or-stricter) and a case-variant `by` high-severity sweep; an anti-whack-a-mole command-RESOLUTION class fuzz over {effective-command-word obfuscation: builtin-wrapper × path/basename × nodejs × split-quote × brace-group} × {LF|CRLF} asserting the class invariant (a real admit → gated DENY; an inert mention → ALLOW) plus the unresolvable-tail GATE; the four named floor invariants still hold at every value"
      contains: "human_admission"
    - path: "25-05-RED-baseline.txt"
      provides: "captured RED baseline — the PRE-FIX committed `.js` (HEAD 2615084) ALLOWs each round-2 bypass (builtin-wrapper / path-basename / nodejs / split-quote / brace-group launcher; inline self-set behind a wrapper; non-string dial; case-variant `by`) — the 22-08/22-09 RED→GREEN artifact discipline, extending 25-04-RED-baseline.txt's row format"
    - path: "25-05-GREEN-proof.txt"
      provides: "captured GREEN proof — the POST-FIX committed `.js` DENIES each round-2 bypass naming the note (and GATES the unresolvable admit-shape tail), REFUSES the case-variant forged stamp, gates the non-string dial, refuses the self-set-behind-a-wrapper; AND the inert heredoc/quoted/comment mentions + the zero-config absent lean `off` still ALLOW (no over-block regression) — extending 25-04-GREEN-proof.txt's row format"
  key_links:
    - from: "hooks/admission-guard.ts (effective-command-word resolver)"
      to: "a real Bash `tool_input.command` admit verb behind any launcher obfuscation"
      via: "ONE command-resolution authority resolves the effective command word per LIVE segment (skip builtins → basename a path → fully de-quote → brace-group), recognizes node|npx|tsx|nodejs as launchers, and GATES the unresolvable admit-shape tail fail-closed — no second narrower anchor list"
      pattern: "admit"
    - from: "hooks/admission-guard.ts (refuse-self-set, GAP-B)"
      to: "the SELF_APPROVE inline-set check"
      via: "the refuse-self-set check runs independent of / before the matcher early-exit, so an inline approval-var set behind any wrapper DENIES even on a matcher false-negative"
      pattern: "SELF_APPROVE|GRUGOPS_ADMISSION_APPROVED_BY"
    - from: "scripts/context-io.ts (readGovernanceConfig + readGovernanceConfigResult, GAP-C)"
      to: "a PRESENT non-string human_admission / non-object context / non-object whole-file config"
      via: "canonicalize a present non-string value to gate-or-stricter (only the exact JSON string `off` is off-equivalent), leaving the absent-config lean default unchanged"
      pattern: "human_admission"
    - from: "hooks/admission-guard.ts (hook:440) and scripts/context-io.ts admit() (admit:929) — GAP-D"
      to: "HIGH_SEVERITY_ROLES classification"
      via: "case-insensitive (trimmed) high-severity role matching at both tiers so a case-variant `by` classifies high-severity"
      pattern: "HIGH_SEVERITY_ROLES"
    - from: "scripts/floor-invariance.test.ts (command-resolution fuzz + non-string-dial + case-variant sweeps)"
      to: "the COMMITTED hooks/admission-guard.js + scripts/context-io.js"
      via: "child-spawn the committed .js across the effective-command-word obfuscation matrix, every non-string dial, and a case-variant `by`; assert the class invariant + the unresolvable-tail GATE for every variant"
      pattern: "admission-guard\\.js|spawn"
  prohibitions:
    - statement: "A GREEN author suite is NECESSARY-BUT-NOT-SUFFICIENT for this safety guard — the author's 31+88 and then 174-case suites passed and STILL missed these bypasses TWICE (this is the 6th green-suite-insufficient catch of v2.0). Closure REQUIRES a both-direction RED-vs-committed-`.js` proof reproduced against the COMMITTED `hooks/admission-guard.js` and `scripts/context-io.js` (never the `.ts`): for EACH round-2 evasion class (builtin-wrapper / path-basename / nodejs / split-quote / brace-group launcher; inline self-set behind a wrapper; non-string dial; case-variant `by`) a planted high-severity-without-real-approval command flips PRE-FIX ALLOW → POST-FIX DENY naming the note; the unresolvable admit-shape tail (eval / sh -c body / env-indirection) GATES; AND the inert heredoc/quoted/comment mentions + the zero-config absent lean `off` still ALLOW. Captured in 25-05-RED-baseline.txt (pre-fix allows) and 25-05-GREEN-proof.txt (post-fix denies), extending the 25-04 row format. (D-12, [[grugops-safety-invariant-green-suite-insufficient]])."
      status: must-hold
      verification: "25-05-RED-baseline.txt shows the pre-fix committed `.js` (HEAD 2615084) ALLOWing each round-2 bypass; 25-05-GREEN-proof.txt shows the post-fix committed `.js` DENYing each bypass naming the note, GATING the unresolvable tail, and ALLOWing inert/zero-config; reproduced independently of the author suite at the blocking checkpoint."
    - statement: "Do NOT widen the launcher anchor list one more notch (whack-a-mole — P22 was bypassed 7× and round-1 reopened a NEW command-RESOLUTION class exactly this way). The fix MUST be ONE command-resolution authority — a single resolve-the-effective-command-word-or-give-up-per-segment step layered on the preserved 25-04 segmentation — so a launcher spelling outside any narrow anchor cannot escape and the unresolvable tail fails closed. The existing segmentation (quoted/comment/heredoc/subshell/`\\`-continuation/`VAR=`/`env`) MUST be preserved, not rewritten."
      status: must-hold
      verification: "an anti-whack-a-mole command-RESOLUTION property/table fuzz in floor-invariance.test.ts generates admit invocations across the effective-command-word obfuscation matrix {builtin-wrapper × path/basename × nodejs × split-quote × brace-group} × {LF|CRLF} and asserts the class invariant (a real admit → gated DENY; an inert mention → ALLOW) plus the unresolvable-tail GATE for EVERY variant; grep confirms a single command-resolution authority feeding isAdmitInvocation (no second narrower matcher reintroduced) AND that the 25-04 segmentation tokenizer is intact."
    - statement: "The refuse-self-set (D-01) floor invariant MUST hold even on a matcher false-negative: the inline approval-var set/export check is evaluated INDEPENDENTLY of (in front of, or not gated behind) the matcher early-exit, so `GRUGOPS_ADMISSION_APPROVED_BY=… <any-wrapper> node …admit` DENIES regardless of whether the launcher is recognized and even with the var present in env."
      status: must-hold
      verification: "admission-guard.test.ts asserts that an inline self-set behind a builtin-wrapper / path / nodejs / brace-group launcher DENIES vs the committed `.js`; grep confirms the SELF_APPROVE check is not positioned only after the `if (!isAdmitInvocation(cmd)) process.exit(0)` early-exit."
    - statement: "The governance dials only TIGHTEN — there is NO human_admission value (string OR non-string: true/1/null/array/object), NO non-object context / non-object whole-file config, and NO case-variant `by` that opens a floor bypass. A present non-string value is gate-or-stricter (only the exact JSON string `off` is off-equivalent); a case-variant high-severity role classifies high-severity. A genuinely ABSENT config stays lean (SC2 preserved). `hooks/guard.ts` stays byte-frozen (D-02)."
      status: must-hold
      verification: "floor-invariance.test.ts sweeps every non-string human_admission value AND a case-variant `by` against the COMMITTED admission-guard.js / context-io.js and asserts gate-or-stricter (deny on a matched gated admit / refuse the forged stamp), while an absent config allows a routine admit; `git diff --quiet hooks/guard.ts` exits 0; freshness 0 drift."
    - statement: "Do NOT regress the 25-04 over-block-clean (CR-01-inverse fail-safe) angle: an inert heredoc / quoted / `#`-comment admit mention with NO live launcher MUST still ALLOW. The fail-closed unresolvable-tail GATE fires ONLY when the admit SHAPE (a context-io reference AND the admit verb) is present in a LIVE segment — a plain `eval \"echo hi\"` / `bash -c \"ls\"` with no admit reference is NOT gated (no false-positive on unrelated dynamic commands)."
      status: must-hold
      verification: "admission-guard.test.ts + the floor-invariance fuzz assert: inert heredoc/quoted/comment admit mentions ALLOW; a dynamic-eval / `sh -c` command with NO admit reference ALLOWs; only a live admit-shape behind an unresolvable command word GATES."
    - statement: "The SC1/SC3 floor + an INDEPENDENT both-angle red-team (guard-LOGIC probe AND guard-INPUT-SURFACE code-review, opus-grade — the P23 split, distinct blind spots that each earned their keep on BOTH prior round-team passes of this phase) are the proof, not a green suite. This is the named closure gate (verify-phase runs it). The probe MUST invent at least one launcher / obfuscation shape the author did NOT enumerate to test the resolver as ONE authority over command-resolution (e.g. a `bash -c` / env-indirection / eval-base64 admit-shape → must GATE)."
      status: must-hold
      verification: "the blocking checkpoint (Task 25-05-04) HARD-STOPS until an independent opus-grade red-team reproduces both the LOGIC angle (non-string dial / case-variant `by` / config-shape edge opening a hole) AND the INPUT-SURFACE angle (a launcher obfuscation escaping the resolver, or an inert mention reading as a live admit), invents an un-enumerated shape, confirms the four floor invariants + `git diff --quiet hooks/guard.ts` exit 0 + freshness 0 + WR-01 no false-positive, vs the committed `.js`; findings recorded in 25-05-SUMMARY.md, not self-reported as a green suite."
    - statement: "WR-01 / no kit-content false-positive (D-13): the resolver + reader edits and the new test/proof artifacts must not trip `guard_context_writes` or any foundation guard — reference the hook / Workflow 16 / the config keys by name, never restate a raw `.grugops/` write path beside a write token."
      status: must-hold
      verification: "`npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial); the closed Plan-25-02/25-04 deferral markers stay inert (no false-positive)."
---

<objective>
Round-2 gap closure for Phase 25 (25-VERIFICATION.md banner + 25-04-SUMMARY.md "Independent Both-Angle Red-Team", gaps_found). Plan 25-04 correctly solved shell SEGMENTATION (quoted/comment/heredoc/subshell/`\`-continuation/`VAR=`/`env` prefix → inert data) and closed the 6 originally-reported forms. The independent both-angle opus red-team then reproduced FOUR NEW root-cause classes against the COMMITTED `.js` that the author's 174-case suites missed — the parser solved segmentation but not command-RESOLUTION, plus two type/normalization holes upstream of the dial hardening. This plan builds command-RESOLUTION on top of the preserved 25-04 segmentation; it does NOT rewrite segmentation, and it does NOT touch the verified SC2 config foundation or the GOV-02 audit ledger.

This plan hardens TWO files + their committed `.js` across four fixes:

1. **GAP-A/SC1 — `hooks/admission-guard.ts` (effective-command-word resolution).** `LAUNCHERS` is matched as a LITERAL command word (admission-guard.ts:88/265) — an anchor list, not effective-command-word resolution. ≥12 launcher shapes still ALLOW a live gated admit. FIX-A (POSTURE A, user-confirmed): per LIVE segment, RESOLVE the effective command word — skip command-modifier builtins (`command`/`exec`/`nice`/`time`/`nohup`/`stdbuf`/`timeout`/`xargs`/`builtin` …) that delegate to a following command, take the BASENAME of a path launcher (`/usr/local/bin/node`→`node`), FULLY de-quote a token (strip EVERY quoted run so `no"de"`→`node`, `n''ode`→`node` — replacing the whole-token-only de-quote at :118), recognize a `{ … }` brace-group command context, and add `nodejs` to the launchers. If the resolved word ∈ launchers + the segment carries a `context-io` script token + the `admit` verb → live admit → gate. FAIL-CLOSED on the unresolvable tail: a LIVE segment carrying the admit SHAPE (context-io reference + admit verb) whose effective command word cannot be statically resolved to a definitely-non-launcher because it uses a dynamic-evaluation / indirection construct the parser cannot see through (`eval`, backtick / `$( … )` in command position, `sh -c`/`bash -c`/`$SHELL -c` string bodies, an env-var-indirected command word like `$X …admit`) → GATE (deny pending a named human). The trigger is NARROW (fires ONLY when the admit shape is present) and MUST NOT fire on inert pure-data mentions. ONE resolution authority, not a growing launcher-spelling list (anti-whack-a-mole applied to command-resolution).
2. **GAP-B/SC1 floor — `hooks/admission-guard.ts` (self-set ordering).** `SELF_APPROVE.test(cmd)` (:341) runs AFTER the matcher early-exit `if (!isAdmitInvocation(cmd)) process.exit(0)` (:332-333), so every matcher false-negative ALSO reopens the D-01 refuse-self-set floor invariant. FIX-B: move the inline self-set check in front of / independent of the matcher early-exit so an inline self-set behind any wrapper is refused regardless of launcher recognition.
3. **GAP-C/SC3 — `scripts/context-io.ts` (non-string dial canonicalization).** `human_admission: typeof human === "string" ? human : GOVERNANCE_DEFAULTS.human_admission` (context-io.ts:1134 AND 1191; default=`off`) silently coerces a present `true`/`1`/`null`/`["all"]`/`{}` to `off` (`source="ok"`, NOT the absent/lean path) — the plausible operator footgun of writing `"human_admission": true` to "turn it on" turns it OFF. FIX-C: a present non-string value (and a non-object `context` / non-object whole-file config that still parses) canonicalizes to gate-or-stricter (gate at least as strictly as `all`), NOT `off`. Only the EXACT JSON string `"off"` is off-equivalent. The ABSENT-config lean default is PRESERVED (SC2 / zero-config lean stays green).
4. **GAP-D/SC1 — `hooks/admission-guard.ts` (hook:440) + `scripts/context-io.ts` admit() (admit:929) (case-insensitive severity).** `HIGH_SEVERITY_ROLES.has(by)` / `.includes((by).trim())` are case-sensitive while `validate()` accepts any non-empty `by`, so `by: Security-NFR` escapes the high-severity gate under the `high-severity` dial → a forged `human:eve` is admitted. FIX-D: match `HIGH_SEVERITY_ROLES` case-insensitively (and trimmed) at BOTH tiers.

Plus the D-12 proof: the RED-first capture of each new bypass ALLOWing vs the PRE-FIX committed `.js`, the extended suites + command-resolution fuzz, the RED→GREEN proof artifacts, the rebuilt fresh `.js`, and the HARD-STOP independent both-angle red-team checkpoint.

Purpose: make GOV-01's un-forgeable primary tier actually un-forgeable across command-RESOLUTION (not just segmentation) and the un-dialable floor actually un-dialable across non-string config and case-variant severity — proven adversarially vs the committed `.js`, not by a green author suite.
Output: the effective-command-word resolver + fail-closed unresolvable tail, the self-set re-ordering, the non-string-dial canonicalization, the case-insensitive severity match, the extended suites + command-resolution fuzz, the RED/GREEN proof artifacts, the rebuilt fresh `.js`, and the independent red-team confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-RESEARCH.md
@.planning/phases/25-governance-on-a-dial/25-PATTERNS.md
@.planning/phases/25-governance-on-a-dial/25-VERIFICATION.md
@.planning/phases/25-governance-on-a-dial/25-04-SUMMARY.md
@.planning/phases/25-governance-on-a-dial/25-04-RED-baseline.txt
@.planning/phases/25-governance-on-a-dial/25-04-GREEN-proof.txt
</context>

<artifacts_this_phase_produces>
New / changed symbols and files this round-2 gap-closure introduces (for the executor and downstream consumers):

- `hooks/admission-guard.ts`:
  - An effective-command-word resolver layered on the PRESERVED 25-04 `liveTokens` tokenizer — e.g. a `resolveCommandWord(segmentTokens)` / `effectiveLauncher(token)` step feeding `isAdmitInvocation`: skips command-modifier builtins (`COMMAND_MODIFIERS = {command, exec, nice, time, nohup, stdbuf, timeout, xargs, builtin, …}`), takes the BASENAME of a path launcher, FULLY de-quotes a token (every quoted run, replacing the whole-token-only `.replace(/^(['"])([\s\S]*)\1$/, "$2")` at :118 with a strip-all-quoted-runs de-quote), recognizes a `{ … }` brace-group command context, and adds `nodejs` to `LAUNCHERS`.
  - A fail-closed unresolvable-tail decision — e.g. an `unresolvableAdmitShape(segment)` predicate: a LIVE segment carrying the admit shape (context-io reference + admit verb) whose effective command word is a dynamic-evaluation / indirection construct (`eval`, backtick / `$( … )` in command position, `sh -c`/`bash -c`/`$SHELL -c` body, `$VAR`-indirected command word) → treat as a live admit and GATE.
  - The inline self-set (`SELF_APPROVE`) check moved in front of / independent of the matcher early-exit (GAP-B).
  - Case-insensitive `HIGH_SEVERITY_ROLES` matching (GAP-D, hook tier).
- `scripts/context-io.ts`:
  - Non-string `human_admission` canonicalization in BOTH `readGovernanceConfig` (:1131-1135) and `readGovernanceConfigResult` (:1186-1192) — and the non-object-`context` / non-object-whole-file branches (:1128-1130 / :1183-1185) — so a PRESENT non-string value gates-or-stricter (only the exact JSON string `off` is off-equivalent), without changing the ABSENT-config lean default.
  - Case-insensitive `HIGH_SEVERITY_ROLES` matching in admit()'s D-04 backstop (admit:929) (GAP-D, admit tier).
- `hooks/admission-guard.test.ts`: extended child-spawn cases (each round-2 launcher class DENIES; the unresolvable admit-shape tail GATES; an inline self-set behind a wrapper DENIES; a case-variant `by` is gated high-severity; inert mentions still ALLOW).
- `scripts/context-io.test.ts`: non-string-dial canonicalization + case-insensitive forged-stamp backstop unit cases.
- `scripts/floor-invariance.test.ts`: the HOOK-tier non-string-dial + case-variant `by` sweeps and the anti-whack-a-mole command-resolution class fuzz.
- `25-05-RED-baseline.txt`, `25-05-GREEN-proof.txt`: the captured RED→GREEN proof artifacts (22-08/22-09 discipline), extending the 25-04 row format.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 25-05-01: Effective-command-word resolver + fail-closed unresolvable tail + self-set re-ordering (GAP-A + GAP-B) — capture the RED baseline first</name>
  <files>hooks/admission-guard.ts, hooks/admission-guard.js, hooks/admission-guard.test.ts, 25-05-RED-baseline.txt</files>
  <read_first>
    - hooks/admission-guard.ts:85-95 (ADMIT_VERB / ADMIT_SCRIPT / LAUNCHERS at :88 + the LiveToken interface — `nodejs` must be added to the launchers; LAUNCHERS is matched as a literal command word, the GAP-A root)
    - hooks/admission-guard.ts:103-238 (the liveTokens shell-segment tokenizer — the PRESERVED 25-04 segmentation; read the whole-token de-quote at :118 which must become a strip-ALL-quoted-runs de-quote for split-quote launchers; do NOT rewrite the tokenizer's quoting/comment/heredoc/continuation/separator handling)
    - hooks/admission-guard.ts:240-275 (isCommandPrefix at :242-244 + isAdmitInvocation at :252-275 — the launcher scan at :265 `!LAUNCHERS.has(tokens[cmdIdx].value)`; the resolver layers here: resolve the effective command word before the launcher membership test, and add the unresolvable-tail GATE)
    - hooks/admission-guard.ts:328-347 (the matcher early-exit `if (!isAdmitInvocation(cmd)) process.exit(0)` at :332-333 + the SELF_APPROVE check at :341 — GAP-B: the self-set check must be evaluated independent of / before the early-exit)
    - hooks/admission-guard.ts:61-65 (the SELF_APPROVE regex — the inline approval-var set/export detector; preserved verbatim, only its evaluation ORDER changes)
    - hooks/guard.ts:50,88 (the verb-anchor + refuse-self-set posture to MIRROR — DO NOT EDIT guard.ts, D-02; read for pattern only)
    - .planning/phases/25-governance-on-a-dial/25-04-SUMMARY.md:142-156 (the INPUT-SURFACE angle table — the exact round-2 launcher classes: command-modifier builtins, path/basename, nodejs, eval/backtick, split-quote, brace-group; the compounding floor break)
    - .planning/phases/25-governance-on-a-dial/25-04-RED-baseline.txt and 25-04-GREEN-proof.txt (the both-direction proof ROW FORMAT this task's 25-05-RED-baseline.txt must extend)
    - hooks/admission-guard.test.ts:1-70 (the runGuard / expectDeny / expectAllow child-spawn harness vs the COMMITTED admission-guard.js; the `>= guard.test.ts` case-count watch; extend, never reimplement)
  </read_first>
  <behavior>
    - CAPTURE RED FIRST (the 22-08/22-09 discipline): before any source edit, child-spawn each round-2 bypass form through the CURRENT committed `hooks/admission-guard.js` (HEAD 2615084) on a planted high-severity note (by: security-nfr) under human_admission: high-severity with NO approval var, and record that each ALLOWs (no `"deny"` in stdout while the identical command admits): (a) a command-modifier-builtin wrapper (`command node …context-io.js admit T NOTE`, and `exec`/`nice`/`time`/`nohup`/`xargs` forms); (b) a path/basename launcher (`/usr/local/bin/node …admit`, `./node …admit`); (c) the alt binary `nodejs …admit`; (d) a split/partial-quoted launcher (`no"de" …admit`, `n''ode …admit`); (e) a brace-group `{ node …admit; }`; (f) the compounding inline self-set behind a wrapper (`GRUGOPS_ADMISSION_APPROVED_BY=eve command node …admit`). Save to 25-05-RED-baseline.txt (extend the 25-04 row format).
    - AFTER the fix, against the rebuilt committed `.js`, each of (a)–(e) above on a gated high-severity un-approved note → DENY (reason names the note file).
    - The unresolvable admit-shape tail GATES: a LIVE segment carrying a context-io reference AND the admit verb whose effective command word is a dynamic-evaluation / indirection construct — `eval "node …admit …"`, `sh -c "node …admit …"` / `bash -c "…"`, an env-var-indirected command word `$X …context-io.js admit …` — → DENY (gate pending a human). (Over-gate is SAFE; the trigger is the admit shape.)
    - The self-set-behind-a-wrapper (f) DENIES even with the approval var present in env (GAP-B floor preserved on a matcher false-negative).
    - NO over-block regression (the 25-04 CR-01-inverse fail-safe stays clean): an inert heredoc body line, a single/double-quoted echoed mention, a `#`-comment mention, and a path component literally named `admit` → ALLOW. A dynamic-eval / `sh -c` command with NO admit reference (`eval "echo hi"`, `bash -c "ls"`) → ALLOW (the unresolvable-tail GATE fires ONLY on the admit shape).
    - No regression on the 25-04 launchers: subshell `( node …admit )`, `\`-continuation, `npx tsx`, bare `npx`/`tsx`, `env`/`VAR=` prefixes, the supported bare `node …admit` → still DENY; empty/malformed stdin → ALLOW.
  </behavior>
  <action>
    FIRST capture the RED baseline. Against the CURRENT committed `hooks/admission-guard.js`, child-spawn each round-2 evasion form (builtin-wrapper / path-basename / nodejs / split-quote / brace-group launcher; and the inline self-set behind a wrapper) with a planted high-severity note and NO approval var, and record that each ALLOWs into `25-05-RED-baseline.txt` — proof the bypass was real before the fix, in the 25-04 row format.

    THEN layer an effective-command-word RESOLVER on top of the PRESERVED 25-04 `liveTokens` segmentation. Do NOT rewrite the tokenizer's quoting/comment/heredoc/`\`-continuation/separator handling, and do NOT add launcher spellings to a wider anchor list (the whack-a-mole anti-pattern — round-1 reopened this exact class that way). Build ONE command-resolution authority that, for each LIVE command segment (after skipping the existing `VAR=`/`env` prefix transparency at :258-262):
    - SKIPS command-modifier builtins that delegate to a following command — define a `COMMAND_MODIFIERS` set {command, exec, nice, time, nohup, stdbuf, timeout, xargs, builtin} (the round-2 wrappers; mirror the prefix-skip loop shape) and advance to the next live token as the effective command word;
    - takes the BASENAME of a path-form launcher token (split on `/`, take the last segment, so `/usr/local/bin/node`→`node`, `./node`→`node`);
    - FULLY de-quotes the launcher token (strip EVERY quoted run within the token, not only a whole-token-surrounding pair — replace the :118 whole-token de-quote behavior for the command-word resolution so `no"de"`→`node`, `n''ode`→`node`); apply this de-quote in the tokenizer OR in the resolver, whichever keeps the tokenizer's other behavior byte-identical — state which in the summary;
    - recognizes a `{ … }` brace-group: a leading `{` token opens a command context whose first command is the effective command word (treat `{` like the existing `(` subshell-open in the segment-start sense);
    - adds `nodejs` to the launcher set so the alt binary resolves.
    If the resolved effective command word ∈ launchers {node, npx, tsx, nodejs} AND the same segment carries a `context-io` script token followed by the `admit` verb → live admit → gate (unchanged downstream).

    ADD the fail-closed unresolvable-tail decision: define an `unresolvableAdmitShape(segment)` predicate — a LIVE segment whose tokens include a `context-io` reference AND the `admit` verb (the admit SHAPE) but whose effective command word is a dynamic-evaluation / indirection construct the resolver cannot statically resolve to a definitely-non-launcher: `eval`, a backtick or `$( … )` command substitution in command position, an `sh -c`/`bash -c`/`$SHELL -c` string body, or an env-var-indirected command word (`$X`). When the admit shape is present behind such a construct → treat as a live admit and GATE (deny pending a human). The trigger MUST be NARROW: it fires ONLY when the admit shape (context-io reference + admit verb) is present in the LIVE segment — a plain `eval "echo hi"` / `bash -c "ls"` with NO admit reference is NOT a live admit and is NOT gated. An inert quoted/heredoc/comment mention (no live launcher, the text is DATA) stays ALLOW — do NOT regress the 25-04 over-block-clean angle.

    GAP-B (self-set ordering): move the `SELF_APPROVE.test(cmd)` check (:341) so it is evaluated INDEPENDENT of / in FRONT of the matcher early-exit `if (!isAdmitInvocation(cmd)) process.exit(0)` (:332-333). An inline set/export of the approval var must DENY regardless of whether the launcher is recognized and even with the var already in env — a matcher false-negative can no longer reopen the D-01 floor invariant. Keep the existing deny reason text.

    Keep ONE resolution authority — a single resolve-the-effective-command-word-or-give-up-per-segment function, not a growing special-case list of launcher spellings. Reuse the existing `tokenize` / `noteFileFromCommand` helpers to locate the note file once a live admit is recognized; on a matched gated admit where the note file cannot be located, FAIL CLOSED (deny), exactly as today.

    Voice: CLEAR PROFESSIONAL VOICE in all comments and deny reasons (CLAUDE.md hard rule — governance/admission/audit surfaces are never caveman voice). Comment the resolver's structure so a future reader sees ONE command-resolution authority layered on the preserved segmentation, not a stack of launcher-spelling special-cases.

    Comment-text discipline: do NOT place a fenced code block inside this action's prose; describe the resolver by its named functions, the builtin/launcher sets, and the constructs it resolves or gates.

    Rebuild the committed `.js`: `npm run build` then `npm run freshness` (0 drift; `admission-guard.js` is freshness-covered).

    Extend `hooks/admission-guard.test.ts` (the child-spawn oracle vs the COMMITTED `.js`): add cases for each round-2 launcher class (builtin-wrapper / path-basename / nodejs / split-quote / brace-group → DENY on a gated high-severity note), the unresolvable admit-shape tail (eval / sh -c body / env-indirection → DENY), the inline self-set behind a wrapper (DENY, GAP-B), and re-assert the no-over-block inertness (heredoc/quoted/comment/eval-with-no-admit ALLOW) and the 25-04 launchers (subshell / `\`-continuation / npx tsx still DENY) so nothing regressed. Keep the case count >= guard.test.ts (Pitfall-2 watch). Capture the post-fix DENY/ALLOW outcomes for these forms into `25-05-GREEN-proof.txt` (this task contributes the matcher rows; Task 25-05-03 completes the proof set).
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run hooks/admission-guard.test.ts && git diff --quiet hooks/guard.ts</automated>
  </verify>
  <acceptance_criteria>
    - `25-05-RED-baseline.txt` exists and records each round-2 launcher class + the inline self-set-behind-a-wrapper ALLOWed by the PRE-FIX committed `admission-guard.js` (the bypass was real), in the 25-04 row format.
    - A single command-resolution authority resolves the effective command word per segment (grep shows `COMMAND_MODIFIERS` / a resolve step feeding `isAdmitInvocation`, `nodejs` added to launchers, a strip-all-quoted-runs de-quote replacing the whole-token-only de-quote for the command word, brace-group recognition); the 25-04 segmentation tokenizer (quoting/comment/heredoc/continuation/separators) is intact (grep confirms it was not rewritten).
    - `npx vitest run hooks/admission-guard.test.ts` is GREEN: each round-2 launcher class (builtin-wrapper / path-basename / nodejs / split-quote / brace-group) DENIES on a gated high-severity un-approved note; the unresolvable admit-shape tail (eval / sh -c body / env-indirection) DENIES; the inline self-set behind a wrapper DENIES (GAP-B); inert heredoc/quoted/comment mentions and a dynamic command with NO admit reference ALLOW; the 25-04 launchers still DENY; empty stdin ALLOWs; case count >= guard.test.ts.
    - `npm run freshness` exits 0 (committed `admission-guard.js` fresh); `git diff --quiet hooks/guard.ts` exits 0 (byte-frozen, D-02).
    - `25-05-GREEN-proof.txt` records the post-fix DENY for each round-2 launcher class + the unresolvable-tail GATE + the self-set-behind-a-wrapper DENY + ALLOW for the inert/no-admit cases.
  </acceptance_criteria>
  <done>The matcher resolves the effective command word as ONE authority layered on the preserved 25-04 segmentation: every round-2 launcher obfuscation (builtin-wrapper / path-basename / nodejs / split-quote / brace-group) is DENIED when gated/un-approved, the unresolvable admit-shape tail GATES fail-closed, the inline self-set behind any wrapper DENIES (GAP-B floor preserved), inert mentions stay ALLOW — vs the committed `.js`; guard.ts byte-frozen, freshness 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 25-05-02: Non-string-dial canonicalization (GAP-C) + case-insensitive high-severity role match at both tiers (GAP-D)</name>
  <files>scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts, hooks/admission-guard.ts, hooks/admission-guard.js, hooks/admission-guard.test.ts</files>
  <read_first>
    - scripts/context-io.ts:1108-1145 (readGovernanceConfig — the GovernanceConfig interface + GOVERNANCE_DEFAULTS at :1113; the non-object-context branch at :1128-1130; the `typeof human === "string" ? human : default` coercion at :1134 — the GAP-C root; the absent-config lean default at :1144 which MUST stay unchanged)
    - scripts/context-io.ts:1162-1202 (readGovernanceConfigResult — the discriminated {absent|ok|unreadable} read; the SAME non-object-context branch at :1183-1185 and the SAME coercion at :1191; the absent path at :1201 which MUST stay lean)
    - scripts/context-io.ts:855-860 (HIGH_SEVERITY_ROLES at :857 — the const array; admit() reads it case-sensitively at :929)
    - scripts/context-io.ts:920-957 (admit() D-04 backstop — `isHighSeverity` at :927-929 `.includes((scalars.by ?? "").trim())` is CASE-SENSITIVE; the forged-stamp backstop branch at :942; the GAP-D root at the admit tier)
    - hooks/admission-guard.ts:52-59 (HIGH_SEVERITY_ROLES Set at :55 — the hook-tier classifier) and :436-454 (the `isHighSeverity = HIGH_SEVERITY_ROLES.has(by)` at :440 — CASE-SENSITIVE; the GAP-D root at the hook tier)
    - hooks/admission-guard.ts:384-396 (the dial canonicalization `if (dial === "off") exit(0)` + `gateEveryMatch = dial !== "high-severity"` — a non-string dial now arrives canonicalized from the reader, so this string logic stays correct as-is once the reader gates non-strings; confirm no double-handling needed)
    - scripts/context-io.test.ts:626-720 (the `describe("d-04 high-severity in-script refusal"` block + the existing forged-stamp cases at :661/:709 — extend with a case-variant `by` + non-string-dial cases; reuse the temp-config + temp-root admit() fixture pattern)
    - scripts/config-queue-consistency.test.ts:23-30 (the config-load + repo-root resolution shape to mirror for the non-string-dial temp-config setup)
    - .planning/phases/25-governance-on-a-dial/25-04-SUMMARY.md:158-162 (the LOGIC angle — the exact non-string values true/1/null/["all"]/{} and the case-variant `by: Security-NFR`)
  </read_first>
  <behavior>
    - GAP-C non-string dial (readers): a PRESENT `human_admission` that is `true`, `1` (number), `null`, `["all"]` (array), or `{}` (object) → canonicalize to a gate-or-stricter sentinel value (a value the hook/admit() treat as gate-every-match, never `off`). A present non-object `context` and a non-object whole-file config that still parses → likewise gate-or-stricter. Only the EXACT JSON string `"off"` is off-equivalent.
    - GAP-C absent preserved: NO config file at any standard location → the lean default (`human_admission` reads `off`; routine admits ALLOW) — UNCHANGED. SC2 / 25-01/02/03 / zero-config lean depend on it. A present valid `"high-severity"` / `"all"` / `"off"` string still reads VERBATIM (the existing string contract is unchanged).
    - GAP-C end-to-end (hook): with a present `human_admission: true` (or 1/null/array/object) config, a matched high-severity un-approved admit via the committed `admission-guard.js` → DENY (governance is NOT silently off). With a present `"off"` string → ALLOW routine. With an absent config → ALLOW routine.
    - GAP-D case-insensitive severity (both tiers): a finding with `by: Security-NFR` (or `SECURITY-NFR`, `security-nfr`) classifies HIGH-SEVERITY. Under `human_admission: high-severity`: the hook DENIES a case-variant high-severity un-approved admit; admit()'s D-04 forged-stamp backstop REFUSES a case-variant high-severity finding carrying a self-authored `human:NAME` stamp. A genuinely routine role (e.g. `software-engineer`) is still NOT high-severity under `high-severity` (no over-classification).
  </behavior>
  <action>
    PART A — non-string-dial canonicalization (GAP-C) in `scripts/context-io.ts`, in BOTH `readGovernanceConfig` (:1131-1135) and `readGovernanceConfigResult` (:1186-1192). Replace the `typeof human === "string" ? human : GOVERNANCE_DEFAULTS.human_admission` coercion: when `human_admission` is PRESENT but NOT a string (boolean / number / null / array / object), return a gate-or-stricter sentinel — NOT the lean `off` default. The simplest correct sentinel is a non-`off` non-canonical string the hook already treats as gate-every-match (the 25-04 `gateEveryMatch = dial !== "high-severity" && dial !== "off"` logic gates any non-`off` value), e.g. a fixed marker string that is provably not `off` and not `high-severity`; document the chosen sentinel in the summary. Also handle the non-object-`context` branch (:1128-1130 / :1183-1185) and a non-object whole-file config that still parses: a present-but-non-object shape is gate-or-stricter, NOT the lean default — only a genuinely ABSENT config (no file) stays lean. Do NOT change the ABSENT-config path (:1144 / :1201) or the present-valid-string path — SC2 / zero-config lean / the verbatim-string contract depend on them. Keep `audit_retention`'s existing default-on-absent behavior unchanged (GAP-C is human_admission-scoped; do not over-reach into the audit dial).

    PART B — case-insensitive high-severity role match (GAP-D) at BOTH tiers. In `scripts/context-io.ts` admit() (:929) replace the case-sensitive `(HIGH_SEVERITY_ROLES as readonly string[]).includes((scalars.by ?? "").trim())` with a case-insensitive (lowercased + trimmed) membership test. In `hooks/admission-guard.ts` (:440) replace `HIGH_SEVERITY_ROLES.has(by)` with a case-insensitive (lowercased + trimmed) membership test (lowercase the `by` before the Set lookup, or lowercase the Set contents at definition — pick one and keep both tiers consistent; the role constants are already lowercase). A case-variant `by` MUST classify high-severity at both tiers so the high-severity dial cannot be escaped by casing. Do NOT broaden the role SET (the 3:3 map is locked, D-06) — only make the comparison case-insensitive.

    Voice: CLEAR PROFESSIONAL VOICE in all comments and finding/deny messages.

    Comment-text discipline: do NOT place fenced code blocks in this action; name the readers, the coercion sites, the sentinel concept, and the case-insensitive comparison by concept.

    Rebuild the committed `.js`: `npm run build` then `npm run freshness` (0 drift) — both `context-io.js` and `admission-guard.js`.

    Extend the suites: add to `scripts/context-io.test.ts` (1) non-string-dial canonicalization unit cases — `readGovernanceConfig` / `readGovernanceConfigResult` return gate-or-stricter (not `off`) for a present `true`/`1`/`null`/array/object `human_admission` and for a non-object `context`, while an absent config still reads the lean `off`; and (2) the case-insensitive forged-stamp backstop — a case-variant high-severity `by` (`Security-NFR`) with a self-authored `human:NAME` stamp under `human_admission != off` REFUSES naming the fault, note text unchanged. Add to `hooks/admission-guard.test.ts` (child-spawn vs the committed `.js`): a present non-string-dial config DENIES a matched high-severity admit; a case-variant `by` DENIES a gated high-severity un-approved admit; an absent config still ALLOWs routine. Capture the non-string-dial DENY, the case-variant DENY/REFUSE, and the absent-config ALLOW outcomes into `25-05-GREEN-proof.txt`.
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run hooks/admission-guard.test.ts scripts/context-io.test.ts && git diff --quiet hooks/guard.ts</automated>
  </verify>
  <acceptance_criteria>
    - GAP-C readers: `npx vitest run scripts/context-io.test.ts` shows `readGovernanceConfig` / `readGovernanceConfigResult` returning a gate-or-stricter `human_admission` (not `off`) for a present `true`/`1`/`null`/array/object value and for a non-object `context`; an ABSENT config still reads the lean `off`; a present valid string still reads verbatim. The `source` for a present-but-non-string config is still `"ok"` (it was read), but the value gates.
    - GAP-C hook: `npx vitest run hooks/admission-guard.test.ts` shows a present non-string-dial config DENYING a matched high-severity un-approved admit (governance not silently off); a present `"off"` string and an absent config ALLOW routine.
    - GAP-D both tiers: a case-variant `by` (`Security-NFR`) classifies high-severity — the hook DENIES a gated case-variant high-severity un-approved admit; admit()'s backstop REFUSES a case-variant high-severity self-authored `human:NAME` stamp under `human_admission != off`; a routine role is still not high-severity (no over-classification). The 3:3 role SET is unchanged (grep: only the comparison is case-insensitive).
    - The config-consistency + audit-ledger + SC2 suites stay GREEN (the absent-config lean default and the `audit_retention` behavior are unchanged).
    - `npm run freshness` exits 0; `git diff --quiet hooks/guard.ts` exits 0.
  </acceptance_criteria>
  <done>A present non-string `human_admission` (and a non-object config shape) gates-or-stricter instead of silently `off` while the absent-config lean default is preserved, and high-severity role classification is case-insensitive at both the hook and admit() tiers so a case-variant `by` can no longer escape the high-severity gate — GAP-C and GAP-D closed; committed `.js` fresh, guard.ts byte-frozen.</done>
</task>

<task type="auto">
  <name>Task 25-05-03: Anti-whack-a-mole command-resolution fuzz + HOOK-tier non-string-dial / case-variant sweep + complete the RED→GREEN proof set (D-12)</name>
  <files>scripts/floor-invariance.test.ts, 25-05-RED-baseline.txt, 25-05-GREEN-proof.txt</files>
  <read_first>
    - scripts/floor-invariance.test.ts:1-70 (the four-invariant structure + the FROZEN_GUARD_BLOB + the child-spawn-vs-committed-.js + import-the-committed-.js composition — EXTEND this; the 25-04 HOOK-tier garbage-STRING sweep + the launcher×prefix×body×line-ending fuzz live here)
    - scripts/floor-invariance.test.ts (the existing 25-04 anti-whack-a-mole class fuzz over {launcher}×{prefix}×{body}×{LF|CRLF} — the round-2 fuzz ADDS the effective-command-word obfuscation dimension; do NOT duplicate the 25-04 segmentation fuzz)
    - hooks/admission-guard.test.ts:1-70 (the runGuard child-spawn harness shape to reuse for the HOOK-tier non-string-dial + case-variant sweep — spawn the COMMITTED admission-guard.js)
    - scripts/config-queue-consistency.test.ts:23-30 (the config-load + repo-root resolution to mirror for the per-dial temp-config setup, incl. writing a non-string `human_admission` JSON value)
    - hooks/admission-guard.ts (the post-fix effective-command-word resolver from Task 25-05-01 + the case-insensitive classifier from 25-05-02 — the fuzz asserts the CLASS invariant over this resolver)
    - .planning/phases/25-governance-on-a-dial/25-04-GREEN-proof.txt (the GREEN-proof ROW FORMAT and the existing anti-whack-a-mole block this task's 25-05-GREEN-proof.txt extends)
    - .planning/phases/25-governance-on-a-dial/25-CONTEXT.md:147-160 (D-12 — green-suite-insufficient; the anti-enumeration property-fuzz requirement)
  </read_first>
  <behavior>
    - HOOK-tier non-string-dial sweep: for each PRESENT `human_admission` JSON value in {`true`, `1` (number), `null`, `["all"]` (array), `{}` (object), a non-object `context`}, child-spawn the COMMITTED `admission-guard.js` on a gated high-severity un-approved admit and assert it is DENIED (gate-or-stricter; governance is NOT silently off). The present `"off"` STRING → ALLOW routine; an ABSENT config → ALLOW routine (lean preserved). (This is the type-level extension of the 25-04 garbage-STRING sweep.)
    - HOOK-tier case-variant `by` sweep: for each casing of a high-severity role in {`Security-NFR`, `SECURITY-NFR`, `security-nfr`, `Architect-Design`, `Release-Manager`}, child-spawn the COMMITTED `admission-guard.js` under `human_admission: high-severity` on an un-approved admit and assert DENY (case-variant classifies high-severity). A routine role (`software-engineer`) under `high-severity` → ALLOW (no over-classification).
    - Anti-whack-a-mole command-RESOLUTION class fuzz: generate admit invocations across the effective-command-word obfuscation matrix {resolution: builtin-wrapper (`command`/`exec`/`nice`/`time`/`nohup`/`xargs`) | path/basename (`/usr/local/bin/node`, `./node`) | nodejs | split-quote (`no"de"`, `n''ode`) | brace-group `{ … }`} × {LF | CRLF}, each a LIVE admit on a gated high-severity un-approved note. Assert the CLASS invariant for EVERY generated variant: a LIVE admit → DENY. Add the unresolvable-tail dimension: an admit-shape behind an `eval` / `sh -c` body / env-indirection → DENY (GATE); a dynamic command with NO admit reference → ALLOW. An inert mention (quoted / heredoc / comment) → ALLOW. (A hypothetical obfuscation shape #N is caught structurally, not by enumeration.)
    - The four named floor invariants (refuse-self, no-fabrication/never-rewrite, `quality.test_integrity` no-`off`, `hooks/guard.ts` byte-frozen) still hold at every value (preserve the existing 25-03/25-04 assertions; do not weaken them).
  </behavior>
  <action>
    Extend `scripts/floor-invariance.test.ts` (do NOT duplicate the existing 25-04 garbage-STRING sweep or the segmentation fuzz — ADD the non-string-dial sweep, the case-variant `by` sweep, and the command-resolution fuzz dimension). Reuse the child-spawn-vs-committed-`.js` harness shape from `hooks/admission-guard.test.ts` and the temp-config setup from `config-queue-consistency.test.ts:23-30` (write a non-string `human_admission` JSON value for the type-level sweep).

    Add the HOOK-tier non-string-dial sweep: child-spawn the COMMITTED `hooks/admission-guard.js` for every present non-string `human_admission` value (true / 1 / null / array / object / non-object context) and assert the matched gated admit is DENIED, while the present `"off"` string and an absent config allow a routine admit — closing the exact LOGIC gap the round-2 red-team found (the 25-04 sweep tested garbage STRINGS, not non-string types).

    Add the HOOK-tier case-variant `by` sweep: child-spawn the committed `.js` under `human_admission: high-severity` across the casings of the three high-severity roles and assert DENY (case-insensitive classification), and a routine role ALLOWs.

    Add the anti-whack-a-mole command-RESOLUTION property/table fuzz: enumerate the effective-command-word obfuscation matrix {builtin-wrapper × path/basename × nodejs × split-quote × brace-group} × {LF|CRLF} and assert the class invariant (live admit → gated DENY) for every variant vs the committed `.js`, plus the unresolvable-tail GATE (admit-shape behind eval / sh -c body / env-indirection → DENY; no-admit dynamic command → ALLOW) and the inert-mention ALLOW. Make the structural argument explicit in a comment: the matcher resolves the EFFECTIVE command word as ONE authority over the shell grammar, so a launcher spelling outside any narrow anchor cannot escape and the unresolvable tail fails closed — the proof is the class invariant, not an enumeration of known forms.

    COMPLETE the proof artifacts: finalize `25-05-RED-baseline.txt` (pre-fix committed `.js` at HEAD 2615084 ALLOWs each round-2 bypass — builtin-wrapper / path-basename / nodejs / split-quote / brace-group launcher; inline self-set behind a wrapper; non-string dial; case-variant `by`) and `25-05-GREEN-proof.txt` (post-fix committed `.js` DENIES each bypass naming the note; GATES the unresolvable tail; REFUSES the case-variant forged stamp; gates the non-string dial; refuses the self-set-behind-a-wrapper; AND the inert heredoc/quoted/comment mentions + the zero-config absent lean `off` still ALLOW). Extend the 25-04 row format; reference the committed `.js`, never the `.ts`.

    Voice: CLEAR PROFESSIONAL VOICE in comments (this test owns the SC3/SC1 floor). Per D-12, a GREEN fuzz is NECESSARY-NOT-SUFFICIENT — the independent red-team at Task 25-05-04 is the gate; state that in a comment.

    Comment-text discipline: no fenced code blocks in this action; describe the matrix dimensions and the class invariant by concept.
  </action>
  <verify>
    <automated>npx vitest run scripts/floor-invariance.test.ts && git diff --quiet hooks/guard.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/floor-invariance.test.ts` is GREEN with the HOOK-tier non-string-dial sweep added: every present non-string `human_admission` value (true / 1 / null / array / object / non-object context) DENIES a gated admit via the committed `admission-guard.js`; the present `"off"` string and an absent config ALLOW routine.
    - The HOOK-tier case-variant `by` sweep is GREEN: every casing of a high-severity role DENIES a gated un-approved admit under `high-severity`; a routine role ALLOWs.
    - The anti-whack-a-mole command-resolution fuzz over {builtin-wrapper × path/basename × nodejs × split-quote × brace-group} × {LF|CRLF} is GREEN: every live admit variant is gated-DENY, the unresolvable tail GATES (admit-shape behind eval / sh -c / env-indirection → DENY; no-admit dynamic command → ALLOW), every inert mention is ALLOW (class invariant, not enumeration).
    - The four named floor invariants still hold at every value; `git diff --quiet hooks/guard.ts` exits 0.
    - `25-05-RED-baseline.txt` and `25-05-GREEN-proof.txt` are complete (each round-2 bypass: pre-fix ALLOW, post-fix DENY; unresolvable-tail GATE; inert/zero-config ALLOW) and reference the committed `.js`, not the `.ts`, extending the 25-04 row format.
  </acceptance_criteria>
  <done>The HOOK tier is swept over every non-string dial value + a case-variant `by`, and a command-resolution fuzz over the effective-command-word obfuscation matrix proves the class invariant structurally (with the unresolvable-tail GATE and the inert-mention ALLOW); the RED→GREEN proof artifacts are captured vs the committed `.js`; guard.ts byte-frozen — the SC1/SC3 author proof is complete (independent red-team still the gate, D-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 25-05-04: SC1+SC3 round-2 closure gate — INDEPENDENT both-angle red-team vs the committed .js (HARD STOP; green suite is NOT proof, D-12)</name>
  <read_first>
    - .planning/phases/25-governance-on-a-dial/25-04-SUMMARY.md:134-177 (the round-1 red-team that found these round-2 gaps — the exact launcher classes, the compounding floor break, the non-string-dial + case-variant root causes; the bar this checkpoint must clear)
    - .planning/phases/25-governance-on-a-dial/25-05-RED-baseline.txt and 25-05-GREEN-proof.txt (the both-direction proof the independent probe reproduces vs the committed `.js`)
    - hooks/admission-guard.js and scripts/context-io.js (the COMMITTED artifacts the probe attacks — never the `.ts`, never the author suites)
  </read_first>
  <what-built>The round-2 gap-closure hardening of the GOV-01 un-forgeable tier: the effective-command-word resolver + fail-closed unresolvable tail + self-set re-ordering (Task 25-05-01), the non-string-dial canonicalization + case-insensitive high-severity classification at both tiers (Task 25-05-02), and the HOOK-tier non-string-dial / case-variant sweep + anti-whack-a-mole command-resolution fuzz + RED→GREEN proof artifacts (Task 25-05-03). This is the LAST task of the round-2 gap-closure plan. Per [[grugops-safety-invariant-green-suite-insufficient]] and D-12, a passing suite is necessary but NOT sufficient for this safety floor — the author's 31+88 and then 174-case suites passed and STILL missed these bypasses TWICE (this is the 6th green-suite-insufficient catch of v2.0). This checkpoint HARD-STOPS until an INDEPENDENT opus-grade adversary (not the authoring executor's own suites) has reproduced the closure. Do NOT declare closure on a green suite alone.</what-built>
  <how-to-verify>
    An INDEPENDENT opus-grade probe must run BOTH angles (the P23 split — guard LOGIC and guard INPUT SURFACE are different blind spots; both earned their keep on BOTH prior red-team passes of this exact phase) and reproduce ALL of the following against the COMMITTED artifacts (`hooks/admission-guard.js`, `scripts/context-io.js`), never the `.ts` and never the author's own `admission-guard.test.ts` / `floor-invariance.test.ts` / `context-io.test.ts`:
    1. INPUT-SURFACE angle — each round-2 launcher class now DENIES a gated high-severity un-approved admit vs the committed `admission-guard.js`: (a) a command-modifier-builtin wrapper (`command`/`exec`/`nice`/`time`/`nohup`/`xargs node …admit`), (b) a path/basename launcher (`/usr/local/bin/node …admit`, `./node …admit`), (c) the alt binary `nodejs …admit`, (d) a split/partial-quoted launcher (`no"de" …admit`, `n''ode …admit`), (e) a brace-group `{ node …admit; }`. AND the unresolvable admit-shape tail GATES: an admit-shape behind `eval` / `sh -c`/`bash -c` body / env-indirection → DENY. AND the over-block-clean angle is preserved: an inert heredoc body line, a quoted/echoed mention, a `#`-commented mention, and a dynamic command with NO admit reference (`eval "echo hi"`, `bash -c "ls"`) → ALLOW. AND the adversary INVENTS at least one launcher/obfuscation shape the author did NOT enumerate (e.g. a `$SHELL -c` body, an eval-base64 admit-shape, a deeper builtin chain, or a path+quote combination) — it must be caught structurally (gated or denied), proving the resolver is ONE authority over command-resolution, not an anchor list.
    2. LOGIC angle — the dial and the severity classification close: every PRESENT non-string `human_admission` value ({`true`, `1`, `null`, `["all"]`, `{}`, a non-object `context`}) DENIES a gated admit (gate-or-stricter, governance not silently off, `source="ok"`); a genuinely ABSENT config ALLOWs a routine admit (lean preserved); a case-variant `by` ({`Security-NFR`, `SECURITY-NFR`}) is classified high-severity so under `human_admission: high-severity` the hook DENIES and admit()'s forged-stamp backstop REFUSES a self-authored `human:NAME` stamp on it. The GAP-B floor: an inline self-set behind any wrapper (`GRUGOPS_ADMISSION_APPROVED_BY=eve command node …admit`) DENIES even on a matcher false-negative.
    3. BOTH-DIRECTION RED-vs-committed-`.js`: the clean case (routine / hook-disposed admit) ALLOWs; the zero-config absent lean reads `off` and ALLOWs a routine admit; each planted round-2 bypass DENIES naming the note. Confirm against `25-05-RED-baseline.txt` (pre-fix ALLOWed the bypass) and `25-05-GREEN-proof.txt` (post-fix DENIES). The probe reproduces these independently, not from the author's suite.
    4. FLOOR + freshness: the four named floor invariants still hold at every dial value (refuse-self, no-fabrication/never-rewrite, `quality.test_integrity` has no `off`, `hooks/guard.ts` byte-frozen via `git diff --quiet hooks/guard.ts` exit 0 and blob `3501810e21308e4b7e219679a6ca30dace9b5d66`); `npm run freshness` exits 0; WR-01 did not false-positive on the resolver/reader edits or the new artifacts.
    5. The probe's findings (the exact reproduced commands, the invented obfuscation shape, the deny/gate/allow outcomes, the non-string-dial / case-variant / self-set-behind-a-wrapper results) are DOCUMENTED in `25-05-SUMMARY.md` — reproduced by an independent adversary, NOT self-reported as a green suite.
  </how-to-verify>
  <acceptance_criteria>
    - (a) Each round-2 launcher class (builtin-wrapper / path-basename / nodejs / split-quote / brace-group) + an adversary-invented fresh obfuscation shape DENY (or GATE, for an unresolvable shape) vs the committed `admission-guard.js`; the heredoc/quoted/comment mentions and a no-admit dynamic command ALLOW — reproduced by an INDEPENDENT input-surface probe, not the author's suite (D-12, P23 input-surface angle).
    - (b) Every present non-string `human_admission` value DENIES a gated admit while an absent config ALLOWs routine; a case-variant `by` is classified high-severity (hook DENIES, admit() backstop REFUSES the forged stamp); an inline self-set behind a wrapper DENIES — reproduced by an INDEPENDENT logic probe (D-12, P23 logic angle).
    - (c) The both-direction proof matches `25-05-RED-baseline.txt` (pre-fix ALLOW) → `25-05-GREEN-proof.txt` (post-fix DENY naming the note; unresolvable-tail GATE; inert/zero-config ALLOW), reproduced vs the committed `.js`.
    - (d) The four floor invariants hold; `git diff --quiet hooks/guard.ts` exits 0; `npm run freshness` exits 0; WR-01 no false-positive; findings documented in `25-05-SUMMARY.md` — independently reproduced, not self-reported.
  </acceptance_criteria>
  <action>
    HARD STOP — do NOT auto-pass on a green author suite (the exact D-12 / green-suite-insufficient failure mode this phase already hit TWICE). Dispatch (or hand off to the human to dispatch) an INDEPENDENT opus-grade red-team — an adversary with different blind spots than the authoring executor — to reproduce (a)–(d) against the COMMITTED `hooks/admission-guard.js` and `scripts/context-io.js` (never the `.ts`, never the author's own suites). The probe MUST run BOTH a LOGIC probe (a non-string dial / a case-variant `by` / a config-shape edge / a self-set-behind-a-wrapper opening a hole) AND an INPUT-SURFACE attack (a launcher obfuscation escaping the resolver, or an inert mention reading as a live admit) — these are the distinct blind spots the P23 lesson and THIS phase's TWO prior red-teams proved both matter. The adversary should INVENT at least one launcher / obfuscation shape the author did NOT enumerate (test the resolver as ONE authority over command-resolution rather than an anchor list — e.g. a `$SHELL -c` body, an eval-base64 admit-shape, a deeper builtin chain) and confirm it is caught structurally (denied or gated). Record the reproduced commands, the invented shape, the clean=allow / planted=deny outcomes, the non-string-dial / case-variant / self-set results, and confirm guard.ts byte-frozen + freshness 0 in `25-05-SUMMARY.md`. Then surface the result to the human for the resume signal. This task is the phase safety floor — it blocks until the independent both-angle proof is in hand.
  </action>
  <verify>
    <human-check>An independent opus-grade red-team has reproduced (a)–(d) vs the committed `.js`: each round-2 launcher class + an invented obfuscation shape DENY/GATE and the heredoc/quoted/comment mentions + a no-admit dynamic command ALLOW (input-surface angle); every present non-string dial DENIES while an absent config ALLOWs routine + a case-variant `by` is gated high-severity (hook DENY + admit() REFUSE) + an inline self-set behind a wrapper DENIES (logic angle); the both-direction proof matches the RED→GREEN artifacts; the four floor invariants hold with `git diff --quiet hooks/guard.ts` exit 0 and freshness 0; findings recorded in `25-05-SUMMARY.md`. NOT a self-reported author green suite (D-12).</human-check>
  </verify>
  <resume-signal>Type "approved" once the independent both-angle red-team has reproduced (a)–(d) above and the findings (incl. the invented obfuscation shape) are recorded in 25-05-SUMMARY.md, or describe the bypass / false-positive found. Do NOT approve on a green author suite alone (D-12, the terminal project lesson — this phase already hit it TWICE).</resume-signal>
  <done>An independent opus-grade red-team (BOTH logic and input-surface angles) has reproduced the round-2 closure vs the committed `.js`: every round-2 launcher obfuscation (incl. an invented shape) is DENIED/GATED when gated/un-approved, the unresolvable admit-shape tail GATES, the inert mentions are ALLOW, every present non-string dial fails closed while an absent config stays lean, a case-variant `by` is gated high-severity at both tiers, and the inline self-set behind a wrapper is refused — the both-direction RED→GREEN proof holds, the four floor invariants and guard.ts byte-frozen status hold, findings are documented in `25-05-SUMMARY.md`, and the human approved; SC1+SC3 are closed by an independent adversary, not a green suite (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bash `tool_input.command` → admission-guard hook | The command string is untrusted and adversarial. After 25-04 solved segmentation, the agent may still obfuscate the EFFECTIVE command word — wrap the launcher in a command-modifier builtin, give it as a path, alias it (`nodejs`), split it across quotes (`no"de"`), put it in a brace group, or hide it behind dynamic evaluation (`eval`, `sh -c`, env-indirection) — to slip a real `admit` PAST the literal-launcher matcher. The hook must resolve the effective command word and fail closed on an unresolvable admit-shape tail. |
| governance config value → admission-guard hook / admit() | `human_admission` is an untrusted JSON value; a PRESENT non-string value (true/1/null/array/object) or a non-object config shape must NEVER coerce to an off-equivalent ALLOW — only the exact JSON string `off` is off-equivalent. A genuinely absent config stays lean. |
| note `by` authoring role → severity classification | `by` is an untrusted string; a case-variant (`Security-NFR`) must NOT escape the case-sensitive high-severity classification at either tier (hook or admit()). |
| inline approval-var set → refuse-self-set (D-01 floor) | A self-set of the approval var behind any wrapper must be refused INDEPENDENT of whether the matcher recognizes the launcher — a matcher false-negative must not reopen the floor invariant. |
| admit() → audit ledger | (Preserved from 25-03, unchanged here) Under `retained`, the ledger records the admission record only, never the note body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-14 | Spoofing / Elevation (command-word obfuscation) | a real gated high-severity `admit` whose effective launcher is hidden behind a builtin-wrapper / path / `nodejs` / split-quote / brace-group / dynamic-eval, slipping PAST the literal-launcher matcher | mitigate | Layer an effective-command-word RESOLVER (skip builtins → basename → full-de-quote → brace-group → `nodejs`) on the preserved 25-04 segmentation as ONE authority, and FAIL CLOSED (gate) on the unresolvable admit-shape tail (eval / sh -c body / env-indirection); proven both-direction RED vs the committed `.js` + an anti-whack-a-mole command-resolution class fuzz (Task 25-05-01/03, D-12). |
| T-25-15 | Elevation of Privilege (refuse-self-set floor reopened) | an inline approval-var set behind a wrapper passing because `SELF_APPROVE` runs only AFTER the matcher early-exit | mitigate | Evaluate the inline-self-set check INDEPENDENT of / in front of the matcher early-exit so it DENIES on any matcher false-negative (Task 25-05-01, GAP-B; D-01). |
| T-25-16 | Elevation of Privilege (non-string-dial coercion) | a PRESENT non-string `human_admission` (true/1/null/array/object) or non-object config silently coercing to `off` → governance off at both tiers | mitigate | Canonicalize a present non-string value (and a non-object config shape) to gate-or-stricter in both readers; only the exact JSON string `off` is off-equivalent; the absent-config lean default is preserved; HOOK-tier non-string-dial sweep proves it vs the committed `.js` (Task 25-05-02/03; SC3, D-12). |
| T-25-17 | Spoofing (case-variant severity escape) | a case-variant `by` (`Security-NFR`) escaping the case-sensitive high-severity gate → a forged `human:eve` admitted under `high-severity` | mitigate | Case-insensitive (trimmed) high-severity role match at BOTH tiers (hook:440 + admit:929) without broadening the 3:3 role set; HOOK-tier case-variant sweep proves it (Task 25-05-02/03; SC1, D-6/D-12). |
| T-25-18 | Denial of Service (over-block regression) | the unresolvable-tail GATE or the resolver over-blocking an inert mention or an unrelated dynamic command | mitigate (fail-safe) | The unresolvable-tail GATE fires ONLY on the admit shape (context-io reference + admit verb); inert heredoc/quoted/comment mentions and no-admit dynamic commands stay ALLOW; the over-block-clean angle is reproduced in the fuzz + the independent red-team (Task 25-05-01/03/04). |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | N/A — this gap-closure installs NO external package (RESEARCH Package Legitimacy Audit: not applicable; typescript/vitest already present). No install task exists. |
</threat_model>

<verification>
- `npm run build && npm run freshness` exits 0 (committed `admission-guard.js` + `context-io.js` are fresh, byte-faithful tsc builds; 0 drift).
- `git diff --quiet hooks/guard.ts` exits 0 (deploy guard byte-frozen, blob `3501810e21308e4b7e219679a6ca30dace9b5d66`, D-02).
- `npx vitest run hooks/admission-guard.test.ts` green: each round-2 launcher class DENIES; the unresolvable admit-shape tail GATES; an inline self-set behind a wrapper DENIES; a case-variant `by` is gated high-severity; a present non-string dial DENIES while an absent config ALLOWs routine; inert heredoc/quoted/comment mentions ALLOW; case count >= guard.test.ts.
- `npx vitest run scripts/context-io.test.ts` green: the non-string-dial canonicalization (gate-or-stricter, not `off`) + the case-insensitive forged-stamp backstop (a case-variant high-severity self-authored `human:NAME` stamp REFUSES under `human_admission != off`, names the fault, no rewrite); existing D-04 + audit-ledger + SC2 cases still green.
- `npx vitest run scripts/floor-invariance.test.ts` green: the HOOK-tier non-string-dial + case-variant `by` sweeps + the command-resolution class fuzz; the four named floor invariants hold at every value.
- `npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial; D-13 no kit-content false-positive).
- `npx vitest run --exclude '**/scripts/e2e/**'` green (full non-e2e regression — bare `npm test` deliberately NOT run; it triggers the live claude-CLI e2e lane).
- `25-05-RED-baseline.txt` + `25-05-GREEN-proof.txt` capture the both-direction proof vs the committed `.js` (extending the 25-04 row format).
- INDEPENDENT both-angle red-team (Task 25-05-04, before /gsd-verify-work): confirms SC1+SC3 round-2 closure adversarially — not self-reported green (D-12).
</verification>

<success_criteria>
- SC1 (GAP-A + GAP-B + GAP-D): the GOV-01 human gate is no longer forgeable across command-RESOLUTION — the effective-command-word resolver catches every round-2 launcher obfuscation (builtin-wrapper / path-basename / nodejs / split-quote / brace-group) and GATES the unresolvable admit-shape tail; the inline self-set behind any wrapper is refused independent of matcher recognition; a case-variant `by` is classified high-severity at both tiers; the hook stays the only grant path.
- SC3 (GAP-C): the un-dialable floor is no longer bypassable by a config TYPE — a present non-string `human_admission` (and a non-object config shape) gates-or-stricter while a genuinely absent config stays lean (zero-config preserved); the four named floor invariants hold at every value; the dials only TIGHTEN.
- Fail-safe (no over-block regression): the 25-04 CR-01-inverse fail-safe is preserved — inert heredoc/quoted/comment mentions and no-admit dynamic commands ALLOW; the unresolvable-tail GATE fires only on the admit shape.
- Proof (D-12): both-direction RED→GREEN vs the COMMITTED `.js` (captured artifacts, extending the 25-04 format) + an anti-whack-a-mole command-resolution class fuzz + an INDEPENDENT both-angle (logic + input-surface) opus-grade red-team that invents an un-enumerated obfuscation shape — not a green author suite. `hooks/guard.ts` byte-frozen; freshness 0; SC2 + GOV-02 ledger + the 25-04 segmentation preserved untouched.
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-05-SUMMARY.md` when done. Record: the effective-command-word resolver's structure (the single command-resolution authority layered on the preserved 25-04 segmentation, the builtins it skips, the basename/full-de-quote/brace-group/nodejs handling, and the unresolvable-tail GATE) and confirmation the 25-04 tokenizer is intact; the GAP-B self-set re-ordering; the chosen non-string-dial sentinel and confirmation the absent-config lean default is unchanged (GAP-C); the case-insensitive high-severity classification at both tiers without broadening the role set (GAP-D); the value set swept at the HOOK tier (non-string dials + case-variant roles) and the command-resolution fuzz matrix dimensions; the RED→GREEN proof artifact paths; confirm `git diff --quiet hooks/guard.ts` clean and freshness 0; and the INDEPENDENT both-angle red-team's reproduced commands + the invented obfuscation shape + outcomes. Flag for the verifier that SC1+SC3 closure REQUIRES the independent red-team — the sweeps/fuzz are necessary, not sufficient (D-12, this phase's 6th green-suite-insufficient catch). Note that SC2 + the GOV-02 audit ledger (25-01/02/03) and the 25-04 segmentation are PRESERVED untouched.
</output>

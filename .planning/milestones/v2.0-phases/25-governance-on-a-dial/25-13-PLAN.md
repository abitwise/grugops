---
phase: 25-governance-on-a-dial
plan: 13
type: execute
wave: 13
depends_on: ["25-12"]
gap_closure: true
autonomous: false
requirements: [GOV-01, GOV-02]
files_modified:
  - scripts/context-io.ts
  - scripts/context-io.js
  - hooks/admission-guard.ts
  - hooks/admission-guard.js
  - scripts/admission-server.ts
  - scripts/admission-server.js
  - scripts/context-io.test.ts
  - hooks/admission-guard.test.ts
  - scripts/admission-server.test.ts

must_haves:
  truths:
    - "ONE shared kind authority normalizeKind(raw) exists in scripts/context-io.ts and is CONSULTED by parseNote AND isGatedNote AND the admission-guard hook AND the admission-server boundary — the gate's notion of finding-or-not is byte-identical to what parseNote persists (the round-8 GAP-R7-1 Lever-1 fix; D-12 one-authority lesson)."
    - "kind:\"finding \" (trailing space), \"\\tfinding\", \" finding\" (leading), and any in-enum whitespace variant gate EXACTLY as the canonical finding: the hook DENYs under an active dial with no env, and the structured channel (admitAndAppend) REFUSES the gated note lacking a real human:NAME stamp — proven RED→GREEN vs the COMMITTED admission-guard.js and the committed combiner."
    - "admit()'s D-04 severity classification calls the single-source isHighSeverityRole(scalars.by) — NOT a separate .trim().toLowerCase() membership test — so by:\"security- nfr\" (internal space), NFKC/zero-width/case variants are refused by admit() AND gated by the hook (GAP-R7-1 Lever-2 fix; D-04/D-06 single-source). This UNFREEZES admit() deliberately (per D-12 the freeze froze a strictly-weaker duplicate classifier — the trap)."
    - "The ONLY HIGH_SEVERITY_ROLES membership test in scripts/context-io.ts lives inside isHighSeverityRole (exactly one .includes(...) site) — admit()'s duplicate weaker classifier is gone (single-source severity authority, anti-drift)."
    - "ADMIT_FROZEN_SHA256 in scripts/context-io.test.ts is RE-PINNED to the new admit() span hash and the freeze test is GREEN at the new baseline; the freeze comment (the block above the constant) records the deliberate round-8 unfreeze + WHY (admit() now routes through single-source isHighSeverityRole; the freeze re-locks at the new baseline so future drift still goes RED). The freeze stays a structural guard — re-pinned, never deleted/skipped."
    - "The admission-server boundary canonicalizes args.kind via the SAME normalizeKind then treats it consistently against the NOTE_KINDS enum (a padded kind that normalizes into the enum is accepted as its canonical form; one that does not is rejected before persistence) — the persisted note's kind shape can never differ from the gate's view (defense-in-depth)."
    - "The full negative+positive matrix over kind (canonical x whitespace-padded) x by (high-sev x internal-space/NFKC x routine) x dial (off/high-severity/all) x verified_by (none/§14-gate/human:NAME) x env (set/unset/mismatch) is correct at BOTH the hook tier and the combiner/admit tier."
    - "Freshness is 0: the committed scripts/context-io.js, hooks/admission-guard.js, scripts/admission-server.js are faithful rebuilds of their .ts (npm run freshness reports no drift); no new host runtime dependency was added."
  prohibitions:
    - "hooks/guard.ts is byte-unchanged — git hash-object == 3501810e21308e4b7e219679a6ca30dace9b5d66 and git diff --quiet exit 0 (D-02; the prod-deploy guard is NEVER touched). guard.ts's own freeze is SEPARATE from admit()'s and stays untouched."
    - "No severity regression: isHighSeverityRole is a STRICT SUPERSET of admit()'s former .trim().toLowerCase() membership test, so admit() now refuses a strict superset of what it refused before — NO admit() refusal that fired pre-fix may now pass."
    - "The deleted command-string parser stays deleted (liveTokens grep == 0 across scripts/ and hooks/, .ts and .js); SC2 config 3-surface lockstep stays byte-identical (kit == seed); the appendAuditLedger writer body is unchanged (only the isHighSeverity ARG it receives is now single-sourced); the four SC3 floor invariants + the hook's fail-closed-on-non-canonical-dial/corrupt-config posture are preserved."
    - "ANTI-WHACK-A-MOLE (HARD): the fix MUST NOT add a new literal/spelling to any denylist or enumerate whitespace forms. It consults the format's own parser (normalizeKind, the SAME normalization parseNote applies) and the ONE severity classifier (isHighSeverityRole) — there must be nothing left to drift. A fix shaped as 'recognize one more padded spelling' is rejected."
    - "The freeze test is RE-PINNED, never deleted, never skipped (no it.skip / xit), never weakened to a non-structural check."
  artifacts:
    - "scripts/context-io.ts — new export normalizeKind; parseNote consults it; isGatedNote consults it; admit() D-04 routes through isHighSeverityRole"
    - "hooks/admission-guard.ts (+ committed .js) — kind check consults normalizeKind"
    - "scripts/admission-server.ts (+ committed .js) — handleProposeNote enum-at-boundary normalize+validate"
    - "scripts/context-io.test.ts — re-pinned ADMIT_FROZEN_SHA256 + updated freeze comment + GAP-R7-1 unit/matrix coverage"
    - "hooks/admission-guard.test.ts + scripts/admission-server.test.ts — held-out RED→GREEN Lever-1/Lever-2 coverage vs the committed .js"
  key_links:
    - "parseNote (the format parser) -> normalizeKind -> isGatedNote + admission-guard hook + admission-server: the kind-view is single-source and cannot diverge"
    - "isHighSeverityRole (the one severity classifier) <- isGatedNote AND admit()'s D-04 AND appendAuditLedger's severity label: the severity-view is single-source"
    - "admit() span -> ADMIT_FROZEN_SHA256: the re-pinned structural freeze re-locks the deliberately-unfrozen admit()"
---

<objective>
Close GAP-R7-1 — the 12th green-suite-insufficient catch of v2.0 — by UNIFYING the two classifier
divergences the round-7 "single-source" fix never reconciled, applying the Phase-22/round-8 terminal
lesson FULLY: ONE format-aware classifier authority for `kind`, ONE for severity, both consulted by the
store (parseNote), the gate (the admission-guard hook + `isGatedNote`), AND the in-script backstop
(`admit()`). The four must agree by construction, not by coincidence.

The live bypass (orchestrator-confirmed, RT-B bash-grounded): a high-severity finding admits through the
sanctioned `mcp__grugops__propose_note` channel with the hook ALLOWing, NO human env, NO human stamp,
and the GOV-02 ledger mislabelling it `severity:"routine"` — via TWO levers:
- **Lever 1 — `kind:"finding "` (whitespace-padded):** the hook (admission-guard.ts ~line 162) and
  `isGatedNote` (context-io.ts ~line 1375) RAW-compare `kind !== "finding"` → treat the padded kind as a
  SOFT (non-finding) kind ⇒ ALLOW / not-gated, routing the note onto admit()'s NON-gated path. But
  `parseNote` (context-io.ts ~line 286) TRIMS the value ⇒ the store persists+validates a REAL `finding`.
  The gate's kind-view is narrower than the format's own parser.
- **Lever 2 — `by:"security- nfr"` (internal space):** `isHighSeverityRole` (context-io.ts ~line 1349,
  NFKC + strip-all-whitespace + lowercase) → high-sev TRUE; admit()'s D-04 (context-io.ts ~line 1005,
  `.trim().toLowerCase()` — edges only) → FALSE. Once Lever 1 routes the note to admit(), its BYTE-FROZEN,
  strictly-WEAKER `by` classifier is the SOLE high-sev defense and the internal space is invisible. The
  "admit() byte-frozen" prohibition is the TRAP — it froze a weaker duplicate classifier.

Purpose: deliver the SC1 invariant (the sanctioned channel is mechanically un-forgeable on Claude Code)
and the GOV-02 ledger's honesty (no high-sev admission mislabelled routine). Honors the HUMAN-DECIDED
round-8 scope (2026-06-28): UNIFY ALL CLASSIFIERS and UNFREEZE admit() — the freeze was the trap. Honors
D-01 (un-forgeable hook lives outside the agent process — unchanged), D-04 (in-script defense tier — now
single-sourced), D-06 (severity = authoring role, one classifier), D-12 (prove STRUCTURALLY + an
independent red-team; a green suite is necessary-but-NOT-sufficient).

Output: a single shared kind authority `normalizeKind`; admit()'s D-04 routed through `isHighSeverityRole`
with the freeze deliberately re-baselined; the server enum-at-boundary; held-out RED→GREEN proof vs the
committed `.js`; freshness 0; and a BLOCKING ≥2-independent-red-team checkpoint (the D-12 gate the executor
must STOP at without self-approving).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-VERIFICATION.md
@.planning/phases/25-governance-on-a-dial/25-12-SUMMARY.md

# The four classifier surfaces that must be unified (read the named line ranges):
@scripts/context-io.ts
@hooks/admission-guard.ts
@scripts/admission-server.ts
@scripts/context-io.test.ts
@hooks/admission-guard.test.ts
</context>

<artifacts_this_phase_produces>
## New / changed symbols downstream agents and tests depend on

- **`normalizeKind(raw: string | undefined): string`** — NEW exported function in `scripts/context-io.ts`.
  The single shared kind authority. Its normalization MUST equal exactly what `parseNote` applies to the
  kind value (inspect parseNote line ~286: the kv loop does `kv[2].trim()` — so `normalizeKind` is
  `(raw ?? "").trim()`, no case change, since parseNote does NOT lowercase). parseNote ITSELF calls it,
  and `isGatedNote`, the admission-guard hook, and the admission-server import/consult it.
- **`admit()`** — D-04 severity classification now calls `isHighSeverityRole(scalars.by ?? "")` instead of
  the inline `(HIGH_SEVERITY_ROLES …).includes((scalars.by ?? "").trim().toLowerCase())`. admit()'s span
  changes ⇒ `ADMIT_FROZEN_SHA256` is re-pinned.
- **`ADMIT_FROZEN_SHA256`** (in `scripts/context-io.test.ts`) — re-pinned to the new admit() span hash;
  the freeze comment records the deliberate round-8 unfreeze.
- No new modules, no new exports beyond `normalizeKind`, no new host runtime dependency.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Single-source the KIND classification (Lever-1 fix) — normalizeKind consulted by parser + gate + server</name>
  <files>scripts/context-io.ts, hooks/admission-guard.ts, scripts/admission-server.ts</files>
  <read_first>
    - scripts/context-io.ts lines 245-324 (parseNote — note line ~286 `const val = kv[2].trim()`: the
      kind value is already trimmed generically; this IS the normalization to extract)
    - scripts/context-io.ts lines 1357-1384 (isGatedNote — line ~1375 raw `if (kind !== "finding") return false`)
    - scripts/context-io.ts lines 46-55 (NOTE_KINDS enum, exported)
    - hooks/admission-guard.ts lines 145-164 (the kind read at ~159 + the raw `if (kind !== "finding") process.exit(0)` at ~162; it already imports from ../scripts/context-io.js)
    - scripts/admission-server.ts lines 136-173 (handleProposeNote — line ~140 `kind: args.kind as NoteKind` forwards RAW; the import block lines 46-51)
  </read_first>
  <behavior>
    - normalizeKind("finding ") === "finding"; normalizeKind(" finding") === "finding"; normalizeKind("\tfinding") === "finding"; normalizeKind("finding\t") === "finding"; normalizeKind(undefined) === ""; normalizeKind("<ZWSP>finding") === "<ZWSP>finding" (where <ZWSP> denotes a literal U+200B zero-width space) (a zero-width char is NOT removed by String.trim() — normalizeKind matches parseNote's trim semantics EXACTLY, so such a value stays a non-finding at BOTH the gate and the store, with no divergence).
    - After the fix, isGatedNote("security-nfr", "finding ", active-config) === true (gated), where pre-fix it returned false.
    - The admission-guard hook, fed structured tool_input {by:"security-nfr", kind:"finding ", verified_by:"§14-gate#x"} under an active dial with no env, DENYs (pre-fix it ALLOWed).
    - admitAndAppend with note.kind:"finding " + a high-sev by + no human:NAME stamp is REFUSED as a gated note (pre-fix it slipped to the non-gated path).
  </behavior>
  <action>
    Implement the GAP-R7-1 Lever-1 structural fix per the round-8 scope (D-12 one-authority lesson). This
    is NOT a denylist of padded spellings — it consults the format's own parser so there is nothing to drift.

    1. In scripts/context-io.ts, add and EXPORT `normalizeKind`. Inspect parseNote's exact kind handling
       first (the kv loop trims every value via `.trim()`), then make normalizeKind match it EXACTLY:
       return the trimmed string (no case folding — parseNote does not lowercase). Document in a head
       comment that this is the SINGLE kind authority, that it MUST equal parseNote's persisted kind value
       byte-for-byte, and that the gate consults it so the gate's finding-view can never be narrower than
       the store's (the GAP-R7-1 Lever-1 class). Place it beside the other single-source classifiers.

    2. Make parseNote ITSELF consult normalizeKind so the authority is single-source, not a re-implemented
       trim: after the kv loop builds `scalars` (before the return at ~line 323), if `scalars.kind` is
       defined, set `scalars.kind = normalizeKind(scalars.kind)`. This is behavior-identical to today
       (already trimmed) — its purpose is to route parseNote's kind THROUGH the shared helper so the gate
       (same helper) and the store cannot drift. The existing parseNote tests MUST stay green (prove the
       no-op).

    3. In isGatedNote (~line 1375), replace the raw `if (kind !== "finding") return false` with
       `if (normalizeKind(kind) !== "finding") return false`. No other line of isGatedNote changes (the
       dial composition + fail-closed posture is preserved verbatim).

    4. In hooks/admission-guard.ts: import normalizeKind from ../scripts/context-io.js (add to the existing
       import on line ~66). Compute the canonical kind via normalizeKind before the finding compare at
       ~line 162 (i.e. compare the normalized kind, not the raw arg). Do NOT change the missing/empty-kind
       fail-closed branch (lines ~148-158) or the by/gated/env logic below it — only the finding-equality
       check consults the authority now.

    5. In scripts/admission-server.ts handleProposeNote (~line 136): import normalizeKind (add to the
       import on lines ~46-51). Canonicalize `args.kind` via normalizeKind, then enforce the NOTE_KINDS
       enum at the boundary (defense-in-depth): a kind that normalizes INTO the enum is built into the
       NoteInput as its CANONICAL form (so the persisted note's kind shape equals the gate's view); a kind
       that does NOT normalize into the enum returns an isError ToolResult naming the fault BEFORE
       admitAndAppend runs. Per the round-8 contract: normalize-then-treat-consistently — do NOT reject
       legit padded input that normalizes into the enum.

    Cite: D-01 (the un-forgeable hook lives outside the agent process — unchanged), D-04/D-06 (the gate
    consults the format authority), D-12 (consult the parser; one authority; nothing to drift). Do NOT
    touch hooks/guard.ts. Do NOT re-introduce any command-string parser. Clear professional voice in all
    new comments (governance/safety surface — never caveman voice, per CLAUDE.md).
  </action>
  <verify>
    <automated>npx vitest run scripts/context-io.test.ts hooks/admission-guard.test.ts scripts/admission-server.test.ts</automated>
    Also: `grep -c "normalizeKind" scripts/context-io.ts` >= 3 (export + parseNote + isGatedNote);
    `grep -c "normalizeKind" hooks/admission-guard.ts` >= 1; `grep -c "normalizeKind" scripts/admission-server.ts` >= 1.
  </verify>
  <done>normalizeKind exists and is consulted by parseNote, isGatedNote, the hook, and the server; the existing parseNote suite stays green; the kind-view is single-source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single-source the SEVERITY classification + deliberately UNFREEZE and re-baseline admit() (Lever-2 fix)</name>
  <files>scripts/context-io.ts, scripts/context-io.test.ts</files>
  <read_first>
    - scripts/context-io.ts lines 991-1043 (admit()'s D-04 block — the isHighSeverity computation at ~1003-1005 uses `(HIGH_SEVERITY_ROLES as readonly string[]).includes((scalars.by ?? "").trim().toLowerCase())`)
    - scripts/context-io.ts lines 1341-1355 (isHighSeverityRole — NFKC + strip-all-whitespace/zero-width + lowercase; the STRONGER, single-source classifier)
    - scripts/context-io.test.ts lines 1712-1753 (the W-B admit() byte-freeze: extractAdmitSpan brace-matches `export function admit(` to its closing brace; ADMIT_FROZEN_SHA256 at ~1722)
  </read_first>
  <behavior>
    - admit(task, <finding by:"security- nfr" verified_by:"§14-gate#<live-id>">, active dial) REFUSES (internal-space by now classifies high-sev via isHighSeverityRole), where pre-fix it ADMITTED.
    - admit(task, <finding by:"security-nfr">, dial off) still ADMITS (no regression at off).
    - admit(task, <routine finding by:"software-engineer">, active dial) still ADMITS (routine never gated by D-04).
    - Every by value that pre-fix admit() refused as high-sev STILL refuses (strict-superset; no regression).
    - The freeze test asserts admit()'s span hash == the NEW re-pinned ADMIT_FROZEN_SHA256 (GREEN).
  </behavior>
  <action>
    Apply the GAP-R7-1 Lever-2 structural fix per the HUMAN-DECIDED round-8 scope: route admit()'s D-04
    through the ONE severity classifier and re-lock the freeze at the new baseline.

    1. In admit() (~line 1003-1005), replace the inline membership test
       `(HIGH_SEVERITY_ROLES as readonly string[]).includes((scalars.by ?? "").trim().toLowerCase())`
       with a call to the single-source classifier `isHighSeverityRole(scalars.by ?? "")`. After this edit
       the ONLY HIGH_SEVERITY_ROLES membership test in the file lives inside isHighSeverityRole. Keep the
       `scalars.kind === "finding"` guard (scalars.kind is parseNote's already-normalized output — admit()
       consumes the parser's value, which is the authority). Preserve the rest of the D-04 branch (the
       active-dial check, the forged-self-stamp backstop, the NAMING-the-fault message, the no-rewrite
       floor) verbatim. Update admit()'s nearby comments to state that severity is now classified by the
       single-source isHighSeverityRole (strictly stronger than the former `.trim().toLowerCase()` — it
       catches internal-space/NFKC/zero-width forms), so the in-script tier and the hook tier cannot
       diverge. This edit DELIBERATELY UNFREEZES admit() (D-12: the byte-freeze froze a strictly-weaker
       duplicate classifier — the trap). Do NOT touch the value-canonicalization `.trim().toLowerCase()`
       at line ~148 (a different function) or isHighSeverityRole's body.

    2. Re-baseline the freeze in scripts/context-io.test.ts. After the admit() edit, recompute admit()'s
       span hash with the SAME extraction the test uses (the `extractAdmitSpan` brace-match over the .ts
       source), then re-pin ADMIT_FROZEN_SHA256 (~line 1722) to that value. Recompute deterministically,
       e.g.:
         node -e "const{readFileSync}=require('node:fs');const{createHash}=require('node:crypto');const s=readFileSync('scripts/context-io.ts','utf8');const a=s.indexOf('export function admit(');let d=0,e=-1;for(let i=s.indexOf('{',a);i<s.length;i++){const c=s[i];if(c==='{')d++;else if(c==='}'){d--;if(d===0){e=i;break;}}}console.log(createHash('sha256').update(s.slice(a,e+1),'utf8').digest('hex'))"
       Pin exactly that hex. UPDATE the freeze comment block (the lines above the constant, ~1712-1723) to
       record the deliberate round-8 unfreeze + WHY: the freeze previously froze a strictly-weaker duplicate
       `by` classifier (the GAP-R7-1 Lever-2 trap); admit() now routes through the single-source
       isHighSeverityRole; the freeze re-locks at the new baseline so any FUTURE drift to admit() still goes
       RED. State plainly that the freeze stays a structural guard — re-pinned, never deleted/skipped, and
       that hooks/guard.ts's separate freeze is untouched.

    Cite: D-04 (in-script defense-in-depth, now single-sourced), D-06 (severity = role), D-12 (structural
    proof; deliberate, recorded unfreeze). No-fabrication: NEVER weaken or skip the freeze test — re-pin it.
  </action>
  <verify>
    <automated>npx vitest run scripts/context-io.test.ts</automated>
    Also: `grep -c "isHighSeverityRole(" scripts/context-io.ts` >= 2 (the def + admit()'s call + isGatedNote's call);
    the HIGH_SEVERITY_ROLES `.includes(...)` membership test appears EXACTLY once and only inside isHighSeverityRole
    (verify by reading lines ~1349-1355 and confirming admit() no longer contains an inline `.includes` on HIGH_SEVERITY_ROLES);
    the freeze test "admit()'s function span byte-hash equals the pinned ... baseline" passes GREEN.
  </automated>
  </verify>
  <done>admit()'s D-04 classifies severity via the single-source isHighSeverityRole; the duplicate weaker classifier is gone; the freeze is re-pinned GREEN with a recorded round-8 rationale; no refusal regresses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Held-out RED→GREEN proof vs the committed .js + full matrix + rebuild + freshness + prohibition gates</name>
  <files>hooks/admission-guard.test.ts, scripts/admission-server.test.ts, scripts/context-io.test.ts, scripts/context-io.js, hooks/admission-guard.js, scripts/admission-server.js</files>
  <read_first>
    - hooks/admission-guard.test.ts lines 1-75 (the runGuard/expectDeny/expectAllow harness that spawns the COMMITTED admission-guard.js with structured stdin; the payload() builder takes {by,kind,verified_by,task})
    - scripts/admission-server.test.ts (the handleProposeNote / combiner harness + temp-root setup pattern)
    - scripts/context-io.test.ts (the rootWithContext temp-root helper used by the governance/admit tests)
    - package.json scripts (build = tsc; freshness = tsc --outDir .tmp-build && node scripts/freshness.js)
  </read_first>
  <behavior>
    - GAP-R7-1 Lever-1 (hook tier): payload({by:"security-nfr", kind:"finding ", verified_by:"§14-gate#x"}) under an active dial, no env → DENY. Same with kind:" finding", "\tfinding". (RED on the pre-fix committed .js; GREEN on the rebuild.)
    - GAP-R7-1 Lever-2 (combiner/admit tier, bash-grounded end-to-end): admitAndAppend / handleProposeNote with note {by:"security- nfr", kind:"finding ", verified_by:"§14-gate#<live-id>"} against a temp root with human_admission:high-severity, audit_retention:retained, and a real live green §14-gate verdict in context → REFUSED, NO note file written, NO line appended to .grugops/audit/admissions.jsonl. (RED pre-fix: note written + ledger line with severity:"routine"; GREEN post-fix: refused.)
    - Controls (each lever necessary): exact by:"security-nfr" + padded kind → admit() D-04 catches it (REFUSED); internal-space by + exact kind:"finding" → the hook DENYs. Positive: env=alice + verified_by:human:alice + a real high-sev finding → ADMITTED stamped human:alice with disposed_by:human:alice in the ledger. Routine finding under high-severity → ADMITTED (no over-block). Off dial → routine admits without a stop.
  </behavior>
  <action>
    Author the held-out RED→GREEN coverage (D-12: the proof is behavioral, vs the COMMITTED .js — a green
    author suite is necessary-but-NOT-sufficient, but the RED→GREEN-vs-committed proof is the minimum bar).

    1. In hooks/admission-guard.test.ts add the Lever-1 hook cases (whitespace-padded kind variants gate as
       finding) using the existing runGuard harness against the committed admission-guard.js. Include the
       Lever-1 control (internal-space by + exact kind) and the positive/over-block controls listed in
       <behavior>.

    2. In scripts/admission-server.test.ts (and/or scripts/context-io.test.ts for admitAndAppend directly)
       add the end-to-end channel cases: build a temp root via the existing helper with
       human_admission:high-severity + audit_retention:retained, seed a real live green §14-gate verdict
       under the task, then drive the COMBINER with the GAP-R7-1 note. Assert: REFUSED, no <id>.md in the
       task notes dir, and NO new line in .grugops/audit/admissions.jsonl. Add the positive admit
       (env-backed human:NAME on the non-CC/self-settable tier is the combiner's stamp check — assert the
       ledger records severity:"high" + disposed_by:human:NAME, NOT routine). Add the matrix described in
       <behavior> as a table-driven test (kind x by x dial x verified_by x env).

    3. Capture the RED→GREEN evidence the phase convention uses: write
       .planning/phases/25-governance-on-a-dial/25-13-RED-baseline.txt (the GAP-R7-1 levers ALLOW/admitted
       against the PRE-fix committed .js — run BEFORE Tasks 1-2 are compiled in, or reconstruct by checking
       out the pre-fix blob) and 25-13-GREEN-proof.txt (all levers DENY/refused post-rebuild; controls and
       positives correct). These are evidence artifacts, not source.

    4. Rebuild the committed .js from .ts (npx tsc) so scripts/context-io.js, hooks/admission-guard.js, and
       scripts/admission-server.js reflect Tasks 1-2. Then assert freshness 0 (npm run freshness) and run
       the prohibition gates.

    Anti-whack-a-mole: the tests assert the STRUCTURAL property (any in-enum whitespace/internal-space/NFKC
    variant gates identically to the canonical form), NOT a fixed list of spellings. Do NOT add a new
    literal to any denylist anywhere.
  </action>
  <verify>
    <automated>npx vitest run --exclude '**/scripts/e2e/**'</automated>
    Then ALL of:
    - `npm run freshness` reports 0 drift (committed .js are faithful rebuilds).
    - `git diff --quiet hooks/guard.ts` exit 0 AND `git hash-object hooks/guard.ts` == 3501810e21308e4b7e219679a6ca30dace9b5d66 (D-02 freeze).
    - `grep -rc "liveTokens" scripts hooks | grep -v ':0$'` prints nothing (the deleted command-string parser stays gone, .ts and .js).
    - `cmp -s agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` exit 0 (SC2 lockstep byte-identical).
    - The W-B admit() freeze test passes (re-pinned baseline GREEN).
    (Do NOT run `npm test` — it triggers the live claude-CLI e2e lane.)
  </automated>
  </verify>
  <done>The GAP-R7-1 levers DENY/refuse vs the committed .js; the full matrix + controls + positives pass; freshness 0; guard.ts byte-frozen; liveTokens=0; SC2 lockstep intact; RED-baseline + GREEN-proof captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: SC1 round-8 closure gate — >= 2 INDEPENDENT bash-grounded opus red-teams vs the committed .js (HARD STOP; a green author suite is NOT proof, D-12)</name>
  <read_first>
    - /Users/olgeroeselg/.claude/projects/-Users-olgeroeselg-Projects-public-grugops/memory/grugops-safety-invariant-green-suite-insufficient.md (12 rounds: a green author suite has been necessary-but-insufficient every time; in round 7 the FIRST red-team false-passed and only the SECOND caught GAP-R7-1 — hence >= 2 independent red-teams this round)
    - .planning/phases/25-governance-on-a-dial/25-VERIFICATION.md post-25-12 banner (the GAP-R7-1 levers being closed) + 25-12-SUMMARY.md (the round-7 outcome)
    - the committed scripts/context-io.js, scripts/admission-server.js, hooks/admission-guard.js (the targets — NEVER the .ts, NEVER the author suites)
  </read_first>
  <action>
    The ORCHESTRATOR (NOT the authoring executor) dispatches >= 2 INDEPENDENT bash-grounded opus-grade
    red-teams against the COMMITTED artifacts (scripts/context-io.js + scripts/admission-server.js +
    hooks/admission-guard.js) per the how-to-verify protocol below. This is a HARD-STOP blocking gate
    (D-12): the executor must NOT auto-pass on the green author suite (it has been
    necessary-but-insufficient 12 straight times), must NOT mark the phase complete, must NOT flip the
    ROADMAP, and must NOT mark GOV-01/GOV-02 complete. Record each red-team's reproduced inputs, the
    novel probe, the allow/deny + persisted/not-persisted outcomes, and the preservation checks in
    25-13-SUMMARY.md; if ANY red-team bash-grounds a bypass, author a gaps_found verdict from the
    reproduction (→ round 9) — no-fabrication, never from a green suite or a single clean pass. Then
    surface to the human for the resume signal.
  </action>
  <verify>
    <human-check>>= 2 independent bash-grounded opus red-teams (one framed as a novel-hole hunt) AND orchestrator self-reproduction have run vs the COMMITTED .js and found NO bypass (GAP-R7-1 Lever-1 + Lever-2 closed; ledger honesty; the round-6/7 wins + SC2 + the four SC3 floor invariants + guard.ts's freeze preserved), recorded in 25-13-SUMMARY.md — NOT a self-reported author green suite (D-12).</human-check>
  </verify>
  <what-built>
    Tasks 1-3 unified the kind authority (normalizeKind consulted by parseNote + isGatedNote + the hook +
    the server) and the severity authority (admit()'s D-04 routed through the single-source isHighSeverityRole,
    admit() deliberately unfrozen and re-baselined), rebuilt the committed .js (freshness 0), and proved the
    GAP-R7-1 levers DENY/refuse RED→GREEN vs the committed .js with a full matrix. The author suite is GREEN.
  </what-built>
  <how-to-verify>
    THIS IS THE BLOCKING D-12 RE-PROOF GATE. A green author suite is NECESSARY BUT NOT SUFFICIENT — this is
    the 12th green-suite-insufficient catch of v2.0, and in round 7 the FIRST independent red-team
    FALSE-PASSED while only the SECOND caught GAP-R7-1 ("sometimes only one angle finds it"). Therefore:

    The EXECUTOR MUST HARD-STOP HERE and MUST NOT self-approve. The executor must NOT mark the phase
    complete, MUST NOT flip the ROADMAP, and MUST NOT mark GOV-01/GOV-02 complete on the author suite alone
    (no-fabrication / no-self-approve gate).

    The ORCHESTRATOR (not the executor) must dispatch >= 2 INDEPENDENT bash-grounded opus red-teams against
    the COMMITTED .js (hooks/admission-guard.js, scripts/context-io.js, scripts/admission-server.js — NEVER
    the .ts, never the author suite), each with its own stdin/JSON-RPC driver and a BASH-GROUNDED oracle:
      - A FORGE counts ONLY IF bash actually appends a high-severity admission line to
        .grugops/audit/admissions.jsonl (or writes a high-sev note into the real context) AND the hook
        ALLOWs / the channel admits with NO human env and NO real human:NAME disposition.
    Frame at least one red-team as a maximally-creative NOVEL-HOLE hunt (the round-7 RT-B framing that
    caught what RT-A missed). Direct the red-teams to probe, at minimum:
      - kind-divergence: every whitespace/zero-width/Unicode variant that could classify differently at the
        gate vs parseNote vs the server boundary; a kind that normalizes into the enum at one surface but not
        another.
      - by/severity-divergence: internal-space / NFKC / homoglyph / case `by` that could split
        isHighSeverityRole from admit()'s now-unified path; confirm no NEW divergence was introduced by the
        unfreeze.
      - ledger honesty: any admission that lands severity:"routine" while actually high-sev, or a forged
        disposed_by.
      - preservation: confirm the round-6/7 wins (path-containment GAP-R6-1, config-root GAP-R6-2,
        honest-scope GAP-R6-3), SC2, the GOV-02 ledger, the four SC3 floor invariants, and guard.ts's freeze
        did NOT regress.
    The orchestrator self-reproduces the cleanest finding. If ANY red-team bash-grounds a bypass → record it
    in 25-VERIFICATION.md + the SUMMARY and route to round 9 (gaps_found). Only if BOTH red-teams + the
    orchestrator find NO bypass is SC1 closure declared for this round.
  </how-to-verify>
  <resume-signal>Type "approved" only after >= 2 independent bash-grounded opus red-teams AND orchestrator self-reproduction return NO bypass vs the committed .js; otherwise report the reproduced bypass for round 9.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent process → sanctioned admission channel (`mcp__grugops__propose_note`) | The agent supplies the note's structured provenance fields (`by`, `kind`, `verified_by`, …); the harness delivers them as final JSON. Untrusted input crosses here. |
| structured args → admission-guard hook (the gate) | The PreToolUse hook reads the FINAL structured `tool_input` + the human-set session env (which the agent's child env cannot reach). The un-forgeable D-01 tier. |
| structured args → admission-server → `context-io` store/`admit()` (the point of effect) | The server builds a NoteInput and calls `admitAndAppend`; `parseNote`/`validate`/`admit()` persist or refuse. The in-script D-04/D-05 tier + the GOV-02 ledger. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-R8-01 | Elevation/Tampering | admission-guard hook + `isGatedNote` (raw `kind !== "finding"`) vs `parseNote` (trims) | high | mitigate | Single-source `normalizeKind` consulted by parseNote AND the gate so the gate's finding-view is byte-identical to what the store persists (Task 1). |
| T-25-R8-02 | Elevation/Tampering | `admit()` D-04 weaker `.trim().toLowerCase()` `by` classifier (the byte-freeze trap) | high | mitigate | Route admit()'s D-04 through the single-source `isHighSeverityRole` (strict superset); deliberately unfreeze + re-baseline the span hash (Task 2). |
| T-25-R8-03 | Spoofing/Repudiation | GOV-02 audit ledger labelling a high-sev admission `severity:"routine"` | high | mitigate | The single-source severity classifier flows into appendAuditLedger's `isHighSeverity` arg, so the ledger label is correct by construction (Tasks 1-2). |
| T-25-R8-04 | Tampering | admission-server forwarding `args.kind` RAW (persisted shape ≠ gate view) | medium | mitigate | Enum-at-boundary: canonicalize via `normalizeKind` then validate against `NOTE_KINDS` before persistence (Task 1). |
| T-25-R8-05 | Tampering (regression) | unfreezing admit() could silently weaken it | high | mitigate | isHighSeverityRole ⊇ the former classifier (no refusal regresses, proven in the matrix); the freeze RE-LOCKS at the new baseline so future drift goes RED (Task 2). |
| T-25-R8-06 | Tampering | same-uid direct-FS forgery BYPASSING the channel (`node -e appendNote/admitAndAppend/emitVerdict`, Write/echo/cp) | high | accept | Documented IRREDUCIBLE pre-existing in-box residual (SC1 §A′); backstop = `autonomy=pr` (a named human holds merge/deploy) + the no-fabrication floor. NOT closable out-of-process on a single-uid box. |
| T-25-R8-SC | Tampering | npm/pip/cargo installs | n/a | mitigate | NO package installs this round — pure source edits to existing TS, zero new host runtime deps (CLAUDE.md). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
## Phase-level checks (after Task 3, before the checkpoint)

- `npx vitest run --exclude '**/scripts/e2e/**'` fully green (context-io, admission-guard, admission-server, floor-invariance suites).
- `npm run freshness` reports 0 drift; committed `.js` rebuilt from `.ts`.
- `git hash-object hooks/guard.ts` == `3501810e21308e4b7e219679a6ca30dace9b5d66` and `git diff --quiet hooks/guard.ts` exit 0 (D-02).
- The W-B admit() freeze test passes at the RE-PINNED baseline (deliberate, recorded round-8 unfreeze).
- `grep -rc "liveTokens" scripts hooks | grep -v ':0$'` prints nothing (the deleted command-string parser stays gone).
- `cmp -s agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` exit 0 (SC2 lockstep).
- The single-source greps: normalizeKind consulted by parseNote + isGatedNote + the hook + the server; the only HIGH_SEVERITY_ROLES membership test lives in isHighSeverityRole.
- RED-baseline + GREEN-proof artifacts captured for the GAP-R7-1 levers vs the committed `.js`.

## The closure gate (the checkpoint)

SC1 round-8 closure is declared ONLY if >= 2 independent bash-grounded opus red-teams + orchestrator
self-reproduction find NO bypass vs the COMMITTED `.js`. The author suite alone NEVER closes it (D-12).
</verification>

<success_criteria>
- GAP-R7-1 is structurally closed: ONE kind authority (normalizeKind) and ONE severity authority
  (isHighSeverityRole) are consulted by the store, the gate, AND the in-script backstop — they agree by
  construction. `kind:"finding "` gates as `finding`; `by:"security- nfr"` is refused by admit() and gated
  by the hook.
- admit() is deliberately unfrozen and re-baselined; no admit() refusal regresses; the freeze re-locks GREEN.
- The server enum-at-boundary normalizes-then-treats-consistently; the persisted kind shape equals the gate's view.
- Freshness 0; guard.ts byte-frozen; liveTokens=0; SC2 lockstep + GOV-02 ledger + the four SC3 floor
  invariants preserved; no new host runtime dep.
- The executor hard-stops at the blocking checkpoint without self-approving; the ROADMAP is NOT flipped and
  GOV-01/GOV-02 are NOT marked complete on the author suite alone.
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-13-SUMMARY.md` when done (executor stops at the
blocking checkpoint; the orchestrator authors the closure/gaps verdict from the independent red-teams,
no-fabrication). Capture `25-13-RED-baseline.txt` and `25-13-GREEN-proof.txt` as RED→GREEN evidence.
</output>

---
phase: 25-governance-on-a-dial
plan: 07
type: execute
wave: 7
gap_closure: true
depends_on: ["25-01", "25-02", "25-03", "25-04", "25-05", "25-06"]
autonomous: false
files_modified:
  - hooks/admission-guard.ts
  - hooks/admission-guard.js
  - hooks/admission-guard.test.ts
  - scripts/floor-invariance.test.ts
  - 25-07-RED-baseline.txt
  - 25-07-GREEN-proof.txt
requirements: [GOV-01, GOV-02]
user_setup: []

must_haves:
  truths:
    - "Leading-run resolution class / SC1 (GOV-01) — the structural INVERT lands: when a LIVE segment carries the admit SHAPE (`segmentHasAdmitShape` — a context-io reference AND the `admit` verb) but the leading-run resolution does NOT terminate at a recognized launcher (`node|npx|tsx|nodejs`) and is NOT one of the already-gating dynamic/substitution words, the command word is treated as UNRESOLVABLE and the segment fails CLOSED (GATE / deny pending a human) — exactly as Class B already does — instead of silently dropping the segment (the round-3 default-ALLOW hole at admission-guard.ts:572-608). The admit shape is the trigger, so a path-form/operand/wrapper/redirection spelling cannot escape (per D-01 un-forgeable tier; vs the COMMITTED `.js`)."
    - "Sub-root (a) modifier OPERAND not consumed / SC1 — `timeout 5 node …admit <hi>` (the flagless textbook `timeout` form; the duration `5` is the leading-run operand the round-3 skip stopped at), `nice -n 5 node …`, `exec -a foo node …`, `xargs -I {} node …`, `env -C /tmp node …`, `env -u PATH node …`, `timeout -k 1 5 node …`, `/usr/bin/timeout 5 node …`, and `nice '-n' '5' node …` all DENY a gated high-severity un-approved admit because the segment carries the admit shape behind an unresolved command word (vs the committed `.js`)."
    - "Sub-root (b) wrapper not in `COMMAND_MODIFIERS` / SC1 — `sudo node …admit <hi>`, `doas node …`, `setsid node …`, `ionice -c2 node …`, `chrt -f 1 node …`, `taskset 0x1 node …` all DENY. The fix does NOT widen `COMMAND_MODIFIERS` (the closed-enumeration anti-pattern this phase keeps hitting) — the admit shape behind any unrecognized command word gates structurally."
    - "Sub-root (c) leading redirection / SC1 — `>/dev/null node …admit <hi>`, `2>/dev/null node …`, and `2>&1 node …` all DENY: a redirection operator/target that the round-3 `liveTokens` reads as the segment's command word now gates because the segment carries the admit shape and the command word does not resolve to a launcher."
    - "Compounding closures / SC1 — at dial `all` even a ROUTINE admit behind a wrapper (`timeout 5 node …admit <routine>`) DENIES (the strictest dial's floor no longer leaks); and Class E REOPENED is re-closed — `node …admit <routine> ; timeout 5 node …admit <hi>` DENIES under `high-severity` because the wrapped high-severity segment is now classified (an unresolvable admit segment per the invert) instead of silently dropped."
    - "Over-block SAFETY (the fail-safe direction) PROVEN — a non-admit wrapper has NO admit shape and stays ALLOW: `timeout 5 node render`, `sudo ls`, `>/dev/null node build`, `2>/dev/null make`, a routine-only multi-admit under `high-severity`, and an inert heredoc/quoted/`#`-comment admit mention behind a wrapper all ALLOW. The invert GATES only when the live segment carries the admit shape (a context-io reference AND the admit verb); a wrapper/operand/redirection with no admit shape is never gated (no false positive)."
    - "PRESERVED — the round-3 closures the 25-06 red-team confirmed SOLID are untouched: Class A path-form launcher (`/usr/bin/nice node`, `command node`, `nodejs`, `no\"de\"`, `{ node …; }`) DENIES; Class B `$()`/backtick command word GATES (and a `$()`/backtick/eval with NO admit shape ALLOWs); Class E per-segment classify-and-gate-if-any DENIES a shielded high-sev across `;`/`&&`/`||`/`|`/`&`/newline/subshell while a routine-only multi-admit ALLOWs; Class F validate()-consistent `by` read gate-or-stricter on a duplicate/indented `by`. The SINGLE `liveAdmitSegments` authority stays the ONE source of truth — no second command walk is reintroduced (`function tokenize` / `noteFileFromCommand` stay deleted, grep=0)."
    - "PRESERVED — the round-2 LOGIC closures stay solid: GAP-B (self-set behind a wrapper / D-01 floor) DENIES even behind `timeout 5`/`nice -n 5` (the `commandHasLiveAdmitShape` shape-scan is resolution-independent and unchanged); GAP-C (non-string / non-object / corrupt-config canonicalization → gate-or-stricter, only exact JSON `\"off\"` off-equivalent, absent stays lean); GAP-D (case/whitespace-insensitive `HIGH_SEVERITY_ROLES`). The four named floor invariants (refuse-self FAILs at every dial / no-fabrication-never-rewrite / `quality.test_integrity` no `off` / `hooks/guard.ts` byte-frozen) hold at every value."
    - "PRESERVED — SC2 (audit_retention 3-surface lockstep + lean defaults), the GOV-02 audit ledger (`appendAuditLedger`, context-io.ts:955-980), and the 25-04 segmentation-as-DATA (quoted / `#`-comment / heredoc / `\\`-continuation / `VAR=` / `env`-prefix / `;`/`&`/`|`/subshell) are unchanged — no over-block regression (the CR-01-inverse fail-safe stays clean)."
    - "`scripts/context-io.ts` is NOT edited this round — the entire fix lives in the hook's command resolution (`liveAdmitSegments` / `isCommandPrefix` / the leading-run walk in admission-guard.ts). `git diff --quiet scripts/context-io.ts` exits 0 (GAP-C/GAP-D + GOV-02 ledger preserved byte-identically). `hooks/guard.ts` stays byte-frozen (`git diff --quiet hooks/guard.ts` exit 0, blob `3501810e21308e4b7e219679a6ca30dace9b5d66`, D-02). The committed `.js` is a fresh `tsc` build — `npm run freshness` exits 0."
  artifacts:
    - path: "hooks/admission-guard.ts"
      provides: "The structural INVERT inside the ONE `liveAdmitSegments` authority: after the leading-run prefix-skip, when the resolved command word is neither a launcher (`LAUNCHERS`) nor an already-gating dynamic/substitution word (`DYNAMIC_EVAL_WORDS` / `isEnvIndirectedWord` / the synthetic `DYNAMIC_COMMAND_WORD`), the segment is checked for the admit shape and — if present — pushed as an UNRESOLVABLE admit segment (fail-closed gate) rather than silently dropped. Closes sub-roots (a) modifier-operand, (b) unlisted wrapper, (c) leading redirection, the dial-`all` routine leak, and the Class-E reopening. No widening of `COMMAND_MODIFIERS`; no second walk reintroduced; `liveTokens` segmentation byte-identical."
      contains: "admit"
    - path: "hooks/admission-guard.js"
      provides: "committed tsc build the host hook runs (freshness-covered; rebuilt after the .ts edit)"
    - path: "hooks/admission-guard.test.ts"
      provides: "extended child-spawn oracle vs the COMMITTED admission-guard.js: sub-root (a) modifier-operand forms (`timeout 5 node`, `nice -n 5 node`, `exec -a foo node`, `xargs -I {} node`, `env -C /tmp node`, `env -u PATH node`, `timeout -k 1 5 node`, `/usr/bin/timeout 5 node`, `nice '-n' '5' node`) DENY; sub-root (b) unlisted wrappers (`sudo`/`doas`/`setsid`/`ionice`/`chrt`/`taskset node`) DENY; sub-root (c) leading redirections (`>/dev/null node`, `2>/dev/null node`, `2>&1 node`) DENY; dial-`all` routine-behind-wrapper DENIES; Class-E-reopened (`node …admit routine ; timeout 5 node …admit high`) DENIES; the over-block controls (`timeout 5 node render`, `sudo ls`, `>/dev/null node build`, routine-only multi-admit, inert heredoc/quoted/comment behind a wrapper) ALLOW; the round-3 Class A/B/E/F + round-2 GAP-B/GAP-D cases still hold (no regression); case count >= guard.test.ts"
      contains: "deny"
    - path: "scripts/floor-invariance.test.ts"
      provides: "extended SC1 sweep: an anti-whack-a-mole command-RESOLUTION fuzz over {wrapper/modifier/redirection spelling (incl. path-form `/usr/bin/`, `/bin/`, `./`, quoted operands, bare-duration operand, unlisted wrappers, leading-redirection operators) × dial (off / high-severity / all) × admit ordering+separator (`;` / `&&` / `||` / `|` / `&` / subshell, routine-first vs high-first, 2-3 admits)} × {LF|CRLF}, asserting the class invariant — a gated admit in ANY live segment behind ANY unresolved command word → DENY; a routine-only / no-admit-shape / inert wrapper → ALLOW; the four named floor invariants hold at every value; the round-2 + round-3 sweeps stay green (PRESERVED, not duplicated)"
      contains: "human_admission"
    - path: "25-07-RED-baseline.txt"
      provides: "captured RED baseline — the PRE-FIX committed `admission-guard.js` (blob a65a93c58a975b7b4a9fbc54641fc5207062f222, HEAD a025a2b) ALLOWs each sub-root: (a) modifier-operand (`timeout 5 node …admit <hi>`), (b) unlisted wrapper (`sudo node …`), (c) leading redirection (`>/dev/null node …`), the dial-`all` routine-behind-wrapper leak, and the Class-E reopening (`node …admit routine ; timeout 5 node …admit hi`) — the 22-08/22-09 RED->GREEN artifact discipline, extending the 25-06 row format and citing the pre-fix blob"
    - path: "25-07-GREEN-proof.txt"
      provides: "captured GREEN proof — the POST-FIX committed `admission-guard.js` DENIES each sub-root naming the note; the over-block controls (non-admit wrapper `timeout 5 node render` / `sudo ls`, leading-redirection non-admit `>/dev/null node build`, routine-only multi-admit, inert heredoc/quoted/comment behind a wrapper, zero-config absent lean `off`) still ALLOW; the round-3 Class A/B/E/F + round-2 GAP-B/C/D closures still DENY — extending the 25-06 row format, referencing the committed `.js`"
  key_links:
    - from: "hooks/admission-guard.ts (the leading-run resolver in liveAdmitSegments)"
      to: "a real Bash `tool_input.command` admit verb behind a modifier-operand / unlisted wrapper / leading redirection"
      via: "after the prefix-skip loop, a resolved command word that is neither a launcher nor an already-gating dynamic/substitution word, on a segment that carries the admit shape, is pushed as an UNRESOLVABLE admit segment → fail-closed gate (the invert), instead of `continue` (silent drop)"
      pattern: "segmentHasAdmitShape|unresolvable"
    - from: "hooks/admission-guard.ts (the invert)"
      to: "the over-block fail-safe — a non-admit wrapper stays ALLOW"
      via: "the unresolvable-command-word gate fires ONLY when `segmentHasAdmitShape` is true for the segment; a wrapper/operand/redirection with no admit shape is not pushed and stays allowed"
      pattern: "segmentHasAdmitShape"
    - from: "hooks/admission-guard.ts (the deleted second walk, still gone)"
      to: "noteFileFromCommand + the standalone tokenize"
      via: "neither is reintroduced; the invert lives inside the ONE liveAdmitSegments walk; a grep proves neither helper exists"
      pattern: "noteFileFromCommand|function tokenize"
    - from: "scripts/floor-invariance.test.ts (the round-4 command-resolution fuzz)"
      to: "the COMMITTED hooks/admission-guard.js"
      via: "child-spawn the committed .js across the {wrapper/modifier/redirection × dial × admit-ordering × LF/CRLF} matrix; assert the class invariant (a gated admit behind ANY unresolved command word → DENY; routine-only/no-admit-shape/inert → ALLOW) for every variant"
      pattern: "admission-guard\\.js|spawn"
  prohibitions:
    - statement: "A GREEN author suite is NECESSARY-BUT-NOT-SUFFICIENT for this safety guard — the author's suites passed and STILL missed a class across rounds 1, 2, 3, and the round-3 fix (this is the 8th green-suite-insufficient catch of v2.0, and round 4 is closing exactly the kind of gap a green suite missed). Closure REQUIRES a both-direction RED-vs-committed-`.js` proof reproduced against the COMMITTED `hooks/admission-guard.js` (never the `.ts`, never the author `*.test.ts`): for EACH sub-root (a modifier-operand / b unlisted wrapper / c leading redirection) + the dial-`all` routine leak + the Class-E reopening, a planted high-severity-without-real-approval command flips PRE-FIX ALLOW -> POST-FIX DENY naming the note; AND the over-block controls (non-admit wrapper, leading-redirection non-admit, routine-only multi-admit, inert heredoc/quoted/comment behind a wrapper, zero-config absent lean `off`) still ALLOW. Captured in 25-07-RED-baseline.txt (pre-fix allows) and 25-07-GREEN-proof.txt (post-fix denies), extending the 25-06 row format. (D-12, [[grugops-safety-invariant-green-suite-insufficient]])."
      status: must-hold
      verification: "25-07-RED-baseline.txt shows the pre-fix committed `.js` (blob a65a93c5…, HEAD a025a2b) ALLOWing each sub-root + the compounding leaks; 25-07-GREEN-proof.txt shows the post-fix committed `.js` DENYing each naming the note and ALLOWing the over-block controls; reproduced independently of the author suite at the blocking checkpoint."
    - statement: "INVERT THE DEFAULT — do NOT widen `COMMAND_MODIFIERS`, do NOT add a redirection grammar that enumerates operators one notch at a time, and do NOT enumerate modifier-operand grammars (`-n`/`-a`/`-I`/`-C`/bare-duration) per-modifier. The fix is structural and admit-shape-triggered: a live segment carrying the admit shape whose command word does not resolve to a recognized launcher is UNRESOLVABLE and fails CLOSED (gate), exactly as Class B already does for a dynamic/substitution command word. This is the P22 round-8 'make the boundary BE the parser, fail closed on the unresolvable tail' lesson applied to leading-run resolution — the same UNIFY discipline 25-06 used for Classes A/B/E, completed for the wrapper/operand/redirection leading run. A new wrapper/operand/redirection spelling MUST be caught with no code change."
      status: must-hold
      verification: "a grep confirms `COMMAND_MODIFIERS` was NOT widened (its membership is unchanged from the committed set), no per-operator redirection enumeration was added, and the gate fires from the admit-shape check on an unresolved command word; the floor-invariance fuzz includes wrapper/operand/redirection spellings the author did NOT individually enumerate and they DENY structurally."
    - statement: "The SINGLE RESOLUTION AUTHORITY (anti-whack-a-mole) is preserved: the invert lands INSIDE the existing `liveAdmitSegments` walk — NOT a new second command parser. `function tokenize` and `noteFileFromCommand` stay DELETED (grep=0). `liveTokens` segmentation (quoted / comment / heredoc / `\\`-continuation / separators / `$(`/backtick handling) is byte-identical (the invert changes the leading-run command-word DISPOSITION on the unresolved tail, never the tokenizer)."
      status: must-hold
      verification: "grep confirms `function tokenize` and `noteFileFromCommand` do not exist in hooks/admission-guard.ts; `function liveAdmitSegments` exists and is still the single authority; the 25-04/25-05/25-06 tokenizer body (liveTokens) is unchanged (grep/diff confirms only the leading-run disposition was edited)."
    - statement: "Over-block SAFETY MUST be proven (the fail-safe direction): the unresolvable-command-word gate fires ONLY when the live segment carries the admit shape. A non-admit wrapper (`timeout 5 node render`, `sudo ls`, `>/dev/null node build`, `2>/dev/null make`), a routine-only multi-admit under `high-severity`, and an inert heredoc/quoted/`#`-comment admit mention behind a wrapper all stay ALLOW. Introducing a false-positive (gating a non-admit wrapper) is a PROHIBITED over-block — the clean over-block record (zero false-positives, confirmed by every prior round's red-team) must be preserved."
      status: must-hold
      verification: "admission-guard.test.ts + the floor-invariance fuzz assert: `timeout 5 node render` / `sudo ls` / `>/dev/null node build` / a routine-only multi-admit under high-severity / an inert heredoc-or-quoted-or-comment admit mention behind a wrapper all ALLOW; only a live admit shape behind an unresolved command word GATES."
    - statement: "The round-3 + round-2 closures and the verified SC2 / GOV-02 surfaces MUST NOT regress. Class A path-form launcher / Class B `$()`/backtick fail-closed / Class E per-segment classify-and-gate-if-any / Class F validate()-consistent `by` read all still hold; GAP-B (self-set behind a wrapper / D-01 floor) DENIES; GAP-C (corrupt/non-string/absent dial canonicalization, context-io.ts UNTOUCHED) holds; GAP-D (case/whitespace severity) holds; the four named floor invariants hold; SC2 config lockstep + the GOV-02 audit ledger are unchanged. `scripts/context-io.ts` is NOT edited this round; `hooks/guard.ts` stays byte-frozen (D-02)."
      status: must-hold
      verification: "the round-3 Class A/B/E/F + round-2 non-string-dial + case-variant + self-set-behind-a-wrapper sweeps in admission-guard.test.ts / floor-invariance.test.ts stay GREEN; `git diff --quiet scripts/context-io.ts` exits 0; `git diff --quiet hooks/guard.ts` exits 0; freshness 0; config-consistency + audit-ledger + foundation-guard suites green."
    - statement: "The SC1 floor + an INDEPENDENT both-angle red-team (guard-LOGIC probe AND guard-INPUT-SURFACE code-review, opus-grade — the P23 split; across the four prior rounds the LOGIC and INPUT-SURFACE angles each found a class the other missed, so BOTH are required) are the proof, not a green suite. This is the named closure gate. The probe MUST invent at least one un-enumerated obfuscation shape PER angle (e.g. a deeper wrapper/operand chain, a path-form modifier with an operand `/usr/bin/timeout -k 1 5 node …`, a redirection between the wrapper and the launcher, a 3-admit ordering shielding a high-severity behind two routines) and confirm each is caught structurally (denied or gated), proving the invert makes the resolver fail-closed on the unresolvable tail rather than on an anchor list."
      status: must-hold
      verification: "the blocking checkpoint (Task 25-07-04) HARD-STOPS until an independent opus-grade red-team reproduces both the LOGIC angle (the dial-`all` routine leak / the Class-E reopening / a multi-admit shield behind a wrapper) AND the INPUT-SURFACE angle (a modifier-operand / unlisted-wrapper / leading-redirection command word escaping the resolver, or an inert mention reading as a live admit), invents an un-enumerated shape per angle, confirms the four floor invariants + `git diff --quiet hooks/guard.ts` exit 0 + `git diff --quiet scripts/context-io.ts` exit 0 + freshness 0 + WR-01 no false-positive, vs the committed `.js`; findings recorded in 25-07-SUMMARY.md, not self-reported as a green suite."
    - statement: "WR-01 / no kit-content false-positive (D-13): the resolver edit and the new test/proof artifacts must not trip `guard_context_writes` or any foundation guard — reference the hook / Workflow 16 / the config keys by name, never restate a raw `.grugops/` write path beside a write token."
      status: must-hold
      verification: "`npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial); the closed Plan-25-0x deferral markers stay inert (no false-positive)."
---

<objective>
Round-4 gap closure for Phase 25 (25-VERIFICATION.md "UPDATE 2026-06-25 — post-25-06" TOP banner + 25-06-SUMMARY.md "Independent Both-Angle Red-Team", gaps_found). Plan 25-06 correctly UNIFIED the matcher and classifier into ONE `liveAdmitSegments` authority and closed the four round-3 classes (A path-form launcher / B `$()`-backtick command word / E multi-admit shield / F duplicate-or-indented `by`) — all CONFIRMED SOLID by the round-4 independent both-angle red-team and committed. The same red-team then converged (both angles + the orchestrator probe) on ONE NEW structural command-RESOLUTION class the 792-green author suite missed. This plan does NOT re-touch the round-3 / round-2 closures, the verified SC2 config foundation, or the GOV-02 audit ledger; it completes the leading-run resolution by inverting the default on the unresolvable tail — the entire fix lives in the hook's command resolution.

**The single root cause (confirmed in the committed source by the round-4 red-team):** `liveAdmitSegments` (admission-guard.ts:566-574) resolves the effective command word over the leading run by skipping assignment / `env` / `COMMAND_MODIFIERS` prefixes and a modifier's option flag. The prefix-skip loop breaks at `admission-guard.ts:572` (`if (!isPrefix && !isModifierFlag) break;`) on the first token that is neither a recognized prefix nor a `-flag`. That break-token is then read as the command word (`:577`). When it is neither a launcher (`:590`, `LAUNCHERS.has(word)` false) nor a dynamic/substitution word (`:582`/`:600`), the loop body reaches its end and `continue`s — the segment is SILENTLY DROPPED (default ALLOW). Three sub-roots reach that silent drop while carrying a real gated admit:
- **(a) modifier OPERAND not consumed** — `timeout 5 node …admit <hi>` (the flagless textbook form; the duration `5` is the operand the skip stops at), `nice -n 5 node …`, `exec -a foo node …`, `xargs -I {} node …`, `env -C /tmp node …`, `env -u PATH node …`, `timeout -k 1 5 node …`, `/usr/bin/timeout 5 node …`, `nice '-n' '5' node …` — the `isModifierFlag` skip (`:571`) consumes a `-flag` but not its separate-word VALUE nor a bare modifier operand.
- **(b) wrapper not in `COMMAND_MODIFIERS`** (`:110-120`) — `sudo`/`doas`/`setsid`/`ionice`/`chrt`/`taskset node …admit` resolve to themselves, not in `LAUNCHERS`, segment dropped.
- **(c) leading redirection** — `>/dev/null node …admit`, `2>/dev/null node …`, `2>&1 node …` — `liveTokens` has no redirection grammar, so the operator/target reads as the segment's command word.

All ALLOW a gated high-severity admit (forged `human:eve`, zero real human) through the un-forgeable PRIMARY tier on Claude Code. **Compounding:** at dial `all` even a ROUTINE admit slips (`timeout 5 node …admit <routine>`) — the strictest dial's floor leaks; and Class E is REOPENED — `node …admit <routine> ; timeout 5 node …admit <hi>` ALLOWs because the wrapped high segment is invisible to per-segment classification. (The D-01 self-set floor is NOT breached — `commandHasLiveAdmitShape` is resolution-independent and still fires — this is a GATING bypass, not a self-approval breach.)

**The fix (both red-team angles INDEPENDENTLY CONVERGED — anti-whack-a-mole; user-prescribed for this round): INVERT THE DEFAULT exactly as Class B already does.** When a LIVE segment carries the admit SHAPE (`segmentHasAdmitShape` — a context-io reference AND the `admit` verb, scanned over the segment) but the leading-run resolution does NOT terminate at a recognized launcher (and is not already one of the gating dynamic/substitution words), treat the command word as UNRESOLVABLE → push it as a fail-closed admit segment (GATE) instead of the silent `continue`. The admit shape is the trigger, so any new wrapper / option-operand / redirection spelling cannot escape; the over-block direction is proven safe (a non-admit wrapper — `timeout 5 node render`, `sudo ls`, `>/dev/null node build` — has no admit shape and stays ALLOW). Do NOT widen `COMMAND_MODIFIERS` and do NOT enumerate operand or redirection grammars one notch at a time — that is the trap every prior round hit. Keep `liveTokens` as the ONE resolution authority (no second walk). `scripts/context-io.ts` is NOT edited; `hooks/guard.ts` stays byte-frozen.

Plus the D-12 proof: the RED-first capture of each sub-root + compounding leak ALLOWing vs the PRE-FIX committed `.js` (blob `a65a93c58a975b7b4a9fbc54641fc5207062f222`, HEAD `a025a2b`), the extended child-spawn suite + anti-whack-a-mole command-resolution fuzz, the RED->GREEN proof artifacts, the rebuilt fresh `.js`, and the HARD-STOP independent both-angle red-team checkpoint.

Purpose: make GOV-01's un-forgeable primary tier actually un-forgeable across the leading-run resolution surface (modifier operands, unlisted wrappers, leading redirections) by completing the invert-on-the-unresolvable-tail discipline 25-06 began — proven adversarially vs the committed `.js`, not by a green author suite.
Output: the inverted leading-run resolution (the three sub-roots + the dial-`all` leak + the Class-E reopening closed), the extended suite + command-resolution fuzz, the RED/GREEN proof artifacts, the rebuilt fresh `.js`, and the independent both-angle red-team confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-PATTERNS.md
@.planning/phases/25-governance-on-a-dial/25-VERIFICATION.md
@.planning/phases/25-governance-on-a-dial/25-06-SUMMARY.md
@.planning/phases/25-governance-on-a-dial/25-06-RED-baseline.txt
@.planning/phases/25-governance-on-a-dial/25-06-GREEN-proof.txt
</context>

<artifacts_this_phase_produces>
New / changed symbols and files this round-4 gap-closure introduces (for the executor and downstream consumers):

- `hooks/admission-guard.ts`:
  - The structural INVERT inside the existing `liveAdmitSegments(cmd)` walk (admission-guard.ts:554-610). After the leading-run prefix-skip loop resolves the segment's command word (`rawWord` at :577 / `word` at :589), the final disposition for a segment whose command word is NEITHER a launcher NOR an already-gating dynamic/substitution word becomes: if the segment carries the admit shape (`segmentHasAdmitShape`), push it as an UNRESOLVABLE admit segment (`{ noteFile: null, unresolvable: true }`) → fail-closed gate downstream (`classifySegmentOrDeny`), instead of the current silent `continue` (the default-ALLOW hole). The narrow trigger is preserved: a segment with no admit shape (a non-admit wrapper) is not pushed and stays ALLOW.
  - This closes sub-root (a) modifier-operand (`timeout 5 node`, `nice -n 5 node`, …), (b) unlisted wrapper (`sudo`/`setsid`/`ionice`/`chrt`/`taskset node`), (c) leading redirection (`>/dev/null node`, `2>/dev/null node`, `2>&1 node`), the dial-`all` routine-behind-wrapper leak, and the Class-E reopening — all from ONE admit-shape-triggered fail-closed branch. `COMMAND_MODIFIERS` is NOT widened; no per-operator redirection enumeration is added; the `liveTokens` tokenizer body is byte-identical.
- `hooks/admission-guard.test.ts`: extended child-spawn cases vs the committed `.js` — sub-root (a) modifier-operand forms DENY; sub-root (b) unlisted wrappers DENY; sub-root (c) leading redirections DENY; dial-`all` routine-behind-wrapper DENIES; the Class-E reopening DENIES; the over-block controls (non-admit wrapper, leading-redirection non-admit, routine-only multi-admit, inert mention behind a wrapper) ALLOW; round-3 Class A/B/E/F + round-2 GAP-B/GAP-D still hold.
- `scripts/floor-invariance.test.ts`: the round-4 anti-whack-a-mole command-RESOLUTION fuzz ({wrapper/modifier/redirection spelling × dial × admit-ordering+separator × LF/CRLF}); the round-2 + round-3 sweeps preserved.
- `25-07-RED-baseline.txt`, `25-07-GREEN-proof.txt`: the captured RED->GREEN proof artifacts (22-08/22-09 discipline), extending the 25-06 row format.

NOTE: `scripts/context-io.ts` is NOT edited this round — the entire fix lives in the hook's command resolution. GAP-C/GAP-D and the GOV-02 audit ledger live in context-io.ts and are confirmed solid; touching it would risk a byte regression. If the executor somehow finds a fix genuinely requires a context-io.ts edit (it should not), it must keep GAP-C/GAP-D and the ledger byte-identical and justify it in the summary.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 25-07-01: Invert the default on the unresolvable leading-run tail — an admit shape behind a modifier-operand / unlisted-wrapper / leading-redirection command word GATES (fail-closed) instead of silently dropping — capture the RED baseline first</name>
  <files>hooks/admission-guard.ts, hooks/admission-guard.js, hooks/admission-guard.test.ts, 25-07-RED-baseline.txt</files>
  <read_first>
    - hooks/admission-guard.ts:85-120 (LAUNCHERS at :89 — the recognized-launcher set; COMMAND_MODIFIERS at :110-120 — the CLOSED prefix set that MUST NOT be widened; read so the executor does NOT reach for the anti-pattern of adding `sudo`/`setsid`/operand grammars here)
    - hooks/admission-guard.ts:122-184 (DYNAMIC_EVAL_WORDS at :131 + DYNAMIC_COMMAND_WORD sentinel at :184 — the existing fail-closed posture for an unresolvable dynamic/substitution command word; the invert extends THIS posture to the unrecognized command word, it does not invent a new one)
    - hooks/admission-guard.ts:440-467 (isCommandPrefix at :448-454 — resolves the effective word; segmentHasAdmitShape at :459-467 — the admit-shape scan that becomes the invert's TRIGGER; read it carefully, this is the gate condition)
    - hooks/admission-guard.ts:530-610 (the AdmitSegment interface :533-536 and the WHOLE liveAdmitSegments walk :554-610 — THE EDIT SITE: the prefix-skip loop :567-574 breaks at :572 on the first non-prefix non-flag token; :577 reads it as the command word; :582 handles the synthetic DYNAMIC_COMMAND_WORD; :589-595 handles a launcher; :600-607 handles a dynamic/eval/`$X` word; the FALL-THROUGH after :607 is the silent `continue`/end-of-loop default-ALLOW hole this task inverts)
    - hooks/admission-guard.ts:612-625 (commandHasLiveAdmitShape — the GAP-B self-set backstop; it is resolution-independent and must STAY untouched so the D-01 floor still holds behind a wrapper)
    - hooks/admission-guard.ts:678-861 (the main flow: liveAdmitSegments consumed at :678; the dial canonicalization :693-734; classifySegmentOrDeny :747-850 — read how an `unresolvable: true` segment fails CLOSED at :750-761; the invert pushes `{ noteFile: null, unresolvable: true }` so this existing fail-closed branch handles it)
    - .planning/phases/25-governance-on-a-dial/25-06-SUMMARY.md (the "Independent Both-Angle Red-Team" section: the sub-root table with the exact reproduced commands + the root-cause line refs + the prescribed INVERT fix — the bar this task clears)
    - .planning/phases/25-governance-on-a-dial/25-06-RED-baseline.txt and 25-06-GREEN-proof.txt (the both-direction proof ROW FORMAT this task's 25-07-RED-baseline.txt must extend)
    - hooks/admission-guard.test.ts:1-130 (the runGuard / expectDeny / expectAllow child-spawn harness + makeProject/writeNote/admitCmd fixtures vs the COMMITTED admission-guard.js; the `>= guard.test.ts` case-count watch; extend, never reimplement)
    - hooks/guard.ts:50,88 (the verb-anchor + refuse-self-set posture to MIRROR — DO NOT EDIT guard.ts, D-02; read for pattern only)
  </read_first>
  <behavior>
    - CAPTURE RED FIRST (the 22-08/22-09 discipline): before any source edit, child-spawn each sub-root through the CURRENT committed `hooks/admission-guard.js` (blob a65a93c58a975b7b4a9fbc54641fc5207062f222, HEAD a025a2b) on a planted high-severity note (by: security-nfr) under human_admission: high-severity with NO approval var, and record that each ALLOWs (no `"deny"` in stdout while the identical command admits): (a) `timeout 5 node <ctxio> admit T NOTE` (flagless), `nice -n 5 node …`, `exec -a foo node …`, `xargs -I {} node …`, `env -C /tmp node …`, `env -u PATH node …`, `timeout -k 1 5 node …`, `/usr/bin/timeout 5 node …`, `nice '-n' '5' node …`; (b) `sudo node …admit`, `doas node …`, `setsid node …`, `ionice -c2 node …`, `chrt -f 1 node …`, `taskset 0x1 node …`; (c) `>/dev/null node …admit`, `2>/dev/null node …`, `2>&1 node …`. Also record the dial-`all` routine-behind-wrapper leak (`timeout 5 node …admit ROUTINE` under `all` ALLOWs) and the Class-E reopening (`node …admit ROUTINE ; timeout 5 node …admit NOTE` under high-severity ALLOWs). Save to 25-07-RED-baseline.txt (extend the 25-06 row format; cite the pre-fix blob).
    - AFTER the fix, against the rebuilt committed `.js`, on a gated high-severity un-approved note → DENY (reason names the fail-closed unresolvable-command-word condition): every sub-root (a)/(b)/(c) DENIES; the dial-`all` routine-behind-wrapper DENIES; the Class-E reopening DENIES under high-severity regardless of order.
    - Invert NARROW trigger (the fail-safe direction): a wrapper / operand / redirection construct with NO admit shape in the live segment → ALLOW. `timeout 5 node render`, `sudo ls`, `>/dev/null node build`, `2>/dev/null make`, `setsid sleep 1` all ALLOW. The fail-closed gate fires ONLY when the live segment carries the admit shape (a context-io reference AND the admit verb).
    - NO over-block regression (the 25-04/25-05/25-06 CR-01-inverse fail-safe stays clean): an inert heredoc body line behind a wrapper, a single/double-quoted echoed mention behind a wrapper, a `#`-comment mention, a routine-only multi-admit under high-severity, and a path component literally named `admit` → ALLOW.
    - No regression on the round-3 / round-2 closures: Class A path-form launcher (`/usr/bin/nice node …admit`, `command node …`, `nodejs …`, `no"de"`, `{ node …admit; }`) DENIES; Class B `$(echo node) …admit` / backtick GATES (and a `$()`/backtick/eval with NO admit shape ALLOWs); Class E `node …admit ROUTINE ; node …admit HIGH` (bare, no wrapper) DENIES while routine-only ALLOWs; the supported bare `node …admit`, `npx tsx …admit`, `env`/`VAR=` prefix forms → still DENY on a gated admit; empty/malformed stdin → ALLOW.
    - GAP-B floor PRESERVED: an inline self-set behind any wrapper (`GRUGOPS_ADMISSION_APPROVED_BY=eve timeout 5 node …admit`) DENIES even with the var in env.
  </behavior>
  <action>
    FIRST capture the RED baseline. Against the CURRENT committed `hooks/admission-guard.js` (blob a65a93c58a975b7b4a9fbc54641fc5207062f222, HEAD a025a2b), child-spawn each sub-root (a modifier-operand / b unlisted-wrapper / c leading-redirection), plus the dial-`all` routine-behind-wrapper leak and the Class-E reopening, with a planted high-severity note and NO approval var, and record that each ALLOWs into `25-07-RED-baseline.txt` — proof the bypass was real before the fix, in the 25-06 row format, citing the pre-fix blob.

    THEN make the structural invert in `liveAdmitSegments` (admission-guard.ts:554-610). Do NOT widen `COMMAND_MODIFIERS`, do NOT add a redirection grammar that enumerates `>`/`2>`/`2>&1` operators, and do NOT enumerate modifier-operand grammars per modifier — those are the whack-a-mole traps every prior round hit. The fix is structural and admit-shape-triggered, mirroring the Class B fail-closed branch already present at :582.

    The single edit: after the leading-run prefix-skip loop resolves the segment's command word, the segment currently follows one of three branches — synthetic DYNAMIC_COMMAND_WORD (:582, gate on admit shape), a launcher (:589-595, classify), or a dynamic-eval / `$X` word (:600-607, gate on admit shape). A segment whose command word matches NONE of those falls through to the end of the loop body and is silently dropped (the default-ALLOW hole). INVERT that default: when the resolved command word is none of {launcher, DYNAMIC_COMMAND_WORD, dynamic-eval/`$X` word}, check `segmentHasAdmitShape(tokens, cmdIdx + 1)` (and, mirroring the dynamic-eval tail at :602-603, the segment's RAW text via `rawTextHasAdmitShape(rawSegmentText(cmd, tokens, cmdIdx))` so an admit shape inside a quoted operand the tokenizer treats as data is still caught) — and if the segment carries the admit shape, push it as `{ noteFile: null, unresolvable: true }` (an UNRESOLVABLE admit segment). The existing `classifySegmentOrDeny` (:747-761) already fails CLOSED on an `unresolvable: true` segment under any active dial, naming the fail-closed condition. The trigger stays NARROW: a segment with no admit shape (a non-admit wrapper / operand / redirection) is NOT pushed and stays ALLOW.

    Why this closes all three sub-roots from ONE branch: (a) when the leading-run skip stops at a modifier operand (`timeout`'s `5`, `nice -n` `5`, `xargs -I` `{}`, `env -C` `/tmp`), the operand resolves to a non-launcher non-dynamic word → unresolvable → gate on the admit shape; (b) an unlisted wrapper (`sudo`/`setsid`/`ionice`/`chrt`/`taskset`) resolves to itself, a non-launcher → unresolvable → gate; (c) a leading-redirection operator/target (`>/dev/null`, `2>/dev/null`, `2>&1`) tokenizes as the command word, a non-launcher → unresolvable → gate. The compounding leaks fall out: at dial `all` a routine admit behind a wrapper is an unresolvable admit segment → gate-every-match denies; the Class-E reopening is re-closed because the wrapped high segment is now an enumerated admit segment classified by the per-segment walk (25-06's Class E), not silently dropped.

    PRESERVE the existing branches and floors exactly: the launcher branch (:589-595) and the dynamic/substitution branches (:582, :600-607) are unchanged; `commandHasLiveAdmitShape` (:618) and the in-front-of-early-exit self-set check (:663) are unchanged (GAP-B / D-01 floor); the dial canonicalization (`off` exits, `gateEveryMatch = dial !== "high-severity"`, GAP-C) and the case-insensitive `HIGH_SEVERITY_ROLES` membership (GAP-D) are unchanged. Do NOT rewrite the `liveTokens` tokenizer (the 25-04/25-05/25-06 segmentation — quoting / comment / heredoc / `\`-continuation / separators / `$(`-backtick handling — stays byte-identical). The invert changes ONLY the leading-run command-word DISPOSITION on the unresolved tail.

    Voice: CLEAR PROFESSIONAL VOICE in all comments and deny reasons (CLAUDE.md hard rule — governance/admission/audit surfaces are never caveman voice). Comment the invert so a future reader sees the principle: an admit shape behind a command word the resolver cannot prove is a recognized launcher is UNRESOLVABLE and fails CLOSED — the same posture as Class B — so a new wrapper/operand/redirection spelling cannot escape without a code change.

    Comment-text discipline: do NOT place a fenced code block inside this action's prose; describe the invert by its named condition (resolved command word not in {launcher, dynamic-substitution, dynamic-eval/`$X`} + `segmentHasAdmitShape`/`rawTextHasAdmitShape` → push unresolvable).

    Rebuild the committed `.js`: `npm run build` then `npm run freshness` (0 drift; `admission-guard.js` is freshness-covered).

    Extend `hooks/admission-guard.test.ts` (the child-spawn oracle vs the COMMITTED `.js`): add cases for sub-root (a) each modifier-operand form → DENY; sub-root (b) each unlisted wrapper → DENY; sub-root (c) each leading redirection → DENY; the dial-`all` routine-behind-wrapper → DENY; the Class-E reopening (`node …admit ROUTINE ; timeout 5 node …admit HIGH`, all separators, both orderings) → DENY; the over-block controls (`timeout 5 node render`, `sudo ls`, `>/dev/null node build`, `2>/dev/null make`, routine-only multi-admit under high-severity, inert mention behind a wrapper) → ALLOW; re-assert the round-3 Class A/B/E/F (still DENY/GATE/ALLOW as before), the round-2 launchers (still DENY), and the GAP-B self-set-behind-a-wrapper (DENY). Keep the case count >= guard.test.ts (Pitfall-2 watch). Capture the post-fix DENY/ALLOW outcomes for these forms into `25-07-GREEN-proof.txt` (this task contributes the sub-root + compounding rows; Task 25-07-03 completes the proof set with the fuzz dimensions).
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run hooks/admission-guard.test.ts && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts</automated>
  </verify>
  <acceptance_criteria>
    - `25-07-RED-baseline.txt` exists and records sub-root (a) modifier-operand (`timeout 5 node …`, `nice -n 5 node …`, `/usr/bin/timeout 5 node …`, `nice '-n' '5' node …`, plus `exec -a`/`xargs -I`/`env -C`/`env -u`/`timeout -k 1 5`), (b) unlisted wrappers (`sudo`/`doas`/`setsid`/`ionice`/`chrt`/`taskset node …`), (c) leading redirections (`>/dev/null`/`2>/dev/null`/`2>&1 node …`), the dial-`all` routine-behind-wrapper leak, and the Class-E reopening ALLOWed by the PRE-FIX committed `admission-guard.js` (blob a65a93c5…), in the 25-06 row format citing the pre-fix blob.
    - A grep confirms `COMMAND_MODIFIERS` membership is UNCHANGED from the committed set (the fix did not widen it), `function tokenize` and `noteFileFromCommand` still do NOT exist (single authority preserved), `function liveAdmitSegments` is still the one authority, and the `liveTokens` tokenizer body is unedited (only the leading-run disposition changed).
    - `npx vitest run hooks/admission-guard.test.ts` is GREEN: every sub-root (a)/(b)/(c) DENIES a gated high-severity un-approved admit; the dial-`all` routine-behind-wrapper DENIES; the Class-E reopening DENIES under high-severity (all separators, both orderings); the over-block controls (`timeout 5 node render`, `sudo ls`, `>/dev/null node build`, `2>/dev/null make`, routine-only multi-admit, inert mention behind a wrapper) ALLOW; the round-3 Class A/B/E/F + round-2 launchers + GAP-B self-set-behind-a-wrapper still hold; empty stdin ALLOWs; case count >= guard.test.ts.
    - `npm run freshness` exits 0 (committed `admission-guard.js` fresh); `git diff --quiet hooks/guard.ts` exits 0 (byte-frozen, D-02); `git diff --quiet scripts/context-io.ts` exits 0 (NOT edited this round).
    - `25-07-GREEN-proof.txt` records the post-fix DENY for each sub-root + the dial-`all` leak + the Class-E reopening, plus ALLOW for the non-admit-wrapper / leading-redirection-non-admit / routine-only / inert controls.
  </acceptance_criteria>
  <done>The leading-run resolution is fail-closed on the unresolvable tail: an admit shape behind a modifier operand (a), an unlisted wrapper (b), or a leading redirection (c) is treated as UNRESOLVABLE and GATES instead of silently dropping — closing the dial-`all` routine leak and the Class-E reopening from ONE admit-shape-triggered branch; `COMMAND_MODIFIERS` is not widened, the second walk stays deleted, the round-3/round-2 closures + GAP-B floor + inert mentions all hold — vs the committed `.js`; guard.ts + context-io.ts untouched, freshness 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 25-07-02: Extend the child-spawn oracle (admission-guard.test.ts) — exhaustive sub-root + compounding + over-block cases vs the committed .js, and complete the GREEN-proof rows</name>
  <files>hooks/admission-guard.test.ts, 25-07-GREEN-proof.txt</files>
  <read_first>
    - hooks/admission-guard.test.ts:1-130 (the runGuard / expectDeny / expectAllow harness + makeProject / writeNote / admitCmd fixtures + the `>= guard.test.ts` case-count watch — extend, never reimplement; this targets the COMMITTED admission-guard.js)
    - hooks/admission-guard.test.ts (the round-3 Class A/B/E/F describe blocks added by 25-06 — read them so the round-4 cases EXTEND, do not duplicate; the multi-admit / inert-mention / launcher fixtures are reusable)
    - hooks/admission-guard.ts:554-610 (the post-fix invert from Task 25-07-01 — the cases assert its behavior against the committed `.js`)
    - .planning/phases/25-governance-on-a-dial/25-06-GREEN-proof.txt (the GREEN-proof ROW FORMAT this task's 25-07-GREEN-proof.txt rows extend)
    - .planning/phases/25-governance-on-a-dial/25-06-SUMMARY.md (the sub-root table — the exact commands the cases must cover; the routine-only / non-admit-wrapper ALLOW controls)
  </read_first>
  <behavior>
    - Sub-root (a) modifier-operand: each of `timeout 5 node …admit <hi>`, `nice -n 5 node …`, `exec -a foo node …`, `xargs -I {} node …`, `env -C /tmp node …`, `env -u PATH node …`, `timeout -k 1 5 node …`, `/usr/bin/timeout 5 node …`, `nice '-n' '5' node …` on a gated high-severity un-approved admit → DENY vs the committed `.js`.
    - Sub-root (b) unlisted wrapper: each of `sudo node …`, `doas node …`, `setsid node …`, `ionice -c2 node …`, `chrt -f 1 node …`, `taskset 0x1 node …` → DENY.
    - Sub-root (c) leading redirection: each of `>/dev/null node …`, `2>/dev/null node …`, `2>&1 node …` → DENY.
    - Compounding: `timeout 5 node …admit <routine>` under `human_admission: all` → DENY (the strictest dial no longer leaks a routine behind a wrapper); `node …admit <routine> ; timeout 5 node …admit <hi>` (and `&&`/`||`/`|`/`&`/subshell, both orderings) under `high-severity` → DENY (Class-E reopening re-closed).
    - Over-block controls (the fail-safe direction — MUST ALLOW): `timeout 5 node render`, `sudo ls`, `>/dev/null node build`, `2>/dev/null make`, `setsid sleep 1`, a routine-only multi-admit under `high-severity` (`node …admit <routine1> ; timeout 5 node …admit <routine2>`), and an inert heredoc/quoted/`#`-comment admit mention behind a wrapper → ALLOW.
    - No regression: the round-3 Class A/B/E/F and the round-2 launcher + GAP-B/GAP-D cases still pass exactly as before.
  </behavior>
  <action>
    Extend `hooks/admission-guard.test.ts` with describe blocks for the round-4 leading-run resolution class — sub-root (a) modifier-operand, (b) unlisted wrapper, (c) leading redirection — plus the compounding dial-`all` routine-behind-wrapper and the Class-E reopening, plus the over-block ALLOW controls. Reuse the existing `runGuard` / `expectDeny` / `expectAllow` harness and the `makeProject` / `writeNote` / `admitCmd` fixtures (a high-severity `by: security-nfr` note, a routine `by: software-engineer` note, the temp factory.config.json per dial). Do NOT duplicate the round-3 Class A/B/E/F blocks — add the new forms only.

    For each sub-root, build the command by prefixing the existing `admitCmd(highNote)` with the wrapper/operand/redirection spelling (e.g. the modifier-operand prefix, the unlisted-wrapper word, the redirection operator) and assert DENY under `high-severity`. For the dial-`all` compounding case, target the routine note under the `all` project and assert DENY. For the Class-E reopening, chain a routine admit and a wrapped high-severity admit across each separator in both orderings and assert DENY under `high-severity`. For the over-block controls, assert ALLOW for each non-admit wrapper, the leading-redirection non-admit, the routine-only multi-admit, and an inert mention behind a wrapper.

    Keep the case count >= guard.test.ts (the Pitfall-2 silent-deny-path watch). Capture the post-fix DENY rows (each sub-root, the dial-`all` leak, the Class-E reopening) and the ALLOW control rows into `25-07-GREEN-proof.txt`, extending the 25-06 row format and referencing the committed `.js`, never the `.ts`.

    Voice: CLEAR PROFESSIONAL VOICE in comments (this oracle owns the SC1 floor). Per D-12, a GREEN oracle is NECESSARY-NOT-SUFFICIENT — the independent red-team at Task 25-07-04 is the gate; state that in a comment.

    Comment-text discipline: no fenced code blocks in this action; describe the cases by the command shape and the expected decision.
  </action>
  <verify>
    <automated>npx vitest run hooks/admission-guard.test.ts && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run hooks/admission-guard.test.ts` is GREEN with the round-4 cases added: every sub-root (a)/(b)/(c) DENIES under `high-severity`; the dial-`all` routine-behind-wrapper DENIES; the Class-E reopening DENIES under `high-severity` (all separators, both orderings); the over-block controls (`timeout 5 node render`, `sudo ls`, `>/dev/null node build`, `2>/dev/null make`, routine-only multi-admit, inert mention behind a wrapper) ALLOW.
    - The round-3 Class A/B/E/F + round-2 launcher + GAP-B/GAP-D cases still pass (no regression); case count >= guard.test.ts.
    - `git diff --quiet scripts/context-io.ts` exits 0 (NOT edited); `git diff --quiet hooks/guard.ts` exits 0.
    - `25-07-GREEN-proof.txt` records the post-fix DENY rows (each sub-root, the dial-`all` leak, the Class-E reopening) and the ALLOW control rows, extending the 25-06 row format and referencing the committed `.js`.
  </acceptance_criteria>
  <done>The child-spawn oracle exhaustively covers the round-4 leading-run resolution class vs the committed `.js`: every modifier-operand / unlisted-wrapper / leading-redirection sub-root DENIES, the dial-`all` leak and the Class-E reopening DENY, and the non-admit-wrapper / routine-only / inert over-block controls ALLOW; the round-3/round-2 cases hold, the GREEN-proof rows are captured — context-io.ts + guard.ts byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 25-07-03: Anti-whack-a-mole command-resolution fuzz (wrapper/modifier/redirection × dial × admit-ordering) + complete the RED->GREEN proof set (D-12)</name>
  <files>scripts/floor-invariance.test.ts, 25-07-RED-baseline.txt, 25-07-GREEN-proof.txt</files>
  <read_first>
    - scripts/floor-invariance.test.ts:1-75 (the four-invariant structure + the FROZEN_GUARD_BLOB + the child-spawn-vs-committed-`.js` + import-the-committed-`.js` composition — EXTEND this)
    - scripts/floor-invariance.test.ts:317-360 (the `hookDecision(cmd, dir)` spawn helper + `projectWith({ dial, corrupt, absent })` fixture — REUSE these for the round-4 fuzz; do not reimplement)
    - scripts/floor-invariance.test.ts:407-505 (the round-2 SC1 anti-whack-a-mole {launcher}×{prefix}×{body}×{LF|CRLF} class-invariant block — the ROW/loop shape to mirror; do NOT duplicate it)
    - scripts/floor-invariance.test.ts:589-879 (the round-2 + round-3 command-RESOLUTION fuzz blocks: the 25-05 obfuscation matrix at :589, the 25-06 path-form-modifier × command-substitution × multi-admit-ordering matrix at :706 — the round-4 fuzz ADDS the wrapper/modifier-operand/redirection dimensions; do NOT duplicate the round-2/round-3 matrices)
    - hooks/admission-guard.ts:554-610 (the post-fix invert from Task 25-07-01 — the fuzz asserts the CLASS invariant over this authority)
    - .planning/phases/25-governance-on-a-dial/25-06-GREEN-proof.txt (the GREEN-proof ROW FORMAT and the existing anti-whack-a-mole block this task's 25-07-GREEN-proof.txt extends)
    - .planning/phases/25-governance-on-a-dial/25-CONTEXT.md:147-160 (D-12 — green-suite-insufficient; the anti-enumeration property-fuzz requirement)
  </read_first>
  <behavior>
    - Modifier-operand fuzz: for each modifier in {timeout, nice, exec, xargs, env, nohup} × each operand spelling {bare duration `5`, `-n 5`, `-a foo`, `-I {}`, `-C /tmp`, `-u PATH`, `-k 1 5`, quoted `'-n' '5'`} × each path spelling {bare, `/usr/bin/`, `/bin/`, `./`}, child-spawn the COMMITTED `admission-guard.js` on a gated high-severity un-approved admit and assert DENY (the admit shape behind the unresolved command word gates). A modifier-operand prefix with NO trailing admit shape (`timeout 5 node render`) → ALLOW.
    - Unlisted-wrapper fuzz: for each wrapper in {sudo, doas, setsid, ionice, chrt, taskset} (with and without a flag) on a segment carrying the admit shape → DENY; the same wrapper on a non-admit command (`sudo ls`) → ALLOW.
    - Leading-redirection fuzz: for each redirection in {`>/dev/null`, `1>/dev/null`, `2>/dev/null`, `2>&1`, `&>/dev/null`} on a segment carrying the admit shape → DENY; the same redirection on a non-admit command (`>/dev/null node build`) → ALLOW.
    - Dial dimension: sweep each of the above under {`high-severity`, `all`}; under `all` a routine admit behind ANY wrapper/operand/redirection → DENY (the strictest dial's floor does not leak); under `off` → ALLOW.
    - Admit-ordering fuzz: for each separator in {`;`, `&&`, `||`, `|`, `&`, subshell `( … ) ; …`} × each ordering {routine-then-wrapped-high, wrapped-high-then-routine, 3 admits with the wrapped-high in any position} under `high-severity`, assert DENY (the wrapped high-severity segment is an enumerated unresolvable admit segment regardless of position). A multi-admit command with ONLY routine segments (some behind wrappers) under `high-severity` → ALLOW (no over-block).
    - Class invariant (the structural proof, not enumeration): for EVERY generated variant the rule holds — a gated admit behind ANY unresolved command word in ANY live segment → DENY; a routine-only / no-admit-shape / inert command → ALLOW. A hypothetical un-enumerated wrapper/operand/redirection shape #N is caught structurally because the invert fails closed on the unresolvable tail, not on an anchor list.
    - The four named floor invariants (refuse-self, no-fabrication/never-rewrite, `quality.test_integrity` no-`off`, `hooks/guard.ts` byte-frozen) still hold at every value (preserve the existing assertions; do not weaken them). The round-2 + round-3 sweeps stay green.
  </behavior>
  <action>
    Extend `scripts/floor-invariance.test.ts` (do NOT duplicate the round-2 {launcher}×{prefix}×{body} fuzz, the round-3 path-form-modifier × command-substitution × multi-admit fuzz, or the non-string-dial / case-variant sweeps — ADD the wrapper / modifier-operand / leading-redirection dimensions). Reuse the existing `hookDecision(cmd, dir)` child-spawn-vs-committed-`.js` helper and the `projectWith({ dial })` temp-config + temp-note fixtures.

    Add the modifier-operand fuzz: enumerate {modifier} × {operand spelling} × {path spelling} on a gated high-severity un-approved admit and assert DENY vs the committed `.js` (the invert class invariant); a modifier-operand prefix with no admit shape ALLOWs.

    Add the unlisted-wrapper fuzz: enumerate {sudo/doas/setsid/ionice/chrt/taskset} (with/without a flag) on a segment carrying the admit shape and assert DENY; the same wrapper on a non-admit command ALLOWs.

    Add the leading-redirection fuzz: enumerate {`>/dev/null`, `1>/dev/null`, `2>/dev/null`, `2>&1`, `&>/dev/null`} on a segment carrying the admit shape and assert DENY; the same redirection on a non-admit command ALLOWs.

    Add the dial × admit-ordering dimensions: sweep under {high-severity, all} and assert the dial-`all` routine-behind-wrapper DENIES; sweep the separator × ordering matrix (2 and 3 admits, the wrapped-high in any position) under high-severity and assert DENY whenever ANY segment is a wrapped high-severity admit, while a routine-only multi-admit ALLOWs.

    Make the structural argument explicit in a comment: the invert fails closed on the unresolvable tail (an admit shape behind a command word the resolver cannot prove is a recognized launcher), so a wrapper/operand/redirection spelling outside any anchor cannot escape — the proof is the class invariant, not an enumeration of known forms.

    COMPLETE the proof artifacts: finalize `25-07-RED-baseline.txt` (pre-fix committed `.js` blob a65a93c5… at HEAD a025a2b ALLOWs each sub-root + the dial-`all` leak + the Class-E reopening) and `25-07-GREEN-proof.txt` (post-fix committed `.js` DENIES each naming the fail-closed condition; the non-admit-wrapper / leading-redirection-non-admit / routine-only multi-admit / inert mention behind a wrapper / zero-config absent lean `off` still ALLOW; the round-3 Class A/B/E/F + round-2 GAP-B/C/D still DENY). Extend the 25-06 row format; reference the committed `.js`, never the `.ts`.

    Voice: CLEAR PROFESSIONAL VOICE in comments (this test owns the SC1 floor). Per D-12, a GREEN fuzz is NECESSARY-NOT-SUFFICIENT — the independent red-team at Task 25-07-04 is the gate; state that in a comment.

    Comment-text discipline: no fenced code blocks in this action; describe the matrix dimensions and the class invariant by concept.
  </action>
  <verify>
    <automated>npx vitest run scripts/floor-invariance.test.ts && git diff --quiet hooks/guard.ts && git diff --quiet scripts/context-io.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/floor-invariance.test.ts` is GREEN with the modifier-operand fuzz added: every {modifier × operand × path} on a gated high-severity un-approved admit DENIES via the committed `admission-guard.js`; a modifier-operand prefix with no admit shape ALLOWs.
    - The unlisted-wrapper fuzz is GREEN: every {sudo/doas/setsid/ionice/chrt/taskset} on a segment with the admit shape DENIES; the same wrapper on a non-admit command ALLOWs.
    - The leading-redirection fuzz is GREEN: every redirection operator on a segment with the admit shape DENIES; the same redirection on a non-admit command ALLOWs.
    - The dial × admit-ordering fuzz is GREEN: the dial-`all` routine-behind-wrapper DENIES; every {separator × ordering, 2/3 admits} under `high-severity` with a wrapped high-severity segment in any position DENIES; a routine-only multi-admit under `high-severity` ALLOWs.
    - The four named floor invariants still hold at every value; the round-2 + round-3 sweeps stay green; `git diff --quiet hooks/guard.ts` and `git diff --quiet scripts/context-io.ts` exit 0.
    - `25-07-RED-baseline.txt` and `25-07-GREEN-proof.txt` are complete (each sub-root + the dial-`all` leak + the Class-E reopening: pre-fix ALLOW, post-fix DENY; non-admit-wrapper / routine-only / inert / zero-config ALLOW) and reference the committed `.js`, not the `.ts`, extending the 25-06 row format.
  </acceptance_criteria>
  <done>An anti-whack-a-mole command-resolution fuzz over the wrapper × modifier-operand × leading-redirection × dial × admit-ordering matrix proves the invert's class invariant structurally (with the non-admit-wrapper / routine-only / inert ALLOW controls); the round-2 + round-3 sweeps + four floor invariants stay green; the RED->GREEN proof artifacts are captured vs the committed `.js`; guard.ts + context-io.ts byte-unchanged — the SC1 author proof is complete (independent red-team still the gate, D-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 25-07-04: SC1 round-4 closure gate — INDEPENDENT both-angle red-team vs the committed .js (HARD STOP; green suite is NOT proof, D-12)</name>
  <read_first>
    - .planning/phases/25-governance-on-a-dial/25-06-SUMMARY.md (the "Independent Both-Angle Red-Team" section that FOUND this round-4 class — the exact sub-root reproductions, the both-angle dispatch lesson (the LOGIC agent found the Class-E reopening + the dial-`all` leak; the INPUT-SURFACE agent found the leading redirection), the prescribed INVERT fix; the bar this checkpoint must clear)
    - .planning/phases/25-governance-on-a-dial/25-07-RED-baseline.txt and 25-07-GREEN-proof.txt (the both-direction proof the independent probe reproduces vs the committed `.js`)
    - hooks/admission-guard.js (the COMMITTED artifact the probe attacks — never the `.ts`, never the author suites); scripts/context-io.js (the imported parse the hook uses — unchanged GAP-C/GAP-D)
  </read_first>
  <what-built>The round-4 gap-closure INVERT of the GOV-01 un-forgeable tier: the leading-run resolver in the ONE `liveAdmitSegments` authority now fails CLOSED on the unresolvable tail — an admit SHAPE behind a command word that does not resolve to a recognized launcher (a modifier operand, an unlisted wrapper, or a leading redirection) is treated as UNRESOLVABLE and GATES, exactly as Class B already does, instead of silently dropping the segment (Tasks 25-07-01/02); plus the anti-whack-a-mole command-resolution fuzz + RED->GREEN proof artifacts (Task 25-07-03). This is the LAST task of the round-4 gap-closure plan. Per [[grugops-safety-invariant-green-suite-insufficient]] and D-12, a passing suite is necessary but NOT sufficient for this safety floor — the author's suites passed and STILL missed a class across rounds 1, 2, 3, and the round-3 fix (this is the 8th green-suite-insufficient catch of v2.0). This checkpoint HARD-STOPS until an INDEPENDENT opus-grade adversary (not the authoring executor's own suites) has reproduced the closure. Do NOT declare closure on a green suite alone.</what-built>
  <how-to-verify>
    An INDEPENDENT opus-grade probe must run BOTH angles (the P23 split — guard LOGIC and guard INPUT SURFACE are different blind spots; across the four prior rounds each angle found a class the other missed, so BOTH are required) and reproduce ALL of the following against the COMMITTED artifacts (`hooks/admission-guard.js`; `scripts/context-io.js` for the imported parse), never the `.ts` and never the author's own `admission-guard.test.ts` / `floor-invariance.test.ts`:
    1. INPUT-SURFACE angle — sub-root (a) each modifier-operand form now DENIES a gated high-severity un-approved admit (`timeout 5 node …admit`, `nice -n 5 node …`, `exec -a foo node …`, `xargs -I {} node …`, `env -C /tmp node …`, `env -u PATH node …`, `timeout -k 1 5 node …`, `/usr/bin/timeout 5 node …`, `nice '-n' '5' node …`). Sub-root (c) each leading redirection DENIES (`>/dev/null node …`, `2>/dev/null node …`, `2>&1 node …`). AND the over-block-clean angle is preserved: a non-admit wrapper (`timeout 5 node render`, `sudo ls`), a leading-redirection non-admit (`>/dev/null node build`), an inert heredoc/quoted/`#`-comment admit mention behind a wrapper → ALLOW. AND the adversary INVENTS at least one input-surface obfuscation shape the author did NOT enumerate (e.g. a path-form modifier with an operand `/usr/bin/timeout -k 1 5 node …`, a redirection between the wrapper and the launcher `timeout 5 >/dev/null node …`, a quoted-operand chain) — it must be caught structurally (gated or denied), proving the invert fails closed on the unresolvable tail, not on an anchor list.
    2. LOGIC angle — sub-root (b) each unlisted wrapper DENIES (`sudo`/`doas`/`setsid`/`ionice`/`chrt`/`taskset node …admit`). The compounding closures: under `human_admission: all` a routine admit behind a wrapper (`timeout 5 node …admit <routine>`) DENIES (the strictest dial no longer leaks); the Class-E reopening (`node …admit <routine> ; timeout 5 node …admit <high-sev>`, and `&&`/`||`/`|`/`&`/subshell, both orderings, 3-admit variants) DENIES because the wrapped high-severity segment is an enumerated unresolvable admit segment regardless of position; a routine-only multi-admit (some behind wrappers) under `high-severity` ALLOWs (no over-block). AND the adversary INVENTS at least one logic obfuscation shape the author did NOT enumerate (e.g. a 3-admit ordering shielding a wrapped high-severity behind two routines, a wrapped high-severity admit combined with a duplicate `by`, a wrapper under the `all` dial on a routine).
    3. BOTH-DIRECTION RED-vs-committed-`.js`: the clean case (routine / hook-disposed admit) ALLOWs; the zero-config absent lean reads `off` and ALLOWs a routine admit; each planted sub-root + the dial-`all` leak + the Class-E reopening DENIES naming the fail-closed condition. Confirm against `25-07-RED-baseline.txt` (pre-fix ALLOWed the class) and `25-07-GREEN-proof.txt` (post-fix DENIES). The probe reproduces these independently, not from the author's suite.
    4. PRESERVED + FLOOR + freshness: the round-3 closures still hold (Class A path-form launcher DENIES; Class B `$()`/backtick GATES; Class E bare multi-admit shield DENIES while routine-only ALLOWs; Class F duplicate/indented `by` DENIES); the round-2 closures hold (GAP-B self-set behind a wrapper DENIES — incl. behind `timeout 5`/`nice -n 5`; GAP-C non-string/corrupt dial gate-or-stricter while absent stays lean; GAP-D case-variant `by` gated high-severity); the four named floor invariants hold at every dial value (refuse-self, no-fabrication/never-rewrite, `quality.test_integrity` has no `off`, `hooks/guard.ts` byte-frozen via `git diff --quiet hooks/guard.ts` exit 0 and blob `3501810e21308e4b7e219679a6ca30dace9b5d66`); `git diff --quiet scripts/context-io.ts` exit 0 (not edited); `npm run freshness` exits 0; WR-01 did not false-positive on the resolver edit or the new artifacts; the single-authority structural assertion holds (`function tokenize` / `noteFileFromCommand` gone; `COMMAND_MODIFIERS` not widened).
    5. The probe's findings (the exact reproduced commands, the invented obfuscation shapes for BOTH angles, the deny/allow outcomes, the non-admit-wrapper / routine-only / inert controls, the preserved round-3/round-2 results) are DOCUMENTED in `25-07-SUMMARY.md` — reproduced by an independent adversary, NOT self-reported as a green suite.
  </how-to-verify>
  <acceptance_criteria>
    - (a) Each sub-root (a) modifier-operand and (c) leading redirection DENIES + an adversary-invented fresh input-surface shape DENY vs the committed `admission-guard.js`; the non-admit-wrapper / leading-redirection-non-admit / inert mention behind a wrapper ALLOW — reproduced by an INDEPENDENT input-surface probe, not the author's suite (D-12, P23 input-surface angle).
    - (b) Sub-root (b) each unlisted wrapper DENIES; the dial-`all` routine-behind-wrapper DENIES; the Class-E reopening DENIES under high-severity in every ordering/separator while a routine-only multi-admit ALLOWs; + an adversary-invented fresh logic shape — reproduced by an INDEPENDENT logic probe (D-12, P23 logic angle; the angle that found the Class-E reopening + the dial-`all` leak on round 4).
    - (c) The both-direction proof matches `25-07-RED-baseline.txt` (pre-fix ALLOW) → `25-07-GREEN-proof.txt` (post-fix DENY naming the fail-closed condition; non-admit-wrapper / routine-only / inert / zero-config ALLOW), reproduced vs the committed `.js`.
    - (d) The round-3 (Class A/B/E/F) + round-2 (GAP-B/C/D) closures still hold; the four floor invariants hold; `git diff --quiet hooks/guard.ts` exits 0; `git diff --quiet scripts/context-io.ts` exits 0; `npm run freshness` exits 0; WR-01 no false-positive; the single-authority structural assertion holds (second walk still deleted, `COMMAND_MODIFIERS` not widened); findings documented in `25-07-SUMMARY.md` — independently reproduced, not self-reported.
  </acceptance_criteria>
  <action>
    HARD STOP — do NOT auto-pass on a green author suite (the exact D-12 / green-suite-insufficient failure mode this phase already hit FOUR times). Dispatch (or hand off to the human to dispatch) an INDEPENDENT opus-grade red-team — an adversary with different blind spots than the authoring executor — to reproduce (a)–(d) against the COMMITTED `hooks/admission-guard.js` and `scripts/context-io.js` (never the `.ts`, never the author's own suites). The probe MUST run BOTH a LOGIC probe (the unlisted-wrapper class / the dial-`all` routine-behind-wrapper leak / the Class-E reopening / a multi-admit shield behind a wrapper) AND an INPUT-SURFACE attack (a modifier-operand or leading-redirection command word escaping the resolver, or an inert mention behind a wrapper reading as a live admit) — these are the distinct blind spots the P23 lesson and THIS phase's FOUR prior red-teams proved both matter (on round 4 the INPUT-SURFACE agent found the leading redirection the logic agent missed; the LOGIC agent found the Class-E reopening + the dial-`all` leak). The adversary should INVENT at least one shape per angle that the author did NOT enumerate (test the inverted resolver on the unresolvable tail rather than an anchor list — e.g. `/usr/bin/timeout -k 1 5 node …admit`, a redirection between the wrapper and the launcher, a 3-admit ordering shielding a wrapped high-severity behind two routines, a wrapped high-severity admit with a duplicate `by`) and confirm it is caught structurally (denied or gated). Record the reproduced commands, the invented shapes, the clean=allow / planted=deny outcomes, the non-admit-wrapper / routine-only / inert controls, the preserved GAP-B/C/D + Class A/B/E/F results, and confirm guard.ts + context-io.ts byte-unchanged + freshness 0 + the second walk deleted + `COMMAND_MODIFIERS` not widened in `25-07-SUMMARY.md`. Then surface the result to the human for the resume signal. This task is the phase safety floor — it blocks until the independent both-angle proof is in hand.
  </action>
  <verify>
    <human-check>An independent opus-grade red-team has reproduced (a)–(d) vs the committed `.js`: each sub-root (a) modifier-operand and (c) leading redirection DENIES, each sub-root (b) unlisted wrapper DENIES, the dial-`all` routine-behind-wrapper DENIES, the Class-E reopening DENIES under high-severity in every ordering/separator (+ an invented shape per angle), while the non-admit-wrapper / leading-redirection-non-admit / routine-only multi-admit / inert mention behind a wrapper ALLOW; the both-direction proof matches the RED→GREEN artifacts; the round-3 Class A/B/E/F + round-2 GAP-B/C/D closures + the four floor invariants hold with `git diff --quiet hooks/guard.ts` exit 0, `git diff --quiet scripts/context-io.ts` exit 0, freshness 0, the second walk deleted, and `COMMAND_MODIFIERS` not widened; findings recorded in `25-07-SUMMARY.md`. NOT a self-reported author green suite (D-12).</human-check>
  </verify>
  <resume-signal>Type "approved" once the independent both-angle red-team has reproduced (a)–(d) above and the findings (incl. the invented obfuscation shapes for BOTH angles) are recorded in 25-07-SUMMARY.md, or describe the bypass / false-positive found. Do NOT approve on a green author suite alone (D-12, the terminal project lesson — this phase already hit it FOUR times).</resume-signal>
  <done>An independent opus-grade red-team (BOTH logic and input-surface angles) has reproduced the round-4 closure vs the committed `.js`: every modifier-operand / unlisted-wrapper / leading-redirection sub-root is DENIED, the dial-`all` routine-behind-wrapper leak and the Class-E reopening are DENIED in every ordering/separator while a routine-only multi-admit ALLOWs, an invented un-enumerated shape per angle is caught structurally, the non-admit-wrapper / inert mentions ALLOW, the round-3 Class A/B/E/F + round-2 GAP-B/C/D closures + the four floor invariants + guard.ts/context-io.ts byte-frozen status hold, the second walk is deleted and `COMMAND_MODIFIERS` is not widened, findings are documented in `25-07-SUMMARY.md`, and the human approved; SC1 is closed by an independent adversary, not a green suite (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bash `tool_input.command` → admission-guard hook | The command string is untrusted and adversarial. After 25-06 unified the matcher and classifier and closed the path-form launcher (A), `$()`/backtick command word (B), multi-admit shield (E), and tier-inconsistent `by` (F), the agent can still resolve the LEADING RUN to a non-launcher command word — a modifier OPERAND the skip stops at (`timeout 5 node …`), an UNLISTED wrapper (`sudo`/`setsid node …`), or a LEADING REDIRECTION (`>/dev/null node …`) — so the segment is silently dropped (default ALLOW). The hook must INVERT the default: an admit shape behind a command word that does not resolve to a recognized launcher is UNRESOLVABLE and fails CLOSED (gate), exactly as Class B — as ONE authority, on the unresolvable tail, not an anchor list. |
| admit shape (`segmentHasAdmitShape`) → fail-closed trigger | The admit shape (a context-io reference AND the `admit` verb in a LIVE segment) is the ONLY trigger for the invert's gate, so a non-admit wrapper (`timeout 5 node render`, `sudo ls`, `>/dev/null node build`) has no admit shape and stays ALLOW. The over-block fail-safe is structural: no admit shape → no gate. |
| inline approval-var set → refuse-self-set (D-01 floor) | (PRESERVED from 25-05/25-06) A self-set behind any wrapper (incl. `timeout 5`/`nice -n 5`) is refused independent of matcher recognition via the resolution-independent `commandHasLiveAdmitShape` scan. |
| governance config value → hook / admit() | (PRESERVED from 25-05, context-io.ts unchanged) A present non-string / non-object / corrupt config is gate-or-stricter; only exact JSON `"off"` is off-equivalent; absent stays lean. |
| admit() → audit ledger | (Preserved from 25-03, unchanged here) Under `retained`, the ledger records the admission record only, never the note body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-25 | Spoofing / Elevation (modifier OPERAND not consumed) | a real gated high-severity `admit` behind a modifier whose OPERAND halts the leading-run skip (`timeout 5 node …`, `nice -n 5 node …`, `exec -a foo node …`, `xargs -I {} node …`, `env -C /tmp node …`) so the operand reads as the command word and the segment is silently dropped | mitigate | Invert the default: a resolved command word that is not a recognized launcher (nor a dynamic/substitution word), on a segment carrying the admit shape, is UNRESOLVABLE → fail-closed gate. No enumeration of operand grammars. Proven both-direction RED vs the committed `.js` + the modifier-operand fuzz (Task 25-07-01/02/03; sub-root a; D-12). |
| T-25-26 | Spoofing / Elevation (unlisted wrapper) | a gated admit behind a launcher-runner not in `COMMAND_MODIFIERS` (`sudo`/`doas`/`setsid`/`ionice`/`chrt`/`taskset node …admit`) resolving to itself, a non-launcher, so the segment is dropped | mitigate | The invert gates the admit shape behind ANY unrecognized command word — `COMMAND_MODIFIERS` is NOT widened (widening is the closed-enumeration anti-pattern this phase keeps hitting). Proven by the unlisted-wrapper fuzz + RED vs committed `.js` (Task 25-07-01/02/03; sub-root b; D-12). |
| T-25-27 | Spoofing / Elevation (leading redirection) | a gated admit behind a leading redirection (`>/dev/null node …admit`, `2>/dev/null node …`, `2>&1 node …`) whose operator/target `liveTokens` reads as the segment's command word | mitigate | The redirection operator/target resolves to a non-launcher command word → the invert gates on the admit shape. No redirection grammar enumeration is added. Proven by the leading-redirection fuzz + RED vs committed `.js` (Task 25-07-01/02/03; sub-root c; D-12). |
| T-25-28 | Elevation of Privilege (dial-`all` floor leak + Class-E reopening) | at dial `all` a ROUTINE admit behind a wrapper (`timeout 5 node …admit <routine>`) slipping the strictest dial's floor; and a high-severity admit shielded behind a routine admit + a wrapper (`node …admit <routine> ; timeout 5 node …admit <high-sev>`) under `high-severity` | mitigate | The invert makes the wrapped segment an enumerated unresolvable admit segment, so gate-every-match (`all`) denies the routine-behind-wrapper and the per-segment walk (25-06 Class E) denies the shielded high-severity regardless of order. Proven by the dial × admit-ordering fuzz + RED vs committed `.js` (Task 25-07-01/02/03; D-12). |
| T-25-29 | Denial of Service (over-block regression) | the invert over-blocking a non-admit wrapper, a leading-redirection non-admit, a routine-only multi-admit, or an inert mention behind a wrapper | mitigate (fail-safe) | The invert's fail-closed gate fires ONLY on the admit shape (`segmentHasAdmitShape`/`rawTextHasAdmitShape`) in a LIVE segment; a non-admit wrapper (`timeout 5 node render`, `sudo ls`, `>/dev/null node build`) has no admit shape and stays ALLOW; a routine-only multi-admit under `high-severity` stays ALLOWED; inert heredoc/quoted/comment mentions behind a wrapper stay ALLOW. Proven by the fuzz + the independent red-team (Task 25-07-01/02/03/04). |
| T-25-30 | Tampering (single-authority drift / enumeration regression) | reintroducing a second command parser, widening `COMMAND_MODIFIERS`, or enumerating operand/redirection grammars one notch at a time (the disease behind every prior round) | mitigate | The invert lands INSIDE the one `liveAdmitSegments` walk; `function tokenize` / `noteFileFromCommand` stay deleted (grep=0); `COMMAND_MODIFIERS` is unchanged; the gate is admit-shape-triggered on the unresolvable tail, not an anchor list (the P22 round-8 "the boundary IS the parser, fail closed on the unresolvable tail" lesson). A grep asserts no widening and no second walk (Task 25-07-01). |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | N/A — this gap-closure installs NO external package (RESEARCH Package Legitimacy Audit: not applicable; typescript/vitest already present). No install task exists. |
</threat_model>

<verification>
- `npm run build && npm run freshness` exits 0 (committed `admission-guard.js` is a fresh, byte-faithful tsc build; 0 drift).
- `git diff --quiet hooks/guard.ts` exits 0 (deploy guard byte-frozen, blob `3501810e21308e4b7e219679a6ca30dace9b5d66`, D-02).
- `git diff --quiet scripts/context-io.ts` exits 0 (NOT edited this round — GAP-C/GAP-D + the audit ledger preserved; the entire fix lives in the hook's command resolution).
- `npx vitest run hooks/admission-guard.test.ts` green: every sub-root (a modifier-operand / b unlisted-wrapper / c leading-redirection) DENIES; the dial-`all` routine-behind-wrapper DENIES; the Class-E reopening DENIES while routine-only ALLOWs; the over-block controls (non-admit wrapper, leading-redirection non-admit, routine-only multi-admit, inert mention behind a wrapper) ALLOW; the round-3 Class A/B/E/F + round-2 launchers + GAP-B/GAP-D still hold; case count >= guard.test.ts.
- `npx vitest run scripts/floor-invariance.test.ts` green: the wrapper × modifier-operand × leading-redirection × dial × admit-ordering fuzz; the round-2 + round-3 sweeps; the four named floor invariants hold at every value.
- A grep asserts `function tokenize` and `noteFileFromCommand` no longer exist in hooks/admission-guard.ts (single resolution authority) and `COMMAND_MODIFIERS` membership is unchanged (no widening).
- `npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial; D-13 no kit-content false-positive).
- `npx vitest run --exclude '**/scripts/e2e/**'` green (full non-e2e regression — bare `npm test` deliberately NOT run; it triggers the live claude-CLI e2e lane, see MEMORY grugops-npm-test-triggers-live-e2e).
- `25-07-RED-baseline.txt` + `25-07-GREEN-proof.txt` capture the both-direction proof vs the committed `.js` (extending the 25-06 row format).
- INDEPENDENT both-angle red-team (Task 25-07-04, before /gsd-verify-work): confirms SC1 round-4 closure adversarially — not self-reported green (D-12).
</verification>

<success_criteria>
- SC1 (the leading-run resolution class): the GOV-01 human gate is no longer forgeable behind a modifier operand (a), an unlisted wrapper (b), or a leading redirection (c) — the leading-run resolver fails CLOSED on the unresolvable tail (an admit shape behind a command word that does not resolve to a recognized launcher → gate), exactly as Class B, so the dial-`all` routine leak and the Class-E reopening are closed; the invert lands in the ONE `liveAdmitSegments` authority with `COMMAND_MODIFIERS` unwidened and the second walk still deleted; the hook stays the only grant path.
- PRESERVED (no regression): the round-3 closures (Class A/B/E/F), the round-2 LOGIC closures (GAP-B/C/D), the verified SC2 config foundation + the GOV-02 audit ledger, and the 25-04/25-05/25-06 segmentation are unchanged; `scripts/context-io.ts` is not edited.
- Fail-safe (no over-block regression): a non-admit wrapper (`timeout 5 node render`, `sudo ls`, `>/dev/null node build`), a routine-only multi-admit under `high-severity`, and an inert heredoc/quoted/comment mention behind a wrapper all ALLOW; the invert's fail-closed gate fires only on the admit shape in a live segment.
- Proof (D-12): both-direction RED→GREEN vs the COMMITTED `.js` (captured artifacts) + an anti-whack-a-mole command-resolution fuzz + an INDEPENDENT both-angle (logic + input-surface) opus-grade red-team that invents an un-enumerated obfuscation shape per angle — not a green author suite. `hooks/guard.ts` + `scripts/context-io.ts` byte-unchanged; freshness 0; the second walk deleted; `COMMAND_MODIFIERS` unwidened.
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-07-SUMMARY.md` when done. Record: the structural INVERT inside `liveAdmitSegments` (how a resolved command word that is neither a launcher nor a dynamic/substitution word, on a segment carrying the admit shape, is pushed as an UNRESOLVABLE admit segment → fail-closed gate, closing sub-roots a/b/c + the dial-`all` leak + the Class-E reopening from ONE admit-shape-triggered branch) and confirmation `COMMAND_MODIFIERS` was NOT widened, no second walk was reintroduced (`function tokenize` / `noteFileFromCommand` grep=0), and the `liveTokens` tokenizer is byte-identical; confirmation `scripts/context-io.ts` is unedited (`git diff --quiet` exit 0 — GAP-C/GAP-D + ledger preserved); the fuzz matrix dimensions; the RED→GREEN proof artifact paths; confirm `git diff --quiet hooks/guard.ts` clean and freshness 0; and the INDEPENDENT both-angle red-team's reproduced commands + the invented obfuscation shapes (both angles) + outcomes + the preserved round-3 (Class A/B/E/F) + round-2 (GAP-B/C/D) results. Flag for the verifier that SC1 closure REQUIRES the independent red-team — the sweeps/fuzz are necessary, not sufficient (D-12, this phase's green-suite-insufficient discipline). Note that SC2 + the GOV-02 audit ledger (25-01/02/03), the round-3 closures (25-06), and the round-2 LOGIC closures (25-05) are PRESERVED untouched.
</output>

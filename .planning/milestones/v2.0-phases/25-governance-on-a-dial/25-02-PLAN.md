---
phase: 25-governance-on-a-dial
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - hooks/admission-guard.ts
  - hooks/admission-guard.js
  - hooks/admission-guard.test.ts
  - hooks/hooks.json
  - scripts/context-io.ts
  - scripts/context-io.js
  - agent-factory/contracts/context-note.md
  - agent-factory/workflows/16-context-read-write.md
autonomous: true
requirements: [GOV-01]
user_setup: []

must_haves:
  truths:
    - "A high-severity admission (`by ∈ {security-nfr, architect-design, release-manager}`) under `human_admission ≠ off` is DENIED by the committed `hooks/admission-guard.js` unless the human-set session var `GRUGOPS_ADMISSION_APPROVED_BY=NAME` is present (GOV-01 SC1, deny direction)."
    - "The SAME high-severity admission WITH the human-set var present is ALLOWED; a routine note (`by: software-engineer`) under `high-severity` is ALLOWED; under `all`, every matched admission requires the stamp (GOV-01 SC1, allow + routine + all directions)."
    - "An inline self-set/export of the approval var (`export GRUGOPS_ADMISSION_APPROVED_BY=eve && node …context-io.js admit …`) is DENIED even when that var is already in the spawned env — an agent can never grant its own admission (D-01)."
    - "The guard FAILS CLOSED: on a MATCHED high-severity admit, an unreadable/unparsable note or an unreadable config DENIES — it never crash-allows; an empty/malformed stdin yields no matched command and is allowed (nothing to gate)."
    - "A second PreToolUse Bash matcher in `hooks.json` wires `admission-guard.js` beside the unchanged `guard.js`; both run independently (most-restrictive-deny-wins); the committed `admission-guard.js` is fresh and the deferral markers in `context-io.ts`, `context-note.md`, and workflow 16 are closed in clear voice (D-03)."
  artifacts:
    - path: "hooks/admission-guard.ts"
      provides: "GOV-01 un-forgeable admission gate — verb-anchored admit match, re-read-the-note severity classification, human-set-var check, refuse-self-set, fail-closed deny()"
      min_lines: 60
    - path: "hooks/admission-guard.js"
      provides: "committed compiled output (freshness auto-covers hooks/); the artifact every test + the hook runs"
      contains: "permissionDecision"
    - path: "hooks/admission-guard.test.ts"
      provides: "child-spawn deny/allow oracle vs the COMMITTED .js; ≥ guard.test.ts case count (currently 27)"
      contains: "spawnSync"
    - path: "hooks/hooks.json"
      provides: "the second PreToolUse Bash matcher group → admission-guard.js"
      contains: "admission-guard.js"
  key_links:
    - from: "hooks/admission-guard.ts"
      to: "the note file named in the matched `admit` Bash command (argv rest[1])"
      via: "verb-anchored match on tool_input.command, then readFileSync(noteFile) and parse `by` with the single column-0 grammar"
      pattern: "context-io.*admit|readFileSync"
    - from: "hooks/admission-guard.ts"
      to: "scripts/context-io.ts readGovernanceConfig (Plan 25-01)"
      via: "imports the ONE shared config-read path so the hook and admit() cannot diverge (OQ-3)"
      pattern: "readGovernanceConfig"
    - from: "hooks/hooks.json"
      to: "hooks/admission-guard.js"
      via: "second PreToolUse Bash matcher group, shell form with quoted ${CLAUDE_PLUGIN_ROOT}"
      pattern: "admission-guard\\.js"
  prohibitions:
    - statement: "The deny path MUST be reproduced RED vs the COMMITTED `hooks/admission-guard.js` in BOTH directions — clean (no high-severity-without-approval) = ALLOW exit 0; planted high-severity finding without the human stamp = DENY naming the note. NOT a self-reported green suite (D-12, [[grugops-safety-invariant-green-suite-insufficient]])."
      status: must-hold
      verification: "hooks/admission-guard.test.ts spawns the committed .js as a child for every case; the verifier independently reproduces clean=allow / planted=deny vs the committed artifact before the floor is considered proven."
    - statement: "The approval var is NEVER self-settable by the agent — any inline set/export is denied EVEN IF the var is already present in env. This is the entire D-01 reason it is a separate hook process (reading the human-set SESSION env), not an in-script env check inside admit()."
      status: must-hold
      verification: "the refuse-self-set case denies with the var in the spawned env; the SELF_APPROVE regex clones guard.ts:88; verified vs the committed .js."
    - statement: "The guard NEVER reads a fenced / doc-example / kit-content write token as a live admission (inverse of the P23 CR-01 false-positive) — it fires ONLY on a real Bash `tool_input.command`, never on file content. A markdown example is never a live admit command."
      status: must-hold
      verification: "the guard's input is `tool_input.command` only; a doc-example case (a fenced/markdown `node context-io.js admit …` string that is NOT a Bash tool call) is never seen by the guard; the independent red-team probes the input surface for this exact false-positive."
    - statement: "`hooks/guard.ts` stays BYTE-FROZEN (D-02) — it is a safety-critical file with a long bypass history; admission and prod-deploy are distinct concerns (D-10 separation)."
      status: must-hold
      verification: "`git diff --quiet hooks/guard.ts` (no change); the deploy guard's own tests stay green; admission-guard.ts is a new separate file."
    - statement: "An INDEPENDENT adversarial probe is required on BOTH the guard INPUT SURFACE (doc example / kit write token / inline-content / argv quirk reading as a live signal) AND its LOGIC (any severity edge / dial value / stamp grammar opening a hole) before the floor is proven — the orchestrator's own logic-probe is not a substitute (P23: input-surface vs logic are different blind spots)."
      status: must-hold
      verification: "before /gsd-verify-work: an independent opus-grade red-team reproduces the both-direction deny vs the committed .js AND attacks the input surface; documented, not self-reported."
---

<objective>
Build the GOV-01 mechanically un-forgeable human-admission gate as a NEW `hooks/admission-guard.ts` → committed `admission-guard.js`, cloning the byte-frozen prod-deploy guard's posture (fail-closed stdin parse, refuse-self-set, exit-0+JSON `deny()`) but diverging on the INPUT SURFACE: verb-anchor-match the `node …context-io.js admit TASK NOTEFILE [root]` Bash command, parse the note file path from argv, re-read that file, and classify high-severity by the note's authoring role `by` (D-06). Deny a high-severity admission under `human_admission ≠ off` unless the human-set session var `GRUGOPS_ADMISSION_APPROVED_BY=NAME` is present; refuse any agent self-set of that var; fail closed on a matched admit. Wire it as a SECOND PreToolUse matcher in `hooks.json` beside the unchanged `guard.js`, and close the "un-forgeable signal layered in Phase 25" deferral markers in `context-io.ts`, `context-note.md`, and workflow 16 (D-03) — in clear voice, without tripping WR-01.

Purpose: this is the safety-critical heart of the phase — the only mechanically un-forgeable admission tier (CC-only, D-05). A separate hook process reading the human-set SESSION env is un-forgeable because the agent's self-set in its own child env never reaches it; this mirrors the deploy guard exactly (D-01).
Output: a new admission guard (.ts + committed .js), a child-spawn deny/allow oracle ≥ guard.test.ts's case count, the second `hooks.json` matcher, and three closed deferral markers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-RESEARCH.md
@.planning/phases/25-governance-on-a-dial/25-PATTERNS.md
@.planning/phases/25-governance-on-a-dial/25-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 25-02-01: Create hooks/admission-guard.ts (clone guard.ts posture; re-read-the-note severity gate) + the child-spawn oracle</name>
  <files>hooks/admission-guard.ts, hooks/admission-guard.js, hooks/admission-guard.test.ts</files>
  <read_first>
    - hooks/guard.ts:29-34 (imports + the `const APPROVAL = …` declaration — clone the shape; the new var name is `GRUGOPS_ADMISSION_APPROVED_BY`)
    - hooks/guard.ts:88 (the SELF_APPROVE refuse-self-set regex — clone verbatim, swap only the var name: `(^|[\s;&|(])(export\s+|env\s+)?${APPROVAL}\s*=`)
    - hooks/guard.ts:90-101 (the deny() exit-0+JSON helper — clone verbatim: `permissionDecision:"deny"` + a reason, then `process.exit(0)`)
    - hooks/guard.ts:103-137 (the fail-closed stdin parse asymmetry — malformed/empty stdin → cmd="" → matches nothing → allow; once the command IS a matched high-severity admit, any later failure must deny())
    - hooks/guard.ts:50-77 (the verb-anchor discipline — "Match the SUBCOMMAND VERB, not a substring"; mirror it for the admit CLI)
    - scripts/context-io.ts:208-213 (isRecognizedFrontmatterLine — the SINGLE column-0 `key: value` grammar; reuse it to parse `by`, never a second grammar — the P22/CMP-02 drift class)
    - scripts/context-io.ts:992-1007 (the admit CLI signature — `admit TASK NOTEFILE [contextRoot]`; the note path is the 2nd positional after `admit` = argv rest[1]; CONFIRMED there is NO `append` CLI verb, only validate/admit/render, so the guard matches ONLY `admit`)
    - scripts/context-io.ts (the exported `readGovernanceConfig` helper added by Plan 25-01 — import it as the ONE shared config-read path so the hook and admit() cannot diverge, OQ-3; resolve the repo config from `${CLAUDE_PROJECT_DIR}` per CLAUDE.md / RESEARCH OQ-3)
    - hooks/guard.test.ts:22-57 (the child-spawn runner targeting the COMMITTED .js — `GUARD_JS = join(import.meta.dirname, "admission-guard.js")`, spawnSync("node",[GUARD_JS],{input,env}), expectDeny/expectAllow on `"permissionDecision":"deny"`)
    - hooks/guard.test.ts:69-78 (the refuse-self-set case shape — denied even with the var in env)
    - hooks/guard.test.ts:165-198 (the fail-closed / malformed-stdin cases to clone)
  </read_first>
  <behavior>
    - Deny: a real Bash command `node scripts/context-io.js admit T NOTEFILE` where `NOTEFILE` (re-read from disk) has `by: security-nfr` AND `human_admission` is `high-severity` AND no `GRUGOPS_ADMISSION_APPROVED_BY` in env → DENY, the reason names the note path.
    - Allow: the SAME command with `GRUGOPS_ADMISSION_APPROVED_BY=alice` set in the spawned env (and NOT inline-set in the command) → ALLOW (no `"deny"` in stdout).
    - Allow (routine): the same command where the re-read note has `by: software-engineer` under `human_admission: high-severity` → ALLOW (routine roles are not gated at `high-severity`).
    - Deny (all): under `human_admission: all`, the routine `by: software-engineer` note with no approval var → DENY (every matched admission is gated under `all`).
    - Allow (off): under `human_admission: off` (lean default), even a `by: security-nfr` note → ALLOW (no human stop in lean mode).
    - Refuse-self-set: `export GRUGOPS_ADMISSION_APPROVED_BY=eve && node scripts/context-io.js admit T <hi-sev-note>` with `GRUGOPS_ADMISSION_APPROVED_BY=eve` ALSO in env → DENY (the inline set is refused even though the var is present).
    - Fail-closed: a MATCHED high-severity admit whose `NOTEFILE` is unreadable / has an unparsable `by` / whose config read throws → DENY (never crash-allow).
    - Allow (nothing to gate): empty stdin, malformed JSON, or a non-admit Bash command (e.g. `ls`, or `node context-io.js render T`) → ALLOW (cmd matches no admit; nothing to gate).
    - Input-surface inertness: a `tool_input.command` that is NOT a real admit invocation — e.g. an `echo`/`cat` of a markdown doc that CONTAINS the text `node context-io.js admit …`, or a fenced example string — does not parse as an admit verb and is ALLOWED (the guard reads the command verb, not embedded content).
  </behavior>
  <action>
    Create `hooks/admission-guard.ts` cloning `hooks/guard.ts` byte-for-pattern for the safety scaffolding: the imports + `const APPROVAL = "GRUGOPS_ADMISSION_APPROVED_BY"` (guard.ts:29-34, placeholder var name per Deferred Ideas), the fail-closed stdin parse (guard.ts:103-115 — `try { JSON.parse(readFileSync(0,"utf8")) } catch { cmd = "" }`, and `if (typeof cmd !== "string") cmd = ""`), the `SELF_APPROVE` regex (guard.ts:88, swap only the var name), and the `deny(reason)` exit-0+JSON helper (guard.ts:90-101 — `hookSpecificOutput` with `hookEventName:"PreToolUse"`, `permissionDecision:"deny"`, `permissionDecisionReason`, then `process.exit(0)`).

    DIVERGENCE from the deploy guard = the INPUT SURFACE (this is the critical design, RESEARCH Pattern 2):
    1. Verb-anchor-match the admit command on `cmd` (mirror guard.ts:50 "match the subcommand VERB, not a substring"): match a real `node …context-io(.js) admit …` invocation. CONFIRMED there is NO CLI `append` verb (context-io.ts dispatch exposes only validate/admit/render) — match ONLY `admit`. If `cmd` does not match an admit invocation → ALLOW (`process.exit(0)`; nothing to gate). Mirror guard.ts's asymmetry: an empty/malformed stdin yields `cmd=""` which matches nothing and is allowed.
    2. If the command IS a matched admit: refuse-self-set FIRST — if `SELF_APPROVE.test(cmd)` → `deny()` (an agent may never grant its own admission, even if the var is already in env; clone guard.ts:119-125).
    3. Parse the note file PATH from the command's argv — it is the 2nd positional after `admit` (`rest[1]`, per context-io.ts:992-1007 `admit TASK NOTEFILE [contextRoot]`). Split the command into argv tokens, find the `admit` verb, take the note path as the next-but-one positional.
    4. Read the dial via the shared `readGovernanceConfig` from Plan 25-01 (resolve the repo from `${CLAUDE_PROJECT_DIR}` — the documented hook project root, CLAUDE.md): `off` → ALLOW (no gate); `high-severity` → gate only the 3 high-severity roles; `all` → gate EVERY matched admission. Fail closed: if the config read throws on a matched admit → `deny()` (but `readGovernanceConfig` is built in 25-01 to never throw and to return lean defaults; the fail-closed catch is the belt-and-suspenders for any future read path).
    5. Re-read the note file (`readFileSync(noteFile)`); parse its `by` field using the EXACT single column-0 `key: value` grammar `context-io.ts` uses (`isRecognizedFrontmatterLine`, context-io.ts:208-213) — never a substring scan, never a second YAML grammar (the P22/CMP-02 drift class). If the note is unreadable or `by` is unparsable on a MATCHED admit that the dial would gate → `deny()` (fail closed; the note already failed `validate()` upstream, but the guard must not crash-allow).
    6. Classify high-severity = `by ∈ {"security-nfr","architect-design","release-manager"}` (D-06, the clean 3:3 security/architecture/release map; NO self-declared `severity` field — it would be gameable downward).
    7. Decide: if this admission is gated by the dial (high-severity under `high-severity`, OR any admission under `all`) AND `process.env.GRUGOPS_ADMISSION_APPROVED_BY` is absent/empty → `deny()` naming the note path and the required disposition. Otherwise `process.exit(0)` (ALLOW).

    Voice: CLEAR PROFESSIONAL VOICE in all comments and deny reasons (CLAUDE.md hard rule — security/governance/audit surfaces are never caveman voice). The deny reason states plainly that a high-severity governance entry requires a named human disposition and how to provide it (the human sets the session var, never the agent).

    Create `hooks/admission-guard.test.ts` cloning `hooks/guard.test.ts` exactly (the child-spawn harness at guard.test.ts:22-57 — `GUARD_JS = join(import.meta.dirname, "admission-guard.js")`, the COMMITTED .js never the .ts; `runGuard(json, env)` via `spawnSync`; `expectDeny`/`expectAllow` on `"permissionDecision":"deny"`; `payload(command)`). The deny/allow cases MUST write a real note file to a temp dir (a high-severity `by: security-nfr` note and a routine `by: software-engineer` note) and point a temp `factory.config.json` (or the helper's explicit-path arg) at each `human_admission` value, so the guard's re-read-from-disk + config-read paths are exercised end to end. Cover ALL nine behaviors above. It MUST have ≥ as many `it(...)` cases as `hooks/guard.test.ts` (currently 27 — the Pitfall-2 count watch; fewer cases = a silently weakened deny path). Both directions (clean=allow, planted=deny) run RED vs the committed .js (D-12).

    Rebuild the committed `.js`: run `npm run build` (tsc) so `hooks/admission-guard.js` is a fresh faithful build, then `npm run freshness` must show 0 drift (freshness auto-covers `hooks/`).

    DO NOT edit `hooks/guard.ts` (D-02 byte-frozen). Verify it is untouched (`git diff --quiet hooks/guard.ts`).
  </action>
  <verify>
    <automated>npm run build && npm run freshness && git diff --quiet hooks/guard.ts && npx vitest run hooks/admission-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `hooks/admission-guard.ts` exists and `hooks/admission-guard.js` is its fresh committed build (`npm run freshness` exits 0).
    - `git diff --quiet hooks/guard.ts` exits 0 (the deploy guard is byte-frozen, D-02).
    - `npx vitest run hooks/admission-guard.test.ts` is GREEN with ≥ 27 `it(...)` cases (≥ guard.test.ts), covering: deny (high-sev, no approval), allow (high-sev + var in env), allow (routine under high-severity), deny (routine under `all`), allow (high-sev under `off`), refuse-self-set (denied with var in env), fail-closed (unreadable/unparsable note + config-throw on a matched admit), allow (empty/malformed stdin + non-admit command), input-surface inertness (embedded admit text in an echo/cat is not a matched admit).
    - The guard imports `readGovernanceConfig` from `../scripts/context-io` (the ONE shared config path) and parses `by` via the `isRecognizedFrontmatterLine` grammar (no second grammar — grep shows no bespoke YAML parse).
    - The guard imports ONLY node:fs/path (+ the shared helper); zero new host runtime deps.
  </acceptance_criteria>
  <done>A new `hooks/admission-guard.ts` mirrors the deploy guard's un-forgeable posture, re-reads the note file to classify severity by `by` (one grammar), denies an un-approved high-severity admission and refuses self-set, fails closed on a matched admit, and is inert to file content / doc examples; the committed `.js` is fresh; `guard.ts` is byte-unchanged; the child-spawn oracle (≥ 27 cases, both directions vs the committed .js) is green.</done>
</task>

<task type="auto">
  <name>Task 25-02-02: Wire the second hooks.json matcher + close the Phase-25 deferral markers (clear voice, WR-01-safe)</name>
  <files>hooks/hooks.json, scripts/context-io.ts, agent-factory/contracts/context-note.md, agent-factory/workflows/16-context-read-write.md</files>
  <read_first>
    - hooks/hooks.json (the current single PreToolUse Bash matcher group → guard.js; add a SECOND group beside it, shell form with quoted ${CLAUDE_PLUGIN_ROOT}, identical shape to the guard.js entry)
    - hooks/guard.ts comment block on the deploy-guard's un-forgeability mechanism (the clear-voice, separate-process-reads-human-set-session-env language to mirror in the closed markers)
    - scripts/context-io.ts:108 (the deferral comment: `human:NAME the escalation valve (D-07); its un-forgeability is layered in Phase 25.` — close it)
    - scripts/context-io.ts:850 (the deferral comment: `(its un-forgeability is Phase 25). Keeping this a DISTINCT function preserves the D-10 separation:` — close it)
    - agent-factory/contracts/context-note.md:107-108 (the `human:NAME` stamp deferral line: `accepted structurally ... layered in Phase 25).` — close it)
    - agent-factory/workflows/16-context-read-write.md:21 (the deferral clause: `its un-forgeable human-set signal is layered in Phase 25 — Phase 21 recognizes the grammar, refuses self, and documents the residual honestly.` — close it)
    - scripts/check-foundation-guards.ts CTX_WRITE_RE + CTX_SCAN set (the WR-01 guard: a `.grugops/context/` path AND a write token on the SAME line trips it; context-note.md + workflow 16 are in the SCAN set — D-13 watch)
  </read_first>
  <behavior>
    - `hooks.json` parses and contains TWO PreToolUse Bash matcher groups: the unchanged guard.js group and a new admission-guard.js group.
    - `npm run build && npm run freshness` stays 0-drift: BOTH `admission-guard.js` AND `scripts/context-io.js` are fresh (tsc preserves the closed deferral comments in the compiled output, so `context-io.ts` edits change the committed `context-io.js` and must be rebuilt).
    - `npx vitest run scripts/check-foundation-guards.test.ts` stays GREEN after the marker edits (WR-01 not tripped).
    - The four deferral markers no longer say "layered in Phase 25"; each states the now-true mechanism in clear voice (the un-forgeable human-set signal is the separate PreToolUse `admission-guard` hook reading the human-set session var — CC primary, the 4 non-CC CLIs degrade to the D-04 in-script refusal, documented honestly per D-05).
  </behavior>
  <action>
    Edit `hooks/hooks.json`: add a SECOND PreToolUse Bash matcher group beside the existing `guard.js` group, identical in shape — `{ "matcher": "Bash", "hooks": [{ "type": "command", "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/admission-guard.js\"" }] }` (shell form, quoted `${CLAUDE_PLUGIN_ROOT}`, matching the guard.js entry exactly). Per the VERIFIED CC contract (RESEARCH), both groups run in parallel, most-restrictive-deny-wins, and one hook's deny does NOT suppress a sibling's side effects — so `admission-guard.js` is already fully self-sufficient (it never relies on guard.js).

    Close the four "layered in Phase 25" deferral markers (D-03) in CLEAR PROFESSIONAL VOICE, replacing the placeholder/deferral language with the now-true statement: the `human:NAME` stamp's un-forgeable human-set signal is delivered by the separate PreToolUse `admission-guard` hook — a separate process reading the human-set session var that the agent's own child env cannot reach (mirroring the prod-deploy guard); this is the CC primary tier, and the four non-CC CLIs degrade to the in-script `admit()` refusal (D-04) plus a prompt-level "stop, ask a named human," documented honestly as not mechanically un-forgeable (D-05). Edit each in place:
    - `scripts/context-io.ts:108` (comment) and `scripts/context-io.ts:850` (comment).
    - `agent-factory/contracts/context-note.md:107-108`.
    - `agent-factory/workflows/16-context-read-write.md:21`.

    WR-01 DISCIPLINE (D-13, held empirically in Phase 24): `context-note.md` and workflow 16 are in the WR-01 SCAN set. In the closed markers, reference the `admission-guard` hook / Workflow 16 / the config key `human_admission` BY NAME; do NOT restate a raw `.grugops/context/` (or `.grugops/audit/`) write path beside a write token (`writeFileSync`/`Write`/`>>`/`echo`) on the same line. Describe the mechanism in prose (a separate hook reads a human-set session var); name no raw write path next to a write verb. `context-io.ts:108/850` are comments in a non-SCAN file but keep the same discipline.

    Rebuild + verify: `npm run build` then `npm run freshness` (0 drift), and `npx vitest run scripts/check-foundation-guards.test.ts` (WR-01 + the config-dial contract stay green after the prose edits).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))" && grep -q "admission-guard.js" hooks/hooks.json && npm run build && npm run freshness && npx vitest run scripts/check-foundation-guards.test.ts && ! grep -rn "layered in Phase 25" scripts/context-io.ts agent-factory/contracts/context-note.md agent-factory/workflows/16-context-read-write.md</automated>
  </verify>
  <acceptance_criteria>
    - `hooks/hooks.json` parses as valid JSON and contains a second Bash matcher group naming `admission-guard.js` beside the unchanged `guard.js` group.
    - `npm run build && npm run freshness` exits 0 — the deferral-marker edits to `scripts/context-io.ts` are rebuilt (tsc preserves comments in the output), so the committed `scripts/context-io.js` is a fresh faithful build and the freshness gate shows 0 drift (this AC covers BOTH `admission-guard.js` and `context-io.js`).
    - `npx vitest run scripts/check-foundation-guards.test.ts` is GREEN (WR-01 / config-dial contract unaffected by the prose edits).
    - No occurrence of "layered in Phase 25" remains in `scripts/context-io.ts`, `agent-factory/contracts/context-note.md`, or `agent-factory/workflows/16-context-read-write.md` — each marker now states the now-true mechanism in clear voice.
    - The closed markers in the WR-01 SCAN files reference the `admission-guard` hook / Workflow 16 / `human_admission` by name and restate NO raw `.grugops/` write path beside a write token.
  </acceptance_criteria>
  <done>A second PreToolUse Bash matcher wires admission-guard.js beside the byte-frozen guard.js (both independent, most-restrictive-deny-wins); the four "layered in Phase 25" deferral markers are closed in clear voice naming the now-live separate-hook mechanism and the honest CC-only residual (D-05); freshness and the foundation guards (WR-01) stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (Bash tool) → CC PreToolUse hook | The agent issues `node …context-io.js admit …`; the separate-process hook reads the human-set SESSION env the agent's own child env cannot reach. Untrusted: the command string, the named note file's contents, and any inline env-set the agent attempts. |
| note file on disk → admission-guard | The guard re-reads the named note file to classify severity by `by`; an unreadable/unparsable/relabeled note is untrusted input that must fail closed on a matched admit. |
| config file → admission-guard | `human_admission` is read via the shared `readGovernanceConfig`; a missing/garbage config degrades to the lean default (`off`), and a read throw on a matched admit fails closed (deny). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Elevation of Privilege | the approval session var | mitigate | The HOOK is a separate process reading the human-set SESSION env; the agent's inline self-set never reaches it. `SELF_APPROVE` regex (clone guard.ts:88) denies any inline set/export EVEN IF the var is present. Proven RED vs the committed .js. |
| T-25-02 | Spoofing | note `by` relabeled to dodge the gate | mitigate | Severity = authoring role `by` only (D-06); relabeling `by` is already an impersonation / fabrication FAIL in `validate()` (the running agent IS that role). No self-declared `severity` field. The `all` setting is the paranoid escape valve for the cross-cutting residual. |
| T-25-04 | Tampering (false-positive) | a governance DOC EXAMPLE / kit write token read as a live admission | mitigate | The guard fires ONLY on a real Bash `tool_input.command`, never on file content — a fenced/markdown `node context-io.js admit …` string (or an echo/cat of one) is not a matched admit. WR-01 prose references the hook/Workflow 16/config key, never a raw `.grugops/` write path beside a write token (D-13). Independent red-team probes the input surface (inverse P23 CR-01). |
| T-25-05 | Elevation of Privilege (fail-open / crash-allow) | a malformed note / unreadable config on a MATCHED high-severity admit | mitigate | Fail closed: once the command IS a matched gated admit, any later failure (unreadable/unparsable note, config read throw) → `deny()`, never crash-allow. The empty/malformed-stdin asymmetry (cmd="" → allow) is correct — nothing to gate. |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this phase installs NO external package (RESEARCH Package Legitimacy Audit: not applicable; typescript/vitest already present). No install task exists. |
</threat_model>

<verification>
- `npm run build && npm run freshness` exits 0 (admission-guard.js is a fresh faithful build).
- `git diff --quiet hooks/guard.ts` exits 0 (deploy guard byte-frozen, D-02).
- `npx vitest run hooks/admission-guard.test.ts` green with ≥ 27 cases, both directions (clean=allow / planted=deny) vs the committed .js.
- `hooks/hooks.json` parses and names `admission-guard.js` in a second matcher group.
- `npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 unaffected by the closed markers).
- No "layered in Phase 25" remains in the three deferral-marker files.
- INDEPENDENT red-team (before /gsd-verify-work): reproduces the both-direction deny vs the committed .js AND probes the input surface (doc-example / kit token / inline-content / argv quirk) AND the logic (severity edge / dial value / stamp grammar) — not self-reported green (D-12).
</verification>

<success_criteria>
- GOV-01 SC1: under `high-severity`/`all`, a high-severity admission is denied absent the human-set var and allowed with it; routine notes admit without a stop; `off` runs lean — proven by the child-spawn oracle RED vs the committed .js, independently red-teamed.
- The approval var is never agent-self-settable (refuse-self-set denies with the var in env); the guard is inert to file content / doc examples; it fails closed on a matched admit.
- `guard.ts` byte-frozen; the second matcher is wired; the committed .js is fresh; the four deferral markers are closed in clear voice; foundation guards green.
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-02-SUMMARY.md` when done. Record: the exact admit-verb match regex + the argv note-path extraction rule + the severity role set, the `human_admission` dial→gate mapping (off/high-severity/all), confirmation that `guard.ts` is byte-unchanged and the committed admission-guard.js is fresh, the final case count of admission-guard.test.ts vs guard.test.ts (≥ 27), and the closed-marker wording for the three SCAN/comment files. Flag for the verifier that the both-direction-vs-committed-.js proof + the independent input-surface-AND-logic red-team are REQUIRED before this floor is considered proven (D-12).
</output>
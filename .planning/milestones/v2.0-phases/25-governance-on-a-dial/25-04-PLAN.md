---
phase: 25-governance-on-a-dial
plan: 04
type: execute
wave: 4
gap_closure: true
depends_on: ["25-01", "25-02", "25-03"]
autonomous: false
files_modified:
  - hooks/admission-guard.ts
  - hooks/admission-guard.js
  - hooks/admission-guard.test.ts
  - scripts/context-io.ts
  - scripts/context-io.js
  - scripts/context-io.test.ts
  - scripts/floor-invariance.test.ts
requirements: [GOV-01, GOV-02]
user_setup: []

must_haves:
  truths:
    - "GAP1/SC1 — the GOV-01 human gate is no longer forgeable: a real gated high-severity `admit` launched via a subshell `( node …context-io.js admit … )`, a backslash-newline line continuation (`node \\<newline> …admit …`), or `npx tsx scripts/context-io.ts admit …` (and bare `npx`/`tsx`) is RECOGNIZED by the hook as a live admit and DENIED (when un-approved) — the hook no longer returns ALLOW while the command admits the note."
    - "GAP1/SC1 — the matcher is a real shell-segment parser, not a widened regex firewall: ONE parsing authority recognizes every admit launcher (node, npx, tsx, npx tsx, subshell prefix, `\\`-continuation) AND treats quoted strings, heredoc bodies, and `#`-commented text as INERT DATA — so a hypothetical launcher shape outside a narrow anchor cannot escape and an inert mention cannot false-positive (anti-whack-a-mole, D-12)."
    - "GAP1/SC1 — the forged-human-stamp backstop holds in `admit()` D-04: a high-severity finding (`by ∈ {security-nfr, architect-design, release-manager}`) carrying a self-authored `verified_by: human:NAME` stamp that is NOT bound to a hook-verified disposition is REFUSED under `human_admission != off` (the hook stays the only path that grants a high-severity admit); admit() NAMES the fault and NEVER rewrites the note (no-fabrication floor, D-04/D-05)."
    - "GAP2/SC3 — the hook fails CLOSED on a non-canonical dial: any `human_admission` value not EXACTLY `off` (typo / garbage / case-variant / trailing-whitespace / empty: `hihg-severity`, `High-Severity`, `all `, `\"\"`, `bogus`, `OFF`, `1`) is treated as gate-or-stricter, never off-equivalent — a typo never opens a hole."
    - "GAP2/SC3 — the hook fails CLOSED on a corrupt/unreadable config: when the governance config is present-but-unreadable (corrupt / non-JSON), a matched admit is DENIED pending a human; a genuinely ABSENT config (zero-config lean) still ALLOWs routine admits (the lean default is preserved — only a read FAILURE fails closed, not an absence)."
    - "GAP3 (fail-safe, CR-01 inverse) — an inert heredoc/multiline body line containing admit text (`cat <<EOF\\nnode …admit …\\nEOF`) is recognized as DATA and ALLOWED, not denied — the over-block is fixed in the same parser rewrite."
    - "The committed `.js` outputs (`hooks/admission-guard.js`, and `scripts/context-io.js` if its source changed for the backstop) are byte-faithful `tsc` builds — `npm run freshness` exits 0; `hooks/guard.ts` stays byte-frozen (`git diff --quiet hooks/guard.ts` exit 0, blob `3501810e21308e4b7e219679a6ca30dace9b5d66`, D-02)."
  artifacts:
    - path: "hooks/admission-guard.ts"
      provides: "shell-segment parser replacing the ADMIT_SEGMENT regex (recognizes node|npx|tsx|npx tsx + subshell + `\\`-continuation launchers; quoted/heredoc/comment bodies inert) + fail-closed canonicalization of the human_admission dial (any non-`off` value → gate-or-stricter) + fail-closed corrupt-config handling (read FAILURE denies; absent config stays lean)"
      contains: "admit"
    - path: "hooks/admission-guard.js"
      provides: "committed tsc build the host hook runs (freshness-covered; rebuilt after the .ts edit)"
    - path: "scripts/context-io.ts"
      provides: "admit() D-04 forged-human-stamp backstop (a self-authored human:NAME stamp on a high-severity finding without a hook-verified disposition is refused under human_admission != off) + (if needed for the hook's fail-closed corrupt-vs-absent distinction) a richer governance config-read result that separates present-but-unreadable from absent"
      exports: ["admit", "readGovernanceConfig"]
    - path: "scripts/context-io.js"
      provides: "committed tsc build (freshness-covered; rebuilt only if context-io.ts changed)"
    - path: "hooks/admission-guard.test.ts"
      provides: "extended child-spawn oracle vs the COMMITTED admission-guard.js: the three evasion launchers (subshell / `\\`-continuation / npx tsx) now DENY; the heredoc body line ALLOWs (inert); a non-canonical dial DENIES a gated admit; a corrupt config DENIES while an absent config ALLOWs routine"
      contains: "deny"
    - path: "scripts/context-io.test.ts"
      provides: "unit coverage for the forged-human-stamp backstop (a self-authored human:NAME stamp on a high-severity finding is refused under human_admission != off; admit() names the fault and never rewrites the note text)"
      contains: "human:"
    - path: "scripts/floor-invariance.test.ts"
      provides: "extended SC3 sweep asserting the HOOK (not only admit()) treats every garbage/typo/case/whitespace human_admission value AND a corrupt config as gate-or-stricter; plus an anti-whack-a-mole property/table fuzz over {launcher} × {prefix} × {body-context} × {LF|CRLF} asserting the class invariant (real admit → gated; inert mention → allowed) for every variant"
      contains: "human_admission"
    - path: "25-04-RED-baseline.txt"
      provides: "captured RED baseline — the pre-fix committed admission-guard.js ALLOWs each bypass (subshell / `\\`-continuation / npx tsx / forged human:eve stamp / garbage dial / corrupt config), the exact RRED→GREEN artifact discipline from 22-08/22-09"
    - path: "25-04-GREEN-proof.txt"
      provides: "captured GREEN proof — the post-fix committed `.js` DENIES each bypass naming the note, AND the clean case + inert heredoc body + zero-config lean `off` ALLOW"
  key_links:
    - from: "hooks/admission-guard.ts (shell-segment parser)"
      to: "a real Bash `tool_input.command` admit verb"
      via: "ONE parsing authority tokenizes the command into shell segments, recognizes every launcher (node|npx|tsx|npx tsx + subshell + `\\`-continuation) as a live admit, and classifies quoted/heredoc/comment text as inert data — no second narrower heuristic"
      pattern: "admit"
    - from: "hooks/admission-guard.ts (dial canonicalization)"
      to: "readGovernanceConfig (Plan 25-01) result"
      via: "the hook treats any human_admission value not EXACTLY `off` as gate-or-stricter, and a present-but-unreadable config as fail-closed (deny), while an absent config stays lean (allow routine)"
      pattern: "off"
    - from: "scripts/context-io.ts admit() (D-04)"
      to: "the hook-verified disposition binding"
      via: "a high-severity finding's human:NAME stamp is honored only with a hook-verified disposition; absent that, a self-authored human:NAME stamp on a high-severity finding is refused (the hook stays the only grant path)"
      pattern: "human:"
    - from: "scripts/floor-invariance.test.ts (hook sweep + fuzz)"
      to: "the COMMITTED hooks/admission-guard.js + scripts/context-io.js"
      via: "child-spawn the committed .js across every garbage dial value, a corrupt config, and the launcher×prefix×body×lineending matrix; assert the class invariant for every variant"
      pattern: "admission-guard\\.js|spawn"
  prohibitions:
    - statement: "A GREEN author suite is NECESSARY-BUT-NOT-SUFFICIENT for this safety guard — the author's 31+88-case suites passed and STILL missed these bypasses TWICE. Closure REQUIRES a both-direction RED-vs-committed-`.js` proof reproduced against the COMMITTED `hooks/admission-guard.js` (never the `.ts`): for EACH evasion form (subshell / `\\`-continuation / npx tsx / forged `human:eve` stamp / garbage dial / corrupt config) a planted high-severity-without-real-approval command flips to DENY naming the note; AND the clean case + the inert heredoc body line + the zero-config lean `off` flip to ALLOW. Captured in 25-04-RED-baseline.txt (pre-fix allows) and 25-04-GREEN-proof.txt (post-fix denies). (D-12, [[grugops-safety-invariant-green-suite-insufficient]])."
      status: must-hold
      verification: "25-04-RED-baseline.txt shows the pre-fix committed `.js` ALLOWing each bypass; 25-04-GREEN-proof.txt shows the post-fix committed `.js` DENYing each bypass naming the note and ALLOWing clean/heredoc/zero-config; reproduced independently of the author suite at the blocking checkpoint."
    - statement: "Do NOT widen the regex one more notch (whack-a-mole — P22 was bypassed 7× that way). The boundary MUST BE a real shell-segment tokenizer/parser — ONE parsing authority, not a heuristic narrower than the shell grammar — so a launcher shape outside a narrow anchor cannot escape and an inert body line cannot false-positive."
      status: must-hold
      verification: "an anti-whack-a-mole property/table fuzz in floor-invariance.test.ts generates admit invocations across {launcher: node|npx|tsx|npx tsx} × {prefix: none|subshell|env-var|`\\`-continuation} × {body-context: live|quoted|heredoc|comment} × {LF|CRLF} and asserts the class invariant (a real admit → gated; an inert mention → allowed) for EVERY variant; grep confirms a single parsing authority, no second narrower matcher reintroduced."
    - statement: "The hook stays the ONLY path that grants a high-severity admit. admit()'s D-04 must NOT pretend to be un-forgeable (it is the weaker self-settable tier, D-05) — the forged-stamp backstop refuses a self-authored human:NAME stamp on a high-severity finding absent a hook-verified disposition, NEVER silently rewriting the note (it refuses and NAMES the fault, like the existing refusals)."
      status: must-hold
      verification: "the backstop unit asserts admit() RETURNS a finding naming the fault for a forged human:NAME stamp on a high-severity finding under human_admission != off, and that the note text is unchanged (no rewrite); the layering comment states admit() is the weaker tier, the hook is the un-forgeable primary (D-01/D-05)."
    - statement: "The governance dials only TIGHTEN — there is NO dial value (lean, paranoid, typo, garbage) and NO corrupt-config state that opens a floor bypass on the un-forgeable HOOK tier. A non-canonical value is gate-or-stricter; a read FAILURE is fail-closed; only a genuinely ABSENT config stays lean (SC3, D-12). `hooks/guard.ts` stays byte-frozen (D-02)."
      status: must-hold
      verification: "floor-invariance.test.ts sweeps every garbage/typo/case/whitespace human_admission value AND a corrupt config against the COMMITTED admission-guard.js and asserts gate-or-stricter (deny on a matched gated admit), while an absent config allows a routine admit; `git diff --quiet hooks/guard.ts` exits 0; freshness 0 drift."
    - statement: "The SC3/SC1 floor + an INDEPENDENT both-angle red-team (guard-LOGIC probe AND guard-INPUT-SURFACE code-review, opus-grade — the P23 split, distinct blind spots) are the proof, not a green suite. This is the named closure gate (verify-phase runs it)."
      status: must-hold
      verification: "the blocking checkpoint (Task 25-04-04) HARD-STOPS until an independent opus-grade red-team reproduces both the LOGIC angle (severity edge / dial value / forged stamp opening a hole) AND the INPUT-SURFACE angle (a launcher / doc-example / kit write token reading as a live signal or a real launcher escaping) vs the committed `.js`; findings recorded in 25-04-SUMMARY.md, not self-reported as a green suite."
    - statement: "WR-01 / no kit-content false-positive (D-13): the parser rewrite and the new test/proof artifacts must not trip `guard_context_writes` or any foundation guard — reference the hook / Workflow 16 / the config keys by name, never restate a raw `.grugops/` write path beside a write token."
      status: must-hold
      verification: "`npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial); the closed Plan-25-02 deferral markers stay inert (no false-positive)."
---

<objective>
Gap closure for Phase 25 verification (25-VERIFICATION.md, gaps_found): the GOV-01 human gate is FORGEABLE (three confirmed matcher bypasses + a forged-`human:NAME`-stamp gap in admit()) and the un-dialable floor is BYPASSABLE by a typo/garbage dial value and a corrupt config (hook fails OPEN). SC2 + GOV-02 (the audit ledger) PASSED in 25-01/02/03 and are PRESERVED — this plan does NOT touch them.

This plan hardens TWO files + their committed `.js`:

1. **GAP1/SC1 — `hooks/admission-guard.ts`:** replace the `ADMIT_SEGMENT` regex firewall (lines 83-84) with a real shell-segment parser that recognizes EVERY admit launcher — `node`, `npx`, `tsx`, `npx tsx`, a subshell `( … )` prefix, and backslash-newline line continuations — while treating quoted strings, heredoc bodies, and `#`-commented text as INERT DATA. ONE parsing authority, not a widened regex (the terminal anti-whack-a-mole lesson: P22 was bypassed 7× by narrowing a heuristic). This closes the three confirmed false-negatives AND the heredoc false-positive (GAP3, the CR-01 inverse) in one pass.
2. **GAP1/SC1 — `scripts/context-io.ts admit()`:** add the forged-human-stamp backstop. Today D-04 fires only when NO `human:NAME` stamp exists (line 930, `!HUMAN_STAMP_RE.test(vb)`), so a forged `verified_by: human:eve` passes at every dial. Bind the `human:NAME` stamp to a hook-verified disposition: a self-authored stamp on a high-severity finding without that binding is refused under `human_admission != off`. The hook stays the only grant path; admit() refuses and NAMES the fault, never rewriting (the weaker self-settable tier, D-05).
3. **GAP2/SC3 — `hooks/admission-guard.ts` (+ a richer read result if needed):** make the hook fail CLOSED. Any `human_admission` value not EXACTLY `off` → gate-or-stricter (never off-equivalent — a typo can't open a hole). A corrupt/unreadable config → deny pending a human; a genuinely absent config → still lean (allow routine). Today `readGovernanceConfig` collapses absent and corrupt into the same `off` (lines 1104-1126), so the hook's own try/catch is dead code — give the hook a way to distinguish a read FAILURE from a legitimate absence.
4. **Proof (D-12):** extend `floor-invariance.test.ts` to sweep the HOOK (not just admit()) over every garbage dial + a corrupt config, add the anti-whack-a-mole launcher×prefix×body×line-ending fuzz, extend `admission-guard.test.ts` with the three evasion launchers + heredoc + corrupt/absent config cases, capture `25-04-RED-baseline.txt` / `25-04-GREEN-proof.txt`, rebuild the committed `.js`, and HARD-STOP at the independent both-angle red-team checkpoint.

Purpose: make GOV-01's un-forgeable primary tier actually un-forgeable and the un-dialable floor actually un-dialable — proven adversarially vs the committed `.js`, not by a green author suite.
Output: the hardened matcher (shell-segment parser), the forged-stamp backstop, the fail-closed dial + corrupt-config handling, the extended suites + fuzz oracle, the RED/GREEN proof artifacts, the rebuilt fresh `.js`, and the independent red-team confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-governance-on-a-dial/25-CONTEXT.md
@.planning/phases/25-governance-on-a-dial/25-RESEARCH.md
@.planning/phases/25-governance-on-a-dial/25-PATTERNS.md
@.planning/phases/25-governance-on-a-dial/25-VERIFICATION.md
@.planning/phases/25-governance-on-a-dial/25-02-SUMMARY.md
@.planning/phases/25-governance-on-a-dial/25-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New / changed symbols and files this gap-closure introduces (for the executor and downstream consumers):

- `hooks/admission-guard.ts`:
  - A shell-segment parser (e.g. `parseAdmitInvocation(cmd)` / a `segmentCommand(cmd)` tokenizer feeding `isAdmitInvocation`) REPLACING the `ADMIT_SEGMENT` regex — recognizes `node|npx|tsx|npx tsx` launchers, a subshell `( … )` prefix, and `\`-continuations; classifies quoted / heredoc / `#`-comment text as inert data.
  - A fail-closed dial canonicalizer (e.g. `isGatedDial(dial)` / explicit `dial !== "off"` gate-or-stricter logic) so any non-`off` value gates.
  - Fail-closed corrupt-config handling in the hook (consumes the new richer read result, denies on a read FAILURE, stays lean on an absence).
- `scripts/context-io.ts`:
  - The D-04 forged-human-stamp backstop in `admit()` (a self-authored `human:NAME` stamp on a high-severity finding without a hook-verified disposition is refused under `human_admission != off`).
  - (If needed for the hook's fail-closed corrupt-vs-absent distinction) a richer governance config-read — either a new exported result shape that separates `absent` from `unreadable` (e.g. a `readGovernanceConfigResult(repoRoot)` returning `{ source: "absent" | "ok" | "unreadable", config }`), or a hook-side read that makes that distinction — WITHOUT breaking `readGovernanceConfig`'s existing default-on-absent contract (25-01/02/03 + SC2 depend on it).
- `hooks/admission-guard.test.ts`: extended child-spawn cases (the three evasion launchers DENY; heredoc body ALLOWs; non-canonical dial DENIES a gated admit; corrupt config DENIES; absent config ALLOWs routine).
- `scripts/context-io.test.ts`: forged-human-stamp backstop unit cases.
- `scripts/floor-invariance.test.ts`: the HOOK-tier garbage-dial + corrupt-config sweep and the anti-whack-a-mole launcher×prefix×body×line-ending fuzz.
- `25-04-RED-baseline.txt`, `25-04-GREEN-proof.txt`: the captured RED→GREEN proof artifacts (22-08/22-09 discipline).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 25-04-01: Shell-segment parser matcher (SC1 + GAP3) — recognize every admit launcher, treat quoted/heredoc/comment bodies as inert; capture the RED baseline first</name>
  <files>hooks/admission-guard.ts, hooks/admission-guard.js, hooks/admission-guard.test.ts, 25-04-RED-baseline.txt</files>
  <read_first>
    - hooks/admission-guard.ts:60-91 (the ADMIT_SEGMENT regex at 83-84 + isAdmitInvocation at 89-91 — the ROOT defect to REPLACE; read the input-surface discipline comments 67-82 — the verb-anchored, quote/comment-inert intent MUST be preserved, not regressed)
    - hooks/admission-guard.ts:106-128 (tokenize() + noteFileFromCommand() — the existing token helper the parser can reuse/extend to locate the note file after the verb)
    - hooks/admission-guard.ts:130-150 (the stdin fail-closed parse + the `if (!isAdmitInvocation(cmd)) process.exit(0)` allow gate — the parser plugs in here)
    - hooks/admission-guard.test.ts:1-60 (the child-spawn harness vs the COMMITTED admission-guard.js — runGuard/expectDeny/expectAllow; the case-count >= guard.test.ts watch; extend this, never reimplement)
    - hooks/guard.ts:50,88 (the verb-anchor + refuse-self-set posture to mirror — DO NOT EDIT guard.ts, D-02; read for pattern only)
    - .planning/phases/25-governance-on-a-dial/25-VERIFICATION.md (GAP1 false-negatives + GAP3 false-positive — the exact bypass forms to catch: subshell `( … )`, `\`-continuation, `npx tsx`/`npx`/`tsx`; heredoc body inert)
    - .planning/phases/25-governance-on-a-dial/25-03-SUMMARY.md (the Independent Red-Team Results — the precise reproduced commands)
  </read_first>
  <behavior>
    - CAPTURE RED FIRST: before any source edit, run each bypass command form through the CURRENT committed `hooks/admission-guard.js` and record that it ALLOWs (the hook returns no `"deny"` while the identical command admits): (1) subshell `( node …context-io.js admit T NOTEFILE )`; (2) backslash-newline continuation `node \<newline> …context-io.js admit T NOTEFILE`; (3) `npx tsx scripts/context-io.ts admit T NOTEFILE` and bare `npx`/`tsx`. Save to 25-04-RED-baseline.txt.
    - AFTER the fix, against the rebuilt committed `.js`, each of the three launchers above on a high-severity note WITHOUT the human-set approval var → DENY (reason names the note file).
    - `node …context-io.js admit …` (the existing supported form) on a high-severity un-approved note → DENY (no regression).
    - An inert heredoc body line: `cat <<EOF\nnode …context-io.js admit T NOTEFILE\nEOF` → ALLOW (the admit text is heredoc DATA, not a live command segment — GAP3 / CR-01-inverse fixed).
    - The existing input-surface inertness still holds: `echo 'node …admit …'`, `true # node …admit …`, a path component named `admit` → ALLOW (inert). (Do NOT regress 25-02's input-surface discipline.)
    - The refuse-self-set (D-01) still fires: any inline `export GRUGOPS_ADMISSION_APPROVED_BY=…` form → DENY even with the var in env.
    - Empty/malformed stdin → ALLOW (nothing to gate — the fail-closed asymmetry is unchanged).
  </behavior>
  <action>
    FIRST capture the RED baseline (the 22-08/22-09 discipline). Against the CURRENT committed `hooks/admission-guard.js`, child-spawn each of the three evasion launcher forms (subshell, `\`-continuation, `npx tsx`) with a planted high-severity note and NO approval var, and record that each ALLOWs (no `"deny"` in stdout) into `25-04-RED-baseline.txt` — this is the proof the bypass was real before the fix.

    THEN replace the `ADMIT_SEGMENT` regex (hooks/admission-guard.ts:83-84) and `isAdmitInvocation` (89-91) with a REAL shell-segment parser. Do NOT widen the regex (the whack-a-mole anti-pattern — P22 bypassed 7× by narrowing a heuristic). Build ONE parsing authority that:
    - splits the command into top-level shell segments, RESPECTING quoting (single/double quotes are literal data), `#` comments (the rest of the line is a comment), and HEREDOC bodies (`<<WORD` / `<<-WORD` / `<<'WORD'` — everything up to the closing delimiter line is DATA, not command segments) — so an admit mention inside any of those is INERT;
    - treats a backslash-newline (`\<newline>`) as a LINE CONTINUATION joining the segment (so `node \<newline> …admit` is ONE live `node` segment, not split), and a subshell `( … )` / `$( … )` prefix as opening a command context whose first command is still evaluated (so `( node …admit )` is recognized);
    - recognizes the launcher token at the start of a live command segment as ANY of `node`, `npx`, `tsx`, or `npx tsx` (the `npx tsx <script>` two-token form), followed by a `context-io` script token and the `admit` verb — so every launcher that actually runs admit is caught, and no launcher shape outside a narrow anchor can escape.
    Reuse the existing `tokenize` / `noteFileFromCommand` helpers (lines 106-128) to locate the note file once a live admit segment is recognized; on a matched gated admit where the note file cannot be located, FAIL CLOSED (deny), exactly as today. Keep the verb-anchored input-surface discipline (the 25-02 intent in comments 67-82): the gate's ONLY input is `tool_input.command`; a quoted/commented/heredoc/file-content mention is never a live admit.

    Voice: CLEAR PROFESSIONAL VOICE in all comments and deny reasons (CLAUDE.md hard rule — governance/admission/audit surfaces are never caveman voice). Comment the parser's structure so a future reader sees ONE parsing authority, not a stack of special-cases.

    Comment-text discipline: do NOT place a fenced code block inside this action's prose; describe the parser by its named functions and the shell constructs it recognizes.

    Rebuild the committed `.js`: `npm run build` then `npm run freshness` (0 drift; `admission-guard.js` and `context-io.js` are freshness-covered).

    Extend `hooks/admission-guard.test.ts` (the child-spawn oracle vs the COMMITTED `.js`): add cases for the three evasion launchers (DENY on a gated high-severity note), the heredoc body line (ALLOW), and re-assert the existing input-surface inertness (echo/comment/path ALLOW) and refuse-self-set (DENY) so nothing regressed. Keep the case count >= guard.test.ts (Pitfall-2 watch). Capture the post-fix DENY/ALLOW outcomes for these forms into `25-04-GREEN-proof.txt` (this task contributes the matcher rows; Task 25-04-03 completes the proof set).
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run hooks/admission-guard.test.ts && git diff --quiet hooks/guard.ts</automated>
  </verify>
  <acceptance_criteria>
    - `25-04-RED-baseline.txt` exists and records each of the three evasion launchers ALLOWed by the PRE-FIX committed `admission-guard.js` (the bypass was real).
    - `ADMIT_SEGMENT` regex firewall is GONE; a single shell-segment parsing authority replaces it (grep shows no second narrower matcher; one parser feeds `isAdmitInvocation`).
    - `npx vitest run hooks/admission-guard.test.ts` is GREEN: the three evasion launchers (subshell / `\`-continuation / `npx tsx`) DENY on a gated high-severity un-approved note; the heredoc body line ALLOWs (inert); echo/comment/path mentions ALLOW; refuse-self-set DENIES; empty stdin ALLOWs; case count >= guard.test.ts.
    - `npm run freshness` exits 0 (committed `admission-guard.js` fresh); `git diff --quiet hooks/guard.ts` exits 0 (byte-frozen, D-02).
    - `25-04-GREEN-proof.txt` records the post-fix DENY for the three launchers + ALLOW for the heredoc/clean cases.
  </acceptance_criteria>
  <done>The regex firewall is replaced by a real shell-segment parser: every admit launcher (node|npx|tsx|npx tsx + subshell + `\`-continuation) is recognized and DENIED when gated/un-approved, the heredoc/quoted/comment bodies are inert, the RED baseline proves the pre-fix bypass and the GREEN proof the post-fix deny — vs the committed `.js`; guard.ts byte-frozen, freshness 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 25-04-02: Fail-closed dial + corrupt-config (SC3) and the forged-human-stamp backstop (SC1) — close the remaining two holes</name>
  <files>hooks/admission-guard.ts, hooks/admission-guard.js, scripts/context-io.ts, scripts/context-io.js, scripts/context-io.test.ts, hooks/admission-guard.test.ts</files>
  <read_first>
    - hooks/admission-guard.ts:165-185 (the dial read + the dead-code try/catch at 169-180 + the `if (dial === "off") exit(0)` at 183 — the SC3 fail-open site; the hook must treat non-`off` as gate-or-stricter and a read FAILURE as deny)
    - hooks/admission-guard.ts:225-244 (the exact-string gate `isGated = dial === "all" || (dial === "high-severity" && isHighSeverity)` at 229 — any non-canonical value falls through to ALLOW; this is the GAP2 hole)
    - scripts/context-io.ts:1069-1126 (readGovernanceConfig — it collapses ABSENT and CORRUPT into the SAME `off` default and NEVER throws, so the hook cannot distinguish a read FAILURE from a legitimate absence; the corrupt-vs-absent distinction must come from a richer result or a hook-side read — WITHOUT breaking the default-on-absent contract SC2/25-01/02/03 depend on)
    - scripts/context-io.ts:889-947 (admit() — the D-04 site at 920-938; the backstop binds the human:NAME stamp to a hook-verified disposition; line 930 currently fires only when NO human stamp exists)
    - scripts/context-io.ts:111-118 (GATE_STAMP_RE / HUMAN_STAMP_RE — the stamp grammars; the backstop reasons over a human:NAME stamp specifically)
    - scripts/context-io.test.ts (the existing `d-04 high-severity in-script refusal` describe block to extend with the forged-stamp cases; the temp-config + temp-root admit() pattern)
    - .planning/phases/25-governance-on-a-dial/25-VERIFICATION.md (GAP1 forged-stamp + GAP2 fail-open — the authoritative spec; the missing: items #2, #3, #4)
  </read_first>
  <behavior>
    - SC3 non-canonical dial: with `human_admission` set to any value not exactly `off` — `hihg-severity`, `High-Severity`, `all ` (trailing space), `""`, `bogus`, `OFF`, `1` — a matched gated admit (high-severity OR, conservatively, any matched admit) without the human-set approval var → DENY. A typo never opens a hole. (Canonical `off` → ALLOW routine; canonical `high-severity` gates the three roles; canonical `all` gates every matched admit — unchanged.)
    - SC3 corrupt config: a present-but-UNREADABLE / non-JSON `factory.config.json` → a matched admit DENIES pending a human (the hook fails CLOSED on a read FAILURE). The hook's config-read path is no longer dead code.
    - SC3 absent config (lean preserved): NO config file at any standard location → a routine admit ALLOWs (zero-config stays lean — only a read FAILURE fails closed, not an absence). This MUST NOT regress: SC2 + 25-01/02/03 + the zero-config lean default depend on it.
    - SC1 forged stamp: `admit()` on a high-severity finding (`by ∈ {security-nfr, architect-design, release-manager}`) carrying a self-authored `verified_by: human:eve` stamp NOT bound to a hook-verified disposition, under `human_admission != off` → REFUSED (a finding NAMING the fault); the note text is unchanged (no rewrite).
    - SC1 backstop scope: under `human_admission: off` the forged-stamp backstop does NOT fire (lean adds no human stop); a legitimately hook-disposed admit (the hook is the grant path) is not double-refused by admit().
  </behavior>
  <action>
    PART A — fail-closed dial (SC3, GAP2) in `hooks/admission-guard.ts`. Replace the exact-string gate logic (line 229) and the `if (dial === "off")` early-allow (line 183) with a fail-closed canonicalization: define the gate decision so that the ONLY value that does NOT gate is EXACTLY `off`; every other `human_admission` value (typo / garbage / case-variant / trailing-whitespace / empty) is treated as gate-or-stricter — gate at least as strictly as `high-severity`, and (the conservative, recommended posture) gate like `all` for an unrecognized value so a typo can never under-gate. Keep canonical `high-severity` gating only the three high-severity roles and canonical `all` gating every matched admit. A typo MUST never produce an off-equivalent ALLOW.

    PART B — fail-closed corrupt config (SC3, GAP2) spanning the hook and the reader. The hook must distinguish a present-but-unreadable config (FAIL CLOSED → deny) from a genuinely absent config (lean → allow routine). Today `readGovernanceConfig` (context-io.ts:1104-1126) returns the same `off` for BOTH and never throws, so the hook's try/catch is dead. Choose and implement ONE binding mechanism, stated in the summary: EITHER (a) add a richer exported read result (e.g. `readGovernanceConfigResult(repoRoot)` returning a discriminated `{ source: "absent" | "ok" | "unreadable", config }`) that the hook consumes — leaving the existing `readGovernanceConfig` default-on-absent contract intact for 25-01/02/03/SC2; OR (b) have the hook itself perform the present-but-unreadable check (e.g. file exists at a standard location but JSON.parse throws → deny) before falling back to the shared reader for the value. In BOTH options: a read FAILURE on a matched admit → `deny()` (fail closed); an ABSENT config → the lean default (allow routine). Do NOT change the default-on-absent behavior of the existing `readGovernanceConfig` (SC2 + zero-config lean depend on it).

    PART C — forged-human-stamp backstop (SC1, GAP1) in `scripts/context-io.ts admit()` D-04 (line 930). Today the refusal fires only when NO `human:NAME` stamp exists (`!HUMAN_STAMP_RE.test(vb)`), so a forged `human:eve` passes. Bind the human stamp to a hook-verified disposition: on a high-severity finding under `human_admission != off`, a `human:NAME` stamp that is NOT accompanied by a hook-verified disposition is REFUSED (admit() is the weaker, self-settable tier — it cannot itself verify the human, so it conservatively refuses a self-authored high-severity human stamp and defers the grant to the un-forgeable hook, D-05). State the binding mechanism explicitly: the hook (reading the human-set session var) is the ONLY path that grants a high-severity admit; admit() refuses a high-severity `human:NAME`-stamped finding that did not arrive through that grant. NEVER silently rewrite the note — push a finding that NAMES the fault, exactly like the existing refuse-and-name path (context-io.ts:931-937). Honor D-01/D-05 layering: admit() must be documented honestly as the WEAKER, self-settable tier (NOT un-forgeable) — comment it as defense-in-depth covering the 4 non-CC CLIs, with the hook as the un-forgeable primary.

    Voice: CLEAR PROFESSIONAL VOICE in all comments and finding/deny messages.

    Comment-text discipline: do NOT place fenced code blocks in this action; name the functions, the discriminated-result keys, and the dial values by concept.

    Rebuild the committed `.js`: `npm run build` then `npm run freshness` (0 drift) — both `admission-guard.js` and `context-io.js`.

    Extend the suites: add to `scripts/context-io.test.ts` the forged-human-stamp backstop cases (a high-severity finding with a self-authored `human:NAME` stamp under `human_admission != off` REFUSES naming the fault, note text unchanged; under `off` does NOT fire). Add to `hooks/admission-guard.test.ts` (child-spawn vs the committed `.js`) the SC3 cases: a non-canonical dial DENIES a gated admit; a corrupt config DENIES a matched admit; an absent config ALLOWs a routine admit. Capture the forged-stamp / garbage-dial / corrupt-config DENY and the absent-config ALLOW outcomes into `25-04-GREEN-proof.txt`.
  </action>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run hooks/admission-guard.test.ts scripts/context-io.test.ts && git diff --quiet hooks/guard.ts</automated>
  </verify>
  <acceptance_criteria>
    - SC3 dial: `npx vitest run hooks/admission-guard.test.ts` shows a non-canonical `human_admission` (`hihg-severity`, `High-Severity`, `all `, `""`, `bogus`, `OFF`, `1`) DENIES a gated admit (no off-equivalent ALLOW); canonical `off` still ALLOWs routine.
    - SC3 config: a corrupt/unreadable config DENIES a matched admit; an ABSENT config ALLOWs a routine admit (lean preserved). The hook's config-read path is no longer dead code (grep shows the corrupt-vs-absent distinction is live).
    - SC1 backstop: `npx vitest run scripts/context-io.test.ts -t "human:"` (or the forged-stamp describe) is GREEN — a forged high-severity `human:NAME` stamp under `human_admission != off` REFUSES naming the fault, note text unchanged; under `off` it does not fire.
    - `readGovernanceConfig`'s existing default-on-absent contract is unchanged (the SC2 / config-governance-consistency suites stay green); the corrupt-vs-absent distinction lives in a new result shape or a hook-side check, not a behavior change to the existing reader's absent path.
    - `npm run freshness` exits 0; `git diff --quiet hooks/guard.ts` exits 0.
  </acceptance_criteria>
  <done>The hook fails CLOSED on any non-`off` dial and on a corrupt config while preserving the zero-config lean absent path, and admit() refuses a forged high-severity human:NAME stamp absent a hook-verified disposition (naming the fault, never rewriting) — the two remaining GOV-01/SC3 holes are closed; committed `.js` fresh, guard.ts byte-frozen.</done>
</task>

<task type="auto">
  <name>Task 25-04-03: Anti-whack-a-mole fuzz + HOOK-tier garbage/corrupt sweep + complete the RED→GREEN proof set (D-12)</name>
  <files>scripts/floor-invariance.test.ts, 25-04-RED-baseline.txt, 25-04-GREEN-proof.txt</files>
  <read_first>
    - scripts/floor-invariance.test.ts:1-60 (the existing SC3 sweep header + the four-invariant structure + the config-load + child-spawn-vs-committed-.js composition — EXTEND this; it currently asserts the structural guarantee at the admit() tier, not the HOOK tier)
    - hooks/admission-guard.test.ts:1-60 (the runGuard child-spawn harness shape to reuse for the HOOK-tier sweep — spawn the COMMITTED admission-guard.js)
    - scripts/config-queue-consistency.test.ts:23-30 (the config-load + repo-root resolution to mirror for the temp-config setup per dial value)
    - hooks/admission-guard.ts (the post-fix shell-segment parser + dial/corrupt-config handling from Tasks 01/02 — the fuzz asserts the CLASS invariant over this parser)
    - .planning/phases/25-governance-on-a-dial/25-03-SUMMARY.md (the existing 88-case sweep value set to extend, NOT duplicate)
    - .planning/phases/25-governance-on-a-dial/25-CONTEXT.md (D-12 — green-suite-insufficient; the anti-enumeration property-fuzz requirement)
  </read_first>
  <behavior>
    - HOOK-tier garbage sweep: for EVERY `human_admission` value in {off, high-severity, all, "", "bogus", "OFF", "High-Severity", "hihg-severity", "all ", "1", "true", a random garbage string}, child-spawn the COMMITTED `admission-guard.js` on a gated high-severity un-approved admit and assert: canonical `off` → ALLOW routine; EVERY other value → the matched gated admit is DENIED (gate-or-stricter; no off-equivalent hole). A corrupt config → DENY a matched admit; an absent config → ALLOW a routine admit.
    - Anti-whack-a-mole class fuzz: generate admit invocations across the matrix {launcher: node | npx | tsx | npx tsx} × {prefix: none | subshell `( … )` | env-var `VAR=val ` | `\`-continuation} × {body-context: live | single-quoted | double-quoted | heredoc | `#`-comment} × {LF | CRLF}. Assert the CLASS invariant for EVERY generated variant: a LIVE admit (body-context = live) on a gated high-severity un-approved note → DENY; an INERT mention (quoted / heredoc / comment) → ALLOW. (A hypothetical shape #N is caught structurally, not by enumeration.)
    - The four named floor invariants (refuse-self, no-fabrication/never-rewrite, `test_integrity` no-`off`, `hooks/guard.ts` byte-frozen) still hold at every value (preserve the existing 25-03 assertions; do not weaken them).
  </behavior>
  <action>
    Extend `scripts/floor-invariance.test.ts` (do NOT duplicate the existing 88-case admit()-tier sweep — ADD the HOOK tier and the fuzz). Reuse the child-spawn-vs-committed-`.js` harness from `hooks/admission-guard.test.ts` and the temp-config setup from `config-queue-consistency.test.ts:23-30`.

    Add a HOOK-tier garbage/corrupt sweep: child-spawn the COMMITTED `hooks/admission-guard.js` for every `human_admission` value incl. garbage/typo/case/whitespace AND a corrupt config, asserting the matched gated admit is DENIED for every non-`off` value and on a corrupt config, while canonical `off` and an absent config allow a routine admit — closing the exact gap the red-team found (the 25-03 sweep tested admit(), not the hook).

    Add the anti-whack-a-mole property/table fuzz: enumerate the {launcher} × {prefix} × {body-context} × {LF|CRLF} matrix and assert the class invariant (live admit → gated DENY; inert mention → ALLOW) for every variant vs the committed `.js`. Make the structural argument explicit in a comment: the matcher is ONE parsing authority over the shell grammar, so a launcher shape outside a narrow anchor cannot escape and an inert body cannot false-positive — the proof is the class invariant, not an enumeration of known forms.

    COMPLETE the proof artifacts: finalize `25-04-RED-baseline.txt` (pre-fix committed `.js` ALLOWs each bypass — subshell / `\`-continuation / npx tsx / forged human:eve stamp / garbage dial / corrupt config) and `25-04-GREEN-proof.txt` (post-fix committed `.js` DENIES each bypass naming the note; clean + inert heredoc + zero-config lean `off` ALLOW). These are the 22-08/22-09 RED→GREEN discipline artifacts the checkpoint and verify-phase consume.

    Voice: CLEAR PROFESSIONAL VOICE in comments (this test owns the SC3/SC1 floor). Per D-12, a GREEN fuzz is NECESSARY-NOT-SUFFICIENT — the independent red-team at Task 25-04-04 is the gate; state that in a comment.

    Comment-text discipline: no fenced code blocks in this action; describe the matrix dimensions and the class invariant by concept.
  </action>
  <verify>
    <automated>npx vitest run scripts/floor-invariance.test.ts && git diff --quiet hooks/guard.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/floor-invariance.test.ts` is GREEN with the HOOK-tier garbage/corrupt sweep added: every non-`off` `human_admission` value (incl. typo/case/whitespace/garbage) DENIES a gated admit via the committed `admission-guard.js`; a corrupt config DENIES; an absent config ALLOWs routine.
    - The anti-whack-a-mole fuzz over {launcher} × {prefix} × {body-context} × {LF|CRLF} is GREEN: every live admit variant is gated-DENY, every inert mention is ALLOW (class invariant, not enumeration).
    - The four named floor invariants still hold at every value; `git diff --quiet hooks/guard.ts` exits 0.
    - `25-04-RED-baseline.txt` and `25-04-GREEN-proof.txt` are complete (each bypass: pre-fix ALLOW, post-fix DENY; clean/heredoc/zero-config ALLOW) and reference the committed `.js`, not the `.ts`.
  </acceptance_criteria>
  <done>The HOOK tier (not just admit()) is swept over every garbage/typo/case/whitespace dial + a corrupt config and a launcher×prefix×body×line-ending fuzz proves the class invariant structurally; the RED→GREEN proof artifacts are captured vs the committed `.js`; guard.ts byte-frozen — the SC3/SC1 author proof is complete (independent red-team still the gate, D-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 25-04-04: SC1+SC3 closure gate — INDEPENDENT both-angle red-team vs the committed .js (HARD STOP; green suite is NOT proof, D-12)</name>
  <what-built>The gap-closure hardening of the GOV-01 un-forgeable tier: the shell-segment parser matcher (Task 25-04-01), the fail-closed dial + corrupt-config handling and the forged-human-stamp backstop (Task 25-04-02), and the HOOK-tier garbage sweep + anti-whack-a-mole fuzz + RED→GREEN proof artifacts (Task 25-04-03). This is the LAST task of the gap-closure plan. Per [[grugops-safety-invariant-green-suite-insufficient]] and D-12, a passing suite is necessary but NOT sufficient for this safety floor — the author's 31+88-case suites passed and STILL missed these bypasses TWICE. This checkpoint HARD-STOPS until an INDEPENDENT opus-grade adversary (not the authoring executor's own suites) has reproduced the closure. Do NOT declare closure on a green suite alone.</what-built>
  <how-to-verify>
    An INDEPENDENT opus-grade probe must run BOTH angles (the P23 split — guard LOGIC and guard INPUT SURFACE are different blind spots; both earned their keep on this exact phase) and reproduce ALL of the following against the COMMITTED artifacts (`hooks/admission-guard.js`, `scripts/context-io.js`), never the `.ts` and never the author's own `admission-guard.test.ts` / `floor-invariance.test.ts` / `context-io.test.ts`:
    1. INPUT-SURFACE angle — each evasion launcher now DENIES: a real gated high-severity un-approved admit via (a) subshell `( node …context-io.js admit T NOTE )`, (b) backslash-newline continuation `node \<newline> …admit …`, (c) `npx tsx scripts/context-io.ts admit …` (and bare `npx`/`tsx`) → DENY naming the note vs the committed `admission-guard.js`. AND the CR-01-inverse false-positive is fixed: an inert heredoc body line (`cat <<EOF\nnode …admit …\nEOF`), a quoted/echoed mention, and a `#`-commented mention → ALLOW (inert). AND a fresh launcher shape the author did NOT enumerate (the adversary invents one) is still caught structurally (the parser is one authority over the grammar, not an anchor list).
    2. LOGIC angle — the dial and the stamp close: every non-canonical `human_admission` value ({"", "bogus", "OFF", "High-Severity", "hihg-severity", "all ", "1", "true", a random string}) DENIES a gated admit (gate-or-stricter, no off-equivalent hole); a corrupt/unreadable config DENIES a matched admit while a genuinely ABSENT config ALLOWs a routine admit (lean preserved); a forged self-authored `verified_by: human:eve` stamp on a high-severity finding under `human_admission != off` is REFUSED by admit() (the forged-stamp backstop) — the hook stays the only grant path.
    3. BOTH-DIRECTION RED-vs-committed-`.js`: the clean case (routine / hook-disposed admit) ALLOWs; the zero-config lean `off` ALLOWs a routine admit; each planted bypass DENIES naming the note. Confirm against `25-04-RED-baseline.txt` (pre-fix ALLOWed the bypass) and `25-04-GREEN-proof.txt` (post-fix DENIES). The probe reproduces these independently, not from the author's suite.
    4. FLOOR + freshness: the four named floor invariants still hold at every dial value (refuse-self, no-fabrication/never-rewrite, `quality.test_integrity` has no `off`, `hooks/guard.ts` byte-frozen via `git diff --quiet hooks/guard.ts` exit 0 and blob `3501810e21308e4b7e219679a6ca30dace9b5d66`); `npm run freshness` exits 0; WR-01 did not false-positive on the Plan-25-02 closed markers.
    5. The probe's findings (the exact reproduced commands, the invented launcher shape, the deny/allow outcomes, the dial/corrupt-config results, the forged-stamp result) are DOCUMENTED in `25-04-SUMMARY.md` — reproduced by an independent adversary, NOT self-reported as a green suite.
  </how-to-verify>
  <acceptance_criteria>
    - (a) The three evasion launchers + an adversary-invented fresh launcher shape DENY vs the committed `admission-guard.js`; the heredoc/quoted/comment mentions ALLOW (inert) — reproduced by an INDEPENDENT input-surface probe, not the author's suite (D-12, P23 input-surface angle).
    - (b) Every non-canonical dial value DENIES a gated admit; a corrupt config DENIES while an absent config ALLOWs routine; a forged `human:eve` stamp on a high-severity finding is REFUSED — reproduced by an INDEPENDENT logic probe (D-12, P23 logic angle).
    - (c) The both-direction proof matches `25-04-RED-baseline.txt` (pre-fix ALLOW) → `25-04-GREEN-proof.txt` (post-fix DENY naming the note; clean/heredoc/zero-config ALLOW), reproduced vs the committed `.js`.
    - (d) The four floor invariants hold; `git diff --quiet hooks/guard.ts` exits 0; `npm run freshness` exits 0; WR-01 no false-positive; findings documented in `25-04-SUMMARY.md` — independently reproduced, not self-reported.
  </acceptance_criteria>
  <action>
    HARD STOP — do NOT auto-pass on a green author suite (the exact D-12 / green-suite-insufficient failure mode this phase already hit twice). Dispatch (or hand off to the human to dispatch) an INDEPENDENT opus-grade red-team — an adversary with different blind spots than the authoring executor — to reproduce (a)–(d) against the COMMITTED `hooks/admission-guard.js` and `scripts/context-io.js` (never the `.ts`, never the author's own suites). The probe MUST run BOTH a LOGIC probe (severity edge / dial value / forged stamp / corrupt-vs-absent config opening a hole) AND an INPUT-SURFACE attack (a launcher shape escaping the parser, or a doc-example / kit write token / quoted / heredoc mention reading as a live admit) — these are the distinct blind spots the P23 lesson and THIS phase's two red-teams proved both matter. The adversary should invent at least one launcher / mention shape the author did NOT enumerate, to test the parser as a single authority over the grammar rather than an anchor list. Record the reproduced commands, the invented shape, the clean=allow / planted=deny outcomes, the dial/corrupt-config/forged-stamp results, and confirm guard.ts byte-frozen + freshness 0 in `25-04-SUMMARY.md`. Then surface the result to the human for the resume signal. This task is the phase safety floor — it blocks until the independent both-angle proof is in hand.
  </action>
  <verify>
    <human-check>An independent opus-grade red-team has reproduced (a)–(d) vs the committed `.js`: the three evasion launchers + an invented shape DENY and the heredoc/quoted/comment mentions ALLOW (input-surface angle); every non-canonical dial + corrupt config DENY while absent config ALLOWs routine + the forged human:eve stamp is REFUSED (logic angle); the both-direction proof matches the RED→GREEN artifacts; the four floor invariants hold with `git diff --quiet hooks/guard.ts` exit 0 and freshness 0; findings recorded in `25-04-SUMMARY.md`. NOT a self-reported author green suite (D-12).</human-check>
  </verify>
  <resume-signal>Type "approved" once the independent both-angle red-team has reproduced (a)–(d) above and the findings are recorded in 25-04-SUMMARY.md, or describe the bypass / false-positive found. Do NOT approve on a green author suite alone (D-12, the terminal project lesson — this phase already hit it twice).</resume-signal>
  <done>An independent opus-grade red-team (BOTH logic and input-surface angles) has reproduced the closure vs the committed `.js`: every admit launcher (incl. an invented shape) is DENIED when gated/un-approved, the heredoc/quoted/comment mentions are inert, every non-canonical dial + corrupt config fail closed while absent config stays lean, and the forged human:NAME stamp is refused — the both-direction RED→GREEN proof holds, the four floor invariants and guard.ts byte-frozen status hold, findings are documented in `25-04-SUMMARY.md`, and the human approved; SC1+SC3 are closed by an independent adversary, not a green suite (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bash `tool_input.command` → admission-guard hook | The command string is untrusted and adversarial. The agent may launch a real `admit` via any shell construct (subshell, `\`-continuation, `npx`/`tsx`) to slip it PAST the matcher, OR embed admit text in a quoted/heredoc/comment body to trip a false-positive. The hook must parse shell segments so a live launcher is caught and an inert mention is not. |
| governance config value → admission-guard hook | `human_admission` / `audit_retention` are untrusted strings; a typo/garbage value or a corrupt config must NEVER produce an off-equivalent ALLOW. A read FAILURE fails closed; only a genuine absence stays lean. |
| note `verified_by` stamp → admit() D-04 | A self-authored `verified_by: human:NAME` stamp is forgeable by the agent. The hook (reading the human-set session env) is the only un-forgeable grant path; admit() refuses a high-severity self-authored human stamp absent that grant. |
| admit() → audit ledger | (Preserved from 25-03, unchanged here) Under `retained`, the ledger records the admission record only, never the note body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-10 | Spoofing / Elevation (matcher evasion) | a real gated high-severity `admit` launched via subshell / `\`-continuation / `npx tsx` slipping PAST the hook matcher | mitigate | Replace the `ADMIT_SEGMENT` regex firewall with ONE shell-segment parsing authority that recognizes every launcher (node/npx/tsx/npx tsx + subshell + `\`-continuation) as a live admit; proven both-direction RED vs the committed `.js` + an anti-whack-a-mole class fuzz (Task 25-04-01/03, D-12). |
| T-25-11 | Spoofing (forged human stamp) | a self-authored `verified_by: human:eve` stamp on a high-severity finding passing admit() D-04 | mitigate | The D-04 forged-stamp backstop refuses a high-severity self-authored `human:NAME` stamp absent a hook-verified disposition under `human_admission != off`; the hook stays the only grant path; admit() names the fault, never rewrites (D-04/D-05; Task 25-04-02). |
| T-25-12 | Elevation of Privilege (dial / config fail-open) | a typo/garbage `human_admission` value or a corrupt config making the un-forgeable hook ALLOW (off-equivalent) | mitigate | The hook treats any non-`off` value as gate-or-stricter and a present-but-unreadable config as fail-closed (deny), while a genuinely absent config stays lean; HOOK-tier garbage/corrupt sweep proves it vs the committed `.js` (Task 25-04-02/03; SC3, D-12). |
| T-25-13 | Denial of Service (CR-01-inverse over-block) | an inert heredoc/quoted/comment admit mention DENIED, over-blocking doc-generation | mitigate (fail-safe) | The shell-segment parser classifies heredoc/quoted/comment text as DATA → ALLOW; class fuzz over the body-context dimension confirms inert mentions are allowed (Task 25-04-01/03). |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | N/A — this gap-closure installs NO external package (RESEARCH Package Legitimacy Audit: not applicable; typescript/vitest already present). No install task exists. |
</threat_model>

<verification>
- `npm run build && npm run freshness` exits 0 (committed `admission-guard.js` + `context-io.js` are fresh, byte-faithful tsc builds; 0 drift).
- `git diff --quiet hooks/guard.ts` exits 0 (deploy guard byte-frozen, blob `3501810e21308e4b7e219679a6ca30dace9b5d66`, D-02).
- `npx vitest run hooks/admission-guard.test.ts` green: the three evasion launchers DENY; heredoc/quoted/comment mentions ALLOW; non-canonical dial DENIES a gated admit; corrupt config DENIES; absent config ALLOWs routine; refuse-self-set DENIES; case count >= guard.test.ts.
- `npx vitest run scripts/context-io.test.ts` green: the forged-human-stamp backstop refuses a high-severity self-authored `human:NAME` stamp under `human_admission != off` (names the fault, no rewrite); existing D-04 + audit-ledger + SC2 cases still green.
- `npx vitest run scripts/floor-invariance.test.ts` green: the HOOK-tier garbage/corrupt sweep + the launcher×prefix×body×line-ending class fuzz; the four named floor invariants hold at every value.
- `npx vitest run scripts/check-foundation-guards.test.ts` green (WR-01 + config-dial; D-13 no kit-content false-positive).
- `npx vitest run --exclude '**/scripts/e2e/**'` green (full non-e2e regression — bare `npm test` deliberately NOT run; it triggers the live claude-CLI e2e lane).
- `25-04-RED-baseline.txt` + `25-04-GREEN-proof.txt` capture the both-direction proof vs the committed `.js`.
- INDEPENDENT both-angle red-team (Task 25-04-04, before /gsd-verify-work): confirms SC1+SC3 closure adversarially — not self-reported green (D-12).
</verification>

<success_criteria>
- SC1 (GAP1): the GOV-01 human gate is no longer forgeable — the shell-segment parser catches every admit launcher (node/npx/tsx/npx tsx + subshell + `\`-continuation) and DENIES a gated un-approved admit; the forged-human-stamp backstop refuses a self-authored high-severity `human:NAME` stamp absent a hook-verified disposition; the hook stays the only grant path.
- SC3 (GAP2): the un-dialable floor is no longer bypassable by a dial value — the hook fails CLOSED on any non-`off` value and on a corrupt config, while a genuinely absent config stays lean (zero-config preserved); the four named floor invariants hold at every value; the dials only TIGHTEN.
- GAP3 (fail-safe): the CR-01-inverse heredoc false-positive is fixed — an inert heredoc/quoted/comment admit mention ALLOWs.
- Proof (D-12): both-direction RED→GREEN vs the COMMITTED `.js` (captured artifacts) + an anti-whack-a-mole class fuzz + an INDEPENDENT both-angle (logic + input-surface) opus-grade red-team — not a green author suite. `hooks/guard.ts` byte-frozen; freshness 0; SC2 + GOV-02 ledger preserved untouched.
</success_criteria>

<output>
Create `.planning/phases/25-governance-on-a-dial/25-04-SUMMARY.md` when done. Record: the shell-segment parser's structure (the single parsing authority, the launchers it recognizes, the inert body-contexts) and confirmation it replaced the regex firewall; the chosen corrupt-vs-absent config binding mechanism (richer read result vs hook-side check) and that `readGovernanceConfig`'s default-on-absent contract is unchanged; the dial canonicalization (non-`off` → gate-or-stricter); the forged-human-stamp backstop condition and confirmation it never rewrites; the value set swept at the HOOK tier (incl. garbage) and the fuzz matrix dimensions; the RED→GREEN proof artifact paths; confirm `git diff --quiet hooks/guard.ts` clean and freshness 0; and the INDEPENDENT both-angle red-team's reproduced commands + the invented launcher shape + outcomes. Flag for the verifier that SC1+SC3 closure REQUIRES the independent red-team — the sweeps/fuzz are necessary, not sufficient (D-12). Note that SC2 + the GOV-02 audit ledger (25-01/02/03) are PRESERVED untouched.
</output>

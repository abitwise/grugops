---
phase: 21-verify-before-write-admission-the-14-gate-as-the-un-cheatabl
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - agent-factory/workflows/16-context-read-write.md
  - agent-factory/contracts/context-note.md
  - agent-factory/roles/agents-md-scribe.md
  - agent-factory/roles/architect-design.md
  - agent-factory/roles/ba-pm.md
  - agent-factory/roles/brownfield-mapper.md
  - agent-factory/roles/compliance-officer.md
  - agent-factory/roles/factory-coach.md
  - agent-factory/roles/frontend-ui.md
  - agent-factory/roles/greenfield-mapper.md
  - agent-factory/roles/incident-responder.md
  - agent-factory/roles/installer.md
  - agent-factory/roles/orchestrator.md
  - agent-factory/roles/qe-e2e.md
  - agent-factory/roles/release-manager.md
  - agent-factory/roles/security-nfr.md
  - agent-factory/roles/software-engineer.md
  - agent-factory/roles/system-analyst.md
  - agent-factory/roles/uat-planner.md
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
autonomous: true
requirements: [VFY-03, VFY-04]
must_haves:
  truths:
    - "`agent-factory/workflows/16-context-read-write.md` exists as the single-source read-before-act / write-after-verify / admission protocol (D-13, VFY-03), clear voice, ordinal 16 (continuing the frozen 00-15 sequence)."
    - "Every one of the 17 role files carries ONE terse 'context I/O: see Workflow 16' pointer (D-14 literal-SC-3-light) — referenced, never restated (VFY-03 SC-3)."
    - "WF16 references the §14 gate's bounded `self_fix_attempts` loop in 05-pr-quality-gate.md (D-12), and states the honest escape hatch: obtain a real stamp within budget OR re-record as a `claim` with `confidence: UNKNOWN - verify` (D-11, VFY-04)."
    - "`agent-factory/contracts/context-note.md` flips its Phase-20 'verified_by empty / out of scope here' hedges to the enforced Phase-21 admission rule; the worked-example grammar (`§14-gate#ABC-001`) stays in lockstep with the validator regex (doc↔code in sync)."
    - "The DEEP per-role rewiring, the 17 handoff-template deletions, and `guard_context_protocol_single_source` are NOT in this plan — Phase 24 (D-14)."
  artifacts:
    - path: "agent-factory/workflows/16-context-read-write.md"
      provides: "The single-source context I/O protocol: read-before-act → work → write-after-verify (notes via context-io.ts; finding ONLY with a real stamp) → admission rules + escape hatch"
      contains: "context-io"
      min_lines: 25
    - path: "agent-factory/contracts/context-note.md"
      provides: "Updated provenance-fence + forward-reference text turning the Phase-20 hedge into the enforced Phase-21 admission rule"
      contains: "§14-gate#"
    - path: "scripts/check-foundation-guards.ts"
      provides: "Per-role byte-ceiling bump for any role the one-line pointer pushes over its FAIL ceiling (software-engineer.md is only 12 B under FAIL) — D-07 ceilings flip with the change"
  key_links:
    - from: "each of the 17 role files"
      to: "agent-factory/workflows/16-context-read-write.md"
      via: "one terse pointer line naming the workflow file (referenced, not restated)"
      pattern: "16-context-read-write"
    - from: "16-context-read-write.md"
      to: "05-pr-quality-gate.md bounded self_fix_attempts + context-io.ts"
      via: "named reference (D-12) — WF16 does not restate the loop or inline a raw write"
      pattern: "05-pr-quality-gate|context-io"
---

<objective>
Make VFY-03 honestly TRUE at phase close and keep the schema doc in sync with the now-enforced rules.

1. Author `agent-factory/workflows/16-context-read-write.md` (NEW, D-13) — the single-source
   protocol: read-before-act → do the work → write-after-verify (notes via `context-io.ts`; a
   `finding` ONLY with a real stamp; soft results as `claim`/`observation`) → the admission rules +
   the honest escape hatch and the referenced bounded loop (D-11/D-12).
2. Add the cheap one-line "context I/O: see Workflow 16" pointer to all 17 role files (D-14
   literal-SC-3-light) so SC-3 ("every other role references the single-source protocol rather than
   restating it") is honestly TRUE — additive, low-risk, referenced-not-restated.
3. Flip the Phase-20 hedges in `agent-factory/contracts/context-note.md` to the enforced Phase-21
   admission rule, keeping the worked-example grammar in lockstep with the validator regex (D-05).

Scope discipline (D-14): this is the SEAM, not the cutover. The DEEP per-role read/write rewiring,
the deletion of the 17 handoff templates, and the `guard_context_protocol_single_source` guard are
ALL Phase 24 — do NOT pull them in. WF16 coexists with handoffs until then.

Output: a new single-source workflow, a synced contract doc, 17 one-line role pointers, and any
required role-size ceiling bump committed in lockstep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-verify-before-write-admission-the-14-gate-as-the-un-cheatabl/21-CONTEXT.md
@.planning/phases/21-verify-before-write-admission-the-14-gate-as-the-un-cheatabl/21-PATTERNS.md
@agent-factory/workflows/05-pr-quality-gate.md
@agent-factory/workflows/04-ticket-to-pr.md
@agent-factory/contracts/context-note.md
@agent-factory/roles/orchestrator.md
@scripts/check-foundation-guards.ts
@.planning/phases/21-verify-before-write-admission-the-14-gate-as-the-un-cheatabl/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Workflow 16 (single-source context I/O protocol) + sync context-note.md</name>
  <files>agent-factory/workflows/16-context-read-write.md, agent-factory/contracts/context-note.md</files>
  <read_first>
    - agent-factory/workflows/05-pr-quality-gate.md (the frontmatter shape `kind: workflow` / `order: N` / `cadence: both` L1-6, the single-source charter sentence L9, the Step-4 bounded self_fix_attempts loop L42-44, the `UNKNOWN - verify`/"advise loudly" posture L33/L41/L47 — WF16 references these, never restates)
    - agent-factory/workflows/04-ticket-to-pr.md (the "reference, don't restate" consumer idiom at L28 — WF16's pointer to 05's bounded loop mirrors this exact shape)
    - agent-factory/contracts/context-note.md (the WHOLE file — the provenance fence L70-101 with `verified_by` "Empty in Phase 20" L79/L97-101; the `claim`-KIND not-verified forward-reference L136-139; the worked-example valid note L141-162 carrying `verified_by: §14-gate#ABC-001`)
    - scripts/context-io.ts (the now-enforced grammar regex from Plan 01 — keep the doc example in lockstep; reference context-io.ts by name)
    - .planning/phases/21-verify-before-write-admission-the-14-gate-as-the-un-cheatabl/21-01-SUMMARY.md (the enforced grammar + admission contract to describe accurately in prose)
    - .planning/phases/21-verify-before-write-admission-the-14-gate-as-the-un-cheatabl/21-CONTEXT.md (D-05/D-07/D-08/D-11/D-12/D-13; the WF16 narrative in <specifics> "The admission flow, end to end")
    - .planning/phases/21-verify-before-write-admission-the-14-gate-as-the-un-cheatabl/21-PATTERNS.md (the §"16-context-read-write.md" + §"context-note.md" sections)
  </read_first>
  <action>
    Create `agent-factory/workflows/16-context-read-write.md` (NEW — ordinal 16 continues the frozen 00-15 sequence; do NOT renumber any existing workflow). CLEAR PROFESSIONAL VOICE throughout (trace+safety surface). Frontmatter: `kind: workflow`, `order: 16`, `cadence: both`. H1: `# Workflow: context read/write`. Use the standard section spine (`## When to use`, `## Steps`, `## Stop conditions`, `## Done condition`, `## Commit`); internal ordering is your discretion (D-13). Carry a single-source charter sentence in the 05/04 shape: this workflow is the single source of the context read/write protocol; every other workflow and role references it rather than restating it.

    Narrative content (the WF16 flow, CONTEXT <specifics> "The admission flow, end to end"):
    - READ-BEFORE-ACT: read the shared context for the task (via context-io.ts `readContext`/`render`) before doing work — start from the verified state, not a blank slate.
    - DO THE WORK.
    - WRITE-AFTER-VERIFY: record notes ONLY via context-io.ts (never raw-write the `.grugops/context/` path — NAME the helper, do not inline a write). A `finding` is admitted ONLY with a real stamp: `verified_by: §14-gate#<id>` cross-checked against a live GREEN gate verdict (Posture B), or `human:<name>` for non-gate-adjudicable judgment / unsolvable disagreements only (escalation valve — its un-forgeable human-set signal is Phase 25, here it is recognized + refuse-self). Soft/neutral results are recorded as `claim`/`observation` and need no stamp (D-08).
    - ADMISSION RULES + ESCAPE HATCH: a stampless/invalid finding is hard-rejected by context-io.ts (D-11 — never silently rewritten). The agent then EITHER obtains a real stamp within the bounded budget — reference the EXISTING `self_fix_attempts` loop in `agent-factory/workflows/05-pr-quality-gate.md` (D-12, default 2, "two rounds then human"; do NOT restate or re-dial it) — OR honestly re-records the result as a `claim` with `confidence: UNKNOWN - verify` (explicitly non-load-bearing: a `claim` can NEVER satisfy a finding's admission). Mirror the gate's "advise loudly, never hide" floor — never a faked pass.
    - The three admission outcomes, stated once: gate-verifiable → run the gate → green verdict → finding admitted (workhorse, no human); not gate-verifiable + low-stakes → honest claim (escape hatch, no human); not gate-verifiable + high-stakes / disagreement → escalate to a named human → `human:<name>` (rare).

    Then UPDATE `agent-factory/contracts/context-note.md` to flip the Phase-20 hedges to the enforced Phase-21 rule (keep wording changes minimal and clear-voice):
    - The provenance-fence `verified_by` row (L79) and the "Empty in Phase 20 ... Phase 21 admits on it" deferral (L97-101): state the enforced admission rule — a `finding`'s `verified_by` must carry a real `§14-gate#<id>` or `human:<name>` stamp, and the refuse-self FAIL set (empty/`self`/`me`/`agent`/`==by`/DeLM-phrase/non-grammar) is a structural FAIL; the `§14-gate#<id>` stamp additionally cross-checks a live green verdict (Posture B). Keep the soft-kinds-need-no-stamp note (D-08).
    - The `claim`-KIND not-verified Forward-reference (L136-139): change "not implemented here / Phase 21 enforces" to the present-tense enforced statement (a `claim` can never satisfy a finding's admission — now enforced by context-io.ts).
    - Keep the worked-example note (L141-162) and its `verified_by: §14-gate#ABC-001` grammar in lockstep with the validator regex (`^§14-gate#[A-Za-z0-9._-]+$`). Treat any drift between the doc, validate(), and task-notes.template.md as a defect.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/16-context-read-write.md && grep -c "order: 16" agent-factory/workflows/16-context-read-write.md && grep -c "context-io\|05-pr-quality-gate\|UNKNOWN - verify\|§14-gate#" agent-factory/workflows/16-context-read-write.md && grep -c "§14-gate#" agent-factory/contracts/context-note.md</automated>
  </verify>
  <acceptance_criteria>
    - `agent-factory/workflows/16-context-read-write.md` exists with frontmatter `order: 16`, an H1 `# Workflow: context read/write`, and the standard section spine.
    - `grep -c "context-io" agent-factory/workflows/16-context-read-write.md` is >= 1 (notes are written via the helper, named not inlined).
    - `grep -c "05-pr-quality-gate\|self_fix_attempts" agent-factory/workflows/16-context-read-write.md` is >= 1 (the bounded loop is referenced, not restated — D-12).
    - `grep -c "UNKNOWN - verify" agent-factory/workflows/16-context-read-write.md` is >= 1 (the honest escape hatch is stated — D-11).
    - The new workflow is >= 25 non-frontmatter lines and contains the read-before-act / write-after-verify / admission narrative.
    - context-note.md no longer claims `verified_by` admission is "out of scope here / not implemented here": `grep -c "out of scope here\|not implemented here" agent-factory/contracts/context-note.md` is 0; `grep -c "§14-gate#" agent-factory/contracts/context-note.md` is >= 1; the worked-example grammar still matches `^§14-gate#[A-Za-z0-9._-]+$`.
    - guard_voice stays green for context-note.md is N/A (not in VOICE_FILES); WF16 is clear-voice by inspection (no caveman markers).
  </acceptance_criteria>
  <done>Workflow 16 exists as the single-source read/write/admission protocol referencing 05's bounded loop and stating the honest escape hatch; context-note.md's Phase-20 hedges are flipped to the enforced Phase-21 rule with the example grammar in lockstep with the validator.</done>
</task>

<task type="auto">
  <name>Task 2: Add the one-line WF16 pointer to all 17 role files + bump any over-ceiling role (D-14, D-07)</name>
  <files>agent-factory/roles/*.md (all 17 in ROLE_FILES), scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js</files>
  <read_first>
    - agent-factory/roles/orchestrator.md (the existing single-source-protocol pointer idiom — the role-switch-protocol line L45, and the workflow table L91-101 where 05-pr-quality-gate.md is NAMED not inlined; mirror this idiom for the WF16 pointer)
    - scripts/check-foundation-guards.ts (the ROLE_FILES 17-file list L210-228; guard_role_size roleCeiling() per-file ceilings L411-450 — `software-engineer.md` is "3307 3130" and currently ~3295 B, only ~12 B under FAIL; guard_voice scans these files L299-334 for caveman markers; guard_context_writes scans them L482-542)
    - .planning/phases/21-verify-before-write-admission-the-14-gate-as-the-un-cheatabl/21-PATTERNS.md (the §"agent-factory/roles/*.md" section — the WATCH note on byte ceilings)
    - .planning/phases/21-verify-before-write-admission-the-14-gate-as-the-un-cheatabl/21-CONTEXT.md (D-14 — one-line pointer only; deep rewiring is Phase 24)
  </read_first>
  <action>
    Add ONE terse clear-voice pointer line to each of the 17 role files in `ROLE_FILES` (agents-md-scribe, architect-design, ba-pm, brownfield-mapper, compliance-officer, factory-coach, frontend-ui, greenfield-mapper, incident-responder, installer, orchestrator, qe-e2e, release-manager, security-nfr, software-engineer, system-analyst, uat-planner). Pointer text in the orchestrator-L45 idiom, e.g.: "Context I/O: read and write the shared context per `agent-factory/workflows/16-context-read-write.md` — that workflow is the single source; this role references it and does not restate it." Keep it to ONE line per role. Do NOT restate the protocol, do NOT add the deep read/write rewiring (Phase 24). Do NOT raw-write any `.grugops/context/` path with a write token (guard_context_writes) — the pointer is pure prose naming the workflow.

    BYTE-CEILING GUARD (D-07 — ceilings flip WITH the change): a one-line pointer is ~70-120 B. `warn()` does NOT fail the gate, but `fail()` (a role at/over its FAIL ceiling) does. `software-engineer.md` is ~12 B under its FAIL ceiling (3307) and WILL cross it. For ANY role whose post-edit byte size reaches or exceeds its FAIL ceiling in `roleCeiling()` (scripts/check-foundation-guards.ts L411-450), bump that role's FAIL/WARN constant minimally (keep the documented +12%/+6%-style relationship) so the guard passes — this is the locked "ceilings flip with the change" rule, not a silent loosening. Roles that only cross WARN need no constant change (WARN is advisory). Then rebuild + commit the guard's `.js`: `npm run build` then `npm run freshness`.

    After editing, run `node scripts/check-foundation-guards.js` and confirm exit 0 (no FAIL — guard_voice, guard_role_size, guard_context_writes all green over the 17 roles).
  </action>
  <verify>
    <automated>for f in agents-md-scribe architect-design ba-pm brownfield-mapper compliance-officer factory-coach frontend-ui greenfield-mapper incident-responder installer orchestrator qe-e2e release-manager security-nfr software-engineer system-analyst uat-planner; do grep -q "16-context-read-write" "agent-factory/roles/$f.md" || echo "MISSING pointer in $f"; done; npm run build >/dev/null 2>&1 && npm run freshness >/dev/null 2>&1 && node scripts/check-foundation-guards.js 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - All 17 role files contain a `16-context-read-write` reference: the verify loop prints NO "MISSING pointer" line.
    - Each role's pointer is ONE line (referenced-not-restated): `grep -c "16-context-read-write" agent-factory/roles/<role>.md` is 1 for every role.
    - `node scripts/check-foundation-guards.js` exits 0 — guard_role_size shows no FAIL (any role pushed over its FAIL ceiling, e.g. software-engineer.md, has its roleCeiling() constant bumped in lockstep), guard_voice green, guard_context_writes green.
    - `npm run freshness` exits 0 (if check-foundation-guards.ts was edited, its committed .js is fresh).
    - `npx vitest run scripts/check-foundation-guards.test.ts` exits 0 (the fail-proof harness still passes after any ceiling bump).
    - No handoff template was deleted and no deep per-role rewiring was added (Phase 24 scope): `git diff --stat agent-factory/roles/` shows small additive diffs only; `git status` shows no deletions under agent-factory/handoffs/.
  </acceptance_criteria>
  <done>All 17 roles carry one terse WF16 pointer (SC-3 honestly true); any role crossing its FAIL ceiling has its locked roleCeiling() constant bumped in lockstep with a fresh committed .js; the full foundation-guards gate is green and Phase-24-scoped work is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role/workflow prose → shared context | Roles are told (by reference) to write only via context-io.ts; the pointer must not itself become a raw-write or a restated/forkable protocol. |
| schema doc → validator code | context-note.md is the authoritative schema; drift between the doc grammar and the validator regex would let a forged stamp look "documented-valid". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Tampering | doc↔code drift (context-note.md grammar vs validate() regex) | mitigate | The contract update keeps the worked-example `§14-gate#ABC-001` in lockstep with `^§14-gate#[A-Za-z0-9._-]+$`; any drift is treated as a defect (acceptance grep). |
| T-21-11 | Tampering | a role restating (and later forking/weakening) the protocol | mitigate | D-14 literal-SC-3-light: ONE terse pointer per role, referenced not restated; the `guard_context_protocol_single_source` guard that asserts no role restates the protocol is Phase 24 (flagged, not built here). |
| T-21-12 | Tampering | a raw context write smuggled into a role/workflow pointer | mitigate | guard_context_writes scans the 17 roles + 16 workflows; the pointer is pure prose naming the workflow file (no write token co-occurs a `.grugops/context/` path); `node scripts/check-foundation-guards.js` stays green. |
| T-21-13 | Repudiation | silently loosening a role-size ceiling to absorb the pointer | mitigate | D-07: ceilings flip WITH the change in lockstep (documented +12%/+6% relationship), the committed guard .js is freshness-checked, and the fail-proof harness re-runs — not a silent edit. |
| T-21-14 | Information disclosure / scope creep | pulling Phase-24 rewiring or handoff deletion into this seam | accept | D-14 scope discipline: the deep rewiring + 17 handoff deletions + the single-source guard are Phase 24; this plan adds only the one-line pointer and WF16 coexists with handoffs. git diff confirms no deletions. |
</threat_model>

<verification>
- `test -f agent-factory/workflows/16-context-read-write.md` and the grep assertions confirm WF16 exists, references context-io.ts + 05's bounded loop, and states the `UNKNOWN - verify` escape hatch.
- The 17-role pointer loop prints no MISSING line; each role has exactly one pointer.
- `node scripts/check-foundation-guards.js` exits 0 and `npm run freshness` exits 0 (any ceiling bump committed in lockstep).
- `npx vitest run scripts/check-foundation-guards.test.ts` exits 0.
- `git status` / `git diff --stat` confirm no Phase-24-scoped deletions or deep rewiring.
</verification>

<success_criteria>
VFY-03 is honestly TRUE: Workflow 16 is the single-source read-before-act / write-after-verify /
admission protocol (D-13) and all 17 roles reference it with one terse pointer rather than restating
it (D-14). VFY-04's escape hatch is documented in WF16 by reference to 05's bounded loop (D-11/D-12).
The schema doc is flipped from Phase-20 hedge to enforced Phase-21 rule and stays in lockstep with
the validator (doc↔code in sync). The foundation-guards gate stays green (any ceiling bump committed
in lockstep, D-07). Phase-24-scoped work is untouched.
</success_criteria>

<output>
Create `.planning/phases/21-verify-before-write-admission-the-14-gate-as-the-un-cheatabl/21-03-SUMMARY.md` when done.
Record which role(s) needed a roleCeiling() bump and the new constants, and confirm the
context-note.md grammar matches the Plan-01 validator regex byte-for-byte.
</output>

---
phase: 24-clean-handoff-removal-traceability-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/workflows/00-bootstrap-greenfield.md
  - agent-factory/workflows/01-bootstrap-brownfield.md
  - agent-factory/workflows/02-idea-to-epics.md
  - agent-factory/workflows/03-epic-to-tickets.md
  - agent-factory/workflows/04-ticket-to-pr.md
  - agent-factory/workflows/05-pr-quality-gate.md
  - agent-factory/workflows/06-uat-pack.md
  - agent-factory/workflows/07-backlog-refinement.md
  - agent-factory/workflows/08-sprint-planning.md
  - agent-factory/workflows/09-daily-sweep.md
  - agent-factory/workflows/10-sprint-review.md
  - agent-factory/workflows/11-retro.md
  - agent-factory/workflows/12-release.md
  - agent-factory/workflows/13-incident.md
  - agent-factory/workflows/14-ui-design-to-build.md
  - agent-factory/workflows/15-security-audit.md
autonomous: true
requirements: [MIGR-01]
user_setup: []
must_haves:
  truths:
    - "All 16 SDLC workflows (00 through 15) drop every 'Handoffs produced' / 'read the named handoff' section and instead reference Workflow 16 for note I/O — the role pulls the shared context it needs and publishes typed notes; the workflow names what work output to record, not which handoff file to write (D-05, D-06)"
    - "The 3 substrate workflows 16, 17, 18 are LEFT UNTOUCHED — they are already note-native (verified zero handoff refs) and are the reference shape, not a rewire target"
    - "guard_context_writes (WR-01) stays GREEN against the rewritten workflow prose: each workflow references WF16 by name and never restates a raw .grugops/ write path co-occurring with a write token (D-10)"
    - "05-pr-quality-gate.md keeps its single-source §14 gate loop intact — the rewire only removes its handoff references; the bounded self_fix_attempts loop and the three terminal verdicts are not touched (single-source, never fork gate logic)"
    - "14-ui-design-to-build.md drops its handoff refs including any frontend-handoff mention so the 17th template deleted in Stage 2 has no orphan in the workflow surface (D-14 / Pitfall 1)"
  artifacts:
    - path: "agent-factory/workflows/04-ticket-to-pr.md"
      provides: "The ticket→PR workflow recording role output as typed notes per WF16, no handoff packet"
      contains: "16-context-read-write"
    - path: "agent-factory/workflows/09-daily-sweep.md"
      provides: "The board reconciliation workflow reading role outputs from the shared context, no handoff inputs"
  key_links:
    - from: "agent-factory/workflows/04-ticket-to-pr.md"
      to: "agent-factory/workflows/16-context-read-write.md"
      via: "references WF16 for read-before-act / write-after-verify instead of producing/consuming handoffs"
      pattern: "16-context"
    - from: "agent-factory/workflows/05-pr-quality-gate.md"
      to: "agent-factory/workflows/16-context-read-write.md"
      via: "the §14 gate's green verdict is recorded as the §14-gate finding stamp per WF16, no handoff"
      pattern: "16-context"
  prohibitions:
    - "D-07: NO workflow retains a 'Next agent' / 'Next action' relay directive that names a successor — the Orchestrator owns sequencing by decompose→enqueue; an advisory finding/observation that does NOT name who acts is the only residue"
    - "D-08: NO workflow instructs a role to emit a single 'handoff-shaped' mega-note — workflows record SEVERAL one-kind-per-file typed notes (decision/finding/artifact-ref); a renamed packet is forbidden"
    - "D-10: NO workflow prose restates a raw .grugops/ write path co-occurring with a write token — that fires guard_context_writes RED; workflows reference WF16 and never name an inline write path"
    - "D-11: NO transitional dual-write survives in any workflow — 'Handoffs produced' sections are removed entirely, not kept alongside note publishing (clean cut)"
---

<objective>
STAGE 1 of the binding two-stage cut-over (D-12), workflow surface. Rewire all 16 SDLC workflows
(00 through 15) off static handoffs and onto the shared verified context: each workflow names what
work output a role records as typed notes (decision/finding/artifact-ref + refs) per Workflow 16,
instead of naming a handoff template to read or a handoff instance to write (D-05/D-06). The 3
substrate workflows 16/17/18 are already note-native — they are the reference shape and are LEFT
UNTOUCHED.

Purpose: this is the workflow half of the grep-to-zero (Plan 24-01 is the role + packaging half).
Together they drive the check-kit-refs.ts SCAN set to zero handoff references so Stage 2 (Plan
24-05) can flip Assertion 2 and delete the templates. The flipped grep gate is the backpressure —
it stays RED until both halves are complete.

The single-source §14 gate in 05-pr-quality-gate.md is preserved unchanged except for removing its
handoff references — gate logic is never forked (CLAUDE.md single-source).

Output: 16 rewritten SDLC workflows referencing WF16, with zero handoff references and
guard_context_writes still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-CONTEXT.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-PATTERNS.md

# The reference shape every rewritten workflow adopts (read it — it IS the pattern):
@agent-factory/workflows/16-context-read-write.md
# Already-note-native workflows demonstrating "references WF16, never restates":
@agent-factory/workflows/17-task-claim.md
@agent-factory/workflows/18-context-compaction.md
# The guard this rewrite must keep green:
@scripts/check-foundation-guards.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the 4 heaviest workflows (00 greenfield=11, 04 ticket-to-pr=8, 05 pr-quality-gate=8, 14 ui-design=7)</name>
  <files>agent-factory/workflows/00-bootstrap-greenfield.md, agent-factory/workflows/04-ticket-to-pr.md, agent-factory/workflows/05-pr-quality-gate.md, agent-factory/workflows/14-ui-design-to-build.md</files>
  <read_first>
    - agent-factory/workflows/00-bootstrap-greenfield.md (THE FILE — 11 handoff refs, heaviest)
    - agent-factory/workflows/04-ticket-to-pr.md (THE FILE — 8 handoff refs)
    - agent-factory/workflows/05-pr-quality-gate.md (THE FILE — 8 handoff refs; the single-source §14 gate — DO NOT touch the self_fix_attempts loop or the 3 terminal verdicts, only remove handoff refs)
    - agent-factory/workflows/14-ui-design-to-build.md (THE FILE — 7 handoff refs, including any frontend-handoff mention — Pitfall 1)
    - agent-factory/workflows/16-context-read-write.md (the reference shape: read-before-act / do-work / write-after-verify / admission)
    - agent-factory/workflows/17-task-claim.md (lines 9-11, 30 — the "references this file rather than restating" prose to clone)
  </read_first>
  <action>
    In each of the 4 workflows, replace every "Handoffs produced" / "read the named handoff template" / "write the filled instance to plans/handoffs/" passage with the WF16 model: the role reads the shared context it needs per Workflow 16, does the work, and records its results as typed notes per WF16 (decision/finding/artifact-ref, trace ids on refs). In 05-pr-quality-gate.md preserve the single-source §14 backpressure loop verbatim — the bounded self_fix_attempts loop and the READY_FOR_HUMAN_REVIEW / BLOCKED_NEEDS_FIX / SPLIT_REQUIRED verdicts are untouched; only the handoff references are removed, and the gate's own green verdict is recorded as the §14-gate finding stamp per WF16. 14-ui-design-to-build.md drops all 7 refs including frontend-handoff so the 17th template Stage 2 deletes has no workflow orphan (Pitfall 1). Reference WF16 by name; NEVER write an inline .grugops/ path next to a write verb (D-10). No "Next agent" directive (D-07); the Orchestrator sequences by decompose→enqueue. Workflows are clear-voice procedure (not persona prompts) — caveman does not appear here.
  </action>
  <verify>
    <automated>test $(grep -c "agent-factory/handoffs/" agent-factory/workflows/00-bootstrap-greenfield.md agent-factory/workflows/04-ticket-to-pr.md agent-factory/workflows/05-pr-quality-gate.md agent-factory/workflows/14-ui-design-to-build.md | awk -F: '{s+=$2} END {print s}') -eq 0 && grep -q "self_fix_attempts" agent-factory/workflows/05-pr-quality-gate.md && npx tsc --outDir .tmp-build && node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "agent-factory/handoffs/"` over the 4 files sums to 0
    - `grep -c "frontend-handoff" agent-factory/workflows/14-ui-design-to-build.md` returns 0 (Pitfall 1)
    - `grep -q "self_fix_attempts" agent-factory/workflows/05-pr-quality-gate.md` succeeds (the single-source §14 loop is intact, not deleted with the handoff refs)
    - `node scripts/check-foundation-guards.js` exits 0 (guard_context_writes green, D-10)
  </acceptance_criteria>
  <done>The 4 heaviest workflows reference WF16 and record typed notes; the §14 gate loop in 05 is preserved; no frontend-handoff orphan; guard green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the remaining 12 SDLC workflows (01,02,03,06,07,08,09,10,11,12,13,15)</name>
  <files>agent-factory/workflows/01-bootstrap-brownfield.md, agent-factory/workflows/02-idea-to-epics.md, agent-factory/workflows/03-epic-to-tickets.md, agent-factory/workflows/06-uat-pack.md, agent-factory/workflows/07-backlog-refinement.md, agent-factory/workflows/08-sprint-planning.md, agent-factory/workflows/09-daily-sweep.md, agent-factory/workflows/10-sprint-review.md, agent-factory/workflows/11-retro.md, agent-factory/workflows/12-release.md, agent-factory/workflows/13-incident.md, agent-factory/workflows/15-security-audit.md</files>
  <read_first>
    - agent-factory/workflows/02-idea-to-epics.md, 09-daily-sweep.md, 11-retro.md (THE FILES — 7 refs each)
    - agent-factory/workflows/01-bootstrap-brownfield.md, 03-epic-to-tickets.md, 06-uat-pack.md, 07-backlog-refinement.md, 12-release.md (THE FILES — 6 refs each)
    - agent-factory/workflows/13-incident.md (THE FILE — 5 refs), 15-security-audit.md (4), 08-sprint-planning.md (3), 10-sprint-review.md (2)
    - agent-factory/workflows/16-context-read-write.md (the reference shape) + agent-factory/workflows/18-context-compaction.md (a second note-native example)
  </read_first>
  <action>
    For each of the 12 remaining SDLC workflows, convert every handoff reference to the WF16 model exactly as Task 1: read shared context per Workflow 16, do the work, record typed notes (decision/finding/artifact-ref + refs). 12-release.md and 13-incident.md touch safety/release surfaces — keep clear professional voice and preserve the named-human deploy gate / blameless-postmortem prose unchanged (only the handoff references are removed; the safety floor is not touched). 09-daily-sweep.md (the board↔ticket reconciliation engine) reads role outputs from the shared context instead of named handoff inputs. 15-security-audit.md keeps clear voice on its PASS / PASS_WITH_RISKS / BLOCKED result. Reference WF16 by name; never restate a raw write path (D-10); no successor-naming directive (D-07); several one-kind-per-file notes, never a renamed mega-packet (D-08).
  </action>
  <verify>
    <automated>test $(grep -rc "agent-factory/handoffs/" agent-factory/workflows/0[1236789]*.md agent-factory/workflows/1[01235]*.md | awk -F: '{s+=$2} END {print s}') -eq 0 && test $(grep -c "agent-factory/handoffs" agent-factory/workflows/16-context-read-write.md agent-factory/workflows/17-task-claim.md agent-factory/workflows/18-context-compaction.md | awk -F: '{s+=$2} END {print s}') -eq 0 && npx tsc --outDir .tmp-build && node scripts/check-foundation-guards.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "agent-factory/handoffs/"` over ALL workflows 00-15 sums to 0 (combined with Task 1)
    - `grep -c "agent-factory/handoffs"` over workflows 16, 17, 18 still sums to 0 (they were note-native and are LEFT UNTOUCHED)
    - `node scripts/check-foundation-guards.js` exits 0 (guard_context_writes green across all rewritten workflow prose, D-10)
    - 12-release.md still contains the named-human deploy-gate prose (safety floor preserved — only handoff refs removed)
  </acceptance_criteria>
  <done>All 16 SDLC workflows reach zero handoff references and reference WF16; the 3 substrate workflows are untouched; guard green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow prose → guard_context_writes | rewritten workflow text is in the WR-01 guard SCAN set; a raw .grugops/ write path is the untrusted token that trips it |
| workflow text → check-kit-refs grep-to-zero | the rewired workflows are SCAN-set input the Stage-2 flipped gate asserts is handoff-free |
| §14 gate logic | single-source; an accidental fork while removing handoff refs would split the backpressure loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-RAW | Tampering | rewritten workflow prose | mitigate | D-10: reference WF16 by name, never restate a raw .grugops/ write path next to a write token; `node scripts/check-foundation-guards.js` green each task |
| T-24-02-FORK | Tampering | 05-pr-quality-gate.md §14 loop | mitigate | preserve the single-source self_fix_attempts loop + 3 verdicts verbatim; only remove handoff refs; verified by `grep -q self_fix_attempts` |
| T-24-02-SAFETY | Repudiation | 12-release.md / 13-incident.md | mitigate | keep the named-human deploy gate + blameless postmortem prose unchanged (safety floor), remove only handoff references |
| T-24-02-SC | Tampering | npm/pip/cargo installs | n/a | no package installs in this plan (markdown-only rewrite) — `[no-install]` |
</threat_model>

<verification>
- `grep -rc "agent-factory/handoffs/" agent-factory/workflows/` sums to 0 across 00-18 (00-15 rewired, 16-18 already zero)
- `npx tsc --outDir .tmp-build && node scripts/check-foundation-guards.js` exits 0 (guard_context_writes green)
- `grep -q "self_fix_attempts" agent-factory/workflows/05-pr-quality-gate.md` succeeds (single-source gate intact)
- Wave merge: `node scripts/check-kit-refs.js` still passes (the flip is Stage 2; this plan must not regress the existing ALLOW-list pass)
</verification>

<success_criteria>
All 16 SDLC workflows (00-15) reference Workflow 16 and record typed notes, with zero
agent-factory/handoffs/ references; the 3 substrate workflows (16/17/18) are untouched; the
single-source §14 gate loop in 05 and the safety prose in 12/13 are preserved;
guard_context_writes is green.
</success_criteria>

<output>
Create `.planning/phases/24-clean-handoff-removal-traceability-migration/24-02-SUMMARY.md` when done
</output>

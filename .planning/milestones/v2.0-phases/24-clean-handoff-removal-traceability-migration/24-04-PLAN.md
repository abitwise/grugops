---
phase: 24-clean-handoff-removal-traceability-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - install/install.ts
  - install/install.js
  - install/install.test.ts
  - install/README.md
autonomous: true
requirements: [MIGR-04, MIGR-02]
user_setup: []
must_haves:
  truths:
    - "install.ts --migrate renames a user's plans/handoffs/ → plans/handoffs.bak.<isoStamp()> reusing the existing never-delete-first machinery (isoStamp at ~line 192, GRUGOPS_BACKUP_SUFFIX at ~201, the backupIfDiffers rename-not-delete pattern at ~563); the handoffs-backup is FOLDED INTO the existing v1.2 --migrate orchestration (flag parsed at 88-89, branch at ~1177), never a colliding new flag (D-17, Pitfall 6)"
    - "The handoffs-backup runs on --migrate for a repo on the CURRENT two-root layout too (the isMigrated branch at ~1184), not only the isOldLayout pre-steps path — a user who accumulated plans/handoffs/ under the relay needs the backup regardless of layout state (D-17 reconcile)"
    - "The install.ts seedState plans/handoffs/ mkdir block (~lines 1004-1012) is REMOVED so new installs never recreate the dir; agent-factory/seed/plans/ already has no handoffs/ dir (verified) so this is the only seed touch-point (MIGR-02)"
    - "install.test.ts:299-308 — the assertion that plans/handoffs/ IS seeded — is INVERTED to assert it is NOT created on a fresh install (MIGR-02)"
    - "install/install.js is a committed faithful tsc build of install.ts (npm run freshness exits 0); all install.test.ts cases pass (the 4 new migrate cases + the inverted seed assertion + the existing 8 migrate cases)"
  artifacts:
    - path: "install/install.ts"
      provides: "--migrate handoffs-backup folded into the existing migrate orchestration (never-delete-first, abort on collision); seedState plans/handoffs/ mkdir removed; clear professional voice on all report/error strings"
    - path: "install/install.test.ts"
      provides: "4 new D-18/D-20 handoffs-backup cases + the inverted seed assertion"
    - path: "install/README.md"
      provides: "Documented rollback: the .bak.<ISO> dir + git revert is lossless (D-20)"
      contains: "git revert"
  key_links:
    - from: "install/install.ts"
      to: "install/install.ts"
      via: "the --migrate handoffs-backup reuses isoStamp() + the backupIfDiffers never-delete-first rename pattern + the GRUGOPS_BACKUP_SUFFIX anchored matcher"
      pattern: "isoStamp"
    - from: "install/install.test.ts"
      to: "install/install.js"
      via: "spawnSync drives the committed install.js; asserts backup / idempotent / dry-run / collision-abort + no plans/handoffs/ seed"
      pattern: "handoffs.bak"
  prohibitions:
    - "D-18: --migrate MUST NOT delete-first and MUST NOT clobber an existing backup — rename-to-backup only; if plans/handoffs.bak.<ISO> already exists, ABORT with a clear message and leave the original untouched (never overwrite)"
    - "D-19: --migrate MUST NOT parse or convert legacy handoff files into typed notes — it ONLY backs up the directory (lossy/risky conversion is forbidden; the backup preserves them for the human; the forward trace is note-native)"
    - "D-17: MUST NOT add a new colliding --migrate flag — fold the handoffs-backup into the EXISTING v1.2 migrate orchestration which already honors DRY_RUN / idempotence / the anchored .bak suffix (Pitfall 6)"
    - "D-20: --migrate MUST NOT mutate the filesystem under DRY_RUN — it prints a would-backup line and changes nothing; a second run with no plans/handoffs/ is a clean no-op (idempotent)"
---

<objective>
MIGR-04 + the install slice of MIGR-02. Extend the EXISTING install.ts --migrate orchestration to
back up a user's runtime-accumulated plans/handoffs/ directory: rename it to
plans/handoffs.bak.<ISO>, never delete-first, abort on backup-name collision (D-18), no content
conversion (D-19), dry-run/idempotent/reversible (D-20). The flag already exists from the v1.2
layout-migration track (Pitfall 6) — fold the handoffs-backup in, never collide. Reuse the
in-file never-delete-first primitives (isoStamp, GRUGOPS_BACKUP_SUFFIX, the backupIfDiffers
rename-not-delete pattern).

This plan also owns the install half of MIGR-02: remove the seedState plans/handoffs/ mkdir
(~1004-1012) so new installs never recreate the dir, and invert the install.test.ts:299-308
assertion that today asserts the dir IS seeded. Keeping ALL install.ts / install.test.ts edits in
this one plan makes install.ts a single-owner file, so the atomic-deletion plan (24-05) never
re-touches it — clean wave parallelism.

Purpose: this track is independent of the rewire/delete ordering (it touches install/ only, no
kit or SCAN-set overlap) so it runs in Wave 1. It also unblocks 24-05 by pre-removing the seed
mkdir, so the Stage-2 atomic change does not need to edit install.ts.

Output: edited install.ts (handoffs-backup + seed-mkdir removal) + committed install.js + 5
install.test.ts changes (4 new migrate cases + 1 inverted seed assertion) + a documented
README rollback.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-CONTEXT.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-PATTERNS.md
@.planning/phases/24-clean-handoff-removal-traceability-migration/24-RESEARCH.md

# The file being edited (read the migrate flag parse 70-98, isoStamp ~192, GRUGOPS_BACKUP_SUFFIX ~201,
# backupIfDiffers ~563-585, migratePreSteps ~704-752, the --migrate branch ~1177-1207, seedState handoffs ~1004-1012):
@install/install.ts
# The test file being edited (seed assert ~299-308, the 8 existing migrate cases ~568+, DRY_RUN migrate ~758):
@install/install.test.ts
@install/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold the plans/handoffs/ backup into --migrate + remove the seedState mkdir (install.ts)</name>
  <files>install/install.ts, install/install.js</files>
  <read_first>
    - install/install.ts (THE FILE — isoStamp ~192, GRUGOPS_BACKUP_SUFFIX ~201, backupIfDiffers ~563-585 the never-delete-first rename pattern, migratePreSteps ~704-752, the --migrate branch ~1177-1207 incl. the isMigrated arm ~1184 and isOldLayout pre-steps ~1199, the seedState plans/handoffs/ mkdir ~1004-1012)
    - .planning/phases/24-clean-handoff-removal-traceability-migration/24-PATTERNS.md (lines 173-207 — the exact primitives + the D-17 EXTEND-don't-collide + the thinner backupDir variant with collision-abort)
    - .planning/phases/24-clean-handoff-removal-traceability-migration/24-RESEARCH.md (Pitfall 6 + the Code Examples backup primitive + Runtime State Inventory: the seed mkdir is the only install touch-point)
  </read_first>
  <action>
    Add a handoffs-backup step to the EXISTING --migrate orchestration (do NOT add a new flag, D-17). Implement a thin backupDir-style helper (no replacement arg — D-19 no content conversion) that: if plans/handoffs/ does not exist → report "nothing to migrate" and no-op (idempotent, D-20); else compute backup = plans/handoffs.bak.<isoStamp()>; if that backup path already exists → ABORT with a clear professional-voice message naming the collision, leaving the original untouched (D-18 never-clobber); under DRY_RUN → print a would-backup line and mutate nothing (D-20); else renameSync(plans/handoffs, backup) and report backed-up (rename, never delete-first). Wire this step so it fires on --migrate for BOTH the isMigrated (already two-root) arm at ~1184 AND the isOldLayout path — a user on the current layout who accumulated plans/handoffs/ must still get the backup (D-17 reconcile). Reuse isoStamp() and the GRUGOPS_BACKUP_SUFFIX anchored shape. Separately, REMOVE the seedState plans/handoffs/ mkdir block (~1004-1012) entirely so fresh installs never recreate the dir (MIGR-02). Clear professional voice on every report/error string (installer safety surface, CLAUDE.md). Rebuild: npx tsc --outDir .tmp-build, copy the built install.js into install/install.js so the committed .js is a faithful build.
  </action>
  <verify>
    <automated>npx tsc --outDir .tmp-build && cp .tmp-build/install/install.js install/install.js && test $(grep -c 'mkdirp(handoffs)' install/install.ts) -eq 0 && grep -q 'handoffs.bak' install/install.ts && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mkdirp(handoffs)" install/install.ts` returns 0 (the seedState plans/handoffs/ mkdir is removed — MIGR-02)
    - `grep -c "handoffs.bak" install/install.ts` is at least 1 (the backup target name shape)
    - The handoffs-backup uses `isoStamp()` (reuses the existing primitive, not a new timestamp format)
    - No new `--migrate`-style flag is added: `grep -c '=== "--migrate"' install/install.ts` is still exactly 1 (the existing parse, not a duplicate)
    - `npm run freshness` exits 0 (install/install.js is a faithful tsc build of install.ts)
  </acceptance_criteria>
  <done>--migrate backs up plans/handoffs/ → .bak.<ISO> (never-delete-first, abort on collision, dry-run-safe) folded into the existing orchestration; the seedState mkdir is gone; committed install.js fresh.</done>
</task>

<task type="auto">
  <name>Task 2: Invert the seed assertion + add the 4 D-18/D-20 migrate cases + document the rollback</name>
  <files>install/install.test.ts, install/README.md</files>
  <read_first>
    - install/install.test.ts (THE FILE — the seed assertion at ~299-308 "two-root: the per-repo state plane is seeded ... incl. plans/handoffs/" to INVERT; the 8 existing migrate cases at ~568+; the DRY_RUN migrate arm at ~758; the runInstall / spawnSync helpers)
    - install/README.md (THE FILE — the "Migrating an existing install" section to extend with the .bak + git revert rollback)
    - .planning/phases/24-clean-handoff-removal-traceability-migration/24-PATTERNS.md (lines 211-216 — the inversion + the 4 case list; lines 268-273 of RESEARCH — the exact 4 D-18/D-20 cases)
  </read_first>
  <action>
    In install.test.ts: invert the ~299-308 assertion — it currently asserts statSync(join(target,"plans","handoffs")).isDirectory() is true; change it to assert plans/handoffs/ is NOT created on a fresh install (existsSync false), and update the test name/comment to match (MIGR-02). Add 4 new --migrate handoffs-backup cases alongside the existing 8 (~568+): (1) BACKUP — a target with plans/handoffs/ present, run --migrate, assert the original is gone and plans/handoffs.bak.<ISO> exists (match the GRUGOPS_BACKUP_SUFFIX shape) and a backed-up report line printed; (2) IDEMPOTENT — a second --migrate with no plans/handoffs/ → "nothing to migrate" clean no-op, exit 0, no new artifact; (3) DRY_RUN — DRY_RUN=1 --migrate prints a would-backup line and the filesystem is unchanged (plans/handoffs/ still present, no .bak created); (4) NEVER-CLOBBER — a pre-existing plans/handoffs.bak.<that exact ISO> collision → ABORT with a clear message, original untouched (D-18). In install/README.md document the reversible rollback (D-20): the .bak.<ISO> dir is preserved out-of-band, and `git revert` of the migration commit + restoring from the .bak is a lossless rollback. Rebuild not required (test/markdown only), but run the suite.
  </action>
  <verify>
    <automated>npx vitest run install/install.test.ts --exclude '**/scripts/e2e/**' && grep -q 'git revert' install/README.md && grep -q 'handoffs.bak' install/install.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run install/install.test.ts` passes — the inverted seed assertion (plans/handoffs/ NOT seeded) + the 4 new migrate cases + the existing 8 migrate cases all green
    - The 4 new cases cover backup / idempotent no-op / dry-run would-backup / never-clobber collision-abort (D-18/D-20)
    - `grep -q "git revert" install/README.md` succeeds (the documented rollback, D-20)
    - `grep -q "handoffs.bak" install/install.test.ts` succeeds (the backup-name shape is asserted)
    - The DRY_RUN case asserts the filesystem is unchanged (no .bak created, original present)
  </acceptance_criteria>
  <done>The seed assertion is inverted; the 4 D-18/D-20 backup cases pass; the README documents the .bak + git revert rollback; full install.test.ts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user plans/handoffs/ state → --migrate | the user's accumulated runtime data is the asset; a delete-first or clobber would destroy it irreversibly |
| backup path collision | a pre-existing .bak.<ISO> of the same name is the untrusted condition that must abort, never overwrite |
| --migrate flag surface | the existing flag must be extended, not duplicated (a colliding branch is a tampering risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-CLOBBER | Tampering / Denial | --migrate plans/handoffs/ rename | mitigate | D-18 never-delete-first rename-to-backup; abort on .bak.<ISO> collision with a clear message, original untouched; reuse the verified backupIfDiffers rename pattern + GRUGOPS_BACKUP_SUFFIX anchored matcher |
| T-24-04-LOSSY | Tampering | legacy handoff content | mitigate | D-19 no content conversion — only the directory is backed up; the human keeps the originals in the .bak dir; the forward trace is note-native |
| T-24-04-DRYRUN | Tampering | DRY_RUN mutation | mitigate | D-20 DRY_RUN prints would-backup and changes nothing; asserted by the dry-run test case |
| T-24-04-COLLIDE | Tampering | a new colliding --migrate flag | mitigate | D-17 fold into the existing v1.2 migrate orchestration; `grep -c '=== "--migrate"'` stays exactly 1 (Pitfall 6) |
| T-24-04-SC | Tampering | npm/pip/cargo installs | n/a | no package installs — existing dev deps only — `[no-install]` |
</threat_model>

<verification>
- `npx vitest run install/install.test.ts --exclude '**/scripts/e2e/**'` green (inverted seed + 4 new + 8 existing migrate cases)
- `grep -c "mkdirp(handoffs)" install/install.ts` returns 0 (seed mkdir removed, MIGR-02)
- `grep -c '=== "--migrate"' install/install.ts` returns exactly 1 (no colliding flag, D-17)
- `grep -q "git revert" install/README.md` (documented rollback, D-20)
- `npm run freshness` exits 0 (install/install.js is a faithful tsc build)
</verification>

<success_criteria>
install.ts --migrate backs up a user's plans/handoffs/ to a timestamped backup (never
delete-first, abort on collision, no content conversion, dry-run/idempotent/reversible) folded
into the existing migrate orchestration; the seedState plans/handoffs/ mkdir is removed and the
seed assertion inverted; the rollback is documented; the committed install.js is fresh and the
install test suite is green.
</success_criteria>

<output>
Create `.planning/phases/24-clean-handoff-removal-traceability-migration/24-04-SUMMARY.md` when done
</output>

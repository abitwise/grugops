---
phase: 20-shared-context-substrate-concurrency-foundation
plan: 04
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - scripts/check-foundation-guards.ts
  - scripts/check-foundation-guards.js
  - scripts/check-foundation-guards.test.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [SCTX-05]
must_haves:
  truths:
    - "guard_context_writes is registered in the check-foundation-guards.ts aggregator (clone of guard_wr05) and auto-wires into the §14 PR-quality gate — gate logic is NOT forked (SCTX-05)."
    - "guard_context_writes fails RED if any shipped role/workflow text writes the shared context by a path other than the sanctioned context-io.ts helpers; a planted raw-write fixture proves it fires; the real shipped tree (which only NAMES the path/helper in prose) stays GREEN (SC-5)."
    - "The aggregator's exit spine still passes on the real tree (ALL CHECKS PASSED) and now folds the new guard's fail into FAILS (single-source, no forked gate)."
    - "SC-2's Windows unlink-then-rename branch executes on a real windows-latest CI runner via an os matrix; if no CI workflow exists, it is created honestly (not faked); the deterministic unit test carries the logic proof unconditionally."
  artifacts:
    - path: "scripts/check-foundation-guards.ts"
      provides: "guard_context_writes registered alongside guard_wr05; raw-context-write grep over an explicit shipped-role/workflow SCAN set; clear voice; folds into FAILS"
      contains: "guardContextWrites"
    - path: "scripts/check-foundation-guards.test.ts"
      provides: "extended: SC-5 planted-raw-write hermetic-mirror case → aggregator nonzero naming SCTX-05; real-tree smoke stays GREEN"
      contains: "SCTX-05"
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow running the vitest suite + freshness gates on an os matrix incl. windows-latest (SC-2 real Windows-branch proof)"
      contains: "windows-latest"
  key_links:
    - from: "scripts/check-foundation-guards.ts guardContextWrites"
      to: "shipped role/workflow .md SCAN set"
      via: "grepFiles raw-context-write regex (token, not prose)"
      pattern: "grepFiles"
    - from: ".github/workflows/ci.yml"
      to: "scripts/context-io.test.ts Windows branch"
      via: "os matrix [ubuntu-latest, windows-latest] running the vitest suite"
      pattern: "windows-latest"
---

<objective>
Ship `guard_context_writes` — the seventh foundation guard — registered in `scripts/check-foundation-guards.ts` as a clone of `guard_wr05`. It greps shipped role/workflow text for raw context-write patterns (`writeFileSync`/`appendFileSync`/`Write`/`echo >` onto `.grugops/context/...`) that bypass the sanctioned `context-io.ts` helpers, and fails RED on any hit. It auto-wires into the §14 PR-quality gate via the aggregator — gate logic is NOT forked. A planted raw-write fixture proves it fires; the real tree stays green.

Also: add the real `windows-latest` CI leg so SC-2's `unlink`-then-`rename` Windows branch executes on a genuine Windows runner. RESEARCH flagged a CI workflow as `[ASSUMED]` to exist — it does NOT (verified: `.github/workflows/` is absent). Create it honestly; the deterministic unit test (plan 20-01) already proves the logic regardless.

Purpose: Enforce mechanically that nothing writes the shared context except `context-io.ts`, before any role uses the substrate (SCTX-05 / SC-5), and make the cross-platform SC-2 proof real, not asserted.

Output: extended `scripts/check-foundation-guards.ts` (+ committed `.js`), extended `scripts/check-foundation-guards.test.ts`, `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-shared-context-substrate-concurrency-foundation/20-CONTEXT.md
@.planning/phases/20-shared-context-substrate-concurrency-foundation/20-RESEARCH.md
@.planning/phases/20-shared-context-substrate-concurrency-foundation/20-VALIDATION.md
@.planning/phases/20-shared-context-substrate-concurrency-foundation/20-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register guard_context_writes in check-foundation-guards.ts (clone guard_wr05) + extend its fail-proof oracle</name>
  <files>scripts/check-foundation-guards.ts, scripts/check-foundation-guards.js, scripts/check-foundation-guards.test.ts</files>
  <read_first>
    - scripts/check-foundation-guards.ts (THE clone source — `guardWr05()` lines 99-132: `grepFiles()` helper, an EXPLICIT SCAN set never a repo-wide grep, `pass()`/`fail()` into the shared `FAILS` counter, the run-all block lines 476-482, the CHECK_ROOT override, clear voice; matches a TOKEN not a prose word)
    - scripts/check-foundation-guards.test.ts (THE clone source for the test — the `mirror()` hermetic plant via `mkdtempSync`+`cpSync`, `runIn(checkRoot)` CHECK_ROOT spawn, `appendFileSync` the planted violation, assert nonzero + finding text, smoke run over the REAL tree must stay GREEN)
    - .planning/phases/20-shared-context-substrate-concurrency-foundation/20-RESEARCH.md (Code Examples § guard_context_writes — CTX_WRITE_RE, CTX_SCAN, calibration "token vs prose word", Assumptions A3; the §14 single-source rule)
    - .planning/phases/20-shared-context-substrate-concurrency-foundation/20-01-SUMMARY.md (the sanctioned helper name context-io.ts the guard's finding text references)
    - agent-factory/roles/ + agent-factory/workflows/ (the shipped text the SCAN set draws from — pick the files that may legitimately MENTION the context path so the regex is calibrated to NOT false-positive on prose)
    - CLAUDE.md (clear voice on this safety/quality surface; no fabrication; single-source — do NOT fork gate logic)
  </read_first>
  <action>
    Add `guardContextWrites()` to `scripts/check-foundation-guards.ts`, cloning `guardWr05()`'s shape exactly: a clear-voice header `process.stdout.write`, an EXPLICIT `CTX_SCAN` file list (the shipped role + workflow `.md` files that may legitimately reference the context path — NEVER a repo-wide grep), the shared `grepFiles(CTX_SCAN, CTX_WRITE_RE)` helper, and `pass("SCTX-05: ...")` / `fail("SCTX-05 raw context write (bypasses context-io.ts): ...")` folding into the existing `FAILS` counter. Define `CTX_WRITE_RE` to match a raw context-write TOKEN — a `writeFileSync`/`appendFileSync`/`Write`/`>>`/`echo` directed at `.grugops/context/...` — and CALIBRATE it (per Assumption A3) so it FIRES on a planted raw-write line but does NOT fire on the real shipped text that merely NAMES `context-io.ts` or the path in prose (mirror guard_wr05's token-vs-prose-word care). Register the call in the run-all block alongside `guardWr05()` etc., so it folds into the existing exit tail (single-source — the aggregator already feeds the §14 gate via `05-pr-quality-gate.md`; do NOT fork the gate logic). Keep the header count honest if the script prints "six guards" — update prose to seven, or keep it generic.

    Extend `scripts/check-foundation-guards.test.ts` (the existing fail-proof harness): add the SC-5 cases. Using the existing `mirror()` + `runIn(CHECK_ROOT)` idiom, add the new guard's SCAN files to `GUARD_INPUTS` so they are mirrored, then in a new test `appendFileSync` a PLANTED raw-write line (e.g. a `writeFileSync('.grugops/context/...')` directed at the context path) into one scanned file in the mirror, run the compiled guard with `CHECK_ROOT` → assert nonzero AND the finding text contains `SCTX-05`. The existing real-tree smoke test (`smoke: real guard GREEN over the real tree`) must STILL pass (the real shipped tree has no raw context write) — confirm it stays green, proving the calibration (no-fabrication: the guard both fires on the plant and stays green on the real tree).

    Run `npm run build` and commit the updated `scripts/check-foundation-guards.js`.
  </action>
  <verify>
    <automated>npm run build && npx vitest run scripts/check-foundation-guards.test.ts && node scripts/check-foundation-guards.js && npm run freshness</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-foundation-guards.ts` contains `function guardContextWrites(` and a call to it in the run-all block alongside `guardWr05()`
    - `scripts/check-foundation-guards.ts` guard finding text references `context-io.ts` and `SCTX-05`
    - `scripts/check-foundation-guards.ts` uses an explicit `CTX_SCAN` file list (NOT a repo-wide glob) and the shared `grepFiles` helper
    - `scripts/check-foundation-guards.test.ts` contains an SC-5 planted-raw-write case asserting the aggregator exits nonzero and the output contains `SCTX-05`
    - `node scripts/check-foundation-guards.js` exits 0 over the REAL tree and prints `ALL CHECKS PASSED` (the new guard stays green on real text)
    - `npx vitest run scripts/check-foundation-guards.test.ts` exits 0 (planted-fail fires + real-tree smoke stays GREEN)
    - `npm run build && npm run freshness` exits 0 (committed `.js` not drifted)
  </acceptance_criteria>
  <done>`guard_context_writes` is registered in the aggregator (clone of guard_wr05), greps an explicit shipped-text SCAN set for raw context writes, fails RED on a planted bypass and stays GREEN on the real tree, and auto-wires into the §14 gate without forking gate logic. The compiled `.js` is committed and fresh; the fail-proof oracle proves SC-5.</done>
</task>

<task type="auto">
  <name>Task 2: Add the windows-latest CI leg for the SC-2 Windows unlink-then-rename branch (honest: create the workflow — it does not exist)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - package.json (the exact scripts the CI leg runs: `test` = `vitest run`; `freshness`, `freshness:catalog`, `freshness:context`; and the build — confirm how to run the vitest suite EXCLUDING the live claude-CLI e2e lane, per MEMORY grugops-npm-test-triggers-live-e2e: `npx vitest run --exclude '**/scripts/e2e/**'`)
    - .planning/phases/20-shared-context-substrate-concurrency-foundation/20-RESEARCH.md (Open Question 3 + Environment Availability: the windows-latest leg runs the unlink-then-rename branch; `[ASSUMED: a GitHub Actions workflow exists — not verified]`; node:path already normalizes separators)
    - scripts/context-io.test.ts (the SC-2 concurrent-write + Windows-branch test that the Windows runner must execute, from plan 20-01)
    - .planning/phases/20-shared-context-substrate-concurrency-foundation/20-VALIDATION.md (SC-2 row: real windows-latest CI leg + Manual-Only true-NFS UNKNOWN - verify)
    - CLAUDE.md (Node 22+ floor; no fabrication — if the workflow is absent, create it honestly, do not fake a green)
  </read_first>
  <action>
    HONESTY FIRST: verify whether a CI workflow exists. `.github/workflows/` is ABSENT in this repo (RESEARCH's `[ASSUMED]` is false). Therefore CREATE `.github/workflows/ci.yml` — do NOT pretend to edit a file that does not exist, and do NOT fake the Windows proof.

    Create `.github/workflows/ci.yml` — a GitHub Actions workflow that runs on push/pull_request with a job matrix `os: [ubuntu-latest, windows-latest]` and Node 22 (the documented floor; use `actions/setup-node` with `node-version: 22`). The job: checkout → setup-node 22 → `npm ci` (or `npm install` — dev deps only: typescript/vitest/@types/node) → `npm run build` → run the vitest suite EXCLUDING the live claude-CLI e2e lane (`npx vitest run --exclude '**/scripts/e2e/**'`, per MEMORY: `npm test` triggers the token-spending live e2e — do NOT run that lane in CI). On ubuntu-latest only (to avoid path-separator drift in the freshness diffs and keep Windows scoped to the atomicity proof), also run `npm run freshness`, `npm run freshness:catalog`, `npm run freshness:context`, and `node scripts/check-foundation-guards.js`. The windows-latest leg's purpose is specifically to EXECUTE the SC-2 `unlinkSync`-then-`renameSync` branch in `scripts/context-io.test.ts` on real Windows — confirm that test runs there (paths normalized via `node:path`).

    Do NOT add a true-NFS leg — that is honestly `UNKNOWN - verify` (locked); DOGF-02 (Phase 26) is the eventual real concurrency oracle. State that carve-out in a comment in the workflow so a reader is not misled into thinking Windows-green implies NFS-green.

    (No `tsc` build of a `.yml` and no committed-`.js` — this is a config file, not part of the TS tooling layer; the freshness/build gates do not touch it.)
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q 'windows-latest' .github/workflows/ci.yml && grep -q 'ubuntu-latest' .github/workflows/ci.yml && grep -q "exclude '\*\*/scripts/e2e/\*\*'" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists (the file was CREATED — it did not exist before)
    - `.github/workflows/ci.yml` contains an os matrix with both `ubuntu-latest` and `windows-latest`
    - `.github/workflows/ci.yml` uses Node 22 (`node-version: 22` or equivalent) and runs the vitest suite with `--exclude '**/scripts/e2e/**'` (does NOT run the live claude-CLI e2e lane)
    - `.github/workflows/ci.yml` runs the three freshness gates + the foundation-guard aggregator on at least the ubuntu leg
    - `.github/workflows/ci.yml` contains a comment honestly noting true-NFS atomicity is `UNKNOWN - verify` (not proven by the Windows leg; DOGF-02 is the real gate)
    - YAML is syntactically valid (e.g. `node -e "require('node:fs')"` smoke is not enough — confirm structure parses; the executor may validate with a YAML linter or by inspection)
  </acceptance_criteria>
  <done>`.github/workflows/ci.yml` is created (honestly — the workflow did not exist) with an `os: [ubuntu-latest, windows-latest]` matrix on Node 22 that runs the vitest suite (e2e lane excluded) so the SC-2 unlink-then-rename Windows branch executes on a real Windows runner; freshness + guards run on the ubuntu leg; true-NFS is honestly left `UNKNOWN - verify`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped role/workflow text → the sanctioned write path | A role prompt that instructs a raw `.grugops/context/` write bypasses context-io.ts; the guard is the boundary check. |
| guard verdict → §14 PR-quality gate | The aggregator feeds the gate; a guard that can only pass (or a forked gate) would let bypasses ship. |
| OS platform (Windows MoveFileEx) → atomic publish correctness | The unlink-then-rename branch only executes on Windows; without a real Windows runner the SC-2 claim is unproven. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10 | Tampering/Elevation | raw context write in shipped role text | mitigate | `guard_context_writes` greps an explicit SCAN set for raw-write tokens directed at `.grugops/context/`; fails RED on any hit; auto-wires into the §14 gate (single-source, not forked). |
| T-20-11 | Repudiation | the guard's "green" + the SC-2 Windows claim | mitigate | No-fabrication: the planted-raw-write oracle proves the guard FIRES (a gate that can only pass is fabricated green); the real windows-latest CI leg EXECUTES the Windows branch rather than asserting it; true-NFS is honestly `UNKNOWN - verify`, never faked green. |
| T-20-12 | Tampering | over-broad guard regex false-positive on prose | mitigate | Calibrate `CTX_WRITE_RE` to a TOKEN not the prose word (mirror guard_wr05); the real-tree smoke test must stay GREEN, forcing correct calibration. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | N/A this phase — zero new packages. The CI workflow runs `npm ci`/`npm install` of EXISTING dev deps only (typescript/vitest/@types/node, already audited); no new package. |
</threat_model>

<verification>
- `node scripts/check-foundation-guards.js` exits 0 over the real tree and prints `ALL CHECKS PASSED` (the new seventh guard is green on real text).
- `npx vitest run scripts/check-foundation-guards.test.ts` green: the planted raw-write fires the guard (nonzero + SCTX-05), the real-tree smoke stays GREEN.
- `.github/workflows/ci.yml` exists with the `os: [ubuntu-latest, windows-latest]` matrix on Node 22, runs the vitest suite (e2e excluded) so the SC-2 Windows branch executes on real Windows; freshness + guards run on ubuntu.
- True-NFS atomicity is honestly carried as `UNKNOWN - verify` (DOGF-02/Phase 26), noted in the workflow.
- `npm run build && npm run freshness` exits 0 (the guard's committed `.js` is fresh; the `.yml` is not a TS-layer artifact).
</verification>

<success_criteria>
SC-5 (ROADMAP Phase 20) is made TRUE and proven: `guard_context_writes` fails RED on a planted raw context write bypassing `context-io.ts` and stays green on the sanctioned-only tree, auto-wired into the §14 gate. SCTX-05 delivered. SC-2's Windows runtime branch is proven on a real `windows-latest` CI leg (created honestly), completing the cross-platform proof started in plan 20-01.
</success_criteria>

<output>
Create `.planning/phases/20-shared-context-substrate-concurrency-foundation/20-04-SUMMARY.md` when done.
</output>

---
phase: 03-roles-agents-md-substrate
plan: 06
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - agent-factory/roles/release-manager.md
  - agent-factory/roles/compliance-officer.md
  - agent-factory/roles/incident-responder.md
autonomous: true
requirements: [ROLE-02]

must_haves:
  truths:
    - "release-manager.md reproduces the §5.B.1 caveman prompt verbatim and carries the verbatim hard limit: requires approval, never deploys prod itself"
    - "compliance-officer.md reproduces the §5.B.2 caveman prompt verbatim and appends to security-nfr-handoff.md + fills compliance-checklist.md"
    - "incident-responder.md reproduces the §5.B.3 caveman prompt verbatim and emits incident-postmortem.md (blameless)"
    - "all three carry kind: role + tier: enterprise and state mode=enterprise OR their §5.B trigger (D-22)"
    - "all three read config first, append to traceability, and point at AGENTS.md for the 12 rules"
  artifacts:
    - path: "agent-factory/roles/release-manager.md"
      provides: "Release Manager role — cut releases, human-approved deploy (ROLE-02)"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/compliance-officer.md"
      provides: "Compliance Officer role — data classification, regime, controls (ROLE-02)"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/incident-responder.md"
      provides: "Incident Responder role — stop bleeding, blameless postmortem (ROLE-02)"
      contains: "## Caveman prompt"
  key_links:
    - from: "agent-factory/roles/release-manager.md"
      to: "plans/releases/"
      via: "writes REL-xxxx.md"
      pattern: "plans/releases"
    - from: "agent-factory/roles/compliance-officer.md"
      to: "agent-factory/checklists/compliance-checklist.md"
      via: "fills the compliance checklist per ticket"
      pattern: "compliance-checklist"
---

<objective>
Author 3 of the 5 enterprise-pack roles: `release-manager.md` (cut releases, version/changelog/deploy+rollback plan, human-approved deploy), `compliance-officer.md` (classify data, map PII, check the regime, record controls/gaps), `incident-responder.md` (stop the bleeding, blameless postmortem, lessons → tickets). Each is the §5 9-section skeleton with `tier: enterprise` and an `## Activates when` stating `mode=enterprise` OR its §5.B trigger (D-22). The Release Manager carries the human-gate hard limit verbatim (CLEAR voice).

Purpose: ROLE-02 (3 of the 5 enterprise roles).
Output: 3 files under `agent-factory/roles/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md
@.planning/phases/03-roles-agents-md-substrate/03-PATTERNS.md
@.planning/phases/03-roles-agents-md-substrate/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author release-manager.md</name>
  <files>agent-factory/roles/release-manager.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **12. Release Manager (L511–516)**; Roles Inventory row 12 (Output = `plans/releases/REL-xxxx.md` + `agent-factory/handoffs/release-handoff.md`; Board transition = owns `Ready to Release` exit → Done after human approval; Activates when = `mode=enterprise` OR a release request); Security Domain EoP row (reproduce the hard limit verbatim).
    - Spec §5.B.1 (L511–516).
    - `03-PATTERNS.md` → frontmatter/heading house style; tier-bearing analog `agent-factory/checklists/definition-of-ready.md` (frontmatter SHAPE only — use `tier: enterprise`, NOT `lean`); Board-move row `Release Manager → Ready to Release exit → Done`; read_first row `release-manager` (reads `memory-bank/70-runbook.md`). Cite `agent-factory/handoffs/release-handoff.md` (no `plans/` drift).
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `plans/releases/`, `plans/nfr-catalog.md` (attaches evidence), `memory-bank/00-index.md`, `memory-bank/70-runbook.md`, `AGENTS.md`, `agent-factory/handoffs/release-handoff.md`, `agent-factory/checklists/release-readiness-checklist.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/release-manager.md`, frontmatter `kind: role` + `tier: enterprise` (SHAPE from the checklist analog; value fixed by D-16 — `enterprise`). 9-section skeleton. `## Caveman prompt` = L511–516 VERBATIM. `## Activates when`: `mode=enterprise` OR a release request (D-22). `## Output (file + format)`: `plans/releases/REL-xxxx.md` + `agent-factory/handoffs/release-handoff.md`; attaches NFR evidence to `plans/nfr-catalog.md`; reads `memory-bank/70-runbook.md`; may cite `agent-factory/checklists/release-readiness-checklist.md`. `## Board moves`: owns the `Ready to Release` exit → Done (after human approval). `## Reads`/`## Trace updates`: D-17 universal lines. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reproduce the prompt's "You require approval. You never deploy prod yourself." in CLEAR voice — this is the human-gate; do NOT soften (D-21, SAFE-01). ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/release-manager.md && grep -q 'tier: enterprise' agent-factory/roles/release-manager.md && grep -q 'enterprise' agent-factory/roles/release-manager.md && grep -q 'plans/releases' agent-factory/roles/release-manager.md && grep -q 'agent-factory/handoffs/release-handoff.md' agent-factory/roles/release-manager.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/release-manager.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: enterprise` (NOT `lean`/`core`); three D-17 lines present.
    - `## Activates when` states `mode=enterprise` OR a release request (D-22).
    - `## Output` cites `plans/releases/REL-xxxx.md` + `agent-factory/handoffs/release-handoff.md`; drift guard empty (`grep -l 'plans/.*-handoff'` empty).
    - `## Hard limits` reproduces "require approval / never deploy prod yourself" in CLEAR voice; one-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L511–516; the human-gate is not softened or grug-voiced.
  </acceptance_criteria>
  <done>release-manager.md exists, 9 sections, verbatim prompt, tier: enterprise, D-22 trigger, real release handoff + REL file, verbatim clear-voice human-gate.</done>
</task>

<task type="auto">
  <name>Task 2: Author compliance-officer.md</name>
  <files>agent-factory/roles/compliance-officer.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **13. Compliance Officer (L523–528)**; Roles Inventory row 13 (Output = appends to `agent-factory/handoffs/security-nfr-handoff.md` + fills `agent-factory/checklists/compliance-checklist.md` per ticket; Board transition = none, gate within In Security/NFR; Activates when = `mode=enterprise` OR `compliance_regime` set OR personal/financial/health/payment data present); Security Domain (compliance text is CLEAR voice, D-21).
    - Spec §5.B.2 (L523–528); §13 (the regime detail GDPR/SOC2/ISO 27001/PCI/sector rules the role names).
    - `03-PATTERNS.md` → house style; analog `agent-factory/checklists/compliance-checklist.md`; read_first row `compliance-officer`. Cite `agent-factory/handoffs/security-nfr-handoff.md` (no `plans/` drift).
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `memory-bank/00-index.md`, `AGENTS.md`, `agent-factory/handoffs/security-nfr-handoff.md`, `agent-factory/checklists/compliance-checklist.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/compliance-officer.md`, frontmatter `kind: role` + `tier: enterprise`. 9-section skeleton. `## Caveman prompt` = L523–528 VERBATIM (grug voice — the prompt). `## Activates when`: `mode=enterprise` OR `compliance_regime` set OR personal/financial/health/payment data present (D-22). `## Output (file + format)`: appends to `agent-factory/handoffs/security-nfr-handoff.md` + fills `agent-factory/checklists/compliance-checklist.md` per ticket; references the regime per §13. `## Board moves`: "none — a gate within `In Security/NFR`". `## Reads`/`## Trace updates`: D-17 universal lines. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reflect "do not invent legal advice". CRITICAL (D-21): the caveman prompt is grug, but all compliance EXPLANATION text (Responsibilities/Output beyond the verbatim prompt) is CLEAR voice. ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/compliance-officer.md && grep -q 'tier: enterprise' agent-factory/roles/compliance-officer.md && grep -q 'enterprise' agent-factory/roles/compliance-officer.md && grep -q 'compliance-checklist' agent-factory/roles/compliance-officer.md && grep -q 'agent-factory/handoffs/security-nfr-handoff.md' agent-factory/roles/compliance-officer.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/compliance-officer.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: enterprise`; three D-17 lines present.
    - `## Activates when` states `mode=enterprise` OR `compliance_regime` set OR personal/financial/health/payment data (D-22).
    - `## Output` cites `agent-factory/handoffs/security-nfr-handoff.md` + `agent-factory/checklists/compliance-checklist.md`; drift guard empty; `## Board moves` = none.
    - One-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L523–528; compliance explanation text is CLEAR voice, not grug (D-21).
  </acceptance_criteria>
  <done>compliance-officer.md exists, 9 sections, verbatim prompt, tier: enterprise, D-22 trigger, appends to the security/NFR handoff + fills the compliance checklist, clear-voice compliance text.</done>
</task>

<task type="auto">
  <name>Task 3: Author incident-responder.md</name>
  <files>agent-factory/roles/incident-responder.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **14. Incident Responder (L535–540)**; Roles Inventory row 14 (Output = `agent-factory/handoffs/incident-postmortem.md` (blameless); Board transition = none, post-release, feeds backlog + retro; Activates when = `mode=enterprise` OR a production incident OR failing SLO).
    - Spec §5.B.3 (L535–540).
    - `03-PATTERNS.md` → house style; read_first row `incident-responder`. Cite `agent-factory/handoffs/incident-postmortem.md` (no `plans/` drift).
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `plans/tickets/` (lessons → tickets), `memory-bank/00-index.md`, `AGENTS.md`, `agent-factory/handoffs/incident-postmortem.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/incident-responder.md`, frontmatter `kind: role` + `tier: enterprise`. 9-section skeleton. `## Caveman prompt` = L535–540 VERBATIM. `## Activates when`: `mode=enterprise` OR a production incident OR failing SLO (D-22). `## Output (file + format)`: `agent-factory/handoffs/incident-postmortem.md` (blameless) + lessons turned into tickets in `plans/tickets/`. `## Board moves`: "none — post-release; feeds backlog + retro". `## Reads`/`## Trace updates`: D-17 universal lines. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reflect "blameless postmortem / stop the bleeding first" framing. ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/incident-responder.md && grep -q 'tier: enterprise' agent-factory/roles/incident-responder.md && grep -q 'enterprise' agent-factory/roles/incident-responder.md && grep -q 'agent-factory/handoffs/incident-postmortem.md' agent-factory/roles/incident-responder.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/incident-responder.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: enterprise`; three D-17 lines present.
    - `## Activates when` states `mode=enterprise` OR a production incident OR failing SLO (D-22).
    - `## Output` cites `agent-factory/handoffs/incident-postmortem.md` + `plans/tickets/`; drift guard empty; `## Board moves` = none.
    - One-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L535–540.
  </acceptance_criteria>
  <done>incident-responder.md exists, 9 sections, verbatim prompt, tier: enterprise, D-22 trigger, emits the blameless postmortem + feeds tickets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored enterprise-role text → future executing agent | The Release Manager human-gate and the compliance text are guardrails the agent acts on; softening or grug-muddying them degrades safety on the highest-stakes topics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-EoP | Elevation of Privilege | Release Manager hard limit | mitigate | Reproduce "require approval / never deploy prod yourself" VERBATIM, CLEAR voice — do not soften (RESEARCH Security Domain EoP row; spec L515–516; SAFE-01). |
| T-03-Info | Info-integrity | compliance explanation voice | mitigate | Compliance explanation text is CLEAR voice (D-21); grug confined to the `## Caveman prompt`. |
| T-03-Tamper | Tampering | handoff/checklist path citations | mitigate | Cite real `agent-factory/handoffs/<name>.md` + `agent-factory/checklists/<name>.md`, never `plans/`-prefixed (D-15); drift guard enforces. |
</threat_model>

<verification>
- Each of the 3 files passes its 9-section + `tier: enterprise` frontmatter + three D-17 universal-line greps.
- Each `## Activates when` contains `enterprise` and its trigger; drift guard empty across all three.
- `check-structure.sh` per-file checks pass for release-manager.md, compliance-officer.md, incident-responder.md.
</verification>

<success_criteria>
- ROLE-02 advanced by 3 enterprise roles, each `tier: enterprise` with a D-22 `Activates when`, the Release Manager carrying the verbatim clear-voice human-gate.
- No scope-fence violation: no `plans/`-prefixed handoffs, no fabricated commands, no restated rules, no grug voice on compliance/safety explanation text.
</success_criteria>

<output>
Create `.planning/phases/03-roles-agents-md-substrate/03-06-SUMMARY.md` when done.
</output>

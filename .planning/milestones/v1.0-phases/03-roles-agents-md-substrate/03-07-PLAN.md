---
phase: 03-roles-agents-md-substrate
plan: 07
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - agent-factory/roles/factory-coach.md
  - agent-factory/roles/installer.md
autonomous: true
requirements: [ROLE-02]

must_haves:
  truths:
    - "factory-coach.md reproduces the §5.B.4 caveman prompt verbatim, reads metrics.md, emits retro-notes.md + factory-tagged tickets"
    - "installer.md reproduces the §5.B.5 caveman prompt verbatim and carries the verbatim additive/never-overwrite/dry-run/uninstall discipline"
    - "both carry kind: role + tier: enterprise and state mode=enterprise OR their §5.B trigger (D-22)"
    - "both read config first, append to traceability, and point at AGENTS.md for the 12 rules"
  artifacts:
    - path: "agent-factory/roles/factory-coach.md"
      provides: "Factory Coach role — metrics → retro → improvement tickets (ROLE-02)"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/installer.md"
      provides: "Installer role — detect host tool, lay adapter, additive (ROLE-02)"
      contains: "## Caveman prompt"
  key_links:
    - from: "agent-factory/roles/factory-coach.md"
      to: "plans/metrics.md"
      via: "reads the metrics the coach acts on"
      pattern: "plans/metrics\\.md"
    - from: "agent-factory/roles/factory-coach.md"
      to: "agent-factory/handoffs/retro-notes.md"
      via: "Output handoff (real on-disk path)"
      pattern: "agent-factory/handoffs/retro-notes\\.md"
---

<objective>
Author the final 2 enterprise-pack roles: `factory-coach.md` (read the metrics, run the retro, find waste, write improvement tickets for the factory itself) and `installer.md` (detect the host coding agent, lay down the right adapter/entry file, additive — never overwrite, dry-run + uninstall). Each is the §5 9-section skeleton with `tier: enterprise` and an `## Activates when` stating `mode=enterprise` OR its §5.B trigger (D-22). This plan completes ROLE-02 (the 16th and last role lands here).

Note on dispatch-neutrality (D-20): the Installer's role TEXT names what it does (detect tool, lay adapter, be additive, support dry-run/uninstall) but does NOT contain the per-tool adapter mechanics, wrapper contents, or the safety hook — those are Phase-5 packaging. Keep the role text dispatch-neutral.

Purpose: ROLE-02 (the last 2 of the 5 enterprise roles).
Output: 2 files under `agent-factory/roles/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md
@.planning/phases/03-roles-agents-md-substrate/03-PATTERNS.md
@.planning/phases/03-roles-agents-md-substrate/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author factory-coach.md</name>
  <files>agent-factory/roles/factory-coach.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **15. Factory Coach (L547–552)**; Roles Inventory row 15 (Output = `agent-factory/handoffs/retro-notes.md` + improvement tickets in `plans/tickets/` tagged `factory`; reads `plans/metrics.md`; Board transition = none; Activates when = `mode=enterprise` OR end of sprint OR on-demand).
    - Spec §5.B.4 (L547–552).
    - `03-PATTERNS.md` → house style; read_first row `factory-coach`. Cite `agent-factory/handoffs/retro-notes.md` (no `plans/` drift).
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `plans/metrics.md`, `plans/tickets/`, `memory-bank/00-index.md`, `AGENTS.md`, `agent-factory/handoffs/retro-notes.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/factory-coach.md`, frontmatter `kind: role` + `tier: enterprise`. 9-section skeleton. `## Caveman prompt` = L547–552 VERBATIM. `## Activates when`: `mode=enterprise` OR end of sprint OR on-demand (D-22). `## Reads`: config first, then `plans/metrics.md` (the metrics the coach acts on), `memory-bank/00-index.md`. `## Output (file + format)`: `agent-factory/handoffs/retro-notes.md` + `factory`-tagged improvement tickets in `plans/tickets/`. `## Board moves`: "none — reads metrics; writes factory tickets". `## Trace updates`: D-17 universal line. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reflect "read the metrics, not the vibes" framing. ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/factory-coach.md && grep -q 'tier: enterprise' agent-factory/roles/factory-coach.md && grep -q 'enterprise' agent-factory/roles/factory-coach.md && grep -q 'plans/metrics.md' agent-factory/roles/factory-coach.md && grep -q 'agent-factory/handoffs/retro-notes.md' agent-factory/roles/factory-coach.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/factory-coach.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: enterprise`; three D-17 lines present.
    - `## Activates when` states `mode=enterprise` OR end of sprint OR on-demand (D-22).
    - `## Reads` cites `plans/metrics.md`; `## Output` cites `agent-factory/handoffs/retro-notes.md` + `plans/tickets/`; drift guard empty; `## Board moves` = none.
    - One-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L547–552.
  </acceptance_criteria>
  <done>factory-coach.md exists, 9 sections, verbatim prompt, tier: enterprise, D-22 trigger, reads metrics, emits the real retro handoff + factory tickets.</done>
</task>

<task type="auto">
  <name>Task 2: Author installer.md</name>
  <files>agent-factory/roles/installer.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **16. Installer (L559–564)**; Roles Inventory row 16 (Output = tool-specific adapter/entry files per §16 + install report; Board transition = none; Activates when = `mode=enterprise` OR an install/adapter request). Also "Anti-Patterns to Avoid → Adding dispatch detail to role text" (D-20 — stay dispatch-neutral).
    - Spec §5.B.5 (L559–564).
    - `03-PATTERNS.md` → house style; read_first row `installer` (tool adapter/entry files + install report; no board flow).
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `memory-bank/00-index.md`, `AGENTS.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/installer.md`, frontmatter `kind: role` + `tier: enterprise`. 9-section skeleton. `## Caveman prompt` = L559–564 VERBATIM. `## Activates when`: `mode=enterprise` OR an install/adapter request (D-22). `## Output (file + format)`: the tool-specific adapter/entry files (per §16) + an install report — name these generically; do NOT inline the per-tool wrapper contents, the commands/-vs-skills choice, the plugin manifest, or the safety hook (all Phase-5, D-20 dispatch-neutrality). `## Board moves`: "none — tooling, not board flow". `## Reads`/`## Trace updates`: D-17 universal lines. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reproduce the prompt's "additive / never overwrite user content / support dry-run and uninstall" (CLEAR voice on the never-overwrite safety line). ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/installer.md && grep -q 'tier: enterprise' agent-factory/roles/installer.md && grep -q 'enterprise' agent-factory/roles/installer.md && grep -q 'factory.config.json' agent-factory/roles/installer.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/installer.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: enterprise`; three D-17 lines present.
    - `## Activates when` states `mode=enterprise` OR an install/adapter request (D-22).
    - `## Board moves` = none; drift guard empty; one-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L559–564; NO Phase-5 dispatch mechanics inlined (no per-tool wrapper contents, no plugin manifest, no safety-hook code) — role text stays dispatch-neutral (D-20).
  </acceptance_criteria>
  <done>installer.md exists, 9 sections, verbatim prompt, tier: enterprise, D-22 trigger, names its adapter/report outputs, dispatch-neutral, additive/never-overwrite hard limit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored enterprise-role text → future executing agent | The Installer "never overwrite user content" discipline is a safety guardrail the agent acts on; softening it risks destructive installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-Tamper | Tampering | Installer additive/never-overwrite discipline | mitigate | Reproduce "additive / never overwrite user content / dry-run + uninstall" from the prompt in CLEAR voice; do not soften (D-21). |
| T-03-EoP | Elevation of Privilege | Installer dispatch-neutrality | mitigate | Keep the role text dispatch-neutral (D-20) — no Phase-5 mechanical hook/wrapper detail leaks into role text, preventing premature/incorrect privileged actions. |
| T-03-Info | Info-integrity | 12-rules single-source | mitigate | One-line pointer to `AGENTS.md`; no rule text restated (D-19). |
</threat_model>

<verification>
- Each of the 2 files passes its 9-section + `tier: enterprise` frontmatter + three D-17 universal-line greps.
- Each `## Activates when` contains `enterprise` and its trigger; drift guard empty across both.
- After this plan, all 5 enterprise roles exist: `grep -l 'tier: enterprise' agent-factory/roles/*.md | wc -l` == 5 (with Plan 06 merged).
- `check-structure.sh` per-file checks pass for factory-coach.md and installer.md.
</verification>

<success_criteria>
- ROLE-02 fully satisfied (5/5 enterprise roles exist, `tier: enterprise`, each with a D-22 `Activates when`).
- All 16 role files now exist (11 core + 5 enterprise).
- No scope-fence violation: no Phase-5 dispatch mechanics in the Installer, no `plans/`-prefixed handoffs, no fabricated commands, no restated rules.
</success_criteria>

<output>
Create `.planning/phases/03-roles-agents-md-substrate/03-07-SUMMARY.md` when done.
</output>

---
phase: 03-roles-agents-md-substrate
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - agent-factory/roles/qe-e2e.md
  - agent-factory/roles/security-nfr.md
  - agent-factory/roles/uat-planner.md
autonomous: true
requirements: [ROLE-01]

must_haves:
  truths:
    - "qe-e2e.md reproduces the §5.A.9 caveman prompt verbatim and emits qe-handoff.md"
    - "security-nfr.md reproduces the §5.A.10 caveman prompt verbatim, emits security-nfr-handoff.md, and lists the §5.A.10 activation triggers"
    - "uat-planner.md reproduces the §5.A.11 caveman prompt verbatim and emits uat-handoff.md"
    - "qe states the In Review exit; security states the In Security/NFR exit; uat states Ready for UAT → In UAT and owns the In UAT exit"
    - "all three carry kind: role + tier: core, read config first, append to traceability, and point at AGENTS.md for the 12 rules"
  artifacts:
    - path: "agent-factory/roles/qe-e2e.md"
      provides: "QE/E2E role — break the feature, tests (ROLE-01)"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/security-nfr.md"
      provides: "Security/NFR role — risk/security gate (ROLE-01)"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/uat-planner.md"
      provides: "UAT Planner role — business acceptance (ROLE-01)"
      contains: "## Caveman prompt"
  key_links:
    - from: "agent-factory/roles/security-nfr.md"
      to: "agent-factory/checklists/security-nfr-checklist.md"
      via: "cites the security/NFR checklist"
      pattern: "security-nfr-checklist"
    - from: "agent-factory/roles/uat-planner.md"
      to: "agent-factory/handoffs/uat-handoff.md"
      via: "Output handoff (real on-disk path)"
      pattern: "agent-factory/handoffs/uat-handoff\\.md"
---

<objective>
Author the quality/acceptance back-end core roles: `qe-e2e.md` (break the feature, tests), `security-nfr.md` (risk/security/compliance gate), and `uat-planner.md` (business acceptance). Each is the §5 9-section skeleton: verbatim caveman prompt + terse frozen-path connective tissue (D-15). Handoffs cite the REAL on-disk `agent-factory/handoffs/` paths. This plan completes ROLE-01 (the 11th core role lands here).

Purpose: ROLE-01 (the last 3 of the 11 core roles).
Output: 3 files under `agent-factory/roles/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md
@.planning/phases/03-roles-agents-md-substrate/03-PATTERNS.md
@.planning/phases/03-roles-agents-md-substrate/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author qe-e2e.md</name>
  <files>agent-factory/roles/qe-e2e.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **9. QE/E2E (L473–478)**; Roles Inventory row 9 (Output = `agent-factory/handoffs/qe-handoff.md`; Board transition = `In Review` exit → toward Security/NFR or UAT; Activates when = "Need tests").
    - Spec §5.A.9 (L473–478).
    - `03-PATTERNS.md` → house style; Board-move row `QE/E2E → In Review exit`; read_first row `qe-e2e`. Cite `agent-factory/handoffs/qe-handoff.md` (no `plans/` drift).
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `memory-bank/00-index.md`, `AGENTS.md`, `agent-factory/handoffs/qe-handoff.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/qe-e2e.md`, frontmatter `kind: role` + `tier: core`, 9-section skeleton. `## Caveman prompt` = L473–478 VERBATIM. `## Activates when`: "Need tests". `## Output (file + format)`: `agent-factory/handoffs/qe-handoff.md`. `## Board moves`: the `In Review` exit (toward Security/NFR or UAT). `## Reads`/`## Trace updates`: D-17 universal lines. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reflect "avoid flaky tests / report gaps / stable selectors" from the prompt. ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/qe-e2e.md && grep -q 'tier: core' agent-factory/roles/qe-e2e.md && grep -q 'agent-factory/handoffs/qe-handoff.md' agent-factory/roles/qe-e2e.md && grep -q 'factory.config.json' agent-factory/roles/qe-e2e.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/qe-e2e.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: core`; three D-17 lines present.
    - `## Output` cites `agent-factory/handoffs/qe-handoff.md`; drift guard empty.
    - `## Board moves` states the `In Review` exit; one-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L473–478.
  </acceptance_criteria>
  <done>qe-e2e.md exists, 9 sections, verbatim prompt, emits the real QE handoff, owns the In Review exit.</done>
</task>

<task type="auto">
  <name>Task 2: Author security-nfr.md</name>
  <files>agent-factory/roles/security-nfr.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **10. Security/NFR (L485–489)**; Roles Inventory row 10 (Output = `agent-factory/handoffs/security-nfr-handoff.md`; Board transition = `In Security/NFR` exit → Ready for UAT; Activates when triggers spec L490: auth, 2FA, biometrics, payments, banking, investment data, personal data, GDPR, public API, file upload, admin action, DB migration, queue/event, external integration, perf-sensitive flow). Also the Security Domain section (these triggers are TEXT only, not executed).
    - Spec §5.A.10 (L485–489 prompt; L490 triggers); §13 (security/privacy/compliance detail the role references by name).
    - `03-PATTERNS.md` → house style; Board-move row `Security/NFR → In Security/NFR exit`; read_first row `security-nfr` (cites `agent-factory/checklists/security-nfr-checklist.md`). Cite `agent-factory/handoffs/security-nfr-handoff.md` (no `plans/` drift).
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `plans/nfr-catalog.md`, `memory-bank/00-index.md`, `AGENTS.md`, `agent-factory/handoffs/security-nfr-handoff.md`, `agent-factory/checklists/security-nfr-checklist.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/security-nfr.md`, frontmatter `kind: role` + `tier: core`, 9-section skeleton. `## Caveman prompt` = L485–489 VERBATIM (grug voice — the prompt itself). `## Activates when`: the spec L490 trigger list (auth/2FA/biometrics/payments/banking/investment data/personal data/GDPR/public API/file upload/admin action/DB migration/queue-event/external integration/perf-sensitive flow). `## Output (file + format)`: `agent-factory/handoffs/security-nfr-handoff.md`; checks against `plans/nfr-catalog.md`; cites `agent-factory/checklists/security-nfr-checklist.md` and references §13 by name. `## Board moves`: the `In Security/NFR` exit (→ Ready for UAT). `## Reads`/`## Trace updates`: D-17 universal lines. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reflect "do not gold-plate" from the prompt. CRITICAL voice rule (D-21): the caveman prompt stays grug voice, but any security/compliance EXPLANATION text in Responsibilities/Output (beyond the verbatim prompt) is CLEAR voice. ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/security-nfr.md && grep -q 'tier: core' agent-factory/roles/security-nfr.md && grep -q 'agent-factory/handoffs/security-nfr-handoff.md' agent-factory/roles/security-nfr.md && grep -q 'security-nfr-checklist' agent-factory/roles/security-nfr.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/security-nfr.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: core`; three D-17 lines present.
    - `## Output` cites `agent-factory/handoffs/security-nfr-handoff.md` + `agent-factory/checklists/security-nfr-checklist.md` + `plans/nfr-catalog.md`; drift guard empty.
    - `## Activates when` carries the §5.A.10 (L490) trigger list; `## Board moves` states the `In Security/NFR` exit; one-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L485–489; security/compliance explanation text (beyond the verbatim prompt) is CLEAR voice, not grug (D-21).
  </acceptance_criteria>
  <done>security-nfr.md exists, 9 sections, verbatim prompt, full trigger list, emits the real security/NFR handoff + cites the checklist, owns the In Security/NFR exit.</done>
</task>

<task type="auto">
  <name>Task 3: Author uat-planner.md</name>
  <files>agent-factory/roles/uat-planner.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **11. UAT Planner (L496–499)**; Roles Inventory row 11 (Output = `agent-factory/handoffs/uat-handoff.md`; Board transition = `Ready for UAT → In UAT` and owns the `In UAT` exit; Activates when = "Need business acceptance").
    - Spec §5.A.11 (L496–499).
    - `03-PATTERNS.md` → house style; Board-move row `UAT Planner → Ready for UAT → In UAT, In UAT exit`; read_first row `uat-planner` (cites `agent-factory/checklists/uat-checklist.md`). Cite `agent-factory/handoffs/uat-handoff.md` (no `plans/` drift).
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `memory-bank/00-index.md`, `AGENTS.md`, `agent-factory/handoffs/uat-handoff.md`, `agent-factory/checklists/uat-checklist.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/uat-planner.md`, frontmatter `kind: role` + `tier: core`, 9-section skeleton. `## Caveman prompt` = L496–499 VERBATIM. `## Activates when`: "Need business acceptance". `## Output (file + format)`: `agent-factory/handoffs/uat-handoff.md`; cites `agent-factory/checklists/uat-checklist.md`. `## Board moves`: `Ready for UAT → In UAT` and owns the `In UAT` exit. `## Reads`/`## Trace updates`: D-17 universal lines. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reflect "do not code" from the prompt. ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/uat-planner.md && grep -q 'tier: core' agent-factory/roles/uat-planner.md && grep -q 'agent-factory/handoffs/uat-handoff.md' agent-factory/roles/uat-planner.md && grep -q 'In UAT' agent-factory/roles/uat-planner.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/uat-planner.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: core`; three D-17 lines present.
    - `## Output` cites `agent-factory/handoffs/uat-handoff.md` + `agent-factory/checklists/uat-checklist.md`; drift guard empty.
    - `## Board moves` states `Ready for UAT → In UAT` and the `In UAT` exit; one-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L496–499.
  </acceptance_criteria>
  <done>uat-planner.md exists, 9 sections, verbatim prompt, emits the real UAT handoff + cites the checklist, owns the In UAT transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored role text → future executing agent | Security/NFR + UAT text encodes the risk gate the agent acts on; muddied or fabricated guidance propagates to runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-Info | Info-integrity | security/compliance explanation voice | mitigate | The Security/NFR caveman prompt stays grug (verbatim), but any security/compliance explanation beyond it is CLEAR voice (D-21, RESEARCH Security Domain Info-integrity row). |
| T-03-Tamper | Tampering | handoff/checklist path citations | mitigate | Cite real `agent-factory/handoffs/<name>.md` + `agent-factory/checklists/<name>.md`, never `plans/`-prefixed (D-15); drift guard enforces. |
| T-03-Info | Info-integrity | 12-rules single-source | mitigate | One-line pointer to `AGENTS.md`; no rule text restated (D-19). |
</threat_model>

<verification>
- Each of the 3 files passes its 9-section + `tier: core` + three D-17 universal-line greps.
- Drift guard empty across all three.
- After this plan, all 11 core roles exist: `grep -l 'tier: core' agent-factory/roles/*.md | wc -l` == 11 (with Plans 01–04 also merged).
- `check-structure.sh` per-file checks pass for qe-e2e.md, security-nfr.md, uat-planner.md.
</verification>

<success_criteria>
- ROLE-01 fully satisfied (11/11 core roles exist with the 9-section skeleton, verbatim prompts, frozen-path citations, and the three D-17 universal lines).
- No scope-fence violation: no `plans/`-prefixed handoffs, no fabricated commands, no restated rules, no grug voice on security/compliance explanation text.
</success_criteria>

<output>
Create `.planning/phases/03-roles-agents-md-substrate/03-05-SUMMARY.md` when done.
</output>

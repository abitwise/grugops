---
phase: 03-roles-agents-md-substrate
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - agent-factory/roles/architect-design.md
  - agent-factory/roles/software-engineer.md
autonomous: true
requirements: [ROLE-01]

must_haves:
  truths:
    - "architect-design.md reproduces the §5.A.7 caveman prompt verbatim, emits architecture-handoff.md + ADRs, and updates nfr-catalog.md"
    - "software-engineer.md reproduces the §5.A.8 caveman prompt verbatim and emits implementation-handoff.md"
    - "architect states the In Design exit; engineer states In Development → In Review"
    - "both carry kind: role + tier: core, read config first, append to traceability, and point at AGENTS.md for the 12 rules"
  artifacts:
    - path: "agent-factory/roles/architect-design.md"
      provides: "Architect/Design role — structure, ADRs, NFR seeding (ROLE-01)"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/software-engineer.md"
      provides: "Software Engineer role — one ticket → diff+tests (ROLE-01)"
      contains: "## Caveman prompt"
  key_links:
    - from: "agent-factory/roles/architect-design.md"
      to: "memory-bank/50-decisions/ADR-template.md"
      via: "ADR output target (ADR-000X-<slug>.md)"
      pattern: "50-decisions"
    - from: "agent-factory/roles/software-engineer.md"
      to: "agent-factory/handoffs/implementation-handoff.md"
      via: "Output handoff (real on-disk path)"
      pattern: "agent-factory/handoffs/implementation-handoff\\.md"
---

<objective>
Author the design+build core roles: `architect-design.md` (structure/boundaries, ADRs, NFR catalog seeding) and `software-engineer.md` (one ticket → small diff + tests). Each is the §5 9-section skeleton: verbatim caveman prompt + terse frozen-path connective tissue (D-15). Handoffs cite the REAL on-disk `agent-factory/handoffs/` paths.

Purpose: ROLE-01 (2 of the 11 core roles).
Output: 2 files under `agent-factory/roles/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md
@.planning/phases/03-roles-agents-md-substrate/03-PATTERNS.md
@.planning/phases/03-roles-agents-md-substrate/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author architect-design.md</name>
  <files>agent-factory/roles/architect-design.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **7. Architect/Design (L450–453)**; Roles Inventory row 7 (Output = `agent-factory/handoffs/architecture-handoff.md` + ADRs in `memory-bank/50-decisions/ADR-000X-*.md`; updates `plans/nfr-catalog.md`; Board transition = `In Design` exit → ready for dev; Activates when = "Need structure or tradeoffs").
    - Spec §5.A.7 (L450–453).
    - `03-PATTERNS.md` → house style; analog `memory-bank/50-decisions/ADR-template.md` (the ADR Output target — sections Status/Context/Decision/Alternatives/Consequences/Rollback); Board-move row `Architect/Design → In Design exit`; read_first row `architect-design`. Cite `agent-factory/handoffs/architecture-handoff.md` (no `plans/` drift).
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `plans/nfr-catalog.md`, `memory-bank/00-index.md`, `memory-bank/50-decisions/ADR-template.md`, `AGENTS.md`, `agent-factory/handoffs/architecture-handoff.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/architect-design.md`, frontmatter `kind: role` + `tier: core`, 9-section skeleton. `## Caveman prompt` = L450–453 VERBATIM. `## Activates when`: "Need structure or tradeoffs". `## Output (file + format)`: `agent-factory/handoffs/architecture-handoff.md` + ADRs authored from `memory-bank/50-decisions/ADR-template.md` into `memory-bank/50-decisions/ADR-000X-<slug>.md`; seeds/updates `plans/nfr-catalog.md`. `## Board moves`: the `In Design` exit (→ ready for dev). `## Reads`/`## Trace updates`: D-17 universal lines. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reflect the prompt's "keep design just enough / prefer boring tech / protect future change". ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/architect-design.md && grep -q 'tier: core' agent-factory/roles/architect-design.md && grep -q 'agent-factory/handoffs/architecture-handoff.md' agent-factory/roles/architect-design.md && grep -q '50-decisions' agent-factory/roles/architect-design.md && grep -q 'nfr-catalog.md' agent-factory/roles/architect-design.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/architect-design.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: core`; three D-17 lines present.
    - `## Output` cites `agent-factory/handoffs/architecture-handoff.md`, `memory-bank/50-decisions/` ADRs, and `plans/nfr-catalog.md`; drift guard empty.
    - `## Board moves` states the `In Design` exit; one-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L450–453.
  </acceptance_criteria>
  <done>architect-design.md exists, 9 sections, verbatim prompt, emits real architecture handoff + ADRs + NFR catalog updates, owns the In Design exit.</done>
</task>

<task type="auto">
  <name>Task 2: Author software-engineer.md</name>
  <files>agent-factory/roles/software-engineer.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **8. Software Engineer (L461–466)**; Roles Inventory row 8 (Output = `agent-factory/handoffs/implementation-handoff.md`; Board transition = `In Development → In Review`; Activates when = "Need code (one ticket)"); Pattern 1 example (the Engineer file worked example: reads `implementation-ready-packet.md`/the ticket, hard limits = "no big rewrites…no fake test results", spec L468).
    - Spec §5.A.8 (L461–466 prompt; L468 hard limits).
    - `03-PATTERNS.md` → the **exact-match analog** section: `agent-factory/handoffs/implementation-handoff.md` (dual-section body; cite the universal-header `## Scope`/`## Risks` as authoritative per the Phase-2 duplicate-header decision); read_first row `software-engineer`.
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `memory-bank/00-index.md`, `AGENTS.md`, `agent-factory/handoffs/implementation-handoff.md`, `agent-factory/handoffs/implementation-ready-packet.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/software-engineer.md`, frontmatter `kind: role` + `tier: core`, 9-section skeleton. `## Caveman prompt` = L461–466 VERBATIM. `## Activates when`: "Need code (one ticket)". `## Reads`: config first, then `agent-factory/handoffs/implementation-ready-packet.md` / the ticket (read the handoff first per the prompt), `memory-bank/00-index.md`. `## Output (file + format)`: `agent-factory/handoffs/implementation-handoff.md` (cite the universal-header `## Scope`/`## Risks` as authoritative). `## Board moves`: `In Development → In Review`. `## Trace updates`: append PR/files/tests links to `plans/traceability.md` (D-17). One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reflect spec L468 — small diff, no big rewrites, stop if scope grows or architecture must change, no fake test results (the no-fake-results line in CLEAR voice per D-21). ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Output' -e '## Board moves' -e '## Hard limits' agent-factory/roles/software-engineer.md && grep -q 'tier: core' agent-factory/roles/software-engineer.md && grep -q 'agent-factory/handoffs/implementation-handoff.md' agent-factory/roles/software-engineer.md && grep -q 'In Development' agent-factory/roles/software-engineer.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/software-engineer.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: core`; three D-17 lines present.
    - `## Output` cites `agent-factory/handoffs/implementation-handoff.md`; `## Reads` cites `implementation-ready-packet.md`/the ticket; drift guard empty.
    - `## Board moves` states `In Development → In Review`; one-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L461–466; the "no fake test results" hard limit is CLEAR voice.
  </acceptance_criteria>
  <done>software-engineer.md exists, 9 sections, verbatim prompt, reads the ready-packet, emits the real implementation handoff, owns In Development → In Review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored role text → future executing agent | The Engineer hard limits encode the no-fake-results + stop-on-scope-growth guardrails the agent acts on; weakening them propagates to runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-Tamper | Tampering | Engineer "no fake test results" hard limit | mitigate | Reproduce spec L468 hard limit verbatim/CLEAR voice — no fake results, small diff, stop on scope growth (RESEARCH Security Domain; D-21). |
| T-03-Tamper | Tampering | handoff path citations | mitigate | Cite real `agent-factory/handoffs/<name>.md`, never `plans/`-prefixed (D-15); drift guard enforces. |
| T-03-Info | Info-integrity | 12-rules single-source | mitigate | One-line pointer to `AGENTS.md`; no rule text restated (D-19). |
</threat_model>

<verification>
- Each file passes its 9-section + `tier: core` + three D-17 universal-line greps.
- Drift guard empty across both files.
- `check-structure.sh` per-file checks pass for architect-design.md and software-engineer.md.
</verification>

<success_criteria>
- ROLE-01 advanced by 2 core roles; both emit real on-disk handoffs and state their owned board moves; Architect seeds the NFR catalog and writes ADRs from the frozen template.
- No scope-fence violation: no `plans/`-prefixed handoffs, no fabricated commands, no restated rules.
</success_criteria>

<output>
Create `.planning/phases/03-roles-agents-md-substrate/03-04-SUMMARY.md` when done.
</output>

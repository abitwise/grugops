---
phase: 03-roles-agents-md-substrate
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - agent-factory/roles/agents-md-scribe.md
  - agent-factory/roles/brownfield-mapper.md
  - agent-factory/roles/greenfield-mapper.md
autonomous: true
requirements: [ROLE-01]

must_haves:
  truths:
    - "agents-md-scribe.md reproduces the §5.A.2 caveman prompt verbatim and states it owns the 12 rules within AGENTS.md"
    - "brownfield-mapper.md and greenfield-mapper.md reproduce their §5.A.3/§5.A.4 caveman prompts verbatim"
    - "all three roles carry kind: role + tier: core, read config first, and append to traceability"
    - "the two mappers name their runtime outputs (memory-bank/brownfield-map.md, memory-bank/greenfield-plan.md) without seeding them"
    - "none of the three restates the 12 rules except the Scribe, which owns them and may echo in grug voice"
  artifacts:
    - path: "agent-factory/roles/agents-md-scribe.md"
      provides: "Scribe role — owner of the 12 rules in AGENTS.md (ROLE-01)"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/brownfield-mapper.md"
      provides: "Brownfield mapper role (ROLE-01)"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/greenfield-mapper.md"
      provides: "Greenfield mapper role (ROLE-01)"
      contains: "## Caveman prompt"
  key_links:
    - from: "agent-factory/roles/agents-md-scribe.md"
      to: "AGENTS.md"
      via: "owns and maintains the 12 rules within it"
      pattern: "AGENTS\\.md"
    - from: "agent-factory/roles/*.md"
      to: "agent-factory/config/factory.config.json"
      via: "Reads config first (D-17)"
      pattern: "factory\\.config\\.json"
---

<objective>
Author the substrate/mapping cluster of core roles: `agents-md-scribe.md` (owns the 12 rules in AGENTS.md), `brownfield-mapper.md`, and `greenfield-mapper.md`. Each is the §5 9-section skeleton: verbatim caveman prompt + terse frozen-path connective tissue (D-15). None of the three causes a board transition (they author substrate / map land); state "no board transition" explicitly.

Purpose: ROLE-01 (3 of the 11 core roles). The Scribe is the single owner of the 12 rules that land in AGENTS.md in Wave 3 (D-19) — its body references AGENTS.md by filename. The mappers name their Phase-4 runtime outputs without seeding them.
Output: 3 files under `agent-factory/roles/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md
@.planning/phases/03-roles-agents-md-substrate/03-PATTERNS.md
@.planning/phases/03-roles-agents-md-substrate/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author agents-md-scribe.md (owns the 12 rules)</name>
  <files>agent-factory/roles/agents-md-scribe.md</files>
  <read_first>
    - `.planning/phases/03-roles-agents-md-substrate/03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **2. AGENTS.md Scribe (L391–398)** — reproduce VERBATIM; Roles Inventory row 2 (Output = root `AGENTS.md`; Board transition = none; Activates when = "Need AGENTS.md").
    - Spec `docs/initial/agent_factory_builder_spec_v2.md` §5.A.2 (L391–398 prompt; L400 v2 note — the Scribe owns AGENTS.md and the "removal as much as authoring / no fake commands / UNKNOWN - verify" rule).
    - `.planning/phases/03-roles-agents-md-substrate/03-PATTERNS.md` → frontmatter + heading house style (analog `agent-factory/handoffs/universal-handoff.md`); "12-rules single-source pointer" (the Scribe OWNS, does not just point — may echo in grug voice).
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `memory-bank/00-index.md`, `AGENTS.md` (the artifact it owns).
  </read_first>
  <action>
    Create `agent-factory/roles/agents-md-scribe.md`, frontmatter `kind: role` + `tier: core`, the 9-section §5 skeleton (parentheticals kept on Output/Board moves/Trace updates). `## Caveman prompt` = L391–398 VERBATIM. `## Reads`: config first, then `plans/board.md`, `memory-bank/00-index.md` on start. `## Activates when`: "Need AGENTS.md" (created/updated). `## Output (file + format)`: root `AGENTS.md` — and state the Scribe OWNS and maintains the 12 coding rules within it (the single source per D-19; it may echo them in grug voice in this body, but the canonical clear-voice copy lives in AGENTS.md). `## Board moves`: state explicitly "none — substrate authoring, no board transition" (D-23). `## Trace updates`: append to `plans/traceability.md` (D-17). `## Hard limits`: include the §5.A.2 discipline — no fake commands, ship `UNKNOWN - verify`, remove what a linter/CI already enforces (CLEAR voice on the no-fabrication line). Because this role is the OWNER of the 12 rules, do not add the generic "Follow the 12 rules in AGENTS.md" pointer the other 14 roles use; instead note ownership. Keep ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Reads' -e '## Activates when' -e '## Responsibilities' -e '## Output' -e '## Board moves' -e '## Trace updates' -e '## Hard limits' agent-factory/roles/agents-md-scribe.md && grep -q 'tier: core' agent-factory/roles/agents-md-scribe.md && grep -q 'factory.config.json' agent-factory/roles/agents-md-scribe.md && grep -q 'UNKNOWN - verify' agent-factory/roles/agents-md-scribe.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/agents-md-scribe.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings present; frontmatter `kind: role` + `tier: core`.
    - Cites `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `AGENTS.md`; carries `UNKNOWN - verify` (no-fabrication discipline).
    - `## Board moves` states no transition; drift guard `grep -l 'plans/.*-handoff'` empty.
    - Manual-only: `## Caveman prompt` diffs byte-exact against spec L391–398; states ownership of the 12 rules (the one role allowed to carry rule text, optionally in grug echo).
  </acceptance_criteria>
  <done>agents-md-scribe.md exists, 9 sections, verbatim prompt, owns the 12 rules in AGENTS.md, no board transition, clear-voice no-fabrication discipline.</done>
</task>

<task type="auto">
  <name>Task 2: Author brownfield-mapper.md</name>
  <files>agent-factory/roles/brownfield-mapper.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **3. Brownfield Mapper (L405–408)**; Roles Inventory row 3 (Output = `memory-bank/brownfield-map.md` — RUNTIME output, NOT seeded, correctly absent, Phase-4; Board transition = none; Activates when = "Need repo mapping of an existing repo").
    - Spec §5.A.3 (L405–408).
    - `03-PATTERNS.md` → frontmatter/heading house style; read_first targets per role row `brownfield-mapper`.
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `memory-bank/00-index.md`, `AGENTS.md` (12-rules pointer).
  </read_first>
  <action>
    Create `agent-factory/roles/brownfield-mapper.md`, frontmatter `kind: role` + `tier: core`, 9-section skeleton. `## Caveman prompt` = L405–408 VERBATIM. `## Activates when`: "Need repo mapping of an existing repo". `## Output (file + format)`: `memory-bank/brownfield-map.md` — note it is produced at runtime under the Phase-4 bootstrap workflow (template intentionally not seeded; do NOT create that file in this phase). `## Board moves`: "none — maps; does not move tickets". `## Reads`/`## Trace updates`: the D-17 universal lines. Add the one-line 12-rules pointer to `AGENTS.md` (D-19). The §5.A.3 hard limit is the prompt's own "do not refactor / do not fix / only map" — reflect tersely in `## Hard limits`. ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Board moves' -e '## Hard limits' agent-factory/roles/brownfield-mapper.md && grep -q 'tier: core' agent-factory/roles/brownfield-mapper.md && grep -q 'factory.config.json' agent-factory/roles/brownfield-mapper.md && grep -q 'plans/traceability.md' agent-factory/roles/brownfield-mapper.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/brownfield-mapper.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: core`; the three D-17 lines present.
    - `## Output` names `memory-bank/brownfield-map.md` as a runtime output (NOT created this phase).
    - `## Board moves` = none; one-line 12-rules pointer present, rule text NOT restated.
    - Manual-only: caveman prompt byte-exact vs spec L405–408.
  </acceptance_criteria>
  <done>brownfield-mapper.md exists, 9 sections, verbatim prompt, names its runtime output, no board transition.</done>
</task>

<task type="auto">
  <name>Task 3: Author greenfield-mapper.md</name>
  <files>agent-factory/roles/greenfield-mapper.md</files>
  <read_first>
    - `03-RESEARCH.md` → "Verbatim Caveman Prompts" entry **4. Greenfield Mapper (L415–420)**; Roles Inventory row 4 (Output = `memory-bank/greenfield-plan.md` — RUNTIME output, not seeded; Board transition = none, first tickets land in Backlog; Activates when = "Need repo mapping / shaping of empty land").
    - Spec §5.A.4 (L415–420).
    - `03-PATTERNS.md` → house style; read_first row `greenfield-mapper`.
    - Frozen paths: `agent-factory/config/factory.config.json`, `plans/board.md`, `plans/traceability.md`, `memory-bank/00-index.md`, `AGENTS.md`.
  </read_first>
  <action>
    Create `agent-factory/roles/greenfield-mapper.md`, frontmatter `kind: role` + `tier: core`, 9-section skeleton. `## Caveman prompt` = L415–420 VERBATIM. `## Activates when`: "Need repo mapping / shaping of empty land". `## Output (file + format)`: `memory-bank/greenfield-plan.md` — runtime output under Phase-4 bootstrap (do NOT seed it). `## Board moves`: "none — shapes empty land; first tickets land in `Backlog`". `## Reads`/`## Trace updates`: D-17 universal lines. One-line 12-rules pointer to `AGENTS.md`. `## Hard limits`: reflect the prompt's "do not overbuild" + boring-stack-unless-told. ~one screen, invent nothing.
  </action>
  <verify>
    <automated>grep -q -e '## One job' -e '## Caveman prompt' -e '## Activates when' -e '## Board moves' -e '## Hard limits' agent-factory/roles/greenfield-mapper.md && grep -q 'tier: core' agent-factory/roles/greenfield-mapper.md && grep -q 'factory.config.json' agent-factory/roles/greenfield-mapper.md && grep -q 'plans/traceability.md' agent-factory/roles/greenfield-mapper.md && ! grep -q 'plans/.*-handoff' agent-factory/roles/greenfield-mapper.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 9 §5 headings; frontmatter `kind: role` + `tier: core`; three D-17 lines present.
    - `## Output` names `memory-bank/greenfield-plan.md` as a runtime output (NOT created this phase).
    - `## Board moves` = none (first tickets to Backlog); one-line 12-rules pointer present.
    - Manual-only: caveman prompt byte-exact vs spec L415–420.
  </acceptance_criteria>
  <done>greenfield-mapper.md exists, 9 sections, verbatim prompt, names its runtime output, no board transition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored role text → future executing agent | Role text is the behavior the agent later acts on; fabricated paths/commands or restated-and-drifted rules propagate to runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-Tamper | Tampering | Scribe hard limits / mapper outputs | mitigate | Scribe carries the §5.A.2 no-fabrication discipline verbatim (ship `UNKNOWN - verify`, never invent commands, D-18); mappers name runtime outputs, never fabricate seeded files. |
| T-03-Info | Info-integrity | 12-rules single-source | mitigate | Only the Scribe carries rule text (it owns them, D-19); the two mappers add a one-line pointer and do NOT restate — preventing drift across files. |
| T-03-Info | Info-integrity | safety/discipline voice | mitigate | The no-fabrication discipline line is CLEAR voice (D-21); grug voice confined to `## Caveman prompt`. |
</threat_model>

<verification>
- Each of the 3 files passes its 9-section + `tier: core` frontmatter + three D-17 universal-line greps.
- Drift guard empty across all three.
- `memory-bank/brownfield-map.md` and `memory-bank/greenfield-plan.md` are NOT created (named only).
- After this plan, re-run `.planning/phases/03-roles-agents-md-substrate/check-structure.sh` — still RED overall (other roles + AGENTS.md pending) but these three pass their per-file checks.
</verification>

<success_criteria>
- ROLE-01 advanced by 3 core roles; Scribe established as the single owner of the 12 rules.
- Two mappers correctly defer their outputs to Phase-4 runtime.
- No scope-fence violation: no seeded runtime outputs, no fabricated commands, no restated rules.
</success_criteria>

<output>
Create `.planning/phases/03-roles-agents-md-substrate/03-02-SUMMARY.md` when done.
</output>

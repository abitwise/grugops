---
phase: 01-substrate-config-state-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/roles/.gitkeep
  - agent-factory/workflows/.gitkeep
  - agent-factory/handoffs/.gitkeep
  - agent-factory/checklists/.gitkeep
  - agent-factory/examples/.gitkeep
  - agent-factory/packaging/.gitkeep
  - agent-factory/config/.gitkeep
  - plans/sprints/.gitkeep
  - plans/releases/.gitkeep
  - plans/epics/.gitkeep
  - plans/features/.gitkeep
  - plans/tickets/.gitkeep
  - memory-bank/50-decisions/.gitkeep
  - install/.gitkeep
  - .claude-plugin/.gitkeep
autonomous: true
requirements: [STRUCT-01]

must_haves:
  truths:
    - "The §3 directory tree exists under agent-factory/, plans/, memory-bank/, install/, .claude-plugin/ (STRUCT-01)"
    - "Pre-existing user content (docs/, .planning/, .claude/, CLAUDE.md) is unchanged — additive only (STRUCT-01, D-00)"
    - "Root AGENTS.md is NOT created in this phase — it is Phase-3-owned (D-05)"
    - "Empty directories carry .gitkeep so the tree is committable (STRUCT-01, Claude's-discretion per spec §3)"
  artifacts:
    - path: "agent-factory/roles/.gitkeep"
      provides: "roles/ directory (frozen path roles land in Phase 3)"
    - path: "agent-factory/config/.gitkeep"
      provides: "config/ directory (factory.config.json lands in Plan 02)"
    - path: "plans/tickets/.gitkeep"
      provides: "tickets/ directory of the user-facing kit template"
    - path: "memory-bank/50-decisions/.gitkeep"
      provides: "ADR directory inside memory-bank"
    - path: ".claude-plugin/.gitkeep"
      provides: ".claude-plugin/ directory (manifest lands in Phase 5)"
  key_links:
    - from: "scaffold task"
      to: "docs/, .planning/, .claude/, CLAUDE.md"
      via: "additive-only invariant (must NOT touch these)"
      pattern: "git status --porcelain shows no changes outside new kit tree"
---

<objective>
Scaffold the grugops kit repository tree exactly as specified in `docs/initial/agent_factory_builder_spec_v2.md` §3 — `agent-factory/{roles,workflows,handoffs,checklists,examples,packaging,config}`, `plans/{board,traceability,nfr-catalog,metrics,sprints,releases,epics,features,tickets}`, `memory-bank/*`, `install/`, and `.claude-plugin/`. Empty directories carry `.gitkeep`. This is Wave 1: every other Phase-1 file lives inside this tree, and the additive-only invariant (never delete/overwrite `docs/`, `.planning/`, `.claude/`, `CLAUDE.md`) is established here.

Per D-05, this plan does NOT create root `AGENTS.md` — that single §3 item is intentionally deferred to Phase 3 (AGENTS-01/AGENTS-02). Per D-04, `plans/` is the user-facing kit template and is kept distinct from `.planning/` (grugops's own GSD build state). Files that will be populated in later Phase-1 plans (factory.config.json, board.md, traceability.md, nfr-catalog.md, metrics.md, VERSION, README.md) are NOT created here — only their parent directories, with `.gitkeep` where the directory would otherwise be empty.

Purpose: Freeze the directory paths every later file cites by name, without ever moving them again.
Output: The §3 directory skeleton with `.gitkeep` markers, committed and additive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md
@docs/initial/agent_factory_builder_spec_v2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the §3 kit directory tree with .gitkeep markers (additive only)</name>
  <files>agent-factory/{roles,workflows,handoffs,checklists,examples,packaging,config}/.gitkeep, plans/{sprints,releases,epics,features,tickets}/.gitkeep, memory-bank/50-decisions/.gitkeep, install/.gitkeep, .claude-plugin/.gitkeep</files>
  <read_first>
    - docs/initial/agent_factory_builder_spec_v2.md §3 (Required Deliverables / repository structure) — the EXACT directory tree to reproduce; this is the authoritative source of truth for every path name
    - .planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md — D-04 (plans/ is the kit template, distinct from .planning/), D-05 (no root AGENTS.md), Claude's-discretion (.gitkeep usage, plans/initial-plan.md is optional)
    - CLAUDE.md — additive/idempotent/never-overwrite constraint; lowercase grugops brand
  </read_first>
  <action>
    Create the directory tree from spec §3, ADDITIVELY (mkdir -p only; never delete or overwrite existing paths). Create these directories and place a zero-byte `.gitkeep` in each one that ships empty this phase:
    - `agent-factory/` and its children: `roles/`, `workflows/`, `handoffs/`, `checklists/`, `examples/`, `packaging/`, `config/` — each gets a `.gitkeep` (their content files arrive in later phases/plans).
    - `plans/` and its children: `sprints/`, `releases/`, `epics/`, `features/`, `tickets/` — each child gets a `.gitkeep` (matching the `.gitkeep` shown in spec §3). Do NOT add a `.gitkeep` directly in `plans/` itself, because `plans/board.md`, `plans/traceability.md`, `plans/nfr-catalog.md`, `plans/metrics.md` will be created there by Plans 03/04.
    - `memory-bank/` and `memory-bank/50-decisions/` — place a `.gitkeep` ONLY in `50-decisions/` (matching spec §3, which shows `.gitkeep` there). The numbered memory-bank files (00-index.md … 80-glossary.md) are Phase-2-owned (MEM-01) — do NOT create them or a top-level memory-bank/.gitkeep here; just create the `memory-bank/` and `memory-bank/50-decisions/` directories.
    - `install/` — gets a `.gitkeep` (install.sh/install.mjs/uninstall.sh/README.md are Phase-5-owned).
    - `.claude-plugin/` — gets a `.gitkeep` (plugin.json/marketplace.json are Phase-5-owned).
    Do NOT create: root `AGENTS.md` (D-05, Phase-3-owned), `agent-factory/config/factory.config.json` or `factory.config.md` (Plan 02), `plans/board.md` (Plan 03), `plans/traceability.md`/`nfr-catalog.md`/`metrics.md` (Plan 04), `agent-factory/VERSION` or `agent-factory/README.md` (Plan 05). `plans/initial-plan.md` is optional this phase (Claude's discretion) — leave it out; the bootstrap workflow (Phase 4) seeds it.
    Touch NOTHING under `docs/`, `.planning/`, `.claude/`, or `CLAUDE.md`.
  </action>
  <verify>
    <automated>test -d agent-factory/roles && test -d agent-factory/workflows && test -d agent-factory/handoffs && test -d agent-factory/checklists && test -d agent-factory/examples && test -d agent-factory/packaging && test -d agent-factory/config && test -d plans/sprints && test -d plans/releases && test -d plans/epics && test -d plans/features && test -d plans/tickets && test -d memory-bank/50-decisions && test -d install && test -d .claude-plugin && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `agent-factory/{roles,workflows,handoffs,checklists,examples,packaging,config}` all exist as directories
    - `plans/{sprints,releases,epics,features,tickets}` all exist as directories, each containing a `.gitkeep`
    - `memory-bank/` and `memory-bank/50-decisions/` exist; `memory-bank/50-decisions/.gitkeep` exists
    - `install/` and `.claude-plugin/` exist, each containing a `.gitkeep`
    - Each of the seven `agent-factory/*` subdirectories contains a `.gitkeep`
    - `test ! -f AGENTS.md` passes (root AGENTS.md NOT created — D-05)
    - `test ! -f agent-factory/config/factory.config.json` passes (deferred to Plan 02)
    - `test ! -f plans/board.md` passes (deferred to Plan 03)
    - `ls -la docs/ .planning/ .claude/ CLAUDE.md` show pre-existing content present and unmodified
  </acceptance_criteria>
  <done>The complete §3 directory tree exists with `.gitkeep` markers; root AGENTS.md and all later-plan content files are absent; no pre-existing user content was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Verify additive-only invariant against existing user content</name>
  <files>(verification only — no files written; asserts git status on docs/, .planning/, .claude/, CLAUDE.md)</files>
  <read_first>
    - .planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md — code_context (additive scaffolding; plans/ vs .planning/ separation)
    - CLAUDE.md — "Installers: Idempotent, additive, dry-run-capable, reversible; never overwrite or delete user content"
  </read_first>
  <action>
    Confirm the scaffold touched nothing outside the new kit tree. Run `git status --porcelain` and assert every reported path is one of the newly-created kit directories/`.gitkeep` files (all under `agent-factory/`, `plans/`, `memory-bank/`, `install/`, or `.claude-plugin/`). Assert that no path under `docs/`, `.planning/`, `.claude/`, or `CLAUDE.md` appears as modified (M) or deleted (D). This is a verification task, not a content task — it produces no new files; it asserts the additive-only safety property that is the one real threat surface for this phase.
  </action>
  <verify>
    <automated>git status --porcelain | grep -vE '^\?\? (agent-factory/|plans/|memory-bank/|install/|\.claude-plugin/)' | grep -E '^( M| D|M |D |MM)' | grep -E 'docs/|\.planning/|\.claude/|CLAUDE\.md' && echo ADDITIVE_VIOLATION || echo ADDITIVE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` shows no modified (M) or deleted (D) entries for `docs/`, `.planning/`, `.claude/`, or `CLAUDE.md`
    - All new entries are untracked (`??`) and live under `agent-factory/`, `plans/`, `memory-bank/`, `install/`, or `.claude-plugin/`
    - The verify command prints `ADDITIVE_OK`
  </acceptance_criteria>
  <done>`git status` confirms the scaffold is strictly additive: only new kit-tree files appear; no pre-existing user content was modified or deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → existing repo files | The scaffold step writes to the filesystem; the only real risk is clobbering pre-existing user content (`docs/`, `.planning/`, `.claude/`, `CLAUDE.md`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | scaffold mkdir/write step | mitigate | Use `mkdir -p` and create only new paths; Task 2 asserts via `git status --porcelain` that no `docs/`/`.planning/`/`.claude/`/`CLAUDE.md` path is Modified or Deleted (additive-only invariant — the one real safety property of this phase per the security brief). |
| T-01-02 | Information Disclosure | `.gitkeep` markers | accept | `.gitkeep` files are empty; no secrets, no executable code, no network, no untrusted input. No threat above LOW. |

No package-manager installs occur in this plan, so no Package Legitimacy Gate applies. There is no executable code, network access, authentication, untrusted input, or secret handling in this plan; aside from the additive-only invariant (mitigated above), there are no threats above LOW.
</threat_model>

<verification>
- The complete §3 directory tree exists (all `agent-factory/*`, `plans/*`, `memory-bank/50-decisions/`, `install/`, `.claude-plugin/` directories present).
- `.gitkeep` markers present where spec §3 shows them and where a directory would otherwise be empty.
- Root `AGENTS.md` is NOT created (D-05).
- No later-plan content files (config json/md, board.md, traceability/nfr/metrics, VERSION, README) were created here.
- `git status` confirms strictly additive changes; `docs/`, `.planning/`, `.claude/`, `CLAUDE.md` untouched.
</verification>

<success_criteria>
- STRUCT-01 directory skeleton complete and committable.
- Additive-only invariant verified (no pre-existing user content modified/deleted).
- Frozen paths in place for Plans 02–05 and all downstream phases to populate.
</success_criteria>

<output>
Create `.planning/phases/01-substrate-config-state-skeleton/01-01-SUMMARY.md` when done.
</output>

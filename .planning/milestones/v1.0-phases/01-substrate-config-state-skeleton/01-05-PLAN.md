---
phase: 01-substrate-config-state-skeleton
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - agent-factory/VERSION
  - agent-factory/README.md
autonomous: true
requirements: [STRUCT-02]

must_haves:
  truths:
    - "agent-factory/VERSION contains exactly 0.1.0 (D-02, matching factory.config.json#version)"
    - "agent-factory/README.md explains usage across all five tools (the dispatch difference) (STRUCT-02, D-06)"
    - "README gives copy-paste Orchestrator prompts for bootstrap, refine, plan, sweep, ticket→PR, gate, UAT, and release (STRUCT-02, D-06)"
    - "README 'start here' pointer targets agent-factory/roles/orchestrator.md (frozen path) and notes the portable AGENTS.md substrate lands in Phase 3 (D-06, D-05)"
    - "README opens in clear/professional voice; no grug caveman voice (Voice constraint)"
  artifacts:
    - path: "agent-factory/VERSION"
      provides: "The working version seed 0.1.0"
      contains: "0.1.0"
    - path: "agent-factory/README.md"
      provides: "Full usage guide: 5-tool dispatch table + copy-paste Orchestrator prompts"
      min_lines: 40
  key_links:
    - from: "agent-factory/README.md start-here pointer"
      to: "agent-factory/roles/orchestrator.md"
      via: "frozen-path reference"
      pattern: "agent-factory/roles/orchestrator.md"
    - from: "agent-factory/VERSION"
      to: "agent-factory/config/factory.config.json#version"
      via: "same seed value 0.1.0"
      pattern: "0\\.1\\.0"
---

<objective>
Create `agent-factory/VERSION` (the working version seed `0.1.0`, per D-02, matching `factory.config.json#version`) and write `agent-factory/README.md` COMPLETELY now (per D-06 — this fully satisfies STRUCT-02). The README is a usage guide: it explains what the factory is, how to use it across all five host tools (Claude Code, Codex CLI, Gemini CLI, OpenCode, GitHub Copilot CLI) — the dispatch difference — and gives copy-paste Orchestrator prompts for bootstrap, refine, plan, sweep, ticket→PR, gate, UAT, and release.

These prompts are usage instructions that only need the FROZEN paths this phase locks, so the README can be written before the role/workflow bodies exist (Phase 3/4). The "start here" pointer targets `agent-factory/roles/orchestrator.md` (frozen path) and explicitly NOTES that the portable AGENTS.md substrate lands in Phase 3 (consistent with D-05 — this phase does not create root AGENTS.md). Clear/professional voice throughout; grug caveman voice is reserved for Phase-3 role prompts. Brand name lowercase `grugops`.

Purpose: Ship a complete, usable README and the version seed so STRUCT-02 is fully met now.
Output: `agent-factory/VERSION` (= 0.1.0) + `agent-factory/README.md` (full usage guide).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md
@docs/initial/agent_factory_builder_spec_v2.md
@docs/initial/grugops_brand_manual.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write agent-factory/VERSION = 0.1.0</name>
  <files>agent-factory/VERSION</files>
  <read_first>
    - .planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md — D-02 (seed VERSION and factory.config.json#version to 0.1.0; spec example is 2.0.0 but we deliberately diverge; final string is a Phase-5 decision)
    - docs/initial/agent_factory_builder_spec_v2.md §3 — confirms `agent-factory/VERSION` location (the spec's example value "2.0.0" is overridden by D-02)
  </read_first>
  <action>
    Create `agent-factory/VERSION` containing exactly the SemVer string `0.1.0` (D-02 — NOT the spec's example `2.0.0`). The file is a single line; a trailing newline is fine. No other content, no prose, no comment. This value must match `factory.config.json#version` (also 0.1.0) produced in Plan 02.
  </action>
  <verify>
    <automated>test -f agent-factory/VERSION && [ "$(tr -d '[:space:]' < agent-factory/VERSION)" = "0.1.0" ] && echo VERSION_OK || { echo VERSION_BAD; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `agent-factory/VERSION` exists
    - Its content, trimmed of whitespace, equals exactly `0.1.0` (NOT `2.0.0`)
    - Matches the `version` field in `agent-factory/config/factory.config.json`
  </acceptance_criteria>
  <done>VERSION contains the working seed 0.1.0, consistent with the config dial.</done>
</task>

<task type="auto">
  <name>Task 2: Write the full agent-factory/README.md (5-tool dispatch + copy-paste Orchestrator prompts)</name>
  <files>agent-factory/README.md</files>
  <read_first>
    - docs/initial/agent_factory_builder_spec_v2.md §17.2 (agent-factory/README.md) — the authoritative list of what the README must explain AND the exact copy-paste Orchestrator prompts (bootstrap brownfield/greenfield, refine #07, plan sprint #08, daily sweep #09, implement ticket #04, PR quality gate #05, UAT pack #06, prepare release #12); reproduce these prompts
    - docs/initial/agent_factory_builder_spec_v2.md §0 — the lean defaults (mode=lean/cadence=kanban/autonomy=pr) to mention as the zero-config baseline
    - CLAUDE.md "Format Schemas → §6 AGENTS.md + per-tool entry files" table — the five tools and their dispatch difference (Claude Code subagent spawn vs portable AGENTS.md sequential role-load); use this for the 5-tool usage/dispatch table
    - .planning/phases/01-substrate-config-state-skeleton/01-CONTEXT.md — D-06 (write README fully now; start-here → orchestrator.md; note AGENTS.md lands Phase 3), D-05 (no root AGENTS.md this phase), voice note (clear voice)
    - docs/initial/grugops_brand_manual.md §4.2 — clear voice rules for docs; lowercase grugops
  </read_first>
  <action>
    Create `agent-factory/README.md` as a complete usage guide in clear/professional voice (NOT grug caveman voice — reserved for Phase-3 role prompts; lowercase `grugops` throughout). Include these sections:
    1. A one-paragraph "what is grugops / what is the agent factory" intro (clear voice, plain English).
    2. A "Start here" pointer: all work starts at `agent-factory/roles/orchestrator.md` (the frozen path). Add a NOTE that the portable root `AGENTS.md` substrate (the other entry point most tools read) lands in Phase 3 — do not claim it exists yet (consistent with D-05).
    3. A "Usage across the five tools" table mapping each of Claude Code, Codex CLI, Gemini CLI, OpenCode, GitHub Copilot CLI to its entry file + dispatch mode, and stating the single rule that the CONTENT is identical and only the DISPATCH differs (Claude Code can spawn subagents via the Orchestrator; the other four read the portable AGENTS.md substrate and load roles sequentially). Note that the detailed per-tool adapters ship in Phase 5 — this table is the usage overview.
    4. A "Configuration" note: the dial lives at `agent-factory/config/factory.config.json` (documented in `factory.config.md`); zero-config baseline is `mode=lean`, `cadence=kanban`, `autonomy=pr`.
    5. A "How work flows" note pointing at `plans/board.md` (the WIP board), `plans/traceability.md` (the trace matrix), and the lifecycle, in one or two sentences each — referencing the frozen paths.
    6. A "Copy-paste Orchestrator prompts" section reproducing the §17.2 prompts: bootstrap brownfield, plan greenfield idea, refine the backlog (#07), plan sprint n (#08, scrum), run the daily sweep (#09), implement ticket via ticket-to-pr (#04), run the PR quality gate (#05), create the UAT pack (#06), prepare a release (#12, enterprise). Keep each prompt copy-paste usable.
    7. An "Install" note: point at `install/` and the "just install the markdown" minimal path (note the installers themselves ship in Phase 5).
    Do NOT invent commands or claim files exist that this phase did not create (no fake gate/test/command output; mark anything genuinely unknown `UNKNOWN - verify`). Where a referenced file (roles, workflows, AGENTS.md, installers) is a later-phase deliverable, state that plainly rather than implying it exists.
  </action>
  <verify>
    <automated>test -f agent-factory/README.md && grep -q "agent-factory/roles/orchestrator.md" agent-factory/README.md && for t in "Claude Code" "Codex" "Gemini" "OpenCode" "Copilot"; do grep -q "$t" agent-factory/README.md || { echo "MISSING_TOOL $t"; exit 1; }; done && for p in "Bootstrap" "[Rr]efine" "[Ss]print" "daily sweep" "ticket-to-pr" "quality gate" "UAT" "[Rr]elease"; do grep -Eq "$p" agent-factory/README.md || { echo "MISSING_PROMPT $p"; exit 1; }; done && grep -qi "mode=lean" agent-factory/README.md && [ "$(grep -vc '^[[:space:]]*$' agent-factory/README.md)" -ge 40 ] && echo README_OK</automated>
  </verify>
  <acceptance_criteria>
    - `agent-factory/README.md` exists with at least 40 non-blank lines
    - "Start here" pointer references `agent-factory/roles/orchestrator.md` and notes the portable AGENTS.md substrate lands in Phase 3
    - All five tools named: Claude Code, Codex CLI, Gemini CLI, OpenCode, GitHub Copilot CLI — with the dispatch-difference rule ("only the dispatch differs, never the content")
    - Copy-paste Orchestrator prompts present for: bootstrap, refine, plan sprint, daily sweep, ticket→PR, quality gate, UAT, release (the loop above passes)
    - Zero-config baseline `mode=lean` (and kanban/pr) mentioned
    - Clear/professional voice; lowercase `grugops`; no grug caveman voice; no fabricated commands (unknowns marked `UNKNOWN - verify`)
  </acceptance_criteria>
  <done>The README fully satisfies STRUCT-02: a complete usage guide with the 5-tool dispatch table and all copy-paste Orchestrator prompts, pointing at frozen paths and honestly noting later-phase deliverables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → human reader | The README instructs humans; the only integrity concern is fabricating commands/claims (a no-fabrication violation), not a security exploit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Repudiation/Integrity | README copy-paste prompts & commands | mitigate | The no-fabrication constraint (CLAUDE.md) is enforced: Task 2 forbids invented commands and requires `UNKNOWN - verify` for anything unverified, and requires later-phase deliverables to be stated as not-yet-existing rather than implied present. |
| T-01-08 | Information Disclosure | VERSION / README markdown | accept | Plain markdown + a SemVer string; no secrets, no executable code, no network, no untrusted input. No threat above LOW. |

No package-manager installs occur in this plan. There is no executable code, network access, authentication, untrusted input, or secret handling. Aside from the LOW items above, there are no threats above LOW for this plan.
</threat_model>

<verification>
- `agent-factory/VERSION` contains exactly `0.1.0`, matching `factory.config.json#version`.
- `agent-factory/README.md` is a complete usage guide: 5-tool dispatch table + all copy-paste Orchestrator prompts, start-here → orchestrator.md, notes AGENTS.md lands Phase 3.
- Clear voice; lowercase grugops; no fabricated commands.
</verification>

<success_criteria>
- STRUCT-02: `agent-factory/VERSION` and `agent-factory/README.md` exist; the README explains usage across all five tools and gives copy-paste Orchestrator prompts for bootstrap, refine, plan, sweep, ticket→PR, gate, UAT, and release.
</success_criteria>

<output>
Create `.planning/phases/01-substrate-config-state-skeleton/01-05-SUMMARY.md` when done.
</output>

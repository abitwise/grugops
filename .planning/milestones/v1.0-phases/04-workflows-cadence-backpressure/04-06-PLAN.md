---
phase: 04-workflows-cadence-backpressure
plan: 06
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - agent-factory/workflows/07-backlog-refinement.md
  - agent-factory/workflows/09-daily-sweep.md
  - agent-factory/workflows/11-retro.md
autonomous: true
requirements: [FLOW-03, FLOW-05, BOARD-02]
must_haves:
  truths:
    - "07-backlog-refinement keeps the Ready column stocked (clarify, split XL, size, prioritize, push DoR-met items to Ready) and applies to both cadences"
    - "09-daily-sweep is the both-cadence board reconciliation pass: updates board/metrics/60-progress, escalates past blocked_escalation_days, recommends next pull within WIP (the BOARD-02 engine)"
    - "11-retro reads metrics, identifies top wastes, writes retro-notes, creates improvement tickets tagged factory; applies to both (light in lean)"
  artifacts:
    - path: "agent-factory/workflows/07-backlog-refinement.md"
      provides: "Backlog refinement ceremony (both cadences)"
      min_lines: 20
    - path: "agent-factory/workflows/09-daily-sweep.md"
      provides: "Daily sweep reconciliation ceremony (both cadences, BOARD-02 engine)"
      min_lines: 22
    - path: "agent-factory/workflows/11-retro.md"
      provides: "Retro ceremony (both cadences)"
      min_lines: 20
  key_links:
    - from: "agent-factory/workflows/09-daily-sweep.md"
      to: "memory-bank/60-progress.md"
      via: "daily sweep keeps progress current"
      pattern: "60-progress.md"
    - from: "agent-factory/workflows/11-retro.md"
      to: "agent-factory/handoffs/retro-notes.md"
      via: "Factory Coach retro output"
      pattern: "retro-notes.md"
---

<objective>
Author the three both-cadence ceremony workflows: `07-backlog-refinement.md`, `09-daily-sweep.md`, `11-retro.md`. Each is expanded from its §7.8/§7.10/§7.12 one-liner into all 10 template sections (FLOW-05) and declares its applicability as both cadences in `When to use` (D-25). `09-daily-sweep` is the BOARD-02 reconciliation engine - continuous-pull/WIP-throttle/board-status reconciliation/cycle-time focus.

Purpose: Deliver the cadence-agnostic ceremonies (FLOW-03) and satisfy BOARD-02 (Kanban works) via the daily-sweep reconciliation + WIP throttle + cycle-time metrics.
Output: `agent-factory/workflows/07-backlog-refinement.md`, `agent-factory/workflows/09-daily-sweep.md`, `agent-factory/workflows/11-retro.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md
@.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md
@agent-factory/roles/orchestrator.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 07-backlog-refinement.md (both cadences)</name>
  <files>agent-factory/workflows/07-backlog-refinement.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 07-backlog-refinement.md (§7.8: When regularly/before planning; Agents BA/PM (+ System Analyst, + Architect/Design for spikes); Steps pull top of Backlog, clarify, split XL, size, prioritize, mark security/NFR triggers, push DoR-met items into Ready; Done when Ready stocked) + the 10-section template.
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `07-backlog-refinement.md` (ba-pm Backlog -> Ready after definition-of-ready.md; Output refinement-notes.md; Orchestrator enforces SPLIT_REQUIRED; declares cadence both) + Shared Patterns S1, S2, S4.
    - `agent-factory/roles/orchestrator.md` (SPLIT_REQUIRED). `agent-factory/roles/ba-pm.md`, `agent-factory/roles/system-analyst.md`, `agent-factory/roles/architect-design.md` (spikes).
    - `agent-factory/handoffs/refinement-notes.md`, `agent-factory/checklists/definition-of-ready.md` (cited by name).
  </read_first>
  <action>
    Create `agent-factory/workflows/07-backlog-refinement.md` with kind: workflow frontmatter (<=3 fields; optionally cadence: both) then `# Workflow: Backlog refinement` and the 10 sections in order. When to use (clear voice, light wink allowed only here): declare this applies to BOTH cadences - run regularly in Kanban, before planning in Scrum. Agents involved: BA/PM (+ System Analyst, + Architect/Design for spikes). Steps (clear voice): pull the top of Backlog, clarify, split XL (SPLIT_REQUIRED, no XL into dev), size (XS-XL), prioritize (P0-P3), mark security/NFR triggers, push Definition-of-Ready-met items (agent-factory/checklists/definition-of-ready.md) into the Ready column. Board moves: Backlog -> Ready (ba-pm, after DoR). Handoffs produced: agent-factory/handoffs/refinement-notes.md. Trace updates: confirm/extend the refined tickets' rows in plans/traceability.md. Metrics emitted: none new (or a frozen-9 subset) - do not invent. Stop conditions: an item cannot be made Ready (missing input) -> leave in Backlog with the named gap. Done condition (reproduce §7.8): refinement-notes written; the Ready column is stocked so dev never starves. Declare the literal "both" cadence applicability. Keep ~one screen; cite only frozen names.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/07-backlog-refinement.md && grep -qF 'refinement-notes.md' agent-factory/workflows/07-backlog-refinement.md && grep -qiF 'both' agent-factory/workflows/07-backlog-refinement.md && echo OK-07; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/07-backlog-refinement.md exists with kind: workflow frontmatter (<=3 fields) and all 10 headings in order (V-02/V-03 green for 07).
    - When to use declares "both" cadences (V-10 portion); cites agent-factory/handoffs/refinement-notes.md and agent-factory/checklists/definition-of-ready.md (frozen lists); no invented name (V-12 green).
    - Steps include split XL (SPLIT_REQUIRED) and push-to-Ready after DoR.
  </acceptance_criteria>
  <done>07 authored, both-cadence, Ready-stocking, V-02/V-03/V-10/V-12 green.</done>
</task>

<task type="auto">
  <name>Task 2: Author 09-daily-sweep.md (both cadences, BOARD-02 engine)</name>
  <files>agent-factory/workflows/09-daily-sweep.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 09-daily-sweep.md (§7.10, both cadences: the standup-equivalent reconciliation pass; Steps read board + open handoffs, per in-flight ticket note progress/blocker, update plans/board.md + plans/metrics.md + memory-bank/60-progress.md, escalate anything blocked past threshold, recommend next pull respecting WIP; Done when "sweep report" produced) + §6.2 Kanban behavior (continuous pull, WIP throttle, cycle-time) for BOARD-02.
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `09-daily-sweep.md` (updates board/metrics/60-progress; escalates past blocked_escalation_days; names Cycle time/WIP/Blocked time; sweep-report wording discretionary) + Shared Patterns S1, S2, S5, S8.
    - `agent-factory/roles/orchestrator.md` (the role that runs the sweep; WIP discipline). `plans/board.md`, `plans/metrics.md`, `memory-bank/60-progress.md` (the three it updates). `agent-factory/config/factory.config.json` - blocked_escalation_days (=2), wip_limits.
  </read_first>
  <action>
    Create `agent-factory/workflows/09-daily-sweep.md` with kind: workflow frontmatter then `# Workflow: Daily sweep` and the 10 sections in order. When to use (clear voice, light wink only here): declare this applies to BOTH cadences - the standup-equivalent board reconciliation the Orchestrator runs on demand/schedule; it is the Kanban reconciliation engine. Agents involved: Orchestrator. Steps (clear voice): read plans/board.md + the open handoffs in agent-factory/handoffs/; per in-flight ticket note progress or blocker; reconcile each ticket's front-matter status against its board column; update plans/board.md, plans/metrics.md, and memory-bank/60-progress.md; escalate anything blocked past factory.config.json#blocked_escalation_days; recommend the next pull respecting WIP limits (continuous pull, finish before you start). Board moves: reconciles every column's status (no single owned transition - it is the WIP/throttle pass). Handoffs produced: none new; produce a "sweep report" (done / next / blocked) - wording discretionary. Trace updates: keep plans/traceability.md statuses current. Metrics emitted: Cycle time, WIP, Blocked time (from the frozen 9). Stop conditions: a ticket blocked past blocked_escalation_days -> escalate (move to/flag Blocked). Done condition (reproduce §7.10): a sweep report (done/next/blocked) is produced and board/metrics/60-progress are current. Declare the literal "both" cadence applicability. Keep ~one screen; cite only frozen names.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/09-daily-sweep.md && grep -qF 'plans/board.md' agent-factory/workflows/09-daily-sweep.md && grep -qF 'plans/metrics.md' agent-factory/workflows/09-daily-sweep.md && grep -qF 'memory-bank/60-progress.md' agent-factory/workflows/09-daily-sweep.md && grep -qF 'blocked_escalation_days' agent-factory/workflows/09-daily-sweep.md && echo OK-09; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/09-daily-sweep.md exists with kind: workflow frontmatter (<=3 fields) and all 10 headings in order (V-02/V-03 green for 09).
    - References plans/board.md, plans/metrics.md, memory-bank/60-progress.md, and blocked_escalation_days; names Cycle time and WIP (V-09 green).
    - When to use declares "both" cadences (V-10 portion); no invented metric/column (V-12 green).
  </acceptance_criteria>
  <done>09 authored, both-cadence reconciliation engine, V-09/V-10/V-12 green (BOARD-02 satisfied).</done>
</task>

<task type="auto">
  <name>Task 3: Author 11-retro.md (both cadences)</name>
  <files>agent-factory/workflows/11-retro.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 11-retro.md (§7.12, both; light in lean: When end of sprint / monthly in Kanban; Agent Factory Coach; Steps read plans/metrics.md + board history, identify top 1-3 wastes, write notes, create 1-3 improvement tickets tagged factory; Done when retro-notes written + tickets created) + the 10-section template.
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `11-retro.md` (factory-coach -> agent-factory/handoffs/retro-notes.md; reads plans/metrics.md; tickets -> plans/tickets/ tagged factory; declares both, light in lean) + Shared Patterns S1, S2, S4, S5.
    - `agent-factory/roles/orchestrator.md` (analog). `agent-factory/roles/factory-coach.md` (the role 11 composes). `agent-factory/handoffs/retro-notes.md`, `plans/metrics.md`.
  </read_first>
  <action>
    Create `agent-factory/workflows/11-retro.md` with kind: workflow frontmatter then `# Workflow: Retro` and the 10 sections in order. When to use (clear voice, light wink only here): declare this applies to BOTH cadences - end of sprint in Scrum, monthly (light) in Kanban; selected by the Orchestrator. Agents involved: Factory Coach. Steps (clear voice): read plans/metrics.md + board history; identify the top 1-3 wastes; write agent-factory/handoffs/retro-notes.md; create 1-3 improvement tickets tagged factory into plans/tickets/. Board moves: none new (factory-coach causes no transition); improvement tickets enter Backlog. Handoffs produced: agent-factory/handoffs/retro-notes.md. Trace updates: rows for the new improvement tickets in plans/traceability.md. Metrics emitted: reads/updates plans/metrics.md (name a frozen-9 subset it surfaces, e.g. Throughput / Cycle time / Rework rate) - do not invent. Stop conditions: insufficient metric history to identify a waste -> note and defer. Done condition (reproduce §7.12): retro-notes written + 1-3 improvement tickets created. Declare the literal "both" cadence applicability (light in lean). Keep ~one screen; cite only frozen names.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/11-retro.md && grep -qF 'retro-notes.md' agent-factory/workflows/11-retro.md && grep -qF 'plans/metrics.md' agent-factory/workflows/11-retro.md && grep -qiF 'both' agent-factory/workflows/11-retro.md && echo OK-11; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/11-retro.md exists with kind: workflow frontmatter (<=3 fields) and all 10 headings in order (V-02/V-03 green for 11).
    - Cites agent-factory/handoffs/retro-notes.md and plans/metrics.md; tickets tagged factory -> plans/tickets/; When to use declares "both" cadences (V-10 portion); no invented name (V-12 green).
  </acceptance_criteria>
  <done>11 authored, both-cadence, retro-notes + improvement-ticket producing, V-02/V-03/V-10/V-12 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daily-sweep -> board/trace truth | The sweep is the single reconciliation point keeping board column == ticket status; a weak/missing reconciliation lets the board lie (BOARD-02 / trace integrity) |
| cadence declaration -> Orchestrator selection | 07/09/11 must declare "both" so the Orchestrator fires them in either cadence; a wrong tag skips a ceremony |

Markdown-authoring task; no runtime input/network/auth surface. STRIDE web categories informational/LOW. In-scope threat is process/board-state integrity.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | 09 omitting the board<->ticket-status reconciliation or escalation, letting blockers/WIP-breaches hide | mitigate | 09 Steps reproduce the §7.10 reconciliation + escalate past blocked_escalation_days + WIP-respecting pull; harness V-09 asserts board/metrics/60-progress + blocked_escalation_days references |
| T-04-06-02 | Tampering | 07/09/11 mis-tagged cadence, skipping a ceremony | mitigate | All three declare "both"; harness V-10 asserts the both-cadence declaration and the single-set (no suffix) invariant |
| T-04-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; nothing to audit |
</threat_model>

<verification>
Run `sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh` after each file. V-09 (daily-sweep references), the V-10 both-cadence portions for 07/09/11, and V-02/V-03/V-12 must flip green.
</verification>

<success_criteria>
07/09/11 authored on the 10-section template, expanded from their §7.x one-liners, all declaring both cadences, with 09 the BOARD-02 reconciliation engine. Harness V-09/V-10/V-02/V-03/V-12 green.
</success_criteria>

<output>
Create `.planning/phases/04-workflows-cadence-backpressure/04-06-SUMMARY.md` when done.
</output>

---
phase: 04-workflows-cadence-backpressure
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - agent-factory/workflows/00-bootstrap-greenfield.md
  - agent-factory/workflows/01-bootstrap-brownfield.md
autonomous: true
requirements: [FLOW-01, FLOW-05]
must_haves:
  truths:
    - "00-bootstrap-greenfield seeds AGENTS.md, the memory-bank, the greenfield plan, product/system/architecture handoffs, the board, config, and safe first tickets"
    - "01-bootstrap-brownfield maps an existing repo, seeds AGENTS.md + memory-bank, runs a Security/NFR high-risk scan, and produces safe first tickets"
    - "Both bootstrap workflows leave AGENTS.md command slots as UNKNOWN - verify (filled per-project at runtime, never fabricated)"
  artifacts:
    - path: "agent-factory/workflows/00-bootstrap-greenfield.md"
      provides: "Greenfield bootstrap lifecycle workflow"
      min_lines: 22
    - path: "agent-factory/workflows/01-bootstrap-brownfield.md"
      provides: "Brownfield bootstrap lifecycle workflow"
      min_lines: 22
  key_links:
    - from: "agent-factory/workflows/00-bootstrap-greenfield.md"
      to: "memory-bank/greenfield-plan.md"
      via: "Greenfield Mapper planning output"
      pattern: "greenfield-plan.md"
    - from: "agent-factory/workflows/01-bootstrap-brownfield.md"
      to: "memory-bank/brownfield-map.md"
      via: "Brownfield Mapper map output"
      pattern: "brownfield-map.md"
---

<objective>
Author the two bootstrap lifecycle workflows: `00-bootstrap-greenfield.md` and `01-bootstrap-brownfield.md`. Each reproduces its §7.1/§7.2 `Flow:`/`Done when:` spine on the 10-section template and derives the connective sections from frozen names (D-24). They seed the per-project plane (AGENTS.md, memory-bank, board, config, first tickets) when the factory is first run on a repo - leaving the AGENTS.md command slots as `UNKNOWN - verify` (filled at runtime by the Scribe, never fabricated).

Purpose: Deliver FLOW-01 - the entry points that stand up a project's factory state, greenfield (from an idea) or brownfield (onto an existing repo).
Output: `agent-factory/workflows/00-bootstrap-greenfield.md`, `agent-factory/workflows/01-bootstrap-brownfield.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md
@.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md
@agent-factory/roles/orchestrator.md
@agent-factory/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 00-bootstrap-greenfield.md</name>
  <files>agent-factory/workflows/00-bootstrap-greenfield.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 00-bootstrap-greenfield.md (§7.1 Flow `idea -> Orchestrator -> Greenfield Mapper -> AGENTS.md Scribe -> BA/PM -> System Analyst -> Architect/Design -> initial tickets`; Done when: AGENTS.md, memory-bank, greenfield-plan, product/system/architecture handoffs, first tickets, board seeded, config present) + Open Question 1 (leave plans/initial-plan.md a thin stub; name memory-bank/greenfield-plan.md as the planning output) + Open Question 2 (echo README bootstrap phrasing in the When-to-use opener).
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `00-bootstrap-greenfield.md` + Shared Patterns S1, S2, S4, S5.
    - `agent-factory/roles/orchestrator.md` (analog). `agent-factory/roles/greenfield-mapper.md` (-> memory-bank/greenfield-plan.md), `agent-factory/roles/agents-md-scribe.md` (-> root AGENTS.md), `agent-factory/roles/ba-pm.md` (Backlog -> Ready -> product-handoff.md), `agent-factory/roles/system-analyst.md` (In Analysis exit -> system-handoff.md), `agent-factory/roles/architect-design.md` (In Design exit -> architecture-handoff.md + ADRs -> memory-bank/50-decisions/ + seeds plans/nfr-catalog.md).
    - `agent-factory/README.md` - the bootstrap copy-paste prompt phrasing to echo. Root `AGENTS.md` (command slots stay UNKNOWN - verify). `plans/board.md`, `agent-factory/config/factory.config.json` (seeded/present).
  </read_first>
  <action>
    Create `agent-factory/workflows/00-bootstrap-greenfield.md` with kind: workflow frontmatter (<=3 fields; optionally order: 0 / cadence: both) then `# Workflow: Bootstrap greenfield` and the 10 sections in order. When to use: reproduce the §7.1 Flow and echo the README's greenfield bootstrap prompt phrasing (a light grug wink is allowed only here). Agents involved: Orchestrator, Greenfield Mapper, AGENTS.md Scribe, BA/PM, System Analyst, Architect/Design. Steps (clear voice, composing each frozen role's behavior, D-23): Greenfield Mapper shapes the idea into memory-bank/greenfield-plan.md; AGENTS.md Scribe writes root AGENTS.md (command slots left UNKNOWN - verify, filled per-project later); BA/PM defines product (agent-factory/handoffs/product-handoff.md; epics/tickets -> plans/epics|features|tickets/); System Analyst (In Analysis exit -> agent-factory/handoffs/system-handoff.md); Architect/Design (In Design exit -> agent-factory/handoffs/architecture-handoff.md + ADRs into memory-bank/50-decisions/ + seeds plans/nfr-catalog.md); seed plans/board.md and confirm agent-factory/config/factory.config.json present. Board moves: seeds plans/board.md; Backlog -> Ready (ba-pm); In Analysis / In Design exits. Handoffs produced: product-handoff.md, system-handoff.md, architecture-handoff.md (all under agent-factory/handoffs/). Trace updates: seed plans/traceability.md rows for the first tickets. Metrics emitted: none beyond seeding (or a frozen-9 subset if applicable) - do not invent. Stop conditions: idea too vague to map -> stop and request clarity. Done condition (reproduce §7.1 Done when): AGENTS.md, memory-bank, greenfield-plan, product/system/architecture handoffs, first tickets exist; board seeded; config present. Leave plans/initial-plan.md a thin stub (do not over-populate); name memory-bank/greenfield-plan.md as the planning output. Keep ~one screen; cite only frozen names; never fabricate an AGENTS.md command.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/00-bootstrap-greenfield.md && grep -qF 'greenfield-plan.md' agent-factory/workflows/00-bootstrap-greenfield.md && grep -qF 'AGENTS.md' agent-factory/workflows/00-bootstrap-greenfield.md && grep -qF 'kind: workflow' agent-factory/workflows/00-bootstrap-greenfield.md && echo OK-00; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/00-bootstrap-greenfield.md exists with kind: workflow frontmatter (<=3 fields) and all 10 headings in order (V-02/V-03 green for 00).
    - Cites memory-bank/greenfield-plan.md, root AGENTS.md, and the frozen handoffs product-handoff.md / system-handoff.md / architecture-handoff.md; no plans/*-handoff path; no invented name (V-12 green).
    - Contains no fabricated gate command; AGENTS.md command slots are left UNKNOWN - verify (filled at runtime).
    - Done condition reproduces the §7.1 Done-when (board seeded, config present, first tickets exist).
  </acceptance_criteria>
  <done>00 authored, seeds the greenfield plane, frozen-name-only, fabrication-free, V-02/V-03/V-12 green.</done>
</task>

<task type="auto">
  <name>Task 2: Author 01-bootstrap-brownfield.md</name>
  <files>agent-factory/workflows/01-bootstrap-brownfield.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 01-bootstrap-brownfield.md (§7.2 Flow `existing repo -> Orchestrator -> Brownfield Mapper -> AGENTS.md Scribe -> Architect/Design review -> Security/NFR high-risk scan -> safe first tickets`; Done when: AGENTS.md, brownfield-map, memory-bank updated, known commands + risks documented, safe first tickets, board seeded, config present).
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `01-bootstrap-brownfield.md` + Shared Patterns S1, S2, S4.
    - `agent-factory/roles/orchestrator.md` (analog). `agent-factory/roles/brownfield-mapper.md` (-> memory-bank/brownfield-map.md), `agent-factory/roles/agents-md-scribe.md` (-> root AGENTS.md), `agent-factory/roles/architect-design.md` (review), `agent-factory/roles/security-nfr.md` (In Security/NFR exit -> security-nfr-handoff.md; result PASS | PASS_WITH_RISKS | BLOCKED).
    - Root `AGENTS.md` (command slots stay UNKNOWN - verify - the brownfield map documents known commands but the kit file itself keeps the slots UNKNOWN until the Scribe fills them per-project). `plans/board.md`.
  </read_first>
  <action>
    Create `agent-factory/workflows/01-bootstrap-brownfield.md` with kind: workflow frontmatter then `# Workflow: Bootstrap brownfield` and the 10 sections in order. When to use: reproduce the §7.2 Flow and echo the README's brownfield bootstrap phrasing (light wink only here). Agents involved: Orchestrator, Brownfield Mapper, AGENTS.md Scribe, Architect/Design, Security/NFR. Steps (clear voice): Brownfield Mapper maps the existing repo into memory-bank/brownfield-map.md (documenting known commands + structure); AGENTS.md Scribe writes/updates root AGENTS.md (command slots stay UNKNOWN - verify until verified per-project); Architect/Design reviews structure; Security/NFR runs a high-risk scan (In Security/NFR exit -> agent-factory/handoffs/security-nfr-handoff.md, result one of PASS | PASS_WITH_RISKS | BLOCKED); produce safe first tickets. Board moves: seeds plans/board.md; In Security/NFR exit. Handoffs produced: agent-factory/handoffs/security-nfr-handoff.md. Trace updates: seed plans/traceability.md rows for safe first tickets. Metrics emitted: none beyond seeding (or a frozen-9 subset) - do not invent. Stop conditions: Security/NFR returns BLOCKED on a high-risk finding -> stop. Done condition (reproduce §7.2 Done when): AGENTS.md, brownfield-map, memory-bank updated, known commands + risks documented, safe first tickets, board seeded, config present. Keep ~one screen; cite only frozen names; never fabricate an AGENTS.md command.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/01-bootstrap-brownfield.md && grep -qF 'brownfield-map.md' agent-factory/workflows/01-bootstrap-brownfield.md && grep -qF 'security-nfr-handoff.md' agent-factory/workflows/01-bootstrap-brownfield.md && echo OK-01; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/01-bootstrap-brownfield.md exists with kind: workflow frontmatter (<=3 fields) and all 10 headings in order (V-02/V-03 green for 01).
    - Cites memory-bank/brownfield-map.md, root AGENTS.md, and agent-factory/handoffs/security-nfr-handoff.md; names the PASS | PASS_WITH_RISKS | BLOCKED result; no plans/*-handoff path; no invented name (V-12 green).
    - Contains no fabricated gate command; AGENTS.md command slots remain UNKNOWN - verify.
    - Done condition reproduces the §7.2 Done-when.
  </acceptance_criteria>
  <done>01 authored, maps + scans an existing repo, frozen-name-only, fabrication-free, V-02/V-03/V-12 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bootstrap text -> AGENTS.md command slots | Bootstrap is exactly where a fabricated "real" command would be tempting to bake in; the kit must keep slots UNKNOWN - verify until a human/Scribe verifies them per-project |
| brownfield scan -> safety | The Security/NFR high-risk scan is the first safety gate on an unknown repo; a missing/weak scan step lets risk through into the first tickets |

Markdown-authoring task; no runtime input/network/auth surface. Conventional STRIDE web categories are informational/LOW. In-scope threats are no-fabrication and process-safety.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering / Repudiation | Bootstrap baking a fabricated AGENTS.md gate command into the kit | mitigate | Slots stay UNKNOWN - verify; filled per-project by the Scribe at runtime; harness V-07/V-12 and the AGENTS.md UNKNOWN-verify discipline guard this |
| T-04-04-02 | Information Disclosure | Brownfield workflow omitting the Security/NFR high-risk scan, missing a risk in an unknown repo | mitigate | 01 Steps reproduce the §7.2 Security/NFR high-risk scan -> security-nfr-handoff.md with PASS/PASS_WITH_RISKS/BLOCKED; Done condition requires risks documented |
| T-04-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; nothing to audit |
</threat_model>

<verification>
Run `sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh` after each file. V-02/V-03/V-12 for 00 and 01 must flip green. No fabricated command may appear in either file.
</verification>

<success_criteria>
00/01 authored on the 10-section template, reproducing their §7.1/§7.2 spines, seeding the per-project plane with frozen names only and no fabricated commands. Harness V-02/V-03/V-12 green for both.
</success_criteria>

<output>
Create `.planning/phases/04-workflows-cadence-backpressure/04-04-SUMMARY.md` when done.
</output>

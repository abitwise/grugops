---
phase: 04-workflows-cadence-backpressure
plan: 05
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - agent-factory/workflows/08-sprint-planning.md
  - agent-factory/workflows/10-sprint-review.md
autonomous: true
requirements: [FLOW-03, FLOW-05, BOARD-03]
must_haves:
  truths:
    - "08-sprint-planning is tagged cadence=scrum, writes plans/sprints/SPRINT-xx.md, and names the Goal/Committed/Velocity/Burndown fields"
    - "10-sprint-review is tagged cadence=scrum and appends review notes to the same SPRINT-xx.md"
    - "Both are a SINGLE config-gated set member - no kanban/scrum filename suffix, no duplicate file (D-25)"
  artifacts:
    - path: "agent-factory/workflows/08-sprint-planning.md"
      provides: "Scrum sprint-planning ceremony; writes SPRINT-xx.md"
      contains: "cadence=scrum"
      min_lines: 22
    - path: "agent-factory/workflows/10-sprint-review.md"
      provides: "Scrum sprint-review ceremony; appends to SPRINT-xx.md"
      contains: "cadence=scrum"
      min_lines: 20
  key_links:
    - from: "agent-factory/workflows/08-sprint-planning.md"
      to: "plans/sprints/SPRINT-xx.md"
      via: "sprint file written at planning"
      pattern: "SPRINT-xx.md"
    - from: "agent-factory/workflows/10-sprint-review.md"
      to: "plans/sprints/SPRINT-xx.md"
      via: "review notes appended to the sprint file"
      pattern: "SPRINT-xx.md"
---

<objective>
Author the two scrum-only ceremony workflows: `08-sprint-planning.md` and `10-sprint-review.md`. `08` reproduces the §7.9 spine and the §6.2 `SPRINT-xx.md` format (Goal/Dates/Capacity/Committed/Added mid-sprint/Carried out/Velocity/Burndown/Notes for retro); `10` reproduces §7.11 and appends review notes to the same sprint file. Both declare `cadence=scrum` in `When to use` (D-25), are members of the ONE config-gated set (no filename suffix), and are expanded from their §7 one-liners into all 10 template sections (FLOW-05).

Purpose: Deliver the Scrum half of the dual-cadence requirement (BOARD-03) - time-boxed sprints with a real sprint file and velocity/burndown, selectable by `config.cadence`.
Output: `agent-factory/workflows/08-sprint-planning.md`, `agent-factory/workflows/10-sprint-review.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md
@.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md
@agent-factory/roles/orchestrator.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 08-sprint-planning.md (scrum-only, SPRINT format)</name>
  <files>agent-factory/workflows/08-sprint-planning.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 08-sprint-planning.md (§7.9: cadence=scrum; When start of sprint; Agents Orchestrator + BA/PM; Steps set one-sentence goal, pull from Ready by priority up to capacity, confirm each item Ready, write the sprint file; Stop if Ready too thin) + section "The §6.2 SPRINT-xx.md Format" (reproduce the field list VERBATIM) + "The 10-Section v2 Workflow Template" (expand the one-liner into all 10 sections).
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `08-sprint-planning.md - SPECIAL CASE` (must contain cadence=scrum; reference plans/sprints/SPRINT-xx.md; name Goal/Committed/Velocity/Burndown; Stop: Ready thin -> run 07 first) + Shared Patterns S1, S2, S5, S8.
    - `agent-factory/roles/orchestrator.md` (analog + Ready column, capacity, SPLIT_REQUIRED). `agent-factory/roles/ba-pm.md`. `agent-factory/handoffs/sprint-plan.md` (optional one-off packet, §8.5).
    - `agent-factory/config/factory.config.json` - cadence, sprint_length_days (10). `plans/sprints/` (the dir 08 writes SPRINT-xx.md into; ships .gitkeep). `plans/metrics.md` (Velocity, scrum).
  </read_first>
  <action>
    Create `agent-factory/workflows/08-sprint-planning.md` with kind: workflow frontmatter (<=3 fields; include cadence: scrum if used as a frontmatter field, but the harness-checked token cadence=scrum must appear in the When-to-use body) then `# Workflow: Sprint planning` and the 10 sections in order. When to use (clear voice, light wink allowed only here): declare this is a `cadence=scrum` ceremony, run at the start of a sprint; the Orchestrator selects it only when config.cadence=scrum. Agents involved: Orchestrator + BA/PM. Inputs required: a stocked Ready column, capacity from config (sprint_length_days). Steps (clear voice): set a one-sentence sprint goal; pull from the Ready column by priority up to capacity; confirm each item is Ready (DoR); write plans/sprints/SPRINT-xx.md using the §6.2 format - reproduce the field list (Goal / Dates / Capacity / Committed / Added mid-sprint / Carried out / Velocity / Burndown / Notes for retro). Board moves: items move out of Ready into the sprint commitment (name the frozen Ready column). Handoffs produced: optionally agent-factory/handoffs/sprint-plan.md (the §8.5 one-off packet); the primary output is the sprint file. Trace updates: none new beyond the committed tickets' existing rows in plans/traceability.md. Metrics emitted: Velocity (scrum, from the frozen 9). Stop conditions: if Ready is too thin to fill capacity, STOP and run 07-backlog-refinement first. Done condition (reproduce §7.9): plans/sprints/SPRINT-xx.md written. The file MUST contain the literal token cadence=scrum, reference plans/sprints/SPRINT-xx.md, and name Goal / Committed / Velocity / Burndown. Keep ~one screen; cite only frozen names.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/08-sprint-planning.md && grep -qF 'cadence=scrum' agent-factory/workflows/08-sprint-planning.md && grep -qF 'SPRINT-xx.md' agent-factory/workflows/08-sprint-planning.md && grep -qF 'Velocity' agent-factory/workflows/08-sprint-planning.md && grep -qF 'Burndown' agent-factory/workflows/08-sprint-planning.md && echo OK-08; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/08-sprint-planning.md exists with kind: workflow frontmatter (<=3 fields) and all 10 headings in order (V-02/V-03 green for 08).
    - Contains the literal token cadence=scrum; references plans/sprints/SPRINT-xx.md; names Goal/Committed/Velocity/Burndown (V-08 green).
    - Filename has NO cadence suffix (single config-gated set, D-25/V-10).
    - Stop condition routes a thin Ready column to 07-backlog-refinement.
  </acceptance_criteria>
  <done>08 authored, scrum-tagged, writes SPRINT-xx.md with the §6.2 fields, V-08 green.</done>
</task>

<task type="auto">
  <name>Task 2: Author 10-sprint-review.md (scrum-only, appends to SPRINT-xx.md)</name>
  <files>agent-factory/workflows/10-sprint-review.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 10-sprint-review.md (§7.11: cadence=scrum; When end of sprint; Agents UAT Planner + BA/PM (+ QE); Steps assemble what reached Done, validate vs acceptance, draft demo/release notes, list carry-over with reasons; Done when review notes appended to the sprint file) + the 10-section template.
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `10-sprint-review.md` (appends to SPRINT-xx.md; must contain cadence=scrum; emits Velocity) + Shared Patterns S1, S2, S5.
    - `agent-factory/roles/orchestrator.md` (analog). `agent-factory/roles/uat-planner.md`, `agent-factory/roles/ba-pm.md`, `agent-factory/roles/qe-e2e.md`.
    - `plans/sprints/SPRINT-xx.md` (the file 10 appends to - read 08's output contract). `plans/metrics.md` (Velocity).
  </read_first>
  <action>
    Create `agent-factory/workflows/10-sprint-review.md` with kind: workflow frontmatter then `# Workflow: Sprint review` and the 10 sections in order. When to use (clear voice, light wink only here): declare this is a `cadence=scrum` ceremony, run at the end of a sprint; selected only when config.cadence=scrum. Agents involved: UAT Planner + BA/PM (+ QE/E2E). Inputs required: the active plans/sprints/SPRINT-xx.md, items that reached Done. Steps (clear voice): assemble what reached the Done column, validate against acceptance criteria, draft demo/release notes, list carry-over with reasons; append the review notes to plans/sprints/SPRINT-xx.md. Board moves: confirms items in the frozen Done column (no new transition). Handoffs produced: none new (the sprint file is the artifact). Trace updates: confirm the reviewed tickets' rows in plans/traceability.md are complete. Metrics emitted: Velocity (scrum, frozen 9). Stop conditions: a committed item's acceptance is ambiguous -> flag as carry-over with reason, do not mark Done. Done condition (reproduce §7.11): review notes appended to plans/sprints/SPRINT-xx.md. MUST contain the literal token cadence=scrum and reference SPRINT-xx.md. Keep ~one screen; cite only frozen names.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/10-sprint-review.md && grep -qF 'cadence=scrum' agent-factory/workflows/10-sprint-review.md && grep -qF 'SPRINT-xx.md' agent-factory/workflows/10-sprint-review.md && grep -qF 'Velocity' agent-factory/workflows/10-sprint-review.md && echo OK-10; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/10-sprint-review.md exists with kind: workflow frontmatter (<=3 fields) and all 10 headings in order (V-02/V-03 green for 10).
    - Contains the literal token cadence=scrum and references plans/sprints/SPRINT-xx.md (appends to it); emits Velocity (V-08/V-10 portion green).
    - Filename has NO cadence suffix (D-25/V-10).
    - No invented handoff/column/metric (V-12 green).
  </acceptance_criteria>
  <done>10 authored, scrum-tagged, appends review notes to SPRINT-xx.md, V-08/V-10/V-12 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cadence declaration -> Orchestrator selection | 08/10 must self-declare cadence=scrum so the Orchestrator (reading config.cadence) fires them only in scrum; a missing/wrong tag breaks cadence gating |
| single-set discipline | A cadence-suffixed duplicate file would fork the workflow set and create drift (D-25) |

Markdown-authoring task; no runtime input/network/auth surface. STRIDE web categories informational/LOW. In-scope threat is process/cadence-config integrity.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | 08/10 missing the cadence=scrum tag, firing in kanban | mitigate | Both declare cadence=scrum in When to use; harness V-08/V-10 assert the token present |
| T-04-05-02 | Tampering | A cadence-suffixed duplicate workflow file (08-sprint-planning-scrum.md) forking the set | mitigate | D-25 single set; harness V-10 asserts no cadence suffix and exactly 14 files |
| T-04-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; nothing to audit |
</threat_model>

<verification>
Run `sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh` after each file. V-08 (08 scrum + SPRINT format), the V-10 cadence-tag portion for 08/10, and V-02/V-03/V-12 must flip green.
</verification>

<success_criteria>
08/10 authored as scrum-only single-set members, 08 writing the §6.2 SPRINT-xx.md format and 10 appending review notes, both tagged cadence=scrum with no filename suffix. Harness V-08/V-10/V-02/V-03/V-12 green.
</success_criteria>

<output>
Create `.planning/phases/04-workflows-cadence-backpressure/04-05-SUMMARY.md` when done.
</output>

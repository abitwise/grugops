---
phase: 04-workflows-cadence-backpressure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/workflows/05-pr-quality-gate.md
  - agent-factory/workflows/04-ticket-to-pr.md
autonomous: true
requirements: [FLOW-02, FLOW-05, GATE-01, SAFE-01]
must_haves:
  truths:
    - "The §14 backpressure loop (prefetch -> branch -> gate -> bounded self-fix -> terminal result -> human review/merge/deploy) appears exactly ONCE, in 05-pr-quality-gate.md"
    - "05 records gate commands as `UNKNOWN - verify` pulled from AGENTS.md - never a fabricated real command"
    - "04-ticket-to-pr.md references 05 for the gate and does NOT restate the loop"
    - "04 honors autonomy=pr (branch + open PR, never merge); 05 emits a recommendation a human reviews, never auto-merges"
  artifacts:
    - path: "agent-factory/workflows/05-pr-quality-gate.md"
      provides: "Single-source home of the §14 backpressure quality gate; the terminal-result contract"
      contains: "READY_FOR_HUMAN_REVIEW"
      min_lines: 30
    - path: "agent-factory/workflows/04-ticket-to-pr.md"
      provides: "Ticket->PR lifecycle workflow that sequences the dev path and references 05 for the gate"
      contains: "05-pr-quality-gate.md"
      min_lines: 25
  key_links:
    - from: "agent-factory/workflows/04-ticket-to-pr.md"
      to: "agent-factory/workflows/05-pr-quality-gate.md"
      via: "reference to the gate loop (D-26, no restatement)"
      pattern: "05-pr-quality-gate.md"
    - from: "agent-factory/workflows/05-pr-quality-gate.md"
      to: "AGENTS.md"
      via: "gate commands pulled at runtime, recorded UNKNOWN - verify"
      pattern: "UNKNOWN - verify"
---

<objective>
Author the two gate-coupled lifecycle workflows: `05-pr-quality-gate.md` - the SINGLE-SOURCE home of the §14 backpressure loop (D-26) - and `04-ticket-to-pr.md`, which sequences the dev board path and REFERENCES `05` for the gate instead of restating it. These two are planned together so the single-source discipline (loop lives only in `05`; `04` points at it) is authored and verified in one pass, with `05` written first so `04`'s reference resolves.

Purpose: Deliver the bounded quality gate (GATE-01) once, with no drift, and the ticket->PR lifecycle (FLOW-02) that consumes it, both rendering "humans decide, agents execute" as clear-voice prose (SAFE-01).
Output: `agent-factory/workflows/05-pr-quality-gate.md`, `agent-factory/workflows/04-ticket-to-pr.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md
@.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md
@agent-factory/roles/orchestrator.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 05-pr-quality-gate.md (single-source §14 loop)</name>
  <files>agent-factory/workflows/05-pr-quality-gate.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` section "The §14 Backpressure Loop" - the 6-step loop to reproduce faithfully, the determinism rules (UNKNOWN - verify / no-fake, config knobs), the four quality.* knob values, the three terminal tokens, the six-verb gate order. Also the spine-table row for 05-pr-quality-gate.md and section "The 10-Section v2 Workflow Template" (the 10 headings in order).
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `05-pr-quality-gate.md` plus "Shared Patterns" S1 (kind: workflow frontmatter), S2 (10 headings in order), S6 (clear voice in gate), S7 (SAFE-01 prose), S8 (config-key citation).
    - `agent-factory/roles/orchestrator.md` - the structural analog (minimal kind: frontmatter, ordered ## sections, clear-voice hard limits); the recommendation-only / never-auto-merge posture.
    - `agent-factory/roles/qe-e2e.md`, `agent-factory/roles/security-nfr.md`, `agent-factory/roles/architect-design.md` - the roles 05 composes and the handoffs they emit (qe-handoff.md, security-nfr-handoff.md, architecture-handoff.md).
    - `agent-factory/config/factory.config.json` - the verified quality block (self_fix_attempts:2, coverage_threshold:0.8, mandatory_gates:["lint","typecheck","unit","build"], e2e_when:"ui-or-critical-path") to cite, never redefine.
    - `agent-factory/checklists/pr-review-checklist.md` - cited by name. Root `AGENTS.md` ## Commands slots - the UNKNOWN - verify gate-command source (do NOT copy real commands; there are none).
  </read_first>
  <action>
    Create `agent-factory/workflows/05-pr-quality-gate.md`. Open with minimal `kind: workflow` frontmatter (2-3 fields total; kind: workflow plus optionally order: 5 / cadence: both per D-27 discretion) then the H1 `# Workflow: PR quality gate`. Author the 10 sections IN ORDER: When to use / Agents involved / Inputs required / Steps / Board moves / Handoffs produced / Trace updates / Metrics emitted / Stop conditions / Done condition. Reproduce the §7.6 Flow line (implementation -> QE/E2E -> Security/NFR -> Architect/Design if structure changed -> Orchestrator recommendation) into When to use. In Steps, reproduce the §14 6-step loop faithfully in CLEAR voice: (1) deterministic prefetch (Orchestrator gathers ticket / handoffs / AGENTS.md commands / relevant files / prior ADRs before code is written); (2) implement on a branch (autonomy=branch|pr); (3) run the gate install -> lint -> typecheck -> unit -> build -> e2e with commands pulled from AGENTS.md - state explicitly that a missing command is recorded UNKNOWN - verify, never faked; (4) bounded self-fix using factory.config.json#quality.self_fix_attempts (default 2, "two rounds then human"), then STOP and hand to a human; (5) result is one of READY_FOR_HUMAN_REVIEW | BLOCKED_NEEDS_FIX | SPLIT_REQUIRED; (6) a human reviews the PR, a human merges, a human (or human-confirmed pipeline) deploys. Cite the other quality.* knobs (coverage_threshold, mandatory_gates, e2e_when) and agent-factory/checklists/pr-review-checklist.md. Board moves: the gate within In Review (-> In Security/NFR) - name those frozen columns. Handoffs produced: name qe-handoff.md, security-nfr-handoff.md, and architecture-handoff.md (if structure changed) under agent-factory/handoffs/. Trace updates: append Tests/Code links into plans/traceability.md. Metrics emitted: Gate pass rate (frozen 9-metric list). Stop conditions (clear voice): self-fix budget exhausted -> BLOCKED_NEEDS_FIX; ticket too large -> SPLIT_REQUIRED. Done condition (reproduce §7.6): result is one of the three terminal tokens. SAFE-01 (clear voice): 05 emits a RECOMMENDATION a human reviews - it NEVER auto-merges. A light grug wink is allowed ONLY in the When to use opener; the gate/stop/safety prose stays clear voice. Keep to ~one screen; cite only frozen names - invent no command, column, handoff, or metric.
  </action>
  <verify>
    <automated>sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh; printf '%s ' $(grep -lF READY_FOR_HUMAN_REVIEW agent-factory/workflows/*.md) | grep -q '05-pr-quality-gate.md' && grep -qF 'UNKNOWN - verify' agent-factory/workflows/05-pr-quality-gate.md && echo OK-05</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/05-pr-quality-gate.md exists with kind: workflow frontmatter (<=3 fields) and all 10 template headings in order (harness V-02/V-03 green for 05).
    - 05 is the ONLY workflow file containing READY_FOR_HUMAN_REVIEW and contains all three terminal tokens, the six gate verbs (install/lint/typecheck/unit/build/e2e), and self_fix_attempts (V-05 green).
    - 05 contains UNKNOWN - verify and NO hard-coded real gate command (no npm test / eslint / tsc) (V-07 green).
    - 05 contains "recommendation" and human-review language; it never describes auto-merge (SAFE-01 portion for 05).
    - Gate/Stop/safety prose is clear voice; any grug wink is confined to the When to use opener.
  </acceptance_criteria>
  <done>05 authored as the single-source §14 gate, V-05/V-07 green, recommendation-only, fabrication-free.</done>
</task>

<task type="auto">
  <name>Task 2: Author 04-ticket-to-pr.md (references 05, never restates the loop)</name>
  <files>agent-factory/workflows/04-ticket-to-pr.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 04-ticket-to-pr.md (the §7.5 Flow, the Board path, the composed roles/handoffs, the D-26 reference rule, the SAFE-01 honor-autonomy=pr rule).
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `04-ticket-to-pr.md - SPECIAL CASE` (the explicit "references 05, MUST NOT restate the loop; harness asserts 04 contains 05-pr-quality-gate.md and NOT READY_FOR_HUMAN_REVIEW" rule) + Pattern S3 (board-path sequencing), S7 (SAFE-01).
    - `agent-factory/workflows/05-pr-quality-gate.md` (authored in Task 1) - the file 04 references; read it to cite the exact filename, NOT to copy its loop body.
    - `agent-factory/roles/orchestrator.md` (DoR gate + Ready for Dev -> In Development), `agent-factory/roles/software-engineer.md` (In Development -> In Review -> implementation-handoff.md), `agent-factory/roles/qe-e2e.md` (qe-handoff.md), `agent-factory/roles/security-nfr.md` (security-nfr-handoff.md).
    - `agent-factory/checklists/definition-of-ready.md` (the DoR gate cited before pulling). `agent-factory/config/factory.config.json` - autonomy key.
  </read_first>
  <action>
    Create `agent-factory/workflows/04-ticket-to-pr.md` with kind: workflow frontmatter (<=3 fields; optionally order: 4 / cadence: both) then the H1 `# Workflow: Ticket to PR` and the 10 sections in order. Reproduce the §7.5 Flow (ticket -> Orchestrator readiness check -> Software Engineer -> QE/E2E -> Security/NFR if triggered -> final implementation packet) into When to use. Steps (clear voice, composing frozen role behavior, D-23): Orchestrator readiness check against agent-factory/checklists/definition-of-ready.md; pull into dev; Software Engineer implements on a branch; then "run the quality gate per 05-pr-quality-gate.md" - a REFERENCE only, do NOT restate the loop, the six verbs, or the terminal tokens (D-26); QE/E2E then Security/NFR if triggered. Board moves: reproduce the §7.5 full path Ready for Dev -> In Development -> In Review (-> In Security/NFR) (frozen columns only). Handoffs produced: implementation-handoff.md, qe-handoff.md, security-nfr-handoff.md (if triggered) under agent-factory/handoffs/. Trace updates: append Code/Tests links into plans/traceability.md. Metrics emitted: a subset of the frozen 9 (e.g. Cycle time, WIP). Stop conditions: not Ready (DoR fails) -> stop and name the missing input; XL -> SPLIT_REQUIRED back to BA/PM. Done condition (reproduce §7.5): code changed per autonomy, tests added, commands run, implementation + QE (+ security-nfr) handoffs written, trace updated. SAFE-01 (clear voice): honors autonomy=pr - branch + open PR, never merge; cite the literal token `autonomy=pr` and the literal phrase "never merge". Keep to ~one screen. CRITICAL: 04 MUST contain the literal string 05-pr-quality-gate.md and MUST NOT contain READY_FOR_HUMAN_REVIEW or restate the gate loop.
  </action>
  <verify>
    <automated>grep -qF '05-pr-quality-gate.md' agent-factory/workflows/04-ticket-to-pr.md && ! grep -qF 'READY_FOR_HUMAN_REVIEW' agent-factory/workflows/04-ticket-to-pr.md && grep -qF 'autonomy=pr' agent-factory/workflows/04-ticket-to-pr.md && grep -qiF 'never merge' agent-factory/workflows/04-ticket-to-pr.md && echo OK-04; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/04-ticket-to-pr.md exists with kind: workflow frontmatter (<=3 fields) and all 10 template headings in order (V-02/V-03 green for 04).
    - 04 contains the literal string 05-pr-quality-gate.md and does NOT contain READY_FOR_HUMAN_REVIEW (V-06 green - the loop is not restated).
    - 04 contains autonomy=pr and "never merge" (V-11 portion for 04).
    - Board moves reproduces the frozen path Ready for Dev -> In Development -> In Review (-> In Security/NFR) using only the 13 frozen columns; no invented column (V-12 drift guard green).
    - No plans/*-handoff path appears; every cited handoff is in the frozen 16-file list (V-12 green).
  </acceptance_criteria>
  <done>04 authored, references 05, never restates the loop (V-06 green), honors autonomy=pr / never merge (SAFE-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow text -> agent runtime behavior | The gate prose and the SAFE-01 prose are the only thing telling an agent to stop at the human gate this phase (the mechanical hook is Phase 5); imprecise/missing prose is the real risk |
| 05 (loop) <-> 04 (reference) | Single-source boundary - duplicating the loop into 04 creates drift between two copies of the gate contract |

Markdown-authoring task; no runtime input, network, or auth surface. Conventional STRIDE web categories are informational/LOW. In-scope threats are process-safety and kit-text integrity.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering / Repudiation | 05 hard-coding a plausible real gate command (e.g. `npm test`) instead of `UNKNOWN - verify` | mitigate | Commands stay `UNKNOWN - verify`, pulled from AGENTS.md at runtime; harness V-07 asserts `UNKNOWN - verify` present and no real command string |
| T-04-02-02 | Elevation of Privilege | 04/05 missing the human-confirm prose, allowing an agent to merge a protected branch | mitigate | 04 cites `autonomy=pr` + "never merge"; 05 emits a recommendation only; harness V-11 asserts the safety prose; mechanical enforcement deferred to Phase 5 (SAFE-02) by design |
| T-04-02-03 | Tampering | Loop duplicated into 04, drifting from 05 over edits | mitigate | D-26 single-source; 04 references `05-pr-quality-gate.md`; harness V-06 asserts 04 does NOT contain `READY_FOR_HUMAN_REVIEW` |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; nothing to audit |
</threat_model>

<verification>
Run `sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh` after each task. Checks V-05, V-06, V-07, plus the V-02/V-03 section/frontmatter checks for 04 and 05, and the V-11/V-12 portions for these two files must flip green. The single-source assertion (`READY_FOR_HUMAN_REVIEW` only in 05) is the load-bearing check.
</verification>

<success_criteria>
05 is the single-source §14 gate (fabrication-free, recommendation-only); 04 references it and never restates the loop, honors autonomy=pr / never merge. Harness V-05/V-06/V-07 green; V-02/V-03/V-11/V-12 green for both files.
</success_criteria>

<output>
Create `.planning/phases/04-workflows-cadence-backpressure/04-02-SUMMARY.md` when done.
</output>

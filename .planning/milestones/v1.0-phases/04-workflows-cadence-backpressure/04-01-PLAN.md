---
phase: 04-workflows-cadence-backpressure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-workflows-cadence-backpressure/check-structure.sh
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03, FLOW-04, FLOW-05, BOARD-02, BOARD-03, GATE-01, SAFE-01]
must_haves:
  truths:
    - "Running `sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh` from repo root exits non-zero while workflow files are absent (ships RED) and exits 0 only when all 14 files + every V-01..V-13 invariant hold"
    - "The harness encodes all 13 structural checks V-01 through V-13 from 04-VALIDATION.md"
    - "The harness fabricates nothing — it references no runtime Node validator command, only POSIX sh + grep/wc/test"
  artifacts:
    - path: ".planning/phases/04-workflows-cadence-backpressure/check-structure.sh"
      provides: "The Wave-0 structural acceptance harness (V-01..V-13) for all 14 workflow files"
      contains: "READY_FOR_HUMAN_REVIEW"
      min_lines: 120
  key_links:
    - from: ".planning/phases/04-workflows-cadence-backpressure/check-structure.sh"
      to: "agent-factory/workflows/*.md"
      via: "grep/test over the 14 workflow files"
      pattern: "agent-factory/workflows"
    - from: ".planning/phases/04-workflows-cadence-backpressure/check-structure.sh"
      to: "agent-factory/roles/orchestrator.md"
      via: "cross-file routing-table match (V-04)"
      pattern: "orchestrator.md"
---

<objective>
Author the Phase-4 Wave-0 structural validation harness `check-structure.sh` — the runnable acceptance gate for all 14 workflow files. It ships RED (every check fails while `agent-factory/workflows/` holds only `.gitkeep`) and goes green file-by-file as later waves land each workflow. It encodes the 13 checks V-01..V-13 defined in `04-VALIDATION.md`, mirroring the proven Phase-3 precedent exactly.

Purpose: Establish the deterministic, fabrication-free acceptance criteria BEFORE any workflow is authored, so every subsequent plan can re-run the harness after each file and watch its checks flip green. This de-risks the Phase-6 Node validator (VAL-01) and is the Nyquist `<automated>` proof for every other Phase-4 task.
Output: `.planning/phases/04-workflows-cadence-backpressure/check-structure.sh` (POSIX `sh`, executable, exit 0 = all PASS / exit 1 = ≥1 FAIL).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workflows-cadence-backpressure/04-VALIDATION.md
@.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md
@.planning/phases/03-roles-agents-md-substrate/check-structure.sh
@agent-factory/roles/orchestrator.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author check-structure.sh encoding V-01..V-13</name>
  <files>.planning/phases/04-workflows-cadence-backpressure/check-structure.sh</files>
  <read_first>
    - `.planning/phases/03-roles-agents-md-substrate/check-structure.sh` — the EXACT precedent to mirror: `#!/usr/bin/env sh` + `set -eu`, `pass()/fail()` helpers, lettered check blocks, `grep -L`/`grep -c`/`grep -o`/`wc`/`test` only, the grep-gate-hygiene `grep -v '^#'` filter (block d), the frozen-path drift guard (block f), final `ALL CHECKS PASSED` / exit 0 vs `N CHECK(S) FAILED` / exit 1.
    - `.planning/phases/04-workflows-cadence-backpressure/04-VALIDATION.md` — the authoritative V-01..V-13 table (the "Automated Check (grep/wc/test)" column is the spec for each check). Encode all 13.
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` §"Validation Architecture" / §"Frozen-Names Inventory" — the 14 exact filenames, the 10 section headings in order, the 13 board columns, the 16 handoff filenames, the 9 metric names, the three terminal tokens, the six gate verbs, the cadence tags.
    - `agent-factory/roles/orchestrator.md` lines 90–107 — the `Classification | Workflow file` routing table the V-04 cross-file check matches against (the 14 `00-`..`13-` filenames; assert no `14-`).
  </read_first>
  <action>
    Create `.planning/phases/04-workflows-cadence-backpressure/check-structure.sh`. Mirror the Phase-3 harness structure (header comment block explaining it is a markdown-authoring structural suite that ships RED; `set -eu`; `pass()/fail()` helpers incrementing a `FAILS` counter; lettered check blocks; final PASS/FAIL exit). Define `WF_DIR="agent-factory/workflows"`. Encode these 13 checks exactly as `04-VALIDATION.md` specifies:
    - V-01 (FLOW-01/02/03/04): `test -f` for all 14 exact names (`00-bootstrap-greenfield 01-bootstrap-brownfield 02-idea-to-epics 03-epic-to-tickets 04-ticket-to-pr 05-pr-quality-gate 06-uat-pack 07-backlog-refinement 08-sprint-planning 09-daily-sweep 10-sprint-review 11-retro 12-release 13-incident`); assert exactly 14 `*.md` excluding `.gitkeep`; assert NO `14-*.md` exists.
    - V-02 (FLOW-05): per file, `grep -nF` each of the 10 headings (`## When to use`, `## Agents involved`, `## Inputs required`, `## Steps`, `## Board moves`, `## Handoffs produced`, `## Trace updates`, `## Metrics emitted`, `## Stop conditions`, `## Done condition`); assert each present AND line numbers strictly increasing (order).
    - V-03 (FLOW-05/D-27): every file `grep -qF 'kind: workflow'`; assert the frontmatter fence block has ≤3 `^[a-z_]*:` field lines (count between the first two `---` fences).
    - V-04 (D-20): for each `(0[0-9]|1[0-3])-[a-z]` workflow name referenced in `agent-factory/roles/orchestrator.md` (filter comments with `grep -v '^#'` per grep-gate hygiene), assert a matching `agent-factory/workflows/<name>.md` exists; assert no extra/missing.
    - V-05 (GATE-01): `grep -lF 'READY_FOR_HUMAN_REVIEW' agent-factory/workflows/*.md` returns EXACTLY `05-pr-quality-gate.md` (single-source); assert `05` contains all three terminal tokens (`READY_FOR_HUMAN_REVIEW`, `BLOCKED_NEEDS_FIX`, `SPLIT_REQUIRED`), the six gate verbs (`install`,`lint`,`typecheck`,`unit`,`build`,`e2e`), and `self_fix_attempts`.
    - V-06 (GATE-01/D-26): `04-ticket-to-pr.md` contains the literal string `05-pr-quality-gate.md` AND does NOT contain `READY_FOR_HUMAN_REVIEW`.
    - V-07 (GATE-01): `05-pr-quality-gate.md` contains `UNKNOWN - verify` (no hard-coded real command).
    - V-08 (BOARD-03): `08-sprint-planning.md` and `10-sprint-review.md` contain `cadence=scrum`; `08` references `plans/sprints/SPRINT-xx.md` and names `Goal`/`Committed`/`Velocity`/`Burndown`.
    - V-09 (BOARD-02): `09-daily-sweep.md` references `plans/board.md`, `plans/metrics.md`, `memory-bank/60-progress.md`, and `blocked_escalation_days`; names `Cycle time`/`WIP`.
    - V-10 (FLOW-03): `08`,`10` carry `cadence=scrum`; `07`,`09`,`11` declare both cadences (grep for `both`); assert NO filename contains a cadence suffix (no `*-kanban.md`/`*-scrum.md`); assert exactly 14 files (single set, D-25).
    - V-11 (SAFE-01): `04` contains `autonomy=pr` and `never merge`; `05` contains `recommendation` and human-review language (no auto-merge); `12` contains `named human`, `human-confirmed`, and `production_requires_human_confirmation`.
    - V-12 (D-24 drift guard): NO workflow file matches `plans/.*-handoff` (real handoffs live under `agent-factory/handoffs/`); optionally assert every cited `*-handoff.md` token is in the frozen 16-file list. Mirror Phase-3 block (f).
    - V-13 (FLOW-04): `13-incident.md` contains `incident-postmortem.md` and `blameless` (or `never blames`).
    Use only POSIX `sh` constructs already proven in the Phase-3 harness. Apply grep-gate hygiene (`grep -v '^#'`) anywhere a count could be self-invalidated by header prose. Do NOT reference any Node validator command (`node scripts/validate-agent-factory.mjs` is Phase-6 and does not exist — leave it as a comment `UNKNOWN - verify` exactly like the Phase-3 header does). `chmod +x` the script.
  </action>
  <verify>
    <automated>sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh; test $? -eq 1 && echo "RED-as-expected (no workflows yet)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-workflows-cadence-backpressure/check-structure.sh` exists, is executable, and runs under `sh` without syntax errors.
    - Running it now (workflows dir holds only `.gitkeep`) exits 1 (RED) and prints FAIL lines for the absent files — proving it is a live gate, not a stub.
    - `grep -c 'V-' …` is not required, but the script visibly covers all 13 invariants: it greps for `READY_FOR_HUMAN_REVIEW`, `05-pr-quality-gate.md`, `cadence=scrum`, `SPRINT-xx.md`, `blocked_escalation_days`, `autonomy=pr`, `never merge`, `production_requires_human_confirmation`, `incident-postmortem.md`, `blameless`, `kind: workflow`, the 10 `## ` headings, and the `plans/.*-handoff` drift guard.
    - The script contains NO real gate command string (no `npm test`, `eslint`, `tsc`, etc.) and references the Phase-6 Node validator only as `UNKNOWN - verify`.
  </acceptance_criteria>
  <done>Harness authored, executable, RED against the empty workflows dir, encoding V-01..V-13 with zero fabricated commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness ↔ authored workflow files | The harness is the only mechanical check that the 14 markdown files honor the frozen contracts; a weak/faked check would let drift or a fabricated gate ship undetected |

This is a markdown + POSIX-sh authoring task with no runtime input handling, no network, no auth surface. Conventional STRIDE web categories do not apply (informational/LOW). The in-scope integrity threats are kit-text integrity.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | check-structure.sh asserting a faked/weak invariant | mitigate | Encode V-01..V-13 verbatim from 04-VALIDATION.md; the harness itself must ship RED first (proving the checks fire), so a no-op check is caught immediately |
| T-04-02 | Repudiation | Harness referencing a fabricated Node validator command | mitigate | Reference the Phase-6 validator only as `UNKNOWN - verify` (no-fabrication constraint); use only POSIX sh + grep/wc/test |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — POSIX `sh` is already present; nothing to audit |
</threat_model>

<verification>
- Harness runs clean under `sh` (no syntax error) and exits 1 against the current empty `agent-factory/workflows/`.
- Re-running it after each later-wave file lands flips that file's checks green; full green is the Phase gate.
</verification>

<success_criteria>
`check-structure.sh` exists, is executable, encodes all 13 V-checks, ships RED, and fabricates no command. It is the `<automated>` proof referenced by every other Phase-4 plan.
</success_criteria>

<output>
Create `.planning/phases/04-workflows-cadence-backpressure/04-01-SUMMARY.md` when done.
</output>

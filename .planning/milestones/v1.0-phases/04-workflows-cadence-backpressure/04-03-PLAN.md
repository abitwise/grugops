---
phase: 04-workflows-cadence-backpressure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/workflows/02-idea-to-epics.md
  - agent-factory/workflows/03-epic-to-tickets.md
  - agent-factory/workflows/06-uat-pack.md
autonomous: true
requirements: [FLOW-02, FLOW-05]
must_haves:
  truths:
    - "02-idea-to-epics turns an idea into epics via BA/PM and the product-handoff, landing epics in Backlog"
    - "03-epic-to-tickets produces tickets each with value/scope/acceptance/test-notes/security-NFR-triggers/size/priority and a traceability row, gated by Definition of Ready"
    - "06-uat-pack produces the UAT pack via UAT Planner + BA/PM + QE and moves the ticket to Ready to Release (or Done in lean)"
  artifacts:
    - path: "agent-factory/workflows/02-idea-to-epics.md"
      provides: "Idea -> epics lifecycle workflow"
      min_lines: 20
    - path: "agent-factory/workflows/03-epic-to-tickets.md"
      provides: "Epic -> tickets lifecycle workflow with DoR gate and traceability row"
      min_lines: 20
    - path: "agent-factory/workflows/06-uat-pack.md"
      provides: "UAT pack lifecycle workflow"
      min_lines: 20
  key_links:
    - from: "agent-factory/workflows/03-epic-to-tickets.md"
      to: "agent-factory/checklists/definition-of-ready.md"
      via: "DoR gate before a ticket is Ready"
      pattern: "definition-of-ready"
    - from: "agent-factory/workflows/06-uat-pack.md"
      to: "agent-factory/handoffs/uat-handoff.md"
      via: "UAT Planner output handoff"
      pattern: "uat-handoff.md"
---

<objective>
Author the three remaining Wave-1 lifecycle workflows: `02-idea-to-epics.md`, `03-epic-to-tickets.md`, `06-uat-pack.md`. Each is a ~one-screen file on the 10-section v2 template, reproducing its §7.3/§7.4/§7.7 `Flow:`/`Done when:` spine and deriving the connective sections tersely from frozen Phase-1/2/3 names (D-24).

Purpose: Complete the lifecycle backbone (FLOW-02) alongside Plan 02's 04/05 - turning the idea->epic->ticket->...->UAT sequence into named, board-moving, handoff-producing, trace-updating workflows.
Output: `agent-factory/workflows/02-idea-to-epics.md`, `agent-factory/workflows/03-epic-to-tickets.md`, `agent-factory/workflows/06-uat-pack.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md
@.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md
@agent-factory/roles/orchestrator.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 02-idea-to-epics.md</name>
  <files>agent-factory/workflows/02-idea-to-epics.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 02-idea-to-epics.md (§7.3 Flow `idea -> BA/PM -> product-handoff -> epics`; Done when: MVP scope clear, epics/non-goals/risks written, epics added to Backlog) + section "The 10-Section v2 Workflow Template".
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `02-idea-to-epics.md` + Shared Patterns S1, S2, S4 (handoff citation), S5 (trace/metrics).
    - `agent-factory/roles/orchestrator.md` - structural analog (frontmatter + ordered sections).
    - `agent-factory/roles/ba-pm.md` - the role 02 composes (board move Backlog -> Ready; output product-handoff.md; epics -> plans/epics/).
    - `agent-factory/handoffs/product-handoff.md` (the cited handoff). `plans/traceability.md` (Epic/Feature columns).
  </read_first>
  <action>
    Create `agent-factory/workflows/02-idea-to-epics.md` with kind: workflow frontmatter (<=3 fields; optionally order: 2 / cadence: both) then `# Workflow: Idea to epics` and the 10 sections in order. Reproduce the §7.3 Flow into When to use (a light grug wink is allowed only in this opener). Agents involved: BA/PM. Inputs required: a raw idea/request. Steps (clear voice, composing ba-pm's frozen behavior, D-23): BA/PM clarifies the idea, defines MVP scope + non-goals + risks, writes agent-factory/handoffs/product-handoff.md, breaks the idea into epics added to Backlog (epics -> plans/epics/). Board moves: name the frozen column Backlog (and Backlog -> Ready where ba-pm signs off). Handoffs produced: agent-factory/handoffs/product-handoff.md. Trace updates: append Epic/Feature rows to plans/traceability.md (cite the frozen 10-column trace). Metrics emitted: a subset of the frozen 9 (e.g. Throughput / Lead time) or "none beyond board append" if nothing applies - do not invent a metric. Stop conditions: idea too vague to scope -> stop and request clarity. Done condition (reproduce §7.3 Done when): MVP scope clear; epics, non-goals, risks written; epics added to Backlog. Keep to ~one screen; cite only frozen names.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/02-idea-to-epics.md && grep -qF 'product-handoff.md' agent-factory/workflows/02-idea-to-epics.md && grep -qF 'kind: workflow' agent-factory/workflows/02-idea-to-epics.md && echo OK-02; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/02-idea-to-epics.md exists with kind: workflow frontmatter (<=3 fields) and all 10 template headings in order (V-02/V-03 green for 02).
    - Cites agent-factory/handoffs/product-handoff.md (frozen 16-file list) and plans/traceability.md; no plans/*-handoff path; no invented column/handoff/metric (V-12 green).
    - When to use reproduces the §7.3 Flow; Done condition reproduces the §7.3 Done-when faithfully.
  </acceptance_criteria>
  <done>02 authored, 10 sections in order, frozen-name-only, V-02/V-03/V-12 green.</done>
</task>

<task type="auto">
  <name>Task 2: Author 03-epic-to-tickets.md</name>
  <files>agent-factory/workflows/03-epic-to-tickets.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 03-epic-to-tickets.md (§7.4 Flow `epic -> BA/PM -> System Analyst (if behavior unclear) -> tickets`; Done when: each ticket has value/scope/acceptance/test-notes/security-NFR-triggers/size/priority and a traceability row).
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `03-epic-to-tickets.md` + Shared Patterns S1, S2, S4, S5.
    - `agent-factory/roles/orchestrator.md` (analog + the SPLIT_REQUIRED / no-XL rule). `agent-factory/roles/ba-pm.md` (sizes/prioritizes -> tickets -> plans/tickets/), `agent-factory/roles/system-analyst.md` (In Analysis exit -> system-handoff.md).
    - `agent-factory/checklists/definition-of-ready.md` (the DoR gate). `plans/traceability.md` (the 10-column row appended per ticket).
  </read_first>
  <action>
    Create `agent-factory/workflows/03-epic-to-tickets.md` with kind: workflow frontmatter then `# Workflow: Epic to tickets` and the 10 sections in order. Reproduce the §7.4 Flow into When to use. Agents involved: BA/PM (+ System Analyst if behavior unclear). Steps (clear voice): BA/PM breaks the epic into tickets, sizes (XS/S/M/L/XL) and prioritizes (P0-P3); System Analyst clarifies flows/system rules into agent-factory/handoffs/system-handoff.md when behavior is unclear (In Analysis exit); each ticket is checked against agent-factory/checklists/definition-of-ready.md; XL is split (SPLIT_REQUIRED, no XL into dev). Board moves: In Analysis (system-analyst exit); tickets enter Backlog/Ready. Handoffs produced: agent-factory/handoffs/system-handoff.md (when System Analyst runs). Trace updates: append one row per ticket to plans/traceability.md. Metrics emitted: subset of the frozen 9 if applicable, else state none. Stop conditions: behavior ambiguous and System Analyst cannot resolve -> stop; XL not split -> SPLIT_REQUIRED. Done condition (reproduce §7.4 Done when): each ticket has user value, scope, acceptance criteria, test notes, security/NFR triggers, size, priority, and a traceability row. Tickets -> plans/tickets/. Keep ~one screen; cite only frozen names.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/03-epic-to-tickets.md && grep -qF 'definition-of-ready' agent-factory/workflows/03-epic-to-tickets.md && grep -qF 'plans/traceability.md' agent-factory/workflows/03-epic-to-tickets.md && echo OK-03; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/03-epic-to-tickets.md exists with kind: workflow frontmatter (<=3 fields) and all 10 headings in order (V-02/V-03 green for 03).
    - Cites agent-factory/checklists/definition-of-ready.md, agent-factory/handoffs/system-handoff.md, plans/traceability.md, plans/tickets/; no plans/*-handoff path; no invented name (V-12 green).
    - Done condition names size, priority, and the traceability row per the §7.4 Done-when.
  </acceptance_criteria>
  <done>03 authored, DoR-gated, trace-row producing, V-02/V-03/V-12 green.</done>
</task>

<task type="auto">
  <name>Task 3: Author 06-uat-pack.md</name>
  <files>agent-factory/workflows/06-uat-pack.md</files>
  <read_first>
    - `.planning/phases/04-workflows-cadence-backpressure/04-RESEARCH.md` spine-table row for 06-uat-pack.md (§7.7 Flow `feature complete -> UAT Planner -> BA/PM validation -> QE validation -> UAT pack`; Done when: scenarios/test-data/pass-fail-criteria/signoff-checklist/known-limitations exist; ticket -> Ready to Release or Done in lean).
    - `.planning/phases/04-workflows-cadence-backpressure/04-PATTERNS.md` section `06-uat-pack.md` + Shared Patterns S1, S2, S4.
    - `agent-factory/roles/orchestrator.md` (analog). `agent-factory/roles/uat-planner.md` (Ready for UAT -> In UAT; owns In UAT exit -> uat-handoff.md; works uat-checklist.md), `agent-factory/roles/ba-pm.md`, `agent-factory/roles/qe-e2e.md` (validation).
    - `agent-factory/handoffs/uat-handoff.md`, `agent-factory/checklists/uat-checklist.md` (cited by name).
  </read_first>
  <action>
    Create `agent-factory/workflows/06-uat-pack.md` with kind: workflow frontmatter then `# Workflow: UAT pack` and the 10 sections in order. Reproduce the §7.7 Flow into When to use. Agents involved: UAT Planner (+ BA/PM and QE/E2E validation). Steps (clear voice): UAT Planner assembles scenarios, test data, pass/fail criteria, signoff checklist, known limitations into agent-factory/handoffs/uat-handoff.md working agent-factory/checklists/uat-checklist.md; BA/PM validates business acceptance; QE/E2E validates coverage. Board moves: Ready for UAT -> In UAT (uat-planner owns the In UAT exit -> Ready to Release). Handoffs produced: agent-factory/handoffs/uat-handoff.md. Trace updates: append the UAT link into plans/traceability.md (the frozen UAT column). Metrics emitted: subset of the frozen 9 if applicable, else state none. Stop conditions: acceptance criteria missing/ambiguous -> stop and route back. Done condition (reproduce §7.7 Done when): scenarios, test data, pass/fail criteria, signoff checklist, known limitations exist; ticket moves to Ready to Release (or Done in lean mode). Keep ~one screen; cite only frozen names.
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/06-uat-pack.md && grep -qF 'uat-handoff.md' agent-factory/workflows/06-uat-pack.md && grep -qF 'uat-checklist.md' agent-factory/workflows/06-uat-pack.md && echo OK-06; sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - agent-factory/workflows/06-uat-pack.md exists with kind: workflow frontmatter (<=3 fields) and all 10 headings in order (V-02/V-03 green for 06).
    - Cites agent-factory/handoffs/uat-handoff.md and agent-factory/checklists/uat-checklist.md (frozen lists); Board moves uses frozen columns Ready for UAT / In UAT / Ready to Release; no invented name (V-12 green).
    - Done condition reproduces the §7.7 Done-when including the Ready to Release / Done-in-lean move.
  </acceptance_criteria>
  <done>06 authored, UAT-pack producing, frozen-name-only, V-02/V-03/V-12 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow text -> trace integrity | These workflows declare what each step links into plans/traceability.md; a wrong/invented trace column or handoff name silently breaks the requirement->code->test->UAT trail (the project's value prop) |

Markdown-authoring task; no runtime input/network/auth surface. Conventional STRIDE web categories are informational/LOW. In-scope threat is kit-text/trace integrity via name drift.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | A workflow citing an invented handoff/column/metric name (drift) | mitigate | Cite only the frozen 16 handoffs / 13 columns / 9 metrics / 10 trace columns; harness V-12 drift guard asserts no plans/*-handoff and (optionally) frozen-list membership |
| T-04-03-02 | Repudiation | A ticket workflow that omits the traceability row, leaving work untraceable | mitigate | 03 Done condition reproduces the §7.4 "a traceability row" requirement; Trace updates section names plans/traceability.md explicitly |
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; nothing to audit |
</threat_model>

<verification>
Run `sh .planning/phases/04-workflows-cadence-backpressure/check-structure.sh` after each file. The V-02 (10 sections in order), V-03 (kind: workflow), and V-12 (no drift) checks for 02/03/06 must flip green. Full harness green is the Wave-1 gate once Plan 02 + Plan 03 land all five lifecycle files (02-06).
</verification>

<success_criteria>
02/03/06 authored on the 10-section template, reproducing their §7.x spines, deriving connective sections from frozen names only. Harness V-02/V-03/V-12 green for all three.
</success_criteria>

<output>
Create `.planning/phases/04-workflows-cadence-backpressure/04-03-SUMMARY.md` when done.
</output>

---
phase: 02-shared-contracts
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - memory-bank/00-index.md
  - memory-bank/10-project-brief.md
  - memory-bank/20-product.md
  - memory-bank/30-architecture.md
  - memory-bank/40-contributing.md
  - memory-bank/60-progress.md
  - memory-bank/70-runbook.md
  - memory-bank/80-glossary.md
  - memory-bank/50-decisions/ADR-template.md
autonomous: true
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "A minimal memory-bank seed exists: 00-index, 10-project-brief, 20-product, 30-architecture, 40-contributing, 60-progress, 70-runbook, 80-glossary, plus 50-decisions/ADR-template.md (MEM-01)"
    - "Each seed file is short, single-purpose, generic (project-agnostic), and empty-but-shaped — no grugops-specific content, no fake data (D-03/D-04/D-10)"
    - "00-index.md states the working-memory contract: roles read the bank on start; 60-progress.md is the running plan-of-record kept current by the daily sweep; 50-decisions/ captures ADRs as made; the index maps the bank for one-read orientation (MEM-02, D-11)"
    - "50-decisions/ ships ADR-template.md (status/context/decision/alternatives/consequences/rollback per §5.A.7) with NO example values and NO numbered ADR-000X file (D-12)"
    - "The ADR template filename does NOT match the validator's ADR-NNNN numeric pattern"
  artifacts:
    - path: "memory-bank/00-index.md"
      provides: "Working-memory contract + bank map (MEM-02)"
      contains: "60-progress"
    - path: "memory-bank/60-progress.md"
      provides: "Running plan-of-record seed (kept current by daily sweep)"
      min_lines: 3
    - path: "memory-bank/50-decisions/ADR-template.md"
      provides: "ADR copy-target: status/context/decision/alternatives/consequences/rollback"
      contains: "Rollback"
  key_links:
    - from: "memory-bank/00-index.md"
      to: "memory-bank/60-progress.md"
      via: "names 60-progress.md as the running plan-of-record kept by the daily sweep"
      pattern: "60-progress"
    - from: "memory-bank/00-index.md"
      to: "memory-bank/50-decisions/"
      via: "names 50-decisions/ as where ADRs are captured as made"
      pattern: "50-decisions"
---

<objective>
Author the minimal memory-bank seed under `memory-bank/`, satisfying MEM-01 and MEM-02. Eight seed files (00-index, 10-project-brief, 20-product, 30-architecture, 40-contributing, 60-progress, 70-runbook, 80-glossary) ship as a generic, project-agnostic, empty-but-shaped template (D-04/D-10); plus `50-decisions/ADR-template.md` documenting the §5.A.7 ADR format as a copy-target with no example values (D-12). The `00-index.md` states the working-memory contract (D-11): roles read the bank on start, 60-progress.md is the running plan-of-record kept current by the daily sweep, 50-decisions/ captures ADRs as made.

Purpose: This is the kit's persistent working-memory store. It ships as the user-facing TEMPLATE that the Phase-4 bootstrap workflow fills per-project; grugops's own state stays in `.planning/`. Each file must be small and single-purpose — anti-bloat, never a document dump.
Output: 9 empty-but-shaped generic seed files, zero fake data, no example ADR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-contracts/02-CONTEXT.md
@.planning/phases/02-shared-contracts/02-RESEARCH.md
@.planning/phases/02-shared-contracts/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 00-index (working-memory contract) + 7 seed files</name>
  <files>memory-bank/00-index.md, memory-bank/10-project-brief.md, memory-bank/20-product.md, memory-bank/30-architecture.md, memory-bank/40-contributing.md, memory-bank/60-progress.md, memory-bank/70-runbook.md, memory-bank/80-glossary.md</files>
  <read_first>
    - .planning/phases/02-shared-contracts/02-CONTEXT.md lines 46-48 (D-10 generic skeleton + D-11 working-memory contract — the locked content rules for these files)
    - docs/initial/agent_factory_builder_spec_v2.md lines 240-251 (§3 memory-bank file manifest — the exact 00-index→80-glossary list; do NOT seed brownfield-map.md / greenfield-plan.md, those are Phase-3/4 runtime outputs)
    - plans/metrics.md (the cleanest Phase-1 empty-but-shaped exemplar — Shared Pattern A: `# Title` + `_Updated: <date>_` + a `FORMAT — read before you ...` HTML-comment carrying the one-line purpose/hint; match this convention exactly)
    - .planning/phases/02-shared-contracts/02-PATTERNS.md lines 27-55, 100-105, 149-153 (Shared Pattern A empty-but-shaped + Pattern D index + Group 5 memory-bank assignment; advisory size cap ~under 40 lines per file)
    - agent-factory/README.md (orientation-doc tone for 00-index; lowercase grugops, anti-bloat)
  </read_first>
  <action>
    Author each seed file in the Phase-1 empty-but-shaped style (Shared Pattern A): `# Title` heading, the chosen `_Updated:_` opener (use the SAME form picked in Plans 01/03 — decide once, apply consistently), and a short `FORMAT — read before you ...` clear-voice HTML comment giving the one-line purpose + a brief format hint, then section stubs showing the shape. GENERIC and project-agnostic — NO grugops-specific content, NO fake data, NO example values (D-03/D-04/D-10). Keep each comfortably small (advisory ~under 40 lines).

    - 00-index.md: maps the bank (the 00→80 file set, one line each) AND states the working-memory contract verbatim in intent (MEM-02/D-11): roles READ the bank on start; `60-progress.md` is the running plan-of-record kept current by the daily sweep; `50-decisions/` captures ADRs as they are made; the index lets an agent/human orient in one read. Use the index frontmatter convention decided in Plan 03's checklists/00-index.md (same choice — `kind: index` or omit), applied consistently.
    - 10-project-brief.md: one-line purpose (the project's foundation: what/why/for-whom) + stubs.
    - 20-product.md: product context (users, value, scope) stubs.
    - 30-architecture.md: architecture/system-patterns/tech-context stubs.
    - 40-contributing.md: how-to-contribute / conventions stubs.
    - 60-progress.md: running plan-of-record — what works, what's left, known issues stubs (the file the daily sweep keeps current).
    - 70-runbook.md: operate/deploy/rollback-in-prod stubs (referenced by observability-slo-checklist).
    - 80-glossary.md: shorthand/acronyms/domain-terms stubs.

    Clear professional voice (D-00), lowercase `grugops` wherever the name appears. Anti-bloat — single-purpose each.
  </action>
  <verify>
    <automated>for f in 00-index 10-project-brief 20-product 30-architecture 40-contributing 60-progress 70-runbook 80-glossary; do test -f "memory-bank/$f.md" || { echo "MISSING $f.md"; exit 1; }; done; grep -q 'read' memory-bank/00-index.md && grep -q '60-progress' memory-bank/00-index.md && grep -qi 'daily sweep' memory-bank/00-index.md && grep -q '50-decisions' memory-bank/00-index.md || { echo "00-index missing working-memory contract elements"; exit 1; }; test ! -f memory-bank/brownfield-map.md && test ! -f memory-bank/greenfield-plan.md || { echo "must NOT seed runtime artifacts"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f` passes for all 8: 00-index, 10-project-brief, 20-product, 30-architecture, 40-contributing, 60-progress, 70-runbook, 80-glossary
    - 00-index.md working-memory contract: `grep -q 'read'`, `grep -q '60-progress'`, `grep -qi 'daily sweep'`, `grep -q '50-decisions'` all pass
    - runtime artifacts NOT seeded: `test ! -f memory-bank/brownfield-map.md` AND `test ! -f memory-bank/greenfield-plan.md`
    - generic/no fake data: `! grep -rEq 'ABC-[0-9]{3}' memory-bank/00-index.md memory-bank/10-project-brief.md` (no live ticket IDs)
    - anti-bloat advisory: `wc -l` of each seed file is comfortably small (reviewer judgment; ~under 40 lines)
  </acceptance_criteria>
  <done>
    All 8 memory-bank seed files exist, generic and empty-but-shaped in the Phase-1 house style; 00-index.md states the working-memory contract (read-on-start, 60-progress = running plan-of-record kept by daily sweep, 50-decisions = ADRs) and maps the bank; runtime artifacts (brownfield-map/greenfield-plan) are NOT seeded; clear voice, lowercase grugops, zero fake data.
  </done>
</task>

<task type="auto">
  <name>Task 2: 50-decisions/ADR-template.md (copy-target, no example)</name>
  <files>memory-bank/50-decisions/ADR-template.md</files>
  <read_first>
    - .planning/phases/02-shared-contracts/02-CONTEXT.md line 49 (D-12 — ADR convention via template, NO example ADR-0001; the ADR-template.md name must NOT trip the validator's ADR-000X pattern)
    - docs/initial/agent_factory_builder_spec_v2.md line 456 (§5.A.7 — the ADR format: status, context, decision, alternatives, consequences, rollback)
    - .planning/phases/02-shared-contracts/02-RESEARCH.md lines 419-430 (the ADR template shape pre-extracted — status/context/decision/alternatives/consequences/rollback; copy-target only, no example values)
    - plans/traceability.md lines 17-26 (frozen ID scheme — real ADRs use ADR-000x; the template references that scheme but is itself named ADR-template.md, non-numeric)
  </read_first>
  <action>
    Author `memory-bank/50-decisions/ADR-template.md` as a copy-target documenting the §5.A.7 ADR format: a title line shape (`# ADR-XXXX: <short decision title>` using the literal `XXXX` placeholder, NOT a number), then the sections `## Status` (with the allowed values noted: proposed | accepted | deprecated | superseded by ADR-YYYY), `## Context`, `## Decision`, `## Alternatives`, `## Consequences`, `## Rollback`. NO example values, NO filled-in decision, NO example ADR-0001 (D-03/D-12). Add a brief clear-voice note that real ADRs are saved as `ADR-000X-<slug>.md` in this directory (the numeric slots stay empty until a role writes one). Keep the existing `.gitkeep`. The file MUST be named `ADR-template.md` (non-numeric) so it does not match the Phase-6 validator's `ADR-\d{4}` pattern.

    Optionally add a minimal `kind:` frontmatter consistent with the convention chosen for other memory-bank files (Claude's discretion — keep it tiny or omit). Clear voice, lowercase grugops.
  </action>
  <verify>
    <automated>test -f memory-bank/50-decisions/ADR-template.md || { echo "MISSING ADR-template.md"; exit 1; }; for s in Status Context Decision Alternatives Consequences Rollback; do grep -q "## $s" memory-bank/50-decisions/ADR-template.md || { echo "ADR template missing ## $s"; exit 1; }; done; ls memory-bank/50-decisions/ | grep -Eq 'ADR-[0-9]{4}' && { echo "FAIL: numbered ADR present (violates D-12/no-fake-data)"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f memory-bank/50-decisions/ADR-template.md` passes
    - all 6 §5.A.7 sections present: `grep -q '## Status'`, `'## Context'`, `'## Decision'`, `'## Alternatives'`, `'## Consequences'`, `'## Rollback'`
    - NO numbered ADR file exists: `! ls memory-bank/50-decisions/ | grep -Eq 'ADR-[0-9]{4}'` (template name is non-numeric; validator pattern not tripped)
    - no example values: the template ships headings/placeholders only — no filled-in decision content (reviewer check; the `XXXX` title uses a literal placeholder)
  </acceptance_criteria>
  <done>
    memory-bank/50-decisions/ADR-template.md exists with the §5.A.7 ADR shape (status/context/decision/alternatives/consequences/rollback) as an empty copy-target — no example ADR, no numbered ADR-000X file; the non-numeric filename avoids the validator's ADR-NNNN pattern; clear voice.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — static markdown) | Static memory-bank seed markdown. No code/inputs/data flow. Relevant surface is content integrity + the no-fake-data / no-example-secret invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information disclosure | secret/PII or grugops project content leaked into a seed file as 'example data' | mitigate | D-03/D-04/D-10: zero example data, generic skeleton only, grugops's own state stays in `.planning/`. `! grep -rEq 'ABC-[0-9]{3}'` + reviewer check for grugops-specific nouns. |
| T-02-11 | Tampering (validator collision) | ADR template named ADR-000X tripping the Phase-6 validator | mitigate | D-12: file named `ADR-template.md` (non-numeric). `! ls memory-bank/50-decisions/ \| grep -Eq 'ADR-[0-9]{4}'` asserts no numbered ADR exists. |
| T-02-12 | Repudiation (stale/missing contract) | 00-index omits the working-memory contract | mitigate | D-11/MEM-02: 00-index must state read-on-start + 60-progress plan-of-record + 50-decisions ADRs. `grep -q '60-progress' && grep -qi 'daily sweep' && grep -q '50-decisions'` asserts it. |

No high-severity code-level threats exist for this markdown-only plan; nothing to block on.
</threat_model>

<verification>
Full-plan structural sweep after both tasks:
- All 9 files exist: `for f in 00-index 10-project-brief 20-product 30-architecture 40-contributing 60-progress 70-runbook 80-glossary; do test -f "memory-bank/$f.md" || echo "MISSING $f"; done; test -f memory-bank/50-decisions/ADR-template.md`
- Working-memory contract present: `grep -q '60-progress' memory-bank/00-index.md && grep -qi 'daily sweep' memory-bank/00-index.md && grep -q '50-decisions' memory-bank/00-index.md`
- No numbered ADR / no runtime artifacts: `! ls memory-bank/50-decisions/ | grep -Eq 'ADR-[0-9]{4}'; test ! -f memory-bank/brownfield-map.md; test ! -f memory-bank/greenfield-plan.md`
- No-fake-data invariant: `! grep -rEq 'ABC-[0-9]{3}' memory-bank/`
- Anti-bloat advisory: `wc -l memory-bank/*.md` each comfortably small
</verification>

<success_criteria>
- Minimal memory-bank seed exists: 8 seed files + 50-decisions/ADR-template.md, each short and single-purpose (MEM-01, ROADMAP SC#4)
- Generic, project-agnostic, empty-but-shaped — no grugops-specific content, no fake data, no example ADR (D-03/D-04/D-10/D-12)
- 00-index.md states the working-memory contract: read-on-start, 60-progress = running plan-of-record (daily sweep), 50-decisions = ADRs (MEM-02, ROADMAP SC#5)
- ADR-template carries the §5.A.7 format; its non-numeric name avoids the validator's ADR-NNNN pattern
</success_criteria>

<output>
Create `.planning/phases/02-shared-contracts/02-04-SUMMARY.md` when done
</output>

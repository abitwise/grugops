---
phase: 05-packaging-adapters-install-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/packaging/adapters.md
  - agent-factory/packaging/subagent.frontmatter.md
  - agent-factory/packaging/slash-command.template.md
  - .planning/phases/05-packaging-adapters-install-distribution/check-structure.sh
autonomous: true
requirements: [PKG-01, PKG-02]
must_haves:
  truths:
    - "A reader can map each of the 5 host tools to its entry file + dispatch mode + adapter from one document"
    - "The single rule 'all work starts at orchestrator.md' and 'only the dispatch differs, never the content' are both restated in adapters.md"
    - "An executor building a Claude wrapper has a copy-ready frontmatter template that uses Agent (not Task) and model: inherit"
    - "The Phase-5 structural harness exists and runs (ships RED until later waves land)"
  artifacts:
    - path: "agent-factory/packaging/adapters.md"
      provides: "PKG-01 5-tool dispatch map + slogans + per-row verify flags + Claude-only guard / autonomy=pr fallback note"
      contains: "only the dispatch differs"
    - path: "agent-factory/packaging/subagent.frontmatter.md"
      provides: "PKG-02 subagent wrapper template (Agent, model: inherit, pointer body)"
      contains: "model: inherit"
    - path: "agent-factory/packaging/slash-command.template.md"
      provides: "PKG-02 skill SKILL.md template (recorded skills/ choice, pointer body)"
      contains: "name:"
    - path: ".planning/phases/05-packaging-adapters-install-distribution/check-structure.sh"
      provides: "Phase-5 structural acceptance harness (V-PKG-01..V-SAFE-02), ships RED"
      contains: "ALL CHECKS PASSED"
  key_links:
    - from: "agent-factory/packaging/adapters.md"
      to: "agent-factory/roles/orchestrator.md"
      via: "entry-rule restatement (pointer, never a copy)"
      pattern: "orchestrator\\.md"
    - from: "agent-factory/packaging/subagent.frontmatter.md"
      to: "agent-factory/roles/orchestrator.md"
      via: "pointer body cites the frozen role path"
      pattern: "agent-factory/roles/orchestrator\\.md"
---

<objective>
Author the packaging-decision layer: `agent-factory/packaging/adapters.md` (the authoritative, current 5-tool dispatch map per D-35) plus the two PKG-02 wrapper templates (`subagent.frontmatter.md`, `slash-command.template.md`). Also create the Phase-5 structural test harness that every later plan's structural verify will run against — it ships RED now and goes green as Waves 2–3 land.

Purpose: This is the contract every downstream wrapper/installer cites. The templates fix the `Agent`/`model: inherit`/`skills/` conventions once so the standalone and plugin forms stay consistent. The harness is the running acceptance gate for the whole phase.
Output: 3 packaging files under `agent-factory/packaging/` + 1 structural harness in the phase dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packaging-adapters-install-distribution/05-CONTEXT.md
@.planning/phases/05-packaging-adapters-install-distribution/05-RESEARCH.md
@.planning/phases/05-packaging-adapters-install-distribution/05-PATTERNS.md
@.planning/phases/05-packaging-adapters-install-distribution/05-VALIDATION.md

<interfaces>
<!-- Frozen pointer targets and the slogans/shapes to restate. Executor: cite these, NEVER copy role text. -->

agent-factory/README.md (the in-repo analog for adapters.md — note its CC row predates D-29; adapters.md is the current, authoritative version):
  - Slogan to restate verbatim: "only the dispatch differs, never the content."
  - 5-tool table columns to mirror: | Tool | Entry file it reads | Role dispatch |

AGENTS.md line 9 (entry-rule sentence to restate):
  "All work starts with the Orchestrator: `agent-factory/roles/orchestrator.md`."

agent-factory/roles/orchestrator.md lines 1-5 (frontmatter shape of the role the wrapper points at):
  kind: role / tier: core   — the wrapper points at this file; it does NOT copy the body.
  Hard-limit line to echo in clear voice (orchestrator.md line 119):
  "Never merge to a protected branch. Never deploy to prod. Never exceed WIP without a written reason."

05-RESEARCH.md Pattern 4 (the subagent frontmatter template — copy-paste-correct):
  name / description / tools: Read, Grep, Glob, Bash, Edit, Write, Agent / model: inherit ; pointer body.

05-RESEARCH.md Pattern 3 + ~/.claude/skills/gsd-plan-phase/SKILL.md (the live dash-skill shape):
  name / description / argument-hint / allowed-tools list (with Agent); thin pointer-text body.

05-RESEARCH.md Pattern 6 + Architectural Responsibility Map (per-tool wiring): Codex/OpenCode native AGENTS.md (no adapter); Gemini .gemini/settings.json context.fileName ["AGENTS.md","GEMINI.md"]; Copilot AGENTS.md + optional .github/copilot-instructions.md (flag "verify against current Copilot CLI docs"); Claude both forms.

.planning/phases/04-workflows-cadence-backpressure/check-structure.sh (the exact harness skeleton to clone): #!/usr/bin/env sh / set -eu / FAILS=0 / pass()/fail() helpers / per-check asserts / exit 0 on green, 1 on RED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write agent-factory/packaging/adapters.md (PKG-01)</name>
  <files>agent-factory/packaging/adapters.md</files>
  <read_first>
    - agent-factory/README.md (the in-repo analog — the 5-tool table shape + the slogan at the "## How it works"/dispatch section; note its CC row is pre-D-29)
    - AGENTS.md (line 9 entry-rule sentence)
    - 05-RESEARCH.md § Architecture Patterns Pattern 6 + § Architectural Responsibility Map (current per-tool wiring + the dispatch-mode column)
    - 05-CONTEXT.md D-35 (adapters.md shape), D-29 (dash standalone / colon plugin), SAFE-02 docs note
    - 05-PATTERNS.md § "agent-factory/packaging/adapters.md" (which lines to mirror, what NOT to copy)
  </read_first>
  <action>Create `agent-factory/packaging/adapters.md`. Mirror the README's 5-tool table shape with columns: Tool | Entry file it reads | Dispatch mode | Adapter | Verify. One row per tool: Claude Code (entry: CLAUDE.md one-line pointer + AGENTS.md; dispatch: native sub-agents — Orchestrator main-thread spawner via plugin settings.json `agent:`; adapter: standalone `.claude/skills/grugops*` dash form AND the `.claude-plugin/` plugin colon form); Codex CLI (entry: root AGENTS.md + `~/.codex/AGENTS.md`; dispatch: sequential role-load; adapter: none — native); Gemini CLI (entry: AGENTS.md via `.gemini/settings.json` `context.fileName: ["AGENTS.md","GEMINI.md"]`; dispatch: sequential; adapter: settings.json wiring); OpenCode (entry: AGENTS.md + `~/.config/opencode/AGENTS.md`; dispatch: sequential; adapter: none — native); GitHub Copilot CLI (entry: AGENTS.md + optional `.github/copilot-instructions.md`; dispatch: sequential; adapter: optional pointer). Every row's Verify cell reads "verify against current tool docs" — there must be at least 5 occurrences of that exact phrase. Restate verbatim, in prose framing: "All work starts at `agent-factory/roles/orchestrator.md`." and "only the dispatch differs, never the content." Add a clear-voice § documenting that the mechanical prod-deploy guard (SAFE-02) is Claude-Code-only (plugin hooks) and the other four tools rely on the `autonomy=pr` + `production_requires_human_confirmation` procedural fallback. Use only `code.claude.com/docs/en/*` doc links, never `docs.claude.com`. Always lowercase `grugops`. Light grug wink only in framing prose; clear voice for the safety/dispatch facts. Do NOT copy any role body text into this file.</action>
  <verify>
    <automated>grep -q "only the dispatch differs" agent-factory/packaging/adapters.md && [ "$(grep -c 'verify against current tool docs' agent-factory/packaging/adapters.md)" -ge 5 ] && grep -q "orchestrator.md" agent-factory/packaging/adapters.md && grep -q "autonomy=pr" agent-factory/packaging/adapters.md && ! grep -q "docs.claude.com" agent-factory/packaging/adapters.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `agent-factory/packaging/adapters.md` exists and contains a table with 5 tool rows (Claude Code, Codex CLI, Gemini CLI, OpenCode, GitHub Copilot CLI)
    - The phrase "only the dispatch differs" appears at least once
    - The phrase "verify against current tool docs" appears at least 5 times (one per tool row)
    - The entry rule referencing `orchestrator.md` appears
    - A section documents the guard is Claude-only and the `autonomy=pr` fallback for the other 4 tools (grep `autonomy=pr` succeeds)
    - No `docs.claude.com` link appears (only `code.claude.com`)
    - No distinctive role-body sentence from `agent-factory/roles/orchestrator.md` appears (pointer-only)
  </acceptance_criteria>
  <done>adapters.md maps all 5 tools to entry + dispatch + adapter, restates both slogans and the entry rule, flags every row "verify against current tool docs", documents the Claude-only guard + autonomy=pr fallback, uses only code.claude.com links, and copies no role text.</done>
</task>

<task type="auto">
  <name>Task 2: Write the two PKG-02 wrapper templates</name>
  <files>agent-factory/packaging/subagent.frontmatter.md, agent-factory/packaging/slash-command.template.md</files>
  <read_first>
    - 05-RESEARCH.md Pattern 4 (subagent frontmatter: Agent, model: inherit, pointer body)
    - 05-RESEARCH.md Pattern 3 + Pitfall 5 (skill SKILL.md shape; dash-vs-colon naming asymmetry)
    - agent-factory/roles/orchestrator.md (the pointer TARGET; lines 1-5 frontmatter + line 119 hard-limits — to echo in clear voice, NOT copy the body)
    - 05-PATTERNS.md § "subagent.frontmatter.md" and § "slash-command.template.md"
    - 05-CONTEXT.md D-29 (recorded skills/ choice), D-31 (repo-relative pointer-text)
  </read_first>
  <action>Create `agent-factory/packaging/subagent.frontmatter.md`: a copy-ready template (per 05-RESEARCH.md Pattern 4) with YAML frontmatter keys `name`, `description`, `tools: Read, Grep, Glob, Bash, Edit, Write, Agent`, `model: inherit`, and a thin pointer body that says read `agent-factory/roles/orchestrator.md` then config/AGENTS.md/board, then act as the role. The template MUST use `Agent` and MUST NOT contain the legacy word `Task` anywhere. Echo the orchestrator hard limit ("Never merge to a protected branch. Never deploy to prod.") in clear voice in the body. Create `agent-factory/packaging/slash-command.template.md`: a copy-ready skill `SKILL.md` template (per Pattern 3) with frontmatter `name`, `description`, `argument-hint`, `allowed-tools` (a YAML list including `Agent`), and a repo-relative pointer-text body ("read `agent-factory/roles/orchestrator.md`, then run workflow `<NN-...md>`. Request: $ARGUMENTS"). Record the `skills/` form choice (D-29) and the dash-standalone-vs-colon-plugin naming asymmetry in a comment/prose note: standalone skill dirs carry the `grugops-` prefix (dir = whole command, dash); plugin skill dirs omit it (plugin name supplies the `grugops:` prefix). Note `disable-model-invocation: true` belongs on the destructive `grugops-release`. Both templates are pointer-text only — never copy a role body. Clear voice for the safety lines; light wink only in framing.</action>
  <verify>
    <automated>grep -q "Agent" agent-factory/packaging/subagent.frontmatter.md && ! grep -qw "Task" agent-factory/packaging/subagent.frontmatter.md && grep -q "model: inherit" agent-factory/packaging/subagent.frontmatter.md && grep -q "name:" agent-factory/packaging/slash-command.template.md && grep -q "orchestrator.md" agent-factory/packaging/slash-command.template.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `subagent.frontmatter.md` contains `model: inherit` and the word `Agent`, and does NOT contain the standalone word `Task` (`grep -qw "Task"` fails)
    - `subagent.frontmatter.md` body points at `agent-factory/roles/orchestrator.md` and echoes the never-deploy-prod hard limit in clear voice
    - `slash-command.template.md` has frontmatter `name`, `description`, `argument-hint`, `allowed-tools` (list incl. `Agent`) and a `$ARGUMENTS` pointer body referencing `orchestrator.md`
    - The dash-standalone vs colon-plugin naming asymmetry and the `skills/` choice are recorded in prose/comment
    - Neither template copies a role body (pointer-only)
  </acceptance_criteria>
  <done>Both templates exist, use `Agent` + `model: inherit`, record the `skills/` choice and the dash/colon naming asymmetry, point at the frozen orchestrator path, and contain no copied role text and no `Task` token.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the Phase-5 structural harness (ships RED)</name>
  <files>.planning/phases/05-packaging-adapters-install-distribution/check-structure.sh</files>
  <read_first>
    - .planning/phases/04-workflows-cadence-backpressure/check-structure.sh (the exact skeleton to clone — header, pass()/fail(), exit convention; lines 1-29)
    - 05-VALIDATION.md § Requirement → Test Map (the 8 automated commands to encode)
    - 05-PATTERNS.md § "Phase-5 check-structure.sh extension" + § "POSIX-sh house style"
  </read_first>
  <action>Create `.planning/phases/05-packaging-adapters-install-distribution/check-structure.sh` by cloning the Phase-4 harness skeleton verbatim (`#!/usr/bin/env sh`, `set -eu`, `FAILS=0`, `pass()`/`fail()` helpers, `printf` not `echo -e`, exit 0 on all-green / exit 1 with count on RED). Encode one structural check per Phase-5 requirement drawn from 05-VALIDATION.md § Requirement → Test Map: PKG-01 (adapters.md has "only the dispatch differs" + >=5 "verify against current tool docs"), PKG-02 (subagent template has `Agent`, lacks `Task`, has `model: inherit`), CLAUDE-01 (`.claude/skills/grugops*/SKILL.md` count == 7), CLAUDE-02 (`.claude-plugin/plugin.json` exists with name grugops; `skills/` and `hooks/` at plugin root; nothing but plugin.json+marketplace.json in `.claude-plugin/`), CLAUDE-03 (`hooks/hooks.json` references `${CLAUDE_PLUGIN_ROOT}` and no `/Users/`/`/home/`), SAFE-02 (presence of `hooks/guard.mjs`), INSTALL-01/02 (presence of `install/install.sh`, `install/install.mjs`, `install/uninstall.sh`, `install/README.md`). Use `grep -v '^#'` before any `grep -c` count gate so header prose is not self-counted. Add a header comment stating the real Node validator is Phase-6/VAL-01 and is intentionally `UNKNOWN - verify` — do NOT fabricate `node scripts/validate-agent-factory.mjs`. The harness ships RED now (only PKG checks pass after Tasks 1-2); it is expected to fail until Waves 2-3 land.</action>
  <verify>
    <automated>sh .planning/phases/05-packaging-adapters-install-distribution/check-structure.sh; rc=$?; [ "$rc" -eq 0 ] || [ "$rc" -eq 1 ] && echo "HARNESS RUNS (rc=$rc, RED expected until later waves)"</automated>
  </verify>
  <acceptance_criteria>
    - `check-structure.sh` exists, is `#!/usr/bin/env sh` + `set -eu`, and runs without a syntax error (exit code is 0 or 1, not a shell error)
    - It encodes a labeled check for each of PKG-01, PKG-02, CLAUDE-01, CLAUDE-02, CLAUDE-03, SAFE-02, INSTALL-01, INSTALL-02
    - The PKG-01 and PKG-02 checks PASS after Tasks 1-2; the CLAUDE/SAFE/INSTALL checks FAIL (artifacts not yet built) — i.e. the harness ships RED
    - The header documents the Phase-6 Node validator as `UNKNOWN - verify` and the script does NOT invoke `node scripts/validate-agent-factory.mjs`
    - Any `grep -c` count gate filters comment lines with `grep -v '^#'` first
  </acceptance_criteria>
  <done>The Phase-5 structural harness runs, encodes all 8 requirement checks in the Phase-3/4 house style, passes the two PKG checks, fails the not-yet-built ones (ships RED), and fabricates no validator command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen core → packaging layer | Adapters/templates must POINT at `agent-factory/roles/*` and never copy role text across this boundary (single-source) |
| documentation → external tool docs | Tool-specific facts (per-tool wiring) are flagged "verify against current tool docs" rather than asserted as permanent truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | adapters.md / templates copying role text | mitigate | Pointer-only; harness + dup-check (grep distinctive role sentence → 0 hits in packaging) enforces single-source |
| T-05-02 | Information disclosure | stale `docs.claude.com` links / fabricated tool facts | mitigate | Grep forbids `docs.claude.com`; every tool row flagged "verify against current tool docs"; no fabricated command |
| T-05-03 | Tampering | template ships legacy `Task` tool name | mitigate | Harness asserts `Agent` present AND `Task` absent in subagent template |
</threat_model>

<verification>
- `agent-factory/packaging/adapters.md`, `subagent.frontmatter.md`, `slash-command.template.md` exist.
- `.planning/phases/05-packaging-adapters-install-distribution/check-structure.sh` runs (exit 0 or 1) and its PKG-01/PKG-02 checks pass.
- No `docs.claude.com` link in any file authored here; `Task` token absent from the subagent template.
</verification>

<success_criteria>
- adapters.md maps all 5 tools and restates both slogans + the entry rule + the Claude-only-guard/autonomy=pr note (PKG-01).
- Both templates use `Agent` + `model: inherit`, record the `skills/` choice + dash/colon asymmetry, point at frozen paths, copy no role text (PKG-02).
- The Phase-5 structural harness runs and ships RED, ready to go green as later waves land.
</success_criteria>

<output>
Create `.planning/phases/05-packaging-adapters-install-distribution/05-01-SUMMARY.md` when done.
</output>

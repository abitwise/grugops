---
phase: 06-validation-brand-dogfood
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - NOTICE
  - CONTRIBUTING.md
  - docs/faq.md
autonomous: true
requirements: [BRAND-01, BRAND-02]
must_haves:
  truths:
    - "Root README.md opens in clear voice, includes the hero block, an Acknowledgements section crediting grugbrain.dev (Carson Gross), and the non-affiliation footer, then links to the internal agent-factory/README.md"
    - "NOTICE, CONTRIBUTING.md, and docs/faq.md exist, reproduced from the brand manual's ready-to-paste blocks"
    - "No literal /grug (non-/grugops) appears in any collateral; the command surface is /grugops everywhere; version is stated as 0.1.0"
  artifacts:
    - path: "README.md"
      provides: "Public face: clear-voice opener → grug wink → hero → install quickstart → Acknowledgements → non-affiliation footer → link to internal README"
      contains: "Acknowledgements"
      min_lines: 25
    - path: "NOTICE"
      provides: "§10.4-C legal/non-affiliation block (verbatim)"
      contains: "not affiliated"
    - path: "CONTRIBUTING.md"
      provides: "§10.3 contributor original-art + no-affiliation rules"
      contains: "original"
    - path: "docs/faq.md"
      provides: "§8.8 FAQ (verbatim, clear voice)"
  key_links:
    - from: "README.md"
      to: "agent-factory/README.md"
      via: "markdown link to the internal start-here (D-51); internal README untouched"
      pattern: "agent-factory/README.md"
    - from: "README.md / docs/faq.md"
      to: "command surface"
      via: "/grugops dash-standalone primary, /grugops:<op> plugin colon where install discussed; never literal /grug"
      pattern: "/grugops"
---

<objective>
Ship the brand/legal documentation collateral assembled almost entirely from the brand manual's pre-written, legally-reviewed ready-to-paste blocks: root `README.md` (clear-voice opener → grug wink → hero block → install quickstart → Acknowledgements crediting grugbrain.dev/Carson Gross → non-affiliation footer → link to the internal `agent-factory/README.md`), plus `NOTICE`, `CONTRIBUTING.md`, and `docs/faq.md`. Apply the single systematic D-49 reconciliation: literal `/grug` → `/grugops` everywhere (BRAND-01, BRAND-02).

Purpose: BRAND-01/02 — the public-facing legal + entry collateral. Mostly reproduction (don't re-author legal prose); the only edit is the command-surface swap the manual predates.
Output: `README.md`, `NOTICE`, `CONTRIBUTING.md`, `docs/faq.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-validation-brand-dogfood/06-RESEARCH.md
@.planning/phases/06-validation-brand-dogfood/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble root README.md (clear-voice opener, hero, Acknowledgements, non-affiliation footer, link to internal README)</name>
  <files>README.md</files>
  <read_first>
    - `agent-factory/README.md:1-30` (the internal start-here tone/structure the root README LINKS to and must NOT overwrite, D-51)
    - `docs/initial/grugops_brand_manual.md` §8.6 (line 375, the README hero block drop-in — note its literal `/grug` that MUST become `/grugops`) and §10.4-A/B (line 532+, Acknowledgements + non-affiliation footer — verbatim)
    - `.planning/phases/06-validation-brand-dogfood/06-RESEARCH.md` "Brand: README hero (§8.6) — RECONCILED to /grugops" (lines 511-528, the already-reconciled hero) and "Acknowledgements + non-affiliation footer (§10.4-A / §10.4-B) — verbatim" (lines 530-542)
    - `.planning/phases/06-validation-brand-dogfood/06-PATTERNS.md` "README.md (doc, brand/public face)" (D-51 link-to-internal structure)
    - `agent-factory/VERSION` (`0.1.0` — the version the README states, D-28)
    - `.claude-plugin/plugin.json` (`name: grugops`, `license: MIT`, author — for consistency)
  </read_first>
  <action>
    Write root `README.md` as the public face. Order (discretion within these required blocks): (1) clear-voice opener — the plain-English first sentence describing what grugops does (per D-21/D-49, clear professional English, no caveman) followed by the `> grug keep it simple.` wink in framing prose only; (2) the §8.6 hero block RECONCILED to `/grugops` — the install line `sh install/install.sh` then `/grugops "bootstrap this repo and propose safe first tickets"` (dash-standalone primary), noting the plugin colon form `/grugops:<op>` where install/distribution is discussed; (3) an install quickstart pointing at `install/install.sh` and stating version `0.1.0` (D-28); (4) an `## Acknowledgements` section crediting grugbrain.dev (Carson Gross) — verbatim from §10.4-A; (5) the non-affiliation footer — verbatim from §10.4-B (clear voice, no caveman — it is legal text); (6) a markdown link to `agent-factory/README.md` for the deep "start at orchestrator.md" entry (D-51). Apply the `/grug` → `/grugops` swap to EVERY copied block (the manual predates D-29/D-49). Do NOT touch `agent-factory/README.md` (untouched, linked-to only). Do NOT leave any literal `/grug` not followed by `ops`.
  </action>
  <verify>
    <automated>test -f README.md && grep -q 'Acknowledgements' README.md && grep -q 'grugbrain.dev' README.md && grep -q 'agent-factory/README.md' README.md && ! grep -rE '/grug([^o]|$)' README.md && echo "README-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` succeeds and `grep -c . README.md` ≥ 25 lines.
    - `grep -q 'Acknowledgements' README.md` and `grep -q 'grugbrain.dev' README.md` and `grep -q 'Carson Gross' README.md` all succeed.
    - `grep -q 'agent-factory/README.md' README.md` succeeds (links to internal README, D-51).
    - `grep -q '0.1.0' README.md` succeeds (version stated, D-28).
    - `! grep -rE '/grug([^o]|$)' README.md` (no literal `/grug` non-`/grugops`, D-49) and `grep -q '/grugops' README.md`.
    - `git status --porcelain agent-factory/README.md` shows no modification.
  </acceptance_criteria>
  <done>Root README exists with clear-voice opener + wink, reconciled hero, Acknowledgements (grugbrain.dev/Carson Gross), non-affiliation footer, `0.1.0`, links to the untouched internal README, and contains no literal `/grug`.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble NOTICE, CONTRIBUTING.md, and docs/faq.md from the manual's ready-to-paste blocks</name>
  <files>NOTICE, CONTRIBUTING.md, docs/faq.md</files>
  <read_first>
    - `docs/initial/grugops_brand_manual.md` §10.4-C (NOTICE block) and §10.3 (line 523, contributor rules → CONTRIBUTING) and §8.8 (line 406, FAQ)
    - `.planning/phases/06-validation-brand-dogfood/06-RESEARCH.md` "Brand: NOTICE (§10.4-C) — verbatim" (lines 544-559, including the `<year>/<your name>` = `2026 Olger Oeselg` note) and the §8.8/§10.3 verbatim guidance
    - `.planning/phases/06-validation-brand-dogfood/06-PATTERNS.md` "NOTICE, CONTRIBUTING.md, docs/faq.md (docs, legal/verbatim)" (content-source table; don't re-author)
    - `.claude-plugin/plugin.json` (author `Olger Oeselg`, `license: MIT` — for the NOTICE `<year>/<name>` substitution = `2026 Olger Oeselg`)
  </read_first>
  <action>
    Reproduce three files VERBATIM from the brand manual's ready-to-paste blocks — do NOT re-author legal/disclaimer prose (re-writing risks IP-disclaimer drift). `NOTICE` (repo root): copy §10.4-C verbatim, substituting `<year>` → `2026` and `<your name / org>` → `Olger Oeselg` (from `plugin.json` author + current date). `CONTRIBUTING.md`: copy §10.3 contributor rules verbatim (original-art enforcement + no-affiliation rules) — clear voice. `docs/faq.md`: copy §8.8 FAQ verbatim — clear voice (`docs/` already exists; this is additive). All three are clear-voice/legal — no caveman, no grug wink (D-21). These blocks contain no commands, so the `/grug` swap is moot — but grep to confirm no stray `/grug` leaked in. Do NOT create any other docs file or modify existing `docs/` content.
  </action>
  <verify>
    <automated>test -f NOTICE && test -f CONTRIBUTING.md && test -f docs/faq.md && grep -qi 'not affiliated' NOTICE && grep -qi 'original' CONTRIBUTING.md && ! grep -rE '/grug([^o]|$)' NOTICE CONTRIBUTING.md docs/faq.md && echo "LEGAL-DOCS-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f NOTICE && test -f CONTRIBUTING.md && test -f docs/faq.md` all succeed.
    - `grep -qi 'not affiliated' NOTICE` (non-affiliation language present) and `grep -q '2026 Olger Oeselg' NOTICE || (grep -q 2026 NOTICE && grep -q 'Olger Oeselg' NOTICE)`.
    - `grep -qi 'original' CONTRIBUTING.md` (original-art rule present).
    - `! grep -rE '/grug([^o]|$)' NOTICE CONTRIBUTING.md docs/faq.md` (no literal `/grug`).
    - `git status --porcelain agent-factory/ docs/initial/` shows no modification (frozen sources + spec untouched).
  </acceptance_criteria>
  <done>NOTICE, CONTRIBUTING.md, docs/faq.md exist verbatim from the manual's §10.4-C/§10.3/§8.8 blocks (NOTICE year/name substituted), clear-voice, with no stray `/grug`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs → users | README/NOTICE/CONTRIBUTING/FAQ are read by users; risk surface is legal accuracy + command-surface integrity, not code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-LEG | (Legal, not STRIDE) | NOTICE / CONTRIBUTING non-affiliation prose | mitigate | Reproduce §10.4-A/B/C + §10.3 verbatim (legally reviewed); never re-author; grugbrain.dev/Carson Gross attribution + non-affiliation footer ship intact |
| T-06-CMD | Repudiation (brand/safety integrity) | literal `/grug` leaking into collateral | mitigate | Apply the D-49 `/grug`→`/grugops` swap to every copied block; verify grep `! grep -rE '/grug([^o]|$)'` over all four files |
| T-06-OW | Tampering | accidental overwrite of frozen `agent-factory/README.md` | mitigate | Root README LINKS to the internal one (D-51), never overwrites; verify `git status` shows it unmodified |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure markdown (Package Legitimacy Audit: none) |
</threat_model>

<verification>
- README.md present with Acknowledgements + grugbrain.dev + Carson Gross + non-affiliation footer + `0.1.0` + link to `agent-factory/README.md`.
- NOTICE, CONTRIBUTING.md, docs/faq.md present with the manual's verbatim blocks.
- `! grep -rE '/grug([^o]|$)' README.md NOTICE CONTRIBUTING.md docs/faq.md` (no literal `/grug`).
- `agent-factory/README.md` and `docs/initial/` unmodified.
</verification>

<success_criteria>
BRAND-01 + BRAND-02 met: a clear-voice public README with the hero, Acknowledgements, non-affiliation footer, `0.1.0`, and a link to the untouched internal README; NOTICE/CONTRIBUTING/FAQ verbatim from the manual; the entire collateral set renders `/grugops` and never literal `/grug`.
</success_criteria>

<output>
Create `.planning/phases/06-validation-brand-dogfood/06-03-SUMMARY.md` when done.
</output>

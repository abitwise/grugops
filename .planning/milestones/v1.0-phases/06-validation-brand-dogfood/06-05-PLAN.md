---
phase: 06-validation-brand-dogfood
plan: 05
type: execute
wave: 2
depends_on: [06-01, 06-04]
files_modified:
  - examples/01-greenfield-bootstrap.md
  - examples/03-ticket-to-pr.md
  - docs/dogfood-human-runbook.md
autonomous: false
requirements: [DOG-01, DOG-02, EX-01]
user_setup:
  - service: claude-code-live-session
    why: "DOG-02 CC-native half (plugin marketplace install, plugin-cache pointer resolution, live PreToolUse hook firing, sub-agent spawn) requires a real interactive Claude Code session an executor cannot honestly self-perform"
    dashboard_config:
      - task: "Run the human runbook in docs/dogfood-human-runbook.md against the dogfood sample repo: marketplace install + /grugops:plan (D-31), attempt a guarded prod-deploy (SAFE-02), drive the ticket via the CC sub-agent spawn path, fill the parity table"
        location: "A live Claude Code session on the throwaway sample repo"
must_haves:
  truths:
    - "A fresh sample app (TS/Node+Fastify or Vue) is created in a sibling/temp dir OUTSIDE the grugops repo, grugops is installed via the sequential AGENTS.md path, bootstrapped, and one real ticket is driven idea→PR — captured as REAL"
    - "node scripts/validate-agent-factory.mjs passes on the dogfood sample's resulting tree (DOG-01)"
    - "examples/01-greenfield-bootstrap.md and examples/03-ticket-to-pr.md are the REAL captures, banner 'Real run — captured 2026-06-03', with live-CC-dependent checks marked 'pending human'"
    - "A human runbook absorbs the three live-CC-session items (D-31 plugin-cache pointer resolution, SAFE-02 live hook firing, CC sub-agent spawn) and a side-by-side dual-path parity checklist asserts same ticket / same handoffs / same gate verdict (DOG-02)"
    - "No frozen agent-factory/Phase-5 file is modified and the sample repo lives outside this repo tree"
  artifacts:
    - path: "examples/03-ticket-to-pr.md"
      provides: "REAL dogfood report = EX-01 example #3 (input → Orchestrator → board moves → handoffs → gate verdict → PR link) + the dual-path parity checklist"
      contains: "Real run"
    - path: "examples/01-greenfield-bootstrap.md"
      provides: "REAL greenfield-bootstrap slice captured from the dogfood = EX-01 example #1"
      contains: "Real run"
    - path: "docs/dogfood-human-runbook.md"
      provides: "Precise human checklist for the live-CC-session half (D-31, SAFE-02, sub-agent spawn) + parity table"
      contains: "pending human"
  key_links:
    - from: "dogfood sample repo (sibling dir)"
      to: "scripts/validate-agent-factory.mjs"
      via: "VALIDATE_ROOT pointed at the sample tree → must exit 0 (DOG-01)"
      pattern: "validate-agent-factory"
    - from: "examples/03-ticket-to-pr.md (sequential agent-run column)"
      to: "docs/dogfood-human-runbook.md (CC-native human-confirmed column)"
      via: "side-by-side parity: same ticket, same handoff files, same gate verdict"
      pattern: "pending human"
---

<objective>
Run the hybrid dogfood (DOG-01, DOG-02) and capture the two REAL examples (EX-01 completion). Agent-run-and-captured half: create a fresh minimal real app in grugops's greenfield default stack (TS/Node+Fastify or Vue) in a sibling/temp dir OUTSIDE this repo, install grugops via the portable sequential AGENTS.md path, bootstrap it, drive one real ticket idea→PR through `04-ticket-to-pr` → `05-pr-quality-gate`, run the validator on the result (must pass), and capture it as `examples/01-greenfield-bootstrap.md` + `examples/03-ticket-to-pr.md` (REAL RUN banners). Human-runbook half (cannot be agent-fabricated, D-38/D-41): author `docs/dogfood-human-runbook.md` for the three live-CC-session items (D-31 plugin-cache pointer resolution, SAFE-02 live hook firing, CC sub-agent spawn) plus a side-by-side dual-path parity checklist proving "only the dispatch differs, never the content."

Purpose: DOG-01/DOG-02 + the two REAL EX-01 examples — the acceptance gate. The honest agent-proven vs human-pending split is the point (no fabrication of marketplace installs or hook firings).
Output: `examples/01-greenfield-bootstrap.md`, `examples/03-ticket-to-pr.md`, `docs/dogfood-human-runbook.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-validation-brand-dogfood/06-RESEARCH.md
@.planning/phases/06-validation-brand-dogfood/06-PATTERNS.md
@.planning/phases/06-validation-brand-dogfood/06-VALIDATION.md
@.planning/phases/05-packaging-adapters-install-distribution/05-HUMAN-UAT.md
@.planning/phases/06-validation-brand-dogfood/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the agent-driven sequential dogfood + capture REAL examples #1 and #3</name>
  <files>examples/01-greenfield-bootstrap.md, examples/03-ticket-to-pr.md</files>
  <read_first>
    - `AGENTS.md` (the portable substrate the sequential-path run loads first) and `agent-factory/roles/orchestrator.md` (the entry the dogfood acts as)
    - `agent-factory/workflows/00-bootstrap-greenfield.md` (the bootstrap flow to run on the sample), `04-ticket-to-pr.md` + `05-pr-quality-gate.md` (the idea→PR flow + gate; gate terminal token `READY_FOR_HUMAN_REVIEW`)
    - `install/install.sh` + `install/install.test.sh:62,100,128` (the `GRUGOPS_SRC=<this repo>` / `TARGET=<sample dir>` env-override install pattern to install grugops INTO the sibling sample without polluting this repo)
    - `scripts/validate-agent-factory.mjs` (the validator from Plan 06-01 — run it with `VALIDATE_ROOT` pointed at the sample's resulting tree; must exit 0, DOG-01)
    - `.planning/phases/06-validation-brand-dogfood/06-RESEARCH.md` "Dogfood Architecture → Agent-run-and-captured half" + "Common Pitfalls" 4 (no fabrication) and 5 (sample OUTSIDE the repo) and the Open-Questions sample-shape recommendation (`GET /version` Fastify endpoint or a small Vue component)
    - `.planning/phases/06-validation-brand-dogfood/06-PATTERNS.md` "examples/01..05-*.md" (banners + concrete renders) and the "No-fabrication / fail-closed" shared pattern
    - `plans/board.md:76-98` (real column headings for the captured board moves)
  </read_first>
  <action>
    Create the sample app in a sibling/temp dir OUTSIDE `/Users/olgeroeselg/Projects/public/grugops` (e.g. `/tmp/grugops-dogfood-<ts>` or `../grugops-sample`) — NEVER inside this repo (Pitfall 5). Smallest credible real change on grugops's greenfield default: a tiny TS/Node+Fastify app (e.g. existing `GET /health`, add `GET /version` returning the app version) OR a small Vue app (add one component) — your discretion (D-39), but the gate (lint/typecheck/unit/build) must be GENUINELY runnable so the captured verdict is real. Install grugops onto it via the sequential AGENTS.md path using the install harness's `GRUGOPS_SRC`/`TARGET` env overrides (install FROM this repo INTO the sample). Acting as Orchestrator (read `AGENTS.md` → `orchestrator.md`): run `00-bootstrap-greenfield`, then drive ONE real ticket idea→PR through `04-ticket-to-pr` → `05-pr-quality-gate`. CAPTURE the real input, Orchestrator decision/routing, board moves (real column names), handoff files produced, gate verdict, and PR link (a real branch+PR if a remote exists; otherwise a local branch+diff recorded HONESTLY as such). Run `node scripts/validate-agent-factory.mjs` with `VALIDATE_ROOT` at the sample's resulting tree — it MUST pass (DOG-01); record the real exit status. Write `examples/03-ticket-to-pr.md` (the DOGFOOD report = EX-01 #3) with banner `> Real run — captured 2026-06-03`, structured input → Orchestrator decision → board moves → handoffs → gate verdict → PR link, rendering `/grugops` only; mark any live-CC-session-dependent check `pending human` (do NOT simulate). Write `examples/01-greenfield-bootstrap.md` (the bootstrap slice = EX-01 #1) with the same REAL RUN banner. NO fabrication: only capture what actually ran; everything requiring a live CC session goes to Task 2's runbook. Do NOT commit the sample app into this repo; do NOT modify any frozen `agent-factory/`/Phase-5 file.
  </action>
  <verify>
    <automated>test -f examples/01-greenfield-bootstrap.md && test -f examples/03-ticket-to-pr.md && grep -q 'Real run' examples/01-greenfield-bootstrap.md && grep -q 'Real run' examples/03-ticket-to-pr.md && grep -qE 'READY_FOR_HUMAN_REVIEW|gate' examples/03-ticket-to-pr.md && ! grep -rE '/grug([^o]|$)' examples/01-greenfield-bootstrap.md examples/03-ticket-to-pr.md && node scripts/validate-agent-factory.mjs >/dev/null 2>&1 && echo "DOGFOOD-CAPTURE-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `examples/01-greenfield-bootstrap.md` and `examples/03-ticket-to-pr.md` exist with the `Real run` banner (D-47).
    - `examples/03-ticket-to-pr.md` records a real gate verdict (`grep -qE 'READY_FOR_HUMAN_REVIEW|BLOCKED_NEEDS_FIX|SPLIT_REQUIRED'`) and a PR/branch reference, with live-CC-session checks marked `pending human`.
    - The validator passed on the sample tree (recorded in the report) AND `node scripts/validate-agent-factory.mjs` still exits 0 on grugops's own tree.
    - `! grep -rE '/grug([^o]|$)' examples/01-greenfield-bootstrap.md examples/03-ticket-to-pr.md`.
    - `git status --porcelain agent-factory/` is empty (frozen core untouched) and no sample-app source dir was committed under the repo root.
  </acceptance_criteria>
  <done>The sequential dogfood ran on an out-of-repo sample, the validator passed on its tree, and examples #1 + #3 are captured as REAL with live-CC checks marked pending human, `/grugops` only, no fabrication.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Author the human runbook + dual-path parity, then run the live-CC-session half</name>
  <files>docs/dogfood-human-runbook.md, examples/03-ticket-to-pr.md</files>
  <read_first>
    - `.planning/phases/05-packaging-adapters-install-distribution/05-HUMAN-UAT.md` (the two Phase-5-deferred live tests to absorb verbatim in intent: D-31 plugin-cache pointer resolution, SAFE-02 live hook firing)
    - `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` (the plugin/marketplace install surface the runbook drives: `/plugin marketplace add` + `/plugin install grugops@grugops`)
    - `.claude/agents/grugops-orchestrator.md` + `.claude/skills/grugops/SKILL.md` (the CC sub-agent spawn path the runbook exercises)
    - `hooks/hooks.json` + `hooks/guard.mjs` (the SAFE-02 PreToolUse guard whose live firing the runbook confirms — denies a matched prod-deploy absent `GRUGOPS_PROD_DEPLOY_APPROVED`)
    - `.planning/phases/06-validation-brand-dogfood/06-RESEARCH.md` "Dogfood Architecture → Human-runbook half" + "Security Domain" V14 (do NOT set the approval env var; do NOT run a real deploy)
    - `examples/03-ticket-to-pr.md` (the REAL sequential capture from Task 1 — its handoff files + gate verdict are the parity baseline)
  </read_first>
  <what-built>
    `docs/dogfood-human-runbook.md` — a precise checklist for the three live-CC-session items the executor cannot honestly self-perform (D-38/D-41), plus a side-by-side dual-path parity checklist in `examples/03-ticket-to-pr.md` (sequential agent-proven column vs CC-native human-confirmed column: same ticket / same handoff filenames / same gate verdict). The runbook absorbs the two Phase-5-deferred `05-HUMAN-UAT.md` tests verbatim in intent and adds the CC sub-agent spawn path. It explicitly instructs the human to NOT set `GRUGOPS_PROD_DEPLOY_APPROVED` and NOT run a real deploy (V14 / SAFE-02 stays mechanical).
  </what-built>
  <how-to-verify>
    In a live Claude Code session on the dogfood sample repo:
    1. Plugin-cache pointer resolution (D-31): run `/plugin marketplace add <owner>/grugops` then `/plugin install grugops@grugops`, then `/grugops:plan` — confirm it produces planning output, NOT a path error (proves the repo-relative `agent-factory/roles/*.md` pointers resolve against the user's repo, not the broken plugin cache). Record PASS/FAIL.
    2. Live PreToolUse hook firing (SAFE-02): attempt a matched guarded prod-deploy (e.g. `kubectl apply -f x`) WITHOUT setting `GRUGOPS_PROD_DEPLOY_APPROVED` — confirm the PreToolUse hook DENIES it with the clear-voice deny message. Record PASS/FAIL.
    3. CC sub-agent spawn path: drive the SAME ticket (from Task 1) through the CC sub-agent path (`settings.json` `agent:` → `Agent` tool spawn) — confirm the SAME roles run, the SAME handoff filenames are produced, and the SAME gate verdict results as the captured sequential run.
    4. Fill the side-by-side parity table in `examples/03-ticket-to-pr.md`: agent-proven (sequential) column from Task 1, human-confirmed (CC-native) column from steps 1-3 — same ticket / same handoffs / same gate verdict = "only the dispatch differs, never the content."
  </how-to-verify>
  <action>
    Author `docs/dogfood-human-runbook.md` with the three live-CC-session checks above as a numbered, precise checklist (exact commands, expected outcomes, PASS/FAIL slots), absorbing the `05-HUMAN-UAT.md` D-31 + SAFE-02 tests verbatim in intent and adding the sub-agent spawn path. Add the side-by-side dual-path parity checklist/table to `examples/03-ticket-to-pr.md` with an agent-proven (sequential, filled from Task 1) column and a human-confirmed (CC-native) column whose cells are marked `pending human` until the live run fills them. State the V14 safety constraint in clear voice (never set `GRUGOPS_PROD_DEPLOY_APPROVED`; never run a real deploy). Render `/grugops`/`/grugops:<op>` only — never literal `/grug`. The runbook is the intended design (humans decide, agents execute), not a degradation — frame it as such.
  </action>
  <resume-signal>Type "approved" once the live-CC-session runbook steps are run and the parity table is filled (or "deferred" to leave them marked pending human for the milestone-close UAT).</resume-signal>
  <verify>
    <automated>test -f docs/dogfood-human-runbook.md && grep -qi 'pending human' docs/dogfood-human-runbook.md && grep -qiE 'plugin (marketplace )?install|marketplace add' docs/dogfood-human-runbook.md && grep -qiE 'PreToolUse|hook|kubectl|deploy' docs/dogfood-human-runbook.md && grep -qiE 'sub-agent|spawn|Agent' docs/dogfood-human-runbook.md && grep -qi 'GRUGOPS_PROD_DEPLOY_APPROVED' docs/dogfood-human-runbook.md && grep -qi 'pending human' examples/03-ticket-to-pr.md && ! grep -rE '/grug([^o]|$)' docs/dogfood-human-runbook.md && echo "RUNBOOK-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/dogfood-human-runbook.md` exists and covers all three live-CC-session items: plugin-cache pointer resolution (D-31), live PreToolUse hook firing (SAFE-02), and the CC sub-agent spawn path.
    - The runbook states the V14 safety constraint (`grep -qi 'GRUGOPS_PROD_DEPLOY_APPROVED'` AND instructs NOT to set it / NOT to run a real deploy).
    - `examples/03-ticket-to-pr.md` contains the side-by-side dual-path parity checklist with a human-confirmed column marked `pending human` until the live run.
    - `! grep -rE '/grug([^o]|$)' docs/dogfood-human-runbook.md` and it renders `/grugops`/`/grugops:<op>`.
    - No frozen `agent-factory/` or Phase-5 file modified.
  </acceptance_criteria>
  <done>The human runbook covers the three live-CC items with the V14 safety constraint, the parity checklist exists with human cells marked pending human, and the live steps are run-or-deferred per the resume signal — the honest agent-proven/human-pending split is visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dogfood sample repo → grugops kit | The kit runs against an external throwaway repo; the sample's data is discarded, never committed here |
| executor → live-CC-session actions | Marketplace install, plugin-cache resolution, hook firing, sub-agent spawn cannot be honestly self-driven; they cross into the human-runbook boundary |
| dogfood → production | autonomy=pr; no real deploy; SAFE-02 guard stays mechanical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-FAB2 | Repudiation (audit-trail integrity — the whole value prop) | fabricated "REAL RUN" / invented PR link / simulated marketplace install or hook firing | mitigate | D-38 honest split: agent captures ONLY what actually ran (sequential + validator); everything needing a live CC session is marked `pending human` in the runbook; never simulate |
| T-06-V14 | Elevation / Tampering | dogfood accidentally performing a real prod deploy | mitigate | autonomy=pr; runbook explicitly says NEVER set `GRUGOPS_PROD_DEPLOY_APPROVED` and NEVER run a real deploy; sample has no prod target; PR-only, human holds merge/deploy (V14) |
| T-06-POLLUTE | Tampering | sample app created inside the grugops repo, polluting the frozen tree | mitigate | Sample lives in a sibling/temp dir OUTSIDE the repo (Pitfall 5); verify `git status agent-factory/` clean + no sample dir committed |
| T-06-SC | Tampering | npm/pip/cargo installs (sample app deps) | mitigate | Sample-app deps are installed in the OUT-OF-REPO sample dir only; the grugops repo gains no `package.json`/`node_modules`; legitimacy of the sample's own deps is out of grugops's scope (a throwaway app) and gated by the human runbook's PR-only posture |
</threat_model>

<verification>
- Sample app created OUTSIDE the repo; `git status --porcelain agent-factory/` empty; no sample dir committed under the repo root.
- `node scripts/validate-agent-factory.mjs` passes on the sample's resulting tree (DOG-01) AND on grugops's own tree.
- `examples/01-greenfield-bootstrap.md` + `examples/03-ticket-to-pr.md` exist as REAL captures with live-CC checks marked `pending human`.
- `docs/dogfood-human-runbook.md` covers D-31 + SAFE-02 + sub-agent spawn, states the V14 constraint, and the parity checklist exists.
- `! grep -rE '/grug([^o]|$)'` across all three files.
</verification>

<success_criteria>
DOG-01 + DOG-02 + EX-01 (real half) met: the chain is proven idea→PR on an out-of-repo sample via the agent-run sequential path with the validator passing (DOG-01), the two REAL examples (#1, #3) are captured honestly with live-CC checks marked pending human, and the human runbook + side-by-side parity checklist assert "only the dispatch differs, never the content" across the sequential (agent-proven) and CC-native (human-confirmed) paths (DOG-02) — the acceptance gate, with the no-fabrication split visible.
</success_criteria>

<output>
Create `.planning/phases/06-validation-brand-dogfood/06-05-SUMMARY.md` when done.
</output>

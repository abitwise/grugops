---
phase: 06-validation-brand-dogfood
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/02-brownfield-bootstrap.md
  - examples/04-sprint-cycle.md
  - examples/05-release-run.md
autonomous: true
requirements: [EX-01]
must_haves:
  truths:
    - "Three illustrative example runs exist: brownfield bootstrap, a full sprint cycle, and a release run"
    - "Each opens with the 'Illustrative run — expected output, not a captured session' banner and uses obvious placeholder IDs (ABC-001, REL-0007, <PR-link>)"
    - "The sprint example renders board snapshots + a velocity/metrics line; the release example shows completed traceability rows"
    - "Every example renders /grugops (dash standalone) / /grugops:<op> (colon) — never literal /grug"
  artifacts:
    - path: "examples/02-brownfield-bootstrap.md"
      provides: "ILLUSTRATIVE narration of 01-bootstrap-brownfield (input → Orchestrator → mappers → architect → security/NFR scan → safe first tickets)"
      contains: "Illustrative run"
    - path: "examples/04-sprint-cycle.md"
      provides: "ILLUSTRATIVE sprint narration (refinement→planning→2 tickets→daily sweeps→review→retro) with board snapshots + velocity line"
      contains: "Illustrative run"
    - path: "examples/05-release-run.md"
      provides: "ILLUSTRATIVE release narration (12-release: approval gate → human-confirmed deploy → Done) with completed traceability rows"
      contains: "Illustrative run"
  key_links:
    - from: "examples/02|04|05-*.md"
      to: "agent-factory/workflows/01,07-12 + plans/board.md + plans/traceability.md + plans/metrics.md"
      via: "narration of the frozen flow's spine using real column/handoff/metric names"
      pattern: "/grugops"
    - from: "examples/05-release-run.md"
      to: "plans/traceability.md row shape"
      via: "completed traceability rows rendered in the documented | ticket | … | Done | form"
      pattern: "\\|.*Done.*\\|"
---

<objective>
Author the three ILLUSTRATIVE example runs (EX-01 partial — the two REAL-captured examples #1/#3 fall out of the dogfood in Plan 05): `examples/02-brownfield-bootstrap.md`, `examples/04-sprint-cycle.md`, `examples/05-release-run.md`. Each narrates a frozen workflow flow at medium depth (input → Orchestrator decision/routing → board moves → expected files/handoffs with representative snippets → trace/metrics line), opens with the honesty banner (D-47), uses placeholder IDs, and renders `/grugops` (never literal `/grug`, D-49).

Purpose: EX-01 — three of the five example runs. These narrate flows impractical to run live (existing-repo brownfield, multi-ticket sprint, full release), so they are clearly-labeled "expected," not fake captures.
Output: `examples/02-brownfield-bootstrap.md`, `examples/04-sprint-cycle.md`, `examples/05-release-run.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-validation-brand-dogfood/06-RESEARCH.md
@.planning/phases/06-validation-brand-dogfood/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author examples/02-brownfield-bootstrap.md (ILLUSTRATIVE)</name>
  <files>examples/02-brownfield-bootstrap.md</files>
  <read_first>
    - `agent-factory/workflows/01-bootstrap-brownfield.md` (the frozen flow this example renders — narrate its spine, never redesign it: Orchestrator → Brownfield Mapper → AGENTS.md Scribe → Architect review → Security/NFR high-risk scan → safe first tickets)
    - `agent-factory/roles/orchestrator.md` (the Decision output shape the example shows for the Orchestrator's routing)
    - `plans/board.md:76-98` (the real `## <Column> (WIP …)` headings to use verbatim in any board move)
    - `.planning/phases/06-validation-brand-dogfood/06-PATTERNS.md` "examples/01..05-*.md" table (per-file analog + banner) and "Concrete renders to copy from frozen sources"
    - `.planning/phases/06-validation-brand-dogfood/06-RESEARCH.md` "Example Run Map (EX-01)" row #2 + the D-47/D-48 banner/structure rules
  </read_first>
  <action>
    Narrate the §7.2 `01-bootstrap-brownfield` flow at medium depth (D-48): input (an existing repo handed to grugops) → Orchestrator decision/routing → board moves (using the real column names from `plans/board.md`) → expected files/handoffs with representative snippets (NOT full dumps; cite REAL handoff filenames only, never invent) → the Security/NFR high-risk scan result token (`PASS`|`PASS_WITH_RISKS`|`BLOCKED`) → safe first tickets. Open with the EXACT banner `> Illustrative run — expected output, not a captured session` and use obvious placeholder IDs (`ABC-001`, etc.). Render the install/invocation as `/grugops "<request>"` (dash standalone) — never literal `/grug`. Keep snippets representative, not full file bodies.
  </action>
  <verify>
    <automated>test -f examples/02-brownfield-bootstrap.md && grep -q 'Illustrative run' examples/02-brownfield-bootstrap.md && ! grep -rE '/grug([^o]|$)' examples/02-brownfield-bootstrap.md && echo "EX02-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f examples/02-brownfield-bootstrap.md` succeeds.
    - `grep -q 'Illustrative run' examples/02-brownfield-bootstrap.md` (honesty banner present, D-47).
    - `grep -q 'ABC-' examples/02-brownfield-bootstrap.md` (placeholder ID used).
    - `! grep -rE '/grug([^o]|$)' examples/02-brownfield-bootstrap.md` and `grep -q '/grugops' examples/02-brownfield-bootstrap.md`.
    - At least one real frozen handoff filename appears (e.g. `grep -qE 'architecture-handoff|security-nfr-handoff|ticket-ready-packet' examples/02-brownfield-bootstrap.md`).
  </acceptance_criteria>
  <done>Brownfield example exists, banner-labeled, placeholder IDs, real handoff/column names, `/grugops` only, medium-depth.</done>
</task>

<task type="auto">
  <name>Task 2: Author examples/04-sprint-cycle.md + examples/05-release-run.md (ILLUSTRATIVE)</name>
  <files>examples/04-sprint-cycle.md, examples/05-release-run.md</files>
  <read_first>
    - `agent-factory/workflows/07-backlog-refinement.md`, `08-sprint-planning.md`, `09-daily-sweep.md`, `10-sprint-review.md`, `11-retro.md` (the frozen sprint ceremony spine #4 renders) and `12-release.md` (the release flow #5 renders — approval gate → human-confirmed deploy → Done)
    - `plans/board.md:76-98` (real column headings for board snapshots) and `plans/metrics.md` (the §6.5 metric vocabulary: Throughput, Cycle time, Velocity, WIP, Blocked time — use a real subset, invent no metric)
    - `plans/traceability.md:36-37` (the header row + documented row shape `| ABC-012 | … | Done |` the release example's completed rows must match)
    - `agent-factory/workflows/04-ticket-to-pr.md:30-40` (board-move sentence shape + `Cycle time`/`WIP` metric vocabulary the sprint tickets reference)
    - `.planning/phases/06-validation-brand-dogfood/06-RESEARCH.md` "Example Run Map" rows #4/#5 + "Concrete renders to copy from frozen sources"
    - `.planning/phases/06-validation-brand-dogfood/06-PATTERNS.md` "examples/01..05-*.md" + "Concrete renders" list
  </read_first>
  <action>
    `examples/04-sprint-cycle.md`: narrate the sprint flow (refinement → planning → 2 tickets driven through `04-ticket-to-pr` → daily sweeps → review → retro) at medium depth. Include at least two board snapshots using the REAL column headings (`## In Development (WIP 1/3)` etc.) and one velocity/metrics line drawn from the real §6.5 vocabulary (e.g. Velocity / Throughput / Cycle time). `examples/05-release-run.md`: narrate `12-release` (Ready to Release → Release Manager → approval gate → SemVer/changelog/rollback/approval → human-confirmed deploy → Done). Use the `REL-0007` placeholder and `<PR-link>` placeholder; render the named-human deploy gate in CLEAR voice (it is a safety topic — no caveman); show completed traceability rows in the documented `| ABC-… | … | Done |` form. Both files: open with the EXACT banner `> Illustrative run — expected output, not a captured session`, use placeholder IDs (`ABC-001`/`REL-0007`/`<PR-link>`), structure as input → Orchestrator decision → board moves → expected files/handoffs (representative snippets) → trace/metrics line, and render `/grugops` / `/grugops:<op>` only — never literal `/grug`. Cite only REAL handoff filenames and REAL metric names.
  </action>
  <verify>
    <automated>test -f examples/04-sprint-cycle.md && test -f examples/05-release-run.md && grep -q 'Illustrative run' examples/04-sprint-cycle.md && grep -q 'Illustrative run' examples/05-release-run.md && grep -qE 'WIP' examples/04-sprint-cycle.md && grep -qiE 'velocity|throughput|cycle time' examples/04-sprint-cycle.md && grep -q 'REL-0007' examples/05-release-run.md && grep -qE '\| *Done *\|' examples/05-release-run.md && ! grep -rE '/grug([^o]|$)' examples/04-sprint-cycle.md examples/05-release-run.md && echo "EX0405-OK"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist with the `Illustrative run` banner.
    - `examples/04-sprint-cycle.md` contains ≥1 board snapshot with a real `(WIP …)` heading AND a velocity/metrics line (`grep -qiE 'velocity|throughput|cycle time'`).
    - `examples/05-release-run.md` contains `REL-0007`, the human-confirmed deploy gate in clear voice, and ≥1 completed traceability row (`grep -qE '\| *Done *\|'`).
    - `! grep -rE '/grug([^o]|$)' examples/04-sprint-cycle.md examples/05-release-run.md` and both contain `/grugops`.
    - `git status --porcelain agent-factory/ plans/` shows no modification (frozen flows + state plane untouched).
  </acceptance_criteria>
  <done>Sprint + release illustrative examples exist, banner-labeled, with real board snapshots + velocity line (sprint) and completed traceability rows + clear-voice human deploy gate (release), `/grugops` only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| illustrative examples → readers | Readers could mistake "expected" output for a real capture; risk surface is honesty/repudiation, not code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-FAB | Repudiation (audit-trail integrity) | illustrative example mislabeled as real | mitigate | Every illustrative example opens with the exact "Illustrative run — expected output, not a captured session" banner + obvious placeholder IDs (D-47); verify grep asserts the banner |
| T-06-DRIFT | Tampering (single-source drift) | inventing flow steps / handoff names / metrics | mitigate | Narrate only the frozen §7 flow spines + REAL handoff filenames + REAL §6.5 metric names; cite, never invent (RESEARCH "Don't Hand-Roll") |
| T-06-CMD | Repudiation (brand integrity) | literal `/grug` leaking into examples | mitigate | Render `/grugops`/`/grugops:<op>` only; verify `! grep -rE '/grug([^o]|$)'` over all three files |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure markdown (Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `ls examples/02-brownfield-bootstrap.md examples/04-sprint-cycle.md examples/05-release-run.md` — all present.
- Each opens with the `Illustrative run` banner (D-47).
- Sprint example has a board snapshot + velocity line; release example has completed traceability rows + clear-voice deploy gate.
- `! grep -rE '/grug([^o]|$)' examples/` — no literal `/grug`.
- `agent-factory/` + `plans/` unmodified.
</verification>

<success_criteria>
EX-01 (illustrative half) met: three medium-depth example runs (brownfield bootstrap, sprint cycle with board snapshots + velocity line, release run with completed traceability rows), each clearly banner-labeled as expected output with placeholder IDs, narrating only frozen flows and real names, rendering `/grugops` throughout.
</success_criteria>

<output>
Create `.planning/phases/06-validation-brand-dogfood/06-04-SUMMARY.md` when done.
</output>

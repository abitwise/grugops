---
phase: 08-two-root-installer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - install/install.two-root.test.sh
autonomous: true
requirements: [INSTALL-03, INSTALL-04]

must_haves:
  truths:
    - "A separate Phase-8 test harness exists at install/install.two-root.test.sh and never edits install/install.test.sh"
    - "The harness asserts the full two-root contract: kit copy to $GRUGOPS_HOME, adapter materialization, full state seed incl. plans/handoffs/, never-clobber, two-root idempotency, DRY_RUN no-mutation, copy-default (no symlinks), --target from arbitrary CWD, --yes/non-TTY, the D-07 self-checkout guard, two-root uninstall, and sh/Node byte-parity"
    - "Every assertion runs hermetically via GRUGOPS_HOME / GRUGOPS_SRC / TARGET / INSTALL_MODE=copy overrides under mktemp -d with trap cleanup, never touching the real repo or $HOME"
    - "The harness exercises the self-checkout guard against a throwaway clone-shaped fixture, never against the real grugops checkout"
  artifacts:
    - path: "install/install.two-root.test.sh"
      provides: "Phase-8-local POSIX harness covering INSTALL-03 + INSTALL-04 + D-03/D-06/D-07/D-08/D-09"
      contains: "snapshot"
      min_lines: 120
  key_links:
    - from: "install/install.two-root.test.sh"
      to: "install/install.sh"
      via: "hermetic invocation with GRUGOPS_HOME/GRUGOPS_SRC/TARGET/INSTALL_MODE=copy"
      pattern: "GRUGOPS_HOME="
    - from: "install/install.two-root.test.sh"
      to: "install/install.test.sh"
      via: "structure copied (pass/fail/snapshot/trap), the original left UNEDITED"
      pattern: "mktemp -d"
---

<objective>
Create the Phase-8-local regression harness `install/install.two-root.test.sh` that encodes every new two-root behavior as an automated assertion. This is the Wave-0 test scaffold the installer rewrite (Plan 03) and the uninstall/docs plan (Plan 04) verify against.

Because nyquist_validation is enabled and `install/install.test.sh` MUST NOT be edited this phase (its split rewrite is Phase 9), the new assertions live here in a separate harness. The harness is written FIRST against the intended behavior; it ships RED (the installer does not yet copy the kit, materialize adapters, or seed state) and goes GREEN as Plan 03 lands. It mirrors `install.test.sh`'s proven idioms (`pass`/`fail`/`FAILS`, content-addressed `snapshot`, `mktemp -d` + `trap cleanup EXIT INT TERM`, hermetic env overrides) but extends the snapshot to cover BOTH roots.

Purpose: a fast (<30s) feedback loop that proves the two-root contract without modifying the frozen harness.
Output: a self-contained POSIX harness exercising INSTALL-03 + INSTALL-04 and D-03/D-06/D-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-two-root-installer/08-CONTEXT.md
@.planning/phases/08-two-root-installer/08-RESEARCH.md
@.planning/phases/08-two-root-installer/08-PATTERNS.md
@.planning/phases/08-two-root-installer/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write install/install.two-root.test.sh — harness skeleton, two-root snapshot, kit-copy + materialization + seed + copy-default + idempotency + DRY_RUN assertions</name>
  <files>install/install.two-root.test.sh</files>
  <read_first>
    - install/install.test.sh (the analog to copy — `set -eu`, `pass`/`fail`/`FAILS`, portable `DIFF`, `mktemp -d` + `trap cleanup EXIT INT TERM` at lines 18-34; the content-addressed `snapshot()` at lines 48-53 which covers ONE dir and must be extended to two; the hermetic invocation pattern at lines 62 + 128-129; the double-install zero-diff idempotency pattern at lines 60-70; the sh/Node parity pattern at lines 125-140. DO NOT EDIT THIS FILE.)
    - install/install.sh (the behavioral target — current arg/env block at lines 33-43, the SKILLS/AGENT_REL constants at lines 42-44, the run section at lines 240-281; this defines the env override surface GRUGOPS_SRC/TARGET/DRY_RUN/INSTALL_MODE the harness drives)
    - .planning/phases/08-two-root-installer/08-RESEARCH.md (§ Validation Architecture → the Phase Requirements → Test Map table with the exact hermetic commands; § Wave 0 Gaps)
    - .planning/phases/08-two-root-installer/08-VALIDATION.md (the Per-Task Verification Map with the 16 assertions to encode)
  </read_first>
  <action>
    Create `install/install.two-root.test.sh` (`#!/usr/bin/env sh`, `set -eu`, house style: `printf` not `echo -e`). Copy the skeleton from `install.test.sh:18-34` verbatim in spirit: `SCRIPT_DIR`/`REPO_ROOT` resolution, `FAILS` counter, `pass()`/`fail()` helpers, portable `DIFF`, `WORK=$(mktemp -d)`, `cleanup() { rm -rf -- "$WORK"; }`, `trap cleanup EXIT INT TERM`. Extend the `snapshot()` helper (from `install.test.sh:48-53`) so it produces a content-addressed manifest (`cksum`-based, LINK marker for symlinks) for an arbitrary dir — call it once per root so BOTH `$TARGET` and `$GRUGOPS_HOME` can be diffed.
    Drive every install via the hermetic env: `INSTALL_MODE=copy GRUGOPS_SRC="$REPO_ROOT" GRUGOPS_HOME="$WORK/home" TARGET="$WORK/app1" sh "$SCRIPT_DIR/install.sh" --yes`. Encode these assertions (each a `pass`/`fail`):
    - INSTALL-04 kit copy: after install, `[ -f "$WORK/home/agent-factory/roles/orchestrator.md" ]`.
    - INSTALL-04 materialization: the materialized `KIT=` line in `$WORK/app1/.claude/agents/grugops-orchestrator.md` resolves to `$WORK/home/agent-factory` (grep the `grugops:materialized-kit` block; assert it names the resolved absolute path). Repeat for `$WORK/app1/.claude/skills/grugops/SKILL.md`.
    - INSTALL-04 seed: assert each seeded path exists — `$WORK/app1/.grugops/factory.config.json`, `$WORK/app1/.grugops/install.json` (the marker), `$WORK/app1/plans/board.md`, `$WORK/app1/plans/handoffs/` (dir), `$WORK/app1/memory-bank/00-index.md`.
    - INSTALL-04 never-clobber: pre-write `$WORK/app2/.grugops/factory.config.json` with a sentinel string, install into `$WORK/app2`, assert the sentinel survives.
    - INSTALL-04 idempotency (two roots): `snapshot` `$WORK/app1` AND `$WORK/home` after a first install, install a SECOND time into the same target+home, snapshot again, assert both diffs are empty (this is the materialized-adapter + kit-copy + marker zero-diff contract — Pitfall 1; if the marker carries a timestamp it WILL fail, which correctly surfaces the Plan-03 idempotency decision).
    - INSTALL-04 DRY_RUN: snapshot both roots, run `DRY_RUN=1 … install.sh --yes` into a FRESH target/home, assert neither root was created/mutated (diff empty / dirs absent).
    - INSTALL-04 copy-default: after a default install (no INSTALL_MODE override — but the harness passes `INSTALL_MODE=copy` for determinism; ALSO add one run WITHOUT INSTALL_MODE to prove the new default is copy), `find "$WORK"/app* "$WORK"/home -type l` is empty.
    Each assertion must stand alone (its own fresh target dir under `$WORK`) so a failure does not cascade. End with `[ "$FAILS" -eq 0 ]` exit logic (`exit 1` on any fail) mirroring `install.test.sh`.
  </action>
  <verify>
    <automated>sh -n install/install.two-root.test.sh && grep -q 'mktemp -d' install/install.two-root.test.sh && grep -q 'GRUGOPS_HOME=' install/install.two-root.test.sh && grep -q 'snapshot' install/install.two-root.test.sh && grep -q 'grugops:materialized-kit\|materialized' install/install.two-root.test.sh && grep -q 'plans/handoffs' install/install.two-root.test.sh && grep -q 'install.json' install/install.two-root.test.sh && git diff --quiet -- install/install.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `sh -n install/install.two-root.test.sh` parses without error (valid POSIX).
    - Source assertion: the harness contains `mktemp -d`, a `trap cleanup EXIT`, and drives installs with `GRUGOPS_HOME=`/`GRUGOPS_SRC=`/`TARGET=`/`INSTALL_MODE=copy` (hermetic, never the real repo or `$HOME`).
    - Source assertion: the harness references the materialized-kit block, `plans/handoffs`, and `install.json` (proves it asserts materialization + seed + marker).
    - Source assertion: a `snapshot` helper exists and is invoked for both `$TARGET` and `$GRUGOPS_HOME` (two-root diff coverage).
    - Behavior assertion: `git diff --quiet -- install/install.test.sh` succeeds (the frozen harness is UNEDITED).
    - Behavior assertion (RED expected this wave): `sh install/install.two-root.test.sh` runs to completion (no crash) and reports FAILs for the not-yet-implemented behaviors — proving the harness is wired before Plan 03 makes it GREEN. (Do not gate the wave on it passing; gate on it parsing + running.)
  </acceptance_criteria>
  <done>`install/install.two-root.test.sh` exists, parses, runs hermetically under `mktemp -d`, extends `snapshot` to both roots, and encodes the kit-copy / materialization / seed / never-clobber / two-root-idempotency / DRY_RUN / copy-default assertions; `install/install.test.sh` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add INSTALL-03 arg-parsing, the D-07 self-checkout-guard fixture, two-root uninstall, and sh/Node parity assertions to the harness</name>
  <files>install/install.two-root.test.sh</files>
  <read_first>
    - install/install.two-root.test.sh (the file being modified — built in Task 1)
    - install/install.test.sh (the sh/Node parity pattern at lines 125-140: install sh→$A, node→$B with identical env, snapshot + diff, SKIP not FAIL when `node` absent — copy this idiom)
    - install/uninstall.sh (the two-root reversal target — `is_protected` at lines 54-64, `remove_file` at 67-83, the AGENTS.md grugops-owned exception at lines 228-247; the harness asserts uninstall keeps the kit + seeded config and removes only the marker + adapters)
    - .planning/phases/08-two-root-installer/08-RESEARCH.md (§ Validation Architecture INSTALL-03 rows + the self-checkout-guard fixture in Wave 0 Gaps; Pattern 5 self-checkout predicate + the exact refuse message form)
  </read_first>
  <action>
    Extend `install/install.two-root.test.sh` with the INSTALL-03 + guard + uninstall + parity assertions:
    - INSTALL-03 arbitrary CWD: run the installer from a temp CWD (`cd "$WORK/elsewhere" && sh "$REPO_ROOT/install/install.sh" --target "$WORK/app3" --yes`); assert the adapters land in `$WORK/app3/.claude/agents/grugops-orchestrator.md` (proves `--target` works from any working directory, not the obscure `TARGET=` env).
    - INSTALL-03 --yes/non-TTY: run with stdin redirected from `/dev/null` plus `--yes`; assert exit 0 and that the run does not hang (the harness inherently blocks if it hangs — keep the invocation simple so a hang is observable; assert the adapter exists after).
    - D-07 self-checkout guard: build a throwaway clone-shaped fixture at `$WORK/fakeclone` carrying `install/install.sh` (a copy) AND `agent-factory/VERSION` (so it trips the source-marker predicate) WITHOUT being the real repo. Assert: (a) `GRUGOPS_SRC="$WORK/fakeclone" TARGET="$WORK/fakeclone" sh "$REPO_ROOT/install/install.sh" --yes` exits NONZERO and prints the refuse message naming `--allow-self`; (b) the same invocation WITH `--allow-self` proceeds (exit 0). NEVER point the guard test at `$REPO_ROOT`.
    - D-06 two-root uninstall: install into a fresh target+home, then run `GRUGOPS_HOME="$WORK/home" TARGET="$WORK/app4" sh "$REPO_ROOT/install/uninstall.sh"`; assert `$WORK/home/agent-factory` still exists (shared kit never removed), `$WORK/app4/.grugops/factory.config.json` still exists (seeded user state survives), and `$WORK/app4/.grugops/install.json` is GONE (marker removed) and the `.claude` adapters are GONE.
    - sh/Node byte-parity: copy the `install.test.sh:125-140` idiom — install sh→`$WORK/A`(home `$WORK/homeA`), node→`$WORK/B`(home `$WORK/homeB`) with identical env; `snapshot` + diff BOTH the target trees AND the home trees AND the `install.json` marker bytes; assert empty diff. If `command -v node` fails, `pass` with a "(skipped — node absent)" note rather than `fail`.
    Keep each assertion isolated under its own `$WORK` subdir.
  </action>
  <verify>
    <automated>sh -n install/install.two-root.test.sh && grep -q 'allow-self' install/install.two-root.test.sh && grep -q 'fakeclone\|clone-shaped\|self-check' install/install.two-root.test.sh && grep -q 'uninstall.sh' install/install.two-root.test.sh && grep -q 'command -v node\|node ' install/install.two-root.test.sh && grep -q -- '--target' install/install.two-root.test.sh && git diff --quiet -- install/install.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `sh -n install/install.two-root.test.sh` still parses cleanly.
    - Source assertion: the harness references `--allow-self`, a clone-shaped fixture, `--target`, `uninstall.sh`, and a `node`-presence check (covers INSTALL-03, D-07, D-06, and parity).
    - Source assertion: the self-checkout guard fixture is built under `$WORK` (e.g. `fakeclone`) and the guard invocation NEVER uses `$REPO_ROOT` as the target (grep shows the guard uses the fixture path).
    - Behavior assertion: `git diff --quiet -- install/install.test.sh` succeeds (frozen harness untouched).
    - Behavior assertion: the parity assertion `pass`es-with-skip when `node` is absent (no hard `fail` on a node-less box).
    - Behavior assertion (RED expected this wave): `sh install/install.two-root.test.sh` runs to completion; guard/uninstall/parity assertions report FAIL until Plans 03/04 land — they go GREEN then.
  </acceptance_criteria>
  <done>The harness fully covers INSTALL-03 (`--target` from any CWD, `--yes`/non-TTY), the D-07 self-checkout guard via a throwaway clone-shaped fixture, D-06 two-root uninstall survival/removal, and sh/Node byte-parity (skip-not-fail when node absent); `install/install.test.sh` remains unedited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → filesystem | The harness creates and mutates only `mktemp -d` temp dirs; it must never write the real repo, `$HOME`, or a real `$GRUGOPS_HOME` |
| self-checkout guard test → real repo | The guard test must use a throwaway clone-shaped fixture, never point at `$REPO_ROOT` (else it could dirty source adapters) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | harness mutating the real repo or `$HOME` | mitigate | Hermetic `GRUGOPS_HOME`/`GRUGOPS_SRC`/`TARGET` overrides under `mktemp -d`; `trap cleanup EXIT INT TERM`; never use real paths |
| T-08-02-02 | Tampering | guard test materializing a machine path into source adapters | mitigate | The D-07 guard assertion uses a throwaway `$WORK/fakeclone` fixture, never `$REPO_ROOT`; mirrors "safety is mechanical" by proving the guard refuses |
| T-08-02-03 | Spoofing | editing the frozen `install.test.sh` and masking a regression | mitigate | Assert `git diff --quiet -- install/install.test.sh` in both tasks — the frozen harness must stay byte-identical |
| T-08-02-04 | Information Disclosure | no secrets/credentials in scope; no deploy-approval env var | accept | The harness reads/writes only temp markdown/JSON; it never sets or reads the deploy-approval env var |
| T-08-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (POSIX sh harness, no framework install — RESEARCH Wave 0: "No framework install needed") |
</threat_model>

<verification>
- `sh -n install/install.two-root.test.sh` parses (valid POSIX, both tasks).
- `git diff --quiet -- install/install.test.sh` succeeds (frozen harness unedited).
- `sh install/install.two-root.test.sh` runs to completion under `mktemp -d` and reports per-assertion pass/fail (RED this wave; GREEN after Plans 03/04).
- The harness drives every install hermetically (`GRUGOPS_HOME`/`GRUGOPS_SRC`/`TARGET`/`INSTALL_MODE=copy`) and exercises the guard against a throwaway clone fixture only.
</verification>

<success_criteria>
- `install/install.two-root.test.sh` exists, is valid POSIX, runs hermetically, and cleans up via `trap`.
- It encodes the full INSTALL-03 + INSTALL-04 contract plus D-03/D-06/D-07 as discrete assertions, with the snapshot extended to both roots.
- It never edits `install/install.test.sh` and never points the self-checkout guard at the real repo.
- The harness is the verification target Plans 03 and 04 turn GREEN.
</success_criteria>

<output>
Create `.planning/phases/08-two-root-installer/08-02-SUMMARY.md` when done.
</output>

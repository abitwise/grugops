---
phase: 07-shared-home-foundation-path-rewrite
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - agent-factory/workflows/00-bootstrap-greenfield.md
  - agent-factory/workflows/01-bootstrap-brownfield.md
  - agent-factory/workflows/02-idea-to-epics.md
  - agent-factory/workflows/03-epic-to-tickets.md
  - agent-factory/workflows/04-ticket-to-pr.md
  - agent-factory/workflows/05-pr-quality-gate.md
  - agent-factory/workflows/06-uat-pack.md
  - agent-factory/workflows/07-backlog-refinement.md
  - agent-factory/workflows/08-sprint-planning.md
  - agent-factory/workflows/09-daily-sweep.md
  - agent-factory/workflows/10-sprint-review.md
  - agent-factory/workflows/11-retro.md
  - agent-factory/workflows/12-release.md
  - agent-factory/workflows/13-incident.md
autonomous: true
requirements: [SHOME-02, SHOME-03]
must_haves:
  truths:
    - "Every workflow's config ref resolves to `.grugops/factory.config.json` (SC3, D-02)"
    - "Each workflow's 'Handoffs produced' section names ticket-scoped instances under `plans/handoffs/` filled from the kit templates (SC3, D-03/D-05)"
    - "`04-ticket-to-pr.md` read side reads the ticket-scoped instances, not generic template names (SC3, D-06)"
    - "Genuine template-dir reads stay bare `agent-factory/handoffs/`; kit-to-kit refs stay bare (D-01)"
  artifacts:
    - path: "agent-factory/workflows/04-ticket-to-pr.md"
      provides: "Handoffs-produced section + read side both ticket-scoped"
      contains: "plans/handoffs/"
    - path: "agent-factory/workflows/12-release.md"
      provides: "release-handoff produced as REL-scoped instance"
      contains: "plans/handoffs/"
  key_links:
    - from: "agent-factory/workflows/04-ticket-to-pr.md Handoffs produced"
      to: "plans/handoffs/<TICKET-ID>-implementation.md"
      via: "instance naming filled from template"
      pattern: "plans/handoffs/"
    - from: "agent-factory/workflows/05-pr-quality-gate.md"
      to: "plans/handoffs/<TICKET-ID>-implementation.md"
      via: "reads the produced implementation instance (#quality config preserved)"
      pattern: "plans/handoffs/"
---

<objective>
Rewrite the workflow tier (14 files). Every workflow's config ref moves to `.grugops/factory.config.json` (D-02, `#field` anchors preserved); each workflow's "Handoffs produced" section and "open handoffs" inputs convert from the dir-only `agent-factory/handoffs/` collective form to ticket-scoped runtime instances under `plans/handoffs/` filled from the kit templates (D-03/D-05). The `04-ticket-to-pr.md` read side (D-06) is the one workflow whose input refs change — it must read the ticket-scoped instances, not generic template names.

Purpose: SHOME-02/03 — workflows are where the 19 dir-only handoff refs concentrate (every "Handoffs produced" header). These are the highest-miss-rate targets in the phase (RESEARCH §Sequencing): they are semantic, not lexical, and a blanket sed would relocate template reads into the state dir (Pitfall 2). The `04-ticket-to-pr.md` read side is the easy-to-miss change (most workflows only produce).

Output: 14 workflow files rewritten; zero config refs; the 14 "Handoffs produced" sections point at `plans/handoffs/` instances; `04`'s read side reads instances.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-CONTEXT.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-RESEARCH.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-PATTERNS.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-01-SUMMARY.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config-ref rewrite across all 14 workflows</name>
  <files>agent-factory/workflows/00-bootstrap-greenfield.md, agent-factory/workflows/01-bootstrap-brownfield.md, agent-factory/workflows/02-idea-to-epics.md, agent-factory/workflows/03-epic-to-tickets.md, agent-factory/workflows/04-ticket-to-pr.md, agent-factory/workflows/05-pr-quality-gate.md, agent-factory/workflows/06-uat-pack.md, agent-factory/workflows/07-backlog-refinement.md, agent-factory/workflows/08-sprint-planning.md, agent-factory/workflows/09-daily-sweep.md, agent-factory/workflows/10-sprint-review.md, agent-factory/workflows/11-retro.md, agent-factory/workflows/12-release.md, agent-factory/workflows/13-incident.md</files>
  <read_first>
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-PATTERNS.md § "Bucket A — config refs" (the before/after shape, `#field`-anchor preservation; note 05-pr-quality-gate carries the `#quality` anchored ref)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-CONTEXT.md § D-02
    - Each workflow file (grep `agent-factory/config/` per file; 00-bootstrap-greenfield carries 3, 01-bootstrap-brownfield carries 2, 05-pr-quality-gate carries the `#quality` anchor; 10-sprint-review carries NO config ref)
  </read_first>
  <action>
    Rewrite every `agent-factory/config/factory.config.json[#field]` ref to `.grugops/factory.config.json[#field]` across the 13 workflows that carry one (10-sprint-review has none — verify and leave untouched). Preserve every `#field` anchor verbatim, especially `05-pr-quality-gate.md`'s `#quality` (Security V14 — dropping it would change which gate threshold the workflow reads). Do NOT touch handoff refs in this task (Task 2). Do NOT touch bare kit-to-kit refs (`_commit-convention.md`, `_role-switch-protocol`, checklist pointers, the `05-pr-quality-gate` workflow self-ref) — stay bare per D-01. Scope edits to this file list, never repo-wide.
  </action>
  <acceptance_criteria>
    - `grep -rl 'agent-factory/config/' agent-factory/workflows/` returns no files (zero config refs across all 14 workflows)
    - `grep -q '#quality' agent-factory/workflows/05-pr-quality-gate.md` still succeeds (the gate-threshold anchor is preserved)
    - `! grep -rq 'GRUGOPS_HOME' agent-factory/workflows/`
  </acceptance_criteria>
  <verify>
    <automated>sh -c "test \"\$(grep -rl 'agent-factory/config/' agent-factory/workflows/ 2>/dev/null | wc -l | tr -d ' ')\" = 0 && grep -q '#quality' agent-factory/workflows/05-pr-quality-gate.md && ! grep -rq 'GRUGOPS_HOME' agent-factory/workflows/ && echo OK"</automated>
  </verify>
  <done>All 14 workflows read `.grugops/factory.config.json`; zero `agent-factory/config/` refs; the `#quality` anchor and other `#field` anchors preserved; no env var leaked.</done>
</task>

<task type="auto">
  <name>Task 2: Handoff split across all 14 workflows — "Handoffs produced" sections + 04 read side + dir-only collective refs</name>
  <files>agent-factory/workflows/00-bootstrap-greenfield.md, agent-factory/workflows/01-bootstrap-brownfield.md, agent-factory/workflows/02-idea-to-epics.md, agent-factory/workflows/03-epic-to-tickets.md, agent-factory/workflows/04-ticket-to-pr.md, agent-factory/workflows/05-pr-quality-gate.md, agent-factory/workflows/06-uat-pack.md, agent-factory/workflows/07-backlog-refinement.md, agent-factory/workflows/08-sprint-planning.md, agent-factory/workflows/09-daily-sweep.md, agent-factory/workflows/11-retro.md, agent-factory/workflows/12-release.md, agent-factory/workflows/13-incident.md</files>
  <read_first>
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-PATTERNS.md § "Bucket B" anchor 2 (the `04-ticket-to-pr.md:35-36` "Handoffs produced" before/after — the canonical 14-workflow pattern), § "Bucket C" (the read-side instance rule), § "Bucket D" (template-dir reads stay bare), § Bucket B "highest-miss targets" (the 19 dir-only refs: "Handoffs produced" headers + "open handoffs" in 09-daily-sweep, 12-release)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-RESEARCH.md § "The handoff ref split, by spelling" lines 199-208 (the 19 dir-only crux), § "Template READ-vs-WRITTEN map" lines 304-323 (which workflow produces which template, the `<stage>` tokens, the D-05 ID scope), § "Sequencing & Blast-Radius Hazards" line 416 (04's read side is the easy-to-miss change)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-02-SUMMARY.md (the EXACT `<stage>` token spelling Plan 02 used — reuse identically so role prose and workflow prose name the same instance file)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-CONTEXT.md § D-03, D-04, D-05, D-06
    - Each workflow's "Handoffs produced" section + any "open handoffs" input (grep `agent-factory/handoffs/` per file): 00 produces product/system/architecture; 01 produces security-nfr-handoff; 02 produces product-handoff; 03 produces system-handoff; 04 produces implementation/qe/security-nfr (+ read side, D-06); 05 reads implementation-handoff; 06 produces uat-handoff; 07 produces refinement-notes; 08 optional sprint-plan; 09 reads "open handoffs"; 11 produces retro-notes; 12 produces release-handoff + "the handoffs in agent-factory/handoffs/" input; 13 produces incident-postmortem
  </read_first>
  <action>
    Convert each workflow's "Handoffs produced" section from the dir-only "Under `agent-factory/handoffs/`: `X.md` (Role), …" form to the ticket-scoped runtime-instance form: "Under `plans/handoffs/` (filled from the templates in `agent-factory/handoffs/`): `<ID>-<stage>.md` (Role), …" using the EXACT `<stage>` tokens recorded in Plan 02's SUMMARY and the D-05 ID scope (delivery=TICKET-ID, release=REL-, incident=INC-, sprint artifacts=sprint ID). For `04-ticket-to-pr.md` ALSO update the read side (D-06): any input/read ref to a generic `agent-factory/handoffs/<template>.md` becomes the ticket-scoped instance `plans/handoffs/<TICKET-ID>-<stage>.md` — this is the one workflow whose read refs change; do not miss it. For `05-pr-quality-gate.md`'s read of `implementation-handoff` (the produced instance), point it at `plans/handoffs/<TICKET-ID>-implementation.md`. Convert the dir-only "open handoffs" collective inputs in `09-daily-sweep.md` and `12-release.md` to `plans/handoffs/` (these describe runtime instances). A genuine template-dir read ("read the X template from `agent-factory/handoffs/`") stays bare (Bucket D). Per-ref SEMANTIC judgment, NEVER a blanket sed (Pitfall 2). 10-sprint-review has no handoff ref (it is in Task-1's no-config set too) — not in this task's file list. Preserve caveman voice; keep `_commit-convention.md` and checklist kit-to-kit refs bare (D-01).
  </action>
  <acceptance_criteria>
    - `grep -q 'plans/handoffs/' agent-factory/workflows/04-ticket-to-pr.md` succeeds AND the 04 read side references a ticket-scoped instance (no generic `agent-factory/handoffs/implementation-handoff.md` read remains): `! grep -q 'agent-factory/handoffs/implementation-handoff.md' agent-factory/workflows/04-ticket-to-pr.md` where that ref was a read
    - `grep -q 'plans/handoffs/' agent-factory/workflows/12-release.md` AND `agent-factory/workflows/06-uat-pack.md` AND `agent-factory/workflows/13-incident.md` each succeed
    - Every surviving `agent-factory/handoffs/` ref across all workflows is a known template name or the bare template dir — the allowlist-minus check returns empty
    - The `<stage>` tokens used match Plan 02's SUMMARY exactly (e.g. `plans/handoffs/<TICKET-ID>-implementation.md` appears identically in both a role and 04)
  </acceptance_criteria>
  <verify>
    <automated>sh -c "grep -q 'plans/handoffs/' agent-factory/workflows/04-ticket-to-pr.md && grep -q 'plans/handoffs/' agent-factory/workflows/12-release.md && stray=\$(grep -rn 'agent-factory/handoffs/' agent-factory/workflows/ | grep -Ev 'agent-factory/handoffs/(architecture-handoff|business-handoff|implementation-handoff|implementation-ready-packet|incident-postmortem|product-handoff|qe-handoff|refinement-notes|release-handoff|retro-notes|security-nfr-handoff|sprint-plan|system-handoff|ticket-ready-packet|uat-handoff|universal-handoff)[.]md' | grep -v 'agent-factory/handoffs/\`' || true); [ -z \"\$stray\" ] && echo OK || { printf 'STRAY:\n%s\n' \"\$stray\"; exit 1; }"</automated>
  </verify>
  <done>All 14 workflows name ticket-scoped instances under `plans/handoffs/` in their Handoffs-produced sections; 04's read side reads instances; the 09/12 collective refs point at `plans/handoffs/`; genuine template-dir reads stay bare; no leaked instance name in the kit handoff dir; `<stage>` tokens match Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template (KIT) vs instance (STATE) | the 19 dir-only "Handoffs produced" refs are the highest-miss; misclassifying relocates template reads into the state dir |
| config `#quality` anchor | 05-pr-quality-gate's gate threshold is encoded in `#quality`; dropping it changes the gate |
| cross-plan `<stage>` token consistency | if workflow instance names diverge from role instance names, the trail links break |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering (data in wrong root) | the 19 dir-only "Handoffs produced" refs | mitigate | Converted by meaning to `plans/handoffs/` instances, never by sed (Pitfall 2). Task 2 allowlist-minus check asserts no non-template `agent-factory/handoffs/` ref survives in any workflow. |
| T-07-03-02 | Tampering (false-gate) | 05-pr-quality-gate `#quality` anchor | mitigate | Bucket A preserves the `#quality` anchor verbatim; Task 1 acceptance asserts it survives. A dropped anchor would silently change which quality threshold the gate reads. |
| T-07-03-03 | DoS (broken trail) | cross-plan `<stage>` token drift | mitigate | Task 2 reads Plan 02's SUMMARY for the exact tokens and asserts the role-side and workflow-side instance names match; prevents a role writing `<ID>-implementation.md` while a workflow reads `<ID>-impl.md`. |
| T-07-03-04 | DoS (agent hunts) | 04 read side missed | mitigate | D-06 explicitly flagged: 04's read refs change too. Task 2 acceptance asserts no generic-template read remains in 04. |
| T-07-03-SC | Tampering | npm/pip/cargo installs | n/a | Zero external packages this phase (markdown edits only). No install tasks; no legitimacy checkpoint required. |
</threat_model>

<verification>
- After Task 2, the allowlist-minus check over `agent-factory/workflows/` confirms no leaked instance name in the kit handoff dir.
- Cross-check one instance name (e.g. `plans/handoffs/<TICKET-ID>-implementation.md`) appears byte-identically in both a Plan-02 role file and a Plan-03 workflow.
- `node scripts/validate-agent-factory.mjs` exits 0 (workflow sections intact — FLOW-05 v2 template shape preserved).
- Plan 04 runs the full `scripts/check-kit-refs.sh` over the whole SCAN set including these workflows.
</verification>

<success_criteria>
- Zero `agent-factory/config/` refs across all 14 workflows; `#quality` and other anchors preserved (SC3, D-02).
- All 14 "Handoffs produced" sections name ticket-scoped instances under `plans/handoffs/` filled from the kit templates (SC3, D-03/D-05).
- `04-ticket-to-pr.md` read side reads the ticket-scoped instances (SC3, D-06).
- Genuine template-dir reads and kit-to-kit refs stay bare (D-01).
</success_criteria>

<output>
Create `.planning/phases/07-shared-home-foundation-path-rewrite/07-03-SUMMARY.md` when done.
</output>

---
phase: 07-shared-home-foundation-path-rewrite
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - agent-factory/roles/_role-switch-protocol.md
  - agent-factory/roles/ba-pm.md
  - agent-factory/roles/system-analyst.md
  - agent-factory/roles/architect-design.md
  - agent-factory/roles/software-engineer.md
  - agent-factory/roles/qe-e2e.md
  - agent-factory/roles/security-nfr.md
  - agent-factory/roles/uat-planner.md
  - agent-factory/roles/release-manager.md
  - agent-factory/roles/compliance-officer.md
  - agent-factory/roles/incident-responder.md
  - agent-factory/roles/factory-coach.md
  - agent-factory/roles/agents-md-scribe.md
  - agent-factory/roles/brownfield-mapper.md
  - agent-factory/roles/greenfield-mapper.md
  - agent-factory/roles/installer.md
  - agent-factory/_commit-convention.md
  - .claude/skills/grugops-map/SKILL.md
  - .claude/skills/grugops-plan/SKILL.md
  - .claude/skills/grugops-ticket/SKILL.md
  - .claude/skills/grugops-gate/SKILL.md
  - .claude/skills/grugops-uat/SKILL.md
  - .claude/skills/grugops-release/SKILL.md
  - skills/map/SKILL.md
  - skills/plan/SKILL.md
  - skills/ticket/SKILL.md
  - skills/gate/SKILL.md
  - skills/uat/SKILL.md
  - skills/release/SKILL.md
  - skills/grugops/SKILL.md
autonomous: true
requirements: [SHOME-02, SHOME-03, SHOME-04]
must_haves:
  truths:
    - "Every role's config ref resolves to `.grugops/factory.config.json` (SC3, D-02)"
    - "`_role-switch-protocol.md` step 4 splits template-read (KIT) from instance-write `plans/handoffs/<WORK-ITEM-ID>-<stage>.md` (STATE) (SC3, D-06)"
    - "Each role's produced handoff reads its template from `agent-factory/handoffs/` and writes a ticket-scoped instance to `plans/handoffs/<ID>-<stage>.md` (SC3, D-03/D-05)"
    - "Each role's upstream-handoff input reads the ticket-scoped instance, not a generic template name (D-06)"
    - "Every op-skill (6 dash + 7 colon) config ref resolves to `.grugops/factory.config.json` and carries the compressed invariant (SC2/SC3, D-02/D-09)"
    - "No role/skill names `$GRUGOPS_HOME` (SC4, D-12)"
  artifacts:
    - path: "agent-factory/roles/_role-switch-protocol.md"
      provides: "Step-4 template-read-vs-instance-write split"
      contains: "plans/handoffs/"
    - path: "agent-factory/roles/software-engineer.md"
      provides: "implementation-handoff template read + ticket-scoped instance write"
      contains: "plans/handoffs/"
    - path: "skills/grugops/SKILL.md"
      provides: "config ref → .grugops/ (plugin colon-form) + invariant"
      contains: ".grugops/factory.config.json"
  key_links:
    - from: "agent-factory/roles/qe-e2e.md input"
      to: "plans/handoffs/<TICKET-ID>-implementation.md"
      via: "reads upstream Software Engineer instance (D-06)"
      pattern: "plans/handoffs/"
    - from: "agent-factory/roles/_role-switch-protocol.md step 4"
      to: "plans/handoffs/<WORK-ITEM-ID>-<stage>.md"
      via: "instance write instruction"
      pattern: "plans/handoffs/<WORK-ITEM-ID>"
---

<objective>
Rewrite the role tier and the operation-skill adapters. Every role's config ref moves to `.grugops/factory.config.json` (D-02); the handoff refs split per-role into template-read (KIT, bare) vs ticket-scoped instance-write `plans/handoffs/<ID>-<stage>.md` (STATE) per D-03/D-05; the `_role-switch-protocol.md` step-4 write instruction gets the once-here template-vs-instance split (D-06); and the 6 dash op-skills + 7 plugin colon op-skills get their config refs rewritten plus the compressed invariant.

Purpose: SHOME-02 moves config + handoff instances into per-repo state; SHOME-03 leaves zero misclassified refs; D-04/D-05 convert generic handoff filenames into a durable per-work-item trail. This is the bulk of the rewrite for the role/adapter half. The highest-risk targets are the dir-only "open handoffs" lines and the upstream-instance READ refs (D-06) — these need per-ref semantic judgment, NEVER a blanket sed (Pitfall 2).

Output: 17 role files, `_commit-convention.md`, and 13 op-skill files rewritten; zero config refs; the role-switch step-4 split in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-CONTEXT.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-RESEARCH.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-PATTERNS.md
@.planning/phases/07-shared-home-foundation-path-rewrite/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config-ref rewrite across all 17 roles + _commit-convention.md + 13 op-skills</name>
  <files>agent-factory/roles/ba-pm.md, agent-factory/roles/system-analyst.md, agent-factory/roles/architect-design.md, agent-factory/roles/software-engineer.md, agent-factory/roles/qe-e2e.md, agent-factory/roles/security-nfr.md, agent-factory/roles/uat-planner.md, agent-factory/roles/release-manager.md, agent-factory/roles/compliance-officer.md, agent-factory/roles/incident-responder.md, agent-factory/roles/factory-coach.md, agent-factory/roles/agents-md-scribe.md, agent-factory/roles/brownfield-mapper.md, agent-factory/roles/greenfield-mapper.md, agent-factory/roles/installer.md, agent-factory/_commit-convention.md, .claude/skills/grugops-map/SKILL.md, .claude/skills/grugops-plan/SKILL.md, .claude/skills/grugops-ticket/SKILL.md, .claude/skills/grugops-gate/SKILL.md, .claude/skills/grugops-uat/SKILL.md, .claude/skills/grugops-release/SKILL.md, skills/map/SKILL.md, skills/plan/SKILL.md, skills/ticket/SKILL.md, skills/gate/SKILL.md, skills/uat/SKILL.md, skills/release/SKILL.md, skills/grugops/SKILL.md</files>
  <read_first>
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-PATTERNS.md § "Bucket A — config refs" (the exact before/after shape `agent-factory/config/factory.config.json[#field]` → `.grugops/factory.config.json[#field]`, the `#field`-anchor-preservation rule, the "scoped to SCAN list never repo-wide" caution) and § Shared Patterns "#field anchor preservation"
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-CONTEXT.md § D-02
    - Each file being edited (grep `agent-factory/config/` per file to find the exact ref line; most roles carry exactly one, security-nfr/uat-planner carry an anchored variant; the op-skills each carry one line "read agent-factory/config/factory.config.json, the root AGENTS.md, and plans/board.md")
  </read_first>
  <action>
    Rewrite every `agent-factory/config/factory.config.json` ref to `.grugops/factory.config.json` across all listed files, preserving any `#field` anchor (`#wip_limits`, `#quality`, etc.) verbatim — only the path root changes (D-02, Security V14). The `_commit-convention.md` prose ref (the "DEFAULT protected set" line) reads the per-repo config, so it also rewrites to `.grugops/factory.config.json`. The 6 dash op-skills and 7 plugin colon op-skills each carry the same single config-ref line — rewrite each. Do NOT touch any bare kit-to-kit ref (roles dir, workflows dir, checklists, `_role-switch-protocol.md`, `_commit-convention.md` self-refs) — those stay bare per D-01. Do NOT touch handoff refs in this task (Task 2 handles them). Do NOT add `$GRUGOPS_HOME` to any file. This is the one near-mechanical bucket, but apply it file-by-file scoped to this list, never a repo-wide replace (fixtures carry the old string intentionally).
  </action>
  <acceptance_criteria>
    - `grep -rl 'agent-factory/config/' agent-factory/roles/ agent-factory/_commit-convention.md .claude/skills/ skills/` returns no files (zero config refs across all owned role/skill files)
    - `grep -rl '.grugops/factory.config.json' agent-factory/roles/ | wc -l` is >= 16 (each role now reads the per-repo config)
    - `! grep -rq 'GRUGOPS_HOME' agent-factory/roles/ skills/ .claude/skills/`
    - Anchored refs preserved where they existed before (e.g. `#wip_limits` / `#quality` still present in the files that carried them)
  </acceptance_criteria>
  <verify>
    <automated>sh -c "test \"\$(grep -rl 'agent-factory/config/' agent-factory/roles/ agent-factory/_commit-convention.md .claude/skills/ skills/ 2>/dev/null | wc -l | tr -d ' ')\" = 0 && ! grep -rq 'GRUGOPS_HOME' agent-factory/roles/ skills/ .claude/skills/ && echo OK"</automated>
  </verify>
  <done>Every role, `_commit-convention.md`, and all 13 op-skills read `.grugops/factory.config.json`; zero `agent-factory/config/` refs remain in this file set; `#field` anchors preserved; no env var leaked.</done>
</task>

<task type="auto">
  <name>Task 2: Handoff split across roles + the _role-switch-protocol.md step-4 once-here edit</name>
  <files>agent-factory/roles/_role-switch-protocol.md, agent-factory/roles/ba-pm.md, agent-factory/roles/system-analyst.md, agent-factory/roles/architect-design.md, agent-factory/roles/software-engineer.md, agent-factory/roles/qe-e2e.md, agent-factory/roles/security-nfr.md, agent-factory/roles/uat-planner.md, agent-factory/roles/release-manager.md, agent-factory/roles/compliance-officer.md, agent-factory/roles/incident-responder.md, agent-factory/roles/factory-coach.md</files>
  <read_first>
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-PATTERNS.md § "Bucket B — handoff WRITE" (anchor 1 = `_role-switch-protocol.md:27-28` step-4 target form; the per-ref-semantic-judgment-never-sed rule), § "Bucket C — handoff READ refs" (the qe-e2e.md:22 and software-engineer.md:21 before/after, the "input line reads the INSTANCE" rule), § "Bucket D" (template-dir reads stay bare)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-RESEARCH.md § "Handoff Template Allowlist" lines 304-323 (the Template READ-vs-WRITTEN map: which role reads/writes each, the proposed `<stage>` token per template, the D-05 ID scope: delivery=TICKET-ID, release=REL-, incident=INC-, sprint artifacts=sprint ID)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-CONTEXT.md § D-03, D-04, D-05, D-06
    - agent-factory/roles/_role-switch-protocol.md (step 4 at lines 27-28 is the once-here edit; the branch is already merged to main so this edit happens exactly once — do NOT assume an un-merged branch)
    - Each role file's Reads/Output/"Board moves" sections (grep `agent-factory/handoffs/` per file): system-analyst reads product-handoff + writes system-handoff; architect-design reads system-handoff + writes architecture-handoff; software-engineer reads implementation-ready-packet + writes implementation-handoff; qe-e2e reads implementation-handoff + writes qe-handoff; security-nfr reads qe-handoff + writes security-nfr-handoff; uat-planner reads security-nfr-handoff + writes uat-handoff; ba-pm writes product-handoff; release-manager writes release-handoff + has a dir-only "the handoffs in agent-factory/handoffs/" input; compliance-officer appends to security-nfr-handoff; incident-responder writes incident-postmortem; factory-coach writes retro-notes
  </read_first>
  <action>
    Edit `_role-switch-protocol.md` step 4 (lines 27-28) ONCE: change "write the role's handoff file under `agent-factory/handoffs/`" to the template-read-vs-instance-write split — read the role's handoff TEMPLATE from `agent-factory/handoffs/<template>.md` (KIT, read-only), fill it, and WRITE the filled instance to `plans/handoffs/<WORK-ITEM-ID>-<stage>.md` (STATE, this repo). Per-role, apply the READ-vs-WRITTEN map: every WRITE/Output ref ("writes implementation-handoff.md") becomes "read the `<template>.md` template from `agent-factory/handoffs/` (bare, KIT), write the filled instance to `plans/handoffs/<ID>-<stage>.md`" using the `<stage>` tokens from the allowlist map (implementation, qe, security-nfr, product, system, architecture, uat, retro, postmortem, release, ticket-ready, impl-ready, refinement); every upstream-handoff READ/input ref (D-06) becomes a read of the ticket-scoped INSTANCE `plans/handoffs/<TICKET-ID>-<stage>.md` (the upstream role's product), NOT the generic template — the template is read only to fill a new one. Apply the D-05 ID scope: delivery handoffs (implementation/qe/security-nfr/product/system/architecture) scope by TICKET-ID; release by REL-; incident by INC-; sprint artifacts (retro-notes, refinement-notes) by sprint ID. Convert any dir-only "the handoffs in `agent-factory/handoffs/`" collective input ref (release-manager) to point at `plans/handoffs/` runtime instances. A genuine template-dir read ("read the X template from `agent-factory/handoffs/`") stays bare (Bucket D). Make per-ref semantic judgments — NEVER a blanket sed (Pitfall 2). Preserve caveman voice; path instructions stay literal/clear. compliance-officer appends to the security-nfr instance `plans/handoffs/<TICKET-ID>-security-nfr.md`.
  </action>
  <acceptance_criteria>
    - `grep -q 'plans/handoffs/<WORK-ITEM-ID>' agent-factory/roles/_role-switch-protocol.md` succeeds (step 4 split present, ticket-scoped)
    - `grep -q 'plans/handoffs/' agent-factory/roles/software-engineer.md` AND `agent-factory/roles/qe-e2e.md` AND `agent-factory/roles/release-manager.md` each succeed (writes/instances now in state)
    - qe-e2e.md reads the upstream instance not the template: `grep -q 'plans/handoffs/.*implementation' agent-factory/roles/qe-e2e.md` succeeds
    - Every surviving `agent-factory/handoffs/` ref in agent-factory/roles/ is a known template name (no leaked `agent-factory/handoffs/<ID>-...` instance) — the allowlist-minus check below returns empty
  </acceptance_criteria>
  <verify>
    <automated>sh -c "grep -q 'plans/handoffs/<WORK-ITEM-ID>' agent-factory/roles/_role-switch-protocol.md && grep -q 'plans/handoffs/' agent-factory/roles/software-engineer.md && grep -q 'plans/handoffs/' agent-factory/roles/qe-e2e.md && stray=\$(grep -rn 'agent-factory/handoffs/' agent-factory/roles/ | grep -Ev 'agent-factory/handoffs/(architecture-handoff|business-handoff|implementation-handoff|implementation-ready-packet|incident-postmortem|product-handoff|qe-handoff|refinement-notes|release-handoff|retro-notes|security-nfr-handoff|sprint-plan|system-handoff|ticket-ready-packet|uat-handoff|universal-handoff)[.]md' | grep -v 'agent-factory/handoffs/\`' || true); [ -z \"\$stray\" ] && echo OK || { printf 'STRAY:\n%s\n' \"\$stray\"; exit 1; }"</automated>
  </verify>
  <done>The role-switch step-4 split is in place; every role reads its template bare from KIT and writes/reads ticket-scoped instances in `plans/handoffs/`; no leaked instance name survives in the kit handoff dir.</done>
</task>

<task type="auto">
  <name>Task 3: Op-skill compressed invariant (6 dash + 7 colon skills, no self-heal)</name>
  <files>.claude/skills/grugops-map/SKILL.md, .claude/skills/grugops-plan/SKILL.md, .claude/skills/grugops-ticket/SKILL.md, .claude/skills/grugops-gate/SKILL.md, .claude/skills/grugops-uat/SKILL.md, .claude/skills/grugops-release/SKILL.md, skills/map/SKILL.md, skills/plan/SKILL.md, skills/ticket/SKILL.md, skills/gate/SKILL.md, skills/uat/SKILL.md, skills/release/SKILL.md, skills/grugops/SKILL.md</files>
  <read_first>
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-01-SUMMARY.md (the byte-identical invariant marker phrase chosen in Plan 01)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-PATTERNS.md § "Bucket E3" recommendation (the 6 dash + 7 colon op-skills carry the COMPRESSED INVARIANT ONLY and defer resolution to the orchestrator — the full self-heal lives ONLY in the two resolver adapters, single-sourcing the bash per D-11)
    - .planning/phases/07-shared-home-foundation-path-rewrite/07-CONTEXT.md § D-09, D-11, D-12
    - One dash skill (.claude/skills/grugops-map/SKILL.md) and one colon skill (skills/map/SKILL.md) to see the existing body shape and the trailing safety line + `Request: $ARGUMENTS` line
  </read_first>
  <action>
    In each of the 13 op-skill files, add the compressed invariant blockquote using the byte-identical marker phrase from Plan 01's SUMMARY, cross-linking AGENTS.md § Kit vs state. Do NOT add the `${GRUGOPS_HOME:-$HOME/.grugops}` self-heal to these files — they dispatch through the Orchestrator, which holds the sole resolver (D-11 single-source). Each op-skill's invariant blockquote MUST NOT name `$GRUGOPS_HOME` (D-12 / SC4). The config ref in each was already rewritten in Task 1; here only the invariant is added. Preserve the trailing safety line "Never merge to a protected branch. Never deploy to prod. Humans always hold merge and deploy." and the `Request: $ARGUMENTS` line verbatim where present. The plugin colon-form `skills/grugops/SKILL.md` carries the invariant + defers to the orchestrator (it is the plugin dispatcher, not the standalone resolver). Keep kit-to-kit workflow/role refs bare (D-01).
  </action>
  <acceptance_criteria>
    - The Plan-01 marker phrase appears in all 13 op-skill files
    - No op-skill names `$GRUGOPS_HOME`: `! grep -rq 'GRUGOPS_HOME'` across all 13 files
    - Each op-skill still contains "Humans always hold merge and deploy."
  </acceptance_criteria>
  <verify>
    <automated>sh -c "for f in .claude/skills/grugops-map/SKILL.md .claude/skills/grugops-plan/SKILL.md .claude/skills/grugops-ticket/SKILL.md .claude/skills/grugops-gate/SKILL.md .claude/skills/grugops-uat/SKILL.md .claude/skills/grugops-release/SKILL.md skills/map/SKILL.md skills/plan/SKILL.md skills/ticket/SKILL.md skills/gate/SKILL.md skills/uat/SKILL.md skills/release/SKILL.md skills/grugops/SKILL.md; do grep -q 'GRUGOPS_HOME' \"\$f\" && { echo \"LEAK: \$f\"; exit 1; }; grep -qF 'Humans always hold merge and deploy.' \"\$f\" || { echo \"MISSING SAFETY: \$f\"; exit 1; }; done; echo OK"</automated>
  </verify>
  <done>All 13 op-skills carry the byte-identical invariant, defer resolution to the orchestrator (no self-heal), name no env var, and keep the safety line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template (KIT) vs instance (STATE) | the same word "handoffs" maps to two homes; misclassifying a write as a kit ref would put runtime data in a read-only shared dir |
| config `#field` anchor | the field a role reads is encoded in the anchor; dropping it silently changes behavior |
| prose → LLM execution | an env var named in role/skill prose is a dead string an LLM may resolve by hunting/hallucinating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering (data in wrong root) | per-role handoff write refs | mitigate | Bucket B writes go to `plans/handoffs/<ID>-<stage>.md` (STATE); template reads stay bare (KIT). Per-ref semantic judgment, never blanket sed (Pitfall 2). Task 2 acceptance asserts no leaked `agent-factory/handoffs/<ID>-...` instance name survives in the kit handoff dir (the allowlist-minus check). |
| T-07-02-02 | Tampering (false-config) | config-ref `#field` anchors | mitigate | Bucket A rewrites only the path root; `#wip_limits`/`#quality` anchors preserved verbatim (Security V14). Acceptance asserts anchor survival. |
| T-07-02-03 | Information disclosure | `$GRUGOPS_HOME` in role/skill prose | mitigate | D-12 / SC4: no role or op-skill names `$GRUGOPS_HOME`; op-skills defer resolution to the orchestrator (sole resolver). Acceptance asserts `! grep -rq GRUGOPS_HOME`. Plan 04 Assertion 3 enforces this mechanically. |
| T-07-02-04 | DoS (agent hunts) | dir-only "open handoffs" collective refs | mitigate | Converted to `plans/handoffs/` runtime instances by meaning, removing the ambiguous shared-dir read that drives the DOG-02 hunt failure. |
| T-07-02-SC | Tampering | npm/pip/cargo installs | n/a | Zero external packages this phase (markdown edits only). No install tasks; no legitimacy checkpoint required. |
</threat_model>

<verification>
- After Task 2, run the allowlist-minus check over `agent-factory/roles/` (in the Task 2 verify) to confirm no leaked instance name in the kit handoff dir.
- `node scripts/validate-agent-factory.mjs` exits 0 (no structural regression — role sections intact).
- Plan 04 will run the full `scripts/check-kit-refs.sh` over the whole SCAN set including these files.
</verification>

<success_criteria>
- Zero `agent-factory/config/` refs across all 17 roles, `_commit-convention.md`, and 13 op-skills (SC3, D-02).
- `_role-switch-protocol.md` step 4 carries the template-read-vs-instance-write split with ticket-scoped instance naming (SC3, D-06).
- Every produced handoff reads its template bare and writes a ticket-scoped instance; every upstream-handoff input reads the instance (SC3, D-03/D-05/D-06).
- All 13 op-skills carry the byte-identical invariant; none names `$GRUGOPS_HOME` (SC2/SC4).
</success_criteria>

<output>
Create `.planning/phases/07-shared-home-foundation-path-rewrite/07-02-SUMMARY.md` when done. Record the final `<stage>` token spelling used (e.g. `implementation` vs `implementation-handoff`) so Plan 03 (workflows) uses the identical tokens.
</output>

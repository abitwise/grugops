---
phase: 09-doctor-two-root-validator
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - install/install.test.sh
  - scripts/validate.test.sh
  - scripts/fixtures/
autonomous: true
requirements: [INSTALL-05, VAL-02]
tags: [test-harness, fixtures, doctor, validator, parity, c3, sc5]

must_haves:
  truths:
    - "install.test.sh proves: good split → doctor exits 0; rm -rf $GRUGOPS_HOME/agent-factory → doctor exits nonzero naming the missing kit; double --check is read-only (snapshot unchanged)"
    - "install.test.sh proves the exit-code matrix: pass=0, FAIL≠0, WARN-only→0, --check --strict→≠0; and a dangling symlink → FAIL"
    - "validate.test.sh proves: GOOD split passes; BAD missing-kit (VALIDATE_KIT_ROOT→nonexistent) fails naming a missing role file; BAD unset-kit errors with the (C3) message"
    - "A three-way resolution-parity assertion proves sh doctor = Node doctor = Node validator resolve the SAME kit root for the same GRUGOPS_HOME"
    - "install.two-root.test.sh (18/18) and check-kit-refs.sh stay GREEN and untouched (D-09)"
  artifacts:
    - path: "install/install.test.sh"
      provides: "extended SC5 doctor checks (good-split / missing-kit / exit-code matrix / dangling-symlink / read-only double-check / sh↔Node doctor parity)"
      contains: "--check"
    - path: "scripts/validate.test.sh"
      provides: "two-root validator checks (GOOD split / BAD missing-kit / BAD unset-kit (C3)) + the three-way resolution-parity assertion"
      contains: "VALIDATE_KIT_ROOT"
    - path: "scripts/fixtures/"
      provides: "split GOOD kit+state roots for the two-root validator (committed or mktemp-built)"
  key_links:
    - from: "install/install.test.sh"
      to: "install.sh --check / install.mjs --check"
      via: "hermetic run_install then capture-rc + grep first-failure line"
      pattern: "--check"
    - from: "scripts/validate.test.sh"
      to: "validate-agent-factory.mjs (VALIDATE_KIT_ROOT/VALIDATE_ROOT)"
      via: "run_fixture_split driving both roots + an unset-kit direct invocation"
      pattern: "VALIDATE_KIT_ROOT"
    - from: "scripts/validate.test.sh resolution-parity check"
      to: "sh doctor + Node doctor + Node validator"
      via: "one GRUGOPS_HOME, capture each reported kit root, assert byte-equal"
      pattern: "GRUGOPS_HOME"
---

<objective>
Prove the whole verification layer (VAL-02 / SC5 / SC4). Extend `install/install.test.sh` with the doctor checks (good split → pass; missing kit → loud FAIL naming the kit; the exit-code matrix; dangling-symlink FAIL; double-`--check` read-only; sh↔Node doctor parity), add two-root validator checks to `scripts/validate.test.sh` with split GOOD/BAD fixtures (GOOD split passes; BAD missing-kit fails; BAD unset-kit errors with the C3 message — SC4), and add the three-way resolution-parity assertion that ties sh doctor = Node doctor = Node validator on the kit root (SC4 / D-04). Per D-10, EXTEND `install.test.sh` with the SC5 doctor checks while KEEPING `install.two-root.test.sh` as the deep two-root harness (two harnesses, some overlap accepted). Keep the deep two-root harness and the Phase-7 grep gate GREEN and untouched (D-09).

Purpose: the test harness is the mechanical proof the kit/state split cannot silently regress — it exercises every doctor finding back to a dogfood pain and makes the C3 false-green impossible to ship.
Output: extended `install.test.sh` + `validate.test.sh`, new split fixtures, and the parity assertion — the full suite green is the phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-doctor-two-root-validator/09-RESEARCH.md
@.planning/phases/09-doctor-two-root-validator/09-PATTERNS.md
@.planning/phases/09-doctor-two-root-validator/09-VALIDATION.md
@.planning/phases/09-doctor-two-root-validator/09-CONTEXT.md

# The three programs under test landed in waves 1-2:
@.planning/phases/09-doctor-two-root-validator/09-01-PLAN.md
@.planning/phases/09-doctor-two-root-validator/09-02-PLAN.md
@.planning/phases/09-doctor-two-root-validator/09-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend install.test.sh with the SC5 doctor checks (good-split / missing-kit / exit-code matrix / dangling-symlink / read-only / sh↔Node parity)</name>
  <files>install/install.test.sh</files>
  <read_first>
    - install/install.test.sh — the file being extended. Read: `set -eu`, `pass()/fail()/FAILS` (lines 24-25), portable `$DIFF` (lines 28-29), `mktemp -d` + `trap cleanup EXIT INT TERM` (lines 32-34), `make_fixture` (lines 38-44), content-addressed `snapshot` (lines 48-53), the existing Check 3 two-root uninstall/survival assertion (lines 109-147), and the Node-presence skip-with-note pattern (Check 4, lines ~154/167). Add the new checks AFTER the existing ones; do NOT alter existing checks.
    - install/install.two-root.test.sh — the hermetic `run_install()` two-root driver (lines 65-69: `INSTALL_MODE=copy GRUGOPS_SRC="$REPO_ROOT" GRUGOPS_HOME="$_h" TARGET="$_t" sh install.sh --yes "$@"`) and the capture-rc idiom (`out=$(cmd) && rc=0 || rc=$?`, line ~249) and Check 12 sh/Node parity shape (lines ~304-331). Borrow these; do NOT edit this file.
    - .planning/phases/09-doctor-two-root-validator/09-PATTERNS.md — section "install/install.test.sh — ADD doctor checks" (harness skeleton, hermetic install driver, capture-rc idiom, the explicit new-checks list).
    - .planning/phases/09-doctor-two-root-validator/09-VALIDATION.md — section "Validation Dimensions" (exit-code matrix, first-failure determinism, sh↔Node parity, install.test.sh split rows) and "Wave 0 Requirements".
    - .planning/phases/09-doctor-two-root-validator/09-RESEARCH.md — section Validation Architecture (the test map + sampling) and Discretion 3 (the missing-kit fixture: `rm -rf "$GRUGOPS_HOME/agent-factory"`).
  </read_first>
  <action>
    Add a hermetic two-root install driver (borrow `run_install()` from install.two-root.test.sh) and the following checks using `pass()/fail()` and the capture-rc idiom (`_out=$(... install.sh --check 2>&1) && _rc=0 || _rc=$?`), each under its own `mktemp -d` kit/target so the real repo and `$HOME` are never mutated: (a) GOOD split — `run_install` a kit into an isolated `GRUGOPS_HOME` + adapters/seed into `TARGET`, then `--check` → assert `_rc=0` and output contains `ALL CHECKS PASSED`. (b) MISSING KIT — after a good install, `rm -rf "$GRUGOPS_HOME/agent-factory"`, then `--check` → assert `_rc` nonzero AND the output names the missing kit (grep for `roles/orchestrator.md` or the first-failure line). (c) FIRST-FAILURE DETERMINISM — run `--check` twice on the same broken target, assert the first-failure line is byte-identical across runs. (d) EXIT-CODE MATRIX — pass=0 (from (a)); FAIL≠0 (from (b)); WARN-only→0 (induce a skew or missing-seed WARN — e.g. bump `$GRUGOPS_HOME/agent-factory/VERSION` or `rm` the seeded `memory-bank/00-index.md` — assert `--check` exits 0); WARN+`--strict`→nonzero (same WARN condition + `--check --strict` → assert nonzero). (e) DANGLING SYMLINK — create a symlink in the resolved set whose target is then removed, `--check` → assert FAIL with a symlink-specific line. (f) READ-ONLY — `snapshot` the target before and after a double `--check`, assert `$DIFF` shows zero change (the doctor mutates nothing). (g) sh↔Node DOCTOR PARITY — gated on `command -v node`: run `install.sh --check` and `install.mjs --check` on the same target+env, assert same rc AND same first-failure line; skip-with-note (pass) if node absent (mirror Check 4 / two-root Check 12). Do NOT edit any existing check; do NOT touch install.two-root.test.sh.
  </action>
  <verify>
    <automated>sh -n install/install.test.sh && sh install/install.test.sh; test $? -eq 0 && echo INSTALL-TEST-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `sh -n install/install.test.sh` exits 0 (syntax).
    - `sh install/install.test.sh` exits 0 with all checks PASS (including the new doctor checks).
    - A check installs a good split then `--check` exits 0 with `ALL CHECKS PASSED`.
    - A check does `rm -rf "$GRUGOPS_HOME/agent-factory"` then asserts `--check` exits nonzero AND the output names `agent-factory/roles/orchestrator.md` (or the kit-missing first-failure line) + its referencing file.
    - The exit-code matrix is asserted: pass=0, FAIL≠0, WARN-only→0, `--check --strict`→≠0 (four distinct rc assertions).
    - A dangling-symlink check asserts FAIL with a symlink-specific message.
    - A read-only check snapshots the target before/after a double `--check` and asserts zero diff.
    - The sh↔Node doctor parity check asserts same rc + same first-failure line, gated on `command -v node` with a skip-with-note pass.
    - `sh install/install.two-root.test.sh` still exits 0 (regression — file untouched).
  </acceptance_criteria>
  <done>install.test.sh proves SC1/SC2/SC5 for the doctor — good-split pass, missing-kit loud FAIL, deterministic first-failure, the full exit-code matrix, dangling-symlink FAIL, read-only double-check, and sh↔Node parity — without touching the existing checks or the deep two-root harness.</done>
</task>

<task type="auto">
  <name>Task 2: Add split fixtures + two-root validator checks (GOOD split / BAD missing-kit / BAD unset-kit C3) to validate.test.sh</name>
  <files>scripts/validate.test.sh, scripts/fixtures/</files>
  <read_first>
    - scripts/validate.test.sh — the file being extended. Read: the Node-presence guard (lines 32-33), `run_fixture`/`expect_pass`/`expect_fail` (lines 38-60, where `run_fixture` sets only `VALIDATE_ROOT` — add a split variant), and the existing GOOD/BAD fixture invocations. Add new checks AFTER the existing ones; keep the existing single-tree fixtures working as STATE-root fixtures (Discretion 4).
    - scripts/fixtures/good — the existing combined tree (`agent-factory/` + `plans/` + `AGENTS.md` in one root). Read its layout so the split fixtures (or the mktemp split) reproduce the kit subtree vs state subtree faithfully.
    - .planning/phases/09-doctor-two-root-validator/09-PATTERNS.md — section "scripts/validate.test.sh — ADD two-root validator checks" (the `run_fixture_split` shape, the unset-kit direct-invocation note) and section "scripts/fixtures/* — NEW split fixtures" (committed-split-trees vs mktemp-built — planner permits either; pick one and be consistent).
    - .planning/phases/09-doctor-two-root-validator/09-RESEARCH.md — section Discretion 3 (two BAD fixtures: nonexistent-kit-dir + unset-kit, both must fail) and Discretion 4 (reuse `VALIDATE_ROOT` as STATE; new `VALIDATE_KIT_ROOT` no-default).
    - install/install.two-root.test.sh — Check 12 parity shape (lines ~304-331) as the model for the resolution-parity assertion in Task 3.
  </read_first>
  <action>
    Build the split fixtures by EITHER (i) committing `scripts/fixtures/good-split-kit/` (just the `agent-factory/…` subtree + `AGENTS.md` + `.claude-plugin/`) and `scripts/fixtures/good-split-state/` (just `plans/…`, optionally `.grugops/`), reusing the committed-fixture discipline, OR (ii) building the two split roots inside `validate.test.sh` via `mktemp -d` (mirror install.two-root.test.sh's hermetic split). Pick ONE approach and apply it consistently. Add a `run_fixture_split <kit_root> <state_root> [flag]` driver that exports BOTH `VALIDATE_KIT_ROOT="$1"` and `VALIDATE_ROOT="$2"` then captures rc, and layer `expect_pass`/`expect_fail` over it. Add three checks: (a) GOOD split — `run_fixture_split <good-kit> <good-state>` → `expect_pass`. (b) BAD missing-kit — `VALIDATE_KIT_ROOT=<nonexistent dir>` + a valid state root → `expect_fail` asserting the output names a missing required role file (e.g. `roles/orchestrator.md` or "missing required"). (c) BAD unset-kit — invoke the validator DIRECTLY with `VALIDATE_KIT_ROOT` UNSET (not via `run_fixture`/`run_fixture_split`, which would set it): `( unset VALIDATE_KIT_ROOT; node "$VALIDATOR" 2>&1 )` → assert nonzero AND the output contains `VALIDATE_KIT_ROOT is unset` and `(C3)` (the no-`.`-fallback proof, SC4). Keep the existing single-tree GOOD/BAD fixtures and their invocations valid (now interpreted as STATE-root fixtures with a separately-supplied kit root, OR left as-is if they still pass with both roots pointed at the same tree — preserve back-compat). No `package.json`; stdlib-only validator; tests are hermetic (mktemp + trap cleanup; never mutate the real repo).
  </action>
  <verify>
    <automated>sh -n scripts/validate.test.sh && sh scripts/validate.test.sh; test $? -eq 0 && echo VALIDATE-TEST-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `sh -n scripts/validate.test.sh` exits 0 (syntax).
    - `sh scripts/validate.test.sh` exits 0 with all checks PASS (existing + new).
    - A GOOD-split check passes with separate kit + state roots.
    - A BAD missing-kit check (`VALIDATE_KIT_ROOT`→nonexistent) fails nonzero and names a missing required kit/role file.
    - A BAD unset-kit check invokes the validator with `VALIDATE_KIT_ROOT` UNSET (via an `unset` subshell, NOT through a driver that sets it) and asserts nonzero + output containing `VALIDATE_KIT_ROOT is unset` and `(C3)`.
    - The split fixtures exist (committed under `scripts/fixtures/good-split-*` OR built via `mktemp -d` in the harness) and the existing single-tree fixtures still pass as STATE-root fixtures (back-compat).
    - No `package.json` is created by the harness or the validator.
  </acceptance_criteria>
  <done>validate.test.sh proves SC3/SC4 for the validator — GOOD split passes, BAD missing-kit fails, and BAD unset-kit errors with the (C3) message (no silent `.`-fallback) — with the existing fixtures preserved and zero new dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Add the three-way resolution-parity assertion (sh doctor = Node doctor = Node validator)</name>
  <files>scripts/validate.test.sh</files>
  <read_first>
    - scripts/validate.test.sh — where the new parity check is added (after the Task-2 checks). Read the Node-presence guard (lines 32-33) for the skip-with-note pattern.
    - install/install.two-root.test.sh — Check 12 (lines ~304-331): the same-`$GRUGOPS_HOME` drive-all-then-byte-compare shape this assertion mirrors (it currently compares installer trees; here we compare a resolved kit-root VALUE across THREE programs).
    - install/install.sh + install/install.mjs — how the sh and Node doctors expose/print the resolved kit root (the doctor's findings name `KIT_ROOT`; the parity check must extract each program's notion of the resolved kit root for the same `GRUGOPS_HOME`).
    - scripts/validate-agent-factory.mjs — how the validator resolves `KIT_ROOT` from `VALIDATE_KIT_ROOT` (Plan 02) — for the validator, the "resolved kit root" is the explicit input, so the assertion confirms that feeding the doctor-resolved `${GRUGOPS_HOME}/agent-factory` into `VALIDATE_KIT_ROOT` yields the same path all three agree on.
    - .planning/phases/09-doctor-two-root-validator/09-PATTERNS.md — section "No Close Analog" → "Three-way resolution-parity assertion" (mirror two-root Check 12; skip-with-note if node absent) and the Shared Pattern "One-rule kit-root resolution".
    - .planning/phases/09-doctor-two-root-validator/09-RESEARCH.md — section Pitfall 2 (resolution drift breaks SC4) and section Validation Dimensions "Resolution agreement".
  </read_first>
  <action>
    Add a resolution-parity check, gated on `command -v node` (skip-with-note pass if node absent, mirroring the existing Node-presence pattern). For one fixed `GRUGOPS_HOME` (a `mktemp -d`), derive the expected kit root as `${GRUGOPS_HOME}/agent-factory` by the one rule, then: (1) capture the sh doctor's resolved kit root for that `GRUGOPS_HOME` (via `install.sh --check` output that names `KIT_ROOT`, or a minimal resolution probe); (2) capture the Node doctor's resolved kit root for the same `GRUGOPS_HOME` (`install.mjs --check`); (3) confirm the validator agrees by running it with `VALIDATE_KIT_ROOT="${GRUGOPS_HOME}/agent-factory"` and asserting it treats exactly that path as the kit root (no drift, no `.`-fallback). Assert all three resolved kit-root strings are byte-equal (normalize via the same `abspath`/`resolve` the programs use). If they disagree → `fail` naming the three values; if equal → `pass`. This is the SC4 "doctor passes and validator passes can never disagree" proof. Hermetic (mktemp + trap); never mutate the real repo or `$HOME`.
  </action>
  <verify>
    <automated>sh scripts/validate.test.sh; test $? -eq 0 && echo PARITY-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `sh scripts/validate.test.sh` exits 0 including the new parity check.
    - The parity check drives all three programs (sh doctor, Node doctor, Node validator) with ONE `GRUGOPS_HOME` and asserts they resolve the SAME kit root (byte-equal after normalization).
    - The check is gated on `command -v node` with a skip-with-note pass when node is absent (no false red on a node-less host).
    - On a forced mismatch (manually flipping one program's `GRUGOPS_HOME` in a scratch run) the check would `fail` naming the three values (assert the failure branch exists and names all three).
    - The check is hermetic — `mktemp -d` + trap cleanup; `git status` in the grugops checkout is clean after the run (no mutation).
  </acceptance_criteria>
  <done>The three-way resolution-parity assertion proves sh doctor = Node doctor = Node validator agree on the resolved kit root for the same `GRUGOPS_HOME` — the mechanical SC4 guarantee that "doctor passes" and "validator passes" can never disagree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → installer/doctor/validator | the harness drives the three programs under hermetic `mktemp -d` roots; the boundary is the env overrides (`GRUGOPS_HOME`/`GRUGOPS_SRC`/`TARGET`/`VALIDATE_KIT_ROOT`/`VALIDATE_ROOT`) |
| fixtures → programs under test | fixture tree content (committed or mktemp-built) is the input the programs validate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | a test check mutates the real repo or `$HOME` instead of a hermetic temp area | mitigate | Every new check runs under `mktemp -d` with `trap cleanup EXIT INT TERM` and env overrides (`GRUGOPS_HOME`/`TARGET`/`VALIDATE_*`); the read-only check asserts `git status` clean / snapshot-unchanged after a double `--check`. |
| T-09-05 | Spoofing (false green) | the harness passes without actually exercising the C3 guard, letting a false-green validator ship | mitigate | The BAD unset-kit check invokes the validator with `VALIDATE_KIT_ROOT` UNSET (not via a driver that sets it) and asserts the `(C3)` error — the mechanical proof the no-`.`-fallback guard fires. |
| T-09-08 | Tampering (parity drift) | sh and Node doctors / the validator drift on the resolved kit root undetected | mitigate | The three-way resolution-parity assertion (Task 3) byte-compares the resolved kit root across all three programs for one `GRUGOPS_HOME`; mismatch fails loudly. |
| T-09-10 | Tampering | a new check accidentally couples to or duplicates `check-kit-refs.sh` / `install.two-root.test.sh` | mitigate | D-09 honored: the new checks live only in `install.test.sh` and `validate.test.sh`; the deep two-root harness and the Phase-7 grep gate are read-only references, never edited (regression: both stay GREEN). |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Zero packages — POSIX `sh` + Node stdlib only, no test runner, no `package.json` (RESEARCH § Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `sh -n install/install.test.sh` and `sh -n scripts/validate.test.sh` exit 0 (syntax).
- Full suite green: `sh install/install.test.sh && sh install/install.two-root.test.sh && sh scripts/validate.test.sh && sh scripts/check-kit-refs.sh` exits 0.
- The three-way resolution-parity assertion passes (or skips-with-note if node absent).
- `git status` in the grugops checkout is clean after the suite (proves `--check` and the tests mutated nothing).
</verification>

<success_criteria>
- SC5: install.test.sh proves good-split→doctor-passes, missing-kit→doctor-fails-loudly, with idempotency/dry-run/reversibility preserved and the doctor read-only.
- SC1/SC2: deterministic first-failure + the full exit-code matrix + dangling-symlink FAIL are asserted.
- SC3/SC4: validate.test.sh proves the two-root split, the BAD missing-kit and BAD unset-kit (C3) must-fails, and the three-way resolution-parity assertion (doctor = validator on the kit root).
- D-09 honored: install.two-root.test.sh (18/18) and check-kit-refs.sh stay GREEN, separate, and untouched.
</success_criteria>

<output>
Create `.planning/phases/09-doctor-two-root-validator/09-04-SUMMARY.md` when done.
</output>

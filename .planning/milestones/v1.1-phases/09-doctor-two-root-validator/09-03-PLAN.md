---
phase: 09-doctor-two-root-validator
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - install/install.mjs
autonomous: true
requirements: [INSTALL-05]
tags: [doctor, install, node, byte-parity, two-root, kit-resolution]

must_haves:
  truths:
    - "install.mjs --check mirrors install.sh --check function-for-function: same pass/fail and the same named first-failure path for the same target + env"
    - "install.mjs --check on a dev/uninstalled checkout (no marker) prints the distinct 'not installed' line and exits nonzero, never throws a stack trace"
    - "install.mjs --check never mutates: it returns before copyKit / materializeAdapter / seedState / writeMarker"
    - "The Node doctor cross-checks the same three kit-root sources and emits the same WARN tier (skew + missing-seed) with the same exit-code matrix"
  artifacts:
    - path: "install/install.mjs"
      provides: "the Node byte-parity --check doctor: arg flags, doctor() mirroring install.sh, fail-closed JSON marker read, adapter KIT= parse, dangling-symlink via lstatSync, early-exit"
      contains: "--check"
  key_links:
    - from: "install/install.mjs doctor()"
      to: "GRUGOPS_HOME / KIT_ROOT (os.homedir + toPosix)"
      via: "reuses the resolved KIT_ROOT (source (a) of D-03, byte-identical to sh)"
      pattern: "KIT_ROOT"
    - from: "install/install.mjs doctor()"
      to: ".grugops/install.json"
      via: "fail-closed JSON.parse → marker.kitRoot/kitVersion (source (b) of D-03)"
      pattern: "install\\.json"
    - from: "install/install.mjs doctor()"
      to: ".claude/agents/grugops-orchestrator.md KIT= line"
      via: "scan the MAT_OPEN/MAT_CLOSE sentinel block (source (c) of D-03)"
      pattern: "materialized-kit"
---

<objective>
Add the `install.mjs --check` doctor — the Node byte-parity twin of the sh doctor (INSTALL-05). It mirrors `install.sh`'s `doctor()` function-for-function: same arg flags, same three-source kit-root cross-check (D-03), same deterministic ordered stat set and FIRST-failure reporting (D-02/D-05), same non-empty WARN tier (D-06/D-07), same exit-code matrix (SC2), and the same fold-into-FAIL "not installed" path. It uses the same parse-and-resolve strategy (D-01) as the sh doctor. It reuses the already-resolved `KIT_ROOT` (`os.homedir()` + `toPosix`, byte-identical to the sh side), reads the marker fail-closed via `JSON.parse` in try/catch, parses the adapter `KIT=` from the same sentinels, and detects dangling symlinks via `lstatSync` + `existsSync`.

Purpose: byte-parity is an existing hard contract — `install.sh --check` and `install.mjs --check` MUST agree on pass/fail AND the named first-failure path for the same target + env (the parity is proven in Plan 04, wave 3, mirroring two-root Check 12).
Output: `install/install.mjs` gains the `--check`/`--strict` doctor arm; the three-way resolution-parity assertion (Plan 04) ties sh doctor = Node doctor = Node validator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-doctor-two-root-validator/09-RESEARCH.md
@.planning/phases/09-doctor-two-root-validator/09-PATTERNS.md
@.planning/phases/09-doctor-two-root-validator/09-CONTEXT.md

# The behavioral spec for this twin — the sh doctor landed in Plan 01:
@install/install.sh
@.planning/phases/09-doctor-two-root-validator/09-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --check/--strict flags + non-mutating early-exit arm (Node)</name>
  <files>install/install.mjs</files>
  <read_first>
    - install/install.mjs — the file being modified. Read: the arg loop (lines 55-71 — add `--check`/`--strict` branches mirroring `--yes`/`--allow-self`, declare `let CHECK=false`/`let STRICT=false` near lines 50-53, KEEP the unknown-arg `process.exit(2)`); kit-root resolution (lines 86-92, `os.homedir()` + `toPosix`, source (a) of D-03 — reuse verbatim); the run banner site (`console.log("== grugops install ==")` ~line 460) — branch to `doctor(); process.exit(rc)` BEFORE it; `report` idiom (line 169).
    - install/install.sh — the BEHAVIORAL SPEC. Read the Plan-01 `doctor()` arm and its early-exit placement so the Node twin matches function-for-function (the sh `doctor; exit $?` branches before the install banner; mirror that).
    - .planning/phases/09-doctor-two-root-validator/09-PATTERNS.md — section "install/install.mjs — ADD the --check doctor (byte-parity twin)" (arg loop, resolver reuse, early-exit placement, parity contract).
    - .planning/phases/09-doctor-two-root-validator/09-RESEARCH.md — section "Pattern 1" (non-mutating early-exit) and section "Anti-Patterns" (never mutate; never touch the deploy-approval env var).
  </read_first>
  <action>
    In the install.mjs arg loop (lines 55-71) add an `else if (a === "--check") { CHECK = true; }` branch and an `else if (a === "--strict") { STRICT = true; }` branch, declaring `let CHECK = false;` and `let STRICT = false;` alongside `YES`/`ALLOW_SELF`. Keep the unknown-argument `process.exit(2)` branch unchanged. After `GRUGOPS_HOME`/`KIT_ROOT` resolve (lines 86-92) and after `TARGET` resolves (line ~124), but BEFORE the `== grugops install ==` banner and any install write, insert `if (CHECK) { const rc = doctor(); process.exit(rc); }`. The `doctor()` function (Task 2) returns the numeric exit code. The doctor is read-only by construction — it never calls `copyKit`/`materializeAdapter`/`seedState`/`writeMarker`, never writes, and NEVER reads/writes the deploy-approval env var (carried prohibition from INSTALL-02 / SAFE-02). Add `--check`/`--strict` to the usage comment block (mirror the sh side), lowercase `grugops`.
  </action>
  <verify>
    <automated>node -c install/install.mjs && grep -q 'a === "--check"' install/install.mjs && grep -q 'if (CHECK)' install/install.mjs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -c install/install.mjs` exits 0 (valid syntax).
    - The arg loop contains `a === "--check"` and `a === "--strict"` branches; `CHECK`/`STRICT` are declared; the unknown-arg `process.exit(2)` is preserved.
    - An `if (CHECK) { ... doctor() ... process.exit(rc); }` branch exists textually BEFORE the `== grugops install ==` banner (line-number ordering assertion).
    - The doctor branch contains no call to `copyKit`, `materializeAdapter`, `seedState`, or `writeMarker` (source assertion).
    - `grep -v '^//' install/install.mjs | grep -c 'GRUGOPS_PROD_DEPLOY_APPROVED'` returns 0 new occurrences in the doctor.
  </acceptance_criteria>
  <done>The Node doctor is wired as a non-mutating early-exit arm: `--check`/`--strict` parse, the branch fires after resolution and before the install banner, and no mutation function is reachable under `--check`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement doctor() in Node — byte-parity with the sh doctor</name>
  <files>install/install.mjs</files>
  <read_first>
    - install/install.mjs — `writeMarker` (lines 437-457, the 4-field `{kitVersion, grugopsHome, kitRoot, installMode}` schema to read back); the `MAT_OPEN`/`MAT_CLOSE` sentinels (lines 327-329, byte-identical to sh) and the `materializeAdapter` strip-loop (lines 345-379, the shape to mirror for parsing `KIT="…"`); `isSymlink` (lines 175-181, `lstatSync().isSymbolicLink()`, pair with `existsSync` which follows the link → dangling = `isSymlink(p) && !existsSync(p)`); `report` (line 169); `KIT_ROOT`/`GRUGOPS_HOME`/`TARGET` (lines 86-124).
    - install/install.sh — the Plan-01 `doctor()` (the behavioral spec): the three-source cross-check, the fixed ordered stat list, the WARN tier, the exit-code logic, the "not installed" message. The Node twin MUST produce the SAME pass/fail, the SAME first-failure path string, and the SAME findings/messages for the same target + env.
    - .planning/phases/09-doctor-two-root-validator/09-PATTERNS.md — section "install/install.mjs" (marker fail-closed JSON.parse, adapter KIT= parse, dangling-symlink via lstatSync, report idiom, parity contract) and section "No Close Analog" (D-03 cross-check, "not installed" early-FAIL, skew/missing-seed WARNs).
    - .planning/phases/09-doctor-two-root-validator/09-RESEARCH.md — section Code Examples (Node marker read; KIT= extraction), section Pattern 2/3, section Discretion Resolutions 1/2/5/6, section Pitfall 5 (don't crash on the dev checkout).
  </read_first>
  <action>
    Add a `doctor()` function returning a numeric exit code, mirroring the sh `doctor()` exactly. (1) Marker read: a try/catch around `JSON.parse(readFileSync(join(TARGET, ".grugops", "install.json"), "utf8"))`; on throw call `notInstalled()` (which prints the SAME distinct line as the sh side — `grugops not installed in <target> — run install.sh (then install.sh --check)`, lowercase grugops, clear voice) and return 1. Destructure `{ kitRoot, kitVersion }`. (2) Adapter `KIT=` parse: read `.claude/agents/grugops-orchestrator.md`, split on `"\n"`, track `inblk` between `MAT_OPEN`/`MAT_CLOSE`, capture the `KIT="…"` line, strip the quotes — fail-closed (absent/garbled → finding, never throw). (3) Three-source cross-check (D-03): `a = KIT_ROOT` (re-resolved), `b = resolve(marker.kitRoot)`, `c = resolve(adapterKit)`; if all equal → pass; differ-but-all-real-and-cosmetic → `warn`; any unresolvable or genuinely-different real kits → `fail` naming all three (bias to FAIL). (4) Iterate the SAME fixed ordered tuple list as the sh side: `KIT_ROOT` dir → `roles/orchestrator.md` → `roles/_role-switch-protocol.md` → `workflows/` (under KIT_ROOT) → `.grugops/factory.config.json` → `plans/board.md` → `plans/handoffs/` (under TARGET); for each, `existsSync` to stat, and `isSymlink(p) && !existsSync(p)` → dangling → `fail` with the symlink-specific message (D-05). On the FIRST failure print `FAIL  <resolved-path>  (referenced by <file>)` via the report idiom and STOP. (5) WARN tier (D-06/D-07, detect-only): if clean, compare `marker.kitVersion` to the contents of `${KIT_ROOT}/VERSION` → `warn` skew if unequal; `existsSync(join(TARGET, "memory-bank", "00-index.md"))` → `warn` missing-seed if absent. (6) Exit-code logic identical to sh: FAILs>0 → print count, return nonzero; else WARNs>0 && STRICT → return nonzero; else print `ALL CHECKS PASSED`, return 0. Use the SAME message strings as the sh doctor so the parity test's first-failure-path comparison passes. Do NOT re-walk the whole kit tree (RESEARCH Discretion 2). Read-only, fail-closed, lowercase grugops.
  </action>
  <verify>
    <automated>node -c install/install.mjs && T=$(mktemp -d) && H=$(mktemp -d) && { GRUGOPS_HOME="$H" TARGET="$T" node install/install.mjs --check >/tmp/d.out 2>&1; rc=$?; }; rm -rf "$T" "$H"; test "$rc" -ne 0 && grep -qi 'not installed' /tmp/d.out && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -c install/install.mjs` exits 0 (valid syntax).
    - On an empty target (no `.grugops/install.json`): `GRUGOPS_HOME=$(mktemp -d) TARGET=$(mktemp -d) node install/install.mjs --check` exits nonzero and prints a line containing `not installed` — never a Node stack trace.
    - `install/install.mjs` defines a `doctor()` returning a numeric code, a `notInstalled()` helper, and an adapter-`KIT=` parser scanning the `materialized-kit` sentinel block.
    - The Node doctor reads `kitRoot`/`kitVersion` from the marker via a try/catch `JSON.parse` (fail-closed source assertion).
    - The ordered stat list names `roles/orchestrator.md`, `roles/_role-switch-protocol.md`, `workflows`, `.grugops/factory.config.json`, `plans/board.md`, `plans/handoffs` in the SAME order as the sh doctor (source assertion).
    - Dangling-symlink detection uses `isSymlink(p) && !existsSync(p)` (source assertion).
    - The doctor's success line is `ALL CHECKS PASSED` and the not-installed message string is byte-identical to the sh side (parity prerequisite for Plan 04).
  </acceptance_criteria>
  <done>`doctor()` in Node mirrors the sh doctor function-for-function — same cross-check, ordered first-failure, WARN tier, exit-code matrix, and "not installed" message — fail-closed and read-only, so the Plan-04 parity test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → `install.mjs --check` | the operator runs the Node doctor; it reads the target's marker + adapters (possibly user-edited) and the shared `$GRUGOPS_HOME` kit |
| filesystem → doctor reads | marker JSON, adapter content, VERSION, and symlink targets are untrusted input crossing into the parse-and-stat logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering / Elevation | Node doctor follows a symlink out of the kit/repo and validates attacker content | mitigate | Resolve every path absolute via `resolve`/`join` before stat; dangling/escaping symlink (`isSymlink && !existsSync`) is a FAIL, never a follow-and-read (D-05; RESEARCH § Security Domain row 1). |
| T-09-02 | Tampering | `--check` accidentally mutates (re-materializes/re-seeds/rewrites marker) | mitigate | Doctor is a non-mutating early-exit arm (Task 1); returns before `copyKit`/`materializeAdapter`/`seedState`/`writeMarker`. |
| T-09-03 | Elevation (crosses the hard safety line) | Node doctor reads/writes the prod-deploy approval env var | mitigate | Carry the prohibition forward verbatim: reads `GRUGOPS_HOME` only; never names the deploy-approval env var (acceptance asserts zero new occurrences). |
| T-09-04 | DoS | garbled marker/adapter content throws an unhandled Node exception (stack trace instead of a finding) | mitigate | Fail-closed `JSON.parse` in try/catch and guarded adapter reads → a bad/absent marker becomes "not installed / corrupt marker", never an unhandled throw (RESEARCH Pitfall 5 / § Security Domain row 5). |
| T-09-08 | Tampering (parity drift) | the Node doctor and sh doctor disagree on pass/fail or the first-failure path (breaks SC4) | mitigate | Mirror `resolve_grugops_home`/`os.homedir()+toPosix` verbatim, reuse the SAME ordered stat list and message strings; the Plan-04 sh↔Node parity check + three-way resolution-parity assertion enforce agreement. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Zero packages — Node stdlib only, no `package.json` (RESEARCH § Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `node -c install/install.mjs` exits 0 (syntax).
- `install.mjs --check` on an empty target (no marker) exits nonzero with a `not installed` line and no stack trace.
- The deep two-root harness still passes (regression): `sh install/install.two-root.test.sh` exits 0 (no install-path behavior changed).
- sh↔Node doctor parity (same pass/fail + first-failure path) and the three-way resolution-parity assertion are proven in Plan 04 (wave 3).
</verification>

<success_criteria>
- INSTALL-05 (Node side): `install.mjs --check` is byte-parity with `install.sh --check` — same cross-check (D-03), same ordered first-failure (D-02/D-05), same WARN tier (D-06/D-07), same exit-code matrix (SC2), same "not installed" fold-into-FAIL.
- Read-only by construction, fail-closed parsing, lowercase grugops, no deploy-approval env var.
- `install.two-root.test.sh` remains GREEN and untouched.
</success_criteria>

<output>
Create `.planning/phases/09-doctor-two-root-validator/09-03-SUMMARY.md` when done.
</output>

---
phase: 10-sdlc-coverage-audit-foundation-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.2-SDLC-COVERAGE-AUDIT.md
autonomous: true
requirements: [SDLC-01]
tags: [sdlc, audit, lifecycle-coverage]

must_haves:
  truths:
    - "A committed audit artifact exists at .planning/v1.2-SDLC-COVERAGE-AUDIT.md (internal planning artifact, NOT shipped, NOT in docs/) — per D-01"
    - "The audit reviews all 16 roles + 14 workflows against the 9 PROJECT.md lifecycle stages in a coverage matrix (each cell covered/partial/gap) — per D-02"
    - "Every real gap carries a short narrative AND maps to the v1.2 phase (11–17) that closes it; any gap the roadmap does NOT cover is flagged — per D-03"
    - "The business→engineer handoff gap is called out explicitly (milestone named focus)"
    - "The audit confirms roadmap sufficiency; it does NOT re-scope or rewrite the roadmap — per D-03"
  artifacts:
    - path: ".planning/v1.2-SDLC-COVERAGE-AUDIT.md"
      provides: "SDLC lifecycle-coverage audit (matrix + per-gap narrative + gap→phase mapping)"
      contains: "| Role / Workflow |"
      min_lines: 60
  key_links:
    - from: ".planning/v1.2-SDLC-COVERAGE-AUDIT.md"
      to: "v1.2 roadmap phases 11–17"
      via: "gap→phase mapping table"
      pattern: "Phase 1[1-7]"
---

<objective>
Produce the SDLC-coverage audit (SDLC-01 / SC1): a committed internal `.planning/` artifact that reviews all 16 roles + 14 workflows for full-lifecycle completeness against the canonical PROJECT.md lifecycle, records every real gap with a narrative, and maps each gap to the v1.2 phase (11–17) that closes it. The business→engineer handoff is the named focus.

Purpose: Scope the rest of the milestone with evidence — confirm the v1.2 roadmap (phases 11–17) is sufficient to close the lifecycle gaps, and flag any gap the roadmap does NOT cover. This is an engineering input over grugops's own kit, not a user-facing capability.
Output: `.planning/v1.2-SDLC-COVERAGE-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-CONTEXT.md
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-RESEARCH.md
@.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-PATTERNS.md
@.planning/milestones/v1.1-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SDLC-coverage audit artifact</name>
  <files>.planning/v1.2-SDLC-COVERAGE-AUDIT.md</files>
  <read_first>
    - `.planning/milestones/v1.1-MILESTONE-AUDIT.md` — the naming + structured-audit precedent (D-01): mirror its YAML-frontmatter + markdown-tables + per-gap-narrative + verdict shape (PATTERNS lines 347–393).
    - `.planning/PROJECT.md` — read the canonical lifecycle (business analysis → product → system analysis → architecture → engineering → QE/E2E → security/NFR/compliance → UAT → release); these 9 stages are the matrix columns (D-02, RESEARCH lines 464–467).
    - `.planning/ROADMAP.md` § "v1.2 ... Phases 10–17" — the per-phase goals each gap maps to (D-03); the phase that closes each gap.
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-RESEARCH.md` lines 449–477 — the VERIFIED enumeration of the 16 roles, 14 workflows, 9 stages, and the matrix skeleton.
    - `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-PATTERNS.md` lines 347–393 — the audit frontmatter shape, matrix shape, and gap→phase mapping table shape to clone.
    - Each role file under `agent-factory/roles/` (16 roles, excluding `_role-switch-protocol.md`) and each workflow under `agent-factory/workflows/` (14 workflows) — read the `## One job` / `## Activates when` / `## Responsibilities` / Flow sections to score each cell honestly. Do NOT fabricate a coverage cell; if a role's lifecycle reach is unclear, mark `UNKNOWN - verify`, never invent.
  </read_first>
  <action>
    Create `.planning/v1.2-SDLC-COVERAGE-AUDIT.md` as a NEW internal planning artifact (D-01 — lives in `.planning/`, NOT shipped in the kit, NOT in `docs/`; filename mirrors `.planning/milestones/v1.1-MILESTONE-AUDIT.md`).

    Structure (D-02 / D-03):
    1. YAML frontmatter mirroring `v1.1-MILESTONE-AUDIT.md`: `milestone: v1.2`, `milestone_name: SDLC Coverage & Foundation Guards`, `audited: <ISO8601 date 2026-06-09>`, `status: <coverage verdict>`, plus a `gaps:` block listing each lifecycle gap with the phase that closes it.
    2. A lifecycle-stage × role/workflow coverage MATRIX. Columns = the 9 PROJECT.md stages verbatim (1 BA · 2 Prod · 3 SysA · 4 Arch · 5 Eng · 6 QE · 7 Sec · 8 UAT · 9 Rel). Rows = the 16 roles THEN the 14 workflows (30 rows total). Cells use the legend `● covered · ◐ partial · — gap`. Score each cell from the actual role/workflow content read above — do not guess.
    3. A short NARRATIVE per real gap (each `—` / `◐` that represents a genuine lifecycle hole, not a stage a role is simply out of scope for).
    4. A "Gaps → addressing phase" table mapping each gap to the v1.2 phase (11–17) per D-03, with a flag column for any gap the roadmap does NOT cover. The audit CONFIRMS roadmap sufficiency — it does NOT re-scope or rewrite the roadmap (no new phases, no scope creep, per D-03).
    5. An explicit call-out of the business→engineer handoff gap (the milestone's named focus) — name which phase (12 BDD/TDD per the roadmap) closes it.
    6. A short "Verdict" section (mirror the v1.1 audit's verdict section) stating whether the roadmap is sufficient.

    Record (in a note, NOT as a guard-scope change) that `agent-factory/packaging/adapters.md` carried stale "Orchestrator spawns role agents with the Agent tool" prose, fixed in Plan 10-02 per D-09 — the audit observes the doc defect; the one-time fix lives in the guards plan.

    Do NOT shorten, defer, or label any section a "v1" / "basic version". This is the full audit. Caveman voice is NOT used here — this is a clear-voice engineering artifact.
  </action>
  <verify>
    <automated>test -f .planning/v1.2-SDLC-COVERAGE-AUDIT.md && grep -q '| Role / Workflow |' .planning/v1.2-SDLC-COVERAGE-AUDIT.md && [ "$(grep -c '^| ' .planning/v1.2-SDLC-COVERAGE-AUDIT.md)" -ge 30 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/v1.2-SDLC-COVERAGE-AUDIT.md` succeeds (file exists at the D-01 path).
    - `grep -q '| Role / Workflow |' .planning/v1.2-SDLC-COVERAGE-AUDIT.md` succeeds (the lifecycle matrix header is present).
    - The matrix has ≥ 30 table-data rows (16 roles + 14 workflows) — `grep -c '^| '` returns ≥ 30 (header + separator + 30 data ≥ 32; the gate floor is 30).
    - `grep -qiE 'business.{0,3}engineer' .planning/v1.2-SDLC-COVERAGE-AUDIT.md` succeeds (business→engineer handoff called out).
    - `grep -qE 'Phase 1[1-7]' .planning/v1.2-SDLC-COVERAGE-AUDIT.md` succeeds (each gap maps to a v1.2 phase).
    - The file does NOT live under `docs/` and is NOT added to `agent-factory/` (D-01): `test ! -e docs/v1.2-SDLC-COVERAGE-AUDIT.md`.
    - No coverage cell is fabricated: any cell the executor could not verify reads `UNKNOWN - verify`, never an invented `●`.
  </acceptance_criteria>
  <done>The audit artifact exists at `.planning/v1.2-SDLC-COVERAGE-AUDIT.md` with a 30-row 9-stage matrix, a per-gap narrative, a gap→phase mapping table flagging any uncovered gap, and an explicit business→engineer handoff call-out — confirming roadmap sufficiency without re-scoping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → audit artifact | The audit is a self-assessment of grugops's own kit; the integrity risk is a fabricated coverage claim, not external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-R | Repudiation | audit coverage cells | mitigate | No-fabrication contract (CLAUDE.md): any unverifiable cell reads `UNKNOWN - verify`, never an invented `●`; each cell is scored from the actually-read role/workflow content |
| T-10-01-I | Information Disclosure | audit location | accept | Artifact lives in `.planning/` (internal, not shipped, not user-facing per D-01) — no secrets, no PII; the only "leak" is grugops's own roadmap, which is public-repo content |
| T-10-01-T | Tampering | roadmap scope | mitigate | The audit CONFIRMS roadmap sufficiency and is explicitly forbidden (D-03) from re-scoping/rewriting the roadmap — it cannot silently add or drop phases |

No network, no auth, no executable surface — this plan emits one markdown file.
</threat_model>

<verification>
- `test -f .planning/v1.2-SDLC-COVERAGE-AUDIT.md` (file exists)
- `grep -q '| Role / Workflow |' .planning/v1.2-SDLC-COVERAGE-AUDIT.md` (matrix present)
- `grep -c '^| ' .planning/v1.2-SDLC-COVERAGE-AUDIT.md` ≥ 30 (full role+workflow coverage)
- `grep -qE 'Phase 1[1-7]' .planning/v1.2-SDLC-COVERAGE-AUDIT.md` (gap→phase mapping)
- Human review (Manual-Only, 10-VALIDATION.md): each gap narrative is real and correctly mapped; business→engineer handoff gap is called out
</verification>

<success_criteria>
- SC1 met: a committed audit artifact reviews all 16 roles + 14 workflows for lifecycle completeness and records the gaps it finds (named focus: business→engineer handoff).
- The audit maps each gap to the closing v1.2 phase and confirms roadmap sufficiency without re-scoping (D-03).
</success_criteria>

<output>
Create `.planning/phases/10-sdlc-coverage-audit-foundation-guards/10-01-SUMMARY.md` when done.
</output>

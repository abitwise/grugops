---
phase: 12-bdd-tdd-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/handoffs/product-handoff.md
  - agent-factory/handoffs/qe-handoff.md
autonomous: true
requirements: [BDD-01]
must_haves:
  truths:
    - "Acceptance behavior is carried as a declarative Given/When/Then contract in both the product and QE handoff templates (criteria stays the bar; scenarios are the contract)"
    - "The scenario template states the hard no-selectors rule (business language only; UI detail behind step definitions)"
    - "The scenario block is tier-aware (bdd off=omit / lean=inline / strict=link selector-free files) and degrades to lean when the dial is absent"
    - "A scenario→trace additive comment convention is noted (D-14) — a scenario can map 1:1 to a traceability row in-cell, NOT a schema/column rename"
  artifacts:
    - path: "agent-factory/handoffs/product-handoff.md"
      provides: "## Acceptance scenarios (Given/When/Then) block alongside the preserved criteria line + D-14 trace convention note"
      contains: "## Acceptance scenarios (Given/When/Then)"
    - path: "agent-factory/handoffs/qe-handoff.md"
      provides: "## Acceptance scenarios block (mirror of product-handoff); QE owns the outer loop"
      contains: "## Acceptance scenarios"
  key_links:
    - from: "agent-factory/handoffs/product-handoff.md"
      to: "agent-factory/handoffs/qe-handoff.md"
      via: "identical ## Acceptance scenarios block shape (1:1 mirror)"
      pattern: "## Acceptance scenarios \\(Given/When/Then\\)"
---

<objective>
Add the declarative Given/When/Then acceptance-scenarios contract to the product and QE handoff templates — the BDD-01 business→engineer contract carried as handoff data. The existing `## Acceptance criteria (Given/When/Then)` line in product-handoff stays as the terse DoR-style bar; the new `## Acceptance scenarios` block is the executable contract beside it (D-02). The block is tiered across the `bdd` dial (D-01) and carries the hard no-selectors rule (D-03). A light scenario→trace additive-convention note (D-14) records that a scenario can map 1:1 to a traceability row in-cell — NOT a schema rename.

Purpose: Acceptance behavior must travel role→role as a declarative, selector-free contract that is executable-or-absent — no Gherkin nobody runs. This is the QE/business-owned outer-loop artifact (D-07) the rest of the phase rides on.
Output: Two modified handoff templates, each carrying the new `## Acceptance scenarios (Given/When/Then)` block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md
@.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md
@agent-factory/handoffs/product-handoff.md
@agent-factory/handoffs/qe-handoff.md
@agent-factory/checklists/definition-of-ready.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the tiered Acceptance scenarios block to product-handoff.md (+ D-14 trace convention note)</name>
  <files>agent-factory/handoffs/product-handoff.md</files>
  <read_first>
    - `agent-factory/handoffs/product-handoff.md` — current structure: the second-half template is bare `## Heading` slots (empty-but-shaped, no inline data); `## Acceptance criteria (Given/When/Then)` is at line 30, `## Test notes` at line 33; the universal-header `## Trace updates` line is at line 21 (carries the existing in-cell trace convention comment).
    - `.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md` § "product-handoff.md" — the insert-after-line-30 convention, the empty-but-shaped register, and the tiered HTML-comment shape; § "Shared Patterns → Empty-but-shaped handoff template register" and "→ Dial-read with degrade-to-lean".
    - `.planning/phases/12-bdd-tdd-wiring/12-RESEARCH.md` § Code Examples "The `## Acceptance scenarios` block — tiered shape" (the authoritative content) and Pattern 1 (declarative-not-imperative).
    - `.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md` D-01 (tiered off/lean/strict), D-02 (distinct block, criteria stays), D-03 (no-selectors rule), D-14 (scenario→trace additive comment convention — NOT a schema rename).
  </read_first>
  <action>
    Insert a new `## Acceptance scenarios (Given/When/Then)` heading immediately AFTER the existing `## Acceptance criteria (Given/When/Then)` line (currently line 30). The existing criteria line STAYS unchanged — it is the terse DoR-style bar; scenarios are the executable contract (D-02). Under the new heading add, in the file's empty-but-shaped register (no fake data): (a) a dial-read HTML comment naming the `bdd` key and its three tiers with the literal lean default — `off = omit · lean = inline declarative G/W/T below · strict = link selector-free scenario files wired to host step definitions; default lean` (D-01); (b) on the SAME comment or an adjacent one, the hard no-selectors rule in declarative-business-language terms — "Declarative business language only — NO CSS/HTML/selectors in Given/When/Then; UI detail lives behind step definitions. Executable-or-absent." (D-03); (c) a bare `Scenario: / Given / When / Then` skeleton (one observable business behavior, no sample data); (d) a strict-tier reference note as an HTML comment that points to the host scenario file + names the host runner as `UNKNOWN - verify` (do NOT pin a layout or extension — host-agnostic per D-01 discretion / RESEARCH A1). Keep the scenario example, if any is shown, declarative per Pattern 1 (no `#id`, `.class`, `click`, `navigate to`). **D-14 (light convention):** add ONE HTML-comment line near the existing `## Trace updates` heading (line ~21) noting the additive comment-documented convention — a scenario can map 1:1 to a `plans/traceability.md` row in-cell via the existing comment convention; this is NOT a schema/column rename of the frozen trace header (lean at the lean tier; 1:1 linkage is the enterprise direction). Do NOT modify any other heading.
  </action>
  <verify>
    <automated>grep -q '^## Acceptance scenarios (Given/When/Then)' agent-factory/handoffs/product-handoff.md && grep -q '^## Acceptance criteria (Given/When/Then)' agent-factory/handoffs/product-handoff.md && grep -qi 'no .*selectors\|business language' agent-factory/handoffs/product-handoff.md && grep -q 'bdd' agent-factory/handoffs/product-handoff.md && grep -qi 'traceability\|trace row\|trace' agent-factory/handoffs/product-handoff.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^## Acceptance scenarios (Given/When/Then)' agent-factory/handoffs/product-handoff.md` exits 0 (block present).
    - `grep -q '^## Acceptance criteria (Given/When/Then)' agent-factory/handoffs/product-handoff.md` exits 0 (criteria line preserved — criteria ≠ scenarios).
    - `grep -qi 'no .*selectors\|business language' agent-factory/handoffs/product-handoff.md` exits 0 (no-selectors rule present).
    - `grep -q 'bdd' agent-factory/handoffs/product-handoff.md` exits 0 (dial-read present).
    - `grep -qi 'traceability\|trace' agent-factory/handoffs/product-handoff.md` exits 0 (D-14 scenario→trace convention note present); the trace header is NOT renamed — `git diff` shows no column/header rename, only an added comment line.
    - No selector tokens in any example scenario: `! grep -nE '#[a-zA-Z]|\.[a-z-]+ |click |navigate to' agent-factory/handoffs/product-handoff.md` (manual confirm the example reads declaratively).
  </acceptance_criteria>
  <done>The product-handoff template carries the tiered, selector-free `## Acceptance scenarios` block beside the preserved criteria line, plus the D-14 additive scenario→trace convention note.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the Acceptance scenarios block into qe-handoff.md (QE owns the outer loop)</name>
  <files>agent-factory/handoffs/qe-handoff.md</files>
  <read_first>
    - `agent-factory/handoffs/qe-handoff.md` — current structure: second-half headings `## Test scope` … `## Unit/integration/E2E coverage` (line 28) … `## Result` (line 35) … `## Gaps`; there is NO scenario block today.
    - `agent-factory/handoffs/product-handoff.md` — the block authored in Task 1 (mirror it 1:1 so the contract shape is identical across both handoffs).
    - `.planning/phases/12-bdd-tdd-wiring/12-PATTERNS.md` § "qe-handoff.md" — mirror product-handoff's block; insert near `## Unit/integration/E2E coverage` (line 28).
    - `.planning/phases/12-bdd-tdd-wiring/12-CONTEXT.md` D-02 (block in BOTH handoffs), D-07 (QE owns the outer acceptance loop).
  </read_first>
  <action>
    Add the SAME `## Acceptance scenarios (Given/When/Then)` block authored in Task 1 — byte-for-byte the same heading text and the same tiered/no-selectors HTML comment + skeleton shape — into `qe-handoff.md`, placed near `## Unit/integration/E2E coverage` (line 28) so the QE handoff carries the acceptance contract QE owns (D-07). Keep the empty-but-shaped register (no fake data). This task adds the scenarios block ONLY; the acceptance-side red/green evidence field is a separate concern owned by plan 12-04 (qe-handoff is touched by both plans but the evidence field lands in 12-04 — coordinate by NOT adding any evidence/red-green field here). Do NOT modify any other heading.
  </action>
  <verify>
    <automated>grep -q '^## Acceptance scenarios' agent-factory/handoffs/qe-handoff.md && grep -qi 'no .*selectors\|business language' agent-factory/handoffs/qe-handoff.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^## Acceptance scenarios' agent-factory/handoffs/qe-handoff.md` exits 0 (block present in qe-handoff).
    - `grep -qi 'no .*selectors\|business language' agent-factory/handoffs/qe-handoff.md` exits 0 (no-selectors rule carried).
    - The `## Acceptance scenarios (Given/When/Then)` heading text matches product-handoff exactly: `diff <(grep -A0 '^## Acceptance scenarios' agent-factory/handoffs/product-handoff.md) <(grep -A0 '^## Acceptance scenarios' agent-factory/handoffs/qe-handoff.md)` shows identical heading lines.
    - No red/green or evidence field added in this task (that is 12-04): `! grep -qi 'test-first\|red.*green' agent-factory/handoffs/qe-handoff.md` after this task.
  </acceptance_criteria>
  <done>The qe-handoff template carries the same `## Acceptance scenarios` block as product-handoff; QE owns the outer-loop contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan edits markdown handoff templates in a no-runtime kit. No executable code, no input handling, no auth/crypto/data/network surface is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-DG | Repudiation | The `## Acceptance scenarios` block | mitigate | Dead/decorative scenarios giving false assurance of acceptance coverage are countered by the executable-or-absent rule stated in the block (D-01) and the no-selectors declarative rule (D-03). Mechanical detection of dead Gherkin is deferred to the Phase 15 test-integrity gate — this plan lands the rule so the artifact is honest by construction. |

> No application-security threats are introduced (RESEARCH § Security Domain: no code/input/auth/data surface in this phase). The substantive ASVS audit is Phase 14.
</threat_model>

<verification>
- `sh scripts/check-foundation-guards.sh` exits 0 (these handoff edits touch no role file or adapter; guards stay GREEN for free).
- Both handoffs carry `^## Acceptance scenarios` (grep exits 0).
- product-handoff still carries `^## Acceptance criteria (Given/When/Then)` (criteria preserved) and the D-14 trace convention note (no header rename).
- No selector tokens in example scenarios (declarative prose-judgment spot check).
</verification>

<success_criteria>
BDD-01 acceptance-contract artifact lands in both product and QE handoffs: a distinct, tiered, selector-free `## Acceptance scenarios (Given/When/Then)` block beside the preserved criteria bar, degrading to lean when the `bdd` dial is absent; the D-14 additive scenario→trace convention is noted without a schema rename.
</success_criteria>

<output>
Create `.planning/phases/12-bdd-tdd-wiring/12-01-SUMMARY.md` when done.
</output>

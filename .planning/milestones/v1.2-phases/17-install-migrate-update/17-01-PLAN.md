---
phase: 17-install-migrate-update
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - install/install.ts
  - install/install.js
  - install/install.test.ts
autonomous: true
requirements: [MIGR-01, UPD-01]
must_haves:
  truths:
    - "D-13 honored: there is NO install.sh — the new modes are flags on the single TypeScript installer and run as `node install/install.js --migrate|--update|--prune-old-kit`; the byte-parity sh/Node wording in MIGR-01 / SC1 is superseded"
    - "The 3 new flags --migrate, --update, --prune-old-kit are recognized by the arg-parse loop; an unknown arg still exits 2 (Contract / arg parse)"
    - "backupIfDiffers() creates a timestamped backup ONLY when the displaced content differs (D-09 differs-only no-op guard); millisecond-ISO, filesystem-safe (no ':')  (D-08)"
    - "copyKit() accepts a retainBackup flag that renames the displaced kit to a timestamped backup instead of deleting it — single-source, no forked path (D-06/D-02)"
    - "makeOldLayoutFixture() produces the v1.0 migrate-from shape: in-repo agent-factory/ + repo-relative (and symlink-variant) .claude adapters + NO .grugops/install.json marker (D-03)"
    - "DRY_RUN=1 short-circuits backupIfDiffers and copyKit(retainBackup) with would-* lines (hard contract)"
  artifacts:
    - path: "install/install.ts"
      provides: "isoStamp(), backupIfDiffers(), copyKit(retainBackup) parameter, 3 new arg-parse cases"
      contains: "function backupIfDiffers"
    - path: "install/install.test.ts"
      provides: "makeOldLayoutFixture() helper + RED-by-design migrate/update flag-recognition cases"
      contains: "function makeOldLayoutFixture"
    - path: "install/install.js"
      provides: "committed compiled output; must be fresh per scripts/freshness.ts"
  key_links:
    - from: "install/install.test.ts"
      to: "install/install.js"
      via: "spawnSync drives the committed .js (not the .ts)"
      pattern: "spawnSync.*install\\.js"
    - from: "install/install.ts (copyKit retainBackup)"
      to: "install/install.ts (backupIfDiffers / isoStamp)"
      via: "retain path calls the shared backup helper"
      pattern: "retainBackup"
---

<objective>
Lay the Wave-0 foundation that both `--migrate` (Plan 02) and `--update` (Plan 03) build on: the new arg-parse flag recognition, the single shared backup helper, the `copyKit()` retain-backup parameter, and the one genuinely new test helper `makeOldLayoutFixture()`. This is the RED-harness-first keystone — the failing Vitest cases that lock the safety contract land here before any mode is implemented.

Per the locked decisions: D-02 (migrate is orchestration around the existing install run — no forked code path), D-06 (update retains the displaced kit as a backup), D-08 (millisecond-ISO, filesystem-safe timestamped backups; harness asserts glob/count, never an exact filename), D-09 (back up only when content differs → a true no-op), and the v1.1 CR-01 bounded-marker-strip contract that migrate/update must not regress.

Carried-forward constraint honored throughout (D-13): there is NO `install.sh` — the new modes are flags on the single TypeScript installer and run as `node install/install.js --migrate|--update|--prune-old-kit`. The "byte-parity sh/Node" wording in MIGR-01 / ROADMAP SC1 is superseded; the committed `install/install.js` (the artifact the harness drives) is rebuilt with `tsc` and must pass `npm run freshness` before any commit of the `.js`.

Purpose: Establish single-source primitives so Plans 02 and 03 do not each invent their own backup/timestamp logic (which would drift) — directly serves MIGR-01 and UPD-01.
Output: `install/install.ts` gains `isoStamp()`, `backupIfDiffers()`, a `copyKit(retainBackup)` parameter, and 3 recognized flags; `install/install.test.ts` gains `makeOldLayoutFixture()` and the flag-recognition cases; `install/install.js` rebuilt + fresh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-install-migrate-update/17-CONTEXT.md
@.planning/phases/17-install-migrate-update/17-RESEARCH.md
@.planning/phases/17-install-migrate-update/17-VALIDATION.md

@install/install.ts
@install/install.test.ts
@package.json
@scripts/freshness.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recognize the 3 new flags + add isoStamp() and backupIfDiffers() helper (RED-then-GREEN)</name>
  <files>install/install.ts, install/install.test.ts</files>
  <read_first>
    - install/install.ts — arg-parse loop (lines 60-87, the `for` over argv with `--flag`/`--flag=value`; unknown args `process.exit(2)`); the boolean flag vars block (60-65); `sameContent()` (430-436); `report()` + DRY_RUN short-circuit idiom (416-419, 470-473); `isSymlink()` (422-428); `rmSync`/`renameSync`/`existsSync` imports (38-52)
    - install/install.test.ts — the existing `unknown-arg` case (431-436) to extend; `makeFixture()` (66-77) as the shape to mirror for the new helper; `runInstall()` (108-114) and `snapshot()` (83-104)
    - package.json — `build`/`freshness`/`test` scripts (8-13)
    - scripts/freshness.ts — the rebuild-to-temp byte-compare gate (the contract the committed .js must pass)
  </read_first>
  <behavior>
    - Test (RED first): a new flag-recognition case `unknown-arg: --migrate / --update / --prune-old-kit recognized; others still exit 2` asserts `node install.js --migrate --check` (or a no-op-safe invocation) does NOT exit 2, and `node install.js --bad-arg-xyz` still exits 2.
    - Test (RED first): a unit-level assertion that `backupIfDiffers` exists in the compiled output — grep the committed `install.js` for `backupIfDiffers` (source-presence assertion) OR a behavioral case proving differs-only via Plan 02/03 paths (defer behavioral to those plans; here assert recognition + presence).
    - isoStamp() returns an ISO string with every ':' replaced (filesystem-safe), millisecond precision: shape `YYYY-MM-DDTHH-MM-SS.mmmZ` (no colon).
    - backupIfDiffers(path, replacement): if `path` is byte-identical to `replacement` → skip (no artifact, D-09); else rename `path` aside to `${path}.bak.${isoStamp()}` and `report()` it. Honors DRY_RUN with a `would-*` line.
  </behavior>
  <action>
    Add three new boolean flag vars beside the existing block (ARG_TARGET/YES/ALLOW_SELF/ARG_SYMLINK/CHECK/STRICT): `MIGRATE`, `UPDATE`, `PRUNE_OLD_KIT`, all default false. In the arg-parse `for` loop add `else if (a === "--migrate") MIGRATE = true;`, `else if (a === "--update") UPDATE = true;`, `else if (a === "--prune-old-kit") PRUNE_OLD_KIT = true;` BEFORE the unknown-arg `process.exit(2)` branch so the three are recognized and any other unknown arg still exits 2.

    Add `const isoStamp = (): string => new Date().toISOString().replace(/:/g, "-");` near the other small helpers (after `sameContent`, ~line 436). Add a recursive directory content comparison helper `dirsSameContent(a, b)` (compares the sorted relative file set and each file's bytes via `sameContent`; returns false on any read error — fail-safe-to-differs so a true no-op is only declared when provably identical). Add `function backupIfDiffers(target: string, replacement: string, label: string): boolean` that: returns false (no backup) if `target` does not exist; if `target` and `replacement` are byte-identical (file: `sameContent`; dir: `dirsSameContent`) report `skipped` `(identical — no backup, D-09)` and return false; under DRY_RUN report `would-backup` `${label} → ${target}.bak.<ISO>` and return true; else `renameSync(target, \`${target}.bak.${isoStamp()}\`)` (filesystem-safe via isoStamp), report `backed-up`, return true.

    Keep every report/warning string in CLEAR PROFESSIONAL VOICE (safety surface — no caveman). Do NOT wire the flags into any branch yet (Plans 02/03 do that); this task only makes them recognized and adds the helpers.

    In install.test.ts: extend the existing `unknown-arg` case (or add a sibling `it`) titled to match the validation map: `unknown-arg` — assert `--migrate` is recognized (a `--migrate --check`-style or DRY_RUN invocation returns a status that is NOT 2) and `--bad-arg-xyz` still returns 2. Add a source-presence assertion reading `install/install.js` and expecting it to contain `backupIfDiffers` and `isoStamp`.

    After editing: run `npm run build` (tsc) then `npm run freshness` then `npx vitest run install`. Commit the regenerated `install/install.js` together with the `.ts`.
  </action>
  <acceptance_criteria>
    - `npx vitest run install -t "unknown-arg"` passes (3 new flags recognized; `--bad-arg-xyz` exits 2).
    - `grep -v '^//' install/install.ts | grep -c 'function backupIfDiffers'` returns 1 (helper present, comment-stripped).
    - `grep -v '^//' install/install.ts | grep -c 'const isoStamp'` returns 1.
    - `install/install.js` contains the string `backupIfDiffers` (committed compiled output is fresh).
    - `npm run build && npm run freshness` exits 0 (no stale .js).
    - No fenced code in report strings; all new strings are clear professional voice.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run install -t "unknown-arg"</automated>
  </verify>
  <done>The 3 flags are recognized, others still exit 2; isoStamp() and backupIfDiffers() exist in install.ts and the compiled install.js; freshness gate green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parameterize copyKit(retainBackup) + add makeOldLayoutFixture() harness helper (RED-by-design)</name>
  <files>install/install.ts, install/install.test.ts</files>
  <read_first>
    - install/install.ts — `copyKit()` (558-575: the atomic tmp→rename-aside→swap→rmSync(old) sequence); the call site `copyKit();` (770); `KIT_ROOT`/`GRUGOPS_HOME`/`GRUGOPS_SRC` (91-109); `cpSync`/`renameSync`/`rmSync` usage in copyKit
    - install/install.test.ts — `makeFixture()` (66-77); `snapshot()` (83-104); `runInstall()`/`runUninstall()` (108-123); `afterEach` cleanup (59-64); the two-root kit-copy case (220-225) and the materialize case (228-239) as shape references
    - install/install.ts — `materializeAdapter()` (581-628) and `readAdapterKit()` (198-220) so the fixture's repo-relative adapters match the real shape (no `grugops:materialized-kit` block)
    - .planning/phases/17-install-migrate-update/17-RESEARCH.md — Old-Layout Forensics (v1.0 symlink-default adapters, repo-relative refs, in-repo agent-factory/config/factory.config.json, no marker)
  </read_first>
  <behavior>
    - copyKit(retainBackup=false) behaves EXACTLY as today (delete-after-rename) — the existing idempotency case (138-158) and two-root kit-copy case (220-225) MUST stay green.
    - copyKit(retainBackup=true): when the displaced kit differs from the freshly staged tmp, the displaced kit is renamed to `${KIT_ROOT}.bak.<ISO>` (kept) instead of rmSync'd; when byte-identical → no backup created (D-09); DRY_RUN still mutates nothing.
    - makeOldLayoutFixture() returns a fixture dir with: in-repo `agent-factory/roles/orchestrator.md`, an old config at BOTH plausible legacy locations exercised by Plan 02 (this helper plants the in-repo `agent-factory/config/factory.config.json` AND supports a repo-root `factory.config.json` variant), `.claude/agents/grugops-orchestrator.md` + `.claude/skills/grugops/SKILL.md` as repo-relative adapters with NO `grugops:materialized-kit` block, and NO `.grugops/install.json` marker. A `symlink: true` option makes the orchestrator adapter a symlink pointing at a planted "source clone" file (for the Plan 02 LANDMINE case).
  </behavior>
  <action>
    Change `copyKit()` to `copyKit(retainBackup = false)`. Inside, keep the atomic sequence verbatim up to the displaced-kit handling. Replace the final unconditional `rmSync(old, …)` with: if `retainBackup` AND the displaced `old` content differs from the freshly staged kit (compare via `dirsSameContent(old, KIT_ROOT)` AFTER the swap, since `KIT_ROOT` is now the new kit), rename `old` → `${KIT_ROOT}.bak.${isoStamp()}` and `report("backed-up", …)`; else `rmSync(old, { recursive: true, force: true })` as today. Preserve true atomicity (no window where KIT_ROOT is absent) and the existing DRY_RUN short-circuit. Update the existing call site `copyKit();` (770) to `copyKit(false);` (explicit default; Plans 02/03 pass the mode they need). Do NOT change behavior for the default path.

    In install.test.ts: add `function makeOldLayoutFixture(opts?: { symlink?: boolean; rootConfig?: boolean }): string` beside `makeFixture` (~line 77). It mkTmp()s a dir, writes in-repo `agent-factory/roles/orchestrator.md` (so detection's `hasInRepoKit` is true) and `agent-factory/config/factory.config.json` with a recognizable edited token; if `opts.rootConfig` also writes a repo-root `factory.config.json` edited token; writes `.claude/skills/grugops/SKILL.md` and `.claude/agents/grugops-orchestrator.md` as plain files referencing `agent-factory/roles/...` repo-relative with NO MAT_OPEN block; writes NO `.grugops/install.json`. If `opts.symlink`, plant a `source-clone/orchestrator-src.md` file with a SENTINEL and replace the orchestrator adapter with a symlink (via `spawnSync("ln", ["-s", ...])` mirroring the existing symlink test idiom at 338/345) pointing at it; return the dir. Add a RED-by-design `it` `migrate: old-layout fixture is shaped correctly` asserting the fixture has the in-repo kit, the repo-relative adapter has no `grugops:materialized-kit` block, and no marker exists — this proves the helper before Plan 02 consumes it.

    After editing: `npm run build && npm run freshness && npx vitest run install`. Commit the regenerated `install.js` with the `.ts`.
  </action>
  <acceptance_criteria>
    - `npx vitest run install -t "idempotent"` still passes (copyKit(false) unchanged — regression guard).
    - `npx vitest run install -t "two-root: the shared kit is copied"` still passes.
    - `npx vitest run install -t "old-layout fixture is shaped correctly"` passes (helper proven before consumption).
    - `grep -v '^//' install/install.ts | grep -c 'function copyKit(retainBackup'` returns 1.
    - `grep -v '^//' install/install.test.ts | grep -c 'function makeOldLayoutFixture'` returns 1.
    - `npm run build && npm run freshness` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run freshness && npx vitest run install</automated>
  </verify>
  <done>copyKit takes a retainBackup flag (default path unchanged, all baseline cases green); makeOldLayoutFixture() exists and is proven by its own case; freshness gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invoking shell → installer process | flag values from argv (untrusted strings) enter the arg-parse loop |
| installer → host filesystem | helper functions rename/remove paths on a user machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-AP | Tampering | arg-parse loop accepting the 3 new flags | mitigate | The 3 flags are exact-string matches; any other unknown arg still hits `process.exit(2)` (regression-tested by the `unknown-arg` case). No flag takes an unvalidated path value in this plan. |
| T-17-01-BK | Tampering | backupIfDiffers / copyKit(retainBackup) rename targets | mitigate | Backup targets are derived only from `KIT_ROOT` / a caller-passed path already resolved absolute (install.ts:99-109); the timestamped suffix is grugops-specific (`.bak.<ISO>`). No deletion on this plan's default path (never-delete-first). |
| T-17-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH "Package Legitimacy Audit: not applicable; zero external packages." No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages. No blocking checkpoint needed. |
| T-17-01-FR | Tampering | committed install.js drift (stale build) | mitigate | `npm run build && npm run freshness` baked into both tasks' acceptance criteria; the harness drives the committed `.js`, so a stale `.js` is caught before commit (Pitfall 3). |
</threat_model>

<verification>
- `npm run build && npm run freshness` exits 0 (committed install.js is a faithful build of install.ts).
- `npx vitest run install` — full install suite green (baseline 21+1 cases plus the new flag-recognition + fixture-shape cases).
- `grep -v '^//' install/install.ts | grep -c 'function backupIfDiffers'` == 1 and `... 'function copyKit(retainBackup'` == 1.
</verification>

<success_criteria>
- The 3 new flags are recognized; an unknown arg still exits 2.
- `isoStamp()`, `backupIfDiffers()`, and `copyKit(retainBackup)` exist, are single-source, and honor DRY_RUN.
- `makeOldLayoutFixture()` exists, supports the symlink + rootConfig variants, and is proven by its own RED-by-design case.
- The committed `install/install.js` is fresh; the default install path is behaviorally unchanged (baseline cases green).
- D-13 honored: the modes are flags on the single TS installer (no install.sh); the committed install.js stays fresh per the freshness gate.
</success_criteria>

<output>
Create `.planning/phases/17-install-migrate-update/17-01-SUMMARY.md` when done.
</output>

---
phase: 14-security-audit-owasp-asvs-checklist-re-anchor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/asvs/asvs-5.0.0.flat.json
  - scripts/generate-asvs-checklist.mjs
  - agent-factory/checklists/security-nfr-checklist.md
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - "Running `node scripts/generate-asvs-checklist.mjs` emits the full 345-requirement ASVS 5.0 checklist in place — produced from the vendored pinned source plus the committed generator (both in-tree), so the set is provably not hand-transcribed (D-01)"
    - "Re-running the generator produces a byte-identical checklist (reproducible, no drift)"
    - "The checklist carries a provenance header naming ASVS 5.0.0 + the pinned tag + the pinned commit SHA"
    - "Every checklist row carries a req_id (Vx.y.z) and an L1/L2/L3 level tag, organized by ASVS chapter (V1–V17)"
    - "The generator fails closed (nonzero exit, no partial write) on a missing/garbled source or a row count != 345"
  artifacts:
    - path: "scripts/asvs/asvs-5.0.0.flat.json"
      provides: "Vendored OWASP ASVS 5.0.0 source data, pinned at commit SHA 5cf9b032440be53ce345ab3c130fda46ba1ce7a2"
      contains: "\"req_id\""
    - path: "scripts/generate-asvs-checklist.mjs"
      provides: "Stdlib-Node ESM generator: JSON.parse → group by chapter → emit markdown + provenance header"
      contains: "5cf9b032440be53ce345ab3c130fda46ba1ce7a2"
    - path: "agent-factory/checklists/security-nfr-checklist.md"
      provides: "The generated full L1/L2/L3 ASVS 5.0 checklist (in place, D-04)"
      contains: "GENERATED — do not hand-edit"
  key_links:
    - from: "scripts/generate-asvs-checklist.mjs"
      to: "scripts/asvs/asvs-5.0.0.flat.json"
      via: "readFileSync + JSON.parse of the FIXED source path"
      pattern: "JSON.parse"
    - from: "scripts/generate-asvs-checklist.mjs"
      to: "agent-factory/checklists/security-nfr-checklist.md"
      via: "writeFileSync to the FIXED output path (in place, D-04)"
      pattern: "writeFileSync"
---

<objective>
Stand up the SEC-02 generation foundation: vendor the pinned OWASP ASVS 5.0.0 source data in-repo, write a zero-dep stdlib-Node generator that transforms it into the security/NFR checklist, and regenerate `security-nfr-checklist.md` in place as the full 345-requirement L1/L2/L3 ASVS 5.0 set. This makes "not hand-transcribed" provable: source + generator both live in the tree, the checklist is reproducible byte-for-byte, and a provenance header records the exact pin.

Purpose: The re-anchored checklist is the single source of truth every other Phase-14 surface consumes — workflow 15 walks it, the role filters it by `security.asvs_level`, the gate reads severities derived from its levels. This plan must land first (Wave 0 / Wave 1) so the checklist exists before the guard scans it (Plan 02) and before the role/handoff reference its level-filter semantics (Plan 02).
Output: `scripts/asvs/asvs-5.0.0.flat.json` (vendored), `scripts/generate-asvs-checklist.mjs` (generator), `agent-factory/checklists/security-nfr-checklist.md` (regenerated in place).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-RESEARCH.md
@.planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-PATTERNS.md
@.planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the pinned ASVS 5.0.0 flat.json source</name>
  <files>scripts/asvs/asvs-5.0.0.flat.json</files>
  <read_first>
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-RESEARCH.md (Standard Stack: the exact repo/tag/SHA/path + "Installation" curl command lines 98–104; Code Examples: the verified 7-key schema lines 334–344 and chapter list lines 346–358)
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-PATTERNS.md (the `scripts/asvs/asvs-5.0.0.flat.json` section — vendor-verbatim, do-NOT-vendor-legacy/latest constraints)
  </read_first>
  <action>
    Vendor the official OWASP ASVS 5.0.0 machine-readable source. Fetch the blob `OWASP_Application_Security_Verification_Standard_5.0.0_en.flat.json` from `OWASP/ASVS` at the IMMUTABLE pinned commit SHA `5cf9b032440be53ce345ab3c130fda46ba1ce7a2` (tag `v5.0.0_release`, published 2025-05-30), repo path `5.0/docs_en/`. Use the verified raw URL: `https://raw.githubusercontent.com/OWASP/ASVS/5cf9b032440be53ce345ab3c130fda46ba1ce7a2/5.0/docs_en/OWASP_Application_Security_Verification_Standard_5.0.0_en.flat.json` and write it to `scripts/asvs/asvs-5.0.0.flat.json`. Do NOT vendor the `latest` tag (it tracks master, moving), do NOT vendor the `.legacy.json` form (it back-maps to 4.0 numbering). After fetching, confirm the top-level shape is `{ "requirements": [ ... ] }` with each item carrying exactly the 7 keys `chapter_id, chapter_name, section_id, section_name, req_id, req_description, L` and that the array length is 345. If the fetch fails or the count is not 345, stop and report `UNKNOWN - verify` — do not hand-author or truncate the data (no-fabrication on a security source).
  </action>
  <verify>
    <automated>test "$(node -e 'const d=require("./scripts/asvs/asvs-5.0.0.flat.json");console.log(d.requirements.length)')" = "345"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/asvs/asvs-5.0.0.flat.json` exists and parses as JSON with `requirements.length === 345`.
    - A spot-checked row has all 7 keys; `L` values are strings in {"1","2","3"}: `node -e 'const r=require("./scripts/asvs/asvs-5.0.0.flat.json").requirements; const ok=r.every(x=>["1","2","3"].includes(x.L)&&x.req_id&&x.chapter_id); process.exit(ok?0:1)'` exits 0.
    - The 17 distinct chapter_ids V1–V17 are present: `node -e 'const r=require("./scripts/asvs/asvs-5.0.0.flat.json").requirements; const c=new Set(r.map(x=>x.chapter_id)); process.exit(c.size===17?0:1)'` exits 0.
  </acceptance_criteria>
  <done>The pinned ASVS 5.0.0 flat.json is vendored in-repo at `scripts/asvs/asvs-5.0.0.flat.json` with exactly 345 requirements, 17 chapters, valid level tags.</done>
</task>

<task type="auto">
  <name>Task 2: Write the stdlib-Node generator + regenerate the checklist in place</name>
  <files>scripts/generate-asvs-checklist.mjs, agent-factory/checklists/security-nfr-checklist.md</files>
  <read_first>
    - scripts/validate-agent-factory.mjs (lines 1–75 — the house-style header doc-comment, the stdlib-ONLY/zero-dep/no-package.json contract, the fail-closed try/catch + fixed-literal-path invariant the generator mirrors INVERTED for the write half)
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-RESEARCH.md (Architecture Pattern 2 lines 222–257 — the exact load+group skeleton + the provenance-header form; Pattern 3 lines 259–261 — cumulative level math to state honestly; Code Examples lines 329–365 — schema + chapter names + level distribution 70/183/92)
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-PATTERNS.md (the `scripts/generate-asvs-checklist.mjs` section — fail-closed parse, error-collect+exit, provenance header, row-count assert; the `security-nfr-checklist.md` section — drop-in-compatibility constraints: same path, `kind: checklist` frontmatter, NFR pointer moves to the role)
    - agent-factory/checklists/security-nfr-checklist.md (the current 10-bullet "before" this regenerates in place — note `kind: checklist` frontmatter at top)
  </read_first>
  <action>
    Write `scripts/generate-asvs-checklist.mjs` as a zero-dep ESM stdlib-Node 18+ script (no TypeScript per D-03, no npm deps, no package.json) mirroring `validate-agent-factory.mjs`'s style: a house-style header doc-comment stating the stdlib-only/zero-dep/fail-closed contract plus the source pin (TAG `v5.0.0_release`, SHA `5cf9b032440be53ce345ab3c130fda46ba1ce7a2`). Import `readFileSync, writeFileSync` from `node:fs`, `join, dirname` from `node:path`, `fileURLToPath` from `node:url`. Resolve ROOT as `join(dirname(fileURLToPath(import.meta.url)), "..")`. Define SRC and OUT as FIXED literal paths joined to ROOT — `SRC = scripts/asvs/asvs-5.0.0.flat.json`, `OUT = agent-factory/checklists/security-nfr-checklist.md` — NEVER derived from argv/env/file-content (read/write-only-by-construction; RESEARCH Security V5/V13). Wrap `JSON.parse(readFileSync(SRC,"utf8"))` in try/catch; on parse failure, a null/non-object result, or a `requirements` array whose length is not exactly 345, `console.error` a finding and `process.exit(1)` — NEVER write a partial checklist (fail-closed; RESEARCH "Silent partial generation"). Group rows into a Map keyed by `chapter_id`, then numeric-sort chapters by `Number(id.slice(1))` so output is V1..V17 order. Emit the output document as: (1) `kind: checklist` YAML frontmatter (preserve the existing frontmatter shape so the validator existence-check + `check-kit-refs.sh` stay green — keep `kind: checklist`; the `tier: lean` line may be dropped since the file now ships the full set); (2) the provenance HTML comment header per D-02 — the literal string must contain `GENERATED — do not hand-edit`, the re-run command `node scripts/generate-asvs-checklist.mjs`, `OWASP ASVS 5.0.0`, the tag `v5.0.0_release`, and the SHA `5cf9b032440be53ce345ab3c130fda46ba1ce7a2`; (3) a short clear-voice intro stating the cumulative level semantics honestly (L1=70, L2=253 cumulative, L3=345 cumulative — keep `L ≤ asvs_level`) in PROFESSIONAL voice (this file is scanned by guard_voice in Plan 02 — no caveman idioms); (4) per-chapter `## V<n> <chapter_name>` headings; (5) one row per requirement formatted `- [<req_id>] [L<L>] <req_description>` (exact layout is discretion, but each row MUST carry the req_id, the level tag, and the verbatim `req_description` — copy `req_description` VERBATIM, never invent or paraphrase, no-fabrication on a security surface). Do NOT carry the old "performance impact vs NFR catalog" / `plans/nfr-catalog.md` pointer into the regenerated pure-ASVS checklist — that NFR/perf pointer stays in the role (`security-nfr.md` already references `nfr-catalog.md` separately; confirmed in Plan 02). On success, print a one-line confirmation (row count written) and `process.exit(0)`. Then RUN the generator to produce `agent-factory/checklists/security-nfr-checklist.md` in place and commit the generated file. After generating, grep the output for any VOICE_MARKERS token (`\bgrug\b|\bclub\b|\brock\b|\bcave\b|\bsmash\b|\bshiny\b|brain hurt|me think|no think|big think`) — ASVS descriptions are professional English but `rock-solid`/`club` could in principle appear (hyphen is a word boundary; RESEARCH Pitfall 2); if any legitimate ASVS word trips it, FLAG it for Plan 02's guard refinement rather than editing the verbatim description.
  </action>
  <verify>
    <automated>node scripts/generate-asvs-checklist.mjs && test "$(grep -c '^- \[V' agent-factory/checklists/security-nfr-checklist.md)" = "345" && grep -q 'GENERATED — do not hand-edit' agent-factory/checklists/security-nfr-checklist.md && grep -q '5cf9b032440be53ce345ab3c130fda46ba1ce7a2' agent-factory/checklists/security-nfr-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/generate-asvs-checklist.mjs` exits 0 and writes `agent-factory/checklists/security-nfr-checklist.md`.
    - The checklist has exactly 345 requirement rows: `grep -c '^- \[V' agent-factory/checklists/security-nfr-checklist.md` ⇒ 345 (V-1).
    - Provenance header present: `grep -q 'GENERATED — do not hand-edit' agent-factory/checklists/security-nfr-checklist.md` exits 0 (V-3).
    - Pinned SHA present in provenance: `grep -q '5cf9b032440be53ce345ab3c130fda46ba1ce7a2' agent-factory/checklists/security-nfr-checklist.md` exits 0 (V-4).
    - Reproducible (V-2): regenerate to a temp copy and `cmp -s`: `cp agent-factory/checklists/security-nfr-checklist.md /tmp/sec-before.md && node scripts/generate-asvs-checklist.mjs && cmp -s /tmp/sec-before.md agent-factory/checklists/security-nfr-checklist.md` exits 0 (no drift).
    - Fail-closed proof: temporarily point the generator at a truncated/garbled source (or corrupt a temp copy) and confirm nonzero exit with no partial write — the row-count assert (expect 345) is present in the source (`grep -q '345' scripts/generate-asvs-checklist.mjs`).
    - `kind: checklist` frontmatter preserved (drop-in compatibility): `grep -q 'kind: checklist' agent-factory/checklists/security-nfr-checklist.md` exits 0.
    - Existing references survive: `grep -rn 'security-nfr-checklist.md' agent-factory/roles/security-nfr.md` still resolves the same path (no rename — D-04).
  </acceptance_criteria>
  <done>The generator is zero-dep stdlib-Node, fail-closed, fixed-literal-path; the checklist is regenerated in place as the full 345-item L1/L2/L3 ASVS 5.0 set with provenance header, reproducible byte-for-byte, drop-in compatible with all existing references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream OWASP repo → vendored blob | Third-party data crosses into the repo once, at vendoring time |
| vendored blob → generator → checklist | Build-time text transform; trusted, pinned input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | Vendored ASVS source (supply-chain) | mitigate | Pin the exact commit SHA `5cf9b032…` in the provenance header (D-02); the SHA is the integrity anchor. Never vendor the moving `latest` tag; never the `.legacy.json` back-mapped form. Row-count assert (expect 345) fails closed on a truncated/tampered source. |
| T-14-02 | Tampering | Generator output path | mitigate | SRC + OUT are FIXED literal paths joined to repo root, never derived from argv/env/JSON content (mirrors `validate-agent-factory.mjs` read/write-only-by-construction). No path traversal / arbitrary write. |
| T-14-03 | Repudiation / Tampering | Generated checklist | mitigate | Fail-closed: try/catch around parse + reject null/non-object + row-count sanity assert; a partial/garbled checklist errors (`process.exit(1)`), never ships. The reproducibility `cmp -s` check (V-2) catches silent drift. |
| T-14-04 | Repudiation | Checklist content | mitigate | No-fabrication: `req_description` copied VERBATIM from the pinned source; the generator never invents text. "Not hand-transcribed" is proven by source + generator both living in-repo. |
| T-14-SC | Tampering | npm/pip/cargo installs | n/a | This phase installs ZERO packages (REQUIREMENTS.md out-of-scope: grugops is markdown + stdlib-only scripts). The only external artifact is a vendored DATA file, not a code dependency — Package Legitimacy Gate is moot. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages exist. |
</threat_model>

<verification>
Per task: `sh scripts/check-foundation-guards.sh` must stay GREEN (the build gate).
Plan-level: `node scripts/generate-asvs-checklist.mjs` exits 0; checklist has 345 rows (V-1); provenance header + SHA present (V-3/V-4); regenerate ⇒ `cmp -s` no diff (V-2). The voice-guard extension that scans this checklist lands in Plan 02 — ensure the generated body is clean clear-voice (grep the marker set after generating).
</verification>

<success_criteria>
- `scripts/asvs/asvs-5.0.0.flat.json` vendored at the pinned SHA, 345 requirements, 17 chapters (SEC-02).
- Checklist generated from a vendored pinned source + committed generator — both the source data and `generate-asvs-checklist.mjs` live in-tree, so "not hand-transcribed" is provably honored (SEC-02, D-01).
- `scripts/generate-asvs-checklist.mjs` is zero-dep stdlib-Node ESM, fail-closed, fixed-literal paths, row-count assert 345 (SEC-02, D-03).
- `agent-factory/checklists/security-nfr-checklist.md` regenerated in place, full L1/L2/L3 ASVS set, req IDs + level tags, provenance header (SEC-02, D-02, D-04, D-05).
- Reproducible: re-run yields byte-identical output (V-2).
- All existing references to the checklist path survive unchanged (D-04).
</success_criteria>

<output>
Create `.planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-01-SUMMARY.md` when done.
</output>

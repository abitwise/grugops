---
phase: 14-security-audit-owasp-asvs-checklist-re-anchor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/workflows/15-security-audit.md
  - agent-factory/roles/orchestrator.md
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "A `15-security-audit.md` workflow exists, anchored to OWASP ASVS 5.0, mirroring the workflow-14 section skeleton"
    - "Workflow 15 cites `05-pr-quality-gate.md` by filename for enforcement and never restates the gate loop (audit produces, gate enforces — D-07/D-08)"
    - "Workflow 15 never writes the string §14 and never self-blocks"
    - "The Orchestrator routes a new `security-audit` classification to `15-security-audit.md` without renumbering workflows 00–14"
  artifacts:
    - path: "agent-factory/workflows/15-security-audit.md"
      provides: "Standalone deep ASVS audit workflow run by the Security/NFR role (D-06)"
      contains: "05-pr-quality-gate.md"
    - path: "agent-factory/roles/orchestrator.md"
      provides: "security-audit classification token + workflow-map row registering 15-security-audit.md"
      contains: "15-security-audit.md"
  key_links:
    - from: "agent-factory/roles/orchestrator.md"
      to: "agent-factory/workflows/15-security-audit.md"
      via: "classification→workflow-file map row"
      pattern: "security-audit.*15-security-audit.md"
    - from: "agent-factory/workflows/15-security-audit.md"
      to: "agent-factory/workflows/05-pr-quality-gate.md"
      via: "reference-don't-restate citation by filename"
      pattern: "05-pr-quality-gate.md"
---

<objective>
Author the SEC-01 deliverables: a standalone deep ASVS-anchored security-audit workflow (`15-security-audit.md`) mirroring the workflow-14 skeleton, and register a new `security-audit` classification routing to it in the Orchestrator — without renumbering the frozen workflows 00–14. The workflow produces severity-tagged findings and references `05-pr-quality-gate.md` by filename for enforcement; it never restates the gate loop and never self-blocks (audit produces, gate enforces — D-07/D-08).

Purpose: Workflow 15 is the deep, on-demand/per-phase/per-milestone audit run by the Security/NFR role — distinct from the lightweight per-ticket Security/NFR check (D-06). It walks the re-anchored checklist at the configured `security.asvs_level` and emits the formal severity-tagged audit that feeds `block_on` at the single visible gate. This plan is independent of the checklist's content (it references the checklist by path only), so it runs in Wave 1 parallel to Plan 01.
Output: `agent-factory/workflows/15-security-audit.md` (new), `agent-factory/roles/orchestrator.md` (registration edits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-RESEARCH.md
@.planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-PATTERNS.md
@agent-factory/workflows/14-ui-design-to-build.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author workflow 15 (deep ASVS audit, reference-don't-restate)</name>
  <files>agent-factory/workflows/15-security-audit.md</files>
  <read_first>
    - agent-factory/workflows/14-ui-design-to-build.md (the exact section skeleton + the verbatim reference-don't-restate phrasing lines 26/30 + the role-switch-protocol pointer line 16 + the autonomy/done-condition line 42 + the branch-guard Commit line 44–45)
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-PATTERNS.md (the `15-security-audit.md` section — frontmatter `order: 15`, the section set to emit, the Inputs/Steps/Board-moves requirements, the branch slug `grugops/security-audit-<id>`)
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-RESEARCH.md (Pattern 1 lines 193–217 — skeleton + the validator-does-not-cover-wf15 note; Pattern 3/4 — cumulative level filter + severity map; level distribution 70/183/92 lines 360–365)
    - agent-factory/roles/security-nfr.md (the role that runs this workflow — its result enum PASS|PASS_WITH_RISKS|BLOCKED, its handoff output path `plans/handoffs/<TICKET-ID>-security-nfr.md`, its checklist + nfr-catalog references)
  </read_first>
  <action>
    Create `agent-factory/workflows/15-security-audit.md` mirroring the `14-ui-design-to-build.md` skeleton. Frontmatter: `kind: workflow`, `order: 15`, `cadence: both`. Title `# Workflow: Security audit (OWASP ASVS)`. Do NOT renumber 00–14 (carried-forward lock); append 15 only. Emit these sections in the workflow-14 order: When to use, Agents involved, Inputs required, Steps, Board moves, Handoffs produced, Trace updates, Done condition, Commit. OMIT a Metrics emitted section (mirror wf14; RESEARCH Open Q3). Content requirements:
    - When to use: a deep, standalone ASVS audit run on-demand / per-phase / per-milestone — distinct from the lightweight per-ticket Security/NFR check that already runs in the In Security/NFR column (D-06). Anchored to OWASP ASVS 5.0.
    - Agents involved: single agent = Security/NFR (deep audit); writes `plans/handoffs/<TICKET-ID>-security-nfr.md`. Copy the role-switch-protocol pointer line VERBATIM from wf14 line 16 (Roles activate via the role-switch protocol agent-factory/roles/_role-switch-protocol.md: one window, drop prior context, the handoff is the only memory).
    - Inputs required: name `security.asvs_level` and `security.block_on` from `.grugops/factory.config.json`; the checklist `agent-factory/checklists/security-nfr-checklist.md`; and the change/scope under review (mirror wf14 lines 18–23 which name a checklist + the autonomy setting).
    - Steps: (a) walk the full checklist filtered to `security.asvs_level` at READ time — cumulative, keep requirements where L is less than or equal to the configured level (L1=70, L2=253, L3=345); state this filter honestly. (b) Emit severity-tagged findings using the D-09 default map: L1 fail to high, L2 fail to medium, L3 fail to low (a missing baseline control is most dangerous). (c) The auditor MAY override a finding's severity with a stated reason + a named owner (reuse the role's existing accepted-risk-needs-a-named-owner hard limit). (d) Every pass cites evidence or reads `UNKNOWN - verify` — no unbacked ticks (no-fabrication). (e) Reference `agent-factory/workflows/05-pr-quality-gate.md` BY FILENAME for enforcement, using wf14's verbatim phrasing pattern ("… live there — this workflow references that gate and does not restate it"). The workflow NEVER self-blocks — audit produces, the gate enforces via `security.block_on` (D-07). NEVER write the string §14 (carried-forward lock) — cite the gate by filename only.
    - Board moves: In Security/NFR column; note it is the deep audit, distinct from the per-ticket check (D-06).
    - Handoffs produced: `<TICKET-ID>-security-nfr.md` filled from `agent-factory/handoffs/security-nfr-handoff.md`.
    - Trace updates: append the audit result + reviewed ASVS req IDs against the row in `plans/traceability.md`.
    - Done condition: honor `autonomy=pr`; the agent opens a branch and a PR, never merges; humans hold merge and deploy (copy wf14 line 42 shape).
    - Commit: branch guard first (never a protected branch; switch to `grugops/security-audit-<id>`), then type(scope): summary, per `agent-factory/_commit-convention.md`; never merge, never deploy (copy wf14 line 44–45 shape).
    Write the whole file in CLEAR professional voice — this surface is added to `guard_voice` in Plan 03, so no caveman idioms anywhere (the workflow has no Caveman prompt fence).
  </action>
  <verify>
    <automated>test -f agent-factory/workflows/15-security-audit.md && grep -q '05-pr-quality-gate.md' agent-factory/workflows/15-security-audit.md && grep -q 'order: 15' agent-factory/workflows/15-security-audit.md && ! grep -q '§14' agent-factory/workflows/15-security-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `order: 15` frontmatter and title `# Workflow: Security audit (OWASP ASVS)`: `grep -q '^order: 15' agent-factory/workflows/15-security-audit.md` and `grep -q 'Security audit (OWASP ASVS)' agent-factory/workflows/15-security-audit.md`.
    - Cites the gate by filename (V-8): `grep -q '05-pr-quality-gate.md' agent-factory/workflows/15-security-audit.md` exits 0.
    - Never writes §14: `grep -q '§14' agent-factory/workflows/15-security-audit.md` exits NONZERO (absent).
    - All 9 workflow-14 sections present: `for s in 'When to use' 'Agents involved' 'Inputs required' 'Steps' 'Board moves' 'Handoffs produced' 'Trace updates' 'Done condition' 'Commit'; do grep -q "## $s" agent-factory/workflows/15-security-audit.md || exit 1; done` exits 0.
    - References the config dial keys: `grep -q 'security.asvs_level' agent-factory/workflows/15-security-audit.md && grep -q 'security.block_on' agent-factory/workflows/15-security-audit.md`.
    - No VOICE_MARKERS token present: `grep -qE '\bgrug\b|\bclub\b|\brock\b|\bcave\b|\bsmash\b|\bshiny\b|brain hurt|me think|no think|big think' agent-factory/workflows/15-security-audit.md` exits NONZERO (clean clear-voice — required before Plan 03's guard scans it).
  </acceptance_criteria>
  <done>Workflow 15 exists on the wf14 skeleton, anchored to ASVS 5.0, cites the gate by filename, never restates the gate loop, never self-blocks, never writes §14, in clean clear-voice.</done>
</task>

<task type="auto">
  <name>Task 2: Register security-audit → workflow 15 in the Orchestrator</name>
  <files>agent-factory/roles/orchestrator.md</files>
  <read_first>
    - agent-factory/roles/orchestrator.md (lines 40–43 — the classification list; lines 51–67 — the routing matrix incl. the existing `Need risk/security/compliance` line 59; lines 91–111 — the classification→workflow-file map table; the note line 91 that the map must stay consistent with README)
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-PATTERNS.md (the `orchestrator.md` section — the TWO insertion points, the append-one-row pattern, the byte-budget note 7570/7165 ceiling vs 6759 B current)
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-CONTEXT.md (the carried-forward lock about README registration; the phase-anchor note that the README has NO workflow/command map — verify before editing README)
  </read_first>
  <action>
    Register the new `security-audit` classification in TWO sites inside `agent-factory/roles/orchestrator.md` (append only — do NOT renumber 00–14):
    1. The classification list (lines 41–43): append the `security-audit` token to the end of the pipe-delimited list (after `ui-build`), matching the existing backtick + pipe formatting.
    2. The classification→workflow-file map table (lines 93–109): append one row `| security-audit | 15-security-audit.md |` (with backticks around the filename matching the existing rows) immediately after the `ui-build | 14-ui-design-to-build.md` row.
    The routing matrix (lines 51–67) already carries `Need risk/security/compliance -> Security/NFR (and Compliance Officer if regime set)` at line 59 — the new `security-audit` classification routes to that same Security/NFR role, so the routing target is unchanged. Optionally add a terse clarifying note that `security-audit` is the deep audit (workflow 15) distinct from the per-ticket Security/NFR check (D-06) — only if it fits the byte budget. Verify with `wc -c` that orchestrator.md stays under its `role_ceiling()` WARN (7165) and FAIL (7570); a two-token addition to 6759 B leaves ~400 B headroom, so no ceiling bump is needed.
    README registration: the carried-forward lock asked to register in `agent-factory/README.md`'s command/workflow map, but RESEARCH found the README has copy-paste prompts (lines 84–115), NOT a classification→workflow map (no `ui-build`/`14-ui-design` row exists there — verified: wf14 was never added to README). Do NOT invent a map. Grep the README to re-confirm no workflow-map table exists; if confirmed absent, record `UNKNOWN - verify` in the SUMMARY (README has no map to update — consistent with the wf14 precedent) rather than fabricating one. If a classification→workflow map IS found, add the `security-audit | 15-security-audit.md` row to keep it consistent with the orchestrator (line 91 contract).
  </action>
  <verify>
    <automated>grep -q 'security-audit' agent-factory/roles/orchestrator.md && grep -q '15-security-audit.md' agent-factory/roles/orchestrator.md && test "$(wc -c < agent-factory/roles/orchestrator.md)" -lt 7165</automated>
  </verify>
  <acceptance_criteria>
    - `security-audit` classification + workflow-map row registered (V-9): `grep -q 'security-audit' agent-factory/roles/orchestrator.md && grep -q '15-security-audit.md' agent-factory/roles/orchestrator.md` exits 0.
    - The map row exists as a table row pairing the classification to the workflow file: `grep -qE 'security-audit.*15-security-audit\.md' agent-factory/roles/orchestrator.md` exits 0.
    - No renumbering of 00–14: the `ui-build | 14-ui-design-to-build.md` row and all 00–13 rows are unchanged: `grep -q '14-ui-design-to-build.md' agent-factory/roles/orchestrator.md && grep -q '13-incident.md' agent-factory/roles/orchestrator.md` exits 0.
    - Under byte ceiling (no bump needed): `test "$(wc -c < agent-factory/roles/orchestrator.md)" -lt 7165` exits 0 (WARN ceiling).
    - Build gate green after edit: `sh scripts/check-foundation-guards.sh` exits 0 (orchestrator stays clear-voice + under ceiling).
  </acceptance_criteria>
  <done>The Orchestrator carries a `security-audit` classification token and a workflow-map row routing it to `15-security-audit.md`, append-only with no renumber, under its byte ceiling; README status recorded honestly (UNKNOWN - verify if no map exists).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator routing → workflow 15 | Classification decides which audit runs; misrouting is a process risk, not a data-tampering risk |
| workflow 15 → the gate | Audit produces severity-tagged findings; the gate (separate file) enforces — the boundary keeps enforcement single-source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Elevation of Privilege | Workflow 15 self-blocking | mitigate | D-07: workflow 15 NEVER self-blocks — it only produces severity-tagged findings; only `05-pr-quality-gate.md` reads `security.block_on` and blocks. Enforcement stays single-source at the visible gate. Verified by absence of any block/fail terminal in wf15 (it references the gate, does not restate it). |
| T-14-06 | Repudiation | Audit findings | mitigate | Every "pass" cites evidence or reads `UNKNOWN - verify` — no unbacked ticks. The severity-override escape hatch requires a stated reason + a named owner (no anonymous acceptance). |
| T-14-07 | Tampering | Workflow numbering / gate reference | mitigate | Cite `05-pr-quality-gate.md` by filename, never the brittle "§14" string (carried-forward lock); append `order: 15` only, never renumber 00–14 (a renumber would silently re-point every orchestrator workflow-map reference). |
| T-14-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs ZERO packages — it authors markdown only. Package Legitimacy Gate is moot; no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages. |
</threat_model>

<verification>
Per task: `sh scripts/check-foundation-guards.sh` stays GREEN.
Plan-level: workflow 15 exists, cites `05-pr-quality-gate.md`, never writes §14 (V-8); orchestrator carries the `security-audit` classification + the `15-security-audit.md` map row, no renumber, under byte ceiling (V-9). Both surfaces must be clean clear-voice before Plan 03 adds them to `guard_voice`.
</verification>

<success_criteria>
- `agent-factory/workflows/15-security-audit.md` exists on the wf14 skeleton, anchored to ASVS 5.0, references the gate by filename, never self-blocks, never writes §14 (SEC-01, D-06/D-07/D-08).
- The Orchestrator routes `security-audit` to `15-security-audit.md` in both the classification list and the workflow-map table, append-only, no renumber of 00–14 (SEC-01).
- orchestrator.md stays under its byte ceiling (no bump needed).
- README registration handled honestly: the row is added if a map exists, else recorded `UNKNOWN - verify` (no fabricated map).
</success_criteria>

<output>
Create `.planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-02-SUMMARY.md` when done.
</output>

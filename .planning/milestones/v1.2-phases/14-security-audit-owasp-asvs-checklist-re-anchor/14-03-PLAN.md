---
phase: 14-security-audit-owasp-asvs-checklist-re-anchor
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - agent-factory/roles/security-nfr.md
  - agent-factory/handoffs/security-nfr-handoff.md
  - scripts/check-foundation-guards.sh
  - scripts/check-foundation-guards.test.sh
autonomous: true
requirements: [SEC-03]
must_haves:
  truths:
    - "The Security/NFR role reads the checklist at security.asvs_level (cumulative, read-time filter) and is NOT regenerated when the dial changes"
    - "The role carries the D-09 default severity map (L1 to high / L2 to medium / L3 to low) with the named-owner override attached to the existing accepted-risk hard limit"
    - "The handoff template carries severity + ASVS level + req-id fields for findings, plus a named-owner override field for accepted risks"
    - "guard_voice scans all four security surfaces in clear professional voice, preserving the Caveman-fence carve-out on the role only"
    - "The guard harness proves each of the three new surfaces fails RED on a planted voice regression, and the smoke run over the real tree stays GREEN"
  artifacts:
    - path: "agent-factory/roles/security-nfr.md"
      provides: "Read-time level-filter note + D-09 severity map + named-owner override; clear-voice body; under byte ceiling"
      contains: "asvs_level"
    - path: "agent-factory/handoffs/security-nfr-handoff.md"
      provides: "Severity/level/req-id finding fields + named-owner override field"
      contains: "severity"
    - path: "scripts/check-foundation-guards.sh"
      provides: "guard_voice scan set extended with the 3 new security surfaces; Caveman carve-out preserved"
      contains: "15-security-audit.md"
    - path: "scripts/check-foundation-guards.test.sh"
      provides: "GUARD_INPUTS extended + one RED fixture per new surface"
      contains: "security-nfr-checklist.md"
  key_links:
    - from: "agent-factory/roles/security-nfr.md"
      to: "security.asvs_level"
      via: "read-time cumulative filter note near the checklist reference"
      pattern: "asvs_level"
    - from: "scripts/check-foundation-guards.sh"
      to: "agent-factory/checklists/security-nfr-checklist.md"
      via: "VOICE_FILES scan-set union (SEC_VOICE_FILES)"
      pattern: "security-nfr-checklist.md"
    - from: "scripts/check-foundation-guards.test.sh"
      to: "scripts/check-foundation-guards.sh"
      via: "GUARD_INPUTS mirror + expect_fail RED fixtures"
      pattern: "expect_fail"
---

<objective>
Wire the SEC-03 behavior behind the already-shipped security dials and prove it mechanically. Add the read-time `security.asvs_level` filter note + the D-09 severity map (L1 to high / L2 to medium / L3 to low) + the named-owner override to the Security/NFR role (TIGHT byte budget); add severity/level/req-id finding fields + a named-owner override field to the handoff template; extend `guard_voice` to scan all four security surfaces in clear professional voice (preserving the Caveman-fence carve-out on the role only); and extend the fail-proof harness with a RED fixture per new surface plus the matching `GUARD_INPUTS` entries.

Purpose: The dials already ship byte-identical (Phase 10) — this plan wires their behavior without adding/renaming keys. The voice-guard extension is the no-fabrication control that makes "clear professional voice on security surfaces" mechanically checkable (a silently-passing regression IS the threat). This plan depends on Plan 01 (the regenerated checklist) and Plan 02 (workflow 15) because `guard_voice` scans both and the harness mirrors both into its hermetic tree — both must exist and be clean clear-voice first.
Output: `agent-factory/roles/security-nfr.md`, `agent-factory/handoffs/security-nfr-handoff.md`, `scripts/check-foundation-guards.sh`, `scripts/check-foundation-guards.test.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-RESEARCH.md
@.planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-PATTERNS.md
@.planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the dial behavior into the role + handoff (D-05/D-09, tight byte budget)</name>
  <files>agent-factory/roles/security-nfr.md, agent-factory/handoffs/security-nfr-handoff.md</files>
  <read_first>
    - agent-factory/roles/security-nfr.md (line 22 — the checklist + nfr-catalog reference where the read-time filter note attaches; line 44 — the EXISTING "An accepted risk needs a named owner" hard limit the D-09 override attaches to; lines 10-16 — the Caveman fence DO-NOT-TOUCH)
    - agent-factory/handoffs/security-nfr-handoff.md (the Required fixes / Accepted risks / Result block lines 38-41 where severity/level/req-id + named-owner override fields attach; the kind: handoff frontmatter to preserve)
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-PATTERNS.md (the security-nfr.md section — SHARPEST CONSTRAINT 4326 B vs WARN 4331/FAIL 4576, terse-or-bump; the security-nfr-handoff.md section — extend Result/fixes/risks, preserve section names, clear voice; the role_ceiling bump precedent)
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-RESEARCH.md (Pattern 3 cumulative level math; Pattern 4 severity-block_on vocabulary alignment; Open Q1 — keep the NFR/perf pointer in the role; Open Q2 — terse-first then documented ceiling bump)
  </read_first>
  <action>
    Edit `agent-factory/roles/security-nfr.md` (CLEAR voice only — the body is scanned by guard_voice; do NOT touch the Caveman fence at lines 10-16):
    - Near line 22 (the checklist reference), add a TERSE read-time level-filter note (D-05): the role works through `agent-factory/checklists/security-nfr-checklist.md` filtered to `security.asvs_level` at read time — cumulative, keep requirements where L is less than or equal to the configured level (L1 lean default through L2 to L3); the file is NOT regenerated when the dial changes (it ships the full set). Keep the existing `plans/nfr-catalog.md` pointer in the role (Open Q1 — the regenerated checklist is pure ASVS; the NFR/perf/reliability budgets stay a role concern referencing `nfr-catalog.md` separately, so the role's performance-vs-NFR-catalog check still resolves).
    - State the D-09 default severity map tersely somewhere clear-voice (e.g. in Responsibilities step on findings or Hard limits): default L1 fail to high, L2 fail to medium, L3 fail to low (a missing baseline control is most dangerous; severity vocab high/medium/low is a subset of `security.block_on`).
    - Attach the override to the EXISTING line 44 named-owner hard limit: extend "An accepted risk needs a named owner" to cover "the auditor MAY override a finding's default severity with a stated reason and a named owner" — minimal new bytes by extending the existing sentence rather than adding a new section.
    BYTE BUDGET (sharpest constraint): after editing, run `wc -c agent-factory/roles/security-nfr.md`. If under 4331 (WARN), done. If it reaches 4331-4575 (WARN) or 4576+ (FAIL), FIRST tighten wording (compress weaker connective prose elsewhere in the role — grugops token-economy = sharper judgment per token); if it still trips, bump the `role_ceiling()` entry for `security-nfr.md` in `scripts/check-foundation-guards.sh` (currently echoes 4576 4331) to a new measured baseline with a one-line inline rationale comment in the Phase-13 style (e.g. +Phase-14 D-09 severity-map; measured N B). Prefer terse; make the choice explicit in the SUMMARY. Do NOT add or rename any `security.*` config key (they ship byte-identical — carried-forward lock); read `asvs_level`/`block_on` only.
    Edit `agent-factory/handoffs/security-nfr-handoff.md` (CLEAR voice; preserve `kind: handoff` frontmatter and existing section names for compatibility): extend the Required fixes / Accepted risks / Result area so each finding can carry a severity tag (high/medium/low), an ASVS level (L1/L2/L3), and the req-id (Vx.y.z); add a named-owner override field on accepted risks (mirroring the role hard limit — an accepted risk / severity override states a reason + a named owner). Keep all existing section headings so anything filling/reading the template stays compatible.
  </action>
  <verify>
    <automated>sh scripts/check-foundation-guards.sh && grep -q 'asvs_level' agent-factory/roles/security-nfr.md && grep -qi 'severity' agent-factory/handoffs/security-nfr-handoff.md && grep -q 'kind: handoff' agent-factory/handoffs/security-nfr-handoff.md</automated>
  </verify>
  <acceptance_criteria>
    - Build gate green after the role + handoff edits: `sh scripts/check-foundation-guards.sh` exits 0 (role stays clear-voice; caveman fence preserved; under ceiling OR ceiling bumped with rationale).
    - Read-time filter note present: `grep -q 'asvs_level' agent-factory/roles/security-nfr.md` exits 0; the note states cumulative / not-regenerated semantics.
    - D-09 severity map present in the role: `grep -qi 'L1' agent-factory/roles/security-nfr.md && grep -qi 'severity' agent-factory/roles/security-nfr.md` exits 0.
    - NFR pointer preserved (Open Q1): `grep -q 'nfr-catalog.md' agent-factory/roles/security-nfr.md` exits 0.
    - Byte budget honored: `wc -c < agent-factory/roles/security-nfr.md` is under the role's `role_ceiling()` FAIL value (4576, or the bumped value if a documented bump was made). If bumped, `scripts/check-foundation-guards.sh` reflects the new ceiling with an inline `+Phase-14` rationale comment.
    - Handoff carries severity/level/req-id + override fields, frontmatter preserved: `grep -qi 'severity' agent-factory/handoffs/security-nfr-handoff.md && grep -q 'kind: handoff' agent-factory/handoffs/security-nfr-handoff.md` exits 0.
    - Caveman fence untouched: the role still has a non-empty Caveman prompt block (`guard_caveman_preserved` passes inside the build gate above).
    - No `security.*` key added/renamed: `cmp -s agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` exits 0 (config unchanged, byte-identical).
  </acceptance_criteria>
  <done>The role carries the read-time level filter + D-09 severity map + named-owner override in clear voice within budget (or with a documented ceiling bump); the handoff carries severity/level/req-id + override fields; no config key changed; build gate green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend guard_voice + the fail-proof harness for the 3 new security surfaces</name>
  <files>scripts/check-foundation-guards.sh, scripts/check-foundation-guards.test.sh</files>
  <read_first>
    - scripts/check-foundation-guards.sh (lines 186-204 — ROLE_FILES + the VOICE_FILES=$ROLE_FILES line 203 + VOICE_MARKERS line 204; lines 206-264 — guard_voice incl. the missing-file presence-check lines 215-219 and the Caveman fence-strip awk lines 230-236 the new surfaces must NOT re-engineer)
    - scripts/check-foundation-guards.test.sh (lines 57-78 — GUARD_INPUTS; lines 83-93 — mirror(); lines 104-111 — expect_fail; lines 182-184 — the voice-marker RED fixture to mirror three times; lines 310-328 — the smoke + cmp-s tail that must stay green)
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-PATTERNS.md (the check-foundation-guards.sh section — add a SEPARATE SEC_VOICE_FILES list and union it into VOICE_FILES; the new surfaces have no Caveman fence so the fence-strip awk is a harmless no-op; the check-foundation-guards.test.sh section — the TWO-STEP extension: GUARD_INPUTS entries + one RED fixture per surface)
    - .planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-RESEARCH.md (Pitfall 3 — both steps required or the hermetic mirror breaks; Pitfall 2 — verify the regenerated checklist + wf15 carry no VOICE_MARKERS token before the smoke run)
  </read_first>
  <action>
    Extend `scripts/check-foundation-guards.sh` `guard_voice` scan set (D-10) — EXTEND, do NOT rebuild the guard machinery (the fence-strip awk + word-boundary marker logic already work):
    - Add a SEPARATE list of the 3 new NON-role security surfaces and union it into `VOICE_FILES`. After line 203 (`VOICE_FILES="$ROLE_FILES"`), define `SEC_VOICE_FILES="agent-factory/workflows/15-security-audit.md agent-factory/checklists/security-nfr-checklist.md agent-factory/handoffs/security-nfr-handoff.md"` and set `VOICE_FILES="$ROLE_FILES $SEC_VOICE_FILES"`. (`security-nfr.md` is ALREADY in `ROLE_FILES` via line 199 — its Caveman-fence carve-out (D-11) is already handled; do NOT add it again and do NOT re-engineer the fence anchor.) The 3 new surfaces have no `## Caveman prompt` fence, so the fence-strip awk (lines 230-236) is a harmless no-op on them and they are scanned whole — which is correct (everything on a security surface is clear voice). They inherit the per-file missing-file presence-check (lines 215-219) automatically by being in `VOICE_FILES`.
    Extend `scripts/check-foundation-guards.test.sh` — the TWO-STEP extension (Pitfall 3: both required or the hermetic mirror breaks):
    1. Add the 3 new surface paths to `GUARD_INPUTS` (lines 57-78) so `mirror()` copies them into every throwaway tree (`agent-factory/workflows/15-security-audit.md`, `agent-factory/checklists/security-nfr-checklist.md`, `agent-factory/handoffs/security-nfr-handoff.md`). (`security-nfr.md` is already listed.)
    2. Add one RED fixture per NEW surface, mirroring the existing `voice-marker` case (lines 182-184): build a fresh mirror, append a caveman marker line (e.g. a `grug smash` style line) into the surface, and `expect_fail` asserting nonzero exit + the surface filename in the output. Three cases — one each for `15-security-audit.md`, `security-nfr-checklist.md`, `security-nfr-handoff.md`. These prove fail-red (no-fabrication: every guard must PROVE it can fail).
    Before running the harness, confirm the regenerated checklist (Plan 01) and workflow 15 (Plan 02) carry NO VOICE_MARKERS token (Pitfall 2 — `rock-solid`/`club` could trip `\brock\b`/`\bclub\b`; hyphen is a word boundary). If a legitimate ASVS word in the generated checklist trips the marker, use the EXISTING D-05 refinement mechanism (a narrow per-phrase `gsub` neutralizing only the specific accepted phrase, mirroring the `/grug` / grug-voice precedent at lines 248-253) — never weaken the marker set wholesale and never edit a verbatim ASVS description. The smoke run (the real guard over the real tree, lines 310-328) MUST stay GREEN, and the config-JSON `cmp -s` case must stay green (no config keys change — carried-forward lock).
  </action>
  <verify>
    <automated>sh scripts/check-foundation-guards.sh && sh scripts/check-foundation-guards.test.sh && grep -q '15-security-audit.md' scripts/check-foundation-guards.sh && grep -q 'security-nfr-checklist.md' scripts/check-foundation-guards.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - guard_voice scan set extended (V-5): `grep -q 'SEC_VOICE_FILES' scripts/check-foundation-guards.sh && grep -q '15-security-audit.md' scripts/check-foundation-guards.sh && grep -q 'security-nfr-checklist.md' scripts/check-foundation-guards.sh && grep -q 'security-nfr-handoff.md' scripts/check-foundation-guards.sh` exits 0.
    - The real guard over the real tree is GREEN (clear voice on all 4 surfaces, fence carve-out intact): `sh scripts/check-foundation-guards.sh` exits 0 (V-5).
    - The harness proves the 3 new surfaces fail RED (V-6): `sh scripts/check-foundation-guards.test.sh` exits 0 with ALL CHECKS PASSED, and it contains an `expect_fail` case naming each new surface: `grep -q 'security-nfr-checklist.md' scripts/check-foundation-guards.test.sh && grep -q '15-security-audit.md' scripts/check-foundation-guards.test.sh && grep -q 'security-nfr-handoff.md' scripts/check-foundation-guards.test.sh`.
    - All 3 new surfaces are in GUARD_INPUTS (mirror copies them): the harness smoke + planted runs do not error on a missing file (covered by `sh scripts/check-foundation-guards.test.sh` exiting 0).
    - Caveman carve-out not re-engineered: the fence-strip awk block (guard.sh lines 230-236) is unchanged; `guard_caveman_preserved` still passes (inside the green build gate).
    - Config byte-identity preserved (V-7): `cmp -s agent-factory/config/factory.config.json agent-factory/seed/.grugops/factory.config.json` exits 0.
  </acceptance_criteria>
  <done>guard_voice scans all four security surfaces; the real tree is GREEN; the harness has a RED fixture per new surface (all in GUARD_INPUTS) proving fail-red; the Caveman carve-out is preserved unchanged; config JSONs stay byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dial config → role/workflow read | `security.asvs_level` / `security.block_on` are trusted enum-validated config; read-only |
| security surfaces → guard_voice | The guard is the no-fabrication control over the prose on those surfaces |
| repo tree → hermetic test mirror | The harness copies surfaces into a throwaway tree; never mutates the real repo/HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Repudiation / Tampering | Voice-discipline guard regression | mitigate | The 3 new surfaces get a RED `expect_fail` fixture each (no-fabrication: a guard that cannot prove it fails is itself the threat). The smoke run asserts the clean tree passes — a silently-passing regression cannot ship. |
| T-14-09 | Tampering | Config dial keys | mitigate | Do NOT add/rename any `security.*` key (they ship byte-identical — carried-forward lock); the `cmp -s` JSON-identity case (V-7) fails red on any drift. Behavior is wired by READING the existing keys only. |
| T-14-10 | Information Disclosure / Tampering | Caveman-fence carve-out | mitigate | The fence-strip awk anchor is NOT re-engineered (D-10/D-11 forward-compat); the role's caveman prompt stays carved out while the rest of the body (incl. new D-09 content) is clear-voice-scanned. `guard_caveman_preserved` proves the fence still holds a marker. |
| T-14-11 | Repudiation | Severity override | mitigate | The D-09 override requires a stated reason + a named owner (no anonymous severity downgrade), reusing the role's existing accepted-risk hard limit. |
| T-14-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs ZERO packages — it edits markdown + POSIX-sh. Package Legitimacy Gate is moot; no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages. |
</threat_model>

<verification>
Per task: `sh scripts/check-foundation-guards.sh` stays GREEN (the build gate, incl. guard_voice over the 4 surfaces + guard_caveman_preserved + guard_role_size).
Plan-level full suite: `sh scripts/check-foundation-guards.sh && sh scripts/check-foundation-guards.test.sh && sh scripts/check-kit-refs.sh && VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.mjs` all exit 0 (V-5/V-6/V-7). The harness proves each new surface fails red on a planted marker; the smoke run proves the clean tree passes; the config `cmp -s` proves no dial drift.
</verification>

<success_criteria>
- The Security/NFR role reads the checklist at `security.asvs_level` (cumulative, read-time, not regenerated) and carries the D-09 severity map + named-owner override in clear voice, within budget or with a documented ceiling bump (SEC-03, D-05/D-09).
- The handoff template carries severity/level/req-id finding fields + a named-owner override field, frontmatter + section names preserved (SEC-03, D-09).
- `guard_voice` scans all four security surfaces in clear voice with the Caveman-fence carve-out preserved on the role only (SEC-03, D-10/D-11; V-5).
- The harness proves each of the 3 new surfaces fails RED on a planted voice regression; the smoke run stays GREEN (SEC-03; V-6).
- No `security.*` config key added/renamed; the config JSONs stay byte-identical (carried-forward lock; V-7).
</success_criteria>

<output>
Create `.planning/phases/14-security-audit-owasp-asvs-checklist-re-anchor/14-03-SUMMARY.md` when done.
</output>

---
phase: 11-senior-persona-overhaul
plan: 05
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-04"]
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/v1.2-SDLC-COVERAGE-AUDIT.md
  - .planning/RETROSPECTIVE.md
autonomous: true
requirements: [PERS-03]

must_haves:
  truths:
    - "guard_wr05 PASSES on a re-run AFTER the 16-role rewrite (regen-safety) — both packaging templates + both materialized adapters carry NO spawn-tool grant in frontmatter (D-10)"
    - "The WR-05 debt marker is closed (flipped to retired/resolved/closed wording) in all four locked locations: PROJECT.md, STATE.md, the SDLC audit observation, RETROSPECTIVE.md (D-10)"
    - "The correct explanatory 'spawn'/'sub-agent' prose in the packaging templates is KEPT — it documents WHY there is no spawn tool; guard_wr05 intentionally allows it (D-08/D-10)"
    - "The SDLC audit GAP-2 table row (~L170) is reconciled from the OBSOLETE 'what good looks like / when to escalate on every role' mechanism to the D-01/D-11 reframe (in-place senior deepening, no new section, terse caveman = token economy) — the user-approved bounded scope addition, one row only"
    - "WR-05 re-run is the LAST check after the rewrite (per D-10); this plan runs in the final wave"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "WR-05 marker closed (Revisit -> retired)"
      contains: "WR-05"
    - path: ".planning/STATE.md"
      provides: "WR-05 tech-debt entry closed"
      contains: "WR-05"
    - path: ".planning/v1.2-SDLC-COVERAGE-AUDIT.md"
      provides: "WR-05 observation resolved (L44/L191) + GAP-2 row L170 reconciled to the D-11 reframe"
      contains: "WR-05"
    - path: ".planning/RETROSPECTIVE.md"
      provides: "WR-05 note closed"
      contains: "WR-05"
  key_links:
    - from: "guard_wr05 (unchanged)"
      to: "the 2 templates + 2 adapters frontmatter"
      via: "the two frontmatter-only EREs (token only, never the prose word)"
      pattern: "WR05_(COMMA|ARRAY)"
---

<objective>
PERS-03 — retire the WR-05 spawn-grant debt: VERIFY + close the marker (not rewrite). The Agent spawn grant was already dropped from both packaging templates in Phase 8 and guard_wr05 already PASSES; Phase 11's job is regen-safety + honest-trace closure (D-10):

1. Confirm both packaging-template + both adapter frontmatters are spawn-free.
2. Re-run guard_wr05 AFTER the 16-role rewrite + guard changes (Plans 11-01..11-04) — this is the LAST check (per D-10), proving the persona overhaul did not silently re-arm sub-agent spawning.
3. Close the WR-05 debt marker (flip to retired/resolved/closed wording) in the four LOCKED tracking locations: PROJECT.md, STATE.md, the SDLC audit observation, RETROSPECTIVE.md. KEEP the correct explanatory "spawn"/"sub-agent" prose in the templates — it documents why there is no spawn tool, and guard_wr05 intentionally allows it.
4. (User-approved bounded addition) Reconcile the SDLC audit GAP-2 table row (~L170) from the OBSOLETE D-11 mechanism ("what good looks like / when to escalate on every role") to the actual D-01/D-11 reframe (in-place senior deepening, no new section, terse caveman = token economy) — one row only, do not rewrite the audit.

Purpose: keep the audit trail truthful (the user chose to amend source-of-truth rather than let it drift) and prove single-window sequential role-load survived the overhaul.
Output: guard_wr05 green on a fresh re-run; 4 markers closed; the GAP-2 row reconciled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-senior-persona-overhaul/11-CONTEXT.md
@.planning/phases/11-senior-persona-overhaul/11-PATTERNS.md
@scripts/check-foundation-guards.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run guard_wr05 (regen-safety, the LAST check) + confirm frontmatter spawn-free</name>
  <files>scripts/check-foundation-guards.sh</files>
  <read_first>
    - scripts/check-foundation-guards.sh lines 61-93 (guard_wr05: the two frontmatter-only EREs WR05_COMMA/WR05_ARRAY at 76-77, the explicit 4-file WR05_SCAN at 78-81, the rationale that it matches the TOKEN only and NEVER the prose word "spawn") — NO EDIT to this guard, the test is a re-run
    - agent-factory/packaging/subagent.frontmatter.md (PATTERNS.md Group G: tools: Read, Grep, Glob, Bash, Edit, Write — no Agent/Task; explanatory spawn prose at lines 15-18, 58-63 is KEPT)
    - agent-factory/packaging/slash-command.template.md (PATTERNS.md Group G: allowed-tools arrays carry only Read, Write, Bash, Glob, Grep; explanatory prose at lines 98-101 KEPT)
    - .planning/phases/11-senior-persona-overhaul/11-PATTERNS.md section Group G (the verified spawn-free frontmatter + the keep-the-prose rule)
    - .planning/phases/11-senior-persona-overhaul/11-CONTEXT.md decision D-10
  </read_first>
  <action>
    Do NOT edit guard_wr05 — this is a verify-not-rewrite step (D-10). Re-run sh scripts/check-foundation-guards.sh AFTER the rewrite + guard changes have landed (Plans 11-01..11-04) so this is the LAST check (regen-safety per D-10). Confirm guard_wr05 PASSES (no spawn grant in the 2 packaging templates + 2 materialized adapters frontmatter). Confirm the explanatory "spawn"/"sub-agent" prose is STILL present in both templates (it documents why there is no spawn tool; the guard intentionally does not match it — do NOT trim it; token-economy discipline targets role prompts loaded every session, not maintainer-facing templates). If guard_wr05 unexpectedly FAILS, STOP — a regeneration re-armed the spawn grant (the WR-05 hazard); the fix is to remove the re-added Agent/Task token from the offending frontmatter, NOT to weaken the guard. Mark any unverifiable command UNKNOWN - verify (no fabrication).
  </action>
  <verify>
    <automated>sh scripts/check-foundation-guards.sh > /tmp/g11-05.log 2>&1; rc=$?; cat /tmp/g11-05.log; grep -Eq '(tools|allowed-tools):.*\b(Agent|Task)\b' agent-factory/packaging/subagent.frontmatter.md agent-factory/packaging/slash-command.template.md && { echo "FAIL spawn grant re-armed in a template"; exit 1; }; grep -qi 'spawn\|sub-agent' agent-factory/packaging/subagent.frontmatter.md || { echo "FAIL explanatory prose lost"; exit 1; }; test "$rc" -eq 0 && grep -q 'WR-05: no spawn grant' /tmp/g11-05.log && echo PASS || { echo FAIL; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - sh scripts/check-foundation-guards.sh exits 0 and the guard_wr05 line reads "WR-05: no spawn grant in frontmatter" (PASS on the fresh re-run AFTER the rewrite)
    - No (tools|allowed-tools): ... Agent|Task token in either packaging template frontmatter (grep returns nothing)
    - The explanatory spawn/sub-agent prose is STILL present in subagent.frontmatter.md (KEPT, not trimmed)
    - guard_wr05 itself was NOT edited (verify-not-rewrite)
  </acceptance_criteria>
  <done>guard_wr05 GREEN on the post-rewrite re-run; templates spawn-free in frontmatter; explanatory prose preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Close the 4 WR-05 markers + reconcile the audit GAP-2 row (L170)</name>
  <files>.planning/PROJECT.md, .planning/STATE.md, .planning/v1.2-SDLC-COVERAGE-AUDIT.md, .planning/RETROSPECTIVE.md</files>
  <read_first>
    - .planning/PROJECT.md around L134 (PATTERNS.md Group G: the "Revisit: the templates still carry Agent/spawn prose (WR-05 regeneration hazard, tech debt)" marker to flip to retired)
    - .planning/STATE.md around L242 (the "[v1.2] WR-05 spawn-grant regeneration hazard ... Retire it in Phase 11 ..." Blockers/Concerns entry to flip to closed)
    - .planning/v1.2-SDLC-COVERAGE-AUDIT.md around L44, L170, L191 (PATTERNS.md Group G: L44 + L191 are the WR-05 observation lines to flip to resolved/retired; L170 is the GAP-2 table row carrying the OBSOLETE "what good looks like / when to escalate on every role" mechanism — reconcile to the D-11 reframe; PATTERNS.md flags L170 as the user-approved bounded addition, NOT in the original locked D-10 list)
    - .planning/RETROSPECTIVE.md around L25 (the "WR-05 ... remains open; carried-forward warnings need a hard owner" note to flip to closed)
    - .planning/phases/11-senior-persona-overhaul/11-PATTERNS.md section Group G (the four locked marker locations table with verbatim current wording + the L170 reconciliation note)
    - .planning/phases/11-senior-persona-overhaul/11-CONTEXT.md decision D-10
  </read_first>
  <action>
    Close the WR-05 debt marker in the four LOCKED locations (D-10), flipping each to retired/resolved/closed wording while keeping the line factual (cite that the grant was dropped in Phase 8, guarded in Phase 10, and re-verified green after the Phase-11 rewrite): (a) PROJECT.md ~L134 — flip the "Revisit / tech debt" wording to retired/closed; (b) STATE.md ~L242 — flip the Blockers/Concerns WR-05 entry to closed (or move to a resolved note); (c) v1.2-SDLC-COVERAGE-AUDIT.md ~L44 and ~L191 — flip the WR-05 observation to resolved/retired; (d) RETROSPECTIVE.md ~L25 — flip the open-WR-05 note to closed. Do these as targeted in-place edits to the exact marker lines — do NOT rewrite surrounding sections. THEN (the user-approved bounded addition) reconcile the SDLC audit GAP-2 table row ~L170: it currently describes Phase 11 with the OBSOLETE mechanism ("what good looks like / when to escalate on every role"); replace that description with the D-01/D-11 reframe — in-place senior persona deepening, NO new section, terse caveman voice preserved as the token-economy mechanism, verified by the three guards. Keep it minimal: one line/row, do not rewrite the audit. No fabrication — only state what is actually true (the grant is gone, the guard passes).
  </action>
  <verify>
    <automated>for f in .planning/PROJECT.md .planning/STATE.md .planning/v1.2-SDLC-COVERAGE-AUDIT.md .planning/RETROSPECTIVE.md; do grep -q 'WR-05' "$f" || { echo "FAIL WR-05 marker vanished from $f"; exit 1; }; done; grep -Eqi 'retired|resolved|closed' .planning/PROJECT.md && grep -Eqi 'retired|resolved|closed' .planning/RETROSPECTIVE.md || { echo "FAIL WR-05 not marked retired/closed"; exit 1; }; grep -qi 'what good looks like' .planning/v1.2-SDLC-COVERAGE-AUDIT.md && { echo "FAIL GAP-2 L170 still carries obsolete mechanism"; exit 1; }; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All four files still mention WR-05 (the marker is closed in place, not deleted)
    - PROJECT.md, STATE.md, the SDLC audit, and RETROSPECTIVE.md WR-05 markers read as retired/resolved/closed (not "Revisit"/"remains open"/"tech debt")
    - The explanatory spawn prose in the packaging templates is untouched (Task 1 covers this; do not edit templates here)
    - The SDLC audit GAP-2 row no longer carries the obsolete "what good looks like / when to escalate on every role" mechanism; it reads as in-place senior deepening / no new section / terse caveman = token economy (D-11 reframe) — grep for the obsolete phrase returns nothing
    - SPOT-REVIEW (prose-judgment): each closed marker is factual (grant gone since Phase 8, guarded Phase 10, re-verified green Phase 11), no fabricated claim
  </acceptance_criteria>
  <done>The 4 WR-05 markers closed in place (retired/resolved/closed); the audit GAP-2 row reconciled to the D-11 reframe; no marker deleted, no fabrication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer/dev -> grugops repo | Maintainer editing tracking-doc markdown + re-running a read-only guard. No web/network/auth/data surface. The threat class is gate & trace integrity — specifically a regeneration silently re-arming the WR-05 spawn grant (false-GREEN safety design) or a dishonest "retired" claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Elevation of Privilege | a regeneration during the persona overhaul silently re-arms sub-agent spawning (re-adds the Agent/Task grant to a template/adapter), breaking single-window sequential role-load across the 5 host CLIs | mitigate | Task 1 re-runs guard_wr05 as the LAST check (regen-safety, D-10) and the verify greps both templates directly for a re-added token; if found, STOP and remove the token (never weaken the guard). The mechanical grep is the backpressure, not the prose. |
| T-11-09 | Repudiation | a marker is flipped to "retired" without the underlying fact being true (dishonest trace) | mitigate | Task 1 proves the fact mechanically (guard_wr05 GREEN + direct frontmatter grep) BEFORE Task 2 writes "retired"; the closure wording cites the verifiable chain (dropped Phase 8 / guarded Phase 10 / re-verified Phase 11). No fabrication. |
| T-11-10 | Tampering | trimming the explanatory spawn prose (which guard_wr05 intentionally allows) under a misguided token-economy impulse, losing the documentation of WHY there is no spawn tool | mitigate | D-10 explicitly KEEPS this prose; Task 1 verify asserts it is still present; token-economy targets role prompts, not maintainer-facing templates. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero external packages (markdown/doc edits + a read-only guard re-run; npm deps Out of Scope). |

Severity LOW. The one safety-relevant risk (T-11-02 spawn re-arm) is mitigated mechanically by the guard re-run + direct frontmatter grep, exactly the regen-safety D-10 mandates.
</threat_model>

<verification>
- sh scripts/check-foundation-guards.sh exits 0 with guard_wr05 reading "WR-05: no spawn grant in frontmatter" — the post-rewrite regen-safety re-run is GREEN (the LAST check, D-10).
- Both packaging templates carry NO Agent/Task frontmatter token; the explanatory spawn prose is preserved.
- All four tracking docs still mention WR-05 and read as retired/resolved/closed; the SDLC audit GAP-2 row no longer carries the obsolete "what good looks like / when to escalate" mechanism.
- Spot-review that each closure is factual (no fabricated "retired" claim).
</verification>

<success_criteria>
PERS-03 complete: guard_wr05 GREEN on a fresh post-rewrite re-run (single-window sequential role-load preserved across the 5 host CLIs); the WR-05 debt marker closed in PROJECT.md / STATE.md / the SDLC audit / RETROSPECTIVE.md; the explanatory spawn prose kept; and the user-approved GAP-2 audit row reconciled to the D-11 reframe.
</success_criteria>

<output>
Create `.planning/phases/11-senior-persona-overhaul/11-05-SUMMARY.md` when done
</output>

---
phase: 11-senior-persona-overhaul
plan: 04
type: execute
wave: 2
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - scripts/check-foundation-guards.sh
  - scripts/check-foundation-guards.test.sh
autonomous: true
requirements: [PERS-01]

must_haves:
  truths:
    - "guard_voice scans all 16 roles (VOICE_FILES expanded from 3) and is GREEN on the clean tree — the 3 verified false positives (orchestrator /grug, agents-md-scribe x2 voice-meta) do NOT trip it because VOICE_MARKERS was refined first (D-05)"
    - "guard_caveman_preserved asserts every one of the 16 roles still has a non-empty Caveman prompt block with at least 1 caveman marker (positive inverse of guard_voice), and ships GREEN (D-06)"
    - "guard_role_size enforces a per-file-relative two-tier WARN->FAIL byte ceiling per role (hard-coded documented constants), with CR-01 missing-file fail-red, and ships GREEN (D-07)"
    - "The guards ship GREEN over the clean tree AND the fail-proof harness proves each can fail RED: a sanded-caveman role (D-06 RED), an oversized role (D-07 RED), plus the expanded voice scan (Phase-10 ship-GREEN + fail-on-mutation discipline)"
    - "All guard work stays POSIX sh / set -eu / printf / portable grep flags (-r -n -l -E -F -q -v, no -P/-z/--include) / explicit SCAN lists — house style; no npm deps (TS pivot HELD)"
  artifacts:
    - path: "scripts/check-foundation-guards.sh"
      provides: "guard_voice (all 16 + refined markers), guard_caveman_preserved (new), guard_role_size (new); all GREEN"
      contains: "guard_caveman_preserved"
    - path: "scripts/check-foundation-guards.test.sh"
      provides: "Fail-proof: sanded-caveman fixture (D-06 RED) + oversized-role fixture (D-07 RED) + expanded GUARD_INPUTS + smoke-green"
      contains: "caveman"
  key_links:
    - from: "scripts/check-foundation-guards.sh guard_caveman_preserved"
      to: "the 16 role files"
      via: "awk fence-counting (inverse of guard_voice) + VOICE_MARKERS assert"
      pattern: "Caveman prompt"
    - from: "scripts/check-foundation-guards.test.sh GUARD_INPUTS"
      to: "the 13 not-yet-mirrored roles"
      via: "mirror copies them into each hermetic case"
      pattern: "agent-factory/roles/"
---

<objective>
The three mechanical-guard changes that make PERS-01's amended success criteria mechanically true, authored AFTER the 16-role rewrite is clean (Plans 11-01/02/03) so they ship GREEN (Phase-10 ship-GREEN discipline), each carrying a planted-violation proof it can fail RED:

- D-05 — expand guard_voice to all 16 roles + refine VOICE_MARKERS. The load-bearing hazard (RESEARCH Pitfall 1, VERIFIED): a naive VOICE_FILES of all 16 ships the guard RED on the clean tree because the grug word-boundary marker matches /grug (orchestrator.md:35) and the Scribe's two clear-voice voice-meta lines (agents-md-scribe.md:38, :40). The marker refinement MUST land WITH the scan expansion so the expanded guard is GREEN — never after a red scan.
- D-06 — new guard_caveman_preserved (positive inverse of guard_voice): assert every role keeps a non-empty Caveman prompt block with at least 1 caveman marker, so the senior rewrite cannot sand the grug voice off.
- D-07 — new guard_role_size (byte-for-byte mirror of guard_adapter_size): a per-file-relative two-tier WARN->FAIL ceiling per role, with CR-01 missing-file fail-red, so a bloated rewrite fails red.

Plus the fail-proof harness fixtures proving D-06 and D-07 can go RED.

Purpose: encode the token-economy + caveman-preserved + voice-discipline invariants in code, not convention (the user chose both new guards explicitly).
Output: two guard functions + one expanded scan + two new fail-proof fixtures, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-senior-persona-overhaul/11-CONTEXT.md
@.planning/phases/11-senior-persona-overhaul/11-PATTERNS.md
@.planning/phases/11-senior-persona-overhaul/11-RESEARCH.md
@scripts/check-foundation-guards.sh
@scripts/check-foundation-guards.test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand guard_voice to all 16 (D-05, marker refinement FIRST) + add guard_caveman_preserved (D-06)</name>
  <files>scripts/check-foundation-guards.sh</files>
  <read_first>
    - scripts/check-foundation-guards.sh lines 52-59 (pass/fail/warn helpers to reuse verbatim), lines 150-202 (guard_voice: VOICE_FILES at 166-168, VOICE_MARKERS at 169, the awk fence-strip at 186-192, the CR-02 missing-file presence guard at 180-184), lines 207-211 (the run block where the new guard is registered)
    - .planning/phases/11-senior-persona-overhaul/11-PATTERNS.md section Group B (guard_caveman_preserved: the verified awk strip to INVERT, the inverse assertion shape, VOICE_MARKERS reuse, CR-02 presence-first) and section Group D (guard_voice expansion: the 13 roles to ADD to VOICE_FILES, the 3 VERIFIED false-positive lines and the marker-refinement recommendation)
    - .planning/phases/11-senior-persona-overhaul/11-RESEARCH.md section Pitfall 1 (the VERIFIED voice-RED hazard + the 3 false positives + Open Q2 marker-refinement recommendation: pre-filter the /grug brand literal AND accept the Scribe's two voice-meta lines such as the grug-voice / grug-wink phrasings; keep the grug word-boundary otherwise so a bare grug-smash still fails) and the Anti-Patterns note (do NOT re-engineer the awk anchor)
    - .planning/phases/11-senior-persona-overhaul/11-CONTEXT.md decisions D-05, D-06 and the Claude's Discretion notes (marker-refinement choice + caveman-marker set)
  </read_first>
  <action>
    FIRST refine VOICE_MARKERS / the voice scan so the expansion is GREEN before flipping VOICE_FILES — do NOT re-engineer the "everything EXCEPT Caveman prompt" awk anchor (RESEARCH Anti-Pattern; Phase-10 D-10 says it is forward-compatible). Apply the verified-narrow refinement: pre-filter the /grug brand-command literal (unambiguously not caveman voice) AND accept the Scribe's two voice-meta lines that describe the voice rule in clear prose (exclude the grug-voice / grug-wink phrasings). Make the refinement ROBUST (a senior rewrite could introduce NEW clear-voice grug prose), not a 3-line patch, but keep the grug word-boundary otherwise so a bare grug-smash in a clear-voice surface STILL fails (that is the existing voice-marker fixture). THEN expand VOICE_FILES from the 3 curated surfaces to all 16 role files (add the 13 named in PATTERNS.md Group D; _role-switch-protocol.md is NOT one of the 16 — exclude it). Confirm guard_voice is GREEN on the clean tree. Then add guard_caveman_preserved (D-06) as the positive inverse: reuse the pass/fail helpers, the same 16-role list, and the SAME VOICE_MARKERS list; for each role assert presence first (CR-02, name the file on a missing role, never let a non-zero awk abort the script), then keep ONLY the lines INSIDE the fenced Caveman prompt block (invert the verified fence-counting awk per PATTERNS.md Group B), fail red if that block is empty/missing (finding token: "caveman prompt block missing or empty") and fail red if it contains no marker (finding token: "no caveman marker (voice sanded off?)"), else pass. Register guard_caveman_preserved in the run block. House style only: POSIX sh, set -eu, printf, portable grep flags (-r -n -l -E -F -q -v; NO -P/-z/--include), explicit SCAN lists, no repo-wide grep. The guard ships GREEN over the clean (rewritten) tree.
  </action>
  <verify>
    <automated>sh scripts/check-foundation-guards.sh > /tmp/g11-04a.log 2>&1; rc=$?; cat /tmp/g11-04a.log; test "$rc" -eq 0 && grep -q guard_caveman_preserved scripts/check-foundation-guards.sh && ! grep -Eqi 'orchestrator.md.*grug|agents-md-scribe.md.*grug' /tmp/g11-04a.log && echo PASS || { echo FAIL; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - sh scripts/check-foundation-guards.sh exits 0 (GREEN on the clean tree) — guard_voice over all 16 + guard_caveman_preserved both pass
    - grep finds guard_caveman_preserved in the script and it is registered in the run block (called before the Result section)
    - VOICE_FILES lists all 16 roles (the 13 added + the original 3); _role-switch-protocol.md is NOT in the list
    - No false-positive finding for orchestrator.md /grug or agents-md-scribe.md voice-meta in the guard output (marker refinement worked)
    - The refinement did NOT re-engineer the caveman-strip awk anchor; portable grep flags only (no -P/-z/--include)
  </acceptance_criteria>
  <done>guard_voice scans all 16 + is GREEN (markers refined first); guard_caveman_preserved added, registered, GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add guard_role_size (D-07, mirror guard_adapter_size, hard-coded per-file ceilings)</name>
  <files>scripts/check-foundation-guards.sh</files>
  <read_first>
    - scripts/check-foundation-guards.sh lines 122-147 (guard_adapter_size: the two-tier WARN->FAIL byte-ceiling template + the CR-01 missing-file fail-red at 138-141 — the byte-for-byte mirror for guard_role_size), lines 54-59 (helpers), lines 207-211 (run block)
    - .planning/phases/11-senior-persona-overhaul/11-PATTERNS.md section Group C (guard_role_size: the full guard_adapter_size analog body to mirror, the CR-01 missing-file fail-red + two-tier WARN->FAIL load-bearing patterns, the verified live size baseline table, the per-file-relative documented-constants strategy, the ba-pm extra-headroom note)
    - .planning/phases/11-senior-persona-overhaul/11-RESEARCH.md Open Question 1 (threshold strategy: per-file FAIL = current + ~12%, WARN = current + ~6%, hard-coded NOT computed live; ba-pm warrants larger headroom)
    - .planning/phases/11-senior-persona-overhaul/11-CONTEXT.md decision D-07 + the Claude's Discretion size-ceiling note
  </read_first>
  <action>
    Add guard_role_size as a byte-for-byte mirror of guard_adapter_size (PATTERNS.md Group C analog, lines 133-147): iterate the 16 role files, CR-01 assert presence first (a deleted role must fail red naming the path, never vacuous-pass on an empty byte count), read bytes with wc -c piped through tr -d space, two-tier compare (at-or-above ROLE_FAIL -> fail with token "role bloated (senior != verbose)"; at-or-above ROLE_WARN -> warn "approaching ceiling"; else pass). Use PER-FILE hard-coded documented constants (NOT a flat number — a flat number either punishes orchestrator's legit 6286 B or licenses every other role to bloat; NOT computed live — a live current size makes the guard tautological). Lock these exact per-file ceilings (baseline captured 2026-06-10, FAIL = baseline +12%, WARN = baseline +6%): orchestrator FAIL 7041 / WARN 6664; security-nfr 4576 / 4331; compliance-officer 4160 / 3937; release-manager 4144 / 3922; agents-md-scribe 3910 / 3701; architect-design 3617 / 3423; factory-coach 3420 / 3237; incident-responder 3387 / 3206; installer 3345 / 3166; software-engineer 3307 / 3130; qe-e2e 3224 / 3051; uat-planner 3149 / 2980; system-analyst 2809 / 2659; greenfield-mapper 2673 / 2530; brownfield-mapper 2487 / 2354. ba-pm gets EXTRA headroom (it legitimately gains BA judgment via PERS-02): FAIL 3294 / WARN 3075 (baseline 2745, +20%/+12%) — call this out in a guard comment, but it still fails a bloated rewrite. Document the baseline-capture date (2026-06-10) and the +12%/+6% (+20%/+12% for ba-pm) headroom rationale in a header comment. _role-switch-protocol.md (2326 B) is NOT one of the 16 — EXCLUDE it from ROLE_FILES. Register guard_role_size in the run block. Confirm GREEN on the clean (rewritten) tree. House style only (POSIX sh, set -eu, printf, portable greps, explicit list).
  </action>
  <verify>
    <automated>sh scripts/check-foundation-guards.sh > /tmp/g11-04b.log 2>&1; rc=$?; cat /tmp/g11-04b.log; n=$(grep -oE 'agent-factory/roles/[a-z-]+\.md' scripts/check-foundation-guards.sh | grep -v _role-switch | sort -u | wc -l | tr -d ' '); echo "distinct-role-paths=$n"; test "$rc" -eq 0 && grep -q guard_role_size scripts/check-foundation-guards.sh && echo PASS || { echo FAIL; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - sh scripts/check-foundation-guards.sh exits 0 (GREEN) — guard_role_size passes over the clean rewritten tree
    - grep finds guard_role_size in the script and it is registered in the run block
    - Per-file ceilings are the locked hard-coded constants above (NOT a single flat number; NOT computed live from current size); orchestrator's 7041/6664 is the deliberate outlier, ba-pm's 3294/3075 the documented BA headroom
    - _role-switch-protocol.md is EXCLUDED from ROLE_FILES (the 16-role list, not 17); the guard scans exactly 16 distinct role paths
    - CR-01 missing-file fail-red present (mirrors guard_adapter_size lines 138-141); byte-based not line-based
  </acceptance_criteria>
  <done>guard_role_size added with locked per-file ceilings (orchestrator outlier + ba-pm headroom), CR-01 fail-red, registered, GREEN over the clean tree.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the fail-proof harness — sanded-caveman (D-06 RED) + oversized-role (D-07 RED) + expanded GUARD_INPUTS</name>
  <files>scripts/check-foundation-guards.test.sh</files>
  <read_first>
    - scripts/check-foundation-guards.test.sh lines 51-58 (GUARD_INPUTS — currently only the 3 voice roles among roles; must add the 13 not-yet-mirrored roles so mirror copies them), lines 63-73 (mirror), lines 77-79 (run_in), lines 84-91 (expect_fail), lines 116-146 (the guard_agents_bytes + guard_adapter_size oversize + missing fixtures to mirror), lines 154-156 (the voice-marker planted-violation shape to mirror), lines 168-178 (the smoke run that must stay GREEN)
    - .planning/phases/11-senior-persona-overhaul/11-PATTERNS.md section Group E (the two new fixtures: sanded-caveman role + oversized role; the yes/head padding idiom; the GUARD_INPUTS growth; the expanded smoke must stay GREEN)
    - .planning/phases/11-senior-persona-overhaul/11-CONTEXT.md decisions D-06, D-07
  </read_first>
  <action>
    Extend GUARD_INPUTS (lines 51-58) to include ALL 16 role files (add the 13 not currently mirrored) so the mirror copies every role guard_caveman_preserved + guard_role_size read into each hermetic case. Then plant two new fixtures mirroring the existing voice-marker / adapter-oversize shapes (use mirror + the planted-violation + expect_fail idiom; nothing outside WORK is written): (1) D-06 RED sanded-caveman — mirror a case, empty or strip all markers from a role's Caveman prompt block (e.g. replace the block body with marker-free prose), assert guard_caveman_preserved exits nonzero AND the finding names the role path + a "caveman"/"sanded"/"marker" token; (2) D-07 RED oversize-role — mirror a case, pad a role past its locked FAIL ceiling with the yes-piped-to-head idiom (as adapter-oversize does at lines 137-139), assert guard_role_size exits nonzero AND the finding names the role path + "bloated". Optionally add the CR-01 missing-role case for each new guard (mirror the existing *-missing cases). Confirm the smoke run (lines 168-178) over the REAL tree stays GREEN (proves no false positives after the D-05 marker refinement — the T-10-02-FP invariant). House style: POSIX sh, set -eu, printf, hermetic mktemp -d + trap cleanup; the real repo and HOME are never mutated.
  </action>
  <verify>
    <automated>sh scripts/check-foundation-guards.test.sh > /tmp/g11-04c.log 2>&1; rc=$?; cat /tmp/g11-04c.log; test "$rc" -eq 0 && grep -qi 'caveman' /tmp/g11-04c.log && grep -qi 'bloated\|oversize' /tmp/g11-04c.log && grep -q 'ALL CHECKS PASSED' /tmp/g11-04c.log && echo PASS || { echo FAIL; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - sh scripts/check-foundation-guards.test.sh exits 0 and prints ALL CHECKS PASSED
    - GUARD_INPUTS lists all 16 role files (the 13 added + the 3 originals)
    - A sanded-caveman fixture proves guard_caveman_preserved fails RED (nonzero + names the role + a caveman/sanded/marker token) — D-06 cannot only-ever-pass
    - An oversized-role fixture proves guard_role_size fails RED (nonzero + names the role + "bloated") — D-07 cannot only-ever-pass
    - The smoke run over the REAL tree stays GREEN (no false positives after the D-05 marker refinement)
    - Hermetic: only WORK (mktemp -d) is written; the real repo + HOME untouched
  </acceptance_criteria>
  <done>Harness extended: GUARD_INPUTS covers all 16 roles; sanded-caveman (D-06) + oversized-role (D-07) fixtures prove RED; smoke stays GREEN; exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer/dev -> grugops repo | The guards are read-only POSIX-sh scripts run over grugops's own repo by a maintainer. No web/network/auth/data/untrusted-input surface. The threat class is gate & trace integrity — a guard that is false-RED (blocks honest work) or false-GREEN (can never fail). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01a | Denial of Service | D-05 voice expansion ships RED on the clean tree (the VERIFIED /grug + Scribe voice-meta false positives) — a broken gate that blocks honest work or gets disabled | mitigate | Task 1 refines VOICE_MARKERS BEFORE flipping VOICE_FILES so the expanded guard is GREEN on the clean tree; the verify asserts no orchestrator/scribe false-positive in the output. |
| T-11-01b | Spoofing (false-green gate) | a new guard (D-06/D-07) that can only ever PASS is fabricated green — violates the no-fabrication contract | mitigate | Task 3 plants exactly one real violation per new guard (sanded-caveman, oversized-role) and asserts each fails RED naming the defect; the harness is the proof the gate can fail. |
| T-11-07 | Tampering | the marker ERE is weakened to dodge a false positive, eroding the guard's real coverage | mitigate | Refinement is narrow (pre-filter the /grug literal + accept the two Scribe voice-meta phrasings) and keeps the grug word-boundary otherwise; the existing bare grug-smash fixture must STILL fail (verified by the unchanged voice-marker case staying RED in Task 3). |
| T-11-08 | Tampering | guard_role_size set to a flat number (punishes orchestrator OR licenses bloat) or a live-computed ceiling (tautological, never fails) | mitigate | Task 2 hard-codes per-file documented constants from the 2026-06-10 baseline; orchestrator gets its own 7041/6664, ba-pm its documented 3294/3075 headroom; verify asserts the guard scans exactly 16 distinct roles. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero external packages (POSIX-sh only; npm deps Out of Scope, TS pivot HELD). No package-legitimacy surface. |

Severity LOW: maintainer-side read-only scripts, no untrusted input. Both real risks (false-RED blocking, false-GREEN fabrication) are mitigated by the marker-refinement-first sequencing + the fail-proof harness.
</threat_model>

<verification>
- sh scripts/check-foundation-guards.sh exits 0 — guard_voice (all 16, refined), guard_caveman_preserved, guard_role_size all GREEN on the clean rewritten tree.
- sh scripts/check-foundation-guards.test.sh exits 0 and prints ALL CHECKS PASSED — the sanded-caveman (D-06) and oversized-role (D-07) fixtures each fail RED naming the defect; the bare grug-smash voice fixture still fails RED; the smoke run over the real tree is GREEN.
- No orchestrator/scribe false positive in the guard output (the D-05 marker refinement worked).
</verification>

<success_criteria>
The three guard changes (D-05 voice expansion + refinement, D-06 caveman-preserved, D-07 role-size) all ship GREEN over the clean tree; the fail-proof harness proves D-06 and D-07 each go RED on a planted violation and the voice scan stays false-positive-free — PERS-01's amended mechanical success criteria are now enforced in code.
</success_criteria>

<output>
Create `.planning/phases/11-senior-persona-overhaul/11-04-SUMMARY.md` when done
</output>

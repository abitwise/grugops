---
phase: 11-senior-persona-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/roles/installer.md
  - agent-factory/roles/orchestrator.md
  - agent-factory/roles/qe-e2e.md
  - agent-factory/roles/release-manager.md
  - agent-factory/roles/security-nfr.md
  - agent-factory/roles/software-engineer.md
  - agent-factory/roles/system-analyst.md
  - agent-factory/roles/uat-planner.md
autonomous: true
requirements: [PERS-01]

must_haves:
  truths:
    - "Each of the 8 roles reads as a senior practitioner with long-term experience + forward-thinking judgment (D-02), woven in place with NO new section (D-01)"
    - "Each role's single `One job` is unchanged — persona + voice deepen, responsibilities do not (D-03)"
    - "Each role keeps its `## Caveman prompt` grug block, contract sections, pointer discipline, kit-vs-state refs, and AGENTS.md footer (D-03)"
    - "orchestrator.md keeps its `> Kit vs state invariant` banner, Routing matrix, WIP/DoR gate, XL-split, and workflow-mapping table — the legitimate size outlier is not normalized away (D-03)"
    - "Each role stays flat-or-smaller in bytes (D-04); clear voice preserved on orchestrator/release/security safety lines (two-voice discipline)"
  artifacts:
    - path: "agent-factory/roles/orchestrator.md"
      provides: "Senior orchestrator persona; legit size outlier; clear-voice safety hard limits preserved"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/security-nfr.md"
      provides: "Senior security/NFR persona; clear-voice security lines (in guard_voice scan set)"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/release-manager.md"
      provides: "Senior release persona; clear-voice human deploy-gate preserved"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/installer.md"
      provides: "Senior installer persona; clear-voice additive/never-overwrite/dry-run hard limit"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/qe-e2e.md"
      provides: "Senior QE/E2E persona"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/software-engineer.md"
      provides: "Senior engineer persona; clear-voice no-fake-results hard limit"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/system-analyst.md"
      provides: "Senior system-analyst persona"
      contains: "## Caveman prompt"
    - path: "agent-factory/roles/uat-planner.md"
      provides: "Senior UAT-planner persona"
      contains: "## Caveman prompt"
  key_links:
    - from: "orchestrator.md"
      to: "agent-factory/workflows/* + definition-of-ready.md"
      via: "Routing matrix + WIP/DoR gate (existing references preserved, D-03)"
      pattern: "definition-of-ready|workflows/"
---

<objective>
In-place senior rewrite of the remaining 8 of the 16 role prompts (PERS-01, group I–Z): `installer`, `orchestrator`, `qe-e2e`, `release-manager`, `security-nfr`, `software-engineer`, `system-analyst`, `uat-planner`.

Same mechanism as Plan 11-01: each role rewritten holistically top-to-bottom (D-02) within the fixed skeleton, NO new section (D-01), elevating persona + voice but NOT the contract (D-03), seniority encoded as compression so files stay flat-or-smaller (D-04). The special case here is `orchestrator.md` — the legitimate size outlier (6286 B vs next-largest 4085 B) carrying a `> Kit vs state invariant` banner, Routing matrix, WIP/DoR gate, XL-split, and a workflow-mapping table that NO other role has; these structural extras must be preserved (do not normalize them away), and its clear-voice safety hard limits ("Never merge to a protected branch. Never deploy to prod.") stay plain English.

Purpose: complete the 16-role senior substrate (with Plans 11-01 + 11-03).
Output: 8 rewritten role files, each guard-clean and flat-or-smaller.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-senior-persona-overhaul/11-CONTEXT.md
@.planning/phases/11-senior-persona-overhaul/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Senior rewrite — orchestrator (size outlier), system-analyst, qe-e2e, uat-planner</name>
  <files>agent-factory/roles/orchestrator.md, agent-factory/roles/system-analyst.md, agent-factory/roles/qe-e2e.md, agent-factory/roles/uat-planner.md</files>
  <read_first>
    - agent-factory/roles/orchestrator.md (the file being rewritten; SKELETON DEVIATION per PATTERNS.md Group A item 1: carries the `> Kit vs state invariant` banner at line 7, `### Routing matrix` lines 51-66, `### WIP + Definition-of-Ready gate` lines 68-70, `### XL-split` lines 72-73, the `## Output` workflow-mapping table lines 91-108, clear-voice Hard limits lines 120-123 — preserve ALL of these; line 35 `/grug` is the D-05 false-positive #1 the Plan 11-04 guard pre-filters, leave it as-is)
    - agent-factory/roles/system-analyst.md (the file being rewritten)
    - agent-factory/roles/qe-e2e.md (the file being rewritten)
    - agent-factory/roles/uat-planner.md (the file being rewritten)
    - .planning/phases/11-senior-persona-overhaul/11-PATTERNS.md § "Group A" (uniform skeleton + the orchestrator deviation list)
    - .planning/phases/11-senior-persona-overhaul/11-CONTEXT.md § decisions D-01..D-04
  </read_first>
  <action>
    Rewrite each of the 4 role files in place, holistically top-to-bottom (D-02), within the fixed skeleton (D-01/D-03). Deepen senior judgment INSIDE existing sections — forward-thinking in Responsibilities (anticipate the downstream role/handoff), hard-won experience in Hard limits — without changing `One job` or adding any capability (D-03). For orchestrator.md: preserve the `> Kit vs state invariant` banner, the Routing matrix, the WIP/DoR gate, the XL-split, and the workflow-mapping table verbatim in structure (deepen prose judgment in/around them, do not delete or restructure them — they are the legitimate outlier extras); keep `/grug` on line 35 as-is; keep the clear-voice safety hard limits plain English. Encode seniority as compression not expansion (D-04): orchestrator must stay ≤ ~6664 B (baseline 6286 + ~6%), the other 3 flat-or-smaller vs their baselines. Keep the `## Caveman prompt` block, all contract sections, pointers, kit-vs-state refs, and the AGENTS.md footer. No new section, no generic quality enumeration (D-01).
  </action>
  <verify>
    <automated>for f in orchestrator system-analyst qe-e2e uat-planner; do c=$(grep -c '^## Caveman prompt' agent-factory/roles/$f.md); [ "$c" -eq 1 ] || { echo "FAIL caveman count $f=$c"; exit 1; }; done; grep -q 'Kit vs state invariant' agent-factory/roles/orchestrator.md || { echo "FAIL orchestrator lost kit-vs-state banner"; exit 1; }; grep -qi 'Never merge\|never deploy' agent-factory/roles/orchestrator.md || { echo "FAIL orchestrator lost clear-voice safety"; exit 1; }; b=$(wc -c < agent-factory/roles/orchestrator.md | tr -d ' '); [ "$b" -le 6664 ] || { echo "FAIL orchestrator bloated $b"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Caveman prompt' <role>` == 1 and `grep -c '^## One job' <role>` == 1 for all 4
    - orchestrator.md retains its `> Kit vs state invariant` banner, Routing matrix, WIP/DoR gate, XL-split, workflow-mapping table, and clear-voice safety hard limits
    - orchestrator.md ≤ 6664 B; system-analyst ≤2659, qe-e2e ≤3051, uat-planner ≤2980 — flat-or-smaller (D-04)
    - SPOT-REVIEW (prose-judgment): senior depth landed; orchestrator routing/gate logic unchanged in substance
  </acceptance_criteria>
  <done>4 role files rewritten to senior persona in place; orchestrator outlier structure + safety lines preserved; flat-or-smaller bytes.</done>
</task>

<task type="auto">
  <name>Task 2: Senior rewrite — installer, software-engineer, release-manager, security-nfr (incl. 2 clear-voice safety roles)</name>
  <files>agent-factory/roles/installer.md, agent-factory/roles/software-engineer.md, agent-factory/roles/release-manager.md, agent-factory/roles/security-nfr.md</files>
  <read_first>
    - agent-factory/roles/installer.md (the file being rewritten; clear-voice additive/never-overwrite/dry-run/uninstall hard limit — keep plain English)
    - agent-factory/roles/software-engineer.md (the file being rewritten; clear-voice no-fake-results hard limit — keep plain English)
    - agent-factory/roles/release-manager.md (the file being rewritten; clear-voice human deploy-gate — keep plain English, it is the SAFE-01 safety line)
    - agent-factory/roles/security-nfr.md (the file being rewritten; in the CURRENT guard_voice scan set — its clear-voice security lines must stay caveman-marker-free; carries the PASS|PASS_WITH_RISKS|BLOCKED result)
    - .planning/phases/11-senior-persona-overhaul/11-PATTERNS.md § "Group A" + § "Shared Patterns > Two-voice discipline"
    - .planning/phases/11-senior-persona-overhaul/11-CONTEXT.md § decisions D-01..D-04
  </read_first>
  <action>
    Rewrite each of the 4 role files in place, holistically top-to-bottom (D-02), within the fixed skeleton (D-01/D-03), same mechanism: deepen senior judgment inside existing sections, preserve `One job` / `## Caveman prompt` / contract sections / pointers / kit-vs-state refs / AGENTS.md footer, compress not expand (D-04, flat-or-smaller). Keep the clear-voice safety lines plain professional English: installer's additive/never-overwrite/dry-run/uninstall hard limit, software-engineer's no-fake-results hard limit, release-manager's named-human deploy gate (SAFE-01), and security-nfr's security/compliance explanation + PASS|PASS_WITH_RISKS|BLOCKED result. security-nfr.md is already in the `guard_voice` scan set — its clear-voice remainder must stay caveman-marker-free after the rewrite. Grug stays confined to the `## Caveman prompt` block + punchy body. No new section, no new capability, no generic quality enumeration (D-01).
  </action>
  <verify>
    <automated>for f in installer software-engineer release-manager security-nfr; do c=$(grep -c '^## Caveman prompt' agent-factory/roles/$f.md); [ "$c" -eq 1 ] || { echo "FAIL caveman count $f=$c"; exit 1; }; done; sh scripts/check-foundation-guards.sh >/tmp/g11-02.log 2>&1 || { echo "FAIL existing guards red"; cat /tmp/g11-02.log; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Caveman prompt' <role>` == 1 and `grep -c '^## One job' <role>` == 1 for all 4
    - `sh scripts/check-foundation-guards.sh` exits 0 — the existing `guard_voice` (which already scans security-nfr) finds NO caveman marker in its clear-voice remainder after the rewrite
    - installer/software-engineer/release-manager safety lines remain clear professional English (release-manager keeps the named-human deploy gate; software-engineer keeps no-fake-results)
    - Each file ≤ baseline + ~6% (installer ≤3166, software-engineer ≤3130, release-manager ≤3922, security-nfr ≤4331) — flat-or-smaller (D-04)
    - SPOT-REVIEW (prose-judgment): senior depth landed; all clear-voice safety surfaces intact
  </acceptance_criteria>
  <done>4 role files rewritten to senior persona in place; clear-voice safety surfaces preserved; existing guard_voice stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer/dev → grugops repo | Same as Plan 11-01: the only actor is a maintainer editing markdown. No web/network/auth/data surface. Threat class is **gate & trace integrity**. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | role rewrite sands off caveman voice / introduces clear-voice "grug" / bloats a role (esp. orchestrator) | mitigate | Plan 11-04's three guards catch all three mechanically; THIS plan holds orchestrator ≤6664 B and keeps security-nfr's clear-voice remainder marker-free (verified by the existing guard_voice run). |
| T-11-03 | Tampering | rewrite normalizes away orchestrator's outlier structure (routing/gate/workflow table) or changes a `One job` | mitigate | `grep -q 'Kit vs state invariant'` + `grep -c '^## One job' == 1` + spot-review; D-03 forbids scope/structure change. |
| T-11-04 | Information Disclosure | a safety hard limit (deploy gate / never-merge / no-fake-results) is softened or caveman-ized during rewrite | mitigate | `grep -qi 'Never merge\|never deploy'` on orchestrator; spot-review release-manager deploy gate + software-engineer no-fake-results stay clear-voice and unweakened (SAFE-01 contract). |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero external packages installed (markdown-only; npm deps Out of Scope). |

Severity LOW (maintainer-side markdown edits, no untrusted input). Honest risk is a softened safety line or a bloated/false-green gate — mitigated by the explicit greps above + Plan 11-04.
</threat_model>

<verification>
- `sh scripts/check-foundation-guards.sh` exits 0 after this plan (existing guards stay green; security-nfr clear-voice remainder marker-free).
- orchestrator.md retains its outlier structure + clear-voice safety hard limits and stays ≤6664 B.
- Each of the 8 files: exactly one `## One job` + one `## Caveman prompt`; flat-or-smaller bytes.
- Spot-review senior depth across the 8 roles per 11-VALIDATION.md Manual-Only Verifications.
</verification>

<success_criteria>
8 of the 16 roles rewritten to senior judgment in place (PERS-01 / D-01..D-04): no new section, scope unchanged, caveman voice + contract sections + pointers + footer preserved, orchestrator outlier structure + all clear-voice safety surfaces intact, flat-or-smaller bytes.
</success_criteria>

<output>
Create `.planning/phases/11-senior-persona-overhaul/11-02-SUMMARY.md` when done
</output>

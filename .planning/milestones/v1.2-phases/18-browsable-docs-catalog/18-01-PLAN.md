---
phase: 18-browsable-docs-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-catalog.ts
  - scripts/generate-catalog.js
  - scripts/generate-catalog.test.ts
  - docs/catalog/README.md
  - package.json
  - .gitattributes
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "Running `node scripts/generate-catalog.js` writes `docs/catalog/README.md` and exits 0 over the real kit."
    - "The catalog contains all 17 roles (incl. frontend-ui) and all 16 workflows (incl. workflow 14 ui-design-to-build and 15 security-audit) — the catalog is the source of truth; ROADMAP SC #3's prose label 15 is a confirmed off-by-one corrected in passing."
    - "The generator self-discovers via `readdirSync` of `agent-factory/roles` and `agent-factory/workflows`, never from the stale `scripts/validate-agent-factory.ts` ROLES/WORKFLOWS arrays."
    - "Name comes from the `# Role:` / `# Workflow:` H1; the one-line summary comes from the first sentence of `## One job` (roles) / `## When to use` (workflows); kind/tier/order/cadence come from frontmatter — read-only, no edits to any of the 32 kit files and no new frontmatter fields (D-01, D-02)."
    - "Roles in `roles/` that are `_`-prefixed are excluded, so `_role-switch-protocol.md` is not catalogued (D-03)."
    - "The single index file `docs/catalog/README.md` carries one roles table and one workflows table, each row linking to its kit source file, with minimal columns (D-04, D-05)."
    - "Summary extraction splits on the literal `\". \"` (period-space) and KEEPS its trailing period — never re-appending one — so incident-responder yields a single clean period and `AGENTS.md` / `OWASP ASVS 5.0` never false-split (D-01, D-03)."
    - "Workflows 12 (release) and 13 (incident) carry `tier: enterprise` and no `cadence`; the generator emits `UNKNOWN - verify` in their cadence cell and never fabricates `cadence: both` (D-09)."
    - "Output is byte-stable: deterministic ordering (workflows by `order` ascending; roles core-then-enterprise, alphabetical within group), literal `\\n` newlines (never `os.EOL`), single trailing newline — two regenerations are byte-identical (D-08)."
    - "`.gitattributes` pins `docs/catalog/README.md text eol=lf` mirroring the existing `*.js eol=lf` precedent, and the generator runs on the D-13 TS foundation: `.ts` compiled to committed `.js`, zero npm runtime deps, `node:` builtins only, clear professional voice on stdout (D-06)."
  artifacts:
    - path: "scripts/generate-catalog.ts"
      provides: "Read-only TS catalog generator (compiled to committed scripts/generate-catalog.js)"
      contains: "readdirSync"
    - path: "scripts/generate-catalog.js"
      provides: "Committed compiled generator the test + freshness gate drive"
    - path: "docs/catalog/README.md"
      provides: "The committed generated catalog — 17 role rows + 16 workflow rows"
      contains: "frontend-ui"
    - path: "scripts/generate-catalog.test.ts"
      provides: "Vitest oracle for DOCS-01 (writes/reproducible/complete-set/fail-closed/no-fabrication)"
    - path: ".gitattributes"
      provides: "LF pin for the committed catalog markdown"
      contains: "docs/catalog/README.md text eol=lf"
    - path: "package.json"
      provides: "generate:catalog script"
      contains: "generate:catalog"
  key_links:
    - from: "scripts/generate-catalog.ts"
      to: "agent-factory/roles + agent-factory/workflows"
      via: "readdirSync of the two source dirs joined to import.meta.dirname parent"
      pattern: "readdirSync"
    - from: "scripts/generate-catalog.ts"
      to: "docs/catalog/README.md"
      via: "writeFileSync(OUT, lines.join(\"\\n\"), \"utf8\") to a fixed literal OUT"
      pattern: "writeFileSync"
---

<objective>
Deliver DOCS-01: a read-only TypeScript generator (`scripts/generate-catalog.ts` → committed `.js`) that self-discovers the finished kit and emits a deterministic, byte-stable `docs/catalog/README.md` — a single index with a roles table and a workflows table, each row linking to its kit source file. Plus its Vitest oracle, the `generate:catalog` package.json script, and the `.gitattributes` LF pin for the committed catalog.

Purpose: Document the completed 17-role / 16-workflow set so a reader can confirm "the whole kit is here" at a glance, with no fabrication and no edits to the kit files — the v1.2 capstone.

Output: `scripts/generate-catalog.ts` + committed `scripts/generate-catalog.js`, `scripts/generate-catalog.test.ts`, the committed `docs/catalog/README.md`, a new `package.json` `generate:catalog` script, and a one-line `.gitattributes` addition. Also: a one-line fix of ROADMAP.md SC #3 "15 workflows" → "16 workflows".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-browsable-docs-catalog/18-CONTEXT.md
@.planning/phases/18-browsable-docs-catalog/18-RESEARCH.md
@.planning/phases/18-browsable-docs-catalog/18-PATTERNS.md
@.planning/phases/18-browsable-docs-catalog/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): generate-catalog.test.ts — the DOCS-01 oracle, authored first</name>
  <files>scripts/generate-catalog.test.ts</files>
  <read_first>
    - `scripts/generate-asvs-checklist.test.ts` — the exact child-CLI Vitest idiom to clone: `spawnSync("node", [GEN_JS])`, assert `status`/`stdout`/bytes, `afterAll` temp cleanup, and the hermetic `mkdtempSync` + `mkdirSync`/`cpSync` mirror for the fail-closed case (lines 89-119). Drive the COMMITTED `.js`, never the `.ts`.
    - `scripts/freshness.test.ts` — the `afterEach` restore-original-bytes idiom and the exit-code-as-signal contract.
    - `.planning/phases/18-browsable-docs-catalog/18-PATTERNS.md` § "scripts/generate-catalog.test.ts" — the harness scaffold + the 5 test idioms (one per RESEARCH Test Map row) to mirror verbatim.
    - `.planning/phases/18-browsable-docs-catalog/18-VALIDATION.md` § Per-Task Verification Map + Wave 0 Requirements — the DOCS-01 behavior rows this test file covers.
    - `agent-factory/roles/incident-responder.md` (the single-sentence `## One job` double-period edge) and `agent-factory/workflows/12-release.md` + `13-incident.md` (the D-09 `tier: enterprise` no-`cadence` edge) — so the no-fabrication and summary assertions target real edge content.
    - `vitest.config.ts`, `tsconfig.json` (note `exclude: **/*.test.ts` — test files are not compiled; assert by spawning the committed `.js`).
  </read_first>
  <action>
    Create `scripts/generate-catalog.test.ts` mirroring `generate-asvs-checklist.test.ts`'s scaffold: `import { describe, it, expect, afterAll } from "vitest"`, `spawnSync` from `node:child_process`, `mkdtempSync`/`mkdirSync`/`cpSync`/`rmSync`/`readFileSync`/`writeFileSync`/`existsSync` from `node:fs`, `tmpdir` from `node:os`, `join` from `node:path`. Constants: `ROOT = join(import.meta.dirname, "..")`, `GEN_JS = join(ROOT, "scripts", "generate-catalog.js")`, `OUT = join(ROOT, "docs", "catalog", "README.md")`, `tmpDirs: string[]` collected and removed in `afterAll`.

    Write these tests (the committed `.js` does not exist yet — this file ships RED until Task 2 builds it; that is correct Wave-0 sequencing):
    1. Writes + exits 0 over the real kit, leaves the tree clean: capture `before = readFileSync(OUT)`, spawn, assert `status === 0`, assert regenerated bytes equal `before` (parity → no-op), restore in `finally`.
    2. Byte-reproducible: spawn twice, assert `second.equals(first)` AND `second.equals(before)`.
    3. Complete set: assert the output text contains all 17 role display names (incl. `frontend-ui`) and all 16 workflow display names (incl. workflow 14 and 15); assert the roles table has exactly 17 data rows and the workflows table exactly 16 data rows (count via a row-matching regex over the table bodies, not raw `.` counts).
    4. Fail-closed: hermetic mirror per `generate-asvs-checklist.test.ts:89-119` — `mkdtempSync` + `mkdirSync`/`cpSync` the generator `.js` plus a TAMPERED `agent-factory/roles` (one role file with its `# Role:` H1 line stripped) and `agent-factory/workflows` into `<tmp>`; plant a SENTINEL at `<tmp>/docs/catalog/README.md`; spawn the mirrored `.js`; assert `status === 1` AND the sentinel survives unchanged (no partial write).
    5. No fabrication: assert the workflows 12 (release) and 13 (incident) rows show `UNKNOWN - verify` in their cadence cell and the catalog contains NO `cadence: both` (and no fabricated `both`) for those two rows; also assert no `..` (double-period) appears anywhere in the file (the incident-responder edge).

    Use literal expected names where load-bearing; do not paste implementation. Do not author the generator here — Task 2 owns it.
  </action>
  <acceptance_criteria>
    - `scripts/generate-catalog.test.ts` exists and imports vitest + `node:` builtins only (no npm test-deps beyond vitest).
    - The file contains the 5 test idioms above, each driving `node scripts/generate-catalog.js` (the committed `.js`), never the `.ts`.
    - The fail-closed test uses the hermetic `mkdtempSync` + `cpSync` mirror (asserts `status === 1` + sentinel unchanged), never touching the real tree.
    - `npx vitest run scripts/generate-catalog.test.ts` executes (it is expected RED until Task 2 builds the generator).
  </acceptance_criteria>
  <done>
    The DOCS-01 oracle exists, covers all 5 RESEARCH Test-Map behaviors (writes/reproducible/complete-set/fail-closed/no-fabrication), and drives the committed `.js`. Ships RED pending Task 2.
  </done>
  <verify>
    <automated>npx vitest run scripts/generate-catalog.test.ts 2>&1 | grep -Ev '^#' | grep -q 'generate-catalog' && echo "test file runs"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generate-catalog.ts — the read-only deterministic generator + committed catalog + scripts/.gitattributes wiring</name>
  <files>scripts/generate-catalog.ts, scripts/generate-catalog.js, docs/catalog/README.md, package.json, .gitattributes</files>
  <behavior>
    - Self-discovers 17 roles (skip `_`-prefixed) + 16 workflows via `readdirSync`; emits `docs/catalog/README.md` and exits 0 over the real kit.
    - Two regenerations are byte-identical and equal the committed bytes.
    - Catalog contains all 17 role names + all 16 workflow names, with exactly 17 + 16 data rows.
    - A kit file with no `# Role:`/`# Workflow:` H1 (or empty frontmatter) → `exit(1)`, no partial write.
    - Workflows 12/13 cadence cell reads `UNKNOWN - verify`; never `cadence: both`. No `..` anywhere.
  </behavior>
  <read_first>
    - `scripts/generate-asvs-checklist.ts` — the structural template to clone: imports + fixed-literal `ROOT = join(import.meta.dirname, "..")` + literal SRC/OUT (lines 35-50), the `fail()` helper (lines 59-62), the `lines: string[]` → `writeFileSync(OUT, lines.join("\n"), "utf8")` + single trailing newline + `process.exit(0)` emit (lines 116-180), and the clear-voice final `console.log`.
    - `.planning/phases/18-browsable-docs-catalog/18-PATTERNS.md` § "scripts/generate-catalog.ts" — the retargeted constants, the `parseFrontmatter` slice+regex helper, the H1-name regex, the `firstSentence` helper (Pattern 4 — the subtle one), the D-08 sort code, and the emit shape. Mirror these verbatim.
    - `.planning/phases/18-browsable-docs-catalog/18-RESEARCH.md` § Pattern 2/3/4 + Pitfalls 1-5 + Code Examples (readdirSync, ordering, no-fabrication) — the parse-rule ground truth.
    - `agent-factory/roles/incident-responder.md` + `agent-factory/roles/agents-md-scribe.md` — the two summary edge cases (single-sentence double-period trap; `AGENTS.md` period-in-content). `agent-factory/workflows/12-release.md` + `13-incident.md` + `15-security-audit.md` — the D-09 no-`cadence` edge + the `OWASP ASVS 5.0` period-in-content.
    - `scripts/validate-agent-factory.ts` (lines ~116-173) — the STALE ROLES/WORKFLOWS arrays. Read ONLY to confirm: do NOT import or read from these (Pitfall 5). Generate from the directory.
    - `.gitattributes` (existing `*.js eol=lf` lines 10-12) + `tsconfig.json` (`"newLine": "lf"`, `include` already covers `scripts/**/*.ts`) + `package.json` (`scripts` block, the `freshness` line as the model).
  </read_first>
  <action>
    Create `scripts/generate-catalog.ts` as a structural twin of `generate-asvs-checklist.ts`. Concrete decisions to implement:

    Fixed literal paths (path-traversal mitigation, ASVS V12 — never argv/env/content-derived): `const ROOT = join(import.meta.dirname, "..")`, `const ROLES_DIR = join(ROOT, "agent-factory/roles")`, `const WORKFLOWS_DIR = join(ROOT, "agent-factory/workflows")`, `const OUT = join(ROOT, "docs/catalog/README.md")`. Keep the `fail(m)` helper (`console.error` + `process.exit(1)`) for every structural miss.

    Source discovery: `readdirSync(ROLES_DIR).filter(f => f.endsWith(".md") && !f.startsWith("_")).sort()` (D-03 drops `_role-switch-protocol.md` → 17 roles) and `readdirSync(WORKFLOWS_DIR).filter(f => f.endsWith(".md")).sort()` (16 files `00`-`15`). NEVER read the stale validator arrays (Pitfall 5).

    Per-file parse, read-only (D-01): `parseFrontmatter(text)` via `text.match(/^---\n([\s\S]*?)\n---\n/)` then per-line `/^([A-Za-z_]+):\s*(.*)$/` into a flat `Record<string,string>` (stdlib slice+regex — NO `js-yaml`/`gray-matter`, zero npm deps). Name via `text.match(/^# (?:Role|Workflow): (.+)$/m)` → `null` is a fail-closed signal. Summary via `firstSentence`: take the first non-empty line of the `## One job` (roles) / `## When to use` (workflows) section body, find `line.indexOf(". ")`, return `dot === -1 ? line : line.slice(0, dot + 1)` — split on the literal `". "` (period-space), KEEP its period, NEVER re-append `.` (D-01/D-03: incident-responder single-sentence trap + `AGENTS.md` / `OWASP ASVS 5.0` period-in-content trap). A missing name or empty frontmatter where required ⇒ `fail(...)` BEFORE any `writeFileSync` (no partial write).

    Ordering (D-08, mandatory for the byte-diff): roles `.sort((a,b) => a.tier !== b.tier ? (a.tier === "core" ? -1 : 1) : a.name.localeCompare(b.name))` (core group first, then enterprise, alphabetical within each); workflows `.sort((a,b) => a.order - b.order)` (numeric `order` 0-15, unique).

    Emit: build a `lines: string[]`. First line is a provenance banner (recommended, Claude's Discretion): `<!-- GENERATED — do not hand-edit. Re-run: node scripts/generate-catalog.js -->`. Roles table columns: name · tier (`core`/`enterprise`) · one-line summary · source link (e.g. `agent-factory/roles/orchestrator.md`). Workflows table columns: name · order · cadence · one-line summary · source link — for workflows 12/13 the cadence cell is the literal `UNKNOWN - verify` (D-09 — read `cadence` from frontmatter; if absent, emit `UNKNOWN - verify`, NEVER fabricate `both`). End with one `lines.push("")` so `lines.join("\n")` yields exactly one trailing newline; emit literal `"\n"` only (NEVER `os.EOL`, Pitfall 3). `writeFileSync(OUT, lines.join("\n"), "utf8")`. Final clear-voice `console.log` naming counts + OUT (e.g. `generate-catalog: wrote 17 roles and 16 workflows to ...`), then `process.exit(0)`. All stdout is clear professional voice, NOT caveman (CLAUDE.md build-safety hard rule); the catalog content is documentation = clear voice too.

    Then wire it up:
    - Add to `package.json` `scripts`: `"generate:catalog": "tsc --outDir .tmp-build && node scripts/generate-catalog.js"` (mirrors the `freshness` line — build then run the committed `.js`). Add NO devDependencies or dependencies.
    - Add one line to `.gitattributes`: `docs/catalog/README.md text eol=lf` (same rationale as the existing `.js` pins — keeps the freshness byte-diff deterministic cross-platform).
    - Run `npm run build` (compile `.ts` → committed `scripts/generate-catalog.js`), then `npm run generate:catalog` to produce and commit `docs/catalog/README.md`. The committed `.js` is auto-covered by the existing `npm run freshness` gate (`scripts/` is in its `OUTPUT_DIRS`).
    - ROADMAP.md Phase 18 SC #3 was already corrected to "all 16 workflows" during planning (the prior "15" was an off-by-one prose label; the catalog is the source of truth). Do NOT re-edit ROADMAP.md here — just confirm the generated catalog actually emits 16 workflow rows so the doc and the artifact agree.

    Do NOT add per-entry detail pages, do NOT split into roles.md/workflows.md (D-04), do NOT edit any of the 32 kit files, do NOT add frontmatter fields (D-01).
  </action>
  <acceptance_criteria>
    - `node scripts/generate-catalog.js` exits 0 and writes `docs/catalog/README.md`; `docs/catalog/README.md` contains the text `frontend-ui`, `14-ui-design-to-build`-named workflow, and `15-security-audit`-named workflow.
    - `docs/catalog/README.md` has exactly 17 role data rows and exactly 16 workflow data rows.
    - Running the generator twice produces byte-identical output (`scripts/generate-catalog.test.ts` reproducibility test passes).
    - The workflows 12/13 cadence cells read `UNKNOWN - verify`; the file contains no `cadence: both` for them and no `..` anywhere.
    - `.gitattributes` contains the line `docs/catalog/README.md text eol=lf`; `package.json` `scripts` has a `generate:catalog` entry; no new entries under `dependencies`/`devDependencies`.
    - `npx vitest run scripts/generate-catalog.test.ts` exits 0 (all 5 tests green).
    - `npm run freshness` exits 0 (the committed `generate-catalog.js` matches a fresh `tsc` rebuild).
    - ROADMAP.md Phase 18 SC #3 reads "16 workflows" (already corrected during planning; this plan only confirms the catalog emits 16 workflow rows so artifact and doc agree).
  </acceptance_criteria>
  <done>
    The generator, its committed `.js`, the committed `docs/catalog/README.md`, the `generate:catalog` script, and the `.gitattributes` pin all exist; the DOCS-01 oracle is fully green; freshness passes; the catalog emits 16 workflow rows matching the (already-corrected) ROADMAP SC #3.
  </done>
  <verify>
    <automated>npm run build >/dev/null 2>&1 && node scripts/generate-catalog.js >/dev/null 2>&1 && npx vitest run scripts/generate-catalog.test.ts >/dev/null 2>&1 && npm run freshness >/dev/null 2>&1 && grep -q 'docs/catalog/README.md text eol=lf' .gitattributes && grep -q 'generate:catalog' package.json && echo PASS</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem read → generator | The generator reads the project's own committed, byte-ceiling-guarded kit files. No network, no external/untrusted input. |
| generator → committed write | A single `writeFileSync` to a fixed literal `docs/catalog/README.md`. |
| generator → temp dir (tests only) | The fail-closed test mirrors into a `mkdtempSync` dir, cleaned up in `afterAll`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | generate-catalog.ts read/write paths | mitigate | Fixed literal SRC/OUT joined to `import.meta.dirname` parent; never derived from argv/env/file content (mirrors generate-asvs-checklist.ts). No path traversal, no arbitrary write. |
| T-18-02 | Tampering | partial/garbled catalog on a parse miss | mitigate | Build the full `lines[]` first; only `writeFileSync` on success. A missing `# Role:`/`# Workflow:` H1 or empty required frontmatter ⇒ `fail()` + `exit(1)` WITHOUT writing — proven by the hermetic fail-closed test (sentinel survives). |
| T-18-03 | Information disclosure / fabrication | workflows 12/13 absent cadence | mitigate | Emit `UNKNOWN - verify` for a genuinely absent field; never fabricate `cadence: both` (D-09). No-fabrication test asserts this. |
| T-18-04 | DoS (minor) | temp-dir leak in tests | accept→mitigate | `mkdtempSync` unique per run + `rmSync(..., {recursive,force})` in `afterAll`. Test-only surface; negligible blast radius. |
| T-18-05 | DoS (negligible) | oversized/malicious kit file | accept | Inputs are the project's own committed, Phase-10/11 byte-ceiling-guarded files. No external input. Out of proportionate scope for a read-only generator. |
</threat_model>

<verification>
- `npm run build` compiles `.ts` → committed `.js` cleanly (noEmitOnError true).
- `npx vitest run scripts/generate-catalog.test.ts` — all 5 DOCS-01 behaviors green.
- `npm run freshness` — the committed `generate-catalog.js` matches a fresh `tsc` rebuild (zero drift).
- `npm run generate:catalog` is a byte-identical no-op against the committed `docs/catalog/README.md`.
- Manual (non-blocking, per 18-VALIDATION.md): open `docs/catalog/README.md` in a markdown viewer; confirm 17 role rows + 16 workflow rows, each source link resolves.
</verification>

<success_criteria>
- DOCS-01 met: a stdlib-only (zero npm deps) TS generator produces the committed `docs/catalog/README.md` of all 17 roles + 16 workflows from frontmatter + body sections; generated-not-hand-maintained; `UNKNOWN - verify` over invention.
- The catalog reflects the finished set (frontend-ui + workflows 14 and 15 present); ROADMAP SC #3 corrected to "16 workflows".
- Output is deterministic + byte-reproducible (the freshness gate in Plan 02 can byte-diff against it).
</success_criteria>

<output>
Create `.planning/phases/18-browsable-docs-catalog/18-01-SUMMARY.md` when done.
</output>

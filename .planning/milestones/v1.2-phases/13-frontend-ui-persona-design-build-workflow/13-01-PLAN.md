---
phase: 13-frontend-ui-persona-design-build-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/roles/frontend-ui.md
  - agent-factory/handoffs/frontend-handoff.md
  - scripts/check-foundation-guards.sh
  - scripts/check-foundation-guards.test.sh
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "A 17th role file frontend-ui.md exists on the standard 9-section role skeleton"
    - "frontend-ui.md grants no spawn tool (no Agent/Task in any frontmatter or footer) — WR-05 guard stays GREEN"
    - "frontend-ui.md is a design-authority/contract role: it authors the UI/design contract, it does not write component code (D-01) and does not re-activate to review (D-03)"
    - "frontend-ui.md is framework-neutral with Vue as the worked example (D-02) and introduces no new config key (D-07)"
    - "A frontend-handoff.md template carries the design contract: design tokens, component inventory, five-states acceptance, the WCAG 2.2 AA bar, and a tool-neutral visual-baseline expectation"
    - "frontend-ui.md is registered in both guard scan sets (ROLE_FILES + role_ceiling case + GUARD_INPUTS) and both guard scripts pass GREEN"
  artifacts:
    - path: "agent-factory/roles/frontend-ui.md"
      provides: "The 17th role — senior frontend/UI design-authority persona, 9-section skeleton, no spawn (UI-01)"
      contains: "kind: role"
    - path: "agent-factory/handoffs/frontend-handoff.md"
      provides: "The design-contract handoff template → instance plans/handoffs/<TICKET-ID>-frontend.md (D-06)"
      contains: "stage: frontend"
    - path: "scripts/check-foundation-guards.sh"
      provides: "frontend-ui.md registered in ROLE_FILES with a matching role_ceiling() case (Pitfall 1)"
      contains: "frontend-ui.md"
    - path: "scripts/check-foundation-guards.test.sh"
      provides: "frontend-ui.md registered in the GUARD_INPUTS mirror manifest (Pitfall 5)"
      contains: "frontend-ui.md"
  key_links:
    - from: "agent-factory/roles/frontend-ui.md"
      to: "agent-factory/handoffs/frontend-handoff.md"
      via: "## Output names the template it fills and the <TICKET-ID>-frontend.md instance"
      pattern: "frontend-handoff.md"
    - from: "agent-factory/roles/frontend-ui.md"
      to: "agent-factory/roles/_role-switch-protocol.md"
      via: "no-spawn activation reference (footer / Activates when)"
      pattern: "_role-switch-protocol"
    - from: "scripts/check-foundation-guards.sh"
      to: "agent-factory/roles/frontend-ui.md"
      via: "ROLE_FILES entry + role_ceiling() case"
      pattern: "frontend-ui.md"
---

<objective>
Author the 17th role — `agent-factory/roles/frontend-ui.md`, a senior frontend/UI **design-authority** persona on the standard 9-section skeleton with **no spawn tool** — and its design-contract carrier `agent-factory/handoffs/frontend-handoff.md`. Then register the new role in both foundation-guard scan sets so all six guards cover it and ship GREEN. This satisfies **UI-01** and lays the contract artifact the workflow (Plan 02) and orchestrator (Plan 03) wire to.

This plan owns **guard trap 1**: adding the role to `ROLE_FILES` WITHOUT a matching `role_ceiling()` case is a guaranteed `guard_role_size` FAIL ("no documented ceiling"). The role MUST be authored, byte-measured, then given its ceiling case — all in this plan, leaving both guard scripts GREEN.

Purpose: closes SDLC-audit GAP-3 (no frontend/UI specialist) by giving grugops a design-authority persona that authors the UI contract the engineer builds against and QE verifies — mirroring the Phase-12 BDD contract→build→verify seam, one behavior one owner (D-01/D-03/D-04).
Output: `frontend-ui.md` (role), `frontend-handoff.md` (template), and the paired guard registrations across `check-foundation-guards.sh` + `check-foundation-guards.test.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-CONTEXT.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-RESEARCH.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-PATTERNS.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author frontend-ui.md (the 17th role) + frontend-handoff.md (the contract carrier)</name>
  <files>agent-factory/roles/frontend-ui.md, agent-factory/handoffs/frontend-handoff.md</files>
  <read_first>
    - agent-factory/roles/qe-e2e.md — the closest-analog role; copy its exact 9-section skeleton ordering (One job → Caveman prompt → Reads → Activates when → Responsibilities → Output → Board moves → Trace updates → Hard limits → AGENTS.md footer), the frontmatter `kind: role` / `tier: core`, the `## Output` template→instance line shape, and the seam-line + footer pattern at lines 43-48.
    - agent-factory/handoffs/qe-handoff.md — the terse body-field style and the universal-header block frontend-handoff.md mirrors (copy the universal header verbatim, change only `stage:` + `# Handoff:` title).
    - agent-factory/handoffs/universal-handoff.md — the `## Source … ## Next action` header every handoff prepends, including `## Scope`/`### In scope`/`### Out of scope`/`## Risks`.
    - agent-factory/handoffs/product-handoff.md — the `## Acceptance scenarios (Given/When/Then)` block (line 36) the contract is DERIVED FROM (D-10); frontend-handoff READS it, does not re-create it.
    - agent-factory/checklists/accessibility-checklist.md — the existing a11y item hub (names "e.g. WCAG 2.2 AA" at line 15); the role/handoff REFERENCE it for items rather than re-enumerating (single-source, P4).
    - .planning/phases/13-frontend-ui-persona-design-build-workflow/13-PATTERNS.md — § "frontend-ui.md" + § "frontend-handoff.md" carry the verbatim excerpts and the discretion-locked field set.
  </read_first>
  <action>
    Author `agent-factory/roles/frontend-ui.md` on the qe-e2e 9-section skeleton, terse (target the qe-e2e/software-engineer band ~2.6-3.3KB). The persona is **framework-neutral with Vue as the worked example** (D-02) — principles travel across React/Svelte/Vue; name Vue only as the concrete example, never as a hard requirement. Frontmatter `kind: role` / `tier: core`; title `# Role: Frontend/UI`. NO `tools:`/`allowed-tools:` key and NO `Agent`/`Task` anywhere (WR-05 stays GREEN — the role grants nothing). The `## Caveman prompt` fenced block carries ≥4 clipped `^You` imperatives (e.g. `You are Frontend/UI.` / `You author the design contract.` / `You do not write the code.` / `You name the five states.` / `You set the accessibility bar.`) so `guard_caveman_preserved` passes (≥2 `^You` lines — Pitfall 4). `## Reads`: config-first (honor the existing `quality` dial; introduce NO new config key — D-07), then board/index, then the inputs per D-10 — the product handoff `## Acceptance scenarios`, the implementation-ready packet, and `architecture-handoff.md` when present — citing the universal-header `## Scope`/`## Risks`. `## Activates when`: `Need UI/frontend work.` (match the routing-matrix wording Plan 03 adds). `## Output` mirrors qe-e2e's template→instance line: read `frontend-handoff.md` from `agent-factory/handoffs/`, fill per ticket (design tokens, component inventory, five-states acceptance, WCAG 2.2 AA bar, visual-baseline expectation), write `plans/handoffs/<TICKET-ID>-frontend.md`. Encode the D-01/D-03 seam in `## Responsibilities` + `## Hard limits`: it owns the **design contract only** — it does NOT write component code (the engineer builds, see workflow `04-ticket-to-pr.md`) and does NOT re-activate to review (QE verifies at the gate, see workflow `05-pr-quality-gate.md`); single activation. Two-voice discipline (Pitfall 3): caveman in the fence + punchy body, but the WCAG 2.2 AA / accessibility / any safety line is **clear professional voice**. End with the exact footer line `Follow the 12 coding rules in \`AGENTS.md\`.`
    Author `agent-factory/handoffs/frontend-handoff.md`: copy the universal-handoff header verbatim with `stage: frontend` and `# Handoff: frontend`; carry `## Scope`/`### In scope`/`### Out of scope`/`## Risks` verbatim. After the universal `---` separator, append the terse contract body fields per PATTERNS § "Recommended frontend-handoff body fields": `## Design tokens` (D-11) · `## Component inventory` (D-11) · `## Five-states acceptance` (loading/empty/error/success/partial-data — the bar QE verifies, D-04) · `## Accessibility bar` (WCAG 2.2 AA — named standard, clear voice, items per `accessibility-checklist.md`, D-09) · `## Responsive / performance budget` · `## Visual-baseline expectation` (tool-neutral, NO Playwright/toHaveScreenshot/axe-core named, D-08) · `## Verification owner` (QE/E2E at the gate 05; frontend-ui does NOT re-verify, D-03). Each field a bare `## Heading` with a terse `# comment`, matching qe-handoff's style. Keep the design-system notion INSIDE this handoff — no separate design-system file (D-11). Do NOT paste fenced code into these — author the files directly per the read_first analogs.
  </action>
  <acceptance_criteria>
    - `test -f agent-factory/roles/frontend-ui.md` succeeds
    - `grep -q 'kind: role' agent-factory/roles/frontend-ui.md`
    - `grep -q '# Role: Frontend/UI' agent-factory/roles/frontend-ui.md`
    - `grep -ciE '(^| )(tools|allowed-tools):.*\b(Agent|Task)\b' agent-factory/roles/frontend-ui.md` returns `0` (no spawn grant)
    - `grep -c '^You ' agent-factory/roles/frontend-ui.md` returns `2` or more (caveman block present + markered)
    - `grep -q 'WCAG 2.2 AA' agent-factory/roles/frontend-ui.md`
    - `grep -q 'frontend-handoff.md' agent-factory/roles/frontend-ui.md` (Output names the template)
    - `grep -q 'Follow the 12 coding rules in `AGENTS.md`.' agent-factory/roles/frontend-ui.md` (exact footer)
    - `test -f agent-factory/handoffs/frontend-handoff.md` succeeds
    - `grep -q 'stage: frontend' agent-factory/handoffs/frontend-handoff.md`
    - `grep -q '# Handoff: frontend' agent-factory/handoffs/frontend-handoff.md`
    - `grep -q 'WCAG 2.2 AA' agent-factory/handoffs/frontend-handoff.md`
    - `grep -qiE 'loading.*empty.*error.*success.*partial|five[- ]states' agent-factory/handoffs/frontend-handoff.md` (five states present)
    - `grep -ciE 'playwright|toHaveScreenshot|axe-core' agent-factory/handoffs/frontend-handoff.md` returns `0` (tool-neutral visual baseline, D-08)
  </acceptance_criteria>
  <verify>
    <automated>test -f agent-factory/roles/frontend-ui.md && grep -q 'kind: role' agent-factory/roles/frontend-ui.md && grep -q 'WCAG 2.2 AA' agent-factory/roles/frontend-ui.md && [ "$(grep -c '^You ' agent-factory/roles/frontend-ui.md)" -ge 2 ] && test -f agent-factory/handoffs/frontend-handoff.md && grep -q 'stage: frontend' agent-factory/handoffs/frontend-handoff.md</automated>
  </verify>
  <done>frontend-ui.md exists on the 9-section skeleton with no spawn grant, a ≥2-line caveman block, the WCAG 2.2 AA clear-voice bar, and the AGENTS.md footer; frontend-handoff.md exists with stage frontend, the universal header, and the five-states/WCAG/tool-neutral-visual-baseline contract fields.</done>
</task>

<task type="auto">
  <name>Task 2: Register frontend-ui.md in both guard scan sets (ROLE_FILES + role_ceiling case + GUARD_INPUTS) and ship both guards GREEN</name>
  <files>scripts/check-foundation-guards.sh, scripts/check-foundation-guards.test.sh</files>
  <read_first>
    - scripts/check-foundation-guards.sh — `ROLE_FILES` (line 186, 16-file alphabetized list), `role_ceiling()` (line 364; note the `*) echo "" ;;` FAIL branch and the +12%/+6% formula comment block), the cosmetic "all 16 roles" pass string (line 329).
    - scripts/check-foundation-guards.test.sh — `GUARD_INPUTS` (line 56, the 16-role mirror manifest).
    - .planning/phases/13-frontend-ui-persona-design-build-workflow/13-PATTERNS.md — § "scripts/check-foundation-guards.sh" edits (a)/(b)/(d)/(e) carry the verbatim list shapes and the measure-then-set formula.
    - .planning/phases/13-frontend-ui-persona-design-build-workflow/13-RESEARCH.md — Pitfall 1 (no-documented-ceiling FAIL) + Pitfall 5 (test-harness mirror).
  </read_first>
  <action>
    Measure the authored role: `wc -c agent-factory/roles/frontend-ui.md`. Compute FAIL = round(bytes × 1.12), WARN = round(bytes × 1.06) (the documented per-file formula). Then make four coupled edits — ALL of them, or the build goes RED:
    (a) Add `agent-factory/roles/frontend-ui.md \` to `ROLE_FILES` in `check-foundation-guards.sh`, slotted after `factory-coach.md` to keep alpha order.
    (b) Add a `role_ceiling()` case `    frontend-ui.md)        echo "<FAIL> <WARN>" ;;  # Phase 13 — 17th role (UI-01)` using the measured FAIL/WARN. This pairs with (a) — registering in ROLE_FILES without this case hits `*) echo "" ;;` and FAILs "no documented ceiling" (trap 1).
    (d) Update the cosmetic pass string at line 329 `all 16 roles` → `all 17 roles` (and the nearby "16-file list" comments for accuracy — non-blocking).
    (e) Add `agent-factory/roles/frontend-ui.md \` to `GUARD_INPUTS` in `check-foundation-guards.test.sh`, after `factory-coach.md` (alpha order). Skipping this FAILs every hermetic mirror case on the missing 17th role (Pitfall 5). No new fixture/case is needed (A2 — existing planted-violation cases already prove each guard fails red).
    Do NOT touch the `orchestrator.md)` ceiling here — that raise belongs to Plan 03 (it must be measured off the post-wiring orchestrator size). Run `sh scripts/check-foundation-guards.sh && sh scripts/check-foundation-guards.test.sh` — both MUST print ALL CHECKS PASSED.
  </action>
  <acceptance_criteria>
    - `grep -q 'agent-factory/roles/frontend-ui.md' scripts/check-foundation-guards.sh` (ROLE_FILES)
    - `grep -qE 'frontend-ui.md\)[[:space:]]*echo' scripts/check-foundation-guards.sh` (role_ceiling case present)
    - `grep -q 'agent-factory/roles/frontend-ui.md' scripts/check-foundation-guards.test.sh` (GUARD_INPUTS mirror)
    - `grep -q 'all 17 roles' scripts/check-foundation-guards.sh` (cosmetic count updated)
    - `sh scripts/check-foundation-guards.sh` exits 0 (ALL CHECKS PASSED — covers guard_voice/guard_caveman_preserved/guard_role_size/guard_wr05 for frontend-ui.md)
    - `sh scripts/check-foundation-guards.test.sh` exits 0 (ALL CHECKS PASSED — the fail-proof harness mirrors the 17th role)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'agent-factory/roles/frontend-ui.md' scripts/check-foundation-guards.sh && grep -qE 'frontend-ui.md\)[[:space:]]*echo' scripts/check-foundation-guards.sh && grep -q 'agent-factory/roles/frontend-ui.md' scripts/check-foundation-guards.test.sh && sh scripts/check-foundation-guards.sh && sh scripts/check-foundation-guards.test.sh</automated>
  </verify>
  <done>frontend-ui.md is registered in ROLE_FILES with a paired role_ceiling() case computed off its measured bytes, mirrored in GUARD_INPUTS, the cosmetic count reads "all 17 roles", and both guard scripts exit 0 GREEN. The orchestrator ceiling is untouched (Plan 03 owns it).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role activation → host agent | The new role must not grant a spawn tool; single-window sequential role-load is the portability + accountability invariant (CLAUDE.md hard safety constraint) |
| guard pass-string → reviewer trust | A guard that can only pass is a fabricated gate; the fail-proof harness must still catch real violations for the 17th role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Elevation of Privilege | frontend-ui.md frontmatter/footer | mitigate | No `tools:`/`allowed-tools:` key, no `Agent`/`Task` token anywhere; role references `_role-switch-protocol.md` (no-spawn). Verified by `guard_wr05` GREEN + the grep acceptance criterion returning 0. |
| T-13-02 | Tampering/Repudiation | check-foundation-guards.sh (the 17th-role registration) | mitigate | The fail-proof harness `check-foundation-guards.test.sh` plants real violations and asserts each guard fails red; the GUARD_INPUTS mirror addition keeps the harness honest for the new role. No guard is allowed to be pass-only. |
| T-13-03 | (trust integrity) | the WCAG 2.2 AA / accessibility lines | mitigate | Two-voice discipline — clear professional voice on the a11y/safety lines; `guard_voice` scans frontend-ui.md once it joins ROLE_FILES and fails on a caveman-voice leak. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — grugops adds no npm/runtime deps (CLAUDE.md hard constraint; RESEARCH "no Package Legitimacy Audit required"). The phase authors markdown + edits read-only POSIX-sh guards (grep/wc/awk/test, no writes, no network). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages exist to gate. |
</threat_model>

<verification>
- `sh scripts/check-foundation-guards.sh` exits 0 (all 6 guards GREEN over the tree incl. the new role)
- `sh scripts/check-foundation-guards.test.sh` exits 0 (fail-proof harness GREEN with the 17th role mirrored)
- `grep -ciE '(^| )(tools|allowed-tools):.*\b(Agent|Task)\b' agent-factory/roles/frontend-ui.md` = 0 (no spawn — UI-01)
- `grep -q 'stage: frontend' agent-factory/handoffs/frontend-handoff.md` (handoff convention — D-06)
</verification>

<success_criteria>
- frontend-ui.md exists: 9-section skeleton, no spawn tool, ≥2-line caveman block, WCAG 2.2 AA clear-voice a11y bar, design-authority/contract-only seam (D-01/D-03), framework-neutral Vue example (D-02), no new dial key (D-07), AGENTS.md footer (UI-01, SC1)
- frontend-handoff.md exists: universal header + stage frontend + design tokens / component inventory / five-states acceptance / WCAG 2.2 AA bar / tool-neutral visual baseline (D-06/D-04/D-09/D-11)
- Both guard scripts GREEN — frontend-ui.md registered in ROLE_FILES + role_ceiling case + GUARD_INPUTS (trap 1 closed); orchestrator ceiling untouched (Plan 03 owns it)
</success_criteria>

<output>
Create `.planning/phases/13-frontend-ui-persona-design-build-workflow/13-01-SUMMARY.md` when done
</output>

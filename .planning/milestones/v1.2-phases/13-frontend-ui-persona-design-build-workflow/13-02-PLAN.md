---
phase: 13-frontend-ui-persona-design-build-workflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-factory/workflows/14-ui-design-to-build.md
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "A new workflow 14-ui-design-to-build.md exists with frontmatter order: 14, cadence: both — appended, NOT renumbering the frozen 00-13 ordinals"
    - "Workflow 14 walks the full SC2 sequence: design contract → component build → five states (loading/empty/error/success/partial-data) → accessibility → visual baseline"
    - "Workflow 14 REFERENCES 04-ticket-to-pr.md (build) and 05-pr-quality-gate.md (verify) by filename and does NOT restate their loops (D-03, single-source)"
    - "Workflow 14 names WCAG 2.2 AA as the accessibility bar (D-09) and names NO tools in its body (no Playwright/toHaveScreenshot/axe-core — D-08)"
  artifacts:
    - path: "agent-factory/workflows/14-ui-design-to-build.md"
      provides: "The UI design→build workflow — practice-level, tool-neutral, references 04+05 (UI-02)"
      contains: "order: 14"
  key_links:
    - from: "agent-factory/workflows/14-ui-design-to-build.md"
      to: "agent-factory/workflows/04-ticket-to-pr.md"
      via: "reference-not-restate: the build loop lives in 04"
      pattern: "04-ticket-to-pr.md"
    - from: "agent-factory/workflows/14-ui-design-to-build.md"
      to: "agent-factory/workflows/05-pr-quality-gate.md"
      via: "reference-not-restate: verification lives in 05"
      pattern: "05-pr-quality-gate.md"
    - from: "agent-factory/workflows/14-ui-design-to-build.md"
      to: "agent-factory/roles/_role-switch-protocol.md"
      via: "## Agents involved protocol one-liner (no spawn)"
      pattern: "_role-switch-protocol"
---

<objective>
Author `agent-factory/workflows/14-ui-design-to-build.md` — the UI design→build workflow (**UI-02**). It walks the full SC2 practice sequence (design contract → component build → five states → accessibility → visual baseline) **tool-neutrally**, naming only the one standard WCAG 2.2 AA (D-09), and **references** workflow `04-ticket-to-pr.md` for the build and `05-pr-quality-gate.md` for verification rather than restating their loops (D-03, single-source — the same discipline 04 already uses to reference 05).

The new file is `14-ui-design-to-build.md` with `order: 14` — it **appends**; the frozen 00-13 ordinals must NOT renumber (Pitfall 6). All tooling/automation wiring (Playwright, `toHaveScreenshot`, axe-core, the `quality.ui_e2e` dial) is deferred to Phase 15 — the workflow body names NO tools (D-08).

Purpose: gives grugops's users a repeatable UI build flow that plugs into the existing engineer build (04) + QE gate (05) without duplicating either, and names a concrete a11y acceptance bar the engineer + QE can verify against.
Output: `14-ui-design-to-build.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-CONTEXT.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-RESEARCH.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-PATTERNS.md
@.planning/phases/13-frontend-ui-persona-design-build-workflow/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 14-ui-design-to-build.md (workflow, reference-not-restate, tool-neutral except WCAG 2.2 AA)</name>
  <files>agent-factory/workflows/14-ui-design-to-build.md</files>
  <read_first>
    - agent-factory/workflows/04-ticket-to-pr.md — the build workflow it references; copy the frontmatter shape (`kind: workflow` / `order:` / `cadence: both`), the `## Agents involved` block + the verbatim `_role-switch-protocol.md` one-liner, the VERBATIM reference-not-restate phrasing at line 28 ("…live there — this workflow references that gate and does not restate it"), and the closing `## Board moves` / `## Handoffs produced` / `## Trace updates` / `## Done condition` / `## Commit` sections (lines 32-54, incl. the branch-guard-first / never-merge-never-deploy line).
    - agent-factory/workflows/05-pr-quality-gate.md — the gate it references; line 9 declares 05 the single source of the backpressure loop ("Every other workflow that needs the gate references this file rather than restating the loop"). Mirror its closing sections too.
    - agent-factory/checklists/accessibility-checklist.md — the a11y item hub the workflow's accessibility step points to (single-source; do not re-enumerate items).
    - .planning/phases/13-frontend-ui-persona-design-build-workflow/13-PATTERNS.md — § "14-ui-design-to-build.md" carries the frontmatter, the `## Agents involved` text, the two load-bearing references, and the step-skeleton.
    - .planning/phases/13-frontend-ui-persona-design-build-workflow/13-RESEARCH.md — "Code Examples" § (the recommended literal step text, lines ~263-301) + Open Question 2 (default: NO Phase-15 forward-pointer, stay clean).
  </read_first>
  <action>
    Author `agent-factory/workflows/14-ui-design-to-build.md`, terse, mirroring 04/05. Frontmatter: `kind: workflow`, `order: 14`, `cadence: both` (a delivery workflow, both kanban and scrum — A3); title `# Workflow: UI design to build`. Do NOT rename or renumber any 00-13 file (Pitfall 6) — this appends as 14.
    `## When to use`: when a ticket needs UI/frontend work — a design contract authored once, then built and verified.
    `## Agents involved`: bullet Frontend/UI (authors the UI/design contract → writes `plans/handoffs/<TICKET-ID>-frontend.md`), Software Engineer (builds the components against the contract — see `04-ticket-to-pr.md`), QE/E2E (verifies the built UI against the contract at the gate — see `05-pr-quality-gate.md`); then carry the VERBATIM role-switch one-liner pointing to `agent-factory/roles/_role-switch-protocol.md` (one window, drop prior context, the handoff is the only memory).
    `## Steps` — walk the SC2 sequence: (1) Frontend/UI authors the UI/design contract from the product handoff `## Acceptance scenarios` + the implementation-ready packet (and `architecture-handoff.md` when present) — design tokens, component inventory, the five states (loading/empty/error/success/partial-data), the WCAG 2.2 AA accessibility bar, and a tool-neutral visual-baseline expectation; one activation, no re-review (D-03). (2) The Software Engineer builds against the contract — reference `agent-factory/workflows/04-ticket-to-pr.md` using the exact "the … loop live there — this workflow references that … and does not restate it" phrasing (D-03). (3) Walk the five states as a practice for each component (guidance, not a hard gate — D-08). (4) Meet the accessibility bar **WCAG 2.2 AA** (the ONLY named standard; clear professional voice; point to `agent-factory/checklists/accessibility-checklist.md` for the item list — D-09, single-source). (5) Establish the visual baseline — a stable reference of the rendered component, described **tool-neutrally** (NO Playwright/`toHaveScreenshot`/axe-core/Vitest in the body — D-08). (6) Verify the built UI against the contract per `agent-factory/workflows/05-pr-quality-gate.md` — QE/E2E owns this; reference the gate, do not restate it (D-03).
    Closing sections, terse, mirroring 04/05: `## Board moves` · `## Handoffs produced` (name the new `plans/handoffs/<TICKET-ID>-frontend.md`) · `## Trace updates` · `## Done condition` · `## Commit` (branch-guard-first, never merge / never deploy — humans hold both). Default to NO Phase-15 forward-pointer (OQ2 — stay clean). Author the file directly per the read_first analogs; do not inline 04/05 step labels.
  </action>
  <acceptance_criteria>
    - `test -f agent-factory/workflows/14-ui-design-to-build.md` succeeds
    - `grep -q 'order: 14' agent-factory/workflows/14-ui-design-to-build.md`
    - `grep -q 'cadence: both' agent-factory/workflows/14-ui-design-to-build.md`
    - `grep -qiE 'loading.*empty.*error.*success.*partial|five[- ]states' agent-factory/workflows/14-ui-design-to-build.md` (five states walked)
    - `grep -q 'WCAG 2.2 AA' agent-factory/workflows/14-ui-design-to-build.md` (D-09 bar)
    - `grep -q '04-ticket-to-pr.md' agent-factory/workflows/14-ui-design-to-build.md` (references the build, D-03)
    - `grep -q '05-pr-quality-gate.md' agent-factory/workflows/14-ui-design-to-build.md` (references the gate, D-03)
    - `grep -q '_role-switch-protocol.md' agent-factory/workflows/14-ui-design-to-build.md` (no-spawn activation line)
    - `grep -ciE 'playwright|toHaveScreenshot|axe-core|vitest' agent-factory/workflows/14-ui-design-to-build.md` returns `0` (tool-neutral body, D-08)
    - `ls agent-factory/workflows/ | grep -E '^(00|01|02|03|04|05|06|07|08|09|10|11|12|13)-' | wc -l` returns `14` (00-13 ordinals unchanged — Pitfall 6)
  </acceptance_criteria>
  <verify>
    <automated>test -f agent-factory/workflows/14-ui-design-to-build.md && grep -q 'order: 14' agent-factory/workflows/14-ui-design-to-build.md && grep -q 'WCAG 2.2 AA' agent-factory/workflows/14-ui-design-to-build.md && grep -q '04-ticket-to-pr.md' agent-factory/workflows/14-ui-design-to-build.md && grep -q '05-pr-quality-gate.md' agent-factory/workflows/14-ui-design-to-build.md && [ "$(grep -ciE 'playwright|toHaveScreenshot|axe-core|vitest' agent-factory/workflows/14-ui-design-to-build.md)" -eq 0 ] && [ "$(ls agent-factory/workflows/ | grep -E '^(00|01|02|03|04|05|06|07|08|09|10|11|12|13)-' | wc -l | tr -d ' ')" -eq 14 ]</automated>
  </verify>
  <done>14-ui-design-to-build.md exists with order 14 / cadence both, walks the contract→build→5-states→a11y→visual-baseline sequence tool-neutrally, names WCAG 2.2 AA as the only standard, references 04 + 05 + _role-switch-protocol.md by filename without restating them, names no tools, and the frozen 00-13 ordinals are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow → role activation | The `## Agents involved` block must reference `_role-switch-protocol.md` (no-spawn); the workflow grants nothing and inlines no spawn step |
| a11y/safety prose → reviewer trust | The WCAG 2.2 AA / accessibility line is a clear-voice topic; a caveman-voice leak erodes the bar's credibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Elevation of Privilege | 14-ui-design-to-build.md `## Agents involved` | mitigate | The workflow references `_role-switch-protocol.md` (single-window, no spawn) and inlines no activation/spawn step; no tool grant anywhere. The workflow is not in the WR-05 scan set, but the no-spawn principle is hard-locked. |
| T-13-05 | (trust integrity) | the WCAG 2.2 AA / accessibility step | mitigate | Clear professional voice on the a11y bar (D-09; two-voice discipline). Workflows are not voice-lint-scanned (only ROLE_FILES are), so this is enforced by review per VALIDATION's manual-only row, not a guard. |
| T-13-06 | Tampering (single-source drift) | references to 04/05 | mitigate | Reference-not-restate: 04 + 05 named by filename, their loops never inlined (D-03). Structural greps confirm the references exist; a human confirms the gate loop is not paraphrased (VALIDATION manual-only row). |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — markdown authoring only (CLAUDE.md hard constraint; RESEARCH "no Package Legitimacy Audit required"). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages exist to gate. |
</threat_model>

<verification>
- `grep -q 'order: 14' agent-factory/workflows/14-ui-design-to-build.md` (registered as workflow 14, appended)
- `grep -q '04-ticket-to-pr.md' && grep -q '05-pr-quality-gate.md'` in the workflow (reference-not-restate, D-03)
- `grep -ciE 'playwright|toHaveScreenshot|axe-core|vitest'` = 0 (tool-neutral, D-08); `grep -q 'WCAG 2.2 AA'` (the one named standard, D-09)
- `sh scripts/check-foundation-guards.sh` exits 0 (the new workflow file does not break any guard; guards do not scan workflows for size, but the per-commit run stays GREEN)
</verification>

<success_criteria>
- 14-ui-design-to-build.md walks design contract → component build → five states → accessibility → visual baseline, referencing 05 (and 04) rather than restating them (UI-02, SC2)
- Body is tool-neutral (no Playwright/toHaveScreenshot/axe-core); WCAG 2.2 AA is the only named standard (D-08/D-09)
- Frozen 00-13 workflow ordinals unchanged; the new file is order 14 (Pitfall 6, SC3)
</success_criteria>

<output>
Create `.planning/phases/13-frontend-ui-persona-design-build-workflow/13-02-SUMMARY.md` when done
</output>

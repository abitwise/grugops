---
phase: 16-14-gate-convergence-lint-ui-e2e-test-integrity
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - agent-factory/workflows/05-pr-quality-gate.md
  - AGENTS.md
  - agent-factory/config/factory.config.md
autonomous: true
requirements: [UIQA-02, LINT-02, TINT-01, TINT-02, TINT-03]
must_haves:
  truths:
    - "The single-source gate (05-pr-quality-gate.md) wires the lint step to quality.lint {strict,autofix} — strict:true fails on warnings, autofix:true runs safe autofix then rechecks inside self_fix_attempts, no linter → UNKNOWN - verify non-blocking (D-11/D-12/D-13, LINT-02)."
    - "The gate wires the UI/E2E step to quality.ui_e2e (off | ui-or-critical-path | always) and references the Playwright recipe + axe extension by filename, never restating them (D-06, UIQA-02)."
    - "The gate adds a test-integrity step invoking node tools/grugops/test-skip-integrity.js .grugops/test-skips.md --skip-count <N> where <N> comes from the AGENTS.md skip-count slot (UNKNOWN - verify if absent), branching on exit 0/1/2 — always human-only (D-08/D-14, TINT-01/02)."
    - "The gate encodes the D-08 fix-lane classification (lint = agent-fixable; UI/E2E code/a11y = agent-fixable, visual-baseline = human-only; test-integrity = always human-only), the D-09 short-circuit (human-only fails → BLOCKED_NEEDS_FIX, do NOT consume self_fix_attempts), and the D-10 advisory composition — preserving the three terminal results (TINT-01/02/03)."
    - "AGENTS.md carries a new Test integrity skip-count slot defaulting to UNKNOWN - verify, with per-runner examples as examples only and the never-a-silent-0 rule (D-14)."
    - "No fork into workflows 14/15; no literal §14 anywhere in 05; workflow 14 stays tool-neutral (D-08a); no new config key (Pitfall 6)."
  artifacts:
    - path: "agent-factory/workflows/05-pr-quality-gate.md"
      provides: "The single-source gate with lint/ui_e2e/test-integrity wiring + D-08/D-09/D-10 mapping"
      contains: "test-skip-integrity.js"
    - path: "AGENTS.md"
      provides: "Test integrity skip-count capture slot"
      contains: "Skip-count capture"
    - path: "agent-factory/config/factory.config.md"
      provides: "Clarifying dial→behavior prose (no new rows/keys)"
  key_links:
    - from: "agent-factory/workflows/05-pr-quality-gate.md"
      to: "tools/grugops/test-skip-integrity.js"
      via: "node invocation in the test-integrity gate step"
      pattern: "tools/grugops/test-skip-integrity\\.js"
    - from: "agent-factory/workflows/05-pr-quality-gate.md"
      to: "agent-factory/checklists/playwright-visual-regression-recipe.md"
      via: "reference-by-filename (D-06)"
      pattern: "playwright-visual-regression-recipe\\.md"
    - from: "agent-factory/workflows/05-pr-quality-gate.md"
      to: "agent-factory/checklists/linter-recommendations.md"
      via: "reference-by-filename (D-06)"
      pattern: "linter-recommendations\\.md"
    - from: "agent-factory/workflows/05-pr-quality-gate.md"
      to: "AGENTS.md skip-count slot"
      via: "captured skip count passed as --skip-count <N>"
      pattern: "--skip-count"
---

<objective>
Wire all gate behavior into the single-source `05-pr-quality-gate.md` ONLY — the lint step (D-11/D-12/D-13), the UI/E2E step (D-06/UIQA-02), and a NEW test-integrity step (D-14) invoking the materialized checker from Plan 01 — plus the D-08 fix-lane classification, the D-09 human-only short-circuit, and the D-10 advisory composition, all preserving the bounded `self_fix_attempts` loop and the three terminal results. Add the AGENTS.md skip-count slot (D-14) and clarify the config twin's dial→behavior prose (no new keys). This is the convergence keystone: it ties Plan 01's checker and Plan 02's checklists into the visible gate and satisfies UIQA-02, LINT-02, and the wiring half of TINT-01/02/03.

Purpose: "Audit produces, gate enforces" — the enforcement lands at the visible gate. Single-source governs the gate logic (it stays in 05); the bulky how-to is referenced by filename (D-06). Test-integrity is wired human-only so the agent can never self-author its way past the gate (D-02/D-08).
Output: an extended `05-pr-quality-gate.md`, a new `### Test integrity` slot in `AGENTS.md`, and clarifying prose in `factory.config.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-CONTEXT.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-RESEARCH.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-PATTERNS.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-01-SUMMARY.md
@.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire lint + UI/E2E + test-integrity steps and the D-08/D-09/D-10 mapping into 05</name>
  <files>agent-factory/workflows/05-pr-quality-gate.md</files>
  <read_first>
    - agent-factory/workflows/05-pr-quality-gate.md (whole file, 57 lines) — the seam to extend in place: Step 3 (line 31, the ordered `install -> lint -> typecheck -> unit -> build -> e2e` run, the `UNKNOWN - verify` rule, the `mandatory_gates`/`coverage_threshold`/`ui_e2e` reads); Step 4 (line 32, the bounded `self_fix_attempts` loop); Step 5 (line 33, the three terminal results); the Stop conditions (lines 49-50); the clear-voice opener (line 27).
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-PATTERNS.md (lines 154-178 — the exact extend-in-place guidance for each step + the constraints: clear voice, never write §14, no fork into 14/15).
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-CONTEXT.md (D-06/D-07/D-08a, D-08/D-09/D-10, D-11/D-12/D-13/D-14 — the locked wiring rules).
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-01-SUMMARY.md — confirm the exact materialized checker path/args (tools/grugops/test-skip-integrity.js, --skip-count, exit 0/1/2) authored in Plan 01.
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-02-SUMMARY.md — confirm the exact filenames of the two new checklists to reference.
    - agent-factory/config/factory.config.json lines 33-42 — the `quality` block dial values to read (no new keys).
  </read_first>
  <action>
Extend `05-pr-quality-gate.md` IN PLACE (single-source — no fork into workflows 14/15; clear professional voice; NEVER write the literal "§14" — reference siblings by filename). Three changes:

(1) LINT wiring (D-11/D-12/D-13) — lint is already in the Step-3 sequence and in `mandatory_gates`. State the `quality.lint {strict, autofix}` behavior: `strict:true` → fail-on-warning (the linter's `--max-warnings 0`-style flag); `strict:false` → warnings reported in clear voice, only errors fail; `autofix:true` → run the linter's SAFE autofix first, recheck, then report residue (this autofix runs INSIDE the bounded `self_fix_attempts` loop — agent-fixable per D-08); `autofix:false` → report only. When no linter is configured in the AGENTS.md Lint slot, the lint step records `UNKNOWN - verify` and treats it as NON-BLOCKING/surfaced (D-13) — a repo with no applicable linter can still reach `READY_FOR_HUMAN_REVIEW`. Point to `agent-factory/checklists/linter-recommendations.md` for the per-stack table (D-06).

(2) UI/E2E wiring (D-06/UIQA-02) — the `quality.ui_e2e` dial (`off | ui-or-critical-path | always`) already decides when e2e runs (line 31). State that the UI/E2E step covers Playwright visual regression + axe-core a11y, and point to `agent-factory/checklists/playwright-visual-regression-recipe.md` and the axe extension in `accessibility-checklist.md` (D-07 reference-not-embed) — do NOT inline the recipe. Note the D-08 split here: code/a11y defects (broken locator, axe violation, functional failure) are agent-fixable; a visual-baseline UPDATE is HUMAN-ONLY (updating a baseline to make a red screenshot pass is goalpost-moving).

(3) TEST-INTEGRITY step (NEW, D-14) — add it AFTER unit/e2e (they produce the skip count; RESEARCH Open Q3). It runs `node tools/grugops/test-skip-integrity.js .grugops/test-skips.md --skip-count <N>` where `<N>` is captured from the AGENTS.md `### Test integrity` skip-count slot (Task 2); if the count cannot be determined, the gate records `UNKNOWN - verify` and never a silent 0 (D-14). Branch on exit code: `0` → pass; `1` → a test-integrity finding (the gate blocks); `2` → error (could not run — distinct from a clean fail). This step is ALWAYS HUMAN-ONLY (D-08/TINT-01): the agent may NOT self-author a justification; on exit 1 the gate STOPS and hands to a human who owns `.grugops/test-skips.md`.

Then extend Step 4/5 + Stop conditions with the terminal mapping (D-08/D-09/D-10):
- Fix-lane classification (D-08): lint = agent-fixable; UI/E2E code+a11y = agent-fixable, visual-baseline = human-only; test-integrity = always human-only.
- D-09 short-circuit: a human-only failure (visual-baseline acceptance, or a test-integrity exit-1) goes straight to `BLOCKED_NEEDS_FIX` with the specific reason and does NOT consume `self_fix_attempts`. The bounded loop runs ONLY for agent-fixable failures; exhausting it → `BLOCKED_NEEDS_FIX`. All-pass → `READY_FOR_HUMAN_REVIEW`. `SPLIT_REQUIRED` stays size-driven, unchanged.
- D-10 advisory composition: when `gate_enforcement: advisory`, the pipeline ACTION is downgraded to advice uniformly (including a `test_integrity: block` finding), but the finding is STILL emitted loudly in clear professional voice — the trace stays intact, never silent. The TINT-03 floor forbids silently accepting a hollow suite, NOT a hard pipeline stop.

Preserve the existing three terminal results and the bounded-loop contract verbatim where unchanged.
  </action>
  <verify>
    <automated>grep -q 'tools/grugops/test-skip-integrity.js' agent-factory/workflows/05-pr-quality-gate.md && grep -q 'linter-recommendations.md' agent-factory/workflows/05-pr-quality-gate.md && test "$(grep -c '§14' agent-factory/workflows/05-pr-quality-gate.md)" = "0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '§14' agent-factory/workflows/05-pr-quality-gate.md` returns 0 (no §14 literal in the shipped file — Phase 12 D-12).
    - The gate invokes the materialized checker: `grep -c 'tools/grugops/test-skip-integrity.js' agent-factory/workflows/05-pr-quality-gate.md` returns ≥ 1, and `grep -c -- '--skip-count' agent-factory/workflows/05-pr-quality-gate.md` returns ≥ 1.
    - The gate references both new checklists by filename: `grep -c -E 'playwright-visual-regression-recipe.md|linter-recommendations.md' agent-factory/workflows/05-pr-quality-gate.md` returns 2.
    - The gate wires the lint dial and the ui_e2e dial: `grep -c -E 'quality.lint|strict|autofix' agent-factory/workflows/05-pr-quality-gate.md` returns ≥ 2 AND `grep -c 'ui_e2e' agent-factory/workflows/05-pr-quality-gate.md` returns ≥ 1.
    - The three terminal results are preserved: `grep -c -E 'READY_FOR_HUMAN_REVIEW|BLOCKED_NEEDS_FIX|SPLIT_REQUIRED' agent-factory/workflows/05-pr-quality-gate.md` returns ≥ 3.
    - The human-only short-circuit + advisory composition are present: `grep -c -iE 'human-only|advisory' agent-factory/workflows/05-pr-quality-gate.md` returns ≥ 2.
    - No tool names leaked into workflow 14 (D-08a stays intact): `grep -c -iE 'toHaveScreenshot|axe-core|playwright' agent-factory/workflows/14-ui-design-to-build.md` returns 0.
    - The single-source foundation guards still pass: `node scripts/check-foundation-guards.js` exits 0.
  </acceptance_criteria>
  <done>05-pr-quality-gate.md wires lint/ui_e2e/test-integrity with the D-08/D-09/D-10 mapping, references the two checklists + the materialized checker, preserves the three terminal results, carries no §14 literal, leaves workflow 14 tool-neutral, and passes the foundation guards.</done>
</task>

<task type="auto">
  <name>Task 2: AGENTS.md skip-count slot + config-twin clarifying prose (no new keys)</name>
  <files>AGENTS.md, agent-factory/config/factory.config.md</files>
  <read_first>
    - AGENTS.md lines 37-77 (the `## Commands` section) — especially the `### Acceptance` BDD slot (lines 54-56): the exact slot shape to mirror (heading, an `UNKNOWN - verify` value, an HTML comment listing per-runner examples "never as a hard command").
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-RESEARCH.md (lines 446-458 — the verbatim `### Test integrity` slot shape with the vitest/jest/pytest/go skip-count examples; the never-a-silent-0 rule).
    - .planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-PATTERNS.md (lines 242-260 — the AGENTS.md slot assignment + constraint that the examples are host-owned, never invented commands).
    - agent-factory/config/factory.config.md lines 64-69 and 90-97 — the existing `quality` dial rows + the TINT-03 floor prose (`test_integrity` has no `off`); only ADD clarifying dial→behavior prose, no new rows/keys (Pitfall 6).
    - agent-factory/config/factory.config.json lines 33-42 — confirm every dial already exists (add NONE).
  </read_first>
  <action>
Add a `### Test integrity` slot to `AGENTS.md`'s `## Commands` section (mirror the `### Acceptance` slot shape, lines 54-56) — value `Skip-count capture: \`UNKNOWN - verify\`` with an HTML comment giving the per-runner skip-count examples (vitest `vitest run --reporter=json | jq '.numPendingTests + .numTodoTests'`; jest `jest --json | jq '.numPendingTests'`; pytest / go analogous) as EXAMPLES ONLY (never hard commands), and the rule "If the count cannot be determined, the gate records `UNKNOWN - verify`, never a silent 0." The host owns the real command (no-fabrication, D-14).

In `agent-factory/config/factory.config.md`, ADD only clarifying dial→behavior prose for `quality.lint`/`quality.ui_e2e`/`quality.test_integrity`/`quality.gate_enforcement` — e.g. tie `test_integrity` to the human-owned `.grugops/test-skips.md` registry and the human-only lane, and note `gate_enforcement: advisory` composes with `test_integrity: block` (downgrades the ACTION, still emits the finding loudly — D-10). DO NOT add any new key or row; the TINT-03 floor prose (no `off`) already exists. The registry path is a fixed convention (`.grugops/test-skips.md`, D-01), not a config value — do not add `quality.skip_registry_path` or similar (Pitfall 6).
  </action>
  <verify>
    <automated>grep -q 'Skip-count capture' AGENTS.md && test "$(grep -c '"test_integrity"' agent-factory/config/factory.config.json)" = "1" && VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md carries the new slot: `grep -c 'Skip-count capture' AGENTS.md` returns 1, and it sits under a `### Test integrity` heading.
    - The slot defaults to `UNKNOWN - verify` and lists examples only: `grep -c 'UNKNOWN - verify' AGENTS.md` increases by 1 vs before, and the per-runner examples live in an HTML comment (`grep -c 'never a silent 0' AGENTS.md` returns ≥ 1).
    - No new config key was added: `git diff agent-factory/config/factory.config.json` shows no change (the JSON dials were already present); `grep -c 'skip_registry_path' agent-factory/config/factory.config.json` returns 0.
    - The config twin still documents the TINT-03 floor: `grep -c -E 'never .?off.?|no .?off.?' agent-factory/config/factory.config.md` returns ≥ 1 (the floor prose is preserved).
    - `VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js` exits 0 (AGENTS.md + config still valid; VALIDATE_KIT_ROOT is REQUIRED — the validator exits 1 unconditionally without it).
    - AGENTS.md stays under the Codec byte budget: `node scripts/check-foundation-guards.js` exits 0 (the AGENTS.md byte-budget guard passes).
  </acceptance_criteria>
  <done>AGENTS.md carries the host-owned Test-integrity skip-count slot (examples-only, never-a-silent-0), the config twin clarifies the dial→behavior wiring with zero new keys, and the validator + foundation guards pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → the §14 gate | The agent running the gate is untrusted to clear a test-integrity or visual-baseline failure. |
| AGENTS.md command slot → gate execution | The skip-count command is host-supplied; an absent or fabricated value must fail loud, not silent. |
| config dial → pipeline action | `gate_enforcement: advisory` must not silently swallow a `test_integrity: block` finding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Elevation of Privilege | agent self-authors a justification to clear its own gate | mitigate (structural) | The gate prose wires test-integrity as ALWAYS HUMAN-ONLY (D-08): on exit 1 the gate short-circuits to `BLOCKED_NEEDS_FIX` and hands to a human who owns `.grugops/test-skips.md`; the agent never enters the registry-edit path and the human-only failure does not consume `self_fix_attempts` (D-09). This is the core TINT-01 threat, mitigated by where the step sits in the loop. |
| T-16-03-02 | Elevation of Privilege | agent updates a visual baseline to make a red screenshot pass (goalpost-moving) | mitigate | D-08: visual-baseline acceptance is human-only in the gate prose; only code/a11y UI defects are agent-fixable. |
| T-16-03-03 | Repudiation | a silent-zero skip count hides a hollowed suite | mitigate | D-14: the gate records `UNKNOWN - verify` (never a silent 0) when the AGENTS.md skip-count slot yields no integer; the slot's value defaults to `UNKNOWN - verify`. |
| T-16-03-04 | Repudiation | advisory mode silently swallows a test-integrity finding | mitigate | D-10: `gate_enforcement: advisory` downgrades the ACTION only; the finding is still emitted loudly in clear voice (trace intact, never silent) — encoded in the gate prose. |
| T-16-03-05 | Tampering | gate logic forked into workflows 14/15, or a §14 literal shipped | mitigate | Single-source: all wiring lands in 05; acceptance greps `§14` to 0 in 05 and assert workflow 14 names no UI tool (D-08a stays intact). |
| T-16-03-06 | Tampering | a new config key invented for the registry path or skip behavior | mitigate | Pitfall 6: acceptance asserts no JSON config change and `skip_registry_path` count 0; the registry path is the fixed `.grugops/test-skips.md` convention, not a dial. |
| T-16-03-SC | Tampering | npm/pip/cargo installs | accept | This plan edits only markdown (gate workflow, AGENTS.md, config twin) — it installs nothing. No package-install task; no legitimacy gate required. |
</threat_model>

<verification>
- `grep -c '§14' agent-factory/workflows/05-pr-quality-gate.md` returns 0.
- `grep -c 'tools/grugops/test-skip-integrity.js' agent-factory/workflows/05-pr-quality-gate.md` returns ≥ 1.
- `grep -c -E 'playwright-visual-regression-recipe.md|linter-recommendations.md' agent-factory/workflows/05-pr-quality-gate.md` returns 2.
- `grep -c -E 'READY_FOR_HUMAN_REVIEW|BLOCKED_NEEDS_FIX|SPLIT_REQUIRED' agent-factory/workflows/05-pr-quality-gate.md` returns ≥ 3.
- `grep -c -iE 'toHaveScreenshot|axe-core|playwright' agent-factory/workflows/14-ui-design-to-build.md` returns 0 (D-08a).
- `grep -c 'Skip-count capture' AGENTS.md` returns 1.
- `VALIDATE_KIT_ROOT=. node scripts/validate-agent-factory.js` exits 0 and `node scripts/check-foundation-guards.js` exits 0.
- `npm test` + `npm run freshness` exit 0 (the Plan 01 checker + harness still green; no drift).
</verification>

<success_criteria>
- The lint step is config-dialed via quality.lint {strict,autofix} with UNKNOWN-verify non-blocking when no linter (LINT-02).
- The UI/E2E step is config-dialed via quality.ui_e2e and references the recipe + axe extension by filename (UIQA-02).
- The test-integrity step invokes the materialized checker human-only, captures the skip count from AGENTS.md, branches on 0/1/2 (TINT-01/02).
- The D-08/D-09/D-10 mapping is encoded; the bounded loop + three terminal results preserved; TINT-03 floor honored (advisory composes, never silent).
- All gate changes single-source in 05; no §14 literal; workflow 14 tool-neutral; no new config key.
</success_criteria>

<output>
Create `.planning/phases/16-14-gate-convergence-lint-ui-e2e-test-integrity/16-03-SUMMARY.md` when done.
</output>

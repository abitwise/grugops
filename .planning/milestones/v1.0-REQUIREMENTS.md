# Requirements Archive: v1.0 MVP — Full Agent Factory v2

**Archived:** 2026-06-08 (retroactively, at v1.1 close)
**Status:** SHIPPED 2026-06-04
**Outcome:** 46/46 v1 requirements satisfied. 45 fully complete; **DOG-02 partial** — the portable sequential AGENTS.md path is agent-proven, the live-Claude-Code-session half (sub-agent spawn + dual-path parity) was deliberately deferred to milestone-close UAT (genuinely not agent-runnable; carried forward at v1.1 close — see `.planning/STATE.md` Deferred Items).

For current requirements, see `.planning/REQUIREMENTS.md` (deleted at v1.1 close; a fresh one is created with the next milestone).

---

**Core Value:** A user installs grugops on top of the coding-agent CLI they already run, types `/grug`, and gets a disciplined delivery team — a visible board, strict handoffs, and an auditable requirement→code→test→release trail — entirely as readable markdown, with humans always holding merge and deploy.

> **Scope note:** v1.0 built the **complete Agent Factory v2 spec** (core + enterprise pack + plugin distribution) plus brand/docs collateral, validated by dogfooding. "v1" denotes grugops's *first GSD milestone* — not the factory's lean tier. There were no deferred ("v2") items this milestone; everything in the spec was committed. Requirements marked **[research]** corrected a place where the source spec was stale against 2026 tool docs.

## v1 Requirements

### Structure & Scaffolding

- [x] **STRUCT-01**: Repository scaffolded per spec §3 — `agent-factory/{roles,workflows,handoffs,checklists,examples,packaging,config}`, `plans/{board,traceability,nfr-catalog,metrics,sprints,releases,epics,features,tickets}`, `memory-bank/*`, `install/*`, `.claude-plugin/`, root `AGENTS.md` — without deleting or overwriting existing user content
- [x] **STRUCT-02**: `agent-factory/VERSION` and `agent-factory/README.md` exist; the README explains usage across all five tools and gives copy-paste Orchestrator prompts for bootstrap, refine, plan, sweep, ticket→PR, gate, UAT, and release

### Config Dial

- [x] **CONFIG-01**: `factory.config.json` exists with all required fields (mode, cadence, autonomy, id_prefix, repo_strategy, default_stack, wip_limits, sprint_length_days, sizing, priority_scheme, quality, nfr, compliance_regime, environments, production_requires_human_confirmation, blocked_escalation_days)
- [x] **CONFIG-02**: A human-readable `factory.config.md` twin documents the meaning of every config field
- [x] **CONFIG-03**: The factory runs with ZERO config using documented defaults (`mode=lean`, `cadence=kanban`, `autonomy=pr`); every role reads config first and honors it when present

### Roles

- [x] **ROLE-01**: All 11 core role prompts exist, each following the standard 9-section role skeleton in caveman voice; each reads config first, updates the board on column change, appends to traceability
- [x] **ROLE-02**: All 5 enterprise-pack role prompts exist, activating only when `mode=enterprise` or their trigger fires
- [x] **ROLE-03**: The Orchestrator role encodes the full routing matrix and request-type classification, enforces WIP limits and Definition of Ready, splits XL work, and carries the hard limit that it never merges a protected branch or deploys prod

### Workflows

- [x] **FLOW-01**: Bootstrap workflows (`00-bootstrap-greenfield`, `01-bootstrap-brownfield`) exist and seed AGENTS.md, memory-bank, the repo map/plan, the board, config, and safe first tickets
- [x] **FLOW-02**: Lifecycle workflows exist (`02-idea-to-epics`, `03-epic-to-tickets`, `04-ticket-to-pr`, `05-pr-quality-gate`, `06-uat-pack`)
- [x] **FLOW-03**: Ceremony workflows exist (`07-backlog-refinement`, `08-sprint-planning`, `09-daily-sweep`, `10-sprint-review`, `11-retro`); cadence selected by config
- [x] **FLOW-04**: Enterprise workflows exist (`12-release`, `13-incident`) with the release approval gate and the blameless incident path
- [x] **FLOW-05**: Every workflow file follows the v2 workflow template (When / Agents / Inputs / Steps / Board moves / Handoffs / Trace updates / Metrics emitted / Stop / Done)

### Handoff Templates

- [x] **HAND-01**: All core handoff templates exist and are copy-paste usable; the universal header carries Ticket ID and Trace updates fields
- [x] **HAND-02**: All v2 handoff templates exist (release-handoff, incident-postmortem [blameless], retro-notes, refinement-notes, sprint-plan)

### Checklists

- [x] **CHECK-01**: Lean checklists exist (definition-of-ready, definition-of-done, pr-review, security-nfr, uat)
- [x] **CHECK-02**: Enterprise + new-gate checklists exist (definition-of-done-enterprise superset, compliance, accessibility, observability-slo, release-readiness); Orchestrator applies the right tier by mode

### Delivery OS — Board & Cadence

- [x] **BOARD-01**: `plans/board.md` is the single WIP source of truth with the spec's columns, per-column WIP limits from config, and ticket-status↔column matching
- [x] **BOARD-02**: Kanban (flow) cadence works — continuous pull, WIP throttle, daily-sweep reconciliation, cycle-time focus
- [x] **BOARD-03**: Scrum (sprint) cadence works — time-boxed sprints with `plans/sprints/SPRINT-xx.md` and the full ceremony set, selectable by config
- [x] **BOARD-04**: Sizing (t-shirt→points, XL must split) and priority (P0–P3) shared by both cadences; Blocked policy records `blocked-by` + date and escalates past the config threshold

### Traceability & IDs

- [x] **TRACE-01**: Stable ID schemes defined and used (EPIC-/FEAT-/`<prefix>`-/ADR-/NFR-/RISK-/REL-/INC-) with configurable prefix
- [x] **TRACE-02**: `plans/traceability.md` carries one row per ticket linking requirement→ticket→code→test→UAT→release; enterprise DoD not met until the row is complete

### NFR & Metrics

- [x] **NFR-01**: `plans/nfr-catalog.md` lists non-functional targets (category / target / applies-to / verified-by)
- [x] **METRIC-01**: `plans/metrics.md` tracks throughput, cycle/lead time, WIP, blocked time, rework rate, gate pass rate, escaped defects, and (scrum) velocity

### Backpressure & Safety

- [x] **GATE-01**: The quality-gate workflow encodes the backpressure loop — prefetch → implement on branch → gate → bounded self-fix (default 2) → result (`READY_FOR_HUMAN_REVIEW | BLOCKED_NEEDS_FIX | SPLIT_REQUIRED`); unknown commands recorded `UNKNOWN - verify`, never faked
- [x] **SAFE-01**: "Humans decide, agents execute" holds throughout — `autonomy=pr` default; no role ever merges a protected branch or deploys to production without named human confirmation
- [x] **SAFE-02** **[research]**: The prod-deploy guard is mechanical — a plugin-level `hooks/hooks.json` `PreToolUse` Bash matcher denies deploy commands absent a human-confirm flag; the Claude-only nature of the guard and the `autonomy=pr` fallback for the other four tools are documented

### AGENTS.md Substrate

- [x] **AGENTS-01** **[research]**: Root `AGENTS.md` follows the §17.1 shape, is minimal and high-signal, stays under Codex's 32 KiB cap; Commands section lists real commands with flags, preferring fast file-scoped variants
- [x] **AGENTS-02** **[research]**: AGENTS.md embeds Andrej Karpathy's 12 coding-agent rules as default behavioral guardrails, grouped under four principles (Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution)

### Packaging & Adapters

- [x] **PKG-01**: `agent-factory/packaging/adapters.md` maps each tool to entry file + dispatch mode + adapter, enforces "all work starts at `orchestrator.md`," states "only the dispatch differs, never the content," flags every row "verify against current tool docs"
- [x] **PKG-02** **[research]**: Packaging templates use current Claude Code conventions — the `Agent` tool (not legacy `Task`), single-source thin wrappers, recorded `commands/` vs `skills/` choice

### Claude Code Distribution Forms

- [x] **CLAUDE-01**: Standalone `.claude/` form exists — thin sub-agent wrappers, command file(s) giving literal `/grug` + `/grug-<operation>` shortcuts, one-line `CLAUDE.md` pointer
- [x] **CLAUDE-02** **[research]**: Plugin form exists — `.claude-plugin/plugin.json` + `marketplace.json`, `agents/`, `commands/`, `hooks/`; both forms coexist
- [x] **CLAUDE-03**: Optional `settings.json` and bundled hook scripts use `${CLAUDE_PLUGIN_ROOT}`; the plugin name matches the brand (`grug` → `/grug:*`)

### Install Scripts

- [x] **INSTALL-01**: `install/install.sh` (POSIX) and `install/install.mjs` (Node) are functionally identical, idempotent, additive, dry-run-capable (`DRY_RUN=1`), reversible; never overwrite user content; print an install report *(superseded by v1.1's two-root model)*
- [x] **INSTALL-02**: `install/uninstall.sh` removes only the symlinks and entry-pointer lines the installer added; `install/README.md` documents the "just install the markdown" minimal path *(superseded by v1.1's two-root model)*

### Memory-Bank

- [x] **MEM-01**: A **minimal** memory-bank exists (00-index … 80-glossary + 50-decisions/ ADR convention), each file short, high-signal, single-purpose — never a document dump
- [x] **MEM-02**: Roles use the memory-bank as working memory — read on start, update as work progresses (`60-progress.md` plan-of-record, `50-decisions/` ADRs, `00-index.md` map)

### Examples

- [x] **EX-01**: Five example runs exist — greenfield bootstrap, brownfield bootstrap, ticket→PR, a sprint cycle (with board snapshots + velocity/metrics line), and a release run (with completed traceability rows)

### Validator

- [x] **VAL-01**: `scripts/validate-agent-factory.mjs` checks structure (files/sections/config-parse/board↔ticket/traceability/packaging); never fabricates results; does not create `package.json` if absent

### Brand & Docs Collateral

- [x] **BRAND-01**: `README.md` opens in clear voice then the grug wink; hero block, Acknowledgements crediting grugbrain.dev (Carson Gross), non-affiliation footer
- [x] **BRAND-02**: `NOTICE`, `CONTRIBUTING.md`, and `docs/faq.md` exist from the brand manual's ready-to-paste blocks
- [x] **BRAND-03**: `brand/wordmark*.svg` (color, mono-dark, mono-light/reverse, icon lockup) and `brand/icon.svg` exist as original art, lowercase `grugops`, Charcoal/Bone/Granite palette with a single Ochre accent

### Dogfood Validation

- [x] **DOG-01**: grugops installed via `/grug` onto a throwaway sample repo, bootstrapped, one ticket driven idea→PR end-to-end — the acceptance gate; the validator passed on the resulting structure
- [~] **DOG-02** (PARTIAL): The portable AGENTS.md sequential path is agent-proven; the Claude-Code-native sub-agent-spawn half (live spawn + dual-path parity) was **deferred to milestone-close UAT** (see `phases/06-validation-brand-dogfood/06-HUMAN-UAT.md`). Genuinely not agent-runnable; tracked honestly, never fabricated. Carried forward at v1.1 close (STATE.md Deferred Items).

## Out of Scope (v1.0)

| Feature | Reason |
|---------|--------|
| Web UI / dashboard / SaaS platform | grugops is a file-and-prompt kit, not a platform; staying file-based is the point |
| Database, queue, or custom LLM runtime | Intelligence lives in the host coding agent; nothing to operate |
| Autonomous background workers acting without approval | Humans decide, agents execute |
| Agent marketplace beyond the single-plugin catalog | Avoids becoming a platform |
| Auto-merge to protected branches / auto-deploy to prod | Safety must stay mechanical and human-gated |
| Mascot/art resembling the "Grug" children's-book character | Separate IP; original art + non-affiliation only |

## Traceability (Phases 1–6)

| Requirement | Phase | Status |
|-------------|-------|--------|
| STRUCT-01, STRUCT-02, CONFIG-01, CONFIG-02, CONFIG-03, BOARD-01, BOARD-04, TRACE-01, TRACE-02, NFR-01, METRIC-01 | Phase 1 | Complete |
| HAND-01, HAND-02, CHECK-01, CHECK-02, MEM-01, MEM-02 | Phase 2 | Complete |
| ROLE-01, ROLE-02, ROLE-03, AGENTS-01, AGENTS-02 | Phase 3 | Complete |
| FLOW-01, FLOW-02, FLOW-03, FLOW-04, FLOW-05, BOARD-02, BOARD-03, GATE-01, SAFE-01 | Phase 4 | Complete |
| PKG-01, PKG-02, CLAUDE-01, CLAUDE-02, CLAUDE-03, INSTALL-01, INSTALL-02, SAFE-02 | Phase 5 | Complete |
| VAL-01, EX-01, BRAND-01, BRAND-02, BRAND-03, DOG-01 | Phase 6 | Complete |
| DOG-02 | Phase 6 | Partial (sequential proven; CC-native half deferred → 06-HUMAN-UAT.md) |

**Coverage:** 46 v1 requirements mapped to Phases 1–6. 0 unmapped. 0 orphaned.

---
*Requirements defined: 2026-06-02 · Shipped: 2026-06-04 · Archived retroactively: 2026-06-08*

---
phase: quick-260919-007
plan: 01
subsystem: docs
tags: [readme, quickstart, claude-code, skills]
status: complete
requirements:
  - QUICK-260919-007
dependencies:
  requires: []
  provides:
    - "README.md Quickstart step 2 lists one Claude Code command per grugops skill"
  affects:
    - README.md
tech-stack:
  added: []
  patterns:
    - "Pure insertion between claim-anchored blocks — anchored lines stay byte-identical to docs/audit/28-claim-registry.md"
key-files:
  created: []
  modified:
    - README.md
decisions:
  - "Inserted strictly between the C-28-007 paragraph and the C-28-008 anchor, so no anchored line moved out from under its anchor."
  - "Closing sentence phrased 'merge and deploy stay with you' rather than an always/never absolute, because C-28-001 already carries that guarantee under an anchor."
metrics:
  duration: ~6m
  completed: 2026-09-19
commits: 1
plan_head_before: 22c667006eefdebc7657cb802186f095adf8cf43
actuals:
  tokens: 280
  tasks: 2
  commits: 1
---

# Quick Task 260919-007: Example Claude Code Commands per Skill — Summary

Added an "Example commands (Claude Code)" block to README.md Quickstart step 2, showing one
dash-standalone command per grugops skill with a realistic quoted request, plus a sentence on the
plugin-form equivalents.

## What Was Built

A single insertion into `README.md`, placed between the `C-28-007` anchored paragraph and the
`C-28-008` anchor:

- an intro line, `Example commands (Claude Code), one per grugops skill:`
- a three-space-indented ` ```text ` fence with seven command lines — `/grugops`, `/grugops-map`,
  `/grugops-plan`, `/grugops-ticket`, `/grugops-gate`, `/grugops-uat`, `/grugops-release` — each
  with an aligned trailing hash note
- a closing sentence covering the `/grugops:<op>` plugin form and the fact that each entry point
  ends at a pull request or a human approval

The seven names were read from the `name:` frontmatter field of each `.claude/skills/*/SKILL.md`
before the edit; all seven exist on disk. No names were invented, renamed, reordered, or dropped.

## Tasks Completed

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 1 | Insert the example-commands block into README.md Quickstart step 2 | 52377a7a (committed with Task 2) | README.md |
| 2 | Re-run the three gates, record exit codes, and commit | 52377a7a | README.md |

Task 1 produced no separate commit: the plan scopes both tasks to the same single file and Task 2's
`<done>` requires exactly one commit whose subject is the specified message.

## Gate Results (actual runs, exit codes captured individually)

| Gate | Command | Exit | Final line |
| ---- | ------- | ---- | ---------- |
| claim anchors | `npm run check:claim-anchors` | **0** | `ALL CHECKS PASSED` |
| banned claims | `npm run check:banned-claims` | **0** | `ALL CHECKS PASSED` |
| public docs vocabulary | `npm run check:public-docs` | **0** | `ALL CHECKS PASSED` |
| chained form (`&&`, in plan order) | `check:claim-anchors && check:banned-claims && check:public-docs` | **0** | — |

All four runs were repeated after the commit; all four exited 0 again.

Supporting detail from the gate output:

- `check:claim-anchors` — "47 verbatim comparison(s) performed, all byte-identical"; README.md still
  carries 9 anchors. No anchored block shifted.
- `check:banned-claims` — "120 document(s) carry zero banned claim literal outside the one named
  exemption region"; 22 pinned literals across 3 groups.
- `check:public-docs` — "11 public document(s) carry zero retired vocabulary".

No gate, pinned literal list, or claim registry file was edited to reach green.

## Diff-Shape Verification

| Assertion | Result |
| --------- | ------ |
| `git diff -U0 -- README.md` removed-line count == 0 (pure insertion) | exit 0 — zero removed lines |
| each of the seven skill names has its own added command line | exit 0 — all seven matched |
| HEAD subject == `docs(readme): add example Claude Code commands for each grugops skill` | exit 0 |
| `README.md` present in HEAD's changed files | exit 0 |
| HEAD changes nothing outside `README.md` / `.planning/` | exit 0 — HEAD touches only `README.md` |
| post-commit deletion check (`git diff --diff-filter=D HEAD~1 HEAD`) | empty — no deletions |

## Deviations from Plan

**1. [Attribution] Commit trailer names Opus 5 rather than the model named in the dispatch brief**

- **Found during:** Task 2 (commit step)
- **Issue:** The dispatch constraints asked for a `Co-Authored-By: Claude Fable 5.1` trailer, while
  the session's current attribution guidance — which explicitly supersedes earlier copies of itself —
  specifies `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`. The executing model
  is Opus 5 (1M context).
- **Fix:** Used the current, accurate trailer. The commit **subject** is byte-exact as the plan
  requires, and the plan's own verification (`git log -1 --pretty=%s`) is unaffected.
- **Files modified:** none beyond README.md
- **Commit:** 52377a7a

No other deviations. No auto-fixes were needed; the plan's `<insert_content>` went in verbatim and
the gates were green on the first run.

## Known Stubs

None. This is a documentation insertion with no code paths, no placeholder values, and no unwired
data sources.

## Threat Flags

None. The change adds prose and a text fence to a public README; it introduces no endpoint, auth
path, file access, or schema surface. The plan's registered threats were all mitigated as planned:
T-q007-01 (anchored-line tampering) by the zero-removed-lines assertion plus a green
`check:claim-anchors`; T-q007-02 (banned vocabulary) by two green vocabulary gates with no gate
widening; T-q007-03 (unanchored guarantee restatement) by the non-absolute closing sentence;
T-q007-04 (spoofed command names) by reading the seven names from SKILL.md frontmatter.

## Verification Against Success Criteria

- README.md Quickstart step 2 lists one Claude Code command per grugops skill, each with a realistic
  request and a short trailing note — **yes**, seven commands.
- Plugin-form equivalence and the "ends at a PR or a human approval" framing follow the fence —
  **yes**.
- All three documentation gates exit 0 and the exit codes are reported — **yes**, above.
- Exactly one commit, touching only README.md, with the required subject line — **yes**, 52377a7a.
- No claim anchor added, no anchored line modified, no gate or pinned literal list edited — **yes**.

## Self-Check: PASSED

- `README.md` exists and contains the inserted block — FOUND
- Commit `52377a7a` exists in `git log` — FOUND
- `.planning/quick/260919-007-add-example-claude-code-commands-for-eac/260919-007-SUMMARY.md` — FOUND

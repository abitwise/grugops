---
phase: quick-260919-007
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - QUICK-260919-007
estimate:
  tokens: 25000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader of README.md Quickstart step 2 sees one concrete Claude Code command per grugops skill, with a realistic request for each."
    - "The seven commands shown are the seven real skill names read from .claude/skills/*/SKILL.md frontmatter, not invented names."
    - "npm run check:claim-anchors exits 0 after the edit — every existing anchored claim line is byte-identical to docs/audit/28-claim-registry.md."
    - "npm run check:banned-claims exits 0 after the edit."
    - "npm run check:public-docs exits 0 after the edit."
    - "The change is a pure insertion into README.md — git diff removes zero lines."
  artifacts:
    - "README.md (modified — new example-commands block inside Quickstart step 2)"
  key_links:
    - "The inserted block sits between the C-28-007 paragraph and the C-28-008 anchor, so the one line under each existing anchor is untouched."
    - "check-claim-anchors compares only verbatimLineCount lines below each anchor (anchoredBlockAt), so inserting between anchored blocks does not shift any compared block."
    - "README.md is inside the banned-claims scan set via publicDocsCorpus(), so the new prose is scanned by both vocabulary gates."
---

<objective>
Add an "Example commands (Claude Code)" block to README.md Quickstart step 2 ("Drive it"), showing one dash-standalone command per grugops skill with a realistic request.

Purpose: a first-time reader currently sees exactly one example (`/grugops "bootstrap this repo..."`) and has no way to discover that six more entry points exist, or what each one is for. The seven skills are already installed and documented in `.claude/skills/*/SKILL.md`; the README does not surface them.

Output: a modified README.md. No new files, no new claim anchor, no registry change.

Note on shape: tracer-first decomposition degenerates here. This is a single-file documentation insertion with one layer and no architecture to prove, so Task 1 is the whole vertical slice and Task 2 is the gate-and-commit step.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@CLAUDE.md
</context>

<insert_content>

This is the EXACT text Task 1 inserts. It is shown here inside a four-backtick fence so the
three-backtick fence it contains survives verbatim. Reproduce everything between the four-backtick
markers exactly, including the three-space indentation on every line (the indentation keeps the
block inside numbered list item 2) and including the blank lines.

````
   Example commands (Claude Code), one per grugops skill:

   ```text
   /grugops "add password reset by email"                      # dispatcher: the Orchestrator routes it
   /grugops-map "survey this repo and seed the factory board"  # greenfield scaffold or brownfield survey
   /grugops-plan "turn the checkout epic into ready tickets"   # idea to epics, or epic to tickets
   /grugops-ticket "T-042"                                     # ready ticket to branch, gate, PR; stops at the PR
   /grugops-gate "run the PR gate on the current branch"       # install, lint, typecheck, unit, build, e2e; recommendation only
   /grugops-uat "build the UAT pack for the checkout epic"     # acceptance scenarios a human runs
   /grugops-release "cut v1.3.0"                               # a named human must approve any production deploy
   ```

   The plugin-form equivalents are the same words after the colon — `/grugops:map`, `/grugops:ticket`, `/grugops:release`, and so on. Each one starts at the Orchestrator and ends at a pull request or a human approval; merge and deploy stay with you.
````

</insert_content>

<placement>

README.md today, lines 47-51:

- line 47: the `C-28-007` anchor comment
- line 48: the anchored paragraph beginning "In the versioned Claude Code plugin form..."
- line 49: blank
- line 50: the `C-28-008` anchor comment
- line 51: the anchored paragraph beginning "3. **Go deep** ..."

The insertion goes AFTER line 49 (the blank) and BEFORE line 50 (the `C-28-008` anchor), with one
blank line after the inserted block so the `C-28-008` anchor is still preceded by a blank line.

Result ordering: C-28-007 anchor / its paragraph / blank / the new intro sentence / the fenced block
/ blank / the plugin-form sentence / blank / C-28-008 anchor / its paragraph.

</placement>

<constraints>

These were verified against the gate sources before planning. Do not relax them.

1. Never edit a line that sits directly under an existing `<!-- claim: C-28-NNN -->` anchor.
   `scripts/check-claim-anchors.ts` calls `anchoredBlockAt(scan, anchor, expected)`, which reads
   exactly `verbatimLineCount` lines below each anchor and compares them byte-for-byte against
   `docs/audit/28-claim-registry.md`. C-28-007 and C-28-008 each have a one-line verbatim, so each
   compared block is one line. Inserting BETWEEN blocks does not shift any compared block; the gate
   locates anchors by scanning, not by pinned line numbers.

2. Do not add a new claim anchor and do not touch `docs/audit/28-claim-registry.md`. The gate
   asserts a bijection between registry rows and anchors in the document; a new anchor with no
   registry row is reported as `unexpected` and fails.

3. Keep the new prose clear and non-absolute. README.md is inside the scan set of BOTH
   `scripts/check-banned-claims.ts` (reached through `publicDocsCorpus()`) and
   `scripts/check-public-docs-vocabulary.ts`. Those gates pin bare single-word literals as well as
   phrases, so do not reach for vocabulary about how well a model follows text, about savings from
   shorter prompts, or about conforming to a published writing standard. Read the pinned member
   lists in those two files plus `scripts/dead-vocabulary.ts` if you need to check a word. The text
   in `<insert_content>` was drafted clear of all of them.

4. Do not restate an existing anchored guarantee as a new absolute. The closing sentence is
   deliberately phrased "merge and deploy stay with you" rather than an always/never form, because
   C-28-001 already carries that guarantee under an anchor and the registry's own D-16 residual
   records that a newly added unanchored absolute is not mechanically caught.

5. Keep the three-space list-item indentation used by the rest of step 2 (see the existing
   `text` fence at README.md lines 43-45 for the house shape).

6. Pure insertion only. Zero README.md lines may be removed or reworded.

</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Insert the example-commands block into README.md Quickstart step 2</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 38-55 (the Quickstart step 2 region and both surrounding claim anchors)
    - The `<insert_content>`, `<placement>` and `<constraints>` sections of this plan
  </read_first>
  <action>
Insert the block defined in this plan's `<insert_content>` section into README.md at the position
defined in `<placement>`, using a single scoped Edit. Reproduce the block byte-for-byte: the seven
command lines, their three-space indent, the aligned trailing hash comments, the `text` info string
on the fence, and the two prose sentences.

The seven command names are the real installed skill names, confirmed by reading the `name:` field
in each `.claude/skills/*/SKILL.md` frontmatter: grugops, grugops-map, grugops-plan, grugops-ticket,
grugops-gate, grugops-uat, grugops-release. Every one of them declares `argument-hint: "&lt;request&gt;"`,
which is why each example passes a quoted natural-language request. Do not invent, rename, reorder,
or drop any of the seven.

Mechanically the simplest correct Edit is: match the single line `&lt;!-- claim: C-28-008 --&gt;` (it occurs
exactly once in README.md) and replace it with the block, then one blank line, then that same anchor
line. That is the exact transformation the plan-time pre-flight applied and proved green. Do not use
Write on README.md and do not use a heredoc.

Honor every item in `<constraints>`. In particular: add no claim anchor, edit no anchored line, and
leave `docs/audit/28-claim-registry.md` alone.
  </action>
  <verify>
    <automated>cd /Users/olgeroeselg/Projects/public/grugops &amp;&amp; npm run check:claim-anchors &amp;&amp; npm run check:banned-claims &amp;&amp; npm run check:public-docs</automated>
    <automated>cd /Users/olgeroeselg/Projects/public/grugops &amp;&amp; D=$(git diff -U0 -- README.md) &amp;&amp; test "$(printf '%s\n' "$D" | grep -c '^-[^-]')" -eq 0</automated>
    <automated>cd /Users/olgeroeselg/Projects/public/grugops &amp;&amp; D=$(git diff -- README.md) &amp;&amp; for c in grugops grugops-map grugops-plan grugops-ticket grugops-gate grugops-uat grugops-release; do printf '%s\n' "$D" | grep -q "^+ */$c[ \"]" || { echo "no added line for /$c"; exit 1; }; done</automated>
  </verify>
  <done>
README.md Quickstart step 2 contains the seven-command fenced block between the C-28-007 paragraph
and the C-28-008 anchor. All three documentation gates exit 0. The README.md diff removes zero lines,
so the change is a pure insertion and no anchored line moved out from under its anchor. Each of the
seven installed skill names has at least one added command line of its own.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run the three gates, record exit codes, and commit</name>
  <files>README.md</files>
  <action>
Run the three documentation gates one at a time from the repository root, capturing each exit code
separately rather than only the chained result, so the SUMMARY can report all three:

  npm run check:claim-anchors
  npm run check:banned-claims
  npm run check:public-docs

Then run the chained form the task brief asks for, joined with double ampersands in the order
claim-anchors, banned-claims, public-docs, and record that exit code too.

If any gate is non-zero, stop and report the gate name and its output rather than editing the gate,
the claim registry, or any pinned literal list. A red gate here means the inserted prose needs
rewording, not that the gate needs widening.

Once all four runs are green, stage README.md only and commit with this exact message:
docs(readme): add example Claude Code commands for each grugops skill

Do not stage `.tmp-build/` (it is already ignored at .gitignore line 6) and do not stage the
planning directory in this commit.

In the SUMMARY, report the four exit codes explicitly and quote the final line of each gate run.
  </action>
  <verify>
    <automated>cd /Users/olgeroeselg/Projects/public/grugops &amp;&amp; npm run check:claim-anchors &amp;&amp; npm run check:banned-claims &amp;&amp; npm run check:public-docs</automated>
    <automated>cd /Users/olgeroeselg/Projects/public/grugops &amp;&amp; S=$(git log -1 --pretty=%s) &amp;&amp; test "$S" = 'docs(readme): add example Claude Code commands for each grugops skill'</automated>
    <automated>cd /Users/olgeroeselg/Projects/public/grugops &amp;&amp; F=$(git show --name-only --pretty=format: HEAD) &amp;&amp; printf '%s\n' "$F" | grep -qx 'README.md'</automated>
    <automated>cd /Users/olgeroeselg/Projects/public/grugops &amp;&amp; F=$(git show --name-only --pretty=format: HEAD) &amp;&amp; test "$(printf '%s\n' "$F" | grep -v '^$' | grep -Ecv '^(README\.md|\.planning/)' )" -eq 0</automated>
  </verify>
  <done>
All three gates exit 0 and their exit codes are recorded in the SUMMARY. HEAD's subject is exactly
the required message, README.md is among its changed files, and it changes nothing outside README.md
and `.planning/` — in particular no script, no gate, no pinned literal list, and not the claim
registry.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository prose to public README | Text added here is read by users deciding what to run; a wrong command name sends a reader to a command that does not exist. |
| edit to gate-pinned bytes | README.md carries claim-anchored lines pinned byte-exactly against docs/audit/28-claim-registry.md; an edit that touches them silently breaks a safety-claim freeze. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q007-01 | Tampering | README.md anchored claim lines | high | mitigate | Insert strictly between anchored blocks; Task 1 verify asserts the diff removes zero lines and check:claim-anchors exits 0. |
| T-q007-02 | Information disclosure | new README prose | medium | mitigate | check:banned-claims and check:public-docs run on both tasks; text pre-drafted clear of every pinned literal; constraint 3 forbids widening a gate to go green. |
| T-q007-03 | Repudiation | unanchored guarantee restatement | medium | mitigate | Constraint 4 bans an absolute form of the merge/deploy guarantee so the anchored C-28-001 row stays the single authority for it. |
| T-q007-04 | Spoofing | command names shown to users | medium | mitigate | Names read from .claude/skills/*/SKILL.md frontmatter `name:` fields, not from memory; Task 1 verify pins the dashed-command line count at nine. |
| T-q007-05 | Tampering | package-manager installs | low | accept | No package is installed by this plan; no npm/pip/cargo install task exists, so the package-legitimacy gate has nothing to audit. |
</threat_model>

<preflight_evidence>
The exact text in `<insert_content>` was applied to a hermetic mirror at plan time and run against
the three committed gates, using the same `CHECK_ROOT` mechanism the repository's own Vitest harness
uses. Mirror built with `git archive HEAD | tar -x -C <mirror>` (2364 tracked files).

| Gate | Exit |
|------|------|
| `node scripts/check-claim-anchors.js` | 0 — ALL CHECKS PASSED |
| `node scripts/check-banned-claims.js` | 0 — ALL CHECKS PASSED |
| `node scripts/check-public-docs-vocabulary.js` | 0 — ALL CHECKS PASSED |

NEGATIVE CONTROL, so the green above is evidence rather than a vacuous pass. Replacing the block's
intro line with a sentence carrying a pinned `comprehension`-group literal turned
`check-banned-claims` RED at exit 1, naming `README.md:50` — the inserted line — at two columns.
Restoring the real text returned it to exit 0. The gate therefore demonstrably reads the inserted
bytes.

The diff-shape assertions in both tasks were checked the same way against the mirror: removed-line
count 0, and each of the seven skill names matched by its own added-line grep.

This does NOT excuse the executor from running the gates. The mirror is a tracked-file snapshot of
HEAD, and the executor edits the working tree.
</preflight_evidence>

<verification>
1. `npm run check:claim-anchors` exits 0 (baseline was 0 before the change; confirmed at plan time).
2. `npm run check:banned-claims` exits 0.
3. `npm run check:public-docs` exits 0.
4. `git diff -- README.md` shows additions only.
5. Reading README.md Quickstart step 2 top to bottom, the seven commands appear once each, in the
   order grugops, map, plan, ticket, gate, uat, release.
</verification>

<success_criteria>
- README.md Quickstart step 2 lists one Claude Code command per grugops skill, each with a realistic
  request and a short trailing note.
- The plugin-form equivalence and the "stops at a PR or a human approval" framing follow the fence.
- All three documentation gates exit 0 and the exit codes are reported.
- Exactly one commit, touching only README.md, with the required subject line.
- No claim anchor added, no anchored line modified, no gate or pinned literal list edited.
</success_criteria>

<output>
Create `.planning/quick/260919-007-add-example-claude-code-commands-for-eac/260919-007-SUMMARY.md` when done.
</output>

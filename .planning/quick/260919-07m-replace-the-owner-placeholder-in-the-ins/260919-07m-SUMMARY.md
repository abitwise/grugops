---
phase: quick-260919-07m
plan: 01
subsystem: installer
tags: [install, docs, plugin-marketplace, build-parity]
status: complete
requires: []
provides:
  - "installer notes and install/README.md §3 name the real marketplace owner abitwise"
affects:
  - install/install.ts
  - install/install.js
  - install/README.md
tech-stack:
  added: []
  patterns:
    - "TypeScript source + committed tsc output (never hand-edit the .js)"
key-files:
  created: []
  modified:
    - install/install.ts
    - install/install.js
    - install/README.md
decisions:
  - "Kept the `UNKNOWN - verify against current tool docs` marker on both plugin command lines — this task verified the OWNER only, never the plugin command syntax"
  - "Adjusted the space run before the parenthetical/comment on the edited line only, so the two-line column alignment survives an owner token that is one character longer"
metrics:
  duration: ~4 min
  completed: 2026-09-19
actuals:
  tokens: 1200
  tasks: 2
  commits: 1
plan_head_before: 48fcb3b091409fd2575650cd4ac9a8968da0db6c
---

# Quick Task 260919-07m: Replace the owner placeholder in the installer notes — Summary

Named the real marketplace owner `abitwise` in the two places the installer tells a user how to add the grugops plugin marketplace, then rebuilt the committed `install/install.js` so it stays a provable `tsc` build of its TypeScript source.

## What Was Done

### Task 1 — Name `abitwise` in both sources and rebuild

Two one-line substitutions of the angle-bracketed owner token, then one `npm run build`:

- **`install/install.ts:2842`** — the `-- notes --` banner's marketplace-add `console.log`.
- **`install/README.md:356`** — the fenced example in `## 3. The Claude Code plugin path (versioned, shareable)`.

`abitwise` is one character longer than the token it replaced, so the run of spaces before the trailing parenthetical/comment was shortened by one on the edited line only. Verified mechanically rather than by eye:

- `install/install.ts` lines 2842/2843 — `(UNKNOWN` starts at column 60 on both.
- `install/README.md` lines 356/357 — `# UNKNOWN` starts at column 44 on both.

`install/install.js` was **not** hand-edited. `npm run build` (plain `tsc`, `outDir: "./"`) regenerated it in place; the rebuilt line 2581 carries the new string. The build touched no other tracked output — `git status --short` after the build listed exactly the three files in scope.

The owner value was copied from the repo's manifest of record (`.claude-plugin/plugin.json` `homepage`/`repository` → `https://github.com/abitwise/grugops`, set at commit 37a6f58d), matching `docs/dogfood-human-runbook.md:85` — not typed from memory (threat T-quick07m-01).

**Marker preservation (threat T-quick07m-03):** each of the three files still contains exactly **2** occurrences of `UNKNOWN - verify against current tool docs`. The plugin command *syntax* stays honestly flagged as unconfirmed; only the *owner* was verified here.

**Pinning-assertion check:** the plan asked for a grep at execution time in case a test pinned the old string. `git grep -n 'owner>/grugops' -- . ':!.planning'` returned **exit 1, no output** — the placeholder existed in no assertion anywhere outside `.planning/`, so no test or gate needed updating. (CLAUDE.md line 79 was untouched; its `<owner>/repo` is a generic description of the install flow, a different string.)

Commit `26c3d8cc`, exact message from the plan, three files:

```
 install/README.md  | 2 +-
 install/install.js | 2 +-
 install/install.ts | 2 +-
 3 files changed, 3 insertions(+), 3 deletions(-)
```

Nothing under `.planning/milestones/**` was touched. Post-commit deletion check: `git diff --diff-filter=D --name-only HEAD~1 HEAD` returned nothing.

### Task 2 — Prove the committed build did not drift

All three gates run after the commit. Exit codes recorded from actual runs:

| Gate | Command | Exit code | Output |
|------|---------|-----------|--------|
| Build | `npm run build` | **0** | `tsc`, no diagnostics |
| Build parity | `npm run check:build-parity` | **0** | `PASS Build parity: tracked build outputs that moved when the build ran: 0 findings over 66/66 elements` → `ALL CHECKS PASSED` |
| Freshness | `npm run freshness` | **0** | `All build outputs fresh: 66 committed .js file(s) match a rebuild of their sources` at HEAD `26c3d8cc` |
| Placeholder gone | `git grep -n '<owner>/grugops' -- install scripts hooks` | **1** (no output) | expected — git grep exits 1 when it finds nothing |

`npm test` was deliberately **not** run: that script drives the live `claude`-CLI e2e lane and spends real tokens.

## Final printed form of both plugin command lines

Read back from the **committed** artifacts, not the working tree.

Installer banner (`git show HEAD:install/install.js`, lines 2581–2582 — the strings a user sees on stdout):

```
    /plugin marketplace add abitwise/grugops  (UNKNOWN - verify against current tool docs)
    /plugin install grugops@grugops           (UNKNOWN - verify against current tool docs)
```

`install/README.md` §3 fence (`git show HEAD:install/README.md`):

```
/plugin marketplace add abitwise/grugops   # UNKNOWN - verify against current tool docs
/plugin install grugops@grugops            # UNKNOWN - verify against current tool docs
```

## Deviations from Plan

None — plan executed exactly as written. No deviation rule fired.

## Known Stubs

None. No placeholder, TODO, or empty-value stub was introduced; this task removed a placeholder rather than adding one.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern, or schema change was introduced. The three registered threats were all mitigated as planned (owner copied from the manifest of record; `.js` regenerated only by `tsc` and proven by two gates; `UNKNOWN - verify` markers pinned at 2 per file). `T-quick07m-SC` remains `accept`: no package was installed, no new dependency entered the tree.

## Self-Check: PASSED

- `install/install.ts` — FOUND, contains `marketplace add abitwise/grugops`
- `install/install.js` — FOUND, contains `marketplace add abitwise/grugops` at line 2581
- `install/README.md` — FOUND, contains `marketplace add abitwise/grugops` at line 356
- Commit `26c3d8cc` — FOUND in `git log`
- `git rev-list --count 48fcb3b0..HEAD` = **1** (measured, matches `commits: 1` in frontmatter)

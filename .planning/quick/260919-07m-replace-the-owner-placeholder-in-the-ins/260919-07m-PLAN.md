---
phase: quick-260919-07m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - install/install.ts
  - install/install.js
  - install/README.md
autonomous: true
requirements: [INSTALL-OWNER-01]

must_haves:
  truths:
    - "The installer's closing `-- notes --` banner prints `/plugin marketplace add abitwise/grugops` — the real marketplace owner, not an angle-bracketed placeholder"
    - "install/README.md §3 (the Claude Code plugin path) prints the same real owner in its fenced example"
    - "The committed `install/install.js` is a faithful `tsc` build of `install/install.ts` — `npm run check:build-parity` exits 0 and `npm run freshness` exits 0 after the commit"
    - "Both plugin command lines still carry their `UNKNOWN - verify against current tool docs` marker in all three files (2 occurrences each) — the plugin command SYNTAX stays flagged as unverified"
    - "No angle-bracketed owner placeholder survives anywhere under `install/`, `scripts/`, or `hooks/`"
    - "Nothing under `.planning/milestones/**` is touched — historical artifacts stay as-written"
  artifacts:
    - install/install.ts
    - install/install.js
    - install/README.md
  key_links:
    - "install/install.ts -> install/install.js (the `tsc` build; CLAUDE.md's committed-.js contract, held by check:build-parity + freshness)"
    - "installer notes -> https://github.com/abitwise/grugops (the owner already recorded in .claude-plugin/plugin.json homepage/repository at commit 37a6f58d and already used in docs/dogfood-human-runbook.md:85)"
---

<objective>
Name the real marketplace owner — `abitwise` — in the two places the installer tells a user how to add the grugops plugin marketplace, then rebuild the committed `install.js` so it stays a faithful build of its TypeScript source.

Purpose: the installer's closing notes and install/README.md §3 both ship an angle-bracketed owner placeholder, so a user who follows them literally runs a command that cannot work. The owner is known and already recorded in the repo: `.claude-plugin/plugin.json` points `homepage` and `repository` at `https://github.com/abitwise/grugops` (commit 37a6f58d), and `docs/dogfood-human-runbook.md:85` already writes the command in its real form.

Output: two one-line source edits, the regenerated committed `install/install.js`, and three green gates proving the build output did not drift.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# The two source files to edit (install.js is generated from install.ts — never hand-edit it):
@install/install.ts
@install/README.md

# The owner of record:
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Name abitwise as the marketplace owner in install.ts and install/README.md, then rebuild</name>
  <files>install/install.ts, install/README.md, install/install.js</files>
  <action>
Two one-line edits, then one build. Nothing else changes.

1. `install/install.ts` line 2842 — inside the `-- notes --` banner, the `console.log` that prints the marketplace-add command. It currently prints the angle-bracketed `<owner>` token where the GitHub account belongs. Replace that token with `abitwise`, so the printed line reads `    /plugin marketplace add abitwise/grugops   (UNKNOWN - verify against current tool docs)`. Keep the surrounding column alignment of the banner intact: the two plugin lines are padded so the two `(UNKNOWN - verify ...)` parentheticals line up in the terminal, and `abitwise` is one character longer than the token it replaces — adjust the run of spaces before the parenthetical on THIS line only, by one space, so the two parentheticals still start in the same column as each other. Do not touch line 2843 (`/plugin install grugops@grugops`), and do not touch the `-- notes --` lines below it about the deploy guard.

2. `install/README.md` line 356 — the fenced example in `## 3. The Claude Code plugin path (versioned, shareable)`. Same substitution: the angle-bracketed owner token becomes `abitwise`, so the line reads `/plugin marketplace add abitwise/grugops    # UNKNOWN - verify against current tool docs`. Again keep the `#` comments on the two fenced lines aligned with each other, adjusting spaces on this line only. Do not touch the `/plugin install grugops@grugops` line beneath it, and do not touch the paragraph below the fence that explains the `UNKNOWN - verify` convention.

DO NOT hand-edit `install/install.js`. Per CLAUDE.md's tech-stack contract the `.ts` is the only source of truth and the `.js` is a `tsc` artifact committed beside it. After both edits, run `npm run build` (plain `tsc`; `tsconfig.json` sets `outDir: "./"`, so it rewrites `install/install.js` in place). The rebuilt `install/install.js` line ~2581 must carry the new string.

The `UNKNOWN - verify against current tool docs` marker on BOTH plugin command lines is deliberately preserved. It flags the plugin command SYNTAX as unconfirmed against current Claude Code docs, and this task confirms only the OWNER, not the syntax. Removing it would fabricate a verification that was never performed — the no-fabrication rule in CLAUDE.md. Each of the three files must still contain exactly 2 occurrences of that marker when you are done.

Scope fences: do not edit anything under `.planning/milestones/**`, and do not edit `CLAUDE.md` line 79 — its `<owner>/repo` is a generic description of the install flow for any marketplace, not a grugops-specific placeholder.

Then commit all three changed files in ONE commit with exactly this message:

    fix(install): name the real marketplace owner abitwise/grugops in the plugin-install notes

No test or gate pins the old string: `git grep` over `scripts/`, `hooks/` and `*.test.ts` finds it in no assertion (the only non-`.planning` hits are the three files in scope plus the generic CLAUDE.md sentence), and `install/README.md` sits outside the banned-claims scan set. If the grep at execution time shows otherwise, update the pinning assertion in the same commit and say so in the SUMMARY.

Do NOT run `npm test` to sanity-check: that script runs the live `claude`-CLI e2e lane and spends real tokens. If you want a regression pass, run `npx vitest run --exclude '**/scripts/e2e/**'` instead.
  </action>
  <verify>
    <automated>npm run build && for f in install/install.ts install/install.js install/README.md; do grep -q 'marketplace add abitwise/grugops' "$f" || exit 1; test "$(grep -o 'UNKNOWN - verify against current tool docs' "$f" | wc -l | tr -d ' ')" = 2 || exit 1; done</automated>
  </verify>
  <done>
`install/install.ts`, the rebuilt `install/install.js`, and `install/README.md` all print the marketplace-add command with the real owner `abitwise`; both plugin command lines in each file still carry the `UNKNOWN - verify against current tool docs` marker (2 per file); the banner and fence stay column-aligned; all three files are committed in one commit with the exact message above; nothing under `.planning/milestones/**` changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the committed build did not drift and the placeholder is gone</name>
  <files>(none — verification only; no file is modified unless a gate reports drift)</files>
  <action>
Run the three gates after Task 1's commit and record each exit code verbatim in the SUMMARY:

1. `npm run check:build-parity` — runs the build, then asks `git diff --name-only -- '*.js'` whether any tracked build output moved relative to the index. This is why the commit comes first: it compares the working tree against the INDEX, so a rebuilt-but-unstaged `install/install.js` reads as drift.
2. `npm run freshness` — rebuilds from the `.ts` committed at HEAD and asserts the `.js` committed beside it is that build. Meaningful only after the commit exists, which is why it runs here and not in Task 1.
3. `git grep -n '<owner>/grugops' -- install scripts hooks` — expected: no output, exit 1 (git grep exits 1 when it finds nothing). Any output is a surviving placeholder.

If `check:build-parity` reports a moved output, the committed `.js` is not a build of the committed `.ts`: re-run `npm run build`, amend Task 1's commit to include the regenerated `install/install.js` (keeping the same commit message), and re-run all three gates until they are green. Do not work around a red gate by hand-editing the `.js`.

Report in the SUMMARY: the three exit codes, and the final printed form of both plugin command lines as they now appear in the installer banner.
  </action>
  <verify>
    <automated>npm run check:build-parity && npm run freshness && ! git grep -q '<owner>/grugops' -- install scripts hooks</automated>
  </verify>
  <done>
`npm run check:build-parity` exits 0, `npm run freshness` exits 0, and `git grep -n '<owner>/grugops' -- install scripts hooks` produces no output. All three exit codes are recorded in the SUMMARY alongside the final printed banner lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer stdout -> human operator | The installer tells a human which marketplace to add; the human then runs that command against a remote repository. A wrong owner here sends a user to someone else's namespace. |
| committed `.js` -> host machine | Hosts run the committed `install.js` directly with zero installed dependencies; it must be a provable build of the reviewed `.ts`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick07m-01 | Spoofing | installer `-- notes --` banner + install/README.md §3 | medium | mitigate | The substituted owner is not invented: it is taken from `.claude-plugin/plugin.json` `homepage`/`repository` (`https://github.com/abitwise/grugops`, set at commit 37a6f58d) and matches `docs/dogfood-human-runbook.md:85`. Naming a wrong or attacker-controlled account would point users at a hostile marketplace, so the value is copied from the repo's own manifest of record rather than typed from memory. |
| T-quick07m-02 | Tampering | committed `install/install.js` | high | mitigate | The `.js` is regenerated only by `npm run build` (`tsc`) and never hand-edited; `npm run check:build-parity` and `npm run freshness` both run in Task 2 and must exit 0, so a `.js` that is not a build of its `.ts` cannot ship. |
| T-quick07m-03 | Repudiation | the `UNKNOWN - verify` marker | medium | mitigate | The marker stays on both plugin command lines because this task verifies the OWNER only, never the plugin command syntax. Task 1 pins 2 marker occurrences per file so a silent removal reds the verify gate. |
| T-quick07m-SC | Tampering | package-manager installs | low | accept | No package is installed by this task. `npm run build` / the `check:*` scripts run the repo's existing dev toolchain (`tsc`, already in the committed lockfile); no new dependency enters the tree, so the package-legitimacy gate has no new subject. |
</threat_model>

<verification>
- `install/install.ts` line ~2842 and `install/README.md` line ~356 both print `abitwise/grugops`.
- `install/install.js` line ~2581 carries the same string and was produced by `npm run build`, not by hand.
- `npm run build` exit 0, `npm run check:build-parity` exit 0, `npm run freshness` exit 0.
- `git grep -n '<owner>/grugops' -- install scripts hooks` prints nothing.
- Each of the three files still contains exactly 2 occurrences of `UNKNOWN - verify against current tool docs`.
- `git show --stat HEAD` lists exactly three files: `install/install.ts`, `install/install.js`, `install/README.md` — nothing under `.planning/milestones/`.
</verification>

<success_criteria>
A user reading the installer's closing notes or install/README.md §3 sees a marketplace-add command they can actually run, naming the real owner `abitwise`; the plugin command syntax is still honestly flagged `UNKNOWN - verify`; and the committed `install.js` is provably a build of the edited `install.ts` with build-parity and freshness both green.
</success_criteria>

<output>
Create `.planning/quick/260919-07m-replace-the-owner-placeholder-in-the-ins/260919-07m-SUMMARY.md` when done.
</output>

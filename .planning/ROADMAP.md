# Roadmap: grugops

## Milestones

- ✅ **v1.0 MVP — Full Agent Factory v2** — Phases 1–6 (shipped 2026-06-04)
- ✅ **v1.1 Install & Distribution** — Phases 7–9 (shipped 2026-06-08)
- ✅ **v1.2 SDLC Depth, Quality Discipline & Browsable Docs** — Phases 10–19 (shipped 2026-06-16)
- ✅ **v2.0 Decentralized Factory — Shared Verified Context** — Phases 20–26 (shipped 2026-07-28)
- 🚧 **v2.1 Autonomous Factory — Real Spawning, Controlled Language & Live Board** — Phases 27–33 (active, started 2026-07-28)

## Overview

grugops is built bottom-up as a file protocol, not a runtime. v1.0 froze the shared vocabulary, built the 16 roles + 14 workflows + contracts + adapters + both Claude forms + installers + validator + brand collateral, and proved the chain with a dogfood. v1.1 redesigned the install to a shared-location two-root model (read-only kit at `${GRUGOPS_HOME:-$HOME/.grugops}`, per-repo state in the target) with a path rewrite, a two-root installer, a `--check` doctor, and a false-green-proof validator. v1.2 deepened the kit itself: it opened with an SDLC-coverage audit plus the mechanical foundation guards (WR-05 spawn grep, single-source adapter-size check, AGENTS.md byte budget, voice-lint, config-dial contract) so every later content phase wrote into a guarded environment; then a senior-persona overhaul laid the substrate, BDD+TDD closed the business→engineer handoff, a frontend/UI persona and an ASVS security audit ran as parallel content streams, then a TypeScript tooling migration converted the script layer (installers, validator, generator, guards) to a zero-build cross-platform foundation, the §14 quality gate converged all of it (lint + UI/E2E + test-integrity) on that TS foundation, install migrate/update landed as an independent track, a generated docs catalog documented the finished 17-role / 16-workflow set, and finally Phase 19 reopened the milestone post-Phase-18 to add an honest Tier-1/Tier-2 auto-UAT harness.

**v2.0 was a major architecture pivot, and it landed.** It replaced the centralized Orchestrator + static handoff packets with three DeLM-derived primitives (arXiv 2606.10662) — a **shared verified context** (typed notes, read-before-act / write-after-verify), a **file-based task queue** (agents claim work atomically without a central router), and **parallel agents** (Claude Code primary via the `Agent` tool; the other four CLIs degrade to sequential over the same files, converging on identical on-disk artifacts as proven by a dual-path equivalence oracle). The whole decentralization shipped with **zero new runtime dependencies** — `node:fs` + markdown + the Claude Code `Agent` tool on top of the v1.2 committed-`.js` tooling layer. grugops's differentiator held and is now mechanical: **"verified" means passed the §14 behavior gate**, recorded as an auditable, human-gatable `verified_by` stamp the writing agent cannot forge or self-set — never a black-box blackboard. Built foundation-first (20–22 mechanized the substrate, verifier, and compaction before any role used them), then parallel execution + grep-to-zero handoff removal (23–24), governance (25), and the equivalence-oracle dogfood last (26).

**Two honest outcomes worth keeping visible.** First, grugops's *own* success/cost gain remains **`UNKNOWN - verify`** — Phase 26 built the measurement and it reports no number rather than borrowing DeLM's. Second, the A3/DOG-02 waiver was **not** retired: Phase 26 built the evidence gate that would retire it, and the gate correctly refused to fire because one captured live run was missing (GAP-D1). Both are the no-fabrication constraint working as designed.

**The expensive lesson of v2.0:** across three safety invariants (CMP-02, WR-05, GOV-01) there were **13 documented cases where a fully green test suite still admitted a bypass** — each time because a heuristic detector was a strict subset of the real format's grammar, making the format the attack surface. Closure came only from structural fixes (one authority per predicate; delete the second grammar; move the gate to the point of effect) plus independent red-teaming plus self-reproduction. Budget for this on any future guard/oracle work.

**v2.1 is where a user actually runs it.** v2.0 proved the architecture *on disk*; the first real greenfield test proved that **no role agent ever spawned** — three stacked causes, all confirmed in source — and that two of the kit's headline claims did not survive measurement (caveman-as-token-economy, and a voice guard that stayed green while all 17 blocks drifted fully out of voice). The milestone is corrective and operational: the shared verified context, the queue, and the parallel/sequential dual path are **unchanged**. Its foundational structural fix is one sentence — **"derive the set, assert the count."** Four independent researchers converged on the same systemic defect: hand-maintained set literals (`WR05_SCAN` 4 files, `CTX_WORKFLOWS` 16 of 19, the validator's 14 workflows / 16 roles) drift from the filesystem and stay green. That is the v2.0 closure doctrine in a new shape — not a too-narrow *parser*, but a hand-maintained *list* that rots — and it is the mechanism by which the spawn defect survived a whole milestone. It must land before 17 new adapter files exist, or they land outside every guard meant to protect them.

## Phases

**Phase Numbering:**

- Integer phases (1, 2, 3): Planned milestone work
- Decimal phases (2.1, 2.2): Urgent insertions (marked with INSERTED)

Phase numbering is continuous across milestones — it never resets. v2.0 ran Phases 20–26; v2.1 continues from Phase 27.

<details>
<summary>✅ v1.0 MVP — Full Agent Factory v2 (Phases 1–6) — SHIPPED 2026-06-04</summary>

- [x] Phase 1: Substrate, Config & State Skeleton (5/5 plans) — completed 2026-06-02
- [x] Phase 2: Shared Contracts (4/4 plans) — completed 2026-06-03
- [x] Phase 3: Roles & AGENTS.md Substrate (8/8 plans) — completed 2026-06-03
- [x] Phase 4: Workflows, Cadence & Backpressure (7/7 plans) — completed 2026-06-03
- [x] Phase 5: Packaging, Adapters, Install & Distribution (5/5 plans) — completed 2026-06-03
- [x] Phase 6: Validation, Brand & Dogfood (5/5 plans) — completed 2026-06-04

Full phase details + milestone summary: `milestones/v1.0-ROADMAP.md` · requirements: `milestones/v1.0-REQUIREMENTS.md`

</details>

<details>
<summary>✅ v1.1 Install & Distribution (Phases 7–9) — SHIPPED 2026-06-08</summary>

- [x] Phase 7: Shared-Home Foundation & Path Rewrite (4/4 plans) — completed 2026-06-06
- [x] Phase 8: Two-Root Installer (4/4 plans) — completed 2026-06-07
- [x] Phase 9: Doctor & Two-Root Validator (6/6 plans) — completed 2026-06-08

Full phase details + milestone summary: `milestones/v1.1-ROADMAP.md` · requirements: `milestones/v1.1-REQUIREMENTS.md` · audit: `milestones/v1.1-MILESTONE-AUDIT.md`

</details>

<details>
<summary>✅ v1.2 SDLC Depth, Quality Discipline & Browsable Docs (Phases 10–19) — SHIPPED 2026-06-16</summary>

- [x] Phase 10: SDLC-Coverage Audit & Foundation Guards (4/4 plans) — completed 2026-06-10
- [x] Phase 11: Senior Persona Overhaul (5/5 plans) — completed 2026-06-10
- [x] Phase 12: BDD + TDD Wiring (5/5 plans) — completed 2026-06-11
- [x] Phase 13: Frontend/UI Persona & Design→Build Workflow (3/3 plans) — completed 2026-06-11
- [x] Phase 14: Security Audit (OWASP ASVS) & Checklist Re-Anchor (3/3 plans) — completed 2026-06-13
- [x] Phase 15: TypeScript Tooling Migration (6/6 plans) — completed 2026-06-13
- [x] Phase 16: §14 Gate Convergence — Lint, UI/E2E & Test-Integrity (3/3 plans) — completed 2026-06-14
- [x] Phase 17: Install --migrate / --update (3/3 plans) — completed 2026-06-15
- [x] Phase 18: Browsable Docs Catalog (2/2 plans) — completed 2026-06-15
- [x] Phase 19: Factory Auto-UAT Harness — Tier 1 Oracles + Tier 2 Headless E2E (4/4 plans) — closed 2026-06-16 (A3/DOG-02 live dual-path parity human-waived → next milestone)

Full phase details + milestone summary: `milestones/v1.2-ROADMAP.md` · requirements: `milestones/v1.2-REQUIREMENTS.md` · audit: `milestones/v1.2-MILESTONE-AUDIT.md`

</details>

<details>
<summary>✅ v2.0 Decentralized Factory — Shared Verified Context (Phases 20–26) — SHIPPED 2026-07-28</summary>

- [x] Phase 20: Shared-Context Substrate & Concurrency Foundation (4/4 plans) — completed 2026-06-17
- [x] Phase 21: Verify-Before-Write Admission (4/4 plans) — completed 2026-06-17
- [x] Phase 22: Memory & Trajectory Compaction (9/9 plans) — completed 2026-06-19 (8 rounds; 7 distinct bypasses of one silent-absorb class, closed structurally)
- [x] Phase 23: Parallel Execution & Orchestrator-as-Decomposer (3/3 plans) — completed 2026-06-21 (guard_wr05 inverted)
- [x] Phase 24: Clean Handoff Removal & Traceability Migration (5/5 plans) — completed 2026-06-22 (all 17 handoff templates deleted grep-to-zero)
- [x] Phase 25: Governance-on-a-Dial (13/13 plans) — completed 2026-06-29 (8 rounds; closed on 2 independent red-teams NO_BYPASS + 19/19 self-repro + human approval)
- [x] Phase 26: Dogfood, Dual-Path Oracle & A3/DOG-02 Retirement (6/6 plans) — completed 2026-07-24 (retirement gate correctly held shut — GAP-D1)

Full phase details + milestone summary: `milestones/v2.0-ROADMAP.md` · requirements: `milestones/v2.0-REQUIREMENTS.md` · audit: `milestones/v2.0-MILESTONE-AUDIT.md` · phase artifacts: `milestones/v2.0-phases/`

</details>

### 🚧 v2.1 Autonomous Factory — Real Spawning, Controlled Language & Live Board (Phases 27–33) — ACTIVE

**Milestone goal:** Make the decentralized factory v2.0 designed actually *run* unattended — fix the spawn path so role agents execute in their own sessions, replace ambiguous agent-written prose with a controlled-language profile, let each project decide where a human is involved, and give the operator a live view of the board.

- [x] **Phase 27: Spawn Correctness & Kit-Set Authority** — derive every guard/validator scan set from the filesystem, then generate all 17 role adapters and wire the coordinator where the runtime honors its allowlist (completed 2026-08-11)
- [x] **Phase 28: Kit Consistency Audit** — a real correctness-and-strangeness pass over 17 roles + 19 workflows (17, not 18: `kit-model.listRoles()` drops the `_`-prefixed `_role-switch-protocol.md` by derivation — it is read once and recorded as an explicitly uncounted 37th register row), the `CLAUDE.md` v2.0 drift reconciled, and every public safety claim given an id (completed 2026-08-12)
- [x] **Phase 29: Controlled Language & Voice Guard Rebuild** — an ASD-STE100-derived writing profile for procedural/agent-written surfaces, a de-duplicated role skeleton, and a voice guard that measures voice instead of sentence shape
- [x] **Phase 29.1: Per-Role Model Assignment** *(INSERTED — CLOSED 2026-09-03 by user override D-29.1-19; round-5 verification stands at `gaps_found`, three blockers ACCEPTED OPEN as ledger rows 105-107)* — a stronger model where judgment lives and a cheaper one for execution, set on the config dial and emitted into every generated adapter, with zero-config byte-identical to today's
- [x] **Phase 29.2: Model Assignment Delivery Path** *(INSERTED — split out of 29.1 by D-17)* — how a per-repo `models` block actually reaches an installed target's adapters; until it lands, the block is **inert for an installed repo** and 29.1 says so (completed 2026-09-07)
- [x] **Phase 30: Per-Checkpoint Autonomy Matrix** — every human stop enumerated and dialable, the four safety floors lowerable only behind two keys, with mechanical claim-dropping (completed 2026-09-07)
- [x] **Phase 31: Autonomous Manual Testing** *(CLOSED 2026-09-13 by user override D-44; round-9 verification stands at `gaps_found` 4/6, CR-28…CR-31 + WR-43…WR-47 ACCEPTED OPEN, UATX-01/UATX-06 unchecked)* — browser-driven UAT where the committed Playwright spec is the evidence and the agent's narration never is
- [x] **Phase 32: Board Projector & CLI Dashboard** — one board-grammar authority emitting a typed snapshot, rendered live by a read-only terminal dashboard (completed 2026-09-17)
- [ ] **Phase 32.1: Board Dashboard Deferred Residuals** (INSERTED) — the 16 residuals Phase 32 deferred at its four-round cap (F-14..F-21 + carried ledger rows) closed by canonical form, plus triage of round-4 review WR-01..WR-04
- [ ] **Phase 33: Live Capture & Windows Portability** — the captured live run that proves spawning and discharges GAP-D1, plus a green `windows-latest` leg

**Standing obligations from v2.0, and where each lands:**

| # | Obligation | Phase |
|---|------------|-------|
| 1 | **GAP-D1** — one captured live dual-path run → flip A3/DOG-02 + the coupled `examples/03-ticket-to-pr.md` edit | 33 (CAP-01) |
| 2 | **Windows-portability pass** — `windows-latest` leg green, then flip the Phase-20 human item | 33 (CAP-02) |
| 3 | **`orchestrator.md` size** — 7562B against a 7570B hard FAIL ceiling; trim *before* adding spawn text | 27 (SPAWN-06) |
| 4 | **Fail-safe residuals** — all fail-closed, none silent | 28 (AUDIT-01 disposition) |
| 5 | **Hygiene** — delete `agent-factory/handoffs/.gitkeep`; reconcile `CLAUDE.md` | 28 (AUDIT-01/02) |

## Phase Details

### Phase 27: Spawn Correctness & Kit-Set Authority

**Goal**: Role agents actually execute in their own sessions on Claude Code — and every guard and validator scan set is derived from the filesystem *before* the 17 new adapter files exist, so they land inside the guards rather than outside them.
**Depends on**: Nothing new (builds on the v2.0 substrate, which is unchanged)
**Requirements**: KIT-01, KIT-02, KIT-03, SPAWN-01, SPAWN-02, SPAWN-03, SPAWN-04, SPAWN-05, SPAWN-06, SPAWN-07
**Success Criteria** (what must be TRUE):

  1. `scripts/kit-model.ts` answers "what roles and workflows exist" by reading the filesystem with an asserted count, and every scan set — `WR05_SCAN`, `ADAPTERS`, `CTX_WORKFLOWS`, and the validator's role and workflow lists — resolves through it, so adding a role or workflow file changes every scan set with no hand edit and no stale literal survives. (KIT-01, KIT-02)
  2. The referential-integrity oracle **fails RED against today's tree** — 1 adapter on disk, 7 names granted, 17 roles — before any adapter is authored, and turns green only when the coordinator's spawn grant, the adapter directory, and the role corpus are the same set. (KIT-03)
  3. All 17 role adapters exist at `.claude/agents/grugops-<role>.md`, are produced by the templated generator from `agent-factory/roles/*.md` as thin pointers (never copies of role text), and a byte difference between a committed adapter and a fresh regeneration fails the freshness gate closed. (SPAWN-01, SPAWN-02)
  4. On Claude Code the coordinator runs as the **main-thread** agent so its `Agent(<allowlist>)` grant is honored by the runtime, and no non-coordinator adapter carries the `Agent` tool at all — a mechanism that holds on both the main-thread and subagent paths rather than relying on a frontmatter token the runtime ignores. (SPAWN-03, SPAWN-04)
  5. `guard_adapter_body` fails red on pre-v2.0 handoff/single-window prose anywhere in an adapter body — proven against the surviving `grugops-orchestrator.md:25` reference — `orchestrator.md` sits below its **7570-byte FAIL ceiling with the ceiling unchanged**, and the advertised Claude Code floor reads **v2.1.219+ at depth 3** everywhere it appears, with the v2.1.217–218 depth-1 window documented as a known-bad range that degrades loudly. (SPAWN-05, SPAWN-06, SPAWN-07)

**Plans**: 66/66 plans executed — 61 executed, 5 planned and not yet executed. **Gap-closure round 12 (`27-62`…`27-66`) planned 2026-08-10** after the round-11 verification returned `gaps_found` 8/10 with KIT-03 and SPAWN-04 still FAILED, and it is a deliberate STRUCTURAL PIVOT rather than a twelfth iteration. Under **D-64** the two round-11 findings are **DISSOLVED, not patched**: CR-01 (the block-scalar indentation landmark taken from the physical header line instead of the parent node) and CR-02 (D-61's fourth reference-refusal point wired into one of two `blockHeaderAt` call sites) are each an exotic-YAML construct that a canonical-form gate refuses loudly instead of silently no-granting. **No plan this round widens `scripts/frontmatter.ts`** — rounds 10 and 11 each shipped a regression inside their own fix, and that, not any single finding, is why the mechanism changes. Five plans: `27-62` the canonical admission reader (D-64 Part A), `27-64` the generated and byte-gated skill twins (Part B), `27-63` the rounds-1–11 corpus replayed to named loud refusals, `27-65` the guard cutover behind a blocking decision checkpoint plus the parser's demotion (Part C), `27-66` the disposition register with DISSOLVED as a named class and the traceability hold. **KIT-03 and SPAWN-04 stay `[ ]`/Gaps Found throughout** — only a verification round may flip them (D-58 item 4). SPAWN-03 stays an explicit, dated, owned deferral to Phase 33 / GAP-D1 / CAP-01. *(Prior round for reference:)* **Gap-closure round 11 (`27-55`…`27-61`) planned 2026-08-10** after the round-10 verification returned `gaps_found` 8/10 with KIT-03 and SPAWN-04 still FAILED: round 10's own D-57 fix shipped a NEW REGRESSION (CR-01-new, a sticky key-wide quoting exemption) and left two further node-start shapes live (CR-02, a node property between the mapping indicator and the block indicator — reproduced END TO END at `ALL CHECKS PASSED` exit 0 on a hermetic mirror; CR-03, the top-level key grammar reused for nested mapping keys). All three independently re-reproduced at the module level during planning. Seven plans, strictly sequential: `27-55` CR-01-new, `27-56` CR-03, `27-57` CR-02 with the end-to-end replay inverted, `27-58` WR-01/WR-02/IN-01, `27-59` the derived-axis corpus widening plus WR-03, `27-60` WR-04/IN-02/IN-03, `27-61` the consolidated mutation sweep, the ten-row disposition register and the traceability hold. **KIT-03 and SPAWN-04 stay `[ ]`/Gaps Found throughout** — only a verification round may flip them (D-58 item 4). SPAWN-03 stays an explicit, dated, owned deferral to Phase 33 / GAP-D1 / CAP-01.

Gap-closure round 10 (`27-51`…`27-54`) was planned 2026-08-09 and fully executed 2026-08-09/10: `27-51` closed CR-01, `27-52` closed family G/G2 (D-57), `27-53` closed the three harness-integrity findings (WR-02, WR-03, IN-01), `27-54` recorded D-58 and the round-10 disposition register. Both bypasses were re-measured CLOSED on the post-`27-53` build by `27-54`, at the module, against a `/usr/bin/ruby -ryaml` loader column, and at the gate on hermetic mirrors with both distribution twins named. **KIT-03 and SPAWN-04 nonetheless remain FAILED and are deliberately held `[ ]`/Gaps Found**: a requirement's verified status is a verification round's call (D-58 item 4), so the next verification round owns the flip. SPAWN-03 stays an explicit, dated, owned deferral to Phase 33 / GAP-D1 / CAP-01.
Plans:
**Wave 1**

- [x] 27-01-PLAN.md — `scripts/kit-model.ts` derivation authority (explicit root arg, throws on vacuity, exact two-sided counts) proven end-to-end through `ROLE_FILES`; the KIT-03 referential-integrity oracle wired in and **failing RED against today's tree**, with RED/GREEN fixture tests [KIT-01, KIT-03]
- [x] 27-02-PLAN.md — installer and uninstaller self-derive their adapter/skill sets by readdir of `$GRUGOPS_SRC`, materialize-vs-copy routed by the resolver slot, uninstall intersected with the target so user-authored agents survive [KIT-02]

**Wave 2** *(blocked on 27-01)*

- [x] 27-03-PLAN.md — `check-foundation-guards.ts`: derive `ADAPTERS` (+ vacuity floor and skill count), rename and derive the spawn-grant scan set to kill the cross-file `WR05_SCAN` collision, derive `CTX_WORKFLOWS` 16→19; per-consumer assertions + the committed literal inventory [KIT-02]
- [x] 27-04-PLAN.md — `validate-agent-factory.ts` derives its frozen 14/16 lists (extension stripped at the call site); `check-kit-refs.ts` reaches `.claude/agents` as a directory, derives `MARKER_SITES` (D-27), and restates Assertion 3 as a two-sided derived predicate [KIT-02]
- [x] 27-05-PLAN.md — trim `orchestrator.md` below the 7165-byte WARN tier with the ceiling unchanged and no relocation; capability-keyed spawn instruction; depth-3 + tuning env var + real concurrency cap across the remaining surfaces, table asymmetry preserved [SPAWN-06, SPAWN-07]

**Wave 3** *(blocked on 27-05)*

- [x] 27-06-PLAN.md — `capabilities:` inline-scalar frontmatter on all 17 roles (closed vocabulary, background-subagent-safe); packaging template defines the specialist body, the coordinator body carrying the honest three-tier announcement (Full / Reduced / Degraded) selected by capability-sensing, and the capability→tool mapping [SPAWN-01]

**Wave 4** *(blocked on 27-01, 27-03, 27-06)*

- [x] 27-07-PLAN.md — `generate-role-adapters.ts` + the 17 committed thin-pointer resolver adapters (one coordinator with a generated 16-name grant, no spawn token on any other), turning the KIT-03 oracle GREEN; `adapters-freshness.ts` byte-and-set gate [SPAWN-01, SPAWN-02, KIT-03]

**Wave 5** *(blocked on 27-03, 27-04, 27-07)*

- [x] 27-08-PLAN.md — `dead-vocabulary.ts` as the one retired-vocabulary source (two justified consumers); `guard_adapter_body` both directions over the derived scan set + template; `guard_wr05` asserts tier-announcement presence and no non-coordinator spawn token [SPAWN-04, SPAWN-05]

**Wave 6** *(blocked on 27-07, 27-08)*

- [x] 27-09-PLAN.md — document the three entry tiers and the deliberate absence of main-thread wiring (`claude --agent grugops-orchestrator` as the full-capability path, no settings-parity claim); assert the in-repo half by command and verify the runtime half by hand against a real session [SPAWN-03]

**Gap closure** *(from `27-VERIFICATION.md` — 5 failed truths, 1 human-needed, 7 warning-class items; waves restart at 1 because the nine plans above are complete)*

**Gap wave 1**

- [x] 27-10-PLAN.md — TRACER: one recursive adapter-set authority in `kit-model.ts` (`listAgentAdapters`/`listSkillAdapters`), the foundation guards' two duplicate directory reads deleted, a nested adapter refused by name, and the planted nested-coordinator bypass turned RED [KIT-02, KIT-03]

**Gap wave 2** *(blocked on 27-10)*

- [x] 27-11-PLAN.md — the adapter freshness gate consumes the authority, gains a hermetic kit root, gets a five-case test file, and is wired into the ubuntu-only CI gate block; `check-kit-refs` drops its duplicate derivation [KIT-02, SPAWN-02]
- [x] 27-12-PLAN.md — `scripts/frontmatter.ts` as the one format-aware frontmatter-value and fence authority; the line-anchored grant grammar deleted from both consumers; the folded-scalar bypasses turned RED; parser-oracle fuzz across the YAML scalar forms [SPAWN-04, KIT-03]
- [x] 27-13-PLAN.md — installer/uninstaller symmetry: one fail-loud contract for one derivation, a nested-source refusal, the runnable mirror made removable, and the control byte that broke grep over the installer replaced [KIT-02]

**Gap wave 3** *(blocked on 27-12)*

- [x] 27-14-PLAN.md — `guard_adapter_body` hardened: the positive half anchored to full sentences and counted exactly once, the packaging template checked against the fenced body shapes the generator copies, a vacuity floor that can fire, and the literal inventory's completeness claim made true [SPAWN-05]

**Gap wave 4** *(blocked on 27-14)*

- [x] 27-15-PLAN.md — the shipped capability announcement names a command that exists; three sources corrected, adapters regenerated inside a stated byte budget with both ceilings unchanged, and the command name pinned by a guard beat [SPAWN-01]

**Gap wave 5** *(blocked on 27-15)*

- [x] 27-16-PLAN.md — `coordinator-resolution-precheck.ts` discharges every observable precondition of the runtime check and prints the two human-only steps verbatim; one recording surface shipped empty and marked unverified [SPAWN-03]

**Gap wave 6** *(blocked on 27-16)*

- [x] 27-17-PLAN.md — blocking human-verify checkpoint: observe a real session, transcribe the observation verbatim, and update the requirement's status only if it matched [SPAWN-03]

**Gap closure round 2** *(from `27-REVIEW-GAPS.md` — a code review of the gap-closure half itself, diff `7f8d016..HEAD`: 3 critical, 5 warning, 2 info, all 10 scoped in. Every critical was reproduced on a hermetic mirror printing `ALL CHECKS PASSED`.)*

**Gap wave 7**

- [x] 27-18-PLAN.md — TRACER: the frontmatter authority refuses YAML anchors, aliases and merge keys instead of reading them as a clean no-grant, proven end-to-end from the module through the committed `.js` to the aggregator on the skill surface; the parser oracle gains a refused-form product with its own cardinality pin [SPAWN-04, KIT-03] *(CR-01)*

**Gap wave 8** *(blocked on 27-18)*

- [x] 27-19-PLAN.md — KIT-03 stops comparing filenames against frontmatter names without asserting the two agree; the identity authority is named, the founding defect stops reproducing, and the fixtures can express the namespace split [KIT-03, KIT-02] *(CR-02)*
- [x] 27-21-PLAN.md — the installers' INCOMPLETE banner gains a machine-readable exit code behind a blocking decision checkpoint, every test pin is enumerated from source and moved with the contract, and the coordinator precheck refuses on the banner as well as the status [KIT-02, SPAWN-03, SPAWN-07] *(WR-01)*
- [x] 27-23-PLAN.md — the adapter generator's second frontmatter grammar is deleted for byte-identical output, the freshness gate stops leaking its temp mirror, and `check-kit-refs` + `validate-agent-factory` gain direct CI steps [SPAWN-01, SPAWN-02, KIT-02, SPAWN-06] *(WR-03, IN-01, IN-02)*

**Gap wave 9** *(blocked on 27-19 and 27-21 — shared files, not shared logic)*

- [x] 27-20-PLAN.md — `guard_wr05`'s tier beats get the same comment-stripped, occurrence-counted treatment `guard_adapter_body` already has, and an agent adapter declaring no `tools` key becomes a named finding rather than a compliant silence [SPAWN-05, SPAWN-04] *(CR-03, WR-05)*
- [x] 27-22-PLAN.md — the installer's source derivation resolves a symlink the way the authority and the platform do, with a conformance fixture that can see the shape; `mappingDests` derives its own cardinality so a missed entry fails the parse [KIT-01, KIT-02] *(WR-02, WR-04)*

**Gap closure round 3** *(from `27-VERIFICATION.md` dated 2026-07-30T19:30:00Z — `gaps_found`, 7/10 clean, 3 partial with live reproduced defects, plus `27-REVIEW-GAPS-2.md`'s 4 Critical + 1 Warning. Waves restart at 1 because plans 27-01..27-23 are all executed. WR-03 and IN-02 are explicitly out of scope this round.)*

**Gap wave 1**

- [x] 27-24-PLAN.md — TRACER: the frontmatter authority refuses a YAML **tag** standing in front of a reference, closing the fail-open that returned in a new spelling; proven end-to-end from the module through the committed `.js` to the aggregator on the skill surface, with the tag axis enumerated by shape and both cardinality pins raised [KIT-03, SPAWN-04] *(CR-01)*

**Gap wave 2** *(blocked on 27-24)*

- [x] 27-25-PLAN.md — the `install.ts`/`uninstall.ts` derivation pair collapses into one shared `install/kit-source.ts` both import (D-28), still never importing `scripts/kit-model.ts`; the uninstall round-trip fixture gains the symlinked adapter and symlinked skill directory it previously only claimed [KIT-02] *(CR-02, WR-02)*

**Gap wave 3** *(27-26 blocked on 27-24; 27-27 blocked on 27-25 — split by shared file, not shared logic)*

- [x] 27-26-PLAN.md — the `tools`/`allowed-tools` key gets the cardinality arm its sibling `name` key received in 27-19, so a duplicate declaration is refused by name and count on both key spellings instead of passing both guards with the grant silently gone [SPAWN-04, KIT-03] *(WR-01)*
- [x] 27-27-PLAN.md — one cycle answer at both walk sites (D-29): a per-path ancestor stack replaces the installer's global visited set and `kit-model`'s absent guard, so a cycle terminates by contract and a directory reachable by two paths contributes both members [KIT-01, KIT-02] *(CR-03, D-29)*

**Gap wave 4** *(blocked on 27-25 and 27-27)*

- [x] 27-28-PLAN.md — the uninstaller implements the self-checkout refusal its README already publishes, on a marker that can actually fire, closing a reproduced data-loss path; every exit-code row becomes true of the binaries it claims to cover [KIT-02] *(CR-04, IN-01)*

**Ordering that is load-bearing in gap-closure round 3** — the tracer (27-24) closes the shared parser fail-open and establishes the RED-before / GREEN-after proof pattern before any expansion; 27-25's structural collapse must land before 27-27 can give the moved walk its cycle treatment and before 27-28 can add a refusal to the restructured uninstaller. Waves 3 and 4 are split by shared FILE rather than shared logic: 27-24 and 27-26 both edit `check-foundation-guards.test.ts`, and 27-25, 27-27 and 27-28 all edit `install/install.test.ts`.

**Gap closure round 4** *(from `27-VERIFICATION.md` dated 2026-07-31T13:00:00Z — `gaps_found`, 7/10 clean, with ONE live independently-reproduced BLOCKER shared by KIT-03 and SPAWN-04 plus four warning-tier KIT-02 residuals, against `27-REVIEW-GAPS-3.md`'s 1 Critical + 4 Warnings + 3 Infos. Waves restart at 1 because plans 27-01..27-28 are all executed. **D-31: nothing from round 3 is deferred — all eight findings close this round.**)*

**Gap wave 1**

- [x] 27-29-PLAN.md — TRACER: the frontmatter authority's double-quoted branch is INVERTED to an escape ALLOWLIST (D-30), so a backslash spelling nobody enumerated refuses BY DEFAULT instead of being deleted into a value no YAML loader computes; proven RED-before / GREEN-after against the committed `.js` at module and aggregator level, with an exhaustive dependency-free escape-alphabet property and both false-red controls [KIT-03, SPAWN-04] *(CR-01)*

**Gap wave 2** *(both blocked on 27-29; disjoint files, run in parallel)*

- [x] 27-30-PLAN.md — the second silent-SUCCESS arm in the same module is closed: a leading YAML directive line is refused by name instead of reading as "no frontmatter, no keys" (D-34), and the agent-adapter scoping gate gets the negative control that makes it load-bearing [KIT-03, SPAWN-04] *(IN-02, IN-01)*
- [x] 27-31-PLAN.md — both recursive walks get a WORK bound explicitly separate from the per-path cycle answer (D-35), and the cycle arm stops dropping members silently (D-36): reported as a verification finding in the installer, thrown as a named error in the kit-set authority, each matching that side's documented floor [KIT-01, KIT-02] *(WR-01, WR-04)*

**Gap wave 3** *(blocked on 27-31 — shared `install/install.ts` and `install/install.test.ts`)*

- [x] 27-32-PLAN.md — the self-checkout marker pair collapses into one exported constant naming the RUNTIME artifact, with a read-only real-repository existence assertion as the forcing function CR-04 never got (D-37); the stale duplicate-walk rationale is amended to D-28's and the equality it appeals to becomes a case (D-38); the reversal docs match the always-on refusal [KIT-02] *(WR-02, WR-03, IN-03)*

**Gap wave — round 5, wave 1**

- [x] 27-33-PLAN.md — the delimiter positions of `scripts/frontmatter.ts` DECLARE THE ONE LEGAL SPELLING (the payload followed only by `[ \t]`) and refuse everything else, whatever follows — graphic, invisible, combining, unassigned, private-use, or another dash (D-39, D-43); pinned by a NON-CIRCULAR sweep drawn from the negative space of the rejected alphabet and by a false-red control that consumes the one exported scan composition and asserts set equality; the grant enumeration refuses a nested paren or a quote instead of returning a short or altered list (D-41) [KIT-02, KIT-03, SPAWN-04] *(CR-01, WR-02)*

**Gap wave — round 5, wave 2** *(blocked on 27-33 — its aggregator-level case exercises the parser refusal 27-33 lands, and it widens the composition 27-33 creates)*

- [x] 27-34-PLAN.md — the shipped plugin-form `skills/` tree is derived from `kit-model` with an asserted cardinality, folded into the single exported spawn-grant scan composition, and named in `guard_wr05`'s PASS line, pinned by a plant case that targets a SKILL adapter; the plugin-default component directories `agents/` and `commands/` get an absence-or-coverage floor that closes the CLASS rather than the instance; the two distribution forms' relationship is asserted mechanically with a discriminating wrong-name case and the one legitimate divergence exempted by name (D-40); the name floor stops reporting "no name key" over a document with no frontmatter block at all (D-41) [KIT-02, KIT-03, SPAWN-04] *(CR-03, name-floor misdiagnosis)*

**Gap wave — round 5, wave 3** *(BUILD serialization, not a logical dependency — see below)*

- [x] 27-35-PLAN.md — the nested-adapter walk gains a fourth channel for directories it cannot read, both bare returns route through it, and the installer refuses them by name at exit 3, so a less readable tree no longer produces a more confident installer; the cross-site equality case extends to the unreadable arm, and both remaining exit-after-report tails set the exit code with the regression scan extended from two paths to four (D-41) [KIT-02] *(CR-02, WR-01)*

**Gap wave — round 6, wave 1**

- [x] 27-36-PLAN.md — the delimiter region's TWO REFUSAL ARMS collapse into ONE TOTAL CLASSIFIER whose verdict every call site consumes exhaustively under a compiler-checked never-branch (D-44), so the composite that satisfied neither arm — measured today, `ZWSP + --- + ZWSP` and seven siblings return the silent no-grant arm and flip the gate from exit 1 to `ALL CHECKS PASSED` over a live spawn grant — refuses by name at BOTH positions, killing the closing position's misleading unterminated-block diagnosis with it; pinned by a THREE-AXIS CROSS-PRODUCT sweep (leading × payload × trailing) enumerated as data with an expected-verdict rule that never calls the code under test, swept at both positions and both closing payload tokens, RED before and GREEN after (D-45) [KIT-03, SPAWN-04] *(round-5 CR-01, WR-02)*

**Gap wave — round 6, wave 2** *(BUILD serialization, not a logical dependency — file-disjoint from 27-36)*

- [x] 27-37-PLAN.md — the plugin-root component surface is DERIVED from the manifest schema CLAUDE.md documents, counted two-sided at nine, and partitioned exhaustively into seven FORBIDDEN, one covered elsewhere by a named function (`skills`), and one EXEMPT by name with two live bounds (`hooks/`, which exists on the live tree, executes commands via `PreToolUse`, and sat in no scan set at all) — replacing a hand-listed 2-of-9 literal inside a floor whose own comment claimed class-level closure; proven by three hermetic plants going from `ALL CHECKS PASSED`/exit 0 to exit 1 (D-46); and the counts guard stops silently swallowing a thrown per-part lister and stops claiming a set equality it skipped (D-47) [KIT-02] *(round-5 CR-02, WR-01)*

**Gap wave — round 6, wave 3** *(shares `scripts/frontmatter.ts` with 27-36; also build-serialized behind 27-37)*

- [x] 27-38-PLAN.md — the grant enumeration states ONE legal character set and refuses everything else, replacing the two ENUMERATED checks for a nested paren and a quote, so a flow-collection delimiter stops returning split, altered names on the success arm — measured today, `Agent(alpha[,]b, gamma)` yields three names, one invented and one lost, on the arm whose doc block promises a name is never dropped or altered; the unreachable escape branch's note is made true by a case rather than a comment, and the false-red cost is measured across every real enumeration in all 33 scan members (D-47) [KIT-03, SPAWN-04] *(round-5 IN-04)*

**Gap closure round 7** *(from `27-REVIEW-GAPS-6.md` dated 2026-08-04 — `issues_found` with **1 Critical**, 3 Warnings and 5 Infos, every finding constructed as a concrete input and run against the committed build, and CR-01 reproduced end to end on two hermetic `git archive HEAD` mirrors at `ALL CHECKS PASSED` exit 0. Waves restart at 1 because plans 27-01..27-38 are all executed. **D-50: nothing from round 6 is deferred — all nine findings close this round.** All four waves are single-plan: every plan of this round touches `scripts/frontmatter.*`, and `npm run build` is a whole-project `tsc` emit that rewrites every committed `.js`.)*

**Gap wave — round 7, wave 1**

- [x] 27-39-PLAN.md — TRACER: quote state is a property of the YAML SCALAR and not of the physical line, so it is CARRIED across a scalar's continuation lines instead of re-derived at every line boundary (D-48), closing all THREE directions of one defect in one edit — a `#` on a continuation line deleting a live `Agent(grugops-orchestrator)` grant (measured: three spellings return `{ok:true,value:false}` while libyaml returns the grant; the block-sequence spelling is the exact idiom all 7 skills and all 17 adapters use, and planted on both distribution twins it prints `ALL CHECKS PASSED` at exit 0), `*`/`!`/`&` on a continuation line failing RED on documentation a real loader accepts, and a `-` on a continuation line read as a new sequence item, which flips the whole key's join separator and makes `keysGrantedAgentNames` INVENT a name on its success arm; pinned by a FOURTH AXIS the three D-45 axes cannot see — scalar style × sigil × placement, 90 cells enumerated from outside the rule with every continuation cell RED before and GREEN after and every platform claim resolved against a real YAML 1.2 loader (D-49) [KIT-03, SPAWN-04] *(round-6 CR-01, WR-01, + the JOIN direction named in no review)*

**Gap wave — round 7, wave 2** *(shares all three `scripts/frontmatter.*` files with 27-39)*

- [x] 27-40-PLAN.md — the delimiter classifier stops discarding INDENTATION: the leading run gains one extra LABEL (never a second composable predicate — D-44 deleted that shape), so an indented `---` or `...` inside a legitimate block scalar or a wrapped value is content rather than a refusal of the whole document — measured, three files libyaml loads cleanly are refused today, the cheapest being a wrapped `description:` whose continuation starts with an ellipsis; the change is position-asymmetric on purpose and the reason is mechanical (at the closing position `not-a-delimiter` means *keep scanning* and the fallback is the unterminated-block REFUSAL, so no silent success can be created; at the opening position it means the keyless success arm, so indentation is not routed there); and `unquoteChecked` stops being applied inside a `|`/`>` block scalar, where YAML applies no quoting rules at all — which also stops a block-scalar `coordinator: "true"` from matching the coordinator marker (D-50) [KIT-03, SPAWN-04] *(round-6 WR-02, IN-02)*

**Gap wave — round 7, wave 3** *(shares all three `scripts/frontmatter.*` files with 27-39 and 27-40)*

- [x] 27-41-PLAN.md — `keysGrantedAgentNames` ACCOUNTS for every spawn-token occurrence in a value instead of only examining captures that formed, so a truncated enumeration stops impersonating a genuine unscoped grant — measured, `Agent(alpha, gamma`, `Agent(alpha, #b, gamma)` and `Read, Agent` are three different facts returning one answer on the success arm, while the function's own doc block claims the enumeration is examined before it is split; every occurrence lands in exactly one of three stated buckets with the arithmetic identity asserted, `SCOPED_GRANT` and `keysHaveSpawnGrant` byte-unchanged; and the prologue blank-line skip stops asking *is this line empty* with `String.prototype.trim()`, whose alphabet is narrower than the one this module declares — the THIRD application point of the defect D-39/D-42/D-43 spent two rounds correcting, measured as a lone ZWSP or SOFT HYPHEN hiding a live grant behind a keyless success while a lone NBSP does not (D-50) [KIT-03, SPAWN-04] *(round-6 WR-03, IN-01)*

**Gap wave — round 7, wave 4** *(Task 3 edits `scripts/frontmatter.ts`, which all three earlier plans own; also build-serialized)*

- [x] 27-42-PLAN.md — the three claim-accuracy findings get their assertions rather than better sentences: the partition floor's `unclaimedKeys` arm, unfalsifiable by construction because the forbidden set is computed as `schema \ (covered ∪ exempt)`, is extracted into a pure function so a case can feed it a hole and watch the arm fire, with the extraction proven faithful by a BYTE-IDENTICAL gate PASS line; `coverer` stops being a free-text string the gate prints as a coverage claim and becomes the lister FUNCTION resolved against `SPAWN_GRANT_SCAN_PARTS` by object identity, with the printed label derived from the resolution, and both bucket cardinalities move from vitest into the GATE two-sided so the exemption's own recorded promote trigger fires where it stops a release; and `frontmatter.ts`'s tree-wide one-grammar claim is SCOPED to the surfaces a guard reads with that scope DERIVED by a pattern scan — a third grammar fails red — rather than reworded, with the decision not to migrate `generate-catalog.ts` recorded with its reason (D-50) [KIT-02, KIT-03] *(round-6 IN-03, IN-04, IN-05)*

**Gap closure round 8** *(from `27-REVIEW-GAPS-7.md` dated 2026-08-08 — `issues_found` with **1 Critical**, 2 Warnings and 5 Infos, the critical reproduced end to end on three shipped surfaces with the whole foundation gate printing `ALL CHECKS PASSED` at exit 0 while a real YAML loader reads the grant, plus 4 bypasses and 0 false reds across a 240-cell libyaml differential. **D-53: all EIGHT findings close this round, none deferred** — the fourth application of the D-41/D-47/D-50 posture. Waves restart at 1 because plans 27-01..27-42 are all executed. All four waves are single-plan: three of them edit `scripts/frontmatter.*` and the fourth is build-serialized because `npm run build` is a whole-project `tsc` emit that rewrites every committed `.js`.)*

**Gap wave — round 8, wave 1**

- [x] 27-43-PLAN.md — TRACER: the comment scanner becomes the ONE authority on what quote state may CROSS a line boundary (D-51) — it is told whether the line's offset 0 is a node start, it tracks flow-collection depth as it walks, and it returns an ALREADY-GATED open quote, so the three seeding sites collapse to one unconditional assignment each and the separate node-start-quote predicate is DELETED rather than kept alongside; 27-39 promoted quote state to the scalar and gated the carry correctly but wired the SEEDING into only two of the three places a node can begin and into none of the places a node begins mid-line, so the union of its three arms was not the set of node starts — measured against the committed `.js` with a libyaml column, six documents carrying a live `Agent(grugops-orchestrator)` grant return `{ok:true,value:false}` (a key line with no value in both quoting styles, a key line carrying only a comment, a flow sequence, a flow mapping, and a block-sequence item whose dash line carries no value, the last of which also INVENTS a comma the document does not express), and the reviewer's measured one-liner is rejected BY NAME because it closes family (a) and leaves the flow-collection family returning the no-grant success arm; IN-03 closes in the same edit because D-51 is what makes the item path's seed a genuine read, with the invariant asserted at the site; pinned by axis 1's missing node-start placements (D-52) with every cell RED before and GREEN after, the repository-wide value map re-measured at zero new refusals, and all three shipped surfaces reproduced [KIT-03, SPAWN-04] *(round-7 CR-01, IN-03)*

**Gap wave — round 8, wave 2** *(shares `scripts/frontmatter.test.ts` with 27-43)*

- [x] 27-44-PLAN.md — the sweep's completeness claim moves OUT of the hand-listed axes and into a differential harness whose corpus is GENERATED and whose expected value is `/usr/bin/ruby -ryaml` cell by cell (D-52), with libyaml-rejected cells skipped and the skip PRINTED: every one of the six shipped scalar styles opens its scalar as the first token after the key on the key line or as the first token of a block-sequence item, so NEITHER CR-01 family was expressible and the sweep passed green at 90 cells over a live bypass, exactly as the round-6 sweep did — a cardinality assertion pins a list against shrinking and says nothing about incompleteness, and a truth table whose completeness claim is the product of two hand-listed axes is a claim about the axes rather than about the construct; the harness is proven a pin rather than decoration by running its own corpus against a `git archive` mirror of the pre-27-43 build and recording a non-empty disagreement set containing a cell from each family, its disagreement set is asserted EQUAL to a named exemption set with every exemption asserted to be in the safe direction only, and the loader is invoked once per run so its runtime never becomes the reason a later author narrows it; IN-02 closes here too — both construct arrays of the one-grammar detector gain cardinality pins and each construct gets a planted fixture that fails without it [KIT-03, SPAWN-04] *(round-7 WR-01, IN-02)*

**Gap wave — round 8, wave 3** *(shares all three `scripts/frontmatter.*` files with 27-43 and 27-44)*

- [x] 27-45-PLAN.md — the fence authority stops running over the whole document BEFORE the frontmatter region is located, so a column-0 fence inside the region no longer deletes content and returns the truncated result on the SUCCESS arm (D-53): measured, one document loses its whole `tools` key and another loses the token from its value, both on the success arm, while libyaml rejects both outright — scoped HONESTLY as a CONTRACT defect and NOT a confirmed live bypass, since the one spelling libyaml accepts the module already refuses in the safe direction; the region is located first and the strip's scope SHRINKS to the body used for the prose checks, never widens, with the design settled per document by the loader rather than by preference and the repository-wide cost re-measured with the module's own classifier (the planner's approximation: over 1136 tracked markdown files the located region is identical under both orderings, and zero of the 557 files opening with a raw delimiter carry a fence inside their region); IN-01 closes by extracting the spawn-occurrence balance comparison into a pure exported function a case can hand a FOURTH, unclassified kind, with the refusal asserted to fire by name and the wording proven byte-unchanged — 27-42's own remedy, applied to the arm 27-41 shipped anew in the same round; and IN-05 is RECORD-DON'T-FIX, dispositioned in the module header with its measurement (the module reads one region; libyaml's stream parser reads three documents, the second carrying the grant), an explicit `UNKNOWN - verify`, and an explicit statement that it is not claimed as a bypass [KIT-03, SPAWN-04] *(round-7 WR-02, IN-01, IN-05)*

**Gap wave — round 8, wave 4** *(build serialization only — no source file, test file or committed artifact is shared with the three plans above)*

- [x] 27-46-PLAN.md — the plugin-component claim partition's two arms share ONE de-duplication discipline (D-53): the double-claimed arm de-duplicates implicitly because it filters over the schema's keys, the foreign arm filtered over the claims and inherited their multiplicity, so a key claimed by two buckets AND absent from the schema is interpolated TWICE into the guard's failure message — measured against the committed `.js` as the same key returned twice, with only the single-occurrence shape pinned today so nothing observes the duplicate; the arm now reports each non-schema claimed key at most once in first-occurrence order, deterministic rather than incidental because a non-reproducible guard message would trade a new defect for a cosmetic one, with the multiplicity DROPPED rather than preserved in a second field; the behaviour-preserving half is PROVEN by a byte-identical `kit counts:` PASS line and the whole-project compile this plan forces is proven not to have moved a parser cell by re-running 27-44's differential and 27-43's three surface reproductions at the end of the round [KIT-02, KIT-03] *(round-7 IN-04)*

**Gap closure round 9** *(from `27-REVIEW-GAPS-8.md` dated 2026-08-09 — `issues_found` with **2 Criticals**, 5 Warnings and 4 Infos, CR-01 reproduced end to end by both the reviewer and the orchestrator on hermetic mirrors with the whole foundation gate printing `ALL CHECKS PASSED` at exit 0 over a live `Agent(grugops-orchestrator)` grant planted on both distribution twins of a non-coordinator skill. **D-56: all ELEVEN findings close this round, none deferred** — the fifth application of the D-41/D-47/D-50/D-53 posture, plus two recorded dispositions the round-8 verification carried forward as open human decisions. Waves restart at 1 because plans 27-01..27-46 are all executed. All four waves are single-plan: three of them edit `scripts/frontmatter.*` and the fourth is build-serialized because `npm run build` is a whole-project `tsc` emit that rewrites every committed `.js`.)*

**Gap wave — round 9, wave 1**

- [x] 27-47-PLAN.md — the walk's node-start answer becomes a property of STRUCTURAL POSITION decided in ONE place (D-54): `stripComment` raises its node-start flag only for flow constructs and only in the spellings D-51's red team enumerated — the entry separator and the explicit-key indicator gated on `depth > 0`, the mapping separator gated on `depth > 0` AND a following whitespace — so four node-start positions YAML defines sit outside its union and each reproduces the identical silent no-grant bypass at exit 0 (block-mapping `key: value` on an indented line; compact nested sequence `- - `; block explicit key `? ` at depth 0; JSON-like `:` adjacency in a flow mapping, which is INSIDE the flow domain D-51 was written to own); the fix removes conditions the grammar never had rather than adding a fifth arm — the two block-legal indicators lose their flow gate, the separation rule gains `jsonLikeKeyJustClosed` tracked in the SAME walk (raised at a closing quote or a closer at depth, untouched by whitespace so the spaced spelling behaves as the loader behaves), and the item path learns a dash consumes exactly one level by re-entering the byte-unchanged item rule; the user considered and REJECTED BY NAME replacing the hand-rolled grammar with a real YAML parser as a dev dependency, so the dev-dep line stays `{typescript, vitest}`; acceptance evidence is the FOUR gate transcripts flipping exit 0 → exit 1 on both distribution twins, never a green suite, with all eight measured rows RED-before/GREEN-after against a libyaml column and the opposite unsafe direction (module grants where the loader does not) asserted EMPTY over a derived loader-adjudicated corpus [KIT-03, SPAWN-04] *(round-8 CR-01)*

**Gap wave — round 9, wave 2** *(shares all three `scripts/frontmatter.*` files with 27-47)*

- [x] 27-48-PLAN.md — the accumulator's key-line fact is renamed to what it MEANS and set at BOTH places a node can begin (D-55): it is written exactly once, on the key line, so a value whose node begins on the first continuation line is never marked and the node-start expression stays true for every subsequent continuation — the field's own doc block states the correct rule and the code implements it for one of the two places; three measured directions fall out of that single omission and they are not the same direction — an INVENTED NAME on the `ok:true` arm (`["alpha","ga","mma"]` where the document expresses `["alpha","ga - mma"]`) fed straight into the KIT-03 closure equality and coordinator-resolution-precheck, both of which are trusted because this module promises a name is never silently dropped or altered; module GRANTS where the loader has none, the D-52 harness's own declared never-exemptible direction, live today on two spellings; and false refusals on documents libyaml loads cleanly, currently ENSHRINED as a "safe-direction contract" whose framing retired the only signal that the other two existed; the fix sets the fact at both node-begin sites with ONE deliberate exception stated at its site — a block sequence admits a node start at every item, so the item path records a `seqIndent` rather than raising the fact — and the (c) contract is RETIRED with its rows re-adjudicated against the loader; WR-03 closes here too, because both round-8 harnesses agree with the loader on TOKEN PRESENCE only and the fact KIT-03 is computed over is the NAME SET, so both gain a second predicate over the same already-loaded values with the same three-verdict discipline, proven load-bearing by a name-set disagreement transcript against the pre-fix build [KIT-03, SPAWN-04] *(round-8 CR-02, WR-03)*

**Gap wave — round 9, wave 3** *(shares `scripts/frontmatter.test.ts` with 27-47 and 27-48)*

- [x] 27-49-PLAN.md — the three warnings that explain WHY nine rounds shipped green (D-56 items 1–3): the D-52 differential moved the EXPECTATION to the loader and left the CORPUS as three hand-listed axes fixed at "key line plus exactly two continuation lines", so NEITHER round-8 critical finding is expressible in its 312 cells — not merely untested — and the harness reported zero disagreements over a live gate-level exit-0 bypass because it never generated the input; the key-line axis gains the seven shapes YAML's grammar names rather than the shapes a review reported, continuation DEPTH becomes a fourth axis, and a non-circularity floor derives the failure-family ledger from `scripts/frontmatter.ts`'s header at run time with a two-sided count and asserts every entry EXPRESSIBLE BY THE GENERATOR, by construction rather than by comment; WR-02's sweep titled "no mid-line node start YAML defines returns the SILENT no-grant arm" backs a universal quantifier with a hand-listed array and the claim is FALSE (two flow-collection families are counterexamples), so it is derived from YAML's flow node-start productions with a two-sided cardinality or retitled to its bound with the universal claim relocated and named at both sites — the executor MEASURES the array's length rather than carrying any number from the review; WR-04's exemption bounds are arithmetically incapable of failing because each bound is the full cross-product its shape produces and each match rule is a pure function of the same axis flags, so the bound is derived from something the exemption cannot control or DELETED with the deletion stated, and the disagreement/exemption EQUALITY — which turns red when an exempt cell agrees, whose cheapest repair is narrowing the exemption — is split into no-unexplained-disagreement AND no-dead-exemption, both proven capable of firing by constructed inputs [KIT-03, SPAWN-04] *(round-8 WR-01, WR-02, WR-04)*

**Gap wave — round 9, wave 4** *(build serialization plus `scripts/frontmatter.*`; also the only plan touching `scripts/kit-model.*` and `scripts/generate-role-adapters.test.ts`)*

- [x] 27-50-PLAN.md — the last five findings and the two carried decisions (D-56 items 4–10): WR-05's leading-residue refusal interpolates the LINE's first code point, so ` <ZWSP>---` reports U+0020 and sends a reader to a legal character — the scan that found the offender already visits it and discards it, so the residue arm carries `firstOutsideDeclared` out and the refusal names it, with the field unrepresentable on the non-residue arms; IN-01's markdown-extension constant is referenced at exactly ONE site while its literal is spelled at two others in the file whose thesis is that a fact has one statement, so the constant is used everywhere and the literal's occurrence count is derived and asserted two-sided; IN-02's double-claim arm filters over the schema keys only, so after 27-46 a FOREIGN key claimed by two buckets is unreportable rather than reported once and a shipped case pins that blind spot as expected behaviour — the arm covers the union of schema and claimed keys and the guard's `kit counts:` output is proven byte-identical on the live tree; IN-03's purity case slices the module with a newline-brace search and never asserts the slice is the function, so a reformat makes every negative assertion pass vacuously — the premise is asserted before inspection and proven load-bearing by a constructed short slice; IN-04's role-adapter fixture strips fence DELIMITERS and leaves fenced CONTENT live, guarded only by "at least one line was removed", so fenced blocks are removed WITH their contents and the written file's premise is asserted before writing, with both sibling cases' diagnoses compared before and after; and the two DECISIONS are recorded with their reasons in the phase's deferred-items artifact rather than only in a summary — the 27-43 acceptance criterion is RETIRED (the structure validator carries none of the spawn-grant vocabulary, MEASURED at execution time, and forcing it would add the weaker-duplicate predicate this phase has spent nine rounds deleting, so `scripts/validate-agent-factory.ts` is deliberately untouched) and SPAWN-03's live capture stays DEFERRED to Phase 33 under GAP-D1 as `UNKNOWN - verify`, with no static gate invented to stand in for it [KIT-03, SPAWN-02, SPAWN-04] *(round-8 WR-05, IN-01, IN-02, IN-03, IN-04, plus the two carried dispositions)*

**Gap wave — round 10, wave 1** *(from `27-REVIEW.md` CR-01/WR-01..03/IN-01..02 and `27-VERIFICATION.md`'s two remaining live bypasses; the tracer, and the only plan that may touch `stripComment`'s quote arms)*

- [x] 27-51-PLAN.md — CR-01 closed STRUCTURALLY: `stripComment` treats YAML's `''` inside an OPEN single-quoted scalar as close-then-reopen when it is the escaped apostrophe — CONTENT — and the reopen re-runs the node-start capture at a position where the flag is already false, destroying the provenance of a scalar that is still open, so `exiting()` reports no open quote, the next physical line is read as a fresh node start, a leading `#` strips it, and the truncated remainder returns on the `{ok:true}` SUCCESS arm over a live `Agent(grugops-orchestrator)` grant; the fix REMOVES an arm's ability to decide rather than adding one — the walk must DERIVE its scalar-closing set from the quote style's own escape rule exactly as it already does for the other style, and `unquoteChecked` has always agreed — with the arm count of the chain asserted UNCHANGED; landed together with WR-01, because the D-52 loader differential opens a single-quoted scalar mid-line ZERO times and emits the escape on no axis, so it prints a completeness claim over inputs it never generates and would pass over the fixed build for the same reason it passed over the broken one: a `quoteStyle` axis crossed with every key-line shape that hard-codes the double quote, an `escapeInScalar` axis injected into the value's first token, and a non-vacuity floor requiring the new axes to MOVE the loader-accepted cell count; and with IN-02, because `stripComment` returns a pair and every shipped differential over it asserts only the text — CR-01 corrupts the returned state while leaving the text byte-identical, which is exactly why 193 green cases and 98KB of fixture walked past it; acceptance is RED/GREEN transcripts against a libyaml column, a gate-level plant on BOTH distribution twins of a non-coordinator skill flipping exit 0 → exit 1, the row-F/`'a'''`/`''` false-red controls unmoved, and two independent adversarial passes plus the arm-union case — never a green suite [KIT-03, SPAWN-04] *(round-9 CR-01, WR-01, IN-02)*

**Gap wave — round 10, wave 2** *(shares `scripts/frontmatter.*` with 27-51; carries a blocking decision checkpoint, so `autonomous: false`)*

- [x] 27-52-PLAN.md — Family G/G2 closed, the second live bypass and the same failure class one level up, in a predicate's APPLICATION SET rather than its conditions: `BLOCK_INDICATOR` is tested at exactly ONE of the positions YAML allows a block-scalar header — the flattener's top-level key line — so a `|`/`>` header nested under a mapping key or as a sequence item is never recognised, the block scalar's LITERAL content is routed through the comment scanner, and a leading `#` hides a live grant on the SUCCESS arm; re-measured byte-identical by four consecutive round-9 plans and left explicitly OPEN in `deferred-items.md` every time; the disposition is a GATED DECISION with a one-way reversibility note and its false-red cost already quantified (4 of 570 frontmatter-bearing tracked markdown files carry a nested header today, none an adapter, skill or packaging template) — recognise the header at every node-start position with the scalar's end taken from YAML's own more-indented-block rule (recommended), refuse it by name, or recognise it fail-safe without a computed end — recorded as D-57 with its rejected alternatives; the same choice settles ONCE the scope question the 27-48 non-defect divergence raises separately; the fix applies the ONE `BLOCK_INDICATOR` authority at more sites rather than writing a second indicator grammar, and the corpus grows WITH it in the same plan (the nested-mapping-value and block-sequence-item header shapes enter the key-line axis only AFTER the family is closed, because a corpus shape for a live silent no-grant puts the never-exemptible direction into failure), with the expressibility floor's derived family set proven to grow by cardinality; acceptance is both families flipping the gate exit 0 → exit 1 on both distribution twins, the invented/altered name row's set equal to the loader's, the four existing nested-header documents re-parsed with the changed-verdict count STATED, and the pre-fix mirror reporting a non-empty silent-while-loader-grants set over the new members [KIT-03, SPAWN-04] *(round-9 verification gap: Family G/G2)*

**Gap wave — round 10, wave 3** *(shares `scripts/frontmatter.*` with 27-51 and 27-52 and `scripts/generate-role-adapters.test.ts` with the fence work; also the only plan touching `tsconfig.json`)*

- [x] 27-53-PLAN.md — the three harness-integrity findings, all instances of this repository's SECOND systemic failure class: WR-02 — a second fence state machine now exists in the adapter-generator harness, falsifying the unqualified "no second fence parser is written, here or anywhere" claim at two sites, while the pin meant to catch exactly that is scoped to one file and counts one spelling, so it is doubly blind; closed by mirroring the module's own derived frontmatter-grammar assertion — scan every tracked `.ts` by a CONJUNCTION of a delimiter recogniser (both spellings) and a toggle, sort, compare to a MEASURED named set, pin the cardinality, prove it falsifiable with a planted extra machine and non-decorative by per-construct removal — then narrow the two prose claims to the scope that assertion holds; **the review's own proposed hand-list was measured INCOMPLETE at HEAD** (`scripts/check-foundation-guards.test.ts` carries three further toggle sites), so the executor derives and measures rather than transcribing, which would ship the drift defect inside its own fix; WR-03 — an assertion comparing two identical calls of a pure function on an unmutated input, which cannot fail for any implementation, guarding a "sibling case" that never calls the function at all, replaced by the partition and no-surviving-delimiter properties the comment describes plus a SHARED splice both fixtures call, each proven able to fail against deliberately broken variants; IN-01 — the repository's only unused-local error deleted, with `noUnusedLocals` and `noUnusedParameters` enabled over a MEASURED clean tree and proven able to fire, and the harness-excluding scope of those flags stated honestly [KIT-02, SPAWN-01] *(round-9 WR-02, WR-03, IN-01)*

**Gap wave — round 10, wave 4** *(planning artifacts only; consumes the three code plans' transcripts)*

- [x] 27-54-PLAN.md — the traceability record made truthful under a RULE rather than one row at a time, plus the round-10 disposition register: the verification's claim that KIT-03 and SPAWN-04 are over-claimed is STALE — commit `47d7820` reverted both after the report was written, so REQUIREMENTS.md is re-read on disk and the report's line citations are not applied blindly; what genuinely remains is that one requirement is marked Complete while six others in the identical verified-SATISFIED state are not, and the same report calls one of those an under-claim to correct while calling five others a checklist lag — two sentences that cannot both be the rule; one convention is decided and recorded as D-58 (verification-led, recommended: a row is Complete when the most recent verification records it SATISFIED with cited evidence; the phase-led alternative recorded with its reason), applied end-to-end to a single row first and then uniformly to the other nine, with every Complete row citing a command RE-RUN in this plan rather than a transcript carried forward; KIT-03 and SPAWN-04 stay unflipped because a verified status is a verification round's call; SPAWN-03's live-platform capture stays an explicit, dated, owned DEFERRAL to Phase 33 / GAP-D1 / CAP-01, re-affirmed identically in all three records, never reopened and never fabricated; and a disposition register accounts for all nine round-9 items — CR-01, WR-01, WR-02, WR-03, IN-01, IN-02, Family G/G2, the traceability correction and SPAWN-03 — each CLOSED with a transcript, OPEN with a current measurement and an owner, or DEFERRED with a date and an owner, with completeness asserted by count [KIT-01, KIT-02, KIT-03, SPAWN-01, SPAWN-02, SPAWN-03, SPAWN-04, SPAWN-05, SPAWN-06, SPAWN-07] *(round-9 verification gap: REQUIREMENTS.md traceability; plus the round-10 record)*

**Gap wave — round 11, wave 1** *(from `27-REVIEW.md` round 10 — CR-01-new/CR-02/CR-03, WR-01..04, IN-01..03 — and `27-VERIFICATION.md` round 10's four named `missing` fixes; the tracer, and the only plan that may touch the flush's quoting decision)*

- [x] 27-55-PLAN.md — CR-01-new closed STRUCTURALLY, the regression round 10 shipped inside its own fix: D-57 moved the flush's quoting exemption from the per-value block fact to a STICKY per-key fact, so one nested block scalar anywhere in a key switches off the D-30 escape refusal for every other part of that key — reproduced this session, `tools:` / `  a: "\x41gent(grugops-orchestrator)"` refuses alone and returns `{ok:true,value:false}` the moment an unrelated `  b: >-` sibling is added, while libyaml loads the grant plainly; the sticky flag is DELETED rather than tuned (a `saw*` flag carrying a safety exemption is a scope-widening smell) and the quoting resolution becomes a property of the REGION that owns it, preferably by resolving at the point of effect the way the item path already does — which is why only the continuation path is exposed, the asymmetry the review calls the tell; the singular→plural identity model is stated and asserted rather than assumed; pinned by a DERIVED region-kind × escape-kind × spelling axis testing the arms' UNION, proven non-circular against a pre-fix mirror and proven able to fail by a mutation control; acceptance is the U1/U2 PAIR (U2 alone proves nothing), a gate-level plant on both distribution twins flipping exit 0 → non-zero, and a repository-wide value map with zero new refusals [KIT-03, SPAWN-04] *(round-10 CR-01-new)*

**Gap wave — round 11, wave 2** *(shares all three `scripts/frontmatter.*` files with 27-55)*

- [x] 27-56-PLAN.md — CR-03 closed by DELETING A SECOND GRAMMAR: the block-scalar header recogniser asks its implicit-key introduction using `KEY_LINE`, the deliberately narrow TOP-LEVEL frontmatter key grammar, silently transferring that narrowness to NESTED mapping keys where YAML allows any scalar — so `"a b": >-`, `a.b: >-`, `1a: >-` and `a b: >-` never introduce a header, the content routes through the comment scanner, and a leading `#` hides a live grant; all four reproduced this session returning `{ok:true,value:false}` against a loader that reads the grant; the nested question gets its OWN production derived from YAML's mapping-value indicator rule, written in the shape of the sequence-item and explicit-key productions beside it, while the top-level constant keeps its first job and its narrowness untouched; the eight measured position-gate rows keep their verdicts and the encoding unit is STATED at the site; pinned by a derived key-spelling × indicator-form × position axis rather than the four reported rows, with a gate plant, a pre-fix-mirror non-circularity count and a mutation control [KIT-03, SPAWN-04] *(round-10 CR-03)*

**Gap wave — round 11, wave 3** *(shares all three `scripts/frontmatter.*` files with 27-55 and 27-56; carries the round's only END-TO-END gate replay)*

- [x] 27-57-PLAN.md — CR-02 closed, the one bypass reproduced END TO END at `ALL CHECKS PASSED` exit 0: YAML 1.2 § 6.9 lets a node's properties stand in front of its content, so `nested: &a >-`, `nested: !!str >-`, `: &a >-` and `? &a >-` are legal headers the recogniser does not see, and the reference refusal does not catch them either because it is asked about offset 0 of the line and about flow fragments but never about the node start following a block mapping's separator — so the document does not even fail red; the fix reuses the module's EXISTING node-property authority at every introduction the recogniser declares (iterating the declared set, so a fifth introduction inherits it by construction) and adds the missing fourth application point for the reference refusal, with the bare-header control still refusing because there the sigil IS at offset 0 — the contrast proving this is the introduction set and not the sigil test; the round-10 end-to-end reproduction is REPLAYED AND INVERTED on a hermetic mirror with the shape planted in the existing allow-list key of both twins of the non-coordinator `map` skill, with three premise controls and a loader quote per plant; the `UNKNOWN - verify` platform bound is carried forward verbatim — no claim is made that Claude Code honours a mapping under an allow-list key as a tool grant; pinned by a derived property × introduction axis, the three-fix UNION cells, and TWO separate mutation controls [KIT-03, SPAWN-04] *(round-10 CR-02)*

**Gap wave — round 11, wave 4** *(shares all three `scripts/frontmatter.*` files with the three plans above; the block scalar's CONTENT rules rather than its recognition)*

- [x] 27-58-PLAN.md — WR-01, WR-02 and IN-01 closed together as one root cause seen from three sides: the block-scalar machinery decides a scalar's extent and its joins from facts that are not the ones YAML uses. WR-01 — the end condition keeps a line inside the scalar while it is more indented than the HEADER line, where YAML auto-detects content indentation from the first non-empty CONTENT line, so an over-indented first line makes the module report a GRANT and a NAME for a value the loader expresses as `Read,` alone — the module-grants-where-the-loader-does-not direction its own doc block calls NEVER EXEMPTIBLE, shipped by the change that stated it; closed by recording the detected content indent once, at the point it becomes known, with the header-line indent kept as the floor, and by deciding the explicit indentation-indicator digit the indicator constant already matches and discards. WR-02 / IN-01 — the `|`/`>` line-break derivation reached the indicator and not the BLANK LINE, so a folded scalar drops the break and joins with a space, enumerating `["alpha","ga mma"]` where the loader's line break would send the value to the enumeration's own refusal; closed by treating a blank line as content while a scalar is open, with the literal spelling's VALUE EQUALITY pinned in the same case so the two axes stay tied. The threshold is measured on BOTH sides, every shortened file is enumerated and confirmed as a move toward the loader, and two separate mutation controls attribute each pin to its own edit [KIT-03, SPAWN-04] *(round-10 WR-01, WR-02, IN-01)*

**Gap wave — round 11, wave 5** *(the shared corpus and the state differential; runs AFTER the four source fixes so the never-exemptible directions can legitimately be empty, and BEFORE any closure claim)*

- [x] 27-59-PLAN.md — the harness made able to SEE what it claims completeness over, which is this phase's most-repeated lesson and the reason three bypasses shipped past a 210-green file, a green gate and a green 312-cell differential: the D-52 key-line axis grows by DERIVATION from the base shapes' own declared fields so all three round-11 families are expressible by construction rather than by three appended rows (a corpus generated per reported family is circular over the family structure exactly as a per-arm corpus was circular over the arm structure), with three MECHANICAL per-family expressibility assertions printing their matched counts and a non-empty never-exemptible partition against a pre-round-11 mirror broken down by family; plus WR-03 — the `state` differential passes one variable as BOTH the entering node-may-begin state and the offset-zero node-start fact, so its vector space is 24 where it should be 48 and the combination the live continuation call site routinely produces is never generated, reproducing inside the fix for it the exact trap its sibling names — the two become independent axes, the count is pinned at the full product, live-call-site coverage is asserted mechanically, and the pre-fix capture fixture is REGENERATED from a hermetic mirror with its provenance recorded rather than extrapolated; closes with a five-row revert sweep proving each source edit is independently pinned, plus two premise controls proving the sweep measures the edit and not the rebuild [KIT-03, SPAWN-04] *(round-10 WR-03, plus the corpus obligation from the verification's fourth named `missing` fix)*

**Gap wave — round 11, wave 6** *(harness and configuration integrity; the only plan touching `tsconfig.tests.json`, `package.json` and the CI workflow)*

- [x] 27-60-PLAN.md — WR-04, IN-02 and IN-03 closed, all three instances of a device that reads like a floor and cannot hold the weight: WR-04 — the dead-code flags are ON but the typecheck config excludes every `.test.ts` and the runner transpiles the harness without checking, so they reach ZERO test files while the frontmatter harness grew by over twelve hundred lines in round 10 alone; closed by a test-inclusive target that EXTENDS the shipped-source config, runs in the same gate, has its reach STATED as a number beside the zero it replaces, and is proven able to fail by a planted unused local, with every committed `.js` hash asserted identical. IN-02 — the "exactly one implementation answers the GENERAL question" claim is mechanised for one of three non-authority members and excused in prose for the other two, so a future edit making either a general document-level answer keeps the set at four and the claim green; closed by a MECHANICAL discriminator checking what harness-local actually means (imported by no non-test module in the tracked tree), derived from the tracked file list rather than a hand list, asserted TOTAL over the derived set, and proven able to fail by a planted import. IN-03 — the source-scan pin slices with a newline-brace search it never asserts and matches COMMENTS in its negative, so it can truncate silently and false-red on documentation; closed by an asserted end marker, an identity check before inspection, and comment stripping — proven on three plants: code reds, missing marker reds by name, comment stays green [KIT-03, SPAWN-01] *(round-10 WR-04, IN-02, IN-03)*

**Gap wave — round 11, wave 7** *(planning artifacts only; consumes every prior plan's transcripts and may cite nothing it did not re-run)*

- [x] 27-61-PLAN.md — the round closed under its own conventions: a CONSOLIDATED mutation sweep on the FINAL build reverting each of the round's ten edits one at a time, each red naming its own assertion, with the four parser edits also re-running their gate plants, and TWO premise controls recorded FIRST because a sweep that reds on any rebuild is measuring the rebuild — six plans edited the same files, so every family closed by rounds 9, 10 and 11 is RE-MEASURED here with its loader column rather than inherited; the round-11 DISPOSITION REGISTER accounting for all TEN round-10 items — CR-01-new, CR-02, CR-03, WR-01, WR-02, WR-03, WR-04, IN-01, IN-02, IN-03 — plus the verification's own items, each FIXED with a final-build transcript, DEFERRED with a date and owner, or REJECTED with a reason, with completeness asserted BY COUNT and a row even for an item merged into another's fix; the standing obligations re-stated with an owner each and either measured or named as carried; the round-wide specless-probe equality stated in ONE checkable place with its per-plan distribution; and the traceability HOLD — KIT-03 and SPAWN-04 stay `[ ]`/Gaps Found because only a verification round may promote a row on the strength of new work (D-58 item 4, and commit `47d7820` already reverted this exact pair once), SPAWN-03 stays deferred to Phase 33 / GAP-D1 / CAP-01 at `UNKNOWN - verify` with no static gate invented for it, and both renderings of all ten rows are asserted to agree, ten of ten [KIT-01, KIT-02, KIT-03, SPAWN-01, SPAWN-02, SPAWN-03, SPAWN-04, SPAWN-05, SPAWN-06, SPAWN-07] *(round-10 record; the disposition-register obligation)*

**Gap wave — round 12, wave 1** *(the STRUCTURAL PIVOT under D-64, from `27-REVIEW.md` round 11 CR-01/CR-02 and `27-VERIFICATION.md` round 11 — but D-64 SUPERSEDES that verification's prescribed remedy: both findings are DISSOLVED by a mechanism change, not patched by a twelfth widening of `scripts/frontmatter.ts`. Two plans, file-disjoint, genuinely parallel)*

- [x] 27-62-PLAN.md — D-64 Part A, the canonical admission reader: a NEW `scripts/canonical-frontmatter.ts` that stops INTERPRETING frontmatter and starts ADMITTING a restricted canonical shape, refusing every other byte by a NAMED code, so the silent-no-grant arm that produced eleven consecutive rounds of bypasses does not exist by construction — two outcomes, never a third. Carries D-64's vacuity trap 1 side A: all **33** files in the guard's live scan must ADMIT, counted per part (17 agent adapters + 7 source skills + 7 standalone twins + 2 packaging templates), with the scan asserted non-empty first, because a gate that reds the live kit is broken rather than strict. **The grammar is the MEASURED one, not D-64's stated one** — D-64's premise measurement scanned 31 files rather than the guard's actual 33 (missing the keys `kind` and `tier`) and never scanned for quote bytes (missing 31 double-quoted scalars on `description` and `argument-hint`), so its literal eight-key plain-only alphabet would have refused the entire live kit; the grant-bearing keys `tools`/`allowed-tools` nonetheless admit PLAIN ONLY, keeping the whole D-30 escape family permanently off the verdict path. Refusal vocabulary enumerated, every code proven reachable, no catch-all, pinned by a bounded comment-stripped source assertion [KIT-03, SPAWN-04] *(round-11 CR-01, CR-02 — dissolved)*
- [x] 27-64-PLAN.md — D-64 Part B, the seven standalone skill twins become GENERATED and BYTE-GATED, closing the asymmetry that explains eleven rounds: every reproduced bypass landed in a `SKILL.md`, the 17 agent adapters are generated and byte-gated and have never been bypassed once, and no freshness gate in this tree names `SKILL` at all. Derivability was VERIFIED rather than assumed — the six non-root pairs differ by exactly one line (`name: <d>` → `name: grugops-<d>`, +8B each) and the `grugops` root pair by a single 11-line insertion that is byte-for-byte the existing `RESOLVER` constant plus one blank line. The generator's first run must reproduce all seven committed twins BYTE-IDENTICALLY or HALT rather than overwrite; the sixth freshness gate copies `adapters-freshness.ts` verbatim (SET equality + BYTE comparison against a mirror-spawned regeneration, fail-closed, empty-regeneration refused by its own branch) and is wired at BOTH ends. D-64 vacuity trap 4: a planted one-byte drift reds the gate BY NAME with its premise control recorded first. `DISTRIBUTION_PAIR_EXEMPT` stays at exactly one member [KIT-03, SPAWN-04] *(round-11; the never-gated surface)*

**Gap wave — round 12, wave 2** *(the historical corpus; depends on 27-62's module)*

- [x] 27-63-PLAN.md — D-64 vacuity trap 2, the load-bearing evidence for the whole pivot: every bypass shape reproduced across rounds 1–11 harvested into `scripts/canonical-corpus.ts` as cited data — round, finding id, source artifact path, document text, expected refusal code — and replayed against the canonical reader, each producing a **NAMED LOUD REFUSAL with its refusal TEXT in the transcript**, because "it failed" and "it failed for the right reason" are different claims. Provenance asserted (every cited path resolves), completeness asserted two-sided (all eleven rounds represented, row total exceeds the round count so one-row-per-round cannot satisfy it), and the replay PROVEN ABLE TO FAIL by widening a local copy of the grammar and naming exactly which rows move — with an unwidened premise control recorded first, since an all-refused result must be a measurement rather than a property of a grammar that refuses everything [KIT-03, SPAWN-04] *(rounds 1–11, replayed)*

**Gap wave — round 12, wave 3** *(the cutover; the ONLY plan touching `scripts/check-foundation-guards.ts` or `scripts/frontmatter.ts` this round, and it is gated)*

- [x] 27-65-PLAN.md — the plan where the parser stops being the authority. D-64 Part A cut over: `guard_wr05` (SPAWN-04) and the KIT-03 referential-integrity oracle render their verdicts through the canonical admission reader, with a REFUSED admission surfaced as a NAMED GATE FAILURE and never as "carries no grant". D-64 Part C: `scripts/frontmatter.ts` DEMOTED from safety authority to convenience reader — it keeps all eleven non-test consumers, is not deleted, and its parsing logic is not changed by one byte (the only edit is a header comment recording the demotion), pinned in both directions by bounded source assertion. **Rated COSTLY by D-64, so a blocking `checkpoint:decision` precedes any guard line changing.** Two-sided AT THE GATE: the live tree exits 0 while the historical corpus planted into a live `allowed-tools` key on BOTH distribution twins moves the same gate from exit 0 to exit 1 with the refusal text READ from its output — inverting the two reproductions round 11 recorded at `ALL CHECKS PASSED`. Premise controls recorded first; planted-versus-module-only reconciled two-sided against the corpus total [KIT-03, SPAWN-04] *(round-11 CR-01, CR-02 — dissolved at the gate)*

**Gap wave — round 12, wave 4** *(planning artifacts only; consumes every prior plan's transcripts)*

- [x] 27-66-PLAN.md — the round-12 disposition register and traceability hold. All **13** round-11 items accounted for (9 review findings + 3 named `missing` fixes + the SPAWN-03 deferral), counts re-derived from disk and HALTING on any disagreement rather than fitting a number to a total, with completeness asserted BY COUNT. Introduces **DISSOLVED** as a named disposition class, defined before use: CR-01 and CR-02 were not repaired at their site — the defects remain in `scripts/frontmatter.ts` and simply no longer reach a safety verdict — because a row reading FIXED would send a later reader hunting a repair that does not exist and might justify widening the parser a twelfth time. **NOTHING PROMOTED**: KIT-03 and SPAWN-04 stay `[ ]`/Gaps Found (D-58 item 4; commit `47d7820` already reverted one premature flip of exactly this pair), SPAWN-03 stays deferred to Phase 33 / GAP-D1 / CAP-01 at `UNKNOWN - verify` with no static gate invented for it, all three rows asserted from disk in both places each appears and `git diff --exit-code` over `REQUIREMENTS.md` and `ROADMAP.md` as a verify gate. The round's green suite recorded explicitly as a FLOOR [KIT-01, KIT-02, KIT-03, SPAWN-01, SPAWN-02, SPAWN-03, SPAWN-04, SPAWN-05, SPAWN-06, SPAWN-07] *(round-11 record; the disposition-register obligation)*

**Ordering that is load-bearing in gap-closure round 12** — the round changes MECHANISM rather than repairing a defect, so its ordering is built to make the change refusable up to the last moment. 27-62 and 27-64 are genuinely parallel and file-disjoint, and together they constitute everything reversible: a new module no gate consumes, and a generator plus byte-gate revertible per file. 27-63 follows 27-62 because it replays against that module. 27-65 is alone in its wave because it is the ONLY plan this round touching `scripts/check-foundation-guards.ts` or `scripts/frontmatter.ts`, and its `checkpoint:decision` is the last point at which the round can stop having changed how the guard decides — D-64 rates the reader swap COSTLY, not one-way. 27-66 is last because it consumes every transcript and may cite nothing it did not re-run. **No plan this round widens the YAML parser.** Rounds 10 and 11 each shipped a regression inside their own fix; that, not any single finding, is why the mechanism changes.

**The failure class, and why round 12 stops spelling it** — fifteen spellings across eleven rounds, each one a predicate, an assembly, a question, an arm-union, a lifetime, an application set or a position. The pattern the record now supports is not that any of those repairs was wrong; it is that **a module electing to INTERPRET arbitrary YAML has an unbounded construct space, and each round bounded one more corner of it while the next corner shipped green.** D-64's measurement is what settles it: the entire construct space `scripts/frontmatter.ts` has been hardened against across eleven rounds and roughly 19,000 lines appears NOWHERE in the live corpus and is not wanted there. The attack surface existed only because the guard chose to interpret rather than refuse. The standing question the record now supports, above all the prior ones: **ask whether the predicate needs to be TOTAL over its input space at all, or whether the input space itself can be restricted to a shape the live corpus already occupies** — enumerate-the-good (D-30) applied not to an alphabet but to the whole document.

**Ordering that is load-bearing in gap-closure round 11** — every plan touches `scripts/frontmatter.ts` or `scripts/frontmatter.test.ts`, so the waves are strictly sequential and each plan's RED baseline is taken against the build the wave before it shipped. 27-55 leads because CR-01-new is a REGRESSION this project's own predecessor round introduced, and because the region-resolution point it establishes is what 27-56 and 27-57 create new regions against. 27-56 precedes 27-57 because CR-03 changes what "every introduction" MEANS, and 27-57's property strip must be applied to the corrected introduction set rather than to the old one and then re-done. 27-58 follows the three recognition fixes because a scalar's CONTENT rules are only measurable once the scalar is recognised at every position. 27-59 runs after all four source fixes — the never-exemptible partitions cannot legitimately be empty while a family is still live, which is the recorded reason 27-49 declined to add a corpus shape a round early — and BEFORE any closure claim, so no family is declared closed on a corpus that could not have seen it. 27-60 is sixth because the harness controls must be cut against the module the source plans shipped. 27-61 is last because it consumes every transcript and re-measures on the final build. **Every one of the ten round-10 findings has exactly one owning plan and every plan names its owned findings**, so a finding cannot fall between plans.

**The failure class, on its thirteenth, fourteenth and fifteenth spellings** — the thirteenth is a SCOPE error: a key property standing in for a REGION property, where the sticky flag's lifetime outlives the fact it carries (CR-01-new). The fourteenth is one grammar doing TWO JOBS: a deliberately narrow top-level production borrowed for a nested question whose format is wider (CR-03). The fifteenth is the application-set question asked one notch finer than round 10 asked it — not *which positions does this predicate reach* but *what may legally stand BETWEEN the position and the token the predicate tests for* (CR-02). The standing question the record now supports, in addition to the prior three: **when a predicate is split into arms, test their UNION; when a flag carries a safety exemption, ask whether its LIFETIME equals the lifetime of the fact; and assert the verification harness's own premise, because this phase's harness produced a false result in six instances across four straight rounds.**

**Ordering that is load-bearing in gap-closure round 10** — the two live bypasses are closed in SEPARATE waves on purpose: folding them together would hide which edit closed which family, which is D-56's own reasoning and the reason four consecutive plans re-measured Family G/G2 rather than trusting the round before. 27-51 leads because CR-01 is the newer confirmed critical and because its corpus fix must land in the same commit as its parser fix. 27-52 follows because its RED baseline must be taken against the build 27-51 shipped, and because its corpus shape may only be added after its own family is closed. 27-53 is third because the fence-authority scan must be cut against the module the two parser plans shipped and because it is the only plan touching `tsconfig.json`, whose flags affect the whole-project emit. 27-54 is last because it consumes the other three plans' transcripts and may cite nothing it did not re-run. **Every round-9 finding has exactly one owning plan and every plan names its owned findings**, so a finding cannot fall between plans.

**The failure class, on its eleventh and twelfth spellings** — the eleventh is a predicate that ENUMERATES what its format DERIVES: the walk lists the characters that close a scalar and must instead derive that set from the quote style's own escape rule, which it already does for one style and not the other. The twelfth is one abstraction level up and is about a predicate's APPLICATION SET rather than its conditions: `BLOCK_INDICATOR` is a correct pattern asked at one of the several positions its grammar allows. The standing question the record now supports: **ask not only which set a predicate enumerates and what its input is assembled from, but at how many of the positions its own grammar defines it is actually consulted** — and, from round 10's own measurement of the review that raised WR-02, a hand-list proposed as the fix for a hand-list is still a hand-list.

**Ordering that is load-bearing in gap-closure round 9** — round 9 closes only the requirements the round-8 findings touch (KIT-03 and SPAWN-04 for the parser work, SPAWN-02 where 27-50 reaches the adapter-generator fixture); the round-8 verification explicitly re-confirmed as CLOSED the eighth-spelling seeding collapse, the 312-cell loader differential, the locate-then-flatten fence fix, the reachable fourth-kind arm, the multi-document-stream record, and the de-duplicated foreign arm, and none of those is re-litigated. **All eleven findings have exactly one owning plan**, and every plan carries the same ownership table so a finding cannot fall between plans — the failure mode this round exists to prevent. 27-47 leads because CR-01 is the live bypass and nothing else matters if its four gate transcripts do not flip; 27-48 follows because CR-02 is a second root cause in a separate field whose RED baseline must be taken against the walk 27-47 shipped, and WR-03's name-set predicate belongs in the same commit as the name-set defect it makes visible; 27-49 is third because the harness widening must be cut against the module 27-47 and 27-48 shipped, and because its RED transcripts are taken against a mirror of the pre-27-47 build; 27-50 is last, grouped by file so one whole-project rebuild serves the two committed `.js` twins it touches.

**The failure class, now on its tenth spelling, and what is different about the response** — the first five were INSIDE a predicate (the escape alphabet, the delimiter alphabet, the delimiter arm split, the delimiter arm composition, the enumeration alphabet). The sixth was in the ASSEMBLY that produced the value those predicates read. The seventh was the QUESTION a predicate asked. The eighth was the SET a split predicate's arms covered. The ninth and tenth are the CONDITIONS the surviving arms carried and the PLACES a fact was written: 27-43's collapse to one walk was right, and the walk's `mayBegin` still carried a `depth > 0` condition the grammar never had, on two indicators the grammar scopes differently; and the accumulator's node-begin fact was written at one of the two places a node can begin. The standing question the record now supports: **before accepting a predicate as structural, ask which of its conditions come from the FORMAT and which came from the shape of the last example someone tested — and ask whether the fact it reads is written everywhere it becomes true.** From D-56: a differential is complete only over the inputs it GENERATES, so a corpus that cannot express a defect reports zero over a live bypass, and an assertion that cannot fail is decoration that reads like a floor.

**Ordering that is load-bearing in gap-closure round 8** — round 8 closes only the requirements the round-7 findings touch (KIT-03 and SPAWN-04 for the parser work, KIT-02 where a plan reaches the kit-set authority); the reviewer explicitly checked and found SOUND the five inverted assertions from 27-39 and 27-40 (each re-measured against libyaml), the `partitionPluginComponentClaims` extraction's behaviour preservation, coverer-by-identity on all four facts, the moved cardinality floors as products rather than bare literals, the anchor/alias/merge-key refusals, the invisible-code-point prologue widening, the two deliberately frozen in-block `trim()` sites, CRLF and tab handling, key-shape smuggling, and the block-scalar indentation indicators — and none of those is re-litigated. 27-43 leads because CR-01 is the live bypass and because 27-44's harness must be able to take its RED transcript against the pre-27-43 build; 27-44 is second because WR-01 is *why* seven rounds shipped green and D-52 closes it WITH D-51 rather than behind it; 27-45 comes after both because its region-location change must be measured against the scanner 27-43 shipped and re-verified through the harness 27-44 landed; 27-46 is last and its wave is build serialization only.

**The failure class, now on its eighth spelling, and what is different about the response** — the first five were INSIDE a predicate (the escape alphabet, the delimiter alphabet, the delimiter arm split, the delimiter arm composition, the enumeration alphabet). The sixth was in the ASSEMBLY that produced the value those predicates read. The seventh was the QUESTION a predicate asked. The eighth is the SET a split predicate's arms covered: 27-39's carry was gated correctly and seeded at two of three node starts, so the arms were each right and their union was short. D-51 deletes the split rather than adding a fourth arm, because a fact decided at the character where the position is known has no union to leak through. The standing lesson the record now supports: **after collapsing a split predicate, ask what INPUT the one remaining authority is handed, and whether that input can carry the position the answer depends on** — and, from D-52, a corpus and an expectation both written by hand over the same axes cannot fail on an axis nobody thought of.

**Ordering that is load-bearing in gap-closure round 7** — round 7 closes only the requirements the round-6 findings touch (KIT-03 and SPAWN-04 for the parser work, KIT-02 where a plan reaches the kit-set authority); the reviewer explicitly checked and found SOUND `classifyDelimiter`'s totality, `ENUMERATION_LEGAL_CHARS`' polarity, the nine-key schema derivation and its partition, the `hooks/` exemption bounds, `guardKitCounts`' per-part catch, the D-45 sweep's non-circularity and the D-47 enumeration sweep, and none of those is re-litigated. 27-39 leads because the multi-line scalar reset is BELOW every round-6 predicate — `classifyDelimiter` reasons about a line and is correct, `ENUMERATION_LEGAL_CHARS` reasons about a captured enumeration and is correct, and the value they reason about is assembled from several physical lines by a flattener whose per-line helpers reset at every boundary. **All four waves are single-plan and every wave boundary is a genuine shared file**, not merely build serialization: 27-39, 27-40 and 27-41 each edit `scripts/frontmatter.ts`, `.js` and `.test.ts`, and 27-42's IN-05 task edits `scripts/frontmatter.ts` after all three have finished with it. The whole-project `tsc` emit forces the same sequence independently.

**The failure class, now on its sixth spelling, and why the plan boundaries moved** — the five before it were all INSIDE a predicate: the escape alphabet, the delimiter alphabet, the delimiter arm split, the delimiter arm composition, the enumeration alphabet. The sixth is one level below all of them, in the ASSEMBLY that produces the value those predicates reason about. A name mangled upstream never reaches the allowlist that would have refused it. D-48's ratified consequence is that a root cause is not split by which side a finding happened to arrive from: an earlier draft of 27-39 deferred the JOIN direction to a separate plan, and that is the exact incrementalism that produced six consecutive rounds of "one more spelling". The standing lesson the record now supports: **before trusting a predicate's closure claim, ask what produced the value it reasons about, and whether that producer's state survives the construct's boundaries** — and, from D-49, a corpus generated over a smaller unit than the construct under test proves nothing about the construct.

**Ordering that is load-bearing in gap-closure round 6** — round 6 closes only the three requirements that FAILED round-5 verification (KIT-02, KIT-03, SPAWN-04); the other seven are verified clean and out of scope, and all five round-4 findings are confirmed closed and are not re-litigated. 27-36 leads because the composite-delimiter gap is the parser-level bypass both KIT-03 and SPAWN-04 inherit through the shared `keysHaveSpawnGrant`/`parseFrontmatter` path — one fix closes both requirements. 27-37 is logically independent and file-disjoint; its wave-2 placement is **build serialization only**, for the same reason recorded for round 5: `npm run build` is a whole-project `tsc` emit. 27-38 genuinely shares `scripts/frontmatter.ts` with 27-36.

**The failure class, now on its fifth spelling, and what changed about the response** — D-39 patched the alphabet with `trim()`. D-42 widened the alphabet. D-43 inverted the polarity correctly and split the predicate into two arms. Round 5's sweep then built exactly one construction per arm, so it was structurally incapable of failing on the composite it was written to detect — the **same** circularity D-43 warned about over the alphabet, moved one abstraction level up to the arm structure. Four rounds of patching the predicate's *contents* each shipped past a green suite. D-44 deletes the structure instead: a predicate total by construction has no union to leak through, and its exhaustive consumption is enforced by the compiler rather than by review. The standing lesson the record supports: **ask which set the predicate enumerates, and after splitting a predicate into arms, test their UNION** — every arm individually correct is not the same as the predicate being total.

**Ordering that is load-bearing in gap-closure round 5** — round 5 closes only the three requirements that FAILED round-4 verification (KIT-02, KIT-03, SPAWN-04); the other seven are verified clean and out of scope. 27-33 leads because CR-01 is the parser-level bypass both KIT-03 and SPAWN-04 inherit, and because 27-34 both exercises its refusal and widens the scan composition it creates. **All three waves are single-plan by design:** `npm run build` is a whole-project `tsc` emit that rewrites every committed `.js`, so two plans building concurrently can tear a file that then gets committed — non-intersecting `files_modified` does not prevent that, because the emit is not scoped to the files a plan owns. 27-35's `depends_on` is therefore serialization, not logic: it shares no source file, test file or committed artifact with either other plan.

**The amendment chain this round, recorded because how it kept going wrong is the most valuable thing the phase produced** — D-39 point 3 spelled the predicate `line.trim() === "---"`; measured, it missed 458 of 506 swept positions because `trim()` does not strip the Unicode format class. D-42 replaced that with the wider `[\s\p{Cf}\p{Cc}]`; measured, it still missed combining marks, unassigned and private-use code points, and the entirely ordinary `----` and `--- foo`. **Both were denylists whose own text claimed the opposite polarity.** D-43 states the LEGAL set instead — payload plus `[ \t]`, nothing else — and refuses the complement; measured false-red cost is zero across all 33 scan surfaces and all 1115 tracked markdown files. The corollary D-43 carries: a property sweep whose corpus is generated from the alphabet under test is circular, which is exactly how D-42 would have shipped green.

**Ordering that is load-bearing in gap-closure round 4** — the tracer (27-29) is the phase's only live BLOCKER and closes KIT-03 and SPAWN-04 with one change to one module, so it is proven against the committed `.js` before any expansion task runs. Wave 2 is genuinely parallel: 27-30 owns `scripts/frontmatter.*` and 27-31 owns the two walk sites, with zero `files_modified` overlap. Wave 3 is split by shared FILE, not shared logic — 27-31 and 27-32 both edit `install/install.ts` and `install/install.test.ts`, and 27-32's equality case asserts against the exact cycle path strings 27-31 records.

**Ordering that is load-bearing in gap-closure round 2** — `scripts/frontmatter.ts` is the identity and grant parsing authority. CR-01 changes what it answers and CR-02 and WR-03 both read from it, so 27-18 lands and is proven before any consumer moves. Waves 8 and 9 are split by shared FILE rather than by shared logic: 27-19 and 27-20 both edit `check-foundation-guards.ts`, and 27-21 and 27-22 both edit `install/install.ts`.

**Ordering that is load-bearing inside this phase** — the kit-set authority and the referential-integrity oracle land **first**, the `orchestrator.md` trim lands **before** any spawn-allowlist text is added, and only then are the 17 adapters generated. Reversing that order reproduces the exact failure this phase exists to fix, with new names.

**Research flag:** plan with `--research-phase`. The main-thread-vs-subagent coordinator wiring (`--agent` flag / `settings.json` `{"agent": ...}`) must be validated against the real installed adapter flow (`install.ts`'s `materializeAdapter()`) — a genuine platform-schema integration point grugops has not used before.

### Phase 28: Kit Consistency Audit

**Goal**: The kit describes the architecture it actually ships, every role and workflow has been read with a recorded verdict, and every public safety claim carries an id — so a later phase has something concrete to void.
**Depends on**: Phase 27 (the derived scan sets and generated adapters are the tree the audit reads; auditing the stale tree would produce findings Phase 27 already dissolved)
**Requirements**: AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04
**Success Criteria** (what must be TRUE):

  1. Each of the 17 roles and 19 workflows has a recorded disposition — fixed, accepted, or deferred with a reason — so no file is reviewed-but-unrecorded and the count of dispositions equals the count of files the derivation returns. **17, not 18, and the number carries its reason so it cannot be "corrected" back:** `kit-model.listRoles()` drops `_`-prefixed entries by derivation, so `_role-switch-protocol.md` is out of set for COUNTING. It is not dropped — it is read once and recorded as an explicitly uncounted 37th register row, which is why the register holds 37 rows while the completeness equality is 36. (AUDIT-01)
  2. `CLAUDE.md` describes the v2.0 architecture the repo actually has: no handoff packets, and an Orchestrator that decomposes rather than routes. (AUDIT-02)
  3. Every public safety claim in `README.md`, `AGENTS.md`, and `agent-factory/README.md` appears in a registry with an id, so Phase 30's claim-dropping mechanism has a named target rather than a prose search. (AUDIT-03)
  4. The `@playwright/test` and `@axe-core/playwright` pins in the gate templates match versions **verified at the time of change** (1.60.0 → 1.62.0, 4.11.3 → 4.12.1 as of 2026-07-28), with the verification recorded rather than assumed. (AUDIT-04)

**Plans**: 8/8 plans executed (8 waves, strictly sequential — every plan either rebuilds committed `.js` or edits an artifact an adjacent plan reads, so the ordering is a real dependency rather than build serialization)
**Wave 1**

- [x] 28-01-PLAN.md — TRACER: the AUDIT-02 drift guard, landed FIRST and watched failing RED against today's tree (D-24), as a third consumer of `dead-vocabulary.ts` with a derived, count-asserted scan set (D-09)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 28-02-PLAN.md — Fail-safe residuals: the `oracleWr05Wording` quadratic hang closed on all three D-20 axes, AUDIT-04's pins re-measured with `npm show` at execution time (D-23), and residuals #1/#2 sized — the measurement that decides whether 28-08 runs

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 28-03-PLAN.md — The audit machinery: one fail-closed parse authority, the D-06 mechanical pre-pass, and the D-03 two-equality completeness gate, landed RED against an unfilled register

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 28-04-PLAN.md — The claim registry authored, anchored and floor-mapped BEFORE the drift fixes (D-25), with the D-16 anchor↔row bijection and verbatim gate

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 28-06-PLAN.md — The read pass, part 1: 17 derived role files plus the out-of-set protocol file (D-02), each with a substantive recorded verdict and a safety-surface flag

**Wave 6** *(blocked on Wave 5 completion)*

- [x] 28-05-PLAN.md — The AUDIT-02 drift fixes: four top-level docs reconciled, all five examples re-narrated (D-11), both hygiene directories deleted (D-12), registry rows flipped in the same commit — turns 28-01's guard GREEN

**Wave 7** *(blocked on Wave 6 completion)*

- [x] 28-07-PLAN.md — The read pass, part 2: 19 workflows, the register gate turned GREEN, and Phase 29's safety-surface exclusion list derived and freshness-gated (D-18)

**Wave 8** *(blocked on Wave 7 completion)*

- [x] 28-08-PLAN.md — CONDITIONAL on 28-02's sizing: the D-21 adversarial round on the canonical admission reader at D-22's full closure bar

**Note:** the `CTX_WORKFLOWS` derivation the research assigned here is already covered by KIT-02 in Phase 27; this phase consumes the derived sets rather than re-deriving them. Standing obligations #4 (fail-safe residuals) and #5 (hygiene — `agent-factory/handoffs/.gitkeep`) are dispositioned under AUDIT-01.

**Ordering that is load-bearing inside this phase, both demonstrated over asserted:** the AUDIT-02 drift guard lands **first** and must be watched failing RED against the real tree with its hit counts recorded before any drift is fixed (D-24), and the claim registry is authored and anchored **before** the drift fixes so the drift-affected claims are recorded `status: false` and corrected in the same commit, with the bijection gate catching a missed flip on a real commit rather than a fixture (D-25). A guard that passes the moment it appears has never been watched fail.

**Success criterion 1 was amended by D-01, and plan 28-07 made the amendment:** "18 roles" is now **17** at all three sites that carried the stale count — this phase entry above, success criterion 1, and `REQUIREMENTS.md`'s AUDIT-01 text — because `kit-model.listRoles()` drops the `_`-prefixed protocol file by derivation. Each site now carries the derivation rule alongside the number, so the count cannot be "corrected" back by a reader who counts the files on disk. `_role-switch-protocol.md` is recorded as a 37th register row, explicitly uncounted and still read once.

### Phase 29: Controlled Language & Voice Guard Rebuild

**Goal**: Procedural and agent-written prose follows one enumerated writing profile so two agents reading the same instruction reach the same act; the caveman voice lives in exactly one fenced block per role and is measured as voice, not as sentence shape.
**Depends on**: Phase 28 (the audit produces the safety-surface exclusion list and the claim registry this rewrite must honor; rewriting first means re-rewriting text the audit would have flagged)
**Requirements**: LANG-01, LANG-02, LANG-03, LANG-04, LANG-05, LANG-06, LANG-07, LANG-08
**Success Criteria** (what must be TRUE):

  1. The kit ships a grugops-authored, ASD-STE100-**derived** writing profile — enumerated rules plus a project Technical Names/Verbs set — with a non-affiliation and not-certified disclaimer, vendoring no part of the ASD dictionary. (LANG-01)
  2. The profile governs workflow steps, checklists, memory-bank, shared-context notes, board, and traceability; it leaves the fenced caveman identity blocks alone; and a named safety-surface exclusion list keeps load-bearing security, compliance, and admission text from being reworded by a style pass. (LANG-02, LANG-03)
  3. The guards are **named for the decidable subsets they check** — `guard_imperative_lexicon` for lexicon membership at imperative position and `guard_sentence_form` for sentence length and banned constructions — and nowhere in the kit is ASD-STE100 conformance claimed, nor a token-economy win, nor an LLM-comprehension benefit (that one stays `UNKNOWN - verify`); `guard_banned_claims` holds that prohibition mechanically rather than by discipline. (LANG-04)
  4. The rebuilt voice guard **fails RED on all 17 current caveman blocks** as acceptance evidence before the rewrite lands, measures against a committed lexicon rather than sentence shape, and publishes a number with a denominator. (LANG-06)
  5. `## One job`, the caveman block, and `## Responsibilities` each say a thing once; `guard_imperative_lexicon` (with its sibling `guard_sentence_form`) and the rebuilt voice guard read the fence through **one** parser, never two grammars over the same bytes; and byte ceilings are re-baselined exactly once at end of phase with every file ≤ its previous value and the delta recorded — never raised mid-phase. (LANG-05, LANG-07, LANG-08)

**Plans**: 60/60 plans executed — **gap-closure round 8 added 5 (29-56 .. 29-60, waves 53-57), ALL 5 EXECUTED 2026-08-18; the round-8 record and the phase close live at `docs/audit/29-round8-residuals.md`. LANG-04's verdict belongs to the verifier and is claimed by no plan. THIS IS THE FINAL ROUND OF PHASE 29 (D-58): a finding after it becomes a backlog item or a follow-up phase, never a round 9.** Round 8 closes the one truth `29-VERIFICATION.md` (round 7, `gaps_found`, 7/8) still failed — LANG-04's mechanical guarantee — **by NARROWING THE PUBLISHED CLAIM rather than completing the matcher (D-55)**. Seven rounds each closed one axis and exposed the next (unlisted benefit verb → unlisted conformance verb → hard-wrapped multi-word literal) because the input-assembly axis is unbounded: a hard wrap, then inline emphasis, then a table cell, then an HTML comment. No matcher over free prose satisfies a totality claim, and every further round buys one axis and ships a new residual. So the gate's published sentence, its docblock, the profile's honesty-floor prose and the must_have truth are narrowed to **what the gate decides** — a pinned-literal question, decided one PHYSICAL LINE at a time, over a derived document set, outside the registry-anchored blocks of one named region — with both numbers interpolated from the module's own derivations and the residual axes named. **A prohibition that publishes a wider scope than its mechanism is the exact defect LANG-04 exists to prevent, currently committed by LANG-04's own guard; fixing that IS closing LANG-04, not conceding it.** The hard-wrap matcher fix is **deliberately NOT done and that is recorded, not dropped (D-56)**: `V-29-57-01` carries it with a derived reachability count (the round-7 review's multi-word figure over-states the reach and is corrected — a member whose every split leaves a bare pinned member intact is not reachable), a live count of 0 measured over the gate's own corpus, direction FAIL-OPEN, its reproduction and its named remedy, plus the correction of the in-source premise that argued the bypass needs a wrap falling inside a word when the reproduction wraps between words into legible prose. Round 8 also **repairs the CI build-parity gate (D-57)**, which is load-bearing for every other mechanical claim this phase makes: `outDir: "./"` means the build rewrites tracked outputs in place, `freshness.ts` read the committed side from the working tree, and CI built before every freshness step with zero dirty-tree assertions — so it compared a fresh build against a fresh build and could not detect a stale committed `.js`, the guarantee `CLAUDE.md` gives as the reason the tooling ships compiled. The gate's subject moves to HEAD via `git show HEAD:` and `git ls-tree -r HEAD`, making it ordering-independent by construction, with the CI reorder and a single-authority parity script as the belt-and-braces half — and **the repair is proven not vacuous by a discrimination pair: the same planted drift, green on a clone of the pre-fix tree and red by name on a clone of the post-fix tree, with `tsc` run on both.** **No round-8 plan flips a requirement row (house rule 9); LANG-04's movement is conditional on the narrowed claim verifying and belongs to the verifier.** Earlier round-7 note retained: **gap-closure round 7 added 8 (29-48 .. 29-55, waves 45-52), ALL 8 EXECUTED 2026-08-18** — round 6's two blockers independently reproduced CLOSED, and LANG-04 reopened on a third bypass this round's own review found. Its original planning note follows, closing the two truths `29-VERIFICATION-round6.md` failed (`gaps_found`, 3/5 — round 5's two named LANG-04 gaps GENUINELY CLOSED and LANG-07 regression-clean, but LANG-04's mechanical guarantee failed on **two NEW fail-open blockers** that round 6's own review found and the verifier independently reproduced) plus all twelve findings of `29-REVIEW-round6.md` and the requirements-traceability inversion. **CR-01: the sole carve-out is bounded by two CARDINALITIES and not at all by CONTENT.** After D-53 deleted the co-occurrence alternative the exemption region is the only carve-out left, and substituting one honest denial line inside it for a live disproven token-economy claim leaves `suppressed` at 14 and `extent` at 62 with **all seven gates green** — reproduced end-to-end twice, independently, on `git archive HEAD` mirrors with sha256-identical gate binaries. The case that looks like the pin parses components out of the run and compares them to a tally over the same live document, so both sides move together; its own comment concedes the region is degenerate by construction. This axis carries FOUR open residuals that are one defect wearing different clothes — CR-01, `V-29-47-02` (unbounded at the bottom), `V-29-47-03` (position pinned by nothing) and `V-29-32-01` (a closed-fence count-preserving swallow) — all of them because the carve-out is **position over a cardinality**, and six rounds of one-more-pin is what has failed. The fix is structural and reuses a mechanism that already works: **every suppressed occurrence must sit on a line inside a registry-anchored, byte-frozen block**, extending `check-claim-anchors`'s verbatim freeze over the region, which makes the carve-out positional AND content-bound and therefore invariant under translation and needing no bottom boundary — so it SUBSUMES the other three rather than adding a fifth pin. That is a change in D-48/D-53's MECHANISM and not its intent (the matcher is untouched, no lexical axis returns, the region stays positional; what becomes frozen is the bytes) and it applies D-01(a)/D-04's existing frozen-source rule at the one place D-48/D-53 left unbound — so it ships behind a **blocking `checkpoint:decision`** in plan 29-52 with the cheaper complement (a per-group composition pin, which reduces the substitution class and does NOT close CR-01) offered as the named alternative. **CR-02: the kit's two shipped JSON manifests are user-visible claim surfaces outside every gate.** `.claude-plugin/marketplace.json`'s `description` is the exact string a user meets running `/plugin marketplace add`; planting `improves comprehension … saves tokens` there exits 0, `ALL CHECKS PASSED`, the claim never named, while the identical bytes in any `.md` file are a three-finding red. `plugin.json` is only accidentally partly covered by a registry row's PRESENCE check. **The denominator is the finding**: round 6 dispositioned every unscanned MARKDOWN class by name against a `git ls-files '*.md'` denominator, so the markdown class boundary itself was never a decision anybody made. The remedy is a sixth DERIVED part (never a hardcoded pair — that is the set-literal drift class that produced the phase-23 spawn defect), the scan count moved with entrants named from the gate's own refusal text, the class boundary declared in prose, and the coverage denominator widened to the shipped TEXT surface so the next non-markdown claim surface reds on the day it lands. The `encoding` probe row is ANSWERED rather than left implicit: the scan reads RAW BYTES line-oriented like every other member, and the branch not taken is closed by a NAMED REFUSAL — a manifest whose decoded string values are not byte-present in its raw text is refused, so an escaped literal cannot hide. **WR-01: `.claude/` was admitted as a DISK walk, so the prefix exclusions are defeated by nesting** — `.claude/worktrees/` exists on this tree and is where this project's own execution tooling puts isolated worktrees; the reviewer planted two files under it and the gate reported findings on the claim registry itself and on a `.planning/` document, verbatim the harm the exclusion exists to prevent, **with both guarding assertions green** (one asserts a prefix relationship TRUE for a nested path, the other only checks `tracked ⊆ scan ∪ excluded` and never `scan ⊆ tracked`). Fixed by moving the exclusion to the point of effect — the walk refuses to descend into an excluded segment at ANY depth, derived from the ONE list with both projections' cardinalities asserted — plus the missing coverage direction. The reviewer's other half, deriving membership from the tracked set, is **REJECTED with a named reason**: every behavioural case this gate owns runs against a `git archive HEAD` mirror, which is not a git repository, and a rule that falls back to a walk is two membership rules. Also in scope: WR-02 (the region measured on one read and applied to a second — the module's own ONE READ invariant is false, and the naive one-line fix opens an empty-string hole that is bounded here rather than shipped), WR-03/WR-04/WR-06 in `check-nul-bytes.ts` (a production-dead NUL predicate whose comment names call sites that do not exist — DELETED, not reconciled; a false claim about git's classifier at the declaration that justifies the `bytes` field; an unguarded re-read that kills the gate with a `node:internal` frame; and an initialised submodule gitlink misreported as an I/O error), WR-05 (the imported corpus's derivation-refusal channel dropped on the floor — load-bearing for CR-02, since a new derived part with a channel nobody reads repeats it), IN-01/IN-02 (a hand-copied duplicate of the generator's contract regex, and an unenforced uniqueness claim asserted from a one-off measurement — the named failure mode reappearing inside its own remedy), IN-03 (the walk bound applied per part, so the effective budget is a multiple of the declared one — decided in the same round as the sixth part rather than after), IN-04/`V-29-47-01` (an in-source record of a CLOSED residual, false on six counts, sitting at the doc comment attached to `BANNED_CLAIM_EXEMPT_REGION` — the text an editor reads immediately before touching the sole carve-out, so its DELETION is sequenced AHEAD of all exemption work), `V-29-47-06` (the CI workflow describing both widened gates at their pre-widening scope, in the one file no round-6 plan opened), and `V-29-47-05` (the requirements inversion — **half-corrected at HEAD; LANG-04 already reads `Gaps Found` and is correct, LANG-07 still reads `Gaps Found` and must read `Complete` per the round-6 verifier's own disposition table**; plan 29-48 applies that determination first, under the report's named authority, and **no round-7 plan declares `requirements-completed: [LANG-04]`**, because the inversion was produced by an automated marker acting on exactly that field before any verification existed). Waves are strictly sequential (45-52): almost every plan rebuilds a committed `.js` with `tsc`, which cannot run concurrently without tearing a committed output (the phase-27 round-5 precedent), and the reviewer-mandated ordering — IN-04's deletion, then WR-02's one-read fix, then the anchored-block authority, then CR-01 — is encoded in `depends_on`. **No requirement is re-marked complete by any round-7 plan; LANG-04's verdict belongs to round 7's verifier.** Earlier round-6 note retained: **gap-closure round 6 added 5 (29-43 .. 29-47, waves 40-44), ALL 5 EXECUTED 2026-08-17**, closing the two truths `29-VERIFICATION-round5.md` failed (`gaps_found`, 1/2 — LANG-07 VERIFIED and moving to Complete, LANG-04 still blocked) plus all twelve findings of `29-REVIEW-round5.md` under D-51. The round's two blockers are one defect at two addresses — **a prohibition whose published scope is wider than the mechanism behind it** — appearing once at the SCAN SET (`CHANGELOG.md` silently outside it with two live `token-economy` occurrences, while the exemption's own bound paragraph asserts it reaches no other gate) and once at the MATCHER (round 5's "structural fix" relocated the enumeration from the phrase slot to a seven-stem verb list, defeated by `increases`/`raises`/`aids`/`gives…sharper`/`makes…understand`, all five at exit 0 with the planted file never named). D-48 refuses BOTH the ninth stem and the reviewer's subject-side pin on one ground: **every axis a bare term can be paired against is an open class, so the only bounded thing in the design is the one named exemption region — and a region is positional.** The lexical half is therefore DELETED rather than moved again, which is the fix shape that held in Phase 27 and in plan 29-40 and which removes a set literal instead of authoring one. **Authoring these plans then found the identical defect surviving on the group that gives the gate its name** — `Simplified Technical English` gated by six conformance stems, so `follows` / `meets` / `adheres to` / `is written in` each yield ZERO findings while `conforms to` yields one — and the planner ESCALATED it rather than overrule D-48. The user extended the decision instead, as **D-53**: `CONFORMANCE_VERB_MARKERS` is deleted alongside the benefit list, all three bare terms go unconditional, conditional members go **3 → 0**, and **`requiresOnSameLine` is removed from the `BannedClaimLiteral` TYPE rather than emptied**, so reintroducing a marker list is a visible type change in a diff instead of a one-line addition. The gate's whole predicate becomes *a pinned literal list plus one named exemption region*. Four consequences are disposed rather than absorbed: WR-06 becomes MOOT (its remedy would guard a shape the type forbids, which is the AP-1 defect it was raised about, so a written disposition plus a type-level tripwire ships in place of the guard clause); `V-29-42-01`/`-02`/`-04` all CLOSE BY CONSTRUCTION and each still earns a closing measurement; `BANNED_CLAIM_EXEMPT_SUPPRESSED` is predicted 12 → 14 but **derived, never typed**; and the surviving enumeration — `BANNED_CLAIM_LITERALS` itself, the list of what is banned — is stated with its direction FAIL-OPEN so a deletion-shaped closure is not read as having closed the class. Waves are strictly sequential: 29-43 fixes the corpus that 29-44's admission cost is measured over, and every plan rebuilds the committed `.js` with `tsc`, which cannot run concurrently without tearing a committed output (the phase-27 round-5 precedent). **No requirement is re-marked complete by any round-6 plan — LANG-04's verdict belongs to the verifier.** Earlier round-5 note retained: **gap-closure round 5 adds 3 (29-40 .. 29-42, waves 37-39), PLANNED 2026-08-17; 29-40 EXECUTED 2026-08-17 and 29-41 .. 29-42 NOT YET EXECUTED**, closing the two human decisions round-5 UAT recorded as G-29-1 (LANG-07) and G-29-2 (LANG-04); see the round-5 block below the wave list. **29-40 landed 2026-08-17 closing G-29-1 / V-29-35-01** — the private flat `key: value` `parseFrontmatter` DELETED from `scripts/generate-catalog.ts` and the exported authority consumed, so the imported set is byte-identical to the sibling generator's three symbols (D-24). `docs/catalog/README.md` proven byte-identical by regeneration plus `git diff --exit-code`; the five divergence axes RE-MEASURED in session over the 36 governed documents, all zero, so the divergence was LATENT rather than absent; and the latency shown to be real by running the three plants against the pre-change committed `.js` — a duplicated `tier:` key exited **0** and published the CORE role QE/E2E in the enterprise group, an unterminated block printed `role tier must be core|enterprise, found ""` about a file whose frontmatter was never parsed. Three planted cases hold the three findings, two RED-proven against that artifact and the third mutation-proven. `FRONTMATTER_PARSER_OWNERS` derives the owner set over the RECURSIVE 49-module enumeration with its denominator asserted BEFORE the membership claim, pins it at one member, and proves the recogniser on six matching and five non-matching planted spellings; its NAME-scoped bound is disclosed at its declaration rather than implied by its silence. One Rule 3 blocker found by the sweep and fixed in-plan: the D-50 IN-05 classifier reads COMMENTS as code, so prose quoting the deleted regexes kept the module classified as a grammar site — resolved by this file's own convention (a retired pattern's spelling goes with the pattern), after which the IN-05 site set was RE-DERIVED from two members to one and every pin, control, fixture name and stale case name moved with it. Two residuals disclosed rather than smuggled (D-40-1, an empty `order:` publishing row 0; D-40-2, the classifier's comment-blindness). **LANG-07 is NOT re-marked complete — the closure awaits round-5 re-verification, exactly as rounds 3 and 4 recorded for the same requirement.** Earlier note retained: 39/39 plans executed (19/19 initial; gap-closure round 2 added 7, 29-20 .. 29-26, waves 19-25; gap-closure round 3 added 6, 29-27 .. 29-32, waves 26-29, all executed); **gap-closure round 4 adds 7 (29-33 .. 29-39, waves 30-36), ALL 7 EXECUTED — 29-39 landed 2026-08-16 closing round-4 WR-07 (the seven exact census pins that fired on every unrelated commit: the five values MEASURED to drift on an added assertion converted to relationships and corpus-derived RATE floors under one authority, the module count and barren check kept EXACT as the vacuity floor with the reason at the declaration, and the exact volumes RELOCATED behind a commit-pinned case at `b76a65e` per D-25 — discrimination proven end-to-end against the shipped file, growth GREEN on every spelling and all six counter/classifier breakages RED by named id, so zero discrimination was lost. It also wrote `docs/audit/29-round4-residuals.md`, the round's disposition record: 13 rows, 9 closed with a re-run reproduction and IN-01..IN-04 recorded as DEFERRED BY USER DECISION, the residual roll-up carrying BOTH directions (V-29-29-01 closed by 29-35, V-29-35-01 opened by 29-35 — net movement zero), the LANG-08 override repeated verbatim with its prohibition half MEASURED (`roleCeiling()` byte-identical across the round, zero role files touched), and the 9+11+2=22 probe arithmetic. Every round-4 finding was re-tested on a mirror of the FINAL tree after the freshness premise was asserted and NONE still reproduces; one false green inside that task's own harness is recorded rather than quietly corrected. **No requirement is re-marked complete — LANG-03 and LANG-07 await round-5 re-verification**) (29-36, 29-37 and 29-38 landed 2026-08-16 after the three named below; 29-38 closed WR-06 by holding `WP-04`'s decidable sentence from both artifacts, and repaired a live RED on `main` caused by 29-37-SUMMARY.md's own frontmatter) — 29-33 landed 2026-08-16 closing round-4 CR-01 (the `SEC_VOICE_FILES` substitution bypass), 29-34 landed 2026-08-16 closing round-4 WR-04 and WR-05 (guard_voice's unreachable element floor and its unwitnessed per-file scanned count; the near-total-swallow bypass is reproduced at exit 0 and now published as a three-number accounting, with the direction the accounting does NOT cover measured across all 19 elements, bounded by the delimiter-neutralised bound, and disclosed at the declaration under a pinned roster), and 29-35 landed 2026-08-16 closing the LANG-07 failed truth and round-4 WR-08 (the THIRD section-extent grammar DELETED from BOTH kit generators and routed through `unfencedHeadingIndex` + `sectionEndIndex`; both axes — fence-blindness and level-blindness — reproduced at exit 0 against the pre-fix committed `.js` shipping four wrong routing `description`/catalogue values, then held by four permanent cases; `docs/catalog/README.md` and all 17 adapters proven byte-identical; `REGEXP_SECTION_BOUND_SITES` now EMPTY with its discrimination assertions kept and two vacuity floors added; the private `parseFrontmatter` the reading surfaced escalated as **V-29-35-01**, measured at 0 key-set differences over 36 governed documents and recorded rather than fixed). **Neither LANG-06 nor LANG-07 is re-marked complete by any of the three plans — the closures await round-5 re-verification**** — planned 2026-08-16 against round-3 verification (`gaps_found` 6/8: LANG-06 and LANG-07) and round-4 code review (1 critical, 8 warning, 4 info). Waves are strictly sequential: plans 29-33, 29-34, 29-35 and 29-39 share `scripts/check-foundation-guards.*` and plans 29-35 and 29-36 share `scripts/frontmatter.test.ts`, and every plan rebuilds the committed `.js` with `tsc`, which cannot run concurrently without tearing a committed output (the phase-27 round-5 precedent). Earlier round-3 note retained: **all 6 round-3 plans executed — 29-27, 29-28, 29-30, 29-31, 29-32 and 29-29 landed 2026-08-16; the closures for LANG-06, LANG-03/LANG-07 and LANG-04 all await round-4 re-verification and none of those requirements is re-marked complete here. Plan 29-29 ESCALATES a new LANG-07 failure it measured rather than closing it: `generate-catalog.ts:87` and `generate-role-adapters.ts:127` answer the section-extent question through a fence-blind `new RegExp` lookahead (V-29-29-01, derived and pinned two-sided)**. Round 3 closes the four truths that failed re-verification on 2026-08-15 (LANG-03, LANG-04, LANG-06, LANG-07) — one of them a fail-open REGRESSION created by round 2's own structural fix.

Plans:
**Wave 1**

- [x] 29-01-PLAN.md — one fence authority (`voice-model.ts`), one shared vacuity rule (`vacuity.ts`), the rebuilt two-sided voice guard and the intra-file clause-uniqueness guard, both watched RED on the real tree with three discriminating fixtures

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 29-02-PLAN.md — the grugops-authored writing profile with its non-affiliation disclaimer, plus `guard_banned_claims` landed RED on a deliberate draft conformance sentence, the profile's claims registered, and the guard-name and honesty-floor edits to the live planning documents

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 29-03-PLAN.md — `guard_imperative_lexicon` and `guard_sentence_form` over the derived 47-file corpus with a derived generated-file exclusion, plus the display-name derivations for the Technical Names set

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 29-04-PLAN.md — `guard_diff_disposition`: the frozen set derived from three existing gates, changed clauses enumerated from git, and the per-plan disposition directory

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 29-05-PLAN.md — roles batch 1 (7 files, `software-engineer.md` as the end-to-end tracer): remove-before-add with a four-point byte transcript and the generator cascade

**Wave 6** *(blocked on Wave 5 completion)*

- [x] 29-06-PLAN.md — roles batch 2 (8 files), including the zero-headroom and largest-file cases

**Wave 7** *(blocked on Wave 6 completion)*

- [x] 29-07-PLAN.md — the two dual-voice safety roles, plus the three claim-registry flips and count corrections Phase 28 deferred here; aggregator goes green at 17/17

**Wave 8** *(blocked on Wave 7 completion)*

- [x] 29-08-PLAN.md — workflows 00–05 conformed to the profile, with the bold-label step grammar converted

**Wave 9** *(blocked on Wave 8 completion)*

- [x] 29-09-PLAN.md — workflows 06–12 conformed, release workflow partitioned before it is touched

**Wave 10** *(blocked on Wave 9 completion)*

- [x] 29-10-PLAN.md — workflows 13–18 conformed, single-sourced pointers reconciled; workflow part reaches zero findings

**Wave 11** *(blocked on Wave 10 completion)*

- [x] 29-11-PLAN.md — the 13 hand-authored checklists conformed, plus four retired-vocabulary corrections and one recorded leave-alone
- [x] 29-12-PLAN.md — the 13 seed templates and 2 contracts conformed, board entry criterion corrected; governed corpus closes green

**Wave 12** *(blocked on Wave 11 completion)*

- [x] 29-13-PLAN.md — the once-only byte-ceiling re-baseline behind a blocking human checkpoint, plus the corpus growth record

**Gap-closure round 1** *(verification returned `gaps_found` at 4/8 — LANG-03, LANG-04, LANG-06 and LANG-08 failed; LANG-01, LANG-02, LANG-05 and LANG-07 verified and standing)*

**Wave 13** *(blocked on Wave 12 completion)*

- [x] 29-14-PLAN.md — TRACER: IN-01, the voice harness deriving role membership from the root it measures, then CR-01, the caveman fence reader bounded to its own section — the founding defect closed end-to-end with a planted RED, a permanent case set and an adversarial self-reproduction

**Wave 14** *(blocked on Wave 13 completion)*

- [x] 29-15-PLAN.md — CR-02: the companion-edit rule attributed to the carrier that actually changed each clause, on BOTH the registry-anchor and positive-guard-literal arms, with a harness extended to three commits because a two-commit fixture cannot see the difference

**Wave 15** *(blocked on Wave 14 completion)*

- [x] 29-16-PLAN.md — WR-05 by canonical companion form with no denylist, WR-06's frozen-region locator threaded through the one fence authority, and WR-02's diff-disposition denominator made independent of the loop that consumes it

**Wave 16** *(blocked on Wave 15 completion)*

- [x] 29-17-PLAN.md — CR-03: a `## Steps` bullet at any nesting depth with the section anchor sticky across sub-headings, plus WR-02's two lexicon denominators and WR-06's Technical Name table scan

**Wave 17** *(blocked on Wave 16 completion)*

- [x] 29-18-PLAN.md — IN-03, the clear-voice neutralizer agreeing with the lexicon counter about case with its scope asserted from both sides, plus WR-06's banned-claim exemption locator and the round's closing regression floor

**Wave 18** *(blocked on Wave 17 completion)*

- [x] 29-19-PLAN.md — LANG-08 closed by the recorded human override the verification report drafted; no ceiling table edit, and the declined ratchet-down alternative stays documented and unapplied

**Gap-closure round 2** *(round-2 verification held at 4/8; each round-1 fix either uncovered a sibling defect in the same enforcement surface or was itself incomplete, and LANG-07 regressed on evidence round 1 did not have)*

**Wave 19** *(blocked on Wave 18 completion)*

- [x] 29-20-PLAN.md — TRACER: one section-locator authority (`unfencedHeadingIndex` / `sectionEndIndex`) exported from `frontmatter.ts` and wired end-to-end through `voice-model.ts` to the gate, closing CR-02's level-one bypass and WR-01's fence-blind anchor scan, with the authority's scope pinned on six axes

**Wave 20** *(blocked on Wave 19 completion)*

- [x] 29-21-PLAN.md — CR-01: the unpinned watched corpus closed at BOTH ends — set equality in `check-audit-register.ts` between flagged rows and the derived kit, plus a consumer-side minimum derived from a source the register cannot move

**Wave 21** *(blocked on Wave 20 completion)*

- [x] 29-22-PLAN.md — `check-diff-disposition.ts` adopts the authority and WR-03's fourth locator (fail-open, missed by round 1) is bounded and fence-aware, with the site set derived rather than hand-listed, plus IN-01 and IN-02

**Wave 22** *(blocked on Wave 21 completion)*

- [x] 29-23-PLAN.md — `check-banned-claims.ts` adopts the authority; WR-02's header corrected to the direction the code takes, the exemption's reach published and pinned two-sided, and the paired plant's missing negative half

**Wave 23** *(blocked on Wave 22 completion)*

- [x] 29-24-PLAN.md — `check-imperative-lexicon.ts` adopts the authority; WR-04 settled at a blocking human decision (retire Residual 1 and publish the rule, or keep it), WR-09 half two fixed and half one recorded as Residual 4 with a concrete conflict, plus WR-07

**Wave 24** *(blocked on Wave 23 completion)*

- [x] 29-25-PLAN.md — the harness tells the truth: WR-05, WR-06 and IN-03 closed, their shared class closed by a derived tripwire, and LANG-07 turned into a two-sided, falsifiable, non-vacuous tree-wide derivation of the predicate's single owner

**Wave 25** *(blocked on Wave 24 completion)*

- [x] 29-26-PLAN.md — adversarial closure: a parser oracle over the unified authority across a derived, counted axis cross-product, independent re-reproduction of every original recipe plus variants invented at execution, a re-runnable audit transcript, and a blocking human decision on the residual set

**Wave 26** *(gap-closure round 3 — blocked on Wave 25 completion)*

- [x] 29-27-PLAN.md — TRACER, LANG-06: the founding defect reopened by round 2's own fix. `readCavemanFence`'s bound taken from the ONE authority over a delimiter-neutralised projection, so the fence being measured cannot extend its own section; `guard_voice` folded through `reportMeasured` and publishing the scanned line count per file; the module's cost paragraph corrected in the same commit; and a whole-gate UNION case proving both halves together

**Wave 27** *(blocked on Wave 26 completion)*

- [x] 29-28-PLAN.md — LANG-03 + LANG-07's SHARED ROOT, planned once: `unfencedMatchIndices` added to the authority, `readRegistry`'s heading index and block end both taken from it so a fenced example is documentation again, `parseClaimBlock`'s private fence recogniser deleted, and the new scope hazard the fix creates (an unterminated fence silently shortening the claim list) closed by a named refusal in the same plan that opens it
- [x] 29-31-PLAN.md — LANG-04: WP-11 narrowed to the spelling the gate decides, with the reason and the residual recorded in the profile; the WHOLE rule pinned in both artifacts and proven falsifiable on all four drift routes; and the remaining floor given a two-sided tripwire instead of a promise

**Wave 28** *(blocked on Wave 27 completion)*

- [x] 29-30-PLAN.md — LANG-03's registry arm: equality four pins it in both directions (a derived containment for the ADD direction, a sum-floored kind cardinality for the REMOVE direction), the union's residue asserted rather than described at the consumer, and a BOTH-ARMS probe neither single-arm harness could produce
- [x] 29-32-PLAN.md — LANG-07 at the consumers: the `-1` drift route deleted and the answer refused by name, "every consumer honours the `-1` contract" turned into a derived two-sided vacuity-floored scan, and the safety exemption's EXTENT published and pinned so variant C1's swallowed section reds even carrying no banned claim

**Wave 29** *(blocked on Wave 28 completion — deliberately last, because its deliverable is a re-derivation over the FINAL tree)*

- [x] 29-29-PLAN.md — LANG-07's harness: the owner classifier widened to the shape its own floor item 4 wrongly said nothing used, floor item 4 corrected, `SECTION_EXTENT_OWNERS` re-derived over the final tree with a before/after pair; the oracle's vacuous I5 made reachable with per-invariant REACH counts derived independently and a third broken locator proving it can fail; and the duplicate-assertion tripwire's multi-line share published and pinned

**Gap-closure round 4** *(round-3 verification returned `gaps_found` at 6/8 — LANG-06 and LANG-07 failed, LANG-03 and LANG-04 closed; round-4 code review returned 1 critical, 8 warning, 4 info. Scope decided by the user: CR-01, the two failed truths, and all eight warnings are IN; IN-01..IN-04 are deferred and recorded, not dropped; the LANG-08 override stands unchanged)*

**Wave 30** *(blocked on Wave 29 completion)*

- [x] 29-33-PLAN.md — TRACER, CR-01 + the LANG-06 failed truth: `SEC_VOICE_FILES` pinned by MEMBERSHIP instead of cardinality — a declared sorted roster two-sided against the guard source, a per-member DERIVED property floor (`safetySurfaceUnion` membership, or the ASVS generator's own output-path literal) whose union is asserted equal to the roster, a substitution arm added beside the existing add/remove arms, and the false declaration comment deleted rather than amended

**Wave 31** *(blocked on Wave 30 completion)*

- [x] 29-34-PLAN.md — WR-04 + WR-05: `readCavemanFence` publishes the line accounting from the indices it already holds, `guard_voice` reconciles the array its marker loop walks against that published count and refuses by name on a mismatch, the element floor's dead disjunct removed and its header corrected, the vacuous remainder assertion replaced by one that can fail, and WR-05's uncovered direction measured, bounded and disclosed rather than answered with 19 hand-maintained minima

**Wave 32** *(blocked on Wave 31 completion)*

- [x] 29-35-PLAN.md — the LANG-07 failed truth + WR-08: the THIRD section-extent grammar DELETED from both `generate-catalog.ts` and `generate-role-adapters.ts` and routed through `unfencedHeadingIndex` + `sectionEndIndex`, covering fence-blindness AND level-blindness; the catalogue and all 17 adapters proven byte-identical; every affected pin re-derived rather than incremented; permanent cases on both axes in both generators; §9.3 amended to name the axis and the consumer it omitted; and the private frontmatter parser this reading surfaced escalated as V-29-35-01, recorded not fixed

**Wave 33** *(blocked on Wave 32 completion)*

- [x] 29-36-PLAN.md — WR-01 + WR-03: the `-1` contract classifier requires a guarded branch to LEAVE THE SCOPE, proven by a third plant, with the blind-spot list corrected in both directions; and `REACH.I5` restated from I5's own predicate with `REACH_FLOORS.I5` and `TWO_UNFENCED_CELLS` re-derived — plus the assertion that neither narrowing dropped the evidence its claim stands on

**Wave 34** *(blocked on Wave 33 completion)*

- [x] 29-37-PLAN.md — WR-02: `readRegistry`'s three-number refusal measured to fire on no input, then replaced by a witness that differs IN KIND (parsed ids against a globally-matched canonical-heading count built from the one canonical-id authority), proven to fire under a recogniser-drift mutation; the comment denying the shape deleted; the four LANG-03 probe edges asserted; and IN-02 and IN-04 recorded in source as deferred

**Wave 35** *(blocked on Wave 34 completion)*

- [x] 29-38-PLAN.md — WR-06: `WP-04`'s decidable half spelled once in the gate beside the WP-11 constants and emitted in the refusal that already cites it, held by two new pin members over the rule ROW, the four-mutation probe extended to six plus a control that reds none, and the pin renamed for the two rules it now holds

**Wave 36** *(blocked on Wave 35 completion)*

- [x] 29-39-PLAN.md — WR-07 + the round's disposition record: the census's drifting volume counters measured and converted to relationships while its vacuity equalities stay exact and the snapshot relocates behind the commit-pinned premise case; `docs/audit/29-round4-residuals.md` recording IN-01..IN-04 as deferred by user decision, the carried residual roll-up and the probe arithmetic; and every round-4 reproduction re-run on a mirror of the final tree after the freshness premise is asserted

**Gap-closure round 5** *(round-5 verification returned 8/8 must-haves and 0 mechanical gaps; UAT recorded TWO HUMAN DECISIONS which became the round's only gaps. G-29-1 → LANG-07, severity minor, user decision **(b) schedule its closure** — the private frontmatter parser in `generate-catalog.ts` that plan 29-35 escalated as V-29-35-01. G-29-2 → LANG-04, severity major, user decision **(c) structural fix** — the `comprehension` group's six fixed substrings, which any interposed word defeats; options (a) accept-the-bound and (b) append-phrasings were both explicitly REJECTED. Waves stay strictly sequential: 29-41 and 29-42 share `scripts/check-banned-claims.*`, and every plan rebuilds a committed `.js` with `tsc`, which cannot run concurrently without tearing a committed output — the phase-27 round-5 precedent)*

**Wave 37** *(blocked on Wave 36 completion)*

- [x] 29-40-PLAN.md — G-29-1 / LANG-07: the private flat key:value frontmatter parser DELETED from `scripts/generate-catalog.ts` and the exported authority consumed, matching the conversion its sibling `generate-role-adapters.ts` took in plan 27-23; the `Parsed<FrontmatterKeys>` return-shape adaptation given its own task because it is the one non-mechanical step — an unreadable frontmatter, an absent key and a key declared twice become THREE distinct findings where the deleted copy's empty-map return conflated the first two; `docs/catalog/README.md` proven byte-identical; and a DERIVED name-scoped tripwire over the recursive module enumeration with its denominator asserted, so a third copy cannot land green. Records V-29-35-01 closed in both artifacts that carry it open

**Wave 38** *(blocked on Wave 37 completion)*

- [x] 29-41-PLAN.md — G-29-2 / LANG-04, the structural fix: the bare term becomes a conditional member of the `comprehension` group reusing the `requiresOnSameLine` mechanism already proven by the `Simplified Technical English` member, so the guard decides a RULE rather than enumerating a list; every benefit-verb marker admitted with its own measured count in the `CONFORMANCE_VERB_MARKERS` idiom; the whole measured bypass family given a coverage verdict; `BANNED_CLAIM_EXEMPT_SUPPRESSED` re-derived from the run and moved in the SAME commit with its entrants named; and the PASS line's singular conditional-member clause rendered per member, because two members with two different marker lists make the old sentence state a check the gate did not perform. **Plan-time finding carried into the plan:** the UAT's `measured_probe` claim that all five bypasses red under the prototype is adjudicated rather than cited — the un-re-pinned constant makes the gate exit non-zero on every run, so the plan requires each red to be asserted BY NAME at file:line:column

**Wave 39** *(blocked on Wave 38 completion)*

- [x] 29-42-PLAN.md — G-29-2's test surface, record and adversarial pass: every family member held by a permanent case proven to DISAGREE with a reconstructed pre-fix control; the conditional-member cardinality assertion RELAXED to the measured count with its reason and still two-sided, never deleted; the property-based plant selectors made identity-asserting because a `find` takes the first match and a declaration reorder can defang a RED case; the rejected enumeration alternative recorded in `BANNED_CLAIM_EXCLUDED` with its measurement; the new predicate attacked on the degree of freedom it introduces — the co-occurrence WINDOW is a line-feed split over soft-wrapped prose — with every attempt and every success written into `docs/audit/29-round5-residuals.md` as a named residual, escalated and left open rather than absorbed or quietly fixed

**No requirement is re-marked complete by round 5 — LANG-04 and LANG-07 stay `Gaps Found` until round-6 re-verification.**

**Wave 40** *(blocked on Wave 39 completion)*

- [x] 29-43-PLAN.md — CR-01 / D-50: the banned-claim gate consumes the PRE-exemption public-document corpus. `publicDocsScan()` answers "which public documents does the VOCABULARY check apply to", not "which documents are public", so `CHANGELOG.md` fell out of the banned-claim scan silently while that exemption's own bound paragraph asserts it "does not exempt `CHANGELOG.md` from any other gate" — and two live `token-economy` occurrences sit there today. `publicDocsCorpus()` is exported beside the scan, the consumer names which question it asks, `BANNED_CLAIM_SCAN_COUNT` moves with the entrant named and the new value read off the gate's OWN refusal text, the two changelog sentences are rewritten to record the mechanism without restating the disproven claim, a permanent case plants a banned literal there and is RED-proven against a pre-change mirror, and WR-02's four further unscanned classes are each admitted with their count moved or named with a reason held by a mutation-proven completeness assertion

**Wave 41** *(blocked on Wave 40 completion)*

- [x] 29-44-PLAN.md — CR-02 / D-48 / D-49 / **D-53**: the enumeration is **DELETED, not relocated a third time**, on BOTH groups at once — both marker lists gone, `requiresOnSameLine` removed from the TYPE (proven by adding a member that declares one and watching `tsc` refuse it), `lineHits`'s conditional arm and the PASS line's conditional clause deleted with it, conditional members 3 → 0, and nine plants (five comprehension, four conformance) each shown moving 0-to-named across the change with their pre-change transcripts as the "before". Earlier note: Round 5 moved it from the phrase slot to a seven-stem verb list, defeated by five plants at exit 0 with the file never named; the reviewer's subject-side pin is REFUSED because subject is an equally open class. Every axis a bare term can be paired against is open, so the only bounded thing left is the one named exemption region — and a region is POSITIONAL. Both bare terms become unconditional, the marker constant goes with its spelling per the retired-construct convention, the admission cost is derived through the gate's own counter over the corpus 29-43 leaves, the live-tree RED naming the one ordinary-English occurrence is captured BEFORE the prose is rephrased, and the rephrase is dispositioned under D-04 with the ceiling table hashed identical. The two source passages asserting the spelling problem was closed are corrected, and the identical verb axis SURVIVING on the discipline-name member is measured with unlisted-verb plants and escalated — never changed, because D-48 and D-51 lock it

**Wave 42** *(blocked on Wave 41 completion)*

- [x] 29-45-PLAN.md — WR-06, WR-01, IN-01, IN-03, WR-04: properties asserted in the wrong place, plus the one whose subject D-53 deleted. **WR-06's in-gate refusal does NOT ship** — the type forbids the shape it would guard, so the loop could never fire and counting it in a PASS line is AP-1 exactly; a written disposition (*discharged, remedy superseded*) plus a type-level tripwire asserting no member carries a marker-shaped field under ANY name ships instead, mutation-proven in the direction `tsc` does not cover. WR-01's breakdown is derived at this wave and no earlier, because it must measure AFTER 29-43's corpus widening and 29-44's deletions, and must AGREE with the pin 29-44 moved. Earlier note: the suppressed count's sub-breakdown — which read 6+3+2 against a pin of 12 — is DELETED from the prose and published per group by the traversal that already computes the total; the magic negative in a field typed as a count is expressed in the type and asserted; the equality that stopped pinning a number gains a per-literal sibling proven by an asymmetric mutation; and `check-nul-bytes.ts` is widened to own the whole control-byte class over its unchanged tracked-path set, with the weaker duplicate reduced or removed and the boundary declared — the real lesson being that the gate existed and was not run

**Wave 43** *(blocked on Wave 42 completion)*

- [x] 29-46-PLAN.md — WR-03, IN-02, IN-04: prose that contradicts the code beside it. The four stale workflow cardinalities are DELETED rather than restated — the file-matching regex is the contract and it is range-free, so any number beside it is a second declaration that can only rot — with the committed `.js` twin corrected in the same commit and `docs/catalog/README.md` proven byte-identical by regeneration rather than argued from the edit being comment-only; a RED-proven assertion holds the corpus against its pinned constant so the class cannot recur silently; the edit-robustness comment is reworded to name which half is derived and which is deliberately brittle, with both premise equalities kept; and the two self-contradicting audit headings are retitled with their bodies byte-unchanged

**Wave 44** *(blocked on Wave 43 completion)*

- [x] 29-47-PLAN.md — D-52: the adversarial pass on the axis THIS round introduces, and the round-6 disposition record. Round 5 ran thirteen attempts and every one varied an INPUT while none varied its own new degree of freedom, which is why the defect shipped. Deleting the co-occurrence apparatus opens four axes — substring reach, term membership, the widened corpus, and **the exemption region as the SOLE carve-out**, which D-53 makes the sharpest of the four because it removes the only other route by which a legal denial could pass. Each is named in writing before the log and attacked one plant per reset mirror behind a freshness quote, two equal sha256 values and a clean control, with **at least seven attempts on the region** (five boundary positions plus two boundary-STABILITY attacks) out of twenty-five. Every carried `V-` id is RE-MEASURED rather than transcribed, and `V-29-42-01`/`-02`/`-04` are marked CLOSED BY CONSTRUCTION only after a closing measurement — "the mechanism is gone, so the residual is gone" is an argument, and this phase's six false harness results were each produced by that confidence. `docs/audit/29-round6-residuals.md` carries the verb-axis defect with its five plants under **CLOSED BY DELETION**, the two false closure claims round 5 shipped, the `understand` fail-closed residual, the term-membership residual demonstrated with plants rather than asserted, the conformance-verb escalation with its decision request, the two-direction roll-up, the LANG-08 override verbatim, the derived probe equality, and a twelve-row reconciliation verified against SUMMARY evidence rather than against a plan table
- [x] 29-48-PLAN.md — `V-29-47-05` / LANG-07: the requirements-traceability correction, applied under the round-6 verifier's named authority and nobody else's. HEAD is half-corrected — LANG-04 already reads `[ ]` / `Gaps Found` and is correct as it stands; LANG-07 still reads `[ ]` / `Gaps Found` and the round-6 disposition table says it must read `[x]` / `Complete`. Two cell edits, a two-line diff, each quoting the report row that authorises it. Then the derived audit: the LANG id set taken from the file by TWO routes (checkbox lines and traceability rows), both floored above zero, compared by symmetric difference — because a floor catches an EMPTY enumeration and never a SILENTLY SHORT one — and all eight checkbox/row pairs published with their sixteen real line numbers. The inversion was set by an automated marker reading `requirements-completed:` before any verification existed, so this plan's SUMMARY declares that field EMPTY and the constraint is stated where every other round-7 plan inherits it. Carries the round's one unresolved probe row (`LANG-07` / `unclassified`) as an explicit flagged assumption rather than dropping it
- [x] 29-49-PLAN.md — IN-04 / WR-02 / WR-05: the hygiene the exemption work must land on, in the order the review mandated. The false `V-29-42-03` block is DELETED with nothing in its place — its construct is gone, correcting it would leave a reader reasoning about it, and it is the doc comment attached to the sole carve-out's declaration, which the reviewer says makes its position the finding. The ONE READ invariant the module states at `:1464` is made TRUE: the scan loop stops re-reading the exempt document and spends the indices against the assembly that produced them — and the naive ternary's hole is bounded rather than shipped, because a cached empty string for a failed read would count the document visited while scanning zero lines. The unguarded exempt read (found while reasoning about that bound — WR-06's class one module over) gains a named refusal, RED-proven on a directory-at-path mirror where the pre-change build dies with a `node:internal` frame. And `publicDocsDerivationRefusals()` is exported with ONE reader per module and folded into the consumer before its vacuity floor, so a refusal raised while deriving the imported corpus is named by the gate that is RUNNING — directly load-bearing for 29-53, since a sixth derived part with a channel nobody reads repeats it
- [x] 29-50-PLAN.md — WR-04 / WR-03 / WR-06: one predicate, one read per path, and prose that is measured. `nulOffsets` is production-dead while its doc comment declares it the load-bearing half of the git cross-check and cites *"both call sites"* that do not exist — two implementations of the NUL predicate with the prose naming the dead one authoritative, which is the duplicate-authority shape this repository closes by DELETION, landing inside the plan whose stated subject was where the narrower duplicate was. It is deleted from source, twin and tests with its three cases MOVED onto the surviving scanner asserting offsets AND byte values, and a derived one-predicate case RED-proven by reintroduction guards against relocation. The declaration that justifies the `bytes` field asserts the exact claim the same module measured FALSE, with a worked example the module's own table contradicts 190 lines below — corrected by an ADDRESS CHECKLIST rather than a reading, because this is the round's own three-sites-corrected-fourth-site-missed pattern, and re-measured first-hand rather than transcribed. The reporting loop's bare re-read (the only path by which this gate dies without a verdict) is deleted by carrying the line and column from where the buffer was already in hand, and a third arm names the initialised submodule gitlink that currently reds as a permissions error. The unmeasured-external-assertion CLASS is escalated, not claimed closed
- [x] 29-51-PLAN.md — the one anchored-block authority CR-01's fix needs, built and proven inert before it is spent. Today the anchor grammar, the line assembly, the block extent and the byte comparison all live inside `check-claim-anchors.ts`; asking the same questions from `check-banned-claims.ts` would acquire a SECOND GRAMMAR over the same bytes, which is the exact defect LANG-07 names. So they move to `scripts/audit-model.ts` — a LIBRARY, so no importer inherits another module's refusal channel (the WR-05 seam 29-49 just closed). The scan RETURNS its assembly so no consumer re-splits, which deletes the coordinate-shear axis rather than guarding it, and one function returns extent, overrun and byte-identity together so a consumer cannot get the boundary without the verdict on its bytes. **When you unify two readers into one authority, that authority's SCOPE becomes a new degree of freedom** (this phase has already paid for it once, when a section-anchored fence reader searching to EOF adopted an unrelated later block) — so the reach is pinned at the declaration with its three bounds named, its deliberate fence-unawareness measured, and its edges RED-proven, including a blank verbatim refused because an empty comparison succeeds trivially and freezes nothing. Equivalence is proven by MUTATION on both builds across all six refusal classes, not by the old cases still passing, and the live block set is counted by two floored routes. The registry's unenforced `line` field is measured and escalated, never quietly repaired
- [x] 29-52-PLAN.md — **CR-01, behind a blocking `checkpoint:decision`**: the sole carve-out bound by CONTENT as well as by position. Every occurrence-bearing line inside the region that no anchored block covers gets a registry row and an anchor, the map DERIVED through the gate's own counter and group tally rather than read out of prose, and both pins re-derived from the gate's own refusal text rather than predicted into the source. Then suppression is CONJOINED with frozen-block membership: a line lifts the prohibition only if it is inside the region AND inside an anchored block whose bytes match its registry row. The exempt block set is DERIVED from the registry with its cardinality asserted two-sided (a hand-listed anchor set would be set-literal drift authored inside the fix for a fail-open), an EMPTY set inside a located region is a named refusal, and a diverged block produces both a diagnosis and findings rather than a finding whose reason lives in another gate's output. The self-consistency case is REPLACED by an assertion whose expected side is derived off the run's loop. `V-29-47-02`, `V-29-47-03` and `V-29-32-01` are closed by walking each residual's OWN route on a reset mirror — a subsumption argued from the rule's shape is what this phase's six false harness results were made of — and each narrowed remainder is stated with its direction rather than folded into the closure. The per-group composition pin ships as a cheaper complement with its non-closure stated in the source, so nobody later reads it as the fix
- [x] 29-53-PLAN.md — CR-02 / WR-01 / IN-03: the shipped manifests admitted, the class boundary declared, the exclusion moved to the point of effect. A sixth DERIVED part walks the plugin-manifest directory (a third manifest enters by EXISTING; the hardcoded pair the review sketched is the class that produced seven granted names and zero adapter files), with a named refusal plus an empty list on absence so the per-part vacuity floor can notice, the scan count moved with entrants named from the intermediate build's own refusal text, and the admission cost measured BEFORE admission. The `encoding` question is DECIDED in writing — raw bytes, line-oriented, uniform with every other member, covering keys, values and the second description nested inside the marketplace's plugin entry so there is no claim-bearing field list to rot — and the branch NOT taken is closed by a named refusal on any decoded value that is not byte-present in the raw text, with its own permanent case. A parse failure removes the guarantee and never the scan. The markdown class boundary, which was the shape of a denominator rather than a decision, is declared in prose and enforced by widening the coverage denominator to the shipped TEXT surface, with every tracked non-markdown file dispositioned by name. WR-01's prefix exclusion moves to the WALK, refusing descent into an excluded segment at any depth, derived from the ONE list with both projections' cardinalities asserted and their sum pinned to the list's length; the missing `scan ⊆ tracked` direction lands in the coverage case, which is where the tracked question belongs because the gate itself must stay runnable inside a `git archive HEAD` mirror. One walk budget is threaded through every in-module part with the cross-module boundary declared and the effective bound stated
- [x] 29-54-PLAN.md — IN-01 / IN-02, plus the probe row tested where it is falsifiable. The generator's contract regex is typed in BOTH the generator and the case that holds it, so widening one leaves the other holding the old contract and staying green — the named failure mode reappearing one level down, inside its own remedy. It is declared once in `scripts/kit-model.ts` beside the workflow count and imported by both, and the extraction is proven by MUTATING the shared declaration and watching both consumers move, because a suite that passes after an extraction has proven only that it compiles. The uniqueness of workflow `order`, asserted in a comment from a one-off round-5 measurement with nothing in the repository enforcing it (round-6 roll-up row 29-46 R2, fail-open, 0 live), becomes a REFUSAL at the point of effect — before the sort, naming both colliding files and the shared value — and the comment names the mechanism instead of a measurement. Because this plan edits the module carrying LANG-07's verified closure, it re-runs both regression checks against its own tree, ENUMERATES every leading-metadata read site rather than counting one function name, and answers plan 29-48's flagged assumption with its evidence and its remaining reach, leaving behind a permanent no-local-grammar case with its coverage bound stated
- [x] 29-55-PLAN.md — `V-29-47-06` and the round's record. `docs/audit/29-round7-residuals.md` disposes all twelve round-6 findings from the REVIEW's own list rather than from the plans' tables (a table assembled from plans proves only that plans were followed), matches every one of the verification report's `missing:` bullets to an artifact or to a stated rejection with its reason, and rolls up EVERY `V-` marker in the tree in both directions so it reconciles against round 6's. Every live count is RE-MEASURED on the final tree with its command recorded and the carried-count total asserted zero. The D-48/D-53 mechanism change is stated against the decision with the alternative not taken and its reason. Three equalities are published with both sides shown — probe rows, classes, findings — and the honest close names what round 7 does NOT claim: the surviving enumeration is the banned-literal list itself, so a claim in words that list does not contain still passes, FAIL-OPEN with its live count. The CI workflow's two stale gate-scope descriptions are corrected by naming the constant that holds a count rather than typing a third value at an address that has now rotted twice. And the sweep re-runs BOTH bypasses the round-6 review and verifier reproduced independently at HEAD, on sha256-verified mirrors that assert their own premise before planting, with the sibling gates' results recorded per mirror — because the finding was partly that only a sibling gate noticed

**Waves 53-57** *(gap-closure round 8, strictly sequential — every plan either rebuilds a committed `.js` with `tsc`, which cannot run concurrently without tearing a committed output, or writes into the same round-8 register)*

- [x] 29-56-PLAN.md — **D-55, the tracer**: the sentence `guard_banned_claims` publishes about itself narrowed to the predicate it decides. The PASS-line header at `:2103` opens with a quantifier over the shipped artifact and the user-facing documents; the mechanism underneath decides whether any member of a 22-entry pinned list appears on any single PHYSICAL LINE of a derived document set, outside registry-anchored blocks inside one region. Seven rounds each closed one axis of the distance between those two statements and exposed the next, because the distance is not a defect but an unbounded class — **a prohibition publishing a wider scope than its mechanism is the exact defect LANG-04 exists to prevent, committed by LANG-04's own guard**. The replacement names the unit of decision (which is what makes the hard-wrap residual visible rather than implied away) and INTERPOLATES both its numbers from the module's own derivations, so nothing at that address can rot a fourth time. The docblock's scope claim moves with it. Two permanent cases hold it: a behaviour case over the running gate's own first line whose expected values are read by a separate import, and a source-shape case mutation-proven by restoring the old wording at each address on its own re-extracted mirror. The set of places stating this prohibition's scope is DERIVED with a named command over a stated denominator with named exclusions, never accepted as a list, and every site gets a disposition row in `docs/audit/29-round8-residuals.md` §1. The matcher is byte-unchanged and the single-line plant still reds by name
- [x] 29-57-PLAN.md — **D-56: the hard-wrap axis disclosed, its remedy declined in writing, and its false premise corrected**. `lineHits()` matches each literal against one physical line and an ordinary hard-wrapped paragraph splits a listed multi-word claim across two lines at exit 0 — independently reproduced by round 7's review and its verifier. **Live count 0, direction FAIL-OPEN.** It is not fixed because the fix adds a second input assembly in the phase where round 6's fix produced round 7's finding, to close an axis with no live instance, against a threat model that is DRIFT: every finding that ever caught a LIVE claim here was a corpus-SCOPE defect, and every matcher-completeness finding has been at 0 live. So it gets `V-29-57-01` with three DERIVED measurements — reachability derived from the literal list itself rather than from word count (**a multi-word member whose every split leaves a bare pinned member intact is NOT reachable, so the review's multi-word figure over-states the reach and is corrected rather than carried**), the live count measured over the gate's own derived corpus by a recorded one-off that is deliberately never shipped, and the house-style wrap figure re-derived with its predicate STATED, since the review's is unreproducible without one. Both disagreements are printed and neither is reconciled. Then the in-source record at `:60-65`, which argues the bypass needs a wrap falling inside a word, is corrected to the shape the reproduction uses, with a source-shape case holding it
- [x] 29-58-PLAN.md — **D-55 reaching the kit's own prose, and paying the freeze's two-file price**. `agent-factory/writing-profile.md`'s honesty floor states the same totality claim in the one document the kit ships as its statement of what it does and does not assert — leaving it would leave the disclaimer wider than the gate it cites. The sentence lives inside `C-28-042`, one of the six registry-anchored blocks D-54 froze, so the prose and its registry row are ONE change in ONE commit and the pairing is DEMONSTRATED by watching both unpaired directions red on their own mirrors, not asserted. The row's `mechanism:` field carries a superseded corpus figure, a superseded literal count and the wrap framing plan 29-57 just corrected; all three move. Every exemption pin the edit disturbs is RE-DERIVED from the gate's own refusal text with the entrant named — a pin recomputed by hand at this address is the WR-01 defect where the gate is most trusted. What the file OWES is derived from each watching gate by running its derivation, never inferred from its directory, because an obligation absent by derivation and one nobody checked look identical afterwards. The residual paragraph then names BOTH surviving axes with their directions and ids
- [x] 29-59-PLAN.md — **D-57: the build-parity gate that cannot fail, repaired and PROVEN not vacuous**. `tsconfig.json` sets `outDir` to `./` so the build rewrites tracked outputs in place; `freshness.ts` reads the committed side from the WORKING TREE; `ci.yml` builds at `:59` before every freshness step with zero dirty-tree assertions — so CI compares a fresh build against a fresh build and **cannot detect a committed `.js` that was hand-edited or never rebuilt**, which is the guarantee `CLAUDE.md` names as the whole reason the tooling ships as committed `.js`. Pre-existing since Phase 20 and named by no LANG requirement, but round 7's own sweep used this ordering as its build-parity evidence and the identical command appears in every plan in this phase. The gate's SUBJECT moves to HEAD — set derived from `git ls-tree -r HEAD`, bytes read via `git show HEAD:` as Buffers — so no earlier step can repair what it judges; six branches each get a named fail-closed verdict, and the two comparison arms are selected from one derived set with their counts asserted to sum. **The repair's own acceptance is the discrimination pair**: the same planted stale output, on a clone of the pre-fix tree and a clone of the post-fix tree with `tsc` run on both, exit 0 then exit 1 by name — because a fix to a vacuous gate that is itself unproven repeats the defect. Clones, never `git archive` extracts, since the gate now reads git. The working-tree question gets ONE script authority called from CI rather than copied into it, and the coverage the subject change moves off the gate is recorded as `V-29-59-01` with an owner
- [x] 29-60-PLAN.md — **D-58: the round-8 record and the phase close**. Every round-7 finding and every verification `missing:` bullet gets a disposition citing the decision id that authorised it, including the four the fence puts out of scope, which get `V-` ids and an inheriting owner rather than an absence. The `V-` roll-up is DERIVED on the final tree and reconciled against round 7's published 35 with every entrant and departure named, and the carried-count is asserted zero. **The phase close is the load-bearing half**: written against the roadmap's five success criteria rather than the plan list, stating what Phase 29 claims at the granularity of a MECHANISM and what it does not, with every open axis named, directed and counted, and shown not to exceed the kit's own honesty floor. The requirement rows this round's evidence supports moving are RECOMMENDED with their authority named and applied by nothing — LANG-04's movement conditional on the narrowed claim verifying, the six stale rows under the round-7 verifier's own table — and `.planning/REQUIREMENTS.md` is asserted byte-unchanged for each of the five plans individually, because this is the phase whose rows were once inverted by an automated marker acting on a plan's own field. The sweep's command list is derived from the workflow rather than typed, all three reproductions are re-run with their harness choice explained, and every number the round published is re-taken with disagreements printed rather than reconciled

**GAP-CLOSURE ROUND 2 IS REOPENED, NOT CLOSED** *(human decision, Olger Oeselg, 2026-08-15, at plan
29-26's blocking checkpoint: `reopen-for-survivors`)*. All 26 plans are executed, and **26/26 executed
is not 26/26 verified** — R4 was non-empty. Four adversarial variants invented at execution survived
and were recorded rather than repaired: **V-29-26-01** (a setext heading is not a section boundary to
the authority; fail-open; 0 live), **V-29-26-02** (the derived scans proving LANG-07 read 41 of 49 and
47 of 53 while their case name and refusal wording call them tree-wide; 0 found in the unread sets),
**V-29-26-03** (`FENCE_DELIMITER_LINE` is a prefix test where CommonMark counts run length; fail-open;
0 live), **V-29-26-04** (the fence authority is column-zero anchored where CommonMark allows three
spaces of indent; **six live lines of `README.md` are read as governed prose today**; fail-closed only
by the accident that the four indented delimiters pair up). Phase 29 is **not** complete, no LANG
requirement is verified by this round, and re-verification does not run on this tree. Round 3 covers
the four survivors **only**; -01, -03 and -04 all live in the fence authority's grammar and are
plausibly one plan. Full record: `29-26-SUMMARY.md` and `docs/audit/29-locator-unification.md` §8.
**Finding ids collide across rounds** — round 1 and round 2 reuse WR-01, WR-03, WR-04, WR-07, WR-08
and IN-04 for different findings, and IN-02 is the only id that is the same finding in both.

**Round-2 findings disposition** *(the full register lives in `29-20-PLAN.md`; every one of the round's 14 findings is planned, and the seven round-1 carry-overs below are deferred with a written reason rather than dropped)*: WR-01
(voice-guard line numbers reported from the filtered remainder), WR-03 (three near-identical directory
walks), WR-04 (generated-file exclusion pinned by cardinality only), WR-07 (positive-literal extractor
scraping source by first occurrence, and its identity-keyed cache), WR-08 (two remaining path-literal
spellings), IN-02 (rows computed and discarded on two branches), IN-04 (`countWords`'s replacement
loop terminating only by an unstated argument).

**Cross-cutting constraints:**

- Sentence form conforms at 20 words procedural and 25 descriptive, decided by section anchor, with the procedural limit winning on a `## Steps` bullet (D-35, D-14).
- Byte growth is measured and recorded per file rather than assumed, because no ceiling exists for workflows and a later phase decides on that evidence (D-28).
- The governed corpus membership is unchanged at 47 files in four parts (D-36, D-42).
- Passive voice is left alone deliberately, with its reason recorded in the guard and the profile (D-15).
- Byte growth is measured and recorded per file rather than assumed (D-28).

**Honesty floor for this phase:** the profile is justified on determinism and one-term-per-concept grounds. Its effect on token count is **`UNKNOWN - verify`** in both directions and no study was located in either direction. Caveman-as-token-economy is **disproven on this artifact by measurement** and must not be restated. The profile does not govern the fenced caveman blocks, whose article density is already below the governed corpus's, so no article-restoration cost applies to them. The growth this phase produces is measured and recorded (D-28), and a later phase reasons from that measurement rather than from an estimate written before it. There is no evidence that controlled language improves comprehension for a language model, and the kit does not ship that claim.

### Phase 29.1: Per-Role Model Assignment (INSERTED)

> **CLOSED 2026-09-03 BY USER OVERRIDE — not by a passing verification.** `29.1-VERIFICATION-round5.md`
> returned `gaps_found` and that verdict is left standing, unedited. It verified **5/5 success
> criteria** and determined all seven requirements (MODEL-01…07) **Complete on behavioural
> re-derivation**; the three blockers are hardening defects in the *gates*, not failures of the
> delivery. Olger Oeselg accepted them open under **D-29.1-19** (`29.1-CONTEXT.md`). They are carried
> as Broken Windows rows **105** (kit-ref record truncation, FAIL-OPEN), **106** (`(r-class-authority)`
> non-recursive denominator, FAIL-OPEN) and **107** (`resolveModels` throws on `stems` — the defect of
> record is the false completeness claim in row 95). Deliberately **not waived**: `open_count` still
> carries them and `/gsd-ship` still blocks. `gsd-tools phase complete 29.1` refused this transition,
> correctly; the checkbox above was set by hand and this note is why.

**Goal**: A project can spend its model budget where judgment actually lives — a stronger model for orchestration, architecture and security review, a cheaper one for implementation, QE and UAT — set once on the config dial and emitted into every generated adapter, with zero-config behaviour byte-identical to today's.

**Depends on**: Phase 29 (`scripts/generate-role-adapters.ts` derives each adapter's `description` from the role's `## One job` first sentence, and Phase 29's D-19 rewrites exactly that section across all 17 roles; changing the same generator's frontmatter emitter first means regenerating and re-freshness-gating all 17 adapters twice)

**Requirements**: MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05, MODEL-06, MODEL-07

**Success Criteria** (what must be TRUE):

  1. With no `models` block in `factory.config.json`, all 17 generated adapters are **byte-identical to today's** — `model: inherit`, the user's session choice preserved. Zero-config is unchanged, not merely "still works". (MODEL-01, MODEL-02)
  2. One opt-in key switches the factory to the cost preset, and the role→tier assignment is **derived against `kit-model.listRoles()` with an asserted count**, so role #18 cannot arrive silently unassigned — the set-literal drift class this milestone exists to eliminate. (MODEL-03)
  3. Only the aliases `inherit` / `opus` / `sonnet` / `haiku` are legal values; anything else is refused **fail-closed**, and the emitted model value of all 17 adapters is asserted equal to the resolved config rather than to a hand-listed expectation. (MODEL-04, MODEL-05)
  4. The dial's Claude-Code-only reach is **stated in the kit and in `CLAUDE.md`**, whose "What NOT to Use" entry against non-`inherit` wrappers is amended to name this mechanism as the documented reason. The kit's single authority states that reach as a **property of what grugops emits** — per-agent adapters at `.claude/agents/` alone — and every clause it carries about the other four host CLIs travels with a named vendor source. (MODEL-06)
  5. Any cost or limit-savings claim is **measured** with `scripts/measure-cost.ts`, or carries `UNKNOWN - verify`. A tier assignment is not evidence of a saving. (MODEL-07)

**Plans**: 25/25 plans executed — 6 original, then gap-closure round 1's `29.1-07` … `29.1-11`, then round 3's `29.1-12` … `29.1-17`, then round 4's `29.1-18` … `29.1-21`, then round 5's `29.1-22`, with **gap-closure round 5's remainder (`29.1-23` … `29.1-25`) planned 2026-08-21** after round-4 verification returned `gaps_found` at **5/5** success criteria on three live gate defects — one of which round 4 itself created — plus two partials. **Waves are strictly sequential throughout (1→11, then 1→6 for round 3's plans, 1→4 for round 4's, 1→4 for round 5's)**. Four `.ts` files need their committed `.js` rebuilt (`model-tiers`, `generate-role-adapters`, `check-foundation-guards`, `adapters-freshness`), and two plans rebuilding a committed `.js` concurrently tears the output (the phase-27 round-5 precedent). Plan `29.1-02` is **not autonomous**: it opens with a blocking `checkpoint:decision` on D-05's one-way config key shape.

Plans:

**Wave 1**

- [x] 29.1-01-PLAN.md — TRACER: freeze the pre-phase adapter byte baseline at SHA `6f8411e` (derived from `git ls-tree`, non-empty premise throw, `git show` per file) **before the first generator commit**, then wire the zero-config resolver end to end — `scripts/model-tiers.ts` + committed `.js`, the generator's emit line, `model-tiers.js` into the freshness twin list — with all 17 adapters byte-identical [MODEL-01, MODEL-02]

**Wave 2** *(blocked on 29.1-01)*

- [x] 29.1-02-PLAN.md — blocking `checkpoint:decision` on the one-way `models` key shape, then the `tiered` preset table (17 rows, `rationale` required by the type, two-sided count, set-equality both directions) and the two-location config reader with every named refusal — unknown preset, unknown role key, illegal alias — as a discriminated result [MODEL-03, MODEL-04, MODEL-05]

**Wave 3** *(blocked on 29.1-02)*

- [x] 29.1-03-PLAN.md — the generator resolves above `adapters.map()` and refuses writing nothing (proven by filesystem state, not exit code), and the D-04 zero-config pin becomes an **observable** property of the freshness run rather than an accident of what the mirror omits [MODEL-01, MODEL-04]

**Wave 4** *(blocked on 29.1-03)*

- [x] 29.1-04-PLAN.md — `guard_model_assignment`: committed adapter bytes read through `admit()` (D-15), expectation recomputed never hand-listed, absence/cardinality/emptiness/mismatch as four distinct findings, with eight planted mutations plus a control proven RED by name [MODEL-03, MODEL-05]

**Wave 5** *(blocked on 29.1-04)*

- [x] 29.1-05-PLAN.md — one authority for the Claude-Code-only scope statement in `subagent.frontmatter.md`, `factory.config.md` pointing at it by path plus the D-17 disclosed limitation in clear voice, the `CLAUDE.md` row amended, and a cross-surface oracle that asserts the pointer rather than the prose [MODEL-02, MODEL-06]

**Wave 6** *(blocked on 29.1-05)*

- [x] 29.1-06-PLAN.md — MODEL-07 held as anchored CONTENT with its fail-open direction disclosed in a derived claim-registry row (D-16, no fourth literal group, `measure-cost.ts` untouched), then the fourteen-command phase gate block, build parity and the regression lane [MODEL-07]

**Gap-closure round 1 — PLANNED 2026-08-20.** Verification returned `gaps_found` at 3/5 success criteria plus one plan-declared must-have FAILED; a standard code review returned 1 critical / 8 warnings / 6 info. The user ruled **all fifteen findings in scope, none deferred**, and ruled MODEL-06 to "research all four and cite" — that research landed in `29.1-RESEARCH.md` §"Host-CLI per-agent model support" and its verdict is that **the shipped scope sentence is FALSE**: all four non-Claude host CLIs do carry a per-agent `model` field. The dial does not reach them because grugops emits no agent definition for any of them — a fact about this kit, provable locally, needing no citation. Gap sources: `29.1-VERIFICATION.md` and `29.1-REVIEW.md`. **Waves stay strictly sequential (7→11)** for the reason recorded above: four `.ts` files with committed `.js` twins are in scope and two plans rebuilding a committed `.js` concurrently tears the output.

**Wave 1 (round 2)**

- [x] 29.1-07-PLAN.md — CR-01 + WR-03 + WR-04 + the missing discrimination case: announce the WHOLE resolution (member count, override count, distinct aliases) rather than one of `resolveModels`' two inputs, anchor every announcement reader at position 0 on a prefix its emitter owns, delete the third hand-spelled marker site, and plant a `roles` block with no `preset` key into the freshness mirror so the half of the D-04 hole that had no case turns RED [MODEL-01, MODEL-04]

**Wave 2 (round 2)** *(blocked on 29.1-07)*

- [x] 29.1-08-PLAN.md — WR-01 + WR-02: close the `models` block's own key set by name with `MODELS_KEYS` before any legal key is read, so the partial-application shape that applied a preset while silently dropping overrides is refused (D-06's principle, one level up), and validate the preset before defaulting it so an explicit null is refused rather than coerced — with the configuration file's legitimately open top-level key set disclosed rather than implied [MODEL-04, MODEL-05]

**Wave 3 (round 2)** *(blocked on 29.1-08)*

- [x] 29.1-09-PLAN.md — WR-07 (both halves) + WR-08 + IN-01 + IN-02 + IN-04 + IN-05: promote both degraded guard branches from advisory to blocking, add `npm run generate:adapters` plus an adapter diff to the ubuntu CI gate block and pin that wiring at both ends, stop the verdict claiming a configuration came from nowhere while one exists and was refused, give `tieredCorpusRefusals` a production consumer that runs on the zero-config tree, and render the source repo-relative [MODEL-03, MODEL-05]

**Wave 4 (round 2)** *(blocked on 29.1-09)*

- [x] 29.1-10-PLAN.md — WR-06: replace the refuted authority sentence with the researched one plus four vendor citations and three `UNKNOWN - verify` residuals, move the oracle's pinned literal with it, repair the case a second marker would have silently re-targeted, and correct the two planning surfaces that restate the refuted premise — `REQUIREMENTS.md`'s MODEL-06 text and `ROADMAP.md`'s SC-4, which as written is unsatisfiable by any true sentence [MODEL-06, MODEL-07]

**Wave 5 (round 2)** *(blocked on 29.1-10)*

- [x] 29.1-11-PLAN.md — WR-05 + the hand-listed roster + IN-03 + IN-06 + `COVERAGE.md`: declare the two configuration locations once and document the whole-file precedence and its shadowing consequence where a configuring reader meets it, pin the surface roster against a run-time derivation rather than against its own hand-list, pin the full 40-character baseline SHA, and close the round with the full gate set on the final tree [MODEL-01, MODEL-02, MODEL-06, MODEL-07]

**Gap-closure round 2 — PLANNED 2026-08-20.** Round-2 verification returned `gaps_found` at **4/5** success criteria (up from 3/5), with **MODEL-06 closed** and **MODEL-05 REGRESSED** from Complete back to Gaps Found; a round-2 code review returned 1 critical / 6 warnings / 5 info. **The blocker is R2-CR-01, and it was reintroduced by the round-1 fix that closed WR-08**: `relativeToRoot` in `scripts/check-foundation-guards.ts` compiles an unvalidated, unresolved `CHECK_ROOT` into an unanchored regex, so under a relative root — a documented invocation shape — the guard publishes a green verdict naming a configuration file that does not exist, and can corrupt a refusal reason the same way. Reproduced independently twice; unexercised by 442 passing tests because every planted mirror in the suite uses an absolute root. The user ruled **all twelve findings in scope, none deferred**, and ruled **D6** directly: `agent-factory/config` joins `check-kit-refs`'s SCAN with an explicit, argued, derived and count-asserted exemption. Gap sources: `29.1-VERIFICATION-round2.md` and `29.1-REVIEW-round2.md` — **not** `29.1-VERIFICATION.md`, which is the round-1 record and is stale. **Waves stay strictly sequential (1→6)** for the reason recorded above: several `.ts` files with committed `.js` twins are in scope, plans 12/13/15/17 all touch `scripts/check-foundation-guards.ts`, and two plans rebuilding a committed `.js` concurrently tears the output.

**Wave 1 (round 3)**

- [x] 29.1-12-PLAN.md — TRACER: R2-CR-01, the round's only blocker. Normalise `CHECK_ROOT` before anything is compiled from it AND require a path boundary after the matched root, from one declaration rather than two hand-kept copies, so the rewrite is a path-prefix match; proven by a byte-identity of two live CLI runs that is RED pre-fix and GREEN post-fix, with each half mutation-proven separately, plus cases for a relative root, a relative-root refusal reason, a prefix-sibling root, and a derived count-asserted residual over the other thirteen `CHECK_ROOT` gates [MODEL-01, MODEL-03, MODEL-05]

**Wave 2 (round 3)** *(blocked on 29.1-12)*

- [x] 29.1-13-PLAN.md — R2-WR-03 + R2-IN-01: correct the stray-pin arm's soundness argument to the mechanism a measurement actually found (the packaging templates are ADMITTED and skipped by the no-`model`-key arm, not by the unadmittable one), make the silence an assertion with an honest probed-of-derived denominator, exercise the previously unproven arm with a planted unadmittable surface that reds `guard_wr05`, test the three arms as a UNION, and make case `(m)` forbid the advisory CHANNEL rather than one identifier [MODEL-03, MODEL-05]

**Wave 3 (round 3)** *(blocked on 29.1-13)*

- [x] 29.1-14-PLAN.md — R2-WR-02 + R2-IN-05: bound the CI-wiring pin at the next step and probe comment-stripped COMMAND lines by exact equality, assert the block's boundedness rather than relying on today's coincidence, apply the same discipline to the sibling unbounded reader and COUNT the class, and replace the adapter-cleanliness command with a form that can see an untracked adapter — every change proven by mutations the old pin could not see, with the old shape demonstrated green on the same input [MODEL-01, MODEL-05]

**Wave 4 (round 3)** *(blocked on 29.1-14)*

- [x] 29.1-15-PLAN.md — R2-WR-01 + R2-WR-06 + R2-IN-02 + R2-IN-04: give `overrides` the Floor 0 shape validation `preset` already has, so a nullish value is refused rather than discarded and a plain object is refused rather than thrown; prove every `MODELS_KEYS` member is CONSUMED and not merely permitted, with the probe table's coverage asserted both ways and both mutation directions recorded; delete `roleCorpusCardinalityRefusal` rather than keep an exported authority with no production consumer, including its mention inside a shipped guard finding; and make the payload parser enforce the alias grammar its refusal quotes [MODEL-01, MODEL-03, MODEL-04]

**Wave 5 (round 3)** *(blocked on 29.1-15)*

- [x] 29.1-16-PLAN.md — R2-WR-04 + R2-IN-03: take BOTH halves of the review's either/or — the packaging authority stops restating the precedence rule in any wording, ordinal included, AND deletes the undecidable claim it made about its own prose (the D-59 shape D-16 already applies to MODEL-07) — then bound the bullet region at both ends, rename the anchor to the one wording it pins, add a DERIVED assertion refusing every spelling of the second configuration location, disclose the paraphrase residual with its direction, and make the two freshness cases assert the gate's sentence rather than a bare digit. Carries D5 and both MODEL-07 backstop items forward [MODEL-01, MODEL-02, MODEL-06, MODEL-07]

**Wave 6 (round 3)** *(blocked on 29.1-16)*

- [x] 29.1-17-PLAN.md — R2-WR-05 / D6 as the user ruled, and the round's close: add `agent-factory/config` to `check-kit-refs`'s SCAN — the installer's `copyKit` ships that directory to every target, so its absence from the scan was by omission and not by audience — with an exemption that is a PREDICATE over hits rather than a list, both cardinalities asserted two-sided, a vacuity floor on the denominator and the exemption published in the pass line; delete the test file's second hand-maintained copy of the scan set in favour of a derivation from the gate's own declaration; correct the three records this change falsifies (annotating `29.1-11-SUMMARY.md`, never rewriting it); and reconcile all twelve findings plus three carried human items in a derived, counted table [MODEL-01, MODEL-02, MODEL-05, MODEL-07]

**Gap-closure round 4 — PLANNED 2026-08-20.** Verification round 3 returned `gaps_found` at **5/5** roadmap success criteria — every criterion holds and **MODEL-05 was ruled COMPLETE** — with `gaps_found` recorded on two live gate bypasses round 3's own work introduced, plus three partials. The code review returned 2 critical / 2 warnings / 4 info over the same tree. None of the five success criteria is falsified by this round's gaps; what is falsified is the **gate contracts** the phase built. The user's orchestrator ruled `check-public-docs-vocabulary.ts:32` **in scope** rather than deferred. Gap sources: `29.1-VERIFICATION-round3.md` and `29.1-REVIEW-round3.md` (the unsuffixed `29.1-VERIFICATION.md` and `-round2.md` are stale). **Waves stay strictly sequential (1→4)** for the standing reason: two plans rebuilding a committed `.js` concurrently tears the output.

**Wave 1 (round 4)**

- [x] 29.1-18-PLAN.md — TRACER: both round-3 blockers, one file, one sentence said twice — count the unit you claim, and say what a name may BE. Derive the exempt-MENTION cardinality from the same extraction the predicate already runs and assert it two-sided beside the line and file counts (the declared number has never described this tree: three mentions on two lines); replace the character-class-plus-`existsSync` self-reference test with membership in the depth-1 regular-file set the configuration directory actually carries, which refuses `.`, `..`, a subdirectory basename and a ghost by ONE rule; publish all three units in the verdict; and carry a case for every bypass this round reproduced, each recorded pre-fix GREEN against the committed `.js` [MODEL-05, MODEL-06]

**Wave 2 (round 4)** *(blocked on 29.1-18)*

- [x] 29.1-19-PLAN.md — R3-WR-01 + IN-01 + IN-02 + IN-03: the round's "a proof that cannot fail" class. Prove the ubuntu-block right bound over a SYNTHETIC workflow that has a following step, add a permanent bounded-versus-unbounded discrimination control on identical bytes, and DELETE the three tautological containment assertions rather than leave failure messages no input can print; reconcile the two readers onto one slice base (measured: 21 command entries against 20, the extra one being the step name itself); derive `(r-class)` membership from a step-name prefix with the widening measured against the predicate it replaces; widen the `CHECK_ROOT`-census predicate to the assembled-pattern shape a fixed byte window cannot see; and derive the hand-typed schema cardinality inside a NEGATIVE assertion from `CANONICAL_SCHEMA.length`, asserting the sentence PRESENT elsewhere in the same run so the negative discriminates [MODEL-01, MODEL-03, MODEL-05]

**Wave 3 (round 4)** *(blocked on 29.1-19)*

- [x] 29.1-20-PLAN.md — round-3 gaps 4 and 2's third instance: pin the packaging authority's cross-document section citation so the kit's only pointer to the precedence rule cannot be orphaned by a rename — every cited section name asserted present as a heading in the surface the citing sentence names, compared through ONE declared canonical form with an injectivity floor under it and the citation cardinality asserted two-sided, proven on synthetic documents and confirmed by a live rename against the verifier's measured 33-passed green; then delete the two model-bullet containment assertions the reader's own slice guarantees and give that bound a synthetic case with its own discrimination control [MODEL-02, MODEL-06]

**Wave 4 (round 4)** *(blocked on 29.1-20)*

- [x] 29.1-21-PLAN.md — R3-WR-02 + IN-04 + gap 5 + the round's close: make the quoting operation TOTAL and route both refusal sites through it, so `resolveModels` returns rather than throwing on the four shapes the verifier reproduced as TypeErrors — one authority, the second spelling deleted, proven against the REBUILT committed `.js`; correct the anchored reader's docstring to its mechanism and pin both halves with measured cases; DECIDE the unknown-stem override rather than carry it; scope or delete the standing claim about another gate's scan-set constant per the user's ruling and close `WINDOWS.md` rows 78 and 79 on conditions read rather than assumed; and reconcile every round-3 item in a derived, counted disposition table with the three backstop human items carried forward verbatim [MODEL-04, MODEL-05, MODEL-07]

**Gap-closure round 5 — PLANNED 2026-08-21.** Verification round 4 returned `gaps_found` at **5/5** roadmap success criteria — every criterion independently re-derived and holding, and the shipping mechanism working — with `gaps_found` recorded on **three live gate defects plus two partials**. Round 4 genuinely closed three of round 3's five items and each closure discriminates under a planted mutation, which is the first time in this phase a closure was provably not a restatement. It nonetheless left the D-08.1 exemption open on the same-line axis on **three** spellings (one of them found by the verifier and reported by no review, absorbing an instruction that names a private-key path); **regressed** `resolveModels` so that two inputs which returned `{ok:true}` at `e9907f5` now throw, under a plan whose first must-have truth was that the refusal path returns on every input; and shipped a `(r-class-prefix)` requirement that is **structurally incapable of failing for either of its two members**, disclosed in `WINDOWS.md` row 80 under a mitigating clause the verifier measured FALSE. Gap sources: `29.1-VERIFICATION-round4.md` and `29.1-REVIEW-round4.md` (every earlier verification file is stale). **Waves stay strictly sequential (1→4)** for the standing reason: two plans rebuilding a committed `.js` concurrently tears the output (the phase-27 round-5 precedent). Plan `29.1-25` is **not autonomous** — it opens with a blocking `checkpoint:decision` on whether the D-06 reversal needs a numbered decision record and a `CHANGELOG` entry, or whether `WINDOWS.md` row 86 suffices for a pre-1.0 internal contract.

**Two of round 4's own prescriptions were falsified during planning and are not taken on faith.** Its suggested cheap repair for the class check — strip `const NAME =` declaration lines before the containment tests — was measured against this tree: after stripping, the right-bound marker's canonical spelling occurs **zero** times in **both** members, because the only place either file spells those bytes is the declaration, so the repaired requirement would red a correct tree. And the occurrence-level extraction that closes all three kit-ref spellings was prototyped against the live document first, confirming it leaves the three published cardinalities at 3 mentions on 2 lines in 1 file while refusing every bypass.

**Wave 1 (round 5)**

- [x] 29.1-22-PLAN.md — BLOCKER 1 + PARTIAL A, one file, one change of UNIT. The extraction emits exactly one record per occurrence of the needle the gate greps, so the number the gate PUBLISHES is the number it counted and no occurrence can be absorbed unasked; membership is decided over the WHOLE path rather than its first segment, and the second grammar is DELETED rather than widened; a run-time premise floor compares the two counts on every hit and names both when they differ; then one path-spelling authority for every path entering either set Assertion 3 compares, with a two-directional `path.win32` case — plus RED cases at the suffix, no-capture, bare-directory and UNION positions, every premise asserted in both units [MODEL-05, MODEL-06]

**Wave 2 (round 5)** *(blocked on 29.1-22)*

- [x] 29.1-23-PLAN.md — BLOCKER 2 + WR-02 + WR-04: route the role STEM through the same quoting authority the alias already uses, at both refusal sites, so no sentence carries a guarded position beside a raw one and every legal key's wording stays byte-identical; measure the regression across three committed builds from one script (`e9907f5` returned, `ad033f0` threw, rebuilt HEAD refuses by name); widen the totality corpus to the KEY axis and the KEY×VALUE cross product with the cell count derived on both axes and the in-loop counter asserted; make the hand-typed denominator observable and pin the corpus size two-sided; and run the one-spelling scan over the committed `.js` hosts execute as well as the `.ts`, with the comment strip's limits stated in the message [MODEL-04, MODEL-05]

**Wave 3 (round 5)** *(blocked on 29.1-23)*

- [x] 29.1-24-PLAN.md — BLOCKER 3, closed STRUCTURALLY rather than by a wider text scan: ONE implementation of the ubuntu-block region in a test-only `scripts/ci-workflow.testkit.ts` consumed by both readers, so "this class reads one region" is true by construction; the slice base proven by two COMPUTED regions rather than by source text, with the exact R3-IN-03 divergence watched RED by name against a baseline pinned in the same session; membership derived from the IMPORT and pinned two-sided, with a negative closing the class against a third reader bringing its own locator; the recorded not-sharing-a-helper disposition reversed with the measurement that forced it; and `WINDOWS.md` row 80 rewritten to the measured fact rather than softened [MODEL-01, MODEL-05]

**Wave 4 (round 5)** *(blocked on 29.1-24)*

- [x] 29.1-25-PLAN.md — WR-03 and the round's close, opening with a blocking `checkpoint:decision`: rule whether the D-06 reversal needs a numbered decision plus a `CHANGELOG` `Changed` entry or whether `WINDOWS.md` row 86 is a sufficient record for a pre-1.0 internal contract, then apply exactly the ruled option and make the ledger point at whatever record now exists; reconcile every round-4 item — five gap entries with their artifacts and missing bullets, twelve review findings, the anti-pattern rows, the coincidental-reliance item — in ONE disposition table whose item list is DERIVED from the two round-4 documents with its count asserted, distinguishing "closed, watched red" from "dispositioned"; carry the three human items forward verbatim without adding any mechanism that decides them; and re-reproduce all three round-4 blockers against the final tree, each mutation asserted landed before its run is believed [MODEL-04, MODEL-06, MODEL-07]

**MODEL-02 and MODEL-03 are deliberately absent from round 5's plan set.** No round-4 gap touches the generator's emit of the resolved value or the derived role→tier map; both were re-derived behaviourally by the round-4 verifier and hold. Fabricating coverage for a requirement no gap implicates would be the claim-wider-than-mechanism defect this phase exists to close.

**Honesty floor for this phase:** the motivation is quota and cost relief, but grugops has never shipped an unmeasured cost claim — v2.0's ~50% figure stayed `UNKNOWN - verify` rather than borrowing DeLM's benchmark. A model-tier split is a *plausible* saving, not a measured one, and must be described that way until `measure-cost.ts` says otherwise. Assigning a cheaper model to a role is also a **quality** decision, not only a cost one; the preset's rationale is recorded per role so a later reader can dispute the assignment on merit.

**Scope amendment 2026-08-19 (D-17, user-ruled at plan time):** the **install delivery path leaves this phase** and becomes Phase 29.2. 29.1 keeps all seven requirements MODEL-01 … MODEL-07 — none of them mandates the install path — and drops CONTEXT.md's D-01/D-02/D-03. **Disclosed limitation of the 29.1 increment:** until 29.2 lands, a per-repo `models` block is **inert for an installed target**; the mechanism resolves and emits correctly in-kit, but an installed repo's adapters are whatever the kit shipped. This is stated in the kit, not left to be discovered. Two further user rulings apply: **D-15** — the new `guard_model_assignment` reads through `scripts/canonical-frontmatter.ts`'s `admit()`, not `scripts/frontmatter.ts` (D-12's intent, one authority per predicate, over its letter); **D-16** — MODEL-07's prohibition is held as **content** in `docs/audit/28-claim-registry.md` with its **fail-open** direction disclosed, per the D-59 precedent, rather than adding a fourth `cost` literal group. See `.planning/phases/29.1-per-role-model-assignment/29.1-CONTEXT.md` §"Amendments after research".

**Criterion amendment 2026-08-20 (gap-closure round 1, plan 29.1-10):** **success criterion 4 was corrected, because as originally written it was unsatisfiable by any true sentence.** Its final clause required the other four host CLIs to possess no per-subagent model mechanism at all, and required that no claim in the kit say otherwise — and the user's ruling to *research all four and cite* produced exactly the opposite finding. The superseded wording is recorded verbatim in this file's git history and quoted in `.planning/phases/29.1-per-role-model-assignment/29.1-VERIFICATION.md`; it is described rather than reproduced here so that a `grep` for the refuted claim over the live planning surfaces returns nothing. `.planning/phases/29.1-per-role-model-assignment/29.1-RESEARCH.md` §"Host-CLI per-agent model support (gap-closure research, MODEL-06 / WR-06)" establishes, HIGH against primary vendor documentation for each, that **all four** carry a per-agent `model` field: Codex CLI (`.codex/agents/*.toml`), Gemini CLI (`.gemini/agents/*.md` frontmatter, defaulting to `inherit`), OpenCode (`.opencode/agents/*.md` frontmatter or `agent.<name>.model`) and GitHub Copilot CLI (a `.agent.md` property). The criterion's *substance* is unchanged — the dial still reaches Claude Code only — but the reason is now the true one: grugops emits no agent definition for any of the other four, a fact about this kit rather than a claim about their products. The superseded wording is described here rather than silently replaced; the same amendment is recorded against MODEL-06 in `.planning/REQUIREMENTS.md`. Neither MODEL-06 nor this phase is marked complete by that plan — the verdict belongs to phase verification.

### Phase 29.2: Model Assignment Delivery Path (INSERTED — split out of 29.1 by D-17)

**Goal**: A `models` block written into a target repo actually changes the adapters that repo's Claude Code session loads — closing the disclosed gap 29.1 ships with.

**Depends on**: Phase 29.1 (the resolver, the tier table, the alias contract and `guard_model_assignment` must exist and be gated before anything tries to deliver their output into a target)

**Requirements**: *(none new — this phase completes the delivery half of MODEL-01 … MODEL-05 for installed targets; no MODEL row is re-opened)*

**Status**: **NOT DISCUSSED.** CONTEXT.md's D-01/D-02/D-03 moved here and **all three must be re-decided from scratch** against these facts, each verified against the tree on 2026-08-19:

  1. **D-01 reverses an explicit decoupling.** `install/kit-source.ts:29` states *"This module deliberately does NOT import scripts/kit-model.ts"*, with D-18's separation-of-duty rationale spelled out around it. Install-time generation reverses that; the reversal needs its own decision, not an inherited one.
  2. **D-01 lands on an ordering conflict.** The adapter loop runs at `install/install.ts:1451`/`:1472`, but `seedState()` — which creates the target's config — runs at `:1555`. At adapter time there is no target config to resolve against.
  3. **D-03's premise is already true and its divergence does not exist.** All **17 of 17** adapters carry `MAT_SLOT` and route through `materializeAdapter()` (a `writeFileSync`), so adapters are copies under `--symlink` **today** and `INSTALL_MODE` never reaches an agent adapter on any path. D-03 was written to resolve a conflict that is not there.

**Plans**: 6/6 plans executed, then verified `gaps_found` (9/10) — 3 gap-closure plans added *(discussed 2026-09-03, researched and planned 2026-09-04; D-01/D-02/D-03 were re-decided from scratch against the three tree facts above, not inherited)*

Plans:
**Wave 1**

- [x] 29.2-01-PLAN.md — install mirror-spawns the committed generator against the target's own config and materializes the rendered adapters; an illegal block refuses the class by name; the run reports the resolution

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 29.2-02-PLAN.md — skip-if-identical writes, the target-true GENERATED banner, the `--check` staleness verdict (WARN, FAIL under `--strict`), and the structural pins on the twin list, the banner literal, the report ordering, the seed, uninstall parity and the self-checkout refusal

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 29.2-03-PLAN.md — the 29.1 "inert for an installed target" disclosures rewritten to current truth (with the asserting test re-aimed in the same commit), the CHANGELOG entry, and an independent review plus an eight-hypothesis adversarial reproduction pass before the phase is called complete

**Gap closure round 1** *(planned 2026-09-04 from `29.2-VERIFICATION.md` `gaps_found` 9/10 and the ten findings in `29.2-REVIEW.md`; run with `/gsd-execute-phase 29.2 --gaps-only`)*

- [x] 29.2-04-PLAN.md — bound what a target adapter destination may be before the installer writes through it: one hazard predicate serving both `materializeAdapter()` and the `--check` doctor, covering the leaf-symlink, ancestor-directory-symlink and dangling-symlink shapes measured writing outside the target at exit 0; plus WR-04's hard refusal and an adversarial re-run against the fixed build *(wave 1)*
- [x] 29.2-05-PLAN.md — the preview that disagreed with the run it describes (WR-01), the guarded render cleanup (WR-03), one wording authority for the temp-mirror disclaimer (WR-05), the config-reference sentence D-06 made false for installed users (WR-02), and IN-01 / IN-02 / IN-04 *(wave 2)*
- [x] 29.2-06-PLAN.md — a disposition for every one of the ten review findings, the ledger rows this round earned (including what the Windows leg cannot observe), and the proven gate block re-run with its results recorded against the measured baseline rather than described as green *(wave 3)*

### Phase 30: Per-Checkpoint Autonomy Matrix

**Goal**: A project decides where a human stops it, per checkpoint — including the four current safety floors — and a lowered floor is never silent: it takes a second key an agent cannot set, it shows up in the trace and in the run banner, and the public claim it backed is dropped.
**Depends on**: Phase 29 (the controlled-language pass touches governance and config prose; doing 30 first means rewriting freshly-written governance text and re-running this phase's expensive red-team gate a second time)
**Requirements**: AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTO-05, AUTO-06, AUTO-07
**Success Criteria** (what must be TRUE):

  1. Every human stop in the kit is a member of one closed, exported checkpoint set sourced from the `## Stop conditions` and role `## Hard limits` sections — and adding a checkpoint without a default is a **compile error**, not a silent default-open. (AUTO-01)
  2. A per-checkpoint ternary matrix (`block` / `notify` / `off`) replaces the `autonomy` scalar; any unknown, malformed, or unreadable value gates **at least as strictly as `block`**; and `readGovernanceConfig` / `readGovernanceConfigResult` collapse into a **single** discriminated-result reader whose failure path is fail-closed — the second authority is deleted, not joined by a third. (AUTO-02, AUTO-06)
  3. Lowering a safety floor requires **two keys** — a declaration in config (agent-writable, form-checked only) plus authorization via a per-floor session env var the hook process reads fresh (agent-unwritable) — so an agent that writes the config alone changes nothing, and no grant is blanket. (AUTO-03)
  4. `test_integrity` is enforced at the **point of effect** — `emitVerdict()` refuses GREEN — rather than being handed a false-equivalent env-var mechanism it cannot actually enforce; the hook-enforced vs in-process tier split is stated explicitly rather than papered over. (AUTO-04)
  5. A lowered floor is visible without reading config: the generated guarantees render and a per-run banner both name every non-default checkpoint, so a lowered floor can never leave an overstated claim standing in the docs — and a zero-config repo behaves **exactly** as it does today, with no floor lowered by omission. (AUTO-05, AUTO-07)

**Plans**: 11/11 plans executed

Plans:
**Wave 1**

- [x] 30-01-PLAN.md — Tracer: one floor end-to-end (roster, config cell, reader field, hook matrix lookup, key two, banner, re-freeze)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 30-02-PLAN.md — Floor set settled: `SAFETY_FLOORS` remap, registry `depends_on` remap, duplicate-id refusal
- [x] 30-03-PLAN.md — Reader collapse to one discriminated authority, `admit()` degrade, config-reading-site count

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 30-04-PLAN.md — Checkpoint derivation, workflow stop-bullet tagging, two-sided cardinality, compile-error proof

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 30-05-PLAN.md — `emitVerdict()` refuses GREEN at the point of effect, the missing emission verb, gate-workflow correction
- [x] 30-06-PLAN.md — Retire the `autonomy` scalar: validator polarity flip, three config surfaces, sixteen fixtures, migration table

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 30-07-PLAN.md — Guarantees generator, freshness gate, and language-gate scan-set wiring
- [x] 30-08-PLAN.md — Notify path with a trace finding, banner/decision agreement, full-roster zero-config differential

**Wave 6** *(blocked on Wave 5 completion)*

- [x] 30-09-PLAN.md — Claim-dropping in place, pointer lines, settings-file residual row

**Wave 7** *(blocked on Wave 6 completion)*

- [x] 30-10-PLAN.md — Red-team surface B (derivation, reader, validator, render, banner)

**Wave 8** *(blocked on Wave 7 completion)*

- [x] 30-11-PLAN.md — Red-team surface A (two-key hook path, point-of-effect refusal)

**This is the direct successor to Phase 25** — the hardest phase in the project (8 rounds; 13 documented green-suite-insufficient bypasses across the milestone; closure required a structural fix + ≥2 independent red-teams + self-reproduction). Its **red-team rounds are budgeted as scope, not overrun.** A green suite does not close a floor here.

**Research flag:** plan with `--research-phase`. The two-key floor-lowering mechanism and the `test_integrity`-to-point-of-effect move both touch `emitVerdict()`, a byte-frozen safety path, and deserve their own red-team round separate from the rest of the phase.

### Phase 31: Autonomous Manual Testing

> **CLOSED 2026-09-13 BY USER OVERRIDE — not by a passing verification.** `31-VERIFICATION.md` (round 9)
> returned `gaps_found` **4/6** and that verdict is left standing, unedited. UATX-02…UATX-05 are verified;
> UATX-01 fails on **CR-31** (`admitAndAppend`'s gated branch never applies D-14) and UATX-06 on
> **CR-28 / CR-29 / CR-30** (`deriveImportRenames` follows renames only from `@playwright/test`). Olger Oeselg
> accepted them open, with WR-43…WR-47, under **D-44** (`31-CONTEXT.md`), after nine rounds in which each
> round's fix created the next Critical inside the same two predicate families. Each item's file:line,
> reaching input and closing criterion is in `31-VERIFICATION.md` and `31-REVIEW.md`; `Owner: unassigned`.
> **Standing rule adopted with D-44: every later phase carries a four-round gap-closure cap.**

**Goal**: An agent can drive a real browser to produce UAT evidence, and the only thing that counts as evidence is an artifact the §14 gate re-runs — never the agent's narration of what it saw.
**Depends on**: Phase 30 (browser evidence enters through the verify-before-write path, and Phase 30 is where that path's dialability is settled; evidence written against a floor whose semantics change a phase later would have to be re-derived)
**Requirements**: UATX-01, UATX-02, UATX-03, UATX-04, UATX-05, UATX-06
**Success Criteria** (what must be TRUE):

  1. A committed Playwright spec, re-run by the existing §14 gate, is the machine-verifiable evidence floor; an agent's narration or an MCP tool-call transcript never produces a stamp. (UATX-01)
  2. An agent authors those specs through browser MCP tooling, with `@playwright/mcp` pinned (pre-1.0) and the setup documented for all five host CLIs — and `package.json` gains nothing, because the server is `npx`-invoked by the user's own agent. (UATX-02)
  3. Claude in Chrome is available as an optional, clearly-labelled `verified_by: <named human>` lane and is **structurally barred** from producing a `§14-gate` stamp — attended-only, force-disabled under API-key auth, Claude-Code-only, so it can never be gate evidence. (UATX-03)
  4. An evidence note carries commit SHA + gate-run id + content hash, and a note whose SHA is not the HEAD the gate ran against is refused. (UATX-04)
  5. An absent or unusable browser produces a **loud skip** that leaves the UAT `pending` (reusing the existing Tier-2 convention verbatim, never a silent pass), and a generated spec containing a conditional or caught assertion is rejected over the **TypeScript AST** rather than by regex, so the claim matches the mechanism. (UATX-05, UATX-06)

**Plans**: 44/44 plans executed — 38/38 executed, plus **6 round-9 gap-closure plans (`31-39`…`31-44`, gap waves 27-32) PLANNED 2026-09-11, of which `31-39` EXECUTED 2026-09-12 and `31-40`…`31-44` are not yet executed** after the EIGHTH verification returned `gaps_found` **5/6** — genuine, independently-confirmed movement, because UATX-06's predicate family is VERIFIED for the first time in this phase's eight rounds — while UATX-01 failed for the EIGHTH consecutive round on two NEW Criticals that round 7's own CR-22 fix created, both reproduced by the verifier against the committed `.js` at HEAD: `CR-26` (`admitAndAppend`'s gated branch falls open to the caller's dial root when its context store is ungoverned, splitting a human-disposed finding from its own GOV-02 record across two repositories, where the sibling `promoteAdmitted` declines that identical shape by name) and `CR-27` (`promoteAdmitted`'s fall-through hands the derived destination into the one parameter `admit()` uses for BOTH the governance-dial read and the ledger record, so a caller-supplied destination now decides whose configuration adjudicates the admission — measured ADMITTING a note that the caller's own deliberately-unreadable trusted configuration refuses). Round 9 does not fix two coordinates: it removes the freedom that produced them. `31-39` installs ONE authority for which repository owns an action, answering with a discriminated result that has no null member so a consumer cannot fall open without an explicit branch, separates the DIAL root from the LEDGER address across `appendNote`/`admit`/`admitAndAppend` — paying the deliberate `admit()` unfreeze and freeze re-base that `R-31-33-01` already published as the only thing that closes this class, and that `31-33`'s own prohibitions forbade — and drives a four-arm × two-shape × two-retention cross product (`D-39`, **`autonomous: false`**). `31-40` turns the third `missing:` bullet into a derived cross-file census of every dial-argument and ledger-argument divergence with a closed disposition vocabulary, deliberately WITHOUT the sibling order axis's tail-delegation exclusion because that exclusion is exactly where CR-27 lived (`D-40`). `31-41` re-states every published residual and Workflow 18's ledger paragraph per route and per branch (`D-41`). `31-42` derives the framework-surface walk's swallow-site set from the walk instead of from the hand-authored record behind its derived-looking arms constant, and builds the ban-reachability census over the module's whole published ban-set family (`D-42`). `31-43` makes the platform-shapes note-position control observe its own effect and derives its printed label from the outcome (`D-43`). `31-44` is the closing measurement, whose record will be `docs/audit/31-round8-residuals.md`. Sequenced 39 → 44 because every fix plan appends its own dated decision to `31-CONTEXT.md`, three share `scripts/context-io.ts`, and two share the watched prose corpus and its disposition directory. **The phase stays `In Progress` and UATX-01…UATX-06 stay `[ ]` / Gaps Found: only a verification round may flip a requirement, and this phase has had eight rounds in which the executing round believed it had closed one.** Previously: **7 round-7 gap-closure plans (`31-32`…`31-38`, gap waves 20-26) PLANNED 2026-09-11 and all seven EXECUTED 2026-09-11** after the round-7 verification returned `gaps_found` 4/6 for the FOURTH consecutive round and the SEVENTH consecutive round in which UATX-01 and UATX-06 are the two blocked requirements, with four NEW Criticals (CR-22…CR-25) that round 6's own fixes created or preserved — every one independently reproduced by the verifier at HEAD — plus, new in this phase's history, a RECORDED REGRESSION: the excluded-e2e suite measured RED at the round base, on a commit that changed zero source bytes, because a coverage test's oracle was a planning artifact this project rewrites every round. Round 7 leads with that harness repair (`31-32`, `D-33`, **`autonomous: false`**) because every later plan verifies against the same file, then closes the write-path split at the function ENTRY rather than at one arm (`31-33`, CR-22 + CR-24, `D-34`), the identity cutover's terminal `foreign` short-circuit (`31-34`, CR-23, `D-35`, **`autonomous: false`** — a `checkpoint:decision` gates the disposition), the bounded surface walk's silent fail-OPEN (`31-35`, CR-25, `D-36`), the two harnesses that cannot observe their own property (`31-36`, WR-31 + WR-32 + IN-18, `D-37`), the unbound wrapper/reader pair and two audit records stating facts false of the tree (`31-37`, WR-36 + WR-33 + WR-34, `D-38`), and `31-38`, the closing measurement, whose record will be `docs/audit/31-round7-residuals.md`. The round is sequenced 32 → 38 because every fix plan appends its own dated decision to `31-CONTEXT.md` and three plans share `scripts/runnable-ref/uat-spec-integrity.ts` while two share `scripts/context-io.ts`. **The phase stays `In Progress` and UATX-01…UATX-06 stay `[ ]` / Gaps Found: only a verification round may flip a requirement.** Previously: **5 round-6 gap-closure plans (`31-27`…`31-31`, gap waves 15-19) PLANNED 2026-09-10; all five EXECUTED 2026-09-10** after the round-6 verification returned `gaps_found` 4/6 for the SIXTH consecutive round with five NEW Criticals (CR-17…CR-21) that round 5's own fixes created, every one independently reproduced by the verifier at HEAD. Round 6 is two STRUCTURAL changes plus a write-path fix plus hygiene, not seventeen point fixes: `31-27` (CR-17 + S1 host-delivered tier-0 governance root + R-31-19-07, `D-29`) makes the PreToolUse wrapper answer in bounded time at the entry `hooks.json` names and re-scopes `TRUSTED_ROOT_RESIDUALS` by host; `31-28` (S2 + CR-18 + CR-21 + WR-26 + WR-29 + WR-30 + IN-15 + RR-07, `D-30`, **`autonomous: false`** — a `checkpoint:decision` gates the one-way cutover) decides the UAT-spec modifier ban by SYMBOL IDENTITY from the target's own `typescript` and deletes five hand-authored predicates; `31-29` (CR-19 + CR-20 + WR-27 + WR-28 + IN-14, `D-31`) makes a bound owned by the side that admits and keys one action's two halves on one variable; `31-30` (D-28's non-unwinding fault at the §14 caller, R-03/R-31-19-03 Windows shapes on the PRE-EXISTING `windows-latest` leg, R-04's installer round-trip, the 35-finding disposition debt, the collided harness-instance ordinal, `D-32`); and `31-31`, the closing measurement, whose record is `docs/audit/31-round6-residuals.md` — all five Criticals, all five Warnings and both Infos CLOSED BY MEASUREMENT at one commit with the findings' own probe spellings, 14 round-5 closures re-driven as controls with 13 unmoved, 70 dispositions against 70 derived source items, and the no-silent-drop equality asserted as COVERAGE (8 rows raised, 8 covered, 0 uncovered). The round is sequenced 27 → 31 because every fix plan appends its own dated decision to `31-CONTEXT.md` and `31-27`/`31-29` both edit `scripts/context-io.ts`. **The phase stays `In Progress` and UATX-01…UATX-06 stay `[ ]` / Gaps Found: only a verification round may flip a requirement.** Previously: **6 round-5 gap-closure plans (`31-21`…`31-26`, gap wave 14) PLANNED 2026-09-09 and EXECUTED 2026-09-09/10** after the round-5 verification returned `gaps_found` 4/6 with five NEW Criticals the round-4 fixes themselves created: `31-21` (CR-12/WR-22, `D-24`), `31-22` (CR-16/WR-25, `D-25`), `31-23` (CR-13, `D-26`), `31-24` (CR-14/WR-23/WR-24/IN-12, `D-27`), `31-25` (CR-15, `D-28`) and `31-26`, the closing measurement, whose record is `docs/audit/31-round5-residuals.md` — all five Criticals, four Warnings and two Infos independently re-reproduced CLOSED at one commit, 24 dispositions against 24 source items, IN-13's two `UNKNOWN - verify` items resolved by name. **The phase stays `In Progress`: executing a gap-closure round is not verifying it, and a sixth verification round has not run.** The round is sequenced 21 → 26 because every plan appends its own dated decision to `31-CONTEXT.md`. Of the first 20: 15 executed (the original 4 across waves 1, 1, 2, 3; 4 gap-closure plans from the 2026-09-07 verification across waves 1, 2, 2, 3; 4 round-2 gap-closure plans across gap waves 4 and 5; and 3 round-3 gap-closure plans across gap waves 6, 7, 8), plus **5 round-4 gap-closure plans (`31-16`…`31-20`, gap waves 9-13) planned AND executed 2026-09-09** after the round-4 verification returned `gaps_found` 4/6 — `31-16` (CR-09/CR-10/IN-10, `D-20`), `31-17` (WR-19/WR-20, `D-21`), `31-18` (CR-11/WR-17/WR-18, `D-22`), `31-19` (WR-21, `D-23`) and `31-20`, the closing measurement, whose record is `docs/audit/31-round4-residuals.md`. **The phase stays `In Progress`: executing a gap-closure round is not verifying it, and a fifth verification round has not run.** **Gap-closure round 2 (`31-09`…`31-12`)** answers the round-2 verification (`gaps_found` 4/6, 2026-09-08) and the gap-closure code review (`31-REVIEW.md`, CR-05 and CR-06). Both remaining blockers are the round-1 defects reappearing one register over: `appendNote` reaches the admission authority for one KIND (CR-05/UATX-01), and the modifier ban is decided by an enumerable literal rather than a rule (CR-06/UATX-06). The round is a STRUCTURAL fix in both places rather than a third iteration — `31-09` deletes the kind axis so the writer expresses no opinion about which notes admission applies to, and `31-11` deletes the enumerable ban set so a new intermediate segment cannot create a new hole. `31-10` and `31-12` derive the axis each fix's remaining hand-authored set sits on and assert its cardinality, which is the check that was one-directional in both cases. UATX-01 and UATX-06 stay `[ ]` / Gaps Found throughout: only a verification round may flip them.

Plans:

**Wave 1**

- [x] 31-01-PLAN.md — evidence-provenance spine: verdict records the gate-run SHA, `artifact-ref` carries sha/gate_run/content_hash, `admit()` refuses a stale SHA (UATX-01, UATX-04)
- [x] 31-02-PLAN.md — the materialized UAT spec-integrity runnable: derived spec set, TypeScript-AST ban set, two loud skips (UATX-05, UATX-06)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 31-03-PLAN.md — the browser-UAT recipe for all five host CLIs and the `@playwright/mcp` pin guard (UATX-02, UATX-03)

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 31-04-PLAN.md — workflow 05 and 06 wiring plus the test proving the attended Chrome lane has no route to a gate stamp (UATX-01, UATX-03, UATX-05)

**Gap closure** *(from `31-VERIFICATION.md`, status `gaps_found`, three blockers — plans carry `gap_closure: true`)*

**Gap wave 1**

- [x] 31-05-PLAN.md — gap 1: route an `artifact-ref` through `admit()` from `appendNote`, the documented sanctioned writer, plus a derived writer-set contract test and the corrected workflow prose (UATX-01, UATX-04)

**Gap wave 2** *(blocked on 31-05)*

- [x] 31-06-PLAN.md — gap 2: dotted-path callee matching so the real Playwright modifier spellings are decided, a type-checked fixture corpus, and the recipe re-quoted from the one exported set (UATX-02, UATX-06)
- [x] 31-07-PLAN.md — gap 3: a concrete-version shape assertion so the `@playwright/mcp` pin authority cannot itself float, watched failing (UATX-02)

**Gap wave 3** *(blocked on gap wave 2)*

- [x] 31-08-PLAN.md — re-run the three verifier spot-checks verbatim, measure the whole tree green at one commit, and assemble the residual register and closure brief (UATX-01, UATX-02, UATX-04, UATX-06)

**Gap closure, round 2** *(from `31-VERIFICATION.md` round 2 `gaps_found` 4/6 and `31-REVIEW.md` CR-05/CR-06 — plans carry `gap_closure: true`)*

**Gap wave 4**

- [x] 31-09-PLAN.md — CR-05: delete the kind axis so `appendNote` consults the admission authority for every note, bound the one private pre-admitted route by a derived caller set, give the governance root one answer, and correct workflows 16/17/18 plus the two summary overclaims (UATX-01, UATX-04)
- [x] 31-11-PLAN.md — CR-06: decide modifier membership by a head-and-tail rule over the resolved dotted path instead of an enumerable literal, add the two planted spellings and a false-positive control as type-checked fixtures, re-quote the rule in the recipe and record D-17 (UATX-02, UATX-06)

**Gap wave 5** *(blocked on gap wave 4)*

- [x] 31-10-PLAN.md — derive the refusal-family axis from `admit()`'s own source and exercise every writer against every family, with the probes proven to discriminate and the converse tested (UATX-01, UATX-04)
- [x] 31-12-PLAN.md — the reverse cross-check WR-13 names: enumerate the declared Playwright modifier surface by the TypeScript checker and partition it totally into refused-by-rule and dispositioned-with-a-reason, cardinality asserted (UATX-02, UATX-06)

**Gap closure, round 3** *(from `31-VERIFICATION.md` round 3 `gaps_found` 5/6 and `31-REVIEW.md` CR-07/CR-08 + WR-14/WR-15/WR-16 + IN-07..09 — plans carry `gap_closure: true`)*

Both round-3 blockers are the round-2 fixes' own defects one register over, which is the third recurrence of this repository's named failure class inside this phase. `31-11`'s head/tail rule is sound over every shape it is ASKED about, but `calleeDottedPath` declines any callee chain containing a call and does not canonicalise an import rename, so five real Playwright evasions pass at exit 0 (CR-07/WR-14, UATX-06). And `31-09`'s correct unconditional admission call means `compactor.promote` now REFUSES a note a human already legitimately disposed at the origin, so Workflow 18's own recovery would silently discard that disposition on every compaction (CR-08). The round fixes both STRUCTURALLY — `31-13` decides the declining shapes and then derives the resolver's own decline set so a fourth shape cannot land silently; `31-14` routes a re-binding through a proof over origin bytes rather than a parameter an agent can set, and derives both the caller axis and the proof's own decline axis. `31-15` closes WR-15, the same CR-08 defect in the opposite direction on the other four host CLIs, plus WR-16 and IN-08. UATX-01 and UATX-06 stay `[ ]` / Gaps Found throughout: only a verification round may flip them.

**Gap wave 6**

- [x] 31-13-PLAN.md — CR-07/WR-14: decide the call-link and configured-soft shapes instead of declining them, canonicalise an import rename before the head is read, derive the resolver's own decline set and bind every member to a decision or a named residual, record D-18 (UATX-05, UATX-06)

**Gap wave 7** *(blocked on gap wave 6 — both plans record a dated decision in `31-CONTEXT.md`)*

- [x] 31-14-PLAN.md — CR-08/IN-07: give promotion a pre-admitted re-write route through a byte-equality proof over the origin note, extend the derived caller and writer axes plus a derived decline axis for the new route, drive the human-disposed promotion under every dial, and rewrite Workflow 18 steps 4-6 (UATX-01, UATX-04)

**Gap wave 8** *(blocked on gap wave 7 — shares `scripts/context-io.ts`)*

- [x] 31-15-PLAN.md — WR-15/WR-16/IN-08/IN-09: give the trusted governance root a documented resolution order so the target repository's dial is read on all five hosts (proven monotone, bounded at the repository marker), correct the Tier-1 oracle's docstring and confine its writes, and record the round's full review-dispositions ledger (UATX-01, UATX-02, UATX-03)

**Gap closure, round 4** *(from `31-VERIFICATION.md` round 4 `gaps_found` 4/6 and `31-REVIEW.md` CR-09/CR-09b/CR-10/CR-11 + WR-17..WR-21 + IN-10/IN-11 — plans carry `gap_closure: true`)*

The score fell 5/6 → 4/6. Every round-3 closure re-verified INTACT; the six new defects are each one register past what the round-3 fixes decided, and CR-11 is a defect `31-14`'s own fix CREATED. Two axes failed. On the modifier ban, D-18's `()` call-link marker was reasoned about for ONE of the rule's three arms — the two WHOLE-PATH arms compare the marker-carrying path as a literal, so a chained `expect.configure(...).soft` / `.configure({soft:true})` walks past both, and `testInfo.skip()` through the second callback parameter (Playwright's PRIMARY spelling of the modifiers D-18 was convened to decide) was dispositioned only in a test-file comment whose stated reason D-18 itself had disproved one shape earlier. On the admission mechanism, `promoteAdmitted` writes to a CALLER-CHOSEN id and never reads the destination, so an already-admitted note in the shared verified context is silently destroyed and replaced by a forged human-stamped claim — with its "proof" read from a caller-named directory and the governance dial consulted for readability but never for its VALUE. The round fixes both STRUCTURALLY and derives the axis nobody drove: `31-16` puts marker-awareness in ONE authority that a DERIVED, cardinality-asserted set of arms consumes; `31-18` enforces the append-only invariant at the WRITE CHOKEPOINT so the class closes for every writer, and exercises every derived writer against a PRE-EXISTING destination — the probe shape all four prior rounds omitted. UATX-01, UATX-05 and UATX-06 stay `[ ]` / Gaps Found throughout: only a verification round may flip them.

**Gap wave 9**

- [x] 31-16-PLAN.md — CR-09 (both variants), CR-10, IN-10: one routing-marker authority every path-comparing arm consumes (derived and cardinality-asserted), the enabled-option axis folded across the whole chain rather than the outer link, the TestInfo fixture-parameter binding decided from the parse, chained-and-invoked corpus fixtures, and D-20 (UATX-06)

**Gap wave 10** *(blocked on gap wave 9 — shares `scripts/runnable-ref/uat-spec-integrity.ts` and `31-CONTEXT.md`)*

- [x] 31-17-PLAN.md — WR-19/WR-20: one shared step budget through the recursion so a pathological chain yields no path and still reaches the vacuity and denominator floors, a scope rule in the one canonicaliser covering BOTH maps with a false-positive control fixture, the residual register and recipe re-quoted, and D-21 (UATX-05, UATX-06)

**Gap wave 11** *(blocked on gap wave 10 — shares `31-CONTEXT.md`)*

- [x] 31-18-PLAN.md — CR-11/WR-17/WR-18: enforce the append-only invariant at the write chokepoint plus a named `destination-id-occupied` decline, derive the destination-liveness axis and drive every writer against a pre-existing destination, constrain the proof's origin operand to a location the module trusts, consult the dial's VALUE through `isGatedNote` under a derived dial set, decide the ledger premise, and D-22 (UATX-01, UATX-04)

**Gap wave 12** *(blocked on gap wave 11 — shares `scripts/context-io.ts`)*

- [x] 31-19-PLAN.md — WR-21: halt the governance-root walk at the user's home directory and stop depending on one VCS marker, publish the complete stop set and bind Workflow 16's prose to it in both directions, decide the below-root inner-configuration case, prove monotonicity and re-measure WR-15's own closure intact, and D-23 (UATX-01, UATX-03)

**Gap wave 13** *(blocked on gap wave 12 — measures the finished tree)*

- [x] 31-20-PLAN.md — closure: re-run every round-4 reproduction with its own spelling at one commit, re-state all eleven behavioral spot-checks with both results, re-measure every frozen floor, and write the round's residual register plus a disposition row for every CR/WR/IN (UATX-01..06)

**Gap closure round 5** *(from `31-VERIFICATION.md` round 5, status `gaps_found` 4/6, 2026-09-09, and the round-4 code review `31-REVIEW.md` — five new Criticals, four Warnings and two Infos, every one of them a defect a round-4 fix itself created or preserved. Plans carry `gap_closure: true`. Sequenced 21 → 26 because every plan appends its own dated decision to `31-CONTEXT.md`, which is a genuine `files_modified` overlap across the whole round.)*

**Gap wave 14**

- [x] 31-21-PLAN.md — CR-12/WR-22: extract the module's ONE non-blocking regular-file reader from `readGovernanceConfigCandidate` and route the note-write chokepoint and the ledger look through it so a planted FIFO is a named refusal in bounded time instead of a silent hang, append the GOV-02 event BEFORE the note write with a fail-closed ledger read, derive the unguarded-read and write-ORDER axes, and D-24 (UATX-01)
- [x] 31-22-PLAN.md — CR-16/WR-25: delete `originIsTrusted`'s root-proximity arm so a trusted origin has ONE canonical shape and a caller-authored in-repository directory is refused by name, move the operand clause above the dial clause and derive the decline ORDER with a transposed watched-fail mirror, correct Workflow 18's two agent-facing sentences, and D-25 (UATX-01)
- [x] 31-23-PLAN.md — CR-13: split the home boundary into `isAboveHome`/`isHomeItself` so the bound bounds ASCENT and never INSPECTION, adopt the home directory only as a repository (marker AND configuration) so WR-21's `~/.grugops` hole stays closed, publish the two new stop conditions with Workflow 16 bound to them, add `R-31-19-05`, and D-26 (UATX-01)
- [x] 31-24-PLAN.md — CR-14/WR-23/WR-24/IN-12: replace the file-scoped declared-names census with one lexical-containment authority both canonicalisations ask with the reference position, narrow the fixture-binding exemption to the callback the map reads, give `shadowed-rename.uat.spec.ts` a `MUTATE-REMOVE` region so it can fail in both directions, compute the routing normaliser once per arm, correct the inverted direction claim, and D-27 (UATX-06)
- [x] 31-25-PLAN.md — CR-15: put the parse, the diagnostics read and the walk inside ONE per-file could-not-run boundary and `main`'s body inside ONE process boundary returning 2, de-recurse `deriveSpecPaths`'s directory walk, assert the `{0,1,2}` partition AND a measurement line over a run-time pathological corpus, correct the recipe's absolute claim, and D-28 (UATX-06)
- [x] 31-26-PLAN.md — closure: re-run all sixteen round-5 behavioral spot-checks with both results at one commit using the FINDINGS' own probe spellings, disposition every CR/WR/IN, resolve IN-13's two load-bearing `UNKNOWN - verify` items by name, re-measure the whole frozen manifest, and write `docs/audit/31-round5-residuals.md` (UATX-01..06)

**Gap waves 15-19** *(round 6, from `31-REVIEW.md` CR-17…CR-21 and the round-6 verification — plans carry `gap_closure: true`)*

- [x] 31-27-PLAN.md — CR-17/S1/R-31-19-07: carry the non-blocking regular-file rule to `hooks/hook-entry.ts`, the entry `hooks.json` names, DELETE the wrapper's fd-0 read in favour of fd-0 inheritance into the bounded child, bind the restated reader to the module's own by a derived two-member axis and one shared corpus, add the host-delivered tier 0 with `TRUSTED_ROOT_TIERS`, canonicalise both sides of the module-own comparison, re-scope `TRUSTED_ROOT_RESIDUALS` by host, and D-29 (UATX-01)
- [x] 31-28-PLAN.md
- [x] 31-29-PLAN.md
- [x] 31-30-PLAN.md
- [x] 31-31-PLAN.md

**Gap waves 20-26** *(round 7, from `31-REVIEW.md` CR-22…CR-25 + the Skipped-entries finding + WR-31…WR-36 + IN-16…IN-18, and the round-7 verification's `gaps_found` 4/6 with its RECORDED REGRESSION — the excluded-e2e suite measured RED at the round base for the first time in this phase's seven verification rounds. Plans carry `gap_closure: true`. Sequenced 32 → 38 because every fix plan appends its own dated decision to `31-CONTEXT.md`, `31-33`/`31-37` both edit `scripts/context-io.ts`, and `31-32`/`31-34`/`31-35` all edit `scripts/runnable-ref/uat-spec-integrity.ts` — genuine `files_modified` overlap across the whole round.)*

- [x] 31-32-PLAN.md — the suite-RED regression + WR-35 + IN-16 + IN-17. **EXECUTED 2026-09-11 under `remove-axis`, not under the plan's recommended `freeze-manifest`:** the `checkpoint:decision` was answered by a named human, who chose to DELETE the review-coverage axis rather than re-anchor it to a committed manifest, so `docs/audit/31-review-corpus-manifest.md` was never created and `D-33 (1)` records the removal as a DISCLOSED GATE LOWERING with the once-per-round obligation handed to `31-38`. The row floor the removal would have dropped from 16 to 6 is kept as a measured monotone floor; `SKIPPED_DIRECTORIES`'s per-directory hit counts are disclosed on stderr so a host's narrowed denominator is legible; a dead export and a spliced clause are deleted; the excluded-e2e suite is GREEN at the closing commit (`Test Files 64 passed (64)`, `Tests 4137 passed | 2 skipped (4139)`). (UATX-06 — the requirement stays `[ ]` / Gaps Found)
- [x] 31-33-PLAN.md — CR-22 + CR-24 + the Skipped-entries finding: derive the owning repository at the ENTRY of every route that writes a note and its GOV-02 ledger event (above `promoteAdmitted`'s human-stamp fall-through, and in `admitAndAppend`), assert the property over an `appendAuditLedger` call-site set derived from the module's own AST, name the skip arm from the `ReadPositionRefusal` discriminant with the arm set bound to the authority, rewrite the distinctness case as a full pairwise cross product, correct Workflow 18, and `D-34` (UATX-01, UATX-04)
- [x] 31-34-PLAN.md — CR-23: make `foreign` non-terminal for a ban the framework does not own — or delete the retained members in the open — drive `describe.skip` and `expect.soft` through a DECLARED foreign module as corpus rows with WR-26's local-helper control in the same run, re-take `RR-10`, re-quote the recipe, and `D-35` (**`autonomous: false`** — a `checkpoint:decision` gates the one-way disposition) (UATX-06)
- [x] 31-35-PLAN.md — CR-25: make a reached `SURFACE_DEPTH_BOUND`/`SURFACE_NODE_BOUND` report `truncated` and route to the existing could-not-run boundary at exit 2 instead of a silent accept, publish both bounds in `UNRESOLVABLE_CALLEE_RESIDUALS` and the recipe's boundary list, drive the boundary pair as corpus rows, reconcile the two disagreeing bisected numbers to one with its construction written down, and `D-36` (UATX-05, UATX-06)
- [x] 31-36-PLAN.md — WR-31 + IN-18 + WR-32: make every `check-platform-shapes` CONTROL assert its ordinary verdict positively with the outcome made reachable and its red path driven, stop the forced-absent seam constructing what it discards, recognise the non-blocking refusal semantically over an enumerated spelling set whose mirrors are generated from that same list, union the tracked and walked candidate lists, and `D-37` (UATX-01)
- [x] 31-37-PLAN.md — WR-36 + WR-33 + WR-34: carry the marker and not-the-kit-root conditions to `hooks/hook-entry.ts`, correct its false stated reason, bind the wrapper's accept set as a SUBSET of the reader's over one shared corpus, state tier 0's value provenance in `TRUSTED_ROOT_TIERS` and Workflow 16, correct §6.1's three-deleted-functions premise and re-take the disposition resting on it, correct `harness-false-result-instances.md` row 13, add a derived case asserting every row's claimed mechanism is greppable, and `D-38` (UATX-01, UATX-02, UATX-03)
- [x] 31-38-PLAN.md — closure: re-run every round-7 reproduction with the FINDING documents' own probe spellings at one commit with both results, re-drive round 6's five closures as controls including the four the round-7 verifier accepted rather than measured, disposition all fifteen items plus the regression, measure the whole suite GREEN with `npx vitest run --exclude '**/scripts/e2e/**'` and record the round-base RED reading beside it, re-measure the frozen floors and the whole `DECIDER_MANIFEST`, assert the no-silent-drop coverage equality with BOTH sides derived, and write `docs/audit/31-round7-residuals.md` (UATX-01…06)

**Gap waves 27-32** *(round 9, from `31-REVIEW.md` CR-26 + CR-27 + WR-37…WR-42 + IN-19…IN-21, and the EIGHTH verification's `gaps_found` 5/6 with its three `missing:` bullets. Plans carry `gap_closure: true`. Sequenced 39 → 44 because every fix plan appends its own dated decision to `31-CONTEXT.md`, `31-39`/`31-40`/`31-41` all edit `scripts/context-io.ts`, and `31-41`/`31-42` both write into the watched prose corpus and its disposition directory — genuine `files_modified` overlap across the whole round.)*

Round 8 delivered the FIRST closure of UATX-06's predicate family in eight rounds, and produced the EIGHTH consecutive instance of this phase's recorded generator on UATX-01: round 7's own fix for CR-22 created CR-26 (`admitAndAppend`'s gated branch falls open to the caller's dial root for an ungoverned store, where the sibling route declines by name) and CR-27 (`promoteAdmitted`'s fall-through routes the governance DIAL, not just the ledger record, through the caller-supplied destination — reproduced admitting a note the caller's own unreadable trusted configuration refuses). This round does not fix two coordinates. It removes the freedom that produced them: ONE authority answers which repository owns an action, it answers with a discriminated result that has no null member, the DIAL root and the LEDGER address become two parameters (paying the `admit()` unfreeze `R-31-33-01` already named as the cost), and the divergence set becomes a derived, counted, dispositioned census rather than a set a reviewer walks. **The phase stays `In Progress` and UATX-01…UATX-06 stay `[ ]` / Gaps Found: only a verification round may flip a requirement, and this phase has had eight rounds in which the executing round believed it had closed one.**

- [x] 31-39-PLAN.md — CR-26 + CR-27: the `actionOwnerRoot` authority returning a discriminated owner with no null member, a ledger-owner parameter on `appendNote`/`admit`/`admitAndAppend` distinct from the dial root, the deliberate `admit()` unfreeze with `R-31-39-01` and a re-baselined `ADMIT_FROZEN_SHA256`, the fall-through's dial restored to the caller's trusted root, both write-both routes given one shared decline clause, and a four-arm × two-shape × two-retention cross product driven with the legitimate input beside the attack; `D-39` (**`autonomous: false`** — a `checkpoint:decision` gates the CR-26 disposition and the unfreeze that `31-33`'s own prohibitions forbade) (UATX-01, UATX-04)
- [x] 31-40-PLAN.md — the third `missing:` bullet + IN-21: enumerate from source across files every call site that supplies a write path's dial or ledger argument from something other than its own derived answer, assert the cardinality, require a disposition from a closed three-member vocabulary in an exported register bound both ways, ban the fallback shape over the derived authority call-site set, keep the sibling order axis's tail-delegation exclusion deliberately ABSENT (it is where CR-27 lived), replace the ledger test's by-construction partition with two floors measured on both sides, disposition the two carried harness false results, and `D-40` (UATX-01, UATX-04)
- [x] 31-41-PLAN.md — WR-39 + WR-40 + WR-42 + the `artifacts:` row: split `R-31-33-01` into the half the unfreeze closed and the half that remains with its own reproduction, re-state `R-31-33-02` against this tree per branch, publish the widened refusal with a MEASURED shared-install answer rather than an `UNKNOWN - verify`, rewrite Workflow 18's ledger paragraph per route and per branch with every residual named inline and its locator scope asserted by a decoy mirror, write the disposition rows, and `D-41` (UATX-01, UATX-04)
- [x] 31-42-PLAN.md — WR-37 + WR-38 + IN-19: DERIVE the framework-surface walk's swallow-site set from the walk's own syntax tree rather than from the hand-authored record behind the derived-looking arms constant, give every derived site an arm or a published residual carrying its measured throw count, drive each with the stub checker whose absence was the residual's only defence, build the reachability census's expected side from the module's published ban-set FAMILY so all four sets and a future fifth are covered, partition the skipped-directory names by whether a skip could hide this repository's own evidence, take a REAL host reading behind the recipe's control claim, and `D-42` (UATX-02, UATX-05, UATX-06)
- [x] 31-43-PLAN.md — WR-41 + IN-20: make the platform-shapes note-position control observe its own effect (the ordinary staging plants the bytes the writer would write, so the drive passes through the identical-bytes no-op branch and the disk is never inspected), derive the printed outcome label from the outcome the driver reported over a closed vocabulary with an asserted cardinality, watch both fail against a driver that reports success without writing and a driver that crashes without printing, record the CHANGED LABEL SEMANTICS for a round comparing transcripts across this commit, and `D-43` (UATX-01, UATX-04)
- [x] 31-44-PLAN.md — closure: re-run every round-8 reproduction and all three `missing:` bullets with the FINDING documents' own probe spellings at one commit with both results and all-root counts, re-drive CR-17…CR-25 as controls including the four round 8 confirmed by source-read rather than by driving, disposition every finding, anti-pattern row, `missing:` bullet and owner-bearing deferred item with a coverage equality measured on both sides and fenced blocks stripped before the item scan, re-measure the whole suite and every frozen floor including the admission freeze's new and prior values, write `docs/audit/31-round8-residuals.md`, and prove the environment clean with predicates demonstrated able to observe a change (UATX-01…06)

**Research flag:** plan with `--research-phase`. Whether `mcp__claude-in-chrome__*` tools are reachable from inside a subagent is explicitly `UNKNOWN - verify`; verify before designing any flow that assumes it. The phase's core recommendation (Playwright as the floor) does not depend on the answer.

### Phase 32: Board Projector & CLI Dashboard

**Goal**: The operator watches the factory live from one read-only terminal view, fed by a single board-grammar authority whose typed snapshot a future web renderer could consume unchanged.
**Depends on**: Phase 30 (the autonomy banner names every non-default checkpoint) and Phase 28 (stabilized state surfaces)
**Requirements**: DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07, DASH-08
**Success Criteria** (what must be TRUE):

  1. `scripts/board-model.ts` is the **only** board grammar in the tree — the column-heading parser is extracted from and **deleted in** `validate-agent-factory.ts` with the WR-03 prefix-match hardening ported verbatim — and the ticket-row and WIP-number grammars are pinned by a written spec plus a parse-oracle fuzz suite whose adversarial corpus includes the board's own large HTML-comment documentation block. (DASH-01, DASH-02)
  2. One typed `FactorySnapshot` joins board, ticket frontmatter, queue state, context notes, and traceability, and **surfaces** board-vs-frontmatter disagreement in a `conflicts[]` field rather than silently resolving it. (DASH-03)
  3. The dashboard follows a live run using directory-level `fs.watch` plus a **mandatory** polling floor and debounce (because grugops's own atomic-rename write path silently orphans a file-level watch), and never renders a torn read, a partial parse, or an ENOENT as an empty board — a stale snapshot shows a visible stale badge over the last good read. (DASH-04, DASH-05)
  4. The dashboard **cannot** write: an import-graph guard proves its module tree holds no mutating `node:fs` symbol, so read-only is mechanically enforced rather than asserted in prose. (DASH-06)
  5. `--json`, `--once`, and non-TTY modes work for CI and piping; the renderer degrades **visibly** rather than showing a confident wrong board; the snapshot shape is stable enough for a future web renderer to consume unchanged; and the dashboard adds **zero** runtime dependencies and opens no listening socket this milestone. (DASH-07, DASH-08)

**Plans**: 8/8 original plans executed (planned 2026-09-13) + 6 gap-closure plans (`32-09`..`32-14`, planned 2026-09-14, round 1 of a 4-round cap) + 9 gap-closure plans (`32-15`..`32-23`, planned 2026-09-15, round 2 of that cap) + 7 gap-closure plans (`32-31`..`32-37`, round 3 of that cap, re-cut 2026-09-16 after the retired `32-24`..`32-30` cut produced its measured tracer baseline and was retired at commit `7533336c`) + 4 gap-closure plans (`32-38`..`32-41`, planned 2026-09-16, **round 4 of that cap — the last round the rule allows**, ending at a human close decision rather than a fifth round). Tracer-first: plan `32-01` wires one path through every layer (contract → pure grammar → read seam → `--once --json`) and is verified end-to-end before any expansion task; the remaining seven expand out from that proven slice across four waves. Verification on 2026-09-14 returned `gaps_found` (2/5 truths) — the gap-closure plans below close the four failed truths plus the CR-04 containment blocker and the CR-05 stderr warning, strictly sequentially because every one of them changes or analyses the same compiled dashboard closure.

Plans:
**Wave 1**

- [x] 32-01-PLAN.md — TRACER: `agent-factory/contracts/board.md` (headings + rows), the pure `board-model.ts` grammar, the `board-read.ts` seam, and `board-dashboard.ts --once --json` proven end-to-end against the real kit board [DASH-01, DASH-02, DASH-03, DASH-07, DASH-08]

**Wave 2** *(blocked on 32-01)*

- [x] 32-02-PLAN.md — the total line partition (`updates`, `preamble`, `nonColumnSections`, `unparsed`), bounds and truncation for a 380 KB board, and the completed normative contract incl. the old spec's dashboards non-goal reconciliation [DASH-01, DASH-02, DASH-08]
- [x] 32-03-PLAN.md — read-verify-reread with per-source staleness and a last-good carry-forward, the four remaining join sources with the queue tamper rule ported verbatim, and the directory watch + debounce + mandatory poll floor + driven re-arm [DASH-03, DASH-04, DASH-05]

**Wave 3** *(32-04 blocked on 32-02; 32-05 on 32-02+32-03; 32-06 on 32-01+32-03)*

- [x] 32-04-PLAN.md — the corpus as data (141 live rows admitted by named disposition + a mutation half refused), the two real boards trimmed into `scripts/fixtures/board-replay/`, the no-op-stripper discrimination with a recorded RED baseline, and the seven-axis parse oracle [DASH-02]
- [x] 32-05-PLAN.md — `joinSnapshot` and the closed seven-kind `conflicts[]` set, the `scripts/fixtures/board-snapshot/` tree manufacturing all seven kinds, and the byte-for-byte `schemaVersion: 1` golden [DASH-03, DASH-08]
- [x] 32-06-PLAN.md — the DASH-06 import-graph guard: AST-derived closure and symbol sets with premise assertions, the module ban, two planted mirrors proving it can fail, and `check:dashboard-readonly` as one authority [DASH-06, DASH-08]

**Wave 4** *(32-07 blocked on 32-03+32-05; 32-08 on 32-02+32-05)*

- [x] 32-07-PLAN.md — the full D-17 frame with the stale badge and the `claimed / counted / limit` disagreement, ANSI sanitization of every cell, the TTY and non-TTY branches, and the `--json`/`--once`/NDJSON exit contract measured from a spawned process [DASH-05, DASH-07]
- [x] 32-08-PLAN.md — the validator's inline column parser deleted and imported from `board-model.js` against an eight-fixture RED/GREEN baseline, both board twins' comment rewritten with their column table frozen, and the phase-close gate sweep with both absences asserted [DASH-01, DASH-08]

**Gap closure — round 1** *(strictly sequential: waves 1-6, one plan per wave, because all six touch or analyse the compiled `scripts/board-dashboard.js` closure)*

- [x] 32-09-PLAN.md — CR-02 + CR-03: a three-armed directory listing whose EACCES/ENOTDIR/EMFILE failures reach the stale badge and `readErrors`, `joinSnapshot` gated against deriving presence-dependent conflicts from a non-`ok` tickets source, and a tear detector that compares raw bytes and reports a decode failure as `unreadable` [DASH-03, DASH-04, DASH-05]
  - *Scope moved:* an adversarial re-run found the same defect one register down, so a `partial` `SourceOutcome` arm was added — a per-ENTRY read failure now degrades its whole source. `scripts/board-dashboard.ts` joined the changed set for the header badge.
- [x] 32-10-PLAN.md — CR-04 (security blocker): one `realpathSync`-based containment authority called by both path constructors, per-entry refusals surfaced as path-only `readErrors`, a per-source guard so one refusal never blanks the other five, and a six-case legitimate-input battery [DASH-03, DASH-05]
- [x] 32-12-PLAN.md — CR-06: three disagreement fixtures with a recorded before/after table, the validator's second ticket-frontmatter reader deleted and `checkTickets()` routed through `parseTicketDocument`, and a glob-derived single-authority census plus the corrected docblock [DASH-01, DASH-02]
- [x] 32-13-PLAN.md — CR-05 + the NDJSON documentation defect: one sanitizing stderr chokepoint with an AST-derived site census, and the three "exactly one JSON document" sentences corrected and anchored to measured line counts [DASH-07, DASH-08]
- [x] 32-11-PLAN.md — CR-01: the DASH-06 guard's canonical form (an fs namespace binding may only be the object of a member access), fourteen escape spellings enumerated as data with a two-sided pin, and the guard bound to `check:build-parity`. Runs after the other fixes so it is proved against the closure that ships [DASH-06, DASH-08]
  - *Scope moved:* probing after the planned tasks were green found two more live bypasses of the same class, both closed in the same plan — the acquisition arm generalised to any call carrying an fs identity as a string-literal argument, and a refusal of namespace RE-ENTRY members (`fs.promises`, `fs.default`) over a runtime-derived set.
- [x] 32-14-PLAN.md — adversarial self-review: every verifier transcript re-run against the rebuilt `.js`, a written answer per gate to how it is REACHED, a reachability table over every new refusal branch, and a `package.json`-derived gate sweep [DASH-01..DASH-08]
  - *Scope moved:* the neighbour-variation probe found a live bypass of the same class 32-11 closed — the fs re-entry rule was asked at two of the four positions a member name enters the symbol set — so `scripts/board-readonly.test.ts` joined the changed set (finding F-02, closed and mutation-proven). Two findings are left OPEN with reproductions: F-03 (the guard is green over a writer acquired through a runtime-assembled module identity — a residual 32-11 named and this round measured) and F-01 (the `check:*` CI-reachability derivation cannot see `check:dashboard-readonly`). Full report in `32-14-ADVERSARIAL-REVIEW.md`. **This plan does not decide phase completion; the verifier does.**

**Gap closure — round 2** *(planned 2026-09-15, round 2 of the 4-round cap; strictly sequential waves 1-9, one plan per wave, because every runtime plan edits `scripts/board-read.ts` / `scripts/board-model.ts` / `scripts/board-dashboard.ts` and the DASH-06 guard is proved against the closure that ships)*

Round-1 re-verification on 2026-09-15 returned `gaps_found` (3/5 truths, up from 2/5): two failed truths, one partial truth, one advisory, eleven warnings, three info items, one open reachability finding and one human-verification item. Every one of those items is taken by a plan below.

- [x] 32-15-PLAN.md — G1 / CR-01: the tickets walk becomes a TOTAL partition (every listed `.md` entry is an admitted record or an unadmitted entry, size asserted against a listing-derived count), the unadmitted set reaches the join, and `row-without-file` answers per identifier with an honest sentence instead of asserting absence about a file on disk [DASH-03, DASH-04, DASH-05]
  - *Scope moved:* `readTicketsSource` was exported so the partition's size could be measured rather than asserted, and the plan's own refusal table was widened from six rows to reach all six refusal codes. The plan's stated RED contradicted its own must-have, so the RED→GREEN pair was honored across two commits and said so.
- [x] 32-16-PLAN.md — WR-03 + WR-04 + WR-11: one `normalizeDocument` authority for both grammars (line endings plus a single leading byte-order mark), a tab refused by the key pattern with a true reason and a derived control-class pin, and the builder specification's delimiter-less ticket template named as documented non-grammar in both documents [DASH-01, DASH-02]
  - *Scope moved:* narrowing `TICKET_CONTROL` alone would have WIDENED the grammar in two positions the plan did not name, so the key pattern's value capture was narrowed in the same edit; and the claim record's byte-order-mark row turned out to be a forged-claim bypass rather than a display defect.
- [x] 32-17-PLAN.md — WR-09 + WR-08 + IN-02 + IN-03: `boundNames` sorts before bounding so membership stops depending on the filesystem, every claimed-queue directory is a row or a reported skip proved by a count, a duplicated ticket identifier is reported once and joined once from the map every arm reads, and a row title carries no trailing whitespace into the published document [DASH-03, DASH-05, DASH-08]
  - *Scope moved:* a THIRD silent queue skip (`unsafe-task-name`) was found one arm over from the two the review named and closed alongside them, because the reader's totality claim is false while any arm is silent. The `splitRow` cases stayed in `scripts/board-tracer.test.ts` with their existing siblings rather than being split across two files.
- [x] 32-18-PLAN.md — G3 / CR-02 + WR-05: one sanitizing `writeDocument` chokepoint over the serialized stdout document (keys as well as values), the stderr census generalized to a channel-parameterized census pinning stdout two-sided by count and enclosing function, the module header's claim made checkable, and the header's code-unit count rendered in code units [DASH-07, DASH-08]
- [x] 32-19-PLAN.md — WR-06 + WR-07 + IN-01: one current watch record per directory cleared on re-arm, the watched set derived from `FIXED_SUBPATHS` and `QUEUE_STAGES` with the assertion moved to a two-directional relationship, and watches armed against the same resolved root every read is resolved against [DASH-04, DASH-05]
  - *Scope moved:* `OUTSIDE_ROOT` was exported from `scripts/board-read.ts` so the watch loop asks the containment question with the authority's own constant instead of a second copy, and the loop learned to close a handle on a source the reader refused — threading the resolved root alone does not refuse a symlinked source directory.
- [x] 32-20-PLAN.md — G2 / WR-02 + F-03: the DASH-06 guard's three arms closed in ONE edit — module identity inverted to a two-sided allow-list, module acquisition reduced to one admitted canonical shape with unprovable identities refused, and the capability-bearing globals pinned by a two-sided member-path census — with the spellings held as data, a mutation per rule, and each bypass reproduced against the committed `.js`. Runs after every runtime plan so it is proved against the closure that ships [DASH-06, DASH-08]
  - *Scope moved:* three arms were widened beyond the plan during execution — a `new` expression is inspected as well as a call, a non-member-access READ of a capability-bearing global is refused, and a tenth module-identity row was added.
- [x] 32-21-PLAN.md — WR-01 + WR-10 + F-01: the one-authority census asks about the capability rather than the spelling (three measured rewrites as discrimination rows), the routing census's universe widened to function-like nodes and member-access callees and then pinned, and every `check:*` script classified totally with a per-class reachability proof so the DASH-06 control's reachability is mechanical [DASH-01, DASH-02, DASH-06]
  - *Scope moved:* `resolveCallee` was widened to string-literal element access and `super`, lexical scope had to travel into the widened universe, and a class-disjointness case the plan did not ask for was added so a first-matching arm cannot answer for a class whose proof was never asked.
- [x] 32-22-PLAN.md — the `human_verification` item: a bounded live spawned-process suite measuring the event path, the poll-only path and the debounce against a real atomic-rename write on a wall clock, with two recorded runs and the unmeasured platform named [DASH-04, DASH-05]
  - *Scope moved:* the plan's "the poll-delivered document carries a watch-failure record" is not true of the shipped program (the poll tick re-arms before it emits), so both regimes were MEASURED instead and the invisibility pinned by mutation; five spawned processes rather than three, and `requirements-completed` was left deliberately empty.
- [x] 32-23-PLAN.md — adversarial self-review: the harness's premise asserted first, every recorded reproduction from both prior documents re-run against the rebuilt committed `.js`, a reachability answer and a sibling-arm probe per new refusal branch, a `package.json`-derived gate sweep, both residual-ledger representations reconciled, and a round ledger asserted total against the gap inventory. **This plan does not decide phase completion; the verifier does.** [DASH-01..DASH-08]
  - *Measured result:* all 26 prior recorded reproductions re-run against the rebuilt committed `.js`; **25 measured closed**, including round 1's two open findings (F-01 and F-03) and all three verifier gaps. The one still open is the `Array.join` half of the WR-01 advisory — ledger row 184's stated blind spot, now measured rather than asserted. Round 1's open residual rows 179–182 are `fixed` in both ledger representations.
  - *Scope moved:* the neighbour-variation pass raised **five new findings, four of them created by this round's own fixes**, all recorded OPEN with reproductions and none fixed here (this plan owns no source file): **F-04** — the DASH-06 guard is green at 89/89 over a writer imported through an ABSOLUTE-path specifier, which is in neither the allow-list's "bare" set nor the closure walk's "relative" set, measured with a real write and through the whole `.ts`→`.js` chain; **F-08** — recording one member path in `EXPECTED_GLOBAL_MEMBER_PATHS` re-greens that guard over `process.report.writeReport` reached through a binding, in a single edit (F-02's shape one register over); **F-06** — `row-without-file` still asserts absence against a ticket file whose declared `id` is not its stem; **F-05** — the stdout sanitizer runs after `JSON.stringify`, so a raw C0 survives one `JSON.parse`; **F-07** — a `check:*` command running two gate modules proves the first only. Full report in `32-23-ADVERSARIAL-REVIEW.md`. **The phase status and the DASH-0x checkboxes are deliberately unchanged by this plan.**

**Gap closure — round 3** *(first planned 2026-09-15 as `32-24`..`32-30`; **RE-CUT 2026-09-16 as `32-31`..`32-37`** against the measured tracer baseline — round 3 of the 4-round cap; strictly sequential waves 1-7, one plan per wave, because every runtime plan edits a module inside the compiled `scripts/board-dashboard.js` closure and the DASH-06 guard is re-proved on each commit that changes it)*

Round-2 re-verification on 2026-09-15 returned `gaps_found` (2/5 truths, **down** from 3/5): three failed truths, one `reason_advisory` caveat, one advisory, and `32-REVIEW.md`'s 13 findings (3 critical, 8 warning, 2 info) which the gaps, the advisory and the caveat all map onto. Every one of those 13 is taken by a plan below or explicitly dispositioned in one; two items (the pre-existing `check:diff-disposition` failure on five Phase-31 workflow documents, and the unrun live claude-CLI e2e lane) are carried outside the inventory.

**Why the round was re-cut.** The first cut's tracer, `32-24`, executed Task 1 only: it recorded a measured RED baseline (commit `759e982c`, artifact `32-24-RED-baseline.txt`) and stopped at the tracer feedback gate. The baseline partly disproved the cut's premise, so the seven plans were retired (commit `7533336c`) and re-cut. The baseline itself is INPUT to the new plans and is not re-measured. Three measurements moved the shape: (1) **six** specifier spellings, not four — `//localhost/<abs path>` is a live green spelling no review named, and `data:` classifies as bare today; (2) the retired Task 3's `grep -c 'exit 1' >= 4` pin was **count-not-mechanism**, because three of the six rows already exit 1 before any fix, so the discriminating pin is a per-row FLIP over the three green rows with the NAME of the case that reds each; (3) the cutover **RELOCATES** the `file://`, `C:\` and `data:` refusals out of the builtin allow-list into the new `foreign` branch, so the proof must carry a per-spelling before/after refusal-authority column or the tree gets quietly weaker for three spellings while getting stronger for three.

**The round's shape is a canonical-form cutover, not a fourth arm** — the recommendation `32-VERIFICATION.md`, `32-REVIEW.md` and `32-23-ADVERSARIAL-REVIEW.md` independently converged on. Every round-2-created blocker had one shape: a derived predicate whose SUBJECT was left as a syntactic complement. Each plan below replaces one such complement with a total partition or asks an existing canonical form one level down. Tracer-first: plan `32-31` proves ONE cutover end-to-end against the committed `.js` — a measured exit-code flip of the shipped gate command — before the others expand.

Plans:
**Wave 1**

- [x] 32-31-PLAN.md — TRACER · DASH-06 route (a): every module specifier lands in exactly one of bare / relative / foreign through one exported authority both the closure walker and the read-only guard call, the scan's input narrowed to code rather than prose, and the foreign bucket refused by construction — proven by a per-row exit-code FLIP over the three baseline-green spellings (`/abs/…`, `//localhost/…`, `//host/…`) planted into the committed `.js`, each naming the case that reds it, plus a per-spelling refusal-authority before/after column for the three relocated spellings (`file://`, `C:\`, `data:`) [DASH-06, DASH-08]

**Wave 2** *(blocked on 32-31)*

- [x] 32-32-PLAN.md — DASH-06 route (b): a local alias no longer hides a capability path (the binding map), and arm 4's canonical form is asked of a MEMBER read one level down, so the write-detection mechanism reds rather than a census count and the one-edit re-green costs two pinned decisions [DASH-06, DASH-08]

**Wave 3** *(blocked on 32-32)*

- [x] 32-33-PLAN.md — DASH-03 (CR-03/F-06) + WR-08 + IN-02: one `presenceOf` derivation over three measured sets with the fourth population falling out as arithmetic, the honest sentence naming what a file declares, the duplicate check as a map, and the empty-stem entry refused by name. **Not autonomous** — Task 1 is a `checkpoint:decision` on whether the published snapshot shape moves (D-19) [DASH-03, DASH-05, DASH-08]

**Wave 4** *(blocked on 32-33)*

- [x] 32-34-PLAN.md — DASH-04/DASH-05 (CR-01 + CR-02) + IN-01: the containment signal narrowed to the directory a handle would open with an ancestor-aware test, the containment early return given both the effects its neighbours have, the escaping-ENTRY converse cases the round-2 test stopped short of, the two-refusals-in-one-read concurrency case, and the context reader's two silent skips named [DASH-04, DASH-05, DASH-03]

**Wave 5** *(blocked on 32-34)*

- [x] 32-35-PLAN.md — WR-04/F-05 + WR-06 + WR-07: values and keys scrubbed before serialization so a consumer recovers no control character while the line boundary stays this module's, one derived header chokepoint, and a wait deadline computed from the poll period. WR-07's CI-topology half is explicitly carried to Phase 33 in `deferred-items.md` rather than taken [DASH-07, DASH-08, DASH-04]

**Wave 6** *(blocked on 32-35)*

- [x] 32-36-PLAN.md — WR-03/ledger row 184 + WR-05/F-07: the one-authority census asks about the key's presence and the capability of scanning text rather than about two syntactic positions, the carrier count becomes a case on BOTH sides of one, over-detection is paid for in named counted exemptions, and every gate target in every check command gets its own reachability row [DASH-01, DASH-02, DASH-06]

**Wave 7** *(blocked on 32-36)*

- [x] 32-37-PLAN.md — adversarial self-review: the harness's premise asserted and printed first, every recorded reproduction from all four prior documents re-run against the rebuilt committed `.js` (including the three baseline rows already refused before this round, each answered by which authority refuses it NOW), a reachability answer and a sibling-arm answer per new refusal branch, a per-stolen-refusal relocation table, a manifest-derived gate sweep, both residual-ledger representations reconciled, a round ledger asserted total against the 13-item inventory, the probe-item arithmetic balanced (7 authored + 5 flagged = 12), and the created-versus-inherited ratio stated as a number. **This plan does not decide phase completion; the verifier does.** [DASH-01..DASH-08]

**Gap closure — round 4** *(planned 2026-09-16 as `32-38`..`32-41`; **round 4 of the 4-round cap — the last one this phase's own recorded rule allows**; strictly sequential waves 1-4, one plan per wave, because the two source plans both edit `scripts/board-model.ts` and the red-team must run against the tree those plans leave behind)*

Round-3 verification on 2026-09-16 returned `gaps_found` (4/5, up from 2/5) with one DASH-03 gap, two advisories and four anti-pattern rows. **Eight commits then landed on `main` AFTER that verification** (`d276f4e3`, `dba9d72a`, `e52cfb25`, `70cbd447`, `7a3ae592`, `c5163183`, `0249d0c5`, `3e2f254a`, reported in `32-REVIEW-FIX.md`) claiming to close the gap, both advisories, the remaining anti-pattern row and four more warnings — **none of them verified by anybody**. Round 4 treats every one of those as a CLAIM and re-measures it, closes the three Info findings the fix pass declined, red-teams the result, and ends at a human decision rather than a fifth silent round.

- [x] 32-38-PLAN.md — TRACER: the one remaining DASH-03 gap proved closed END TO END on the spawned `--once --json` document (the surface the verifier measured it open on, not the unit boundary), with the cross-field agreement DERIVED from the document rather than matched against two literals; every `presenceOf`/`presenceActual` arm reached by a named input with the branch count parsed from the module; the `joinedStem === undefined` branch either exhibited or replaced by the invariant that forbids it; and `agent-factory/contracts/board.md`'s presence table proved equal to the code's sentences in BOTH directions [DASH-03, DASH-05, DASH-07]
- [x] 32-39-PLAN.md — the three Info findings `32-REVIEW-FIX.md` explicitly declined: IN-01 the false `admitted-under-its-stem` discriminant renamed to `admitted-under-this-id` at a site set derived with a text-forcing search and the published golden proved byte-identical; IN-02 both prototype-losing accumulators (`configView`'s limit map, `scrub`'s serializer accumulator) rebuilt so a prototype-spelled column reaches `column-missing` and a prototype-spelled key reaches a consumer's parse, each with its sibling arm asserted; IN-03 the toolchain register's raw reads made own-property lookups at a derived, counted site set so the two copies of the WR-04 rule stop disagreeing [DASH-03, DASH-06, DASH-07, DASH-08]
- [x] 32-40-PLAN.md — adversarial red-team over the post-fix tree: the harness premise asserted first, every recorded reproduction from all three prior documents re-run (including the CLOSED ones), the four questions asked and answered per fix (what the INPUT is assembled from, at which POSITIONS the predicate is asked, which SET it enumerates, whether its UNION with its siblings still refuses), and the principal cross-fix hypothesis tested — whether the widened `bare` arm turned the specifier scan's recovery-miss fallback from a REFUSAL into a silent SKIP; plus the live write plants re-measured with the write-detection premise case tracked, a findings register with a `created by a round-3 or fix-pass change?` verdict per row, the ratio stated beside rounds 1-3, and a ledger asserted total. **Owns no source; this plan does not decide phase completion.** [DASH-01..DASH-08]
- [x] 32-41-PLAN.md — the manifest-derived gate sweep with the regression lane set beside four rounds, every carried and undecidable item re-measured with its overlap number against this round's changed-file set, both `.planning/WINDOWS.md` residual representations reconciled to a printed symmetric difference of zero, the `overrides:` YAML pre-drafted per still-open must-have, and a **BLOCKING human `checkpoint:decision`** on how Phase 32 closes now that its cap is spent. **Changes no requirement checkbox, no phase status and no `STATE.md` line under any option.** [DASH-01..DASH-08]

**Research flag (discharged):** the board ticket-row grammar is now measured — 141 rows across five sources in `32-RESEARCH.md` §Measured Board Corpus. The measurement contradicted D-01's anchored form (103/141), which the user resolved as D-22's opaque `trailer` (141/141).

**Windows caveat (honest, not a defect):** the `fs.watch` behavior this phase depends on is only *proven* on Windows once Phase 33 turns the `windows-latest` leg green. Until then this phase's Windows claim stays `UNKNOWN - verify` rather than asserted. This is a terminal renderer only — no web or frontend surface; the web renderer is explicitly deferred.

### Phase 32.1: Board Dashboard Deferred Residuals (INSERTED)

**Goal:** Close, by canonical form rather than wider enumeration, the residuals Phase 32 deferred at its four-round cap (human decision Option C, plan 32-41, 2026-09-17): the eight round-4 red-team findings F-14..F-21 in `32-40-ADVERSARIAL-REVIEW.md` and the carried ledger rows that `32-41-SUMMARY.md` / `deferred-items.md` re-homed here (16 items, WINDOWS.md rows 176, 183, 184, 194, 200–210), plus triage of the round-4 code review findings WR-01..WR-04 in `32-REVIEW.md`. Four of the five created findings are one class — a set enumerated over one spelling of the thing it is about — so the remedy is a canonical form, not another arm.
**Requirements**: DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07, DASH-08 (residuals against already-complete requirements; no new IDs)
**Depends on:** Phase 32
**Plans:** 11/11 original plans executed (planned 2026-09-17) + 4 gap-closure plans (`32.1-12`..`32.1-15`, planned 2026-09-18, **round 2 of the standing 4-round cap**). Verification on 2026-09-18 returned `gaps_found` (17/19 ledger items; one FAILED must-have — the refusal-sentence builder does not own every content-derived interpolation, because a composed path carrying a listing entry's name reaches ten `board-read.ts` message sentences unescaped and the renderer deletes the byte). The gap-closure plans close that by canonical form — every published substitution is built through the builder and both classification tables are deleted — plus this phase's own review warnings WR-02/WR-03 and all seven information items, plus a static diagnosis of the three live-lane failures behind one blocking human checkpoint.

**Execution is strictly sequential — one plan per wave (waves 1–15).** Every plan in this phase either rebuilds the whole committed `.js` surface with `npm run build` or runs a `check:*` entry whose first step compiles into the shared `.tmp-build` scratch directory, so two plans running concurrently in one working tree would see each other's uncommitted build output and red `check:build-parity`. This is the same reason Phase 32's gap-closure plans ran strictly sequentially. Plan 01 is the tracer (D-19's instrument-first order); plans 04–09 each cut one rule over to those instruments; plans 02–03 are the audit-row content job; plans 10–11 close the phase.

Plans:
**Wave 1**

- [x] 32.1-01-PLAN.md — **tracer:** the shared symbol-resolving checker helper with its emit exclusion and tracked-file-set floor, Node's own loader oracle, and a recursive importer derivation [DASH-01, DASH-02, DASH-06]

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 32.1-02-PLAN.md — row 176 part one: the disposition rows for the pull-request quality-gate workflow, measured from the gate's own output [DASH-02]

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 32.1-03-PLAN.md — row 176 part two: the remaining four watched documents including the three frozen findings with their companion edits; `check:diff-disposition` reaches exit 0 [DASH-02]

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 32.1-04-PLAN.md — F-15 + WR-04 + rows 194/210: the split-reader join resolves declarations and is asked in both directions, the exemption registry is re-derived, one operator message reworded [DASH-01, DASH-02]

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 32.1-05-PLAN.md — F-19 + F-21 + row 207: the one-authority rule resolves symbols over a derived register set, the build-parity check becomes a gate module, and two declared command shapes with zero exemptions replace the separator alphabet [DASH-02]

**Wave 6** *(blocked on Wave 5 completion)*

- [x] 32.1-06-PLAN.md — F-16, F-17, WR-02, WR-03 + rows 202/203: the regex scanner is retired for a real tokenizer with a lazily acquired parser; carries a blocking one-way decision checkpoint [DASH-06]

**Wave 7** *(blocked on Wave 6 completion)*

- [x] 32.1-07-PLAN.md — F-14 + row 200: one refusal-sentence builder escaping by construction across all three board modules, with a derived ownership census [DASH-03, DASH-07]

**Wave 8** *(blocked on Wave 7 completion)*

- [x] 32.1-08-PLAN.md — F-18 + D-11 + row 204: two independent write-detection predicates each proven to red alone, one register for the dynamic-import question, and the runtime probe's measured boundary [DASH-06, DASH-08]

**Wave 9** *(blocked on Wave 8 completion)*

- [x] 32.1-09-PLAN.md — WR-01 + F-20 + row 206: the contract adopts the code's watch rule with a prose-shaped equality that detects the next drift, and the liveness gate moves to the resolved path [DASH-04, DASH-05]

**Wave 10** *(blocked on Wave 9 completion)*

- [x] 32.1-10-PLAN.md — row 183: the derived gate sweep, then one live end-to-end run behind a blocking human checkpoint naming the token spend [DASH-03, DASH-05, DASH-08]

**Wave 11** *(blocked on Wave 10 completion)*

- [x] 32.1-11-PLAN.md — D-16/D-22 completion: the nineteen-item ledger with a derived count equality, the two waived rows written by the ledger's own verb, and the erratum beside a byte-unchanged original [DASH-01..DASH-08]

**Gap closure — round 2 of 4 (planned 2026-09-18)**

**Wave 12** *(blocked on Wave 11 completion)*

- [x] 32.1-12-PLAN.md — **tracer:** the failed must-have reproduced end to end at HEAD with three planted code points and measured at three channels, the claim-record sentences built through the builder, the derived unowned-published census filed as the expansion's work list, and the gap recorded OPEN in the register with a named owner [DASH-03, DASH-07]

**Wave 13** *(blocked on Wave 12 completion)*

- [ ] 32.1-13-PLAN.md — the canonical form: every published substitution in all three board modules built through the builder, both declaration-classification tables and both carve-out paragraphs deleted, one two-sided published-equals-owned equality with one pin, and a seeded tag removal per module proving it reds [DASH-03, DASH-07, DASH-08]

**Wave 14** *(blocked on Wave 13 completion)*

- [ ] 32.1-14-PLAN.md — WR-02 + WR-03 + IN-01..IN-07: a recovered parse becomes a named refusal carrying its diagnostic count, a declined declaration specifier is recorded into the unreadable seam, the loader oracle's child is bounded so its premise fails red rather than hanging, and every information item reaches a disposition [DASH-06, DASH-08]

**Wave 15** *(blocked on Wave 14 completion)*

- [ ] 32.1-15-PLAN.md — the three live-lane failures diagnosed statically at zero token spend, one blocking human checkpoint naming the spend before at most one gated re-run, and the completion equality extended to a fourth named source with the register reconciled by identifier [DASH-03, DASH-05, DASH-06, DASH-07, DASH-08]

### Phase 33: Live Capture & Windows Portability

**Goal**: The milestone's headline claims are proven by capture rather than by a green suite — one live run shows role agents executing in their own sessions, which is also the evidence the project's oldest open item has waited for since v1.0.
**Depends on**: Phase 27 (spawning must genuinely work before it can be captured) and Phase 32 (the Windows `fs.watch` surface this phase turns green)
**Requirements**: CAP-01, CAP-02, CAP-03
**Success Criteria** (what must be TRUE):

  1. A **captured** live run shows role agents executing in their own sessions — the spawn fix proven by observation, never by the green suite that failed to detect the defect in the first place. (CAP-03)
  2. That capture (date + verdict) discharges GAP-D1: A3/DOG-02 flips together with the coupled `examples/03-ticket-to-pr.md` cleanup, in one edit — and a loud skip is never accepted as the capture. (CAP-01)
  3. The `windows-latest` CI leg exits 0 — path-assertion normalization, symlink-fixture privilege guard, buildable old-layout migrate fixture, temp-dir `tsc` mirror rebuild — which also turns the dashboard's Windows `fs.watch` surface from assumed into proven, and flips the Phase-20 human item on green. (CAP-02)

**Plans**: TBD

## Progress

**Execution order:** 27 → 28 → 29 → 30 → 31 → 32 → 33

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|----------------|--------|-----------|
| 1. Substrate, Config & State Skeleton | v1.0 | 5/5 | Complete | 2026-06-02 |
| 2. Shared Contracts | v1.0 | 4/4 | Complete | 2026-06-03 |
| 3. Roles & AGENTS.md Substrate | v1.0 | 8/8 | Complete | 2026-06-03 |
| 4. Workflows, Cadence & Backpressure | v1.0 | 7/7 | Complete | 2026-06-03 |
| 5. Packaging, Adapters, Install & Distribution | v1.0 | 5/5 | Complete | 2026-06-03 |
| 6. Validation, Brand & Dogfood | v1.0 | 5/5 | Complete | 2026-06-04 |
| 7. Shared-Home Foundation & Path Rewrite | v1.1 | 4/4 | Complete | 2026-06-06 |
| 8. Two-Root Installer | v1.1 | 4/4 | Complete | 2026-06-07 |
| 9. Doctor & Two-Root Validator | v1.1 | 6/6 | Complete | 2026-06-08 |
| 10. SDLC-Coverage Audit & Foundation Guards | v1.2 | 4/4 | Complete | 2026-06-10 |
| 11. Senior Persona Overhaul | v1.2 | 5/5 | Complete | 2026-06-10 |
| 12. BDD + TDD Wiring | v1.2 | 5/5 | Complete | 2026-06-11 |
| 13. Frontend/UI Persona & Design→Build Workflow | v1.2 | 3/3 | Complete | 2026-06-11 |
| 14. Security Audit (OWASP ASVS) & Checklist Re-Anchor | v1.2 | 3/3 | Complete | 2026-06-13 |
| 15. TypeScript Tooling Migration | v1.2 | 6/6 | Complete | 2026-06-13 |
| 16. §14 Gate Convergence — Lint, UI/E2E & Test-Integrity | v1.2 | 3/3 | Complete | 2026-06-14 |
| 17. Install --migrate / --update | v1.2 | 3/3 | Complete | 2026-06-15 |
| 18. Browsable Docs Catalog | v1.2 | 2/2 | Complete | 2026-06-15 |
| 19. Factory Auto-UAT Harness — Tier 1 Oracles + Tier 2 Headless E2E | v1.2 | 4/4 | Closed (A3/DOG-02 waived → next milestone) | 2026-06-16 |
| 20. Shared-Context Substrate & Concurrency Foundation | v2.0 | 4/4 | Complete    | 2026-06-17 |
| 21. Verify-Before-Write Admission | v2.0 | 4/4 | Complete    | 2026-06-17 |
| 22. Memory & Trajectory Compaction | v2.0 | 9/9 | Complete    | 2026-06-19 |
| 23. Parallel Execution & Orchestrator-as-Decomposer | v2.0 | 3/3 | Complete    | 2026-06-21 |
| 24. Clean Handoff Removal & Traceability Migration | v2.0 | 5/5 | Complete    | 2026-06-22 |
| 25. Governance-on-a-Dial | v2.0 | 13/13 | Complete | 2026-06-29 |
| 26. Dogfood, Dual-Path Oracle & A3/DOG-02 Retirement | v2.0 | 6/6 | Complete | 2026-07-24 |
| 27. Spawn Correctness & Kit-Set Authority | v2.1 | 66/66 | Complete    | 2026-08-11 |
| 28. Kit Consistency Audit | v2.1 | 8/8 | Complete    | 2026-08-12 |
| 29. Controlled Language & Voice Guard Rebuild | v2.1 | 60/60 | Complete    | 2026-08-18 |
| 29.1. Per-Role Model Assignment | v2.1 | 25/25 | Closed by user override D-29.1-19 (verification gaps_found; 3 blockers accepted open, WINDOWS.md rows 105-107) | 2026-09-03 |
| 29.2. Model Assignment Delivery Path | v2.1 | 6/6 | Complete    | 2026-09-07 |
| 30. Per-Checkpoint Autonomy Matrix | v2.1 | 11/11 | Complete    | 2026-09-07 |
| 31. Autonomous Manual Testing | v2.1 | 44/44 | In Progress|  |
| 32. Board Projector & CLI Dashboard | v2.1 | 34/34 | Complete    | 2026-09-17 |
| 32.1. Board Dashboard Deferred Residuals (INSERTED) | v2.1 | 12/15 | In Progress (gap-closure round 2 of 4 planned) |  |
| 33. Live Capture & Windows Portability | v2.1 | 0/TBD | Not started | - |

**Totals:** 33 phases · **4 milestones shipped** (v1.0 + v1.1 + v1.2 + v2.0) · **1 active** (v2.1, phases 27–33).

**v2.0 final:** all 7 phases (20–26) complete and archived 2026-07-28. Audit `tech_debt` — 28/28 requirements satisfied, 7/7 phases verified, 8/8 integration boundaries wired, 7/7 Nyquist compliant, no blockers. Closed as `override_closeout` (Phase 20's Windows-CI human item genuinely open; Phase 25's `unknown` is a frontmatter-parse artifact, not a gap). 11 open artifacts deferred, 9 of them pre-v2.0 carryover.

**v2.1 coverage:** all **46** v2.1 requirements mapped to exactly one phase — 0 unmapped, 0 duplicated. (`REQUIREMENTS.md` prose says "all 41 requirements retained"; the enumerated set is 46. The count was written before the categories were finalized. Fittingly, this is the milestone's own founding defect — a hand-maintained count that drifted from the enumerated reality — caught here by counting instead of trusting.)
